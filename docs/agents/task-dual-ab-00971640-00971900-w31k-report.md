# Dual A/B report — W31-K OWN `aa_00971640` + `aa_00971900`

**Date:** 2026-07-29  
**Agent:** OWN-ONLY dual W31-K  
**Scope:** VAs `0x00971640`, `0x00971900` only. Dual A/B + artifacts.  
**Excluded:** Launcher; WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / parent ledger.  
**Ghidra:** `decompile_function` + `batch_decompile` + `analyze_function_complete` + `get_function_by_address` + `read_memory` (+ callers/callees/xrefs). **No** `disassemble_bytes`.  
**Image:** `autoassault.exe` base `0x400000`.  
**Partition:** `docs/reconstruction/reviews/WAVE_2026-07-29_wave31_partition_map.md` (W31-K).

---

## Verdict

| Unit | Verdict |
|------|---------|
| `aa_00971640` AssPreloader_EnqueueEligibleFromRing_Inferred | **accept-with-gaps** — ESI/RET4 + three-gate enqueue + SetEvent sealed; product method English + nested dual open |
| `aa_00971900` AssPreloader_IsKeyTrackedRecursive_Inferred | **accept-with-gaps** — ECX/RET8/AL + drain + +0x7c contains + optional recurse sealed; nested drain/deps dual open |

---

## VA `0x00971640` — sealed facts

1. **Body:** `0x00971640`–`0x009716f7` (**184 B** / `0xB8`; pad `CC`).
2. **ABI:** **ESI** = source GuardedVector ring; stack **AssPreloader\* host**; epilogue **`RET 4`** (`C2 04 00`); void. Decompiler `unaff_ESI` + void surface — **bytes win**.
3. **Classification:** **worker**.
4. **Semantics:** For each key slot in ESI ring:
   - Tree lower_bound on **host+0x10** (`FUN_0043d5e0`); require iterator == **host+0x14** (absent).
   - Resource lookup via **\*(host+0x110)** (`FUN_0044e8c0`); require cache end **or** `*(node+0xc)==0`.
   - If not `GuardedVector_ContainsFirstEqual` on **host+0x1c**: push via `FUN_0043e4b0` (value copy `*key`), set dirty.
   - If dirty: **`SetEvent(*(HANDLE*)(host+4))`** (IAT `0x009c618c`).
5. **Callers (3):** `FUN_00971700` @ `0x00971773`, `FUN_009717a0` @ `0x009717f9`, `FUN_00971820` @ `0x009718cd` (all build temp ring → ESI, push host).
6. **Callees:** `FUN_0043d5e0`, `FUN_0044e8c0`, `FUN_0043e5b0`, `FUN_0043e4b0`, `SetEvent`.
7. **Name:** `AssPreloader_EnqueueEligibleFromRing_Inferred` (Ghidra `FUN_00971640`; **Inferred** structural; AssPreloader family via sibling `assPreloader.cpp` on `FUN_00971480`). Reject scaffold `Named_CalleeOf_Named_*VOG_DEBUG_ST*` plate.
8. **Decompile ≡ raw** for CF; **bytes supersede** ABI (ESI + RET 4).  
   Full hex: raw W31-K append (184 B).

### Gaps

- Product/PDB method English.  
- Nested tree/cache/`FUN_0043e4b0` product plates (not owned).  
- Runtime / bit-exact / differential.

### Dual A/B

| Review | Path | Verdict |
|---|---|---|
| A | `docs/reconstruction/reviews/A_aa_00971640_AssPreloader_EnqueueEligibleFromRing_Inferred.md` | **accept-with-gaps** |
| B | `docs/reconstruction/reviews/B_aa_00971640_AssPreloader_EnqueueEligibleFromRing_Inferred.md` | **accept-with-gaps** |

### Files

| Kind | Path |
|------|------|
| Raw | `docs/reconstruction/raw/aa_00971640_FUN_00971640.md` |
| Annotated | `docs/reconstruction/raw/aa_00971640_FUN_00971640.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/AssPreloader_EnqueueEligibleFromRing_Inferred.cpp` |
| Clean twin | `docs/reconstruction/reconstructed-exact/FUN_00971640.cpp` |
| Function | `docs/reconstruction/functions/aa_00971640_FUN_00971640.md` |
| Function named | `docs/reconstruction/functions/aa_00971640_AssPreloader_EnqueueEligibleFromRing_Inferred.md` |

---

## VA `0x00971900` — sealed facts

1. **Body:** `0x00971900`–`0x00971a19` (**282 B** / `0x11A`; pad `CC`).
2. **ABI:** **ECX=this** (AssPreloader); stack `(const int* key, char recurse)`; epilogue **`RET 8`** all exits; return **AL** bool. SEH `LAB_009b09cf`.
3. **Classification:** **worker** (self-recursive).
4. **Semantics:**
   - Always **`FUN_00971480(this)`** (drain/process; sibling logs under `assPreloader.cpp`).
   - If **`GuardedVector_ContainsFirstEqual(this+0x7c, key)`** → return 1.
   - If `recurse==0` → return 0.
   - Else: zero local ring; **`FUN_00744360(key, &local)`**; for each child slot recurse with **ECX=this**; any hit → `FUN_005b2ba0` clear + return 1; else clear + return 0.
5. **Callers (2):** `FUN_004ed310` @ `0x004ed38a` (recurse=0; ECX from manager+0x6c), self @ `0x009719cc`.
6. **Callees:** `FUN_00971480`, `FUN_0043e5b0`, `FUN_00744360`, self, `FUN_005b2ba0`.
7. **Name:** `AssPreloader_IsKeyTrackedRecursive_Inferred` (Ghidra `FUN_00971900`; **Inferred**). Reject scaffold VOG_DEBUG plate.
8. **Decompile ≡ raw** for CF; **bytes supersede** recursive ECX + contains base **+0x7c**.  
   Full hex: raw W31-K append (282 B).

### Gaps

- Product/PDB method English.  
- Nested drain / dep-collector dual (not owned).  
- Product labels for +0x7c tracked vs +0x1c pending pairing.  
- Runtime / bit-exact / differential.

### Dual A/B

| Review | Path | Verdict |
|---|---|---|
| A | `docs/reconstruction/reviews/A_aa_00971900_AssPreloader_IsKeyTrackedRecursive_Inferred.md` | **accept-with-gaps** |
| B | `docs/reconstruction/reviews/B_aa_00971900_AssPreloader_IsKeyTrackedRecursive_Inferred.md` | **accept-with-gaps** |

### Files

| Kind | Path |
|------|------|
| Raw | `docs/reconstruction/raw/aa_00971900_FUN_00971900.md` |
| Annotated | `docs/reconstruction/raw/aa_00971900_FUN_00971900.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/AssPreloader_IsKeyTrackedRecursive_Inferred.cpp` |
| Clean twin | `docs/reconstruction/reconstructed-exact/FUN_00971900.cpp` |
| Function | `docs/reconstruction/functions/aa_00971900_FUN_00971900.md` |
| Function named | `docs/reconstruction/functions/aa_00971900_AssPreloader_IsKeyTrackedRecursive_Inferred.md` |

---

## AutoCore impact

| Unit | Port note |
|------|-----------|
| `00971640` | Port with **ESI source ring** + stack host + **ret 4**. Do **not** drop ESI because decompiler said `unaff_ESI`. Eligibility = absent tree **and** unloaded/missing cache **and** not in **host+0x1c**. Signal event only if dirty. |
| `00971900` | Port as **ECX this** + `(key*, recurse)` + **ret 8** + **AL**. Always drain first. Membership vector is **this+0x7c** (not +0x1c). Preserve self-recursion ECX. |
| Pair with | W30-B `GuardedVector_ContainsFirstEqual` (`0x0043e5b0`); sibling drain `FUN_00971480`; callers `FUN_00971700`/`a0`/`820`, `FUN_004ed310`. |

---

## Sibling context (not owned)

| VA | Role |
|---|---|
| `0x00971480` | AssPreloader drain/process (`assPreloader.cpp` strings) |
| `0x0043e5b0` | `GuardedVector_ContainsFirstEqual` (W30-B) |
| `0x0043e4b0` | guarded push (pending +0x1c path) |
| `0x0043d5e0` | tree lower_bound helper |
| `0x0044e8c0` | resource cache lookup by key |
| `0x00744360` | dependency key collector into ring |
| `0x005b2ba0` | GuardedVector clear/free |
| `0x00971700` / `0x009717a0` / `0x00971820` | callers that build temp rings then enqueue |
| `0x004ed310` | primary external caller of is-tracked (recurse=0) |

---

## Process

- OWN VAs only; no Launcher; no parent ledger edits.  
- Seal tools: decompile + analyze_function_complete + get_function_by_address + read_memory (+ xrefs). **No** `disassemble_bytes`.  
- Dual A (fidelity) + B (adversarial) both filed per VA.  
- Scaffold raw bodies preserved; re-verify **appended** only.  
- No invented product plates; structural `_Inferred` names only.  
- Rejected VOG_DEBUG_ST scaffold aliases.  
- Avoided bare `undefined4` in clean (`uint8_t` / `void*` / explicit structs).
