# Dual A/B report — W38-Z OWN `aa_00966760` + `aa_00968680`

**Date:** 2026-08-04  
**Agent:** OWN-ONLY dual W38-Z  
**Scope:** VAs `0x00966760`, `0x00968680` only. Dual A/B + artifacts.  
**Excluded:** Launcher; WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / parent ledger.  
**Ghidra:** `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + xrefs + `get_assembly_context` + parent/callee context. **No** `disassemble_bytes`.  
**Image:** `autoassault.exe` base `0x400000`.  
**Partition:** `docs/reconstruction/reviews/WAVE_2026-08-04_wave38_partition_map.md` (W38-Z).

---

## Verdict

| Unit | Verdict |
|------|---------|
| `aa_00966760` GfxStatsMonitor_RecreateResourceManagerQuery_Inferred | **accept-with-gaps** — EDI host / CreateQuery(RESOURCEMANAGER=5) / out `+0x3d4` / clear `+0x3d8` / always return 0 / 47 B sealed; product class English open |
| `aa_00968680` GfxBufferedViewList_ClearField184_Inferred | **accept-with-gaps** — `DAT_00d1ee30` circular walk / zero payload`+0x184` / plain RET / 40 B sealed; field product English open |

**Terminal coverage:** false (no Launcher / runtime / bit-exact Confirmed).

---

## VA `0x00966760` — sealed facts

1. **Body:** `0x00966760`–`0x0096678e` inclusive (**47 B** / `0x2F`); pad `CC` then sibling release `@0x00966790` (Release query `+0x3d4` + null; not OWN).
2. **ABI:** register **EDI = Stats Monitor host\*** (parent `MOV EDI,[device+0x79c]` / `param_1[0x1e7]`); **no** stack formals; plain **`RET`** (`c3`); returns **always 0** (`xor eax,eax`).
3. **Semantics:** Recreate D3D resource-manager query for Stats Monitor after device Reset:
   - `dev = *DAT_00d1f044` (`IDirect3DDevice9*` via wrapper first field; W35-R publish).
   - `dev->CreateQuery(D3DQUERYTYPE_RESOURCEMANAGER /*5*/, &host->query@+0x3d4)` via vtbl **`+0x1d8`** (method index **118**).
   - Clear flag byte **`host+0x3d8 = 0`**.
   - If HRESULT `< 0`, force **`host+0x3d4 = 0`**.
   - **Do not** propagate CreateQuery HRESULT (always return 0).
4. **Parent product context:** sole caller `FUN_0075eff0` Device Reset (`gfxDevice.cpp`); fail string `"Failed to Reset() device on Stats Monitor"` — **unreachable** given always-0 return (parent still structures the step in the Reset chain after Effect Factory / before Global Effect).
5. **Classification:** leaf (virtual CreateQuery only).
6. **Callers (1):** `FUN_0075eff0` @ `0x0075f313`.
7. **Callees:** `IDirect3DDevice9::CreateQuery` (vcall).
8. **Name:** `GfxStatsMonitor_RecreateResourceManagerQuery_Inferred` (Ghidra `FUN_00966760`; **Inferred** from CreateQuery + parent Stats Monitor string). **Reject** scaffold `Named_CalleeOf_Named_gfxDevice_00966760`.
9. **Decompile ≡ bytes** for CF; **bytes win** on always-0 return and EDI formal. Full hex in raw W38-Z append.

### Gaps

- Product C++ class name for host at `device+0x79c` beyond log "Stats Monitor".  
- Flag `+0x3d8` product English (issued/active/valid).  
- Why CreateQuery failure is swallowed.  
- Runtime / bit-exact / differential.

### Dual A/B

| Review | Path | Verdict |
|---|---|---|
| A | `docs/reconstruction/reviews/A_aa_00966760_GfxStatsMonitor_RecreateResourceManagerQuery_Inferred.md` | **accept-with-gaps** |
| B | `docs/reconstruction/reviews/B_aa_00966760_GfxStatsMonitor_RecreateResourceManagerQuery_Inferred.md` | **accept-with-gaps** |

### Files

| Kind | Path |
|------|------|
| Raw | `docs/reconstruction/raw/aa_00966760_FUN_00966760.md` |
| Annotated | `docs/reconstruction/raw/aa_00966760_FUN_00966760.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/GfxStatsMonitor_RecreateResourceManagerQuery_Inferred.cpp` |
| Clean twin | `docs/reconstruction/reconstructed-exact/FUN_00966760.cpp` |
| Function | `docs/reconstruction/functions/aa_00966760_FUN_00966760.md` |
| Function named | `docs/reconstruction/functions/aa_00966760_GfxStatsMonitor_RecreateResourceManagerQuery_Inferred.md` |

---

## VA `0x00968680` — sealed facts

1. **Body:** `0x00968680`–`0x009686a7` inclusive (**40 B** / `0x28`); pad `CC` then sibling `FUN_009686e0` @ `0x009686b0` (pre-Reset list walk + `FUN_0096c8e0`; not OWN).
2. **ABI:** **no** formals; plain **`RET`** (`c3`); void leaf.
3. **Semantics:** Walk global circular list sentinel **`DAT_00d1ee30`**:
   - Node: `next@+0`, payload*`@+8`.
   - For each node with non-null payload: **`*(payload+0x184) = 0`**.
   - Empty list (head→next == head) early-out.
4. **List domain:** product **gfxBufferedView** registry — ctor `FUN_00968a50` / `GfxBufferedView_Ctor_Inferred` (W37-I) always inserts `this` and initializes **`+0x180/+0x184 = 1`**. This unit clears **`+0x184`** only on Device Reset **success**.
5. **Classification:** leaf.
6. **Callers (1):** `FUN_0075eff0` @ `0x0075f362` (success arm after factories; before GetDeviceCaps rebind).
7. **Callees:** none.
8. **Name:** `GfxBufferedViewList_ClearField184_Inferred` (Ghidra `FUN_00968680`; **Inferred** from list + field). **Reject** scaffold `Named_CalleeOf_Named_gfxDevice_00968680`.
9. **Decompile ≡ bytes**. Full hex in raw W38-Z append.

### Gaps

- Product English for `gfxBufferedView+0x184` (ctor 1 → post-reset 0).  
- Proof every payload is full `gfxBufferedView` (0x198) vs base view.  
- Runtime / bit-exact / differential.

### Dual A/B

| Review | Path | Verdict |
|---|---|---|
| A | `docs/reconstruction/reviews/A_aa_00968680_GfxBufferedViewList_ClearField184_Inferred.md` | **accept-with-gaps** |
| B | `docs/reconstruction/reviews/B_aa_00968680_GfxBufferedViewList_ClearField184_Inferred.md` | **accept-with-gaps** |

### Files

| Kind | Path |
|------|------|
| Raw | `docs/reconstruction/raw/aa_00968680_FUN_00968680.md` |
| Annotated | `docs/reconstruction/raw/aa_00968680_FUN_00968680.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/GfxBufferedViewList_ClearField184_Inferred.cpp` |
| Clean twin | `docs/reconstruction/reconstructed-exact/FUN_00968680.cpp` |
| Function | `docs/reconstruction/functions/aa_00968680_FUN_00968680.md` |
| Function named | `docs/reconstruction/functions/aa_00968680_GfxBufferedViewList_ClearField184_Inferred.md` |

---

## AutoCore impact

| Unit | Port note |
|------|-----------|
| `00966760` | Port as **Stats Monitor post-Reset query recreate**: EDI/host at `device+0x79c`; `IDirect3DDevice9::CreateQuery(RESOURCEMANAGER, &query@+0x3d4)`; clear `+0x3d8`; null query on fail; **return 0 always**. Do **not** treat parent fail-string as live HRESULT path. Device from `*DAT_00d1f044`. |
| `00968680` | Port as **gfxBufferedView list field clear** after successful Device Reset: walk `DAT_00d1ee30` circular list; set each payload`+0x184 = 0`. Do **not** unlink/delete. Do **not** merge with sibling `009686e0` (different gate + callee). |
| Shared | Both consumed only by **`FUN_0075eff0` Device Reset** (`gfxDevice.cpp`) — Stats Monitor recreate is mid-success-chain; buffered-view clear is late success arm. Co-owned only by W38-Z partition pair. |

Shared context (not owned):

| VA | Role |
|---|---|
| `0x0075eff0` | Device Reset parent (`gfxDevice.cpp`) |
| `DAT_00d1f044` | gfx device wrapper publish (W35-R) |
| `DAT_00d1ee30` | gfxBufferedView global list sentinel |
| `0x00968a50` | `GfxBufferedView_Ctor_Inferred` (W37-I) — list insert + `+0x184=1` |
| `0x00966790` | Sibling query Release + null |
| `0x009686e0` | Sibling pre-Reset list walk |
| `0x00968720` | gfxBufferedViews factory Reset (parent chain) |

Closes W38-Z OWN pair: Stats Monitor CreateQuery recreate after Reset + global gfxBufferedView list clear of field `+0x184` on Reset success.

---

## Process

- OWN VAs only; no Launcher; no parent ledger edits.  
- Seal tools: decompile + read_memory + function/xref helpers + assembly context. **No** `disassemble_bytes`.  
- Dual A (fidelity) + B (adversarial) both filed per VA.  
- Scaffold raw bodies preserved; re-verify **appended** only.  
- No invented product names; `_Inferred` structural names; Stats Monitor English from parent log string only.  
- Rejected long Named_CalleeOf scaffold aliases.  
- Clean avoids bare `undefined4` (`uint32_t` / explicit structs).
