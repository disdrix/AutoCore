# Dual A/B report — W18-J OWN-ONLY (`0x00536740`, `0x00538200`)

**Date:** 2026-07-29  
**Agent:** OWN-ONLY dual W18-J  
**Scope:** OWN ONLY VAs `0x00536740`, `0x00538200`. Dual A/B + artifacts.  
**Excluded:** Launcher; WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / parent ledger.  
**Ghidra:** `decompile_function` + `read_memory` only (no `disassemble_bytes`).  
**Image:** `autoassault.exe` base `0x400000`.

---

## Verdicts

| Unit | Canonical | Verdict |
|------|-----------|---------|
| `aa_00536740` | `CVOGCharacter_CompleteRegionMissions` | **accept-with-gaps** — region/pocket bulk → prereq `(1,1)` sealed |
| `aa_00538200` | `CNDHash_ReclaimAll_ValueOwning_009cefec` | **accept-with-gaps** — owning bucket reclaim + freelist stamp sealed |

---

## `aa_00536740` — CVOGCharacter_CompleteRegionMissions

### Sealed facts

1. **ABI:** `__thiscall` — **ECX** = character; stack **region**, **pocket**; **`ret 8`**; always **return 1**.  
   Boundary: `0x00536740` … `0x0053691f` (`c2 08 00`).

2. **Phase 1:** lock global mission-def hash via `FUN_0053fff0` (`Mission_EnsureRegionMissions` / `DAT_00b041fc`), snapshot all defs into temp list, unlock `hash+0x1d`.

3. **Filters (per def):**
   - `def+0x128 == region`
   - `pocket == 0xFFFFFFFF` **OR** `def+0x12c == pocket`
   - `u16 def+0x90 == u8 game+0x532`
   - `u16 def+0x92 == 0xFFFF` **OR** `== u8 game+0x531`
   - game blob via nested `*( *( *(this+4)+4 ) + this + 0xac ) + 0x3c`

4. **Completed skip:** walk `char+0x538`; if key match and `node+8 != 0` → skip.

5. **Cascade:** `CVOGCharacter_CheckMissionPrerequisites(this, id, 1, 1)` — fire-and-forget.

6. **Sole caller:** `0x009544aa` (client debug chat ~`0x00954400`); toast `"Completing region: %i, pocket: %i"` @ `0x00a28a18`.

7. **Not** `SearchAutoMissions` (`0x00532b60`) — different filter + award path.

8. **Live decompile body ≡ 2026-07-23 raw surface.**

### Gaps

1. Product PDB symbol exact string.  
2. Mission.Read English for `+0x128`/`+0x12c` (region/pocket Probable via toast).  
3. Product names for `game+0x531` / `+0x532`.  
4. Full outer debug-command dual.  
5. Runtime / bit-exact.

### Files

| Kind | Path |
|------|------|
| Review A | `docs/reconstruction/reviews/A_aa_00536740_CVOGCharacter_CompleteRegionMissions.md` |
| Review B | `docs/reconstruction/reviews/B_aa_00536740_CVOGCharacter_CompleteRegionMissions.md` |
| Function record | `docs/reconstruction/functions/aa_00536740_FUN_00536740.md` |
| Clean | `docs/reconstruction/reconstructed-exact/CVOGCharacter_CompleteRegionMissions.cpp` |
| Scaffold clean | `docs/reconstruction/reconstructed-exact/FUN_00536740.cpp` |
| Raw (authoritative + live) | `docs/reconstruction/raw/aa_00536740_FUN_00536740.md` |
| Annotated | `docs/reconstruction/raw/aa_00536740_FUN_00536740.annotated.md` |

---

## `aa_00538200` — CNDHash_ReclaimAll_ValueOwning_009cefec

### Sealed facts

1. **ABI:** `__thiscall` hash in **ECX**; no stack args; bare **`c3`**.  
   Boundary: `0x00538200` … `0x00538261`.

2. **Role:** owning full-bucket reclaim for node freelist stamp **`009cefec`**:
   - for `i = 0 .. mask(+0x08)` **inclusive**
   - walk chain; stamp vtbl; `operator_delete(node+8)` if set; freelist push at `this+0x20`; clear bucket head

3. **Does not:** zero `count(+0x0c)`; free table `+0x10`; free freelist slab; call scalar dtor `FUN_00537550`.

4. **Decompiler gap:** `operator_delete` “does not return” — **FALSE** (bytes continue after `add esp,4`).

5. **Sole direct caller:** `CNDHash_FreeBuckets_Owning_009cfa88` `0x00539110` @ `0x0053911a`.

6. **Family:** remove `0x00538000`, node dtor `0x00537550`, recreate `0x00539e30` (pending / `009cfa88`).

7. **Live decompile body ≡ 2026-07-23 raw surface.**

### Gaps

1. Product FreeBuckets/Reclaim English names.  
2. Exhaustive ownership proof for all `009cefec` hash users.  
3. Runtime / bit-exact.

### Files

| Kind | Path |
|------|------|
| Review A | `docs/reconstruction/reviews/A_aa_00538200_CNDHash_ReclaimAll_ValueOwning_009cefec.md` |
| Review B | `docs/reconstruction/reviews/B_aa_00538200_CNDHash_ReclaimAll_ValueOwning_009cefec.md` |
| Function record | `docs/reconstruction/functions/aa_00538200_FUN_00538200.md` |
| Clean | `docs/reconstruction/reconstructed-exact/CNDHash_ReclaimAll_ValueOwning_009cefec.cpp` |
| Scaffold clean | `docs/reconstruction/reconstructed-exact/FUN_00538200.cpp` |
| Raw (authoritative + live) | `docs/reconstruction/raw/aa_00538200_FUN_00538200.md` |
| Annotated | `docs/reconstruction/raw/aa_00538200_FUN_00538200.annotated.md` |

Absolute roots: `C:\Users\josh\Documents\GitHub\AutoCore\` + paths above.

---

## AutoCore impact

- **CompleteRegionMissions:** client **debug** bulk path only (sole xref). Server auto-grant remains `SearchAutoMissions` / Requirements / GiveMission — do not wire production mission grant to this debug driver. Region (`def+0x128`) / pocket (`def+0x12c`) filters and completed-hash skip at `char+0x538` are useful layout seals for mission tools.
- **CNDHash reclaim `009cefec`:** confirms owning teardown for pending-objective-style hashes; FreeBuckets zeros count then reclaims then frees table. Server mirrors of owning maps must delete values before freelist recycle — non-owning twin is a different stamp (`009cf02c`).
