# Dual A/B report — W30-H OWN `aa_00565950` + `aa_00518370`

**Date:** 2026-07-29  
**Agent:** OWN-ONLY dual W30-H  
**Scope:** VAs `0x00565950`, `0x00518370` only. Dual A/B + artifacts.  
**Excluded:** Launcher; WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / parent ledger.  
**Ghidra:** `decompile_function` + `analyze_function_complete` + `get_function_by_address` + `read_memory` (+ callers/callees/xrefs). **No** `disassemble_bytes`.  
**Image:** `autoassault.exe` base `0x400000`.

---

## Verdict

| Unit | Verdict |
|------|---------|
| `aa_00565950` CVOGSpawnPoint_ActivateFromStream_Inferred | **accept-with-gaps** — stream unpack + range clamp 75.0f + version gates + MI thunk; field atlas residual |
| `aa_00518370` Object_SetMissionStateFx_Inferred | **accept** — state@+0x154, secondary teardown+bit2, events 9/10/11 vs audio-only >2 sealed |

---

## VA `0x00565950` — sealed facts

1. **Body:** `0x00565950`–`0x00566485` (**2870 B / `0xB36`**); pad `CC` after **`ret 0x10`**.
2. **ABI:** **thiscall** — ECX = spawn-point **MI subobject** (negative field offsets); stack cleaned **16 B**; returns **0** if stream null else **1**.
3. **Semantics:** Versioned stream unpack via `stream->vtbl+0x14`; add world float4 offset into position; clamp activation range ≤0 to **`DAT_009d2eb8` = 75.0f** with debug string `******BAD SPAWN POINT ACTIVATION RANGE: %d %f`; optional linked spawns; race/slot maps via `FUN_00567860` InsertAlways (W29-M).
4. **Classification:** **worker** (large); not leaf.
5. **Entry:** sole direct CODE xref from MI this-adjustor thunk **`0x00567CBE`** (`sub ecx,[ecx-4]; jmp body`) — vtable-facing.
6. **Name:** `CVOGSpawnPoint_ActivateFromStream_Inferred` (Ghidra `FUN_00565950`; prior string-only plate too narrow). Neighbors: `CVOGSpawnPoint_CreateCreature` / `SetObjectActiveState` / `CreateTemplateVehicle`.
7. **Decompile ≡ bytes** for CF (live 2026-07-29 ≡ raw 2026-07-23); SEH frame stack alias noise does not alter spine.

### Gaps

- Full product field / version schema atlas.  
- Fourth stack formal role under `ret 0x10`.  
- Nested spawn helper product English.  
- Runtime / bit-exact.

### Dual A/B

| Review | Path | Verdict |
|---|---|---|
| A | `docs/reconstruction/reviews/A_aa_00565950_CVOGSpawnPoint_ActivateFromStream_Inferred.md` | **accept-with-gaps** |
| B | `docs/reconstruction/reviews/B_aa_00565950_CVOGSpawnPoint_ActivateFromStream_Inferred.md` | **accept-with-gaps** |

### Files

| Kind | Path |
|------|------|
| Raw | `docs/reconstruction/raw/aa_00565950_FUN_00565950.md` |
| Annotated | `docs/reconstruction/raw/aa_00565950_FUN_00565950.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/CVOGSpawnPoint_ActivateFromStream_Inferred.cpp` |
| Clean twin | `docs/reconstruction/reconstructed-exact/FUN_00565950.cpp` |
| Function | `docs/reconstruction/functions/aa_00565950_FUN_00565950.md` |
| Function named | `docs/reconstruction/functions/aa_00565950_CVOGSpawnPoint_ActivateFromStream_Inferred.md` |

---

## VA `0x00518370` — sealed facts

1. **Body:** `0x00518370`–`0x00518580` (**529 B / `0x211`**); pad `CC` after **`ret 4`**.
2. **ABI:** **thiscall** — ECX = object host; stack `int newState`; **`ret 4`**.
3. **Semantics:** Write state `@+0x154`; gate on clonebase FX master + vtbl+`0x1CC` linked; if same state and `@+0x184` bit2 → return; `Object_TeardownSecondaryFxList_Inferred(1)` then **set** bit2; state **>2** → complete audio only; states **0/1/2** → FxMaster events **9/10/11**, attach vtbl+`0xF8`, `Vector_PushDword` (Ghidra name `CVOGReaction_FailMissionNotify`), audio, `FUN_004b7e50`.
4. **Classification:** **worker**.
5. **CODE callers (5):** `CVOGReaction_SpawnObject` / `FUN_005825d0` / `FUN_00582670` (state **3**); `FUN_005c8270` / `FUN_005c8580` (progress → 0..2).
6. **Name:** `Object_SetMissionStateFx_Inferred` (Ghidra `FUN_00518370`; prior SpawnObject-only alias too narrow).
7. **Decompile ≡ bytes** for CF (live 2026-07-29 ≡ raw 2026-07-23).

### Gaps

- Product English / state enum.  
- Exact roles of `FUN_004b7e50` / `FUN_00723b20`.  
- Runtime / bit-exact.

### Dual A/B

| Review | Path | Verdict |
|---|---|---|
| A | `docs/reconstruction/reviews/A_aa_00518370_Object_SetMissionStateFx_Inferred.md` | **accept** |
| B | `docs/reconstruction/reviews/B_aa_00518370_Object_SetMissionStateFx_Inferred.md` | **accept** |

### Files

| Kind | Path |
|------|------|
| Raw | `docs/reconstruction/raw/aa_00518370_FUN_00518370.md` |
| Annotated | `docs/reconstruction/raw/aa_00518370_FUN_00518370.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/Object_SetMissionStateFx_Inferred.cpp` |
| Clean twin | `docs/reconstruction/reconstructed-exact/FUN_00518370.cpp` |
| Function | `docs/reconstruction/functions/aa_00518370_FUN_00518370.md` |
| Function named | `docs/reconstruction/functions/aa_00518370_Object_SetMissionStateFx_Inferred.md` |

---

## AutoCore impact

| Unit | Port note |
|------|-----------|
| `00565950` | Client/sector spawn-point activation must version-gate stream fields, add world offset to position, clamp bad range to **75.0f**, and use **InsertAlways** (not insert-or-find) for race/slot maps. Enter via MI this-adjust when calling from vtable. Do not flatten to CreateCreature-only. |
| `00518370` | Mission/progress FX: write `+0x154`, always teardown secondary then re-arm bit2 on `+0x184`. State **3+** is **audio-only**; only 0/1/2 load events 9/10/11. Do not treat `CVOGReaction_FailMissionNotify` as mission-fail — it is Vector_PushDword. Closes residual caller left open by W29-N teardown dual. |

---

## Process

- OWN VAs only; no Launcher; no parent ledger edits.  
- Seal tools: decompile + analyze_function_complete + get_function_by_address + read_memory (+ xrefs/callers/callees). **No** `disassemble_bytes`.  
- Dual A (fidelity) + B (adversarial) both filed per VA.  
- Scaffold raw bodies preserved; W30-H re-verify **appended**.  
- No invented product names without evidence; `_Inferred` structural names only.  
- Related nested seals used read-only: W29-N (`00516930`), W29-M (`00567860`), FxMaster (`004a16d0`).  
- Pattern: `docs/agents/task-dual-ab-00516930-00517250-w29n-report.md`.
