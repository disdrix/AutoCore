# Dual A/B report — W29-S OWN `aa_00560020` + `aa_00560750`

**Date:** 2026-07-29  
**Agent:** OWN-ONLY dual W29-S  
**Scope:** VAs `0x00560020`, `0x00560750` only. Dual A/B + artifacts.  
**Excluded:** Launcher; WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / parent ledger.  
**Ghidra:** `decompile_function` / `batch_decompile` + `read_memory` + `get_function_by_address` + xrefs/callers/callees + `get_assembly_context` + `analyze_function_complete`. **No** `disassemble_bytes`.  
**Image:** `autoassault.exe` base `0x400000`.  
**Partition:** `docs/reconstruction/reviews/WAVE_2026-07-29_wave29_partition_map.md` → W29-S  

---

## Verdict

| Unit | Verdict |
|------|---------|
| `aa_00560020` WorldObj_UnregisterAndRelease_Inferred | **accept-with-gaps** — unenroll CF/ABI/`ret 4`/list+refcount sealed; product English + nested `0062d960`/`0062a510` open |
| `aa_00560750` PhysHost_MergeSortedPairList_Inferred | **accept-with-gaps** — EAX dest + stack src merge CF sealed; equal-key vcalls sealed; PUSH EBP residual + product types open |

---

## VA `0x00560020` — sealed facts

1. **Body:** `0x00560020`–`0x0056010c` (**237 B** / `0xED`); pad `CC` after `ret 4`.
2. **ABI:** **thiscall**; ECX = **manager**; stack = **object***; void; **`ret 4`**.
3. **CF:** lock `0062d960(*(mgr+0x234))` → extract unbind pairs via `*(mgr+0xc4)->vtbl+0xc` → optional `006cad30` + `006caaa0` (ECX=`mgr+0x138`, empty left range) → `0062a510` → refcount-- at `obj+6`, clear `obj+0x8`, destroy if 0 → swap-remove from `mgr+0x120`/`+0x124` → `0055f4c0`.
4. **Twin (not OWN):** enroll `FUN_0055ff20` (vtbl+4 extract, ref++, push list, `0062a4d0`).
5. **Not** `ActionManager_UnregisterAndRelease` (`0055df40`) — different list/helpers.
6. **Callers (8):** `004e9720`, `004ea350`, `004c4660`, `009302b0`, `00562680`, `005db1b0`, `00609f00`, `0064ce20` (manager often `world+0xE4A4`).
7. **Name:** `WorldObj_UnregisterAndRelease_Inferred` (Ghidra `FUN_00560020`).
8. **Decompile ≡ bytes** for CF; machine fixes: `unaff_*` = extract out-buffer; `006caaa0` thiscall.

### Gaps

- Product English for manager / object.  
- Nested duals of `0062d960`, `0062a510`.  
- Full extract out-buffer map.  
- Some callers also touch `obj+6` after return (local ref pattern).  
- Runtime / bit-exact — open.

### Dual A/B

| Review | Path | Verdict |
|---|---|---|
| A | `docs/reconstruction/reviews/A_aa_00560020_WorldObj_UnregisterAndRelease_Inferred.md` | **accept-with-gaps** |
| B | `docs/reconstruction/reviews/B_aa_00560020_WorldObj_UnregisterAndRelease_Inferred.md` | **accept-with-gaps** |

### Files

| Kind | Path |
|------|------|
| Raw | `docs/reconstruction/raw/aa_00560020_FUN_00560020.md` |
| Annotated | `docs/reconstruction/raw/aa_00560020_FUN_00560020.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/WorldObj_UnregisterAndRelease_Inferred.cpp` |
| Clean twin | `docs/reconstruction/reconstructed-exact/FUN_00560020.cpp` |
| Function | `docs/reconstruction/functions/aa_00560020_FUN_00560020.md` |
| Function named | `docs/reconstruction/functions/aa_00560020_WorldObj_UnregisterAndRelease_Inferred.md` |

---

## VA `0x00560750` — sealed facts

1. **Body:** `0x00560750`–`0x00560a2c` (**733 B** / `0x2DD`); pad `CC` after plain `ret`.
2. **ABI:** custom — **EAX = dest** pair-list (`host+0x68`); **stack = src** pair-list; void; plain **`ret`** (caller `ADD ESP,4`).
3. **CF:** `LocalWorklist16_Init(dest.count)` → copy dest → grow dest for sum → dual-key sorted merge → equal keys: `entry[2]->vtbl+0x14` then partner `vtbl+0x10` → final count `(dest_n - equals) + src.count` → drain tails → inline worklist release on `DAT_00b05060`.
4. **Record:** stride **0x10**; keys at `*(+0)+4` and `*(+4)+4`.
5. **Caller (1):** `PhysSim_MergeHosts_Inferred` (`00560a30`) @ `0x00560a6c` — closes W20-B open dual of this VA.
6. **Callees:** `0055f520`, `005b3300` (elem 0x10), arena vcalls.
7. **Name:** `PhysHost_MergeSortedPairList_Inferred` (Ghidra `FUN_00560750`).
8. **Decompile ≡ bytes** for CF; `in_EAX` is real dest formal (not phantom).

### Gaps

- Product English for pair-record / absorb virtuals.  
- Record `+0x0C` field meaning.  
- Sole-caller `PUSH EBP` residual (function reads only Stack[0x4]).  
- Runtime / bit-exact — open.

### Dual A/B

| Review | Path | Verdict |
|---|---|---|
| A | `docs/reconstruction/reviews/A_aa_00560750_PhysHost_MergeSortedPairList_Inferred.md` | **accept-with-gaps** |
| B | `docs/reconstruction/reviews/B_aa_00560750_PhysHost_MergeSortedPairList_Inferred.md` | **accept-with-gaps** |

### Files

| Kind | Path |
|------|------|
| Raw | `docs/reconstruction/raw/aa_00560750_FUN_00560750.md` |
| Annotated | `docs/reconstruction/raw/aa_00560750_FUN_00560750.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/PhysHost_MergeSortedPairList_Inferred.cpp` |
| Clean twin | `docs/reconstruction/reconstructed-exact/FUN_00560750.cpp` |
| Function | `docs/reconstruction/functions/aa_00560750_FUN_00560750.md` |
| Function named | `docs/reconstruction/functions/aa_00560750_PhysHost_MergeSortedPairList_Inferred.md` |

---

## AutoCore impact

| Unit | Port note |
|------|-----------|
| `00560020` | Object teardown / unenroll must run **full** unregister (pair reverse + list remove + refcount), not only destroy. Distinct from `ActionManager_UnregisterAndRelease` (`0055df40`). Preserve **ECX=manager**, **`ret 4`**, optional pair path when extract count ≠ 0. |
| `00560750` | Host island merge must **sorted-merge** `host+0x68` lists with equal-key absorb — **not** append. Stage **dest in EAX**, **src on stack**. Skipping absorb leaks pair/constraint objects across merged islands. |

---

## Sibling context (not owned)

| VA | Role |
|---|---|
| `0x0055ff20` | Enroll twin of `00560020` |
| `0x0055df40` | `ActionManager_UnregisterAndRelease_Inferred` (different) |
| `0x006cad30` | `LinkPair_CanonAndSort_Inferred` (W28-Q) |
| `0x006caaa0` | `LinkPair_SortedDiffWalk_Inferred` (W27-N) |
| `0x0055f4c0` | `LocalWorklist_Release_Inferred` (W27-N) |
| `0x0055f520` | `LocalWorklist16_Init_Inferred` (W28-R) |
| `0x005b3300` | `HBBuffer_GrowRealloc_Inferred` |
| `0x00560a30` | `PhysSim_MergeHosts_Inferred` (sole caller of `00560750`) |
| `0x0062d960` / `0x0062a510` | lock / notify helpers used by unregister |

---

## Process

- OWN VAs only; no Launcher; no parent ledger edits.  
- Seal tools: decompile + read_memory (+ function metadata / xrefs / assembly context). **No** `disassemble_bytes`.  
- Dual A (fidelity) + B (adversarial) both filed per VA.  
- Scaffold raw bodies preserved; re-verify **appended** (W29-S sections).  
- No invented product names — `_Inferred` structural names only.  
- Avoided bare `undefined4` in clean (used `uint32_t` / typed structs).

---

## Report path

`docs/agents/task-dual-ab-00560020-00560750-w29s-report.md`
