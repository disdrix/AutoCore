# Dual A/B report — W22-B OWN-ONLY (`aa_00568200`, `aa_00518940`)

**Date:** 2026-07-29  
**Agent:** W22-B OWN-ONLY  
**Scope:** OWN ONLY VAs `0x00568200`, `0x00518940`. Dual A/B + artifacts.  
**Excluded:** Launcher; WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / other parent ledgers.  
**Ghidra:** `decompile_function` + `read_memory` only. No `disassemble_bytes`.  
**Image:** `autoassault.exe` base `0x400000`.

---

## Verdict

| Unit | Verdict |
|------|---------|
| `aa_00568200` StdTree_IteratorIncrement_Isnil49 | **accept** — tree iterator++ / successor; is_nil @ `+0x49`; 74 B; multi-caller (not journal id-fill) |
| `aa_00518940` Object_SharedBase_Ctor | **accept** — shared dual-base object-core field ctor; TFID/CS/dirty/counter sealed; type-dependent ECX embeds |

---

## Sealed facts — `0x00568200`

1. **ABI:** `__fastcall` ECX = iterator (`node**`); epilogue **`ret`** (`c3`). Body **`0x00568200`–`0x0056824a`** (**74 bytes**).

2. **Role:** MSVC red-black / map **tree successor** (`_Tree_iterator::operator++`) in place.

3. **Node layout:** `+0` left, `+4` parent, `+8` right, **`+0x49` is_nil**. Twin algorithm of `aa_004cb270` (is_nil @ **`+0x29`**).

4. **CF:** If current is_nil → no-op. Else if right non-nil → leftmost of right subtree. Else climb parents while current was right-child; store parent (may be nil head / end).

5. **Bytes:** All is_nil guards use displacement **`0x49`** (`80 xx 49 00`); both exits `c3`.

6. **Callers:** `Client_UpdateMissionJournal` (`008ae130`), `FUN_00538780`, `FUN_00568c60`, `FUN_00569560` (insert-with-hint successor). **No callees.**

7. **Falsified prior:** Journal id-vector fill — body has no vector push; leaf link walk only.

8. **Name:** `StdTree_IteratorIncrement_Isnil49` — role-sealed **INFERRED**. Ghidra: `FUN_00568200`.

---

## Sealed facts — `0x00518940`

1. **ABI:** `__thiscall`/`__fastcall` ECX = **object core** `this`; returns `this` (ESI→EAX); epilogue SEH teardown + **`ret`** (no stack formals). Body **`0x00518940`–`0x00518c14`** (**724 bytes**).

2. **Role:** Shared **dual-base / virtual-base field constructor** for client object core. Outer most-derived ctors LEA ECX to the core subobject at **type-dependent offsets**.

3. **Embed samples (image LEA):** Creature `+0x500`; Vehicle `+0x670`; `FUN_005c82a0` `+0x110`; `FUN_005d4830` `+0xe0`; intermediate `FUN_0051e7d0` `+0x40`.

4. **TFID:** Copies `g_abTfidInvalid_9CDF88` (`FFFFFFFF FFFFFFFF 0 0`) to **`+0x40`** and **`+0x160`**.

5. **Float:** `+0xb8 = g_flOne` (`1.0f` @ `0x00a0f2a0`).

6. **Flags:** `+0x17c = (x & 0xffe22210) | 0x22210`; then **`FUN_00512670`** (`Object_PropagateDirtyBit0x20000`) ORs **`0x20000`** (and walks empty `+0xac` chain).

7. **CS helper:** `operator_new(0x2c)` → stage vtbl `009ce07c` → `InitializeCriticalSection(+4)` → final vtbl `009ce154` → store at **`+0xb0`** (null on OOM).

8. **Counter:** `DAT_00b04200++`; if first instance, zero `DAT_00b041f0..f8`.

9. **Does not** install Creature/Vehicle primary vtbls — outer ctors own those.

10. **Name:** `Object_SharedBase_Ctor` — role-sealed **INFERRED**. Ghidra: `FUN_00518940`. Prior GiveItem-callee name incomplete.

---

## Gaps

### `00568200`
1. Product English for each map value type using is_nil@+0x49.  
2. Color bit offset unused here.  
3. Runtime / bit-exact / differential — open.

### `00518940`
1. Product C++ class string (CVOGObject vs lower mixin).  
2. Full field English dictionary; bit meanings inside default `0x22210` beyond dirty `0x20000`.  
3. Runtime / bit-exact / differential — open.  
4. OOM null CS helper policy per caller — parent-dependent.

---

## Files

### `aa_00568200`

| Kind | Path |
|------|------|
| Review A | `docs/reconstruction/reviews/A_aa_00568200_StdTree_IteratorIncrement_Isnil49.md` |
| Review B | `docs/reconstruction/reviews/B_aa_00568200_StdTree_IteratorIncrement_Isnil49.md` |
| Function record | `docs/reconstruction/functions/aa_00568200_StdTree_IteratorIncrement_Isnil49.md` |
| Scaffold record | `docs/reconstruction/functions/aa_00568200_FUN_00568200.md` |
| Clean | `docs/reconstruction/reconstructed-exact/StdTree_IteratorIncrement_Isnil49.cpp` |
| Scaffold clean | `docs/reconstruction/reconstructed-exact/FUN_00568200.cpp` |
| Raw (authoritative body + re-verify append) | `docs/reconstruction/raw/aa_00568200_FUN_00568200.md` |
| Annotated | `docs/reconstruction/raw/aa_00568200_FUN_00568200.annotated.md` |
| Scratch | `tmp/a_00568200.md` |

### `aa_00518940`

| Kind | Path |
|------|------|
| Review A | `docs/reconstruction/reviews/A_aa_00518940_Object_SharedBase_Ctor.md` |
| Review B | `docs/reconstruction/reviews/B_aa_00518940_Object_SharedBase_Ctor.md` |
| Function record | `docs/reconstruction/functions/aa_00518940_Object_SharedBase_Ctor.md` |
| Scaffold record | `docs/reconstruction/functions/aa_00518940_FUN_00518940.md` |
| Clean | `docs/reconstruction/reconstructed-exact/Object_SharedBase_Ctor.cpp` |
| Scaffold clean | `docs/reconstruction/reconstructed-exact/FUN_00518940.cpp` |
| Raw (authoritative body + re-verify append) | `docs/reconstruction/raw/aa_00518940_FUN_00518940.md` |
| Annotated | `docs/reconstruction/raw/aa_00518940_FUN_00518940.annotated.md` |
| Scratch | `tmp/a_00518940.md` |

### Report

| Kind | Path |
|------|------|
| This report | `docs/agents/task-dual-ab-00568200-00518940-w22b-report.md` |

---

## AutoCore impact

### `00568200`
- Port as pure **tree iterator ++** with is_nil at **`+0x49`** (not `+0x29` twin).
- Mission journal and map insert-with-hint sites call this only to **step** trees; do not fold id collection or rebalance into this unit.
- Prefer one shared helper with a nil-offset parameter if both +0x29 and +0x49 families are hosted.

### `00518940`
- Port as **object-core subobject ctor**, not complete-object Creature/Vehicle ctor.
- Outer most-derived construction must pass the correct embed offset (`+0x500` creature, `+0x670` vehicle, etc.).
- Preserve dirty default (`0x20000` via `Object_PropagateDirtyBit0x20000`) and invalid TFID sentinels.
- Server may omit CS helper if no client lock parity required — document if skipped.

---

## Process

- OWN VAs only; no Launcher; no parent ledger edits.  
- Seal tools: `decompile_function` + `read_memory` (+ bounds/callers/callees/xrefs).  
- Dual A (fidelity) + B (adversarial) both filed per VA.  
- Scaffold raw bodies preserved; re-verify **appended** only.  
- Clean sources avoid bare `undefined4` (`uint32_t` / `uint8_t` / `int32_t`).
