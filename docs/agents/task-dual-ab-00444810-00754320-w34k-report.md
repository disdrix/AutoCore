# Dual A/B report — W34-K OWN `aa_00444810` + `aa_00754320`

**Date:** 2026-07-29  
**Agent:** OWN-ONLY dual W34-K  
**Scope:** VAs `0x00444810`, `0x00754320` only. Dual A/B + artifacts.  
**Excluded:** Launcher; WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / parent ledger.  
**Ghidra:** `decompile_function` + `analyze_function_complete` + `read_memory` + xrefs. **No** `disassemble_bytes`.  
**Image:** `autoassault.exe` base `0x400000`.  
**Partition:** `docs/reconstruction/reviews/WAVE_2026-07-29_wave34_partition_map.md` (W34-K).

---

## Verdict

| Unit | Verdict |
|------|---------|
| `aa_00444810` StringVecShell_TidyAndClearGlobal | **accept** — ECX shell tidy + `DAT_00d1f1fc` clear sealed (58 B); SEH-only Palantir unwind |
| `aa_00754320` Palantir_CompleteDtor | **accept-with-gaps** — complete-dtor stage order/ABI/globals + product class sealed; nested `+0x10`/`+0x30` duals open |

---

## VA `0x00444810` — sealed facts

1. **Body:** `0x00444810`–`0x0044484A` exclusive (**58 B** / `0x3A`; pad `CC` then `FUN_00444850`).
2. **ABI:** **ECX=`StringVecShell*`**; bare **`RET`** (`C3`); void. SEH `LAB_009bd525`. ECX preserved through SEH prolog into tidy.
3. **Semantics:**
   - `StringVec_Tidy(ECX)` (sealed W33-D `0x00431ae0` — destroy stride `0x1c`, free, null triad).
   - `DAT_00d1f1fc = 0` (drop published path-list shell pointer).
4. **Classification:** worker — one callee.
5. **Callers (2, SEH only):** `Unwind@009afd09` / `Unwind@009afd4a` — both `ECX = frame_this + 0xA0` then **`JMP`** here (Palantir complete-dtor SEH table `LAB_009afd1c`).
6. **Name:** `StringVecShell_TidyAndClearGlobal` (Ghidra `FUN_00444810`; **Inferred** structural). Not equal to bare `StringVec_Tidy` (extra global clear).
7. **Decompile ≢ bytes** on ABI (void / no formal); **bytes + SEH sites win**. Full hex: raw W34-K append.

### Gaps

- Product English for path-list role.  
- Runtime / bit-exact.

### Dual A/B

| Review | Path | Verdict |
|---|---|---|
| A | `docs/reconstruction/reviews/A_aa_00444810_StringVecShell_TidyAndClearGlobal.md` | **accept** |
| B | `docs/reconstruction/reviews/B_aa_00444810_StringVecShell_TidyAndClearGlobal.md` | **accept** |

### Files

| Kind | Path |
|------|------|
| Raw | `docs/reconstruction/raw/aa_00444810_FUN_00444810.md` |
| Annotated | `docs/reconstruction/raw/aa_00444810_FUN_00444810.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/StringVecShell_TidyAndClearGlobal.cpp` |
| Clean twin | `docs/reconstruction/reconstructed-exact/FUN_00444810.cpp` |
| Function | `docs/reconstruction/functions/aa_00444810_FUN_00444810.md` |
| Function named | `docs/reconstruction/functions/aa_00444810_StringVecShell_TidyAndClearGlobal.md` |
| Scratch | `docs/reconstruction/tmp/a_00444810.md` |

---

## VA `0x00754320` — sealed facts

1. **Body:** `0x00754320`–`0x007543A9` exclusive (**137 B** / `0x89`; pad `CC` then `FUN_007543b0`).
2. **ABI:** **ECX=`Palantir*`** (`MOV ESI,ECX`); no stack formals; bare **`RET`** (`C3`); void. **Does not** `operator_delete(this)`.
3. **Semantics (complete dtor):**
   - Install dtor-phase vtbl `PTR_FUN_00a9f76c`.
   - Destroy `basic_string` at **`this+0xB4`** (IAT `0x009c62f4`).
   - `StringVec_Tidy(this+0xA0)`; **`DAT_00d1f1fc = 0`**.
   - `FUN_0075d8e0(this+0x30)` — **cdecl stack** 1-arg.
   - `FUN_00964d00(this+0x10)` — **cdecl stack** 1-arg.
   - **`DAT_00d1f058 = 0`** (host / Palantir singleton global).
4. **Related:**
   - Scalar deleting dtor `FUN_004447e0` = call this + optional `operator_delete` if `flags&1` (`RET 4`) — vtbl[0].
   - Vtbl[1] `FUN_00754480` logs `Palantir.cpp:0x184` / `"Palantir::ResetDevice failed..."` → **product class Palantir sealed**.
   - SEH unwind reuses `0x00444810` for path-list phase.
5. **Classification:** worker.
6. **Callers:** `FUN_004447e0` @ `0x004447e3`; `Unwind@009b0038`, `Unwind@009b0065`.
7. **Callees:** IAT `~basic_string`, `StringVec_Tidy`, `FUN_0075d8e0`, `FUN_00964d00`.
8. **Name:** `Palantir_CompleteDtor` (Ghidra `FUN_00754320`; **Inferred** + product evidence).
9. **Decompile ≢ bytes** on SEH restore / ECX for tidy / nested calling convention; **bytes win**. Full hex: raw W34-K append.

### Gaps

- Nested duals for `FUN_0075d8e0` / `FUN_00964d00` (member types at `+0x30` / `+0x10`).  
- Exact MSVC demangle plate.  
- Runtime / bit-exact.

### Dual A/B

| Review | Path | Verdict |
|---|---|---|
| A | `docs/reconstruction/reviews/A_aa_00754320_Palantir_CompleteDtor.md` | **accept-with-gaps** |
| B | `docs/reconstruction/reviews/B_aa_00754320_Palantir_CompleteDtor.md` | **accept-with-gaps** |

### Files

| Kind | Path |
|------|------|
| Raw | `docs/reconstruction/raw/aa_00754320_FUN_00754320.md` |
| Annotated | `docs/reconstruction/raw/aa_00754320_FUN_00754320.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/Palantir_CompleteDtor.cpp` |
| Clean twin | `docs/reconstruction/reconstructed-exact/FUN_00754320.cpp` |
| Function | `docs/reconstruction/functions/aa_00754320_FUN_00754320.md` |
| Function named | `docs/reconstruction/functions/aa_00754320_Palantir_CompleteDtor.md` |
| Scratch | `docs/reconstruction/tmp/a_00754320.md` |

---

## AutoCore impact

| Unit | Port note |
|------|-----------|
| `00444810` | Port as **tidy + clear published shell pointer**: `StringVec_Tidy(shell)` then `DAT_00d1f1fc = nullptr`. **ECX=shell**, bare RET. Used from Palantir dtor SEH when shell is **`host+0xA0`**. Do **not** treat as bare tidy; do **not** skip global clear. |
| `00754320` | Port as **Palantir complete dtor** (not scalar): vtbl → `00a9f76c`; destroy `+0xB4` string; tidy `+0xA0` path shell + clear `DAT_00d1f1fc`; nested dtor `+0x30` then `+0x10`; clear **`DAT_00d1f058`**. Scalar free is **only** in `FUN_004447e0`. Nested subobjects remain residual. |

Shared layout / globals:

| Offset / global | Role |
|-----------------|------|
| `Palantir+0x00` | vtbl (`PTR_FUN_00a9f76c` in dtor phase) |
| `Palantir+0x10` | subobject → `FUN_00964d00` (unowned) |
| `Palantir+0x30` | subobject → `FUN_0075d8e0` (unowned) |
| `Palantir+0xA0` | `StringVecShell` path list (also `*DAT_00d1f1fc`) |
| `Palantir+0xB4` | `basic_string` |
| `DAT_00d1f1fc` | published path-list shell* (nulled on tidy) |
| `DAT_00d1f058` | Palantir host singleton* (nulled at end of complete dtor) |

Closes high-mention nested residuals: Palantir complete dtor + path-list SEH tidy helper under AssManager/Palantir teardown / `StringVec_Tidy` callers.

---

## Process

- OWN VAs only; no Launcher; no parent ledger edits.  
- Seal tools: decompile + analyze_function_complete + read_memory + xrefs. **No** `disassemble_bytes`.  
- Dual A (fidelity) + B (adversarial) both filed per VA.  
- Scaffold raw bodies preserved; re-verify **appended** only.  
- No invented product names without evidence — Palantir class identity from vtbl peer log string; shell helper keeps structural name.  
- Avoided bare `undefined4` in clean (used `uint8_t` / explicit structs).
