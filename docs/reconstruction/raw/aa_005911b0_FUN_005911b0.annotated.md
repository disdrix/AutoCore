# Annotated low-level: CVOGObject_JustCreatedInit (`FUN_005911b0`)

| Field | Value |
|---|---|
| Stable ID | `aa_005911b0` |
| VA | `0x005911b0`–`0x00593233` (8324 B / `0x2083`) |
| Canonical name | `CVOGObject_JustCreatedInit` |
| Ghidra name | `FUN_005911b0` |
| System | client object / physics-gfx spawn finalize |
| Date | 2026-07-29 (W22-H dual seal) |

## Machine-level notes

- Source: raw capture + live W22-H decompile / `read_memory` / callers / callees.
- **Role:** **Just-created** finalize for a client object (`this`): install pose/orientation onto gfx body, optional skybox particles, ragdoll bind, rigidbody build/attach, multi-body clone, `addEntity` into sim.
- **ABI:** `__thiscall` — ECX = object*, stack `char mode` (1 = batch force path, 0 = deferred tick path); **`ret 4`**; return **1** success / **0** early abort.
- **Idempotency gate:** `*(byte*)(this + 0x910) & 1` → skip to return 1 (caller batch also ORs this bit after success with mode=1).
- **Early abort (mode==0):** if soft-flags indicate skip-or-deferred path → `FUN_004a7a20(&this)` and return **0** (tick caller bails).
- **Profiler:** root `"Just created"`; nested `"skyboxphase"`, `"ragdoll"`, `"rigidbody"`, `"addEntity"`.
- **RNG scale:** `CVOGReaction_RandomUnitScalar` yields u16 stream; scale by `DAT_00aaa638` ≈ **1/65535**.
- Prefer phase map over line-by-line for ports — body is large (~8 KB) with many optional flag branches.

## Signature

```c
// __thiscall; ret 4
uint32_t CVOGObject_JustCreatedInit(Object* this, char mode /*0 tick / 1 batch*/);
// returns 1 = completed (or already marked); 0 = deferred/abort
```

## Phase map

```
Just created:
  if (this+0x910 & 1) return 1
  if (mode==0 && soft_skip_flags) { FUN_004a7a20(&this); return 0 }
  copy pending pos → active pos
  if (gfx_body):
    build orientation (flags @+0x90d: 0x20/0x40/0x80 random axes/angle)
    apply pos/rot via gfx vfunc +0x10 and physics helpers
    skyboxphase: sync particle hosts
    optional attach FX once (@+0x90f bit0)
  optional random scale from clonebase
  if (ragdoll requested once): load ragdoll; clear bit on failure + log
  if (rigidbody path enabled):
    build/load RB (FUN_00590cb0 / FUN_0058fd30 multi)
    impulse / multi-body placement
    addEntity
  return 1
```

## Key offsets (this object)

| Offset | Role (inferred) |
|---|---|
| `+0x818..+0x824` | active / pending position dwords (`[0x206..0x20b]` as int*) |
| `+0x8C4..` | orientation quaternion block (`[0x231..]`) |
| `+0x90c..+0x911` | flag bytes (spawn / soft / just-created / deferred) |
| `+0x910` bit0 | just-created **done** latch |
| `+0x914` (`[0x245]`) | gfx body pointer |
| `+0x260` (`[0x98]`) | rigidbody host pointer |
| `+0x28` (`[10]`) | clonebase / template |
| `+0x2be` | name / asset path C-string region (ragdoll/RB) |

## Callers

| Caller | Mode | Post-condition |
|---|---|---|
| `FUN_00594290` | `1` | `*(this+0x910) \|= 1` after each |
| `FUN_00595230` | `0` | if return 0, leave tick early (deferred) |

## Open questions

1. Exact product class name (`CVOGObject` vs creature/vehicle subclass entry).
2. Full bit dictionary for `+0x90c..+0x911`.
3. Runtime golden for multi-RB + ragdoll failure paths.
4. Bit-exact — open.
