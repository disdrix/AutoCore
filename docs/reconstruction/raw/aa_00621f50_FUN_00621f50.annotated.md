# Annotated low-level: FUN_00621f50 → CVOGHBSkill_HitCharge_Execute_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00621f50` |
| VA | `0x00621f50`–`0x0062205d` (**270 B** / `0x10E`) |
| System | skills-abilities |
| Preferred name | `CVOGHBSkill_HitCharge_Execute_Inferred` |
| Date | 2026-08-05 (R12-005 dual; prior scaffold 2026-07-23) |

## Machine-level notes

- Source: raw capture for `aa_00621f50` + live Ghidra re-verify 2026-08-05.
- Prefer assembly / `read_memory` when decompiler conflicts (period-field overlay is noise).
- Class identity from RTTI COL on installed vtbl `009d1624`, not from intermediate OnDoHit parent ctor alone.
- Sole install site is DATA vtbl slot `@0x009d1650` = vtbl+0x2C.

## Pseudocode (annotated)

```c
// SEH frame: LAB_009a893b; six stack formals; ret 0x18; return 1
// ECX this unused (virtual free-function style)

undefined4
FUN_00621f50(undefined4 pSource, undefined4 pSkillBlob, int pWorld,
             int pTargetTable, undefined4 pTfid16, undefined4 arg7)
{
  char index = 0;

  for (;;) {
    // entry stride 0x10: { coidLo, coidHi, bGlobal, pad }
    int *entry = (int *)(pTargetTable + index * 0x10);

    // sentinel {-1, -1, bGlobal==0}
    if (entry[0] == -1 && entry[1] == -1 && (char)entry[2] == 0) {
      return 1;
    }

    // ResolveObjectTarget(this=world+0xe4e8, bGlobal, lo, hi)
    int target = CVOGReaction_ResolveObjectTarget(
        *(void **)(pWorld + 0xe4e8),
        (char)entry[2], entry[0], entry[1]);

    if (target != 0) {
      CVOGHBBase *pAction = operator_new(0x6d0);  // HitCharge size
      if (pAction != 0) {
        // Parent: CVOGHBSkill_OnDoHit_Ctor (installs OnDoHit vtbl first)
        FUN_00621350(pAction, pSource, pSkillBlob, pWorld, target, pTfid16, arg7);
        pAction->pVTable = &PTR_FUN_009d1624;     // HitCharge vtbl
        *(uint32_t *)((char *)pAction + 0x6c0) = 0;
        *(float *)((char *)pAction + 0x6c4) = 0.0f;
      }
      if (pAction->pOwnerObject /* +0x18 */ != 0) {
        CVOGHBList_Enqueue(*(void **)(pWorld + 0xe4ec), pAction);
        CVOGHBBase_Start(pAction);
      } else {
        // deleting dtor vtbl[0](1)
        (**(code **)pAction->pVTable)(1);
      }
    }
    index++;
  }
}
```

## Sealed identity

| Item | Value |
|---|---|
| RTTI | `.?AVCVOGHBSkill_HitCharge@@` @ type_info `0x00af2b4c` |
| COL | `0x00aae1f0` at `009d1620` (vtbl-4) |
| Vtbl | `PTR_FUN_009d1624` |
| Slot | `+0x2C` @ `0x009d1650` |
| Parent ctor | `0x00621350` OnDoHit |
| Peer (do not merge) | `0x00621700` OnDoHit execute (`0x6c0` / `009d15c4`) |

## Open questions

- Product C++ method name for vtbl+0x2C (Execute is role-inferred).
- Product English for HitCharge fields `+0x6c0` / `+0x6c4`.
- Virtual dispatch call sites (not OWN).
- Runtime / bit-exact / differential.
