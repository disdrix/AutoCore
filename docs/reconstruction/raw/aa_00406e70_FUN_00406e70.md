# Raw capture: FUN_00406e70

| Field | Value |
|---|---|
| **Stable ID** | `aa_00406e70` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00406e70` |
| **Canonical name** | `FUN_00406e70` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int * FUN_00406e70(int *param_1,int param_2,undefined4 param_3)

{
  int iVar1;
  int unaff_EDI;
  
  iVar1 = *(int *)(unaff_EDI + 4);
  if (iVar1 != 0) {
    if ((*(int *)(unaff_EDI + 8) - iVar1) / 0xc != 0) {
      iVar1 = (param_2 - iVar1) / 0xc;
      goto LAB_00406eb5;
    }
  }
  iVar1 = 0;
LAB_00406eb5:
  FUN_004082f0(param_2,1,param_3);
  *param_1 = *(int *)(unaff_EDI + 4) + iVar1 * 0xc;
  return param_1;
}
```

---

## WQ9G-I re-verify (2026-08-04)

**Tooling:** Ghidra `batch_decompile` / `decompile_function` + `analyze_function_complete` + `read_memory` + callers/xrefs. **No** `disassemble_bytes`. **No** Launcher.

### Live decompile (equiv scaffold body)

```c
int * FUN_00406e70(int *param_1,int param_2,undefined4 param_3)

{
  int iVar1;
  int unaff_EDI;
  
  iVar1 = *(int *)(unaff_EDI + 4);
  if (iVar1 != 0) {
    if ((*(int *)(unaff_EDI + 8) - iVar1) / 0xc != 0) {
      iVar1 = (param_2 - iVar1) / 0xc;
      goto LAB_00406eb5;
    }
  }
  iVar1 = 0;
LAB_00406eb5:
  FUN_004082f0(param_2,1,param_3);
  *param_1 = *(int *)(unaff_EDI + 4) + iVar1 * 0xc;
  return param_1;
}
```

### Byte seal (`read_memory` @ `0x00406e70`, 128 B)

- Body **104 B** / `0x68`: `0x00406e70`–`0x00406ed8` exclusive; pad `CC` to `0x00406ee0`.
- Prologue uses **EDI** as vector host (`mov esi,[edi+4]` begin) — decompiler `unaff_EDI`.
- Index: magic `0x2AAAAAAB` signed `/12` (`imul`/`sar 1`/`cdq`-style abs); empty → index 0; else `(where-begin)/12` in ESI.
- Call site: `push value*; push 1; push where; mov ecx,edi; call FUN_004082f0` (`e8` → `0x004082f0`).
- Rebind: `lea edx,[esi+esi*2]; lea ecx,[eax+edx*4]` → `begin' + index*12`; `*out = ecx`; `EAX = out*`.
- Epilogue: **`C2 0C 00`** (`ret 0x0C`) — three stack formals.

### Cross-refs

| Kind | Value |
|------|-------|
| Callers | `FUN_004062a0` only (1 UNCONDITIONAL_CALL @ `0x0040630c`) |
| Callees | `FUN_004082f0` (InsertN / grow for elem 0xc) |
| Parent role | push_back slow path: EAX=vec → `mov edi,eax`; capacity miss → this unit with where=`end` |

### Recovered ABI / name

- **Name:** `StdVector_InsertOne_RebindIt_Elem12_EdiVec_Inferred`
- **ABI:** EDI=vector* (+4 begin, +8 end, +0xC capEnd); stack `T** out`, `T* where`, `const T* value`; **ret 0xC**; EAX=out*
- **Stride:** **0xC** (12 B / 3 dwords) — Confirmed
- **Role:** insert-one + index rebind (MSVC vector iterator invalidation fix after realloc)

