# Raw capture: FUN_0082d540

| Field | Value |
|---|---|
| **Stable ID** | `aa_0082d540` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0082d540` |
| **Canonical name** | `FUN_0082d540` / product `CVOGMenu_CompleteDtor` |
| **System** | MSVC intermediate complete dtor — CVOGMenu |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0082d540(undefined4 *param_1)

{
  void *pvStack_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  puStack_8 = &LAB_009b4443;
  pvStack_c = ExceptionList;
  ExceptionList = &pvStack_c;
  *param_1 = &PTR_FUN_00a72f6c;
  local_4 = 0;
  FUN_0082ce20();
  local_4 = 0xffffffff;
  FUN_00792c20();
  ExceptionList = pvStack_c;
  return;
}
```

---

## Re-verify WQ9K-G (2026-08-05)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `batch_decompile` + `analyze_function_complete` + `disassemble_function` + `read_memory` (body + vtbl + RTTI COL/type_info). **No** `disassemble_bytes`. |
| **Body** | `0x0082d540`–`0x0082d58e` inclusive (**79 B** / `0x4F`); pad `CC` then next. |
| **ABI** | **ECX=`this`**; SEH `LAB_009b4443`; EH states **0** during `0082ce20`, then **`0xFFFFFFFF`** for base; epilogue **`ADD ESP,0x10; RET`**; void; does **not** free `this`. |
| **Semantics** | MSVC **complete destructor** for **CVOGMenu**: install `PTR_FUN_00a72f6c` at `*this`; run menu hash teardown `FUN_0082ce20` (EH=0); chain deep base complete `FUN_00792c20` (CNDUIDialog). |
| **RTTI** | COL dword @ `0x00a72f68` → `0x00ab6214` → type_info `0x00afe39c` → **`.?AVCVOGMenu@@`** → **CVOGMenu**. |
| **Vtbl[0]** | `PTR_FUN_00a72f6c[0]` = `0x004166d0` scalar-deleting (`FUN_0082d540` then optional `delete`). |
| **Callers (code)** | `FUN_004166d0` (scalar); `FUN_008327f0` (CMenuChatlog complete); plus other menu completes `0082e0a0` / `0082f450` / `0082ff40` / `008309d0` / `00831030` / `00831cd0`; many SEH Unwind thunks. |
| **Xrefs** | 22 (mixed CALL + Unwind). |
| **Classification** | wrapper (thin intermediate complete over menu-hash helper + CNDUIDialog). |
| **Decompile ≡ disasm** | Yes for install / EH / two CALLs / ADD ESP,0x10; RET. Body hex sealed. |
| **Product name** | `CVOGMenu_CompleteDtor` |

### Body hex (entry → RET)

```
6a ff 68 43 44 9b 00 64 a1 00 00 00 00 50 64 89 25 00 00 00 00
51 56 8b f1 89 74 24 04 c7 06 6c 2f a7 00 c7 44 24 10 00 00 00 00
e8 b0 f8 ff ff c7 44 24 10 ff ff ff ff 8b ce e8 a1 56 f6 ff
8b 4c 24 08 5e 64 89 0d 00 00 00 00 83 c4 10 c3
```

Integrity: original scaffold body retained above; this section appends only.
