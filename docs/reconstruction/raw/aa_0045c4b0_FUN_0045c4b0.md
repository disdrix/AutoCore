# Raw capture: FUN_0045c4b0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0045c4b0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0045c4b0` |
| **Canonical name** | `FUN_0045c4b0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0045c4b0(uint param_1)

{
  int iVar1;
  uint uVar2;
  int unaff_EBX;
  
  iVar1 = *(int *)(unaff_EBX + 4);
  if (iVar1 == 0) {
    uVar2 = 0;
  }
  else {
    uVar2 = (*(int *)(unaff_EBX + 8) - iVar1) / 0x1c;
  }
  if (uVar2 < param_1) {
    FUN_0045cd30(*(undefined4 *)(unaff_EBX + 8),&stack0x00000004);
    return;
  }
  if ((iVar1 != 0) && (param_1 < (uint)((*(int *)(unaff_EBX + 8) - iVar1) / 0x1c))) {
    FUN_0045cce0(unaff_EBX,&stack0x00000004,iVar1 + param_1 * 0x1c,*(int *)(unaff_EBX + 8));
  }
  return;
}
```

---

## W35-M re-verify (2026-07-29) — append only

**Tools:** `batch_decompile` / `analyze_function_complete` + `read_memory` + callee `FUN_0045cce0` decompile (context only). **No** `disassemble_bytes`.  
**Body (bytes win):** `0x0045c4b0`–`0x0045c552` exclusive (**162 B** / `0xA2`). Ghidra body end `0045c551` = last byte of `C2 1C 00`. Pad `CC` then next.

### Full hex (162 B)

```

```

### Sealed ABI (bytes win)

| Formal | Location | Role |
|---|---|---|
| vec* | **EBX** (`unaff_EBX`) | `StdVectorPod28*` triad host |
| new_size | **ECX** (`param_1`) | desired element count |
| value | **stack** 0x1c bytes by value | fill template for grow (passed as `value*` into InsertN) |
| clean | **`RET 0x1C`** (`C2 1C 00`) | callee pops 28 B value blob |

Vector triad (matches InsertN Pod28 family):

| Off | Field |
|---|---|
| +0x04 | begin |
| +0x08 | end |
| +0x0C | (capEnd — unused in this unit) |

Size = `(end−begin)/0x1c` via magic **`0x92492493`** (×`imul` signed reciprocal for ÷28). Null begin → size 0.

### Control flow

1. `size = begin? (end−begin)/0x1c : 0`.
2. **Grow** if `size < new_size`:
   - `count = new_size − size`
   - `FUN_0045cd30(count@ECX, vec@EDX, where=end, value=&stack_value)` — sealed InsertN Pod28
   - `RET 0x1C`
3. **Shrink** if `begin != 0` and `new_size < size`:
   - `new_end = begin + new_size*0x1c` (`lea` form: `eax=new_size*8; eax-=new_size; lea eax,[begin+eax*4]`)
   - `FUN_0045cce0(vec, out_it*, first=new_end, last=old_end)` — erase `[new_end, end)` (POD range shift + publish end)
4. Equal / empty: fall through, `RET 0x1C`.

### Callers / callees

| Dir | Symbol | Note |
|---|---|---|
| Caller (1) | `FUN_00459750` @ `0x004599bf` | unowned parent |
| Callee | `FUN_0045cd30` | `StdVector_InsertN_Pod28` (W34-P sealed) @ site `0x0045c506` |
| Callee | `FUN_0045cce0` | erase/truncate range (POD 7-dword move) |

### Name

**`StdVector_Resize_Pod28`** (Inferred structural — MSVC-style `vector<T>::resize(n, value)` for POD T size 0x1c).  
Reject scaffold `Named_CalleeOf_Named_anmTrackMaster_inl_*`.

### Classification

**worker**.
