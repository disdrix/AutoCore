# Annotated low-level: FUN_0067b070 / Net_SockaddrIn_InitZero

| Field | Value |
|---|---|
| Stable ID | `aa_0067b070` |
| VA | `0x0067b070` |
| Body | `0x0067b070`–`0x0067b094` (37 bytes) |
| System | network / sockaddr fill |
| Date | 2026-07-29 (W25-O dual seal) |
| Named role | `Net_SockaddrIn_InitZero` (**Inferred** / structural) |

## Machine-level notes

- **ABI:** thiscall/fastcall — **ECX** = 16-byte out buffer; plain `ret`; returns buffer in **EAX**.
- **Callee:** Winsock `htons` via IAT `0x009c6830` (same as `Net_SockaddrIn_Init`).
- **Role:** Zero-initialize a `sockaddr_in`-shaped 16-byte object:
  1. Clear four dwords (16 bytes).
  2. Set family word to **0** (`AF_UNSPEC`).
  3. Set addr dword to **0**.
  4. `htons(0)` → port word.
  5. Return `this`.
- **Twin:** `Net_SockaddrIn_Init` @ `0x0067b0a0` is the same skeleton with family=`2` (AF_INET), non-zero addr/port args, `ret 8`.
- **Typical callers:** net object ctors (`FUN_006708b0`), `getsockname`/`getpeername` prep (`FUN_0067b9a0` / `FUN_0067ba30`) before `FUN_0067b0e0` copies result into an address object.
- Prior auto-name `Named_CalleeOf_Named_VOGChat_*` is **not** evidence of chat semantics — body has no chat strings or opcodes.

## Pseudocode (annotated)

```c
// 16-byte sockaddr_in-like buffer; product type name Open
SockaddrIn16 * __fastcall Net_SockaddrIn_InitZero(SockaddrIn16 *sa)
{
  // xor eax,eax; four dword stores
  *(uint32_t *)sa = 0;
  ((uint32_t *)sa)[1] = 0;
  ((uint32_t *)sa)[2] = 0;
  ((uint32_t *)sa)[3] = 0;

  sa->family = 0;                 // not AF_INET
  sa->addr = 0;
  sa->port = htons(0);            // IAT 0x009c6830

  return sa;
}
```

## Open questions

- Product/TNL type name for the 16-byte object (same residual as Init family).
- Whether any path relies on family remaining `0` vs immediately overwritten by Init/getpeername.
- Runtime / bit-exact deferred.
