# Annotated low-level: StdTree_InsertAndRebalance_Val8_Isnil15_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00438140` |
| VA | `0x00438140` |
| System | std map/set tree Val8 / isnil15 |
| Date | 2026-08-04 (W38-L dual seal) |
| Canonical | `StdTree_InsertAndRebalance_Val8_Isnil15_Inferred` |

## Machine-level notes

- Body **383 B** inclusive `0x00438140`–`0x004382be`; epilogue **`83 C4 50 C2 10 00`** (`ADD ESP,0x50; RET 0x10`).
- SEH frame `LAB_009bc919`.
- **EDI = map** — `81 7F 08 FE FF FF 1F` compares `map->size` to **`0x1ffffffd`**.
- Stack args (RET 0x10): `out*`, `addLeft`, `where*`, `value*`.
- Buynode `FUN_00439110`: `operator_new(0x18)`; copies `*ESI` / `ESI[1]` into key@+0x0C / mapped@+0x10; color arg 0; isnil@+0x15 = 0. Inside this fn ESI reloaded from stack value*.
- Link: empty head → root/leftmost/rightmost; else left (`addLeft!=0`) or right; maybe update extremities.
- RB loop: parent color byte **`+0x14`**; uncle recolor or rotate via **`FUN_004219b0`** (L) / **`FUN_0046f030`** (R).
- Root painted black; `*out = new`.
- **No key compare** — always-insert; parent insert-or-find owns uniqueness.
- Rotate callees are isnil15 family but **distinct VAs** from W24-O `004e22d0` / `006753b0`.

## Pseudocode (annotated)

```c
// EDI = MapShell* (head@+4, size@+8)
void FUN_00438140(Node** out, char addLeft, Node* where, Val8* value)
{
  if (map->size > 0x1ffffffd) throw "map/set<T> too long";
  Node* n = Buynode_Val8(head, where, head, /*color*/0, value); // FUN_00439110
  map->size++;
  // link n under where (left/right) + maybe leftmost/rightmost
  // while parent.color == red: recolor or rotate
  head->parent->color = black;
  *out = n;
  // RET 0x10
}
```

## Open questions

- Product/demangle map types across six multi-site callers.
- Full dual of buynode `FUN_00439110` and rotate clones `004219b0` / `0046f030`.
- Buynode null OOM path (operator_new fail).
- Runtime / bit-exact / differential.
