# Raw capture: FUN_00767070

| Field | Value |
|---|---|
| **Stable ID** | `aa_00767070` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00767070` |
| **Canonical name** | `FUN_00767070` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_00767070(void)

{
  uint uVar1;
  undefined4 *unaff_ESI;
  int iVar2;
  
  iVar2 = unaff_ESI[6] * 4;
  unaff_ESI[8] = iVar2;
  if (-1 < iVar2 + -1) {
    do {
      uVar1 = (**(code **)(*(int *)*unaff_ESI + 0x18))(&DAT_00a2a000,1);
      unaff_ESI[1] = unaff_ESI[1] | uVar1;
      iVar2 = iVar2 + -1;
    } while (iVar2 != 0);
  }
  return unaff_ESI[1];
}
```

---

## Append — dual A/B re-verify 2026-07-29 (W17-R OWN)

| Field | Value |
|---|---|
| **Tool** | Ghidra `decompile_function` + `read_memory` (no `disassemble_bytes`) |
| **Program** | `autoassault.exe` |
| **Body range** | `0x00767070`–`0x0076709d` (`get_function_by_address`) |
| **Live decompile** | **≡** raw body above (byte-identical CF / offsets) |
| **Sole caller** | `FUN_00767160` / `stoChunkWriter_WriteFormatted` @ `0x007671ba` |
| **Xrefs** | 1 (`0x007671ba`) |

### Body bytes (`read_memory` @ `0x00767070`, length 48)

```
8B 46 18          mov  eax, [esi+0x18]       ; depth
03 C0             add  eax, eax
03 C0             add  eax, eax             ; depth * 4
89 46 20          mov  [esi+0x20], eax       ; textCol = depth*4
83 E8 01          sub  eax, 1
78 1B             js   skip_loop            ; if count==0 skip
57                push edi
8D 78 01          lea  edi, [eax+1]         ; edi = count
; loop:
8B 0E             mov  ecx, [esi]           ; stream*
8B 01             mov  eax, [ecx]
6A 01             push 1
68 00 A0 A2 00    push DAT_00a2a000         ; " "
FF 50 18          call [eax+0x18]           ; write(buf,1)
09 46 04          or   [esi+0x04], eax      ; sticky status
83 EF 01          sub  edi, 1
75 EA             jnz  loop
5F                pop  edi
; skip_loop / ret:
8B 46 04          mov  eax, [esi+0x04]
C3                ret
```

### DAT seal

| Address | Hex | ASCII |
|---|---|---|
| `0x00a2a000` | `20 00 00 00 …` | `" "` (space, len 1) |

### Call-site seal (`WriteFormatted` @ `0x007671b4`)

```
83 7E 20 00        cmp  dword [esi+0x20], 0   ; textCol
75 0A              jnz  mid_line_space
E8 B1 FE FF FF     call FUN_00767070          ; indent (ESI = writer)
09 46 04           or   [esi+0x04], eax
```

Writer register ABI: **ESI = stoChunkWriter\*** (carried from caller's `mov esi, eax` at WriteFormatted entry). No stack args; **`ret`** (not RET n).
