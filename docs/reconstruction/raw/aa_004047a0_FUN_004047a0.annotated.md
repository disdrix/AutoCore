# Annotated low-level: UiToast_UninitializedCopy_0x98 (`FUN_004047a0`)

| Field | Value |
|---|---|
| Stable ID | `aa_004047a0` |
| VA | `0x004047a0`–`0x0040480b` (108 B) |
| Canonical name | `UiToast_UninitializedCopy_0x98` |
| Ghidra name | `FUN_004047a0` |
| System | `missions-progression` / shared UI toast vector helpers |
| Date | 2026-07-29 (W22-H dual seal) |

## Machine-level notes

- Source: raw capture + live W22-H `decompile_function` / `read_memory`.
- **Role:** MSVC-style `uninitialized_copy` for UI toast POD elements of stride **`0x98`**.
- **Algorithm:** for `src` in `[src_begin, src_end)` step `0x98`: `UiToast_PodCopyElement_0x98(dst, src)`; `dst += 0x98`; return final `dst`.
- **ABI:** **cdecl** (`C3` plain `ret`). EAX = advanced destination end pointer.
- **Callee:** sealed `UiToast_PodCopyElement_0x98` (`FUN_00404710`) — `rep movsd` of `0x26` dwords; null-check on **dst** only; `ret 8`.
- **Caller:** `FUN_00404510` thiscall thunk (vector member) reorders `(this, first, last, result)` → free helper args.
- Extra stack formal at `[ebp+0x14]` loaded to EBX and moved to ECX before the leaf call — **dead** for `PodCopyElement` (stdcall 2-arg). Wrapper also pushes a redundant 5th `result`.
- SEH frame present (`LAB_009bd1c0`); state `0` during loop, `-1` at epilogue. No C++ objects that throw on this path; frame is MSVC codegen habit for the toast helper cluster.
- Prefer assembly when decompiler drops the 4th formal (decomp shows 3 params; bytes load four).

## Recovered CF (byte-aligned)

```c
// cdecl: (src_begin, src_end, dst [, unused_this [, unused_dst_again]])
// returns dst end after copies
void *UiToast_UninitializedCopy_0x98(void *src_begin, void *src_end, void *dst, void *unused_this /* optional */)
{
    // SEH register (elided)
    for (; src_begin != src_end; src_begin = (char*)src_begin + 0x98) {
        UiToast_PodCopyElement_0x98(dst, src_begin); // stdcall (dst, src)
        dst = (char*)dst + 0x98;
    }
    return dst;
}
```

## Call graph (OWN context)

| Direction | VA | Name |
|---|---|---|
| Callee | `0x00404710` | `UiToast_PodCopyElement_0x98` |
| Caller | `0x00404510` | toast-vector thiscall thunk (sibling of fill-n path) |
| Sibling fill-n | `0x00404130` | `UiToast_UninitializedFillN_0x98` |
| Sibling construct-N | `0x00404670` | `UiToast_ConstructN_0x98` |

## Open questions

1. Retail / PDB symbol (structural name only).
2. Exact product type of the 4th formal (allocator vs vector this) — unused by body.
3. Runtime / bit-exact image diff — open.
