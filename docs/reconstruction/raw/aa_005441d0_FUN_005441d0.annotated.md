# Annotated low-level: GuardedVector_InsertRange_Thiscall

| Field | Value |
|---|---|
| Stable ID | `aa_005441d0` |
| VA | `0x005441d0`–`0x00544587` exclusive (**951 B**) |
| Ghidra | `FUN_005441d0` |
| Inferred name | `GuardedVector_InsertRange_Thiscall` |
| System | STL / guarded ring-deque container |
| Date | 2026-07-29 (W32-I re-verify) |

## Machine-level notes

- Source: raw capture + live `batch_decompile` / `read_memory` (W32-I).
- **No** `disassemble_bytes`.
- SEH frame `LAB_009a4170`; `RET 0x1C` (7 stack dwords).
- ECX = dest container (`MOV ESI,ECX`).
- Layout: `+0x04` pages, `+0x08` capacity, `+0x0c` begin, `+0x10` size (GuardedVector family).
- Insert-range shorter-side policy mirrors W29-A `GuardedVector_InsertN`, but source is a **range of elements** (not N copies of one fill), and room is made with **thiscall** push pair W31-I (`0043e3d0` / `0043d670`).
- Sole caller `FUN_00545190`: zeros dest header then inserts full source range at index 0 (copy-construct path).

## ABI

```
// ECX = dest GuardedVector*
// stack (7 dwords, RET 0x1C) — decompiler labels 6 + ECX; copy-ctor pushes 7:
//   approx: (dest_or_out, insert_at, first.cont, first.idx, last.cont, last.idx, extra)
void GuardedVector_InsertRange_Thiscall(
    GuardedVectorHeader* dest /*ECX*/,
    /* 7 stack formals — see raw re-verify */);
```

## Control flow (annotated)

```
prefix = insert_at - dest->begin
suffix = dest->size - prefix
n      = last_idx - first_idx

if (prefix < suffix):          // front-biased
  if (prefix < n):
    push_front elements from source tail-window via 0043e3d0
    push_front prefix copies from dest near-insert via 0043e3d0
    FUN_0043ec50 ring-assign remaining insert window
    return
  else:
    push_front n copies from dest near-insert via 0043e3d0
    FUN_0043ec50 internal shuffle
    // fall through to final assign of [first,last)
else:                          // back-biased
  if (suffix < n):
    push_back from source remainder + dest suffix via 0043d670
    FUN_0043ec50
    return
  else:
    push_back n from dest via 0043d670
    FUN_0043ebb0 ring-assign backward shuffle

FUN_0043ec50 assign [first,last) into dest hole
```

## Pseudocode (annotated copy of raw)

See raw body. Key renames for readers:

| Raw | Meaning |
|---|---|
| `param_1` | dest container (ECX) |
| `param_3` | absolute insert index |
| `param_4`/`param_5` | source range start (container, abs index) |
| `param_6`/`param_7` | source range end (container, abs index) |
| `FUN_0043e3d0` | `GuardedVector_PushFront_Thiscall` (W31-I) |
| `FUN_0043d670` | `GuardedVector_PushBack_Thiscall` (W31-I) |
| `FUN_0043ec50` | ring element assign forward (iterator pair out) |
| `FUN_0043ebb0` | ring element assign backward (iterator pair out) |

## Callers / callees

| Direction | Symbol |
|---|---|
| Caller | `FUN_00545190` (1 xref @ `005451de`) |
| Callees | `0043e3d0`, `0043d670`, `0043ec50`, `0043ebb0` |
