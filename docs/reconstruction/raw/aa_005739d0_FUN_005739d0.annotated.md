# Annotated low-level: FriendsList_FindEntryByTfid_Inferred (`FUN_005739d0`)

| Field | Value |
|---|---|
| Stable ID | `aa_005739d0` |
| VA | `0x005739d0`–`0x00573a28` exclusive (**88 B** / `0x58`) |
| Ghidra | `FUN_005739d0` |
| Canonical (inferred) | `FriendsList_FindEntryByTfid_Inferred` |
| System | social / friends list (partition nest skills-abilities residual) |
| Date | 2026-08-05 (R13-039 re-annotate) |
| Agent | OWN-ONLY dual R13-039 (dual start 2686) |

## Machine-level notes

- **Leaf** — no callees (`analyze_function_complete` classification `leaf`).
- **`__thiscall`**: ECX = multi-list host; stack `tfid_lo` (EDI), `tfid_hi` (EBX); **`RET 0x8`**.
- Vector begin **`[ECX+0x8]`** → ESI; end **`[ECX+0xc]`**; count `(end-begin)>>2` or 0 if begin==0.
- Entry key: **`[entry+0x8]`** vs `tfid_lo`, **`[entry+0xc]`** vs `tfid_hi` (exact int equality).
- Hit: reload begin, `LEA EAX,[begin+i*4]; MOV EAX,[EAX]` → entry*; miss: `XOR EAX,EAX`.
- Dual terminal bytes `C2 08 00`; pad `CC` before friends name twin `FUN_00573a30`.

## Pseudocode (annotated)

```c
// __thiscall; RET 0x8; EAX = entry* | 0
// listHost: friends pointer-vector at +0x8 / +0xc (same host family as DAT_00d1da2c)
// entry: TFID lo/hi at +0x08 / +0x0c; display name at +0x1a (caller-consumed, not compared here)
void* FriendsList_FindEntryByTfid_Inferred(void* listHost /*ECX*/,
                                           int tfid_lo, int tfid_hi)
{
  int i = 0;
  for (;;) {
    void** begin = *(void***)((char*)listHost + 0x8);
    int count = (begin == 0)
      ? 0
      : ((*(char**)((char*)listHost + 0xc) - (char*)begin) >> 2);
    if (count <= i) break;
    void* entry = begin[i];
    if (*(int*)((char*)entry + 0x8) == tfid_lo &&
        *(int*)((char*)entry + 0xc) == tfid_hi)
      return entry;
    i++;
  }
  return 0;
}
```

## Callers (role)

| Caller | Site | Notes |
|---|---|---|
| `FUN_0080f3b0` | `0x0080f3f5` | Presence notify: hit → `"Your friend"` + name@`entry+0x1a` + online/offline; flag==0 uses enemy twin `FUN_00573b50` |
| `FUN_00574020` | `0x00574031` | insert-if-missing: miss → `FUN_005746e0` |
| `FUN_00574270` | `0x00574281` | insert-if-missing twin: miss → `FUN_005746e0` |

## Open questions

- Product / MSVC demangle for multi-list host and friends entry type.
- Full entry packing beyond TFID@+8/+0xc and name@+0x1a.
- Runtime / bit-exact (Terminal false).
