# Annotated low-level: FUN_00984ee0 → AssPackManager_OpenPackFile_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00984ee0` |
| VA | `0x00984ee0`–`0x00985006` (**295 B**) |
| System | palantir assets / assPackManager |
| Date | 2026-07-23 scaffold; **W31-H seal 2026-07-29** |

## Machine-level notes

- Source: raw capture + live `read_memory` full body + caller sites in `FUN_007b75b0`.
- Prefer bytes for ABI: **EBX=this**, **ECX=path**, stack flag, **RET 4** (decompiler omits EBX as formal).
- Nested free: `FUN_0071a9d0` (open), `FUN_00719970` (0x44 ctor), `FUN_004406e0` (vector push).
- Plate file: `C:\vog\1_code\palantir\palantir\assets\assPackManager.cpp` lines 0x75 / 0x7b / 0x80.

## Layout (AssPackManager host)

| Offset | Role |
|---|---|
| +0x28 | vector of pack* (begin/end/cap style; push via `FUN_004406e0`) |
| +0x38 | `CRITICAL_SECTION` |
| +0x50 | `char` lock-enable |

## Pseudocode (annotated)

```c
// Retail ABI (sealed):
//   EBX = AssPackManager* this
//   ECX = const char* path
//   stack char flag  // 0 or 1 → pack+0x40
//   RET 4; returns 0 / -1
int AssPackManager_OpenPackFile_Inferred(/*EBX*/ Manager* self,
                                         /*ECX*/ const char* path,
                                         /*stack*/ char flag)
{
  CRITICAL_SECTION* cs = (CRITICAL_SECTION*)(self + 0x38);
  if (*(char*)(self + 0x50)) EnterCriticalSection(cs);

  // log Opening; new(0x44); FUN_00719970(pack, flag);
  // FUN_0071a9d0 open (EAX=path, EDI=pack)
  if (open_rc < 0) {
    // log FAILED; LeaveCS if locked; return -1;
  }
  // log SUCCESS; push pack* into vector at self+0x28
  if (*(char*)(self + 0x50)) LeaveCriticalSection(cs);
  return 0;
}
```

## Callers

- `AssManager_InitPrecompileAndResolvers_Inferred` (`0x007b75b0`) — batch open of `.glm` packs after resolver B install.
- `FUN_00985010` compact path.
- `FUN_009963f0`.

## Confidence

| Claim | Level |
|---|---|
| ABI EBX/ECX/stack/RET4 | **High** (call site bytes) |
| Open/fail/success CF | **High** |
| Vector @ +0x28 | **High** (`LEA EAX,[EBX+0x28]`) |
| Nested open body English | **Med** (not OWN) |
| Product method demangle | **Low** |
