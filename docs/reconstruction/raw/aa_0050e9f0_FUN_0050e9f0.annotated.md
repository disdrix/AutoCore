# Annotated low-level: FUN_0050e9f0 → StdTree_Lrotate_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0050e9f0` |
| VA | `0x0050e9f0`–`0x0050ea3d` inclusive (**78 B**) |
| System | MSVC std `_Tree` left-rotate (isnil@+0x21 / color@+0x20) |
| Date | 2026-08-05 (R10-030 OWN dual) |
| Ghidra | `FUN_0050e9f0` |
| Promoted | `StdTree_Lrotate_Isnil21_Inferred` |

## Machine-level notes

- Live re-verify 2026-08-05: decompile ≡ raw CF; leaf; **RET 4** ×3; isnil test `80 7E 21 00`.
- Node layout: **+0 left**, **+4 parent**, **+8 right**, **+0x20 color**, **+0x21 isnil**.
- Tree shell: **this+4 = head**; **head+4 = root**.
- Classical `_Lrotate`: `y = x->right`; rehang `y->left` under `x`; promote `y` into `x`'s parent slot; hang `x` as `y->left`.
- Twin Rrotate `FUN_005a27f0` (isnil@+0x21; residual, not OWN here).
- Peer dualed: `StdTree_Lrotate_Isnil29` `0x004192a0` (byte-identical except isnil imm `0x29` vs `0x21`).
- Shared helper — **not** skill/reaction-specific despite old auto alias chain.

## Pseudocode (annotated)

```c
// __thiscall; ECX = tree*; stack = Node* x; void; RET 4
void __thiscall StdTree_Lrotate_Isnil21_Inferred(int tree, int x)
{
  int *y;       // x->right
  int *parent;

  y = *(int **)(x + 8);                    // y = x->right
  *(int *)(x + 8) = *y;                    // x->right = y->left
  if (*(char *)(*y + 0x21) == '\0') {      // if y->left is not isnil
    *(int *)(*y + 4) = x;                  //   y->left->parent = x
  }
  y[1] = *(int *)(x + 4);                  // y->parent = x->parent
  if (x == *(int *)(*(int *)(tree + 4) + 4)) {
    // x was root
    *(int **)(*(int *)(tree + 4) + 4) = y; // root = y
    *y = x;                                // y->left = x
    *(int **)(x + 4) = y;                  // x->parent = y
    return;                                // ret 4
  }
  parent = *(int **)(x + 4);
  if (x == *parent) {
    // x was left child
    *parent = (int)y;
    *y = x;
    *(int **)(x + 4) = y;
    return;                                // ret 4
  }
  // x was right child
  parent[2] = (int)y;
  *y = x;
  *(int **)(x + 4) = y;
  return;                                  // ret 4
}
```

## Control flow

| Stage | Action |
|---|---|
| 1 | `y = x.right`; `x.right = y.left` |
| 2 | If `y.left` non-nil → `y.left.parent = x` (isnil@+0x21) |
| 3 | `y.parent = x.parent` |
| 4a | Root case → `head.parent = y` |
| 4b | Left-of-parent → `parent.left = y` |
| 4c | Right-of-parent → `parent.right = y` |
| 5 | `y.left = x`; `x.parent = y` |

## Open questions

- Product / MSVC demangle for each map instantiation using this family.
- Full dual of twin Rrotate `005a27f0` (role sealed; residual OWN elsewhere).
- Runtime / bit-exact / differential.
