# Annotated low-level: FUN_004082f0 → StdVector_InsertN_Elem12_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004082f0` |
| VA | `0x004082f0` |
| Body | `0x004082f0`–`0x00408587` (**663 B** / `0x297`) |
| System | util / container (`std::vector` InsertN, POD **elem 0xC**) |
| Date | 2026-07-23 scaffold; **2026-08-04 WQ9H-B** dual seal |
| Ghidra | `FUN_004082f0` |
| Named | `StdVector_InsertN_Elem12_Inferred` |

## Machine-level notes

- **thiscall**: **ECX** = vector* (begin@+4, end@+8, capEnd@+0xC).
- Stack: `T* where`, `uint count`, `const T* value` (pointer; 3 dwords snapped to locals).
- Element stride **0xC** throughout (`/0xc`, `*0xc`, magic `0x2AAAAAAB`).
- Max element count **`0x15555555`** (~`INT_MAX/12`).
- Growth: **1.5×** (`cap + cap>>1`), else exact `size+count` via `FUN_00480c80`.
- **`ret 0xC`** — three stack formals cleaned by callee.
- Sole listed caller: InsertOne rebind `FUN_00406e70` (count=1).

## Recovered control flow

```c
void __thiscall InsertN(Vec* this, T* where, uint count, const T* value)
{
  Pod12 tmp = *value; // 3 dwords
  size_t cap = begin ? (capEnd-begin)/12 : 0;
  if (count == 0) return;
  size_t size = begin ? (end-begin)/12 : 0;
  if (max - size < count) length_error(); // FUN_00418130
  if (cap < size + count) {
    // grow 1.5x or size+count; newbuf; prefix copy; Ufill; suffix copy; delete; rebind triad
    return;
  }
  // in-place: two sub-cases by (end-where)/12 vs count
  //  near-end: uninit_copy where..end → where+count; Ufill hole; end += count*12
  //  far: uninit_copy end-count..end → end; copy_backward; assign-fill hole
}
```

## Call graph

| Direction | VA | Role |
|---|---|---|
| Callee | `00409b40` / `0040a590` | uninit_copy stride 0xC |
| Callee | `00406ee0` | Ufill N (dualed WQ9G-I) |
| Callee | `00480fb0` | copy_backward 3-dword |
| Callee | `0042ac90` | assign-fill with template |
| Callee | `00480c80` | size() |
| Callee | `00418130` | overflow |
| Caller | `00406e70` | InsertOne count=1 |
| Twin | `00408050` | InsertN elem **0x28** (WQ9H-A) |
| Twin | `004073a0` / `00419880` | InsertN **dword** |

## Open (residual)

- Product demangle / English type of 12-byte element.
- Full duals of uninit_copy / copy_backward / assign-fill leaves.
- Runtime / bit-exact / differential.
