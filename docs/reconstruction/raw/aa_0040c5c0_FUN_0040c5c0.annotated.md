# Annotated low-level: Client_UiToastQueue_Push (`FUN_0040c5c0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0040c5c0` |
| VA | `0x0040c5c0` |
| System | missions-progression (shared UI toast) |
| Date | 2026-07-23 scaffold; dual seal 2026-07-29 |

## Machine-level notes

- Source: raw + live Ghidra decompile 2026-07-29 + `read_memory`.
- **ABI:** `__thiscall`, one stack arg (element*), exits **`ret 4`** (`C2 04 00`).
- **Queue:** begin `*(this+4)`, end `*(this+8)`, capacity-end `*(this+0xC)`. `this+0` unused here.
- **Stride:** element size **`0x98`** (size/cap via signed magic div; bump `ADD …,0x98`).
- **Fast path:** `FUN_00404670(end, 1, src)` → `FUN_00404710` copies **0x26 dwords**; end += 0x98.
- **Grow path:** `MOV ECX, queue`; `FUN_0040c720` → `FUN_00403980` (1.5× growth / insert).
- Parent decompile may show phantom args on `FUN_00404670` (caller pushes 5 dwords + `ADD ESP,0x14`); callee uses 3.
- Callers: `ADD ECX, 0xE8C8` after entity chain `*(*(char+4)+4+char+0xA8)`.
- Role: **toast queue push only** — not grant, not audio, not sprintf.

## Pseudocode (annotated)

```c
/* Client_UiToastQueue_Push — structural name; product open */
void __thiscall Client_UiToastQueue_Push(int queue /*this*/, undefined4 *pElement)
{
  int begin;
  int end;
  int size;
  int cap;

  begin = *(int *)(queue + 4);
  if ((begin != 0) &&
      ((uint)((*(int *)(queue + 8) - begin) / 0x98) <
       (uint)((*(int *)(queue + 0xc) - begin) / 0x98))) {
    end = *(int *)(queue + 8);
    /* POD construct one ToastElement at end */
    FUN_00404670(end, 1, pElement);   /* → FUN_00404710 dword copy ×0x26 */
    *(int *)(queue + 8) = end + 0x98;
    return;
  }
  /* capacity exhausted or empty buffer — grow + insert */
  FUN_0040c720(queue, /*outIt*/ &scratch, *(int *)(queue + 8), pElement);
  return;
}
```

## Callee roles (owned by this dual only as dependency)

| Addr | Role |
|------|------|
| `0x00404670` | Loop construct N elements of size 0x98 via copy |
| `0x00404710` | `memcpy`-equivalent 0x26 dwords |
| `0x0040c720` | Compute insert index; call grow insert |
| `0x00403980` | Reallocate / shift / place (MSVC-like vector growth) |

## Open questions

- Product/PDB names for queue type and API.
- Owner class of object at entity `+0xA8` (queue at `+0xE8C8`).
- Element field product English beyond offset table (see dual A).
