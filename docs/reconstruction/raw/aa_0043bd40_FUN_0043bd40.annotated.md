# Annotated low-level: FUN_0043bd40 → StdTree_OperatorIndex_Val12_U32Key_Eax_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0043bd40` |
| VA | `0x0043bd40` |
| System | std map operator[] / get-or-insert (Val12, unsigned key, EAX key*) |
| Date | 2026-08-04 (W37-Q OWN-ONLY dual) |
| Body | `0x0043bd40`–`0x0043bda5` exclusive (**101** B) |
| ABI | ECX=map; EAX=`uint32_t* key`; plain `ret`; returns `mapped*` @ node+0x10 |

## Machine-level notes

- Source: raw capture + live Ghidra decompile/`read_memory` (no `disassemble_bytes`).
- Val12 family: isnil@+0x19, key@+0x0c, mapped@+0x10/+0x14, color@+0x18 — same as W29-Q `StdTree_OperatorIndex_Val12`.
- **Unsigned** key compares (`jnb` family), not signed `jge` of `0x0053af20`.
- **Register key ABI**: key pointer enters in **EAX** (bytes `mov esi,eax`); **no** stack key / **no** `ret 4`.
- Sole callee: `FUN_0043be60` (insert-hint / rebalance — unowned).
- Call sites write two dwords into returned mapped (fn ptr + zero, or host ptr + zero).

## Pseudocode (annotated)

```c
// ECX = MapShell_Val12*  |  EAX = const uint32_t* key
// plain RET
// returns pointer to mapped (val1, val2) at node+0x10
uint32_t* StdTree_OperatorIndex_Val12_U32Key_Eax_Inferred(
    MapShell_Val12* map,
    const uint32_t* key /* EAX */)
{
    MapNode_Val12* head = map->head;          // *(this+4)
    MapNode_Val12* candidate = head;
    MapNode_Val12* node = head->left;          // root (header[1])

    if (node->isnil == 0) {
        // unsigned lower_bound: first node with key >= *key
        do {
            if (node->key < *key)             // unsigned
                node = node->right;
            else {
                candidate = node;
                node = node->left;
            }
        } while (node->isnil == 0);
    }

    // Hit: candidate in-tree and candidate.key <= *key → equality
    if (candidate != head && candidate->key <= *key)
        return &candidate->val1;              // node+0x10

    // Miss: insert Val12{*key, 0, 0} with hint=candidate
    Val12 v = { *key, 0, 0 };
    MapNode_Val12* outIt;
    FUN_0043be60(map, &outIt, candidate, &v); // register/stack ABI per helper
    return &outIt->val1;                      // *outIt + 0x10
}
```

## Call graph

| Direction | Symbol |
|---|---|
| Callee | `FUN_0043be60` (insert-hint helper) |
| Callers | `FUN_0073e3a0` (effect manager ctor register), `FUN_0096eec0` (NestedHash host register), `FUN_0072a660`, `FUN_0098d870` (Z-fill), `00733896` |

## Seal confidence

| Claim | Level |
|---|---|
| CF lower_bound + hit/miss | High |
| Unsigned key @ +0x0c / isnil @ +0x19 | High |
| ECX map + EAX key* + plain ret | High |
| Return mapped @ +0x10 | High |
| Insert stages `{key,0,0}` Val12 | High |
| Product map demangle / owner type | Open |
| `FUN_0043be60` full contract | Unowned |
