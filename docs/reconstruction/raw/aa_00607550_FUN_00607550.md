# Raw capture: FUN_00607550

| Field | Value |
|---|---|
| **Stable ID** | `aa_00607550` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00607550` |
| **Canonical name** | `FUN_00607550` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __fastcall FUN_00607550(int param_1)

{
  uint uVar1;
  int iVar2;
  uint uVar3;
  undefined4 uVar4;
  undefined *puVar5;
  
  puVar5 = &DAT_00d029e0;
  if (*(char *)(param_1 + 0x220) == '\0') {
    puVar5 = &DAT_00d029f0;
  }
  uVar1 = *(uint *)(param_1 + 0x23c);
  if (-1 < (int)uVar1) {
    iVar2 = *(int *)(puVar5 + 4);
    if (iVar2 == 0) {
      uVar3 = 0;
    }
    else {
      uVar3 = *(int *)(puVar5 + 8) - iVar2 >> 4;
    }
    if (uVar1 < uVar3) {
      if ((iVar2 != 0) && (uVar1 < (uint)(*(int *)(puVar5 + 8) - iVar2 >> 4))) {
        return *(undefined4 *)(uVar1 * 0x10 + 4 + iVar2);
      }
      uVar4 = FUN_00608620();
      return uVar4;
    }
  }
  return 0;
}
```

---

## WQ8R-E re-verify (2026-08-04) — append only

| Field | Value |
|---|---|
| **Tools** | `decompile_function` + `read_memory` + callers/xrefs/callees + `get_assembly_context` @ call site. **No** `disassemble_bytes` |
| **Live decompile** | ≡ raw 2026-07-23 CF |
| **Body** | `0x00607550`–`0x006075a8` exclusive (**88 B** / `0x58`); pad `CC`×7; next `Outpost_GetPulseXpPercent` @ `0x006075b0` |
| **ABI** | ECX=outpost `this` (`8B C1`); no stack formals; plain **`RET`** (`C3`); returns EAX dword |
| **Callees** | `FUN_00608620` (`"invalid vector<T> subscript"` noreturn throw) — pathological path only |
| **Callers** | `Outpost_CaptureOutpost` (`FUN_006082e0`) @ `0x006085cc` only |
| **Globals** | `DAT_00d029e0` (bank when `this+0x220 != 0`), `DAT_00d029f0` (bank when `+0x220 == 0`); BSS vector headers (`begin@+4`, `end@+8`) |
| **Classification** | worker / pure lookup (no host writes) |
| **Canonical (Inferred)** | `Outpost_GetScheduleEntryValueAtCursor_Inferred` |
| **System** | outpost / schedule (WQ-008 residual; sole caller is dualed CaptureOutpost) |
| **Reject scaffold** | `Named_CalleeOf_Named_CalleeOf_CVOGReaction_Dispatch_00607550` — wrong parent chain; sole code caller is CaptureOutpost |

### Full body hex (88 B)

```
8bc180b82002000000b9e029d0007505b9f029d000568bb03c02000085f67c358b510485d2750433c0eb088b41082bc2c1f8043bf0731e85d2740c8b41082bc2c1f8043bc67705e984100000c1e6048b4416045ec333c05ec3
```

### Byte-sealed algorithm

```
EAX = this (ECX)
table = DAT_00d029e0
if (*(char*)(this+0x220) == 0): table = DAT_00d029f0
ESI = *(uint32*)(this+0x23c)          // schedule cursor (signed)
if (ESI < 0): return 0
begin = *(table+4)
size  = begin ? ((*(table+8)-begin) >> 4) : 0   // stride 0x10
if (ESI >= size): return 0
// in-range path (MSVC-style operator[] recheck):
if (begin == 0 || size <= ESI): jmp FUN_00608620  // noreturn throw
return *(uint32*)(begin + ESI*0x10 + 4)           // entry field +4
```

### Call-site context (CaptureOutpost @ 0x006085cc)

```
// after setting +0x238 = scheduleCount (from DAT_00d029f8 size path) and +0x23c = 0:
CALL 0x00607550
// result → timerBlob(+0x248)[+8]; GetTickCount → timerBlob[+0x14]
```

### Twin (not owned)

- `Outpost_GetPulseXpPercent` @ `0x006075b0`: same bank/cursor/stride; returns **float at entry+8** instead of dword at entry+4.

