# Annotated low-level: CVOGPhaseDistort_ctor (FUN_00576ed0)

| Field | Value |
|---|---|
| Stable ID | `aa_00576ed0` |
| VA | `0x00576ed0`–`0x00576f11` exclusive (65 B / `0x41`) |
| System | client / VOGEnvironmentSector phases |
| Date | 2026-07-29 (W32-T dual seal) |
| Canonical | `CVOGPhaseDistort_ctor` |
| RTTI | **`.?AVCVOGPhaseDistort@@`** Confirmed |

## Machine-level notes

- SEH `LAB_009a514a` present in bytes; decompiler may elide frame.
- `mov eax,ecx` early keeps **this** in EAX through `ret 4`.
- Vtbl `PTR_FUN_009d3bc0`; COL `0x00aaed0c` → type_info `0x00af31a0`.
- Sole factory: `PalantirEnv_InitPhases_Inferred` lazy singleton `DAT_00b04818` (`new(0x18)` + view@env`+0xC0`).
- Related: dtor `FUN_00576f20`; init `FUN_00576d70`; method plate `CVOGPhaseDistort::AddPieceInstance()` @ `FUN_00576f70`.
- **Reject** scaffold name `Named_CalleeOf_Drive_NDRiver_fx_00576ed0`.

## Layout (object size **0x18**)

| Off | Role |
|---:|---|
| `+0x00` | vtbl `PTR_FUN_009d3bc0` |
| `+0x04` | GfxView* (ctor arg) |
| `+0x08` | not written by ctor (open / later fill) |
| `+0x0C` | 0 (freed in dtor if non-null) |
| `+0x10` | 0 |
| `+0x14` | 0 |

## Pseudocode (annotated)

```c
// __thiscall; ret 4; returns this
CVOGPhaseDistort* __thiscall CVOGPhaseDistort_ctor(CVOGPhaseDistort* self, void* view)
{
  // SEH LAB_009a514a
  self->view = view;                 // +0x04
  self->vtbl = &PTR_FUN_009d3bc0;    // CVOGPhaseDistort
  self->slot0c = 0;                  // +0x0C
  self->slot10 = 0;                  // +0x10
  self->slot14 = 0;                  // +0x14
  return self;
}
```

## Open questions

1. Product English for fields `+0x08` / piece-instance tails (method residual).
2. Runtime / bit-exact / differential — open.
