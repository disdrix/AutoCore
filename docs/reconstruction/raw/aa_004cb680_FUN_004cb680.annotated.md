# Annotated low-level: FUN_004cb680 → SkillCNDHash_ReleaseChainsToFreelist_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004cb680` |
| VA | `0x004cb680`–`0x004cb6e1` (**98 B**) |
| System | skills-abilities / skill-family CNDHash |
| Date | 2026-07-23 scaffold; **2026-08-04 WQ9F-A dual** |
| Canonical | `SkillCNDHash_ReleaseChainsToFreelist_Inferred` |

## Machine-level notes

- **ABI:** `__thiscall`; **ECX** = skill CNDHash*; bare **`ret`** (`C3`). Entry `mov ebx,ecx`.
- **Hash layout used:** table ptr `@+0x10`, **mask** `@+0x08` (post-alloc), freelist head `@+0x20`.
- **Bucket sentinel:** each table slot points at a 0xc bucket record; **chain head** = `bucket[+4]`.
- **Node layout (chain):** `+0` vtbl/freelist-next; `+0x08` owned payload*; `+0x0c` next chain link.
- Per node: save next `@+0xc` → stamp `PTR_FUN_009cb968` → if payload non-null `operator_delete` → zero payload → push node onto freelist (`*node = old freelist; freelist = node`).
- Clear each bucket chain head to **0**.
- Loop `i = 0 .. mask` inclusive (`jbe` vs `[ebx+8]`).
- Decompiler marks `operator_delete` noreturn — **false**; epilogue continues (bytes after call `add esp,4`).
- Sole caller: destroy bucket table `0x004cba60` (recreate + hash dtor path).

## Pseudocode (annotated)

```c
// void __thiscall SkillCNDHash_ReleaseChainsToFreelist_Inferred(SkillCNDHash *hash)
void __fastcall FUN_004cb680(int hash)
{
  uint bucketIndex = 0;
  do {
    // table[i] → bucket; chain = bucket->head (+4)
    undefined4 *node = *(undefined4 **)(*(int *)(*(int *)(hash + 0x10) + bucketIndex * 4) + 4);
    while (node != 0) {
      undefined4 *next = (undefined4 *)node[3];       // +0x0c
      *node = &PTR_FUN_009cb968;                       // temporary dtor stamp
      if ((void *)node[2] != 0) {                      // +0x08 payload
        operator_delete((void *)node[2]);              // returns (not noreturn)
      }
      node[2] = 0;
      *node = *(undefined4 *)(hash + 0x20);            // freelist next link
      *(undefined4 **)(hash + 0x20) = node;            // push freelist
      node = next;
    }
    // clear bucket head
    *(undefined4 *)(*(int *)(*(int *)(hash + 0x10) + bucketIndex * 4) + 4) = 0;
    bucketIndex = bucketIndex + 1;
  } while (bucketIndex <= *(uint *)(hash + 8));        // mask inclusive
}
```

## Open questions

- Product/PDB class English for skill CNDHash / node type behind `009cb968`.
- Full freelist pop / reuse path (e.g. `FUN_0059c8a0` after dtor destroy) not dualled here.
- Runtime / bit-exact open.
