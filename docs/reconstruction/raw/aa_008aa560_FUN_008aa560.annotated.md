# Annotated low-level: MissionDialog_ClearResponseButtonSlots_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_008aa560` |
| VA | `0x008aa560` |
| Ghidra | `FUN_008aa560` |
| Inferred name | `MissionDialog_ClearResponseButtonSlots_Inferred` |
| System | `missions-progression` / mission dialog chrome |
| Date | 2026-08-04 (WQ7R-C) |

## Machine-level notes

- Live Ghidra decompile ≡ scaffold raw (WQ7R-C re-verify).
- Body `0x008aa560`–`0x008aa5a6` exclusive (**70 B** / `0x46`); pad `CC`.
- **ABI:** **EBX = mission dialog host***; no stack args; plain `RET`. Decompiler `unaff_EBX` is the real this — sealed by sole caller `MOV EBX,EDI` @ `0x008ac136` before `CALL 0x008aa560`.
- Fixed **8** slots (not driven by `dialog+0x510` count).
- Dual banks:
  - `dialog+0x518` — 8× TFID pairs (`int32 lo/hi`), written to **−1/−1**
  - `dialog+0x708` — 8× widget pointers, destroyed via **host vtbl+0xBC** then zeroed
- Sole caller `FUN_008ac110` (`Mission_i_d_npc_2d_btn_response_xml`) uses this as **pre-rebuild wipe** before allocating new `i_d_npc_2d_btn_response.xml` buttons into `+0x708`.
- Does **not** send packets; not a journal updater.

## Param / local renames

| Raw | Meaning | Evidence |
|---|---|---|
| `unaff_EBX` | mission dialog UI* | caller `MOV EBX,EDI`; offsets `+0x518/+0x708` match dialog layout |
| `piVar3` | cursor on widget bank `+0x708` | `lea esi,[ebx+0x708]`; `+4` per iter |
| `piVar4` | cursor on TFID bank `+0x518` | `lea edi,[ebx+0x518]`; `+8` per iter |
| `iVar2` / EBP | slot counter = 8 | `mov ebp,8` |
| `iVar1` | widget ptr for slot | loaded before null test |
| vtbl+0xBC | destroy/release child widget | `call [edx+0xbc]` with ECX=dialog, arg=widget |

## Pseudocode (annotated)

```c
// EBX = dialog (register this)
void MissionDialog_ClearResponseButtonSlots_Inferred(void)
{
  int *widgets = (int *)((char *)dialog + 0x708);   // unaff_EBX + 0x1c2
  int *tfids   = (int *)((char *)dialog + 0x518);   // unaff_EBX + 0x146
  int n = 8;
  do {
    int w = *widgets;
    tfids[0] = -1;
    tfids[1] = -1;
    if (w != 0) {
      (**(code **)(*dialog + 0xbc))(w);  // destroy widget
      *widgets = 0;
    }
    tfids += 2;
    widgets += 1;
    n -= 1;
  } while (n != 0);
}
```

## Open questions

- Product name for vtbl+0xBC (DestroyChild / ReleaseWindow / similar).
- Whether all 8 slots are always live capacity or only lower `+0x510` count is used by rebuild (clear still wipes full 8).
- Exact TFID field semantics of the pair at `+0x518` (mission-key vs response id) — HandleButton consumes `dialog+0x518+idx*8` (external CHANGE_LOG seal).
