# Annotated low-level: FUN_0060b440

| Field | Value |
|---|---|
| Stable ID | `aa_0060b440` |
| VA | `0x0060b440`–`0x0060b49f` |
| System | heartbeat / missions-progression (pending use-object aux) |
| Date | 2026-07-29 (W26-T seal; prior scaffold 2026-07-23) |
| Proposed name | `CVOGHBPendingUseObject_OnHeartBeat_Inferred` (**Probable**) |

## Machine-level notes

- OnHeartBeat vtbl slot `+0x0C` on `PTR_FUN_009df668` (ctor `FUN_0060b380`).
- Prefer assembly for field map: decompiler mis-typed subclass fields as base debounce pads.
- Fail paths write **Remove** (`DAT_00af49ac` = 16) into out status.
- Success: commit resolve (`FUN_00535ed0(1)`) then `CVOGHBBase_RescheduleAfterFire`.

## Pseudocode (annotated)

```c
// __thiscall  ECX = CVOGHBPendingUseObject*
// stack uint32_t* pOutStatus; returns pOutStatus; ret 4
// EHBStatus: Continue=0, Remove=16 (DAT_00af49ac)
uint32_t* __thiscall FUN_0060b440(CVOGHBPendingUseObject* self, uint32_t* pOut)
{
  // Gates: owner, character, secondary, not stopped
  if (self->pOwnerObject != 0
      && self->pCharacter != 0          // +0x28 (decomp: param_1[1].pVTable)
      && self->pSecondaryObject != 0    // +0x24 (decomp: packed debounce)
      && self->cStopped == 0) {
    // Still the character's pending secondary?
    if (*(int*)((char*)self->pCharacter + 0xc9c) != (int)self->pSecondaryObject) {
      *pOut = DAT_00af49ac;  // Remove
      return pOut;
    }
    // Commit pending use-object on character
    FUN_00535ed0(/*this=character*/ 1);  // ResolveUseObjectPending(commit=1)
    CVOGHBBase_RescheduleAfterFire(self, pOut);
    return pOut;
  }
  *pOut = DAT_00af49ac;  // Remove
  return pOut;
}
```

## Control flow

```
OnHeartBeat(pOut):
  if !owner || !character(+0x28) || !secondary(+0x24) || stopped(+0x20):
    *pOut = Remove(16); return pOut
  if character[+0xc9c] != secondary(+0x24):
    *pOut = Remove(16); return pOut
  ResolveUseObjectPending(character, commit=1)
  RescheduleAfterFire(this, pOut)
  return pOut
```

## Layout (subclass)

| Off | Role |
|---|---|
| `+0x18` | pOwnerObject |
| `+0x20` | cStopped |
| `+0x24` | pSecondaryObject (set by ctor from RecordFirstTimeEvent target) |
| `+0x28` | pCharacter |

## Open questions

- Product English for HB class / first-time event period field `object+0x25c` (ctor arg ×1000 → period ms).
- Post-commit: does Resolve clear `+0xc9c` so a subsequent beat would Remove? (expected; residual runtime).
