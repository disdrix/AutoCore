# Raw capture: FUN_00437b00

| Field | Value |
|---|---|
| **Stable ID** | `aa_00437b00` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00437b00` |
| **Canonical name** | `FUN_00437b00` |
| **System** | arda2/storage (stoAbstractUnserializationFactory + stoChunkReader) |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_00437b00(int *param_1)

{
  undefined4 uVar1;
  int iVar2;
  undefined4 local_4;
  
  uVar1 = FUN_0076a900();
  FUN_00438c40();
  FUN_0046c1b0();
  iVar2 = FUN_00438c40();
  if (local_4 == *(int *)(iVar2 + 8)) {
    iVar2 = 0;
  }
  else {
    iVar2 = (**(code **)(local_4 + 0xc))(uVar1);
  }
  *param_1 = iVar2;
  if (iVar2 != 0) {
    uVar1 = (**(code **)(*(int *)(iVar2 + 4) + 4))();
    return uVar1;
  }
  return 0xffffffff;
}
```

---

## W36-A live re-verify (2026-07-29)

| Field | Value |
|---|---|
| **Tools** | `decompile_function` + `read_memory` + `analyze_function_complete` + `get_function_by_address` + callers (no `disassemble_bytes`) |
| **Body** | `0x00437b00`–`0x00437b57` (**88 B** / `0x58`); pad `CC` |
| **Live decompile** | ≡ scaffold raw CF; **byte correction** on creator call args + vfunc stack arg |
| **Preferred name** | `Sto_AbstractUnserializationFactory_LookupCreateUnserialize_Inferred` |
| **Classification** | worker |

### Full body hex (88 B)

```
83ec08568bf7e8f52d330089442404e82c1100008bf08d4c24048d442408e88d460300e8181100008b4c24083b48085e7405ff510ceb0233c085c08b4c240c8901740e8b50048d480457ff520483c408c383c8ff83c408c3
```

### Byte-level control flow (sealed from `read_memory`)

```
sub  esp, 8
push esi
mov  esi, edi                     ; EDI = stoChunkReader* (register input)
call FUN_0076a900                 ; stoChunkReader_PeekChunkTag (this=ESI) → EAX=FourCC
mov  [esp+4], eax                 ; local tag
call FUN_00438c40                 ; MapA GetInstance
mov  esi, eax                     ; ESI = map*
lea  ecx, [esp+4]                 ; &tag
lea  eax, [esp+8]                 ; &found_node
call FUN_0046c1b0                 ; NestedHash lookup
call FUN_00438c40                 ; MapA again (for sentinel)
mov  ecx, [esp+8]                 ; found node
cmp  ecx, [eax+8]                 ; == empty sentinel?
pop  esi
je   not_found
call dword ptr [ecx+0xc]          ; creator() — 0 stack args (sample: new+ctor)
jmp  have_obj
not_found:
xor  eax, eax
have_obj:
test eax, eax
mov  ecx, [esp+0xc]               ; param_1 out*
mov  [ecx], eax                   ; *out = created object or 0
je   fail
mov  edx, [eax+4]                 ; vtable* at object+4
lea  ecx, [eax+4]                 ; this = object+4
push edi                          ; stack arg = chunk reader
call dword ptr [edx+4]            ; vfunc slot 1 (thiscall, callee cleans 1 arg)
add  esp, 8                       ; free frame (after callee RET 4)
ret
fail:
or   eax, 0xFFFFFFFF
add  esp, 8
ret
```

### Decompiler corrections (bytes win)

| Decompile claim | Bytes | Verdict |
|---|---|---|
| `creator(uVar1)` passes peeked tag | `FF 51 0C` with **no PUSH** before call | **Correct to 0-arg creator** — sample `FUN_00444d00` is `void→ptr` |
| vfunc `()` no args | `PUSH EDI` then `FF 52 04` | **EDI (chunk reader) is stack arg** |
| `local_4` naming | tag at `[esp+4]` after `push esi`; node at `[esp+8]` | Locals = tag + node out |

### ABI (sealed)

| Item | Evidence |
|---|---|
| Convention | **cdecl** frame; plain `RET`; caller `ADD ESP,4` (one stack arg) |
| Stack arg | `param_1` = `int*` / object\*\* out slot |
| Register arg | **EDI** = `stoChunkReader*` (moved to ESI for Peek; later pushed to vfunc) |
| Returns | EAX = vfunc status on success; **`0xFFFFFFFF`** if create failed / miss |
| Side effect | `*param_1 = created_object or 0` |

### Callers

| Caller | Sites (xrefs) | Role |
|---|---|---|
| `FUN_00765740` | `0x00765b93`, `0x00765c43`, `0x00765f73`, `0x00765fe3` (**4**) | gfxBody unserialize path — loops vector slots, ORs status |

Call-site pattern: `PUSH out_slot; MOV EDI, reader; CALL FUN_00437b00; ADD ESP,4`.

### Callees

| VA | Role |
|---|---|
| `FUN_0076a900` | `stoChunkReader_PeekChunkTag` (ESI this) |
| `FUN_00438c40` | MapA GetInstance (×2) |
| `FUN_0046c1b0` | NestedHash lookup (×1) — unowned W36-B |
| indirect `[node+0xc]` | registered creator |
| indirect `[*(obj+4)+4]` | created object unserialize vfunc |

### Pair

Insert/register path: `FUN_004449b0` (same Map A, writes creator at `+0xc`).
