# Annotated low-level: FUN_004bc580 → List_RemoveAll

| Field | Value |
|---|---|
| Stable ID | `aa_004bc580` |
| VA | `0x004bc580`–`0x004bc67b` (**252 B**) |
| System | client::list (CS-guarded intrusive list) |
| Date | 2026-08-04 (W37-U dual seal) |

## Machine-level notes

- Source: raw capture + W37-U re-verify (decompile + read_memory; no `disassemble_bytes`).
- **ABI:** ECX = list host; bare **`RET`** (`C3`). Decompiler `__fastcall` matches ECX formal.
- Layout (family with `List_TraversalLock` / `List_IterateNext`):
  - `+0x04` — `CRITICAL_SECTION`
  - `+0x1c` — head pointer
  - `+0x20`, `+0x24` — companion heads / cleared with head
  - `+0x28` — traversal-lock flag (non-zero blocks RemoveAll)
- Node: vtbl @ `+0`, field @ `+4`, next @ `+8`.
- Product string seals name: `"List Error!  RemoveAll Call Stack:\n%s\n"` @ `0x00a467ec`.
- Mid-body SEH landing (chkstk `0x1004`, stack walk, VOG_DEBUG_STOP) is **not** main CF — decompiler correctly omits from primary path.
- Throw `0x80070005` when locked (same HRESULT used by `List_IterateNext` lock fail).

## Control flow (sealed)

```
EnterCriticalSection(host+4)
if (host[+0x28]) { LeaveCS; throw 0x80070005 }
while (node = host[+0x1c]) {
  host[+0x1c] = node[+8]
  node[+4] = 0
  (*node->vtbl)(node, 1)   // scalar deleting dtor
}
host[+0x24] = host[+0x20] = host[+0x1c] = 0
LeaveCriticalSection(host+4)
```

## Open questions

- Product C++ class name of list host / node payload types across callers.
- Whether `+0x20`/`+0x24` are secondary chains or cursor/count fields (always zeroed; drain walks only `+0x1c`).
- Runtime / bit-exact / differential.
