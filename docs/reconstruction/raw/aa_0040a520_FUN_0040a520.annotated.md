# Annotated low-level: FUN_0040a520

| Field | Value |
|---|---|
| Stable ID | `aa_0040a520` |
| VA | `0x0040a520` |
| System | STL / vector POD helpers (elem stride 0x28) |
| Date | 2026-08-05 (WQ9J-A OWN dual) |

## Machine-level notes

- Source: raw capture for `aa_0040a520` + live re-verify.
- Prefer assembly / `read_memory` when decompiler invents empty callees or false thiscall-on-vector.
- Range **uninitialized_copy** for **0x28**-byte elements: half-open `[src_begin, src_end)` → `dest`.
- Sole element leaf: dualed `StdVector_PodCopyElement_0x28_Inferred` (`0x00409f30`).
- Callers: `FUN_00408050` (InsertN / reallocate path, 2 sites); trampoline `FUN_00409ae0`.
- Twin range worker for Elem12: `FUN_0040a590` (step +0x0C; residual, not this OWN).

## Pseudocode (annotated)

```c
// ABI: ECX=src_end; stack src_begin, dest; returns advanced dest in EAX; plain RET.
// Decomp "__thiscall" is free helper (no vector this).
void* StdVector_UninitializedCopy_Elem0x28_Inferred(
    /*ECX*/ const void* src_end,
    /*stack*/ const void* src_begin,
    /*stack*/ void* dest)
{
  // SEH: LAB_009bd0b0, try level 0
  while (src_begin != src_end) {
    // machine: MOV EDX,ESI; MOV EAX,EDI; CALL 00409f30
    StdVector_PodCopyElement_0x28_Inferred(dest, src_begin);
    dest = (char*)dest + 0x28;
    src_begin = (const char*)src_begin + 0x28;
  }
  return dest;
}
```

## Sealed facts

| Fact | Evidence |
|---|---|
| Body 101 B | `0040a520`–`0040a584` + full hex |
| Stride 0x28 both cursors | `ADD EDI,0x28` / `ADD ESI,0x28` |
| PodCopy leaf 00409f30 | CALL target + EAX/EDX setup |
| ECX = end | `MOV EBX,ECX` then `CMP ESI,EBX` |
| Return advanced dest | `MOV EAX,EDI` before epilogue |
| SEH frame | `PUSH 0x9bd0b0` / FS:[0] |

## Open questions

- Product / PDB English for the 0x28 POD element type (same residual as PodCopy leaf).
- Full dual of trampoline `00409ae0` and parent InsertN `00408050` (roles sealed).
- Runtime / bit-exact / differential.

## Name

**StdVector_UninitializedCopy_Elem0x28_Inferred** — role/CF sealed; product demangle open → `_Inferred`.
