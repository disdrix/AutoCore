# Annotated low-level: FUN_00437e40 → StdVector_Resize_Elem56_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00437e40` |
| VA | `0x00437e40`–`0x00437f1e` exclusive (**222 B**) |
| System | MSVC `vector<T>::resize(n, value)`, T size **0x38** (two `basic_string`) |
| Date | 2026-08-04 (W38-F) |

## Machine-level notes

- Source: raw capture + W38-F re-verify (`decompile` + `read_memory`). **No** `disassemble_bytes`.
- **Bytes win** on ABI: **EDI=vec***, **ECX=new_size**, stack **0x38** by-value fill, **`RET 0x38`**.
- Decompiler `unaff_EDI` / `__fastcall(param_1)` understates register-vec + stack-value formals.
- Size math: magic `0x92492493` → ÷ **0x38**; grow `FUN_00438520`; shrink `FUN_00438430`.
- Epilogue: `FUN_00437da0(&stack_value)` — element dtor destroys **two** MSVC `basic_string` at **+0** and **+0x1c** (T layout evidence).
- Reject scaffold `Named_CalleeOf_Named_gfxBody_*`.

## Pseudocode (bytes-informed)

```c
// EDI = StdVectorElem56* vec
// ECX = uint32_t new_size
// stack = Elem56 value (0x38); RET 0x38
void StdVector_Resize_Elem56_Inferred(StdVectorElem56* vec /*EDI*/,
                                      uint32_t new_size /*ECX*/,
                                      Elem56 value /*stack*/)
{
  Elem56* begin = vec->begin; // +4
  uint32_t size = begin ? (uint32_t)((char*)vec->end - (char*)begin) / 0x38 : 0;

  if (size < new_size) {
    FUN_00438520(vec, vec->end, new_size - size, &value); // InsertN grow
  } else if (begin && new_size < size) {
    Elem56* new_end = (Elem56*)((char*)begin + new_size * 0x38);
    Elem56* it_tmp;
    FUN_00438430(vec, &it_tmp, new_end, vec->end); // erase [new_end, end)
  }
  // destroy stack value (two basic_string)
  FUN_00437da0(&value);
  // RET 0x38
}
```

## Open questions

- Product/PDB English for T (dual-string element under gfxBody residual only).
- Unowned InsertN `FUN_00438520` / erase `FUN_00438430` dual seal.
- Runtime / bit-exact / differential.
