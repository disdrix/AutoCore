# Annotated low-level: FUN_0043c4f0 → GuardedVector_PushBackLocked

| Field | Value |
|---|---|
| Stable ID | `aa_0043c4f0` |
| VA | `0x0043c4f0`–`0x0043c54d` (**94 B**) |
| System | STL / guarded ring-deque container |
| Date | 2026-07-23 scaffold; W30-D seal 2026-07-29 |

---

## Machine-level notes

- Optional critical-section wrapper around `FUN_0043c830` / `GuardedVector_PushBack`.
- Retail ABI: **EDI**=container, stack=`value*`, **`RET 4`**. Decompiler `void(param_1)` omits EDI.
- Gate `container+0x2c` (char); CS at `container+0x14` (24-byte CRITICAL_SECTION through `+0x2b`).
- SEH frame (`LAB_009be0c8`) so Leave runs on exception unwind.
- Nested PushBack dual-sealed W29-B (EAX=container, page-map push_back).
- Sole caller `FUN_007b75b0` sets `EDI = *DAT_00d1f058 + 0x58` (resolver GuardedVector; same slot as `AssManager_RemoveResolver`).

---

## Pseudocode (annotated)

```c
// EDI = GuardedVector*; stack value*; RET 4
void GuardedVector_PushBackLocked(const uint32_t* value /*stack*/)
{
  // SEH: state=-1, handler LAB_009be0c8
  if (*(char*)(edi + 0x2c) != 0)
    EnterCriticalSection((LPCRITICAL_SECTION)(edi + 0x14));
  // SEH state = 0
  // retail: mov eax, edi; push value; call FUN_0043c830
  GuardedVector_PushBack(/*EAX*/ edi, value);
  if (*(char*)(edi + 0x2c) != 0)
    LeaveCriticalSection((LPCRITICAL_SECTION)(edi + 0x14));
}
```

---

## Open residual

- Product/PDB class English (inferred `GuardedVector_*` family only).
- Runtime / bit-exact under contended CS.
