# Annotated low-level: FUN_00461ef0 → RcElem28_AssignBackward

| Field | Value |
|---|---|
| Stable ID | `aa_00461ef0` |
| VA | `0x00461ef0`–`0x00461f7a` exclusive (**138 B**) |
| System | MSVC-style RcElem28 assign-backward / copy_backward (stride **0x1c**) |
| Date | 2026-07-23 scaffold; W35-P seal 2026-07-29 |
| Canonical name | `RcElem28_AssignBackward` (**Inferred**) |

## Machine-level notes

- **Bytes win** on ABI: **EAX=dest_end**, stack **first\*** / **last\***, plain **`RET`**, returns final dest in **EAX**.
- Same RC resource gates as `RcElem28_AssignFillRange` / W34-P insert-n.
- Sole parent: `StdVector_InsertN_RcElem28` mid-insert path @ `0x00461a7c`.
- Reject scaffold `Named_CalleeOf_*_gfxDevice_*`.

## Signature (sealed)

```c
// EAX=dest_end*, stack (first*, last*); plain RET; returns dest after reverse
RcElem28* RcElem28_AssignBackward(
    RcElem28* dest_end,        /* EAX */
    RcElem28* first,           /* stack */
    RcElem28* last);           /* stack */
```

## Control flow (annotated)

1. If `first == last` → return `dest_end` (EAX).
2. Step source/dest back one element (`-0x1c`).
3. Loop: RC-assign `*dest = *source` (AddRef/Release); step both back; until source cursor == `first`.
4. Return final dest pointer (start of written range).

## Full hex

See raw W35-P append (138 B).
