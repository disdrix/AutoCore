# Annotated low-level: InventoryGrid_CompleteDtor (`FUN_005725a0`)

| Field | Value |
|---|---|
| Stable ID | `aa_005725a0` |
| VA | `0x005725a0`–`0x0057264d` inclusive (**174 B** / `0xAE`) |
| Ghidra name | `FUN_005725a0` |
| Canonical name | `InventoryGrid_CompleteDtor` |
| System | inventory-transfer |
| Date | 2026-08-05 (MEGA-061 OWN dual; supersedes 2026-07-23 scaffold) |
| Authority | `read_memory` full body + `disassemble_function` (Ghidra decompile truncated at false-noreturn) |

---

## Purpose

MSVC **complete-object destructor** for the InventoryGrid host (`PTR_FUN_009d3390`, size **0x68**). Reverse of `InventoryGrid_ctor_Inferred` (`0x00572650`). Called only from dualed scalar-deleting dtor `InventoryGrid_ScalarDeletingDtor` (`0x00573910` / vtbl[0]). Does **not** free the host heap block.

---

## ABI (sealed)

| Item | Value | Evidence |
|---|---|---|
| Convention | `__thiscall` / ECX this | `MOV EDI,ECX` @ `0x005725bb` |
| Stack formals | none | bare `RET` (`C3`) |
| Return | void (no EAX contract) | epilogue no `MOV EAX,…` |
| Cleanup | `ADD ESP,0x14; RET` after SEH | `0x0057264a`–`0x0057264d` |

```c
// ECX = InventoryGrid* this; void; bare RET
void __thiscall InventoryGrid_CompleteDtor(InventoryGrid *self);
```

---

## Control flow (bytes authority)

```text
SEH frame (LAB_009a4f42)
self->vtbl = PTR_FUN_009d3390
InventoryGrid_ClearItemsAndReEmptyCells_Inferred(self)   // FUN_00570f70
if (self->cellArray /*+0x28*/ != null)
    operator_delete[](cellArray)
self->cellArray = null
// map at +0x5c:
FUN_00573700(map, *head, head)   // erase all nodes
operator_delete(head)            // free sentinel @ +0x60
map._Myhead = 0; map._Mysize = 0
// list at +0x2c:
list.vtbl = PTR_FUN_009d3384; FUN_004bc950(list)
list.vtbl = PTR_FUN_009cb334; FUN_004bc950(list)
DeleteCriticalSection(&self->cs /*+0x30*/)
restore FS:[0]; RET
```

| Stage | Match | Conf |
|---|---|---|
| Vtbl reinstall `009d3390` | `C7 07 90 33 9D 00` | **High** |
| Clear items first | `CALL 00570f70` before free cells | **High** |
| `delete[]` cells @ +0x28 | `CMP/JZ` + `CALL 0048981c` | **High** |
| Tree erase + free sentinel | `00573700` then `00489822` | **High** |
| List dual-vtbl drain | `9d3384`/`9cb334` + `004bc950`×2 | **High** |
| DeleteCS @ +0x30 | IAT `[009c61f0]` after `ADD ESI,4` | **High** |
| Bare RET (not RET 4) | `C3` | **High** |
| Not host free | no `operator_delete(this)` | **High** |

---

## Annotated pseudocode (bytes-complete; not truncated decompile)

```c
// Ghidra decompile stops after operator_delete(sentinel) — false noreturn.
// Full CF recovered from read_memory + disassemble_function.

void __thiscall InventoryGrid_CompleteDtor(InventoryGrid *self)
{
  // SEH: push -1, LAB_009a4f42, FS:[0]
  self->vtbl = &PTR_FUN_009d3390;                 // +0x00

  InventoryGrid_ClearItemsAndReEmptyCells_Inferred(self); // 0x00570f70

  void *cells = self->cellArray;                  // +0x28
  if (cells != nullptr)
    operator_delete[](cells);
  self->cellArray = nullptr;

  // RB-map object at +0x5c; _Myhead at +0x60; size at +0x64
  Map *map = &self->itemMap;                      // +0x5c
  void *head = map->_Myhead;                      // +0x60
  FUN_00573700(map, /*out*/ &tmp, *head, head);   // erase [begin, end)
  operator_delete(head);
  map->_Myhead = nullptr;
  map->_Mysize = 0;

  // Embedded TraversalLock list at +0x2c (CS at +0x30 = list+4)
  ListHost *list = &self->itemList;               // +0x2c
  list->vtbl = &PTR_FUN_009d3384;
  FUN_004bc950(list);                             // drain under CS
  list->vtbl = &PTR_FUN_009cb334;
  FUN_004bc950(list);                             // base step (empty)

  DeleteCriticalSection(&self->cs);               // +0x30; IAT 009c61f0
  // restore ExceptionList / RET
}
```

---

## Callers / callees

| Direction | Target | Note |
|---|---|---|
| Caller | `InventoryGrid_ScalarDeletingDtor` `0x00573910` | sole code CALL; dualed **accept** |
| Callee | `InventoryGrid_ClearItemsAndReEmptyCells_Inferred` `0x00570f70` | dualed MEGA-090 |
| Callee | `operator_delete[]` `0x0048981c` | cells |
| Callee | `FUN_00573700` | map erase-range (residual) |
| Callee | `operator_delete` `0x00489822` | sentinel |
| Callee | `FUN_004bc950` | list drain helper (residual) |
| Callee | `DeleteCriticalSection` IAT | `this+0x30` |

---

## Related (not owned)

| VA | Name | Relation |
|---|---|---|
| `0x00573910` | `InventoryGrid_ScalarDeletingDtor` | vtbl[0]; calls this then optional free |
| `0x00572650` | `InventoryGrid_ctor_Inferred` | reverse construction order |
| `0x00570f70` | `ClearItemsAndReEmptyCells` | first stage of complete dtor |
| `0x00570720` | `AllocateCellArray` | used by clear (re-empty) |

---

## Open / residual

1. Product/MSVC demangle string for InventoryGrid class (naming-only; family sealed).
2. Tree key/type English for map @ `+0x5c` / erase helper `00573700` (not dualed here).
3. Full dual of list helper `004bc950` (behavior sealed enough for this VA's CF).
4. Runtime / bit-exact / differential — open (no Launcher).
