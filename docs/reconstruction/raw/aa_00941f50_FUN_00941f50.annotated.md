# Annotated low-level: Client_UseInventoryItemByCoid

| Field | Value |
|---|---|
| Stable ID | `aa_00941f50` |
| VA | `0x00941f50` |
| System | inventory-transfer / skills-abilities |
| Date | 2026-08-04 (WQ8R-I dual seal) |
| Ghidra | `FUN_00941f50` |

## Machine-level notes

- **EAX = Client*** at entry (`mov esi, eax`).
- Stack: `coidLo`, `coidHi`, `mode` → **`ret 0x0C`**.
- Grid path: `client+0xE98` (character) → `+0x250` (cargo host) → `+0x2B0` (**cargo InventoryGrid***).
- Invalid / missing host / null character / COID sentinel `lo&hi == -1` → **AL = 0**.
- Success path: `FindItemByCoid` → **EDI = item**, **ESI = client**, tail `Client_UseInventoryItem_Inferred(mode)`.
- Decompiler `return uVar1 & 0xffffff00` is **noise** — image uses `xor al, al`.
- **No** null check on Find result before Use (caller / Use body residual).

## Pseudocode (byte-accurate)

```c
// EAX = Client*
// ret 0x0C; AL = bool
uint8_t Client_UseInventoryItemByCoid(uint32_t coidLo, uint32_t coidHi, uint32_t mode)
{
  Client *client = /* EAX */;
  Character *ch = *(Character **)((char *)client + 0xE98);
  if (ch == NULL)
    return 0;
  void *host = *(void **)((char *)ch + 0x250);
  if (host == NULL)
    return 0;
  if ((coidLo & coidHi) == 0xFFFFFFFFu)
    return 0;

  InventoryGrid *cargo = *(InventoryGrid **)((char *)host + 0x2B0);
  Item *item = InventoryGrid_FindItemByCoid(cargo, coidLo, coidHi);
  // ESI = client, EDI = item  (register convention for Use)
  return Client_UseInventoryItem_Inferred(mode); // 0x00941d50
}
```

## Call graph

```
orphan UI @ 0x0086152C  (skill-bit gate on item)
        │  EAX=g_client(0x00D1A840), push mode/coid
        ▼
Client_UseInventoryItemByCoid  (this VA)
        │  cargo FindItemByCoid
        ▼
Client_UseInventoryItem_Inferred  (0x00941d50)
        ├─ equip arm FUN_00941b20
        └─ skill arm C2S 0x2045 size 0x28
```

## Open / residual

- Product/PDB symbol.
- Whether Find-null is always prevented by callers.
- Full recovery of orphan UI function containing `0x0086152C` (Ghidra function gap).
- Runtime / bit-exact.
