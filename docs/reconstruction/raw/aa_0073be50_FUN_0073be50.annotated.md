# Annotated low-level: GlyphCacheList_ForEach_OnDeviceReset_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0073be50` |
| VA | `0x0073be50`–`0x0073be74` exclusive (**36 B**) |
| System | graphics / GlyphCache device-reset broadcast |
| Date | 2026-08-04 (W38-U dual) |

## Machine-level notes

- **ABI:** **EBX** = list container; no stack args; return **0**; plain `RET`.
- **List:** circular sentinel at `*(EBX+8)`; walk `node = node->next` (`*node`); stop when `node == sentinel`.
- **Payload:** ESI = `*(node+0x2c)` before `CALL FUN_00755820` — same +0x2c GlyphCache* slot as W37-AE map node.
- **Caller:** sole site inside `GfxDevice_Reset` success path (`MOV EBX,[device+0x758]`).
- Decompiler `unaff_EBX` / parameterless `FUN_00755820()` incomplete — **bytes win**.

## Pseudocode (annotated)

```c
// EBX = container live-in. Returns 0.
uint32_t GlyphCacheList_ForEach_OnDeviceReset_Inferred(void* container /*EBX*/)
{
  ListNode* sentinel = *(ListNode**)((char*)container + 8);
  ListNode* node = sentinel->next;   // *sentinel
  if (node != sentinel) {
    do {
      void* payload = *(void**)((char*)node + 0x2c);  // GlyphCache*
      FUN_00755820(payload);                           // ESI = payload
      node = node->next;
    } while (node != *(ListNode**)((char*)container + 8));
  }
  return 0;
}
```

## Open questions

- Exact product type of manager at `device+0x758`.
- Full dual of per-cache hook `FUN_00755820`.
- Runtime confirmation on device-reset path.
