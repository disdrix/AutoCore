# Annotated low-level: StdList_Destroy_FreeHead_ViaClearEsi_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00402280` |
| VA | `0x00402280` |
| Body | `0x00402280`–`0x0040229c` inclusive (**29 B** / `0x1D`) |
| Canonical name | `StdList_Destroy_FreeHead_ViaClearEsi_Inferred` |
| Ghidra name | `FUN_00402280` |
| System | shared `client::stdlist` (partition hint: missions-progression / createNDUIDialogs nested) |
| Date | 2026-08-05 (MEGA-067 OWN dual refresh) |
| Terminal | **false** |

## Machine-level notes

- Source: raw capture + MEGA-067 live re-verify (append-only).
- Prefer assembly / `read_memory` when decompiler conflicts — **false noreturn** on `operator_delete` drops null-head store and epilogue.
- ABI: **ECX** = list shell*; bare **RET**; void.
- Callee clear leaf is dualed **`StdList_Clear_ESI`** (`0x00415e90`) — ESI register ABI; this unit converts thiscall→ESI then free head.
- Twin destroy VA `0x00403430` uses Clear-ECX path (`0x00404060`) with identical outer CF (bytes differ only in CALL displacements).
- Partition hint `Mission_createNDUIDialogs nested` is **parent seed noise**: unit is shared list-shell dtor (SEH locals + `eh_vector` of 4×0xC shells under UI object `vtbl 00a4c61c` / `i_d_q.xml`).

## Assembly (authoritative)

```text
00402280  PUSH ESI
00402281  MOV  ESI, ECX              ; thiscall list* → ESI
00402283  CALL StdList_Clear_ESI     ; 0x00415e90 — free nodes, re-ring head, size=0
00402288  MOV  EAX, [ESI+4]          ; sentinel*
0040228b  PUSH EAX
0040228c  CALL operator_delete       ; 0x00489822
00402291  ADD  ESP, 4
00402294  MOV  dword ptr [ESI+4], 0  ; null head (decomp-hidden)
0040229b  POP  ESI
0040229c  RET
```

## Pseudocode (byte-complete; not decompiler-truncated)

```c
// ECX = StdListShell* (head @ +4, size @ +8)
void __fastcall StdList_Destroy_FreeHead_ViaClearEsi_Inferred(StdListShell* list)
{
  // machine: ESI = list; CALL StdList_Clear_ESI (uses ESI)
  StdList_Clear_ESI(list);                 // FUN_00415e90
  operator_delete(list->head);             // free sentinel
  list->head = nullptr;
}
```

## Layout

| Offset | Field | Role here |
|---|---|---|
| +0 | unused / allocator | untouched |
| +4 | head / sentinel* | cleared of elements, then deleted, then nulled |
| +8 | size | zeroed inside clear leaf |

Shell size at `eh_vector` sites: **0xC** (count 4 @ host `+0x5cc`).

## Call graph (OWN VA only)

| Role | Target |
|---|---|
| Callees | `StdList_Clear_ESI` `0x00415e90`; `operator_delete` |
| Unwind callers | 10× `LEA ECX,local; JMP 00402280` |
| Structured parent (evidence) | `FUN_008a5ba0` dtor path / `FUN_008a5c90` ctor path — vector of 4 shells |
| Ctor pair | `FUN_004023f0` (alloc empty sentinel) |
| Twin destroy | `0x00403430` via Clear-ECX |

## Open questions / gaps

- Product demangle `list<T>` / exact element node payload (clear leaf only `operator_delete`s nodes).
- Full English plate for host class `PTR_FUN_00a4c61c` / `i_d_q.xml` window (out of OWN scope).
- Runtime / bit-exact / differential.

## Rejected names

- `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Mission_createNDUI_00402280` — parent-seed scaffold; not mission-unique.
- Merge with `0x00403430` / `0x00497360` as same VA — clones only.
- Mission createNDUIDialogs primary identity — falsified by multi-site SEH + shared stdlist CF.
