# Raw capture: FUN_00424890

| Field | Value |
|---|---|
| **Stable ID** | `aa_00424890` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00424890` |
| **Body** | `0x00424890`–`0x004248bb` (`ret` inclusive) |
| **Canonical name** | `FUN_00424890` (structural: `Rng_Mt19937_Seed`) |
| **System** | RNG / MT19937 |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00424890(undefined4 param_1,uint param_2)

{
  uint *in_EAX;
  int iVar1;
  
  iVar1 = 1;
  *in_EAX = param_2;
  do {
    in_EAX[1] = (*in_EAX >> 0x1e ^ *in_EAX) * 0x6c078965 + iVar1;
    iVar1 = iVar1 + 1;
    in_EAX = in_EAX + 1;
  } while (iVar1 < 0x270);
  return;
}
```

---

## Live re-seal (W19-A dual, 2026-07-29)

| Field | Value |
|---|---|
| **Tool** | Ghidra `decompile_function` + `read_memory` + `analyze_function_complete` + `get_function_by_address` |
| **Program** | `autoassault.exe` |
| **Live decompile** | ≡ frozen raw CF above (unchanged) |
| **Body range** | `00424890`–`004248bb` |
| **Classification** | leaf |
| **Xrefs** | 2 (`007a4308`, `007a423f`) |
| **Callers** | `FUN_007a4200`, `FUN_007a42d0` |
| **Callees** | none |

### Byte seal (`read_memory` @ `0x00424890` length 48)

```
b9 01 00 00 00        ; mov  ecx, 1
89 10                 ; mov  [eax], edx          ; mt[0] = seed (EDX)
56                    ; push esi
8b 10                 ; mov  edx, [eax]
8b f2                 ; mov  esi, edx
c1 ee 1e              ; shr  esi, 0x1e
33 f2                 ; xor  esi, edx            ; mt[i-1] ^ (mt[i-1] >> 30)
69 f6 65 89 07 6c     ; imul esi, esi, 0x6c078965
03 f1                 ; add  esi, ecx            ; + i
89 70 04              ; mov  [eax+4], esi        ; mt[i]
83 c1 01              ; add  ecx, 1
83 c0 04              ; add  eax, 4
81 f9 70 02 00 00     ; cmp  ecx, 0x270
7c de                 ; jl   loop
5e                    ; pop  esi
c3                    ; ret
cc cc cc cc           ; int3 pad → twist sibling @ 004248c0
```

### ABI note (bytes supersede decompiler param names)

| Register | Role |
|---|---|
| **EAX** | `uint32* mt` state base (in/out walk pointer) |
| **EDX** | seed (`uint32`) written to `mt[0]` |
| **ECX** | loop index (starts 1; not a this-pointer) |
| Stack | none; near `RET` (`C3`) |

Decompiler `param_1` is unused phantom storage; seed is **EDX**, state is **EAX**.
