# Annotated low-level: FUN_005d6870 → Math_Float3_IsFiniteIEEE_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005d6870` |
| VA | `0x005d6870`–`0x005d68ab` (60 B) |
| System | math / IEEE finite gate (float3) |
| Date | 2026-07-29 (W27-J live seal) |
| Sibling | `Math_Float4_IsFiniteIEEE_Inferred` @ `0x005d68b0` (same mask; loop bound 4) |

## Machine-level notes

- **ABI:** `__thiscall` — **ECX** = `float[3]*` (or any 3 consecutive float32 words); stack arg = `uint8_t *outOk`; **`RET 4`**.
- **Leaf:** no calls; two return sites (fail early / success).
- **Test:** for `i ∈ {0,1,2}`: if `(as_u32(v[i]) & 0x7f800000) == 0x7f800000` → Inf/NaN class → `*outOk = 0` and return; else after all three pass `*outOk = 1`.
- **Does not** test denormal specially (denorm/zero pass); **does not** test magnitude, unit length, or normalize.
- **EAX residual:** last `MOV EAX,[esp+8]` leaves `outOk` pointer in EAX — callers often treat return as `char*` to the out flag (Ghidra void signature still correct as primary ABI).
- **Scratch:** `PUSH ECX` provides a stack slot for `MOVSS` → integer bit test (no FPU).

## Pseudocode (annotated)

```c
// Math_Float3_IsFiniteIEEE_Inferred — Ghidra FUN_005d6870
// *outOk = 1 iff none of three float32 words has all-ones exponent (Inf/NaN).
void __thiscall FUN_005d6870(float *vec3 /* ECX */, uint8_t *outOk /* stack */)
{
  int i = 0;
  do {
    // IEEE-754 binary32: exp field bits [30:23] all 1 ⇒ Inf or NaN
    if ((*(uint32_t *)(vec3 + i) /* as dword at +i*4 */ & 0x7f800000u) == 0x7f800000u) {
      *outOk = 0;
      return;
    }
    i = i + 1;
  } while (i < 3);
  *outOk = 1;
  return;
}
```

(Decompiler form uses `param_1 + iVar1 * 4` on `int` this — same addressing.)

## Callers (live `get_xrefs_to` / `get_function_callers`)

| Site / parent | Role (context) |
|---|---|
| `VehicleAction_applyAction` @ `0x005991d0` | angular impulse finite gate before apply |
| `FUN_005cedf0` / CVOGHBAI_UpdateAimOffset | aim delta finite before normalize |
| `FUN_004e8dc0`, `FUN_004e9060` | math path gates |
| `FUN_004c6360`, `FUN_00517680`, `FUN_0053d170`, `FUN_005c2030` | various float3 validity |
| `FUN_004dcb80` ×3, `FUN_004deb20`, `FUN_004dfcc0` | sector/map save / bulk paths |
| + `0x0060644c` | additional xref site |

## Open / residual

- Product/PDB symbol string — none in body (**Inferred** name).
- Runtime golden samples — deferred (mask logic sealed).
