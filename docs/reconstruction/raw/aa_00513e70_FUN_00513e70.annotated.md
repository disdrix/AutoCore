# Annotated low-level: Item_IsStackEligible (`FUN_00513e70`)

| Field | Value |
|---|---|
| Stable ID | `aa_00513e70` |
| VA | `0x00513e70` |
| System | `inventory-transfer` |
| Date | 2026-07-23 scaffold; annotated dual 2026-07-29 |

## Machine-level notes

- Source: raw capture for `aa_00513e70` + live re-decompile/read_memory 2026-07-29.
- **Eligibility only** — not stack capacity (`0x0040abf0`).
- **Broken** read is **inline** (`item+0x17c` bit 19); setter is sibling `0x00513de0`.
- Prefer assembly when decompiler conflicts (here: equivalent).

## Pseudocode (annotated)

```c
// Item_IsStackEligible — ECX = item*
// return 1 if stack paths may treat item as merge/display-stackable

undefined4 __fastcall FUN_00513e70(int param_1 /* Item* item */)
{
  // holder = item+0xa8 (type / clonebase holder)
  // blob   = holder+0x3c
  // stackFlag = blob[+0x3f2] & 0x40
  // broken    = item[+0x17c] bit 19 (ItemFlag_Broken)
  // typeClass = holder+0x38

  if ((((*(byte *)(*(int *)(*(int *)(param_1 + 0xa8) + 0x3c) + 0x3f2) & 0x40) == 0) ||
      ((*(uint *)(param_1 + 0x17c) >> 0x13 & 1) != 0)) &&
     (*(int *)(*(int *)(param_1 + 0xa8) + 0x38) != 4)) {
    return 0;  // non-type4 and (no stack flag or broken)
  }
  return 1;    // type4 OR (stack flag && !broken)
}
```

## Offsets

| Path | Offset | Role |
|------|--------|------|
| item | `+0xa8` | type holder |
| holder | `+0x38` | type class; **4** → always eligible |
| holder | `+0x3c` | clonebase blob* |
| blob | `+0x3f2` bit `0x40` | stack-eligible flag |
| item | `+0x17c` bit 19 (`0x80000`) | `ItemFlag_Broken` (blocks) |

## Open questions

- Product taxonomy of type class `4`.
- Original PDB name.
