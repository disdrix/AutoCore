# Annotated low-level: CNDUIWndBuffered_CompleteDtor (`FUN_0078ca80`)

| Field | Value |
|---|---|
| Stable ID | `aa_0078ca80` |
| VA | `0x0078ca80`–`0x0078caef` (**112 B**) |
| System | client / ND UI buffered window |
| Date | 2026-08-05 (WQ9K-F dual seal) |
| Product name | `CNDUIWndBuffered_CompleteDtor` |
| Ghidra | `FUN_0078ca80` |

## Machine-level notes

- Source: raw capture + WQ9K-F re-verify (`disassemble_function` + full-body `read_memory`).
- Prefer assembly when decompiler conflicts (none here).
- Role: MSVC **complete destructor** for **CNDUIWndBuffered** (RTTI `.?AVCNDUIWndBuffered@@`).
- ABI: ECX = this; SEH `LAB_009b3fee`; bare `RET`; does **not** free host.

## Signature

```c
// ECX = this. SEH LAB_009b3fee. ADD ESP,0x10; C3. void.
void __fastcall CNDUIWndBuffered_CompleteDtor(void* self);
```

## Control flow (annotated)

```c
void __fastcall FUN_0078ca80(undefined4 *self /* ECX */)
{
  // SEH frame: LAB_009b3fee
  *self = &PTR_FUN_00a99f74;           // CNDUIWndBuffered primary vtbl
  // EH state = 0
  FUN_007a8580();                      // shared UI teardown helper
  FUN_0078c3d0();                      // buffered-window derived helper (thiscall ESI)

  // HostBase / nested at +0x2A8 (same slot ctor allocates via new(0x80))
  void *nested = (void*)self[0xAA];    // +0x2A8
  if (nested) {
    // scalar-deleting dtor of nested: vtbl[0](nested, 1)
    (*(code**)(*(undefined4**)nested))(1);
  }
  self[0xAA] = 0;                      // always null after free path

  // EH state = -1
  FUN_007b5be0();                      // base CNDUIWindow complete
  // SEH restore; return (no free of self)
}
```

## Field / offset map

| Offset | Index | Role |
|---|---|---|
| `+0x00` | `[0]` | vtbl → `PTR_FUN_00a99f74` |
| `+0x2A8` | `[0xAA]` | owned nested HostBase*; scalar-deleted then nulled |

## Peers

| Peer | VA | Note |
|---|---|---|
| Scalar | `0x004293f0` | vtbl[0]; complete then `flags&1` delete |
| Ctor | `0x0078caf0` | `CNDUIWndBuffered_Ctor_Inferred` (W34-T) |
| Base complete | `0x007b5be0` | `CNDUIWindow_CompleteDtor` (OWN WQ9K-F pair) |
| Derived caller | `0x00834520` | `CWndVehicle_CompleteDtor` (WQ9J-B) |

## Open residual

- Product English for `FUN_007a8580` / `FUN_0078c3d0`.
- Nested HostBase type at `+0x2A8` (ctor path: HostBase_DefaultCtor / EnsureAux).
- Runtime / bit-exact / differential.
