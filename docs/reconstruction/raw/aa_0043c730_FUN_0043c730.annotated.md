# Annotated low-level: FUN_0043c730 / GuardedVector_CopyAssign

| Field | Value |
|---|---|
| Stable ID | `aa_0043c730` |
| VA | `0x0043c730`–`0x0043c7d6` (**167 B**) |
| System | STL / guarded container (nested body of `GuardedVector_AssignTo`) |
| Date | 2026-07-23 scaffold; **2026-07-29 W26-G dual seal** |
| Canonical (inferred) | `GuardedVector_CopyAssign` |
| Ghidra | `FUN_0043c730` |

## Machine-level notes

- **Custom register ABI:** **EAX = source**, **EDI = dest**. No stack formals. Frame `SUB/ADD ESP,8`. Plain `RET`.
- Sole caller `FUN_0043c5f0` (`GuardedVector_AssignTo`) sets EAX/EDI then `CALL`.
- Container header (shared with lock wrapper CS at `+0x14`):
  - `+0x04` slot map pointer
  - `+0x08` capacity
  - `+0x0c` begin index (ring)
  - `+0x10` size
- Self-assign (`EDI == EAX`) is a pure no-op.
- Empty source → `FUN_0040d9c0` with **ESI = dest** (full clear: pop elements, delete map).
- Non-empty: branch on `source.size <= dest.size` → fit path (`FUN_0043ce90` + `FUN_0043c8c0`) else grow path (`FUN_0043ce90` + `FUN_0043cec0` → `FUN_0043cf40`).
- Nested helpers not owned; CF boundaries sealed from this unit + live decompile of helpers for shape only.

## Pseudocode (annotated)

```c
// Custom ABI: EAX=source, EDI=dest; void
void GuardedVector_CopyAssign(void /*EAX src, EDI dst*/)
{
  uint dest_size;
  int src_begin;
  int src;   // EAX
  int dst;   // EDI
  uint32_t local_8;
  uint32_t local_4;

  if (dst != src) {
    if (*(uint32_t *)(src + 0x10) == 0) {
      // ESI=dst inside FUN_0040d9c0
      FUN_0040d9c0();   // clear dest
      return;
    }
    dest_size = *(uint32_t *)(dst + 0x10);
    src_begin = *(int *)(src + 0x0c);
    if (*(uint32_t *)(src + 0x10) <= dest_size) {
      FUN_0043ce90();   // prep / erase entry
      FUN_0043c8c0(&local_8, local_8, local_4, dst,
                   *(int *)(dst + 0x10) + *(int *)(dst + 0x0c));
      return;
    }
    FUN_0043ce90();
    FUN_0043cec0(dst, dst,
                 *(int *)(dst + 0x10) + *(int *)(dst + 0x0c),
                 src, src_begin + dest_size, src,
                 *(int *)(src + 0x10) + *(int *)(src + 0x0c));
  }
  return;
}
```

## Call graph

| Dir | Target | Role |
|---|---|---|
| Caller | `FUN_0043c5f0` | optional CS then this assign |
| Callee | `FUN_0040d9c0` | clear dest when source empty |
| Callee | `FUN_0043ce90` | thin wrapper → `FUN_0043d390` |
| Callee | `FUN_0043c8c0` | fit/copy when dest has enough size |
| Callee | `FUN_0043cec0` | grow/copy path → `FUN_0043cf40` |

## Open questions

- Product/PDB class name (`GuardedVector` is structural inference from W25-R parent dual).
- Element type (helpers treat map as pointer blocks / ring of dword slots).
- Exact erase/grow arithmetic inside unowned `FUN_0043c8c0` / `FUN_0043cec0` / `FUN_0043cf40`.
- Runtime / bit-exact.
