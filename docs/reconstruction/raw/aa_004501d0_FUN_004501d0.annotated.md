# Annotated low-level: AnimEventVec_InsertN_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004501d0` |
| VA | `0x004501d0`–`0x00450526` exclusive (**854 B** / `0x356`) |
| System | client / animation events (vector insert) |
| Date | 2026-07-29 (W34-O; scaffold 2026-07-23 preserved in raw) |
| Canonical | `AnimEventVec_InsertN_Inferred` (Ghidra `FUN_004501d0`) |

## Machine-level notes

- **ABI:** Four **stack** formals — `vec*`, `where*`, `count`, `value*` — **not** ECX thiscall for the vector. Epilogue **`C2 10 00`** (`RET 0x10`).
- **SEH:** `LAB_009becf8`; states cover new/relocate/fill and local template lifetime.
- **Element:** stride **0x1c**; vtbl `PTR_FUN_009d4828`; fields `+4/+8`; nested range at `+0xc` via `FUN_00444100` / assign `FUN_0044ffa0` / destroy `FUN_00987560`.
- **Layout:** begin@`+4`, end@`+8`, capEnd@`+0xC` — same triad as `StdVector_Elem28_Size` (`0x00469c50`, W33-J).
- **Grow:** max elems `0x9249249`; 1.5× capacity; floor `size+count`.
- **Sole caller:** `FUN_00450050` resize — when `size < newSize`, inserts `newSize-size` value copies at `end`.
- Prefer bytes for `RET 0x10` and stack-arg polarity (decompiler sometimes loses register formals on callees).

## Pseudocode (annotated)

```c
// 4 stack args; RET 0x10; void
void AnimEventVec_InsertN_Inferred(
    void *vec, void *where, unsigned count, void *value)
{
  // local template from value: vtbl 009d4828 + +4/+8 + nested FUN_00444100
  // if count==0: destroy local; return
  // capacity = (capEnd-begin)/0x1c; size = (end-begin)/0x1c
  // if size+count > 0x9249249: FUN_004540b0()  // length_error
  // if capacity < size+count:
  //   newCap = max(cap+cap/2, size+count)  // via StdVector_Elem28_Size when needed
  //   buf = operator_new(newCap*0x1c)
  //   uninit_copy [begin,where) -> buf
  //   fill_n count value copies
  //   uninit_copy [where,end) after hole
  //   destroy+delete old range; rebind begin/end/capEnd
  // else in-place:
  //   if tail room < count: uninit_copy tail, fill, assign prefix hole
  //   else: uninit_copy tail out, copy_backward / assign loop FUN_0044ffa0
  // destroy local template (FUN_00987560)
}
```

## Open questions

- Product demangle for exact `anmAnimEvent` / element class of `PTR_FUN_009d4828`.
- Nested helpers `FUN_00450600` / `FUN_00450560` / `FUN_004501a0` / `FUN_00450530` / `FUN_0044ffa0` product English (free).
- Whether any other caller exists beyond resize (currently 1 xref).
- Runtime / bit-exact.
