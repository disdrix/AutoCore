# Raw capture: FUN_00431430

| Field | Value |
|---|---|
| **Stable ID** | `aa_00431430` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00431430` |
| **Canonical name** | `FUN_00431430` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall
FUN_00431430(undefined4 param_1,undefined4 param_2,undefined4 param_3,undefined4 param_4)

{
  FUN_0046a2c0(param_2,param_3,param_4,param_1,param_4);
  return;
}
```

---

## W33-H append (2026-07-29) — decompile + read_memory seal

| Field | Value |
|---|---|
| **Tools** | `decompile_function`, `analyze_function_complete`, `read_memory`, xrefs. **No** `disassemble_bytes`. |
| **Body (bytes)** | `0x00431430`–`0x00431450` exclusive (**32 B** / `0x20`); next fn `FUN_00431450` |
| **Ghidra body** | `00431430`–`0043144f` (matches) |
| **ABI** | **ECX=`vector*`** (this); stack **`first*`**, **`last*`**, **`dest*`**; **`RET 0x0C`** (`C2 0C 00`) |
| **Return** | **EAX** = end of constructed dest range (propagated from `FUN_0046a2c0`). Decompiler shows `void` — **bytes/use-site win** (`uVar3 = FUN_00431430(...)` in insert). |
| **Callee** | `FUN_0046a2c0` @ `0x0046a2c0` (string range uninitialized copy-ctor; stride 0x1c) — cdecl, caller `ADD ESP,0x14` |
| **Callers** | `FUN_004306b0` / `StdVector_InsertN_BasicString` @ `0x004308e2`, `0x00430976` only |
| **Name** | `StdVector_UninitializedCopy_BasicString` (**Inferred** structural thin thiscall rebind) |

### Semantics (bytes authority)

1. Load stack `dest` (`[esp+0xC]`), `last` (`[esp+8]`).
2. Push args for cdecl: `FUN_0046a2c0(first, last, dest, this, dest)` — 5 dwords, `ADD ESP,0x14`.
   - Core work uses **first/last/dest** (string placement copy-ctors in callee); `this` + trailing `dest` are forwarded extras (allocator/EH context residual; callee body uses the triad).
3. `RET 0x0C` — three stack formals cleaned by callee.

### Call-site roles (parent insert, not owned)

- Hole path: `Ucopy(where, end, where+count*0x1c)` then fill tail.
- Mid-shift: `end' = Ucopy(end-count, end, end)` then backward-move + fill hole.

### Full body hex (32 B)

```
8b44240c8b542408508b442408518b4c2414515250e8768e030083c414c20c00
```

Decompile ≡ forward CF; return-type corrected by use-site + no EAX clobber.
