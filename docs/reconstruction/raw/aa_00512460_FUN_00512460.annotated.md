# Annotated low-level: Object_SetRaceIdOwnerChain_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00512460` |
| VA | `0x00512460` – `0x00512487` |
| Ghidra name | `FUN_00512460` |
| Canonical name | `Object_SetRaceIdOwnerChain_Inferred` |
| System | object / faction |
| Date | 2026-07-29 (dual A/B W24-G) |

## Machine-level notes

- Source: raw capture + live Ghidra `decompile_function` / `read_memory` 2026-07-29.
- Prefer assembly when decompiler conflicts — **no conflict** (decompiler while ≡ byte loop).
- Leaf: no calls; no plate comment.
- Companion getter at `0x00512440` (`Object_GetRootRaceId`).

## Byte decode (authoritative)

```
00512460  83 B9 AC 00 00 00 00   cmp  dword ptr [ecx+0ACh], 0   ; owner?
00512467  8B 54 24 04            mov  edx, dword ptr [esp+4]    ; race/faction id
0051246B  8D 81 AC 00 00 00      lea  eax, [ecx+0ACh]          ; &owner
00512471  89 51 10               mov  dword ptr [ecx+10h], edx ; write self +0x10
00512474  74 0F                  jz   00512485                 ; no owner → done
00512476  8B 00                  mov  eax, dword ptr [eax]     ; cur = owner
00512478  89 50 10               mov  dword ptr [eax+10h], edx ; write cur +0x10
0051247B  05 AC 00 00 00         add  eax, 0ACh                ; &cur->owner
00512480  83 38 00               cmp  dword ptr [eax], 0
00512483  75 F1                  jnz  00512476                 ; next owner
00512485  C2 04 00               ret  4
```

## Field map

| Offset | Width | Role |
|---|---|---|
| `+0xAC` | ptr32 | Owner / parent object (null = root) |
| `+0x10` | s32 / DWORD | Race / faction id (`RACE_ID_INFERRED`) — **written** |

## Algorithm (annotated)

```
// thiscall: ECX = pObject, stack arg = raceId
*(pObject + 0x10) = raceId;          // always self
curOwnerSlot = pObject + 0xAC;
if (*curOwnerSlot == NULL) return;
while (true) {
  cur = *curOwnerSlot;               // advance to owner
  *(cur + 0x10) = raceId;            // write owner (eventually includes root)
  curOwnerSlot = cur + 0xAC;
  if (*curOwnerSlot == NULL) break;  // stop after writing object with null owner
}
```

Writes **self and every owner up to and including the root**. Does **not** stop at root without writing root.

## Pseudocode (annotated copy of raw)

```c
void __thiscall Object_SetRaceIdOwnerChain_Inferred(int pObject, undefined4 raceId)
{
  int iVar1;
  int *piVar2;
  
  iVar1 = *(int *)(pObject + 0xac);
  piVar2 = (int *)(pObject + 0xac);
  *(undefined4 *)(pObject + 0x10) = raceId;
  while (iVar1 != 0) {
    iVar1 = *piVar2;
    *(undefined4 *)(iVar1 + 0x10) = raceId;
    piVar2 = (int *)(iVar1 + 0xac);
    iVar1 = *piVar2;
  }
  return;
}
```

## Call surface (Ghidra)

| Kind | Value |
|---|---|
| Callees | none (leaf) |
| Named callers | `CVOGSpawnPoint_CreateCreature`, `CVOGSpawnPoint_CreateTemplateVehicle`, `VehicleNet_ReconcilePrediction`, `FUN_004ca910`, `FUN_005131b0`, `FUN_00565950`, `FUN_005d2600`, `FUN_0060a030`, `FUN_00615020`, `FUN_0061e410` |
| Extra call sites | `00562c52`, `0057d072`…`0057d380` (reaction/dispatch range) |

Domain use: spawn `FactionDirty` apply (`docs/NPC.md` §15.3), vehicle/driver faction push, reaction dispatch race writes.

## Open questions

- Product C++ member names for `+0xAC` / `+0x10`.
- Whether every call site intends root-inclusive rewrite vs leaf-only (body always rewrites chain).
- Cycle / corruption policy on owner graphs.
- Runtime / bit-exact / image diff.
