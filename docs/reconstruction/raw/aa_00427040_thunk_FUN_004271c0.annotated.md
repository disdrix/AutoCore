# Annotated low-level: StdVector_DestroyRuns_0x80_Thunk_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00427040` |
| VA | `0x00427040`–`0x00427044` inclusive (**5 B**) |
| Ghidra name | `thunk_FUN_004271c0` |
| Canonical name | `StdVector_DestroyRuns_0x80_Thunk_Inferred` |
| System | skills-abilities / UI text-layout SEH alias |
| Date | 2026-08-05 (R10-003 OWN dual) |
| Parent | `0x004271c0` `StdVector_DestroyRuns_0x80_Inferred` (MEGA-128 **accept**) |

## Machine-level notes

- Source: raw capture + R10-003 live re-verify for `aa_00427040`.
- **Bytes authority:** this VA is a pure **`JMP 0x004271c0`** (`E9 7B 01 00 00`). Decompiler follows the thunk and prints the parent body — do **not** treat the expanded decompile as local instructions at `0x00427040`.
- Parent dual seals the real tidy: EBX-this vector, stride **0x80**, per-elem `FUN_00426f70` (~wstring @+0x0C), `operator_delete(begin)`, zero begin/end/cap.
- Callers are **SEH Unwind JMP** slots only (`009ac305`, `009add47`), both do `MOV EBX,[EBP+4]; ADD EBX,0x80` before jumping here — same vector subobject offset as `UiTextLayout_CompleteDtor_Inferred` (`LEA EBX,[layout+0x80]`).

## Pseudocode (authoritative for THIS VA)

```c
// 0x00427040 — pure JMP thunk (5 bytes)
// ABI identical to parent: EBX = Vec80Shell*
void StdVector_DestroyRuns_0x80_Thunk_Inferred(void /* EBX = vector* */)
{
    // JMP 0x004271c0  →  StdVector_DestroyRuns_0x80_Inferred
    __asm { jmp StdVector_DestroyRuns_0x80_Inferred }
}
```

## Parent semantics (inherited; not re-dualed)

```c
// 0x004271c0 — dualed MEGA-128
// Vec80Shell: begin@+4, end@+8, cap@+0xC; elem stride 0x80
void StdVector_DestroyRuns_0x80_Inferred(/* EBX = Vec80Shell* v */)
{
    uint8_t* begin = v->begin; // [EBX+4]
    if (begin == nullptr) {
        v->begin = v->end = v->capacity = nullptr;
        return;
    }
    for (uint8_t* p = begin; p != v->end; p += 0x80) {
        FUN_00426f70(p); // ECX=p; destroys wstring at p+0x0C
    }
    operator_delete(v->begin);
    v->begin = v->end = v->capacity = nullptr;
}
```

## Call graph (this unit)

```text
Unwind@009ac2fc / Unwind@009add3e
  MOV EBX, [EBP+4]          ; host / layout*
  ADD EBX, 0x80             ; vector subobject @ +0x80
  JMP 0x00427040            ; THIS thunk
    JMP 0x004271c0          ; StdVector_DestroyRuns_0x80_Inferred
      CALL FUN_00426f70     ; per-elem ~wstring@+0x0C  [R10-026 residual]
      CALL operator_delete  ; free begin

UiTextLayout_CompleteDtor_Inferred (0x00797d70)  [dualed WQ9L-F]
  LEA EBX, [ESI+0x80]
  CALL 0x004271c0           ; direct to parent (not this thunk)
```

## Open questions

- Product English for 0x80 "run" element beyond sealed `wstring@+0x0C` (inherited `_Inferred`).
- Full dual of trampoline `FUN_00426f70` (R10-026 residual; not OWN).
- Runtime / bit-exact / differential (Terminal false).
