# Annotated low-level: VehicleCrew_ContainsMember_Inferred (FUN_005749d0)

| Field | Value |
|---|---|
| Stable ID | `aa_005749d0` |
| VA | `0x005749d0` |
| Canonical | `VehicleCrew_ContainsMember_Inferred` |
| Ghidra | `FUN_005749d0` |
| System | `skills-abilities` (crew / gather-mode 0xb helper; also destroy-object) |
| Date | `2026-08-04` (WQ9R-A dual) |

## Machine-level notes

- **Leaf** thiscall membership test on a 4-slot crew table at `this+0x198`.
- Slot stride **0x10**; compares first two dwords (COID lo/hi) only.
- Candidate COID loaded via SharedBase offset table: `*(candidate+4)` → `*(*+4)` → base; then `base+candidate+0x164/0x168` (same COID layout used by inventory TFID paths).
- Returns **byte bool** in AL (`0` / `1`). Decompiler `uint3`/`CONCAT31` is Ghidra noise — epilogue is `xor al,al` / `mov al,1` + `ret 4`.
- **Not** faction / friendly-fire math by itself; callers use it as **crew-roster membership**.
- Primary skill path: gather/filter mode **0xb** ("same vehicle / plant crew") via `FUN_0058a810` when `vehicle+0xCB0 != 0` (crew host pointer).
- Twin writer: `FUN_005755b0` fills free slot (`COID pair == 0xFFFFFFFF`) and bumps count `+0x1D8`.
- Twin clearer: `FUN_00574a90` zeros matching slot.

## Pseudocode (annotated, meaningful names)

```c
// __thiscall  RET 4
// Returns bool in AL (0 / 1)
bool VehicleCrew_ContainsMember_Inferred(
    CrewTableHost* this,   // ECX — often *(vehicle+0xCB0)
    void* candidate)       // stack+4 — object whose COID is tested
{
  if (candidate == nullptr) {
    return false;
  }
  int offBase = *(int*)(*(int*)((char*)candidate + 4) + 4);
  int coidLo = *(int*)(offBase + 0x164 + (int)candidate);
  int coidHi = *(int*)(offBase + 0x168 + (int)candidate);

  for (unsigned slot = 0; ; ) {
    int byteOff = (int)(slot * 0x10);
    if (coidLo == *(int*)((char*)this + 0x198 + byteOff) &&
        coidHi == *(int*)((char*)this + 0x19C + byteOff)) {
      return true;
    }
    slot = (unsigned char)((unsigned char)slot + 1);
    if (slot > 3) {
      return false;
    }
  }
}
```

## Callers (selected)

| Function | Role |
|---|---|
| `FUN_0058a810` mode `0xb` | Same-vehicle OR crew-host contains other vehicle/member |
| `Skill_FilterTargetForResolveList_Inferred` | Ally/crew accept arm (`skill+0x615&0x20`, host `+0xCB0`) |
| `FUN_0058cd60` | Area gather pair filter residual |
| `FUN_005755b0` | Guard before insert (already member → skip) |
| `Client_RecvDestroyObject` | Crew-related destroy UX / cleanup gates |
| `FUN_007ffef0`, `FUN_0082fb30`, `FUN_00930fc0` | Client residual |

## Open gaps

- Product English / MSVC demangle for crew-host class (vtable of `this`).
- Whether `+0x198` slots always hold vehicle COIDs vs character COIDs (callers pass both vehicle from `vtbl+0x210` and resolved members).
- Field English for remaining 8 bytes of each 0x10 slot (writer stores object* at `(slot+0x1A)*0x10`).
- Runtime / bit-exact / differential (no Launcher).
