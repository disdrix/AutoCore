# Annotated low-level: FUN_00574a90 / VehicleCrew_ClearMemberObject_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00574a90` |
| VA | `0x00574a90`–`0x00574af4` exclusive (100 B) |
| System | `skills-abilities` (crew roster; clear path for Client_ClearObjectReferences) |
| Date | 2026-08-04 WQ9D-G dual (scaffold 2026-07-23) |

## Machine-level notes

- Source: raw + live Ghidra decompile / `read_memory` / analyze complete / callers (2026-08-04).
- **`__thiscall` + `ret 4`**: ECX = crew host; stack = candidate object*.
- **AL bool** return (miss `xor al,al`; hit `mov al,1`).
- ECX preserved as **candidate** through the scan (only ESI/EDX/EAX/EDI/EBX used in loop) so `FUN_00520330` thiscall targets the **member**, not the host.

### Crew host layout (partial)

| Offset | Role | Evidence |
|---|---|---|
| `+0x198` + `i*0x10` | COID lo | scan `cmp [edx], edi` |
| `+0x19c` + `i*0x10` | COID hi | scan `cmp [edx+4], ebx` |
| `(i+0x1A)*0x10` | object* | hit: `mov dword [eax+esi], 0` → slots `+0x1A0..+0x1D0` |
| `+0x1D8` | member count (byte) | insert twin; **not** touched here |

### Candidate COID

Same SharedBase path as Contains/Insert:

```
offBase = *(*(candidate+4)+4)
coidLo  = *(offBase + candidate + 0x164)
coidHi  = *(offBase + candidate + 0x168)
```

### FUN_00520330

```
// __thiscall ret 4
*(this + 0xCB0) = arg;
```

Clear path: `arg=0` → unlink member's crew-host back-pointer. Insert twin sets it to host.

### Odd behavior preserved

- **Does not** free the COID slot (`0xFFFFFFFF` sentinel used by insert free-scan).
- **Does not** decrement `+0x1D8`.
- After success, `VehicleCrew_ContainsMember` still matches that COID until another writer clears COID.

## Pseudocode (annotated)

```c
// __thiscall; RET 4; AL bool
uint8_t VehicleCrew_ClearMemberObject_Inferred(void *host, void *candidate)
{
  int off = *(int *)(*(int *)((char *)candidate + 4) + 4);
  int lo  = *(int *)(off + 0x164 + (int)candidate);
  int hi  = *(int *)(off + 0x168 + (int)candidate);
  int *slot = (int *)((char *)host + 0x198);
  int i = 0;
  while (slot[0] != lo || slot[1] != hi) {
    i++;
    slot += 4; /* +0x10 */
    if (i > 3) return 0;
  }
  *(int *)((char *)host + (i + 0x1A) * 0x10) = 0; /* object* */
  /* thiscall ECX=candidate */
  *(int *)((char *)candidate + 0xCB0) = 0; /* FUN_00520330(0) */
  return 1;
}
```

## Call site (`Client_ClearObjectReferences`)

When destroyed object has `vtbl+0x1dc()` non-null and local player object has non-null `+0xCB0` crew host:

```
host = *(localPlayerObj + 0xCB0)
cand = destroyed.vtbl+0x1dc()
if VehicleCrew_ClearMemberObject(host, cand) && ui:
  refresh UI vtbls
```

## Open questions

- Product English for crew-host class / whether COID leave-behind is intentional destroy-only partial clear.
- Slot bytes `+8/+0xC` within each 0x10 COID row (unused by this unit).
