# Annotated low-level: FUN_0085ce20 → Client_UI_ItemHover_ApplyRETinkerGadgetTooltip_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0085ce20` |
| VA | `0x0085ce20`–`0x0085d571` |
| System | `inventory-transfer` |
| Date | 2026-08-04 (WQ8R-H dual seal; prior scaffold 2026-07-23) |
| Tools | decompile + force_decompile + read_memory + assembly context; **no** disassemble_bytes |

## ABI (bytes + callers)

| Slot | Role | Evidence |
|------|------|----------|
| **EAX → ESI** | Target **item object\*** | Prologue `mov esi, eax`; null-check vs EDI=0 |
| **EBX** | **UI status/tooltip widget\*** | Prologue `cmp ebx, edi`; vtbl `+0x308` set text, `+0x314` set pos; `widget+0xce` enable flag |
| stack `[ebp+8]` | `char strictMode` | `cmp byte ptr [ebp+8], 0`; callers `PUSH 0` (`FUN_0085d900`) or `PUSH 1` (hover widgets); cdecl `ADD ESP,4` |
| return | void | leave / ret; no meaningful EAX |

Not thiscall. Decompiler `in_EAX` / `unaff_EBX` incomplete — **bytes win**.

## Globals

| Symbol | Role |
|--------|------|
| `DAT_00d1b6d8` | Local character / player host |
| `DAT_00d1b9a8` | Cursor / UI host (vtbl+0x120 screen point) |
| `DAT_00d1d900` | Multi-mode / pending field = **`client+0x30c0` abs** — here **0=RE, 4=TinkerKit, 5=Gadget** |
| `DAT_00d1d938`/`93c` | Pending **TinkeringKit** TFID lo/hi |
| `DAT_00d1d940`/`944` | Pending **Gadget** TFID lo/hi |
| `g_flZero` | Zero float compare for RE skill gate |

## Mode map (`DAT_00d1d900`)

| Value | Path | Primary strings / RTTI |
|------:|------|------------------------|
| **0** | Reverse Engineer status | "Broken items cannot be further Reverse Engineered."; craft mat type `0x1a`; "Cannot be Reverse Engineered."; "(No Recipe)."; stack split; "Requires At Least %d Reverse Engineering" |
| **5** | Gadget attach status | RTTI `CVOGGadget`; codes from `FUN_00513400`: 0 ok (cursor path), 1 no slots, 2 wrong type, 3 duplicate, else generic refuse |
| **4** | Tinkering Kit status | RTTI `CVOGTinkeringKit`; codes from `FUN_00513c10`: 0 no slots, 1 skill, 2 kit too primitive, 3 category mismatch (Power Plants/Weapons/Vehicles/Wheelsets/Armor/Other), 4 ok |
| other | Clear tooltip (`vtbl+0x308(NULL)`) + set cursor crosshair via `FUN_007fd420` | early return (no final pos bind) |

## Shared item flags

- **Broken:** `(obj+0x17c >> 19) & 1` ≡ `obj[0x5f]` bit 19 — RE further-block (when `strictMode==0`); TinkerKit hard refuse.
- **Craft material type:** `*(obj->def + 0x38) == 0x1a`.
- **Stack qty:** vtbl `+0x25c` on item; `FUN_00513e70` stackability helper.

## Epilogue (most success/error paths)

```
widget->vtbl[+0x308](msgBuf);          // set status text (or empty)
pt = cursorHost->vtbl[+0x120](...,1,0); // screen point
widget->vtbl[+0x314](pt.x, pt.y);      // place tooltip
```

Ok paths when `widget+0xce != 0` may call `FUN_007fd420` (cursor icon) with widget vtbl `+0x140`/`+0x120` points before/with text.

## Callers (6 xrefs)

| Site | Context |
|------|---------|
| `FUN_0085d900` @ `0085d92f` | `PUSH 0`; char has `+0x48c` item and `char+0xcd0==0` |
| `008b59b2`, `008b90ad`, `008c218f`, `009059a4`, `00909b70` | Inventory/item UI; `PUSH 1`; EBX/EAX preload item + widget |

## Classification

**Worker / UI presenter** — does not send C2S transfer packets; only composes local status text for craft-mode hover/refresh.

## Name

`Client_UI_ItemHover_ApplyRETinkerGadgetTooltip_Inferred`

Reject scaffold `Skill_You_do_not_possess_the_Tinkering_skill_to_work_o*` (single-string scrape; function covers RE+Gadget+Tinker).
