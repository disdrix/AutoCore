# Annotated low-level: NpcMissionDialog_TeardownChildWidgetLists (`FUN_008aa320`)

| Field | Value |
|---|---|
| Stable ID | `aa_008aa320` |
| VA | `0x008aa320` |
| Body | `0x008aa320`–`0x008aa3ef` exclusive (**207** B) |
| Canonical name | `NpcMissionDialog_TeardownChildWidgetLists` |
| Prior / alias | `FUN_008aa320` |
| System | missions-progression / client UI |
| Date | `2026-07-29` (W21-T OWN-ONLY dual) |
| Tools | Ghidra `decompile_function` + `read_memory` (+ function meta / xrefs / assembly context). **No** `disassemble_bytes` |

---

## Machine-level notes

- **ABI:** **ESI = NpcMissionDialog*** (same object as `Client_MissionDialog_FlushPreparedResponse`); bare **`ret`**; saves **EBX / EBP / EDI**. **Not** ECX-thiscall — sole caller keeps ESI live across the call.
- **Two vector-like pointer ranges** on the dialog (begin/end pairs, count = `(end-begin)>>2` when begin ≠ 0):
  1. `ESI[0x1cb]` / `ESI[0x1cc]` → bytes **`+0x72c` / `+0x730`**
  2. `ESI[0x1cf]` / `ESI[0x1d0]` → bytes **`+0x73c` / `+0x740`**
- **Per non-null child:**
  1. `child->vtbl+0x440()` — same vtable slot index as FlushPreparedResponse on the dialog (destroy/close style virtual).
  2. `dialog->vtbl+0xb0(child)` — host remove/detach (ECX = dialog, stack arg = child ptr).
- **Does not** zero begin/end, free vector storage, or clear `dialog+0x650` prepared response (that is Flush's job separately / not here).
- **Leaf of named FUN_***: only virtual calls. Classified "leaf" by complete-analysis for non-virtual callees.
- **Sole caller:** `Client_MissionDialog_FlushPreparedResponse` @ `0x008ab998` — after optional wire flush + reward UI cleanup; before dialog `vtbl+0x3ac` and `FUN_00792490`.

## Layout

| Field | Dword index | Byte offset | Role |
|---|---|---|---|
| vec1 begin | `0x1cb` | `+0x72c` | first dynamic child list |
| vec1 end | `0x1cc` | `+0x730` | one-past-end |
| vec2 begin | `0x1cf` | `+0x73c` | second dynamic child list |
| vec2 end | `0x1d0` | `+0x740` | one-past-end |
| child vtbl+0x440 | — | — | per-child teardown virtual |
| dialog vtbl+0xb0 | — | — | remove/detach child from host |

## Pseudocode (annotated)

```c
// ESI = NpcMissionDialog* dialog
// bare ret; no stack args
void NpcMissionDialog_TeardownChildWidgetLists(void)
{
  int count, i;
  int *begin;

  // ---- list 1 @ +0x72c / +0x730 ----
  if (dialog[0x1cb] == 0)
    count = 0;
  else
    count = (dialog[0x1cc] - dialog[0x1cb]) >> 2;

  for (i = 0; i < count; i++) {
    int *slot = (int *)(dialog[0x1cb] + i * 4);
    if (*slot != 0) {
      (**(code **)(*(int *)*slot + 0x440))();           // child vtbl+0x440
      (**(code **)(*dialog + 0xb0))(                    // dialog vtbl+0xb0
          *(int *)(dialog[0x1cb] + i * 4));
    }
  }

  // ---- list 2 @ +0x73c / +0x740 ----
  if (dialog[0x1cf] == 0)
    count = 0;
  else
    count = (dialog[0x1d0] - dialog[0x1cf]) >> 2;

  for (i = 0; i < count; i++) {
    int off = i * 4;
    if (*(int *)(dialog[0x1cf] + off) != 0) {
      (**(code **)(**(int **)(dialog[0x1cf] + off) + 0x440))();
      (**(code **)(*dialog + 0xb0))(
          *(int *)(dialog[0x1cf] + off));
    }
  }
}
```

## Confidence

| Claim | Level |
|---|---|
| ESI dialog ABI; bare ret | **High** |
| Two begin/end vector pairs + count formula | **High** (bytes + decompile) |
| Per child: vtbl+0x440 then dialog vtbl+0xb0 | **High** |
| No vector clear / no +0x650 touch | **High** |
| Only caller = FlushPreparedResponse teardown half | **High** |
| Product English of the two lists | Open (dynamic reward/option widgets inferred) |
| Exact product meaning of vtbl+0xb0 | Open (remove/detach role **High** structurally) |
| Runtime / bit-exact | Open |
