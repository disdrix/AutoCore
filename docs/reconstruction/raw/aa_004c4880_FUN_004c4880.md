# Raw capture: FUN_004c4880

| Field | Value |
|---|---|
| **Stable ID** | `aa_004c4880` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004c4880` |
| **Canonical name** | `FUN_004c4880` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_004c4880(int param_1,uint *param_2)



{

  int iVar1;

  

  *(uint *)(param_1 + 0x108) = *param_2;

  *(uint *)(param_1 + 0x10c) = param_2[1];

  *(uint *)(param_1 + 0x110) = param_2[2];

  *(uint *)(param_1 + 0x114) = param_2[3];

  iVar1 = *(int *)(param_1 + 8);

  if (iVar1 != 0) {

    if (((*(uint *)(param_1 + 0x108) & *(uint *)(param_1 + 0x10c)) == 0xffffffff) &&

       (*(char *)(param_1 + 0x110) == '\0')) {

      *(undefined4 *)(iVar1 + 0x28) = 0xb;

    }

    else {

      *(undefined4 *)(iVar1 + 0x28) = 10;

    }

    FUN_00560e90(*(undefined4 *)(param_1 + 8),1);

  }

  return;

}
```

---

## Live re-decompile (2026-07-29 W26-F)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` + `analyze_function_complete` |
| **Program** | `autoassault.exe` base `0x400000` |
| **Body** | `0x004c4880`–`0x004c48f1` (**114 B** inclusive last byte of `RET 4`; pad `CC` after) |
| **Epilogue** | `5F 5E C2 04 00` (`POP EDI; POP ESI; RET 4`) — **thiscall**, 1 stack formal |
| **Classification** | worker (1 callee) |
| **Callees** | `FUN_00560e90` / `WorldObj_RebindActivate_Inferred` @ `0x00560e90` (call site `0x004c48e8` → `0x00560e90`) |
| **Callers (1)** | `FUN_00626890` @ call `0x0062690d` |
| **Xrefs** | 1 unconditional CALL |
| **Integrity** | Live decompile ≡ 2026-07-23 raw body (identical CF) |

### Machine hex (full body, 114 B)

```
8b44240456578b388d91080100008bf2893e8b7804897e048b7808897e088b400c89460c8b410885c074428b3223720483feff7506807a08007409c740280a000000eb07c740280b000000518bc4c600018b51048b41088b52048b8c0aa80000008b89a4e4000050e8a3c509005f5ec20400
```

### Sealed ABI

```
// thiscall; ECX = object; stack[0] = const TFID_16* / uint32[4]* source
// void Object_SetTFID_ApplyPhysMode_Rebind(Object* this, const uint32_t src[4])
// 1) memcpy 16 B TFID block → this+0x108..+0x114
// 2) if this+8 (phys/world child) non-null:
//      mode = ((tfid.lo & tfid.hi) == 0xFFFFFFFF && (char)tfid.word2 == 0) ? 0xB : 10
//      *(uint32*)(phys+0x28) = mode
//      WorldObj_RebindActivate(manager, phys, flag=1)  // decompiler surface: FUN_00560e90(phys,1)
// RET 4
```

### TFID / mode evidence (siblings, same object layout)

| Sibling | VA | Role |
|---|---|---|
| `FUN_004c4440` | `0x004c4440` | Loads `TFID_16` from `this+0x108` (`dwCoidLo/Hi`, +8/+12); resolves via `Object_ResolveFromTFID` when **not** invalid |
| `FUN_004c4620` | `0x004c4620` | Pure mode leaf: invalid TFID → **0xB**, else **10** (same predicate as this function) |

Invalid TFID predicate (shared):

```
(tfid[0] & tfid[1]) == 0xFFFFFFFF  &&  (char)tfid[2] == 0
```

### Call-site wiring (`FUN_00626890` @ `0x0062690d`)

`read_memory` near call:

```
lea edx, [base+esi+0x164]   ; subobject / TFID-bearing side
mov ecx, [ebp+0x6c0]        ; ECX = this for callee
push edx                    ; stack formal = source TFID block pointer
call FUN_004c4880
```

Outer flow: after entity resolve `vtbl+0x210`, apply TFID/mode rebind, then `FUN_0051f890(1)` and further HB/action setup. Decompiler of caller often shows only the LEA expression (thiscall ECX omitted / simplified).

### Live decompile (identical to raw)

```c
void __thiscall FUN_004c4880(int param_1,uint *param_2)

{
  int iVar1;
  
  *(uint *)(param_1 + 0x108) = *param_2;
  *(uint *)(param_1 + 0x10c) = param_2[1];
  *(uint *)(param_1 + 0x110) = param_2[2];
  *(uint *)(param_1 + 0x114) = param_2[3];
  iVar1 = *(int *)(param_1 + 8);
  if (iVar1 != 0) {
    if (((*(uint *)(param_1 + 0x108) & *(uint *)(param_1 + 0x10c)) == 0xffffffff) &&
       (*(char *)(param_1 + 0x110) == '\0')) {
      *(undefined4 *)(iVar1 + 0x28) = 0xb;
    }
    else {
      *(undefined4 *)(iVar1 + 0x28) = 10;
    }
    FUN_00560e90(*(undefined4 *)(param_1 + 8),1);
  }
  return;
}
```

### Byte-level notes vs decompiler surface

- Prologue loads stack formal into EAX, pushes ESI/EDI, `LEA EDX,[ECX+0x108]`, copies 4 dwords from `*param_2`.
- Mode write: `MOV dword ptr [EAX+0x28], 10` / `11` (`0xB`).
- Call prep is richer than decompiler: builds manager `ECX` via `*(this+4)` chain (`+0xA8` / `+0xE4A4`) then `PUSH` phys object and `CALL 0x00560e90` — matches dual-sealed `WorldObj_RebindActivate_Inferred` (manager in ECX, object on stack). Surface `FUN_00560e90(phys,1)` omits manager (known residual on that unit).
- Stack flag `1` written as byte onto the pushed slot before the call.

