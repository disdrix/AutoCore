# Annotated low-level: Gfx_DepthStencilFormat_ToBpp_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0043b800` |
| VA | `0x0043b800`–`0x0043b831` exclusive (**49 B**) |
| System | graphics / D3D depth-stencil format utility |
| Date | 2026-08-04 (W38-U dual) |

## Machine-level notes

- **ABI:** EAX = `D3DFORMAT` depth/stencil enum; return EAX = bits-per-pixel; plain `RET`.
- **Impl:** range-reduce `format - 0x47`, table-driven jump (`JT@0x0043b834`, `IDX@0x0043b848`).
- **Role:** depth/stencil bpp only. Color/texture bpp twin is **`FUN_0040ead0`** (different enum band).
- **Callers:** GfxDevice_Reset / surface size / Clear Z-flag / adapter score — all `(bpp*W*H)>>3` or non-zero checks.
- Prefer bytes for body bounds (jump tables live after `RET` chain, not executable CF after leaf returns).

## Sealed mapping

| D3DFMT (enum) | hex | bpp |
|---|---|---|
| D32 | 0x47 | 32 |
| D15S1 | 0x49 | 15 |
| D24S8 | 0x4B | 24 |
| D24X8 | 0x4D | 24 |
| D24X4S4 | 0x4F | 24 |
| D16 | 0x50 | 16 |
| other / band holes | — | 0 |

## Pseudocode (annotated)

```c
// EAX in/out. Leaf.
uint32_t Gfx_DepthStencilFormat_ToBpp_Inferred(uint32_t format /*EAX*/)
{
  switch (format) {
  case 0x47: return 0x20;           // D3DFMT_D32
  case 0x49: return 0x0F;           // D3DFMT_D15S1
  case 0x4B:
  case 0x4D:
  case 0x4F: return 0x18;           // D24S8 / D24X8 / D24X4S4
  case 0x50: return 0x10;           // D3DFMT_D16
  default:   return 0;
  }
}
```

## Open questions

- Product/PDB function name.
- Runtime verification of mem-acct counters at `DAT_00d1f614+0x3b0`.
