# Raw capture: FUN_00407000

| Field | Value |
|---|---|
| **Stable ID** | `aa_00407000` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00407000` |
| **Canonical name** | `FUN_00407000` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int FUN_00407000(void)

{
  int unaff_ESI;
  int unaff_EDI;
  
  FUN_00409e20();
  return unaff_ESI * 0x10 + unaff_EDI;
}
```

---

## R10-007 re-verify (2026-08-05)

**Tooling:** Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_callers` + `get_function_xrefs` + `get_assembly_context` + `read_memory`. **No** `disassemble_bytes`. **No** Launcher.

### Live decompile (equiv scaffold body)

```c
int FUN_00407000(void)

{
  int unaff_ESI;
  int unaff_EDI;
  
  FUN_00409e20();
  return unaff_ESI * 0x10 + unaff_EDI;
}
```

### Byte / disasm seal (`read_memory` + `disassemble_function` @ `0x00407000`)

- Body **29 B** / `0x1d`: `0x00407000`–`0x0040701d` exclusive; pad `CC CC` then next fn @ `0x00407020`.
- Hex: `8b442404 50 51 8b4c240c 56 57 e80f2e0000 8bc6 c1e004 83c410 03c7 c20400`
- Disasm:
  - `mov eax,[esp+4]; push eax; push ecx; mov ecx,[esp+0xc]` — stack formal0 = value*; ECX reloaded to value* for construct-N
  - `push esi; push edi; call FUN_00409e20` then **`add esp,0x10`** (cdecl construct-N + phantom pushes)
  - End pointer: `mov eax,esi; shl eax,4; add eax,edi` → **`dest + count * 0x10`**
  - Epilogue: **`C2 04 00`** (`ret 4`) — one stack formal (value*)

### Callee construct-N (`FUN_00409e20` + `FUN_00409f70`)

- Dualed parent construct-N: `StdVector_ConstructN_Elem0x10_Inferred` @ `0x00409e20` (MEGA-044)
- ECX=value*, stack dest + count; SEH `LAB_009bc610`; loop count times; each `FUN_00409f70` copies **4 dwords** and advances dest by **0x10**
- cdecl plain **RET** (`C3`); this wrapper cleans `add esp,0x10`

### Cross-refs

| Kind | Value |
|------|-------|
| Callers | `FUN_00408640` only (2 sites @ `0x00408746`, `0x0040880c`) |
| Callees | `FUN_00409e20` |
| Parent role | dualed InsertN `StdVector_InsertN_Elem0x10_Inferred`: after uninit-copy prefix (realloc) or hole-shift (in-place), plant N template copies |

### Recovered ABI / name

- **Name:** `StdVector_UninitializedFillN_Elem0x10_Inferred`
- **ABI:** EDI=dest*, ESI=count, stack `const T* value`; **ret 4**; EAX=`dest + count*0x10`
- **Stride:** **0x10** — Confirmed (`shl eax,4` + construct-N `add esi,0x10`)
- **Role:** uninitialized_fill_n for 16-byte POD vector elements
- **Twins:** `00406e50` Ufill 0x28; `00406ee0` Ufill 0xC; construct-N `00409e20`; InsertN `00408640`

### Completeness (analyze_function_complete)

- classification: wrapper; is_thunk: false; code_line_count: 6
- xrefs: 2 UNCONDITIONAL_CALL from `FUN_00408640`
- decompiler signature incomplete (`void` + unaff_*); asm authoritative

