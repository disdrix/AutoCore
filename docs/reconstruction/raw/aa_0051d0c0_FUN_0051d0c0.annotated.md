# Annotated low-level: ObjectCsList_ScalarDeletingDtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051d0c0` |
| VA | `0x0051d0c0`–`0x0051d0de` exclusive (**30 B** / `0x1E`) |
| Ghidra name | `FUN_0051d0c0` |
| Canonical name | `ObjectCsList_ScalarDeletingDtor_Inferred` |
| System | object core / CS-owned list teardown |
| Date | 2026-08-04 (W37-O re-verify; scaffold 2026-07-23) |

## Machine-level notes

- Source: raw capture + W37-O `read_memory` full body + vtbl `read_memory` @ `0x009ce154`.
- MSVC **scalar deleting destructor** for **derived** CS-list vtbl `PTR_FUN_009ce154` (slot0).
- Wraps sealed complete dtor `FUN_0051d0e0` / `ObjectCsList_CompleteDtor_Inferred` (W31-R), then optional `operator_delete` on `flags&1`.
- Ghidra marks `operator_delete` noreturn — **false**; bytes fall through.
- **Accuracy (skill-adjacent warning):** **Not a skill function.** Object/CVOG CS-list dtor. Do **not** invent skill semantics. Nearby `CVOGClonedObject` string is object system.

## Signature (sealed)

```c
// __thiscall; ECX=list*; stack flags; RET 4; returns this
void * __thiscall ObjectCsList_ScalarDeletingDtor_Inferred(void *list, uint8_t flags);
```

## Control flow

```
MOV ESI, ECX
CALL FUN_0051d0e0          // complete dtor (no free)
TEST [ESP+8], 1            // flags bit0
JZ skip
  PUSH ESI
  CALL operator_delete
  ADD ESP, 4
skip:
MOV EAX, ESI
POP ESI
RET 4
```

## Xrefs

| Kind | Address | Note |
|---|---|---|
| DATA | `0x009ce154` | vtbl slot0 → this unit |
| CODE callers | none | virtual dispatch only |

## Pseudocode (annotated ≡ raw; ignore false noreturn)

```c
void * __thiscall ObjectCsList_ScalarDeletingDtor_Inferred(void *self, uint8_t flags)
{
  FUN_0051d0e0(self);                 // complete dtor
  if ((flags & 1) != 0) {
    operator_delete(self);            // cdecl; fall-through after return
  }
  return self;
}
```

## Related (not owned)

| VA | Name / role |
|---|---|
| `0x0051d0e0` | `ObjectCsList_CompleteDtor_Inferred` — body called here |
| `0x0051bf10` | `ObjectCsListBase_ScalarDeletingDtor_Inferred` — base scalar twin |
| `0x0051bc90` | CS drain (from complete) |
| `0x0051b8a0` | `ObjectCsList_ClearDestroy_Inferred` (W30-F) |

## Gaps

- Product / MSVC demangle English for derived CS-list class.
- Array-delete (`flags&2`) not present — scalar-only body (normal).
- Runtime / bit-exact / differential.
