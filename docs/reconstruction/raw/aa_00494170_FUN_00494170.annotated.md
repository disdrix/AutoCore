# Annotated low-level: FUN_00494170 → Host_ClearElem0x28Array_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00494170` |
| VA | `0x00494170`–`0x004941af` (**64 B** / `0x40`) |
| System | host / cookie-vector free (stride **0x28**) |
| Date | 2026-07-29 (W30-Q dual) |
| Semantic name | `Host_ClearElem0x28Array_Inferred` |

## Machine-level notes

- Source: raw `aa_00494170` + live re-verify W30-Q.
- **ECX** = host object; `mov esi, ecx`.
- Host layout fragment (same as Class_009c7a1c vector head):
  - `+0x28` → cookie-vector base (element storage; count cookie at `base-4`)
  - `+0x04` → dim / count X (zeroed)
  - `+0x08` → dim / count Y (zeroed)
- Free path: if `arr = host+0x28` nonzero →
  `_eh_vector_destructor_iterator_(arr, 0x28, *(arr-4), FUN_005b8000)` then
  `operator_delete[](arr-4)`; always zero `+0x28/+0x4/+0x8`.
- Element dtor sealed W29-F as `Elem0x28_CompleteDtor` (`0x005b8000`).
- Same free fragment appears **inlined** in `Class_009c7a1c_CompleteDtor` (`0x004970b0`); this VA is the standalone free helper.
- **No static callers** in Ghidra xref DB (orphan relative to static graph; body still live-adjacent to rebuild `0x004941b0`).

## Pseudocode (annotated)

```c
// __thiscall-equivalent; ECX=host; bare RET; void
void __fastcall Host_ClearElem0x28Array_Inferred(int host)
{
  void *arr = *(void **)(host + 0x28);
  if (arr != (void *)0x0) {
    // stride 0x28; count cookie immediately before storage
    _eh_vector_destructor_iterator_(arr, 0x28, *(int *)((int)arr + -4), FUN_005b8000);
    operator_delete__((void *)((int)arr + -4));
  }
  *(undefined4 *)(host + 0x28) = 0;
  *(undefined4 *)(host + 4) = 0;
  *(undefined4 *)(host + 8) = 0;
  return;
}
```

## Open questions

- Product/PDB host type English (Class_009c7a1c peer vs distinct host).
- Why no static callers (dead export / dynamic only / unreferenced after link).
- Runtime / bit-exact.
