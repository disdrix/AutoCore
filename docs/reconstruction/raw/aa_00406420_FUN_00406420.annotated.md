# Annotated low-level: CNDHash_Dtor_00a2c2b0 (`FUN_00406420`)

| Field | Value |
|---|---|
| Stable ID | `aa_00406420` |
| VA | `0x00406420`–`0x0040649a` (**123 B**) |
| System | container / CNDHash (host vtbl `00a2c2b0`) |
| Date | 2026-07-23 scaffold; **2026-08-04 WQ9H-D dual seal** |

## Machine-level notes

- True **`__thiscall`**: `MOV ESI,ECX` at entry; bare `ret`.
- Lock `@+0x1d` path is **non-fatal**: two `FUN_007a4480` logs then fall through (no throw, no skip destroy).
- Destroy twin uses **EAX** ABI: `MOV EAX,ESI; CALL 0x00406fc0`.
- Freelist subobject dtor: `LEA EAX,[ESI+0x20]; PUSH EAX; CALL FUN_00416e80` — decompiler `param_1+8` = dword offset of `+0x20`.
- Sole caller is vtbl[0] scalar-deleting dtor `FUN_00406520` (not OWN).

## Pseudocode (annotated)

```c
// ABI: __thiscall; ECX = CNDHash*; bare ret
void CNDHash_Dtor_00a2c2b0(CNDHash *self /*ECX*/)
{
  // SEH LAB_009beaf7
  self->vtbl = &PTR_FUN_00a2c2b0; // re-stamp during destroy
  if (self->lockedForTraversal /* +0x1d */) {
    vog_Log(0, "HashError:Destructor, already locked for traversal");
    vog_Log(0, "VOG_DEBUG_STOP");
    // continue — soft assert only
  }
  CNDHash_DestroyBucketTable_00a2c2e4(/*EAX=*/self); // zero live; freelist chains; free table
  FreelistSlabVector_dtor(/* stack */ self + 0x20);  // FUN_00416e80 — residual
}
```

## Call graph

```text
vtbl[00a2c2b0][0] = FUN_00406520  (scalar deleting)
  CALL CNDHash_Dtor_00a2c2b0       [OWN WQ9H-D]
    ├─ CNDHash_DestroyBucketTable_00a2c2e4  [sealed WQ9G-J]
    │     └─ FUN_004085e0 freelist release  [residual / WQ9H-A]
    └─ FUN_00416e80(this+0x20) freelist vector dtor [residual]
  if (flags & 1) operator_delete(this)
```

## Open residual

- Product / PDB English.
- Full dual of `FUN_00416e80` freelist vector + scalar dtor wrapper (not OWN).
- Runtime / bit-exact / differential.
