# Raw capture: FUN_0073d940

| Field | Value |
|---|---|
| **Stable ID** | `aa_0073d940` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0073d940` |
| **Canonical name** | `FUN_0073d940` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int __thiscall FUN_0073d940(int param_1,int param_2)

{
  int iVar1;
  char cVar2;
  int *piVar3;
  int *piVar4;
  
  if ((param_2 != 0) && (*(int *)(param_2 + 8) != 0)) {
    for (piVar3 = *(int **)(param_1 + 4); piVar3 != *(int **)(param_1 + 8); piVar3 = piVar3 + 5) {
      if (*piVar3 == *(int *)(param_2 + 8)) {
        piVar4 = (int *)piVar3[2];
        piVar3 = (int *)piVar3[3];
        if (piVar4 == piVar3) {
          return 0;
        }
        while ((iVar1 = *piVar4, iVar1 == param_2 || (cVar2 = FUN_00752590(), cVar2 == '\0'))) {
          piVar4 = piVar4 + 1;
          if (piVar4 == piVar3) {
            return 0;
          }
        }
        return iVar1;
      }
    }
  }
  return 0;
}
```

---

## W37-X re-verify (2026-08-04) — append only

**Agent:** OWN-ONLY dual W37-X  
**Tools:** `decompile_function` / `batch_decompile` + `read_memory` + `analyze_function_complete` + `get_function_by_address` + `get_assembly_context` (call sites). **No** `disassemble_bytes`.

### Body

| Field | Value |
|---|---|
| Range | `0x0073d940`–`0x0073d9ab` |
| Size | **108 B** / `0x6C` |
| Epilogue | `RET 4` (`C2 04 00`) ×3 exit paths |
| Pad | `CC` then next @ `0x0073d9b0` (SEH prologue) |

### Full hex (108 B)

```
53558b6c240c85ed5657741f8b550885d274188b41048b49083bc1740e8d49003910741083c0143bc175f55f5e5d33c05bc204008b70088b580c3bf374ed8bff8b3e3bfd740d8bd58bc7e8014c010084c0751083c6043bf375e65f5e5d33c05bc204008bc75f5e5d5bc20400
```

### Bytes-sealed ABI

- **ECX** = registry host (`param_1`); callers load `MOV ECX, dword ptr [DAT_00d1f61c]`.
- **Stack[4]** = material pointer (`param_2`); `MOV EBP,[ESP+0xC]` after `PUSH EBX; PUSH EBP`.
- **RET 4** stdcall stack cleanup of one arg.
- Returns material* in **EAX**, or **0**.

### Bytes-sealed control flow

1. Null-check material and `material+8` (effect/key).
2. Walk registry table `[host+4, host+8)` stride **0x14** (5 dwords); match `entry+0 == material+8`.
3. On hit: scan pointer range `[entry+8, entry+0xC)`.
4. Skip self (`candidate == material`) and candidates where `FUN_00752590(candidate, material)==0` (EAX/EDX register formals in bytes).
5. Return first non-self compatible candidate, else 0.

### Call graph (live)

- **Callees:** `FUN_00752590` only.
- **Callers (14):** `FUN_0099f450`, `FUN_0098df00`, `FUN_0099f810`, `FUN_0099e4a0`, `FUN_0098fd80`, `FUN_007300c0`, `FUN_0099eb60`, `FUN_0096f740`, `FUN_0099d060`, `FUN_00731130`, `FUN_0099e0a0`, `FUN_0099e2c0`, `FUN_00759090`, `FUN_0099e6c0`.
- **Xrefs:** 17.
- Post-call pattern (e.g. `0x0099e849`): if non-null → `+1` at `found+4`, first-ref vfunc+4; dec old holder at slot; store found — **refcount swap is caller-side**, not this body.

### Correction vs prior wave notes

Prior duals described `FUN_0073d940` as the refcount-swap itself. Live body is **lookup only**. Callers perform AddRef/Release/store after a non-null return.

### Decompile ≡ bytes

CF, stride 0x14, key at +8, range [+8,+0xC), callee `FUN_00752590`, triple `RET 4` — **match**. Decompiler omits EAX/EDX formals into `FUN_00752590`; bytes seal `MOV EDX,EBP; MOV EAX,EDI; CALL`.
