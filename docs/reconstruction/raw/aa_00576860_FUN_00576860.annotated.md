# Annotated low-level: WaterPhase_RegisterToken_Inferred (`FUN_00576860`)

| Field | Value |
|---|---|
| Stable ID | `aa_00576860` |
| VA | `0x00576860`–`0x00576889` exclusive (**41 B**) |
| System | client / VOGEnvironmentSector water phase |
| Date | 2026-07-29 (W32-S re-verify; scaffold 2026-07-23) |

## Machine-level notes

- Source: raw `aa_00576860_FUN_00576860.md` + live `read_memory`.
- **No SEH**; bare `ret` (`c3`).
- Decompiler hides `mov ecx,[DAT_00d1f620]` before `FUN_00746400` — **bytes seal thiscall container**.
- `FUN_00576c40` is thiscall on the `new(4)` result (token vtbl install).
- Fail path merges OOM and null-token into **`OR EAX,-1; RET`**.

## Control flow

```
token = operator_new(4)
if token == null: return -1
obj = FUN_00576c40(token)     // *token = vtbl 0x009d3ac4; return this
if obj == null: return -1
return FUN_00746400(DAT_00d1f620, obj)
  // find in vector → 1; else append → 0
```

## Return values (parent uses `hr < 0`)

| EAX | Parent effect |
|---|---|
| -1 | log `"Water phase initialization failed"` |
| 0 or 1 | continue (success) |

## Pseudocode (annotated; bytes-faithful)

```c
// cdecl/default; no stack formals; bare RET
int32_t WaterPhase_RegisterToken_Inferred(void)
{
  void *token = operator_new(4);
  if (token != nullptr) {
    // thiscall micro-ctor: install PTR_FUN_009d3ac4
    int32_t obj = (int32_t)FUN_00576c40(token);
    if (obj != 0) {
      // thiscall registry: ECX = *DAT_00d1f620 host, stack = obj
      return FUN_00746400(/*this*/ DAT_00d1f620, obj);
    }
  }
  return -1;
}
```

## Twin

Distort `FUN_00576d70` same skeleton / same `DAT_00d1f620` / different micro-ctor.

## Open questions

- Product type of 4-byte token (`CVOGPhase*` family from nearby rdata).
- Full semantics of registry host at `DAT_00d1f620` (vector of phase tokens).
- Whether append (`FUN_004406e0`) can fail and how that surfaces as 0 vs throw.
- Runtime / bit-exact / differential.
