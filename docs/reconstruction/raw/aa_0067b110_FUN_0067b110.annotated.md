# Annotated low-level: FUN_0067b110

| Field | Value |
|---|---|
| Stable ID | `aa_0067b110` |
| VA | `0x0067b110` |
| System | network-nazgul-netAddress |
| Date | 2026-07-29 (OWN-ONLY dual seal) |
| Proposed name | `netAddress_TrivialDtor` / `Address_Dtor` (**High** role; product name open) |

## Machine-level notes

- Source: raw capture for `aa_0067b110` + live `decompile_function` / `read_memory` 2026-07-29.
- Body is a **single-byte** `ret` (`c3`); remaining `cc` are alignment padding to `FUN_0067b120`.
- No parameters, no stores, no callees, no globals.
- Used as MSVC SEH scope-exit / stack-unwind target for 16-byte Address locals.

## Cluster (adjacent, not owned)

| VA | Role (inferred) |
|---|---|
| `FUN_0067b070` | Address default ctor (zero + `htons(0)` port) |
| `FUN_0067b0e0` | Address copy (4 dwords) |
| `FUN_0067b110` | **This unit** — trivial dtor |
| `FUN_0067b120` | Address assign-if-not-same (4 dwords) |
| `FUN_0067ba30` / `FUN_0067b9a0` | getpeername / getsockname wrappers that call this dtor |

## Pseudocode (annotated)

```c
// Trivial destructor for nazgul 16-byte Address / sockaddr POD.
// Ghidra: void FUN_0067b110(void) { return; }
// Bytes: c3
// Callers often appear to pass an arg in decompiler output — ABI artifact;
// body consumes nothing and returns void.

void FUN_0067b110(void)
{
  return;
}
```

## Open questions

1. Product class name (`netAddress` vs `Address` vs nested in netSocket TU only).
2. Whether any non-trivial Address variant shares this dtor (unlikely; body is empty).
3. Full caller inventory (hundreds of SEH sites across Auth/chat/net) — not required to seal CF.
