# Annotated low-level: CVOGMenu_CompleteDtor (FUN_0082d540)

| Field | Value |
|---|---|
| Stable ID | `aa_0082d540` |
| VA | `0x0082d540`–`0x0082d58e` (**79 B**) |
| System | MSVC intermediate complete dtor — `CVOGMenu` / `PTR_FUN_00a72f6c` |
| Date | 2026-08-05 (WQ9K-G re-annotate) |

## Machine-level notes

- **ECX = this** (`8B F1`); SEH `LAB_009b4443`.
- Install vtbl: `C7 06 6C 2F A7 00` → `*this = 0x00a72f6c`.
- EH state 0 → `CALL FUN_0082ce20` (menu hash/map teardown at host+`0x14d` dword / +0x534).
- EH state −1 → `MOV ECX,ESI; CALL FUN_00792c20` (CNDUIDialog complete).
- Epilogue: restore FS:[0], `ADD ESP,0x10`, `RET`. Does **not** free host.
- RTTI seals product **CVOGMenu** (not generic “menu base”).
- Scalar twin `FUN_004166d0` (vtbl[0]) calls this then optional `operator_delete(this)`.
- Derived complete example: `CMenuChatlog_CompleteDtor` `0x008327f0` chains here after installing `00a70524`.

## Pseudocode (annotated)

```c
// ECX = this; SEH; ADD ESP,0x10; RET; complete (no free this)
void __fastcall CVOGMenu_CompleteDtor(void* self)
{
  // SEH link LAB_009b4443
  *self = &PTR_FUN_00a72f6c;     // CVOGMenu vtbl
  // EH = 0
  FUN_0082ce20(self);            // owned hash at [0x14d]; residual dual
  // EH = -1
  FUN_00792c20(self);            // CNDUIDialog_CompleteDtor (shared UI base)
  // unlink SEH; ADD ESP,0x10; RET
}
```

## Open questions

- Full field map of CVOGMenu beyond hash slot `[0x14d]` / dual of `0082ce20`.
- Vtbl slots [1+] English for `00a72f6c`.
- Runtime / bit-exact / differential.
