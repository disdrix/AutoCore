# Raw capture: FUN_00436ef0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00436ef0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00436ef0` |
| **Canonical name** | `FUN_00436ef0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00436ef0(int param_1,undefined4 *param_2)

{
  undefined4 *in_EAX;
  
  for (; param_1 != 0; param_1 = param_1 + -1) {
    if (in_EAX != (undefined4 *)0x0) {
      *in_EAX = *param_2;
    }
    in_EAX = in_EAX + 1;
  }
  return;
}
```

---

## W34-D re-verify append (2026-07-29)

| Field | Value |
|---|---|
| **Tool** | Ghidra `batch_decompile` + `analyze_function_complete` + `read_memory` + callers/xrefs |
| **Integrity** | Scaffold raw body above preserved; this section appended only |
| **Live decompile** | ≡ 2026-07-23 scaffold CF |
| **Body** | `0x00436ef0`–`0x00436f06` inclusive (**23 B** / `0x17`); pad `CC` @ `0x00436f07` |
| **Epilog** | bare **`C3`** |
| **Classification** | leaf — 0 callees |
| **Callers** | 14+ including `FUN_004646e0` (PtrVecShell fill path) and many STL-ish fill sites |
| **Xrefs** | 18 |

### Full body hex (23 B)

```
85c976125685c074048b32893083c00483e90175f05ec3
```

### Byte-sealed ABI / CF

```
TEST ECX,ECX / JBE done          ; count==0 → ret (TEST clears CF ⇒ JBE≡JZ)
PUSH ESI
loop:
  TEST EAX,EAX / JZ skip_store   ; null-safe dest
  MOV ESI,[EDX] / MOV [EAX],ESI  ; *dst = *src  (re-read *EDX each iter)
skip_store:
  ADD EAX,4
  SUB ECX,1 / JNZ loop
POP ESI
RET
```

| Slot | Register | Role |
|---|---|---|
| count | **ECX** | element count (dword slots) |
| src* | **EDX** | pointer to dword replicated |
| dest* | **EAX** | write cursor; advanced by 4 each iter even if null |
| return | none meaningful | void; EAX ends as dest+count (or count*4 if dest was null) |

Decompiler `__fastcall (count, src*)` + phantom `in_EAX` ≡ bytes. **Not** stack formals; bare RET.
