# Annotated low-level: FUN_005226e0 → Character_SumCommodityTierMaxCapacities_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005226e0` |
| VA | `0x005226e0`–`0x00522709` inclusive (**42 B**) |
| System | `inventory-transfer` |
| Date | 2026-08-05 (R13-034 dual re-verify) |
| Ghidra | `FUN_005226e0` |
| Canonical | `Character_SumCommodityTierMaxCapacities_Inferred` |
| Parent | `0x00520340` `Character_CalcCommodityTierMaxCapacity_Inferred` |

## Machine-level notes

- Source: raw capture + live Ghidra 2026-08-05.
- Prefer assembly when decompiler conflicts — **bytes win** on thiscall (decompiler drops ECX).
- Only callee: dualed max-capacity leaf `FUN_00520340` (thiscall + ret 4, tier stack arg).
- Loop domain **tiers 1..5** (not 0..5). Sibling `FUN_005204f0` (R13-033) sums 0..5 with tier0→0 — **do not merge**.
- Zero direct xrefs recovered; still a clean Σ-max wrapper over sealed leaf.

## Pseudocode (bytes-corrected)

```c
// __thiscall: ECX = Character*
// RET (no stack args cleaned)
int Character_SumCommodityTierMaxCapacities_Inferred(Character* self)
{
  int sum = 0;
  int tier = 0;
  do {
    tier = tier + 1; // 1..5
    sum += Character_CalcCommodityTierMaxCapacity_Inferred(self, tier);
  } while (tier < 5);
  return sum;
}
```

## Layout / ABI

| Item | Role | Confidence |
|---|---|---|
| ECX this | Character* (saved EBX) | **High** |
| EDI | running sum | **High** |
| ESI | current tier 1..5 | **High** |
| CALLEE | `FUN_00520340` max capacity | **High** (dual R12-018) |
| RET `C3` | no stack cleanup | **High** |
| Callers | none recovered | **Open** (gap) |

## Open questions

- Product / MSVC demangle English for the sum wrapper.
- Why zero static xrefs (dead / indirect / stripped call graph).
- Whether any UI uses total max Complexity (= sum of five tier maxes) vs per-tier.
- Runtime / bit-exact.
