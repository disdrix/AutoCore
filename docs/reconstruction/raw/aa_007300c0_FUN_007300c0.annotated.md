# Annotated low-level: FUN_007300c0 → PalToolNoMap_Host0x34_MeshBuildUpload_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_007300c0` |
| VA | `0x007300c0`–`0x007308b7` (**2040 B** / `0x7F8`) |
| System | palantir graphics / PalTool material + mesh upload |
| Date | 2026-07-29 (W36-J dual seal) |
| Ghidra | `FUN_007300c0` |
| Named | `PalToolNoMap_Host0x34_MeshBuildUpload_Inferred` |

---

## Machine-level notes

- **ABI:** stack `host*` (not ECX-thiscall); **`RET 4`**; returns **0** success or **0xFFFFFFFF** failure.
- **SEH:** `LAB_009b1eb6`; large stack frame (`SUB ESP,0xE4` + registers).
- **Host size context:** sole caller W35-E `HostPtrTable12_Bootstrap_EDI` allocates **`new(0x34)`** with inline multi-vtbl (`00aa33c8` / `00a9db18`×2 / `00a9da80`) then calls this builder.
- **Material slot:** single channel at **`host+0x24`** using plate `PalToolMaterialNoMap.fx` + MatDiffuse/Ambient/Specular/Emissive (same string family as `aa_00731130`).
- **Host field stores sealed from decompile:**
  - `host+0x20` ← local vertex vector length / 0x24 / 3
  - `host+0x1c` ← local index vector length / 6
  - `host+0x2c` ← **4**
  - material ref at `host+0x24` after `FUN_0073d940` swap
  - index/VB attachment path via `FUN_0044bbc0(..., host+0x14, 0)`
- **Stride:** material child `@(*mat+0x14)+0x10` char non-zero → **0x10** else **0x20**.
- **Upload:** D3D-style Lock at COM vtbl **+0x2c** with flag **0x800**; dword/byte copy loops; Unlock helpers (`FUN_00414a50`, `FUN_0044ba80`).
- **Format token:** `FUN_00414bc0(0x152)` twice (VB creation path).
- **Decompiler gaps:** second upload block mis-recovers some stack as absolute `piRam00000010` etc.; treat as stack aliasing, not real null-page IO. Unreachable warnings at `0x00730710`/`0x00730716`.
- **Caller:** `FUN_0073c810` @ `0x0073c9f5` only (1 xref).

## Recovered contract

```c
// PalToolNoMap_Host0x34_MeshBuildUpload_Inferred
// stdcall; host span 0x34 from bootstrap; material @+0x24; counts @+0x1c/+0x20; flag @+0x2c=4
uint32_t PalToolNoMap_Host0x34_MeshBuildUpload_Inferred(void *host /* stack */);
// return 0 success, 0xFFFFFFFF failure
```

## Open questions

- Product English for 0x34 multi-vtbl host class (neighborhood gfxToolFactory only).
- Exact vertex struct packing for 0x24-byte verts / 6-byte indices.
- Full contracts of unowned mesh helpers (`FUN_009813*`, `FUN_00414*`, `FUN_0044b*`, `FUN_00747390`).
- Clean recovery of second geometry stack block (decompiler alias gap).
- Runtime / bit-exact / differential.
