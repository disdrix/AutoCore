# Raw capture: FUN_00489892

| Field | Value |
|---|---|
| **Stable ID** | `aa_00489892` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00489892` |
| **Canonical name** | `FUN_00489892` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __cdecl operator_new(uint param_1)

{
  void *pvVar1;
  
                    /* WARNING: Could not recover jumptable at 0x00489892. Too many branches */
                    /* WARNING: Treating indirect jump as call */
  pvVar1 = operator_new(param_1);
  return pvVar1;
}
```

## R10-002 re-verify (2026-08-05)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual R10-002 |
| **Tools** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_xrefs_from` + `get_function_xrefs` + `get_function_callers` + `analyze_function_completeness`. **No** `disassemble_bytes` primary |
| **Live name** | `operator_new` |
| **Classification** | **thunk** (IAT import trampoline) |
| **Signature** | `void * __cdecl operator_new(uint param_1)` |
| **Body** | `0x00489892`–`0x00489897` inclusive (**6 B**) |
| **Live decompile** | ≡ scaffold CF (indirect jump treated as call to `operator_new`) |

### Bytes (`read_memory` @ `0x00489892`, 6 B + neighbors)

```text
00489892  FF 25 64 66 9C 00     JMP dword ptr [0x009c6664]   ; PTR_operator_new
00489898  FF 25 5C 66 9C 00     JMP dword ptr [0x009c665c]   ; next IAT thunk (sibling)
0048989e  FF 25 50 66 9C 00     JMP dword ptr [0x009c6650]   ; next
```

Hex body only: `ff2564669c00`

### IAT slot (`read_memory` @ `0x009c6664`)

```text
009c6664  A2 A8 6E 00           ; dword = 0x006ea8a2 (image-bound CRT import target at capture)
```

`get_xrefs_from` @ `0x00489892`:
- To `009c6664` data `PTR_operator_new_009c6664` **[INDIRECTION]**
- To `EXTERNAL:0000009e` function `operator_new` **[COMPUTED_JUMP]**

### Semantics (sealed)

1. Single instruction: **unconditional JMP through IAT** — no stack frame, no local work.
2. **ABI:** `__cdecl` — size in stack arg; CRT returns `void*` in EAX (or throws `std::bad_alloc` inside MSVCR — DLL body not dualed here).
3. **Name Confirmed:** Ghidra import / PE symbol `operator_new` (not game product string).
4. **Not game domain:** MSVC C++ heap allocation entry; used by ~**3519** xrefs (Ghidra `search_functions_enhanced` `xref_count`).
5. Sibling import cluster: `operator_delete[]` `0x0048981c`, `operator_delete` `0x00489822`, `operator_new[]` `0x00489834`, this VA `0x00489892`.
6. Secondary same-named thunk exists at `0x00685b20` (**1** xref) — **not** this OWN VA; do not conflate stable IDs.

### Parent dual context (not owned)

Parent dual `0x004f3a30` `Vehicle_CreateCargoInventoryFromPageCount` (inventory-transfer) lists this VA as `operator_new` callee for cargo `InventoryGrid` allocation.

### Live decompile (2026-08-05)

```c
void * __cdecl operator_new(uint param_1)

{
  void *pvVar1;
  
                    /* WARNING: Could not recover jumptable at 0x00489892. Too many branches */
                    /* WARNING: Treating indirect jump as call */
  pvVar1 = operator_new(param_1);
  return pvVar1;
}
```

Decompiler models IAT JMP as recursive call; **bytes win**: pure `JMP [IAT]`, no return path in this unit.

