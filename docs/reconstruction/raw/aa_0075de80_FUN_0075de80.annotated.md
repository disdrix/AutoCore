# Annotated low-level: FUN_0075de80 / AssManager_SweepTrackedAssets_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0075de80` |
| VA | `0x0075de80`–`0x0075e2ce` exclusive (**1358 B** / `0x54E`) |
| System | `assets` / `assManager` |
| Date | 2026-07-29 (W32-O dual seal; scaffold 2026-07-23 preserved in raw) |
| Canonical | `AssManager_SweepTrackedAssets_Inferred` (**Inferred**) |
| Ghidra | `FUN_0075de80` |

## Machine-level notes

- Source: raw capture + live W32-O re-verify (`decompile` + `read_memory` + xrefs). **No** `disassemble_bytes`.
- Prefer bytes on ABI: **`RET 8`**, ECX this, two stack formals.
- Decompiler CS “return address” stack junk and `operator_delete` “does not return” warnings are noise — bytes/control flow authoritative.
- Nested GuardedVector / map helpers owned by other duals (W31-J PushBack Stride2 U32U8; W31-N CS wrapper; find/erase family).

## ABI (sealed)

```c
// __thiscall; ECX = assManager*; stack mode + ageThreshold; RET 8
void AssManager_SweepTrackedAssets_Inferred(
    void *assManager /*ECX*/,
    int mode /*stack*/,
    float ageThreshold /*stack*/);
```

Prologue: `55 8B EC 83 E4 F8 … 8B F1` (ESI=this).  
Epilogue: `… 5B 8B E5 5D C2 08 00`.

## Semantics (annotated)

1. SEH frame `LAB_009b0a03`; zero two stack vectors (key vec + asset* vec).
2. `now = FUN_0076c330()` once per entry (reused each outer pass).
3. **Outer** `do` over list at `this+8` until exit condition.
4. For each node (`node[3]=asset*`, `node[2]=key`, `node[4]=keep`):
   - **mode==2 && asset**: if age ≥ threshold and `asset+0x10==0` → optional secondary erase (`this+0x6c`), log `assManager.cpp:0x1BB`, `FUN_00745f80`, `vtbl[0](asset)`, null asset slot; fall through.
   - Shared branch:
     - **mode==1**: optional CS push of key into secondary (`FUN_0043e9e0` under host+`0xC0`); unlink node; append key + asset* to stack vectors.
     - **else**: if keep-flag set → next; if no asset → erase; if asset unloaded → destroy+erase+count++.
5. Exit if `mode==2` **or** destroy-count `< 1`; else rescan (mode≠2 unload drain).
6. Post: for each collected pair, `FUN_0044e8c0` + conditional `FUN_0043c6f0`/`FUN_0043ba40` reinsert; free stack vectors; return.

## Callers / callees

| Kind | Symbols |
|------|---------|
| Callers (7) | `FUN_007544f0`, `FUN_007b7000`, `FUN_004e09e0`, `FUN_008d4690`, `FUN_00937560`, `FUN_009521c0`, `FUN_009523d0` |
| Notable callees | `FUN_0043e9e0` (W31-J), `FUN_0043e7f0` (W32-N partition), `FUN_0044e8c0`, `vog_LogMessage` |

## Open questions

- Product/PDB method name on AssManager.
- Exact product enums for `mode` beyond structural 0/1/2 shapes.
- Asset static type / vtbl[0] product English.
- Runtime / bit-exact under concurrent CS.
