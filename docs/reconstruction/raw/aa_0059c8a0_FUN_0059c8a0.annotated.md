# Annotated low-level: FUN_0059c8a0 → FreelistSlabVector_dtor

| Field | Value |
|---|---|
| Stable ID | `aa_0059c8a0` |
| VA | `0x0059c8a0` |
| Body | `0x0059c8a0`–`0x0059c8e1` (66 B / `0x42`, bare `ret`) |
| Canonical | `FreelistSlabVector_dtor` (Ghidra `FUN_0059c8a0`; **Inferred**) |
| System | container / CNDHash freelist slab vector |
| Date | 2026-07-29 W24-M dual seal |
| Tools | `decompile_function` + `analyze_function_complete` + `read_memory` (no `disassemble_bytes`) |

---

## Machine-level notes

- **ABI:** `__thiscall` ECX = freelist subobject* (typically `CNDHash+0x20`); bare `ret`.
- **Loop:** `for (it = begin(+0x08); it != end(+0x0c); ++it) _aligned_free(*it)` via IAT `0x009c6578`.
- **Vector free:** if `begin != null` → `operator_delete(begin)` (**returns** — decompiler noreturn is FALSE).
- **Always** zero `begin(+0x08)`, `end(+0x0c)`, `capacity_end(+0x10)` after optional delete.
- **Does not** touch freelist head at subobject `+0x00` (nodes already pushed/reclaimed by FreeBuckets reclaim).
- **Callers:** 38+ CNDHash dtors / twins; thunk `0x0059cbc0`. From dtor `0053b920`: `lea ecx,[this+0x20]`.

---

## Pseudocode (annotated, machine-corrected)

```c
// void __thiscall FreelistSlabVector_dtor(FreelistSlabVector *this)
void __fastcall FUN_0059c8a0(int param_1 /* ECX freelist subobject */)
{
  undefined4 *it = *(undefined4 **)(param_1 + 0x8);   // begin
  undefined4 *end = *(undefined4 **)(param_1 + 0xc);  // end
  while (it != end) {
    _aligned_free((void *)*it);  // each slab block
    it = it + 1;                 // next pointer slot
  }
  if (*(void **)(param_1 + 0x8) != (void *)0x0) {
    operator_delete(*(void **)(param_1 + 0x8)); // RETURNS
  }
  // always (both paths):
  *(undefined4 *)(param_1 + 0x8)  = 0;
  *(undefined4 *)(param_1 + 0xc)  = 0;
  *(undefined4 *)(param_1 + 0x10) = 0;
}
```

### Decompiler CF bugs (sealed)

| Decompiler claim | Machine |
|---|---|
| `operator_delete` does not return | FALSE — falls through to three stores |
| Zero only when begin==null | FALSE — zero always; null path only skips delete |

---

## Layout (freelist subobject = CNDHash+0x20)

| Off | Field | Touched by this? |
|---|---|---|
| `+0x00` | freelist head (node*) | **No** |
| `+0x04` | (unused / pad / other) | **No** |
| `+0x08` | slab** begin | free + null |
| `+0x0c` | slab** end | null |
| `+0x10` | slab** capacity end | null |

---

## Open / gaps

1. Product type name (`std::vector`-like vs custom).
2. Alignment policy of `_aligned_free` slabs (alloc twin not owned here).
3. Runtime / bit-exact not run.
