# Annotated low-level: FUN_00984c70 → AssPackManager_AddDirectoryTree_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00984c70` |
| VA | `0x00984c70`–`0x00984d25` (**182 B** / `0xB6`) |
| System | AssPackManager / offline pack ingest |
| Date | 2026-07-23 scaffold; **W33-E dual seal 2026-07-29** |
| Preferred name | `AssPackManager_AddDirectoryTree_Inferred` |

## Machine-level notes

- Source: raw capture + W33-E re-verify append (full body hex).
- Prefer **bytes** over decompiler `__thiscall` labeling.
- Nested `FUN_00984910` is AssPackManager add-file (`assPackManager.cpp` log paths) — unowned plate.
- Nested `FUN_009839b0` = `Fs_DirRecurseCollectLeafPaths_Inferred` (W32-D sealed).
- **0 static xrefs** — offline / dynamic / tooling entry.

## Control flow (sealed)

```
listing = empty string vector
rc = DirRecurseCollectLeafPaths(path /*ECX*/, &listing)
if rc >= 0:
  for leaf in listing:                    // stride 0x1c
    tmp = copy(leaf)
    AssPackManager_AddFile(               // FUN_00984910
      leaf_cstr /*ECX*/,
      pack /*stack/EBP*/,
      mode /*EDI — gap*/,
      1, 0, 0)
    destroy tmp
destroy listing                           // FUN_00431ae0
return 0
```

## Signature (sealed, bytes)

```c
// ECX = path; stack pack*; EDI = mode (implicit / unaff — gap)
// bare RET; EAX = 0
int AssPackManager_AddDirectoryTree_Inferred(
    const char* dir_path,       // ECX
    void* ass_pack_manager,     // stack [esp+4]
    /* int mode_in_EDI */);     // register — not written in body
```

Decompiler shape (misleading): `undefined4 __thiscall FUN_00984c70(param_1, param_2)` with `unaff_EDI`.

## Bytes win

| Bytes | Meaning |
|---|---|
| `PUSH ECX; PUSH &listing; CALL FUN_009839b0; ADD ESP,8` | path in **ECX**; cdecl collect |
| `MOV EBP,[ESP+0x44]` | stack formal = pack host |
| `… SSO → ECX=c_str; PUSH 0; PUSH 0; PUSH 1; PUSH EDI; PUSH EBP; CALL FUN_00984910` | add-file formals |
| `XOR EAX,EAX; …; RET` | always return 0 |

## Open questions

- Product plate for this wrapper (PDB name).
- Who sets **EDI** mode and who calls (0 static xrefs).
- Full `FUN_00984910` formal plate (flags `1,0,0` meaning).
- Runtime / bit-exact / differential.

## Pseudocode

See raw body + clean `AssPackManager_AddDirectoryTree_Inferred.cpp`.
