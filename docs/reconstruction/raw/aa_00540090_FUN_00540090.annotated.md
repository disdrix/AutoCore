# Annotated: FUN_00540090 → SkillDef_BuildFxMasterName_Inferred

| Field | Value |
|---|---|
| **Stable ID** | `aa_00540090` |
| **VA** | `0x00540090`–`0x00540282` |
| **Ghidra** | `FUN_00540090` |
| **Canonical** | `SkillDef_BuildFxMasterName_Inferred` |
| **System** | skills-abilities / client-fx |
| **Date** | 2026-07-29 W24-N |
| **Tools** | decompile + read_memory + analyze_function_complete + xrefs (no disassemble_bytes) |

---

## Purpose

Materialize the FX master basename for a skill definition payload into a caller-supplied buffer:

```
"skill_" + classTag + categoryTag + skillNameCString
```

Used by FxCache ensure/apply paths before `FxMaster_LoadMasterFromXML`.

---

## ABI

| Slot | Role |
|---|---|
| stack+4 | `SkillDefPayload*` — base where `+0x180`, `+0x17c`, `+0x5a6` apply. Callers pass **RB node + 0x10**. |
| stack+8 | `char* out` |
| stack+0xC | `size_t cap` (typically `0x400`) |
| convention | **cdecl** — plain `ret`; caller `add esp, 0xC` |
| return | void (strncpy side effect) |

---

## Control flow

```
buf[0..] = "skill_\0"          // DAT_009d01bc..c2
switch skill[+0x180]:
  0 → append "h_"
  1 → append "m_"
  2 → append "c_"
  * → append "n_"
switch skill[+0x17c]:
  0 → append "c_"
  1 → append "e_"
  2 → append "o_"
  3 → append "r_"
  * → append "fixme_"
append memcpy skill[+0x5a6] C-string (dword bulk + tail)
strncpy(out, buf, cap)
```

Appends use strlen-to-NUL then write 2–7 byte string literals (MSVC-optimized).

---

## Offsets (relative to param_1)

| Offset | Use | Confidence |
|---|---|---|
| `+0x180` | class enum (int) | High |
| `+0x17c` | category enum (int) | High |
| `+0x5a6` | skill name C-string | High (≡ node+0x5B6) |

---

## Callers

1. `FxCache_EnsureMasterAndResolve` (`0x005408f0`) @ `0x00540979` — after SkillDefMap exact-find.
2. `FxCache_ApplyWithNfxEnsure` (`0x00542790`) @ `0x005428f2`.
3. `FUN_005c74e0` SpecialFX init path @ `0x005c761a`.

Callee: `strncpy` only.

---

## Confidence

| Claim | Level |
|---|---|
| CF + string table + cdecl ABI | **High** |
| Role = FX master basename builder | **High** (callers + `"skill_"`) |
| Product labels for enums | **Open** |
| Bit-exact / runtime golden | **Open** |
