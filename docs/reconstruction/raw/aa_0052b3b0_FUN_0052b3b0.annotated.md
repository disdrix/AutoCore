# Annotated low-level: FUN_0052b3b0 → CVOGCharacter_IsMissionJournalReady

| Field | Value |
|---|---|
| Stable ID | `aa_0052b3b0` |
| VA | `0x0052b3b0` |
| Canonical name | `CVOGCharacter_IsMissionJournalReady` |
| System | `missions-progression` |
| Date | 2026-07-29 (dual A/B seal) |
| Sibling | `aa_0052b420` turn-in (mode 1) |

## Machine-level notes

- Source: live Ghidra decompile + `read_memory` 2026-07-29; matches raw body CF.
- `__thiscall`: ECX = character; stack = mission def; epilogue `ret 4`.
- Shared preamble with `FUN_0052b420` (last obj + active hash `+0x548`).
- Differs: `FUN_0052a020(..., flag=0)` and **no** NPC type-3 loop.

## Layout (sealed)

| Offset | Object | Meaning |
|---|---|---|
| `mission+0x130` | byte | objective count |
| `mission+0x13c` | ptr | objective pointer array |
| `obj+0x10` | u32 | objective id (hash key) |
| `char+0x548` | ptr | active-objectives CNDHash |
| `hash+8` | u32 | bucket mask |
| `hash+0x10` | ptr | buckets |
| `node+0x10` | u32 | key |
| `node+0xc` | ptr | next |
| `node+0x8` | ptr | payload (must non-null) |

## Pseudocode (annotated)

```c
// bool __thiscall IsMissionJournalReady(Character* this, MissionDef* mission)
undefined4 __thiscall FUN_0052b3b0(int param_1 /*char*/, int param_2 /*mission*/)
{
  int lastObj;
  uint objId;
  int node;
  char ready;

  // G1 last objective
  lastObj = *(int *)(*(int *)(param_2 + 0x13c) + -4 + (uint)*(byte *)(param_2 + 0x130) * 4);
  objId = *(uint *)(lastObj + 0x10);

  // G2 active-objectives hash walk @ char+0x548
  node = *(int *)(*(int *)(*(int *)(*(int *)(param_1 + 0x548) + 0x10) +
                           (*(uint *)(*(int *)(param_1 + 0x548) + 8) & objId) * 4) + 4);
  while( true ) {
    if (node == 0) {
      return 0;
    }
    if (objId == *(uint *)(node + 0x10)) break;
    node = *(int *)(node + 0xc);
  }
  if (node == 0) {
    return 0;
  }
  if (*(int *)(node + 8) == 0) {
    return 0;  // payload required
  }

  // G3 mode 0 readiness (eval vtbl+0xc) — NOT mode 1 turn-in
  ready = FUN_0052a020(lastObj, 0);  // decompiler elides this=char (ECX)
  if (ready == '\0') {
    return 0;
  }
  return 1;
  // NO G4 NPC filter (contrast FUN_0052b420)
}
```

## Callers

| VA | Function | Role |
|---|---|---|
| `0x008a2032` | `FUN_008a2020` | wrapper: null `DAT_00d1b6d8` → 0; else thiscall |
| `0x0082a25f` | `FUN_0082a050` | UI `"(Complete)"` vs empty |
| `0x00829c5c` | `FUN_00829b20` | journal helper |

## Contrast vs `FUN_0052b420`

| | Journal (this) | Turn-in |
|---|---|---|
| Flag to `0052a020` | 0 | 1 |
| NPC arg | none | optional |
| Epilogue | `ret 4` | `ret 8` |

## Open questions

- Retail symbol.
- Full product names of journal UI callers.
