# Annotated low-level: FUN_005360f0 / CVOGCharacter_TickUseObjectPending_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005360f0` |
| VA | `0x005360f0`–`0x00536143` (84 B) |
| Ghidra | `FUN_005360f0` |
| Named | `CVOGCharacter_TickUseObjectPending_Inferred` |
| System | `missions-progression` / use-object pending |
| Date | 2026-07-29 (W26-S seal) |
| Dual | A/B accept |

## Machine-level notes

- **thiscall** ECX=character; one float stack formal (frame dt); **AL** bool; **`ret 4`**.
- Gate triple (AND):
  1. VBase presence: `*( *( *(this+4)+4 ) + this + 0xa8 ) + 0x7e != 0`
  2. Pending flag `this+0xc7c != 0`
  3. Timer `*(float*)(this+0xc80) > 0.0` (COMISS 0.0 vs timer; JA fail if timer ≤ 0)
- Tick: `timer -= dt`; store; if `timer <= 0.0` call `ResolveUseObjectPending(1)`.
- Returns **1** if any tick step ran (gate passed); **0** if gate failed (no mutation).
- Sibling commit body: W25-A sealed `CVOGCharacter_ResolveUseObjectPending_Inferred` @ `0x00535ed0` (clears `+0xc80` among pending slots).
- Sole CODE caller: client tick path `FUN_00930040` with `DAT_00d09874+0x30` frame delta.

## Pseudocode (annotated)

```c
// bool __thiscall CVOGCharacter_TickUseObjectPending_Inferred(Character* this, float dt)
// RET 4; AL = 1 if countdown advanced, else 0

bool __thiscall FUN_005360f0(int this /*ECX*/, float dt /*stack*/)
{
  // vbase via MSVC this-adjust chain +0xa8; flag at object+0x7e
  if (*(char *)(*(int *)(*(int *)(*(int *)(this + 4) + 4) + 0xa8 + this) + 0x7e) == 0)
    return 0;
  if (*(char *)(this + 0xc7c) == 0)
    return 0;
  if (!(*(float *)(this + 0xc80) > 0.0f))
    return 0;

  float remaining = *(float *)(this + 0xc80) - dt;
  *(float *)(this + 0xc80) = remaining;
  if (remaining <= 0.0f) {
    // commit pending use-object (kind 10 / UI 0x14 / secondary clear — see aa_00535ed0)
    FUN_00535ed0(/*this ECX*/, 1);
  }
  return 1;
}
```

## Image seal (read_memory @ entry)

| Region | Bytes / fact |
|---|---|
| Entry | `8B 41 04 8B 50 04 8B 84 0A A8 00 00 00` |
| Vbase gate | `80 78 7E 00 74 3C` |
| Flag gate | `80 B9 7C 0C 00 00 00 74 33` |
| Timer > 0 | `0F 57 C9` / `0F 2F 89 80 0C 00 00 73 27` |
| Sub/store | `F3 0F 10 81 80 0C 00 00` / `F3 0F 5C 44 24 04` / `F3 0F 11 81 80 0C 00 00` |
| Commit | `6A 01 E8 96 FD FF FF` → `FUN_00535ed0` |
| Epilogues | `B0 01 C2 04 00` / `32 C0 C2 04 00` |

## Open questions

- Product/PDB method name for countdown tick.
- Exact English for flag `+0xc7c` vs cancel flag `+0xc7d` (owned by Resolve dual).
- Who **arms** `+0xc80` initial duration (not this unit).
