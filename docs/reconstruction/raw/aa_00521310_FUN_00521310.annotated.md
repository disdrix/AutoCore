# Annotated low-level: CVOGCharacter_CreateApply_AttachVehicleAddToMap_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00521310` |
| VA | `0x00521310`–`0x00521430` (288 bytes) |
| Ghidra | `FUN_00521310` |
| Canonical | `CVOGCharacter_CreateApply_AttachVehicleAddToMap_Inferred` |
| System | character create / vehicle attach / sector map |
| Date | 2026-07-29 (W18-R dual A/B seal) |
| Tools | `decompile_function` + `read_memory` only |

## Machine-level notes

- Epilogue: `ret 0x18` — six stack args.
- `param_1` is a **subobject** (`this_adj`); character base ≈ `param_1 - 0xda0`.
- CurrentVehicle COID at `this_adj-0xb90` / `-0xb8c` (matches NPC.md player path).
- `FUN_004c49d0` = `Creature::SetVehicle` (sealed prior art).
- `FUN_004bafe0` = `Object_ResolveTarget_VCall1d4` (resolve + vtbl+0x1d4).
- Packet type imm `0x2016` sealed in body.
- LOD float `0x4479c000` = **999.0f**.

## Annotated control flow

```c
// void __thiscall CVOGCharacter_CreateApply_AttachVehicleAddToMap(
//     CharSub *this_adj, int *pktOrCtx, a3, a4, a5, a6, char flag)

if ((char)flag != 0) {
  FUN_005c93f0(pktOrCtx, a3, a4, a5, a6, flag);  // optional pre-apply
}

// Resolve CurrentVehicleCoid -> vehicle*
vehicle = Object_ResolveTarget_VCall1d4(
  /*bGlobal/type*/ *(u8*)(mi_chain(this_adj, -0xc34)),
  *(u32*)(this_adj - 0xb90),
  *(u32*)(this_adj - 0xb8c));

Creature_SetVehicle(/*character creature view*/, vehicle);  // FUN_004c49d0

if (vehicle != 0) {
  FUN_0053d970(0);  // vehicle-side post (fastcall ECX=vehicle residual)
  vehicle[0x101] = (*(char*)(this_adj - 0xa94) == 0);  // bool flag invert
}

CVOGSectorMap_AddCharacter(this_adj - 0xda0);

// AutoAssault LOD handler boost when create packet type 0x2016
if (*pktOrCtx == 0x2016) {
  host = *(void**)(this_adj - 0xd58);
  if (host && (lod = *(host + 0x94)) != 0) {
    aa = __RTDynamicCast(lod, 0, gfxLODHandler, gfxLODHandler_AutoAssault, 0);
    if (aa) FUN_00760fe0(0, 999.0f);
  }
}

// Optional equip-gfx list clear
obj = *(void**)(mi_chain(this_adj, -0xcf8));
if (obj && *(int*)(obj + 0xe898) != 0) {
  EquipGfxHost_ClearListIfFlag(obj);  // FUN_004962b0
}
```

## Callers

| Site | Caller | Role |
|---|---|---|
| `0x005238c7` | `CVOGCharacter_CreateFromPacket` | Primary create path when vehicle COID valid |
| `0x0053bed3` | undefined / no function | Residual |

## Alias corrections

- Prior: `Named_CalleeOf_CVOGSectorMap_AddCharacter_00521310` — **wrong direction**.
- NPC.md label "Character create apply / CurrentVehicleCoid → SetVehicle" — **confirmed**.

## Open

1. Full semantic names for `param_3..param_6` (forwarded only when flag set).
2. Exact meaning of `vehicle+0x101` bool and `this_adj-0xa94`.
3. Second xref site function recovery.
4. Runtime / differential — open.
