# Annotated low-level: phySkeleton_unserialize

| Field | Value |
|---|---|
| Stable ID | `aa_0096b930` |
| VA | `0x0096b930`–`0x0096ba79` exclusive (**329 B**) |
| Canonical name | `phySkeleton_unserialize` |
| Ghidra name | `FUN_0096b930` |
| System | physics / phy asset I/O (`phySkeleton.cpp`) |
| Date | 2026-08-04 (W38-K) |

## Machine-level notes

- Source: raw capture + W38-K `read_memory` full body + string plate at `0x00aa1254`.
- SEH frame `LAB_009b3077`; stoChunk enter then leave if `local_18` scope flag.
- Imm compare `81 7C 24 18 45 4B 53 50` → tag **`0x50534b45`** (`PSKE` / file bytes `EKSP`).
- Version check: `local_10 == 1` after `SUB EAX,1; JZ success_body`.
- Binary vs text via **`reader+0x4044`**: `FUN_00437050` vs `FUN_007689e0` (int32 readers) into stack slot reused as count.
- `FUN_0096b150(self, count)` before bone loop (resize / seed bone vector — not dual-owned here).
- Bone vector triad at **`self+0x0C` / `+0x10`** (begin/end of `phyBone**`); stride **4** (pointer).
- After each `phyBone_unserialize`, write **`bone+0x144 = index`** (caller-side index assignment; sealed by phyBone dual W16 family).
- Post-success: **`self+0x1C += 1`**, **`self+4 |= 3`** (revision / dirty flags).
- Both tails: **`RET 8`**.

## Recovered CF (annotated)

```c
// stack (self*, reader*); RET 8; returns status (0xffffffff on fail)
uint32_t phySkeleton_unserialize(phySkeleton* self, stoChunkReader* reader)
{
  // SEH + EnterChunkScope(reader) → local_tag, local_ver, local_scopeFlag
  if (chunkTag != 0x50534b45) { // PSKE
    vog_LogMessage(phySkeleton.cpp, 0x3a, 3, "Invalid TAG unserializing phySkeleton");
    goto fail;
  }
  if (chunkVersion != 1) {
    msg = format("Invalid chunk version (%i) unserializing phySkeleton", ver);
    vog_LogMessage(phySkeleton.cpp, 0x53, 3, msg);
    goto fail;
  }
  // version 1:
  uint32_t count_status;
  int count;
  if (*(int*)(reader + 0x4044) == 0)
    count_status = read_i32_binary(reader, &count);   // FUN_00437050
  else
    count_status = read_i32_text(reader, &count);     // FUN_007689e0
  FUN_0096b150(self, count);
  int index = 0;
  for (phyBone** it = *(phyBone***)(self + 0x0C);
       it != *(phyBone***)(self + 0x10);
       ++it, ++index) {
    phyBone_unserialize(/* reader via ESI/param convention */);
    (*it)->index_at_0x144 = index;
  }
  *(int*)(self + 0x1C) += 1;
  *(uint32_t*)(self + 4) |= 3;
  LeaveScope_if(local_scopeFlag); // FUN_00769e40
  return count_status;

fail:
  LeaveScope_if(local_scopeFlag);
  return 0xffffffff;
}
```

## Call graph

| Direction | VA | Name / role |
|---|---|---|
| Caller | `0x00765740` ×2 | gfxBody unserialize — nested PSKE at `body+0x98` |
| Callee | `stoChunkReader_EnterChunkScope` | enter chunk |
| Callee | `FUN_00437050` / `FUN_007689e0` | binary / text int32 |
| Callee | `FUN_0096b150` | apply count / prepare bones |
| Callee | `phyBone_unserialize` (`0x0096e280`) | per-bone PBON |
| Callee | `FUN_0076cec0` | format version error |
| Callee | `vog_LogMessage` | error log |
| Callee | `FUN_00769e40` | leave chunk scope |

## Open questions

1. Exact product English for `FUN_0096b150` and flag bits at `self+4` / counter at `self+0x1C`.
2. phyBone** vector allocator / element ownership (not this VA).
3. Runtime / bit-exact on retail PSKE assets.
4. SEH partial-construction teardown plate beyond leave-scope.
