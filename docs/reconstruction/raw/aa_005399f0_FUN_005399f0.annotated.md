# Annotated low-level: Map_EraseNode_Val12 (`FUN_005399f0`)

| Field | Value |
|---|---|
| Stable ID | `aa_005399f0` |
| VA | `0x005399f0` |
| Canonical | `Map_EraseNode_Val12` (**Inferred**) |
| System | std map/set red-black tree erase (Val12 / node 0x1c) |
| Date | 2026-07-29 (W27-R; supersedes 2026-07-23 scaffold annotate) |

## Machine-level notes

- **Body:** `0x005399f0`–`0x00539ca6` exclusive (**694** B). Ghidra end `0x00539c76` is **wrong** (`operator_delete` noreturn truncates).
- **ABI:** `__thiscall` ECX=map*; stack `(Node** outIt, Node* node)`; **`ret 8`**.
- **Node Val12:** `left/parent/right` + 3-dword value + **`color@+0x18`** + **`isnil@+0x19`**.
- **Map shell:** `head@+0x04` (leftmost / root / rightmost), **`size@+0x08`**.
- **Throw:** `"invalid map/set<T> iterator"` → `_CxxThrowException(..., DAT_00acc34c)`.
- **Post-delete (bytes):** if size>0 then size--; `*outIt = succ`.
- **Rotates:** sealed Val12 `FUN_00573170` / `FUN_00418c10`.
- **Callee `FUN_005ae0b0`:** early node path (value/orphan prep) — leave FUN_*; not re-owned.
- Prefer bytes when decompiler omits post-delete tail.

## Pseudocode (annotated)

```c
// __thiscall; RET 8
void Map_EraseNode_Val12(MapShell* map, Node** outIt, Node* node)
{
  // SEH LAB_009a3ea2
  if (node->isnil) { // +0x19
    // basic_string("invalid map/set<T> iterator") + exception vtbl
    // _CxxThrowException(..., &DAT_00acc34c); // no-return
  }
  FUN_005ae0b0(/* node path — see raw */);
  // classic MSVC _Tree erase:
  //  - choose replacement y (successor or sole child)
  //  - rewire parent left/right; fix head leftmost/root/rightmost
  //    leftmost update may call FUN_005adfa0 (min); rightmost FUN_00418bf0 (max)
  //  - if erased was black: RB fixup loop with color flips +
  //    FUN_00573170 (Lrotate) / FUN_00418c10 (Rrotate)
  operator_delete(node);          // FALLS THROUGH (decomp "noreturn" residual)
  if (map->size > 0) map->size--;
  *outIt = succ;
  // ret 8
}
```

## Ghidra residual

- Many "Removing unreachable block" warnings: RB case folding, not alternate CF.
- `param_2` / outIt poorly recovered in surface decomp; sealed via caller + epilogue store.
- Sole caller `FUN_0053a260` precomputes successor for its loop and still passes `&param_3` as outIt.
