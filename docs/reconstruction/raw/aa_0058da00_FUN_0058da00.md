# Raw capture: FUN_0058da00

| Field | Value |
|---|---|
| **Stable ID** | `aa_0058da00` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0058da00` |
| **Canonical name** | `FUN_0058da00` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0058da00(void *param_1)



{

  if (*(char *)((int)param_1 + 0x29) == '\0') {

    FUN_0058da00(*(undefined4 *)((int)param_1 + 8));

                    /* WARNING: Subroutine does not return */

    operator_delete(param_1);

  }

  return;

}
```


---

## Live re-seal append — 2026-07-29 W24-T dual A/B

| Field | Value |
|---|---|
| **Tools** | Ghidra `decompile_function` + `analyze_function_complete` + `read_memory` |
| **Excluded** | `disassemble_bytes`; Launcher; parent ledgers |
| **Live decompile** | **Incomplete** — collapses left-walk loop; **bytes authoritative** |
| **Body** | `0x0058da00`–`0x0058da34` (**53** bytes) |
| **Epilog** | `POP EDI/ESI/EBX; RET 4` (`5F 5E 5B C2 04 00`) |
| **ABI** | stack `node*`; ECX threaded (thiscall-style, **unused in body**); void; `RET 4` |
| **isnil** | byte @ node **`+0x29`** (`80 7F 29 00` / `80 7E 29 00`) |
| **Links** | left `+0`, right `+8` (parent `+4` unused here) |
| **CF (bytes)** | if isnil return; else loop: recurse free **right+8**, `operator_delete` current, walk **left+0**, until isnil |
| **Caller (1 external)** | `FUN_0058df60` / `MapFloatTfid_EraseRange_Inferred` full-clear path @ `0x0058df7c` — `FUN_0058da00(head[1])` |
| **Self-xref** | recursive call site `0x0058da17` |
| **Sibling twin** | `StdMap_FreeSubtree_Isnil15_Inferred` @ `0x004ba770` (same CF; isnil **+0x15**) |
| **Role** | Free **MapFloatTfid** red-black **subtree** (gather distance-map teardown) |
| **Canonical name** | `MapFloatTfid_FreeSubtree_Isnil29_Inferred` |
| **Reject prior alias** | `Named_CalleeOf_...PollB_0058da00` — chain-name scaffold only |

### Body hex (`read_memory` 53 B)

```
5356578b7c2410807f29008bd98bf7751e8b4608508bcbe8e4ffffff8b3657e8febdefff83c404807e29008bfe74e25f5e5bc20400
```

### Byte CF sketch (overrides decompiler)

```
node = stack_arg
if node->isnil(+0x29): return
loop:
  FreeSubtree(node->right)     // +8, thiscall ECX preserved
  left = node->left            // +0
  operator_delete(node)
  node = left
  if node->isnil: break
return
```
