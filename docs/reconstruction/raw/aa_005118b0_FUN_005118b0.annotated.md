# Annotated low-level: StdTree_Buynode_Node24_Isnil21_Inferred (FUN_005118b0)

| Field | Value |
|---|---|
| Stable ID | `aa_005118b0` |
| VA | `0x005118b0`–`0x00511935` inclusive (**134 B** / `0x86`) |
| System | skills-abilities / STL map-set helpers (Node24 isnil@+0x21 family) |
| Date | 2026-08-05 (R13-030 OWN dual) |
| Canonical | `StdTree_Buynode_Node24_Isnil21_Inferred` |

## Machine-level notes

- **ABI:** `__stdcall`; **5 stack args**; **`RET 0x14`** (`C2 14 00`); **EAX** = node* (or null on OOM). Not thiscall (entry is frame-prolog only; no ECX this).
- **Alloc:** `operator_new(0x24)` → Node24.
- **Links:** `left=param1`, `parent=param2`, `right=param3` at +0/+4/+8.
- **Value:** `key = *param4` at **+0x0C**; residual `FUN_005114e0` thiscall on **node+0x10** with source `param4+1` (vector-of-0x10-byte-elements copy-construct; R13-029 residual).
- **Color / isnil:** `color = param5` at **+0x20**; **isnil forced 0** at **+0x21**.
- **SEH:** FS:[0] frame + `LAB_009a34e1`; unwind tail after RET is compiler EH (outside dual body claims).
- **Sole caller:** parent dual insert `StdTree_InsertAndRebalance_Node24_Isnil21_Inferred` (`0x00511950`, R12-014) at `0x005119e2` with `(head, where, head, value, 0)` → red node.

## Layout (Node24 / isnil21)

| Offset | Field |
|---|---|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x0C | int key (`*value`) |
| +0x10 | vector-like payload (copy via `FUN_005114e0`, element stride 0x10) |
| +0x20 | color (0 red / 1 black) |
| +0x21 | isnil (forced 0) |
| size | **0x24** |

## Pseudocode (annotated)

```c
// __stdcall RET 0x14; EAX = Node24*
Node24* StdTree_Buynode_Node24_Isnil21_Inferred(
    Node24* left, Node24* parent, Node24* right,
    const ValueIntKeyVec* value, uint8_t color)
{
  // SEH frame (ExceptionList / LAB_009a34e1)
  Node24* node = (Node24*)operator_new(0x24);
  if (node != nullptr) {
    node->left = left;
    node->parent = parent;
    node->right = right;
    node->key = value->key;                 // *param_4 → +0x0C
    FUN_005114e0(&node->vec, &value->vec);  // thiscall ECX=node+0x10; src=param_4+1
    node->color = color;                    // +0x20
    node->isnil = 0;                        // +0x21
  }
  return node;
}
```

## Open questions

- Product / MSVC demangle for `value_type` (int key + vector element type).
- Full dual of residual vector ctor `FUN_005114e0` (R13-029, not OWN).
- Runtime / bit-exact / differential.
