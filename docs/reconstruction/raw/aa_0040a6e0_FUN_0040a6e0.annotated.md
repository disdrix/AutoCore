# Annotated low-level: PodPlacementCopyConstruct_IfNonNull_Elem0x138_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0040a6e0` |
| VA | `0x0040a6e0` |
| Ghidra | `FUN_0040a6e0` |
| Named | `PodPlacementCopyConstruct_IfNonNull_Elem0x138_Inferred` |
| System | util / POD placement (Logic-UI event width **0x138**) |
| Date | 2026-08-04 (WQ9I-D dual seal) |
| Dual | A/B `reviews/*_aa_0040a6e0_PodPlacementCopyConstruct_IfNonNull_Elem0x138_Inferred.md` |

## Machine-level notes

- Source: raw capture + live re-verify (no overwrite of raw body; append only).
- Prefer **bytes + call sites** when decompiler hides thiscall ECX handoff into `00408880`.
- SEH frame (`LAB_009bdcb1`) wraps the placement path; scope toggles 0 → 0xffffffff around the call.

## Pseudocode (annotated)

```c
// cdecl; bare ret; parent cleans 8 bytes
// Stack[0x4] = dest*, Stack[0x8] = const src* (0x138 POD)
void PodPlacementCopyConstruct_IfNonNull_Elem0x138_Inferred(
    void* dest,
    const void* src)
{
  // SEH install: ExceptionList frame @ LAB_009bdcb1; scope=0
  if (dest != nullptr) {
    // thiscall: ECX=dest, stack=src, RET 4
    PodCopyConstruct_Elem0x138_Inferred(dest, src);  // FUN_00408880
  }
  // SEH uninstall; ADD ESP,0x10; bare RET
}
```

## Call graph

```text
FUN_00409cc0  ConstructN-style fill (dest, count, src)  stride +0x138 each
  └─ FUN_00409f10  thin stdcall RET8 trampoline
       └─ FUN_0040a6e0  [OWN]  null-checked placement
            └─ PodCopyConstruct_Elem0x138_Inferred (0x00408880) [WQ9H-I]

FUN_0040a4a0  uninit-copy (src_begin, src_end, dst)  stride +0x138
  └─ FUN_00409f10 → FUN_0040a6e0 (same)
```

LogicUiEventQueue_InsertN (`0x004076a0`) calls `00408880` **directly** for the stack value buffer — not via this placement wrapper.

## Sealed facts

| Claim | Conf |
|---|---|
| Dest null → no-op (no construct) | **High** |
| Non-null → POD copy-construct 0x138 via dualed leaf | **High** |
| Width 0x138 via callee + parent stride | **High** |
| cdecl formals / bare ret | **High** |
| Role = MSVC placement / uninitialized construct helper | **High** |
| Product field map of 0x138 event record | **Open** |

## Open questions

- Product / PDB English type for the 0x138 Logic-UI event POD.
- Whether `00409f10` trampoline is a separate dual unit (not OWN here).
- Runtime / bit-exact / differential.
