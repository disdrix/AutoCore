# Annotated low-level: FUN_00401d30 → BasicStringFlag_CtorFromStringAndChar_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00401d30` |
| VA | `0x00401d30`–`0x00401d83` inclusive (**84 B** / `0x54`) |
| Canonical name | `BasicStringFlag_CtorFromStringAndChar_Inferred` |
| Ghidra name | `FUN_00401d30` |
| System | missions-progression (partition parent); unit = shared aggregate construct-from-parts |
| Date | 2026-08-05 (MEGA-065 OWN-ONLY dual) |
| Twin | `BasicStringFlag_CopyCtor_EdiSrc_Inferred` `0x00402040` (MEGA-080 dualed) |

---

## Machine-level notes

- Source: raw capture `aa_00401d30` + live re-verify APPEND 2026-08-05.
- Prefer assembly when decompiler conflicts: **param_3 is `const char*` / byte source**, not `basic_string*`.
- Layout sealed: `basic_string` footprint **0x1C** then **uint8_t flag @ +0x1C** (same as copy twin).
- ABI: three stack formals; **`RET 0xC`**; return dest in EAX. Not thiscall for the aggregate.
- Sole callee: IAT string copy-ctor `[0x009c62ec]`.
- Sole caller: `FUN_00980160` @ `0x009801af` (parse/insert parent under mission-chain residual).
- Retire scaffold `Named_CalleeOf_Named_CalleeOf_Mission_Attempting_to_open_null_d_00401d30`.

## Pseudocode (annotated; ABI-corrected)

```c
// dest on stack; src string*; char* for flag byte; EAX=dest; RET 0xC
BasicStringFlag *
BasicStringFlag_CtorFromStringAndChar_Inferred(
    BasicStringFlag *dest,           /* stack param_1 */
    const basic_string<char> *src,   /* stack param_2 */
    const uint8_t *flag_src)         /* stack param_3 — BYTE load */
{
  // SEH: PUSH -1; PUSH LAB_009bbfb9; FS:[0] chain
  basic_string_copy_ctor(&dest->str, src);   // CALL [0x009c62ec]; ECX=dest
  dest->flag = *flag_src;                    // MOV DL,[ECX]; MOV [ESI+0x1c],DL
  return dest;
}
```

### Decompiler vs assembly

| Decompiler (raw) | Assembly truth |
|---|---|
| `param_3` typed `basic_string*` | `MOV ECX,[ESP+0x1c]; MOV DL,[ECX]` — **byte** |
| `param_1[0x1c] = *param_3` (string-sized index) | `MOV byte ptr [ESI+0x1c],DL` — offset **0x1c** after 0x1c-byte string |
| Signature looks like 3× string* | dest = aggregate; src = string; flag = char* |

## Control flow

1. Install SEH (`LAB_009bbfb9` / FS:[0]) — shared with copy twin / string dtor `004020f0`.
2. `basic_string` copy-ctor into `dest+0`.
3. SEH state = 0 (string live).
4. Copy flag byte from `*param_3` → `dest+0x1c`.
5. SEH state = -1; unlink; return dest; **`RET 0xC`**.

## Callers / callees

| Direction | Target | Notes |
|---|---|---|
| Caller | `FUN_00980160` @ `0x009801af` | after `substr`; then `00402040` with EDI=EAX |
| Callee | `[0x009c62ec]` string copy-ctor | demangle `...QAE@ABV01@@Z` |

## Open questions (gaps)

- Product English for flag byte (bool / enum / tag).
- Parent map/value_type product name for insert peer `FUN_00401c50` / node ctor `FUN_00401fe0`.
- Runtime / bit-exact / differential (Terminal false).
