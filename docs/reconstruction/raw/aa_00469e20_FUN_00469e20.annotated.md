# Annotated low-level: StdVector_InsertOne_RebindIt_Elem28_ValueEdx_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00469e20` |
| VA | `0x00469e20`–`0x00469e92` exclusive (**114 B**) |
| System | MSVC `vector` insert-one + rebind, POD elem **0x1c** |
| Date | 2026-07-29 (W36-Q) |
| Ghidra | `FUN_00469e20` |

## Machine-level notes

- **EDI = vec*** (not thiscall ECX). Confirmed entry `8B 77 04` uses EDI+4 begin; parent `FUN_00469c80` loads `mov edi, ecx` then calls with EDI live.
- Stack formals (after pushes): **outIt*** @ arg0 → EBP; **where** @ arg1 → EBX; **value*** @ arg2 → EDX into InsertN.
- **RET 0x0C** (`C2 0C 00`) — three dword stack args.
- Returns **outIt*** in EAX (`mov eax, ebp`).
- Stride **0x1c** via magic `0x92492493` (same family as W34-Q insert-n / W33-J size).
- Engine: sealed `StdVector_InsertN_Elem28_ValueEdx_Inferred` (`0x00469f50`) with **count=1**.
- Index computed **before** insert; rebind uses **post-insert** begin.

## Pseudocode (annotated)

```c
// EDI=vec*, stack (outIt*, where, value*); RET 0xC
Elem28** StdVector_InsertOne_RebindIt_Elem28_ValueEdx_Inferred(
    StdVectorElem28* vec,   /* EDI */
    Elem28** outIt,         /* stack0 */
    Elem28* where,          /* stack1 */
    const Elem28* value)    /* stack2 → EDX */
{
  Elem28* begin = vec->begin; /* +4 */
  uint32_t index = 0;

  if (begin != nullptr) {
    uint32_t size = (vec->end - begin) / 0x1c; /* +8 */
    if (size != 0)
      index = (where - begin) / 0x1c;
  }

  /* EDX=value*, stack (vec, where, count=1), RET 0xC on InsertN */
  StdVector_InsertN_Elem28_ValueEdx_Inferred(value, vec, where, 1);

  *outIt = (Elem28*)((char*)vec->begin + index * 0x1c);
  return outIt;
}
```

## Call graph

| Role | VA / name |
|---|---|
| Sole caller | `FUN_00469c80` @ `0x00469cf9` — push_back: spare-cap → fill-n (`FUN_0046a260`); else this |
| Sole callee | `0x00469f50` `StdVector_InsertN_Elem28_ValueEdx_Inferred` (W34-Q) |

## Twin / siblings

| VA | Role |
|---|---|
| `0x004612d0` | `StdVector_InsertOne_RebindIt_RcElem28` — same CF/ABI; RC insert-n engine |
| `0x0040dbf0` | dword insert-one rebind (thiscall/stride-4 — do not unify) |

## Open gaps

- Product/PDB T English for POD Elem28.
- Parent `FUN_00469c80` dual not owned.
- Runtime / bit-exact.
