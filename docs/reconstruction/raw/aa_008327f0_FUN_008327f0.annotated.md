# Annotated low-level: CMenuChatlog_CompleteDtor (FUN_008327f0)

| Field | Value |
|---|---|
| Stable ID | `aa_008327f0` |
| VA | `0x008327f0`–`0x0083282a` inclusive (**59 B** / `0x3B`) |
| System | MSVC derived complete destructor / `CMenuChatlog` / `PTR_FUN_00a70524` |
| Date | 2026-08-05 (WQ9J-C OWN dual) |
| Canonical name | `CMenuChatlog_CompleteDtor` |
| Ghidra name | `FUN_008327f0` |

## Machine-level notes

- Source: live `batch_decompile` + `disassemble_function` + full-body `read_memory` + `analyze_function_complete` + RTTI COL walk.
- Classic MSVC **complete** dtor (not scalar): reinstall most-derived vtbl, chain base teardown, leave host free to scalar.
- ABI: **ECX=this**; SEH frame; **`ADD ESP,0x10; RET`**; void; no stack args.
- Sole CALL site: scalar `FUN_004165f0` (vtbl[0]) dualed WQ9I-J.
- Base chain (not OWN): `FUN_0082d540` installs intermediate `PTR_FUN_00a72f6c`, runs `FUN_0082ce20`, then `FUN_00792c20` (deep UI-window base teardown).
- RTTI product name **CMenuChatlog** sealed; peer ctor string `"i_m_chatlog.xml"` is corroborating only.

## Pseudocode (annotated)

```c
// ECX = this; SEH; ADD ESP,0x10; RET; complete dtor — does not free this
void __fastcall CMenuChatlog_CompleteDtor(void* self)
{
  // SEH: push -1; push LAB_009b453f; link FS:[0]
  *reinterpret_cast<void**>(self) = &PTR_FUN_00a70524;  // CMenuChatlog vtbl
  // EH state = 0xFFFFFFFF
  FUN_0082d540(self);  // intermediate base complete → … → FUN_00792c20
  // restore ExceptionList; ADD ESP,0x10; RET
}
```

## Control flow

```text
SEH prologue (LAB_009b453f)
*this = PTR_FUN_00a70524
EH = -1
CALL FUN_0082d540(this)     // base chain
SEH epilogue; ADD ESP,0x10; RET
```

## Related (not owned)

| Unit | VA | Note |
|---|---|---|
| Scalar | `0x004165f0` | WQ9I-J dual (vtbl[0]); name still vtbl-based `_Inferred` at dual time |
| Intermediate complete | `0x0082d540` | install `00a72f6c` + `0082ce20` + `00792c20` |
| Deep base | `0x00792c20` | large CNDUIWnd-family teardown |
| Peer ctor | `0x00832830` | same vtbl; `"i_m_chatlog.xml"` |

## Open questions

- Full dual of intermediate `0082d540` / deep base `00792c20` (not OWN).
- Vtbl slots [1+] English.
- Runtime / bit-exact / differential — open.
