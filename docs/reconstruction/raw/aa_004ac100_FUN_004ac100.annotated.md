# Annotated low-level: FUN_004ac100 → MapChild_RebuildHostElemsAndIntGrid_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004ac100` |
| VA | `0x004ac100`–`0x004ac215` exclusive (**277 B** / `0x115`) |
| System | map-child spatial table rebuild (host 0x28-elems + int grid) |
| Date | 2026-07-29 (W31-L re-annotate) |
| Canonical | `MapChild_RebuildHostElemsAndIntGrid_Inferred` |

---

## Machine-level notes

- **thiscall** ECX = map-child object (0x3fc class from `FUN_004ad730` path); stack formal **unused**; **`ret 4`**; returns **0/1** in AL.
- Fail: `*(this+0x3d8)==0` → `FUN_007a4480(0,"VOG_DEBUG_STOP"@0x00a15844)` → return 0. String is a **guard label**, not the method name — reject `Named_VOG_DEBUG_STOP_004ac100`.
- Host rebuild: load mapCtx=`*(this+0x340)`, host=`*(mapCtx+0xe898)`, `FUN_004941b0(host, mapCtx)` — decompile drops host load.
- Dim tiles: `*(this+0x384) = signed_div16(*(this+0x10))`; same for `+0x388` from `+0x14`. Formula `(x+(x>>31&0xf))>>4`.
- Int grid at `+0x38c`: `delete[]` old; `new[](dimX*dimY*4)`; zero with `rep stosd` + remainder `rep stosb` (bytes; decompile remainder dead).
- Follow-ups: `FUN_004abd00(this)`, `FUN_004aa300(this)`, `FUN_004930b0(*(mapCtx+0xe894))`.
- Tail branch on `DAT_00b03631`: `FUN_005c0f40(this)` vs `FUN_005bf720()`; copy flag → `DAT_00b03632`; return 1.
- Callers: `FUN_004ac220` (push 0), `FUN_004d9cd0` (ecx=map[+0xe4f8] child), site `0x00952078`.

## Pseudocode (bytes-corrected)

```c
// __thiscall; ECX=mapChild; unused stack arg cleaned (ret 4); returns uint8_t 0/1
uint8_t __thiscall MapChild_RebuildHostElemsAndIntGrid_Inferred(void* self, int /*unused*/)
{
  if (*(int*)((char*)self + 0x3d8) == 0) {
    FUN_007a4480(0, "VOG_DEBUG_STOP");
    return 0;
  }

  void* mapCtx = *(void**)((char*)self + 0x340);
  void* host   = *(void**)((char*)mapCtx + 0xe898);
  FUN_004941b0(host, mapCtx);  // Host_RebuildElem0x28ArrayFromMap_Inferred (W30-Q)

  int dimX = (*(int*)((char*)self + 0x10) + ((*(int*)((char*)self + 0x10) >> 31) & 0xf)) >> 4;
  int dimY = (*(int*)((char*)self + 0x14) + ((*(int*)((char*)self + 0x14) >> 31) & 0xf)) >> 4;
  *(int*)((char*)self + 0x384) = dimX;
  *(int*)((char*)self + 0x388) = dimY;

  void* old = *(void**)((char*)self + 0x38c);
  if (old) operator_delete[](old);
  *(void**)((char*)self + 0x38c) = nullptr;

  size_t bytes = (size_t)dimX * (size_t)dimY * 4;
  uint32_t* grid = (uint32_t*)operator_new[](bytes);
  *(uint32_t**)((char*)self + 0x38c) = grid;
  // zero: rep stosd (bytes/4) + rep stosb (bytes&3)

  FUN_004abd00(self);
  FUN_004aa300(self);
  FUN_004930b0(*(void**)((char*)mapCtx + 0xe894));

  if (DAT_00b03631 != 0) {
    FUN_005c0f40(self);
  } else {
    FUN_005bf720();
  }
  DAT_00b03632 = DAT_00b03631;
  return 1;
}
```

## Open questions

- Product English for map-child / host / `+0x3d8` required field.
- Semantics of `FUN_004abd00` / `FUN_004aa300` / `FUN_004930b0` (not OWN).
- Meaning of `DAT_00b03631` / `DAT_00b03632` debug switch.
- Runtime / bit-exact / differential.
