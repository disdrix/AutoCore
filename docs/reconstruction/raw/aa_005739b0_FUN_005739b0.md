# Raw capture: FUN_005739b0

| Field | Value |
|---|---|
| **Stable ID** | `aa_005739b0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005739b0` |
| **Canonical name** | `FUN_005739b0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_005739b0(undefined4 *param_1,undefined4 *param_2,undefined4 *param_3)

{
  for (; param_1 != param_2; param_1 = param_1 + 1) {
    *param_1 = *param_3;
  }
  return;
}
```
# Dual A/B re-verify (W19-B, 2026-07-29)

**Tools:** Ghidra `decompile_function` + `read_memory` only. No `disassemble_bytes`. No Launcher.

### Live decompile (re-confirm)

```c
void FUN_005739b0(undefined4 *param_1,undefined4 *param_2,undefined4 *param_3)

{
  for (; param_1 != param_2; param_1 = param_1 + 1) {
    *param_1 = *param_3;
  }
  return;
}
```

### Function bounds

| Field | Value |
|---|---|
| Entry | `0x005739b0` |
| Body | `0x005739b0`–`0x005739cd` (**30 bytes** inclusive of `ret`; + INT3 pad) |
| Ghidra name | `FUN_005739b0` |
| ABI | `__cdecl` — three stack pointers; bare **`ret`** (no `ret N`) |
| Leaf | yes (no callees) |

### Byte seal (`read_memory` @ `0x005739b0`, 48 B; body 30 B + INT3 pad)

```
hex: 8b4424048b4c24083bc174118b54240c568b32893083c0043bc175f55ec3
     cc cc ...

8B 44 24 04        mov  eax, [esp+4]       ; first (dst cursor)
8B 4C 24 08        mov  ecx, [esp+8]       ; last  (dst end, exclusive)
3B C1              cmp  eax, ecx
74 11              je   done               ; empty range
8B 54 24 0C        mov  edx, [esp+0xC]     ; value* (template dword ptr)
56                 push esi
                 loop:
8B 32              mov  esi, [edx]         ; re-load *value each iter
89 30              mov  [eax], esi         ; *first = *value
83 C0 04           add  eax, 4             ; first++
3B C1              cmp  eax, ecx
75 F5              jne  loop
5E                 pop  esi
                 done:
C3                 ret
CC...              int3 pad
```

### Algorithm seal

1. `first = [esp+4]`, `last = [esp+8]`, `value = [esp+0xC]` — all pointers.
2. While `first != last`: store `*value` into `*first`; advance `first` by **4** bytes (dword element).
3. Re-reads `*value` every iteration (not hoisted to a register for the whole loop — classic fill-from-pointer).
4. Empty range (`first == last`) is a no-op return.
5. **Does not** advance or write through `value`. **Does not** construct objects (POD dword assign).

### Callers (sample; 29 listed)

`FUN_004073a0` (`StdVector_InsertN_Dword` family), `FUN_004507a0` (vector insert fill after shift), `FUN_004490a0`, `FUN_00493830`, `FUN_0049c8a0`, `FUN_004a6f70`, `FUN_004a76e0`, `FUN_004ba850`, `FUN_004c2bb0`, `FUN_004e39d0`, `FUN_005064e0`, `FUN_00539810`, `FUN_005438c0`, `FUN_00548210`, `FUN_00574470`, `FUN_0057fd40`, `FUN_00596770`, `FUN_005a3920`, `FUN_005b7860`, `FUN_005b7bd0`, `FUN_005c9cc0`, `FUN_005f52b0`, `FUN_00602920`, `FUN_00604500`, `FUN_006762a0`, `FUN_0067f930`, `FUN_006890c0`, `FUN_0068bae0`, `FUN_00697f60`.

Example parent `FUN_004507a0` (insert-N): after shifting/growing, calls `FUN_005739b0(begin_hole, end_hole, &template_dword)` to assign the template across the newly exposed dword range.

### Role seal

MSVC-style **`std::fill` / range fill** for **4-byte POD** elements over half-open `[first, last)`. Shared helper for vector insert / assign paths.

**Canonical name (INFERRED role):** `StdFill_DwordRange`

