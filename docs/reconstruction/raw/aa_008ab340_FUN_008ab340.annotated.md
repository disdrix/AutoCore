# Annotated low-level: FUN_008ab340 → MissionSelect_ClearRowWidgetVectors_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_008ab340` |
| VA | `0x008ab340` |
| System | `missions-progression` |
| Date | 2026-08-05 (MEGA-105 refresh) |
| Preferred name | `MissionSelect_ClearRowWidgetVectors_Inferred` |

## Machine-level notes

- Live Ghidra re-verify MEGA-105: body **521 B**, ESI host ABI, two pointer-vector destroy/clear loops.
- Scaffold `unaff_ESI` is **deliberate register ABI** (not junk): callers set ESI = host.
- Decompiler shows `memmove(..., 0)` because image computes `end - end` size before the call; effect is **end = begin**.
- Retire scaffold plate `Named_CalleeOf_Mission_Select_a_Mission_008ab340` — alias only.

## Layout (mission-select host, ESI)

| Offset | Role |
|---|---|
| +0x000 | vtbl* (host) |
| +0x72c | vector A begin (`T**`) — list-row widgets bank 1 |
| +0x730 | vector A end |
| +0x734 | vector A capacity (dtor only) |
| +0x73c | vector B begin (`T**`) — list-row widgets bank 2 |
| +0x740 | vector B end |
| +0x744 | vector B capacity (dtor only) |

## Per-element destroy protocol

```text
w = vec[i]
if w:
  if w->vtbl[0x3d8]():        // bool query (product open)
    w->vtbl[0x440]()          // follow-up (product open)
  if *(w + 0x2b0) != 0:
    host->vtbl[0xB0](w)       // host unregister/release child
  if w:
    w->vtbl[0](1)             // scalar deleting dtor
  vec[i] = 0
// then end = begin (used-range clear)
```

## Pseudocode (annotated, ABI-corrected)

```c
// ESI = MissionSelectHost*  (register this; no stack args; plain RET)
void MissionSelect_ClearRowWidgetVectors_Inferred(void)
{
  int *host; /* ESI */
  /* ---- Vector A @ +0x72c / +0x730 ---- */
  int count;
  if (host[0x1cb] == 0) count = 0;
  else count = (host[0x1cc] - host[0x1cb]) >> 2;
  for (int i = 0; i < count; i++) {
    int *slot = (int *)(host[0x1cb] + i * 4);
    if (*slot != 0) {
      int *w = (int *)*slot;
      char q = (*(char (**)(int *))(*(int *)w + 0x3d8))(w);
      if (q != 0)
        (*(void (**)(int *))(*(int *)w + 0x440))(w);
      if (*(int *)((char *)w + 0x2b0) != 0)
        (*(void (**)(int *, int *))(*host + 0xb0))(host, w);
      if (*slot != 0 && (w = (int *)*slot) != 0)
        (*(void (**)(int *, int))(*w))(w, 1);
      *slot = 0;
    }
  }
  {
    void *begin = (void *)host[0x1cb];
    void *end = (void *)host[0x1cc];
    if (begin && ((int)end - (int)begin) >> 2 != 0 && begin != end) {
      /* image: memmove(begin, end, 0); end = begin */
      host[0x1cc] = (int)begin;
    }
  }
  /* ---- Vector B @ +0x73c / +0x740 (mirror) ---- */
  if (host[0x1cf] == 0) count = 0;
  else count = (host[0x1d0] - host[0x1cf]) >> 2;
  for (int i = 0; i < count; i++) {
    int *slot = (int *)(host[0x1cf] + i * 4);
    if (*slot != 0) {
      int *w = (int *)*slot;
      char q = (*(char (**)(int *))(*(int *)w + 0x3d8))(w);
      if (q != 0)
        (*(void (**)(int *))(*(int *)w + 0x440))(w);
      if (*(int *)((char *)w + 0x2b0) != 0)
        (*(void (**)(int *, int *))(*host + 0xb0))(host, w);
      if (*slot != 0 && (w = (int *)*slot) != 0)
        (*(void (**)(int *, int))(*w))(w, 1);
      *slot = 0;
    }
  }
  {
    void *begin = (void *)host[0x1cf];
    void *end = (void *)host[0x1d0];
    if (begin && ((int)end - (int)begin) >> 2 != 0 && begin != end) {
      host[0x1d0] = (int)begin;
    }
  }
}
```

## Call graph

```text
FUN_008ab550  Mission_Select_a_Mission (parent residual MEGA-051)
  ├─ CALL 008ab340 @ 008ab6c4  (show-path early return)
  └─ CALL 008ab340 @ 008ab791  (hide-path fallthrough)

FUN_008ac340  host dtor (not dualled)
  └─ CALL 008ab340 @ 008ac36c  then operator_delete both buffers
```

## Open questions

- Product/PDB English for function and virtuals +0x3d8 / +0x440 / host +0xB0.
- Product distinction between vector A vs B (two row banks / pages).
- Runtime / bit-exact / differential (no Launcher).
