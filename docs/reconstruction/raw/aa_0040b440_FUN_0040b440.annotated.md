# Annotated low-level: StdTree_EraseAndRebalance_Val12_RetC_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0040b440` |
| VA | `0x0040b440`–`0x0040b6d0` exclusive (**656 B** / `0x290`) |
| Ghidra end | `0x0040b6a0` (false noreturn on `operator_delete`) |
| Canonical name | `StdTree_EraseAndRebalance_Val12_RetC_Inferred` |
| Ghidra name | `FUN_0040b440` |
| System | inventory-transfer (partition R13-018; unit role = generic MSVC `_Tree` erase) |
| Date | 2026-08-05 (R13-018 OWN dual) |
| ABI | **3 stack args**, **`ret 0xC`** — **not** thiscall RET 8 |

## Machine-level notes

- Source: raw capture for `aa_0040b440` + R13-018 live re-verify.
- Prefer assembly when decompiler conflicts (two-child path "unreachable"; epilogue past false noreturn).
- Layout **Val12**: color@**+0x18**, isnil@**+0x19**, node **0x1C**, shell head@+4 / size@+8.
- Do **not** merge with thiscall Val12 erase `0x004e4130` (RET 8) or isnil21/29/31 erase families.
- Parent dual `0x0092b2a0` MissionTracker_FillTrackedTargetSlots (not dualled here).

## Calling convention (assembly-sealed)

| Slot | Role | Evidence |
|---|---|---|
| stack0 | MapShell* | `MOV EBP,[ESP+0x58]` after SEH+locals; uses `EBP+4` head, `EBP+8` size |
| stack1 | outIt** | epilogue `MOV EAX,[ESP+0x5c]; MOV [EAX],EDX` |
| stack2 | node* | early `MOV EAX,[ESP+0x18]`; isnil check `+0x19` |
| cleanup | **`ret 0xC`** | `C2 0C 00` @ `0x0040b6cd` |
| Call sites | 3× PUSH | `0040bcae`, `0092b741` |

## Semantics

MSVC-style **`_Tree` erase + RB rebalance** for **isnil@+0x19** (Val12):

1. SEH frame (`LAB_009bc919` / FS:[0]).
2. Nil iterator → `"invalid map/set<T> iterator"` @ `0x00a152f0` → `FUN_00401bc0` → `_CxxThrowException` / `DAT_00acc34c`.
3. Successor via `FUN_00457cc0` (iterator++ isnil19 — dualed W38-B).
4. Unlink + head extremity fix (min `0043bf90` / max `0040b8f0`); two-child swap path @ `0x0040b539` (color swap +0x18).
5. If erased black → recolor + Lrotate `00446b10` / Rrotate `0040b910`.
6. `operator_delete(node)` (no value dtor); size--; `*outIt = succ`; **ret 0xC**.

## Pseudocode (annotated; ABI corrected)

```c
// Stack stdcall-style: map*, outIt**, node*; ret 0xC
// Decompiler shows void + false noreturn on delete — epilogue continues (size--, *outIt, ret 0xC).

void StdTree_EraseAndRebalance_Val12_RetC_Inferred(
    MapShell_Val12 *map,      // stack0 → EBP
    MapNode_Val12 **outIt,    // stack1
    MapNode_Val12 *node)      // stack2
{
  // SEH: LAB_009bc919
  if (node->isnil /* +0x19 */) {
    // basic_string("invalid map/set<T> iterator" @ 0x00a152f0)
    // FUN_00401bc0 → _CxxThrowException(..., &DAT_00acc34c)
  }

  FUN_00457cc0(); // iterator++ / capture successor (Isnil19)

  // Replacement child selection (MSVC erase splice)
  MapNode_Val12 *repl = node->left;
  if (repl->isnil == 0) {
    if (node->right->isnil == 0)
      repl = /* successor path / right-min via two-child plate */;
  } else {
    repl = node->right;
  }
  // ... unlink; fix root/leftmost(min 0043bf90)/rightmost(max 0040b8f0) ...
  // two-child successor-swap @ 0x0040b539 (decomp "unreachable")

  if (node->color /* +0x18 */ == 1 /* black */) {
    // RB fixup: Lrotate FUN_00446b10 / Rrotate FUN_0040b910
  }

  operator_delete(node); // no value dtor
  if (map->size > 0) map->size--;
  *outIt = succ;
  // ret 0xC
}
```

## Callees

| VA | Role |
|---|---|
| `FUN_00457cc0` | `StdTree_IteratorIncrement_Isnil19` (dualed) |
| `FUN_0043bf90` | min / leftmost (residual) |
| `FUN_0040b8f0` | max / rightmost (residual) |
| `FUN_00446b10` | Lrotate Val12 family |
| `FUN_0040b910` | Rrotate Val12 family |
| `FUN_00401bc0` | out_of_range ctor |
| `operator_delete` | free node |
| basic_string / `_CxxThrowException` | throw plate |

## Callers

| Site | Function | Notes |
|---|---|---|
| `0x0040bcae` | `FUN_0040bc10` | erase-range loop (R13-020) |
| `0x0092b741` | `FUN_0092b2a0` | MissionTracker parent (partition parent) |

## Open questions

- Product / PDB demangle for host map type under MissionTracker / erase-range parent.
- Dual of range parent `FUN_0040bc10`, min `0043bf90`, max `0040b8f0`, L/R rotates.
- Runtime / bit-exact / differential (deferred; Terminal false).
