# Annotated low-level: SkillStaging_ZeroPayloadFields_Inferred (`FUN_0054cc10`)

| Field | Value |
|---|---|
| Stable ID | `aa_0054cc10` |
| VA | `0x0054cc10`–`0x0054d43f` (2096 B / `0x830`) |
| System | skills-abilities |
| Date | 2026-08-05 (R13-038 dual seal; scaffold 2026-07-23) |
| Canonical name | `SkillStaging_ZeroPayloadFields_Inferred` |
| Ghidra | `FUN_0054cc10` |
| Dual start | 2686 |
| Partition parent dual | `0x0054cbb0` (adjacency; not semantic) |
| Semantic caller | `0x0054a290` `SkillDef_InstallLoadedRecord` |

## Machine-level notes

- **Leaf** bulk zero of skill-staging / skill-template mid-fields.
- Entry: `XORPS xmm0,xmm0`; `MOV EAX,ECX` (this → EAX); then interleaved `MOVSS [eax+imm],xmm0` and `MOV [eax+imm],ECX` (ECX zeroed after this-copy) for every dword slot from **`+0x28` through `+0x534`**.
- Store order is not strictly ascending at the head (`+0x2c`, `+0x30`, then `+0x28`, …) — independent stores; final state is all-zero over that range.
- Terminal bare **`C3`** at `0x0054d43f`. No callees, no branches, no SEH.
- Sole call site `0x0054a2d1`: `LEA ECX,[ESP+0x1a0]` → `CALL` — ECX points at install-path stack staging buffer (same base the caller then `REP STOSD` for `0x14e` dwords / `0x538` B).
- Relative to full staging clear: this leaf zeros only **`[base+0x28, base+0x538)`**, leaving a **`0x28`-byte header** untouched; caller’s subsequent full clear zeros the header too. On the only known path the mid-zero is a subset of the following memset — still a real shared-shape leaf with sealed field span.

## Signature (image-sealed)

```c
// thiscall: ECX = staging/template object*
// 0 stack args; bare RET; void
void __fastcall SkillStaging_ZeroPayloadFields_Inferred(void *self);
```

## Pseudocode (behavior-preserving compact form)

```c
void __fastcall SkillStaging_ZeroPayloadFields_Inferred(void *self)
{
  /* retail: SSE + integer dword stores; equivalent final state: */
  uint32_t *p = (uint32_t *)((char *)self + 0x28);
  int i;
  for (i = 0; i < 0x144; i++) {   /* 0x28 .. 0x534 inclusive */
    p[i] = 0;
  }
}
```

## Field range seal

| Bound | Offset | Evidence |
|---|---|---|
| First mid-field | `+0x28` | entry stores `mov [eax+0x28],ecx` (after `+0x2c/+0x30`) |
| Last field | `+0x534` | exit `movss [eax+0x534],xmm0` then `ret` |
| Count | `0x144` dwords | `(0x534-0x28)/4+1` |
| Staging size (caller) | `0x538` B / `0x14e` dwords | post-call `MOV ECX,0x14e` + `REP STOSD` |

## Call graph (OWN evidence only)

```text
FUN_0054a290  SkillDef_InstallLoadedRecord  [not OWN this dual]
  └─ after element-count gate
  └─ LEA ECX,[ESP+0x1a0]
  └─ CALL FUN_0054cc10   [OWN R13-038]
  └─ REP STOSD 0x14e dwords @ same buffer
  └─ pack flags / FUN_005495f0 / map insert …
```

## Gaps

1. Product English for each dword/float slot in `+0x28..+0x534` (open; bulk zero only sealed).
2. Why mid-zero exists before full `0x14e` clear on sole path (redundant subset; historical or shared-helper shape).
3. Whether other indirect/vtable reachability exists beyond the single CALL xref.
4. Runtime / bit-exact / differential — open (Terminal false).
