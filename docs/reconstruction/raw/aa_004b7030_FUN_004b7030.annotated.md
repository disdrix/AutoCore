# Annotated low-level: FUN_004b7030 / NDSpecialFX_ForEachOwnedBit7_ApplyFxToggle_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004b7030` |
| VA | `0x004b7030` |
| Body | `004b7030`–`004b708a` (90 B) |
| System | `client-fx` / NDSpecialFX owned-child apply |
| Date | 2026-07-29 (W24-D dual seal) |
| Proposed name | `NDSpecialFX_ForEachOwnedBit7_ApplyFxToggle_Inferred` |

## Machine-level notes

- **ABI:** thiscall, ECX = FX host; four stack args; epilogue `ret 0x10` (`C2 10 00`).
- **List:** circular sentinel at `host+0x204` (same own-list as `NDSpecialFX_DeactivateClear_Inferred` @ `0x004b7150`).
- **Node:** `esi = *cursor`; payload / child object = `esi[2]` (`[esi+8]`).
- **Gate:** `*(byte*)(child + 0x90e) & 0x80` (bit7) — same flag family as deactivate phys-detach / special-FX bit7.
- **Callee this:** bytes set `ECX = child` before both apply helpers (decompiler drops this).
- **Branch:** `stack_char param_4 == 0` → `FUN_0058e300` (no slot filter; requires `child+0x25c < 0`); else → `FUN_0058e270` (slot filter `param_5`, or `-1` wild).
- **Callee role (inferred from their bodies):** start/stop particle/FX payloads at child `+0x258`/`+0x914`/`+0x254` and toggle `+0x90f` bit0.

## Signature

```c
// thiscall; RET 0x10 (4 stack dwords)
void __thiscall NDSpecialFX_ForEachOwnedBit7_ApplyFxToggle_Inferred(
    void *fx_host,          // ECX
    int want_start,         // stack+0x04 → child apply arg0
    int want_stop,          // stack+0x08 → child apply arg1
    char use_slot_filter,   // stack+0x0c; 0 => 0058e300 else 0058e270
    int slot_filter);       // stack+0x10; passed only on 0058e270 path (-1 = any non--1 slot)
```

## Pseudocode (annotated)

```c
void __thiscall FUN_004b7030(
    int host,
    undefined4 want_start,
    undefined4 want_stop,
    char use_slot_filter,
    undefined4 slot_filter)
{
  int *node;
  int *child;

  node = *(int **)(*(int *)(host + 0x204));   // first after sentinel
  if (node != *(int **)(host + 0x204)) {
    do {
      child = (int *)node[2];                 // [node+8]
      if ((*(byte *)((int)child + 0x90e) & 0x80) != 0) {
        // ECX = child  (byte-proven; not in raw decompile call form)
        if (use_slot_filter == '\0') {
          FUN_0058e300(child, want_start, want_stop);
        } else {
          FUN_0058e270(child, want_start, want_stop, slot_filter);
        }
      }
      node = (int *)*node;
    } while (node != (int *)*(int *)(host + 0x204));
  }
}
```

## Callers (live xrefs)

| Site | Function | Typical args |
|---|---|---|
| `0x004c815f`, `0x004c81bf` | `FUN_004c8110` | `(0, effect*, 1, -1)` |
| `0x004ffe12` | `FUN_004ff850` | `(*slot_host, 0, 1, slotIndex)` — near `%s_VEHICLE_EXHAUST_%d` path |
| `0x00582a0b` | `FUN_005829d0` | `(fx, 0, 0, -1)` → unrestricted `0058e300` |
| `0x00583f43`, `0x00583f81` | `FUN_00583f10` | passthrough fan-out over attachment tables |
| `0x0058437f` | `FUN_00584330` | `(0, fx, 0, -1)` |

## Open questions

- Product C++ method name on NDSpecialFX host.
- Exact English for `+0x90e` bit7 (phys-detach vs “has FX payload” vs both).
- Full dual of `FUN_0058e270` / `FUN_0058e300` (referenced as residual in W23-H deactivate review).
- Runtime golden / bit-exact.
