# Annotated low-level: FUN_005b3f60 / Obj_DefaultCtor_SetVtbl_EmptyListAt10_ZeroF4At20

| Field | Value |
|---|---|
| Stable ID | `aa_005b3f60` |
| VA | `0x005b3f60`–`0x005b3f9a` exclusive (**58 B** / `0x3A`) |
| System | host object / MSVC default ctor |
| Date | 2026-07-29 (W30-O re-verify) |

## Machine-level notes

- **Bytes authority:** body is default ctor for a **0x30**-byte host with final vtbl `PTR_FUN_009d95b4`.
- Installs vtbl, constructs empty circular list at `this+0x10` via `FUN_005b43b0` sentinel buy (head @ `+0x14`, size @ `+0x18`), zeros four dwords as floats at `+0x20..+0x2c` via `xorps`/`movss`.
- Decompile indexes (`param_1[5]`/`[6]` = `+0x14`/`+0x18`) are numerically correct but hide list base LEA `EDI = this+0x10`.
- Does **not** initialize `+0x04..+0x0C` or `+0x1C` (gap / host-owned elsewhere).
- Sole live default-ctor path: both callers `operator_new(0x30)` then this, store at host `+0xe4fc`.
- Pairs with body dtor `Obj_BodyDtor_SetVtbl_DestroyStdListAt10` (`0x005b3b20`, W28-N) and scalar dtor `FUN_005b44d0` (vtbl[0]).

## Pseudocode (byte-corrected)

```c
// ECX = this*; returns this in EAX; bare RET
void * __fastcall Obj_DefaultCtor_SetVtbl_EmptyListAt10_ZeroF4At20(void *self)
{
  void *list = (uint8_t *)self + 0x10;
  void *sentinel;

  *(const void **)self = (const void *)0x009d95b4; // PTR_FUN_009d95b4
  // ECX = list (unused inside buy helper)
  sentinel = StdList_BuySelfLinkedSentinel_0x30(); // FUN_005b43b0
  *(void **)((uint8_t *)list + 4) = sentinel;      // head
  *(uint32_t *)((uint8_t *)list + 8) = 0;          // size
  *(float *)((uint8_t *)self + 0x20) = 0.0f;
  *(float *)((uint8_t *)self + 0x24) = 0.0f;
  *(float *)((uint8_t *)self + 0x28) = 0.0f;
  *(float *)((uint8_t *)self + 0x2c) = 0.0f;
  return self;
}
```

## Layout (ctor-sealed)

| Offset | Role |
|---|---|
| +0x00 | vtbl* (`PTR_FUN_009d95b4`) |
| +0x04..+0x0C | **not** written by this unit |
| +0x10 | embedded list object base |
| +0x14 | list head* (sentinel) |
| +0x18 | list size (=0) |
| +0x1C | **not** written by this unit |
| +0x20..+0x2C | four zeroed floats (movss) |
| size | **0x30** (from `new` sites) |

## Open questions

- Product English class name (vtbl adjacency is shader `"vs.1.1…"` text).
- Meaning of host fields at +0x04..+0x0C / +0x1C.
- Product meaning of the four floats at +0x20 (populate path overwrites some).
- Runtime / bit-exact — open.
