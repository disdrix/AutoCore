# Annotated low-level: FUN_0073d8d0 → EffEffectFactory_OnDeviceLost_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0073d8d0` |
| VA | `0x0073d8d0`–`0x0073d937` exclusive (**103 B** / `0x67`) |
| System | graphics / Effects / effEffectFactory (via Device Reset caller) |
| Date | 2026-08-04 (W38-V) |
| Name | `EffEffectFactory_OnDeviceLost_Inferred` (**Inferred**; sealed by Device Reset shutdown placement + pair with Effect Factory reset) |

## Machine-level notes

- Source: raw capture + W38-V re-verify; **bytes win** on ABI (EAX host) and global tail.
- Sole caller `FUN_0075eff0` (`gfxDevice.cpp` Device Reset) loads `EAX = [EBP+0x764]` (same host as `0073d870`) during **"Beginning shutdown for Device Reset()"** phase, before `IDirect3DDevice9::Reset`.
- Pair: `FUN_0073d870` is the post-reset recreate walk.
- Per entry: `FUN_0074f0c0` with **ESI=`*entry`** (releases global/shared + entry effect via vtbl **`+0x114`** / globals **`+0x10c`** — unowned callee); param range → `FUN_00752640` (**EBX** formal).
- Tail: `DAT_00d1f048[+0xC]=0`; if `DAT_00d1f044[+0x77C]==1` (`[0x1df]` dword index) then device iface vtbl **`+0x134`(0)** — same slot caller uses for software-VP toggle after reset.
- Reject scaffold alias `Named_CalleeOf_Named_gfxDevice_0073d8d0`.

## Pseudocode (annotated)

```c
// EAX = EffEffectFactory* host (register formal)
void EffEffectFactory_OnDeviceLost_Inferred(void /* EAX host */)
{
  for (entry = *(host+4); entry != *(host+8); entry += 0x14) {
    // ESI = *entry
    FUN_0074f0c0();                       // OnLost-style release (unowned)
    for (p = entry[+8]; p != entry[+0xC]; p += 4) {
      // EBX = *p
      FUN_00752640();                     // release param GPU refs (unowned)
    }
  }
  *(uint8_t *)(DAT_00d1f048 + 0xC) = 0;
  if (DAT_00d1f044[0x1df] == 1) {         // device[+0x77C]
    // PUSH 0; PUSH *DAT_00d1f044; CALL [vtbl+0x134]
    (*DAT_00d1f044)->vtbl[+0x134](*DAT_00d1f044, 0);
  }
}
```

## Open questions

- Product English for factory / entry / `FUN_0074f0c0` plate (W38-X owns `0074f0c0`).
- Exact product name of device vtbl `+0x134` (software VP residual from caller CF only).
- Full dual of `FUN_00752640` param release (not owned).
