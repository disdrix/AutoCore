# Annotated low-level: FUN_004160f0 → CWidgetRotateVehicle_ScalarDeletingDtor

| Field | Value |
|---|---|
| Stable ID | `aa_004160f0` |
| VA | `0x004160f0`–`0x0041610f` exclusive (**31 B** / `0x1F`) |
| System | MSVC scalar-deleting destructor / `CWidgetRotateVehicle` |
| Date | 2026-08-05 (WQ9I-F dual) |

## Machine-level notes

- Source: raw capture + WQ9I-F re-verify (`read_memory` full body + RTTI COL + ctor install site).
- Entry: `56 8B F1` → ESI = this (ECX).
- Extra `56` push ESI before complete dtor: `FUN_00416110` takes **stack this** and `RET 4` (not thiscall).
- flags bit0 gate identical to sibling `004160d0`.
- Optional free + return this + `RET 4` identical family pattern.
- DATA site: dword @ `0x00a6ffa0` = `0x004160f0` = `PTR_LAB_00a6ff94[3]` (vtbl+0x0C).
- Host class: COL @ `0x00a6ff90` → `.?AVCWidgetRotateVehicle@@`; ctor `FUN_00833500` writes `*obj = PTR_LAB_00a6ff94`.
- Complete body only rewrites vtbl to base `PTR_LAB_00a9bbe8` (no owned members).

## Pseudocode (annotated)

```c
// ECX=this; stack flags; RET 4; returns this*
void* __thiscall CWidgetRotateVehicle_ScalarDeletingDtor(void* self, uint8_t flags)
{
  CWidgetRotateVehicle_CompleteDtor(self);  // FUN_00416110 (stack this)
  if ((flags & 1) != 0) {
    operator_delete(self);
  }
  return self;
}
```

## Open questions

- Why virtual dtor lives at vtbl slot +0x0C (base methods occupy [0..2]) — structural; not a CF gap.
- Complete dtor dual is WQ9I-G OWN (`0x00416110`).
- Runtime / bit-exact / differential — open (no Launcher).
