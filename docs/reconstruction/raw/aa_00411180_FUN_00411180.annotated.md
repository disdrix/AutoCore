# Annotated low-level: StdVector_PushBack_Dword_EaxVec_EsiVal_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00411180` |
| VA | `0x00411180` |
| Canonical | `StdVector_PushBack_Dword_EaxVec_EsiVal_Inferred` |
| System | container util (cross-cutting; journal consumer) |
| Date | 2026-08-04 (WQ7R-D dual) |

## Machine-level notes

- Source: raw capture + WQ7R-D `read_memory` / live decompile.
- **Bytes win** over decompiler `void FUN_00411180(void)` — registers are formals.
- Distinct from sealed `Vector_PushDword` @ `0x004149d0` (**thiscall ECX** + stack value + `RET 4`); this unit is **EAX=vec / ESI=value*** + bare `RET`.
- Grow callee `FUN_0040b6d0` owned by WQ7R-E (not this dual).

## Signature (sealed)

```c
// EAX = vec*, ESI = const uint32_t* value; bare RET
void StdVector_PushBack_Dword_EaxVec_EsiVal_Inferred(
    StdVectorDword* vec /*EAX*/,
    const uint32_t* value /*ESI*/);
```

## Layout

```
vec+0x04  T* begin
vec+0x08  T* end
vec+0x0c  T* cap_end
// element stride 4
```

## Pseudocode (annotated)

```c
void StdVector_PushBack_Dword_EaxVec_EsiVal_Inferred(
    StdVectorDword* vec /*EAX*/,
    const uint32_t* value /*ESI*/)
{
  uint32_t* begin = vec->begin; // +4
  uint32_t size = 0;
  if (begin != NULL)
    size = (uint32_t)(vec->end - begin); // >>2

  if (begin != NULL) {
    uint32_t cap = (uint32_t)(vec->cap_end - begin);
    if (size < cap) {
      uint32_t* end = vec->end;
      *end = *value;
      vec->end = end + 1;
      return;
    }
  }
  // grow/insert path — FUN_0040b6d0 (WQ7R-E)
  // EDI=vec, ESI=value, stack end, EBX=&out_it scratch
  FUN_0040b6d0(vec->end, value);
}
```

## Call sites of interest

| Site | Parent | Setup |
|---|---|---|
| `0x008ae429` | `Client_UpdateMissionJournal` | `LEA EAX,[EBX+0x738]`; `LEA ESI,[ESP+0x14]` |
| `0x008ae4ef` | same | `LEA EAX,[EBX+0x728]`; `LEA ESI,[ESP+0x14]` |
| `0x008ae5ef` | same | `LEA EAX,[EBX+0x738]`; `LEA ESI,[ESP+0x1c]` |
| `0x008a2ed2` | `FUN_008a2df0` | `LEA EAX,[ESP+0x40]`; `LEA ESI,[EDX+EBP*4]` |

Journal uses local stack dword + host vectors at `+0x728` / `+0x738` — mission-id collect for Active/New panels (site-level only; body is generic).

## Confidence

| Claim | Level |
|---|---|
| Dword vector push CF | High |
| EAX/ESI register ABI + bare RET | High |
| Stride 4 | High |
| Grow via `0x0040b6d0` | High |
| Product element English always mission id | Falsified (multi-caller generic) |
| Runtime / bit-exact | Open |
