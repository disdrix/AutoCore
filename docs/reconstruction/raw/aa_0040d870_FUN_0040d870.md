# Raw capture: FUN_0040d870

| Field | Value |
|---|---|
| **Stable ID** | `aa_0040d870` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0040d870` |
| **Canonical name** | `FUN_0040d870` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_0040d870(void *param_1,byte param_2)

{
  FUN_0040d890();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```

---

## W33-A re-verify append (2026-07-29)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual W33-A |
| **Tools** | `decompile_function` + `analyze_function_complete` + `read_memory` + xrefs. **No** `disassemble_bytes` |
| **Live decompile** | ≡ scaffold 2026-07-23 (complete + flags&1 delete + return this) |
| **Body** | `0x0040d870`–`0x0040d88e` exclusive (**30 B** / `0x1E`); pad `CC` |
| **ABI** | **ECX=`this`**; stack **`flags`**; **`RET 4`** (`C2 04 00`); returns **this** in EAX |
| **Callees** | `FUN_0040d890` (`Class_009c7948_CompleteDtor`), `operator_delete` |
| **Callers** | DATA only — `PTR_FUN_009c7948[0]` @ `0x009c7948` |
| **Name (Inferred)** | `Class_009c7948_ScalarDeletingDtor` |
| **Peer** | `Class_009c7938_ScalarDeletingDtor` @ `0x0040d590` (W31-E; same CF shape) |

### Full-body hex (`read_memory`, 30 B)

```
568bf1e818000000f644240801740956e89dbf070083c4048bc65ec20400
```

### Vtbl `0x009c7948` (`read_memory`, 12 B slots)

| Slot | Value | Role |
|---|---|---|
| [0] | `0x0040d870` | this scalar-deleting dtor |
| [1] | `0x0074d470` | open (not owned) |
| [2] | `0x0074d440` | open (not owned) |

### Byte-sealed facts

1. `ESI = this`; `CALL FUN_0040d890` (complete @ `0x0040d890`).
2. `TEST byte [esp+8], 1` / `JE` skip — flags bit0 only (not vector-deleting).
3. `operator_delete(this)` cdecl + `ADD ESP,4` when bit0 set (false noreturn).
4. `MOV EAX,ESI; POP ESI; RET 4` — always returns this; cleans flags.
5. Classification: worker (MSVC scalar-deleting / vtbl[0]).
