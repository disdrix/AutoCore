# Annotated low-level: FUN_004612d0 → StdVector_InsertOne_RebindIt_RcElem28

| Field | Value |
|---|---|
| Stable ID | `aa_004612d0` |
| VA | `0x004612d0`–`0x00461342` exclusive (**114 B**) |
| System | MSVC `vector` insert-one + rebind iterator, refcounted elem **0x1c** |
| Date | 2026-07-23 scaffold; W35-Q seal 2026-07-29 |
| Canonical name | `StdVector_InsertOne_RebindIt_RcElem28` (**Inferred**) |

## Machine-level notes

- **Bytes win** on ABI: **EDI=`vec*`**, stack **`(outIt*, where, value*)`**, **`RET 0xC`**, return **`outIt*`**.
- Decompiler collapses value* (ECX formal of InsertN) and omits stack depth clean.
- Index snapshot **before** insert-n so rebind survives realloc of begin.
- Sole live caller: `FUN_00460f90` @ `0x00461006` (push_back: spare-cap fill-n else this insert-one).
- Callee: sealed `StdVector_InsertN_RcElem28` (`0x004617a0`) with **count=1**.
- Reject scaffold long `Named_CalleeOf_*gfxDevice*`.

## Signature (sealed)

```c
// EDI=vec*, stack (outIt*, where, value*); RET 0xC
RcElem28** StdVector_InsertOne_RebindIt_RcElem28(
    StdVectorRc28* vec,        /* EDI */
    RcElem28** outIt,          /* stack0 — written + returned */
    RcElem28* where,           /* stack1 */
    const RcElem28* value);    /* stack2 → ECX into InsertN */
```

## Control flow (annotated)

1. `begin = vec->begin`.
2. If empty size → `index = 0`; else `index = (where - begin) / 0x1c`.
3. `StdVector_InsertN_RcElem28(value@ECX, vec, where, count=1)`.
4. `*outIt = vec->begin + index * 0x1c`.
5. `return outIt` (`RET 0xC`).

## Full hex

See raw W35-Q append (114 B).
