# Raw capture: FUN_004d0e90

| Field | Value |
|---|---|
| **Stable ID** | `aa_004d0e90` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004d0e90` |
| **Canonical name** | `FUN_004d0e90` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_004d0e90(int param_1)

{
  char cVar1;
  
  if ((*(byte *)(param_1 + 0x17c) & 1) == 0) {
    cVar1 = FUN_004e15a0(param_1);
    if (cVar1 == '\0') {
      FUN_004024d0(param_1);
      *(uint *)(param_1 + 0x17c) = *(uint *)(param_1 + 0x17c) | 1;
    }
  }
  return;
}
```

---

## Live re-decompile (2026-07-29) — dual W17-J

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` (no `disassemble_bytes`) |
| **Integrity** | Append-only; decompiler body ≡ 2026-07-23 raw **surface**, but bytes seal **missed ECX this + ret 4** |
| **Canonical (dual)** | `Object_EnqueueDeferredOnce` |

### Decompiler output (≡ raw body)

```c
void FUN_004d0e90(int param_1)

{
  char cVar1;
  
  if ((*(byte *)(param_1 + 0x17c) & 1) == 0) {
    cVar1 = FUN_004e15a0(param_1);
    if (cVar1 == '\0') {
      FUN_004024d0(param_1);
      *(uint *)(param_1 + 0x17c) = *(uint *)(param_1 + 0x17c) | 1;
    }
  }
  return;
}
```

### Bytes (`read_memory` 64 @ `0x004d0e90`)

```
56 8b 74 24 08 f6 86 7c 01 00 00 01 57 8b f9 75 23
8b 8f fc e5 00 00 56 e8 f3 06 01 00 84 c0 75 13
8b 8f fc e5 00 00 56 e8 13 16 f3 ff 83 8e 7c 01 00 00 01
5f 5e c2 04 00 cc cc cc cc cc cc cc cc
```

Byte-level reconstruction (sealed ABI; decompiler drops ECX host):

```text
push esi
mov  esi, [esp+8]              ; stack arg = Object*
test byte ptr [esi+0x17c], 1   ; ObjectFlag_DeferredQueued bit0
push edi
mov  edi, ecx                  ; this = reaction/world host
jnz  already                     ; bit already set → skip
mov  ecx, [edi+0xe5fc]         ; queue object*
push esi
call FUN_004e15a0              ; list-contains(object) → AL
test al, al
jnz  already                     ; already in list → skip
mov  ecx, [edi+0xe5fc]
push esi
call FUN_004024d0              ; list-append(object)
or   dword ptr [esi+0x17c], 1  ; set sticky queued bit
already:
pop  edi
pop  esi
ret  4
```

- Boundary: `0x004d0e90` … `0x004d0ec8` (`c2 04 00` + `CC`)
- Call @ `004d0ea8` → `FUN_004e15a0` (next `004d0ead` + `0x000106f3`)
- Call @ `004d0eb8` → `FUN_004024d0` (next `004d0ebd` + signed `0xfff31613`)
- Field: host `+0xe5fc` queue; object flags `+0x17c` bit **0** mask `0x1`
