# Annotated low-level: FUN_006507c0 → CVOGHBMissionEscort_ctor

| Field | Value |
|---|---|
| Stable ID | `aa_006507c0` |
| VA | `0x006507c0`–`0x006508c8` |
| Canonical | `CVOGHBMissionEscort_ctor` |
| System | missions-progression / heartbeat |
| Date | 2026-08-04 (WQ9G-E dual) |

---

## Purpose

Construct a **`CVOGHBMissionEscort`** heartbeat action: base ctor → install Escort vtable → seed fields from owner context + objective/requirement host → attach owner → start period 1000 ms.

---

## Annotated CF

```c
// __thiscall ECX=this; stack: ownerCtx, reqHost; ret 8; returns this
CVOGHBMissionEscort* CVOGHBMissionEscort_ctor(
    CVOGHBMissionEscort* this,
    void* ownerCtx,     // param_2 — character / reaction context chain
    void* reqHost)      // param_3 — objective/requirement host (size ≥0x4c)
{
  // SEH frame LAB_009a8ea8
  CVOGHBBase_ctor(this);
  this->pVTable = &PTR_FUN_009e52c4;          // Escort vtable (RTTI COL@-4)

  // +0x24 ← nested world/slot ptr via ownerCtx (+4/+4/+0xa8+ownerCtx)
  this->field_24 = NestedWorldSlot(ownerCtx);

  // +0x38..+0x44 default TFID pad DAT_009e52b0..bc = {−1,−1,0,0}
  CopyTfidPad(this + 0x38, DAT_009e52b0);

  this->pReqHost = reqHost;                   // +0x6C
  FUN_0060c3c0(reqHost);                      // resolve req+0x44 match if −1

  this->targetMatchRef    = *(u32*)(reqHost + 0x44);   // +0x2C
  this->matchByObjRefMode = *(u8*)(reqHost + 0x48);    // +0x30
  this->missionKey        = *(*(u32**)(reqHost + 4) + 4); // +0x28 from *(req+4)+0x10
  this->maxEscortRange    = *(float*)(reqHost + 0x1c); // +0x48
  // complete-zone dual keys + radius from req +0x28..+0x3c → this +0x50..+0x68
  this->completeZoneA_lo  = *(u32*)(reqHost + 0x28);   // +0x50
  this->completeZoneA_hi  = *(u32*)(reqHost + 0x2c);   // +0x54
  this->completeZoneR     = *(u32*)(reqHost + 0x38);   // +0x58
  this->completeZoneB_lo  = *(u32*)(reqHost + 0x30);   // +0x60
  this->completeZoneB_hi  = *(u32*)(reqHost + 0x34);   // +0x64
  this->completeZonePad   = *(u32*)(reqHost + 0x3c);   // +0x68

  this->field_1c = 0x0b;                      // +0x1C subtype/tag
  CVOGHBBase_SetPeriodAndCounter(this, -1000, true);
  CVOGHBBase_AttachOwnerObject(this, NestedOwnerObject(ownerCtx));
  this->nPeriodMs = 1000;                     // +0x08 overwrite after SetPeriod
  this->dwLastTick = g_dwClientTickMs;        // +0x70
  return this;
}
```

Field English for complete-zone / max-range aligns with dualed `CVOGHBMissionEscort_OnHeartBeat` (`aa_006508d0`); product labels residual.

---

## Machine notes

| Claim | Evidence |
|---|---|
| Body 264 B / `ret 8` | `get_function_by_address` + exit hex `C2 08 00` |
| Vtable `PTR_FUN_009e52c4` | `c7 06 c4 52 9e 00` |
| RTTI `.?AVCVOGHBMissionEscort@@` | TD `0x00af4a10` string |
| OnHB slot `+0x0C` = `0x006508d0` | vtable dword read |
| Default TFID pad −1/−1/0/0 | `read_memory` `0x009e52b0` |
| Period 1000 ms | `c7 46 08 e8 03 00 00` |
| Tag `0x0b` @ +0x1C | `c7 46 1c 0b 00 00 00` |
| Primary factory | `FUN_0060c860` after progress gate; `operator_new(0x78)` |

---

## Open / residual

- Product English for reqHost field dictionary beyond dualed OnHB usage.
- Second call site `0x00613e62` lives in a Ghidra function gap (bytes show `E8` → this).
- Exact C++ type of `ownerCtx` / `reqHost` beyond offset usage.
- Runtime / bit-exact / differential.
