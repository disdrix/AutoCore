# Annotated: StdVector_InsertN_Dword_Via4ea70_Inferred (FUN_00463e30)

| Field | Value |
|---|---|
| Stable ID | `aa_00463e30` |
| VA | `0x00463e30` |
| Sealed name | `StdVector_InsertN_Dword_Via4ea70_Inferred` |
| System | util / container (generic dword vector) |
| Date | 2026-08-04 (W38-M) |

## Machine-level notes

- **ABI:** `__thiscall`; ECX = vector; stack `(where*, count, value*)`; **`ret 0x0C`**.
- **Triad:** begin `+4`, end `+8`, capEnd `+0xC`; element stride **4** (`>>2` / `*4`).
- **Growth:** 1.5× (`cap + (cap>>1)`), max elements **`0x3fffffff`**, overflow → `FUN_004540b0`.
- **Helpers:** `0044ea70` forward copy, `00436ef0` fill-n, `0044ea50` backward copy.
- Prefer bytes after `operator_delete` (decompiler noreturn false).
- Generic multi-domain worker (UI / gfx / mission / net sites).

## Role-named sketch

```c
void __thiscall StdVector_InsertN_Dword_Via4ea70(
    VectorDword* this,
    uint32_t* where,
    uint32_t count,
    uint32_t* value)
{
  uint32_t fill = *value;
  uint32_t cap = begin ? (capEnd - begin) : 0;   // >>2
  if (count == 0) return;
  uint32_t size = begin ? (end - begin) : 0;
  if (0x3fffffffU - size < count) FUN_004540b0(); // noreturn
  if (cap < size + count) {
    // 1.5x grow, new, copy prefix, fill, copy suffix, delete old, rewrite triad
  } else if ((end - where) < count) {
    // in-place end-biased shift + fill; end += count
  } else {
    // in-place mid shift (fwd/back helpers) + fill
  }
}
```

## Open questions

- Exact MSVC demangle / PDB name.
- Whether any caller relies on non-POD element ctor (helpers are POD dword stores).
- Full caller taxonomy; runtime / bit-exact / differential.
