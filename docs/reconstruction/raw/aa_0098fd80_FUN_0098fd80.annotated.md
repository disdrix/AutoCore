# Annotated low-level: FUN_0098fd80

| Field | Value |
|---|---|
| Stable ID | `aa_0098fd80` |
| VA | `0x0098fd80`–`0x0098fee9` |
| System | palantir graphics / PalTool material (no-map) |
| Date | 2026-08-04 (W37-A dual seal) |
| Inferred name | `PalToolNoMap_Host0x30_MaterialSlot0x28_Init_Inferred` |

## Machine-level notes

- Live decompile **≡** raw capture (2026-07-23); no CF delta on W37-A re-verify.
- **Sole caller** `FUN_0073c810` @ `0x0073cac3`: allocates **0x30** host (`FUN_00457ac0`), zeros material holder at `+0x28` and flag bytes `+0x2c..+0x2e`, stores as HostPtrTable12 **slot9**, then `FUN_0098fd80(host)`.
- **Stack formal** `param_1` = host* (**Confirmed** by caller push). Not ECX-thiscall.
- Material holder field: **`host + 0x28`**.
- Effect plate: **`PalToolMaterialNoMap.fx`** via `FUN_00989e00` → `FUN_009701d0`.
- Scalar effect params via `FUN_00442640`:
  - `"DepthBias"` ← `g_flMsToSeconds_Inferred` (**0.001f** @ `0x00a0f72c`) — **shared float pool name**, not product "ms" semantics here.
  - `"GhostAlpha"` ← `g_flMultiKillCountBlend` (**0.1f** @ `0x00a0f730`) — same pool reuse.
- Material params via null-holder → `-1` sentinel else `FUN_00752370` + `FUN_0096fff0` when handle ≥ 0:
  - `"MatDiffuse"`, `"MatSpecular"`, `"MatEmissive"`.
- Emissive float4 packed on stack before bind: **(1.0, 0.4, 0.4, 1.0)** = `g_flOne`, `DAT_00a0f720`, `DAT_00a0f720`, `g_flOne`.
- Terminal **refcount swap**: `FUN_0073d940(*slot)` → inc new `[1]`, optional vtbl+4 first-acquire; dec old `[1]`, optional vtbl+8 release; store new into `*slot`.
- **Always returns 0** (statuses of intermediate binds not OR-accumulated — unlike `FUN_00731130`).

## Pseudocode (annotated)

```c
// stdcall host*; RET width open (decompiler returns 0)
uint32_t FUN_0098fd80(void *host /* size 0x30 from caller */)
{
  int *mat_slot;           // host + 0x28
  int *param_handle;
  int *new_holder;
  int *old_holder;
  int *refp;
  /* local name-shell / param temps */
  undefined1 name_shell[4]; // local_20 — also reused as MatDiffuse out
  float depth_bias;         // local_1c
  float ghost_alpha;        // local_18
  undefined1 mat_spec_out[4]; // local_14
  float emissive[4];        // local_10..local_4 as RGBA

  FUN_00989e00(name_shell, "PalToolMaterialNoMap.fx");
  mat_slot = (int *)((char *)host + 0x28);
  FUN_009701d0(name_shell);   // thiscall recovery of mat vs shell: residual

  depth_bias = g_flMsToSeconds_Inferred;   // 0.001f
  FUN_00442640("DepthBias", &depth_bias);
  ghost_alpha = g_flMultiKillCountBlend;   // 0.1f
  FUN_00442640("GhostAlpha", &ghost_alpha);

  // MatDiffuse
  if (*mat_slot == 0) {
    /* reuse stack formal as -1 sentinel storage (decompiler) */
    host = (void *)(intptr_t)-1;
    param_handle = (int *)&host;
  } else {
    param_handle = (int *)FUN_00752370(name_shell, "MatDiffuse");
  }
  if (*param_handle >= 0) {
    FUN_0096fff0(mat_slot, *param_handle);
  }

  // MatSpecular
  if (*mat_slot == 0) {
    host = (void *)(intptr_t)-1;
    param_handle = (int *)&host;
  } else {
    param_handle = (int *)FUN_00752370(mat_spec_out, "MatSpecular");
  }
  if (*param_handle >= 0) {
    FUN_0096fff0(mat_slot, *param_handle);
  }

  // MatEmissive float4 = (1, 0.4, 0.4, 1) then bind
  emissive[0] = g_flOne;          // 1.0f
  emissive[1] = DAT_00a0f720;     // 0.4f
  emissive[2] = DAT_00a0f720;     // 0.4f
  emissive[3] = g_flOne;          // 1.0f
  if (*mat_slot == 0) {
    host = (void *)(intptr_t)-1;
    param_handle = (int *)&host;
  } else {
    param_handle = (int *)FUN_00752370(&host, "MatEmissive");
  }
  if (*param_handle >= 0) {
    FUN_0096fff0(mat_slot, *param_handle);
  }

  // Refcount-swap material holder
  new_holder = (int *)FUN_0073d940(*mat_slot);
  if (new_holder != 0) {
    new_holder[1] = new_holder[1] + 1;
    if (new_holder[1] == 1) {
      (**(code **)(*new_holder + 4))();   // vtbl+4 first acquire
    }
    old_holder = (int *)*mat_slot;
    if (old_holder != 0) {
      refp = old_holder + 1;
      *refp = *refp - 1;
      if (*refp == 0) {
        (**(code **)(*old_holder + 8))(); // vtbl+8 release
      }
    }
    *mat_slot = (int)new_holder;
  }
  return 0;
}
```

## Confidence

| Claim | Level |
|---|---|
| Sole caller + host size 0x30 + slot `+0x28` | **Confirmed** (caller decompile + xrefs) |
| Plate effect + param strings | **Confirmed** |
| DepthBias=0.001 / GhostAlpha=0.1 / emissive (1,0.4,0.4,1) | **Confirmed** (read_memory + loads) |
| Refcount vtbl+4 / +8 pattern | **High** (shared with sibling PalTool inits) |
| Always return 0 | **High** |
| Product host class English | **Unknown** (keep `_Inferred`) |
| Exact thiscall ECX for `FUN_009701d0` / `FUN_00442640` | **Probable** residual (decompiler drops this) |
| Shared-float symbol semantics (ms / multi-kill) for shader params | **Not product** — numeric values only |

## Open questions

- Product/PDB name for 0x30 host type beyond effect plate.
- Full contracts of unowned callees (`FUN_009701d0`, `FUN_00442640`, `FUN_00752370`, `FUN_0096fff0`, `FUN_0073d940`).
- Runtime / bit-exact / differential (no Launcher).
