# Annotated low-level: FUN_00456780 / StdVector_DwordResize_EcxCount_EdxVec_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00456780` |
| VA | `0x00456780`–`0x004567f5` exclusive (**117 B** / `0x75`) |
| System | STL dword vector resize (ECX-count / EDX-vec ABI) |
| Date | 2026-08-04 (W38-H dual) |

## Machine-level notes

- **Bytes win** over decompiler on grow path and fill formal.
- ABI: **EDX=`vec*`**, **ECX=`newCount`**, stack **`fill`**, **`RET 4`**.
- begin `@+4`, end `@+8`, stride **4** (`SAR 2` / `LEA [reg+reg*4]`).
- Grow → `FUN_00456960` InsertN (count = n−size, fill via `LEA EAX,[ESP+…]`).
- Shrink → `FUN_00456660` erase `[begin+n, end)` (memmove-publish).
- Do **not** merge with `FUN_0044a380` (ECX=vec/EAX=n) or `FUN_004367f0` (stack n, `RET 8`).

## Signature (byte-sealed)

```c
// EDX=vec*; ECX=newCount; stack fill; RET 4
void StdVector_DwordResize_EcxCount_EdxVec_Inferred(
    void* vec /*EDX*/,
    uint32_t newCount /*ECX*/,
    uint32_t fill /*stack*/);
```

## Pseudocode (annotated, bytes-corrected)

```c
void StdVector_DwordResize_EcxCount_EdxVec_Inferred(uint32_t n /*ECX*/, VecDword* vec /*EDX*/, uint32_t fill /*stack*/)
{
  uint32_t* begin = vec->begin; // +4
  uint32_t size = begin ? (vec->end - begin) : 0; // end at +8, >>2

  if (size < n) {
    // InsertN: EAX=&fill, ECX=n-size, stack end, EDX=vec
    FUN_00456960(/*insert count in ECX*/, vec, vec->end, &fill);
    return; // RET 4
  }
  if (begin && n < size) {
    // erase [begin+n, end); EBX scratch
    FUN_00456660(vec, begin + n, vec->end);
  }
  // RET 4
}
```

## Callers (sample)

Pool freelist shrink (`PoolHost_Acquire0x40` / `0x00986070`), physics/asset vector trims (`FUN_00765740`, `FUN_00765b74`), many other dword-vector hosts (35 Ghidra callers; 58 xrefs).

## Confidence

| Claim | Level |
|---|---|
| Resize role + dword stride | **Confirmed** (bytes) |
| EDX vec / ECX n / fill / RET 4 | **Confirmed** (bytes + call sites) |
| Grow/shrink callees | **Confirmed** |
| Product/PDB English | Open |
| Runtime / bit-exact | Open |
