# Raw capture: FUN_00437000

| Field | Value |
|---|---|
| **Stable ID** | `aa_00437000` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00437000` |
| **Canonical name** | `FUN_00437000` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __fastcall FUN_00437000(undefined4 param_1)

{
  undefined1 uVar1;
  int in_EAX;
  int iVar2;
  undefined1 *unaff_EDI;
  undefined1 local_1;
  
  if (*(int *)(in_EAX + 0x20) < *(int *)(in_EAX + 0x18) + 1) {
    iVar2 = FUN_00435df0(*(int *)(in_EAX + 0x1c) + *(int *)(in_EAX + 0x18));
    if ((iVar2 < 0) || (*(int *)(in_EAX + 0x20) < 1)) {
      local_1 = (undefined1)((uint)param_1 >> 0x18);
      *unaff_EDI = local_1;
      return 0xffffffff;
    }
  }
  uVar1 = *(undefined1 *)(*(int *)(in_EAX + 0x18) + in_EAX + 0x2c);
  *(int *)(in_EAX + 0x18) = *(int *)(in_EAX + 0x18) + 1;
  *unaff_EDI = uVar1;
  return 0;
}
```

---

## Append — dual A/B re-verify 2026-08-04 (W38-D OWN)

| Field | Value |
|---|---|
| **Tool** | Ghidra `batch_decompile` / `decompile_function` + `force_decompile` + `read_memory` + `analyze_function_complete` + `get_function_by_address` + assembly context on call sites. **No** `disassemble_bytes` |
| **Program** | `autoassault.exe` |
| **Body range** | `0x00437000`–`0x00437048` (**73 B** / `0x49`; pad `CC` @ `0x00437049`; next `stoChunkReader_ReadU32` @ `0x00437050`) |
| **Live / force decompile** | **≡** raw body above (decompiler omits ESI remap; **bytes win** on ABI) |
| **Sole callee** | `FUN_00435df0` @ `0x00435df0` (stream-window ensure; not owned) |
| **Callers (sample)** | mode wrapper `FUN_00439a50`; binary paths `FUN_00765740`, `FUN_00963fb0`, `FUN_0072c860`, … — 10 xrefs |
| **Classification** | worker |
| **Sibling** | `stoChunkReader_ReadU32` `0x00437050` (same EAX-reader + window@+0x14 shape; stack out + RET 4) |

### Body bytes (`read_memory` @ `0x00437000`, length 80; body ends `C3`)

```
51                push ecx                      ; local / align (entry ECX spill)
56                push esi
8B F0             mov  esi, eax                 ; ESI = stoChunkReader* (EAX on entry)
8B 46 18          mov  eax, [esi+0x18]          ; cursor = reader+0x18 (= window+0x04)
83 C6 14          add  esi, 0x14                ; ESI = stream window (reader+0x14)
8D 48 01          lea  ecx, [eax+1]             ; need cursor+1
3B 4E 0C          cmp  ecx, [esi+0x0c]          ; vs available
7E 23             jle  have_byte
8B 56 08          mov  edx, [esi+0x08]          ; base
03 D0             add  edx, eax                 ; base+cursor
52                push edx
8B CE             mov  ecx, esi                 ; thiscall window
E8 D1 ED FF FF    call FUN_00435df0             ; → 0x00435df0
85 C0             test eax, eax
7C 06             jl   fail
83 7E 0C 01       cmp  dword [esi+0x0c], 1
7D 0C             jge  have_byte
; fail: write spill high-byte of entry-ECX push-slot to *EDI, return -1
8A 4C 24 07       mov  cl, [esp+7]              ; high byte of pushed entry ECX
83 C8 FF          or   eax, -1
88 0F             mov  [edi], cl
5E                pop  esi
59                pop  ecx
C3                ret
; have_byte:
8B 46 04          mov  eax, [esi+0x04]          ; cursor
8A 4C 30 18       mov  cl, [eax+esi+0x18]       ; *(u8*)(window+0x18+cursor)
83 C0 01          add  eax, 1
89 46 04          mov  [esi+0x04], eax          ; cursor++
33 C0             xor  eax, eax                 ; return 0
88 0F             mov  [edi], cl                ; *out = byte
5E                pop  esi
59                pop  ecx
C3                ret
```

Full hex (73 B):
`51568bf08b461883c6148d48013b4e0c7e238b560803d0528bcee8d1edffff85c07c06837e0c017d0c8a4c240783c8ff880f5e59c38b46048a4c301883c00189460433c0880f5e59c3`

### ABI seal

| Fact | Value |
|---|---|
| Entry | **EAX** = `stoChunkReader*` (full reader; remaps `+0x14` to window) |
| Entry | **EDI** = `uint8_t* out` |
| Stack | none (bare **`RET`**) |
| Return | **0** success; **`0xffffffff`** fail |
| Window | `reader+0x14` — same map as ReadU32 / ReadHeader16 |
| Fail *out | stores `[esp+7]` (high byte of entry-ECX push slot) then −1; **do not rely** on *out on failure |

### Call-site seal — mode wrapper `FUN_00439a50` @ `0x00439a50`

```
cmp  dword [eax+0x4044], 0   ; binary mode?  EAX=reader
jnz  text
mov  edi, ecx                ; out byte* (from wrapper ECX)
call FUN_00437000            ; EAX still reader
...
text:
push edi
mov  edi, eax                ; reader into EDI for text peer
call FUN_00769660            ; text bool-token peer (not owned)
```

### Call-site seal — direct binary (`0x00765caa`)

```
cmp  dword [ebp+0x4044], 0
jnz  text
lea  edi, [esp+0x74]         ; local byte
mov  eax, ebp                ; reader
call FUN_00437000
```
