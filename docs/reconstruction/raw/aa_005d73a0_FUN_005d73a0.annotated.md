# Annotated low-level: CVOGHBAIDriver_AxisPark_Inferred (FUN_005d73a0)

| Field | Value |
|---|---|
| Stable ID | `aa_005d73a0` |
| VA | `0x005d73a0`–`0x005d73e7` inclusive (**72 B** / `0x48`) |
| Ghidra name | `FUN_005d73a0` |
| Canonical name | `CVOGHBAIDriver_AxisPark_Inferred` |
| System | input-drive-control / HB AI driver |
| Date | 2026-08-05 (MEGA-015 dual refresh) |
| Prior scaffold | `FUN_005d73a0` only (no Named_CalleeOf plate) |

## Machine-level notes

- **`__thiscall`**: **ECX** = `CVOGHBAIDriver* this`; **no stack args**.
- Body ends in **tail `JMP FUN_005cc560`** after `POP ESI` — no local `RET`; void return via cleanup callee.
- Vehicle entity pointer at **`this+0xbc`** is loaded three times for axis writers + once for Push.
- Axis park sequence (sealed):
  1. `SetSteerInput(vehicle, 0)` → `vehicle+0x618`
  2. `SetLongitudinalInput(vehicle, 0)` → `vehicle+0x614`
  3. `SetHandbrake(vehicle, 1)` → `vehicle+0x61c`
  4. `FireWeapons(this, 0, 0)` — **may-fire=0** clears weapon mask (`RET 8` callee)
  5. `PushDriveAxesToController(vehicle)` — commit axes into VehicleAction controller
  6. `FUN_005cc560(this)` — owner/target counter cleanup residual
- **Class:** RTTI `.?AVCVOGHBAIDriver@@`; vtable `009db02c` slot **`+0x10`**.
- **Dispatch:** DATA-only xref (virtual). No direct CALL sites.
- Prefer assembly when decompiler elides the second FireWeapons stack arg or shows void `__fastcall` — encoding is ECX-this + two `PUSH 0` before FireWeapons.

## Pseudocode (annotated ≡ raw ≡ live)

```c
// ECX = CVOGHBAIDriver* this; no stack args; void; tail → FUN_005cc560
void __thiscall CVOGHBAIDriver_AxisPark_Inferred(void *self /* ECX */)
{
  void *vehicle = *(void **)((char *)self + 0xbc);

  VehicleEntity_SetSteerInput(vehicle, 0);           // +0x618 = 0
  VehicleEntity_SetLongitudinalInput(vehicle, 0);    // +0x614 = 0
  VehicleEntity_SetHandbrake(vehicle, 1);            // +0x61c = 1

  // FireWeapons is __thiscall RET 8: two stack dwords; only first is may-fire gate
  CVOGHBAIFollowVehicle_FireWeapons(self, /*mayFire*/ 0, /*unused*/ 0);

  VehicleEntity_PushDriveAxesToController(vehicle);

  FUN_005cc560(self);  // tail-call in assembly
}
```

## Call graph

| Role | Target |
|---|---|
| Host class | `CVOGHBAIDriver` (RTTI + ctor `FUN_005d7bb0` installs vtbl `009db02c`) |
| Vtable slot | `*[009db02c + 0x10]` |
| Direct callers | **none** |
| Callees | SetSteer `004f5620`; SetLong `004f5650`; SetHB `004f3620`; FireWeapons `005d7100`; Push `004fbc10`; `FUN_005cc560` |
| Related dualed peers | SetSteer / SetLong / SetHB / PushDriveAxes (axis pipeline); FireWeapons dualed accept-with-gaps |

## Open questions

1. Product English method name (no scoped profiler string in body) — **Inferred**.
2. Which AI / HB paths virtual-call `vtbl+0x10` (stop, idle, leash, disable?).
3. Full semantics of residual `FUN_005cc560` counter-dec.
4. Runtime / bit-exact / differential — open (Terminal false).
