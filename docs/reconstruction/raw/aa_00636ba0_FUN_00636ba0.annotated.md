# Annotated low-level: CVOGHBMoveVehicle_OnEnd (FUN_00636ba0)

| Field | Value |
|---|---|
| Stable ID | `aa_00636ba0` |
| VA | `0x00636ba0` |
| Canonical name | `CVOGHBMoveVehicle_OnEnd` |
| Ghidra name | `FUN_00636ba0` |
| System | `input-drive-control` / heartbeat OnEnd |
| Date | 2026-08-05 (MEGA-017 refresh) |
| Body | `0x00636ba0`–`0x00636bbf` inclusive (**32 B**) |

## Machine-level notes

- Source: raw capture + MEGA-017 live re-verify (`disassemble_function` + `read_memory`). Prefer **assembly** when decompiler omits the empty-ret tail.
- **this** = `CVOGHBMoveVehicle*` in **ECX**; vehicle entity at **`this+0x24`** (ctor `FUN_00636bd0` stores ctor arg there).
- **OnEnd** role: installed at **vtbl+0x10** on `PTR_FUN_009e3b70` / subclass `PTR_FUN_009d5590`; reached only via `CVOGHBBase_Stop` virtual tail (`jmp [eax+0x10]`), not direct CALL.
- Axis park is **steer-only**: `SetSteerInput(0)` then `PushDriveAxesToController`. Does **not** touch longitudinal (`+0x614`) or handbrake (`+0x61c`).
- Push dual sealed: copies thr/HB to controller; **does not** copy steer (`+0x618` is applyAction ramp target).
- Tail `JMP 0x005081e0` = bare `C3` (distinct from `CVOGHBBase_EmptyVFunc` `0x005081f0`).

## Pseudocode (assembly-complete; decompiler + sealed tail)

```c
// CVOGHBMoveVehicle::OnEnd  — vtbl+0x10
// thiscall: ECX = this; no stack args; ends via JMP empty-ret stub

void __thiscall CVOGHBMoveVehicle_OnEnd(CVOGHBMoveVehicle *this)
{
  VehicleEntity *entity = *(VehicleEntity **)((char *)this + 0x24);

  if (entity != NULL) {
    // gated write of entity+0x618 = 0.0f when suppress mask allows
    VehicleEntity_SetSteerInput(entity, 0.0f);
    // thr/HB bridge to controller at *(entity+0x1a0)+8; steer not pushed
    VehicleEntity_PushDriveAxesToController(entity);
  }

  // machine: MOV ECX,this; POP ESI; JMP 0x005081e0 (C3)
  return;
}
```

## Instruction map (`read_memory` @ `0x00636ba0`, 32 B)

```text
56                      push  esi
8B F1                   mov   esi, ecx              ; this
8B 4E 24                mov   ecx, [esi+0x24]       ; entity
85 C9                   test  ecx, ecx
74 0F                   jz    skip                  ; → 00636bb9
6A 00                   push  0                     ; steer float bits
E8 6F EA EB FF          call  VehicleEntity_SetSteerInput   ; 0x004f5620
8B 4E 24                mov   ecx, [esi+0x24]
E8 57 50 EC FF          call  VehicleEntity_PushDriveAxesToController ; 0x004fbc10
skip:
8B CE                   mov   ecx, esi
5E                      pop   esi
E9 1F 16 ED FF          jmp   0x005081e0            ; empty RET
```

Hex: `568bf18b4e2485c9740f6a00e86feaebff8b4e24e85750ecff8bce5ee91f16edff`

## Related (context only; not OWN)

| VA | Role |
|---|---|
| `0x00636bd0` | `CVOGHBMoveVehicle` ctor — binds entity → `+0x24` |
| `0x00636cc0` | fire/heartbeat — MoveToTarget; Stop on fail |
| `0x00636b90` / `0x00636d10` | dtor / scalar dtor |
| `0x005081d0` | `CVOGHBBase_Stop` → virtual OnEnd |
| `0x004f5620` / `0x004fbc10` | dualed steer setter + push |
| `0x005d73a0` | different AI full axis-park (thr0/steer0/HB1) |

## Open questions

- Runtime / live CE confirmation under Stop after MoveToTarget complete.
- Product PDB symbol for method beyond RTTI class + OnEnd slot convention.
- Exhaustive inventory of who constructs `CVOGHBMoveVehicle` / `ToMouse` (ctor residual).
