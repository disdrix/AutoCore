# Annotated low-level: BasicString_FillAssignRange_0x1c

| Field | Value |
|---|---|
| Stable ID | `aa_00431450` |
| VA | `0x00431450`–`0x00431477` exclusive (**39 B**) |
| System | MSVC `basic_string<char>` range fill (assign) |
| Date | 2026-07-29 (W33-I re-seal) |
| Ghidra | `FUN_00431450` |

## Machine-level notes

- **cdecl** free helper: stack `first*`, `last*`, `value*`; plain `RET`.
- Loop: `ESI=first`, `EDI=last`, `EBX=value`; while ESI≠EDI: `ECX=ESI`, push value, IAT `operator=` `[0x009c63c4]`, `ADD ESI,0x1C`.
- **Not** uninitialized fill — destinations are live strings (assign, not ctor).
- **Not** count-N API — end-iterator range.
- Sole parent: `StdVector_InsertN_BasicString` in-place arms (hole + mid).

## Pseudocode (annotated)

```c
// first, last, value — pointers to basic_string slots (stride 0x1c)
void BasicString_FillAssignRange_0x1c(
    BasicString_0x1c* first,
    BasicString_0x1c* last,
    const BasicString_0x1c* value)
{
  for (; first != last; first = (BasicString_0x1c*)((char*)first + 0x1c)) {
    basic_string::operator=(first, value);  // IAT
  }
}
```

## Open questions

1. Product / MSVC demangle plate (likely internal `std::fill` / vector assign helper).
2. Runtime / bit-exact / differential — open.
