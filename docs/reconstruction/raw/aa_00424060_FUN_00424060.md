# Raw capture: FUN_00424060

| Field | Value |
|---|---|
| **Stable ID** | `aa_00424060` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00424060` |
| **Canonical name** | `FUN_00424060` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
/* WARNING: Function: __chkstk replaced with injection: alloca_probe */

void __fastcall FUN_00424060(int param_1)

{
  undefined4 *puVar1;
  undefined4 local_18;
  undefined1 *local_14;
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  puStack_c = &LAB_009bd790;
  local_10 = ExceptionList;
  local_18 = 0x424083;
  local_14 = &stack0xffffefdc;
  local_8 = 0;
  ExceptionList = &local_10;
  EnterCriticalSection((LPCRITICAL_SECTION)(param_1 + 4));
  puVar1 = *(undefined4 **)(param_1 + 0x1c);
  if (*(char *)(param_1 + 0x28) != '\0') {
    LeaveCriticalSection((LPCRITICAL_SECTION)(param_1 + 4));
    local_18 = 0x80070005;
                    /* WARNING: Subroutine does not return */
    _CxxThrowException(&local_18,(ThrowInfo *)&DAT_00acc430);
  }
  while (puVar1 != (undefined4 *)0x0) {
    *(undefined4 *)(param_1 + 0x1c) = puVar1[2];
    (**(code **)*puVar1)(1);
    puVar1 = *(undefined4 **)(param_1 + 0x1c);
  }
  *(undefined4 *)(param_1 + 0x24) = 0;
  *(undefined4 *)(param_1 + 0x20) = 0;
  *(undefined4 *)(param_1 + 0x1c) = 0;
  LeaveCriticalSection((LPCRITICAL_SECTION)(param_1 + 4));
  ExceptionList = local_10;
  return;
}
```

---

## WQ9L-E re-verify append (2026-08-05)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **WQ9L-E** |
| **Tools** | `batch_decompile` + `analyze_function_complete` + `disassemble_function` + `read_memory` + callers/xrefs + RTTI COL on UI nested vtbls. **No** `disassemble_bytes`. |
| **Live decompile** | ≡ original raw CF |
| **Body** | `0x00424060`–`0x0042415a` inclusive (**251 B** / `0xFB`); next `FUN_00424160` |
| **ABI** | **ECX** = list (`8B F1`); void; bare **`RET`** (`C3`) |
| **Named (inferred)** | `ObjectCsList_DestroyAll_Inferred` |
| **Product string (SEH catch)** | `"List Error!  DestroyAll Call Stack:\n%s\n"` @ `0x00a33af0` + `"VOG_DEBUG_STOP"` |
| **Terminal** | **false** |

### Main-path assembly (disassemble_function)

```
00424060  PUSH EBP / MOV EBP,ESP
00424063  PUSH -1 / PUSH LAB_009bd790 / FS:[0] SEH frame
00424079  MOV EAX,0x1004 / CALL __chkstk
00424086  MOV ESI,ECX
0042408b  LEA EDI,[ESI+0x4]          ; CRITICAL_SECTION*
0042408e  XOR EBX,EBX
00424090  PUSH EDI / EnterCriticalSection
0042409a  CMP byte ptr [ESI+0x28],BL ; traversal/busy flag
0042409d  MOV EAX,dword ptr [ESI+0x1c]
004240a0  MOV ECX,EAX
004240a2  JZ  drain
004240a4  LeaveCriticalSection
004240ab  throw 0x80070005 via _CxxThrowException(+ DAT_00acc430)
drain:
004240c0  CMP EAX,EBX / JZ done
004240c4  MOV EDX,dword ptr [EAX+0x8] ; node->next
004240c7  MOV dword ptr [ESI+0x1c],EDX
004240ca  MOV EAX,dword ptr [ECX]
004240cc  PUSH 1 / CALL [EAX]         ; scalar dtor delete
004240d0  MOV EAX,[ESI+0x1c] / MOV ECX,EAX / JMP drain
done:
00424137  MOV [ESI+0x24],0 / [ESI+0x20],0 / [ESI+0x1c],0
00424140  ADD ESI,4 / LeaveCriticalSection
0042414a  restore FS:[0] / POP* / RET
```

SEH filter block `0x004240d7`–`0x00424136` (not on happy path): logs `VOG_DEBUG_STOP` + `"List Error!  DestroyAll Call Stack:\n%s\n"`.

### Entry + epilogue hex samples

Entry (32 B):
```
55 8B EC 6A FF 68 90 D7 9B 00 64 A1 00 00 00 00 50 64 89 25 00 00 00 00 51 B8 04 10 00 00 E8 ED
```

Epilogue (`0x00424137`):
```
89 5E 24 89 5E 20 89 5E 1C 83 C6 04 56 FF 15 B8 61 9C 00 8B 4D F4 5F 5E 64 89 0D 00 00 00 00 5B 8B E5 5D C3
```

### Layout (ObjectCsList family)

| Off | Role |
|---|---|
| `+0x00` | vtbl |
| `+0x04` | `CRITICAL_SECTION` |
| `+0x1C` | list head (node*) |
| `+0x20` | count/aux |
| `+0x24` | count/aux |
| `+0x28` | traversal/busy flag (throw if set) |
| node `+0x00` | vtbl |
| node `+0x08` | next |

**No** `node+4 = 0` (same as twin `ObjectCsList_ClearDestroyNoNode4_Inferred` / `0x0051bc90`; opposite of `0x0051b8a0`).

### Twin / RTTI

- CF-identical clone of `FUN_0051bc90` (`ObjectCsList_ClearDestroyNoNode4_Inferred`) — same 251 B class, different SEH label.
- CNDUIWindow nested member `@ +0x1E8`: dual-phase vtbls `PTR_FUN_00a964b8` → `PTR_FUN_00a964ac` then this ×2 + `DeleteCriticalSection(+0x1EC)`.
- COL on `00a964ac` → type_info `.?AV?$CNDDoubleList@PAUSTextMarkup@CNDUIWindow@@$00@@`
- COL on `00a964b8` → `.?AV?$CNDDoubleListPtr@PAUSTextMarkup@CNDUIWindow@@$00@@`
- Shared method: many list complete/scalar dtors call this (not TextMarkup-only).

### Callers (sample)

`CNDUIWindow_CompleteDtor` (`007b5be0` ×2), `FUN_00424160` (complete), `FUN_00424510` (scalar on `00a964ac`), `FUN_007aff00`, `FUN_00423ea0`, many `00424*` / UI list dtors. `xref_count=38`.

### Classification

Worker list method — drains under CS; does **not** free list or `DeleteCriticalSection` (callers do).
