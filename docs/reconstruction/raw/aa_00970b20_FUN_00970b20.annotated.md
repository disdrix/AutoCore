# Annotated low-level: SideHost_PushKeyFlag0_Inferred (`FUN_00970b20`)

| Field | Value |
|---|---|
| Stable ID | `aa_00970b20` |
| VA | `0x00970b20`–`0x00970b55` (**54 B**) |
| System | Side-host / NDResource optional path (`cache+0x6c` host) |
| Date | 2026-07-23 scaffold; W33-N seal 2026-07-29 |

## Machine-level notes

- **ECX** = key holder (`key = *(u32*)ecx`); stack **host***; **`RET 0x4`**.
- Builds `PodU32U8{key, flag:0}` on stack via `FUN_0043e8f0` → `PodU32U8_StoreSplit` (`0043ea50`, W32-M).
- Pushes pod into **locked** stride-2 U32U8 vector at **`host+0xAC`** via `GuardedVector_PushBackLocked_Stride2_U32U8` (`0043e7f0`, W32-N).
- Bit-twin of `FUN_00970b60` (flag imm `0` vs `1` only).
- Prefer bytes over decompiler (void/no-args surface is incomplete).

## Pseudocode (annotated)

```c
// thiscall: ECX = keyHolder*; stack = host*; RET 0x4
void SideHost_PushKeyFlag0_Inferred(void* keyHolder /*ECX*/, void* host /*stack*/)
{
  PodU32U8 pod; // built at [esp+0x10] via adapter
  // adapter path: ESI=&pod; stack sources key=*keyHolder, flag=0
  // FUN_0043e8f0() → PodU32U8_StoreSplit(dest=&pod, pU32, pU8)
  pod.key  = *(uint32_t*)keyHolder;
  pod.flag = 0;
  GuardedVector_PushBackLocked_Stride2_U32U8(
      &pod,
      (GuardedVectorHeader*)((uint8_t*)host + 0xAC) /*EDI*/);
}
```

## ABI / layout

| Slot | Meaning |
|---|---|
| ECX | key holder; first dword = event key |
| stack[+4] | side host object |
| host+0xAC | GuardedVector header (locked U32U8 ring) |
| flag | constant **0** |

## Call graph

| Role | Symbol |
|---|---|
| Adapter | `FUN_0043e8f0` → `PodU32U8_StoreSplit` |
| Nested push | `FUN_0043e7f0` / `GuardedVector_PushBackLocked_Stride2_U32U8` |
| Callers | `FUN_0075e2d0` @ `0075e333`; `FUN_00971030` @ `0097103b` |
| Twin | `FUN_00970b60` flag=1 |

## Open questions

- Product/PDB host class and flag-0 event English.
- Nested adapter `0043e8f0` dual free.
- Runtime / bit-exact.
