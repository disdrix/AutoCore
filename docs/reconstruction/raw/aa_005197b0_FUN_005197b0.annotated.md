# Annotated low-level: FUN_005197b0 → EnsureFactionsLookupLoaded

| Field | Value |
|---|---|
| Stable ID | `aa_005197b0` |
| VA | `0x005197b0`–`0x00519a22` (626 B) |
| System | factions / client table bring-up |
| Date | 2026-08-04 (WQ9R-F dual; supersedes 2026-07-23 scaffold notes) |
| Canonical | `EnsureFactionsLookupLoaded` |

## Machine-level notes

- Source: raw capture + live re-decompile + `read_memory` + loader callee.
- Prefer assembly when decompiler conflicts (hash this, null-check, end free).
- **ABI:** no stack args; void; SEH `LAB_009a3683`.
- **Hash:** `CNDHash_Recreate` `FUN_0051d290(4)` with **ECX = `0x00b04314`**.
- **DB:** enter `FUN_007e1d80` / leave `FUN_007b7df0` with ECX=`*[0x00b0469c]`.
- **Load:** `FUN_007ce550` — product xpath **`//tFactions/row`**; fills temp vector of **0xc4**-byte rows (`id`, wide A @+4, wide B @+0x44).
- **Materialize:** `operator_new(100)` → id @0, MBCS A @+4, MBCS B @+0x24 (WideCharToMultiByte + code page from `PTR_FUN_00af8c9c`).
- **Insert:** `FUN_0051e5d0` ECX=hash, key=id, value=rec, flag=0.
- **Lazy gate (caller):** `FUN_0051a120` if `DAT_00b04320==0`.
- **Boot:** InitInstance `FUN_0094a6a0` alongside XP/credits/paint ensures.
- Fail: load status `<0` → `VOG_DEBUG_STOP`; WCMAP fail → `FUN_004048c0`; OOM → `FUN_004048e0(0x8007000e)`.

## Pseudocode (annotated)

```c
void EnsureFactionsLookupLoaded(void)
{
  SEH_enter;
  CNDHash_Recreate(&g_FactionHash /*0x00b04314*/, /*power*/ 4);
  vector_0xc4 rows = {};
  DbEnter(*g_pDbCtx /*0x00b0469c*/);
  int st = LoadTFactions(/*FUN_007ce550*/ &rows); // //tFactions/row
  DbLeave(*g_pDbCtx);
  if (st < 0) FUN_007a4480(0, "VOG_DEBUG_STOP");

  for (uint i = 0; rows.begin && i < count(rows, 0xc4); ++i) {
    FactionSrc* s = rows.begin + i; // stride 0xc4
    FactionRec* r = operator_new(100);
    r->id = s->id;
    WideToMbcs(s->nameA /*+4*/, r->nameA /*+4*/);   // decomp null-check hazard on +4
    WideToMbcs(s->nameB /*+0x44*/, r->nameB /*+0x24*/);
    CNDHash_Insert(&g_FactionHash, r->id, r, /*allowDup*/ 0);
  }
  operator_delete(rows.begin); // NOT noreturn
  SEH_leave;
}
```

## Open questions

- English names of the two string columns.
- Exact CNDHash field map for `DAT_00b0431c/20/24` vs host base.
- Full dual of loader `FUN_007ce550` and insert `FUN_0051e5d0`.
