# Raw capture: FUN_0048a060

| Field | Value |
|---|---|
| **Stable ID** | `aa_0048a060` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0048a060` |
| **Canonical name** | `FUN_0048a060` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0048a060(uint param_1)

{
  undefined4 local_4;
  
  local_4 = param_1 & 0xffffff00;
  if (1 < *(int *)(param_1 + 0x14)) {
    FUN_00489f20(*(undefined4 *)(param_1 + 0x10),0,*(int *)(param_1 + 0x14) + -1,local_4);
  }
  return;
}
```

---

## MEGA-116 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-116** |
| **Tools** | Ghidra `decompile_function` + `analyze_function_complete` + `get_function_callers` / `get_function_callees` + `get_xrefs_to` + `disassemble_function` + `read_memory` + `get_function_by_address` |
| **Forbidden** | `disassemble_bytes` (not used); Launcher (not used); parent ledgers (not touched) |
| **Program** | `autoassault.exe` @ image base `0x400000` (Ghidra project AA-decode) |

### Live decompile (2026-08-05) — ≡ scaffold CF

```c
void __fastcall FUN_0048a060(uint param_1)

{
  undefined4 local_4;
  
  local_4 = param_1 & 0xffffff00;
  if (1 < *(int *)(param_1 + 0x14)) {
    FUN_00489f20(*(undefined4 *)(param_1 + 0x10),0,*(int *)(param_1 + 0x14) + -1,local_4);
  }
  return;
}
```

### Body / ABI (sealed via disassemble_function + read_memory)

| Item | Value |
|---|---|
| Entry | `0x0048a060` |
| Inclusive end | `0x0048a082` (**35 B** / `0x23`) |
| Next function | `FUN_0048a083` @ `0x0048a083` |
| Convention | **thiscall** — **ECX** = hit-list object (decompiler shows as `__fastcall` `param_1` = ECX) |
| Return | void; bare **`RET`** (`C3`) after `POP ECX` |
| Stack frame | single local via `PUSH ECX` / low-byte zero |

**Body hex (35 B):**
```
51 8B 41 14 83 F8 01 C6 04 24 00 7E 14 8B 14 24
52 48 50 8B 41 10 6A 00 50 E8 A2 FE FF FF 83 C4 10
59 C3
```

**Disassembly (authoritative ABI):**
```text
0048a060  PUSH ECX
0048a061  MOV EAX,dword ptr [ECX + 0x14]      ; count
0048a064  CMP EAX,0x1
0048a067  MOV byte ptr [ESP],0x0              ; local = this & 0xFFFFFF00
0048a06b  JLE 0x0048a081                      ; skip if count <= 1
0048a06d  MOV EDX,dword ptr [ESP]
0048a070  PUSH EDX                            ; arg4 = masked this (unused by sort body)
0048a071  DEC EAX
0048a072  PUSH EAX                            ; arg3 = count-1 (hi index)
0048a073  MOV EAX,dword ptr [ECX + 0x10]
0048a076  PUSH 0x0                            ; arg2 = lo index 0
0048a078  PUSH EAX                            ; arg1 = array base @ this+0x10
0048a079  CALL 0x00489f20                     ; quicksort elem 0x30 by float +0x14
0048a07e  ADD ESP,0x10                        ; cdecl 4 args
0048a081  POP ECX
0048a082  RET
```

### analyze_function_complete

| Field | Value |
|---|---|
| classification | worker |
| signature (Ghidra) | `undefined FUN_0048a060(void)` |
| callees | `FUN_00489f20` |
| callers | `FUN_00925820` |
| xrefs | `00925c3e` (count 1) |
| locals | `local_4` Stack[-0x4]; `param_1` register ECX |

### Callers / xrefs

| Kind | Value |
|---|---|
| Callers | **1** — `FUN_00925820` (`Client_InteractWorldClickHub`, dualed W17-C) |
| Xrefs | **1** UNCONDITIONAL_CALL @ `0x00925c3e` |
| Callees | **1** — `FUN_00489f20` (hit-list sort core; MEGA-115 OWN) |

### Call-site context (`Client_InteractWorldClickHub` @ `0x00925c3e`)

```text
00925c06  CALL 0x0040c410          ; init/clear soft-cast hit container (vtbl + count0 + inline buf)
00925c1f  MOV ECX, [g_client+0xe4a4]
00925c2d  PUSH &hitList / PUSH &rayOrQuery
00925c32  CALL 0x0055e1e0          ; soft-cast fill → FUN_006ca890 hit-list core
00925c37  LEA ECX,[ESP + 0xe0]     ; ECX = hit-list object (this)
00925c3e  CALL 0x0048a060          ; OWN: sort if count>1
00925c43  MOV ECX,[ESP + 0xf4]     ; count @ this+0x14
... walk hits; FUN_0040afb0 resolve object* from hit slot +0x20
```

### Hit-list layout (from ctor `FUN_0040c410` + consumer)

| Offset | Role |
|---|---|
| `+0x00` | vtbl (`PTR_LAB_00a2f538` after init) |
| `+0x10` | array base (inline at `this+0x20` / `param_1+8` dwords) |
| `+0x14` | **count** (int) |
| elem size | **0x30** (sort stride `LEA reg,[i+i*2]; SHL 4`) |
| sort key | **float @ elem+0x14** (ascending quicksort) |
| object slot | **ptr @ elem+0x20** (hub walk + `FUN_0040afb0`) |

### Semantics (sealed)

Gate + dispatch only:

1. Read `count = *(int*)(this + 0x14)`.
2. If `count <= 1`, return (no sort).
3. Else call `FUN_00489f20(base, 0, count-1, this & 0xFFFFFF00)` — full-range quicksort of soft-cast hits.
4. 4th arg is only recursive pass-through in `FUN_00489f20` (not used for compare) — preserve odd `this & ~0xFF` mask.

### CF identity

Scaffold raw (2026-07-23) **≡** live decompile (2026-08-05) **≡** assembly control flow.

### Naming (this dual)

Promote **`SoftCastHitList_SortIfCountGt1_Inferred`**. Product English open → `_Inferred`. System: **interaction-activation**.

