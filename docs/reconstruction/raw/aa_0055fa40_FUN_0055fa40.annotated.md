# Annotated low-level: WorldObj_BindLinks_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0055fa40` |
| VA | `0x0055fa40`–`0x0055fbeb` (428 B) |
| Ghidra | `FUN_0055fa40` |
| Named (INFERRED) | `WorldObj_BindLinks_Inferred` |
| System | world-object / phys bind |
| Date | 2026-07-29 (W26-E dual seal) |
| Pair | Unbind twin `FUN_0055fbf0` (W26-D); used by `WorldObj_RebindActivate` `00560e90` |

## Machine-level notes

- Source: live `decompile_function` + `read_memory` (no `disassemble_bytes`).
- ABI: **thiscall** ECX = **manager**; stack = **world object**; **`ret 4`**.
- Gate: requires `*(obj+0xc) != 0` (bindable component / body host).
- Scale uses manager profile float at `*(manager+0xcc)+8` × global **0.5f** (`DAT_00a0f298`).
- Two paths when scale (as int) > 0:
  - Simple: `obj+0x40 == 0` → single `FUN_006297e0(pose, scale, 0, 0)`.
  - Complex: stack worklist + `FUN_006caaa0` + per-entry `FUN_006297e0`, optional heap free via allocator `DAT_00b05060`.
- Teardown: `FUN_0055f4c0` (local arena / container free helper).
- Callers are enroll/rebind/activate manager helpers only (not network packet decoders directly).

## Pseudocode (annotated)

```c
// thiscall manager*; 1 stack arg object*; ret 4
void WorldObj_BindLinks_Inferred(Manager *manager /*ECX*/, WorldObj *obj /*stack+4*/)
{
  if (*(int *)(obj + 0x0C) == 0)
    return;

  // prepare bind scratch from manager+0x234 (call: ECX=stack buf, push field)
  FUN_0062d960(/*stack this*/, *(uint32_t *)(manager + 0x234));

  float scale = *(float *)(*(int *)(manager + 0xCC) + 8) * 0.5f; // DAT_00a0f298
  void *pose = (void *)(*(int *)(obj + 0x14) + 0x20);

  // object component vtbl+0x18(pose, scale, outScratch)
  (**(code **)(**(int **)(obj + 0x0C) + 0x18))(pose, scale, local_60);
  // manager component vtbl+4(obj+0x1c, …)
  (**(code **)(**(int **)(manager + 0xC4) + 4))(obj + 0x1C, auStack_6c, /*…*/);

  if ((int)scale > 0) {
    if (*(char *)(obj + 0x40) == 0) {
      FUN_006297e0(pose, scale, 0, 0);          // simple attach
    } else {
      // stack Vector-like { vtbl=PTR_FUN_009d2820, count=0, cap=-0x7ffffffc, buf }
      // install at *(manager+0x138)+0x24; optional ctx = manager+0xd0 ? +8 : 0
      // FUN_006caaa0(pose, scale, 0, 0, ctx);
      // restore install; walk entries → FUN_006297e0(e+0x80, e+0x84, 0, 0); clear e+0x84
      // maybe free buffer via DAT_00b05060 vtbl+0x14
    }
  }
  FUN_0055f4c0(/*local container*/);            // always on success path
}
```

## Sealed residuals

| Claim | Confidence |
|---|---|
| thiscall manager + object; ret 4 | **High** (bytes) |
| Gate on obj+0xc | **High** |
| Scale half-factor 0.5f | **High** (`read_memory`) |
| Simple vs complex by obj+0x40 | **High** (CF) |
| Product English for manager/obj fields | **Medium** |
| Exact geometry of FUN_006297e0 / FUN_006caaa0 | Out of OWN |
| Runtime / bit-exact | **Open** |

## Open questions

1. Product/PDB name for bind helper and manager class.
2. Semantic of `obj+0x40` (same flag family used by `0053dbb0` AI gate).
3. Whether server ghost bind needs the complex multi-entry path.
