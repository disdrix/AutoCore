# Annotated low-level: FUN_004130d0 → CVOGDialog_ScalarDeletingDtor

| Field | Value |
|---|---|
| Stable ID | `aa_004130d0` |
| VA | `0x004130d0`–`0x004130ee` exclusive (**30 B** / `0x1E`) |
| System | missions-progression / MSVC scalar-deleting destructor / `CVOGDialog` |
| Date | 2026-08-05 (R12-010 dual) |

## Machine-level notes

- Source: raw capture + R12-010 re-verify (`read_memory` full body + vtbl DATA xref + parent RTTI seal).
- Entry: `56 8B F1` → ESI = this (ECX).
- Complete dtor `FUN_0087b7d0` called thiscall (no push of this; callee reads ECX).
- flags bit0 gate: `F6 44 24 08 01` / `74 09`.
- Optional free: push ESI → `operator_delete` (`0x00489822`) → `add esp,4`.
- Return this: `8B C6 5E C2 04 00`.
- Ghidra "noreturn" on `operator_delete` is false (epilogue present).
- `disassemble_function` omitted the `ADD ESP,4` line; **bytes authority** via `read_memory`.
- vtbl[0]: dword @ `0x00a58c6c` = `0x004130d0`.
- RTTI (parent dual R11-030): COL `0x00ab4984` → type_info `0x00af8e50` → `.?AVCVOGDialog@@`.

## Pseudocode (annotated)

```c
// ECX=this; stack flags; RET 4; returns this*
void* __thiscall CVOGDialog_ScalarDeletingDtor(void* self, uint8_t flags)
{
  CVOGDialog_CompleteDtor(self);    // FUN_0087b7d0 (not dualed here)
  if ((flags & 1) != 0) {
    operator_delete(self);          // false-noreturn in Ghidra
  }
  return self;
}
```

## Open questions

- Full member map of complete dtor `FUN_0087b7d0` (not owned; chains `CNDUIDialog_CompleteDtor` `0x00792c20`).
- Runtime / bit-exact / differential — open (no Launcher).
