# Annotated low-level: MissionTracker_ResetUiStateFields_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00929c00` |
| VA | `0x00929c00`–`0x00929c50` inclusive (**81 B** / `0x51`) |
| Ghidra | `FUN_00929c00` |
| Canonical | `MissionTracker_ResetUiStateFields_Inferred` |
| System | missions-progression / mission-tracker UI state |
| Date | 2026-08-05 (MEGA-087 dual refresh) |
| Prior scaffold | 2026-07-23 batch capture |

## Machine-level notes

- **ABI:** custom **EAX** = tracker object (`client+0x4d0` family); no stack args; bare `RET`; returns **1** in `AL`.
- **Not** thiscall/ECX — call sites do `MOV EAX,EDI/ESI` then `CALL`.
- **Leaf:** no callees; single BB; cyclomatic complexity 1.
- **Float:** `DAT_00aaa6fc` is IEEE **25.0f** (`0x41c80000`), moved with `MOVSS` into `tracker+0x104`.
- Prefer assembly when decompiler shows `in_EAX` phantom — confirmed by call-site setup.
- Parents (not dualled here): `FUN_0092a590` (set/clear active mission on tracker), `FUN_0092c080` (tracker derived-state rebuild; inlines same field plate at entry, then re-calls this on region change).

## Pseudocode (annotated)

```c
// EAX = MissionTracker* tracker
// AL  = 1 (always)
uint8_t MissionTracker_ResetUiStateFields_Inferred(void /* EAX = tracker */)
{
  // DAT_00aaa6fc @ 0x00aaa6fc = 25.0f
  float defaultRange = DAT_00aaa6fc; // MOVSS load

  tracker->id_at_18     = 0xFFFFFFFFu; // +0x18
  tracker->slot_at_20   = 0xFFFFFFFFu; // +0x20
  tracker->slot_at_24   = 0xFFFFFFFFu; // +0x24
  tracker->flag_at_54   = 0;           // +0x54  (parent may set 1 when waypoint live)
  tracker->dword_at_80  = 0;           // +0x80
  tracker->dword_at_ac  = 0;           // +0xac
  tracker->dword_at_28  = 0;           // +0x28
  tracker->flag_at_100  = 0;           // +0x100
  tracker->flag_at_101  = 0;           // +0x101
  tracker->flag_at_102  = 0;           // +0x102
  tracker->range_at_104 = defaultRange; // +0x104 f32
  tracker->sent_at_108  = 0xFFFFFFFFu; // +0x108
  tracker->sent_at_10c  = 0xFFFFFFFFu; // +0x10c
  return 1;
}
```

## Call graph (OWN context)

```text
Client_TryBindActiveMissionTracker  0x0092fd00  [dualed]
  └─ FUN_0092a590  Tracker_SetActiveMissionObjective  [residual]
        ├─ clear/miss: FUN_0092a3d0  (vector tidy @ +0x11c)  [residual]
        └─ clear/miss: FUN_00929c00  ResetUiStateFields  [OWN MEGA-087]

FUN_0092c640  [residual]
  └─ FUN_0092c080  tracker derived rebuild  [residual]
        ├─ (inline same field plate at entry)
        └─ region change: FUN_00929c00  [OWN MEGA-087]
```

## Open questions

- Product English for `+0x18..+0x10c` slots (HUD range caption vs minimap vs waypoint TFID).
- Formal RTTI name of tracker type at `client+0x4d0`.
- Why `0092c080` both inlines and later re-calls this helper (region-delta rearm only for second call).
