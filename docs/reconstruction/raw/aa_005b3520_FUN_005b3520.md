# Raw capture: FUN_005b3520

| Field | Value |
|---|---|
| **Stable ID** | `aa_005b3520` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005b3520` |
| **Canonical name** | `FUN_005b3520` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_005b3520(int param_1)

{
  int iVar1;
  undefined4 uVar2;
  
  if (*(int *)(param_1 + 0x154) == 0) {
    iVar1 = *(int *)(*(int *)(param_1 + 0x15c) + 0xe894);
    if ((*(int *)(iVar1 + 0x108) != 0) && (*(int *)(iVar1 + 0x104) == 0)) {
      *(undefined4 *)(param_1 + 0x154) = *(undefined4 *)(*(int *)(iVar1 + 0x108) + 0x8c);
      uVar2 = *(undefined4 *)(*(int *)(*(int *)(*(int *)(param_1 + 0x15c) + 0xe894) + 0x104) + 0x8c)
      ;
      *(undefined4 *)(param_1 + 0x150) = uVar2;
      FUN_0074e310(uVar2);
    }
  }
  return;
}
```

---

## W37-F re-verify append (2026-08-04)

| Field | Value |
|---|---|
| **Wave** | W37-F OWN-ONLY dual |
| **Tools** | `batch_decompile` / `decompile_function` / `analyze_function_complete` / `get_function_by_address` / `read_memory` / `get_function_callers` / `get_function_callees` / `analyze_dataflow` — **no** `disassemble_bytes` |
| **Body** | `0x005b3520`–`0x005b357a` inclusive (**91 B** / `0x5B`); pad `CC` then peer `FUN_005b3580` |
| **Live decompile** | ≡ scaffold raw CF (2026-07-23) |

### Full body hex (91 B)

```
83b9540100000075518b815c0100008b9094e8000083ba0801000000743c83ba040100000075338b92080100008b928c0000008991540100008b8094e800008b90040100008b828c0000008981500100008b490450e896ad1900c3
```

### Byte-sealed control flow

1. `CMP dword [ECX+0x154], 0` / `JNZ ret` — once-only when slot `+0x154` empty.
2. `EAX = [ECX+0x15c]` (parent; ctor `FUN_005b35a0` stores ctor arg at dword index `0x57` = `+0x15c`).
3. `EDX = [EAX+0xe894]` — nested light host (same slot family as W32-R PalantirEnv lights).
4. `CMP [EDX+0x108], 0` / `JZ ret` — require directional light non-null.
5. `CMP [EDX+0x104], 0` / `JNZ ret` — enter only when hemispheric slot **is null** (`analyze_dataflow` CBRANCH → `0x005b357a`).
6. Body (if entered):
   - `[ECX+0x154] = *([EDX+0x108] + 0x8c)` — dir light effect core (W32-R: light`+0x8c`).
   - Reload host: `EAX = [[ECX+0x15c]+0xe894]`; `EDX = [EAX+0x104]`; `EAX = [EDX+0x8c]`.
   - `[ECX+0x150] = EAX`.
   - `ECX = [ECX+4]`; `PUSH EAX`; `CALL FUN_0074e310` (thiscall aux + resource).
7. `RET` (bare `C3`).

### Residual (sealed, not hidden)

Gate requires `host+0x104 == 0` then immediately loads `*(host+0x104)+0x8c`. After W32-R rebuild both `+0x108` and `+0x104` are non-null → this body **no-ops**. Path with dir set and hemi null would null-deref on the hemi load. Document as product residual; do not “fix” CF in clean.

### Callers / callees (live)

| Direction | VA | Role |
|---|---|---|
| Caller | `FUN_005b39a0` @ `0x005b39af` | `CVOGEnvironmentReflect_Enable_Inferred` (W33-T) |
| Caller | `FUN_0048f370` @ `0x0048f64b` | `PalantirEnv_RebuildDefaultLights_Inferred` (W32-R) when `env+0xC4 != 0` |
| Callee | `FUN_0074e310` | refcount bind of resource onto aux (`this=[self+4]`) |

### ABI

- **ECX = `CVOGEnvironmentReflect*`** (thiscall; `param_1` in decompiler).
- No stack formals; bare **RET**; void (no EAX set).
- Host span: reads through `+0x15c`; writes `+0x150` / `+0x154`; uses aux `+4`.

### Name

`CVOGEnvironmentReflect_ResolveLightEffectSlots_Inferred` — host RTTI Confirmed on enable/ctor family (W32-T/W33-T). Reject scaffold `Named_CalleeOf_*`.
