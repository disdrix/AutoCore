# Annotated low-level: FUN_0051b2a0 → Object_LazyFillMatColorVecFromMesh_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051b2a0` |
| VA | `0x0051b2a0`–`0x0051b302` exclusive |
| System | object / mesh material color bank |
| Date | 2026-08-04 (WQ9R-H dual seal) |

## Machine-level notes

- **ABI:** `__thiscall` + 4 stack dwords; `RET 0x10`.
- **Guard:** only runs when mat-color `std::vector`-like at `this+0x148/0x14C` is empty (begin null **or** `(end-begin)>>4 == 0`).
- **Source:** `vtbl+0x1CC()` → if non-null and `+8` non-null, that pointer is the mesh material list head for `FUN_00519280`.
- **Sink:** `this+0x144` bank (vector fields at +4/+8/+C relative = this+0x148/0x14C/0x150).
- **Callee `FUN_00519280` (WQ9R-E owns dual):** per mesh entry queries `"MatDiffuse"`, `"MatAmbient"`, `"MatEmissive"` and appends 0x10-byte float4 records; applies stack color overrides via `FUN_009700f0`.
- **Sibling caller `FUN_004c5480`:** calls this unit first, then may fill a **second** bank at adjusted-this offsets from a direct mesh pointer (`-0x27c`) if that bank is empty — dual-bank path; do not merge.

## Pseudocode (annotated)

```c
// __thiscall Object_LazyFillMatColorVecFromMesh_Inferred(this, c0,c1,c2,c3)
// RET 0x10
void Object_LazyFillMatColorVecFromMesh_Inferred(
    void *self,
    uint32_t c0, uint32_t c1, uint32_t c2, uint32_t c3)
{
  uint8_t *base = (uint8_t *)self;
  void *begin = *(void **)(base + 0x148);
  void *end   = *(void **)(base + 0x14C);

  if (begin != NULL && (((uint8_t *)end - (uint8_t *)begin) >> 4) != 0)
    return; // already populated

  void *probe = (*(fn_probe *)(*(void **)self + 0x1CC))(self);
  if (probe == NULL || *(void **)((char *)probe + 8) == NULL)
    return;

  FUN_00519280(
      *(void **)((char *)probe + 8),  // mesh material list
      base + 0x144,                   // color bank host
      c0, c1, c2, c3);                // override colors
}
```

## Open residual

- Product name / demangle for virtual slot.
- Exact type returned by vtbl+0x1CC (gfx mesh wrapper).
- Semantic of the four stack dwords (float4 color vs four independent params) — forwarded opaque; `FUN_00519280` treats them as Mat* override source.
- Runtime / bit-exact / differential.
