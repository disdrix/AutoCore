# Raw capture: FUN_00404d70

| Field | Value |
|---|---|
| **Stable ID** | `aa_00404d70` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00404d70` |
| **Canonical name** | `GameData_LookupCloneBaseByCbid` |
| **System** | clonebase / game data |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00404d70(int param_1,uint param_2)

{
  CNDHash_LookupByKey(*(void **)(param_1 + 0xf10),param_2);
  return;
}
```

---

## Append — dual A/B seal (2026-07-29)

| Field | Value |
|---|---|
| **Canonical name** | `GameData_LookupCloneBaseByCbid` |
| **Body** | `0x00404d70`–`0x00404d7a` (10 B tail-call thunk) |
| **Tools** | `decompile_function`, `read_memory`, `get_function_by_address`, callees/callers/xrefs |
| **Three-rep** | decompile CF ≡ bytes (MOV ECX,[ECX+0xF10]; JMP Lookup); return type corrected to void* |
| **Dual verdict** | **accept** (A+B) |

### Body bytes (`read_memory` @ `0x00404d70`, 16 B)

```
8B 89 10 0F 00 00    MOV  ECX, [ECX+0xF10]     ; hash* from host
E9 A5 BB 1A 00       JMP  CNDHash_LookupByKey  ; → 0x005b0920 (tail)
CC CC CC CC CC CC    INT3 padding
```

Hex: `8b89100f0000e9a5bb1a00cccccccccc`

Jump target check: next=`0x00404d7b` + rel32 `0x001abba5` = **`0x005b0920`** (`CNDHash_LookupByKey`, sealed `aa_005b0920`).

### Sealed ABI (decompiler void is wrong)

- **`__thiscall`**: ECX = game-data host (`DAT_00b041fc` at all sampled call sites), stack **`uint32_t cbid`**, epilogue **`RET 4`** (from Lookup tail), return **value `void*`** in **EAX** (clonebase / item def or NULL).
- Sole behavior: `return CNDHash_LookupByKey(*(void**)(this + 0xf10), cbid);`
- Not void — GiveItemByCbid / CreateCreature / Collect_SlotAction all test EAX for null and read def fields (`+0x38` type, `+0x3c` specifics, …).

### Host global

- Call sites load `MOV ECX, [0x00b041fc]` then `PUSH cbid; CALL 0x00404d70`.
- `DAT_00b041fc` = game-data owner; hash table pointer at owner **`+0xf10`**.
- Owner product class English open; offset **`+0xf10`** role sealed as **CNDHash\*** for CBID→clonebase.

### Reviews

- A: `docs/reconstruction/reviews/A_aa_00404d70_GameData_LookupCloneBaseByCbid.md`
- B: `docs/reconstruction/reviews/B_aa_00404d70_GameData_LookupCloneBaseByCbid.md`
