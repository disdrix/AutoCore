# Raw capture: FUN_00403c60

| Field | Value |
|---|---|
| **Stable ID** | `aa_00403c60` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00403c60` |
| **Canonical name** | `FUN_00403c60` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 *
FUN_00403c60(basic_string<char,struct_std::char_traits<char>,class_std::allocator<char>_> *param_1)

{
  char cVar1;
  undefined4 *puVar2;
  bool bVar3;
  int in_EAX;
  undefined4 *puVar4;
  undefined4 *puVar5;
  
  puVar5 = (undefined4 *)(*(undefined4 **)(in_EAX + 4))[1];
  cVar1 = *(char *)((int)puVar5 + 0x131);
  puVar2 = *(undefined4 **)(in_EAX + 4);
  while (cVar1 == '\0') {
    bVar3 = std::operator<<char,struct_std::char_traits<char>,class_std::allocator<char>_>
                      ((basic_string<char,struct_std::char_traits<char>,class_std::allocator<char>_>
                        *)(puVar5 + 3),param_1);
    if (bVar3) {
      puVar4 = (undefined4 *)puVar5[2];
      puVar5 = puVar2;
    }
    else {
      puVar4 = (undefined4 *)*puVar5;
    }
    puVar2 = puVar5;
    puVar5 = puVar4;
    cVar1 = *(char *)((int)puVar4 + 0x131);
  }
  return puVar2;
}
```

---

## Re-verify append — MEGA-041 OWN-ONLY dual 2026-08-05

**Agent:** MEGA-041  
**Tools:** `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + callers/xrefs + `get_assembly_context`. **No** `disassemble_bytes`. No Launcher.  
**Integrity:** scaffold body above preserved; this section append-only.  
**Image:** `autoassault.exe` base `0x400000`.

### Live decompile = scaffold CF

Live Ghidra decompile **=** 2026-07-23 raw: MSVC ordered-tree **lower_bound** walk on `std::string` keys with **isnil @ +0x131**. Candidate starts at head/end; on `!(node.key < key)` promote candidate and descend left; else descend right. Return candidate node*.

### Body / ABI (bytes + disassembly)

| Fact | Value |
|---|---|
| Body | `0x00403c60`–`0x00403ca6` inclusive (**71 B** / `0x47`); pad `CC` before `FUN_00403cb0` @ `0x00403cb0` |
| Convention | **EAX = map** (`_Myhead` at map+4); stack `const std::string*`; **EAX = node*** out; **`RET 4`** (`C2 04 00`) |
| isnil | **`+0x131`** (`CMP byte ptr [ESI+0x131],0` / `80 BE 31 01 00 00`) |
| Key | `std::string` at **node+0x0C** (`LEA ECX,[ESI+0xC]`) |
| Links | left@0 parent@4 right@8 (classic MSVC `_Tree_node`) |
| Callee | IAT `[0x009c62e8]` ? `std::operator<` on two `basic_string` (cdecl, `ADD ESP,8`) |

Full body hex:

`
8b4004558b6c2408568b700480be3101000000578bf87529538b1de8629c00908d4e0c5551ffd383c40884c074058b7608eb048bfe8b3680be310100000074e05b8bc75f5e5dc20400
`

Disassembly (sealed):

`	ext
00403c60  MOV  EAX, [EAX+4]          ; head = map->_Myhead
00403c63  PUSH EBP
00403c64  MOV  EBP, [ESP+8]          ; key = stack string*
00403c68  PUSH ESI
00403c69  MOV  ESI, [EAX+4]          ; cur = head->parent (root)
00403c6c  CMP  BYTE PTR [ESI+0x131], 0
00403c73  PUSH EDI
00403c74  MOV  EDI, EAX              ; cand = head (end())
00403c76  JNZ  exit
00403c78  PUSH EBX
00403c79  MOV  EBX, [0x009c62e8]     ; operator<
loop:
00403c80  LEA  ECX, [ESI+0xC]        ; &node.key
00403c83  PUSH EBP
00403c84  PUSH ECX
00403c85  CALL EBX                   ; operator<(node.key, key)
00403c87  ADD  ESP, 8
00403c8a  TEST AL, AL
00403c8c  JZ   go_left
00403c8e  MOV  ESI, [ESI+8]          ; right
00403c91  JMP  next
go_left:
00403c93  MOV  EDI, ESI              ; cand = cur
00403c95  MOV  ESI, [ESI]            ; left
next:
00403c97  CMP  BYTE PTR [ESI+0x131], 0
00403c9e  JZ   loop
00403ca0  POP  EBX
exit:
00403ca1  MOV  EAX, EDI              ; return cand
00403ca3  POP  EDI / ESI / EBP
00403ca6  RET  4
`

### Callers / xrefs

| Site | Parent | Role |
|---|---|---|
| `0x0093bdfa` | `FUN_0093bd10` | Slash-command dispatch: `LEA EAX,[EBX+4]` + `PUSH` string ? lower_bound; compare result to end `[EBX+8]`; prefix-match + level gate + handler `[node+0x12C]` |
| `0x0095798a` | `FUN_00957820` | Slash help list: paired with upper_bound `FUN_00403d60`; iterate `[lb, ub]` via `FUN_00404000` |
| `0x00957c7b` | `FUN_00957820` region | Second lower_bound site (same help/list path family) |

- **xref_count:** 3 UNCONDITIONAL_CALL  
- **callers (functions):** `FUN_0093bd10`, `FUN_00957820`  
- **callees:** `std::operator<` (IAT `0x009c62e8` ? `0x006eadd0`)  
- **classification:** worker / MSVC `_Tree::lower_bound` string-key specialization

### Semantics seal

Classic lower_bound: first node with `!(node.key < key)` (i.e. `key <= node.key` in order). Returns head/end when no such node.

Twin **upper_bound** peer: `FUN_00403d60` (`operator<(key, node.key)` inverted walk).  
Family peers: `StdTree_IteratorIncrement_Isnil131` `0x00404000`; erase/free isnil131 units.

**Name:** `StdTree_LowerBound_StringKey_Isnil131_Inferred` (Ghidra `FUN_00403c60`). Product host type English open ? `_Inferred`.

### Decompile = assembly CF

Yes. ABI is **EAX-map + stack key + RET 4** (decompiler `in_EAX` is real; not thiscall-ECX).

### Gaps (not sealed here)

- Product map type English (slash-command registry inferred from callers only).  
- Runtime / bit-exact / differential.  
- Full node payload field English beyond key@+0xC / isnil@+0x131 / handler@+0x12C evidence from callers.
