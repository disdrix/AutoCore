# Annotated low-level: StdTree_EraseAndRebalance_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005a4590` |
| VA | `0x005a4590` |
| Canonical | `StdTree_EraseAndRebalance_Isnil21_Inferred` |
| Ghidra | `FUN_005a4590` |
| System | skills-abilities / MSVC `std::_Tree` erase (isnil@+0x21) |
| Date | 2026-08-05 (R12-032 OWN dual; scaffold 2026-07-23) |

## Machine-level notes

- **Role:** single-node erase + RB rebalance for **isnil@+0x21 / color@+0x20** trees (node **0x24** in this neighborhood via sibling buynode `005a4870`).
- **ABI:** `__thiscall` ECX=map shell; stack `Node** outIt`, `Node* node`; **`ret 8`**.
- **Throw:** if `node->isnil` (`+0x21 != 0`) → `"invalid map/set<T> iterator"` / `DAT_00acc34c`.
- **Successor:** `FUN_004e12c0` (iterator increment / succ prep isnil21) before unlink.
- **Extremities:** leftmost via `FUN_004cb2c0` (min isnil21), rightmost via `FUN_00421a60` (max isnil21).
- **Rotates:** L `FUN_0050e9f0` / R `FUN_005a27f0` (dualed R10-030 / R11-007).
- **Value dtor:** if `node[+0x14] != 0` → `operator_delete` heap buffer; zero `+0x14/+0x18/+0x1c` (vector-like begin/end/cap); then free node.
- **Epilogue (decomp miss):** `size--` when size>0; `*outIt = succ`; `ret 8`. Ghidra body ends early at `0x005a482f` due to false noreturn on `operator_delete`.
- **Caller:** erase-range shell `FUN_005a4c30` only (1 xref).
- **Unreachable-block warnings** around `0x005a46ad`–`0x005a46f3` — Ghidra may elide full both-children successor-swap path; CF sealed to live decompile + byte epilogue (same posture as isnil29 peer erase).

## Layout (node 0x24)

| Offset | Field |
|---|---|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x0C | value key dword (from sibling buynode) |
| +0x10 | value aux / vector this-base |
| +0x14 | owned heap begin (freed on erase) |
| +0x18 | end |
| +0x1C | capacity end |
| +0x20 | color (0 red / 1 black) |
| +0x21 | isnil |
| size | **0x24** |

Shell: `head@+4`, `size@+8`.

## Pseudocode (annotated)

See raw body + clean `StdTree_EraseAndRebalance_Isnil21_Inferred.cpp`. High-level:

```
if node.isnil: throw "invalid map/set<T> iterator"
succ_prep(outIt)           // FUN_004e12c0
repl = splice_child(node)  // left/right isnil21 selection
relink parent/root; fix head leftmost/rightmost (min/max)
if node.color == black: RB rebalance with L/R isnil21 rotates
if node[+0x14]: delete buffer; zero +0x14/+0x18/+0x1c
operator_delete(node)
if map.size > 0: map.size--
*outIt = succ; ret 8
```
