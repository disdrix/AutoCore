# Raw capture: FUN_0043e8f0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0043e8f0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0043e8f0` |
| **Canonical name** | `FUN_0043e8f0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0043e8f0(void)

{
  FUN_0043ea50(&stack0x00000008);
  return;
}
```

---

## Live re-verify append — W33-M 2026-07-29

**Tools:** `decompile_function` + `read_memory` + `get_function_by_address` + `analyze_function_complete` + `disassemble_function` (listing only; **no** `disassemble_bytes`). No Launcher.

### Function bounds

| Field | Value |
|---|---|
| Entry | `0x0043e8f0` |
| Body end | `0x0043e902` (last insn `RET`) |
| Length | **19 B** / `0x13` |
| Pad | `CC` at `0x0043e903` |

### Full body hex (19 B + 1 pad)

```
8D 44 24 08 50 8D 4C 24 08 8B C6 E8 50 01 00 00 8B C6 C3 | CC
```

### Listing (bytes-aligned)

```
0043e8f0  LEA  EAX,[ESP+0x8]     ; &stack formal2 (u8 value)
0043e8f4  PUSH EAX               ; pU8 for StoreSplit
0043e8f5  LEA  ECX,[ESP+0x8]     ; after push → &formal1 (u32 value)
0043e8f9  MOV  EAX,ESI           ; dest = ESI
0043e8fb  CALL 0x0043ea50        ; PodU32U8_StoreSplit (rel32 +0x150)
0043e900  MOV  EAX,ESI           ; return dest
0043e902  RET                    ; bare RET (cdecl; caller ADD ESP,8)
```

### ABI (bytes win over decompiler `void()`)

| Slot | Source | Conf |
|---|---|---|
| dest | **ESI** → copied to EAX before call + return | **High** |
| u32 value | Stack `[ESP+4]` at entry (address taken → ECX) | **High** |
| u8 value | Stack `[ESP+8]` at entry (address taken → stack pU8) | **High** |
| cleanup | bare **`RET`** — callee of StoreSplit does `RET 4` for its 1 stack formal; **this** function is cdecl (callers `ADD ESP,8`) | **High** |
| return | **EAX = ESI** (dest) | **High** |

### Callee / callers

- **Callee (1):** `FUN_0043ea50` / `PodU32U8_StoreSplit` (W32-M sealed).
- **Xrefs (9) / callers (6):** `NDResourceCache_LookupOrCreate` @ `0075dd96`; `FUN_00970b20` @ `00970b36`; `FUN_00970b60` @ `00970b76`; `FUN_0075de80` @ `0075df62` / `0075e014` / `0075e131` / `0075e1a5`; `FUN_0075d3c0` @ `0075d437`; `FUN_00970e50` @ `00970f30`.

### Caller pattern (context; not OWN)

`FUN_00970b20` / `00970b60`: materialize local `{u32=*ECX, u8=0|1}` via this adapter (ESI=local dest), `ADD ESP,8`, then `PushBackLocked_Stride2_U32U8(EDI=host+0xac, value*=EAX)`.

### Decompile gap

Ghidra collapses to `FUN_0043ea50(&stack0x00000008)` and drops ESI dest, dual LEA, and EAX return. **Bytes win.**

### Name (sealed structural)

`PodU32U8_StoreByValue` (**Inferred**) — by-value stack formals → split-pointer store into ESI dest.
