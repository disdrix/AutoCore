# Annotated low-level: FUN_0076ba10 → Fs_DeleteDirTreeRecursive_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0076ba10` |
| VA | `0x0076ba10`–`0x0076bbff` (**495 B**) |
| System | filesystem / recursive directory delete |
| Date | 2026-07-23 scaffold; **W34-A dual 2026-07-29** |

## Machine-level notes

- Live Ghidra `decompile_function` + `read_memory` (no `disassemble_bytes`).
- CF ≡ 2026-07-23 raw. cdecl bare RET after `ADD ESP,0x70`.
- `param_2` is **byte** recursive/force flag (not a full dword semantic).
- `FUN_0076b080` takes force flag on stack/register; **path is ESI** (decompiler `unaff_ESI` on peer).
- Path join separator `DAT_00a2eb5c` = `"/"` (not `"\\"`).
- Listing stride **0x1c** (`std::string`); tidy via `FUN_00431ae0`.
- Delete failures `printf` + continue; final success is only root `_rmdir`.

## Pseudocode (annotated)

```c
// cdecl; path @ [esp+4], recursive_force @ [esp+8] (byte)
// SEH LAB_009adcab; ADD ESP,0x70; RET
bool Fs_DeleteDirTreeRecursive_Inferred(const char* path, char recursive_force)
{
  // early: non-recursive → rmdir only
  if (recursive_force == 0) {
    return _rmdir(path) == 0;
  }

  // std::vector<std::string> listing{};  // begin/end/cap near local_6c/local_68/local_64
  // out vector object starts at local_70 region
  FUN_0076b3f0(path, &listing);  // Fs_ListDirBasenames_Inferred (W33-C)

  for (each basename in listing; stride 0x1c) {
    // full = path + "/" + basename   (FUN_00416490 twice; SSO cap < 0x10)
    std::string full = std::string(path) + "/" + basename_cstr;

    if (FUN_0076adc0(full.c_str())) {  // Fs_PathIsDirectory_Stat_Inferred
      // recurse with same force flag
      if (!FUN_0076ba10(full.c_str(), recursive_force)) {
        printf("Delete directory failed for: %s %d\n", full.c_str(), *_errno());
      }
    } else {
      // FUN_0076b080(force): ESI=full path; optional chmod then unlink
      if (!FUN_0076b080(/*ESI=*/full.c_str(), recursive_force)) {
        // note: plate uses basename (vector element), not full path
        printf("Delete file failed for: %s %d\n", basename_cstr, *_errno());
      }
    }
    // ~full
  }

  int rc = _rmdir(path);
  FUN_00431ae0(&listing);  // destroy string range + delete buffer
  return rc == 0;
}
```

## Open questions

- Product English plate for `FUN_0076b080` / `FUN_00431ae0` (not owned this wave).
- Exact ECX binding into `FUN_00431ae0` (fastcall / thiscall tidy of vector).
- Runtime / bit-exact / differential — open.
