# Annotated low-level: Skill_InsertActiveCastBinding_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00518ca0` |
| VA | `0x00518ca0`–`0x00518ce4` (**0x44** B) |
| System | skills-abilities |
| Date | 2026-07-29 (W24-H dual A/B) |
| Canonical name | `Skill_InsertActiveCastBinding_Inferred` |
| Ghidra name | `FUN_00518ca0` |
| Prior alias | `Named_CalleeOf_Skill_Skill_d_has_invalid_frequency_d_00518ca0` (**reject** — parent frequency string is not this body) |

## Machine-level notes

- Source: raw capture + live `decompile_function` + `read_memory` (2026-07-29). **No** `disassemble_bytes`.
- Prefer bytes when decompiler conflicts (ECX owner; staged pushes for map insert).
- Epilogue **`ret 0x14`** → five stack dwords cleaned. Entry **ECX** = map owner (preserved through body; used by `FUN_00518c20`).

## ABI (sealed)

```c
// __thiscall  ret 0x14
void Skill_InsertActiveCastBinding_Inferred(
    void *mapOwner,   // ECX — same host as FUN_00518c20 (slot +0x68)
    int   skillId,    // stack+4
    uint  tfid0,      // stack+8
    uint  tfid1,      // stack+0xC
    uint  tfid2,      // stack+0x10
    uint  tfid3);     // stack+0x14
```

## Local pack (bytes)

Frame `sub esp,0x18` then:

| Local off | Source | Role |
|---|---|---|
| `+0` | arg skillId | map key |
| `+4` | **unwritten** | pair padding (node `+0x14`) |
| `+8` | tfid0 | value TFID dword 0 |
| `+0xC` | tfid1 | value TFID dword 1 |
| `+0x10` | tfid2 | value TFID dword 2 |
| `+0x14` | tfid3 | value TFID dword 3 |

## Recovered CF (decompiler noise corrected)

```c
// mapOwner = ECX (preserved; no ECX clobber in pack)
// out-pair temp lands on already-copied stack arg slot (result discarded)
void Skill_InsertActiveCastBinding_Inferred(
    void *mapOwner, int skillId, uint t0, uint t1, uint t2, uint t3)
{
  uint local[6]; // 0x18 bytes; local[1] left uninitialized
  uint outPair[2]; // decompiler: &param_2; write-only discard

  local[0] = skillId;
  // local[1] gap
  local[2] = t0;
  local[3] = t1;
  local[4] = t2;
  local[5] = t3;

  void *map = FUN_00518c20(mapOwner);          // lazy map @ owner+0x68
  FUN_004cbe20(map, &outPair, local);          // map insert-or-find by int key
}
```

Decompiler shows `FUN_00518c20(puVar1, local_18)` multi-arg — **noise**. Getter is unary `__fastcall(owner)`; two pushes before that call are stack args for the subsequent `FUN_004cbe20` thiscall (same staged-push pattern as Lookup/Clear duals).

## Callees

| VA | Role |
|---|---|
| `FUN_00518c20` `0x00518c20` | leave-FUN lazy map get @ `+0x68` |
| `FUN_004cbe20` `0x004cbe20` | MSVC-style map insert-or-find (`pair<it,bool>`); key `*int`, node key `@+0x10`, isnil `@+0x29` |

## Sole caller

| Caller | Site | Setup |
|---|---|---|
| `Skill_HB_FireTail_FxTargetAndPeriodStamp` (`0x00578b30`) | `0x00578c55` | After target `vtbl+0x214` hit + flag `HB+0x639` bit `0x02`: `sub esp,0x10` copy `HB+0x678` TFID16; `push [HB+0x620]` skillId; `lea ecx, […]` mapOwner; `call` |

## Node / map family (from siblings; not re-owned)

| Off | Field |
|---|---|
| map `+4` | `_Myhead` |
| map `+8` | size |
| node `+0x10` | key = skillId |
| node `+0x14` | pad (insert gap) |
| node `+0x18…+0x24` | TFID16 (Lookup/Clear) |
| node `+0x29` | isnil |

## Open questions

- Product/PDB symbol for insert helper and map owner type.
- Whether pad dword `@+4` / node `+0x14` is ever read.
- Soft-insert vs overwrite policy inside `FUN_004cbe20` when key exists (bool=0 path keeps existing).
- Runtime / bit-exact / differential.
