# Annotated low-level: FUN_005706d0 → InventoryGrid_SetDirtyFlag (**INFERRED**)

| Field | Value |
|---|---|
| Stable ID | `aa_005706d0` |
| VA | `0x005706d0` |
| System | `inventory-transfer` |
| Date | 2026-08-04 (WQ8R-J re-annotate; dual 2026-07-29) |
| Tools | decompile + read_memory; **no** disassemble_bytes |

## Machine-level notes

- **ABI:** `__thiscall` / `__fastcall` ECX = InventoryGrid*; **no stack args**; plain `ret` (`C3`).
- **Body 21 B:** optional owner dirty propagate, then always set dirty byte.
- **Callee:** `FUN_00512670` `Object_PropagateDirtyBit0x20000` — ORs `0x20000` into object`+0x17c` and walks `+0xac` chain (owned elsewhere).
- **ECX for callee:** loaded from `grid+0x24` (owner object*), **not** the grid itself.
- **Offsets (OBJECT_LAYOUTS InventoryGrid):**
  - `+0x21` dirty / needs-refresh flag (also set by MergeStackQty `0x00571830`)
  - `+0x24` owner object*
- **Role:** leaf post-mutation dirty mark (place footprint tail, locker refresh, etc.). No place math.

## Pseudocode (annotated)

```c
// __thiscall InventoryGrid*
void InventoryGrid_SetDirtyFlag_Inferred(InventoryGrid* grid)
{
  void* owner = *(void**)((char*)grid + 0x24);
  if (owner != nullptr)
    Object_PropagateDirtyBit0x20000(owner); // FUN_00512670; ECX = owner
  *(uint8_t*)((char*)grid + 0x21) = 1;
}
```

## Open questions

- Product English for dirty flag / owner pointer.
- Who clears `+0x21`.
- Runtime / bit-exact.
