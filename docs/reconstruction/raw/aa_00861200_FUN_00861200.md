# Raw capture: FUN_00861200

| Field | Value |
|---|---|
| **Stable ID** | `aa_00861200` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00861200` |
| **Canonical name** | `FUN_00861200` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
uint __fastcall FUN_00861200(int *param_1)

{
  int in_EAX;
  undefined4 uVar1;
  uint uVar2;
  undefined4 uVar3;
  undefined4 uVar4;
  undefined4 uVar5;
  
  if ((param_1 != (int *)0x0) && (*(int *)(in_EAX + 0x56c) != 0)) {
    uVar3 = *(undefined4 *)(*(int *)(in_EAX + 0x56c) + 4);
    uVar4 = 1;
    uVar1 = (**(code **)(*param_1 + 0x3ac))(uVar3,1);
    uVar2 = FUN_00941b20(uVar1,uVar3,uVar4);
    return uVar2;
  }
  FUN_007a69d0();
  uVar5 = 0;
  uVar4 = 1;
  uVar1 = 0xffffffff;
  uVar3 = FUN_007a6de0("This equipment cannot be changed at this time.",0xffffffff);
  uVar2 = FUN_007fdfb0(&DAT_00d1a840,uVar3,uVar1,uVar4,uVar5);
  return uVar2 & 0xffffff00;
}
```


---

## MEGA-125 re-verify (2026-08-05) — append only

**Agent:** OWN-ONLY dual **MEGA-125** (VA `0x00861200` only).  
**Tools:** Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_xrefs_to` / `get_function_xrefs` + `get_assembly_context` (sites `008615f4`, `00861222`) + parent create/decompile `FUN_008612b0` @ `0x008612b0` (evidence only) + callees `00941b20` / `007a69d0` / `007a6de0` / `007fdfb0`. **No** `disassemble_bytes`. No Launcher.

### Live decompile ≡ raw CF

Ghidra 2026-08-05 live decompile is **byte-identical control flow** to 2026-07-23 raw body:
- gate `param_1 != 0 && *(in_EAX + 0x56c) != 0`
- success: `mode = *(*(in_EAX+0x56c)+4)`; `item->vtbl[+0x3ac](mode,1)`; `FUN_00941b20(...)`; return
- fail: string-pool → localize `"This equipment cannot be changed at this time."` → `FUN_007fdfb0` → return low-byte 0

### Body / ABI (read_memory + disassemble_function sealed)

| Item | Value |
|---|---|
| Entry @ `0x00861200` | `85 C9 74 24` — `TEST ECX,ECX; JZ fail` |
| Window slot | `8B 80 6C 05 00 00` — `MOV EAX,[EAX+0x56c]`; `TEST EAX,EAX; JZ fail` |
| Mode load | `8B 40 04` — `MOV EAX,[EAX+0x4]` |
| Resolve | `8B 11 6A 01 50 FF 92 AC 03 00 00` — `MOV EDX,[ECX]; PUSH 1; PUSH mode; CALL [EDX+0x3ac]` |
| Confirm equip | `50 B8 40 A8 D1 00 E8 ..` — `PUSH EAX; MOV EAX,DAT_00d1a840; CALL FUN_00941b20`; bare `RET` |
| Fail toast | `CALL FUN_007a69d0`; push `0,1,-1,-1,str@00a2cc74`; `CALL FUN_007a6de0`; `PUSH str; PUSH DAT_00d1a840; MOV EAX,0x4e24; CALL FUN_007fdfb0`; `32 C0 C3` — `XOR AL,AL; RET` |
| Body span | `0x00861200`–`0x00861254` exclusive (**84 B** / `0x54`); last insn `RET` @ `0x00861253`; pad `CC` |
| Hex (full) | `85c974248b806c05000085c0741a8b40048b116a0150ff92ac03000050b840a8d100e8f9080e00c3e8a357f4ff6a006a016aff6aff6874cca2008bc8e89f5bf4ff506840a8d100b8244e0000e85fcdf9ff32c0c3` |

### ABI (assembly-sealed)

| Slot | Role | Evidence |
|---|---|---|
| **ECX** | item\* (selected inventory object) | entry `TEST ECX,ECX`; parent site `MOV ECX,EBX` before CALL |
| **EAX** | inventory window\* (UI this) | `MOV EAX,[EAX+0x56c]`; parent site `MOV EAX,EDI` (EDI=window this) |
| stack formals | none at entry | bare dual-register entry |
| cleanup | bare **`RET`** (`C3`) | both exits |
| return | success: EAX from `FUN_00941b20`; fail: **AL=0** (`XOR AL,AL`) | |

### Stack balance note (success → `FUN_00941b20`)

`FUN_00941b20` is dualed as **EAX=client\***, stack **`item*, mode, confirmFlag`**, **`RET 0xC`** (3 dwords). This unit:

```text
PUSH 1                  ; confirmFlag
PUSH mode               ; from window+0x56c +4
CALL item->vtbl[+0x3ac] ; must be caller-clean on those 8B (else RET 0xC underflows)
PUSH result             ; item* for ConfirmEquip
MOV EAX, &DAT_00d1a840
CALL FUN_00941b20       ; RET 0xC balances 12B
```

Peer call sites (`0x00941ddc`, `0x00912627`) push the three stack args explicitly. Here mode/flag are pushed **before** the resolve virtual and left for `00941b20` after a **caller-clean** `vtbl[+0x3ac]`.

### Callers / xrefs

| Kind | Site | Notes |
|---|---|---|
| UNCONDITIONAL_CALL ×1 | `0x008615f4` in parent `FUN_008612b0` (`0x008612b0`–`0x00861675`) | `MOV ECX,EBX; MOV EAX,EDI; CALL 00861200` then window `vtbl[+0x33c](0)` close |
| Data xrefs | none observed | |

Parent (evidence only, not dualled): inventory window command — RTTI soft-cast selected child to **`CWndInventoryItem`**, branch on mode @ `this+0x56c` / `param_1[0x15b]+4` (1/3/4), special types 8 / `0x32`, else **this helper**.

### Callees

| Addr | Role |
|---|---|
| item `vtbl[+0x3ac]` | resolve related object (inventory root / item) with `(mode,1)` |
| `0x00941b20` | dualed **`Client_ConfirmEquipOrCustomizeItem`** |
| `0x007a69d0` | string-pool / locale object |
| `0x007a6de0` | localize C-string → UI string |
| `0x007fdfb0` | client toast/modal show (`EAX=0x4e24` chrome id) |

### Classification

worker / UI equip gate. Canonical dual name: **`UI_InventoryWindow_TryEquipOrLockedToast_Inferred`**. Terminal **false**.
