# Annotated low-level: FUN_0080ce50

| Field | Value |
|---|---|
| Stable ID | `aa_0080ce50` |
| VA | `0x0080ce50` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_0080ce50`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
void __fastcall FUN_0080ce50(int param_1)

{
  char cVar1;
  int in_EAX;
  
  if (*(int *)(param_1 + 0xe98) != 0) {
    cVar1 = FUN_00530550(*(undefined4 *)(in_EAX + 4));
    if (cVar1 != '\0') {
      cVar1 = (**(code **)(**(int **)(*(int *)(param_1 + 0x1034) + 0x514) + 0x3d8))();
      if (cVar1 != '\0') {
        FUN_0090cbc0();
        return;
      }
    }
  }
  return;
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.
