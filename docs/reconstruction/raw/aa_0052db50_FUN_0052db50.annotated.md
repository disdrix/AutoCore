# Annotated low-level: SkillTree_RemoveBySkillId_Inferred (`FUN_0052db50`)

| Field | Value |
|---|---|
| Stable ID | `aa_0052db50` |
| VA | `0x0052db50` |
| Body range | `0x0052db50`–`0x0052dc15` |
| Canonical name | `SkillTree_RemoveBySkillId_Inferred` |
| System | `skills-abilities` |
| Date | 2026-07-23 scaffold; dual seal 2026-07-29 (W20-T) |

## Machine-level notes

- Source: raw + live `decompile_function` + full-body `read_memory` (no `disassemble_bytes`).
- **ABI:** `__thiscall` — ECX = tree/host owner; stack `int nSkillId`; epilogue **`ret 4`** (`c2 04 00`).
- **Container:** dense vector of 12-byte elements: begin `this+0x550`, end `this+0x554`. Element layout: `+0 skill*`, `+4/+8` unknown siblings (copied wholesale on erase).
- **Match:** `skill->id @ +0x5fc == nSkillId`.
- **On match (asm sealed, decompiler incomplete):**
  1. MI adjust: `adj = this + *(*(this+4)+4)`.
  2. `host = adj + 4` → ECX for `Skill_SiblingCastClear`.
  3. TFID pack loaded from `adj+0x164..+0x170` ≡ **`host+0x160..+0x16c`**.
  4. `Skill_SiblingCastClear(host, skillId, tfid[4], flag=1)` — also clears OKToCastAgain.
  5. `operator_delete(skill*)` (returns — decompiler warning is **noise**).
  6. Erase current 12-byte slot: slide tail left by one element if not last; `end -= 0xC`.
  7. **Do not advance** cursor (next element now at same index); continue loop (may match again if duplicates).
- **Non-match:** `cursor += 0xC`.
- Sole caller family: `FUN_00623a20` — three sites with skillIds from context `+0x178/+0x17c/+0x180`; `this` = result of `*(ctx+0x18)->vtbl+0x210(0)`.

## Pseudocode (annotated, asm-corrected)

```c
// void __thiscall SkillTree_RemoveBySkillId_Inferred(void *pThis, int nSkillId)
void __thiscall FUN_0052db50(int param_1 /*this*/, int param_2 /*nSkillId*/)
{
  int *pBegin = *(int **)(param_1 + 0x550);
  int *pEnd   = *(int **)(param_1 + 0x554);
  int *pIt    = pBegin;

  if (pIt == pEnd) return;

  do {
    void *pSkill = (void *)*pIt;  // element+0
    if (*(int *)((char *)pSkill + 0x5fc) == param_2) {
      // MSVC multi-inheritance this-adjust toward cast-list host
      int adj = *(int *)(*(int *)(param_1 + 4) + 4) + param_1;
      void *pHost = (void *)(adj + 4);  // ECX for SiblingCastClear

      // TFID from host+0x160 == adj+0x164
      Skill_SiblingCastClear(
          pHost,
          param_2,
          *(uint32_t *)(adj + 0x164),
          *(uint32_t *)(adj + 0x168),
          *(uint32_t *)(adj + 0x16c),
          *(uint32_t *)(adj + 0x170),
          /*bAlsoOkToCastAgain=*/1);

      operator_delete(pSkill);

      // erase 12-byte element at pIt (vector compact)
      int *pNext = pIt + 3;           // +0xC
      int *pCurEnd = *(int **)(param_1 + 0x554);
      if (pNext != pCurEnd) {
        // slide [pNext, pCurEnd) down onto pIt (3 dwords per element)
        int *dst = pIt;
        int *src = pNext;
        do {
          dst[0] = src[0];
          dst[1] = src[1];
          dst[2] = src[2];
          dst += 3;
          src += 3;
        } while (src != pCurEnd);
      }
      *(int **)(param_1 + 0x554) = (int *)((char *)*(int **)(param_1 + 0x554) - 0xC);
      // keep pIt (element slid into place)
    } else {
      pIt = pIt + 3;  // +0xC
    }
  } while (pIt != *(int **)(param_1 + 0x554));
}
```

## Layout / offsets (sealed)

| Offset | Role |
|---|---|
| `this+0x4` | complete-object / adj thunk pointer (`*(*+4)` → delta) |
| `this+0x550` | vector begin (skill-tree entries, stride 12) |
| `this+0x554` | vector end |
| element `+0` | `skill*` |
| skill `+0x5fc` | skillId key |
| adj `+0x164` / host `+0x160` | TFID dword0 (after `host=adj+4`) |

## Open questions

1. Product/PDB name; class owning `+0x550` vector (skill-tree vs creature).
2. Meaning of element `+4` / `+8` (not read on match path; only bulk-moved).
3. Whether skillIds are unique in the vector (loop allows multi-match).
4. Runtime / bit-exact / differential — open.
