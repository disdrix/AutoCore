# Annotated low-level: EnvSlot_SetActiveBumpGen (`FUN_0074e200`)

| Field | Value |
|---|---|
| Stable ID | `aa_0074e200` |
| VA | `0x0074e200`–`0x0074e23c` (**61 B** / `0x3D`) |
| System | client / env-slot publish / related-object active flag |
| Date | 2026-07-29 (W31-Q re-verify) |
| Semantic name | `EnvSlot_SetActiveBumpGen` (**Inferred**) |

---

## Machine-level notes

- Live `decompile_function` ≡ 2026-07-23 raw CF.
- Full-body `read_memory` seals ABI (`RET 4`) and dual paths (enable gated vs clear).
- Caller bytes at `0x004cdbf0` prove thiscall ECX = slot (`DAT_00d1a54c` old / `*(env+4)` new), not free-function enable-only.
- Reject scaffold plate `Named_CalleeOf_Named_CalleeOf_Named_Could_not_get_Palantir_desk_0074e200`.

## Pseudocode (annotated)

```c
// ECX = env slot object (global DAT_00d1a54c / *(env+4))
// stack char enable; RET 4; void; leaf
void __thiscall EnvSlot_SetActiveBumpGen(void *slot, char enable)
{
  int related = *(int *)((char *)slot + 0x3c);   // related view/obj*
  if (related == 0) return;

  // Always bump generation/dirty counter on related
  // Gate: enable only sticks when global env flag DAT_00d1f048[+0x0f] is set
  if (enable != 0 && *(char *)(DAT_00d1f048 + 0x0f) != 0) {
    *(int *)(related + 0xdc) += 1;
    *(uint8_t *)(related + 0xd1) = 1;   // active
    return;
  }
  *(int *)(related + 0xdc) += 1;
  *(uint8_t *)(related + 0xd1) = 0;     // inactive (disable OR gate closed)
}
```

## Offsets (sealed)

| Site | Role | Conf |
|---|---|---|
| `slot+0x3c` | related object pointer (null-checked) | **High** |
| `related+0xdc` | uint32 generation / dirty counter (always ++ when related) | **High** |
| `related+0xd1` | uint8 active flag (1 or 0) | **High** |
| `DAT_00d1f048+0x0f` | global gate for active=1 | **High** (also mirrored from env+0x78 elsewhere) |

## Call graph

| Direction | Target | Notes |
|---|---|---|
| Caller | `FUN_004cda90` @ `0x004cdc0a` | disable: ECX=`DAT_00d1a54c`, push 0 |
| Caller | `FUN_004cda90` @ `0x004cdc17` | enable: ECX=`*(env+4)`, push 1 |
| Sibling | `FUN_00442b90` | combined disable/enable + `DAT_00d1a54c=` (not owned) |
| Global | `DAT_00d1a54c` | published slot pointer (writer is caller) |

## Open gaps

- Product/MSVC demangle for slot class and related object.
- English for gen counter vs dirty stamp at `+0xdc`.
- Full product role of `DAT_00d1f048` host (graphics/env state block).
- Runtime / bit-exact / differential.
