
# Raw capture: FUN_00517250

| Field | Value |
|---|---|
| **Stable ID** | `aa_00517250` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00517250` |
| **Canonical name** | `FUN_00517250` |
| **System** | client-fx / object primary FX ptr-vector single-remove |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite the 2026-07-23 raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile, 2026-07-23)

```c
void __thiscall FUN_00517250(int param_1,void *param_2)

{
  void *pvVar1;
  undefined4 *_Dst;
  
  if ((param_2 != (void *)0x0) && (*(char *)((int)param_2 + 9) == '\0')) {
    _Dst = *(undefined4 **)(param_1 + 0x124);
    if (_Dst != *(undefined4 **)(param_1 + 0x128)) {
      while (pvVar1 = (void *)*_Dst, pvVar1 != param_2) {
        _Dst = _Dst + 1;
        if (_Dst == *(undefined4 **)(param_1 + 0x128)) {
          return;
        }
      }
      memmove(_Dst,_Dst + 1,(*(int *)(param_1 + 0x128) - (int)(_Dst + 1) >> 2) * 4);
      *(int *)(param_1 + 0x128) = *(int *)(param_1 + 0x128) + -4;
      if (pvVar1 != (void *)0x0) {
        FUN_004b99c0();
                    /* WARNING: Subroutine does not return */
        operator_delete(pvVar1);
      }
    }
  }
  return;
}
```

---

## W29-N re-verify (2026-07-29) — append only

**Tools:** `decompile_function` + `analyze_function_complete` + `get_function_by_address` + `read_memory` + callers. **No** `disassemble_bytes`.

| Field | Live seal |
|---|---|
| Body | `0x00517250`–`0x005172c1` (**114 B / `0x72`** inclusive end); pad `CC` |
| Entry | `mov edx,[esp+4]` then `mov edi,ecx` — **thiscall** + one stack formal |
| Exit | **`ret 4`** (`C2 04 00`) on all paths |
| Live decompile | Outer CF ≡ 2026-07-23 raw; false-noreturn on `operator_delete` is cosmetic (epilogue still runs) |

### Byte spine (sealed)

1. Gate: `fx != NULL && *(char*)(fx+9) == 0`; else return.
2. Linear search primary ptr-vector `this+0x124`..`this+0x128` for pointer equality with `fx`.
3. Not found → return (no free).
4. Found → `memmove` erase slot (IAT `0x009c652c`); `end -= 4` (`+0x128`).
5. If entry non-null → `NDSpecialFX_TeardownCore_Inferred(fx)` (`FUN_004b99c0`) + `operator_delete(fx)`.
6. **No** `FUN_004b7150` / DeactivateClear / StopFinalize call.

### Callers (live)

| Caller | Role |
|---|---|
| `FUN_005179e0` Object_TeardownOwnedFxLists | sticky `+0x130` single-remove |
| `CVOGObject_SetInteractSpecialFX` (`0x005179a0`) | replace old interact FX |

Callees: `memmove`, `FUN_004b99c0`, `operator_delete`. Classification: **worker**.
