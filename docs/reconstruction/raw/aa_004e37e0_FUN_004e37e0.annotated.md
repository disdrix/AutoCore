# Annotated low-level: StdTree_InsertAndRebalance_Val16_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004e37e0` |
| VA | `0x004e37e0`–`0x004e39ca` inclusive (**491 B**) |
| System | skills-abilities / STL map-set (isnil@+0x21) |
| Date | 2026-08-05 (R12-012 dual) |
| Ghidra | `FUN_004e37e0` |
| Canonical | `StdTree_InsertAndRebalance_Val16_Isnil21_Inferred` |

## Machine-level notes

- Source: raw capture + R12-012 live re-verify (`decompile_function` ≡ raw CF).
- Prefer assembly immediates when decompiler rewrites comparisons (`0xffffffd < size` ≡ `cmp size, 0x0FFFFFFE` + `jnb` throw).
- Node layout sealed via buynode `FUN_004e2b80` (`operator_new(0x28)`).
- Parent dual Rrotate `0x005a27f0`; peer Lrotate `0x0050e9f0` (both isnil@+0x21).
- Sole parent shell: `FUN_004e48b0` (insert-or-find; 64-bit key compare on val dwords 0/1).

## Layout

| Offset | Field |
|-------:|-------|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x10 | Val16 payload (4×u32) |
| +0x20 | color (0=red, 1=black) |
| +0x21 | isnil |
| size | **0x28** |

Map shell: `+0x04` head sentinel, `+0x08` size.

## Control flow (annotated)

1. **SEH** frame setup.
2. **Size gate:** if `map->size >= 0x0FFFFFFE` → construct `"map/set<T> too long"` → length_error → `_CxxThrowException`.
3. **Buynode:** `n = FUN_004e2b80(head, where, head, value, color=0)`; `size++`.
4. **Link:**
   - `where == head` → first node: head.parent = head.left = head.right = n
   - `addLeft == 0` → `where->right = n`; maybe update head.right (rightmost)
   - else → `where->left = n`; maybe update head.left (leftmost)
5. **RB loop** while `n->parent->color == red (0)`:
   - uncle cases: recolor or L/R rotate (`0050e9f0` / `005a27f0`); one Lrotate path inlined
6. **Root black:** `head->parent->color = 1`; `*out = n`; restore ExceptionList; `ret 0x10`.

## Pseudocode (live decompile, R12-012)

```c
void __thiscall
FUN_004e37e0(int param_1,undefined4 *param_2,char param_3,undefined4 *param_4,undefined4 param_5)
{
  // param_1=map (ECX), param_2=out, param_3=addLeft, param_4=where, param_5=value*
  // size gate → throw "map/set<T> too long"
  // n = Buynode_Val16(head, where, head, value, red=0); size++
  // link empty | right | left
  // while parent red: recolor / Lrotate 0050e9f0 / Rrotate 005a27f0 / inlined L
  // root.color=black; *out=n; ret 0x10
}
```

## Call graph

| Dir | Target | Note |
|-----|--------|------|
| caller | `FUN_004e48b0` @ `0x004e48b0` | 2 sites; insert-or-find shell |
| grandparent | `CVOGReaction_UpdateRepairStationPosition` | product path |
| grandparent | `FUN_004da160` | residual |
| callee | `FUN_004e2b80` | Val16 buynode clone |
| callee | `FUN_0050e9f0` | Lrotate isnil21 |
| callee | `FUN_005a27f0` | Rrotate isnil21 (parent dual) |
| callee | length_error / `_CxxThrowException` | overflow |

## Rejected interpretations

- InsertHint / InsertOrFind (no key compare here)
- Freelist / CNDHash
- isnil@+0x29 / color@+0x28 family
- Merge with peer always-insert `0x00407200` (different buynode VA; keep separate clones)
