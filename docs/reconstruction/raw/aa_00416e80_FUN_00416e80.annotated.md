# Annotated low-level: FreelistSlabVector_Teardown_Inferred (`FUN_00416e80`)

| Field | Value |
|---|---|
| Stable ID | `aa_00416e80` |
| VA | `0x00416e80`–`0x00416ef6` (**119 B** / `0x77`) |
| System | container / CNDHash freelist slab pointer vector |
| Date | 2026-07-23 scaffold; **2026-08-05 WQ9I-A dual seal** |

## Machine-level notes

- Prefer assembly when decompiler conflicts: **operator_delete returns**; triad zero is **always** after optional free of the pointer array.
- **ABI:** stack `freelistSubobject*`; **`ret 4`** (`C2 04 00`). **Not** thiscall (twin `0x0059c8a0` is thiscall bare ret).
- Call sites push `this+0x20` (`LEA` then `PUSH` / decomp `param_1+8` dword → +0x20).
- IAT `_aligned_free` via `0x009c6578`.
- Does **not** walk freelist head at subobject +0x00 — nodes already reclaimed by DestroyBucketTable / FreeBuckets.

## Pseudocode (annotated)

```c
// ABI: stdcall; stack freelist subobject*; ret 4
void FreelistSlabVector_Teardown_Inferred(FreelistSlab *fl /*stack*/)
{
  // SEH LAB_009bc71b
  void **it  = fl->slabBegin; // +0x08
  void **end = fl->slabEnd;   // +0x0c
  while (it != end) {
    _aligned_free(*it);       // free each aligned slab
    ++it;                     // pointer stride 4
  }
  if (fl->slabBegin != nullptr)
    operator_delete(fl->slabBegin); // free the void* array buffer; RETURNS
  fl->slabBegin = fl->slabEnd = fl->slabCapEnd = nullptr; // +8/+0xc/+0x10 always
}
```

## Call graph

```text
CNDHash_Dtor_00a2c2b0 (FUN_00406420)          [WQ9H-D sealed]
  └─ PUSH this+0x20; CALL FUN_00416e80        [OWN WQ9I-A]
sibling hash dtors (00413850 / 004157f0 / 004169a0)
  └─ same freelist+0x20 teardown
FUN_00416c30  thin wrapper → FUN_00416e80(param)
SEH unwind stubs 009bcf40 / 009bdacb / 009beaed
```

## Twin

| VA | ABI | Name |
|----|-----|------|
| `0x0059c8a0` | thiscall bare ret | `FreelistSlabVector_dtor` (W24-M sealed) |
| `0x00416e80` | stdcall ret 4 | `FreelistSlabVector_Teardown_Inferred` (this dual) |

## Open residual

- Product / PDB English for freelist type.
- Full dual of thin wrapper `FUN_00416c30` and sibling hash dtors (not OWN).
- Runtime / bit-exact / differential.
