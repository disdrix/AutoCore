# Raw capture: FUN_004205e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004205e0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004205e0` |
| **Canonical name** | `FUN_004205e0` |
| **System** | COM / OLE DB accessor bind |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int FUN_004205e0(void)

{
  uint uVar1;
  int *piVar2;
  int iVar3;
  undefined4 *unaff_ESI;
  uint uVar4;
  
  uVar1 = *(uint *)(unaff_ESI[2] + 4);
  iVar3 = 0;
  uVar4 = 0;
  if (uVar1 != 0) {
    do {
      piVar2 = (int *)unaff_ESI[2];
      if ((*(char *)(*piVar2 + 4 + uVar4 * 8) != '\0') &&
         (iVar3 = (**(code **)(*(int *)*unaff_ESI + 0x10))
                            ((int *)*unaff_ESI,unaff_ESI[3],*(undefined4 *)(*piVar2 + uVar4 * 8),
                             piVar2[2]), iVar3 < 0)) {
        return iVar3;
      }
      uVar4 = uVar4 + 1;
    } while (uVar4 < uVar1);
  }
  return iVar3;
}
```

---

## Live re-verify (W16-F dual, 2026-07-29)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` only (no `disassemble_bytes`) |
| **Program** | `autoassault.exe` |
| **Body** | `0x004205e0`–`0x00420623` |
| **Epilogue** | `ret` (`c3`) — no stack args; context in **ESI** |
| **Live decompile** | ≡ raw body above (`unaff_ESI`, binding walk, vtbl+0x10) |

### Entry bytes (read_memory @ `0x004205e0`, first ops)

```text
8b 4e 08    mov ecx, [esi+8]     ; binding table ptr
53          push ebx
8b 59 04    mov ebx, [ecx+4]     ; count
...
33 c0       xor eax, eax         ; hr = 0
33 ff       xor edi, edi         ; index = 0
...
c3          ret
```

### Callers (live)

Only **`FUN_0041c000`** (`OleDb_RebindAccessor_Inferred`) — post-CreateAccessor validation gate (`hr==0 && FUN_004205e0()<0` → re-ReleaseAccessor).

### Callees (live)

None named (indirect call only: `*(**ESI)+0x10`).
