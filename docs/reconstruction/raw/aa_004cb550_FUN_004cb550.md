# Raw capture: FUN_004cb550

| Field | Value |
|---|---|
| **Stable ID** | `aa_004cb550` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004cb550` |
| **Canonical name** | `FUN_004cb550` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_004cb550(void *param_1)

{
  if (*(char *)((int)param_1 + 0x29) == '\0') {
    FUN_004cb550(*(undefined4 *)((int)param_1 + 8));
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return;
}
```

---

## Re-verify append — 2026-08-04 WQ9G-A OWN-ONLY

**Tools:** `batch_decompile` + `analyze_function_complete` + `read_memory` (64 B) + callers/xrefs + `get_assembly_context`. **No** `disassemble_bytes`. No Launcher.

| Claim | Result |
|---|---|
| Live decompile | **Incomplete** — right recurse + false noreturn only; **left-walk missing** (≡ raw 2026-07-23) |
| Bytes authority | Full free-subtree: right recurse, delete, **left spine iterate** |
| Body | `0x004cb550`–`0x004cb584` inclusive (**53 B** / `0x35`); terminal **`C2 04 00`**; pad `CC` |
| ABI | ECX threaded (`MOV EBX,ECX` / recursive `MOV ECX,EBX`); stack node*; **RET 4**; void |
| isnil | `+0x29` (`80 7F 29 00` / `80 7E 29 00`) |
| left / right | left `@+0` (`8B 36`); right `@+8` (`8B 46 08`) |
| External caller (1) | `StdTree_EraseRange_Isnil29_Inferred` (`0x004cbaa0`) full-clear @ `0x004cbabc` — `PUSH head->parent` |
| Self | recursive @ `0x004cb567` |
| Twin CF (do not merge) | `Map_FreeSubtree_Isnil29` @ `0x0051bbc0` (same 53 B shape; different delete rel / sole EraseRange pair) |
| Canonical name | `StdTree_FreeSubtree_Isnil29_Inferred` |

### Full body hex (53 B)

```
53 56 57 8B 7C 24 10 80 7F 29 00 8B D9 8B F7 75 1E
8B 46 08 50 8B CB E8 E4 FF FF FF 8B 36 57 E8 AE E2 FB FF
83 C4 04 80 7E 29 00 8B FE 74 E2 5F 5E 5B C2 04 00
```

### Byte-corrected CF (authoritative)

```
// ECX = tree_base (threaded; unused in body except recursive pass)
// stack = node*; RET 4; void
if (*(uint8_t*)(node+0x29) != 0) ret 4;   // isnil sentinel — do not free
for (;;) {
  FreeSubtree(tree_base, *(node+8));       // right recurse (self)
  left = *node;                            // left @+0
  operator_delete(node);                   // cdecl; ADD ESP,4 — NOT noreturn
  node = left;
  if (*(uint8_t*)(node+0x29) != 0) break;
}
ret 4;
```

### Call-site seal (EraseRange full clear @ `0x004cbabc`)

```
// ECX still = map shell (thiscall from 004cbaa0 entry; EDI copy)
// PUSH root = head->parent; CALL 004cb550
// then reset head L/P/R to head; size=0
```
