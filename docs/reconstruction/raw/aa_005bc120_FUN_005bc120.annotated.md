# Annotated low-level: Emitter_SpawnOnIntervalTick_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005bc120` |
| VA | `0x005bc120`–`0x005bc3bc` (**669 B**) |
| Canonical | `Emitter_SpawnOnIntervalTick_Inferred` |
| Ghidra | `FUN_005bc120` |
| System | client::fx / emitter spawn |
| Date | 2026-07-29 (W26-I OWN dual) |

## Machine-level notes

- Sole caller `FUN_005bc3c0` is the large emitter update (string `"Emitter found at 0,0,0: %d %I64d"`).
- ABI: `__thiscall`, ECX=emitter, two stack formals (`float *dt`, motion/ctx), **`ret 8`**.
- Early-out while interval countdown `+0x804` still positive after subtracting `*dt`.
- On fire: optional multi-group from vector stride `0xC`; clear ring flags; empty asset name → reschedule only.
- Spawn burst capped by short `+0x8E4` and active count vs cap `+0x97C` (0 → unlimited `0x7FFFFFFF`).
- Reschedule uses `CVOGReaction_RandomUnitScalar` ushort stream × range × **≈1/65535** + base.
- Nested callees not OWN: `FUN_005be390` (spawn create), list insert, motion setup (`ObjectMotion_IntegrateStep_Inferred` dualed as `aa_004b1100`).

## Pseudocode (annotated structural)

```c
// __thiscall RET 8; void
void Emitter_SpawnOnIntervalTick_Inferred(int *emitter /*ECX*/, float *dt, uint32_t motionCtx)
{
  // accumulate rate
  emitter[0x24a] = (int)((float)emitter[0x24c] * *dt + (float)emitter[0x24a]);

  float prev = (float)emitter[0x201];
  emitter[0x201] = (int)(prev - *dt);
  if (0.0f < prev - *dt)
    return;   // interval not expired

  int groups = 1;
  if ((*(uint8_t*)(emitter + 0x263) & 0x80) && emitter[3]) {
    // vector at emitter[3]: (end-begin)/0xC
    ...
  }

  // clear +0x2ee on circular nodes from emitter+5
  // empty name skip → LAB_reschedule

  for (int g = 0; g < groups; ++g) {
    int cap = emitter[0x25f] ? emitter[0x25f] : 0x7fffffff;
    if (cap == 0 || emitter[0x28a] < cap) {
      for (short i = 0; i < (short)emitter[0x239]; ++i) {
        if (cap <= emitter[0x28a]) break;
        // create / insert / motion / optional pair
      }
    }
  }

LAB_reschedule:
  // countdown = base + range * (ushort_rng / 65535)
  emitter[0x201] = (int)((float)u16 * (float)emitter[0x238] * DAT_00aaa638
                         + (float)emitter[0x237]);
}
```

## Related

| VA / symbol | Role |
|---|---|
| `FUN_005bc3c0` | parent emitter tick (sole caller) |
| `FUN_005be390` | spawn allocate/init |
| `FUN_004b1100` | `ObjectMotion_IntegrateStep_Inferred` |
| `DAT_00aaa638` | ≈ 1/65535 scale |

## Open questions

- Product English class/method name (Torque emitter type).
- Full layout of emitter object / vector at `this[3]`.
- Semantics of accumulator `+0x928` consumers.
- Runtime / bit-exact RNG schedule.
