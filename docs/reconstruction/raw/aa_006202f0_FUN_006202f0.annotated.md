# Annotated low-level: CVOGHBSkill_SummonSnoop_Ctor

| Field | Value |
|---|---|
| Stable ID | `aa_006202f0` |
| VA | `0x006202f0`–`0x00620323` (52 B) |
| Ghidra | `FUN_006202f0` |
| Canonical | `CVOGHBSkill_SummonSnoop_Ctor` |
| System | skills-abilities |
| Date | 2026-08-05 (R11-016 dual) |

## Machine-level notes

- **Role:** Subclass constructor for `CVOGHBSkill_SummonSnoop` (RTTI Confirmed).
- **Pattern:** `CVOGHBSkillBase_ctor` then override vtbl — no extra field stores (unlike Virus / SpawnEntities subclass ctors).
- **ABI:** `__thiscall` ECX=this; six stack formals; `ret 0x18`; EAX=this.
- **Object size:** `0x6c0` from sole caller `operator_new`.
- Prefer bytes when decompiler understates return type (`undefined` vs `this*`).

## Pseudocode (annotated)

```c
// CVOGHBSkill_SummonSnoop *this  (ECX)
// stack formals match CVOGHBSkillBase_ctor params 2..7
CVOGHBSkill_SummonSnoop* __thiscall
CVOGHBSkill_SummonSnoop_Ctor(
    CVOGHBSkill_SummonSnoop *this,
    void *pSourceObj,     // skill source / TFID seed
    void *pSkillBlob,     // skill runtime blob (0x18c dwords into this+0x24)
    uint32_t arg4,        // → this+0x688  (caller passes WorldCtx*)
    void *pTarget,        // ValidateTarget + AttachOwner subject (spawned entity)
    uint32_t *pTfid16,    // 16-byte TFID → this+0x690
    uint32_t arg7)        // → this+0x6a8 + FUN_0058ab60 heap
{
  CVOGHBSkillBase_ctor(this, pSourceObj, pSkillBlob, arg4, pTarget, pTfid16, arg7);
  *this = &PTR_FUN_009d156c;   // CVOGHBSkill_SummonSnoop vtbl
  return this;
}
```

## Control flow

```
mov esi, ecx
push 6 stack formals (order matches base)
call CVOGHBSkillBase_ctor   ; 0x005788d0
*this = PTR_FUN_009d156c    ; subclass vtbl
eax = esi
ret 0x18
```

## Callers / callees

| Dir | Symbol | VA |
|---|---|---|
| callee | `CVOGHBSkillBase_ctor` | `0x005788d0` |
| caller | `Skill_SpawnTemplateAndStartHB` | call @ `0x006208f4` |

## Open questions

1. Product design English for "SummonSnoop" beyond RTTI demangle (snoop summon skill HB).
2. Full dual of peer OnHB `FUN_00620340` / dtor path `FUN_00651190` (not OWN).
3. Runtime / bit-exact / differential.
