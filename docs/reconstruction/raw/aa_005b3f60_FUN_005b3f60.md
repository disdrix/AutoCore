# Raw capture: FUN_005b3f60

| Field | Value |
|---|---|
| **Stable ID** | `aa_005b3f60` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005b3f60` |
| **Canonical name** | `FUN_005b3f60` |
| **System** | host object / MSVC default ctor |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 * __fastcall FUN_005b3f60(undefined4 *param_1)

{
  undefined4 uVar1;
  
  *param_1 = &PTR_FUN_009d95b4;
  uVar1 = FUN_005b43b0();
  param_1[5] = uVar1;
  param_1[6] = 0;
  param_1[8] = 0;
  param_1[9] = 0;
  param_1[10] = 0;
  param_1[0xb] = 0;
  return param_1;
}
```

---

## Re-verify (W30-O OWN-ONLY dual) — 2026-07-29

| Field | Value |
|---|---|
| **Tools** | Ghidra `decompile_function` / `batch_decompile` + `analyze_function_complete` + `read_memory` + `get_function_by_address` + `get_bulk_xrefs` + `get_assembly_context`. **No** `disassemble_bytes`. |
| **Image** | `autoassault.exe` base `0x400000` |
| **Body** | `0x005b3f60`–`0x005b3f9a` exclusive (**58** B / `0x3A`); Ghidra body end inclusive `005b3f99`; pad `CC` after `C3` |
| **ABI** | `__fastcall` / thiscall-style: **ECX = this**; returns **this** in **EAX**; bare **`C3`** |
| **Bytes (authoritative)** | full hex below — LEA list base, CALL sentinel buy, head/size, `xorps`+`movss` zero four floats |
| **Decompile gap** | Shows `param_1[5]`/`[6]`/`[8..0xb]` without naming list base at `this+0x10` or that zeros are `movss` from cleared XMM0 (same zero bits) |
| **Vtbl** | `PTR_FUN_009d95b4[0]` = `0x005b44d0` (scalar-deleting dtor); next rdata is `"vs.1.1…"` shader text, not class plate |
| **Callees** | `FUN_005b43b0` (`operator_new(0x30)` self-linked list sentinel; ECX list base unused inside helper) |
| **Callers (2)** | `FUN_004cd8f0` @ `0x004cd93f` (`new(0x30)` then this, store host+`0xe4fc`, then separate `FUN_005b3fa0`); `FUN_004cd9a0` @ `0x004cd9f8` (same new/ctor/store; then `FUN_005b42f0`) |
| **Object size** | **0x30** (48 B) from both `PUSH 0x30` / `operator_new` call sites |
| **Semantic name** | `Obj_DefaultCtor_SetVtbl_EmptyListAt10_ZeroF4At20` (inferred; no product plate) |

### Full body hex (`read_memory` @ `0x005b3f60`, 58 B)

```
568bf1578d7e108bcfc706b4959d00e83c0400000f57c0894704c74708000000005ff30f114620f30f114624f30f114628f30f11462c8bc65ec3
```

### Byte-level algorithm

```
push esi / mov esi,ecx / push edi
lea edi,[esi+0x10]          ; embedded list object
mov ecx,edi
mov [esi],0x009d95b4        ; construction / final vtbl
call FUN_005b43b0           ; buy 0x30 sentinel, self-link
xorps xmm0,xmm0
mov [edi+4],eax             ; list head = sentinel  (this+0x14)
mov dword [edi+8],0         ; list size = 0         (this+0x18)
pop edi
movss [esi+0x20],xmm0       ; zero four dwords as floats
movss [esi+0x24],xmm0
movss [esi+0x28],xmm0
movss [esi+0x2c],xmm0
mov eax,esi / pop esi / ret
```
