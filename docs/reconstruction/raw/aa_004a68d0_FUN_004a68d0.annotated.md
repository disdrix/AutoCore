# Annotated low-level: BasicStringW_CopyBackward_0x1c (`FUN_004a68d0`)

| Field | Value |
|---|---|
| Stable ID | `aa_004a68d0` |
| VA | `0x004a68d0`–`0x004a6902` (**51 B**) |
| System | MSVC `basic_string<wchar_t>` reverse range assign (`copy_backward`) |
| Date | 2026-07-29 (W36-T dual seal) |
| Canonical | `BasicStringW_CopyBackward_0x1c` (**Inferred**) |

## Machine-level notes

- **cdecl** 3 stack args: `first*`, `last*`, `dest_end*`; bare **`RET`** (`C3`); returns final dest in **EAX**.
- EBX = first (fixed stop); ESI = last cursor (walks back); EDI = dest_end cursor (walks back).
- Per element: `SUB ESI/EDI, 0x1C`; `PUSH ESI; MOV ECX, EDI; CALL [0x009c63bc]`.
- Empty (`first == last`) returns `dest_end` with no stores.
- Sole parent: `StdVector_InsertN_BasicStringW` (`0x004a7ad0`) mid-insert shift @ `0x004a7dbf`, then fill via `004a68a0`.
- Char twin: `BasicString_CopyBackward_0x1c` (`0x00431790`) — same 51 B shape, IAT `[0x009c63c4]`.

## Pseudocode (annotated)

```c
// cdecl; plain RET; returns final dest*
BasicStringW_0x1c* BasicStringW_CopyBackward_0x1c(
    BasicStringW_0x1c* first,      // stack [esp+4] → EBX
    BasicStringW_0x1c* last,       // stack [esp+8] → ESI
    BasicStringW_0x1c* dest_end)   // stack [esp+C] → EDI
{
  if (first == last) {
    return dest_end;
  }
  do {
    last = (BasicStringW_0x1c*)((char*)last - 0x1C);
    dest_end = (BasicStringW_0x1c*)((char*)dest_end - 0x1C);
    // IAT [0x009c63bc] → 0x006ec314 basic_string<wchar_t>::operator=
    BasicStringW_Assign(dest_end, last);
  } while (last != first);
  return dest_end;
}
```

## Open questions

- Product/MSVC demangle plate.
- Runtime / bit-exact under overlap edges (parent mid-insert only path known).
