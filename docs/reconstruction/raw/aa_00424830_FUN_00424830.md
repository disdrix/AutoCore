# Raw capture: FUN_00424830

| Field | Value |
|---|---|
| **Stable ID** | `aa_00424830` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00424830` |
| **Canonical name** | `FUN_00424830` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
uint FUN_00424830(void)

{
  int in_EAX;
  uint uVar1;
  
  if (*(int *)(in_EAX + 0x9c4) == 0) {
    FUN_004248c0();
  }
  *(int *)(in_EAX + 0x9c4) = *(int *)(in_EAX + 0x9c4) + -1;
  uVar1 = **(uint **)(in_EAX + 0x9c0);
  *(uint **)(in_EAX + 0x9c0) = *(uint **)(in_EAX + 0x9c0) + 1;
  uVar1 = uVar1 ^ uVar1 >> 0xb;
  uVar1 = uVar1 ^ (uVar1 & 0xff3a58ad) << 7;
  uVar1 = uVar1 ^ (uVar1 & 0xffffdf8c) << 0xf;
  return uVar1 >> 0x12 ^ uVar1;
}
```

---

## Live re-verify append (2026-07-29 W20-C)

| Check | Result |
|---|---|
| `decompile_function` @ `0x00424830` | ≡ raw (remaining@+0x9c4, twist if 0, --remaining, *cursor++, temper cascade) |
| `get_function_by_address` | Entry `00424830`; Ghidra body end `00424881` |
| Byte-accurate exclusive end | `0x00424882` (**82** B) — ends `5e c3` |
| `read_memory` length 128 | full body hex below; trailing `cc` pad |
| ABI | **EAX = Mt19937State*** on entry; body copies to **ESI**; bare **`ret`** (`c3`); returns tempered **uint32 in EAX** |
| Callees | `Rng_Mt19937_Twist` `FUN_004248c0` @ `0x004248c0` only (when remaining==0) |
| Xrefs | **34** UNCONDITIONAL_CALL sites (sound, humor, UI, table extract, etc.) |
| Sibling layout | cursor `+0x9c0`, remaining `+0x9c4`, `mt[624]` @ +0; object size **0x9c8** |

**Body hex (0x52 B, exclusive end `0x00424882`):**

```
568bf083bec4090000007505e87f0000008b86c00900008386c4090000ff8b0883c004
8986c00900008bc1c1e80b33c88bd181e2ad583affc1e20733ca8bc1258cdfffffc1e0
0f33c88bc1c1e81233c15ec3
```

**Byte seal (temper):**
```
push esi
mov  esi, eax
cmp  dword [esi+0x9c4], 0
jnz  extract
call 0x004248c0          ; twist (ESI state already set)
mov  eax, [esi+0x9c0]
add  dword [esi+0x9c4], -1
mov  ecx, [eax]
add  eax, 4
mov  [esi+0x9c0], eax
; temper:
;   y ^= y >> 11
;   y ^= (y & 0xff3a58ad) << 7
;   y ^= (y & 0xffffdf8c) << 15
;   return (y >> 18) ^ y
pop  esi
ret
```

**Temper mask equivalence (classic MT19937):**
| Form in body | Classic textbook form |
|---|---|
| `(y & 0xff3a58ad) << 7` | `(y << 7) & 0x9d2c5680` (high mask bits shift out) |
| `(y & 0xffffdf8c) << 15` | `(y << 15) & 0xefc60000` |

Shifts **11 / 7 / 15 / 18** match standard MT19937 temper. Not a custom PRNG.
