# Annotated low-level: CVOGMenu_ScalarDeletingDtor (FUN_004166d0)

| Field | Value |
|---|---|
| Stable ID | `aa_004166d0` |
| VA | `0x004166d0`–`0x004166ed` inclusive (**30 B** / `0x1E`) |
| System | skills-abilities (partition) / MSVC scalar-deleting destructor — **CVOGMenu** |
| Date | 2026-08-05 (MEGA-138 re-verify over 2026-07-23 scaffold) |
| Canonical name | `CVOGMenu_ScalarDeletingDtor` (RTTI product demangle sealed) |
| Ghidra name | `FUN_004166d0` |

## Machine-level notes

- Source: live `decompile_function` + `analyze_function_complete` + full-body `read_memory` + xrefs + RTTI COL/type_info.
- Classic MSVC **scalar-deleting destructor** (vtbl[0] of `PTR_FUN_00a72f6c`):
  1. Always run complete dtor `FUN_0082d540` / `CVOGMenu_CompleteDtor` (install vtbl, menu hash teardown, chain `CNDUIDialog_CompleteDtor`).
  2. If `(flags & 1)` → `operator_delete(this)`.
  3. Return `this`.
- ABI: **ECX=this**; stack **flags**; **`RET 4`**; returns this in EAX.
- DATA xref only: dword at `0x00a72f6c` = `0x004166d0` (vtbl[0]).
- Decompiler “operator_delete does not return” is **false**; epilogue always `MOV EAX,ESI; POP ESI; RET 4`. Bytes include `ADD ESP,4` after delete (disasm listing may drop it under noreturn).
- Product class sealed by RTTI `.?AVCVOGMenu@@` @ `0x00afe39c` (same chain dualed on complete `aa_0082d540`).
- Complete sibling dualed WQ9K-G as `CVOGMenu_CompleteDtor` — does **not** free `this`.
- **Not** vector-deleting (no count arg, only bit0 tested).

## Pseudocode (annotated)

```c
// ECX = this; stack flags; RET 4; returns this*
void* __thiscall CVOGMenu_ScalarDeletingDtor(void* self, uint8_t flags)
{
  CVOGMenu_CompleteDtor(self);  // FUN_0082d540: *self = &PTR_FUN_00a72f6c; hash; CNDUIDialog
  if ((flags & 1u) != 0u) {
    operator_delete(self);
  }
  return self;
}
```

## Byte map (30 B)

```
004166d0  56                 push esi
004166d1  8B F1              mov  esi, ecx              ; this
004166d3  E8 68 6E 41 00     call FUN_0082d540          ; CVOGMenu_CompleteDtor
004166d8  F6 44 24 08 01     test byte [esp+8], 1       ; flags bit0
004166dd  74 09              jz   skip_delete
004166df  56                 push esi
004166e0  E8 3D 31 07 00     call operator_delete       ; @ 0x00489822
004166e5  83 C4 04           add  esp, 4
skip_delete:
004166e8  8B C6              mov  eax, esi
004166ea  5E                 pop  esi
004166eb  C2 04 00           ret  4
```

Hex: `568bf1e8686e4100f644240801740956e83d31070083c4048bc65ec20400`

## Vtbl `PTR_FUN_00a72f6c` @ `0x00a72f6c`

| Slot | VA | Notes |
|---|---|---|
| [0] | `0x004166d0` | this function (scalar-deleting) |
| [1] | `0x00790cc0` | shared family (open) |
| [2] | `0x007a7de0` | shared family (open) |
| [3] | `0x007567e0` | shared family (open) |

## RTTI

| Item | Value |
|---|---|
| COL ptr @ `0x00a72f68` | `0x00ab6214` |
| type_info | `0x00afe39c` |
| Mangled | `.?AVCVOGMenu@@` |
| Demangled | **CVOGMenu** |

## Open questions

- Vtbl slots [1+] English (not owned; shared with CNDUIDialog family).
- Runtime / bit-exact / differential — open (no Launcher).
- Hash field map dual of `FUN_0082ce20` residual under complete (not this unit).
