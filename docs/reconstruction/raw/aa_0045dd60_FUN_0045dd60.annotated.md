# Annotated low-level: FUN_0045dd60 → Anm_KestChunk_ReadF32Array_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0045dd60` |
| VA | `0x0045dd60`–`0x0045de07` (168 B / 0xA8) |
| System | `sto-chunk` / animation track-master residual |
| Date | 2026-08-04 (W37-P dual) |
| Named | `Anm_KestChunk_ReadF32Array_Inferred` |

## Machine-level notes

- **Bytes win** over decompiler void formals: **ECX=elem_count**, **EDI=reader**, stack **dest***, bare **RET** (caller `ADD ESP,4`).
- SEH + `stoChunkReader_EnterChunkScope` (this=scope frame; reader=EDI).
- Tag gate: scope tag dword **`KEST`** `0x4B455354`.
- Success: `stoChunkReader_readF32Array` with **EAX = count*7** (Pod28 = 7×f32), **ECX=dest**, stack reader.
- Leave: `FUN_00769e40` when okFlag; fail returns `0xFFFFFFFF`.
- Sibling shape: `FUN_0045d810` (tag `0x4B535254`) and other KEYR-style readers — same Enter/tag/readF32Array/Leave skeleton.
- Sole caller: `AnmTrackMaster_Unserialize_Inferred` (`0x00459750`) after Pod28 vector resize @ `this+0x1C`.

## Sealed pseudocode (byte-corrected)

```c
/* ECX=elem_count; EDI=stoChunkReader*; stack dest float*; bare RET; caller cleans 4 */
uint32_t Anm_KestChunk_ReadF32Array_Inferred(int elem_count /*ECX*/, float *dest /*stack*/)
{
  /* EDI = reader (register formal) */
  ChunkScope scope;
  stoChunkReader_EnterChunkScope(&scope, reader);
  if (scope.tag == 0x4B455354 /* KEST */) {
    uint32_t st = stoChunkReader_readF32Array(dest, reader, elem_count * 7);
    if (scope.okFlag) FUN_00769e40(/* leave scope */);
    return st;
  }
  if (scope.okFlag) FUN_00769e40();
  return 0xFFFFFFFF;
}
```

## Open questions

- Product English for fourCC **KEST** (key-est? track keyset?).
- Whether okFlag leave ABI formals need full dual of `FUN_00769e40` (already dual-sealed leave helper elsewhere).
- Runtime / bit-exact.
