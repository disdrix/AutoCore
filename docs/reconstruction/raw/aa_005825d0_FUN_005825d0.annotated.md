# Annotated low-level: FUN_005825d0 → Object_UnloadGraphics_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005825d0` |
| VA | `0x005825d0`–`0x00582664` exclusive (**148 B**) |
| Canonical | `Object_UnloadGraphics_Inferred` |
| Ghidra | `FUN_005825d0` |
| System | object / graphics unload / mission-state FX |
| Dual | W31-T 2026-07-29 |

---

## Purpose

Core **unload-graphics** worker for an object MI subobject. Sole CODE caller `FUN_005d4fd0` logs `"UnloadGraphics on %s (%d)"` then invokes this body. Also installed in **14** DATA vtables.

Sequence (bytes-sealed):

1. Remove self from tracking lists: `FUN_004bfd10(manager, this)` where manager this = `*(*(object_host_via_MI+0xA8)+0xE914)`.
2. Clear flag byte `this+0x0F = 0`.
3. Object root `vtbl+0x144()` (destroy/teardown graphics slot family — same offset used by RemoveObject destroy path).
4. `Object_SetMissionStateFx_Inferred(object_root, 3)` (W30-H sealed; state 3 = audio-only / spawn-family).
5. Optional host notify: if `*(host+0xE500) != 0`, call `notify->vtbl[0](object_root)` (host from MI `+0xA8`).
6. Object root `vtbl+0xFC()`.
7. Self `vtbl+0x04()`; self `vtbl+0x18(0)`; self `vtbl+0x18(1)`.

---

## Signature (machine-sealed)

```c
// __thiscall ECX = MI this (param_1); bare RET; void
void __thiscall Object_UnloadGraphics_Inferred(void *mi_this /*ECX*/);
```

---

## Control flow (annotated)

```
ESI = this (MI)
manager = *(*( *(this+4)+4 + this + 0xA8 ) + 0xE914)
FUN_004bfd10(manager /*ECX*/, this /*stack*/)   // decompiler wrongly shows FUN_004bfd10(this) alone
byte[this+0x0F] = 0
root = this + *(*(this+4)+4) + 4
root->vtbl+0x144()
Object_SetMissionStateFx(root, 3)
host = *( *(this+4)+4 + this + 0xA8 )
notify = *(host + 0xE500)
if notify: (*notify)(root)                      // vtbl[0]
root->vtbl+0xFC()
this->vtbl+0x04()
this->vtbl+0x18(0)
this->vtbl+0x18(1)
ret
```

---

## Offsets

| Item | Role |
|---|---|
| `this+0x04` | vbtable / MI meta |
| `this+0x0F` | flag byte cleared on unload |
| MI `+0xA8` | host / reaction-like object with `+0xE500` notify and `+0xE914` manager |
| object `vtbl+0x144` | graphics destroy / unload slot |
| object `vtbl+0xFC` | post-unload virtual |
| self `vtbl+0x04`, `+0x18` | local MI interface teardown (args 0 then 1) |

---

## Confidence

| Claim | Level |
|---|---|
| Body 148 B + bare RET | **High** |
| UnloadGraphics role via sole CODE caller string | **High** |
| SetMissionStateFx(3) | **High** (W30-H callee) |
| First callee this residual vs decompile | **High** (bytes) |
| Product class / vfunc English | **Inferred** |
| FUN_004bfd10 product name | **Open** (not OWN) |
| Runtime / differential | Open |
