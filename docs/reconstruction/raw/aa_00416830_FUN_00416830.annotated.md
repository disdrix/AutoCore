# Annotated low-level: FUN_00416830 → Class_00a733ec_Factory_WithFreeCallback_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00416830` |
| VA | `0x00416830`–`0x00416892` inclusive (**99 B** / `0x63`) |
| System | Class_00a733ec heap factory (free-callback = LAB_00573240) |
| Date | 2026-08-05 (WQ9K-H dual seal) |
| Tools | `batch_decompile` + `analyze_function_complete` + `disassemble_function` + `read_memory` + bulk xrefs. **No** `disassemble_bytes` |

---

## Machine-level notes

- **ABI:** stack `context` (param_1); returns pointer in EAX; **`RET 4`**. Not thiscall (no ECX this).
- **SEH:** standard MSVC `operator_new` EH frame (`LAB_009ab4eb`); EH state −1 → 0 around new → −1 before exit.
- **Object size:** **0x14** sealed by `PUSH 0x14` before `operator_new`.
- **Layout written:**
  - `+0x00` vtbl `PTR_FUN_00a733ec` (slot0 = scalar dtor `0x00416f00`, dualed WQ9J-E)
  - `+0x04` context = stack arg
  - `+0x08` zero
  - `+0x0c` zero
  - `+0x10` free callback = `LAB_00573240`
- **Free callback** (read_memory @ `0x00573240`): cdecl `void free_cb(void *context)`:
  - if context null → return
  - else `context->vtbl[0](flags=1)` — scalar-deleting destroy of owned context
- **Twin:** `FUN_004168c0` identical except `+0x10 = nullptr` (no free).
- **Callers:** Ghidra reports **0** CALL xrefs to this VA; may be unreferenced / reached only by data-driven dispatch not recovered. Body still fully sealeable from bytes.
- **Adjacent rdata:** UTF-16 `"UNI_BUTTONS"` sits after single-slot vtbl at `0x00a733f0` — **not** sealed class English (same residual as WQ9J-E dtor dual).

---

## Control flow

```text
SEH install
p = operator_new(0x14)
if p == null: eax = 0
else:
  p->vtbl = PTR_FUN_00a733ec
  p->context = arg
  p->f08 = 0
  p->f0c = 0
  p->free_cb = LAB_00573240
SEH uninstall
return eax   // RET 4
```

---

## Open questions

- Product / RTTI demangle for `PTR_FUN_00a733ec`.
- Why CALL graph is empty (dead code vs missing xrefs).
- Context type expected by free-callback scalar delete.
- Runtime / bit-exact / differential.
