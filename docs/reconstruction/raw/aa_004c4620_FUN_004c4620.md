# Raw capture: FUN_004c4620

| Field | Value |
|---|---|
| **Stable ID** | `aa_004c4620` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004c4620` |
| **Canonical name** | `FUN_004c4620` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __fastcall FUN_004c4620(int param_1)



{

  if (((*(uint *)(param_1 + 0x108) & *(uint *)(param_1 + 0x10c)) == 0xffffffff) &&

     ((char)*(undefined4 *)(param_1 + 0x110) == '\0')) {

    return 0xb;

  }

  return 10;

}
```

---

## Live re-decompile (2026-07-29 W27-G)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` + `analyze_function_complete` |
| **Program** | `autoassault.exe` base `0x400000` |
| **Body** | `0x004c4620`–`0x004c4658` (**57 B** inclusive last `RET`; pad `CC` after) |
| **Epilogue** | `B8 imm32; 83 C4 10; C3` (`MOV EAX,mode; ADD ESP,0x10; RET`) — **thiscall**, **0** stack formals |
| **Classification** | leaf (0 callees) |
| **Callees** | none |
| **Callers** | none as CALL (DATA/vtable only) |
| **Xrefs (DATA)** | `0x009cbe8c` (vtable slot) |
| **Integrity** | Live decompile ≡ 2026-07-23 raw body (identical CF) |

### Machine hex (full body, 57 B)

```
83ec105681c1080100008bf18b068b4e048b56088b760c23c183f8ff897424105e750484d27409b80a00000083c410c3b80b00000083c410c3
```

### Sealed ABI

```
// thiscall; ECX = object; no stack formals; returns uint32 mode in EAX; RET 0
// uint32_t Object_PhysModeFromStoredTFID(Object* this)
// tfid = this+0x108
// if (tfid.lo & tfid.hi) == 0xFFFFFFFF && (char)tfid.word2 == 0:
//     return 0xB   // unassigned / invalid-TFID phys mode
// else:
//     return 10    // assigned-TFID phys mode
```

### Predicate family

Identical invalid-TFID test as:

| Sibling | VA | Use of predicate |
|---|---|---|
| `FUN_004c4440` | `0x004c4440` | skip resolve when invalid |
| `FUN_004c4880` | `0x004c4880` | write mode to phys+0x28 |

### Byte-level notes

- Prologue: `SUB ESP,0x10; PUSH ESI; ADD ECX,0x108; MOV ESI,ECX` — points ESI at TFID block.
- Loads lo/hi/w2/w3; `AND EAX,ECX; CMP EAX,-1`.
- Branch: `JNE → return 10`; else `TEST DL,DL; JE → return 0xB`; else fallthrough return 10.
- Pure leaf — no CALL, no memory write.

### Live decompile (identical to raw)

```c
undefined4 __fastcall FUN_004c4620(int param_1)

{
  if (((*(uint *)(param_1 + 0x108) & *(uint *)(param_1 + 0x10c)) == 0xffffffff) &&
     ((char)*(undefined4 *)(param_1 + 0x110) == '\0')) {
    return 0xb;
  }
  return 10;
}
```

