# Raw capture: FUN_00401c30

| Field | Value |
|---|---|
| **Stable ID** | `aa_00401c30` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00401c30` |
| **Canonical name** | `FUN_00401c30` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int FUN_00401c30(void)

{
  int in_EAX;
  
  if (*(int *)(in_EAX + 4) == 0) {
    return 0;
  }
  return *(int *)(in_EAX + 8) - *(int *)(in_EAX + 4) >> 2;
}
```

---

## MEGA-082 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-082** |
| **Tools** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` / `get_function_xrefs` + `get_assembly_context` |
| **Forbidden tools** | `disassemble_bytes` (not used); Launcher (not used) |
| **Image** | `autoassault.exe` base `0x400000` |

### Live decompile (≡ scaffold CF)

```c
int FUN_00401c30(void)
{
  int in_EAX;
  if (*(int *)(in_EAX + 4) == 0) {
    return 0;
  }
  return *(int *)(in_EAX + 8) - *(int *)(in_EAX + 4) >> 2;
}
```

### Disassembly (body `0x00401c30`–`0x00401c42` inclusive, **19 B** / `0x13`)

```text
00401c30  MOV  ECX, dword ptr [EAX + 0x4]
00401c33  TEST ECX, ECX
00401c35  JNZ  0x00401c3a
00401c37  XOR  EAX, EAX
00401c39  RET
00401c3a  MOV  EAX, dword ptr [EAX + 0x8]
00401c3d  SUB  EAX, ECX
00401c3f  SAR  EAX, 0x2
00401c42  RET
; pad CC… before next function
```

### Bytes (`read_memory` 32 B @ `0x00401c30`)

```text
8B 48 04 85 C9 75 03 33 C0 C3 8B 40 08 2B C1 C1 F8 02 C3 CC CC …
```

### ABI seal

| Item | Value | Evidence |
|---|---|---|
| Container arg | **EAX** (not ECX) | `MOV ECX,[EAX+4]` / call sites `MOV EAX,<vec>; CALL 00401c30` |
| Return | size in **EAX** | `XOR EAX,EAX` / `SAR EAX,2` then `RET` |
| Cleanup | bare **`RET`** (`C3`) ×2 exits | no stack args |
| Layout | begin **@+4**, end **@+8** | capacity not read |
| Element stride | **4** (`SAR 2`) | dword / pointer vector size |
| Null begin | → **0** | `TEST ECX; JNZ; XOR EAX,EAX; RET` |
| Callees | **none** (leaf) | analyze_function_complete |
| Classification | leaf | analyze_function_complete |

### Twin (distinct ABI, same formula)

| VA | Name | ABI |
|---|---|---|
| `0x004024b0` | dualed `StdVector_DwordSize` | **ECX** = vec (`MOV EDX,[ECX+4]`) |
| `0x00401c30` | this unit | **EAX** = vec (`MOV ECX,[EAX+4]`) |

### Xrefs (11 UNCONDITIONAL_CALL)

| Site | Enclosing / context | Pre-call |
|---|---|---|
| `008b6f5e`, `008b6f7b` | `FUN_008b6eb0` | `MOV EAX,EBX` (vec = EBX after `ADD EBX,4` from host+0x524) |
| `0090593e`, `009059de`, `00905a2b` | orphan region near UI host | `MOV EAX,EBX` / `MOV EAX,EDI` |
| `008a463c`, `008a4674`, `008a4781`, `008a47b4` | orphan region; vec `LEA EBP,[EDI+0x5a0]` | `MOV EAX,EBP` loop bounds |
| `008d516f`, `008d518a` | `FUN_008d4910` (plate parent of `"Your Vehicle Name Is Invalid!  Please Choose Another!"` @ `0x00a3fba4`) case `0x9c45` | `LEA ESI,[EDI+0x8dc]; MOV EAX,ESI` |

### Naming

- Retire scaffold `Named_CalleeOf_Drive_Your_Vehicle_Name_Is_Invalid_Please_Choose_00401c30` (xref-seed only; multi-domain leaf).
- Promote **`StdVector_DwordSize_EaxVec_Inferred`** (structural; product element English open → `_Inferred`).

### Verdict note

CF/ABI/formula sealed. Terminal **false** (no runtime Confirmed).
