# Raw capture: FUN_007fb640

| Field | Value |
|---|---|
| **Stable ID** | `aa_007fb640` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x007fb640` |
| **Canonical name** | `FUN_007fb640` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_007fb640(void)

{
  char cVar1;
  int unaff_ESI;
  
  if (*(int *)(unaff_ESI + 0x1120) != 0) {
    cVar1 = (**(code **)(**(int **)(unaff_ESI + 0x1120) + 0x3d8))();
    if (cVar1 != '\0') {
      if (*(int *)(*(int *)(unaff_ESI + 0x1120) + 0x2b0) != 0) {
        (**(code **)(**(int **)(unaff_ESI + 0xf38) + 0xb0))(*(int *)(unaff_ESI + 0x1120));
      }
                    /* WARNING: Could not recover jumptable at 0x007fb681. Too many branches */
                    /* WARNING: Treating indirect jump as call */
      (**(code **)(**(int **)(unaff_ESI + 0x1120) + 0x440))();
      return;
    }
  }
  return;
}
```

---

## 2026-07-29 — dual OWN-ONLY append (asm-corrected ABI + CF)

| Field | Value |
|---|---|
| **Canonical name (claim)** | `Client_DismissProgressBar_Inferred` |
| **Tool** | Ghidra MCP `decompile_function` + `disassemble_function` + `read_memory` + xrefs |
| **Dual** | `reviews/A_aa_007fb640_*` + `reviews/B_aa_007fb640_*` |

### Prologue / ABI (disassemble_function)

```
007fb640  CMP  dword ptr [ESI + 0x1120], 0
007fb647  JZ   0x007fb687              ; no-op RET
007fb649  MOV  ECX, [ESI + 0x1120]      ; bar
007fb651  CALL dword ptr [EAX + 0x3d8]  ; bool probe → AL
007fb657  TEST AL, AL
007fb659  JZ   0x007fb687
007fb661  CMP  dword ptr [EAX + 0x2b0], 0
007fb668  JZ   0x007fb679              ; skip detach
007fb66a  MOV  ECX, [ESI + 0xf38]
007fb672  PUSH EAX                     ; bar
007fb673  CALL dword ptr [EDX + 0xb0]   ; host detach
007fb679  MOV  ECX, [ESI + 0x1120]
007fb681  JMP  dword ptr [EAX + 0x440]  ; tail-call dismiss (NOT jumptable)
007fb687  RET                          ; plain RET — 0 stack args
```

- **ESI = Client\*** (decompiler `unaff_ESI`); callers set ESI before `CALL` (e.g. ShowTimedProgressBar `MOV ESI,EAX`).
- Decompiler warning “Could not recover jumptable at 0x007fb681” is **misleading**: single tail `JMP [vtbl+0x440]`.

### `read_memory` head (80B)

`83be2011000000…ffa040040000c3` — seals `CMP [ESI+0x1120]`, `CALL [+0x3d8]`, `JMP [+0x440]`, `RET`.

### Callers (5 UNCONDITIONAL_CALL)

| Site | Parent |
|---|---|
| `0x007fb693` | `FUN_007fb690` ShowTimedProgressBar (always first) |
| `0x0081130e` | `Client_RecvSkillStatusEffect` |
| `0x007fea0f` | `FUN_007fe8d0` UseItem UI path |
| `0x0080bea8` | `FUN_0080be70` switch case 3 |
| `0x0091ee11` | `FUN_0091edd0` “Contact Interrupted!” |

### Contract (sealed High)

```
if client+0x1120 == 0: return
if !bar.vtbl+0x3d8(): return
if bar+0x2b0: (client+0xf38).vtbl+0xb0(bar)
tail → bar.vtbl+0x440
```
