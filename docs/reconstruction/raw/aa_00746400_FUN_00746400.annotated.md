# Annotated low-level: FUN_00746400 → StdVector_FindOrPushBackDword_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00746400` |
| VA | `0x00746400`–`0x00746437` (**56 B**) |
| System | container / std::vector-like dword registry |
| Date | 2026-08-04 (W37-V dual seal) |
| Canonical | `StdVector_FindOrPushBackDword_Inferred` (**Inferred**) |

## Machine-level notes

- **ABI:** `__thiscall` — **ECX = vector host**; stack **dword value**; **`RET 4`**.
- **Layout (shared with `FUN_004406e0`):** `+0x04` begin*, `+0x08` end*, `+0x0c` capacityEnd* (touched only in push callee).
- **Return:** **1** = value already present; **0** = appended (not an error — callers treat `< 0` as fail only for OOM wrappers).
- **Decompiler ≡ bytes** for scan/compare/return; push path sealed by `LEA ESI,&arg` + `EAX=this` before `FUN_004406e0` (customcc EAX/ESI).
- Prefer bytes when Ghidra signature says `undefined FUN_00746400(void)` — live decompile still shows thiscall correctly.
- Common host: global **`DAT_00d1f620`** (water/distort phase token registry; also bootstrap registrants).

## Pseudocode (annotated)

```c
// __thiscall ECX=vec; stack value; RET 4
// return 1 = found, 0 = pushed
uint32_t StdVector_FindOrPushBackDword_Inferred(VecDword *vec /*ECX*/, uint32_t value)
{
  uint32_t *cursor = (uint32_t *)vec->begin; // +4
  uint32_t *end    = (uint32_t *)vec->end;   // +8

  while (cursor != end) {
    if (*cursor == value) {
      if (cursor != end)   // bytes keep edge check; normal hit falls through
        return 1;
      goto append;
    }
    cursor++;
  }
append:
  // EAX still = vec; ESI = &value
  StdVector_PushBackDword_Inferred(/*EAX*/ vec, /*ESI*/ &value); // FUN_004406e0
  return 0;
}
```

## Callers (context)

| Caller | Role |
|---|---|
| `WaterPhase_RegisterToken_Inferred` (`0x00576860`) | new(4) token → find-or-append on `DAT_00d1f620` |
| `FUN_00576d70` | distort twin; same registry |
| `FUN_007334a0` / `FUN_00962780` | bootstrap `new(0x20)` objects → same host |
| `FUN_0075fc40` | gfx init; push small vtbl object then nested bootstrap |

## Open questions

- Product C++ typedef / English name of `DAT_00d1f620` host (registry vs free vector).
- Whether `+0x00` is vtbl or unused (never read here).
- Runtime / bit-exact / differential (no Launcher).
