# Raw capture: FUN_0044e100

| Field | Value |
|---|---|
| **Stable ID** | `aa_0044e100` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0044e100` |
| **Body range (Ghidra)** | `0x0044e100`–`0x0044e136` |
| **Canonical name** | `RbTree_AllocEmptyNode_0x30` (structural; Ghidra `FUN_0044e100`) |
| **System** | containers / RB-tree |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0044e100(void)

{
  undefined4 *puVar1;
  
  puVar1 = operator_new(0x30);
  if (puVar1 != (undefined4 *)0x0) {
    *puVar1 = 0;
  }
  if (puVar1 + 1 != (undefined4 *)0x0) {
    puVar1[1] = 0;
  }
  if (puVar1 + 2 != (undefined4 *)0x0) {
    puVar1[2] = 0;
  }
  *(undefined1 *)(puVar1 + 0xb) = 1;
  *(undefined1 *)((int)puVar1 + 0x2d) = 0;
  return;
}
```

---

## Append — 2026-07-29 W21-O dual seal

| Field | Value |
|---|---|
| **Tool** | Ghidra `decompile_function` + `read_memory` (no `disassemble_bytes`) |
| **Live decompile** | ≡ 2026-07-23 raw (void surface; callers use **EAX**) |
| **Body exclusive end** | `0x0044e136` (`c3` at `0x0044e135`; `cc` pad) |
| **Entry bytes (64)** | `6a 30 e8 … 83 c4 04 85 c0 74 06 c7 00 00 00 00 00 8d 48 04 … c6 40 2c 01 c6 40 2d 00 c3` |

### Byte-sealed CF

1. `push 0x30` / `call operator_new` / `add esp,4`
2. `test eax,eax` / `jz` skip zero of `+0`
3. `mov dword [eax], 0`
4. `lea ecx,[eax+4]` / test / `mov dword [ecx],0`
5. `lea ecx,[eax+8]` / test / `mov dword [ecx],0`
6. `mov byte [eax+0x2c], 1`
7. `mov byte [eax+0x2d], 0`
8. `ret` (cdecl, no stack cleanup)

### Return surface correction

Decompiler shows `void`. **Callers assign EAX** (e.g. Profiler_Zone_Ctor `iVar1 = FUN_0044e100()`; CNDAssetStringTable serialize). Machine return = **node\*** in **EAX**.

### Sibling pattern

Parallel factory `FUN_00439770` / `RbTree_AllocEmptyNode_Inferred` allocates **0x18** with flags at `+0x14=1`, `+0x15=0`. This VA is the **0x30** node-size twin (value payload region larger; flags at end `+0x2c`/`+0x2d`).
