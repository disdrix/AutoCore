# Annotated low-level: FUN_005179e0 / Object_TeardownOwnedFxLists_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005179e0` |
| VA | `0x005179e0`–`0x00517a6a` (139 B) |
| System | client-fx / object-owned NDSpecialFX ptr-vector teardown |
| Date | 2026-07-29 (W28-H; scaffold 2026-07-23) |
| Canonical (inferred) | `Object_TeardownOwnedFxLists_Inferred` |

## Machine-level notes

- Source: raw capture + live re-verify. Prefer **bytes** when decompiler conflicts (post-`operator_delete` zeroing; callee ECX).
- Primary vector triad: host`+0x124` begin / `+0x128` end / `+0x12c` capacity-or-end-storage (always cleared together).
- Sticky current ptr host`+0x130` cleared after optional single-remove.
- Secondary list teardown via nested `FUN_00516930(this, 0)` (`+0x138` family; not OWN).
- Free gate matches sealed StopFinalize dual: `fx+9 == 0` and return non-zero → TeardownCore + delete.
- Entries failing the gate are **not** deleted; vector storage is still freed (orphans intentionally if still live/shared).

## Pseudocode (byte-corrected)

```c
// thiscall; virtual + direct; plain ret
void __thiscall Object_TeardownOwnedFxLists_Inferred(void *self)
{
  void **it, **end;
  void *fx;

  FUN_00516930(self, 0);                 /* secondary +0x138 vector; callee ret 4 */

  if (*(void **)((char *)self + 0x130) != 0)
    FUN_00517250(self, *(void **)((char *)self + 0x130));

  *(void **)((char *)self + 0x130) = 0;

  it  = *(void ***)((char *)self + 0x124);
  end = *(void ***)((char *)self + 0x128);
  if (it != end) {
    do {
      fx = *it;
      if (*(char *)((char *)fx + 9) == 0 &&
          NDSpecialFX_StopFinalize_Inferred(fx) != 0) {
        NDSpecialFX_TeardownCore_Inferred(fx);   /* FUN_004b99c0 */
        operator_delete(fx);
      }
      it = (void **)((char *)it + 4);
    } while (it != *(void ***)((char *)self + 0x128));
  }

  if (*(void **)((char *)self + 0x124) != 0)
    operator_delete(*(void **)((char *)self + 0x124));

  *(void **)((char *)self + 0x124) = 0;
  *(void **)((char *)self + 0x128) = 0;
  *(void **)((char *)self + 0x12c) = 0;
}
```

## Open questions

- Product C++ class / member names for the two ptr-vectors and `+0x130`.
- Exact meaning of `fx+9` sticky byte (shared with StopFinalize free polarity).
- Nested dual of `FUN_00516930` / `FUN_00517250` (not OWN).
