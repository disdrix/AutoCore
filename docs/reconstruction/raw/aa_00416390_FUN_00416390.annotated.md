# Annotated low-level: CriticalSection_Initialize_SehToHresult_Ecx_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00416390` |
| VA | `0x00416390` |
| Ghidra | `FUN_00416390` |
| Canonical (inferred) | `CriticalSection_Initialize_SehToHresult_Ecx_Inferred` |
| System | util / Win32 sync (CRITICAL_SECTION) |
| Date | 2026-08-04 (WQ9I-I dual seal) |

## Machine-level notes

- **Body:** 122 B (`0x7A`); `0x00416390`–`0x0041640A` exclusive; pad `CC` before `0x00416410`.
- **ABI:** ECX = `LPCRITICAL_SECTION`; EAX = HRESULT; plain RET (`C3`).
- **Success path (decompiler-visible):** MSVC SEH prolog → `InitializeCriticalSection(cs)` via IAT `[0x009c61ec]` → restore FS:[0] → **return 0**.
- **Exception path (bytes; decompiler collapses):**
  - Scope table at `DAT_00aaa698`: `{ -1, filter=0x004163C4, handler=0x004163D4 }`.
  - Filter returns `1` (`EXCEPTION_EXECUTE_HANDLER`) after stashing exception code.
  - Handler maps:
    - `STATUS_NO_MEMORY` (`0xC0000017`) → `E_OUTOFMEMORY` (`0x8007000E`)
    - any other SEH code → `E_FAIL` (`0x80004005`) via `NEG/SBB/AND 0xFFF93FF7/ADD 0x8007000E`
- **Caller:** `FUN_00489dc5` @ `0x00489e50`: `LEA ECX,[ESI+0x18]`; on `EAX < 0` sets global `DAT_00d1f0f0 = 1`.
- **Pair:** same `+0x18` region pre-zeroed by `Mem_ZeroSixDwords_Ecx_Inferred` (`0x00416370`).
- Not a thiscall method of a C++ type with vtbl; free ECX helper used during host ctor.

## Pseudocode (annotated)

```c
// ECX = LPCRITICAL_SECTION; returns HRESULT
HRESULT CriticalSection_Initialize_SehToHresult_Ecx_Inferred(
    LPCRITICAL_SECTION cs /*ECX*/)
{
  __try {
    InitializeCriticalSection(cs); // IAT [0x009c61ec]
    return 0; // S_OK
  } __except (/* filter: capture code, return EXCEPTION_EXECUTE_HANDLER */) {
    // code == STATUS_NO_MEMORY (0xC0000017) → E_OUTOFMEMORY (0x8007000E)
    // else → E_FAIL (0x80004005)
    return MapStatusNoMemoryToHresult(exception_code);
  }
}
```

## Open residual

- Product / ATL-style demangle plate for the SEH wrapper.
- Exact CRT/ATL helper identity of filter/handler prolog (`DAT_00489cb0`).
- Runtime / bit-exact under forced InitCS failure.
