# Annotated low-level: Map_ExactFindByIntKey (`FUN_0051c150`)

| Field | Value |
|---|---|
| Stable ID | `aa_0051c150` |
| VA | `0x0051c150` |
| System | `skills-abilities` |
| Date | 2026-07-23 scaffold; dual seal 2026-07-29 |

## Machine-level notes

- Source: raw capture for `aa_0051c150` + live dual re-decompile / `read_memory` (2026-07-29).
- **Exact find**, not lower_bound API: walk is lower_bound-shaped; exit requires `cand.key <= key`.
- Nil flag **`+0x29`** (not `+0x15` / `+0x1d` of other map helpers).
- Key int at **`+0x10`**. End sentinel `*(map+4)`.
- `ret 8` → two stack formals + thiscall `this`.
- Callers: Lookup `0x00518d07`, Clear `0x0051a717` only.
- Prefer assembly when decompiler conflicts — force redecompile matched raw.

## Pseudocode (annotated)

```c
// void __thiscall Map_ExactFindByIntKey(mapHeader* this, Node** pOut, int* pKey)
void __thiscall FUN_0051c150(int param_1, undefined4 *param_2, int *param_3)
{
  undefined4 *puVar1;  // walk cursor
  undefined4 *puVar2;  // next child
  undefined4 *puVar3;  // candidate (lower_bound accum) / end

  puVar3 = *(undefined4 **)(param_1 + 4);           // end = map->_Myhead
  if (*(char *)((int)puVar3[1] + 0x29) == '\0') {   // root not nil
    puVar1 = (undefined4 *)puVar3[1];               // cur = end->_Parent (root)
    do {
      if ((int)puVar1[4] < *param_3) {              // key @ +0x10 < search → right
        puVar2 = (undefined4 *)puVar1[2];           // right @ +0x08
      }
      else {
        puVar2 = (undefined4 *)*puVar1;             // left @ +0x00
        puVar3 = puVar1;                            // candidate = cur
      }
      puVar1 = puVar2;
    } while (*(char *)((int)puVar2 + 0x29) == '\0');
  }
  // exact: cand != end && cand.key <= search  (with walk ⇒ equal)
  if ((puVar3 != *(undefined4 **)(param_1 + 4)) && ((int)puVar3[4] <= *param_3)) {
    *param_2 = puVar3;                              // hit node*
    return;
  }
  *param_2 = *(undefined4 **)(param_1 + 4);         // miss → end
  return;
}
```

## Open questions

- Product / RTTI name (structural `Map_ExactFindByIntKey` used in dual reviews).
- Full 0x30 node value layout (owned by Lookup/Clear + factory `FUN_0058d9c0`).
