# Annotated low-level: FUN_007b65d0 → AssManager_ReleaseOwnedResolvers_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_007b65d0` |
| VA | `0x007b65d0`–`0x007b66a3` exclusive (**211** B) |
| Canonical | `AssManager_ReleaseOwnedResolvers_Inferred` |
| Ghidra | `FUN_007b65d0` |
| Wave | W29-D OWN-ONLY |
| Source | raw + live decompile + `read_memory` 2026-07-29 |

## Role

**Destructor body** for an assManager-owned object (vtable `PTR_FUN_00a95b88`) that holds two resolver interface pointers at **`+0x08`** and **`+0x0c`**. Releases each from the global assManager GuardedVector (and a side string-vector), then scalar-deletes the interfaces and nulls the slots.

Evidence: embedded path `...\assets\assManager.cpp` line **`0x27b`**, message **`Removing resolver not found`**.

## ABI

| Slot | Binding | Conf |
|---|---|---|
| this | **EBX** (callers set before body) | **High** |
| stack formals | none | **High** |
| local key | `[esp]` after `push ecx` | **High** |
| return | plain `ret`; void | **High** |

Callers:
- `FUN_004239a0` — scalar deleting dtor: set vtbl, call body, optional `operator_delete`
- `FUN_007b66b0` — complete dtor: set vtbl, call body
- `FUN_00939fd0` — additional site

## Layout (this / EBX)

| Off | Role |
|---|---|
| +0x00 | vtbl (set by dtor wrappers to `PTR_FUN_00a95b88`) |
| +0x08 | resolver A (interface*; optional) |
| +0x0c | resolver B (interface*; optional) |

## Globals

| Sym | Role |
|---|---|
| `DAT_00d1f058` | assManager host object* |
| `DAT_00d1f058+0x58` | GuardedVector of resolvers (find/erase; CS family) |
| `DAT_00d1f1fc` | pointer to `basic_string`-element vector shell (`+4` begin, `+8` end, elem **0x1c**) |

## Control flow

```
// Arm A — slot +8
if [ebx+8] != 0:
  ECX = *DAT_00d1f058
  // EAX still [ebx+8] from entry test
  FUN_00423e00()                    // remove key from manager list (unowned)
  if [ebx+8]: ([ebx+8]->vtbl[0])(1) // scalar deleting dtor
  [ebx+8] = 0
  // prune related string vector (max 20 first-erases)
  for i in 0..0x13:
    v = *DAT_00d1f1fc
    begin = v[+4]
    if begin && (end-begin)/0x1c != 0:
      FUN_0044d4e0(v, &tmp, begin)  // erase one 0x1c element at begin

// Arm B — slot +0xc
key = [ebx+0xc]
if key != 0:
  container = *DAT_00d1f058 + 0x58   // ESI
  found = FUN_0043e5b0(&key)        // EAX=container; GuardedVector contains
  if !found:
    vog_LogMessage(assManager.cpp, 0x27b, 3, "Removing resolver not found")
  else:
    FUN_0043c550(&key)              // GuardedVector_EraseFirstEqual (W28-C)
  if [ebx+0xc]: ([ebx+0xc]->vtbl[0])(1)
  [ebx+0xc] = 0
return
```

## Callees (owned residual noted)

| VA / sym | Role | Dual |
|---|---|---|
| `FUN_00423e00` | arm-A list remove (same log strings) | unowned |
| `FUN_0043e5b0` | GuardedVector contains-equal | unowned |
| `FUN_0043c550` | GuardedVector_EraseFirstEqual | W28-C accept-with-gaps |
| `FUN_0044d4e0` | erase one 0x1c string-elem | unowned |
| `vog_LogMessage` | error log | import-level |

## Gaps

- Product class English beyond assManager.cpp provenance.
- Exact interface type of resolvers at +8/+0xc.
- Full semantics of `DAT_00d1f1fc` vector (why max 20 first-erases).
- Unowned `FUN_00423e00` / `FUN_0043e5b0` / `FUN_0044d4e0` internals.
- Runtime golden.
