# Annotated low-level: FUN_00970e50 → AssPreloader_RetireStaleNeverLoaded_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00970e50` |
| VA | `0x00970e50`–`0x00970fb8` (**360 B**) |
| System | assPreloader / asset preload pipeline |
| Date | 2026-07-29 (W33-O re-verify) |
| Canonical | `AssPreloader_RetireStaleNeverLoaded_Inferred` |
| Ghidra | `FUN_00970e50` |
| Reject | scaffold `Named_assPreloader_00970e50` as final product plate (family only) |

## Machine-level notes

- **ABI:** stack `AssPreloader* self`, `float maxAge`; **`RET 8`**. Decompiler formals match; void return.
- **Clock:** `FUN_0076c330` → current time (float).
- **Map walk:** std-map style header at `self+0x10`; sentinel/end at `self+0x14`. Node fields sealed by bytes:
  - `+0x0C` key object (thiscall into `FUN_0096a630` → `assId: [%s]`)
  - `+0x10` owned object* (optional vtbl[0] with push 1)
  - `+0x14` float stamp
- **Age gate:** keep if `(now - stamp) <= maxAge`; else retire path.
- **Retire path:** release object → format assId → product log line 0x2A8 → push locked PodU32U8 into **`self+0xAC`** → iterator++ → `FUN_00447350(self+0x10)` erase → `retired++`.
- **Tail log:** always logs line 0x2B7 with retire count (even when 0).
- **Caller:** `FUN_007b7000` supplies **300.0f** (`0x43960000`) and preloader from `DAT_00d1f050+0x6c`.

## Pseudocode (annotated)

```c
// stack (self, maxAge); RET 8
void AssPreloader_RetireStaleNeverLoaded_Inferred(void *self, float maxAge)
{
  float now = FUN_0076c330();
  int retired = 0;
  int *end = *(int **)((char*)self + 0x14);
  int *node = (int*)*end;
  while (node != end) {
    float age = now - *(float*)((char*)node + 0x14);
    if (age <= maxAge) {
      FUN_00457cc0(&node); // map iterator++
    } else {
      void **obj = *(void***)((char*)node + 0x10);
      if (obj) (** (void(***)(void*,int)) obj)(obj, 1);
      // assId string from key at node+0xC via FUN_0096a630
      vog_LogMessage(..., 0x2A8, 2, "Preloaded asset <%s> never loaded...");
      // EDI = self+0xAC; locked PushBack Stride2 U32U8
      FUN_0043e8f0(); FUN_0043e7f0();
      FUN_00457cc0(&node);
      FUN_00447350((char*)self + 0x10 /*, node */);
      retired++;
    }
  }
  vog_LogMessage(..., 0x2B7, 2, "Retired %d preload blocks from pool.", retired);
}
```

## Cross-links

- Host ensure installs preloader: `AssManager_EnsureAssPreloader_Inferred` (`0x0075d3c0`, this dual).
- Ring `+0xAC` same container seeded on ensure and used on retire.
- Sibling AssPreloader duals: Enqueue / WorkerLoop / TryPreloadChild (W31–W32).
