# Annotated low-level: Object_ApplyDualFxSlots_SlotFilterWild_Inferred (FUN_004c8110)

| Field | Value |
|---|---|
| Stable ID | `aa_004c8110` |
| VA | `0x004c8110`–`0x004c81e5` |
| Size | 214 bytes |
| System | client-fx / dual effect-slot apply |
| Date | 2026-07-29 (W25-D dual seal) |
| Prior scaffold | 2026-07-23 |

## Machine-level notes

- Nested **thiscall** subobject (negative field offsets; MI entry thunks adjust ECX).
- Two effect pointer slots at `this-0x280` and `this-0x27c` (consecutive dwords).
- For each non-null slot:
  1. `NDSpecialFX_ForEachHostPtrTablePair_ApplyFxToggle_Inferred` / `FUN_00583f10` on `this-0x4c0` with `(0, effect, 1, -1)`.
  2. Walk local host* vector via vb from `*(this-0x4fc)` at offsets `-0x3c4`/`-0x3c0`; each host → sealed `004b7030` with same arg pack.
- Tail-calls `FUN_00584330` with `this-0x430` (sibling path uses filter char 0).
- Arg pack matches W24-D sample: `(0, effect*, 1, -1)` → e270 wild slot path inside apply.

## ABI

```
// Entry: ECX = nested object sub*
// Exit:  tail jmp FUN_00584330 (ECX = this - 0x430)
void __thiscall Object_ApplyDualFxSlots_SlotFilterWild_Inferred(void *self);
```

## Annotated pseudocode

```c
// Ghidra: FUN_004c8110
void __fastcall Object_ApplyDualFxSlots_SlotFilterWild_Inferred(int self /*ECX*/)
{
  int effect;
  int vb;
  int it;

  effect = *(int *)(self - 0x27c);
  if (effect != 0) {
    FUN_00583f10(/*ecx=*/self - 0x4c0, 0, effect, 1, 0xffffffff);
    vb = *(int *)(*(int *)(self - 0x4fc) + 4);
    it = *(int *)(vb - 0x3c4 + self);
    if (it != *(int *)(vb - 0x3c0 + self)) {
      do {
        // ECX = *(int*)it
        FUN_004b7030(0, effect, 1, 0xffffffff);
        it = it + 4;
      } while (it != *(int *)(*(int *)(*(int *)(self - 0x4fc) + 4) - 0x3c0 + self));
    }
  }

  effect = *(int *)(self - 0x280);
  if (effect != 0) {
    FUN_00583f10(/*ecx=*/self - 0x4c0, 0, effect, 1, 0xffffffff);
    vb = *(int *)(*(int *)(self - 0x4fc) + 4);
    it = *(int *)(vb - 0x3c4 + self);
    if (it != *(int *)(vb - 0x3c0 + self)) {
      do {
        FUN_004b7030(0, effect, 1, 0xffffffff);
        it = it + 4;
      } while (it != *(int *)(*(int *)(*(int *)(self - 0x4fc) + 4) - 0x3c0 + self));
    }
  }

  // tail
  FUN_00584330(/*ecx=*/self - 0x430);
}
```

## Layout (touched, relative to this)

| Off | Role |
|---:|---|
| `-0x280` | effect slot B* |
| `-0x27c` | effect slot A* |
| `-0x4c0` | this for host-table fan-out (`00583f10`) |
| `-0x4fc` | vbptr for local host* vector |
| adjusted `-0x3c4/-0x3c0` | local host* begin/end |
| `-0x430` | this for tail `00584330` |

## Open questions

- Product class / member names for dual slots and nested this.
- Exact English for effect* as `want_stop` payload (forwarded; sealed by `004b7030` dual).
- Full dual of chain `00584330` (not owned this wave).
- Runtime / bit-exact.
