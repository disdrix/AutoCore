# Annotated low-level: InventoryGrid_RemoveItemAtXY (`FUN_00571db0`)

| Field | Value |
|---|---|
| Stable ID | `aa_00571db0` |
| VA | `0x00571db0` |
| Body | `0x00571db0`–`0x00571e0e` (94 B) |
| System | `inventory-transfer` |
| Date | `2026-08-04` (WQ8R-G dual seal) |
| Tools | `decompile_function`, `analyze_function_complete`, `read_memory` — **no** `disassemble_bytes` |

## Canonical name

**`InventoryGrid_RemoveItemAtXY`** (Inferred from CF + sealed siblings `FindItemByCoid` / `RemoveItem`; Ghidra `FUN_00571db0`).

## Machine-level notes (read_memory @ entry)

```
00571db0  83 EC 08           sub  esp, 8
00571db3  56                 push esi
00571db4  8B F1              mov  esi, ecx              ; this = InventoryGrid*
00571db6  8B 4E 0C           mov  ecx, [esi+0x0C]       ; height
00571db9  8B 46 08           mov  eax, [esi+0x08]       ; width
...
          movzx  rowX, byte [esp+arg0]                  ; x
          cmp    x, width ; jge fail
          movzx  colY, byte [esp+arg1]                  ; y
          cmp    y, height ; jge fail
          imul   edx, height, x
          add    edx, y                                 ; index = height*x + y
          mov    ecx, [esi+0x28]                        ; cells
          mov    eax, [ecx+edx*8]                       ; coidLo
          mov    ecx, [ecx+edx*8+4]                     ; coidHi
          push   0                                      ; forceFull (for Remove)
          push   1                                      ; removeQty
          push   coidHi
          push   coidLo
          mov    ecx, esi
          call   InventoryGrid_FindItemByCoid           ; RET 8 cleans COID pair
          push   eax                                    ; item*
          mov    ecx, esi
          call   InventoryGrid_RemoveItem               ; RET 0xC
          pop    esi
          add    esp, 8
          ret    8
fail:
          xor    eax, eax
          pop    esi
          add    esp, 8
          ret    8
```

Full entry hex (94 B + pad):
```
83ec08568bf18b4e0c8b4608894c24080fb64c24103bc87d3d8bd00fb6442414895424048b560c3bc27d2b0fafd18b4e2803d08b04d18b4cd1046a006a0151508bcee819f2ffff508bcee881fdffff5e83c408c2080033c05e83c408c20800cc
```

Call targets verified from rel32: `0x00571010` (FindItemByCoid), `0x00571b80` (RemoveItem).

## Pseudocode (annotated)

```c
// __thiscall; RET 8
ItemObject* InventoryGrid_RemoveItemAtXY(InventoryGrid* this, uint8_t x, uint8_t y)
{
  if ((int)x >= *(int*)(this + 0x08)) return 0;   // width
  if ((int)y >= *(int*)(this + 0x0c)) return 0;   // height
  int index = *(int*)(this + 0x0c) * (int)x + (int)y;
  void* cells = *(void**)(this + 0x28);
  uint32_t lo = *(uint32_t*)((char*)cells + index * 8);
  uint32_t hi = *(uint32_t*)((char*)cells + index * 8 + 4);
  ItemObject* item = InventoryGrid_FindItemByCoid(this, lo, hi);
  return InventoryGrid_RemoveItem(this, item, /*qty*/1, /*forceFull*/0);
}
```

## Sibling contrast

| VA | Name | Args | Notes |
|----|------|------|-------|
| `0x00571d80` | RemoveByCoid (thin) | coidLo, coidHi, forceFull | Find + Remove(item,1,force) |
| `0x00571db0` | **RemoveItemAtXY** | x, y | cell stamp → Find + Remove(item,1,**0**) |

## Callers

Ghidra `get_xrefs_to` / bulk xrefs: **none** (0 static CALL sites). May be dead, data-only, or missed analysis. Internal calls only to sealed Find/Remove.

## Open questions

- Product/PDB name.
- Any live caller via function pointer / vtable (not seen in Ghidra xref DB).
- Runtime / bit-exact.
