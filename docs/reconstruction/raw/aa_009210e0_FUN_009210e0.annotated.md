# Annotated low-level: FUN_009210e0 → Client_RefreshInteractPromptTarget_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_009210e0` |
| VA | `0x009210e0`–`0x00921355` |
| System | `interaction-activation` (proximity interact prompt / target refresh) |
| Date | 2026-07-23 scaffold; **2026-07-29 W18-L dual seal** |

## Machine-level notes

- **ABI:** `RET 4` — caller pushes client `this` (`FUN_009216e0`: `PUSH ESI`); **formal unused** in body (globals only).
- **ESI contract:** after each resolve/pick, `MOV ESI,EAX`; final `FUN_008a81a0(DAT_00d1b8c8)` reads **unaff_ESI** as the interact target for "Press … to …" chrome.
- **Radii sealed:** gather r = **15.0f** (`0x41700000`); keep-current max dist² = **225.0f** (`DAT_00aaaca4`).
- **Modes sealed:** `FUN_0058cd60` mode **6** (+dc broadphase); `Skill_GatherTargetsInArea` mode **3** (living interactables); optional mode **7** filterA **0x14** when `*(DAT_00d1b644+0xf5)`; last resort `Client_FindFirstObjectInRadius(15)`.
- Resolve ECX = `*(DAT_00d1b644 + 0xe4e8)` before each `Object_ResolveFromTFID` (bytes sealed).

## Control flow (annotated)

```
1. if DAT_00d1b8c8 == 0 → return
2. if DAT_00d1b8c8->vtbl[+0x3d8]() == 0 → return   // prompt host not ready
3. seed local TFID from null template DAT_00a158b8..
4. playerPos = adj_this(DAT_00d1b6d8)->vtbl[+0x1a0]()  // float4 xyzw
5. Prefer DAT_00d1d888 when:
     non-null AND identity(vtbl+0x210) != local char AND dist² <= 225
   Else:
     FUN_0058cd60(..., r=15, mode=6) → ResolveFromTFID → candidate
6. If have candidate:
     Client_MaybeShowFirstTimeTip(3)
     if candidate → goto UI_UPDATE
7. Skill_GatherTargetsInArea(..., r=15, mode=3, ...) → Resolve
8. If miss and world+0xf5:
     Skill_GatherTargetsInArea(..., mode=7, filterA=0x14) → Resolve
9. If still miss: Client_FindFirstObjectInRadius(15)
10. UI_UPDATE: FUN_008a81a0(DAT_00d1b8c8) with ESI=object*
```

## Open residual

- Product name of `DAT_00d1b8c8` / `vtbl+0x3d8` / `FUN_008a81a0`.
- Whether timer parent always uses this as prompt-only (no UseObject send here — **confirmed**: no send callees).
- Exact type of unused stack formal (client*) beyond caller push.
