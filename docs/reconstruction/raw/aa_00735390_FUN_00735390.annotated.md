# Annotated low-level: PalantirSub10_OwnedElem_CompleteDtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00735390` |
| VA | `0x00735390`–`0x00735403` exclusive (**115 B**) |
| Ghidra | `FUN_00735390` |
| Canonical | `PalantirSub10_OwnedElem_CompleteDtor_Inferred` |
| System | client / Palantir nested owned-element teardown |
| Date | 2026-08-04 (W37-N dual) |

## Machine-level notes

- **ABI:** stdcall 1 stack arg (`elem*`); **`RET 4`**. Not thiscall (no ECX this).
- **SEH:** `LAB_009ab754`; destroy order state 2 (COM+vec) → 0 (string@+0x1c) → −1 (string@+0).
- **COM@+0x48:** null-checked **Release** via vtbl[+8], then zero slot.
- **`FUN_00465d70`:** called with **EBX = elem+0x70C** (LEA sealed). Callee is EBX-this vector destroyer: walk `[begin,end)` stride **0x24**, `FUN_00481d80` per elem, `operator_delete(begin)`. Decompiler omits EBX formal — **bytes win**.
- **Strings:** IAT dtor `@0x009c62f4` for `std::string` at +0x1c then +0.
- **Sole caller:** `PalantirSub10_DestroyOwnedPtrVecAndComUninit` (`0x00964d00`, W35-J) pushes elem, calls this, then `operator_delete(elem)` — this is complete dtor only (does not free host).

## Layout (elem*)

| Off | Role | Conf |
|-----|------|------|
| +0x00 | `std::string` | High |
| +0x1C | `std::string` | High |
| +0x48 | COM iface* | High |
| +0x70C | vector host (EBX this for `FUN_00465d70`) | High |
| span | ≥ 0x71C | High |

## Pseudocode (annotated, bytes-corrected)

```c
// stdcall; RET 4; void
void __stdcall PalantirSub10_OwnedElem_CompleteDtor_Inferred(Elem* self)
{
  // SEH state = 2
  IUnknown* com = self->com_48;           // +0x48
  if (com)
    com->lpVtbl->Release(com);            // vtbl+8
  self->com_48 = nullptr;

  // EBX = &self->vec_70c
  Vec24_Tidy_EBX(&self->vec_70c);         // FUN_00465d70

  // SEH state = 0
  self->str_1c.~basic_string();

  // SEH state = -1
  self->str_00.~basic_string();
}
```

## Gaps

- Product C++ class English for elem / COM iface / 0x24 vector payload.
- Matching ctor / populate path for Palantir+0x10 vector.
- Runtime / bit-exact / differential.
