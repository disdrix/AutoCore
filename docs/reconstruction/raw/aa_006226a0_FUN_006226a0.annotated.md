# Annotated low-level: CVOGHBSkill_OnKill_SpawnPerTargetHB_Inferred (FUN_006226a0)

| Field | Value |
|---|---|
| Stable ID | `aa_006226a0` |
| VA | `0x006226a0`–`0x006227a7` |
| System | skills-abilities |
| Date | 2026-08-05 (R11-020 dual; supersedes 2026-07-23 scaffold notes) |
| Class (RTTI) | **`CVOGHBSkill_OnKill`** — Confirmed |
| Inferred name | `CVOGHBSkill_OnKill_SpawnPerTargetHB_Inferred` |

## Machine-level notes

- Source: raw capture `aa_006226a0` + live Ghidra decompile/disasm/RTTI 2026-08-05.
- Prefer assembly for Resolve thiscall, ctor ECX, and RET 0x18 (decompiler drops reaction this / invents CONCAT31).
- Virtual install only: DATA xref at `PTR_FUN_009d16e4 + 0x2c` (`0x009d1710`).
- Incoming ECX (OnKill HB this) is **unused**; all work uses stack args + world reaction/list.

## ABI (sealed)

```c
// __thiscall + 6 stack args; RET 0x18; EAX = 1 (sentinel only)
uint32_t __thiscall CVOGHBSkill_OnKill_SpawnPerTargetHB_Inferred(
    CVOGHBSkill_OnKill* this,   // ECX — unused
    void* pSourceObj,           // stack+0x04
    void* pSkillBlob,           // stack+0x08
    void* pWorld,               // stack+0x0C  (+0xe4e8 reaction, +0xe4ec HB list)
    TargetEntry* pTargetTable,  // stack+0x10  stride 0x10
    uint32_t* pTfid16,          // stack+0x14  → base ctor
    uint32_t arg7);             // stack+0x18  → base ctor
```

```c
struct TargetEntry {   // stride 0x10
  int32_t idLo;        // +0
  int32_t idHi;        // +4
  int8_t  type;        // +8  (char); pad to 0x10
};
// sentinel: idLo==-1 && idHi==-1 && type==0
```

## Pseudocode (annotated; CF ≡ raw + asm fixes)

```c
uint32_t __thiscall FUN_006226a0(
    void* /*this unused*/,
    void* pSourceObj,
    void* pSkillBlob,
    void* pWorld,
    TargetEntry* pTable,
    uint32_t* pTfid16,
    uint32_t arg7)
{
  char index = 0;
  // SEH frame LAB_009a897b
  for (;;) {
    TargetEntry* e = &pTable[index];  // index * 0x10
    if (e->idLo == -1 && e->idHi == -1 && e->type == 0) {
      return 1;  // always success at sentinel
    }
    // thiscall: ECX = *(pWorld + 0xe4e8)
    void* resolved = CVOGReaction_ResolveObjectTarget(
        *(void**)((char*)pWorld + 0xe4e8), e->type, e->idLo, e->idHi);
    if (resolved != 0) {
      CVOGHBBase* pAction = (CVOGHBBase*)operator_new(0x6c0);
      if (pAction != 0) {
        // thiscall ECX=pAction
        CVOGHBSkillBase_ctor(pAction, pSourceObj, pSkillBlob, pWorld,
                             resolved, pTfid16, arg7);
        pAction->pVTable = &PTR_FUN_009d16e4;  // CVOGHBSkill_OnKill
      }
      if (pAction->pOwnerObject != 0) {  // +0x18; ValidateTarget accept
        CVOGHBList_Enqueue(*(void**)((char*)pWorld + 0xe4ec), pAction);
        CVOGHBBase_Start(pAction);
      } else {
        // scalar deleting dtor vtbl[0](1)
        (*(void(__thiscall**)(CVOGHBBase*, int))pAction->pVTable)(pAction, 1);
      }
    }
    index++;
  }
}
```

## Control-flow stages

| Stage | Behavior |
|---|---|
| 1 | Walk `pTable[i]` stride 0x10 from i=0 |
| 2 | Sentinel (-1,-1,type0) → restore SEH, **return 1** |
| 3 | Resolve via world reaction @ `+0xe4e8` |
| 4 | Miss → next index |
| 5 | Hit → `new(0x6c0)` + `CVOGHBSkillBase_ctor` + OnKill vtbl |
| 6 | Owner set → Enqueue(`world+0xe4ec`) + Start; else dtor(1) |
| 7 | Next index (unbounded until sentinel) |

## Open questions

- Product English for method (OnKill class RTTI Confirmed; method `_Inferred`).
- Who builds the 0x10 target table and who dispatches vtbl+0x2c (virtual only).
- Why this ignores HB `this` (stack-complete args) — same pattern as some peer skill executes.
- Runtime / bit-exact / differential open. Terminal **false**.
