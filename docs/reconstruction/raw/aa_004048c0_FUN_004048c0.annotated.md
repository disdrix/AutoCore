# Annotated low-level: Win32_ThrowLastErrorAsHresult_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004048c0` |
| VA | `0x004048c0` |
| Body | `0x004048c0`–`0x004048d9` (26 bytes) |
| Canonical name | `Win32_ThrowLastErrorAsHresult_Inferred` |
| Ghidra name | `FUN_004048c0` |
| System | `crt-com` / Win32 last-error → C++ throw |
| Date | `2026-07-29` (W22-G dual seal) |

## Machine-level notes

- **Noreturn** failure helper: sample `GetLastError`, pack positive codes as **HRESULT_FROM_WIN32**, throw via sealed `FUN_004048e0` / `Cxx_ThrowException_Param_Inferred`.
- IAT call `[0x009c6230]` = `GetLastError` (`read_memory` sealed).
- Pack mask: `(code & 0xffff) | 0x80070000` when `(int)code > 0`; non-positive codes pass through unchanged (already HRESULT / success-zero path still throws).
- Caller pattern: Win32 API returns 0 / fails → call this (e.g. `MbcsString_ConvertFromWide` on `WideCharToMultiByte == 0`).
- Distinct from raw-param throw `FUN_004048e0` (callers that already hold an HRESULT) and from `_com_raise_error` (`0x007181c0`).

## Pseudocode (annotated)

```c
// noreturn
void Win32_ThrowLastErrorAsHresult_Inferred(void)
{
  DWORD err = GetLastError();                 // IAT [0x009c6230]
  if ((int)err > 0) {
    // HRESULT_FROM_WIN32: FACILITY_WIN32 | severity fail
    err = (err & 0xffff) | 0x80070000;
  }
  FUN_004048e0(err);                          // Cxx_ThrowException_Param → DAT_00acc3fc
  // no return
}
```

## Bytes (`read_memory`)

```text
004048c0  FF1530629C00    call dword ptr [0x009c6230]  ; GetLastError
004048c6  85C0            test eax, eax
004048c8  7E0A            jle  004048d4                 ; skip pack if <= 0
004048ca  25FFFF0000      and  eax, 0x0000FFFF
004048cf  0D00000780      or   eax, 0x80070000
004048d4  50              push eax
004048d5  E806000000      call FUN_004048e0             ; +0x06 → 004048e0
004048da  (CC pad outside body; Ghidra end 004048d9)
```

Hex body: `ff1530629c0085c07e0a25ffff00000d0000078050e806000000`

## Callers (get_function_callers)

| VA | Notes |
|---|---|
| `0x00403450` | `MbcsString_ConvertFromWide` — convert fail |
| `0x00404b30` | Win32 fail path |
| `0x00427530` | Win32 fail path |
| `0x0050c1b0` | Win32 fail path |
| `0x00517400` | Win32 fail path |
| `0x005197b0` | Win32 fail path |
| `0x005320d0` | Win32 fail path |

## Open questions

1. Product/PDB symbol (likely internal CRT/ATL helper; keep `_Inferred`).
2. Full catchable-type / RTTI under ThrowInfo `DAT_00acc3fc` (owned by dual of `004048e0`).
3. Runtime throw catch / bit-exact / differential.
