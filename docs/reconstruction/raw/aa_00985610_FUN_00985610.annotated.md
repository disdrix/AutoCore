# Annotated low-level: GfxIndexBufferFactory_PostRestoreDevice

| Field | Value |
|---|---|
| Stable ID | `aa_00985610` |
| VA | `0x00985610`–`0x009857fb` exclusive (**491 B**) |
| System | client / Palantir graphics — `gfxIndexBufferFactory.cpp` PostRestoreDevice |
| Date | 2026-08-04 W38-AC |
| Canonical name | `GfxIndexBufferFactory_PostRestoreDevice` |
| Ghidra | `FUN_00985610` |

## Machine-level notes

- **ABI:** **EAX** = factory host; plain RET; returns **0** or HRESULT **`<0`**.
- **Product strings (sealed):**
  - path `C:\vog\1_code\palantir\palantir\graphics\gfxIndexBufferFactory.cpp`
  - `"Failed PostResoreDevice() on Index Buffers"` @ line **`0x180`** (product typo *Resore*)
  - `"RestoreSurfaceCallback Failed on Index Buffer (%d indices)"` @ line **400**
- **Same outer map** as PreRestore sibling (`factory+0x14`, flag gate `0x28`, stride `0x20`).
- **Recreate:** `FUN_00743cd0(count, usage, is32bit)` with **EDI** = device-IB*; args from `IB+4`, `IB+8`, `IB+0x10==4`.
- **Callbacks:** secondary tree at slot `+8`, `is_nil@+0x11`; optional object @ `host+0x20` vtbl[0](host); index count for log @ `host+0x1c`.
- **Fail soft:** return recreate HRESULT when err-report code `!= 3`.
- **Fail hard:** VOGCRASH.txt + `raise(SIGABRT=0x16)` + `ExitProcess(3)` when report code `== 3`.
- **Caller:** sole — post-`IDirect3DDevice9::Reset` rebuild; parent fails Reset with `"Failed to Reset() device on IB Factory"` @ gfxDevice.cpp line `0x236`.

## Pseudocode (annotated)

```c
// EAX = GfxIndexBufferFactory* factory
// returns 0 or HRESULT < 0
int GfxIndexBufferFactory_PostRestoreDevice(void /* EAX=factory */)
{
  Node* sentinel = *(Node**)(factory + 0x14);
  for (Node* n = *sentinel; n != sentinel; n = tree_successor(n, is_nil@+0x21)) {
    if ((n->flags@+0x0C & 0x28) == 0) {
      for (Slot* s = n->begin; s != n->end; s = (Slot*)((uint8_t*)s + 0x20)) {
        GfxDeviceIB* ib = s->ib; // *s
        int hr = gfxDeviceIB_CreateOnDevice(ib->count, ib->usage, ib->stride_or_fmt == 4); // FUN_00743cd0 / EDI=ib
        if (hr < 0) {
          if (errReport(path, 0x180, "Failed PostResoreDevice() on Index Buffers") != 3)
            return hr;
          /* VOGCRASH + raise + ExitProcess(3) — noreturn */
        }
        for (CbNode* c = s->cb_head; c != s->cb_sentinel; c = tree_successor(c, is_nil@+0x11)) {
          void** obj = *(void***)(c->host + 0x20);
          if (obj && ((int (*)(void*))obj[0])(c->host) < 0)
            vog_LogMessage(path, 400, 2, format("RestoreSurfaceCallback Failed…", indices@host+0x1c));
        }
      }
    }
  }
  return 0;
}
```

## Open questions

- Precise secondary-tree value_type / callback host class English.
- Whether soft-fail HRESULT is always from CreateIndexBuffer path (−1 from `FUN_00743cd0` on fail) or passthrough D3D codes (bytes pass ESI as returned).
- Runtime / bit-exact.
