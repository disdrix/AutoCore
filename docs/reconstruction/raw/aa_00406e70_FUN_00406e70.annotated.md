# Annotated low-level: StdVector_InsertOne_RebindIt_Elem12_EdiVec_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00406e70` |
| VA | `0x00406e70` |
| Canonical name | `StdVector_InsertOne_RebindIt_Elem12_EdiVec_Inferred` |
| Ghidra name | `FUN_00406e70` |
| System | util / container (`std::vector` POD elem **0xC**) |
| Date | 2026-08-04 (WQ9G-I dual seal) |

## Machine-level notes

- Source: raw capture + WQ9G-I re-verify (`decompile` + `read_memory`).
- **EDI** = vector host (not ECX thiscall). Decompiler `unaff_EDI` is load-bearing.
- Element stride **0xC** sealed by magic divide `0x2AAAAAAB` and `lea [base+index*12]`.
- Insert count hard-coded **1** (`push 1` / `6A 01`).
- Index saved **before** `FUN_004082f0` (may realloc); rebind `*out = begin' + index*0xC` after.
- **`ret 0x0C`** — three stack formals: out*, where*, value*.
- Sole listed caller: `FUN_004062a0` push_back slow path (EAX→EDI vec).

## Pseudocode (annotated)

```c
// customcc: EDI = vector*  (+4 begin, +8 end, +0xC capEnd)
// stack: T** outIt, T* where, const T* value
// ret 0x0C; EAX = outIt
T** StdVector_InsertOne_RebindIt_Elem12_EdiVec_Inferred(
    /*EDI*/ Vec12* vec,
    /*stack*/ T** outIt,
    /*stack*/ T* where,
    /*stack*/ const T* value)  // T is 12-byte POD
{
  T* begin = vec->begin; // +4
  int index;
  if (begin == 0 || ((vec->end - begin) / 12) == 0)
    index = 0;
  else
    index = (where - begin) / 12;

  // thiscall InsertN on ECX=vec; count=1
  FUN_004082f0(/*this*/ vec, where, /*count*/ 1, value);

  *outIt = (T*)((char*)vec->begin + index * 12);
  return outIt;
}
```

## Open questions

- Product / PDB demangle for element type T (12 B).
- Exact English name of parent map/container using this instantiation.
- Runtime / bit-exact / differential open.
- InsertN engine `FUN_004082f0` undualed residual (owned elsewhere).
