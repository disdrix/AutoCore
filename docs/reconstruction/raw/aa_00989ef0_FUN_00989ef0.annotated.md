# Annotated low-level: ResourceCache_SetField10AndConfigure_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00989ef0` |
| VA | `0x00989ef0`–`0x00989f58` (**105 B**) |
| System | ResourceCache / guarded resource host |
| Date | 2026-07-29 (W31-F dual seal) |
| Ghidra | `FUN_00989ef0` |
| Canonical | `ResourceCache_SetField10AndConfigure_Inferred` |

## Machine-level notes

- **ABI:** EDI = object (not ECX-thiscall despite decompiler `__thiscall` label); ECX = dword for `+0x10`; stack arg1 to configure callee; **RET 4**.
- **CS family:** same as `ResourceCache_LookupByName` (`00989e00`): CS at `+0x68`, enable byte at `+0x80`.
- **Subobject:** `LEA EAX,[EDI+0x14]` then `CALL FUN_00996d70` — configure targets embedded object at `+0x14`, not whole host.
- **SEH:** frame `LAB_009adcf3`; state −1 → 0 around critical section + configure.
- **Leave path:** rechecks flag via `[ESI+0x18]` ≡ `[EDI+0x80]` (ESI kept as CS address).
- Prefer assembly when decompiler conflicts (this unit: EDI-this + EAX subobject).

## Pseudocode (byte-corrected)

```c
// EDI = ResourceCache-family object; ECX = field10; stack arg configureFlag; RET 4; void
void ResourceCache_SetField10AndConfigure_Inferred(
    ResourceCache* obj /*EDI*/,
    uint32_t field10 /*ECX*/,
    uint32_t configureFlag /*stack*/)
{
  // SEH: LAB_009adcf3
  if (*(char*)((uint8_t*)obj + 0x80) != 0) {
    EnterCriticalSection((LPCRITICAL_SECTION)((uint8_t*)obj + 0x68));
  }

  *(uint32_t*)((uint8_t*)obj + 0x10) = field10;

  // EAX = obj+0x14
  FUN_00996d70(configureFlag /*stack; EAX=subobject*/);

  if (*(char*)((uint8_t*)obj + 0x80) != 0) {
    LeaveCriticalSection((LPCRITICAL_SECTION)((uint8_t*)obj + 0x68));
  }
}
```

## Open questions

- Product name for `+0x10` (budget / pool size / reservation).
- Full CF of `FUN_00996d70` / nested `FUN_009899a0` / `FUN_00989b00` (not OWN).
- SEH filter `LAB_009adcf3` Leave-on-unwind detail.
