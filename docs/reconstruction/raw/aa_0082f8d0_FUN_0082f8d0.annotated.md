# Annotated low-level: UI_InteractionMenu_AddIgnoreToggle (`FUN_0082f8d0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0082f8d0` |
| VA | `0x0082f8d0`–`0x0082f988` inclusive (size **185 B** / `0xB9`) |
| System | skills-abilities nested → interaction menu / social UI |
| Date | 2026-08-05 (MEGA-074 refresh) |
| Canonical name | `UI_InteractionMenu_AddIgnoreToggle` |
| Ghidra name | `FUN_0082f8d0` |
| Prior alias | `UI_int_btn_generic_0082f8d0` (plate-only; retired) |

---

## Machine-level notes

- **Live decompile ≡ raw CF** (MEGA-074 2026-08-05 ≡ frozen 2026-07-23).
- Prefer assembly for ABI: decompiler shows `void` / `in_EAX` / `unaff_EDI` — both registers are real parent-passed operands.
- **EAX** = target object* (entry `cmp [eax+0x6b4],1`).
- **EDI** = menu host* (stores at `+0x560` / `+0x56c`; `this` for `vtbl+0x444`).
- **ret 0** (`C3`); ESI saved across body for list-host then button temp.
- Direct callee: `FUN_00573af0` — ignore-list linear name lookup (`_stricmp` on entry `+0x10`; vector `[+0x38,+0x3c)`).
- Parent: dualed `UI_InteractionMenu_PopulateTargetButtons_Inferred` (`FUN_0082fe20`) — two call sites, both pack orders.
- Sibling twin (same shape): `FUN_0082f9a0` Friends toggle (ids `0x11177`/`0x1117a`; lookup `FUN_00573a30`).

## ABI

```c
// Parent-preserved registers (not MSVC thiscall):
//   EAX = target object*
//   EDI = interaction menu host*
//   ret 0
void UI_InteractionMenu_AddIgnoreToggle(void /* regs */);
```

## Pseudocode (annotated)

```c
// aa_0082f8d0 @ 0x0082f8d0
// EAX = target; EDI = menu host
void UI_InteractionMenu_AddIgnoreToggle(void)
{
  // Gate: same +0x6B4 numeric family as convoy pack (0082fb30) / friends (0082f9a0).
  // Enter if target.field < 1 OR local.field >= 1.
  if (!(*(int *)(target + 0x6b4) < 1 ||
        *(int *)(DAT_00d1b6d8 + 0x6b4) >= 1)) {
    return;
  }

  // DAT_00d1da2c = social-list host (also used by friends twin with different lookup).
  // Ignore path uses FUN_00573af0 on this host (vector @ +0x38 / entry name @ +0x10).
  if (DAT_00d1da2c != 0) {
    // MSVC adjustor thiscall: resolve secondary vtbl at target+4 chain, slot +0x160.
    // Returns char* display/login name used as ignore-list key.
    name = target.vtbl_adjustor(+0x160)();

    // thiscall FUN_00573af0(ECX=socialHost, stack=name):
    // walk [+0x38,+0x3c) pointer vector; _stricmp(entry+0x10, name); return entry* or 0.
    if (FUN_00573af0(DAT_00d1da2c, name) != 0) {
      // Already ignored → Remove button
      btn = menu.vtbl+0x444("i_m_int_2d_btn_generic.xml", 0, 0x1117c, 0xFFFFFFFF);
      if (btn != 0) {
        btn.vtbl+0x1d8("Remove from Ignore", 1, 1);
      }
      menu[+0x56c] = btn;   // EDI[0x15b]
      return;
    }
  }

  // Not on list (or no list host) → Add button
  btn = menu.vtbl+0x444("i_m_int_2d_btn_generic.xml", 0, 0x11179, 0xFFFFFFFF);
  if (btn != 0) {
    btn.vtbl+0x1d8("Add to Ignore", 1, 1);
  }
  menu[+0x560] = btn;       // EDI[0x158]
}
```

## Sealed constants

| Item | Value |
|---|---|
| XML plate | `i_m_int_2d_btn_generic.xml` @ `0x00a721ac` |
| Remove label | `"Remove from Ignore"` @ `0x00a72120` |
| Add label | `"Add to Ignore"` @ `0x00a72110` |
| Remove id | `0x1117c` |
| Add id | `0x11179` |
| Remove slot | host `+0x56c` |
| Add slot | host `+0x560` |
| Local player | `DAT_00d1b6d8` |
| Social / ignore list host | `DAT_00d1da2c` |

## Call graph (OWN unit)

```
FUN_0082fe20  UI_InteractionMenu_PopulateTargetButtons_Inferred  [dualed]
  ├─ order A (flag==0): FUN_0082f8d0 @ 0x0082fef6 → Friends → Tell → …
  └─ order B (flag!=0): … → Friends → FUN_0082f8d0 @ 0x0082feea
       └─ FUN_00573af0  ignore-list name lookup  [undualed]
```

## Open questions

1. Product / MSVC class name for menu host and `DAT_00d1da2c` type.
2. English for object `+0x6B4` (rank/level-like gate shared with sibling packs).
3. Dual of `FUN_00573af0` / name-getter `vtbl+0x160` (not OWN).
4. Runtime / bit-exact / differential (Terminal false).
