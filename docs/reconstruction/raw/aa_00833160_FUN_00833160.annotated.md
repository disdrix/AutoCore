# Annotated low-level: ShopVehObject_SwapSelectState_ESI_EDI_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00833160` |
| VA | `0x00833160`–`0x00833383` exclusive (**547 B** / `0x223`) |
| Ghidra name | `FUN_00833160` |
| Canonical name | `ShopVehObject_SwapSelectState_ESI_EDI_Inferred` |
| System | shopveh 2d list / object select-state swap |
| Dual | WQ9J-D 2026-08-05 — **accept-with-gaps** |

---

## Signature

```c
// ESI = objA*, EDI = objB*  (caller-preloaded register pair)
// returns 0 if objB==null else 1
uint8_t ShopVehObject_SwapSelectState_ESI_EDI_Inferred(void);
```

| Formal | Source | Conf |
|---|---|---|
| objA | ESI (unaff in decompiler; caller `MOV ESI,[host+0x50c+…]`) | **High** |
| objB | EDI (`TEST EDI,EDI` first) | **High** |
| return | AL `0` / `1` | **High** |
| stack args | none (`RET`) | **High** |

---

## Control flow

1. **Null B:** if `EDI == 0` → `XOR AL,AL; RET` (return 0).
2. **Dword swaps** (temp via regs) between ESI/EDI at:
   - `+0x508`, `+0x50C`, `+0x5D4`, `+0x298`.
3. **Byte field swaps** involving `+0x4FC`, `+0x5D0`, `+0x5DC`, `+0xBC`.
   - Decompiler collapses this chain into repeated writes to dword-index `0x13f` — **assembly is authority** (see raw re-verify).
4. **Nested XformState three-way swap at `+0x510`:**
   - `temp = FieldBlock_CopyFrom_ClearFlags0xBC(stack, ESI+0x510)` (`FUN_00416240`)
   - `XformState_AssignSelectiveByFlags(ESI+0x510, EDI+0x510)` (`FUN_00416160`)
   - `XformState_AssignSelectiveByFlags(EDI+0x510, temp)` (`FUN_00416160`)
5. **16-byte block swap** at `+0x5E0..+0x5EC` (4 dwords via stack temp).
6. **Pair swap** `+0x500` / `+0x504`.
7. **Optional nested pos cross-set:**
   - If `ESI->vtbl[+0x1a0]()` and `EDI->vtbl[+0x1a0]()` both non-null:
     - Let `pB = *(EDI_vtbl_1a0() + 8)`, `pA = *(ESI_vtbl_1a0() + 8)`.
     - `FUN_0040cf90(EDI_sub, pA+0x90)` then `FUN_0040cf90(ESI_sub, pB+0x90)` (position set + dirty/gen — not OWN).
8. **Refresh:** `EDI->vtbl[+0x3bc]()`; `ESI->vtbl[+0x3bc]()`.
9. Return `1`.

---

## Callers / callees

| Dir | Targets |
|---|---|
| Caller (1) | `FUN_0083ac90` @ `0x0083ad72` — reorders 5-slot object window at host `+0x50c` under shopveh list scroll/index logic |
| Callees | `FUN_00416240`, `FUN_00416160`×2, `FUN_0040cf90`×2, vtbl `+0x1a0`, vtbl `+0x3bc` |

---

## Gaps

- Product class demangle for ESI/EDI objects (shopveh list vehicle objects — co-location only).
- English names for each swapped offset.
- Exact meaning of vtbl `+0x1a0` / `+0x3bc`.
- Byte-swap intermediate order (offsets sealed; intermediate temps only fully sealed via assembly listing).
- Runtime / bit-exact / differential.

---

## Naming

`ShopVehObject_SwapSelectState_ESI_EDI_Inferred` — sole caller under shopveh list host; structural select-state + nested xform swap. Scaffold long `Named_CalleeOf_…shopveh…` plate is co-location, not a product symbol at this VA. `_Inferred` until RTTI/string proof.
