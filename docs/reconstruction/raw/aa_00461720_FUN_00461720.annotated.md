# Annotated low-level: FUN_00461720 / StdVector_EraseRange_RcElem28

| Field | Value |
|---|---|
| Stable ID | `aa_00461720` |
| VA | `0x00461720`–`0x0046177F` exclusive (**95 B**) |
| System | MSVC `vector` erase-range, refcounted elem **0x1c** |
| Date | 2026-07-29 (W36-N OWN-ONLY dual) |
| Canonical name | `StdVector_EraseRange_RcElem28` |

## Machine-level notes

- **Bytes win** over decompiler on: stdcall **`RET 0x10`**, four stack formals, EAX as dest into move helper.
- Decompiler shows correct CF and callees; parameter names are structural (`vec`, `outIt`, `first`, `last`).
- Stride **0x1c** sealed by destroy loop `ADD ESI, 0x1C` and by sibling RcElem28 family (resize/insert-n/destroy-range).
- Nested move helper `FUN_00461e60` RC-assigns resource `@+0x10` (AddRef src / Release dest) then copies POD lanes — not dual-owned here.
- Nested Release leaf `FUN_00460d70` — not dual-owned here (see W35-O destroy-range notes).

## ABI (sealed)

```
StdVector_EraseRange_RcElem28(vec*, outIt*, first, last)  // stdcall RET 0x10
  → returns outIt* with *outIt = first
```

| Formal | Stack (entry) | Role |
|---|---|---|
| vec* | `[esp+4]` → EBP | triad `begin@+4` / `end@+8` |
| outIt* | `[esp+8]` | written with `first` |
| first | `[esp+0xC]` → EAX | erase begin (inclusive) |
| last | `[esp+0x10]` → EDX | erase end (exclusive) |

## Control flow

1. If `first == last`: `*outIt = first; return outIt`.
2. `new_end = FUN_00461e60(last, vec->end, first)` with **EAX=first** (dest of tail move).
3. For `cur = new_end; cur != old_end; cur += 0x1c`: `FUN_00460d70(cur)` (EAX=elem).
4. `vec->end = new_end`; `*outIt = first`; return `outIt`.

## Callers

| Caller | Site | Role |
|---|---|---|
| `FUN_004611e0` `StdVector_Resize_RcElem28` | `0x00461293` | shrink: erase `[begin+n*0x1c, end)` |

## Pseudocode (annotated)

```c
// stdcall RET 0x10; stride 0x1c RC element
int* StdVector_EraseRange_RcElem28(
    StdVectorRc28* vec,   // stack
    int* outIt,           // stack
    RcElem28* first,      // stack
    RcElem28* last)       // stack
{
  if (first == last) {
    *outIt = (int)first;
    return outIt;
  }
  // EAX=first dest; move-assign [last, end) → [first, …); return new_end
  RcElem28* new_end = FUN_00461e60(last, vec->end, first);
  RcElem28* old_end = vec->end;
  for (RcElem28* cur = new_end; cur != old_end; cur = (RcElem28*)((char*)cur + 0x1c)) {
    FUN_00460d70(); // EAX = cur; Release resource @+0x10
  }
  vec->end = new_end;
  *outIt = (int)first;
  return outIt;
}
```
