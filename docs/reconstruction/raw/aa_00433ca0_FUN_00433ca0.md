# Raw capture: FUN_00433ca0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00433ca0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00433ca0` |
| **Canonical name** | `FUN_00433ca0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00433ca0(undefined4 param_1,uint param_2)



{

  undefined4 *puVar1;

  uint in_EAX;

  int iVar2;

  undefined4 *puVar3;

  int iVar4;

  int unaff_ESI;

  int unaff_EDI;

  

  if (param_2 < in_EAX) {

    iVar4 = param_2 * 0xc;

    iVar2 = in_EAX - param_2;

    do {

      puVar3 = (undefined4 *)(*(int *)(unaff_EDI + 8) + iVar4);

      if (puVar3 != (undefined4 *)0x0) {

        puVar1 = (undefined4 *)(iVar4 + unaff_ESI);

        *puVar3 = *puVar1;

        puVar3[1] = puVar1[1];

        puVar3[2] = puVar1[2];

      }

      iVar4 = iVar4 + 0xc;

      iVar2 = iVar2 + -1;

    } while (iVar2 != 0);

  }

  return;

}
```

---

## Live re-verify — 2026-07-29 (W28-A OWN)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` + `analyze_function_complete` (+ callers/xrefs) |
| **Program** | `autoassault.exe` base `0x400000` |
| **Body** | `0x00433ca0`–`0x00433cd3` (**52 B**); pad `CC` after plain `RET` |
| **Epilogue** | plain `C3` RET (no stack formals) |
| **Classification** | **leaf** (0 callees) |
| **Callers** | exclusive `FUN_00433c00` (`BitStream_HuffmanAlphabetEnsureCapacity`) @ `0x00433c36` (1 xref) |
| **Decompile** | ≡ raw 2026-07-23 scaffold (same CF; stride `0xC`) |

### Live decompile (2026-07-29)

```c
void __fastcall FUN_00433ca0(undefined4 param_1,uint param_2)

{
  undefined4 *puVar1;
  uint in_EAX;
  int iVar2;
  undefined4 *puVar3;
  int iVar4;
  int unaff_ESI;
  int unaff_EDI;
  
  if (param_2 < in_EAX) {
    iVar4 = param_2 * 0xc;
    iVar2 = in_EAX - param_2;
    do {
      puVar3 = (undefined4 *)(*(int *)(unaff_EDI + 8) + iVar4);
      if (puVar3 != (undefined4 *)0x0) {
        puVar1 = (undefined4 *)(iVar4 + unaff_ESI);
        *puVar3 = *puVar1;
        puVar3[1] = puVar1[1];
        puVar3[2] = puVar1[2];
      }
      iVar4 = iVar4 + 0xc;
      iVar2 = iVar2 + -1;
    } while (iVar2 != 0);
  }
  return;
}
```

### Machine bytes (`read_memory` 52 B)

```
3B D0 73 31 8D 0C 52 03 C9 03 C9 2B C2 53 8B D0 55
8B 47 08 03 C1 74 13 8D 1C 31 8B 2B 89 28 8B 6B 04
89 68 04 8B 5B 08 89 58 08 83 C1 0C 83 EA 01 75 DE
5D 5B C3
```

### Parent register setup at call (`FUN_00433c00` @ `0x00433c36`)

From parent bytes (grow path after `malloc`):

| Step | Effect |
|---|---|
| `mov edi, ecx` | EDI = alphabet container |
| `mov esi, [edi+8]` | ESI = old buffer |
| `mov eax, [edi]` | EAX = live size (`this+0`) |
| `xor edx, edx` | EDX = 0 (start) |
| `mov [edi+8], ebp` | install neu at `container+8` |
| `call FUN_00433ca0` | copy `[0, size)` elems of **0xC** |

### Register contract (sealed from parent + body)

| Reg | Role |
|---|---|
| EDI | container (`+8` = dest buffer ptr, already neu) |
| ESI | source buffer base (old) |
| EAX | end index exclusive (live size when start=0) |
| EDX | start index inclusive (parent forces 0) |
| EBX/EBP | push/pop scratch for 3-dword copy |
| ECX | running byte offset (`i*0xC`) |

Decompiler `__fastcall(param_1,param_2)` is **misleading**: `param_1` unused; `param_2` is EDX start; `in_EAX` is end. True ABI is **custom register** (no stack formals, plain RET). Sister tree helper `FUN_00433c60` uses EBX for container + stride **8**; this unit uses EDI + stride **0xC**.
