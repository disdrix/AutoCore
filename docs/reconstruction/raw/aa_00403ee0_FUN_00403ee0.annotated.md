# Annotated low-level: FUN_00403ee0 → StdTree_Rrotate_Isnil31_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00403ee0` |
| VA | `0x00403ee0`–`0x00403f2f` inclusive (**80 B** / `0x50`) |
| System | MSVC `std::_Tree` right-rotate (isnil @ +0x31) |
| Date | 2026-08-05 (WQ9L-C dual seal) |
| Canonical name | `StdTree_Rrotate_Isnil31_Inferred` |

## Machine-level notes

- Source: raw capture for `aa_00403ee0` + WQ9L-C re-verify append.
- Prefer assembly when decompiler conflicts (RET 4 sealed by listing).
- **ECX** = pivot node `x*`; stack tree/map*; three exits all **`RET 4`**.
- isnil sentinel test only on **y->right** at **`+0x31`** (`80 7A 31 00`).
- Leaf pointer surgery — no callees, no color@+0x30 stores.
- Do **not** merge with isnil2D Rrot `0042a840` or isnil29 Rrot `004192f0` (ABI/offset).

## Semantics (right rotate about x)

```text
y = x->left
x->left = y->right
if !y->right->isnil(+0x31): y->right->parent = x
y->parent = x->parent
if x == root: root = y
else if x was right child of parent: parent->right = y
else: parent->left = y
y->right = x
x->parent = y
```

## Pseudocode (annotated)

```c
// ECX = x*; stack tree*; void; RET 4
void __thiscall FUN_00403ee0(int *x /*ECX*/, int tree /*stack*/)
{
  int y = *x;                              // x->left
  *x = *(int *)(y + 8);                    // x->left = y->right
  if (*(char *)(*(int *)(y + 8) + 0x31) == '\0') {
    *(int **)(*(int *)(y + 8) + 4) = x;    // y->right->parent = x
  }
  *(int *)(y + 4) = x[1];                  // y->parent = x->parent
  if (x == *(int **)(*(int *)(tree + 4) + 4)) {
    *(int *)(*(int *)(tree + 4) + 4) = y;  // root = y
    *(int **)(y + 8) = x;
    x[1] = y;
    return; // RET 4
  }
  int *parent = (int *)x[1];
  if (x == (int *)parent[2]) {
    parent[2] = y;                         // parent->right = y
    *(int **)(y + 8) = x;
    x[1] = y;
    return; // RET 4
  }
  *parent = y;                             // parent->left = y
  *(int **)(y + 8) = x;
  x[1] = y;
  return; // RET 4
}
```

## Callers (OWN context)

| Parent | Sites | Role |
|---|---|---|
| `FUN_00408ed0` `StdTree_EraseAndRebalance_Isnil31_Inferred` | `00409091`, `004090c2`, `0040911f` | RB rebalance R-rot |
| `FUN_00403250` | `00403366`, `0040337a` | insert/rebalance peer |

## Twin / family

| Unit | VA | Role |
|---|---|---|
| L-rotate isnil31 | `0x00403e90` | twin (not OWN this wave) |
| Min isnil31 | `0x00409160` | dualed |
| Max isnil31 | `0x004043e0` | OWN this wave |
| Erase parent | `0x00408ed0` | dualed |

## Open questions

- Product / English map key-value type for isnil@+0x31 node 0x38 family.
- Runtime / bit-exact / differential.
