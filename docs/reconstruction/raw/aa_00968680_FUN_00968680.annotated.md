# Annotated low-level: GfxBufferedViewList_ClearField184_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00968680` |
| VA | `0x00968680` |
| Canonical | `GfxBufferedViewList_ClearField184_Inferred` |
| System | graphics / gfxBufferedView global list (Device Reset success) |
| Date | 2026-08-04 (W38-Z) |

## Machine-level notes

- **Global list**: `DAT_00d1ee30` is circular-list sentinel for product **gfxBufferedView** instances (W37-I ctor `FUN_00968a50` always inserts `this`).
- **Node layout**: `next@+0`, (prev@+4 unused here), **payload\*** `@+8`.
- **Payload field**: write **0** to `payload+0x184`. Ctor defaults `+0x180/+0x184 = 1` (W37-I) — this clear runs on successful Device Reset.
- **No callees**, plain `RET`, no stack formals.
- Sibling `FUN_009686e0` (pre-Reset / lost path) walks same list with different payload gate; not OWN.
- Sole call site: post-success arm of `FUN_0075eff0` (`gfxDevice.cpp` Device Reset) after factories re-init.

## Pseudocode (annotated)

```c
// Walk global gfxBufferedView circular list; clear field +0x184 on each live payload.
void GfxBufferedViewList_ClearField184_Inferred(void)
{
  ListNode *sentinel = DAT_00d1ee30; // head node
  for (ListNode *n = (ListNode *)sentinel->next; n != sentinel; n = (ListNode *)n->next) {
    void *view = n->payload; // n[2]
    if (view != NULL) {
      *(uint32_t *)((uint8_t *)view + 0x184) = 0;
    }
  }
}
```

## Open questions

- Product English for field `gfxBufferedView+0x184` (ctor init 1 → post-reset 0).
- Whether every node payload is always a full `gfxBufferedView` (size 0x198) vs base `GfxView`.
