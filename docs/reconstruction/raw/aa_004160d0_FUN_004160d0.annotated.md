# Annotated low-level: FUN_004160d0 → CWndVehicle_ScalarDeletingDtor

| Field | Value |
|---|---|
| Stable ID | `aa_004160d0` |
| VA | `0x004160d0`–`0x004160ee` exclusive (**30 B** / `0x1E`) |
| System | MSVC scalar-deleting destructor / `CWndVehicle` |
| Date | 2026-08-05 (WQ9I-F dual) |

## Machine-level notes

- Source: raw capture + WQ9I-F re-verify (`read_memory` full body + RTTI COL).
- Entry: `56 8B F1` → ESI = this (ECX).
- Complete dtor `FUN_00834520` called thiscall (no push of this; callee reads ECX).
- flags bit0 gate: `F6 44 24 08 01` / `74 09`.
- Optional free: push ESI → `operator_delete` → `add esp,4`.
- Return this: `8B C6 5E C2 04 00`.
- Ghidra "noreturn" on `operator_delete` is false (epilogue present).
- vtbl[0]: dword @ `0x00a6fbcc` = `0x004160d0`.
- RTTI: COL `0x00ab5d3c` (vtbl−4 @ `0x00a6fbc8`) → `.?AVCWndVehicle@@`.

## Pseudocode (annotated)

```c
// ECX=this; stack flags; RET 4; returns this*
void* __thiscall CWndVehicle_ScalarDeletingDtor(void* self, uint8_t flags)
{
  CWndVehicle_CompleteDtor(self);   // FUN_00834520
  if ((flags & 1) != 0) {
    operator_delete(self);          // false-noreturn in Ghidra
  }
  return self;
}
```

## Open questions

- Full member map of complete dtor `FUN_00834520` (not owned).
- Runtime / bit-exact / differential — open (no Launcher).
