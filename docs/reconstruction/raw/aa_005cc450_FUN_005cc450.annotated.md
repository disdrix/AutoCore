# Annotated low-level: CVOGHBAI_InvokeVtbl4c_AimOffset_Flag1

| Field | Value |
|---|---|
| Stable ID | `aa_005cc450` |
| VA | `0x005cc450`–`0x005cc45b` (12 B) |
| Ghidra | `FUN_005cc450` |
| Canonical | `CVOGHBAI_InvokeVtbl4c_AimOffset_Flag1` |
| System | npc-ai / HBAI / aim-offset dispatch |
| Date | 2026-07-29 (W28-L) |

## Machine-level notes

- **ABI:** `__thiscall`; ECX = HBAI `this`; **0** stack formals on thunk; plain `RET`.
- **Body (12 B):** load vtbl; `PUSH 1`; `LEA EDX,[ECX+0x40]`; `PUSH EDX`; `CALL [EAX+0x4C]`; `RET`.
- **Args to virtual:** pointer `this+0x40` (aim-offset vec4) + constant **1**.
- **Sibling evidence:** `CVOGHBAI_UpdateAimOffset_Inferred` (`0x005cedf0`) stores aim vec4 at `this+0x40..+0x4c`.
- **Call sites:** DoLogic fallthroughs when path/mode byte non-zero (Driver ×3, ProfileType4, CreatureBase).
- **Contrast:** other DoLogic arms call the same vtbl slot with a host-derived scalar (`vtbl+0x1a0` result) — different formal packing; this unit always uses aim storage + flag 1.

## Pseudocode (bytes-faithful)

```c
// thiscall; ret 0
void CVOGHBAI_InvokeVtbl4c_AimOffset_Flag1(void *self)
{
  void **vtbl = *(void ***)self;
  void (*fn)(void *, void *, int) = (void (*)(void *, void *, int))vtbl[0x4c / 4];
  fn(self, (char *)self + 0x40, 1);
}
```

## Open questions

- Product English for virtual at **vtbl+0x4c** (path goal / move-to / aim apply).
- Semantic of flag **1** vs other call-site formals.
- Exact layout of the 16 B at `+0x40` consumed by the virtual (vec4 sealed by aim helper).
- Runtime / bit-exact — open.
