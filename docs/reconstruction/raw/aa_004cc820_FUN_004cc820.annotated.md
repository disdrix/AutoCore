# Annotated low-level: FUN_004cc820 → MatchTargetEval_DefaultReturnFalse_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004cc820` |
| VA | `0x004cc820`–`0x004cc824` inclusive (**5 B**) |
| System | `missions-progression` (MatchTarget evaluator default) |
| Date | 2026-08-05 (MEGA-030 annotated refresh) |
| Canonical (proposed) | `MatchTargetEval_DefaultReturnFalse_Inferred` |
| Ghidra | `FUN_004cc820` |

## Machine-level notes

- Source: raw capture `aa_004cc820` + MEGA-030 live re-verify.
- Prefer assembly / `read_memory` when decompiler conflicts — here decompiler shows `return 0` but **elides `RET 8`**.
- Shared **default MatchTarget evaluator** virtual: always fails match (`AL=0`), cleans two stack formals.
- Installed in many objective/eval vtables (DATA); MI adjustor thunks JMP here after rebasing `ECX`.
- Dualed parent walker: `CVOGObjective_MatchTargetEvaluators` (`0x0059d9c0`) calls `vtbl+0x40` with this ABI.

## Authoritative assembly

```text
004cc820  32 C0        XOR  AL, AL      ; return false
004cc822  C2 08 00     RET  8           ; pop argA + argB (8 bytes)
```

Body hex: `32 c0 c2 08 00` then `CC` pad.

## Pseudocode (annotated; ABI corrected)

```c
// __thiscall: ECX = evaluator* (unused)
// stack: argA, argB (unused; typically character / world from ForInteract walker)
// AL = 0; RET 8
uint8_t __thiscall MatchTargetEval_DefaultReturnFalse_Inferred(
    void* /*this*/,
    void* /*argA*/,
    void* /*argB*/)
{
    return 0; // XOR AL,AL
    // RET 0x8
}
```

## Control flow

```
entry:
  AL = 0
  return (clean 8 stack bytes)
```

No branches. No callees. No memory traffic. No SEH.

## Call graph / xrefs

| Kind | Count | Notes |
|---|---|---|
| Callees | 0 | leaf |
| Function-graph callers | 0 | |
| JMP adjustor thunks | 6 | `004cc819`, `0053be89`, `005a9bc9`, `005c9523`, `00605fc6`, `00608d89` |
| DATA vtbl installs | 80 | dense multi-type default |

### Adjustor pattern (sample `0x0053be89`)

```text
SUB ECX, dword ptr [ECX-4]   ; vbtable / delta
SUB ECX, 0xc90               ; fixed this-adjust
JMP 0x004cc820               ; shared default body
```

## Role in MatchTarget family

| Path | Slot | Notes |
|---|---|---|
| ForInteract walker `0059d9c0` | eval `vtbl+0x40` | first true wins; empty/all-false → 0 |
| Slot44 sibling `0059da10` | eval `vtbl+0x44` | same ABI; pick/hover |
| UseObject matcher `00525bd0` | `vtbl+0x38` | different slot; real MatchTarget on some types |
| UseItem MatchTarget `0060d7f0` | `+0x38` | **not** this stub |
| Collect/Kill/Deliver +0x40 | → **this stub** | dualed sample: always false |

## Open questions / gaps

- Product/PDB demangle for evaluator interface method English.
- Exhaustive per-vtbl slot map (which types install at +0x40 vs +0x44 vs other).
- Runtime Confirmed / bit-exact / differential — open (Terminal false).
