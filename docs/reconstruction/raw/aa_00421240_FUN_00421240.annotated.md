# Annotated low-level: OleDb_EnsureColumnInfoAndEnterBindings_Inferred (`FUN_00421240`)

| Field | Value |
|---|---|
| Stable ID | `aa_00421240` |
| VA | `0x00421240`–`0x00421358` |
| Ghidra name | `FUN_00421240` |
| Structural name | `OleDb_EnsureColumnInfoAndEnterBindings_Inferred` |
| System | COM / OLE DB accessor setup |
| Date | 2026-07-29 (W25-C OWN dual; prior scaffold 2026-07-23) |
| Evidence | `decompile_function` + `read_memory` + IID GUID bytes + sibling seals `aa_00421362`/`aa_0042139b`/`aa_00421677` |

---

## Machine-level notes

- MSVC SEH: `push -1; push LAB_009bc310; …`; try levels 0 / 1 / 2 / −1 observed.
- **`__thiscall`**: ECX = source `IUnknown*`; stack `ctx*`; fail paths **`ret 4`**; success **`jmp 0x00421362`** (shared frame with sealed children — decompiler may render as call).
- QI #1 always: `source->QueryInterface(IID_IAccessor, &pAcc)` with `DAT_00a860ac` = `{0C733A8C-2A1C-11CE-ADE5-00AA0044773D}`.
- Fast path: if `ctx+0x14 != 0` already has column-info, set `ctx+0x1c = 1` and skip GetColumnInfo.
- Slow path: QI `IID_IColumnsInfo` (`DAT_00a85ffc` = `{0C733A11-2A1C-11CE-ADE5-00AA0044773D}`), call **vtbl+0x0C** (`GetColumnInfo`) with outs at `ctx+0x0C/0x14/0x18`, set `ctx+0x1c = 0`, Release IColumnsInfo.
- Alloc: SEH level 2; `new[](colCount * 0x34)` → `[ebp-0x1C]` temp **DBBINDING** array (0x34 = 52 on x86); jmp into `OleDb_AllocBindingsBuf_EnterBuild_Inferred`.
- IAccessor pointer kept live across the join for later CreateAccessor finalize (child chain).

---

## Pseudocode (annotated)

```c
// __thiscall  ECX = IUnknown* source;  [esp+4] = OleDbAccessorBuildCtx*
// success: jmp shared frame @ 0x00421362 (does not return here)
// fail: ret 4, EAX = HRESULT
HRESULT OleDb_EnsureColumnInfoAndEnterBindings_Inferred(IUnknown *source, Ctx *ctx)
{
  // SEH frame omitted
  IUnknown *pAcc = NULL;
  HRESULT hr = source->QueryInterface(IID_IAccessor, (void **)&pAcc);  // DAT_00a860ac
  if (hr < 0) {
    if (pAcc) pAcc->Release();
    return hr;
  }

  if (ctx->colInfo != NULL) {                 // +0x14
    ctx->columnsPreSupplied = 1;              // +0x1c
    goto alloc_and_enter;
  }

  IColumnsInfo *pCols = NULL;
  hr = source->QueryInterface(IID_IColumnsInfo, (void **)&pCols);  // DAT_00a85ffc
  if (hr >= 0) {
    // vtbl+0x0C == GetColumnInfo
    hr = pCols->GetColumnInfo(&ctx->colCount, &ctx->colInfo, &ctx->strings); // +0x0C,+0x14,+0x18
    if (hr >= 0) {
      ctx->columnsPreSupplied = 0;
      if (pCols) pCols->Release();
      goto alloc_and_enter;
    }
  }
  if (pCols) pCols->Release();
  if (pAcc) pAcc->Release();
  return hr;

alloc_and_enter:
  // SEH try level = 2
  void *tempBindings = operator_new__(ctx->colCount * 0x34);  // DBBINDING[colCount]
  // store tempBindings in [ebp-0x1C]; jmp FUN_00421362 (shared EBP/EBX/SEH)
  return OleDb_AllocBindingsBuf_EnterBuild_Inferred(/* shared frame */);
}
```

---

## Control-flow diagram

```
QI IAccessor → fail? cleanup+ret hr
ctx+0x14 != 0 ?
  yes: flag+0x1c=1 → ALLOC
  no:  QI IColumnsInfo → GetColumnInfo(count,info,strings)
       ok? flag+0x1c=0; Release cols → ALLOC
       fail: Release cols+acc; ret hr
ALLOC:
  SEH=2; new[](colCount*0x34) → temp
  jmp 0x00421362
```

---

## Open gaps

1. Ghidra lists **0 direct callers** — call surface may be function-pointer / untyped; residual.
2. Product/PDB name for ctx / owning DbLoad helper (chain under Skill_DbLoad / COM loader).
3. Exact ownership free for GetColumnInfo allocations when `flag==0` (later teardown units).
4. Runtime / bit-exact deferred.
