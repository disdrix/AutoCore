# Annotated low-level: FUN_0075d3c0 → AssManager_EnsureAssPreloader_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0075d3c0` |
| VA | `0x0075d3c0`–`0x0075d466` (**166 B**) |
| System | assets / assManager + assPreloader |
| Date | 2026-07-29 (W33-O re-verify) |
| Canonical | `AssManager_EnsureAssPreloader_Inferred` |
| Ghidra | `FUN_0075d3c0` |
| Reject | scaffold `Named_CalleeOf_Named_CalleeOf_Named_Client_InitInstance_0075d3c0` |

## Machine-level notes

- **ABI:** one stack arg `host*` (`MOV EBP,[ESP+…]`); **`RET 4`**. Decompiler void ok; Ghidra signature may show bare `void`.
- **Gate:** `*(host+0x6c)==0` only path that allocates.
- **Alloc:** `operator_new(0x114)` + `FUN_00970cf0` (AssPreloader ctor) → store at `host+0x6c`.
- **Seed:** circular list sentinel at `host+8` (same list AssManager_SweepTrackedAssets walks). Each node: key dword at `node+8` + flag byte `1` → locked push into **preloader `+0xAC`** via `FUN_0043e8f0` + `FUN_0043e7f0` (`GuardedVector_PushBackLocked_Stride2_U32U8`, W32-N).
- **Callers:** host ctor `FUN_0075d470` (zeros `+0x6c` then ensure); preload-enable toggle `FUN_00934420` on `DAT_00d1f050`.
- Prefer bytes for pack formals (decompiler omits EDI/container and PodU32U8 pair).

## Pseudocode (annotated)

```c
// stack host*; RET 4
void AssManager_EnsureAssPreloader_Inferred(void *host)
{
  if (*(void **)((char*)host + 0x6c) != 0)
    return;

  void *mem = operator_new(0x114);
  void *pre = mem ? FUN_00970cf0(mem /*, host ctx */) : 0;
  *(void **)((char*)host + 0x6c) = pre;

  // circular list head at host+8
  int *head = *(int **)((char*)host + 8);
  for (int *node = (int*)*head; node != head; node = (int*)*node) {
    // pack PodU32U8 { *(node+8), 1 }; EDI = pre+0xAC
    FUN_0043e8f0(/* pack */);
    FUN_0043e7f0(/* push locked stride-2 U32U8 */);
  }
}
```

## Cross-links

- Preloader family: `AssPreloader_*` W31/W32 duals; ctor `FUN_00970cf0`.
- Secondary host `+0x6c` also referenced by `AssManager_SweepTrackedAssets_Inferred` (W32-O sibling).
- Disable path `FUN_00934420` destroys `*(DAT_00d1f050+0x6c)`.
