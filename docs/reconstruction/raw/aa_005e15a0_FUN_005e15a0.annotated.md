# Annotated low-level: FUN_005e15a0 → StdTree_EraseAndRebalance_Val12

| Field | Value |
|---|---|
| Stable ID | `aa_005e15a0` |
| VA | `0x005e15a0`–`0x005e1856` exclusive (694 B / `0x2B6`) |
| System | std red-black tree / map erase+rebalance (Val12) |
| Date | 2026-07-23 scaffold; 2026-07-29 W30-L dual seal |

## Machine-level notes

- **Role:** MSVC `_Tree::erase(const_iterator)` for Val12 nodes (peer of `004e4130` / `00573250` / `005399f0` / `005ae990`).
- **ABI:** `__thiscall` ECX=map; stack `(outIt*, node*)`; **`ret 8`**.
- **Bytes supersede decompiler** on epilogue: after `operator_delete`, **size--** (if size>0), `*outIt = successor`, SEH restore, `ret 8`. Ghidra plate end `005e1826` is false noreturn truncate.
- **Layout Val12 / 0x1c:** left@0, parent@+4, right@+8, value@+0x0c, **color@+0x18**, **isnil@+0x19**.
- **Throw:** isnil → `"invalid map/set<T> iterator"` @ `0x00a152f0` / `DAT_00acc34c`.
- **Callees:** `StdTree_Inc_Val12` (`005ae0b0`), min (`005adfa0`), max (`00418bf0`), L/R rotate Val12 (`00573170`/`00418c10`), `operator_delete`.
- **Caller:** range helper `FUN_005e1990` only (not dualed here).

## Pseudocode (annotated)

```c
// this = MapShell*; outIt / node on stack; ret 8
void __thiscall StdTree_EraseAndRebalance_Val12(MapShell* map, Node** outIt, Node* node)
{
  if (node->isnil) throw "invalid map/set<T> iterator";

  FUN_005ae0b0(); // successor prep into side channel used by *outIt

  // splice replacement child (MSVC erase transplant)
  Node* repl = node->left;
  if (!repl->isnil) {
    if (!node->right->isnil) { /* two-child path: use right subtree */ repl = node->right; }
  } else {
    repl = node->right;
  }
  Node* parent = node->parent;
  if (!repl->isnil) repl->parent = parent;

  // rewire parent / root (head->parent)
  // fix leftmost (head->left) via min if needed
  // fix rightmost (head->right) via max if needed

  if (node->color == BLACK) {
    // RB rebalance loop: recolor / Lrotate(00573170) / Rrotate(00418c10)
  }

  operator_delete(node);
  // BYTES (decomp miss): if (map->size > 0) map->size--; *outIt = succ; ret 8;
}
```

## Open questions

- Product/MSVC demangle for map type owned by `FUN_005e1990` / mission shell.
- Runtime / bit-exact rebalance golden.
