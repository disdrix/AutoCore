# Raw capture: FUN_00436900

| Field | Value |
|---|---|
| **Stable ID** | `aa_00436900` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00436900` |
| **Canonical name** | `FUN_00436900` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00436900(undefined4 param_1)

{
  int in_EAX;
  
  if (*(int *)(in_EAX + 0x4044) == 0) {
    FUN_00437050(param_1);
    return;
  }
  FUN_007689e0(param_1);
  return;
}
```

---

## W22-R re-verify append (2026-07-29)

| Field | Value |
|---|---|
| **Agent** | W22-R OWN-ONLY dual A/B |
| **Tools** | `decompile_function` + `read_memory` + `get_function_by_address` / callers / callees / xrefs |
| **Not used** | `disassemble_bytes`; Launcher; parent ledgers |
| **Body bounds** | `0x00436900`–`0x0043691a` (Ghidra) |
| **Size** | **27** bytes (`0x1b`) to inclusive last `ret` |

### Live decompile (re-verify)

Identical control flow to 2026-07-23 raw body:

```c
void __fastcall FUN_00436900(undefined4 param_1)
{
  int in_EAX;
  if (*(int *)(in_EAX + 0x4044) == 0) {
    FUN_00437050(param_1);
    return;
  }
  FUN_007689e0(param_1);
  return;
}
```

### Entry bytes (`read_memory` 32 B @ `0x00436900`)

```
83 B8 44 40 00 00 00   cmp dword ptr [eax+0x4044], 0
75 07                 jne  0x00436910
51                    push ecx
E8 41 07 00 00        call 0x00437050   ; stoChunkReader_ReadU32 (binary)
C3                    ret
57                    push edi          ; @0x00436910
51                    push ecx
8B F8                 mov  edi, eax     ; text path needs reader in EDI
E8 C7 20 33 00        call 0x007689e0   ; text-mode uint32 token reader
5F                    pop  edi
C3                    ret
CC CC CC CC CC        int3 padding
```

Rel32 checks:
- `call 0x00437050`: next=`0x0043690f`, disp=`+0x741` → **Confirmed**
- `call 0x007689e0`: next=`0x00436916`, disp=`+0x3320c7` → **Confirmed**

### Sealed ABI (bytes + callees)

| Item | Value | Confidence |
|---|---|---|
| Reader | **EAX** (`cmp [eax+0x4044]`; text `mov edi,eax`) | **Confirmed** |
| Out pointer | **ECX** at entry; `push ecx` → stack formal of both callees | **Confirmed** |
| Epilogue | plain **`ret`** (callee cleans `out` via RET 4) | **Confirmed** |
| Return | **EAX** status from binary ReadU32 or text reader (`0` / `0xffffffff`) | **Confirmed** (decompiler `void` understates) |
| Mode gate | `*(reader+0x4044)==0` → binary; else text | **Confirmed** |

### Callees / callers

| Dir | Symbol | VA |
|---|---|---|
| Callee | `stoChunkReader_ReadU32` / `FUN_00437050` | `0x00437050` |
| Callee | `FUN_007689e0` (text uint32; plate `stoChunk.cpp`) | `0x007689e0` |
| Caller | `gfxGeometryPiece_Unserialize` | `0x0074af60` (4 sites) |
| Caller | `gfxVertexBufferImpl_Unserialize` | `0x0099aa20` |
| Caller | `FUN_0095ef50` | `0x0095ef50` (7 sites) |
| Caller | `FUN_00964690` | `0x00964690` |
| Caller | additional site @ `0x0095ffc2` | (xref) |

### Role

Mode-dispatch wrapper for **single uint32** read on `stoChunkReader`: binary peer already sealed as `stoChunkReader_ReadU32`; text peer is `FUN_007689e0` (token/parse path, same file plate). Named **INFERRED**: `stoChunkReader_ReadU32_ModeDispatch`.
