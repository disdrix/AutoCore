# Annotated low-level: FUN_004a6b50 → StdUninitCopy_BasicStringW

| Field | Value |
|---|---|
| Stable ID | `aa_004a6b50` |
| VA | `0x004a6b50`–`0x004a6bdc` exclusive (**140 B** / `0x8C`) |
| Canonical | `StdUninitCopy_BasicStringW` (**Inferred**) |
| System | containers / MSVC `_Uninit_copy` `basic_string<wchar_t>` stride `0x1c` |
| Date | 2026-07-29 (W36-R OWN-ONLY; scaffold 2026-07-23) |

## Machine-level notes

- Source: raw capture + W36-R `read_memory` / decompile re-verify.
- Prefer bytes when decompiler omits catch residual.
- SEH scope `LAB_009a1181`; per-element state 0/1 around IAT copy-ctor.
- IAT copy-ctor `[0x009C62B4]` → `0x006EAF96`; catch dtor `[0x009C62AC]` → `0x006EB03A`.
- Catch rethrow: `_CxxThrowException(0,0)` @ `0x006A3D60`.
- Sole parent: `StdVector_InsertN_BasicStringW` (`0x004a7ad0`, W34-R) — 4 call sites.
- Char twin: `StdUninitCopy_BasicString` (`0x0046a2c0`, W33-G) — same ABI/shape, char IAT.

## Signature (sealed)

```c
// cdecl; stack first*, last*, dest*; bare RET; returns dest_end in EAX
BasicStringW_0x1c* StdUninitCopy_BasicStringW(
    BasicStringW_0x1c* first,
    BasicStringW_0x1c* last,
    BasicStringW_0x1c* dest);
```

## Pseudocode (happy path)

```c
BasicStringW_0x1c*
StdUninitCopy_BasicStringW(BasicStringW_0x1c* first,
                           BasicStringW_0x1c* last,
                           BasicStringW_0x1c* dest)
{
  // SEH: LAB_009a1181
  BasicStringW_0x1c* dest_cur = dest;
  for (; first != last; first = (BasicStringW_0x1c*)((char*)first + 0x1c)) {
    // EH state = 1
    if (dest_cur != nullptr) {
      basic_string_w_copy_ctor(dest_cur, first);  // thiscall ECX=dest_cur
    }
    dest_cur = (BasicStringW_0x1c*)((char*)dest_cur + 0x1c);
    // EH state = 0
  }
  return dest_cur;
  // catch (bytes only): destroy [dest, dest_cur); _CxxThrowException(0,0);
}
```

## Open questions

- Product/MSVC demangle plate for exact `_Uninit_copy` specialization.
- Runtime / bit-exact / differential.
- Null-dest path residual (unused by sole known parent).
