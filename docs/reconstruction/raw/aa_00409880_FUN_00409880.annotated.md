# Annotated low-level: FUN_00409880 / StdTree_NodeCtor_Val24_Isnil29_EaxThis_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00409880` |
| VA | `0x00409880` |
| System | util / MSVC `_Tree` node construct (Val24 / isnil@+0x29) |
| Date | 2026-08-04 (WQ9J-H dual seal; scaffold 2026-07-23) |

## Machine-level notes

- **Leaf** construct helper — no alloc, no callees.
- Dest node in **EAX** (usercall / MSVC post-`new` pattern); value* in **ECX**; left in **EDX**; parent, right, color on stack; **`RET 0xC`**.
- Stores: L/P/R @+0/+4/+8; 6 dwords @+0x10; color@+0x28; isnil@+0x29 = 0. Gap at +0x0C.
- Sole caller: buynode `FUN_00404180` (new 0x30) used by insert/rebalance `FUN_004069f0` (`"map/set<T> too long"`).
- Twin thiscall: `FUN_004098d0` — same layout, different ABI; **do not merge**.
- Decompiler `__fastcall` label is incomplete; prefer assembly ABI above.
- Prefer assembly when decompiler conflicts on register roles.

## Pseudocode (annotated)

```c
/* __usercall node@<eax>(value*@<ecx>, left@<edx>, parent, right, color) RET 0xC */
void StdTree_NodeCtor_Val24_Isnil29_EaxThis_Inferred(
    /*EAX*/ Node* node,
    /*ECX*/ const uint32_t* value6,
    /*EDX*/ Node* left,
    /*stack*/ Node* parent,
    /*stack*/ Node* right,
    /*stack*/ uint8_t color)
{
  node->left = left;
  node->parent = parent;
  node->right = right;
  /* +0x0C untouched */
  node->val[0] = value6[0];
  node->val[1] = value6[1];
  node->val[2] = value6[2];
  node->val[3] = value6[3];
  node->val[4] = value6[4];
  node->val[5] = value6[5];
  node->color = color;   /* +0x28 */
  node->isnil = 0;       /* +0x29 */
}
```

## Open questions

- Product / PDB demangle for `value_type` (6 dwords / Val24).
- Full dual of parent buynode `FUN_00404180` and insert `FUN_004069f0` (not OWN).
- Runtime / bit-exact / differential.
