# Annotated low-level: Gfx_MorphWeightArray_AllocReplaceUnserialize_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004373b0` |
| VA | `0x004373b0`–`0x00437419` exclusive (**105 B**) |
| Ghidra name | `FUN_004373b0` |
| Canonical name | `Gfx_MorphWeightArray_AllocReplaceUnserialize_Inferred` |
| System | graphics / gfxMorphWeightArrayImpl (MWGT chunk) |
| Date | 2026-08-04 (W38-E) |

---

## Machine-level notes

- Source: raw capture + W38-E re-verify (`read_memory` full body, call-site context, callee strings).
- **Bytes win** over decompiler: EBX host, stack reader, `RET 4`, non-void status return.
- No SEH in this unit (inline ctor after bare `operator_new`).
- Host layout: refcounted slot at **`host+4`** (not `*EBX`).
- OOM path **replaces slot with null** and still invokes unserialize — differs from phy twin `0x004372a0`.
- Loop callers step `EBX += 8` (array of 8-byte nodes with payload/slot at +4).

## ABI (sealed)

```c
// EBX = host*  (slot at host+4)
// stack = stoChunkReader* reader
// RET 4
// return status (uint32_t) from FUN_0073efc0
uint32_t Gfx_MorphWeightArray_AllocReplaceUnserialize_Inferred(
    /*EBX*/ void* host,
    /*stack*/ void* reader);
```

## Pseudocode (annotated)

```c
uint32_t Gfx_MorphWeightArray_AllocReplaceUnserialize_Inferred(void* host /*EBX*/, void* reader /*stack*/)
{
  GfxMorphWeightArray* obj = (GfxMorphWeightArray*)operator_new(0x1c);
  if (obj) {
    obj->refcount = 0;
    obj->vtbl = PTR_FUN_00aa2f48;
    obj->f08 = obj->f0c = obj->f10 = obj->f14 = obj->f18 = 0;
  }

  if (obj) {
    if (++obj->refcount == 1) {
      obj->vtbl->on_first_ref(obj);  // FUN_0056f570 nop
    }
  }

  GfxMorphWeightArray* old = *(GfxMorphWeightArray**)((char*)host + 4);
  if (old) {
    if (--old->refcount == 0) {
      old->vtbl->destroy(old);  // FUN_00464890
    }
  }
  *(void**)((char*)host + 4) = obj;  // even if obj == null

  // FUN_0073efc0: ECX=reader, stack=obj; opens MWGT chunk
  return Gfx_MorphWeightArray_Unserialize_MWGT(reader, obj);  // FUN_0073efc0
}
```

## Control flow

```
new(0x1c)
if obj: inline ctor (vtbl + zeros)
if obj: AddRef (+ first-ref vfunc)
Release *(host+4) if any
*(host+4) = obj   // including null
status = unserialize(reader, obj)
return status
```

## Cross-links

| Related | VA | Role |
|---|---|---|
| Unserialize | `0x0073efc0` | MWGT reader (unowned) |
| First-ref nop | `0x0056f570` | vtbl+4 shared |
| Destroy | `0x00464890` | vtbl+8 shared |
| Phy twin pattern | `0x004372a0` | EDI-slot alloc-replace (physics CPDG) |
| gfxBody consumer | `0x00765740` | loop over morph-weight slots |
