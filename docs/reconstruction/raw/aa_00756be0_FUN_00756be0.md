# Raw capture: FUN_00756be0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00756be0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00756be0` |
| **Canonical name** | `FUN_00756be0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __thiscall FUN_00756be0(int param_1,int param_2)

{
  int *piVar1;
  int *_Dst;
  
  piVar1 = *(int **)(param_1 + 0x98);
  _Dst = *(int **)(param_1 + 0x94);
  if (_Dst != piVar1) {
    do {
      if (*_Dst == param_2) break;
      _Dst = _Dst + 1;
    } while (_Dst != piVar1);
    if (_Dst != piVar1) {
      memmove(_Dst,_Dst + 1,(*(int *)(param_1 + 0x98) - (int)(_Dst + 1) >> 2) * 4);
      *(int *)(param_1 + 0x98) = *(int *)(param_1 + 0x98) + -4;
      *(undefined4 *)(param_2 + 0x88) = 0;
      return 0;
    }
  }
  return 0xffffffff;
}
```

---

## Re-verify append — MEGA-121 OWN-ONLY dual 2026-08-05

**Agent:** MEGA-121  
**Tools:** `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + callers/xrefs + `get_assembly_context` @ dualed sites `0x00759f3a` / `0x007918a5` / `0x00756c63` / `0x005767a4`. **No** `disassemble_bytes`. No Launcher.  
**Integrity:** scaffold body above preserved; this section append-only.

### Live decompile ≡ scaffold CF

Live Ghidra decompile **≡** 2026-07-23 raw:

```c
undefined4 __thiscall FUN_00756be0(int param_1,int param_2)
{
  int *piVar1;
  int *_Dst;
  
  piVar1 = *(int **)(param_1 + 0x98);
  _Dst = *(int **)(param_1 + 0x94);
  if (_Dst != piVar1) {
    do {
      if (*_Dst == param_2) break;
      _Dst = _Dst + 1;
    } while (_Dst != piVar1);
    if (_Dst != piVar1) {
      memmove(_Dst,_Dst + 1,(*(int *)(param_1 + 0x98) - (int)(_Dst + 1) >> 2) * 4);
      *(int *)(param_1 + 0x98) = *(int *)(param_1 + 0x98) + -4;
      *(undefined4 *)(param_2 + 0x88) = 0;
      return 0;
    }
  }
  return 0xffffffff;
}
```

CF sealed: load parent child-vector begin/end @ `+0x94`/`+0x98` → linear pointer search for `param_2` → on hit `memmove` compact + shrink end by 4 + zero `child+0x88` + return 0; on miss return `0xffffffff`.

### Body / ABI (bytes + disassembly)

| Fact | Value |
|---|---|
| Body | `0x00756be0`–`0x00756c44` inclusive (**101 B** / `0x65`); pad `CC` then next fn `FUN_00756c50` @ `0x00756c50` |
| Convention | **`__thiscall`**: **ECX** = parent window; stack = child pointer; **`RET 4`** (`C2 04 00`) ×2 exits |
| Return | **EAX** = `0` on erase success; `0xffffffff` if child not found in parent vector |
| Callee | `memmove` via IAT `dword ptr [0x009c652c]` (cdecl; `ADD ESP,0xC` after) |
| Classification | **worker** |

Full live disasm (`disassemble_function`):

```text
00756be0  PUSH ESI
00756be1  MOV ESI,ECX                    ; ESI = parent (this)
00756be3  MOV ECX,dword ptr [ESI+0x98]   ; end
00756be9  MOV EAX,dword ptr [ESI+0x94]   ; begin
00756bef  CMP EAX,ECX
00756bf1  PUSH EDI
00756bf2  MOV EDI,dword ptr [ESP+0xc]    ; child (stack formal after 2 pushes)
00756bf6  JZ  00756c07                  ; empty vector → not found
00756bf8  CMP dword ptr [EAX],EDI
00756bfa  JZ  00756c03
00756bfc  ADD EAX,0x4
00756bff  CMP EAX,ECX
00756c01  JNZ 00756bf8
00756c03  CMP EAX,ECX
00756c05  JNZ 00756c0f                  ; found
00756c07  POP EDI
00756c08  OR  EAX,0xffffffff
00756c0b  POP ESI
00756c0c  RET 0x4
00756c0f  MOV EDX,dword ptr [ESI+0x98]
00756c15  LEA ECX,[EAX+0x4]             ; src = found+1
00756c18  SUB EDX,ECX
00756c1a  SAR EDX,0x2
00756c1d  ADD EDX,EDX
00756c1f  ADD EDX,EDX                   ; byte count = ((end-(found+1))>>2)*4
00756c21  PUSH EDX
00756c22  PUSH ECX
00756c23  PUSH EAX                      ; dst = found
00756c24  CALL dword ptr [0x009c652c]   ; memmove
00756c2a  ADD dword ptr [ESI+0x98],-0x4 ; shrink end
00756c31  ADD ESP,0xc
00756c34  MOV dword ptr [EDI+0x88],0x0  ; clear child parent link
00756c3e  POP EDI
00756c3f  XOR EAX,EAX                   ; return 0
00756c41  POP ESI
00756c42  RET 0x4
```

Entry/exit hex (live `read_memory` 128 B @ `0x00756be0`):

```text
56 8B F1 8B 8E 98 00 00 00 8B 86 94 00 00 00 3B C1 57 8B 7C 24 0C ...
... 5F 83 C8 FF 5E C2 04 00 ... 5F 33 C0 5E C2 04 00 CC CC ...
```

### Offset map (sealed)

| Offset | Role | Evidence |
|---|---|---|
| parent `+0x94` | child vector **begin** (`int**`) | `MOV EAX,[ESI+0x94]`; dualed `gfxUIWindow_CompleteDtor` children walk |
| parent `+0x98` | child vector **end** (`int**`) | `MOV ECX,[ESI+0x98]`; shrink `ADD [ESI+0x98],-4` |
| child `+0x88` | **parent back-pointer** (cleared on detach) | `MOV [EDI+0x88],0`; dualed dtor gates `if +0x88` then call this with `ECX=[child+0x88]` |

### Callers / xrefs (live)

- **24** UNCONDITIONAL_CALL xrefs (inventory `xref_count: 24`).
- **17** named callers from `get_function_callers` + Catch@`0094b325`.
- Dualed parents (assembly authority):

| Site | Parent | ABI at call |
|---|---|---|
| `0x00759f3a` | dualed `gfxUIWindow_CompleteDtor` `FUN_00759de0` | `MOV ECX,[ESI+0x88]`; `PUSH ESI` → ECX=parent, stack=child(self) |
| `0x007918a5` | dualed `CNDUIDialog_TeardownChildHash_Inferred` `FUN_007917c0` | `PUSH ESI`; `MOV ECX,EBP` → ECX=dialog, stack=child |
| `0x00756c63` | peer `FUN_00756c50` (attach / reparent residual) | `MOV ECX,[ESI+0x88]`; `PUSH ESI` then later writes new `+0x88` |
| `0x005767a4` | `FUN_00576790` | `MOV ECX,[global+0x2c]`; `PUSH EAX` (child from `ESI+0x1c`); tests EAX return |

Other CALL sites: `FUN_00753bc0`×2, `FUN_00756d40`×2, `FUN_007919d0`, `FUN_00791bd0`, `FUN_009306d0`, `FUN_009307b0`×2, `FUN_009349b0`, `FUN_00935430`, `FUN_0093e7e0`, `FUN_0094a6a0`×2, `FUN_00576820`, `FUN_00576a10`, plus 3 un-attributed CALL sites in `007919b4` / `00791b14` / `00791bba` range.

### Name decision

Promote **`gfxUIWindow_DetachParent_Inferred`** (partition hint + dualed `gfxUIWindow.cpp` complete-dtor family + clear parent link semantics). Product method demangle open → `_Inferred`.

**Retire:** `Named_CalleeOf_Named_gfxUIWindow_00756be0` (xref-seed scaffold).

**Reject:** thiscall-on-child-only; bare RET; free/delete of child or parent; claim that this walks/destroys children (it only erases one pointer from parent vector).

### Gaps

- Product demangle of exact method name (no RTTI/string on this leaf).
- Dual residual of attach peer `FUN_00756c50` and other non-dualed callers.
- Capacity field (if any beyond begin/end) not touched here — vector uses raw begin/end only.
- Runtime / bit-exact / differential.

