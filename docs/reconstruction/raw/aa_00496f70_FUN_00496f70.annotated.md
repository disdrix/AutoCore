# Annotated low-level: FUN_00496f70 → Class_009c7a1c_Ctor

| Field | Value |
|---|---|
| Stable ID | `aa_00496f70` |
| VA | `0x00496f70`–`0x004970a8` exclusive (**312 B** / `0x138`) |
| System | class / complete-object constructor (vtbl `PTR_FUN_009c7a1c`) |
| Date | 2026-07-29 W29-G |
| Tools | decompile + analyze_function_complete + read_memory (**no** `disassemble_bytes`) |

---

## Machine-level notes

- **ABI:** `__thiscall`; ECX=`this`; one stack formal (parent/context*); return `this`; cleanup **`ret 4`** (`C2 04 00`).
- **SEH:** frame `LAB_009a0d1e`; standard MSVC ctor prologue/epilogue with `ExceptionList`.
- **Vtbl:** `*this = PTR_FUN_009c7a1c` (`C7 06 1C 7A 9C 00`). Vtbl[0] scalar-deleting = `0x004975e0` (W29-F); complete dtor body = `0x004970b0` (W28-M).
- **+4 / +8:** both set to **0x40** (capacity-like pair; zeroed again in complete dtor after vector free).
- **List @ +0x0C:** `LEA EDI,[ESI+0x0C]`; `FUN_00457c60` allocates **0x18** self-linked sentinel → store at **`this+0x10`**; **`this+0x14`=0**. First dword of list object (`this+0x0C`) **not written** by this body (gap).
- **Buffer triples zeroed:** +0x1C, +0x30, +0x40, +0x9C, +0xAC, +0xBC (match complete-dtor free set).
- **Cookie vector ptr @ +0x28:** zero (dtor later destroys if non-null; elem stride 0x28 / dtor `FUN_005b8000`).
- **Floats (SSE):** `DAT_00aaa8a8` = **300.0f** → `+0x54`; `DAT_00aaa6fc` = **25.0f** → `+0x58`; subsequent SSE zeros at float fields through ~`+0x84`.
- **Flags:** `+0x4C..+0x50` pattern `1,0,1,1,1`; copy `*(uint8_t*)(ctx+0x7d)` → `+0x51`; `+0x52=0`, `+0x53=1`; dword `+0x5C=3`.
- **Caller:** sole CODE caller `FUN_004cda90` — `operator_new(0xC8)` then `FUN_00496f70(parent)`; result stored at `parent+0xE898`. Object size **0xC8** fits highest store `+0xC4`.
- **Legacy scaffold name** (`Named_CalleeOf_…Palantir…`) is **narrow** caller-path noise — **do not** promote.

---

## Control flow (sealed)

```
SEH install
*this = PTR_FUN_009c7a1c
this[+4]=this[+8]=0x40
head = StdList_AllocSelfLinkedSentinel_0x18()   // FUN_00457c60
*(this+0x10)=head; *(this+0x14)=0
zero buffer triples + vector ptr + mid fields
flags / floats / ctx+0x7d byte / mode=3
zero remaining buffer tails
return this; ret 4
```

---

## Pseudocode (annotated; offsets in bytes)

```c
// __thiscall; ECX=this; stack: context*; returns this; ret 4
void *Class_009c7a1c_Ctor(void *self, void *ctx)
{
  // SEH LAB_009a0d1e omitted
  *(void **)self = &PTR_FUN_009c7a1c;
  *(uint32_t *)((uint8_t *)self + 0x04) = 0x40;
  *(uint32_t *)((uint8_t *)self + 0x08) = 0x40;

  void *head = FUN_00457c60();           // operator_new(0x18); self-link
  *(void **)((uint8_t *)self + 0x10) = head;
  *(uint32_t *)((uint8_t *)self + 0x14) = 0;

  // zero +0x1C..+0x28 vector and buffer triples (see clean)
  // flags + floats (300.f / 25.f) + copy ctx[0x7d]
  // more zeros for remaining triples
  return self;
}
```

## Gaps

- Product / MSVC demangled class name.
- Semantic labels for 0x40 pair, mode=3, float fields, buffer payloads.
- Whether `this+0x0C` is intentionally uninitialized vs prior zero from `operator_new`.
