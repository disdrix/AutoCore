# Annotated low-level: FUN_0075d610 / NDResourceCache_IsKeyPresent_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0075d610` |
| VA | `0x0075d610`–`0x0075d63f` (**48 B**) |
| System | NDResourceCache / NestedHash find-present |
| Date | 2026-07-23 scaffold; W36-E dual seal 2026-07-29 |

## Machine-level notes

- **Bytes win** over bare Ghidra signature (`undefined FUN(void)`).
- **ECX** = NestedHash map (NDResourceCache head / typically `DAT_00d1f050`).
- **Stack** = `const void* key` (key object pointer; `HashMap_FindIterator` hashes `*(char**)key`).
- **EBX/EDI** register formals established for nested find (EDI=map, EBX=key*).
- `param_2` decompiler reuse: starts as key*, becomes node* after find writes out_it.
- Return **AL/EAX** 1 = present with non-null value at `node+0x0C`; 0 = miss or null value.
- Sole callee: `HashMap_FindIterator_Inferred` (`0x0044e8c0`).
- Not insert/create — pure predicate.

## Pseudocode (annotated)

```c
// ECX = NDResourceCache* / NestedHash map*
// stack key*; RET 4; EAX 0/1
uint32_t __thiscall NDResourceCache_IsKeyPresent_Inferred(void* cache, const void* key)
{
  void* it = (void*)key;              // stack slot reused as out_it
  HashMap_FindIterator_Inferred(&it); // EDI=cache, EBX=key*
  if (it != *(void**)((char*)cache + 0x08) && *(void**)((char*)it + 0x0C) != 0)
    return 1;
  return 0;
}
```

## Open / residual

- Product/PDB method English (IsLoaded / Contains / HasResource).
- Key domain (interned string object vs raw name) shared with FindIterator residual.
- Runtime / bit-exact.
