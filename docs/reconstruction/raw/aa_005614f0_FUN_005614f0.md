# Raw capture: FUN_005614f0

| Field | Value |
|---|---|
| **Stable ID** | `aa_005614f0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005614f0` |
| **Canonical name** | `FUN_005614f0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_005614f0(int param_1,undefined1 *param_2,int param_3)

{
  if (*(char *)(param_1 + 300) == '\x01') {
    if (*(uint *)(param_1 + 0xf4) == (*(uint *)(param_1 + 0xf8) & 0x7fffffff)) {
      FUN_005b3370((int *)(param_1 + 0xf0),4);
    }
    *(int *)(*(int *)(param_1 + 0xf0) + *(int *)(param_1 + 0xf4) * 4) = param_3;
    *(int *)(param_1 + 0xf4) = *(int *)(param_1 + 0xf4) + 1;
    *(short *)(param_3 + 6) = *(short *)(param_3 + 6) + 1;
    *param_2 = 0;
    return;
  }
  if (0 < *(int *)(param_1 + 0x24)) {
    FUN_00561320();
  }
  FUN_0055fbf0(param_3);
  FUN_0055f0b0();
  FUN_0055fd80(param_3);
  *param_2 = 1;
  return;
}
```

---

## Re-verify append — W30-J OWN-ONLY (2026-07-29)

**Tools:** Ghidra `decompile_function` + `read_memory` + `get_function_by_address` + xrefs/callers. **No** `disassemble_bytes`.

| Field | Live |
|---|---|
| Body | `0x005614f0`–`0x00561571` (**129 B** / `0x81`); pad `CC` after `ret 8` |
| ABI | **thiscall**; ECX = manager; stack #1 = out byte*; stack #2 = object; void; **`ret 8`** |
| Adjacent | Immediately after sibling `FUN_00561450` (RemoveOrDefer) body end `0x005614ef` |
| Callers (CODE) | `FUN_00561580`, `FUN_005617c0` (SnapshotHostRefsAndUnbind) |
| Callees | `FUN_005b3370`, `FUN_00561320`, `FUN_0055fbf0`, `FUN_0055f0b0`, `FUN_0055fd80` |

### Machine CF (sealed)

**Defer** (`manager+0x12c == 1`):
1. If count@+0xf4 == (cap@+0xf8 & 0x7fffffff): `FUN_005b3370(manager+0xf0, 4)`
2. Push obj onto pointer vector +0xf0
3. `*(short*)(obj+6)++`
4. `*out = 0`; `ret 8`

**Immediate** (else):
1. If `manager+0x24 > 0`: `FUN_00561320()` (flush pending body pairs)
2. `FUN_0055fbf0(this=manager, obj)` — unbind links
3. `FUN_0055f0b0(this=obj)` — compact obj+0x88 pointer list
4. `FUN_0055fd80(this=manager, obj)` — clear obj+8, host detach/cleanup
5. `*out = 1`; `ret 8`

**Contrast RemoveOrDefer (`00561450`):** immediate path there also calls `0062a490` / `005ffff0`, decrements ref@+6, and destroys when ref==0. This unit does **not**.

**Live decompile ≡ 2026-07-23 raw body** for CF stages.

**Call-site machine (`005617c0` @ `0x00561892`):**
`PUSH obj; PUSH &outSlot; MOV ECX, manager; CALL 005614f0`.
