# Annotated low-level: FUN_009841d0 → Fs_ReadPathListFileToStringVec_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_009841d0` |
| VA | `0x009841d0`–`0x00984335` exclusive (**357 B**) |
| System | filesystem / path-list text reader (`stoFileOSFile`) |
| Date | 2026-07-29 (W34-I dual seal) |

## Machine-level notes

- Source: raw capture + W34-I re-verify (`decompile` + `read_memory` + xrefs). **No** `disassemble_bytes`.
- **ABI:** **cdecl** 2 stack formals — `out_string_vec*`, `const char* path`; bare `RET`; returns `out_vec*` in `EAX`.
- Stack host is a `stoFileOSFile` (vtable `0x00a9d84c`); open via `FUN_007669d0` mode **1** (read).
- Line loop: `Tell() < Size()` then `FUN_00766ee0` (read line into `basic_string`) + `StringVec_PushBack`.
- Result materializes via `FUN_00469d10` assign collector → out (both success and fail paths).
- Sole static caller: `Fs_ExpandPathSpecToStringVec_Inferred` `@` branch (`path[0]=='@'` → this(path+1)).

## Pseudocode (annotated)

```c
// cdecl; bare RET; EAX = out_vec*
void* Fs_ReadPathListFileToStringVec_Inferred(void* out_string_vec, const char* path)
{
  // SEH LAB_009af6c0; stack stoFileOSFile + empty StringVec collector + line basic_string
  int open_rc = stoFileOSFile_Open(&file, path, /*mode*/1, /*flags*/0); // FUN_007669d0
  if (open_rc < 0) {
    StringVec_Assign(out_string_vec, &collector); // FUN_00469d10 — empty on fail
    stoFileOSFile_Dtor(&file);                    // FUN_00766750
    StringVec_Tidy(&collector);                   // FUN_00431ae0
    return out_string_vec;
  }

  basic_string line; // default ctor
  while (stoFile_Tell(&file) < stoFile_Size(&file)) {
    stoFile_ReadLine(&file, &line);               // FUN_00766ee0 (CRLF-aware)
    StringVec_PushBack(&collector, &line);        // FUN_004301f0
  }
  StringVec_Assign(out_string_vec, &collector);   // FUN_00469d10
  ~basic_string(&line);
  stoFileOSFile_Dtor(&file);
  StringVec_Tidy(&collector);
  return out_string_vec;
}
```

## Gaps

- Product English demangle / exact source method name on `stoFile` stream helpers.
- Nested `FUN_00766ee0` / `FUN_007669d0` full plates (unowned).
- Runtime / bit-exact / differential.
