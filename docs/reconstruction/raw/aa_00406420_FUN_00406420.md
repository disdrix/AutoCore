# Raw capture: FUN_00406420

| Field | Value |
|---|---|
| **Stable ID** | `aa_00406420` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00406420` |
| **Canonical name** | `CNDHash_Dtor_00a2c2b0` (sealed 2026-08-04; Ghidra still `FUN_00406420`) |
| **System** | container / CNDHash |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00406420(undefined4 *param_1)

{
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  puStack_8 = &LAB_009beaf7;
  local_c = ExceptionList;
  ExceptionList = &local_c;
  *param_1 = &PTR_FUN_00a2c2b0;
  local_4 = 0;
  if (*(char *)((int)param_1 + 0x1d) != '\0') {
    FUN_007a4480(0,"HashError:Destructor, already locked for traversal");
    FUN_007a4480(0,"VOG_DEBUG_STOP");
  }
  FUN_00406fc0();
  local_4 = 0xffffffff;
  FUN_00416e80(param_1 + 8);
  ExceptionList = local_c;
  return;
}
```

---

## Re-verify 2026-08-04 (WQ9H-D OWN-ONLY dual seal)

| Field | Value |
|---|---|
| **Tools** | `batch_decompile` / `analyze_function_complete` + `read_memory` + `disassemble_function` + callers/xrefs + `get_assembly_context`. **No** `disassemble_bytes`. |
| **Live decompile** | ≡ raw body above (same CF / lock log / destroy / freelist teardown) |
| **Body** | `0x00406420`–`0x0040649a` inclusive (**123 B** / `0x7B`); `CC` pad after bare `ret` |
| **ABI** | **`__thiscall`**: **ECX = CNDHash\***; no stack args; **void**; bare **`ret`** (`C3`). Entry `MOV ESI,ECX` |
| **Semantics** | Re-stamp vtbl `00a2c2b0`; if lock `@+0x1d` ≠ 0 log **`HashError:Destructor, already locked for traversal`** + **`VOG_DEBUG_STOP`** (**non-fatal**, continues); `CNDHash_DestroyBucketTable_00a2c2e4` (**EAX=this**); freelist subobject dtor `FUN_00416e80(this+0x20)` |
| **Destroy call** | `MOV EAX,ESI; CALL 0x00406fc0` — matches EAX custom ABI of destroy twin |
| **Freelist teardown** | `LEA EAX,[ESI+0x20]; PUSH EAX; CALL 0x00416e80` — decompiler `param_1+8` is dword-index (`+0x20`) |
| **String imm** | push `0x00a2c270` = `HashError:Destructor…`; push `0x00a15844` = `VOG_DEBUG_STOP` |
| **Callers (1)** | `FUN_00406520` scalar-deleting dtor (vtbl[`00a2c2b0`][0]) @ `0x00406523` — `MOV ESI,ECX; CALL 0x00406420` then optional `operator_delete` if flags&1 |
| **Xrefs** | 1 UNCONDITIONAL_CALL |
| **Name** | `CNDHash_Dtor_00a2c2b0` (vtbl stamp High; product English open) |
| **Rejected scaffold name** | `Named_VOG_DEBUG_STOP_00406420` |
| **Lock behavior** | Soft assert only — **no** throw / early return on locked traversal |
