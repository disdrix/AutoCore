# Annotated low-level: FUN_00440d20 → StdTree_FreeSubtree_Isnil11_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00440d20` |
| VA | `0x00440d20`–`0x00440d54` inclusive (**53 B** / `0x35`) |
| Canonical name | `StdTree_FreeSubtree_Isnil11_Inferred` |
| Ghidra name | `FUN_00440d20` |
| System | skills-abilities (gfxUIWindow tree shell free / isnil@+0x11) |
| Date | 2026-08-05 (MEGA-132 refresh) |
| Partition hint | `StdTree_ClearShell_AtA4` |

## Machine-level notes

- Source: raw capture + MEGA-132 live Ghidra re-verify.
- **Bytes authority** over decompiler: decompile is collapsed right-only + false noreturn on `operator_delete`.
- Real CF is classic MSVC `_Tree` free-subtree: free right recursively, delete node, walk left spine.
- isnil gate @ **`+0x11`** (not +0x19 / +0x1d / +0x29 / +0x3d).
- left @ **`+0`**, right @ **`+8`**, parent unused in body.
- **No** value/payload destroy before delete (POD / trivial value or already-cleared).
- ABI: **ECX** = tree_base (threaded through recursive calls; unused in body); stack = `node*`; **`RET 4`**.

## Pseudocode (byte-corrected)

```c
// ECX = tree_base (threaded); stack node*; RET 4; void
void __fastcall StdTree_FreeSubtree_Isnil11_Inferred(void *tree_base, void *node)
{
  if (*(char *)((int)node + 0x11) != '\0')
    return;

  for (;;) {
    StdTree_FreeSubtree_Isnil11_Inferred(
        tree_base,
        *(void **)((int)node + 8));          /* free right */
    {
      void *left = *(void **)node;           /* left @ +0 */
      operator_delete(node);                 /* cdecl */
      node = left;
    }
    if (*(char *)((int)node + 0x11) != '\0')
      break;
  }
}
```

## Call graph (this VA only)

```
FUN_00759de0  gfxUIWindow_CompleteDtor   [dualed WQ9L-F]
  └─ free root of shell @ +0xA4  →  FUN_00440d20  [OWN MEGA-132]

FUN_00759090  (UI rebuild path)
  └─ free root of head @ +0xA8   →  FUN_00440d20

FUN_00441960  StdTree_EraseAll_ShellA4 (partition MEGA-136 residual)
  └─ full-clear: free head->parent →  FUN_00440d20

FUN_00440d20  self recurse on right
  └─ operator_delete @ 00489822
```

## Open questions

- Product / MSVC demangle for map key/value of isnil@+0x11 family (node size ~0x14).
- Exact English for shell @ gfxUIWindow `+0xA4` element type.
- Runtime / bit-exact / differential (not claimed).
