# Annotated low-level: StdVector_InsertN_Elem0x28_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00408050` |
| VA | `0x00408050`–`0x004082ed` exclusive (**669 B** / `0x29D`) |
| Ghidra | `FUN_00408050` |
| System | util / container (`std::vector` InsertN, POD **elem 0x28**) |
| Date | 2026-08-04 (WQ9H-A dual seal; supersedes 2026-07-23 scaffold notes) |
| Tools | `batch_decompile` + `read_memory` + `analyze_function_complete` + callers/xrefs + `get_assembly_context` |
| **No** | `disassemble_bytes`; Launcher |

## Machine-level notes

- **ABI (sealed):** **ECX** = `VecPod28*`; **EDX** = `const Pod28*` value; stack0 = `where*`; stack1 = `count`; void; **`ret 8`** (`C2 08 00`).
- **Entry:** SEH frame `LAB_009bd120`; `mov ebx,ecx` (this in EBX); snap **10 dwords** (`rep movsd` count=10) of `*value` into `[ebp-0x58]` local template so value may alias buffer during grow.
- **Stride:** `/0x28` via magic `0x66666667`; max element count constant `0x06666666` (`0x6666666`).
- **Growth:** if `cap < size+count`: newCap = cap + (cap>>1) (1.5×), clamped; if still short, `FUN_00437d80()` size re-read + count; `operator_new(newCap*0x28)`; prefix `FUN_00409ae0(begin,where)`; Ufill `FUN_00406e50(local_template)`; suffix `FUN_00409ae0(where,end)`; `operator_delete(old)`; write begin/end/capEnd triad; return.
- **In-place:** if `(end-where)/0x28 < count` — open hole toward end via `FUN_0040a520` then Ufill; else mid-path `FUN_0040a520` + `FUN_00409b20` then shared `FUN_00409b00` assign-fill of hole.
- **count==0:** early no-op (after capacity read).
- **Caller:** sole xref `FUN_00406de0` InsertOne forces `count=1` (`push 1; push where; mov ecx,edi; mov edx,value`).
- **Twin:** InsertN elem12 `004082f0` (thiscall ret 0xC; different Ufill); dword InsertN `004073a0`.

## Pseudocode (meaningful names)

```c
// ECX=vec*, EDX=value*, stack where*, count; ret 8
void StdVector_InsertN_Elem0x28_Inferred(
    VecPod28 *vec, const Pod28 *value, Pod28 *where, unsigned count)
{
  Pod28 tmp;                 // local_58[10]
  memcpy_dwords(&tmp, value, 10);

  Pod28 *begin = vec->begin; // +4
  unsigned cap = begin ? (vec->capEnd - begin) / 0x28 : 0; // +0xC
  if (count == 0) return;

  unsigned size = begin ? (vec->end - begin) / 0x28 : 0; // +8
  if (0x06666666u - size < count)
    FUN_00418130(); // length_error

  if (cap < size + count) {
    // 1.5× grow, then exact size+count floor via FUN_00437d80
    // new buffer: prefix uninit_copy | Ufill count of tmp | suffix uninit_copy
    // delete old; rebind begin/end/capEnd; ret 8
  } else if ((unsigned)((vec->end - where) / 0x28) < count) {
    // near-end: move tail, Ufill hole, end += count*0x28
  } else {
    // mid: open hole with uninit_copy/move helpers, assign-fill, shared plant
  }
}
```

## Call graph

```text
FUN_00406220 push_back slow
  └─ FUN_00406de0 InsertOne+rebind (EDI=vec; count forced 1)
       └─ FUN_00408050  this unit  (ECX=vec, EDX=value, where, 1)
            ├─ FUN_00406e50 UfillN 0x28     (sealed WQ9G-H)
            ├─ FUN_00409ae0 uninit_copy     (residual)
            ├─ FUN_0040a520 relocate helper (residual)
            ├─ FUN_00409b00 / FUN_00409b20  (residual assign/move)
            ├─ FUN_00418130 length_error
            ├─ FUN_00437d80 size() re-read
            └─ operator_new / operator_delete
```

## Open questions

- Product English type for the 0x28 POD element (10 dwords).
- Full dual of uninit_copy / mid-insert assign-fill leaves (roles sealed by CF).
- Bit-exact / runtime realloc capture.
