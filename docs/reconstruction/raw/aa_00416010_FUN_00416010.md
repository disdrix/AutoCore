# Raw capture: FUN_00416010

| Field | Value |
|---|---|
| **Stable ID** | `aa_00416010` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00416010` |
| **Canonical name** | `FUN_00416010` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00416010(uint param_1,int param_2)

{
  basic_string<char,struct_std::char_traits<char>,class_std::allocator<char>_> local_50 [28];
  undefined1 local_34 [40];
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  local_4 = 0xffffffff;
  puStack_8 = &LAB_009bc919;
  local_c = ExceptionList;
  if (0x1fffffffU - *(int *)(param_2 + 8) < param_1) {
    ExceptionList = &local_c;
    std::basic_string<char,struct_std::char_traits<char>,class_std::allocator<char>_>::
    basic_string<char,struct_std::char_traits<char>,class_std::allocator<char>_>
              (local_50,"list<T> too long");
    local_4 = 0;
    FUN_00401a40(local_50);
                    /* WARNING: Subroutine does not return */
    _CxxThrowException(local_34,(ThrowInfo *)&DAT_00acc388);
  }
  *(uint *)(param_2 + 8) = *(int *)(param_2 + 8) + param_1;
  return;
}
```

---

## WQ9I-E live re-verify (2026-08-04) — append only

**Tools:** Ghidra `batch_decompile` + `analyze_function_complete` + `read_memory` + `disassemble_function` + callers/xrefs + `get_assembly_context`. **No** `disassemble_bytes`.

### Live decompile ≡ raw body

Yes — CF/string/throw path identical to 2026-07-23 scaffold.

### Body / ABI (sealed)

| Fact | Evidence |
|------|----------|
| Body | `0x00416010`–`0x0041607e` inclusive (**111 B** / `0x6F`); plain `RET` (`C3`) |
| ABI | **ECX**=count, **EDX**=list*; no stack args; void |
| Max gate | `MOV ESI,0x1fffffff` then `SUB ESI,size`; `CMP ESI,ECX`; `JNC ok` |
| Size field | load/store `[EDX+8]` (`8B 42 08` / `89 42 08` path via `ADD EAX,ECX`) |
| Throw string | push `0x00a1581c` → `"list<T> too long"` (read_memory) |
| ThrowInfo | `DAT_00acc388` (same family as other list Incsize leaves) |
| SEH | `LAB_009bc919` + FS:[0] frame |

### Callers (3 fn / 4 xrefs)

| Site | Parent | Setup |
|------|--------|-------|
| `0x00415deb` | `FUN_00415dd0` | `MOV ECX,1`; `MOV EDX,[ESP+0xc]` (list*); after `FUN_00415ed0` buy node `0x10` |
| `0x00834728` | `FUN_00834700` | `MOV ECX,1`; `MOV EDX,EBX` |
| `0x008dfd85`, `0x008dfdc2` | `FUN_008dfc60` | `MOV ECX,1`; `MOV EDX,ESI` (twice) |

### Role

MSVC-shaped `std::list::_Incsize` with **max `0x1fffffff`** (distinct from sealed twin `StdList_Incsize` @ `0x0043fe60` max `0x3fffffff` and thiscall twin `FUN_00403600` max `0x6666666`).

### Name

`StdList_Incsize_Max1fffffff` (Inferred — plate string + max imm + ECX/EDX ABI).
