# Annotated low-level: CNDHash_ReleaseNodesToFreelist_Owning_009cefd4

| Field | Value |
|---|---|
| Stable ID | `aa_00538120` |
| VA | `0x00538120` |
| Ghidra | `FUN_00538120` |
| Canonical | `CNDHash_ReleaseNodesToFreelist_Owning_009cefd4` (**Inferred**) |
| System | container / CNDHash (family `009cfa64`, node vtbl `009cefd4`) |
| Date | 2026-07-29 (W17-K dual seal) |

## Machine-level notes

- **ABI:** `__fastcall` / `__thiscall` — `ECX = CNDHash* this` (saved to EBX).
- **Role:** Owning bucket-chain reclaim used by FreeBuckets `0x00539090` before table free.
- **Node layout (0x1c, same as Insert `0x0053c560`):** vtbl / freelist link at `+0`, value `+8`, bucket next `+0xc`, key `+0x10`, list links `+0x14/+0x18`.
- **Owning:** `operator_delete(value)` — bare heap free, **not** virtual deleting dtor (`vtbl(1)`). Contrast non-owning reclaim twins.
- **Loop bound:** `i <= *(hash+8)` inclusive → `mask+1` buckets when `+8` is mask (matches Alloc/Insert family).
- Ghidra “Subroutine does not return” on `operator_delete` is a known false positive.

## Pseudocode (annotated)

```c
// ECX = CNDHash* hash  (family FreeBuckets 009cfa64)
void __fastcall CNDHash_ReleaseNodesToFreelist_Owning_009cefd4(CNDHash *hash)
{
  uint i = 0;
  do {
    // bucket = hash->table[i]; node = bucket->head (+4)
    Node *node = *(Node **)(*(uint8_t **)((char *)hash + 0x10)[i] + 4);
    while (node != NULL) {
      Node *next = node->bucketNext;          // +0x0c
      node->vtbl = &PTR_FUN_009cefd4;         // stamp before free path
      if (node->value != NULL) {              // +0x08
        operator_delete(node->value);         // owning value free
      }
      node->value = NULL;
      // intrusive freelist: *node = old_head; hash->freelist = node
      *(void **)node = *(void **)((char *)hash + 0x20);
      *(Node **)((char *)hash + 0x20) = node;
      node = next;
    }
    // clear chain head
    *(void **)(*(uint8_t **)((char *)hash + 0x10)[i] + 4) = NULL;
    i++;
  } while (i <= *(uint *)((char *)hash + 8));  // inclusive upper bound
}
```

## Layout

| Offset | Role |
|-------:|------|
| hash+0x08 | bucket loop upper bound (mask; inclusive) |
| hash+0x10 | bucket pointer table* |
| hash+0x20 | freelist head |
| bucket+4 | chain head |
| node+0 | vtbl, then freelist next |
| node+8 | owned value* |
| node+0xc | bucket chain next |

## Open questions

- Product/PDB English for this reclaim helper (likely private FreeBuckets stage).
- Exact payload type of `value*` (caller-domain; continent unlock hash uses char `+0x534`).
