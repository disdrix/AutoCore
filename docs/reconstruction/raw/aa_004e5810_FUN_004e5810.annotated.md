# Annotated low-level: FUN_004e5810 → Object_CopyTfidVectorAtE7f8_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004e5810` |
| VA | `0x004e5810`–`0x004e5833` exclusive (**35 B** / `0x23`) |
| System | inventory-transfer — object TFID vector materialize at `+0xe7f8` |
| Date | 2026-08-05 (R13-026 OWN dual) |
| Canonical | `Object_CopyTfidVectorAtE7f8_Inferred` |
| Ghidra | `FUN_004e5810` |

## Machine-level notes

- **ABI sealed:** `__thiscall` ECX = host object; stack arg = destination vector shell*; **RET 4** (`C2 04 00`); returns dest in EAX.
- **Offset sealed:** `ADD ECX, 0xe7f8` — source is member vector at object `+0xe7f8` (TFID pair vector; elem stride 8, count `(end-begin)>>3` at callers).
- **Callee:** `FUN_004e53e0` is the real copy-construct worker (not dualed here). This unit is a thin offset-binding wrapper.
- Prefer assembly when decompiler elides the `this` swap: decompile shows `FUN_004e53e0(param_1+0xe7f8)` without making dest-this explicit; bytes show `MOV ECX,ESI` before CALL.
- Spill `MOV [ESP+8],0` zeroes the first PUSH slot after ADD — compiler dead-store; no extra observable store into the object.

## Pseudocode (annotated)

```c
// __thiscall Object_CopyTfidVectorAtE7f8_Inferred(Object* this, VectorU64Shell* out)
// Returns out*. RET 4.
undefined4 __thiscall FUN_004e5810(int param_1 /*this*/, undefined4 param_2 /*out*/)
{
  // Live: ECX = this+0xe7f8 pushed; ECX = out; CALL FUN_004e53e0
  FUN_004e53e0(/*this=*/param_2, /*src=*/param_1 + 0xe7f8);
  return param_2;
}
```

## Call-site pattern (both callers)

```text
LEA  EAX, [ESP+local_shell]
PUSH EAX
MOV  ECX, object            ; often MI+0xa8 under character
CALL FUN_004e5810           ; out = copy of object+0xe7f8
; count:
MOV  ECX, [EAX+4]           ; begin
TEST ECX,ECX / JNZ
MOV  EAX, [EAX+8]           ; end
SUB  EAX, ECX
SAR  EAX, 3                 ; element count (8-byte elems)
; index:
;   elem = *(begin + idx*8) as TFID pair → ResolveObjectTarget
; optional operator_delete on local shell buffer
```

## Open questions

- Product / PDB English for the `+0xe7f8` member (nearby TFID list vs craft-station list vs generic object association list).
- Full dual of copy worker `FUN_004e53e0` / pod copy `FUN_006a2be0`.
- Runtime / bit-exact / differential.
