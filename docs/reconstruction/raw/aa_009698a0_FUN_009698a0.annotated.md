# Annotated low-level: AssPathList_ListBasenames_FilterPush_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_009698a0` |
| VA | `0x009698a0`–`0x00969a4c` (**428 B**) |
| Canonical | `AssPathList_ListBasenames_FilterPush_Inferred` |
| Wave | W34-B (2026-07-29) |

## Source

- Raw capture + W34-B re-verify append for `aa_009698a0`.
- Live decompile + full-body hex + wrapper @ `0x00996aa0`.

## Annotated control flow

```
AssPathList_ListBasenames_FilterPush(path_list, classifier, type_filter)  // EDI=out
  SEH LAB_009afb56
  for path in path_list[begin..end) stride 0x1c:
    pattern = path + "*.*"                         // DAT_00aa1d38; FUN_00416490
    names = empty string-vec
    FUN_0076b3f0(pattern_cstr, &names)             // W33-C basenames
    for name in names:
      handle = FUN_00989e00(this=0x00d1eac8, name)
      classifier->vtbl[+0x10](&handle, &class_id)
      if type_filter == 0 or class_id == type_filter:
        push handle to EDI dword-vec               // 00436ef0 or 0043bfb0
    destroy names; ~pattern
  return 0                                         // RET 0xC
```

## Wrapper `0x00996aa0`

```
stdcall(classifier, type_filter, out_vec):
  EDI = out_vec
  call this(*DAT_00d1f1fc, classifier, type_filter)
  ret 0xC
```

## Sealed facts

| Fact | Evidence |
|---|---|
| RET 0xC | epilogue `C2 0C 00` |
| EDI out-vec | wrapper + `[edi+4/8/c]` body uses |
| `"*.*"` | `DAT_00aa1d38` = `2a 2e 2a 00` |
| Basename list | `FUN_0076b3f0` (W33-C) |
| Resolve this | `mov ecx, 0x00d1eac8` before `FUN_00989e00` |

## Gaps

- Product plate for classifier `vtbl[+0x10]`.
- Product plate for `FUN_00989e00` / handle type.
- Full AssManager English for `DAT_00d1f1fc` list role.
- Runtime / bit-exact / differential.
