# Annotated low-level: GuardedVector_PushBackLocked_Stride2_U32U8 (`FUN_0043e7f0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0043e7f0` |
| VA | `0x0043e7f0`–`0x0043e84d` (**94 B**) |
| System | STL / guarded ring-deque container (locked push wrapper) |
| Date | 2026-07-23 scaffold; W32-N seal 2026-07-29 |

## Machine-level notes

- Optional CS wrapper around sealed `GuardedVector_PushBack_Stride2_U32U8` (`0x0043e9e0`, W31-J).
- **EDI** = container (decompiler `unaff_EDI`); stack `value*`; **`RET 0x4`**.
- Gate `container+0x2c` (char); CS at `container+0x14`.
- SEH `LAB_009be0c8` so Leave runs on exception unwind (state −1→0 around nested push).
- Bit-twin of W30-D `GuardedVector_PushBackLocked` (`0x0043c4f0`) — only nested CALL rel32 differs.
- Prefer bytes over decompiler for EDI formal and leave-path `[ESI+0x18]` alias of `[EDI+0x2c]`.

## Pseudocode (annotated)

```c
// EDI = container; stack = const PodU32U8* value; RET 0x4
void GuardedVector_PushBackLocked_Stride2_U32U8(const PodU32U8* value /*stack*/,
                                                GuardedVectorHeader* container /*EDI*/)
{
  // SEH install LAB_009be0c8
  if (container->lockEnable /*+0x2c*/ != 0) {
    EnterCriticalSection(&container->cs /*+0x14*/);
  }
  // SEH state = 0
  // MOV EAX, EDI; CALL FUN_0043e9e0(value)
  GuardedVector_PushBack_Stride2_U32U8(container /*EAX*/, value);
  if (container->lockEnable != 0) {
    LeaveCriticalSection(&container->cs);
  }
  // SEH teardown; RET 4
}
```

## ABI / layout

| Offset | Field |
|---|---|
| +0x04 | pages |
| +0x08 | capacity |
| +0x0c | begin |
| +0x10 | size |
| +0x14 | CRITICAL_SECTION (24 B) |
| +0x2c | lockEnable (char) |

## Call graph

| Role | Symbol |
|---|---|
| Nested body | `FUN_0043e9e0` / `GuardedVector_PushBack_Stride2_U32U8` |
| Win32 | EnterCriticalSection / LeaveCriticalSection |
| Callers (8) | NDResourceCache_LookupOrCreate; FUN_00970b20; FUN_00970b60; FUN_0075de80 (×3); FUN_0075d3c0; FUN_00970e50 |

## Open questions

- Product/PDB class English.
- Runtime / bit-exact under contended CS.
