# Annotated low-level: FUN_00914c60 → Client_SetModeLatch493_ParkDriveOnClear_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00914c60` |
| VA | `0x00914c60`–`0x00914ce5` inclusive (**134 B** / `0x86`) |
| Canonical | `Client_SetModeLatch493_ParkDriveOnClear_Inferred` (Ghidra `FUN_00914c60`) |
| System | `input-drive-control` |
| Date | 2026-08-05 (R11-037 dual A/B live re-verify) |
| Parent leaf | `0x00914c20` `Client_Vehicle_StopHbAndZeroLongitudinal_Inferred` (clear path only) |

## Machine-level notes

- **thiscall** ECX = client host; stack arg `enable` (low byte tested); **`RET 4`**.
- **Virtual method**: sole xref is DATA vtable slot `@0x00a2f468` (no direct callers).
- **Latch** `*(u8*)(this+0x493)` gates side-effect branches; final apply is always `FUN_007902a0(this, enable)`.
- **Clear path** (`enable==0` and latch set): `FUN_007fb990` with ESI=`&DAT_00d1a840`, then dualed StopHb+ZeroLong with EAX=this, then optional cursor-host notify `vtbl+4(0)` if `DAT_00d1b9a8` query `vtbl+0xd0` is true.
- **Enable path** (`enable!=0` and latch clear): optional cursor-host notify `vtbl+4(1)` if host non-null and query false.
- **Always**: `XORPS`/`MOVSS` zero **f32** at `this+0x538` (decompiler shows dword-0; sealed as float store).
- Prefer assembly when decompiler conflicts (`+0x538` width / store opcode).

## Pseudocode (annotated)

```c
// thiscall ECX = client; stack char enable; RET 4
void __thiscall Client_SetModeLatch493_ParkDriveOnClear_Inferred(void* client, int enable_raw)
{
  char query;
  unsigned notify_arg;

  if ((char)enable_raw == 0) {
    // clear path — require latch already set
    if (*(char*)((char*)client + 0x493) == 0) goto common;
    // ESI = &DAT_00d1a840 (global client singleton)
    FUN_007fb990();                         // UI/camera fade helper (ESI host)
    // EAX = client
    Client_Vehicle_StopHbAndZeroLongitudinal_Inferred(); // 0x00914c20
    if (DAT_00d1b9a8 == 0) goto common;
    query = DAT_00d1b9a8->vtbl[0xd0/4]();    // query
    if (query == 0) goto common;
    notify_arg = 0;                         // notify clear
  } else {
    // enable path — require latch clear + host present
    if (*(char*)((char*)client + 0x493) != 0 || DAT_00d1b9a8 == 0) goto common;
    query = DAT_00d1b9a8->vtbl[0xd0/4]();
    if (query != 0) goto common;
    notify_arg = 1;                         // notify set
  }
  DAT_00d1b9a8->vtbl[4/4](notify_arg);      // host state sync

common:
  *(float*)((char*)client + 0x538) = 0.0f;  // XORPS + MOVSS
  FUN_007902a0(client, (char)enable_raw);   // apply +0x493 (+ optional +0x4b8 teardown on clear)
}
```

## Sealed callees

| Callee | VA | Role in this body |
|--------|-----|-------------------|
| `FUN_007fb990` | `0x007fb990` | Clear-path only; ESI=`&DAT_00d1a840`; camera/UI object `@+0x1144` fade (`0.1f`) |
| `Client_Vehicle_StopHbAndZeroLongitudinal_Inferred` | `0x00914c20` | Clear-path only; EAX=this; dual-sealed MEGA-098 |
| `FUN_007902a0` | `0x007902a0` | Always; thiscall ECX=this, stack enable → toggle `+0x493` / clear `+0x4b8` |
| `DAT_00d1b9a8` vtbl `+0xd0` | indirect | Query bool before notify |
| `DAT_00d1b9a8` vtbl `+4` | indirect | Notify with 0 (clear) or 1 (enable) |

## Offsets / globals

| Symbol | Role | Confidence |
|--------|------|------------|
| `this+0x493` | mode latch (u8) | **High** (read here; written by `007902a0`) |
| `this+0x538` | f32 cleared every entry | **High** (MOVSS) |
| `DAT_00d1b9a8` | cursor / UI host singleton | **High** (prior duals; tooltip host) |
| `DAT_00d1a840` | global client (`&` loaded imm to ESI) | **High** |
| `this+0x4b8` | owned by `007902a0` teardown (not touched here) | evidence via callee only |

## Open questions

- Product English for latch `+0x493` and timer/field `+0x538`.
- Exact product name of `DAT_00d1b9a8` vtbl slots `+0xd0` / `+4`.
- Owning class of vtable `@00a2f468` (slot product name).
- Runtime / bit-exact / differential.
