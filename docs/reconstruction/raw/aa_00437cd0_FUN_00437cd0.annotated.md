# Annotated low-level: FUN_00437cd0 / ComPtr_ClearSlot

| Field | Value |
|---|---|
| Stable ID | `aa_00437cd0` |
| VA | `0x00437cd0` |
| Canonical | `ComPtr_ClearSlot` |
| System | COM / OLE DB plumbing |
| Date | 2026-07-29 |

## Machine-level notes

- **Body (7 B):** `mov dword ptr [eax], 0` ; `ret`
- **ABI:** slot pointer in **EAX** (`IUnknown**` / `void**`). No stack args; plain `ret`.
- **Role:** zero a COM interface pointer **out-slot** before QI / bind (prepare empty com_ptr). Sibling of `aa_0041d7b0` (Release-if-non-null).
- Parent dual `OleDb_ReleaseAccessorHandleArray` (`0x00422de0`) calls this as “zeros com_ptr slot”.
- Not a CriticalSection, not free/delete, not Release — **store-null only**.

## Pseudocode (annotated)

```c
// void ComPtr_ClearSlot(IUnknown** /*in EAX*/ slot)
void FUN_00437cd0(void)
{
  undefined4 *in_EAX;   // IUnknown** slot — register arg in EAX
  *in_EAX = 0;          // clear slot (no Release)
  return;
}
```

## Call pattern (from dualled parents)

Typical QI prelude:

1. `FUN_00437cd0()` with EAX = address of local / out com_ptr → `*slot = 0`
2. `IUnknown::QueryInterface` into that slot
3. use iface
4. `FUN_0041d7b0()` with EAX = same slot → Release if non-null (**does not re-null** in that unit)

## Open questions

- Product wrapper name (if any); structural name sealed.
- Whether any caller passes non-pointer-sized types (none observed; always dword store).
