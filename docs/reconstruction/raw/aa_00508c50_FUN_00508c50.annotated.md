# Annotated low-level: HandlerTable58_FindById6c (`FUN_00508c50`)

| Field | Value |
|---|---|
| Stable ID | `aa_00508c50` |
| VA | `0x00508c50` |
| Body | `0x00508c50`–`0x00508c91` exclusive (**65** B) |
| Canonical name | `HandlerTable58_FindById6c` (inferred structural) |
| Ghidra | `FUN_00508c50` |
| System | status/hook handler tables (multi-slot family) |
| Date | 2026-07-29 (W21-D OWN-ONLY dual) |
| Live tools | `decompile_function` + `read_memory` (+ meta/xrefs); **no** `disassemble_bytes` |

## Machine-level notes

- **ABI:** `__thiscall` — ECX = registry*; stack arg = `int32 id`; **`ret 4`**.
- **Leaf:** no callees.
- **Table:** `*(registry + 0x58)` = `entry**` pointer table.
- **Count:** exclusive upper bound `DAT_00d02984` (global BSS; not a field of `this`).
- **Match field:** `*(int32*)(entry + 0x6c) == id`.
- **Rejects:** `id == -1`; `DAT_00d02984 <= 0`.
- **Return:** matching entry pointer, else **null** (not index).
- **Family twin:** `HandlerTable54_FindById6c` (`0x00508c00`) uses `+0x54` / `DAT_00d02988`.
- **Equip role (caller dual `aa_004fe380`):** subtype **0xa** (10) handler resolve; twin `00508c00` is subtype **0xb**.
- Prefer bytes + decompile; no invented product English.

## Pseudocode (annotated)

```c
// __thiscall ret 4
// registry table at +0x58; entry id at +0x6c; count = DAT_00d02984
void *__thiscall HandlerTable58_FindById6c(void *registry /*ECX*/, int32_t id /*stack*/)
{
  int32_t i;
  int32_t *cursor; // walks entry** as int*

  i = 0;
  if ((id != -1) && (DAT_00d02984 > 0)) {
    cursor = *(int32_t **)((char *)registry + 0x58);
    do {
      if (*(int32_t *)(*cursor + 0x6c) == id) {
        return (*(void ***)((char *)registry + 0x58))[i];
      }
      i = i + 1;
      cursor = cursor + 1; // +4 bytes
    } while (i < DAT_00d02984);
  }
  return 0;
}
```

## Body hex seal

```
578b7c240833c083ffff74298b158429d00085d253567e198b71588bce8d49008b19397b6c741283c00183c1043bc27cef5e33c05b5fc204008b04865e5b5fc20400
```

Key immediates: `[ecx+0x58]`, `[ebx+0x6c]`, `DAT_00d02984` @ `0x00d02984`, both exits `c2 04 00`.

## Callers

| Function | Role |
|---|---|
| `FUN_004fe380` Vehicle_ApplyItemStatusHooksOnEquip | subtype **0xa** handler resolve |
| `FUN_005092b0`…`FUN_00509940` | loot / type-from-prefix family |
| `FUN_00513580` Item_SumEquippedComponentValues | related table consumer |

## Open questions

1. Product/PDB names for registry and entry types.
2. Loader that fills `+0x58` and `DAT_00d02984`.
3. Runtime / bit-exact / differential — open.
