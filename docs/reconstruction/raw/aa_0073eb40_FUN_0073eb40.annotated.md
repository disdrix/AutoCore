# Annotated low-level: GfxBodyHolder_ReplaceUnserialize_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0073eb40` |
| VA | `0x0073eb40`–`0x0073ebe6` exclusive (**166 B**) |
| System | client / graphics / gfxBody sto load |
| Date | 2026-08-04 (W38-R) |
| Canonical | `GfxBodyHolder_ReplaceUnserialize_Inferred` (**Inferred**) |
| Ghidra | `FUN_0073eb40` |

## Machine-level notes

- **thiscall** `ECX=holder*`, stack `reader*`, **`RET 4`**.
- **EAX status** passthrough from SAMB (`FUN_009615c0`) or GBOD (`FUN_00765740`) arm — decompiler `void` incomplete.
- Holder slot **`+4`**: scalar-deleting dtor of prior body (`PUSH 1; CALL [vtbl]`), then `operator_new(0x14C)` + `FUN_00764030` GfxBody ctor.
- `FUN_0076a900` peeks tag with **ESI=reader** (loaded from stack formal before store).
- Tag **`0x424D4153` SAMB** → master path (`gfxBodyMassageMachine.cpp` plates on callee); else **GBOD** via W37-S `GfxBody_UnserializeGBOD_Inferred` (**stdcall RET 8**).
- Both arms receive `(body, reader)` on stack (`PUSH ESI` before branch; body pushed in-arm).
- Inbound static callers: **open** (Ghidra xrefs empty). Sole known role: only static caller of GBOD unserialize.

## Pseudocode (bytes-corrected)

```c
// thiscall; RET 4; EAX = unserialize status
uint32_t __thiscall GfxBodyHolder_ReplaceUnserialize_Inferred(
    GfxBodyHolder* holder,
    stoChunkReader* reader)
{
    // SEH LAB_009b3f3c
    void** old = *(void***)(holder + 4); // body*
    if (old) {
        ((void (__thiscall*)(void*, int))**old)(old, 1); // scalar deleting dtor
    }
    void* raw = operator_new(0x14C);
    void* body = raw ? FUN_00764030(raw) : NULL; // GfxBody ctor
    *(void**)(holder + 4) = body;

    uint32_t tag = stoChunkReader_PeekChunkTag(); // ESI=reader
    if (tag == 0x424D4153 /* SAMB */) {
        return FUN_009615c0(body, reader); // cdecl; ADD ESP,8
    }
    return GfxBody_UnserializeGBOD_Inferred(body, reader); // stdcall RET 8
}
```

## Open questions

- Product C++ class English for holder / body host.
- Inbound caller recovery (vtable / missed xref).
- Runtime / bit-exact / differential.
