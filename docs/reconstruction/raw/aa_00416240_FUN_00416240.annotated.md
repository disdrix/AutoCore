# Annotated low-level: FUN_00416240 → FieldBlock_CopyFrom_ClearFlags0xBC_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00416240` |
| VA | `0x00416240`–`0x0041625c` inclusive (**29 B** / `0x1D`) |
| System | field-block / selective POD assign helper |
| Date | 2026-08-05 (WQ9I-H dual) |
| Canonical | `FieldBlock_CopyFrom_ClearFlags0xBC_Inferred` |

## Machine-level notes

- **ABI:** `__thiscall`; ECX = dest host; stack `src`; **RET 4**; EAX = dest.
- **Bytes:** `8b442404 56 8bf1 50 c786bc00000000000000 e809ffffff 8bc6 5e c20400`
  - `MOV EAX,[ESP+4]` → src
  - `PUSH ESI; MOV ESI,ECX` → save dest
  - `PUSH EAX` → stack arg for copy
  - `MOV dword [ESI+0xBC], 0` → clear flag dword
  - `CALL FUN_00416160` → selective block copy (ECX still = dest)
  - `MOV EAX,ESI; POP ESI; RET 4`
- **Callee `FUN_00416160` (WQ9I-G, evidence only):** copies mid fields always; copies flag dword `dest[0x2f]` from src; then conditionally copies head/tail ranges based on flag bits 0/1/2; bumps gen `dest[0x2d]++`. So this unit’s pre-clear of `+0xBC` is **immediately overwritten** by the copy — still present in CF.
- **Callers:** mesh/path `FUN_00634c50` (temp dest); object swap `FUN_00833160` (temp = copy of `ESI+0x510`, then mutual `FUN_00416160` to swap nested blocks).
- Scaffold name `…shopveh…` has **no** string/RTTI at this VA → rejected; use structural `_Inferred`.

## Pseudocode (annotated)

```c
// ECX = dest; stack src; RET 4; returns dest*
void* __thiscall FieldBlock_CopyFrom_ClearFlags0xBC_Inferred(void* dest, const void* src)
{
  *(uint32_t*)((uint8_t*)dest + 0xBC) = 0;   // clear flags (index 0x2f)
  FUN_00416160(dest, src);                     // selective field-block assign
  return dest;
}
```

## Open questions

- Product English for the ~0xC0 field-block layout (phySkeleton chain is scaffold-only).
- Whether any caller relies on the pre-clear for aliasing / concurrent observers (no static evidence).
- Dual of `FUN_00416160` (WQ9I-G) for full flag-bit map seal.
