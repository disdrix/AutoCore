# Annotated low-level: FUN_0082fc40 → UI_InteractionMenu_AddTrade_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0082fc40` |
| VA | `0x0082fc40`–`0x0082fd4a` exclusive |
| Body size | **266 B** (`0x10A`) |
| System | skills-abilities / interaction menu UI (Trade pack) |
| Date | 2026-08-05 (MEGA-077 refresh) |
| Prior | 2026-07-23 scaffold annotated |

---

## Machine-level notes

- Source: raw capture + live MEGA-077 re-verify (`disassemble_function` + `read_memory`).
- Decompiler shows `unaff_EBX` / `unaff_ESI` — these are **parent-preserved registers**, not phantom:
  - **EBX** = target object (parent `MOV EBX,EBP` before both `CALL 0x0082fc40`).
  - **ESI** = menu host (this of parent `FUN_0082fe20`).
- Frame uses `AND ESP,0xFFFFFFF0` for SSE squared-distance temps on stack.
- Prefer assembly for ABI; decompile CF ≡ raw for control flow.

---

## ABI

```text
EBX = target Object*
ESI = MenuHost*
ret 0 (C3)
```

No stack parameters. Not standard ECX-thiscall; register contract matches sibling packs under `FUN_0082fe20` (Trade uses **EBX**, Convoy uses **EDX** for target).

---

## Pseudocode (annotated)

```c
// ESI = menu host; EBX = target object; ret 0
void UI_InteractionMenu_AddTrade_Inferred(void)
{
  // locals: SSE stack temps for target XYZ; EDI = button*
  // DAT_00d1b6d8 = local player Object*
  // DAT_00aaa674 = 1600.0f  (max trade range squared → 40 units)

  // Gate on +0x6B4 (same numeric gate family as convoy pack):
  // enter if target.field < 1 OR local.field >= 1
  if ((*(int *)(EBX + 0x6b4) < 1) || (0 < *(int *)(DAT_00d1b6d8 + 0x6b4))) {

    // host->vtbl[0x444/4](xml, flags=0, buttonId=0x11175, -1) → UiButton*
    piVar4 = (int *)(**(code **)(*ESI + 0x444))
                              ("i_m_int_2d_btn_generic.xml", 0, 0x11175, 0xffffffff);
    if (piVar4 != (int *)0x0) {
      // btn->vtbl[0x1D8/4]("Trade", 1, 1)
      (**(code **)(*piVar4 + 0x1d8))("Trade", 1, 1);
    }
    // menu+0x550 = ESI[0x154]
    ESI[0x154] = (int)piVar4;

    // World position: SharedBase vtbl chain on target → vtbl+0x1A0 → float[3]
    pfVar5 = (float *)(**(code **)(*(int *)(*(int *)(*(int *)(EBX + 4) + 4) + 4 + EBX) +
                                  0x1a0))();
    fVar1 = *pfVar5;       // target.x
    fVar2 = pfVar5[1];     // target.y
    fVar3 = pfVar5[2];     // target.z

    // Same chain on local player
    pfVar5 = (float *)(**(code **)(*(int *)(*(int *)(*(int *)(DAT_00d1b6d8 + 4) + 4) + 4 +
                                           DAT_00d1b6d8) + 0x1a0))();

    // If button exists AND dist²(target, local) > 1600.0f → disable
    if (((int *)ESI[0x154] != (int *)0x0) &&
       (DAT_00aaa674 <
        (fVar3 - pfVar5[2]) * (fVar3 - pfVar5[2]) + (fVar2 - pfVar5[1]) * (fVar2 - pfVar5[1]) +
        (fVar1 - *pfVar5) * (fVar1 - *pfVar5))) {
      // btn->vtbl[0xD4/4](0)  // disable / hide (pointer still stored)
      (**(code **)(*(int *)ESI[0x154] + 0xd4))(0);
    }
  }
  return;
}
```

---

## Call graph

| Direction | Targets |
|---|---|
| Callers (1 fn, 2 sites) | `FUN_0082fe20` @ `0x0082fed7`, `0x0082ff0b` |
| Direct callees | none |
| Indirect | host `+0x444`, btn `+0x1D8`, pos `+0x1A0` ×2, btn `+0xD4` |

Parent pack order (evidence only, dualed elsewhere as PopulateTargetButtons):

- order-0: … Tell → **Trade** → Clan → Convoy → Name
- order-≠0: Name → Convoy → Clan → **Trade** → Tell …

---

## Open questions

1. Product / MSVC demangle for menu host class (no RTTI on this VA).
2. English for field `+0x6B4` (rank/level-like numeric gate shared with convoy pack).
3. Exact product name of `vtbl+0x1A0` position getter (SharedBase chain).
4. Runtime / bit-exact / differential (Terminal false).
