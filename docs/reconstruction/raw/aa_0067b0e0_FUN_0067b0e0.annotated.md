# Annotated low-level: FUN_0067b0e0

| Field | Value |
|---|---|
| Stable ID | `aa_0067b0e0` |
| VA | `0x0067b0e0` |
| Body span | `0067b0e0`–`0067b102` exclusive (34 B) |
| System | network-nazgul-netAddress |
| Date | 2026-07-29 (OWN-ONLY dual seal W25-P) |
| Proposed name | `netAddress_CopyAssign` / `Address_CopyFrom` (**High** role; product open) |

## Machine-level notes

- Source: raw capture for `aa_0067b0e0` + live `decompile_function` / `read_memory` 2026-07-29.
- Prefer `read_memory` when decompiler omits `this` at call sites (callers often show only stack src).
- Cluster: nazgul 16-byte Address POD — ctor `0067b070`, **copy this**, dtor `0067b110`, assign `0067b120`.
- Parent duals already name this as 4× dword Address copy (`A_aa_0067ba30_netSocket_GetPeerAddress`).

## Calling convention (sealed)

| Slot | Role | Evidence |
|---|---|---|
| **ECX** | dest `Address*` (this) | entry `mov eax, ecx` |
| **stack arg0** | src `Address*` | `mov ecx, [esp+4]` |
| **Return** | void | no `mov eax, …` result path |
| **ABI** | `__thiscall` + **1 stack arg**; trailer **`ret 4`** | `c2 04 00` |

## Stage map

| Stage | What |
|---|---|
| 1 | Save dest this → EAX |
| 2 | Load src from stack → ECX; push ESI |
| 3 | Copy `[src+0]` → `[dest+0]` |
| 4 | Copy `[src+4]` → `[dest+4]` |
| 5 | Copy `[src+8]` → `[dest+8]` |
| 6 | Copy `[src+0xC]` → `[dest+0xC]` |
| 7 | pop ESI; `ret 4` |

## Pseudocode (annotated)

```c
// __thiscall netAddress_CopyAssign(Address *this /* ECX dest */, const Address *src /* stack */)
// POD: four dword stores. No overlap check (not memmove). No vtbl.

void __thiscall FUN_0067b0e0(uint32_t *dest /* this */, uint32_t *src)
{
  dest[0] = src[0];
  dest[1] = src[1];
  dest[2] = src[2];
  dest[3] = src[3];
}
```

## Object / buffer sizes

| Object | Size | Notes |
|---|---:|---|
| Address / sockaddr overlay | **16** | 4 dwords; matches `namelen=0x10` in GetPeer/GetSock name |
| No heap | 0 | pure stores |

## Open questions

1. Product/PDB spelling (`netAddress` vs `Address` vs TNL `Address`).
2. Whether any caller relies on self-assignment (self-copy is safe — dword-wise).
3. Exact field layout beyond 16B sockaddr_in overlay (family/port/addr/zero).
