# Raw capture: FUN_00513c10

| Field | Value |
|---|---|
| **Stable ID** | `aa_00513c10` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00513c10` |
| **Canonical name** | `FUN_00513c10` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
char __thiscall FUN_00513c10(int *param_1,int param_2,int param_3)

{
  char cVar1;
  short sVar2;
  short sVar3;
  int iVar4;
  
  if (param_3 == 0) {
    return '\x03';
  }
  if ((short *)(param_3 + 0xb0) != (short *)0x0) {
    cVar1 = FUN_00513bc0(param_3);
    if (cVar1 != '\0') {
      if (((uint)param_1[0x5f] >> 0x13 & 1) != 0) {
        return '\0';
      }
      sVar3 = *(short *)(*(int *)(param_1[0x2a] + 0x3c) + 0x4b4);
      sVar2 = (**(code **)(*param_1 + 0x8c))();
      if (sVar3 <= sVar2) {
        return '\0';
      }
      if (param_2 == 0) {
        return '\x03';
      }
      sVar3 = (**(code **)(*param_1 + 0x8c))();
      iVar4 = FUN_00521eb0();
      if ((iVar4 < sVar3 + 1) && (*(int *)(param_2 + 0x6b4) < 1)) {
        return '\x01';
      }
      return (sVar3 + 1 <= (int)*(short *)(param_3 + 0xb0)) * '\x02' + '\x02';
    }
  }
  return '\x03';
}
```


---

## MEGA-036 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-036** |
| **Tools** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_callers` + `get_function_xrefs` + `get_assembly_context` + `read_memory` (208 B) + `batch_decompile` callees/callers. **No** `disassemble_bytes`. **No** Launcher. |
| **Live ≡ raw body** | **Yes** — decompile text identical to authoritative raw pseudocode above |
| **Body** | `0x00513c10`–`0x00513cde` inclusive (**207 B** / `0xCF`); next dualed peer `Item_HasModSlotConfig_Inferred` @ `0x00513cf0` |
| **ABI (assembly)** | **thiscall**: **ECX** = target item; stack `[ESP+4]` = character (`DAT_00d1b6d8` at call sites); stack `[ESP+8]` = `CVOGTinkeringKit*`; **EAX** = status code 0..4; **`RET 8`** on every exit |
| **Prologue seal** | `MOV EAX,[ESP+8]` / `TEST EAX` / `PUSH ESI` / `MOV ESI,ECX` / null-kit → `MOV EAX,3; POP ESI; RET 8` |
| **Callees** | `FUN_00513bc0` (category match kit↔item); vtbl `item[+0x8c]` ×2 (opened-slot count); `FUN_00521eb0` (character skill-tier from `char+0x59b`) |
| **Callers / xrefs** | **3** `UNCONDITIONAL_CALL`: `FUN_0085ce20` @ `0x0085d2bb` (mode-4 tinker tooltip, dualed); orphan gate body @ `0x0085d767` (CMP EAX,4 allow); `FUN_0085f660` @ `0x0085f675` (apply / success-chance dialog) |
| **Return codes (caller-sealed)** | `0` no slots / early broken; `1` skill insufficient; `2` kit too primitive; `3` null/mismatch category; `4` OK to apply |
| **Strings (callers, not this body)** | `"The object has no available slots to open."` / `"You do not possess the Tinkering skill to work on this object."` / `"This Tinkering Kit is too primitive to use on this object."` / `"This Tinkering Kit can only be used on %s."` / broken handled by caller after code≠4 |
| **Name** | `Item_ValidateTinkerKitUse_Inferred` (retire `Named_CalleeOf_Skill_…_00513c10` scaffold) |
| **System** | `inventory-transfer` |
| **Runtime Confirmed** | **not claimed** |

### Assembly CF (sealed)

```text
kit = [ESP+8]
if kit == 0: return 3
EBX = &kit->level_at_0xb0          ; LEA [kit+0xb0]
if !FUN_00513bc0(item, kit): return 3   ; category gate
if item[+0x17c] bit19: return 0         ; Broken
maxSlots = *(short*)(item->host[+0xa8]->def[+0x3c] + 0x4b4)
cur = item->vtbl[+0x8c]()
if cur >= maxSlots: return 0            ; no openable slots
character = [ESP+10] after frame pushes
if character == 0: return 3
cur = item->vtbl[+0x8c]()
need = cur + 1
tier = FUN_00521eb0(character)          ; ECX=character
if tier < need && character[+0x6b4] < 1: return 1
kitLevel = *(short*)(kit+0xb0)
return (kitLevel >= need) ? 4 : 2       ; LEA ECX,[CL+CL+2]; MOV EAX,ECX
```

### read_memory head/tail

- Head: `8b 44 24 08 85 c0 56 8b f1 75 09 b8 03 00 00 00 5e c2 08 00`
- Tail: `0f 9d c1 5f 5b 5e 8d 4c 09 02 8b c1 c2 08 00` then fail-path `5b b8 03 00 00 00 5e c2 08 00`
