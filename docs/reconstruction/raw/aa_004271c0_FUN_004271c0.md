# Raw capture: FUN_004271c0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004271c0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004271c0` |
| **Canonical name** | `FUN_004271c0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_004271c0(void)

{
  int iVar1;
  int unaff_EBX;
  int iVar2;
  
  iVar2 = *(int *)(unaff_EBX + 4);
  if (iVar2 == 0) {
    *(undefined4 *)(unaff_EBX + 4) = 0;
    *(undefined4 *)(unaff_EBX + 8) = 0;
    *(undefined4 *)(unaff_EBX + 0xc) = 0;
    return;
  }
  iVar1 = *(int *)(unaff_EBX + 8);
  for (; iVar2 != iVar1; iVar2 = iVar2 + 0x80) {
    FUN_00426f70();
  }
                    /* WARNING: Subroutine does not return */
  operator_delete(*(void **)(unaff_EBX + 4));
}
```

---

## MEGA-128 re-verify (2026-08-05) — append only

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-128** |
| **Tools** | Live Ghidra HTTP `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` + `get_xrefs_to` + callee decompile `FUN_00426f70` + call-site disasm `FUN_00797d70`. **No** `disassemble_bytes`. |
| **Live decompile** | ≡ scaffold CF; Ghidra **false-noreturn** on `operator_delete` — epilogue zeros begin/end/cap then **`RET`** (`C3`) |
| **Body** | `0x004271c0`–`0x00427204` inclusive (**69 B** / `0x45`); Ghidra exclusive end `0x00427205`; `CC` pad then next |
| **ABI** | **EBX = vector\*** (`unaff_EBX`); no stack formals; terminal **`C3`** (`RET`); **void** |
| **Classification** | worker (vector tidy / destroy-runs for 0x80-stride elements) |
| **Product name** | **`StdVector_DestroyRuns_0x80_Inferred`** — no RTTI; role sealed from CF + sole CALL from `UiTextLayout_CompleteDtor` with `LEA EBX,[layout+0x80]` |

### Full body hex (69 B + pad sample)

```
568b730485f67426578b7b083bf774118bcee899fdffff81c6800000003bf775ef8b430450e83826060083c4045fc7430400000000c7430800000000c7430c000000005ec3
```

(`read_memory` 80 B includes trailing `CC` pad.)

### Byte-level CF (authority)

```
PUSH ESI
ESI = [EBX+4]                         // begin
if ESI == 0:
  [EBX+4]=0; [EBX+8]=0; [EBX+0xC]=0
  POP ESI; RET                        // C3 @ 0x00427204
PUSH EDI
EDI = [EBX+8]                         // end
while ESI != EDI:
  ECX = ESI
  CALL FUN_00426f70                   // ~wstring at elem+0xC (ADD ECX,0xC; JMP IAT)
  ESI += 0x80                         // stride 0x80
PUSH [EBX+4]; CALL operator_delete @ 0x00489822; ADD ESP,4
POP EDI
[EBX+4]=0; [EBX+8]=0; [EBX+0xC]=0
POP ESI; RET
```

### Call targets

| Site | Target | Role |
|---|---|---|
| `0x004271d2` | `0x00426f70` | per-elem dtor trampoline: `ADD ECX,0xC; JMP [~basic_string<wchar_t>]` |
| `0x004271e5` | `0x00489822` | `operator_delete` (cdecl; `ADD ESP,4`) |

### Layout

| Off | Field |
|---|---|
| vec+4 | begin |
| vec+8 | end |
| vec+0xC | capacity_end |
| elem stride | **0x80** |
| elem+0x0C | embedded `basic_string<wchar_t>` destroyed by `FUN_00426f70` |

### Decompile vs bytes

| Topic | Winner |
|---|---|
| null begin → zero triple | **match** |
| walk stride 0x80 + FUN_00426f70 | **match** (bytes seal ECX=elem) |
| `operator_delete` noreturn | **bytes win** — continues; zeros triple; RET |
| EBX-this | **bytes + callers** (decompile unaff_EBX) |

### Callers / xrefs

| From | Kind | Notes |
|---|---|---|
| `FUN_00797d70` @ `0x00797df0` | UNCONDITIONAL_CALL | `LEA EBX,[ESI+0x80]; CALL 004271c0` (UiTextLayout complete dtor; dualed WQ9L-F) |
| `thunk_FUN_004271c0` @ `0x00427040` | UNCONDITIONAL_JUMP | `E9 7B 01 00 00` → `JMP 004271c0` |

### Callees

`FUN_00426f70` (elem dtor trampoline), `operator_delete`

### Notes

- **Not** thiscall: only **EBX** carries vector\*; ECX is used only as elem\* for the per-element dtor call.
- False-noreturn on `operator_delete` **falsified** by post-delete zero triple + `RET`.
- Element product English open (text-layout "run"/glyph row inferred from parent `+0x80` ownership) → `_Inferred`.
- Retire scaffold alias `Named_CalleeOf_Named_CalleeOf_Named_VOG_DEBUG_STOP_004271c0`.
- Twin pattern: dualed `StdVector_Tidy_Stride24_EbxThis_Inferred` `0x00465d70` (stride 0x24).
