# Annotated low-level: StdMap_StringKey_EraseAndRebalance_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004094c0` |
| VA | `0x004094c0` |
| Canonical | `StdMap_StringKey_EraseAndRebalance_Inferred` |
| Ghidra | `FUN_004094c0` |
| System | MSVC `std::_Tree` erase + RB rebalance (string key @+0x0C, isnil@+0x2D) |
| Date | 2026-08-04 WQ9J-F dual seal |

## Machine-level notes

- Source: raw capture + live re-verify (`decompile` + `read_memory` + callers/xrefs + assembly context).
- Throw plate: `"invalid map/set<T> iterator"` @ `0x00a152f0` + `DAT_00acc34c`.
- isnil opcode family: `80 xx 2D 00` throughout; color dword-slot `+0x2C` (`[reg+0x2C]` / `reg[0xb]` as char).
- Value dtor: `~basic_string` on `node+0x0C` before `operator_delete`.
- Decompiler marks multi-child splice blocks "unreachable" and treats `operator_delete` as noreturn — **false**. Bytes continue: size--, `*outIt`, `ret 0xC`.
- Peer insert dual: `StdMap_StringKey_InsertNodeAndRebalance` @ `0x00430b60` (same color/isnil/string layout, node 0x30).
- Rotates: L `FUN_0044e010`, R `FUN_0042a840` (isnil@+0x2d family — not isnil29 pair `004192a0`/`004192f0`).
- Succ / extremities: `FUN_0042ac40` (iterator++), min `FUN_0044e180`, max `FUN_0042ac20`.
- Sole call site: erase-range shell `FUN_00407f90` @ `0x0040802e` (`PUSH` map/outIt/node, **ret 0xC**).

## ABI (byte-sealed)

| Slot | Role |
|---|---|
| stack0 | `MapShell*` (head@+4, size@+8) |
| stack1 | `Node**` out-iterator / successor holder |
| stack2 | `Node*` to erase |
| cleanup | **`RET 0x0C`** |

## Layout

| Offset | Field |
|---|---|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x0C | `std::string` key (MSVC basic_string) |
| +0x28 | mapped dword (peer insert writes) |
| +0x2C | color (0 red / 1 black) |
| +0x2D | isnil |
| size | **0x30** |

## Control flow (sealed)

1. if `node->isnil` → throw invalid iterator
2. `FUN_0042ac40` capture successor (into out-it shadow)
3. Unlink / splice (one-child or successor-swap two-child path — full MSVC erase; decompiler may omit mid blocks)
4. Fix head leftmost/rightmost via min/max when erasing extremity
5. if erased color black → RB recolor + L/R rotate until root or red
6. destroy string @+0x0C; `operator_delete(node)`
7. if `size>0` then `size--`; `*outIt = successor`; `ret 0xC`

## Open gaps

- Product map English type (which string-keyed host).
- Dual of erase-range parent `FUN_00407f90` and rotate/min/max helpers (not OWN).
- Runtime / bit-exact / differential.
