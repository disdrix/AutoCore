# Annotated low-level: FUN_009864e0 → GfxVertexBufferFactory_PreResetReleaseAll_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_009864e0` |
| VA | `0x009864e0`–`0x00986560` inclusive (**129 B**) |
| System | client / Palantir graphics (`gfxVertexBufferFactory` + `gfxDeviceVB.cpp`) |
| Date | 2026-08-04 (W38-AD) |
| Named | `GfxVertexBufferFactory_PreResetReleaseAll_Inferred` |

## Machine-level notes

- **ABI:** EAX = VB factory host; bare `C3`; void.
- **Factory layout:** MSVC-style tree sentinel at `+0x14`. Node: left`+0` parent`+4` right`+8`; flag byte `+0xE` (skip if `& 0x28`); range begin`+0x14`/end`+0x18`; isnil`+0x21`.
- **Per node range:** array of **0x20**-byte slots; `ESI = *(dword*)slot` then `FUN_007465c0` (decompiler omits ESI load).
- **Callee:** `FUN_007465c0` = gfxDeviceVB unlock-all (vtbl+0x30) while lockcount>0, then COM Release (vtbl+8), clear ptr, update `DAT_00d1f614` stats.
- **Sole caller:** `FUN_0075eff0` @ `0x0075f0e7` with EAX = GfxDevice `+0x74C` (ctor slot size 0x20 / `FUN_00986d40`).
- **Sibling:** `FUN_00986570` post-restore same tree walk; strings `gfxVertexBufferFactory.cpp`.
- **IB twin (not OWN):** `FUN_00985580` same tree shape calling `FUN_00743c50` (gfxDeviceIB).

## Pseudocode (annotated)

```c
// EAX = GfxVertexBufferFactory*
void GfxVertexBufferFactory_PreResetReleaseAll_Inferred(void /* EAX factory */)
{
  Node *sentinel = *(Node **)(factory + 0x14);
  Node *node = (Node *)sentinel->left; // *sentinel
  while (node != sentinel) {
    if ((*(unsigned char *)((char*)node + 0xE) & 0x28) == 0) {
      char *end = *(char **)((char*)node + 0x18);
      for (char *slot = *(char **)((char*)node + 0x14); slot != end; slot += 0x20) {
        void *vb = *(void **)slot;  // ESI
        FUN_007465c0(/* ESI=vb */); // unlock-all + Release
      }
    }
    // std::_Tree successor using isnil @ +0x21
    if (*(char *)((char*)node + 0x21) == 0) {
      /* right-leftmost or parent-walk — decompile matches MSVC */
      node = TreeSuccessor(node);
    }
  }
}
```

## Open questions

- Product name of flag bits `0x28` on node+0xE.
- Full 0x20 slot English beyond +0 vb* and +8 callback tree (PostRestore).
- Runtime / bit-exact.
