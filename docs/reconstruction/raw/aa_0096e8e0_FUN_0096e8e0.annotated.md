# Annotated low-level: EffTextureFactory_ReCreateDefaultPool_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0096e8e0` |
| VA | `0x0096e8e0`–`0x0096e9e4` exclusive (**260 B**) |
| System | palantir graphics Effects — effTextureFactory ReCreate (device reset) |
| Date | 2026-08-04 (W38-AB) |
| Ghidra | `FUN_0096e8e0` |
| Canonical | `EffTextureFactory_ReCreateDefaultPool_Inferred` |

## Machine-level notes

- Stack formal `factory*`; **`RET 4`** (`C2 04 00`); SEH `LAB_009b09a1`.
- Sole caller: `GfxDevice_Reset` (`FUN_0075eff0`) — failure plate "Failed to Reset() device on Texture Factory".
- Product source plate: `effTextureFactory.cpp:0x159` with string `Failure on ReCreate() of effTextureImpl %s at <%x>`.
- List walk: circular head at `factory+8`; node value at node+8.
- Gate `impl+0x30 == 0` matches Reset pre-release of DEFAULT-pool textures (same field used as pool pointer after create).
- ReCreate path: `FUN_0096cad0` (thiscall; body begins with `FUN_0096c8e0` release).
- Alt path when `(flags & 0x3000)`: Release interface at `impl+0x4c` only.
- Fail path: `FUN_0096a630` formats `assId: [...]` from impl identity, then log and return hr.
- Reject bare scaffold `Named_effTextureFactory_0096e8e0` as complete product plate (needs ReCreate / DefaultPool semantics).

## Pseudocode (annotated)

```c
// stack factory*; RET 4; returns hr (0 ok, <0 fail)
int EffTextureFactory_ReCreateDefaultPool_Inferred(EffTextureFactory* factory)
{
  // for each DEFAULT-pool impl in factory list:
  //   recreate via FUN_0096cad0 or Release(impl+0x4c)
  //   on hr<0: log factory.cpp:0x159 and return
  // (see clean + raw W38-AB)
}
```

## Open / deferred

- Product class method demangle.
- Full meaning of flag `0x3000` branch (+0x4c role).
- Nested `FUN_0096cad0` / list shell dual not owned.
- Runtime / bit-exact under live device reset.
