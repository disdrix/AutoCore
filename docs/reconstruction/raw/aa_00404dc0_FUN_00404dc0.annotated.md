# Annotated low-level: FUN_00404dc0 → CVOGPhysics_SetRotation (inferred)

| Field | Value |
|---|---|
| Stable ID | `aa_00404dc0` |
| VA | `0x00404dc0` |
| System | physics |
| Date | 2026-07-29 (dual A/B refresh) |

## Machine-level notes

- Source: raw capture for `aa_00404dc0` + live Ghidra decompile/`read_memory` 2026-07-29.
- Prefer assembly when decompiler conflicts (stack out-byte `uStack_4` is artifact; `this` stays ECX→ESI).
- Body size **0x3f**; ends `FF 52 44` / `ret 4`.
- Dual gates:
  1. Ready probe `FUN_005070b0` → optional `FUN_005070d0` if out==0 and `phys+0x44≠0`.
  2. Fully-ready skip: body call only if `phys+0x40==0` OR `phys+0x08==0`.
- Sibling `0x0040d2a0` identical CF with imm **0x40** (position).
- Contrast angVel/lin helpers: no second gate; slots 0x54 / 0x50.

## Pseudocode (annotated copy of raw)

```c
void __thiscall FUN_00404dc0(int phys, undefined4 rotPtr)
{
  char *outReady;
  undefined4 uStack_4;  // decompiler-only; asm uses stack out-byte

  uStack_4 = phys;
  outReady = (char *)FUN_005070b0((int)&uStack_4 + 3); // ECX=phys, push &out
  if ((*outReady == '\0') && (*(int *)(phys + 0x44) != 0)) {
    FUN_005070d0();  // thiscall phys
  }
  // not fully ready → setRotation
  if ((*(char *)(phys + 0x40) == '\0') || (*(int *)(phys + 8) == 0)) {
    (**(code **)(**(int **)(phys + 0x3c) + 0x44))(rotPtr); // body vtbl +0x44
  }
  return; // ret 4
}
```

## Instruction seal (read_memory)

```text
push ecx; push esi
lea eax,[esp+7]; push eax; mov esi,ecx
call FUN_005070b0
cmp byte [eax],0 / jnz skip_prep
cmp dword [esi+0x44],0 / jz skip_prep
mov ecx,esi; call FUN_005070d0
cmp byte [esi+0x40],0 / jz do_call
cmp dword [esi+0x08],0 / jnz skip_call
mov ecx,[esi+0x3c]; mov eax,[esp+0xc]; mov edx,[ecx]
push eax; call dword [edx+0x44]
pop esi; pop ecx; ret 4
```

## Open questions

- Havok product name for vtbl+0x44.
- English names for `phys+0x40` / `+0x08` / `+0x44` / `+0x29`.
- Runtime verification of fully-ready skip under soft network apply.

## Duals

- `docs/reconstruction/reviews/A_aa_00404dc0_CVOGPhysics_SetRotation.md`
- `docs/reconstruction/reviews/B_aa_00404dc0_CVOGPhysics_SetRotation.md`
