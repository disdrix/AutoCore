# Annotated low-level: `Object_LoadOrReplaceAnimSlot` (`FUN_005828b0`)

| Field | Value |
|---|---|
| Stable ID | `aa_005828b0` |
| VA | `0x005828b0` |
| Structural name | `Object_LoadOrReplaceAnimSlot` |
| Ghidra name | `FUN_005828b0` |
| System | `client::special-event` / visual anim slots |
| Date | 2026-07-29 (W18-C dual A/B seal) |

---

## Machine-level notes

- Source: raw `aa_005828b0` + live `decompile_function` / `read_memory` 2026-07-29.
- **ABI sealed by call sites + epilogue:** `__thiscall`, **3 stack args**, **`ret 0xC`**.
- **`this` = visual object** (not the special-event wrapper): callers load ECX from visual field (`+0x10` / `+0x24`).
- Slot objects are **`0x78`** bytes; ctor `FUN_0074ed90`; path load via `FUN_0074ee40`.

---

## Host layout (this = visual)

| Off | Role |
|----:|------|
| `+0x24` | Anim controller / list object* (`vtbl+0x0C` add, `vtbl+0x10` remove) |
| `+0x2c` | `AnimSlot*[...]` array base (index × 4) |

If `*(this+0x24) == 0` → immediate fail (`return 0`).

---

## Anim slot object (`operator_new(0x78)` + `FUN_0074ed90`)

| Off | Init / use |
|----:|------------|
| many | zeroed / defaulted in ctor |
| `+0x3c` | `g_flOne` rate default |
| `+0x4c` | flag byte (ctor default `1`; **this unit overwrites with `param_4`**) |
| `+0x50` | `−1` |
| `+0x70` / `+0x74` | resource / inner after `FUN_0074ee40` |

---

## Pseudocode (annotated)

```c
// bool / uint32 success
// __thiscall Object_LoadOrReplaceAnimSlot(visual*, slotIndex, pathCStr, flagByte)
// ret 0xC

uint32_t __thiscall FUN_005828b0(int this /* visual */,
                                 void *param_2 /* slotIndex as int */,
                                 undefined4 param_3 /* path */,
                                 undefined1 param_4 /* flag → slot+0x4c */)
{
  int slot = (int)param_2;

  // SEH frame (LAB_009a5434) omitted in narrative

  if (*(int *)(this + 0x24) == 0)
    return 0;   // no controller

  // --- replace existing slot ---
  if (*(int *)(this + 0x2c + slot * 4) != 0) {
    // controller->Remove(oldSlot)  vtbl+0x10
    (**(code **)(**(int **)(this + 0x24) + 0x10))(*(int *)(this + 0x2c + slot * 4));
    FUN_0074eb20();           // teardown resources on old (thiscall on old)
    void *old = *(void **)(this + 0x2c + slot * 4);
    if (old) {
      FUN_0074ecf0();         // dtor body on old
      operator_delete(old);   // decomp warning "does not return" is MSVC delete pattern noise
    }
    *(undefined4 *)(this + 0x2c + slot * 4) = 0;
  }

  // --- allocate + construct new 0x78 slot ---
  void *neu = operator_new(0x78);
  undefined4 u = neu ? FUN_0074ed90() /* ctor thiscall on neu */ : 0;
  *(undefined4 *)(this + 0x2c + slot * 4) = u;

  // path resolve into stack temp (FUN_00989e00 this = DAT_00d1eac8 path cache)
  FUN_00989e00(&stackPath, param_3);

  // load .anm (or equivalent) into slot; <0 = fail
  int rc = FUN_0074ee40(&stackPath);
  if (rc < 0)
    return 0;

  // duration from global singleton DAT_00b04c74 +0x238 via FUN_0079a110
  float dur = (float)FUN_0079a110();
  FUN_0074e910(0, dur);   // thiscall on new slot: startTime=0, duration=dur

  *(uint8_t *)(*(int *)(this + 0x2c + slot * 4) + 0x4c) = param_4;

  // controller->Add(newSlot)  vtbl+0x0C
  (**(code **)(**(int **)(this + 0x24) + 0xc))(*(undefined4 *)(this + 0x2c + slot * 4));
  return 1;
}
```

---

## Callers (all load airlift trio)

| Caller | Slots / paths |
|--------|----------------|
| `ClientSpecialEvent_TeleportIn_ctor` `0x00978d20` | 1=`…_at.anm`, 2=`…_aut.anm`, 3=`…_at2.anm`, flag `0` |
| `ClientSpecialEvent_TeleportOut_ctor` `0x009781d0` | same |
| `FUN_00979310` `0x00979310` | same |

---

## Callees

| VA | Role |
|----|------|
| `operator_new` / `operator_delete` | 0x78 alloc / free |
| `FUN_0074ed90` | slot ctor |
| `FUN_0074eb20` | slot resource teardown |
| `FUN_0074ecf0` | slot dtor |
| `FUN_00989e00` | path/string resolve (`ECX=0x00d1eac8`) |
| `FUN_0074ee40` | resource lookup + acquire (return `−1` fail) |
| `FUN_0079a110` | read float duration from `DAT_00b04c74+0x238` |
| `FUN_0074e910` | set slot timing (`+0x40/+0x44/...`) |
| controller vtbl `+0x0C` / `+0x10` | add / remove slot |

---

## Open questions

1. Product English name of the 0x78 anim-slot type and controller at `visual+0x24`.
2. Semantics of flag byte at `slot+0x4c` (callers pass `0`; ctor default `1`).
3. Why duration comes from global `DAT_00b04c74` rather than the loaded resource.
4. Full dual of path/resource helpers (not OWN).
5. Runtime / bit-exact.
