# Annotated low-level: FUN_004a1770 → StringTable_FindOrIntern_u16_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004a1770` |
| VA | `0x004a1770`–`0x004a18a8` exclusive (**312 B**) |
| System | global `vector<basic_string>` intern / lookup |
| Date | 2026-07-29 (W34-I dual seal) |

## Machine-level notes

- Source: raw capture + W34-I re-verify (`decompile` + `read_memory` + xrefs). **No** `disassemble_bytes`.
- **ABI:** **cdecl** 1 stack formal — `const char* name`; bare `RET`; returns `uint` (hit: `index & 0xffff`; miss: `size-1`).
- Global table shell at `0x00b03608` (`ECX` for `StringVec_PushBack`); begin `DAT_00b0360c`, end `DAT_00b03610`; stride **0x1c**.
- Hit: IAT `basic_string::compare` vs C-string; return low 16 bits of index.
- Miss empty: push literal `"Empty"` (`0x00a41278`) then query; return new index.
- Miss non-empty: push query only; return new index.
- Sole caller `FUN_004a18b0` stores results as `uint16_t` at `+0x1dc` (`cloudName`) and `+0x1de` (`skyName`).
- **Reject** scaffold `Named_CalleeOf_Named_VOG_DEBUG_STOP_004a1770` (parent alias is string-trap only).

## Pseudocode (annotated)

```c
// cdecl; bare RET; EAX = u16 index (zero-extended) or size-1 after intern
unsigned StringTable_FindOrIntern_u16_Inferred(const char* name)
{
  // SEH LAB_009a1047
  // g_table @ 0x00b03608: begin DAT_00b0360c, end DAT_00b03610, elem 0x1c
  for (unsigned i = 0, off = 0; ; ++i, off += 0x1c) {
    if (DAT_00b0360c == 0) goto seed_empty_and_push;
    unsigned size = (DAT_00b03610 - DAT_00b0360c) / 0x1c;
    if (size <= i) {
      if (size != 0) goto push_name_only;
      goto seed_empty_and_push;
    }
    if (basic_string_compare(DAT_00b0360c + off, name) == 0)
      return i & 0xffff;
  }

seed_empty_and_push:
  // push "Empty" then name into g_table via StringVec_PushBack
push_name_only:
  // push name
  size = DAT_00b0360c ? (DAT_00b03610 - DAT_00b0360c) / 0x1c : 0;
  return size - 1;
}
```

## Gaps

- Product English name of the global table / owning module.
- Why seed `"Empty"` at index 0 (sentinel convention).
- Table lifetime / init site (unowned).
- Runtime / bit-exact / differential.
