# Annotated low-level: FUN_00409920 → StdTree_FreeSubtree_Isnil131_DestroyStr0C_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00409920` |
| VA | `0x00409920`–`0x00409960` inclusive (**65 B** / `0x41`) |
| System | skills-abilities (STL tree free-subtree; erase-range neighborhood) |
| Date | 2026-08-05 (WQ9J-I OWN-ONLY re-annotate) |
| Named | `StdTree_FreeSubtree_Isnil131_DestroyStr0C_Inferred` |

## Machine-level notes

- **Bytes authority** over decompiler (left-walk omitted; false noreturn on `operator_delete`).
- **ABI:** `__fastcall`-shaped ECX=tree_base + stack node*; **`RET 4`**.
- **isnil @ +0x131**; left@+0; right@+8; parent unused here.
- **Value destroy:** `FUN_00409b80(node)` stdcall — `~basic_string` at **node+0x0c** only (SEH frame).
- **operator_delete** cdecl after destroy (`PUSH node; CALL; ADD ESP,4`).
- Sole external caller: erase-range full-clear `FUN_00407b70` @ `0x00407b8f` (push `head->parent`).

## Pseudocode (byte-corrected)

```c
// ECX = tree_base; stack = node*; RET 4; void
void StdTree_FreeSubtree_Isnil131_DestroyStr0C_Inferred(void *tree_base, void *node)
{
  if (*(uint8_t *)((char *)node + 0x131) != 0)
    return;
  for (;;) {
    StdTree_FreeSubtree_Isnil131_DestroyStr0C_Inferred(
        tree_base, *(void **)((char *)node + 8));
    void *left = *(void **)node;
    FUN_00409b80(node);          // ~basic_string @ +0x0c
    operator_delete(node);
    node = left;
    if (*(uint8_t *)((char *)node + 0x131) != 0)
      break;
  }
}
```

## Open questions

- Product English for node value (string + large POD to isnil@+0x131).
- Dual of value dtor `FUN_00409b80` and erase-range parent `FUN_00407b70` (not OWN).
- Runtime / bit-exact.
