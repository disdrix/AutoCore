# Annotated low-level: FUN_008cc500

| Field | Value |
|---|---|
| Stable ID | `aa_008cc500` |
| VA | `0x008cc500` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_008cc500`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
char * __fastcall FUN_008cc500(int param_1)

{
  int in_EAX;
  
  if (param_1 == 1) {
    if (in_EAX == 1) {
      return "Shaman";
    }
    if (in_EAX != 2) {
      if (in_EAX != 3) {
        return "Champion";
      }
      return "Avenger";
    }
    return "Archon";
  }
  if (param_1 != 2) {
    if (in_EAX == 1) {
      return "Engineer";
    }
    if (in_EAX != 2) {
      if (in_EAX != 3) {
        return "Commando";
      }
      return "Bounty Hunter";
    }
    return "Lieutenant";
  }
  if (in_EAX == 1) {
    return "Constructor";
  }
  if (in_EAX != 2) {
    if (in_EAX != 3) {
      return "Terminator";
    }
    return "Agent";
  }
  return "MasterMind";
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.
