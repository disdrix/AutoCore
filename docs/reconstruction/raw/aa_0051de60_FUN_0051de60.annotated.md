# Annotated low-level: SkillCNDHash_ScalarDeletingDtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051de60` |
| VA | `0x0051de60`–`0x0051de7d` inclusive (**30 B**) |
| Ghidra | `FUN_0051de60` |
| Canonical | `SkillCNDHash_ScalarDeletingDtor_Inferred` |
| System | skills-abilities / skill-family CNDHash |
| Date | 2026-08-04 (WQ9F-H OWN dual) |

## Machine-level notes

- Source: raw capture + WQ9F-H re-verify (`read_memory` full body + `disassemble_function`).
- **Bytes authority** for epilogue after `operator_delete` (decomp warns noreturn incorrectly).
- Role: MSVC scalar deleting dtor — vtbl[0] of `PTR_FUN_009ce1b8` (same vtbl as `SkillCNDHash_Ctor_Inferred`).
- Complete body is **not** this VA: `FUN_0051de80` (`HashError:Destructor, already locked for traversal`).

## Signature (sealed)

```c
// ECX = this (SkillCNDHash*); stack flags; RET 4; returns this*
void* __thiscall SkillCNDHash_ScalarDeletingDtor_Inferred(void* self, uint8_t flags);
```

## Pseudocode (annotated)

```c
void* __thiscall SkillCNDHash_ScalarDeletingDtor_Inferred(void* self, uint8_t flags)
{
  // ESI = self
  SkillCNDHash_CompleteDtor_Inferred(self);  // FUN_0051de80 — not OWN
  if ((flags & 1u) != 0u) {
    operator_delete(self);                   // free heap host only when bit0 set
  }
  return self;                               // RET 4 (C2 04 00)
}
```

## Xrefs

| Kind | Address | Notes |
|---|---|---|
| DATA vtbl[0] | `0x009ce1b8` | dword = `0x0051de60` |
| Code CALL | none recovered | pure virtual dispatch entry |

## Open gaps

- Product/PDB demangle for host class (family uses `_Inferred`).
- Complete dtor `FUN_0051de80` body dual not owned here.
- Runtime / bit-exact open.
