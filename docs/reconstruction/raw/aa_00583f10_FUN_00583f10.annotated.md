# Annotated low-level: NDSpecialFX_ForEachHostPtrTablePair_ApplyFxToggle_Inferred (FUN_00583f10)

| Field | Value |
|---|---|
| Stable ID | `aa_00583f10` |
| VA | `0x00583f10`–`0x00583f9e` |
| Size | 143 bytes |
| System | client-fx / NDSpecialFX multi-host fan-out |
| Date | 2026-07-29 (W25-D dual seal) |
| Prior scaffold | 2026-07-23 |

## Machine-level notes

- Source: scaffold raw + W25-D `decompile_function` / `read_memory` re-verify.
- Complements sealed `NDSpecialFX_ForEachOwnedBit7_ApplyFxToggle_Inferred` (`0x004b7030`): that unit walks **one host's** owned-child list; **this** unit walks **tables of host pointers** and applies that helper to each.
- MSVC **thiscall** + **`ret 0x10`** (four stack args forwarded unchanged).
- Vbase adjust: `base = this + *(*(this+4)+4)` then two `[begin,end)` dword ranges.
- **Decompiler gap (sealed by bytes):** before each `call 004b7030`, `mov ecx,[edi]` loads host from the iterator; Ghidra shows only the four stack formals.

## ABI

```
// Entry: ECX = table_owner*
// Stack: want_start, want_stop, use_slot_filter, slot_filter
// Exit:  ret 0x10; EAX = last base (unused / residual)
void __thiscall NDSpecialFX_ForEachHostPtrTablePair_ApplyFxToggle_Inferred(
    void *table_owner,
    int want_start,
    int want_stop,
    char use_slot_filter,
    int slot_filter);
```

## Annotated pseudocode

```c
// Ghidra: FUN_00583f10
int __thiscall NDSpecialFX_ForEachHostPtrTablePair_ApplyFxToggle_Inferred(
    int this_owner,
    int want_start,
    int want_stop,
    int use_slot_filter,
    int slot_filter)
{
  int base = *(int *)(*(int *)(this_owner + 4) + 4) + this_owner;
  int it = *(int *)(base + 0x128);
  if (it != *(int *)(base + 0x12c)) {
    do {
      // ECX = *(int*)it  — host pointer (bytes; decompiler omits)
      FUN_004b7030(want_start, want_stop, use_slot_filter, slot_filter);
      it = it + 4;
    } while (it != *(int *)(*(int *)(*(int *)(this_owner + 4) + 4) + 0x12c + this_owner));
  }
  int vb = *(int *)(*(int *)(this_owner + 4) + 4);
  it = *(int *)(vb + 0x13c + this_owner);
  base = vb + this_owner;
  if (it != *(int *)(vb + 0x140 + this_owner)) {
    do {
      base = FUN_004b7030(want_start, want_stop, use_slot_filter, slot_filter);
      it = it + 4;
    } while (it != *(int *)(*(int *)(*(int *)(this_owner + 4) + 4) + 0x140 + this_owner));
  }
  return base;
}
```

## Layout (touched)

| Object | Off | Role |
|---|---:|---|
| this | `+4` | vbptr |
| *vbptr | `+4` | vbtable slot → delta |
| base | `+0x128` | host* vector begin (table A) |
| base | `+0x12c` | host* vector end (table A) |
| base | `+0x13c` | host* vector begin (table B) |
| base | `+0x140` | host* vector end (table B) |

## Open questions

- Product C++ member / class name for the table owner.
- English labels for tables A vs B (e.g. primary vs secondary attachment hosts).
- Whether EAX return is consumed by any caller (most treat as void).
- Runtime / bit-exact / differential.
