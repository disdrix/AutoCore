# Raw capture: FUN_0062a410

| Field | Value |
|---|---|
| **Stable ID** | `aa_0062a410` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0062a410` |
| **Canonical name** | `FUN_0062a410` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0062a410(int param_1,undefined4 param_2)



{

  int iVar1;

  int *piVar2;

  

  iVar1 = *(int *)(param_1 + 0x40);

  while (iVar1 = iVar1 + -1, -1 < iVar1) {

    piVar2 = *(int **)(*(int *)(param_1 + 0x3c) + iVar1 * 4);

    if (piVar2 != (int *)0x0) {

      (**(code **)(*piVar2 + 0xc))(param_2);

    }

  }

  FUN_005fff20();

  return;

}
```


---

## Live re-seal append — 2026-07-29 W24-P dual A/B

| Field | Value |
|---|---|
| **Tools** | Ghidra `decompile_function` + `read_memory` + `get_function_by_address` + callers/callees/signature (**no** `disassemble_bytes`) |
| **Excluded** | Launcher; parent ledgers; `disassemble_bytes` |
| **Live decompile** | Matches 2026-07-23 raw body (no CF drift) |
| **Body** | `0x0062a410`–`0x0062a444` exclusive (**52** / `0x34` B); Ghidra max `0062a443` |
| **Epilog** | `MOV EAX, EDI; POP EDI; POP ESI; JMP FUN_005fff20` — **tail-call** compact; **no** `ret N` |
| **ABI** | **cdecl** two stack args: `manager`, `action`. Caller (`FUN_0055df40`) does `ADD ESP, 8` after return |
| **EAX residual** | Before JMP, `EAX = manager + 0x3c` (pointer-vector header `{base*, count}`); `FUN_005fff20` consumes **EAX** (`in_EAX`) |
| **Callers** | `FUN_0055df40` (`ActionManager_UnregisterAndRelease_Inferred`) only (Ghidra callers) |
| **Callees** | `FUN_005fff20` @ `0x005fff20` (null-slot compact of vector at EAX) |
| **Role** | Walk manager listener table reverse; call each non-null listener `vtbl+0xC(action)`; compact nulls |
| **Canonical name** | `ActionManager_NotifyListenersAndCompact_Inferred` (**Inferred** — no plate string) |

### Body hex (`read_memory` 52 B)

```
56578b7c240c8b774083c73c83ee01781a538b5c24148b078b0cb085c974068b1153ff520c83ee0179ec5b8bc75f5ee9dc5afdff
```

### Byte-derived control flow

```
push esi / push edi
mov  edi, [esp+0xC]          ; manager (arg0)
mov  esi, [edi+0x40]         ; count
add  edi, 0x3c               ; &vector = manager+0x3c  (base at +0, count at +4)
sub  esi, 1
js   after_loop
push ebx
mov  ebx, [esp+0x14]         ; action (arg1)
loop:
  mov  eax, [edi]            ; base*
  mov  ecx, [eax+esi*4]      ; listener*
  test ecx, ecx
  jz   next
  mov  edx, [ecx]            ; vtbl
  push ebx                   ; action
  call dword ptr [edx+0xC]   ; thiscall ECX=listener, stack=action
next:
  sub  esi, 1
  jns  loop
pop  ebx
after_loop:
mov  eax, edi                ; EAX = manager+0x3c
pop  edi / pop esi
jmp  FUN_005fff20            ; compact null slots; ret to original caller
```

### Decompiler residual

- `FUN_005fff20()` shown with no args — **false ABI**. Bytes set **EAX = manager+0x3c** then tail-jump.
- Virtual call is `__thiscall` on listener (`ECX=listener`); action is the sole stack formal to the vfunc.
