# Annotated low-level: FUN_00460d70 → RcElem28_Release

| Field | Value |
|---|---|
| Stable ID | `aa_00460d70` |
| VA | `0x00460d70`–`0x00460d83` exclusive (**19 B**) |
| System | Per-element Release of shared resource `@+0x10` on RC-elem **0x1c** |
| Date | 2026-07-23 scaffold; W36-O seal 2026-07-29 |
| Canonical name | `RcElem28_Release` (**Inferred**) |

## Machine-level notes

- **Bytes win** on ABI: **EAX=`elem*`**, no stack formals, **`RET 0`** or **tail JMP** to `shared->vtbl[+8]`.
- Decompiler warning "jumptable / treating indirect jump as call" — bytes are `FF 62 08` = **`JMP [EDX+8]`**, not `CALL`. Behavior for live return path is equivalent when refcount stays >0; zero path transfers without return to this leaf.
- Shared layout: `+0` vtbl, `+4` refcount (int). Matches AddRef twin shape used by fill-n / copy helpers (refcount++ / vtbl+4).
- Walkers always `MOV EAX, cur; CALL` this leaf (`FUN_00461780`, clear, erase, EH unwind).
- Reject scaffold long `Named_CalleeOf_*`.

## Signature (sealed)

```c
// EAX=elem*, RET 0 (or tail to vtbl+8)
void RcElem28_Release(RcElem28* elem /* EAX */);
```

## Control flow (annotated)

1. `ecx = *(elem + 0x10)` (shared*).
2. If null → ret.
3. `--*(ecx + 4)` (refcount).
4. If refcount != 0 → ret.
5. `edx = *ecx` (vtbl); `jmp [edx+8]` (destroy/free).

## Full hex

See raw W36-O append (19 B).
