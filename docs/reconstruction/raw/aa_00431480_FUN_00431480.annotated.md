# Annotated low-level: BasicString_CopyBackward_Thunk_0x1c

| Field | Value |
|---|---|
| Stable ID | `aa_00431480` |
| VA | `0x00431480`–`0x0043149d` exclusive (**29 B**) |
| System | MSVC `basic_string<char>` copy-backward thunk |
| Date | 2026-07-29 (W33-I re-seal) |
| Ghidra | `FUN_00431480` |

## Machine-level notes

- Thin trampoline: call-site **cdecl 3-arg** (`first`, `last`, `dest_end`); body `PUSH ECX` + reorder into free `FUN_00431790`.
- Ghidra labels `__thiscall` + 4 formals; sole caller does **not** load ECX for this — ECX path is dead scratch; effective contract is 3 stack pointers.
- Callee `FUN_00431790`: classic `copy_backward` on stride **0x1c** via IAT `operator=` (overlap-safe reverse assign).
- Sibling pattern: `FUN_00431430` similarly thunks relocate to `FUN_0046a2c0`.
- Parent mid-insert: open hole by copy-backward of `[where, end-count)` into ending at `old_end`, then fill via `00431450`.

## Pseudocode (annotated)

```c
// Effective call-site contract (cdecl):
void BasicString_CopyBackward_Thunk_0x1c(
    BasicString_0x1c* first,
    BasicString_0x1c* last,
    BasicString_0x1c* dest_end)
{
  // body reorders / ignores ECX; forwards to free helper:
  FUN_00431790(first, last, dest_end);  // copy_backward [first,last) → ending at dest_end
}
```

## Open questions

1. Product demangle for thunk vs free `copy_backward`.
2. Nested `FUN_00431790` dual residual (not W33-I owned).
3. Whether any other caller ever sets ECX meaningfully (none known).
