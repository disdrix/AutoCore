# Annotated low-level: CVOGHBMissionPatrol_ctor

| Field | Value |
|---|---|
| Stable ID | `aa_00650d30` |
| VA | `0x00650d30` |
| Body | `0x00650d30`–`0x00650dd4` (**164 bytes**) |
| Canonical name | `CVOGHBMissionPatrol_ctor` |
| Ghidra name | `FUN_00650d30` |
| System | missions-progression / mission-patrol HB |
| Date | 2026-07-29 (W23-T dual seal; scaffold 2026-07-23) |
| Tools | `decompile_function` + `read_memory` only |

## Machine-level notes

- **ABI:** MSVC `__thiscall` — ECX = `this` (pre-allocated 0x40 HB), stack: `pOwnerOrNull`, `pRequirement`; callee cleanup **`ret 8`**.
- **SEH:** frame with filter `LAB_009a8ec8`; state 0 after base ctor + vftable install.
- **Vftable:** writes `PTR_FUN_009e52e4` — same family as `CVOGHBMissionPatrol_Fn3` (`0x00650e50`), `Fn6` (`0x00650de0`), `Tick` (`0x00650f90`).
- **Type token:** `this+0x1c` (`param_1[7]`) ← **`0xC`** (patrol HB type).
- **Period:** `SetPeriodAndCounter(this, -1000, true)` → `+0x0C` and `+0x10` = `-1000`; then `this+0x08` ← **`1000`** (pulse ms).
- **Owner attach:** if `pOwner != null`, MI adjust: `pOwner + *( *(pOwner+4) + 4 ) + 4`, then `CVOGHBBase_AttachOwnerObject`.
- Prefer assembly when decompiler conflicts; no `disassemble_bytes` used — offsets from decompile + entry hex.

## Layout written (this HB)

| Off | Source | Role (sealed) |
|----:|--------|---------------|
| `+0x00` | `PTR_FUN_009e52e4` | vftable |
| `+0x08` | imm `1000` | pulse period (ms) |
| `+0x0C` / `+0x10` | `-1000` via SetPeriodAndCounter | sentinel / counter |
| `+0x1C` | imm `0xC` | HB type = patrol |
| `+0x24` | `(*req->vtbl+0x58)()` | requirement-derived dword |
| `+0x28` | `*(req[1]+0x10)` | nested field |
| `+0x30` / `+0x34` | `req[0xC]` / `req[0xD]` | TFID-like pair copy |
| `+0x38` | `req[7]` | requirement field copy |
| `+0x18` | via AttachOwnerObject | owner object ptr |

## Pseudocode (annotated)

```c
// thiscall; ret 8
CVOGHBBase *CVOGHBMissionPatrol_ctor(CVOGHBBase *this, void *pOwnerOrNull, int *pRequirement)
{
  // SEH frame ...
  CVOGHBBase_ctor(this);
  *(void **)this = &PTR_FUN_009e52e4;                 // MissionPatrol vftable

  this[9]  = (*(fn **)(*pRequirement + 0x58))();     // vtbl+0x58 on requirement
  this[10] = *(uint32_t *)(pRequirement[1] + 0x10);
  this[0xc] = pRequirement[0xc];
  this[0xd] = pRequirement[0xd];
  this[0xe] = pRequirement[7];
  this[7]   = 0xC;                                   // type

  CVOGHBBase_SetPeriodAndCounter(this, -1000, true);
  if (pOwnerOrNull != NULL) {
    // MSVC multiple-inheritance this-adjust from complete object
    pOwnerOrNull = (void *)(
        (int)pOwnerOrNull
      + *(int *)(*(int *)((int)pOwnerOrNull + 4) + 4)
      + 4);
  }
  CVOGHBBase_AttachOwnerObject(this, pOwnerOrNull);
  this[2] = 1000;                                    // +0x08 pulse
  return this;
}
```

## Call graph

| Dir | Symbol | VA |
|-----|--------|----|
| Caller | `CVOGObjectiveRequirement_Patrol_InitActive` | `0x0060e3d0` (alloc 0x40, enqueue, Start) |
| Callee | `CVOGHBBase_ctor` | `0x00508200` |
| Callee | `CVOGHBBase_SetPeriodAndCounter` | `0x005081a0` |
| Callee | `CVOGHBBase_AttachOwnerObject` | `0x005083b0` |

## Open questions

1. Product English name of class (MissionPatrol is family-inferred from sibling seals + sole parent).
2. Exact semantic of `vtbl+0x58` return and `req[7]` / TFID pair.
3. Runtime / bit-exact / differential — open.
