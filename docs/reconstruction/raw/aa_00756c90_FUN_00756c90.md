# Raw capture: FUN_00756c90

| Field | Value |
|---|---|
| **Stable ID** | `aa_00756c90` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00756c90` |
| **Canonical name** | `FUN_00756c90` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_00756c90(void)

{
  int iVar1;
  int *_Dst;
  int unaff_EDI;
  
  iVar1 = *(int *)(unaff_EDI + 0x88);
  if (iVar1 == 0) {
    return 0xffffffff;
  }
  if ((*(int *)(iVar1 + 0x94) == 0) || (*(int *)(iVar1 + 0x98) - *(int *)(iVar1 + 0x94) >> 2 != 1))
  {
    for (_Dst = *(int **)(iVar1 + 0x94); _Dst != *(int **)(iVar1 + 0x98); _Dst = _Dst + 1) {
      if (*_Dst == unaff_EDI) {
        memmove(_Dst,_Dst + 1,(*(int *)(iVar1 + 0x98) - (int)(_Dst + 1) >> 2) * 4);
        *(int *)(iVar1 + 0x98) = *(int *)(iVar1 + 0x98) + -4;
        break;
      }
    }
    FUN_00456960(*(undefined4 *)(*(int *)(unaff_EDI + 0x88) + 0x94));
  }
  return 0;
}
```

---

## R12-003 re-verify append (2026-08-05)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **R12-003** |
| **Tools** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + callers/xrefs (`get_function_callers` / `get_function_callees` / `get_function_xrefs` / `get_xrefs_to`) |
| **Forbidden tools** | `disassemble_bytes` (not used); Launcher (not used) |
| **Live decompile** | ≡ frozen 2026-07-23 raw CF (null-group early-out; size==1 skip; erase loop; InsertN front) |
| **Body** | `0x00756c90`–`0x00756d31` inclusive (**162 B** / `0xA2`); epilogue success `33 C0 5E 59 C3`, fail `83 C8 FF 5E 59 C3` (`RET` / no stdcall pop) |
| **Inferred name** | `UI_Widget_MoveToFrontInParentGroup_Inferred` |
| **System** | inventory-transfer (UI tab/group chrome helper; no inventory wire) |
| **Parent dual** | `0x00888b00` `UI_Cs2dTabs_SelectTab_Inferred` (R11-031) |
| **Partition** | `WAVE_2026-08-05_r12_residual_partition_map.md` → R12-003 |
| **Terminal** | **false** |

### ABI (sealed via `disassemble_function` + parent call-site disasm)

```text
ENTRY 00756c90:
  PUSH ECX                 ; scratch local for InsertN value slot
  PUSH ESI
  MOV  ESI, [EDI + 0x88]   ; EDI = widget formal (register-only)
  TEST ESI, ESI
  JZ   fail_ret            ; return -1
  ...
  XOR  EAX, EAX            ; success
  POP  ESI
  POP  ECX
  RET                      ; no stack cleanup (no stack formals)
fail_ret:
  OR   EAX, 0xFFFFFFFF
  POP  ESI
  POP  ECX
  RET
```

| Formal | Location | Role |
|---|---|---|
| `widget` | **EDI** | UI widget / tab chrome control |
| (none) | stack | no stack args |
| return | **EAX** | `0` ok; `0xFFFFFFFF` if `widget+0x88 == 0` |

**Not** ECX thiscall. Custom register ABI: EDI in, EAX out, `RET`.

### Object layout

| Offset | Role |
|---:|---|
| `widget+0x88` | parent **group** pointer (not cleared by this unit; contrast `FUN_00756be0`) |
| `group+0x90` | base passed to `FUN_00456960` as vector object (`param_2`) |
| `group+0x94` | vector **begin** (`T**`) |
| `group+0x98` | vector **end** (`T**`) |
| `group+0x9c` | vector capacity end (via InsertN helper layout `param_2+0xc`) |

Vector element size **4** (pointer). Size = `(end - begin) >> 2`.

### Control flow (live ≡ raw)

```text
UI_Widget_MoveToFrontInParentGroup_Inferred(widget@EDI) -> EAX:
  group = widget[+0x88]
  if group == 0: return -1
  begin = group[+0x94]; end = group[+0x98]
  if begin != 0 AND size == 1:
    return 0                    // singleton: no reshuffle
  // else (begin==0 OR size != 1):
  for p in [begin, end):
    if *p == widget:
      memmove(p, p+1, (end - (p+1))); end -= 4; break
  // InsertN: count=1, vec=group+0x90, pos=begin, value=widget
  FUN_00456960(/*ECX=*/1, /*EDX=*/group+0x90, /*stack pos=*/begin, /*EAX=&widget*/)
  return 0
```

Net: **erase self if present, then insert self at front** of parent group pointer vector — i.e. **move-to-front** (or ensure membership at front if absent). Early-out when vector already has exactly one element.

### Callees

| VA / symbol | Role |
|---|---|
| `memmove` @ IAT `0x009c652c` | erase shift |
| `FUN_00456960` | `StdVector` InsertN (count=1 at begin; value=`widget`) |

### Callers (28 UNCONDITIONAL_CALL xrefs; 12 named functions)

| Function | Sites (sample) | EDI setup |
|---|---|---|
| `FUN_00888b00` | `00888bb9`, `00888c73` | collapsed tab `+0x51c` / `+0x520` |
| `FUN_00889410` | 5 sites | inactive tab chrome slots |
| `FUN_00889fb0` | `0088a069`, `0088a123` | `+0x514` / `+0x518` |
| `FUN_00887780` | 5 sites | multi-tab host |
| `FUN_0086b9b0`, `FUN_00878300`, `FUN_00879ea0`, `FUN_0087bc60` | 1–2 each | UI hosts |
| `FUN_008a9630`, `FUN_008b84a0`, `FUN_008e1020`, `FUN_008e6b50` | 1–2 each | UI hosts |
| `@0090a4ac`, `@0090b62c` | 1 each | no containing function in Ghidra |

Typical pairing (parent duals): after `FUN_00822cb0` with **BL=0** (collapse) on same widget, `MOV EDI, widget; CALL 00756c90`.

### Evidence notes

- Live `analyze_function_complete`: classification worker; callees `FUN_00456960`,`memmove`; xref_count **28**.
- Sibling `FUN_00756be0` is true **detach** (erase + clear `widget+0x88`); this unit does **not** clear `+0x88` and re-inserts via InsertN — **not** pure remove.
- Sibling `FUN_00756d40` rebinds/restores `+0x88` via `FUN_00756be0` + `FUN_004406e0` — different attach path.
