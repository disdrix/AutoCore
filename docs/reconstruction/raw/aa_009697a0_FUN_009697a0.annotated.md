# Annotated low-level: StringVec_PushBackIfAbsent_EnsureTrailingDirSep

| Field | Value |
|---|---|
| Stable ID | `aa_009697a0` |
| VA | `0x009697a0`–`0x0096982D` exclusive (**141 B** / `0x8D`) |
| System | `StringVecShell` path list (unique push_back + trailing dir sep) |
| Date | 2026-07-29 (W34-J dual A/B) |
| Ghidra | `FUN_009697a0` |

## Machine-level notes

- **EDI** = `StringVecShell*` (not ESI; contrast push_front `00969830` which uses ESI).
- Stack formal = source path `basic_string*` (`RET 4`).
- Stride **0x1c** = MSVC `basic_string` size in this binary.
- Equality uses IAT `std::operator==` on string elements; **case-sensitive** exact match after normalize.
- Absent ⇒ `StringVec_PushBack` (`FUN_004301f0`, W33-D) with ECX=EDI.
- Present ⇒ no-op (no erase, no reorder).
- Trailing-sep policy owned by `FUN_009694e0` (W32-A): append `/` unless last ∈ `{':','/','\\'}` or empty.

## Pseudocode (annotated)

```c
// EDI = StringVecShell*; stack path*; ret 4; void
// SEH: LAB_009acae3
void StringVec_PushBackIfAbsent_EnsureTrailingDirSep(
    StringVecShell* self /*EDI*/,
    const void* path /*stack basic_string*/)
{
  basic_string local; // 0x1c
  FUN_009694e0(&local, path);           // normalize trailing dir sep

  basic_string* it   = *(basic_string**)(self + 4); // begin
  basic_string* end  = *(basic_string**)(self + 8); // end
  bool absent = (it == end);
  if (!absent) {
    do {
      if (operator==(it, &local)) break;  // found
      it = (basic_string*)((char*)it + 0x1c);
    } while (it != end);
    absent = (it == end);
  }
  if (absent) {
    StringVec_PushBack(self, &local);   // FUN_004301f0 thiscall
  }
  ~basic_string(&local);
}
```

## Sibling family

| VA | Role |
|----|------|
| `0x009694e0` | normalize/copy ensure trailing dir sep |
| `0x009696c0` | normalize → find + erase |
| `0x009697a0` | normalize → find; if absent push_back (**this**) |
| `0x00969830` | normalize → insert at begin (push_front) |

## Gaps

- Ghidra xrefs empty (no sealed retail callers).
- Product English for the path-list container role.
- Runtime / bit-exact / differential open.
