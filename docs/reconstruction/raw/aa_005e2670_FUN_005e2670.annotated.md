# Annotated: FUN_005e2670 → EdRiverHandle_StampPolylineHeightGrid_Inferred

| Field | Value |
|---|---|
| **Stable ID** | `aa_005e2670` |
| **VA** | `0x005e2670`–`0x005e29c8` exclusive (**856 B** / `0x358`) |
| **Canonical name** | `EdRiverHandle_StampPolylineHeightGrid_Inferred` (**Inferred**) |
| **Ghidra name** | `FUN_005e2670` |
| **Wave** | W36-D 2026-07-29 |
| **Raw** | `aa_005e2670_FUN_005e2670.md` (scaffold + W36-D append) |

---

## Purpose (structural)

Editor **river-handle** family worker (vtable @ `0x009dc9b8` near `ed_riverhandle.g*`). Walks a handle chain, materializes polyline segment endpoints into a temporary circular list, stamps each segment’s influence onto a map ushort grid via `FUN_006442c0`, requantizes the union bbox, invalidates 16×16 tiles, then releases the chain.

---

## Signature

```c
// ECX = handle-chain host (this)
// stack: map*, argA, argB(float), argC
// RET 0x10
void EdRiverHandle_StampPolylineHeightGrid_Inferred(
    void* host /*ECX*/,
    void* map /*stack0*/,
    uint32_t argA /*stack1*/,
    float argB /*stack2*/,
    uint32_t argC /*stack3*/);
```

| Slot | Source | Conf |
|---|---|---|
| host | **ECX** | **High** |
| map | Stack[+4]; decompiler `unaff_retaddr` | **High** |
| argA/B/C | Stack[+8/+c/+10] | **High** |
| cleanup | **`RET 0x10`** | **High** |

### Map layout (used fields)

| Off | Field | Conf |
|---|---|---|
| +0x10 | int width / row stride | High |
| +0x18 | float cell size | High |
| +0x3e0 | `uint16_t*` grid | High |

---

## Stages

1. **Seed:** `node = host->vtbl[+0xb8]()`; null → return.
2. **Prep:** `node->vtbl[+0xb4](1)`; alloc circular sentinel (`0043c460`).
3. **Per-node stamp loop:** clear list; `push cell*2.0`; `node->vtbl[+0xbc](list)`; for consecutive list nodes pack xyz@`+8/+c/+10`, sample `vtbl[+0xac]`, call `006442c0(map,…)`, union bbox; advance chain via `*(node+0x14)+4` → `vtbl[+0x24]`.
4. **Requant:** for cells in bbox: ushort→float (`*1/65536`) + blend, clamp `[0,1]`, `*65535`, round (`006a3db0`), store u16.
5. **Tiles:** `004a8d40(map, min−2, min−2, max+2, max+2)`.
6. **Release:** walk start calling `vtbl[+0x70](1,0)`; free list/sentinel; SEH exit.

---

## Constants

| VA | Value | Role |
|---|---|---|
| `0x00a10e74` | 2.0 | cell size ×2 for list fill |
| `0x00af443c` | ~1/65536 | u16→unit |
| `0x00a0f2a0` | 1.0 | clamp high |
| `0x00aaa634` | 65535.0 | unit→u16 domain |

---

## Gaps

- Product/PDB English for host class, vfunc names, argA/B/C semantics.
- Nested `006442c0` / `004a8d40` not dual-owned.
- No runtime / bit-exact.
- Direct code callers none (virtual only).

---

## Verdict

**accept-with-gaps** — ABI, body size, CF stages, constants, callees sealed; product residual.
