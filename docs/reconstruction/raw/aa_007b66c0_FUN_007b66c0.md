# Raw capture: FUN_007b66c0

| Field | Value |
|---|---|
| **Stable ID** | `aa_007b66c0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x007b66c0`–`0x007b6728` |
| **Canonical name** | `FUN_007b66c0` |
| **System** | ND path / interface resource map |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_007b66c0(int param_1)

{
  char *in_EAX;
  char *pcVar1;
  char *pcVar2;
  int iVar3;
  
  iVar3 = param_1;
  if ((*(byte *)(param_1 + 4) & 2) == 0) {
    return 0;
  }
  pcVar1 = strrchr(in_EAX,0x5c);
  pcVar2 = strrchr(in_EAX,0x2f);
  if (pcVar1 == (char *)0x0) {
    if (pcVar2 == (char *)0x0) goto LAB_007b66f8;
  }
  else if (pcVar2 < pcVar1) {
    pcVar2 = pcVar1;
  }
  in_EAX = pcVar2 + 1;
LAB_007b66f8:
  FUN_00989e00(&param_1,in_EAX);
  iVar3 = (**(code **)(**(int **)(iVar3 + 0xc) + 0x14))(&param_1);
  if (iVar3 == 0) {
    return 0;
  }
  return 1;
}
```

---

## Append v2026-07-29 (W17-M OWN-ONLY dual seal)

| Field | Value |
|---|---|
| **Agent** | W17-M |
| **Tools** | `decompile_function` + `force_decompile` + `read_memory` + `get_function_by_address` + callees/callers/xrefs |
| **Avoided** | `disassemble_bytes` |
| **Dual decompile** | Rep1 `decompile_function` ≡ Rep2 `force_decompile` (identical) → no third-rep |

### Body range / size

- Entry `0x007b66c0`, exclusive end `0x007b6728` (Ghidra function body).
- Size **104 B** (`0x68`). Following `CC` pad is not part of the unit.

### `read_memory` body hex (104 B @ `0x007b66c0`)

```
558b6c2408f6450402578bf87654538b1d2c669c00566a5c57ffd36a2f578bf0ffd383c41085f6750685c07508eb093bf076028bc68d7801578d44241850b9c8ead100e8f8361d008b4d0c8b118d44241450ff521485c05e5b76075fb0015dc204005f32c05dc204
```

### ABI (bytes-authoritative)

| Item | Evidence |
|---|---|
| Stack arg | `MOV EBP,[ESP+8]` after `PUSH EBP` → one stack dword = context |
| Path string | `MOV EDI,EAX` at entry → **path C-string in EAX** |
| Epilogue | `RET 4` (`C2 04 00`) both paths → stack arg cleaned by callee (**stdcall-ish** for the stack operand) |
| Return | success `MOV AL,1`; fail `XOR AL,AL` → **byte bool in AL** (0/1) |

### Control flow (sealed)

1. If `(*(byte*)(ctx+4) & 2) == 0` → return **0** (gate bit not set).
2. `strrchr(path,'\\')` and `strrchr(path,'/')` via IAT slot `0x009c662c`.
3. Basename = char after **later** of the two separators; if neither found, use full path.
4. `FUN_00989e00` with `ECX=0x00d1eac8` (string pool thiscall), out-slot on stack, C-string basename → interned token/handle.
5. `obj = *(ctx+0xc)`; `obj->vtbl[0x14/4](&token)` (virtual call offset **+0x14**).
6. Return **0** if virtual result is 0, else **1**.

### Callees

| Target | Role |
|---|---|
| `strrchr` (IAT `0x009c662c`) | last `\\` and `/` |
| `FUN_00989e00` @ `0x00989e00` | string-pool intern (`this=0x00d1eac8`) |
| Virtual `*(**(ctx+0xc))+0x14` | map/table lookup by token |

### Callers (sample / xrefs)

`NDUIWindow_LoadInterfaceFile` (`0x007b0ef0`), `NDXml_LoadDocumentFromPath` (`0x007b6c70`), `FUN_007b6730`, `FUN_007b6770`, `FUN_007b6a90`, `FUN_007b7b70`, `FUN_00724870`, `FUN_0079bf80`, `FUN_00850800`, `FUN_00857ee0`, `FUN_0085d970`, `FUN_0088ad60`, `FUN_008edf80` (×3), `FUN_009463b0`, plus un-named sites. Inventory **xref_count=19**.

### Notes

- Decompiler reuses stack slot `param_1` as out-handle for `FUN_00989e00` after context is saved in `iVar3`/`EDI` path — correct but easy to misread.
- `TEST … / JBE` after flag mask: after `TEST` CF=0 so **JBE ≡ JZ** (gate is zero-bit, not unsigned-below).
