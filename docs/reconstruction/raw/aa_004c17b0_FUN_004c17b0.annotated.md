# Annotated low-level: ParticleFluidLiquidChild_HostLink_Ctor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004c17b0` |
| VA | `0x004c17b0` |
| System | particle / fluid liquid-child host-link |
| Date | 2026-08-04 (W38-L dual seal) |
| Canonical | `ParticleFluidLiquidChild_HostLink_Ctor_Inferred` |

## Machine-level notes

- Body **79 B** inclusive `0x004c17b0`–`0x004c17fe`; epilogue **`C2 04 00`**.
- **ECX=`this`** (8-byte subobject); stack **host\***; returns **this**.
- Vtbl store: `C7 45 00 A4 B7 9C 00` → `*this = 0x009cb7a4`.
- Host store: `89 45 04` → `*(this+4) = host`.
- Null host: `74 34` skips list registration.
- List head: `8B 78 08` → `EDI = *(host+8)` (sentinel).
- IncSize base: `8D 70 04` → `ESI = host+4` (size @ `ESI+8` = `host+0xC`).
- Value setup: `LEA EAX,[ESP+14]; …; MOV [ESP+20],EBP` overwrites stack host slot with `this` before `FUN_006759b0`.
- Splice: `MOV [EDI+4],EBX` (sentinel->prev = node); `MOV [EDX],EBX` (old_prev->next = node).
- Prefer bytes when decompiler flattens `FUN_004c20b0` thiscall target.

## Pseudocode (annotated)

```c
HostLink* __thiscall FUN_004c17b0(HostLink* self, void* host)
{
  self->vtbl = &PTR_LAB_009cb7a4;   // 009cb7a4 → method 004c09c0
  self->host = host;
  if (host != NULL) {
    ListNode* head = *(ListNode**)((char*)host + 8);
    void* value = self;             // stack slot reuse (bytes)
    ListNode* node = FUN_006759b0(head, head->prev, &value); // 0xC node
    FUN_004c20b0((char*)host + 4, 1); // size @ host+0xC; "list too long"
    head->prev = node;
    node->prev->next = node;        // push_back before sentinel
  }
  return self;                      // RET 4
}
```

## Open questions

- Product/PDB English for host-link class / vtbl `009cb7a4` ("ReflectionSu…" neighbor in rdata is suggestive only).
- Full dual of `FUN_006759b0` / `FUN_004c20b0` / vtbl method `0x004c09c0`.
- Runtime / bit-exact / differential.
