# Annotated low-level: FUN_0075b3b0 → GfxView_SetNear_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0075b3b0` |
| VA | `0x0075b3b0`–`0x0075b3cb` (**28 B**) |
| System | graphics / client camera (GfxView) |
| Date | 2026-07-29 W31-O |
| Canonical | `GfxView_SetNear_Inferred` |

## Machine-level notes

- **Leaf** thiscall: ECX=`GfxView*`, stack float `near`, **`RET 4`**.
- Bytes use **MOVSS** (IEEE float), not integer MOV — decompiler `undefined4` is width-only.
- Stores:
  - `+0xF0` = near (matches EnsureProjection near/far pair).
  - `+0x48` = 1 (projection dirty; gate for `GfxView_EnsureProjection_Inferred` `0x0075b7f0`).
  - `+0xCC` = 1 (second dirty; ctor also inits both to 1).
- Polarity twin `FUN_0075b390` (`+0xF4` far) same shape — not owned this wave.
- Primary factory caller `FUN_004cda90` passes **0.5f** (`0x3F000000`) immediately after `GfxView_Ctor`.

## Pseudocode (annotated)

```c
// ECX = GfxView*; stack float near; RET 4
void __thiscall GfxView_SetNear_Inferred(GfxView *this, float nearPlane)
{
  this->nearPlane /* +0xF0 */ = nearPlane;
  this->projDirty /* +0x48 */ = 1;
  this->auxDirty  /* +0xCC */ = 1;
}
```

## Full body hex

```
F3 0F 10 44 24 04   ; movss xmm0, [esp+4]
B0 01               ; mov al, 1
F3 0F 11 81 F0 00 00 00  ; movss [ecx+0xF0], xmm0
88 41 48            ; mov [ecx+0x48], al
88 81 CC 00 00 00   ; mov [ecx+0xCC], al
C2 04 00            ; ret 4
```

## Open questions

- Product English method name (SetNear / SetNearClip / etc.) — **Inferred**.
- Exact product meaning of `+0xCC` beyond “dirty with +0x48”.
