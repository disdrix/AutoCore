# Annotated low-level: FUN_00431ae0 / StringVec_Tidy

| Field | Value |
|---|---|
| Stable ID | `aa_00431ae0` |
| VA | `0x00431ae0`–`0x00431b27` exclusive (**71 B** / `0x47`) |
| System | `std::vector<basic_string>` tidy / buffer release (elem stride 0x1c) |
| Date | 2026-07-29 (W33-D dual) |
| Preferred name | `StringVec_Tidy` |
| Ghidra | `FUN_00431ae0` |
| Reject | `Named_CalleeOf_*assManager*_00431ae0` (scaffold) |

## Machine-level notes

- Live `read_memory` seals full body; epilogue bare **`C3`**.
- Entry: `53 8B D9` → EBX = ECX = this. Single register formal (thiscall / fastcall-this).
- **Null begin:** jump to shared epilogue that zeros **+4 / +8 / +0xC** (idempotent tidy).
- **Non-null:** destroy every live element `[begin, end)` with stride **`0x1c`** via IAT `~basic_string` (`FF 15 F4 62 9C 00`), then **`operator_delete(begin)`** (cdecl; `ADD ESP,4`), then zero triad.
- Ghidra decompile incorrectly marks `operator_delete` as noreturn and drops the post-delete stores — **bytes win**.
- Sibling `FUN_00431aa0` only runs the destroy loop (used during reallocation); this unit is full tidy/free.
- Thunk `0x00431ad0` (`E9 0B 00 00 00`) jumps here; some xrefs hit the thunk.
- Closes residual named by W32-D (`FUN_00431ae0` listing destroy ABI).

## Pseudocode (annotated)

```c
// ECX=self; bare ret; void
void StringVec_Tidy(StringVecShell* self)
{
  basic_string* begin = self->begin; // +4
  if (begin == nullptr) {
    self->begin = nullptr;
    self->end = nullptr;
    self->capEnd = nullptr;
    return;
  }

  basic_string* end = self->end; // +8
  for (basic_string* p = begin; p != end; p = (basic_string*)((uint8_t*)p + 0x1c))
    ~basic_string(p);

  operator_delete(begin);
  self->begin = nullptr;
  self->end = nullptr;
  self->capEnd = nullptr;
}
```

## Open questions

- Product plate for host container beyond `StringVecShell`.
- Exact CRT `operator_delete` import vs local wrapper address (behavioral free is sealed).
- Runtime / bit-exact / differential — open.
