# Annotated low-level: GfxStatsMonitor_RecreateResourceManagerQuery_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00966760` |
| VA | `0x00966760` |
| Canonical | `GfxStatsMonitor_RecreateResourceManagerQuery_Inferred` |
| System | graphics / gfxDevice Device Reset (Stats Monitor) |
| Date | 2026-08-04 (W38-Z) |

## Machine-level notes

- **EDI = Stats Monitor host** (not ECX thiscall). Parent `FUN_0075eff0` does `MOV EDI,[EBP+0x79c]` immediately before call — host is `device+0x79c` (`param_1[0x1e7]`).
- **Device**: `**DAT_00d1f044` is `IDirect3DDevice9*` (wrapper published at `DAT_00d1f044`; first field = device9).
- **Vcall `+0x1d8`**: D3D9 method index 118 = **`IDirect3DDevice9::CreateQuery`**.
- **Type imm 5**: `D3DQUERYTYPE_RESOURCEMANAGER`.
- **Out**: `IDirect3DQuery9**` at `host+0x3d4`.
- **Flag**: `host+0x3d8` cleared to 0 after CreateQuery (issued/active state off).
- **HRESULT fail**: force `*(host+0x3d4)=0`; **return value always 0** (`xor eax,eax`) — CreateQuery failure is **not** propagated to parent.
- Sibling at `0x00966790` (not OWN): Release query if non-null, null `+0x3d4`.

## Pseudocode (annotated)

```c
// EDI = StatsMonitor*  (register param from Device Reset parent)
// returns always 0 (success-coded); parent checks HRESULT are dead for this step
uint32_t GfxStatsMonitor_RecreateResourceManagerQuery_Inferred(void /* EDI host */)
{
  IDirect3DDevice9 *dev = *(IDirect3DDevice9 **)DAT_00d1f044; // *wrapper
  IDirect3DQuery9 **ppQuery = (IDirect3DQuery9 **)(host + 0x3d4);
  HRESULT hr = dev->lpVtbl->CreateQuery(dev, D3DQUERYTYPE_RESOURCEMANAGER /*5*/, ppQuery);
  *(uint8_t *)(host + 0x3d8) = 0;
  if (hr < 0) {
    *ppQuery = NULL;
  }
  return 0;
}
```

## Open questions

- Product C++ class name for host at `device+0x79c` beyond log "Stats Monitor".
- Meaning of flag byte `+0x3d8` (issued / active / valid).
- Why CreateQuery HRESULT is swallowed (always return 0).
