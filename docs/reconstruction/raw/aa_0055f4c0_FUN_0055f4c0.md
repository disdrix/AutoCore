# Raw capture: FUN_0055f4c0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0055f4c0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0055f4c0` |
| **Canonical name** | `FUN_0055f4c0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0055f4c0(undefined4 *param_1)



{

  int *piVar1;

  uint uVar2;

  

  piVar1 = DAT_00b05060;

  uVar2 = param_1[4] * 8 + 0xfU & 0xfffffff0;

  if ((uVar2 == 0) || ((param_1[3] != DAT_00b05060[5] && (DAT_00b05060[5] != 0)))) {

    DAT_00b05060[2] = DAT_00b05060[2] - uVar2;

    piVar1[3] = piVar1[3] + uVar2;

  }

  else {

    (**(code **)(*DAT_00b05060 + 0x28))(param_1[3],uVar2);

  }

  if (-1 < (int)param_1[2]) {

    (**(code **)(*DAT_00b05060 + 0x14))(*param_1,param_1[2] * 8,0x12);

  }

  return;

}
```


---

## W27-N re-verify (2026-07-29) — live Ghidra seal

| Field | Value |
|---|---|
| **Agent** | W27-N OWN-ONLY dual |
| **Tools** | `decompile_function` + `read_memory` + `get_function_by_address` + callers/xrefs (**no** `disassemble_bytes`) |
| **Program** | `autoassault.exe` base `0x400000` |
| **Body** | `0x0055f4c0`–`0x0055f51b` inclusive (**92 / `0x5C` B**); pad `CC` after `RET` |
| **Prologue (read_memory)** | `56 8B F1 8B 46 10 8B 56 0C 8B 0D 60 50 B0 00` — `MOV ESI,ECX`; load desc `+0x10`/`+0xc`; load `DAT_00b05060` |
| **Epilogue (read_memory @ 0x0055f519)** | `5F 5E C3` — **`ret`** (0 stack formals; ECX-only) |
| **ABI** | **__fastcall / thiscall** on descriptor: ECX = local worklist/alloc desc; void; no stack cleanup |
| **Classification** | worker |
| **Callers (5)** | `FUN_0055fa40`, `FUN_0055fbf0`, `FUN_0055ff20`, `FUN_00560020`, `FUN_00634450` (8 xrefs — dual call sites on free paths in bind/unbind) |
| **Callees** | indirect only via `DAT_00b05060` vtable `+0x28` (slab free) and `+0x14` (tagged free `0x12`) |
| **Named role (INFERRED)** | `LocalWorklist_Release_Inferred` |
| **Integrity** | Live decompile 2026-07-29 ≡ raw 2026-07-23 body (CF text match); machine confirms `AND size,0x7fffffff` before `*8` on tag free |

### Live decompile (authoritative 2026-07-29)

```c
void __fastcall FUN_0055f4c0(undefined4 *param_1)

{
  int *piVar1;
  uint uVar2;
  
  piVar1 = DAT_00b05060;
  uVar2 = param_1[4] * 8 + 0xfU & 0xfffffff0;
  if ((uVar2 == 0) || ((param_1[3] != DAT_00b05060[5] && (DAT_00b05060[5] != 0)))) {
    DAT_00b05060[2] = DAT_00b05060[2] - uVar2;
    piVar1[3] = piVar1[3] + uVar2;
  }
  else {
    (**(code **)(*DAT_00b05060 + 0x28))(param_1[3],uVar2);
  }
  if (-1 < (int)param_1[2]) {
    (**(code **)(*DAT_00b05060 + 0x14))(*param_1,param_1[2] * 8,0x12);
  }
  return;
}
```

### Machine note (read_memory / function bytes vs decompile)

Tag-free path at `0x0055f4f6`–`0x0055f516`:

1. `MOV EAX,[ESI+8]` → `TEST EAX,EAX` / `JS` skip if negative.
2. `AND EAX,0x7fffffff` then three `ADD EAX,EAX` → **`(param_1[2] & 0x7fffffff) * 8`**.
3. `PUSH 0x12; PUSH size; PUSH [ESI]; CALL [vtbl+0x14]`.

Decompiler elides the `0x7fffffff` mask in the `* 8` expression; clean reconstruction **must** keep the mask for exactness.

### Descriptor layout (ECX)

| Offset | Use |
|---|---|
| `+0x00` (`param_1[0]`) | pointer passed to tagged free |
| `+0x08` (`param_1[2]`) | signed capacity/count; high bit flag; skip free if `< 0` |
| `+0x0c` (`param_1[3]`) | slab pointer / cookie vs `DAT_00b05060[5]` |
| `+0x10` (`param_1[4]`) | unit count → aligned bytes `((n*8+0xf)&~0xf)` |

### Call-site role

Always invoked as teardown after bind/unbind worklist use (`WorldObj_BindLinks_Inferred` / `WorldObj_UnbindLinks_Inferred` and sibling manager helpers). ECX = address of stack worklist descriptor built on those frames.
