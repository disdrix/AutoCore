# Annotated low-level: ActionManager_NotifyListenersAndCompact_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0062a410` |
| VA | `0x0062a410`–`0x0062a444` exclusive (**52** B) |
| Ghidra | `FUN_0062a410` |
| Canonical | `ActionManager_NotifyListenersAndCompact_Inferred` (**Inferred**) |
| System | vehicle / world action lifecycle (action-manager listeners) |
| Date | 2026-07-29 (W24-P live seal) |
| Tools | `decompile_function` + `read_memory` (no `disassemble_bytes`) |

## Machine-level notes

- **cdecl** `void(manager*, action*)` — two stack args; parent `FUN_0055df40` cleans with `ADD ESP, 8`.
- Listener table is a two-dword vector header at **`manager+0x3c`**: `base*` @ +0x3c, `count` @ +0x40.
- Reverse index walk `count-1 … 0`; skip null slots; non-null: `(*listener->vtbl)[3]` i.e. offset **+0xC**, with `ECX=listener`, stack=`action`.
- After walk: **tail-call** `FUN_005fff20` with **EAX = &vector** (`manager+0x3c`) to compact any nulls listeners may have left (or pre-existing holes).
- Sibling pattern: `FUN_0062a490` walks a different table (`+0x48/+0x4c`) with vtbl **+8**, same compact tail.
- No product plate string in body.

## Pseudocode (annotated)

```c
// cdecl; caller cleans 8
void ActionManager_NotifyListenersAndCompact_Inferred(void *manager, void *action)
{
  int count = *(int *)((char *)manager + 0x40);
  void **base = *(void ***)((char *)manager + 0x3c);
  for (int i = count - 1; i >= 0; --i) {
    void *listener = base[i];
    if (listener != 0) {
      void **vtbl = *(void ***)listener;
      // thiscall: ECX=listener; stack=action; vfunc @ vtbl+0xC
      ((void (__thiscall *)(void *, void *))vtbl[3])(listener, action);
    }
  }
  // residual vs decomp: EAX = manager+0x3c; tail jmp FUN_005fff20 (compact nulls)
  // FUN_005fff20 ends in plain RET (cdecl); original caller still does ADD ESP,8
}
```

## Offsets sealed

| Object | Off | Role |
|---|---:|---|
| manager | +0x3c | listener vector `base*` |
| manager | +0x40 | listener vector `count` |
| listener | vtbl+0xC | notify/unregister virtual (index 3) |
| action | (arg) | payload to each listener vfunc |

## Open / nested (not owned)

- Dual of `FUN_005fff20` compact helper.
- Product English for manager / listener interface.
- Exact semantic of vtbl+0xC (OnRemove vs generic notify).
