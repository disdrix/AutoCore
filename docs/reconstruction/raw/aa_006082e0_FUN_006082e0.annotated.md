# Annotated low-level: Outpost_CaptureOutpost (was FUN_006082e0)

| Field | Value |
|---|---|
| Stable ID | `aa_006082e0` |
| VA | `0x006082e0` |
| Body | `0x006082e0`–`0x00608614` |
| Canonical name | `Outpost_CaptureOutpost` |
| System | skills-abilities / outpost |
| Date | 2026-07-23 scaffold; annotated 2026-07-29 W20-D |

## Machine-level notes

- Source: raw capture + live re-verify W20-D.
- Product string seals role: `"Invalid faction in CaptureOutpost = %d"` @ `0x009dec4c`.
- Prefer body + call-site context for ABI; decompiler shows correct thiscall + one stack arg.
- Many callees dual-sealed elsewhere (CastSkillsForFaction, UpdateBeaconShareRatio, GetRootRaceId); others remain FUN_* (not owned this pass).

## ABI

| Slot | Role |
|---|---|
| **ECX** | outpost `this` (`mov esi,ecx`) |
| **[ebp+8] / stack** | capturer-related object* (`param_2`); may be null |
| Epilogue | **`ret 4`** |
| Return | void |

## Key offsets (outpost this)

| Off | Role |
|---:|---|
| `+0x218` | float capture radius (squared for nearby credit) |
| `+0x220` | char schedule-bank select (0 → DAT_00d029f4/f8; else e4/e8) |
| `+0x234` | int current faction / race id |
| `+0x238` | int schedule entry count (written on success) |
| `+0x23c` | int cleared to 0 on success |
| `+0x248` | timer/blob*; required non-null for capture; `+8` float, `+0x14` tick |

## Control flow (high level)

```
capturer = null; newFaction = 3 (TRIBE|BIOMEK)
if param_2:
  capturer = param_2->vtbl[0x210/4](0)
  if capturer: newFaction = Object_GetRootRaceId(capturerBase)

old = this.faction(+0x234)
if 0 <= old < 3: clearOldSide(old)          // FUN_004cd5f0
if capturer and raceByte < 3: FUN_004cd5d0(raceByte)

if newFaction == old OR this.timerBlob(+0x248) == null:
  return

if newFaction < 0 OR newFaction > 3:
  log("Invalid faction in CaptureOutpost = %d", newFaction)
  return

// valid capture attempt
FUN_005743e0(0,0,0) on this+0x224 region

if capturer:
  creditNearbySameRaceInRadius()            // FUN_00418d70 on capturer + neighbors

if world.flag(+0x7e) == 0:
  return                                    // no faction transfer

// transfer (old → new)
FUN_00606820(old); FUN_00606890(old); FUN_00606ff0(old)
FUN_00606900(new); FUN_00606a00(new)
Outpost_CastSkillsForFaction(new)
FUN_00606a70(new, capturerBaseOrNull)
this.faction = new
Outpost_UpdateBeaconShareRatio()
this.scheduleCount = vectorCount(selected bank)
this.scheduleCursor = 0
timer.float@+8 = FUN_00607550()
timer.tick@+0x14 = GetTickCount()
FUN_006081b0(capturer)
return
```

## Nearby-credit loop (when capturer non-null)

1. Load capturer TFID-ish pair at `+0x164/+0x168` → `FUN_00418d70`.
2. `radiusSq = (this+0x218)^2`.
3. Walk capturer world object list `@ world+0xe75c..+0xe760` (pointer vector, stride 4).
4. Skip self; keep same root race as `newFaction`.
5. Distance via vtbl `+0x1a0` position vs outpost world pos `+0x84/+0x88/+0x8c`.
6. If distSq < radiusSq: credit that object's TFID pair via `FUN_00418d70`.

## Named callees (already dualed / known)

| VA | Name | Role here |
|---|---|---|
| `0x00512440` | `Object_GetRootRaceId` | capturer + neighbor race |
| `0x00606d70` | `Outpost_CastSkillsForFaction` | post-capture cast bank |
| `0x006078e0` | `Outpost_UpdateBeaconShareRatio` | after faction store |
| `0x007a4480` | log | invalid faction |
| EXTERNAL | `GetTickCount` | timer stamp |

## Unresolved callees (not owned)

`FUN_004cd5f0`, `FUN_004cd5d0`, `FUN_005743e0`, `FUN_00418d70`, `FUN_00606820`, `FUN_00606890`, `FUN_00606ff0`, `FUN_00606900`, `FUN_00606a00`, `FUN_00606a70`, `FUN_00607550`, `FUN_006081b0`.

## Open questions

1. Product English / exact type for capturer `param_2` and vtbl `+0x210`.
2. Semantics of `FUN_004cd5f0` / `004cd5d0` (faction clear helpers).
3. Meaning of `FUN_00418d70` credit payload (mission? score? contribute?).
4. What `+0x220` schedule bank switch selects (PVE vs PVP?).
5. Runtime capture of full outpost flip sequence.
