# Annotated low-level: FUN_005e13b0 → StdTree_InsertAndRebalance_Val12

| Field | Value |
|---|---|
| Stable ID | `aa_005e13b0` |
| VA | `0x005e13b0` |
| System | std map/set red-black always-insert (Val12) |
| Date | 2026-07-23 scaffold; **W37-K dual 2026-08-04** |

## Machine-level notes

- Source: raw capture for `aa_005e13b0` + live `read_memory` / decompile (W37-K).
- **Always-insert** — no key compare in this unit; parent `FUN_005e18d0` owns uniqueness.
- Val12: color `@+0x18`, isnil `@+0x19` (used on rotate child reattach).
- Max size `0x15555553` (Val12), **not** Val8 `0x1ffffffd`.
- Buynode `FUN_005ae220` (W24-I); rotates `00573170` / `00418c10` (W26-Q).
- Epilogue: `ADD ESP,0x50; RET 0x10` (4 stack args).

## Sealed algorithm

```
// ECX = MapShell*; stack: outIt*, addLeft, where, value*  (Val12); RET 0x10
if size > 0x15555553: throw "map/set<T> too long"
n = Buynode_Val12(head, where, head, value, color=0)
size++
link: empty-head | right-of-where | left-of-where  (+ leftmost/rightmost)
while parent color red (color@+0x18==0):
  recolor uncle red-black case OR rotate (L/R helpers; one Lrotate inlined)
paint root black; *outIt = n; return
```

## Pseudocode (annotated)

```c
void __thiscall StdTree_InsertAndRebalance_Val12(
    int map,                 // ECX; head@+4 size@+8
    uint32_t **outIt,        // *outIt = new node
    char addLeft,            // 1 left, 0 right
    uint32_t *where,         // parent or head
    const void *value)       // Val12 → node+0x0C
{
  if (*(uint32_t *)(map + 8) > 0x15555553u)
    _CxxThrowException(... "map/set<T> too long" ..., &DAT_00acc388);

  int *n = (int *)FUN_005ae220(*(void **)(map + 4), where,
                               *(void **)(map + 4), value, 0);
  *(int *)(map + 8) += 1;
  // link first / right / left ...
  // RB fixup on color @ parent+0x18 ...
  *(uint8_t *)(*(int *)(*(int *)(map + 4) + 4) + 0x18) = 1; // root black
  *outIt = (uint32_t *)n;
}
```

## Call graph

| Direction | Targets |
|---|---|
| Callees | `FUN_005ae220`, `FUN_00573170`, `FUN_00418c10`, basic_string/exception/`_CxxThrowException` |
| Callers | `FUN_005e18d0` only (2 call sites) |

## Related

- W30-L left full dual of this always-insert open — **closed here**.
- Peer clones: `00539210` (W27-Q), `0051c1c0` (W25-G), `005ae4e0` (W27-T) — share algorithm, **do not merge VAs**.
