# Annotated low-level: StdVector_ThrowTooLong (`FUN_004540b0`)

| Field | Value |
|---|---|
| Stable ID | `aa_004540b0` |
| VA | `0x004540b0` |
| Body | `0x004540b0`–`0x004540fa` (75 bytes) |
| Canonical name | `StdVector_ThrowTooLong` |
| Ghidra | `FUN_004540b0` |
| System | containers / MSVC STL vector |
| Date | 2026-07-29 (W19-B dual seal) |

---

## Machine-level notes

- **SEH frame** installed (`push -1`, `LAB_009bc919`, `fs:[0]`) before locals — required because the path throws.
- Message string is a **static C string** at `0x00a15830` = `"vector<T> too long"` (sealed via `read_memory`).
- `basic_string` is constructed into stack (`lea ecx,[esp+4]` + IAT call `[0x009c62f8]`).
- `FUN_00401a40(string*)` is **thiscall** on ECX = exception object (`lea ecx,[esp+0x20]`), stack arg = string ref — builds a typed exception (vtbl `PTR_FUN_009c7634`).
- `_CxxThrowException(exception_object, &DAT_00acc388)` — **noreturn**. `DAT_00acc388` is the MSVC `ThrowInfo` for this exception type.
- **No formals.** Callers treat this as pure side-effect throw on overflow (size > max).
- Sibling throw sites with the **same string** exist at other VAs (e.g. `FUN_00418130`, `FUN_004932e0`) — this unit is the **widely shared** helper used by many dword-vector grow paths.

---

## Pseudocode (annotated)

```c
/* StdVector_ThrowTooLong — MSVC vector::_Xlen()-class helper (noreturn).
   Builds length_error-class exception with plate "vector<T> too long" and throws. */

void __cdecl StdVector_ThrowTooLong(void)  /* noreturn */
{
  /* SEH + stack: basic_string msg; exception_object ex; */
  /* basic_string_ctor(&msg, "vector<T> too long" @ 0x00a15830); */
  /* FUN_00401a40(&ex, &msg);  // exception from string */
  /* _CxxThrowException(&ex, &DAT_00acc388); */
}
```

Control flow is **linear single block** — always throws; no success return.

---

## Open questions

1. Product/PDB English symbol (likely `std::vector<T>::_Xlen` or similar) — open; role sealed from plate string + CF.
2. Exact C++ exception type hierarchy behind `FUN_00401a40` / `ThrowInfo` (length_error vs logic_error) — out of owned scope unless a dual owns `00401a40`.
3. Runtime / bit-exact — open.
