# Annotated low-level: FUN_00410420 / StdVector_DwordResize_ViaInsertN_004073a0

| Field | Value |
|---|---|
| Stable ID | `aa_00410420` |
| VA | `0x00410420` |
| System | STL / container utility |
| Date | 2026-08-04 (W37-T dual; supersedes 2026-07-23 scaffold notes) |

## Machine-level notes

- Source: raw capture + live `batch_decompile` + `read_memory` (W37-T).
- Prefer assembly when decompiler conflicts — CF here matches.
- Body **105 B** (`0x00410420`–`0x00410488`); pad `CC` then `FUN_00410490`.
- ABI: `__thiscall`; stack `(newCount, fillValue)`; **`ret 8`**.
- Layout: begin `@+4`, end `@+8`; size `(end-begin)>>2`.
- Grow: `FUN_004073a0(end, newCount-size, &fill)`.
- Shrink: `FUN_00410490(&out, begin+newCount*4, end)`.
- Twin: `FUN_004367f0` same CF, grow via `FUN_004507a0`.

## Pseudocode (annotated)

```c
// void __thiscall StdVector_DwordResize_ViaInsertN_004073a0(
//     vec* this, uint newCount, uint fillValue);  // ret 8

void __thiscall FUN_00410420(int param_1, uint param_2 /* newCount */)
{
  uint size;
  int begin = *(int *)(param_1 + 4);

  if (begin == 0) size = 0;
  else size = (*(int *)(param_1 + 8) - begin) >> 2;

  if (size < param_2) {
    // grow: insert (param_2 - size) fill dwords at end
    uint cur = (begin == 0) ? 0 : ((*(int *)(param_1 + 8) - begin) >> 2);
    FUN_004073a0(*(void **)(param_1 + 8), param_2 - cur, &stack0x00000008 /* &fill */);
    return;
  }
  if ((begin != 0) && (param_2 < (uint)((*(int *)(param_1 + 8) - begin) >> 2))) {
    // shrink: erase [begin+newCount, end)
    FUN_00410490(&param_2, begin + param_2 * 4, *(int *)(param_1 + 8));
  }
  return;
}
```

## Open questions

- Product STL demangle (`vector::resize`).
- Why two resize clones (`004073a0` vs `004507a0` grow).
