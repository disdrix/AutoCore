# Annotated low-level: Object_IsWorldPickupType_Inferred (`FUN_005130e0`)

| Field | Value |
|---|---|
| Stable ID | `aa_005130e0` |
| VA | `0x005130e0` |
| Ghidra name | `FUN_005130e0` |
| Inferred name | `Object_IsWorldPickupType_Inferred` |
| System | skills-abilities / client-interact / object-type filter |
| Date | 2026-08-04 (WQ9R-C dual seal) |

## Machine-level notes

- **thiscall**: ECX = cloned object; stack `char requireLoose`; `RET 4`; AL bool.
- Gate: if `requireLoose!=0` and `this+0xac != 0` → **0** (object not “loose” for soft filter / pickup).
- Else switch on **clone type** at `*(uint*)(*(this+0xa8)+0x38)` (CloneBase `+0x38`).
- Type **1** (Object): high bit of clone blob `+0x3f2` (`>> 7`).
- Fixed accept set: quest/item/gadget/powerplant/weapon/wheelset/commodity/armor/tinkering/accessory/money (see raw append table).
- Type **0xe** Vehicle: `__RTDynamicCast` to `CVOGVehicle`; accept only if `vehicle+0x2ac != 0` (special/abandoned flag writers elsewhere).
- Creatures/characters and unlisted types → **0**.
- Callers pass `requireLoose=1` for pickup / gather soft filter; `Skill_FilterTargetForResolveList` can pass `0` (skip +0xac).
- Live decompile 2026-08-04 ≡ raw scaffold. **No** `disassemble_bytes`.

## Pseudocode (annotated)

```c
// Object_IsWorldPickupType_Inferred @ 0x005130e0
// Returns 1 if object type is soft-filter / world-pickup eligible.
byte __thiscall Object_IsWorldPickupType_Inferred(int this_obj, char requireLoose)
{
  int vehicle;

  // requireLoose: when set, reject objects with non-null this+0xac
  if ((requireLoose == 0) || (*(int *)(this_obj + 0xac) == 0)) {
    switch (*(uint *)(*(int *)(this_obj + 0xa8) + 0x38)) {
    case 1: // Object — clone flag bit7 @ blob+0x3f2
      return *(byte *)(*(int *)(*(int *)(this_obj + 0xa8) + 0x3c) + 0x3f2) >> 7;
    case 4:  // QuestObject
    case 6:  // Item
    case 8:  // Gadget
    case 10: // PowerPlant
    case 0xc:  // Weapon
    case 0x10: // WheelSet
    case 0x1a: // Commodity
    case 0x1c: // Armor
    case 0x32: // TinkeringKit
    case 0x34: // Accessory
    case 0x42: // Money
      return 1;
    case 0xe: // Vehicle — special flag only
      vehicle = __RTDynamicCast(this_obj, 0,
                                &CVOGClonedObjectBase::RTTI_Type_Descriptor,
                                &CVOGVehicle::RTTI_Type_Descriptor, 0);
      if ((vehicle != 0) && (*(char *)(vehicle + 0x2ac) != 0)) {
        return 1;
      }
    }
  }
  return 0;
}
```

## Open questions

- Product C++ name / PDB (none recovered) → keep `_Inferred`.
- Exact English of `this+0xac` (non-null blocks requireLoose path) — Probable “attached/secondary hold”.
- Exact product English of vehicle `+0x2ac` (shared special/no-cargo flag) — Probable; sealed as non-zero gate only.
- Runtime / bit-exact open.
