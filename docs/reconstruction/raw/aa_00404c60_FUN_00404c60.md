# Raw capture: FUN_00404c60

| Field | Value |
|---|---|
| **Stable ID** | `aa_00404c60` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00404c60` |
| **Canonical name** | `FUN_00404c60` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00404c60(int param_1,undefined4 *param_2)

{
  *(undefined4 *)(param_1 + 0x80) = *param_2;
  *(undefined4 *)(param_1 + 0x84) = param_2[1];
  *(undefined4 *)(param_1 + 0x88) = param_2[2];
  *(undefined4 *)(param_1 + 0x8c) = param_2[3];
  return;
}
```

---

## MEGA-053 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual MEGA-053 |
| **Tools** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` + `get_function_xrefs` + `get_assembly_context` + caller `batch_decompile`. **No** `disassemble_bytes`. No Launcher. |
| **Image** | `autoassault.exe` base `0x400000` |
| **Ghidra name** | `FUN_00404c60` |
| **Promoted name** | `Object_Store16At80_Inferred` |
| **Body** | `0x00404c60`–`0x00404c88` inclusive (**41 B** / `0x29`); pad `CC` @ `0x00404c89`.. before next symbol cluster |
| **Classification** | leaf |
| **Live decompile ≡ raw CF** | **Yes** (4 dword stores; return) |

### Live decompile (reconfirm)

```c
void __thiscall FUN_00404c60(int param_1, undefined4 *param_2)
{
  *(undefined4 *)(param_1 + 0x80) = *param_2;
  *(undefined4 *)(param_1 + 0x84) = param_2[1];
  *(undefined4 *)(param_1 + 0x88) = param_2[2];
  *(undefined4 *)(param_1 + 0x8c) = param_2[3];
  return;
}
```

### Disassembly seal (`disassemble_function`)

```text
00404c60  MOV EAX, dword ptr [ESP + 0x4]   ; src
00404c64  MOV EDX, dword ptr [EAX]
00404c66  MOV dword ptr [ECX + 0x80], EDX
00404c6c  MOV EDX, dword ptr [EAX + 0x4]
00404c6f  MOV dword ptr [ECX + 0x84], EDX
00404c75  MOV EDX, dword ptr [EAX + 0x8]
00404c78  MOV dword ptr [ECX + 0x88], EDX
00404c7e  MOV EAX, dword ptr [EAX + 0xc]
00404c81  MOV dword ptr [ECX + 0x8c], EAX
00404c87  RET 0x4
```

### Body hex (`read_memory` 48 B @ `0x00404c60`)

```text
8b 44 24 04  8b 10  89 91 80 00 00 00
8b 50 04     89 91 84 00 00 00
8b 50 08     89 91 88 00 00 00
8b 40 0c     89 81 8c 00 00 00
c2 04 00
cc cc cc cc cc cc cc
```

Sealed: **`RET 4`** (`C2 04 00`); dest base **ECX**; src **`[ESP+4]`**; stores at **+0x80 / +0x84 / +0x88 / +0x8c**.

### ABI (sealed)

| Slot | Role | Conf |
|---|---|---|
| ECX | destination object (`this`) | **High** |
| stack `[ESP+4]` | `const uint32_t[4]*` / 16-byte src | **High** |
| cleanup | **`RET 4`** | **High** |
| return | void (EAX not semantic) | **High** |
| callees | **none** (leaf) | **High** |

### Callers / xrefs (4 UNCONDITIONAL_CALL)

| From | Function | Notes |
|---|---|---|
| `0x0058a265` | `FUN_0058a1b0` | skill-creature path; `MOV ECX,EDI` + `PUSH src`; often followed by twin `FUN_00404c30` |
| `0x005c77f1` | `FUN_005c76f0` | plate `CLoadNode::_initPhysics`; multi-inh `LEA ECX,[ECX+ESI*1+4]`; type `==0xe` branch; twin `00404c30` |
| `0x0062061f` | `FUN_00620480` | parent has string `"Skill %d had invalid template %d."`; copies stack blob then twin `00404c30` |
| `0x0091dc79` | `FUN_0091da70` | client net / local-player path; multi-inh this; float pack then twin-family path |

**xref_count = 4**. analyze callers list matches.

### Twin (not OWN — evidence only)

`FUN_00404c30` @ `0x00404c30`: identical shape, dest offsets **+0x90..+0x9c**, same **`RET 4`**. Contiguous 32-byte block `this+0x80..0x9f` when both called as a pair (common pattern).

### Hint consumption

Partition hint *Skill invalid creature/template string* → **parent consumers** (`FUN_00620480` / `FUN_0058a1b0` family), **not** body strings. Body is a generic 16-byte field store leaf.

### Naming

Promote **`Object_Store16At80_Inferred`**. Product field English (pos vs quat vs generic blob) **open** → `_Inferred`. Retire `Named_CalleeOf_*` scaffolds.

**Terminal:** false (no runtime Confirmed).
