# Raw capture: FUN_00409ae0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409ae0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409ae0` |
| **Canonical name** | `FUN_00409ae0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00409ae0(undefined4 param_1,undefined4 param_2,undefined4 param_3)

{
  FUN_0040a520(param_3,param_2,param_1,param_3);
  return;
}
```

---

## WQ9K-A re-verify (2026-08-05) — append only

| Field | Value |
|---|---|
| **Agent** | WQ9K-A OWN-ONLY |
| **Tools** | `batch_decompile` / `decompile_function` + `analyze_function_complete` + `read_memory` + `disassemble_function` + callers/xrefs + `get_assembly_context` |
| **No** | `disassemble_bytes`; Launcher; parent ledgers |
| **Live decompile** | ≡ scaffold CF (thin call to `FUN_0040a520`) — **ABI incomplete** (missing 2nd stack arg / RET 8) |
| **Named** | `StdVector_UninitCopyTrampoline_Elem0x28_Inferred` |

### Body / ABI (bytes authority)

- **Range:** `0x00409ae0`–`0x00409afa` inclusive last RET byte (**27 B** / `0x1b`); pad `CC` to next helper.
- **ABI:** **ECX** = host/vector this (forwarded but **unused** by target body); **EDX** = `dest`; stack `src_begin`, `src_end`; **EAX** = advanced dest from callee; **`RET 8`** (`C2 08 00`).
- **Full body hex:**
```
8B 44 24 04 50 8B 44 24 08 51 8B 4C 24 10 52 50
E8 2B 0A 00 00 83 C4 10 C2 08 00
```
- **`disassemble_function` listing:**
```
00409ae0  MOV EAX,[ESP+4]      ; stack src_begin
00409ae4  PUSH EAX
00409ae5  MOV EAX,[ESP+8]      ; re-read src_begin (post-push)
00409ae9  PUSH ECX             ; host this (unused by 0040a520)
00409aea  MOV ECX,[ESP+10]     ; stack src_end → ECX for target
00409aee  PUSH EDX             ; dest
00409aef  PUSH EAX             ; src_begin
00409af0  CALL FUN_0040a520    ; ECX=src_end; stack begin,dest,(extra×2); ADD ESP,0x10 in caller
00409af5  ADD ESP,0x10
00409af8  RET 8
```

### Semantics

Thin **ABI trampoline** into dualed `StdVector_UninitializedCopy_Elem0x28_Inferred` (`0x0040a520`):
- Reorders InsertN-style registers into free-helper layout (`ECX=end`, stack begin+dest).
- Pushes **4** dwords then `ADD ESP,0x10` — target reads only begin+dest (same pattern as direct InsertN sites).
- No element loop of its own; identity is glue for reallocate prefix/suffix copy.

### Callers / xrefs (2)

| Site | Parent | Setup | Role |
|---|---|---|---|
| `0x00408171` | `FUN_00408050` InsertN grow | `PUSH where; PUSH begin; EDX=newbuf; ECX=this` | prefix uninit_copy `[begin,where) → new` |
| `0x00408198` | `FUN_00408050` InsertN grow | `PUSH end; PUSH where; EDX=after_ufill; ECX=this` | suffix uninit_copy `[where,end) → dest` |

### Callees

- `FUN_0040a520` only (`StdVector_UninitializedCopy_Elem0x28_Inferred`, dualed WQ9J-A).

### Gaps

- Product / MSVC demangle for 0x28 element type (shared family residual).
- Full dual of parent InsertN `00408050` (role sealed; not this OWN).
- Runtime / bit-exact / differential.
