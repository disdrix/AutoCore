# Annotated low-level: EmptyRet (`aa_0056f570`)

| Field | Value |
|---|---|
| Stable ID | `aa_0056f570` |
| VA | `0x0056f570` |
| Body | `0x0056f570` only (`c3`) |
| Canonical name | `EmptyRet` |
| System | shared nop leaf |
| Date | 2026-07-29 (W17-P dual seal; prior scaffold 2026-07-23) |

## Machine-level notes

- **Body:** single-byte **`RET`** (`c3`). No prologue, no stack args, no memory R/W, no callees.
- **Epilogue:** bare `ret` (not `ret N`) — callee does not pop stack arguments. Callers that push args (cdecl debug-print style, or EH frames) clean their own stack.
- **Padding:** following bytes are `cc` (int3) alignment, not executable body.
- **Do not invent:** stores, gates, format parsing, or product-domain work. This unit is intentionally empty.
- Prefer this shared leaf over inventing per-class empty methods when vtbl slots point here.

## Byte seal

```
0056f570  c3                 ret
0056f571  cc…                int3 pad (out of body)
```

## Pseudocode (annotated)

```c
// Shared empty leaf — multi-role nop
// Roles observed via xrefs (not body logic):
//   1) default empty virtual method (many vtables; e.g. CVOGHBBase slot 7)
//   2) MSVC EH Unwind / vector dtor for trivial types
//   3) stripped / disabled debug-print sink (callers pass format strings; body ignores)
void EmptyRet(void)
{
  return;
}
```

## Layout / ABI

| Item | Value | Conf |
|---|---|---|
| Stack args required by body | **none** | High |
| Uses ECX/`this` | **no** (ignored if present) | High |
| Return value | undefined / caller-irrelevant | High |
| Side effects | **none** | High |

## Cross-unit context (not owned)

| Context | Note |
|---|---|
| `CVOGHBPlayerTargetingLink` derived slot 7 | Overrides this nop with clear-selected-target (`0x0051b850`) |
| Base HB vtbl `0x009cdfb0` slot 7 | Points at this EmptyRet |
| Mission dialog class slot near `+0x448` | Empty stub (peer duals) |
| `VehicleEntity_DrivePathNoOp` `0x004f3700` | **Different** single-byte ret (domain-local); not this VA |

## Open questions

1. Original PDB / CRT symbol (likely a compiler-shared empty).
2. Exhaustive DATA-slot census (hundreds of `70 f5 56 00` hits — not required for CF seal).
3. Whether any call site depended on a non-empty body in a different build (stripped-hook hypothesis only).
