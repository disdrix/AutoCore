# Raw capture: FUN_005179e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_005179e0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005179e0` |
| **Canonical name** | `FUN_005179e0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_005179e0(int param_1)

{
  void *pvVar1;
  char cVar2;
  undefined4 *puVar3;
  
  FUN_00516930(0);
  if (*(int *)(param_1 + 0x130) != 0) {
    FUN_00517250(*(int *)(param_1 + 0x130));
  }
  puVar3 = *(undefined4 **)(param_1 + 0x124);
  *(undefined4 *)(param_1 + 0x130) = 0;
  if (puVar3 != *(undefined4 **)(param_1 + 0x128)) {
    do {
      pvVar1 = (void *)*puVar3;
      if ((*(char *)((int)pvVar1 + 9) == '\0') && (cVar2 = FUN_004ba0a0(), cVar2 != '\0')) {
        FUN_004b99c0();
                    /* WARNING: Subroutine does not return */
        operator_delete(pvVar1);
      }
      puVar3 = puVar3 + 1;
    } while (puVar3 != *(undefined4 **)(param_1 + 0x128));
  }
  if (*(void **)(param_1 + 0x124) == (void *)0x0) {
    *(undefined4 *)(param_1 + 0x124) = 0;
    *(undefined4 *)(param_1 + 0x128) = 0;
    *(undefined4 *)(param_1 + 300) = 0;
    return;
  }
                    /* WARNING: Subroutine does not return */
  operator_delete(*(void **)(param_1 + 0x124));
}
```

---

## Re-verify append — 2026-07-29 OWN-ONLY dual W28-H

| Field | Value |
|---|---|
| **Tool** | `decompile_function` + `analyze_function_complete` + `get_function_by_address` + `read_memory` + callers/xrefs |
| **Excluded** | `disassemble_bytes`; Launcher; parent ledger |
| **Live decompile** | ≡ 2026-07-23 raw outer CF (same FUN_*/offset spine) |
| **Body** | `0x005179e0`–`0x00517a6a` (**139 B / `0x8B`**); pad `CC` after `RET` |
| **ABI (bytes)** | **thiscall** `mov esi,ecx`; plain **`RET` (`C3`)**; **no** stack formals cleaned |
| **Callee ABI seal** | `FUN_00516930` ends **`ret 4`** — stack formal **0** is real (`push ebx` after `xor ebx,ebx`); ECX = this |
| **Decompiler gaps sealed by `read_memory`** | (1) `FUN_004ba0a0` / `FUN_004b99c0` ECX = `fx*` (`mov ecx,ebp`); (2) after optional `operator_delete(begin)` **always** write `0` to `+0x124/+0x128/+0x12c` then `pop esi; pop ebx; ret` — false noreturn on delete collapsed the post-free zeroing |
| **CODE callers** | `FUN_00518ec0` @ `0x00518f8b` (object teardown path); `FUN_00584330` @ `0x005843ab` (**tail jmp**, vbase-adjusted this) |
| **DATA xrefs** | vtable installs `0x009ce348`, `0x009d34c0`, `0x009d4248`, `0x009dbcf8` (virtual method) |
| **Named role** | `Object_TeardownOwnedFxLists_Inferred` |

### Byte-backed control flow (preferred over raw decompile for epilogue)

1. `FUN_00516930(this, /*stack*/0)` — secondary ptr-vector at host`+0x138..+0x140` (nested; `ret 4`).
2. If `*(this+0x130) != 0` → `FUN_00517250(this, *(this+0x130))` (remove one owned fx from primary vector).
3. `*(this+0x130) = 0`.
4. Walk primary vector `begin=*(this+0x124)` … `end=*(this+0x128)` (stride **4**):
   - `fx = *it`
   - if `*(char*)(fx+9) == 0` **and** `NDSpecialFX_StopFinalize_Inferred(fx) != 0`:
     - `NDSpecialFX_TeardownCore_Inferred(fx)` (`FUN_004b99c0`)
     - `operator_delete(fx)`
5. If `*(this+0x124) != 0` → `operator_delete(*(this+0x124))`.
6. **Always:** `*(this+0x124)=0; *(this+0x128)=0; *(this+0x12c)=0; return`.
