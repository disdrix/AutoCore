# Annotated low-level: FUN_0074e0d0 → LightMgr_SetActiveSlot3c_Refcount_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0074e0d0` |
| VA | `0x0074e0d0`–`0x0074e107` exclusive (**55 B**) |
| System | Palantir light manager / active refcounted slot |
| Date | 2026-07-23 scaffold; **W37-AE dual seal 2026-08-04** |

## Machine-level notes

- **ABI:** `__thiscall` ECX=`host*`; stack `new_obj*`; **`RET 4`**.
- **Slot:** `host+0x3c` stores refcounted object pointer.
- **Protocol:** AddRef new (`+4` refcount; vtbl[+4] on 0→1) **before** Release old (vtbl[+8] on →0).
- **Callers:** rebuild/tear default lights (`0048f370` / `0048eb10`); Directional attach (`00792600`).
- Prefer bytes when decompiler omits ECX/mgr loads at call sites.
- **No** `disassemble_bytes` used in W37-AE (decompile + `read_memory` only).

## Pseudocode (annotated)

```c
// LightMgr_SetActiveSlot3c_Refcount_Inferred
void __thiscall FUN_0074e0d0(int host /*ECX*/, int *neu /*stack*/)
{
  int *old;

  if (neu != 0) {
    neu[1] = neu[1] + 1;          // refcount at +4
    if (neu[1] == 1) {
      (**(code **)(*neu + 4))();  // vtbl[+4] first acquire
    }
  }
  old = *(int **)(host + 0x3c);
  if (old != 0) {
    old[1] = old[1] - 1;
    if (old[1] == 0) {
      (**(code **)(*old + 8))();  // vtbl[+8] last release
    }
  }
  *(int **)(host + 0x3c) = neu;
}
```

## Body hex (55 B)

`568b74240885f6578bf9741183460401837e040175078b068bceff50048b4f3c85c9740b834104ff75058b11ff520889773c5f5ec20400`

## Open questions

- Product C++ name of light manager host class.
- Exact type of objects at +0x3c (light proxy vs full host; peers pass `light+0x8c`).
