# Raw capture: FUN_006261e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_006261e0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x006261e0` |
| **Canonical name** | `FUN_006261e0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_006261e0(int *param_1,undefined4 param_2,undefined4 param_3)

{
  if ((char)param_2 == '\0') {
    (**(code **)(*param_1 + 0x14))();
  }
  FUN_005083f0(param_2,param_3);
  return;
}
```

---

## Re-verify (2026-07-29 W29-P OWN-ONLY dual)

| Check | Result |
|---|---|
| Live `decompile_function` @ `0x006261e0` | **≡ raw CF** |
| `get_function_by_address` body | `0x006261e0`–`0x00626202` (**35 B** / `0x23`) |
| `read_memory` 64 B | entry `53 8B 5C 24 08…`; epilogue `C2 08 00` (ret 8); pad `CC` then next fn @ `0x00626210` |
| DATA xref | vtbl `0x009e3168` + **0x18** (`0x009e3180`) = this VA |
| Callee | `CVOGHBBase_EndOrDestroy` (`FUN_005083f0`) with **two** stack flags |
| Code callers | **0** (virtual only); sibling factory duals list as HB end override |

**Named (structural):** `CVOGHBActivatePlayer_EndOrDestroy_Inferred` — subclass **EndOrDestroy** at activate-player HB vtbl `+0x18`. When `flag_onEnd==0`, pre-calls own OnEnd (`vtbl+0x14`) so host notify still runs, then base EndOrDestroy (which skips OnEnd on flag 0).
