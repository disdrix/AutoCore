# Annotated low-level: CVOGObjective_SetCompleteCount (`FUN_0059d890`)

| Field | Value |
|---|---|
| Stable ID | `aa_0059d890` |
| VA | `0x0059d890`–`0x0059d89c` (**13** B; exclusive end `0x0059d89d`) |
| System | missions-progression |
| Date | 2026-07-29 (W21-J dual seal) |
| Twin | getter `aa_0059d880` `CVOGObjective_GetCompleteCount` |

---

## Machine-level notes

- Body is pure field store: load stack arg → `[ecx+0x164]` → `ret 4`.
- **No** branches, callees, or secondary side effects.
- Field product English **CompleteCount** sealed by getter dual + XML dump `FUN_005468c0` (`<CompleteCount>%i</CompleteCount>`).
- Sole caller `FUN_00547050` @ `0x005478a9`: load/parse path pushes value and `thiscall`s into objective.

## Recovered CF (annotated)

```c
// thiscall: ECX = objective*; stack: uint32 completeCount; ret 4
void CVOGObjective_SetCompleteCount(void* objective, uint32_t completeCount)
{
    *(uint32_t*)((char*)objective + 0x164) = completeCount;
}
```

## Sibling pair

| VA | Role | Body |
|---|---|---|
| `0x0059d880` | Get CompleteCount | `mov eax,[ecx+0x164]; ret` |
| `0x0059d890` | Set CompleteCount | `mov eax,[esp+4]; mov [ecx+0x164],eax; ret 4` |

## Open questions

- Product/PDB setter spelling (SetCompleteCount vs assignCompleteCount).
- Full mission-XML key → value path inside `FUN_00547050` (parent scope; not this body).
- Runtime / bit-exact — open.
