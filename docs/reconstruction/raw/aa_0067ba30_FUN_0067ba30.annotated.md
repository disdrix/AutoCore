# Annotated low-level: FUN_0067ba30

| Field | Value |
|---|---|
| Stable ID | `aa_0067ba30` |
| VA | `0x0067ba30` |
| System | network-nazgul-netSocket |
| Date | 2026-07-29 (OWN-ONLY dual seal) |
| Proposed name | `netSocket_GetPeerAddress` / `NetSocket_GetPeerName` (**High** role; product name open) |

## Machine-level notes

- Source: raw capture for `aa_0067ba30` + live `decompile_function` / `read_memory` 2026-07-29.
- Prefer assembly/`read_memory` when decompiler omits `this` at call sites.
- Twin: `FUN_0067b9a0` @ `0x0067b9a0` — **identical CF** with `getsockname` instead of `getpeername`.
- Cluster: nazgul `netSocket.cpp` (string on sibling methods `0067b680` / `0067baf0`).

## Calling convention (sealed)

| Slot | Role | Evidence |
|---|---|---|
| **ECX** | `netSocket* this` / `SOCKET*` object with OS handle at `+0` | Entry `mov esi, ecx`; `getpeername(*this, …)` |
| **stack arg0** | `Address* out` (16-byte sockaddr-compatible) | Loaded into ESI after getpeername; dest of `FUN_0067b0e0`; returned in EAX |
| **Return** | `Address* out` in **EAX** | `mov eax, esi` |
| **ABI** | `__thiscall` + **1 stack arg**; trailer **`ret 4`** | `c2 04 00` |

## Stage map

| Stage | What |
|---|---|
| 1 | SEH install (`LAB_009a962c`), state = −1 |
| 2 | Default-construct stack Address temp via `FUN_0067b070` (ECX=&temp) |
| 3 | SEH state = 1; `namelen = 0x10` |
| 4 | `getpeername(*this, &temp, &namelen)` |
| 5 | `FUN_0067b0e0(out, &temp)` — 4× dword copy into out |
| 6 | SEH state low byte cleared; `FUN_0067b110()` trivial dtor of temp |
| 7 | Unlink SEH; return `out` |

## Pseudocode (annotated)

```c
// __thiscall netSocket_GetPeerAddress(SOCKET *this /* ECX */, Address *out /* stack */)
// Returns out. Body: getpeername → copy 16B Address → trivial temp dtor.
// Twin: FUN_0067b9a0 uses getsockname (local name).

undefined4 __thiscall FUN_0067ba30(SOCKET *this, undefined4 out /* Address* */)
{
  int namelen_and_flags[2];   // [0]=namelen, [1]=SEH-related flag in decomp view
  sockaddr temp;              // 16-byte Address / sockaddr staging
  void *exc_prev;
  undefined1 *seh_handler;
  uint seh_state;

  seh_state = 0xffffffff;
  seh_handler = &LAB_009a962c;
  exc_prev = ExceptionList;
  namelen_and_flags[1] = 0;
  ExceptionList = &exc_prev;

  // Default-construct temp Address (zeros + htons(0) port) — ECX = &temp
  FUN_0067b070();             // __fastcall Address_DefaultCtor(&temp)

  seh_state = 1;
  namelen_and_flags[0] = 0x10;  // sizeof(sockaddr_in) / Address
  getpeername(*this, &temp, namelen_and_flags);

  // Copy temp → *out (thiscall: ECX=out, stack=&temp)
  FUN_0067b0e0(&temp);        // Address_CopyAssign(out, &temp)

  namelen_and_flags[1] = 1;
  seh_state = seh_state & 0xffffff00;  // state → 0 (ready for dtor)
  FUN_0067b110();             // Address_TrivialDtor(&temp) — empty body

  ExceptionList = exc_prev;
  return out;
}
```

## Object / buffer sizes

| Object | Size | Notes |
|---|---:|---|
| `netSocket` used here | ≥ 4 | Only `*this` (SOCKET) read |
| Address / sockaddr temp | **16** | `namelen=0x10`; copy is 4 dwords via `FUN_0067b0e0` |
| `getpeername` namelen in/out | 4 | stack int, init 0x10 |

## Open questions

1. Product/PDB spelling (`getPeerAddress` vs `getRemoteAddress` vs free helper).
2. Whether `out` is TNL `Address` or nazgul-specific layout beyond 16B sockaddr overlay.
3. Call-site ECX binding in Auth objects (`+0x30` vs `+0x34`) — caller duals, not OWN.
4. Return-value consumers (many Auth paths ignore EAX and only use filled buffer).
