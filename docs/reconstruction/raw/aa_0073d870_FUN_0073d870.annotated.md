# Annotated low-level: FUN_0073d870 → EffEffectFactory_OnDeviceReset_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0073d870` |
| VA | `0x0073d870`–`0x0073d8c2` exclusive (**82 B** / `0x52`) |
| System | graphics / Effects / effEffectFactory (via Device Reset caller) |
| Date | 2026-08-04 (W38-V) |
| Name | `EffEffectFactory_OnDeviceReset_Inferred` (**Inferred**; sealed by caller string) |

## Machine-level notes

- Source: raw capture + W38-V re-verify; **bytes win** on ABI (EAX host) and always-`0` return.
- Sole caller `FUN_0075eff0` (`gfxDevice.cpp` Device Reset) loads `EAX = [EBP+0x764]` then `CALL`; fail plate **"Failed to Reset() device on Effect Factory"**.
- Pair: `FUN_0073d8d0` (`aa_0073d8d0`) is the pre-reset / OnLost walk on the **same** host slot.
- Stride **0x14** table: host `+4` begin / `+8` end; per entry optional child at `*entry+0xC` → vcall **`+0x118`**; param range `entry+8`…`entry+0xC` → `FUN_007525f0` (EDI formal).
- Reject scaffold alias `Named_CalleeOf_Named_gfxDevice_0073d870`.

## Pseudocode (annotated)

```c
// EAX = EffEffectFactory* host (register formal)
// returns 0 always (caller still tests HRESULT-style < 0)
uint32_t EffEffectFactory_OnDeviceReset_Inferred(void /* EAX host */)
{
  // entry*: begin = host+4, end = host+8, stride 0x14
  for (entry = *(host+4); entry != *(host+8); entry += 0x14) {
    obj = *entry;                         // entry+0
    if (*(obj + 0xC) != 0) {
      child = *(obj + 0xC);
      // thiscall-ish: PUSH child; CALL child->vtbl[+0x118]
      child->vtbl[+0x118](child);         // OnResetDevice-style
    }
    for (p = entry[+8]; p != entry[+0xC]; p += 4) {
      // EDI = *p
      FUN_007525f0();                     // rebind/restore params (unowned)
    }
  }
  return 0;
}
```

## Open questions

- Product English for factory class / entry / child `+0x118` method (ID3DXEffect-like OnReset residual only).
- Why always-0 return while caller checks failure (defensive template shared with IB/VB/Texture factories).
- Full `FUN_007525f0` dual (W38 sibling family / not owned here).
