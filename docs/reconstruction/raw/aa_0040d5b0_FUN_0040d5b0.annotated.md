# Annotated low-level: FUN_0040d5b0 / StdTree_Clear_PtrVal_Vtbl0C

| Field | Value |
|---|---|
| Stable ID | `aa_0040d5b0` |
| VA | `0x0040d5b0` |
| Structural name | `StdTree_Clear_PtrVal_Vtbl0C` (**Inferred**) |
| System | MSVC `_Tree` clear / Val12 + pointer values |
| Date | 2026-07-29 (W30-P) |

## Machine-level notes

- Source: raw capture + W30-P `read_memory` full body + `analyze_function_complete`.
- **Bytes win** on free path (decompiler false-noreturn collapse).
- Value destroy: thiscall `vtbl[+0x0c]` on `T*` at `node+0x14` — no `operator_delete` on `T*` in this unit.
- Iterator++ is sealed `StdTree_Inc_Val12` (`0x005ae0b0`, W28-N **accept**).
- Nested free helper `FUN_0040d630` = recursive free of non-nil right-chain (OWN residual).
- Also installed as `PTR_FUN_009c7938[2]` (DATA `0x009c7940`); complete dtor `FUN_0040d820` calls it after vtbl install.

## Pseudocode (byte-corrected)

```c
// ECX = tree host (head@+8, size@+0xc). Bare RET. void.
void StdTree_Clear_PtrVal_Vtbl0C(TreeHost* self)
{
  Node* head = self->head;           // *(self+8)
  Node* it = head->left;
  while (it != head) {
    void* obj = *(void**)((char*)it + 0x14);
    (*(void(***)(void))obj)[3]();    // vtbl[+0xc]
    StdTree_Inc_Val12(&it);
  }
  Node* root = head->parent;
  // free all non-nil nodes via right-subtree helper + left walk
  if (!root->isnil) {
    Node* node = root;
    while (!node->isnil) {
      StdTree_FreeSubtree_Right(self_tree_base, node->right); // FUN_0040d630
      Node* left = node->left;
      operator_delete(node);
      node = left;
    }
  }
  // empty sentinel
  head->parent = head;
  self->size = 0;
  head->left = head;
  head->right = head;
}
```

## Open questions

- Product/MSVC demangle for map key/value type and for vtbl[+0xc] method (Release vs complete dtor vs other).
- Full identity of host class behind `PTR_FUN_009c7938` (parent dual).
- Runtime / bit-exact / differential.
