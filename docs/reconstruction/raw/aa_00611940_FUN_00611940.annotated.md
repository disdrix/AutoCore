# Annotated low-level: FUN_00611940

| Field | Value |
|---|---|
| Stable ID | `aa_00611940` |
| VA | `0x00611940` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_00611940`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
float10 __thiscall FUN_00611940(int param_1,int param_2)

{
  int iVar1;
  float fVar2;
  
  if (*(int *)(param_1 + 0x14) == 0) {
    return (float10)g_flOne;
  }
  if ((*(int *)(param_2 + 0x250) != 0) && (*(int *)(*(int *)(param_2 + 0x250) + 0x2b0) != 0)) {
    iVar1 = FUN_005711c0(*(undefined4 *)(param_1 + 0x10),0);
    fVar2 = g_flOne;
    if ((float)iVar1 / (float)*(int *)(param_1 + 0x14) < g_flOne) {
      iVar1 = FUN_005711c0(*(undefined4 *)(param_1 + 0x10),0);
      fVar2 = (float)iVar1 / (float)*(int *)(param_1 + 0x14);
    }
    return (float10)fVar2;
  }
  return (float10)g_flZero;
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.

---

## v2026-07-29 inventory collect bridge (append-only)

| Field | Value |
|---|---|
| Canonical name | `CVOGObjectiveRequirement_Collect_Eval` |
| Role | Progress fraction `min(1, count/NumToCollect)` from **cargo inventory** |
| Count | `InventoryGrid_CountItemsByCbid` @ `0x005711c0` |
| Offsets | CBID `req+0x10`, NumToCollect `req+0x14` |
| Cargo | `char+0x250` → `+0x2b0` |
| Vtable | Collect `PTR_FUN_009e12c4` +0x10 → `0x00611940` |
| Note | Unlike Kill/UseItem Eval (slot-float threshold), Collect Eval is inventory-derived |
| Duals | A/B `aa_00611940_CVOGObjectiveRequirement_Collect_Eval` |
| Chain | `reviews/CHAIN_2026-07-29_inventory_collect.md` |

---

## v2026-07-29 dual residual (append-only)

| Field | Value |
|---|---|
| Scratch | `reviews/a_00611940.md` |
| ABI | `__thiscall`; stack0=character; stack1 unread (family Eval formal); **RET 8** |
| Count | ECX=cargo; `includeBroken=0` both sites; **two real CALLs** when ratio&lt;1 |
| Return | SSE ratio + `FLD` ST0 (`float10` = x87 convention) |
| Constants | `g_flOne` `0x00a0f2a0`=1.0f; `g_flZero` `0x00a0f518`=0.0f |
| Authority | Inventory-derived Eval; not `0x2071` slot reader (server path separate) |

