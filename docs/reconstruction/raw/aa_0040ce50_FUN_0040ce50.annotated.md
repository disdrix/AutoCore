# Annotated low-level: Vehicle_GetModChipSkill_Inferred (FUN_0040ce50)

| Field | Value |
|---|---|
| Stable ID | `aa_0040ce50` |
| VA | `0x0040ce50` |
| System | input-drive-control / vehicle race-item |
| Date | 2026-07-29 (dual A/B; supersedes 2026-07-23 scaffold notes) |

## Machine-level notes

- Source: raw capture + live re-decompile + full-leaf `read_memory` (2026-07-29).
- Prefer assembly when decompiler conflicts — here CF agrees.
- ECX = vehicle*; plain `ret` (no stack cleanup).
- `vehicle+0x270` = race-item* hardpoint (inventory equip map).
- Dual-base walk matches `Vehicle_GetHpRegenRate` (`0x004fb630`); field here is **dword `+0x3bc`**, miss sentinel **−1**.
- Sole caller `Client_Input_TryUseModChip` (`0x0091f6b0`): −1 → `"You have no mod chip!"`; else LocalCastValidate skill arg.
- Return type skill object* vs skill id: **Medium** residual (do not over-claim).

## Pseudocode (annotated)

```c
// __fastcall ECX=vehicle*
uint32_t Vehicle_GetModChipSkill_Inferred(void *vehicle)
{
  void *pRace = *(void **)((char *)vehicle + 0x270);
  if (pRace == 0) {
    return 0xffffffff; /* sentinel: no mod-chip skill */
  }
  /* dual-base race → clonebase@+0xAC → sub@+0x3C → dword@+0x3BC */
  int adj = *(int *)(*(int *)(*(int *)((char *)pRace + 4) + 4) + 0xac + (int)pRace);
  int sub = *(int *)(adj + 0x3c);
  return *(uint32_t *)(sub + 0x3bc);
}
```

## Leaf bytes (sealed)

```
83b97002000000  cmp [ecx+0x270],0
741d            je miss
8b8170020000    mov eax,[ecx+0x270]
8b4804          mov ecx,[eax+4]
8b5104          mov edx,[ecx+4]
8b8402ac000000  mov eax,[edx+eax+0xac]
8b483c          mov ecx,[eax+0x3c]
8b81bc030000    mov eax,[ecx+0x3bc]
c3              ret
83c8ff          or eax,-1
c3              ret
```

## Open questions

- Product name.
- Exact type of dword `@+0x3bc` (skill* vs id/handle).
- Whether non-mod-chip race items leave `+0x3bc` as −1 or a different sentinel.
