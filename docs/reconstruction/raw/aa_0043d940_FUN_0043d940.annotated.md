# Annotated low-level: GuardedVector_Resize_Thiscall

| Field | Value |
|---|---|
| Stable ID | `aa_0043d940` |
| VA | `0x0043d940`–`0x0043d989` (**74 B**) |
| System | STL / guarded ring-deque container |
| Date | 2026-07-29 (W33-L seal; scaffold 2026-07-23) |
| Canonical | `GuardedVector_Resize_Thiscall` (**Inferred**) |
| Ghidra | `FUN_0043d940` |

## Machine-level notes

- **ABI:** **ECX=container** thiscall; stack **`(new_size, fill)`** by value; **`RET 0x8`**.
- Decompiler lists only `param_2` (new_size) — **bytes win**: both epilogues `C2 08 00`.
- Grow: LEA fill* to stack[+8] fill slot → `GuardedVector_InsertN_Thiscall` (`0x0043e080`) at `insert_at = begin+size`, `N = new_size-size`.
- Shrink: `FUN_0043df90` erase `[begin+new_size, begin+size)` (thiscall erase-range twin of W27-C `GuardedVector_EraseRange`).
- Equal sizes: no-op.
- Header: `+0x0c` begin, `+0x10` size (family `+0x04` pages / `+0x08` capacity unused here).
- Twin of W28-D `GuardedVector_Resize` (`0x0043c7e0`, EAX/ECX register ABI + internal fill0).

## Pseudocode (sealed)

```c
// ECX = container; stack new_size, fill; RET 8
void GuardedVector_Resize_Thiscall(GuardedVectorHeader* c /*ECX*/,
                                   uint32_t new_size,
                                   uint32_t fill)
{
  uint32_t size = c->size;
  if (size < new_size) {
    GuardedVector_InsertN_Thiscall(c, c, c->begin + size, new_size - size, &fill);
    return;
  }
  if (new_size < size) {
    GuardedVectorIteratorPair out;
    FUN_0043df90(&out, c, c->begin + new_size, c, c->begin + size);
  }
}
```

## Callers / callees

| Direction | Target | Note |
|---|---|---|
| Caller (1) | `FUN_0043d650` | fill=0 seed wrapper; stack new_size; RET 4 |
| Callee | `FUN_0043e080` | `GuardedVector_InsertN_Thiscall` (W32-F) |
| Callee | `FUN_0043df90` | thiscall erase-range (unowned) |

## Open residuals

- Product/PDB English.
- Nested erase `FUN_0043df90` free.
- Runtime / bit-exact.
