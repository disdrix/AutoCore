# Raw capture: FUN_005738d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_005738d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005738d0` |
| **Canonical name** | `FUN_005738d0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_005738d0(int param_1)

{
  int local_4;
  
  local_4 = param_1;
  FUN_00573700(&local_4,**(undefined4 **)(param_1 + 4),*(undefined4 **)(param_1 + 4));
                    /* WARNING: Subroutine does not return */
  operator_delete(*(void **)(param_1 + 4));
}
```

---

## Re-verify append — R11-004 OWN-ONLY dual 2026-08-05

**Agent:** R11-004 OWN-ONLY dual  
**Tools:** live Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` / `get_function_xrefs` / `get_xrefs_to` + `get_function_signature`. **No** `disassemble_bytes`. No Launcher.  
**Integrity:** scaffold body above preserved; this section append-only.

### Live decompile ≡ scaffold CF (truncated)

Live Ghidra decompile **≡** 2026-07-23 raw shape: full-range erase via `FUN_00573700` then `operator_delete(head)`. Decompiler marks `operator_delete` **noreturn** and **drops** the post-delete null epilogue — **bytes authority**.

### Body / ABI (bytes + disassembly)

| Fact | Value |
|---|---|
| Body | `0x005738d0`–`0x005738fc` inclusive (**45 B** / `0x2D`); pad `CC` to next unit |
| Convention | **`__fastcall` / thiscall-as-ECX**: ECX = map shell (`head@+4`, `size@+8`); bare **`RET`** (`C3`); void |
| Semantics | MSVC `_Tree` **tidy free-head** (no SEH): full erase `[begin,end)=[*head,head)`, `operator_delete(head)`, null head+size |
| Callees | dualed `StdTree_EraseRange_Val12_InventoryGrid_Inferred` `FUN_00573700`; `operator_delete` @ `0x00489822` (cdecl + `ADD ESP,4`) |
| Callers | 3 UNCONDITIONAL_CALL from SEH unwind stubs only (`Unwind@009a4f37`, `@009a4f57`, `@009a4f91`) — no direct product CALL sites in analyze |
| Classification | **worker** (`analyze_function_complete`) |
| Peer clone | byte-identical skeleton to dualed `StdTree_Tidy_FreeHead_Bare_Val12` @ `0x0040d6d0` (W33-A); only CALL displacements differ (erase=`00573700`, delete reloc) |

### Full-body hex (`read_memory`, 45 B)

```
51568bf18b46048b0850518d44240c508bcee819feffff8b4e0451e8325ff1ff83c40433c08946048946085e59c3
```

### Byte-sealed control flow

```
// entry: ESI = ECX = map shell; push ECX allocates out-iter slot
head = [ESI+4]
begin = *head                         // head->left
CALL FUN_00573700(ESI, &out, begin, head)  // full-range erase; RET 0xC
operator_delete(head); ADD ESP,4      // cdecl free sentinel
[ESI+4] = 0; [ESI+8] = 0              // head=null; size=0  (decomp drops)
POP ESI; POP ECX; RET                 // C3 bare
```

### Disassembly (`disassemble_function` + hex)

| VA | Bytes / insn | Role |
|---|---|---|
| `005738d0` | `51` PUSH ECX | out-iter stack slot |
| `005738d1` | `56` PUSH ESI | save |
| `005738d2` | `8B F1` MOV ESI,ECX | map shell |
| `005738d4` | `8B 46 04` MOV EAX,[ESI+4] | head |
| `005738d7` | `8B 08` MOV ECX,[EAX] | begin = *head |
| `005738d9`–`e0` | push head; push begin; LEA out; push out; MOV ECX,ESI | erase args |
| `005738e2` | `E8 19 FE FF FF` CALL `00573700` | dualed range erase |
| `005738e7`–`eb` | MOV ECX,[ESI+4]; PUSH; CALL `00489822` | free head |
| `005738f0` | `83 C4 04` ADD ESP,4 | cdecl cleanup |
| `005738f3`–`f9` | XOR EAX,EAX; MOV [ESI+4],EAX; MOV [ESI+8],EAX | null head+size |
| `005738fb`–`fc` | POP ESI; POP ECX; RET | bare exit |

### Unwind ownership (xref seal)

| Unwind stub | Prep | Target |
|---|---|---|
| `009a4f37` | `MOV ECX,[EBP-0x14]; ADD ECX,0x5c; JMP 005738d0` | map shell @ object `+0x5c` (InventoryGrid layout) |
| `009a4f57` | `MOV ECX,[EBP-0x10]; ADD ECX,0x5c; JMP 005738d0` | same `+0x5c` shell |
| `009a4f91` / `009a4f94` | third UNCONDITIONAL_CALL site into this tidy | SEH unwind family |

Layout matches dualed InventoryGrid map shell @ **`+0x5c`**, head @ **`+0x60`**, size @ **`+0x64`**.

### Peer clone note

| VA | Name | Erase leaf | Notes |
|---|---|---|---|
| `0x0040d6d0` | `StdTree_Tidy_FreeHead_Bare_Val12` (W33-A) | `0040d700` | generic Val12 bare tidy |
| `0x005738d0` | **this unit** | `00573700` (InventoryGrid) | same 45 B skeleton; InventoryGrid free/erase leaves |
| `0x0040d7c0` | `StdTree_Tidy_FreeHead_Val12` (W32-B) | `0040d700` | SEH-framed peer of bare (not this VA) |

### Name decision

Promote **`StdTree_Tidy_FreeHead_Bare_Val12_InventoryGrid_Inferred`** (Ghidra `FUN_005738d0`).

- Structural family: bare Val12 tidy free-head (peer `0040d6d0`).  
- Owner context: InventoryGrid placement map shell (`+0x5c`) via dualed erase `00573700`.  
- Product demangle open → **`_Inferred`**.  
- Retire scaffold-only plate as sole name.

### Gaps

- Product/MSVC demangle for map `T` / key English.  
- Which enclosing class frames each of the three unwind stubs (beyond `+0x5c` shell).  
- Runtime / bit-exact / differential open (no Launcher).
