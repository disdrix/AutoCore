# Annotated low-level: FUN_00507350 → VOGPhysics_QueryObject_Dtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00507350` |
| VA | `0x00507350`–`0x005073bd` inclusive (**110 B** / `0x6E`); exclusive end `0x005073be`; pad `CC` |
| System | interaction-activation |
| Date | 2026-08-05 (R13-028 dual seal) |
| Canonical name | `VOGPhysics_QueryObject_Dtor_Inferred` |
| Ghidra name | `FUN_00507350` |
| Parent dual | `0x00581190` `VOGPhysics_QueryObject_SubInit_Inferred` (R12-027) |

## Machine-level notes

- Prefer assembly when decompiler conflicts. Decompiler shows `__fastcall` + `FUN_005ffdb0(0)` without ECX — bytes seal **`__thiscall`** (`MOV ESI,ECX` / `MOV ECX,ESI` before `CALL FUN_005ffdb0`) and **`RET`** (`C3`).
- Free-size path: bytes `AND EAX,0x7FFFFFFF` then triple-`ADD EAX,EAX` (=`*8`) before pool free; decompiler collapses to `param_1[0xe]*8` which is equivalent once signed test proves high bit clear.
- Linked release: short refcount @ **linked+0x06**; zero → `vtbl[0](1)`.
- Capacity empty sentinel **`0x80000000`** @ **`+0x38`** (matches SubInit seed); free only when signed capacity ≥ 0.
- Three-phase vtbl: provisional `009cd99c` → mid `009d81e8` → base `009cc290`.
- Scalar deleting wrapper is **peer** `FUN_005073f0` (not this VA).

## Layout (header through +0x38; matches SubInit)

| Off | Role |
|----:|---|
| +0x00 | vtbl (cycled in dtor) |
| +0x04 | ushort size/flag word; high bit → `FUN_005ffdb0(0)` clear path |
| +0x06 | self refcount (SubInit seeds 1; not decremented in this body) |
| +0x0C | `linked_obj*` — release / optional scalar-delete |
| +0x30 | growable buffer pointer |
| +0x38 | capacity dword; high bit set = empty (no free) |

## Pseudocode (annotated)

```c
// __thiscall ECX=self; RET 0
void __thiscall VOGPhysics_QueryObject_Dtor_Inferred(uint32_t *self)
{
  uint32_t *linked = (uint32_t *)self[3];           // +0x0C
  *self = &PTR_FUN_009cd99c;                        // provisional

  if (linked != nullptr) {
    short *rc = (short *)((uint8_t *)linked + 6);
    if (--*rc == 0)
      (*(code **)*linked)(1);                       // scalar-delete/release
  }

  int cap = (int)self[0xe];                         // +0x38
  if (cap >= 0) {
    uint32_t n = ((uint32_t)cap & 0x7FFFFFFFu) * 8;
    // pool free: (*DAT_00b05060)[vtbl+0x14](ptr@+0x30, n, tag 0x12)
    ((**((code ***)*DAT_00b05060 + 0x14 / 4)))(self[0xc], n, 0x12);
  }

  *self = &PTR_FUN_009d81e8;                        // mid base
  uint16_t flags = *(uint16_t *)(self + 1);         // +0x04
  if ((short)flags < 0) {
    flags &= 0x7FFF;
    *(uint16_t *)(self + 1) = flags;
    FUN_005ffdb0(self, 0);                          // thiscall; clear weak bit path
  }

  *self = &PTR_LAB_009cc290;                        // final base
}
```

## Callers

| Caller | Site | Note |
|---|---|---|
| `FUN_005073f0` | `0x005073f3` | scalar deleting: body then free self size `*(ushort*)(this+4)` tag `0x10` if bit0 |
| `FUN_005074d0` | `0x005075b7` | derived multi-buffer dtor → this |
| `FUN_006343d0` | `0x0063443e` | derived array-of-ptrs dtor → this |

## Open questions

- Product/PDB English for class owning vtbl `009cd99c` / mid `009d81e8` / base `009cc290`.
- Exact semantics of flag word @ +4 (size for scalar free vs ownership bit only).
- Linked object type English (parent uses as vcall target).
- Runtime / bit-exact / differential.
