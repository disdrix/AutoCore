# Annotated low-level: Perf_QpcBeginNamed_Inferred (FUN_007a4400)

| Field | Value |
|---|---|
| Stable ID | `aa_007a4400` |
| VA | `0x007a4400`–`0x007a445c` (**92 B**) |
| System | perf / QPC named begin |
| Date | 2026-07-29 (W26-D re-seal; scaffold 2026-07-23) |
| Ghidra name | `FUN_007a4400` |
| Canonical (inferred) | `Perf_QpcBeginNamed_Inferred` |

## Machine-level notes

- **ABI:** `__thiscall`; **ECX** = timer slot; stack `char* label`; epilogue **`ret 4`**; **EAX** = this.
- **Slot layout:** `+0` = `LARGE_INTEGER` start counter (8 B); `+8` = null-terminated label string.
- **Once-init flag:** `DAT_00b00a74` (byte). First call: clear flag, `QueryPerformanceFrequency`, store `g_flOne / (float)freq` at `DAT_00afa254` (float reciprocal for later duration math).
- **IAT:** `[0x009c61b4]` = QueryPerformanceFrequency; `[0x009c61b0]` = QueryPerformanceCounter (`read_memory` 2026-07-29).
- **CF:** once-init → strcpy label to `this+8` (decompiler form uses `8 - (int)param_2` offset arithmetic) → QPC into `this` → return this.
- **Leaf of game logic:** only Win32 QPF/QPC. High xref count (39) from init/sector/map/UI stages with label strings (`"@@new CVOGSectorMap"`, `"Client InitInstance"`, `"ReadMusicRegions"`, …).
- **Not** `FUN_007a4480` (that is NDError/vog_LogMessage). Prior auto-name `Named_CalleeOf_Named_VOG_DEBUG_STOP_007a4400` is xref-path noise.

## Pseudocode (annotated)

```c
// thiscall; ret 4; returns this
LARGE_INTEGER * __thiscall Perf_QpcBeginNamed_Inferred(LARGE_INTEGER *slot /*ECX*/, char *label)
{
  char c;
  int delta;
  LARGE_INTEGER freq;

  if (DAT_00b00a74 != 0) {
    DAT_00b00a74 = 0;
    QueryPerformanceFrequency(&freq);
    DAT_00afa254 = g_flOne / (float)(long long)freq.QuadPart;
  }
  // strcpy(slot+8, label) — Ghidra form:
  delta = 8 - (int)label;
  do {
    c = *label;
    label[(int)slot + delta] = c;  // == *( (char*)slot + 8 + (label_cursor - label) )
    label = label + 1;
  } while (c != '\0');
  QueryPerformanceCounter(slot);
  return slot;
}
```

## Globals

| Symbol | Role |
|---|---|
| `DAT_00b00a74` | once-init gate (byte; static image value 1) |
| `DAT_00afa254` | float `1.0f / QPF` after first begin |
| `g_flOne` (`0x00a0f2a0`) | float 1.0 used in reciprocal |

## Open residual

- Product English class name for the timer slot / original Torque plate.
- Who consumes `DAT_00afa254` for elapsed seconds (end/stop helper not OWN).
- Runtime bit-exact / differential — open.
