# Annotated low-level: FUN_005c6720 → TrackPayload_MatchObject_TfidOrPtrIdentity_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005c6720` |
| VA | `0x005c6720`–`0x005c6788` exclusive (**104 B**) |
| System | track-manager payload ↔ object match |
| Date | 2026-08-04 (W38-S) |
| Named | `TrackPayload_MatchObject_TfidOrPtrIdentity_Inferred` |

## Machine-level notes

- **Authority:** `read_memory` body + decompile CF. **No** `disassemble_bytes`.
- **ABI:** thiscall; ECX=payload; stack object*; bool AL; **`ret 4`**.
- Callers wire as `mov ecx, payload; push object; call` (W37-T sealed at untrack site).
- Peer pure TFID compare: `TFID_EqualsObjectId` @ `0x00404aa0` (cdecl two TFID ptrs) — this unit is **payload-relative** and has **pointer-identity fallback**.

## Layout (payload / track entry)

| Offset | Role |
|---|---|
| `+0x10` | object* back-pointer (used only when both TFIDs invalid) |
| `+0x18` | TFID lo (`int32`) |
| `+0x1c` | TFID hi (`int32`) |
| `+0x20` | TFID global (`int8`) |
| `+0x51` | flag written by sibling finder `FUN_004bfdb0` (not this unit) |

## Object TFID locate

```
adj  = *(*(object + 4) + 4)
tfid = (TFID*)(object + adj + 0x164)
// compares tfid->lo, tfid->hi, tfid->global  (9 addressable bytes)
```

## Control flow

```
if (payload.lo & payload.hi == -1) && (obj.lo & obj.hi == -1):
  return payload.objectPtr == object
else:
  return payload.tfid == object.tfid   // 2×i32 + i8
```

## Pseudocode (annotated)

```c
// __thiscall payload; stack (object*); ret 4; returns bool
bool TrackPayload_MatchObject_TfidOrPtrIdentity_Inferred(void *payload, void *object)
{
  uint32_t plo = *(uint32_t *)((char *)payload + 0x18);
  uint32_t phi = *(uint32_t *)((char *)payload + 0x1c);
  int adj = *(int *)(*(int *)((char *)object + 4) + 4);
  uint32_t *ot = (uint32_t *)((char *)object + adj + 0x164);

  if ((plo & phi) == 0xFFFFFFFFu && (ot[0] & ot[1]) == 0xFFFFFFFFu)
    return *(void **)((char *)payload + 0x10) == object;

  return ot[0] == plo
      && ot[1] == phi
      && *(int8_t *)(ot + 2) /* ot+8 */ == *(int8_t *)((char *)payload + 0x20);
}
```

## Open / residual

- Product English for payload class and host track manager.
- Exact MI/vb meaning of `*(object+4)` (secondary vptr vs vbtable) — formula sealed, product type open.
- Runtime / bit-exact / differential.
