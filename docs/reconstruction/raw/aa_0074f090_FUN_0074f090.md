# Raw capture: FUN_0074f090

| Field | Value |
|---|---|
| **Stable ID** | `aa_0074f090` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0074f090` |
| **Canonical name** | `FUN_0074f090` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int FUN_0074f090(void)

{
  int in_EAX;
  int iVar1;
  
  if (*(int *)(in_EAX + 0xc) != 0) {
    iVar1 = (**(code **)(**(int **)(in_EAX + 0xc) + 0x118))(*(int **)(in_EAX + 0xc));
    return (-1 < iVar1) - 1;
  }
  return 0;
}
```

---

## W38-W re-verify (2026-08-04) — append only

| Field | Value |
|---|---|
| **Body** | `0x0074f090`–`0x0074f0be` exclusive (**46 B** / `0x2E`) |
| **Ghidra body** | `0074f090`–`0074f0bd` (last byte = `RET`) |
| **Pad** | `CC CC` then next |
| **Tools** | `batch_decompile` + `read_memory` (46 B+) + `analyze_function_complete` + callers/xrefs + sole-caller call-site + sibling `FUN_0074f0c0`. **No** `disassemble_bytes` |
| **Live decompile** | CF ≡ scaffold; **bytes win** on EAX formal + SETGE→0/−1 + null path |
| **Sole caller** | `GfxDevice_Reset` `FUN_0075eff0` @ `0x0075f333` — fail plate `"Failed to Reset() device on Global Effect"` |

### Full body hex (46 B)

```
83780c0074198b400c8b0850ff911801000033d285c00f9dc283ea018bc2c333c033d285c00f9dc283ea018bc2c3
```

### Byte anchors

```text
0x0074f090: 83 78 0C 00                CMP dword [EAX+0xC], 0    ; this in EAX
            74 19                      JE  null_path
            8B 40 0C                   MOV EAX, [EAX+0xC]        ; inner iface*
            8B 08                      MOV ECX, [EAX]            ; vtbl
            50                         PUSH EAX                  ; this for stdcall/thiscall-ish
            FF 91 18 01 00 00          CALL [ECX+0x118]           ; OnReset-class vcall
            33 D2                      XOR EDX, EDX
            85 C0                      TEST EAX, EAX
            0F 9D C2                   SETGE DL                  ; result >= 0 ?
            83 EA 01                   SUB EDX, 1                ; 0 if ok, -1 if fail
            8B C2 C3                   MOV EAX, EDX; RET
0x0074f0af: 33 C0 33 D2 85 C0 …        null path → same SETGE form on 0 → return 0
            … 8B C2 C3
```

### Caller site (GfxDevice_Reset)

```text
0x0075f329: 8B 85 60 07 00 00          MOV EAX, [EBP+0x760]
            8B 40 04                   MOV EAX, [EAX+4]
            E8 58 FD FE FF             CALL FUN_0074f090
            3B C3                      CMP EAX, EBX              ; EBX=0
            7D 26                      JGE success_continue
            ; else log "Failed to Reset() device on Global Effect" → return -1
```
