# Annotated low-level: FUN_00604e30 → CNDHash_ValueClearOwnedPtrs

| Field | Value |
|---|---|
| Stable ID | `aa_00604e30` |
| VA | `0x00604e30`–`0x00604e84` (**85 B** / `0x55`) |
| System | container / CNDHash (medal family; nested **value** teardown for stamp `009ce0a8`) |
| Date | 2026-07-29 (W26-P dual seal; prior scaffold 2026-07-23) |
| Canonical | `CNDHash_ValueClearOwnedPtrs` (**Inferred**) |

## Machine-level notes

- Source: raw capture + live `decompile_function` / `read_memory` (no `disassemble_bytes`).
- **ECX = value\*** (`__thiscall`; Ghidra shows `__fastcall`).
- Four sequential owned-pointer slots at **`value+0x04`, `+0x08`, `+0x0C`, `+0x10`**:
  1. load pointer
  2. if non-null → `operator_delete` (cdecl: push + `ADD ESP,4`) — **returns**
  3. zero the slot (always, including null path)
- **Does not** touch `value+0` (no stamp/vtbl rewrite here).
- **Does not** free `value` itself — parent reclaim/dtor does `operator_delete(value)`.
- Decompiler noreturn warning on `operator_delete` is **false** (body continues after each call).
- Closes residual left open by W25-F reclaim dual ("full nested field matrix of FUN_00604e30").

## Layout (value object)

| Off | Field | Mutation |
|---|---|---|
| `+0x00` | (untouched) | — |
| `+0x04` | owned ptr | delete-if-nonnull; zero |
| `+0x08` | owned ptr | delete-if-nonnull; zero |
| `+0x0C` | owned ptr | delete-if-nonnull; zero |
| `+0x10` | owned ptr | delete-if-nonnull; zero |

## Pseudocode (annotated)

```c
// void __thiscall CNDHash_ValueClearOwnedPtrs(void *value)
// plain ret; operator_delete RETURNS
void __fastcall FUN_00604e30(int value)
{
  void *p;
  p = *(void **)(value + 4);
  if (p != 0) operator_delete(p);     // cdecl; returns
  *(void **)(value + 4) = 0;

  p = *(void **)(value + 8);
  if (p != 0) operator_delete(p);
  *(void **)(value + 8) = 0;

  p = *(void **)(value + 0xc);
  if (p != 0) operator_delete(p);
  *(void **)(value + 0xc) = 0;

  p = *(void **)(value + 0x10);
  if (p != 0) operator_delete(p);
  *(void **)(value + 0x10) = 0;
}
```

## Call graph (owned VA only)

| Dir | Symbol | VA / note |
|---|---|---|
| Caller | `CNDHash_ReclaimAll_ValueOwning_009ce0a8` (`FUN_0051bfb0`) | `0x0051bfe4` — thiscall on node value then `operator_delete(value)` |
| Caller | `FUN_0051bd90` (scalar dtor stamp `009ce0a8`) | `0x0051bda3` — same nested then delete value pattern |
| Callee | `operator_delete` | ×4; cdecl; returns |

## Twin / family

| Unit | Role |
|---|---|
| **This** | Nested value field teardown (4 owned heap ptrs) |
| `CNDHash_ReclaimAll_ValueOwning_009ce0a8` `0x0051bfb0` | parent reclaim; calls this then free value |
| Plain owning reclaim siblings `009cefec` / `009cefe4` | `operator_delete(value)` only — **no** this helper |

## Rejected alias

`Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Named_VOG_DEBUG_ST_00604e30` — **reject** as product id (no body string; VOG chain is parent assert noise).

## Open questions

- Product / PDB English for the value type (medal payload field names).
- Exact pointees of the four slots (buffers? nested objects?).
- Runtime / bit-exact / differential.
