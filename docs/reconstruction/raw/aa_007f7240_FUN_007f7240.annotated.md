# Annotated low-level: FUN_007f7240

| Field | Value |
|---|---|
| Stable ID | `aa_007f7240` |
| VA | `0x007f7240` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_007f7240`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
int __fastcall FUN_007f7240(undefined4 param_1,undefined4 param_2,int param_3)

{
  byte in_AL;
  
  switch(param_2) {
  case 0:
    if (in_AL < 5) {
      return param_3 + 6 + (uint)in_AL * 0x34;
    }
    break;
  case 1:
    if (in_AL < 6) {
      return param_3 + 0x10a + (uint)in_AL * 0x34;
    }
    break;
  case 2:
    if (in_AL < 0x78) {
      return param_3 + 0x242 + (uint)in_AL * 0x34;
    }
    break;
  case 3:
    if (in_AL < 0x14) {
      return param_3 + 0x1aa2 + (uint)in_AL * 0x34;
    }
  }
  return 0;
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.
