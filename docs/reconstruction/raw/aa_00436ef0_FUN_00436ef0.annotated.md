# Annotated low-level: Mem_FillDwordN_FromSrc_EaxEcxEdx (FUN_00436ef0)

| Field | Value |
|---|---|
| Stable ID | `aa_00436ef0` |
| VA | `0x00436ef0`–`0x00436f06` (**23 B** / `0x17`) |
| Ghidra | `FUN_00436ef0` |
| Preferred | `Mem_FillDwordN_FromSrc_EaxEcxEdx` |
| System | stl-helpers / POD fill |
| Date | 2026-07-23 scaffold; W34-D seal 2026-07-29 |

## Machine-level notes

- Leaf integer loop; **no stack formals**; bare **`RET`**.
- Register ABI: **EAX**=dest, **ECX**=count, **EDX**=src*.
- Null-safe: if dest is NULL, still advances EAX and decrements ECX without store (guards `operator_new` failure paths).
- Re-reads `*EDX` every iteration (not a once-hoisted value).
- Primary sealed parent path: `PtrVecShell_InitFill_Inferred` (`0x004646e0`) after `operator_new(count*4)`.
- Related (different ABI): `Mem_FillDwordN_ReturnEnd` (`0x004651d0`) — EDI/ESI/EDX + returns end.
- Reject scaffold `Named_CalleeOf_*`.

## Pseudocode (annotated)

```c
// Register ABI: EAX=dst, ECX=count, EDX=src*; bare ret; leaf
void Mem_FillDwordN_FromSrc_EaxEcxEdx(
    uint32_t* dst /*EAX*/,
    int count /*ECX*/,
    uint32_t const* src /*EDX*/)
{
  for (; count != 0; count = count - 1) {
    if (dst != nullptr) {
      *dst = *src;
    }
    dst = dst + 1;
  }
}
```

## Open / residual

- Product English for non-vector call sites (many).
- Whether any caller relies on EAX out = dest+count after null-path.
- Runtime / bit-exact / differential.
