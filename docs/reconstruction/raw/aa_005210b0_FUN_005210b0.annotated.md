# Annotated low-level: Character_ClearEquippedRaceItemTfId_Inferred (`FUN_005210b0`)

| Field | Value |
|---|---|
| Stable ID | `aa_005210b0` |
| VA | `0x005210b0` |
| Body | `0x005210b0`–`0x0052112b` (124 bytes) |
| Canonical name | `Character_ClearEquippedRaceItemTfId_Inferred` |
| Ghidra | `FUN_005210b0` |
| System | character race-item equip / inventory / ghost |
| Date | 2026-07-29 (W21-E dual seal) |

---

## Machine-level notes

- **ABI:** ECX = `character*` (`mov edi,ecx` at entry). No stack formals. Plain `ret` (not `ret N`).
- **TFID cache** at `this+0x518` (16 bytes): treated as `TFID_16` (parent duals + field `dwCoidLo`).
- **Validity gate (bytes):**
  - `eax = *(this+0x518) & *(this+0x51c)`; if `eax != -1` → treat as present, **or**
  - if `*(char*)(this+0x520) != 0` → treat as present;
  - else early return (already clear / null sentinel).
- **Host gate:** load host = `*( *(this+4)+4 + this + 0xa8 )` (MSVC multi-base / component pattern used by parent sync). If host is null → skip body (no resolve/clear).
- **Resolve path (bytes, decompiler simplified):**
  1. `ecx = *(host + 0xe4e8)` (resolve context / map table).
  2. `push &TFID` (`this+0x518`); call `Object_ResolveFromTFID` @ `0x004bb950`.
  3. If non-null: `Object_EnqueueDeferredOnce(host, obj)` — sealed sibling `aa_004d0e90` (`FUN_004d0e90`): ECX=host, stack=obj, sticky deferred bit on object.
- **Clear path (always when gates pass):** memcpy-style 4× dword copy from null template `DAT_009cee98`..`DAT_009ceea4` into `this+0x518`..`+0x524`.
  - Template sealed: `{0xffffffff, 0xffffffff, 0, 0}`.
- **Role:** teardown helper for character equipped race-item binding — schedule prior world object for deferred work, wipe TFID cache. Called from `Character_SyncEquippedRaceItemFromCbid_Inferred` (`0x00522f70`) on CBID/object mismatch, and `FUN_0052f260`.
- Does **not** clear CBID at `this+0x514` (parent owns that). Does **not** immediately free the object (deferred enqueue only).

---

## Pseudocode (annotated)

```c
/* Character_ClearEquippedRaceItemTfId_Inferred
   If race-item TFID slot looks live and host component exists:
     resolve world object; enqueue deferred once; write null TFID template. */

void __thiscall Character_ClearEquippedRaceItemTfId(void *self /*ECX*/)
{
  uint32_t *tfid = (uint32_t *)((char *)self + 0x518);

  /* gate: (tfid[0] & tfid[1]) != 0xffffffff  OR  *(char*)(self+0x520) != 0 */
  /* host = *(*(self+4)+4 + self + 0xa8); if host==0 return; */

  /* obj = Object_ResolveFromTFID_via(*(host+0xe4e8), tfid); */
  /* if (obj) Object_EnqueueDeferredOnce(host, obj); */

  /* copy DAT_009cee98 16B → self+0x518  (ff..ff ff..ff 00..00 00..00) */
}
```

---

## Open questions

1. Product/PDB English symbol for this exact helper — open; role **INFERRED** from parent race-item sync + TFID/host CF.
2. Full semantics of `host+0xe4e8` resolve context object (map vs world table) — out of owned scope; sealed only as call intermediate.
3. Whether any other callers beyond `0x00522f70` / `0x0052f260` exist outside current xref set.
4. Runtime / bit-exact / differential — open.
