# Raw capture: FUN_0074e200

| Field | Value |
|---|---|
| **Stable ID** | `aa_0074e200` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0074e200` |
| **Canonical name** | `FUN_0074e200` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_0074e200(int param_1,char param_2)

{
  int iVar1;
  
  iVar1 = *(int *)(param_1 + 0x3c);
  if (iVar1 != 0) {
    if ((param_2 != '\0') && (*(char *)(DAT_00d1f048 + 0xf) != '\0')) {
      *(int *)(iVar1 + 0xdc) = *(int *)(iVar1 + 0xdc) + 1;
      *(undefined1 *)(iVar1 + 0xd1) = 1;
      return;
    }
    *(int *)(iVar1 + 0xdc) = *(int *)(iVar1 + 0xdc) + 1;
    *(undefined1 *)(iVar1 + 0xd1) = 0;
  }
  return;
}
```

---

## W31-Q re-verify (2026-07-29) — append only

| Field | Value |
|---|---|
| **Tools** | `decompile_function` + `analyze_function_complete` + `read_memory` + xrefs + caller bytes @ `0x004cdbf0`. **No** `disassemble_bytes` |
| **Live decompile** | ≡ raw 2026-07-23 CF (byte-identical logic) |
| **Body** | `0x0074e200`–`0x0074e23c` inclusive last `C2 04 00` (**61 B** / `0x3D`); pad `CC CC`; next `FUN_0074e240` |
| **ABI** | **ECX=`this`** (slot / `DAT_00d1a54c` object); stack **`char enable`** @ `[esp+4]`; cleanup **`RET 4`** (`C2 04 00`); void |
| **Callees** | none (leaf) |
| **Callers** | sole static: `FUN_004cda90` (`Client_InitPalantirViewBundle_Inferred`) @ `0x004cdc0a` (enable=0) and `0x004cdc17` (enable=1) |
| **Classification** | leaf |
| **Semantic name** | `EnvSlot_SetActiveBumpGen` (**Inferred** structural) |

### Full body hex (61 B)

```
8b413c85c07434807c240400741e8b0d48f0d10080790f0074128380dc00000001b1018888d1000000c2040032c98380dc000000018888d1000000c20400
```

### Byte-sealed algorithm

```
// thiscall; ECX=slot; stack char enable; RET 4
related = *(this + 0x3c)
if related == 0: return
if enable != 0 AND *(DAT_00d1f048 + 0x0f) != 0:
  *(related + 0xdc) += 1
  *(related + 0xd1) = 1
  return
// disable path OR enable blocked by global gate
*(related + 0xdc) += 1
*(related + 0xd1) = 0
return
```

### Caller site (bytes @ `0x004cdbf0`, not owned)

```
// after FUN_0048fc90(env,1):
//   ESI = *(env+4)   // new slot
//   ECX = DAT_00d1a54c
//   if ECX:  push 0; call FUN_0074e200     // disable previous
//   if ESI:  push 1; mov ecx,esi; call     // enable new
//   DAT_00d1a54c = ESI
```

Sibling (not owned): `FUN_00442b90` folds disable-old + enable-new + global publish into one free function using the same related offsets `+0x3c` / `+0xdc` / `+0xd1` and gate `DAT_00d1f048+0xf`.
