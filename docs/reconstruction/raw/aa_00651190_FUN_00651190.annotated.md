# Annotated low-level: FUN_00651190

| Field | Value |
|---|---|
| Stable ID | `aa_00651190` |
| VA | `0x00651190` |
| System | `skills-abilities` |
| Date | `2026-08-05` (R12-001 dual seal; prior scaffold 2026-07-23) |
| Inferred name | `CVOGHBSkill_SharedScalarDeletingDtor_Inferred` |

## Machine-level notes

- Source: raw capture for `aa_00651190` + R12-001 re-verify.
- Prefer assembly when decompiler conflicts (delete-noreturn warning is false).
- Role: MSVC **scalar deleting destructor** — complete body then optional free.
- Complete body entered via **thunk** `0x00548f90` → `FUN_00578a60` (`CVOGHBSkillBase` complete dtor: restore `PTR_FUN_009d3fdc`, free skill fields, tail `CVOGHBBase_dtor`).
- ABI: `__thiscall` ECX=this; stack flags; **`ret 4`**; EAX=this.
- Free gate: `(flags & 1)` only — no array-delete path.
- Hosts: **shared** vtbl[0] on many skill subclass tables (e.g. Master `0x009d0e6c`, XPAdder `0x009e5304`), not base `0x009d3fdc` (that uses `0x00578de0`).
- CODE sites are **computed** virtual deletes: `push 1; call [vtbl+0]` on owner-null paths.

## Pseudocode (annotated)

```c
// __thiscall  ret 4
void * __thiscall FUN_00651190(void *thisHb /* ECX */, byte flags /* stack */)
{
  // CALL 0x00548f90 → JMP FUN_00578a60 (CVOGHBSkillBase complete dtor)
  thunk_FUN_00578a60();  // decompiler elides this; ECX = thisHb

  if ((flags & 1) != 0) {
    // machine continues after delete — decompiler "does not return" is noise
    operator_delete(thisHb);
  }
  return thisHb;  // EAX
}
```

## Open questions

- Product/PDB mangled symbol for the merged scalar dtor.
- Complete dtor `FUN_00578a60` dual residual (not OWN for R12-001).
- Exhaustive subclass vtbl inventory beyond sealed samples.
- Runtime / bit-exact / differential (Terminal false).
