# Annotated: FUN_004cc400 → StdMap_OperatorIndex_Tfid_Isnil29_Inferred

| Field | Value |
|---|---|
| **Stable ID** | `aa_004cc400` |
| **VA** | `0x004cc400` |
| **Named** | `StdMap_OperatorIndex_Tfid_Isnil29_Inferred` |
| **Ghidra** | `FUN_004cc400` |
| **Agent** | WQ9G-C OWN-ONLY dual 2026-08-04 |

---

## Role

MSVC-style **`std::map::operator[]`** (get-or-insert mapped) for the **isnil@+0x29** tree family with **TFID-shaped pair key** (hi signed primary @ +0x14, lo unsigned secondary @ +0x10). Sole caller of dualed insert-with-hint `StdTree_InsertHint_Isnil29_Inferred` (`0x004cc220`).

---

## ABI

```c
// __thiscall  RET 4
int* /* mapped* */ __thiscall StdMap_OperatorIndex_Tfid_Isnil29_Inferred(
    MapShell_Isnil29 *map,   // ECX  head@+4 size@+8
    const uint32_t *key16);  // stack +4  four dwords (TFID-shaped)
// returns node + 0x20 (mapped; aggro score int)
```

---

## Control flow

1. **`StdMap_LowerBound_Tfid_Isnil29`** (`0x004cb4b0`) with key.
2. If `it == head` **or** `key < *it` (hi signed then lo unsigned) → **miss**:
   - Stage local value = `{key[0..3], mapped=0}`.
   - **`StdTree_InsertHint_Isnil29_Inferred`**(outIt, where=lower_bound result, value).
   - `it = *outIt`.
3. **Hit or after insert:** return `it + 0x20` (mapped reference).

Equality gate (bytes): after lower_bound, hit when not-end and ¬(key < node):

| Compare | Branch |
|---|---|
| `key.hi > node.hi` (signed) | hit |
| `key.hi < node.hi` | insert |
| hi equal, `key.lo >= node.lo` (unsigned) | hit |
| else | insert |

---

## Layout

| Offset | Field |
|---|---|
| node +0x10 / +0x14 | key lo (u32) / hi (s32) — TFID-shaped pair |
| node +0x18 / +0x1C | remaining TFID dwords (stored; not in compare) |
| node +0x20 | mapped (int score on aggro map) |
| node +0x28 / +0x29 | color / isnil |

---

## Call graph

| Role | Target |
|---|---|
| lower_bound | `StdMap_LowerBound_Tfid_Isnil29` `0x004cb4b0` (W31-N dual) |
| insert-hint | `StdTree_InsertHint_Isnil29_Inferred` `0x004cc220` (WQ9F-E dual; **sole caller**) |
| Callers | `FUN_004ca760` (aggro score upsert); `FUN_004ea350` ×3; site `0x005dd869` |

---

## Rejects

- Always-insert / no equality gate.
- Int-key operator[] / Val12 peers.
- Scaffold product English without demangle.
- Claiming full 16-byte lexicographic compare (only 8-byte pair ordered).
