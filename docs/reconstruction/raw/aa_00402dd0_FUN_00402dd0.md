# Raw capture: FUN_00402dd0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00402dd0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00402dd0` |
| **Canonical name** | `FUN_00402dd0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00402dd0(void)

{
  uint uVar1;
  int *piVar2;
  uint in_EAX;
  int unaff_ESI;
  
  uVar1 = *(uint *)(unaff_ESI + 8);
  if (uVar1 < in_EAX) {
    FUN_004034c0(unaff_ESI,*(undefined4 *)(unaff_ESI + 4),in_EAX - uVar1);
    return;
  }
  while( true ) {
    if (uVar1 <= in_EAX) {
      return;
    }
    piVar2 = (int *)(*(int **)(unaff_ESI + 4))[1];
    if (piVar2 != *(int **)(unaff_ESI + 4)) break;
    uVar1 = *(uint *)(unaff_ESI + 8);
  }
  *(int *)piVar2[1] = *piVar2;
  *(int *)(*piVar2 + 4) = piVar2[1];
                    /* WARNING: Subroutine does not return */
  operator_delete(piVar2);
}
```

---

## Re-verify append — MEGA-127 OWN-ONLY dual 2026-08-05

**Agent:** MEGA-127  
**Tools:** `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` / `get_xrefs_to` + `get_assembly_context`. **No** `disassemble_bytes`. No Launcher.  
**Integrity:** scaffold body above preserved; this section append-only.  
**System:** missions-progression (nested under `Client_DebugListMissionsStatus` + shared list resize utility).

### Live decompile ≡ scaffold CF (with assembly corrections)

Live Ghidra decompile **≡** 2026-07-23 raw CF shape (compare size → grow via `FUN_004034c0` / shrink unlink-delete loop).  
**Assembly corrects decompiler defects:**

1. **`operator_delete` returns** — decompiler "Subroutine does not return" is **false**; after CALL: `ADD ESP,4` then **`ADD dword ptr [ESI+8], -1`** then re-CMP/loop.
2. **ABI is not `void()`** — **ESI** = list\*, **EAX** = target count, **stack fill**, **`RET 4`** ×2 exits.
3. Shrink unlinks **`head->_Prev`** (list **back**), not an arbitrary node.

### Body / ABI (bytes + disassembly)

| Fact | Value |
|---|---|
| Body | `0x00402dd0`–`0x00402e1f` inclusive (**80 B** / `0x50`) |
| Convention | **ESI** = list shell\*; **EAX** = `new_size`; stack = **fill value** (address taken as `LEA ECX,[ESP+0x14]` into grow helper); void; **`RET 4`** ×2 |
| List layout | **`+4`** = myhead/sentinel\*; **`+8`** = `_Mysize` |
| Node layout | **`+0`** = `_Next`; **`+4`** = `_Prev` (MSVC circular list) |
| Callees | `FUN_004034c0` (grow/insert-N); `operator_delete` @ `0x00489822` (cdecl free node) |

Dataflow (sealed):

```text
PUSH EDI
MOV EDI, EAX                    ; target = new_size
MOV EAX, [ESI+8]                ; cur = size
CMP EDI, EAX
JBE shrink_or_eq

; GROW: target > cur
MOV ECX, [ESI+4]                ; head
SUB EDI, EAX                    ; n = target - cur
PUSH EDI / PUSH ECX / PUSH ESI
LEA ECX, [ESP+0x14]             ; &fill (caller stack arg)
CALL FUN_004034c0               ; insert n copies of fill before head (push_back-ish)
POP EDI
RET 4

shrink_or_eq:
JNC done                        ; target == cur
; SHRINK: while target < size, pop_back + delete
loop:
  MOV ECX, [ESI+4]              ; head
  MOV EAX, [ECX+4]              ; node = head->_Prev (back)
  CMP EAX, ECX
  JZ  recheck                   ; empty ring — recheck size
  ; unlink node from circular list
  MOV ECX, [EAX+4] ; prev
  MOV EDX, [EAX]   ; next
  MOV [ECX], EDX                ; prev->_Next = next
  MOV ECX, [EAX]
  MOV EDX, [EAX+4]
  PUSH EAX
  MOV [ECX+4], EDX              ; next->_Prev = prev
  CALL operator_delete
  ADD ESP, 4
  ADD dword ptr [ESI+8], -1     ; size--
recheck:
  CMP EDI, [ESI+8]
  JB  loop
done:
POP EDI
RET 4
```

Body hex (full 80 B):  
`578bf88b46083bf876158b4e042bf85751568d4c2414e8d50600005fc20400732b8b4e048b41043bc1741c8b48048b1089118b088b500450895104e8126a080083c404834608ff3b7e0872d55fc20400`

### Callers / xrefs

| Site | Parent | Role |
|---|---|---|
| `0x00957447` | `Client_DebugListMissionsStatus` `FUN_009572e0` | **clear** local mission id list (`XOR EAX,EAX`; `LEA ESI,[ESP+0x40]`; `PUSH` fill) before rebuild / `FUN_00402d10` push |
| `0x008ecb86` | `FUN_008eca80` | clear local list (`XOR EAX,EAX`) in mission traversal path |
| `0x00801d84` | `FUN_00801c50` | clear list at object+`0x5c0` (`LEA ESI,[EAX+0x5c0]`; EAX=0) |
| `0x0080b87f` | `FUN_0080b7f0` | clear list at `EDI+EAX*4+0x5cc` before refill from packet words |
| `0x008a5d7c` | `FUN_008a5c90` | clear list (`MOV ESI,EBP`; EAX=0) during UI/window ctor path |

- **xref_count = 5** UNCONDITIONAL_CALL.
- **All live call sites set EAX=0** → effective **clear/resize-to-empty**; grow path still present and sealed for general resize.
- Call-site pattern: `PUSH fill`; `XOR EAX,EAX` (or set count); `LEA/MOV ESI, list`; `CALL 0x00402dd0`.

### Name decision

Promote **`StdList_Resize_EsiList_EaxCount_Inferred`**.  
- Not mission-specific logic (shared STL-style list resize; mission debug is one consumer).  
- Product element type / fill width open → `_Inferred`.  
- Retire scaffold **`Named_CalleeOf_Client_DebugListMissionsStatus_00402dd0`**.  
- Keep distinct from **`StdList_Clear_ESI`** `0x00415e90` (clear-only, bare RET, no fill/grow) and ECX twin clear `0x00404060`.

### Gaps

- Exact element type / sizeof(T) for fill stack slot (callers push 0 / pointer-sized zeros).
- Full dual of grow helper `FUN_004034c0` / insert `FUN_004040f0` (MEGA-134 residual).
- Runtime / bit-exact / differential.
