# Annotated low-level: FUN_00538a40

| Field | Value |
|---|---|
| Stable ID | `aa_00538a40` |
| VA | `0x00538a40` |
| System | `missions-progression` |
| Date | `2026-07-29` (refine; raw body 2026-07-23) |

## Machine-level notes

- **Leaf** map **find** (exact int key): lower_bound-style walk + equality gate → head on miss.
- Prefer assembly / `read_memory` when call-site decomp drops map `this` (`ECX`).
- Body length **0x60** (96 bytes); dual exits `ret 8`.
- isnil @ **node+0x49** (not +0x1d — that is the smaller XP-table map template @ `0x004054f0`).
- Key @ **node+0x10**, signed compares (`JGE` / `JL`).
- Domain callers pass **map = character+0x508** (reward region / mission staging).

## ABI

```
ECX          = map*
[ESP+4]      = Node** ppOut
[ESP+8]      = const int* pKey
returns void; *ppOut = node or head
stack cleanup: ret 8
```

## Pseudocode (annotated)

```c
/* Map_FindByIntKey — exact key; out = node* or head */
void __thiscall FUN_00538a40(Map *map, Node **ppOut, const int *pKey)
{
  Node *head = map->head;           /* *(map+4) */
  Node *candidate = head;
  Node *node = head->parent;        /* root */

  if (node->isnil == 0) {           /* +0x49 */
    do {
      if (node->key < *pKey) {      /* +0x10 signed */
        node = node->right;         /* +0x08 */
      } else {
        candidate = node;
        node = node->left;          /* +0x00 */
      }
    } while (node->isnil == 0);
  }

  if (candidate != head && !(*pKey < candidate->key)) {
    *ppOut = candidate;             /* exact match */
    return;
  }
  *ppOut = head;                    /* end / miss */
}
```

## Body image (`read_memory` @ 0x00538a40, len 96)

```
518b51048b420480784900578b7c24107518568b373970107d058b4008eb048bd08b00
8078490074ec5e8b41043bd08954241074188b0f3b4a107c118d4424108b108b44240c
89105f59c20800894424048d4424048b108b44240c89105f59c20800
```

Highlights:

| Bytes | Meaning |
|---|---|
| `8B 51 04` | `EDX = [ECX+4]` head |
| `80 78 49 00` | isnil test `+0x49` |
| `39 70 10` / `7D` | `CMP key@+0x10`; `JGE` left |
| `8B 40 08` | right child |
| `3B 4A 10` / `7C` | equality gate `JL` → end |
| `C2 08 00` | `ret 8` (×2) |

## Open questions

1. Product export name.
2. Color bit / full node size (value sealed by siblings @ +0x18).
3. Runtime multi-key verification.
