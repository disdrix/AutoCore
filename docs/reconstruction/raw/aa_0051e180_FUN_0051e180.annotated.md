# Annotated low-level: Map_Tidy_FreeHead_B

| Field | Value |
|---|---|
| Stable ID | `aa_0051e180` |
| VA | `0x0051e180`–`0x0051e1ad` inclusive (**46 B**) |
| Ghidra | `FUN_0051e180` |
| Canonical | `Map_Tidy_FreeHead_B` |
| System | skills-abilities / STL map tidy (EraseRange_B host) |
| Date | 2026-08-04 (WQ9F-H OWN dual) |

## Machine-level notes

- Source: raw capture + WQ9F-H re-verify (`read_memory` full 46 B + partial `disassemble_function` + xrefs).
- **Decompiler incomplete**: truncates after `operator_delete` (false noreturn). Bytes seal null head@+4 / size@+8 + bare RET.
- Same 46 B tidy template as dualed `Map_Tidy_FreeHead` (`0x0051da30`); only erase callee differs.
- Erase callee: dualed **`Map_EraseRange_B`** (`0x0051d880`, isnil@+0x29 family). Full range `(*head, head)`.

## Signature (sealed)

```c
// ECX = map*; bare RET (C3); void. head @ +4; size @ +8
void __fastcall Map_Tidy_FreeHead_B(MapFacade* map);
```

## Pseudocode (annotated — bytes-complete)

```c
void __fastcall Map_Tidy_FreeHead_B(MapFacade* map)
{
  void* out; // stack slot from entry PUSH ECX
  MapNode* head = map->head;
  Map_EraseRange_B(map, &out, head->left /* *head */, head); // full clear
  operator_delete(head);
  map->head = nullptr;
  map->size = 0;
  // does NOT free map facade
}
```

## Xrefs / callers

| Kind | Address | Notes |
|---|---|---|
| Code | `FUN_009c3970` @ `0x009c3970` | `MOV ECX,0x00b0427c` / `JMP 0x0051e180` atexit tidy |
| Callee | `Map_EraseRange_B` `0x0051d880` | full-range erase |
| Callee | `operator_delete` | free sentinel head only |

## Peer discrimination

| Peer | VA | Erase callee |
|---|---|---|
| `Map_Tidy_FreeHead` | `0x0051da30` | `Map_EraseRange` `0x0051c7c0` |
| `Map_Tidy_FreeHead_B` | `0x0051e180` | `Map_EraseRange_B` `0x0051d880` |
| `FUN_0051e150` | `0x0051e150` | `Map_EraseRange_Isnil15` (not this dual) |

## Open gaps

- Product map host class / K,V at static `0x00b0427c`.
- Runtime / bit-exact open.
