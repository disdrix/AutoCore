# Annotated: InventoryGrid_RemoveItemByCoid (`FUN_00571d80`)

| Field | Value |
|---|---|
| Stable ID | `aa_00571d80` |
| VA | `0x00571d80` |
| Canonical | `InventoryGrid_RemoveItemByCoid` |
| Ghidra | `FUN_00571d80` |
| System | `inventory-transfer` |
| Date | 2026-08-04 (WQ8R-D re-verify) |
| Tools | decompile + read_memory; no disassemble_bytes |

## Machine seal

- **ABI:** MSVC `__thiscall` — `InventoryGrid*` in **ECX**; three stack dwords; **`ret 0x0C`**.
- **Returns:** `ItemObject*` in EAX (from `InventoryGrid_RemoveItem`); decompiler said `void`.
- **Body:** 39 bytes (`0x00571d80`–`0x00571da6`).
- **Formula (exact):**

```c
ItemObject* InventoryGrid_RemoveItemByCoid(
    InventoryGrid* grid,   // ECX
    int32_t coidLo,        // stack0
    int32_t coidHi,        // stack1
    int forceFull)         // stack2 (char/int; passed through)
{
    ItemObject* item = InventoryGrid_FindItemByCoid(grid, coidLo, coidHi);
    return InventoryGrid_RemoveItem(grid, item, /*removeQty=*/1, forceFull);
}
```

- **removeQty** is a **hardcoded 1** (`push 1` @ `0x00571d90`) — not a formal.
- Null item from Find → RemoveItem gate returns null (no crash path in Remove when item==0).
- Rel32 to `0x00571010` / `0x00571b80` **Confirmed**.

## Annotated pseudocode

```c
// thiscall; ret 0x0C
ItemObject* InventoryGrid_RemoveItemByCoid(
    InventoryGrid* this,
    int32_t coidLo,
    int32_t coidHi,
    int forceFull)
{
    ItemObject* item = InventoryGrid_FindItemByCoid(this, coidLo, coidHi); // 0x00571010
    return InventoryGrid_RemoveItem(this, item, 1, forceFull);             // 0x00571b80
}
```

## Call surface

| Direction | Targets |
|---|---|
| Callees | `InventoryGrid_FindItemByCoid` `0x00571010`; `InventoryGrid_RemoveItem` `0x00571b80` |
| Callers | `Client_RecvInventoryUseItemResponse`, `FUN_00810170`, `FUN_00810670`, `FUN_004f8e00` |

## Open questions

- Product PDB name (canonical is role-inferred from sealed callees).
- Whether any caller passes non-0/1 forceFull beyond bool-like `(pkt+0x2c!=0)`.
- Runtime / bit-exact / image diff open.
