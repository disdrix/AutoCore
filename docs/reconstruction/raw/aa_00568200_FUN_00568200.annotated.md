# Annotated low-level: StdTree_IteratorIncrement_Isnil49 (FUN_00568200)

| Field | Value |
|---|---|
| Stable ID | `aa_00568200` |
| VA | `0x00568200`–`0x0056824a` (74 B) |
| System | shared `client::stdtree` |
| Date | 2026-07-29 (W22-B dual seal) |
| Canonical | `StdTree_IteratorIncrement_Isnil49` |
| Ghidra | `FUN_00568200` |

## Machine-level notes

- Source: raw capture + W22-B re-verify (`decompile_function` + `read_memory`).
- ABI: `__fastcall` — ECX = iterator (`node**`); `ret` (`c3`).
- Node header: `+0` left, `+4` parent, `+8` right, `+0x49` is_nil.
- Twin algorithm: `aa_004cb270` (`StdTree_IteratorIncrement_Inferred`) with is_nil at **`+0x29`** (smaller value nodes).
- Color bit (~`+0x48` on 0x50 map nodes) unused by ++.
- **Not** mission journal id collect (falsified 2026-07-29).

## Pseudocode (annotated)

```c
// ECX = it  (node**)
void __fastcall StdTree_IteratorIncrement_Isnil49(int *it)
{
  char isnil;
  int parent;
  int *right;
  int *leftwalk;

  int node = *it;
  // already at end/nil sentinel → no-op
  if (*(char *)(node + 0x49) != '\0') {
    return;
  }

  right = *(int **)(node + 8);
  if (*(char *)((int)right + 0x49) == '\0') {
    // successor = leftmost node of right subtree
    isnil = *(char *)(*right + 0x49);
    leftwalk = (int *)*right;          // left child of right
    while (isnil == '\0') {
      isnil = *(char *)(*leftwalk + 0x49);
      right = leftwalk;
      leftwalk = (int *)*leftwalk;
    }
    *it = (int)right;
    return;
  }

  // no right child: climb while we were the right child of parent
  parent = *(int *)(node + 4);
  isnil = *(char *)(parent + 0x49);
  while ((isnil == '\0') && (*it == *(int *)(parent + 8))) {
    *it = parent;
    parent = *(int *)(parent + 4);
    isnil = *(char *)(parent + 0x49);
  }
  *it = parent;   // may be nil head (= end())
}
```

## Open questions

- Product English for template instantiations (map key/value types per site).
- Color offset unused here (not required for successor).
- Runtime / differential open.
