# Annotated low-level: FUN_004c3a40 / CVOGObject_SetLocalPosition4_Gated_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004c3a40` |
| VA | `0x004c3a40` |
| Body span | `0x004c3a40` – `0x004c3ab0` (113 bytes) |
| System | `input-drive-control` + object pose |
| Date | 2026-07-23 scaffold; **2026-07-29 dual A/B strengthen** |

---

## Machine-level notes

- Source: raw capture for `aa_004c3a40` + live three-rep 2026-07-29.
- ABI: `__thiscall`, stack `float *in`, **`ret 4`**.
- IAT `_isnan` via `dword ptr [0x009c655c]` held in EBX for three calls.
- NaN path: `PUSH 0x00a15844` (`"VOG_DEBUG_STOP"`), `PUSH 0`, `CALL FUN_007a4480` — **does not return early**.
- Gate: `TEST byte ptr [ECX + EDI + 0xb8], 0xC7` after `typeOff = *(*(this+4)+4)` in ECX, this in EDI.
- Store: `PUSH ESI` / `MOV ECX,EDI` / `CALL FUN_004cb3e0` → four dwords at `this+0x240..0x24c`.
- Gate **home** differs from SetSteerInput (`wobj` pointer at `+0xb0`, flags at `wobj+0xb4`); **mask** same `0xC7`.

## Pseudocode (annotated)

```c
// thiscall: ECX = object, [esp+4] = float *in (vec4)
void __thiscall FUN_004c3a40(int self, float *in)
{
  // --- NaN trap on xyz only (W unchecked) ---
  if (_isnan((double)in[0]) != 0
   || _isnan((double)in[1]) != 0
   || _isnan((double)in[2]) != 0) {
    FUN_007a4480(0, "VOG_DEBUG_STOP");  // mode 0 → logger no-op
  }

  // --- component flag gate (type layout offset) ---
  // typeOff = *(*(self+4)+4)
  // flags  = *(u8*)(self + typeOff + 0xb8)
  if ((*(byte *)(*(int *)(*(int *)(self + 4) + 4) + 0xb8 + self) & 0xC7 /*199*/) == 0) {
    FUN_004cb3e0(self, in);  // self+0x240..0x24c = in[0..3]
  }
  return;
}
```

## Instruction map (sealed)

| VA | Ops | Meaning |
|----|-----|---------|
| `004c3a40` | `push ebx; mov ebx,[0x009c655c]` | `_isnan` IAT |
| `004c3a47` | `push esi; mov esi,[esp+0xc]` | `in` after 2 pushes |
| `004c3a4c`–`004c3a82` | triple `fld`/`isnan` | xyz; any → trap |
| `004c3a84` | `push "VOG_DEBUG_STOP"; push 0; call 007a4480` | trap |
| `004c3a93` | `mov eax,[edi+4]; mov ecx,[eax+4]` | typeOff |
| `004c3a99` | `test byte [ecx+edi+0xb8],0xc7` | gate |
| `004c3aa1` | `jnz epilogue` | closed |
| `004c3aa3` | `push esi; mov ecx,edi; call 004cb3e0` | open store |
| `004c3aab`–`004c3aae` | `pop*3; ret 4` | epilogue |

## Open questions

- Product / RTTI method name (remain `_Inferred`).
- Producers of `+0xb8` bits under mask `0xC7`.
- Full consumer set for `+0x240` beyond getter `FUN_004c3ac0`.
