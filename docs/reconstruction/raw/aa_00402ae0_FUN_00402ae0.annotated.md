# Annotated low-level: FUN_00402ae0 → StdMap_Find_Tfid_Isnil29_EaxMap_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00402ae0` |
| VA | `0x00402ae0`–`0x00402b2f` inclusive (**80 B** / `0x50`) |
| System | `interaction-activation` (partition MEGA-083; client map at `+0xd24`) |
| Date | `2026-08-05` (MEGA-083 refresh) |
| Named | `StdMap_Find_Tfid_Isnil29_EaxMap_Inferred` |
| Prior scaffold alias | `Named_CalleeOf_Client_SendRespawnInSector_00402ae0` (**retired**) |

---

## Machine-level notes

- **Live** Ghidra decompile ≡ raw CF; formals `in_EAX` / `unaff_EDI` are **real ABI** (EAX=map, EDI=key), not phantoms to discard.
- Stack formal `param_1` = `Node** outIt` only; **RET 4**.
- Callee `FUN_00403e50` = residual lower_bound (isnil@**+0x29**, key lo@**+0x10** / hi@**+0x14**).
- Post-check is equality gate (JL/JG on hi, JC on lo) — MSVC `_Tree::find` shape, **not** pure lower_bound.
- Callers load TFID-shaped keys from entity **`+0x160` / `+0x164`** and map hosts at **`client+0xd24`** or global `0x00d1b564`.
- Twin layout/CF: dualed `StdMap_Find_Tfid_Isnil29_Inferred` (`0x004cba00`) but **thiscall RET 8** — do **not** merge ABI.

---

## Recovered ABI

```c
// EAX = map (head @ +4); EDI = const KeyU32I32*; stack Node** outIt; RET 4
// *outIt written; EAX = outIt on return
Node** StdMap_Find_Tfid_Isnil29_EaxMap_Inferred(
    /*EAX*/ MapHost_Head4* map,
    /*EDI*/ const KeyU32I32* pKey,
    /*stack*/ Node_Isnil29** outIt);
```

---

## Pseudocode (annotated, ABI-corrected)

```c
// raw formals preserved in comments
int * FUN_00402ae0(int *outIt /* param_1 */)
{
  // int in_EAX;     → MapHost* map
  // uint *unaff_EDI; → const KeyU32I32* pKey
  int lb = FUN_00403e50(/* map=EAX, key=EDI→EDX */);  // lower_bound
  int head = *(int *)(map + 4);

  if (lb != head) {
    // node.key <= *pKey  (hi signed @ +0x14, lo unsigned @ +0x10)
    if ((*(int *)(lb + 0x14) <= (int)pKey[1]) &&
        ((*(int *)(lb + 0x14) < (int)pKey[1]) ||
         (*(uint *)(lb + 0x10) <= pKey[0]))) {
      *outIt = lb;          // hit
      return outIt;
    }
  }
  *outIt = head;            // miss / end
  return outIt;
}
```

---

## Node / key layout (structural)

| Offset | Field | Notes |
|---|---|---|
| node+0x00 | left | tree link |
| node+0x04 | parent | tree link |
| node+0x08 | right | tree link |
| node+0x10 | key.lo | u32 |
| node+0x14 | key.hi | i32 signed compare |
| node+0x20 | value / payload | callers (`FUN_00944d50`, key-down path) |
| node+0x29 | isnil | used inside residual lower_bound only |

| Key | Field |
|---|---|
| key+0 | lo u32 |
| key+4 | hi i32 |

---

## Callers (inventory)

| Site | Parent | Role |
|---|---|---|
| `0x00935340` | `Client_SendRespawnInSector` | Gate respawn packet if find node == `client+0xd28` |
| `0x00944e4b` | `FUN_00944d50` | Map `@+0xd24`; replace/release payload `@node+0x20` |
| `0x0091120a` | `Client_Input_OnKeyDown_MatchAction` | Global map `d1b564`; RTTI on payload |
| `0x0091df4e` | `FUN_0091da70` | Same global map; TFID key |
| `0x00957e3a` | (site parent) | Client map `@+0xd24` / stash `@+0xd28` |

---

## Open questions / gaps

- Product English for map host at `client+0xd24` and value_type at `node+0x20`.
- Residual lower_bound `FUN_00403e50` not dualled here (exclusive VA).
- Whether `client+0xd28` stores node* vs object* (callers mix node compares and payload use).
- Runtime / bit-exact / differential (Terminal **false**).
