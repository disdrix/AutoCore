# Annotated low-level: FUN_0073e980 → Gfx_EffectMap_ReleaseAllEntries_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0073e980` |
| VA | `0x0073e980`–`0x0073e9af` exclusive (**47 B**) |
| System | client / Palantir graphics — effect map / `gfxDevice` reset shutdown |
| Date | 2026-08-04 (W38-W) |

## Machine-level notes

- **ABI:** no formals; plain **`RET`**. Uses global tree head **`DAT_00d1ee68`**.
- **Container:** MSVC-style tree: head/sentinel in `ESI`; begin=`*head`; end when `cur==head`. Iterator++ is **`FUN_004425f0`** with **`EDX=&cur`** (isnil @ node `+0x11`).
- **Payload:** dword at node **`+0xC`** pushed to **`FUN_0073e780`** — releases per-entry D3D/effect slots (vector of objects, freelist, clear count@`+0x10`). Does **not** erase tree nodes.
- **Role:** device-reset **shutdown** for-each: release all registered effect-entry resources while walking the global map. Sole caller **`GfxDevice_Reset`** (`FUN_0075eff0`) immediately before sibling lost-device pass **`FUN_0074f0c0`**.
- Related single-entry teardown **`FUN_0073e800`** also touches `DAT_00d1ee68` / erase via `DAT_00d1ee64`.
- **Bytes win** on stack slot for `cur`, `LEA EDX,[ESP+4]` into iterator++, payload @`+0xC`.

## Pseudocode (annotated)

```c
// void Gfx_EffectMap_ReleaseAllEntries_Inferred(void)
// Walk global effect tree at DAT_00d1ee68; release each node[+0xC] payload.
void FUN_0073e980(void)
{
  int *head = DAT_00d1ee68;           // ESI — sentinel / end()
  int *cur  = (int *)*DAT_00d1ee68;   // begin(); also stored @ [ESP+4]
  while (cur != head) {
    FUN_0073e780(cur[3]);             // payload @ node+0xC — release resources
    FUN_004425f0(/*EDX=*/&cur);       // tree iterator++ (isnil@+0x11)
  }
}
```

## Open questions

- Product English name of map / value_type (effect set? material cache?).
- Full dual of `FUN_0073e780` / tree erase helpers (unowned).
- Whether color lives @`+0x10` with value @`+0xC` (layout consistent with isnil@`+0x11` + load@`+0xC`).
