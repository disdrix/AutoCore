# Annotated low-level: FUN_004025e0 → StdList_PushBack_Val0x28_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004025e0` |
| VA | `0x004025e0`–`0x00402611` inclusive last byte (**50 B** / `0x32`) |
| Canonical name | `StdList_PushBack_Val0x28_Inferred` |
| Ghidra name | `FUN_004025e0` |
| System | shared MSVC `std::list` (missions-progression seed via `CVOGReaction_TransferMap`) |
| Date | 2026-08-05 (MEGA-059 dual seal; prior scaffold 2026-07-23) |

## Machine-level notes

- Source: live Ghidra decompile ≡ raw CF; ABI sealed via `disassemble_function` + `read_memory`.
- **ECX** = `std::list*` shell; stack `const T*` with **T size 0x28**; **`RET 4`**.
- `_Myhead` @ list+4; size @ list+8 (via callee).
- Node layout Next@0 / Prev@4 / value@+8 (0x28 bytes); node size **0x30**.
- Link pattern = MSVC `_Insert(_Myhead, val)` = **`push_back`** (insert before sentinel).
- Callees: `FUN_00403560` buynode (`operator_new(0x30)` + `FUN_00404390` ctor, **RET 0xC**); `FUN_00403600` `_Incsize` max **`0x6666666`** (`0xFFFFFFFF/0x28`), string `"list<T> too long"`, **RET 4**.
- Sole caller: `CVOGReaction_TransferMap` @ `0x004d3893` with `ECX = reaction+0xe778`, stack local T built from resolved object fields.
- Prefer assembly when decompiler conflicts (TransferMap decompile shows `&stack0xffffffd4` without this — assembly seals list at `EDI+0xe778`).

## Pseudocode (annotated)

```c
// __thiscall: ECX = list*; stack = const Val0x28*; RET 4
void __thiscall StdList_PushBack_Val0x28_Inferred(StdListShell *list, const void *value /*0x28*/)
{
  ListNode0x30 *head = list->_Myhead;                 // *(list+4)
  ListNode0x30 *node = Buynode0x30(head, head->_Prev, value); // FUN_00403560
  Incsize_Max6666666(list, 1);                       // FUN_00403600
  head->_Prev = node;
  node->_Prev->_Next = node;
}
```

## Open questions

- Product English for `T` (0x28-byte transfer payload stored in reaction list @ +0xe778).
- Full reaction object layout beyond list embed offset.
- Runtime / bit-exact / differential open.
