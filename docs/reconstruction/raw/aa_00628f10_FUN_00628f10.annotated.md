# Annotated low-level: ActionHost_RemoveReverseListEntry_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00628f10` |
| VA | `0x00628f10`–`0x00628f64` exclusive (**84** B) |
| Ghidra | `FUN_00628f10` |
| Canonical | `ActionHost_RemoveReverseListEntry_Inferred` (**Inferred**) |
| System | vehicle / world action lifecycle (host reverse list) |
| Date | 2026-07-29 (W24-P live seal) |
| Tools | `decompile_function` + `read_memory` (no `disassemble_bytes`) |

## Machine-level notes

- **`__thiscall`**: ECX = **host** object; stack arg = **action** pointer; **`ret 4`**.
- Parent `FUN_0055df40` loads `ECX = *(action+0x10)` then `push action; call` — host is the reverse back-link stored on the action.
- List: pointer array at **`host+0x4c`**, count at **`host+0x50`**.
- Algorithm: linear find; **always** `count--`; if index < new count, shift-left elements to close the hole (order-preserving remove).
- Side effects after remove: **`*(action+0x10) = 0`**, **`*(uint8*)(host+0x28) = 1`** (dirty).
- No callees; pure structural.
- No product plate string.

## Pseudocode (annotated)

```c
void __thiscall ActionHost_RemoveReverseListEntry_Inferred(void *host, void *action)
{
  int count = *(int *)((char *)host + 0x50);
  int index = 0;
  if (count > 0) {
    int *p = *(int **)((char *)host + 0x4c);
    do {
      if (*p == (int)action) goto found;
      ++index;
      ++p;
    } while (index < count);
  }
  index = -1;
found:
  *(int *)((char *)host + 0x50) = count - 1; // always
  // shift if index still in range of new count
  // *(action+0x10) = 0;
  // *(char*)(host+0x28) = 1;
}
```

## Offsets sealed

| Object | Off | Role |
|---|---:|---|
| host | +0x4c | reverse list `base*` (action*) |
| host | +0x50 | reverse list `count` |
| host | +0x28 | dirty / rebuild flag (set to 1) |
| action | +0x10 | back-pointer to host (cleared to 0) |

## Open / nested (not owned)

- Product English for host class.
- Whether +0x28 dirty triggers a deferred rebuild path.
- Callers `00560f70` / `00561580` full dual context (not owned here).
