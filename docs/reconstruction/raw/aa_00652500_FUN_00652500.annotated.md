# Annotated low-level: FUN_00652500

| Field | Value |
|---|---|
| Stable ID | `aa_00652500` |
| VA | `0x00652500` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_00652500`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
int __thiscall FUN_00652500(int *param_1,char *param_2,int param_3)

{
  int iVar1;
  
  if (param_1[8] != 0xffffffff) {
    iVar1 = send(param_1[8],param_2,param_3,0);
    if ((0 < iVar1) && (iVar1 != -1)) {
      return iVar1;
    }
    iVar1 = WSAGetLastError();
    if (iVar1 != 0x2733) {
      (**(code **)(*param_1 + 8))();
    }
  }
  return 0;
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.
