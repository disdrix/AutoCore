# Annotated low-level: Math_QuatExtractForwardZ (`FUN_004e8a40`)

| Field | Value |
|---|---|
| Stable ID | `aa_004e8a40` |
| VA | `0x004e8a40` |
| Body | `004e8a40` – `004e8acb` |
| System | `input-drive-control` (pure math leaf) |
| Date | 2026-07-29 (dual seal; supersedes 2026-07-23 scaffold notes) |

## Machine-level notes

- Leaf: no calls, no branches, single return.
- Stack args: `param_1` = unit quat XYZW, `param_2` = out direction (W forced 0).
- `g_flLevelUpUiBase_Inferred` @ `0x00a10e74` = **2.0f** (shared pool; **not** UI here).
- `g_flOne` @ `0x00a0f2a0` = **1.0f**.
- Output = rotation-matrix **column 2** (local +Z forward in world space).
- Siblings (not owned): right `0x004e8ad0`, up `0x004e8b60`.

## Pseudocode (annotated)

```c
// Math_QuatExtractForwardZ — unit quat XYZW → chassis forward (+Z), W=0
void FUN_004e8a40(float *param_1 /* quat */, float *param_2 /* out */)
{
  float x, y, z, w;
  float two;  // 2.0f @ g_flLevelUpUiBase_Inferred (0xa10e74)

  two = g_flLevelUpUiBase_Inferred;
  x = *param_1;
  y = param_1[1];
  z = param_1[2];
  w = param_1[3];

  // out.x = 2*(z*x + y*w)   = 2(xz + yw)
  *param_2 = (z * x + y * w) * g_flLevelUpUiBase_Inferred;

  // out.y = 2*(z*y - x*w)   = 2(yz - xw)
  param_2[1] = (z * y - x * w) * two;

  // out.z = 1 - 2*(x*x + y*y)
  param_2[2] = g_flOne - (x * x + y * y) * two;

  // homogeneous direction
  param_2[3] = 0.0;
  return;
}
```

## Consumer notes (role only — bodies not owned)

- PushDriveAxes @ `0x004fbe43`: `forwardSpeed = dot(linVel, forward)` for overspeed thr polarity.
- MoveToTarget @ `0x004fc82e`: align / forward-speed dots.
- applyAction @ `0x005993fd`: chassis forward for action path.

## Open questions

- Optional runtime golden (identity / 90° yaw).
- Bit-exact image diff deferred.
