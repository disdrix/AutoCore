# Annotated low-level: StdTree_InsertAndRebalance_Val12 (FUN_00539210)

| Field | Value |
|---|---|
| Stable ID | `aa_00539210` |
| VA | `0x00539210` |
| Body | `0x00539210`–`0x005393fa` exclusive (**490** B) |
| Canonical | `StdTree_InsertAndRebalance_Val12` (inferred) |
| Ghidra | `FUN_00539210` |
| System | MSVC std `_Tree` always-insert + RB rebalance (Val12 node) |
| Date | 2026-07-29 (W27-Q seal; scaffold 2026-07-23) |

## Machine-level notes

- **ABI:** `__thiscall`; **ECX** = map (`head` @ `+4`, `size` @ `+8`); stack **`outIt*`**, **`addLeft`**, **`where*`**, **`value*`**; **`RET 0x10`**.
- **Max size:** if `size > 0x15555553` → throw `"map/set<T> too long"` @ `0x00a1530c` / `DAT_00acc388`.
- **Buynode:** `FUN_005ae220(head, where, head, value, color=0)` — sealed `StdTree_Buynode_Val12` (W24-I); node **0x1c**, red, isnil 0, 3-dword value copy.
- **Link:** empty head → root=leftmost=rightmost=n; else left (`addLeft!=0`) or right of `where` (+ extremity update).
- **RB loop:** while parent color red (`+0x18 == 0`): recolor uncle or rotate (`FUN_00573170` / `FUN_00418c10` / **inlined Lrotate** on parent-is-right branch).
- **Exit:** paint root black; `*outIt = n`; restore SEH; `add esp,0x50; ret 0x10`.
- **Not a key walk** — parent insert-or-find owns lower-bound / duplicate gate.
- **Peer clone:** `Map_TreeInsertAndRebalance_Val12` @ `0x0051c1c0` (W25-G) — same size constant, same buynode, same body length 490 B; that VA sole-called from category-cooldown; **this VA multi-caller**.
- **Callers:** `FUN_0053a1a0`, `FUN_0053a8e0` (9 xrefs).

## Annotated control flow

```c
// __thiscall map*, outIt*, addLeft, where*, value*  — always-insert Val12 node
void __thiscall StdTree_InsertAndRebalance_Val12(
    MapShell* map, Node** outIt, char addLeft, Node* where, const Val12* value)
{
  // SEH LAB_009a3ea2
  if (map->size > 0x15555553u)
    throw_map_too_long();  // "map/set<T> too long" @ 0x00a1530c

  Node* n = StdTree_Buynode_Val12(map->head, where, map->head, value, /*red*/ 0);
  map->size += 1;

  if (where == map->head) {
    map->head->parent = n;
    map->head->left = n;
    map->head->right = n;
  } else if (addLeft == 0) {
    where->right = n;
    if (where == map->head->right) map->head->right = n;
  } else {
    where->left = n;
    if (where == map->head->left) map->head->left = n;
  }

  // while parent red: recolor OR Lrotate(00573170) / Rrotate(00418c10) / inlined Lrotate
  // root->color = black; *outIt = n; ret 0x10
}
```

## Live ≡ raw

Live 2026-07-29 decompile ≡ 2026-07-23 raw (identical CF). Entry max-size cmp + epilogue `ret 0x10` sealed via `read_memory`.

## Open questions

- Product demangle for maps owned by `FUN_0053a1a0` / `FUN_0053a8e0`.
- Whether buynode null is UB (no null check in decomp).
- Runtime / bit-exact golden not run.
