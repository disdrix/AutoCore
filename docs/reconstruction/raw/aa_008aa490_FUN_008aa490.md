# Raw capture: FUN_008aa490

| Field | Value |
|---|---|
| **Stable ID** | `aa_008aa490` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x008aa490` |
| **Canonical name** | `FUN_008aa490` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_008aa490(undefined4 param_1,undefined4 param_2)

{
  int in_EAX;
  
  *(char *)(in_EAX + 0x580) = (char)param_2;
  if (*(int **)(in_EAX + 0x6e8) != (int *)0x0) {
    (**(code **)(**(int **)(in_EAX + 0x6e8) + 4))(param_2);
  }
  return;
}
```

---

## MEGA-078 re-verify (2026-08-05) — live Ghidra

**Agent:** OWN-ONLY dual MEGA-078  
**Tools:** `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` / `get_function_xrefs` / `get_xrefs_to` + `get_assembly_context` @ `00943a57`. **No** `disassemble_bytes`.  
**Terminal:** false (no Launcher / runtime Confirmed).

### Live decompile (2026-08-05) ≡ raw CF

```c
void __fastcall FUN_008aa490(undefined4 param_1,undefined4 param_2)
{
  int in_EAX;
  
  *(char *)(in_EAX + 0x580) = (char)param_2;
  if (*(int **)(in_EAX + 0x6e8) != (int *)0x0) {
    (**(code **)(**(int **)(in_EAX + 0x6e8) + 4))(param_2);
  }
  return;
}
```

Decompiler caveats (corrected via assembly):
- `in_EAX` is **dialog host this** (register EAX), not phantom garbage.
- `param_2` is **EDX/DL** (flag); storage `register:EDX`.
- `param_1` has empty storage — decompiler noise; **no ECX formal**.
- Signature display `__fastcall(param_1,param_2)` is **wrong** for MSVC thiscall; real ABI is **EAX=host, DL=flag, bare RET**.

### Disassembly (sealed)

```
008aa490  MOV  ECX,dword ptr [EAX + 0x6e8]
008aa496  TEST ECX,ECX
008aa498  MOV  byte ptr [EAX + 0x580],DL
008aa49e  JZ   0x008aa4a6
008aa4a0  MOV  EAX,dword ptr [ECX]
008aa4a2  PUSH EDX
008aa4a3  CALL dword ptr [EAX + 0x4]
008aa4a6  RET
```

**Body:** `0x008aa490`–`0x008aa4a6` inclusive (**23 B** / `0x17`); pad `CC` then sibling `FUN_008aa4b0` @ `0x008aa4b0`.

**Body hex (23 B):**
```
8B 88 E8 06 00 00 85 C9 88 90 80 05 00 00 74 06 8B 01 52 FF 50 04 C3
```

### ABI (sealed)

| Item | Value | Conf |
|---|---|---|
| Host / this | **EAX** (caller `MOV EAX,EBX` @ `0x00943a55`; EBX = client+0x1058 dialog ctrl) | **High** |
| Flag | **DL** (low byte of EDX); source `[ESP+0x1c]` after `FUN_0052b420` turn-in probe | **High** |
| Stack formals | none (bare `RET` / `C3`) | **High** |
| Return | void | **High** |
| Child object | `*(host+0x6e8)` → ECX for optional vcall | **High** |
| Flag store | `*(char*)(host+0x580) = DL` | **High** |
| Optional notify | if child ≠ 0: `PUSH EDX`; `CALL [child.vtbl+4]` (ECX=child) | **High** |
| Static callees | **none** (virtual only) | **High** |

### Callers / xrefs (live)

| Kind | Value |
|---|---|
| Classification | leaf (no static FUN_* edges; one virtual callee) |
| Code callers | **1** — `Client_ShowNpcMissionDialogUI` @ `0x00943a57` **UNCONDITIONAL_CALL** |
| Xrefs to | `From 00943a57 in Client_ShowNpcMissionDialogUI [UNCONDITIONAL_CALL]` |
| Xrefs from | none (vcall target dynamic) |

### Call-site context (parent pre-present)

```text
00943a43  CALL FUN_0052b420              ; turn-in readiness → AL
00943a48  TEST AL,AL
00943a4a  JZ   0x00943a51                ; keep prior [ESP+0x1c] if false
00943a4c  MOV  byte ptr [ESP+0x1c],0x1   ; flag = 1 when ready
00943a51  MOV  DL,byte ptr [ESP+0x1c]
00943a55  MOV  EAX,EBX                   ; EBX = mission dialog host (piVar2)
00943a57  CALL FUN_008aa490              ; OWN — store flag + notify child
00943a5c  MOV  ECX,EDI
00943a5e  MOV  ESI,EBX
00943a60  CALL Client_NpcDialog_PrepareResponseOpcode  ; 0x008abd70
00943a65  MOV  EAX,0x1
00943a6a  MOV  ECX,EBX
00943a6c  CALL FUN_008ab550              ; multi-mission panel fill residual
00943a71  MOV  EAX,[ESP+0x18]
00943a75  CALL FUN_008aa4b0              ; dialog helper residual MEGA-058
```

Parent then branches on the same readiness flag for header/chrome/reward setup (`FUN_008aaf60` / `FUN_008ac110` / `FUN_008aa510` / `FUN_008aa760`) and parks axes via `FUN_0092f090`.

### Semantics (sealed structure)

1. Load optional child widget/controller from **dialog+0x6e8**.
2. Store turn-in readiness **char** at **dialog+0x580** (always; even if child null).
3. If child non-null: notify via **vtbl[+4]** with stack arg = full EDX (flag in low byte).

Does **not** present the dialog, clear response slots, fill mission list, or set opcode 0x206E — those are sibling/parent steps. Pure **pre-present flag apply + optional child notify**.

### Name proposal

| Field | Value |
|---|---|
| Ghidra | `FUN_008aa490` |
| Proposed | `MissionDialog_SetTurnInReadyFlag_Inferred` |
| Retired scaffold | `Named_CalleeOf_Client_ShowNpcMissionDialogUI_008aa490` |
| System | `missions-progression` |
| Partition hint | Dialog pre-present setup (parent `0x009438f0`) |

### Completeness note

`analyze_function_complete` classification **leaf**; completeness score low due to phantom `in_EAX` / unresolved struct fields — ABI sealed externally via disasm + call site. Return type display void matches bare RET.
