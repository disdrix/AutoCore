# Annotated low-level: StdTree_EraseAndRebalance_Isnil31_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00408ed0` |
| VA | `0x00408ed0` |
| Canonical | `StdTree_EraseAndRebalance_Isnil31_Inferred` (Ghidra `FUN_00408ed0`) |
| System | MSVC `std::_Tree` erase + RB rebalance (isnil @ +0x31) |
| Date | 2026-08-05 WQ9K-E dual seal |

## Machine-level notes

- Prefer assembly when decompiler conflicts (esp. false noreturn on `operator_delete` truncating epilogue).
- **isnil @ +0x31**, **color @ +0x30** (node size **0x38** — peer buyhead `0x00409180`).
- **No** key/`basic_string` destructor — free is bare `operator_delete` (trivial/POD value).
- Successor capture: `FUN_00408590` (`StdTree_IteratorIncrement_Isnil31`) with out-it shadow on stack.
- Extremity fix: min `FUN_00409160` (isnil31), max `FUN_004043e0`.
- RB rotates: L `FUN_00403e90`, R `FUN_00403ee0` (isnil31 family pair — not isnil29/isnil2d pairs).
- Decompiler marks mid two-child splice blocks "unreachable" — retail still contains the path (bytes `0x00408fc9`…); preserve full MSVC erase semantics in ports.

## ABI

| Slot | Role |
|---|---|
| stack0 | MapShell* (`head@+4`, `size@+8`) |
| stack1 | outIt** (successor written at epilogue) |
| stack2 | node* to erase |
| cleanup | **`ret 0x0C`** |

## Control flow (annotated)

```text
SEH (LAB_009bc919)
if node->isnil(+0x31) != 0:
  throw "invalid map/set<T> iterator"  // string @ 0x00a152f0, ThrowInfo DAT_00acc34c
FUN_00408590()                         // in-order successor into out-it shadow
// splice / two-child successor-swap (MSVC erase)
// fix head root / left / right extremities (min/max isnil31)
if erased color black:
  RB rebalance loop (color@+0x30, isnil@+0x31 guards) via L/R rotates
operator_delete(node)
if map->size > 0: map->size--
*outIt = successor
ret 0xC
```

## Peers (do not merge)

| VA | Family |
|---|---|
| `0x004cb740` | isnil@+0x29 erase (thiscall RET8) |
| `0x004094c0` | isnil@+0x2D StringKey erase (stack RET0xC + ~string) |
| `0x00408590` | iterator++ isnil31 (succ helper) |
| `0x00409160` | tree min isnil31 |
| `0x00409180` | buyhead isnil31 size 0x38 |

## Caller

- Sole: `FUN_00407cb0` @ `0x00407d4e` — erase-range while loop (isnil31).
