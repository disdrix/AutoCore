# Annotated low-level: LightMgr_ListRegister_PushBack_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0074e310` |
| VA | `0x0074e310` |
| Canonical name | `LightMgr_ListRegister_PushBack_Inferred` (**Inferred**) |
| Ghidra | `FUN_0074e310` |
| System | Palantir light / effect-list manager |
| Date | 2026-08-04 (W38-T) |
| Body | `0x0074e310`–`0x0074e375` inclusive (**102 B** / `0x66`); `ret 4` |

## Machine-level notes

- Prefer **bytes** over decompiler: Ghidra drops resize size math into bare `FUN_00449440()`.
- Host layout used here: **pointer vector begin@+8, end@+0xC** (same as unregister `FUN_0074e260`).
- Grow path: `lea edx,[this+4]; ecx=size+1; call FUN_00449440` → `FUN_00449560` resizes to N (append).
- Refcount idiom matches `LightMgr_SetActiveSlot3c` (`+4` count; `vtbl[+4]` first-add; `vtbl[+8]` last-release).
- Extra step vs single-slot setter: **`vtbl[+0x30](*this)`** bind after store.
- `param_2` null: AddRef skipped; store null + `vtbl[+0x30]` on null would fault — callers always push non-null cores.

## Annotated semantics

```
// __thiscall host, stack obj*; ret 4; returns 0
void LightMgr_ListRegister_PushBack_Inferred(Host* host, RefCountedVObj* obj)
{
  // vector at host+8/+0xC (helper base host+4 so +4/+8 = begin/end)
  size = (host->begin == 0) ? 0 : (host->end - host->begin) / 4;
  FUN_00449440(/*edx=*/&host->vec_prefix, /*ecx=*/size + 1);  // resize → size+1

  slot = host->end - 1;  // last dword after growth

  if (obj) {
    obj->refcount++;                 // +4
    if (obj->refcount == 1)
      obj->vtbl[1](obj);             // +4
  }

  old = *slot;
  if (old) {
    old->refcount--;
    if (old->refcount == 0)
      old->vtbl[2](old);             // +8
  }

  *slot = obj;
  obj->vtbl[12](*(void**)host);      // +0x30 bind(*host first dword)
  return 0;
}
```

## Open residual

- Product C++ class English for host at `env+4` / Reflect aux.
- Exact product meaning of `vtbl[+0x30]` bind.
- Whether grow zero-inits new slot (release then usually no-op) — inside unowned `FUN_004497d0`.
- Runtime / bit-exact / differential.
