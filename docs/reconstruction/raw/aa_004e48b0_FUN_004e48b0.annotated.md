# Annotated low-level: FUN_004e48b0 → StdMap_InsertOrFind_PairKey_Isnil21_Via4e37e0_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004e48b0` |
| VA | `0x004e48b0`–`0x004e4982` inclusive (**211 B** / `0xD3`); exclusive `0x004e4983` |
| System | skills-abilities / MSVC `std::map` insert-or-find (pair key, isnil@+0x21) |
| Date | 2026-08-05 (R13-005 OWN-ONLY dual; scaffold 2026-07-23) |
| Parent dual | `0x004e37e0` `StdTree_InsertAndRebalance_Val16_Isnil21_Inferred` |
| Peer clone | `0x00407060` `StdMap_InsertOrFind_PairKey_Isnil21_Inferred` (insert via `00407200` — keep separate) |

---

## Machine-level notes

- **ABI:** `__thiscall` ECX = map shell*; stack arg0 = `pair<node*,bool>*` out; stack arg1 = value/key*; **`RET 0x8`**.
- **Family:** isnil@**+0x21**, color@**+0x20**, Val16 payload @+0x10 (node size 0x28 via buynode under insert worker).
- **Key compare:** signed primary dword @ node+0x14 (value+4); unsigned secondary @ node+0x10 (value+0). Same order as dualed peer `00407060` / isnil29 peer `004cbee0` / less-leaf `00401480`.
- **Insert worker:** dualed `FUN_004e37e0` (always-insert + RB rebalance; uniqueness **not** in worker).
- **Predecessor:** residual `FUN_005adff0` when lower-bound went left and `where` is not begin (leftmost).
- **Equal hit:** write `{node, inserted=0}` — **no** mapped-value rewrite.
- **Callers:** repair-station reaction map path + residual `FUN_004da160` (2 UNCONDITIONAL_CALL xrefs).

Prefer assembly when decompiler cursor-aliases `param_3` / `where` (raw reuses `param_3` as where holder).

---

## Pseudocode (annotated)

```c
// StdMap_InsertOrFind_PairKey_Isnil21_Via4e37e0_Inferred
// ECX = map; [esp+4] = out {it, inserted@+4}; [esp+8] = value*
// RET 0x8
void __thiscall FUN_004e48b0(MapShell *map, InsertPair *out, const ValPair *value)
{
  Node *head = map->head;          // [map+4]
  Node *where = head;
  bool goLeft = true;
  char addLeft = 1;
  const ValPair *val = value;

  // lower_bound from root (head->parent); stop at isnil@+0x21
  Node *child = head->parent;      // head[1]
  if (child->isnil == 0) {
    do {
      where = child;
      // go right when node.key <= value.key
      //   signed hi (node+0x14) primary; unsigned lo (node+0x10) secondary
      if (NodeLeKey(where, val)) {
        child = where->right;      // +8
        goLeft = false;
        addLeft = 0;
      } else {
        child = where->left;       // +0
        goLeft = true;
        addLeft = 1;
      }
    } while (child->isnil == 0);
  }

  Node *hint = where;              // decomp reuses param_3 as holder

  if (goLeft) {
    // empty / insert before begin: where == leftmost (*head)
    if (where == head->left) {
      Node *tmp = where;
      Node **pNew = FUN_004e37e0(map, &tmp, /*addLeft*/1, where, val);
      out->it = *pNew;
      out->inserted = 1;           // byte at out+4
      return;
    }
    FUN_005adff0(&hint);           // predecessor for uniqueness check
  }

  // strict node < key → open slot; else equal hit
  if (NodeLtKey(hint, val)) {
    Node *tmp = where;
    Node **pNew = FUN_004e37e0(map, &tmp, addLeft, where, val);
    out->it = *pNew;
    out->inserted = 1;
    return;
  }

  out->inserted = 0;
  out->it = hint;
}
```

---

## Layout

| Offset | Field |
|---|---|
| node+0x00 | left |
| node+0x04 | parent |
| node+0x08 | right |
| node+0x10 | key_lo (unsigned secondary) |
| node+0x14 | key_hi (signed primary) |
| node+0x18..+0x1c | mapped remainder of Val16 |
| node+0x20 | color |
| node+0x21 | isnil |
| map+0x04 | head |
| map+0x08 | size (owned by insert worker) |
| out+0x00 | iterator / node* |
| out+0x04 | inserted (byte) |

---

## Call graph (OWN evidence only)

```text
CVOGReaction_UpdateRepairStationPosition  (0x004d38b0)  @ 004d391f
FUN_004da160                              (0x004da160)  @ 004da224
  └─ FUN_004e48b0  StdMap_InsertOrFind_PairKey_Isnil21_Via4e37e0_Inferred  [OWN R13-005]
        ├─ FUN_004e37e0  StdTree_InsertAndRebalance_Val16_Isnil21_Inferred  [dualed R12-012] ×2
        └─ FUN_005adff0  tree predecessor (residual)

Peer clone (do not merge):
  0x00407060  StdMap_InsertOrFind_PairKey_Isnil21_Inferred  → insert 00407200
  0x004cbee0  StdMap_InsertOrFind_PairKey_Isnil29_Inferred  → isnil@+0x29
```
