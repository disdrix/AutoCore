# Raw capture: FUN_007b6420

| Field | Value |
|---|---|
| **Stable ID** | `aa_007b6420` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x007b6420`–`0x007b644f` (body; next unit @ `0x007b6450`) |
| **Canonical name** | `FUN_007b6420` (inferred: `Path_GetFileName`) |
| **System** | path / ND resource helpers |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
char * FUN_007b6420(void)

{
  char *pcVar1;
  char *pcVar2;
  char *unaff_EDI;
  
  pcVar1 = strrchr(unaff_EDI,0x5c);
  pcVar2 = strrchr(unaff_EDI,0x2f);
  if (pcVar1 == (char *)0x0) {
    if (pcVar2 == (char *)0x0) {
      return unaff_EDI;
    }
  }
  else if (pcVar2 < pcVar1) {
    pcVar2 = pcVar1;
  }
  return pcVar2 + 1;
}
```

---

## Append — dual seal 2026-07-29 (W17-L OWN-ONLY)

| Field | Value |
|---|---|
| **Agent** | W17-L OWN-ONLY dual A/B |
| **Tools** | `decompile_function` + `force_decompile` (identical) + `read_memory` + `get_function_pcode` basic + `analyze_function_complete` + `get_assembly_context` sample call sites. **No** `disassemble_bytes`. |
| **Dual decompile** | Rep1 ≡ Rep2 — third-rep **not required** |
| **Body hex** (`read_memory` @ `0x007b6420`, 48 B to second RET) | `538b1d2c669c00566a5c57ffd36a2f578bf0ffd383c41085f6750985c0750b5e8bc75bc33bf076028bc65e83c0015bc3` |

### Byte-level algorithm (sealed)

```
PUSH EBX
MOV EBX, [0x009c662c]     ; IAT → strrchr
PUSH ESI
PUSH 0x5C                 ; '\\'
PUSH EDI                  ; path (register arg)
CALL EBX                  ; strrchr(path, '\\')
PUSH 0x2F                 ; '/'
PUSH EDI
MOV ESI, EAX              ; ESI = last backslash or NULL
CALL EBX                  ; strrchr(path, '/')
ADD ESP, 0x10             ; cdecl clean both calls
TEST ESI, ESI
JNZ  has_backslash
TEST EAX, EAX
JNZ  only_slash           ; → pop esi; add eax,1; pop ebx; ret
POP ESI
MOV EAX, EDI              ; no separators → return original path
POP EBX
RET
has_backslash:
CMP ESI, EAX              ; max(\, /) by pointer order
JBE  use_eax              ; if ESI <= EAX keep slash result
MOV EAX, ESI              ; else use backslash
use_eax / only_slash:
POP ESI
ADD EAX, 1                ; past separator
POP EBX
RET
```

### ABI (sealed)

- **Input:** path string in **EDI** (register convention; not stack).
- **Return:** `char*` in **EAX** — either original path (no `\`/`/`) or pointer **one past** the last separator.
- **Callees:** `strrchr` only (IAT slot `0x009c662c`).
- **Preserved:** EBX (push/pop). ESI used as temp.
- **Epilogue:** plain `RET` (no RET n).

### Call surface

Inventory **22** xrefs. Named callers include `NDUIWindow_LoadInterfaceFile`, `NDXml_LoadDocumentFromPath`, `FUN_007b6450` (path-exists with search roots). Sample: `LEA EDI,[esp+…]` / `MOV EDI,[ebp+0xc]` then `CALL 0x007b6420`.
