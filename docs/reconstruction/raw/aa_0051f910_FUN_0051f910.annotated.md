# Annotated low-level: LookupRaceShortCode_Inferred (`FUN_0051f910`)

| Field | Value |
|---|---|
| Stable ID | `aa_0051f910` |
| VA | `0x0051f910` |
| Body | `0051f910`–`0051f93b` exclusive (**43 B** / `0x2B`); pad `CC` |
| System | skills-abilities |
| Date | 2026-08-05 (R13-032 dual seal) |
| Role name | `LookupRaceShortCode_Inferred` |
| Rejected alias | `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Named_VOGClient_0051f910` |

---

## Machine-level notes

- **ABI:** cdecl; 1 stack arg `raceId` at `[esp+4]`; plain `RET` (`c3`); EAX = `const char*` (static rodata). **Not** thiscall / stdcall.
- **Leaf:** no callees; pure table.
- **Cascade:** successive `sub/jz` (not jump table):
  - `sub eax,0` / jz → `"HUM"`
  - `sub eax,1` / jz → `"MUT"`
  - `sub eax,1` / jz → `"BIO"`
  - else → `"Unknown"`
- Prefer assembly when decompiler conflicts (Ghidra analyze signature `undefined (void)` is wrong; decompile `char*(int)` is correct).
- Twin of dualed English leaf `LookupRaceDisplayName_Inferred` @ `0x0051f8e0` (same cascade, different strings).
- Shared `"Unknown"` rodata with English twin @ `0x00a37c4c`.
- Contiguous short-code pack: `HUM\0MUT\0BIO\0` @ `0x009cef04`.

## Pseudocode (annotated)

```c
// cdecl; plain RET; static rodata return — do not free
const char * LookupRaceShortCode_Inferred(int raceId)
{
  if (raceId == 0) {
    return "HUM";      // 0x009cef04 — Human
  }
  if (raceId != 1) {
    if (raceId != 2) {
      return "Unknown"; // 0x00a37c4c — shared with English twin
    }
    return "BIO";       // 0x009cef0c — Biomek
  }
  return "MUT";         // 0x009cef08 — Mutant
}
```

## Callers

| From | Function | Use |
|---|---|---|
| `0x00871273` | `FUN_00871150` | `sprintf("%d/%s/%s", level, classShort, raceShort)` after `FUN_0051fa10` |

Packed host byte `@EBP+0x1e`: high nibble = class (for class short), low nibble = race id for this leaf; level `@EBP+0x1f`.

## Open questions

- Product/PDB method symbol English (role sealed → `_Inferred`).
- Host English name for `FUN_00871150` (VOG/UI panel residual).
- Dual of class-short peer `FUN_0051fa10` (not OWN).
- Runtime / bit-exact / differential.
