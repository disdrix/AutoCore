# Annotated low-level: FUN_0058e270 / NDSpecialFX_Child_ApplyFxToggle_SlotFilter_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0058e270` |
| VA | `0x0058e270`–`0x0058e2f1` (129 B) |
| System | client-fx / NDSpecialFX child FX toggle (slot-filtered) |
| Date | 2026-07-29 (W26-K dual seal) |
| Dual | `reviews/A_aa_0058e270_*` + `reviews/B_aa_0058e270_*` |

## Machine-level notes

- Source: raw + live `decompile_function` / `read_memory` (no `disassemble_bytes`).
- Prefer bytes for ECX of `0096d*` helpers and `ret 0x0C`.
- Sibling unrestricted unit: `FUN_0058e300` (gate `child+0x25c < 0` only; no slot match).
- Prior scaffold alias `Named_CalleeOf_…_VEHICLE_EXHAUST_…` is **too narrow** — exclusive caller is generic `004b7030`.

## Child layout (this / ESI)

| Offset | Role | Conf |
|---|---|---|
| `+0x25c` | Slot id (int); `-1` = no valid slot / skip | **Confirmed** |
| `+0x258` (600) | Handle/key for start query + attach | **Confirmed** |
| `+0x254` | Third attach arg | **Confirmed** |
| `+0x914` | Particle/link handle for stop + attach | **Confirmed** |
| `+0x90f` bit0 | Active / linked flag (`\|=1` on start success, `&=~1` otherwise) | **Confirmed** |
| `+0x90e` bit7 | Gate on caller `004b7030` (not tested here) | Peer sealed |

## Formals

| Formal | Storage | Role | Conf |
|---|---|---|---|
| `this` / child | ECX | NDSpecialFX-owned child | **Confirmed** |
| `param_2` effect_start | Stack[0x4] | Non-null → start path; **ECX this** for `0096d460`/`0096dc80` | **Confirmed** |
| `param_3` effect_stop | Stack[0x8] | Non-null → stop path; **ECX this** for `0096d550` | **Confirmed** |
| `param_4` slot_filter | Stack[0xC] low byte | `-1`/0xFF wild; else match `+0x25c` via movsx | **Confirmed** |

Naming note: upstream duals call these `want_start` / `want_stop`. Bytes show they are **object pointers** when non-zero (used as this for particle helpers), not mere boolean flags — truthiness gates the path; non-null value is the effect/controller object.

## Pseudocode (annotated)

```c
// thiscall; ret 0x0C
void __thiscall FUN_0058e270(
    void *child,
    void *effect_start,   /* param_2 — may be 0 */
    void *effect_stop,    /* param_3 — may be 0 */
    int slot_filter)      /* low byte compared; -1 wild */
{
  unsigned char sf = (unsigned char)slot_filter;
  int child_slot = *(int *)((char *)child + 0x25c);

  if ((sf == 0xFF || child_slot == (int)(signed char)sf) && child_slot != -1) {
    if (effect_stop != 0) {
      /* ECX = effect_stop; push *(child+0x914) */
      FUN_0096d550(/*this=*/effect_stop, *(uint32_t *)((char *)child + 0x914));
    }
    if (effect_start != 0) {
      /* ECX = effect_start; push *(child+0x258) */
      if (FUN_0096d460(/*this=*/effect_start,
                       *(uint32_t *)((char *)child + 0x258)) == 0) {
        FUN_0096dc80(/*this=*/effect_start,
                     *(uint32_t *)((char *)child + 0x258),
                     *(uint32_t *)((char *)child + 0x914),
                     *(uint32_t *)((char *)child + 0x254));
        *(unsigned char *)((char *)child + 0x90f) |= 1;
        return;
      }
    }
    *(unsigned char *)((char *)child + 0x90f) &= 0xFE;
  }
}
```

## Call graph

| Dir | VA | Role |
|---|---|---|
| Caller | `0x004b7030` | Exclusive; bit7 children; passes `param_5` as slot |
| Callee | `0x0096d550` | Detach/stop (peer: Respawn dtor uses as unlink) |
| Callee | `0x0096d460` | Query existing link; 0 → need attach |
| Callee | `0x0096dc80` | Attach/start particle link |

## Open questions

- Product method name / C++ type of child and effect objects.
- Full dual of `0096d550` / `0096d460` / `0096dc80` (out of OWN).
- Whether slot_filter high bytes ever matter (bytes only use low byte + movsx).
- Runtime / bit-exact.
