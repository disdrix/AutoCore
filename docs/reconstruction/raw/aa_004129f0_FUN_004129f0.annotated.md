# Annotated: Map_Tree_Predecessor_Isnil15_Edx (FUN_004129f0)

| Field | Value |
|---|---|
| Stable ID | `aa_004129f0` |
| VA | `0x004129f0` |
| Sealed name | `Map_Tree_Predecessor_Isnil15_Edx` |
| System | std red-black tree / map iterator (isnil@+0x15) |
| Date | 2026-08-04 (W38-M) |

## Machine-level notes

- **ABI:** `EDX = Node**`; no stack args; bare `C3`; void.
- **Leaf:** no callees.
- **isnil** probed as `cmp byte ptr [reg+0x15], 0` throughout.
- Links: left `[n]`, parent `[n+4]`, right `[n+8]`.
- Prefer bytes over decompiler for register (EDX vs invented ECX/fastcall pair).
- Peer ECX twin: `Map_Tree_Predecessor_Isnil15` @ `0x005a2850`.

## Pseudocode (role-named)

```c
// Retail: EDX = it
void Map_Tree_Predecessor_Isnil15_Edx(Node** it /*EDX*/)
{
  Node* cur = *it;
  if (cur->isnil != 0) {
    *it = cur->right;
    return;
  }
  Node* left = cur->left;
  if (left->isnil == 0) {
    Node* p = left;
    while (p->right->isnil == 0)
      p = p->right;
    *it = p;
    return;
  }
  Node* parent = cur->parent;
  if (parent->isnil == 0) {
    while (parent->isnil == 0 && *it == parent->left) {
      *it = parent;
      parent = parent->parent;
    }
    if (parent->isnil == 0)
      *it = parent;
  }
}
```

## Open questions

- Product / MSVC demangle (`_Tree_unchecked_const_iterator::operator--`).
- Full value payload / color layout per caller map.
- Runtime / bit-exact / differential.
