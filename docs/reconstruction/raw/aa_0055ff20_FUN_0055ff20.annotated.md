
# Annotated low-level: PhysMgr_AttachBody_Inferred (FUN_0055ff20)

| Field | Value |
|---|---|
| Stable ID | `aa_0055ff20` |
| VA | `0x0055ff20`–`0x00560017` (**248 B**) |
| System | phys / manager body attach |
| Date | 2026-07-29 W29-R |
| Ghidra | `FUN_0055ff20` |
| Canonical | `PhysMgr_AttachBody_Inferred` |

---

## Machine-level notes

- **thiscall** ECX=manager; stack=body; returns body in EAX; **`ret 4`**.
- Frame: `PUSH EBP; MOV EBP,ESP; AND ESP,~0xF; SUB ESP,0x34; PUSH EBX/ESI/EDI`.
- `body[+8] = manager` (param_2[2]).
- Local worklist init `FUN_0062d960` from `*(mgr+0x234)` (sibling of bind/unbind).
- Body `vtbl+0x18` then mgr `[+0xc4] vtbl+4` with null-safe `body+0x1c` collect link pairs into worklist.
- Serial stamp: `body[+0x20] = mgr[+0x130]++`.
- If pair count ≠ 0: CanonAndSort left; SortedDiffWalk(left,n, empty,0) with ECX=`*(mgr+0x138)`.
- Short refcount at `body+6` incremented.
- Body pointer vector at `mgr+0x120` / count `+0x124` / cap `+0x128` (high bit masked); grow via `FUN_005b3370(vec, 4)`.
- `FUN_0062a4d0(manager, body)` notifies host action list (`mgr+0x54`/`+0x58`).
- Always `LocalWorklist_Release` (`FUN_0055f4c0`).
- Twin detach: `FUN_00560020` (W29-S).

## Pseudocode (annotated)

```c
// thiscall; ret 4; returns body
int * PhysMgr_AttachBody_Inferred(int manager /*ECX*/, int *body /*stack*/)
{
  body[2] = manager;
  // LocalWorklist_Init(*(manager+0x234))
  // body->vtbl+0x18(work_buf)
  // slot = (body == -0xC) ? 0 : body+0x1C
  // *(manager+0xc4)->vtbl+4(slot, outs…)
  // body[8] = manager[+0x130]++
  // if count: CanonAndSort; SortedDiffWalk(new, n, empty, 0, ctx)
  // *(short*)(body+6)++
  // grow mgr body list if full; push body; count++
  // FUN_0062a4d0(manager, body)
  // LocalWorklist_Release
  // return body
}
```

## Open / residual

- Product body / manager class English.
- Full layout of body beyond sealed offsets.
- Runtime / bit-exact open.
