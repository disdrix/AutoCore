# Annotated low-level: Object_AggroMap_DecayOrPurge_Inferred (FUN_004c8610)

| Field | Value |
|---|---|
| Stable ID | `aa_004c8610` |
| VA | `0x004c8610`–`0x004c8779` exclusive (**361 B**) |
| System | Object-hosted isnil29 map @ +0x154 (aggro / threat) |
| Date | 2026-08-04 WQ9F-B OWN dual |
| Canonical | `Object_AggroMap_DecayOrPurge_Inferred` |

## Machine-level notes

- `MOV EDI,ECX` thiscall; map head `*(this+0x158)`; inorder walk until back to head.
- Every visit: `score = (int)((float)score * 0.85f)` at node+0x20 (`CVTSI2SS` / `MULSS [DAT_00aaa680]` / `CVTTSS2SI`).
- **Decay mode** (`forcePurge == 0`):
  - if scaled score **≠** old → **keep** node; advance inorder successor (isnil@+0x29 walks).
  - if scaled == old (fixed point; practically **score was 0**) → fall through to purge path.
- **Purge mode** (`forcePurge != 0`) or zero residual:
  - if node TFID (`+0x10/+0x14/+0x18`) equals host MI primary identity (`adj+0x44/+0x48/+0x4c`) → call `vtbl+0x258` with `DAT_009cb8c0` (clear selected target pattern).
  - compute successor; `StdTree_EraseAndRebalance_Isnil29_Inferred(map@+0x154, &outIt, node)`.
- Erase call site: `LEA ECX,[EDI+0x154]`; `PUSH node`; `PUSH &local_out`; `CALL 0x004cb740`.
- Epilogue: `ADD ESP,0x10; RET 4`.
- Peer `FUN_004c8780` (WQ9F-C OWN residual): same map; resolves `Object_ResolveFromTFID((TFID_16*)(node+0x10))` — seals key English as TFID; this unit is decay/purge, not resolve.
- Sole caller `FUN_00638ac0`: AI timer (`0x5dc` = 1500) → `FUN_004c8610(0)` on owner context.

## Pseudocode (annotated)

```c
// __thiscall RET 4
void Object_AggroMap_DecayOrPurge_Inferred(Object* self, char forcePurge)
{
  MapShell* map = (MapShell*)(self + 0x154); // head at self+0x158
  Node* it = map->head->left; // **head in decomp = inorder begin
  // retail: piVar7 = *head; walk until it == head
  Node* head = *(Node**)(self + 0x158);
  Node* cur = head->left_or_begin; // decomp: *head
  // actual: cur = *head; while cur != head
  for (Node* cur = (Node*)*head; cur != head; ) {
    int oldScore = cur->score;           // +0x20
    int newScore = (int)((float)oldScore * 0.85f);
    cur->score = newScore;
    Node* next = cur;
    if (forcePurge == 0 && newScore != oldScore) {
      next = InorderSuccessor_Isnil29(cur);
    } else {
      if (TfidEq(cur->tfid, HostPrimaryTfid(self))) {
        Host_ClearSelectedTarget_Vtbl258(self, kNullTfid_DAT_009cb8c0);
      }
      next = InorderSuccessor_Isnil29(cur);
      StdTree_EraseAndRebalance_Isnil29_Inferred(map, &outIt, cur);
      // outIt may be ignored; walk continues from precomputed next
    }
    cur = next;
  }
}
```

Note: successor is computed **before** erase so the walk remains valid (classic map erase-while-iterate).

## Open questions

- Product method English on CVOGObject / HBAI owner.
- Exact TFID field widths beyond peer `TFID_16` cast.
- Whether forcePurge!=0 has live callers in this image (xrefs only show decay-0).
- Runtime / bit-exact / differential.
