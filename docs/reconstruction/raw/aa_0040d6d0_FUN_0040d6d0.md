# Raw capture: FUN_0040d6d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0040d6d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0040d6d0` |
| **Canonical name** | `FUN_0040d6d0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0040d6d0(int param_1)

{
  int local_4;
  
  local_4 = param_1;
  FUN_0040d700(&local_4,**(undefined4 **)(param_1 + 4),*(undefined4 **)(param_1 + 4));
                    /* WARNING: Subroutine does not return */
  operator_delete(*(void **)(param_1 + 4));
}
```

---

## W33-A re-verify append (2026-07-29)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual W33-A |
| **Tools** | `decompile_function` + `analyze_function_complete` + `read_memory` + xrefs. **No** `disassemble_bytes` |
| **Live decompile** | ≡ scaffold 2026-07-23 body (erase full + delete head) but **false noreturn truncates epilogue** |
| **Body** | `0x0040d6d0`–`0x0040d6fe` exclusive (**46 B** / `0x2E`); pad `CC` |
| **ABI** | **ECX=`tree`/map facade** (`head@+4`, `size@+8`); bare **`RET`** (`C3`); void |
| **Callees** | `FUN_0040d700` (`StdTree_EraseRange_Val12`), `operator_delete` |
| **Callers** | Unwind `@009bda70` → `JMP` to this (SEH unwind tidy) |
| **Layout** | same Val12 facade as W32-B `aa_0040d7c0` |
| **Name (Inferred)** | `StdTree_Tidy_FreeHead_Bare_Val12` — bare (no SEH) twin of `StdTree_Tidy_FreeHead_Val12` @ `0x0040d7c0` |

### Full-body hex (`read_memory`, 46 B)

```
51568bf18b46048b0850518d44240c508bcee8190000008b4e0451e832c1070083c40433c08946048946085e59c3
```

### Byte-sealed facts

1. `ESI = ECX` (tree). `push ECX` allocates out-iter slot (initial value = this; **unused as value** — only written by erase).
2. Full-range erase: `FUN_0040d700(tree, &out, *head /*left*/, head)` — `E8` → `0x0040d700`.
3. `operator_delete(head)` cdecl + `ADD ESP,4` — decompiler “does not return” is **false**; bytes continue.
4. `head = 0; size = 0` (`XOR EAX,EAX; MOV [ESI+4],EAX; MOV [ESI+8],EAX`).
5. `POP ESI; POP ECX; RET` (`C3`). No SEH frame (unlike peer `0040d7c0` 92 B with `LAB_009bda78`).
6. Classification: worker. Peer: W32-B `StdTree_Tidy_FreeHead_Val12` (SEH + same tidy order).
