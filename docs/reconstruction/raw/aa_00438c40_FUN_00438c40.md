# Raw capture: FUN_00438c40

| Field | Value |
|---|---|
| **Stable ID** | `aa_00438c40` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00438c40` |
| **Canonical name** | `FUN_00438c40` |
| **System** | arda2/storage (unserialization factory map A) |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined * FUN_00438c40(void)

{
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  puStack_8 = &LAB_009bcc4d;
  local_c = ExceptionList;
  if ((DAT_00d1fd40 & 1) == 0) {
    DAT_00d1fd40 = DAT_00d1fd40 | 1;
    local_4 = 0;
    ExceptionList = &local_c;
    FUN_00457ac0(&DAT_00d1fd18);
    _atexit((_func_4879 *)&LAB_009c3310);
  }
  ExceptionList = local_c;
  return &DAT_00d1fd18;
}
```

---

## W35-D re-verify (2026-07-29) — append only

| Field | Value |
|---|---|
| **Tools** | `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + `get_function_by_address` + `get_function_xrefs`. **No** `disassemble_bytes`. |
| **Body** | `0x00438c40`–`0x00438c99` (**90 B** / `0x5A`); pad `CC` then twin `FUN_00438ca0` @ `0x00438ca0`. |
| **Classification** | worker (Meyers singleton getter) |
| **ABI** | **cdecl**, `parameters: []`, plain `RET` after `ADD ESP,0xC`. Returns pointer in **EAX**. |
| **Live decompile** | Identical CF to raw (once-flag bit0, ctor, atexit, return static). |
| **Callees** | `FUN_00457ac0` (`NestedHash_Ctor_Sentinel0x10_Inferred`), `_atexit` |
| **Callers (2)** | `FUN_004449b0` @ `0x004449b5`/`bd`/`ed` (Sto AbstractUnserializationFactory insert); `FUN_00437b00` @ `0x00437b0f`/`23` (factory lookup/invoke). **5** call sites total. |
| **Static bag** | `DAT_00d1fd18` — NestedHash span **0x28** (`00d1fd18`…`00d1fd3f`) |
| **Once-flag** | `DAT_00d1fd40` bit0 (`TEST`/`OR` with `1`) |
| **SEH** | `LAB_009bcc4d` |
| **atexit thunk** | `LAB_009c3310`: `MOV EAX, 0x00d1fd18; JMP 0x004493eb` → lands on **`RET 4`** epilogue of `FUN_004493d0` (effectively **no-op** NestedHash free at process exit). |
| **Decompile ≡ bytes** | **Yes** for once-flag, push-static, call ctor, atexit, `MOV EAX, imm32 &DAT_00d1fd18`, plain RET. |
| **Inferred name** | `Sto_UnserialFactoryMapA_GetInstance_Inferred` — reject scaffold `Named_CalleeOf_*_gfxBody_*`. |

### Full body hex (90 B)

```
64a1000000006aff684dcc9b0050b801000000840540fdd100648925000000007525090540fdd1006818fdd100c744240c00000000e846ee01006810339c00e8520c050083c4048b0c24b818fdd10064890d0000000083c40cc3
```

### atexit thunk hex (`read_memory` @ `0x009c3310`, 10 B + pad)

```
b818fdd100 e9d660a8ff   ; MOV EAX,&DAT_00d1fd18 ; JMP 0x004493eb (RET 4)
```

### Call target checks (bytes)

| Site | Rel | Target |
|---|---|---|
| `E8` @ `0x00438c75` | `+0x0001ee46` | `0x00457ac0` NestedHash ctor |
| `E8` after push atexit | (CRT) | `_atexit` |
