# Raw capture: FUN_00522000

| Field | Value |
|---|---|
| **Stable ID** | `aa_00522000` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00522000` |
| **Canonical name** | `FUN_00522000` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
byte __fastcall FUN_00522000(int param_1)

{
  byte bVar1;
  
  bVar1 = (*(byte *)(param_1 + 0x59a) >> 1) + 3;
  if (7 < bVar1) {
    bVar1 = 8;
  }
  return bVar1;
}
```

---

## Re-verify (MEGA-100, 2026-08-05)

| Field | Value |
|---|---|
| **Tools** | Ghidra HTTP `decompile_function` + `force_decompile` + `analyze_function_complete` + `disassemble_function` + `get_function_callers` / `get_function_callees` + `get_xrefs_to` + `read_memory` + call-site `read_memory`. **No** `disassemble_bytes`. |
| **Body** | `0x00522000`–`0x00522016` inclusive (**23 B** / `0x17`); pad `CC` to sibling `FUN_00522020` @ `0x00522020` |
| **Prologue/body hex (23 B)** | `0fb6819a050000d1e883c00383f8087c05b808000000c3` |
| **force ≡ batch decompile** | Yes — identical CF |
| **Callees** | none (leaf) |
| **Callers (5 named)** | `FUN_008b2f00`, `FUN_008b3400`, `FUN_008b3670`, `FUN_008e5ce0` (dualed parent), `FUN_008e5e00` |
| **Xrefs** | **7** UNCONDITIONAL_CALL: `008b2f21`, `008b31f7`, `008b343c`, `008b369d`, `008e5d80`, `008e61c4`, `008e83c8` |

### Live decompile (force, 2026-08-05)

```c
byte __fastcall FUN_00522000(int param_1)

{
  byte bVar1;
  
  bVar1 = (*(byte *)(param_1 + 0x59a) >> 1) + 3;
  if (7 < bVar1) {
    bVar1 = 8;
  }
  return bVar1;
}
```

### Live disassembly (`disassemble_function`)

```text
00522000: MOVZX EAX,byte ptr [ECX + 0x59a]
00522007: SHR   EAX,0x1
00522009: ADD   EAX,0x3
0052200c: CMP   EAX,0x8
0052200f: JL    0x00522016
00522011: MOV   EAX,0x8
00522016: RET
```

### ABI seal (bytes + call sites)

- **ECX** = Character* (`this`); decompiler labels as `param_1` / `__fastcall`.
- **No stack formals**; **bare RET** (not `RET n`).
- **EAX** = table count in `[3..8]` (typically).
- All sampled call sites load **`mov ecx, dword ptr [DAT_00d1b6d8]`** immediately before `call`:
  - `008e5d80` (parent `FUN_008e5ce0`): `8B 0D D8 B6 D1 00` then `E8 …`
  - `008e61c4` (`FUN_008e5e00`): same
  - `008b2f21`, `008b343c`, `008b369d`, `008b31f7`, `008e83c8`: same ECX global load
- Null-character branches at UI sites (`008b3400` / `008b3670` / `008b31f7`) store **literal 8** as the loop bound instead of calling — matches clamp max.

### Formula seal

```text
count = ( *(uint8_t*)(character + 0x59a) >> 1 ) + 3
if (count >= 8) count = 8   // cmp eax,8 ; jl keep ; mov eax,8
return count                // EAX
```

Decompiler `if (7 < bVar1)` ≡ assembly `cmp/jl` clamp-at-8.

### Role evidence (callers, not owned)

Every named caller uses return value as **upper bound** over equip TFID pairs at **`character+0x5a0`**, stride **8** (`lo` @ +0x5a0+i*8, `hi` @ +0x5a4+i*8), often via `CVOGReaction_ResolveObjectTarget(1, lo, hi)`.

### analyze_function_complete (summary)

| Field | Value |
|---|---|
| classification | leaf |
| xref_count | 7 |
| callees | [] |
| callers | FUN_008b3400, FUN_008b3670, FUN_008e5ce0, FUN_008e5e00, FUN_008b2f00 |
| return_type_warning | Ghidra listing `undefined` / void display untrusted — EAX is live count |

