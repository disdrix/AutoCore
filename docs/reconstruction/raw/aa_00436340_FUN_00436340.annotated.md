# Annotated low-level: FUN_00436340

| Field | Value |
|---|---|
| Stable ID | `aa_00436340` |
| VA | `0x00436340` |
| Body span | `00436340` – `004363aa` |
| System | container / circular pointer-buffer utility |
| Inferred role | `CircularPtrBuf_PushCopy_0x14` (leave Ghidra `FUN_*` as canonical) |
| Date | 2026-07-29 (OWN dual W16-H sealed) |

---

## Machine-level notes

- **ABI:** buffer object in **EAX** (`this`), source element in **EDI** (5×`uint32` / **0x14** bytes). No stack args. Plain **`ret`**. Void return.
- **Layout of buffer object** (this unit):

| Offset | Role | Conf |
|---|---|---|
| `+0x00` | unused here | — |
| `+0x04` | `void **` slot pointer array | **High** |
| `+0x08` | `uint capacity` (slot count) | **High** |
| `+0x0c` | `int head` index | **High** |
| `+0x10` | `int count` live elements | **High** |

- **Write index:** `(head + count) mod capacity` (implemented as add then subtract capacity if `capacity <= index`).
- **Grow:** if `capacity <= count + 1`, call **`FUN_00436650(1)`** with **EBX=this** (circular reallocate; residual callee — not dual-owned). Leaves at least one free slot after push.
- **Lazy element alloc:** if `slots[writeIndex] == null`, `operator_new(0x14)` and store pointer. If still null (OOM), **skip copy** but still **`count++`** (can leave a null slot counted).
- **Copy:** five dword moves `*dest = *src` … no constructors/destructors.
- Multi-domain utility (nearby range mixes sto-chunk, gfx, vector helpers). **No product string** on this VA — keep `FUN_00436340`.

## Pseudocode (assembly-faithful)

```c
// EAX = CircularPtrBuf* this; EDI = const uint32_t src[5]
void FUN_00436340(void /* in_EAX this, unaff_EDI src */)
{
  int this = in_EAX;
  uint writeIndex;
  uint32_t *elem;

  if (*(uint *)(this + 8) <= (uint)(*(int *)(this + 0x10) + 1)) {
    FUN_00436650(1);   // EBX = this
  }

  writeIndex = *(int *)(this + 0xc) + *(int *)(this + 0x10);
  if (*(uint *)(this + 8) <= writeIndex) {
    writeIndex -= *(uint *)(this + 8);
  }

  if (*(int *)(*(int *)(this + 4) + writeIndex * 4) == 0) {
    void *p = operator_new(0x14);
    *(void **)(*(int *)(this + 4) + writeIndex * 4) = p;
  }

  elem = *(uint32_t **)(*(int *)(this + 4) + writeIndex * 4);
  if (elem != 0) {
    elem[0] = unaff_EDI[0];
    elem[1] = unaff_EDI[1];
    elem[2] = unaff_EDI[2];
    elem[3] = unaff_EDI[3];
    elem[4] = unaff_EDI[4];
  }

  *(int *)(this + 0x10) = *(int *)(this + 0x10) + 1;
}
```

## Open questions

- Product / STL name of the container (custom ring of heap POD nodes vs game-specific queue).
- Element struct identity of the **0x14**-byte payload (callers not dual-owned).
- Whether counted-null after `operator_new` failure is reachable / relied upon.
- Runtime / differential open.
