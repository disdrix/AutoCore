# Raw capture: FUN_00438ca0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00438ca0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00438ca0` |
| **Canonical name** | `FUN_00438ca0` |
| **System** | arda2/storage (unserialization factory map B) |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined * FUN_00438ca0(void)

{
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  puStack_8 = &LAB_009bcc2d;
  local_c = ExceptionList;
  if ((DAT_00d1fd14 & 1) == 0) {
    DAT_00d1fd14 = DAT_00d1fd14 | 1;
    local_4 = 0;
    ExceptionList = &local_c;
    FUN_00457ac0(&DAT_00d1fcec);
    _atexit((_func_4879 *)&LAB_009c3300);
  }
  ExceptionList = local_c;
  return &DAT_00d1fcec;
}
```

---

## W35-D re-verify (2026-07-29) — append only

| Field | Value |
|---|---|
| **Tools** | `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + `get_function_by_address` + `get_function_xrefs`. **No** `disassemble_bytes`. |
| **Body** | `0x00438ca0`–`0x00438cf9` (**90 B** / `0x5A`); pad `CC` then next. |
| **Classification** | worker (Meyers singleton getter) |
| **ABI** | **cdecl**, `parameters: []`, plain `RET` after `ADD ESP,0xC`. Returns pointer in **EAX**. |
| **Live decompile** | Identical CF to raw (once-flag bit0, ctor, atexit, return static). |
| **Callees** | `FUN_00457ac0` (`NestedHash_Ctor_Sentinel0x10_Inferred`), `_atexit` |
| **Callers (2)** | `FUN_00444a40` @ `0x00444a45`/`4d`/`7d` (Sto AbstractUnserializationFactory insert sibling); `FUN_00437c90` @ `0x00437c9f`/`b3` (factory lookup/invoke sibling). **5** call sites total. |
| **Static bag** | `DAT_00d1fcec` — NestedHash span **0x28** (`00d1fcec`…`00d1fd13`) |
| **Once-flag** | `DAT_00d1fd14` bit0 (`TEST`/`OR` with `1`) |
| **SEH** | `LAB_009bcc2d` |
| **atexit thunk** | `LAB_009c3300`: `MOV EAX, 0x00d1fcec; JMP 0x004493eb` → same **`RET 4`** no-op as MapA. |
| **Decompile ≡ bytes** | **Yes** for once-flag, push-static, call ctor, atexit, `MOV EAX, imm32 &DAT_00d1fcec`, plain RET. |
| **Twin** | Byte-identical shape to `FUN_00438c40` except SEH/flag/static/atexit immediates + call relocs. |
| **Inferred name** | `Sto_UnserialFactoryMapB_GetInstance_Inferred` — reject scaffold `Named_CalleeOf_*_gfxBody_*`. |

### Full body hex (90 B)

```
64a1000000006aff682dcc9b0050b801000000840514fdd100648925000000007525090514fdd10068ecfcd100c744240c00000000e8e6ed01006800339c00e8f20b050083c4048b0c24b8ecfcd10064890d0000000083c40cc3
```

### atexit thunk hex (`read_memory` @ `0x009c3300`, 10 B + pad)

```
b8ecfcd100 e9e660a8ff   ; MOV EAX,&DAT_00d1fcec ; JMP 0x004493eb (RET 4)
```

### Call target checks (bytes)

| Site | Rel | Target |
|---|---|---|
| `E8` @ `0x00438cd5` | `+0x0001ede6` | `0x00457ac0` NestedHash ctor |
| `E8` after push atexit | (CRT) | `_atexit` |

### BSS adjacency (context; not owned)

```
DAT_00d1fcc0  NestedHash C (FUN_0044fee0 / W35-E)
DAT_00d1fce8  once C
DAT_00d1fcec  NestedHash B  ← this getter
DAT_00d1fd14  once B
DAT_00d1fd18  NestedHash A  ← twin 0x00438c40
DAT_00d1fd40  once A
```
