# Annotated low-level: Character_Craft_HasMatchingStationInRange_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0052eb90` |
| VA | `0x0052eb90`–`0x0052ed73` exclusive (**483 B** / `0x1E3`) |
| Ghidra | `FUN_0052eb90` |
| Inferred | `Character_Craft_HasMatchingStationInRange_Inferred` |
| System | inventory-transfer — craft / reverse-engineer station proximity gate |
| Date | 2026-08-05 (R12-021 dual seal; scaffold 2026-07-23) |

## Machine-level notes

- **ABI:** thiscall; **ECX** = character (`DAT_00d1b6d8` at all live callers); stack **craft/target object***; **AL** bool; **`RET 0x4`**.
- **Bypass:** `character+0x6b4 > 0` → true (same free/RE-open flag family as RE required-level zero path).
- **Discipline miss:** `DisciplineDefMap_TryCopyPayloadById` miss → true (no station requirement row).
- **Range:** surface distance ≤ **`DAT_00a0f524` = 50.0f** via dualed `Object_SurfaceDistance3D_Inferred`.
- **Station match:** nearby object clonebase short `+0x3f4` ∈ {`0xD`,`0xE`,`0xF`} and matching DisciplineDef payload flag at buffer `+0x1F0/+0x1F4/+0x1F8` non-zero.
- **List source:** character virtual-base object at MI `+0xa8`; TFID vector at that object `+0xe7f8` (via `FUN_004e5810`); resolve ctx `+0xe4e8`.
- Prefer assembly when decompiler return width / stack-slot names conflict.

## Pseudocode (annotated)

```c
// bool __thiscall Character_Craft_HasMatchingStationInRange_Inferred(
//     Character* self /*ECX*/, Object* craftObject /*stack*/)
// RET 0x4; AL in {0,1}

uint8_t __thiscall FUN_0052eb90(int self, int craftObject)
{
  // MI-adjusted object pointer at self+0xa8 (via vbtable *( *(self+4)+4 ))
  int *charObj = *(int **)( *( *(self + 4) + 4 ) + self + 0xa8 );

  if (charObj == 0 || craftObject == 0)
    return 0;

  // Free / RE-open bypass (also short-circuited by some callers before CALL)
  if (*(int *)(self + 0x6b4) > 0)
    return 1;

  // DisciplineDef payload 0x1FC keyed by craftObject clonebase+0x4ac
  uint8_t discBuf[0x1FC];
  int disciplineId = *(int *)( *(*(craftObject + 0xa8) + 0x3c) + 0x4ac );
  if (!DisciplineDefMap_TryCopyPayloadById_Inferred(disciplineId, discBuf))
    return 1; // no row → gate open

  // Tail flags for station subtypes 0xD / 0xE / 0xF
  int flagD = *(int *)(discBuf + 0x1F0);
  int flagE = *(int *)(discBuf + 0x1F4);
  int flagF = *(int *)(discBuf + 0x1F8);

  for (unsigned idx = 0; ; ++idx) {
    // Materialize TFID vector at charObj+0xe7f8
    // count = (end - begin) / 8
    // resolve each via CVOGReaction_ResolveObjectTarget(charObj+0xe4e8, lo, hi)
    ...
    if (idx >= count)
      return 0;

    Object *near = Resolve(...);
    if (near == 0 || near->vtbl[0x1c8/4]() == 0)
      continue;

    void *poseB = near->vtbl[0x1c8/4]();
    void *poseA = char_subobj->vtbl[0x19c/4](poseB); // this = MI subobject at self+4 chain
    float dist = Object_SurfaceDistance3D_Inferred(poseA, poseB);
    if (dist > 50.0f)
      continue;

    short subtype = *(short *)( *( *(near + 0xa8) + 0x3c ) + 0x3f4 );
    if (subtype == 0xD && flagD != 0) return 1;
    if (subtype == 0xE && flagE != 0) return 1;
    if (subtype == 0xF && flagF != 0) return 1;
  }
}
```

## Callers (sealed)

| Site | Host role |
|---|---|
| Craft CreateChildWidgets `008e6b50` | `host+0x7e2 = result` |
| Craft host refresh / tick | same gate byte / enable flag |
| `UI_BuildItemTooltipStats` | tooltip greyscale when false |

## Confidence

| Claim | Level |
|---|---|
| CF + thiscall RET 4 + bool AL | **High** |
| 50.0f distance gate | **High** (`read_memory`) |
| DisciplineDef try-copy + tail flags | **High** (dualed `0052b040`) |
| Station subtype 0xD/0xE/0xF product English | **Open** |
| `+0x6b4` product English | **Open** (RE-free family Probable) |
| Runtime / bit-exact | **Open** |
