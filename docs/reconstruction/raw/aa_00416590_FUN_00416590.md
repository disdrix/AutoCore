# Raw capture: FUN_00416590

| Field | Value |
|---|---|
| **Stable ID** | `aa_00416590` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00416590` |
| **Canonical name** | `FUN_00416590` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00416590(undefined4 *param_1)

{
  *param_1 = 0;
  param_1[2] = 0;
  param_1[3] = 0;
  param_1[4] = 0;
  return;
}
```

---

## WQ9I-J re-verify append (2026-08-05)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **WQ9I-J** |
| **Tools** | `batch_decompile` / `analyze_function_complete` + `read_memory` + xrefs + `get_assembly_context`. **No** `disassemble_bytes` |
| **Live decompile** | ≡ scaffold 2026-07-23 CF (zero `*p`, `p[2]`, `p[3]`, `p[4]`; **skip `p[1]` / +4**) |
| **Body** | `0x00416590`–`0x004165CF` exclusive (**63 B** / `0x3F`); pad `CC` |
| **ABI** | **ECX = this** (elem*); **no stack args**; plain **`RET`** (`C3`); void |
| **SEH** | Prologue installs `LAB_009bcfab`; EH state stays `0xFFFFFFFF` (no live cleanup during body); frame tear `ADD ESP,0x10` + `C3` |
| **Semantics** | Default-ctor for **0x14-byte** pointer-vector shell: zero `+0`, `+8`, `+0xC`, `+0x10`; **leave `+4` untouched** |
| **Elem size seal** | Parent `_eh_vector_constructor_iterator_(base, 0x14, 9, FUN_00416590, FUN_00417ee0)` @ `FUN_00820830` (`LEA …+0xEE4`, count 9) |
| **Callees** | none (leaf) |
| **Xrefs** | DATA: `0x0082085d` (`PUSH 0x416590` ctor ptr); CODE CALL: `0x009bf3c5` (`ECX=0xd218e0`), `0x009bf5f5` (`0xd218f4`), `0x009bf615` (`0xd21908`), `0x009bf635` (`0xd2191c`) — four BSS shells stride **0x14**, each followed by `_atexit` |
| **Elem dtor peer (not owned)** | `FUN_00417ee0` — same SEH plate `LAB_009bcfab`; if `*(this+8)!=0` `operator_delete`; zero triad `+8/+0xC/+0x10` |
| **Name (Inferred)** | `PtrVecShell0x14_DefaultCtor_ZeroSkip4_Inferred` |
| **Reject scaffold alias** | `Named_CalleeOf_…_Mission_createNDUI_00416590` — generic util ctor; multi-site (audio UI array + BSS); not mission-specific |

### Full-body hex (`read_memory`, 63 B)

```
6aff68abcf9b0064a1000000005064892500000000518bc133c9890889042489480889480c894810c744240cffffffff8b4c240464890d0000000083c410c3
```

### Byte-sealed facts

1. SEH install (`PUSH -1` / `PUSH LAB_009bcfab` / FS:[0]); `MOV EAX,ECX` host; `XOR ECX,ECX`.
2. Stores zero at `[EAX]`, `[EAX+8]`, `[EAX+0xC]`, `[EAX+0x10]` — **no store to `[EAX+4]`**.
3. `MOV [ESP],EAX` only stashes host in the local SEH slot (not a field write).
4. Single exit: restore FS:[0], `ADD ESP,0x10`, `C3`.
5. Classification: **leaf** worker / default element ctor.
