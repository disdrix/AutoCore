# Annotated low-level: FUN_00403d60 → StdTree_UpperBound_StringKey_Isnil131_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00403d60` |
| VA | `0x00403d60`–`0x00403da6` inclusive (**71 B** / `0x47`) |
| System | skills-abilities / shared `client::stdtree` (isnil@+0x131 string-key map) |
| Canonical | `StdTree_UpperBound_StringKey_Isnil131_Inferred` |
| Date | 2026-08-05 (R10-020 OWN dual; scaffold 2026-07-23 refreshed) |

## Machine-level notes

- Twin of dualed lower_bound `StdTree_LowerBound_StringKey_Isnil131_Inferred` (`0x00403c60`).
- Classic MSVC `_Tree::upper_bound`: first node with `key < node.key` (strictly greater), or head/`end()`.
- Compare inverted vs lower_bound: here `operator<(key, node.key)`; true → cand=cur, left; false → right.
- isnil @ **+0x131** (`80 BE 31 01 00 00`); string key @ **+0x0C** (`LEA [ESI+0xC]`).
- ABI: **EAX** = map (`_Myhead` at map+4); stack = `const std::string*`; **EAX** out = node*; **`RET 4`**. Not ECX thiscall.
- Callee: string `operator<` via IAT `[0x009c62e8]` → `0x006eadd0` (cdecl `ADD ESP,8`).
- Callers: `FUN_00957820` only (2 sites) — slash-help range `[lb, ub)` pairs this with lower_bound then walks with `StdTree_IteratorIncrement_Isnil131` (`0x00404000`).

## Pseudocode (annotated)

```c
// EAX = map*; stack key*; EAX out = node*; RET 4
// upper_bound: first node with key < node.key, else end()
StdTreeNode *StdTree_UpperBound_StringKey_Isnil131_Inferred(
    /*EAX*/ void *map,
    /*stack*/ const std::string *key)
{
  StdTreeNode *head = *(StdTreeNode **)((char *)map + 4);  // _Myhead
  StdTreeNode *cur  = head->parent;                        // root  [head+4]
  StdTreeNode *cand = head;                                // end()

  if (cur->isnil /* +0x131 */ != 0)
    return cand;

  do {
    // operator<(key, node.key)  — push order: key first, node.key second in cdecl
    if (std::operator<(*key, *(string *)((char *)cur + 0x0C))) {
      cand = cur;
      cur  = cur->left;   // +0
    } else {
      cur  = cur->right;  // +8
    }
  } while (cur->isnil /* +0x131 */ == 0);

  return cand;
}
```

## Control-flow seal (assembly)

```text
MOV EAX, [EAX+4]              // head
MOV EBP, [ESP+8]              // key*
MOV ESI, [EAX+4]              // root
MOV EDI, EAX                  // cand = end
CMP BYTE PTR [ESI+0x131], 0
JNZ ret_cand
MOV EBX, [0x009c62e8]         // operator<
loop:
  LEA ECX, [ESI+0xC]          // &node.key
  PUSH ECX / PUSH EBP / CALL EBX / ADD ESP,8   // op<(key, node.key)
  TEST AL, AL
  JZ  go_right                // !(key < node.key)
  MOV EDI, ESI                // cand = cur
  MOV ESI, [ESI]              // left
  JMP next
go_right:
  MOV ESI, [ESI+8]            // right
next:
  CMP BYTE PTR [ESI+0x131], 0
  JZ  loop
ret_cand:
  MOV EAX, EDI
  RET 4
```

## Open questions

- Product English for host map type (slash-command registry inferred from callers only).
- Runtime / bit-exact / differential (not run; Terminal false).
