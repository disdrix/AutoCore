# Annotated low-level: NDAssetImage_WriteAlphaPlaneByte

| Field | Value |
|---|---|
| Stable ID | `aa_00433280` |
| VA | `0x00433280` |
| Body | `0x00433280`–`0x004332ac` |
| Canonical name | `NDAssetImage_WriteAlphaPlaneByte` |
| Prior scaffold | `FUN_00433280` |
| System | `gfx-asset` / `NDAssetImage` / alpha plane |
| Date | `2026-07-29` (W23-A dual seal) |

---

## Machine-level notes

- **Prefer bytes over decompiler** for ABI: Ghidra shows `__fastcall` + phantom `in_EAX` / `unaff_ESI`. Machine uses **EAX=this**, **ESI=row**, **EDX=col**, stack byte, **`RET 4`**.
- Leaf: no callees.
- Bounds: plane non-null; `0 ≤ row < height(+0x10)`; `0 ≤ col < width(+0xc)`.
- Index: `plane[row * width + col] = value` (row-major, 1 byte/pixel plane).
- Out-of-bounds / null plane: **silent no-op** (no assert).

## Sealed ABI

```c
// Custom register + stdcall hybrid (MSVC optimized leaf):
//   EAX = NDAssetImage* this
//   ESI = int row (y)
//   EDX = int col (x)
//   [esp+4] = uint8_t alpha
//   ret 4
void NDAssetImage_WriteAlphaPlaneByte(/*eax*/ image, /*esi*/ row, /*edx*/ col, uint8_t alpha);
```

## Layout (fields touched)

| Off | Type | Role |
|---|---|---|
| `+0x0c` | `int32` | width (columns) |
| `+0x10` | `int32` | height (rows) |
| `+0x1b0` | `uint8_t*` | alpha plane base (or null) |

## Annotated CF

```
plane = *(this + 0x1b0)
if plane == null: return
if row < 0 or row >= *(this+0x10): return
if col < 0 or col >= *(this+0x0c): return
*(plane + row * width + col) = alpha
return
```

## Callers

| Site | Parent | Use |
|---|---|---|
| `0x00434efb` | `NDAssetImage_DecodeUncompressedTgaRow` | 32bpp TGA: write A after RGB expand |
| `0x00435a10` | PNG decode (`FUN_00435410`) | write A from RGBA row buffer |

## Sibling

- `FUN_004332b0` / read counterpart: same offsets; returns plane byte or 0 (AL cleared on miss).

## Pseudocode (faithful; registers explicit)

```c
void NDAssetImage_WriteAlphaPlaneByte_reg(
    /*eax*/ int *image,
    /*esi*/ int row,
    /*edx*/ int col,
    /*stack*/ uint8_t alpha)
{
  uint8_t *plane = *(uint8_t **)((char *)image + 0x1b0);
  if (plane == 0) return;
  if (row < 0) return;
  if (row >= *(int *)((char *)image + 0x10)) return;
  if (col < 0) return;
  {
    int width = *(int *)((char *)image + 0x0c);
    if (col >= width) return;
    plane[row * width + col] = alpha;
  }
}
```

## Open / residual

1. Product English name of alpha plane allocator / when `+0x1b0` is allocated.
2. Full `NDAssetImage` size and other plane fields.
3. Runtime live verification.
4. Sibling read `0x004332b0` dual not owned this wave.
