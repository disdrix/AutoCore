# Annotated low-level: CSoundManager_TryQueueMissionCompleteSound_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00725a70` |
| VA | `0x00725a70`–`0x00725bcd` (349 bytes) |
| Ghidra | `FUN_00725a70` |
| Canonical | `CSoundManager_TryQueueMissionCompleteSound_Inferred` |
| System | client audio / `CSoundManager` / mission-complete SFX |
| Date | 2026-07-29 (W18-R dual A/B seal) |
| Tools | `decompile_function` + `read_memory` only |

## Machine-level notes

- Prologue: `push ebp; mov ebp, ecx` — **thiscall**, manager in EBP.
- Epilogue: `ret 8` — two stack args (path/desc key, float* worldPos).
- Enable gate: `cmp byte ptr [ebp+0x258], 0` (`this+600`).
- Listener XZ: `this+0x220` (X), `this+0x228` (Z). Y not in radius test.
- Fixed max dist²: `DAT_00aaa880 = 10000.0f` (not per-descriptor).
- Resolve: `FUN_007249c0` with path arg + global flag byte `DAT_00d1a614`.
- Alloc: freelist pop `FUN_00480460` → tail init `FUN_00480170` → slot in ESI.
- `strncpy(slot, resolved, 0x104)`.
- Hardcoded slot patch (see raw re-verify).
- Queue: `FUN_00480350(this+0x310)`.

## Annotated control flow

```c
// bool __thiscall CSoundManager_TryQueueMissionCompleteSound(
//     CSoundManager *this, const char *pathOrKey, const float *worldPos3)

// 1) master enable
if (*(char *)(this + 0x258) == 0) return 0;

// 2) fixed-radius proximity (XZ only). Fail only if BOTH:
//    - ||pos|| > 0
//    - (this.x - pos.x)^2 + (this.z - pos.z)^2 > 10000.0
dx = this->listenX - pos[0];
dz = this->listenZ - pos[2];
if (length(pos) > 0.0f && abs(dx*dx + dz*dz) > 10000.0f) return 0;

// 3) resolve filesystem / table path
src = CSoundManager_ResolveSoundPath(pathOrKey, /*flag*/ DAT_00d1a614);
if (!src) return 0;

// 4) freelist slot ~0x160
slot = AlignedFreelist_PopSlot();
if (slot) SoundQueueSlot_InitTailFields(slot, /*src16 from regs*/);
// decompiler may show null on failed pop; callers still strncpy — residual risk

// 5) clone path + hardcoded mission-complete fields
strncpy(slot, src, 0x104);
*(uint32_t*)(slot+0x120) = 0xFFFFFFFF;  // TFID lo
*(uint32_t*)(slot+0x124) = 0xFFFFFFFF;  // TFID hi
*(uint8_t*)(slot+0x128) = 0;
*(uint8_t*)(slot+0x10b) = 0;
*(uint8_t*)(slot+0x131) = 0;
*(uint32_t*)(slot+0x104) = 0x32;        // type = 50
*(float*)(slot+0x150) = 0.0f;
*(float*)(slot+0x118) = 0.0f;
*(float*)(slot+0x154) = 0.0f;
*(float*)(slot+0x110) = 20.0f;          // DAT_00aaa6e8
*(float*)(slot+0x11c) = 1.0f;           // g_flOne
*(float*)(slot+0x114) = 100.0f;         // DAT_00aaa7ac radius-like

// 6) enqueue
StdList_PushPayloadNode(this + 0x310, slot);
return 1;
```

## Contrast vs sibling `0x007258a0`

| Aspect | `00725a70` (this) | `007258a0` TryQueueProximity |
|---|---|---|
| Busy gate types 0xC/0xD | **none** | `FUN_00722b80` |
| Radius | **fixed 10000 dist²** | `desc+0x114` squared |
| Slot fields | **hardcoded** type 0x32 / TFID -1 | **copy from descriptor** |
| Sole known caller | mission-complete audio | FX / SpecialFX play |

## Open

1. Exact identity of `param_2` (raw path string vs thin descriptor) — caller passes reaction `+0x1ea` string after `Client_GetMissionCompleteAudioTable`.
2. Whether freelist-null still strncpy's (UB) — decompiler shows path; not sealed crash-safe.
3. Runtime / differential — open.
