# Annotated low-level: FUN_005380d0

| Field | Value |
|---|---|
| Stable ID | `aa_005380d0` |
| VA | `0x005380d0` |
| System | `missions-progression` |
| Date | `2026-07-29` (annotated dual seal; raw body 2026-07-23) |
| Dual | `A/B_aa_005380d0_StagingMapNode_Construct.md` |

## Machine-level notes

- Source: raw capture for `aa_005380d0` + fresh Ghidra `decompile_function` / `read_memory` (2026-07-29).
- **Prefer body bytes** when decompiler conflicts: decomp says `void`; image returns node\* in **EAX** (`RET 0x14`).
- Sole callee: `operator_new`. Sole caller: `FUN_00538ea0` @ call site `0x00538f32`.
- Value copy is `REP MOVSD` with `ECX=0xE` (14 dwords) to `node+0x10`.
- Color @ `+0x48` from stack arg; isnil @ `+0x49` forced `0`.
- `+0x0C` and `+0x4A..+0x4F` not written.

## Body image (hex, length 0x40 usable)

```
6a 50 e8 bb 17 f5 ff 83 c4 04 85 c0 74 31
8b 4c 24 04 8b 54 24 08 56 8b 74 24 14 89 08
8b 4c 24 10 57 89 48 08 89 50 04 8a 54 24 1c
8d 78 10 b9 0e 00 00 00 f3 a5 5f 88 50 48
c6 40 49 00 5e c2 14 00
```

## Pseudocode (annotated; return type corrected)

```c
// Image: freestanding, RET 0x14, EAX = StagingMapNode*
StagingMapNode* FUN_005380d0(
    StagingMapNode* left,        // param_1
    StagingMapNode* parent,      // param_2
    StagingMapNode* right,       // param_3
    const uint32_t* valuePair,   // param_4 — 14 dwords: key, pad, payload[12]
    uint8_t color)               // param_5
{
  StagingMapNode* node = (StagingMapNode*)operator_new(0x50);
  if (node != NULL) {
    node->left = left;           // +0x00
    node->right = right;         // +0x08
    node->parent = parent;       // +0x04
    // +0x0C intentionally not written
    uint32_t* dst = (uint32_t*)((uint8_t*)node + 0x10);
    for (int i = 0xE; i != 0; --i) {  // image: REP MOVSD
      *dst++ = *valuePair++;
    }
    *(uint8_t*)((uint8_t*)node + 0x48) = color;
    *(uint8_t*)((uint8_t*)node + 0x49) = 0;  // isnil = real node
  }
  return node;
}
```

## Caller contract (`FUN_00538ea0`)

```
node = FUN_005380d0(head, where, head, valuePair, /*color*/ 0);
// then size++, link child, RB rebalance — not this unit
```

## Open questions

- Product English / MSVC `_Tree_node` typedef name.
- Semantic of pad dword at `+0x14`.
- Runtime / bit-exact verification.
