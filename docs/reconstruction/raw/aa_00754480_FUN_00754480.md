# Raw capture: FUN_00754480

| Field | Value |
|---|---|
| **Stable ID** | `aa_00754480` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00754480` |
| **Canonical name** | `FUN_00754480` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __fastcall FUN_00754480(int param_1)

{
  int iVar1;
  
  iVar1 = FUN_0075eff0(*(undefined4 *)(param_1 + 0x28),*(undefined4 *)(param_1 + 0xb0),
                       *(undefined1 *)(param_1 + 5));
  if (iVar1 < 0) {
    vog_LogMessage("C:\\vog\\1_code\\palantir\\palantir\\framework\\Palantir.cpp",0x184,0,
                   "Palantir::ResetDevice failed. Will try again in 1000 msec. ");
    *(undefined1 *)(param_1 + 6) = 1;
    Sleep(1000);
    return 0xffffffff;
  }
  *(undefined1 *)(param_1 + 6) = 0;
  (**(code **)(**(int **)(param_1 + 0x2c) + 8))(param_1 + 8);
  return 0;
}
```

---

## W35-K dual append (2026-07-29)

| Field | Value |
|---|---|
| **Wave** | W35-K OWN-ONLY |
| **Tools** | `decompile_function` / `batch_decompile` + `analyze_function_complete` + `read_memory` + xrefs. **No** `disassemble_bytes` |
| **Body** | `0x00754480`–`0x007544E0` exclusive (**96 B** / `0x60`; pad `CC` then next) |
| **Preferred name** | `Palantir_ResetDevice` (**product** from log `"Palantir::ResetDevice failed..."` + path `Palantir.cpp:0x184`) |
| **ABI** | **ECX=`Palantir*`** (`MOV ESI,ECX`); no stack formals; bare **`RET`** (`C3`); **EAX** return `0` success / `0xFFFFFFFF` failure |
| **Decompile ≢ bytes** | Decompiler omits **`EAX = this+8`** live-in to `FUN_0075eff0` and the **`PUSH EDI`** (this+8) before the success-path virtual call setup is only partial in CF notes — **bytes win** on register setup for nested device reset |

### Full body hex (96 B)

```
568bf18a46058b8eb00000008b5628578d7e085051528bc7e853ab000085c07d2e685cf8a9006a0068840100006810f9a900e80929240083c41068e8030000c6460601ff15a8619c005f83c8ff5ec38b4e2cc64606008b0157ff50085f33c05ec3
```

### Bytes-authority CF

```
// ECX=this (Palantir*); bare RET; EAX = 0 | -1
int Palantir_ResetDevice(Palantir* self)
{
  // ESI=self; EDI=self+8
  // FUN_0075eff0(EAX=self+8, stack: [self+0x28], [self+0xB0], byte[self+5])
  hr = FUN_0075eff0(/* device path; unowned */);
  if (hr < 0) {
    vog_LogMessage("...\\Palantir.cpp", 0x184, 0,
                   "Palantir::ResetDevice failed. Will try again in 1000 msec. ");
    self->flag_retry = 1;          // byte at +6
    Sleep(1000);                   // IAT [0x009c61a8]
    return -1;
  }
  self->flag_retry = 0;            // +6 = 0
  // thiscall: ECX = *(self+0x2C); PUSH self+8; CALL vtbl[2]  (slot +8)
  (*( *(self+0x2C)->vtbl + 8 ))(self + 8);
  return 0;
}
```

### Related

| Item | Detail |
|---|---|
| Vtbl | `PTR_FUN_00a9f76c` slot **[1]** = this (W34-K: slot[0]=scalar dtor `FUN_004447e0`, complete dtor `Palantir_CompleteDtor`) |
| Nested | `FUN_0075eff0` — gfx device reset (`gfxDevice.cpp` "Beginning shutdown for Device Reset()") — **unowned** |
| Success notify peer | `FUN_007a3c80` @ `0x007a3c84` CALL — on success `SendNotifyMessageA(HWND_BROADCAST, 0x801B, 0, 0)` and sets `this+0x12C = 1` |
| Product path | `C:\vog\1_code\palantir\palantir\framework\Palantir.cpp` |
| Failure string @ `0x00a9f85c` | `"Palantir::ResetDevice failed. Will try again in 1000 msec. "` |
| Path string @ `0x00a9f910` | `Palantir.cpp` path |

### Callers / callees

| Callers | Callees |
|---|---|
| `FUN_007a3c80` (direct); also vtbl[1] of `PTR_FUN_00a9f76c` | `FUN_0075eff0`, `vog_LogMessage`, IAT `Sleep` |

### Layout (this / Palantir offsets used)

| Offset | Role |
|---|---|
| `+0x05` | byte passed into device reset (stack arg3) |
| `+0x06` | retry flag: `1` on fail, `0` on success |
| `+0x08` | subobject / device-related block (`EAX` + success vcall arg) |
| `+0x28` | pointer → gfx device host for `FUN_0075eff0` |
| `+0x2C` | object* with vtbl; success path calls vtbl[+8] |
| `+0xB0` | dword passed as stack arg2 to device reset |
