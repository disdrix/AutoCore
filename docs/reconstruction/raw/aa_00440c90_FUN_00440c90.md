# Raw capture: FUN_00440c90

| Field | Value |
|---|---|
| **Stable ID** | `aa_00440c90` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00440c90` |
| **Canonical name** | `FUN_00440c90` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00440c90(void *param_1)

{
  if (*(char *)((int)param_1 + 0x1d) == '\0') {
    FUN_00440c90(*(undefined4 *)((int)param_1 + 8));
    FUN_00442730();
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return;
}
```

---

## Re-verify append — 2026-08-05 MEGA-131 OWN-ONLY

**Tools:** Ghidra HTTP `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_xrefs_to`. **No** `disassemble_bytes`. No Launcher.

| Claim | Result |
|---|---|
| Live decompile | **Incomplete** — right recurse + `FUN_00442730` + false noreturn on `operator_delete`; **left-walk loop missing** |
| Bytes authority | Full free-subtree with value destroy: right recurse → destroy val → delete → left iterate |
| Body | `0x00440c90`–`0x00440cca` inclusive (**59 B** / `0x3B`); exclusive end `0x00440ccb`; pad `CC` |
| ABI | ECX = tree shell (threaded `MOV EBX,ECX`); stack = node*; void; **RET 4** (`C2 04 00`) |
| isnil | **+0x1d** (`80 7F 1D 00` / `80 7E 1D 00`) |
| left / right | left `@+0` (`8B 36`); right `@+8` (`8B 46 08`) |
| Value destroy | `MOV EAX,EDI; CALL FUN_00442730` — embedded tree shell at **node+0x10** (head@+0x14, size@+0x18) |
| Classification | worker |
| Canonical name | `StdTree_FreeSubtree_Isnil1d_DestroyTreeVal10_Inferred` |
| Scaffold retired | `Named_CalleeOf_Named_gfxUIWindow_00440c90` |

### Full body hex (59 B)

```
53 56 57 8B 7C 24 10 80 7F 1D 00 8B D9 8B F7 75 25
8B 46 08 50 8B CB E8 E4 FF FF FF 8B 36 8B C7 E8 7B 1A 00 00
57 E8 67 8B 04 00 83 C4 04 80 7E 1D 00 8B FE 74 DB
5F 5E 5B C2 04 00
```

### Byte-corrected CF (authoritative)

```
// ECX = tree_shell (threaded); stack = node*; RET 4
if (*(uint8_t*)(node+0x1d) != 0) ret 4;
for (;;) {
  FreeSubtree(tree_shell, *(node+8));   // right
  left = *node;                         // left @+0
  FUN_00442730(node);                   // EAX=node; destroy embedded tree @+0x10
  operator_delete(node);                // cdecl; ADD ESP,4
  node = left;
  if (*(uint8_t*)(node+0x1d) != 0) break;
}
ret 4;
```

### Callers / xrefs (4 UNCONDITIONAL_CALL)

| From | Function | Role |
|---|---|---|
| `0x00440ca7` | `FUN_00440c90` | self right-recurse |
| `0x00441740` | `FUN_00441720` | erase-range full-clear (`push head->parent`; ECX=tree) |
| `0x00759e20` | `FUN_00759de0` `gfxUIWindow_CompleteDtor` | clear shell at **this+0x10** (`LEA EDI,[ESI+0x10]`; push root) |
| `0x00758d82` | `FUN_00758c80` | clear shell at **this+0x10** (same pattern) |

### Callees

| Target | Role |
|---|---|
| `FUN_00440c90` | self |
| `FUN_00442730` | value dtor: erase nested isnil@+0x3d tree at node+0x10, delete head, zero head/size |
| `operator_delete` (`0x00489822`) | free node |

### Discriminators

| Peer | VA | isnil | Value dtor | Host shell |
|---|---|---|---|---|
| `Map_FreeSubtree_Isnil1d` | `0x0051be10` | +0x1d | **none** | Map_EraseRange_Isnil1d |
| `FUN_00440ab0` (shell+0x04 free) | `0x00440ab0` | +0x3d | `FUN_00442720` | gfxUIWindow +0x04 |
| **This** | `0x00440c90` | **+0x1d** | **`FUN_00442730`** | gfxUIWindow **+0x10** |

### Disassembly (sealed)

```
00440c90  PUSH EBX / ESI / EDI
00440c93  MOV EDI,[ESP+0x10]          ; node
00440c97  CMP byte ptr [EDI+0x1d],0   ; isnil
00440c9b  MOV EBX,ECX                 ; tree_shell
00440c9d  MOV ESI,EDI
00440c9f  JNZ epilogue
; loop:
00440ca1  MOV EAX,[ESI+8]             ; right
00440ca4  PUSH EAX
00440ca5  MOV ECX,EBX
00440ca7  CALL FUN_00440c90
00440cac  MOV ESI,[ESI]               ; left
00440cae  MOV EAX,EDI
00440cb0  CALL FUN_00442730
00440cb5  PUSH EDI
00440cb6  CALL operator_delete
00440cbb  ADD ESP,4
00440cbe  CMP byte ptr [ESI+0x1d],0
00440cc2  MOV EDI,ESI
00440cc4  JZ loop
; epilogue:
00440cc6  POP EDI / ESI / EBX
00440cc9  RET 4
```
