# Raw capture: FUN_0040d630

| Field | Value |
|---|---|
| **Stable ID** | `aa_0040d630` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0040d630` |
| **Canonical name** | `FUN_0040d630` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0040d630(void *param_1)

{
  if (*(char *)((int)param_1 + 0x19) == '\0') {
    FUN_0040d630(*(undefined4 *)((int)param_1 + 8));
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return;
}
```

---

## W31-A re-verify (2026-07-29) — append only

| Field | Value |
|---|---|
| **Tools** | `decompile_function` + `analyze_function_complete` + `read_memory` + xrefs. **No** `disassemble_bytes` |
| **Live decompile** | ≡ raw scaffold CF shape, but **collapsed**: false noreturn on `operator_delete`; omits **left-walk loop** and **ECX tree_base** threading; omits `RET 4` |
| **Body** | `0x0040d630`–`0x0040d664` exclusive end after `C2 04 00` (**53 B** / `0x35`); pad `CC` |
| **ABI** | **ECX = tree_base** (threaded to recursive calls; unused otherwise); stack arg `node*` @ `[esp+4]`; **`RET 4`** (`C2 04 00`); void |
| **Callees** | self, `operator_delete` |
| **Callers** | `FUN_0040d5b0` @ `0x0040d5f6` (clear free path); `FUN_0040d700` @ `0x0040d71c` (erase free path); self @ `0x0040d647` |
| **Classification** | worker |

### Full body hex (53 B)

```
5356578b7c2410807f19008bd98bf7751e8b4608508bcbe8e4ffffff8b3657e8cec1070083c404807e19008bfe74e25f5e5bc20400
```

### Byte-sealed algorithm (overrides decompiler)

```
// entry
push ebx,esi,edi
EDI = arg0 (node)           // [esp+0x10] after 3 pushes
EBX = ECX                   // tree_base (this+4 from clear/erase callers)
ESI = EDI
if node.isnil@+0x19 != 0:
  goto epilogue             // nil sentinel: no free

loop:                       // while !node.isnil
  push node->right          // [esi+8]
  ECX = EBX
  call FUN_0040d630         // free right subtree
  ESI = node->left          // [esi+0]  (saved before free)
  push EDI                  // current node
  call operator_delete
  add esp, 4
  EDI = ESI
  if !ESI.isnil: goto loop

epilogue:
  pop edi,esi,ebx
  ret 4
```

### Layout (Val12 family — matches sealed clear W30-P)

| Offset | Role |
|---|---|
| node+0x00 | left |
| node+0x04 | parent |
| node+0x08 | right |
| node+0x19 | isnil (`0` = real node) |

### Decompile gaps

1. Not a single-step free: **left-walk loop** after recursive right free.
2. **ECX tree_base** is preserved (`MOV EBX,ECX`) and passed to recursive calls — decompiler dropped it.
3. Epilogue is **`RET 4`**, not bare `RET` / noreturn after first `operator_delete`.
4. `operator_delete` is **cdecl** (`ADD ESP,4`); function itself cleans one stack arg.
