# Annotated low-level: FUN_00629d90 / PhysSim_LtBroadPhase_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00629d90` |
| VA | `0x00629d90` |
| System | physics / sub-island broadphase + narrowphase |
| Date | 2026-07-29 (W28-S dual seal) |
| Canonical (inferred) | `PhysSim_LtBroadPhase_Inferred` |

## Machine-level notes

- Source: raw capture + live decompile 2026-07-29 ≡ raw CF.
- **Profile whole-fn:** `"LtBroadPhase"` — seals collide-only role (not integrate/applyAction).
- Stages: `StCalcAabbs` → `St3AxisSweep` → `StExamine` → commit pairs (`FUN_006297e0`) → narrowphase (`FUN_00629c40` or continuous `FUN_00629990`).
- **ABI:** `__thiscall`; **ECX = sub-island**; **2 stack args**; **`ret 8`**.
- Call-site (`FUN_00561910` @ `0x00561a13`): `ECX = subIslands[i]`; push `island+0x140` (stepBlock); push `*(island+0xc4)` (ctx). Decompiler this/stack map is partial — body fields on `param_1` match sub-island.
- Margin: `*(float*)(*(parentWorld+0xcc)+8) * DAT_00a0f298` with `DAT_00a0f298 = 0.5f`.
- Body AABB scratch: stride **0x20** per body; pointer table aligned to 16.
- Sets `subIsland+0x30 = 1` after pair commit.
- Parent world flag `+0x23e` selects simple vs continuous narrowphase.
- Prefer verified notes: `docs/reconstruction/physics/verified/fn_00561910_islandStep.md` §4.

## Pseudocode (annotated)

```c
// thiscall: ECX = subIsland; stack: ctx, stepBlock; ret 8
void __thiscall PhysSim_LtBroadPhase_Inferred(void *subIsland /*ECX*/, /*ctx, stepBlock via stack*/)
{
  // profile "LtBroadPhase"
  // FUN_0062d960(parentWorld+0x238) x2 — pipeline prep
  // alloc AABB buf (bodyCount * 0x20) + ptr table (align16 bodyCount*4) via DAT_00b05060
  // margin = agent.field_8 * 0.5f

  // "StCalcAabbs"
  // for each body in subIsland[+0x3c / +0x40]:
  //   collect collidable ptr; shape vtbl[+0x18](xform+0x20, margin, aabbOut)

  // "St3AxisSweep"
  // (*stepBlock.vtbl)[+0x14](aabbPtrs, aabbBuf, bodyCount, &pairOut)

  // "StExamine"
  // optional merge static pairs from subIsland+0x74/+0x78 into pair buffer
  // free AABB scratch
  // FUN_006297e0(...)  // Phys_CommitPairListStorage_Inferred
  // subIsland+0x30 = 1
  // free remaining scratch (allocator tag 0x12 on pair buffers)
  // if (!(parentWorld+0x23e)) FUN_00629c40(...); else FUN_00629990(...);
}
```

## Open questions

- Exact product type of `*(island+0xc4)` ctx vs stepBlock binding under decompiler residual.
- Full semantics of `FUN_0062d960` / narrowphase callees (not OWN).
- Runtime / bit-exact open.
