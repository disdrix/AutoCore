# Raw capture: FUN_0082f9a0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0082f9a0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0082f9a0` |
| **Canonical name** | `FUN_0082f9a0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0082f9a0(void)

{
  int in_EAX;
  undefined4 uVar1;
  int iVar2;
  int *piVar3;
  int *unaff_EDI;
  
  if ((*(int *)(in_EAX + 0x6b4) < 1) || (0 < *(int *)(DAT_00d1b6d8 + 0x6b4))) {
    if (DAT_00d1da2c != 0) {
      uVar1 = (**(code **)(*(int *)(*(int *)(*(int *)(in_EAX + 4) + 4) + 4 + in_EAX) + 0x160))();
      iVar2 = FUN_00573a30(uVar1);
      if (iVar2 != 0) {
        piVar3 = (int *)(**(code **)(*unaff_EDI + 0x444))
                                  ("i_m_int_2d_btn_generic.xml",0,0x1117a,0xffffffff);
        if (piVar3 != (int *)0x0) {
          (**(code **)(*piVar3 + 0x1d8))("Remove from Friends",1,1);
        }
        unaff_EDI[0x159] = (int)piVar3;
        return;
      }
    }
    piVar3 = (int *)(**(code **)(*unaff_EDI + 0x444))
                              ("i_m_int_2d_btn_generic.xml",0,0x11177,0xffffffff);
    if (piVar3 != (int *)0x0) {
      (**(code **)(*piVar3 + 0x1d8))("Add to Friends",1,1);
    }
    unaff_EDI[0x156] = (int)piVar3;
  }
  return;
}
```

---

## MEGA-075 live re-verify (2026-08-05)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-075** |
| **Tools** | `decompile_function`, `analyze_function_complete`, `disassemble_function`, `get_function_callers`, `get_function_xrefs`, `get_assembly_context`, `get_function_signature`, `read_memory` |
| **Forbidden tools used** | none (`disassemble_bytes` not used) |
| **Live decompile ≡ frozen raw CF** | **YES** (2026-08-05 ≡ 2026-07-23 body) |
| **Terminal / runtime Confirmed** | **false** |

### Body / size

- VA range: `0x0082f9a0`–`0x0082fa58` inclusive; exclusive end `0x0082fa59`
- Size: **185 B** (`0xB9`)
- Pad after body: `CC` ×7 at `0x0082fa59+` (next unit boundary)
- Epilogues: `POP ESI; RET` (`5E C3`) at `0x0082fa1e`/`0x0082fa1f` (remove path) and `0x0082fa57`/`0x0082fa58` (add path); early gate exit bare `RET` (`C3`) at `0x0082fa58` shared tail for local-gate fail is the add-path ret; gate fail jumps to `0x0082fa58`

### Entry / ABI (disassemble_function)

```
0082f9a0  CMP dword ptr [EAX + 0x6b4], 1     ; EAX = target object*
0082f9a7  JL  0082f9bc                        ; target field < 1 → enter body
0082f9a9  MOV ECX, [DAT_00d1b6d8]             ; local player
0082f9af  CMP dword ptr [ECX + 0x6b4], 1
0082f9b6  JL  0082fa58                        ; local field < 1 → exit
0082f9bc  PUSH ESI
...
0082f9e7  MOV EAX, [EDI]                      ; EDI = menu host*
0082f9f7  MOV ECX, EDI
0082f9f9  CALL [EAX+0x444]                    ; create button
...
0082fa18  MOV [EDI+0x564], ESI                ; Remove slot
0082fa1e  POP ESI
0082fa1f  RET                                 ; ret 0
...
0082fa51  MOV [EDI+0x558], ESI                ; Add slot
0082fa57  POP ESI
0082fa58  RET                                 ; ret 0
```

**ABI sealed:**
- **EAX** = target object* (parent sets `MOV EAX,EBP` before both call sites)
- **EDI** = menu host* (parent `MOV EDI,ESI` at `0x0082feba` before pack branch; second path re-asserts at `0x0082fefd`)
- Stack args: **none**
- Return: **void**, **`RET`** (`C3`) — **ret 0**, not thiscall `ret N`
- ESI clobbered (saved/restored around body); uses local ESI for button ptr

### Callers / xrefs (live)

| Kind | Address | Function | Notes |
|---|---|---|---|
| UNCONDITIONAL_CALL | `0x0082fee3` | `FUN_0082fe20` | order-flag ≠ 0 path: after Trade/Clan/Convoy packs; `MOV EAX,EBP` |
| UNCONDITIONAL_CALL | `0x0082feff` | `FUN_0082fe20` | order-flag == 0 path: after Ignore; `MOV EAX,EBP; MOV EDI,ESI` |

- Callers: **1** function (`FUN_0082fe20` = dualed `UI_InteractionMenu_PopulateTargetButtons_Inferred`)
- Xrefs: **2** (both UNCONDITIONAL_CALL)
- Callees (direct `E8`): **`FUN_00573a30`** only (friends-list name lookup, thiscall)
- Indirect: target `vtbl+0x160`, menu `vtbl+0x444`, button `vtbl+0x1d8`

### Strings / immediates (`read_memory`)

| VA | Content |
|---|---|
| `0x00a721ac` | `"i_m_int_2d_btn_generic.xml"` |
| `0x00a72144` | `"Remove from Friends"` |
| `0x00a72134` | `"Add to Friends"` |
| imm | button id **`0x1117a`** (Remove), **`0x11177`** (Add), flags `0`, `-1` |
| slots | host **`+0x564`** Remove (`EDI[0x159]`), **`+0x558`** Add (`EDI[0x156]`) |
| globals | `DAT_00d1b6d8` local player; `DAT_00d1da2c` friends-list host for `FUN_00573a30` |

### Control flow (sealed)

```
if !(target+0x6B4 < 1 || local+0x6B4 >= 1): return
if DAT_00d1da2c != 0:
  name = target.vtbl+0x160()   ; MSVC adjustor thiscall shape
  if FUN_00573a30(DAT_00d1da2c, name) != 0:   ; already friend
    btn = menu.vtbl+0x444("i_m_int_2d_btn_generic.xml", 0, 0x1117a, -1)
    if btn: btn.vtbl+0x1d8("Remove from Friends", 1, 1)
    menu+0x564 = btn
    return
btn = menu.vtbl+0x444(..., 0x11177, -1)
if btn: btn.vtbl+0x1d8("Add to Friends", 1, 1)
menu+0x558 = btn
return
```

### Name

- Canonical: **`UI_InteractionMenu_AddFriendsToggle`**
- Ghidra: `FUN_0082f9a0`
- Prior plate alias: `UI_int_btn_generic_0082f9a0` (retire for role name)
- Role sealed by Add/Remove Friends English + ids + parent pack order; product menu-host MSVC class open

### Signature completeness note

`analyze_function_complete` warns return type `undefined` / void display — sealed as void `ret 0` via epilogue `C3` and no EAX return use. `in_EAX` is real (parent-passed target), not phantom for ABI purposes; `unaff_EDI` is real parent-preserved menu host.
