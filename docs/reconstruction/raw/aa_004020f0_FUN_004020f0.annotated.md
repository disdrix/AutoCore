# Annotated low-level: FUN_004020f0 → BasicStringFlag_Dtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004020f0` |
| VA | `0x004020f0`–`0x00402126` inclusive (**55 B** / `0x37`) |
| System | missions-progression (partition parent chain); **unit role** = shared MSVC aggregate dtor (string only) |
| Date | 2026-08-05 (R10-018 dual refresh) |
| Canonical name | `BasicStringFlag_Dtor_Inferred` |
| Ghidra | `FUN_004020f0` |

## Machine-level notes

- **Purpose:** Destroy a `{ std::basic_string<char> @+0 (size 0x1c); uint8_t flag @+0x1c }` aggregate by running only the string destructor. Trailing flag is POD — no extra work.
- **ABI:** stack object*; nested `thiscall` (`MOV ECX,[ESP+0x10]`); void; **`RET 4`**.
- **Callee:** IAT `[0x009c62f4]` = `basic_string::~basic_string()` (Ghidra EXTERNAL `~basic_string`).
- **SEH:** frame via `LAB_009bbfb9` (shared with twin ctor `00401d30` and copy-ctor `00402040`). SEH state stays `0xffffffff` (function does not construct a new object).
- **Callers (7):** all `Unwind@*` sites — MSVC exception-cleanup registration for the string-bearing object. No non-unwind direct callers in Ghidra xrefs.
- Prefer assembly for stack-offset ABI (`[ESP+0x10]` after SEH install) over decompiler-only reading.

## Pseudocode (annotated)

```c
// object* on stack; RET 4
void BasicStringFlag_Dtor_Inferred(BasicStringFlag *obj /* stack */)
{
  // SEH install (LAB_009bbfb9); state = -1
  // MOV ECX, obj; CALL [IAT basic_string dtor]
  basic_string_dtor(&obj->str);
  // SEH teardown; ADD ESP,0xC; RET 4
  // flag @ +0x1c intentionally untouched (POD)
}
```

## Layout (sealed via family twins)

| Off | Width | Role |
|----:|------:|------|
| `+0x00` | `0x1C` | `std::basic_string<char>` (MSVC size in this binary) — **destroyed here** |
| `+0x1C` | 1 | trailing flag / tag byte (POD; no dtor work) |

Object footprint at least **0x1D**; dtor body only needs the string subobject at `+0`.

## Twin / family

| VA | Name / role |
|---|---|
| `0x00401d30` | `BasicStringFlag_CtorFromStringAndChar_Inferred` — **RET 0xC** (dualed MEGA-065) |
| `0x00402040` | `BasicStringFlag_CopyCtor_EdiSrc_Inferred` — **RET 4** (dualed MEGA-080) |
| `0x00401fe0` | parent node ctor embeds StringFlag at `+0xc` (residual) |

## Open questions

- Product English for the flag byte (bool? enum? tag?) — keep `_Inferred` on family name.
- Full product map/node type for parents that embed this aggregate.
- Which concrete frames own each of the 7 Unwind sites (type identity beyond string-bearing).
- Runtime / bit-exact / differential: open (no Launcher; Terminal false).
