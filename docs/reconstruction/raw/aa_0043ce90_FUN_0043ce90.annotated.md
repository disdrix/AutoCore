# Annotated low-level: GuardedVector_RingCopyRange (`FUN_0043ce90`)

| Field | Value |
|---|---|
| Stable ID | `aa_0043ce90` |
| VA | `0x0043ce90`–`0x0043cebe` (47 B) |
| Ghidra name | `FUN_0043ce90` |
| Inferred name | `GuardedVector_RingCopyRange` |
| System | STL / guarded container (unlocked assign helpers) |
| Date | 2026-07-29 (W27-B OWN dual) |

## Machine-level notes

- Thin **argument-marshalling worker** used only by `GuardedVector_CopyAssign` (`FUN_0043c730`) on both fit and grow paths.
- High-level decompile is `FUN_0043d390(); return;` — correct control flow; body is 47 B of stack reshape + call + `EAX = ESI`.
- Parent pushes **6** stack dwords, then calls; after return parent does `ADD ESP, 0x18`. This unit itself does `ADD ESP, 0x24` for its internal pushes.
- `FUN_0043d390` (unowned) performs the ring-aware element copy and writes the out cursor pair.

## ABI contract

```
// Parent: 6 stack args + ESI = int outPair[2]
// This unit: reshapes → CALL FUN_0043d390; MOV EAX, ESI; ADD ESP,0x24; RET
// Return: EAX = ESI (pointer to out pair)
void* GuardedVector_RingCopyRange(/* stack+ESI per parent */);
```

| Item | Evidence | Conf |
|---|---|---|
| Exclusive parent `FUN_0043c730` ×2 | xrefs `0x0043c76e`, `0x0043c7aa` | **High** |
| Sole callee `FUN_0043d390` | rel32 from bytes + callees API | **High** |
| Returns ESI in EAX | `8B C6` before clean | **High** |
| Internal clean 0x24 | `83 C4 24` | **High** |
| Parent cleans 0x18 after | parent bytes post-call | **High** |

## Pseudocode (annotated)

```c
// Decompiler view (high level):
void FUN_0043ce90(void)
{
  FUN_0043d390();  // ring element copy; out via *ESI
  return;          // retail also: EAX = ESI
}

// Semantic role in GuardedVector_CopyAssign:
//   1) Ring-copy source element range into dest range (via FUN_0043d390)
//   2) Leave out-cursor in local_8/local_4 for fit (FUN_0043c8c0) or grow (FUN_0043cec0)
```

## Call graph

| Dir | Target | Note |
|---|---|---|
| Caller | `FUN_0043c730` fit + grow | only |
| Callee | `FUN_0043d390` | ring copy body (not OWN) |

## Open questions

- Product/PDB class English for GuardedVector / ring header.
- Full formal list of `FUN_0043d390` stack slots after reshape (unowned residual).
- Runtime / bit-exact element order under wraparound.
