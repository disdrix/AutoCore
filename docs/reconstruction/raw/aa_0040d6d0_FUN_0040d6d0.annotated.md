# Annotated low-level: StdTree_Tidy_FreeHead_Bare_Val12 (FUN_0040d6d0)

| Field | Value |
|---|---|
| Stable ID | `aa_0040d6d0` |
| VA | `0x0040d6d0`–`0x0040d6fe` exclusive (**46 B** / `0x2E`) |
| System | MSVC `_Tree` tidy / free header (Val12) — bare (no SEH) |
| Date | 2026-07-29 (W33-A re-verify over 2026-07-23 scaffold) |
| Canonical name | `StdTree_Tidy_FreeHead_Bare_Val12` (**Inferred**) |
| Ghidra name | `FUN_0040d6d0` |

## Machine-level notes

- Source: live `decompile_function` + full-body `read_memory` + `analyze_function_complete` + xrefs.
- **Bytes win** over decompiler: `operator_delete` is **not** noreturn; epilogue nulls `head@+4` and `size@+8` then `RET`.
- `push ECX` at entry allocates the out-iterator stack slot (decompiler `local_4 = param_1`); value is only written by `FUN_0040d700`.
- Full-range erase args: `first = *head` (left/@0), `last = head` — same gate as W32-C `StdTree_EraseRange_Val12`.
- Peer with SEH: `StdTree_Tidy_FreeHead_Val12` @ `0x0040d7c0` (W32-B, 92 B, `LAB_009bda78`).
- Sole code xref: Unwind `@009bda70` (`MOV ECX,[EBP-10h]; JMP 0040d6d0`).

## Pseudocode (annotated; epilogue from bytes)

```c
// ECX = tree* (head@+4, size@+8). Bare RET. void.
void __fastcall StdTree_Tidy_FreeHead_Bare_Val12(StdTree_Val12* tree)
{
  void* out; /* stack slot; initial garbage/this unused */
  StdTreeNode_Val12* head = tree->head;
  // full range [begin, end) = [*head, head)
  FUN_0040d700(tree, &out, head->left, head);  // StdTree_EraseRange_Val12
  operator_delete(head);                       // free sentinel
  tree->head = nullptr;
  tree->size = 0;
}
```

## Open questions

- Product/MSVC demangle for map value type T (Val12 structural only).
- Why two tidy twins (bare vs SEH) — likely ctor-unwind vs complete-dtor paths; product plate open.
- Nested erase-range value-dtor policy owned under `aa_0040d700` (W32-C).
