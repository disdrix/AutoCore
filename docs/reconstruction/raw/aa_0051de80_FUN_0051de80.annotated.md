# Annotated low-level: SkillCNDHash_CompleteDtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051de80` |
| VA | `0x0051de80`–`0x0051deea` inclusive (**107 B** / `0x6B`) |
| Ghidra | `FUN_0051de80` |
| Canonical | `SkillCNDHash_CompleteDtor_Inferred` |
| System | skills-abilities / skill-family CNDHash |
| Date | 2026-08-04 (WQ9G-A OWN dual) |

## Machine-level notes

- Source: raw capture + WQ9G-A re-verify (`read_memory` full body + string/vtbl + callers/xrefs + assembly_context).
- Role: MSVC **complete object destructor** for skill CNDHash — sole code callee of dualed scalar deleting dtor `SkillCNDHash_ScalarDeletingDtor_Inferred` (`0x0051de60` / vtbl[0] of `PTR_FUN_009ce1b8`).
- Same vtbl install as dualed `SkillCNDHash_Ctor_Inferred` (`0x0051dd60`).
- Lock `@+0x1d` soft-logs only (same family policy as `SkillCNDHash_Recreate_Inferred`).
- Destroy path: bucket table (`0x004cba60`) then freelist/slab host at `this+0x20` (`FUN_0059c8a0`).
- Does **not** free the host object (scalar dtor does that when flags&1).

## Signature (sealed)

```c
// ECX = SkillCNDHash*; no stack args; bare RET; void
void __thiscall SkillCNDHash_CompleteDtor_Inferred(void *self);
```

## Pseudocode (byte-corrected)

```c
void __thiscall SkillCNDHash_CompleteDtor_Inferred(SkillCNDHash *self)
{
  // SEH: PUSH -1; PUSH LAB_009a371b; FS:[0] frame
  self->vtbl = &PTR_FUN_009ce1b8;   // C7 06 B8 E1 9C 00

  if (self->lock /* @+0x1d */ != 0) {
    log(0, "HashError:Destructor, already locked for traversal"); // 0x00a2c270
    log(0, "VOG_DEBUG_STOP");                                    // 0x00a15844
    // continue — no abort
  }

  SkillCNDHash_DestroyBucketTable_Inferred(self); // 004cba60; ECX=self
  FreelistSlabHost_Teardown_Inferred(&self->host@+0x20); // FUN_0059c8a0; LEA ECX,[ESI+0x20]

  // SEH leave; bare ret (C3)
}
```

## Xrefs

| Kind | Address | Notes |
|---|---|---|
| CALL | `0x0051de63` | sole code caller — scalar deleting dtor |
| DATA | `PTR_FUN_009ce1b8` | vtbl shared with ctor; [0]=scalar dtor, not this VA |

## Open gaps

- Product/PDB demangle for host class behind `009ce1b8`.
- Full dual of `FUN_0059c8a0` freelist/slab teardown (not OWN).
- Full vtbl method map beyond ctor/scalar/complete chain.
- Runtime / bit-exact open.
