# Annotated low-level: FUN_00407060 → StdMap_InsertOrFind_PairKey_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00407060` |
| VA | `0x00407060` |
| Body | `0x00407060`–`0x00407133` exclusive (**211 B** / `0xD3`) |
| System | MSVC `std::map` insert-or-find (isnil@+0x21, pair key) |
| Date | 2026-08-05 (WQ9I-C dual; scaffold 2026-07-23) |
| Dual | A/B accept-with-gaps |

## Machine-level notes

- Source: raw capture + live `disassemble_function` / `read_memory` (WQ9I-C).
- Prefer assembly when decompiler reuses `param_3` as walk cursor (machine keeps value in EBP).
- **isnil @ +0x21**; color unused here (insert worker owns color@+0x20).
- Pair key compare matches dualed leaf `StdPairKey_Less_HiSignedLoUnsigned_Inferred` (`0x00401480`) and isnil29 peer `StdMap_InsertOrFind_PairKey_Isnil29_Inferred` (`0x004cbee0`).
- Sole caller: dualed `StdTree_InsertHint_Isnil21_Inferred` (`0x00406560`) fallthrough when hint neighborhood fails.
- Insert worker `FUN_00407200` is WQ9I-B OWN residual (not dualed here).
- Predecessor `FUN_005adff0` shared isnil21 tree walk residual.

## ABI

```text
__thiscall  RET 0x8
  ECX              = MapShell*  (head @ +4, size @ +8)
  [esp+4]  out     = InsertPair*  { Node* it;  uint8_t inserted; }
  [esp+8]  value   = ValPair*     { uint32_t lo; int32_t hi; ...mapped... }
```

## Compare order (walk / insert gate)

```text
// go right when node <= key
  (int)node.hi < (int)key.hi
  OR ((int)node.hi == (int)key.hi AND (uint)node.lo <= (uint)key.lo)

// insert when node < key (after lower-bound)
  (int)node.hi < (int)key.hi
  OR ((int)node.hi == (int)key.hi AND (uint)node.lo < (uint)key.lo)

// else equal hit: out = {node, 0} — no mapped rewrite
```

## Pseudocode (annotated)

```c
// thiscall RET 8
void __thiscall StdMap_InsertOrFind_PairKey_Isnil21_Inferred(
    MapShell_Isnil21 *map,
    InsertPair *out,
    const ValPairKey *value)
{
  Node *head = map->head;           // map+4
  Node *where = head;
  bool goLeft = true;
  char addLeft = 1;

  Node *node = head->parent;        // root
  if (node->isnil == 0) {           // isnil @ +0x21
    Node *child = node;
    do {
      where = child;
      // node <= key → right
      if ((int)where->key_hi < (int)value->key_hi ||
          ((int)where->key_hi <= (int)value->key_hi &&
           (uint)where->key_lo <= (uint)value->key_lo)) {
        child = where->right;
        goLeft = false;
        addLeft = 0;
      } else {
        child = where->left;
        goLeft = true;
        addLeft = 1;
      }
    } while (child->isnil == 0);
  }

  Node *hint = where;

  if (goLeft) {
    // leftmost empty open → always insert left of where
    if (where == head->left) {      // *head == leftmost
      Node *tmp = where;
      Node **pNew = FUN_00407200(map, &tmp, 1, where, value);
      out->it = *pNew;
      out->inserted = 1;
      return;
    }
    FUN_005adff0(&hint);            // predecessor (thiscall/fastcall style on hint*)
  }

  // if hint < value → insert; else equal
  if ((int)hint->key_hi < (int)value->key_hi ||
      ((int)hint->key_hi == (int)value->key_hi &&
       (uint)hint->key_lo < (uint)value->key_lo)) {
    Node *tmp = where;
    Node **pNew = FUN_00407200(map, &tmp, addLeft, where, value);
    out->it = *pNew;
    out->inserted = 1;
    return;
  }

  out->inserted = 0;
  out->it = hint;
}
```

## Open questions

- Product pair/value demangle (TFID composite vs custom).
- Full dual of insert worker `FUN_00407200` (WQ9I-B) and predecessor `FUN_005adff0`.
- Runtime / bit-exact / differential.
