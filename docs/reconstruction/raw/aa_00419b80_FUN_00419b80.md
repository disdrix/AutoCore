# Raw capture: FUN_00419b80

| Field | Value |
|---|---|
| **Stable ID** | `aa_00419b80` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00419b80` |
| **Canonical name** | `FUN_00419b80` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int FUN_00419b80(int param_1,undefined4 param_2)

{
  int iVar1;
  int unaff_EBX;
  
  iVar1 = FUN_00419b40(param_1,param_2);
  if (iVar1 != 0) {
    if (param_1 != 0) {
      *(undefined4 *)(param_1 + 0x10) = *(undefined4 *)(iVar1 + 0x10);
      FUN_00419ad0();
      return iVar1;
    }
    *(undefined4 *)(unaff_EBX + 4) = *(undefined4 *)(iVar1 + 0x10);
  }
  FUN_00419ad0();
  return iVar1;
}
```

---

## v2026-07-29 W18-P dual re-verify (append only)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` + `analyze_function_complete` (no `disassemble_bytes`) |
| **Body** | `0x00419b80`–`0x00419bc9` (**73** bytes; `ret 8`) |
| **Live decompile** | ≡ original raw scaffold CF (register fiction retained) |
| **Callers** | `FUN_00413a60` (`CNDHash_Remove_u64`), `FUN_004197b0` |
| **Callees** | `FUN_00419b40` (`CNDHash_BucketFindNodeByU64Key`), `FUN_00419ad0` (bucket chain touch/cycle repair) |

### Body hex (full)

```
8b4424088b4c2404565750518d7c24148bc3e8a9ffffff8bf085f6741f8b4c240c85c98b461074118bfb894110e81effffff5f8bc65ec208008943048bfbe80dffffff5f8bc65ec20800
```

### Machine ABI (supersedes decompiler param names)

```
// EBX = bucketHeader*  (caller-supplied; not a stack formal)
// stack stdcall: keyLo, keyHi  → ret 8
// predOut storage: reuses caller keyLo stack slot via LEA EDI,[esp+…] into find
// return EAX = Node* or NULL (unlinked but not freed)
```

### Byte-level CF

1. Push keyHi/keyLo; `LEA EDI, [esp+14]` → predOut in original keyLo slot; `MOV EAX,EBX`; `CALL FUN_00419b40`.
2. `ESI = node`. If null → `EDI=EBX`; `CALL FUN_00419ad0`; `EAX=0`; `ret 8`.
3. If non-null: `ECX = *predOut` (value now in keyLo slot — **not** keyLo).
4. `EAX = node->next` (`[ESI+0x10]`).
5. If pred != 0: `pred->next = node->next`; else `bucket->head ([EBX+4]) = node->next`.
6. `EDI=EBX`; `CALL FUN_00419ad0`; return node in EAX.

### Decompiler traps sealed

| Fiction | Machine truth |
|---|---|
| `if (param_1 != 0)` tests keyLo | Tests **predecessor node** (keyLo slot reused as predOut) |
| `unaff_EBX` unexplained | **EBX = bucket header** (EAX into find) |
| Early return skips `FUN_00419ad0` on miss | Miss still calls ad0 with EDI=bucket |
