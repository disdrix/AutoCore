# Annotated low-level: Character_FormatLevelRaceClassLine_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0092e710` |
| VA | `0x0092e710` |
| Body | `0x0092e710`–`0x0092e8a7` inclusive (**408 B** / `0x198`) |
| Ghidra name | `FUN_0092e710` |
| Canonical name | `Character_FormatLevelRaceClassLine_Inferred` (**Inferred**) |
| System | missions-progression / character display UI |
| Date | 2026-08-05 (R10-016 dual refresh) |
| Prior scaffold | `Named_CalleeOf_Skill_Combat_Fighting_and_driving_ability_Affect_0092e710` — **retired** |

## Machine-level notes

- **ABI (bytes win over Ghidra `void` / single `int` formal):**
  - Stack: `character*` (cdecl; caller `ADD ESP,4` @ `0x008f980b`).
  - Register: **`EBX` = output `char*` buffer** (must be writable NUL-terminated string; typically empty).
  - Frame: `SUB ESP,0xC; PUSH EBP; EBP = arg`.
  - Returns **`int` strlen** of resulting buffer (or **0** if character==0 or EBX==0).
  - Plain **`RET`** (not `RET n`); callee-cleanup of locals only.
- **Decompiler `unaff_EBX`:** intentional second input from call site (`LEA EBX, local_108`), not free register noise.
- **Decompiler `FUN_00521800(0xffffffff)`:** the `-1` is residual for following `FUN_007a6de0(..., -1)`; race/class helpers are **ECX-this** (`Character_GetRaceDisplayName_Inferred` / `Character_GetClassDisplayName_Inferred`). ECX must be the character object when those run.
- Prefer assembly/`read_memory` when decompiler conflicts on formals or return type.

## Control flow (annotated)

```c
// int __cdecl Character_FormatLevelRaceClassLine_Inferred(void* character)
//   with EBX = char* outBuf  (register param)
int FUN_0092e710(int param_1 /* character* on stack */)
{
  char cVar1;
  char *pcVar2;
  char *pcVar3;
  int _Value;          // level from vtbl+0x27c
  undefined4 uVar4;    // race/class display string (ring)
  uint uVar5, uVar6;
  char *unaff_EBX;     // OUT buffer (EBX)
  char *pcVar7, *pcVar8;
  int _Radix;
  undefined4 uVar9;    // -1 length for ring copy
  char local_c[12];    // itoa scratch

  // Gate: both character and out buffer required
  if ((param_1 != 0) && (unaff_EBX != (char *)0x0)) {

    // --- "Level " prefix (TLS ring → append to out) ---
    FUN_007a69d0();                                    // TLS string table
    pcVar2 = (char *)FUN_007a6de0("Level ", 0xffffffff); // @ 0x00a2d280
    // strlen(pcVar2); append dword/byte copy to end of unaff_EBX
    // (inline strcpy-to-end pattern × many below)

    // --- decimal level ---
    _Radix = 10;
    pcVar3 = local_c;
    // COMlike nested vtbl resolve on character, then call slot +0x27c
    _Value = (**(code **)(*(int *)(*(int *)(*(int *)(param_1 + 4) + 4)
              + 4 + param_1) + 0x27c))();
    _itoa(_Value, pcVar3, _Radix);
    // append local_c digits to end of unaff_EBX

    // --- ' ' + race display name ---
    // *(uint16*)end = DAT_00a2a000  (" \0")
    uVar4 = FUN_00521800(/* ECX=character */);         // Character_GetRaceDisplayName_Inferred
    pcVar2 = (char *)FUN_007a6de0(uVar4, 0xffffffff);  // re-buffer into ring
    // append race string to unaff_EBX

    // --- ' ' + class display name ---
    // *(uint16*)end = DAT_00a2a000  (" \0")
    uVar4 = FUN_00521900(/* ECX=character */);         // Character_GetClassDisplayName_Inferred
    pcVar2 = (char *)FUN_007a6de0(uVar4, 0xffffffff);
    // append class string to unaff_EBX

    // return strlen(unaff_EBX)
    return strlen_like(unaff_EBX);
  }
  return 0;
}
```

## Output shape

```text
Level <n> <Race> <Class>
```

Examples (product strings from dualed helpers):

- `Level 42 Human Soldier` (class English owned by residual `FUN_0051f940`)
- Race cascade: Human / Mutant / Biomek / Unknown
- Spaces: fixed `"Level "` prefix + `DAT_00a2a000` separators before race and class

## Call graph

| Role | Symbol | VA | Notes |
|---|---|---|---|
| Sole caller | `FUN_008f97c0` | `0x008f97c0` | Character sheet UI refresh; when `DAT_00d1b6d8` (player) non-null and widget `this[0x520]` set |
| Call site | — | `0x008f9806` | `PUSH player; LEA EBX,buf; CALL; ADD ESP,4` then vtbl `+0x1d8` set-text |
| Callee | `FUN_007a69d0` | `0x007a69d0` | TLS string-table object |
| Callee | `FUN_007a6de0` | `0x007a6de0` | Ring-buffer copy; returns slot ptr |
| Callee | `_itoa` | IAT | radix 10 |
| Callee (dualed) | `Character_GetRaceDisplayName_Inferred` | `0x00521800` | ECX-this → `"Human"`/`"Mutant"`/`"Biomek"`/`"Unknown"` |
| Callee (dualed) | `Character_GetClassDisplayName_Inferred` | `0x00521900` | ECX-this → class English via `FUN_0051f940` |

## Constants

| Symbol / VA | Value | Role |
|---|---|---|
| `0x00a2d280` | `"Level "` | Prefix literal |
| `DAT_00a2a000` | `0x0020` (`" \0"`) | Space separator (uint16 store) |
| vtbl `+0x27c` | method | Character level integer getter |
| `local_c[12]` | stack | itoa scratch |

## Open questions

- Product/PDB name for this formatter and for vtbl `+0x27c`.
- Full Character C++ layout (offsets only partially shared with race/class duals).
- Runtime golden string capture.

## Confidence

| Claim | Level |
|---|---|
| CF ≡ live decompile ≡ raw | **Confirmed** |
| ABI stack char* + EBX out + strlen return | **Confirmed** (call site + epilogue bytes) |
| Output = `Level n Race Class` | **Confirmed** (literals + dualed helpers) |
| Sole caller character-sheet UI | **Confirmed** (1 xref) |
| Product method English | **None** |
| vtbl+0x27c official name | **High** role (level), **None** product symbol |
