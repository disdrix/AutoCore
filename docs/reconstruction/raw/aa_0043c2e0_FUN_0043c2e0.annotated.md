# Annotated low-level: FUN_0043c2e0 → Map_TreeInsertAndRebalance_Val12_EsiBuy

| Field | Value |
|---|---|
| Stable ID | `aa_0043c2e0` |
| VA | `0x0043c2e0`–`0x0043c45f` exclusive (**383** B) |
| Canonical | `Map_TreeInsertAndRebalance_Val12_EsiBuy` |
| Ghidra | `FUN_0043c2e0` |
| Wave | W29-D OWN-ONLY |
| Source | raw + live decompile + `read_memory` 2026-07-29 |

## Role

MSVC std **map/set always-insert + RB rebalance** for **Val12** nodes (`operator_new(0x1c)`, value 12 B @ +0x0c, color @ +0x18, isnil @ +0x19). Buys via **`FUN_0046c6c0`** (ESI value convention, W28-T). Parent callers own key walk / duplicate gate.

Structural twin of `aa_0044a8c0` (`Map_TreeInsertAndRebalance_Val12_EsiBuy_Inferred`) — **same** buy helper, **different** rotate pair:

| This unit | Twin `aa_0044a8c0` |
|---|---|
| L `FUN_00446b10` | L `FUN_00573170` |
| R `FUN_0040b910` | R `FUN_00418c10` |

## ABI

| Slot | Binding | Conf |
|---|---|---|
| map | **EDI** (+0x04 head, +0x08 size); **no** `mov edi,ecx` in body | **High** |
| outIt | stack arg0 | **High** |
| addLeft | stack arg1 (char) | **High** |
| where | stack arg2 (node*) | **High** |
| value* | stack arg3 → loaded to **ESI** before buy | **High** (bytes; decompiler omitted) |
| cleanup | `add esp,0x50; ret 0x10` | **High** |
| SEH | `LAB_009bc919` | **High** |

## Node / shell layout

```
MapShell:  +0x04 head sentinel, +0x08 size
MapNode:   +0 left, +4 parent, +8 right, +0xc value[3], +0x18 color, +0x19 isnil
```

## Control flow

```
if size > 0x15555553:
  throw length_error("map/set<T> too long")  // string 0x00a1530c, ThrowInfo DAT_00acc388

ESI = value*
n = FUN_0046c6c0(head, where, head, color=0)
size++

// link
if where == head: head.parent=head.left=n; (rightmost path may set head.right)
else if addLeft: where.left=n; if where==head.left: head.left=n
else: where.right=n; if where==head.right: head.right=n

// RB while parent.color == red (0)
  uncle recolor OR rotate (FUN_00446b10 / FUN_0040b910, thiscall ECX=map via push EDI patterns)

root (head.parent).color = black
*outIt = n
ret 0x10
```

## Callers (6 functions / 23 xrefs)

`FUN_0043be60`, `FUN_00445680`, `FUN_0046c450`, `FUN_0046c5c0`, `FUN_0046f6e0`, `FUN_0046f810` — insert parents (key walk residual, not OWN).

## Gaps

- Product/MSVC demangle per map instantiation.
- Buynode null OOM path (no check in body).
- Full dual of rotate pair `00446b10` / `0040b910` (likely Val12 L/R clones; unowned).
- Runtime golden.
