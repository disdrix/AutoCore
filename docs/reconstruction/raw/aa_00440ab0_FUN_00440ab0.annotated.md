# Annotated low-level: FUN_00440ab0 → StdTree_FreeSubtree_Isnil3D_DestroyVal0C_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00440ab0` |
| VA | `0x00440ab0`–`0x00440aeb` exclusive (**59 B** / `0x3B`) |
| System | skills-abilities / gfxUIWindow tree shell @ +0x04 |
| Date | 2026-08-05 (MEGA-130 dual refresh) |
| Canonical name | `StdTree_FreeSubtree_Isnil3D_DestroyVal0C_Inferred` |
| Partition alias | `StdTree_ClearShell_At04` |
| Ghidra name | `FUN_00440ab0` |

## Machine-level notes

- Source: raw capture + MEGA-130 live re-verify (`read_memory` full body + call-site assembly).
- **Prefer bytes over decompiler** — Ghidra collapses left-walk after false-noreturn on `operator_delete`.
- Node layout (isnil@**+0x3D** family):
  - `+0x00` left, `+0x04` parent, `+0x08` right
  - `+0x0C` embedded value object (destroyed via `FUN_00442720` = `EAX+=0xC` then `FUN_00440770`)
  - `+0x3D` isnil (`0` = real node, non-zero = sentinel)
- ABI: **ECX** = tree host; stack **node\***; void; **`RET 4`**.
- Does **not** reset head/size — callers (gfxUIWindow dtor / erase-all) do empty-sentinel fixup after return.
- ECX threaded into recursive calls only (unused for loads/stores in body) — classic MSVC `_Tree` free helper.

## Annotated control flow (bytes-accurate)

```c
// ECX = tree_host*; stack arg = node*; RET 4; void
void StdTree_FreeSubtree_Isnil3D_DestroyVal0C_Inferred(void *tree_host, void *node)
{
  // CMP [node+0x3D],0 / JNZ ret
  if (*(char *)((int)node + 0x3d) != '\0') {
    return;
  }

  // left-spine loop (decompiler missed this)
  for (;;) {
    // recurse free right child
    StdTree_FreeSubtree_Isnil3D_DestroyVal0C_Inferred(
        tree_host, *(void **)((int)node + 8));

    void *left = *(void **)node;          // [node+0]

    // FUN_00442720: destroy value embedded at node+0x0C
    //   ADD EAX,0xC; PUSH EAX; CALL FUN_00440770; RET
    FUN_00442720(/* EAX = node */);

    operator_delete(node);

    node = left;
    if (*(char *)((int)node + 0x3d) != '\0') {
      break;  // hit nil sentinel
    }
  }
  // RET 4
}
```

## Decompiler vs bytes

| Stage | Decompile | Bytes |
|---|---|---|
| isnil@+0x3D early-out | Yes | Yes |
| Recurse right@+8 | Yes | Yes |
| Left-spine walk after delete | **Missing** (false-noreturn) | **Yes** (`MOV ESI,[ESI]` / `JZ` loop) |
| Value dtor `00442720` | Yes (arg recovery weak) | Yes (`MOV EAX,EDI` before CALL) |
| `operator_delete` + `ADD ESP,4` | Yes (shown noreturn) | Yes (returns) |
| `RET 4` | implied void ret | Yes (`C2 04 00`) |

## Callers

| Site | Role |
|---|---|
| `FUN_00759de0` @ `0x00759e79` | gfxUIWindow complete: free shell @ **this+0x04** then reset sentinel |
| `FUN_004413c0` @ `0x004413e0` | erase-all when range is whole tree |
| `Catch@004424ff` @ `0x00442506` | EH free of staged node |
| self @ `0x00440ac7` | right-subtree recursion |

## Open questions

- Product English for map key/value type (value dtor `00440770` / base `00440230`).
- Exact node size (isnil@+0x3D implies ≥0x3E; likely 0x40-aligned family).
- Peer duals MEGA-131 (`00440c90` isnil1D) / MEGA-132 (`00440d20` isnil11) residual.
