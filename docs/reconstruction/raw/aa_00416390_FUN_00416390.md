# Raw capture: FUN_00416390

| Field | Value |
|---|---|
| **Stable ID** | `aa_00416390` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00416390` |
| **Canonical name** | `FUN_00416390` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __fastcall FUN_00416390(LPCRITICAL_SECTION param_1)

{
  void *local_14;
  undefined *puStack_10;
  undefined *puStack_c;
  undefined4 local_8;
  
  puStack_c = &DAT_00aaa698;
  puStack_10 = &DAT_00489cb0;
  local_14 = ExceptionList;
  local_8 = 0;
  ExceptionList = &local_14;
  InitializeCriticalSection(param_1);
  ExceptionList = local_14;
  return 0;
}
```

---

## Re-verify (WQ9I-I, 2026-08-04)

| Field | Value |
|---|---|
| **Tools** | `batch_decompile` + `analyze_function_complete` + `read_memory` + callers/xrefs + `get_assembly_context` |
| **Not used** | `disassemble_bytes`; Launcher |
| **Live decompile** | ≡ raw success path (SEH setup + `InitializeCriticalSection` + return 0). Exception arms under-shown in decompiler; sealed from **bytes**. |
| **Body** | `0x00416390`–`0x0041640A` exclusive (**122 B** / `0x7A`); pad `CC` before `0x00416410` |
| **ABI** | **ECX** = `LPCRITICAL_SECTION`; **EAX** = **HRESULT** (0 success; failure codes below); plain **`RET`** (`C3`) |
| **Semantics** | MSVC SEH-wrapped **`InitializeCriticalSection(cs)`** converting SEH exceptions to HRESULT |
| | 1. Frame: `PUSH -1`; scope table `DAT_00aaa698` (`FF FF FF FF` + filter `0x004163C4` + handler `0x004163D4`); handler `DAT_00489cb0`; install `FS:[0]`. |
| | 2. EH state `0`; `PUSH ECX`; **`CALL dword ptr [0x009C61EC]`** (IAT → `InitializeCriticalSection`). |
| | 3. **Success:** `JMP` epilogue; **EAX = 0**. |
| | 4. **Filter** (`0x004163C4`): capture exception code → stack; return **1** (`EXCEPTION_EXECUTE_HANDLER`). |
| | 5. **Handler:** `code - 0xC0000017` (`STATUS_NO_MEMORY`); if equal → **`0x8007000E`** (`E_OUTOFMEMORY`); else → **`0x80004005`** (`E_FAIL`) via `NEG/SBB/AND 0xFFF93FF7/ADD 0x8007000E`. |
| **Classification** | worker |
| **Callees** | `InitializeCriticalSection` (IAT `[0x009c61ec]`) |
| **Callers (1)** | `FUN_00489dc5` @ `0x00489e50` — `LEA ECX,[ESI+0x18]`; CALL; `TEST EAX,EAX` / `JGE` success else set `DAT_00d1f0f0 = 1` |
| **Name (inferred)** | `CriticalSection_Initialize_SehToHresult_Ecx_Inferred` |
| **SEH table** | `read_memory` `0x00aaa698`: `ffffffff c4634100 d4634100` → filter `0x004163C4`, handler `0x004163D4` |
| **Pair** | Pre-zero of same `+0x18` CS region via `Mem_ZeroSixDwords_Ecx_Inferred` (`0x00416370`) in `FUN_00489d95` |
