# Annotated low-level: StdTree_EraseNode_Val12 / FUN_005ae990

| Field | Value |
|---|---|
| Stable ID | `aa_005ae990` |
| VA | `0x005ae990` |
| Body | `0x005ae990`–`0x005aec44` exclusive (**692** B); Ghidra lists end `005aec16` (false noreturn) |
| Canonical | `StdTree_EraseNode_Val12` (inferred) |
| System | MSVC std `_Tree` single-node erase + RB rebalance (Val12 / node 0x1c) |
| Date | 2026-07-29 W27-T |
| Live tools | `decompile_function` + `read_memory` (**no** `disassemble_bytes`) |

## Machine-level notes

- **ABI:** `__thiscall` ECX = map shell (`head@+4`, `size@+8`); stack `(outIt*, node*)`; **`ret 8`**.
- **Nil gate:** if `node->isnil` (@+0x19) != 0 throw `"invalid map/set<T> iterator"` (ThrowInfo `DAT_00acc34c`).
- **Successor:** `FUN_005ae0b0` advances iterator (in-order next) for return value.
- **Unlink:** select replacement child (left/right/nil rules); reparent; update root / begin (`FUN_005adfa0` leftmost) / end (`FUN_00418bf0` rightmost).
- **RB fixup:** if erased node was black (`color@+0x18 == 1`), classic sibling recolor/rotate via `FUN_00573170` / `FUN_00418c10` until root.
- **Delete:** `operator_delete(node)` — **falls through** (decomp WARNING noreturn is false).
- **Epilogue (byte-sealed):** if `map->size > 0` then `size--`; `*outIt = successor`; restore SEH; `add esp,0x54; ret 8`.
- **Sole caller:** `FUN_005af3a0` range erase (loops this for [first,last); also whole-tree clear path separate).

## Layout (Val12)

| Offset | Field |
|---|---|
| node+0x00 | left |
| node+0x04 | parent |
| node+0x08 | right |
| node+0x0c | value[12] |
| node+0x18 | color (0=red, 1=black) |
| node+0x19 | isnil |
| map+0x04 | head |
| map+0x08 | size |

## Pseudocode (annotated)

```c
// __thiscall; ret 8
void StdTree_EraseNode_Val12(MapShell* map, Node** outIt, Node* node)
{
  if (node->isnil)
    throw_invalid_map_set_iterator(); // "invalid map/set<T> iterator"

  Node* succ = node;
  FUN_005ae0b0(&succ); // in-order successor for *outIt

  // unlink node; splice replacement; fix begin/end/root
  // if node was black: RB erase fixup (rotates L/R)
  operator_delete(node); // falls through

  if (map->size > 0)
    map->size--;
  *outIt = succ;
}
```

## Confidence

| Claim | Level |
|---|---|
| CF unlink + rebalance | **High** (live decomp) |
| isnil@+0x19 / color@+0x18 | **High** |
| size-- + *outIt + ret 8 | **High** (bytes past delete) |
| Sole caller FUN_005af3a0 | **High** |
| Product demangle | **Inferred** / open |
| Runtime / differential | Open |
