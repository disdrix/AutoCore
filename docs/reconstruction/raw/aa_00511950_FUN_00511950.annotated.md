# Annotated low-level: FUN_00511950 → StdTree_InsertAndRebalance_Node24_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00511950` |
| VA | `0x00511950`–`0x00511b3a` inclusive (**491 B** / `0x1EB`) |
| System | MSVC std `_Tree` always-insert + RB rebalance (isnil@+0x21 / node **0x24**) |
| Date | 2026-08-05 (R12-014 OWN dual) |
| Ghidra | `FUN_00511950` |
| Promoted | `StdTree_InsertAndRebalance_Node24_Isnil21_Inferred` |

## Machine-level notes

- Live re-verify 2026-08-05: decompile ≡ raw CF; **RET 0x10**; size gate `0xCCCCCCB`; color@+0x20; isnil@+0x21.
- **Node 0x24** (nested buynode `FUN_005118b0` `operator_new(0x24)`), distinct from Val16/node0x28 peer `00407200`.
- Layout: left@+0, parent@+4, right@+8, **int key @+0x0C**, vector payload @+0x10.., **color@+0x20**, **isnil@+0x21**.
- Tree shell: this+4 = head; this+8 = size; head+0 leftmost, head+4 root, head+8 rightmost.
- Always-insert: no key compare in this body; parent `FUN_00511b40` does lower-bound / uniqueness on int key.
- Rotates: `StdTree_Lrotate_Isnil21_Inferred` `0050e9f0` (dualed R10-030); `StdTree_Rrotate_Isnil21_Inferred` `005a27f0` (dualed R11-007 parent dual). One Lrotate path is **inlined** (right-parent uncle-black final step).
- Max size gate **0xCCCCCCB** (not Val16 peer's `0xFFFFFFE`).
- SEH frame `LAB_009a31f2`; throw path builds length_error with `"map/set<T> too long"`.

## Pseudocode (annotated)

```c
// __thiscall; ECX = map*; stack = (Node** out, char addLeft, Node* where, const Value* value)
// void; RET 0x10; *out = newly inserted node
void __thiscall StdTree_InsertAndRebalance_Node24_Isnil21_Inferred(
    MapShell* map, Node** out, char addLeft, Node* where, const void* value)
{
  // SEH: LAB_009a31f2
  if (map->size >= 0xCCCCCCBu) {
    // length_error("map/set<T> too long") — non-returning _CxxThrowException
  }

  Node* head = map->head;
  // buynode 005118b0: new(0x24); links=head/where/head; color=red(0); isnil=0; copy value
  Node* n = FUN_005118b0(head, where, head, value, /*red*/ 0);
  map->size += 1;

  if (where == head) {
    head->parent = n;   // root
    head->left = n;     // leftmost
    head->right = n;    // rightmost
  } else if (addLeft == 0) {
    where->right = n;
    if (where == head->right) head->right = n;
  } else {
    where->left = n;
    if (where == head->left) head->left = n;
  }

  // RB insert-fixup while parent->color == red (0)
  Node* x = n;
  while (x->parent->color == 0) {
    Node* p = x->parent;
    Node* gp = p->parent;
    if (p == gp->left) {
      Node* uncle = gp->right;
      if (uncle->color == 0) {
        p->color = 1; uncle->color = 1; gp->color = 0;
        x = gp;
      } else {
        if (x == p->right) {
          FUN_0050e9f0(map, p); // Lrotate
          x = p;
        }
        x->parent->color = 1;
        x->parent->parent->color = 0;
        FUN_005a27f0(map, x->parent->parent); // Rrotate
      }
    } else {
      Node* uncle = gp->left;
      if (uncle->color == 0) {
        p->color = 1; uncle->color = 1; gp->color = 0;
        x = gp;
      } else {
        if (x == p->left) {
          FUN_005a27f0(map, p); // Rrotate
          x = p;
        }
        x->parent->color = 1;
        x->parent->parent->color = 0;
        // Lrotate inlined (isnil@+0x21 nil-child parent link)
      }
    }
  }
  map->head->parent->color = 1; // root black
  *out = n;
}
```

## Control flow

| Stage | Action |
|---|---|
| 1 | size ≥ 0xCCCCCCB → throw length_error |
| 2 | buynode red Node24; size++ |
| 3 | link under where (empty / left / right + extremum update) |
| 4 | while parent red: uncle recolor or L/R rotate |
| 5 | root black; *out = n; RET 0x10 |

## Open questions

- Product / MSVC demangle for map value_type (int key + vector of 0x10 elements).
- Full dual of nested buynode `005118b0` and parent InsertOrFind `00511b40` (not OWN).
- Runtime / bit-exact / differential.
