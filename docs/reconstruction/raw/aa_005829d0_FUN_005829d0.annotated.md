# Annotated low-level: NDSpecialFX_BindHostRefreshAndWalkRelated_Inferred (FUN_005829d0)

| Field | Value |
|---|---|
| Stable ID | `aa_005829d0` |
| VA | `0x005829d0`–`0x00582a10` (**65 B** / `0x41`) |
| Ghidra name | `FUN_005829d0` |
| Canonical name | `NDSpecialFX_BindHostRefreshAndWalkRelated_Inferred` |
| Prior scaffold alias | `Named_CalleeOf_Drive_s_VEHICLE_EXHAUST_d_005829d0` — **too narrow** (multi-caller surface) |
| System | client-fx / NDSpecialFX host bind wrapper |
| Date | 2026-07-29 (W24-A dual seal) |
| Tools | `decompile_function` + `read_memory` + `analyze_function_complete` (no `disassemble_bytes`) |

## Machine-level notes

- **thiscall** host object in **ECX**; **3** stack formals; epilogue **`ret 0xC`**.
- Stage 1: MI-adjust host this via `*(this-0x8C)` / `+4` lea, then call **`FUN_00518590`** (`NDSpecialFX_BindHostAndRefresh_Inferred`, peer dual) with stack args `param_2`, `param_3`, `param_4`.
- Stage 2: if **`*(this-0x88) != 0`**, call **`FUN_004b7030`** with:
  - **ECX** = `param_2` (FX session/template — same as stack arg1)
  - stack: related object, `0`, `0`, `-1` (`0xFFFFFFFF`)
- Thin wrapper; no local state mutation beyond callee side effects.
- Callers include hardpoint expansion (`004ff850`) and at least one other host path (`005a9760`). **12** xrefs total — not vehicle-exhaust-only.
- Residual: product role of `this-0x88` related pointer; full `004b7030` semantics (W24-D OWN); BindHost stack arity 2 vs 3-push site (see below).

## Control flow (sealed)

```
host = ECX
fx   = stack arg1 (param_2)
a2   = stack arg2 (param_3)
a3   = stack arg3 (param_4)

adj = *(host - 0x8C)
ecx_bind = adj[1] + host - 0x8C     // MI this-adjust
FUN_00518590(ecx_bind, fx, a2, a3)  // peer: BindHostAndRefresh (arity residual)

related = *(host - 0x88)
if related != 0:
  FUN_004b7030(ecx=fx, related, 0, 0, -1)
return  // ret 0xC
```

## ABI / MI notes

| Item | Evidence | Conf |
|---|---|---|
| thiscall ECX = host | `8B F1` | **High** |
| ret 0xC (3 stack formals) | `C2 0C 00` | **High** |
| MI lea at `this-0x8C` | `8B 96 74 FF FF FF` / `8D 8C 30 74 FF FF FF` | **High** |
| Related field `this-0x88` | `8B B6 78 FF FF FF` | **High** |
| Walk ECX = param_2 | `8B CF` before `004b7030` | **High** |
| Walk args `(related,0,0,-1)` | `6A FF 6A 00 6A 00 56` | **High** |
| BindHost stack arity | 3 pushes here; peer dual lists 2 | **Partial** |

## Open questions

1. Product English for the object at `this-0x88` (sibling FX? attachment node? host child?).
2. Full `FUN_004b7030` contract (OWN residual / W24-D).
3. Whether BindHost cleans 8 or 12 stack bytes (peer dual vs this 3-push site).
4. Class owner of the MI layout that places this method at the adjusted `this`.

## Pseudocode (annotated)

```c
// thiscall host; stdcall-ish 3 stack formals; ret 0xC
void __thiscall NDSpecialFX_BindHostRefreshAndWalkRelated_Inferred(
    int hostThis,           // ECX — often MI-adjusted subobject pointer
    void *fxSession,        // Stack[0x4] — FX template/session (param_2)
    uint32_t arg2,          // Stack[0x8]  — forwarded to BindHost
    uint32_t arg3)          // Stack[0xC]  — forwarded to BindHost
{
  int *mi = *(int **)(hostThis - 0x8C);
  int adjusted = mi[1] + hostThis - 0x8C;

  // Peer: NDSpecialFX_BindHostAndRefresh_Inferred
  // Ghidra display flattens adjusted ECX into the call:
  FUN_00518590(/*ECX=adjusted host*/, fxSession, arg2, arg3);

  int related = *(int *)(hostThis - 0x88);
  if (related != 0) {
    // ECX = fxSession for thiscall walk helper
    FUN_004b7030(/*ECX=fxSession*/, related, 0, 0, 0xFFFFFFFF);
  }
}
```
