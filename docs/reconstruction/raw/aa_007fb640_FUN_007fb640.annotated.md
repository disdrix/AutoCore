# Annotated low-level: Client_DismissProgressBar_Inferred (`FUN_007fb640`)

| Field | Value |
|---|---|
| Stable ID | `aa_007fb640` |
| VA | `0x007fb640` |
| System | skills-abilities / client-ui |
| Date | 2026-07-23 scaffold / 2026-07-29 dual |

## Machine-level notes

- **ESI = Client\*** for entire body (decompiler `unaff_ESI`).
- Zero stack formals; plain `RET` at no-op exit.
- Final dismiss is **`JMP [vtbl+0x440]`** (tail call), not `CALL`. Decompiler “jumptable” warning is a false positive for a single indirect JMP.
- Prefer assembly when decompiler conflicts (esp. ESI + tail JMP).

## Pseudocode (annotated, asm-corrected)

```c
// ESI = Client*
void Client_DismissProgressBar_Inferred(void)
{
  // bar @ client+0x1120
  if (*(int *)(ESI + 0x1120) == 0)
    return;

  // bool probe: bar.vtbl+0x3d8
  char visibleOrActive =
      (**(code **)(**(int **)(ESI + 0x1120) + 0x3d8))();
  if (visibleOrActive == 0)
    return;

  // optional detach: if bar+0x2b0, host(client+0xf38).vtbl+0xb0(bar)
  if (*(int *)(*(int *)(ESI + 0x1120) + 0x2b0) != 0) {
    (**(code **)(**(int **)(ESI + 0xf38) + 0xb0))(*(int *)(ESI + 0x1120));
  }

  // tail-call hide/dismiss: bar.vtbl+0x440
  (**(code **)(**(int **)(ESI + 0x1120) + 0x440))();
  // (asm: JMP — does not return here)
}
```

## Offsets

| Disp | Role |
|---|---|
| `client+0x1120` | Progress-bar widget\* |
| `client+0xf38` | UI host/manager for detach (`vtbl+0xb0`) |
| `bar+0x2b0` | Attachment gate (non-zero → detach) |
| `bar.vtbl+0x3d8` | Bool probe (can-dismiss / is-active) |
| `bar.vtbl+0x440` | Hide/dismiss (tail) |

## Open questions

- Product class name for `+0x1120` widget.
- English names for `+0x3d8` / `+0x440` / host `+0xb0`.
- Whether `+0xf38` is the same object family as show-path `+0xf40`.
