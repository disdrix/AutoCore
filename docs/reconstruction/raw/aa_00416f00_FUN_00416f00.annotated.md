# Annotated low-level: Class_00a733ec_ScalarDeletingDtor_Inferred (FUN_00416f00)

| Field | Value |
|---|---|
| Stable ID | `aa_00416f00` |
| VA | `0x00416f00`–`0x00416f2e` inclusive (**47 B**) |
| Canonical name | `Class_00a733ec_ScalarDeletingDtor_Inferred` |
| System | MSVC scalar-deleting destructor |
| Date | 2026-08-05 (WQ9J-E dual seal) |

## Machine-level notes

- **ABI:** `__thiscall` — ECX = this; stack flags; **`RET 4`**; returns this* in EAX.
- **Vtbl:** always writes `PTR_FUN_00a733ec` at `*this` (slot0 of that vtbl **is** this function @ `0x00a733ec`).
- **Complete teardown is inline:** if `this+0x10` free callback ≠ null, call it **cdecl** with `this+0x04` context (`ADD ESP,4` after CALL).
- **Scalar free:** if `(flags & 1)`, `operator_delete(this)` then fall through (decomp noreturn WARNING is **false**).
- **Not** array-deleting (`flags&2` never tested).
- Factories (not OWN): `FUN_00416830` (callback=`LAB_00573240`), `FUN_004168c0` (callback=null); both `operator_new(0x14)`.
- Adjacent UTF-16 `"UNI_BUTTONS"` after vtbl is **not** sealed as product class English.

## Pseudocode (byte-faithful)

```c
// ECX = self; stack flags; RET 4; returns self
void *__thiscall Class_00a733ec_ScalarDeletingDtor_Inferred(void *self, uint8_t flags)
{
  void (__cdecl *cb)(void *) = *(void (**)(void *))((char *)self + 0x10);
  *(void **)self = &PTR_FUN_00a733ec;          // reinstall vtbl

  if (cb != NULL) {
    cb(*(void **)((char *)self + 4));          // free context
  }

  if ((flags & 1) != 0) {
    operator_delete(self);                     // returns
  }
  return self;
}
```

## Open questions

- Product / RTTI demangle for class behind `PTR_FUN_00a733ec`.
- Identity of `LAB_00573240` free callback (factory path only).
- Runtime / bit-exact / differential.
