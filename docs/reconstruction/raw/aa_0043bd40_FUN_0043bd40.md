# Raw capture: FUN_0043bd40

| Field | Value |
|---|---|
| **Stable ID** | `aa_0043bd40` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0043bd40` |
| **Canonical name** | `FUN_0043bd40` |
| **System** | std red-black map operator[] / get-or-insert (Val12, u32 key) |
| **Capture timestamp** | `2026-08-04` |
| **Tool** | Ghidra MCP `batch_decompile` / `decompile_function` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 * __fastcall FUN_0043bd40(int param_1)

{
  uint *in_EAX;
  undefined4 *puVar1;
  undefined4 *puVar2;
  int *piVar3;
  undefined4 *puVar4;
  
  puVar4 = *(undefined4 **)(param_1 + 4);
  if (*(char *)((int)puVar4[1] + 0x19) == '\0') {
    puVar1 = (undefined4 *)puVar4[1];
    do {
      if ((uint)puVar1[3] < *in_EAX) {
        puVar2 = (undefined4 *)puVar1[2];
      }
      else {
        puVar2 = (undefined4 *)*puVar1;
        puVar4 = puVar1;
      }
      puVar1 = puVar2;
    } while (*(char *)((int)puVar2 + 0x19) == '\0');
  }
  if ((puVar4 != *(undefined4 **)(param_1 + 4)) && ((uint)puVar4[3] <= *in_EAX)) {
    return puVar4 + 4;
  }
  piVar3 = (int *)FUN_0043be60(puVar4);
  return (undefined4 *)(*piVar3 + 0x10);
}
```

## W37-Q re-verify (2026-08-04) — append only

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual W37-Q |
| **Tools** | `batch_decompile`, `decompile_function`, `analyze_function_complete`, `get_function_by_address`, `get_xrefs_to`, `read_memory` (**no** `disassemble_bytes`) |
| **Live decompile** | ≡ capture above (`in_EAX` = key pointer is real register ABI, not phantom) |
| **Body** | `0x0043bd40`–`0x0043bda5` exclusive (**101** B / `0x65`); pad `CC` after final `RET` |
| **Prologue** | `mov edx,[ecx+4]` (head); `sub esp,0x10`; `mov esi,eax` (save key*) |
| **Epilogue** | `add esp,0x10; ret` (plain **`RET`**, both hit + miss) |
| **ABI** | **ECX** = map shell (`head` @ `+4`); **EAX** = `uint32_t* key`; **no stack args**; returns `mapped*` at `node+0x10` |
| **Callees** | `FUN_0043be60` only (insert-hint helper; unowned) |
| **Callers (5 xrefs)** | `FUN_0073e3a0` @ `0073e43e`; `FUN_0096eec0` @ `0096ef4a`; `FUN_0072a660` @ `0072a974`; `FUN_0098d870` @ `0098d983`; site `00733896` |
| **Node / Val12** | key@`+0x0c` (**unsigned** compare); isnil@`+0x19`; mapped @`+0x10`/`+0x14`; color@`+0x18` |
| **Inferred name** | `StdTree_OperatorIndex_Val12_U32Key_Eax_Inferred` |
| **Full body hex (101 B)** | `8b510483ec10568bf08b4204807819007518578b3e39780c73058b4008eb048bd08b008078190074ec5f3b510474078b063b420c73288b3633c0897424088944240c89442410528d44240c8d742408e8cc0000008b0083c0105e83c410c38d42105e83c410c3` |

### Semantics seal

1. Start at map head (`*(this+4)`); root = `head->parent`/`head[1]` (MSVC header left/parent layout as used by peer Val12 ops).
2. If root not isnil (`byte[node+0x19]==0`): **unsigned lower_bound** — if `node.key < *key` go right (`node[2]`), else keep candidate and go left (`*node`).
3. **Hit** if candidate ≠ head and `candidate.key <= *key` (equality under lower_bound): return `candidate + 4` ≡ `node+0x10`.
4. **Miss**: stage Val12 `{*key, 0, 0}` on stack; call `FUN_0043be60` insert-hint with candidate; return `*outIt + 0x10`.
5. Operator[]-style unique map get-or-insert; default-mapped zeros on create.
6. **Not** the signed thiscall+`ret 4` twin `StdTree_OperatorIndex_Val12` (`0x0053af20`) — different ABI + unsigned key.

### Context

- Callers write two dwords into returned mapped: e.g. `{&LAB_0073e260, 0}`, `{&LAB_0096ed30, 0}`, or `{ptr, 0}` in Z-fill path.
- Same Val12 isnil@+0x19 family as W29-Q `0x0053af20` / W28-Q InsertHint.
