# Annotated low-level: FUN_00965ab0 / MeshWeightDebugOverlay_Update

| Field | Value |
|---|---|
| Stable ID | `aa_00965ab0` |
| VA | `0x00965ab0`–`0x0096650A` exclusive (**2650 B** / `0xA5A`) |
| System | Client gfx mesh-weight debug overlay |
| Date | 2026-07-29 (W36-N OWN-ONLY dual) |
| Canonical name | `MeshWeightDebugOverlay_Update` |

## Machine-level notes

- Large SEH function; Ghidra type propagation is noisy (float/int punning on stack colors and strings). Prefer sealed control flow + string domain + callee contracts over stack var names.
- **EDI** holds overlay object for the body (`mov edi,[ebp+8]`).
- Sole caller is gfx BeginScene helper `FUN_0075fb90` (path string `graphics\gfxDevice.cpp`) — not a device method itself; overlay object lives at **device+0x7A0**.
- Fade rate: flag `@+0x40==0` uses **−0.005f** (`0x00aaa9f4`); else **+0.005f** (`g_flHardKillInterpolate` @ `0x00aaa6a4`). Progress `@+0x48` clamped to **[0,1]**.
- When progress is 0: call UI object vtbl+4 and return (hidden).
- Rebuild path uses sealed **`StdVector_Resize_RcElem28`** (`0x004611e0`) to clear/prepare an RC-elem28 line list, then **`FUN_00460f90`** push_back for each drawn line element.
- Mesh-entry iteration: vector `@+0x5c..+0x60` with **stride 0xC** (`piStack_13c += 3`).

## ABI (sealed)

```
void MeshWeightDebugOverlay_Update(Overlay* self);  // stdcall RET 4
```

| Formal | Source | Conf |
|---|---|---|
| self* | stack `[ebp+8]` → EDI | **High** |
| RET 4 | caller `push esi; call; mov ecx,esi` no `ADD ESP` | **High** |

## High-level control flow

1. SEH prolog; `self = [ebp+8]`.
2. `now = FUN_0076c3c0()`; `dt = now - self->lastTime(+0x30)`; unsigned fixup via `2^32` if negative-as-int.
3. `delta = dt * rate` (sign/rate from `@+0x40`).
4. `progress = clamp(self->progress(+0x48) + delta, 0, 1)`; store back.
5. If `progress == 0`: UI vtbl+4; return.
6. If `!dirty(+0x41) && progress == 1`: skip rebuild (still may fall through lightly).
7. Rebuild (dirty or mid-fade):
   - Begin UI nesting on `self->ui(+0x34)` (vtbl+8/+4, `FUN_00758c80`, nest counter `@+0x68`).
   - `alphaByte = round(progress * 255)`.
   - If mesh vector empty: single placeholder string path (`FUN_0040aeb0` + draw).
   - Else: construct zero-resource `RcElem28` fill value; **`FUN_004611e0` resize** (clear lines); iterate meshes/modes; sprintf labels; push UI elems via **`FUN_00460f90`** with RC AddRef/Release on font resource `@+0x3c`.
   - End nesting; layout rects (`FUN_00758460` / `FUN_00757ff0` / `FUN_00759090`); apply align flags `@+0x54`; write UI origin `@+0x78/+0x7c`; **`dirty = 0`**.
8. Return (ExceptionList restored).

## Object field map (partial, sealed by use)

| Off | Role |
|---|---|
| +0x30 | last timestamp (uint) |
| +0x34 | UI/draw context* (vtbl) |
| +0x38 | parent/metrics* |
| +0x3c | shared font/resource (RC) |
| +0x40 | rate-mode byte |
| +0x41 | dirty byte |
| +0x48 | progress float [0,1] |
| +0x4c | x offset |
| +0x50 | y offset |
| +0x54 | align flags (bit0 x, bit1 y) |
| +0x5c | mesh entries begin* |
| +0x60 | mesh entries end* (elem size 0xC) |

## Callers / callees (owned attention)

| Dir | VA / symbol | Note |
|---|---|---|
| Caller | `0x0075fb90` @ `0x0075fc18` | gfx BeginScene path |
| Callee | `0x004611e0` | `StdVector_Resize_RcElem28` (W35-Q sealed) |
| Callee | `0x00460f90` | push_back RC-elem28 (unowned) |
| Callee | `0x0076c3c0` | time base |

## Gaps

- Full product class / PDB name.
- Complete UI draw primitive contracts (`00759730`, `00755ab0`, …).
- Exact mesh-entry struct beyond stride 0xC and used fields.
- Bit-exact / runtime.
