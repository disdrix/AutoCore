# Raw capture: FUN_00508c00

| Field | Value |
|---|---|
| **Stable ID** | `aa_00508c00` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00508c00` |
| **Canonical name** | `FUN_00508c00` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int __thiscall FUN_00508c00(int param_1,int param_2)

{
  int iVar1;
  int *piVar2;
  
  iVar1 = 0;
  if ((param_2 != -1) && (0 < DAT_00d02988)) {
    piVar2 = *(int **)(param_1 + 0x54);
    do {
      if (*(int *)(*piVar2 + 0x6c) == param_2) {
        return (*(int **)(param_1 + 0x54))[iVar1];
      }
      iVar1 = iVar1 + 1;
      piVar2 = piVar2 + 1;
    } while (iVar1 < DAT_00d02988);
  }
  return 0;
}
```

---

## W20-R re-verify (2026-07-29) — dual A/B seal

| Field | Value |
|---|---|
| **Agent** | W20-R OWN-ONLY |
| **Tools** | `decompile_function` + `read_memory` + `get_function_by_address` + callers/xrefs (no `disassemble_bytes`) |
| **Live decompile** | **Identical** to 2026-07-23 raw body |
| **Body range** | `0x00508c00`–`0x00508c41` (**66 bytes**) |
| **Inferred name** | `HandlerTable54_FindById6c` |

### `read_memory` @ `0x00508c00` length 66 (hex)

```
578b7c240833c083ffff74298b158829d00085d253567e198b71548bce8d4900
8b19397b6c741283c00183c1043bc27cef5e33c05b5fc204008b04865e5b5fc20400
```

### Byte-level CF seal (no disassemble_bytes; reconstructed from hex)

```text
push edi
mov  edi, [esp+8]          ; id (stack arg after push)
xor  eax, eax              ; i = 0
cmp  edi, -1
jz   ret0                  ; id==-1 → 0
mov  edx, [DAT_00d02988]   ; global count
test edx, edx
push ebx
push esi
jle  ret0_pops             ; count<=0 → 0
mov  esi, [ecx+0x54]       ; table base (this+0x54)
mov  ecx, esi
; align nop: lea ecx,[ecx+0]
loop:
  mov  ebx, [ecx]          ; entry*
  cmp  [ebx+0x6c], edi     ; entry->id @ +0x6c
  jz   found
  add  eax, 1
  add  ecx, 4
  cmp  eax, edx
  jl   loop
  pop  esi
  xor  eax, eax
  pop  ebx
  pop  edi
  ret  4
found:
  mov  eax, [esi+eax*4]    ; return table[i]
  pop  esi
  pop  ebx
  pop  edi
  ret  4
```

### ABI

- `__thiscall`: **ECX** = owner/registry with pointer table at **`+0x54`**
- Stack: **one dword** id; **`ret 4`**
- Return: entry pointer (`void*`) or **0**

### Family (same pattern, different table slot / count)

| VA | Table offset | Count global |
|---|---|---|
| `0x00508ac0` | `+0x44` | `DAT_00d02974` |
| `0x00508bb0` | `+0x50` | `DAT_00d02980` |
| **`0x00508c00`** | **`+0x54`** | **`DAT_00d02988`** |
| `0x00508c50` | `+0x58` | `DAT_00d02984` |

All match `*(entry+0x6c) == id`. Equip path uses this unit for item-def subtype **0xb** handlers (`Vehicle_ApplyItemStatusHooksOnEquip` dual).

### Callers (Ghidra)

`FUN_004fe380`, `FUN_005092b0`, `FUN_00509410`, `FUN_00509550`, `FUN_00509680`, `FUN_005097b0`, `FUN_00509940`, `FUN_00513580`.

### Gaps

- Product/PDB name of registry and of entry type at table slots
- Static image `DAT_00d02988` is zero (runtime-filled)
- Exact semantic of subtype **0xb** vs **0xa** twin `00508c50` beyond equip dual notes
