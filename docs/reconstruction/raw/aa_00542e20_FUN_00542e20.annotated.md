# Annotated low-level: FUN_00542e20 / Host_DrainPendingNfxEventQueue_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00542e20` |
| VA | `0x00542e20`–`0x00543050` (**561 B**) |
| System | host pending NFX / special-FX event queue |
| Date | 2026-07-29 (W35-F OWN-ONLY dual) |
| Canonical | `Host_DrainPendingNfxEventQueue_Inferred` |

## Machine-level notes

- **ABI:** **ECX = host**; one stack dword (**RET 4**). Sole caller loads ECX from `DAT_00b041fc` and pushes `*(session+0xe4e8)`.
- Body primary state machine uses **ECX host** fields only in decompile; stack arg is ABI-present (port must preserve RET 4).
- Local `char[260]` for path strip of `"_nfx.xml"`.
- List: sentinel at `host+0xf28`, count/flag at `host+0xf2c`.
- Hash table: `host+0xf0c` (mask +8, bucket base +0x10).
- Node payload (entry after list links, decompile uses `iVar1 = **list` then +8 adjust in bytes for payload start): TFIDs, skill/event ids, ring at +0x44, flags +0x40/+0x58.

## Control flow (annotated)

```
if (host->queueCount == 0) return;
loop:
  node = host->list.front
  ready = PreloadRing_TryDrain(node.ring, &flag40, &cursor)  // FUN_004ed310
  if (!ready) break
  row = HashLookup(host->map, node.key_2c)
  if (!row):
    unlink+delete front; continue
  if (node.boundFlag_58 == 0):
    evt = BindForEvent(node.skillId, 1, -1)  // FUN_004a16d0
    if (!evt): unlink+delete; continue
    a = ResolveTFID(node.tfid_18); b = ResolveTFID(node.tfid_8)
    if (a) a->vtbl[0x238/4](...payload..., b, ..., evt)
    unlink+delete; continue
  else:
    maybe LoadMasterFromXML(strip _nfx.xml from front string)
    node.boundFlag_58 = 0
    ResizeZeroFill(node.vec, 0)  // FUN_0043d650
    reset cursor/flag; walk tree FUN_004a04f0(node.ringTree,1,1,1)
  if (host->queueCount == 0) return
if (!node.flag40) FinishRing(node.ring)  // FUN_00971820
```

## Call site (bytes)

`FUN_0094b520` (client frame): after load-queue block when `client+0xe04 != 0`:

```
mov eax, [client+0xe04]
mov edx, [eax+0xe4e8]
mov ecx, [DAT_00b041fc]
push edx
call FUN_00542e20   ; RET 4
```

## Pseudocode source

See raw capture body + W35-F re-verify append in `aa_00542e20_FUN_00542e20.md`.
