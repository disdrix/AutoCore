# Annotated low-level: FUN_00575c00 / Object_009d33c4_ScalarDeletingDtor

| Field | Value |
|---|---|
| Stable ID | `aa_00575c00` |
| VA | `0x00575c00`–`0x00575c1e` exclusive (**30 B** / `0x1E`) |
| Semantic name | `Object_009d33c4_ScalarDeletingDtor` |
| System | object / scalar deleting destructor |
| Date | 2026-07-29 (W29-H) |

## Machine-level notes

- Source: raw capture + W29-H `read_memory` seal.
- MSVC scalar deleting dtor: complete dtor then optional `operator_delete` when `(flags & 1)`.
- Installed as **vtbl[0]** of `PTR_FUN_009d33c4` (bytes at `0x009d33c4` = `00 5C 57 00` LE → `0x00575c00`).
- `RET 4` cleans the flags stack formal.
- Ghidra false-noreturn on `operator_delete` — fall-through returns `this`.
- Do **not** merge with complete dtor `FUN_00575350` or base dtor `FUN_00518ec0`.

## Pseudocode (annotated)

```c
// __thiscall; ECX=this; stack flags; RET 4
void *Object_009d33c4_ScalarDeletingDtor(void *self, unsigned char flags)
{
  Object_009d33c4_CompleteDtor(self); // FUN_00575350
  if ((flags & 1) != 0) {
    operator_delete(self);            // fall-through (not noreturn)
  }
  return self;
}
```

## Open questions

- Product demangle for derived type (same gap as complete dtor / ctor).
- Array-deleting path (flags & 2) not present — scalar only (no vector deleting dtor in this slot).
