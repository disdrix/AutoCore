# Raw capture: FUN_00409bd0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409bd0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409bd0` |
| **Canonical name** | `FUN_00409bd0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00409bd0(int param_1,int param_2,int param_3,undefined4 param_4)

{
  int iVar1;
  int local_8;
  int local_4;
  
  iVar1 = param_2 - param_1;
  do {
    iVar1 = iVar1 >> 4;
    if (iVar1 < 0x21) {
LAB_00409c67:
      if (1 < iVar1) {
        FUN_0040a410(param_1,param_2,param_4);
      }
      return;
    }
    if (param_3 < 1) {
      if (0x20 < iVar1) {
        if (0x10 < (int)(param_2 - param_1 & 0xfffffff0U)) {
          FUN_0040a820(param_1,param_2,param_4);
        }
        FUN_0040a380(param_1,param_2,param_4);
        return;
      }
      goto LAB_00409c67;
    }
    FUN_00409f90(&local_8,param_1,param_2,param_4);
    iVar1 = local_4;
    param_3 = param_3 / 2 + (param_3 / 2) / 2;
    if ((int)(local_8 - param_1 & 0xfffffff0U) < (int)(param_2 - local_4 & 0xfffffff0U)) {
      FUN_00409bd0(param_1,local_8,param_3,param_4);
      param_1 = iVar1;
    }
    else {
      FUN_00409bd0(local_4,param_2,param_3,param_4);
      param_2 = local_8;
    }
    iVar1 = param_2 - param_1;
  } while( true );
}
```

---

## MEGA-043 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-043** |
| **Tools** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_callers` / `get_function_xrefs` + `get_assembly_context` + `read_memory` + `batch_decompile` callees + caller `FUN_00930360`. **No** `disassemble_bytes`. No Launcher. |
| **Image** | `autoassault.exe` base `0x400000` |
| **Body** | `0x00409bd0`–`0x00409cb9` inclusive (**234 B** / `0xEA`) — Ghidra `get_function_by_address` |
| **Live decompile** | ≡ scaffold raw CF (do-loop introsort; threshold 0x20; partition + recurse; heap fallback) |
| **ABI** | **`__cdecl`**: 4 stack args; plain **`RET`** (`C3` @ `0x00409c82` and `0x00409cb9`); caller `ADD ESP,0x10` @ `0x00930608` |
| **Stride** | `(last-first) >> 4` / `SAR EAX,4` → element size **16** (`0x10`) |
| **Depth reduce** | `ideal = ideal/2 + (ideal/2)/2` (= ¾) — classic MSVC `_Sort` |
| **Callees** | `FUN_00409f90` (partition pair-out); `FUN_0040a410` (insertion ≤32); `FUN_0040a820`+`FUN_0040a380` (make_heap+sort_heap when depth exhausted); self (2 recurse sites) |
| **Callers** | External: **`FUN_00930360`** @ `0x009305fc` (1 site). Self: `0x00409c3e`, `0x00409c4f`. Xrefs: 3 UNCONDITIONAL_CALL |
| **Caller args** | `FUN_00409bd0(begin, end, count, &LAB_0092cdd0)` where `count = (end-begin)>>4`; pred @ `0x0092cdd0` is float-less on record `+4` |
| **Scaffold reject** | `Named_CalleeOf_*PollBoundActions*` — partition lineage noise; live sole external parent is sort-target-list `FUN_00930360`, not PollBoundActions body |
| **Name promote** | `StdSort_RanItStride16_Inferred` (`_Inferred`: MSVC pattern sealed; no RTTI demangle / product plate) |
| **Terminal** | **false** |

### Entry prologue hex (`read_memory` @ `0x00409bd0`, 32 B)

```text
83 EC 08          SUB ESP, 8
53                PUSH EBX
8B 5C 24 10       MOV EBX, [ESP+0x10]   ; first
55                PUSH EBP
56                PUSH ESI
57                PUSH EDI
8B 7C 24 20       MOV EDI, [ESP+0x20]   ; last
8B C7             MOV EAX, EDI
2B C3             SUB EAX, EBX
C1 F8 04          SAR EAX, 4            ; count = (last-first)/16
83 F8 20          CMP EAX, 0x20
7E 7C             JLE short_path
```

### Epilog #1 hex (`read_memory` @ `0x00409c7e`)

```text
5B                POP EBX
83 C4 08          ADD ESP, 8
C3                RET                   ; plain cdecl RET (not RET n)
```

### Call-site context (`0x009305fc` in `FUN_00930360`)

```text
; EBX = element count (after SAR 4), EDI = begin, ESI = end
PUSH 0x0092cdd0   ; pred (float key @+4 less-than)
PUSH EBX          ; ideal = count
PUSH ESI          ; last
PUSH EDI          ; first
CALL 0x00409bd0
ADD ESP, 0x10
```

### Predicate leaf (`read_memory` @ `0x0092cdd0`) — evidence only, not OWN VA

```text
MOV EAX, [ESP+8]       ; b
MOV ECX, [ESP+4]       ; a
MOVSS XMM0, [EAX+4]
COMISS XMM0, [ECX+4]
JBE  -> AL=0
MOV AL, 1; RET         ; true iff a.f32[+4] < b.f32[+4]
XOR AL, AL; RET
```

### CF stages (sealed)

| Stage | Evidence | Conf |
|---|---|---|
| Count via SAR 4 | `C1 F8 04` | Confirmed |
| If count ≤ 32 → insertion (`FUN_0040a410`) if count > 1 | CMP/JLE + CALL `0040a410` | Confirmed |
| If ideal ≤ 0 → heap path (`0040a820` if span>16, then `0040a380`) | TEST ESI / JLE heap | Confirmed |
| Else partition `00409f90` → out pair local_8/local_4 | CALL + pair store | Confirmed |
| Recurse smaller half; iterate other (tail loop) | two self CALLs + JMP loop | Confirmed |
| Depth *= ¾ | CDQ/SAR/ADD sequence | Confirmed |

