# Annotated low-level: FUN_0043d570 / StdTree_OperatorIndex_Val12_U32Key_EdiEdx_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0043d570` |
| VA | `0x0043d570`–`0x0043d5e0` exclusive (**112 B**) |
| System | std red-black map operator[] / get-or-insert (Val12, u32 key, EDI map / EDX key*) |
| Date | 2026-07-23 scaffold; **2026-08-04 W38-A** dual seal |

## Machine-level notes

- **Bytes win** over decompiler: map in **EDI**, key* in **EDX**, plain **RET**, return mapped* in **EAX**.
- Val12 layout: L`+0` / P`+4` / R`+8` / key-u32`+0x0C` / mapped`+0x10` / color`+0x18` / isnil`+0x19`.
- Root = `head->parent` (`[head+4]`), same as W37-M `StdMap_FindEq_UintKey_Val12` sibling at `0x0043d5e0`.
- Miss path: `FUN_0043c8b0` packs `{*key,0,0}` into stack Val12 (EAX dest, EDX key*, ECX zero-mapped source); `FUN_0043d7d0` insert-hint (ECX=map, stack hint, ESI=out it*, EAX=Val12*).
- Odd/preserved: staging zeros 20 B region then c8b0 overwrites Val12; self-consistent with peer op[] units.
- Prefer assembly when decompiler shows `unaff_EDI` / unused `__fastcall param_1`.

## Pseudocode (annotated, ABI-corrected)

```c
// EDI = MapShell_Val12* map
// EDX = const uint32_t* key
// returns int32_t* mapped at node+0x10  (pair val1,val2)
// plain RET; SUB ESP,0x18 frame

int32_t *StdTree_OperatorIndex_Val12_U32Key_EdiEdx_Inferred(void)
{
  MapNode *head = *(MapNode **)(map + 4);   // EDI
  MapNode *cand = head;
  MapNode *node = head->parent;             // root

  if (node->isnil == 0) {
    do {
      if (node->key < *key) {               // unsigned; key from [EDX]
        node = node->right;
      } else {
        cand = node;
        node = node->left;
      }
    } while (node->isnil == 0);
  }

  // equality after lower_bound: cand != end && *key >= cand.key
  if (cand != head && *key >= cand->key) {
    return (int32_t *)((char *)cand + 0x10);
  }

  // miss: insert default-mapped zeros
  Val12 staged = { *key, 0, 0 };            // via FUN_0043c8b0
  MapNode **it = FUN_0043d7d0(map, cand, &staged);
  return (int32_t *)((char *)(*it) + 0x10);
}
```

## Open questions

- Product/PDB map English (AssPreloader tree slot type residual).
- Full ABI of unowned `FUN_0043d7d0` / `FUN_0043c8b0` beyond call-site seal.
- Runtime / bit-exact.
