# Raw capture: FUN_00404670

| Field | Value |
|---|---|
| **Stable ID** | `aa_00404670` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00404670` |
| **Canonical name** | `FUN_00404670` |
| **System** | `missions-progression` / shared UI toast vector helpers |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00404670(int param_1,int param_2,undefined4 param_3)

{
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  puStack_c = &LAB_009bc750;
  local_10 = ExceptionList;
  local_8 = 0;
  ExceptionList = &local_10;
  for (; param_2 != 0; param_2 = param_2 + -1) {
    FUN_00404710(param_1,param_3);
    param_1 = param_1 + 0x98;
  }
  ExceptionList = local_10;
  return;
}
```

---

## Live re-verify (2026-07-29, OWN-ONLY W16-S)

| Check | Result |
|---|---|
| `decompile_function` @ `0x00404670` | **≡ body above** (SEH + count loop + `FUN_00404710` + `+0x98`) |
| `read_memory` entry 64 B + loop 48 B + epilogue | SEH frame `LAB_009bc750`; stride `81 c6 98 00 00 00`; epilogue **`c3`** (cdecl) |
| Callers | `FUN_00404130`, `FUN_0040c5c0` |
| Callee | `FUN_00404710` @ `0x00404710` (`ret 8` POD copy `0x26` dwords) |

**Note:** Parent call sites may push **5** stack dwords + `ADD ESP,0x14` (phantom thiscall noise). Body formals that affect CF are **`(dst, count, src)`** only; `[ebp+0x14]` is loaded into `EBX`/`ECX` before the pod-copy call and is **unused** by stdcall leaf `FUN_00404710`.
