# Annotated low-level: GuardedVector_EraseFirstEqual_ThiscallRange

| Field | Value |
|---|---|
| Stable ID | `aa_0043e630` |
| VA | `0x0043e630`–`0x0043e6c1` (**146 B** / `0x92`) |
| Ghidra | `FUN_0043e630` |
| Canonical | `GuardedVector_EraseFirstEqual_ThiscallRange` (**Inferred**) |
| System | STL / guarded ring-deque container |
| Date | 2026-07-29 (W35-A dual seal) |

## Machine-level notes

- Source: live `decompile_function` + full-body `read_memory` (W35-A).
- **Bytes win** on ABI: decompiler shows `unaff_ESI` + void/`undefined1` surface; entry uses **ESI=container** with no load; epilogue **`RET 4`**.
- Twin of W28-C `GuardedVector_EraseFirstEqual` (`0x0043c550`, also 146 B / ESI / RET4) but nested erase is **thiscall** `FUN_0043df90` (W34-L) instead of EAX `FUN_0043c8c0`.
- Optional CS: flag `+0x2c` → Enter/Leave `+0x14` (IAT `0x009c61bc` / `0x009c61b8`).
- Ring page map: `page = idx>>2`; if `capacity <= page` then `page -= capacity`; sub-index via `idx + page*(-4)`.
- On first `pages[page][sub] == *key` → `EraseRange_Thiscall([idx, idx+1))`, found=1, leave.
- Reject scaffold plate `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Nam_0043e630`.

## Signature (sealed)

```c
// ESI = container; stdcall 1 stack formal (const int* key); RET 0x4; AL = found
uint8_t GuardedVector_EraseFirstEqual_ThiscallRange(
    const int* key /*stack*/,
    GuardedVectorHeader* container /*ESI*/);
```

## Layout (family)

| Off | Field |
|---|---|
| +0x04 | pages** (array of page ptrs; 4 dwords/page) |
| +0x08 | capacity (page count) |
| +0x0c | begin (absolute front index) |
| +0x10 | size (element count) |
| +0x14 | CRITICAL_SECTION |
| +0x2c | lock-enable flag (char) |

## Pseudocode (annotated)

```c
// ESI = container; stack key*; RET 4; AL = found
uint8_t FUN_0043e630(const int* key)
{
  GuardedVectorHeader* c = (GuardedVectorHeader*)/*ESI*/;

  if (c->lockEnable /*+0x2c*/) {
    EnterCriticalSection(&c->cs /*+0x14*/);
  }

  uint32_t idx = c->begin;                 // +0x0c
  uint8_t found = 0;
  uint32_t end = c->size + c->begin;       // +0x10

  while (idx != end) {
    uint32_t page = idx >> 2;
    int lane = (int)page * -4;             // sub = idx % 4
    if ((uint32_t)c->capacity /*+0x08*/ <= page) {
      page -= c->capacity;
    }
    int elem = *(int*)(c->pages[page] /*+0x04*/ + (idx + lane) * 4);
    if (elem == *key) {
      // ECX=c; EraseRange_Thiscall out, from=(c,idx), to=(c,idx+1)
      GuardedVector_EraseRange_Thiscall(/*ECX=c*/, &out, c, idx, c, idx + 1);
      found = 1;
      break;
    }
    idx++;
  }

  if (c->lockEnable) {
    LeaveCriticalSection(&c->cs);
  }
  return found;
}
```

## Call graph

| Dir | Target | Role |
|---|---|---|
| Calls | Enter/LeaveCriticalSection | optional CS |
| Calls | `FUN_0043df90` / EraseRange_Thiscall | single-slot erase |
| Called by | `FUN_00971030` | AssPreloader: ESI=`this+0x1c` pending ring |
| Called by | `NDResourceCache_LookupOrCreate` @ `0x0075ddb2` | ESI=`base+0x1c` |

## Open residual

- Product/PDB class English.
- Element type beyond dword POD equality.
- Nested EraseRange free owned by W34-L (not re-sealed here).
- Runtime / bit-exact.
