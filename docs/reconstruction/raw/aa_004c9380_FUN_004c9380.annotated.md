# Annotated: FUN_004c9380 → Object_AggroMap_ExportToIntKeyMap_Inferred

| Field | Value |
|---|---|
| **Stable ID** | `aa_004c9380` |
| **VA** | `0x004c9380` |
| **Named** | `Object_AggroMap_ExportToIntKeyMap_Inferred` |
| **Ghidra** | `FUN_004c9380` |
| **Agent** | WQ9G-C OWN-ONLY dual 2026-08-04 |

---

## Role

Export every live node of the **object aggro map** (sentinel head @ **this+0x158**, isnil@**+0x29**) into a **destination** int-key map/tree via dualed `StdMap_InsertOrFind_IntKey_Isnil29_Inferred` (`0x004cbe20`). Sole live caller: **`CVOGHBAIBase_GetTargetFromAggro`** (builds temp score-keyed tree then walks for in-range hostile pick).

---

## ABI (disasm supersedes decomp)

```c
// __thiscall  RET 4
void __thiscall Object_AggroMap_ExportToIntKeyMap_Inferred(
    Object *self,              // ECX  aggro head @ +0x158
    MapShell_Isnil29 *dest);   // stack +4
```

Decompiler showed `__fastcall` / no stack arg — **wrong**. `MOV EDI,[ESP+0x30]` loads dest; `MOV ECX,EDI` before insert-or-find; epilogue **`C2 04 00`**.

---

## Control flow

1. `head = *(self+0x158)`; `node = *head` (leftmost).
2. If empty (`node == head`) return.
3. Loop until `node == head`:
   - Pack value:
     - **key** = `(int)node[+0x20]` (aggro score)
     - **value[+8..+0x14]** = TFID dwords from `node[+0x10..+0x1C]`
     - **value[+4] not written** (stack residual; Val24-sized blob)
   - `StdMap_InsertOrFind_IntKey_Isnil29_Inferred(dest, &outPair, &value)` — **ECX=dest**
   - Advance **inorder successor** (classic isnil29: right-min else parent walk while right-child)
4. Return void.

---

## Source node layout (aggro; sealed peers)

| Offset | Field |
|---|---|
| +0x10..+0x1C | TFID-shaped 16 B key region |
| +0x20 | int score (mapped on aggro; **key** on export dest) |
| +0x28 / +0x29 | color / isnil |

---

## Odd behavior (preserve)

- Dest uniqueness is **score only**. Two aggro entries with equal score → second hit **no rewrite** (insert-or-find contract). May drop TFID payload of duplicates.
- Dest value dword1 (offset +4) never stored by this writer.

---

## Call graph

| Role | Target |
|---|---|
| Insert-or-find | `StdMap_InsertOrFind_IntKey_Isnil29_Inferred` `0x004cbe20` (WQ9F-D dual) |
| Caller | `CVOGHBAIBase_GetTargetFromAggro` `0x00638ec0` @ `0x00638f50` |

---

## Rejects

- Claiming this **is** insert-or-find (it is the **caller**).
- Claiming this mutates the source aggro map (read-only walk + export).
- Product method English without PDB (`_Inferred`).
- Decomp `__fastcall`/no-arg signature.
