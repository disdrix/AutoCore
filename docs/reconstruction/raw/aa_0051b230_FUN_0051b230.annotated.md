# Annotated low-level: FUN_0051b230 → Object_CreateFromEmbeddedCbid_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051b230` |
| VA | `0x0051b230`–`0x0051b298` exclusive |
| System | object-spawn / CVOGClonedObjectBase virtual |
| Date | 2026-08-04 (WQ9R-H dual seal) |

## Machine-level notes

- **ABI:** `__thiscall` ECX=host object; RET bare; EAX=created object* or NULL.
- **Factory:** `CVOGReaction_GiveItemByCbid(cbid, 0)` with `cbid = *(*(this+0xA8)+0x34)`.
- **Init chain (on success only):**
  1. `obj->vtbl[2]` @ +8: `(cbid, host=*(this+0xA4), 1)`
  2. `obj->vtbl[0x2E]` @ +0xB8: `(*(this+0xB8))`
  3. `FUN_00516720(obj, *(this+0x158))` — optional display/name string path
  4. `*(obj+0xC4) = *(this+0xC4)`
- **Callers:** creature clone path `FUN_004c8f00` and weapon clone path `FUN_0056b0a0` both this-adjust then `dynamic_cast` result to `CVOGCreature*` / `CVOGWeapon*`.
- **Sibling:** `AllocateNewObjectFromCbid` (`0x004cf120`) — same GiveItem + vtbl+8 + FUN_00516720 family; different host (sector map) and richer template apply.

## Pseudocode (annotated)

```c
// __thiscall Object_CreateFromEmbeddedCbid_Inferred(this)
// Returns CVOGClonedObjectBase* (or NULL). Callers often dynamic_cast.
void *Object_CreateFromEmbeddedCbid_Inferred(void *self)
{
  int cbid = *(int *)(*(int *)((char *)self + 0xA8) + 0x34);
  void *obj = CVOGReaction_GiveItemByCbid(cbid, /*trailing*/ 0);
  if (obj == NULL) return NULL;

  // vtbl+8 init (thiscall)
  (*(fn_init *)(*(void **)obj + 8))(obj, cbid, *(void **)((char *)self + 0xA4), 1);
  // vtbl+0xB8 property
  (*(fn_arg *)(*(void **)obj + 0xB8))(obj, *(void **)((char *)self + 0xB8));
  // name/string from self+0x158
  FUN_00516720(obj, *(char **)((char *)self + 0x158));
  // copy dword field
  *(int *)((char *)obj + 0xC4) = *(int *)((char *)self + 0xC4);
  return obj;
}
```

## Open residual

- Product MSVC demangle / plate for this virtual slot.
- Exact type of `*(this+0xA8)` descriptor (clonebase vs reaction blob vs template).
- Semantic English for `+0xB8` arg and `+0xC4` dword (weapon path overwrites `obj+0xC4` as byte after cast).
- Runtime / bit-exact / differential (no Launcher).
