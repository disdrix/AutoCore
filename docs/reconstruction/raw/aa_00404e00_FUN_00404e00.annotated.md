
# Annotated low-level: FUN_00404e00 / CVOGPhysics_WriteNotFullyReadyOut_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00404e00` |
| VA | `0x00404e00`–`0x00404e22` inclusive (**35 B** / `0x23`) |
| System | physics (partition seed: skills-abilities / RecvSkillStatusEffect nested) |
| Date | 2026-08-05 (MEGA-049 dual refresh) |
| Ghidra | `FUN_00404e00` |
| Promoted | `CVOGPhysics_WriteNotFullyReadyOut_Inferred` |

## Machine-level notes

- Source: raw capture for `aa_00404e00` + MEGA-049 live re-verify.
- Prefer assembly when decompiler conflicts (parent call sites lose this; body is clear).
- **Leaf** — no callees. Writes a single out-byte; returns out* in EAX.
- **ABI:** thiscall ECX=`CVOGPhysics*`; stack `uint8_t* out`; **`RET 4`**.
- **Polarity:** `*out = 0` iff fully ready (`byte(+0x40)!=0 && dword(+0x08)!=0`); else `*out = 1`.
- Same fully-ready fields as dualed `CVOGPhysics_SetRotation` (`0x00404dc0`) / setPosition peer (`0x0040d2a0`).
- Callers: skill bound-status motion table `FUN_005d0d60` @ `0x005d0fb6`; AI DoLogic `FUN_005d7f70` @ `0x005d80a5`. Both load ECX from `entity+0x08`.

## Pseudocode (annotated)

```c
// ECX = CVOGPhysics* this
// stack = uint8_t* out
// RET 4; EAX = out on exit
void __thiscall CVOGPhysics_WriteNotFullyReadyOut_Inferred(int phys, undefined1 *out)
{
  // fully ready: ready-byte set AND secondary host/body link non-null
  if ((*(char *)(phys + 0x40) != '\0') && (*(int *)(phys + 8) != 0)) {
    *out = 0;   // fully ready → soft path NOT allowed
    return;     // EAX = out
  }
  *out = 1;     // not fully ready → soft path allowed
  return;       // EAX = out
}
```

## Layout (this = CVOGPhysics)

| Off | Use |
|---:|---|
| `+0x08` | Nonzero contributes to fully-ready (with `+0x40`) |
| `+0x40` | Byte; nonzero contributes to fully-ready |

## Call graph (OWN unit only)

```text
FUN_005d0d60  Skill_ApplyBoundStatusMotionTable_Inferred
  └─ CALL 00404e00 @ 005d0fb6   ECX=*(entity+8)
FUN_005d7f70  CVOGHBAICreatureBase_DoLogic
  └─ CALL 00404e00 @ 005d80a5   ECX=*(entity+8)
FUN_00404e00  CVOGPhysics_WriteNotFullyReadyOut_Inferred  [leaf]
```

Partition seed RecvSkillStatusEffect reaches this via nested `005d0d60` only — body role is physics readiness, not skill packet decode.

## Open questions

- Product English names for `+0x40` / `+0x08` fields.
- Runtime Confirmed / bit-exact image diff.
