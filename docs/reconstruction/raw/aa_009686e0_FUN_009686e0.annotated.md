# Annotated low-level: GfxBufferedViews_ReleaseDeviceTextures_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_009686e0` |
| VA | `0x009686e0`–`0x00968715` exclusive (**53 B**) |
| Ghidra | `FUN_009686e0` |
| System | palantir/graphics — gfxBufferedView list / Device Reset shutdown |
| Date | 2026-08-04 (W38-AA) |

## Machine-level notes

- Sole caller: `GfxDevice_Reset` (`FUN_0075eff0`) @ `0x0075f0ec` on **shutdown** path.
- Global list head: `DAT_00d1ee30` (same list ctor `FUN_00968a50` registers into).
- Node layout: `*node = next`, `node[2] = GfxBufferedView* payload`.
- View→texture: `*(view + 0x44)`; gate field `*(tex + 0x30) == 0` means “needs device-loss release”.
- Callee `FUN_0096c8e0` is **thiscall** (`MOV ECX, tex`); decompiler shows zero formals.
- Plain `RET` (`C3`); pushes/pops ESI only.
- Prefer bytes for thiscall ECX; decompile ≡ CF for walk/gate/call.

## Pseudocode (annotated)

```c
// void GfxBufferedViews_ReleaseDeviceTextures_Inferred(void)
// Walk g_BufferedViewList; release device textures that need reset.
void FUN_009686e0(void)
{
  int *head = DAT_00d1ee30;          // list sentinel
  int *node = (int *)*head;          // first

  if (node != head) {
    do {
      GfxBufferedView *view = (GfxBufferedView *)node[2]; // payload
      if (view != NULL) {
        EffTextureImpl *tex = *(EffTextureImpl **)((char *)view + 0x44);
        if (*(int *)((char *)tex + 0x30) == 0) {
          FUN_0096c8e0(tex);         // thiscall ECX=tex — release GPU resources
          head = DAT_00d1ee30;       // reload after potential reentrancy
        }
      }
      node = (int *)*node;           // next
    } while (node != head);
  }
}
```

## Open questions

- Product English for texture field `+0x30` (reset/needs-recreate flag) and `view+0x44` slot.
- Full `FUN_0096c8e0` dual not owned here (W38-AB owns `0x0096c8e0`).
- Runtime / bit-exact / differential.
