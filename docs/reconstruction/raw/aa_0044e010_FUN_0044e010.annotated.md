# Annotated low-level: FUN_0044e010 → StdTree_LeftRotate_Isnil2D_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0044e010` |
| VA | `0x0044e010`–`0x0044e05b` inclusive (**76 B**) |
| System | MSVC std tree left-rotate (isnil@+0x2D / StringKey family) |
| Date | 2026-08-05 (WQ9K-I dual seal) |
| Tools | decompile + read_memory + analyze + xrefs + assembly context; **no** `disassemble_bytes` |

## Machine-level notes

- **Role:** Red-black / std `_Tree` **left rotate** helper for node family with **isnil@+0x2D**.
- **ABI:** ECX = node X; stack = map shell; **`RET 4`** on all three exits.
- **Nil test:** only `*(char*)(Y.left + 0x2D) == 0` (non-nil) before parent fixup — matches StringKey map erase/insert peers.
- **Peer:** right-rotate `FUN_0042a840` (WQ9K-J OWN). Used by StringKey erase `004094c0` and insert `00430b60`.
- **Not:** isnil29 rotate pair; not head buy/erase; no color flip inside this leaf (callers recolor at +0x2C).
- Reject scaffold `Named_CalleeOf_*` name; reject freelist/CNDHash.

## Pseudocode (annotated)

```c
// __thiscall: ECX = node X; stack map*; RET 4
void StdTree_LeftRotate_Isnil2D_Inferred(Node *x, MapShell *map)
{
  Node *y = x->right;                 // +0x08
  x->right = y->left;                 // y.left may be nil
  if (y->left->isnil /* +0x2D */ == 0)
    y->left->parent = x;              // +0x04
  y->parent = x->parent;
  Node *head = map->myhead;           // *(map+4)
  if (x == head->parent /* root */)   // *(head+4)
    head->parent = y;
  else if (x == x->parent->left)
    x->parent->left = y;
  else
    x->parent->right = y;
  y->left = x;
  x->parent = y;
}
```

## Node layout (isnil2D / StringKey 0x30 family)

| Off | Field |
|-----|-------|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x0C | string key (peer maps) |
| +0x2C | color (callers) |
| +0x2D | isnil |

## Callers

| Caller | Sites | Role |
|--------|-------|------|
| `FUN_0042a930` | 3 | tree rebalance (insert-side helper) |
| `FUN_00430b60` | 2 | StringKey insert+rebalance |
| `FUN_004094c0` | 3 | StringKey erase+rebalance |
| `FUN_00401db0` | 2 | tree rebalance peer |

## Open questions

- Product English map type hosting this rotate (StringKey inferred from peers).
- Right-rotate peer dual is WQ9K-J OWN.
- Runtime / bit-exact / differential.
