# Raw capture: FUN_004673b0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004673b0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004673b0` |
| **Canonical name** | `FUN_004673b0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_004673b0(undefined4 *param_1,undefined4 *param_2)

{
  undefined4 *in_EAX;
  undefined4 *puVar1;
  undefined4 *puVar2;
  
  if (param_1 != param_2) {
    puVar1 = in_EAX + 6;
    puVar2 = param_1 + 6;
    do {
      if (in_EAX != (undefined4 *)0x0) {
        *in_EAX = *param_1;
        in_EAX[1] = param_1[1];
        in_EAX[2] = param_1[2];
        in_EAX[3] = param_1[3];
        puVar1[-2] = puVar2[-2];
        puVar1[-1] = puVar2[-1];
        *puVar1 = *puVar2;
      }
      param_1 = param_1 + 7;
      in_EAX = in_EAX + 7;
      puVar1 = puVar1 + 7;
      puVar2 = puVar2 + 7;
    } while (param_1 != param_2);
  }
  return;
}
```

---

## W35-T re-verify (2026-07-29) — OWN dual seal

| Field | Value |
|---|---|
| **Tooling** | `batch_decompile` + `analyze_function_complete` + `get_function_by_address` + `read_memory` (90 B). **No** `disassemble_bytes`. |
| **Ghidra body** | `004673b0`–`00467406` inclusive (**87 B** / `0x57`); pad `CC` then next |
| **Classification** | leaf (0 callees) |
| **Xrefs** | 4 unconditional CALL sites — all inside `FUN_00466ea0` |
| **Callers** | sole: `FUN_00466ea0` (`StdVector_InsertN_Elem28_CountEcx_Inferred`, W34-Q) @ `0x00466ff9`, `0x00467031`, `0x004670e9`, `0x0046713b` |
| **Inferred name** | `StdVector_UninitializedCopy_Elem28_Inferred` |

### Live decompile (2026-07-29) = raw CF

```c
// Decompiler plate: __thiscall (WRONG for host). Formals: ECX=first, stack last; EAX=dest (in_EAX).
void FUN_004673b0(undefined4 *param_1 /*ECX=first*/, undefined4 *param_2 /*stack=last*/)
{
  undefined4 *in_EAX; // dest
  undefined4 *puVar1;
  undefined4 *puVar2;

  if (param_1 != param_2) {
    puVar1 = in_EAX + 6;
    puVar2 = param_1 + 6;
    do {
      if (in_EAX != (undefined4 *)0x0) {
        *in_EAX = *param_1;
        in_EAX[1] = param_1[1];
        in_EAX[2] = param_1[2];
        in_EAX[3] = param_1[3];
        puVar1[-2] = puVar2[-2];
        puVar1[-1] = puVar2[-1];
        *puVar1 = *puVar2;
      }
      param_1 = param_1 + 7;
      in_EAX = in_EAX + 7;
      puVar1 = puVar1 + 7;
      puVar2 = puVar2 + 7;
    } while (param_1 != param_2);
  }
  return;
}
```

### Machine bytes (`read_memory` @ `0x004673b0`)

Full body hex (**87 B**):

```
56 8B F1 3B 74 24 08 74 4C 53 55 8D 48 18 8D 56 18 57 85 C0 74 2A 8B FE 8B 2F 8B D8 89 2B 8B 6F 04 89 6B 04 8B 6F 08 89 6B 08 8B 7F 0C 89 7B 0C 8B 7A F8 89 79 F8 8B 7A FC 89 79 FC 8B 3A 89 39 83 C6 1C 83 C0 1C 83 C1 1C 83 C2 1C 3B 74 24 14 75 C0 5F 5D 5B 5E C3
```

| Offset | Bytes | Meaning |
|---|---|---|
| +0 | `56` | `PUSH ESI` |
| +1 | `8B F1` | `MOV ESI,ECX` — **first** = ECX |
| +3 | `3B 74 24 08` | `CMP ESI,[ESP+8]` — **last** on stack (after push) |
| +7 | `74 4C` | `JE` early-out if first==last |
| +9 | `53 55` | `PUSH EBX; PUSH EBP` |
| +B | `8D 48 18` | `LEA ECX,[EAX+0x18]` — dest tail cursor (**EAX=dest**) |
| +E | `8D 56 18` | `LEA EDX,[ESI+0x18]` — src tail cursor |
| +11 | `57` | `PUSH EDI` |
| +12 | `85 C0` | `TEST EAX,EAX` |
| +14 | `74 2A` | `JE` skip stores if dest null |
| +16… | 7× dword load/store | POD copy lanes 0..6 (offsets 0,4,8,C,10,14,18) |
| +3E | `83 C6 1C` / `83 C0 1C` / `83 C1 1C` / `83 C2 1C` | advance first/dest/cursors by **0x1C** |
| +4A | `3B 74 24 14` | `CMP ESI,[ESP+0x14]` — last after 3 more pushes |
| +4E | `75 C0` | `JNE` loop |
| +50 | `5F 5D 5B 5E C3` | pop edi/ebp/ebx/esi; bare **`RET`** |

### ABI / layout (bytes win)

| Slot | Source | Conf |
|---|---|---|
| dest | **EAX** | **High** |
| first | **ECX** | **High** |
| last | stack `[ESP+4]` at entry (cdecl-ish; caller cleans) | **High** |
| stride | **0x1C** (7 dwords) | **High** |
| cleanup | bare `RET` (`C3`) — **not** RET n | **High** |
| thiscall plate | **reject** — ECX is range first, not host vector | **High** |
| return | void (no live return use from sole caller) | **High** |

### Semantics

MSVC-style **uninitialized_copy** / trivial relocate for POD T size **0x1c**:

```
for (p = first, d = dest; p != last; p += 1, d += 1)
  if (d) *d = *p;   // 7-dword fieldwise assign
```

Null-dest stores skipped but cursors still advance (compiler artifact; parent always passes fresh `operator_new` buffer). Leaf — no ctor/dtor, no callees.

### Live = raw

Decompiler CF = scaffold 2026-07-23. **Bytes win** on ABI: EAX=dest, ECX=first, stack=last, bare RET; reject `__thiscall` plate.
