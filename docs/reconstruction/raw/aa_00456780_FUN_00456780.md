# Raw capture: FUN_00456780

| Field | Value |
|---|---|
| **Stable ID** | `aa_00456780` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00456780` |
| **Canonical name** | `FUN_00456780` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00456780(uint param_1,int param_2)

{
  int iVar1;
  uint uVar2;
  
  iVar1 = *(int *)(param_2 + 4);
  if (iVar1 == 0) {
    uVar2 = 0;
  }
  else {
    uVar2 = *(int *)(param_2 + 8) - iVar1 >> 2;
  }
  if (param_1 <= uVar2) {
    if ((iVar1 != 0) && (param_1 < (uint)(*(int *)(param_2 + 8) - iVar1 >> 2))) {
      FUN_00456660(param_2,iVar1 + param_1 * 4,*(int *)(param_2 + 8));
    }
    return;
  }
  if (iVar1 != 0) {
    FUN_00456960(*(undefined4 *)(param_2 + 8));
    return;
  }
  FUN_00456960(*(undefined4 *)(param_2 + 8));
  return;
}
```

---

## W38-H re-verify (2026-08-04)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual W38-H |
| **Tools** | `batch_decompile` / `analyze_function_complete` + `read_memory` + callers/xrefs + call-site `get_assembly_context` + callee decompiles. **No** `disassemble_bytes` |
| **Live decompile** | CF skeleton grow/shrink/equal; **grow args incomplete** (missing count/fill) |
| **Body** | `0x00456780`–`0x004567f5` exclusive (**117 B** / `0x75`); pad `CC` then next |
| **Classification** | worker |

### Bytes (`read_memory` full body 117 B)

```text
8b420485c056750433f6eb088b72082bf0c1fe023bf1733485c075158b420833f6502bce8d44240ce8b30100005ec204008b72082bf08b4208c1fe02502bce8d44240ce8980100005ec2040085c07421578b7a088bf72bf0c1fe023bce731153578d048850528d5c241ce871feffff5b5f5ec20400
```

### Byte-sealed ABI (overrides decompiler formals)

| Slot | Evidence | Role |
|---|---|---|
| **EDX** | `MOV EAX,[EDX+4]` begin; shrink `PUSH EDX` | `vec*` (MSVC vector control block) |
| **ECX** | `CMP ESI,ECX` size vs n; grow `SUB ECX,ESI` insert count | **newCount** (dword element count) |
| **stack+4** | `RET 4` (`C2 04 00` ×3); grow `LEA EAX,[ESP+0xC]` = &fill | **fill value** (dword by value) |
| cleanup | three `C2 04 00` exits | **RET 4** |

Decompiler `void __fastcall (uint param_1, int param_2)` maps param_1?ECX / param_2?EDX but **omits fill** and collapses grow to `FUN_00456960(end)` only. **Bytes win**.

### Semantics (sealed)

MSVC-style **`vector<uint32_t>::resize(n, value)`** with **ECX=n**, **EDX=vec**:

1. `size = begin(+4) ? (end(+8)-begin)>>2 : 0`.
2. **Grow** (`size < n`): `FUN_00456960` InsertN — `EAX=&fill`, `ECX=n-size`, stack `end`, `EDX=vec` (empty and non-empty entry points both compute `n-size`).
3. **Shrink** (`begin && n < size`): `FUN_00456660(vec, begin+n*4, end)` with `EBX` out-scratch; memmove-publish end.
4. Equal / null-begin: no-op.

### Twin family (do not merge)

| VA | ABI | Grow | Shrink |
|---|---|---|---|
| `0x00456780` (this) | EDX=vec, ECX=n, fill, RET 4 | `FUN_00456960` | `FUN_00456660` |
| `0x0044a380` (W37-Z) | ECX=vec, EAX=n, fill, RET 4 | `FUN_00463e30` | `FUN_00469f10` |
| `0x004367f0` | stack n+fill, RET 8 | different InsertN | erase twin |

### Call-site seals

- `PoolHost_Acquire0x40` `0x009860ba`/`ce`: `PUSH 0` fill; `LEA ECX,[size-1]`; EDX=host+0x10 freelist vec.
- `FUN_004665d6`: `MOV EDX,ESI`; `XOR ECX,ECX`; `PUSH EBP` fill ? resize(0).
- `FUN_00765b74`: `LEA EDX,[EBX+0xbc]`; `PUSH 0`; ECX=count from prior.

### Name

`StdVector_DwordResize_EcxCount_EdxVec_Inferred` (Ghidra `FUN_00456780`; **Inferred**). **Reject** `Named_CalleeOf_Named_gfxBody_00456780`.
