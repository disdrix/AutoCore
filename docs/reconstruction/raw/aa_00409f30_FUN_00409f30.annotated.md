# Annotated low-level: FUN_00409f30

| Field | Value |
|---|---|
| Stable ID | `aa_00409f30` |
| VA | `0x00409f30` |
| System | STL / vector POD helpers (elem stride 0x28) |
| Date | 2026-08-05 (WQ9I-B OWN dual) |

## Machine-level notes

- Source: raw capture for `aa_00409f30` + live re-verify.
- Prefer assembly / `read_memory` when decompiler invents phantom formals.
- Leaf POD element copy for **0x28**-byte elements (10 dwords).
- Callers: dualed ConstructN `StdVector_ConstructN_Elem0x28_Inferred` (`0x00409d40`); range worker `FUN_0040a520` (step +0x28 both cursors).

## Pseudocode (annotated)

```c
// ABI: EAX=dest, EDX=src; plain ret. Decomp "fastcall param_1" is phantom.
void StdVector_PodCopyElement_0x28_Inferred(void /*EAX dest*/, void /*EDX src*/)
{
  // machine: EDI=EAX, ESI=EDX
  if (dest != nullptr) {
    // ECX = 10; REP MOVSD  →  40 bytes = 0x28
    memcpy(dest, src, 0x28);  // dword-aligned POD only
  }
}
```

## Sealed facts

| Fact | Evidence |
|---|---|
| Body 20 B | Ghidra body `00409f30`–`00409f43` + bytes |
| 10-dword count | imm `B9 0A 000000` + `F3 A5` |
| Null dest skip | `TEST EDI,EDI` / `JZ` past copy |
| EAX/EDX ABI | callers `MOV EAX,dest; MOV EDX,src` then CALL |
| Stride consumer | ConstructN advances dest `+0x28` after each call |

## Open questions

- Product / PDB English for the 0x28 POD element type.
- Runtime / bit-exact / differential.

## Name

**StdVector_PodCopyElement_0x28_Inferred** — role/CF sealed; product demangle open → `_Inferred`.
