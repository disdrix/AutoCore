# Annotated low-level: UI_ItemDetailPanel_Refresh_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0084b890` |
| VA | `0x0084b890` |
| Body | `0x0084b890`–`0x0084ca69` (**4569** bytes / `0x11D9`) |
| System | client UI / inventory item detail panel |
| Date | 2026-07-29 (W22-K seal) |
| Ghidra name | `FUN_0084b890` |
| Canonical (inferred) | `UI_ItemDetailPanel_Refresh_Inferred` |
| Prior alias | `UI_overlay_broken` (string-weak; keep as evidence string only) |

## Machine-level notes

- **ABI:** `__thiscall` / decompiler `__fastcall` — **ECX = panel `this`**. Machine: `mov ebp, ecx`. No stack args. `ret` (`c3`).
- **Dispatch:** sole xref is **DATA** at `0x00a697e4` (vtable slot) — **virtual method**, not a direct CALL site in code.
- **Gate:** early out if `*(this + 0x518) == 0` (decomp `param_1[0x146]` typed `float*` but compared as pointer/object — **item/character object bind**).
- **Role:** full **layout + paint/refresh** of an item-detail / inspect UI panel: overlay texture, percent/name text, child widget stacking, tooltip stats (`UI_BuildItemTooltipStats`), optional angle gauges, footer type label.
- **Not** network, skill cast, or freelist logic.

### Important `this` child slots (decomp float-index → byte)

| Decomp index | Byte off | Role (Probable) |
|---|---|---|
| `param_1[0x146]` | `+0x518` | Bound object* (gate) |
| `param_1[0x14a]` | `+0x528` | char flag (footer branch) |
| `param_1[0x14b]` | `+0x52c` | primary chrome / root widget* |
| `param_1[0x14d]` | `+0x534` | overlay host* (`+0x488` flag set) |
| `param_1[0x14e]`…`[0x158]` | `+0x538`… | stacked child widgets* |
| `param_1[0x14f]` | `+0x53c` | text/name widget* |
| `param_1[0x151]` | `+0x544` | conditional block widget* |
| `param_1[0x157]`/`[0x158]` | | angle/gauge widgets* |
| `param_1[0x159]` | | final action/show widget* |

### Object fields touched (via `this+0x518`)

| Off on object | Use |
|---|---|
| `+0xa8` → `+0x38` / `+0x3c` | type id / subtype table (short `+0x3f4==8` weapon-ish) |
| `+0xc8` (200) | arg to `FUN_00519d20` skill-mask path |
| `+0x17c` bit 19 | broken-overlay / mode gate with `DAT_00d1791c` |
| `+0x160/+0x164` | COID pair vs player table |
| vfunc `+0x15c`, `+0x1e0` | name/string + nested object for gauges |

### Constants

| Symbol | Value | Use |
|---|---|---|
| `DAT_00aaa678` | ≈0.000833 | Y UI scale |
| `DAT_00aaa67c` | ≈0.000625 | X UI scale |
| `DAT_00aaa7ac` | 100.0f | percent clamp |
| `DAT_00aaa99c` | ≈114.5916 | acos scale (`360/π`); product domain residual |
| `DAT_00d1e818/81c` | screen dims | layout |
| `DAT_00d1791c` | char mode flag | broken overlay / percent / footer |
| `DAT_00d1b6d8` | local player* | percent + COID table `@+0x5a0` (8 entries) |

### Strings

- `i_g_2d_overlay_broken.dds` — broken state overlay texture
- `%.01f%% %s` — durability/condition style percent line
- `%s: %s` — labeled name line

## Control-flow stages (annotated)

```
if (this->obj == null) return;                    // +0x518
t = FUN_007a69d0();                               // time
FUN_0084b210(this);
this->overlayHost[+0x488] = 1;
if (subtype_short == 8)
  skillMask = FUN_00519d20(obj+0xc8);
  overlayHost->vfunc(+0x1a0)(mask|0xdf000000);
else
  overlayHost->vfunc(+0x1a0)(0);
FUN_0075bf40();
if (!(obj.flags bit19) && !DAT_00d1791c)
  overlayHost->vfunc(+0x58)();                    // normal
else
  // load "i_g_2d_overlay_broken.dds" via vfunc +0x50
FUN_00845b40 / FUN_00845af0 → local flags;
// scale root widget size from screen * widget metrics * DAT scales
// text widgets: percent (optional), names, locale FUN_007a6de0/6a30
// stack children with size queries vfunc +0x120/+0x140 and place +0x110/+0x118
// UI_BuildItemTooltipStats(obj)
// optional gauges: vfunc +0x1e0 object, acos(field), *114.59, set gauge vfuncs
// COID match loop may set DAT_00d1791c
// footer: type==4 clear label else FUN_005140d0 subtype string
// final show/hide on param_1[0x159]
```

## Open questions

1. Product class name of panel (vtbl at `0x00a697e0` region) — not PDB-proven.
2. Exact English for each child widget slot `+0x52c…`.
3. Full meaning of skill mask `0xdf000000` OR and subtype 8.
4. Runtime which UI open path installs vtbl and binds `+0x518`.

## Confidence

| Claim | Level |
|---|---|
| Virtual thiscall refresh/layout of item panel | **High** |
| Gate at +0x518 | **Confirmed** |
| Strings / tooltip callee / acos gauge math | **Confirmed** |
| Scale constant float values | **Confirmed** (`read_memory`) |
| Per-widget English names | **Tentative** |
| Product symbol | **Probable** / inferred |
