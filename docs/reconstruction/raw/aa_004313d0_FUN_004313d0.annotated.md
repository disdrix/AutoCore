# Annotated low-level: StdTree_IteratorDecrement (`FUN_004313d0`)

| Field | Value |
|---|---|
| Stable ID | `aa_004313d0` |
| VA | `0x004313d0`–`0x00431420` (80 B) |
| System | shared `client::stdtree` (MSVC `_Tree` predecessor) |
| Date | 2026-07-29 (W22-O dual seal) |
| Canonical name | `StdTree_IteratorDecrement` |
| Ghidra name | `FUN_004313d0` |

## Machine-level notes

- Source: raw capture + W22-O re-verify (`decompile_function` + `read_memory`).
- **EDX** = `node**` iterator; body never touches ECX.
- Nil flag at **`node+0x2d`** (not `+0x29` — different node size than `aa_004cb270` increment twin).
- Layout: `+0` left, `+4` parent, `+8` right.
- When at end (isnil): assigns **right** child → MSVC header rightmost (predecessor of `end()`).
- No callees; plain `ret`.
- Call pattern: `lea edx, [local_it]; call 0x004313d0`.

## Pseudocode (annotated)

```c
// INFERRED name: StdTree_IteratorDecrement
// EDX = node** it  (in-place)
void StdTree_IteratorDecrement(void /*ECX unused*/, Node **it /*EDX*/)
{
  Node *node = *it;

  // end()/header: predecessor = rightmost (header->_Right)
  if (node->isnil /* +0x2d */ != 0) {
    *it = node->right; /* +8 */
    return;
  }

  // If left child exists: go left, then rightmost
  Node *left = node->left; /* +0 */
  if (left->isnil == 0) {
    Node *walk = left->right; /* +8 */
    while (walk->isnil == 0) {
      left = walk;
      walk = walk->right;
    }
    *it = left;
    return;
  }

  // Else climb while current is left child of parent
  Node *parent = node->parent; /* +4 */
  if (parent->isnil == 0) {
    do {
      if (*it != parent->left) break;
      *it = parent;
      parent = parent->parent;
    } while (parent->isnil == 0);
    if (parent->isnil == 0) {
      *it = parent;
    }
  }
}
```

## Cross-checks

| Claim | Evidence |
|---|---|
| Predecessor not successor | Left→rightmost + climb-while-left; nil→right |
| Used by string map insert | `StdMap_StringKey_InsertOrFind` after goLeft |
| Sibling ++ is different VA | `0x004cb270` nil@`+0x29` |

## Gaps

1. Product C++ symbol (`std::_Tree_unchecked_const_iterator::operator--` exact template).
2. Full payload layout beyond tree header (key @ +0x0C for string map nodes — parent dual).
3. Runtime / bit-exact — open.
