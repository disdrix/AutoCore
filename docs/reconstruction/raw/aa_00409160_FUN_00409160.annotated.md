# Annotated low-level: StdTree_Min_Isnil31 (FUN_00409160)

| Field | Value |
|---|---|
| Stable ID | `aa_00409160` |
| VA | `0x00409160`–`0x00409172` inclusive (**19 B**) |
| Canonical name | `StdTree_Min_Isnil31` |
| System | MSVC `std::_Tree` min / leftmost (isnil @ +0x31) |
| Date | 2026-08-05 (WQ9J-E dual seal) |

## Machine-level notes

- **ABI:** custom register — **EAX** = subtree root node* in; **EAX** = leftmost non-nil out; plain **`RET`**. ECX is scratch only.
- **isnil:** every guard at **`+0x31`** (`80 79 31 00`).
- **Layout:** left at `+0` (followed while non-nil).
- **Semantics:** classic MSVC `_Tree_min` — if `node->left` is nil, return `node`; else walk left until next left is nil; return last real node.
- **Caller (1):** map erase `FUN_00408ed0` @ `0x00408f9d` — `MOV EAX,EDI; CALL; MOV [EBX],EAX` when recomputing head leftmost after erase.
- **Twins / family:** `StdTree_Min_Isnil29` @ `0x0051b5d0` (stack-arg ABI); peer `StdTree_IteratorIncrement_Isnil31` @ `0x00408590`.
- Decompiler reports `void` / phantom `in_EAX` — **override** with call-site EAX store.

## Pseudocode (byte-faithful)

```c
// EAX = node in/out; bare RET
node_t *StdTree_Min_Isnil31(node_t *node /*EAX*/)
{
  node_t *left = node->left;                   // [EAX]
  if (left->isnil /*+0x31*/ != 0)
    return node;                               // already min
  do {
    node = left;
    left = node->left;
  } while (left->isnil == 0);
  return node;                                 // EAX
}
```

## Open questions

- Product map key/value English for isnil@+0x31 instantiation (shared helper).
- Color bit unused here.
- Runtime / bit-exact / differential.
