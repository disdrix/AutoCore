# Annotated low-level: FUN_004611e0 → StdVector_Resize_RcElem28

| Field | Value |
|---|---|
| Stable ID | `aa_004611e0` |
| VA | `0x004611e0`–`0x004612c5` exclusive (**229 B**) |
| System | MSVC `vector` resize, refcounted elem **0x1c** |
| Date | 2026-07-23 scaffold; W35-Q seal 2026-07-29 |
| Canonical name | `StdVector_Resize_RcElem28` (**Inferred**) |

## Machine-level notes

- **Bytes win** on ABI: **EDI=`vec*`**, **ECX=`new_size`**, stack **T by-value (0x1c)**, **`RET 0x1C`**.
- Decompiler marks ECX as thiscall `this` and EDI as `unaff_EDI` — incorrect register map.
- Stride **0x1c** via magic `0x92492493` (same family as W34-P `StdVector_InsertN_RcElem28`).
- Grow → sealed insert-n at end (`0x004617a0`); shrink → erase-range `FUN_00461720`.
- Epilogue **Release** of by-value fill element resource `@+0x10` (refcount-- → vtbl+8).
- Sole caller: `FUN_00965ab0` @ `0x00965f47` (gfx/mesh residual).
- Reject scaffold `Named_CalleeOf_*gfxDevice*`.

## Signature (sealed)

```c
// EDI=vec*, ECX=new_size, stack T[0x1c] by-value; RET 0x1C; void
void StdVector_Resize_RcElem28(
    StdVectorRc28* vec,     /* EDI */
    uint32_t new_size,      /* ECX */
    RcElem28 value);        /* stack by-value 0x1c */
```

## Control flow (annotated)

1. SEH `LAB_009be618`; EH state 0; snapshot size from triad.
2. If `size < n`: InsertN at `end` with count `n-size`, value* = stack T.
3. Else if `begin && n < size`: erase `[begin+n*0x1c, end)`.
4. Else equal: fall through.
5. EH state −1; Release stack T resource if non-null; restore SEH; `add esp,0x10`; `ret 0x1C`.

## Element / vector (shared with insert-n)

| Off | Field |
|---|---|
| vec +0x04 | begin |
| vec +0x08 | end |
| vec +0x0C | capEnd |
| T +0x10 | shared resource* (refcount @ +4; Release vtbl+8) |

## Full hex

See raw W35-Q append (229 B).
