# Annotated low-level: VehiclePhys_Mode2Helper_Ctor (FUN_0063f400)

| Field | Value |
|---|---|
| Stable ID | `aa_0063f400` |
| VA | `0x0063f400`–`0x0063f4c6` (199 B) |
| Canonical | `VehiclePhys_Mode2Helper_Ctor` |
| Ghidra | `FUN_0063f400` |
| System | input-drive-control / vehicle phys mode |
| Date | 2026-07-29 W25-K |

## Machine-level notes

- **thiscall**: ECX = freshly allocated 0x60 helper; stack = phys host; RET 4; EAX = this.
- Sole caller `VehiclePhys_SetModeHelper` mode-2 path (`0x005d4094`).
- Factory pre-tags object: `*(u16*)(obj+4) = 0x60` size; ctor sets refcount at **+6**.
- Vtable `PTR_FUN_009e3ec0`; FLT_MAX sentinels + unit diagonal floats + four blend constants.
- Nested `FUN_0063f330(this, *(host+0xc))` seeds extent at helper+8 (owned residual).
- Prefer bytes over analyze meta (`undefined (void)` is wrong).

## Signature

```c
// __thiscall RET 4; returns this
Mode2Helper* VehiclePhys_Mode2Helper_Ctor(Mode2Helper* this, PhysHost* host);
```

## Pseudocode (annotated)

```c
Mode2Helper* __thiscall VehiclePhys_Mode2Helper_Ctor(Mode2Helper* self, PhysHost* host)
{
  // Blend / scale defaults (product names from Ghidra symbols; English residual)
  self->f50 = DAT_00a0f718;                 // ~0.01f
  self->f54 = g_flHardKillInterpolate;      // ~0.005f
  self->f58 = g_flMultiKillCountBlend;      // 0.1f
  self->f5c = DAT_00a0f70c;                 // 0.2f

  self->refcount_u16_at_6 = 1;
  self->vtbl = &PTR_FUN_009e3ec0;

  // Two FLT_MAX quads at +0x10 and +0x30
  float flMax = DAT_009e3ebc; // 0x7F7FFFFF
  self->q10[0]=self->q10[1]=self->q10[2]=self->q10[3] = flMax;
  self->q30[0]=self->q30[1]=self->q30[2]=self->q30[3] = flMax;

  // Zero triple + 1.0 at +0x20 / +0x40 families
  self->v20 = 0; self->v24 = 0; self->v28 = 0; self->v2c = 1.0f;
  self->v40 = 0; self->v44 = 0; self->v48 = 0; self->v4c = 1.0f;

  FUN_0063f330(self, *(void**)(host + 0xc)); // seed +8 from host entity/rb
  return self;
}
```

## Open questions

- Product English for the four blend constants and FLT_MAX fields.
- Full mode-2 vtable method names (`PTR_FUN_009e3ec0`).
- `FUN_0063f330` dual seal (nested).
- Runtime / bit-exact.
