# Raw capture: FUN_0040a520

| Field | Value |
|---|---|
| **Stable ID** | `aa_0040a520` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0040a520` |
| **Canonical name** | `FUN_0040a520` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int __thiscall FUN_0040a520(int param_1,int param_2,int param_3)

{
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  puStack_c = &LAB_009bd0b0;
  local_10 = ExceptionList;
  local_8 = 0;
  ExceptionList = &local_10;
  for (; param_2 != param_1; param_2 = param_2 + 0x28) {
    FUN_00409f30();
    param_3 = param_3 + 0x28;
  }
  ExceptionList = local_10;
  return param_3;
}
```

---

## WQ9J-A re-verify (2026-08-05) — append only

| Field | Sealed value |
|---|---|
| **Body** | `0x0040a520`–`0x0040a584` inclusive (**101 B** / `0x65`); next pad / helper then twin `0040a590` |
| **SEH** | `PUSH -1; PUSH LAB_009bd0b0; FS:[0]` frame; try-level `0` → `-1` on exit |
| **ABI** | **ECX** = `src_end`; stack `[ebp+8]` = `src_begin`; stack `[ebp+0xc]` = `dest`; **EAX** = advanced dest; plain **`RET`** (`C3`). Callers push 4 dwords + `ADD ESP,0x10` (extra 2 unused by this body) |
| **Semantics** | MSVC-style **uninitialized_copy** over half-open `[src_begin, src_end)` into `dest` for **elem stride 0x28**; each step calls dualed `StdVector_PodCopyElement_0x28_Inferred` (`0x00409f30`) with **EAX=dest, EDX=src**; advances both cursors **+0x28** |
| **Callees** | `FUN_00409f30` only |
| **Callers (2)** | `FUN_00408050` InsertN/grow path (2 sites @ `0x0040824c`, `0x004082b3`); thin trampoline `FUN_00409ae0` @ `0x00409af0` |
| **Xrefs** | 3 UNCONDITIONAL_CALL |
| **Name** | `StdVector_UninitializedCopy_Elem0x28_Inferred` (Ghidra `FUN_0040a520`) |
| **Decompile note** | Decomp labels `__thiscall` and empty `FUN_00409f30()` — **bytes override**: free helper; PodCopy gets EAX/EDX from `MOV EDX,ESI; MOV EAX,EDI` before CALL |

### Assembly (from disassemble_function + read_memory; no disassemble_bytes)

```text
0040a520  PUSH EBP
0040a521  MOV  EBP, ESP
0040a523  PUSH -1
0040a525  PUSH 0x9bd0b0          ; SEH
0040a52a  MOV  EAX, FS:[0]
0040a530  PUSH EAX
0040a531  MOV  FS:[0], ESP
0040a538  PUSH ECX
0040a539  PUSH EBX
0040a53a  PUSH ESI
0040a53b  PUSH EDI
0040a53c  MOV  [EBP-0x10], ESP
0040a53f  MOV  EDI, [EBP+0xc]    ; dest
0040a542  MOV  EBX, ECX          ; src_end
0040a544  MOV  ESI, [EBP+0x8]    ; src_begin
0040a547  MOV  [EBP-4], 0
0040a54e  MOV  EDI, EDI          ; align nop
0040a550  CMP  ESI, EBX
0040a552  JZ   0040a56b
0040a554  MOV  EDX, ESI          ; src → PodCopy EDX
0040a556  MOV  EAX, EDI          ; dest → PodCopy EAX
0040a558  CALL 00409f30
0040a55d  ADD  EDI, 0x28
0040a560  MOV  [EBP+0xc], EDI
0040a563  ADD  ESI, 0x28
0040a566  MOV  [EBP+0x8], ESI
0040a569  JMP  0040a550
0040a56b  MOV  [EBP-4], -1
0040a572  MOV  EAX, EDI          ; return advanced dest
0040a574  MOV  ECX, [EBP-0xc]
0040a577  MOV  FS:[0], ECX
0040a57e  POP  EDI
0040a57f  POP  ESI
0040a580  POP  EBX
0040a581  MOV  ESP, EBP
0040a583  POP  EBP
0040a584  RET
```

### Call-site memory (ABI seal)

```text
; InsertN path @ 0040824c — uninit_copy range into new/shifted buffer
PUSH ECX / PUSH EBX / PUSH EAX / PUSH ESI
MOV  ECX, EDI              ; src_end
CALL 0040a520
ADD  ESP, 0x10
; [ebp+8]=ESI begin, [ebp+0xc]=EAX dest, ECX=EDI end

; InsertN path @ 004082b3 — shift tail right; return becomes new end
... SUB ECX, EAX           ; begin = end - n*0x28 (byte)
PUSH EDX / PUSH EBX / PUSH EDI / PUSH ECX
MOV  ECX, EDI              ; src_end
CALL 0040a520
ADD  ESP, 0x10
MOV  [EBX+8], EAX          ; vector.end = advanced dest

; Trampoline FUN_00409ae0 @ 00409af0 reorders stack → same ECX/begin/dest shape; RET 8
```

### Hex body (`read_memory` 101 B)

```
55 8B EC 6A FF 68 B0 D0 9B 00 64 A1 00 00 00 00
50 64 89 25 00 00 00 00 51 53 56 57 89 65 F0 8B
7D 0C 8B D9 8B 75 08 C7 45 FC 00 00 00 00 8B FF
3B F3 74 17 8B D6 8B C7 E8 D3 F9 FF FF 83 C7 28
89 7D 0C 83 C6 28 89 75 08 EB E5 C7 45 FC FF FF
FF FF 8B C7 8B 4D F4 64 89 0D 00 00 00 00 5F 5E
5B 8B E5 5D C3
```
