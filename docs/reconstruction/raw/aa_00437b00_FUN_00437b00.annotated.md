# Annotated low-level: FUN_00437b00 / Sto_AbstractUnserializationFactory_LookupCreateUnserialize_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00437b00` |
| VA | `0x00437b00`–`0x00437b57` (**88 B**) |
| System | arda2/storage — stoAbstractUnserializationFactory + stoChunkReader |
| Date | 2026-07-23 scaffold; **W36-A seal 2026-07-29** |
| Preferred name | `Sto_AbstractUnserializationFactory_LookupCreateUnserialize_Inferred` |

## Machine-level notes

- Source: raw + live `decompile_function` / `read_memory` (no `disassemble_bytes`).
- **Peek** next chunk FourCC without consuming stream (`FUN_0076a900`, ESI=reader from EDI).
- **Lookup** tag in Map A (`FUN_00438c40` + `FUN_0046c1b0`); miss if node == `*(map+8)` sentinel → create fails (0).
- **Create** via `call [node+0xc]` — 0 stack args; sample creators `operator_new` + ctor return object\*.
- **Store** `*param_1 = object`.
- **Unserialize**: thiscall on `(object+4)` vfunc slot 1 with **EDI (chunk reader)** as single stack arg; callee-clean; return status in EAX.
- Fail: `EAX = -1` without calling vfunc.
- Decompiler invents `creator(tag)` — **falsified by bytes** and by sample creator `FUN_00444d00(void)`.
- Scaffold name `Named_CalleeOf_Named_gfxBody_*` is **caller provenance only** (gfxBody.cpp uses this); body is storage factory + chunk, not graphics.

## Pseudocode (annotated)

```c
// cdecl; param_1 = out object**; EDI = stoChunkReader*
// returns vfunc status, or 0xFFFFFFFF on miss/null create
uint32_t FUN_00437b00(int *param_1)
{
  uint32_t tag;
  void *map;
  void *node;
  void *obj;
  uint32_t status;

  // ESI = EDI (chunk reader)
  tag = FUN_0076a900();                 // PeekChunkTag(this=ESI)
  map = FUN_00438c40();                 // MapA
  // FUN_0046c1b0: ESI=map, ECX=&tag, EAX=&node
  FUN_0046c1b0();
  map = FUN_00438c40();
  if (node == *(void **)((char *)map + 8)) {
    obj = 0;                            // miss
  } else {
    obj = ((void *(*)(void))*(void **)((char *)node + 0xc))(); // creator — 0 args
  }
  *param_1 = (int)obj;
  if (obj != 0) {
    // this = obj+4; push reader; call vtable[1]
    status = (*(uint32_t (**)(void))(*(int *)((char *)obj + 4) + 4))();
    // bytes: PUSH EDI; CALL [EDX+4] with ECX=obj+4
    return status;
  }
  return 0xffffffff;
}
```

## Open questions

- Exact C++ type of created object / vtable at `obj+4` (sample size 0xC4 via creator `0x00444d00`).
- MapB twin `FUN_00437c90` (W36-C) — parallel path, not owned here.
- Runtime / bit-exact / differential.
