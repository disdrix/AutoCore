# Annotated low-level: FUN_007a26c0 → Palantir_ExtendedHost_Ctor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_007a26c0` |
| VA | `0x007a26c0`–`0x007a297D` exclusive (**701 B**) |
| System | Palantir / client host shell |
| Date | 2026-08-04 (W37-R) |

## Machine-level notes

- Source: raw capture + W37-R `read_memory` / decompile.
- Prefer bytes when decompiler signature shows bare `undefined` / omits `RET 4`.
- Stack-self ABI matches `Palantir_BaseCtor` (`FUN_007543b0`, W35-I).
- Overwrites base vtbl `PTR_FUN_00a9f76c` family → derived `PTR_FUN_00a97170`.
- Publishes **`DAT_00d17958`** (host singleton used by profiler counters elsewhere); base ctor publishes **`DAT_00d1f058`** separately.

## Pseudocode (annotated)

```c
// stack self*; RET 4; EAX = self
// SEH: LAB_009b0088
uint32_t* Palantir_ExtendedHost_Ctor_Inferred(uint32_t* self)
{
  // FUN_007543b0(self) — Palantir_BaseCtor (CoInit, NDResourceCache @+0x30, strings, …)
  // *self = PTR_FUN_00a97170;  // derived host vtbl
  // FUN_00424bf0(self+0xD8);
  // stamp floats/flags/resolution (see raw W37-R table)
  // self[0xB5] = FUN_004933f0();  // +0x2D4
  // DAT_00d17958 = self;
  // node = operator_new(0xA8); circular list head; RGB/A 0xFF clusters; +0x6C=0
  // self[0xB7] = node; // +0x2DC
  // zero self[0x4F..0x56] (+0x13C..+0x158)
  return self;
}
```

## Open questions

- Product demangle for derived type behind `PTR_FUN_00a97170` (RTTI not sealed this wave).
- Product role of `FUN_00424bf0` subobject @ `+0xD8` and `FUN_004933f0` result @ `+0x2D4`.
- Exact type of `operator_new(0xA8)` list node (color triple storage clear; container name open).
- Runtime / bit-exact / differential — open.
