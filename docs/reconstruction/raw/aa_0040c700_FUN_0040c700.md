# Raw capture: FUN_0040c700

| Field | Value |
|---|---|
| **Stable ID** | `aa_0040c700` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0040c700` |
| **Canonical name** | `FUN_0040c700` |
| **System** | client::list |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0040c700(int param_1)

{
  if (*(char *)(param_1 + 0x28) != '\0') {
    *(undefined1 *)(param_1 + 0x28) = 0;
    LeaveCriticalSection((LPCRITICAL_SECTION)(param_1 + 4));
  }
  return;
}
```

---

## Live re-verify append — 2026-08-04 (WQ9D-E)

| Field | Value |
|---|---|
| Tool | Ghidra MCP `batch_decompile` + `read_memory` + callers/xrefs; **no** `disassemble_bytes` |
| Live decompile | ≡ raw body above |
| Body bytes | `0x0040c700`–`0x0040c715` exclusive (**21 B**): `cmp byte [ecx+0x28],0` / `je` / `mov byte [ecx+0x28],0` / `add ecx,4` / `push ecx` / `call [IAT LeaveCriticalSection]` / `ret` / `CC` pad |
| ABI | MSVC **`__fastcall`/`__thiscall`**: `ECX` = list*; plain `ret`; void |
| Layout | `list+4` = `CRITICAL_SECTION`; `list+0x28` = traversal-lock flag — **exact inverse** of sealed `List_TraversalLock` (`0x004294f0`) |
| Callers | `Client_RecvSkillStatusEffect` (×2 sites), `FUN_0057ade0`, `FUN_008e5e00` (×2), `FUN_008fdb60`, `FUN_008ff5a0` (×2), `FUN_00920a50`, `FUN_0092c080`, plus additional sites in `0x0057cc03..0x0057d6ea` range — **17** UNCONDITIONAL_CALL total |
| Callees | `LeaveCriticalSection` (Win32 IAT) |
| Named | `List_TraversalUnlock` — role High (pair of string-sealed TraversalLock); product class name open |
