# Raw capture: FUN_00426f70

| Field | Value |
|---|---|
| **Stable ID** | `aa_00426f70` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00426f70` |
| **Canonical name** | `FUN_00426f70` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00426f70(int param_1)

{
                    /* WARNING: Could not recover jumptable at 0x00426f73. Too many branches */
                    /* WARNING: Treating indirect jump as call */
  std::basic_string<wchar_t,struct_std::char_traits<wchar_t>,class_std::allocator<wchar_t>_>::
  ~basic_string<wchar_t,struct_std::char_traits<wchar_t>,class_std::allocator<wchar_t>_>
            ((basic_string<wchar_t,struct_std::char_traits<wchar_t>,class_std::allocator<wchar_t>_>
              *)(param_1 + 0xc));
  return;
}
```

---

## R10-026 re-verify (2026-08-05) — append only

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **R10-026** |
| **Tools** | Live Ghidra `decompile_function` + `analyze_function_complete` + `get_function_by_address` + `read_memory` + `get_function_callers` + `get_function_xrefs` + `analyze_data_region` (IAT) + parent/caller decompiles `FUN_004271c0` / `FUN_00427590` / `FUN_005ac510`. **No** `disassemble_bytes`. |
| **Live decompile** | ≡ scaffold CF: thiscall ECX + 0xC → `~basic_string<wchar_t>` (decompiler shows call; bytes are tail JMP) |
| **Body** | `0x00426f70`–`0x00426f78` inclusive (**9 B** / `0x9`); Ghidra body `00426f70 - 00426f78`; `CC` pad after |
| **ABI** | **ECX = element\*** (`__fastcall`/`__thiscall` this); no stack formals; **void**; control does not return via local RET (tail JMP IAT) |
| **Classification** | stub (Ghidra); structural role = member-dtor **trampoline** |
| **Product name** | **`RunElem_WStringDtor_Plus0C_Trampoline_Inferred`** — no RTTI; role sealed from 2-insn body + dualed parent `StdVector_DestroyRuns_0x80_Inferred` |

### Full body hex (9 B + pad sample)

```
83c10cff25ac629c00cccccccccccccc
```

(`read_memory` 32 B: body 9 B then `CC` pad then next function prologue.)

### Byte-level CF (authority)

```
00426f70  ADD ECX, 0x0C                 ; 83 C1 0C
00426f73  JMP DWORD PTR [0x009C62AC]    ; FF 25 AC 62 9C 00
```

IAT slot `0x009c62ac` symbol (Ghidra):
`PTR_~basic_string<wchar_t,struct_std::char_traits<wchar_t>,class_std::allocator<wchar_t>_>_009c62ac`

IAT dword (image): `3A B0 6E 00` → `0x006EB03A` (runtime entry; not dualled).

### Decompile vs bytes

| Topic | Winner |
|---|---|
| thiscall ECX = host/elem base | **match** (param_1 / register ECX) |
| destroy wstring at +0x0C | **match** |
| call vs tail-JMP | **bytes win** — `FF 25` IAT JMP (decompiler "treating indirect jump as call" + false "jumptable" warning) |
| body size 9 B | **bytes** |
| no local epilogue RET | **bytes** |

### Callers / xrefs (live)

| From | Kind | Notes |
|---|---|---|
| `FUN_004271c0` @ `0x004271d2` | UNCONDITIONAL_CALL | dualed parent `StdVector_DestroyRuns_0x80_Inferred`; `MOV ECX,ESI` then CALL; elem stride **0x80** |
| `FUN_00427590` @ `0x00427599` | UNCONDITIONAL_CALL | destroy-range helper; loop `+0x80` (same elem family) |
| `FUN_005ac510` @ `0x005ac530` | UNCONDITIONAL_CALL | node tidy path (recursive free); trampoline still ECX-based |
| `Unwind@009ac2f4` @ `0x009ac2f7` | UNCONDITIONAL_CALL | SEH unwind |
| `Unwind@009add21` @ `0x009add27` | UNCONDITIONAL_CALL | SEH unwind |
| `Unwind@009add36` @ `0x009add39` | UNCONDITIONAL_CALL | SEH unwind |
| `Unwind@009be260` @ `0x009be266` | UNCONDITIONAL_CALL | SEH unwind |

`xref_count` = **7** (3 code + 4 unwind).

### Callees

None as direct CALL sites — single **IAT tail-jump** to MSVC `~basic_string<wchar_t>`.

### Layout (elem)

| Off | Field |
|---|---|
| elem+0x00 | other fields (not dualled here) |
| elem+0x0C | embedded `basic_string<wchar_t>` destroyed by this trampoline |
| elem stride (callers) | **0x80** (vector destroy-runs / destroy-range) |

### Notes

- **Not** a full element destructor / vtbl scalar deleting dtor — only `~wstring` at **+0x0C**.
- Decompiler "jumptable" warning is noise for `FF 25` IAT JMP.
- Retire scaffold alias `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Named_VOG_DEBUG_ST_00426f70`.
- Parent dual MEGA-128 already sealed this body as `ADD ECX,0xC; JMP IAT ~wstring`.
- Product element English beyond structural "run"/0x80 layout remains open → `_Inferred`.
- Terminal: **false** (no runtime Confirmed).
