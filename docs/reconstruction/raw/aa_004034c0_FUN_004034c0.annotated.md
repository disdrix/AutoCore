# Annotated low-level: FUN_004034c0 → StdList_InsertN_BeforeNode_Dword_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004034c0` |
| VA | `0x004034c0`–`0x00403522` inclusive (**99 B** / `0x63`) |
| System | shared MSVC `std::list` (missions-progression nested via debug list resize) |
| Date | 2026-08-05 (MEGA-134 refresh) |
| Canonical name | `StdList_InsertN_BeforeNode_Dword_Inferred` |
| Ghidra name | `FUN_004034c0` |
| Prior scaffold | `Named_CalleeOf_Named_CalleeOf_Client_DebugListMissionsStatus_004034c0` (**retired**) |

## Machine-level notes

- Source: raw capture + live MEGA-134 re-verify (Ghidra HTTP `decompile_function` / `disassemble_function` / `read_memory` / callers+xrefs).
- **Assembly overrides decompiler** on the insert call: list object is passed in **ECX** to `FUN_004040f0`, not as a hidden stack formal.
- SEH frame uses `LAB_009bd210`; catch body at `0x00403523` (not a separate dual unit).
- Prefer this annotated view for ABI; clean C mirrors sealed formals.

## Sealed ABI

| Slot | Role | Evidence |
|------|------|----------|
| **ECX** | `const uint32_t* val` | `MOV EBX,ECX`; later `PUSH EBX` as insert value* |
| **[EBP+8]** | `list*` | `MOV ECX,[EBP+8]` before `CALL FUN_004040f0` |
| **[EBP+0xC]** | `where` node* | `MOV EDI,[EBP+0xC]`; `PUSH EDI` |
| **[EBP+0x10]** | `count` | `MOV ESI,[EBP+0x10]`; loop `SUB ESI,1` |
| return | void | no EAX produce |
| cleanup | **`RET 0xC`** | three stack dwords |

## Pseudocode (assembly-corrected)

```c
// Insert `count` copies of *val immediately before `where` (MSVC list insert-N).
// Typical resize-grow call: where = list->_Myhead (sentinel) → N× push_back.
void StdList_InsertN_BeforeNode_Dword_Inferred(
    const uint32_t *val,   /* ECX */
    StdListDword *list,    /* stack1: +4 head, +8 size */
    ListNodeDword *where,  /* stack2 */
    unsigned count)        /* stack3 */
{
    /* SEH: LAB_009bd210 */
    for (; count != 0; --count) {
        // FUN_004040f0 — thiscall list:
        //   BuyNode(where, where->Prev, val) size 0xC
        //   Incsize(+1) via FUN_00404840 ("list<T> too long" / max 0x3fffffff)
        //   rewire where->Prev / new->Prev->Next
        StdList_InsertOne_BeforeNode_Dword(list, where, val); // FUN_004040f0
    }
}
```

## Callers / callees

| Kind | VA / name | Notes |
|---|---|---|
| Caller (sole) | `FUN_00402dd0` @ `0x00402dd0` site `0x00402de6` | list resize grow: `count = target - size`, where=head, ECX=&stack T |
| Callee | `FUN_004040f0` | insert one before node + Incsize |
| Indirect | `FUN_00418700` | BuyNode `operator_new(0xC)` dword payload |
| Indirect | `FUN_00404840` | `_Incsize` max `0x3fffffff` |

Parent of `00402dd0` includes `Client_DebugListMissionsStatus` (`0x009572e0`) among others — this unit remains a **shared STL helper**, not mission logic.

## Open questions

- Exact MSVC CRT source line / template specialization name (product demangle open) → keep `_Inferred`.
- Whether any other T (non-dword) shares a twin with different BuyNode size (this chain is **0xC** / dword only).
- Runtime / bit-exact / differential: **open** (no Launcher).
