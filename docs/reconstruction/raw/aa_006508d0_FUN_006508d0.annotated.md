# Annotated low-level: CVOGHBMissionEscort_OnHeartBeat

| Field | Value |
|---|---|
| Stable ID | `aa_006508d0` |
| VA | `0x006508d0` |
| Canonical name | `CVOGHBMissionEscort_OnHeartBeat` |
| Ghidra | `FUN_006508d0` |
| System | missions-progression / heartbeat |
| Date | 2026-08-04 (WQ7R-H live seal) |

## Machine-level notes

- **Body:** `0x006508d0`–`0x00650cf6` (`read_memory` entry/exit; epilogue `C2 04 00`).
- **ABI:** `__thiscall` (ECX = `CVOGHBMissionEscort*`); stack `EHBStatus* out`; **`ret 4`**; returns `out`.
- **Vtable bind:** `PTR_FUN_009e52c4` slot **+0x0C** = OnHeartBeat (after scalar dtor, StampLastFireTime, TryFire). DATA xref only — invoked dynamically via `CVOGHBBase_TryFire`.
- **Class:** COL @`0x00ab1e9c` → TD `0x00af4a10` **`.?AVCVOGHBMissionEscort@@`**. Ctor `FUN_006507c0` installs this vtable; sets period-class **0xb**.
- **Not outpost:** `"outpost"` string @`0x009e52a4` sits near default TFID pad `DAT_009e52b0`; RTTI for outpost is separate `CVOGHBOutpost` @`0x00af5134`.
- **Fail path drain:** `CVOGReaction_FailMission` then `FUN_005307e0` — matches FailMission dual **drain YES** table for this caller.
- **Success path:** `CVOGReaction_CompleteObjective(missionKey, 0xffffffff, 0xffffffff, force=1)`.
- **Out status:** `*out = DAT_00af514c` (**16** = Remove / `g_dwTimedActionDefaultPeriodMs` family from TryFire dual).

## Control flow (annotated)

```
bComplete = false
char = RTDynamicCast(owner, CVOGCharacter); if !char: *out=16; return out
missionNode = Lookup(char+0x548, missionKey)
lost = (missionNode == null)
if TFID cache invalid:
  scan char TFID list (or DAT_009e52b0 default); match CBID or obj-ref mode; stash TFID
target = ResolveFromTFID(cached TFID)
if !target: lost = true
else if !lost:
  refresh target; get player pos
  if maxRange>0 and dist(player,target) > maxRange: lost = true
  else: optional complete-zone object distance → bComplete
optional second ResolveObjectTarget arm may also set bComplete
if lost:
  if missionNode: FailMission(**(node+0x14c)); HandleEndQuest()
  if target: mark destroy (+0xcc=1); FUN_004d0e90; *out=16; return
else if !bComplete:
  RescheduleAfterFire; return out
else:
  CompleteObjective(missionKey, -1, -1, 1)
  optional destroy target if HB flag
*out = 16; return out
```

## Open questions

- Exact product field names for max escort distance / complete-zone TFID pair on HB blob.
- Whether force=1 CompleteObjective here is always correct for mid-chain escort objectives.
- Runtime / bit-exact distance behavior.
