# Annotated low-level: stoChunkReader_TextReadString (`FUN_0076a200`)

| Field | Value |
|---|---|
| Stable ID | `aa_0076a200` |
| VA | `0x0076a200`–`0x0076a2b5` |
| System | storage / arda2 `stoChunk` |
| Date | 2026-07-23 scaffold; W38-J seal 2026-08-04 |
| Canonical | `stoChunkReader_TextReadString` (**Inferred**) |

## Machine-level notes

- **ECX** = `std::string* out`; **EBX** = reader; plain **`ret`** (`C3`). No stack args on this leaf.
- Thin wrapper around sealed `stoChunkReader_ReadQuotedTag` (`0x00769b70`): `mov edi,ecx; push ebx; call`.
- ReadQuotedTag success (`hr >= 0`) → return **0** immediately (no further format).
- Fail → format `"Expected string but got \"%s\" in chunk \"%s\""` using **out** c_str (token/partial) + current chunk name (`FUN_00436250`), log `stoChunk.cpp:0x44a`, return **−1**.
- Mode peer: binary `FUN_00436f10` via dispatcher `FUN_00439a20` (dispatcher has stack reader + `ret 4`; this leaf does not).
- Decompiler shows `unaff_EBX` / `__fastcall param_1` — register ABI sealed from prologue + call sites, not decompiler names.
- Heavy fan-in (50+ CODE xrefs): effect/phy/anim unserializers, mode-dispatched string fields.

## Pseudocode (annotated)

See raw `aa_0076a200_FUN_0076a200.md` + W38-J live seal. Clean: `stoChunkReader_TextReadString.cpp`.
