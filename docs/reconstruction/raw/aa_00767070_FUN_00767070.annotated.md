# Annotated low-level: FUN_00767070 → stoChunkWriter_WriteTextIndent

| Field | Value |
|---|---|
| Stable ID | `aa_00767070` |
| VA | `0x00767070`–`0x0076709d` |
| System | `storage` / arda2 `stoChunk` |
| Date | 2026-07-23 scaffold; dual A/B annotate 2026-07-29 (W17-R) |

## Machine-level notes

- **Register ABI:** `stoChunkWriter*` in **ESI** (not stack; not ECX). Sole caller `stoChunkWriter_WriteFormatted` leaves writer in ESI.
- **Return:** sticky status dword at `writer+0x04` in **EAX**.
- **Purpose:** at line-start (`textCol==0` in caller), emit `depth * 4` space characters and set `textCol = depth*4`.
- Stream write via `(*stream->vtbl)[+0x18](buf, len)` — same write slot as WriteFormatted / EndChunk.
- Indent char is `DAT_00a2a000` = `" "` (single space).

## Writer layout used here

| Off | Dword idx | Field |
|---:|---:|---|
| `+0x00` | `[0]` | `stream*` (vtable object) |
| `+0x04` | `[1]` | sticky status (OR of write results) |
| `+0x18` | `[6]` | nest **depth** (BeginChunk++ / EndChunk--) |
| `+0x20` | `[8]` | **textCol** (column / line budget) |

## Pseudocode (annotated)

```c
// ESI = stoChunkWriter*  (register ABI)
// returns status (writer+0x04)

uint32_t stoChunkWriter_WriteTextIndent(void /* ESI = writer */)
{
  int count = writer->depth /* +0x18 */ * 4;
  writer->textCol /* +0x20 */ = count;          // advance column to indent width

  if (count > 0) {                              // decomp: if (-1 < count-1)
    do {
      // stream vtbl+0x18 write(DAT_00a2a000 /* " " */, 1)
      uint32_t hr = stream_write(writer->stream, &kSpace, 1);
      writer->status |= hr;
      count--;
    } while (count != 0);
  }
  return writer->status;
}
```

## Control flow

```
count = depth * 4
textCol = count
if count == 0: goto ret          // js after sub eax,1
loop:
  status |= write(" ", 1)
  count--
  jnz loop
ret:
  return status
```

No SEH. No branches other than zero-count skip and loop back.

## Call graph

| Direction | Symbol | VA | Note |
|---|---|---|---|
| Callee | stream `vt+0x18` write | — | virtual; not a fixed FUN_* |
| Caller | `stoChunkWriter_WriteFormatted` | `0x00767160` | call @ `0x007671ba` when textCol==0 |

## Open residual

- Product English name in source plate (`arda2/storage/stoChunk.cpp`) — structural name only.
- Runtime / bit-exact image diff not run.
