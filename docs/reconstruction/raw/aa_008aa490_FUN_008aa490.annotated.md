# Annotated low-level: FUN_008aa490 → MissionDialog_SetTurnInReadyFlag_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_008aa490` |
| VA | `0x008aa490`–`0x008aa4a6` (**23 B** / `0x17`) |
| System | `missions-progression` |
| Date | 2026-08-05 (MEGA-078 annotated refresh) |
| Ghidra name | `FUN_008aa490` |
| Proposed name | `MissionDialog_SetTurnInReadyFlag_Inferred` |
| Parent | `Client_ShowNpcMissionDialogUI` (`0x009438f0`) @ `0x00943a57` |
| Partition | MEGA-078 — Dialog pre-present setup |

---

## Machine-level notes

- Prefer assembly when decompiler conflicts: **EAX = host**, **DL = flag**; decompiler `in_EAX` / `__fastcall(param_1,param_2)` are framing artifacts.
- **No** stack formals; bare **`RET`** (`C3`).
- Offset **`+0x580`**: char turn-in readiness flag on NPC mission dialog host.
- Offset **`+0x6e8`**: optional child object*; if non-null, vcall **`vtbl[+4]`** with flag.
- Sole caller places this **after** `FUN_0052b420` (turn-in readiness probe) and **before** `Client_NpcDialog_PrepareResponseOpcode` / panel fill / reward chrome.
- Sibling residuals (not owned): `FUN_008aa4b0` (MEGA-058), `FUN_008aa510` (MEGA-046), `FUN_008ab550` (MEGA-051).

---

## Pseudocode (ABI-corrected)

```c
// EAX = MissionDialogHost* dialog
// DL  = char turnInReady  (0/1 from FUN_0052b420 path)
// void; bare RET
void MissionDialog_SetTurnInReadyFlag_Inferred(void /* EAX=dialog, DL=flag */)
{
  void* child = *(void**)((char*)dialog + 0x6e8);
  *(char*)((char*)dialog + 0x580) = (char)flag;   // DL
  if (child != NULL) {
    // thiscall: ECX=child; stack arg = full EDX (flag in low byte)
    (*(void(__thiscall**)(void*, uint32_t))(*(uint32_t*)child + 4))(child, edx);
  }
}
```

### Live Ghidra decompile (unedited; for CF compare)

```c
void __fastcall FUN_008aa490(undefined4 param_1,undefined4 param_2)
{
  int in_EAX;
  
  *(char *)(in_EAX + 0x580) = (char)param_2;
  if (*(int **)(in_EAX + 0x6e8) != (int *)0x0) {
    (**(code **)(**(int **)(in_EAX + 0x6e8) + 4))(param_2);
  }
  return;
}
```

---

## Assembly seals

| Bytes | Meaning |
|---|---|
| `8B 88 E8 06 00 00` | `MOV ECX,[EAX+0x6e8]` child |
| `85 C9` | `TEST ECX,ECX` |
| `88 90 80 05 00 00` | `MOV [EAX+0x580],DL` flag store |
| `74 06` | `JZ` skip vcall if child null |
| `8B 01` | `MOV EAX,[ECX]` child vtbl |
| `52` | `PUSH EDX` flag arg |
| `FF 50 04` | `CALL [EAX+4]` vtbl[+4] |
| `C3` | bare `RET` |

### Caller seal (`Client_ShowNpcMissionDialogUI`)

```
00943a43  CALL  FUN_0052b420       ; turn-in readiness → AL
00943a48  TEST  AL,AL
00943a4a  JZ    skip_set1
00943a4c  MOV   [ESP+0x1c],1
skip_set1:
00943a51  MOV   DL,[ESP+0x1c]
00943a55  MOV   EAX,EBX            ; dialog host
00943a57  CALL  FUN_008aa490
```

---

## Open questions / gaps

1. Product/PDB name for dialog host class and field `+0x580` / `+0x6e8`.
2. Product name for child `vtbl[+4]` method.
3. Whether upper 24 bits of EDX are contractually zero at the vcall (caller only writes DL).
4. Runtime / bit-exact / differential (Terminal false).
