# Annotated low-level: StringVecShell_TidyAndClearGlobal

| Field | Value |
|---|---|
| Stable ID | `aa_00444810` |
| VA | `0x00444810`–`0x0044484A` exclusive (**58 B**) |
| Ghidra | `FUN_00444810` |
| Preferred | `StringVecShell_TidyAndClearGlobal` |
| System | Palantir path-list shell tidy + global clear (`DAT_00d1f1fc`) |
| Date | 2026-07-29 (W34-K dual) |

## Machine-level notes

- **ABI:** ECX = `StringVecShell*`. SEH prologue does not clobber ECX (`MOV EAX,FS:[0]` / pushes only).
- **Callee:** `StringVec_Tidy` (`0x00431ae0`) — full buffer release (stride `0x1c`).
- **Global:** `DAT_00d1f1fc = 0` after tidy (published path-list shell pointer).
- **Callers:** only SEH unwind thunks for `Palantir_CompleteDtor` (`0x00754320`):
  - `009afd09`: `ECX = [EBP-0x10] + 0xA0` then `JMP` here
  - `009afd4a`: `ECX = [EBP+4] + 0xA0` then `JMP` here
- **Decompile gap:** shows no formal / `FUN_00431ae0()` bare — **bytes + unwind sites win**.

## Pseudocode (bytes authority)

```c
// ECX=shell; bare RET; SEH LAB_009bd525
void StringVecShell_TidyAndClearGlobal(StringVecShell* shell)
{
  StringVec_Tidy(shell);   // FUN_00431ae0
  DAT_00d1f1fc = nullptr;
}
```

## Open questions

- Product English for the path-list role of `DAT_00d1f1fc` / shell at `Palantir+0xA0`.
- Runtime / bit-exact.
