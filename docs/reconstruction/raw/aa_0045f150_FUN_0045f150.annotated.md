# Annotated low-level: StdUninitFillN_Elem28_CountEax_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0045f150` |
| VA | `0x0045f150`–`0x0045f198` (**73 B** / `0x49`) |
| System | stl-helpers / POD fill-n (elem 0x1c) |
| Date | 2026-07-29 (W35-L dual seal) |
| Ghidra | `FUN_0045f150` |
| Canonical | `StdUninitFillN_Elem28_CountEax_Inferred` |

## Machine-level notes

- Source: raw capture + W35-L `read_memory` full body.
- Prefer bytes when decompiler omits EAX count formal.
- Stride **0x1c** (7 dwords) sealed by `ADD EDX/EAX, 0x1C` and seven dword stores.
- Null dest gate mirrors dword fill sibling `Mem_FillDwordN_FromSrc_EaxEcxEdx` (still advances).
- Count uses unsigned `TEST/JBE` (zero and high-bit early-out).
- Template pointer (ECX) is **not** advanced — same value written `count` times.
- Leaf; bare `RET`.

## Pseudocode (bytes-sealed)

```c
// EAX = count; ECX = template* (const Elem28); EDX = dest*
// bare RET; leaf
void StdUninitFillN_Elem28_CountEax_Inferred(
    /*EAX*/ uint32_t count,
    /*ECX*/ const uint32_t* template_7dwords,
    /*EDX*/ uint32_t* dest)
{
  if (count == 0) return; // JBE
  do {
    if (dest != nullptr) {
      dest[0] = template_7dwords[0];
      dest[1] = template_7dwords[1];
      dest[2] = template_7dwords[2];
      dest[3] = template_7dwords[3];
      dest[4] = template_7dwords[4];
      dest[5] = template_7dwords[5];
      dest[6] = template_7dwords[6];
    }
    dest += 7; // +0x1c bytes
    count -= 1;
  } while (count != 0);
}
```

## Role in parents

Called from `StdVector_InsertN_Pod28` (`FUN_0045cd30`) on realloc and in-place hole-fill paths: place `count` copies of the insert value into a new/extended slot range. Parent snapshots value to stack (`[ebp-0x30]`) and passes that as ECX.

## Open questions

- Product/MSVC demangle (`uninitialized_fill_n` / `_Uninit_fill_n` plate).
- Concrete POD T identity for the 0x1c element.
- Runtime / bit-exact / differential.
