# Annotated low-level: FUN_0043e970 / GuardedVector_PushBack_Stride2_U32x2

| Field | Value |
|---|---|
| Stable ID | `aa_0043e970` |
| VA | `0x0043e970`–`0x0043e9dd` (**110 B**) |
| System | STL / guarded ring-deque (stride-2) |
| Date | 2026-07-29 (W31-J) |
| Named | `GuardedVector_PushBack_Stride2_U32x2` (**Inferred**) |

## Machine-level notes

- Prefer **bytes** over decompiler for ABI, slot address (`LEA EAX,[EAX+ESI*8]`), and stack formal.
- Container in **EAX** → EBX; grow callee `FUN_0043ea60` expects **EBX=this** (W30-A sealed).
- Page map: `+0x04` pages, `+0x08` capacity, `+0x0c` begin, `+0x10` size.
- Element stride **8** bytes; **2** slots per `new(0x10)` page; map index uses `>>1` / `&1`.
- Store helper `FUN_0043ece0` (not owned): null-safe copy of two dwords ECX→EAX.

## Annotated control flow

```
// EAX=container, stack=const U32x2*
PUSH EBX; MOV EBX,EAX; PUSH ESI; PUSH EDI

if ((begin+size)&1)==0 && capacity <= (size+2)>>1:
  PUSH 1; CALL GuardedVector_GrowBlockMap_Stride2  // EBX=this

abs = begin + size
page = abs >> 1
if capacity <= page: page -= capacity

if pages[page] == 0:
  pages[page] = operator_new(0x10)

dest = pages[page] + (abs & 1) * 8
CALL Pod8_CopyIfNonNull(dest /*EAX*/, value /*ECX*/)  // FUN_0043ece0

size += 1   // begin unchanged
POP EDI; POP ESI; POP EBX; RET 4
```

## Open residual

- Product/PDB class English for element type.
- Runtime / bit-exact under concurrent parents.
- Nested free of GrowBlockMap / operator_new (owned elsewhere).
