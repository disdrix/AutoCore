# Annotated low-level: Class_00a6f61c_ScalarDeletingDtor (FUN_004160b0)

| Field | Value |
|---|---|
| Stable ID | `aa_004160b0` |
| VA | `0x004160b0`–`0x004160cd` |
| System | MSVC scalar-deleting destructor (vtbl `PTR_FUN_00a6f61c`) |
| Date | 2026-08-04 (WQ9I-E dual seal) |
| Canonical | `Class_00a6f61c_ScalarDeletingDtor` |

## Machine-level notes

- **ABI:** `__thiscall` — **ECX** = `this`; stack **`flags`** (cleaned as 4 B); epilogue **`RET 4`**; returns **`this`** in EAX.
- Entry `56 8B F1` → ESI holds self for free/return.
- Always calls complete dtor **`FUN_0097a8a0`** (sets `*this = PTR_FUN_00a6f61c`, chains base `FUN_00792c20`).
- If `(flags & 1)`: `operator_delete(this)` (cdecl + `ADD ESP,4`).
- **Decompiler false-noreturn** on `operator_delete` — free returns; then `MOV EAX,ESI; RET 4`.
- Sole xref is **DATA** vtbl[0] at `0x00a6f61c` (value `0x004160b0`).
- Ctor twin `FUN_0097a8e0` installs same vtbl; UI asset `i_w_tut_2d_wnd_highlight.dds` (product class English still open).

## Pseudocode (annotated)

```c
// ECX = this; [ESP+4] = flags; RET 4
void * __thiscall Class_00a6f61c_ScalarDeletingDtor(void *self, byte flags)
{
  FUN_0097a8a0(self);                 // complete dtor (vtbl reinstall + base chain)
  if ((flags & 1) != 0) {
    operator_delete(self);            // returns; NOT noreturn
  }
  return self;
}
```

## Open questions

- Product / RTTI demangle for class behind `PTR_FUN_00a6f61c`.
- Full dual of complete dtor `0097a8a0` / base `00792c20` (not OWN).
- Runtime Confirmed.
