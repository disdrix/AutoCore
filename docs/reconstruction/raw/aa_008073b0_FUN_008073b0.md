# Raw capture: FUN_008073b0

| Field | Value |
|---|---|
| **Stable ID** | `aa_008073b0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x008073b0` |
| **Canonical name** | `FUN_008073b0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_008073b0(short param_1,undefined4 param_2)

{
  char cVar1;
  int unaff_ESI;
  
  if (*(int *)(unaff_ESI + 0xc7c) != 0) {
    cVar1 = (**(code **)(**(int **)(unaff_ESI + 0xc7c) + 8))();
    if (cVar1 != '\0') {
      (**(code **)(**(int **)(unaff_ESI + 0xc7c) + 0x18))(0xffffffff,param_2,(int)param_1,0);
      return 0;
    }
  }
  return 0x80004005;
}
```

---

## Versioned residual — dual A/B seal 2026-07-29 (W18-B)

| Field | Value |
|---|---|
| **Tools** | Ghidra `decompile_function` + `read_memory` only (no `disassemble_bytes`) |
| **Live decompile** | ≡ 2026-07-23 raw (`unaff_ESI` client, short size, buffer) |
| **Body range** | `0x008073b0`–`0x008073ee` |
| **Canonical name (sealed dual)** | `Client_SendGlobalPacket` |
| **System** | network-global (sibling of `Client_SendSectorPacket` @ `0x00807460`) |
| **Sibling offset** | sector conn `client+0xc78` / global conn `client+0xc7c` |

### Byte seal (`read_memory` @ `0x008073b0`, 64 B)

```
83 BE 7C 0C 00 00 00     cmp  dword ptr [esi+0xc7c], 0
74 0F                     jz   fail_E_FAIL
8B 8E 7C 0C 00 00         mov  ecx, [esi+0xc7c]     ; conn*
8B 01                     mov  eax, [ecx]           ; vtbl
FF 50 08                  call dword ptr [eax+8]    ; IsReady / connected?
84 C0                     test al, al
75 08                     jnz  do_send
B8 05 40 00 80            mov  eax, 0x80004005      ; E_FAIL
C2 08 00                  ret  8

; do_send:
0F BF 44 24 04            movsx eax, word ptr [esp+4]  ; size (short)
8B 8E 7C 0C 00 00         mov  ecx, [esi+0xc7c]
8B 11                     mov  edx, [ecx]
6A 00                     push 0                    ; reliability flags = 0
50                        push eax                  ; (int)size
8B 44 24 10               mov  eax, [esp+0x10]      ; buffer (orig [esp+8])
50                        push eax
6A FF                     push -1                   ; 0xFFFFFFFF channel mask
FF 52 18                  call dword ptr [edx+0x18] ; send
33 C0                     xor  eax, eax             ; S_OK = 0
C2 08 00                  ret  8
CC                        int3
```

### Closed form (sealed)

```text
// ESI = client* (NOT ECX thiscall); stack: short size, void* buffer; ret 8
conn = *(client + 0xc7c);
if (conn == null) return E_FAIL;                 // 0x80004005
if (conn->vtbl[+0x08]() == 0) return E_FAIL;     // not ready
conn->vtbl[+0x18](0xFFFFFFFF, buffer, (int)size, 0);
return 0;                                        // S_OK
```

### Contrast vs sector sibling `Client_SendSectorPacket` (`0x00807460`)

| | Global `0x008073b0` | Sector `0x00807460` |
|---|---|---|
| Client register | **ESI** | **ECX** (thiscall) |
| Conn offset | `+0xc7c` | `+0xc78` |
| Pre-send ready check | **vtbl+0x08** bool | none |
| Null conn | **E_FAIL** | silent drop, still 0 |
| Reliability arg | hardcode **0** | `~(EAX>>2)&2` ∈ {0,2} |
| Success return | **0** | **0** |
