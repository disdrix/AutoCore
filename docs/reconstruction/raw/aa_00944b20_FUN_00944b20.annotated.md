# Annotated low-level: Client_TeardownGameplaySession_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00944b20` |
| VA | `0x00944b20`–`0x00944d03` |
| Ghidra name | `FUN_00944b20` |
| Proposed name | `Client_TeardownGameplaySession_Inferred` |
| System | client-session / login / return-to-login |
| Date | 2026-07-29 (dual A/B seal; scaffold 2026-07-23) |

---

## Machine-level notes

- **Decompile ≡ body bytes** (`read_memory` 2026-07-29, 500 B covers full body). No `disassemble_bytes`.
- **ABI:** `this` arrives in **EAX** (not ECX); stack `char fullTeardown` cleaned by **`RET 4`**. Prologue: `PUSH EBX; PUSH ESI; XOR EBX,EBX; CMP [ESP+0xC],BL; PUSH EDI; MOV ESI,EAX`.
- Call sites almost always: `PUSH 1; MOV EAX, 0x00d1a840; CALL/JMP 0x00944b20` — global client object `g_ClientMain` @ `0x00d1a840`.
- **param_1 != 0** → deep UI/widget/audio/mission/map teardown; **param_1 == 0** still clears `+0xf44` buffer, `+0x113`/`+0x134`, and runs lighter leave paths.
- Callers: login fail (`RecvPlayFail`), account kicked, auth “return to login”, missing-map-content fail inside reinitPhysics.

## Control flow (sealed summary)

```
ESI = this (from EAX)
if (fullTeardown) {
  // five UI COM slots: set flags, Release(0) via vtbl+4 if non-null
  // +0x309c / +0x30a8 / +0x30a4 / +0x30ac / +0x30b0
  FUN_007fc970(); FUN_007fc360();
  Client_GetMissionCompleteAudioTable(); FUN_007246d0();
  if (*(this+0x31ec)) FUN_005742a0();
  if (buf = *(this+0x31f0)) { zero 0x82 dwords; *buf = -1; }
  FUN_00943db0(); FUN_0079e7d0(*(this+0xde8));
  FUN_00943b80(0); FUN_00442fa0(0,0,0,0,0); FUN_00410420(0,0);
}
// always:
if (*(this+0xf44)) operator_delete[](*(this+0xf44));
*(this+0xf44) = 0;
FUN_008076c0();
*(this+0x113) = 0; *(this+0x134) = 0;
if (fullTeardown) FUN_00801c50(this);
if (*(this+0xf40) && **(this+0xf40) == *(this+0xf38)) {
  if (fullTeardown) FUN_009302b0();
  FUN_00937560(this);
}
if (DAT_00d1b77c != 0) {
  if (fullTeardown) FUN_0048f370();
  FUN_009133a0(0);
}
return;
```

## Widget / flag map (fullTeardown only)

| Ready flag | Clear flag | COM pointer | Action if non-null |
|---|---|---|---|
| `+0x30b4=1` | `+0x30b5=0` | `+0x309c` | `(*com)->vtbl[4](0)` |
| `+0x30ba=1` | `+0x30bb=0` | `+0x30a8` | same |
| `+0x30b8=1` | `+0x30b9=0` | `+0x30a4` | same |
| `+0x30bc=1` | `+0x30bd=0` | `+0x30ac` | same |
| `+0x30be=1` | `+0x30bf=0` | `+0x30b0` | same |

Note: after first release, body also calls `FUN_007fc970` before continuing the remaining four pairs (order sealed by decompile).

## Open questions

1. Product names of the five UI COM objects and helper callees (`007fc970`, `007fc360`, `008076c0`, …).
2. Meaning of `+0xf40`/`+0xf38` equality gate and `DAT_00d1b77c` session singleton.
3. Whether any live path actually passes `fullTeardown=0` (kicked path forces 1 via stack rewrite + tail JMP).
