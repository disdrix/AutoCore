# Annotated low-level: Map_EraseRange_Isnil1d_Inferred (FUN_0051d700)

| Field | Value |
|---|---|
| Stable ID | `aa_0051d700` |
| VA | `0x0051d700`–`0x0051d7b4` (180 B exclusive) |
| Canonical | `Map_EraseRange_Isnil1d_Inferred` |
| System | STL / map-set (WQ-009 skill residual helper; isnil@+0x1d family) |
| Date | 2026-08-04 WQ9E-I OWN dual |

## Machine-level notes

- Source: raw capture + live re-verify for `aa_0051d700`.
- ABI: `__thiscall` ECX=map; stack `(out_it, first, last)`; **ret 0x0c**.
- isnil@**+0x1d** (not +0x29 / +0x15). Color for erase-node sibling lives near +0x1c (`FUN_0051c880`).
- Full clear when `first == *head && last == head`: free via `FUN_0051be10(root)`; reset head parent/left/right to head; size=0; `*out = *head`.
- Partial: inline tree successor (isnil@+0x1d) then `FUN_0051c880(map, &first, node)`.
- Sole caller: map host dtor `FUN_0051e120` (erase begin..end then `operator_delete(head)`).

## Pseudocode (annotated)

```c
void __thiscall Map_EraseRange_Isnil1d_Inferred(
    void *map,        // ECX — head @ +4, size @ +8
    void **out_it,
    void *first,
    void *last)
{
  void *head = *(void **)((char *)map + 4);

  // Full-range fast path: [begin, end) == whole tree
  if (first == *(void **)head && last == head) {
    FUN_0051be10(*(void **)((char *)head + 4)); // free root (parent of head)
    *(void **)((char *)head + 4) = head;        // parent
    *(unsigned *)((char *)map + 8) = 0;         // size
    *(void **)head = head;                      // left
    *(void **)((char *)head + 8) = head;        // right
    *out_it = *(void **)head;
    return;
  }

  while (first != last) {
    void *succ = first;
    // inline successor with isnil@+0x1d (same structure as Map_EraseRange,
    // different isnil offset)
    // ...
    FUN_0051c880(map, &first, first); // single-node erase + rebalance
    first = succ;
  }
  *out_it = first;
}
```

## Call graph

| Direction | Target | Notes |
|---|---|---|
| Caller | `FUN_0051e120` | complete dtor only |
| Callee | `FUN_0051be10` | full-tree free isnil@+0x1d |
| Callee | `FUN_0051c880` | single erase + RB rebalance |

## Family / peers (not OWN)

| VA | Role |
|---|---|
| `0x0051c7c0` | `Map_EraseRange` isnil@+0x29 |
| `0x0051d880` | `Map_EraseRange_B` isnil@+0x29 |
| `0x0051d7c0` | range erase isnil@+0x15 (WQ9E-J) |
| `0x004bacf0` | `StdMap_EraseRange_Inferred` isnil@+0x15 |
| `0x0051be90` | `Map_FreeSubtree_B` isnil@+0x29 (this dual sibling) |

## Open questions

- Product/PDB symbol; exact `map<K,V>` payload English.
- Dual of free `FUN_0051be10` / erase-node `FUN_0051c880` (not OWN).
- Runtime / bit-exact open.
