# Annotated low-level: StdMap_EraseRange_Val12 (`FUN_004e5120`)

| Field | Value |
|---|---|
| Stable ID | `aa_004e5120` |
| VA | `0x004e5120` |
| Canonical | `StdMap_EraseRange_Val12` (**Inferred**) |
| System | std map/set red-black tree range erase (Val12 / isnil@+0x19) |
| Date | 2026-07-29 (W28-P; supersedes 2026-07-23 scaffold annotate) |

## Machine-level notes

- **Body:** `0x004e5120`–`0x004e51d4` exclusive (**180** B / `0xB4`); pad `CC`.
- **ABI:** `__thiscall` ECX=map*; stack `(Node** outIt, Node* first, Node* last)`; **`ret 0x0c`** (byte-sealed both exits).
- **Map shell:** `head@+0x04` (leftmost / root / rightmost on head), **`size@+0x08`**.
- **Node Val12:** left@0 parent@4 right@8 value@0x0c; **isnil@+0x19** (used for successor; color not touched here).
- **Full-range fast path:** `first == head->left` && `last == head` → `FUN_004e2940(head->parent)` free-all, reset head self-links, size=0, `*outIt = *head`.
- **Partial range:** classic MSVC successor + single erase `FUN_004e4130` (W27-Q Val12 erase+rebalance).
- **Sole caller:** `FUN_004e7350` clears whole map then deletes sentinel head (map destroy / clear-and-free-head).
- Prefer this unit over isnil@+0x15 peer `aa_004bacf0` when the tree is Val12.

## Pseudocode (annotated)

```c
// __thiscall; RET 0x0C
void StdMap_EraseRange_Val12(MapShell* map, Node** outIt, Node* first, Node* last)
{
  Node* head = map->head;
  if (first == head->left && last == head) {
    FUN_004e2940(head->parent);   // recursive free non-nil Val12 nodes
    head->parent = head->left = head->right = head;
    map->size = 0;
    *outIt = head->left;          // begin == end
    return;                       // ret 0x0c
  }
  while (first != last) {
    Node* succ = first;
    if (!first->isnil) {          // +0x19
      // standard in-order successor (right-min or parent climb)
      // ...
    }
    FUN_004e4130(map, &first, first); // StdTree_EraseAndRebalance_Val12; ret 8
    first = succ;
  }
  *outIt = first;
  // ret 0x0c
}
```

## Ghidra residual

- Decompiler omits explicit `ret 0x0c`; sealed by bytes (`c2 0c 00` ×2).
- `FUN_004e4130` shown with 2 stack formals — correct for thiscall erase (`outIt`, `node`); ECX=map from `mov edi,ecx` / `mov ecx,edi` before call.
- Recursive free `FUN_004e2940` decompile may drop left-child recurse under false-noreturn on `operator_delete`; role as Val12 full-tree free is sealed by isnil@+0x19 + sole use from this full-range path.
