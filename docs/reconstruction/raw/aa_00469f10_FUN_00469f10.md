# Raw capture: FUN_00469f10

| Field | Value |
|---|---|
| **Stable ID** | `aa_00469f10` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00469f10` |
| **Canonical name** | `FUN_00469f10` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00469f10(undefined4 *param_1,undefined4 *param_2)

{
  undefined4 *puVar1;
  undefined4 *in_EAX;
  undefined4 *puVar2;
  int unaff_EBX;
  
  if (param_1 != param_2) {
    puVar1 = *(undefined4 **)(unaff_EBX + 8);
    puVar2 = param_1;
    for (; param_2 != puVar1; param_2 = param_2 + 1) {
      *puVar2 = *param_2;
      puVar2 = puVar2 + 1;
    }
    *(undefined4 **)(unaff_EBX + 8) = puVar2;
  }
  *in_EAX = param_1;
  return;
}
```

---

## W38-N re-verify (2026-08-04) — append only

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual W38-N |
| **Tools** | `batch_decompile` / `analyze_function_complete` + `read_memory` + callers/xrefs + sole-caller shrink site `0x0044a380`. **No** `disassemble_bytes`. |
| **Body** | `0x00469f10`–`0x00469f41` exclusive (**49 B** / `0x31`; pad `CC` then next) |
| **Canonical name** | `StdVector_DwordEraseRange_EbxVec_Inferred` (**Inferred**) |
| **Verdict** | **accept** |

### ABI (bytes win)

| Slot | Source | Evidence |
|---|---|---|
| `vec*` | **EBX** | `MOV ESI,[EBX+8]`; `MOV [EBX+8],EDX` |
| `outIt*` | **EAX** | final `MOV [EAX],EDI` (`*outIt = first`) |
| `first` | stack `[esp+4]` | `MOV EDI,[esp+8]` after `PUSH EDI` |
| `last` | stack `[esp+8]` | `MOV ECX,[esp+8]` at entry |
| cleanup | **`RET 8`** | `C2 08 00` |

Decompiler `unaff_EBX` / `in_EAX` / stack pair — **register formals sealed by bytes + sole caller**.

### Semantics

MSVC-style **`vector<uint32_t>::erase(first, last)`** for POD dword:

```
if (first != last) {
  dest = first;
  for (src = last; src != end; ++src, ++dest)
    *dest = *src;          // dword assigns (ADD 4)
  end = dest;              // *(vec+8) = dest
}
*outIt = first;
```

- **No** element dtors; **no** free / capacity shrink; begin `@+4` untouched.
- Stride **4** sealed (`ADD ECX,4` / `ADD EDX,4`).

### Full body hex (49 B)

```
8b4c2408578b7c24083bf9741e568b73083bce8bd77410558b29892a83c10483c2043bce75f25d8953085e89385fc20800
```

### Callers / callees

| Dir | Target |
|---|---|
| Caller (1) | `FUN_0044a380` @ `0x0044a3df` (`StdVector_DwordResize_EaxCount_Inferred` shrink; W37-Z) |
| Callees | none (leaf) |

Sole-caller shrink site:

```
LEA EAX, [begin + n*4]   ; first
PUSH end                 ; last
PUSH first
LEA EAX, [esp+0x18]      ; outIt*
MOV EBX, ECX             ; vec*
CALL FUN_00469f10
```

### Twin distinction

| VA | Name | ABI |
|---|---|---|
| `0x00410490` | `StdVector_EraseRange_Dword_Inferred` (W16-S) | thiscall `ECX=vec`; stack `outIt,first,last`; **`RET 0xC`**; `memmove` |
| `0x00469f10` | **this unit** | **EBX=vec**, **EAX=outIt\***, stack `first,last`; **`RET 8`**; dword loop |

Do **not** merge.
