# Annotated low-level: QuickBar_RebindPageColumns_Inferred (`FUN_008a0e10`)

| Field | Value |
|---|---|
| Stable ID | `aa_008a0e10` |
| VA | `0x008a0e10` |
| Canonical name | `QuickBar_RebindPageColumns_Inferred` |
| System | `skills-abilities` |
| Date | 2026-07-29 (W22-D dual seal) |

## Machine-level notes

- Source: raw capture + live `decompile_function` + `read_memory` (no `disassemble_bytes`).
- **stdcall** one arg (`RET 4`): host dialog pointer on stack.
- Early-outs: host virtual `vtbl+0x3d8` must return non-zero **and** `DAT_00d1b6d8 != 0`.
- Then `FUN_007fce10(&DAT_00d1a840)` (UI context prep; product name open).
- Loop **exactly 10** columns; widgets at `host+0x63c` (`host[399]`).
- Page index from `host+0x50c` (`host[0x143]`); slot key = `page*10 + col`.
- Type table `DAT_00d1da60[key]` stride **0x18**: type@+0, id@+8, skillId@+0x10.
- Decompiler **understates** type-1/2 callee arguments; bytes carry register/stack setup.

## Host layout (this unit)

| Off / index | Role | Confidence |
|---|---|---|
| `host[0]` vtbl | `+0x3d8` visibility/ready virtual | High (call site) |
| `host[0x143]` / `+0x50c` | current page | High (SelectPage dual) |
| `host[399..408]` / `+0x63c` | 10 column widget pointers | High |

## Type dispatch

| type | Action | Callee |
|---|---|---|
| 1 | Bind skill to column widget | `FUN_00826860` |
| 2 | Bind item to column widget | `FUN_008283a0` |
| 5 | Bind macro (const 5 + id) | `FUN_00827670` |
| else | Clear column widget | `QuickBar_ClearActiveSlot_INFERRED` |

### Callee ABI (image; decompiler incomplete)

```
type 1:  EAX = *(DAT_00d1da70 + key*0x18);  // skill id
         ECX = widget;  push widget;  CALL FUN_00826860
type 2:  ECX = *(DAT_00d1da68 + key*0x18);  // item id
         EAX = widget;  CALL FUN_008283a0
type 5:  push *(DAT_00d1da68 + key*0x18);   // macro id
         EAX = 5;  CALL FUN_00827670
else:    ESI = widget;  CALL QuickBar_ClearActiveSlot_INFERRED
```

## Pseudocode (annotated; CF ≡ decompile + callee ABI notes)

```c
// stdcall: host on stack
void QuickBar_RebindPageColumns_Inferred(int *host)
{
  if (!host->vtbl[+0x3d8](host)) return;
  if (DAT_00d1b6d8 == 0) return;

  FUN_007fce10(&DAT_00d1a840);

  int *slotWidget = &host[399]; // +0x63c
  for (int col = 0; col < 10; col++, slotWidget++) {
    int key = col + host[0x143] * 10; // page*10+col
    int type = *(int *)((char *)&DAT_00d1da60 + key * 0x18);
    int widget = *slotWidget;

    if (type == 1) {
      // FUN_00826860(widget, skillId) — see ABI above
      FUN_00826860(/*widget*/);
    } else if (type == 2) {
      FUN_008283a0(/*widget, itemId*/);
    } else if (type == 5) {
      FUN_00827670(*(uint32_t *)((char *)&DAT_00d1da68 + key * 0x18));
    } else {
      QuickBar_ClearActiveSlot_INFERRED(/*widget*/);
    }
  }
}
```

## Callers (role)

| Caller | Role |
|---|---|
| `QuickBar_SelectPage_Inferred` `0x008a0ed0` | After page store — rebind columns for new page |
| `CDlgQuickBar_CreateChildWidgets_Inferred` `0x008a16b0` | Initial page column rebind after build |
| `FUN_008a1280` | Related QB host path |
| `Client_RecvCreateCharacter` | Post-login / char create UI refresh of QB |

## Open questions

- Product names for type-1/2/5 binders and `FUN_007fce10` / `DAT_00d1b6d8` / `DAT_00d1a840`.
- Exact skill/item/macro field meanings inside the 0x18 slot row beyond type/id.
- Whether type-1 push of widget is a hidden second formal or SEH/cookie residual (image pushes before call).
- Runtime: page switch rebind + empty slots clear.
