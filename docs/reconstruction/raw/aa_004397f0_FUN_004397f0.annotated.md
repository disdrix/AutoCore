# Annotated low-level: CircularSentinel_Alloc0x10_Inferred (`FUN_004397f0`)

| Field | Value |
|---|---|
| Stable ID | `aa_004397f0` |
| VA | `0x004397f0`–`0x00439809` (**26 B** / `0x1A`) |
| System | shared circular-list / hash-bucket node factory |
| Date | 2026-07-23 scaffold; W35-C seal 2026-07-29 |

---

## Machine-level notes

- **Role:** allocate a **0x10-byte** node and install **circular next/prev** (`*node = node`, `*(node+4) = node`). Return node* in **EAX**.
- **ABI:** no formals; bare **`C3`**; cdecl `operator_new` cleaned with `add esp,4`.
- **Bytes ≡ decompile CF** for stores; decompiler `void` return is **wrong** (EAX preserved through stores).
- **Family:** identical control flow to `CircularSentinel_Alloc0x28_Inferred` (`0x00423d60`, 26 B) and `StdList_AllocSentinel_0xC_Inferred` (`0x004933f0`, 26 B) with size **0x10**. Non-SEH.
- **Callers (4):**
  - `FUN_00457ac0` (`NestedHash_Ctor_Sentinel0x10_Inferred`, W34-E) @ `0x00457adb` — sole NestedHash bag factory path for this size
  - `FUN_004a0b90` @ `0x004a0df2` — NDSpecialFX instantiate temp list
  - `FUN_007c6c50` @ `0x007c6ca2`
  - `FUN_007c7200` @ `0x007c7252`
- Product English for remaining **0x08** payload bytes of the node is **unowned / open**.

## Control flow (annotated)

```
push 0x10
call operator_new          ; EAX = node or null
add esp, 4
test eax, eax
jz  skip_next
  mov [eax], eax           ; next = self
skip_next:
lea ecx, [eax+4]
test ecx, ecx              ; null+4 → 4 (non-null); only nulls if EAX null and path hit
jz  skip_prev
  mov [ecx], eax           ; prev = self
skip_prev:
ret                        ; EAX = node*
```

## Open questions

- Product type of 0x10 node (hash entry vs list node with key/value).
- Whether any caller relies on unwritten payload dwords after +8.
- Runtime / bit-exact / differential.
