# Annotated low-level: CVOGHB_TargetingLink_scalar_dtor_Inferred (`FUN_00512070`)

| Field | Value |
|---|---|
| Stable ID | `aa_00512070` |
| VA | `0x00512070`–`0x0051208d` |
| System | `heartbeat` / targeting-link |
| Date | 2026-07-29 W19-G OWN dual |
| Canonical | `CVOGHB_TargetingLink_scalar_dtor_Inferred` |

---

## Machine-level notes

- Classic **MSVC scalar-deleting destructor** (`??_G` shape): complete dtor then optional `operator_delete` when `flags & 1`.
- **ABI:** `__thiscall` — `this` in ECX; stack `flags` (byte); **`ret 4`**.
- **Vtbl placement:** sole DATA xref is **slot 0** of `PTR_FUN_009cdfb0` (family installed by `CVOGHB_TargetingLink_ctor_Inferred` / `FUN_00604d40`).
- **Base body `FUN_00512060`:** rewrites vtbl to `PTR_FUN_009cdfb0` (idempotent for base objects) then `FUN_00508390` (owner-list detach path).
- **Derived twin:** `CVOGHBPlayerTargetingLink_scalar_dtor` @ `0x0051b870` (vtbl `009cdfd4` slot 0) calls same base body via near call/thunk then optional delete — same CF shape.
- No product strings in body. Class product name for base family remains **Inferred** (`CVOGHB_TargetingLink_*` from ctor dual); no RTTI string sealed on this VA alone.

---

## Signature (semantic)

```c
// MSVC scalar deleting dtor — vtbl[0]
void* __thiscall CVOGHB_TargetingLink_scalar_dtor_Inferred(
    void* thisHb /* ECX */,
    uint8_t flags /* stack+4; bit0 => operator_delete */);
// ret 4
```

---

## Pseudocode (annotated)

```c
void* __thiscall FUN_00512070(void* this, uint8_t flags)
{
  FUN_00512060(this);           // complete dtor: vtbl demote + detach
  if ((flags & 1) != 0) {
    operator_delete(this);      // heap free when deleting dtor
  }
  return this;
}
```

---

## Open questions

1. Exact MSVC mangled name (`??_G…` vs `??_E…`) — no PDB.
2. Product RTTI class string for base targeting-link (ctor dual uses `_Inferred`).
3. Whether a separate array-deleting dtor exists (not this body).
