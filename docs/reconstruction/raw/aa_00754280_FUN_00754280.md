# Raw capture: FUN_00754280

| Field | Value |
|---|---|
| **Stable ID** | `aa_00754280` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00754280` |
| **Canonical name** | `FUN_00754280` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_00754280(void)

{
  undefined4 *puVar1;
  undefined4 *puVar2;
  int unaff_EDI;
  
  if (*(int *)(unaff_EDI + 0xd4) != 0) {
    vog_LogMessage("C:\\vog\\1_code\\palantir\\palantir\\framework\\Palantir.cpp",0x1fd,2,
                   "Preload thread already runnning.");
    return 0;
  }
  puVar1 = operator_new(0x10);
  puVar2 = (undefined4 *)0x0;
  if (puVar1 != (undefined4 *)0x0) {
    puVar1[3] = 0;
    puVar1[1] = 0;
    puVar1[2] = 0;
    *puVar1 = &PTR_FUN_00a9e9bc;
    puVar1[3] = *(undefined4 *)(DAT_00d1f050 + 0x6c);
    puVar2 = puVar1;
  }
  *(undefined4 **)(unaff_EDI + 0xd4) = puVar2;
  FUN_0071aef0();
  return 1;
}
```

---

## W31-F re-verify (2026-07-29) — append only

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `analyze_function_complete` + `read_memory` (+ callers/callees) |
| **Body** | `0x00754280`–`0x007542e8` (**105 B** / `0x69`); pad `CC` after last RET |
| **ABI** | **EDI=host** (Palantir / `*DAT_00d1f058`); **0** stack formals; plain `RET`; return **AL** 0/1 |
| **Live decompile** | ≡ raw 2026-07-23 CF (already-running gate, new thrThread 0x10, store `+0xd4`, start thread, ret 1) |
| **Bytes delta vs decompile** | Decompiler omits **ESI=thread**, **EDX=0x40000** stack size, **AL=0** (no CREATE_SUSPENDED) into `FUN_0071aef0` — sealed from `read_memory` |
| **Classification** | **worker** — callees `vog_LogMessage`, `operator_new`, `FUN_0071aef0` (thrThread CreateThread) |
| **Xrefs (2)** | `0x007b7724` (`FUN_007b75b0` AssManager init), `0x00934453` (`FUN_00934420` Preloading enabled) |
| **Callers** | `FUN_007b75b0`, `FUN_00934420` |
| **Name** | `Palantir_StartPreloadThread_Inferred` (Ghidra `FUN_00754280`; **Inferred** from Palantir.cpp:0x1fd + "Preload thread already runnning." + thrThread Create + host `+0xd4`) |

### Full body hex (105 B)

```
5633f639b7d4000000741d68d8f7a9006a0268fd0100006810f9a900e81f2b240083c41032c05ec36a10e8e355d3ff83c4043bc6741d89700c897004897008c700bce9a9008b0d50f0d1008b516c89500c8bf0ba0000040032c089b7d4000000e80b6cfcffb0015ec3
```

### Sealed semantics

1. If `host+0xd4 != 0`: log Palantir.cpp:0x1fd level 2 `"Preload thread already runnning."` → **AL=0** RET.
2. Else `operator_new(0x10)` thrThread-sized object:
   - On non-null: zero `+0x04/+0x08/+0x0c`; vftable `PTR_FUN_00a9e9bc`; `+0x0c = *(DAT_00d1f050 + 0x6c)` (NDResourceCache-related slot).
   - ESI = object or **0** if new failed.
3. `host+0xd4 = ESI` (stores null on OOM).
4. `FUN_0071aef0` with **ESI=thread**, **EDX=0x40000** (stack size), **AL=0** → CreateThread start routine `LAB_0071aed0`, flags `0x10000` (STACK_SIZE_PARAM_IS_A_RESERVATION), not suspended.
5. **AL=1** RET (even if new returned null — retail CF).

### Layout (host / thread)

| Off | Object | Role |
|-----|--------|------|
| host `+0xd4` | Palantir host | preload thrThread* |
| thr `+0x00` | thrThread | vftable `PTR_FUN_00a9e9bc` |
| thr `+0x04` | thrThread | thread id (filled by CreateThread) |
| thr `+0x08` | thrThread | HANDLE (filled by CreateThread) |
| thr `+0x0c` | thrThread | context ptr from `DAT_00d1f050+0x6c` |

### Strings (read_memory)

- `0x00a9f910`: `C:\vog\1_code\palantir\palantir\framework\Palantir.cpp`
- `0x00a9f7d8`: `Preload thread already runnning.` (retail typo: runnning)
- Adjacent rdata near vftable: `Waiting for preload thread to shut down (%d)...\n`
