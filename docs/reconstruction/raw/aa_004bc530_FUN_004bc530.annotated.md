# Annotated low-level: CNDHash_TraverseToNext_ListNext20 (FUN_004bc530)

| Field | Value |
|---|---|
| Stable ID | `aa_004bc530` |
| VA | `0x004bc530`–`0x004bc57d` inclusive (**78 B** / `0x4E`) |
| Canonical name | `CNDHash_TraverseToNext_ListNext20` |
| Ghidra name | `FUN_004bc530` |
| Rejected scaffold | `Named_VOG_DEBUG_STOP_004bc530` (assert string only) |
| System | container / CNDHash (interaction-activation consumers via `FUN_004bae00`) |
| Date | 2026-08-05 (MEGA-029 dual seal; annotated refresh) |
| Twin | dualed `CNDHash_TraverseToNext` `0x00411900` — **different node layout** |

---

## Machine-level notes

- Source: raw capture `aa_004bc530` + MEGA-029 live re-verify (decompile ≡ raw CF).
- **ABI sealed:** `__thiscall`; ECX = hash this; stack `int* cursor`; EAX = payload or 0; **`RET 4`**.
- **Traversal lock** at `this+0x1d` (char). Unlocked → dual log via `FUN_007a4480`, then **fall-through continues** (not fatal).
- **List head** at `this+0x14`.
- **Node family (this VA):** list-next at **`node+0x20`**, payload/value at **`node+0x0c`** (0x28-class / inv-style nodes).
- **Twin `0x00411900`:** next at `node+0x14`, payload at `node+8` (0x1c-class / skill-style). Same product string; **do not merge ports**.
- Companion lock setter: `FUN_00402c40` (`HashError:TraversalLock, already locked for traversal`) sets `+0x1d = 1`.
- Sole caller shell `FUN_004bae00` locks → calls this → clears `+0x1d` (two hash slots at parent `+4` / `+8`).

---

## Pseudocode (annotated)

```c
// __thiscall RET 4
// ECX = CNDHash* this; stack = int* cursor (inout node*)
// returns payload*(node+0xc) or 0
undefined4 __thiscall CNDHash_TraverseToNext_ListNext20(int this_hash, int *cursor)
{
  int node;

  // TraversalLock required (read-only check; non-fatal log)
  if (*(char *)(this_hash + 0x1d) == '\0') {
    FUN_007a4480(0, "HashError:TraverseToNext, not locked for traversal"); // @ 0x00a27c9c
    FUN_007a4480(0, "VOG_DEBUG_STOP");                                     // @ 0x00a15844
    // fall through — does NOT return early
  }

  if (*cursor == 0) {
    // first step: seed from ordered-list head
    *cursor = *(int *)(this_hash + 0x14);
    node = *(int *)(this_hash + 0x14);
  }
  else {
    // subsequent steps: advance via node list-next @ +0x20
    node = *(int *)(*cursor + 0x20);
    *cursor = node;
  }

  if (node != 0) {
    return *(undefined4 *)(node + 0xc); // payload / value
  }
  return 0; // end of list
}
```

---

## Offsets (sealed)

| Object | Offset | Role | Conf |
|---|---|---|---|
| hash this | `+0x14` | ordered list head (node*) | **Confirmed** |
| hash this | `+0x1d` | traversal lock byte | **Confirmed** |
| node | `+0x20` | list next (this family) | **Confirmed** |
| node | `+0x0c` | payload returned to caller | **Confirmed** |

---

## Call graph (OWN evidence)

```text
FUN_004bae00  [parent shell — not dualled here]
  ├─ FUN_00402c40  TraversalLock (set +0x1d=1)
  ├─ FUN_004bc530  CNDHash_TraverseToNext_ListNext20  [OWN MEGA-029]
  │     └─ FUN_007a4480  (log only if unlocked)
  └─ clear +0x1d = 0

Twin (do not merge):
  FUN_00411900  CNDHash_TraverseToNext  [dualed; next+0x14 / payload+8]
```

---

## Open questions / gaps

- Runtime / bit-exact / differential (no Launcher).
- Product stamp / exact COList table English for this 0x28-node family instance (offsets sealed; class brand via HashError only).
- Parent `FUN_004bae00` dual residual (OWN other).
- Full freelist/bucket sibling map for this stamp pair.
