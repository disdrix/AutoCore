# Raw capture: FUN_0040a6e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0040a6e0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0040a6e0` |
| **Canonical name** | `FUN_0040a6e0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0040a6e0(int param_1,undefined4 param_2)

{
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  puStack_8 = &LAB_009bdcb1;
  local_c = ExceptionList;
  local_4 = 0;
  if (param_1 != 0) {
    ExceptionList = &local_c;
    FUN_00408880(param_2);
  }
  ExceptionList = local_c;
  return;
}
```

---

## Re-verify append — WQ9I-D (2026-08-04)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **WQ9I-D** |
| **Tools** | `batch_decompile` + `analyze_function_complete` + `read_memory` + callers/xrefs + `get_assembly_context` |
| **No** | `disassemble_bytes`; Launcher; parent ledgers |
| **Live decompile** | **≡** 2026-07-23 raw body (SEH + null-dest gate + `FUN_00408880`) |
| **Body** | `0x0040a6e0`–`0x0040a729` exclusive end / **74 B** (`0x4A`); pad `CC` |
| **Epilogue** | `ADD ESP,0x10` (SEH frame) + bare **`C3`** (cdecl formals; parent cleans) |
| **Callee** | `PodCopyConstruct_Elem0x138_Inferred` (`0x00408880`, dualed WQ9H-I) — thiscall ECX=dest, stack src, **RET 4** |
| **Sole xref** | `FUN_00409f10` @ `0x00409f1a` — thin stdcall wrapper (`RET 8`) |
| **Upstream** | `FUN_00409cc0` ConstructN fill (dest, count, src) stride **0x138**; `FUN_0040a4a0` uninit-copy range (src_begin, src_end, dst) stride **0x138** |
| **Named** | `PodPlacementCopyConstruct_IfNonNull_Elem0x138_Inferred` |
| **Terminal** | **false** |

### Entry / SEH / gate bytes (hex prefix)

```text
6A FF 68 B1 DC 9B 00 64 A1 00 00 00 00 50 64 89 25 00 00 00 00
51 8B 4C 24 14 89 0C 24 C7 44 24 0C 00 00 00 00 85 C9 74 0A
8B 44 24 18 50 E8 6D E1 FF FF
… ADD ESP,10 / C3 / CC…
```

### Sealed ABI

| Slot | Role |
|------|------|
| Stack[0x4] | `void* dest` (placement site; null → no-op) |
| Stack[0x8] | `const void* src` (0x138 POD source) |
| ECX at CALL | dest (thiscall handoff into `00408880`) |
| cleanup | bare `ret`; sole parent `ADD ESP,8` |
| return | void |

### Call site (wrapper)

```text
00409f10  MOV EAX, [ESP+8]   ; src
00409f14  MOV ECX, [ESP+4]   ; dest
00409f18  PUSH EAX
00409f19  PUSH ECX
00409f1a  CALL 0040a6e0
00409f1f  ADD ESP, 8
00409f22  RET 8
```
