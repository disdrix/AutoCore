# Annotated low-level: FUN_009373e0 → Client_DialogGate_OpenIndex10_ParkDriveAxes_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_009373e0` |
| VA | `0x009373e0`–`0x00937462` (**131 B** / `0x83`) |
| System | `input-drive-control` |
| Date | 2026-08-05 (MEGA-024 refresh) |
| Canonical name | `Client_DialogGate_OpenIndex10_ParkDriveAxes_Inferred` |
| Ghidra name | `FUN_009373e0` |

## Machine-level notes

- Source: raw capture + MEGA-024 live re-verify (`disassemble_function` + `read_memory` + call-site context).
- Prefer assembly when decompiler conflicts (ABI, handbrake arg, stack formal).
- **EDI** = client host (caller sets `MOV EDI,ECX`).
- Stack formal (1 dword) feeds `FUN_008aa4b0` as NPC/object stamp; sole site passes **0**.
- Gate: `client+0xf40` non-null and `*(*f40) == *(f38)`.
- Open: `Client_OpenDialogByIndex(client, 10, 0, 0)`.
- Caption: `MissionDialog_SetNpcObjectAndNameCaption` on `*(client+0x1058)`.
- Park: thr **0**, steer **0**, handbrake **1** on vehicle at `*(client+0xe98)+0x250`.
- HB path is **tail JMP** to `VehicleEntity_SetHandbrake` after rewriting `[ESP+4]=1`.
- Epilogue early exits: **`RET 4`**.

## Pseudocode (annotated; machine ABI)

```c
// Retail ABI: EDI=client host; stack arg dword (NPC stamp for caption helper); RET 4
// Ghidra scaffold showed void/unaff_EDI and SetHandbrake() with no arg — corrected below.

void Client_DialogGate_OpenIndex10_ParkDriveAxes_Inferred(/* EDI */ Client* client,
                                                          int npcOrClear /* stack */)
{
  void** dialogHostPtr = *(void***)(client + 0xf40);
  if (dialogHostPtr == 0)
    return; /* RET 4 */

  if (*dialogHostPtr != *(void**)(client + 0xf38))
    return; /* RET 4 */

  Client_OpenDialogByIndex(client, /*index*/ 10, /*force*/ 0, /*allowDrive*/ 0);

  {
    void* missionDialog = *(void**)(client + 0x1058);
    // FUN_008aa4b0: ESI=missionDialog, EAX=npcOrClear
    MissionDialog_SetNpcObjectAndNameCaption_Inferred(missionDialog, npcOrClear);
  }

  void* actor = *(void**)(client + 0xe98);
  if (actor == 0)
    return; /* RET 4 */

  VehicleEntity* vehicle = *(VehicleEntity**)((char*)actor + 0x250);
  if (vehicle == 0)
    return; /* RET 4 */

  VehicleEntity_SetLongitudinalInput(vehicle, 0.0f);
  VehicleEntity_SetSteerInput(vehicle, 0.0f);
  // tail-call: stack formal rewritten to 1
  VehicleEntity_SetHandbrake(vehicle, 1);
}
```

## Offsets (High)

| Offset | Role |
|---|---|
| `client+0xf40` | active dialog host pointer-pointer |
| `client+0xf38` | focus / current dialog host* compared to `**f40` |
| `client+0x1058` | mission dialog UI host* for caption helper |
| `client+0xe98` | local actor / player object* |
| `actor+0x250` | vehicle entity* |
| vehicle `+0x614` / `+0x618` / `+0x61c` | thr / steer / HB (via dualed setters) |

## Call graph

```
stub 0x00952b20 (vmethod RET 8, returns 1)
  └─ FUN_009373e0  Client_DialogGate_OpenIndex10_ParkDriveAxes_Inferred  [OWN MEGA-024]
        ├─ Client_OpenDialogByIndex (0x007fef20) index=10
        ├─ MissionDialog_SetNpcObjectAndNameCaption_Inferred (0x008aa4b0)
        ├─ VehicleEntity_SetLongitudinalInput (0x004f5650) 0
        ├─ VehicleEntity_SetSteerInput (0x004f5620) 0
        └─ VehicleEntity_SetHandbrake (0x004f3620) 1  [tail]
```

## Open questions

- Product English for dialog index **10** slot (menu enum residual on `Client_OpenDialogByIndex`).
- Product name of vmethod stub @ `0x00952b20` and why RET 8 (two unused formals).
- Exact product meaning of `f40`/`f38` host equality beyond "dialog gate".
- Runtime / bit-exact / differential — open.
