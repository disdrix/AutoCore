# Annotated low-level: HandlerTable54_FindById6c (`FUN_00508c00`)

| Field | Value |
|---|---|
| Stable ID | `aa_00508c00` |
| VA | `0x00508c00` |
| Body | `0x00508c00`–`0x00508c41` (66 B) |
| System | status/hook handler tables (multi-slot family) |
| Date | 2026-07-29 (W20-R dual re-verify) |
| Ghidra | `FUN_00508c00` |
| Inferred | `HandlerTable54_FindById6c` |

## Machine-level notes

- **Leaf** linear scan; no callees.
- Prefer **`read_memory`** for ABI: `ret 4`, ECX=this, one stack id.
- Decompiler CF matches bytes exactly (no residual unaffs).
- Table is an array of **pointers**; each pointee has **id at `+0x6c`**.
- Global **`DAT_00d02988`** is the exclusive scan limit (not a local length field).

## Pseudocode (annotated)

```c
// INFERRED: HandlerTable54_FindById6c
// ECX = registry*; stack0 = int32 id; ret 4
void *__thiscall HandlerTable54_FindById6c(void *registry, int id)
{
  int i;
  void **table;

  i = 0;
  if (id != -1 && DAT_00d02988 > 0) {
    table = *(void ***)((char *)registry + 0x54);
    do {
      // entry id @ +0x6c
      if (*(int *)((char *)table[i] + 0x6c) == id) {
        return table[i];
      }
      i = i + 1;
    } while (i < DAT_00d02988);
  }
  return 0;
}
```

## Role

Find a **handler/def entry** by id in the registry’s **`+0x54`** pointer table. Used by equip status-hook applicator for subtype **0xb** and by loot/item prefix helpers in the same family.

## Open questions

- Product name of registry class and entry type.
- Who populates `DAT_00d02988` and the `+0x54` vector.
- Whether id `-1` is a sentinel “no skill/mod” only or also used as clear.
