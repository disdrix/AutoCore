# Annotated low-level: FUN_00437050 / stoChunkReader_ReadU32

| Field | Value |
|---|---|
| Stable ID | `aa_00437050` |
| VA | `0x00437050`–`0x00437095` |
| Canonical name | `stoChunkReader_ReadU32` |
| Prior / Ghidra | `FUN_00437050` |
| System | `storage` / arda2 `stoChunk` |
| Date | 2026-07-23 scaffold; dual A/B seal 2026-07-29 (W18-N OWN) |

## Machine-level notes

- Prefer body bytes over decompiler for register ABI: entry **EAX = reader**, not ECX thiscall.
- Decompiler `in_EAX` offsets are **reader-relative** and match `window = reader+0x14` fields used by sibling `stoChunkStreamWindow_ReadHeader16` (`aa_004368b0`).
- Sole nested call: `FUN_00435df0` thiscall with **ECX = window**, stack arg `base+cursor`.
- Post-ensure re-check is **`available >= 4`**, not `available >= cursor+4` (same pattern as ReadHeader16’s `available >= 16`).
- Binary-mode peer of text path `FUN_007689e0` (mode gate at `reader+0x4044` via dispatcher `FUN_00436900`).

## Window / reader field map

| Reader off | Window off | Role |
|---:|---:|---|
| `+0x14` | `+0x00` | stream window base |
| `+0x18` | `+0x04` | cursor |
| `+0x1c` | `+0x08` | base (ensure abs partner) |
| `+0x20` | `+0x0c` | available |
| `+0x2c` | `+0x18` | data buffer base |

## Annotated control flow

```
stoChunkReader_ReadU32(EAX=reader, stack out*):
  window = reader + 0x14
  cursor = window.cursor  // reader+0x18
  if (window.available < cursor + 4) {
    hr = FUN_00435df0(window /*ECX*/, window.base + cursor)
    if (hr < 0 || window.available < 4) return -1
  }
  *out = *(u32*)(window + 0x18 + window)
  window.cursor += 4
  return 0
```

## Pseudocode (raw, with sealed names)

```c
// EAX = stoChunkReader* reader; stack: uint32_t* out; RET 4
int32_t stoChunkReader_ReadU32(uint32_t* out /* stack */, /* EAX reader */)
{
  // decompiler form uses reader-relative offsets (≡ window at +0x14)
  if (*(int*)(reader + 0x20) < *(int*)(reader + 0x18) + 4) {
    int hr = FUN_00435df0(*(int*)(reader + 0x1c) + *(int*)(reader + 0x18));
    // actual call: ECX = reader+0x14
    if (hr < 0 || *(int*)(reader + 0x20) < 4)
      return 0xffffffff;
  }
  *out = *(uint32_t*)(*(int*)(reader + 0x18) + reader + 0x2c);
  *(int*)(reader + 0x18) += 4;
  return 0;
}
```

## Open questions

1. Product English plate name (ReadU32 vs ReadInt32 — bytes are width-4 dword copy; signedness is call-site).
2. Full ensure/refill contract of `FUN_00435df0` (not owned).
3. Runtime / bit-exact image diff.
