# Annotated low-level: Vehicle_FireAllPrimaryWeapons_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004f50d0` |
| VA | `0x004f50d0` |
| Canonical | `Vehicle_FireAllPrimaryWeapons_Inferred` (Ghidra `FUN_004f50d0`) |
| System | combat / vehicle weapons |
| Date | 2026-07-29 (refined; raw body 2026-07-23) |

## Machine-level notes

- **Convention:** vehicle in **ECX** (`mov ebp, ecx`).
- **Layout:** `vehicle+0x260` → pointer to hardpoint `weapon*` table; indices via byte offsets `0,4,8` (`i < 0xC`).
- **Primary group:** `weapon->vtbl[+0x38]()` thiscall; secondary twin uses **+0x3C** (`FUN_004f5110`).
- **Start fire:** `FUN_0056d520(weapon)` thiscall — sets firing flag `weapon+0xC7` among other HB side effects (see callee).
- **Return:** `AL = OR` of per-weapon start results (existential aggregate).
- **Not in body:** heat check, turret `+0x264`, skill cast, net send.
- **Sole static caller:** `Input_TryFirePrimaryWeapons_Inferred` @ `0x00922270` (call site `0x0092236c`) after `FUN_004f5290` heat probe.

## Pseudocode (annotated)

```c
// ECX = vehicle*
byte __fastcall Vehicle_FireAllPrimaryWeapons_Inferred(int vehicle /*param_1*/)
{
  int *pWeapon;          // piVar1
  char isPrimary;        // cVar2  — vtbl+0x38
  byte started;          // bVar3  — FUN_0056d520 result
  byte anyStarted;       // bVar4
  int byteOff;           // iVar5

  anyStarted = 0;
  byteOff = 0;
  do {
    // table base at vehicle+0x260; slot = table[byteOff/4]
    pWeapon = *(int **)(byteOff + *(int *)(vehicle + 0x260));
    if (pWeapon != (int *)0x0) {
      isPrimary = (**(code **)(*pWeapon + 0x38))();  // thiscall weapon
      if (isPrimary != '\0') {
        started = FUN_0056d520();  // decomp omits ECX; asm ECX=pWeapon
        anyStarted = anyStarted | started;
      }
    }
    byteOff = byteOff + 4;
  } while (byteOff < 0xc);
  return anyStarted;
}
```

## Open questions

- Product / PDB names for this unit and weapon vtbl+0x38.
- Full sealed dual for `FUN_0056d520`.
- Physics docs “stabilizer +0x260” vs combat weapon table wording.
