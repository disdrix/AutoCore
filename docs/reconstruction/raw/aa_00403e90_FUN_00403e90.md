# Raw capture: FUN_00403e90

| Field | Value |
|---|---|
| **Stable ID** | `aa_00403e90` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00403e90` |
| **Canonical name** | `FUN_00403e90` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00403e90(int param_1,int param_2)

{
  int *piVar1;
  int *piVar2;
  
  piVar1 = *(int **)(param_1 + 8);
  *(int *)(param_1 + 8) = *piVar1;
  if (*(char *)(*piVar1 + 0x31) == '\0') {
    *(int *)(*piVar1 + 4) = param_1;
  }
  piVar1[1] = *(int *)(param_1 + 4);
  if (param_1 == *(int *)(*(int *)(param_2 + 4) + 4)) {
    *(int **)(*(int *)(param_2 + 4) + 4) = piVar1;
    *piVar1 = param_1;
    *(int **)(param_1 + 4) = piVar1;
    return;
  }
  piVar2 = *(int **)(param_1 + 4);
  if (param_1 == *piVar2) {
    *piVar2 = (int)piVar1;
    *piVar1 = param_1;
    *(int **)(param_1 + 4) = piVar1;
    return;
  }
  piVar2[2] = (int)piVar1;
  *piVar1 = param_1;
  *(int **)(param_1 + 4) = piVar1;
  return;
}
```

---

## Re-verify append — WQ9L-B OWN-ONLY dual 2026-08-05

**Agent:** WQ9L-B  
**Tools:** `batch_decompile` + `analyze_function_complete` + `disassemble_function` + `read_memory` + callers/xrefs + `get_assembly_context`. **No** `disassemble_bytes`. No Launcher.  
**Integrity:** scaffold body above preserved; this section append-only.

### Live decompile ≡ scaffold CF

Live Ghidra decompile **≡** 2026-07-23 raw: classic left-rotate about node X with isnil guard at **+0x31**. Three exits (root / left-child / right-child parent cases).

### Body / ABI (bytes + disassembly)

| Fact | Value |
|---|---|
| Body | `0x00403e90`–`0x00403edb` inclusive (**76 B** / `0x4C`); pad `CC` to twin Rrotate `0x00403ee0` |
| Convention | **`__thiscall`**: **ECX = node X**; stack = **map/tree\***; void; **`RET 4`** ×3 (`C2 04 00`) |
| isnil | **`+0x31`** (`CMP byte ptr [EDX+0x31],0`) |
| Color | not written in this leaf (callers write **+0x30** around CALL) |
| Callees | **none** (leaf) |

Dataflow (sealed):

```text
Y = X.right              ; [ECX+8]
X.right = Y.left
if Y.left not isnil@+0x31: Y.left.parent = X
Y.parent = X.parent
if X is root (head->parent): root = Y
elif X is left of parent: parent.left = Y
else: parent.right = Y
Y.left = X; X.parent = Y
RET 4
```

Body hex (leading):  
`8B4108 8B10 895108 8B10 807A3100 7503 894A04 8B5104 895004 8B542404 … C20400` (×3 paths)

### Callers / xrefs

| Site | Parent | Role |
|---|---|---|
| `0x00409062`, `0x004090ab`, `0x00409107` | dualed `StdTree_EraseAndRebalance_Isnil31_Inferred` `FUN_00408ed0` | RB erase rebalance Lrot |
| `0x0040334a`, `0x00403396` | `FUN_00403250` | insert/rebalance peer (same isnil31 family) |

- **xref_count = 5** UNCONDITIONAL_CALL.
- Call-site pattern (erase): `PUSH` map (`EBP`); `MOV ECX, node`; `CALL 0x00403e90`; nearby color@**+0x30** / isnil@**+0x31**.
- Right-rotate twin: `FUN_00403ee0` (WQ9L-C OWN residual — not dualled here).
- Peer families: isnil29 `StdTree_Lrotate_Isnil29` `0x004192a0`; isnil2D `StdTree_LeftRotate_Isnil2D_Inferred` `0x0044e010` — **different** isnil offsets; do not merge.

### Name decision

Promote **`StdTree_Lrotate_Isnil31_Inferred`**. Product host map English open → `_Inferred`. Reject skill/mission plate misnames; reject right-rotate mislabel; reject isnil29/2D merge.

### Gaps

- Product / English map type for isnil31 family (0x38 nodes).
- Full dual of insert parent `00403250` and Rrotate twin `00403ee0` (role sealed).
- Runtime / bit-exact / differential.
