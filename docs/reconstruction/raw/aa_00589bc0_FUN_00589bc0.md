# Raw capture: FUN_00589bc0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00589bc0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00589bc0` |
| **Canonical name** | `FUN_00589bc0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int FUN_00589bc0(undefined4 param_1,undefined4 param_2)



{

  int nHeatDelta;

  VehicleCombatPools_Inferred *pVehicle;

  int iVar1;

  

  pVehicle = (VehicleCombatPools_Inferred *)

             __RTDynamicCast(param_2,0,&CVOGClonedObjectBase::RTTI_Type_Descriptor,

                             &CVOGVehicle::RTTI_Type_Descriptor,0);

  if ((pVehicle == (VehicleCombatPools_Inferred *)0x0) &&

     (iVar1 = __RTDynamicCast(param_2,0,&CVOGClonedObjectBase::RTTI_Type_Descriptor,

                              &CVOGCreature::RTTI_Type_Descriptor,0), iVar1 != 0)) {

    pVehicle = *(VehicleCombatPools_Inferred **)(iVar1 + 0x250);

  }

  iVar1 = 0;

  if (pVehicle != (VehicleCombatPools_Inferred *)0x0) {

    nHeatDelta = *(int *)(*(int *)(pVehicle->pOpaqueHeader_Inferred + 4) + 4);

    iVar1 = pVehicle->nCurrentHeat;

    if ((*(int *)(pVehicle->pOpaqueHeader_Inferred + nHeatDelta + 0xa8) != 0) &&

       (*(char *)(*(int *)(pVehicle->pOpaqueHeader_Inferred + nHeatDelta + 0xa8) + 0x7e) != '\0')) {

      Vehicle_AddHeat(pVehicle,nHeatDelta);

    }

    iVar1 = pVehicle->nCurrentHeat - iVar1;

  }

  return iVar1;

}
```


---

## Live re-verify — 2026-08-05 MEGA-005 OWN-ONLY dual

**Tooling:** Ghidra HTTP `127.0.0.1:8089` — `decompile_function` + `force_decompile` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` + `get_xrefs_to` + `get_function_by_address` + parent/peer decompile samples (`0061b6f0`, `00618270`) + `Vehicle_AddHeat` (`004f7210`) disasm head. **No** `disassemble_bytes`. No Launcher.

**Integrity:** Original 2026-07-23 raw body above is **preserved**. This section appends live re-verify only. Decompiler CF is **not** authoritative for math/ABI — assembly + `read_memory` seal.

### Function plate

| Field | Live value |
|---|---|
| Entry | `0x00589bc0` |
| Body | `0x00589bc0`–`0x00589c69` inclusive (**170 B** / `0xAA`); next sibling `FUN_00589c70` |
| Ghidra name | `FUN_00589bc0` |
| Program | `autoassault.exe` base `0x400000` |
| Classification | worker |
| Xrefs | 5 `UNCONDITIONAL_CALL` |
| Bound callers | `FUN_0061b6f0`, `FUN_00618270` |
| Callees | `__RTDynamicCast` @ `0x004898a4`, `Vehicle_AddHeat` @ `0x004f7210` |

### Live decompile (still wrong — keep for delta)

`force_decompile` + `decompile_function` re-emit the same 2-arg body as the 2026-07-23 scaffold: casts `param_2`, invents `nHeatDelta` from opaque-header adjustor dword, gates on `+0xa8`/`+0x7e`, calls `Vehicle_AddHeat(pVehicle, nHeatDelta)`, returns heat delta. **Missing:** third float arg, percent-of-max path (`COMISS`/`MULSS`/`CVTTSS2SI` on `[ESI+0x244]`), and correct delta source (`ECX` from float trunc, not header adjustor).

### Full disassembly (mnemonic seal — authoritative)

```text
00589bc0  PUSH ESI
00589bc1  PUSH EDI
00589bc2  MOV EDI,[ESP+0x10]              ; param_2 target (after 2 pushes)
00589bc6  PUSH 0
00589bc8  PUSH 0x00aff68c                ; CVOGVehicle::RTTI_Type_Descriptor
00589bcd  PUSH 0x00af8e10                ; CVOGClonedObjectBase::RTTI_Type_Descriptor
00589bd2  PUSH 0
00589bd4  PUSH EDI
00589bd5  CALL __RTDynamicCast            ; 0x004898a4
00589bda  MOV ESI,EAX                     ; vehicle* or null
00589bdc  ADD ESP,0x14
00589bdf  TEST ESI,ESI / JNZ 00589c02
00589be3  PUSH EAX                        ; 0
00589be4  PUSH 0x00af8d0c                ; CVOGCreature::RTTI_Type_Descriptor
00589be9  PUSH 0x00af8e10
00589bee  PUSH EAX
00589bef  PUSH EDI
00589bf0  CALL __RTDynamicCast
00589bf5  ADD ESP,0x14
00589bf8  TEST EAX / JZ 00589c02
00589bfc  MOV ESI,[EAX+0x250]             ; creature → embedded vehicle*
00589c02  XOR EAX,EAX                     ; default return 0
00589c04  TEST ESI / JZ 00589c67          ; no vehicle → ret 0
00589c08  MOVSS XMM0,[ESP+0x14]           ; param_3 float amount
00589c0e  COMISS XMM0,[g_flOne@00a0f2a0]  ; amount > 1.0?
00589c15  JA 00589c36                     ; absolute
00589c17  MOVSS XMM1,[DAT_00aaa668]       ; -1.0f
00589c1f  COMISS XMM1,XMM0                ; -1.0 > amount?
00589c22  JA 00589c36                     ; absolute
00589c24  CVTSI2SS XMM1,[ESI+0x244]       ; max heat
00589c2c  MULSS XMM1,XMM0
00589c30  CVTTSS2SI ECX,XMM1              ; delta = trunc(max*amount)
00589c34  JMP 00589c3a
00589c36  CVTTSS2SI ECX,XMM0              ; delta = trunc(amount)
00589c3a  MOV EAX,[ESI+0x4]
00589c3d  MOV EDX,[EAX+0x4]
00589c40  MOV EAX,[EDX+ESI+0xa8]          ; net/ghost object*
00589c47  TEST EAX,EAX
00589c49  MOV EDI,[ESI+0x150]             ; save nCurrentHeat
00589c4f  JZ 00589c5f                     ; skip AddHeat if null
00589c51  CMP byte ptr [EAX+0x7e],0
00589c55  JZ 00589c5f                     ; skip if gate byte clear
00589c57  PUSH ECX                        ; nHeatDelta
00589c58  MOV ECX,ESI                     ; this = vehicle
00589c5a  CALL Vehicle_AddHeat            ; 0x004f7210 thiscall
00589c5f  MOV EAX,[ESI+0x150]
00589c65  SUB EAX,EDI                     ; return actual heat change
00589c67  POP EDI / POP ESI
00589c69  RET                             ; bare C3 — cdecl
```

### Body bytes (`read_memory` 170 B @ `0x00589bc0`)

```text
56 57 8B 7C 24 10 6A 00 68 8C F6 AF 00 68 10 8E AF 00 6A 00 57 E8 CA FC EF FF
8B F0 83 C4 14 85 F6 75 1F 50 68 0C 8D AF 00 68 10 8E AF 00 50 57 E8 AF FC EF FF
83 C4 14 85 C0 74 06 8B B0 50 02 00 00 33 C0 85 F6 74 5F F3 0F 10 44 24 14
0F 2F 05 A0 F2 A0 00 77 1F F3 0F 10 0D 68 A6 AA 00 0F 2F C8 77 12
F3 0F 2A 8E 44 02 00 00 F3 0F 59 C8 F3 0F 2C C9 EB 04 F3 0F 2C C8
8B 46 04 8B 50 04 8B 84 32 A8 00 00 00 85 C0 8B BE 50 01 00 00 74 0E
80 78 7E 00 74 08 51 8B CE E8 B1 D5 F6 FF 8B 86 50 01 00 00 2B C7 5F 5E C3
```

### Constants (`read_memory`)

| Symbol | VA | Bytes | Value |
|---|---|---|---|
| `g_flOne` | `0x00a0f2a0` | `00 00 80 3F` | `1.0f` |
| `DAT_00aaa668` | `0x00aaa668` | `00 00 80 BF` | `-1.0f` |

### ABI (sealed via body + call-site cleanup)

| Item | Seal |
|---|---|
| Convention | **`__cdecl`** (bare `RET` / `C3`; no ECX this on entry) |
| arg0 `param_1` | stack `[ESP+4]` — **source** (passed by parents; **unused in body**) |
| arg1 `param_2` | stack `[ESP+8]` → EDI — **target** for RTTI |
| arg2 `param_3` | stack float `[ESP+0xC]` — **amount** |
| return | EAX = `nCurrentHeat' - nCurrentHeat` or 0 |
| cleanup | callers **`ADD ESP,0xC`** (3×4 B) |

### Call-site samples (parent/peer)

| Site | Parent | Pattern |
|---|---|---|
| `0x0061b7d2` | `FUN_0061b6f0` skill HB tick | gate `skill[+0x114] != 0`; `amount = skill[+0x6c4]*skill[+0x114]`; push amount / target`[+0x18]` / `FUN_00578270` source; **`ADD ESP,0xC`** |
| `0x0061858a` | `FUN_00618270` peer | same heat float channel shape; **`ADD ESP,0xC`** |
| `0x0061beaf` | (switch/orphan plate) | 3-arg cdecl call pattern |
| `0x0061e8de` | (switch case) | 3-arg cdecl |
| `0x0061ea44` | (switch case) | 3-arg cdecl |

### Semantics (assembly-authoritative)

1. RTTI-cast `param_2` → `CVOGVehicle`; else `CVOGCreature` then load vehicle* at **`+0x250`**.
2. If no vehicle → return 0. **`param_1` is never read.**
3. `amount ∈ [-1.0, 1.0]` → `delta = trunc(nMaxHeat * amount)` with **`nMaxHeat` @ vehicle+0x244**; else `delta = trunc(amount)` (`CVTTSS2SI`).
4. Snapshot `cur = nCurrentHeat` @ **`+0x150`**.
5. If net object `*( *(vehicle+4)+4 + vehicle + 0xA8 )` non-null **and** `*(char*)(net+0x7E) != 0`: `Vehicle_AddHeat(this=vehicle, delta)` (thiscall; positive heats / negative cools).
6. Return `nCurrentHeat - cur` (0 if gate skipped or unchanged after clamp inside AddHeat).

### Sign convention

| `amount` | effect |
|---|---|
| positive | heat up (delta > 0 into `Vehicle_AddHeat`) |
| negative | cool (delta < 0) |
| in `[-1,1]` | fraction of **max** heat |
| outside | absolute trunc |

### Decompile vs assembly delta (must preserve in clean)

| Topic | Decompiler | Assembly seal |
|---|---|---|
| Arity | 2 args | **3** (source, target, float) |
| Heat delta source | opaque-header adjustor dword | **float trunc / max\*frac** in ECX |
| Percent path | **absent** | `COMISS` band + `[ESI+0x244]` |
| `Vehicle_AddHeat` 2nd arg | adjustor (wrong) | **ECX delta** (correct) |
| Field current heat | `nCurrentHeat` (named) | **`+0x150`** |
| Field max heat | not used for scale | **`+0x244`** |

### Xrefs / callers (live)

```text
get_function_callers: FUN_00618270 @ 00618270; FUN_0061b6f0 @ 0061b6f0
get_xrefs_to:
  From 0061b7d2 in FUN_0061b6f0 [UNCONDITIONAL_CALL]
  From 0061858a in FUN_00618270 [UNCONDITIONAL_CALL]
  From 0061beaf [UNCONDITIONAL_CALL]
  From 0061e8de [UNCONDITIONAL_CALL]
  From 0061ea44 [UNCONDITIONAL_CALL]
```
