# Annotated low-level: UI_CraftHost_RefreshCraftEnableAndStatus_Inferred (`FUN_008e55e0`)

| Field | Value |
|---|---|
| Stable ID | `aa_008e55e0` |
| VA | `0x008e55e0`–`0x008e598d` (**942 B** / `0x3AE`) |
| System | `inventory-transfer` |
| Date | `2026-08-05` (R13-015 dual seal) |
| Canonical name | `UI_CraftHost_RefreshCraftEnableAndStatus_Inferred` |

---

## Machine-level notes

- **ABI:** host in **EAX** → `MOV EBX,EAX`; no stack args; plain **`RET`** (`C3`).
- Prefer assembly when decompiler shows `in_EAX` phantom or float compares on status codes.
- Status out-param from `FUN_0052d450` is **integer** (`CMP ECX,2/4/5`), not denormal float.
- Success-path chance display loads **100.0** double from `DAT_00aaa708`, not the out-param float.
- Cost: `Item_GetStoreSellPrice` × **0.85f** (`DAT_00aaa680`) → CRT **ceil** → int; compare to i64 credits `(char+0x720)−(char+0x728)`.
- Tier loop calls dualed `Character_CalcCommodityTierMaxCapacity` for 1..5 with returns **discarded**.
- Retired scaffold: `Named_CalleeOf_Client_RefreshOpenMissionUiWindows_008e55e0`.

---

## Annotated control flow

```text
if DAT_00d1b6d8 == 0: return

zero msg[0x200+]
host+0x7d8 = 1
FUN_008e54a0()                         // residual; ESI often = host

for tier = 1..5:
  Character_CalcCommodityTierMaxCapacity(DAT_00d1b6d8, tier)  // discard

station_ok = 0
if host+0x7cc:
  station_ok = Character_Craft_HasMatchingStationInRange(char, host+0x7cc)

afford_ok = 1
if host+0x7cc && FUN_005202d0(char, tfid_lo, tfid_hi):
  price = Item_GetStoreSellPrice(host+0x7cc)
  cost  = (int)ceil(price * 0.85f)
  if i64_credits(char) < cost: afford_ok = 0

chance_slot = 0
ready = FUN_0052d450(char, host+0x7cc, &status_or_chance)

if ready && station_ok && host+0x7d8 && afford_ok:
  button+0xd4(1); status+0x268()
  localize("Chance of Success") with 100.0 double
  sprintf(msg, "\n%s: %.1f%%", ...)
  color = white
else:
  if button enabled: button+0xd4(0); button+0x34c()
  status+0x268()
  if !object:
    if !list || list+0x490() > 0: "You must select an item to craft!"
    else: "You need a broken item to craft!"
  else if status == 4: FUN_008e3820()
  else if status == 2: "You don't have the right components!"
  else if !afford_ok:
    append "To craft this from Memory costs" + currency paint (0040f4e0 / 00977a30)
  else if !station_ok || status == 5: FUN_008e3940()
  else: "Chance of Success" %i%%
  color = black/zero rgb

status+0x15c(1, &color)
FUN_008e41b0()            // host in EAX — Complexity labels
button+0x34c()
if msg[0]: status+0x1d8(msg,1,1)
status+0xcc(1); status+0x34c()
```

---

## Call sites

| Site | Pattern |
|---|---|
| `0x008e5caa` | `MOV EAX,ESI; CALL` (bind resolved) |
| `0x0093a9c1` | `MOV EAX,EDI; CALL` (mission UI refresh) |
| `0x008e63e3` | `MOV EAX,EDI; CALL` (craft tick) |

---

## Open questions

- Product demangle for host / widget classes.
- Dual seals for residual helpers (`008e54a0`, `0052d450`, `005202d0`, `008e41b0`, `008e3820`, `008e3940`).
- Why tier-max returns are discarded.
- Whether success-path 100.0% is intentional UX.
