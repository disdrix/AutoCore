# Annotated low-level: Fs_DirRecurseCollectLeafPaths_Inferred (FUN_009839b0)

| Field | Value |
|---|---|
| Stable ID | `aa_009839b0` |
| VA | `0x009839b0`–`0x00983b53` (**420 B**) |
| System | filesystem / path enumeration |
| Date | 2026-07-29 (W32-D dual seal) |
| Ghidra name | `FUN_009839b0` |
| Inferred name | `Fs_DirRecurseCollectLeafPaths_Inferred` |

## Machine-level notes

- Source: raw capture + W32-D live `decompile_function` / `read_memory`.
- Prefer bytes when decompiler drops thiscall ECX for `FUN_004301f0`.
- ABI: **cdecl** `(const char* path, StringVector* out)`; bare `RET`; returns `0` / `-1`.
- `DAT_00a2eb5c` = `"/"` (read_memory `2f 00 00 00`).
- `FUN_0076adc0` = is-directory (`stat` + mode bit) — context only, not owned.
- `FUN_004301f0` = `std::vector<std::string>`-shaped push (stride `0x1c`) thiscall — context only.
- Self-recursive on subdirectory children; leaf paths collected into **`param_2`**.

## Pseudocode (annotated, bytes-corrected)

```c
// cdecl; returns 0 on dir walk success, -1 if path is not a directory
int Fs_DirRecurseCollectLeafPaths_Inferred(const char* path, void* out_string_vec /*stack*/)
{
  // SEH LAB_009ad54e
  if (!FUN_0076adc0(path)) {
    return -1;  // not a directory
  }

  StringVec listing = {};
  FUN_0076b3f0(path, &listing);  // fill listing with entry names

  for (each entry in listing; stride 0x1c) {
    std::string full = path + "/" + entry;  // FUN_00416490 + FUN_00416410
    if (!FUN_0076adc0(full.c_str_or_sso())) {
      FUN_004301f0(out_string_vec, full);   // thiscall: ECX=out, push leaf
    } else {
      Fs_DirRecurseCollectLeafPaths_Inferred(full.c_str_or_sso(), out_string_vec);
    }
  }

  FUN_00431ae0(&listing);  // destroy listing
  return 0;
}
```

## Callers

| Site | Role |
|---|---|
| `FUN_00984340` @ `0x009844a1` | when path is dir and recurse flag (`param_3`) set |
| `FUN_00984c70` @ `0x00984ca6` | collect then process each path via `FUN_00984910` |
| self @ `0x00983ada` | subdirectory recursion |

## Open questions

- Product English for the higher-level enumerator (`FUN_00984340` / `FUN_00984c70`).
- Whether `param_2` vector is always empty on entry (callers zero begin/end).
- Runtime / bit-exact / differential.
