# Annotated low-level: Math_AcosClamped (FUN_0040d0a0)

| Field | Value |
|---|---|
| Stable ID | `aa_0040d0a0` |
| VA | `0x0040d0a0` |
| Canonical name | `Math_AcosClamped` (inferred) |
| Ghidra symbol | `FUN_0040d0a0` |
| System | `physics` / shared math |
| Date | 2026-07-29 (dual A/B seal; supersedes 2026-07-23 scaffold notes) |

## Machine-level notes

- **ABI:** `__cdecl`, float `x` at `[esp+4]`, return radians in **ST0**.
- **Gate:** `FABS(x)` vs `g_flOne` (`0x00a0f2a0` = 1.0). `|x| < 1` → `_CIacos`; else 0 or π.
- **π:** `DAT_00a27c3c` (`db 0f 49 40`) used when saturated and `x ≤ 0`.
- **Callee:** `_CIacos` @ `0x006a3e26` (rel32 from `0x0040d0d9`).
- Body `0x0040d0a0`–`0x0040d0de` (0x3F bytes); `CC` pad after.

## Pseudocode (annotated; ≡ raw ≡ live)

```c
// Domain-safe acos (radians). Early-out avoids CRT domain error for |x| > 1.
float10 Math_AcosClamped(float x)
{
  float10 fVar1;
  float fVar2;

  if (g_flOne <= ABS(x)) {
    /* |x| >= 1 */
    fVar2 = 0.0;                 /* acos(+1) endpoint; also +exterior */
    if (x <= 0.0) {
      fVar2 = DAT_00a27c3c;      /* π — acos(-1) endpoint; also -exterior */
    }
    return (float10)fVar2;
  }
  /* |x| < 1 — CRT acos */
  fVar1 = (float10)_CIacos();    /* arg already ST0 from FLD x */
  return fVar1;
}
```

## Instruction map (seal)

| VA | Op | Meaning |
|---|---|---|
| `40d0a0` | `FLD [esp+4]` | load x |
| `40d0a4` | `FABS` | \|x\| |
| `40d0a6` | `FLD [g_flOne]` | 1.0 |
| `40d0ac` | `FXCH` | ST0=\|x\| |
| `40d0ae` | `FCOMIP` | \|x\| ? 1 |
| `40d0b2` | `JC 40d0d5` | \|x\| < 1 → acos |
| `40d0b4`… | SSE saturates | 0 if x>0 else π |
| `40d0d5` | `FLD` + `CALL _CIacos` | interior |
| `40d0de` | `RET` | |

## Open questions

- Product / PDB name (leave `FUN_0040d0a0` in Ghidra until promote).
- NaN / −0 paths not runtime-probed.
- Caller-side unit-dot construction (OWN VA ends here).
