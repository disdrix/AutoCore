# Raw capture: FUN_00578270

| Field | Value |
|---|---|
| **Stable ID** | `aa_00578270` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00578270` |
| **Canonical name** | `FUN_00578270` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00578270(int param_1)

{
  CVOGReaction_ResolveObjectTarget
            (*(undefined1 *)(param_1 + 0x660),*(undefined4 *)(param_1 + 0x658),
             *(undefined4 *)(param_1 + 0x65c));
  return;
}
```

---

## Re-verify append — 2026-07-29 W28-F

| Field | Value |
|---|---|
| **Tool** | Ghidra `decompile_function` + `read_memory` + `get_function_by_address` + callers. **No** `disassemble_bytes`. |
| **Live decompile ≡ raw body** | **Partial** — CF of the single call is same, but surface is **void** and **omits reaction-this ECX** |
| **Body** | `0x00578270`–`0x00578298` (**40 B / `0x28`**; pad `CC` after plain `RET`) |
| **Classification** | **thin wrapper** (1 callee) |
| **Callers (19 code)** | includes `CVOGHBMissionPatrol_Tick` (`00650f90`), `FUN_00626890` (`Object_ActivatePlayerEnqueueHB_Inferred`)×2, many HB/mission/UI units (`00618270`…`0083b920`) |
| **Callee** | `CVOGReaction_ResolveObjectTarget` @ `0x004bae70` (thiscall) |
| **ABI** | **thiscall/fastcall** ECX = **host object**; **returns** `void*`/`object*` in **EAX** (passthrough from callee); plain **`ret`** (`C3`) |
| **Entry bytes** | `8B C1 8B 90 5C 06 00 00` (`mov eax,ecx`; load `[this+0x65c]`) |
| **Exit bytes** | `E8 … C3` then `CC` pad |
| **Field map (host)** | `+0x658` dword id/lo; `+0x65c` dword id/hi; `+0x660` byte flag; `+0x688` → subsystem; `*(+0x688)+0xe4e8` → **reaction this** |
| **Role name** | `Object_ResolveTargetViaReaction_Inferred` (structural; product/PDB open) |

### Control-flow (sealed)

1. `EAX = ECX` (host).
2. Push `[host+0x65c]`, `[host+0x658]`, low byte of `[host+0x660]` (as stack formals).
3. `ECX = *(*(host+0x688) + 0xe4e8)` — reaction object **this**.
4. `CALL CVOGReaction_ResolveObjectTarget` (`0x004bae70`).
5. Plain `RET` — **EAX return value preserved** (not void).

### Callee contract (context, not OWN)

`CVOGReaction_ResolveObjectTarget(this, flag, id0, id1)`:
- Early `0` when `(id0 & id1) == 0xFFFFFFFF`.
- `flag==0` gates on `*(reaction+0xd)`; else `*(reaction+0xc)`.
- On pass: `FUN_004e3260(id0,id1)` lookup; else `0`.

### Notes

- Scaffold decompile **drops** reaction-this load and marks **void** — both contradicted by bytes (`mov ecx,[ecx+0xe4e8]` path + callers assigning `iVar = FUN_00578270()`).
- W27-F consumers treat return as manager-ish then `vfunc+0x210(0)` for player — product English of return type OPEN.
- Nested `CVOGReaction_ResolveObjectTarget` / `FUN_004e3260` dual OPEN (not OWN).
