# Annotated low-level: Class_00aa05cc_Ctor_Inferred (`FUN_0074d750`)

| Field | Value |
|---|---|
| Stable ID | `aa_0074d750` |
| VA | `0x0074d750`–`0x0074d7ad` (**94 B** / `0x5E`) |
| System | class host / dual NestedHash bag + AnimSlot bind path |
| Date | 2026-07-23 scaffold; W35-C seal 2026-07-29 |

---

## Machine-level notes

- **Role:** ECX thiscall placement ctor for **0x5C** host with vtbl `PTR_FUN_00aa05cc`, zeroed head dwords, and two nested NestedHash-family bags.
- **ABI:** **ECX=this**; no stack formals; bare **`C3`**; returns **this in EAX**. SEH `LAB_009acbbf` + `ADD ESP,0x10`.
- **Layout (sealed):**
  | Offset | Init | Notes |
  |---|---|---|
  | `+0x00` | `PTR_FUN_00aa05cc` | vtbl |
  | `+0x04` | `0` | dword store |
  | `+0x08` | `0` | `XORPS`/`MOVSS` (4 B) |
  | `+0x0c` | NestedHashBag | `FUN_0044a0d0` / W34-D (span 0x28; sentinel 0x28) |
  | `+0x34` | NestedHash 0x14 | `FUN_0043bdf0` / W34-E (span 0x28; sentinel 0x14) |
  | end | `0x5C` | matches callers' `operator_new(0x5c)` |
- **Callees (sealed duals):** `NestedHashBag_Ctor_Inferred` (`0x0044a0d0`), `NestedHash_Ctor_Sentinel0x14_Inferred` (`0x0043bdf0`).
- **Callers:** multi-site geo/anm bind hosts (sky_box path, object anim attach). After ctor: `FUN_0074d420(geo)`, vtbl+4 / +0xc / +0x18 with AnimSlot (`0x0074ed90`) and tree host (`new(0x14)` Class_009c7948 family).
- Product/PDB English for class plate open — structural vtbl name only.

## Control flow (annotated)

```
; SEH prolog LAB_009acbbf
xorps xmm0, xmm0
mov   esi, ecx              ; this
lea   eax, [esi+0x0c]
push  eax                   ; NestedHashBag* self
mov   [esp+8], esi
mov   dword [esi], PTR_FUN_00aa05cc
mov   dword [esi+4], 0
movss [esi+8], xmm0         ; zero +8
call  NestedHashBag_Ctor   ; 0044a0d0  RET 4
lea   eax, [esi+0x34]
push  eax
mov   [esp+0x14], 0         ; SEH state = 0 (after first nested)
call  NestedHash_Ctor_0x14  ; 0043bdf0  RET 4
mov   eax, esi              ; return this
; SEH epilog; add esp,0x10; ret
```

## Open questions

- Product/PDB demangle for `PTR_FUN_00aa05cc` class.
- Semantics of head `+0x04/+0x08` beyond zero init.
- Full contracts of vtbl methods / `FUN_0074d420` (unowned).
- Runtime / bit-exact / differential.
