# Annotated low-level: Object_ResolveTargetViaReaction_Inferred (`FUN_00578270`)

| Field | Value |
|---|---|
| Stable ID | `aa_00578270` |
| VA | `0x00578270`–`0x00578298` (40 B) |
| System | object / reaction / targeting |
| Date | 2026-07-29 (W28-F dual seal) |
| Ghidra | `FUN_00578270` |

## Machine-level notes

- **ABI:** ECX = host object; **returns pointer in EAX**; plain `ret` (`C3`).
- **Not void** — callers assign return (`iVar2 = FUN_00578270()`).
- **Reaction this** loaded from `*(*(host+0x688)+0xe4e8)` into ECX before call.
- Stack formals to callee: flag byte `host+0x660`, id0 `host+0x658`, id1 `host+0x65c`.
- Scaffold decompile omits reaction-this and return; bytes seal both.

## Pseudocode (annotated)

```c
// object* __fastcall Object_ResolveTargetViaReaction(Host *host /*ECX*/)
void *Object_ResolveTargetViaReaction_Inferred(int host)
{
  void *reaction = *(void **)(*(int *)(host + 0x688) + 0xe4e8);
  uint8_t flag = *(uint8_t *)(host + 0x660);
  uint32_t id0 = *(uint32_t *)(host + 0x658);
  uint32_t id1 = *(uint32_t *)(host + 0x65c);

  // thiscall: ECX=reaction; stack flag, id0, id1
  return CVOGReaction_ResolveObjectTarget(reaction, flag, id0, id1);
}
```

## Open questions

- Product type of host (`+0x688` subsystem) and of return (manager vs object).
- Semantic of id pair at `+0x658/+0x65c` (TFID dual-dword vs other).
- Flag at `+0x660` product meaning (callee uses to pick `reaction+0xc` vs `+0xd` gate).
- Dual of `CVOGReaction_ResolveObjectTarget` / `FUN_004e3260` (not OWN).
