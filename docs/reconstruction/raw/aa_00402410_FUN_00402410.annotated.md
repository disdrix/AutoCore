# Annotated low-level: StdList_CopyCtor_Inferred (FUN_00402410)

| Field | Value |
|---|---|
| Stable ID | `aa_00402410` |
| VA | `0x00402410`–`0x00402495` inclusive (**134 B** / `0x86`) |
| Ghidra name | `FUN_00402410` |
| Canonical name | `StdList_CopyCtor_Inferred` |
| System | shared MSVC `std::list` (partition seed: missions-progression / parent `0x004023f0`) |
| Date | 2026-08-05 (R10-019 OWN dual re-verify) |
| Prior scaffold | 2026-07-23 unknown-system annotated |

## Machine-level notes

- Source: raw capture `aa_00402410` + live Ghidra re-verify R10-019.
- Prefer assembly when decompiler conflicts — **ABI sealed by `RET 0x8`**, not decompiler void/`__cdecl` display.
- Sibling of dualed empty default ctor `StdList_DefaultCtor_Inferred` (`0x004023f0`): same head@+4 / size@+8 empty init, then range-insert copy.
- Element type `T` is site-specific (caller `FUN_00955010` walks nodes with payload ptr + vtable); not recovered in this unit.

## Semantics (sealed)

```text
StdList_CopyCtor_Inferred(dest, src) -> dest
  SEH frame (LAB_009bd220); state -1
  dest._Myhead = StdList_AllocEmptySentinel_Inferred()   // 0x0040fb90
  dest._Mysize = 0
  SEH state = 0   // empty dest constructed; dtor path can free head
  FUN_00404490(dest, *dest._Myhead, *src._Myhead, src._Myhead)
    // ⇒ insert [src.begin(), src.end()) before dest.end()
  return dest
```

## Pseudocode (annotated)

```c
// stdcall: dest @ [EBP+8], src @ [EBP+0xC]; EAX = dest; RET 8
void* /*list**/ __stdcall StdList_CopyCtor_Inferred(void* dest, void* src)
{
  // SEH: local_8 = -1; handler = LAB_009bd220
  void* head = StdList_AllocEmptySentinel_Inferred(); // FUN_0040fb90
  *(void**)((char*)dest + 4) = head;                  // _Myhead
  *(uint32_t*)((char*)dest + 8) = 0;                  // _Mysize
  // SEH state = 0

  void* src_head = *(void**)((char*)src + 4);
  void* src_begin = *(void**)src_head;                // head->_Next
  void* dest_head = *(void**)((char*)dest + 4);
  void* where = *(void**)dest_head;                   // empty ⇒ sentinel

  // FUN_00404490 → FUN_00404560: for (n = begin; n != end; n = n->_Next)
  //   FUN_004040f0(where, &n->value);  // insert dword (or T) before where
  FUN_00404490(dest, where, src_begin, src_head);

  return dest;
}
```

## ABI (sealed)

| Slot | Role | Conf |
|---|---|---|
| stack `[EBP+8]` | dest list* | **Confirmed** (`MOV ESI,[EBP+8]`) |
| stack `[EBP+0xC]` | src list* | **Confirmed** (`MOV EAX,[EBP+0xC]`) |
| ECX on entry | unused | **Confirmed** (not thiscall; differs from default-ctor sibling) |
| EAX out | dest* | **Confirmed** (`MOV EAX,ESI`) |
| cleanup | `RET 0x8` | **Confirmed** (`C2 08 00`) |

## Callers / callees

| Direction | VA / name | Notes |
|---|---|---|
| Caller (×8) | `FUN_00955010` | sole parent; all UNCONDITIONAL_CALL |
| Callee | `0x0040fb90` `StdList_AllocEmptySentinel_Inferred` | dualed |
| Callee | `0x00404490` | residual range-insert wrapper |
| Sibling | `0x004023f0` `StdList_DefaultCtor_Inferred` | empty default only |
| Pair destroy | `0x00403430` `StdList_Destroy_FreeHead_Inferred` | dualed |

## Open questions / gaps

- Element type `T` of copied lists (caller-site payloads look like object* with vtable).
- Unwritten `list+0` (allocator / `_Container_base`).
- Full dual of insert chain `00404490` / `00404560` / `004040f0` (residual).
- Runtime / bit-exact / differential (no Launcher; terminal false).
- Why stdcall 2-arg rather than thiscall copy-ctor shape — observed bytes; treat as sealed ABI for this unit regardless of MSVC decoration.
