# Raw capture: FUN_00402ae0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00402ae0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00402ae0` |
| **Canonical name** | `FUN_00402ae0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int * FUN_00402ae0(int *param_1)

{
  int in_EAX;
  int iVar1;
  uint *unaff_EDI;
  
  iVar1 = FUN_00403e50();
  if (iVar1 != *(int *)(in_EAX + 4)) {
    if ((*(int *)(iVar1 + 0x14) <= (int)unaff_EDI[1]) &&
       ((*(int *)(iVar1 + 0x14) < (int)unaff_EDI[1] || (*(uint *)(iVar1 + 0x10) <= *unaff_EDI)))) {
      *param_1 = iVar1;
      return param_1;
    }
  }
  *param_1 = *(int *)(in_EAX + 4);
  return param_1;
}
```

---

## MEGA-083 re-verify (2026-08-05)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-083** |
| **Tools** | Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` + `get_function_xrefs` + `get_assembly_context` |
| **No** | `disassemble_bytes`; Launcher; parent ledgers; other VAs dualled |
| **Live decompile** | ≡ scaffold body (2026-07-23) for CF (register formals still phantom in decompiler) |
| **Named** | `StdMap_Find_Tfid_Isnil29_EaxMap_Inferred` |

### Body / ABI (bytes authority)

- **Range:** `0x00402ae0`–`0x00402b2f` inclusive (**80 B** / `0x50`); next function at `0x00402b30` (`PUSH ECX`…).
- **ABI (custom / non-thiscall):**
  - **EAX** = map host (`head` @ `+0x04`)
  - **EDI** = `const KeyU32I32*` (TFID-shaped: lo @ `+0`, hi signed @ `+4`)
  - stack: `Node** outIt` (single arg) — loaded to EBX via `[ESP+0xC]` after two pushes
  - epilogue **`RET 4`** (`C2 04 00`) both exits
  - effect: `*outIt = node or head`; **EAX** returned as `outIt` after store
- **Full body hex (80 B):**
```
51 53 8B 5C 24 0C 56 8B D7 8B F0 E8 60 13 00 00
8B 76 04 3B C6 89 44 24 10 74 21 8B 4F 04 3B 48
14 7C 19 7F 07 8B 17 3B 50 10 72 10 8D 44 24 10
8B 00 89 03 5E 8B C3 5B 59 C2 04 00 89 74 24 08
8D 44 24 08 8B 00 89 03 5E 8B C3 5B 59 C2 04 00
```

### Disassembly (function listing; not disassemble_bytes)

```
00402ae0  PUSH ECX
00402ae1  PUSH EBX
00402ae2  MOV  EBX, [ESP+0xC]      ; outIt
00402ae6  PUSH ESI
00402ae7  MOV  EDX, EDI            ; key → EDX for lower_bound
00402ae9  MOV  ESI, EAX            ; map
00402aeb  CALL FUN_00403e50        ; lower_bound (isnil@+0x29, key lo/hi)
00402af0  MOV  ESI, [ESI+0x4]      ; head / end
00402af3  CMP  EAX, ESI
00402af5  MOV  [ESP+0x10], EAX     ; stash lb
00402af9  JZ   miss
00402afb  MOV  ECX, [EDI+0x4]      ; key.hi
00402afe  CMP  ECX, [EAX+0x14]     ; node.hi (signed)
00402b01  JL   miss
00402b03  JG   hit
00402b05  MOV  EDX, [EDI]          ; key.lo
00402b07  CMP  EDX, [EAX+0x10]     ; node.lo (unsigned)
00402b0a  JC   miss                ; key.lo < node.lo → miss
hit:
00402b0c  LEA  EAX, [ESP+0x10]
00402b10  MOV  EAX, [EAX]
00402b12  MOV  [EBX], EAX          ; *outIt = lb
00402b14  POP  ESI
00402b15  MOV  EAX, EBX
00402b17  POP  EBX
00402b18  POP  ECX
00402b19  RET  4
miss:
00402b1c  MOV  [ESP+0x8], ESI      ; head
00402b20  LEA  EAX, [ESP+0x8]
00402b24  MOV  EAX, [EAX]
00402b26  MOV  [EBX], EAX          ; *outIt = head
00402b28  POP  ESI
00402b29  MOV  EAX, EBX
00402b2b  POP  EBX
00402b2c  POP  ECX
00402b2d  RET  4
```

### Semantics (decompile ≡ equality gate bytes)

1. `lb = FUN_00403e50(map=EAX, key=EDI)` — residual lower_bound walk; isnil@**+0x29**; key slots node **+0x10** lo / **+0x14** hi.
2. If `lb == head` → `*outIt = head` (end / miss).
3. Else if `node.key <= *key` with **hi signed** then **lo unsigned** (JL/JG then JC) → `*outIt = lb` (hit).
4. Else `*outIt = head` (miss after non-end lower_bound).

Equality is lower_bound + `!(key < node)` = MSVC `_Tree::find` shape. **No** insert/erase/mutate. **Read-only.**

### Distinct from twin `0x004cba00`

| Unit | Map | Key | Stack | RET | Lower_bound |
|---|---|---|---|---|---|
| `004cba00` StdMap_Find_Tfid_Isnil29_Inferred | **ECX** thiscall | stack | outIt + pKey | **8** | `004cb4b0` |
| **`00402ae0` (this)** | **EAX** | **EDI** | outIt only | **4** | `00403e50` |

Same key layout / isnil family / equality CF; **different ABI clone**.

### Callers / xrefs (5 UNCONDITIONAL_CALL)

| Site | Parent | Notes |
|---|---|---|
| `0x00935340` | `Client_SendRespawnInSector` (`0x00935300`) | `LEA EAX,[ESI+0xd24]` map; `LEA EDI,[…+0x164]` TFID key; `CMP *out,[ESI+0xd28]` |
| `0x00944e4b` | `FUN_00944d50` | map `@EBX+0xd24`; key `@EBP+0x160`; payload `@node+0x20` |
| `0x0091120a` | `Client_Input_OnKeyDown_MatchAction` (`0x00911030`) | map `@0x00d1b564`; end `@0x00d1b568`; then RTTI cast on `node+0x20` |
| `0x0091df4e` | `FUN_0091da70` | map `@0x00d1b564`; TFID `@…+0x164` |
| `0x00957e3a` | (unnamed parent @ site) | map `EBP+0xd24`; key `…+0x164`; compare `EBP+0xd28`; payload `@node+0x20` |

Ghidra callers list: `Client_SendRespawnInSector`, `Client_Input_OnKeyDown_MatchAction`, `FUN_0091da70`, `FUN_00944d50` (+ xref site `0x00957e3a`).

### Callee

- Sole: `FUN_00403e50` @ `0x00403e50` — residual **lower_bound** (isnil29 / TFID pair key). **Not dualled** (MEGA-083 exclusive VA).

### analyze_function_complete

| Item | Value |
|---|---|
| Classification | worker |
| xref_count | 5 |
| callees | `FUN_00403e50` |
| Signature (Ghidra) | `undefined FUN_00402ae0(void)` — **do not trust**; bytes seal EAX/EDI/RET4 |

