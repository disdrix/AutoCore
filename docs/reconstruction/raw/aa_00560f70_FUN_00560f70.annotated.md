# Annotated low-level: FUN_00560f70 / WorldObj_RehomeLinksFromSnapshot_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00560f70` |
| VA | `0x00560f70`–`0x00561312` |
| System | world-object / phys rehome after mode exit |
| Date | 2026-07-29 (W27-O dual seal) |

## Machine-level notes

- Live `decompile_function` 2026-07-29 **≡** raw 2026-07-23 body.
- **thiscall** manager + object + snapshot `{void** data; int count}`; epilogue **`ret 8`**.
- Phase 0 identical to `00560f30`: save `manager+0x12c`, set 1, `0055e760`+`0055fa40`, restore.
- Phase 1: re-home **snapshot children** onto host sub-object `obj+0x44` list `+0x58` (grow via `005b3370` elemSize 4); virtual `vtbl+0x28` then second virtual (decompiler `unaff_EBX` residual); write `child+0x14 = host`.
- Phase 2: walk **obj link array** `obj+0x88` / count `+0x8c`; skip when `vtbl+0x20() == 0xb`; else **open-code** `PhysSim_PushBodyPair` policy onto `manager+0x20` (same as `0055efd0`).
- Phase 3: walk **obj action array** `obj+0x94` / count `+0x98`; match against host list `*(manager+0x2c)+0x4c`; on hit `006292a0` push + `00628f10` remove reverse; set `action+0x10 = host`; collect neighbors via action `vtbl+0x18` into arena buffer from `DAT_00b05060`; push more body pairs.
- Arena free path at exit (vtbl+0x28 / +0x14).

## Pseudocode (annotated stages)

```c
// void __thiscall WorldObj_RehomeLinksFromSnapshot_Inferred(
//     Manager *m, WorldObj *obj, Snapshot *snap /* {ptr*, count} */)
// RET 8
void __thiscall FUN_00560f70(int param_1, int param_2, int *param_3)
{
  // --- Phase 0: guarded reattach (same as 00560f30) ---
  uint8_t prev = *(uint8_t *)(param_1 + 300);
  *(uint8_t *)(param_1 + 300) = 1;
  FUN_0055e760(param_2);
  FUN_0055fa40(param_2);
  *(uint8_t *)(param_1 + 300) = prev;

  int *host = *(int **)(param_2 + 0x44);   // sim-host / island node

  // --- Phase 1: snapshot children → host list +0x58 ---
  for (int i = 0; i < param_3[1]; i++) {
    void *child = ((void **)param_3[0])[i];
    // grow host+0x58 dword vector if full (005b3370 elemSize 4)
    // store child; child vtbl+0x28(out16); second virtual; child+0x14 = host
  }

  // --- Phase 2: link array obj+0x88 / +0x8c → body pairs ---
  for (int j = 0; j < *(int *)(param_2 + 0x8c); j++) {
    void *link = ((void **)(*(int *)(param_2 + 0x88)))[j];
    if (link->vtbl[0x20/4]() == 0xb) continue;
    // if link+0xc == obj && other body flag+0x40==0 → pair (obj, other)
    // else if *(link+0xc)+0x40==0 → pair (obj, link+0xc)
    // open-coded PhysSim_PushBodyPair on manager+0x20 (005b3300 grow, elem 8)
  }

  // --- Phase 3: action array obj+0x94 / +0x98 → rehome + pairs ---
  // arena alloc ~0x40 dwords from DAT_00b05060
  // for each action: match host reverse list; 006292a0; 00628f10; action+0x10=host
  // action vtbl+0x18 fills temp list; pair each other body with +0x40==0
  // arena free
}
```

## Residual: `unaff_EBX`

Decompiler shows `(**(*unaff_EBX + 8))(child, &out)` after `child->vtbl+0x28(local_24)`. EBX is not a formal — almost certainly loaded from the 16-byte out-buffer / prior call result. CF of surrounding stores and list push is still sealed; exact second-virtual target type is a **gap**.

## Call graph

| Direction | Symbol | Role |
|---|---|---|
| Caller | `FUN_005d4440` | when **old** mode == **7**; snapshot from `FUN_005617c0` |
| Callee | `FUN_0055e760` / `FUN_0055fa40` | guarded reattach |
| Callee | `FUN_005b3370` | host child-list grow (elem 4) |
| Callee | `FUN_005b3300` | body-pair vector grow (elem 8) — inlined pair policy |
| Callee | `FUN_006292a0` | `Phys_HostActionList_Push_Inferred` |
| Callee | `FUN_00628f10` | `ActionHost_RemoveReverseListEntry_Inferred` |

## Open questions

- Product name for mode **7** / snapshot layout from `005617c0`.
- Exact type of second virtual after `vtbl+0x28` (`unaff_EBX`).
- Product English for link type code **0xb**.
- Runtime / bit-exact open.
