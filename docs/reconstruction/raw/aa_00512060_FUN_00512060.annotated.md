# Annotated low-level: CVOGHB_TargetingLink_complete_dtor_Inferred (FUN_00512060)

| Field | Value |
|---|---|
| Stable ID | `aa_00512060` |
| VA | `0x00512060`–`0x0051206a` |
| Canonical | `CVOGHB_TargetingLink_complete_dtor_Inferred` |
| System | heartbeat / targeting-link |
| Date | 2026-07-23 scaffold; 2026-07-29 W20-A dual seal |

## Machine-level notes

- Body is **11 bytes**:
  - `c7 01 b0 df 9c 00` — `mov dword ptr [ecx], 0x009cdfb0` (restore targeting-link family vtbl).
  - `e9 25 63 ff ff` — **tail jmp** to `FUN_00508390` @ `0x00508390` (relative from next-IP `0x0051206b`).
  - Following `cc` is int3 pad (not body).
- Decompiler shows `FUN_00508390(); return;` — **control-flow equivalent** to tail-jmp (no local work after).
- `FUN_00508390` (not owned here) immediately:
  1. `*this = PTR_FUN_009cdab0` (base `CVOGHBBase` vtbl),
  2. if `this[6] != 0` and `*(this[6]+0xb0) != 0`: `FUN_005085b0(this)` owner-list detach.
- So net effect of this complete dtor: intermediate family vtbl write, then demote to base + conditional detach.
- Callers:
  - Scalar-deleting dtor `FUN_00512070` (vtbl `009cdfb0` slot 0) CALL.
  - `thunk_FUN_00512060` @ `0x0051b890` JMP (same body shape; used by derived scalar path).

## Pseudocode (annotated)

```c
// MSVC complete object destructor for base targeting-link HB family.
// ECX = this (heartbeat object).
void __fastcall CVOGHB_TargetingLink_complete_dtor_Inferred(void **thisHb /* ECX */)
{
  *thisHb = (void *)&PTR_FUN_009cdfb0;   // family vtbl restore
  // tail: jmp FUN_00508390  — base dtor (vtbl→009cdab0 + optional detach)
  FUN_00508390(thisHb);
}
```

## Open questions

- Product/PDB mangled symbol (`??1…@…`).
- Whether intermediate vtbl write is required for virtual bases / members (none observed in this 11-byte body).
- Full owner-list layout behind `FUN_005085b0` (owned elsewhere).
