# Annotated low-level: FUN_00467320 / StdVector_UninitializedFillN_Elem28_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00467320` |
| VA | `0x00467320`–`0x0046736e` (**79 B** / `0x4F`) |
| System | containers / std::vector uninitialized_fill_n (POD stride 0x1c) |
| Date | 2026-07-29 (W35-T seal; scaffold 2026-07-23) |
| Inferred name | `StdVector_UninitializedFillN_Elem28_Inferred` |

## Machine-level notes

- Source: raw capture + W35-T live re-verify (`decompile` + `read_memory` + xrefs).
- **ABI (bytes win):** **ECX=dest**, **EDX=value\***, **EAX=count**; bare `RET`; no stack args.
- Stride **0x1C**: 7 dword fieldwise assign from `*value` into each dest slot.
- Early-out when count==0 (`TEST ESI,ESI` / `JBE` after `MOV ESI,EAX`).
- Null dest: skip stores, still advance dest/count (same pattern as sibling copy @ `0x004673b0`).
- Leaf; sole caller `FUN_00466ea0` (W34-Q insert-n fill arms).

## Pseudocode (annotated)

```c
// ECX = dest*, EDX = value*, EAX = count; bare RET
// POD T size 0x1c — trivial uninitialized_fill_n
void StdVector_UninitializedFillN_Elem28(
    Pod28* dest /*ECX*/,
    const Pod28* value /*EDX*/,
    int count /*EAX*/)
{
  if (count == 0) {
    return;
  }
  do {
    if (dest != nullptr) {
      dest->d0 = value->d0;
      dest->d1 = value->d1;
      dest->d2 = value->d2;
      dest->d3 = value->d3;
      dest->d4 = value->d4;
      dest->d5 = value->d5;
      dest->d6 = value->d6;
    }
    dest += 1; // +0x1c
    count -= 1;
  } while (count != 0);
}
```

## Open questions

- Product/PDB T English (parent residual only).
- Runtime / bit-exact open.
- Null-dest path never exercised by sole known caller.
