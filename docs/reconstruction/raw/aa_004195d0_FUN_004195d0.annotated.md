# Annotated low-level: CNDHash_Recreate_00a2c2e4

| Field | Value |
|---|---|
| Stable ID | `aa_004195d0` |
| VA | `0x004195d0`–`0x00419618` (**73 B**) |
| System | container / CNDHash (stamp `00a2c2e4`) |
| Date | 2026-08-04 (WQ9H-E dual; re-annotated from 2026-07-23 scaffold) |
| Canonical | `CNDHash_Recreate_00a2c2e4` |

---

## Machine-level notes

- **ABI:** `PUSH ESI; MOV ESI,EAX` → **EAX = hash\***; **BL = log2** (caller-held); bare **`POP ESI; RET`**.
- **Lock continue:** if `*(hash+0x1d) != 0`, log Recreate HashError + `VOG_DEBUG_STOP`, then **fall through** (no abort).
- **Destroy:** `MOV EAX,ESI; CALL 0x00406fc0` — matches dualed **EAX** ABI `CNDHash_DestroyBucketTable_00a2c2e4`.
- **Seed (bytes order):** `CL=BL`; `EAX=1; SHL EAX,CL`; store log2 `@+0x1c`, count `@+8`, zero `@+0x18` and `@+0x14`.
- **Alloc:** `CALL 0x00406f20` with **ESI** preserved — dualed `CNDHash_AllocBucketTable_00a2c2e4` (post-condition mask = count−1).
- **Sole caller** `FUN_007fe640`: after traversal unlock of `*(owner+0x3084)`, **`MOV BL,2`** then recreate (4 buckets).
- Prefer entry/exit bytes over decompiler `void`/unaff names.

## Pseudocode (annotated)

```c
// custom: EAX = CNDHash* hash; BL = log2; bare ret
void CNDHash_Recreate_00a2c2e4(/* EAX */ void *hash, /* BL */ uint8_t log2)
{
  // ESI = hash
  if (*(char *)(hash + 0x1d) != 0) {
    FUN_007a4480(0, "HashError:Recreate, already locked for traversal");
    FUN_007a4480(0, "VOG_DEBUG_STOP");
    // CONTINUE — not fatal
  }

  CNDHash_DestroyBucketTable_00a2c2e4(/* EAX */ hash); // 0x00406fc0

  *(uint8_t *)(hash + 0x1c) = log2;
  *(int *)(hash + 8) = 1 << (log2 & 0x1f);   // provisional COUNT
  *(uint32_t *)(hash + 0x18) = 0;             // ordered tail
  *(uint32_t *)(hash + 0x14) = 0;             // ordered head

  CNDHash_AllocBucketTable_00a2c2e4(/* ESI */ hash); // 0x00406f20 → mask at +8
}
```

## Callers

| Caller | Site | Setup |
|---|---|---|
| `FUN_007fe640` | `0x007fe707` | unlock `hash+0x1d=0`; `EAX=*(owner+0x3084)`; `BL=2` |

## Callees

| Callee | Role |
|---|---|
| `FUN_007a4480` | log |
| `CNDHash_DestroyBucketTable_00a2c2e4` (`0x00406fc0`) | tear down (dualed WQ9G-J) |
| `CNDHash_AllocBucketTable_00a2c2e4` (`0x00406f20`) | rebuild (dualed WQ9G-J) |

## Contrasts

| Peer | ABI | Helpers |
|---|---|---|
| Inventory `FUN_004138d0` | same EAX/BL bare ret | `00413e20` / `00413d80` |
| Skill `SkillCNDHash_Recreate_Inferred` | thiscall ECX + stack log2, `ret 4` | `004cba60` / `004cb410` |

## Open residual

- Product / PDB English for host class behind vtbl `00a2c2b0` / stamp `00a2c2e4`.
- Full dual of caller `FUN_007fe640` (not OWN).
- Runtime / bit-exact / differential.
