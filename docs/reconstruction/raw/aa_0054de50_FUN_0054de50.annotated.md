# Annotated low-level: FUN_0054de50 / SkillElementTypeMap_TreeInsertAndRebalance

| Field | Value |
|---|---|
| Stable ID | `aa_0054de50` |
| VA | `0x0054de50` |
| Body | `0x0054de50`–`0x0054e03a` |
| System | `skills-abilities` |
| Inferred name | `SkillElementTypeMap_TreeInsertAndRebalance` |
| Date | 2026-07-29 (W20-B re-annotate) |

## Machine-level notes

- MSVC `_Tree` **always-insert + red-black rebalance** for the **0x18-node** skill-element type map.
- Parent walk/find is `SkillElementTypeMap_InsertOrFind` (`0x0054eb20`); this helper never compares keys.
- Prefer sealed offsets over decompiler `undefined4` labels.
- Rotates: `FUN_004e22d0` / `FUN_006753b0` (map-relative thiscall roles sealed by call sites in rebalance loop; product English open).
- Capacity / size field: `map+0x08`; head/end sentinel: `map+0x04`.
- Head link convention: `head[0]=leftmost`, `head[1]=root`, `head[2]=rightmost`.

## ABI

| Item | Value | Evidence |
|---|---|---|
| Convention | `__thiscall` | ECX map → EDI |
| Stack args | 4 dwords | outIt*, addLeft, where*, value* |
| Epilogue | `RET 0x10` | `c2 10 00` |
| Size cap | `size > 0x1ffffffd` → throw | `cmp [edi+8],0x1ffffffd` |
| Throw string | `"map/set<T> too long"` | push `0x00a1530c` |

## Annotated CF (semantic)

```c
// Always insert (caller ensured miss). RET 0x10.
void __thiscall SkillElementTypeMap_TreeInsertAndRebalance(
    MapShell *map,     // ECX
    Node **outIt,
    char addLeft,      // 1 left / 0 right of where
    Node *where,
    const IntPtrPair *value)
{
  if (map->size > 0x1ffffffd)
    throw length_error("map/set<T> too long");

  // FUN_006173a0: new(0x18), links, copy key+mapped, color=0 (red), isnil=0
  Node *n = Node_Construct_0x18(map->head, where, map->head, value, /*color*/0);
  map->size++;

  if (where == map->head) {
    map->head->parent = map->head->left = map->head->right = n; // first element
  } else if (addLeft == 0) {
    where->right = n;
    if (where == map->head->right) map->head->right = n; // rightmost
  } else {
    where->left = n;
    if (where == map->head->left) map->head->left = n; // leftmost
  }

  // RB rebalance while parent color red (+0x14 == 0): uncle recolor / rotates
  // Exit: root color = black (1); *outIt = n
  RbFixup(map, n);
  *outIt = n;
}
```

## Offsets sealed

| Object | Off | Field |
|---|---:|---|
| MapShell | +0x04 | head / end sentinel* |
| MapShell | +0x08 | size (u32) |
| Node | +0x00 | left* |
| Node | +0x04 | parent* |
| Node | +0x08 | right* |
| Node | +0x0C | key (int type id) |
| Node | +0x10 | mapped (factory*) |
| Node | +0x14 | color (0 red / 1 black) |
| Node | +0x15 | isnil |

## Call graph

| Dir | VA | Role |
|---|---|---|
| Caller | `0x0054eb20` | SkillElementTypeMap_InsertOrFind (×2) |
| Callee | `0x006173a0` | Node construct `new(0x18)` |
| Callee | `0x004e22d0` | Tree rotate |
| Callee | `0x006753b0` | Tree rotate |
| Callee | CRT | `_CxxThrowException` / string / exception |
