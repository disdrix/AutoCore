# Annotated low-level: StdTree_BuyHeadNode_Isnil31_Size0x38_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00409180` |
| VA | `0x00409180` |
| Canonical | `StdTree_BuyHeadNode_Isnil31_Size0x38_Inferred` |
| Ghidra | `FUN_00409180` |
| System | MSVC `std::_Tree` head/sentinel allocation (isnil@+0x31, node 0x38) |
| Date | 2026-08-04 WQ9J-F dual seal |

## Machine-level notes

- Source: raw capture + live re-verify (`decompile` + `read_memory` + caller context).
- Decompiler shows `void`; machine returns **EAX = node\*** (caller `MOV [ESI+4],EAX`).
- Color store is `MOV [EAX+0x30], BL` with `EBX=1` (black); isnil `MOV [EAX+0x31], DL` with `EDX=0`.
- MSVC field-address null checks (`if (node+1 != 0)` etc.) are present; semantic intent is zero L/P/R.
- Sole caller `FUN_00405f20` completes head: isnil=1, self-link L/P/R, host size@+8=0.
- **Not** freelist / CNDHash / Client constructor product logic — scaffold chain-of-caller name is rejected.
- Twin isnil family: iterator++ `StdTree_IteratorIncrement_Isnil31` @ `0x00408590`. Contrast isnil29 head `0x00408a30` (size 0x30).

## Pseudocode (annotated)

```c
// ABI: no stack formals; bare RET; EAX = Node*
Node* StdTree_BuyHeadNode_Isnil31_Size0x38_Inferred(void)
{
  // SEH: LAB_009bc670
  Node* node = operator_new(0x38);   // PUSH 0x38

  if (node != NULL) node->left = 0;     // +0x00
  if (&node->parent != NULL) node->parent = 0; // +0x04 (MSVC field check form)
  if (&node->right != NULL) node->right = 0;   // +0x08

  node->color = 1;   // +0x30 black
  node->isnil = 0;   // +0x31 (callers force 1 for head)

  return node; // EAX
}
```

## Layout (this unit writes)

| Offset | Field | Init |
|---|---|---|
| +0x00 | left | 0 |
| +0x04 | parent | 0 |
| +0x08 | right | 0 |
| +0x0C..+0x2F | value (Val36 region) | untouched |
| +0x30 | color | **1** (black) |
| +0x31 | isnil | **0** |
| size | | **0x38** |

## Open gaps

- Product English / PDB demangle for host map types using isnil@+0x31 0x38 nodes.
- Value payload type inside Val36.
- Runtime / bit-exact / differential.
