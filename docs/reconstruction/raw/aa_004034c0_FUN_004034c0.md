# Raw capture: FUN_004034c0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004034c0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004034c0` |
| **Canonical name** | `FUN_004034c0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_004034c0(undefined4 param_1,undefined4 param_2,undefined4 param_3,int param_4)

{
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  puStack_c = &LAB_009bd210;
  local_10 = ExceptionList;
  local_8 = 0;
  ExceptionList = &local_10;
  for (; param_4 != 0; param_4 = param_4 + -1) {
    FUN_004040f0(param_3,param_1);
  }
  ExceptionList = local_10;
  return;
}
```

---

## Re-verify MEGA-134 (2026-08-05) — live Ghidra

| Field | Value |
|---|---|
| **Wave** | MEGA-134 OWN-ONLY dual |
| **Tools** | `decompile_function` + `disassemble_function` + `read_memory` + `analyze_function_completeness` + `get_function_callers` / `get_xrefs_to` / `get_function_callees` |
| **No** | `disassemble_bytes` primary; Launcher; parent ledgers |
| **Image** | `autoassault.exe` base `0x400000` |
| **Body** | `0x004034c0`–`0x00403522` inclusive (**99 B** / `0x63`); SEH catch `@00403523` |
| **Epilogue** | `RET 0xC` (`C2 0C 00`) |

### Live decompile (≡ 2026-07-23 raw CF)

```c
void __thiscall FUN_004034c0(undefined4 param_1,undefined4 param_2,undefined4 param_3,int param_4)
{
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  puStack_c = &LAB_009bd210;
  local_10 = ExceptionList;
  local_8 = 0;
  ExceptionList = &local_10;
  for (; param_4 != 0; param_4 = param_4 + -1) {
    FUN_004040f0(param_3,param_1);
  }
  ExceptionList = local_10;
  return;
}
```

**Decompiler caveat (assembly overrides):** Ghidra prints `FUN_004040f0(param_3,param_1)` without the list **this**. Live asm loads `ECX = [EBP+8]` (= list / formal `param_2`) before the call. Insert is **list thiscall**: `ECX=list`, stack `(where, val*)`.

### Live assembly (authoritative ABI)

```text
004034c0  PUSH EBP / MOV EBP,ESP
004034c3  SEH: PUSH -1; PUSH LAB_009bd210; FS:[0] chain
004034d8  SUB ESP,0xC; PUSH EBX/ESI/EDI
004034e1  MOV EBX,ECX              ; EBX = val* (entry ECX)
004034e3  MOV ESI,[EBP+0x10]       ; ESI = count
004034f0  MOV EDI,[EBP+0x0C]       ; EDI = where node*
004034f3  TEST ESI,ESI / JBE done
loop:
004034f7  PUSH EBX                 ; val*
004034f8  PUSH EDI                 ; where
004034f9  MOV ECX,[EBP+0x08]       ; list*
004034fc  CALL FUN_004040f0        ; insert one before where
00403501  SUB ESI,1 / store back / JMP loop
done:
00403509  SEH unlink; POP EDI/ESI/EBX; LEAVE
00403520  RET 0x0C
```

### Hex (99 B)

```
558bec6aff6810d29b0064a100000000506489250000000083ec0c5356578965f0
8bd98b75108975ecc745fc000000008b7d0c85f6761253578b4d08e8ef0b0000
83ee01897510ebeac745fcffffffff8b4df464890d000000005f5e5b8be55dc20c00
```

### Call graph (this VA only)

| Direction | Target | Evidence |
|---|---|---|
| **Callee** | `FUN_004040f0` @ `0x004040f0` | sole CALL; insert-before + Incsize |
| **Callee chain** | `FUN_00418700` BuyNode `0xC`; `FUN_00404840` Incsize max `0x3fffffff` / `"list<T> too long"` | via `004040f0` asm |
| **Caller** | `FUN_00402dd0` @ `0x00402dd0` | **1** UNCONDITIONAL_CALL @ `0x00402de6` |
| **Xrefs to** | same single site | `get_xrefs_to` |

### Sole caller site (grow half of list resize)

```text
; FUN_00402dd0 — ESI=list*, EAX=target size, stack arg = T value
MOV EDI,EAX
MOV EAX,[ESI+8]            ; _Mysize
CMP EDI,EAX / JBE shrink
MOV ECX,[ESI+4]            ; sentinel head
SUB EDI,EAX                ; count = target - size
PUSH EDI / PUSH ECX / PUSH ESI
LEA ECX,[ESP+0x14]         ; &value (stack T)
CALL FUN_004034c0          ; insert N copies before head (= push_back N)
RET 4
```

### Sealed signature (assembly)

```c
// ECX = const uint32_t* val
// [ESP+4]=list*, [ESP+8]=where_node*, [ESP+0xC]=count
// void; RET 0xC
void StdList_InsertN_BeforeNode_Dword_Inferred(
    const uint32_t *val /*ECX*/,
    void *list /*stack1*/,
    void *where /*stack2*/,
    unsigned count /*stack3*/);
```

### Classification

- **worker** (completeness): auto-name, no plate, shared MSVC `std::list` insert-N helper.
- **Not** mission-product logic; nested under `Client_DebugListMissionsStatus` only via list-resize parent `FUN_00402dd0`.
- Node payload **dword** (BuyNode `operator_new(0xC)`: Next/Prev/value).
- Name: `StdList_InsertN_BeforeNode_Dword_Inferred` (retire `Named_CalleeOf_*` scaffold).

### Completeness snapshot

`analyze_function_completeness`: classification **worker**; score 0 (undocumented scaffold); decompilation available; not thunk.

