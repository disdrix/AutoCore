# Annotated low-level: StdMap_FreeSubtree_Isnil15_Inferred (`FUN_004ba770`)

| Field | Value |
|---|---|
| Stable ID | `aa_004ba770` |
| VA | `0x004ba770`–`0x004ba7a4` |
| Canonical | `StdMap_FreeSubtree_Isnil15_Inferred` (Ghidra leave-name `FUN_004ba770`) |
| System | STL `std::map` / `std::set` node teardown (isnil `+0x15` family) |
| Date | 2026-07-29 (W20-N OWN-ONLY dual) |
| Tools | `decompile_function` + `read_memory` (no `disassemble_bytes`) |

## Machine-level notes

- **ABI:** stack arg `node*`; **`ret 4`**. ECX is saved and re-passed on recurse but **not used** for the walk (same pattern as other subtree free helpers).
- **isnil** gate: `cmp byte [node+0x15], 0` — non-zero = sentinel/nil, no free.
- **Links:** left `@+0`, parent `@+4` (unused here), right `@+8` (classic MSVC `_Tree_node`).
- **Algorithm (byte-sealed):** free right subtree recursively → `operator_delete(self)` → advance to left → loop until isnil. Depth-first free without recursion on the left spine.
- **Decompiler residual:** collapses to “recurse right then delete; never return.” Bytes prove left-walk loop and normal return. Prefer bytes.
- **Callers:** full-tree free from `StdMap_EraseRange_Inferred` (`FUN_004bacf0`); NDSpecialFX map clear (`FUN_004b85d0`).

## Control flow

```
node = stack_arg
if node->isnil (+0x15): return
loop:
  FreeSubtree(node->right)     // +8
  left = node->left            // +0  (snapshot before delete)
  operator_delete(node)
  node = left
  if !node->isnil: goto loop
return
```

## Pseudocode (annotated; CF ≡ bytes, not raw decompiler)

```c
// stack node*; ret 4; ECX threaded unused
void StdMap_FreeSubtree_Isnil15_Inferred(void *node)
{
  if (*(uint8_t *)((char *)node + 0x15) != 0)
    return; /* isnil / header */

  for (;;) {
    StdMap_FreeSubtree_Isnil15_Inferred(*(void **)((char *)node + 8)); /* right */
    void *left = *(void **)node; /* left @ +0 */
    operator_delete(node);
    node = left;
    if (*(uint8_t *)((char *)node + 0x15) != 0)
      break;
  }
}
```

## Open questions

- Product mangled name (`std::_Tree::_Erase` variant vs hand-rolled twin).
- Whether any caller expects payload dtor beyond plain `operator_delete` (node-only free; value dtor done by erase helpers first when needed).
- Runtime / bit-exact vs image.
