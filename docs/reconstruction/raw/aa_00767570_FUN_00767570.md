# Raw capture: FUN_00767570

| Field | Value |
|---|---|
| **Stable ID** | `aa_00767570` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00767570` |
| **Canonical name** | `FUN_00767570` |
| **System** | storage / arda2 stoChunk |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
bool FUN_00767570(void)

{
  uint uVar1;
  uint uVar2;
  uint uVar3;
  uint *unaff_EDI;
  
  uVar1 = *unaff_EDI;
  if (uVar1 == 0) {
    return false;
  }
  uVar3 = unaff_EDI[2];
  uVar2 = 0;
  if (uVar1 != 0) {
    uVar3 = uVar3 + (uVar1 >> 5) * 4;
    uVar2 = uVar1 & 0x1f;
  }
  uVar1 = *(uint *)(uVar3 + (uVar2 - 1 >> 5) * 4);
  FUN_004362c0(0);
  return (uVar1 & 1 << ((byte)(uVar2 - 1) & 0x1f)) != 0;
}
```

---

## W38-I live seal (2026-08-04) — append only

**Tools:** `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + xrefs/callers. **No** `disassemble_bytes`.

### Live decompile

Identical to raw body above (decompiler still collapses `FUN_004362c0` new-size/`param_2`).

### Machine bytes (`read_memory` @ `0x00767570`, 80 B)

```
8B 17 85 D2 75 03 32 C0 C3
8B 4F 08 53 33 C0 85 D2 56 76 0D
8B C2 8B F0 C1 EE 05 8D 0C B1 83 E0 1F
83 E8 01 8B F0 83 E0 1F C1 EE 05 8D 34 B1
8B C8 BB 01 00 00 00 D3 E3
6A 00 8D 42 FF 8B CF 85 1E 0F 95 C3
E8 07 ED CC FF
5E 8A C3 5B C3
CC CC
```

Body: `0x00767570`–`0x007675BD` (last `RET`; pad `CC`). Size **0x4E** (78 B).

### Byte-refined CF (corrects decompiler `FUN_004362c0(0)`)

1. `EDI` = soft-skip view: `size` at `+0`, bitset base at `+8`.
2. If `size == 0` → `XOR AL,AL; RET` (false).
3. Compute word ptr for bit `(size-1)` (same math as `stoChunkReader_SoftSkipIfBitSet` @ `0x00767120`).
4. `TEST [word], mask` → `SETNE BL` (capture prior bit).
5. `PUSH 0` (resize fill/arg) ; `LEA EAX,[size-1]` (new size) ; `MOV ECX,EDI` ; `CALL FUN_004362c0` — **shrink size from N → N-1** (pop top bit).
6. `MOV AL,BL; POP ESI; POP EBX; RET` — return prior bit as bool.

### Xrefs

- Single call site: `0x00765C96` in `FUN_00765740` (gfxBody unserialize, `gfxBody.cpp`). Return value **ignored** at that site (side-effect pop).
