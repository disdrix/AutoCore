# Annotated low-level: StdTree_InsertAndRebalance_Isnil29_Inferred (FUN_00406c40)

| Field | Value |
|---|---|
| Stable ID | `aa_00406c40` |
| VA | `0x00406c40`–`0x00406dd5` exclusive (**405 B**) |
| System | STL map/set — MSVC `_Tree` always-insert + RB rebalance (isnil@+0x29) |
| Date | 2026-08-04 (WQ9E-G dual; scaffold 2026-07-23) |
| Canonical | `StdTree_InsertAndRebalance_Isnil29_Inferred` |
| Ghidra | `FUN_00406c40` |

## Machine-level notes

- Prefer live decompile + `read_memory` for ABI (epilogue **`ret 0x10`**, max-size imm **`0x0AAAAAA9`**).
- Color byte **`+0x28`**, isnil **`+0x29`** — same family as dualed `StdTree_Lrotate_Isnil29` / `StdTree_Rrotate_Isnil29_Inferred`.
- Buynode `FUN_00407e30`: `operator_new(0x30)` then `FUN_004098d0` (links + value 6×u32 @+0x10 + color + isnil=0).
- Parent `FUN_00406040` does key lower-bound / uniqueness; this unit never compares keys.
- Rotates called with **ECX = map** (bytes `mov ecx,edi` / `8B CF` before `E8` to `004192a0`/`004192f0`).

## ABI

```text
__thiscall
  ECX  = MapShell*   (head @ +4, size @ +8)
  stack0 = Node** out
  stack1 = char addLeft   (0 → link as right child; non-0 → left)
  stack2 = Node* where    (parent / hint; head means empty tree)
  stack3 = Val* value     (0x18-byte payload for node ctor)
  RET 0x10
  *out = newly inserted node; root color forced black
```

## Pseudocode (role-annotated)

```c
// ECX = map
void __thiscall StdTree_InsertAndRebalance_Isnil29_Inferred(
    MapShell* map, Node** out, char addLeft, Node* where, Val18* value)
{
  // 1) capacity: if map->size > 0x0AAAAAA8 → throw "map/set<T> too long"
  // 2) n = Buynode0x30(head, where, head, value, color=red=0)  // FUN_00407e30
  // 3) map->size++
  // 4) link:
  //      where==head → root=leftmost=rightmost=n
  //      addLeft==0  → where->right=n; maybe head->rightmost=n
  //      else        → where->left=n;  maybe head->leftmost=n
  // 5) RB while parent color red:
  //      parent is left of grand:
  //        uncle=grand->right red → recolor; climb
  //        else if x is right → Lrotate(parent); then Rrotate(grand)
  //      parent is right of grand: mirror (Rrotate/Lrotate)
  //    Rotates: FUN_004192a0 L, FUN_004192f0 R (isnil29)
  // 6) root->color = black; *out = n
}
```

## Callees

| VA / symbol | Role |
|---|---|
| `FUN_00407e30` | buynode 0x30 |
| `FUN_004098d0` (via buynode) | node field init; isnil@+0x29=0 |
| `StdTree_Lrotate_Isnil29` `0x004192a0` | left rotate |
| `StdTree_Rrotate_Isnil29_Inferred` `0x004192f0` | right rotate |
| `FUN_00401a40` + `_CxxThrowException` | length_error |

## Callers

| Caller | Role |
|---|---|
| `FUN_00406040` | insert-or-find (uint key @ node+0x10); 2 sites |

## Residuals

- Product / MSVC demangle for map value type (0x18 payload).
- Exact English for parent map host class.
- Runtime / bit-exact / differential.
