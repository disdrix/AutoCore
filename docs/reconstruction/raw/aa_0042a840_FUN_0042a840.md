# Raw capture: FUN_0042a840

| Field | Value |
|---|---|
| **Stable ID** | `aa_0042a840` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0042a840` |
| **Canonical name** | `FUN_0042a840` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_0042a840(int *param_1,int param_2)

{
  int iVar1;
  int *piVar2;
  
  iVar1 = *param_1;
  *param_1 = *(int *)(iVar1 + 8);
  if (*(char *)(*(int *)(iVar1 + 8) + 0x2d) == '\0') {
    *(int **)(*(int *)(iVar1 + 8) + 4) = param_1;
  }
  *(int *)(iVar1 + 4) = param_1[1];
  if (param_1 == *(int **)(*(int *)(param_2 + 4) + 4)) {
    *(int *)(*(int *)(param_2 + 4) + 4) = iVar1;
    *(int **)(iVar1 + 8) = param_1;
    param_1[1] = iVar1;
    return;
  }
  piVar2 = (int *)param_1[1];
  if (param_1 == (int *)piVar2[2]) {
    piVar2[2] = iVar1;
    *(int **)(iVar1 + 8) = param_1;
    param_1[1] = iVar1;
    return;
  }
  *piVar2 = iVar1;
  *(int **)(iVar1 + 8) = param_1;
  param_1[1] = iVar1;
  return;
}
```

---

## WQ9K-J re-verify (2026-08-05) — append only

| Field | Sealed value |
|---|---|
| **Body** | `0x0042a840`–`0x0042a88f` inclusive (**80 B** / `0x50`); three `RET 4` exits |
| **ABI** | **ECX** = node `x*`; stack `[esp+4]` = tree/map*; void; **`RET 4`** (`C2 04 00`). **Not** ECX=tree (isnil29 invert) |
| **Semantics** | MSVC-style **right rotate**: `y = x->left`; hoist `y` into `x`'s place; hang `x` as `y->right` |
| **isnil** | guard `@+0x2D` on `y->right` before reparent (`80 7A 2D 00`) |
| **Layout used** | left@+0, parent@+4, right@+8; tree head@+4; root = head->parent @ head+4 |
| **Recolor** | none (no color@+0x2C writes) — pure structural leaf |
| **Callees** | none (leaf) |
| **Callers (4)** | `FUN_00401db0`, `FUN_0042a930`, `FUN_00430b60` (StringKey insert), `FUN_004094c0` (StringKey erase) |
| **Xrefs** | 10 UNCONDITIONAL_CALL |
| **Peer L-rotate** | `FUN_0044e010` (same isnil@+0x2D, same ECX=node ABI) |
| **Name** | `StdTree_Rrotate_Isnil2D_Inferred` (Ghidra `FUN_0042a840`) |
| **Decompile note** | Decomp `__thiscall` on node + stack tree matches bytes; three early RETs preserved |

### Assembly (from disassemble_function + read_memory; no disassemble_bytes)

```text
0042a840  MOV  EAX, [ECX]           ; y = x->left
0042a842  MOV  EDX, [EAX+8]         ; y->right
0042a845  MOV  [ECX], EDX           ; x->left = y->right
0042a847  MOV  EDX, [EAX+8]
0042a84a  CMP  byte ptr [EDX+0x2d], 0  ; isnil on y->right
0042a84e  JNZ  0042a853
0042a850  MOV  [EDX+4], ECX         ; y->right->parent = x
0042a853  MOV  EDX, [ECX+4]
0042a856  MOV  [EAX+4], EDX         ; y->parent = x->parent
0042a859  MOV  EDX, [ESP+4]         ; tree
0042a85d  MOV  EDX, [EDX+4]         ; head
0042a860  CMP  ECX, [EDX+4]         ; x == root?
0042a863  JNZ  0042a871
0042a865  MOV  [EDX+4], EAX         ; root = y
0042a868  MOV  [EAX+8], ECX         ; y->right = x
0042a86b  MOV  [ECX+4], EAX         ; x->parent = y
0042a86e  RET  4
0042a871  MOV  EDX, [ECX+4]         ; parent
0042a874  CMP  ECX, [EDX+8]         ; x was right child?
0042a877  JNZ  0042a885
0042a879  MOV  [EDX+8], EAX
0042a87c  MOV  [EAX+8], ECX
0042a87f  MOV  [ECX+4], EAX
0042a882  RET  4
0042a885  MOV  [EDX], EAX           ; else left child
0042a887  MOV  [EAX+8], ECX
0042a88a  MOV  [ECX+4], EAX
0042a88d  RET  4
```

### Call-site memory (ABI seal)

```text
; StringKey insert rebalance FUN_00430b60 @ 00430c72
PUSH EDI                ; tree/map
MOV  ECX, [EAX+4]       ; node (parent path)
CALL 0042a840           ; RET 4 cleans push
; color fixup around call uses +0x2C (not touched inside rotate)

; StringKey erase FUN_004094c0 @ 0040968a
PUSH EDX                ; tree
; ECX = node
CALL 0042a840
```

### Hex body (`read_memory` 80 B)

```
8B 01 8B 50 08 89 11 8B 50 08 80 7A 2D 00 75 03
89 4A 04 8B 51 04 89 50 04 8B 54 24 04 8B 52 04
3B 4A 04 75 0C 89 42 04 89 48 08 89 41 04 C2 04 00
8B 51 04 3B 4A 08 75 0C 89 42 08 89 48 08 89 41 04
C2 04 00 89 02 89 48 08 89 41 04 C2 04 00
```
