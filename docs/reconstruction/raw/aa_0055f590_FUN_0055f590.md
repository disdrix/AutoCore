# Raw capture: FUN_0055f590

| Field | Value |
|---|---|
| **Stable ID** | `aa_0055f590` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0055f590` |
| **Canonical name** | `FUN_0055f590` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0055f590(undefined4 *param_1)



{

  int *piVar1;

  int iVar2;

  

  piVar1 = DAT_00b05060;

  iVar2 = param_1[4] * 0x10;

  if ((iVar2 == 0) || ((param_1[3] != DAT_00b05060[5] && (DAT_00b05060[5] != 0)))) {

    DAT_00b05060[2] = DAT_00b05060[2] + param_1[4] * -0x10;

    piVar1[3] = piVar1[3] + iVar2;

  }

  else {

    (**(code **)(*DAT_00b05060 + 0x28))(param_1[3],iVar2);

  }

  if (-1 < (int)param_1[2]) {

    (**(code **)(*DAT_00b05060 + 0x14))(*param_1,param_1[2] << 4,0x12);

  }

  return;

}
```


---

## W28-R re-verify (2026-07-29) — live Ghidra seal

| Field | Value |
|---|---|
| **Agent** | W28-R OWN-ONLY dual |
| **Tools** | `decompile_function` + `read_memory` + `get_function_by_address` + callers/xrefs (**no** `disassemble_bytes`) |
| **Program** | `autoassault.exe` base `0x400000` |
| **Body** | `0x0055f590`–`0x0055f5e7` inclusive (**88 / `0x58` B**); pad `CC` after `RET` |
| **Prologue (read_memory)** | `56 8B F1 8B 46 10 8B 56 0C 8B 0D 60 50 B0 00 C1 E0 04 83 C0 0F 83 E0 F0` — ECX desc; load count/slab; align16(`count*0x10`) |
| **Epilogue (read_memory @ 0x0055f5e5)** | `5F 5E C3` — pop EDI/ESI; **plain `ret`** (0 stack formals) |
| **ABI** | **__fastcall/thiscall**; ECX = worklist desc; void; no stack cleanup |
| **Classification** | worker; callees only via `DAT_00b05060` vtable `+0x28` (slab free) and `+0x14` (tagged free tag `0x12`) |
| **Callers (1 / 1 xref)** | `FUN_006297e0` (`Phys_CommitPairListStorage_Inferred`) — paired after `FUN_0055f520` |
| **Named role (INFERRED)** | `LocalWorklist16_Release_Inferred` |
| **Integrity** | Live decompile 2026-07-29 ≡ raw 2026-07-23 CF; machine has align16 on slab size and `AND 0x7fffffff` before `<<4` on tag free (decompiler omits both) |

### Live decompile (authoritative 2026-07-29)

```c
void __fastcall FUN_0055f590(undefined4 *param_1)

{
  int *piVar1;
  int iVar2;
  
  piVar1 = DAT_00b05060;
  iVar2 = param_1[4] * 0x10;
  if ((iVar2 == 0) || ((param_1[3] != DAT_00b05060[5] && (DAT_00b05060[5] != 0)))) {
    DAT_00b05060[2] = DAT_00b05060[2] + param_1[4] * -0x10;
    piVar1[3] = piVar1[3] + iVar2;
  }
  else {
    (**(code **)(*DAT_00b05060 + 0x28))(param_1[3],iVar2);
  }
  if (-1 < (int)param_1[2]) {
    (**(code **)(*DAT_00b05060 + 0x14))(*param_1,param_1[2] << 4,0x12);
  }
  return;
}
```

### Machine corrections vs decompiler

- Slab size: `(desc[4]*0x10 + 0xf) & 0xfffffff0` (not bare `*0x10`).
- Tag free nbytes: `(desc[2] & 0x7fffffff) << 4` then push tag **`0x12`** (decompiler shows bare `<<4`).
- Sibling of `LocalWorklist_Release_Inferred` (`0x0055f4c0`, element stride **8**); this unit is stride **16**.
