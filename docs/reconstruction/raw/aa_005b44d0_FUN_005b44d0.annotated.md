# Annotated low-level: Obj_009d95b4_ScalarDeletingDtor (`FUN_005b44d0`)

| Field | Value |
|---|---|
| Stable ID | `aa_005b44d0` |
| VA | `0x005b44d0` |
| Body | `0x005b44d0`–`0x005b44ee` exclusive (**30** B / `0x1E`) |
| System | host object / MSVC scalar-deleting destructor |
| Date | 2026-07-29 (W30-N dual seal) |

## Machine-level notes

- Classic MSVC **scalar-deleting dtor** wrapper: save `this` in ESI, call body dtor, optional `operator_delete` on flags bit0, return `this`, `RET 4`.
- **Ghidra false noreturn** on `operator_delete` — bytes fall through to `MOV EAX,ESI; POP ESI; RET 4`.
- Body twin `FUN_005b3b20` / `Obj_BodyDtor_SetVtbl_DestroyStdListAt10` (W28-N): installs final vtbl `0x009d95b4`, destroys list at `this+0x10`.
- Sole xref is **DATA** from `PTR_FUN_009d95b4[0]`; virtual dispatch only.
- Adjacent rdata after vtbl dword is shader text (`vs.1.1\ndcl_position…`), **not** a class name plate.

## Signature (byte-sealed)

```c
// __thiscall; ECX=this; stack flags; RET 4; EAX=this
void* __thiscall Obj_009d95b4_ScalarDeletingDtor(void* self, uint8_t flags);
```

## Algorithm (annotated)

```
ESI = this
call Obj_BodyDtor_SetVtbl_DestroyStdListAt10(this)   // FUN_005b3b20
if (flags & 1):
  operator_delete(this)
return this   // RET 4
```

## Call surface

| Direction | Detail |
|---|---|
| Callees | `FUN_005b3b20` (body), `operator_delete` |
| Callers | none code; DATA vtbl `0x009d95b4` |
| Related | ctors `FUN_005b3f60` / `FUN_005b4260` install same vtbl |

## Open questions

- Product English / MSVC demangle for type behind `PTR_FUN_009d95b4`.
- Runtime hit / bit-exact — open.
