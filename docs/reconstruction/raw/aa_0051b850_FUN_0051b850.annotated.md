# Annotated low-level: CVOGHBPlayerTargetingLink_ClearOwnerSelectedTarget (`FUN_0051b850`)

| Field | Value |
|---|---|
| Stable ID | `aa_0051b850` |
| VA | `0x0051b850`–`0x0051b861` |
| System | `heartbeat` / targeting link / object combat-target |
| Date | Dual A/B **2026-07-29**; gap-fill artifacts **2026-08-05** |

---

## Machine-level notes

- **ABI:** `__thiscall` — `ECX = this` (`CVOGHBPlayerTargetingLink` / HB object, size `0x28` at alloc site). No stack args; bare `ret` (`c3`).
- **Classification:** leaf virtual — **no callees**; only DATA xref from derived vtbl slot 7.
- **Step 1:** Load `owner = *(this + 0x18)` — `CVOGHBBase.pOwnerObject` (AttachOwner / ctor attach entity).
- **Step 2:** If owner null → return (no store).
- **Step 3:** `*(owner + 0xA0) = 0` — clear **selected combat/target pointer** on the owning entity (same field as `Object_SetSelectedTarget` `0x005172d0`).
- Prefer assembly when decompiler uses `__fastcall` label — image is thiscall ECX-only.

---

## Byte seal

```
0051b850  8B 41 18              mov eax, [ecx+0x18]       ; pOwnerObject
0051b853  85 C0                 test eax, eax
0051b855  74 0A                 jz   0051b861             ; null owner → ret
0051b857  C7 80 A0 00 00 00 00 00 00 00
                                mov dword ptr [eax+0xA0], 0  ; clear selected target
0051b861  C3                    ret
0051b862  CC…                   int3 pad
```

Hex (body through `c3`):  
`8b411885c0740ac780a000000000000000c3`

---

## Pseudocode (annotated)

```c
// __thiscall CVOGHBPlayerTargetingLink_ClearOwnerSelectedTarget(this)
// Derived vtbl slot 7 (PTR_FUN_009cdfd4 + 0x1C). Base slot 7 = FUN_0056f570 (nop).
void __fastcall FUN_0051b850(int this /* ECX */)
{
  int owner = *(int *)(this + 0x18);   // CVOGHBBase.pOwnerObject
  if (owner != 0) {
    *(undefined4 *)(owner + 0xa0) = 0; // entity selected-target pointer → null
  }
  return;
}
```

---

## Call surface

| Role | VA | Note |
|---|---|---|
| DATA xref only | `0x009cdff0` | Derived vtbl slot 7 entry |
| Code callers | none | Invoked only via virtual dispatch |
| Callees | none | Leaf store |
| Related write | `Object_SetSelectedTarget` `0x005172d0` | Sets / manages `entity+0xA0` |
| Related ctor | `CVOGHBPlayerTargetingLink_ctor` `0x00512090` | Installs vtbl `0x009cdfd4` |
| Base counterpart | `FUN_0056f570` | Empty `ret` — intentional asymmetry |

---

## Vtbl contrast (first 8 slots)

| Slot | Base `0x009cdfb0` | Derived `0x009cdfd4` |
|---|---|---|
| 0 | `0x00512070` | **`0x0051b870`** (scalar dtor) |
| 1–6 | shared | shared |
| 7 | `0x0056f570` (nop) | **`0x0051b850`** (this — clear owner selected target) |

---

## Why base is nop

Selected-target field `+0xA0` lives on the **selecting** entity. Target-side HB (base vtbl) must not zero the selector's target. Only self-side `CVOGHBPlayerTargetingLink` owns this side effect.

---

## Open questions

1. Product virtual method English (OnEnd / OnDetach / partner-clear — **INFERRED** role only).
2. Exhaustive call sites that dispatch slot 7 (Tick destroy path vs explicit vcall).
3. Whether any non-virtual path clears `+0xA0` equivalently (out of scope for this leaf).
