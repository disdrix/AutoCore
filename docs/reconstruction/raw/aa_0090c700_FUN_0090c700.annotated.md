# Annotated low-level: FUN_0090c700 → MedalUI_PrepClearButtonSlots_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0090c700` |
| VA | `0x0090c700`–`0x0090c74c` (**77 B** / `0x4D`) |
| System | `missions-progression` |
| Date | 2026-08-05 (MEGA-102 refresh) |
| Prior scaffold | `Named_CalleeOf_Named_VOG_DEBUG_STOP_0090c700` (retired) |
| Proposed name | `MedalUI_PrepClearButtonSlots_Inferred` |

---

## Machine-level notes

- **Authority:** live `disassemble_function` + `read_memory` (MEGA-102). Decompiler CF matches bytes; recover offsets from listing (`+0x540` not decompiler dword index alone).
- **ABI:** host in **EDI** (not ECX). Parent `FUN_0090cbc0` does `MOV EDI,ESI` immediately before the call.
- **Slot array:** `host+0x540` … 24 dwords (`0x18`). Sibling init `FUN_0090c750` zeros the same range without dtor calls; this unit is the **live teardown** path.
- **Virtuals on host:** `vtbl[+0x3d8]` query (byte AL); `vtbl[+0xb0](slot)` detach when query true.
- **Virtual on slot:** MSVC scalar deleting dtor at `vtbl[0]` with **flags=1** (dtor + free).
- **Parent role:** first step of medal UI rebuild after global medal host `DAT_00d1b6d8` and host visibility gate; then `FUN_0090c810` count, `FUN_0090c9a0` rebuild (`i_d_a_2d_btn_achievement.xml`).

---

## Pseudocode (annotated)

```c
// EDI = medal UI host (caller-seeded)
// void; bare RET
void MedalUI_PrepClearButtonSlots_Inferred(void /* EDI=host */)
{
  // ESI = &host->buttonSlots[0]   @ +0x540
  // EBX = 0x18 remaining
  int* slot = (int*)((char*)host + 0x540);
  int remaining = 0x18;

  do {
    if (*slot != 0) {
      // Host visible/active? (thiscall ECX=host)
      char visible = host->vtbl[+0x3d8]();
      if (visible != 0) {
        // Detach/remove UI child (thiscall ECX=host, arg=slot*)
        host->vtbl[+0xb0](*slot);
      }
      // Scalar-delete button widget if still held
      if (*slot != 0) {
        ((void (__thiscall*)(void*, int))(*(void***)*slot)[0])((void*)*slot, 1);
      }
      *slot = 0;
    }
    slot += 1;          // +4 bytes
    remaining -= 1;
  } while (remaining != 0);
}
```

---

## Offsets / constants

| Symbol | Value | Role |
|---|---|---|
| `kButtonSlotBase` | `+0x540` | first of 24 `CNDUI*` / widget* slots |
| `kButtonSlotCount` | `0x18` (24) | page capacity (3×8 grid; parent pages by 3) |
| `kHostVfuncVisible` | vtbl `+0x3d8` | byte return; gates detach |
| `kHostVfuncDetachChild` | vtbl `+0xb0` | remove child from host |
| `kScalarDeleteFlags` | `1` | dtor + `operator delete` |

---

## Call graph (this VA only)

```text
FUN_0090cbc0  (medal UI rebuild host — residual parent MEGA partition)
  └─ FUN_0090c700  MedalUI_PrepClearButtonSlots_Inferred  [OWN MEGA-102]
       ├─ host vtbl[+0x3d8]   (virtual)
       ├─ host vtbl[+0xb0]    (virtual, conditional)
       └─ slot  vtbl[0](1)    (scalar deleting dtor, conditional)

Siblings (not owned):
  FUN_0090c810  count medals for UI
  FUN_0090c9a0  allocate/bind achievement button ("i_d_a_2d_btn_achievement.xml")
  FUN_0090c750  zero slots + related host fields (ctor-ish clear, no dtor)
```

---

## Open questions

- Product class name of medal UI host (EDI/ESI object).
- English names for host vtbl `+0x3d8` / `+0xb0`.
- Exact widget type behind slot pointers (XML implies achievement 2D button).
- Runtime / bit-exact / differential — open (no Launcher).
