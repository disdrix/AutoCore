# Annotated low-level: FUN_00459750 → AnmTrackMaster_Unserialize_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00459750` |
| VA | `0x00459750`–`0x00459a5a` (**779 B**) |
| System | palantir/animation — `anmTrackMaster_inl` |
| Date | 2026-07-23 scaffold; **W36-M dual seal 2026-07-29** |

## Machine-level notes

- **thiscall**: ECX=`this` (saved EBX); stack `ctx*`; **`RET 4`**.
- SEH + `stoChunkReader_EnterChunkScope` on local chunk state.
- Tag gate (host u32): **PBAM** `0x5042414D` (file `MABP`) or **TRAK** `0x5452414B` (file `KART`).
- Fail log: `"..\\palantir/animation/anmTrackMaster_inl.h"` via `vog_LogMessage`.
- Dual I/O: `*(ctx+0x4044)==0` → `FUN_004*` readers; else `FUN_007*` pack readers.
- Type fourCC at `this+0x10`: if read == `0x4B455556` → **EFCT** `0x45464354`, else **SKEL** `0x534B454C`.
- Hashes via `FUN_00989e00` → `this+0x14` / `this+0x18`.
- **Pod28** vector at **`this+0x1C`**: `ADD EBX,0x1C` then sealed `StdVector_Resize_Pod28` (`FUN_0045c4b0` @ call `0x004599bf`) with stack 0x1C value blob.
- Optional `FUN_0045dd60` when count &gt; 0.
- Returns: **0** success, **0xFFFFFFFE** mid error, **0xFFFFFFFF** fail.
- Xrefs: **DATA only** `0x00aa3d54` (no direct CALL).

## Pseudocode (annotated summary)

```c
uint32_t __thiscall AnmTrackMaster_Unserialize_Inferred(int* self, int* ctx)
{
  // SEH; EnterChunkScope → local_4c=tag, local_48=version, local_50=owns_scope
  if (tag != 0x5042414D && tag != 0x5452414B) {
    vog_LogMessage("..\\palantir/animation/anmTrackMaster_inl.h", …);
    goto fail; // -1
  }
  // version>2 optional path; dual I/O via ctx+0x4044
  // type: (read==0x4B455556)? EFCT : SKEL → self[4]
  // strings → hashes self[5], self[6]
  // ints; count = …
  // StdVector_Resize_Pod28((Pod28Vec*)(self+7) /*this+0x1C*/, count, value);
  // if count>0: FUN_0045dd60(…); may return 0
  // cleanup strings/scope
  return 0; // or -2 / -1
}
```

Full decompiler body retained in raw capture.

## Open questions

- Product class/method demangle.
- Complete field map beyond sealed offsets.
- Nested helper duals (`FUN_0045dd60`, I/O, hash) unowned.
- Owner of DATA slot `0x00aa3d54`.
- Runtime / bit-exact.
