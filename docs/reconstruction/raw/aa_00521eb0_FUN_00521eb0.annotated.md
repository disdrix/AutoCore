# Annotated low-level: Item_GetTinkerSkillOrCap_Inferred (FUN_00521eb0)

| Field | Value |
|---|---|
| Stable ID | `aa_00521eb0` |
| VA | `0x00521eb0`–`0x00521ef6` inclusive (**71 B** / `0x47`) |
| Ghidra name | `FUN_00521eb0` |
| Canonical name | `Item_GetTinkerSkillOrCap_Inferred` |
| System | inventory-transfer / tinker kit skill gate |
| Date | 2026-08-05 (MEGA-106 dual refresh) |
| Prior scaffold | `Named_CalleeOf_Named_CalleeOf_Skill_You_do_not_possess_the_Tink_00521eb0` (**retired**) |

## Machine-level notes

- **Leaf** switch: no callees; pure map of `uint8` at **`this+0x59b`** → tier **0..5**.
- **ABI:** ECX = object (item); no stack args; **EAX** = tier; plain **`RET`**.
- **Callers seal ECX = item** (not character): both sites `MOV ECX,EDI` where EDI is the item holding `+0x6b4` and `+0x59b`.
- Character skill is **parent-side** via vfunc `*[vtbl+0x8c]`; compared as `skill+1` against this return.
- Jump table at `0x00521ef8` (outside body; 9 dwords). Default for `v>9` forces **5** via `SBB`/`AND 5`.
- Prefer assembly when decompiler shows `__fastcall` vs thiscall — encoding is ECX-this, identical.

## Mapping (sealed)

| `*(uint8*)(item+0x59b)` | return |
|---|---|
| 0 | 0 |
| 1, 2 | 1 |
| 3, 4 | 2 |
| 5, 6 | 3 |
| 7, 8 | 4 |
| 9 | 5 |
| ≥10 | 5 |

Equivalent: `v == 0 ? 0 : min((v + 1) / 2, 5)`.

## Pseudocode (annotated ≡ raw ≡ live)

```c
// ECX = item*; EAX = tier 0..5; RET
byte __thiscall Item_GetTinkerSkillOrCap_Inferred(int item /* ECX */)
{
  byte v = *(byte *)(item + 0x59b);
  if (v == 0)
    return 0;
  switch (v) {
  case 1: case 2: return 1;
  case 3: case 4: return 2;
  case 5: case 6: return 3;
  case 7: case 8: return 4;
  case 9:         return 5;
  default:        return (v > 9) ? 5 : 0; /* only v>9 reaches default */
  }
}
```

## Parent use (evidence only — not dualled)

| Caller | Site | Compare |
|---|---|---|
| `FUN_00513c10` (partition: Item_ValidateTinkerKitUse) | `0x00513ca4` | fail path if `tier < skill+1` and `item+0x6b4 < 1` → status `1` |
| `FUN_00521f20` | `0x00521f59` | if `tier >= skill+1`, build float from raw `+0x59b` + peer fields |

## Open questions

- Product English for field `+0x59b` (tinker rank? quality? kit tier?) — **Inferred**.
- Exact product name of vfunc `+0x8c` (character skill level).
- Runtime / bit-exact / differential — open (Terminal false).
