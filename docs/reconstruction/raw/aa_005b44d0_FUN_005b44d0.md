# Raw capture: FUN_005b44d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_005b44d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005b44d0` |
| **Canonical name** | `FUN_005b44d0` |
| **System** | host object / MSVC scalar-deleting destructor |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_005b44d0(void *param_1,byte param_2)

{
  FUN_005b3b20();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```

---

## W30-N re-verify (2026-07-29)

**Tools:** live `decompile_function` + `analyze_function_complete` + `read_memory` (full body) + `get_bulk_xrefs` + vtbl dword @ `0x009d95b4`. **No** `disassemble_bytes`.

### Sealed facts

1. **Body:** `0x005b44d0`–`0x005b44ee` exclusive (**30** B / `0x1E`). Epilogue `C2 04 00` (`RET 4`). Pad `CC CC`. Ghidra plate `005b44d0`–`005b44ed`.
2. **ABI:** **`__thiscall`**; ECX = `this`; stack formal `flags` (accessed as `[esp+8]` after `push esi`); cleans **4** bytes; returns `this` in EAX.
3. **Semantics:** MSVC **scalar-deleting destructor** = **vtbl[0]** of `PTR_FUN_009d95b4` (`*(uint32*)0x009d95b4 == 0x005b44d0`):
   - `ESI = ECX`
   - `call FUN_005b3b20` (`Obj_BodyDtor_SetVtbl_DestroyStdListAt10`, W28-N **accept-with-gaps**)
   - `if (flags & 1) operator_delete(this)`
   - `return this; RET 4`
4. **False noreturn:** Ghidra marks `operator_delete` noreturn; bytes are normal `CALL` + `ADD ESP,4` + `MOV EAX,ESI` + `POP ESI` + `RET 4`.
5. **Xrefs:** 1 DATA from vtbl `0x009d95b4` only (no code CALL sites — dispatched via vtbl).
6. **Callees:** `FUN_005b3b20`, `operator_delete`.
7. **Live decompile ≡ scaffold raw CF** for all stages.
8. **Semantic name:** `Obj_009d95b4_ScalarDeletingDtor` (vtbl-disambiguated; product English open — adjacent rdata is shader text `vs.1.1…`).

### Full-body hex (`read_memory`)

```
56 8B F1 E8 48 F6 FF FF F6 44 24 08 01 74 09 56 E8 3D 53 ED FF 83 C4 04 8B C6 5E C2 04 00
```

- `E8 48 F6 FF FF` from `0x005b44d3` → `0x005b3b20`
- `F6 44 24 08 01` / `74 09` — flags bit0 gate
- `C2 04 00` — ret 4
