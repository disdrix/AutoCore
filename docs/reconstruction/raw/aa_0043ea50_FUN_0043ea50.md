# Raw capture: FUN_0043ea50

| Field | Value |
|---|---|
| **Stable ID** | `aa_0043ea50` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0043ea50` |
| **Canonical name** | `FUN_0043ea50` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_0043ea50(undefined4 *param_1,undefined1 *param_2)

{
  undefined4 *in_EAX;
  
  *in_EAX = *param_1;
  *(undefined1 *)(in_EAX + 1) = *param_2;
  return;
}
```

---

## Dual A/B re-verify (W32-M, 2026-07-29)

**Tools:** Ghidra `decompile_function` + `read_memory` + `get_function_by_address` + `analyze_function_complete` + callers/xrefs. **No** `disassemble_bytes`. No Launcher.

### Live decompile (re-confirm)

```c
void __thiscall FUN_0043ea50(undefined4 *param_1,undefined1 *param_2)

{
  undefined4 *in_EAX;
  
  *in_EAX = *param_1;
  *(undefined1 *)(in_EAX + 1) = *param_2;
  return;
}
```

### Function bounds

| Field | Value |
|---|---|
| Entry | `0x0043ea50` |
| Body | `0x0043ea50`–`0x0043ea5f` (**16 B** / `0x10`) |
| Ghidra name | `FUN_0043ea50` |
| Classification | **leaf** |
| Next | `FUN_0043ea60` (`GuardedVector_GrowBlockMap_Stride2`, W30-A) immediately after body (no INT3 between last `RET 4` and next entry) |

### Byte seal (`read_memory` @ `0x0043ea50`, 20 B)

```
hex: 8b118b4c240489108a11885004c20400 8b430883...

8B 11           MOV  EDX, [ECX]         ; *pU32
8B 4C 24 04     MOV  ECX, [ESP+4]       ; pU8 (stack formal)
89 10           MOV  [EAX], EDX         ; dst.u32 = *pU32
8A 11           MOV  DL, [ECX]          ; *pU8
88 50 04        MOV  [EAX+4], DL        ; dst.u8 @ +4
C2 04 00        RET  4                  ; stdcall 1 stack arg
```

Preceding pad at `0x0043ea4e`–`0x0043ea4f`: `CC CC` (after `FUN_0043e9e0` epilogue).

### Sealed ABI / algorithm

| Claim | Evidence |
|---|---|
| **EAX = dst** (not null-checked) | stores `[EAX]` / `[EAX+4]`; no `TEST EAX` |
| **ECX = const uint32_t*** | first load `[ECX]` before ECX is overwritten |
| **Stack `[ESP+4] = const uint8_t*** | `MOV ECX,[ESP+4]` then byte load |
| **`RET 0x4`** | `C2 04 00` |
| Write width **5 B** (u32 + u8) into 8-byte slot layout | +0 and +4 only; trailing 3 B unwritten |
| **Not** contiguous-POD twin of `0043ecf0` | split sources vs single `src*` + null check |
| Leaf | no callees |
| Decompiler `__thiscall` label | partial (ECX formal); **misses EAX dest** — **bytes win** |

### Callers / xrefs

| Kind | Value |
|---|---|
| Callers (1) | `FUN_0043e8f0` (thin adapter; not owned) |
| Xref | `0x0043e8fb` UNCONDITIONAL_CALL |
| Adapter bytes @ `0x0043e8f0` | `LEA EAX,[ESP+8]; PUSH EAX; LEA ECX,[ESP+8]; MOV EAX,ESI; CALL 0043ea50; MOV EAX,ESI; RET` — stack values → split pointers; **ESI=dst** |

### Named role (sealed)

**`PodU32U8_StoreSplit`** — write dword from one pointer and byte from another into `dst` with **no** null gate. Distinct from null-safe contiguous `FUN_0043ecf0` (`Pod5_CopyIfNonNull`).
