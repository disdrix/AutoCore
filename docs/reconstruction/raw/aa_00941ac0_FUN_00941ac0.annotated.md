# Annotated low-level: Client_CastAfterPrompt (`FUN_00941ac0`)

| Field | Value |
|---|---|
| Stable ID | `aa_00941ac0` |
| VA | `0x00941ac0` |
| System | skills-abilities |
| Date | 2026-07-23 (scaffold); dual seal 2026-07-29 |
| Canonical name | `Client_CastAfterPrompt` |

## Machine-level notes

- Source: raw capture for `aa_00941ac0`.
- Prefer assembly when decompiler conflicts — here decomp `unaff_ESI` / `unaff_EDI` are **real** register formals (call sites set them).
- `Client_RequestCastSkill` epilogue `RET 0x18` (six stack dwords).
- This unit epilogue `RET 4` (aim pointer only).
- `DAT_00d1d900` = `&DAT_00d1a840 + 0x30c0` (pending skill absolute).
- `MOV ECX,[EDI+0x30c4]` before cast is a **dead stack store** — not an argument to RequestCast.

## Pseudocode (annotated; register formals recovered)

```c
// EDI = client*, ESI = int tfid[4]*, stack arg = aim*
void Client_CastAfterPrompt(void *pAim)
{
  char blocked;
  int skillId;

  blocked = Client_CastBlockFeedback(); /* EAX=EDI */
  if (blocked == '\0') {
    skillId = *(int *)(client /*EDI*/ + 0x30c0);
    Client_RequestCastSkill(
        skillId,
        ESI[0], ESI[1], ESI[2], ESI[3],
        pAim); /* EAX=EDI; callee RET 0x18 */
  }
  /* EAX = -1 */
  Client_PromptSkillTargetSelect(client /*EDI*/, /*show=*/0);
  return; /* RET 4 */
}
```

## Open questions (post-dual)

- Aim buffer product names (`DAT_00d1a640` / `DAT_00d1a660`).
- `FUN_00861680` product name.
- Ghidra symbol still `FUN_00941ac0` until rename.
