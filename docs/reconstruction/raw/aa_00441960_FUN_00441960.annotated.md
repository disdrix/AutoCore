# Annotated low-level: FUN_00441960 → StdTree_EraseRange_Isnil11_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00441960` |
| VA | `0x00441960`–`0x00441a11` inclusive (**178 B** / `0xB2`) |
| System | skills-abilities — STL tree range-erase (isnil@+0x11); gfxUIWindow shell@+0xA4 |
| Date | 2026-08-05 (MEGA-136 refresh; raw 2026-07-23) |
| Canonical name | `StdTree_EraseRange_Isnil11_Inferred` |
| Partition alias | `StdTree_EraseAll_ShellA4` |
| Scaffold retired | `Named_CalleeOf_Named_gfxUIWindow_00441960` |

## Machine-level notes

- **EDI** = MapShell* (not ECX thiscall). Decompiler `unaff_EDI` sealed by disasm + call sites (`MOV EDI,EBP` / `MOV EDI,EAX`).
- Stack: `outIt*`, `first`, `last`; both exits **`RET 0xC`**; **EAX** = `outIt*`.
- isnil@**+0x11**; color likely @+0x10; value @+0x0C (4 B) — product map English open.
- Full clear: `first==head->left && last==head` → free-subtree root via dualed `StdTree_FreeSubtree_Isnil11_Inferred` (`0x00440d20`), reset head extremities + size0, `*outIt=head->left`.
- Partial: classic successor (isnil11) then `FUN_004477a0(map, &stack_first, node)` single erase+rebalance (residual).
- Prefer assembly when decompiler drops erase args (`FUN_004477a0()` empty).

## Pseudocode (annotated)

```c
// EDI = MapShell* map { proxy@0, head@4, size@8 }
// stack: Node** outIt, Node* first, Node* last
// RET 0xC; returns outIt*
Node **StdTree_EraseRange_Isnil11_Inferred(Node **outIt, Node *first, Node *last)
{
  Node *head = map->head;                 // [EDI+4]

  // Full [begin,end) clear
  if (first == head->left && last == head) {
    StdTree_FreeSubtree_Isnil11_Inferred(map, head->parent); // CALL 00440d20
    head->parent = head;
    map->size = 0;
    head->left = head;
    head->right = head;
    *outIt = head->left;
    return outIt;                         // RET 0xC
  }

  while (first != last) {
    Node *node = first;
    // tree successor (isnil@+0x11): right-min or parent walk
    if (first->isnil == 0) {
      Node *r = first->right;
      if (r->isnil == 0) {
        first = r;
        Node *l = r->left;
        while (l->isnil == 0) { first = l; l = l->left; }
      } else {
        Node *p = first->parent;
        Node *cur = first;
        while (p->isnil == 0 && cur == p->right) {
          cur = p; p = p->parent;
        }
        first = p;                        // successor
      }
    }
    FUN_004477a0(map, /*out*/ &stack_first_slot, node); // residual single erase
  }
  *outIt = first;
  return outIt;                           // RET 0xC
}
```

## Call graph (OWN evidence only)

```text
gfxUIWindow_CompleteDtor (0x00759de0)  [dualed WQ9L-F]
  └─ shell @ +0xA4 residual: FUN_00441960 then operator_delete(head)

FUN_00440200  (EH / shell tidy)
  └─ FUN_00441960(begin,end) then operator_delete(head)

FUN_00441960  StdTree_EraseRange_Isnil11_Inferred  [OWN MEGA-136]
  ├─ full: StdTree_FreeSubtree_Isnil11_Inferred (0x00440d20)  [dualed MEGA-132]
  └─ partial: FUN_004477a0  erase+rebalance isnil11  [residual]
```

## Open questions

- Product demangle of map key/value (isnil11 / 0x14-class node).
- Dual seal of single-erase `FUN_004477a0`.
- Runtime / bit-exact / differential.
