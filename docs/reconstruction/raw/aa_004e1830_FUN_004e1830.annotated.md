# Annotated low-level: LockedList_TryAdvanceIterator_Loot_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004e1830` |
| VA | `0x004e1830` |
| Body | `0x004e1830`–`0x004e18dc` (**172 bytes**) |
| Canonical name | `LockedList_TryAdvanceIterator_Loot_Inferred` |
| Ghidra name | `FUN_004e1830` |
| System | combat-loot / locked list walk |
| Date | 2026-07-29 (W23-T dual seal; scaffold 2026-07-23) |
| Tools | `decompile_function` + `read_memory` only |

## Machine-level notes

- **ABI:** MSVC `__thiscall` — ECX = list host; stack: `int *pCursor`, `uint32_t *pOutPayload`; **`ret 8`**.
- **No SEH** (unlike twin `0x004022a0` which installs `LAB_009bc110`).
- **IAT:** `TryEnterCriticalSection` `@ [0x009c61a0]`; `LeaveCriticalSection` `@ [0x009c61b8]`.
- **Prologue (regs):** push ebx/ebp/esi/edi; `edi = pCursor`; `ebx = *pCursor`; `esi = this`.
- Prefer assembly when decompiler conflicts; body recovered from decompile + entry hex.

## Layout (list host + node)

| Off | Role |
|----:|------|
| `list+0x04` | `CRITICAL_SECTION` |
| `list+0x1C` | head node pointer |
| `list+0x28` | traversal-enabled flag (byte; must be non-zero) |
| `node+0x04` | payload dword written to `*pOutPayload` |
| `node+0x08` | next node pointer |

## Return codes

| Code | Meaning |
|-----:|---------|
| `0` | Node available: cursor advanced / seeded, payload written |
| `1` | End (or empty start): cursor/payload cleared or start-empty |
| `0x80070005` | Flag `+0x28==0` **or** `TryEnterCriticalSection` failed (`E_ACCESSDENIED`) |

## Pseudocode (annotated)

```c
// thiscall; ret 8
uint LockedList_TryAdvanceIterator_Loot_Inferred(
    int list, int *pCursor, uint32_t *pOutPayload)
{
  int cur = *pCursor;
  if (*(char *)(list + 0x28) != 0) {
    CRITICAL_SECTION *cs = (CRITICAL_SECTION *)(list + 4);
    if (TryEnterCriticalSection(cs) != 0) {
      if (*pCursor == 0) {
        // seed from head
        uint32_t payload = 0;
        if (*(int *)(list + 0x1c) != 0)
          payload = *(uint32_t *)(*(int *)(list + 0x1c) + 4);
        *pOutPayload = payload;
        *pCursor = *(int *)(list + 0x1c);
        LeaveCriticalSection(cs);
        return (uint)(*(int *)(list + 0x1c) == 0); // 1 if empty
      }
      if (*(int *)(cur + 8) != 0) {
        *pCursor = *(int *)(cur + 8);
        *pOutPayload = *(uint32_t *)(*(int *)(cur + 8) + 4);
        LeaveCriticalSection(cs);
        return 0;
      }
      *pCursor = 0;
      *pOutPayload = 0;
      LeaveCriticalSection(cs);
      return 1; // end
    }
  }
  return 0x80070005;
}
```

## Twin

| Unit | VA | Diff vs this |
|------|----|--------------|
| `List_IterateNext` / `LockedList_TryAdvanceIterator_Inferred` | `0x004022a0` | Same CF + offsets; **has SEH**; inventory/LevelUp callers |

## Call graph

| Dir | Symbol | VA |
|-----|--------|----|
| Caller | `KillLoot_RollTableItems_Inferred` | `0x0050aa80` |
| Caller | `FUN_0050a7d0` | `0x0050a7d0` |
| Caller | `FUN_004d5c10` | `0x004d5c10` |
| Callee | `TryEnterCriticalSection` / `LeaveCriticalSection` | Win32 IAT |

## Open questions

1. Product container type name (same `client::list` family as `0x004022a0`?).
2. Why second COMDAT/copy without SEH (TU split residual).
3. Runtime golden walk — open.
