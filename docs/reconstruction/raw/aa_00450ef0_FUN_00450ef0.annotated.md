# Annotated low-level: FUN_00450ef0

| Field | Value |
|---|---|
| Stable ID | `aa_00450ef0` |
| VA | `0x00450ef0` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_00450ef0`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
int __fastcall FUN_00450ef0(int param_1)

{
  int in_EAX;
  
  if (in_EAX == 4) {
    param_1 = param_1 / 3;
  }
  else {
    if ((in_EAX < 5) || (6 < in_EAX)) {
      vog_LogMessage("..\\palantir/graphics/gfxShadowVolume.h",0x61,3,"Unimplemented code reached");
      return 0;
    }
    param_1 = param_1 + -2;
  }
  if (param_1 < 2) {
    param_1 = 0;
  }
  return param_1;
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.
