# Annotated low-level: CVOGHBSkill_OnHit_ctor (FUN_00617ad0)

| Field | Value |
|---|---|
| Stable ID | `aa_00617ad0` |
| VA | `0x00617ad0`–`0x00617b33` (100 B / `0x64`) |
| System | `skills-abilities` |
| Date | 2026-08-05 (R11-012 dual seal) |
| Product name | `CVOGHBSkill_OnHit_ctor` |
| Name confidence | **Confirmed** class via RTTI; ctor role **Confirmed** (vtbl install after base) |

## Machine-level notes

- Source: raw capture `aa_00617ad0` + R11-012 live re-verify.
- `__thiscall`: ECX=`this` (ESI); 6 stack args; **`RET 0x18`**; EAX=`this`.
- Sole callee: `CVOGHBSkillBase_ctor` (`0x005788d0`) — parent dual sealed.
- After base: install OnHit vtbl `PTR_FUN_009d0f1c`.
- Skill-blob float at `+0x4c` vs `g_flZero` (`0x00a0f518`):
  - **≠ 0:** `this+0x6c4 = 1`; `this+0x6c0 = CVTTSS2SI(blob+0x4c)` (truncate toward 0).
  - **== 0 / unordered:** `this+0x6c4 = 0`; leave `+0x6c0` untouched (base tail zeros do not cover 0x6c0 — residual: field may be uninitialized when flag=0).
- Decompiler `param_1[0x1b0]` / `param_1+0x1b1` are dword-index views of byte offsets **+0x6c0** / **+0x6c4** (asm sealed).
- Callers often `operator_new(0x6d0)` then call this, then **overwrite** subclass vtbl — OnHit is an intermediate base for hit-family HB skills.
- Scaffold plate `Named_CalleeOf_Named_VOG_DEBUG_STOP_00617ad0` **retired** (RTTI supersedes chain-of-caller plate).

## Pseudocode (annotated)

```c
// CVOGHBSkill_OnHit* __thiscall
// CVOGHBSkill_OnHit_ctor(this, pSourceObj, pSkillBlob, arg4, pTarget, pTfid, arg7)
// ret 0x18; EAX = this
CVOGHBSkill_OnHit* __thiscall CVOGHBSkill_OnHit_ctor(
    CVOGHBSkill_OnHit* self,   // ECX
    void* pSourceObj,          // stack — TFID seed only (base)
    void* pSkillBlob,          // stack — skill runtime; float @ +0x4c
    uint32_t arg4,             // stack → base +0x688
    void* pTarget,             // stack — ValidateTarget + AttachOwner (base)
    uint32_t* pTfid,           // stack — 16B TFID (base)
    uint32_t arg7)             // stack → base +0x6a8
{
  CVOGHBSkillBase_ctor(self, pSourceObj, pSkillBlob, arg4, pTarget, pTfid, arg7);
  self->pVTable = &PTR_FUN_009d0f1c; // CVOGHBSkill_OnHit

  float f = *(float*)((char*)pSkillBlob + 0x4c);
  if (f != g_flZero) {
    *(uint8_t*)((char*)self + 0x6c4) = 1;          // enable
    *(int*)((char*)self + 0x6c0) = (int)f;         // CVTTSS2SI
    return self;
  }
  *(uint8_t*)((char*)self + 0x6c4) = 0;
  return self; // +0x6c0 not written on this path
}
```

## Layout (OnHit extension beyond CVOGHBSkillBase)

| Offset | Size | Role | Confidence |
|-------:|-----:|------|------------|
| `+0x00` | 4 | vtbl `PTR_FUN_009d0f1c` | **Confirmed** (RTTI) |
| `+0x24`… | `0x630` | skill blob copy (base) | Confirmed (parent) |
| `+0x6c0` | 4 | int from `skillBlob+0x4c` float (only if ≠0) | **Confirmed** store; product English **Open** |
| `+0x6c4` | 1 | enable flag for `+0x6c0` | **Confirmed** |
| size | `0x6d0` | factory `operator_new` | **High** (callers) |

## Open questions

- Product English for `skillBlob+0x4c` / `this+0x6c0` (Ghidra type overlay on a sibling factory labels period-like names — **not** adopted without string/use evidence).
- Whether flag=0 path leaves `+0x6c0` garbage vs zeroed by allocator (allocators vary).
- Subclass vtbls / OnHit virtual methods catalog (not OWN).
