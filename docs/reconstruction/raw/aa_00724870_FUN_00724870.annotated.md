# Annotated low-level: CSoundManager_ProbeSoundFileExists_Inferred (`FUN_00724870`)

| Field | Value |
|---|---|
| Stable ID | `aa_00724870` |
| VA | `0x00724870` |
| Body | `0x00724870`–`0x007249b0` (320 / `0x140` bytes) |
| Canonical name | `CSoundManager_ProbeSoundFileExists_Inferred` |
| Ghidra name | `FUN_00724870` |
| System | client audio / CSoundManager path probe |
| Date | 2026-07-29 (W21-M dual seal; prior scaffold 2026-07-23) |

## Machine-level notes

- **Customcc:** **EAX** = input path C string; stack = `(CSoundManager* this, char* outPath, int* outKind)`; **`RET 0x0C`** (stdcall 3 args). Return **AL** bool.
- Decompiler `param_1` / `in_EAX` split is real: path arrives in **EAX**; first stack arg is **this** (loaded into **ECX** for `FUN_00722790`).
- `FUN_00722790` appends **`.ogg`** or **`_lxx.ogg`** depending on `this+0x308`.
- FS probe gated by config singleton bit0 at `cfg+4` (`FUN_007b6a20` → `DAT_00d20c38`).
- Search roots: **`DAT_00d1e574` … `< 0x00d1e7a4`**, stride **`0x1c`** (20× MSVC string). Each trial: `root + basename(path)` via `FUN_007b6420`.
- Fallback: `FUN_007b66c0(cfg)` if bit1 set (basename → object lookup `vtbl+0x14`).
- Success always writes **`*outKind = 2`** when non-null; copies **normalized `local_208`** (input+extension), **not** the rooted `local_104` trial path.
- Parent `FUN_007249c0` uses this as existence gate before play/resolve (`"Sound file doesn't exist: %s"`).

## Recovered CF

```c
// customcc: EAX = path; stdcall stack this, outPath, outKind
bool CSoundManager_ProbeSoundFileExists_Inferred(
    CSoundManager *self /*stack*/,
    char *outPath /*opt*/,
    int *outKind /*opt*/,
    const char *path /*EAX*/)
{
  char local_208[0x103]; // + guard / adjacent local_104[260]
  char local_104[260];

  strcpy(local_208, path);
  Sound_AppendOggExtension(self, local_208); // FUN_00722790

  PathConfig *cfg = GetPathConfigSingleton(); // FUN_007b6a20
  if (cfg->flags & 1) {
    if (PathFileExistsA(local_208)) goto ok;
    const char *base = Basename(local_208); // FUN_007b6420
    for (StdString *r = g_searchRoots; r < g_searchRootsEnd; ++r) { // 0x1c stride
      strcpy(local_104, r->c_str());
      strcat(local_104, base);
      if (PathFileExistsA(local_104)) goto ok;
    }
  }
  if (!PathConfig_FallbackLookup(cfg, local_208)) // FUN_007b66c0
    return false;
ok:
  if (outPath) strcpy(outPath, local_208);
  if (outKind) *outKind = 2;
  return true;
}
```

## Pseudocode (authoritative decompile — register names noisy)

```c
undefined1 FUN_00724870(undefined4 param_1,int param_2,undefined4 *param_3)
{
  // … see raw aa_00724870_FUN_00724870.md …
  // in_EAX = path; param_1 = this (stack); param_2 = outPath; param_3 = outKind*
}
```

## Open questions

- Product name for probe helper and exact meaning of outKind **`2`**.
- Runtime fill of `DAT_00d1e574` root table (who populates).
- Whether rooted hit should ever rewrite `outPath` to full `local_104` (bytes say no).
- Dual of `FUN_00722790` / `FUN_007b66c0` / path-config object.
