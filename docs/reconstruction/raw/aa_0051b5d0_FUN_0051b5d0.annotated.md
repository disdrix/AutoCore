# Annotated low-level: FUN_0051b5d0 → StdTree_Min_Isnil29

| Field | Value |
|---|---|
| Stable ID | `aa_0051b5d0` |
| VA | `0x0051b5d0`–`0x0051b5ea` |
| System | MSVC `std::_Tree` / map (isnil @ +0x29) |
| Date | 2026-08-04 WQ9D-B dual seal |
| Proposed name | `StdTree_Min_Isnil29` |

## Machine-level notes

- **Role:** MSVC `_Tree_min` / leftmost non-nil node in subtree rooted at arg.
- **Node layout (this size class):** `+0x00` left, `+0x04` parent, `+0x08` right; color @ `+0x28` (`node[10]`); **isnil @ +0x29**.
- **ABI:** `__cdecl` (stack arg, bare `ret`); **EAX = result node\***.
- **Decompiler trap:** void + walk-into-nil is **wrong**; prefer bytes (see raw re-verify).
- **Twin pattern:** `StdTree_Min_Val12` @ `0x005adfa0` (isnil @ +0x19).
- **Consumers:** `Map_EraseNode` (`0x0051cb40`) and sibling erase helpers recompute `head->left` after unlink when successor subtree is non-nil.
- Peer rightmost: `FUN_00421b50` (called as `_Max` from same erase sites) — not OWN this partition.

## Pseudocode (byte-correct)

```c
// Ghidra FUN_0051b5d0 — corrected return
Node* StdTree_Min_Isnil29(Node *node)
{
  Node *left = node->left;           // *node
  if (left->isnil != 0)              // +0x29
    return node;
  do {
    node = left;
    left = node->left;
  } while (left->isnil == 0);
  return node;
}
```

## Raw decompiler (authoritative capture body; return type wrong)

```c
void FUN_0051b5d0(int *param_1)
{
  char cVar1;
  param_1 = (int *)*param_1;
  cVar1 = *(char *)((int)param_1 + 0x29);
  while (cVar1 == '\0') {
    param_1 = (int *)*param_1;
    cVar1 = *(char *)((int)param_1 + 0x29);
  }
  return;
}
```

## Open questions

1. Product/PDB symbol (likely `_Tree_min` / `_Min` template instance).
2. Exact value payload size for this isnil@+0x29 node class (erase body uses color@+0x28).
3. Runtime / bit-exact open.
