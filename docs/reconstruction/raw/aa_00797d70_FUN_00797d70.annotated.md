# Annotated low-level: FUN_00797d70 / UiTextLayout_CompleteDtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00797d70` |
| VA | `0x00797d70`–`0x00797e17` (**168 B** / `0xA8`) |
| System | UI — owned text-layout block complete destructor |
| Date | 2026-08-05 (WQ9L-F dual seal) |
| Ghidra name | `FUN_00797d70` |
| Canonical name | `UiTextLayout_CompleteDtor_Inferred` |

---

## Machine-level notes

- **ABI:** `__stdcall` one arg — `ESI = [ESP+0x18]` after SEH/push frame; terminal **`RET 4`** (`C2 04 00` @ `0x00797e16`); void; does **not** free host.
- **Authority:** Decompile CF matches bytes once false-noreturn on `operator_delete` is corrected (body continues through `FUN_004271c0` + `~basic_string` + `RET 4`).
- **Role:** Complete destructor of a heap-owned **text layout** object used heavily by CNDUIWindow (`this+0x28C` / dword index `0xA3`): callers always do `FUN_00797d70(p); operator_delete(p);`.
- **Not** a window class dtor: no vtbl install; not thiscall; stack arg.
- **No product RTTI** on this helper → `_Inferred`. Reject scaffold `Named_CalleeOf_Named_VOG_DEBUG_STOP_*`.

## Offset map (obj = stack arg)

| Offset | Role |
|---|---|
| `+0x0C` | `std::basic_string<wchar_t>` — final dtor |
| `+0x80` | Vector of 0x80-stride elements — `FUN_004271c0` (EBX=`obj+0x80`) |
| `+0x94` / `+0x98` / `+0x9c` | Free buffer vector begin/end/cap — `operator_delete` begin + always-null |
| `+0xA4` | float base metric — accumulates `+0xA8` then keep |
| `+0xA8` | float delta metric — added into `+0xA4`, then zeroed |

## Control flow (annotated)

```
SEH LAB_009add5a; ESI = layout*
EH=2
  float@+0xA4 += float@+0xA8; float@+0xA8 = 0
  EBX=ESI; FUN_00797530(1)          // finalize layout (trim/flush)
  free vector @+0x94 (begin) + null triple
EH=0
  EBX = obj+0x80; FUN_004271c0()    // destroy run vector
EH=-1
  ~wstring(obj+0x0C)
SEH restore; RET 4
```

## Call pattern (CNDUIWindow)

```c
// from CNDUIWindow_CompleteDtor @ free of +0x28C:
if (void* p = self[0xA3]) {
  FUN_00797d70(p);   // this unit
  operator_delete(p);
}
self[0xA3] = 0;
```

## Open residual

- Product demangle / class name of layout block.
- Element type of 0x80-stride runs; precise meaning of float metrics.
- Runtime / bit-exact / differential open.
