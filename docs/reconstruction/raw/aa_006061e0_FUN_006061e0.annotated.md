# Annotated low-level: CVOGHBWakeupSkill_ctor (`FUN_006061e0`)

| Field | Value |
|---|---|
| Stable ID | `aa_006061e0` |
| VA | `0x006061e0`–`0x00606353` |
| System | `skills-abilities` / heartbeat |
| Date | 2026-07-29 W19-G OWN dual |
| Canonical | `CVOGHBWakeupSkill_ctor` (RTTI Confirmed) |

---

## Machine-level notes

- **ABI:** `__thiscall` — `this` in **ECX** (saved to EBP); **8** stack args (`ret 0x20`).
- **Object size (caller):** `operator_new(0x674)` at ApplyStatusEffectLocal / ApplyLocalById delay path.
- **Base:** `CVOGHBBase_ctor` then install derived vtbl `PTR_FUN_009de7b4`.
- **Skill embed:** dword-copy `0x18c` dwords (`0x630` B) from `pSkill` → `this+0x24` (`rep movsd`).
- **Vector of TFIDs:** MSVC-ish container base at `this+0x65c` with `_Myfirst@+4` / `_Mylast@+8` / `_Myend@+0xC` ⇒ absolute `+0x660` / `+0x664` / `+0x668`. Element size **16** (shift `>> 4`).
- **Grow vs in-place:** if begin null or `(cap-begin)/16 <= (end-begin)/16` → `FUN_004cbfc0` reallocate-insert; else `FUN_00608720` copy one TFID at end and advance end by `0x10`.
- **Sentinel terminator:** walk `param_6` while not invalid TFID (`lo!=-1 || hi!=-1 || byte2!=0`); after walk always append `DAT_009de7a0` invalid sentinel.
- Prefer assembly/`read_memory` when decompiler thiscall surface on `FUN_004cbfc0` drops ECX (container = `this+0x65c`).

---

## Field seed map (byte offsets)

| Offset | Decomp form | Role (sealed High / Confirmed) |
|-------:|---|---|
| `+0x00` | `*param_1` | vtbl → `PTR_FUN_009de7b4` |
| `+0x08` | `param_1[2]` | period / type short seed (`param_2`) |
| `+0x0C` | `param_1[3] = 1` | period sentinel seed |
| `+0x10` | `param_1[4] = 1` | period counter seed |
| `+0x18` | (base) | owner after `AttachOwnerObject` |
| `+0x24` … `+0x653` | `param_1+9` copy `0x18c` dwords | embedded skill runtime blob |
| `+0x654` | `param_1[0x195]` | world pointer (`param_5`) |
| `+0x658` | byte `param_1[0x196]` | skipBusy / flag low byte of `param_9` |
| `+0x65c` | `param_1+0x197` | TFID vector object base |
| `+0x660` | `param_1[0x198]` | vector begin (also first TFID slot for Fire) |
| `+0x664` | `param_1[0x199]` | vector end |
| `+0x668` | `param_1[0x19a]` | vector capacity end |
| `+0x66c` | `param_1[0x19b]` | position / pos arg (`param_7`) |
| `+0x670` | `param_1[0x19c]` | dice seed (`param_8`) |

Cross-unit: Fire dual `aa_00606180` reads `+0x24` skill, `+0x654` world, `+0x658` flag, `+0x660` tfid, `+0x66c` pos, `+0x670` seed — matches this seed map.

---

## Signature (semantic)

```c
// __thiscall  this = pre-allocated CVOGHBWakeupSkill* (0x674)
// stack (left→right, cdecl-like cleanup by callee ret 0x20):
//   param_2  skillTypeShort / period seed
//   param_3  host/owner object for AttachOwnerObject
//   param_4  skill runtime source (0x630 B blob)
//   param_5  world*
//   param_6  TFID list (16 B elems) terminated by invalid sentinel
//   param_7  pos*
//   param_8  random seed
//   param_9  flag (low byte → +0x658)
CVOGHBWakeupSkill* __thiscall CVOGHBWakeupSkill_ctor(
    CVOGHBWakeupSkill* this,
    uint32_t skillTypeOrPeriodSeed,
    void* hostObject,
    uint32_t* skillBlobSrc,
    void* world,
    int* tfidList,
    void* pos,
    uint32_t seed,
    uint32_t flagByteInLow);
```

---

## Pseudocode (annotated)

```c
// SEH: state -1 → 1 after field seed / before AttachOwner
CVOGHBBase_ctor(this);
*this = PTR_FUN_009de7b4;                    // CVOGHBWakeupSkill vtbl
memcpy(this+0x24, skillBlob, 0x630);         // 0x18c dwords
*(this+0x654) = world;
*(uint8_t*)(this+0x658) = (uint8_t)flag;
vector_tfid begin/end/cap = 0;               // +0x660..+0x668
*(this+0x66c) = pos;
*(this+0x0C) = 1; *(this+0x10) = 1;          // period seeds
*(this+0x670) = seed;
*(this+0x08) = skillTypeOrPeriodSeed;
CVOGHBBase_AttachOwnerObject(this, host);

// append each TFID until invalid
while (tfid not invalid) {
  if (vector full or unallocated) grow_insert(end, 1, *tfid);
  else { copy16(end, tfid); end += 0x10; }
  tfid += 4; // next 16 B
}
// always append DAT_009de7a0 invalid sentinel
return this;
```

---

## Open questions

1. Product English for `param_2` / `param_9` beyond Fire dual use of `+0x658` as skipBusy.
2. Whether `FUN_004cbfc0` thiscall surface should be renamed vector-insert (sibling unit).
3. Runtime / bit-exact image diff.
