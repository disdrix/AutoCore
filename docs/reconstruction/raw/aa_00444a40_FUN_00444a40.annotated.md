# Annotated low-level: Sto_UnserialFactoryMapB_InsertCreator_Inferred (`FUN_00444a40`)

| Field | Value |
|---|---|
| Stable ID | `aa_00444a40` |
| VA | `0x00444a40`–`0x00444ac5` (**134 B** / `0x86`) |
| System | arda2/storage — stoAbstractUnserializationFactory map B insert |
| Date | 2026-07-23 scaffold; **W36-C seal 2026-07-29** |

## Machine-level notes

- **Role:** Insert / bind a **creator function** into NestedHash **map B** (`FUN_00438ca0` → `DAT_00d1fcec`) keyed by a **FourCC tag**.
- **ABI:** cdecl stack **tag** (`uint32_t`); **EBX = creator fn ptr** (register formal). Void return. Plain `RET` after `ADD ESP,0x10`.
- **Flow:**
  1. `sentinel = *(MapB_Get()+8)`
  2. `find(tag)` — if node ≠ sentinel → `vog_LogMessage` duplicate (header line `0x24`, severity `2`)
  3. `find(tag)` again — if node == sentinel → `insert({tag,0})` via `FUN_0046bf90` (EDI=bag)
  4. Always `*(node+0xC) = EBX` (bind / rebind creator)
- **Strings (sealed):** `..\\arda2/storage/stoAbstractUnserializationFactory.h`; `"Inserting duplicate creator tag to factory"`.
- **Callers:** thin wrappers `FUN_00444910` (`DSDL`/`0x4C445344`), `FUN_00444930` (`AADL`/`0x4C444141`).
- **Twin of** MapA insert `FUN_004449b0` (same body shape; MapA uses `FUN_00438c40`).
- **Reject** scaffold `Named_CalleeOf_*_Palantir_*`. Prefer structural name over bare registry `Sto_AbstractUnserializationFactory_00444a40`.

## Pseudocode (annotated)

```c
// EBX = creator_fn* (register formal); stack arg = FourCC tag
void Sto_UnserialFactoryMapB_InsertCreator_Inferred(uint32_t tag /* stack */)
{
  NestedHash* bag;
  void* sentinel;
  void* node;
  // pair for insert: {tag, 0}

  bag = Sto_UnserialFactoryMapB_GetInstance_Inferred(); // FUN_00438ca0
  sentinel = bag->sentinel;                             // +0x08

  bag = Sto_UnserialFactoryMapB_GetInstance_Inferred();
  node = NestedHash_Find(bag, &tag);                    // FUN_0046c1b0
  if (node != sentinel) {
    vog_LogMessage("..\\arda2/storage/stoAbstractUnserializationFactory.h",
                   0x24, 2, "Inserting duplicate creator tag to factory");
  }

  bag = Sto_UnserialFactoryMapB_GetInstance_Inferred();
  node = NestedHash_Find(bag, &tag);
  if (node == sentinel) {
    // insert key pair {tag, 0}; node = inserted
    node = NestedHash_InsertIfMissing(bag, tag, 0);     // FUN_0046bf90
  }
  *(void**)((uint8_t*)node + 0x0C) = (void*)creator_fn_EBX;
}
```

## Sealed offsets / callees

| Symbol | Role |
|---|---|
| `FUN_00438ca0` | MapB GetInstance (W35-D) |
| `FUN_0046c1b0` | NestedHash find (W36-B) |
| `FUN_0046bf90` | NestedHash insert (W36-B) |
| `node+0x0C` | creator function pointer slot |
| `bag+0x08` | sentinel |

## Open gaps

- Creator signature behind EBX.
- Product map-B tag domain catalog beyond DSDL/AADL wrappers.
- Unowned find/insert bodies.
- Runtime / bit-exact / differential.
