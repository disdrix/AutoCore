# Annotated low-level: RbTree_AllocEmptyNode_Inferred (`FUN_00439770`)

| Field | Value |
|---|---|
| Stable ID | `aa_00439770` |
| VA | `0x00439770` |
| Body | `0x00439770`–`0x004397a6` |
| System | containers / std::map-like RB tree |
| Date | 2026-07-29 (W18-S dual seal) |
| Tools | `decompile_function` + `read_memory` (no `disassemble_bytes`) |

---

## Purpose

Allocate and zero-init a **0x18-byte tree node** used as an **empty RB-tree sentinel / header seed**. Does **not** self-link; callers (map/list ctors) wire parent/left/right and set nil flag.

---

## Signature (sealed)

```c
// cdecl; returns node* in EAX (decompiler may show void)
void *FUN_00439770(void);
// alias: RbTree_AllocEmptyNode_Inferred
```

---

## Layout written by this unit

| Offset | Write | Role (inferred from callers) |
|---|---|---|
| `+0x00` | `0` | left link (caller may self-point) |
| `+0x04` | `0` | parent link |
| `+0x08` | `0` | right link |
| `+0x0C`..`+0x13` | **not written** | residual heap / unused in sentinel path |
| `+0x14` | `1` | color-like flag (seed 1) |
| `+0x15` | `0` | nil/header flag seed; callers often set to **1** |

---

## Annotated CF

```c
void *RbTree_AllocEmptyNode_Inferred(void)
{
  // push 0x18; operator_new; add esp,4
  uint32_t *node = (uint32_t *)operator_new(0x18);
  if (node != NULL) {
    node[0] = 0;           // +0x00
  }
  // decompiler also guards node+1 / node+2; asm lea/test always proceeds for non-null base
  if (node != NULL) {
    node[1] = 0;           // +0x04
    node[2] = 0;           // +0x08
    *((uint8_t *)node + 0x14) = 1;
    *((uint8_t *)node + 0x15) = 0;
  }
  return node;             // EAX; decompiler may omit
}
```

---

## Caller pattern (evidence, not this body)

`FUN_004e7450` (`__fastcall` map-header init):

1. `sentinel = FUN_00439770()`
2. `header+4 = sentinel`
3. `sentinel+0x15 = 1`
4. self-link `sentinel[0]=sentinel[1]=sentinel[2]=sentinel`
5. `header+8 = 0` (size)

Same factory used by FX catalog ctor, loot manager, character ctor trees, etc. (~25 xrefs).

---

## Open questions

1. Exact product/RTTI name of node type (MSVC `_Tree_node` variant vs custom).
2. Meaning of bytes `+0x0C`..`+0x13` for non-sentinel (value-bearing) nodes of this size.
3. Whether any caller relies on `+0x14==1` without rewriting it.
