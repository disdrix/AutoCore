# Raw capture: FUN_0096f510

| Field | Value |
|---|---|
| **Stable ID** | `aa_0096f510` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0096f510` |
| **Canonical name** | `FUN_0096f510` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0096f510(int *param_1)

{
  int *piVar1;
  
  param_1 = (int *)*param_1;
  if (param_1 != (int *)0x0) {
    piVar1 = param_1 + 1;
    *piVar1 = *piVar1 + -1;
    if (*piVar1 == 0) {
                    /* WARNING: Could not recover jumptable at 0x0096f51e. Too many branches */
                    /* WARNING: Treating indirect jump as call */
      (**(code **)(*param_1 + 8))();
      return;
    }
  }
  return;
}
```

---

## W37-H re-verify (2026-08-04)

| Field | Value |
|---|---|
| **Tool** | Ghidra `batch_decompile` / `decompile_function` + `read_memory` + callers/callees/xrefs. **No** `disassemble_bytes`. |
| **Body** | `0x0096f510`–`0x0096f522` exclusive (**18 B** / `0x12`); pad `CC` to peer ctor `0x0096f530` |
| **Hex** | `8b 09 85 c9 74 0b 83 41 04 ff 75 05 8b 01 ff 60 08 c3` |
| **CF vs raw 2026-07-23** | **Identical** decompile CF |
| **ABI** | **ECX=`handle*`** (load `*ecx`); bare **`C3`**; **void**. No stack formals. |
| **Semantics** | If `*handle == null` → return. Else **`--refcount` at payload+4**; if remaining ≠ 0 → return; if 0 → **tail `JMP [vtbl+8]`** (`FF 60 08`, not `CALL`) with payload in ECX. **Does not clear `*handle`**. Does not touch flag bytes +4/+5/+6 of the 8-byte handle shell. |
| **Decompiler note** | "jumptable" warning is false positive on `JMP [EAX+8]` Release tail-call. |
| **Peer ctor** | `RefCountedPtr3Flags_DefaultCtor_Inferred` @ `0x0096f530` (W35-G) — vector_ctor binds size **8**, ctor=`0096f530`, dtor=this (`FUN_00457720` DATA). |
| **Distinct from** | `FUN_0096efd0` (RefCountedPtr release: ref @+8, iface @+4, **clears** handle, SEH). |
| **Callers** | Heavy: SEH Unwind@* (many), `CVOGTacArc_dtor_helper`, `FUN_0048fc90`, `FUN_004c0fe0`×4, `FUN_00586c40`×4, ParticleFluid / graphics hosts, DATA dtor slots `@0045777b` / `@004577d0`, etc. |
| **Callees** | none direct (indirect Release only) |
| **Name** | `RefCountedPtr3Flags_Dtor_Inferred` |
| **Reject** | Scaffold `Named_CalleeOf_CVOGTacArc_dtor_helper_0096f510` |
