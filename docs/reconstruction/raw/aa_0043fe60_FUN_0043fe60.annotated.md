# Annotated low-level: FUN_0043fe60 → StdList_Incsize

| Field | Value |
|---|---|
| Stable ID | `aa_0043fe60` |
| VA | `0x0043fe60`–`0x0043fece` |
| System | shared MSVC `std::list` |
| Canonical | `StdList_Incsize` (**Inferred** — MSVC `_Incsize` shape + `"list<T> too long"`) |
| Date | 2026-07-23 scaffold; **W23-E seal 2026-07-29** |

## Machine-level notes

- **ABI:** `__fastcall` — **ECX** = count, **EDX** = list*. Plain `RET` (no stack cleanup).
- **Layout:** `list+8` = `_Mysize` (MSVC list header companion to `_Myhead` at `+4`).
- **Overflow:** if `(0x3fffffffU - size) < count` → construct `std::string("list<T> too long")` → `FUN_00401a40` → `_CxxThrowException` with `ThrowInfo` `DAT_00acc388`.
- **Happy path:** `_Mysize += count`.
- **Twin:** `FUN_00403600` is `__thiscall` same CF with max `0x6666666` (different list/node specialization) — not owned this dual.
- Live decompile **≡** raw body; entry/exit/constants sealed via `read_memory`.

## Pseudocode (annotated)

```c
// MSVC std::list::_Incsize(size_type count)
// ECX = count, EDX = list*
void __fastcall StdList_Incsize(uint32_t count, StdListHeader *list)
{
  // SEH registration (ExceptionList / LAB_009bc919) — frame only
  uint32_t size = list->_Mysize;          // [edx+8]
  if (0x3fffffffU - size < count) {
    // throw length_error / length exception with plate "list<T> too long"
    std::string msg("list<T> too long");  // @ 0x00a1581c
    FUN_00401a40(&msg);                   // promote to exception object
    _CxxThrowException(exc, &DAT_00acc388); // noreturn
  }
  list->_Mysize = size + count;
}
```

## Call graph (owned unit)

| Dir | Target | Role |
|---|---|---|
| callee | `FUN_00401a40` | exception object builder |
| callee | `_CxxThrowException` | MSVC throw |
| callee | `basic_string` ctor | message |
| caller | `StdList_PushPayloadNode` (`00480350`) | size++ after buy node |
| caller | Auth send / many list inserts | shared leaf |

## Open questions

- Exact C++ exception type identity beyond ThrowInfo pointer (length_error vs custom).
- Whether every caller always passes count==1 (most do; not proven universal).
- Product symbol string absent (STL inlined).
