# Raw capture: FUN_004975e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004975e0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004975e0` |
| **Canonical name** | `FUN_004975e0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_004975e0(void *param_1,byte param_2)

{
  FUN_004970b0();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```

---

## W29-F re-verify (2026-07-29)

**Tools:** live `decompile_function` + `analyze_function_complete` + `read_memory` (full body). **No** `disassemble_bytes`.

### Sealed facts

1. **Body:** `0x004975e0`–`0x004975fe` exclusive (**30 B** / `0x1E`). Epilogue `C2 04 00` (`RET 4`). Pad `CC CC`. Next fn starts `56 8B 74 24 08…` @ `0x004975fe`.
2. **ABI:** **`__thiscall`**; ECX = `this`; stack formal `flags` (accessed as `[esp+8]` after `push esi`); cleans **4** bytes; returns `this` in EAX.
3. **Semantics:** MSVC **scalar-deleting destructor** = **vtbl[0]** of `PTR_FUN_009c7a1c` (`*(uint32*)0x009c7a1c == 0x004975e0`):
   - `ESI = ECX`
   - `call FUN_004970b0` (complete-object body; W28-M **accept-with-gaps** as `Class_009c7a1c_CompleteDtor`)
   - `if (flags & 1) operator_delete(this)`
   - `return this; RET 4`
4. **False noreturn:** Ghidra marks `operator_delete` noreturn; bytes are normal `CALL` + `ADD ESP,4` + `MOV EAX,ESI` + `POP ESI` + `RET 4`.
5. **Xrefs:** 1 DATA from vtbl `0x009c7a1c` only (no code CALL sites in xref list — dispatched via vtbl).
6. **Callees:** `FUN_004970b0`, `operator_delete`.
7. **Live decompile ≡ scaffold raw CF** for all stages.

### Full-body hex (`read_memory`)

```
56 8B F1 E8 C8 FA FF FF F6 44 24 08 01 74 09 56 E8 2D 22 FF FF 83 C4 04 8B C6 5E C2 04 00
```

- `E8 C8 FA FF FF` from `0x004975e3` → `0x004970b0`
- `F6 44 24 08 01` / `74 09` — flags bit0 gate
- `C2 04 00` — ret 4
