# Raw capture: FUN_00409970

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409970` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409970` |
| **Canonical name** | `FUN_00409970` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00409970(void *param_1)

{
  if (*(char *)((int)param_1 + 0x2d) == '\0') {
    FUN_00409970(*(undefined4 *)((int)param_1 + 8));
    FUN_00409b80(param_1);
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return;
}
```

---

## WQ9J-I re-verify (2026-08-05)

| Field | Value |
|---|---|
| **Agent** | WQ9J-I OWN-ONLY |
| **Tools** | `batch_decompile` / `decompile_function` + `analyze_function_complete` + `read_memory` + `disassemble_function` + callers/xrefs + `get_assembly_context` |
| **No** | `disassemble_bytes`; Launcher; parent ledgers |
| **Live decompile** | ≡ scaffold (collapsed right-only + false noreturn) — **bytes win** |
| **Named** | `StdTree_FreeSubtree_Isnil2d_DestroyStr0C_Inferred` |

### Body / ABI (bytes authority)

- **Range:** `0x00409970`–`0x004099aa` inclusive last RET byte (**59 B** / `0x3B`); pad `CC` to `0x004099b0`.
- **ABI:** ECX = tree_base (threaded; unused in body except recursive pass); stack `node*`; **void**; **`RET 4`** (`C2 04 00`).
- **Full body hex:**
```
53 56 57 8B 7C 24 10 80 7F 2D 00 8B D9 8B F7 75 24
8B 46 08 50 8B CB E8 E4 FF FF FF 8B 36 57 E8 EC 01 00 00
57 E8 88 FE 07 00 83 C4 04 80 7E 2D 00 8B FE 74 DC
5F 5E 5B C2 04 00
```

### Semantics (bytes — decompiler incomplete)

```
if node.isnil@+0x2d: ret 4
loop:
  FreeSubtree(tree_base, node.right@+8)   // self, ECX=tree_base
  left = node.left@+0
  FUN_00409b80(node)                      // stdcall RET4; ~basic_string @ node+0x0c
  operator_delete(node)                   // cdecl; ADD ESP,4
  node = left
  if !node.isnil@+0x2d: continue
ret 4
```

- **Value destroy:** yes — same shared `FUN_00409b80` as `00409920` (string @ +0x0c only).
- CF twin of `00409920` except isnil offset **0x2d** vs **0x131** (and rel32 targets).
- Decompiler drops left-walk and marks delete noreturn — **false**.

### Callers / xrefs (2)

| Site | Parent | Role |
|---|---|---|
| `0x00409987` | self | recursive right free |
| `0x00407fb0` | `FUN_00407f90` | full-range clear: `MOV ECX,EDI`; push `head->parent`; free; head reset + size=0 |

### Callees

- self, `FUN_00409b80` (string value dtor @+0x0c, stdcall RET4), `operator_delete` (`0x00489822`).
