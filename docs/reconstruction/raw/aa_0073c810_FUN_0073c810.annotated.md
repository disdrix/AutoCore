# Annotated low-level: FUN_0073c810 → HostPtrTable12_Bootstrap_EDI_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0073c810` |
| VA | `0x0073c810`–`0x0073cb6d` (**862 B** / `0x35E`) |
| System | host pointer-table bootstrap (graphics/palantir neighborhood) |
| Date | 2026-07-29 (W35-E dual seal) |
| Ghidra | `FUN_0073c810` |
| Named | `HostPtrTable12_Bootstrap_EDI_Inferred` |

---

## Machine-level notes

- **EDI register formal:** decompiler `unaff_EDI` is intentional — prologue saves ECX/EBX/EBP/ESI only; body writes `*EDI` / `EDI[n]` for n=0..11.
- **Return:** XOR EAX,EAX → **0** always (success code).
- **SEH:** frame `LAB_009b2f9d`; state dword ladder 0..10 around each `new`.
- **Pattern for slots 0–4,7–8:** `new(0xC4)` → null check → `FUN_00748960` (ECX=obj HostBase-ish) → dual vtbl overwrite → store to EDI[i] → typed init callee.
- **Slot 5:** smaller `0x44` host via `FUN_00457720` (vector of subobjects).
- **Slot 6:** `0x34` host with three vtbl pairs inline (no `FUN_00748960`).
- **Slot 9:** NestedHash0x10 shell + host tail bytes beyond 0x28 → `FUN_0098fd80`.
- **Slot 10:** NestedHashBag0x18 + extended tail → `FUN_0098df00` (W34-G sole NestedHashBag caller site).
- **Slot 11:** large `0x14C` via `FUN_00764030`; final `FUN_007647c0(EDI[8])` wires slot8 into last object path.
- **Callers:** Ghidra reports **zero** xrefs — gap (indirect call / unanalyzed).
- **Neighborhood:** `FUN_0073c610` is gfxToolFactory.cpp checkerboard path; `FUN_0073cb70` also EDI-host and `PalDiffMap.fx` — product host likely graphics factory, not sealed as English name without string in this body.

## Recovered contract

```c
// HostPtrTable12_Bootstrap_EDI_Inferred
// EDI = void** table (at least 12 pointer slots / 0x30 bytes)
// returns 0
uint32_t HostPtrTable12_Bootstrap_EDI_Inferred(void **table /* EDI */);
```

## Open questions

- Who loads EDI and calls this (0 xrefs).
- Product English for each dual-vtbl 0xC4 subclass.
- Full contracts of unowned init callees (`FUN_0099*`, `FUN_0073*`, `FUN_00764030`).
- Runtime / bit-exact.
