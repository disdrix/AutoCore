# Raw capture: FUN_005206d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_005206d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005206d0` |
| **Canonical name** | `FUN_005206d0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
/* WARNING: Globals starting with '_' overlap smaller symbols at the same address */

int FUN_005206d0(undefined4 param_1,int param_2)

{
  double dVar1;
  
  switch(param_1) {
  case 0:
  case 1:
    dVar1 = ceil((double)((float)param_2 * _DAT_009cdff4));
    return (int)ROUND(dVar1);
  case 2:
    dVar1 = ceil((double)((float)param_2 * _DAT_009cefcc));
    return (int)ROUND(dVar1);
  case 3:
    dVar1 = ceil((double)((float)param_2 * _DAT_009cefc8));
    return (int)ROUND(dVar1);
  case 4:
    dVar1 = ceil((double)((float)param_2 * _DAT_00aaa7c4));
    return (int)ROUND(dVar1);
  case 5:
    dVar1 = ceil((double)((float)param_2 * _DAT_009cefc4));
    return (int)ROUND(dVar1);
  default:
    return 0;
  }
}
```

---

## R12-019 re-verify (2026-08-05 OWN dual) — append only

**Agent:** R12-019 OWN-ONLY dual for VA `0x005206d0` only.  
**Tools:** Ghidra `decompile_function` + `analyze_function_complete` + `get_function_callers` / `get_function_xrefs` / `get_xrefs_to` + `read_memory`. **No** `disassemble_bytes`. No Launcher.  
**Partition:** `WAVE_2026-08-05_r12_residual_partition_map.md` → R12-019. Dual start 2646. System: inventory-transfer. Parent dual: `0x0052e1d0`. Terminal **false**.

### Live decompile ≡ raw CF

2026-08-05 live `decompile_function` matches 2026-07-23 raw body: switch on `param_1` (0..5 / default), `ceil((float)param_2 * inv_mult[case])` → int ROUND, default 0. Only callee `ceil`.

### Body bounds (`read_memory` + analyze disasm)

| Item | Value |
|---|---|
| Start | `0x005206d0` |
| Last insn | `RET 8` (`C2 08 00`) @ `0x00520827`–`0x00520829` |
| Inclusive end | `0x00520829` |
| Size | **346 B** / `0x15A` |
| Align | `MOV EDI,EDI` (`8B FF`) @ `0x0052082a` |
| Jump table | 6× dword @ `0x0052082c`–`0x00520843` |
| Pad | `CC` after table |

Jump table (LE dwords @ `0x0052082c`):

| Case | Target |
|---:|---|
| 0 | `0x005206ec` |
| 1 | `0x005206ec` (shared with 0) |
| 2 | `0x0052072a` |
| 3 | `0x00520768` |
| 4 | `0x005207a6` |
| 5 | `0x005207e4` |

Default path @ `0x00520822`: `XOR EAX,EAX` → frame restore → `RET 8`.

### Prologue / epilogue (`read_memory` @ `0x005206d0`)

```text
55                push ebp
8B EC             mov ebp, esp
83 E4 F8          and esp, -8
83 EC 08          sub esp, 8
8B 45 08          mov eax, [ebp+8]     ; tier / row-count selector
83 F8 05          cmp eax, 5
0F 87 3D 01 00 00 ja  default          ; → 0x00520822
FF 24 85 2C085200 jmp dword ptr [eax*4 + 0x0052082c]
```

Each case path (pattern):

```text
DB 45 0C          fild dword ptr [ebp+0x0c]  ; cost int
83 EC 08          sub esp, 8
D8 0D <imm32>     fmul dword ptr [inv_mult]
DD 1C 24          fstp qword ptr [esp]
FF 15 88659C00    call [ceil IAT @ 0x009c6588]
; FNSTCW / OR 0x0C / FLDCW / FISTP → EAX (ROUND)
8B E5 5D C2 08 00 mov esp,ebp / pop ebp / ret 8
```

### ABI seal

| Item | Value | Conf |
|---|---|---|
| Convention | **stdcall** two stack args (no ECX use) | **High** |
| Arg0 | `[EBP+8]` tier/row-count selector (0..5) | **High** |
| Arg1 | `[EBP+0xC]` signed cost int | **High** |
| Return | **EAX** int level | **High** |
| Cleanup | **`RET 8`** (`C2 08 00`) all exits | **High** |
| This | **none** (not thiscall) | **High** |

Ghidra analyze display `undefined FUN_005206d0(void)` is **incorrect**.

### Constants (`read_memory` LE float)

| VA | Hex | Float | = 1/mult | Cases |
|---|---|---:|---:|---|
| `0x009cdff4` | `2549923e` | **0.2857143** | 1/3.5 | 0, 1 |
| `0x009cefcc` | `6edbb63e` | **0.3571429** | 1/2.8 | 2 |
| `0x009cefc8` | `3ccff33e` | **0.4761905** | 1/2.1 | 3 |
| `0x00aaa7c4` | `6edb363f` | **0.7142857** | 1/1.4 | 4 |
| `0x009cefc4` | `6edbb63f` | **1.4285715** | 1/0.7 | 5 |

Inv mults are exact inverses of dualed capacity mults on `Character_GetReverseEngineerSkillCapacityByTier_Inferred` (`0x00520520`): `{3.5, 3.5, 2.8, 2.1, 1.4, 0.7}`.

### Formula (sealed)

```text
level = ceil( (float)cost * inv_mult[tier] )   // as int in EAX via FISTP ROUND
// tier > 5 or unknown → 0
// cases 0 and 1 share inv_mult = 1/3.5
```

### Callers / xrefs (live)

| Kind | Value |
|---|---|
| Callers | **only** `FUN_0052e1d0` (`Character_ComputeReverseEngineerRequiredLevel_Inferred`) |
| Xrefs | 4× `UNCONDITIONAL_CALL` @ `0x0052e5c6`, `0x0052e5d5`, `0x0052e5fb`, `0x0052e60a` |
| xref_count | **4** |
| Callees | `ceil` only |

### Parent call-site roles (evidence; parent dual R11-002)

From parent decompile (not re-dualled):

1. `FUN_005206d0(row_count, total_cost)` — aggregate secondary material cost after recipe walk; max into required level.
2. Loop tiers **1..5**: `FUN_005206d0(tier, row_cost[tier])` — per-tier cost→level; max into required level.
3. Parent also inlines `ceil(primary_cost * DAT_009cdff4)` for base (same inv as cases 0/1) without calling this VA.

### Name decision (R12-019)

**`ReverseEngineer_CeilCostToLevelByTier_Inferred`**

Evidence:

1. Sole caller is dualed RE required-level parent `Character_ComputeReverseEngineerRequiredLevel_Inferred` (`0x0052e1d0`).
2. Pure cost×inv_mult→ceil int; no character/item this.
3. Inv mult table mirrors dualed capacity helper `0x00520520` (skill×mult floor) as the **inverse scale** for level from cost.
4. Parent uses return as candidate for `max(...)` required RE skill level shown in tooltip `"Requires At Least %d Reverse Engineering"`.
5. No PDB / RTTI / in-body product strings → **`_Inferred`**.

**Retire:** `Named_CalleeOf_Named_CalleeOf_Skill_You_do_not_possess_the_Tink_005206d0` (tinkering-string scaffold from ancestor plate; wrong domain).

### Not claimed

Runtime Confirmed / bit-exact / differential / Launcher. Product English for tier index vs recipe-row ordinal. Parent dual already sealed R11-002 (not re-dualled).

### Integrity

Raw pseudocode body above the first `---` **unchanged**. This section is append-only re-verify.
