# Annotated low-level: FUN_0043c460 / CircularSentinel_Alloc0x14_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0043c460` |
| VA | `0x0043c460`–`0x0043c479` (**26 B** / `0x1A`) |
| System | shared circular-list / hash-bucket sentinel factory |
| Date | 2026-07-23 scaffold; W35-B dual 2026-07-29 |

## Machine-level notes

- `push 0x14` / `operator_new` / self-link next@+0 and prev@+4 / bare `ret`.
- Decompiler `void` is wrong — EAX returns node*.
- OOM residual: when EAX=0, `lea ecx,[eax+4]` yields 4 and second store may write address 4 (same MSVC shape as 0xC/0x28 twins).
- Payload bytes `+0x08..+0x13` unwritten by this factory.

## Control flow

```
node = operator_new(0x14)
if (node) node->next = node
// lea ecx, node+4; if (ecx) *ecx = node   // prev = self
return node   // EAX
```

## Callers

| Caller | Use |
|---|---|
| `NestedHash_Ctor_Sentinel0x14` (`0x0043bdf0`) | head @ bag+0x08 |
| `HeadBag_Ctor_Sentinel0x14` (`0x004642d0`) | head @ bag+0x08 |
| `FUN_005e2670` | temporary circular list head |
| `FUN_007c77b0` | temporary circular list head (loot weights path) |

## Open gaps

- Product payload English for remaining 0x0C bytes of the 0x14 node.
- Runtime / bit-exact / differential.
