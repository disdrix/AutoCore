# Annotated low-level: FUN_004040f0 → StdList_InsertOne_BeforeNode_Dword_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004040f0` |
| VA | `0x004040f0`–`0x00404122` inclusive (**51 B** / `0x33`) |
| System | shared MSVC `std::list` (partition hint: missions-progression) |
| Date | 2026-07-23 scaffold; **annotated refresh 2026-08-05 R10-005** |
| Canonical name | `StdList_InsertOne_BeforeNode_Dword_Inferred` |
| Ghidra name | `FUN_004040f0` |
| Dual | A/B **accept-with-gaps** (R10-005) |

## Machine-level notes

- Source: raw capture + live Ghidra re-verify (`decompile` / `disassemble_function` / `read_memory` / xrefs).
- Decompiler **elides** list `this` (ECX) and prints `FUN_00404840(1)` without ECX — **assembly is authoritative** for ABI.
- Prefer assembly when decompiler conflicts (this unit: ABI only; CF matches).
- Callees dualed elsewhere: BuyNode `0x00418700` (MEGA-081), InsertN parent `0x004034c0` (MEGA-134). Incsize `0x00404840` residual.

## ABI (sealed)

```text
__thiscall
  ECX       = StdListDword* list
  [ESP+4]   = ListNodeDword* where   // insert BEFORE this node
  [ESP+8]   = const uint32_t* val    // BuyNode stores *val
  return    = void
  cleanup   = RET 0x8
```

## Pseudocode (annotated; asm-corrected this + Incsize)

```c
// Insert one dword element immediately before `where`.
// node layout 0xC: Next@0, Prev@4, Myval@8
void __thiscall StdList_InsertOne_BeforeNode_Dword_Inferred(
    StdListDword *list /*ECX*/,
    ListNodeDword *where,
    const uint32_t *val)
{
  // BuyNode(link0=where, link1=where->prev, pValue=val)
  //   → node->next = where; node->prev = where->prev; node->val = *val
  ListNodeDword *node =
      (ListNodeDword *)FUN_00418700(where, where->prev, val);  // StdList_BuyNode_Dword_Seh

  FUN_00404840(list, 1);   // _Incsize(1); throws "list<T> too long" if size would exceed 0x3fffffff

  where->prev = node;
  node->prev->next = node; // splice into circular list
}
```

## Control-flow stages

| Stage | Bytes / evidence | Conf |
|---|---|---|
| Capture val*, where*, list | `MOV EAX,[ESP+8]` / `MOV EBX,[ESP+8]` / `MOV EDI,ECX` | **Confirmed** |
| BuyNode(where, where->prev, val) | `PUSH`×3 + `CALL 00418700` | **Confirmed** |
| Incsize(list, 1) | `PUSH 1; MOV ECX,EDI; CALL 00404840` | **Confirmed** |
| where->prev = node | `MOV [EBX+4],ESI` | **Confirmed** |
| node->prev->next = node | `MOV EDX,[ESI+4]; MOV [EDX],ESI` | **Confirmed** |
| RET 8 | `C2 08 00` | **Confirmed** |

## Callers

| Caller | Site | Role |
|---|---|---|
| `FUN_004034c0` `StdList_InsertN_BeforeNode_Dword_Inferred` | `0x004034fc` | N× insert-one (resize grow) |
| `FUN_00404560` | `0x004045a0` | insert-range from iterator pair; val = `src_node+8` |
| orphan `0x004cd020` | CALL only | no Ghidra function; thin wrapper fragment |

## Relation to push_back twin

`StdList_PushBack_Dword_Inferred` @ `0x00402d10` is the same buy+inc+splice with **where fixed to list->_Myhead** (insert before sentinel = append). This unit is the **general insert-before-node** form.

## Open questions

1. Exact CRT demangle spelling (`std::list<T>::_Insert` / `_Buynode` wrapper) — `_Inferred`.
2. Product element `T` English beyond dword payload (shared multi-domain list).
3. Null-new safety: BuyNode may return 0; body still rewires (same residual as push_back dual).
4. Runtime / bit-exact / differential — open (no Launcher).
5. Dual seal of Incsize `0x00404840` — residual (string path already High via prior duals).
