# Annotated low-level: CVOGMap_SampleHeightfieldY (`FUN_004cd220`)

| Field | Value |
|---|---|
| Stable ID | `aa_004cd220` |
| VA | `0x004cd220` |
| System | `world-terrain` |
| Canonical name | `CVOGMap_SampleHeightfieldY` (**Probable**) |
| Date | 2026-07-23 scaffold; **2026-07-29** dual A/B strengthen |

## Machine-level notes

- Source: raw capture for `aa_004cd220` + live body `read_memory` 2026-07-29.
- Prefer **body bytes** when decompiler drops stack args (tail JMP to `FUN_005a58c0`).
- `map+0xe4e0` = heightfield* (same slot as `CVOGMap_CastTerrainHeight`).
- `g_flZero` @ `0x00a0f518` = 0.0f (null HF return).
- ABI: `__thiscall` ECX=map; stack `float x, float z`; **RET 8**; ST0 = Y.

## Body bytes (authoritative)

```
004cd220  8B 89 E0 E4 00 00   mov  ecx, [ecx+0xe4e0]   ; hf
004cd226  85 C9               test ecx, ecx
004cd228  74 05               jz   short return_zero
004cd22a  E9 91 86 0D 00      jmp  FUN_005a58c0         ; this=hf
004cd22f  D9 05 18 F5 A0 00   fld  dword ptr [g_flZero]
004cd235  C2 08 00            ret  8
```

## Pseudocode (annotated; ABI-corrected)

```c
/* Decompiler shows __fastcall(int) + FUN_005a58c0() — incomplete due to tail JMP.
   Body + callers: thiscall map, (x,z) stack, RET 8. */

float10 __thiscall CVOGMap_SampleHeightfieldY(int *map, float worldX, float worldZ)
{
  int *hf;

  hf = *(int **)((char *)map + 0xe4e0);
  if (hf != 0) {
    /* tail: ECX = hf; stack (x,z) preserved */
    return (float10)FUN_005a58c0(hf, worldX, worldZ);
  }
  return (float10)g_flZero;  /* 0.0f @ 0x00a0f518 */
}
```

## Call-site shapes

| Caller | Args passed | Role |
|---|---|---|
| `Client_Input_DriveControlTick` | `(fStack_88, fStack_80)` after transform | Camera/look ground Y |
| `CVOGCreature_FindTerrainHeight` | `(*worldPos, worldPos[2])` | Initial HF sample before cast/ray |
| Many others | world X/Z pairs | Shared map API |

## Dual reviews

- `reviews/A_aa_004cd220_CVOGMap_SampleHeightfieldY.md`
- `reviews/B_aa_004cd220_CVOGMap_SampleHeightfieldY.md`

## Open questions

- Exact product / PDB method name.
- Full dual for `FUN_005a58c0` (out of OWN scope for this unit).
- Runtime / differential verification.
