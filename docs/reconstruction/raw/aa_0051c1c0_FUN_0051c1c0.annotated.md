# Annotated low-level: FUN_0051c1c0 → Map_TreeInsertAndRebalance_Val12

| Field | Value |
|---|---|
| Stable ID | `aa_0051c1c0` |
| VA | `0x0051c1c0` |
| Body | `0x0051c1c0`–`0x0051c3aa` exclusive |
| System | std map/set red-black tree |
| Date | 2026-07-29 (W25-G seal; prior scaffold 2026-07-23) |
| Live tools | `decompile_function` + `read_memory` (no `disassemble_bytes`) |

## Machine-level notes

- **Role:** MSVC-style `std::_Tree::_Insert` always-insert + red-black rebalance for **Val12** nodes (`StdTree_Buynode_Val12` / `FUN_005ae220`, node size **0x1c**, color @ +0x18, isnil @ +0x19).
- **Not** a key walk — parent `Skill_CategoryCooldownMap_Insert` owns lower-bound / duplicate detection; this unit only allocates, links, rebalances, writes `*outIt`.
- **Max size:** `if (map->size > 0x15555553) throw length_error("map/set<T> too long")` — template-specific constant (≠ `0x1ffffffd` used by IntPtr SkillElementTypeMap insert).
- **Link cases:**
  1. `where == head` → first element: root = leftmost = rightmost = n
  2. `addLeft == 0` → `where->right = n`; maybe update rightmost
  3. else → `where->left = n`; maybe update leftmost
- **RB loop:** while parent color is red (0): uncle recolor **or** rotate (`FUN_00573170` / `FUN_00418c10` / inlined path with isnil guard). Exit: paint root black, `*outIt = n`.
- **ABI:** `__thiscall` ECX=map shell (`+0x04` head, `+0x08` size); 4 stack args; **`ret 0x10`**. Epilogue leaves EAX = outIt* (caller treats return as iterator holder).
- **Callees:** `FUN_005ae220`, `FUN_00573170`, `FUN_00418c10`, `basic_string` ctor, `exception::exception`, `_CxxThrowException`.
- **Callers:** only `Skill_CategoryCooldownMap_Insert` (2 sites) in this image.

## Pseudocode (annotated)

```c
// thiscall ECX = MapShell*
// stack: Node** outIt, char addLeft, Node* where, const Val12* value
// ret 0x10; *outIt = new node; EAX ≈ outIt
void __thiscall Map_TreeInsertAndRebalance_Val12(
    MapShell *map, Node **outIt, char addLeft, Node *where, const void *value)
{
  // SEH frame LAB_009a3502
  if (map->size > 0x15555553u) {
    // length_error "map/set<T> too long" @ 0x00a1530c → _CxxThrowException(..., DAT_00acc388)
    noreturn;
  }
  // Buynode: left=head, parent=where, right=head, *value (3 dwords), color=0 (red)
  Node *n = StdTree_Buynode_Val12(map->head, where, map->head, value, /*color*/ 0);
  map->size += 1;
  if (where == map->head) {
    map->head->parent = n;   // root
    map->head->left = n;     // leftmost
    map->head->right = n;    // rightmost
  } else if (addLeft == 0) {
    where->right = n;
    if (where == map->head->right) map->head->right = n;
  } else {
    where->left = n;
    if (where == map->head->left) map->head->left = n;
  }
  // RB fixup while parent is red (color@+0x18 == 0); recolor / rotate;
  // on exit paint root black and *outIt = n
  ...
}
```

## Open questions

- Product/MSVC demangle for the exact map type (sole caller is category-cooldown).
- Full product names for rotate helpers `FUN_00573170` / `FUN_00418c10` (out of ownership).
- Runtime OOM from buynode null (insert does not null-check return of `FUN_005ae220` in decompile).
- Bit-exact / differential verification.
