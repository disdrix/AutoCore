# Annotated low-level: FUN_007227e0 / Audio_LinearGainToMilliBels

| Field | Value |
|---|---|
| Stable ID | `aa_007227e0` |
| VA | `0x007227e0`–`0x00722821` |
| System | audio / interface sound |
| Date | 2026-07-29 (W18-M dual seal) |
| Inferred name | `Audio_LinearGainToMilliBels` |

## Machine-level notes

- Source: raw capture + dual seal append for `aa_007227e0`.
- **Prefer FPU bytes over decompiler** on the main path: `FLDLG2` + `FYL2X` + `FMUL −2000.0`, not linear `0.30103*(1/x)*k`.
- Clamps sealed: `x <= 0 → −10000.f`, `x >= 1 → 0.f`.
- `__stdcall` (`RET 4`); return in ST0.
- Callers often store result at sound-object `+0x1b0` via `FSTP`.

## Sealed control flow

```
if (x <= 0.0f) return -10000.0f;           // DAT_00aaa5d8
if (x >= 1.0f) return 0.0f;                // g_flZero
return (float)(2000.0 * log10(x));         // via log10(1/x)*(-2000)
```

## Pseudocode (decompiler — main path NOT bit-exact)

```c
/* WARNING: decompiler main expression is LINEAR and WRONG.
   Sealed: 2000*log10(x) with clamps above. */

float10 FUN_007227e0(float param_1)
{
  if (param_1 <= 0.0) {
    return (float10)DAT_00aaa5d8;   // -10000.f
  }
  if (g_flOne <= param_1) {
    return (float10)g_flZero;       // 0.f
  }
  /* DECOMPILER: 0.30103*(1/param)*(-2000) — DO NOT PORT */
  /* SEALED:     FLDLG2; FYL2X on (1/param); FMUL -2000.0 */
  return (float10)(2000.0 * log10((double)param_1));
}
```

## Open questions

- Studio symbol (name **INFERRED** from audio callers + millibel math).
- Whether any caller passes `x > 1` expecting non-zero (body forces 0).
- Exact product name of field `+0x1b0` on the sound voice/object.
