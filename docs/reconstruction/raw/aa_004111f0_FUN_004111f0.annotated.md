# Annotated low-level: FUN_004111f0 / MissionRuntimeStateBlob_Init

| Field | Value |
|---|---|
| Stable ID | `aa_004111f0` |
| VA | `0x004111f0`–`0x0041121b` |
| System | missions-progression (GiveMission path) |
| Date | 2026-07-23 scaffold; annotated dual 2026-07-29 |

## Machine-level notes

- **Leaf:** no callees, no globals, no branches.
- **Arg:** `param_1` in **ECX** (`undefined4*`); sole writer target.
- **Return:** decompiler shows `void`, but bytes `MOV EAX,ECX` then only memory stores → **EAX = pBlob** at `RET`. Sole caller (`CVOGReaction_GiveMission` @ `0x00532988`) assigns the return into the state-blob local after `operator_new(0x30)`.
- **Store surface (final state):**
  - `uint32` at `+0x00, +0x04, …, +0x28` (11 dwords) → `0xFFFFFFFF`
  - `uint8` at `+0x2C` → `0`
  - `+0x2D..+0x2F` **not** written (not a full `memset(0x30, 0xFF)`)
- **Asm store order:** byte `+0x2C`, dwords `+0x20/+0x24/+0x28`, then `+0x00..+0x1C`. Order irrelevant to final values.
- **Decompiler quirk:** `*(undefined1*)(param_1 + 0xb)` with `undefined4* param_1` means **byte offset `0x2C`**, not byte index 11.
- Parent (not this unit): optional later copy of **0xC dwords** from prior tree node `+0x18` over the blob; then `FUN_0053c660` binds blob on `char+0x530`.

## Bytes (`read_memory` @ `0x004111f0`)

```
8B C1          MOV  EAX, ECX
83 C9 FF       OR   ECX, 0xFFFFFFFF
C6 40 2C 00    MOV  BYTE PTR [EAX+0x2C], 0
89 48 20       MOV  [EAX+0x20], ECX
89 48 24       MOV  [EAX+0x24], ECX
89 48 28       MOV  [EAX+0x28], ECX
8B D0          MOV  EDX, EAX
89 0A          MOV  [EDX], ECX
89 4A 04       MOV  [EDX+04], ECX
89 4A 08       MOV  [EDX+08], ECX
89 4A 0C       MOV  [EDX+0C], ECX
89 4A 10       MOV  [EDX+10], ECX
89 4A 14       MOV  [EDX+14], ECX
89 4A 18       MOV  [EDX+18], ECX
89 4A 1C       MOV  [EDX+1C], ECX
C3             RET
```

Hex: `8bc183c9ffc6402c008948208948248948288bd0890a894a04894a08894a0c894a10894a14894a18894a1cc3`

## Pseudocode (annotated copy of raw)

```c
// __fastcall: ECX = pBlob; EAX return = pBlob (not shown by decompiler)
void __fastcall FUN_004111f0(undefined4 *param_1)  /* effectively returns param_1 in EAX */
{
  *(undefined1 *)(param_1 + 0xb) = 0;   // byte @ +0x2C
  param_1[8] = 0xffffffff;              // +0x20
  param_1[9] = 0xffffffff;              // +0x24
  param_1[10] = 0xffffffff;             // +0x28
  *param_1 = 0xffffffff;
  param_1[1] = 0xffffffff;
  param_1[2] = 0xffffffff;
  param_1[3] = 0xffffffff;
  param_1[4] = 0xffffffff;
  param_1[5] = 0xffffffff;
  param_1[6] = 0xffffffff;
  param_1[7] = 0xffffffff;
  return; // EAX still = original ECX
}
```

## Open questions

- Product English for each sentinel dword and byte `+0x2C` (blob consumers / `+0x530` path).
- Optional Ghidra retype to `uint32_t* __fastcall(uint32_t*)`.
