# Annotated low-level: Map_Tidy_FreeHead (`FUN_0051da30`)

| Field | Value |
|---|---|
| Stable ID | `aa_0051da30` |
| VA | `0x0051da30`–`0x0051da5d` inclusive (**46 B**) |
| Canonical name | `Map_Tidy_FreeHead` |
| Ghidra name | `FUN_0051da30` |
| System | STL / map tidy (isnil@+0x29 host of dualed `Map_EraseRange`) |
| Date | 2026-08-04 WQ9E-J OWN dual |

## Machine-level notes

- Source: raw capture + live 2026-08-04 re-verify (`decompile` + `read_memory`; **no** `disassemble_bytes`).
- **Same 46 B tidy template** as dualed `StdTree_Tidy_FreeHead_Bare_Val12` (`0x0040d6d0`): full-range erase → free sentinel → null head+size → bare ret.
- Ghidra decompile **false-noreturn** on `operator_delete` drops the sealed nulling epilogue — **bytes are authority**.
- Calls dualed **`Map_EraseRange`** (`0x0051c7c0`) with `(out, *head, head)` = full clear.
- Does **not** free the map facade object itself (caller may `operator_delete` the map after this).

## Pseudocode (annotated; bytes-complete)

```c
// ECX = map*; bare RET (C3); void
void __fastcall Map_Tidy_FreeHead(MapFacade* map)
{
  void* out_slot; // PUSH ECX at entry (unused value)
  TreeNode* head = map->head; // [map+4]

  // Full-range erase [begin, end) == [*head, head]
  Map_EraseRange(map, &out_slot, head->left /* *head */, head);

  operator_delete(head); // free sentinel only
  map->head = 0;         // bytes: MOV [ESI+4], EAX after XOR
  map->size = 0;         // bytes: MOV [ESI+8], EAX
  return;
}
```

## Callers

| Caller | Site | Role |
|---|---|---|
| `FUN_00518ec0` | `0x00518fd3` | object teardown; map object at `this+0x68` — tidy then outer `operator_delete(map)` |
| `Unwind@009a8e4c` | `0x009a8e4f` | SEH cleanup for same map host |

## Open / residual

- Product English for host class owning the map facade.
- Peer tidies `FUN_0051e150` (isnil15 / `Map_EraseRange_Isnil15`) and `FUN_0051e180` (`Map_EraseRange_B`) not OWN this pass.
- Runtime / bit-exact.
