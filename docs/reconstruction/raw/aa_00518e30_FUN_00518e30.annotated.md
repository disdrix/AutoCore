# Annotated low-level: FUN_00518e30 → Object_CreateOrRecreateSkillHash_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00518e30` |
| VA | `0x00518e30` |
| System | `skills-abilities` |
| Date | 2026-08-04 (WQ9R-J dual seal) |
| Canonical name | `Object_CreateOrRecreateSkillHash_Inferred` |

## Machine-level notes

- Body **142 B** (`0x00518e30`–`0x00518ebe` excl.). All paths end **`ret 4`** (`C2 04 00`); pad `CC`.
- SEH frame `LAB_009a3627`; ESI = owner `this`.
- Slot **`owner+0x70`** = skill-tree **CNDHash\*** (same offset as `Skill_EnsureLoadedInTree` / `Object_RefreshSkillHash_Inferred`).
- **Existing hash path:** `mov ecx, [esi+0x70]`; `push log2`; `call FUN_004cbdc0` — **recreate** hash (string family `"HashError:Recreate, already locked for traversal"`). ECX is the **hash**, not the owner.
- **Missing hash path:** `operator_new(0x34)` (CNDHash sizeof sealed across family); `FUN_0051dd60(new, log2)` installs vtbl `PTR_FUN_009ce1b8`, sets mask `1<<log2`, stores result at `owner+0x70`. OOM → store **0**.
- Stack formal is log2 **byte** (promoted); callers push imm **1** (`FUN_0051b550`) or **4** (orphan site `0x0052a2c5`).
- Classification: **worker** (hash factory / reset helper).

## Pseudocode (annotated)

```c
// Object_CreateOrRecreateSkillHash_Inferred
// Ensure owner+0x70 skill CNDHash exists; if present, recreate at new log2 size.
void __thiscall Object_CreateOrRecreateSkillHash_Inferred(void *owner /* ECX */, unsigned char log2)
{
  // SEH: LAB_009a3627
  void *hash = *(void **)((char *)owner + 0x70);
  if (hash != NULL) {
    // thiscall on HASH: destroy buckets, set log2, realloc table
    CNDHash_Recreate_SkillFamily(hash, log2);  // FUN_004cbdc0
    return;  // ret 4
  }

  void *block = operator_new(0x34);
  if (block != NULL) {
    // ctor returns this; stores vtbl, mask=1<<log2, log2 at +0x1c, etc.
    *(void **)((char *)owner + 0x70) = CNDHash_Ctor_SkillFamily(block, log2);  // FUN_0051dd60
  } else {
    *(void **)((char *)owner + 0x70) = NULL;
  }
  // ret 4
}
```

## Callees

| VA / name | Role |
|---|---|
| `0x004cbdc0` | Skill-family CNDHash **recreate** (destroy + set log2 + alloc buckets) |
| `0x0051dd60` | Skill-family CNDHash **ctor** (`sizeof 0x34`, vtbl `009ce1b8`) |
| `operator_new` | Allocate 0x34 |

## Callers / xrefs

| Site | Notes |
|---|---|
| `FUN_0051b550` / `Object_LoadCloneBaseSkillTrio_Inferred` @ `0x0051b556` | `push 1` then call — intentional wipe before default+bulk skill load |
| CODE `0x0052a2c5` | `push 4`; not owned by a Ghidra function symbol (orphan body region) |

## Open questions

- Product English / MSVC demangle for skill-hash CNDHash class (`PTR_FUN_009ce1b8`).
- Whether recreate preserves freelist policy identically to inventory CNDHash recreate duals (same string; different vtbl/helpers).
- Full owning function for orphan call site `0x0052a2c5`.
- Runtime / bit-exact / differential.
