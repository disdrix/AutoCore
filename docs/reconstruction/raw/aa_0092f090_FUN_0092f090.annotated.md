# Annotated low-level: MissionUI_ParkDriveAxes_Inferred (FUN_0092f090)

| Field | Value |
|---|---|
| Stable ID | `aa_0092f090` |
| VA | `0x0092f090`–`0x0092f0d1` (**66 B** / `0x42`) |
| Ghidra name | `FUN_0092f090` |
| Canonical name | `MissionUI_ParkDriveAxes_Inferred` |
| System | `input-drive-control` |
| Date | 2026-08-05 (MEGA-021 annotated refresh) |
| Prior | 2026-07-23 scaffold (system unknown; unaff_ESI open) |

---

## Machine-level notes

- Source: raw capture + MEGA-021 live re-verify (`disassemble_function` + `read_memory` + call-site context).
- Prefer assembly when decompiler conflicts — **no conflict** this pass (live ≡ raw CF).
- **ABI:** `ESI` = client host; no stack formals; bare `RET` (`C3`). All five call sites seed with `MOV ESI,EBP`.
- **Offsets:** `client+0xe98` → character*; `character+0x250` → vehicle entity* (ECX for dualed setters).
- **Callees (dualed elsewhere):**
  - `VehicleEntity_SetLongitudinalInput` `0x004f5650` — thr float @ entity `+0x614` (0xC7 gate)
  - `VehicleEntity_SetSteerInput` `0x004f5620` — steer float @ entity `+0x618` (0xC7 gate)
  - `VehicleEntity_SetHandbrake` `0x004f3620` — HB byte @ entity `+0x61c` (ungated)
- **Park constants:** thr=`0`, steer=`0`, HB=`1` (integer immediates; float 0.0 bit-pattern is 0).
- **No** `PushDriveAxes` here — only the three setters. Controllers see axes only if a later push path runs.
- After gate, each setter **reloads** `ESI+0xe98` / `+0x250` (no re-null test on thr/steer/HB path).

## Pseudocode (annotated)

```c
// Retail ABI: ESI = client host*; void; bare RET
void MissionUI_ParkDriveAxes_Inferred(void)
{
  // unaff_ESI is the register ABI (not phantom garbage)
  int client = /* ESI */;
  int character = *(int *)(client + 0xe98);
  if (character == 0)
    return;

  int vehicle = *(int *)(character + 0x250);
  if (vehicle == 0)
    return;

  // ECX = vehicle for each thiscall; stack arg cleaned by callee RET 4
  VehicleEntity_SetLongitudinalInput(/*this*/ vehicle, /*float*/ 0);
  VehicleEntity_SetSteerInput(/*this*/ vehicle, /*float*/ 0);
  VehicleEntity_SetHandbrake(/*this*/ vehicle, /*byte*/ 1);
}
```

## Call graph (this unit only)

```text
Client_ShowNpcMissionDialogUI  (0x009438f0)   ×2 sites @ 00943989, 00943b72
FUN_0093e450                       (0x0093e450)   ×3 sites @ 0093e6ce, 0093e745, 0093e76f
  └─ FUN_0092f090  MissionUI_ParkDriveAxes_Inferred  [OWN MEGA-021]
        ├─ VehicleEntity_SetLongitudinalInput(0)   [dualed aa_004f5650]
        ├─ VehicleEntity_SetSteerInput(0)          [dualed aa_004f5620]
        └─ VehicleEntity_SetHandbrake(1)           [dualed aa_004f3620]
```

## Role

Mission / NPC dialog UI **axis park**: freeze player drive inputs (neutral thr/steer + handbrake on) while mission dialog paths run. Shared helper of `Client_ShowNpcMissionDialogUI` and dialog-case dispatcher `FUN_0093e450`.

## Open questions (gaps)

- Product English class name for ESI host (`+0xe98` owner).
- Product English name for residual parent `FUN_0093e450`.
- Whether thr/steer 0 writes are suppressed under entity 0xC7 (setter gate — possible no-op thr/steer while HB still writes).
- Runtime / bit-exact / differential — open (Terminal false).
