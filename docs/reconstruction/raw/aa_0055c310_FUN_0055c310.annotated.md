# Annotated low-level: StdVector_InsertOne_RebindIt_Elem28

| Field | Value |
|---|---|
| Stable ID | `aa_0055c310` |
| VA | `0x0055c310`–`0x0055c383` exclusive (**115 B** / `0x73`) |
| Structural name | `StdVector_InsertOne_RebindIt_Elem28` |
| Ghidra name | `FUN_0055c310` |
| System | MSVC `vector` insert-one + rebind, POD elem **0x1c** |
| Date | 2026-08-04 (W37-AF) |

## Machine-level notes

- Source: raw capture + W37-AF `read_memory` / live decompile. **No** `disassemble_bytes`.
- **ABI:** `__thiscall` ECX=`vec*`; stack `(outIt*, where, value*)`; void; **`RET 0x0C`**.
- Layout: begin@`+4`, end@`+8`, capEnd@`+0xC` (cap used by push_back parent, not this body).
- Stride **0x1c**; divide magic **`0x92492493`**.
- Index snapshotted **before** insert; rebind uses **post-insert** `begin`.
- Engine: sealed `StdVector_InsertN_Elem28` (`0x0055be00`, W34-R) with **count=1**.
- **Twin CF, different ABI:** `StdVector_InsertOne_RebindIt_Elem28_ValueEdx_Inferred` (`0x00469e20`); RC twin `StdVector_InsertOne_RebindIt_RcElem28` (`0x004612d0`, EDI=vec); dword thiscall twin `StdVector_InsertOne_RebindIt_Thiscall_Inferred` (`0x0040dbf0`, stride 4).

## Pseudocode (annotated)

```c
// ECX = VectorElem28*; stack outIt*, where, value*; RET 0x0C; void
void __thiscall StdVector_InsertOne_RebindIt_Elem28(
    VectorElem28* vec,
    Elem28** outIt,
    Elem28* where,
    const Elem28* value)
{
  Elem28* begin = vec->begin;           // [ecx+4]
  uint32_t index = 0;

  if (begin != nullptr) {
    // size = (end - begin) / 0x1c  via magic 0x92492493
    if (((char*)vec->end - (char*)begin) / 0x1c != 0)
      index = ((char*)where - (char*)begin) / 0x1c;
  }

  StdVector_InsertN_Elem28(vec, where, /*count=*/1, value); // FUN_0055be00

  // rebind after possible realloc: *outIt = begin' + index * 0x1c
  *outIt = (Elem28*)((char*)vec->begin + index * 0x1c);
}
```

## Call graph (context)

| Role | VA | Dual? |
|---|---|---|
| Caller push_back | `FUN_0055c710` | open (unowned) |
| Insert-n engine | `0x0055be00` | **W34-R sealed** |
| Resize sibling | `0x0055c250` | owned by other wave agent (exclusive) |

## Open questions

- Product English for POD Elem28 in this host domain (same residual as W34-R).
- Runtime / bit-exact / differential — open.
- Parent push_back dual — residual.
