# Annotated low-level: FUN_00571010 → InventoryGrid_FindItemByCoid

| Field | Value |
|---|---|
| Stable ID | `aa_00571010` |
| VA | `0x00571010` |
| System | `inventory-transfer` |
| Date | 2026-07-29 (supersedes 2026-07-23 scaffold notes) |

## Machine-level notes

- Source: raw capture for `aa_00571010` + live re-decompile/read_memory 2026-07-29.
- Prefer assembly when decompiler conflicts.
- Sibling CBID finder is `0x005710c0` (`InventoryGrid_FindItemByCbid`) — different match key and arity.
- Dual reviews: `A_aa_00571010_InventoryGrid_FindItemByCoid.md`, `B_aa_00571010_InventoryGrid_FindItemByCoid.md`, residual `a_00571010.md`.

## Pseudocode (annotated copy of raw)

```c
// __thiscall InventoryGrid*; stack: coidLo, coidHi; ret 0x08
int __thiscall FUN_00571010(int grid /*this*/, int coidLo, int coidHi)
{
  int done;
  int item;           // local_8
  undefined4 iter;    // local_4

  iter = 0;
  // actually: lea esi,[ecx+0x2C]; TraversalLock(esi)
  FUN_004294f0();
  done = FUN_004022a0(&iter, &item);
  while (true) {
    if (done != 0) {
      // unlock grid+0x54 / LeaveCS(grid+0x30)
      if (*(char *)(grid + 0x54) != '\0') {
        *(undefined1 *)(grid + 0x54) = 0;
        LeaveCriticalSection((LPCRITICAL_SECTION)(grid + 0x30));
      }
      return 0;
    }
    // instance COID pair — NOT clonebase CBID
    if (((item != 0) && (*(int *)(item + 0x160) == coidLo)) &&
       (*(int *)(item + 0x164) == coidHi)) break;
    done = FUN_004022a0(&iter, &item);
  }
  if (*(char *)(grid + 0x54) != '\0') {
    *(undefined1 *)(grid + 0x54) = 0;
    LeaveCriticalSection((LPCRITICAL_SECTION)(grid + 0x30));
  }
  return item;
}
```

## Sealed offsets

| Offset | Role |
|--------|------|
| grid `+0x2C` | list subobject (TraversalLock this) |
| grid `+0x30` / `+0x54` | CS / held flag |
| item `+0x160` / `+0x164` | COID lo / hi match keys |

## Open questions

- Original PDB symbol name.
- Runtime golden / bit-exact image.
- Product label COID vs TFID for the 64-bit id pair (layout sealed either way).
