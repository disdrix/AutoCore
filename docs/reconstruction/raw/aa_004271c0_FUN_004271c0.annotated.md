# Annotated low-level: FUN_004271c0 / StdVector_DestroyRuns_0x80_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004271c0` |
| VA | `0x004271c0`–`0x00427204` inclusive (**69 B** / `0x45`) |
| System | skills-abilities / UI text-layout container (vector tidy) |
| Date | 2026-08-05 (MEGA-128 refresh) |
| Canonical | `StdVector_DestroyRuns_0x80_Inferred` (Ghidra `FUN_004271c0`) |

---

## Machine-level notes

- Source: raw capture + MEGA-128 live re-verify (`decompile_function`, `disassemble_function`, `read_memory`, callers/xrefs).
- **Bytes authority** over decompiler on post-`operator_delete` path (false noreturn).
- ABI: **EBX = vector shell\***; no stack args; bare **`RET`** (`C3`).
- Thunk: `0x00427040` `JMP` here.
- Sole real CALL: `UiTextLayout_CompleteDtor_Inferred` (`0x00797d70`) with `LEA EBX,[layout+0x80]`.

---

## Signature (sealed)

```c
// EBX = vector*; void; RET (C3)
// begin@+4, end@+8, cap@+0xC; element stride 0x80
void StdVector_DestroyRuns_0x80_Inferred(void /* EBX=vec* */);
```

---

## Pseudocode (bytes-corrected)

```c
// EBX = StdVectorShell* v  (begin/end/cap at +4/+8/+0xC)
void StdVector_DestroyRuns_0x80_Inferred(void)
{
  uint8_t* begin = *(uint8_t**)(EBX + 4);
  if (begin == 0) {
    *(uint32_t*)(EBX + 4) = 0;
    *(uint32_t*)(EBX + 8) = 0;
    *(uint32_t*)(EBX + 0xC) = 0;
    return; // POP ESI; RET
  }
  uint8_t* end = *(uint8_t**)(EBX + 8);
  for (uint8_t* p = begin; p != end; p += 0x80) {
    // ECX = p; FUN_00426f70: ADD ECX,0xC; JMP IAT ~basic_string<wchar_t>
    FUN_00426f70(/* ECX = p */);
  }
  operator_delete(*(void**)(EBX + 4));
  *(uint32_t*)(EBX + 4) = 0;
  *(uint32_t*)(EBX + 8) = 0;
  *(uint32_t*)(EBX + 0xC) = 0;
}
```

---

## Control flow

| Stage | Evidence | Conf |
|---|---|---|
| Load begin `[EBX+4]` | `MOV ESI,[EBX+4]` | **High** |
| Null → zero triple + RET | `JZ` to zero MOVs + `C3` | **High** |
| Load end `[EBX+8]` | `MOV EDI,[EBX+8]` | **High** |
| Loop `ECX=p; CALL 00426f70; p+=0x80` | disasm + hex | **High** |
| `operator_delete(begin)` cdecl | `PUSH; CALL 00489822; ADD ESP,4` | **High** |
| Always zero triple before RET | fall-through after `POP EDI` | **High** |
| Element `~wstring` @ +0x0C | callee `ADD ECX,0xC; JMP [IAT]` | **High** |

---

## Call graph (this VA only)

```
FUN_00797d70  UiTextLayout_CompleteDtor_Inferred  [dualed WQ9L-F]
  └─ LEA EBX,[layout+0x80]; CALL FUN_004271c0

thunk_FUN_004271c0 @ 0x00427040
  └─ JMP FUN_004271c0

FUN_004271c0  StdVector_DestroyRuns_0x80_Inferred  [OWN MEGA-128]
  ├─ FUN_00426f70  (~wstring trampoline at elem+0xC)  [unowned]
  └─ operator_delete
```

---

## Open questions / gaps

1. Product English for 0x80-byte run/glyph-row element (fields beyond `wstring@+0xC`).
2. Full dual of `FUN_00426f70` (tiny trampoline sealed; not OWN).
3. Runtime / bit-exact / differential.

---

## Rejected misreads

| Claim | Why rejected |
|---|---|
| thiscall ECX=vector | Only EBX carries shell; ECX is per-elem |
| `operator_delete` noreturn | Bytes continue to zero + RET |
| Free host layout object | Only frees vector buffer at shell+4 |
| Named_CalleeOf_VOG_DEBUG_STOP scaffold | Parent debug path only; not this body |
| Stride 4 / dword vector | `ADD ESI,0x80` sealed |
