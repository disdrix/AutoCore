# Annotated low-level: FUN_0051d0e0 / ObjectCsList_CompleteDtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051d0e0` |
| VA | `0x0051d0e0`–`0x0051d107` exclusive (**39 B**) |
| System | object core / CS-owned list teardown |
| Date | 2026-07-29 (W31-R) |
| Semantic name | `ObjectCsList_CompleteDtor_Inferred` |

## Machine-level notes

- Source: raw + live `read_memory` full body hex (W31-R). Prefer bytes if decompiler omits ECX reloads.
- `FUN_0051bc90` takes `this` in ECX; first call keeps ESI=ECX from prologue; second reloads `MOV ECX,ESI` after base-vtbl store.
- `DeleteCriticalSection` via IAT dword at `0x009c61f0`; arg = `this+4` (`ADD ESI,4` / `PUSH ESI`).
- Complete dtor only: no flags arg, no `operator_delete(this)`. Scalar path is sibling `FUN_0051d0c0` (vtbl `009ce154` slot0).

## Pseudocode (annotated)

```c
// __thiscall/__fastcall; ECX=list*; void; bare RET
// Complete dtor for Object SharedBase CS-list (sizeof 0x2C, core+0xB0).
void __fastcall ObjectCsList_CompleteDtor_Inferred(uint32_t *self)
{
  *self = (uint32_t)&PTR_FUN_009ce154;   // derived vtbl
  FUN_0051bc90(self);                     // drain nodes under CS
  *self = (uint32_t)&PTR_FUN_009ce07c;    // base vtbl
  FUN_0051bc90(self);                     // base-phase drain (usually empty)
  DeleteCriticalSection((LPCRITICAL_SECTION)(self + 1)); // CS @ +4
}
```

## Open questions

- Product / MSVC demangle for class behind `PTR_FUN_009ce154` / `009ce07c`.
- Whether second clear is pure MSVC base-phase boilerplate vs product-required.
- Runtime under concurrent TraversalLock during dtor — open.
