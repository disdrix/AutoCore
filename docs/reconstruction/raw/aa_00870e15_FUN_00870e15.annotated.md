# Annotated low-level: UI_Case_CloseBtn_9c40

| Field | Value |
|---|---|
| Stable ID | `aa_00870e15` |
| VA | `0x00870e15` |
| Ghidra name | `caseD_9c40` / `switchD_00870aaa::caseD_9c40` |
| Canonical name | `UI_Case_CloseBtn_9c40` |
| System | UI dialog message switch |
| Date | `2026-07-29` (dual A/B refine) |

---

## Machine-level notes

- Source: three-rep Ghidra decompile + `read_memory` of body + parent prologue at `0x00870a90`.
- Prefer assembly when decompiler shows `in_EAX` / `unaff_EDI` phantoms — those are **live** registers set by parent, not undefined.
- Case arm for **control id `0x9c40` (40000)** — retail close-button id family (trade / mission / this dialog share the constant).
- Callees: `FUN_007fca10` (generic leave/close host), `FUN_0087b500` (non-click message forward).

---

## Annotated CF (byte-faithful)

```c
// Parent (0x00870a90) already did:
//   EDI = this
//   EAX = msg
//   EDX = controlId   // this arm only when controlId == 0x9c40
//   [stack] msg, controlId still present; RET 8 cleans them

// Entry: 0x00870e15
if (msg == 8) {
    // click / activate on close control
    if (*(int*)(this + 0x50c) == -1) {
        // no current selection id → allow leave
        // EAX = this, EBX = &static_client@0x00d1a840
        FUN_007fca10();   // visible? → vtbl+0x440 class close / Flush path (peer dual)
    }
    // selection present (+0x50c != -1): swallow click, do not leave
    return 1;             // AL=1; pop EDI; ret 8
}

// non-click messages: forward to shared message helper
// ECX=this, stack: msg, controlId
return FUN_0087b500(this, msg, controlId);
```

---

## Field / constant map (this unit)

| Item | Value | Confidence |
|---|---|---|
| Control id | **`0x9c40`** (40000) | **High** — jumptable index 0 = `id - 0x9c40` |
| Message activate | **`8`** | **High** — `cmp eax, 8` |
| Selection gate | **`this+0x50c == -1`** | **High** — `cmp dword [edi+0x50c], -1` |
| Leave host | `FUN_007fca10` @ `0x007fca10` | **High** |
| Client static base for leave | **`0x00d1a840`** in EBX | **High** |
| Forward helper | `FUN_0087b500` @ `0x0087b500` | **High** |
| Epilogue | `pop edi; ret 8` both tails | **High** |

Sibling writer of `this+0x50c` (context, not owned): `FUN_00870680` stores `param_2` into `this[0x143]` (`+0x50c`) and treats **`-1`** as clear-selection.

---

## Open questions

- Product / RTTI class name for the dialog whose vtable slots `0x00870a90`.
- English meaning of selection id at `+0x50c` beyond “−1 = none” (match participant? entity TFID low?).
- Whether leave via `FUN_007fca10` always hits Flush for this class’s `+0x440` (class-dependent; host is generic).
