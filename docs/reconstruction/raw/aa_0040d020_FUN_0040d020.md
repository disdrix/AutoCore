# Raw capture: FUN_0040d020

| Field | Value |
|---|---|
| **Stable ID** | `aa_0040d020` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0040d020` |
| **Canonical name** | `FUN_0040d020` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
float10 FUN_0040d020(float param_1,float param_2,float param_3)

{
  return SQRT((float10)param_3 * (float10)param_3 +
              (float10)param_2 * (float10)param_2 + (float10)param_1 * (float10)param_1);
}
```

---

## MEGA-137 re-verify (2026-08-05) — live Ghidra

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-137** |
| **Tools** | `decompile_function`, `analyze_function_complete`, `disassemble_function`, `get_function_callers`, `get_function_xrefs`, `get_xrefs_to`, `read_memory`, `get_function_by_address` |
| **Program** | `autoassault.exe` (current) |
| **Integrity** | Original 2026-07-23 capture **preserved** above; this section append-only |

### Live decompile (≡ original raw)

```c
float10 FUN_0040d020(float param_1,float param_2,float param_3)

{
  return SQRT((float10)param_3 * (float10)param_3 +
              (float10)param_2 * (float10)param_2 + (float10)param_1 * (float10)param_1);
}
```

### Body / ABI (sealed)

| Fact | Evidence |
|---|---|
| Body | `0x0040d020`–`0x0040d03e` inclusive (**31 B** / `0x1F`); pad `CC` at `0x0040d03f` before `FUN_0040d040` |
| Classification | **leaf** (`analyze_function_complete`); callees **none** |
| ABI | **`__cdecl`**: three `float` stack args `@[esp+4]`, `@[esp+8]`, `@[esp+0xc]`; return in **ST0** (`float10` / x87); plain **`RET`** (not `RET n`) |
| Semantics | Euclidean **3-float length**: `sqrt(x*x + y*y + z*z)` via `FLD`/`FMUL`/`FADDP`/`FSQRT` |

### Instruction seal (`disassemble_function` + `read_memory`)

```text
0040d020  D9 44 24 04          FLD   dword [esp+0x4]     ; x
0040d024  D8 4C 24 04          FMUL  dword [esp+0x4]     ; x*x
0040d028  D9 44 24 08          FLD   dword [esp+0x8]     ; y
0040d02c  D8 4C 24 08          FMUL  dword [esp+0x8]     ; y*y
0040d030  DE C1                FADDP                     ; x*x + y*y
0040d032  D9 44 24 0C          FLD   dword [esp+0xc]     ; z
0040d036  D8 4C 24 0C          FMUL  dword [esp+0xc]     ; z*z
0040d03a  DE C1                FADDP                     ; + z*z
0040d03c  D9 FA                FSQRT
0040d03e  C3                   RET
0040d03f  CC                   INT3  ; pad (not in body)
```

Hex (body through `RET`):
`d9442404d84c2404d9442408d84c2408dec1d944240cd84c240cdec1d9fac3`

### Callers / xrefs (3 UNCONDITIONAL_CALL)

| Site | Function | Role (caller CF) |
|---|---|---|
| `0x009249c0` | `Client_InteractClickPickTarget` (`0x009247b0`) | `len = length(hit − camera_pos)`; then `* _DAT_00aaacbc` (0.015f scale) |
| `0x00924c76` | `Client_InteractClickPickTarget` (`0x009247b0`) | same pattern (second pick path) |
| `0x005bad63` | `FUN_005ba440` (`0x005ba440`) | path/segment distance: `length(p[2..4] − q[0x41..0x43])` |

### analyze_function_complete summary

- signature decompiler display `undefined FUN_0040d020(void)` is **wrong** (storage shows three stack floats; decompile shows three params + ST0 return)
- locals: `param_1` Stack[0x4]:4, `param_2` Stack[0x8]:4, `param_3` Stack[0xc]:4
- xref_count: **3**; callers: `FUN_005ba440`, `Client_InteractClickPickTarget`
- cyclomatic complexity **1**; single basic block

### Name (this dual)

`Math_Vec3Length_Inferred` — pure math leaf; product/PDB symbol unknown. Retire `Named_CalleeOf_Client_InteractClickPickTarget_0040d020` scaffold (parent is one of two callers, not exclusive).

### Gaps remaining

- Product English / PDB name
- Runtime / bit-exact / differential
- Full English of residual caller `FUN_005ba440` (out of OWN VA)
