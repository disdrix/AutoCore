# Annotated low-level: FUN_00431700 / BasicString_UninitializedFillN

| Field | Value |
|---|---|
| Stable ID | `aa_00431700` |
| VA | `0x00431700`–`0x0043178c` exclusive (**140 B** / `0x8C`) |
| System | MSVC-style `_Uninit_fill_n` / construct-n for `basic_string` (stride `0x1c`) |
| Date | 2026-07-23 scaffold; W34-H dual 2026-07-29 |

## Machine-level notes

- **cdecl** (bare `C3`). Callers clean **20 B** (`ADD ESP,0x14`) → five stack pushes at call sites.
- Body formals used: dest*, count, value* only. Extra alloc*/value* ignored (MSVC template rebind residue).
- Loop: count times → if dest≠0, IAT copy-ctor; dest += **0x1c**.
- SEH `LAB_009bc2c1`: on throw, dtor already-constructed range then `_CxxThrowException(0,0)`.
- Callers: `StringVec_PushBack` (`0x004301f0`), `StdUninitFillN_BasicString` (`0x00430390`).
- Reject scaffold `Named_CalleeOf_*VOG_DEBUG*`.

## Pseudocode (annotated)

```c
// cdecl; callers typically push 5 dwords; body uses 3
void BasicString_UninitializedFillN(
    BasicString_0x1c* dest,
    int count,
    const BasicString_0x1c* value
    /* , void* alloc_unused, const BasicString_0x1c* value_echo_unused */)
{
  // SEH LAB_009bc2c1
  for (; count != 0; --count) {
    if (dest != nullptr) {
      // IAT [0x009c62ec] → basic_string copy-ctor
      basic_string_copy_ctor(dest, value);
    }
    dest = (BasicString_0x1c*)((char*)dest + 0x1c);
  }
  // EH: dtor [start,cur) via IAT [0x009c62f4]; _CxxThrowException(0,0)
}
```

## Open questions

- Product demangle plate (`std::_Uninit_fill_n` / `_Construct_n` English).
- Exact role of unused 4th/5th formals at call sites (allocator rebind only?).
- Runtime / bit-exact / differential.
