# Annotated low-level: FUN_00626210 → CVOGHBActivatePlayer_OnEnd_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00626210` |
| VA | `0x00626210`–`0x0062623a` (**43 B** / `0x2B`; pad `CC` after) |
| System | heartbeat / timed-actions / player activate |
| Date | 2026-07-29 (W29-P dual seal) |
| Named | `CVOGHBActivatePlayer_OnEnd_Inferred` |

## Machine-level notes

- **ABI:** **`__thiscall`** ECX = activate-player HB (`0x2c`); **no stack args**; exit via **tail-jmp** to empty `FUN_005081f0` (`C3`). Ghidra surface `__fastcall` / `void` is ECX-only thiscall noise.
- **Install:** DATA vtbl `0x009e3168` slot **`+0x14`** (`0x009e317c`) — standard HB **OnEnd** slot (base `CVOGHBBase_Stop` / `EndOrDestroy` dispatch here).
- **Fields (ctor-sealed, W28-G):**
  - `this+0x24` = host object backref (`pHost`)
  - `this+0x28` = byte reentrancy / skip-host-notify flag (ctor **0**; host path may set **1** before calling EndOrDestroy)
- **CF:**
  1. if `host = *(this+0x24) != 0` **and** `*(this+0x28) == 0`:
     - `*(uint8_t*)(host + 0x6c8) = 1` — host “activate ended” flag
     - `(**(host->vtbl + 0x18))(1, 1)` — host method, two stack flags both **1**
  2. always **tail-jmp** empty `FUN_005081f0` (nop chain shared with other OnEnd units)
- **Does not:** set `this+0x28`, free self, Enqueue/Dequeue, touch period / dormant.
- **Sibling (not OWN):** host-side `FUN_00626160` gates on `host+0x6c4` HB + `host+0x6c8==0`, sets `HB+0x28=1`, then HB `vtbl+0x18`(1,1) — inverse of this notify.

## Pseudocode (annotated)

```c
// CVOGHBActivatePlayer_OnEnd_Inferred
// __thiscall ECX=this (activate-player HB)
// no stack args; void (tail to empty ret)
void CVOGHBActivatePlayer_OnEnd_Inferred(void *this)
{
  void *host = *(void **)((char *)this + 0x24);
  if (host != 0 && *(uint8_t *)((char *)this + 0x28) == 0) {
    *(uint8_t *)((char *)host + 0x6c8) = 1;
    // host vtbl+0x18(this=host, flag0=1, flag1=1)  — identity not OWN
    (**(void (__thiscall ***)(void *, int, int))host)[0x18 / 4](host, 1, 1);
  }
  // tail-jmp FUN_005081f0 — empty RET
}
```

## Byte seal (from `read_memory`)

```
00626210  56                    push esi
00626211  8B F1                 mov esi, ecx              ; this
00626213  8B 46 24              mov eax, [esi+0x24]       ; host
00626216  85 C0                 test eax, eax
00626218  74 19                 jz  skip_notify           ; → 00626233
0062621a  80 7E 28 00           cmp byte ptr [esi+0x28], 0
0062621e  75 13                 jnz skip_notify
00626220  C6 80 C8 06 00 00 01  mov byte ptr [eax+0x6c8], 1
00626227  8B 4E 24              mov ecx, [esi+0x24]       ; host → thiscall ECX
0062622a  8B 01                 mov eax, [ecx]            ; host vtbl
0062622c  6A 01                 push 1
0062622e  6A 01                 push 1
00626230  FF 50 18              call dword ptr [eax+0x18]
00626233  8B CE                 mov ecx, esi              ; this for tail
00626235  5E                    pop esi
00626236  E9 B5 1F EE FF        jmp FUN_005081f0          ; → 005081f0 (C3)
0062623b  CC…                   pad
```

## Open questions

- Product/PDB method spelling for OnEnd on this class.
- Exact English for host `vtbl+0x18` (args `(1,1)` sealed; body not OWN).
- Whether `host+0x6c8` is only activate-complete (sibling gates) vs broader object flag.
- Runtime / bit-exact — open.
