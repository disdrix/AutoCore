# Annotated low-level: StdTree_Buynode_Val12_EsiValue (`FUN_0046c6c0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0046c6c0` |
| VA | `0x0046c6c0` |
| Body | `0x0046c6c0`–`0x0046c702` exclusive (**66** B) |
| System | std red-black tree / Val12 node buy (ESI value) |
| Date | 2026-07-29 (W28-T dual seal) |

## Machine-level notes

- Source: raw capture for `aa_0046c6c0` + W28-T live re-verify.
- **Value pointer is ESI**, not a 5th stack arg — twin of `StdTree_Buynode_Val12` (`0x005ae220`) with alternate ABI.
- Prefer bytes when decompiler labels return as `void` (return is **EAX = node***).
- Callers must load ESI with `value*` before call (see `FUN_0044a8c0` insert path).

## Layout (Val12 node, size `0x1c`)

| Off | Field |
|----:|-------|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x0c | value[0] (from `*ESI`) |
| +0x10 | value[1] |
| +0x14 | value[2] |
| +0x18 | color (stack arg4) |
| +0x19 | isnil (**forced 0**) |

## Pseudocode (annotated)

```c
// stdcall; RET 0x10; EAX = node* | null
// ESI = const Val12* value  (caller-preserved contract)
StdTreeNode_Val12* StdTree_Buynode_Val12_EsiValue(
    StdTreeNode_Val12* left,     // [esp+4]
    StdTreeNode_Val12* parent,   // [esp+8]
    StdTreeNode_Val12* right,    // [esp+0xc]
    uint8_t color)               // [esp+0x10]
{
  StdTreeNode_Val12* n = operator_new(0x1c);
  if (n != nullptr) {
    n->left = left;
    n->parent = parent;
    n->right = right;
    n->value[0] = esi_value[0];
    n->value[1] = esi_value[1];
    n->value[2] = esi_value[2];
    n->color = color;
    n->isnil = 0;
  }
  return n; // EAX
}
```

## Call graph

| Direction | Targets |
|---|---|
| Callees | `operator_new` only |
| Callers | `FUN_0043c2e0`, `FUN_0044a8c0` (`Map_TreeInsertAndRebalance_Val12_EsiBuy_Inferred`, W27-P) |

## Twin note

Do **not** merge with `aa_005ae220` (`ret 0x14`, stack `value*`). Same layout; different buy ABI.

## Open

- Product/MSVC demangle per map instantiation.
- Runtime OOM path.
- Bit-exact / differential.
