# Annotated low-level: StdTree_Buynode_Isnil21_Node0x24_Inferred (FUN_005a4870)

| Field | Value |
|---|---|
| Stable ID | `aa_005a4870` |
| VA | `0x005a4870`–`0x005a48f5` inclusive (**134 B** / `0x86`) |
| System | MSVC `std::_Tree` node buy (isnil@+0x21 / node 0x24) |
| Date | 2026-08-05 R13-008 OWN dual (scaffold 2026-07-23) |
| Canonical | `StdTree_Buynode_Isnil21_Node0x24_Inferred` |

## Machine-level notes

- Source: raw + live `decompile_function` / `disassemble_function` / `read_memory`. Prefer bytes for ABI.
- **`__stdcall RET 0x14`** sealed (`C2 14 00`); five stack args: left, parent, right, value*, color.
- `operator_new(0x24)` then inlined construct:
  - `*node = left; +4=parent; +8=right`
  - `key = *value` → `+0x0C` (u32)
  - `FUN_005a42d0` thiscall: **ECX = node+0x10**, stack arg = `value+4` — vector-like copy of dword range; begin/end/cap land at **+0x14 / +0x18 / +0x1c** (callee writes offsets +4/+8/+0xC of the +0x10 object)
  - `color` → `+0x20`; `isnil=0` → `+0x21`
- OOM: skip construct; return null (`TEST ESI` / `JZ` then `MOV EAX,ESI`).
- Sole caller: always-insert `FUN_005a4950` (dualed R12-033 as `StdTree_InsertAndRebalance_Isnil21_Node0x24_Inferred`) @ `0x005a49e2` with `(head, where, head, value, color=0)`.
- Sibling erase `005a4590` frees heap @+0x14 and deletes node 0x24 — same family layout.
- Distinct from Val16/0x28 buynode `00408990` and Val24/isnil29 0x30 buynodes.
- Partition system **skills-abilities** is inventory label only; machine CF is shared STL helper.

## Pseudocode (annotated)

```c
// __stdcall RET 0x14; EAX = NodeIsnil21_0x24* | null
NodeIsnil21_0x24* StdTree_Buynode_Isnil21_Node0x24_Inferred(
    NodeIsnil21_0x24* left,     // [ebp+8]
    NodeIsnil21_0x24* parent,   // [ebp+0xc]
    NodeIsnil21_0x24* right,    // [ebp+0x10]
    const ValueU32Vec* value,   // [ebp+0x14] → key@0 + vector-like@+4
    uint8_t color)              // [ebp+0x18]
{
  // SEH frame (ExceptionList / LAB_009a6321)
  NodeIsnil21_0x24* node = (NodeIsnil21_0x24*)operator_new(0x24);
  if (node != nullptr) {
    node->left = left;
    node->parent = parent;
    node->right = right;
    node->key = value->key;                 // +0x0C
    // FUN_005a42d0 thiscall ECX=&node->payload (+0x10), src=&value->vec
    VecDword_CopyConstruct(&node->payload, &value->vec);
    node->color = color;                    // +0x20
    node->isnil = 0;                        // +0x21
  }
  return node;
}
```

## Open questions

- Product/PDB demangle for map value_type (u32 key + vector-like dword payload).
- Full dual of vector ctor `FUN_005a42d0` (not OWN).
- Runtime / bit-exact / differential.
