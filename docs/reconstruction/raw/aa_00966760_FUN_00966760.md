# Raw capture: FUN_00966760

| Field | Value |
|---|---|
| **Stable ID** | `aa_00966760` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00966760` |
| **Canonical name** | `FUN_00966760` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_00966760(void)

{
  int iVar1;
  int unaff_EDI;
  
  iVar1 = (**(code **)(*(int *)*DAT_00d1f044 + 0x1d8))
                    ((int *)*DAT_00d1f044,5,(undefined4 *)(unaff_EDI + 0x3d4));
  *(undefined1 *)(unaff_EDI + 0x3d8) = 0;
  if (iVar1 < 0) {
    *(undefined4 *)(unaff_EDI + 0x3d4) = 0;
  }
  return 0;
}
```

---

## W38-Z re-verify append (2026-08-04)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `batch_decompile` + `read_memory` + `analyze_function_complete` + `get_assembly_context` + parent `FUN_0075eff0` decompile |
| **Integrity** | Original raw body above preserved; this section is append-only |

### Live decompile

Matches original raw (unaff_EDI host; CreateQuery-shaped vcall; always return 0).

### Body bytes (47 B @ `0x00966760`–`0x0096678e` inclusive)

```
a144f0d1008b008b08568db7d4030000566a0550ff91d801000085c0c687d8030000007d06c7060000000033c05ec3
```

### Machine notes

- `a1 44 f0 d1 00` / `8b 00` → `IDirect3DDevice9* = **DAT_00d1f044` (wrapper first field; W35-R publish).
- `8d b7 d4 03 00 00` → out-arg `EDI+0x3d4`; parent sets `EDI = [device+0x79c]` (`8b bd 9c 07 00 00` @ `0x0075f30d`).
- `6a 05` / `ff 91 d8 01 00 00` → `IDirect3DDevice9::CreateQuery` vtbl **`+0x1d8`** (method index 118), type imm **5** = `D3DQUERYTYPE_RESOURCEMANAGER`.
- `c6 87 d8 03 00 00 00` → clear flag byte `host+0x3d8`.
- Fail path: `jge` skip / `mov dword ptr [esi],0` zero query out.
- Epilogue: `33 c0` / `5e` / `c3` — **always EAX=0**, plain **RET** (no stack formals).
- Pad `CC` then sibling release tail at `0x00966790` (Release query @ `+0x3d4` + null; not OWN).
- Parent product string: `"Failed to Reset() device on Stats Monitor"` (`gfxDevice.cpp` Device Reset). Parent `test eax / jge` failure path is **dead** given always-0 return.
- Role sealed: `GfxStatsMonitor_RecreateResourceManagerQuery_Inferred` (W38-Z dual A+B **accept-with-gaps**).
