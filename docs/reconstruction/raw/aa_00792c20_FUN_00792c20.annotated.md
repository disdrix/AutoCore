# Annotated low-level: CNDUIDialog_CompleteDtor (FUN_00792c20)

| Field | Value |
|---|---|
| Stable ID | `aa_00792c20` |
| VA | `0x00792c20`–`0x00792d12` (**243 B** true body; Ghidra DB truncated at `00792ceb`) |
| System | MSVC shared UI base complete dtor — `CNDUIDialog` / `PTR_FUN_00a98f44` |
| Date | 2026-08-05 (WQ9K-G re-annotate) |

## Machine-level notes

- **ECX = this** (`8B F9`); SEH `LAB_009b4054`; saves EBX/ESI/EDI.
- Install vtbl: `C7 07 44 8F A9 00` → `*this = 0x00a98f44`.
- EH = 1 for owned teardown phase.
- `CALL FUN_007917c0` — child-hash walk/release at `this+0x4c0` (`[0x130]`).
- Six null-safe scalar-deletes (`PUSH 1; CALL [vtbl+0]`):
  - `+0x4c0 [0x130]`, `+0x4c8 [0x132]`, `+0x4cc [0x133]`, `+0x4d0 [0x134]`, `+0x4d4 [0x135]`, `+0x4d8 [0x136]`
  - **Skips** `+0x4c4 [0x131]` (matches ctor flag slot; not a heap object here).
- `LEA ESI,[EDI+0x4dc]; CALL FUN_00415e90` — list/shell clear with ESI = list object.
- `PUSH [ESI+4]; CALL operator_delete; ADD ESP,4` — free **member** at `this+0x4e0`, then `MOV [ESI+4],0`.
  - Decompiler **false-noreturn** here: `operator_delete` **returns**; body continues.
- EH = −1; `MOV ECX,EDI; CALL FUN_007b5be0` — deeper window complete (not dualed OWN).
- Epilogue: pop EDI/ESI/EBX, restore FS:[0], `ADD ESP,0x10`, `RET`. Does **not** free host.
- RTTI seals product **CNDUIDialog**.
- Scalar twin `FUN_004280d0` (vtbl[0]); peer ctor `FUN_00792d20`.
- Shared base for CVOGMenu (`0082d540`), CWndTutorial (`0097a8a0`), inventory grids, many NDUI dialogs.

## Pseudocode (byte-corrected)

```c
// ECX = this; SEH; ADD ESP,0x10; RET; complete (no free this)
void __fastcall CNDUIDialog_CompleteDtor(void* self)
{
  *self = &PTR_FUN_00a98f44;      // CNDUIDialog
  // EH = 1
  FUN_007917c0(self);
  scalar_delete_if(self + 0x4c0);  // [0x130]
  // skip +0x4c4
  scalar_delete_if(self + 0x4c8);  // [0x132]
  scalar_delete_if(self + 0x4cc);  // [0x133]
  scalar_delete_if(self + 0x4d0);  // [0x134]
  scalar_delete_if(self + 0x4d4);  // [0x135]
  scalar_delete_if(self + 0x4d8);  // [0x136]
  FUN_00415e90(/* this+0x4dc list shell */);
  operator_delete(*(void**)(self + 0x4e0)); // returns
  *(void**)(self + 0x4e0) = 0;
  // EH = -1
  FUN_007b5be0(self);              // deeper base complete
}

// scalar_delete_if(p): if (*p) (**p)(1); *p = 0;
```

## Open questions

- English roles of six owned slots + list shell + `+0x4e0` helper.
- Full dual of `007917c0` / `00415e90` / `007b5be0` (not OWN).
- Ghidra function-body repair (DB end `00792ceb` vs true `00792d12`).
- Runtime / bit-exact / differential.
