# Annotated low-level: FUN_00985f40 → PoolHost_ForEachTrackedNested_CallVtable0C_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00985f40` |
| VA | `0x00985f40`–`0x00985f8a` inclusive (**75 B**) |
| System | client / Palantir graphics (`gfxDevice.cpp` Device Reset pre-shutdown) |
| Date | 2026-08-04 (W38-AD) |
| Named | `PoolHost_ForEachTrackedNested_CallVtable0C_Inferred` |

## Machine-level notes

- **ABI:** EAX = pool/track host; bare `C3`; void. Prologue saves ECX/EBX/EBP/ESI/EDI.
- **Host layout:** vector of container* at `+4` (begin) / `+8` (end). Matches PoolHost **track** span used by `PoolHost_Acquire0x40` push path (`FUN_004406e0`).
- **Container layout:** nested ptr vector begin `+8`, end `+0xC`. Empty if begin==0 or `(end-begin)>>2==0`.
- **Per element:** thiscall `obj->vtbl[+0xC]()` — bytes `8B 0E; 8B 01; FF 50 0C` (ECX=obj).
- **Sole caller:** `FUN_0075eff0` Device Reset after log `"Beginning shutdown for Device Reset()"`; EAX from GfxDevice `+0x73C` (ctor slot / `DAT_00d1f624` PoolHost seed).
- **Order:** this → IB factory pre-reset `FUN_00985580` → VB factory pre-reset `FUN_009864e0`.
- Prefer bytes over decompiler for thiscall ECX.

## Pseudocode (annotated)

```c
// EAX = PoolHost* (track host @ GfxDevice+0x73C)
void PoolHost_ForEachTrackedNested_CallVtable0C_Inferred(void /* EAX host */)
{
  void **end = *(void ***)(host + 8);
  for (void **it = *(void ***)(host + 4); it != end; ++it) {
    void *container = *it;
    void **inner_b = *(void ***)((char*)container + 8);
    if (inner_b == 0) continue;
    int n = (*(char**)((char*)container + 0xC) - (char*)inner_b) >> 2;
    if (n == 0) continue;
    void **inner_e = *(void ***)((char*)container + 0xC);
    for (void **q = inner_b; q != inner_e; ++q) {
      void *obj = *q;
      // thiscall vtbl+0xC
      (*(void(__thiscall **)(void*))(*(int*)obj + 0xC))(obj);
    }
  }
}
```

## Open questions

- Product English for vtbl+0xC (device-lost / pre-reset / release?).
- Exact container type inside track (HostAux0x40 vs other).
- Runtime / bit-exact.
