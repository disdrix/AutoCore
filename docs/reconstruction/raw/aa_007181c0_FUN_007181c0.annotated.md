# Annotated low-level: FUN_007181c0 / `_com_raise_error`

| Field | Value |
|---|---|
| Stable ID | `aa_007181c0` |
| VA | `0x007181c0` |
| Canonical name | `_com_raise_error` (MSVC COM support) |
| Ghidra name | `FUN_007181c0` |
| System | `crt-com` |
| Date | 2026-07-29 (W16-P OWN dual seal) |

---

## Machine-level notes

- **Role:** Non-returning COM raise: construct stack `_com_error(hr, IErrorInfo*, fAddRef=false)` then `_CxxThrowException`.
- **ABI:** `__stdcall`, two stack args, `ret 8`. Does not return on the success path of the throw (MSVC still emits epilogue after the call).
- **Hook:** `PTR_FUN_00af8b20` holds this VA; `_com_issue_error` / `_com_issue_errorex` call through that pointer.
- **Sibling:** `_com_error` ctor @ `0x007181f0` (dual-reviewed); dtor/unwind @ `0x007182e0` (ThrowInfo.pmfnUnwind).
- Prefer assembly when decompiler conflicts — here decompile ≡ bytes.

## Pseudocode (annotated)

```c
// void __stdcall _com_raise_error(HRESULT hr, IErrorInfo* perrinfo)
// MSVC comdef raise leaf — client COM only
void FUN_007181c0(long param_1 /* hr */, IErrorInfo *param_2)
{
  _com_error local_14[16];  // 16-byte stack object (vtbl, hr, IErrorInfo*, m_pszMsg)

  // thiscall ctor @ 0x007181f0: stack (hr, IErrorInfo*, bool fAddRef=false)
  _com_error::_com_error(local_14, param_1, param_2, false);

  // Never returns: ThrowInfo DAT_00acc2c0 → _com_error catchable type
  _CxxThrowException(local_14, (ThrowInfo *)&DAT_00acc2c0);  // → 0x006a3d60
}
```

## Recovered control flow (bytes)

| Step | Effect | Evidence |
|---|---|---|
| 1 | `sub esp, 0x10` — 16-byte local | `83 EC 10` |
| 2 | push `fAddRef = 0` | `6A 00` |
| 3 | push `IErrorInfo*` from `[ebp+0xC]` | `8B 45 0C / 50` |
| 4 | push `HRESULT` from `[ebp+8]` | `8B 4D 08 / 51` |
| 5 | `lea ecx, [ebp-0x10]`; call ctor @ `0x007181f0` | rel32 `E8 18 00 00 00` |
| 6 | push ThrowInfo `0x00acc2c0`; push `&local`; call `_CxxThrowException` | imm + rel → `0x006a3d60` |
| 7 | epilogue `mov esp,ebp; pop ebp; ret 8` | dead after throw |

## Open questions

1. Full CatchableType walk under `0x00acc2b8` / `0x00acc29c` (type_info string) — residual RTTI English.
2. Whether product ever rewrites `PTR_FUN_00af8b20` at runtime (hook; not observed in this unit).
