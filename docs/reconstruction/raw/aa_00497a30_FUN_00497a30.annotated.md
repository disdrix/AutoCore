# Annotated low-level: Ctx_ClearListTrackedOwnedAtScaledXY_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00497a30` |
| VA | `0x00497a30`–`0x00497ab8` inclusive (**137 B** / `0x89`) |
| Ghidra name | `FUN_00497a30` |
| Canonical name | `Ctx_ClearListTrackedOwnedAtScaledXY_Inferred` |
| System | ctx → host grid single-cell owned@+0x24 clear |
| Date | 2026-07-29 (W31-D; scaffold 2026-07-23) |

## Machine-level notes

- Source: raw + live `decompile_function` / `read_memory` body hex + DAT floats (W31-D). **No** `disassemble_bytes`.
- ABI: **`__thiscall`**; ECX = ctx; two stack `uint` (x-like, y-like); **`ret 8`**; returns **1**.
- Host path: `host = *(*(ctx + 0xC4) + 0xE898)` — same `+0xE898` host slot as `Class_009c7a1c` factory (W29-G).
- Cell size: `div = fistp(DAT_00aefa50 * DAT_00a14000)` with FPU RC forced chop (`or ah, 0x0C`). Decompiler labels this `ROUND(...)` (approx).
  - `DAT_00aefa50`: runtime scale float (image **32.0f** `0x42000000`; rebuild path can set **64/32**).
  - `DAT_00a14000`: **0.0625f** (`0x3d800000`).
  - Product: **2.0** (scale 32) or **4.0** (scale 64).
- Index: `(param_3 / div) * dimX + (param_2 / div)` with `dimX = *(host+4)`, arr `*(host+0x28)`, stride **0x28**.
- Owned @ `elem+0x24`: same complete-dtor + delete + always-null as rect clearer.
- **No** static xrefs in Ghidra DB (orphan).

## Pseudocode (annotated)

```c
// ECX = ctx; stack: xUnits, yUnits; returns 1; ret 8
uint32_t __thiscall Ctx_ClearListTrackedOwnedAtScaledXY_Inferred(
    void* ctx, uint32_t xUnits, uint32_t yUnits)
{
  void* host = *(void**)(*(char**)((char*)ctx + 0xC4) + 0xE898);
  char* arr = *(char**)((char*)host + 0x28);
  int dimX = *(int*)((char*)host + 4);

  // FPU: div = (int)trunc_toward_zero(DAT_00aefa50 * DAT_00a14000)
  // DAT_00aefa50 = scale (32.f or 64.f); DAT_00a14000 = 0.0625f
  uint32_t div = (uint32_t)(int)/*fistp*/(DAT_00aefa50 * DAT_00a14000);

  int index = (int)(yUnits / div) * dimX + (int)(xUnits / div);
  char* elem = arr + index * 0x28;
  void* owned = *(void**)(elem + 0x24);
  if (owned) {
    ListTrackedObj_CompleteDtor(owned);  // FUN_004be2a0
    operator_delete(owned);
  }
  *(void**)(elem + 0x24) = 0;
  return 1;
}
```

## Related

| VA / name | Role |
|---|---|
| `aa_00494530` rect clearer | multi-cell same owned pattern |
| `ListTrackedObj_CompleteDtor` `0x004be2a0` | owned complete dtor |
| `Host_RebuildElem0x28ArrayFromMap_Inferred` `0x004941b0` | writes `DAT_00aefa50` scale 64/32 |
| `Class_009c7a1c_Ctor` `0x00496f70` | host; stored @ parent `+0xE898` |

## Open questions

- Product English for ctx type and `+0xC4` child.
- Coordinate unit meaning of stack formals (world vs tile).
- Static call-graph usage (orphan).
- Runtime / bit-exact / differential.
