# Annotated low-level: Host_ClearListTrackedOwnedInRect_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00494530` |
| VA | `0x00494530`–`0x0049459d` inclusive (**110 B** / `0x6E`) |
| Ghidra name | `FUN_00494530` |
| Canonical name | `Host_ClearListTrackedOwnedInRect_Inferred` |
| System | host / grid elem0x28 owned@+0x24 clear (rect) |
| Date | 2026-07-29 (W31-D; scaffold 2026-07-23) |

## Machine-level notes

- Source: raw + live `decompile_function` / `read_memory` body hex (W31-D). **No** `disassemble_bytes`.
- **ECX = host** (`8B F1`); bare **`C3`**; void. Ghidra `__fastcall` ≡ thiscall single-register this.
- Dirty/active rect on host: `x0@+0x64`, `x1@+0x6C`, `y0@+0x68`, `y1@+0x70` (half-open).
- Grid: `dimX@+4`, cookie/storage arr `@+0x28`, element stride **0x28**, owned `ListTrackedObj*` at **elem+0x24**.
- Per cell: if owned → `ListTrackedObj_CompleteDtor` (`FUN_004be2a0`, W30-P) then `operator_delete`; **always** write nullptr to `+0x24`.
- Decompiler drops ECX setup on `FUN_004be2a0` and marks `operator_delete` noreturn — **bytes win** (fall-through zero + loop).
- Static call path: thin wrapper @ `0x0094ece0` loads host via `*( *(this+0xE04) + 0xE898 )`, calls this VA, returns 1 / `ret 8`.

## Pseudocode (annotated)

```c
// ECX = host (Class_009c7a1c-shaped: dimX@+4, arr@+0x28, rect@+0x64..+0x70)
void __fastcall Host_ClearListTrackedOwnedInRect_Inferred(void* host)
{
  uint x = *(uint*)((char*)host + 0x64);      // also written as +100 decimal in raw
  if (x >= *(uint*)((char*)host + 0x6C)) return;

  do {
    uint y = *(uint*)((char*)host + 0x68);
    if (y < *(uint*)((char*)host + 0x70)) {
      do {
        int index = *(int*)((char*)host + 4) * (int)y + (int)x;
        char* arr = *(char**)((char*)host + 0x28);
        char* elem = arr + index * 0x28;
        void* owned = *(void**)(elem + 0x24);
        if (owned) {
          ListTrackedObj_CompleteDtor(owned);  // FUN_004be2a0, thiscall
          operator_delete(owned);
        }
        *(void**)(elem + 0x24) = 0;           // always
        y++;
      } while (y < *(uint*)((char*)host + 0x70));
    }
    x++;
  } while (x < *(uint*)((char*)host + 0x6C));
}
```

## Related

| VA / name | Role |
|---|---|
| `FUN_004be2a0` / `ListTrackedObj_CompleteDtor` (W30-P) | owned complete dtor |
| `FUN_005b8000` / `Elem0x28_CompleteDtor` (W29-F) | element dtor (also deletes owned@+0x24) |
| `FUN_00494700` (W31-C) | sibling rect walk (SEH; not owned here) |
| `0x0094ece0` wrapper | loads host from `this+0xE04` → `+0xE898` |

## Open questions

- Product English for host / rect field names.
- Who writes `+0x64/+0x68/+0x6C/+0x70` before call (rect setup).
- Runtime / bit-exact / differential.
