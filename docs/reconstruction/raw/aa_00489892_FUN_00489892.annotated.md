# Annotated low-level: operator_new (aa_00489892)

| Field | Value |
|---|---|
| Stable ID | `aa_00489892` |
| VA | `0x00489892` |
| Canonical name | `operator_new` |
| System | inventory-transfer (parent dual context); unit itself is **CRT IAT** |
| Date | 2026-08-05 (R10-002 refresh) |
| Classification | **thunk** — 6-byte IAT trampoline |

## Machine-level notes

- Source: raw capture for `aa_00489892` + R10-002 live re-verify.
- Prefer **bytes** when decompiler conflicts: body is solely `JMP dword ptr [PTR_operator_new]`.
- Ghidra decompile warns “Could not recover jumptable” / models recursive `operator_new` call — artifact of IAT indirection, not real recursion.
- Name is **Confirmed** PE/CRT import symbol — not an `_Inferred` product invent.
- Do **not** port as AutoAssault game logic; host uses platform `operator new` / `malloc` policy.

## Pseudocode (annotated)

```c
// IAT thunk @ 0x00489892
// Assembly (authoritative):
//   FF 25 64 66 9C 00    JMP dword ptr [0x009c6664]  ; PTR_operator_new
//
// Signature (MSVC C++):
//   void* __cdecl operator_new(size_t cb);
//
// Behavior: transfer control to CRT heap allocator bound through IAT.
// Return: void* block (EAX) or CRT throws std::bad_alloc (inside DLL).
// Stack: __cdecl — caller cleans size arg.

void * __cdecl operator_new(uint size_bytes)
{
  // Indirect jump only — no local prologue/epilogue in this unit.
  return (*PTR_operator_new_009c6664)(size_bytes);
}
```

## Sealed facts

| Claim | Evidence | Conf |
|---|---|---|
| Body **6 bytes** | `read_memory` `ff2564669c00` | **Confirmed** |
| IAT slot `0x009c6664` | `FF 25 64 66 9C 00` | **Confirmed** |
| External symbol `operator_new` | `get_xrefs_from` → EXTERNAL | **Confirmed** |
| `__cdecl` size formal | Ghidra prototype + call-site `PUSH imm; CALL` pattern | **High** |
| ~3519 call sites | Ghidra `xref_count` on function | **High** |
| Not game domain | CRT import cluster with `operator_delete` / `new[]` | **Confirmed** |

## Callers (role, not exhaustive)

Universal heap entry used across inventory, skills, UI, vehicles, missions, etc. Parent dual of residual wave: `Vehicle_CreateCargoInventoryFromPageCount` (`0x004f3a30`) allocates cargo `InventoryGrid` via this thunk.

Sample named callers (Ghidra window): `CVOGReaction_GiveItemByCbid`, `Vehicle_CreateCargoInventoryFromPageCount`, `Client_SendInventoryAddItem`, `CVOGMission_AddActiveObjective`, `Skill_GetCategoryCooldownMap`, many `FUN_*` buynode/list/map factories.

## Open / out of unit

- Exact MSVCR71/80 `operator new` OOM / throw path (DLL body).
- Runtime IAT rebinding / hotpatch after load.
- Sibling secondary thunk `operator_new` @ `0x00685b20` (1 xref) — separate VA, not dualed here.
