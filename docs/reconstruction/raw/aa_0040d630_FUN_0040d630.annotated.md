# Annotated low-level: FUN_0040d630 / StdTree_FreeSubtree_Val12

| Field | Value |
|---|---|
| Stable ID | `aa_0040d630` |
| VA | `0x0040d630`–`0x0040d664` (**53 B**) |
| Canonical name | `StdTree_FreeSubtree_Val12` (**Inferred** structural) |
| Ghidra name | `FUN_0040d630` |
| System | MSVC `_Tree` free-subtree (Val12 isnil@+0x19) |
| Date | 2026-07-29 (W31-A OWN-ONLY dual) |
| Evidence | Live `decompile_function` + `analyze_function_complete` + `read_memory` (full body) + xrefs. **No** `disassemble_bytes`. |

## Machine-level notes

- **Bytes win** over decompiler: free path is recursive-right + left-walk loop; ECX threaded; `RET 4`.
- Callers pass **ECX = host+4** (tree subobject base) from sealed clear `StdTree_Clear_PtrVal_Vtbl0C` (`0x0040d5b0`) and erase helper `FUN_0040d700`.
- Does **not** destroy value objects (`T*` @ +0x14 / vtbl) — only frees node memory. Value destroy is caller's responsibility (clear phase 1).
- `operator_delete` false-noreturn in decompiler is **ignored**; full hex reaches epilogue.

## Signature (sealed)

```c
// ECX = tree_base (threaded; unused in body except recursive pass)
// stack arg0 = node*; RET 4; void
void __fastcall StdTree_FreeSubtree_Val12(void* tree_base, StdTreeNode_Val12* node);
```

## Pseudocode (byte-correct)

```c
void __fastcall StdTree_FreeSubtree_Val12(void* tree_base, StdTreeNode_Val12* node)
{
  if (node->isnil_at_0x19 != 0)
    return; // RET 4

  for (;;) {
    StdTree_FreeSubtree_Val12(tree_base, node->right); // recursive
    StdTreeNode_Val12* left = node->left;
    operator_delete(node);
    node = left;
    if (node->isnil_at_0x19 != 0)
      break;
  }
}
```

## Callers / callees

| Direction | Targets |
|---|---|
| Callers | `FUN_0040d5b0` @ `0x0040d5f6`; `FUN_0040d700` @ `0x0040d71c`; self @ `0x0040d647` |
| Callees | self, `operator_delete` |

## Open questions

- Product/MSVC demangle for map node type.
- Whether tree_base is ever read inside other siblings (unused here).
- Runtime / bit-exact / differential.
