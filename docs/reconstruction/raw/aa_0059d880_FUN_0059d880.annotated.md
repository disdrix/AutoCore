# Annotated low-level: FUN_0059d880 → CVOGObjective_GetCompleteCount

| Field | Value |
|---|---|
| Stable ID | `aa_0059d880` |
| VA | `0x0059d880` |
| System | `missions-progression` |
| Date | 2026-07-23 scaffold; **2026-07-29 dual residual** |

## Machine-level notes

- Body is a pure dword load from **`this+0x164`** into EAX, then RET.
- Hex: `8b 81 64 01 00 00 c3` (pad `cc` follows function end).
- **Product field:** CompleteCount (XML tag from dump `FUN_005468c0` / string `0x009d0638`).
- **this:** objective definition/instance layout (not character).
- Dual CALL sites in CompleteObjective + EvaluatePending are **real** (zero/≤0 test then compare to walk counter).
- Sibling setter `FUN_0059d890` writes the same offset.

## Pseudocode (annotated)

```c
// ECX = objective*
// returns CompleteCount (uint32) at objective+0x164
undefined4 __fastcall FUN_0059d880(int param_1 /* this */)
{
  return *(undefined4 *)(param_1 + 0x164);  // CompleteCount
}
```

## Callers (static complete)

| Site | Parent | Role |
|---|---|---|
| `00534122`, `0053412d` | CompleteObjective | min-pass; `JLE` then `CMP nPassed` |
| `00534ae2`, `00534aed` | EvaluatePendingObjectives | collect; `JZ` then `CMP success` |
| `00546952` | FUN_005468c0 | `<CompleteCount>%i` dump |

## Open questions

- Retail method spelling.
- Negative CompleteCount content samples (call-site JLE vs JZ).
- Full XML load writer path (`FUN_00547050` → `FUN_0059d890`) — out of this unit.
