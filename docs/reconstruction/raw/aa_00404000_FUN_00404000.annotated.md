# Annotated low-level: FUN_00404000 → StdTree_IteratorIncrement_Isnil131

| Field | Value |
|---|---|
| Stable ID | `aa_00404000` |
| VA | `0x00404000`–`0x00404057` inclusive (**88 B** / `0x58`) |
| Ghidra name | `FUN_00404000` |
| Canonical name | `StdTree_IteratorIncrement_Isnil131` |
| System | shared client::stdtree (MSVC `_Tree_iterator::operator++`) |
| Date | 2026-07-23 scaffold; **2026-08-05 WQ9L-A re-verify** |

## Machine-level notes

- **ABI:** EDX = `node**` (in/out); ECX unused at entry; plain **RET** (`C3`). Call sites: `LEA EDX,[stack_it]; CALL`.
- **isnil** byte at node **`+0x131`** (large-node family; contrast isnil31 @ `+0x31`).
- Links: left `+0`, parent `+4`, right `+8` — classic MSVC `_Tree_node` header.
- Leaf: no callees. CF ≡ raw decompile ≡ assembly.
- Prefer assembly when decompiler invents unused `param_1` as meaningful this.

## Pseudocode (annotated)

```c
// EDX = it (Node**); void; plain RET
void StdTree_IteratorIncrement_Isnil131(int *it /*EDX*/)
{
  char is_nil;
  int parent;
  int *right;
  int *left_walk;

  int node = *it;
  // If already nil/end, no-op
  if (*(char *)(node + 0x131) == '\0') {
    right = *(int **)(node + 8);
    if (*(char *)((int)right + 0x131) == '\0') {
      // Right child real → leftmost of right subtree
      is_nil = *(char *)(*right + 0x131);
      left_walk = (int *)*right;
      while (is_nil == '\0') {
        is_nil = *(char *)(*left_walk + 0x131);
        right = left_walk;
        left_walk = (int *)*left_walk;
      }
      *it = (int)right;
      return;
    }
    // Else climb while current is right child of parent
    parent = *(int *)(node + 4);
    is_nil = *(char *)(parent + 0x131);
    while ((is_nil == '\0') && (*it == *(int *)(parent + 8))) {
      *it = parent;
      parent = *(int *)(parent + 4);
      is_nil = *(char *)(parent + 0x131);
    }
    *it = parent;
  }
  return;
}
```

## Callers

| Site | Parent | Role |
|---|---|---|
| `0x00407bc6` | `StdMap_EraseRange_Isnil131_DestroyStr0C_Inferred` (`00407b70`) | advance first in partial erase loop |
| `0x00408b37` | `StdTree_EraseAndRebalance_Isnil131_DestroyStr0C_Inferred` (`00408ad0`) | capture successor before unlink |
| `0x00957928` / `0x00957a9e` / `0x00957c34` / `0x00957d9e` | `FUN_00957820` | client iterator walks |

## Peers (do not merge)

| VA | Name | Diff |
|---|---|---|
| `0x00408590` | `StdTree_IteratorIncrement_Isnil31` | isnil **+0x31** |
| `0x00457cc0` | `StdTree_IteratorIncrement_Isnil19` | isnil **+0x19** |
| `0x004cb270` | `StdTree_IteratorIncrement_Inferred` | isnil **+0x29** |
| `0x00568200` | `StdTree_IteratorIncrement_Isnil49` | isnil **+0x49**; ECX ABI |
| `0x004313d0` | `StdTree_IteratorDecrement` | predecessor |

## Open questions

- Product English for host map type (payload after string @+0x0c).
- Full dual of `FUN_00957820` consumer sites (role sealed as iterator++).
