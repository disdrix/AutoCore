# Annotated low-level: Map_EraseRange_Isnil15_B

| Field | Value |
|---|---|
| Stable ID | `aa_0051d940` |
| VA | `0x0051d940`–`0x0051d9f3` (**180 B**) |
| System | STL / map-set (isnil@+0x15) |
| Date | 2026-08-04 (WQ9F-J dual seal) |
| Ghidra name | `FUN_0051d940` |
| Proposed name | `Map_EraseRange_Isnil15_B` |

## Machine-level notes

- **ABI:** MSVC **`__thiscall`**. ECX = map (`head@+4`, `size@+8`); stack `(out_it*, first, last)`; **`ret 0x0c`**.
- **isnil @ +0x15** (not +0x29). Color typically @ +0x14 on same Val8 layout.
- **Full clear** when `first == *head && last == head`:
  1. `FUN_0051bed0(head->parent)` free subtree
  2. head parent/left/right → head; `size = 0`
  3. `*out_it = *head`
- **Partial:** tree successor (isnil@+0x15) then `FUN_0051ce00` with **`mov ecx, edi`** restoring map this (decomp plate often omits formal).
- **Twin:** `Map_EraseRange_Isnil15` @ `0x0051d7c0` — same CF / erase node / isnil; free **`FUN_0051be50`** instead of **`FUN_0051bed0`**.
- **Caller:** map host complete dtor `FUN_0051e1b0` only (full erase then `operator_delete(head)`).

## Control-flow map

```
head = map->head  // +4
if first == *head && last == head:
    FUN_0051bed0(head->parent)
    reset head links; size = 0
    *out_it = *head
    ret 12
while first != last:
    succ = tree_successor(first)  // isnil@+0x15
    FUN_0051ce00(map, &first, first)
    first = succ
*out_it = first
ret 12
```

## Node layout (relevant)

| Offset | Role |
|---|---|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x14 | color (RB) |
| +0x15 | **isnil** |

## Gaps

1. Exact `map<K,V>` / `set<K>` instantiation and value size (why twin free exists while erase is shared).
2. Free-subtree `FUN_0051bed0` dual open.
3. Host class English for `FUN_0051e1b0`.
4. Runtime / bit-exact open.
