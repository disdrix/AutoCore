# Raw capture: FUN_0092f090

| Field | Value |
|---|---|
| **Stable ID** | `aa_0092f090` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0092f090` |
| **Canonical name** | `FUN_0092f090` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0092f090(void)

{
  int unaff_ESI;
  
  if ((*(int *)(unaff_ESI + 0xe98) != 0) && (*(int *)(*(int *)(unaff_ESI + 0xe98) + 0x250) != 0)) {
    VehicleEntity_SetLongitudinalInput(0);
    VehicleEntity_SetSteerInput(0);
    VehicleEntity_SetHandbrake(1);
  }
  return;
}
```

---

## MEGA-021 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Wave** | OWN-ONLY dual **MEGA-021** |
| **Tools** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` + `get_function_xrefs` + `get_assembly_context` |
| **Forbidden used** | **none** (`disassemble_bytes` not used; no Launcher) |
| **Live ≡ frozen raw CF** | **Yes** (gate thr0/steer0/HB1 unchanged) |
| **Body** | `0x0092f090`–`0x0092f0d1` inclusive (**66 B** / `0x42`) |
| **Pad** | `CC` fill after `RET` before next function |

### Live decompile (2026-08-05)

```c
void FUN_0092f090(void)
{
  int unaff_ESI;

  if ((*(int *)(unaff_ESI + 0xe98) != 0) && (*(int *)(*(int *)(unaff_ESI + 0xe98) + 0x250) != 0)) {
    VehicleEntity_SetLongitudinalInput(0);
    VehicleEntity_SetSteerInput(0);
    VehicleEntity_SetHandbrake(1);
  }
  return;
}
```

### Live listing (`disassemble_function`)

```text
0092f090  MOV  EAX,dword ptr [ESI + 0xe98]
0092f096  TEST EAX,EAX
0092f098  JZ   0x0092f0d1                 ; early out — no character
0092f09a  MOV  ECX,dword ptr [EAX + 0x250]
0092f0a0  TEST ECX,ECX
0092f0a2  JZ   0x0092f0d1                 ; early out — no vehicle entity
0092f0a4  PUSH 0x0
0092f0a6  CALL 0x004f5650                 ; VehicleEntity_SetLongitudinalInput(this=ECX, 0)
0092f0ab  MOV  EAX,dword ptr [ESI + 0xe98]
0092f0b1  MOV  ECX,dword ptr [EAX + 0x250]
0092f0b7  PUSH 0x0
0092f0b9  CALL 0x004f5620                 ; VehicleEntity_SetSteerInput(this=ECX, 0)
0092f0be  MOV  ECX,dword ptr [ESI + 0xe98]
0092f0c4  MOV  ECX,dword ptr [ECX + 0x250]
0092f0ca  PUSH 0x1
0092f0cc  CALL 0x004f3620                 ; VehicleEntity_SetHandbrake(this=ECX, 1)
0092f0d1  RET
```

### Body hex (66 B, `read_memory`)

```
8B 86 98 0E 00 00 85 C0 74 37 8B 88 50 02 00 00 85 C9 74 2D 6A 00 E8 A5 65 BC FF
8B 86 98 0E 00 00 8B 88 50 02 00 00 6A 00 E8 62 65 BC FF
8B 8E 98 0E 00 00 8B 89 50 02 00 00 6A 01 E8 4F 45 BC FF
C3
```

Rel32 targets sealed: `0x004f5650` (long), `0x004f5620` (steer), `0x004f3620` (HB).

### ABI (sealed)

| Item | Value | Conf |
|---|---|---|
| Client / host | **ESI** (`[ESI+0xe98]`; all 5 call sites `MOV ESI,EBP` before CALL) | **High** |
| Stack formals | none | **High** |
| Return | void; bare **`RET`** (`C3`) | **High** |
| ECX-thiscall for this unit | **No** (ECX only seeded for nested vehicle setters) | **High** |
| Decompiler `unaff_ESI` | register this (not phantom) | **High** |
| Character ptr | `*(ESI + 0xe98)` | **High** |
| Vehicle entity | `*(*(ESI + 0xe98) + 0x250)` | **High** |

### Callees (static)

| VA | Name | Args from this unit |
|---|---|---|
| `0x004f5650` | `VehicleEntity_SetLongitudinalInput` (dualed) | `this=vehicle`, stack float **0** (`PUSH 0`); callee `RET 4` |
| `0x004f5620` | `VehicleEntity_SetSteerInput` (dualed) | `this=vehicle`, stack float **0**; callee `RET 4` |
| `0x004f3620` | `VehicleEntity_SetHandbrake` (dualed) | `this=vehicle`, stack byte **1**; callee `RET 4` |

No `PushDriveAxes` / no direct `+0x614`/`+0x618`/`+0x61c` stores here — park is **via dualed setters**.

### Callers / xrefs (live)

| Kind | Value |
|---|---|
| Classification | worker |
| Code callers (analyze) | `Client_ShowNpcMissionDialogUI` |
| Xrefs | **5× UNCONDITIONAL_CALL** |
| Sites | `0x00943989`, `0x00943b72` in `Client_ShowNpcMissionDialogUI` (`0x009438f0`–`0x00943b7d`); `0x0093e6ce`, `0x0093e745`, `0x0093e76f` in `FUN_0093e450` (`0x0093e450`–`0x0093e7cc`) |

### Call-site ABI seed (all five)

```text
...  MOV ESI,EBP          ; EBP = client host (param_2 / dialog client)
...  CALL 0x0092f090      ; OWN — park thr0/steer0/HB1
```

### Semantics (sealed structure)

If character at `client+0xe98` and vehicle at `character+0x250` both non-null:

1. **Longitudinal = 0** (via gated setter @ `+0x614`).
2. **Steer = 0** (via gated setter @ `+0x618`).
3. **Handbrake = 1** (via ungated setter @ `+0x61c`).

Else: no-op bare RET.

Does **not** push controller axes. Does **not** clear HB. Does **not** set only thr.

### Name proposal

| Field | Value |
|---|---|
| Ghidra | `FUN_0092f090` |
| Proposed | `MissionUI_ParkDriveAxes_Inferred` |
| Retired scaffold | `Named_CalleeOf_Client_ShowNpcMissionDialogUI_0092f090` |
| System | `input-drive-control` |
| Partition hint | Mission-UI axis park thr0/steer0/HB1 (parent `0x009438f0`) |

### Completeness note

`analyze_function_complete`: classification **worker**; return type display `undefined`/void matches bare RET (no EAX use). Hungarian `unaff_ESI` is the sealed register ABI, not a missing formal.
