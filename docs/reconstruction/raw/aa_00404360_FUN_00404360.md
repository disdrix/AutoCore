# Raw capture: FUN_00404360

| Field | Value |
|---|---|
| **Stable ID** | `aa_00404360` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00404360` |
| **Canonical name** | `FUN_00404360` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall
FUN_00404360(undefined4 param_1,undefined4 param_2,undefined4 param_3,undefined4 *param_4,
            undefined1 param_5)

{
  undefined4 *in_EAX;
  int iVar1;
  undefined4 *puVar2;
  
  *in_EAX = param_1;
  in_EAX[2] = param_3;
  in_EAX[1] = param_2;
  puVar2 = in_EAX + 4;
  for (iVar1 = 8; iVar1 != 0; iVar1 = iVar1 + -1) {
    *puVar2 = *param_4;
    param_4 = param_4 + 1;
    puVar2 = puVar2 + 1;
  }
  *(undefined1 *)(in_EAX + 0xc) = param_5;
  *(undefined1 *)((int)in_EAX + 0x31) = 0;
  return;
}
```

---

## Re-verify — 2026-08-05 R10-021 OWN-ONLY dual seal

| Field | Value |
|---|---|
| **Agent** | R10-021 OWN-ONLY dual |
| **Tools** | Ghidra MCP: `decompile_function` + `analyze_function_complete` + `get_function_callers` + `get_function_xrefs` + `read_memory`. **No** `disassemble_bytes`. No Launcher. |
| **Integrity** | scaffold body above preserved; this section append-only |
| **Program** | `autoassault.exe` base `0x400000` |

### Live decompile ≡ scaffold CF

Live `decompile_function` **≡** 2026-07-23 raw control flow: store left/parent/right → copy **8 dwords** value → color byte → isnil=0 → return. Leaf (no callees).

**Decompiler presentation notes (sealed by bytes + sole caller):**

- `in_EAX` is the **node this** (phantom; caller loads `EAX = new node`).
- Ghidra labels `__fastcall` with ECX/EDX = first two args; full ABI is **usercall**: node@&lt;eax&gt;, left@&lt;ecx&gt;, parent@&lt;edx&gt;, right/value*/color stack; **`RET 0xC`**.
- Loop of 8 stores is **`REP MOVSD`** with ECX=8 (Val32).

### Body / ABI (bytes)

| Fact | Value |
|---|---|
| Body | `0x00404360`–`0x0040438C` exclusive (**44 B** / `0x2C`); terminal **`RET 0xC`** (`C2 0C 00`); `CC` pad follows |
| Convention | **`__usercall`**: node@&lt;eax&gt;, left@&lt;ecx&gt;, parent@&lt;edx&gt;, stack right / value* / color; **RET 0xC** |
| Leaf | no CALL targets; `callees: []` |
| Copy | `LEA EDI,[EAX+0x10]`; `MOV ECX,8`; `REP MOVSD` from ESI=value* |
| Color | `MOV [EAX+0x30], DL` |
| Isnil | `MOV byte [EAX+0x31], 0` (hard zero; not from color arg) |

Hex (`read_memory` @ `0x00404360`, 48 B):

```text
56 8B 74 24 0C 89 08 8B 4C 24 08 57 89 48 08 89
50 04 8A 54 24 14 8D 78 10 B9 08 00 00 00 F3 A5
5F 88 50 30 C6 40 31 00 5E C2 0C 00 CC CC CC CC
```

Annotated store map:

```text
PUSH ESI
MOV  ESI, [ESP+0xC]      ; value*  (after push: stack slot of arg2)
MOV  [EAX], ECX          ; +0x00 left
MOV  ECX, [ESP+0x8]      ; right
PUSH EDI
MOV  [EAX+8], ECX        ; +0x08 right
MOV  [EAX+4], EDX        ; +0x04 parent
MOV  DL, [ESP+0x14]      ; color
LEA  EDI, [EAX+0x10]
MOV  ECX, 8
REP  MOVSD               ; +0x10..+0x2F  Val32 (8 dwords)
POP  EDI
MOV  [EAX+0x30], DL      ; color
MOV  byte [EAX+0x31], 0  ; isnil
POP  ESI
RET  0xC
```

### Layout (node size 0x38 from sole caller alloc)

| Offset | Write |
|---|---|
| +0x00 | left ← ECX |
| +0x04 | parent ← EDX |
| +0x08 | right ← stack |
| +0x0C | **not written** by this ctor |
| +0x10..+0x2F | `REP MOVSD` **8 dwords** from value* (**Val32**) |
| +0x30 | color ← stack byte |
| +0x31 | **isnil = 0** |
| size (parent) | **0x38** (`operator_new(0x38)` in `FUN_00403f30`) |

### Callers / xrefs / callees

| Role | Target |
|---|---|
| **Caller (1)** | `FUN_00403f30` (`StdTree_Buynode_Val32_Isnil31_Inferred`) @ `0x00403f86` UNCONDITIONAL_CALL |
| **Xrefs** | **xref_count = 1** |
| **Callees** | none (leaf) |

Call site (parent buynode, evidence only — not OWN):

```text
PUSH [EBP+0x18]          ; color
PUSH [EBP+0x14]          ; value*
PUSH [EBP+0x10]          ; right
MOV  EDX, [EBP+0x0C]     ; parent
MOV  ECX, [EBP+0x08]     ; left
MOV  EAX, ESI            ; this = new node (0x38)
CALL 0x00404360          ; RET 0xC
```

### Name decision

Promote **`StdTree_NodeCtor_Val32_Isnil31_EaxThis_Inferred`**.

- Structural: MSVC `_Tree` node construct (no alloc); isnil@**+0x31**; value **32 B** @+0x10; color@+0x30; EAX-this usercall; RET 0xC.
- Parent dual: `StdTree_Buynode_Val32_Isnil31_Inferred` (`0x00403f30`, MEGA-048) — alloc + call this ctor.
- Family peers: NodeCtor Val24 isnil29 EAX-this `0x00409880`; isnil31 Lrot/Rrot/erase/BuyHead.
- Product / PDB demangle open → **`_Inferred`**.
- **Reject** scaffold `Named_CalleeOf_*NotifyActiveMission*` / multi-hop Client plate (chain-of-caller only; unit is shared STL helper).
- **Reject** naming as buynode (no `operator_new`).
- **Reject** merge with Val24 isnil29 ctors (`00409880` / `004098d0`) or Val16 isnil21 (`004099b0`) — different size/offsets/dword counts.
- **Reject** freelist / placement-new / mission-product mutator.

### Gaps

- Product / MSVC demangle for host `value_type` (8-dword payload English).
- Runtime / bit-exact / differential.
- Parent insert residual `FUN_00403250` not OWN (MEGA-047).

**Terminal:** **false**
