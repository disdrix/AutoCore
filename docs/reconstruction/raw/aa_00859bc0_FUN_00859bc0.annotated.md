# Annotated low-level: Client_UI_MapView_DrawEntitiesFovOverlay (`FUN_00859bc0`)

| Field | Value |
|---|---|
| Stable ID | `aa_00859bc0` |
| VA | `0x00859bc0`–`0x0085a9ef` (3631 B / `0xE2F`) |
| Canonical name | `Client_UI_MapView_DrawEntitiesFovOverlay` (INFERRED) |
| Ghidra name | `FUN_00859bc0` |
| System | client UI / map-view overlay |
| Date | 2026-07-29 (W23-N dual seal) |

## Machine-level notes

- Source: raw capture + live W23-N `decompile_function` / `read_memory` / `analyze_function_complete`.
- **Role:** Composite **map-view draw pass** (entity-rich twin of `FUN_00851630`). Walks two world entity vectors, then markers / patrol / missions / special / squad, then a scaled FOV textured quad.
- **ABI:** **`__thiscall`** — ECX = this*. Stack 16-byte aligned; frame `0x104`. Plain **`ret` (`C3`)**.
- **Primary host:** `*(this + 0x488)` = map config*.
- **No `+0x4fc` enable flag** — different gate than markers-only sibling; requires world entity list heads non-null.
- **World lists:** host at `*(DAT_00d1b644 + 0xe4e8)`; list A at host+`0x28` (begin/end at +`0x2c`/`+0x30`); list B at host+`0x48`.
- **Draw leaf:** `FUN_00859260` (entity/marker textured) and `FUN_00859a90` (icon-type dispatch twin of `FUN_008513d0`).
- **Textures:** entity `config+0x5d0`; marker type `config+0x624+type*4`; FOV `config+0x5cc`.
- **Sizes:** entity path uses `config+0x530/0x534` (list A) and `+0x520/0x524` (list B); markers use `+0x538/0x53c`.
- **FOV radius:** `config+0x514` optionally scaled by `min(1.0,1.0)/config+0x500` when `config+0x5b9==0`, then `* config+0x50c * 0.5`.
- **Layer counters:** same `this+0x68/+0x70/+0x74` pattern as sibling.
- **Dispatch:** vtable DATA only @ `0x00a65a54`.
- Filters use vfuncs `+0x198` (hidden?), `+0x19c` (clonebase), `+0x1d4/0x1d8` (lazy), `+0x1dc` (owner), `+0x210` (owner entity), `FUN_005134e0`, flag bit3 at various sites.
- Ally tint: `DAT_00d1b644+0xf6` OR matching party ids `+0xca8/+0xcac` with local when party flag `local+0xcb0`.

## Recovered CF (phase-accurate)

```c
// __thiscall; no stack args; void
void Client_UI_MapView_DrawEntitiesFovOverlay(MapView* self)
{
    MapConfig* cfg = self->config; // +0x488
    if (!cfg || !g_local || !g_world || !g_world->listHost)
        return;
    WorldLists* wl = g_world->listHost; // +0xe4e8
    if (!wl->listA /*+0x28*/ || !wl->listB /*+0x48*/)
        return;

    // pose = g_local->vtbl[+0x1a0]()

    // --- world list A ---
    for (obj in wl->listA) {
        if (!PassesFilterA(obj)) continue;
        if (obj->lazy == 0) obj->EnsureLazy();
        else if (obj->owner == null)
            DrawEntityIcon(obj, cfg->sizeA, cfg->entityTex); // FUN_00859260
    }
    BumpLayer(self);

    // --- world list B ---
    for (obj in wl->listB) {
        if (!PassesFilterB(obj)) continue;
        tint = AllyTint(obj) ? 1 : 0;
        DrawEntityIcon(obj, cfg->sizeB, cfg->entityTex, tint);
    }
    BumpLayer(self);

    // markers DAT_00d1d964 → FUN_00859260 type tex +0x624
    // auto-patrol → FUN_00859a90
    // mission tree → FUN_00859a90(..., 0x65)
    // special → FUN_00859a90(..., 100)
    // squad → FUN_00859a90(..., 1) with flag filter
    // FOV quad: radius from cfg+0x514 (+optional scale); FUN_004141c0 corners;
    //           FUN_00757890 tex cfg+0x5cc or DAT_00d1ec78
}
```

## Call graph (OWN context)

| Direction | VA | Name / role |
|---|---|---|
| Callee | `0x00859260` | Entity/marker map icon draw |
| Callee | `0x00859a90` | Icon-type dispatch (`Named_VOG_DEBUG_STOP_00859a90`) |
| Callee | `0x00757890` | FOV multi-vert textured draw |
| Callee | `0x00930fc0` | Pose/cookie helper |
| Callee | `0x005134e0` | Entity filter predicate |
| Callee | `0x004141c0` | Project corner to screen |
| Callee | `Client_EvalAutoPatrolWaypoint` | Patrol waypoint eval |
| Callee | `0x00574760` | Squad slot lookup |
| Callee | `0x004e8a40` / `0x004e8ad0` | Basis extract |
| Sibling | `0x00851630` | Markers+cone overlay twin |
| Vtbl | `0x00a65a54` | Sole xref (DATA) |

## Open questions

1. Product / PDB class and method name (which map panel subclass).
2. Semantic English for world list A vs B (loot? vehicles? NPCs?).
3. Full filter flag dictionary (`obj[0x5f]>>3`, clonebase `+0x180>>3`).
4. Runtime / bit-exact — open.
5. Nested dual of `FUN_00859260` / `FUN_00859a90` (out of OWN scope).
