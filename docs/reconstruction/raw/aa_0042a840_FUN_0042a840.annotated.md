# Annotated low-level: FUN_0042a840 → StdTree_Rrotate_Isnil2D_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0042a840` |
| VA | `0x0042a840`–`0x0042a88f` inclusive (**80 B** / `0x50`) |
| System | MSVC `std::_Tree` right-rotate (isnil@+0x2D) |
| Date | 2026-08-05 (WQ9K-J dual) |

## Machine-level notes

- Source: raw capture + WQ9K-J re-verify (`disassemble_function` + `read_memory`).
- Prefer assembly when decompiler conflicts (none material here).
- **ABI trap vs isnil29:** this family uses **ECX = node**, stack = tree; isnil29 pair uses **ECX = tree**, stack = node.
- isnil test: `CMP byte ptr [EDX+0x2d],0` on promoted child's former right (`y->right`).
- Peer L-rotate: `FUN_0044e010` (same isnil/ABI; residual dual if needed).
- Color@+0x2C is **not** written here — rebalance parents recolor separately.

## Pseudocode (annotated)

```c
// ECX = node x*; stack tree*; RET 4
void StdTree_Rrotate_Isnil2D_Inferred(int *x /*ECX*/, int tree /*stack*/)
{
  int y = *x;                              // y = x->left
  *x = *(int *)(y + 8);                    // x->left = y->right
  if (*(char *)(*(int *)(y + 8) + 0x2d) == '\0') {
    *(int **)(*(int *)(y + 8) + 4) = x;    // y->right->parent = x (if real)
  }
  *(int *)(y + 4) = x[1];                  // y->parent = x->parent

  if (x == *(int **)(*(int *)(tree + 4) + 4)) {
    // x was root
    *(int *)(*(int *)(tree + 4) + 4) = y;
    *(int **)(y + 8) = x;                  // y->right = x
    x[1] = y;
    return;
  }
  int *parent = (int *)x[1];
  if (x == (int *)parent[2]) {
    // x was right child
    parent[2] = y;
    *(int **)(y + 8) = x;
    x[1] = y;
    return;
  }
  // x was left child
  *parent = y;
  *(int **)(y + 8) = x;
  x[1] = y;
}
```

## Open questions

- Product/PDB demangle for the owning tree specialization(s).
- Runtime / bit-exact / differential (terminal false).
