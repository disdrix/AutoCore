# Annotated low-level: FUN_0075bf40 / GfxView_SetDiffuseColor_ClearDiffuseTex_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0075bf40` |
| VA | `0x0075bf40`–`0x0075bf95` exclusive (**85 B** / `0x55`) |
| System | GfxView / material color worker |
| Date | 2026-08-04 (W37-L dual seal) |

## Machine-level notes

- **Bytes authority:** short thiscall on GfxView-sized object (callers allocate via `FUN_00968a50` / `0x198` and chain SetNear/SetFar).
- **Prolog:** `PUSH ECX` (local index), `PUSH ESI/EDI`; `EDI = this`.
- **Effect probe:** `ECX = *(this+0x134)`; `LEA ESI,[this+0x134]`.
  - Zero → `local = -1`.
  - Non-zero → `FUN_00752370(thiscall, &local, "DiffuseTexture")` — string `0x00a9ede4`.
- **Clear:** if `local >= 0` → `FUN_00970060(local, 0)` (ESI still effect subobject).
- **Always:** `*(this+0x148) = color`; `*(uint8*)(this+0x129) = 1`; `EAX = 0`; `ret 4`.
- **Decompile gap:** invents `local_4 = param_1` on the non-null branch — **rejected by bytes**.

## Pseudocode (byte-corrected)

```c
// ECX = GfxView*; one stack arg; ret 4; always returns 0
int __thiscall GfxView_SetDiffuseColor_ClearDiffuseTex_Inferred(
    void *self, uint32_t color)
{
  int index = -1;
  void *effect = *(void **)((uint8_t *)self + 0x134);

  if (effect != nullptr) {
    // ESI = self+0x134 for FUN_00970060 unaffiliated-ESI use
    FUN_00752370(effect, &index, "DiffuseTexture");
  }
  if (index >= 0)
    FUN_00970060(index, 0);

  *(uint32_t *)((uint8_t *)self + 0x148) = color;
  *((uint8_t *)self + 0x129) = 1;
  return 0;
}
```

## Layout (this unit)

| Offset | Role |
|---|---|
| +0x129 | dirty flag (set **1**) |
| +0x134 | effect / material subobject\* (may be null) |
| +0x148 | packed color `uint32` (e.g. `0x808080`, `0`) |

## Open questions

- Packed color channel order / alpha.
- Product English method name (Diffuse vs modulate vs clear-color).
- Full effect object at `+0x134` and exact `FUN_00970060` clear semantics.
- Runtime / bit-exact — open.
