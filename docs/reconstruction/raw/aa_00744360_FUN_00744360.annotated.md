# Annotated low-level: PreloadDep_CollectTransitiveKeys_Inferred / FUN_00744360

| Field | Value |
|---|---|
| Stable ID | `aa_00744360` |
| VA | `0x00744360`–`0x0074445c` (**253 B**) |
| Canonical name | `PreloadDep_CollectTransitiveKeys_Inferred` |
| Ghidra name | `FUN_00744360` |
| System | preload / asset dependency graph |
| Date | 2026-07-29 (W32-L) |

## Machine-level notes

- **ABI:** `MOV ESI,ECX` = dependency-tree host (map at `host+0x10`); stack `(const int* key, GuardedVectorHeader* ring)`; `RET 8` (`C2 08 00`).
- **Setup:** `FUN_00426f60` inits key-view; `FUN_0044d960(host+0x10)` returns equal-range iterators (`local_14` begin, `local_10` end) — decompiler shows empty calls (EAX-out idiom).
- **Per node** in `[begin, end)`:
  - Key slot = `node + 0x18` (`local_14 + 6` dwords).
  - Scan **ring** from back (`begin+size` down to `begin`) with GuardedVector page wrap (`+0x04` pages, `+0x08` capacity, `+0x0c` begin, `+0x10` size).
  - If key already present → skip insert.
  - Else: **recurse** `self(key_slot, ring)` with ECX=host; then `GuardedVector_PushFront_Thiscall(ring, key_slot)`.
- **Advance:** MSVC std::map/set bidirectional successor using nil flag at `node+0x25`, left/right/parent at `+0/+4/+8`.
- **Callers:** `FillPreloadList` (`0x004f2540`); AssPreloader family `0x009717a0` / `0x00971820` / `0x00971900` / `0x00971a20`; self.
- **Name:** structural `_Inferred` — no plate string in body. Reject scaffold `Named_CalleeOf_Named_assManager_*`.

## Pseudocode (annotated)

```c
// ECX = dep-tree host (tree header at +0x10)
// stack: key*, out_ring*; RET 8; void
void PreloadDep_CollectTransitiveKeys_Inferred(
    void* host /*ECX*/,
    const int* key /*stack*/,
    GuardedVectorHeader* ring /*stack*/)
{
  // equal_range on host+0x10 for *key → [it, end)
  for (node = it; node != end; node = successor(node)) {
    const int* node_key = (const int*)((char*)node + 0x18);
    if (ring_contains_dword(ring, *node_key))
      continue;
    PreloadDep_CollectTransitiveKeys_Inferred(host, node_key, ring);
    GuardedVector_PushFront_Thiscall(ring, node_key);
  }
}
```

## Gaps

- Product/PDB name for host class and edge relation (depends-on vs depended-by).
- Nested tree helpers `FUN_00426f60` / `FUN_0044d960` not dual-owned here.
- Runtime / bit-exact / cycle behavior beyond membership skip.
