# Annotated low-level: FUN_0078c3d0 → CNDUIWndBuffered_TeardownBufferedOwned_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0078c3d0` |
| VA | `0x0078c3d0`–`0x0078c47e` inclusive (**175 B** / `0xAF`) |
| System | UI / CNDUIWndBuffered buffered-owned teardown |
| Date | 2026-07-23 scaffold; **WQ9L-D2 refresh 2026-08-05** |
| Named | `CNDUIWndBuffered_TeardownBufferedOwned_Inferred` |
| Parent | `CNDUIWndBuffered_CompleteDtor` `0x0078ca80` (dualed WQ9K-F) |
| Evidence | Live decompile + `disassemble_function` + full-body `read_memory` + callers/xrefs. **No** `disassemble_bytes`. |

---

## Machine-level notes

- Prefer assembly when decompiler conflicts (thiscall this for `FUN_0074e260` is sealed by bytes, not decompiler single-arg display).
- Gate field **`this+0x2A4`**: whole body is a no-op when null.
- Nested **HostBase** lives at **`this+0x2A8`** (ctor `0x0078caf0`); this helper does **not** free it — parent complete dtor scalar-deletes after return.
- Owned children at **`+0x2AC`** / **`+0x49C`**: unregister via `FUN_0074e260` then scalar-delete `vtbl[0](1)`.
- COM-like release at `(*(HostBase+4))+0x3C`: refcount at `+4`, destroy via `vtbl[+8]` when zero.
- EDI used as zero constant throughout (no free-standing imm 0 loads for stores).

---

## Signature (sealed)

```c
// ECX = CNDUIWndBuffered* this; bare C3; void
void __fastcall CNDUIWndBuffered_TeardownBufferedOwned_Inferred(void *self);
```

| Formal | Source | Conf |
|---|---|---|
| self | ECX (`8B F1`) | **High** |
| return | void | **High** |
| stack formals | none (`C3`) | **High** |

---

## Pseudocode (annotated, ABI-corrected)

```c
// Gate: only runs when buffered primary owned object is live.
void __fastcall CNDUIWndBuffered_TeardownBufferedOwned_Inferred(void *self /*ECX*/)
{
  void **child_2ac;
  void **child_49c;
  void **child_2a4;
  void *host;          // *(self+0x2A8) HostBase*
  void *host_aux;      // *(host+4)
  int *refobj;         // *(host_aux+0x3C)

  if (*(void **)((char *)self + 0x2A4) == 0)
    return;

  // 1) Unregister + scalar-delete owned @ +0x2AC
  child_2ac = *(void ***)((char *)self + 0x2AC);
  host = *(void **)((char *)self + 0x2A8);
  // thiscall FUN_0074e260(ECX=*(host+4), arg=*(child+0x8C))
  FUN_0074e260(*(void **)((char *)host + 4),
               *(void **)((char *)child_2ac + 0x8C));
  if (child_2ac != 0) {
    // scalar-deleting dtor: ECX=child, PUSH 1, CALL [vtbl+0]
    (*(void (__thiscall **)(void *, int))(*child_2ac))(child_2ac, 1);
  }
  // null +0x2AC (bytes interleave this store with load/CMP of +0x49C)
  *(void **)((char *)self + 0x2AC) = 0;

  // 2) Optional same for owned @ +0x49C
  child_49c = *(void ***)((char *)self + 0x49C);
  if (child_49c != 0) {
    host = *(void **)((char *)self + 0x2A8);
    FUN_0074e260(*(void **)((char *)host + 4),
                 *(void **)((char *)child_49c + 0x8C));
    if (child_49c != 0) {
      (*(void (__thiscall **)(void *, int))(*child_49c))(child_49c, 1);
    }
    *(void **)((char *)self + 0x49C) = 0;
  }

  // 3) Scalar-delete gate object @ +0x2A4
  child_2a4 = *(void ***)((char *)self + 0x2A4);
  if (child_2a4 != 0) {
    (*(void (__thiscall **)(void *, int))(*child_2a4))(child_2a4, 1);
  }
  host = *(void **)((char *)self + 0x2A8);
  *(void **)((char *)self + 0x2A4) = 0;

  // 4) Refcount-release aux slot +0x3C on HostBase+4
  host_aux = *(void **)((char *)host + 4);
  refobj = *(int **)((char *)host_aux + 0x3C);
  if (refobj != 0) {
    refobj[1] = refobj[1] - 1;          // *(refobj+4)--
    if (refobj[1] == 0) {
      (*(void (**)(void))(*(int *)refobj + 8))();  // vtbl[+8]
    }
  }
  *(void **)((char *)host_aux + 0x3C) = 0;
}
```

---

## Offsets (this = CNDUIWndBuffered)

| Offset | Role (this unit) | Conf |
|---|---|---|
| `+0x2A4` | Gate + primary owned object (scalar-deleted) | **High** |
| `+0x2A8` | Nested HostBase* (not freed here) | **High** (ctor/dtor dualed) |
| `+0x2AC` | Secondary owned (unregister + scalar-delete) | **High** |
| `+0x49C` | Tertiary owned (optional same) | **High** |
| HostBase`+4` | Aux registry object for `FUN_0074e260` this | **High** |
| child`+0x8C` | Unregister key passed to `FUN_0074e260` | **High** |
| aux`+0x3C` | COM-like refcounted ptr slot | **High** |
| refobj`+4` | Refcount dword | **High** |
| vtbl`+0` | Scalar-deleting dtor (flag 1) | **High** |
| vtbl`+8` | Release/destroy when refcount hits 0 | **High** |

---

## Call graph context

```text
CNDUIWndBuffered_CompleteDtor  FUN_0078ca80  [dualed WQ9K-F]
  ├─ FUN_007a8580              shared UI teardown
  ├─ FUN_0078c3d0              THIS — TeardownBufferedOwned  [OWN WQ9L-D2]
  │     ├─ FUN_0074e260 ×2     unregister from HostBase aux
  │     ├─ vtbl[0](1) ×≤3      scalar-delete owned children
  │     └─ vtbl[+8]            refcount destroy on aux+0x3C
  ├─ scalar-delete HostBase @ +0x2A8
  └─ FUN_007b5be0              CNDUIWindow_CompleteDtor

CWndVehicle_CompleteDtor 0x00834520 → CNDUIWndBuffered_CompleteDtor
```

---

## Open questions / gaps

1. Product English for owned types at `+0x2A4` / `+0x2AC` / `+0x49C` (no RTTI on those pointers in this unit).
2. Product name / dual of `FUN_0074e260` (unregister-by-key helper; scaffold alias only).
3. Product meaning of aux `+0x3C` COM-like object.
4. Which derived vtables hold the 4 DATA slots (`00a9a32c`, `00a6ff84`, `00a6755c`, `00a66d24`) — method index open.
5. Runtime / bit-exact / differential — open.
