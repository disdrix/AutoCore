# Annotated low-level: FUN_0043cf40 → GuardedVector_GrowAssignRangeCore

| Field | Value |
|---|---|
| Stable ID | `aa_0043cf40` |
| VA | `0x0043cf40`–`0x0043d2f6` (**951 B**) |
| System | STL / guarded ring-deque (family `GuardedVector_*`) |
| Date | 2026-07-29 (W28-B dual; scaffold 2026-07-23 preserved in raw) |
| Ghidra name | `FUN_0043cf40` |
| Inferred name | `GuardedVector_GrowAssignRangeCore` (**Inferred**) |

## Machine-level notes

- **ABI:** stdcall **8** stack formals; SEH frame; **`RET 0x20`** (sealed at `0x0043d2f4`: `C2 20 00`).
- Decompiler lists **7** named formals; sole caller `FUN_0043cec0` pushes **8th = param_1** (dest self-ref). 8th may be unused in body — cleanup still 8 dwords.
- **Sole caller:** `GuardedVector_GrowAssignRange` (`0x0043cec0`) @ `0x0043cee8` (grow path of `GuardedVector_CopyAssign`).
- **Layout (dest `param_1`):** same as erase/copy family — `+0x04` map, `+0x08` capacity, `+0x0c` begin, `+0x10` size.
- Ring element address formula throughout: `map[(idx>>2) wrapped by capacity] + (idx & 3)*4` (decomp: `>>2`, subtract capacity if `>=`, then `(idx + (-4*block))*4`).

## Parameter map (from grow trampoline + uses)

| Slot | Role (structural) | Conf |
|---|---|---|
| p1 | dest container | **High** |
| p2 | scratch / seed (overwritten early with p4 / dest) | **Medium** |
| p3 | dest end/index bound used as `prefix = p3 - begin` | **High** |
| p4 | source container (or source range base object) | **High** |
| p5 | source range begin index | **High** |
| p6 | source end container seed (compare in construct loop) | **Medium** |
| p7 | source range end index; `src_len = p7 - p5` | **High** |
| p8 | dest again (from trampoline; not referenced in decompile) | **High** (push) / **Open** (use) |

## Control flow (sealed shape)

```
prefix  = param_3 - dest.begin
suffix  = dest.size - prefix
src_len = param_7 - param_5

if prefix < suffix:                    // work reverse / front side
  if prefix < src_len:                 // need extra front constructs
    reverse-walk source excess → FUN_0043ce10 (push_front construct)
    reverse-walk dest prefix     → FUN_0043ce10
    FUN_0043d390(...)  ; return
  else:                                // destroy excess dest prefix elems
    reverse-walk (prefix - src_len?) via FUN_0043ce10 on dest
    FUN_0043d390(internal shift...)
    fall to final assign copy
else:                                  // work forward / back side
  if suffix < src_len:                 // need extra back constructs
    forward-walk source excess → FUN_0043c830 (push_back construct)
    forward-walk dest suffix   → FUN_0043c830
    FUN_0043d390(...)  ; return
  else:                                // destroy excess dest suffix
    forward-walk via FUN_0043c830 on dest
    FUN_0043d300(...)                  // reverse-side move helper
// common:
FUN_0043d390(... source range → dest at insert ...)
```

Interpretation (family-consistent, not product English):

- Shorter-side policy mirrors `GuardedVector_EraseRange`.
- `FUN_0043ce10` / `FUN_0043c830` are element **construct/push** helpers (front/back) that may call `GuardedVector_GrowBlockMap` when map full.
- `FUN_0043d300` / `FUN_0043d390` are ring **move/assign** helpers (also used by erase/copy paths) — **unowned residual**.

## Call graph

| Dir | VA | Name / role |
|---|---|---|
| caller | `0x0043cec0` | `GuardedVector_GrowAssignRange` trampoline only |
| callee | `0x0043ce10` | push_front construct (elem) |
| callee | `0x0043c830` | push_back construct (elem) |
| callee | `0x0043d300` | reverse ring move (unowned) |
| callee | `0x0043d390` | forward ring move / range copy (unowned) |

## Gaps

- Full formal English / PDB for p2/p6/p8.
- Nested move packing inside unowned `0043d300` / `0043d390`.
- Exact element-construct side effects of `0043ce10` / `0043c830` (OWN only as call targets).
- Runtime / bit-exact for all four grow-assign arms.
