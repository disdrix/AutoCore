# Annotated low-level: FUN_00584330 / Object_ApplySingleFxSlot_NoSlotFilter_ThenTeardown_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00584330` |
| VA | `0x00584330`–`0x005843af` (127 B) |
| System | client-fx / object FX slot apply + teardown chain |
| Date | 2026-07-29 (W26-K dual seal) |
| Dual | `reviews/A_aa_00584330_*` + `reviews/B_aa_00584330_*` |

## Machine-level notes

- Source: raw capture + live `decompile_function` / `read_memory` (no `disassemble_bytes`).
- Prefer bytes when decompiler omits ECX setup or mis-renders tail jmp as call.
- Sibling of dual-slot wild unit `Object_ApplyDualFxSlots_SlotFilterWild_Inferred` (`0x004c8110`), which **tail-jmps** here at `self-0x430`.
- Pack here is **filter char 0** (unrestricted / `0058e300` path) vs dual-slot's filter char 1 + wild `-1`.

## Offsets (relative to `this` / ESI)

| Offset | Role | Conf |
|---|---|---|
| `-0x88` | Single effect* slot (null-gated) | **Confirmed** |
| `-0x90` | Table-owner this for `FUN_00583f10` (`lea ecx,[esi-0x90]`) | **Confirmed** |
| `-0x8c` | VB-table ptr for local host* vector + teardown this-adjust | **Confirmed** |
| vb+`0xac` / vb+`0xb0` | Host* vector begin/end (stride 4) | **Confirmed** |

## Arg pack to sealed helpers

```
want_start      = 0
want_stop       = effect*   // non-null when gate open
use_slot_filter = 0         // → NDSpecialFX path uses FUN_0058e300
slot_filter     = -1        // unused when filter char 0
```

## Pseudocode (annotated; byte corrections in comments)

```c
// thiscall; no stack formals; tail jmp
void __thiscall FUN_00584330(void *self)
{
  int effect = *(int *)((char *)self - 0x88);
  if (effect != 0) {
    /* ECX = self - 0x90 */
    FUN_00583f10(/*table_owner*/ self-0x90, 0, effect, 0, -1);

    int vb = *(int *)(*(int *)((char *)self - 0x8c) + 4);
    int *it  = *(int **)((char *)self + vb + 0xac);
    int *end = *(int **)((char *)self + vb + 0xb0);
    if (it != end) {
      do {
        /* ECX = *it  (mov ecx,[edi]) */
        FUN_004b7030(/*host*/ *it, 0, effect, 0, -1);
        it = (int *)((char *)it + 4);
      } while (it != *(int **)(
          *(int *)(*(int *)((char *)self - 0x8c) + 4) + 0xb0 + (char *)self));
    }
  }
  /* ECX = (self-0x8c) + *(*(self-0x8c)+4); jmp — not call */
  FUN_005179e0(/*vbase-adjusted*/);
}
```

## Call graph

| Dir | VA | Name / role |
|---|---|---|
| Caller | `0x004c8110` | Dual FX slots; tail `lea ecx,[self-0x430]; jmp` |
| Caller | `0x004fd950`, `0x00505ba0`, `0x005a9220` | Other apply/teardown entry points |
| Callee | `0x00583f10` | Host-ptr table-pair fan-out (sealed W25-D) |
| Callee | `0x004b7030` | Owned-bit7 apply (sealed W24-D) |
| Tail | `0x005179e0` | Container teardown at +0x124/+0x128/+0x130 |

## Open questions

- Product C++ class / single-slot member English name.
- Exact product name of `FUN_005179e0` teardown.
- Runtime / bit-exact.
