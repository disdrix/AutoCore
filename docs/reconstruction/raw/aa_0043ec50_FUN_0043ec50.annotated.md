# Annotated low-level: FUN_0043ec50 / GuardedVector_RingAssignForward

| Field | Value |
|---|---|
| Stable ID | `aa_0043ec50` |
| VA | `0x0043ec50`–`0x0043ecdc` (**140 B** / `0x8C`) |
| System | STL / guarded ring-vector container |
| Date | 2026-07-29 (W33-J seal; scaffold 2026-07-23) |
| Inferred name | `GuardedVector_RingAssignForward` |

## Machine-level notes

- Source: raw capture + W33-J live re-verify.
- **7 stack args**, plain **`C3`** (caller cleans `0x1C`).
- Forward walk: while `(src_cont,src_idx) != (src_end_cont,src_end_idx)`: map both indices through ring block table, dword assign if pointers differ, `src_idx++`, `dst_idx++`.
- Out pair: `{dst_cont, final_dst_idx}`.
- Container reads: slots@+4, capacity@+8 only.
- Leaf used by thiscall insert family (`0043e080`, `0044e1c0`, `005441d0`, `0043df90`).
- Self-pointer skip (`if dst!=src`) avoids redundant store on overlapping same-slot.

## Pseudocode (annotated)

```c
// 7 cdecl stack args; plain RET; returns via out_pair write (EAX not load-bearing)
void GuardedVector_RingAssignForward(
    GuardedVectorIteratorPair* out_pair,          // param_1
    GuardedVectorHeader* src_container,           // param_2
    uint32_t src_index,                           // param_3  (advances)
    GuardedVectorHeader* src_end_container,       // param_4
    uint32_t src_end_index,                       // param_5
    GuardedVectorHeader* dst_container,           // param_6
    uint32_t dst_index)                           // param_7  (advances)
{
  for (; src_container != src_end_container || src_index != src_end_index;
       ++src_index) {
    // map src_index + dst_index through block map (>>2, wrap, lane)
    // if (dst_elem != src_elem) *dst_elem = *src_elem;
    ++dst_index;
  }
  out_pair->container = dst_container;
  out_pair->index = dst_index;
}
```

## Open questions

- Product/PDB class English.
- Exact relationship to clone `FUN_0043d390` (byte-level delta residual).
- Runtime / bit-exact on wrap edges.
