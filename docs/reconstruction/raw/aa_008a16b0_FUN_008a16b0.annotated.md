# Annotated low-level: CDlgQuickBar_CreateChildWidgets_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_008a16b0` |
| VA | `0x008a16b0` |
| Ghidra | `FUN_008a16b0` |
| Canonical | `CDlgQuickBar_CreateChildWidgets_Inferred` |
| System | `skills-abilities` / client QuickBar UI |
| Date | 2026-07-29 (W21-R dual seal) |

## Machine-level notes

- **ABI:** `mov edi,ecx` at entry → host `this` in ECX (`__thiscall` / decomp `__fastcall`). Void; MSVC SEH frame `LAB_009b98c0`.
- **Role:** Build/wire **CDlgQuickBar** child chrome: allocate+ctor children, parent-attach (`vtbl+0xa8`), load skins, stamp page-button indices 0/1 with labels `"1"`/`"2"`, hazard chrome XMLs, Y-reposition, rebind page columns, restore selection.
- **Vtable slot:** data xref `0x00a4d320` → this VA; nearby RTTI-ish strings `Show Quick Menu` / `Hide Quick Menu` @ `0x00a4d338`.
- **Not** slot activate/cast; **not** network. Sibling page select is `QuickBar_SelectPage_Inferred` (`0x008a0ed0`).
- Live decompile **≡** 2026-07-23 raw. Decompiler residual: ExceptionList restore via `pcVar9` (string clobber) is SEH noise; real restore uses saved ExceptionList.
- Prefer `read_memory` for constants/entry; no `disassemble_bytes`.

## Host slot map (dword index → byte off)

| Index | Off | Allocation / ctor | Notes |
|------:|----:|---|---|
| `0x199` | `+0x664` | `new(0x580)` + `FUN_0082f4a0` | mid/center widget (also used by `UI_HostRepositionAnchoredChild_Y`) |
| `0x189` | `+0x624` | `new(0x488)` + `FUN_007b5dd0` | anchor widget; z-order bump |
| `0x18a` | `+0x628` | `new(0x488)` + `FUN_007b5dd0` | second 0x488 chrome |
| `0x18b` | `+0x62c` | `new(0x4cc)` + `FUN_0079c860` | button-class child |
| `0x18c` | `+0x630` | `new(0x4cc)` + `FUN_0079c860` | button-class child |
| `0x19a` | `+0x668` | `new(0x520)` + `FUN_008c4f10` | attach + vtbl `+0x43c`/`+0xfc` |
| `0x18d` | `+0x634` | `new(0x4cc)` + `FUN_0079c860` | skin `i_d_qb_2d_btn_inc.xml`; id `0x9c42`; scale `0.5f` (`0x3f000000`) |
| `0x19b` | `+0x66c` | `new(0x554)` + `UI_QuickBar_CreateSlotButton` | column index `+0x500=0`; label `"1"` via child `+0x548` |
| `0x19c` | `+0x670` | `new(0x554)` + `UI_QuickBar_CreateSlotButton` | column index `+0x500=1`; label `"2"` |
| `0x19d` | `+0x674` | host `vtbl+0x404` load | `i_d_qb_2d_wnd_hazard_bar.xml` |
| `0x19f` | `+0x67c` | host `vtbl+0x404` load | `i_d_qb_2d_wnd_hazard_inc.xml` |
| `0x1a0` | `+0x680` | `new(0x4f8)` + `FUN_0097ffa0` | gauge; skin `i_d_qb_2d_wnd_hazard_gauge.xml`; vtbl `+0x3b0(1.0f)` |
| `0x19e` | `+0x678` | host `vtbl+0x404` load | `i_d_qb_2d_wnd_hazard_label.xml`; copy text into host `+0x523` |
| `0x1a1` | `+0x684` | `new(0x58c)` + `FUN_008b0ce0` | target of Y-reposition helper |

## Shared host counters (z-order)

Pattern after several attaches:

```text
host[0x1a] += 1;                         // child count
host[0x1d] = (int)(float)-count;         // signed display rank residual
if (host[0x1c] < count) host[0x1c] = count;
host.vtbl[+0x3f4](...);
```

## Page-button layout (indices 0 / 1)

Both use sealed `UI_QuickBar_CreateSlotButton` (`0x00825bf0`):

1. parent attach `host.vtbl+0xa8`
2. child `vtbl+0x74(id)` (`0x9c44` / `0x9c45`)
3. child `vtbl+0x43c()`
4. stamp `+0x500` = 0 or 1
5. if label child `+0x548`: `sprintf("%i", n)` → label `vtbl+0x1d8` + `+0x34c`
6. child `vtbl+0x444()`
7. position via `vtbl+0x134` size + host `+0x144/+0x145/+0x146` and scale `DAT_00d1e818 * DAT_00aaa67c` / `DAT_00d1e81c * DAT_00aaa678` (~0.000625 / ~0.000833), then `vtbl+0x118`

## Tail: rebind + selection restore

```text
FUN_008a0370()                              // Y-reposition +0x684 (sealed sibling)
page = host[0x143]                          // +0x50c current page
prevCol = (0<=page<10) ? DAT_00d1da38[page] : -1
FUN_008a04b0()
FUN_008a1110()                              // build/refresh 10 page columns (sibling residual)
FUN_008a0e10(host)                          // rebind page contents from DAT_00d1da60
host.vtbl[+0x448]()
DAT_00d1a9fc = -1                           // clear hover residual
if prevCol invalid OR col[prevCol]+0x548 == 0 OR == 6:
  FUN_008a0210(1)                           // auto-pick next valid column
else:
  FUN_00826780(1)                           // SetSelected — decomp may drop ECX=widget
if DAT_00d1b6d8: FUN_008a0770()
host.vtbl[+0x34c]()                         // final paint
```

Gate on `+0x548` (label child*) differs from SelectPage empty-mask `(+0x550 & +0x554)==-1` — seal CF as written.

## Constants

| Symbol | Value | Role |
|---|---|---|
| `DAT_00aaa678` | ≈ **0.000833f** | Y scale factor × `DAT_00d1e81c` |
| `DAT_00aaa67c` | ≈ **0.000625f** | X scale factor × `DAT_00d1e818` |
| `0x3f000000` | **0.5f** | inc button scale (`vtbl+0xfc`) |
| `0x3f800000` | **1.0f** | hazard gauge `vtbl+0x3b0` |
| `DAT_00d1da38[page]` | i32×10 | last selected column per page |
| `DAT_00d1a9fc` | i32 | hover column residual |
| `DAT_00d1b6d8` | flag | optional post-init path `FUN_008a0770` |

## Open questions

- Product C++ method name (CreateChildWidgets **Inferred**; RTTI class **Probable** `CDlgQuickBar`).
- Exact roles of early 0x488/0x4cc children (no plate strings on those sites).
- ECX handoff into `FUN_00826780` on restore path (decomp residual; sibling seal is thiscall on column widget).
- Runtime golden / bit-exact.
