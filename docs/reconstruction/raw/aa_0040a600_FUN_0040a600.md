# Raw capture: FUN_0040a600

| Field | Value |
|---|---|
| **Stable ID** | `aa_0040a600` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0040a600` |
| **Canonical name** | `FUN_0040a600` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int __thiscall FUN_0040a600(int param_1,int param_2,int param_3)

{
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  puStack_c = &LAB_009bd090;
  local_10 = ExceptionList;
  local_8 = 0;
  ExceptionList = &local_10;
  for (; param_2 != param_1; param_2 = param_2 + 0x18) {
    FUN_00412b10();
    param_3 = param_3 + 0x18;
  }
  ExceptionList = local_10;
  return param_3;
}
```

---

## MEGA-055 re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-055** |
| **Tools** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_callers` / `get_function_xrefs` + `get_assembly_context` + `read_memory` + callee/caller decompiles. **No** `disassemble_bytes`. No Launcher. |
| **Live decompile** | ≡ 2026-07-23 scaffold CF (SEH → for-loop +0x18 → `FUN_00412b10` → return dest) |
| **Body** | `0x0040a600`–`0x0040a664` inclusive (**101 B** / `0x65`); epilogue **`RET`** (`C3`); next bytes `6A 00 6A 00` |
| **Ghidra symbol** | still `FUN_0040a600` (no custom name) |
| **Classification** | worker |

### Live decompile (2026-08-05)

```c
int __thiscall FUN_0040a600(int param_1,int param_2,int param_3)

{
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  puStack_c = &LAB_009bd090;
  local_10 = ExceptionList;
  local_8 = 0;
  ExceptionList = &local_10;
  for (; param_2 != param_1; param_2 = param_2 + 0x18) {
    FUN_00412b10();
    param_3 = param_3 + 0x18;
  }
  ExceptionList = local_10;
  return param_3;
}
```

### Disassembly (ABI-critical)

```text
0040a600  PUSH EBP
0040a601  MOV EBP,ESP
0040a603  PUSH -0x1
0040a605  PUSH 0x9bd090              ; SEH handler LAB_009bd090
0040a60a  MOV EAX,FS:[0x0]
0040a610  PUSH EAX
0040a611  MOV dword ptr FS:[0x0],ESP
0040a618  PUSH ECX                   ; frame local (saved ESP slot)
0040a619  PUSH EBX
0040a61a  PUSH ESI
0040a61b  PUSH EDI
0040a61c  MOV dword ptr [EBP + -0x10],ESP
0040a61f  MOV EDI,dword ptr [EBP + 0xc]   ; dest
0040a622  MOV EBX,ECX                    ; src_end
0040a624  MOV ESI,dword ptr [EBP + 0x8]   ; src_first
0040a627  MOV dword ptr [EBP + -0x4],0x0  ; EH state = 0
0040a62e  MOV EDI,EDI                    ; align/nop
0040a630  CMP ESI,EBX
0040a632  JZ 0x0040a64b
0040a634  MOV ECX,ESI                    ; src → FUN_00412b10 ECX
0040a636  MOV EAX,EDI                    ; dest → FUN_00412b10 EAX
0040a638  CALL 0x00412b10                ; 6-dword (0x18) element copy
0040a63d  ADD EDI,0x18
0040a640  MOV dword ptr [EBP + 0xc],EDI  ; update dest (EH)
0040a643  ADD ESI,0x18
0040a646  MOV dword ptr [EBP + 0x8],ESI  ; update first (EH)
0040a649  JMP 0x0040a630
0040a64b  MOV dword ptr [EBP + -0x4],0xffffffff
0040a652  MOV EAX,EDI                    ; return dest_end
0040a654  MOV ECX,dword ptr [EBP + -0xc]
0040a657  MOV dword ptr FS:[0x0],ECX
0040a65e  POP EDI
0040a65f  POP ESI
0040a660  POP EBX
0040a661  MOV ESP,EBP
0040a663  POP EBP
0040a664  RET                            ; cdecl (caller cleans)
```

### ABI (sealed vs decompiler `__thiscall` mislabel)

| Formal | Location | Role |
|---|---|---|
| `src_end` | **ECX** → EBX | exclusive end of source range |
| `src_first` | stack `[EBP+0x8]` → ESI | begin of source range |
| `dest` | stack `[EBP+0xc]` → EDI | destination write cursor |
| stack+0x10 / +0x14 | pushed by all callers | **unused by body** (vector/EH residue; always 4 dwords pushed) |
| return | **EAX** = EDI after loop | `dest + n*0x18` |
| cleanup | **`RET` (`C3`)** | **cdecl**; callers `ADD ESP,0x10` |

### Callee `FUN_00412b10` (leaf, evidence only)

```text
00412b10  TEST EAX,EAX
00412b12  JZ  0x00412b36
00412b14  … copy [ECX+0..0x14] → [EAX+0..0x14]  (6 dwords / 0x18 B)
00412b36  RET
```

### Callers / xrefs (live)

| From | Parent | Kind |
|---|---|---|
| `0x00412946` | `FUN_00412730` | UNCONDITIONAL_CALL |
| `0x004129ad` | `FUN_00412730` | UNCONDITIONAL_CALL |
| `0x00412a90` | `FUN_00412a80` | UNCONDITIONAL_CALL |

- **Callers (2 fns):** `FUN_00412730`, `FUN_00412a80`
- **Xrefs:** 3 UNCONDITIONAL_CALL
- **Callees:** `FUN_00412b10` only

### Call-site ABI samples (`get_assembly_context`)

**`FUN_00412730` @ `0x00412946`:**
```text
PUSH [EBP-0x28] / PUSH EBX / PUSH EAX / PUSH ESI
MOV ECX, EDI
CALL 0x0040a600
ADD ESP, 0x10
```

**`FUN_00412730` @ `0x004129ad`:**
```text
… PUSH×4; MOV ECX,EDI; CALL 0x0040a600
MOV [EBX+0x8], EAX          ; vector.end = returned dest_end
… CALL 0x00409ed0; ADD ESP,0x14
```

**`FUN_00412a80` @ `0x00412a90` (trampoline):**
```text
; wrapper RET 8; repacks ECX/EDX/stack → CALL 0x0040a600; ADD ESP,0x10; RET 8
```

### Parent context (not dualled)

`FUN_00412730` is SkillSet **vector insert/reallocate** (stride **0x18**, capacity `0xAAAAAAA`, calls dualed `SkillSet_GetEntryCount` `0x00402d80`, dualed `SkillSet_Vector_UninitializedFillN_0x18_Inferred` `0x00406f00`, and this Ucopy). Element size matches dualed count helper `(end-begin)/0x18`.

### Entry bytes (`read_memory` 110 B @ `0x0040a600`)

```text
55 8B EC 6A FF 68 90 D0 9B 00 64 A1 00 00 00 00
50 64 89 25 00 00 00 00 51 53 56 57 89 65 F0 8B
7D 0C 8B D9 8B 75 08 C7 45 FC 00 00 00 00 8B FF
3B F3 74 17 8B CE 8B C7 E8 D3 84 00 00 83 C7 18
89 7D 0C 83 C6 18 89 75 08 EB E5 C7 45 FC FF FF
FF FF 8B C7 8B 4D F4 64 89 0D 00 00 00 00 5F 5E
5B 8B E5 5D C3 …
```

### Named promotion (this wave)

| Prior scaffold | Ruling |
|---|---|
| `FUN_0040a600` | Ghidra / twin keep |
| `Named_CalleeOf_Skill_Uses_SkillSet_GetEntryCount_0040a600` | **Retire** — chain-of-caller; wrong parent seed |
| **`SkillSet_Vector_UninitializedCopy_0x18_Inferred`** | **Promote** — SEH Ucopy of 0x18 SkillSet entries; product demangle open |

### Decompile caveats sealed

1. Ghidra `__thiscall` + 3-int signature is **wrong**: real ABI is **ECX=end + cdecl stack first/dest** (+2 unused pushes).
2. Decompiler elides EAX/ECX setup into `FUN_00412b10()` with no args — bytes show `MOV ECX,ESI; MOV EAX,EDI; CALL`.
3. `param_1` in decompile is **ECX=end**, not a C++ `this` object method receiver in the SkillSet sense (no `this+field` loads in body).
