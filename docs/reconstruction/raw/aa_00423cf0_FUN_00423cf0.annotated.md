# Annotated low-level: AssResolverA_NestedHash_Ctor_Inferred (FUN_00423cf0)

| Field | Value |
|---|---|
| Stable ID | `aa_00423cf0` |
| VA | `0x00423cf0`–`0x00423d51` (**98 B**) |
| System | assets / assManager / resolver A nested member |
| Date | 2026-07-29 (W32-D dual seal) |
| Ghidra name | `FUN_00423cf0` |
| Inferred name | `AssResolverA_NestedHash_Ctor_Inferred` |

## Machine-level notes

- Source: raw capture + W32-D live `decompile_function` / `read_memory`.
- Prefer bytes when decompiler ABI is incomplete (`undefined FUN_00423cf0(void)` is wrong).
- ABI: stack **`this*`**, **`RET 4`**, returns **`this` in EAX** (same convention as parent `AssManager_ResolverA_Ctor_Inferred`).
- Nested member of AssResolverA at outer `+0x08`; span **0x28** to outer CS at `+0x30`.
- Twin structural role of `FUN_00469db0` (resolver-B / AssResolverCore nested member — owned elsewhere).

## Pseudocode (annotated)

```c
// stack(self*); RET 4; returns self
void* AssResolverA_NestedHash_Ctor_Inferred(void* self /*stack*/)
{
  // SEH LAB_009bcd6b
  void* sentinel = FUN_00423d60();       // new(0x28); *p=p; p[1]=p
  *(void**)((char*)self + 0x08) = sentinel;
  *(uint32_t*)((char*)self + 0x0c) = 0;  // count / size

  void* fill = sentinel;
  FUN_004646e0((char*)self + 0x10, 9, &fill);
  // → bucket pointer vector capacity 9 at self+0x14/+0x18/+0x1c, filled w/ sentinel

  *(uint32_t*)((char*)self + 0x20) = 1;
  *(uint32_t*)((char*)self + 0x24) = 1;
  return self;
}
```

## Field map (nested member)

| Off | Write | Role (structural) |
|---|---|---|
| +0x00 | (none) | unused / later |
| +0x04 | (none) | unused / later |
| +0x08 | sentinel* | circular list/hash head node (`FUN_00423d60`) |
| +0x0c | 0 | size / count |
| +0x10 | (base) | vector object base for `FUN_004646e0` |
| +0x14/+0x18/+0x1c | buckets | begin / end / cap_end (capacity 9 × 4 B) |
| +0x20 | 1 | flag / refcount structural |
| +0x24 | 1 | flag / refcount structural |

## Open questions

- Product/PDB type name for the nested hash/map.
- Exact meaning of +0x20/+0x24 (=1) and unwritten +0x00/+0x04.
- Full product contract of `FUN_004646e0` / `FUN_00423d60` (not owned).
