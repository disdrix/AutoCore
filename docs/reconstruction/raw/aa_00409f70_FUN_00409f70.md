# Raw capture: FUN_00409f70

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409f70` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409f70` |
| **Canonical name** | `FUN_00409f70` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00409f70(undefined4 *param_1)

{
  undefined4 *in_EAX;
  
  if (in_EAX != (undefined4 *)0x0) {
    *in_EAX = *param_1;
    in_EAX[1] = param_1[1];
    in_EAX[2] = param_1[2];
    in_EAX[3] = param_1[3];
  }
  return;
}
```

---

## R10-023 re-verify (2026-08-05) — append only

| Field | Sealed value |
|---|---|
| **Body** | `0x00409f70`–`0x00409f8a` inclusive (**27 B** / `0x1B`); pad `CC` after |
| **Bytes** | `85 C0 74 16 8B 11 89 10 8B 51 04 89 50 04 8B 51 08 89 50 08 8B 49 0C 89 48 0C C3` |
| **ABI** | **EAX** = dest `T*`; **ECX** = src `const T*`; plain **`RET`** (`C3`); no stack args |
| **Semantics** | Null-dst no-op; else copy **4 dwords** (16 B / **0x10**) dword0/4/8/0xC |
| **Callees** | none (leaf) |
| **Callers (2)** | `FUN_00409e20` ConstructN Elem0x10 @ `0x00409e58` (`MOV ECX,EBX` template; `MOV EAX,ESI` dest; dest `+0x10`); `FUN_0040a670` uninit_copy Elem0x10 @ `0x0040a6a8` (`MOV ECX,ESI` src; `MOV EAX,EDI` dest; both cursors `+0x10`) |
| **Xrefs** | 2 UNCONDITIONAL_CALL (`00409e58`, `0040a6a8`) |
| **CF metrics** | size 27, CC 2, 3 BB, 0 loops, 0 calls, 1 ret |
| **Name** | `StdVector_PodCopyElement_0x10_Inferred` (Ghidra `FUN_00409f70`) |
| **Decompile note** | Decomp shows `__fastcall param_1` = src in **ECX** and phantom `in_EAX` dest; machine is **EAX dest / ECX src** (not EDX). Peer `00409f30` is **EAX/EDX / 10 dwords**; peer `00409f50` is **EAX/ECX / 3 dwords** |
| **Tools** | Ghidra HTTP `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_callers` + `get_function_xrefs` + `read_memory` + `analyze_control_flow`. **No** `disassemble_bytes`. |

### Assembly (from disassemble_function + read_memory; no disassemble_bytes)

```text
00409f70  TEST EAX, EAX
00409f72  JZ   00409f8a
00409f74  MOV  EDX, dword ptr [ECX]
00409f76  MOV  dword ptr [EAX], EDX
00409f78  MOV  EDX, dword ptr [ECX+0x4]
00409f7b  MOV  dword ptr [EAX+0x4], EDX
00409f7e  MOV  EDX, dword ptr [ECX+0x8]
00409f81  MOV  dword ptr [EAX+0x8], EDX
00409f84  MOV  ECX, dword ptr [ECX+0xc]
00409f87  MOV  dword ptr [EAX+0xc], ECX
00409f8a  RET
```

### Call-site memory (ABI seal)

```text
; ConstructN Elem0x10 loop @ 00409e50 (parent dual MEGA-044)
TEST EDI, EDI
JBE  done
MOV  ECX, EBX      ; value* (fixed template)
MOV  EAX, ESI      ; dest*
CALL 00409f70      ; @ 00409e58
SUB  EDI, 1
...
ADD  ESI, 0x10     ; stride 16
JMP  loop

; uninit_copy Elem0x10 loop @ 0040a6a0 (FUN_0040a670)
CMP  ESI, EBX      ; src_begin vs src_end
JZ   done
MOV  ECX, ESI      ; src
MOV  EAX, EDI      ; dest
CALL 00409f70      ; @ 0040a6a8
ADD  EDI, 0x10
ADD  ESI, 0x10
JMP  loop
; returns advanced dest (EAX=EDI) after loop
```

### Live decompile (2026-08-05) ≡ raw CF

```c
void __fastcall FUN_00409f70(undefined4 *param_1)
{
  undefined4 *in_EAX;
  
  if (in_EAX != (undefined4 *)0x0) {
    *in_EAX = *param_1;
    in_EAX[1] = param_1[1];
    in_EAX[2] = param_1[2];
    in_EAX[3] = param_1[3];
  }
  return;
}
```

### analyze_function_complete (sealed summary)

- classification: **leaf**
- xref_count: **2** (`00409e58`, `0040a6a8`)
- callers: `FUN_00409e20`, `FUN_0040a670`
- callees: none
- locals: `param_1` storage `register:00000004` (ECX); `in_EAX` phantom dest `register:00000000`
