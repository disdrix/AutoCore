# Annotated low-level: FUN_00971900

| Field | Value |
|---|---|
| Stable ID | `aa_00971900` |
| VA | `0x00971900`–`0x00971a19` (**282 B**) |
| Structural | `AssPreloader_IsKeyTrackedRecursive_Inferred` |
| System | assPreloader / asset preload pipeline |
| Date | 2026-07-23 scaffold; **W31-K 2026-07-29** dual seal |

## Machine-level notes

- **ABI (bytes):** `MOV EBP,ECX` (this); stack `key*`, `recurse`; three exits all **`RET 8`**; **AL** bool.
- SEH: `LAB_009b09cf`, EH state starts `-1`, set `0` around local-ring lifetime.
- Always **`FUN_00971480(this)`** first (sibling; logs `assPreloader.cpp`).
- Contains on **`this+0x7c`**: `lea eax,[ebp+0x7c]` + `FUN_0043e5b0` (W30-B sealed).
- Recurse path: zero local ring; `FUN_00744360(key, &local)`; ring walk; self-call with `mov ecx,ebp`; cleanup `FUN_005b2ba0` (ring free).
- Decompiler recursive call drops ECX — **bytes restore** `mov ecx,ebp` before `call self`.

## Sealed control flow

```
FUN_00971480(this)
if ContainsFirstEqual(this+0x7c, key*): return 1
if recurse == 0: return 0
local_ring = empty
FUN_00744360(key, &local_ring)
for child in local_ring:
  if IsKeyTrackedRecursive(this, child, recurse): clear local; return 1
clear local; return 0
```

## Pseudocode (annotated copy of raw)

```c
undefined4 __thiscall FUN_00971900(this, key*, recurse)
{
  // param_1 = this (ECX)
  // Contains uses this+0x7c (bytes), not decompiler-default
  // Self-recursion preserves ECX=this
  ...
}
```

## Gaps

- Product/PDB method English.
- Nested drain `FUN_00971480` and dep collector `FUN_00744360` full dual (not owned).
- Meaning of recurse at product layer (caller `FUN_004ed310` uses 0).
- Runtime / bit-exact.
