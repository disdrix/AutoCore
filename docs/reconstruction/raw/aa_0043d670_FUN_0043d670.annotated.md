# Annotated low-level: FUN_0043d670

| Field | Value |
|---|---|
| Stable ID | `aa_0043d670` |
| VA | `0x0043d670` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_0043d670`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
void __thiscall FUN_0043d670(int param_1,undefined4 param_2)

{
  void *pvVar1;
  uint uVar2;
  uint uVar3;
  
  if (((*(int *)(param_1 + 0xc) + *(int *)(param_1 + 0x10) & 3U) == 0) &&
     (*(uint *)(param_1 + 8) <= *(int *)(param_1 + 0x10) + 4U >> 2)) {
    FUN_0043d990(1);
  }
  uVar3 = *(int *)(param_1 + 0xc) + *(int *)(param_1 + 0x10);
  uVar2 = uVar3 >> 2;
  if (*(uint *)(param_1 + 8) <= uVar2) {
    uVar2 = uVar2 - *(uint *)(param_1 + 8);
  }
  if (*(int *)(*(int *)(param_1 + 4) + uVar2 * 4) == 0) {
    pvVar1 = operator_new(0x10);
    *(void **)(*(int *)(param_1 + 4) + uVar2 * 4) = pvVar1;
  }
  FUN_0043d420(*(int *)(*(int *)(param_1 + 4) + uVar2 * 4) + (uVar3 & 3) * 4,param_2);
  *(int *)(param_1 + 0x10) = *(int *)(param_1 + 0x10) + 1;
  return;
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.

---

# Annotated append — W31-I 2026-07-29

## Sealed identity

| Field | Value |
|---|---|
| Canonical name | `GuardedVector_PushBack_Thiscall` (**Inferred**) |
| Body | `0x0043d670`–`0x0043d6e6` (**119 B**) |
| ABI | **ECX=container**; stack `const uint32_t* value`; **RET 0x4** |
| System | STL / guarded ring-deque container |
| Dual | OWN-ONLY W31-I |
| Grow callee | `GuardedVector_GrowBlockMap_Thiscall` (`0x0043d990`, W30-A) |
| Assign callee | `FUN_0043d420` (null-checked dword copy) |
| Twin | W29-B `GuardedVector_PushBack` (`0x0043c830`, EAX + EBX grow) |

## Semantics

Ring-deque **push_back** of one dword under thiscall ABI:
1. Map-grow when `(begin+size)&3==0` and `capacity <= (size+4)>>2`.
2. Write index `abs = begin+size`; page wrap `page = abs>>2`; if `capacity<=page` then `page-=capacity`.
3. Lazy `operator_new(0x10)` if page null.
4. `FUN_0043d420(slot, value*)` → `*slot=*value` if slot non-null.
5. `size++`; **begin unchanged**.

## Open residual

- Product/PDB English.
- Runtime / bit-exact.
- Element type beyond dword POD.
- Null-`operator_new` still advances size (bytes).
