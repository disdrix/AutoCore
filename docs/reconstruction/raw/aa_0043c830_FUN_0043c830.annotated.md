# Annotated low-level: FUN_0043c830 / GuardedVector_PushBack

| Field | Value |
|---|---|
| Stable ID | `aa_0043c830` |
| VA | `0x0043c830`–`0x0043c8a2` (**115 B**) |
| System | STL / guarded ring-deque container |
| Date | 2026-07-29 (OWN-ONLY dual W29-B) |
| Name | `GuardedVector_PushBack` (**Inferred**) |

## Machine-level notes

- Source: raw capture + live re-verify append for `aa_0043c830`.
- Prefer bytes when decompiler omits register ABI (container is **EAX** → **EBX**).
- GrowBlockMap (`FUN_0043c9b0`) requires **EBX=container**; entry `MOV EBX,EAX` seals that contract.
- Write index is absolute `begin+size`; page wraps when `page >= capacity`.
- **Does not** update `begin` — only `size++` (true push_back polarity vs `0043ce10`).

## Pseudocode (annotated)

```c
// EAX = container; stdcall 1: value*; RET 0x4
void GuardedVector_PushBack(uint32_t* value /*stack*/)
{
  // in_EAX = container
  if (((*(int*)(container + 0xc) + *(int*)(container + 0x10) & 3U) == 0) &&
      (*(uint*)(container + 8) <= (*(int*)(container + 0x10) + 4U) >> 2)) {
    GuardedVector_GrowBlockMap(1); // EBX=container
  }
  uint abs = *(int*)(container + 0xc) + *(int*)(container + 0x10); // begin+size
  uint page = abs >> 2;
  if (*(uint*)(container + 8) <= page) {
    page -= *(uint*)(container + 8); // wrap by capacity
  }
  void** slots = *(void***)(container + 4);
  if (slots[page] == 0) {
    slots[page] = operator_new(0x10);
  }
  uint32_t* slot = (uint32_t*)((char*)slots[page] + (abs & 3) * 4);
  if (slot != 0) {
    *slot = *value;
  }
  *(int*)(container + 0x10) += 1; // size++ only
}
```

## Layout (family)

| Offset | Field |
|---|---|
| `+0x04` | page/slot pointer array |
| `+0x08` | capacity (pages) |
| `+0x0c` | begin (absolute element index) |
| `+0x10` | size (element count) |

## Open questions

- Product/PDB class English.
- Element type beyond dword width (POD copy only).
- Runtime golden under multi-caller parents (`FUN_0043cb00`, `FUN_0043cf40`, others).
