# Annotated low-level: FUN_005d1d20 → Math_QuatExtractForwardZ_OutFirst_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005d1d20` |
| VA | `0x005d1d20`–`0x005d1d93` (116 B) |
| System | math / quaternion basis extract (+Z) |
| Date | 2026-07-29 (W27-J live seal) |
| Twin algebra | `Math_QuatExtractForwardZ` @ `0x004e8a40` — **same closed form**, **arg order swapped** |

## Machine-level notes

- **ABI:** `__cdecl` — stack `[esp+4]` = `out float[4]*`, `[esp+8]` = `quat float[4]*`; bare **`RET`**.
- **Leaf:** no calls; single basic block.
- **Quat layout:** XYZW at indices 0..3 (`*q`=x, `[1]`=y, `[2]`=z, `[3]`=w).
- **Constants:**
  - `g_flLevelUpUiBase_Inferred` @ `0x00a10e74` = **2.0f** (quat double-cover factor; UI name is pool misnomer)
  - `g_flOne` @ `0x00a0f2a0` = **1.0f**
- **Output:** rotation-matrix **column 2** (local +Z → world) as float4 with **W=0**. No re-normalize.

## Closed form

```
// q = (x,y,z,w) unit (assumed by callers; not enforced here)
out[0] = 2 * (z*x + y*w)     // = 2*(xz + yw)
out[1] = 2 * (z*y - x*w)     // = 2*(yz - xw)
out[2] = 1 - 2 * (x*x + y*y)
out[3] = 0
```

Identity `(0,0,0,1)` → `(0,0,1,0)`.

## Pseudocode (annotated ≡ raw ≡ live)

```c
// Math_QuatExtractForwardZ_OutFirst_Inferred — Ghidra FUN_005d1d20
// Args: (out, quat) — reverse of sealed Math_QuatExtractForwardZ(quat, out) @ 0x004e8a40
void FUN_005d1d20(float *out /* param_1 */, float *quat /* param_2 XYZW */)
{
  float two = g_flLevelUpUiBase_Inferred; // 2.0f @ 0xa10e74
  float z = quat[2];
  float x = *quat;
  float z_col = g_flOne - (*quat * *quat + quat[1] * quat[1]) * g_flLevelUpUiBase_Inferred;
  float w = quat[3];
  float y = quat[1];
  out[1] = (quat[2] * quat[1] - quat[3] * *quat) * g_flLevelUpUiBase_Inferred; // 2(yz - xw)
  *out   = (w * y + z * x) * two;                                                 // 2(xz + yw)
  out[2] = z_col;
  out[3] = 0.0f;
  return;
}
```

## Callers

| Site | Parent | Role |
|---|---|---|
| `0x005cf312` | `FUN_005cedf0` / `CVOGHBAI_UpdateAimOffset_Inferred` | pose object orient → forward basis for aim-offset build |

Sole xref this pass — dedicated alt-ABI twin used from HB AI aim path (drive stack uses `0x004e8a40`).

## Open / residual

- Product symbol — none (**Inferred**).
- Runtime golden (identity / 90° yaw) — optional; algebra sealed vs sibling dual.
