# Annotated low-level: FUN_00573a90 → IgnoreList_FindEntryByTfid_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00573a90` |
| VA | `0x00573a90`–`0x00573ae7` exclusive (**87 B** / `0x57`) |
| Canonical | `IgnoreList_FindEntryByTfid_Inferred` |
| Ghidra | `FUN_00573a90` |
| System | social / ignore list (partition nest: skills-abilities residual; live role UI/social) |
| Date | 2026-08-05 (R12-025 dual) |
| Prior scaffold | 2026-07-23 |

## Machine-level notes

- **ABI:** `__thiscall` — ECX = list host; stack `int tfid_lo, int tfid_hi`; **`RET 0x8`**; EAX = entry* or 0.
- **Leaf:** no callees; pure pointer-vector linear scan.
- **Vector:** begin `*(this+0x38)`, end `*(this+0x3c)`; count = `(end-begin)>>2` if begin≠0 else 0.
- **Key:** exact int equality of `entry+0x08`/`entry+0x0c` vs stack TFID pair.
- **Hit:** return first matching entry pointer; **miss/empty:** return 0.
- Prefer assembly when decompiler conflicts — here decompile ≡ disasm CF.

## Pseudocode (named)

```c
// __thiscall; RET 0x8
void* __thiscall IgnoreList_FindEntryByTfid_Inferred(void* this, int tfid_lo, int tfid_hi)
{
  int i = 0;
  for (;;) {
    int count = (*(int*)((char*)this + 0x38) == 0)
      ? 0
      : (*(int*)((char*)this + 0x3c) - *(int*)((char*)this + 0x38)) >> 2;
    if (count <= i) break;
    void* entry = *(void**)(*(int*)((char*)this + 0x38) + i * 4);
    if (*(int*)((char*)entry + 8) == tfid_lo &&
        *(int*)((char*)entry + 0xc) == tfid_hi)
      return entry;
    i++;
  }
  return 0;
}
```

## Call graph (evidence only; callers not dualled here)

```text
FUN_0080eeb0  remove-from-ignore residual
  └─ FUN_00573a90(tfid_lo, tfid_hi)
       hit + entry+0x10 name → "has been removed from your ignore list"
       then FUN_00573ce0(self_tfid, peer_tfid)  // table mutate residual

FUN_0080a170  trade request residual
  └─ FUN_00573a90(peer_tfid) → hit auto-refuse sector ack

FUN_0080fa50  convoy invite residual
  └─ FUN_00573a90(inviter_tfid) → hit auto-decline 0x8006; miss invite UI

Client_RecvBroadcast (0x00810a80)  [dualed]
  └─ FUN_00573a90(object_tfid) → miss only → world float-text bubble

FUN_005740f0  insert-if-missing residual
  └─ FUN_00573a90 → 0 then FUN_005746e0(&pair)
```

## Open questions

- Product class name for `DAT_00d1da2c` multi-list host and ignore entry struct.
- Whether list is sorted (body is O(n) linear — not binary search).
- Full entry packing beyond +8/+0xc TFID and +0x10 name.
