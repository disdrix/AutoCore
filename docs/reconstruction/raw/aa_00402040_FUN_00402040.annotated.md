# Annotated low-level: FUN_00402040 → BasicStringFlag_CopyCtor_EdiSrc_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00402040` |
| VA | `0x00402040`–`0x0040208c` inclusive (**77 B** / `0x4D`) |
| System | missions-progression (partition parent chain); **unit role** = shared MSVC aggregate copy-ctor |
| Date | 2026-08-05 (MEGA-080 dual refresh) |
| Canonical name | `BasicStringFlag_CopyCtor_EdiSrc_Inferred` |
| Ghidra | `FUN_00402040` |
| Rejected scaffold | `Named_CalleeOf_Named_CalleeOf_Mission_Attempting_to_open_null_d_00402040` |

## Machine-level notes

- **Purpose:** Copy-construct a `{ std::basic_string<char> @+0 (size 0x1c); uint8_t flag @+0x1c }` aggregate.
- **ABI:** stack dest*; **EDI** = source*; returns dest* in EAX; **`RET 4`**.
- **Callee:** IAT `[0x009c62ec]` = `basic_string` copy-ctor (`??0?$basic_string@...@@QAE@ABV01@@Z`).
  - `PUSH EDI` supplies the `const string&` stack arg; `MOV ECX,ESI` is thiscall dest.
- **SEH:** frame via `LAB_009bbfb9` (shared with twin ctor `00401d30` and string dtor `004020f0`).
- **Callers (2):**
  1. `FUN_00401fe0` @ `0x0040200a` — map/tree node value at `node+0xc`; EDI from grandparent.
  2. `FUN_00980160` @ `0x009801c0` — after `FUN_00401d30` builds a temp StringFlag; `MOV EDI,EAX` then copy into local.
- Prefer assembly for register contract (`unaff_EDI` in decompiler is real ABI, not noise).

## Pseudocode (annotated)

```c
// dest on stack; source in EDI (register ABI)
BasicStringFlag *
BasicStringFlag_CopyCtor_EdiSrc_Inferred(BasicStringFlag *dest /* stack */)
{
  // SEH install (LAB_009bbfb9)
  // ESI = dest
  // PUSH EDI; MOV ECX,ESI; CALL [IAT basic_string copy-ctor]
  basic_string_copy_ctor(&dest->str, &src_EDI->str);
  dest->flag = src_EDI->flag;   // byte @ +0x1c
  // SEH teardown
  return dest;                  // EAX = ESI; RET 4
}
```

## Layout (sealed)

| Off | Width | Role |
|----:|------:|------|
| `+0x00` | `0x1C` | `std::basic_string<char>` (MSVC size in this binary) |
| `+0x1C` | 1 | trailing flag / tag byte (POD; no dtor) |

Object footprint at least **0x1D**; callers often treat as **0x20**-aligned subobject inside larger nodes.

## Twin / family

| VA | Name / role |
|---|---|
| `0x00401d30` | construct from `(string*, char*)` — **RET 0xC** |
| `0x004020f0` | string-only dtor (flag POD) |
| `0x00401fe0` | parent node ctor embeds this at `+0xc` |

## Open questions

- Product English for the flag byte (bool? enum? color tag?) — keep `_Inferred`.
- Full product map/node type for `FUN_00401fe0` value_type (string+flag + extra bytes @ `+0x2c` / isnil @ `+0x2d`).
- Runtime / bit-exact / differential: open (no Launcher).
