# Annotated low-level: CVOGHBRespawnCharacter_ctor (FUN_005b3210)

| Field | Value |
|---|---|
| Stable ID | `aa_005b3210` |
| VA | `0x005b3210` |
| Body | `0x005b3210`–`0x005b325d` (77 bytes) |
| Canonical name | `CVOGHBRespawnCharacter_ctor` |
| Prior scaffold | `FUN_005b3210` |
| System | `heartbeat` / client death-respawn |
| Date | `2026-07-29` (W23-S dual seal) |
| Tools | Ghidra `decompile_function` + `read_memory` only |

---

## Purpose

Construct a **`CVOGHBRespawnCharacter`** heartbeat action:

1. `CVOGHBBase_ctor(this)`.
2. Install RTTI-backed vtbl **`PTR_FUN_009d957c`** (`.?AVCVOGHBRespawnCharacter@@`).
3. `CVOGHBBase_SetPeriodAndCounter(this, 1, true)` — sentinel/counter = 1 at `+0x0C` / `+0x10`.
4. `this+0x08 = periodMs` (caller supplies pulse period; death path uses **300000**).
5. Attach owner: null if no object; else **this-adjust** from multi-inheritance object pointer into the owner subobject expected by `CVOGHBBase_AttachOwnerObject`.

Sole caller: local-death UI sequence allocates `0x24` bytes, constructs with local character + 300000 ms, enqueues on client HB list, and `Start`s it.

---

## Annotated control flow

```c
// ECX = this (CVOGHBRespawnCharacter*); stack: pObject, periodMs; ret 8
CVOGHBBase_ctor(this);
*this = &PTR_FUN_009d957c;                 // CVOGHBRespawnCharacter vtbl
CVOGHBBase_SetPeriodAndCounter(this, 1, true);  // +0x0C = +0x10 = 1
*(int*)(this + 0x08) = periodMs;           // pulse period (param_3)

if (pObject == NULL) {
  CVOGHBBase_AttachOwnerObject(this, NULL);
  return this;
}
// MSVC complete-object / MI this-adjust:
// owner = pObject + 4 + *(*(pObject + 4) + 4)
CVOGHBBase_AttachOwnerObject(this, AdjustOwnerSubobject(pObject));
return this;
```

### Byte-backed notes (from `read_memory`)

| Observation | Evidence |
|---|---|
| Body ends `ret 8` twice (null / non-null arms) | hex `…c20800…c20800` |
| Vtbl immediate `7c 95 9d 00` | `c7067c959d00` → `0x009d957c` |
| SetPeriodAndCounter pushed `1,1` | `6a016a01` then call |
| `mov [esi+8], eax` after loading stack period | `894608` |
| Null branch: push eax (0), Attach, ret 8 | `50 … e8… c20800` |
| Non-null: `mov ecx,[eax+4]; mov edx,[ecx+4]; lea eax,[edx+eax+4]` | `8b4804 8b5104 8d440204` |

---

## RTTI seal

| Step | Address / value |
|---|---|
| Vtbl | `0x009d957c` |
| COL (vtbl[-1]) | `0x00aaffac` |
| type_info | `0x00af3e88` |
| Name string | `.?AVCVOGHBRespawnCharacter@@` |

---

## Caller contract (`0x00802170`)

```
if (client+0xd38 HB list non-null):
  p = operator_new(0x24)
  p = CVOGHBRespawnCharacter_ctor(p, *(client+0xe98), 300000)
  CVOGHBList_Enqueue(list, p)
  CVOGHBBase_Start(p)
```

- **300000 ms** = 5 minutes pulse/schedule input to `+0x08`.
- Alloc size **0x24** matches thin HB subclass (base fields only; no skill blob).

---

## Open

1. Exact product English of OnHeartBeat / fire path for this class (vfunc residual).
2. Semantic of this-adjust expression vs alternate owner shapes — High that it yields owner subobject for AttachOwner; product type of `pObject` open beyond “character-like from client+0xe98”.
3. Runtime / bit-exact / differential — open.
