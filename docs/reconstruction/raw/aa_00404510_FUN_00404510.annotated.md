# Annotated low-level: UiToast_UninitializedCopy_0x98

| Field | Value |
|---|---|
| Stable ID | `aa_00404510` |
| VA | `0x00404510` |
| Body | `0x00404510`–`0x0040452f` (32 bytes) |
| Canonical name | `UiToast_UninitializedCopy_0x98` |
| Ghidra name | `FUN_00404510` |
| System | `missions-progression` / shared UI toast vector helpers |
| Date | `2026-07-29` (W22-G dual seal) |

## Machine-level notes

- Thin **relocate / uninitialized_copy** trampoline for toast POD (**stride 0x98**).
- Body only reshuffles args and calls `FUN_004047a0`, which walks `[srcBegin, srcEnd)` stepping **`+0x98`**, `UiToast_PodCopyElement_0x98` (`FUN_00404710`, **0x26 dwords**) into the destination cursor, returns advanced dest end in EAX.
- **ABI:** three stack args, **`ret 0x0C`**. Bytes push five dwords into cdecl worker then `add esp,0x14` — same phantom pattern as fill-n twin `0x00404130` and floater copy trampoline `0x004044c0`.
- Sole parent: `UiToastVector_InsertN` (`0x00403980`) — four call sites on realloc relocate + in-place tail/mid shift.
- **Not** fill-from-template (`0x00404130` / ConstructN); **not** the pod leaf (`0x00404710`); **not** assign-patch helper (`0x00404530`).

## Pseudocode (annotated)

```c
// Structural: std::uninitialized_copy for toast POD width 0x98
// Stack formals (ret 0x0C); decompiler thiscall/ECX formals are noise.
void *UiToast_UninitializedCopy_0x98(void *srcBegin, void *srcEnd, void *dst)
{
  // trampoline → FUN_004047a0(srcBegin, srcEnd, dst)
  // worker:
  //   for (p = srcBegin; p != srcEnd; p += 0x98) {
  //     UiToast_PodCopyElement_0x98(dst, p);  // 0x26 dwords
  //     dst = (uint8_t*)dst + 0x98;
  //   }
  //   return dst;
  return FUN_004047a0(srcBegin, srcEnd, dst);
}
```

## Bytes (`read_memory`)

```text
00404510  8B44240C        mov  eax, [esp+0xC]   ; arg3 (dst)
00404514  8B542408        mov  edx, [esp+0x8]   ; arg2 (srcEnd)
00404518  50              push eax
00404519  8B442408        mov  eax, [esp+0x8]   ; original arg1 (srcBegin)
0040451D  51              push ecx              ; phantom ECX
0040451E  8B4C2414        mov  ecx, [esp+0x14]  ; arg3 again
00404522  51              push ecx
00404523  52              push edx              ; srcEnd
00404524  50              push eax              ; srcBegin
00404525  E876020000      call FUN_004047a0     ; → 004047a0
0040452A  83C414          add  esp, 0x14
0040452D  C20C00          ret  0x0C
```

Hex body: `8b44240c8b542408508b442408518b4c2414515250e87602000083c414c20c00`

## Call graph

| Direction | VA | Role |
|---|---|---|
| Callee | `0x004047a0` | range copy loop +0x98 (scaffold; dual residual) |
| Nested | `0x00404710` | `UiToast_PodCopyElement_0x98` (sealed) |
| Caller | `0x00403980` | `UiToastVector_InsertN` (×4 sites) |
| Twin | `0x004044c0` | `CombatFloater_UninitializedCopy_0x38_Inferred` |

## Open questions

1. Product/PDB symbol (`uninitialized_copy` / compiler helper).
2. Optional dual seal of worker `FUN_004047a0` (not OWN this wave).
3. Runtime / bit-exact / differential.
