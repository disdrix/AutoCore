# Annotated low-level: FUN_00756320 / HostBase_EnsureAuxPtrs_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00756320` |
| VA | `0x00756320`–`0x007563bf` exclusive (**159 B** / `0x9F`) |
| System | client / host base (FUN_007560d0 family) |
| Date | 2026-07-29 (W32-Q) |

## Machine-level notes

- SEH frame `LAB_009afd8d`; `PUSH EBX; PUSH ESI; MOV ESI, ECX`.
- Early fail if `[ESI+4]` or `[ESI+8]` non-zero → `OR EAX,-1; RET 4`.
- Flag test: `TEST BYTE [ESP+0x18], 1` (stack formal after frame).
- Bit0 path: `MOV EBX, [DAT_00d1f624]`; `CALL FUN_00986070`; `MOV [ESI+4], EAX`.
- Always: `PUSH 0x14; CALL operator_new`; on success `CALL FUN_00442c80` → `MOV [ESI+8], EAX`; on OOM store 0 at `+8`.
- Success/OOM both return `0` with `RET 4`.
- Base ctor `FUN_007560d0` zeros `+4/+8` and installs `PTR_FUN_00a9f534`.

## Pseudocode (annotated)

```c
// __thiscall; ECX = host base*; stack flags; RET 4
// returns 0 on ensure, 0xFFFFFFFF if +4 or +8 already set
uint32_t __thiscall HostBase_EnsureAuxPtrs_Inferred(void *host, uint32_t flags)
{
  if (*(void **)((char *)host + 4) != 0 || *(void **)((char *)host + 8) != 0)
    return 0xFFFFFFFFu;

  if ((flags & 1u) != 0) {
    // EBX = *DAT_00d1f624  (implicit context for FUN_00986070)
    *(void **)((char *)host + 4) = FUN_00986070(/*EBX context*/);
  }

  void *raw = operator_new(0x14);
  if (raw != 0) {
    *(void **)((char *)host + 8) = FUN_00442c80(raw); // vtbl PTR_FUN_00a9f49c; zeros +8/+c/+10
    return 0;
  }
  *(void **)((char *)host + 8) = 0;
  return 0;
}
```

## Open questions

- Product English for host base class / `FUN_007560d0`.
- What `FUN_00986070` allocates (0x40 path / pool path) and role of `DAT_00d1f624`.
- Product identity of 0x14-byte object (`PTR_FUN_00a9f49c`).
- Flag bits beyond bit0 (only bit0 tested).
