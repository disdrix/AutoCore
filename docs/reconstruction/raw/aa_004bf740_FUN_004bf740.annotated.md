# Annotated low-level: OwningSList_ClearDestroy_ScalarDtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004bf740` |
| VA | `0x004bf740`–`0x004bf76f` exclusive (**47 B**) |
| Canonical name | `OwningSList_ClearDestroy_ScalarDtor_Inferred` |
| Ghidra name | `FUN_004bf740` |
| System | owning singly-linked list clear (ListTrackedObj list region @ host+0x10) |
| Date | 2026-07-29 W31-B |

---

## Machine-level notes

- **Authority:** `read_memory` hex + decompile; **no** `disassemble_bytes`.
- **thiscall:** `56 8B F1` → ESI = ECX list host.
- **Loop:** while `*(host+4) != 0`: unlink head via `node+4` next; call `(*node->vtbl)(1)`; `*(host+0xc)--`.
- **Fast path:** empty head jumps straight to `pop esi; ret`.
- **Indirect only:** no static callees; virtual scalar-deleting dtor on each node.
- **Does not** free list host; **does not** zero count if head already null with nonzero count (caller responsibility for consistency).

## Pseudocode (annotated)

```c
// ECX = OwningSList* host
// host+0x04 = Node* head
// host+0x0c = int count
// Node: vtbl* @+0, Node* next @+4
// vtbl[0](this, 1) = MSVC scalar-deleting destructor

void __fastcall OwningSList_ClearDestroy_ScalarDtor_Inferred(int host)
{
  int headProbe;
  undefined4 *node;

  headProbe = *(int *)(host + 4);
  while (headProbe != 0) {
    node = *(undefined4 **)(host + 4);
    if (node != (undefined4 *)0x0) {
      *(undefined4 *)(host + 4) = node[1];          // head = next
      (**(code **)*node)(1);                         // delete node
      *(int *)(host + 0xc) = *(int *)(host + 0xc) + -1;
    }
    headProbe = *(int *)(host + 4);
  }
  return;
}
```

## Callers

| Site | Role |
|---|---|
| `ListTrackedObj_CompleteDtor` (`0x004be2a0`) @ `0x004be321` | Flag@`+0x5D0` path: clear nested list at `this+0x10` |

## Open / residual

- Product/MSVC demangle for node type T and list host class English.
- Whether count is always equal to node chain length at entry (no assert).
- Runtime / bit-exact / differential.

## Verdict note

**accept-with-gaps** — CF, ABI, offsets, scalar-dtor pattern sealed.
