# Raw capture: FUN_004040f0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004040f0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004040f0` |
| **Canonical name** | `FUN_004040f0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_004040f0(int param_1,undefined4 param_2)

{
  int iVar1;
  
  iVar1 = FUN_00418700(param_1,*(undefined4 *)(param_1 + 4),param_2);
  FUN_00404840(1);
  *(int *)(param_1 + 4) = iVar1;
  **(int **)(iVar1 + 4) = iVar1;
  return;
}
```

---

## R10-005 re-verify (2026-08-05) — OWN-ONLY dual `aa_004040f0`

| Field | Value |
|---|---|
| **Wave** | residual dual R10 / agent **R10-005** |
| **Tools** | Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_xrefs` / callers / callees + `get_function_by_address` |
| **Avoided** | `disassemble_bytes` (policy); Launcher; parent ledgers; other VAs dualled |
| **Integrity** | Scaffold raw body above **preserved**; this section append-only |
| **Terminal** | **false** |

### Live decompile (2026-08-05) = scaffold CF

```c
void FUN_004040f0(int param_1,undefined4 param_2)
{
  int iVar1;
  iVar1 = FUN_00418700(param_1,*(undefined4 *)(param_1 + 4),param_2);
  FUN_00404840(1);
  *(int *)(param_1 + 4) = iVar1;
  **(int **)(iVar1 + 4) = iVar1;
  return;
}
```

**Decompiler caveats (assembly overrides):**
1. Entry **ECX = list*** (`__thiscall`) is elided — body saves `EDI=ECX` and passes it to Incsize.
2. `FUN_00404840(1)` is `__thiscall` Incsize: **ECX=list**, stack count=`1`.
3. Return type prints `void` (correct — no EAX product); cleanup is **`RET 0x8`**.
4. Stack formals: `param_1` = **where node***, `param_2` = **const T*** (value pointer).

### Body range / size

| Item | Value |
|---|---|
| Start | `0x004040f0` |
| End inclusive | `0x00404122` |
| Size | **51 B** / `0x33` |
| `get_function_by_address` | Body `004040f0`–`00404122` |

### Body hex seal (`read_memory` @ `0x004040f0`, length 51 + pad)

```
8b 44 24 08 53 8b 5c 24 08 56 57 8b f9 8b 4b 04
50 51 53 8b cf e8 f6 45 01 00 6a 01 8b cf 8b f0
e8 2b 07 00 00 89 73 04 8b 56 04 5f 89 32 5e 5b
c2 08 00
```

### Disassembly seal (authoritative ABI + CF)

```text
004040f0  MOV EAX,[ESP+8]          ; val* (stack arg2)
004040f4  PUSH EBX
004040f5  MOV EBX,[ESP+8]          ; where* (stack arg1; after push)
004040f9  PUSH ESI
004040fa  PUSH EDI
004040fb  MOV EDI,ECX              ; EDI = list* (this)
004040fd  MOV ECX,[EBX+4]          ; where->_Prev
00404100  PUSH EAX                 ; pValue
00404101  PUSH ECX                 ; link1 = where->_Prev
00404102  PUSH EBX                 ; link0 = where
00404103  MOV ECX,EDI              ; (unused by stdcall BuyNode; preserves list)
00404105  CALL FUN_00418700        ; BuyNode 0xC ? EAX = node*
0040410a  PUSH 1
0040410c  MOV ECX,EDI              ; list this for Incsize
0040410e  MOV ESI,EAX              ; ESI = new node
00404110  CALL FUN_00404840        ; _Incsize(1); may throw "list<T> too long"
00404115  MOV [EBX+4],ESI          ; where->_Prev = node
00404118  MOV EDX,[ESI+4]          ; EDX = node->_Prev
0040411b  POP EDI
0040411c  MOV [EDX],ESI            ; node->_Prev->_Next = node
0040411e  POP ESI
0040411f  POP EBX
00404120  RET 0x8
```

### ABI (sealed)

| Slot | Location | Role | Conf |
|---|---|---|---|
| list* | entry **ECX** | `std::list` this (`_Myhead` @ +4, `_Mysize` @ +8 via Incsize) | **Confirmed** |
| where* | stack `[ESP+4]` | insert **before** this node | **Confirmed** |
| val* | stack `[ESP+8]` | pointer; BuyNode stores `*val` @ node[+8] | **Confirmed** |
| return | void | no product in EAX | **Confirmed** |
| cleanup | **`RET 0x8`** (`C2 08 00`) | stdcall 2 stack args + thiscall ECX | **Confirmed** |

### Semantics (sealed)

```text
node = BuyNode(where, where->_Prev, val*)   // FUN_00418700, operator_new(0xC)
_Incsize(list, 1)                           // FUN_00404840, max 0x3fffffff
where->_Prev = node
node->_Prev->_Next = node                   // classic insert-before-where splice
```

When `where == list->_Myhead` (sentinel), this is **push_back** — same splice as dualed `StdList_PushBack_Dword_Inferred` @ `0x00402d10`, but with explicit where.

### Callees

| VA | Role | Evidence |
|---|---|---|
| `0x00418700` | `StdList_BuyNode_Dword_Seh` — `operator_new(0xC)`; Next/Prev/`*val` | CALL; dualed MEGA-081 |
| `0x00404840` | list `_Incsize` — max `0x3fffffff`, string `"list<T> too long"` | CALL; thiscall ECX=list |

### Callers / xrefs

| Kind | Detail |
|---|---|
| UNCONDITIONAL_CALL xrefs | **3** (`get_function_xrefs`) |
| Named callers | `FUN_004034c0` @ `0x004034fc` (insert-N loop; dualed MEGA-134 as `StdList_InsertN_BeforeNode_Dword_Inferred`) |
| Named callers | `FUN_00404560` @ `0x004045a0` (insert-range: iterate source nodes, `val = node+8`) |
| Orphan site | `0x004cd020` — CALL lands here; **no** Ghidra function at site (thin wrapper fragment) |
| `analyze_function_complete` callers list | `FUN_004034c0`, `FUN_00404560` (orphan omitted from named-caller set) |

### Family / twins

| VA | Name | Relation |
|---|---|---|
| `0x00402d10` | dualed `StdList_PushBack_Dword_Inferred` | same buy+inc+splice with **where = head** fixed |
| `0x004034c0` | dualed `StdList_InsertN_BeforeNode_Dword_Inferred` | loops this unit N times |
| `0x00418700` | dualed `StdList_BuyNode_Dword_Seh` | buy step |
| `0x00404840` | Incsize (`list<T> too long`) | size++ step (not dualled in this OWN) |

### Naming decision (R10-005)

- Promote **`StdList_InsertOne_BeforeNode_Dword_Inferred`** (Ghidra `FUN_004040f0`).
- Retire scaffold **`Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Client_DebugListMi_004040f0`** (parent-seed only; multi-caller shared STL helper).
- Reject mission/debug-only product English: body is generic dword list insert-before.
- `_Inferred`: no PDB demangle string in body; role from CF + dualed family + Incsize string.

### Completeness snapshot

`analyze_function_complete`: classification **worker**; `has_custom_name=false`; return_type `undefined` (void OK); completeness_score ~7.5 / effective ~15; decompilation available; not thunk.

### Integrity

Scaffold raw body **not wiped**. Terminal **false** (no runtime Confirmed).
