# Annotated low-level: `Client_SendItemPickup` (`FUN_00925580`)

| Field | Value |
|---|---|
| Stable ID | `aa_00925580` |
| VA | `0x00925580` |
| Canonical | `Client_SendItemPickup` |
| System | `inventory-transfer` / `interaction-activation` (Activate special gather) |
| Date | `2026-07-29` (dual A/B seal opcode/size) |

---

## Purpose

C2S **ItemPickup** builder: gates on inventory-busy / character selection TFID / dead & UI modal vtbls; packs **opcode `0x2055`**, size **`0x30`**, TFID lo/hi from **ESI**, zeros, and a terminal float from `g_flMsToSeconds_Inferred` (0.001); sends via `Client_SendSectorPacket(&DAT_00d1a840, …)` with **EAX=8** (Ordered).

---

## ABI — **SEALED** (call-site register convention)

| Slot | Role |
|------|------|
| **ESI** | `uint32_t tfidPair[2]*` — gather hit (`FUN_0058cd60` out). Valid when `(pair[0] & pair[1]) != 0xFFFFFFFF` |
| Stack / ECX / EDX | unused as params (body uses globals + ESI) |
| Return | void |

Call-site pattern (all 3 xrefs):

```
lea  esi, [stack_tfid_pair]
call FUN_00925580   ; 0x00925580
```

---

## Gates (in order)

1. `DAT_00d1b4b0 == 0` — inventory busy / grab-in-flight style flag (set by cargo grab paths).
2. `*(DAT_00d1b6d8 + 0xcd0) == 0` — character field (same family as periodic helper `FUN_00925670`).
3. `(*(char+0xcd8) & *(char+0xcdc)) == 0xFFFFFFFF` — empty “active object TFID-like” pair on character.
4. `(*ESI & ESI[1]) != 0xFFFFFFFF` — target TFID pair valid.
5. Character multi-inherit vtbl **`+0x194`** → false (dead/modal; shared with PollBound early-out).
6. `DAT_00d1b958` vtbl **`+0x3d8`** → false.
7. `DAT_00d1b95c` vtbl **`+0x3d8`** → false.

Any fail → silent return (no send).

---

## Packet layout (buffer base = post-`sub esp,0x30` ESP; size **`0x30`**)

| Off | W | Store | Content |
|----:|--:|-------|---------|
| `+0x00` | 4 | `MOV [pkt], 0x2055` | **opcode** `GameOpcode.ItemPickup` |
| `+0x04` | 4 | **none** | pad (stack residual) |
| `+0x08` | 4 | `MOV from [ESI]` | TFID dword0 (Coid lo) |
| `+0x0c` | 4 | `MOV from [ESI+4]` | TFID dword1 (Coid hi) |
| `+0x10` | 4 | `xorps` zero | TFID Global/pad (forced **0**) |
| `+0x14` | 4 | zero | TFID pad |
| `+0x18` | 4 | zero | unused body |
| `+0x1c` | 4 | zero | unused body |
| `+0x20` | 4 | zero | unused body |
| `+0x24` | 4 | zero | unused body |
| `+0x28` | 4 | `movss` from `0x00a0f72c` | float **0.001** (`g_flMsToSeconds_Inferred`) |
| `+0x2c` | 4 | **none** | trailer within size |

**Note:** Only **8 bytes** of TFID identity are copied from ESI; Global byte is **forced 0** (local world object). Product meaning of `+0x18…+0x28` beyond pad/float is **Tentative** (server AutoCore reads only pad4 + TFID16 after opcode strip).

---

## Send path — **SEALED**

```
EAX = 8                    ; → Client_SendSectorPacket flags Ordered (0)
ECX = &DAT_00d1a840        ; client singleton this
Client_SendSectorPacket(this, size=0x30, buffer=pkt)
```

---

## Callers — **SEALED** (3)

| Parent | Site | How ESI is set |
|--------|------|----------------|
| `Client_Input_PollBoundActions` `0x00925d60` | `0x0092794c` | Activate exclusive early gather @ **20.0f** mode 6 → `FUN_0058cd60` → `LEA ESI` |
| `FUN_00925670` | `0x00925707` | ~100 ms throttle auto gather @ **20.0f** mode 6 (last arg 1) |
| `FUN_00925820` | `0x00925be3` | Interact hub branch: hit → send; clears select `[0x1d6]` |

---

## Product mapping

| Client | AutoCore |
|--------|----------|
| Opcode imm `0x2055` | `GameOpcode.ItemPickup` |
| Size `0x30` (incl. opcode) | handler reads pad4 + TFID16 after opcode strip |
| TFID from gather | `ItemPickupPacket.ItemId` |
| `+0x04` unwritten | `UnknownField` (residual) |

---

## Open (not sealed this unit)

1. Semantic names of `DAT_00d1b958` / `DAT_00d1b95c` + vtbl`+0x3d8`.
2. Character `+0xcd0` / `+0xcd8/+0xcdc` field product names.
3. Why float **0.001** is written at `+0x28` (literal global, not a live tick).
4. Full 16-byte TFID source objects (ESI only 8 B pair from gather).
5. Runtime wire capture of Activate-near-loot.
