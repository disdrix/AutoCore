# Annotated low-level: FUN_004e2b80 → StdTree_Buynode_Val16_Isnil21_Inline_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004e2b80` |
| VA | `0x004e2b80` |
| Body | `0x004e2b80`–`0x004e2bce` inclusive (**79 B** / `0x4F`) |
| Promoted name | `StdTree_Buynode_Val16_Isnil21_Inline_Inferred` |
| System | skills-abilities (partition); body = MSVC `std::_Tree` buynode |
| Date | 2026-08-05 (R13-004 OWN dual; scaffold 2026-07-23) |
| Terminal | **false** |

## Machine-level notes

- Source: raw capture for `aa_004e2b80` + R13-004 live re-verify.
- Prefer assembly when decompiler conflicts (void return vs EAX node*).
- **No SEH** frame (contrast peer buynode `00408990`).
- Construct is **inlined** after `operator_new(0x28)` — no call to NodeCtor helper.
- ABI: **`__stdcall`**, 5 stack args, **`RET 0x14`**, **EAX = node\*** | null.

## Layout (node size **0x28**)

| Offset | Field |
|---|---|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x0C | **not written** on this path |
| +0x10 | value dword[0] |
| +0x14 | value dword[1] |
| +0x18 | value dword[2] |
| +0x1C | value dword[3] |
| +0x20 | color (0=red, 1=black) |
| +0x21 | isnil (forced **0**) |
| size | **0x28** |

## Pseudocode (annotated; CF from raw + ABI from disasm)

```c
// __stdcall RET 0x14; EAX = node* | null
// No SEH. No separate NodeCtor.
NodeIsnil21* StdTree_Buynode_Val16_Isnil21_Inline_Inferred(
    NodeIsnil21* left,              // stack[0x4]
    NodeIsnil21* parent,            // stack[0x8]
    NodeIsnil21* right,             // stack[0xc]
    const uint32_t value4[4],       // stack[0x10] pointer
    uint8_t color)                  // stack[0x14]
{
  NodeIsnil21* node = (NodeIsnil21*)operator_new(0x28);
  if (node != nullptr) {
    node->left = left;              // +0x00
    node->parent = parent;          // +0x04
    node->right = right;            // +0x08
    // +0x0C untouched
    node->val[0] = value4[0];       // +0x10
    node->val[1] = value4[1];       // +0x14
    node->val[2] = value4[2];       // +0x18
    node->val[3] = value4[3];       // +0x1c
    node->color = color;            // +0x20
    node->isnil = 0;                // +0x21 hard zero
  }
  return node;                      // EAX (decompiler shows void)
}
```

## Call graph

| Role | Target | Notes |
|---|---|---|
| Caller | `FUN_004e37e0` @ `0x004e3872` | dualed parent insert/rebalance (R12-012); color **0** |
| Caller | `FUN_005a3500` @ `0x005a3592` | peer insert clone (same push plate); residual |
| Alloc | `operator_new(0x28)` | CRT |
| Peer buynode | `0x00408990` | same Val16/isnil21 layout; SEH + ctor `004099b0` — **do not merge** |
| Peer NodeCtor | `0x004099b0` | construct-only twin of peer path |
| Peer empty | `0x004088f0` | empty head shell size 0x28 |
| Contrast Val24 | `0x00407e30` / `0x005a2de0` | size 0x30 / isnil@+0x29 |

## Rejected names

| Name | Why |
|---|---|
| `Named_CalleeOf_*CVOGReaction*` | chain-of-caller scaffold; no product body |
| Skill/ability product method | pure STL buynode; no skill strings/IDs |
| Merge `StdTree_Buynode_Val16_Isnil21_Inferred` (`00408990`) | different body: this is inline/no-SEH clone |
| Merge Val24/isnil29 buynodes | wrong size/isnil offset |
| Merge empty BuyHead `004088f0` | has Val16 copy + color arg |
| thiscall ECX=map / ECX=node | **Reject** — pure stack args; ECX only used as temp |
| void return | **Reject** — EAX = node*; callers consume EAX |
| isnil from color arg | hard `MOV byte [EAX+0x21],0` |

## Open questions

- Product/PDB demangle for host map value_type under skills residual hosts.
- Dual of peer insert clone `FUN_005a3500` (not OWN).
- Runtime / bit-exact / differential.
