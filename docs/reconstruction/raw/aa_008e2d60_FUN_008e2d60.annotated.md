# Annotated low-level: FUN_008e2d60 → Client_UI_CraftHost_ResetRecipePanel_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_008e2d60` |
| VA | `0x008e2d60`–`0x008e2e5e` (255 B) |
| System | `inventory-transfer` |
| Date | 2026-08-05 (MEGA-085 dual seal) |
| Canonical name | `Client_UI_CraftHost_ResetRecipePanel_Inferred` |

## Machine-level notes

- Source: live Ghidra re-verify MEGA-085; raw body preserved with append.
- Prefer assembly when decompiler conflicts — **bytes win** on ABI (`RET 4` stdcall) and on tail `JMP [vtbl+4]` (not a jumptable).
- `param_1` = craft/RE host panel object (same host family as dualed `Client_UI_CraftHost_BindObjectByTfid` / parent `FUN_008e5990`).
- No direct `FUN_*` callees; all side effects are **widget vtbl** calls + TFID/object-slot stores.
- Recovered layout (offsets relative to host):

| Offset | Role (inferred) |
|--------|-----------------|
| `+0x534` | Row chrome widget ptr array (5×, stride 4); each also has peer at `+0x1c` |
| `+0x5b8` | Cell widget ptr array (5 per row × 5 rows) |
| `+0x61c` | Parallel owned object* array (`cellBase+0x64`) — deleted with vtbl[0](1) |
| `+0x680` | Cell TFID pairs (25× `uint32 lo,hi`, invalid = `0xFFFFFFFF`) |
| `+0x748` | Primary recipe/result chrome widget |
| `+0x768` | Optional secondary widget (hide via vtbl+4(0)) |
| `+0x76c` | Optional tertiary widget (tail-call hide) |

## Control flow (annotated)

```c
// stdcall RET 4 — host on stack (bytes). Not thiscall.
void Client_UI_CraftHost_ResetRecipePanel_Inferred(void* host /* stack arg0 */)
{
  // rowPtr = host+0x534; tfidPtr = host+0x680; cellPtr = host+0x5b8
  // outer = 5 rows
  do {
    // inner = 5 cells
    do {
      // If owned object at cell+0x64 (host+0x61c family): scalar-deleting dtor(1)
      // Clear owned ptr; stamp TFID pair = {-1,-1}
      // cellWidget->vtbl+0x2bc()  // query (non-zero → extra cleanup vtbl+0x58)
      // cellWidget->vtbl+0xcc(0)  // hide/disable-like
      // cellWidget->vtbl+0x34c()  // refresh/layout
      // advance tfid +8, cell +4
    } while (--inner);
    // rowPeer[7] / row primary: vtbl+4(0)  // hide-like
    // advance row +4
  } while (--outer);

  // host+0x748: vtbl+0xd4(0); vtbl+0x34c()
  // if host+0x768: vtbl+4(0)
  // if host+0x76c: tail JMP vtbl+4 with arg 0 (returns to our caller)
}
```

## Callers (evidence)

1. **`FUN_008e5990` @ `0x008e59df`** — bind/rebuild path: after storing `host+0x7cc` and optionally destroying prior name chrome, **always** resets recipe panel before re-populating slots.
2. **Orphan body @ `0x008e63dc`** (undefined function starting ~`0x008e6390`) — float accumulate on `host+0x7e4`; under gate, `PUSH EDI` (host) then reset; then refresh helpers `FUN_008e55e0` / `FUN_008e41b0` / `FUN_008e2f40` / `FUN_008e5e00`.

## Open questions

- Product RTTI / English class name of craft host.
- Exact English for vtbl slots `+0x2bc`, `+0x58`, `+0xcc`, `+0x34c`, `+0xd4`, `+4`.
- Whether outer×inner is always a fixed 5×5 recipe matrix vs generalized constant.
- Runtime / bit-exact / differential.
