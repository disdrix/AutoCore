# Annotated: FUN_00573900 → InventoryGrid_SetSecondaryOwner_Inferred

| Field | Value |
|---|---|
| **Stable ID** | `aa_00573900` |
| **VA** | `0x00573900`–`0x00573909` inclusive (**10 B**) |
| **Ghidra name** | `FUN_00573900` |
| **Preferred name** | `InventoryGrid_SetSecondaryOwner_Inferred` |
| **System** | inventory-transfer / InventoryGrid |
| **Annotator** | MEGA-092 dual A/B (2026-08-05) |
| **Source raw** | `docs/reconstruction/raw/aa_00573900_FUN_00573900.md` |
| **Terminal** | **false** |

---

## Purpose (inferred)

InventoryGrid **vtbl+0x14** method: write a single object pointer (secondary owner / character link) to **`grid+0x58`**, then return. No list walk, no item rebind, no flag write.

Paired with:

| Method | Slot | Store | Side effects |
|---|---|---|---|
| `InventoryGrid_SetOwnerFlag_Inferred` | (direct) | `grid+0x4` | flag only |
| **This unit** | **vtbl+0x14** | **`grid+0x58`** | **store only** |
| `InventoryGrid_SetOwner_Inferred` | vtbl+0x18 | `grid+0x24` | store + per-item vtbl+0x158 rebind |

Cargo create (`0x004f3a30`) order after ctor: SetOwnerFlag(1) → vfunc+0x18(owner entity) → **vfunc+0x14(character expr)**.

---

## Annotated pseudocode

```c
// ECX = InventoryGrid* this
// stack: secondaryOwner*  (cargo path: character-ish load via MI chain +0xa8)
// RET 4; void

void __thiscall InventoryGrid_SetSecondaryOwner_Inferred(
    InventoryGrid* this,
    void* secondaryOwner)
{
    // [SEALED] single store at fixed offset 0x58
    *(void**)((char*)this + 0x58) = secondaryOwner;
    return;
}
```

---

## ABI (byte-sealed)

| Item | Evidence | Conf |
|---|---|---|
| `__thiscall` ECX=this | `MOV [ECX+0x58], EAX` | **High** |
| Stack arg @ `[ESP+4]` on entry | `MOV EAX, [ESP+4]` | **High** |
| `RET 4` | `C2 04 00` | **High** |
| Void / EAX undefined | no MOV EAX before RET; analyze warning | **High** |
| Leaf | no CALL | **High** |

---

## Control flow

```
arg = stack[4]
*(this + 0x58) = arg
return (ret 4)
```

Single basic block. No branches. No callees.

---

## Naming rationale

- Partition hint: `InventoryGrid_SetSecondaryOwner`.
- Parent dual labels vfunc+0x14 as secondary owner/character bind vs primary owner at +0x24.
- **No PDB / product string** — keep `_Inferred`.
- Type of `+0x58` value: **Probable** character/owner-char from cargo arg expression; not product-sealed.

---

## Gaps

1. Product English for field `+0x58` and method name.
2. Exact pointee type (character vs abstract owner vs MI-adjusted entity).
3. Full inventory of virtual call sites beyond cargo create (no static CALL edges).
4. Runtime / bit-exact / differential.
