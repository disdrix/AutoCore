# Annotated low-level: EffTextureImpl_ReleaseGpu_Unaccount_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0096c8e0` |
| VA | `0x0096c8e0`–`0x0096ca5a` exclusive (**378 B**) |
| System | palantir graphics Effects — effTextureImpl GPU release / stats unaccount |
| Date | 2026-08-04 (W38-AB) |
| Ghidra | `FUN_0096c8e0` |
| Canonical | `EffTextureImpl_ReleaseGpu_Unaccount_Inferred` |

## Machine-level notes

- **Thiscall**: `mov edi,ecx` at entry; plain `ret` (`C3`).
- Gate: only unaccount when primary COM slot `this+0x10` is non-null; always Release+null three slots at end.
- Flags `this+0x2c`: bit `0x1000` selects size helper (`FUN_0096c730` vs `FUN_0096c810`); low nibble type `2`=cube, `4`=volume, else 2D.
- `+0x14` non-null ⇒ render-target surface path; optional depth at `+0x18`.
- Stats host `DAT_00d1f614` optional (all deltas null-checked).
- Nested: `FUN_0096c730` expects EDI=this; `FUN_0096bb20` expects ESI=this (`mov esi,edi` before call).
- Mirror inverse of create accounting in `FUN_0096cad0` (effTextureImpl.cpp).
- Reject scaffold alias `Named_CalleeOf_Named_gfxDevice_0096c8e0`.

## Pseudocode (annotated)

```c
// __thiscall ECX = EffTextureImpl* this
void EffTextureImpl_ReleaseGpu_Unaccount_Inferred(EffTextureImpl* self)
{
  // if (self->d3dTex /*+0x10*/) unaccount per flags/type into DAT_00d1f614
  // then IUnknown::Release + null on +0x10, +0x14, +0x18
  // (see clean + raw W38-AB)
}
```

## Open / deferred

- Product demangle for method English beyond structural `_Inferred`.
- Exact product meaning of flag `0x1000` / managed-vs-default counter banks.
- Nested size helpers dual not owned.
- Runtime / bit-exact.
