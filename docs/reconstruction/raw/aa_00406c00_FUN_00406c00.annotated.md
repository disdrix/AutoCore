# Annotated low-level: FUN_00406c00 / StdTree_FreeSubtree_Isnil29_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00406c00` |
| VA | `0x00406c00`–`0x00406c34` (**53 B**) |
| System | STL tree free-subtree / isnil@+0x29 |
| Date | 2026-08-04 (WQ9G-G OWN) |
| Named | `StdTree_FreeSubtree_Isnil29_Inferred` |

## Machine-level notes

- Source: raw capture + WQ9G-G re-verify.
- **Decompiler incomplete:** collapses to right-only recurse + false noreturn on `operator_delete`.
- **Bytes authority:** left-walk loop after delete; same CF as `Map_FreeSubtree_Isnil29` (`0x0051bbc0`) except delete rel32.
- Entry: `PUSH EBX/ESI/EDI; MOV EDI,[ESP+0x10]` (node); `MOV EBX,ECX` (tree_base).
- Epilogue: `POP EDI/ESI/EBX; RET 4`.
- Reject scaffold `Named_CalleeOf_Named_CalleeOf_Client_NotifyActiveMissionChanged_00406c00`.

## Annotated CF (bytes)

```
if (node->isnil@0x29) return;
for (;;) {
  FreeSubtree(tree_base, node->right@+8); // self
  left = node->left@+0;
  operator_delete(node);                  // cdecl
  node = left;
  if (node->isnil) break;
}
```

No value destroy. Callers that need typed dtors do that earlier or accept POD values.

## Call sites

- `FUN_00407d70` full-clear (pairs `Map_EraseNode_B` `0x00409220` on partial path).
- `FUN_0092f3b0` / `FUN_009463b0` (Phy reinit map empty @ +0xf20 / +0xf24 region).

## Open residual

- Product map `value_type` for this instantiation vs `Map_FreeSubtree_Isnil29` twin.
- Full dual of range parent `FUN_00407d70` (not OWN).
- Runtime / bit-exact / differential.
