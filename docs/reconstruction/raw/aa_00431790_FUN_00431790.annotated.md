# Annotated low-level: BasicString_CopyBackward_0x1c (`FUN_00431790`)

| Field | Value |
|---|---|
| Stable ID | `aa_00431790` |
| VA | `0x00431790`–`0x004317c2` (**51 B** / `0x33`) |
| System | MSVC `basic_string<char>` copy_backward free helper |
| Date | 2026-07-29 (W34-L seal) |
| Name | `BasicString_CopyBackward_0x1c` (**Inferred**) |

## Machine-level notes

- **cdecl** 3 stack pointers; plain **`C3`**; returns final dest in **EAX**.
- Element stride **`0x1c`** hard-coded (`SUB ESI/EDI, 0x1C`).
- Assign via IAT `[0x009c63c4]` → `0x006ec244` (`basic_string::operator=`).
- Empty range (`first == last`) returns `dest_end` with no stores.
- Sole caller is trampoline `BasicString_CopyBackward_Thunk_0x1c` (`00431480`); parent insert mid-path needs reverse walk for overlap safety.
- Reject scaffold `Named_CalleeOf_*` alias.

## Pseudocode (annotated)

```c
// cdecl; returns final dest_end after reverse walk
BasicString_0x1c* BasicString_CopyBackward_0x1c(
    BasicString_0x1c* first,     // param_1 — exclusive stop
    BasicString_0x1c* last,      // param_2 — walk start
    BasicString_0x1c* dest_end)  // param_3 — dest past-the-end
{
  if (first == last)
    return dest_end;
  do {
    last -= 1;       // -0x1c bytes
    dest_end -= 1;
    *dest_end = *last;  // operator=
  } while (last != first);
  return dest_end;
}
```

## Open questions

- Product/MSVC demangle plate English.
- Runtime / bit-exact under partial-overlap (expected via operator=; not golden-tested here).
