# Annotated low-level: FUN_0082fe20 → UI_InteractionMenu_PopulateTargetButtons_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0082fe20` |
| VA | `0x0082fe20`–`0x0082ff3f` exclusive |
| Body size | **287 B** (`0x11F`) |
| System | interaction menu / social UI |
| Date | 2026-08-04 (WQ9E-D dual re-annotate) |
| Canonical | `UI_InteractionMenu_PopulateTargetButtons_Inferred` |

## Machine-level notes

- Entry: `push esi; mov esi, ecx` → **menu host in ESI/ECX**.
- Gate: host `+0x4C0` non-zero (menu shell active / valid).
- `FUN_0082d2b0`: tear down / rebuild interaction popup shell (blocker xml path).
- Local player global: **`DAT_00d1b6d8`**.
- Target identity: TFID_16 at host **`+0x578`** → `Object_ResolveFromTFID`.
- Local affinity: call through local SharedBase `vtbl+0x298(target)`; non-zero → treat as blocked/same and skip pack.
- Target `vtbl+0x210(0)`: ownership/parent probe; if **null or == local**, do not build social buttons (optional close via `FUN_0090d400` when host `+0x2B0` set).
- Host `vtbl+0x478()`: **button-order flag** (0 vs non-zero reverses pack order).
- Child packs (undualed except convoy): Ignore, Friends, Tell, Trade, Clan, **Convoy (`FUN_0082fb30`)**, nameplate label button.
- Sole xref is **DATA** at `0x00a720e0` (vtbl method), no direct CALL sites.

## Pseudocode (annotated)

```c
// __thiscall / ECX=menuHost; ret 0
void UI_InteractionMenu_PopulateTargetButtons_Inferred(MenuHost* menu)
{
  if (*(int*)((char*)menu + 0x4C0) == 0)
    return;

  FUN_0082d2b0(menu); // rebuild popup shell

  if (DAT_00d1b6d8 == 0
      || (target = Object_ResolveFromTFID((TFID_16*)((char*)menu + 0x578))) == 0
      || local_vtbl_plus_0x298(DAT_00d1b6d8, target) != 0) {
    // cannot build target actions
    if (*(int*)((char*)menu + 0x2B0) != 0)
      FUN_0090d400(); // close-related UI
    return;
  }

  ownerOrSelf = target->vtbl[0x210/4](0);
  if (ownerOrSelf == 0 || ownerOrSelf == DAT_00d1b6d8) {
    if (*(int*)((char*)menu + 0x2B0) != 0)
      FUN_0090d400();
    return;
  }

  orderFlag = menu->vtbl[0x478/4]();
  if (orderFlag == 0) {
    // Ignore, Friends, Tell, Trade, Clan, Convoy, Name
    FUN_0082f8d0(); // Add/Remove Ignore
    FUN_0082f9a0(); // Add/Remove Friends
    FUN_0082f810(); // Send a Tell
    FUN_0082fc40(); // Trade (+ range disable)
    FUN_0082fa60(); // Clan Invite/Kick
    FUN_0082fb30(); // Convoy Invite/Kick (dualed)
    FUN_0082fd50(); // nameplate-style button
  } else {
    // reverse visual pack order
    FUN_0082fd50();
    FUN_0082fb30();
    FUN_0082fa60();
    FUN_0082fc40();
    FUN_0082f810();
    FUN_0082f9a0();
    FUN_0082f8d0();
  }
}
```

## Open questions

- Product class name for menu host (RTTI not on this VA).
- English for `+0x4C0`, `+0x2B0`, `+0x6B4` (gate used by children), `vtbl+0x210` / `+0x298` / `+0x478`.
- Dual of sibling button packs and `FUN_0082d2b0` / `FUN_0090d400` (not OWN).
- Runtime / bit-exact (Terminal false).
