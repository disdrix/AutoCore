# Annotated low-level: StdMap_InsertOrFindByIntKey_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00569320` |
| VA | `0x00569320` |
| Body | `0x00569320`–`0x005693d8` (184 bytes; `ret 8`) |
| Canonical name | `StdMap_InsertOrFindByIntKey_Inferred` |
| Ghidra name | `FUN_00569320` |
| System | container / MSVC `std::map`/`set` insert-or-find |
| Date | 2026-07-29 (W19-J dual A/B) |

## Machine-level notes

- Source: raw + W19-J live `decompile_function` / `read_memory`.
- ABI: `__thiscall` — **ECX = map shell***, stack `(pair_out*, int* pKey)`, **`ret 8`**.
- Prior chain alias `Named_CalleeOf_…_00569320` is scaffold noise only.
- Insert node builder `FUN_005690a0` throws **`"map/set<T> too long"`** — seals map/set family.
- Predecessor when last step was left: `FUN_00537770` (`Map_Predecessor`, dual-sealed isnil `+0x49`).

## Signature

```c
// __thiscall
// Writes *outPair = { iterator_node*, inserted:bool as byte at outPair+4 }
void StdMap_InsertOrFindByIntKey_Inferred(
    MapShell *map,          // ECX; +4 head, +8 size
    InsertPair *outPair,    // [esp+4]
    int *pKey);             // [esp+8]
// ret 8
```

## Node layout (this instance)

| Off | Field |
|---|---|
| `+0x00` | left* |
| `+0x04` | parent* |
| `+0x08` | right* |
| `+0x0C` | **int key** |
| `+0x10+` | value payload (size not sealed here) |
| `+0x48` | color (red/black; used in insert callee) |
| `+0x49` | **isnil** sentinel flag |

## Algorithm

```
node = map->head->parent;  // root
go_left = true
if (!isnil(node)):
  walk:
    go_left = (*pKey < node->key)
    node = go_left ? node->left : node->right
    while !isnil(child)

if go_left:
  if node == *map->head:   // empty / leftmost insert
    it = Insert(map, /*add_left*/1, node, pKey)
    out = {it, inserted=1}; return
  Predecessor(&node)       // FUN_00537770

if node->key < *pKey:
  it = Insert(map, go_left, node, pKey)
  out = {it, inserted=1}; return

// equal key
out = {node, inserted=0}
```

## Callers / callees

| Dir | Symbol | Role |
|---|---|---|
| Caller | `FUN_00569560` only | hinted insert → general path |
| Callee | `FUN_005690a0` | RB insert + rebalance; `"map/set<T> too long"` |
| Callee | `FUN_00537770` | tree predecessor |

## Open questions

1. Product map element type / value size (node total size beyond isnil).
2. Owning map domain of sole caller's callers (chain not dualed here).
3. Runtime / bit-exact / differential — open.
