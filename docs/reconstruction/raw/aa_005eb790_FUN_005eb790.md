# Raw capture: FUN_005eb790

| Field | Value |
|---|---|
| **Stable ID** | `aa_005eb790` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005eb790` |
| **Canonical name** | `FUN_005eb790` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 * __thiscall FUN_005eb790(undefined4 *param_1,undefined4 param_2)

{
  FUN_005eb210(param_2);
  *param_1 = &PTR_FUN_009dce20;
  param_1[0x30] = 0;
  param_1[0x31] = 0;
  param_1[0x32] = 0x80000000;
  return param_1;
}
```

---

## R12-037 live re-verify (2026-08-05) — APPEND ONLY

**Agent:** OWN-ONLY dual **R12-037**  
**Tools:** Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_callers` + `get_function_xrefs` / `get_xrefs_to` + `get_assembly_context` + `read_memory`. **No** `disassemble_bytes`. **No** Launcher / runtime Confirmed.

### Live decompile (unchanged CF vs 2026-07-23 raw)

```c
undefined4 * __thiscall FUN_005eb790(undefined4 *param_1,undefined4 param_2)

{
  FUN_005eb210(param_2);
  *param_1 = &PTR_FUN_009dce20;
  param_1[0x30] = 0;
  param_1[0x31] = 0;
  param_1[0x32] = 0x80000000;
  return param_1;
}
```

### Body / bytes

| Item | Value |
|---|---|
| Entry | `0x005eb790` |
| Last insn | `RET 4` @ `0x005eb7be` (bytes through `0x005eb7c0`) |
| Inclusive range | `0x005eb790`–`0x005eb7c0` (**49 B** / `0x31`) |
| Pad | `CC` from `0x005eb7c1` |
| Hex | `8B 44 24 04 56 50 8B F1 E8 73 FA FF FF C7 06 20 CE 9D 00 33 C0 89 86 C0 00 00 00 89 86 C4 00 00 00 C7 86 C8 00 00 00 00 00 00 80 8B C6 5E C2 04 00` |

### Assembly (disassemble_function — not disassemble_bytes)

```text
005eb790  MOV  EAX, dword ptr [ESP+0x4]   ; stack arg = shape/params*
005eb794  PUSH ESI
005eb795  PUSH EAX
005eb796  MOV  ESI, ECX                   ; this = query object*
005eb798  CALL 0x005eb210                 ; base ctor FUN_005eb210 (dualed R11-008)
005eb79d  MOV  dword ptr [ESI], 0x9dce20  ; derived vtbl PTR_FUN_009dce20
005eb7a3  XOR  EAX, EAX
005eb7a5  MOV  dword ptr [ESI+0xc0], EAX  ; collect buf begin = 0
005eb7ab  MOV  dword ptr [ESI+0xc4], EAX  ; collect count = 0
005eb7b1  MOV  dword ptr [ESI+0xc8], 0x80000000  ; capacity sentinel (empty MSVC-style)
005eb7bb  MOV  EAX, ESI                   ; return this
005eb7bd  POP  ESI
005eb7be  RET  0x4                        ; thiscall, 1 stack arg
```

### ABI seal

| Item | Evidence |
|---|---|
| Convention | **thiscall**: **ECX** = this (query object); stack `params*`; **RET 4** |
| ESI frame | `PUSH ESI` / `MOV ESI,ECX` / restore / RET 4 |
| Return | **EAX** = this (`MOV EAX,ESI`) |
| Layout writes | vtbl@+0; collect triad @ **+0xC0 / +0xC4 / +0xC8** (0, 0, 0x80000000) |
| Alloc size (call sites) | **0xD0** bytes, pool tag **0x2C** via `DAT_00b05060` allocator vfunc+0x10 |

### Callees

| VA | Name | Role |
|---|---|---|
| `0x005eb210` | `FUN_005eb210` / `VOGPhysics_QueryObject_BaseCtor_Inferred` | base query-object ctor (vtbl `009dcde0` + 0xC0 core); dualed R11-008 |

### Callers / xrefs (4 UNCONDITIONAL_CALL)

| Site | Enclosing | Role |
|---|---|---|
| `0x004e97fb` | `FUN_004e9720` | After `FUN_006c7fa0` sphere-shape + alloc **0xD0**/tag **0x2C**; construct this; then `FUN_0055ff20` / vcall slot `+0x30` collect loop |
| `0x004e9f93` | `FUN_004e9aa0` | Placement/probe path: construct collect object on stack frame |
| `0x004ea0f2` | `FUN_004e9aa0` | Second construct site in same placement helper |
| `0x005eb8bf` | factory near dtor cluster (orphan code @ `0x005eb8a0`) | Placement-style factory: alloc 0xD0/tag 0x2C, set size word @+4, `CALL 005eb790`, RET |

### Call-site ABI samples

1. **`FUN_004e9720` @ `0x004e97ba`–`0x004e97fb`:**  
   `PUSH 0x2C; PUSH 0xD0; CALL [allocator+0x10]`; `MOV word [EAX+4],0xD0`; `LEA ECX,[ESP+…]`; `PUSH` params; `CALL 0x005eb790`; EBX=return this.
2. **Factory @ `0x005eb8a0`–`0x005eb8c4`:**  
   `MOV ECX,[00b05060]`; `PUSH 0x2C; PUSH 0xD0; CALL [EAX+0x10]`; `PUSH` params; `MOV ECX,EAX`; `MOV word [EAX+4],0xD0`; `CALL 0x005eb790`; `RET`.
3. **`FUN_004e9aa0` sites:** stack params + ECX = this (0xD0 object); post-call refcount/release patterns match sibling sphere collect.

### Sibling / peer evidence (not dualled)

| VA | Role |
|---|---|
| `0x005ebec0` | dualed sphere collect ctor (vtbl `009dce90`; MEGA-070) — **same CF pattern**, different derived vtbl |
| `0x005eb210` | dualed base query-object ctor (R11-008) |
| `0x005eb7d0` | dtor for this class: free collect triad with stride **×4**, then `FUN_006343d0` |
| `0x005eb810` | scalar-deleting dtor (flag &1 → free 0xD0/tag 0x2C); vtbl slot0 |
| `0x005ebf00` | sphere-class dtor (vtbl `009dce90`; free stride **×8** + per-elem release) — contrast only |
| `0x006c7fa0` | sphere-shape setup used by caller `004e9720` |
| `0x004ea350` | GetObjectsInArea (uses **sphere** collect `005ebec0`, not this VA) |

### Vtbl `009dce20` DATA xrefs

| Site | Role |
|---|---|
| `0x005eb79d` in `FUN_005eb790` | install (this ctor) |
| `0x005eb7d3` in `FUN_005eb7d0` | reinstall in dtor |
| `0x005eb813` in `FUN_005eb810` | reinstall in scalar dtor |

### Semantics seal

- **Derived ctor** for a **0xD0** physics **query-collect object** (sibling of sphere collect):
  1. Base-init via `FUN_005eb210(params)`.
  2. Install derived vtbl `PTR_FUN_009dce20`.
  3. Zero-init **result collect buffer** triad at `this+0xC0`.
- **Not** the sphere collect ctor (`005ebec0` / vtbl `009dce90`).
- **Not** the base query-object ctor (`005eb210` / no +0xC0 triad).
- **Not** sphere shape setup (`006c7fa0`) or area-query loop.
- Scaffold `Named_CalleeOf_Named_CalleeOf_CVOGSpawnPoint_CreateCreature_005eb790` is **wrong** (no spawn strings/paths; pure collect ctor).

### Name proposal

`VOGPhysics_QueryCollect_Ctor_Inferred` (Ghidra `FUN_005eb790`). Product class/method English open → `_Inferred`. Role sealed by: shared base with dualed sphere collect, distinct vtbl `009dce20`, 0xD0 alloc + collect triad, dtor peer free of +0xC0, non-GetObjectsInArea callers.

### Live ≡ raw CF

**Yes** — decompile body byte-identical in CF to 2026-07-23 scaffold; only ABI/offsets/callers refined.
