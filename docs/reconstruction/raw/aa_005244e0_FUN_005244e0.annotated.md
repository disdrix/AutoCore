# Annotated low-level: Character_SumCommodityTfidFiveRowCosts_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005244e0` |
| VA | `0x005244e0` |
| Ghidra | `FUN_005244e0` |
| Canonical name | `Character_SumCommodityTfidFiveRowCosts_Inferred` |
| System | `inventory-transfer` |
| Date | 2026-08-05 (R11-001 OWN dual; prior scaffold 2026-07-23) |

## Machine-level notes

- **ABI:** `__thiscall` — ECX = `Character*`; one stack arg = base of **5-row TFID table**; **`RET 0x4`**; EAX = total cost sum.
- **Table layout:** 5 rows × **0x28** bytes. Each row is 5×`{lo,hi}` COID/TFID pairs (same shape as `Character_SumCommodityTfidRowCost` row).
- **Null head gate:** only tests **first** pair of each row: `(lo & hi) == 0xFFFFFFFF` ⇔ both dwords `0xFFFFFFFF`. If null, **skip** the callee entirely (avoids adding the callee's base accum of 1 for empty rows).
- **Callee:** dualed `Character_SumCommodityTfidRowCost_Inferred` (`0x00522710`) — thiscall + `ret 8`; stack arg0 (row index) is **dead** inside callee but still pushed; cost formula uses **slot index within row**, not this row index.
- **Decompiler:** drops ECX this and callee-cleanup; prefer bytes for ABI.
- **Callers:** none recovered in Ghidra xrefs (gap — may be indirect/dead).

## Pseudocode (annotated)

```c
// __thiscall Character* this
// ret 4: table of 5 rows × 5 TFID pairs
int Character_SumCommodityTfidFiveRowCosts_Inferred(
    Character* self,
    const uint32_t* table /* [5][10] dwords; stride 0x28 */)
{
  int sum = 0;
  int rowIndex = 0;
  const uint32_t* row = table;

  do {
    // Head TFID null? (lo & hi) == 0xFFFFFFFF
    if ((row[0] & row[1]) != 0xFFFFFFFFu) {
      // Passes rowIndex as unused formal; callee walks 5 slots in *row
      sum += Character_SumCommodityTfidRowCost_Inferred(self, (uint32_t)rowIndex, row);
    }
    ++rowIndex;
    row += 10; // +0x28 bytes
  } while (rowIndex < 5);

  return sum;
}
```

## Open questions

- Product English method name (no string/RTTI).
- Live callers (xrefs empty).
- Whether the 5 outer rows map 1:1 to commodity tiers 1..5 (plausible from sibling `FUN_00522780` tier scoring; not proven by this body).
- Runtime / bit-exact / differential.
