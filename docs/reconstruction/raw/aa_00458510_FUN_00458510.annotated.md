# Annotated low-level: FUN_00458510 → StdMap_InsertOrFind_UintKey_Isnil15_EaxEbx_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00458510` |
| VA | `0x00458510` |
| System | std map insert-or-find (Val8 / isnil+0x15 / uint key) |
| Date | 2026-07-23 scaffold; **W37-K dual 2026-08-04** |

## Machine-level notes

- Source: raw capture for `aa_00458510` + live `read_memory` / decompile (W37-K).
- **Bytes win** on ABI: not a plain cdecl single-arg — **EAX** = map shell, **EBX** = value/key blob*, stack **outPair***, **`RET 4`**.
- Prefer unsigned key compares (`SETB` / `JAE`) over signed peer templates.
- Node isnil `@+0x15` (Val8 family) — **not** Val12 isnil `@+0x19`.
- Equal key: **no** value rewrite (`inserted=0`).

## Sealed algorithm

```
// EAX = MapShell* (head@+4, size@+8); EBX = Val8* (key = *EBX as uint32)
// stack: outPair*  →  { node*, inserted:u8 at +4 }; RET 4
walk from head->parent while !isnil:
  goLeft = (*EBX < (uint)node->key@+0x0C)   // SETB
  child = goLeft ? node->left : node->right
if goLeft:
  if node == head->left: insert(addLeft=1) → out{it,1}; ret
  else predecessor(node)                     // FUN_004129f0
if node->key < *EBX: insert(addLeft=goLeft) → out{it,1}; ret
else: out{node,0}; ret                       // equal — no rewrite
```

## Pseudocode (annotated)

```c
// Register ABI sealed by bytes; decompiler "in_EAX" / "unaff_EBX" are real formals.
uint32_t * StdMap_InsertOrFind_UintKey_Isnil15_EaxEbx_Inferred(
    /*EAX*/ int map,
    /*stack*/ uint32_t *outPair,
    /*EBX*/ uint32_t *val)   // key at val[0]
{
  uint32_t *head = *(uint32_t **)(map + 4);
  uint32_t *node = (uint32_t *)head[1]; // root = head->parent
  bool goLeft = true;
  if (*(char *)((int)node + 0x15) == 0) {
    do {
      goLeft = *val < (uint32_t)node[3];   // key @ +0x0C
      node = goLeft ? (uint32_t *)*node : (uint32_t *)node[2];
      // loop uses previous as parent; decompile assigns parent = cur each step
    } while (*(char *)((int)/*child*/ + 0x15) == 0);
  }
  // ... leftmost insert / predecessor / insert-if-less / equal ...
  // insert via FUN_00438140 (EDI=map); predecessor FUN_004129f0
}
```

## Call graph

| Direction | Targets |
|---|---|
| Callees | `FUN_00438140` insert+RB (color@+0x14, max `0x1ffffffd`); `FUN_004129f0` predecessor isnil15 |
| Callers | `FUN_00444850` (FourCC map `DAT_00d21958`), `FUN_00444880` (`DAT_00d21964`), `FUN_0043daf0`, `FUN_004582e0`, `FUN_00752d60`, `FUN_00763620` |

## Related

- W35-H wrapper `FourCCFnPtrMap_TryInsert_DAT_00d21958` left this dual open — **closed here**.
- Peer thiscall/signed isnil15: `aa_004a7580` (W23-F) — different ABI and signed compare.
