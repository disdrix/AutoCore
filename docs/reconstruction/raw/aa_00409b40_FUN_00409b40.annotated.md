# Annotated low-level: FUN_00409b40 → StdVector_UninitCopyTrampoline_Elem12_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00409b40` |
| VA | `0x00409b40`–`0x00409b5a` inclusive (**27 B** / `0x1b`) |
| System | skills-abilities / MSVC-style vector uninit_copy ABI trampoline (elem **0x0C**) |
| Date | 2026-08-05 (R10-008 OWN dual re-annotate; scaffold 2026-07-23) |
| Named | `StdVector_UninitCopyTrampoline_Elem12_Inferred` |

## Machine-level notes

- Source: raw capture + R10-008 re-verify for `aa_00409b40`.
- Prefer assembly when decompiler conflicts — decompiler drops 2nd stack arg, shows void return, and omits **`RET 8`**.
- Target `FUN_0040a590` is dualed as `StdVector_UninitializedCopy_Elem12_Inferred` (WQ9K-J).
- Sole external consumer: InsertN/reallocate grow path `FUN_004082f0` / `StdVector_InsertN_Elem12_Inferred` (2 sites).
- Twin trampoline (elem 0x28): `0x00409ae0` `StdVector_UninitCopyTrampoline_Elem0x28_Inferred` — same body shape, different CALL target.

## Pseudocode (annotated)

```c
// ABI (bytes): ECX=host/this (unused by target), EDX=dest,
//              stack src_begin, src_end; EAX=dest_end; RET 8
// Decompiler shows incomplete __fastcall(3 params) without RET 8.
void* __fastcall StdVector_UninitCopyTrampoline_Elem12_Inferred(
    void* /*ECX*/ host_unused,
    void* /*EDX*/ dest,
    const void* /*stack*/ src_begin,
    const void* /*stack*/ src_end)
{
  // Reorder into free-helper ABI of 0040a590:
  //   ECX = src_end; stack: src_begin, dest, (src_begin, host) extras
  // Target ignores extra stack slots; same 4-push + ADD ESP,0x10 glue as direct sites.
  return StdVector_UninitializedCopy_Elem12_Inferred(
      /*ECX*/ src_end,
      /*stack*/ src_begin,
      /*stack*/ dest);
}
```

## Control flow

| Stage | Match |
|---|---|
| Load src_begin from `[ESP+4]` | Confirmed |
| Push begin; push host ECX | Confirmed |
| Load src_end into ECX | Confirmed |
| Push dest EDX; push begin | Confirmed |
| CALL `0040a590` | Confirmed (rel `+0x0a3b`) |
| `ADD ESP,0x10` | Confirmed |
| `RET 8` | Confirmed |
| No local element loop | Confirmed (27 B body) |

## Call sites (InsertN grow)

```
// Prefix @ 0x00408413:
//   PUSH where ([EBP+8]); PUSH begin ([EBX+4]); EDX=new; ECX=this → [begin,where)→new
//   EAX dest_end stored [EBP-0x1c] then fed to Ufill 00406ee0
// Suffix @ 0x0040843a:
//   PUSH end ([EBX+8]); PUSH where ([EBP+8]); EDX=after Ufill; ECX=this → [where,end)→dest
// Non-grow paths in same parent call 0040a590 directly (no trampoline).
```

## Open residual

- Product T demangle for 0x0C POD element (shared family).
- Runtime / bit-exact / differential.
- Parent InsertN already dualed WQ9H-B (role sealed; not re-owned here).
