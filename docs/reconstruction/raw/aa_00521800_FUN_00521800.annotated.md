# Annotated low-level: Character_GetRaceDisplayName_Inferred (`FUN_00521800`)

| Field | Value |
|---|---|
| Stable ID | `aa_00521800` |
| VA | `0x00521800` |
| Body | `0x00521800`–`0x00521879` (**121 B**) |
| System | missions-progression / character display |
| Date | 2026-08-04 (WQ7R-F dual) |
| Canonical name | `Character_GetRaceDisplayName_Inferred` (**Inferred**; Ghidra `FUN_00521800`) |

## Machine-level notes

- **thiscall** ECX = character/player-like object; first insn `mov eax,[ecx+4]`.
- Nested resolve to race blob: `*( *( *( *(this+4)+4 ) + this + 0xac ) + 0x3c )`, then **byte @ +0x532**.
- Cascade `sub/jz` (not jump table): race **0 Human / 1 Mutant / 2 Biomek / else Unknown**.
- Product strings confirmed via `read_memory`:
  - `0x00a40958` `"Human"`
  - `0x00a40950` `"Mutant"`
  - `0x00a40948` `"Biomek"`
  - `0x00a37c4c` `"Unknown"`
- Buffer path: `FUN_007a69d0` → TLS string table object; `FUN_007a6de0(src, -1)` copies into ring slot and **returns char\***.
- Parent `FUN_005465c0` replaces token **`[$race]`** (len 7) with this return.

## Pseudocode (annotated)

```c
// char* __thiscall Character_GetRaceDisplayName_Inferred(Character* this)
// Returns ring-buffered display name; not a durable heap string.
char * __fastcall Character_GetRaceDisplayName_Inferred(int this_char)
{
  // blob = character "sheet" / race-class state object
  int *vft_chain = *(int **)(this_char + 4);
  int *mid = *(int **)((char *)vft_chain + 4);
  int *slot = *(int **)((char *)mid + this_char + 0xac);
  int blob = *(int *)((char *)slot + 0x3c);
  unsigned char race = *(unsigned char *)(blob + 0x532);

  const char *name;
  if (race == 0)      name = "Human";     // 0x00a40958
  else if (race == 1) name = "Mutant";    // 0x00a40950
  else if (race == 2) name = "Biomek";    // 0x00a40948
  else                name = "Unknown";   // 0x00a37c4c

  void *tls = FUN_007a69d0();             // &DAT_00d1f660 string table
  return FUN_007a6de0(tls, name, -1);     // thiscall copy-out → char*
}
```

## Callers / callees

| Kind | Address / symbol | Role |
|---|---|---|
| Caller | `0x005465c0` FUN_005465c0 | Mission string tokens: replace `[$race]` |
| Caller | `0x005792d0` FUN_005792d0 | Format cases 3/4 (race / faction-race) |
| Caller | `0x0092e710` FUN_0092e710 | "Level N … Race Class" UI line builder |
| Callee | `0x007a69d0` | Get TLS string-table object |
| Callee | `0x007a6de0` | Copy source C-string into ring; return buffer |

## Open questions

- Product/PDB method English (GetRaceName vs FormatRaceDisplayName).
- Exact C++ type of `this` and intermediate `+0xac` / `+0x3c` objects.
- Whether race enum values appear elsewhere as named constants.
