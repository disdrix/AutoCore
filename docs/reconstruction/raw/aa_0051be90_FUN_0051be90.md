
# Raw capture: FUN_0051be90

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051be90` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051be90` |
| **Canonical name** | `FUN_0051be90` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0051be90(void *param_1)

{
  if (*(char *)((int)param_1 + 0x29) == '\0') {
    FUN_0051be90(*(undefined4 *)((int)param_1 + 8));
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return;
}
```

---

## Re-verify append — 2026-08-04 WQ9E-I OWN dual

| Field | Value |
|---|---|
| **Tool** | Ghidra `batch_decompile` / `analyze_function_complete` + `read_memory` + callers/xrefs (**no** `disassemble_bytes`) |
| **Live ≡ original raw CF (decomp plate)** | **Yes** — same right-recurse + delete surface; left-walk restored from bytes |
| **Body** | `0x0051be90`–`0x0051bec5` exclusive (**53 B** / `0x35`); Ghidra body end inclusive `0x0051bec4`; `CC` pad after `ret 4` |
| **ABI bytes** | Stack `node*`; preserves EBX/ESI/EDI; threads ECX unused; **`ret 4`** (`C2 04 00`) |
| **isnil** | `cmp byte ptr [node+0x29], 0` — large-node family (same as `Map_EraseRange_B` / `Map_EraseNode`) |
| **Byte-correct free algebra** | MSVC `_Tree::_Erase` style: while not isnil: free(right); left=`*node`; `operator_delete(node)`; node=left. Decompiler drops left-walk (false-noreturn on `operator_delete`). |
| **Callees** | self (right recurse); `operator_delete` |
| **Callers** | self; **`Map_EraseRange_B` / `FUN_0051d880`** full-clear @ `0x0051d89c` (sole external) |
| **Promoted name** | `Map_FreeSubtree_B` — free-subtree for `Map_EraseRange_B` full clear; twin CF of isnil@+0x29 frees (`MapFloatTfid_FreeSubtree_Isnil29_Inferred`, expected `FUN_0051bbc0` for `Map_EraseRange`) |
