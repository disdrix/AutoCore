# Annotated low-level: FUN_00404a20

| Field | Value |
|---|---|
| Stable ID | `aa_00404a20` |
| VA | `0x00404a20` |
| Body | `00404a20`–`00404a3b` |
| System | `shared-object-pose` |
| Date | 2026-07-29 (dual refine) |
| Role | `Object_GetWorldOrientQuatPtr` (inferred) — pointer to world orientation quaternion |
| Twin | `FUN_00404c90` @ `0x00404c90` (world position pointer; offsets `0xb0` / `0x84`) |

## Machine-level notes

- Source: raw capture for `aa_00404a20`; re-verified live `decompile_function` + `read_memory` 2026-07-29.
- Leaf: no callees, no stores, no globals.
- CC: `this` in **ECX** (`__fastcall` / thiscall-style).
- Physics path: `rb = *(*(this+8)+0x3c); return rb+0x30` (orientation quat xyzw).
- Fallback: `lea eax,[edx+ecx+0x94]` with `edx = *(*(this+4)+4)`.
- Encoding: physics add uses imm8 `83 c0 30` (vs twin imm32 `add eax,0xb0`); CF isomorphic.
- Prefer assembly when decompiler conflicts (none here).

## Pseudocode (annotated copy of raw)

```c
// ECX = pEntity. Returns pointer to orient quat (not by value).
int __fastcall FUN_00404a20(int param_1)
{
  if (*(int *)(param_1 + 8) != 0) {
    // physicsObj → rigidBody → +0x30 quaternion
    return *(int *)(*(int *)(param_1 + 8) + 0x3c) + 0x30;
  }
  // clonebase/type adjust + entity-embedded orient @ +0x94
  return *(int *)(*(int *)(param_1 + 4) + 4) + 0x94 + param_1;
}
```

## Assembly (from `read_memory`)

```
00404a20  8b4108          mov  eax, [ecx+8]
00404a23  85c0            test eax, eax
00404a25  7407            jz   short 00404a2e
00404a27  8b403c          mov  eax, [eax+0x3c]
00404a2a  83c030          add  eax, 0x30
00404a2d  c3              ret
00404a2e  8b4104          mov  eax, [ecx+4]
00404a31  8b5004          mov  edx, [eax+4]
00404a34  8d840a94000000  lea  eax, [edx+ecx+0x94]
00404a3b  c3              ret
```

Hex: `8b410885c074078b403c83c030c38b41048b50048d840a94000000c3`

## Reviews

- `docs/reconstruction/reviews/A_aa_00404a20_Object_GetWorldOrientQuatPtr.md`
- `docs/reconstruction/reviews/B_aa_00404a20_Object_GetWorldOrientQuatPtr.md`

## Open questions

- Retail / PDB symbol for this VA.
- Exact product type of `this+4` adjust chain.
- Runtime / bit-exact verification deferred.
