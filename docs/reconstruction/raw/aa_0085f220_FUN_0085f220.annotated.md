# Annotated low-level: Client_UI_InventoryDropHitTest (`FUN_0085f220`)

| Field | Value |
|---|---|
| Stable ID | `aa_0085f220` |
| VA | `0x0085f220` |
| Canonical name | `Client_UI_InventoryDropHitTest` |
| System | `inventory-transfer` |
| Date | 2026-07-29 (dual seal over 2026-07-23 scaffold) |

## Machine-level notes

- Prefer call-site + helper + CanPlace-prep **bytes** when decompiler invents `extraout_*` / polluted stack slots (`0x85f266` return addr, etc.).
- Dual A/B: `reviews/A_aa_0085f220_Client_UI_InventoryDropHitTest.md`, `B_…`.

## ABI (sealed)

```text
EAX = held item*
ESI = inventory UI window*
stack: outCellX*, outCellY*, outOccupant*   // stdcall, ret 0x0c
AL  = 0 fail / 1 ok
```

Gate: `!item || !outX || !outY || !*(window+0x56c)` → 0.

## Behavioral pipeline

```text
1. Rect containment via UI vtables (+0x120 / +0x144 / +0x140) on window + item
2. cell = pageDim * delta / hitSize   (pageW@typeHost+0x18, pageH@+0x1c)
3. half-cell snap (pitch from screen scale × 1/1600 X, 1/1200 Y constants)
4. clamp so cell+footprint fits page (footprint item+0x4a0 / +0x4a4)
5. *outX,*outY = page-local cells
6. CanPlace(typeHost, sizeX, sizeY, cellX, pageH*pageIdx+cellY, &tfid)
7. optional occupant resolve (FUN_00413b30) + ghost UI (window+0x574)
```

## Cross-links

| Unit | Link |
|------|------|
| DropToGrid | Consumes outs as packet X and **page-local** Y; re-scales wire Y |
| FUN_0085f1d0/f1f0 | Page H/W loaders |
| FUN_00570840 | CanPlace |
| FUN_0085e820 | Cell → pixel for ghost |
| FindFree / Place | Shared grid layout family |

## Pseudocode (annotated copy of raw)

See raw capture body. Interpretation notes only — raw remains authoritative for CF shape.

```c
// EAX=item, ESI=window, stdcall outs
undefined4 Client_UI_InventoryDropHitTest(uint8_t* outX, uint8_t* outY, void** outOcc)
{
  // null gates on item, outX, outY, typeHost=window[0x15b]/+0x56c
  // ... rect tests (vtable) ...
  // cellX/Y from ratio + snap + footprint clamp (item+0x4a0/+0x4a4)
  // *outX = cellX; *outY = cellY;   // PAGE-LOCAL
  // CanPlace(typeHost, sizeX, sizeY, cellX, pageH*pageIdx+cellY, &pair)
  // if fail return 0
  // if outOcc: empty pair → 0 else FUN_00413b30
  // if ghost window[0x15d]/+0x574: FUN_0085e820 + colors + refresh
  // return 1
}
```
