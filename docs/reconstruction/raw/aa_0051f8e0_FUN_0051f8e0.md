# Raw capture: FUN_0051f8e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051f8e0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051f8e0` |
| **Canonical name** | `FUN_0051f8e0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
char * FUN_0051f8e0(int param_1)

{
  if (param_1 == 0) {
    return "Human";
  }
  if (param_1 != 1) {
    if (param_1 != 2) {
      return "Unknown";
    }
    return "Biomek";
  }
  return "Mutant";
}
```

---

## R12-017 re-verify (2026-08-05) — OWN-ONLY dual seal

| Field | Value |
|---|---|
| **Wave** | R12 residual dual start 2646 → **R12-017** |
| **System** | skills-abilities |
| **Partition parent** | `0x00846820` `UI_AppendCraftRequirements_Inferred` |
| **Terminal** | **false** (no runtime Confirmed) |
| **Tools** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_by_address` + `get_function_callers` + `get_xrefs_to` + `read_memory` |
| **Not used** | `disassemble_bytes`; Launcher; parent ledgers |

### Live decompile (authoritative re-verify; CF ≡ scaffold)

```c
char * FUN_0051f8e0(int param_1)

{
  if (param_1 == 0) {
    return "Human";
  }
  if (param_1 != 1) {
    if (param_1 != 2) {
      return "Unknown";
    }
    return "Biomek";
  }
  return "Mutant";
}
```

### Body / ABI (machine-sealed)

| Claim | Evidence |
|---|---|
| Body `0051f8e0`–`0051f90a` (Ghidra); exclusive end `0051f90b`; **43 B** / `0x2B` | `get_function_by_address` + `read_memory` |
| Pad `CC` ×5 then twin short-code leaf `FUN_0051f910` @ `0x0051f910` | `read_memory` after RET; next function |
| cdecl 1-arg; stack `[esp+4]`; plain **`RET`** (`c3`) | entry `8b 44 24 04`; four `c3` exits |
| Leaf; **no callees** | `analyze_function_complete` callees=[] |
| Classification | `leaf` |

### Race map (product strings via imm `mov eax` + `read_memory`)

| raceId | Return string | String VA | Imm bytes |
|---:|---|---|---|
| 0 | `"Human"` | `0x00a40958` | `b8 58 09 a4 00` |
| 1 | `"Mutant"` | `0x00a40950` | `b8 50 09 a4 00` |
| 2 | `"Biomek"` | `0x00a40948` | `b8 48 09 a4 00` |
| else | `"Unknown"` | `0x00a37c4c` | `b8 4c 7c a3 00` |

Cascade is `sub/jz` on EAX (0→Human, then −1→Mutant, then −1→Biomek, else Unknown) — same arm layout as dualed `Character_GetRaceDisplayName_Inferred` (`0x00521800`) but **without** character this / TLS ring.

### Callers / xrefs (2 UNCONDITIONAL_CALL)

| From | In function | Role |
|---|---|---|
| `0x00845520` | `FUN_00845360` `UI_AppendItemRequirements` | item **Requires** race line |
| `0x00846d8c` | `FUN_00846820` `UI_AppendCraftRequirements_Inferred` | craft **"Required Faction"** race line (product string `0x00a6a1e0`) |

Both callers feed race id from def vfunc **`+0x14`** (−1 = none); compare player race byte `@+0x532`.

### Sibling context (not owned)

| VA | Role |
|---|---|
| `0x0051f910` | short race codes HUM/MUT/BIO (same cascade shape) |
| `0x0051f940` | dualed `LookupClassDisplayName_Inferred` |
| `0x00521800` | dualed `Character_GetRaceDisplayName_Inferred` (thiscall + ring; embeds same 0/1/2 map) |

### Name

`LookupRaceDisplayName_Inferred` — role High (UI race English leaf; peer of class lookup). Product/PDB method English open → **`_Inferred`**. Reject scaffold `Named_CalleeOf_Named_Combat_0051f8e0`.

### analyze_function_complete (name=FUN_0051f8e0)

- classification: leaf
- xrefs: `00845520`, `00846d8c`
- callers: `FUN_00845360`, `FUN_00846820`
- callees: []
- locals: `param_1` Stack[0x4]:4
- return_type_warning: Ghidra signature `undefined` / void — decompile returns `char*`; machine returns imm string ptr in EAX (do not trust void)

