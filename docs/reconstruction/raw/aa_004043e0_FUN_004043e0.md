# Raw capture: FUN_004043e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004043e0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004043e0` |
| **Canonical name** | `FUN_004043e0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_004043e0(void)

{
  char cVar1;
  int iVar2;
  int in_EAX;
  
  iVar2 = *(int *)(in_EAX + 8);
  cVar1 = *(char *)(iVar2 + 0x31);
  while (cVar1 == '\0') {
    iVar2 = *(int *)(iVar2 + 8);
    cVar1 = *(char *)(iVar2 + 0x31);
  }
  return;
}
```

---

## WQ9L-C re-verify (2026-08-05) — append only

**Tools:** `batch_decompile` + `decompile_function` + `disassemble_function` + `read_memory` + `analyze_function_complete` + `get_assembly_context` + callers/xrefs. **No** `disassemble_bytes`. No Launcher.

### Body bytes (`read_memory` @ `0x004043e0`, 32 B)

```text
8b 48 08 80 79 31 00 75 12 8d a4 24 00 00 00 00
8b c1 8b 48 08 80 79 31 00 74 f5 c3
cc cc cc cc
```

- Inclusive body: `0x004043e0`–`0x004043fb` (**28 B** / `0x1c`); `CC` pad after.
- Listing (via `disassemble_function`):
  ```text
  004043e0  MOV ECX,[EAX+8]            ; right = node->right
  004043e3  CMP byte ptr [ECX+0x31],0
  004043e7  JNZ 004043fb               ; right isnil → return EAX=node
  004043e9  LEA ESP,[ESP]              ; 7-byte NOP align
  004043f0  MOV EAX,ECX                ; node = right
  004043f2  MOV ECX,[EAX+8]
  004043f5  CMP byte ptr [ECX+0x31],0
  004043f9  JZ  004043f0               ; while right non-nil
  004043fb  RET                        ; EAX = rightmost non-nil
  ```
- ABI: **EAX** = input node*; returns rightmost non-nil in **EAX**; plain **`RET`** (`C3`). Not thiscall.
- isnil every guard **`+0x31`** (`80 79 31 00`).
- Decompiler void/in_EAX phantom is wrong for return — call site seals EAX out.

### Call site (`FUN_00408ed0` @ `0x00408fbf`)

```text
MOV EAX, EDI          ; replacement / subtree root
CALL 0x004043e0
MOV dword ptr [EBX+8], EAX   ; head->right = max(...)
```

Used when fixing head rightmost extremity after unlink (peer min `00409160` updates `head->left`).

### Live decompile

Walk-right while `!isnil(+0x31)` matches bytes; return-in-EAX requires override via call site + listing. Early-exit when right already nil leaves EAX = original node.

### Named role

`StdTree_Max_Isnil31` — twin of dualed `StdTree_Min_Isnil31` @ `0x00409160`; family peer of isnil29 `StdTree_Max_Isnil29_Inferred` @ `0x00421b50` (stack ABI — keep distinct).
