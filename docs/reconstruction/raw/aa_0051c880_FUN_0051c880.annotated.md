# Annotated low-level: Map_EraseNode_Isnil1d_Inferred (FUN_0051c880)

| Field | Value |
|---|---|
| Stable ID | `aa_0051c880` |
| VA | `0x0051c880`–`0x0051cb36` exclusive (**694 B**) |
| Canonical | `Map_EraseNode_Isnil1d_Inferred` |
| System | STL / map-set (WQ-009 skill residual; isnil@+0x1d single erase) |
| Date | 2026-08-04 WQ9F-F OWN dual |

## Machine-level notes

- Source: raw capture + live re-verify for `aa_0051c880`.
- **Bytes authority** for post-delete epilogue: decompiler marks `operator_delete` noreturn and drops size-- / `*out` / `ret 8`.
- isnil@**+0x1d**; color@**+0x1c**; left@**+0**; parent@**+4**; right@**+8**.
- ABI: `__thiscall` ECX=map (`mov ebp,ecx`); stack `(pOutIt, pNode)`; **`ret 8`** (`C2 08 00` @ `0x0051cb33`).
- Sole caller: `Map_EraseRange_Isnil1d_Inferred` (`0x0051d700`) @ `0x0051d79f`.
- Ghidra function body end `0x0051cb06` is incomplete; exclusive end sealed at `0x0051cb36`.

## Pseudocode (byte-corrected surface)

```c
// Decomp plate: ends at operator_delete (false-noreturn).
// Byte-correct epilogue after delete:
//   if (map->size > 0) map->size--;
//   *pOutIt = successor;
//   ret 8;

void __thiscall Map_EraseNode_Isnil1d_Inferred(
    void *map, void **pOutIt, void *pNode)
{
  if (*(char *)((int)pNode + 0x1d) != 0) {
    // throw "invalid map/set<T> iterator"
    _CxxThrowException(...);
  }

  void *succ = pNode;
  FUN_0051b690(&succ); // in-order successor isnil@+0x1d

  // unlink + RB rebalance (color@+0x1c; rotates 00420130/00420180;
  // min 0051b670; max 00421b90) — full cases in raw decompile

  operator_delete(pNode); // FALLS THROUGH

  if (*(uint32_t *)((char *)map + 8) > 0)
    *(uint32_t *)((char *)map + 8) -= 1;
  *pOutIt = succ;
}
```

## Call graph

| Direction | Target | Notes |
|---|---|---|
| Caller | `Map_EraseRange_Isnil1d_Inferred` `0x0051d700` | partial path only |
| Callee | `FUN_0051b690` | successor isnil@+0x1d |
| Callee | `FUN_0051b670` / `FUN_00421b90` | min / max |
| Callee | `FUN_00420130` / `FUN_00420180` | rotates |
| Callee | `operator_delete` | free node (falls through) |
| Callee | `_CxxThrowException` | invalid iterator |

## Open questions

- Exact `map<K,V>` / value size for isnil@+0x1d family.
- Product/PDB symbol.
- Runtime / bit-exact open.
