# Annotated low-level: StdTree_BuyHeadNode_Isnil29_Inferred (FUN_00408a30)

| Field | Value |
|---|---|
| Stable ID | `aa_00408a30` |
| VA | `0x00408a30`–`0x00408ab9` inclusive (**138 B** / `0x8A`) |
| System | MSVC `std::_Tree` head/sentinel node alloc (isnil@+0x29 family) |
| Date | 2026-08-04 WQ9H-J OWN dual (scaffold 2026-07-23) |
| Canonical | `StdTree_BuyHeadNode_Isnil29_Inferred` |

## Machine-level notes

- Source: raw + live decompile / `disassemble_function` / `read_memory`. Prefer bytes for ABI.
- **No stack args**; bare **`RET`** (`C3`); **EAX** = allocated node (decompiler may label void — false).
- `operator_new(0x30)` then:
  - if node: `left = 0`
  - if `&parent` / `&right` (MSVC field-address checks): zero parent/right
  - **`color = 1`** (black) @+0x28; **`isnil = 0`** @+0x29
  - Val24 payload @+0x10..+0x27 **not** written
- Callers complete head construction:
  1. `host+4 = node`
  2. `node->isnil = 1` (**overrides** the 0 written here)
  3. self-link: `left = parent = right = node`
  4. `host+8 = 0` (size)
- Callers: `FUN_00405b40`, `FUN_00405e60`, `FUN_00405fb0`, `FUN_004d98f0` (4 UNCONDITIONAL_CALL).
- Family matches isnil@+0x29 / 0x30 peers (buynode `00407e30`, insert `00406c40`); **not** CNDHash freelist.
- Odd: OOM path still stores color/isnil via EAX (preserve); CRT new normally throws under SEH.

## Pseudocode (annotated)

```c
// no stack args; EAX = NodeIsnil29*  (head shell; callers set isnil=1 + self-link)
NodeIsnil29* StdTree_BuyHeadNode_Isnil29_Inferred(void)
{
  // SEH frame (ExceptionList / LAB_009bc690)
  NodeIsnil29* node = (NodeIsnil29*)operator_new(0x30);
  // MSVC-style per-field null guards (left only true null-check)
  if (node != nullptr)
    node->left = nullptr;
  if ((NodeIsnil29**)((char*)node + 4) != nullptr)  // always true unless node==0 → abs 4
    node->parent = nullptr;
  if ((NodeIsnil29**)((char*)node + 8) != nullptr)
    node->right = nullptr;
  node->color = 1;   // black
  node->isnil = 0;   // callers force 1 for head
  return node;
}
```

## Open questions

- Product/PDB English for host tree type(s) using this head.
- Full dual of tree ctor wrappers (not OWN).
- Runtime / bit-exact / differential.
