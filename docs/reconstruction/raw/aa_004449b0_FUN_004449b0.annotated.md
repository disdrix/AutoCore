# Annotated low-level: FUN_004449b0 / Sto_AbstractUnserializationFactory_InsertCreator_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004449b0` |
| VA | `0x004449b0`–`0x00444a35` (**134 B**) |
| System | arda2/storage — stoAbstractUnserializationFactory |
| Date | 2026-07-23 scaffold; **W36-A seal 2026-07-29** |
| Preferred name | `Sto_AbstractUnserializationFactory_InsertCreator_Inferred` |

## Machine-level notes

- Source: raw capture + live `decompile_function` / `read_memory` (no `disassemble_bytes`).
- **Map A only** via `FUN_00438c40` (`DAT_00d1fd18`); not MapB `0x00438ca0`.
- NestedHash node layout (family 0x10): key at `+0x08` (lookup uses `node[2]`), **creator fn\*** at **`+0x0c`**.
- Empty sentinel = `*(map+8)`; lookup/insert compare against it.
- Decompiler `unaff_EBX` is a **real register input** — sealed by wrapper callers that `MOV EBX, creator` before CALL.
- First lookup uses stack tag (`[esp+0x1c]` after frame = `param_1`) as key; out node at `[esp+8]`.
- On duplicate: log then **still** write EBX to existing `node+0xc` (overwrite creator).
- On miss: `FUN_0046bf90` insert with key pair `{tag, 0}`, EDI=map; then write creator.
- Map ops `FUN_0046c1b0` / `FUN_0046bf90` are **unowned** (W36-B) — treated as NestedHash find/insert only.

## Pseudocode (annotated)

```c
// cdecl; param_1 = FourCC tag; EBX = creator fn* (register)
// void Sto_AbstractUnserializationFactory_InsertCreator_Inferred(uint32_t tag /*stack*/, creator_fn* ebx)
void FUN_004449b0(int param_1 /* FourCC tag */)
{
  int sentinel;
  int *node_out;          // decompiler local_10[0] / [esp+8]
  int *map;
  int *inserted;
  /* EBX = creator — unaff_EBX in raw */
  int key_pair[2];        // for insert path
  uint8_t insert_pair[8]; // FUN_0046bf90 out {node*, inserted_flag}

  map = (int *)FUN_00438c40();                 // MapA GetInstance
  sentinel = *(int *)((char *)map + 8);        // empty sentinel node*
  map = (int *)FUN_00438c40();
  // FUN_0046c1b0: ESI=map, ECX=&param_1, EAX=&node_out  → find tag
  FUN_0046c1b0(/* key=&param_1, out=&node_out; this=map in ESI */);
  if (node_out != (int *)sentinel) {
    vog_LogMessage(
      "..\\arda2/storage/stoAbstractUnserializationFactory.h",
      0x24, 2,
      "Inserting duplicate creator tag to factory");
  }

  map = (int *)FUN_00438c40();
  FUN_0046c1b0(/* same key lookup */);
  if (node_out == (int *)*(int *)((char *)map + 8)) {
    // miss → insert
    key_pair[0] = param_1;
    key_pair[1] = 0;
    // EDI = map for FUN_0046bf90
    inserted = (int *)FUN_0046bf90(insert_pair, (uint *)key_pair);
    node_out = (int *)*inserted;
  }
  // always (re)bind creator at node+0xc
  *(uint32_t *)((char *)node_out + 0xc) = (uint32_t)/*EBX*/;
}
```

## Open questions

- Product English for tag domain A vs B (sibling insert `FUN_00444a40` / MapB).
- Exact NestedHash node/allocator internals inside unowned `FUN_0046bf90` / `FUN_0046c1b0`.
- Runtime / bit-exact / differential verification.
