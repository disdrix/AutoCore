# Raw capture: FUN_0044b5c0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0044b5c0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0044b5c0` |
| **Canonical name** | `FUN_0044b5c0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __thiscall FUN_0044b5c0(int param_1,undefined4 param_2)



{

  undefined4 uVar1;

  int unaff_EDI;

  

  if ((*(uint *)(param_1 + 0xbc) >> 1 & 1) == 0) {

    param_1 = param_1 + 0x40;

  }

  else {

    param_1 = FUN_009730e0();

  }

  if ((*(byte *)(unaff_EDI + 0xbc) & 1) != 0) {

    uVar1 = FUN_00972e50();

    FUN_00412d40(param_2,uVar1,param_1);

    return param_2;

  }

  FUN_00412d40(param_2);

  return param_2;

}
```

---

## W22-C dual seal append — 2026-07-29

**Agent:** OWN-ONLY dual W22-C  
**Tools:** `decompile_function` + `analyze_function_complete` + `read_memory` (no `disassemble_bytes`)  
**Live decompile:** ≡ raw CF; **phantoms:** `unaff_EDI` is a real live-in companion object; second `FUN_00412d40` path is **3-arg** (decompiler dropped args).

### Body / ABI (byte-sealed)

| Fact | Value |
|---|---|
| Body | `0x0044b5c0`–`0x0044b60f` (**80 B**) |
| Convention | **cdecl** (`ret` / `C3` ×2) + **ECX** + **EDI** live-in |
| ECX | primary / xform object |
| EDI | companion / scaleSrc object (flags @ `+0xbc`) |
| stack0 | `float* outMatrix` (16 floats / 4×4) |
| Return | **EAX = outMatrix** (passthrough of stack0) |

### Full body hex (`read_memory`)

```
8b 81 bc 00 00 00 53 8b 5c 24 08 d1 e8 a8 01 56 74 09 e8 09 7b 52 00 8b f0 eb 03 8d 71 40
f6 87 bc 00 00 00 01 74 17 8b cf e8 62 78 52 00 56 50 53 e8 4a 77 fc ff 83 c4 0c 5e 8b c3 5b c3
8b c7 56 50 53 e8 38 77 fc ff 83 c4 0c 5e 8b c3 5b c3
```

### Byte-reconstructed CF

```
; ECX=objA, EDI=objB, [esp+4]=out  (after push ebx: [esp+8]=out)
mov eax, [ecx+0xbc]
push ebx
mov ebx, [esp+8]          ; out
shr eax, 1
test al, 1                ; bit1 of flags@+0xbc
push esi
jz  use_cached
call FUN_009730e0         ; ensure matrix @ objA+0x40; returns +0x40
mov esi, eax
jmp have_mat
use_cached:
lea esi, [ecx+0x40]       ; cached matrix
have_mat:
test byte ptr [edi+0xbc], 1   ; bit0 companion dirty/full-xform
jz  compose_raw
mov ecx, edi
call FUN_00972e50             ; refresh companion transform; EAX=companion
push esi                      ; matA
push eax                      ; companion
push ebx                      ; out
call FUN_00412d40             ; compose 16 floats → out
add esp, 0x0C
pop esi
mov eax, ebx
pop ebx
ret
compose_raw:
mov eax, edi
push esi
push eax
push ebx
call FUN_00412d40
add esp, 0x0C
pop esi
mov eax, ebx
pop ebx
ret
```

### Flag bits @ object `+0xbc`

| Bit | Role (from this + callees) |
|----:|---|
| 0 | companion full-transform dirty → `FUN_00972e50` refresh before compose |
| 1 | primary matrix dirty → `FUN_009730e0` rebuild `@+0x40` else use `@+0x40` raw |

### Callees

| Symbol | VA | Role |
|---|---|---|
| `FUN_009730e0` | `0x009730e0` | ensure primary matrix `@+0x40`; clear bit1 |
| `FUN_00972e50` | `0x00972e50` | rebuild companion transform; clear bit0; return companion |
| `FUN_00412d40` | `0x00412d40` | `out[16] = compose(arg1, arg2)` via `FUN_00412d80` + 16-dword copy |

### Callers

| Site | Parent |
|---|---|
| `0x0074c9eb` | `Bounds_AccumulateTransformedLocal` (`0x0074c9c0`) — ECX=xformRef, EDI=scaleSrc, push local matrix |

### Name

`Object_WriteComposedMatrix` (Ghidra `FUN_0044b5c0`)
