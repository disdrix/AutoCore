# Annotated low-level: Color_PackFloat4_Scale255_Inferred / FUN_00973820

| Field | Value |
|---|---|
| Stable ID | `aa_00973820` |
| VA | `0x00973820`–`0x00973872` (83 B) |
| System | client color pack utility |
| Date | 2026-07-29 (W28-E OWN re-annotate) |

## Machine-level notes

- **ABI (byte-sealed):** **ECX** = dest packed-color buffer; stack `(float *src4)`; epilogue **`ret 4`** (`C2 04 00`).
- Decompiler surface shows only the stack float* and drops ECX → EAX handoff (`8B C1` before call).
- **Constant:** `DAT_00aaa6f8` = **255.0f** — scale normalized float channels to 8-bit range.
- **Callee:** `FUN_00973590(f0*255, f1*255, f2*255, f3*255)` with EAX=ECX dest; clamps each channel to [0,255] with ROUND.
- **Worker** (not leaf).

## Callers

| Caller | Role |
|---|---|
| `FUN_004b18f0` (`ObjectMotion_SyncChildPoseHF_Inferred`) | pack `DAT_00aef980` → `child+0x34` |
| `FUN_005b36f0` | fog color path (`gFogColor` adjacent) |
| `FUN_005e1fc0` | serialize/write color fields (float src + local dest) |
| `FUN_008e4430` | difficulty tier color pack (Trivial…Very Hard) |

## Pseudocode (behavior-preserving)

```c
// ECX = dest byte[4]; stack float* src; ret 4
void Color_PackFloat4_Scale255_Inferred(uint8_t *dest /*ecx*/, const float *src)
{
  // FUN_00973590 packs clamped ROUND values into dest (EAX=dest)
  FUN_00973590(src[0] * 255.0f, src[1] * 255.0f, src[2] * 255.0f, src[3] * 255.0f);
}
```

## Open questions

- Product/PDB name of pack helper and exact D3DCOLOR English.
- Nested dual of `FUN_00973590` (channel index map sealed by its decompile; not OWN).
- Runtime / bit-exact — open.
