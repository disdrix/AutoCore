# Raw capture: FUN_0075d610

| Field | Value |
|---|---|
| **Stable ID** | `aa_0075d610` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0075d610` |
| **Canonical name** | `FUN_0075d610` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __thiscall FUN_0075d610(int param_1,int param_2)

{
  FUN_0044e8c0(&param_2);
  if ((param_2 != *(int *)(param_1 + 8)) && (*(int *)(param_2 + 0xc) != 0)) {
    return 1;
  }
  return 0;
}
```

---

## W36-E re-verify append (2026-07-29)

**Tools:** `batch_decompile` / `decompile_function` + `analyze_function_complete` + `get_function_by_address` + `read_memory` (full body) + callers/xrefs + callee context (`FUN_0044e8c0` dual). **No** `disassemble_bytes`.
**Live decompile:** CF ≡ scaffold 2026-07-23 (HashMap find → end/null gates → 0/1).

### Body (48 B / 0x30)

`0x0075d610`–`0x0075d63f` inclusive; dual `C2 04 00` (`RET 4`); next body at `0x0075d640`.

Full hex:
```
538b5c2408578d44240c8bf950e89e12cfff8b44240c3b47085f5b740e83780c007408b801000000c2040033c0c20400
```

### Sealed ABI / layout (bytes win)

| Fact | Evidence |
|---|---|
| **ECX = map/cache** (`MOV EDI,ECX`) | NestedHash head of `NDResourceCache` / `DAT_00d1f050` |
| **Stack formal = key\*** | `MOV EBX,[ESP+8]` after `PUSH EBX` = original `[ESP+4]` |
| **HashMap_FindIterator ABI** | `EDI=map`, `EBX=key*`, stack `out_it=&key_slot`; overwrites slot with node\* |
| **End sentinel** | `CMP EAX,[EDI+8]` — miss when `it == map+0x08` |
| **Value non-null** | `CMP DWORD PTR [EAX+0xC],0` — node value / resource\* |
| **Return** | `EAX=1` hit; `XOR EAX,EAX` miss; **`RET 4`** |
| **Callee** | sole: `FUN_0044e8c0` (`HashMap_FindIterator_Inferred`, W21-N) |

### Callers (3 fn / 6 xrefs)

| Caller | Sites | Role |
|---|---|---|
| `FUN_004ed310` | `0x004ed3d8`, `0x004ed404` | AssPreloader_ProcessKeyRingStep (W35-A) — cache-present gate |
| `FUN_00583fa0` | `0x00583ff0`, `0x005841b9`, `0x005841cd` | CVOGGraphicsBase LoadNextChildAsset path |
| `FUN_004b6ab0` | `0x004b6b3b` | FX name preload gate (`FUN_00989e00` key) |

### Semantics (sealed)

```
// thiscall ECX=cache; stack key*; RET 4; EAX 0/1
it = HashMap_FindIterator(cache, key)   // out overwrites key stack slot
return (it != cache.end(+0x08)) && (*(void**)(it + 0x0C) != NULL)
```

**Name (Inferred):** `NDResourceCache_IsKeyPresent_Inferred`. **Reject** scaffold `Named_CalleeOf_*VOG_DEBUG*`.
