# Annotated low-level: Object_ApplyPrimaryAndLocalMaterialBanks_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004c5510` |
| VA | `0x004c5510`–`0x004c555d` (**78 B**) |
| Ghidra name | `FUN_004c5510` |
| Canonical name | `Object_ApplyPrimaryAndLocalMaterialBanks_Inferred` |
| System | object / gfx material bank (R10 partition label: skills-abilities residual) |
| Date | 2026-08-05 (R10-029 dual seal; prior scaffold 2026-07-23) |
| Parent dual | `0x005194f0` `Gfx_BindMatDiffuseAmbientEmissive_Inferred` |
| Sibling callee | `0x0051b310` `Object_ApplyMaterialBank_Inferred` |

## Machine-level notes

- **ABI:** `__thiscall` / `__fastcall` ECX = MI **subobject** this. No stack formals. Bare `RET` (`C3`). Preserves ECX via `PUSH ECX` / `POP ECX`; uses ESI as this scratch.
- **Entry:** Only via **JMP adjustor thunks** (`0x004cc9f3`, `0x0053bcf9`) — not direct CALL sites in Ghidra callers list.
- **Path A — primary bank:** Read vbptr-like at `this-0x4FC`; `primary = this-0x4FC + *(vbptr+4)`; `Object_ApplyMaterialBank_Inferred(primary)` which gates bank at `primary+0x144/+0x148/+0x14C` and binds via `vtbl+0x1CC` host.
- **Path B — local bank:** If `host = *(this-0x27C) != 0` and local bank begin `*(this-0x208) != 0` and `(end-begin)>>4 != 0`, call `Gfx_BindMatDiffuseAmbientEmissive_Inferred(host, this-0x20C)` (cdecl, `ADD ESP,8`).
- **Decompiler gap:** Live decompile shows `FUN_0051b310()` with no this rewrite — assembly at `004c5514`–`004c5524` **must** be restored in clean/ports.
- Prefer assembly when decompiler conflicts (this adjust + ESI clobber after count).

## Pseudocode (annotated, bytes-aware)

```c
// thiscall: ECX = MI subobject*
void Object_ApplyPrimaryAndLocalMaterialBanks_Inferred(void *subThis)
{
  // --- Path A: apply primary object's material bank ---
  void *vbptr = *(void **)((char *)subThis - 0x4FC);
  int delta = *(int *)((char *)vbptr + 4);
  void *primary = (char *)subThis - 0x4FC + delta;
  Object_ApplyMaterialBank_Inferred(primary);   // FUN_0051b310

  // --- Path B: bind local Mat* bank if non-empty ---
  void *host = *(void **)((char *)subThis - 0x27C);
  if (host == nullptr) return;

  void *begin = *(void **)((char *)subThis - 0x208);
  void *bankHeader = (char *)subThis - 0x20C;   // begin@+4 end@+8
  if (begin == nullptr) return;

  void *end = *(void **)((char *)bankHeader + 8);
  int count = ((char *)end - (char *)begin) >> 4;  // stride 0x10
  if (count == 0) return;

  Gfx_BindMatDiffuseAmbientEmissive_Inferred(host, bankHeader);  // FUN_005194f0
}
```

## Offset map (relative to MI subobject this)

| Offset | Role | Confidence |
|---|---|---|
| `this-0x4FC` | vbptr / base-table for primary this rewrite | **High** (bytes) |
| `*(vbptr+4)` | additive delta onto `this-0x4FC` → primary | **High** |
| `this-0x27C` | secondary gfx/material **host** pointer | **High** |
| `this-0x20C` | local color/material bank **header** | **High** |
| `this-0x208` | bank begin (`header+4`) | **High** |
| `this-0x204` | bank end (`header+8`) | **High** |
| stride `0x10` | float4 / Mat slot group element | **High** (SAR 4 + dualed binder) |

## Open questions

- Product / RTTI class of the MI subobject that owns these negative offsets.
- Whether `this-0x4FC` is true MSVC vbtable or a custom base-pointer table (layout matches vbtable slot+4 usage).
- When Path B host is populated vs Path A-only objects.
- Runtime / bit-exact verification.
