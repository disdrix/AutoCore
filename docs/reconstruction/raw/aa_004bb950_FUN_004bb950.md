# Raw capture: FUN_004bb950

| Field | Value |
|---|---|
| **Stable ID** | `aa_004bb950` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004bb950` |
| **Canonical name** | `FUN_004bb950` / `Object_ResolveFromTFID` |
| **System** | object-resolve / TFID |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
/* Object_ResolveFromTFID(pTfid)
   
   Thin wrapper: CVOGReaction_ResolveObjectTarget(global, coidLo, coidHi). */

void * Object_ResolveFromTFID(TFID_16 *pTfid)

{
  void *pvVar1;
  
  pvVar1 = (void *)CVOGReaction_ResolveObjectTarget(pTfid->bGlobal,pTfid->dwCoidLo,pTfid->dwCoidHi);
  return pvVar1;
}
```

---

## Live re-decompile + image seal (2026-07-29)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` + `analyze_function_complete` + `get_assembly_context` on call sites |
| **Function range** | `0x004bb950`–`0x004bb966` (`Object_ResolveFromTFID`) |
| **Ghidra signature (incomplete)** | `void * __stdcall Object_ResolveFromTFID(TFID_16 * pTfid)` — **misses ECX this** |
| **Callee** | `CVOGReaction_ResolveObjectTarget` @ `0x004bae70` only |
| **Xref count** | ≥100 (skills, inventory Recv*, interact/patrol, drive, combat) |

### Body bytes (`read_memory` @ `0x004bb950`, 23 B + INT3 pad)

```
8B 44 24 04    MOV  EAX, [ESP+4]       ; pTfid (stack arg0 after ret)
8B 50 04       MOV  EDX, [EAX+4]       ; coidHi
52             PUSH EDX
8B 10          MOV  EDX, [EAX]         ; coidLo
8A 40 08       MOV  AL,  [EAX+8]       ; bGlobal (AL only; upper EAX residual)
52             PUSH EDX                ; coidLo
50             PUSH EAX                ; bGlobal in low byte
E8 0C F5 FF FF CALL 0x004bae70         ; CVOGReaction_ResolveObjectTarget (ECX preserved)
C2 04 00       RET  4                  ; clean 1 stack dword
```

Hex: `8b4424048b5004528b108a40085250e80cf5ffffc20400`

Rel32 check: next=`0x004bb964`, disp=`0xFFFFF50C` → target `0x004bae70`.

### Decompiler gap (sealed by call-site assembly)

Ghidra shows a free/`__stdcall` wrapper with **no** `this`. Image **does not** load ECX inside this body; **callers** load resolve-context into ECX before `CALL 0x004bb950`:

| Site | VA of CALL | ECX setup immediately prior |
|---|---|---|
| `CVOGReaction_CastSkillOnTarget` | `0x004d0b94` | `MOV ECX, [EAX+0xe4e8]` then `PUSH ESI` |
| `Client_EvalAutoPatrolWaypoint` | `0x00929f25` | `MOV ECX, [ECX+0xd34]` then `PUSH ESI` |
| `Client_FindFirstObjectInRadius` | `0x009198a5` | `MOV ECX, [[DAT_00d1b644]+0xe4e8]` then `PUSH` tfid slot |
| `Client_RecvInventoryEquip` | `0x00813fa3` | `MOV ECX, [EAX+0xe4e8]` then `PUSH EDI` |

**Contract:** `__thiscall` passthrough — ECX = resolve context (same as `CVOGReaction_ResolveObjectTarget`); stack = `TFID_16*`; `RET 4`.

### TFID field unpack (this body)

| Offset | Width | Push / use | Repo field |
|---|---|---|---|
| `+0` | u32 | 2nd stack arg to Resolve | `dwCoidLo` |
| `+4` | u32 | 1st stack arg to Resolve | `dwCoidHi` |
| `+8` | u8 | low byte of 0th stack arg | `bGlobal` |

No null check on `pTfid`. No stores. No other callees.
