# Annotated low-level: Phy_CPDefinitionGroup_AllocReplaceUnserialize_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004372a0` |
| VA | `0x004372a0`–`0x0043733e` exclusive (**158 B**) |
| Ghidra name | `FUN_004372a0` |
| Canonical name | `Phy_CPDefinitionGroup_AllocReplaceUnserialize_Inferred` |
| System | physics / phyCPDefinition (CPDG chunk) |
| Date | 2026-08-04 (W38-E) |

---

## Machine-level notes

- Source: raw capture + W38-E re-verify (`read_memory` full body, call-site context, callee strings).
- **Bytes win** over decompiler formals: EDI out-slot, stack reader, `RET 4`, status in EAX.
- SEH protects only alloc+ctor; release/store/unserialize run outside ctor scope state (`local_4 = -1`).
- Shared refcount protocol with `RefCountedSlot_ReleaseAndClear` (`0x00437150`): +4 count, vtbl+4 first-ref, vtbl+8 destroy.
- OOM path is **slot-preserving** (contrast morph twin `0x004373b0`).

## ABI (sealed)

```c
// EDI = T** outSlot
// stack = stoChunkReader* reader
// RET 4
// return status (uint32_t); -1 on alloc failure
uint32_t Phy_CPDefinitionGroup_AllocReplaceUnserialize_Inferred(
    /*EDI*/ void** outSlot,
    /*stack*/ void* reader);
```

## Pseudocode (annotated)

```c
// SEH: LAB_009befeb
uint32_t Phy_CPDefinitionGroup_AllocReplaceUnserialize_Inferred(void** outSlot /*EDI*/, void* reader /*stack*/)
{
  void* mem = operator_new(0x14);
  PhyCPDefGroup* obj = mem ? FUN_004371c0(mem) : nullptr;  // vtbl 00a9da68
  if (!obj) {
    return 0xFFFFFFFFu;  // *outSlot unchanged
  }

  // AddRef
  if (++obj->refcount == 1) {
    obj->vtbl->on_first_ref(obj);  // vtbl+4 → FUN_0056f570 nop
  }

  // Release old slot
  PhyCPDefGroup* old = (PhyCPDefGroup*)*outSlot;
  if (old) {
    if (--old->refcount == 0) {
      old->vtbl->destroy(old);  // vtbl+8 → FUN_00464890
    }
  }
  *outSlot = obj;

  // FUN_00989850: stack=obj, EBX=reader; opens CPDG chunk
  return Phy_CPDefinitionGroup_Unserialize_CPDG(obj, reader);  // FUN_00989850
}
```

## Control flow

```
SEH enter
  new(0x14) + ctor
SEH arm off
if !obj: return -1
AddRef (+ first-ref vfunc)
Release *EDI if any
*EDI = obj
status = unserialize(obj, reader)
return status
```

## Cross-links

| Related | VA | Role |
|---|---|---|
| Ctor | `0x004371c0` | phyCPDefinitionGroupImpl construct |
| Unserialize | `0x00989850` | CPDG reader (unowned) |
| Release helper | `0x00437150` | RefCountedSlot_ReleaseAndClear |
| Morph twin pattern | `0x004373b0` | host+4 slot alloc-replace (gfx) |
| gfxBody consumer | `0x00765740` | multi-chunk load caller |
