# Annotated low-level: FUN_00829ec0 → UI_MissionJournal_MSelectEntry_Ctor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00829ec0` |
| VA | `0x00829ec0` |
| System | client UI / mission journal |
| Date | 2026-08-04 (WQ7R-E dual seal) |
| Canonical | `UI_MissionJournal_MSelectEntry_Ctor_Inferred` |

## Machine-level notes

- Source: raw capture + WQ7R-E `read_memory` / live decompile (no `disassemble_bytes`).
- **Alloc size** at callers: `operator_new(0x510)` — object ends at `+0x50C` dword.
- **ABI:** stack `this` (`[esp+arg]` after SEH), returns `this` in EAX, **`ret 4`**.
- **Base:** `FUN_00792d20` = `NDUIContainerPanel_Ctor_Inferred` (container/panel base).
- **Vtbl:** `PTR_FUN_00a74484` installed after base ctor.
- **Tail fields** match peer chrome builder `UI_MissionJournal_BuildMSelectChrome_Inferred` (`0x00829ce0`):
  - `+0x4FC` category-mode byte (cleared)
  - `+0x4FD` complete-flag byte (cleared)
  - `+0x500` sentinel **−1** (likely unbound mission key / selection)
  - `+0x504` / `+0x508` / `+0x50C` child widget slots (null)
- **Skin:** `NDUIWindow_ReloadInterface("i_d_npc_2d_btn_mselect.xml")` — root mselect entry skin.
- **Role:** mission-journal **entry row** widget ctor used by `Client_UpdateMissionJournal` for Active/New mission lists (control ids base `0x9c4f`).

## Pseudocode (annotated)

```c
// UI_MissionJournal_MSelectEntry_Ctor_Inferred
// stack this; ret 4; returns this
undefined4 * FUN_00829ec0(undefined4 *this)
{
  // SEH frame LAB_009b432e
  FUN_00792d20(this);                      // NDUIContainerPanel base
  *this = &PTR_FUN_00a74484;               // mselect entry vtbl
  *(uint8_t *)((char *)this + 0x4FD) = 0;  // complete flag
  *(uint8_t *)((char *)this + 0x4FC) = 0;  // category mode  (param_1[0x13f] low byte)
  *(uint32_t *)((char *)this + 0x500) = 0xFFFFFFFF; // selection / mission key sentinel
  *(uint32_t *)((char *)this + 0x504) = 0; // mission button slot
  *(uint32_t *)((char *)this + 0x508) = 0; // title slot
  *(uint32_t *)((char *)this + 0x50C) = 0; // status slot
  NDUIWindow_ReloadInterface("i_d_npc_2d_btn_mselect.xml");
  return this;
}
```

## Open questions

- Product C++ class name (Inferred from XML + journal path).
- Exact semantic of `+0x500` beyond “unbound sentinel −1” (mission id written later by journal path — probable).
- Full vtbl map for `PTR_FUN_00a74484`.
- Runtime / bit-exact image verification.
