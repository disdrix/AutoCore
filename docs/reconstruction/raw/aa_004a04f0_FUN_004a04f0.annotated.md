# Annotated low-level: MissionTree_InorderCollectMediaPreloads_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004a04f0` |
| VA | `0x004a04f0`–`0x004a057a` exclusive (**138 B**) |
| Canonical | `MissionTree_InorderCollectMediaPreloads_Inferred` |
| Ghidra | `FUN_004a04f0` |
| System | mission host tree → media preload collect |
| Date | 2026-08-04 (W37-C) |

## Machine-level notes

- **ABI:** **thiscall ECX=`tree_holder*`**; stack **4 dwords** (same shape as `004b88e0`: unused + 3 flags); void; **`RET 0x10`**.
- Tree access: `p = *ecx`; `head = *(p+4)`; begin = `*head`; empty when begin==head.
- Node: **+0 left, +4 parent, +8 right**, **+0x10 payload** (mission-host* → ECX of `004b88e0`), **+0x15 nil-gate** for successor.
- Sole callee: `MissionHost_CollectMediaPreloads_Inferred` (`0x004b88e0`, W32-H) — flag-gated path enqueue.
- Parents set ECX from **object+0x88** (pointer) or embed at **subobj+0x44**; flag triples select graphics/sound/physics/all.
- Decompile CF ≡ live; RET width sealed via `read_memory` (`C2 10 00`).

## Pseudocode (annotated)

```c
// ECX=tree_holder*; stack (unused, flag_media, flag_audio, flag_paths); RET 0x10
void __thiscall MissionTree_InorderCollectMediaPreloads_Inferred(
    TreeHolder* holder,
    uint32_t unused,
    uint32_t flag_media_strings,
    uint32_t flag_mission_audio,
    uint32_t flag_obj_paths)
{
  void* head = *(void**)(*(uint8_t**)holder + 4);
  TreeNode* node = *(TreeNode**)head; // leftmost
  if (node == head) {
    return;
  }
  do {
    void* host = node->payload; // +0x10
    MissionHost_CollectMediaPreloads_Inferred(
        host, unused, flag_media_strings, flag_mission_audio, flag_obj_paths);
    // MSVC-style inorder successor (nil @ +0x15)
    node = TreeInorderSuccessor(node, head);
  } while (node != head);
}
```

## Open gaps

- Product English for tree/holder type (no RTTI/string on this VA).
- Exact MSVC `_Tree` field map for holder first-indirection (`*ecx` then `+4` head).
- Whether +0x15 is stock `_Isnil` or packing-specific — used as nil-gate only.
- Runtime / bit-exact.
