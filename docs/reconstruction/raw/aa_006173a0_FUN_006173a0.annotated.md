# Annotated low-level: FUN_006173a0 → Map_ConstructIntKeyNode_0x18

| Field | Value |
|---|---|
| Stable ID | `aa_006173a0` |
| VA | `0x006173a0`–`0x006173de` |
| System | std map/set node construction (cross-cutting; int-key family) |
| Date | 2026-07-23 scaffold; **2026-07-29 W18-L dual seal** |

## Machine-level notes

- **ABI sealed:** five stack formals, **`RET 0x14`**; returns **`MapNode18*` in EAX** (OOM → 0). Decompiler `void` is a trap.
- **Size sealed:** `operator_new(0x18)` — matches `Map_InsertNodeAndRebalance_IntKey` / `Map_InsertOrFind_IntKey` family.
- **Layout sealed from stores:** left / parent / right / key / mapped / color / isnil=0.
- **Not** tree walk, rebalance, or size update — pure alloc+fill.
- Prefer this annotated body over scaffold; bytes in raw v2026-07-29 append.

## Pseudocode (annotated)

```c
// __stdcall, RET 0x14; returns node* in EAX
MapNode18 *Map_ConstructIntKeyNode_0x18(
    MapNode18 *left,      // param_1 → +0x00
    MapNode18 *parent,    // param_2 → +0x04
    MapNode18 *right,     // param_3 → +0x08
    const IntPair *value, // param_4 → +0x0C key, +0x10 mapped
    uint8_t color)        // param_5 → +0x14; isnil +0x15 = 0
{
  MapNode18 *node = (MapNode18 *)operator_new(0x18);
  if (node != NULL) {
    node->left   = left;
    node->right  = right;
    node->parent = parent;
    node->key    = value->key;
    node->mapped = value->mapped;
    node->color  = color;
    node->isnil  = 0;
  }
  return node; // EAX
}
```

## Callers (role)

| Caller | Role |
|---|---|
| `Map_InsertNodeAndRebalance_IntKey` `0x005d20b0` | primary sealed insert core (color=0 red) |
| 13 other insert-style sites | same 0x18 node family (shared ctor) |

## Open residual

- Product C++ mangled name / exact template args beyond int/int pair.
- Whether every caller uses int/int (layout assumes 2 dwords from value*).
