# Annotated low-level: FUN_0043c550 / GuardedVector_EraseFirstEqual

| Field | Value |
|---|---|
| Stable ID | `aa_0043c550` |
| VA | `0x0043c550`–`0x0043c5e1` (**146 B**) |
| Canonical | `GuardedVector_EraseFirstEqual` (**Inferred**) |
| Ghidra | `FUN_0043c550` |
| System | STL / guarded ring-vector container |
| Date | 2026-07-29 (OWN-ONLY W28-C seal) |

## Machine-level notes

- **ESI=container** at entry (not loaded inside body). Callers set it:
  - `FUN_00423e00`: `MOV ESI,ECX; ADD ESI,0x58` then `CALL 0043c550` with key on stack.
  - `FUN_007b65d0` / assManager cleanup: same erase-first path after find helper.
- Optional CS: gate `char +0x2c`; CS object at `+0x14` (EDI holds CS* for Enter/Leave).
- Linear scan `[begin, begin+size)` using same ring block map as family.
- On first `elem == *key`: call `GuardedVector_EraseRange` for `[idx, idx+1)` with **EAX=ESI**; set found=1; leave CS; return.
- ABI: stdcall **1** stack arg; `RET 4`; return bool in **AL**.
- Immediately followed by `GuardedVector_AssignTo` (`0x0043c5f0`) sharing header + CS layout.

## Pseudocode (annotated)

```c
// retail: ESI=container; stack key*; RET 4; AL=bool
uint8_t GuardedVector_EraseFirstEqual(int* key /*stack*/, GuardedVectorHeader* container /*ESI*/)
{
  if (container->lockEnable) EnterCriticalSection(&container->cs);
  uint8_t found = 0;
  uint32_t idx = container->begin;
  uint32_t end = container->begin + container->size;
  while (idx != end) {
    if (ring_elem(container, idx) == *key) {
      GuardedVector_EraseRange(/*out*/, /*seeds*/, idx, /*seeds*/, idx + 1); // EAX=container
      found = 1;
      break;
    }
    idx++;
  }
  if (container->lockEnable) LeaveCriticalSection(&container->cs);
  return found;
}
```

## Open residual

- Product/PDB English for container class / element type (dword compare only sealed).
- Nested EraseRange free already dual-sealed W27-C (not re-owned here).
- Runtime / multi-match policy (first only) is sealed; no “erase all” path in this body.
