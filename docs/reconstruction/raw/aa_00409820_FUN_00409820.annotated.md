# Annotated low-level: FUN_00409820 → StdTree_Predecessor_Isnil29_Edx_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00409820` |
| VA | `0x00409820`–`0x00409870` inclusive (**81 B** / `0x51`) |
| System | shared client::stdtree (MSVC `_Tree` predecessor / iterator--) |
| Date | 2026-07-23 scaffold; **2026-08-05 WQ9J-G dual seal** |

## Machine-level notes

- **ABI:** **EDX = Node**** (iterator); ECX unused phantom; void; bare **`C3`** at three exits.
- Entry: `8B 02` = `MOV EAX,[EDX]` — not ECX (`8B 01` peer at `0x004cb4f0`).
- **isnil every guard `+0x29`** (`80 xx 29 00`).
- **Three arms (predecessor, not successor):**
  1. if current isnil → `*it = right` (header → rightmost / max)
  2. else if left non-nil → rightmost of left subtree
  3. else climb parents while was **left**-child; `*it` = that ancestor
- **Leaf:** no callees.
- **Sole caller** `FUN_00405bd0` @ `0x00405c33`: `LEA EDX,[ESP+0x0C]` then `CALL` — insert-or-find goLeft/prev path (hi/lo key compare neighborhood).
- **Twin:** `StdTree_Predecessor_Isnil29_Inferred` (`0x004cb4f0`, ECX ABI) — same CF/isnil family.
- **Not** successor (`StdTree_IteratorIncrement_Isnil31` walks right-min + climb while right-child).
- Scaffold chain-of-caller `Named_CalleeOf_*Error_Receiv*` **rejected**.

## Pseudocode (annotated)

```c
// EDX = NodeIsnil29**; void; bare RET
void StdTree_Predecessor_Isnil29_Edx_Inferred(NodeIsnil29** it /*EDX*/)
{
  NodeIsnil29* node = *it;
  if (node->isnil /*+0x29*/) {
    *it = node->right;           // end/header → max
    return;
  }
  NodeIsnil29* left = node->left;
  if (!left->isnil) {
    // rightmost of left subtree
    while (!left->right->isnil)
      left = left->right;
    *it = left;
    return;
  }
  NodeIsnil29* parent = node->parent;
  if (!parent->isnil) {
    while (!parent->isnil && *it == parent->left) {
      *it = parent;
      parent = parent->parent;
    }
    if (!parent->isnil)
      *it = parent;
  }
}
```

## Open / residual

- Product map key/value English for this isnil29 instance (caller uses 2-dword hi/lo keys @ node+0x10/+0x14).
- Relationship to ECX twin call graph (multi-domain vs this single residual caller).
- Runtime empty-tree / header-only cases.
- Runtime / bit-exact / differential.
