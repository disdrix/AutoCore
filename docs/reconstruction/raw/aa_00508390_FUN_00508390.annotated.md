# Annotated low-level: FUN_00508390 → CVOGHBBase_dtor

| Field | Value |
|---|---|
| Stable ID | `aa_00508390` |
| VA | `0x00508390`–`0x005083af` (32 B) |
| System | `heartbeat` / CVOGHBBase lifecycle |
| Date | 2026-07-29 (W21-A dual seal; prior scaffold 2026-07-23) |

## Machine-level notes

- Source: raw capture + W21-A live `decompile_function` / `read_memory`.
- Prefer assembly when decompiler conflicts (callee **ECX = listMgr**, not HB).
- `param_1[6]` ≡ `HB+0x18` = pOwnerObject (dword index 6).
- `*(owner+0xB0)` = list manager pointer (same layout as `CVOGHBBase_AttachOwnerObject`).
- Base vtbl imm `0x009cdab0` matches ctor install and vtbl slot table duals.

## Pseudocode (annotated)

```c
// CVOGHBBase_dtor — complete object dtor body (does not free)
void __fastcall CVOGHBBase_dtor(void *thisHb /* ECX */)
{
  void *pOwner;
  void *pListMgr;

  pOwner = *(void **)((char *)thisHb + 0x18);
  *(void **)thisHb = &PTR_FUN_009cdab0;           // ALWAYS restore base vtbl
  if (pOwner != NULL) {
    pListMgr = *(void **)((char *)pOwner + 0xB0);
    if (pListMgr != NULL) {
      // machine: ECX = pListMgr, stack = thisHb
      FUN_005085b0(pListMgr, thisHb);             // remove-by-key from owner list
    }
  }
  return; // bare ret
}
```

## Sealed offsets

| Offset | Role |
|---|---|
| `this+0x00` | vtbl → `PTR_FUN_009cdab0` |
| `this+0x18` | pOwnerObject |
| `owner+0xB0` | list manager object pointer |

## Open questions

- Product/PDB mangled symbol (`??1CVOGHBBase@@...`) — open.
- Whether every derived complete dtor always chains here after family vtbl restore (many do; not exhaustively audited).
