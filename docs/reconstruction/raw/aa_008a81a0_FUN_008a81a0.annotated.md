# Annotated low-level: Client_InteractPrompt_SetTarget_Inferred (FUN_008a81a0)

| Field | Value |
|---|---|
| Stable ID | `aa_008a81a0` |
| VA | `0x008a81a0`–`0x008a856e` (**974 B**) |
| Canonical | `Client_InteractPrompt_SetTarget_Inferred` |
| Ghidra | `FUN_008a81a0` |
| System | `interaction-activation` / UI chrome |
| Date | 2026-07-29 (W19-I OWN dual seal) |

## Machine-level notes

- **ABI:** stack formal `promptHost*` cleaned by **`ret 4`**. Target object lives in **ESI** (caller convention; decompiler `unaff_ESI`). Not `__thiscall` on the host (host is stack arg; body loads `[ebp+8]`).
- **Role:** classify nearby world object and push **“Press \<key\> to …”** chrome onto the interact prompt host. **Does not** send UseObject (`0x2072`).
- **Primary caller:** `Client_RefreshInteractPromptTarget_Inferred` (`0x009210e0`) leaves picked object in ESI and calls with `DAT_00d1b8c8`.

## Control flow (byte-sealed)

```
host = [ebp+8]
if (ESI == *(host+0x50c)) return;          // same target
if (DAT_00d1b6d8 == 0) return;             // no local character
if (*(host+0x68c) == 0) return;            // no prompt widget

FUN_007a69d0();                            // resolve key-bind / input name source → EBX
zero caption[0x100], keyBuf[0x100]
showFlag = 0

if (ESI == NULL):
    *(host+0x50c) = 0
    goto SHOW_ONLY

// --- talk path ---
if (*(cloneBase(ESI)+0x38) == 0x12):       // type token NPC-ish
    dyn = __RTDynamicCast(...)
    if (FUN_008a8140(/*EAX=dyn*/)):        // talkable gate
        maybe copy default key tip DAT_00d1be65 if FUN_007f9160(0xc,0)==0
        name = adj(dyn)->vtbl+0x160(-1); localize
        verb = "to talk to"
        goto FORMAT

// --- pick up ---
else if (FUN_005130e0(ESI, 1)):            // thiscall ESI
    maybe key tip 0xc / DAT_00d1be65
    name = ESI->vtbl+0x15c(-1); localize
    verb = "to pick up"
    goto FORMAT

// --- trade ---
else if (ent = ESI->vtbl+0x210(0)):
    // optional faction/self gates; skip if same faction ids @ +0x164/+0x168
    // key tip 0x1e / DAT_00d1c20d
    name = adj(ent)->vtbl+0x160(-1)
    verb = "to trade with"
    goto FORMAT  (or fall through)

// --- interact default ---
if (ESI[0x47] != 0 || CVOGCharacter_FindActiveObjectiveIdForInteract(localChar, ESI) != 0):
    maybe key tip 0xc
    name = ESI->vtbl+0x15c(-1)
    verb = "to interact with"
    FORMAT:
      sprintf(caption, "%s <%s> %s %s", "Press", keyBuf, verb, name)  // via FUN_007a6de0
      showFlag = 1
      *(host+0x50c) = ESI
else:
    // leave caption empty; do not update +0x50c

SET_CAPTION:
widget = *(host+0x68c)
widget->vtbl+0x1d8(caption, 1, 1)
*(host+0x51c) = *(host+0x518)

SHOW_ONLY:
widget->vtbl+0xfc(showFlag, 0.5f /*0x3f000000*/)
ret 4
```

## Host / globals

| Symbol / off | Role |
|---|---|
| `param host +0x50c` | cached prompt target object* |
| `param host +0x518` / `+0x51c` | post-caption copy |
| `param host +0x68c` | prompt UI widget* |
| `DAT_00d1b6d8` | local character base |
| `DAT_00d1b644` | world / client mgr (trade gate `+0xf6`) |
| `DAT_00d1be65` | default key-name string (tip 0xc miss) |
| `DAT_00d1c20d` | trade key-name string (tip 0x1e miss) |
| `DAT_00d1b8c8` | sole known host instance (caller) |

## Verb / string evidence

| Verb | Gate |
|---|---|
| `to talk to` | clone type `0x12` + `FUN_008a8140` |
| `to pick up` | `FUN_005130e0(1)` |
| `to trade with` | `vtbl+0x210` entity + faction filters |
| `to interact with` | flag `ESI[0x47]` or active objective via `FUN_00524520` |

## Decompiler noise

| Artifact | Reality |
|---|---|
| `unaff_ESI` | intentional ESI-this convention from callers |
| `__RTDynamicCast()` / `FUN_008a8140()` zero-arg plate | EAX/this staged by prior instructions |
| Self-callee in scaffold notes | false; no recursive call |

## Open

- Product class name of `DAT_00d1b8c8` / host.
- Product English method name (keep `_Inferred`).
- Full schema of `FUN_008a8140` talkable gate (sibling residual).
- Containing function of second xref `0x009254ae`.
- Runtime / bit-exact image diff.

## Pseudocode

See `reconstructed-exact/Client_InteractPrompt_SetTarget_Inferred.cpp`.
