# Annotated low-level: StdList_Incsize_Thiscall (FUN_00404840)

| Field | Value |
|---|---|
| Stable ID | `aa_00404840` |
| VA | `0x00404840`–`0x004048AE` |
| Ghidra name | `FUN_00404840` |
| Canonical | `StdList_Incsize_Thiscall` |
| System | shared MSVC `std::list` (partition seed: missions-progression) |
| Date | 2026-08-05 (R10-006 dual refresh; prior scaffold 2026-07-23) |
| Dual | A+B **accept** |

## Machine-level notes

- **Role:** MSVC `std::list::_Incsize(count)` — size bump with overflow throw.
- **ABI:** **`__thiscall`** — **ECX** = list*, stack dword = `count`, epilogue **`RET 4`** (`C2 04 00`).
- **Size field:** `list+8` = `_Mysize` (`8B 41 08` / `89 41 08`).
- **Max:** immediate **`0x3fffffff`** (`BE FF FF FF 3F`) — same as fastcall twin `StdList_Incsize` `0x0043fe60`, **not** `0x1fffffff` (`StdList_Incsize_Max1fffffff` `0x00416010`).
- **Gate:** `(0x3fffffffU - size) < count` → throw path.
- **Throw:** construct `"list<T> too long"` @ `0x00a1581c` → `FUN_00401a40` (length_error from string) → `_CxxThrowException` + ThrowInfo `DAT_00acc388` (noreturn).
- **Success:** `_Mysize += count`; return.
- **SEH:** frame via `LAB_009bc919` (present; not port-critical).
- **Not:** node alloc/link (BuyNode parent residual `0x00418700`); not vector; not mission-only API despite some callers.
- **Twin contrast:** sealed `StdList_Incsize` @ `0x0043fe60` is **fastcall** ECX=count / EDX=list* / bare `RET` — **do not merge**.

## Pseudocode (annotated)

```c
// MSVC std::list::_Incsize — thiscall specialization
// this (ECX): StdListHeader*  (_Mysize @ +8)
// count (stack): size_type
// RET 4

void __thiscall StdList_Incsize_Thiscall(int list /*ECX*/, uint count /*[esp+4]*/)
{
  // SEH: ExceptionList / LAB_009bc919
  if (0x3fffffffU - *(int *)(list + 8) < count) {
    // basic_string("list<T> too long") @ 0x00a1581c
    // FUN_00401a40 → length_error
    // _CxxThrowException(..., &DAT_00acc388);  // noreturn
  }
  *(uint *)(list + 8) = *(int *)(list + 8) + count;
  return; // ret 4
}
```

## Call-site pattern

```text
; after BuyNode (FUN_00418700)
PUSH 1
MOV  ECX, list
CALL FUN_00404840          ; Incsize +1
; then rewire Myhead->_Prev / node->_Next
```

## Open questions

- Product C++ exception type English beyond ThrowInfo `DAT_00acc388` (shared with other length_error paths).
- Runtime / bit-exact / differential (no Launcher this dual).
