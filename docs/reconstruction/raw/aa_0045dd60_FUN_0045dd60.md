# Raw capture: FUN_0045dd60

| Field | Value |
|---|---|
| **Stable ID** | `aa_0045dd60` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0045dd60` |
| **Canonical name** | `FUN_0045dd60` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_0045dd60(void)

{
  undefined4 uVar1;
  undefined4 unaff_EDI;
  char local_18;
  int local_14;
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  local_4 = 0xffffffff;
  puStack_8 = &LAB_009bef98;
  local_c = ExceptionList;
  ExceptionList = &local_c;
  stoChunkReader_EnterChunkScope(unaff_EDI);
  local_4 = 0;
  if (local_14 == 0x4b455354) {
    uVar1 = stoChunkReader_readF32Array(unaff_EDI);
    local_4 = 0xffffffff;
    if (local_18 != '\0') {
      FUN_00769e40();
    }
    ExceptionList = local_c;
    return uVar1;
  }
  local_4 = 0xffffffff;
  if (local_18 != '\0') {
    FUN_00769e40();
  }
  ExceptionList = local_c;
  return 0xffffffff;
}
```

---

## W37-P re-verify (2026-08-04) — append only

| Field | Value |
|---|---|
| **Tools** | `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + `get_function_by_address` + callers/xrefs. **No** `disassemble_bytes` |
| **Body** | `0x0045dd60`–`0x0045de07` inclusive (**168 B** / `0xA8`); pad `CC` then next |
| **Live decompile** | ≡ 2026-07-23 raw CF (tag gate, Enter/Leave, readF32Array success path) |
| **Xrefs** | 1 CODE from `FUN_00459750` @ `0x004599d6` |
| **Callees** | `stoChunkReader_EnterChunkScope` (`0x004370b0`), `stoChunkReader_readF32Array` (`0x00436090`), `FUN_00769e40` (leave) |

### Full body hex (168 B)

```

```

### Byte-sealed ABI (decompiler misses formals)

| Slot | Role | Evidence |
|---|---|---|
| **ECX** | `elem_count` (int) | `MOV ESI,ECX`; count math `ESI*7` → EAX for `readF32Array` |
| **EDI** | `stoChunkReader*` | `PUSH EDI` as EnterChunkScope reader; re-push before readF32Array |
| **stack[+4]** | `float* dest` | After frame: `MOV ECX,[ESP+0x28]` → dest this for readF32Array; caller `ADD ESP,4` |
| **RET** | bare `C3` | dual epilogs; caller cleans 4 |
| **Return** | `readF32Array` status on tag hit; **`0xFFFFFFFF`** on tag miss | `OR EAX,-1` fail path |

### Byte-sealed CF

1. SEH + `EnterChunkScope(scope=&stack_frame, reader=EDI)`.
2. Scope tag @ `scope+0x0c` must equal **`KEST`** (`0x4B455354`, imm bytes `54 53 45 4B`).
3. On hit: `readF32Array(dest=stack_arg, reader=EDI, count=elem_count*7)` — **7 f32 = one Pod28**.
4. Leave via `FUN_00769e40` when scope `okFlag` (`scope+0x08`) non-zero.
5. Tag miss → leave (if ok) → return `-1`.

### Call site seal (`FUN_00459750` @ `0x004599c8`…)

```
PUSH [this+0x20]     ; vector begin (MSVC vec @ this+0x1C: begin @ +4)
MOV ECX, ESI         ; count
MOV EDI, EBP         ; reader/ctx
CALL 0x0045dd60
ADD ESP, 4
TEST EAX,EAX / JNZ fail
```
