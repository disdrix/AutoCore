# Raw capture: thunk_FUN_00578a60

| Field | Value |
|---|---|
| **Stable ID** | `aa_00548f90` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00548f90` |
| **Canonical name** | `thunk_FUN_00578a60` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall thunk_FUN_00578a60(undefined4 *param_1)

{
  *param_1 = &PTR_FUN_009d3fdc;
  if ((void *)param_1[0x1a8] != (void *)0x0) {
    operator_delete__((void *)param_1[0x1a8]);
  }
  param_1[0x1a8] = 0;
  if ((void *)param_1[0x1ac] != (void *)0x0) {
                    /* WARNING: Subroutine does not return */
    operator_delete((void *)param_1[0x1ac]);
  }
  param_1[0x1ac] = 0;
  param_1[0x1ad] = 0;
  param_1[0x1ae] = 0;
  FUN_00508390();
  return;
}
```

---

## R13-006 re-verify append (2026-08-05) — OWN-ONLY dual

| Field | Value |
|---|---|
| **Agent** | R13-006 OWN-ONLY dual |
| **Partition** | `docs/reconstruction/reviews/WAVE_2026-08-05_r13_residual_partition_map.md` → **R13-006** |
| **Tools** | Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_by_address` + `get_function_callers` + `get_function_xrefs` + `get_xrefs_to` + `get_xrefs_from` |
| **Forbidden** | parent ledgers; other VAs dualed; Launcher; `disassemble_bytes` primary |
| **Parent dual (evidence only)** | `aa_00578a60` `CVOGHBSkillBase_dtor` (R12-026 **accept-with-gaps**) |
| **Promoted name** | `CVOGHBSkillBase_dtor_Thunk_Inferred` |
| **System** | skills-abilities / CVOGHBSkillBase lifecycle |
| **Dual start** | 2686 |
| **Terminal** | **false** |

### Live classification

| Item | Value |
|---|---|
| Ghidra name | `thunk_FUN_00578a60` |
| Classification | **thunk** (`is_thunk: true`; `analyze_function_complete`) |
| Signature (Ghidra) | `undefined thunk_FUN_00578a60(void)` |
| Body range | `00548f90`–`00548f94` |
| Completeness (effective) | ~15 (thunk profile) |

### Authoritative body (bytes win over decompile)

Decompiler **follows the thunk** and prints the **parent body** at `0x00578a60` (vtbl restore / delete[] / delete / base dtor; false `operator_delete` noreturn). **This VA is only a JMP:**

```text
00548f90  JMP  0x00578a60          ; E9 CB FA 02 00
; pad CC… before next region
```

`disassemble_function` @ `0x00548f90`: sole `JMP 0x00578a60`.

`read_memory` 32 B @ `0x00548f90`:

```text
E9 CB FA 02 00 CC CC CC CC CC CC CC CC CC CC CC
6A FF 68 FB 43 9A 00 64 A1 00 00 00 00 50 64 89
```

Hex body: `e9cbfa0200`

- Body: **`0x00548f90`–`0x00548f94` inclusive** (**5 B** / `0x5`)
- Rel32 LE: `CB FA 02 00` = `0x0002FACB`
- Target: `0x00548f90 + 5 + 0x0002FACB = 0x00578A60`
- Pad: `CC` after body

### Xrefs / callers (this VA)

| From | Kind | Role |
|---|---|---|
| `00651193` in `FUN_00651190` | UNCONDITIONAL_CALL | dualed `CVOGHBSkill_SharedScalarDeletingDtor_Inferred` — always CALL thunk then optional free |
| `009a89d3` in `Unwind@009a89d0` | UNCONDITIONAL_CALL | EH cleanup |
| `009a8a03` in `Unwind@009a8a00` | UNCONDITIONAL_CALL | EH cleanup |
| `009a8a33` in `Unwind@009a8a30` | UNCONDITIONAL_CALL | EH cleanup |

`get_xrefs_from` @ `0x00548f90`: sole **UNCONDITIONAL_JUMP** → `FUN_00578a60` @ `0x00578a60`.

### Parent body (target of JMP — evidence only, dualed R12-026)

Parent role: MSVC **complete destructor body** for `CVOGHBSkillBase` (`__thiscall` ECX=this):

```text
*this = &PTR_FUN_009d3fdc
if [this+0x6a0]: operator_delete[](...); zero
if [this+0x6b0]: operator_delete(...); always zero triad +0x6b0/+0x6b4/+0x6b8
JMP CVOGHBBase_dtor (0x00508390)
```

Does **not** free `this` (scalar deleting dtors own that). Does **not** walk vector elements (OnEnd clear `0x00578ac0`).

### ABI (inherited)

| Item | Value |
|---|---|
| Convention | MSVC **`__thiscall`** — **ECX=this** (parent `MOV ESI,ECX`) |
| Stack formals | none |
| Epilogue | parent tail **JMP** base dtor (no bare RET at parent; no RET at thunk) |
| Local body size | **5 B** JMP only |

### Scaffold caveats sealed

1. Scaffold / live decompile expand parent CF as if local — **false** for this VA (bytes = sole JMP).
2. Scaffold `__fastcall` param_1 is decompiler thiscall presentation of parent — ABI is **ECX=this**.
3. `operator_delete` "does not return" is decompiler noise on parent path.
4. System tag promoted from `unknown` → **skills-abilities**.
