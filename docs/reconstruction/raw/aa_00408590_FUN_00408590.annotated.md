# Annotated low-level: StdTree_IteratorIncrement_Isnil31 (`FUN_00408590`)

| Field | Value |
|---|---|
| Stable ID | `aa_00408590` |
| VA | `0x00408590`–`0x004085db` exclusive (**75 B**) |
| System | shared `client::stdtree` |
| Date | 2026-08-04 WQ9H-H dual seal |
| Ghidra name | `FUN_00408590` |
| Canonical | `StdTree_IteratorIncrement_Isnil31` |

## Machine-level notes

- **Leaf** tree/map **iterator successor** (`operator++`), twin of `StdTree_IteratorIncrement_Isnil19` with is_nil at **`+0x31`**.
- **ABI:** **EDX** = `node**` (iterator object holding current node*). ECX unused. Void; plain **`RET` (`C3`)**.
- **Node links:** `+0` left, `+4` parent, `+8` right; **`+0x31`** is_nil byte (color typically adjacent; unused here).
- **Payload family:** isnil@+0x31 ⇒ value region ends near +0x30 (≈ **0x24**-byte `_Myval` after 0xC header) — product type open.
- Call sites both `LEA EDX,[stack_it]` then `CALL` (`FUN_00408ed0`, `FUN_00944770`). Post-call code also checks `[node+0x31]`.
- **Not** CNDHash freelist (`0x004085e0` is the next leaf); co-located residual only.
- Scaffold alias `Named_CalleeOf_Client_NotifyActiveMissionChanged_*` is a **caller-chain label**, not the role.

## Pseudocode (annotated)

```c
// EDX = iterator* (node**)
void StdTree_IteratorIncrement_Isnil31(int *it /*EDX*/)
{
  // param_1/ECX is decompiler phantom — unused
  int node = *it;
  if (*(char *)(node + 0x31) == '\0') {          // real node (not end/nil)
    int *right = *(int **)(node + 8);
    if (*(char *)((int)right + 0x31) == '\0') {
      // successor = leftmost of right subtree
      char c = *(char *)(*right + 0x31);
      int *walk = (int *)*right;                 // left child of right
      while (c == '\0') {
        c = *(char *)(*walk + 0x31);
        right = walk;
        walk = (int *)*walk;
      }
      *it = (int)right;
      return;
    }
    // climb while current was right-child of parent
    int parent = *(int *)(node + 4);
    c = *(char *)(parent + 0x31);
    while (c == '\0' && *it == *(int *)(parent + 8)) {
      *it = parent;
      parent = *(int *)(parent + 4);
      c = *(char *)(parent + 0x31);
    }
    *it = parent;                                 // may be nil head = end()
  }
  // if already nil: no-op
  return;
}
```

## Offsets

| Offset | Role | Conf |
|---|---|---|
| +0 | left | High |
| +4 | parent | High |
| +8 | right | High |
| +0x31 | is_nil | High (every guard) |

## Open questions

- Product map key/value English type(s) for this isnil family.
- Color bit exact offset (unused by successor).
- Runtime / bit-exact / differential.
