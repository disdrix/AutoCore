# Annotated low-level: StdUninitFillN_BasicString (`FUN_00430390`)

| Field | Value |
|---|---|
| Stable ID | `aa_00430390` |
| VA | `0x00430390`–`0x004303c0` exclusive (**48 B**) |
| System | MSVC `vector` / STL uninit helpers — `basic_string` stride `0x1c` |
| Date | 2026-07-29 (W33-G OWN-ONLY dual) |
| Canonical name | `StdUninitFillN_BasicString` |

## Machine-level notes

- Source: raw capture + W33-G re-verify (`decompile` + `read_memory` + xrefs). **No** `disassemble_bytes`.
- **thiscall**: ECX = allocator/host (forwarded); stack `dest*`, `count`, `value*`; **`RET 0x0C`**.
- Returns `dest + count * 0x1C` via `count*8 - count` then `*4` lea (≡ `*28`).
- Sole work: `FUN_00431700(dest, count, value, alloc, value)` — nested fill loop (not dual-owned).
- Sole static caller: `StdVector_InsertN_BasicString` (`FUN_004306b0`) at fill-n sites after grow / in-place hole.

## Pseudocode (annotated)

```c
// thiscall ECX=alloc; stack dest, count, value; RET 0x0C
// returns dest_end = dest + count * 0x1c
BasicString_0x1c* __thiscall StdUninitFillN_BasicString(
    void* alloc,
    BasicString_0x1c* dest,
    int count,
    const BasicString_0x1c* value)
{
  FUN_00431700(dest, count, value, alloc, value);
  // ADD ESP, 0x14 after cdecl helper
  return (BasicString_0x1c*)((char*)dest + count * 0x1c);
}

// Nested FUN_00431700 (unowned): for (; count; --count) {
//   if (dest) basic_string_copy_ctor(dest, value);
//   dest += 0x1c;
// }
```

## Call graph

| Direction | Targets |
|---|---|
| Callers | `FUN_004306b0` @ `0x004307f9`, `0x00430910` only |
| Callees | `FUN_00431700` (`_Uninit_fill_n` body) |

## Open questions

- Exact MSVC demangle for fill-n + allocator formal role of ECX.
- Whether any non-insert caller exists dynamically (0 static xrefs beyond insert-n).
