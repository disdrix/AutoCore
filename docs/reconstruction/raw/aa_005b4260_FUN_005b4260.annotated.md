# Annotated low-level: FUN_005b4260 / Obj_Ctor_SetVtbl_EmptyListAt10_ZeroF4_ThenPopulate

| Field | Value |
|---|---|
| Stable ID | `aa_005b4260` |
| VA | `0x005b4260`–`0x005b42e6` exclusive (**134 B** / `0x86`) |
| System | host object / MSVC ctor + populate |
| Date | 2026-07-29 (W30-O re-verify) |

## Machine-level notes

- Convenience ctor: same empty-shell init as `FUN_005b3f60`, then `FUN_005b3fa0(this, p2..p6)` under MSVC SEH.
- **No code xrefs** — dead/unreferenced in image; live path is default ctor + separate populate.
- Final `ret 0x14` seals five stack parameters (20 B cleaned by callee).
- SEH handler `0x009a6860`; state transitions `-1` → `0` after list head/size written.
- Float zero at `+0x20..+0x2c` runs immediately before populate; populate may overwrite when enable flag (`param_6`) is non-zero.

## Pseudocode (byte-corrected)

```c
// ECX = this*; five stack args; ret 0x14; returns this
void * __thiscall Obj_Ctor_SetVtbl_EmptyListAt10_ZeroF4_ThenPopulate(
    void *self,
    uint32_t p2, uint32_t p3, uint32_t p4, uint32_t p5, uint32_t p6)
{
  // SEH: handler 0x009a6860, state -1
  void *list = (uint8_t *)self + 0x10;
  void *sentinel;

  *(const void **)self = (const void *)0x009d95b4;
  sentinel = StdList_BuySelfLinkedSentinel_0x30(); // FUN_005b43b0
  *(void **)((uint8_t *)list + 4) = sentinel;
  *(uint32_t *)((uint8_t *)list + 8) = 0;
  // SEH state = 0
  *(float *)((uint8_t *)self + 0x20) = 0.0f;
  *(float *)((uint8_t *)self + 0x24) = 0.0f;
  *(float *)((uint8_t *)self + 0x28) = 0.0f;
  *(float *)((uint8_t *)self + 0x2c) = 0.0f;
  FUN_005b3fa0(self, p2, p3, p4, p5, p6); // not owned this wave
  // SEH unlink
  return self;
}
```

## Layout

Same shell as default ctor (`aa_005b3f60`); populate (`FUN_005b3fa0`) may fill list nodes and rewrite `+0x20..+0x2c` when `p6 != 0`.

## Open questions

- Why no live callers (inlined alternative? leftover export?).
- Full product semantics of `FUN_005b3fa0` args (owned by other unit).
- Product class English name.
- Runtime / bit-exact — open.
