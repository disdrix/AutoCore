# Annotated low-level: FUN_00718250

| Field | Value |
|---|---|
| Stable ID | `aa_00718250` |
| VA | `0x00718250` |
| Body span | `00718250`–`0071827b` exclusive (43 B) |
| System | crt-com / MSVC `_com_error` |
| Date | 2026-07-29 (OWN-ONLY dual seal W25-P) |
| Proposed name | `_com_error_ScalarDeletingDtor` |

## Machine-level notes

- Source: raw capture for `aa_00718250` + live `decompile_function` / `read_memory` 2026-07-29.
- Classic MSVC **scalar deleting destructor**:
  1. Call virtual body dtor `~_com_error` (`0x007182e0`)
  2. If `(flags & 1) != 0` → `operator_delete(this)`
  3. Return `this` in EAX
- Vftable `PTR_FUN_00a14034` slot0 points here (sibling reviews for ctor/dtor already noted).
- Ghidra WARNING “does not return” on `operator_delete` is a **decomp residual** — epilogue still runs.

## Calling convention (sealed)

| Slot | Role | Evidence |
|---|---|---|
| **ECX** | `_com_error* this` | prologue store `[ebp-4]` |
| **stack arg0** | delete flags (`uint`) | `[ebp+8]`; masked with `1` |
| **Return** | `this` in **EAX** | `mov eax, [ebp-4]` |
| **ABI** | `__thiscall` + **1 stack arg**; **`ret 4`** | `c2 04 00` |

## Stage map

| Stage | What |
|---|---|
| 1 | Frame; save this |
| 2 | `~_com_error(this)` — vtbl reinstall, Release IErrorInfo, LocalFree msg |
| 3 | Test flags bit0 |
| 4a | bit0 set → `operator_delete(this)` cdecl |
| 4b | bit0 clear → skip free of object storage |
| 5 | EAX=this; leave; ret 4 |

## Pseudocode (annotated)

```c
// MSVC scalar deleting dtor for _com_error
// public: virtual void * __thiscall _com_error::`scalar deleting destructor'(uint flags)

_com_error * __thiscall FUN_00718250(_com_error *this, uint flags)
{
  _com_error::~_com_error(this);   // 0x007182e0 — does NOT free this
  if ((flags & 1) != 0)
    operator_delete(this);         // free object storage when called as delete
  return this;
}
```

## Object / sizes

| Object | Size | Notes |
|---|---:|---|
| `_com_error` (sibling layout) | 0x10 | vtbl+0; hr+4; IErrorInfo*+8; HLOCAL msg+0xC (from dtor dual) |
| This unit | 43 B code | no locals beyond frame this-copy |

## Open questions

1. Vector deleting path (`flags & 2`) — **not present** in this body (scalar-only).
2. Exact CRT version plate string on this VA — siblings cite VS2003 Debug for `~_com_error`.
