# Annotated low-level: XmlAttr_FetchParseLong_StoreByteIfOk

| Field | Value |
|---|---|
| Stable ID | `aa_00799820` |
| VA | `0x00799820` |
| Body | `0x00799820`–`0x0079984b` (44 bytes) |
| System | XML / mission-def attribute parse |
| Canonical name | `XmlAttr_FetchParseLong_StoreByteIfOk` (**Inferred** structural) |
| Ghidra | `FUN_00799820` |
| Date | 2026-07-29 (W21-G dual seal) |

## Machine-level notes

- **cdecl** four args: XML node, attribute name (wide), `uint8_t *outByte`, radix/base for `wcstol`.
- Allocates 4-byte stack slot (`push ecx`) as `long` out for sealed `XmlAttr_FetchAndParseLong` (`FUN_00799460`).
- On fetch success (`al != 0`): writes **only the low byte** of the parsed long to `*outByte`.
- On failure: does **not** touch `*outByte` (same contract as callee for its out long).
- Return value void at this wrapper (success consumed only for the store gate); callee's `char` lives in AL then discarded after test.
- Call site evidence: `FUN_00611a00` → `FUN_00799820(node, L"slot", this+8, 0xa)` — objective requirement slot byte.

## Pseudocode (annotated)

```c
// cdecl
void FUN_00799820(void *node, const wchar_t *attrName, uint8_t *outByte, int base)
{
  char ok;
  long tmp;  // decompiler: local_4[4]

  ok = FUN_00799460(node, attrName, &tmp, base);  // XmlAttr_FetchAndParseLong
  if (ok != '\0') {
    *outByte = (uint8_t)(tmp & 0xFF);  // only low byte
  }
  // else: *outByte unchanged
}
```

## Open questions

- Whether any caller relies on multi-byte long values truncated to byte (vs only 0/1 flags and small slot indices).
- Full census of attr names / bases across all 16 xrefs.
- Runtime / differential.
