# Annotated low-level: `Client_InteractWorldClickHub` (`FUN_00925820`)

| Field | Value |
|---|---|
| Stable ID | `aa_00925820` |
| VA | `0x00925820` |
| Body | `00925820`–`00925d59` (1338 B) |
| Canonical (structural) | `Client_InteractWorldClickHub` |
| Ghidra name | `FUN_00925820` |
| System | `interaction-activation` / client input |
| Date | 2026-07-29 (W17-C dual A/B seal) |
| Tools | `decompile_function` + `read_memory` (no `disassemble_bytes`) |

## Machine-level notes

- **ABI:** thiscall — `ECX = client*`, stack `(param_2, param_3)`, **`ret 8`**.
- **param_3:** packed mouse screen coords — lo word signed X, hi word signed Y (`movsx` both).
- **Dispatch:** installed in client vtable DATA `@ 0x00a2f420` (sibling slot near `0x009251d0`). Virtual only — no direct CALL xrefs.
- **Not** `Client_InteractClickPickTarget` (`0x009247b0`) — that hub is the primary UseObject/25f pick path. This hub handles mouse UI vtbls, drop-destroy confirm, 20f gather → ItemPickup **or** objective-select + IfInteractable.
- Prefer sealed callee names when dual-reviewed; residual FUN_* left unresolved.

## Annotated stages

```
bool Client_InteractWorldClickHub(Client* this, /*param_2*/, packedMouse param_3):

  // Gate: only SKIP when mode==6 AND +0x493==0 AND worldFlag(+0xf5)==0
  if (!(mode!=6 || flag493 || worldFlagF5)):
      return 0

  (sx, sy) = unpack(param_3)
  this->vtbl[0x430/4](selected@+0x4B8, sx, sy)
  this->vtbl[0x428/4](selected@+0x4B8, sx, sy)

  if (DAT_00d1a860 && this[+0xC70]):
      this[+0xA35] = 0
      CVOGHBBase_Stop()            // 0x005081d0
      if (char && char.vehicle@+0x250):
          VehicleEntity_SetLongitudinalInput(0)

  if (selected && selected->vtbl[0x37C/4](param_2, param_3)):
      return 1

  if (!char || char.vtbl+0x194() || ui958.vtbl+0x3d8() || ui95c.vtbl+0x3d8()):
      return 0

  if (!DAT_00d1a860):
      return FUN_0091a350()        // non-world / alt input path

  if (char[+0xCD0] != 0):
      FUN_00914c20()
      DAT_00d1a859 = 0
      char[+0x304] = 0
      Client_ShowModalPrompt(..., "Dropping this item will permanently destroy it...", 0x4E4B, 1, 0)
      return 1

  // Ray + gather
  origin, dir = FUN_0075c340(cam DAT_00d1d86c/70)
  far = origin + dir * 100.0f      // DAT_00aaa7ac
  tfid = (-1, -1)
  if (world+0xE4A4):
      soft hit = FUN_0055e530(ray)
      if hit:
          pt = lerp(origin, far, t)
          FUN_0058cd60(&tfid, 0, char.pos, char.body, &pt, 20.0f, 1, 6, 0, 0, 1)

  if (tfid both -1):
      // list walk hits @ stride 0x30
      for each hit with flag@+0x20:
          obj = *FUN_0040afb0(...)
          if obj and (allowSelf or not self/vehicle):
              if FindActiveObjectiveIdForPick(obj): FUN_0093e120(obj)   // select
              if FindActiveObjectiveIdForInteract(obj):
                  FUN_0093e120(obj)
                  Client_SendUseObject_IfInteractable()
  else:
      Client_SendItemPickup()      // 0x2055; ESI=tfid pair at call site
      this[0x1d6] = 0              // clear select +0x758
      FUN_0093e120(0)

  return 0
```

## Offset map (client / char)

| Access | Byte offset | Notes |
|--------|------------:|-------|
| `param_1[0x331]` | +0xCC4 | Mode enum; gate vs **6** |
| `+0x493` | +0x493 | Byte flag in early gate |
| `param_1[0x12e]` | +0x4B8 | Selected / UI object slot for vtbl mouse |
| `param_1[0x31c]` | +0xC70 | Gate for drive-clear path |
| `+0xA35` | +0xA35 | Cleared before Stop / long input 0 |
| `param_1[0x1d6]` | +0x758 | Select slot cleared on ItemPickup path |
| char `+0xCD0` | | Non-zero → drop-destroy modal |
| char `+0x304` | | Cleared on destroy-modal path |
| char `+0x250` | | Vehicle entity for long-input clear |
| `DAT_00d1b644+0xF5` | | World/UI early-gate byte |
| `DAT_00d1b644+0xE4A4` | | Enables soft ray / gather |

## Confidence

| Claim | Level |
|---|---|
| Large multi-stage world-click hub | **High** |
| ABI thiscall `ret 8`, packed mouse | **Confirmed** |
| Early mode/flag gate | **Confirmed** (bytes) |
| Ray scale **100.0f**, gather **20.0f** | **Confirmed** (`read_memory` + imm) |
| ItemPickup path clears select + `FUN_0093e120(0)` | **Confirmed** (decompile + sealed callee) |
| Objective pick → select; interact → IfInteractable | **High** |
| Vtable DATA install `@ 0x00a2f420` | **Confirmed** |
| Product English for vtbl+0x428/0x430/0x37C | **Open** |
| Full hit-list taxonomy / soft-cast internals | **Partial** (callees not OWN) |

## Open questions

1. Product names for client mode `6`, flags `+0x493` / `+0xA35` / `DAT_00d1a860`.
2. English for mouse vtbl slots `+0x428` / `+0x430` and selected `+0x37C`.
3. Exact identity of `FUN_0093e120` (select/highlight — dual residual).
4. Relationship of this hub vs `Client_InteractClickPickTarget` in live UI event routing (which vtbl fires when).
5. Runtime capture of destroy-modal and ItemPickup branch.
