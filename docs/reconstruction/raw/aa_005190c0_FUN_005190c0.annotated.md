# Annotated low-level: FUN_005190c0 → Object_EnsureIntKeyMapAt0x15c_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005190c0` |
| VA | `0x005190c0` |
| System | skills-abilities / object int-key map (req aggregate) |
| Date | 2026-08-04 (WQ9R-E dual seal) |

## Machine-level notes

- **ABI:** `__fastcall` / `__thiscall` — ECX = host object; no stack formals; returns map* in EAX; bare `C3` RET after `ADD ESP,0x10` (SEH frame).
- **Body:** `0x005190c0`–`0x00519146` exclusive (**134 B**). Pad `CC`.
- **Gate:** `CMP dword [this+0x15C], 0` / `JNZ` skip-create (`read_memory`).
- **Alloc:** `PUSH 0x0C` → `operator_new` map header.
- **Sentinel factory:** `FUN_0040f400` allocates **0x18** node; ensure stamps is_nil at `+0x15`, self-links parent/left/right, size `map+8 = 0`.
- **Store:** `*(this+0x15C) = map`.
- **Return:** always `*(this+0x15C)` (existing or new; null if OOM).
- Prefer decompiler + `read_memory` (no `disassemble_bytes`).

## Pseudocode (annotated)

```c
// Object_EnsureIntKeyMapAt0x15c_Inferred
// Lazy-create empty MSVC-like int-key map header at host+0x15c.
MapHeader* __fastcall Object_EnsureIntKeyMapAt0x15c_Inferred(void *host /* ECX */)
{
  // SEH: LAB_009a363c
  if (*(void **)((char*)host + 0x15c) == NULL) {
    MapHeader *map = (MapHeader *)operator_new(0xC);
    if (map != NULL) {
      TreeNode *sentinel = (TreeNode *)FUN_0040f400(); // new(0x18); zero links
      map->myhead = sentinel;            // +4
      sentinel->is_nil = 1;              // +0x15
      sentinel->parent = sentinel;       // +4
      sentinel->left   = sentinel;       // +0
      sentinel->right  = sentinel;       // +8
      map->size = 0;                    // +8
      // map+0 not written
    }
    *(MapHeader **)((char*)host + 0x15c) = map; // may be null
  }
  return *(MapHeader **)((char*)host + 0x15c);
}
```

## Open questions

- Product type owning `+0x15c` (shared base vs skill-validator host).
- Value type of map entries (insert path uses key @ skill/item `+0x34`, count at node `+0x10`).
- Whether `map+0` is allocator / color / unused.

