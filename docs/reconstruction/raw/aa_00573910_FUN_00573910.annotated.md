# Annotated low-level: FUN_00573910

| Field | Value |
|---|---|
| Stable ID | `aa_00573910` |
| VA | `0x00573910`–`0x0057392d` exclusive (**29 B**) |
| System | inventory-transfer / InventoryGrid MSVC scalar-deleting dtor |
| Date | 2026-07-23 scaffold; WQ8R-E seal 2026-08-04 |
| Canonical | `InventoryGrid_ScalarDeletingDtor` (**Inferred**) |

## Machine-level notes

- Source: raw capture for `aa_00573910` + WQ8R-E full-body hex.
- **Not** the complete-object dtor — that is `FUN_005725a0`.
- **Not** a vector-deleting dtor — only flags bit0; no array count / `operator_delete[]`.
- Ghidra "does not return" on `operator_delete` is false; full 29 B reaches `MOV EAX,ESI; POP ESI; RET 4`.
- DATA xref only: dword @ `0x009d3390` (vtbl[0] of `PTR_FUN_009d3390`) = `0x00573910`.
- Class family sealed via ctor `InventoryGrid_ctor_Inferred` (`0x00572650`) and cargo destroy path `Vehicle_CreateCargoInventoryFromPageCount` (`vtbl[0](1)` on grid at vehicle+`0x2b0`).

## Pseudocode (annotated)

```c
// ECX=this; stack flags; ret 4; returns this*
void* __thiscall InventoryGrid_ScalarDeletingDtor(void* self, uint8_t flags)
{
  FUN_005725a0(self);                 // complete dtor body (not owned)
  if ((flags & 1) != 0) {
    operator_delete(self);            // free heap host when bit0 set
  }
  return self;
}
```

## Related

| Role | VA / symbol |
|---|---|
| Complete dtor | `FUN_005725a0` |
| Ctor installs same vtbl | `InventoryGrid_ctor_Inferred` `0x00572650` |
| Vtbl | `PTR_FUN_009d3390` @ `0x009d3390` |
| Parent destroy | `Vehicle_CreateCargoInventoryFromPageCount` `0x004f3a30` |

## Open questions

- Product/MSVC demangle for host class (InventoryGrid English is family-inferred; RTTI string not recovered here).
- Complete dtor body / member map not owned (`FUN_005725a0`).
- Runtime / bit-exact / differential — open.
