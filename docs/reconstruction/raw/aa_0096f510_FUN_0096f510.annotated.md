# Annotated low-level: FUN_0096f510 → RefCountedPtr3Flags_Dtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0096f510` |
| VA | `0x0096f510`–`0x0096f522` exclusive (**18 B**) |
| System | shared util — RefCountedPtr3Flags dtor/release |
| Date | 2026-08-04 (W37-H) |
| Canonical | `RefCountedPtr3Flags_Dtor_Inferred` |

## Machine-level notes

- Full body: `8B 09 85 C9 74 0B 83 41 04 FF 75 05 8B 01 FF 60 08 C3`
- `mov ecx,[ecx]` — ECX becomes payload*; original handle address lost (cannot clear).
- `add dword ptr [ecx+4], -1` — refcount at payload+4.
- `FF 60 08` = **JMP** dword ptr [EAX+8] (tail Release), not CALL. Decompiler "jumptable" warning is noise.
- Peer ctor immediately after pad: `0x0096f530` zeros pointer + 3 flag bytes.
- Distinct from `0096efd0` (ref@+8, iface@+4, clears handle, SEH).

## Pseudocode (annotated)

```c
// fastcall ECX=handle*; bare RET; void
void RefCountedPtr3Flags_Dtor_Inferred(handle* h)
{
  payload* p = h->ptr;          // [ecx]
  if (!p) return;
  if (--p->refcount /*+4*/ != 0) return;
  // tail: jmp [p->vtbl + 8] with ECX=p
  p->vtbl[2](p);
  // does not write h->ptr = null
}
```

## Open questions

- Product payload type / RTTI English.
- Whether any caller relies on *handle remaining non-null after final release.
- Runtime / bit-exact.
