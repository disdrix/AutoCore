# Annotated low-level: FUN_00512d10 → Object_ApplyFxWithTargetBind_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00512d10` |
| VA | `0x00512d10`–`0x00512ead` |
| System | `client-fx` / skills-abilities |
| Date | 2026-07-29 (W21-A dual seal; prior scaffold 2026-07-23) |

## Machine-level notes

- Prefer assembly when decompiler collapses thiscall for `FUN_004b68c0` / `FUN_004b7e50` (ECX = FX = `param_9`/`ESI`).
- `param_6[0x58..0x5b]` = entity TFID_16 at byte offset **`+0x160`** (0x58×4).
- Nested TFID path uses returned object + MI adjust `*(*(obj+4)+4) + 0x164 + obj` (same pattern as other CVOG object TFID packs).
- `_DAT_00aaa5dc` = **2^32** float — unsigned reinterpret of signed `param_7` after FILD when negative.
- Offset pack is float4: x=0, y=0, z=(float)param_7 (unsigned), w=0.
- Full-apply early exit shares a tail that loads `ECX = *[DAT_00b041fc]` before `FUN_00542790` (host is **not** the method's `this`).

## Signature (annotated)

```c
// __thiscall; ret 0x20 (8 stack dwords)
uint32_t __thiscall Object_ApplyFxWithTargetBind_Inferred(
    void *thisObj,          // ECX  — param_1; apply/attach host (vtbl+0xf8 / +0x19c)
    uint32_t keyOrA,        // [ebp+0x08] param_2 — passed through to FxCache helpers
    uint32_t keyOrB,        // [ebp+0x0c] param_3
    uint32_t arg4,          // [ebp+0x10] param_4
    uint32_t arg5,          // [ebp+0x14] param_5
    int *pTargetEntity,     // [ebp+0x18] param_6 — optional target for TFID bind (nullable)
    int offsetComponent,    // [ebp+0x1c] param_7 — int used as float Z (unsigned FILD path)
    char skipFullNfx,       // [ebp+0x20] param_8 — nonzero → skip live full ApplyWithNfx path
    uint32_t *pFxInOut);    // [ebp+0x24] param_9 — optional in/out FX instance (nullable)
```

## Control flow (annotated)

```
if (skipFullNfx == 0 && *(DAT_00d1f050 + 0x6c) != 0) {
  if (pFxInOut == NULL)
    return FxCache_ApplyWithNfxEnsure(host=DAT_00b041fc, keyB, keyA, arg4, arg5, target, thisObj, offsetComp);
  // else fall through with provided FX
} else {
  if (pFxInOut == NULL)
    pFxInOut = FxCache_EnsureMasterAndResolve(host, keyB, keyA, 1);
  if (pFxInOut == NULL) return 0;
}
*pFxInOut |= 2;
if (pTargetEntity != NULL) {
  nested = pTargetEntity->vtbl[+0x19c]();
  offset4 = {0, 0, (float)unsigned(offsetComponent), 0};
  if (nested == 0)
    tfid = entity[+0x160 .. +0x16c];
  else
    tfid = nested_obj_tfid_at_+0x164;
  NDSpecialFX_SetTargetTfidAndOffset(fx=pFxInOut, tfid, &offset4);
}
if (thisObj->vtbl[+0x19c]() == 0)
  thisObj->vtbl[+0xf8](pFxInOut, 1, 0);
else
  nested_adjust->vtbl[+0xf8](...);   // decompiler omits explicit args; machine pushes
FUN_004b7e50(fx=pFxInOut, 0,0,0,0,0,0);
return 1;
```

## Open questions

- Product method English name / exact key parameter meanings (param_2/3 order vs EnsureMaster).
- Semantic of flag bit **2** on FX header dword.
- Exact float4 axis meaning of `param_7` (treated as third component only).
- Full dual of `FUN_004b7e50` (large post/audio path) — not OWN.
