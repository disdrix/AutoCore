# Annotated low-level: FUN_00409780 → StdTree_BuyHeadNode_Isnil2D_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00409780` |
| VA | `0x00409780`–`0x00409809` inclusive (**138 B** / `0x8A`) |
| System | STL / map-set head node (isnil@+0x2d family, size 0x30) |
| Date | 2026-07-23 scaffold; **2026-08-05 WQ9J-G dual seal** |

## Machine-level notes

- **ABI:** no stack formals; bare **`RET` (`C3`)**; **EAX = node***. Decompiler `void` is wrong.
- **Alloc:** SEH (`LAB_009bc660`) + `operator_new(0x30)`.
- **Stores:** left/parent/right = 0; **color@+0x2c = 1** (`88 58 2c`); **isnil@+0x2d = 0** (`88 50 2d`).
- **MSVC field checks:** `if (node)`, `if (&node->parent)`, `if (&node->right)` — absolute-address odd if `new` returned null without throw; preserve in raw.
- **Sole caller** `FUN_00406190` completes head: isnil=1, circular L/P/R, host size=0.
- **Family:** isnil@**+0x2d** (Val32 payload region +0x0C..+0x2B). **Not** isnil29 BuyHeadNode `00408a30` (color@+0x28 / isnil@+0x29).
- **Not** freelist / CNDHash; **not** value buynode (no value copy args).

## Pseudocode (annotated)

```c
// EAX = NodeIsnil2D*; bare RET
NodeIsnil2D* StdTree_BuyHeadNode_Isnil2D_Inferred(void)
{
  // SEH frame (LAB_009bc660)
  NodeIsnil2D* node = (NodeIsnil2D*)operator_new(0x30);
  if (node)           node->left   = 0;   // +0x00
  if (&node->parent)  node->parent = 0;   // +0x04  (MSVC field-addr check)
  if (&node->right)   node->right  = 0;   // +0x08
  node->color = 1;    // +0x2c black
  node->isnil = 0;    // +0x2d (callers set 1 for head)
  return node;        // EAX
}
```

## Open / residual

- Product/PDB demangle for host map value_type (Val32 region).
- Full dual of tree ctor `FUN_00406190` (not OWN).
- OOM path if `operator_new` returns null without throw.
- Runtime / bit-exact / differential.
