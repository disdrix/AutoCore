# Annotated low-level: FUN_00404c90 (Object_GetWorldPositionPtr)

| Field | Value |
|---|---|
| Stable ID | `aa_00404c90` |
| VA | `0x00404c90` |
| Body | `00404c90`–`00404cad` |
| System | `shared-object-pose` |
| Date | 2026-07-23 (scaffold); dual refresh 2026-07-29 |

## Machine-level notes

- Source: raw capture for `aa_00404c90` (body unchanged; authoritative CF).
- Live `read_memory` 2026-07-29 seals ECX-`this`, dual return paths, leaf purity.
- Twin: `FUN_00404a20` @ `0x00404a20` (quat/orientation pointer; offsets `0x30` / `0x94`).
- Role: returns **pointer** to world position floats (not by-value vec3).
- Reviews: `reviews/A_aa_00404c90_Object_GetWorldPositionPtr.md`, `reviews/B_aa_00404c90_Object_GetWorldPositionPtr.md`.

## Pseudocode (annotated copy of raw)

```c
// __fastcall: ECX = pEntity
// Returns pointer to world-position float slot (xyz at +0/+4/+8).
int __fastcall FUN_00404c90(int param_1 /* pEntity */)
{
  // param_1+8 = physics object / wrapper; when set, use live RB pose
  if (*(int *)(param_1 + 8) != 0) {
    // *(*(phys)+0x3c) = rigid body*; +0xb0 = world position X (see fn_offsets_rigidbody)
    return *(int *)(*(int *)(param_1 + 8) + 0x3c) + 0xb0;
  }
  // Fallback: type/clonebase adjust *( *(this+4)+4 ) + entity + 0x84 embedded pose
  return *(int *)(*(int *)(param_1 + 4) + 4) + 0x84 + param_1;
}
```

## Assembly (from read_memory)

```
8b4108 85c0 7409 8b403c 05b0000000 c3   ; phys path → rb+0xb0
8b4104 8b5004 8d840a84000000 c3         ; fallback → adj+this+0x84
```

## Open questions

- Retail / PDB product name (Ghidra remains `FUN_00404c90`).
- Full C++ type for `this+4` adjustor chain.
- Runtime / bit-exact verification.
