# Annotated low-level: FUN_00437f70 → StdVector_Resize_Elem20_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00437f70` |
| VA | `0x00437f70`–`0x00438040` exclusive (**208 B**) |
| System | MSVC `vector<T>::resize(n, value)`, T size **0x14** |
| Date | 2026-08-04 (W38-F) |

## Machine-level notes

- Source: raw capture + W38-F re-verify (`decompile` + `read_memory`). **No** `disassemble_bytes`.
- **Bytes win** on ABI: **ESI=vec***, **ECX=new_size**, stack **0x14** by-value fill, **`RET 0x14`**.
- Decompiler `unaff_ESI` / `__fastcall(param_1)` understates register-vec + stack-value formals.
- Size math: magic `0x66666667` + `sar 3` → ÷ **0x14**; grow `FUN_004388b0`; shrink `FUN_00438850`.
- Epilogue calls `FUN_00437f20` (unowned; decompile shows vector-of-0x38 destroy — residual vs stack-T role).
- Reject scaffold `Named_CalleeOf_Named_gfxBody_*`.

## Pseudocode (bytes-informed)

```c
// ESI = StdVectorElem20* vec
// ECX = uint32_t new_size
// stack = Elem20 value (0x14); RET 0x14
void StdVector_Resize_Elem20_Inferred(StdVectorElem20* vec /*ESI*/,
                                      uint32_t new_size /*ECX*/,
                                      Elem20 value /*stack*/)
{
  Elem20* begin = vec->begin; // +4
  uint32_t size = begin ? (uint32_t)((char*)vec->end - (char*)begin) / 0x14 : 0;

  if (size < new_size) {
    FUN_004388b0(vec, vec->end, new_size - size, &value); // InsertN grow
  } else if (begin && new_size < size) {
    Elem20* new_end = (Elem20*)((char*)begin + new_size * 0x14);
    Elem20* it_tmp;
    FUN_00438850(vec, &it_tmp, new_end, vec->end); // erase [new_end, end)
  }
  // epilogue helper FUN_00437f20 (residual)
  // RET 0x14
}
```

## Open questions

- Product/PDB English for T (0x14 element).
- Unowned InsertN `FUN_004388b0` / erase `FUN_00438850` dual seal.
- Exact epilogue role of `FUN_00437f20`.
- Runtime / bit-exact / differential.
