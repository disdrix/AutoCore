# Annotated low-level: Map_FreeSubtree_Isnil15 (FUN_0051be50)

| Field | Value |
|---|---|
| Stable ID | `aa_0051be50` |
| VA | `0x0051be50`–`0x0051be85` exclusive (**53 B**) |
| Canonical | `Map_FreeSubtree_Isnil15` |
| System | STL / map-set (WQ-009 skill residual; Map_EraseRange_Isnil15 free helper) |
| Date | 2026-08-04 WQ9F-F OWN dual |

## Machine-level notes

- Source: raw capture + live re-verify for `aa_0051be50`.
- **Bytes authority:** decompiler omits left-child walk; reconstruct from `read_memory`.
- isnil@**+0x15**; left@**+0**; right@**+8**.
- ABI: stack node*; **ret 4**; ECX preserved/threaded unused (`mov ebx,ecx`).
- Sole external caller: `Map_EraseRange_Isnil15` full-clear path (`head->parent` = root).
- Twin CF (different VA): `StdMap_FreeSubtree_Isnil15_Inferred` @ `0x004ba770`.

## Pseudocode (byte-corrected)

```c
// Ghidra plate (misleading left-walk omission):
//   if (!isnil) { free(right); operator_delete(self); }

// Byte-correct MSVC free-subtree (isnil@+0x15):
void Map_FreeSubtree_Isnil15(void *node)
{
  if (*(char *)((int)node + 0x15) != 0)
    return;

  for (;;) {
    Map_FreeSubtree_Isnil15(*(void **)((char *)node + 8)); // right
    {
      void *left = *(void **)node;                   // left @ +0
      operator_delete(node);
      node = left;
    }
    if (*(char *)((int)node + 0x15) != 0)
      break;
  }
}
```

## Call graph

| Direction | Target | Notes |
|---|---|---|
| Caller | `Map_EraseRange_Isnil15` `0x0051d7c0` | full clear only |
| Caller | self | right-child recurse |
| Callee | `operator_delete` | after left snapshot |

## Open questions

- Exact map instantiation / value dtor (delete-only; no inlined value dtor observed).
- Why separate clone of `StdMap_FreeSubtree_Isnil15_Inferred` (`0x004ba770`).
- Runtime / bit-exact open.
