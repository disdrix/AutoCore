# Annotated low-level: FUN_0051f940 → LookupClassDisplayName_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051f940` |
| VA | `0x0051f940`–`0x0051f9f9` |
| Canonical | `LookupClassDisplayName_Inferred` |
| System | missions-progression / character display |
| Date | 2026-08-04 (WQ9G-E dual) |

---

## Purpose

Pure **classId × raceId → product English C-string** leaf used by `Character_GetClassDisplayName_Inferred` (`[$class]`) and UI formatters.

---

## Annotated CF

```c
// cdecl; stack classId, raceId; plain ret; returns const char* (static)
const char* LookupClassDisplayName_Inferred(unsigned classId, int raceId)
{
  switch (classId) {
  case 0: // Commando family
    if (raceId == 1) return "Champion";
    if (raceId == 2) return "Terminator";
    return "Commando";          // race 0 or other
  case 1: // Engineer family
    if (raceId == 1) return "Shaman";
    if (raceId == 2) return "Constructor";
    return "Engineer";
  case 2: // Officer family
    if (raceId == 0) return "Lieutenant";
    if (raceId == 1) return "Archon";
    if (raceId == 2) return "MasterMind";
    return "Officer";
  case 3: // Ranger family
    if (raceId == 0) return "Bounty Hunter";
    if (raceId == 1) return "Avenger";
    if (raceId == 2) return "Agent";
    return "Ranger";
  default:
    return "Unknown";
  }
}
```

### Race columns (product)

| classId | race0 | race1 | race2 | else |
|---|---|---|---|---|
| 0 | Commando | Champion | Terminator | Commando |
| 1 | Engineer | Shaman | Constructor | Engineer |
| 2 | Lieutenant | Archon | MasterMind | Officer |
| 3 | Bounty Hunter | Avenger | Agent | Ranger |
| other | Unknown | | | |

Note: cases 0/1 fold non-{1,2} race to base name; cases 2/3 treat race0 explicitly and fold other non-{1,2} to Officer/Ranger.

---

## Machine notes

| Claim | Evidence |
|---|---|
| Body 185 B / plain `ret` | `get_function_by_address` + `C3` after each `mov eax,imm` |
| cdecl 2-arg | callers push 2 dwords; wrapper `add esp,8` (aa_00521900 dual) |
| Jump table switch on classId | `ff 24 85 fc f9 51 00` + table @ `0x0051f9e8` |
| No callees | analyze_function_complete callees=[] |
| Strings product English | `read_memory` at imm VAs |
| Not ring-buffered here | returns static literals; ring is wrapper `007a6de0` |

---

## Callers (summary)

| Caller | Role |
|---|---|
| `Character_GetClassDisplayName_Inferred` `0x00521900` | `[$class]` wrapper (dualed) |
| `FUN_00521970` | Level+class sprintf UI line |
| `FUN_00845360` | item requirements UI (class token) |
| `FUN_00846820` / `FUN_008967b0` / `FUN_008a3510` | additional UI formatters |

---

## Open / residual

- Product method/PDB English (role **Inferred**).
- Authoritative race id dictionary (0/1/2 as Human/Mutant/Biomek is peer race dual; not re-proven here).
- Runtime / bit-exact / differential.
