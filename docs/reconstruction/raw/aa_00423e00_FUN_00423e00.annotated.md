# Annotated low-level: AssManager_RemoveResolver_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00423e00` |
| VA | `0x00423e00`–`0x00423e43` (**68 B**) |
| Ghidra | `FUN_00423e00` |
| Canonical | `AssManager_RemoveResolver_Inferred` |
| System | `assets` / `assManager` |
| Date | 2026-07-23 scaffold; **2026-07-29 W29-C dual seal** |

## Machine-level notes

- **ABI (bytes, not decompiler surface):**  
  - **ECX** = assManager instance (`this`).  
  - **EAX** = resolver key (dword) to remove.  
  - Stack: saves ECX/ESI; stores key in stack local; plain **`RET`** (caller cleans nothing for this frame).  
  - Decompiler shows `void(void)` — formals live in registers only.
- **Container member:** `this+0x58` → GuardedVector family used by find (`FUN_0043e5b0`) + erase (`FUN_0043c550` / W28-C sealed).
- **Find path:** `MOV EAX, ESI` then `CALL FUN_0043e5b0(&key)` — membership only (bool AL); no mutate.
- **Miss path:** `vog_LogMessage(assManager.cpp, 0x27b, 3, "Removing resolver not found")` then return (soft fail; **no throw**).
- **Hit path:** `FUN_0043c550(&key)` with **ESI** still = container (EraseFirstEqual).
- **Sole direct caller:** `FUN_007b65d0` loads key from `[EBX+8]` and manager from global `DAT_00d1f058`. Sibling inline of same find/log/erase pattern appears later in that parent for `[EBX+0xC]` (not this body).

## Pseudocode (annotated)

```c
// ECX=assManager*, EAX=resolver_key
void AssManager_RemoveResolver_Inferred(void /*ECX this, EAX key*/)
{
  int key = /*in_EAX*/;
  GuardedVectorHeader* cont = (GuardedVectorHeader*)((char*)this + 0x58);

  // FUN_0043e5b0: EAX=cont, arg=&key → AL found (CS-gated linear scan)
  if (!GuardedVector_ContainsFirstEqual(cont, &key)) {
    vog_LogMessage(
      "C:\\vog\\1_code\\palantir\\palantir\\assets\\assManager.cpp",
      0x27b, /* line 635 */
      3,
      "Removing resolver not found");
    return;
  }
  // FUN_0043c550: ESI=cont, arg=&key, RET 4
  GuardedVector_EraseFirstEqual(cont, &key);
}
```

## Open questions

- Product/PDB method English on assManager.
- Exact type of resolver key (object ptr vs id) — treated as dword equality by callees.
- Runtime / bit-exact / differential.
