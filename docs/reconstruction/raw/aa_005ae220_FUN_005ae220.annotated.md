# Annotated low-level: StdTree_Buynode_Val12 (FUN_005ae220)

| Field | Value |
|---|---|
| Stable ID | `aa_005ae220` |
| VA | `0x005ae220` |
| Structural name | `StdTree_Buynode_Val12` |
| System | std / red-black tree node alloc |
| Date | 2026-07-29 (W24-I dual seal) |
| Tools | `decompile_function` + `read_memory` (+ meta/xrefs); **no** `disassemble_bytes` |

---

## Machine-level notes

- Body **72** B; final **`ret 0x14`**.
- Decompiler shows `void` — bytes preserve **EAX = node*** (null on OOM).
- Color at **+0x18**, isnil forced **0** at **+0x19** (not-nil leaf).
- Payload is **exactly 3 dwords** copied from `*value` — matches category-cooldown `{key,start,duration}` and other 12-byte map values.
- Call pattern from `FUN_0051c1c0`: `Buynode(head, where, head, value*, color=0)` then link/rebalance.

## Pseudocode (annotated)

```c
// Returns node* in EAX (decompiler void is wrong)
void *StdTree_Buynode_Val12(
    void *left,           // [esp+4]
    void *parent,         // [esp+8]
    void *right,          // [esp+0xc]
    const uint32_t *val,  // [esp+0x10] → 3 dwords
    uint8_t color)        // [esp+0x14]
{
    uint32_t *node = operator_new(0x1c);
    if (node != nullptr) {
        node[0] = (uint32_t)left;
        node[1] = (uint32_t)parent;
        node[2] = (uint32_t)right;
        node[3] = val[0];
        node[4] = val[1];
        node[5] = val[2];
        *(uint8_t *)(node + 6) = color;          // +0x18
        *(uint8_t *)((uint8_t *)node + 0x19) = 0; // isnil
    }
    return node; // EAX
}
// ret 0x14
```

## Confidence

| Claim | Level |
|---|---|
| Size 0x1c + ret 0x14 + field layout | **High** (bytes) |
| Role = std tree buynode for Val size 12 | **High** (caller `map/set too long` + layout) |
| Product demangled template | Open |
