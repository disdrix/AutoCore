# Annotated low-level: CNDHash_AllocBucketTable_00a2c2e4

| Field | Value |
|---|---|
| Stable ID | `aa_00406f20` |
| VA | `0x00406f20`–`0x00406faf` (**144 B**) |
| Canonical | `CNDHash_AllocBucketTable_00a2c2e4` |
| Prior | `FUN_00406f20` / scaffold `Named_VOG_DEBUG_STOP_00406f20` (**reject** product id) |
| System | container / CNDHash (host vtbl `00a2c2b0`; sentinel stamp `00a2c2e4`) |
| Date | 2026-08-04 (WQ9G-J dual seal) |

## Machine-level notes

- **ABI:** custom **ESI = hash\*** (not thiscall). Entry `push ecx; mov al,[esi+0x1c]`. Success `pop edi; pop ecx; ret`.
- **Log2 gate:** byte `@+0x1c` ∈ **[1,16]** else log+throw.
- **N:** provisional count `@+0x08` (callers seed `1<<log2` before call).
- **Table:** `operator_new[](N*4)` → `@+0x10`.
- **Slab:** `malloc(N*0xc)`; each bucket pointer → successive 0xc sentinel; stamp **`PTR_LAB_00a2c2e4`**, next=0, val=0.
- **Post:** `*(hash+8) = N-1` (**COUNT → MASK**).
- **Fail:** `FUN_007a4480(0,"VOG_DEBUG_STOP")` + `_CxxThrowException` **`0x80004003`**.
- Prefer bytes when decompiler shows `unaff_ESI` — register is intentional.

## Pseudocode (annotated)

```c
// custom: ESI = CNDHash*  (unaff_ESI in decompiler)
// bare ret on success; noreturn on bad log2
void CNDHash_AllocBucketTable_00a2c2e4(void /* ESI */)
{
  char log2 = *(char *)(esi + 0x1c);
  if (!(log2 > 0 && log2 < 0x11)) { // [1,16]
    FUN_007a4480(0, "VOG_DEBUG_STOP");
    local_4 = 0x80004003;
    _CxxThrowException(&local_4, &DAT_00acc430); // noreturn
  }

  int N = *(int *)(esi + 8);
  void *table = operator_new__(N * 4);     // new[]
  *(void **)(esi + 0x10) = table;
  void *slab = malloc(N * 0xc);
  uint i = 0;
  if (N != 0) {
    do {
      ((void **)table)[i] = slab;
      uint32_t *sent = ((void **)table)[i];
      i = i + 1;
      *sent = (uint32_t)&PTR_LAB_00a2c2e4; // stamp
      sent[1] = 0;
      sent[2] = 0;
      slab = (char *)slab + 0xc;
    } while (i < (uint)N);
  }
  *(int *)(esi + 8) = N - 1; // COUNT → MASK
  return;
}
```

## Callers

| Caller | Site | Setup |
|--------|------|--------|
| Hash ctor `FUN_004063a0` | `0x004063fc` | ESI=hash; fields + log2 + `N=1<<log2` already set |
| Recreate `FUN_004195d0` | `0x00419612` | after destroy; write log2/`N`/`+0x14`/`+0x18`; ESI preserved |

## Open residuals

- Product/PDB class English for host vtbl `00a2c2b0` / stamp `00a2c2e4`.
- OOM null-check absence (preserve; intentional vs latent open).
- Full dual of freelist peer `FUN_004085e0` (not OWN).
- Runtime / bit-exact / differential.
