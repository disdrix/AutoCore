# Annotated low-level: CVOGHBMoveVehicle_ScalarDeletingDtor (FUN_00636d10)

| Field | Value |
|---|---|
| Stable ID | `aa_00636d10` |
| VA | `0x00636d10` |
| Canonical name | `CVOGHBMoveVehicle_ScalarDeletingDtor` |
| Ghidra name | `FUN_00636d10` |
| System | `input-drive-control` / MSVC scalar-deleting dtor |
| Date | 2026-08-05 (R11-024 refresh) |
| Body | `0x00636d10`–`0x00636d2d` inclusive (**30 B** / `0x1E`) |

## Machine-level notes

- Source: raw capture + R11-024 live re-verify (`disassemble_function` + `read_memory`). Prefer **assembly** when decompiler marks `operator_delete` as noreturn (epilogue always runs).
- **this** = `CVOGHBMoveVehicle*` in **ECX**; stack flags byte; **`RET 4`**; returns **this\*** in **EAX**.
- **vtbl[0]** of `PTR_FUN_009e3b70` only — DATA xref `0x009e3b70`. Subclass ToMouse uses a different scalar shell (`0x005996a0`).
- Always runs complete/body `FUN_00636b90` first (rewrites `*this = &PTR_FUN_009e3b70`, chains `FUN_00508390` / CVOGHBBase complete dtor). Does **not** free when bit0 clear (stack-lifetime / placement delete path).
- Classic MSVC **scalar** form: only **bit0** tested; no element-count / bit2 vector path.
- Product class sealed by RTTI `.?AVCVOGHBMoveVehicle@@` @ `0x00af33c0` (MEGA-017 / this pass).

## Pseudocode (assembly-complete)

```c
// CVOGHBMoveVehicle::`scalar deleting destructor'  — vtbl[0]
// thiscall: ECX = this; stack flags; RET 4; EAX = this

void *__thiscall CVOGHBMoveVehicle_ScalarDeletingDtor(
    CVOGHBMoveVehicle *this /* ECX */,
    uint8_t flags)
{
  // complete/body: *this = &PTR_FUN_009e3b70; CVOGHBBase_dtor chain
  FUN_00636b90(this);

  if ((flags & 1u) != 0u) {
    operator_delete(this);   // cdecl; machine has ADD ESP,4
  }

  return this;
}
```

## Instruction map (`read_memory` @ `0x00636d10`, 30 B)

```text
56                      push  esi
8B F1                   mov   esi, ecx              ; this
E8 78 FE FF FF          call  FUN_00636b90          ; 0x00636b90 complete/body
F6 44 24 08 01          test  byte ptr [esp+8], 1   ; flags bit0
74 09                   jz    skip                  ; → 00636d28
56                      push  esi
E8 FD 2A E5 FF          call  operator_delete       ; 0x00489822
83 C4 04                add   esp, 4
skip:
8B C6                   mov   eax, esi              ; return this*
5E                      pop   esi
C2 04 00                ret   4
```

Hex: `568bf1e878fefffff644240801740956e8fd2ae5ff83c4048bc65ec20400`

## Related (context only; not OWN)

| VA | Role |
|---|---|
| `0x00636b90` | complete/body dtor (restore MoveVehicle vtbl + base chain) |
| `0x00508390` | `CVOGHBBase` complete dtor (chained by body) |
| `0x00508630` | `CVOGHBBase_ScalarDeletingDtor` (base class shell — different body) |
| `0x00636ba0` | `CVOGHBMoveVehicle_OnEnd` (vtbl+0x10, MEGA-017 dualed) |
| `0x00636cc0` | fire/heartbeat (vtbl+0x0c, sibling residual) |
| `0x00636bd0` | ctor — installs same vtbl, binds entity `@+0x24` |
| `0x009e3b70` | `PTR_FUN_009e3b70` — this unit is dword0 |
| `0x009d5590` | ToMouse vtbl — **different** dword0 (`0x005996a0`) |
| `0x00489822` | `operator_delete` |

## Open questions

- Runtime / live CE confirmation of heap free path (`flags&1`).
- Full dual of complete body `FUN_00636b90` (not OWN this VA).
- PDB method symbol beyond RTTI class + MSVC scalar-deleting pattern.
