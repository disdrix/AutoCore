# Annotated low-level: FUN_0056c0a0 → Weapon_SelectMultiTargets_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0056c0a0` |
| VA | `0x0056c0a0` |
| Body | `0x0056c0a0`–`0x0056c856` exclusive (**1974** B / `0x7B6`) |
| System | combat / vehicle weapon targeting |
| Date | 2026-07-29 (W20-H dual seal) |
| Canonical | `Weapon_SelectMultiTargets_Inferred` |

## Machine-level notes

- **ABI:** `__thiscall` — `ECX = weapon*`; stack `param_2 = locked object list*`, `param_3 = preferred target object*`; **`ret 8`**.
- **Return:** `uint` count of selected targets on success; **`0xffffffff` (−1)** on early fail.
- **Entry:** SEH frame (`LAB_009a4d8e`); stack frame aligned; `mov ebx, ecx` (this in EBX).
- **Owner object:** `*( *(this+4)+4 ) + 0xb0 + this` — adjustor-this owner pointer (same pattern as sibling weapon duals).
- **Fail gates (−1):**
  1. owner null **or** owner `vtbl+0x198` ≠ 0 (dead/invalid status) **or** `param_2 == 0` → optional `FUN_0056bf70` if list `@this+0x13c` present.
  2. `*(this+0x13c) == 0` (no result list host).
  3. owner physics via `vtbl+0x1c8` has `+8 == 0`.
- **Max targets:** `FUN_0056ac50(this)` → clamp **>100** with log severity 0 + string `"WARNING Weaopn <%s> has invalid # of maxtargets(%d) set!"` @ `0x009d2fcc` (product typo **Weaopn**); force max=100.
- **Orientation:**
  - If owner def subtype `*(owner[0x2a]+0x38) == 0x0e`: hardpoint path (`vtbl+0x1d4`, `FUN_004f8c50`, quat transform `004e8bf0`, normalize direction).
  - Else: `FUN_00404a20` + `FUN_004e8a40` default basis.
  - Capture owner world position via `FUN_00404c90` into local xyz(+w).
- **Preferred target (param_3):** if non-null, alive (`vtbl+0x198==0`), hostile/valid via owner `vtbl+0x298`, physics ok → `CVOGPhysicsUtils_FindDistanceToTarget` (`FUN_004e9aa0`); if distance ≠ 0: alloc 0x10 slot via `FUN_005770d0(param_3)`, count=1, bestDist=distance.
- **World list fill** (while count ≠ max): lock-traverse `FUN_004294f0` + `FUN_004022a0` on list state; for each candidate:
  - skip self owner and preferred;
  - owner `vtbl+0x298` ok; candidate alive; flags `obj[0x5f]` bit5 set, bit3 clear; `FUN_005134e0` false;
  - optional skip via `vtbl+0x1d8` + `FUN_0040b1b0` (def-sub predicate);
  - physics + `FindDistanceToTarget`; if dist≠0: append if farther-or-equal to best and room, else insert nearer (shift slots, destroy displaced).
- **Unlock** param_2 CS if held (`+0x28` flag / `LeaveCriticalSection(+4)`).
- **Merge/finalize:** walk secondary locked list; patch matching slot fields; free unmatched nodes; clear host list CS; `FUN_0056f0b0`; `FUN_0056eca0` each selected slot into host list `@this+0x13c`; destroy leftover wrappers; **return count**.

## Key locals (decomp names)

| Local | Role |
|---|---|
| `uStack_210` | maxTargets (clamped ≤100) |
| `uStack_21c` | selected count |
| `puStack_214` | best/reference distance (init float **2560.0f** @ `0x009d2fc8`) |
| `iStack_20c` | insert index sentinel (−1 init) |
| `aiStack_1b4[102]` | slot pointer table (index 0 overlays pos.w / pad; slots at +1…) |
| `fStack_200..1f4` | aim direction (or quat workspace) |
| `uStack_1c0..aiStack_1b4[0]` | owner world position xyzw |
| `iStack_1c8` | owner physics host |

## Slot object (`FUN_005770d0`)

```
vtbl = PTR_LAB_009d3c10
+4 = target object*
+8 = u8 flag (0)
+0xc = dword (0 / later patched from list node)
```

## Open questions

1. Product/RTTI class + exact virtual slot English name.
2. Exact semantic of owner flag bits `obj[0x5f]` bit5/bit3 and `FUN_005134e0`.
3. Full sort stability vs only “closer than best” insert rule residual on stack overlay.
4. Runtime / bit-exact / differential — open.
