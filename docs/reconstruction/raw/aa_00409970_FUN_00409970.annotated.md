# Annotated low-level: FUN_00409970 → StdTree_FreeSubtree_Isnil2d_DestroyStr0C_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00409970` |
| VA | `0x00409970`–`0x004099aa` inclusive (**59 B** / `0x3B`) |
| System | skills-abilities (STL tree free-subtree; erase-range neighborhood) |
| Date | 2026-08-05 (WQ9J-I OWN-ONLY re-annotate) |
| Named | `StdTree_FreeSubtree_Isnil2d_DestroyStr0C_Inferred` |

## Machine-level notes

- **Bytes authority** over decompiler (left-walk omitted; false noreturn on `operator_delete`).
- **ABI:** ECX=tree_base + stack node*; **`RET 4`** — twin of `00409920`.
- **isnil @ +0x2d** (not 0x131); left@+0; right@+8.
- **Value destroy:** same shared `FUN_00409b80` as isnil131 free — string @ +0x0c.
- Sole external caller: erase-range full-clear `FUN_00407f90` @ `0x00407fb0` (push `head->parent`).
- **Do not merge** with isnil131 free `00409920` or no-destroy isnil29 free `00406c00` / `0051bbc0`.

## Pseudocode (byte-corrected)

```c
// ECX = tree_base; stack = node*; RET 4; void
void StdTree_FreeSubtree_Isnil2d_DestroyStr0C_Inferred(void *tree_base, void *node)
{
  if (*(uint8_t *)((char *)node + 0x2d) != 0)
    return;
  for (;;) {
    StdTree_FreeSubtree_Isnil2d_DestroyStr0C_Inferred(
        tree_base, *(void **)((char *)node + 8));
    void *left = *(void **)node;
    FUN_00409b80(node);          // ~basic_string @ +0x0c
    operator_delete(node);
    node = left;
    if (*(uint8_t *)((char *)node + 0x2d) != 0)
      break;
  }
}
```

## Open questions

- Product English for compact node (string + fields through isnil@+0x2d; color likely +0x2c).
- Dual of `FUN_00409b80` / erase parent `FUN_00407f90` (not OWN).
- Runtime / bit-exact.
