# Annotated low-level: FUN_00765740 → GfxBody_UnserializeGBOD_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00765740` |
| VA | `0x00765740`–`0x00766045` (**2310 B** / `0x906`) |
| System | palantir graphics / gfxBody sto unserialize |
| Date | 2026-08-04 (W37-S) |

## Machine-level notes

- **ABI:** stdcall `RET 8` — stack: `gfxBody* body`, `stoChunkReader* reader`; return `uint32` OR-status (or `0xFFFFFFFF` on bad tag/version).
- **SEH:** `LAB_009b31c4`; frame `SUB ESP` region ends with `ADD ESP,0x5C`.
- **Tag gate:** enter chunk → require **GBOD** (`0x47424F44`, file bytes `DOBG`); else plate `"Invalid TAG unserializeing gfxBody, probably exported wrong"` @ `0x00a9dc40` + `gfxBody.cpp` log.
- **Version gate:** `local_2c` after enter:
  - **1** — dedicated v1 path (helpers only, no early factory abort on status).
  - **2** — shared v2/v3 body (no v3 skip prologue).
  - **3** — skip/read two counts + N string skips, then shared body.
  - else plate `"Invalid chunk version (%i) unserializeing gfxBody"` @ `0x00a9dc0c` → −1.
- **Reader mode:** `reader[0x1011]` (`+0x4044`) selects local-buffer helpers (`FUN_00437050` / `FUN_00436f10` / `FUN_00437000` / inline dword via `FUN_00435df0`) vs alternate helpers (`FUN_00768760` / `FUN_0076a200` / `FUN_007689e0` / `FUN_00769660`).
- **Status:** bitwise OR of all reader/helper returns into `local_5c`.
- **Factory (MapA):** `FUN_00437b00` on vectors `body+0xC0..+0xC4` and `body+0xD0..+0xD4` (W36-A sealed). V2/v3 aborts to cleanup if status signed-negative after factory.
- **MapB (v2/v3 only):** optional bool → `FUN_00437c90` → store `body+0x94`, vcall `+0x10` / `+4` (W36-C).
- **Tail:** copy **12 dwords** `body+4` → `body+0x34`; leave chunk (`FUN_00769e40` if scope flag).
- Prefer bytes over decompiler for call ABIs of nested helpers.

## Sealed product evidence

| Source | Content |
|---|---|
| Log path | `C:\vog\1_code\palantir\palantir\graphics\gfxBody.cpp` |
| Bad tag | Invalid TAG unserializeing gfxBody… |
| Bad version | Invalid chunk version (%i) unserializeing gfxBody |
| Peer serialize | GBOD root / GPCE children (other duals) |

## Open gaps

- Full field English for every body offset (`+0x80`, `+0x98`, `+0xC0`, `+0xD0`, `+0x94`, …).
- Product method name beyond path-derived gfxBody unserialize.
- Nested helper contracts not owned here.
- Runtime / bit-exact / differential.
