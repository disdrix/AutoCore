# Raw capture: FUN_004447e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004447e0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004447e0` |
| **Canonical name** | `FUN_004447e0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_004447e0(void *param_1,byte param_2)

{
  FUN_00754320();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```



# W35-J re-verify append (2026-07-29)
# VA 0x004447e0 — append only

## W35-J dual A/B re-verify (2026-07-29)

| Field | Value |
|---|---|
| **Wave** | OWN-ONLY W35-J |
| **Tools** | `decompile_function` + `analyze_function_complete` + `read_memory` + vtbl `0x00a9f76c` + peer complete dtor W34-K. **No** `disassemble_bytes` |
| **Body** | `0x004447e0`–`0x004447FE` exclusive (**30 B** / `0x1E`); pad `CC CC` then next @ `0x00444800` |
| **Ghidra body** | `004447e0`–`004447fd` inclusive |
| **ABI** | **ECX=`Palantir* this`**; stack **`flags:byte`**; **`RET 4`** (`C2 04 00`); returns `this` in EAX |
| **Classification** | worker |
| **Name** | `Palantir_ScalarDeletingDtor` (**Inferred** + vtbl[0] of Palantir dtor-phase vtbl) |

### Full body hex (30 B)

```
568bf1e838fb3000f644240801740956e82d50040083c4048bc65ec20400
```

### Bytes-authority control flow

```
PUSH ESI
ESI = ECX                         // this
CALL FUN_00754320                 // Palantir_CompleteDtor; ECX still this
TEST BYTE PTR [ESP+8], 1          // flags (after PUSH ESI: [ESP+8]=arg1)
JZ skip_delete
  PUSH ESI
  CALL operator_delete
  ADD ESP, 4
skip_delete:
EAX = ESI
POP ESI
RET 4
```

### Vtbl seal

`PTR_FUN_00a9f76c` dword[0] = `0x004447e0` (scalar deleting dtor); dword[1] = `0x00754480` (ResetDevice / Palantir.cpp product string — sealed W34-K).

### Callers

| Site | Notes |
|---|---|
| Data xref `0x00a9f76c` | vtbl[0] only (no direct code CALL xrefs) |

### Callees

`FUN_00754320` (`Palantir_CompleteDtor`, W34-K dualed), `operator_delete`.

### Decompile ≢ bytes

Decompiler omits explicit `MOV ESI,ECX` / ESI save; `FUN_00754320()` looks argless (actually thiscall via ECX). `operator_delete` false noreturn. **Bytes win.** Contract matches classic MSVC scalar deleting destructor: complete dtor then optional free when `flags&1`.

