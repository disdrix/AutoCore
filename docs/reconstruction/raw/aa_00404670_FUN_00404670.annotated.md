# Annotated low-level: FUN_00404670 / UiToast_ConstructN_0x98

| Field | Value |
|---|---|
| Stable ID | `aa_00404670` |
| VA | `0x00404670` |
| System | `missions-progression` / UI toast vector helpers |
| Date | `2026-07-29` (dual seal W16-S) |
| Role | Count×POD construct of **0x98**-byte toast elements |

---

## Machine-level notes

- **Role:** MSVC-style **construct-N / uninitialized_fill_n body** for UI toast POD (`sizeof == 0x98`).
- Loop `count` times: `UiToast_PodCopyElement_0x98(dst, src)` then `dst += 0x98`.
- Source pointer does **not** advance — same template stamped into every slot (construct-from-value).
- **SEH** frame present (`ExceptionList` / `LAB_009bc750`); no try-body side effects beyond the loop.
- **ABI:** free helper, **cdecl** (`ret` = `c3`); used formals `(dst, count, src)`.
- Parent push fast-path (`Client_UiToastQueue_Push` @ `0x0040c5c0`) calls with **`count = 1`**.
- Sibling fill-n wrapper `FUN_00404130` calls this then returns `dst + count * 0x98`.
- Callee dual: `UiToast_PodCopyElement_0x98` / `FUN_00404710` (sealed).

---

## Pseudocode (annotated)

```c
// cdecl: (dst, count, src) — phantom trailing stack slots possible from thiscall parents
// Element stride 0x98; inner copy = 0x26 dwords via FUN_00404710
void UiToast_ConstructN_0x98(void *dst, int count, const void *src)
{
  // SEH: ExceptionList frame (LAB_009bc750); local_8 = 0
  for (; count != 0; count = count - 1) {
    FUN_00404710(dst, src);   // POD copy 0x98 bytes if dst != NULL
    dst = (char *)dst + 0x98;
  }
  // restore ExceptionList
}
```

### Byte anchors (`read_memory`)

| Site | Bytes / meaning |
|---|---|
| Entry | `55 8b ec 6a ff 68 50 c7 9b 00` — SEH prolog + handler `0x009bc750` |
| Load args | `8b 5d 14` / `8b 7d 0c` / `8b 75 08` — `ebx=[ebp+14]` (unused formal), `edi=count`, `esi=dst` |
| Loop gate | `85 ff 76 1d` — `test edi,edi; jbe` exit (≡ zero-count skip) |
| Call | `push [ebp+10]; push esi; mov ecx,ebx; call 00404710` |
| Stride | `81 c6 98 00 00 00` — `add esi, 0x98` |
| Epilogue | `… 5f 5e 5b 8b e5 5d c3` — restore regs; **cdecl ret** |

---

## Open questions

- Product / PDB name for toast element type and this helper.
- Whether any non-toast client reuses this exact VA (static callers are toast chain only: `00404130`, `0040c5c0`).
