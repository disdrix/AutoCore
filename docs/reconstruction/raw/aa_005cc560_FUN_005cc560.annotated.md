# Annotated low-level: CVOGHBAI_DecEntityA4SecondaryCounter_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005cc560` |
| VA | `0x005cc560`–`0x005cc5ad` (78 B / `0x4E`) |
| Ghidra | `FUN_005cc560` |
| Canonical | `CVOGHBAI_DecEntityA4SecondaryCounter_Inferred` |
| System | input-drive-control / npc-ai / HBAI entity counters |
| Date | 2026-08-05 (MEGA-093 refresh; scaffold 2026-07-23) |

## Machine-level notes

- **thiscall**, ECX = HBAI `this`; **0** stack formals; exits via shared bare-`RET` thunk `0x005081e0`.
- `this+0x18` → host **entity/object** pointer (null-checked).
- `entity+0xA4` → **counter block** pointer (null-checked). Same block sibling `FUN_005cc5b0` mutates at `+0x10/+0x14/+0x18/+0x1c/+0x20/+0x24`.
- Indirect **`entity->vtbl[+0x214]()`** (thiscall, ECX=entity) yields **related** object (driver / related-entity getter pattern elsewhere).
- Branch on `related+0x250`:
  - `== 0` → `counters[+0x18]--`
  - `!= 0` → `counters[+0x1c]--`
- Asm pre-adds `0x10` into ESI then uses `+8`/`+0xC` ≡ decomp `+0x18`/`+0x1c` (algebra sealed).
- **Follow-up after AI axis park:** Driver override `FUN_005d73a0` parks steer/long/handbrake, stops FollowVehicle weapons, pushes axes, then **tail-JMP**s here. CreatureBase-family vtables install **this** function directly in the same slot (after `OnHeartBeat`).
- No direct callees; leaf with one virtual.

## Pseudocode (annotated)

```c
// thiscall; ECX = CVOGHBAI* (CreatureBase family / post-park base)
void __thiscall CVOGHBAI_DecEntityA4SecondaryCounter_Inferred(void *self)
{
  int *entity;      // this+0x18
  int *counters;    // entity+0xA4
  int *related;     // entity->vtbl[+0x214]()

  entity = *(int **)((char *)self + 0x18);
  if (entity == 0) return;
  if (entity[0x29] == 0) return;          // entity+0xA4

  counters = (int *)entity[0x29];
  related = (int *)((**(code **)(*entity + 0x214))());  // thiscall ECX=entity

  if (related == 0) return;

  if (related[0x250 / 4] == 0) {
    counters[0x18 / 4] -= 1;              // secondary counter lane A
  } else {
    counters[0x1c / 4] -= 1;              // secondary counter lane B
  }
}
```

## Call graph (this VA)

```text
FUN_005d73a0  [Driver park override — evidence only]
  └─ tail JMP  FUN_005cc560  CVOGHBAI_DecEntityA4SecondaryCounter_Inferred

vtbl DATA (CreatureBase / Character / Mine / Bot / WalkingCreatureTurreted)
  └─ slot after OnHeartBeat → FUN_005cc560
```

## Open questions / gaps

- Product English for entity counter block at **`+0xA4`** (what the secondary pair counts).
- Product English for **`vtbl+0x214`** and flag/field **`related+0x250`**.
- Exact MSVC vtable ordinal across MI layouts (slot after OnHeartBeat sealed on CreatureBase dump; Driver replaces slot with park).
- Runtime / bit-exact / differential — open (no Launcher).
