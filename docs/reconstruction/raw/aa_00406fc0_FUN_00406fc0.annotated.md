# Annotated low-level: CNDHash_DestroyBucketTable_00a2c2e4

| Field | Value |
|---|---|
| Stable ID | `aa_00406fc0` |
| VA | `0x00406fc0`–`0x00406ff3` (**52 B**) |
| Canonical | `CNDHash_DestroyBucketTable_00a2c2e4` |
| Prior | `FUN_00406fc0` / scaffold `Named_CalleeOf_Named_VOG_DEBUG_STOP_00406fc0` (**reject**) |
| System | container / CNDHash (host vtbl `00a2c2b0`; paired alloc stamp `00a2c2e4`) |
| Date | 2026-08-04 (WQ9G-J dual seal) |

## Machine-level notes

- **ABI:** custom **EAX = hash\*** (not thiscall). Entry `push ebx; mov ebx,eax`. Bare `pop ebx; ret`.
- **Zeros** live entry count `@+0x0c` only (not mask `@+0x08`).
- **Freelist walk:** `FUN_004085e0` with **EBX=hash** — drains each bucket chain, reclaims nodes to freelist `@+0x20` (value-owning path: may `operator_delete` payload @ node+0xc; node stamp rewrite). Nested residual (not OWN).
- **Free table:** if `@+0x10` non-null: `free(*table)` (contiguous 0xc **slab** head), `operator_delete[](table)`, null `+0x10`.
- Peer of inventory `CNDHash_DestroyBucketTable_Inferred` (`0x00413e20`, also EAX) and skill `SkillCNDHash_DestroyBucketTable_Inferred` (`0x004cba60`, **thiscall ECX** — do not merge ABI).

## Pseudocode (annotated)

```c
// custom: EAX = CNDHash*  (in_EAX in decompiler)
// bare ret
void CNDHash_DestroyBucketTable_00a2c2e4(void /* EAX */)
{
  // EBX = EAX for freelist helper
  *(uint32_t *)(eax + 0xc) = 0;
  FUN_004085e0(); // EBX=hash; walk table chains → freelist
  if (*(void **)(eax + 0x10) != 0) {
    free(**(void ***)(eax + 0x10));      // free slab (*table)
    operator_delete__(*(void **)(eax + 0x10)); // delete[] table
    *(void **)(eax + 0x10) = 0;
  }
  return;
}
```

## Callers

| Caller | Site | Setup |
|--------|------|--------|
| Hash dtor `FUN_00406420` | `0x0040646e` | after optional locked-traversal HashError logs; `mov eax,esi` |
| Recreate `FUN_004195d0` | `0x004195f6` | after optional locked Recreate HashError; `mov eax,esi` before re-seed + alloc |

## Open residuals

- Product English for this CNDHash instantiation.
- Dual seal of nested freelist `FUN_004085e0`.
- Null-table freelist safety (callers always had a table; no guard in body).
- Runtime / bit-exact / differential.
