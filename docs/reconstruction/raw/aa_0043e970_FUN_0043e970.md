# Raw capture: FUN_0043e970

| Field | Value |
|---|---|
| **Stable ID** | `aa_0043e970` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0043e970` |
| **Canonical name** | `FUN_0043e970` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0043e970(void)

{
  int in_EAX;
  void *pvVar1;
  uint uVar2;
  
  if (((*(int *)(in_EAX + 0xc) + *(int *)(in_EAX + 0x10) & 1U) == 0) &&
     (*(uint *)(in_EAX + 8) <= *(int *)(in_EAX + 0x10) + 2U >> 1)) {
    FUN_0043ea60(1);
  }
  uVar2 = (uint)(*(int *)(in_EAX + 0xc) + *(int *)(in_EAX + 0x10)) >> 1;
  if (*(uint *)(in_EAX + 8) <= uVar2) {
    uVar2 = uVar2 - *(uint *)(in_EAX + 8);
  }
  if (*(int *)(*(int *)(in_EAX + 4) + uVar2 * 4) == 0) {
    pvVar1 = operator_new(0x10);
    *(void **)(*(int *)(in_EAX + 4) + uVar2 * 4) = pvVar1;
  }
  FUN_0043ece0();
  *(int *)(in_EAX + 0x10) = *(int *)(in_EAX + 0x10) + 1;
  return;
}
```

---

## W31-J re-verify (2026-07-29)

**Tools:** `decompile_function` / `batch_decompile`, `read_memory`, `get_function_by_address`, `analyze_function_complete`, callers/callees. **No** `disassemble_bytes`.

### Body bounds
- Entry `0x0043e970` – last `0x0043e9dd` (**110 B** / `0x6E`)
- Pad `CC CC` then `FUN_0043e9e0` @ `0x0043e9e0`

### Full body hex (`read_memory` 110 B)
```
538bd88b43108b4b0c03c8f6c1015657751183c002d1e839430877076a01e8cd0000008b730c0373108b43088bfed1ef3bc777022bf88b5304833cba0075106a10e8dcae04008b4b0483c4048904b98b53048b04ba8b4c241083e6018d04f0e80c030000834310015f5e5bc20400
```

### ABI (bytes win)
- Entry: `PUSH EBX; MOV EBX,EAX` → **EAX = container**
- Stack formal 1: value pointer (`[ESP+0x10]` after EBX/ESI/EDI pushes = original `[ESP+4]`)
- Epilogue: `POP EDI; POP ESI; POP EBX; RET 0x4`
- Void (no intentional return value)

### Semantics (bytes + decompile)
1. Map-grow gate: `((begin+size)&1)==0 && capacity <= (size+2)>>1` → `FUN_0043ea60(1)` (`GuardedVector_GrowBlockMap_Stride2`, EBX=this)
2. `abs = begin+size`; `page = abs>>1`; if `capacity <= page` then `page -= capacity`
3. If `pages[page]==0`: `pages[page] = operator_new(0x10)` (two 8-byte slots)
4. Slot: `page_base + (abs&1)*8`
5. `FUN_0043ece0`: if dest≠null copy **8 bytes** (`*src` dword + `src[1]` dword) — null-check on EAX only
6. `size++`; **begin unchanged** (push_back polarity)

### Store helper `FUN_0043ece0` @ `0x0043ece0` (`read_memory`)
```
85c0740a8b1189108b4904894804c3   ; TEST EAX,EAX / JZ; MOV EDX,[ECX]; MOV [EAX],EDX; MOV ECX,[ECX+4]; MOV [EAX+4],ECX; RET
```

### Call graph
| Role | Symbol |
|------|--------|
| Callers (2) | `FUN_00971280` @ xref `0x00971381`; `FUN_00972140` @ xref `0x00972322` |
| Callees | `FUN_0043ea60` GrowBlockMap_Stride2; `operator_new`; `FUN_0043ece0` Pod8 copy |

### Live decompile
≡ raw 2026-07-23 CF. Decompiler drops EAX container / stack value / LEA slot math; **bytes restore** full ABI + 8-byte store.

### Named inference
`GuardedVector_PushBack_Stride2_U32x2` — structural twin of W29-B `GuardedVector_PushBack` with `>>1`/`&1`/`*8` and W30-A GrowBlockMap_Stride2.
