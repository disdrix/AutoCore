# Raw capture: FUN_00409160

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409160` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409160` |
| **Canonical name** | `FUN_00409160` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00409160(void)

{
  char cVar1;
  int *piVar2;
  int *in_EAX;
  
  piVar2 = (int *)*in_EAX;
  cVar1 = *(char *)((int)piVar2 + 0x31);
  while (cVar1 == '\0') {
    piVar2 = (int *)*piVar2;
    cVar1 = *(char *)((int)piVar2 + 0x31);
  }
  return;
}
```

---

## WQ9J-E re-verify (2026-08-05) — append only

**Tools:** `batch_decompile` + `disassemble_function` + `read_memory` + `analyze_function_complete` + `get_assembly_context` + callers/xrefs. **No** `disassemble_bytes`. No Launcher.

### Body bytes (`read_memory` @ `0x00409160`, 32 B)

```text
8b 08 80 79 31 00 75 0a 8b c1 8b 08 80 79 31 00 74 f6 c3
cc cc cc cc cc cc cc cc cc cc cc cc cc
```

- Inclusive body: `0x00409160`–`0x00409172` (**19 B** / `0x13`); `CC` pad after.
- Listing:
  ```text
  00409160  MOV ECX,dword ptr [EAX]     ; left = node->left
  00409162  CMP byte ptr [ECX+0x31],0
  00409166  JNZ 0x00409172              ; left isnil → return EAX=node
  00409168  MOV EAX,ECX                 ; node = left
  0040916a  MOV ECX,dword ptr [EAX]
  0040916c  CMP byte ptr [ECX+0x31],0
  00409170  JZ  0x00409168              ; while left non-nil
  00409172  RET                         ; EAX = leftmost non-nil
  ```
- ABI: **EAX** = input node*; returns leftmost in **EAX**; plain **`RET`** (`C3`). Not thiscall.
- isnil every guard **`+0x31`** (`80 79 31 00`).
- Decompiler void/in_EAX phantom is wrong for return — call site seals EAX out.

### Call site (`FUN_00408ed0` @ `0x00408f9d`)

```text
MOV EAX, EDI          ; replacement subtree root (non-nil)
CALL 0x00409160
MOV dword ptr [EBX], EAX   ; head->left = min(replacement)
```

Used when erased node was tree leftmost (`*head == erased`).

### Live decompile

Walk-left while `!isnil(+0x31)` matches bytes; return-in-EAX requires override via call site + listing.

### Named role

`StdTree_Min_Isnil31` — twin of `StdTree_Min_Isnil29` @ `0x0051b5d0`; peer `StdTree_IteratorIncrement_Isnil31` @ `0x00408590`.
