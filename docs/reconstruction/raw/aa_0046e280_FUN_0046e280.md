# Raw capture: FUN_0046e280

| Field | Value |
|---|---|
| **Stable ID** | `aa_0046e280` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0046e280` |
| **Canonical name** | `FUN_0046e280` |
| **System** | `network` / sockaddr fill helper |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_0046e280(undefined4 param_1)

{
  FUN_0067b0a0(0xffffffff,0xffffffff);
  return param_1;
}
```

---

## Live re-decompile (2026-07-29, W16-P OWN dual)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` @ `0x0046e280` + `read_memory` (body + callee `0x0067b0a0`) |
| **Ghidra name** | `FUN_0046e280` |
| **Integrity** | Authoritative body above unchanged; this section is refresh-only |

### Fresh decompile (matches original raw)

```c
undefined4 FUN_0046e280(undefined4 param_1)
{
  FUN_0067b0a0(0xffffffff,0xffffffff);
  return param_1;
}
```

**Note:** Decompiler elides `thiscall` ECX. Bytes seal `ECX = param_1` into `FUN_0067b0a0`.

### Body bytes (`read_memory` @ `0x0046e280`, 48 B; body through `ret`)

```
51 56 8B 74 24 0C 6A FF 6A FF 8B CE C7 44 24 0C 00
00 00 00 E8 07 CE 20 00 8B C6 5E 59 C3 CC CC CC
```

Body span: **`0046e280`–`0046e29d`** (30 bytes) then `CC` pad.

### Byte-level CF (sealed)

```
push ecx                      ; save
push esi
mov  esi, [esp+0xC]           ; esi = param_1 (16-byte sockaddr-like*)
push -1                       ; port arg = 0xFFFFFFFF (u16 low = 0xFFFF)
push -1                       ; addr arg = 0xFFFFFFFF (INADDR_NONE)
mov  ecx, esi                 ; this = param_1
mov  dword ptr [esp+0xC], 0   ; zero saved-ecx slot (stack hygiene)
call FUN_0067b0a0             ; → 0x0067b0a0 (thiscall; ret 8)
mov  eax, esi                 ; return param_1
pop  esi
pop  ecx
ret                           ; caller cleans 1 dword (cdecl)
```

### Callee contract (`FUN_0067b0a0` @ `0x0067b0a0`, not OWN — context only)

`__thiscall`; zeros 16 bytes; `*(u16*)this = 2` (AF_INET); `this+4 = addr`; `*(u16*)(this+2) = htons(port)`; returns `this`.
