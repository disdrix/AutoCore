# Annotated low-level: CVOGObjectiveRequirement_UseItem_ApplyObjectUseProgress

| Field | Value |
|---|---|
| Stable ID | `aa_0060d630` |
| VA | `0x0060d630` |
| Canonical name | `CVOGObjectiveRequirement_UseItem_ApplyObjectUseProgress` |
| Ghidra | `FUN_0060d630` |
| System | missions-progression |
| Date | 2026-08-04 (WQ7R-H live seal) |

## Machine-level notes

- **Body:** `0x0060d630`–`0x0060d7e1` (`read_memory` entry/exit; epilogue `B0 01`/`32 C0` + `C2 0C 00`).
- **ABI:** `__thiscall` (ECX = UseItem requirement*); three stack args; **`ret 0x0C`**; **AL** = 0|1.
- **Class:** `CVOGObjectiveRequirement_UseItem` — vtable `PTR_FUN_009dfc1c`, COL `0x00ab151c` → TD `.?AVCVOGObjectiveRequirement_UseItem@@` @`0x00b003a8`.
- **Caller (sole):** `FUN_0060df70` @`0x0060e061` — object-event path for events **9** and **10** after MatchTargetCore `FUN_0060d460` succeeds; when event==10 or `req[0xd]==0` invokes this unit.
- **Progress:** slot float at `progress+4+(byte)(req+0x08)*4` += `g_flOne` (same slot as `UseItem_Eval` `0x0060cfe0`).
- **GiveMission residual:** on re-eval success, if `req+0x4c != -1` call `CVOGReaction_GiveMission`.
- **String:** `0x009e00a0` shared with `UseItem_InitActive` give-fail log.
- **Decompiler hole:** `unaff_EBX` on destroy/remove path — image loads stack args into EDI/EBX; treat third arg as world object for `+0xcc`/`FUN_004d0e90` (matches escort destroy pattern). Prefer image over decompiler for that local.

## Control flow (annotated)

```
if !localPlayer(+0x7e): return 0
if Eval(req, char, progress): return 0          // already complete
if char+0x250==0 or nested+0x2b0==0: return 0
optional GiveByCbid(req+0x28, 1) + log fail
progress.slot[req+8] += 1.0
done = Eval(...)
optional remove-primary / destroy world object (flags @+0x1c family)
optional RemoveInventoryItem(req+0x2c) (flag @+0x30); early return 1 if incomplete+flag
if done:
  optional GiveMission(req+0x4c)
  optional GiveByCbid(req+0x48, 1) + log fail
return 1
```

## Open questions

- Product English for `FUN_0060df70` (event 9 vs 10 distinction beyond code).
- Full `FUN_005310a0` give path (partially covered as InitActive callee).
- Runtime / bit-exact.
