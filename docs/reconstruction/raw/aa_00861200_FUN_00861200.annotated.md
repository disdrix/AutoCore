# Annotated low-level: FUN_00861200 / UI_InventoryWindow_TryEquipOrLockedToast_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00861200` |
| VA | `0x00861200` |
| Body | `0x00861200`–`0x00861254` exclusive (**84 B** / `0x54`) |
| System | inventory-transfer |
| Date | 2026-08-05 (MEGA-125 live re-verify) |
| Ghidra name | `FUN_00861200` |
| Canonical name | `UI_InventoryWindow_TryEquipOrLockedToast_Inferred` |

## Machine-level notes

- **ABI:** dual-register — **ECX = item\***, **EAX = inventory window\***; bare `RET`; no stack formals.
- **Success:** require non-null item and non-null `window+0x56c`; `mode = *(window+0x56c + 4)`; resolve via `item->vtbl[+0x3ac](mode, 1)`; forward to dualed **`Client_ConfirmEquipOrCustomizeItem`** (`0x00941b20`) with client `DAT_00d1a840` and stack `[resolved, mode, confirmFlag=1]` (vtbl caller-clean — see raw re-verify).
- **Fail:** localize `"This equipment cannot be changed at this time."` @ `0x00a2cc74`; show toast/modal chrome **`0x4e24`** via `FUN_007fdfb0`; **return 0**.
- Prefer assembly when decompiler shows `in_EAX` phantom — sealed as window this from parent `MOV EAX,EDI`.

## Pseudocode (annotated)

```c
// ECX = item*, EAX = UI inventory window*
uint __fastcall FUN_00861200(int *item /*ECX*/)
{
  int window; // in_EAX — parent passes EDI (window this)
  undefined4 mode;
  undefined4 resolved;
  uint result;

  // Gate: need selected item and window mode/state object @ +0x56c
  if ((item != (int *)0x0) && (*(int *)(window + 0x56c) != 0)) {
    mode = *(undefined4 *)(*(int *)(window + 0x56c) + 4);
    // Resolve related object (inventory root / equip target context)
    resolved = (**(code **)(*item + 0x3ac))(mode, 1);
    // Client_ConfirmEquipOrCustomizeItem — town/space/bind gates + C2S equip
    // EAX = &DAT_00d1a840; stack item*=resolved, mode, confirmFlag=1
    result = FUN_00941b20(resolved, mode, 1);
    return result;
  }

  // Locked / missing context — user-facing toast
  FUN_007a69d0(); // locale/string pool
  {
    undefined4 s =
      FUN_007a6de0("This equipment cannot be changed at this time.", 0xffffffff);
    FUN_007fdfb0(&DAT_00d1a840, s, 0xffffffff, 1, 0); // EAX chrome id 0x4e24
  }
  return 0; // XOR AL,AL
}
```

## Parent chain (evidence only — not dualled here)

```text
FUN_008612b0  UI inventory window command (RTTI CWndInventoryItem)
  mode @ window+0x56c (+4 discriminates 1 / 3 / 4)
  ... special types 8 / 0x32 / store / use ...
  └─ FUN_00861200  [OWN MEGA-125]  when normal equip path
       └─ FUN_00941b20  Client_ConfirmEquipOrCustomizeItem  [dualed]
            └─ FUN_00931440  Client_SendInventoryEquipC2S  [dualed]
```

## Open questions

- Product English for window class (parent uses `CNDUIWindow` / `CWndInventoryItem` RTTI; exact inventory-sheet class name residual).
- Product method name for `vtbl[+0x3ac]` resolve.
- Semantic English for mode dword @ `*(window+0x56c)+4` (values 1/3/4 observed in parent).
- Runtime / bit-exact / differential (terminal false).
