# Raw capture: FUN_00973690

| Field | Value |
|---|---|
| **Stable ID** | `aa_00973690` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00973690` |
| **Canonical name** | `FUN_00973690` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00973690(float param_1,uint param_2,uint param_3)

{
  float fVar1;
  float *pfVar2;
  uint uVar3;
  uint uVar4;
  float local_8;
  float local_4;
  
  pfVar2 = &local_8;
  local_4 = g_flOne;
  local_8 = 0.0;
  if ((0.0 <= param_1) && (pfVar2 = &local_4, param_1 <= g_flOne)) {
    pfVar2 = &param_1;
  }
  fVar1 = *pfVar2;
  uVar4 = param_2 >> 8 & 0xff;
  uVar3 = param_2 >> 0x10 & 0xff;
  FUN_00973590((float)(int)((param_3 >> 0x10 & 0xff) - uVar3) * fVar1 + (float)uVar3,
               (float)(int)((param_3 >> 8 & 0xff) - uVar4) * fVar1 + (float)uVar4,
               (float)(int)((param_3 & 0xff) - (param_2 & 0xff)) * fVar1 + (float)(param_2 & 0xff),
               (float)(int)((param_3 >> 0x18) - (param_2 >> 0x18)) * fVar1 +
               (float)(param_2 >> 0x18));
  return;
}
```

---

## Re-verify append — 2026-07-29 W28-F

| Field | Value |
|---|---|
| **Tool** | Ghidra `decompile_function` + `read_memory` + `get_function_by_address` + callers/xrefs + `get_assembly_context` at call sites. **No** `disassemble_bytes`. |
| **Live decompile ≡ raw body** | **Yes** (2026-07-29 ≡ 2026-07-23 scaffold; surface omits ECX dest) |
| **Body** | `0x00973690`–`0x0097376e` (**222 B / `0xDE`**; pad `CC` after `RET 0x0C`) |
| **Classification** | **leaf helper** (1 callee: `FUN_00973590`) |
| **Callers (3)** | `FUN_004b18f0` @ `0x004b1925`; `FUN_005ba440` @ `0x005bb070`; `FUN_00756dc0` @ `0x00756efc` |
| **Callee** | `FUN_00973590` — packs 4 float channels → dest BGRA bytes with 0..255 clamp |
| **ABI** | **ECX** = dest packed-color pointer; stack `(float t, uint32 color0, uint32 color1)`; void; epilogue **`ret 0x0C`** (`C2 0C 00`) |
| **Entry bytes** | `83 EC 08 0F 57 C0 F3 0F 10 54 24 0C` (`sub esp,8`; `xorps`; load `t`) |
| **Exit bytes** | `5F 5E 5D 5B 83 C4 08 C2 0C 00` then `CC` |
| **Constants (`read_memory`)** | `g_flOne` @ `0x00a0f2a0` = `00 00 80 3F` = **1.0f**; clamp low uses **0.0** (`xorps`) |
| **Role name** | `Color_LerpPackedARGB_Inferred` (structural; product/PDB open) |

### Control-flow (sealed)

1. Stack frame `sub esp,8`; load `t` from first formal.
2. Clamp `t` to **[0, 1]** via pointer select: `<0 → 0.0`; `> g_flOne → 1.0`; else `&t`.
3. Extract `color0` channels: B=`&0xff`, G=`>>8&0xff`, R=`>>16&0xff`, A=`>>24`.
4. Per channel: `lerp = (float)(int)(c1_ch - c0_ch) * t + (float)c0_ch` (integer channel delta before float).
5. `mov eax,ecx` then `FUN_00973590(R,G,B,A)` writes packed **BGRA** / dword **`0xAARRGGBB`** at `*ECX` (callee clamps each channel to 0..255).
6. Restore regs; `add esp,8`; **`ret 0x0C`**.

### Call-site seal (ECX dest)

| Site | Dest (ECX) | Notes |
|---|---|---|
| `004b1925` | `LEA ECX,[child+0x34]` after `MOV EAX,[slot+8]` | in-place lerp `*(child+0x34)` → `DAT_00afdef0` (`0xFF000000`) at t=**0.75** |
| `005bb070` | `LEA ECX,[EBP+0x2c]` | stack colors + t |
| `00756efc` | `LEA ECX,[EDI+0xc]` | stream/color table walk |

### Notes

- Decompiler surface is `void FUN_00973690(float,uint,uint)` — **misses ECX dest** and **`ret 0x0C`**. Bytes + call-site `LEA ECX` seal both.
- Channel layout matches D3DCOLOR: bits `BB|GG|RR|AA` in memory little-endian B,G,R,A.
- Nested pack helper `FUN_00973590` English/dual OPEN (not OWN).
