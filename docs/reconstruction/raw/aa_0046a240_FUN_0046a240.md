# Raw capture: FUN_0046a240

| Field | Value |
|---|---|
| **Stable ID** | `aa_0046a240` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0046a240` |
| **Canonical name** | `FUN_0046a240` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0046a240(undefined4 param_1,undefined4 *param_2)

{
  undefined4 *in_EAX;
  int iVar1;
  undefined4 *unaff_EBX;
  undefined4 *puVar2;
  undefined4 *puVar3;
  
  while (unaff_EBX != param_2) {
    param_2 = param_2 + -7;
    in_EAX = in_EAX + -7;
    puVar2 = param_2;
    puVar3 = in_EAX;
    for (iVar1 = 7; iVar1 != 0; iVar1 = iVar1 + -1) {
      *puVar3 = *puVar2;
      puVar2 = puVar2 + 1;
      puVar3 = puVar3 + 1;
    }
  }
  return;
}
```

---

## W35-L re-verify append (2026-07-29)

| Field | Value |
|---|---|
| **Tool** | Ghidra `batch_decompile` + `read_memory` + function xrefs. **No** `disassemble_bytes` |
| **Integrity** | Scaffold raw body above preserved; this section appended only |
| **Live decompile** | ≡ 2026-07-23 scaffold CF (reverse 7-dword element move) |
| **Body** | `0x0046a240`–`0x0046a25d` inclusive (**30 B** / `0x1E`); pad `CC`×2 then `FUN_0046a260` @ `0x0046a260` |
| **Epilog** | bare **`C3`** (`POP EDI; POP ESI; RET`) |
| **Classification** | leaf — 0 callees |
| **Callers (3)** | `FUN_00469f50` @ `0x0046a1dc`; `FUN_00466ea0` @ `0x00467150`; `FUN_0045cd30` @ `0x0045cff4` |

### Full body hex (30 B)

```
3bda7419565783ea1c83e81c3bd3b9070000008bf28bf8f3a575eb5f5ec3
```

### Byte-sealed ABI / CF

```
CMP EBX, EDX / JE done       ; src_begin == src_end → empty range
PUSH ESI
PUSH EDI
loop:
  SUB EDX, 0x1C              ; src_end  -= 28
  SUB EAX, 0x1C              ; dest_end -= 28
  CMP EDX, EBX               ; set flags for loop (flags survive REP MOVSD)
  MOV ECX, 7
  MOV ESI, EDX               ; copy from current src element
  MOV EDI, EAX               ; copy to current dest element
  REP MOVSD                  ; 7 dwords
  JNZ loop                   ; while src_end != src_begin
POP EDI
POP ESI
RET
```

| Slot | Register | Role |
|---|---|---|
| dest_end* | **EAX** | exclusive dest end; walks backward by 0x1c |
| src_end* | **EDX** (`param_2`) | exclusive src end; walks backward by 0x1c |
| src_begin* | **EBX** (`unaff_EBX`) | stop when src_end reaches this |
| ECX | clobber | `REP MOVSD` count (=7); **not** a formal |
| decompiler `param_1` | unused | Ghidra phantom; **not** a formal |
| return | none | void; bare RET |

Call-site seal (parent `FUN_00466ea0` @ `0x00467150`):

```
MOV ESI, [ebp+8]     ; where / lower bound
MOV EAX, [ebp+0xC]   ; dest_end related
MOV EDX, EBX         ; src_end
MOV EBX, ESI         ; src_begin = where
CALL FUN_0046a240
```

Decompiler while/for ≡ `copy_backward` for POD stride **0x1c**. Used by elem-28 insert-n mid-shift (W34-P/Q). Sibling `FUN_0046a260` = uninit fill-n (next symbol).
