# Annotated low-level: FUN_00971700 → AssPreloader_EnqueueEligibleFromCacheKeySpan_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00971700` |
| VA | `0x00971700`–`0x00971795` (**150 B**) |
| System | assPreloader / asset preload pipeline |
| Date | 2026-07-29 (W33-M seal) |
| Canonical | `AssPreloader_EnqueueEligibleFromCacheKeySpan_Inferred` |

## Machine-level notes

- Source: raw capture + W33-M live `read_memory` / decompile / listing.
- SEH frame `LAB_009afe03` (shared neighborhood with sibling AssPreloader workers).
- **ESI = host** from ECX; local GuardedVector on stack zero-init.
- Span source: `*(*(host+0x110)+0x64)` then `+0x50` first / `+0x54` last as `const uint32_t*`.
- Insert via sealed `GuardedVector_InsertRange` (W32-G; sole known caller of that unit).
- Enqueue via sealed `AssPreloader_EnqueueEligibleFromRing_Inferred` (W31-K; ESI=local, stack=host).
- **No static call xrefs** — inventory orphan; still body-sealable.

## Sealed ABI

```
// ECX = AssPreloader* host
// bare RET; returns 0 in EAX
```

## Pseudocode (bytes-faithful)

```c
uint32_t AssPreloader_EnqueueEligibleFromCacheKeySpan_Inferred(void* host /*ECX*/)
{
  GuardedVectorHeader local = {}; // zeroed pages/cap/begin/size
  void* ctx = *reinterpret_cast<void**>(
      *reinterpret_cast<uint8_t**>(reinterpret_cast<uint8_t*>(host) + 0x110) + 0x64);
  const uint32_t* first = *reinterpret_cast<const uint32_t**>(
      reinterpret_cast<uint8_t*>(ctx) + 0x50);
  const uint32_t* last  = *reinterpret_cast<const uint32_t**>(
      reinterpret_cast<uint8_t*>(ctx) + 0x54);

  FUN_0043df90(/* erase/init local */);
  GuardedVector_InsertRange(&local, /*seed*/&local, /*insert_at*/0, first, last, /*sixth*/…);
  AssPreloader_EnqueueEligibleFromRing_Inferred(host, &local); // ESI=local
  FUN_005b2ba0(&local);
  return 0;
}
```

## Family contrast

| VA | Role |
|---|---|
| `00971640` | Enqueue eligible from caller ring |
| **`00971700`** | Snapshot cache-ctx key span → local → enqueue |
| `009717a0` | Single key + expand deps → enqueue |
| `00971820` | Per-key expand deps from ring → enqueue |

## Open residual

- Product English for cache-ctx object at `*(cache+0x64)` and fields `+0x50`/`+0x54`.
- Why zero static xrefs (dead / dynamic / unlinked).
- Nested `FUN_0043df90` / `FUN_005b2ba0` full dual ownership elsewhere.
- Runtime / bit-exact.
