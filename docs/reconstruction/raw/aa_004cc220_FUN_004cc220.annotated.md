# Annotated low-level: FUN_004cc220 → StdTree_InsertHint_Isnil29_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004cc220` |
| VA | `0x004cc220` |
| Canonical name | `StdTree_InsertHint_Isnil29_Inferred` |
| System | MSVC `std::_Tree` unique insert-with-hint (isnil@+0x29; 2-dword ordered key) |
| Date | 2026-08-04 (WQ9F-E); scaffold 2026-07-23 |
| Tools | decompile + read_memory + analyze_function_complete + xrefs (**no** disassemble_bytes) |

---

## Machine-level notes

- **Body:** `0x004cc220`–`0x004cc3c4` inclusive (**421 B** / `0x1A5`); exclusive end `0x004cc3c5`; terminal **`C2 0C 00`**; pad `CC`.
- **ABI:** `__thiscall`; ECX = map shell* (`head@+4`, `size@+8`); stack `Node** outIt`, `Node* hint`, `Val* value`; **RET 0x0c**.
- **Node family:** left+0 parent+4 right+8; value @+0x10 (6 dwords / 0x18 via dualed insert); color@+0x28; **isnil@+0x29**; node **0x30**.
- **Key order:** 2 dwords — value `v[0],v[1]` vs node `+0x10,+0x14`. Compare helper `FUN_00401480(a,b)` = true iff `(a.hi < b.hi) || (a.hi == b.hi && a.lo < b.lo)` with hi treated as signed, lo unsigned (decomp).
- **Fast paths →** dualed `StdTree_InsertAndRebalance_Isnil29_Inferred` (`FUN_004cbb60`, RET 0x10):
  1. Empty map (`size==0`): insert under head, addLeft=1.
  2. Hint == leftmost: if value < hint key → insert left of hint.
  3. Hint == head (end): if rightmost key < value → insert right of rightmost.
  4. Neighbor: if value < hint, prevnode; if between pred and hint, insert on open side (isnil test on pred/hint right@+8).
  5. Neighbor: if hint key < value, nextnode; similar open-side insert.
- **Fallback:** `FUN_004cbee0` insert-or-find → `*outIt = pair.node` (inserted flag discarded by this shell).
- **Reject:** `Named_CalleeOf_Named_CalleeOf_Named_VOG_DEBUG_STOP_004cc220` scaffold — shared STL helper, not VOG product method.

---

## Control flow (annotated)

```
if (map->size == 0)
  InsertAndRebalance(out, 1, head, value); return out;

if (hint == head->left) {                 // begin
  if (KeyLess(value, hint+0x10))
    InsertAndRebalance(out, 1, hint, value); return out;
} else if (hint == head) {                // end
  if (KeyLess(rightmost+0x10, value))
    InsertAndRebalance(out, 0, rightmost, value); return out;
} else {
  if (KeyLess(value, hint+0x10)) {
    pred = hint; Prevnode(&pred);
    if (KeyLess(pred+0x10, value)) {
      if (pred->right.isnil) Insert(out, 0, pred, value);
      else                   Insert(out, 1, hint /*or open child*/, value);
      return out;
    }
  }
  if (KeyLess(hint+0x10, value)) {
    succ = hint; Nextnode(&succ);
    if (hint == head || KeyLess(value, succ+0x10)) {
      if (hint->right.isnil) Insert(out, 0, hint, value);
      else                   Insert(out, 1, ..., value);
      return out;
    }
  }
}
// equal / unusable hint
pair = InsertOrFind(value); *out = *pair; return out;
```

---

## Callers / callees

| Role | Symbol |
|---|---|
| Sole external caller | `FUN_004cc400` (1 UNCONDITIONAL_CALL @ `0x004cc457`) — stages 5-dword local value then returns node+0x20 |
| Always-insert | `FUN_004cbb60` / `StdTree_InsertAndRebalance_Isnil29_Inferred` (7 sites) |
| Insert-or-find | `FUN_004cbee0` (fallback; WQ9F-D) |
| Key less | `FUN_00401480` |
| Prev / next | `FUN_004cb4f0` / `FUN_004cb270` |

---

## Peer do-not-merge

| Peer | VA | Diff |
|---|---|---|
| `StdTree_InsertHint_Val12` | `0x0053a8e0` | isnil@+0x19; single int key @+0x0c |
| `StdTree_InsertAndRebalance_Isnil29_Inferred` | `0x004cbb60` | always-insert only (no key compare) |
| `FUN_004cbee0` | `0x004cbee0` | full insert-or-find walk (no hint) |
| `FUN_004cbe20` | `0x004cbe20` | insert-or-find **int** key sibling |

---

## Pseudocode source

Authoritative raw body: `docs/reconstruction/raw/aa_004cc220_FUN_004cc220.md`  
Clean: `docs/reconstruction/reconstructed-exact/StdTree_InsertHint_Isnil29_Inferred.cpp`
