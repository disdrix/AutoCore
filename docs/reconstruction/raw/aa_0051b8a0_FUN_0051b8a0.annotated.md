# Annotated low-level: ObjectCsList_ClearDestroy_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051b8a0` |
| VA | `0x0051b8a0`–`0x0051b99c` exclusive (**252 B** / `0xFC`) |
| Ghidra | `FUN_0051b8a0` |
| Semantic | `ObjectCsList_ClearDestroy_Inferred` |
| System | object core / CS-owned list teardown |
| Date | 2026-07-29 (W30-F) |

## Machine-level notes

- **ABI:** `__thiscall`/`__fastcall`; ECX = CS-list object (`sizeof 0x2C`, vtbl `PTR_FUN_009ce154`); bare frame RET.
- **Large stack:** `__chkstk` with `0x1004` (SEH/`_CxxThrowException` path artifact) — decompiler `__chkstk` warning expected.
- **Primary owner:** Object SharedBase stores this list at **`core+0xB0`** (ctor `Object_SharedBase_Ctor` `new(0x2C)` + `InitializeCriticalSection`).
- **Sibling:** `List_ClearDestroy` `0x0040dc40` — same EnterCS / flag-throw / drain / zero pattern, **different node next offset** (`+0x10` vs `+0x08`).
- **Near twin:** `FUN_0051bc90` — identical drain but **does not** zero `node+4` before scalar dtor (used from CS-list complete dtor path).

## Control flow (annotated)

```c
// ECX = ObjectCsList* (SharedBase+0xB0)

EnterCriticalSection(&list->cs);          // list+4
if (list->traversalFlag) {                // list+0x28
  LeaveCriticalSection(&list->cs);
  throw HRESULT 0x80070005;               // E_ACCESSDENIED
}
node = list->head;                        // list+0x1C
while (node) {
  list->head = node->next;                // node+8
  node->payloadOrRef = 0;                 // node+4  (this unit only)
  (*node->vtbl)(node, 1);                 // scalar-deleting dtor
  node = list->head;
}
list->field24 = 0;
list->field20 = 0;
list->head = 0;
LeaveCriticalSection(&list->cs);
```

## Call sites

| Site | Note |
|---|---|
| `Object_SharedBase_Dtor` `0x00518f6e` | `mov ecx,[esi+0xB0]`; then scalar-delete same pointer |
| CODE `0x00514d30` | orphan function body @ `0x00514d10`; `mov ecx,[esi+0xB0]`; `ret 4` |

## Open gaps

- Product English for CS-list class (`PTR_FUN_009ce154`).
- Field English for `+0x20` / `+0x24` (count/tail family — Probable only).
- Node product type payload.
- Runtime under contended CS / concurrent TraversalLock.
