# Annotated low-level: FUN_00519280 → MaterialList_SeedDefaultDiffuseAmbientEmissive_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00519280` |
| VA | `0x00519280` |
| System | client-fx / material param bank seed |
| Date | 2026-08-04 (WQ9R-E dual seal) |

## Machine-level notes

- **ABI:** stack formals — `src*` @ `[esp+4]`, `dstVecHost*` @ `[esp+8]`, plus **4×float** (float4) for `FUN_009700f0` at stack `+0x0C`. Void; epilogue `POP`×4 + `ADD ESP,0x14; RET`.
- **Body:** `0x00519280`–`0x005194f0` exclusive (**624 B** / `0x270`).
- **Constant:** `g_flOne` @ `0x00A0F2A0` = `00 00 80 3F` (1.0f).
- **Strings:** `"MatDiffuse"`, `"MatAmbient"`, `"MatEmissive"`.
- **Vector dst:** begin `dst+4`, end `dst+8`, capacity `dst+0xC`; element stride **0x10** (float4).
- **Source materials:** pointer range `[src+0xC0, src+0xC4)` count `>>2`.
- Prefer decompiler + `read_memory` (no `disassemble_bytes`).

## Pseudocode (annotated)

```c
// MaterialList_SeedDefaultDiffuseAmbientEmissive_Inferred
// For each material pointer in src list, push default {0,0,0,1} for
// MatDiffuse / MatAmbient / MatEmissive into dst float4 bank, then apply
// caller-supplied float4 (stack) to those effect params.
void MaterialList_SeedDefaultDiffuseAmbientEmissive_Inferred(
    void *srcMaterialOwner,   // +0xC0/+0xC4 pointer vector
    void *dstVecHost,         // vector fields at +4/+8/+0xC
    float colorR, float colorG, float colorB, float colorA /* stack float4 */)
{
  float4 local = {0.f, 0.f, 0.f, 1.f}; // g_flOne
  // FUN_0051def0(...): residual this/target — vector resize helper, N likely 0
  FUN_0051def0(/*see gaps*/);
  local = {0.f, 0.f, 0.f, 1.f};

  int n = (src+0xC0 == 0) ? 0 : ((*(int*)(src+0xC4) - *(int*)(src+0xC0)) >> 2);
  for (int i = 0; i < n; ++i) {
    // for each of MatDiffuse, MatAmbient, MatEmissive:
    FUN_0096f840(name, &local);          // bind default to effect
    if (need_grow(dst)) FUN_0051d4a0(end, 1, &local);
    else { FUN_00608720(end, 1, &local); end += 0x10; }

    FUN_009700f0("MatDiffuse",  &callerFloat4);
    FUN_009700f0("MatAmbient",  &callerFloat4);
    FUN_009700f0("MatEmissive", &callerFloat4);
  }
}
```

## Open questions

- Exact ECX for head `FUN_0051def0` (decompiler drops this).
- Product class of `src` (mesh/fx material table) and host of dst bank.
- Whether loop index uses material pointer value or only count (decomp iterates count only; element load residual).

