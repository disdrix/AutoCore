# Annotated low-level: FUN_0045cd30 → StdVector_InsertN_Pod28

| Field | Value |
|---|---|
| Stable ID | `aa_0045cd30` |
| VA | `0x0045cd30`–`0x0045d01c` exclusive (**748 B**) |
| System | MSVC `vector` insert-n, POD elem **0x1c** |
| Date | 2026-07-23 scaffold; W34-P seal 2026-07-29 |
| Canonical name | `StdVector_InsertN_Pod28` (**Inferred**) |

## Machine-level notes

- **Bytes win** on ABI: **ECX=count**, **EDX=vector\***, stack **where** + **value\***, **`RET 8`**.
- Prologue copies value via **7× MOVSS** into `[ebp-0x30]` local (elem 0x1c) — decompiler omits SSE block.
- Triad: begin `@+4`, end `@+8`, capEnd `@+0xC`; size/cap via magic `0x92492493` (÷28); max `0x9249249`.
- Grow 1.5× (`cap + (cap>>1)`); floor `size+count`; helpers are **trivial** 7-dword range copy/fill (no dtor).
- Sibling contrast: `StdVector_InsertN_BasicString` (`0x004306b0`) is string-aware, **ECX=vec**, **RET 0xC**.
- Reject scaffold `Named_CalleeOf_Named_CalleeOf_Named_anmTrackMaster_inl_*` (domain residual only).

## Signature (sealed)

```c
// ECX=count, EDX=vec*, stack (where, value*); RET 8; void
void StdVector_InsertN_Pod28(
    uint32_t count,           /* ECX */
    StdVectorPod28* vec,      /* EDX */
    Pod28* where,             /* [ebp+8] */
    const Pod28* value);      /* [ebp+0xc] — 7×float/dword source */
```

## Control flow (annotated)

1. SEH frame `LAB_009bddf0`; SSE-load local value from `*value`.
2. `cap = begin? (capEnd-begin)/0x1c : 0`.
3. If `count==0` → epilogue.
4. If `size+count > 0x9249249` → `FUN_004540b0`.
5. **Grow** if `cap < size+count`: newcap 1.5× or size+count; `new[]`; prefix copy `FUN_0045f360`; fill-n `FUN_0045f150`; suffix copy; `delete[]` old; publish triad; return.
6. **In-place**: hole vs mid via `FUN_0045f360` / `FUN_0045f150` / `FUN_0046a240` / `FUN_0045efe0`.
7. `RET 8`.

## Full hex

See raw W34-P append (748 B).
