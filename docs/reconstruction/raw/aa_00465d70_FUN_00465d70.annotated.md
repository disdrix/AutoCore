# Annotated low-level: StdVector_Tidy_Stride24_EbxThis_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00465d70` |
| VA | `0x00465d70`–`0x00465db0` exclusive (**64 B**) |
| System | container / vector tidy (Palantir nested 0x24 elems) |
| Date | 2026-08-04 (W38-R) |
| Canonical | `StdVector_Tidy_Stride24_EbxThis_Inferred` (**Inferred**) |
| Ghidra | `FUN_00465d70` |

## Machine-level notes

- **EBX-this** vector shell: begin@**+4**, end@**+8**, capacity_end@**+0xC**.
- Null begin: zero triple and return (no free).
- Non-null: walk `[begin,end)` stride **0x24**, call `FUN_00481d80` with **ESI=elem** (frees nested buffer @+0x18, zeros +0x18/+0x1c/+0x20), then `operator_delete(begin)`, zero triple.
- Decompiler marks `operator_delete` noreturn — **false**; control continues to zero + RET.
- Thunk `0x00465c30` JMP here.
- Sole real CALL: W37-N `PalantirSub10_OwnedElem_CompleteDtor` with `LEA EBX,[elem+0x70C]`.

## Pseudocode (bytes-corrected)

```c
// EBX = vec*; void; RET
void StdVector_Tidy_Stride24_EbxThis_Inferred(void)
{
    Vec24* v = (Vec24*)/*EBX*/;
    uint8_t* begin = v->begin; // +4
    if (!begin) {
        v->begin = v->end = v->cap = NULL;
        return;
    }
    for (uint8_t* p = begin; p != v->end; p += 0x24) {
        FUN_00481d80(); // ESI = p; free nested @+0x18
    }
    operator_delete(v->begin);
    v->begin = v->end = v->cap = NULL;
}
```

## Open questions

- Product element type English for 0x24 payload.
- Whether +0 of vector shell holds allocator / unused (never written here).
- Runtime / bit-exact / differential.
