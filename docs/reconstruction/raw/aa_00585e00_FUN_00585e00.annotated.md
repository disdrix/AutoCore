# Annotated low-level: FUN_00585e00 / CVOGGraphicsBase_PostLoadScaleAndSelectFx

| Field | Value |
|---|---|
| Stable ID | `aa_00585e00` |
| VA | `0x00585e00`–`0x00586056` (599 B inclusive last RET) |
| System | client / graphics / select-fx / LogicUI |
| Date | 2026-07-29 (W26-N dual seal) |
| Role name | `CVOGGraphicsBase_PostLoadScaleAndSelectFx` (Probable — caller plate `CVOGGraphicsBase::InitializeGraphics`) |

## Machine-level notes

- **ABI:** `__thiscall`; ECX=`this`; stack `char skipSideFx`; **`RET 4`**.
- **Entry:** `55 8B EC 83 E4 F0 81 EC 88 01 00 00 56 8B F1` — frame + ESI=this.
- **Early out:** `FLD dword [g_flZero]` @ `0x00a0f518` then epilogue when `this+8==0` or `FUN_005130e0(1)==0`.
- **Scale math:** `MOVSS xmm0,[0x00a10e74]` (**2.0**) / (`[bounds+0xC]*2.0`) ⇒ reciprocal of bounds scale; Ghidra name `g_flLevelUpUiBase_Inferred` is **misnomer residual** (value sealed, product English open).
- **Near gate:** `DAT_00a0f524` = **50.0f**; compared to distance from `FUN_00404bd0` after transform of related-object pose.
- **LogicUI:** type field `0x1F`; payload float `_DAT_009da8a4` = **3.5f**; quat from object `+0x164..+0x170`.
- **Callee seal:** `FUN_0051aed0` = dual-sealed `Object_PlayPickupSpecialFX` (W25-J); args `(dist < 50, FUN_00516c40(obj))`.
- Prefer decompile CF + `read_memory` constants; **no** `disassemble_bytes`.

## Pseudocode (annotated)

```c
// thiscall ECX=this (CVOGGraphicsBase*); stack char skipSideFx; RET 4; ST0 float
// Role: post-load apply reciprocal mesh scale, place/orient graphics, optional pickup FX + LogicUI 0x1F

float10 __thiscall CVOGGraphicsBase_PostLoadScaleAndSelectFx(int this, char skipSideFx)
{
  // Gate: graphics object at this+8 and FUN_005130e0(1)
  // scale = 1.0f / *(float*)(vtbl48_result + 0xC)   // via 2.0/(field*2.0)
  // host.vtbl+0xB8(scale)
  // pos = this chain +0x84; pos.y += scale; FUN_004e88e0 → world xyz
  // graphics.vtbl+0x10(3, &xyz); FUN_0040cf90(3)
  // if (!skipSideFx):
  //   optional Object_PlayPickupSpecialFX(dist<50, flag)
  //   if clone: Client_SendLogicUiPacket type 0x1F + quat + 3.5f
  // return scale (ST0)
}
```

## Open questions

- Product method English inside `CVOGGraphicsBase` (not plate-string on this VA).
- Exact semantic of graphics vtbl `+0x10` mode **3** and host vtbl `+0xB8`.
- Why `g_flLevelUpUiBase_Inferred` label was applied to **2.0** constant (historical misname).
- Runtime / bit-exact ST0 vs FPU CW.
