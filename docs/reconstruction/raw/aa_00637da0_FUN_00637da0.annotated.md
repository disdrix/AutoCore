# Annotated low-level: CVOGObjectiveRequirement_ctor (`FUN_00637da0`)

| Field | Value |
|---|---|
| Stable ID | `aa_00637da0` |
| VA | `0x00637da0` |
| Canonical | `CVOGObjectiveRequirement_ctor` |
| Prior | `FUN_00637da0` |
| System | `missions-progression` (objective requirement base) |
| Date | 2026-07-29 (W17-Q dual seal) |

## Machine-level notes

- Live tools: `decompile_function` + `read_memory` only (no `disassemble_bytes`).
- Body is two stores + `RET 4`; decompiler missed **return-this** (EAX = original ECX).
- Base class RTTI `.?AVCVOGObjectiveRequirement@@` @ `0x00afe9dc`.
- Shared by all known requirement subclass ctors (Collect, Deliver, Kill, …); each overwrites vfptr after return.

## ABI

```c
// MSVC __thiscall; parent on stack; RET 4; returns this in EAX
void* __thiscall CVOGObjectiveRequirement_ctor(void* this, void* parent);
```

| Fact | Bytes / evidence |
|---|---|
| `this` in ECX | prologue `8B C1` (save to EAX) |
| `parent` stack0 | `8B 4C 24 04` |
| vfptr | `C7 00 AC 3B 9E 00` → `*this = 0x009e3bac` |
| parent field | `89 48 04` → `*(this+4) = parent` |
| epilogue | `C2 04 00` |
| return | EAX holds this |

## Layout written

| Off | Init | Role |
|----:|------|------|
| `+0x00` | `PTR_LAB_009e3bac` | base vfptr (subclasses overwrite) |
| `+0x04` | `parent` | owning objective / parent pointer |

**Not written:** RequirementType (`+0x0c` set by subclass), CBID, counts, flags.

## Pseudocode (annotated)

```c
void* __thiscall CVOGObjectiveRequirement_ctor(void* self, void* parent)
{
  // *self = &PTR_LAB_009e3bac;   // temporary base vtable
  // *(void**)((char*)self + 4) = parent;
  // return self;
  *self = &PTR_LAB_009e3bac;
  self[1] = parent;
  return self;
}
```

## Callers (production subclass ctors)

Collect `0x00611690`, Deliver `0x00610bc0`, plus `005adb20`, `0060c0b0`, `0060cb80`, `0060ced0`, `0060e090`, `0060e9f0`, `0060f0d0`, `0060fe80`, `006101f0`, `00610810`, `00612c80`, `006138a0` (14 total).

## Open questions

1. Product English name of each base vfptr slot (stub cluster @ `009e3bac`).
2. Whether `parent` is always objective-def row vs objective instance (caller-dependent).
3. Runtime / bit-exact image diff.
