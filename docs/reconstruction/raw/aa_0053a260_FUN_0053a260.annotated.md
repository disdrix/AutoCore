# Annotated low-level: FUN_0053a260 / Map_EraseRange_Val12

| Field | Value |
|---|---|
| Stable ID | `aa_0053a260` |
| VA | `0x0053a260` |
| System | MSVC std map/set range erase (Val12 node) |
| Date | 2026-07-29 (W28-S dual seal) |
| Canonical (inferred) | `Map_EraseRange_Val12` |

## Machine-level notes

- Source: raw capture + live decompile 2026-07-29 ≡ raw CF.
- **ABI:** `__thiscall`; ECX = map (`head` @ `+4`, `size` @ `+8`); stack `(outIt*, first*, last*)`; **`ret 0x0c`**.
- **Full-range fast path:** `first == *head && last == head` → `FUN_00538690(head->parent)` recursive free (isnil@+0x19), reset head self-links, `size=0`, `*outIt=*head`.
- **Partial range:** while `first != last`: compute in-order successor (isnil@+0x19 gates), `FUN_005399f0(&first, first)` single-node erase (`Map_EraseNode_Val12`), advance; then `*outIt = first`.
- Sole CODE caller: `FUN_0053ba50` — erase `[begin,end)` then `operator_delete(head)` (map shell teardown).
- Peer single-node: `aa_005399f0` `Map_EraseNode_Val12` (W27-R). Do **not** merge with isnil@+0x29 family (`aa_0051cb40`) or isnil@+0x15 (`StdMap_EraseRange` `aa_004bacf0`).

## Pseudocode (annotated)

```c
// thiscall; ret 0x0c
void __thiscall Map_EraseRange_Val12(
    MapShell *map,     // ECX — head@+4, size@+8
    Node **outIt,      // stack
    Node *first,
    Node *last)        // exclusive end
{
  Node *head = map->head;
  if (first == head->left /*begin*/ && last == head /*end*/) {
    FUN_00538690(head->parent);   // free all non-nil
    head->parent = head->left = head->right = head;
    map->size = 0;
    *outIt = head->left;
    return;
  }
  while (first != last) {
    Node *succ = /* in-order successor using isnil@+0x19 */;
    FUN_005399f0(&first, first);  // Map_EraseNode_Val12; first becomes succ via out-param dance
    first = succ;
  }
  *outIt = first;
}
```

## Open questions

- Exact demangled map type owning this clone (sole parent `FUN_0053ba50`).
- Product English for that map.
- Runtime / bit-exact open.
