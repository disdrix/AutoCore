# Raw capture: FUN_004b6a80

| Field | Value |
|---|---|
| **Stable ID** | `aa_004b6a80` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004b6a80` |
| **Canonical name** | `FUN_004b6a80` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
uint __fastcall FUN_004b6a80(int param_1)

{
  undefined4 *puVar1;
  
  puVar1 = (undefined4 *)**(undefined4 **)(param_1 + 0x1e0);
  while( true ) {
    if (puVar1 == *(undefined4 **)(param_1 + 0x1e0)) {
      return (uint)puVar1 & 0xffffff00;
    }
    if ((*(byte *)(puVar1[2] + 0x989) & 0x10) != 0) break;
    puVar1 = (undefined4 *)*puVar1;
  }
  return CONCAT31((int3)((uint)puVar1 >> 8),1);
}
```

---

## Live re-verify — 2026-07-29 (OWN-ONLY W26-G)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `analyze_function_complete` + `read_memory` |
| **Program** | `autoassault.exe` base `0x400000` |
| **Live ≡ raw scaffold** | **Yes** |
| **Body span** | `0x004b6a80`–`0x004b6aa7` (**40 B**; pad `CC` after last `RET`) |
| **Classification** | **leaf** (0 callees) |
| **Xrefs** | 2 — `0x004ba0df` (`FUN_004ba0a0`), `0x004b8f28` (`FUN_004b8ed0`) |
| **Callers** | `FUN_004b8ed0` (`NDSpecialFX_HostTick_Inferred`), `FUN_004ba0a0` |
| **Callees** | none |

### ABI (bytes)

| Slot | Role |
|---|---|
| **ECX** | host / FX object `this` |
| Formals | none on stack |
| Epilogue | plain `RET` |
| Return | **AL** bool: `0` if no match, `1` if any list node hits flag |

### Semantics (sealed)

Walk circular list sentinel at `this+0x1e0`:

1. `sentinel = *(this+0x1e0)`; `node = *sentinel`.
2. While `node != sentinel`:
   - `elem = node[2]` (`[node+8]`).
   - If `(*(byte*)(elem+0x989) & 0x10) != 0` → return **1**.
   - `node = *node` (next).
3. Exhausted → return **0** (`XOR AL,AL`).

### Full body hex (40 B)

```
8b89e00100008b013bc1567414b210908b7008849689090000750a8b003bc175ef32c05ec3b0015ec3
```

```
8B 89 E0 01 00 00   MOV ECX, [ECX+0x1e0]   ; sentinel
8B 01               MOV EAX, [ECX]         ; first node
3B C1               CMP EAX, ECX
56                  PUSH ESI
74 14               JE ret0
B2 10               MOV DL, 0x10
90                  NOP
8B 70 08            MOV ESI, [EAX+8]       ; payload
84 96 89 09 00 00   TEST [ESI+0x989], DL
75 0A               JNZ ret1
8B 00               MOV EAX, [EAX]
3B C1               CMP EAX, ECX
75 EF               JNZ loop
32 C0               XOR AL, AL
5E                  POP ESI
C3                  RET
B0 01               MOV AL, 1
5E                  POP ESI
C3                  RET
```

### Caller role

Both callers use result as **type-4 optional re-enter / keep-active** probe: non-zero keeps host on active path instead of deactivating.
