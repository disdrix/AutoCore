# Annotated low-level: FUN_00984340 → Fs_ExpandPathSpecToStringVec_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00984340` |
| VA | `0x00984340`–`0x009847bb` (**1148 B** / `0x47C`) |
| System | filesystem / path-spec expansion (ass/pack tool cluster) |
| Date | 2026-07-23 scaffold; **W33-E dual seal 2026-07-29** |
| Preferred name | `Fs_ExpandPathSpecToStringVec_Inferred` |

## Machine-level notes

- Source: raw capture + W33-E re-verify append.
- Prefer **bytes** when decompiler conflicts (SSO path loads, thiscall ECX on `FUN_004301f0` / `FUN_00469d10`).
- Separator constant `DAT_00a2eb5c` = `"/"` (`2f 00 00 00`).
- Regular-file test: `st_mode >> 15 & 1` (`_S_IFREG`); is-dir uses unowned `FUN_0076adc0` (`>> 14`).
- **No external static callers** — only self @ `0x009843e5` for `@`-list expansion.

## Control flow (sealed)

```
collector = empty string vector
if path[0] == '@':
  lines = ReadPathListFile(path+1)          // FUN_009841d0
  for line in lines:
    nested = Expand(line, recurse_flag)    // self
    append nested → collector
else if !IsDirectory(path):                // FUN_0076adc0
  if stat(path) ok && IsRegularFile:
    push path → collector
  else:                                    // glob / pattern
    (dir, pattern) = SplitDirPattern(path) // strrchr / or \
    for entry in ListDir(dir):             // FUN_0076b3f0
      if GlobMatch(entry, pattern):        // FUN_0076b210
        push dir + "/" + entry
else:                                      // directory
  if recurse_flag:
    DirRecurseCollectLeafPaths(path, &collector)  // FUN_009839b0
  else:
    for entry in ListDir(path):
      push path + "/" + entry
Assign(collector → out_vec)                // FUN_00469d10
return out_vec
```

## Signature (sealed)

```c
// cdecl; bare RET; returns out_vec*
void* Fs_ExpandPathSpecToStringVec_Inferred(
    void* out_string_vec,   // stack param_1
    const char* path_spec,  // stack param_2
    char recurse_dirs);     // stack param_3 (nonzero → deep leaf walk)
```

## Bytes win

| Site | Fact |
|---|---|
| Prologue | EDI ← stack arg2; `CMP byte [EDI], 0x40` (`@`) |
| Epilogue | `MOV EAX,EBP` (out); `ADD ESP,0x1B4`; `C3` |
| Join | `FUN_00416490(..., &DAT_00a2eb5c)` with mem `2f 00 00 00` |
| Leaf push | thiscall `FUN_004301f0` (vector ECX; string arg) — decompiler often hides ECX |

## Open questions

- Product/PDB English for this helper vs packer CLI entry.
- External invocation path (0 static xrefs beyond self).
- Exact product name of `FUN_009841d0` path-list reader (unowned).
- Runtime / bit-exact / differential.

## Pseudocode

See raw body (authoritative scaffold decompile) + clean `Fs_ExpandPathSpecToStringVec_Inferred.cpp`.
