# Raw capture: FUN_00989ef0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00989ef0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00989ef0` |
| **Canonical name** | `FUN_00989ef0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00989ef0(undefined4 param_1,undefined4 param_2)

{
  int unaff_EDI;
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  local_4 = 0xffffffff;
  puStack_8 = &LAB_009adcf3;
  local_c = ExceptionList;
  ExceptionList = &local_c;
  if (*(char *)(unaff_EDI + 0x80) != '\0') {
    ExceptionList = &local_c;
    EnterCriticalSection((LPCRITICAL_SECTION)(unaff_EDI + 0x68));
  }
  local_4 = 0;
  *(undefined4 *)(unaff_EDI + 0x10) = param_1;
  FUN_00996d70(param_2);
  if (*(char *)(unaff_EDI + 0x80) != '\0') {
    LeaveCriticalSection((LPCRITICAL_SECTION)(unaff_EDI + 0x68));
  }
  ExceptionList = local_c;
  return;
}
```

---

## W31-F re-verify (2026-07-29) — append only

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `analyze_function_complete` + `read_memory` (+ callers/callees) |
| **Body** | `0x00989ef0`–`0x00989f58` (**105 B** / `0x69`); pad `CC` after `RET 4` |
| **ABI** | **EDI=object** (ResourceCache family); **ECX=field10 value**; stack **1** formal (configure flag); **`RET 0x4`**; void |
| **Live decompile** | ≡ raw 2026-07-23 CF (optional CS, store `+0x10`, configure subobj `+0x14`, leave CS, SEH) |
| **Bytes delta vs decompile** | Decompiler labels `__thiscall` but **this is EDI not ECX**; `LEA EAX,[EDI+0x14]` before `FUN_00996d70` (EAX=subobject) sealed from bytes; `RET 4` sealed |
| **Classification** | **worker** — callees `EnterCriticalSection`, `LeaveCriticalSection`, `FUN_00996d70`; SEH `LAB_009adcf3` |
| **Xrefs (2)** | `0x007b7745` (`FUN_007b75b0` AssManager; `MOV EDI,0x00d1eac8`), `0x0099c22a` (`FUN_0099c1c0`; local stack object) |
| **Callers** | `FUN_007b75b0`, `FUN_0099c1c0` |
| **Name** | `ResourceCache_SetField10AndConfigure_Inferred` (Ghidra `FUN_00989ef0`; **Inferred** structural; same CS layout family as `ResourceCache_LookupByName` `00989e00`: CS `+0x68`, flag `+0x80`; global `0x00d1eac8`) |

### Full body hex (105 B)

```
6aff68f3dc9a0064a10000000050648925000000005180bf800000000053568d77688bd989742408740756ff15bc619c008b44241c50c7442418000000008d4714895f10e837ce0000807e1800740756ff15b8619c008b4c240c5e5b64890d0000000083c410c20400
```

### Sealed semantics

1. Install SEH frame (`LAB_009adcf3`, state −1).
2. If `*(char*)(obj+0x80) != 0`: `EnterCriticalSection(obj+0x68)`.
3. SEH state = 0; `*(obj+0x10) = ECX` (field10).
4. `FUN_00996d70(stack_arg)` with **EAX = obj+0x14** (subobject configure).
5. If `*(char*)(obj+0x80) != 0` (bytes: `[ESI+0x18]` with ESI=`obj+0x68`): `LeaveCriticalSection`.
6. Unlink SEH; `ADD ESP,0x10`; **`RET 4`**.

### Layout (object family)

| Off | Role |
|-----|------|
| `+0x10` | dword written from ECX (budget/size-like at AssManager: `0x10000`/`0x40000`) |
| `+0x14` | subobject base for `FUN_00996d70` |
| `+0x68` | `CRITICAL_SECTION` |
| `+0x80` | CS-enable flag (byte) |

### Call-site evidence

- AssManager `0x007b7730+`: mode bit2 → ECX=`0x40000` else `0x10000`; `PUSH 0`; `MOV EDI,0x00d1eac8`; CALL.
- `FUN_0099c1c0` `0x0099c210+`: `PUSH 1`; `MOV ECX,0x4000`; `LEA EDI,[esp+…]`; CALL.
