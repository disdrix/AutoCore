# Annotated low-level: FUN_004e12c0 → StdTree_IteratorIncrement_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004e12c0` |
| VA | `0x004e12c0`–`0x004e130b` exclusive (**75 B** / `0x4B`) |
| System | skills-abilities (partition) / shared `client::stdtree` (role) |
| Date | 2026-08-05 R13-003 OWN dual |
| Canonical | `StdTree_IteratorIncrement_Isnil21_Inferred` |

## Machine-level notes

- Source: raw capture + R13-003 re-verify (`decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + callers/xrefs + `get_assembly_context`).
- **No** `disassemble_bytes`. Prefer assembly when decompiler conflicts (none here — CF ≡ bytes).
- ABI: **ECX** = `node**` (iterator); void; plain **`RET`** (`C3`).
- Layout: left@+0, parent@+4, right@+8, isnil@**+0x21** (color@+0x20 unused in ++).
- Leaf — no callees. Eight erase/insert-hint isnil21 callers; parent dual is erase `0x00405650`.

## Pseudocode (annotated)

```c
// ECX = iterator (node**)
// In-place successor (operator++). Nil-at-current is a no-op (already end()).
void StdTree_IteratorIncrement_Isnil21_Inferred(int *it /*ECX*/)
{
  char cVar1;
  int iVar2;
  int *piVar3;
  int *piVar4;

  iVar2 = *it;                              // current node*
  if (*(char *)(iVar2 + 0x21) == '\0') {    // if !isnil
    piVar3 = *(int **)(iVar2 + 8);          // right
    if (*(char *)((int)piVar3 + 0x21) == '\0') {
      // successor = leftmost of right subtree
      cVar1 = *(char *)(*piVar3 + 0x21);
      piVar4 = (int *)*piVar3;              // left of right
      while (cVar1 == '\0') {
        cVar1 = *(char *)(*piVar4 + 0x21);
        piVar3 = piVar4;
        piVar4 = (int *)*piVar4;
      }
      *it = (int)piVar3;
      return;
    }
    // right is nil → climb parents while was right-child
    iVar2 = *(int *)(iVar2 + 4);            // parent
    cVar1 = *(char *)(iVar2 + 0x21);
    while ((cVar1 == '\0' && (*it == *(int *)(iVar2 + 8)))) {
      *it = iVar2;
      iVar2 = *(int *)(iVar2 + 4);
      cVar1 = *(char *)(iVar2 + 0x21);
    }
    *it = iVar2;                            // may land on nil head (end)
  }
  return;
}
```

## Open questions

- Product/PDB English for map value_type per caller (shared helper; 8 sites).
- Color bit @+0x20 unused here (not part of successor).
- Runtime / bit-exact / differential (OPEN — not Runtime Confirmed).
