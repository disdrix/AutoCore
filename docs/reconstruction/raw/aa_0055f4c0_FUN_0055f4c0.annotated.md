# Annotated low-level: LocalWorklist_Release_Inferred (FUN_0055f4c0)

| Field | Value |
|---|---|
| Stable ID | `aa_0055f4c0` |
| VA | `0x0055f4c0`–`0x0055f51b` (**92 B**) |
| Ghidra name | `FUN_0055f4c0` |
| Inferred name | `LocalWorklist_Release_Inferred` |
| System | allocator / local worklist teardown |
| Date | 2026-07-29 (W27-N dual seal) |
| Tools | `decompile_function` + `read_memory` + callers/xrefs; **no** `disassemble_bytes` |

## Machine-level notes

- **ABI:** ECX = descriptor; `ret` (no stack formals); void.
- **Global:** `DAT_00b05060` — allocator object (`int*` / vtable at `*[0]`).
- **Slab path:** if aligned size==0 OR (slab ptr ≠ arena-current AND arena-current ≠ 0) → bump counters `[2]-=size`, `[3]+=size`; else vcall `vtbl+0x28(slab,size)`.
- **Tag free:** if `(int)desc[+8] >= 0` → vcall `vtbl+0x14(ptr, (count&0x7fffffff)*8, 0x12)`.
- Decompiler drops `0x7fffffff` mask; machine has it — keep in clean.

## Signature

```c
// __fastcall/thiscall on desc; ret 0; void
void LocalWorklist_Release_Inferred(uint32_t *desc /*ECX*/);
```

## Descriptor

| Off | Field |
|---|---|
| +0x00 | free ptr (`*desc`) |
| +0x08 | signed count / flag (skip if <0) |
| +0x0c | slab cookie vs `DAT_00b05060[5]` |
| +0x10 | unit count for 16-byte-aligned byte size |

## Pseudocode (annotated, machine-exact tag free)

```c
void __fastcall FUN_0055f4c0(uint32_t *param_1)
{
  int *arena = DAT_00b05060;
  uint size = (param_1[4] * 8 + 0xfU) & 0xfffffff0;
  if (size == 0 || (param_1[3] != (uint)arena[5] && arena[5] != 0)) {
    arena[2] = arena[2] - size;
    arena[3] = arena[3] + size;
  } else {
    (**(code **)(*arena + 0x28))(param_1[3], size);
  }
  if ((int)param_1[2] >= 0) {
    uint nbytes = (param_1[2] & 0x7fffffff) * 8;  // machine AND — not bare *8
    (**(code **)(*DAT_00b05060 + 0x14))(*param_1, nbytes, 0x12);
  }
}
```

## Open questions

- Product name for `DAT_00b05060` allocator class.
- Full worklist descriptor fields beyond +0/+8/+c/+10 (stack layout in bind/unbind).
- Runtime / bit-exact — open.
