# Raw capture: FUN_007a4170

| Field | Value |
|---|---|
| **Stable ID** | `aa_007a4170` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x007a4170` |
| **Canonical name** | `FUN_007a4170` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
float10 __thiscall FUN_007a4170(int param_1,uint param_2)

{
  ushort uVar1;
  
  if ((int)param_2 < 0) {
    if (0xfffff < *(int *)(param_1 + 0xc)) {
      *(undefined4 *)(param_1 + 0xc) = 0;
    }
    uVar1 = *(ushort *)(*(int *)(param_1 + 8) + *(int *)(param_1 + 0xc) * 2);
    *(int *)(param_1 + 0xc) = *(int *)(param_1 + 0xc) + 1;
    return (float10)uVar1 * (float10)DAT_00aaa638;
  }
  param_2 = param_2 & 0x800fffff;
  if ((int)param_2 < 0) {
    param_2 = (param_2 - 1 | 0xfff00000) + 1;
  }
  return (float10)*(ushort *)(*(int *)(param_1 + 8) + param_2 * 2) * (float10)DAT_00aaa638;
}
```

---

## Live re-verify append — 2026-08-04 WQ9D-F

| Field | Value |
|---|---|
| Tool | `decompile_function` / `batch_decompile` + `read_memory` + `analyze_function_complete` |
| Program | `autoassault.exe` |
| Body | `0x007a4170`–`0x007a41d4` exclusive (**0x64 B**); pad `CC` after |
| Live decompile | **≡** raw CF |
| Epilogue | dual `C2 04 00` (`ret 4`) — both arms |

### Byte seal

```
007a4170  8B 44 24 04     mov eax,[esp+4]   ; index
          85 C0           test eax,eax
          7D 34           jge  indexed_path
          81 79 0C 00 00 10 00  cmp [ecx+0xC],0x100000
          7C 07           jl   no_wrap
          C7 41 0C 00 00 00 00  mov [ecx+0xC],0
          8B 51 0C        mov edx,[ecx+0xC]
          8B 41 08        mov eax,[ecx+8]
          66 8B 04 50     mov ax,[eax+edx*2]
          83 C2 01        add edx,1
          89 51 0C        mov [ecx+0xC],edx
          0F B7 D0        movzx edx,ax
          … fild; fmul [DAT_00aaa638]; ret 4

indexed:  25 FF FF 0F 80  and eax,0x800FFFFF
          79 07           jns  load
          48              dec eax
          0D 00 00 F0 FF  or eax,0xFFF00000
          40              inc eax
          8B 49 08        mov ecx,[ecx+8]
          66 8B 04 41     mov ax,[ecx+eax*2]
          … fild; fmul [DAT_00aaa638]; ret 4
```

### Constants

| Symbol | VA | Bytes LE | Value |
|---|---|---|---|
| `DAT_00aaa638` | `0x00aaa638` | `80 00 80 37` | float `0x37800080` ≈ **1/65535** (1.525902e-05) |

### Host layout (shared with `Rng_CopyUnitTableWindow_Inferred`)

| Off | Role |
|---|---|
| `+0x08` | table base (`ushort*`, 0x100000 entries / 0x200000 bytes) |
| `+0x0C` | cursor (ushort index) |

### Callers

| Site | Context |
|---|---|
| `0x00508a31` in `FUN_00508a00` | `RandomUnitScalar` → `mov ecx,eax` → this unit (index stack arg) |
| `0x0057ed44` | second UNCONDITIONAL_CALL (same pattern) |
