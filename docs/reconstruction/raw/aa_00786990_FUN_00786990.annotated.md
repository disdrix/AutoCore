# Annotated low-level: Profiler_Leave (`FUN_00786990`)

| Field | Value |
|---|---|
| Stable ID | `aa_00786990` |
| VA | `0x00786990` |
| System | client instrumentation / profiler scope |
| Date | 2026-07-29 (strengthen; was 2026-07-23 scaffold) |
| Dual | `reviews/A_aa_00786990_Profiler_Leave.md`, `reviews/B_aa_00786990_Profiler_Leave.md` |

---

## Machine-level notes

- **Role:** Pop one nested QPC profiler scope on global manager `DAT_00afa27c`.
- **Pair:** Enter = `FUN_00786a00` / `Profiler_Enter` (`RET 4`, name string stack formal).
- **Not mission logic** — only bookends `CVOGCharacter_SearchAutoMissions` and many other named scopes.
- Assembly is authoritative for ESI setup into `FUN_00780690` (decompiler shows `unaff_ESI` on that callee).

### ABI

```
ECX = Profiler* (manager)
no stack args
RET
```

### Offsets (this unit)

| Object | Offset | Meaning |
|---|---|---|
| Manager | `+0x175` | enable `char` (0 = no-op) |
| Manager | `+0x4c` | thread-slot vector base |
| Slot | `+0x00` | thread id |
| Slot | `+0x08` | current node\* |
| Slot | stride `0xC` | |
| Node | `+0x04` | timer block\* |
| Node | `+0x08` | parent node\* |
| Node | `+0x28` | hierarchical total (`+=` scaled) |
| Timer | `+0x08/+0x0c` | accum ticks |
| Timer | `+0x10/+0x14` | divisor |
| Timer | rest | see `FUN_00780690` |

---

## Pseudocode (annotated; CF ≡ raw + live asm)

```c
// Probable name: Profiler_Leave
// ECX = *DAT_00afa27c
void __fastcall Profiler_Leave(int profiler /*this*/)
{
  int idx;
  int *node;       // EDI
  int *timer;      // ESI for FUN_00780690
  int scaled;
  int parent;

  if (*(char *)(profiler + 0x175) == '\0') {
    return;
  }

  // Ensure TLS row; return index into 0xC-stride array at profiler+0x4c
  idx = FUN_007866e0(profiler);
  node = *(int **)(*(int *)(profiler + 0x4c) + 8 + idx * 0xc);

  timer = *(int **)(node + 1);          // node+0x4 — dword index form
  // ASM: MOV ESI, [EDI+4]; CALL FUN_00780690
  FUN_00780690();                       // ESI=timer: QPC stop, accum/last/max/avg/count

  timer = *(int **)(node + 1);
  // scaled = 64-bit accum / 64-bit divisor  (__alldiv @ 0x006a3f00)
  scaled = __alldiv(
      *(unsigned *)((char *)timer + 0x8),
      *(unsigned *)((char *)timer + 0xc),
      *(unsigned *)((char *)timer + 0x10),
      *(unsigned *)((char *)timer + 0x14));
  *(int *)((char *)node + 0x28) += scaled;

  parent = *(int *)((char *)node + 0x8);
  if (parent != 0) {
    idx = FUN_007866e0(profiler);
    *(int *)(*(int *)(profiler + 0x4c) + idx * 0xc + 8) = parent;
  }
  return;
}
```

---

## Call graph (framing)

| Direction | VA | Note |
|---|---|---|
| Callee | `0x007866e0` | thread-slot index (×2) |
| Callee | `0x00780690` | timer accumulate (ESI) |
| Callee | `0x006a3f00` | `__alldiv` |
| Caller | `0x00532b60` SearchAutoMissions | 2 sites, `ECX=[DAT_00afa27c]` |
| Caller | `0x0051b350`, `0x00520dc0`, … | other named scopes |
| Thunk | `0x00480160` | `MOV ECX,[DAT]; JMP Leave` |

---

## Open questions

- Product class name for manager / node.
- Divisor source (`QueryPerformanceFrequency` vs fixed).
- Unit of hierarchical total at `node+0x28`.
