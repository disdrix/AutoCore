# Annotated low-level: FUN_005405e0

| Field | Value |
|---|---|
| Stable ID | `aa_005405e0` |
| VA | `0x005405e0` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_005405e0`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
uint FUN_005405e0(int param_1)

{
  int *piVar1;
  
  piVar1 = DAT_00b04718;
  while( true ) {
    if (piVar1 == DAT_00b0471c) {
      return (uint)piVar1 & 0xffffff00;
    }
    if (*piVar1 == param_1) break;
    piVar1 = piVar1 + 1;
  }
  return CONCAT31((int3)((uint)piVar1 >> 8),1);
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.

---

## Dual residual notes (2026-07-29)

**Sealed (A/B `A_aa_005405e0_*` / `B_aa_005405e0_*`):**

- ABI: **`__cdecl(eventId) → bool AL`**, bare **`RET`**; sole site `ADD ESP,4`
- Linear scan `[DAT_00b04718, DAT_00b0471c)`, stride 4
- Found → `MOV AL,1`; miss/empty → `XOR AL,AL` (decompiler `CONCAT31` is noise)
- **Sole** xref: `CheckMissionRequirements` @ `0x00546394`
- Arg = mission **`+0x118` RequirementEventId**; caller skips when **`-1`**
- Live decompile ≡ raw; product English + vector writers open
