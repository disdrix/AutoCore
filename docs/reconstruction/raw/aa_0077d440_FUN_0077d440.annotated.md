# Annotated low-level: FUN_0077d440 / BitStream_HuffmanAssignCodes

| Field | Value |
|---|---|
| Stable ID | `aa_0077d440` |
| VA | `0x0077d440`–`0x0077d514` (**212 B**) |
| System | net / TNL BitStream Huffman (code assignment) |
| Date | 2026-07-29 W26-C |
| Named alias | `BitStream_HuffmanAssignCodes` (inferred from exclusive parent + alphabet writes) |

## Machine-level notes

- **ABI:** **cdecl**; 3 stack formals; plain **`RET`**.
  - `param_1` — scratch BitStream-shaped object (`&local_138` from parent).
  - `param_2` — node index: **≥0** = internal tree index; **<0** = leaf encoded as `-1 - leafIndex`.
  - `param_3` — depth (bit length so far).
- **Leaf path (`param_2 < 0`):**
  - Alphabet row: `DAT_00d1eabc + (param_2 * 3 + 3) * -4` ≡ base + `(-1 - param_2) * 0xC`.
  - `alphabet[+8] = **(ctx+0xc)` — current code dword (bits walked so far).
  - `alphabet[+4] = (uint8_t)param_3` — bit length.
  - Return (no further recursion).
- **Internal path:**
  - Node at `DAT_00d1eab0 + param_2 * 8` (stride **8**).
  - Snapshot `bitPos = *(ctx+0x18)`.
  - **Left edge (bit 0):** if need grow (`*(ctx+0x30) < bitPos+1`), call `FUN_0042b450` (BitStream expand, 1 bit); on success/ok write clear bit at bitPos and `bitPos++`. Recurse `left = *(short*)(node+4)`, depth+1. Restore bitPos.
  - **Right edge (bit 1):** same expand gate; write **set** bit; recurse `right = *(short*)(node+6)`, depth+1; restore bitPos.
- **Parent:** sole external caller `BitStream_HuffmanBuildTables` with `(scratch, 0, 0)` after tree merge.
- **Callees:** `FUN_0042b450` (buffer expand), self (2 recursive calls).
- Leaf encoding matches builder: leaves stored as negative shorts `-1 - leafIdx` (W25-T sealed).

## Pseudocode (annotated)

```c
// cdecl; void
void FUN_0077d440(BitStreamLike* ctx, int nodeOrLeaf, int depth)
{
  if (nodeOrLeaf < 0) {
    // leafIdx = -1 - nodeOrLeaf; row = alphabet + leafIdx * 0xC
    uint8_t* row = (uint8_t*)(DAT_00d1eabc + (nodeOrLeaf * 3 + 3) * -4);
    *(uint32_t*)(row + 8) = **(uint32_t**)(ctx + 0xc);  // code bits
    *(uint8_t*)(row + 4) = (uint8_t)depth;               // bitlen
    return;
  }

  int savedBitPos = *(int*)(ctx + 0x18);
  int node = DAT_00d1eab0 + nodeOrLeaf * 8;

  // write bit 0 (left), recurse left short@+4
  maybe_expand_and_clear_bit(ctx);
  FUN_0077d440(ctx, (int)*(short*)(node + 4), depth + 1);
  *(int*)(ctx + 0x18) = savedBitPos;

  // write bit 1 (right), recurse right short@+6
  maybe_expand_and_set_bit(ctx);
  FUN_0077d440(ctx, (int)*(short*)(node + 6), depth + 1);
  *(int*)(ctx + 0x18) = savedBitPos;
}
```

## Open questions

- Product English inside TNL (Huffman table build helper name).
- Runtime bit-exact alphabet dump after walk vs retail process memory.
- Expand-fail path (FUN_0042b450 returns 0): bit write skipped — only relevant if scratch capacity exceeded; parent seeds cap 0x20 bits.
