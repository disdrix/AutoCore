# Annotated low-level: Item_CharacterMeetsUseReqs_Inferred (`aa_00514390`)

| Field | Value |
|---|---|
| Stable ID | `aa_00514390` |
| VA | `0x00514390` |
| Body | `0x00514390`–`0x005145a3` (532 bytes / 0x214) |
| Canonical name | `Item_CharacterMeetsUseReqs_Inferred` |
| Ghidra name | `FUN_00514390` |
| System | inventory / equip / skill-use gates |
| Date | `2026-07-29` (dual A/B seal W20-O; prior scaffold 2026-07-23) |
| Tools | `decompile_function` + `read_memory` only |

---

## Machine-level notes

- **ABI:** `__thiscall` — **ECX = item**, stack `[esp+4]=character`, `[esp+8]=flag` (low byte tested), returns bool in **AL**, epilogue **`ret 8`**.
- **Role:** gate whether a **character meets item use/equip requirements**.
- **Early accept (return 1):**
  1. `*(int*)(character + 0x6b4) > 0` — staff/GM-style override (same offset pattern as other client bypasses).
  2. OR item clone type `*(*(item+0xa8)+0x38) == 0x1a`.
- **Race arm:** clonebase vtbl `+0x14` → id; accept if `-1` **or** equals character race byte at MI-adjusted `+0x532`.
- **Faction/req arm:** `*(u32*)(clonebase+0x3e0) == 0xFFFFFFFF` **or** equals character byte `+0x531`.
- **Level:** `req = min( (i16)item+0xe8 + (i16)item+0xea , 0x50 )`; need `req <= character.vtbl+0x27c()`.
- **Stat floors (item shorts vs character getters):**
  - `+0xec` ≤ `Object_GetTargetAccuracyBase` (`FUN_004c4070`)
  - `+0xee` ≤ `Creature_GetPerception` (`FUN_004c41c0`)
  - `+0xf2` ≤ `Creature_GetTheory` (`FUN_004c4140`)
  - `+0xf0` ≤ `Character_GetTechForPoolCalcs(character)`
- **Powerplant optional:** if `item+0x17c` bit 19 set and clone `+0x4ac >= 0`, require `FUN_0052ada0(*(clone+0x4ac)) >= *(clone+0x4b0)`.
- **Special arm (only when `flag != 0`):** if clone `+0x3f2 & 0x80` and character type flags `+0xf5` or `+0xf6` nonzero → iterate `FUN_00411900` looking for entry `+0x62c == 0x26`; clear `*(item+0x70)+0x1d`; fail if type `0x26` absent.
- **Return:** 1 if all gates pass; else 0. Special arm can force 0 even after stat pass.

## Byte-accurate control flow

```c
/* Item_CharacterMeetsUseReqs_Inferred
   char __thiscall (Item *item, Character *ch, int flag);  // RET 8
*/

char __thiscall Item_CharacterMeetsUseReqs_Inferred(Item *item, Character *ch, int flag)
{
  if (*(int *)((char *)ch + 0x6b4) > 0) return 1;
  if (*(int *)(*(int *)((char *)item + 0xa8) + 0x38) == 0x1a) return 1;

  void **clone = *(void ***)(*(int *)((char *)item + 0xa8) + 0x3c);
  int raceId = ((int (__thiscall *)(void *))clone[0][0x14/4])(clone[0]); /* decomp form */
  /* Ghidra: (**(code**)(*clone + 0x14))() with this=clone from item template */

  /* race + faction gates (see raw); then level clamp 0x50; four stat floors;
     optional powerplant; optional special-mode type-0x26 presence when flag!=0 */

  /* success */ return 1;
  /* fail    */ return 0;
}
```

Full CF is in raw / clean — annotated summary above is the sealed contract.

## Ghidra raw (authoritative)

See `aa_00514390_FUN_00514390.md` authoritative block (unchanged; W20-O re-verify append matches live decompile).

## Callers (context)

- `Character_TryEquipItem` (`0x004fabc0`)
- `CVOGCharacter_SetLevelWithFlags` (`0x00533220`) — multiple equip slots, flag 1
- `FUN_0088d980`, `FUN_0092fb30`, `FUN_00941d50`, UI sites `0x00828ad6` / `0x00828eb6`

## Open questions

- Product/PDB English name; English for item shorts `+0xe8..+0xf2`.
- Exact identity of type `0x1a` and iterator type `0x26`.
- Whether `+0x6b4` is strictly GM or also a temporary cheat/dev counter.
- Runtime / differential golden vectors — open.
