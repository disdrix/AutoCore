# Annotated low-level: FUN_004d2e50 → CVOGObject_EvalChildActiveCompleteMissionInteract_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004d2e50` |
| VA | `0x004d2e50`–`0x004d319e` (**847 B** / `0x34F`) |
| Ghidra name | `FUN_004d2e50` |
| Canonical (inferred) | `CVOGObject_EvalChildActiveCompleteMissionInteract_Inferred` |
| System | interaction-activation (partition); missions interact glue |
| Date | 2026-08-05 (MEGA-068 OWN dual refresh) |
| Parent | `CVOGObject_EvalInteractStateFromChildren` `0x004d7640` (type-`0x25` branch) |

## Machine-level notes

- Source: raw `aa_004d2e50` + live Ghidra re-verify (MEGA-068). Prefer assembly when decompiler conflicts (`unaff_EDI`, void signature).
- **ABI sealed:** stack child + character; **`RET 8`** ×3. Parent `MOV ECX,EDI` is unused by body.
- **Returns sealed:** `0`, `3`, `6`, `7` only (AL). `6/7` = `6 + (mission[+0x169] != 0)` same plate as GiveMission branch of parent.
- **Strings:** TraversalLock / TraverseToNext / `VOG_DEBUG_STOP` are hash-lock diagnostics — **not** the function name.
- Retire string-seed aliases `Named_VOG_DEBUG_STOP_004d2e50` / `Mission_VOG_DEBUG_STOP_004d2e50`.

## Signature (sealed)

```c
// __stdcall RET 8; AL = interact state code
// param_1 = child reaction object (parent ESI)
// param_2 = character (parent EBP)
char __stdcall CVOGObject_EvalChildActiveCompleteMissionInteract_Inferred(
    void *child_reaction,
    void *character);
```

| Slot | Asm | Conf |
|---|---|---|
| child_reaction | `[ESP+4]` → EDX | **High** |
| character | `[ESP+8]` → EBP | **High** |
| cleanup | `RET 8` @ three exits | **High** |
| return AL | `0` / `3` / `6` / `7` | **High** |

## Control-flow map

```text
flag = false
if child.missionIdVec[+0x244,+0x248) non-empty:
  for each missionId in vec:
    host = EnsureRegionMissions(DAT_00b041fc)   // FUN_0053fff0
    active = HashLookup(host, missionId)
    if active && active.short[+0xac] == -1:
      if CheckMissionRequirements(active, character)==0:
        goto RET_6_7(active)
    else if HasCompletedMission(character, missionId):
      completed = HashLookup(character.hash[+0x538], missionId)  // param_2[0x14e]
      if completed.short[+0xac] != 0 && CheckReq(completed, character)==0:
        flag = true
      // else continue
    else:
      // incomplete non-active: try CheckReq(active) → RET_6_7 if ok
  return flag ? 3 : 0

if child.byteFilterVec[+0x234,+0x238) non-empty:
  TraversalLock(character.hash[+0x540])         // +0x1d
  walk up to 8 heads with short[+0xac] chain
  if chain ends:
    unlock; TraversalLock; TraverseToNext loop:
      mission = node.payload
      if mission.byte[+0x86] in byteFilterVec:
        if CheckReq(mission, character)==0:
          if !HasCompletedMission(character, *mission):
            unlock; FUN_00547920(0); return 6/7
          else flag = true
    unlock; goto return flag?3:0 path
  unlock
return 0
```

## Key offsets

| Object | Offset | Use |
|---|---|---|
| child | `+0x244` / `+0x248` | `std::vector<uint>` mission IDs (begin/end) |
| child | `+0x234` / `+0x238` | `std::vector<uint>` byte filters (compared to mission `+0x86`) |
| mission | `+0xac` | `short` status (`-1` = active-tracked; `0` special) |
| mission | `+0x86` | `byte` filter key for second path |
| mission | `+0x169` | `char` → adds 1 to base return 6 (→7) |
| character | `+0x538` (`[0x14e]`) | completed-mission hash host |
| character | `+0x540` (`[0x150]`) | traversable mission hash (`lock@+0x1d`, list `@+0x14`) |
| hash node | `+0x8` payload, `+0xc` next, `+0x10` key | CNDHash chain |

## Pseudocode (annotated; ≡ raw)

```c
char FUN_004d2e50(int param_1 /*child*/, int *param_2 /*character*/)
{
  /* full body identical to raw aa_004d2e50_FUN_004d2e50.md —
     see reconstructed-exact/CVOGObject_EvalChildActiveCompleteMissionInteract_Inferred.cpp */
  ...
}
```

Authoritative listing remains in raw capture; clean source is the readable twin.

## Callers / callees

| Direction | Targets |
|---|---|
| Callers (1) | `CVOGObject_EvalInteractStateFromChildren` @ `0x004d7640` (×2 call sites) |
| Callees | `FUN_0053fff0`, `CVOGCharacter_HasCompletedMission`, `CVOGCharacter_CheckMissionRequirements`, `FUN_00547920`, `FUN_00402c40`, `FUN_007a4480` |

## Open questions

1. Product English for return code **3** (inferred: “completed-mission residual interact” / mid-priority).
2. Child vector field names in PDB / plate for `+0x244` vs `+0x234`.
3. Full role of `FUN_00547920` before 6/7 return.
4. Runtime icon mapping for 3/6/7 under type-`0x25` children.
