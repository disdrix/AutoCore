# Annotated low-level: CNDHash_TraverseToNext_RegEdiEsi (`FUN_00411e40`)

| Field | Value |
|---|---|
| Stable ID | `aa_00411e40` |
| VA | `0x00411e40`–`0x00411e80` inclusive (**65 B** / `0x41`) |
| System | interaction-activation / CNDHash container |
| Date | 2026-08-05 (MEGA-114 OWN dual refresh) |
| Canonical name | `CNDHash_TraverseToNext_RegEdiEsi` |
| Ghidra name | `FUN_00411e40` |
| Twin (thiscall) | dualed `CNDHash_TraverseToNext` `0x00411900` |

---

## Machine-level notes

- Source: raw capture + MEGA-114 live re-verify (`decompile` ≡ `force_decompile`; `disassemble_function` + `read_memory`).
- Decompiler shows `unaff_EDI` / `unaff_ESI` — **register ABI**, not missing stack params.
- **EDI** = CNDHash\* (hash object).
- **ESI** = `int*` / `node**` traversal cursor (updated in place).
- **EAX** return = payload at **node+8**, or **0** if exhausted.
- Bare **`RET`** — zero stack args (contrast twin `RET 4`).
- Lock byte **hash+0x1d**: if 0, log `"HashError:TraverseToNext, not locked for traversal"` + `"VOG_DEBUG_STOP"` then **continue** (non-fatal).
- List head **hash+0x14**; list-next **node+0x14**; value/payload **node+8** (0x1c-class node family; not inventory 0x28).
- Does **not** set/clear lock, freelist, or membership — pure ordered-list iterator step.
- Parent `Client_UpdateNpcInteractIcons` (`0x0091b8d0`): hash at client `+0x6f0`; pairs with reg lock helper `FUN_00411e10` (ESI=hash, sets +0x1d).

---

## Pseudocode (ABI-recovered)

```c
// Register convention: EDI = CNDHash* hash, ESI = node** cursor
// Returns payload void* at node+8, or NULL. Bare RET.
undefined4 CNDHash_TraverseToNext_RegEdiEsi(void)
{
  int node; /* EAX */

  if (*(char *)(EDI + 0x1d) == '\0') {
    FUN_007a4480(0, "HashError:TraverseToNext, not locked for traversal");
    FUN_007a4480(0, "VOG_DEBUG_STOP");
    /* fall-through — does not return early */
  }

  if (*ESI == 0) {
    *ESI = *(int *)(EDI + 0x14);   /* seed list head */
    node = *(int *)(EDI + 0x14);
  } else {
    node = *(int *)(*ESI + 0x14);  /* advance list-next */
    *ESI = node;
  }

  if (node != 0) {
    return *(undefined4 *)(node + 8);  /* payload */
  }
  return 0;
}
```

---

## Callers (live)

| Caller | Role (context) |
|---|---|
| `Client_UpdateNpcInteractIcons` `0x0091b8d0` | partition parent; walk interact-candidate hash @ `+0x6f0` |
| `FUN_0078dac0`, `FUN_007ff3d0`, `FUN_00812de0`, `FUN_008153b0`, `FUN_0083c110` | other CNDHash ordered walks (same reg ABI) |

11 UNCONDITIONAL_CALL xrefs (incl. two unlabeled sites `0078a39e` / `0078a422`).

---

## Open questions

- Exact product value_type of payload @ node+8 for each host hash (interact path uses type@+4 + TFID@+8/+0xc).
- Product English for host hashes beyond client+0x6f0.
- Runtime / bit-exact / differential (not claimed).
- Whether unlabeled call sites belong to a stripped/inlined parent function.
