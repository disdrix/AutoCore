# Raw capture: FUN_00968680

| Field | Value |
|---|---|
| **Stable ID** | `aa_00968680` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00968680` |
| **Canonical name** | `FUN_00968680` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00968680(void)

{
  undefined4 *puVar1;
  undefined4 *puVar2;
  
  puVar2 = DAT_00d1ee30;
  for (puVar1 = (undefined4 *)*DAT_00d1ee30; puVar1 != puVar2; puVar1 = (undefined4 *)*puVar1) {
    if (puVar1[2] != 0) {
      *(undefined4 *)(puVar1[2] + 0x184) = 0;
    }
  }
  return;
}
```

---

## W38-Z re-verify append (2026-08-04)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `batch_decompile` + `read_memory` + `analyze_function_complete` + `get_assembly_context` + parent `FUN_0075eff0` + list twin `FUN_00968a50` (W37-I) |
| **Integrity** | Original raw body above preserved; this section is append-only |

### Live decompile

Matches original raw (circular walk of `DAT_00d1ee30`; zero payload`+0x184`).

### Body bytes (40 B @ `0x00968680`–`0x009686a7` inclusive)

```
8b0d30eed1008b018bd13bc274198bff8b480885c9740ac78184010000000000008b003bc275e9c3
```

### Machine notes

- `8b 0d 30 ee d1 00` → sentinel `DAT_00d1ee30` (global `std::list` head for **gfxBufferedView**; ctor `FUN_00968a50` / W37-I registers `this`).
- Node: `next@+0`, payload*`@+8`; walk until node == sentinel.
- `c7 81 84 01 00 00 00 00 00 00` → `*(payload+0x184) = 0` when payload non-null.
- Plain `c3` RET; no stack formals; no callees.
- Pad `CC` then sibling list walk `FUN_009686e0` @ `0x009686b0` (pre-Reset lost-path; not OWN).
- Sole caller: `FUN_0075eff0` Device Reset success arm (`gfxDevice.cpp`) after all factory resets — call site `0x0075f362`.
- Role sealed: `GfxBufferedViewList_ClearField184_Inferred` (W38-Z dual A+B **accept-with-gaps**).
