# Annotated low-level: StdTree_InsertAndRebalance_Isnil31_Inferred (FUN_00403250)

| Field | Value |
|---|---|
| Stable ID | `aa_00403250` |
| VA | `0x00403250`–`0x004033d1` inclusive (**386 B** / `0x182`) |
| Ghidra | `FUN_00403250` |
| Canonical | `StdTree_InsertAndRebalance_Isnil31_Inferred` |
| System | missions-progression (chain) / STL map-set isnil@+0x31 |
| Date | 2026-08-05 (MEGA-047 dual) |

## Machine-level notes

- Source: raw capture + MEGA-047 live re-verify (`decompile` ≡ raw; ABI from `disassemble_function` + call-site context).
- Prefer assembly for ABI: **EDI=map**, **ECX=where**, stack 3 args, **`RET 0xC`** — decompiler `unaff_EDI` / fake thiscall is incomplete.
- Color@**+0x30**, isnil@**+0x31**, node **0x38**, value **0x20** @+0x10.
- Family rotates: dualed `StdTree_Lrotate_Isnil31_Inferred` (`00403e90`); Rrotate twin `00403ee0`.
- Parent insert-or-find: `FUN_00402b30` (key uint @+0x10). Buynode: `FUN_00403f30` → `operator_new(0x38)` + `FUN_00404360`.
- Retire scaffold `Named_CalleeOf_Named_CalleeOf_Client_NotifyActiveMissionChanged_00403250`.

## Signature (sealed)

```c
// EDI = map*; ECX = where*; stack (out**, addLeft, value*); RET 0xC; EAX = out
void StdTree_InsertAndRebalance_Isnil31_Inferred(
    /*EDI*/ MapShell* map,
    /*ECX*/ Node* where,
    Node** out,
    char addLeft,
    const void* value);
```

## Pseudocode (annotated ≡ raw CF)

```c
// SEH frame LAB_009bc919
void InsertAndRebalance_Isnil31(MapShell* map /*EDI*/, Node* where /*ECX*/,
                                Node** out, char addLeft, const void* value)
{
  if (map->size >= 0x7fffffeu) {
    // basic_string("map/set<T> too long")
    // FUN_00401a40 length_error ctor
    // _CxxThrowException(..., DAT_00acc388)  // noreturn
  }

  Node* head = map->head;  // [EDI+4]
  // buynode: left=head, parent=where, right=head, value, color=red(0)
  Node* n = FUN_00403f30(head, where, head, value, /*red*/ 0);
  map->size += 1;

  if (where == head) {
    head->parent = n;   // root
    head->left = n;     // Lmost
    head->right = n;    // Rmost
  } else if (addLeft == 0) {
    where->right = n;
    if (where == head->right)
      head->right = n;
  } else {
    where->left = n;
    if (where == head->left)
      head->left = n;
  }

  // RB: while parent is red (color@+0x30 == 0)
  Node* x = n;
  while (*(char*)(x->parent + 0x30) == 0) {
    Node* p = x->parent;
    Node* g = p->parent;
    if (p == g->left) {
      Node* uncle = g->right;
      if (uncle->color /*+0x30*/ == 0) {
        // recolor: parent/uncle black, grandparent red; climb
        p->color = 1; uncle->color = 1; g->color = 0;
        x = g;
      } else {
        if (x == p->right) {
          FUN_00403e90(/*ECX*/ p, /*stack*/ map);  // Lrotate
          x = p; p = x->parent;
        }
        p->color = 1; p->parent->color = 0;
        FUN_00403ee0(/*ECX*/ p->parent, map);      // Rrotate
      }
    } else {
      Node* uncle = g->left;
      if (uncle->color == 0) {
        p->color = 1; uncle->color = 1; g->color = 0;
        x = g;
      } else {
        if (x == p->left) {
          FUN_00403ee0(/*ECX*/ p, map);            // Rrotate
          x = p; p = x->parent;
        }
        p->color = 1; p->parent->color = 0;
        FUN_00403e90(/*ECX*/ p->parent, map);      // Lrotate
      }
    }
  }
  map->head->parent->color = 1;  // root black
  *out = n;
  return; // RET 0xC; EAX = out
}
```

## Open questions

- Product/PDB map instantiation English (host type under NotifyActiveMissionChanged chain).
- Value payload English (0x20 @+0x10; parent compares first u32 as key).
- Full dual of Rrotate twin `00403ee0`, buynode `00403f30`, parent `00402b30` (OWN residuals / peer MEGAs).
- Runtime / bit-exact / differential.
