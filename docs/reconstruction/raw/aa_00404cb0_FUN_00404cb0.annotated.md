# Annotated low-level: FUN_00404cb0 / CVOGPhysics_ApplyVector_Vtbl5c_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00404cb0` |
| VA | `0x00404cb0`–`0x00404ce1` |
| System | physics / CVOGPhysics |
| Date | 2026-07-29 (dual A/B strengthen; supersedes 2026-07-23 scaffold notes) |

## Machine-level notes

- **Twin** of `CVOGPhysics_ApplyImpulseVector` (`0x0040d260`): same gate + body pointer; vtbl slot **`+0x5c`** here vs **`+0x50`** there.
- Prefer assembly when decompiler presents `FUN_005070b0` as returning `char*` — bytes show out-param write + `EAX = out`.
- `param_2` is a **pointer** to a caller-built vector (typically 4×f32 at call sites), not a scalar float in a register.
- Product English for body vtbl `+0x5c` open (do not seal as linear/angular impulse without implementer dual).

## Pseudocode (annotated)

```c
// __thiscall: ECX = CVOGPhysics* this, stack = void* pVec, RET 4
void __thiscall CVOGPhysics_ApplyVector_Vtbl5c_Inferred(int this, undefined4 pVec)
{
  char outReady;          // decompiler: high byte of stack copy of this
  char *pReady;           // actually &outReady after FUN_005070b0 (EAX)

  // FUN_005070b0(this, &outReady):
  //   if (*(this+0x44)==0) *out=0;
  //   else *out = *(*(this+0x44)+0x29);
  pReady = (char *)FUN_005070b0(/*out*/ &stackThisByte);

  // Optional prepare — apply still runs afterward either way
  if ((*pReady == '\0') && (*(int *)(this + 0x44) != 0)) {
    FUN_005070d0();       // ECX=this; may activate/dirty host path
  }

  // body = *(this+0x3c);  no null check
  // thiscall body->vtbl[+0x5c](pVec)
  (**(code **)(**(int **)(this + 0x3c) + 0x5c))(pVec);
  return;
}
```

## Callers (xref list only)

| VA | Parent |
|---|---|
| `0x00922fd4` | `Client_Input_DriveControlTick` (brake-edge; vec Y=`DAT_00aaacc0`=550.f) |
| `0x0091d64c` | `FUN_0091d460` |
| `0x0091da00` | `FUN_0091d660` |
| `0x0095846c` | unnamed containing range |

## Open questions

- Body vtbl `+0x5c` product method name / Havok identity.
- `FUN_005070b0` / `FUN_005070d0` English (shared with ApplyImpulseVector).
- float3 vs float4 consumption inside implementer.
