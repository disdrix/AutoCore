# Annotated low-level: StdVector_BuyN_BasicString (`FUN_00431b30`)

| Field | Value |
|---|---|
| Stable ID | `aa_00431b30` |
| VA | `0x00431b30`–`0x00431b82` exclusive (**82 B**) |
| System | MSVC `vector<basic_string<char>>` capacity buy |
| Date | 2026-08-04 (W37-E OWN-ONLY) |
| Name | `StdVector_BuyN_BasicString` (**Inferred**) |

## Machine-level notes

- Source: raw capture + W37-E live `read_memory` full 82 B.
- Prefer assembly when decompiler conflicts (throw is **noreturn**; decomp assigns bogus return into `n`).
- **No free** of prior `begin` — triad zero only. Callers must pass empty/default vector or accept leak.
- Elem stride **0x1c** sealed by `n*0x1c` lea/sub/add chain and max `0x9249249` (`0xFFFFFFFF/0x1c` floored).
- Sibling tidy: `StringVec_Tidy` (`0x00431ae0`, W33-D). Sole caller: `StdVector_AssignFrom_BasicString` (`0x00469d10`, W34-S).

## Pseudocode (annotated)

```c
// ECX = VectorBasicString* this; stack n; RET 4; returns 0 (n==0) or 1 (bought)
uint8_t __thiscall StdVector_BuyN_BasicString(VectorBasicString* this, uint32_t n)
{
  this->begin = this->end = this->capEnd = nullptr;  // NO operator_delete
  if (n == 0)
    return 0;
  if (n > 0x9249249)
    FUN_004540b0();  // throws "vector<T> too long" — noreturn
  void* buf = operator_new(n * 0x1c);
  this->begin = this->end = (BasicString_0x1c*)buf;  // size == 0
  this->capEnd = (BasicString_0x1c*)((char*)buf + n * 0x1c);
  return 1;
}
```

## Open residual

- Product/MSVC demangle for exact helper (`_Buy` / `_Buy_nonzero` plate).
- Runtime / bit-exact / differential (no Launcher).
