# Annotated low-level: UI_InteractionMenu_AddFriendsToggle (`FUN_0082f9a0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0082f9a0` |
| VA | `0x0082f9a0`–`0x0082fa58` inclusive (size **185 B** / `0xB9`) |
| System | skills-abilities nested → interaction menu / social UI |
| Date | 2026-08-05 (MEGA-075 refresh) |
| Canonical name | `UI_InteractionMenu_AddFriendsToggle` |
| Ghidra name | `FUN_0082f9a0` |
| Prior alias | `UI_int_btn_generic_0082f9a0` (plate-only; retired) |

---

## Machine-level notes

- **Live decompile ≡ raw CF** (MEGA-075 2026-08-05 ≡ frozen 2026-07-23).
- Prefer assembly for ABI: decompiler shows `void` / `in_EAX` / `unaff_EDI` — both registers are real parent-passed operands.
- **EAX** = target object\* (entry `cmp [eax+0x6b4],1`).
- **EDI** = menu host\* (stores at `+0x558` / `+0x564`; `this` for `vtbl+0x444`).
- **ret 0** (`C3`); ESI saved across body for button temp.
- Direct callee: `FUN_00573a30` — friends-list linear name lookup (`_stricmp` on entry `+0x1a`).
- Parent: dualed `UI_InteractionMenu_PopulateTargetButtons_Inferred` (`FUN_0082fe20`) — two call sites, both pack orders.
- Sibling twin (same shape): `FUN_0082f8d0` Ignore toggle (ids `0x11179`/`0x1117c`).

## ABI

```c
// Parent-preserved registers (not MSVC thiscall):
//   EAX = target object*
//   EDI = interaction menu host*
//   ret 0
void UI_InteractionMenu_AddFriendsToggle(void /* regs */);
```

## Pseudocode (annotated)

```c
// aa_0082f9a0 @ 0x0082f9a0
// EAX = target; EDI = menu host
void UI_InteractionMenu_AddFriendsToggle(void)
{
  // Gate: same +0x6B4 numeric family as convoy pack (0082fb30).
  // Enter if target.field < 1 OR local.field >= 1.
  if (!(*(int *)(target + 0x6b4) < 1 ||
        *(int *)(DAT_00d1b6d8 + 0x6b4) >= 1)) {
    return;
  }

  // DAT_00d1da2c = friends-list / social-list host (vector of entries).
  if (DAT_00d1da2c != 0) {
    // MSVC adjustor thiscall: resolve secondary vtbl at target+4 chain, slot +0x160.
    // Returns char* display/login name used as friends-list key.
    name = target.vtbl_adjustor(+0x160)();

    // thiscall FUN_00573a30(ECX=friendsList, stack=name):
    // walk [+8,+c) pointer vector; _stricmp(entry+0x1a, name); return entry* or 0.
    if (FUN_00573a30(DAT_00d1da2c, name) != 0) {
      // Already friends → Remove button
      btn = menu.vtbl+0x444("i_m_int_2d_btn_generic.xml", 0, 0x1117a, 0xFFFFFFFF);
      if (btn != 0) {
        btn.vtbl+0x1d8("Remove from Friends", 1, 1);
      }
      menu[+0x564] = btn;   // EDI[0x159]
      return;
    }
  }

  // Not on list (or no list host) → Add button
  btn = menu.vtbl+0x444("i_m_int_2d_btn_generic.xml", 0, 0x11177, 0xFFFFFFFF);
  if (btn != 0) {
    btn.vtbl+0x1d8("Add to Friends", 1, 1);
  }
  menu[+0x558] = btn;       // EDI[0x156]
}
```

## Sealed constants

| Item | Value |
|---|---|
| XML plate | `i_m_int_2d_btn_generic.xml` @ `0x00a721ac` |
| Remove label | `"Remove from Friends"` @ `0x00a72144` |
| Add label | `"Add to Friends"` @ `0x00a72134` |
| Remove id | `0x1117a` |
| Add id | `0x11177` |
| Remove slot | host `+0x564` |
| Add slot | host `+0x558` |
| Local player | `DAT_00d1b6d8` |
| Friends list | `DAT_00d1da2c` |

## Call graph (OWN unit)

```
FUN_0082fe20  UI_InteractionMenu_PopulateTargetButtons_Inferred  [dualed]
  ├─ order A: ... → FUN_0082f9a0 @ 0x0082feff → ...
  └─ order B: ... → FUN_0082f9a0 @ 0x0082fee3 → ...
       └─ FUN_00573a30  friends-list name lookup  [undualed]
```

## Open questions

1. Product / MSVC class name for menu host and friends-list object at `DAT_00d1da2c`.
2. English meaning of object field `+0x6B4` (shared gate with convoy/ignore packs).
3. Full English for `vtbl+0x160` (name getter) and button create/set-text slots.
4. Runtime / bit-exact / differential (Terminal false).
