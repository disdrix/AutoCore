# Annotated low-level: FUN_00522780 → Character_CalcCommodityTierFreeScore_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00522780` |
| VA | `0x00522780` |
| System | `inventory-transfer` |
| Date | 2026-08-05 (R10-036 refresh) |
| Ghidra | `FUN_00522780` |
| Inferred name | `Character_CalcCommodityTierFreeScore_Inferred` |
| Parent dual | `0x00522710` `Character_SumCommodityTfidRowCost_Inferred` |

## Machine-level notes

- **ABI:** `__thiscall` ECX = `Character*`; stack `(uint32_t tier, const uint32_t* tfidRow)`; **`ret 8`**.
- **Body:** `0x00522780`–`0x0052283d` exclusive (**189 B**). Jump table at `0x00522840` (tier−1 → case).
- **Role:** Float **free-capacity score** for one commodity tier row: `used` from parent cost sum, `max` from `FUN_00520340(tier)`, then
  `(level * scale[tier] + (base[tier] + free*2)) * 0.01` with `level = *(u8*)(this+0x599)`.
- **Early outs:** `free < 0` or `tier ∉ 1..5` → `0.0f` (shared `g_flZero` path).
- **Caller:** sole xref `FUN_00522860` multiplies per-tier scores when tier head TFID is non-null (product of five tier scores).
- Prefer raw + `read_memory` for float immediates; decompiler CF matches sealed formula.
- Reject scaffold `Named_CalleeOf_…_00522780`.

## Pseudocode (annotated)

```c
// __thiscall Character* this; ret 8
// tier: 1..5 commodity tier index
// tfidRow: 5 × {lo,hi} COID/TFID pairs (same row layout as SumCommodityTfidRowCost)
float Character_CalcCommodityTierFreeScore_Inferred(
    Character* self,
    uint32_t tier,
    const uint32_t* tfidRow)
{
  int used = Character_SumCommodityTfidRowCost_Inferred(self, tier, tfidRow); // FUN_00522710
  int max  = FUN_00520340(self, tier); // level-scaled tier capacity max (not owned)
  int free = max - used;
  if (free < 0)
    return 0.0f;

  int base;
  float scale;
  switch (tier) {
  case 1: base = 50; scale = 2.5f; break; // DAT_00aaa6c4
  case 2: base = 40; scale = 2.0f; break; // 0x00a10e74
  case 3: base = 30; scale = 1.5f; break; // DAT_00aaa68c
  case 4: base = 20; scale = 1.0f; break; // g_flOne
  case 5: base = 10; scale = 0.5f; break; // DAT_00a0f298
  default: return 0.0f;
  }

  uint8_t level = *(uint8_t*)((uint8_t*)self + 0x599);
  // bytes: fild level; fmul scale; fiadd (base + free*2); fmul 0.01
  return (level * scale + (float)(base + free * 2)) * 0.01f; // DAT_00a0f718
}
```

## Offsets / globals

| Symbol | Role | Confidence |
|---|---|---|
| `this+0x599` | uint8 level (same as max-capacity helper) | **High** |
| `base[1..5]` = 50,40,30,20,10 | tier base additive | **High** (imm) |
| `scale[1..5]` = 2.5,2.0,1.5,1.0,0.5 | level weight | **High** (read_memory) |
| `DAT_00a0f718` ≈ 0.01 | final scale-to-fraction | **High** |
| `FUN_00522710` used cost | parent dual sealed | **High** |
| `FUN_00520340` max capacity | CF sealed; name open | **High** CF / open English |

## Open questions

- Product/PDB method English for the score (UI weight? craft fitness?).
- Exact product meaning of `base + free*2` and level×scale mix.
- Dual / name seal for `FUN_00520340` and sole caller `FUN_00522860`.
- Runtime / bit-exact verification (terminal false).
