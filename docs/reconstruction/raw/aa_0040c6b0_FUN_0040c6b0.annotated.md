# Annotated low-level: FUN_0040c6b0 → CNDHash_TraverseToNext_TFID_Node0x28_RegEdiEsi_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0040c6b0` |
| VA | `0x0040c6b0`–`0x0040c6f0` (65 B) |
| System | interaction-activation / CNDHash (npc-interact soft-cast hash) |
| Date | 2026-08-05 (MEGA-112 refresh) |
| Canonical name | `CNDHash_TraverseToNext_TFID_Node0x28_RegEdiEsi_Inferred` |
| Ghidra | `FUN_0040c6b0` |
| Dual | A/B accept-with-gaps |

---

## Machine-level notes

- Source: raw capture + MEGA-112 live re-verify (`decompile_function` ≡ `force_decompile`; `disassemble_function` + `read_memory`).
- **Custom register ABI** (not thiscall): **EDI** = hash\*, **ESI** = node-cursor\*\*, **EAX** = payload (TFID\*), plain **RET**.
- Lock soft-check at **hash+0x1d** — logs `"HashError:TraverseToNext, not locked for traversal"` + `"VOG_DEBUG_STOP"` then **continues** (does not abort).
- Ordered walk: head **hash+0x14**; successor **node+0x20**; return **payload node+0xc**.
- Node family = **0x28 / u64-key** layout (list-next +0x20, value +0xc) — **not** the 0x1c skill/mission node (+0x14/+8) used by dualed `CNDHash_TraverseToNext` `0x00411900`.
- Exclusive consumer: `Client_UpdateNpcInteractIcons` @ `0x0091b8d0` (2 call sites); hash at `*[DAT_00d1b644 + 0xe8f4]`.
- Companion lock set: `FUN_00411e10` (ESI=hash; set +0x1d=1). Parent clears +0x1d after loop.
- Sibling reg-ABI twin with 0x1c node: `FUN_00411e40` (next+0x14, value+8) — residual, not OWN.

---

## Pseudocode (annotated; register ABI restored)

```c
// Retail: EDI = CNDHash*, ESI = Node**, EAX = TFID* payload, plain RET
// Ghidra prints void + unaff_EDI / unaff_ESI.

TFID_16 *CNDHash_TraverseToNext_TFID_Node0x28_RegEdiEsi_Inferred(void)
{
  int node;           // EAX / iVar1
  int *cursor = ESI;  // unaff_ESI — Node**
  int hash = EDI;     // unaff_EDI — CNDHash*

  // Soft assert: must already be TraversalLock'd
  if (*(char *)(hash + 0x1d) == '\0') {
    FUN_007a4480(0, "HashError:TraverseToNext, not locked for traversal");
    FUN_007a4480(0, "VOG_DEBUG_STOP");
    // fall-through — NOT early return
  }

  if (*cursor == 0) {
    // first step: seed from ordered-list head
    *cursor = *(int *)(hash + 0x14);
    node = *(int *)(hash + 0x14);
  } else {
    // subsequent: advance via node list-next @ +0x20 (0x28-class)
    node = *(int *)(*cursor + 0x20);
    *cursor = node;
  }

  if (node != 0) {
    return *(TFID_16 **)(node + 0xc);  // payload = TFID*
  }
  return 0;
}
```

---

## Layout

| Off | Object | Role |
|----:|--------|------|
| `+0x14` | hash | ordered list head (node\*) |
| `+0x1d` | hash | TraversalLock flag (byte) |
| `+0x20` | node | list-next (0x28 / u64-key family) |
| `+0xc` | node | payload value (TFID\* for this consumer) |

---

## Open questions / gaps

- Product English / RTTI for the hash instance at `DAT_00d1b644+0xe8f4`.
- Whether any other site inlines the same body instead of calling this VA (xrefs show only this parent).
- Runtime / bit-exact / differential (terminal false).
