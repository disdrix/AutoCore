# Raw capture: FUN_0076bc00

| Field | Value |
|---|---|
| **Stable ID** | `aa_0076bc00` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0076bc00` |
| **Canonical name** | `FUN_0076bc00` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
bool FUN_0076bc00(LPCSTR param_1,char param_2)

{
  char cVar1;
  BOOL BVar2;
  int iVar3;
  undefined1 local_24 [6];
  uint local_1e;
  
  if (param_2 != '\0') {
    cVar1 = FUN_0076adc0(param_1);
    if (cVar1 != '\0') {
      FUN_0076ba10(param_1,1);
      BVar2 = CreateDirectoryA(param_1,(LPSECURITY_ATTRIBUTES)0x0);
      return BVar2 != 0;
    }
    iVar3 = stat(param_1,local_24);
    if ((iVar3 == 0) && ((local_1e >> 0xf & 1) != 0)) {
      FUN_0076b080(1);
    }
  }
  BVar2 = CreateDirectoryA(param_1,(LPSECURITY_ATTRIBUTES)0x0);
  return BVar2 != 0;
}
```

---

## W37-Z re-verify (2026-08-04)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual W37-Z |
| **Tools** | `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + callers/xrefs + call-site context. **No** `disassemble_bytes` |
| **Live decompile** | ≡ scaffold CF |
| **Body** | `0x0076bc00`–`0x0076bc79` exclusive (**121 B** / `0x79`); pad `CC` then next (`FUN_0076bc80`) |
| **Classification** | worker |

### Bytes (`read_memory` full body 121 B)

```text
83ec24807c242c00568b74242c745856e8abf1ffff83c40484c0741e6a0156e8ecfdffff83c4086a0056ff1538619c0085c00f95c05e83c424c38d4424045056ff1504659c0083c40885c0751a8b4c240ac1e90f80e101884c2430740a6a01e81cf4ffff83c4046a0056ff1538619c0085c00f95c05e83c424c3
```

### Byte-sealed ABI

| Slot | Evidence | Role |
|---|---|---|
| path | After `SUB ESP,0x24` / `PUSH ESI`: load from stack; `PUSH ESI` into callees | `const char* path` |
| force | `CMP BYTE [esp+0x2c],0` pre-push (= arg2) | `char force_clear` |
| return | `SETNZ AL` from `CreateDirectoryA` BOOL; `ADD ESP,0x24; RET` | **bool** in AL |
| convention | Callers `PUSH …; CALL; ADD ESP,8` (e.g. CompactPackFile `0x009850b5`) | **cdecl**, 2 stack args |

### Semantics (sealed)

**Create directory**, with optional force-clear of an existing path:

```
if force_clear:
  if FUN_0076adc0(path):                    // is directory (Fs_PathIsDirectory_Stat)
    FUN_0076ba10(path, 1)                   // Fs_DeleteDirTreeRecursive force
    return CreateDirectoryA(path, NULL) != 0
  if stat(path) == 0 && (st_mode >> 15) & 1: // _S_IFREG (0x8000)
    FUN_0076b080(1)                         // force unlink; ESI=path
// fall-through (no force, missing path, or after file unlink):
return CreateDirectoryA(path, NULL) != 0
```

| Stage | Match | Conf |
|---|---|---|
| force gate | `CMP BYTE` force | **High** |
| is-dir → wipe tree → recreate | `FUN_0076adc0` → `FUN_0076ba10(path,1)` → CreateDirectoryA early return | **High** |
| regular-file → force unlink | `stat` + `SHR 0xF` / `AND 1` = `_S_IFREG`; `FUN_0076b080(1)` with **ESI=path** | **High** |
| always CreateDirectoryA | both arms | **High** |
| bool AL | `SETNZ` | **High** |

`st_mode` at MSVC `_stat` offset **+6** (`local_1e` / `[esp+0x0A]` after frame) — matches CRT layout.

### Callers (4)

| Caller | Site | Notes |
|---|---|---|
| `FUN_004b52e0` | `0x004b5572` | path + flag on stack |
| `FUN_0076bc80` | `0x0076bd5a` | sibling mkpath-style; `ADD ESP,8` |
| `FUN_007a13e0` | `0x007a1474` | force=`1` |
| `FUN_00985010` | `0x009850b5` | CompactPackFile; `"_pk_"` / force `1` |

### Callees

| Symbol | Role |
|---|---|
| `FUN_0076adc0` | `Fs_PathIsDirectory_Stat` (W33-C sealed) |
| `FUN_0076ba10` | `Fs_DeleteDirTreeRecursive` (W34-A sealed) |
| `FUN_0076b080` | force file unlink (`_chmod`/`_unlink`; ESI=path) — peer not dual-owned here |
| `stat` | CRT `_stat` |
| `CreateDirectoryA` | Win32 |

### Name

`Fs_CreateDirectoryForce_Inferred` (**Inferred**). Reject scaffold `Named_CalleeOf_Named_NDRenderEngine_0076bc00`.
