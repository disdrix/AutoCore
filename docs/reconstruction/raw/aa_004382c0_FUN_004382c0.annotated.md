# Annotated low-level: FUN_004382c0

| Field | Value |
|---|---|
| Stable ID | `aa_004382c0` |
| VA | `0x004382c0` |
| System | MSVC string / composite default ctor |
| Date | 2026-08-04 (W38-G dual seal) |

## Machine-level notes

- Source: raw capture for `aa_004382c0` + live `read_memory` / xrefs (W38-G).
- **ABI (sealed):** single **stack** arg (object out/this); **not** ECX-thiscall (`mov esi,[esp+14h]` after SEH); returns object in **EAX**; **`ret 4`**.
- **Body:** default-construct **two** MSVC `basic_string` (each **0x1c**) at `this+0` and `this+0x1c` via IAT `[0x009c6288]` -> `0x006eb25c`.
- Object footprint **0x38** (56) for the pair — matches callers that `sub esp,0x38` then pass ESP as fill for `vector` of 0x38-byte elements (`FUN_00437e40` uses `/0x38` size math).
- **SEH:** `LAB_009bbfb9`; state `-1` then `0` after first string (partial-construction safety).
- **Reject** scaffold `Named_CalleeOf_Named_gfxBody_004382c0` as product name.
- Role name: `Struct_TwoBasicString_DefaultCtor_Inferred`.

## Pseudocode (annotated)

```c
// stack this*; ret 4; returns this
Struct_TwoBasicString *Struct_TwoBasicString_DefaultCtor_Inferred(Struct_TwoBasicString *self)
{
  // SEH register
  basic_string_default_ctor(&self->str0);       // this+0x00, sizeof 0x1c
  // SEH state = 0
  basic_string_default_ctor(&self->str1);       // this+0x1c
  return self;
}
```

## Callers (6 sites / 4 funcs)

| Site | Function | Notes |
|---|---|---|
| `0x00437adb` | `FUN_00437ad0` | Local 0x38 then `FUN_00437e40` resize |
| `0x00765de8` | `FUN_00765740` | gfxBody-related unserialize path |
| `0x0098978f` | `FUN_009895c0` | Then `FUN_00437e40` on host`+0x2c` |
| `0x0095e58a` / `0x0095e87b` / `0x0095ebf6` | `FUN_0095e3e0` | Three sites; same dual-string default |

## Open questions

- Product struct / field English (name+path, key+value, etc.).
- Whether always paired with `FUN_00437e40` (0x38-elem vector resize) or also standalone.
- Runtime / bit-exact / differential.
