# Annotated low-level: UI_InteractionMenu_AddConvoyInviteOrKick_Inferred (`FUN_0082fb30`)

| Field | Value |
|---|---|
| Stable ID | `aa_0082fb30` |
| VA | `0x0082fb30`–`0x0082fc31` exclusive |
| Body size | **257 B** (`0x101`) |
| System | interaction menu / convoy UI |
| Date | 2026-08-04 (WQ9D-I dual; prior scaffold 2026-07-23) |
| Ghidra | `FUN_0082fb30` |
| Named | `UI_InteractionMenu_AddConvoyInviteOrKick_Inferred` |
| Prior alias | `UI_int_btn_generic_0082fb30` (plate-only; superseded for role) |

## Machine-level notes

- **Not a free-standing thiscall.** Parent `FUN_0082fe20` builds the radial/interaction menu; this unit is one button-pack helper.
- **ESI** = menu host object (decompiler `unaff_ESI`); **EDX** = target object (`param_2`).
- Epilogue **`ret`** (no stack cleanup) — register-arg fragment style.
- Entry compares `target+0x6B4` and `localPlayer+0x6B4` — rank/level-like gate; body runs if target field `<1` **or** local field `>0`.
- Branch: if local has no crew host (`+0xCB0==0`) **or** target not in crew (`VehicleCrew_ContainsMember_Inferred`) → **Invite**; else **Kick**.
- Button factory: host `vtbl+0x444(xml, 0, buttonId, -1)`.
- Label: button `vtbl+0x1D8(text, 1, 1)`.
- Disable when `FUN_00574880()` (roster member at host `+0x194` index) **≠** local player — non-leader cannot invite/kick.
- Stores: Invite → `ESI+0x540` (`ESI[0x150]`); Kick → `ESI+0x544` (`ESI[0x151]`).

## Related duals (not OWN)

| VA | Name | Use here |
|---|---|---|
| `0x005749d0` | `VehicleCrew_ContainsMember_Inferred` | Invite vs Kick branch |
| `0x00574760` | `ConvoyRoster_GetMemberBySlot_Inferred` | Same slot math as `FUN_00574880` |
| `0x00574880` | (undualed) roster slot load / leader probe | Disable if not local |

## Open gaps

1. Product class of menu host / full MSVC name.
2. Exact English for `+0x6B4` field (level vs rank vs flags).
3. Full dual of `FUN_00574880` / parent `FUN_0082fe20`.
4. Runtime / bit-exact open.

## Pseudocode

See raw `aa_0082fb30_FUN_0082fb30.md` and clean `UI_InteractionMenu_AddConvoyInviteOrKick_Inferred.cpp`.
