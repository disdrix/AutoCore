# Annotated low-level: Map_FreeSubtree_B (FUN_0051be90)

| Field | Value |
|---|---|
| Stable ID | `aa_0051be90` |
| VA | `0x0051be90`–`0x0051bec5` (53 B exclusive) |
| Canonical | `Map_FreeSubtree_B` |
| System | STL / map-set (WQ-009 skill residual; Map_EraseRange_B free helper) |
| Date | 2026-08-04 WQ9E-I OWN dual |

## Machine-level notes

- Source: raw capture + live re-verify for `aa_0051be90`.
- **Bytes authority:** decompiler omits left-child walk; reconstruct from `read_memory`.
- isnil@**+0x29**; left@**+0**; right@**+8**.
- ABI: stack node*; **ret 4**; ECX preserved/threaded unused.
- Sole external caller: `Map_EraseRange_B` full-clear path (`head->parent` = root).

## Pseudocode (byte-corrected)

```c
// Ghidra plate (misleading left-walk omission):
//   if (!isnil) { free(right); operator_delete(self); }

// Byte-correct MSVC free-subtree (isnil@+0x29):
void Map_FreeSubtree_B(void *node)
{
  if (*(char *)((int)node + 0x29) != 0)
    return;

  for (;;) {
    Map_FreeSubtree_B(*(void **)((char *)node + 8)); // right
    {
      void *left = *(void **)node;                   // left @ +0
      operator_delete(node);
      node = left;
    }
    if (*(char *)((int)node + 0x29) != 0)
      break;
  }
}
```

## Call graph

| Direction | Target | Notes |
|---|---|---|
| Caller | `Map_EraseRange_B` `0x0051d880` | full clear only |
| Caller | self | right-child recurse |
| Callee | `operator_delete` | after left snapshot |

## Open questions

- Exact map instantiation / value dtor (delete-only; no inlined value dtor observed).
- Twin free `FUN_0051bbc0` dual (WQ9E-H OWN — not this agent).
- Runtime / bit-exact open.
