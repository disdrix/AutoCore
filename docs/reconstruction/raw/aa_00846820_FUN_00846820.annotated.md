# Annotated low-level: FUN_00846820 / UI_AppendCraftRequirements_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00846820` |
| VA | `0x00846820` |
| Body | `00846820`–`00847232` (**4115 B** / `0x1013`) |
| System | skills-abilities (client UI craft/recipe requirements) |
| Date | 2026-08-05 (R11-028 dual re-annotate; prior scaffold 2026-07-23) |
| Dual | `reviews/A_aa_00846820_UI_AppendCraftRequirements_Inferred.md` + B |

---

## Machine-level notes

- Source: raw capture + live `disassemble_function` / `read_memory` / callee decompiles.
- Prefer assembly when decompiler emits `unaff_*` / wrong stack object.
- **ESI** = UI text object (register argument; never clobbered).
- **stack arg0** = craftable/item object* (loaded to EBP at entry).
- Return = **line count** (int); early 0 if no local player `DAT_00d1b6d8`.
- Plain `ret` after SEH unwind (`add esp,0x1a8`).
- Reject primary name `Named_Required_Combat_*` — Combat is one of many lines.

---

## ABI

```text
int UI_AppendCraftRequirements_Inferred(ItemOrCraft *item /* stack */, UIText *text /* ESI */)
  SEH LAB_009ac516
  sub esp, 0x19c
  if (g_localPlayer == NULL) return 0;
  // text in ESI; item in [esp+0x1b0] after frame → EBP
  ...
  add esp, 0x1a8; ret
```

---

## Stages (annotated)

### 0. Gate

`cmp [DAT_00d1b6d8], 0` → `xor eax,eax; ret` if null.

### 1. Ingredient count

`ECX = *( *(item+0xa8) + 0x3c )`  // item def  
`call FUN_00599dd0` — counts dwords at `def+0x498, +0x49c, +0x4a0, +0x4a4, +0x4a8` that are not `-1` (0..5).

Also: `FUN_007a69d0` string-table singleton; `FUN_004ce940` recipe/manager lazy init (result stored, used as localize context).

### 2. INGREDIENTS block (if count > 0)

- Header via vfunc text`+0x224` with color `0xff20ff20` and string `"\nINGREDIENTS\n"`.
- Loop offset `EBX = 0x498` step +4, `count` times:
  - object id = `*(def + offset)`
  - hash chain on `DAT_00d1b570 + 0xf10` (CND map)
  - node `+0x10` key match → payload `+0x8`
  - if `payload+0x3c == 0`: `FUN_004f1e20(1,1)` ensure loaded
  - append `"   "` (`DAT_00a5a66c`) via `+0x250`
  - copy name from `*(payload+0x3c)+0x92` through string helper `FUN_00403450` + localize `FUN_007a6de0`
  - if `*(g_localPlayer + 0x6b4) > 0`: sprintf `" %i"` with `*(payload+0x34)` qty
  - append `"\n"`
  - lineCount++

### 3. ENHANCEMENT INGREDIENTS (if item vfunc `+0x60` > 0)

- Header `"\nENHANCEMENT INGREDIENTS\n"` color `0xff20ff20`.
- For `i = 0 .. vfunc+0x60 - 1`:
  - prefix = vfunc `+0x5c`(i)
  - recipe row = `FUN_005097b0(prefix)` → GetTypelessRecipe-style table (+0x70 arrays)
  - for `slot = 0; slot < 0x14; slot += 4`:
    - id = `*(row + slot)`; skip `0xffffffff`
    - hash on `DAT_00b041fc + 0xf10`
    - same name/qty/newline append as ingredients
    - lineCount++

### 4. Separator + Discipline

- Append `"\n"`; lineCount++.
- If `def+0x4ac >= 0`:
  - alloc `0x1fc`, `FUN_0052b040(type, buf)`
  - balance = `FUN_0052ada0(type)` vs threshold `def+0x4b0`
  - color `0xffff0000` if balance < threshold else `0xffffffff`
  - prefix `"Discipline: "` via vfunc `+0x22c`
  - name at `buf+0x2a`; sprintf `"%s - %i\n"`; append colored
  - free buffer
  - lineCount++

### 5. Required Faction (race)

- def vfunc `+0x14` → race id; if != -1:
  - name = `FUN_0051f8e0(vfunc(-1))`
  - format `"Required Faction: %s\n"`
  - met if player creature race byte `@+0x532` equals race id → `+0x250` else red `+0x224` (`0xffff2020`)
  - lineCount++

### 6. Required Class

- if `def+0x3e0 != -1`:
  - name = `FUN_0051f940(classId, playerRace@+0x532, -1)` (parent dual unit)
  - met if player class byte `@+0x531` equals class id
  - same white/red append pattern
  - lineCount++

### 7. Stat requirements (def shorts)

| Offset | Label | Player getter |
|--------|-------|---------------|
| `+0x3fe` | Required Combat | `FUN_004c4070` |
| `+0x400` | Required Perception | `FUN_004c41c0` |
| `+0x402` | Required Tech | `Character_GetTechForPoolCalcs` @ `0x004c3ff0` |
| `+0x404` | Required Theory | `FUN_004c4140` |

Each: if short > 0, format `"%s: %i\n"`; fail if player < req (red); else white; lineCount++.

### 8. Required Level

- level = `(short)item[+0xe8] + (short)item[+0xea]`
- if level < 1: return lineCount (skip level line)
- display/compare capped at **0x50 (80)**
- player level via component chain vfunc `+0x27c`
- fail if player < capped req
- lineCount++; return lineCount

---

## Field map (craft item / def)

| Path | Use |
|------|-----|
| item `+0xa8` → nested `+0x3c` | def* |
| def `+0x498..+0x4a8` | up to 5 ingredient object ids (-1 empty) |
| def `+0x4ac` | discipline type (-1 none; gate `>= 0`) |
| def `+0x4b0` | discipline threshold |
| def vfunc `+0x14` | race/faction id (-1 none) |
| def `+0x3e0` | class id (-1 none) |
| def `+0x3fe..+0x404` | Combat/Perception/Tech/Theory req shorts |
| item `+0xe8`, `+0xea` | level shorts (sum, cap 80) |
| item vfunc `+0x5c`, `+0x60` | enhancement ingredient get/count |
| ingredient payload `+0x34` | display quantity |
| ingredient payload `+0x3c` → `+0x92` | name string |

---

## Pseudocode (role-level; fidelity notes in clean twin)

```c
// ESI = UIText* text; stack = ItemOrCraft* item
int UI_AppendCraftRequirements_Inferred(ItemOrCraft *item)
{
    if (g_localPlayer == NULL) return 0;
    SEH_enter();
    Def *def = item->holder->def; // +0xa8 → +0x3c
    int lines = 0;
    int nIng = CountIngredientSlots(def); // FUN_00599dd0
    // ... ingredients / enhancement / discipline / faction / class / stats / level ...
    return lines;
}
```

---

## Callers

**Open** — live Ghidra xrefs empty. Role still sealed by product strings + callee set + sibling `UI_AppendItemRequirements` (`0x00845360`).

---

## Confidence

| Claim | Level |
|---|---|
| Body span / SEH / plain ret | **High** |
| ESI = UI text; stack = item | **High** |
| Product section strings + colors | **High** |
| Ingredient slot map + count helper | **High** |
| Enhancement via GetTypelessRecipe path | **High** (callee string evidence) |
| Stat/class/race compare sources | **High** (shared with dualed getters / `0051f940`) |
| Return = line count | **High** |
| Static callers | **Open** |
| Product C++ method symbol | **Probable** (`_Inferred`) |
| Runtime tooltip golden | **Open** |
