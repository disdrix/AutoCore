# Raw capture: FUN_0074e310

| Field | Value |
|---|---|
| **Stable ID** | `aa_0074e310` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0074e310` |
| **Canonical name** | `FUN_0074e310` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __thiscall FUN_0074e310(undefined4 *param_1,int *param_2)

{
  int *piVar1;
  int iVar2;
  int *piVar3;
  
  FUN_00449440();
  iVar2 = param_1[3];
  if ((param_2 != (int *)0x0) && (param_2[1] = param_2[1] + 1, param_2[1] == 1)) {
    (**(code **)(*param_2 + 4))();
  }
  piVar3 = *(int **)(iVar2 + -4);
  if (piVar3 != (int *)0x0) {
    piVar1 = piVar3 + 1;
    *piVar1 = *piVar1 + -1;
    if (*piVar1 == 0) {
      (**(code **)(*piVar3 + 8))();
    }
  }
  *(int **)(iVar2 + -4) = param_2;
  (**(code **)(*param_2 + 0x30))(*param_1);
  return 0;
}
```

---

## W38-T re-verify (2026-08-04) — append only

| Field | Value |
|---|---|
| **Tools** | `batch_decompile` / `decompile_function` + `analyze_function_complete` + `read_memory` + xrefs + `get_assembly_context` + callee `FUN_00449440`/`FUN_00449560` (context). **No** `disassemble_bytes` |
| **Body** | `0x0074e310`–`0x0074e375` inclusive (**102 B** / `0x66`); epilogue **`C2 04 00`** (`ret 4`); pad `CC` then next fn |
| **ABI** | `__thiscall`; ECX = list host; stack `(obj*)`; returns **0**; **`ret 4`** |
| **Classification** | worker |
| **Xrefs** | 10 CODE; 7 caller funcs |

### Full body hex (102 B)

```
538bd98b4b0885c98d53045657750433c0eb088b42082bc1c1f8028d4801e80db1cfff8b7b0c8b74241083ef0485f6741183460401837e040175078b068bceff50048b0f85c9740b834104ff75058b11ff520889378b0b8b06518bceff50305f5e33c05bc20400
```

### Byte-sealed control flow (decompiler incomplete)

Decompiler collapses `FUN_00449440()` with no args and omits size math. Bytes seal:

1. `size = (begin==0) ? 0 : (end-begin)>>2` with **begin@host+8**, **end@host+0xC** (`8b 4b 08` / `[edx+8]` where `edx=host+4`).
2. `lea edx,[host+4]; lea ecx,[size+1]; call FUN_00449440` → thin wrapper to `FUN_00449560` **resize vector to size+1** (append one dword slot). Helper views vector via `param_2+4`/`+8` = host+8 / host+0xC.
3. `edi = end-4` (new last slot); **AddRef** `obj` (`+4` refcount; 0→1 → `vtbl[+4]`).
4. **Release** prior slot value if non-null (`refcount--`; →0 → `vtbl[+8]`).
5. Store `obj` at last slot; `obj->vtbl[+0x30](*host)`; `xor eax,eax; ret 4`.

### Callers (sample, assembly-sealed)

| Site | Parent | Pattern |
|---|---|---|
| `0x0048f3e4` | `FUN_0048f370` PalantirEnv_RebuildDefaultLights | `mov ecx,[esi+4]; push [light+0x8c]; call` (Direction) |
| `0x0048f621` | same | same for Hemispheric light core |
| `0x005b3575` | `FUN_005b3520` Reflect resolve | `mov ecx,[ecx+4]; push hemi_core; call` |
| `0x00755f4a` | `FUN_00755f10` | FourCC `BTLG` (`0x474c5442`) path |
| + `0058fe90`, `00792600`, `0078c780` (×2), `00912c20` (×2) | |

### Name

`LightMgr_ListRegister_PushBack_Inferred` — structural (**Inferred**). Peer of `LightMgr_SetActiveSlot3c_Refcount_Inferred` (`0x0074e0d0`) and unregister `FUN_0074e260`. **Reject** scaffold `Named_CalleeOf_Named_VOG_DEBUG_STOP_0074e310`.
