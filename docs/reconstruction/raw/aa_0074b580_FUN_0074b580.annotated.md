# Annotated low-level: EmptyDtorStub

| Field | Value |
|---|---|
| Stable ID | `aa_0074b580` |
| VA | `0x0074b580` only (**1 B** `c3`) |
| Canonical name | `EmptyDtorStub` |
| Ghidra name | `FUN_0074b580` |
| System | shared empty leaf (EH unwind + trivial member dtor) |
| Date | 2026-07-29 W31-B |

---

## Machine-level notes

- **Authority:** `read_memory` = `c3` then `cc` pad; decompile `return;`.
- **Body:** single-byte bare `ret`. No side effects. No callees.
- **ABI:** callers may pass ECX (thiscall member dtor shape) or use as EH unwind target; body ignores all args.
- **Not** mass-shared `EmptyRet` @ `0x0056f570` (different VA; different xref set).
- **Primary sealed roles:**
  1. Empty member dtor stubs in `ListTrackedObj_CompleteDtor` (three offsets).
  2. MSVC EH Unwind table entries (`Unwind@009a…`).
  3. Additional owner teardown call sites (`FUN_004921e0` family).

## Pseudocode (annotated)

```c
// Multi-role empty leaf — MUST NOT invent free/log/notify
void EmptyDtorStub(void)
{
  return;
}
```

## Open / residual

- Original PDB symbol.
- Exhaustive product class names for each thiscall offset site.
- Runtime vacuous.

## Verdict note

**accept** — 1-byte empty ret fully sealed.
