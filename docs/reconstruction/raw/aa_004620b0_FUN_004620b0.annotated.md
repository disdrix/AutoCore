# Annotated low-level: FUN_004620b0 → StdVector_UninitializedCopy_RcElem28

| Field | Value |
|---|---|
| Stable ID | `aa_004620b0` |
| VA | `0x004620b0`–`0x00462194` exclusive (**228 B**) |
| System | MSVC `uninitialized_copy` for refcounted elem **0x1c** |
| Date | 2026-07-23 scaffold; W35-N seal 2026-07-29 |
| Canonical name | `StdVector_UninitializedCopy_RcElem28` (**Inferred**) |

## Machine-level notes

- **Bytes win** on ABI: **ECX=src_begin**, stack **`(src_end, dest)`**, plain **`C3`** (caller cleans), **EAX=dest_end**.
- Ghidra `__thiscall` is **wrong for epilogue** (would be `RET 8`); treat as hybrid ECX-first + caller-cleaned stack.
- Loop step **+0x1c** (7 dwords). Per element: same field copy + AddRef gate as `FUN_00461ac0`.
- Dest null: skip write, still advance dest pointer by 0x1c each iteration.
- SEH `LAB_009be5a1`; EH unwind path Releases partial dest range (vtbl+8 when refcount hits 0) — decompiler happy path only.
- Callers (4): all inside `StdVector_InsertN_RcElem28` (`FUN_004617a0`) grow/in-place relocate paths.
- Reject scaffold `Named_CalleeOf_*gfxDevice*`.

## Signature (sealed)

```c
// ECX=src_begin, stack (src_end, dest); returns dest_end; plain RET (caller cleans 8)
RcElem28* StdVector_UninitializedCopy_RcElem28(
    RcElem28* src_begin,   /* ECX */
    RcElem28* src_end,     /* [ebp+8] */
    RcElem28* dest);       /* [ebp+0xC] */
```

## Element (0x1c)

Same layout as `RcElem28_CopyCtor_EsiEdi` / parent insert-n.

## Control flow (annotated)

1. SEH frame; `EDI=ECX` (src), `ESI=[ebp+0xC]` (dest); state 0.
2. While `src != src_end`:
   - state=1; if dest non-null: construct `*dest` from `*src` with AddRef.
   - dest += 0x1c; src += 0x1c; state=0.
3. Unlink SEH; return dest (EAX).
4. (EH only) Release constructed prefix; rethrow.

## Full hex

See raw W35-N append (228 B).
