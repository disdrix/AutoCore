# Annotated low-level: FUN_0065eb10 / Phys_StackDesc_ResetFlag0_Float1000

| Field | Value |
|---|---|
| Stable ID | `aa_0065eb10` |
| VA | `0x0065eb10` |
| Body | `0x0065eb10`–`0x0065eb22` (19 bytes) |
| System | physics / Havok stack or CCS descriptor |
| Date | 2026-07-29 (W25-O dual seal) |
| Named role | `Phys_StackDesc_ResetFlag0_Float1000` (**Inferred**) |

## Machine-level notes

- **ABI:** `__fastcall` / thiscall-style — **ECX** = 8+ byte descriptor; plain `ret`; **EAX** left as `ECX` (return-this in practice; decompiler shows `void`).
- **Leaf:** no calls.
- **Stores:** byte `*desc = 0`; float at `desc+4 = 1000.0f` from `DAT_00a0f520`.
- **Callers:**
  - `FUN_0064fe40` — post-ctor stack-descriptor teardown after wheels construction (does **not** touch heap wheels instance).
  - `hkDefaultChassis_vtbl_0x18` @ `0x0064feb0` — if arg non-null, tail-jumps here (optional CCS reset).
- Same global `DAT_00a0f520` is the drive-torque clamp ceiling elsewhere; **here** it is only a default float written into a short descriptor.

## Pseudocode (annotated)

```c
// ECX = desc; returns EAX = desc (bytes); decompiler: void
void __fastcall Phys_StackDesc_ResetFlag0_Float1000(uint8_t *desc)
{
  // movss xmm0, [DAT_00a0f520]  ; 1000.0f  Confirmed read_memory
  float f = *(float *)0x00a0f520;

  // mov eax, ecx
  // mov byte ptr [eax], 0
  *desc = 0;

  // movss [eax+4], xmm0
  *(float *)(desc + 4) = f;   // 1000.0f
  return;
}
```

## Open questions

- Product/PDB name for the 8-byte descriptor type (CCS vs wheels stack-desc union).
- Whether `+0` is a type tag, bool flag, or packed field beyond a single clear-to-zero.
- Runtime / bit-exact deferred.
