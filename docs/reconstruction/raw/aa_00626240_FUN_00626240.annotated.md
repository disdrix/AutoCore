# Annotated low-level: FUN_00626240 → CVOGHBSkill_PossessCreature_ctor

| Field | Value |
|---|---|
| Stable ID | `aa_00626240` |
| VA | `0x00626240`–`0x006262e4` (165 B / `0xA5`) |
| System | skills-abilities |
| Date | 2026-08-05 (R11-021 dual re-annotate) |
| Class | `CVOGHBSkill_PossessCreature` (RTTI Confirmed) |
| Parent | `CVOGHBSkillBase_ctor` (`0x005788d0`) |
| Vtbl | `PTR_FUN_009d1ba4` |
| Object size | `0x6f0` |

## Machine-level notes

- Source: raw capture for `aa_00626240` + live re-verify (decompile + `read_memory` + xrefs).
- Prefer assembly / immediates when decompiler width or naming drifts.
- **Do not** treat string `"VOG_DEBUG_STOP"` as the product class name — it is only the cast-fail assert path.
- Scaffold alias `Named_VOG_DEBUG_STOP_00626240` is **retired**.

## Purpose

**PossessCreature skill HB subclass constructor.** After `operator_new(0x6f0)` at the sole full-ctor site:

1. SEH frame (`LAB_009a8ae8`)
2. `CVOGHBSkillBase_ctor(pSource, pSkillBlob, arg4, pTarget, pTfid, arg7)` — copies skill blob, validate-target, attach owner, period
3. Install subclass vtbl `0x009d1ba4`
4. Clear subclass state: `+0x6c8=0`, `+0x6c4=0`, `+0x6c9=0`
5. `__RTDynamicCast(pTarget, 0, CVOGClonedObjectBase, CVOGCreature, 0)` → store at `+0x6c0`
6. If cast null → `FUN_007a4480(0, "VOG_DEBUG_STOP")` (continues; does not abort construction)
7. Return `this` (`ret 0x18`)

## Signature (image-sealed)

```c
// __thiscall  ECX = this (fresh 0x6f0 allocation)
// stack: pSource, pSkillBlob, arg4, pTarget, pTfid, arg7
// ret 0x18
// returns this*
void *CVOGHBSkill_PossessCreature_ctor(
    void *this,
    void *pSourceObj,
    void *pSkillBlob,
    uint32_t arg4,
    void *pTarget,       // dyn_cast → CVOGCreature* @ +0x6c0
    void *pTfid,
    uint32_t arg7);
```

## Field map (subclass tail)

| Offset | Dword idx | Init | Role |
|-------:|----------:|------|------|
| `+0x6c0` | `0x1b0` | dyn_cast result | `CVOGCreature*` possessed/target creature |
| `+0x6c4` | `0x1b1` | `0` | secondary host/HB slot (written later; see non-OWN `FUN_00626160`) |
| `+0x6c8` | byte | `0` | flag (non-OWN EndOrDestroy / pack paths) |
| `+0x6c9` | byte | `0` | flag (non-OWN leaf `0x006262f0` sets `1`) |
| … | … | later | `+0x6d0..` used by non-OWN pack/unpack `FUN_00626400` / `FUN_00626460` |

## Call graph (OWN leaf only)

```
caller @ 0x006263ab  (factory / spawn; not OWN)
  operator_new(0x6f0)
  └─ FUN_00626240 / CVOGHBSkill_PossessCreature_ctor  [OWN]
        ├─ CVOGHBSkillBase_ctor (0x005788d0)
        ├─ __RTDynamicCast (ClonedObjectBase → Creature)
        └─ FUN_007a4480 (optional debug stop)

type registry FUN_0054a640 id 0x1a:
  operator_new(0x6f0); default-ish base; *vtbl = 0x009d1ba4  (DATA only)

default factory FUN_0054cbb0:
  operator_new(0x6f0); FUN_00578830(); *vtbl = 0x009d1ba4  (DATA only; not full arg ctor)
```

## Pseudocode (annotated)

```c
// RTTI: CVOGHBSkill_PossessCreature  COL 0x00aae5ec / TD 0x00afecec
// Vtbl: 0x009d1ba4   Size: 0x6f0   Parent: CVOGHBSkillBase_ctor
void *__thiscall CVOGHBSkill_PossessCreature_ctor(
    void *self,
    void *pSourceObj,
    void *pSkillBlob,
    uint32_t arg4,
    void *pTarget,
    void *pTfid,
    uint32_t arg7)
{
  // SEH: local_4=-1; handler LAB_009a8ae8
  CVOGHBSkillBase_ctor(self, pSourceObj, pSkillBlob, arg4, pTarget, pTfid, arg7);
  // SEH state → 0
  *(uint32_t *)self = 0x009d1ba4u;                 // subclass vtbl
  *((uint8_t *)self + 0x6c8) = 0;
  *(uint32_t *)((char *)self + 0x6c4) = 0;
  *((uint8_t *)self + 0x6c9) = 0;

  void *creature = __RTDynamicCast(
      pTarget, 0,
      &CVOGClonedObjectBase_RTTI,   // 0x00af8e10
      &CVOGCreature_RTTI,           // 0x00af8d0c
      0);
  *(void **)((char *)self + 0x6c0) = creature;
  if (creature == 0) {
    FUN_007a4480(0, "VOG_DEBUG_STOP");            // 0x00a15844
  }
  return self; // ret 0x18
}
```

## Open questions

- Product English for non-OWN vtbl methods (OnHeartBeat / pack / EndOrDestroy slots on `0x009d1ba4`).
- Semantic of `+0x6c4` host pointer producers (beyond EndOrDestroy consumer).
- Runtime / bit-exact / differential — open (Terminal false).
