# Raw capture: FUN_004cc820

| Field | Value |
|---|---|
| **Stable ID** | `aa_004cc820` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004cc820` |
| **Canonical name** | `FUN_004cc820` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined1 FUN_004cc820(void)

{
  return 0;
}
```

---

## MEGA-030 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-030** |
| **Partition** | `docs/reconstruction/reviews/WAVE_2026-08-05_mega_residual_partition_map.md` → MEGA-030 |
| **System (partition)** | `missions-progression` |
| **Hint** | MatchTarget eval stub default |
| **Tools** | Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_xrefs` + `get_function_callers` + `get_assembly_context`. **No** `disassemble_bytes`. No Launcher. |
| **Image** | `autoassault.exe` base `0x400000` |
| **Terminal** | **false** |

### Live decompile (2026-08-05)

```c
undefined1 FUN_004cc820(void)

{
  return 0;
}
```

### Live analyze_function_complete

| Field | Value |
|---|---|
| **name** | `FUN_004cc820` |
| **address** | `004cc820` |
| **classification** | `stub` |
| **signature (Ghidra)** | `undefined FUN_004cc820(void)` — **under-typed** (see ABI seal) |
| **Body** | `004cc820`–`004cc824` inclusive end byte |
| **is_stub** | true |
| **callees** | **none** |
| **callers (function graph)** | **none** (entries are JMP adjustors + DATA vtbl slots) |
| **xref_count** | **86** |
| **disassembly mnemonics** | `XOR` @ `004cc820`; `RET` @ `004cc822` |
| **return_type_warning** | Return type is 'undefined' — verify EAX at RET |

### Authoritative body bytes (`read_memory` @ `0x004cc820`, len 16)

```text
32 c0 c2 08 00 cc cc cc cc cc cc cc cc cc cc cc
```

| VA | Bytes | Instruction |
|---|---|---|
| `0x004cc820` | `32 C0` | `XOR AL, AL` |
| `0x004cc822` | `C2 08 00` | `RET 0x8` |
| `0x004cc825+` | `CC…` | int3 pad (not body) |

**Body size:** **5 B** (`0x004cc820`–`0x004cc824` inclusive).

### Live disassemble_function

```text
004cc820: XOR AL,AL
004cc822: RET 0x8
```

### ABI seal (machine; decompiler understates)

| Claim | Evidence | Conf |
|---|---|---|
| Body ignores `this` / stack | No loads of ECX / `[ESP+…]` | **High** |
| Return **0** in **AL** | `XOR AL,AL` → AL=0; EAX low byte zero | **High** |
| Cleans **8** stack bytes | `RET 0x8` (`C2 08 00`) | **High** |
| `__thiscall` shape for two formals | Shared with dualed `CVOGObjective_MatchTargetEvaluators` (`0x0059d9c0`) which `PUSH argB; PUSH argA; CALL [vtbl+0x40]` and tests **AL**; same epilogue on walk fail | **High** |
| Not bare `RET` / not `RET 4` | bytes sealed | **High** |
| Leaf / no callees | analyze + body | **High** |

Canonical MSVC shape (formals unused):

```c
// ECX = evaluator this (ignored); two stack formals (ignored)
// AL = 0; RET 8
uint8_t __thiscall MatchTargetEval_DefaultReturnFalse_Inferred(
    void* /*this*/,
    void* /*argA*/,
    void* /*argB*/);
```

### Xrefs (live `get_function_xrefs`, limit 100)

**Code flow (6) — all are JMP adjustor thunks, not CALL:**

| From | Kind (Ghidra) | Live insn | Adjustor pattern |
|---|---|---|---|
| `0x004cc819` | UNCONDITIONAL_CALL label | `JMP 0x004cc820` | `SUB ECX,[ECX-4]; SUB ECX,0x3f0` then JMP |
| `0x0053be89` | UNCONDITIONAL_CALL label | `JMP 0x004cc820` | `SUB ECX,[ECX-4]; SUB ECX,0xc90` then JMP |
| `0x005a9bc9` | UNCONDITIONAL_CALL label | `JMP 0x004cc820` | `SUB ECX,[ECX-4]; SUB ECX,0xa0` then JMP |
| `0x005c9523` | UNCONDITIONAL_CALL label | `JMP 0x004cc820` | `SUB ECX,[ECX-4]` then JMP |
| `0x00605fc6` | UNCONDITIONAL_CALL label | `JMP 0x004cc820` | `SUB ECX,[ECX-4]; SUB ECX,0x10` then JMP |
| `0x00608d89` | UNCONDITIONAL_CALL label | `JMP 0x004cc820` | `SUB ECX,[ECX-4]; SUB ECX,0x1a0` then JMP |

These are classic MSVC **multiple-inheritance vtable adjustor thunks** that re-base `ECX` then tail-jump into the shared default MatchTarget body. Neighbor slots JMP sibling stubs (`004cc840`, `0056f5c0`, `0056f5e0`, …).

**DATA (80):** dense vtable pointer installs. Sample clusters:

| VA | Notes |
|---|---|
| `0x009ce2b4` / `0x009ce2b8` | consecutive slots both `004cc820` |
| `0x009d2ad4` / `0x009d2ad8` | pair |
| `0x009d342c` / `0x009d3430` | pair |
| `0x009d3c84` / `0x009d3c88` | pair |
| `0x009d41b4` / `0x009d41b8` | pair |
| `0x009d92c0` / `0x009d92e4` / `0x009d92ec` / `0x009d92f0` | multi-slot |
| `0x009db68c` / `0x009db690` | pair |
| `0x009dbc64` / `0x009dbc68` | pair |
| `0x009dddfc` | singleton-style |
| `0x009de1d4` / `0x009de1d8` | pair |
| `0x009df718` / `0x009df73c` / `0x009df744` / `0x009df748` | multi |
| `0x009dfaa8` / `0x009dfacc` / `0x009dfad4` / `0x009dfad8` / `0x009dfaf0` | multi |
| `0x009dfc30` / `0x009dfc54` / `0x009dfc60` | multi (UseItem family neighborhood) |
| `0x009e0418`…`0x009e3c08` | many objective-type vtbls |
| `0x009cbef8` | DATA |

`read_memory` sample @ `0x009ce2b0`:

```text
70 f5 56 00 | 20 c8 4c 00 | 20 c8 4c 00 | f0 77 56 00
→ 0056f570 , 004cc820 , 004cc820 , 005677f0
```

`read_memory` sample @ `0x009dfaa8` (first dword):

```text
20 c8 4c 00 … → 004cc820
```

### Role seal (cross dual)

Dualed `CVOGObjective_MatchTargetEvaluators` (`aa_0059d9c0` / `0x0059d9c0`) walks objective evaluator array `[this+0x158, this+0x15c)` and invokes `(*(e->vtbl + 0x40))(argA, argB)` testing **AL**. Its dual A/B already document:

- default stub `004cc820` = `XOR AL,AL; RET 8`
- Collect / Kill / Deliver sample: **+0x40 = always-false stub `004cc820`**
- UseItem real MatchTarget lives at **+0x38**, not this stub

Partition hint **“MatchTarget eval stub default”** matches machine + dualed parent evidence.

### Name decision

Promote **`MatchTargetEval_DefaultReturnFalse_Inferred`**.

| Rationale | |
|---|---|
| Always returns false | `XOR AL,AL` sealed |
| MatchTarget eval ABI | `RET 8` + dualed walker `vtbl+0x40` / Slot44 family |
| Shared default across many objective/eval vtbls | 80 DATA xrefs + 6 MI adjustor JMPs |
| Product / PDB demangle | **open** → **`_Inferred`** |
| Reject | invent product class English; claim always-true; claim bare RET; claim CALL-site direct callers without adjustors |

### CF: decompile ≡ raw ≡ bytes

| Stage | Match |
|---|---|
| Return constant 0 | **Yes** (decompile `return 0` ≡ `XOR AL,AL`) |
| No branches / no callees | **Yes** |
| Stack cleanup | decompiler elides `RET 8` → **documented ABI correction** (not CF conflict) |

### Gaps

- Exhaustive product English / PDB for every host evaluator class installing this slot.
- Full slot-offset inventory per vtbl (often +0x40 / pair +0x44; not every DATA site dualled here).
- Runtime / bit-exact / differential.

**Terminal:** **false**
