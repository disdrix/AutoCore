# Annotated low-level: FUN_00573a30 → FriendsList_FindEntryByNameI_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00573a30` |
| VA | `0x00573a30`–`0x00573a8b` exclusive (**91 B** / `0x5B`) |
| Canonical | `FriendsList_FindEntryByNameI_Inferred` |
| Ghidra | `FUN_00573a30` |
| System | social / friends list (partition nest: skills-abilities residual; live role UI/social) |
| Date | 2026-08-05 (R11-005 dual) |
| Prior scaffold | 2026-07-23 |

## Machine-level notes

- **ABI:** `__thiscall` — ECX = list host; stack `char *name`; **`RET 0x4`**; EAX = entry* or 0.
- **Callee:** `_stricmp` via IAT `DAT_009c6678` (cdecl, `ADD ESP,8`).
- **Vector:** begin `*(this+0x8)`, end `*(this+0xc)`; count = `(end-begin)>>2` if begin≠0 else 0.
- **Key:** case-insensitive compare of `entry+0x1a` (C-string) vs `name`.
- **Hit:** return first matching entry pointer; **miss/empty:** return 0.
- Prefer assembly when decompiler conflicts — here decompile ≡ disasm CF.

## Pseudocode (named)

```c
// __thiscall; RET 0x4
void* __thiscall FriendsList_FindEntryByNameI_Inferred(void* this, char* name)
{
  int i = 0;
  for (;;) {
    int count = (*(int*)((char*)this + 8) == 0)
      ? 0
      : (*(int*)((char*)this + 0xc) - *(int*)((char*)this + 8)) >> 2;
    if (count <= i) break;
    void* entry = *(void**)(*(int*)((char*)this + 8) + i * 4);
    if (_stricmp((char*)entry + 0x1a, name) == 0)
      return entry;
    i++;
  }
  return 0;
}
```

## Call graph (evidence only; callers not dualled here)

```text
UI_InteractionMenu_AddFriendsToggle (0x0082f9a0)  [dualed MEGA-075]
  └─ FUN_00573a30(DAT_00d1da2c, targetName)  // membership → Remove vs Add

FUN_0094d900  (/friend chat command residual)
  └─ FUN_00573a30(list, name)  // hit → already-friend msg; miss → 0x801f add

FUN_00830010 case 0x1117a  (Remove from Friends button)
  └─ FUN_00573a30(DAT_00d1da2c, name)
       └─ FUN_0094c680(entry+8, entry+0xc)  // TFID pair
```

## Open questions

- Product class name for `DAT_00d1da2c` host and entry struct.
- Whether list is sorted (body is O(n) linear — not binary search).
- Full entry fields beyond +0x1a name and +8/+0xc TFID.
