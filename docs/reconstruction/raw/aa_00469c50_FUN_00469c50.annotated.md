# Annotated low-level: FUN_00469c50 / StdVector_Elem28_Size

| Field | Value |
|---|---|
| Stable ID | `aa_00469c50` |
| VA | `0x00469c50`–`0x00469c70` (**33 B**) |
| System | containers / std::vector size helper |
| Date | 2026-07-29 (W33-J seal; scaffold 2026-07-23) |
| Inferred name | `StdVector_Elem28_Size` |

## Machine-level notes

- Source: raw capture + W33-J live re-verify.
- **ECX = vec**; begin `@+4`, end `@+8`; null begin → 0; else `(end-begin)/0x1c`.
- Division implemented as signed magic multiply `0x92492493` (MSVC ÷28), not IDIV.
- Leaf; bare RET; no stack args.
- Prefer bytes when decompiler plate is incomplete; here decompile ≡ bytes CF.

## Pseudocode (annotated)

```c
// ECX = vector-like host with begin@+4, end@+8
int __fastcall StdVector_Elem28_Size(void* vec /*ECX*/)
{
  int begin = *(int*)((char*)vec + 4);
  if (begin == 0) {
    return 0;
  }
  // signed (end - begin) / 0x1c  via magic 0x92492493
  return (*(int*)((char*)vec + 8) - begin) / 0x1c;
}
```

## Open questions

- Product/PDB host type varies by caller (generic helper).
- Whether any non-vector host shares +4/+8 layout (contract risk = caller dual).
- Runtime / bit-exact open.
