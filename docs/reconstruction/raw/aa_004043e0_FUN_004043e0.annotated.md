# Annotated low-level: FUN_004043e0 → StdTree_Max_Isnil31

| Field | Value |
|---|---|
| Stable ID | `aa_004043e0` |
| VA | `0x004043e0`–`0x004043fb` inclusive (**28 B** / `0x1c`) |
| System | MSVC `std::_Tree` max / rightmost (isnil @ +0x31) |
| Date | 2026-08-05 (WQ9L-C dual seal) |
| Canonical name | `StdTree_Max_Isnil31` |

## Machine-level notes

- Source: raw capture for `aa_004043e0` + WQ9L-C re-verify append.
- Prefer assembly when decompiler conflicts (void decomp is **wrong** for return).
- **EAX** = input node*; returns rightmost non-nil in **EAX**; plain **`RET`**.
- isnil every guard **`+0x31`** (`80 79 31 00`).
- Leaf — no callees. 7-byte `LEA ESP,[ESP]` is alignment NOP before loop.
- Twin min (left walk): dualed `StdTree_Min_Isnil31` @ `0x00409160`.

## Semantics (tree max / rightmost)

```text
// EAX = node*
right = node->right
if right->isnil(+0x31): return node        // EAX unchanged
do:
  node = right
  right = node->right
while !right->isnil
return node   // EAX = rightmost non-nil
```

Node layout: `+0` left, `+4` parent, `+8` right, color@+0x30, isnil@+0x31 (node size 0x38 family).

## Pseudocode (annotated; decomp void overridden)

```c
// EAX = node* in/out; bare RET
void *FUN_004043e0(void *node /*EAX*/)
{
  void *right = *(void **)((char *)node + 8);
  if (*(char *)((char *)right + 0x31) != '\0') {
    return node; // right is nil sentinel
  }
  do {
    node = right;
    right = *(void **)((char *)node + 8);
  } while (*(char *)((char *)right + 0x31) == '\0');
  return node;
}
```

## Callers (OWN context)

| Parent | Site | Role |
|---|---|---|
| `FUN_00408ed0` erase+rebalance isnil31 | `00408fbf` | `MOV EAX,EDI; CALL; MOV [EBX+8],EAX` → `head->right = max` |

## Twin / family

| Unit | VA | Role |
|---|---|---|
| Min isnil31 | `0x00409160` | dualed WQ9J-E |
| Max isnil29 | `0x00421b50` | stack ABI peer — do not merge |
| Erase parent | `0x00408ed0` | dualed |

## Open questions

- Product / English map key-value type (shared isnil31 helper).
- Runtime / bit-exact / differential.
