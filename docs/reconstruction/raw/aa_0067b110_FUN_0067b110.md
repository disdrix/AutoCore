# Raw capture: FUN_0067b110

| Field | Value |
|---|---|
| **Stable ID** | `aa_0067b110` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0067b110` |
| **Canonical name** | `FUN_0067b110` |
| **System** | network-nazgul-netAddress (trivial dtor) |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0067b110(void)

{
  return;
}
```

---

## v2026-07-29 OWN-ONLY dual re-capture

| Field | Value |
|---|---|
| **Date** | `2026-07-29` |
| **Tools** | Ghidra MCP `decompile_function` @ `0x0067b110` + `read_memory` (no `disassemble_bytes`) |
| **Live decompile** | Identity with 2026-07-23 body above — empty function |
| **Body span** | `0x0067b110` only (`c3` ret); padding `cc` until `FUN_0067b120` @ `0x0067b120` |
| **Bytes** | `c3` then `cc` padding |

### Live decompile (2026-07-29, authoritative re-confirm)

```c
void FUN_0067b110(void)

{
  return;
}
```

### Body bytes (`read_memory` @ `0x0067b110`, length 32)

```
c3 cc cc cc cc cc cc cc cc cc cc cc cc cc cc cc
8b 54 24 04 ...   ; ← start of FUN_0067b120 (not this unit)
```

Hex for executed body: **`c3`** only.

### Role context (callers use; not owned)

- Called from `FUN_0067ba30` / twin `FUN_0067b9a0` as stack-Address cleanup after SEH state clear.
- Widespread SEH unwind / scope-exit for 16-byte Address locals across Auth TCP paths (`AuthClient_*`, `Auth_AuthServer*`).
- Cluster: nazgul Address POD dtor (trivial) adjacent to `FUN_0067b070` (default ctor), `FUN_0067b0e0` (copy), `FUN_0067b120` (assign-if-not-same).
