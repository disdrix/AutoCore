
# Annotated low-level: PhysHost_ReconcileLinkPairs_Inferred (FUN_00634450)

| Field | Value |
|---|---|
| Stable ID | `aa_00634450` |
| VA | `0x00634450`–`0x0063453f` (**240 B**) |
| System | phys / host link-pair reconcile |
| Date | 2026-07-29 W29-R |
| Ghidra | `FUN_00634450` |
| Canonical | `PhysHost_ReconcileLinkPairs_Inferred` |

---

## Machine-level notes

- **thiscall** ECX=host; stack query param; **`ret 4`**.
- Gate: `*(host+8)` manager non-null.
- Dual stride-8 local worklists (`FUN_0062d960` / `FUN_0055f4c0`) sized from `*(mgr+0x238)`.
- Query: `*(mgr+0xc4)->vtbl+0x14` fills two pair lists; flag constant **1**.
- Null-safe link slot: MSVC `host == (T*)-0xC ? 0 : host+0x1C` (bytes: `LEA EAX,[ESI+0xC]; TEST; LEA ECX,[ESI+0x1C]`).
- Reconcile path: CanonAndSort both sides → SortedDiffWalk with **machine ECX = `*(mgr+0x138)`**, predicate/context `*(mgr+0xd0)?+8:0` → compact pointer list at `host+0x3c` via `FUN_005fff20`.
- Callers are pose/AABB write paths (`FUN_00580c00` / `FUN_00580dd0` / `FUN_005eb*`).

## Pseudocode (annotated; residuals noted)

```c
// thiscall; ret 4
void PhysHost_ReconcileLinkPairs_Inferred(int host /*ECX*/, uint32_t query_param /*stack*/)
{
  // if (*(host+8)==0) return;
  // LocalWorklist_Init ×2 from *(mgr+0x238) into stack descs
  // slot = (host == -0xC) ? 0 : host+0x1C;   // MSVC null-this for +0x1C
  // mgr[+0xc4]->vtbl+0x14(&slot, query_param, 1, listB_desc, listA_desc)
  // if either list count non-zero:
  //   LinkPair_CanonAndSort(listA); LinkPair_CanonAndSort(listB);
  //   LinkPair_SortedDiffWalk(this=*(mgr+0x138), listA, nA, listB, nB, *(mgr+0xd0)?+8:0)
  //   compact *(host+0x3c) list (FUN_005fff20, in_EAX = host+0x3c)
  // LocalWorklist_Release ×2
}
```

## Open / residual

- Product English for host type and vtbl+0x14 query.
- Decompiler confuses second-list count with vcall flag `1`.
- Runtime / bit-exact open.
