# Annotated low-level: CVOGHBSkill_XPAdder_ctor (FUN_00650f50)

| Field | Value |
|---|---|
| Stable ID | `aa_00650f50` |
| VA | `0x00650f50`–`0x00650f8e` |
| Body | **63 bytes** (`0x3F`) |
| Canonical name | `CVOGHBSkill_XPAdder_ctor` |
| Prior scaffold | `FUN_00650f50` |
| System | `skills-abilities` |
| Date | 2026-08-05 (R11-025 OWN dual) |
| Class RTTI | **Confirmed** `.?AVCVOGHBSkill_XPAdder@@` @ `0x00af5178` |
| Vtbl | `PTR_FUN_009e5304` |
| Object size | **0x6d0** (sole caller `operator_new`) |
| Parent base | `CVOGHBSkillBase_ctor` @ `0x005788d0` |

## Machine-level notes

- **ABI:** `__thiscall` — `ECX = this`; six stack dwords; **`ret 0x18`**; `EAX = this`.
- **CF:** base skill-HB ctor → install subclass vtbl → zero float at `this+0x6c0` → return this.
- **Decompiler hazards (sealed via bytes):**
  1. Base call shown without `this` — assembly `mov ecx,esi` before `call 0x005788d0`.
  2. `param_1[0x1b0] = 0` is **dword-index** → byte **`+0x6c0`**; store is **`MOVSS`** (float), not integer MOV.
- **RTTI path:** vtbl-4 COL `0x00ab1f30` → type_info `0x00af5178` → `".?AVCVOGHBSkill_XPAdder@@"`.
- **Sole caller:** `FUN_0061c940` @ `0x0061c9f3` and `0x0061ca93` after `operator_new(0x6d0)`; args mirror base plate (source, skill blob, arg4, target, tfid16, arg7) from parent HB fields.
- **Peer evidence (not OWN dual):** vtbl slot consumers of `+0x6c0` float include `0x00650f90` (accumulate XP delta into character `+0xc54`) and `0x006510b0` (subtract stored delta on end). Field role = XP-adder residual float (**High**, peer).

## Pseudocode (annotated)

```c
// __thiscall ECX=this; ret 0x18
CVOGHBSkill_XPAdder* __thiscall CVOGHBSkill_XPAdder_ctor(
    CVOGHBSkill_XPAdder* this,   // ECX  — pre-allocated 0x6d0
    void* pSourceObj,            // stack+0x04  → base param_2
    void* pSkillBlob,            // stack+0x08  → base param_3
    uint32_t arg4,               // stack+0x0C  → base param_4
    void* pTarget,               // stack+0x10  → base param_5
    uint32_t* pTfid16,           // stack+0x14  → base param_6
    uint32_t arg7)               // stack+0x18  → base param_7
{
  CVOGHBSkillBase_ctor(this, pSourceObj, pSkillBlob, arg4, pTarget, pTfid16, arg7);
  this->vtbl = &PTR_FUN_009e5304;          // CVOGHBSkill_XPAdder
  *(float*)((uint8_t*)this + 0x6c0) = 0.0f; // subclass XP residual (MOVSS)
  return this;
}
```

## Layout delta vs CVOGHBSkillBase

| Offset | Field | Confidence |
|-------:|-------|------------|
| `+0x00` | vtbl `PTR_FUN_009e5304` (replaces base skill vtbl) | **Confirmed** |
| `+0x24`… | skill runtime blob (base) | **Confirmed** (base dual) |
| `+0x6c0` | float XP residual (zeroed in ctor; tick/end peers R/W) | **Confirmed** store; role **High** |

Base sealed range ends ~`+0x6b0`; subclass adds float at **`+0x6c0`** within size **`0x6d0`**.

## Open questions

- Product English for float `+0x6c0` beyond “XP residual delta” (peer-derived High).
- Full dual of sole caller `FUN_0061c940` and peer vtbl methods (not OWN).
- Runtime / bit-exact / differential.
