# Annotated low-level: FUN_004b2b90 → ObjectMotion_ApplyStageParams_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004b2b90` |
| VA | `0x004b2b90`–`0x004b3253` (1732 B / `0x6C4`) |
| System | client object motion / reaction stage params |
| Date | 2026-07-29 (W27-E dual seal) |
| Preferred name | `ObjectMotion_ApplyStageParams_Inferred` |
| Ghidra name | `FUN_004b2b90` |

## Machine-level notes

- Source: raw capture + live `decompile_function` 2026-07-29 (≡ scaffold 2026-07-23).
- ABI sealed: `__thiscall` ECX=slot; stack `(char dirty, float dt)`; **`ret 8`** (`C2 08 00`); void.
- Callers: `ObjectMotion_SlotTick_Inferred` (`FUN_004b4620`) with dirty flag after radius/timer; `FUN_005bc120` (related motion path).
- Two modes: **dirty≠0** re-samples runtime fields from current stage template; **dirty==0** lerps by stage progress and integrates rate×dt.
- `DAT_00aaa638` ≈ 1/65535; progress uses `g_flOne - remaining/total`.

## Offset map (selected)

| Access | Role (inferred) |
|---|---|
| `slot[1]` | current stage |
| `slot[4]` / `slot[6]` | primary / secondary rate (float bits) |
| `slot[5]` / `slot[7]` | rate range helpers from stage `+0x2fc` / `+0x304` |
| `slot[9]` / `slot[10]` | rate pair from stage `+0x30c` / `+0x310` |
| `slot[0xb]` | integrated value += rate * dt (always on dirty==0 path) |
| `slot[0xc]` | optional field from stage `+0x318` |
| `slot+0xd..` | byte channels (RGB / alpha-like) |
| `slot[0x3e..]` / `+0xF8` | end-state block (0x1D dwords from stage `+0x36c`) |
| `slot[0x21..]` / `+0x84` | start-state snapshot (copied from `slot+4` after reinit) |
| `slot+0x242` | flag set when stage `+0x3e0` bit5 rate path taken |
| `slot+0x241` | bit from stage `+0x3e3` |
| `stage+0x3e0..0x3e3` | feature bitmasks gating which template fields apply |
| `slot[0x5c]/[0x5d]` | remaining / total duration (progress source) |

## Pseudocode (annotated high level)

```c
// thiscall; ret 8; void
void __thiscall FUN_004b2b90(int *slot, char dirty, float dt)
{
  if (dirty != 0) {
    // copy end-state template: stage+0x36c → slot+0x3e  (0x1D dwords)
    // flag-gated field loads from stage into slot (bits on +0x3e0..+0x3e3)
    // RNG adjustments via CVOGReaction_RandomUnitScalar * DAT_00aaa638
    // optional FUN_004b2910 byte-channel interpolators
    // optional mirror slot[6]=slot[4], slot[0x40]=slot[0x3e] under host mode
    // snapshot: slot+4 → slot+0x21 (0x1D dwords) as lerp baseline
    return;
  }

  // continuous path
  float progress = g_flOne - (float)slot[0x5c] / (float)slot[0x5d];
  // flag-gated lerps baseline→end by progress (bytes + floats)
  // always integrate:
  slot[0xb] = (int)((float)slot[9] * dt + (float)slot[0xb]);
}
```

## Dirty-path flag gates (stage bitmasks)

| Stage flag | Effect (structural) |
|---|---|
| `+0x3e2` bit5 | load `slot[0xc]` from `stage+0x318` |
| `+0x3e0` bit6 | load `slot[0xb]` from `stage+0x314` |
| `+0x3e0` bit0 | load vec3 `slot[0xf..0x11]` from `stage+0x324` |
| `+0x3e0` bit1 | load vec3 `slot[0x13..0x15]` from `stage+0x334` |
| `+0x3e0` bit2 | load 3 bytes at `slot+0xd..0x36` from `stage+0x31c` |
| `+0x3e0` bit3 | load byte `slot+0x37` from `stage+0x31f` (799) |
| `+0x3e0` bit4 | load `slot[4]` from `stage+0x2f8` |
| `+0x3e0` bit5 | load `slot[6]` from `stage+0x300` (± product with `+0x2f8` if `+0x308`); set `slot+0x242=1` |
| `+0x3e0` bit7 | load `slot[9]/[10]` from `stage+0x30c/310` |
| `+0x3e3` bit0 | scale `slot[0xb]` by RNG |
| `+0x3e2` bit7 | jitter `slot[9]` by `slot[10]*RNG` |
| `+0x3e2` bits 1/2 | `FUN_004b2910` on channels |
| `+0x3e2` bits 3/4 | ROUND RNG into bytes `+0x37` / `+0x11f` |
| `+0x3e1` bits 1/3/2/4 | RNG on rates `slot[4]/[6]/[0x3e]/[0x40]` |
| `+0x3e3` bit1 | set `slot+0x241` from `+0x3e3>>2` |

## Continuous-path flag gates

| Stage flag | Effect |
|---|---|
| `+0x3e1` bit5 | `FUN_004b2910(progress, slot+0xd, slot+0x2a, slot+0x47)` |
| `+0x3e2` bit0 | lerp byte `slot+0x37` from `+0xab`→`+0x11f` by progress |
| `+0x3e1` bit0 | lerp rates `slot[4]/[6]` baseline→end (product path if `stage+0x308`) |
| `+0x3e2` bit6 | lerp `slot[0xc]` baseline→end |
| always | `slot[0xb] += slot[9] * dt` |

## Call graph

| Dir | VA | Name |
|---|---|---|
| Caller | `0x004b4620` | `ObjectMotion_SlotTick_Inferred` |
| Caller | `0x005bc120` | related motion/list path (unsealed) |
| Callee | — | `CVOGReaction_RandomUnitScalar` |
| Callee | `0x004b2910` | byte-channel lerp helper (unsealed) |

## Confidence

| Claim | Level |
|---|---|
| CF ≡ live decompile | **Confirmed** |
| ret 8 thiscall void | **Confirmed** |
| dirty / continuous dual path | **Confirmed** |
| progress = 1 − rem/total | **Confirmed** |
| Product field English | **Open** (bitmasks map to stage schema) |
| Runtime / bit-exact | **Open** |
