# Raw capture: FUN_00971030

| Field | Value |
|---|---|
| **Stable ID** | `aa_00971030` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00971030` |
| **Canonical name** | `FUN_00971030` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00971030(undefined4 param_1,undefined4 param_2)

{
  FUN_00970b20(param_1);
  FUN_0043e630(param_2);
  return;
}
```

---

## W36-D live seal (2026-07-29)

### Tools

- `batch_decompile` / `analyze_function_complete` @ `0x00971030`
- `read_memory` 64 B @ entry (body **30 B** used)
- `get_function_by_address` → body `00971030`–`0097104d` (last byte of RET 4; exclusive end `0097104e`, pad `CC`)
- `get_xrefs_to` → sole code caller `FUN_004ed310` @ `0x004ed3a5`
- Nested duals: W33-N `00970b20`, W35-A `0043e630`
- **No** `disassemble_bytes`

### Full body hex (`read_memory` 30 B)

```
56578b7c240c8bf1568bcfe8e0faffff5783c61ce8e7d5acff5f5ec20400
```

### ABI (bytes supersede decompiler)

- **ECX** = AssPreloader* (this; saved in ESI).
- Stack formal 1: **key*** (`MOV EDI,[ESP+0xC]` after two pushes = original `[ESP+4]`).
- Epilogue **`RET 0x4`** (`C2 04 00`).
- Decompiler swaps/elides register map — **bytes win**.

### Control flow

1. `PUSH ESI; PUSH EDI`
2. `EDI = key*`; `ESI = ECX` (preloader)
3. `PUSH ESI` (host); `MOV ECX, EDI` (key holder); `CALL FUN_00970b20` — SideHost_PushKeyFlag0 (W33-N): push `{*key, flag:0}` onto locked U32U8 at **host+0xAC**
4. `PUSH EDI` (key); `ADD ESI, 0x1C` (pending ring at preloader+0x1C); `CALL FUN_0043e630` — GuardedVector_EraseFirstEqual_ThiscallRange (W35-A): ESI=container, stack key*, RET 4
5. `POP EDI; POP ESI; RET 4`

### Callers (1)

| Site | Function | Setup |
|------|----------|--------|
| `0x004ed3a5` | `FUN_004ed310` AssPreloader_ProcessKeyRingStep_Inferred (W35-A) | tracked key + progress_flag path |

### Callees

| VA | Name | Role |
|----|------|------|
| `0x00970b20` | SideHost_PushKeyFlag0_Inferred | notify side-host event vector |
| `0x0043e630` | GuardedVector_EraseFirstEqual_ThiscallRange | erase key from pending ring @ +0x1C |

### Canonical name (inferred)

`AssPreloader_PendingRemoveWithSideNotify_Inferred` — side-notify flag0 then erase-first-equal from AssPreloader pending ring (+0x1C). **Inferred** structural from sealed nested duals + sole caller.

### Decompile note

Live surface is incomplete on ESI/ECX split and stack key. Full ABI recovered from body hex + sealed callee ABIs.
