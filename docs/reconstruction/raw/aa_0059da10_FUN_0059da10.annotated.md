# Annotated low-level: CVOGObjective_MatchTargetEvaluators_Slot44

| Field | Value |
|---|---|
| Stable ID | `aa_0059da10` |
| VA | `0x0059da10` |
| Canonical name | `CVOGObjective_MatchTargetEvaluators_Slot44` |
| Prior | `FUN_0059da10` |
| System | missions-progression / interaction-activation |
| Date | 2026-07-29 |

## Machine-level notes

- Identical CF to sibling `aa_0059d9c0` / `CVOGObjective_MatchTargetEvaluators` except **vtable slot `+0x44`** (sibling uses `+0x40`).
- Evaluator range: half-open `[this+0x158, this+0x15c)` of objective pointers.
- First non-zero char return from slot → **1**; exhausted / empty → **0**.
- Sole xref: `FUN_005245d0` @ `0x005245d0` (pick/hover active-obj id walker on `char+0x548`).
- Call-site Ghidra often shows two formals; thiscall **ECX = objective**, stack ≈ `(character, worldObject)` — same presentation issue as `0059d9c0`.

## Pseudocode (annotated)

```c
// this = objective; argA ≈ character; argB ≈ world object (from sole caller)
undefined4 __thiscall CVOGObjective_MatchTargetEvaluators_Slot44(
    int objective, undefined4 argA, undefined4 argB)
{
  char cVar1;
  undefined4 *puVar2;

  puVar2 = *(undefined4 **)(objective + 0x158);           // begin evaluators
  if (puVar2 != *(undefined4 **)(objective + 0x15c)) {    // end
    do {
      // *puVar2 = evaluator*; (*eval)+0 = vtbl; call vtbl[+0x44]
      cVar1 = (**(code **)(*(int *)*puVar2 + 0x44))(argA, argB);
      if (cVar1 != '\0') {
        return 1;
      }
      puVar2 = puVar2 + 1;
    } while (puVar2 != *(undefined4 **)(objective + 0x15c));
  }
  return 0;
}
```

## Dual residual vs `0x0059d9c0` (memory, 2026-07-29)

| Item | This `0x0059da10` | Sibling `0x0059d9c0` |
|---|---|---|
| Range loads | `[EDI+0x158]` / `[EDI+0x15c]` | same |
| Call | `ff 50 44` | `ff 50 40` |
| Ret | `c2 08 00` | same |
| Sole caller | ForPick `0x005245d0` | ForInteract `0x00524520` |

**Pair residual = one imm8 (slot).** Sealed for static CF.

## Open questions

- Product name / role of requirement vtable **+0x44** vs **+0x40** (interact) vs **+0x38** (MatchTarget UseObject).
- Which requirement types implement non-trivial +0x44.
- Whether any type’s +0x44 body is an alias of +0x40.
