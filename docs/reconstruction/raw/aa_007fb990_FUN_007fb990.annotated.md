# Annotated low-level: FUN_007fb990 → Client_UI_Host1144_QueryAndFade_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_007fb990` |
| VA | `0x007fb990`–`0x007fb9d3` (**68 B**) |
| System | `input-drive-control` |
| Date | 2026-08-05 (R12-007 dual seal) |
| Inferred name | `Client_UI_Host1144_QueryAndFade_Inferred` |

---

## Machine-level notes

- **ABI:** host in **ESI** (not ECX). Callers typically `MOV ESI, 0x00d1a840` (`&DAT_00d1a840`) then `CALL 0x007fb990`. Plain `RET` (`C3`).
- **Field:** `host+0x1144` = NDUI object* installed by `Client_UI_CreateNDUIDialogs_InstallHost_Inferred` (`operator_new(0x488)` + `FUN_007b5dd0`).
- **vtbl+0xd0:** thiscall query → **AL** bool (same slot family as other host queries).
- **vtbl+0xe8:** thiscall → **EAX** state/index; **`-1`** skips fade.
- **vtbl+0xfc:** thiscall `(int arg0, float arg1)` with **`(0, 0.1f)`** — fade/transition (sibling sites use `0.5f` on other slots; this body is hard-imm `0x3dcccccd`).
- Prefer assembly when decompiler conflicts (`unaff_ESI` is real ESI host).

## Pseudocode (annotated)

```c
// ESI = ClientUiHost* host  (custom ESI convention)
// void Client_UI_Host1144_QueryAndFade_Inferred(void)  // plain RET
void FUN_007fb990(void)
{
  char queryOk;       // AL from vtbl+0xd0
  int  stateOrIndex;  // EAX from vtbl+0xe8
  int  host;          // unaff_ESI — sealed as ESI host

  // Gate 1: slot non-null
  if (*(int *)(host + 0x1144) != 0) {
    // Gate 2: query
    queryOk = (**(code **)(**(int **)(host + 0x1144) + 0xd0))();
    if (queryOk != '\0') {
      // Gate 3: state != -1
      stateOrIndex = (**(code **)(**(int **)(host + 0x1144) + 0xe8))();
      if (stateOrIndex != -1) {
        // Fade / transition: arg0=0, arg1=0.1f (0x3dcccccd)
        (**(code **)(**(int **)(host + 0x1144) + 0xfc))(0, 0x3dcccccd);
      }
    }
  }
  return; // C3
}
```

## Callers (15 UNCONDITIONAL_CALL)

Parent dual `FUN_00914c60` @ `00914c9e` (mode latch clear); `Client_Input_OnKeyDown_MatchAction` @ `00911199` (skill prompt cancel); `FUN_009251d0`, `FUN_007fc840`, `FUN_007fc9b0`, `FUN_007fd090`, `FUN_00935430`, `FUN_0090e3c0`, `FUN_008a1e60`, `FUN_009423b0`, plus sites `@0082914c`, `@008c4c37`, `@0090e18b`, `@0087b942`, `@0088ca71`.

## Open questions

- Product English for dialog at `+0x1144` and vtbl slots.
- Semantic of fade arg0=`0` (mode vs alpha target).
- Runtime / bit-exact.
