# Raw capture: FUN_00444810

| Field | Value |
|---|---|
| **Stable ID** | `aa_00444810` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00444810` |
| **Canonical name** | `FUN_00444810` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00444810(void)

{
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  puStack_8 = &LAB_009bd525;
  local_c = ExceptionList;
  local_4 = 0;
  ExceptionList = &local_c;
  FUN_00431ae0();
  DAT_00d1f1fc = 0;
  ExceptionList = local_c;
  return;
}
```

---

## W34-K dual append (2026-07-29)

| Field | Value |
|---|---|
| **Wave** | W34-K OWN-ONLY |
| **Tools** | `decompile_function` + `analyze_function_complete` + `read_memory` + xrefs. **No** `disassemble_bytes` |
| **Body** | `0x00444810`–`0x0044484A` exclusive (**58 B** / `0x3A`; pad `CC` then `FUN_00444850`) |
| **Preferred name** | `StringVecShell_TidyAndClearGlobal` (**Inferred** structural) |
| **ABI** | **ECX=`StringVecShell*`** (thiscall / single-arg fastcall-this); bare **`RET`** (`C3`); void. SEH `LAB_009bd525` |
| **Decompile ≢ bytes** | Decompiler shows `void` / no formal and omits ECX — **bytes + SEH sites win** (ECX preserved through prologue into `StringVec_Tidy`) |

### Full body hex (58 B)

```
64a1000000006aff6825d59b005064892500000000c744240800000000e8aed2feff8b0c24c705fcf1d1000000000064890d0000000083c40cc3
```

### Bytes-authority CF

```
// ECX = StringVecShell* (set by callers / SEH thunks); bare RET
void StringVecShell_TidyAndClearGlobal(StringVecShell* shell)
{
  // FS SEH frame: push -1; push LAB_009bd525; link fs:[0]
  StringVec_Tidy(shell);          // FUN_00431ae0 — ECX unchanged through SEH prolog
  DAT_00d1f1fc = nullptr;         // C7 05 FC F1 D1 00 00 00 00 00
  // unlink SEH; ADD ESP,0xC; RET
}
```

### SEH / caller sites (bytes)

| Site | Pattern | ECX setup |
|---|---|---|
| `Unwind@009afd09` | `MOV ECX,[EBP-0x10]; ADD ECX,0xA0; JMP 0x00444810` | host+`0xA0` |
| `Unwind@009afd4a` | `MOV ECX,[EBP+4]; ADD ECX,0xA0; JMP 0x00444810` | host+`0xA0` |

Both are MSVC unwind helpers for `Palantir_CompleteDtor` (`FUN_00754320`, SEH `LAB_009afd1c`). JMP into this body; RET returns to the unwind caller.

### Notes

- Callee: sealed `StringVec_Tidy` (`0x00431ae0`, W33-D).
- Global `DAT_00d1f1fc` is the published `StringVecShell*` alias (path list); nulled after tidy.
- Not a full vector clear keeping capacity — tidy releases buffer then drops the global alias.
- Classification: leaf-ish SEH worker (one callee).

### Classification

worker — Palantir dtor path-list unwind / shared tidy+clear-global helper.
