# Raw capture: FUN_0083a860

| Field | Value |
|---|---|
| **Stable ID** | `aa_0083a860` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0083a860` |
| **Canonical name** | `FUN_0083a860` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
bool FUN_0083a860(void)

{
  undefined4 in_EAX;
  int iVar1;
  
  iVar1 = FUN_0083a7d0(in_EAX);
  if (iVar1 != 0) {
    return *(int *)(iVar1 + 0x508) != 0;
  }
  return false;
}
```

---

## WQ9L-I re-verify append (2026-08-05)

**Agent:** OWN-ONLY dual WQ9L-I  
**Tools:** `batch_decompile` + `analyze_function_complete` + `disassemble_function` + `read_memory` + callers/xrefs + `get_assembly_context` @ call site. **No** `disassemble_bytes`. No Launcher.

### Function bounds (Ghidra)

| Field | Value |
|---|---|
| Entry | `0x0083a860` |
| Body end (inclusive) | `0x0083a87a` |
| Size | **27 B** / `0x1B` |
| Terminal | bare **`C3` RET** ×2 (success / miss paths) |
| Pad | `CC` ×5 then `FUN_0083a880` @ `0x0083a880` |

### Full body hex (`read_memory`)

```text
0083a860: 50 E8 6A FF FF FF 85 C0 74 0E 33 C9 39 88 08 05 00 00
          0F 95 C1 8A C1 C3 32 C0 C3
```

### Full disasm (authority)

```text
0083a860  PUSH EAX                 ; host* → stack arg for FUN_0083a7d0
0083a861  CALL 0x0083a7d0          ; RET 4; EBX = list_i (live)
0083a866  TEST EAX,EAX
0083a868  JZ   0083a878            ; miss → AL=0
0083a86a  XOR  ECX,ECX
0083a86c  CMP  dword ptr [EAX+0x508], ECX
0083a872  SETNZ CL
0083a875  MOV  AL,CL
0083a877  RET
0083a878  XOR  AL,AL
0083a87a  RET
```

### ABI (bytes + sole caller site)

Call site in parent `FUN_0083ac90` @ `0x0083adc2` (`get_assembly_context`):

```text
0083adbe  MOV EBX, EDI             ; list_i
0083adc0  MOV EAX, EBP             ; host*
0083adc2  CALL 0x0083a860
0083adc7  TEST AL, AL
0083adc9  JNZ  0083add7            ; has bound obj → soft refresh a880(0)
                                  ; else abf0 reload path
```

| Formal | Source | Conf |
|---|---|---|
| host* | **EAX** at entry (pushed to `0083a7d0`) | **High** |
| list_i | **EBX** (live; compared in `0083a7d0` vs GetId−`0x9ca4`) | **High** |
| return | **AL** bool: found slot with non-null `+0x508` | **High** |
| cleanup | bare **RET** (no stack cleanup; callee of a860 cleans its own PUSH) | **High** |

### Nested finder `FUN_0083a7d0` (evidence, not OWN)

- Stack host*; **EBX** list index; **RET 4**.
- Scans `host+0x50c[0..4]`; for non-null slot: `GetId` vtbl `+0x78`; match if `id - 0x9ca4 == EBX`.
- Returns matching slot object* or 0.

### Semantics

```text
slot = FindSlotByListIndex(host, list_i)   // via 0083a7d0
if slot == null: return false
return slot[+0x508] != 0                   // bound payload present
```

Parent reindex uses this on **id-match** path: false → `FUN_0083abf0` (reload); true → `FUN_0083a880(0)` (soft refresh).

### Call graph

| Direction | Target |
|---|---|
| Sole CALL caller | `FUN_0083ac90` (`ShopVehList_ReindexFiveSlotWindowAroundSelect_Inferred`) @ `0x0083adc2` |
| Callee | `FUN_0083a7d0` (slot-by-list-index finder; undualed) |

### Naming

- Ghidra: `FUN_0083a860`
- Canonical: `ShopVehList_HasSlotBoundObjAtListIndex_EaxHost_Ebx_Inferred`
- Reject: thiscall; EAX=list_i (parent clean plate had wrong comment — bytes show EAX=host, EBX=list_i); full reindex merge with `0083ac90`.
