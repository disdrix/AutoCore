# Annotated low-level: StdCopyBackward_Elem28_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0046a240` |
| VA | `0x0046a240`–`0x0046a25d` (**30 B** / `0x1E`) |
| System | stl-helpers / POD copy_backward (elem 0x1c) |
| Date | 2026-07-29 (W35-L dual seal) |
| Ghidra | `FUN_0046a240` |
| Canonical | `StdCopyBackward_Elem28_Inferred` |

## Machine-level notes

- Source: raw capture + W35-L `read_memory` full body.
- Prefer bytes for register formals: decompiler surfaces EDX as `param_2`, EAX as `in_EAX`, EBX as `unaff_EBX`; phantom `param_1` is **not** a formal.
- Classic MSVC `copy_backward` shape: walk exclusive ends backward by element size; `CMP` flags survive `REP MOVSD` (MOVS does not clobber status flags).
- Stride **0x1c** sealed by `SUB EDX/EAX, 0x1C` and `MOV ECX, 7` + `REP MOVSD`.
- Empty range: `EBX == EDX` at entry → immediate `RET`.
- Leaf; bare `RET`.
- Next symbol `FUN_0046a260` = sibling uninit fill-n (different ABI).

## Pseudocode (bytes-sealed)

```c
// EAX = dest_end*; EDX = src_end*; EBX = src_begin*
// bare RET; leaf; ECX clobbered
void StdCopyBackward_Elem28_Inferred(
    /*EAX*/ uint32_t* dest_end,
    /*EDX*/ uint32_t* src_end,
    /*EBX*/ const uint32_t* src_begin)
{
  while (src_begin != src_end) {
    src_end  -= 7; // -0x1c bytes
    dest_end -= 7;
    // copy 7 dwords: *dest_end = *src_end (element)
    for (int i = 0; i < 7; ++i)
      dest_end[i] = src_end[i];
  }
}
```

## Role in parents

Used by elem-28 insert-n mid-shift arms (`FUN_0045cd30`, `FUN_00466ea0`, `FUN_00469f50`): after opening a hole by extending `end`, reverse-move the tail so overlapping ranges stay correct, then assign-fill the insertion slots (`FUN_0045efe0`).

## Open questions

- Product/MSVC demangle (`std::copy_backward` / `_Copy_backward`).
- Concrete POD T identity.
- Runtime / bit-exact / differential.
