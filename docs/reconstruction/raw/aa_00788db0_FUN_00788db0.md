# Raw capture: FUN_00788db0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00788db0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00788db0` |
| **Canonical name** | `FUN_00788db0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_00788db0(undefined4 *param_1,uint param_2)

{
  int iVar1;
  uint uVar2;
  int iVar3;
  undefined4 *puVar4;
  
  if (4 < (int)param_2) {
    iVar3 = (param_2 - 5 >> 2) + 1;
    puVar4 = param_1;
    for (iVar1 = iVar3; iVar1 != 0; iVar1 = iVar1 + -1) {
      *puVar4 = 0;
      puVar4 = puVar4 + 1;
    }
    param_2 = param_2 + iVar3 * -4;
    param_1 = param_1 + iVar3;
  }
  if (0 < (int)param_2) {
    for (uVar2 = param_2 >> 2; uVar2 != 0; uVar2 = uVar2 - 1) {
      *param_1 = 0;
      param_1 = param_1 + 1;
    }
    for (param_2 = param_2 & 3; param_2 != 0; param_2 = param_2 - 1) {
      *(undefined1 *)param_1 = 0;
      param_1 = (undefined4 *)((int)param_1 + 1);
    }
  }
  return 1;
}
```

---

## W37-O append (2026-08-04) — OWN-ONLY dual seal

| Field | Value |
|---|---|
| **Wave** | W37-O |
| **Tools** | `batch_decompile` / `decompile_function` + `analyze_function_complete` + `read_memory` + xrefs/callers. **No** `disassemble_bytes`. |
| **Body** | `0x00788db0`–`0x00788dfc` exclusive (**76 B** / `0x4C`); pad `CC` then next @ `0x00788e00` |
| **Ghidra inclusive end** | last insn `RET` (`C3`) @ `0x00788dfb` |
| **ABI** | **cdecl** — stack **`dest*`**, stack **`size`** (signed compare); bare **`RET`** (`C3`); returns **`1`** in EAX |
| **Semantic name** | `MemZero_DestSize_ReturnOne_Inferred` |
| **Classification** | leaf (0 callees) |
| **Callers (3 CODE)** | `FUN_00497920` @ `0x0049796b`; `FUN_00497c80` @ `0x00497cbf`; `FUN_00498380` @ `0x004985c4` |
| **Callees** | none (`REP STOSD` / `REP STOSB` only) |
| **Live decompile** | ≡ scaffold raw (2026-07-23) CF; **bytes win** on `REP STOS*` form (decompiler expands as dword/byte stores) |
| **Reject** | scaffold `Named_CalleeOf_*Drive_NDRiver_fx_00788db0` — not structural |

### Full body hex (76 B)

```
538b5c2408568b74241083fe04578bfb7e1b8d56fbc1ea0283c20133c08bcaf3ab8d0495000000002bf08d3c1885f67e128bce8bd1c1e90233c0f3ab8bca83e103f3aa5f5eb8010000005bc3
```

### Byte-level CF

1. `PUSH EBX`; `MOV EBX,[ESP+8]` → **dest***; `PUSH ESI`; `MOV ESI,[ESP+10]` → **size** (after two pushes); `PUSH EDI`; `MOV EDI,EBX`.
2. `CMP ESI,4` / `JLE` skip bulk: if **signed size ≤ 4**, skip first stage.
3. Bulk: `LEA EDX,[ESI-5]`; `SHR EDX,2`; `ADD EDX,1` → dword count `floor((size-1)/4)`; `XOR EAX,EAX`; `MOV ECX,EDX`; **`REP STOSD`**.
4. `LEA EAX,[EDX*4]`; `SUB ESI,EAX` remaining; `LEA EDI,[EAX+EBX]` advance dest.
5. Tail (if remaining **signed > 0**): copy rem to ECX/EDX; `SHR ECX,2`; `XOR EAX,EAX`; **`REP STOSD`**; `MOV ECX,EDX`; `AND ECX,3`; **`REP STOSB`**.
6. `POP EDI`; `POP ESI`; `MOV EAX,1`; `POP EBX`; **`RET`**.

### Semantics (sealed)

- Zero **`size`** bytes at **`dest`** when size > 0 (signed); no-op when size ≤ 0.
- Always return **1** (success constant) — not a classic CRT `memset` return-dest.
- Call sites: ListTrackedCtx bank zero `this+0x14, 0xA0` (ctor/clear); flair rebuild `operator_new__(0x52000)` buffer zero (`FUN_00498380`).

### Related (not owned)

| VA | Role |
|---|---|
| `0x00497920` | `ListTrackedCtx_Ctor_Inferred` (W32-P) — calls with `(this+0x14, 0xA0)` |
| `0x00497c80` | `ListTrackedCtx_ClearDestroyAndReset_Inferred` (W33-R) — same bank zero |
| `0x00498380` | flair/table rebuild — zeros 0x52000 heap block @ `this+0xB4` |
