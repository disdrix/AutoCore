# Annotated low-level: FUN_0041a570 → StdMap_InsertOrFind_CharKey_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0041a570` |
| VA | `0x0041a570` |
| Body | `0x0041a570`–`0x0041a628` exclusive (**184 B** / `0xB8`) |
| System | skills-abilities / STL map-set (char-key isnil@+0x21) |
| Date | 2026-08-05 (R13-021 OWN dual) |
| Canonical | `StdMap_InsertOrFind_CharKey_Isnil21_Inferred` |

## Machine-level notes

- Source: raw capture for `aa_0041a570` + R13-021 live re-verify.
- Prefer assembly when decompiler conflicts (none found this dual).
- **ABI:** `__thiscall` ECX=map; stack `InsertPair* out`, `const void* value` (char key @ value+0); **`ret 0x8`**.
- **Layout:** node left@0 / parent@4 / right@8 / **char key @+0x0C** / color@+0x20 / **isnil@+0x21**; map head@+4.
- **Role:** MSVC unique **insert-or-find** for char-key isnil21 family. Lower-bound walk with `*key < node.key`; begin-left insert / predecessor / strict `node.key < key` insert / else equal `{it,0}`.
- Insert worker: dualed `StdTree_InsertAndRebalance_CharKey_Isnil21_Inferred` (`0x0041ba30`, R12-011 parent dual).
- Predecessor: residual `FUN_005adff0` (isnil21 tree `--`).
- Peers (do **not** merge): pair-key isnil21 `00407060`; Val16 always-insert `00407200`; this unit's always-insert child `0041ba30`.

## Pseudocode (annotated)

```c
// __thiscall; RET 0x8
// out: { Node* it; uint8_t inserted; }  // inserted @ out+4
void __thiscall StdMap_InsertOrFind_CharKey_Isnil21_Inferred(
    MapShell *map,          // ECX
    InsertPair *out,        // stack+4
    const char *value)      // stack+8 — char key at *value
{
  char *key = value;
  Node *where = map->head;           // [map+4]
  bool goLeft = true;

  // lower-bound walk from root = head->parent ([head+4])
  if (where->parent->isnil == 0) {   // isnil @ +0x21
    Node *child = where->parent;
    do {
      where = child;
      goLeft = (*key < *(char *)(where + 0x0C));  // char key @ +0x0C
      if (goLeft)
        child = where->left;   // +0
      else
        child = where->right;  // +8
    } while (child->isnil == 0);
  }

  // local where_slot = where (decomp reuses param_3)
  if (goLeft) {
    if (where == map->head->left) {  // insert before begin
      // InsertAndRebalance(map, &where_slot, addLeft=1, where, value)
      Node **p = FUN_0041ba30(&where_slot, 1, where, key);
      out->inserted = 1;
      out->it = *p;
      return;
    }
    FUN_005adff0(&where_slot);  // predecessor; ECX = &where_slot
    where = where_slot;         // reloaded after pred
  }

  // strict miss: where.key < key → insert; else equal
  if (*(char *)((int)where + 0x0C) < *key) {
    Node **p = FUN_0041ba30(&where_slot, goLeft, where, key);
    out->it = *p;
    out->inserted = 1;
    return;
  }

  out->inserted = 0;
  out->it = where;
  return;
}
```

## Call sites (xrefs)

| From | Function | Notes |
|---|---|---|
| `0x007eba53` | `FUN_007eaf20` | `MOV ECX,ESI`; value built via `FUN_0041c140` (char key front) |
| `0x007ec6e4` | `FUN_007eaf20` | same pattern, second site |
| `0x0059ab1c` | `FUN_0059a940` | load path; map @ host+0x51c; char key from fread |

## Open questions

- Product / PDB demangle for map value_type (char key + residual payload via `0041c140`).
- Full dual of predecessor `FUN_005adff0` and host callers.
- Runtime / bit-exact / differential.
