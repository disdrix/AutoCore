# Annotated low-level: StdDestroyRange_RcElem28 (`FUN_00461780`)

| Field | Value |
|---|---|
| Stable ID | `aa_00461780` |
| VA | `0x00461780`–`0x00461797` exclusive (**23 B** / `0x17`) |
| System | MSVC destroy-range / refcounted elem stride **0x1c** |
| Date | 2026-07-29 (W35-O) |
| Canonical name | `StdDestroyRange_RcElem28` (**Inferred**) |

## Machine-level notes

- Decompiler shows `in_EAX` / `unaff_EDI` — **bytes seal** register ABI: **EAX=begin**, **EDI=end**.
- ESI used as cursor (push/pop); stride **`ADD ESI, 0x1C`**.
- Sole callee: `FUN_00460d70` — per-element Release of shared resource `@+0x10` (refcount-- / vtbl+8 when 0). Nested not owned.
- Sits immediately before parent insert-n `FUN_004617a0` @ `0x004617a0` (pad `CC` between).

## Sealed signature

```c
// register ABI; RET 0
void StdDestroyRange_RcElem28(
    RcElem28* begin,   /* EAX */
    RcElem28* end);    /* EDI — exclusive */
```

## Pseudocode (annotated)

```c
void StdDestroyRange_RcElem28(RcElem28* begin /*EAX*/, RcElem28* end /*EDI*/)
{
  RcElem28* cur = begin;  // ESI
  if (cur == end) return;
  do {
    FUN_00460d70(cur);    // Release resource @ cur+0x10 (EAX=cur)
    cur = (RcElem28*)((char*)cur + 0x1c);
  } while (cur != end);
}
```

## Callers

| Site | Role |
|---|---|
| `FUN_004617a0` @ `0x00461947` | Grow path: destroy old buffer `[begin,end)` then `operator delete` |
| `Catch@0046197a` @ `0x00461982` | EH cleanup during insert-n |
| `Catch@00461a34` @ `0x00461a51` | EH cleanup during insert-n |

## Open questions

- Product class English for T.
- Full plate of `FUN_00460d70` / resource vtbl (not owned).
- Runtime / bit-exact open.
