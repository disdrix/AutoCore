# Raw capture: FUN_00570f70

| Field | Value |
|---|---|
| **Stable ID** | `aa_00570f70` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00570f70` |
| **Canonical name** | `FUN_00570f70` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00570f70(void *param_1)

{
  int *piVar1;
  int iVar2;
  int *local_8;
  undefined4 local_4;
  
  local_4 = 0;
  FUN_004294f0();
  iVar2 = FUN_004022a0(&local_4,&local_8);
  piVar1 = local_8;
  while (local_8 = piVar1, iVar2 == 0) {
    if (piVar1 != (int *)0x0) {
      (**(code **)(*piVar1 + 0x158))(0);
      if (piVar1[0x29] == 0) {
        (**(code **)*piVar1)(1);
        local_8 = (int *)0x0;
      }
      else {
        FUN_004d4790(piVar1);
      }
    }
    iVar2 = FUN_004022a0(&local_4,&local_8);
    piVar1 = local_8;
  }
  if (*(char *)((int)param_1 + 0x54) != '\0') {
    *(undefined1 *)((int)param_1 + 0x54) = 0;
    LeaveCriticalSection((LPCRITICAL_SECTION)((int)param_1 + 0x30));
  }
  FUN_004bc580();
  InventoryGrid_AllocateCellArray_Inferred(param_1);
  return;
}
```

---

## MEGA-090 re-verify (2026-08-05)

**Tools:** Ghidra HTTP/`decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_by_address` + `get_function_callers` / `get_function_callees` / `get_xrefs_to`. **No** `disassemble_bytes`. No Launcher.

| Field | Value |
|---|---|
| Body | `0x00570f70`–`0x0057100d` inclusive (**158 B** / `0x9E`) |
| Next | pad `CC CC` then `InventoryGrid_FindItemByCoid` `0x00571010` |
| ABI | **ECX = InventoryGrid*** (`__fastcall` / thiscall-shaped); void; bare **`RET`** (`C3`) |
| Classification | **worker** |
| Live decompile ≡ raw body CF | **Yes** (2026-07-23 scaffold matched 2026-08-05 live) |

### Full body hex (158 B)

```

```

### Assembly-sealed ECX/EDI map (decompiler drops list this)

```text
MOV  EBP, ECX                 ; grid*
LEA  EDI, [EBP+0x2c]          ; embedded List host at grid+0x2c
MOV  ECX, EDI
CALL List_TraversalLock       ; 0x004294f0
...
MOV  ECX, EDI
CALL List_IterateNext         ; 0x004022a0  (LockedList_TryAdvanceIterator_Inferred)
...
; item payload in ESI from cursor out-param
CALL [vtbl+0x158](0)
CMP  [ESI+0xa4], 0
JNZ  → FUN_004d4790(ESI)      ; queue-for-delete
JZ   → (*vtbl)(ESI, 1)        ; scalar-deleting dtor; null cursor
...
; inline TraversalUnlock on list:
CMP  byte [EDI+0x28], 0       ; = grid+0x54
JZ   skip
MOV  byte [EDI+0x28], 0
LeaveCriticalSection(EDI+4)   ; = grid+0x30
MOV  ECX, EDI
CALL List_RemoveAll           ; 0x004bc580
MOV  ECX, EBP
CALL InventoryGrid_AllocateCellArray_Inferred  ; 0x00570720
RET
```

### Callers (3 UNCONDITIONAL_CALL)

| Caller | Site | ECX setup |
|---|---|---|
| `FUN_005725a0` InventoryGrid complete-dtor path | `0x005725cf` | grid this preserved (`MOV EDI,ECX` then CALL) |
| `FUN_005876c0` | `0x005876ca` | `MOV ECX,[ECX+0x35c]` → grid* |
| `FUN_00884720` | `0x0088473a` | `ECX = *(*(host+0x510)+0xce0)` cargo/grid slot |

### Callees

| VA / symbol | Role |
|---|---|
| `0x004294f0` List_TraversalLock | lock embedded list |
| `0x004022a0` LockedList_TryAdvanceIterator_Inferred | walk payloads under lock |
| vtbl+0x158(item, 0) | detach/unbind virtual (item) |
| `0x004d4790` | queue object for delete when `item+0xa4 ≠ 0` |
| item vtbl[0](1) | scalar-deleting dtor when `item+0xa4 == 0` |
| LeaveCriticalSection (IAT) | inline unlock |
| `0x004bc580` List_RemoveAll | drain residual list nodes (requires unlock first) |
| `0x00570720` InventoryGrid_AllocateCellArray_Inferred | free old cells + re-empty occupancy |

### Sealed semantics

1. **Not** a pure cell free: tears down every list payload item, then `List_RemoveAll`, then **re-allocates empty** cells via `AllocateCellArray`.
2. Embedded list host at **`grid+0x2c`** shares TraversalLock / IterateNext / RemoveAll layout (`CS@+4`, head@`+0x1c`, lock@`+0x28`).
3. Twin thin path **`FUN_00570f50`**: RemoveAll + Allocate only (no item walk) — used on alternate cargo clear.
4. Complete-dtor parent `FUN_005725a0` calls this then `operator_delete` of `grid+0x28` cells.

### Name (MEGA-090)

`InventoryGrid_ClearItemsAndReEmptyCells_Inferred` (partition hint `InventoryGrid_FreeCellArray` is incomplete — body re-allocates empty cells after item teardown; retail symbol open → `_Inferred`).
