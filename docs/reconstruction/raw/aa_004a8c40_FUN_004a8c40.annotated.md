# Annotated low-level: FUN_004a8c40

| Field | Value |
|---|---|
| Stable ID | `aa_004a8c40` |
| VA | `0x004a8c40` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_004a8c40`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
undefined4 __thiscall FUN_004a8c40(int param_1,int param_2,int param_3)

{
  if ((((-1 < param_2) && (param_2 < *(int *)(param_1 + 0x10))) && (-1 < param_3)) &&
     (param_3 < *(int *)(param_1 + 0x14))) {
    return *(undefined4 *)
            (*(int *)(param_1 + 0x3a4) + (*(int *)(param_1 + 0x14) * param_2 + param_3) * 4);
  }
  return 0;
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.
