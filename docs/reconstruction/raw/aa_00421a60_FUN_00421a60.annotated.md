# Annotated low-level: FUN_00421a60 → StdTree_Max_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00421a60` |
| VA | `0x00421a60`–`0x00421a7c` exclusive (**28 B**) |
| System | MSVC std `_Tree` max / rightmost (isnil@+0x21); partition skills-abilities |
| Date | 2026-08-05 R13-001 |

## Machine-level notes

- Source: raw capture + R13-001 re-verify (`read_memory` + `disassemble_function`).
- Prefer assembly when decompiler conflicts — **void return is a defect**.
- **cdecl**: `MOV EAX,[ESP+4]` loads node*; plain `RET`; no ECX this.
- **isnil@+0x21**: `CMP byte ptr [ECX+0x21],0` (not color; color is `+0x20` on same node family).
- **right@+8**: walk `node[2]` only (not left@+0 → not min).
- Empty-right: early `JNZ` to `RET` with **EAX still = start node** (not the nil child).
- Non-empty: loop `MOV EAX,ECX; MOV ECX,[EAX+8]; CMP isnil; JZ loop` → EAX = last non-nil.
- Call-site seal (`FUN_00405650` @ `0x0040573e` and peers): `PUSH node; CALL; ADD ESP,4; MOV [head+8],EAX`.

## Pseudocode (decompiler display — not ABI-authoritative)

```c
void FUN_00421a60(int param_1)
{
  char cVar1;
  int iVar2;
  
  iVar2 = *(int *)(param_1 + 8);
  cVar1 = *(char *)(iVar2 + 0x21);
  while (cVar1 == '\0') {
    iVar2 = *(int *)(iVar2 + 8);
    cVar1 = *(char *)(iVar2 + 0x21);
  }
  return;
}
```

## Byte-faithful CF

```
right = node->right          // +8
if right.isnil: return node  // isnil@+0x21
loop:
  node = right
  right = node->right
  if !right.isnil: continue
return node
```

## Layout (isnil21 / node 0x28 family)

| Offset | Field |
|---|---|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x20 | color |
| +0x21 | isnil |

## Callers / callees

| Direction | Targets |
|---|---|
| **Callees** | *(none — leaf)* |
| **Callers (7)** | erase isnil21: `FUN_00405650`, `FUN_004e3e70`, `FUN_00511c00`, `FUN_0059d300`, `FUN_005a2ea0`, `FUN_005a4590`, `FUN_005ae6d0` |

## Open questions

- Product English / MSVC demangle for map value_type of this node size (Val16 / 0x28).
- Twin min `FUN_004cb2c0` dual is separate OWN (R13-002).
- Runtime / bit-exact / differential.
