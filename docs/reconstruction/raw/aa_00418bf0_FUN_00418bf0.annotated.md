# Annotated low-level: StdTree_Max_Val12 (FUN_00418bf0)

| Field | Value |
|---|---|
| Stable ID | `aa_00418bf0` |
| VA | `0x00418bf0` |
| Body | `0x00418bf0`–`0x00418c0b` exclusive (**27** B) |
| Canonical | `StdTree_Max_Val12` (inferred) |
| Ghidra | `FUN_00418bf0` |
| System | MSVC std `_Tree` max / rightmost (Val12 node) |
| Date | 2026-07-29 (W28-O seal; scaffold 2026-07-23) |

## Machine-level notes

- **ABI:** **`__cdecl`**; one stack arg `node*`; returns **`node*`** in **EAX**; pure **`ret`** (`C3`).
- **Leaf:** 0 callees.
- **Node layout (Val12 / 0x1c):** same as Min twin — **`isnil@+0x19`**.
- **Semantics:** classical MSVC `_Tree_max(node)`:
  1. If `node->right` is isnil → return `node`
  2. Else walk right while child is non-nil; return last non-nil
- **Pair:** `StdTree_Min_Val12` @ `0x005adfa0` (left walk).
- **Neighbor:** `StdTree_Rrotate_Val12` @ `0x00418c10` immediately after pad.
- **Decompiler caveat:** live decompile is **void** and walks into the nil sentinel — **incorrect**. Prefer body hex / clean.
- **Shared:** 6 callers — Val12 erase paths that recompute `head->right` (end / rightmost) after unlink.

## Pseudocode (byte-accurate)

```c
// __cdecl; node* → node*  (Tree_max / rightmost non-nil of subtree)
int32_t *StdTree_Max_Val12(int32_t *node)
{
  int32_t *right = (int32_t *)node[2];        /* node->right @ +8 */
  if (*(char *)((int)right + 0x19) != '\0')   /* right isnil */
    return node;
  do {
    node = right;
    right = (int32_t *)node[2];
  } while (*(char *)((int)right + 0x19) == '\0');
  return node;
}
```

## Body hex

```
8b4424048b480880791900750e8d49008bc18b48088079190074f5c3
```

## Live vs raw

- Live 2026-07-29 decompile ≡ 2026-07-23 raw (same defective void CF).
- **Seal uses `read_memory` bytes**, not decompiler return type.

## Open questions

- Product / MSVC demangle per map instantiation.
- Runtime / bit-exact golden not run.
