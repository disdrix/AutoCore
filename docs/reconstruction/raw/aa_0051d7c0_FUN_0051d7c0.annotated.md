# Annotated low-level: Map_EraseRange_Isnil15 (`FUN_0051d7c0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0051d7c0` |
| VA | `0x0051d7c0`–`0x0051d873` inclusive (**180 B**) |
| Canonical name | `Map_EraseRange_Isnil15` |
| Ghidra name | `FUN_0051d7c0` |
| System | STL / map-set (isnil@+0x15; WQ-009 depth-2 residual) |
| Date | 2026-08-04 WQ9E-J OWN dual |

## Machine-level notes

- Source: raw capture + live 2026-08-04 re-verify (`decompile` + `read_memory`; **no** `disassemble_bytes`).
- **isnil @ `+0x15`** discriminates this from dualed `Map_EraseRange` / `Map_EraseRange_B` (`+0x29`).
- Full clear frees via **`FUN_0051be50`** (isnil@+0x15 recursive free of non-nil subtree).
- Partial erase calls **`FUN_0051ce00`** (single-node erase; throws `"invalid map/set<T> iterator"` — STL identity).
- Both exits: **`ret 0x0c`** (three stack args).
- Decomp omits formal `this` restore before `FUN_0051ce00`; bytes use `MOV ECX, EDI` (map in EDI).

## Pseudocode (annotated)

```c
// __thiscall ECX = map*  (head @ +4, size @ +8)
// stack: out_it*, first*, last*   RET 0x0C
void __thiscall Map_EraseRange_Isnil15(
    MapFacade* map,
    TreeNode** out_it,
    TreeNode* first,
    TreeNode* last)
{
  TreeNode* head = map->head; // [map+4]

  // Full clear: [begin, end) == [*head, head]
  if (first == head->left /* *head */ && last == head) {
    FUN_0051be50(head->parent); // free non-nil root subtree
    head->parent = head;
    map->size = 0;
    head->left = head;
    head->right = head;
    *out_it = head->left; // *head after reset
    return; // ret 0x0c
  }

  while (first != last) {
    // In-order successor (isnil @ +0x15)
    TreeNode* succ = first;
    if (/* !isnil(first) */ *(char*)((char*)first + 0x15) == 0) {
      // … right-min or parent climb (standard MSVC tree successor) …
    }
    // Bytes: push node; lea eax,&first; push eax; mov ecx, map; call FUN_0051ce00
    FUN_0051ce00(map, &first, first);
    first = succ;
  }
  *out_it = first;
  return; // ret 0x0c
}
```

## Callers

| Caller | Site | Role |
|---|---|---|
| `FUN_0051e150` | `0x0051e162` | map tidy peer (erase full + free head) for isnil15 family |
| `FUN_0052f260` | `0x0052f533` | large host dtor; full-range erase map at `this-300` then delete head |

## Open / residual

- Product `map<K,V>` / `set<K>` English and value size.
- Dual of free `FUN_0051be50` / erase `FUN_0051ce00` (not OWN).
- Runtime / bit-exact.
