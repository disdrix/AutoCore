# Annotated low-level: FUN_0094e530 → Client_DebugCmd_AddDisciplinePoints

| Field | Value |
|---|---|
| Stable ID | `aa_0094e530` |
| VA | `0x0094e530`–`0x0094e617` exclusive (**231 B**) |
| Canonical | `Client_DebugCmd_AddDisciplinePoints` |
| Ghidra | `FUN_0094e530` |
| System | missions-progression / client debug slash |
| Dual | W31-T 2026-07-29 |

---

## Purpose

Debug slash-command handler for **`adddisciplinepoints`**:

1. Continue `strtok(NULL, delim)` for **key** and **amount** (`atoi`); fail → return **1**.
2. Optional third token: if `atoi == 1`, also set mapped **max** field.
3. Require local character `*(client+0xe98)`.
4. `CVOGCharacter_SetMissionCounter(char, key, amount)` — absolute set node `+0x14`.
5. If flag: `FUN_0052ade0(char, key, amount)` — set node `+0x10` (clamp ≤ `+0x14`), dirty `char+0x634 |= 0x40`.
6. If UI host `client+0x1074` active (`vtbl+0x3d8`), refresh via `vtbl+0x448`.
7. Return **0** success.

Help plate: `"Set discipline [x] to [y], [1] to set max as well"`.

---

## Signature (machine-sealed)

```c
// __thiscall ECX=client; stack (unused_cmdctx?, char* delim); ret 8
// returns 1 = blocked/parse fail; 0 = applied
uint32_t __thiscall Client_DebugCmd_AddDisciplinePoints(
    void *client /*ECX*/,
    void *unused_stack0,
    char *delim);
```

---

## Control flow (annotated)

```
sub esp, 8; save regs; this → stack local
strtok(NULL, delim) → token0; null → ret 1
atoi(token0) → key
strtok(NULL, delim) → token1; null → ret 1
atoi(token1) → amount
flag = false
strtok(NULL, delim) → optional; if present flag = (atoi==1)
char* = *(client+0xe98); null → ret 1
SetMissionCounter(char, key, amount)          // +0x14 absolute
if flag: FUN_0052ade0(char, key, amount)      // +0x10 max + dirty 0x40
ui = *(client+0x1074)
if ui && ui->vtbl+0x3d8(): ui->vtbl+0x448()
return 0
```

---

## Offsets / imports

| Item | Value |
|---|---|
| `client+0xe98` | local character |
| `client+0x1074` | UI host (discipline/progress panel family) |
| UI `vtbl+0x3d8` | active/visible probe |
| UI `vtbl+0x448` | refresh/close-family slot |
| IAT strtok | `*0x009c6684` |
| IAT atoi | `*0x009c667c` |
| Registration | `Client_RegisterDebugSlashCommands` imm `MOV ECX,0x0094e530` with name `"adddisciplinepoints"` |

---

## Confidence

| Claim | Level |
|---|---|
| Body bounds + ret 8 | **High** (`read_memory`) |
| strtok×3 + atoi key/amount/flag | **High** |
| SetMissionCounter then optional max sync | **High** |
| Command/help strings | **High** (literal plate) |
| Product English for discipline key domain | **Inferred** |
| Stack[0x4] role | **Tentative** (unused) |
| Runtime / differential | Open |
