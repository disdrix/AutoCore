# Annotated low-level: FUN_0082f810 → UI_InteractionMenu_AddSendTell_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0082f810` |
| VA | `0x0082f810`–`0x0082f849` inclusive (**58 B** / `0x3A`) |
| System | interaction menu / tell UI (skills-abilities partition) |
| Date | 2026-08-05 (MEGA-073 OWN dual refresh) |
| Prior | 2026-07-23 scaffold (plate-only alias) |

## Machine-level notes

- **EDI** = interaction-menu host (parent `FUN_0082fe20` sets `MOV EDI,ESI` before both CALLs).
- Not standard ECX-thiscall for the leaf itself — host arrives in **EDI**; leaf then does `MOV ECX,EDI` for the virtual create.
- `PUSH ESI` / `POP ESI` frame; **no stack formals**; epilogue **`RET`** (`C3`).
- Create: `host->vtbl[+0x444](xml, 0, buttonId=0x11176, -1)` → button*.
- Label: `btn->vtbl[+0x1D8]("Send a Tell", 1, 1)` when button ≠ null.
- Slot: `*(host + 0x554) = btn` always (null-safe store).
- Sibling pattern matches dualed convoy pack `UI_InteractionMenu_AddConvoyInviteOrKick_Inferred` (`0x0082fb30`): same XML plate + vtbl create/label, different button id / caption / host slot.
- Prefer assembly when decompiler conflicts: decompiler `unaff_EDI` is correct; signature `void`/`void()` understates EDI host formal.

## Pseudocode (annotated)

```c
// EDI = MenuHost*  (parent-preserved)
// ret 0
void UI_InteractionMenu_AddSendTell_Inferred(/* EDI */ MenuHost* menu)
{
  // host->vtbl+0x444( "i_m_int_2d_btn_generic.xml", 0, 0x11176, -1 )
  UiButton* btn = menu->CreateFromXml(
      "i_m_int_2d_btn_generic.xml", 0, /*id*/ 0x11176, /*-1*/ 0xFFFFFFFFu);

  if (btn != nullptr) {
    // btn->vtbl+0x1D8( "Send a Tell", 1, 1 )
    btn->SetCaption("Send a Tell", 1, 1);
  }

  // menu+0x554 = EDI[0x155]
  menu->btnSendTell = btn;
}
```

## Sealed constants

| Item | Value | Evidence |
|---|---|---|
| Plate XML | `i_m_int_2d_btn_generic.xml` @ `0x00a721ac` | push imm + `read_memory` |
| Caption | `Send a Tell` @ `0x00a72158` | push imm + `read_memory` |
| Button id | `0x11176` | `PUSH 0x11176` |
| Create vtbl | `+0x444` | `CALL [EAX+0x444]` |
| Label vtbl | `+0x1D8` | `CALL [EDX+0x1D8]` |
| Host slot | `+0x554` | `MOV [EDI+0x554],ESI` |
| Epilogue | `POP ESI; RET` | bytes `5E C3` |

## Call graph

| Direction | Target | Notes |
|---|---|---|
| Caller | `FUN_0082fe20` @ `0x0082fedc`, `0x0082ff04` | dualed PopulateTargetButtons; EDI=ESI |
| Callee direct | none | leaf for direct calls |
| Indirect | host `vtbl+0x444`, btn `vtbl+0x1D8` | create + label |

## Open questions

- Product MSVC / RTTI name for menu host class (no RTTI on this VA).
- Exact English for create args `(0, id, -1)` and label flags `(1,1)` beyond decompiler shape.
- Command handler that consumes button id `0x11176` (not this unit).
- Runtime / bit-exact (Terminal false; no Launcher).
