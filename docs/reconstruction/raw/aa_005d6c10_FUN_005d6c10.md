# Raw capture: FUN_005d6c10

| Field | Value |
|---|---|
| **Stable ID** | `aa_005d6c10` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005d6c10` |
| **Canonical name** | `FUN_005d6c10` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __fastcall FUN_005d6c10(CVOGHBBase *param_1,uint *param_2,undefined4 param_3)

{
  uint *extraout_EDX;
  
  if (param_1->pOwnerObject != (void *)0x0) {
    FUN_004c7bd0(param_1->nPeriodMs);
    param_2 = extraout_EDX;
  }
  CVOGHBBase_RescheduleAfterFire(param_1,param_2);
  return param_3;
}
```

---

## W30-T re-verify (2026-07-29) — append only

**Tools:** `decompile_function` / `batch_decompile` + `read_memory` + `analyze_function_complete` + `get_function_by_address` + xrefs. **No** `disassemble_bytes`.

### Live decompile ≡ raw body (CF)

Live Ghidra 2026-07-29 matches scaffold CF: if owner attached → slew; always `RescheduleAfterFire`; return status ptr.

### Body / ABI (bytes) — residual corrections

| Item | Seal |
|------|------|
| Body | `0x005d6c10`–`0x005d6c37` inclusive (**0x28 / 40 B**); pad `CC` @ `0x005d6c38+`. Ghidra end exclusive `005d6c38`. |
| ABI | **thiscall** ECX=HB; stack `uint32_t *pOutStatus`; **`C2 04 00`** (`RET 4`); returns `pOutStatus` in EAX. **Not** true `__fastcall` EDX formal (decompiler residual). |
| Owner gate | `CMP DWORD PTR [ESI+0x18],0` → `pOwnerObject`. |
| Slew this | `MOV ECX,[ESI+0x24]` raw-owner stash; `PUSH [ESI+0x08]` period; `CALL FUN_004c7bd0` / `CVOGCreature_SlewFacingYaw_Inferred`. Decompiler residual: prints `FUN_004c7bd0(nPeriodMs)` without ECX=raw owner. |
| Reschedule | `PUSH` stack `pOutStatus`; `MOV ECX,ESI`; `CALL CVOGHBBase_RescheduleAfterFire`. |
| Vtbl | Sole DATA xref `0x009daef4` = `PTR_FUN_009daee8 + 0x0C` (SlewFacingYaw class tick / OnHeartBeat). |

### Classification / xrefs

- **worker** (virtual OnHeartBeat)
- **Callers:** none direct (virtual only via vtbl)
- **Callees:** `CVOGCreature_SlewFacingYaw_Inferred` (`0x004c7bd0`), `CVOGHBBase_RescheduleAfterFire` (`0x00508350`)
- **Sibling ctor:** `CVOGHB_SlewFacingYaw_ctor_Inferred` (`0x005d6b80`, W28-K)

### Name

`CVOGHB_SlewFacingYaw_OnHeartBeat_Inferred` (Ghidra `FUN_005d6c10`; structural twin of sealed ctor + slew callee + HB OnHeartBeat family).
