# Annotated low-level: FUN_0041d7b0 / ComPtr_ReleaseIfNonNull

| Field | Value |
|---|---|
| Stable ID | `aa_0041d7b0` |
| VA | `0x0041d7b0` |
| Canonical | `ComPtr_ReleaseIfNonNull` |
| System | COM / OLE DB plumbing |
| Date | 2026-07-29 |

## Machine-level notes

- **Body (13 B):** load `*slot` → if non-null → `(*iface->vtbl)[2](iface)` i.e. **IUnknown::Release** at **vtbl+8**.
- **ABI:** slot pointer in **EAX** (`IUnknown**`). No stack args; plain `ret`.
- **Does NOT write 0 back into `*slot`.** Unlike `Bstr_FreeAndNull` (`aa_0040d230`), this unit is Release-only. Callers that need a clean slot either re-clear via `aa_00437cd0` or null themselves (see inline null+Release in `00423170` for owner iface).
- **Heavy Unwind@* use:** SEH cleanup for local com_ptrs after QI.
- Parent dual `OleDb_ReleaseAccessorHandleArray` (`0x00422de0`) calls this after releasing accessors to drop the QI'd IAccessor.

## Pseudocode (annotated)

```c
// void ComPtr_ReleaseIfNonNull(IUnknown** /*in EAX*/ slot)
void FUN_0041d7b0(void)
{
  int *piVar1;          // IUnknown* iface
  int *in_EAX;          // IUnknown** slot — register arg in EAX

  piVar1 = (int *)*in_EAX;
  if (piVar1 != (int *)0x0) {
    // IUnknown::Release  — vtbl slot 2 @ +8
    (**(code **)(*piVar1 + 8))(piVar1);
    // note: *in_EAX is NOT stored 0 here
  }
  return;
}
```

## Pairing with ClearSlot

| Helper | VA | Action |
|---|---|---|
| `ComPtr_ClearSlot` | `0x00437cd0` | `*slot = 0` (no Release) |
| `ComPtr_ReleaseIfNonNull` | `0x0041d7b0` | Release `*slot` if non-null (no store 0) |

Typical QI scope:

1. ClearSlot(out)
2. QueryInterface(…, out)
3. use / ReleaseAccessor paths
4. ReleaseIfNonNull(out)

## Open questions

- Product wrapper name (if any); structural name sealed.
- Whether any caller relies on slot retaining non-null after Release (observed: locals going out of scope / SEH).
