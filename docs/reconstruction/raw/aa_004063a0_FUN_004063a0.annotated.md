# Annotated low-level: CNDHash_Ctor_00a2c2b0 (`FUN_004063a0`)

| Field | Value |
|---|---|
| Stable ID | `aa_004063a0` |
| VA | `0x004063a0`–`0x0040641c` (**125 B**) |
| System | container / CNDHash (host vtbl `00a2c2b0`) |
| Date | 2026-07-23 scaffold; **2026-08-04 WQ9H-D dual seal** |

## Machine-level notes

- Prefer assembly when decompiler conflicts (Ghidra invents `__thiscall(byte, this*)`; machine is **CL=log2 / stack this / ret 4**).
- Nested alloc uses **ESI custom ABI** (`CNDHash_AllocBucketTable_00a2c2e4`); ctor keeps ESI=this across CALL.
- Sole production create: `new(0x34)` + **log2=2** → provisional N=4 → post-alloc mask=3.
- Odd preserved: **`this+0x24` not written** (sibling CNDHash ctor pattern).

## Layout written

| Off | Init |
|-----|------|
| +0x00 | vtbl `PTR_FUN_00a2c2b0` |
| +0x04 | 0 |
| +0x08 | `1 << log2` (COUNT → becomes MASK in alloc) |
| +0x0c | 0 (live count) |
| +0x10 | 0 (bucket table*) |
| +0x14 | 0 |
| +0x18 | 0 |
| +0x1c | log2 (u8) |
| +0x1d | 0 (traversal lock) |
| +0x20 | 0 (freelist subobject begin area) |
| +0x24 | **unwritten** |
| +0x28 / +0x2c / +0x30 | 0 |

## Pseudocode (annotated)

```c
// ABI: CL = log2; stack CNDHash* self; ret 4; EAX = self
// Ghidra labels swapped roles as __thiscall(byte, this*) — reject that ABI claim.
CNDHash *CNDHash_Ctor_00a2c2b0(uint8_t log2 /*CL*/, CNDHash *self /*stack*/)
{
  // SEH LAB_009bdae2
  self->vtbl = &PTR_FUN_00a2c2b0;
  self->bucketMaskOrCount = 1u << (log2 & 0x1f); // +0x08 provisional COUNT
  self->field04 = 0;
  self->liveCount = 0;           // +0x0c
  self->bucketTable = 0;         // +0x10
  self->orderedHead = 0;         // +0x14
  self->orderedTail = 0;         // +0x18
  self->log2 = log2;             // +0x1c
  self->lockedForTraversal = 0;  // +0x1d
  // freelist vector subobject @ +0x20: zero +0x20,+0x28,+0x2c,+0x30; skip +0x24
  CNDHash_AllocBucketTable_00a2c2e4(/*ESI=*/self); // count→mask; stamp 00a2c2e4
  return self;
}
```

## Call graph (OWN + sealed neighbors)

```text
FUN_0093e7e0  (host UI/init path)
  operator_new(0x34)
  MOV CL, 2
  CALL CNDHash_Ctor_00a2c2b0          [OWN WQ9H-D]
    └─ CNDHash_AllocBucketTable_00a2c2e4  [sealed WQ9G-J]
 store → host+0x3084
```

## Open residual

- Product / PDB English for class behind vtbl `00a2c2b0`.
- Full dual of sole caller `FUN_0093e7e0` and freelist helper family (not OWN).
- Runtime / bit-exact / differential.
