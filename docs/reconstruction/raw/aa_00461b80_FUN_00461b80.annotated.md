# Annotated low-level: FUN_00461b80 → RcElem28_AssignFillRange

| Field | Value |
|---|---|
| Stable ID | `aa_00461b80` |
| VA | `0x00461b80`–`0x00461bfd` exclusive (**125 B**) |
| System | MSVC-style RcElem28 range fill-by-assign (stride **0x1c**) |
| Date | 2026-07-23 scaffold; W35-P seal 2026-07-29 |
| Canonical name | `RcElem28_AssignFillRange` (**Inferred**) |

## Machine-level notes

- **Bytes win** on ABI: **EAX=dest_first**, **EDI=value\*** (fixed), stack **dest_last\***, plain **`RET`**.
- Element **0x1c** with shared resource `@+0x10`: AddRef source (vtbl+4 when refcount becomes 1), Release dest (vtbl+8 when 0), then store pointer.
- Sole parent: `StdVector_InsertN_RcElem28` (`FUN_004617a0`) hole + mid fill paths (2 call sites).
- Reject scaffold `Named_CalleeOf_*_gfxDevice_*`.

## Signature (sealed)

```c
// EAX=dest_first*, EDI=value*, stack dest_last*; plain RET; void
void RcElem28_AssignFillRange(
    RcElem28* dest_first,      /* EAX */
    const RcElem28* value,     /* EDI */
    RcElem28* dest_last);      /* stack */
```

## Element (0x1c)

| Off | Field |
|---|---|
| +0x00..+0x0C | 4× u32 POD |
| +0x10 | shared resource* (refcount at `*(p+4)`) |
| +0x14..+0x16 | 3× u8 |
| +0x17 | not written by assign |
| +0x18 | u32 |

## Control flow (annotated)

1. `ESI = EAX`; `EBP = dest_last`; if equal → return.
2. Loop: copy dwords 0..3 from `*EDI`; AddRef `EDI->resource`; Release `ESI->resource`; store resource; copy +0x14..+0x16 and +0x18.
3. `ESI += 0x1c`; until `ESI == dest_last`.

## Full hex

See raw W35-P append (125 B).
