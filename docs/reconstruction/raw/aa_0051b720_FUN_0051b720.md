# Raw capture: FUN_0051b720

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051b720` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051b720` |
| **Canonical name** | `FUN_0051b720` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_0051b720(int param_1,int param_2,undefined4 *param_3)

{
  uint uVar1;
  undefined4 *puVar2;
  
  if (0xfffff < *(int *)(param_1 + 0xc) + param_2) {
    *(undefined4 *)(param_1 + 0xc) = 0;
  }
  puVar2 = (undefined4 *)(*(int *)(param_1 + 8) + *(int *)(param_1 + 0xc) * 2);
  for (uVar1 = (uint)(param_2 * 2) >> 2; uVar1 != 0; uVar1 = uVar1 - 1) {
    *param_3 = *puVar2;
    puVar2 = puVar2 + 1;
    param_3 = param_3 + 1;
  }
  for (uVar1 = param_2 * 2 & 3; uVar1 != 0; uVar1 = uVar1 - 1) {
    *(undefined1 *)param_3 = *(undefined1 *)puVar2;
    puVar2 = (undefined4 *)((int)puVar2 + 1);
    param_3 = (undefined4 *)((int)param_3 + 1);
  }
  *(int *)(param_1 + 0xc) = *(int *)(param_1 + 0xc) + 1;
  return;
}
```

---

## Re-verify append — WQ9R-A dual (2026-08-04)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `analyze_function_complete` + `read_memory` + callers |
| **Live decompile** | = raw body (CF identical) |
| **Body** | `0x0051b720`–`0x0051b768` exclusive (**72 B** / `0x48`); epilogue `C2 08 00`; pad `CC` |
| **ABI** | `__thiscall`; ECX = RNG host*; stack `nWords`, `dst*`; **RET 8**; **EAX = pre-increment cursor** (decompiler void is wrong) |
| **Callers (2)** | `Skill_ApplyStatusEffectLocal` @ `0x0051aafb`; `FUN_0061fdf0` @ `0x0061ffdc` |
| **Xrefs** | 2 |
| **Callees** | none (leaf; `rep movsd`/`movsb`) |
| **this** | always `CVOGReaction_RandomUnitScalar()` ? `&DAT_00d20c1c` at both call sites (`mov ecx,eax` after getter) |
| **Named** | `Rng_CopyUnitTableWindow_Inferred` |

### Epilogue / ABI bytes (`read_memory 0x0051b720` len 80)

```
8B 44 24 04          mov eax,[esp+4]        ; nWords
8B D1                mov edx,ecx            ; this (RNG host)
8B 4A 0C             mov ecx,[edx+0xC]      ; cursor
03 C8                add ecx,eax
81 F9 00 00 10 00    cmp ecx,0x100000
7C 07                jl  skip_wrap
C7 42 0C 00 00 00 00 mov dword [edx+0xC],0
...
F3 A5                rep movsd              ; copy (nWords*2)>>2 dwords
...
F3 A4                rep movsb              ; remainder nWords*2 & 3
8B 42 0C             mov eax,[edx+0xC]      ; EAX = old cursor (return)
8D 48 01             lea ecx,[eax+1]
5F                   pop edi
89 4A 0C             mov [edx+0xC],ecx      ; cursor++
5E                   pop esi
C2 08 00             ret 8
```

### Call-site pattern (ApplyStatusEffectLocal @ ~`0x0051aae8`)

```
lea  edx,[esp+buf]
push edx                 ; dst
push 0x258               ; nWords = 600
call CVOGReaction_RandomUnitScalar   ; ? &DAT_00d20c1c (0-arg; stack args reserved for next)
mov  ecx,eax
call FUN_0051b720        ; this=rng, (600, buf); ret 8 cleans both
mov  [edi+0x3C],eax      ; packet lDiceSeed = old cursor
```

Same (600, stack-buf) pattern at `FUN_0061fdf0` @ `0x0061ffdc` (return discarded; buffer later reused as TFID out-list).