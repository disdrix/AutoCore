# Raw capture: FUN_00403450

| Field | Value |
|---|---|
| **Stable ID** | `aa_00403450` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00403450` |
| **Canonical name** | `FUN_00403450` |
| **System** | string / locale / MBCS owned-string |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00403450(undefined4 *param_1,LPCWSTR param_2,UINT param_3)

{
  size_t _Size;
  int iVar1;
  void *pvVar2;
  
  if (param_2 == (LPCWSTR)0x0) {
    *param_1 = 0;
    return;
  }
  iVar1 = lstrlenW(param_2);
  _Size = (iVar1 + 1) * 2;
  if (0x80 < (int)_Size) {
    pvVar2 = malloc(_Size);
    *param_1 = pvVar2;
    if (pvVar2 == (void *)0x0) {
                    /* WARNING: Subroutine does not return */
      FUN_004048e0(0x8007000e);
    }
  }
  iVar1 = WideCharToMultiByte(param_3,0,param_2,iVar1 + 1,(LPSTR)*param_1,_Size,(LPCSTR)0x0,
                              (LPBOOL)0x0);
  if (iVar1 == 0) {
    FUN_004048c0();
  }
  return;
}
```

---

## Reconfirm (2026-07-29, OWN-ONLY dual W16-L)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` (no `disassemble_bytes`) |
| **Live decompile** | ≡ body above (null wide → `*this=0`; `lstrlenW`; size `(len+1)*2`; heap if `size>0x80`; `WideCharToMultiByte`; fail → `FUN_004048c0`) |
| **Bytes (entry)** | `53 55 8B 6C 24 0C 85 ED 8B D9 … C2 08 00` — thiscall ECX=`this`, stack wide+cp, **`ret 8`** |
| **Size threshold** | `cmp edi, 0x80` / `jle` skip malloc — threshold **Confirmed** |
| **OOM path** | `push 0x8007000E; call FUN_004048e0` (`_CxxThrowException`) |
| **IAT** | `lstrlenW`, `malloc`, `WideCharToMultiByte` |

**Integrity:** raw body unchanged; this section only reconfirms live ≡ raw.
