# Annotated low-level: CLoadNode__initSpecialFX

| Field | Value |
|---|---|
| Stable ID | `aa_005c74e0` |
| VA | `0x005c74e0`–`0x005c76ed` |
| Ghidra | `FUN_005c74e0` |
| Canonical | `CLoadNode__initSpecialFX` |
| System | client load / CLoadNode stage pipeline / special FX |
| Date | 2026-07-29 (W23-G dual seal) |

## Machine-level notes

- **String seal:** `"CLoadNode::_initSpecialFX"` @ `0x009da074`.
- **ABI seal:** ECX = stage flags host (clears `+0x62`); stack0 = node*; `RET 0x4`.
- **Return:** `1` continue; `0` when offline path loads FX master XML for an entry with `fx+10==0`.
- **Branch:** `DAT_00d1f050+0x6c==0` → hash map + `FUN_004a6390`; else live `FUN_00542790` (FxCache_ApplyWithNfxEnsure).
- **Hash lock:** byte at hash+0x1d; errors via `FUN_007a4480` + `VOG_DEBUG_STOP`.
- Sibling stage inits: `_initAnim`, `_initPhysics`, `_initFull`, etc.

## Control flow (annotated)

```c
// RET 0x4; ECX=flagHost; stack0=node
uint32_t CLoadNode__initSpecialFX(void *flagHost, void *node)
{
  FUN_0076cf00("CLoadNode::_initSpecialFX");
  int adj = *(int *)(*(int *)((int)node + 4) + 4) + (int)node;

  if (*(char *)(*(int *)(adj + 0xA8) + 0xF5) == 0) {
    int *hash = *(int **)(adj + 0x74);
    if (hash != 0) {
      // lock hash+0x1d (debug stop if already locked)
      for (entry = FUN_00411900(&iter); entry; advance) {
        if (*(int *)(DAT_00d1f050 + 0x6c) == 0) {
          // offline: map lookup by entry+0x5FC
          // FUN_00540090(entry, path, 0x400)
          // if fx && fx[+10]==0: LoadMasterFromXML(path); unlock; return 0
        } else {
          FUN_00542790(*(uint32_t *)(entry + 0x5FC), 4, 1, 0, 0, 0, 0);
        }
      }
      // unlock hash+0x1d
    }
  }
  *(char *)((int)flagHost + 0x62) = 0;
  FUN_0076cef0();
  return 1;
}
```

## Offsets

| Offset | Meaning | Conf |
|---|---|---|
| flagHost+0x62 | stage bit for special FX (cleared on done) | **High** |
| flagHost+0x51 | co-gate in caller | **High** (caller) |
| node MI +4 chain | adj this | **High** |
| adj+0xA8 / +0xF5 | feature/disabled gate | **High** |
| adj+0x74 | hash table* | **High** |
| hash+0x1d | traversal lock byte | **High** |
| entry+0x5FC | FX id key | **High** |
| fx+10 | "already loaded?" char | **High** |

## Open residual

- Product English for DAT_00d1f050+0x6c mode flag.
- Full hash table type (iterator FUN_00411900 contract).
- Runtime / bit-exact.
