# Raw capture: FUN_004c2f20

| Field | Value |
|---|---|
| **Stable ID** | `aa_004c2f20` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004c2f20` |
| **Canonical name** | `FUN_004c2f20` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_004c2f20(int param_1)

{
  if (*(int *)(param_1 + 0x1fc) != 0) {
    FUN_005d0d60();
    return;
  }
  return;
}
```

---

## Append — 2026-07-29 W21-H dual seal (live Ghidra)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` + `analyze_function_complete` / `get_xrefs_to` |
| **No** | `disassemble_bytes` |
| **Live decompile** | ≡ raw body above (cosmetic only) |
| **Machine correction** | Decompiler drops stack arg + mis-labels `__fastcall`; bytes seal **thiscall + 1 stack dword + RET 4** and **tail JMP** to `FUN_005d0d60` |

### `read_memory` @ `0x004c2f20` length 32

```
8b 89 fc 01 00 00    mov  ecx, [ecx+0x1fc]
85 c9                test ecx, ecx
74 05                jz   +5                 ; → ret 4
e9 31 de 10 00       jmp  FUN_005d0d60       ; tail; stack dword forwarded
c2 04 00             ret  4
cc ×14               int3 pad
```

Hex: `8b89fc01000085c97405e931de1000c20400cccccccccccccccccccccccccccc`

### Call site (not owned) — `Skill_ApplyEffectsOnTarget_Inferred` @ `0x00553920`

```
push skillId                 ; skill+0x5fc
mov  ecx, caster
call [caster.vtbl+0x214]     ; returns bound object*
mov  ecx, eax
call FUN_004c2f20            ; ECX=bound*, stack skillId
```

Sole xref: `0x00553936` in `Skill_ApplyEffectsOnTarget_Inferred`.

### Callee

| Address | Name | Role (context) |
|---|---|---|
| `0x005d0d60` | `FUN_005d0d60` | Skill-id table walk → handbrake / impulse / heading / status flags on bound entity |

### Sealed contract (machine)

```c
// Behavior-equivalent of bytes (decomp omits stack formal):
void __thiscall FUN_004c2f20(void *boundObj /*ECX*/, int skillId /*stack, RET 4*/)
{
  void *sub = *(void **)((char *)boundObj + 0x1fc);
  if (sub != NULL)
    return FUN_005d0d60(sub, skillId); // JMP tail
}
```
