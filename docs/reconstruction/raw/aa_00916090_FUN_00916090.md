# Raw capture: FUN_00916090

| Field | Value |
|---|---|
| **Stable ID** | `aa_00916090` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00916090` |
| **Canonical name** | `FUN_00916090` |
| **Proposed name** | `Client_Map_RaySegmentHitDistance_Inferred` |
| **System** | client camera / map collision query |
| **Capture timestamp** | `2026-07-23` (scaffold batch) |
| **Tool** | Ghidra MCP `batch_decompile` (scaffold); live re-seal `decompile_function` + `read_memory` 2026-07-29 |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
float10 FUN_00916090(float param_1,float param_2,float param_3,float param_4,float param_5,
                    float param_6)

{
  int *unaff_ESI;
  float fVar1;
  float fVar2;
  float fVar3;
  float fVar4;
  float fVar5;
  float fVar6;
  float local_80;
  float local_7c;
  float local_78;
  float local_70;
  float local_6c;
  float local_68;
  undefined4 local_64;
  float local_60;
  float local_5c;
  float local_58;
  undefined4 local_54;
  undefined1 local_50;
  undefined4 local_4c;
  undefined1 local_40 [20];
  float local_2c;
  int local_20;
  
  local_70 = param_1;
  local_6c = param_2;
  local_68 = param_3;
  local_60 = param_4;
  local_64 = 0;
  local_5c = param_5;
  local_54 = 0;
  local_50 = 0;
  local_4c = 7;
  local_58 = param_6;
  local_2c = g_flOne;
  local_20 = 0;
  FUN_0055e530(&local_70,local_40);
  if (local_20 != 0) {
    if (unaff_ESI != (int *)0x0) {
      *unaff_ESI = local_20;
    }
    fVar1 = g_flOne - local_2c;
    fVar2 = fVar1 * local_70;
    fVar5 = local_60 * local_2c;
    fVar3 = local_5c * local_2c;
    fVar6 = local_6c * fVar1;
    fVar4 = local_58 * local_2c;
    fVar1 = local_68 * fVar1;
    FUN_004e9530(&local_80,&param_1);
    local_80 = (fVar2 + fVar5) - local_80;
    local_7c = (fVar3 + fVar6) - local_7c;
    local_78 = (fVar4 + fVar1) - local_78;
    return SQRT((float10)local_80 * (float10)local_80 +
                (float10)local_7c * (float10)local_7c + (float10)local_78 * (float10)local_78);
  }
  if (unaff_ESI != (int *)0x0) {
    *unaff_ESI = 0;
  }
  return (float10)g_flZero;
}
```

---

## Live re-seal append (2026-07-29, W23-O OWN dual A/B)

| Field | Value |
|---|---|
| **Tools** | `decompile_function` @ `0x00916090` + callees `0x0055e530` / `0x004e9530`; callers `0x009168d0` / `0x0091b1c0`; `get_function_by_address`; `get_function_callers` / `get_function_xrefs`; `read_memory` entry / cast site / epilogue + constants. **No** `disassemble_bytes`. No Launcher. |
| **Body span** | `00916090` – `00916225` (Ghidra exclusive end `00916225`); ret `C2 18 00` @ `00916223` |
| **Size** | **405** bytes (`0x195`) |
| **Convention** | MSVC **stdcall** — **6 stack floats** (`RET 0x18`); return **x87 ST0** (`float10` / float32 distance). **Optional ESI** = `int*` hit-token out (not a stack arg; `unaff_ESI` in decompiler). |
| **Live decompile** | **Identical** CF to 2026-07-23 raw body (same locals / callees / returns) |

### Constants (`read_memory` length 4)

| Symbol | VA | LE bytes | float32 | Role |
|---|---|---|---:|---|
| `g_flOne` | `0x00a0f2a0` | `00 00 80 3f` | **1.0** | default hit fraction before cast; lerp `1−t` |
| `g_flZero` | `0x00a0f518` | `00 00 00 00` | **0.0** | miss return (ST0 via `fld`) |

### Machine facts (`read_memory`)

**Entry** (`00916090`):
```
55 8B EC 83 E4 F0 81 EC 80 00 00 00   ; push ebp; mov ebp,esp; and esp,~0xF; sub esp,0x80
F3 0F 10 45 08                         ; movss xmm0, [ebp+8]   = start.x
F3 0F 10 4D 14                         ; movss xmm1, [ebp+14]  = end.x  (param_4)
8B 15 44 B6 D1 00                      ; mov edx, [DAT_00d1b644]  = CVOGMap* (client map singleton)
```

**Cast dispatch** (`00916120`):
```
8B 8A A4 E4 00 00   ; mov ecx, [edx+0xE4A4]   ; map collision ctx (same field as CastTerrainHeight)
E8 …                ; call FUN_0055e530       ; thiscall: ECX=ctx, stack ray*, result*
8B 44 24 70         ; mov eax, [esp+0x70]     ; hit token (local_20)
85 C0               ; test eax,eax
0F 84 …             ; jz miss
85 F6               ; test esi,esi
74 02               ; je
89 06               ; mov [esi], eax          ; optional *hitOut = token
```

**Miss epilogue** (`00916210`):
```
85 F6 74 06 C7 06 00 00 00 00   ; if (esi) *esi = 0
D9 05 18 F5 A0 00               ; fld g_flZero
8B E5 5D C2 18 00               ; leave; ret 0x18
```

### Ray packet (stack) — sealed field map

Built at `&local_70` before cast (matches terrain-cast packet pattern from `fn_004cfe60_castTerrain.md`):

| Local | Init | Role |
|---|---|---|
| `local_70..68` | `param_1..3` | **ray start** XYZ |
| `local_64` | `0` | start W / pad |
| `local_60..58` | `param_4..6` | **ray end** XYZ |
| `local_54` | `0` | end W / pad |
| `local_50` | `0` | filter-related byte |
| `local_4c` | **`7`** | **collision filter enum** (terrain height uses 5/18; this path hardcodes **7**) |
| `local_2c` | `g_flOne` | **hit fraction t** (updated by cast; 1.0 = full segment) |
| `local_20` | `0` | **hit token / collidable** (nonzero = hit) |
| `local_40[20]` | (result scratch) | cast result blob (2nd stack arg to `0055e530`) |

### Closed form (sealed)

```
// map = *(DAT_00d1b644); ctx = *(map + 0xe4a4)
// Cast segment start→end via FUN_0055e530(ctx, &ray, &result)
// filter = 7; t0 = 1.0; hit0 = 0

if (hit == 0) {
  if (esi) *esi = 0;
  return 0.0f;
}
if (esi) *esi = hit;
// hitPoint = lerp(start, end, t) = start*(1-t) + end*t
// return |hitPoint - start|   // == |t| * |end-start| for t in R
// (decompiler: copy start via FUN_004e9530, subtract from lerp, then SQRT)
```

### Callees (live)

| VA | Name / role |
|---|---|
| `0x0055e530` | Map collision cast wrapper — `thiscall` ECX=`*(map+0xe4a4)`; stack `ray*`, `result*`; thin → `FUN_006cad80` (verified in `fn_004cfe60_castTerrain.md`) |
| `0x004e9530` | `Math_CopyFloat3ToFloat4_Inferred` — copies start XYZ to scratch (W dirty); dual sealed sibling |

### Callers (live xrefs)

| Site | Function | Role sample |
|---|---|---|
| `0x00916e3d` | `FUN_009168d0` | camera collision / look occlusion loop (8-iter probe) |
| `0x0091b6bd` | `FUN_0091b1c0` | camera helper near `VOG_DEBUG_STOP` string; vertical-ish segment `(x, y+ε, z)→(x,y,z)` |

### Prior scaffold alias

`Named_CalleeOf_Named_VOG_DEBUG_STOP_00916090` — **too narrow** (one caller string path; also used by `FUN_009168d0`).

### Not claimed in raw

- Product English method name on camera class
- Full layout of cast **result** blob at `local_40`
- Semantic English of filter enum value **7** vs terrain 5/18
- Whether `DAT_00d1b644` null is possible (entry loads it unconditionally)
- Runtime live hit correlation
