# Annotated low-level: CVOGHBSkillBase_dtor_Thunk_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00548f90` |
| VA | `0x00548f90`–`0x00548f94` inclusive (**5 B**) |
| Canonical name | `CVOGHBSkillBase_dtor_Thunk_Inferred` |
| Ghidra | `thunk_FUN_00578a60` |
| System | skills-abilities / CVOGHBSkillBase lifecycle |
| Parent | `0x00578a60` `CVOGHBSkillBase_dtor` (R12-026) |
| Date | 2026-08-05 (R13-006 OWN dual; prior scaffold 2026-07-23) |

## Machine-level notes

- **Authoritative body is JMP only** — `E9 CB FA 02 00` → `0x00578a60`.
- Ghidra decompile **follows the thunk** and prints the parent complete-dtor CF; **do not treat expanded decompile as local instructions**.
- Prefer assembly / `read_memory` when decompiler conflicts (this case: always conflicts).
- Parent dual (R12-026) seals complete-body semantics; this unit is a pure alias.
- Callers: shared skill subclass scalar dtor `FUN_00651190` + three EH Unwind CALL sites.

## Authoritative CF (this VA)

```text
00548f90  JMP  CVOGHBSkillBase_dtor   ; 0x00578a60
```

```c
// Clean presentation — retail is a single JMP, not a second body.
void __thiscall CVOGHBSkillBase_dtor_Thunk_Inferred(void *thisHb /* ECX */)
{
  // Bytes: E9 CB FA 02 00
  CVOGHBSkillBase_dtor(thisHb); // control transfer only; no fall-through
}
```

## Inherited parent CF (not local)

```c
// Parent @ 0x00578a60 (dualed R12-026) — decompiler expands this into the thunk:
// *this = &PTR_FUN_009d3fdc;
// if (+0x6a0) delete[] + zero;
// if (+0x6b0) delete buffer; always zero triad +0x6b0/+0x6b4/+0x6b8;
// JMP CVOGHBBase_dtor(this);
```

## Scaffold raw (preserved; NOT local truth)

```c
void __fastcall thunk_FUN_00578a60(undefined4 *param_1)
{
  *param_1 = &PTR_FUN_009d3fdc;
  // ... parent body expanded by Ghidra ...
  FUN_00508390();
  return;
}
```

## Open questions

- Product / PDB demangle for the thunk symbol itself (if any) — keep `_Inferred`.
- Runtime / bit-exact / differential (Terminal false).
