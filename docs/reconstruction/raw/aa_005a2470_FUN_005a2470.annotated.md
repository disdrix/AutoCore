# Annotated low-level: FUN_005a2470

| Field | Value |
|---|---|
| Stable ID | `aa_005a2470` |
| VA | `0x005a2470`–`0x005a248e` exclusive (**30** B) |
| System | `tnl` / NetClassRep |
| Canonical | `TNL_NetClassRepInstance_Dtor` |
| Date | 2026-07-29 (W26-R dual seal) |
| Live tools | `decompile_function` + `analyze_function_complete` + `read_memory` (no `disassemble_bytes`) |

## Machine-level notes

- **ABI:** `__thiscall` ECX=this → ESI; plain `ret`; **does not** `operator_delete` the object (see scalar deleting wrapper `FUN_005a2730`).
- **Order:** snapshot `className` at `+0x20` → install NetClassRepInstance vtbl `0x009d7bf4` → `free(name)` (IAT `0x009c6524`) → restore base ClassRep vtbl `0x009d7af4`.
- **Does not** unlink from global ClassList (`DAT_00d179a4`) — list lifetime is process/static-oriented (atexit path for CRT statics).
- **Sibling ctor:** `FUN_005a2410` / `TNL_NetClassRepInstance_Ctor` — same vtbl + strdup into `+0x20`.
- **Callers:** atexit thunk `FUN_009c3de0` (static GuaranteedOrdered ClassRep @ `DAT_00b04f40`); scalar deleting `FUN_005a2730`.

## Pseudocode (annotated)

```c
// __thiscall ECX = NetClassRepInstance*
void __fastcall FUN_005a2470(void **self)
{
  void *name = self[8];              // +0x20 className (owned heap)
  *self = &PTR_FUN_009d7bf4;         // NetClassRepInstance vtbl during dtor
  free(name);
  *self = &PTR_LAB_009d7af4;         // base ClassRep vtbl
}
```

## Open questions

- Whether any dynamic ClassRep path must also unlink ClassList (not this body).
- Live free of null name (CRT free(NULL) is OK) if ctor failed mid-way.
- Runtime / bit-exact.
