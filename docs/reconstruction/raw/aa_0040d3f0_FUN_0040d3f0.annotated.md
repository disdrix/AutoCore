# Annotated low-level: FUN_0040d3f0 → Math_Vec3NearlyEqual_AbsRel

| Field | Value |
|---|---|
| Stable ID | `aa_0040d3f0` |
| VA | `0x0040d3f0`–`0x0040d44a` (91 B) |
| Canonical name | `Math_Vec3NearlyEqual_AbsRel` |
| Ghidra name | `FUN_0040d3f0` |
| System | math / vec3 compare |
| Date | 2026-07-29 (W26-A dual seal) |

## Machine-level notes

- Source: live Ghidra `decompile_function` + `read_memory` (no `disassemble_bytes`).
- Register formals **ESI**/`a`, **EDI**/`b` are not in the decompiler prototype; stack holds only tolerances.
- Callee `FUN_0040d450` is sealed `Math_FloatNearlyEqual_AbsRel` (W25-T).
- Epilogue `RET 8` seals non-cdecl stack cleanup for the two float tols.
- Prologue saves EBX/EBP as scratch for the two tol loads (`mov ebx,[esp+0xc]` after push ebx pattern).

## ABI

```
// custom: ESI/EDI pointers + stdcall-ish 2 stack floats
uint32_t Math_Vec3NearlyEqual_AbsRel(
    /* ESI */ const float a[3],
    /* EDI */ const float b[3],
    float relTol,   // stack
    float absTol);  // stack; RET 8
// EAX = 1 iff all 3 components nearly equal under abs+rel; else 0
```

| Formal | Source | Conf |
|---|---|---|
| a | ESI → float[3] | **High** (loads [esi]/0/+4/+8) |
| b | EDI → float[3] | **High** (loads [edi]/0/+4/+8) |
| relTol | Stack[0x4] | **High** |
| absTol | Stack[0x8] | **High** |
| return | EAX 0\|1 | **High** |
| cleanup | `RET 8` | **High** |

Compare order per component: `NearlyEqual(b[i], a[i], relTol, absTol)` (EDI first, ESI second — matches scalar formal order a,b of the leaf).

## Pseudocode (annotated)

```c
// Ghidra: FUN_0040d3f0
uint32_t Math_Vec3NearlyEqual_AbsRel(const float *a /*ESI*/, const float *b /*EDI*/,
                                     float relTol, float absTol)
{
  if (!Math_FloatNearlyEqual_AbsRel(b[0], a[0], relTol, absTol)) return 0;
  if (!Math_FloatNearlyEqual_AbsRel(b[1], a[1], relTol, absTol)) return 0;
  if (!Math_FloatNearlyEqual_AbsRel(b[2], a[2], relTol, absTol)) return 0;
  return 1;
}
```

## Callers / callees

| Role | Symbol | Notes |
|---|---|---|
| Callee | `Math_FloatNearlyEqual_AbsRel` @ `0x0040d450` | ×3, component-wise |
| Caller | `FUN_0090f490` @ `0x0090f625` | position nearness; eps `0x358637BD` ≈ 1e-6 both tols |

## Open residual

- Product English / Torque original name (e.g. `mIsEqual` on Point3F) — no string evidence.
- Runtime FPU/SSE CW bit-exact — open.
