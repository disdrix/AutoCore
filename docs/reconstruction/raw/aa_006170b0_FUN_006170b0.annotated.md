# Annotated low-level: Skill_HB_SpawnEntities_Precheck_Inferred (`FUN_006170b0`)

| Field | Value |
|---|---|
| Stable ID | `aa_006170b0` |
| VA | `0x006170b0`–`0x00617399` |
| Canonical | `Skill_HB_SpawnEntities_Precheck_Inferred` (Ghidra leave-name `FUN_006170b0`) |
| System | `skills-abilities` / HB spawn effect subclass (`PTR_FUN_009d0ec4`) |
| Date | 2026-07-29 (W20-N OWN-ONLY dual) |
| Tools | `decompile_function` + `read_memory` (no `disassemble_bytes`) |

## Machine-level notes

- **ABI:** two stack args + **`ret 8`**. Virtual method at **`PTR_FUN_009d0ec4 + 0x1c`** (`0x009d0ee0`); sibling Execute is **vtbl+0x2c** (`0x00615020`). Body uses stack object + skill shell; effect `this` is dispatch-only / unused in recovered CF.
- **RTTI gate:** `__RTDynamicCast(obj, 0, CVOGClonedObjectBase, CVOGCreature, 0)`; fail → **return 2**.
- **Summon vector counts** via `FUN_004c3f10(creature, 0|1)` on creature `+0x1bc/+0x1c0` (and net A−B for flag 0).
- **Skill float `+0x16c`:**
  - `== 100.0f` (`DAT_00aaa7ac`) → **return 0** (skip remaining checks).
  - `== 0.0f` (`g_flZero`) → plant COID chain + **vtbl+0x210(0)**; if non-null, re-call then **`FUN_004c2e60()` (cap) ≤ `FUN_004c3f10(0)` (net count)** → **return 0x10**.
- **Histogram:** empty map header (`FUN_00439770`, isnil=1, self-links); for each TFID in creature vector A (`stride 0x10`, OOB → `DAT_009e1c58`):
  - resolve object; key default `*(obj[0x2a]+0x34)`;
  - if `skill+0x22` non-zero: remap via vtbl `+0x1d8` → `+0x250` adjust → vtbl `+0x1d4` → `FUN_005001f0` key;
  - skip if `*(byte*)(obj+0x17c) & 1`;
  - insert-or-increment count in int-key map (`Map_LowerBoundFindByIntKey` / `FUN_006178d0`).
- **Final gate:** lower-bound key `*(u32*)(skill+0x14c)`; if found and `count >= *(u8*)(skill+0xda)` → teardown map, **return 8** (“too many of that summon”); else teardown, **return 0**.
- **Decompiler residual:** both final arms marked noreturn after `operator_delete`; bytes show `mov eax,8` / `xor eax,eax` + `ret 8`.

## Control flow

```
creature = dynamic_cast<CVOGCreature*>(obj)
if !creature: return 2
netCount = FUN_004c3f10(creature, 0)
rawCountA = FUN_004c3f10(creature, 1)
if skill.float_0x16c == 100.0f: return 0
if skill.float_0x16c == 0.0f:
  plant = COID_chain(creature); r = plant->vtbl[0x210](0)
  if r:
    plant->vtbl[0x210](0); cap = FUN_004c2e60(creature)
    if cap <= netCount: return 0x10
map = empty_int_count_map()
for i in 0..rawCountA-1:
  tfid = vecA[i] or empty_tfid
  o = Object_ResolveFromTFID(tfid)
  if !o: continue
  key = o.clonebase_field  // [0x2a]+0x34
  if skill.flag_0x22: key = remap_via_vtbl_chain(o) or skip
  if o.flags_0x17c & 1: skip
  map[key]++
if map has skill.key_0x14c and map[key] >= skill.u8_0xda:
  destroy map; return 8
destroy map; return 0
```

## Skill-def offsets (this unit)

| Offset | Role |
|---|---|
| `+0x22` | char — enable key remap path |
| `+0xda` | u8 — max allowed count of key (threshold) |
| `+0x14c` | u32 — histogram key to check |
| `+0x16c` | float — 100.0 skip-all; 0.0 enable total-cap branch |

## Open questions

- Product English name of `PTR_FUN_009d0ec4` class.
- Exact product name of key at `obj[0x2a]+0x34` (clonebase id vs template id).
- Whether virtual `this` carries per-instance state used on other slots only.
- Runtime golden for return codes vs FormatFailure table.
