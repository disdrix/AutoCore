# Annotated low-level: StdSort_RanItStride16_Inferred (`FUN_00409bd0`)

| Field | Value |
|---|---|
| Stable ID | `aa_00409bd0` |
| VA | `0x00409bd0`–`0x00409cb9` inclusive (**234 B** / `0xEA`) |
| Ghidra name | `FUN_00409bd0` |
| Canonical name | `StdSort_RanItStride16_Inferred` |
| Prior scaffold | `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Client_Input_PollB_00409bd0` (**retired**) |
| System | MSVC STL `std::_Sort` specialization — random-access, **stride 16** |
| Parent context | Sole external caller `FUN_00930360` (partition plate: Client_SortTargetListByViewAngle); MEGA-043 partition row listed PollBoundActions lineage — **live xrefs falsify PollBoundActions as direct parent** |
| Date | 2026-08-05 (MEGA-043 OWN dual) |
| Tools | decompile + analyze_function_complete + disassemble_function + callers/xrefs + assembly context + read_memory. **No** `disassemble_bytes`. |

---

## Purpose

MSVC Dinkumware-style **introsort driver** for a contiguous array of **16-byte** records:

```text
StdSort_RanItStride16_Inferred(first, last, ideal, pred)
  count = (last - first) / 16
  loop:
    if count ≤ 32:
      if count > 1: InsertionSort(first, last, pred)   // FUN_0040a410
      return
    if ideal ≤ 0:
      if count > 32:
        if span_bytes > 16: MakeHeap(first, last, pred)  // FUN_0040a820
        SortHeap(first, last, pred)                      // FUN_0040a380
      else: fall through to insertion
      return
    (mid_lo, mid_hi) = Partition(first, last, pred)      // FUN_00409f90
    ideal = ideal/2 + (ideal/2)/2                        // ¾ depth budget
    recurse on smaller side; continue loop on larger
```

Not a game-logic action poll. The PollBoundActions `Named_CalleeOf_*` plate is **scaffold noise**.

---

## Signature (sealed)

```c
// __cdecl; plain RET; void
// first/last: pointers into array of 16-byte elements
// ideal: remaining recursion budget (caller seeds with element count)
// pred: bool (*)(const Elem16* a, const Elem16* b)  — less-than
void __cdecl StdSort_RanItStride16_Inferred(
    Elem16 *first,
    Elem16 *last,
    int ideal,
    bool (__cdecl *pred)(const Elem16 *, const Elem16 *));
```

| Formal | Storage after entry | Conf |
|---|---|---|
| first | `[ESP+0x10]` → EBX (`MOV EBX,[ESP+0x10]`) | **Confirmed** |
| last | `[ESP+0x20]` → EDI | **Confirmed** |
| ideal | `[ESP+0x24]` → ESI | **Confirmed** |
| pred | `[ESP+0x28]` pushed to callees | **Confirmed** |
| cleanup | plain `RET` (`C3`); locals `SUB/ADD ESP,8` | **Confirmed** |
| convention | **cdecl** (caller `ADD ESP,0x10`) | **Confirmed** |

---

## Control flow (annotated)

```c
void StdSort_RanItStride16_Inferred(
    int first, int last, int ideal, undefined4 pred)
{
  int count;
  int mid_lo;   // local_8 — partition lower cut
  int mid_hi;   // local_4 — partition upper cut

  count = last - first;
  do {
    count = count >> 4;                 // / sizeof(Elem16)
    if (count < 0x21) {                 // ≤ 32
LAB_insert:
      if (1 < count) {
        FUN_0040a410(first, last, pred); // insertion sort
      }
      return;
    }
    if (ideal < 1) {
      if (0x20 < count) {
        if (0x10 < (int)(last - first & 0xfffffff0U)) {
          FUN_0040a820(first, last, pred); // make_heap
        }
        FUN_0040a380(first, last, pred);   // sort_heap
        return;
      }
      goto LAB_insert;
    }
    FUN_00409f90(&mid_lo, first, last, pred); // unguarded partition → pair
    // mid_hi written as second out dword by partition
    ideal = ideal / 2 + (ideal / 2) / 2;      // ¾
    if ((int)(mid_lo - first & 0xfffffff0U)
        < (int)(last - mid_hi & 0xfffffff0U)) {
      StdSort_RanItStride16_Inferred(first, mid_lo, ideal, pred);
      first = mid_hi;                         // iterate right
    } else {
      StdSort_RanItStride16_Inferred(mid_hi, last, ideal, pred);
      last = mid_lo;                          // iterate left
    }
    count = last - first;
  } while (true);
}
```

---

## Call graph

| Role | Target | Notes |
|---|---|---|
| External caller | `FUN_00930360` @ `0x009305fc` | Builds vector of 16-byte `{obj*, score…}`; sorts by pred `LAB_0092cdd0` |
| Self | `0x00409c3e`, `0x00409c4f` | Recurse smaller half |
| Partition | `FUN_00409f90` | Median-ish unguarded partition; writes pair out |
| Insertion | `FUN_0040a410` | Small-range finish (≤32 elems) |
| Make heap | `FUN_0040a820` | Depth-exhausted path |
| Sort heap | `FUN_0040a380` | Depth-exhausted path |
| Pred (caller-supplied) | `LAB_0092cdd0` | float less-than on `+4` (not dualled here) |

---

## Naming gate

| Evidence | Present? |
|---|---|
| SAR 4 / `& ~0xF` stride-16 math | **Yes** |
| Threshold 32 + insertion + heap fallback | **Yes** (MSVC `_Sort`) |
| Depth ¾ reduction | **Yes** |
| cdecl 4-arg + plain RET | **Yes** |
| RTTI / demangled plate | **No** |
| PollBoundActions body / string | **No** (falsified by xrefs) |

**Decision:** promote **`StdSort_RanItStride16_Inferred`**. Retire `Named_CalleeOf_*PollB*`. Keep `_Inferred` (no product demangle).

---

## Gaps

- Nested helpers `00409f90` / `0040a410` / `0040a380` / `0040a820` not dual-owned here.
- Predicate / element layout English beyond float@+4 at call site.
- Runtime / bit-exact / differential open.
- Other stride specializations (if any) not catalogued in this OWN pass.

---

## Open questions (resolved vs residual)

| Q | Status |
|---|---|
| Calling convention | **Resolved:** cdecl + plain RET |
| Element size | **Resolved:** 16 |
| Algorithm family | **Resolved:** MSVC introsort `_Sort` |
| Product map name | **Open** → `_Inferred` |
| Direct PollBoundActions call | **Falsified** |
