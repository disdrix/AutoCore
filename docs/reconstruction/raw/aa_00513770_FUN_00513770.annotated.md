# Annotated low-level: Item_IsWantedByStore_Inferred (FUN_00513770)

| Field | Value |
|---|---|
| Stable ID | `aa_00513770` |
| VA | `0x00513770` |
| Canonical name | `Item_IsWantedByStore_Inferred` |
| System | `inventory-transfer` |
| Date | 2026-07-29 (dual A/B seal; supersedes 2026-07-23 scaffold notes) |

## Machine-level notes

- Source: raw capture for `aa_00513770` + live re-decompile / `read_memory` 2026-07-29.
- Prefer assembly when decompiler conflicts (`CONCAT31`, free-call `FUN_004f1e20`).
- `this` = inventory Item*; `*(this+0xA8)` = clonebase; `*(clonebase+0x3C)` = def; gate `*(def+0x3B4)==1`.
- Toast polarity: return 0 → `"The store does not want that item."` (`0x00A52924`).

## Pseudocode (annotated)

```c
// bool __thiscall — ECX = Item*
undefined4 /* AL bool */ __thiscall Item_IsWantedByStore_Inferred(int item /* ECX */)
{
  int def;

  // clonebase = *(item+0xA8); ensure def if null
  if (*(int *)(*(int *)(item + 0xa8) + 0x3c) == 0) {
    // thiscall: ECX = *(item+0xa8), stack (0, 1) — not free FUN_004f1e20(0,1)
    FUN_004f1e20(/*clonebase via ECX*/ 0 /*decompiler noise*/, 1);
  }

  def = *(int *)(*(int *)(item + 0xa8) + 0x3c);
  if (def == 0) {
    return 1;   // fail-open when still no def
  }
  // raw shows CONCAT31; asm is: cmp [def+0x3b4],1 / setz al
  return *(int *)(def + 0x3b4) == 1;
}
```

## Open questions

- Product/PDB name for function and def field `+0x3B4`.
- Full dual of ensure-load `FUN_004f1e20` and price sibling `FUN_005142A0`.
