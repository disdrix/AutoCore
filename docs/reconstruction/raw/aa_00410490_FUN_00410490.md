# Raw capture: FUN_00410490

| Field | Value |
|---|---|
| **Stable ID** | `aa_00410490` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00410490` |
| **Canonical name** | `FUN_00410490` |
| **System** | STL / container utility (dword vector erase-range) |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00410490(int param_1,undefined4 *param_2,void *param_3,void *param_4)

{
  size_t _Size;
  void *pvVar1;
  
  if (param_3 != param_4) {
    _Size = (*(int *)(param_1 + 8) - (int)param_4 >> 2) * 4;
    pvVar1 = memmove(param_3,param_4,_Size);
    *(void **)(param_1 + 8) = (void *)((int)pvVar1 + _Size);
  }
  *param_2 = param_3;
  return;
}
```

---

## Live re-verify (2026-07-29, OWN-ONLY W16-S)

| Check | Result |
|---|---|
| `decompile_function` @ `0x00410490` | **≡ body above** |
| `read_memory` 80 B | `this` in `EBX` from `ECX`; end `@+8`; `sar eax,2` + `lea esi,[eax*4]`; IAT call `memmove` via `[0x009c652c]`; `*outIt = first`; **`c2 0c 00`** (`ret 0xC`) |
| Callers | `FUN_00410420`, `FUN_004365e0`, `FUN_004367f0`, `FUN_00449000` |
| Parent dual context | `StdVector_DwordResize` (`0x004367f0`) shrink path calls this to erase `[begin+newCount, end)` |

IAT dword at `0x009c652c` → `0x006ea9d0` (import thunk for `memmove`).
