# Annotated low-level: FUN_00983940 / AssResolverCore_Ctor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00983940` |
| VA | `0x00983940`–`0x009839af` (**112 B**) |
| Structural name | `AssResolverCore_Ctor_Inferred` |
| System | assets / assManager resolver-B core + effect probe host |
| Date | 2026-07-29 (W31-G) |

## Machine-level notes

- Source: scaffold raw + W31-G live `decompile_function` + full-body `read_memory` + xrefs.
- **Decompile ≡ bytes** for stage order; **bytes win** on `DAT_00d1ef58` (**WORD** zero via `66 89 3D`, not DWORD).
- **ABI:** stack `this*`, `RET 4`, return `this` — same as resolver A ctor.
- Does **not** install a vtbl itself. AssManager B path: outer shell sets `PTR_FUN_00aa1f1c` @ mem+0, zeros mem+4, then this ctor on **mem+8**.
- Nested **`FUN_00469db0(this)`** ≡ twin of `FUN_00423cf0` (both call `FUN_00423d60` + `FUN_004646e0(...,9,...)`).
- Triple zero at +0x2c/+0x30/+0x34 (begin/end/cap or freelist triple shape — product open).
- CS at **+0x38** (0x18 B); ready **u8 @ +0x50**.
- Clears five process globals after CS init — side-effect ctor (not pure object init).
- Also placement-called twice from `FUN_00985010` on stack objects before `FUN_00984ee0` probes (W31-H residual).

## Layout (subobject relative to param_1)

| Off | Field |
|---|---|
| +0x00.. | nested `FUN_00469db0` body fields |
| +0x2c | zero |
| +0x30 | zero |
| +0x34 | zero |
| +0x38 | `CRITICAL_SECTION` |
| +0x50 | ready flag `u8 = 1` |

AssManager outer (0x5c): `+0` vtbl `00aa1f1c`, `+4` zero, `+8` = this subobject start.

## Pseudocode (byte-aligned)

```c
// stdcall placement ctor: stack(this*), RET 4, returns this
AssResolverCore* AssResolverCore_Ctor_Inferred(AssResolverCore* self)
{
  // SEH LAB_009ac8fc; EH state stays -1
  FUN_00469db0(self);
  self->tri_2c = 0;
  self->tri_30 = 0;
  self->tri_34 = 0;
  InitializeCriticalSection(&self->cs_at_38);
  self->ready_at_50 = 1;
  DAT_00d1ef44 = 0; // dword
  DAT_00d1ef48 = 0;
  DAT_00d1ef50 = 0;
  DAT_00d1ef5c = 0;
  *(uint16_t*)&DAT_00d1ef58 = 0; // WORD — not dword
  return self;
}
```

## Open questions

- Product English for core class / outer `PTR_FUN_00aa1f1c` shell.
- Meaning of zeroed globals (`00d1ef44..5c`) and triple at +0x2c.
- Nested `FUN_00469db0` full product type.
- Runtime / bit-exact / differential.
