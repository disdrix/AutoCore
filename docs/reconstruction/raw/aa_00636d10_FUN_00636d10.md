# Raw capture: FUN_00636d10

| Field | Value |
|---|---|
| **Stable ID** | `aa_00636d10` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00636d10` |
| **Canonical name** | `FUN_00636d10` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_00636d10(void *param_1,byte param_2)

{
  FUN_00636b90();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```


## R11-024 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **R11-024** |
| **VA** | `0x00636d10` |
| **Ghidra** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_xrefs` / `get_function_callers` / `get_xrefs_to` + `read_memory` (body + vtbl + RTTI). **No** `disassemble_bytes`. |
| **Image** | `autoassault.exe` base `0x400000` |
| **System** | `input-drive-control` / CVOGHBMoveVehicle MSVC scalar-deleting dtor |
| **Body span** | `0x00636d10`–`0x00636d2d` inclusive (**30 B** / `0x1E`); exclusive end `0x00636d2e`; pad `CC CC` then next `0x00636d30` |
| **ABI** | MSVC **thiscall** — `this` in **ECX** (`MOV ESI,ECX`); stack **`uint8_t flags`**; ends **`RET 4`** (`C2 04 00`); return **`this*`** in **EAX** |
| **Semantics** | **Scalar-deleting destructor** (vtbl[0]): always run complete/body dtor `FUN_00636b90` (restore `PTR_FUN_009e3b70`, chain `FUN_00508390` / CVOGHBBase dtor); if `(flags & 1)` → `operator_delete(this)` + `ADD ESP,4`; always return `this` |
| **Callees** | `FUN_00636b90` @ `0x00636b90` (complete/body); `operator_delete` @ `0x00489822` |
| **Callers (code)** | **none** — DATA-only virtual dispatch via vtbl[0] |
| **DATA xrefs** | `0x009e3b70` = dword0 of `PTR_FUN_009e3b70` (**CVOGHBMoveVehicle** vtbl) |
| **Not shared** | subclass `PTR_FUN_009d5590` (CVOGHBMoveVehicleToMouse) dword0 = `0x005996a0` (different scalar shell) |
| **RTTI** | `.?AVCVOGHBMoveVehicle@@` type descriptor `0x00af33c0` |
| **Hex** | `56 8B F1 E8 78 FE FF FF F6 44 24 08 01 74 09 56 E8 FD 2A E5 FF 83 C4 04 8B C6 5E C2 04 00` |
| **Decompiler gap** | Live decompile marks `operator_delete` as noreturn and may omit `ADD ESP,4` / post-delete epilogue — **machine bytes seal** always-return path |

### Live decompile (2026-08-05) — Ghidra

```c
void * __thiscall FUN_00636d10(void *param_1,byte param_2)

{
  FUN_00636b90();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```

### Machine (disassemble_function + read_memory) — authoritative CF

```text
00636d10  PUSH ESI
00636d11  MOV ESI,ECX                 ; this (CVOGHBMoveVehicle*)
00636d13  CALL 0x00636b90             ; complete/body dtor (thiscall ESI/ECX)
00636d18  TEST byte ptr [ESP+0x8],0x1 ; flags bit0 (after ESI push)
00636d1d  JZ  00636d28                ; skip free
00636d1f  PUSH ESI
00636d20  CALL 0x00489822             ; operator_delete
00636d25  ADD ESP,0x4
00636d28  MOV EAX,ESI                 ; return this*
00636d2a  POP ESI
00636d2b  RET 0x4                     ; thiscall stack cleanup
```

### Vtbl binding (read_memory @ 0x009e3b70)

```text
009e3b70  dword0 = 00636d10   ; ScalarDeletingDtor  [OWN]
009e3b74  dword1 = 00508270
009e3b78  dword2 = 005082c0
009e3b7c  dword3 = 00636cc0   ; fire (sibling residual R11-023)
009e3b80  dword4 = 00636ba0   ; OnEnd (MEGA-017 dualed)
...
```

### Call target decode

```text
CALL @00636d13 rel32 FFFFFE78 → 00636b90
CALL @00636d20 rel32 FFE52AFD → 00489822 (operator_delete)
```
