# Annotated low-level: CVOGHBSkillBase_DefaultCtor (FUN_00578830)

| Field | Value |
|---|---|
| Stable ID | `aa_00578830` |
| VA | `0x00578830` |
| Body | `0x00578830`–`0x005788c8` (152 bytes) |
| Canonical name | `CVOGHBSkillBase_DefaultCtor` |
| Prior scaffold | `FUN_00578830` |
| System | `skills-abilities` / heartbeat |
| Date | `2026-07-29` (W23-S dual seal) |
| Tools | Ghidra `decompile_function` + `read_memory` only |

---

## Purpose

**Default / empty** constructor for `CVOGHBSkillBase`:

1. `CVOGHBBase_ctor(this)` — base HB shell.
2. Install **`PTR_FUN_009d3fdc`** (RTTI-backed **CVOGHBSkillBase** vtbl).
3. `SkillRuntime_PartialCtor` (`FUN_0054f3c0`) on **`this+0x24`** — zero/default skill-runtime blob (no catalog copy).
4. Zero skill-HB tail slots (`+0x688`, `+0x6a0`, `+0x6b0..0x6b8`).
5. `CVOGHBBase_AttachOwnerObject(this, NULL)`.
6. Seed owner-TFID block at **`this+0x678`** from invalid sentinel `DAT_009d3f88` = `{FFFFFFFF,FFFFFFFF,0,0}`.

Used by skill-element factories (`operator_new(0x6c0)` → this → **subclass vtbl overwrite**). Distinct from full `CVOGHBSkillBase_ctor` @ `0x005788d0` (blob copy + ValidateTarget + attach target + period).

---

## Annotated control flow

```c
// ECX = this (CVOGHBSkillBase*); SEH frame LAB_009a51f8
CVOGHBBase_ctor(this);                    // base layout + dormant/lastFire
*this = &PTR_FUN_009d3fdc;                // CVOGHBSkillBase vtbl (RTTI Confirmed)
FUN_0054f3c0(/* ECX = this+0x24 */);      // skill runtime partial default
this->dw_0x6b0 = this->dw_0x6b4 = this->dw_0x6b8 = 0;
this->dw_0x688 = 0;
this->dw_0x6a0 = 0;
CVOGHBBase_AttachOwnerObject(this, NULL);
// this+0x678..0x684 ← DAT_009d3f88 invalid TFID sentinel
return this;
```

### Byte-backed notes (from `read_memory`, not `disassemble_bytes`)

| Observation | Evidence |
|---|---|
| `lea ecx,[esi+0x24]` before `FUN_0054f3c0` | body hex contains `8d4e24` … `e8596bfdff` |
| Vtbl immediate `dc 3f 9d 00` | `c706dc3f9d00` → store `0x009d3fdc` |
| Attach with push 0 | `57` (edi=0) then `e823fbf8ff` → `AttachOwnerObject` |
| TFID copy from globals `0x009d3f88..94` | four loads / stores to `[esi+0x678]` |
| No stack formals; returns this | `8bc6` … `c3` after SEH restore |

---

## Offsets (dword index → byte)

| Index | Byte off | Role |
|---|---|---|
| `[0]` | `+0x00` | vtbl → `PTR_FUN_009d3fdc` |
| `[9…]` | `+0x24` | skill-runtime blob (partial-ctor target) |
| `[0x19e..0x1a1]` | `+0x678..0x684` | TFID block (invalid sentinel) |
| `[0x1a2]` | `+0x688` | zeroed (arg4 slot in full ctor) |
| `[0x1a8]` | `+0x6a0` | zeroed (table ptr in full ctor) |
| `[0x1ac..0x1ae]` | `+0x6b0..0x6b8` | zeroed tails |

---

## Distinct from sibling full ctor (`0x005788d0`)

| Step | DefaultCtor (this) | Full `CVOGHBSkillBase_ctor` |
|---|---|---|
| Vtbl | same `009d3fdc` | same |
| Skill blob | partial default only | copy `0x18c` dwords from arg |
| Type tag `+0x1c` | **not set** | `= 1` |
| ValidateTarget | **no** | notify=1 |
| AttachOwner | always **null** | **target** on accept |
| SetPeriod | **no** | period from blob on accept |

---

## Open

1. Product English for method vs compiler-generated default ctor string.
2. Full semantics of zeroed tail fields (shared with full ctor residual).
3. Runtime / bit-exact / differential — open.
