# Annotated low-level: FUN_0074f090 → Gfx_GlobalEffect_OnDeviceReset_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0074f090` |
| VA | `0x0074f090`–`0x0074f0be` exclusive (**46 B**) |
| System | client / Palantir graphics — Global Effect device reset |
| Date | 2026-08-04 (W38-W) |

## Machine-level notes

- **ABI:** **`EAX = holder*`** (phantom `in_EAX` in decompiler is real formal). Bare **`RET`**. Return **`0` / `0xFFFFFFFF`**.
- **Semantics:** if `holder[+0xC] == null` → **0**. Else stdcall-style push of inner iface, **`vtbl[+0x118]`** vcall; map status with **`SETGE` + `SUB 1`**: result **≥ 0 → 0**, **else → −1**.
- **Caller:** sole xref `GfxDevice_Reset` @ `0x0075f333`: `EAX = *(*(EBP+0x760)+4)`; `CMP EAX,0; JGE cont`; fail log **`"Failed to Reset() device on Global Effect"`** (product plate @ gfxDevice.cpp ~0x248).
- **Family:** sibling **`FUN_0074f0c0`** (shutdown) uses same holder shape (`+0xC` iface) with vtbl **`+0x10c` / `+0x114`** lost-device style; this unit is the **rebuild/OnReset** step at **`+0x118`**.
- **Decompile vs bytes:** CF ≡ raw; decompiler null path simplified to `return 0` (null path still runs SETGE form on zero → 0). **Bytes win** on EAX formal.

## Pseudocode (annotated)

```c
// EAX = holder*; bare RET; returns 0 on ok/null, -1 on vcall failure
// Product plate: "Failed to Reset() device on Global Effect"
int FUN_0074f090(void /* EAX = holder* */)
{
  int *inner = *(int **)(holder + 0xC);
  if (inner == 0)
    return 0;
  int status = (*(int (**)(int *))(*(int *)inner + 0x118))(inner);
  // SETGE / SUB 1: status >= 0 → 0; else → -1
  return (status >= 0) ? 0 : -1;
}
```

## Open questions

- Product C++ type of holder / iface (ID3DXEffect? custom GlobalEffect?).
- Exact vtbl[+0x118] English (OnResetDevice vs Reset vs re-bind).
- Identity of `[EBP+0x760]+4` object graph inside GfxDevice_Reset frame (local vs global).
