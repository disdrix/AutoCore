# Annotated low-level: CSoundManager_TickHardKillAndRegions_Inferred (`FUN_007252d0`)

| Field | Value |
|---|---|
| Stable ID | `aa_007252d0` |
| VA | `0x007252d0`–`0x0072589f` |
| System | client audio / `CSoundManager` |
| Date | 2026-07-29 (W18-F OWN dual) |
| Proposed name | `CSoundManager_TickHardKillAndRegions_Inferred` (**High** role; product name open) |

## Machine-level notes

- **ABI trap:** body loads host fields from **EBX** — **EBX-this**, not ECX-this. Stack: `enableFlag` @ `[ebp+8]`, `dt` @ `[ebp+0xC]`, **`RET 8`**.
- **Parent:** `Snd_UpdateSounds_UpdateSoundRegions` `0x00727440`.
- **Two pointer vectors:** hard-kill deferred (`+0x28c/+0x290`) and region/proximity (`+0x29c/+0x2a0`).
- **Hard-kill math:** distance vs radius; re-arm with random table sample × `DAT_00aaa638` + distance-scaled `g_flHardKillInterpolate` clamped by 4.0f.
- Feature bytes `DAT_00afa9b1..b5` gate whole function and sub-branches.

## Control flow (summary)

```
if !DAT_00afa9b1: return
if both vectors empty: return
if enableFlag==0: return
accum += dt  // +0x284
if accum >= 0:
  process hard-kill vector; accum = 0
if DAT_00afa9b2 and |listenerPos| > 0:
  process region vector
FUN_00415d60()
```

## Cross-refs

| Kind | Symbol / VA |
|---|---|
| Caller (1) | `Snd_UpdateSounds_UpdateSoundRegions` `0x00727440` |
| Callee | `CVOGReaction_RandomUnitScalar`, `FUN_004bbbc0`, `FUN_00480310`, `FUN_00723b20`, `FUN_00722f20`, `FUN_00723440`, `FUN_007229f0`, `FUN_007249c0`, `FUN_00480170`, `FUN_00480460`, `FUN_00480350`, `FUN_00480200`, `FUN_004406e0`, `FUN_004933f0`, `FUN_00415d60`, `strncpy` |
