# Annotated low-level: Map_Tidy_FreeHead_Isnil1d_Inferred (FUN_0051e120)

| Field | Value |
|---|---|
| Stable ID | `aa_0051e120` |
| VA | `0x0051e120`–`0x0051e14d` inclusive (**46 B** / exclusive end `0x0051e14e`) |
| Canonical | `Map_Tidy_FreeHead_Isnil1d_Inferred` |
| Ghidra | `FUN_0051e120` |
| System | MSVC map tidy / free header (isnil@+0x1d family host of dualed `Map_EraseRange_Isnil1d_Inferred`) |
| Date | 2026-08-04 (WQ9F-G OWN dual; scaffold 2026-07-23) |
| Peer | dualed `Map_Tidy_FreeHead` @ `0x0051da30` (isnil@+0x29 / `Map_EraseRange`) |

## Machine-level notes

- **Decompiler hazard:** plate marks `operator_delete` noreturn and drops post-delete **null head/size** + bare **RET**. Bytes seal fall-through (same as dualed `Map_Tidy_FreeHead`).
- **No SEH** in body; bare `C3`.
- **ABI:** ECX = map facade; no stack args. Entry `PUSH ECX; PUSH ESI; MOV ESI,ECX` (scratch out-slot is the pushed ECX).
- **Static host:** sole xref is atexit thunk `0x009c3940` → `MOV ECX,0xb04240; JMP 0x0051e120` (global map at `DAT_00b04240`).

## Layout

```text
Map facade (same shell as Map_Tidy_FreeHead family):
  +0x00 (unused / proxy)
  +0x04 head (_Myhead sentinel)
  +0x08 size (_Mysize)

Node family: isnil@+0x1d (from dualed Map_EraseRange_Isnil1d_Inferred)
```

## Control flow (bytes authority)

```text
ESI = this (map)
out slot = push ECX
head = [ESI+4]
CALL Map_EraseRange_Isnil1d_Inferred(map, &out, *head, head)   // full-range @ 0x0051d700
operator_delete(head); ADD ESP,4
[ESI+4] = 0; [ESI+8] = 0
POP ESI; POP ECX; RET
```

## Pseudocode (annotated)

```c
// ECX = map*; bare RET (C3); void
void __fastcall Map_Tidy_FreeHead_Isnil1d_Inferred(MapFacade* map)
{
  void* out; // stack slot only (entry push ECX)
  Node* head = map->head;
  // full range: begin = *head (left), end = head
  Map_EraseRange_Isnil1d_Inferred(map, &out, head->left, head); // 0x0051d700
  operator_delete(head);
  map->head = nullptr;
  map->size = 0;
  // does NOT free map facade
}
```

## Full body hex (46 B)

```
51568bf18b46048b0850518d44240c508bcee8c9f5ffff8b4e0451e8e2b6f6ff83c40433c08946048946085e59c3
```

## Open questions

1. Product/RTTI English for global host `0x00b04240` and exact `map<K,V>` of isnil@+0x1d family.
2. Nested duals of free-subtree / erase-node callees of `Map_EraseRange_Isnil1d_Inferred` (not OWN).
3. Runtime / bit-exact open.
