# Raw capture: FUN_00409b40

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409b40` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409b40` |
| **Canonical name** | `FUN_00409b40` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00409b40(undefined4 param_1,undefined4 param_2,undefined4 param_3)

{
  FUN_0040a590(param_3,param_2,param_1,param_3);
  return;
}
```

---

## R10-008 re-verify (2026-08-05) — append only

| Field | Value |
|---|---|
| **Agent** | R10-008 OWN-ONLY dual |
| **Tools** | `decompile_function` + `analyze_function_complete` + `read_memory` + `disassemble_function` + callers/xrefs + `get_assembly_context` |
| **No** | `disassemble_bytes`; Launcher; parent ledgers |
| **Live decompile** | ≡ scaffold CF (thin call to `FUN_0040a590`) — **ABI incomplete** (missing 2nd stack arg / RET 8 / return EAX) |
| **Named** | `StdVector_UninitCopyTrampoline_Elem12_Inferred` |
| **Parent dual** | `0x0040a590` `StdVector_UninitializedCopy_Elem12_Inferred` (WQ9K-J) |
| **System** | skills-abilities / STL vector POD helpers (elem **0x0C**) |

### Body / ABI (bytes authority)

- **Range:** `0x00409b40`–`0x00409b5a` inclusive last RET byte (**27 B** / `0x1b`); pad `CC` to next helper.
- **ABI:** **ECX** = host/vector this (forwarded but **unused** by target body); **EDX** = `dest`; stack `src_begin`, `src_end`; **EAX** = advanced dest from callee; **`RET 8`** (`C2 08 00`).
- **Full body hex:**
```
8B 44 24 04 50 8B 44 24 08 51 8B 4C 24 10 52 50
E8 3B 0A 00 00 83 C4 10 C2 08 00
```
- **`disassemble_function` listing:**
```
00409b40  MOV EAX,[ESP+4]      ; stack src_begin
00409b44  PUSH EAX
00409b45  MOV EAX,[ESP+8]      ; re-read src_begin (post-push)
00409b49  PUSH ECX             ; host this (unused by 0040a590)
00409b4a  MOV ECX,[ESP+10]     ; stack src_end → ECX for target
00409b4e  PUSH EDX             ; dest
00409b4f  PUSH EAX             ; src_begin
00409b50  CALL FUN_0040a590    ; ECX=src_end; stack begin,dest,(extra×2); ADD ESP,0x10 in caller
00409b55  ADD ESP,0x10
00409b58  RET 8
```

### Semantics

Thin **ABI trampoline** into dualed `StdVector_UninitializedCopy_Elem12_Inferred` (`0x0040a590`):
- Reorders InsertN-style registers into free-helper layout (`ECX=end`, stack begin+dest).
- Pushes **4** dwords then `ADD ESP,0x10` — target reads only begin+dest (same pattern as direct InsertN sites).
- No element loop of its own; identity is glue for reallocate prefix/suffix copy.
- **Byte-identical** to Elem0x28 trampoline `0x00409ae0` except CALL relative target (`+0x0a3b` → `0040a590` vs `+0x0a2b` → `0040a520`).

### Callers / xrefs (2)

| Site | Parent | Setup | Role |
|---|---|---|---|
| `0x00408413` | `FUN_004082f0` InsertN grow | `PUSH where; PUSH begin; EDX=newbuf; ECX=this` | prefix uninit_copy `[begin,where) → new` |
| `0x0040843a` | `FUN_004082f0` InsertN grow | `PUSH end; PUSH where; EDX=after_ufill; ECX=this` | suffix uninit_copy `[where,end) → dest` |

Call-site context (`get_assembly_context`):
```
// Prefix @ 0x00408413 (after operator_new → EAX=new):
//   PUSH [EBP+8]=where; PUSH [EBX+4]=begin; EDX=EAX=new; ECX=EBX=this
//   CALL 00409b40; MOV [EBP-0x1c],EAX  (store dest_end → Ufill start)
// Suffix @ 0x0040843a (after Ufill → EAX=after_fill):
//   PUSH [EBX+8]=end; PUSH [EBP+8]=where; EDX=EAX; ECX=EBX=this
//   CALL 00409b40
```

### Callees

- `FUN_0040a590` only (`StdVector_UninitializedCopy_Elem12_Inferred`, dualed WQ9K-J / WQ-009).

### analyze_function_complete snapshot

| Field | Value |
|---|---|
| classification | wrapper |
| callers | `FUN_004082f0` |
| callees | `FUN_0040a590` |
| xref_count | 2 (`00408413`, `0040843a`) |
| return_type_warning | void display untrusted — EAX from callee is dest_end |

### Gaps

- Product / MSVC demangle for 0x0C element type (shared Elem12 family residual).
- Non-grow InsertN paths call `0040a590` directly (role sealed on parent dual; not this OWN).
- Runtime / bit-exact / differential.
