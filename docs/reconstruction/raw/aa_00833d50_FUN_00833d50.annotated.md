# Annotated low-level: CWndVehicle_DerivedTeardown_EDI

| Field | Value |
|---|---|
| Stable ID | `aa_00833d50` |
| VA | `0x00833d50` |
| Canonical | `CWndVehicle_DerivedTeardown_EDI` (Ghidra `FUN_00833d50`) |
| System | CWndVehicle / UI derived member teardown |
| Date | 2026-08-05 WQ9K-E dual seal |

## Machine-level notes

- **EDI = this** at entry (`MOV ESI,[EDI+0x2A8]`). Not ECX thiscall. Callers keep `this` in EDI (complete dtor preserves EDI from entry `MOV EDI,ECX`).
- Clears **`+0x50C`** first (selection / state slot).
- Nested host **`+0x2A8`**: same notify plate as complete dtor — flag `+0x48=1`, copy `+0x40→+0x44` via subobject `+0x10`, `FUN_0076c4d0` (RDTSC stamp), stack binder `{PTR_LAB_00a9f4d8, nested}`, vcall iface `+0x20` then `+0x10(0)`.
- Vehicle object **`+0x508`**: if non-null, optional child `@ host+0x258` scalar-deleting dtor (`push 1; call [vtbl]`), `Vehicle_SetWheelset(0)` @ `0x004fea90` with ECX=host, then scalar-delete host itself, null `+0x508`.
- Does **not** free CWndVehicle `this`; does **not** call base UI dtor (caller complete does that).
- **Odd behavior (preserve):** complete dtor already notifies nested `@+0x2A8`, then this helper re-notifies the same nested when non-null.

## ABI

```c
// Machine: EDI = CWndVehicle* this; bare RET; void
void CWndVehicle_DerivedTeardown_EDI(void);
```

## Offsets on CWndVehicle

| Offset | Role (inferred) |
|---|---|
| +0x2A8 | nested host pointer (notify target) |
| +0x508 | vehicle object host (scalar-deleted here) |
| +0x50C | dword cleared to 0 at entry |

## Callers

| Caller | Site | Role |
|---|---|---|
| `CWndVehicle_CompleteDtor` `0x00834520` | `0x0083458d` | complete path before base `FUN_0078ca80` |
| `FUN_00833e30` | `0x00833e4f` | related vehicle UI (sets EDI=ESI first) |
| `FUN_0083ac90` | xrefs | UI |
| `FUN_0084b210` | xrefs | UI |
| `FUN_0088d980` Drive_Loading_Vehicles | `0x0088dc96` | loading/teardown path |

## Peers

- Complete: `CWndVehicle_CompleteDtor` @ `0x00834520` (WQ9J-B)
- Scalar: `CWndVehicle_ScalarDeletingDtor` @ `0x004160d0` (WQ9I-F)
- Base: `FUN_0078ca80` CNDUIWndBuffered complete (WQ9K-F OWN)
