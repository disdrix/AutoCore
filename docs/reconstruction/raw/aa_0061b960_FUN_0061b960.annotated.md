# Annotated low-level: Skill_HB_CopyTfid16At668_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0061b960` |
| VA | `0x0061b960`–`0x0061b986` |
| Ghidra | `FUN_0061b960` |
| Named (inferred) | `Skill_HB_CopyTfid16At668_Inferred` |
| System | skills-abilities |
| Date | 2026-08-05 (MEGA-016 OWN-ONLY dual refresh) |
| Parent | `FUN_0061b6f0` @ `0x0061b6f0` (shared skill-HB pulse) |

## Machine-level notes

- Source: raw capture `aa_0061b960` + live re-verify MEGA-016.
- Prefer assembly when decompiler conflicts (decompiler shows `void`; EAX actually returns `out`).
- Leaf: no callees, no branches, no mutation of `this`.
- Offset **`+0x668`** sealed by `add ecx, 0x668` (`81 c1 68 06 00 00`).
- Width **16 B** sealed by four dword pairs covering `+0x668..+0x677`.
- ABI: MSVC **`__thiscall`**, stack `out*`, **`ret 4`**, **EAX = out**.
- Sole caller: `FUN_0061b6f0` @ `0x0061b760` — copies result into stack args for `vtbl+0x88` after `vtbl+0x214` target resolve.
- Shape twin: `Object_CopyTfid16At228_Inferred` (`0x0040aff0`) — identical prologue/body, offset `+0x228` instead of `+0x668`.
- Role: skill-HB object stores a **TFID_16** identity blob at `+0x668`; this leaf extracts it for target-directed apply paths.

## Pseudocode (annotated)

```c
// ECX = skillHB* (layout has TFID_16 at +0x668)
// stack: TFID_16 *out
// ret 4; EAX = out
TFID_16 *__thiscall Skill_HB_CopyTfid16At668_Inferred(void *thisObj, TFID_16 *out)
{
  // 0061b960  mov eax, [esp+4]     ; out → EAX (preserved as return)
  // 0061b964  add ecx, 0x668       ; point at TFID field
  // four dword copies: out[i] ← src[i], i=0..3
  const auto *src = (const uint32_t *)((char *)thisObj + 0x668);
  out->dw0 = src[0];  // +0x668
  out->dw1 = src[1];  // +0x66c
  out->dw2 = src[2];  // +0x670
  out->dw3 = src[3];  // +0x674
  return out;
}
```

## ABI seal

| Item | Evidence |
|---|---|
| Convention | `ret 4` @ `0x0061b984` (`c2 04 00`) |
| ECX = this | `add ecx, 0x668` then loads; caller `mov ecx, esi` |
| Stack arg = out* | `mov eax, [esp+4]`; caller `lea/push` out local |
| EAX = out | entry load never overwritten; post-call `mov ebp, [eax]` |
| Leaf | no CALL in body |
| No this-mutation | only loads from `ecx` after add |

## Call graph (evidence only; parent not OWN)

```text
FUN_0061b6f0  skill-HB shared pulse / status apply  [parent dual residual]
  └─ FUN_0061b960  Skill_HB_CopyTfid16At668_Inferred  [OWN MEGA-016]
       → feeds 4 dwords into target->vtbl+0x88(...) after vtbl+0x214 resolve
```

## Open questions

1. Product/PDB symbol for this leaf (English open → `_Inferred`).
2. Exact RTTI of `this` (skill-HB base vs concrete skill class; parent uses `int*` skill object).
3. Field English for sibling skill-HB offsets beyond TFID role at `+0x668`.
4. Runtime / bit-exact / differential open (no Launcher).
