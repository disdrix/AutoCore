# Annotated low-level: Mission_ExpandPlayerTokenPlaceholders_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005465c0` |
| VA | `0x005465c0` |
| Canonical | `Mission_ExpandPlayerTokenPlaceholders_Inferred` |
| System | missions-progression / mission dialog string prep |
| Date | 2026-08-04 (WQ7R-D dual) |

## Machine-level notes

- Source: raw capture + WQ7R-D `read_memory` / live decompile / call-site context.
- **cdecl** `(character*, std::string*)` — callers `PUSH; PUSH; CALL; ADD ESP,8`.
- Replaces every occurrence of five fixed tokens (find/replace loops until `npos`).
- Does **not** parse free-form `[$…]` syntax; only the five literal needles below.
- Callees `FUN_00521800` / `FUN_00521900` owned by WQ7R-F (race/class string getters).

## Signature (sealed)

```c
// cdecl — 2 stack args, caller cleans 8
void Mission_ExpandPlayerTokenPlaceholders_Inferred(
    Character* character,
    std::string* text);
```

## Token table (image strings)

| Token | VA | Replace len | Source |
|---|---|---|---|
| `[$name]` | `0x009d0620` | 7 | vcall `vtbl+0x160` on adjusted this |
| `[$class]` | `0x009d0614` | 8 | `FUN_00521900` (ECX=character) |
| `[$race]` | `0x009d060c` | 7 | `FUN_00521800` (ECX=character) |
| `[$credits]` | `0x009d0600` | 10 | `sprintf("%I64d", i64@+0x720 − i64@+0x728)` |
| `[$level]` | `0x009d05f4` | 8 | vcall `vtbl+0x27c` → `sprintf("%i", …)` |

Formats: `%I64d` @ `0x00a31e58`, `%i` @ `0x00a37c64`.

## Credits formula

```
lo = *(u32*)(char + 0x720) - *(u32*)(char + 0x728)
hi = *(u32*)(char + 0x724) - *(u32*)(char + 0x72c) - borrow
// displayed as signed 64-bit via %I64d
```

Cross-check: sealed mission docs place character credits at `+0x720` (CompleteObjective / Mission_ComputeObjectiveCredits callers add there). Second qword at `+0x728` is a subtractand — product English open (spent/pending/debt residual).

## Name / level vcall pattern

MSVC multi-inheritance adjustor:

```
base_info = *(char + 4)
adj = *(base_info + 4)
this2 = char + adj + 4
vtbl = *this2
(*vtbl[slot])(this2)   // slot 0x160 name, 0x27c level
```

## Pseudocode (annotated)

```c
void Mission_ExpandPlayerTokenPlaceholders_Inferred(
    Character* ch, std::string* text)
{
  char buf[64];
  // [$name] loop: find → vcall name → replace 7
  // [$class] loop: find → FUN_00521900 → replace 8
  // [$race] loop: find → FUN_00521800 → replace 7
  // [$credits] loop: find → sprintf %I64d (ch+0x720 - ch+0x728) → replace 10
  // [$level] loop: find → vcall level → sprintf %i → replace 8
}
```

## Callers

| Site | Parent | Notes |
|---|---|---|
| `0x008aafc1` | `FUN_008aaf60` | Mission dialog path; often `DAT_00d1b6d8` as char |
| `0x008a3f5c` | `FUN_008a3510` | Mission UI helper chain |

## Confidence

| Claim | Level |
|---|---|
| Five token find/replace CF | High |
| cdecl 2-arg ABI | High |
| Token string literals | High (image) |
| Credits 64-bit delta at +0x720/−0x728 | High (image) |
| Name/level via vtbl +0x160 / +0x27c | High (image) |
| Class/race via 00521900 / 00521800 | High call; product English → WQ7R-F |
| Character type product demangle | Probable |
| Runtime / bit-exact | Open |
