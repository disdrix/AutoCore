# Annotated low-level: GuardedVector_EraseRange_Thiscall (`FUN_0043df90`)

| Field | Value |
|---|---|
| Stable ID | `aa_0043df90` |
| VA | `0x0043df90`–`0x0043e07d` (**238 B** / `0xEE`) |
| System | STL / guarded ring-deque container |
| Date | 2026-07-29 (W34-L seal) |
| Name | `GuardedVector_EraseRange_Thiscall` (**Inferred**) |

## Machine-level notes

- Entry: `SUB ESP,0x10`; `MOV ESI,ECX` → **ECX=container**.
- Epilogue: `ADD ESP,0x10`; **`RET 0x14`** (5 stack formals).
- Shorter-prefix arm: `CALL 0043ebb0` (`GuardedVector_RingAssignBackwardChecked`) then begin-advance loop with wrap `capacity*4` and size shrink; zero begin when emptied.
- Else arm: `CALL 0043ec50` (`GuardedVector_RingAssignForward`) then size-only shrink; zero begin when emptied.
- Out write: `*out = this`; `out[1] = begin + prefix` (`prefix = erase_from_i - begin`).
- Twin of W27-C `GuardedVector_EraseRange` (`0043c8c0`) with **thiscall** container and checked/forward callees from the thiscall family.
- Reject scaffold `Named_CalleeOf_*` alias.

## Pseudocode (annotated)

```c
// ECX = container; stack (out, from_c, from_i, to_c, to_i); RET 0x14
void GuardedVector_EraseRange_Thiscall(
    GuardedVectorHeader* this_ /*ECX*/,
    GuardedVectorIteratorPair* out,
    uint32_t erase_from_c, int erase_from_i,
    uint32_t erase_to_c,   int erase_to_i)
{
  int begin = this_->begin;                 // +0x0c
  uint32_t prefix = (uint32_t)(erase_from_i - begin);
  int erase_cnt = erase_to_i - erase_from_i;
  int end = this_->size + begin;            // +0x10
  uint32_t suffix = (uint32_t)(end - erase_to_i);

  if (prefix < suffix) {
    GuardedVector_RingAssignBackwardChecked(...);  // 0043ebb0
    // for each erased: begin++, wrap at capacity*4, size--; begin=0 if empty
  } else {
    GuardedVector_RingAssignForward(...);          // 0043ec50
    // for each erased: size--; begin=0 if empty
  }
  out->container = this_;
  out->index = this_->begin + (int)prefix;
}
```

## Open questions

- Product/PDB class English.
- Runtime / bit-exact golden on wrap and equal-prefix/suffix edges.
