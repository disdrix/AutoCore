# Annotated low-level: CVOGHBBase_SetPartnerLink (FUN_00604db0)

| Field | Value |
|---|---|
| Stable ID | `aa_00604db0` |
| VA | `0x00604db0` |
| System | `object / combat-target / HB` |
| Date | 2026-07-29 (W20-S dual seal) |
| Ghidra name | `FUN_00604db0` |
| Canonical | `CVOGHBBase_SetPartnerLink` |

## Machine-level notes

- Leaf: **5 instructions** + `ret 4` + int3 pad.
- Live decompile ≡ raw; bytes fully seal CF (no decompiler ambiguity).
- Owner field `+0x18` cross-sealed with `CVOGHBBase_AttachOwnerObject` / targeting ctors.
- Partner field `+0x24` cross-sealed with `Object_SetSelectedTarget` dual (both-ways link).

## Pseudocode (annotated)

```c
// CVOGHBBase_SetPartnerLink
// __thiscall(this, partner*)  ret 4
// Sets this+0x24 = partner when clearing OR when owners differ.
void __thiscall FUN_00604db0(int thisHb, int partnerHb)
{
  // Bytes:
  //   mov eax, [esp+4]
  //   test eax,eax / jz write
  //   mov edx, [eax+0x18]
  //   cmp edx, [ecx+0x18] / je skip
  // write:
  //   mov [ecx+0x24], eax
  //   ret 4
  if (partnerHb == 0 ||
      *(int *)(partnerHb + 0x18) != *(int *)(thisHb + 0x18)) {
    *(int *)(thisHb + 0x24) = partnerHb;
  }
  // same-owner non-null partner: no-op (keeps prior +0x24)
}
```

## Call surface

| Caller | Use |
|---|---|
| `Object_SetSelectedTarget` (`0x005172d0`) ×2 | After allocating self `CVOGHBPlayerTargetingLink` + target base HB: `SetPartner(self, target)` and `SetPartner(target, self)` |

## Open questions

- Product C++ method name.
- Whether any other (indirect) path relies on the same-owner skip (only two xrefs observed).
- Runtime golden of partner pair after target change.
