# Annotated low-level: CNDHash_ReclaimAll_NonOwning_009cefdc

| Field | Value |
|---|---|
| Stable ID | `aa_0053b8c0` |
| VA | `0x0053b8c0`–`0x0053b8ff` |
| System | container / CNDHash (node stamp `PTR_LAB_009cefdc`) |
| Date | 2026-07-29 (W19-L dual; scaffold 2026-07-23) |
| Ghidra | `FUN_0053b8c0` |
| Canonical (Inferred) | `CNDHash_ReclaimAll_NonOwning_009cefdc` |

---

## Machine-level notes

- Live `decompile_function` ≡ raw 2026-07-23; sealed with `read_memory` (no `disassemble_bytes`).
- **Non-owning full-bucket reclaim**: walk every bucket chain, stamp node vtbl `009cefdc`, freelist-push via `this+0x20`, clear heads. **No** `operator_delete` on values; **no** value zero at `+0x08`.
- Stamp is a dead store immediately overwritten by freelist next (same pattern as RemoveAll freelist push).
- Inclusive loop: `bucketIndex <= mask(+0x08)` via `jbe`.
- Does **not** zero `count(+0x0c)` or free table `(+0x10)` — FreeBuckets/dtor responsibilities.
- Sole static caller: FreeBuckets non-owning `0x0053b880` @ site `0x0053b88a`.

### Layout (this CNDHash shell — fields used here)

| Offset | Role |
|---|---|
| `+0x08` | mask (inclusive upper index) |
| `+0x10` | bucket table pointer (`table[i]` → sentinel with head at `+4`) |
| `+0x20` | freelist head |

### Node fields used

| Offset | Role |
|---|---|
| `+0x00` | vtbl / freelist-next (overwritten) |
| `+0x0c` | bucket chain next (`node[3]`) |

---

## Pseudocode (annotated)

```c
// void __thiscall / __fastcall — bare ret
// Ghidra: void __fastcall FUN_0053b8c0(int param_1)
void CNDHash_ReclaimAll_NonOwning_009cefdc(CNDHash *this)
{
  uint32_t i = 0;
  do {
    // head = table[i]->chainHead (+4)
    Node *n = *(Node **)(*(uint8_t **)(*(uint8_t **)((char *)this + 0x10) + i * 4) + 4);
    while (n != 0) {
      Node *next = n->bucketNext;          // +0x0c
      n->vtbl = &PTR_LAB_009cefdc;         // stamp (dead store)
      n->vtbl = *(void **)((char *)this + 0x20); // freelist link
      *(Node **)((char *)this + 0x20) = n;
      n = next;
    }
    // clear bucket head
    *(uint32_t *)(*(uint8_t **)(*(uint8_t **)((char *)this + 0x10) + i * 4) + 4) = 0;
    i++;
  } while (i <= *(uint32_t *)((char *)this + 0x08)); // inclusive mask
}
```

## Contrast vs owning reclaim (`0x00538200`)

| | This (`0053b8c0`) | Owning (`00538200`) |
|---|---|---|
| Stamp | `009cefdc` | `009cefec` |
| `operator_delete(value)` | **no** | **yes** on `node+8` |
| Zero value | **no** | **yes** |
| Freelist `+0x20` | yes | yes |
| Inclusive mask | yes | yes |

## Open questions

1. Product English / mangled FreeBuckets-reclaim name.
2. Whether freelist nodes retain stale `+0x08` value pointers (by design — non-owning external lifetime).
3. Runtime / bit-exact not run.
