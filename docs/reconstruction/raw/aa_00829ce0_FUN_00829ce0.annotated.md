# Annotated low-level: FUN_00829ce0

| Field | Value |
|---|---|
| Stable ID | `aa_00829ce0` |
| VA | `0x00829ce0`–`0x00829e80` exclusive (**416** B) |
| System | client UI / mission journal |
| Canonical (inferred) | `UI_MissionJournal_BuildMSelectChrome_Inferred` |
| Date | 2026-07-29 (W26-R dual seal) |
| Live tools | `decompile_function` + `analyze_function_complete` + `read_memory` (no `disassemble_bytes`) |

## Machine-level notes

- **ABI:** ECX = journal panel `this` → EDI; no stack formals; SEH frame; plain `ret` after `add esp,0x10`.
- **Base UI pass:** `FUN_00792600()` first (shared dialog/control init / child attach walk).
- **Branch:** `byte [this+0x4FC] == 0` → full mission chrome; else category title only.
- **Widgets (byte offsets):** button `+0x504`, title `+0x508`, status `+0x50C` (dword slots `0x141` / `0x142` / `0x143`).
- **Status complete label:** loads AL from `[this+0x4FD]`, ESI=this, calls sealed `FUN_00829ac0` (`UI_MissionJournal_ApplyStatusCompleteLabel_Inferred`).
- **XML load** via child vtbl `+0x28`; **parent attach** via this vtbl `+0xa8`; **refresh** via this vtbl `+0x34c`; mission button also vtbl `+0x74` with imm **60000** (`0xEA60`).
- **Alloc sizes:** button object `0x4CC` + `FUN_0079c860`; status/title objects `0x488` + `FUN_007b5dd0(ptr, 0)`.
- **Decompiler noise:** SEH teardown shown as `ExceptionList = in_stack_ffffffd8` and over-wide `(*vtbl+0xa8)(...)` args — **prefer bytes** for epilogue and single-arg attach calls.
- DATA xref from `0x00a748c0` (method / function pointer table entry).

## Pseudocode (annotated)

```c
// __thiscall ECX = MissionJournalPanel*
void __fastcall FUN_00829ce0(int *panel)
{
  // SEH setup (LAB_009b430a)
  FUN_00792600();   // base UI / child-hash pass on panel

  if ((char)panel[0x13f] /* +0x4FC */ == 0) {
    // Mission mode: button + status + title
    void *btnMem = operator_new(0x4cc);
    int btn = btnMem ? FUN_0079c860(/*thiscall on btnMem*/) : 0;
    panel[0x141] = btn;                 // +0x504
    (*panel->vtbl + 0xa8)(btn);         // attach child
    (*btn->vtbl + 0x28)("i_d_npc_2d_btn_mselect_btn_mission.xml");
    (*btn->vtbl + 0x74)(60000);

    void *stMem = operator_new(0x488);
    int status = stMem ? FUN_007b5dd0(stMem, 0) : 0;
    panel[0x143] = status;              // +0x50C
    (*panel->vtbl + 0xa8)(status);
    (*status->vtbl + 0x28)("i_d_npc_2d_btn_mselect_wnd_status.xml");

    // AL = existing complete flag @ +0x4FD; ESI = panel
    FUN_00829ac0();                     // ApplyStatusCompleteLabel

    void *tiMem = operator_new(0x488);
    int title = tiMem ? FUN_007b5dd0(tiMem, 0) : 0;
    panel[0x142] = title;               // +0x508
    (*panel->vtbl + 0xa8)(title);
    // pcVar4 set to title XML path
  } else {
    // Category mode: title only (cat skin)
    void *tiMem = operator_new(0x488);
    int title = tiMem ? FUN_007b5dd0(tiMem, 0) : 0;
    panel[0x142] = title;
    (*panel->vtbl + 0xa8)(title);
    // pcVar4 = "i_d_npc_2d_btn_mselect_wnd_title_cat.xml"
  }

  (*panel[0x142]->vtbl + 0x28)(titleXmlPath);
  (*panel->vtbl + 0x34c)();             // refresh panel
  // SEH teardown; ret
}
```

## Open questions

- Product plate spelling for the panel class / method.
- Exact product meaning of vtbl `+0x74(60000)` (timeout vs resource id).
- Full identity of `FUN_00792600` / `FUN_0079c860` / `FUN_007b5dd0` (not OWN).
- Whether `+0x4FC` is set before this call (category vs mission layout mode).
- Runtime golden / bit-exact.
