# Raw capture: FUN_00970fc0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00970fc0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00970fc0` |
| **Canonical name** | `FUN_00970fc0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00970fc0(void)

{
  char cVar1;
  undefined4 *puVar2;
  char local_c;
  
  FUN_0043d6f0();
  cVar1 = FUN_0043e850();
  while (cVar1 != '\0') {
    if (local_c == '\0') {
      FUN_0043d770();
    }
    else {
      puVar2 = (undefined4 *)FUN_0043d700();
      *puVar2 = 1;
    }
    cVar1 = FUN_0043e850();
  }
  return;
}
```

---

## W37-AD re-verify (2026-08-04)

**Tools:** live `decompile_function` + `analyze_function_complete` (name=) + `get_function_by_address` + `read_memory` + `get_xrefs_to`. **No** `disassemble_bytes`.

### Live decompile (≡ scaffold raw CF)

```c
void FUN_00970fc0(void)

{
  char cVar1;
  undefined4 *puVar2;
  char local_c;
  
  FUN_0043d6f0();
  cVar1 = FUN_0043e850();
  while (cVar1 != '\0') {
    if (local_c == '\0') {
      FUN_0043d770();
    }
    else {
      puVar2 = (undefined4 *)FUN_0043d700();
      *puVar2 = 1;
    }
    cVar1 = FUN_0043e850();
  }
  return;
}
```

### Function bounds

| Field | Value |
|---|---|
| Entry | `0x00970fc0` |
| Last insn | `0x00971024` (`RET`) |
| Size | **101 B** / `0x65` |
| Pad | `CC` @ `0x00971025+` |
| Ghidra Body | `00970fc0 - 00971024` |
| Classification | worker |

### Full body hex (101 B)

```
558bec83e4f883ec0c53568bd8578d442410e819c7acff8db3ac0000008d7c2410e86ad8acff84c0743481c3dc000000807c2414008d4c2410740f8bfbe8fec6acffc70001000000eb078bc3e85fc7acff8d7c2410e836d8acff84c075d25f5e5b8be55dc3
```

### Byte-sealed ABI / CF (supersedes bare decompile void)

1. Prologue: `PUSH EBP; MOV EBP,ESP; AND ESP,-8; SUB ESP,0xC; PUSH EBX; PUSH ESI; **MOV EBX,EAX**; PUSH EDI` — **host in EAX** (not stack/ECX).
2. `LEA EAX,[ESP+0x10]; CALL FUN_0043d6f0` — zero local `{u32 key=0, u8 flag=0}` (bytes `C7 00 00..; C6 40 04 00`).
3. `LEA ESI,[EBX+0xAC]; LEA EDI,[ESP+0x10]; CALL FUN_0043e850` — **pop** locked ring at **host+0xAC** into local; **AL** success.
4. First-pop miss → epilogue (`TEST AL / JZ exit`).
5. On first success: **`ADD EBX,0xDC` once** → EBX = **host+0xDC** (membership tree) for remaining iterations.
6. Loop body: `CMP [ESP+0x14],0; LEA ECX,[ESP+0x10]`:
   - **flag ≠ 0:** `MOV EDI,EBX; CALL FUN_0043d700` (ECX=key*, EDI=tree); `MOV dword [EAX],1`.
   - **flag == 0:** `MOV EAX,EBX; CALL FUN_0043d770` (EAX=tree; ECX still key*).
7. Reloop: LEA EDI local; CALL 0043e850; TEST AL; JNZ body.
8. Epilogue bare `RET` (no stack args).

### Xrefs / callers (2)

| From | Function | Kind |
|---|---|---|
| `0x00972182` | `FUN_00972140` | UNCONDITIONAL_CALL (`MOV EAX,EBP` host) |
| `0x009712b2` | `FUN_00971280` | UNCONDITIONAL_CALL (`MOV EAX,EBP` host) |

### Callees

`FUN_0043d6f0`, `FUN_0043e850`, `FUN_0043d700`, `FUN_0043d770`.

### Decompile gaps sealed by bytes

- Host ABI: **EAX**, not void/no-arg product.
- Ring base **host+0xAC** (ESI) and tree **host+0xDC** (EBX after ADD).
- Entry shape **{u32 key, u8 flag@+4}**; flag drives branch.
- `local_c` is **not** uninitialized — written by ring pop.

