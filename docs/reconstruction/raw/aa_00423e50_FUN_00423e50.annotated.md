# Annotated low-level: CNDUIWindow_ScalarDeletingDtor

| Field | Value |
|---|---|
| Stable ID | `aa_00423e50` |
| VA | `0x00423e50`–`0x00423e6d` inclusive (**30 B** / `0x1E`) |
| Ghidra name | `FUN_00423e50` |
| Canonical name | `CNDUIWindow_ScalarDeletingDtor` |
| System | skills-abilities residual partition / UI **CNDUIWindow** scalar-deleting dtor |
| Date | 2026-08-05 (MEGA-139 refresh) |
| Parent dual | `aa_007b5be0` `CNDUIWindow_CompleteDtor` (WQ9K-F) |

---

## Machine-level notes

- Source: raw `aa_00423e50` + MEGA-139 live Ghidra re-verify.
- Prefer assembly / `read_memory` when decompiler false-noreturn collapses `ADD ESP,4`.
- Role: MSVC **scalar-deleting destructor** at `vtbl[0]` of `PTR_FUN_00a960ac`.
- Always calls complete body `FUN_007b5be0`; free host only when `(flags & 1)`.
- Returns `this` in EAX; epilogue `RET 4`.
- Product class sealed via RTTI `.?AVCNDUIWindow@@` @ `0x00afe1b4`.

## Pseudocode (annotated)

```c
// ECX = this (CNDUIWindow*); stack flags; RET 4; returns this*
void* __thiscall CNDUIWindow_ScalarDeletingDtor(void* self, uint8_t flags)
{
  // CALL FUN_007b5be0 — complete dtor (vtbl reinstall, free members, NO free this)
  CNDUIWindow_CompleteDtor(self);

  if ((flags & 1u) != 0u) {
    // false-noreturn warning in Ghidra; epilogue still reached
    operator_delete(self);   // @ 0x00489822; cdecl ADD ESP,4
  }

  return self;               // MOV EAX,ESI; POP ESI; RET 4
}
```

## Control-flow / ABI (bytes authority)

```
ESI = this
call CNDUIWindow_CompleteDtor   // 0x007b5be0
if (flags & 1):
  operator_delete(this)
return this                     // RET 4
```

| Stage | Match | Conf |
|---|---|---|
| Complete-then-optional-free | decompile ≡ 30 B hex | **High** |
| bit0 gate only (not vector) | `TEST …,1` / `JZ` | **High** |
| False noreturn ignored | `ADD ESP,4` + epilogue after delete | **High** |
| vtbl[0] | DATA `00a960ac` → `00423e50` | **High** |
| CNDUIWindow family | RTTI + complete/ctor install same vtbl | **High** |

## Full body hex

```
568bf1e8881d3900f644240801740956e8bd59060083c4048bc65ec20400
```

Pad `CC CC` then next code @ `0x00423e6e+`.

## Callers / callees

| Direction | Value |
|---|---|
| Classification | worker |
| Xrefs | DATA vtbl[0] only @ `0x00a960ac` (no bulk UNCONDITIONAL_CALL sites) |
| Callees | `FUN_007b5be0` complete; `operator_delete` @ `0x00489822` |
| Virtual users | any path that `delete`s a `CNDUIWindow*` / vcall slot 0 with flags=1 |

## Open questions

1. Runtime / bit-exact / differential — open (Terminal false).
2. Complete-dtor member English — owned by dualed parent `aa_007b5be0`, not this VA.
3. Product MSVC demangle string for the scalar thunk itself (naming only; family sealed).
