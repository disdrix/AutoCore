# Annotated low-level: CVOGHBWakeupSkill_Fire_Inferred (FUN_00606180)

| Field | Value |
|---|---|
| Stable ID | `aa_00606180` |
| VA | `0x00606180`–`0x006061de` |
| Canonical | `CVOGHBWakeupSkill_Fire_Inferred` |
| Ghidra | `FUN_00606180` |
| System | `skills-abilities` / heartbeat |
| Date | 2026-07-29 (W18-I OWN dual seal) |

## Machine-level notes

- **ABI:** `__thiscall` — `ECX = CVOGHBWakeupSkill*` (HB). Save `ESI`; plain `RET` via tail-jmp to empty `FUN_005081f0`.
- **Dispatch:** sole install at **`CVOGHBWakeupSkill` vtbl `+0x14`** (`0x009de7c8` in `PTR_FUN_009de7b4`). Invoked from `CVOGHBBase_EndOrDestroy` (`0x005083f0`) when `flag_onEnd != 0` — the list **Remove** path in `CVOGHBList_Tick` when TryFire/OnHeartBeat returns status with bit `0x10`.
- **Not** TryFire OnHeartBeat: that is still `vtbl+0x0C` = `CVOGHBBase_RescheduleAfterFire` on this class. **Not** Stop OnEnd (`vtbl+0x10`).

## Control flow (byte-sealed)

```
ESI = this (HB)
if (*(this+0x18) == 0) goto epilogue          // no owner/caster
// ECX still holds owner from load at entry of gate
if (*(uint8*)(this+0x658) == 0):
    call FUN_00514e00()  // ECX = owner — dec active-cast counter on owner+0x64
// always:
AL = *(this+0x658)                            // skipBusy formal
ECX = *(this+0x18)                            // caster this for LocalCastValidate
push 0                                        // rangeExtra
push 0                                        // pSkill = NULL
push EAX                                      // skipBusy = flag@+0x658
call Skill_LocalCastValidate
if EAX == 0:                                  // allow
    push *(this+0x670)                        // randomSeed
    push *(this+0x66c)                        // posOrArg
    push *(this+0x660)                        // target TFID*
    push *(this+0x654)                        // world
    push *(this+0x18)                         // caster
    lea ECX, [this+0x24]                      // skill runtime embedded in HB
    call Skill_ApplyEffectsOnTarget_Inferred
epilogue:
    ECX = this; pop ESI; jmp FUN_005081f0     // empty ret
```

## HB field map (from ctor `CVOGHBWakeupSkill_ctor` `0x006061e0` + this body)

| Offset | Role | Evidence |
|---|---|---|
| `+0x18` | owner / caster object* | gate + LocalCastValidate this + ApplyEffects caster arg |
| `+0x24` | embedded skill runtime (copied 0x18c dwords in ctor) | `lea ecx,[esi+0x24]` → ApplyEffects this |
| `+0x654` | world* | ctor `param_1[0x195]=param_5`; ApplyEffects world |
| `+0x658` | u8 skipBusy / post-dec gate | ctor `param_9` byte; if 0 → FUN_00514e00 |
| `+0x660` | target TFID buffer start (vector) | ApplyEffects targetTfid |
| `+0x66c` | pos / arg | ctor `param_7` |
| `+0x670` | dice / random seed | ctor `param_8` |

**Class identity:** RTTI **Confirmed** `".?AVCVOGHBWakeupSkill@@"` @ `0x00af48f0`; ctor installs `PTR_FUN_009de7b4`; `operator_new(0x674)`.

## Decompiler noise (falsified)

| Decomp artifact | Reality |
|---|---|
| `CONCAT31(in_EAX, flag)` | Flag is always `mov al,[esi+0x658]` after optional dec; `in_EAX` junk |
| `FUN_005538a0` 5-arg no this (2026-07-23 raw) | 6-arg thiscall skill@`+0x24` (live + bytes) |
| `FUN_00514e00()` no formals | ECX = owner from `*(hb+0x18)` (still live after `test ecx,ecx`) |

## Open

- Product English method name (Fire vs OnComplete vs vtbl plate).
- Runtime / bit-exact image diff.
- Precise interaction when `pSkill==NULL` in LocalCastValidate (busy-only path) — sealed as called with null skill.

## Pseudocode (behavior-preserving)

See `reconstructed-exact/CVOGHBWakeupSkill_Fire_Inferred.cpp`.
