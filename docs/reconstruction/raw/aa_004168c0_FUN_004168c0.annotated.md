# Annotated low-level: FUN_004168c0 → Class_00a733ec_Factory_NullFreeCallback_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004168c0` |
| VA | `0x004168c0`–`0x0041691f` inclusive (**96 B**) |
| System | Class_00a733ec factory (null free-callback) |
| Date | 2026-08-05 (WQ9K-I dual seal) |
| Tools | decompile + read_memory + analyze_function_complete; **no** `disassemble_bytes` |

## Machine-level notes

- **Role:** MSVC-style heap factory for objects whose primary vtbl is `PTR_FUN_00a733ec`.
- **Difference vs peer `00416830`:** free-callback slot `+0x10` is **null** here; peer installs `LAB_00573240`.
- **ABI:** stack context arg; return object\* in EAX; **`RET 4`**. SEH wrapper around `operator_new(0x14)`.
- **OOM:** returns null without writing fields (null path `XOR EAX,EAX`).
- **Xrefs:** none recovered in Ghidra for this entry (or twin factory) — structural seal via twin + dtor vtbl family, not CALL graph.
- Reject scaffold name / freelist / CNDHash / thiscall misread.

## Pseudocode (annotated)

```c
// stdcall-ish: 1 stack arg; RET 4; EAX = Class_00a733ec* or null
Class_00a733ec *Class_00a733ec_Factory_NullFreeCallback_Inferred(void *context)
{
  // SEH: LAB_009ab4eb
  Class_00a733ec *obj = (Class_00a733ec *)operator_new(0x14);
  if (obj == nullptr)
    return nullptr;
  obj->vtbl = &PTR_FUN_00a733ec;   // +0x00; [0] = scalar dtor 0x00416f00
  obj->context = context;          // +0x04
  obj->field_0c = 0;               // +0x0c
  obj->field_08 = 0;               // +0x08
  obj->free_callback = nullptr;    // +0x10  ← sole twin diff vs 00416830
  return obj;
}
```

## Layout (sealed)

| Off | Field | Init |
|-----|-------|------|
| +0x00 | vtbl | `PTR_FUN_00a733ec` |
| +0x04 | context | `param_1` |
| +0x08 | field_08 | 0 |
| +0x0c | field_0c | 0 |
| +0x10 | free_callback | **0** |

## Open questions

- Product / RTTI demangle for class behind `PTR_FUN_00a733ec` (adjacent UTF-16 `"UNI_BUTTONS"` not sealed).
- Recover static callers (pointer table / delayed analysis) — Ghidra 0 xrefs.
- Peer factory `00416830` dual is WQ9K-H OWN (not this agent).
