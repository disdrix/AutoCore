# Annotated low-level: FUN_0076b080 → Fs_UnlinkFile_ESI_ForceChmod_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0076b080` |
| VA | `0x0076b080`–`0x0076b0f1` (**114 B** / `0x72`) |
| System | filesystem / single-file delete |
| Date | 2026-08-04 (W37-S) |

## Machine-level notes

- **ABI:** cdecl; stack `char force`; register **ESI = const char\* path**; plain `RET` after `ADD ESP,0x28`.
- Callers push force and `ADD ESP,4`; path always in ESI (call-site `LEA ESI` or preserved path).
- Force path: `stat` twice (MSVC `_stat` layout with mode near `+0xA` of local buf); bit15 = regular file (`_S_IFREG`); bit7 of mode low byte = write (`_S_IWRITE`); if force+reg+!writable → `_chmod(path, 0x180)` = `_S_IREAD|_S_IWRITE`.
- Always `_unlink(ESI)`; return `unlink==0` via `NEG/SBB/ADD` idiom.
- Full hex sealed in raw W37-S append.

## Callers

| Caller | Role |
|---|---|
| `FUN_0076ba10` | Recursive dir purge — file arm (W34-A) |
| `FUN_0076bc00` | Ensure directory (delete regular file if force) |
| `FUN_007b75b0` | Asset manager precompile dir prep |

## Open gaps

- Product English plate (no body string).
- Exact MSVC `_stat` struct field offsets beyond mode bit tests used here.
- Runtime / bit-exact / differential.
