# Raw capture: FUN_00561320

| Field | Value |
|---|---|
| **Stable ID** | `aa_00561320` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00561320` |
| **Body range** | `0x00561320`–`0x00561360` (exclusive end `0x00561361`) |
| **Canonical name** | `PhysSim_FlushPendingBodyPairs_Inferred` (structural; Ghidra `FUN_00561320`) |
| **System** | physics / island pair glue |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00561320(int param_1)

{
  int iVar1;
  int iVar2;
  int iVar3;
  
  iVar3 = 0;
  if (0 < *(int *)(param_1 + 0x24)) {
    do {
      iVar1 = *(int *)(*(int *)(*(int *)(param_1 + 0x20) + iVar3 * 8) + 0x44);
      iVar2 = *(int *)(*(int *)(*(int *)(param_1 + 0x20) + 4 + iVar3 * 8) + 0x44);
      if (iVar1 != iVar2) {
        FUN_00560a30(iVar1,iVar2);
      }
      iVar3 = iVar3 + 1;
    } while (iVar3 < *(int *)(param_1 + 0x24));
  }
  *(undefined4 *)(param_1 + 0x24) = 0;
  return;
}
```

---

## Live re-verify append (2026-07-29 W19-M)

| Check | Result |
|---|---|
| `decompile_function` @ `0x00561320` | ≡ raw body above |
| `get_function_by_address` | Body `00561320`–`00561360` (excl. `00561361`) |
| `read_memory` @ entry | `56 57 8b f1 33 ff …` (`push esi/edi; mov esi, ecx`) |
| Epilogue | `c7 46 24 00 00 00 00 5e c3` → `*[esi+0x24]=0; pop esi; ret` (bare **`ret`**, not `ret n`) |
| ABI | **`__thiscall`/`__fastcall` ECX = world/sim**; **0 stack args**; bare ret |
| Callee | `FUN_00560a30` with **`mov ecx, esi`** (world this) + push hostB, hostA — decompiler drops implicit this |
| Pair stride | `i*8` loads of two dwords at `this+0x20` data — matches producer `PhysSim_PushBodyPair` |
| Host key | each body `+0x44` compared; unequal → merge hosts via `00560a30` |
| Callers | `00561450`, `005614f0`, `00561910` (×3), `00561b60`, `00562680` |

**Producer:** `PhysSim_PushBodyPair_Inferred` (`0x0055efd0`) appends `(bodyA, bodyB)` pairs to `world+0x20` / count `+0x24`.
