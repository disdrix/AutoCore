# Annotated low-level: FUN_0044fe10 → AnmTrackEvents_UnserializeOneEvent_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0044fe10` |
| VA | `0x0044fe10`–`0x0044fe65` (**86 B**) |
| System | anmTrackEvents per-event unserialize (NestedHash factory bag) |
| Date | 2026-07-29 (W36-G dual seal) |
| Ghidra | `FUN_0044fe10` |
| Named | `AnmTrackEvents_UnserializeOneEvent_Inferred` |

---

## Machine-level notes

- Source: raw capture + live W36-G re-verify (decompile + full 86 B hex + callers).
- Prefer bytes when decompiler conflicts (decompiler omits live **EDI** formal; shows factory `(uVar1)` while bytes use pure thiscall `FF 51 0C` on node).
- **ABI:** `uint32_t f(void **out_event /* stack */)` with **EDI = stoChunkReader\*** register formal. Frame `SUB ESP,8`; plain **`RET`**.
- **Bag:** `FUN_0076a900` (W19-R family — read FOURCC/key from chunk reader).
- **Bag:** NestedHash0x10 static once-get `FUN_0044fee0` (W35-E sealed) ×2.
- **Bag:** NestedHash key lookup `FUN_0046c1b0` (ESI=bag, ECX=&key, EAX=&out_node) — sentinel miss iff node == `*(bag+8)`.
- **Virtual:** node vcall **+0xC** = factory; object vcall **+4** = unserialize/init with EDI pushed.
- **Caller product plate:** sole site in `FUN_00987750` (`anmTrackEvents.cpp`, `"anmTrackEvents: Error Reading event!"`).

## Pseudocode (annotated)

```c
// AnmTrackEvents_UnserializeOneEvent_Inferred
// EDI = stoChunkReader* (register); param_1 = out event object**
// Returns: status from obj->vtbl[+4](EDI), or 0xffffffff on miss.
uint32_t AnmTrackEvents_UnserializeOneEvent_Inferred(void **out_event /* stack */)
{
  uint32_t key = FUN_0076a900();          // ESI was set from EDI
  void *bag = NestedHash0x10_StaticOnceGet_d1fcc0_Inferred(); // FUN_0044fee0
  void *node = nullptr;
  FUN_0046c1b0(&key);                     // ESI=bag, EAX=&node out
  bag = NestedHash0x10_StaticOnceGet_d1fcc0_Inferred();
  void *obj = nullptr;
  if (node != *(void **)((char *)bag + 8)) {  // not NestedHash sentinel
    obj = (*(void *(**)(void))(*(uint32_t *)node + 0xC))(); // thiscall factory
  }
  *out_event = obj;
  if (obj != nullptr) {
    return (*(uint32_t (**)(void))(*(uint32_t *)obj + 4))(); // thiscall; EDI on stack
  }
  return 0xffffffffu;
}
```

## Call graph

| Direction | Symbol | Role |
|---|---|---|
| Caller | `FUN_00987750` @ `0x00987987` | anmTrackEvents chunk unserialize event loop |
| Callee | `FUN_0076a900` | read FOURCC/key from EDI reader |
| Callee | `FUN_0044fee0` ×2 | NestedHash0x10 static once-get |
| Callee | `FUN_0046c1b0` | NestedHash lookup by key → node |
| Virtual | node[+0xC] | event factory |
| Virtual | obj[+4] | event unserialize / bind |

## Open questions

- Product C++ class name for factory node / event object (vtbl plates unowned).
- Whether factory takes an implicit register/stack key (decompiler shows `uVar1`; bytes show no PUSH of key before `FF 51 0C`).
- Full contracts of `FUN_0076a900` / `FUN_0046c1b0` (sibling wave ownership).
- Runtime / bit-exact / differential.
