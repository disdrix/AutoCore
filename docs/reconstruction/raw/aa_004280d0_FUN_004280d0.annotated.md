# Annotated low-level: FUN_004280d0 → CNDUIDialog_ScalarDeletingDtor

| Field | Value |
|---|---|
| Stable ID | `aa_004280d0` |
| VA | `0x004280d0`–`0x004280ee` exclusive (**30 B** / `0x1E`) |
| Canonical name | `CNDUIDialog_ScalarDeletingDtor` |
| Ghidra name | `FUN_004280d0` |
| System | skills-abilities (partition); product UI base `CNDUIDialog` |
| Date | 2026-08-05 MEGA-140 re-verify |
| Parent dual | `0x00792c20` `CNDUIDialog_CompleteDtor` (WQ9K-G) |

## Machine-level notes

- Source: raw capture + live Ghidra re-verify (`decompile_function`, `analyze_function_complete`, `read_memory`, xrefs). **No** `disassemble_bytes`.
- MSVC **scalar-deleting destructor** pattern (vtbl[0]): always run complete body, optionally free host, return `this`, **`RET 4`**.
- Ghidra WARNING “Subroutine does not return” on `operator_delete` is a **false positive** — bytes continue through `ADD ESP,4` / `MOV EAX,ESI` / `POP ESI` / `RET 4`.
- Product class sealed by RTTI `.?AVCNDUIDialog@@` via COL before `PTR_FUN_00a98f44`.

## Pseudocode (annotated)

```c
// ECX = this; stack flags; RET 4; returns this*
void* __thiscall CNDUIDialog_ScalarDeletingDtor(void* self, uint8_t flags)
{
  // ESI = self  (56 8B F1)
  CNDUIDialog_CompleteDtor(self);   // FUN_00792c20 — installs PTR_FUN_00a98f44, tears owned slots, base chain

  if ((flags & 1u) != 0u) {
    operator_delete(self);          // free host only when scalar-delete path (bit0)
  }

  return self;                      // EAX = ESI; C2 04 00
}
```

## ABI / CF seal

| Item | Evidence | Conf |
|---|---|---|
| ECX = this | `56 8B F1` | **High** |
| flags on stack | `F6 44 24 08 01` | **High** |
| complete always | `CALL 0x00792c20` | **High** |
| bit0 gate only | `TEST …,1` / `JZ` — not vector | **High** |
| free host | `PUSH ESI; CALL operator_delete; ADD ESP,4` | **High** |
| return this | `8B C6` | **High** |
| RET 4 | `C2 04 00` | **High** |
| vtbl[0] | dword @ `00a98f44` = `004280d0` | **High** |
| product name | RTTI `.?AVCNDUIDialog@@` | **High** |

## Callers / callees

| Direction | Targets |
|---|---|
| Callers | DATA only — `PTR_FUN_00a98f44[0]` @ `0x00a98f44` |
| Callees | `FUN_00792c20` (complete), `operator_delete` @ `0x00489822` |

## Open questions

1. Complete-dtor slot English / deep base dual residual (owned by WQ9K-G, not this VA).
2. Runtime / bit-exact / differential — open (no Launcher; terminal false).
