# Annotated low-level: FUN_0051ba40 → CNDHash_AllocBucketTable_009ce090

| Field | Value |
|---|---|
| Stable ID | `aa_0051ba40` |
| VA | `0x0051ba40`–`0x0051bad0` |
| System | container / CNDHash (medal family; sentinel stamp `009ce090`) |
| Date | 2026-07-29 (W25-F dual seal; prior scaffold 2026-07-23) |
| Canonical | `CNDHash_AllocBucketTable_009ce090` (**Inferred**) |

## Machine-level notes

- Source: raw capture + live `decompile_function` / `read_memory` (no `disassemble_bytes`).
- **ECX = this** (`__thiscall` / decompiler `__fastcall`); plain `ret` on success.
- Preconditions: `log2` byte at `this+0x1C` in **`[1,16]`**; `this+0x08` holds provisional **bucket count N = 1<<log2** (set by Recreate/ctor before call).
- Alloc:
  1. `operator_new[](N * 4)` → pointer table at `this+0x10`
  2. `malloc(N * 0xC)` → contiguous sentinel slab
  3. For `i = 0 .. N-1`: `table[i] = slab + i*0xC`; stamp sentinel vtbl **`0x009ce090`**; head `+4=0`; `+8=0`
  4. `this+0x08 = N - 1` (**count → mask**)
- Fail path: `VOG_DEBUG_STOP` + `_CxxThrowException` with code **`0x80004003`** (`E_POINTER`), `ThrowInfo @ 0x00acc430`.
- Explains FreeBuckets `free(*buckets)`: first table entry points at slab base.

## Layout (this body)

| Off | Field | Mutation |
|---|---|---|
| `+0x08` | count → **mask** | read N; write N−1 |
| `+0x10` | buckets* | ← new[] pointer table |
| `+0x1C` | log2 | read-only gate |
| `+0x20` | freelist | untouched |

### Sentinel (0xC)

| Off | Init |
|---|---|
| `+0x00` | `&PTR_LAB_009ce090` |
| `+0x04` | 0 (chain head) |
| `+0x08` | 0 |

## Pseudocode (annotated)

```c
// void __thiscall CNDHash_AllocBucketTable_009ce090(CNDHash *this)
// plain ret; or noreturn throw
void __thiscall FUN_0051ba40(int thisHash)
{
  char log2 = *(char *)(thisHash + 0x1c);
  if (!(log2 >= 1 && log2 <= 16)) {
    FUN_007a4480(0, "VOG_DEBUG_STOP");
    // throw 0x80004003 / E_POINTER
    _CxxThrowException(...);
  }
  int N = *(int *)(thisHash + 8);              // power-of-two count
  void **table = operator_new[](N * 4);
  *(void **)(thisHash + 0x10) = table;
  void *slab = malloc(N * 0xC);
  for (uint i = 0; i < (uint)N; i++) {
    table[i] = (char *)slab + i * 0xC;
    undefined4 *sent = (undefined4 *)table[i];
    *sent = &PTR_LAB_009ce090;
    sent[1] = 0;
    sent[2] = 0;
  }
  *(int *)(thisHash + 8) = N - 1;              // MASK for key & mask
}
```

## Call graph (owned VA only)

| Dir | Symbol | VA / note |
|---|---|---|
| Caller | `CNDHash_Recreate` (`FUN_0051d230`) | `0x0051d27c` after FreeBuckets + set N |
| Caller | ctor `FUN_0051db60` | `0x0051dbba` (vtbl `009ce1a0`) |
| Callee | `operator_new[]` | pointer table |
| Callee | `malloc` | sentinel slab |
| Callee | `FUN_007a4480` / `_CxxThrowException` | fail path |

## Twin / family

| Unit | Role |
|---|---|
| **This** | Alloc bucket table + sentinels (`009ce090`) |
| `CNDHash_FreeBuckets` `0x0051d150` | reclaim + free slab/table |
| `CNDHash_ReclaimAll_ValueOwning_009ce0a8` `0x0051bfb0` | freelist reclaim |
| `CNDHash_AllocBucketTable_Inferred` `0x00413d80` | parallel pattern, different stamp |

## Open questions

- Product/PDB AllocBuckets English.
- Full vtbl of `009ce090` (methods) — not required for alloc CF.
- Runtime / bit-exact / differential.
