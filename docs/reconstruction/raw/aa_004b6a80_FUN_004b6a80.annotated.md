# Annotated low-level: FUN_004b6a80 / NDSpecialFX_List1e0_AnyFlag10

| Field | Value |
|---|---|
| Stable ID | `aa_004b6a80` |
| VA | `0x004b6a80`–`0x004b6aa7` (**40 B**) |
| System | client-fx / NDSpecialFX residual list probe |
| Date | 2026-07-23 scaffold; **2026-07-29 W26-G dual seal** |
| Canonical (inferred) | `NDSpecialFX_List1e0_AnyFlag10` |
| Ghidra | `FUN_004b6a80` |

## Machine-level notes

- **ABI:** `__fastcall` / thiscall-like — **ECX = host**. No stack formals. Plain `RET`. **AL** bool return.
- **Leaf:** no callees.
- List at **`this+0x1e0`**: circular doubly-linked (sentinel node; `node[0]=next`, `node[2]=payload*`).
- Flag test: **`*(byte*)(payload + 0x989) & 0x10`**.
- Callers:
  - `FUN_004b8ed0` (`NDSpecialFX_HostTick_Inferred`) type-switch **case 4**: non-zero → re-enter active tick path.
  - `FUN_004ba0a0` case 4: non-zero → skip fall-through clear branch.
- Role: **optional keep-active / re-enter probe** for type-4 FX hosts based on residual list `+0x1e0`.

## Pseudocode (annotated)

```c
// ECX = host; returns AL bool
uint8_t NDSpecialFX_List1e0_AnyFlag10(int host /*ECX*/)
{
  uint32_t *node;
  uint32_t *sentinel = *(uint32_t **)(host + 0x1e0);

  node = (uint32_t *)*sentinel;
  while (true) {
    if (node == sentinel) {
      return 0;   // XOR AL,AL — no node with flag
    }
    // node[2] @ +8 = payload object
    if ((*(uint8_t *)(node[2] + 0x989) & 0x10) != 0) {
      break;      // found
    }
    node = (uint32_t *)*node;  // next
  }
  return 1;       // MOV AL,1
}
```

## Bytes (authoritative)

Full 40 B hex — see raw re-verify. Epilogues: `32 C0 5E C3` (false) / `B0 01 5E C3` (true).

## Open questions

- Product English for flag `+0x989` bit `0x10` and list `+0x1e0` element type.
- Why type **4** alone uses this re-enter probe (switch tables in callers).
- Runtime golden.
