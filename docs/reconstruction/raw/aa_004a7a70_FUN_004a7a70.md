# Raw capture: FUN_004a7a70

| Field | Value |
|---|---|
| **Stable ID** | `aa_004a7a70` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004a7a70` |
| **Canonical name** | `FUN_004a7a70` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int __thiscall FUN_004a7a70(undefined4 param_1,int param_2,int param_3,undefined4 param_4)

{
  FUN_004a74f0(param_2,param_3,param_4,param_1,param_4);
  return param_2 + param_3 * 0x1c;
}
```

---

## W36-S re-verify (2026-07-29) — OWN-ONLY dual

**Tools:** `batch_decompile` / `decompile_function` + `get_function_by_address` + `get_function_xrefs` + `get_function_callees` + `read_memory`. **No** `disassemble_bytes`. **No** Launcher. **No** parent ledger.

### Live decompile

Live 2026-07-29 ≡ scaffold raw CF (thin thiscall wrapper → `FUN_004a74f0`; return `dest + count*0x1c`).

### Function extent

| Field | Value |
|---|---|
| Entry | `0x004a7a70` |
| Body | `0x004a7a70`–`0x004a7aa0` exclusive (**48 B** / `0x30`) |
| Next | immediately `FUN_004a7aa0` |

### Full body hex (48 B, `read_memory`)

```
8b44240c568b74240c578b7c240c50518b4c241c515657e864faffff8d14f5000000002bd683c4148d04975f5ec20c00
```

### Byte-sealed facts

| Claim | Evidence |
|---|---|
| Load value (stack arg3) | `8B 44 24 0C` |
| count → ESI, dest → EDI | after push-ESI/EDI stack shuffle |
| Push 5 dwords to fill | value, ECX(alloc), value, count, dest |
| Call `FUN_004a74f0` | `E8 64 FA FF FF` → `0x004a74f0` |
| `ADD ESP, 0x14` | cdecl cleanup of 5 args |
| `EAX = dest + count*0x1c` | `LEA EDX,[ESI*8]; SUB EDX,ESI; LEA EAX,[EDI+EDX*4]` (= count×28) |
| RET 0x0C | `C2 0C 00` |
| ECX this/alloc | forwarded into fill call |

### Xrefs / callees

| Kind | Targets |
|---|---|
| Callers | **only** `FUN_004a7ad0` / `StdVector_InsertN_BasicStringW` @ `0x004a7d4a` (in-place hole arm) |
| Callees | `FUN_004a74f0` (wstring uninit fill loop; W36-R, not owned here) |

### Name

`StdUninitFillN_BasicStringW` — wchar twin of `StdUninitFillN_BasicString` (`0x00430390`, W33-G). **Reject** scaffold `Named_CalleeOf_*_004a7a70`.

### Note on parent grow path

W34-R insert-n **grow** path calls `FUN_004a74f0` **directly** for fill; this wrapper is the **in-place hole** fill that returns `dest_end`. Char twin `00430390` served both grow and in-place sites — wstring splits grow (direct) vs hole (wrapper).

### Verdict

**accept-with-gaps** — fill-n wrapper ABI/CF/return math sealed; nested `FUN_004a74f0` product open.
