# Annotated low-level: CVOGEnvironmentDistort_Enable_Inferred (`FUN_0055cc50`)

| Field | Value |
|---|---|
| Stable ID | `aa_0055cc50` |
| VA | `0x0055cc50`–`0x0055cc6a` exclusive (**26 B** / `0x1A`) |
| System | client / VOGEnvironmentSector / owned Distort phase |
| Date | 2026-07-23 scaffold; **2026-07-29 W33-T dual seal** |

## Machine-level notes

- **ABI sealed by bytes:** `mov eax,[esp+4]; push esi; push eax; mov esi,ecx; call EnsureAux; mov ecx,esi; call 0055ca90; xor eax,eax; pop esi; ret 4`.
- **thiscall:** ECX = `CVOGEnvironmentDistort*` (owned at env`+0xC8`, size **0xA0**).
- Stack formal = **flag** (InitPhases factory always **1**); forwarded to `HostBase_EnsureAuxPtrs_Inferred`.
- Return **0** always (`XOR EAX,EAX`).
- Prefer bytes if decompiler drops `this` preservation (`mov esi,ecx` / `mov ecx,esi`).
- Host RTTI via ctor vtbl `009d26ac` → COL `00aae6cc` → **`.?AVCVOGEnvironmentDistort@@`**.

## Pseudocode (annotated)

```c
// CVOGEnvironmentDistort_Enable_Inferred
// ret 4; returns 0
uint32_t __thiscall CVOGEnvironmentDistort_Enable_Inferred(
    CVOGEnvironmentDistort* self,
    uint32_t flag)
{
  HostBase_EnsureAuxPtrs_Inferred(self, flag);  // FUN_00756320
  FUN_0055ca90(self);  // particle distort child @ +0x90; Particle_Distort.dds / NDParticleDistortBlendNormal.fx
  return 0;
}
```

## Call graph

```
PalantirEnv_InitPhases(env, 1)
  new(0xA0) + FUN_0055c8c0(parent) → env+0xC8
  CVOGEnvironmentDistort_Enable_Inferred(env+0xC8, 1)   // THIS
    → HostBase_EnsureAuxPtrs(flag)
    → FUN_0055ca90 (child particle host bring-up)
```

## Open questions

- Product English for `FUN_0055ca90` nested fields / FX path (residual, not OWN).
- Flag product meaning beyond factory constant `1`.
- Runtime / bit-exact / differential.
