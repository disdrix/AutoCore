# Annotated low-level: FUN_004a74f0 → StdUninitFillN_BasicStringW

| Field | Value |
|---|---|
| Stable ID | `aa_004a74f0` |
| VA | `0x004a74f0`–`0x004a757c` exclusive (**140 B** / `0x8C`) |
| Canonical | `StdUninitFillN_BasicStringW` (**Inferred**) |
| System | containers / MSVC `_Uninit_fill_n` `basic_string<wchar_t>` stride `0x1c` |
| Date | 2026-07-29 (W36-R OWN-ONLY; scaffold 2026-07-23) |

## Machine-level notes

- Source: raw capture + W36-R `read_memory` / decompile re-verify.
- Prefer bytes when decompiler omits catch residual.
- SEH scope `LAB_009a11c1`; per-element state 0/1 around IAT copy-ctor.
- Same IAT copy-ctor/dtor as sibling uninit-copy (`0x009C62B4` / `0x009C62AC`).
- Catch rethrow: `_CxxThrowException(0,0)` @ `0x006A3D60`.
- Callers: insert-n `FUN_004a7ad0` (W34-R); hole helper `FUN_004a7a70`; fill path `FUN_004a8100`.
- Char family: thin wrapper `StdUninitFillN_BasicString` (`0x00430390`) + nested loop body — this unit **is** the full SEH loop (no thin rebind).

## Signature (sealed)

```c
// cdecl; stack dest*, count, value*; bare RET; void
void StdUninitFillN_BasicStringW(
    BasicStringW_0x1c* dest,
    int count,
    const BasicStringW_0x1c* value);
```

## Pseudocode (happy path)

```c
void StdUninitFillN_BasicStringW(BasicStringW_0x1c* dest,
                                 int count,
                                 const BasicStringW_0x1c* value)
{
  // SEH: LAB_009a11c1
  for (; count != 0; --count) {
    // EH state = 1
    if (dest != nullptr) {
      basic_string_w_copy_ctor(dest, value);  // thiscall ECX=dest
    }
    dest = (BasicStringW_0x1c*)((char*)dest + 0x1c);
    // EH state = 0
  }
  // catch (bytes only): destroy partial dest range; _CxxThrowException(0,0);
}
```

## Open questions

- Product/MSVC demangle plate for exact `_Uninit_fill_n` specialization.
- Runtime / bit-exact / differential.
- Full dual seal of non-owned callers `FUN_004a7a70` / `FUN_004a8100`.
