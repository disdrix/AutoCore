# Raw capture: FUN_00628f10

| Field | Value |
|---|---|
| **Stable ID** | `aa_00628f10` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00628f10` |
| **Canonical name** | `FUN_00628f10` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00628f10(int param_1,int param_2)



{

  int iVar1;

  int iVar2;

  int iVar3;

  int *piVar4;

  

  iVar3 = 0;

  if (0 < *(int *)(param_1 + 0x50)) {

    piVar4 = *(int **)(param_1 + 0x4c);

    do {

      if (*piVar4 == param_2) goto LAB_00628f33;

      iVar3 = iVar3 + 1;

      piVar4 = piVar4 + 1;

    } while (iVar3 < *(int *)(param_1 + 0x50));

  }

  iVar3 = -1;

LAB_00628f33:

  *(int *)(param_1 + 0x50) = *(int *)(param_1 + 0x50) + -1;

  if (iVar3 < *(int *)(param_1 + 0x50)) {

    do {

      iVar2 = iVar3 * 4;

      iVar1 = iVar3 * 4;

      iVar3 = iVar3 + 1;

      *(undefined4 *)(*(int *)(param_1 + 0x4c) + iVar1) =

           *(undefined4 *)(*(int *)(param_1 + 0x4c) + 4 + iVar2);

    } while (iVar3 < *(int *)(param_1 + 0x50));

  }

  *(undefined4 *)(param_2 + 0x10) = 0;

  *(undefined1 *)(param_1 + 0x28) = 1;

  return;

}
```


---

## Live re-seal append — 2026-07-29 W24-P dual A/B

| Field | Value |
|---|---|
| **Tools** | Ghidra `decompile_function` + `read_memory` + `get_function_by_address` + callers/callees/signature (**no** `disassemble_bytes`) |
| **Excluded** | Launcher; parent ledgers; `disassemble_bytes` |
| **Live decompile** | Matches 2026-07-23 raw body (no CF drift) |
| **Body** | `0x00628f10`–`0x00628f64` exclusive (**84** / `0x54` B); Ghidra max `00628f63` |
| **Epilog** | `RET 4` (`C2 04 00`) — one stack formal |
| **ABI** | **`__thiscall`**: ECX = host; stack[4] = action*; void |
| **Callers** | `FUN_0055df40`, `FUN_00560f70`, `FUN_00561580` |
| **Callees** | none |
| **Role** | Linear-search host reverse-pointer list (`+0x4c` base / `+0x50` count); shift-remove match; null `action+0x10`; set dirty `host+0x28=1` |
| **Canonical name** | `ActionHost_RemoveReverseListEntry_Inferred` (**Inferred** — no plate string) |
| **Parent residual note** | Parent decomp of `0055df40` omitted `this=*(action+0x10)`; this unit's thiscall is sealed |

### Body hex (`read_memory` 84 B)

```
8b51505633c085d2578b7c240c7e118b714c393e740d83c00183c6043bc27cf283c8ff834150ff8b51503bc27d168bff8b514c8b7482048d148283c00189323b41507cecc74710000000005fc64128015ec20400
```

### Byte-derived control flow

```
mov  edx, [ecx+0x50]         ; count
push esi
xor  eax, eax                ; index = 0
test edx, edx
push edi
mov  edi, [esp+0xC]          ; action
jle  miss_or_empty           ; → or eax,-1 path when count<=0 lands on or eax,-1
mov  esi, [ecx+0x4c]         ; base
find:
  cmp  [esi], edi
  je   found                 ; index in eax
  add  eax, 1 / add esi, 4
  cmp  eax, edx / jl find
miss_or_empty:
or   eax, -1                 ; not found (or count<=0)
found:
add  dword [ecx+0x50], -1    ; count--  (always)
mov  edx, [ecx+0x50]
cmp  eax, edx / jge no_shift
shift:                       ; preserve order of tail
  mov  edx, [ecx+0x4c]
  mov  esi, [edx+eax*4+4]
  lea  edx, [edx+eax*4]
  add  eax, 1
  mov  [edx], esi
  cmp  eax, [ecx+0x50] / jl shift
no_shift:
mov  dword [edi+0x10], 0     ; clear action back-pointer
pop  edi
mov  byte [ecx+0x28], 1      ; host dirty / needs-update flag
pop  esi
ret  4
```

### Behavioral note (not-found)

When the action is **not** in the list, index becomes **-1**, count is still decremented, and the shift loop condition `-1 < new_count` may run a shift from a negative index. Call sites that always pass a registered host (`*(action+0x10)`) avoid this; port should treat missing entry as exceptional.
