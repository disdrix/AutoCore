# Annotated low-level: FUN_0059dc50 → ObjectiveNode_IsEligibleForTracker_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0059dc50` |
| VA | `0x0059dc50`–`0x0059dcb9` inclusive (**106 B** / `0x6A`) |
| System | `missions-progression` |
| Canonical name | `ObjectiveNode_IsEligibleForTracker_Inferred` |
| Ghidra name | `FUN_0059dc50` |
| Date | 2026-08-05 (MEGA-025 dual refresh) |
| Prior scaffold | `Named_CalleeOf_Named_VOG_DEBUG_STOP_0059dc50` (**retired**) |

## Machine-level notes

- Source: live Ghidra re-verify MEGA-025 (`decompile` + `disassemble_function` + `read_memory` + callers/xrefs + call-site context). Prefer assembly when decompiler conflicts.
- **ABI sealed:** ECX = `ObjectiveNode*`; stack = `Character*` (often `DAT_00d1b6d8` or `client+0xe98`); **AL** bool; **`RET 4`**.
- **this** fields: `+0x14c` → template*; template `+0xf8` short gate; node `+0x14` byte; template `[0]` id; node used by sibling `FUN_0059db80`.
- **Character** path: MSVC multi-inheritance this-adjust via `[char+4]` / `[+4]` then virtual **`+0x27c`**.
- Parent dual: `Client_TryBindActiveMissionTracker` (`0x0092fd00`) documents this as the objective eligibility gate before tracker bind.

## Pseudocode (annotated)

```c
// ECX = ObjectiveNode* this
// stack = Character* character
// AL = 1 if node may be selected/bound by mission tracker; else 0
// RET 4
bool __thiscall ObjectiveNode_IsEligibleForTracker_Inferred(
    ObjectiveNode* this,   // ESI
    Character* character)  // EDI from [ESP+0xc]
{
  int eval;

  // 1) null character → ineligible
  if (character == NULL)
    return false;

  // 2) if template marks a special gate short@+0xf8 ≠ 0,
  //    require node active/flag byte@+0x14 ≠ 0
  Template* tmpl = this->template_at_14c; // *(this+0x14c)
  if (tmpl->gate_short_f8 != 0 && this->flag_byte_14 == 0)
    return false;

  // 3) sibling evaluator must not return -1 (not ready / missing)
  eval = FUN_0059db80(this, character); // thiscall ECX=this, stack=character
  if (eval == -1)
    return false;

  // 4) special template-id band [0xdfd..0xe02] inclusive (3581..3586):
  //    require character virtual(+0x27c)() >= 5 (level/rank-like)
  //    decompiler writes 0xdfc < id && id < 0xe03 — same for ints
  uint32_t id = *(uint32_t*)tmpl; // first dword of template
  if (id >= 0xdfd && id <= 0xe02) {
    int v = character->vtbl_adjusted()->fn_0x27c();
    if (v < 5)
      return false;
  }

  return true;
}
```

## Control-flow map (asm ≡ decompile)

| Path | Condition | Result |
|---|---|---|
| fail | `character == 0` | `AL=0` |
| fail | `*(short*)(tmpl+0xf8) != 0 && *(char*)(node+0x14) == 0` | `AL=0` |
| fail | `FUN_0059db80(character) == -1` | `AL=0` |
| fail | `id ∈ [0xdfd,0xe02]` **and** `vtbl+0x27c() < 5` | `AL=0` |
| pass | otherwise | `AL=1` |

## Callers / callees

| Role | Symbol / VA |
|---|---|
| Callee | `FUN_0059db80` (`0x0059db80`) — objective eval vs character hash/requirements |
| Virtual | character `vtbl+0x27c` (this-adjusted) |
| Caller | `Client_TryBindActiveMissionTracker` `0x0092fd00` @ `0x0092fd25` |
| Caller | `FUN_009344b0` @ five sites (objective cycle / next eligible) |
| Caller | `FUN_008a8660`, `FUN_0092b020`, `FUN_008a5240` (hash walks + tracker pick) |

## Open questions (gaps)

- Product English for template short@`+0xf8` and node byte@`+0x14`.
- Product meaning of template-id band `0xdfd`–`0xe02` (six consecutive ids).
- Product name for character virtual `+0x27c` (threshold 5).
- Dual seal of sibling `FUN_0059db80` (out of OWN scope).
- Runtime / bit-exact / differential.
