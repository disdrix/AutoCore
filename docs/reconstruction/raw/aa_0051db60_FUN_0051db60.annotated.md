# Annotated low-level: FUN_0051db60 → CNDHash_Ctor_009ce1a0

| Field | Value |
|---|---|
| Stable ID | `aa_0051db60` |
| VA | `0x0051db60`–`0x0051dbd2` (**115 B** / `0x73`) |
| System | container / CNDHash (medal family; object vtbl `009ce1a0`) |
| Date | 2026-07-29 (W26-P dual seal; prior scaffold 2026-07-23) |
| Canonical | `CNDHash_Ctor_009ce1a0` (**Inferred**) |

## Machine-level notes

- Source: raw capture + live `decompile_function` / `read_memory` (no `disassemble_bytes`).
- **ECX = this** (`__thiscall`); stack **byte log2**; returns **this** in EAX; **`RET 4`**.
- SEH frame: handler `LAB_009a371b`; state `local_4 = 0` around `FUN_0051ba40` (throw-capable alloc).
- Installs object vtbl **`PTR_FUN_009ce1a0`**, zeroes most fields, sets provisional bucket **count** `N = 1 << log2` at `+0x08` and stores raw log2 at `+0x1C`.
- Calls sealed twin **`CNDHash_AllocBucketTable_009ce090`** (`FUN_0051ba40`) which:
  - gates log2 ∈ [1,16]
  - allocates pointer table + 0xC sentinels stamped `009ce090`
  - converts `+0x08` count → **mask** `N-1`
- Field **`+0x24` is not written** by this body (rely on allocator zeroing or unused).
- Static construction site `@ 0x009c0b57`: `log2=2` → N=4, `this=0x00b042e0`.

## Layout (this body)

| Off | Field | Mutation |
|---|---|---|
| `+0x00` | vtbl | ← `&PTR_FUN_009ce1a0` |
| `+0x04` | (zero) | ← 0 |
| `+0x08` | count → later mask | ← `1 << log2` (AllocBucketTable → N−1) |
| `+0x0C` | (zero / count sibling) | ← 0 |
| `+0x10` | buckets\* | ← 0 then filled by Alloc |
| `+0x14` | (zero) | ← 0 |
| `+0x18` | (zero) | ← 0 |
| `+0x1C` | log2 | ← param byte |
| `+0x1D` | flag | ← 0 |
| `+0x20` | freelist | ← 0 |
| `+0x24` | **not written** | — |
| `+0x28`..`+0x30` | (zero) | ← 0 |

## Pseudocode (annotated)

```c
// CNDHash* __thiscall CNDHash_Ctor_009ce1a0(CNDHash *this, uint8_t log2)
// ret 4; returns this
undefined4 * __thiscall FUN_0051db60(undefined4 *thisHash, byte log2)
{
  // SEH: LAB_009a371b
  *thisHash = &PTR_FUN_009ce1a0;
  thisHash[1] = 0;                              // +0x04
  thisHash[3] = 0;                              // +0x0C
  thisHash[2] = 1u << (log2 & 0x1f);            // +0x08 count N
  thisHash[4] = 0;                              // +0x10 buckets
  thisHash[5] = 0;                              // +0x14
  thisHash[6] = 0;                              // +0x18
  *(byte *)(thisHash + 7) = log2;               // +0x1C
  *(byte *)((int)thisHash + 0x1d) = 0;
  thisHash[8] = 0;                              // +0x20 freelist
  // +0x24 not written
  thisHash[10] = 0;                             // +0x28
  thisHash[0xb] = 0;                            // +0x2c
  thisHash[0xc] = 0;                            // +0x30
  FUN_0051ba40();                               // thiscall AllocBucketTable (ECX=this)
  return thisHash;
}
```

## Call graph (owned VA only)

| Dir | Symbol | VA / note |
|---|---|---|
| Caller | static init @ `0x009c0b57` | `PUSH 2; MOV ECX,0x00b042e0; CALL` |
| Callee | `CNDHash_AllocBucketTable_009ce090` (`FUN_0051ba40`) | `@ 0x0051dbba` |

## Twin / family

| Unit | Role |
|---|---|
| **This** | Ctor: vtbl `009ce1a0` + field init + AllocBucketTable |
| `CNDHash_AllocBucketTable_009ce090` `0x0051ba40` | alloc twin (also Recreate) |
| `CNDHash_FreeBuckets` `0x0051d150` | free twin |
| `FUN_0051dfe0` dtor | installs/uses same vtbl family (`009ce1a0`) |
| `CNDHash_Recreate` `0x0051d230` | re-init path (not this ctor) |

## Open questions

- Product / PDB Ctor English / class name beyond CNDHash family.
- Full vtbl method matrix at `009ce1a0`.
- Semantic of unwritten `+0x24`.
- Runtime / bit-exact / differential.
