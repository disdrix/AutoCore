# Annotated low-level: Heightfield_SampleY (FUN_005a58c0)

| Field | Value |
|---|---|
| Stable ID | `aa_005a58c0` |
| VA | `0x005a58c0` |
| Body | `005a58c0` – `005a5956` |
| System | `world-terrain` |
| Date | 2026-07-29 (dual A/B refresh) |

## Machine-level notes

- Source: raw capture + live `decompile_function` / `force_decompile` / `read_memory`.
- Prefer assembly when decompiler conflicts (trunc = CVTTSS2SI; RET 8).
- `this` = heightfield*, not CVOGMap*.
- Sole callee `FUN_005a5810` = bilinear (OWN dual does not own callee).

## Layout (this / heightfield)

| Off | Type | Role |
|----:|------|------|
| +0x0C | int | dimX; clamp max = dimX-2 |
| +0x10 | int | dimZ; clamp max = dimZ-2 |
| +0x30 | float | scaleX (gx = scaleX * worldX) |
| +0x34 | float | heightScale divisor |
| +0x38 | float | scaleZ |

## ABI

```
float10 __thiscall Heightfield_SampleY(hf*, float worldX, float worldZ)
// RET 8; ST0 = Y
```

## Pseudocode (annotated ≡ raw ≡ live)

```c
float10 __thiscall Heightfield_SampleY(int hf, float worldX, float worldZ)
{
  float gx = *(float *)(hf + 0x30) * worldX;
  float gz = *(float *)(hf + 0x38) * worldZ;
  // trunc toward zero (CVTTSS2SI), clamp to [0, dim-2]
  uint cellX_cap = *(int *)(hf + 0xc) - 2;
  uint cellZ_cap = *(int *)(hf + 0x10) - 2;
  // ... SETLE max(0,trunc) then min with cap ...
  float10 raw = FUN_005a5810(cellX, cellZ, gx - (float)cellX, gz - (float)cellZ);
  return raw / (float10)*(float *)(hf + 0x34);
}
```

## Epilogue (read_memory)

```
FDIV [ESI+0x34]
POP EDI
POP ESI
RET 8
```

## Open questions

- Product/PDB name.
- Dual of FUN_005a5810.
- Asset semantics of scale triple; div0 if +0x34 == 0.
