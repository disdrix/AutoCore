# Annotated low-level: FUN_008ac110 → UI_MissionDialog_BuildResponseButtons_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_008ac110` |
| VA | `0x008ac110`–`0x008ac318` (520 B) |
| System | client UI / mission dialog |
| Date | 2026-08-04 (WQ7R-B dual) |

## Machine-level notes

- **EDI = this** (dialog). SEH `LAB_009b63f9`. Bare `ret`.
- Gate `+0x4C0`; cols `+0x640` (min 1); count `+0x510`; slots `+0x708` (EBP cursor).
- **Decompiler bugs corrected by bytes:**
  1. `cVar5` is **not** `btn >> 24` — stack flag last-item ∧ col==0.
  2. Y uses **row** at `[esp+0x14]`, not `unaff_EBP` (EBP is slot*).
- Scale: `DAT_00aaa67c=1/1600`, `DAT_00aaa678=1/1200` × runtime viewport.
- XML `i_d_npc_2d_btn_response.xml`; id base `0x9C46`.

## Pseudocode (corrected)

```c
// EDI = dialog*
void UI_MissionDialog_BuildResponseButtons_Inferred(void)
{
  if (dialog[+0x4C0] == 0) return;
  FUN_008aa560();
  if (dialog[+0x640] < 1) dialog[+0x640] = 1;
  cols = dialog[+0x640];
  count = dialog[+0x510];
  slot = &dialog[+0x708];
  for (i = 0; i < count; ++i, ++slot) {
    row = i / cols; col = i % cols;
    alone_last = (i == count-1) && (col == 0);
    mem = operator_new(0x4CC);
    btn = mem ? FUN_0079c860() : 0;
    *slot = btn;
    dialog.vtbl+0xA8(); // attach
    btn.vtbl+0x28("i_d_npc_2d_btn_response.xml");
    btn.vtbl+0x74(i + 0x9C46);
    // size from +0x5CC/+0x5D0 * viewport * design scale; vtbl+0x08
    base_x = alone_last ? +0x5B4 : +0x5C4;
    base_y = alone_last ? +0x5B8 : +0x5C8;
    x = base_x + (+0x5D4)*col;
    y = base_y + (+0x5D8)*row;
    if (mode(+0x648) == 0 || mode == 3) { x += +0x5BC; y += +0x5C0; }
    // pos scale; btn.vtbl+0x118
  }
}
```

## Open questions

- Product method plate spelling.
- Why alone-last switches origin.
- Full dual of `FUN_008aa560` / child ctor (not OWN).
