# Annotated low-level: CVOGHBBase_EmptyVFunc (FUN_005081f0)

| Field | Value |
|---|---|
| Stable ID | `aa_005081f0` |
| VA | `0x005081f0` |
| Canonical | `CVOGHBBase_EmptyVFunc` |
| System | heartbeat / shared empty leaf |
| Date | 2026-07-23 scaffold; 2026-07-29 W18-A dual seal |

## Machine-level notes

- Body is a single-byte **`c3` (`ret`)**. No stores, loads, or callees.
- Callers may pass `this` in **ECX** (thiscall / fastcall shape) or stack args; **all ignored**.
- Base `CVOGHBBase` vtbl @ `0x009cdab0` slot **+0x14** points here (empty default).
- Subclass OnEnds often **override** `vtbl+0x14`, then **tail-jmp** here as shared nop (e.g. OKToCastAgain OnEnd).
- Neighbor `0x005081e0` is a **different** 1-byte empty ret (Stop's base `vtbl+0x10` default in base table).
- Distinct from mass `EmptyRet` `0x0056f570`.

## Pseudocode (annotated)

```c
// Shared empty leaf — base HB vtbl+0x14 default; multi-domain nop target.
void CVOGHBBase_EmptyVFunc(void /* this ignored */)
{
  return; // c3
}
```

## Open questions

- Original PDB / product symbol string.
- Exhaustive DATA-slot census (many vtbl hits).
