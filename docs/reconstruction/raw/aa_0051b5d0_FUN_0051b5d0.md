# Raw capture: FUN_0051b5d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051b5d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051b5d0` |
| **Canonical name** | `FUN_0051b5d0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0051b5d0(int *param_1)

{
  char cVar1;
  
  param_1 = (int *)*param_1;
  cVar1 = *(char *)((int)param_1 + 0x29);
  while (cVar1 == '\0') {
    param_1 = (int *)*param_1;
    cVar1 = *(char *)((int)param_1 + 0x29);
  }
  return;
}
```

---

## Live re-verify (2026-08-04 WQ9D-B OWN-ONLY)

| Field | Value |
|---|---|
| Tool | Ghidra `decompile_function` / `batch_decompile` + `read_memory` + `get_function_by_address` + callers/xrefs |
| Body | `0x0051b5d0`–`0x0051b5ea` inclusive (**27 B** / `0x1b`); `CC` pad to next @ `0x0051b5f0` |
| Decompile ≡ 2026-07-23 raw | **Yes** (void display unchanged) |
| `disassemble_bytes` | **Not used** |
| Launcher / runtime | **Not run** |

### Bytes (entry, 27 B body + pad)

```
0051b5d0  8b 44 24 04        mov  eax, [esp+4]        ; node
0051b5d4  8b 08              mov  ecx, [eax]          ; left = node->left
0051b5d6  80 79 29 00        cmp  byte ptr [ecx+0x29], 0  ; isnil
0051b5da  75 0e              jne  ret                 ; left nil → return node (EAX)
0051b5dc  8d 64 24 00        lea  esp, [esp+0]        ; MSVC align nop
0051b5e0  8b c1              mov  eax, ecx            ; walk left
0051b5e2  8b 08              mov  ecx, [eax]
0051b5e4  80 79 29 00        cmp  byte ptr [ecx+0x29], 0
0051b5e8  74 f6              je   0051b5e0
0051b5ea  c3                 ret                      ; EAX = leftmost non-nil
0051b5eb  cc cc cc cc cc
```

**Decompiler hazard (sealed):** Ghidra shows `void` and walks into nil; **bytes return EAX = leftmost non-nil** (classic MSVC `_Tree_min`). Same hazard class as `StdTree_Min_Val12` (`aa_005adfa0`).

### Callers (5 UNCONDITIONAL_CALL)

| Site | Function | Role |
|---|---|---|
| `0x004092ea` | `FUN_00409220` | map erase (throw string `"invalid map/set<T> iterator"`) |
| `0x0051cc2f` | `Map_EraseNode` / `FUN_0051cb40` | head→left recompute after unlink |
| `0x004cb82f` | `FUN_004cb740` | same erase family |
| `0x0058dd1f` | `FUN_0058dc30` | same erase family |
| `0x0060391c` | `FUN_00603830` | same erase family |

### Callees

None (leaf).
