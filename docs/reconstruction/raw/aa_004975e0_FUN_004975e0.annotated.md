# Annotated low-level: FUN_004975e0 / Class_009c7a1c_ScalarDeletingDtor

| Field | Value |
|---|---|
| Stable ID | `aa_004975e0` |
| VA | `0x004975e0`–`0x004975fe` exclusive (**30 B**) |
| Semantic name | `Class_009c7a1c_ScalarDeletingDtor` |
| System | class / MSVC scalar-deleting dtor |
| Date | 2026-07-29 (W29-F re-verify) |

## Machine-level notes

- Entry: `push esi; mov esi, ecx` — thiscall this in ECX.
- Always runs complete body `FUN_004970b0` / `Class_009c7a1c_CompleteDtor` (W28-M).
- Optional heap free when `flags & 1` (MSVC scalar-deleting convention).
- Epilogue returns this (`EAX=ESI`) and `ret 4`.
- Primary site is **vtbl[0]** of `PTR_FUN_009c7a1c` @ `0x009c7a1c` → `0x004975e0`.
- Do **not** merge with complete body `0x004970b0` or with element dtor `0x005b8000`.

## Pseudocode (annotated)

```c
// void* __thiscall Class_009c7a1c_ScalarDeletingDtor(void* self, uint8_t flags)
void * __thiscall FUN_004975e0(void *param_1, byte param_2)
{
  FUN_004970b0();                 // Class_009c7a1c_CompleteDtor(this) — ECX preserved as this
  if ((param_2 & 1) != 0) {
    operator_delete(param_1);     // free host heap block when bit0 set
  }
  return param_1;                 // EAX = this; RET 4
}
```

## Sealed layout / ABI

| Item | Value | Conf |
|---|---|---|
| Body size | 30 B | High |
| Convention | `__thiscall`, ret 4 | High |
| Role | vtbl[0] scalar-deleting | High |
| Body twin | `FUN_004970b0` | High |

## Open questions

- Product English / MSVC demangle for class behind `PTR_FUN_009c7a1c` (open; vtbl suffix used).
- Runtime / bit-exact / differential (open).
