# Annotated low-level: StdVector_UninitializedFillN_Elem0x10_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00407000` |
| VA | `0x00407000` |
| Canonical name | `StdVector_UninitializedFillN_Elem0x10_Inferred` |
| Ghidra name | `FUN_00407000` |
| System | util / container (`std::vector` POD elem **0x10**); partition host interaction-activation |
| Date | 2026-08-05 (R10-007 dual seal) |

## Machine-level notes

- Source: raw capture + R10-007 re-verify (`decompile` + `disassemble_function` + `read_memory`).
- **EDI** = dest start, **ESI** = count (decompiler `unaff_*`).
- Stack formal0 = `const T* value`; **`ret 4`**.
- Thin wrapper: construct-N via dualed `FUN_00409e20` (`StdVector_ConstructN_Elem0x10_Inferred`), then return **`dest + count * 0x10`**.
- Construct-N: ECX=value*, stack dest+count; each iter `FUN_00409f70` copies **4 dwords** and advances dest by **0x10**.
- Phantom extra pushes into construct-N cleaned with `add esp,0x10` (MSVC Ufill-wrapper pattern; same family as `00406ee0` / `00406e50`).
- Sole caller: dualed `FUN_00408640` InsertN elem 0x10 (2 sites — realloc path + in-place hole path).
- Body **29 B** / `0x1d`; pad `CC` to next fn @ `0x00407020`.

## Pseudocode (annotated)

```c
// customcc: EDI = dest*, ESI = count
// stack: const T* value   (T = 16-byte POD)
// ret 4; EAX = dest + count*0x10
T* StdVector_UninitializedFillN_Elem0x10_Inferred(
    /*EDI*/ T* dest,
    /*ESI*/ int count,
    /*stack*/ const T* value)
{
  // FUN_00409e20: for i in [0,count): copy 4 dwords value → dest+i
  FUN_00409e20(dest, count /*, ECX=value*/);
  return (T*)((char*)dest + count * 0x10);
}
```

## Open questions

- Product type English for the 16-byte element (generic triad slot under InsertN; SortTargetList packs object*+score on a different construct-N path).
- Dual seal of PodCopy leaf `FUN_00409f70` (not OWN).
- Runtime / bit-exact / differential open.
