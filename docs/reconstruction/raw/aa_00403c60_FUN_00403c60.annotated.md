# Annotated low-level: StdTree_LowerBound_StringKey_Isnil131_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00403c60` |
| VA | `0x00403c60`–`0x00403ca6` (**71 B** / `0x47`) |
| Ghidra name | `FUN_00403c60` |
| Canonical name | `StdTree_LowerBound_StringKey_Isnil131_Inferred` |
| System | skills-abilities / shared `client::stdtree` (isnil131 string-key map) |
| Date | 2026-08-05 (MEGA-041 dual refresh) |
| Prior scaffold | `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Client_QuickBar_Ac_00403c60` — **retired** |

## Machine-level notes

- Source: raw capture + MEGA-041 live re-verify (`decompile` / `disassemble_function` / `read_memory` / callers / xrefs / `get_assembly_context`).
- Prefer assembly for ABI: **EAX = map**, stack string*, **`RET 4`**. Decompiler `in_EAX` is correct; do not invent ECX-thiscall.
- Node family: **isnil @ +0x131**, links left@0 parent@4 right@8, **key `std::string` @ +0x0C**.
- Compare via IAT `[0x009c62e8]` → `std::operator<(basic_string, basic_string)` cdecl.
- Twin upper_bound: `FUN_00403d60` (not dualled here).
- Callers are slash-command dispatch/help (`FUN_0093bd10`, `FUN_00957820`) — residual seed was QuickBar_ActivateSlot nested; body is pure STL lower_bound.

## Pseudocode (annotated)

```c
// EAX = map*  (head at map+4); stack = const std::string* key; RET 4
// returns node* candidate (end() == head when no lower_bound hit)

node_t *
StdTree_LowerBound_StringKey_Isnil131_Inferred(
    /*EAX*/ map_t *map,
    /*stack*/ const std::string *key)
{
  node_t *head = map->_Myhead;           // [EAX+4]
  node_t *cur  = head->_Parent;          // root
  node_t *cand = head;                   // end()

  while (cur->_Isnil /*+0x131*/ == 0) {
    // operator<(cur->key /*+0xC*/, *key)
    if (cur->_Key < *key) {
      cur = cur->_Right;                 // +8
    } else {
      cand = cur;                        // first !(key_node < key)
      cur  = cur->_Left;                 // +0
    }
  }
  return cand;                           // EAX
}
```

## Control flow

| Stage | Assembly | Meaning |
|---|---|---|
| Prologue | `MOV EAX,[EAX+4]`; save EBP/ESI/EDI | load head; preserve |
| Empty / nil root | `CMP [ESI+0x131],0` / `JNZ exit` | root isnil → return head |
| Loop compare | `LEA ECX,[ESI+0xC]`; `CALL [0x009c62e8]` | string `<` |
| True branch | `MOV ESI,[ESI+8]` | go right; cand unchanged |
| False branch | `MOV EDI,ESI`; `MOV ESI,[ESI]` | cand=cur; go left |
| Exit | `MOV EAX,EDI`; `RET 4` | return cand |

## Open questions

- Product English name of host map type (slash-command table is usage evidence only).
- Full payload layout of isnil131 nodes beyond key / isnil / caller-observed handler@+0x12C and level@+0x28.
- Runtime Confirmed / bit-exact / differential.
