# Annotated low-level: FUN_004b0f80 → ObjectMotion_AdvanceStage_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004b0f80` |
| VA | `0x004b0f80`–`0x004b10fe` (383 B / `0x17F`) |
| System | client object motion / reaction stage chain |
| Date | 2026-07-29 (W27-E dual seal) |
| Preferred name | `ObjectMotion_AdvanceStage_Inferred` |
| Ghidra name | `FUN_004b0f80` |

## Machine-level notes

- Source: raw capture + live `decompile_function` 2026-07-29 (≡ scaffold 2026-07-23).
- ABI sealed: `__thiscall` ECX=slot; stack `char mode`; **`ret 4`** (`C2 04 00`); AL 0|1.
- Sole caller: `ObjectMotion_SlotTick_Inferred` (`FUN_004b4620`) — mode **0** after radius rebind, mode **1** on primary duration expiry.
- `DAT_00aaa638` = `80 00 80 37` ≈ 1/65535 (ushort→unit scale).
- Prior scaffold alias `Mission_Uses_Client_GetMissionCompleteAudioTable` is **keyword noise** — audio is a side branch only when `*(host+4)==0`.

## Offset map (slot / stage)

| Access | Role (inferred) |
|---|---|
| `slot[0]` | host object pointer |
| `slot[1]` | current stage node pointer (advanced via `stage+0x2f4`) |
| `slot[0x5c]` / `+0x170` | primary remaining duration (float bits as int) |
| `slot[0x5d]` / `+0x174` | duration total / mirror of [0x5c] at stage entry |
| `slot[0x5e..0x60]` | vec3 (passed to audio key / FX spawn) |
| `stage+0x1ea` | C-string stage name (empty ⇒ skip audio/FX) |
| `stage+0x2ee` | once-flag for `FUN_004b7e50` spawn |
| `stage+0x2ef` bit0 | gate multi-hit flush `FUN_004b6980` |
| `stage+0x2f4` | next stage link |
| `stage+0x1e4` bit2 | duration RNG jitter enable |
| `stage+4` | float RNG scale amplitude |
| `host+0x14` | end-of-chain sentinel compared to advanced stage |
| `host+0x7fb` | secondary-timer host flag (blocks mode-1 end return) |

## Pseudocode (annotated)

```c
// thiscall; ret 4; AL 0|1
// mode: 0 = soft advance (radius rebind); 1 = hard advance (duration end)
uint __thiscall FUN_004b0f80(int *slot /*ECX*/, char mode /*stack*/)
{
  // --- stage side effects before advance ---
  if (*(int *)(*slot + 4) == 0) {
    // Client / "no-host-payload" path: mission-complete audio if name set
    // strlen(stage+0x1ea) via scan; non-empty if end != start+1
    if (/* stage name non-empty */) {
      key = FUN_004e9530(local_20, slot + 0x5e); // build key from slot pos/vec
      Client_GetMissionCompleteAudioTable(stage+0x1ea, key);
      FUN_00725a70(stage+0x1ea, key); // CSoundManager_TryQueueMissionCompleteSound
    }
  } else {
    // Hosted path: one-shot FX/name spawn + optional multi-hit flush
    if (/* name non-empty */ && *(char *)(stage + 0x2ee) == 0) {
      FUN_004b7e50(0, 0, stage+0x1ea, slot[0x5e], slot[0x5f], slot[0x60]);
      *(uint8_t *)(stage + 0x2ee) = 1;
    }
    if ((*(byte *)(stage + 0x2ef) & 1) != 0)
      FUN_004b6980(); // NDSpecialFX_FlushStagedMultiHits
  }

  // --- advance linked stage ---
  next = *(int **)(stage + 0x2f4);
  slot[1] = (int)next;

  // mode-1 only: end sentinel + no secondary timer → fail (remove slot)
  if (mode != 0 && next == (int *)(*slot + 0x14) && *(char *)(*slot + 0x7fb) == 0)
    return 0; // AL=0

  // load duration from *next (stage header dword 0)
  slot[0x5c] = *next;
  if ((*(byte *)((char *)next + 0x1e4) & 4) != 0) {
    // jitter: base + u16 * stage[float+4] * (1/65535)
    u16 = CVOGReaction_RandomUnitScalar_draw();
    slot[0x5c] = (int)((float)u16 * *(float *)((char*)next + 4) * DAT_00aaa638
                       + (float)slot[0x5c]);
  }
  slot[0x5d] = slot[0x5c]; // total = remaining at stage start
  return 1; // AL=1
}
```

## Call graph

| Dir | VA | Name |
|---|---|---|
| Caller | `0x004b4620` | `ObjectMotion_SlotTick_Inferred` (sole) |
| Callee | `0x004e9530` | audio key builder (unsealed) |
| Callee | symbol | `Client_GetMissionCompleteAudioTable` |
| Callee | `0x00725a70` | `CSoundManager_TryQueueMissionCompleteSound_Inferred` (W18-R) |
| Callee | `0x004b7e50` | stage FX/name spawn (unsealed) |
| Callee | `0x004b6980` | `NDSpecialFX_FlushStagedMultiHits_Inferred` |
| Callee | — | `CVOGReaction_RandomUnitScalar` |

## Confidence

| Claim | Level |
|---|---|
| CF ≡ live decompile | **Confirmed** |
| ret 4 + AL 0\|1 | **Confirmed** (`read_memory` epilogue) |
| Sole SlotTick caller / mode 0&1 sites | **Confirmed** |
| Stage-chain advance via `+0x2f4` | **Confirmed** |
| Product C++ name | **Inferred** |
| Runtime / bit-exact | **Open** |
