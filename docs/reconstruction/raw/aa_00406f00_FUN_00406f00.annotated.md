# Annotated low-level: SkillSet_Vector_UninitializedFillN_0x18_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00406f00` |
| VA | `0x00406f00`–`0x00406f1c` exclusive (28 B) |
| System | STL / SkillSet vector (entry stride 0x18) |
| Date | 2026-08-04 (WQ9G-F re-annotate) |
| Ghidra | `FUN_00406f00` |
| Named | `SkillSet_Vector_UninitializedFillN_0x18_Inferred` |

## Machine-level notes

- **Role:** MSVC-style **`vector` uninitialized_fill_n / `_Ufill`** for **element size 0x18** (`NPCSkillSetEntry`).
- **Not** a tree helper despite VA neighborhood of dualed `StdTree_InsertAndRebalance_Isnil29_Inferred` (`0x00406c40`). Address-band residual only.
- **ABI (call-site sealed):**
  - **EDI** = destination `T*`
  - **ESI** = element count `n`
  - **ECX** = vector / SkillSet* (passed through; fill uses value in ECX after reload)
  - stack = `const T* value` (one arg)
  - **return EAX** = `dest + n * 0x18`
  - **`ret 4`**
- **Body:** push value / this / count / dest → `FUN_00412aa0` (loop: `FUN_00412b10` copies 6 dwords, `dest += 0x18`) → `lea eax, [edi + esi*0x18]`.
- **Sole caller:** `FUN_00412730` (SkillSet vector insert / reallocate) — two sites (grow path + in-place path). Uses dualed `SkillSet_GetEntryCount` (`0x00402d80`) for size.
- **Reject:** scaffold `Named_CalleeOf_Skill_Uses_SkillSet_GetEntryCount_*` chain-of-caller name; unaffiliated ESI/EDI (they are intentional register args).

## Pseudocode (annotated)

```c
// EDI = dest, ESI = count, ECX = vector* (unused by fill body after setup),
// stack: const NPCSkillSetEntry* value; ret 4
// returns dest + count * 0x18
NPCSkillSetEntry* __fastcall SkillSet_Vector_UninitializedFillN_0x18_Inferred(
    void *vector /*ECX*/,
    NPCSkillSetEntry *dest /*EDI*/,
    size_t count /*ESI*/,
    const NPCSkillSetEntry *value /*stack*/)
{
  // FUN_00412aa0(value in ECX, dest, count):
  //   for (; count; --count, dest += 1) construct *dest from *value (0x18 memcpy-like)
  UninitializedFillN_0x18(dest, count, value);
  return dest + count; // byte offset count*0x18
}
```

## Open questions

- Product demangle / exact MSVC helper name (`_Ufill` vs `_Uninitialized_fill_n`).
- Full `NPCSkillSetEntry` field English (6 dwords).
- Insert parent `FUN_00412730` undualed (not OWN).
- Runtime / bit-exact open.
