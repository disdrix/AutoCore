# Raw capture: FUN_00461b80

| Field | Value |
|---|---|
| **Stable ID** | `aa_00461b80` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00461b80` |
| **Canonical name** | `FUN_00461b80` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00461b80(undefined4 *param_1)



{

  int *piVar1;

  int *piVar2;

  int *piVar3;

  undefined4 *in_EAX;

  undefined4 *unaff_EDI;

  

  for (; in_EAX != param_1; in_EAX = in_EAX + 7) {

    *in_EAX = *unaff_EDI;

    in_EAX[1] = unaff_EDI[1];

    in_EAX[2] = unaff_EDI[2];

    in_EAX[3] = unaff_EDI[3];

    piVar2 = (int *)unaff_EDI[4];

    if ((piVar2 != (int *)0x0) && (piVar2[1] = piVar2[1] + 1, piVar2[1] == 1)) {

      (**(code **)(*piVar2 + 4))();

    }

    piVar3 = (int *)in_EAX[4];

    if (piVar3 != (int *)0x0) {

      piVar1 = piVar3 + 1;

      *piVar1 = *piVar1 + -1;

      if (*piVar1 == 0) {

        (**(code **)(*piVar3 + 8))();

      }

    }

    in_EAX[4] = piVar2;

    *(undefined1 *)(in_EAX + 5) = *(undefined1 *)(unaff_EDI + 5);

    *(undefined1 *)((int)in_EAX + 0x15) = *(undefined1 *)((int)unaff_EDI + 0x15);

    *(undefined1 *)((int)in_EAX + 0x16) = *(undefined1 *)((int)unaff_EDI + 0x16);

    in_EAX[6] = unaff_EDI[6];

  }

  return;

}
```

---

## W35-P re-verify append (2026-07-29)

**Tools:** `batch_decompile` / `decompile_function` + `analyze_function_complete` (no disasm) + `read_memory` + callers/xrefs. **No** `disassemble_bytes`.

### Body / epilogue

| Field | Value |
|---|---|
| Range | `0x00461b80`–`0x00461bfd` exclusive (**125 B** / `0x7D`); pad `CC`×3 then `FUN_00461c00` @ `0x00461c00` |
| Entry | `55 8B 6C 24 08 56 8B F0` — `push ebp; mov ebp,[esp+8]; push esi; mov esi,eax` |
| Epilogue | `5B 5E 5D C3` — pop ebx/esi/ebp; plain **`RET`** (`C3`) |
| Stride | `ADD ESI, 0x1C` per element |

### ABI (bytes win)

| Formal | Location | Conf |
|---|---|---|
| `dest_first*` | **EAX** → `ESI` cursor | **High** |
| `value*` (const T&) | **EDI** live-in (fixed source; never advanced) | **High** |
| `dest_last*` | stack arg (`[esp+8]` after `push ebp` → `EBP`) exclusive end | **High** |
| return | void; plain **`RET`** | **High** |

Call sites in sole parent `FUN_004617a0` / `StdVector_InsertN_RcElem28`:

- `0x00461a2a` (hole path): `push ebx` (old end); `lea edi,[ebp-0x30]` (local value); `EAX=where`
- `0x00461a89` (mid path): `push edi` (where+count×0x1c); `lea edi,[ebp-0x30]`; `mov eax,esi` (where)

### Element layout (0x1c) — assign lanes

| Off | Op |
|---|---|
| +0x00..+0x0C | 4× dword copy from value |
| +0x10 | shared*: **AddRef** source then **Release** dest then store |
| +0x14..+0x16 | 3× byte copy |
| +0x17 | **not** written |
| +0x18 | dword copy |

### AddRef / Release (same family as W34-P)

- AddRef: if `p != 0`: `++*(p+4)`; if became **1** → `(**p)(+4)(p)` (vtbl+4, ECX=p)
- Release: if `p != 0`: `--*(p+4)`; if **0** → `(**p)(+8)(p)` (vtbl+8)

### Semantics

MSVC-style **range fill by assign** on live **RcElem28** slots (stride **0x1c**):
`for (cur = dest_first; cur != dest_last; cur += 0x1c) *cur = *value` with RC-correct resource swap.
Empty range (`EAX==dest_last`) is no-op.
**Not** uninitialized fill (`FUN_00461c90`); **not** range-to-range; **not** POD memmove.

### Classification / callers

| Item | Value |
|---|---|
| Classification | **leaf** (no static callees; only indirect vtbl) |
| Callers | **1** function / **2** sites: `FUN_004617a0` @ `0x00461a2a`, `0x00461a89` |

### Full body hex (125 B)

```
558b6c2408568bf03bf5746e538d49008bc78b108bce89118b50048951048b50088951088b400c89410c8b5f1085db7414b80100000001430439430475078b138bcbff52048b4e1085c9740b834104ff75058b01ff5008895e108a4f14884e148a57158856158a47168846168b4f18894e1883c61c3bf575975b5e5dc3
```

### Decompile vs bytes

| Claim | Winner |
|---|---|
| Forward assign CF + RC gates | decompile ≡ bytes |
| EAX=dest_first, EDI=value* live-in | **bytes** (decompiler shows `in_EAX` / `unaff_EDI`) |
| Stack dest_last + plain RET | **bytes** |
| Stride 0x1c (`+7` dwords) | decompile ≡ bytes |

### Name

`RcElem28_AssignFillRange` (**Inferred** structural). Reject scaffold `Named_CalleeOf_*_gfxDevice_*`. Sibling: BasicString fill-assign `0x00431450` (W33-I) uses `operator=`; this is inline RC assign.
