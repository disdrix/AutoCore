# Annotated low-level: stoChunkWriter_WriteU32 (was FUN_00439810)

| Field | Value |
|---|---|
| Stable ID | `aa_00439810` |
| VA | `0x00439810` |
| Body | `0x00439810`–`0x00439861` |
| Canonical name | `stoChunkWriter_WriteU32` |
| System | storage / arda2.stoChunk |
| Date | 2026-07-23 scaffold; annotated 2026-07-29 W20-D |

## Machine-level notes

- Source: raw capture for `aa_00439810` + live re-verify W20-D.
- Prefer body bytes / call-site context when decompiler register names conflict.
- Twin of dual-sealed `stoChunkWriter_WriteI32` (`0x00439870`); only format string differs (`"%u"` vs `"%i"`).
- Prefer assembly-shaped register ABI over inventing formal C parameters.

## ABI

| Register / field | Role |
|---|---|
| **ESI** | `stoChunkWriter*` (caller preserves; not ECX thiscall) |
| **EAX** | `const uint32_t*` value pointer on entry |
| Return **EAX** | `writer->status` at `+0x04` (OR-accumulated I/O flags) |
| Epilogue | `pop ecx; ret` (temp slot only; not stack-arg cleanup) |

## Writer layout (this unit)

| Off | Role |
|---:|---|
| `+0x00` | stream object* (vtbl with write @ `+0x18`) |
| `+0x04` | `uint status` (OR flags) |
| `+0x1c` | mode: `0` = binary, nonzero = text |
| `+0x20` | text column (cleared before CRLF in text path) |

## Control flow

```
v = *pValue                                    // *EAX
if writer.mode == 0:                           // [esi+0x1c]
  status |= stream.write(&v, 4)                // vtbl+0x18
  return status
else:
  status |= WriteFormatted(writer, "%u", v)    // EAX=writer, format DAT_00a9d718
  textCol = 0                                  // [esi+0x20]
  status |= stream.write("\r\n", 2)            // DAT_00a97b84
  return status
```

## Constants

| Address | Value |
|---|---|
| `0x00a9d718` | `"%u"` |
| `0x00a97b84` | `"\r\n"` |
| Binary write size | `4` |
| CRLF write size | `2` |

## Pseudocode (annotated copy of raw)

```c
// ESI = writer*, EAX = const uint32_t* pValue
uint32_t stoChunkWriter_WriteU32(void)
{
  uint32_t *in_EAX;      /* pValue */
  uint uVar1;
  uint32_t *unaff_ESI;   /* writer */
  uint32_t local_4;

  local_4 = *in_EAX;
  if (unaff_ESI[7] == 0) {  /* mode binary */
    uVar1 = (**(code **)(*(int *)*unaff_ESI + 0x18))(&local_4, 4);
    unaff_ESI[1] = unaff_ESI[1] | uVar1;
    return unaff_ESI[1];
  }
  /* text path */
  uVar1 = FUN_00767160(&DAT_00a9d718, local_4);  /* "%u" */
  unaff_ESI[1] = unaff_ESI[1] | uVar1;
  unaff_ESI[8] = 0;                              /* textCol */
  uVar1 = (**(code **)(*(int *)*unaff_ESI + 0x18))(&DAT_00a97b84, 2); /* "\r\n" */
  unaff_ESI[1] = unaff_ESI[1] | uVar1;
  return unaff_ESI[1];
}
```

## Callers (sample)

Many serializers: gfx geometry/index/vertex buffers, chunk writers in `0x00458xxx`–`0x0045bxxx` band, `FUN_00744460`, `FUN_00739a60`, etc.

## Open questions

1. Full stream interface type behind `*writer` / vtbl `+0x18` write method name.
2. Runtime golden dumps for values 0, 1, 0xFFFFFFFF in text vs binary.
3. Product English for writer class (stoChunkWriter is descriptive from family duals).
