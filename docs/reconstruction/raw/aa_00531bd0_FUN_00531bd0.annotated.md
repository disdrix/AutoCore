# Annotated low-level: CVOGMission_EnsurePendingObjectiveState_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00531bd0` |
| VA | `0x00531bd0` |
| System | missions-progression |
| Date | 2026-07-29 dual seal |

## Machine-level notes

- Sibling of `CVOGMission_AddActiveObjective` (`0x00531b00`) — packet rehydrate path only.
- `__thiscall` + 2 stack args; **`ret 8`**.
- Gate: `objDef!=0` and evaluator span `[+0x158,+0x15c)` non-empty.
- Lookup/insert hash: **`character+0x55c`**; key **`objDef+0x10`**.
- Node size **0x14**: `[0]=def`, `[1..4]=slot blob` (4 dwords).
- Insert via `FUN_0053c760` **only if** lookup miss (new node).
- Decompiler drops insert ECX — asm reloads `[edi+0x55c]`.
- Sole caller: `CVOGCharacter_ApplyCreateFromPacket` site `0x00535505`.
- **No** `+0x548` def insert, **no** evaluator vcall `+0x28`, **no** dirty `+0x634`.

## Pseudocode (annotated)

```c
void __thiscall FUN_00531bd0(int character, int objDef, int *slotBlob4)
{
  bool isNew;
  int *node;

  if (((objDef != 0) && (*(int *)(objDef + 0x158) != 0)) &&
     (*(int *)(objDef + 0x15c) - *(int *)(objDef + 0x158) >> 2 != 0)) {
    node = CNDHash_LookupByKey(*(void **)(character + 0x55c), *(uint *)(objDef + 0x10));
    isNew = false;
    if (node == 0) {
      node = operator_new(0x14);
      if (node) { node[0]=node[1]=node[2]=node[3]=node[4]=0; }
      isNew = true;
    }
    *node = objDef;
    node[1] = slotBlob4[0];
    node[2] = slotBlob4[1];
    node[3] = slotBlob4[2];
    node[4] = slotBlob4[3];
    if (isNew) {
      // ECX = *(character+0x55c)  — restored in asm
      FUN_0053c760(*(undefined4 *)(objDef + 0x10), node, 0);
    }
  }
  return;
}
```

## Dual

- A: `reviews/A_aa_00531bd0_CVOGMission_EnsurePendingObjectiveState_Inferred.md`
- B: `reviews/B_aa_00531bd0_CVOGMission_EnsurePendingObjectiveState_Inferred.md`
