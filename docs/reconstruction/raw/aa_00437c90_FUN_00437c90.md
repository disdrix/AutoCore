# Raw capture: FUN_00437c90

| Field | Value |
|---|---|
| **Stable ID** | `aa_00437c90` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00437c90` |
| **Canonical name** | `FUN_00437c90` |
| **System** | arda2/storage (stoAbstractUnserializationFactory map B lookup/invoke) |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_00437c90(void)

{
  undefined4 uVar1;
  int iVar2;
  undefined4 local_4;
  
  uVar1 = FUN_0076a900();
  FUN_00438ca0();
  FUN_0046c1b0();
  iVar2 = FUN_00438ca0();
  if (local_4 != *(int *)(iVar2 + 8)) {
    uVar1 = (**(code **)(local_4 + 0xc))(uVar1);
    return uVar1;
  }
  return 0;
}
```

---

## W36-C re-verify (2026-07-29) — append only

| Field | Value |
|---|---|
| **Tools** | `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + `get_function_by_address` + `get_function_callers`. **No** `disassemble_bytes`. |
| **Body** | `0x00437c90`–`0x00437cce` (**63 B** / `0x3F`); pad `CC` then next. |
| **Classification** | worker (factory map B lookup + invoke creator) |
| **ABI** | **No stack formals.** Entry **`EAX = stoChunkReader*`** (register formal; prologue `MOV ESI,EAX`). Peek uses **ESI**. Plain **`RET`** after `ADD ESP,8`. Returns creator result in **EAX**, or **0** on miss. |
| **Live decompile** | CF matches: peek tag → MapB find → if node ≠ sentinel call `[node+0xC]` else 0. |
| **Callees** | `FUN_0076a900` (`stoChunkReader_PeekChunkTag` family), `FUN_00438ca0` (MapB GetInstance ×2), `FUN_0046c1b0` (find ×1) |
| **Callers (1)** | `FUN_00765740` @ `0x00765cce` (1 site). |
| **Map** | Exclusive **MapB** (`FUN_00438ca0` / `DAT_00d1fcec`). Twin of MapA path which uses `FUN_00438c40` at `FUN_00437b00` (W36-A) — MapA body is **larger** (out-param + secondary vcall). |
| **Invoke** | Bytes: `POP ESI` (restore reader); `JE miss`; **`CALL DWORD PTR [ECX+0xC]`**; `ADD ESP,8; RET`. **No stack push** of tag before call. Decompiler's `(tag)` argument is **not** stack-proved — seal as **indirect call through node+0xC** with **ESI restored to reader** before call. Miss path: `XOR EAX,EAX; ADD ESP,8; RET`. |
| **Decompile ≡ bytes** | **Yes** for Peek, MapB×2, find, sentinel compare, dual RET. **Gap** on exact creator calling convention (stack arg vs ESI-only). |
| **Inferred name** | `Sto_UnserialFactoryMapB_LookupInvoke_Inferred`. **Reject** scaffold `Named_CalleeOf_*_gfxBody_*`. |

### Full body hex (63 B)

```
83ec08568bf0e8652c330089442404e8fc0f00008bf08d4c24048d442408e8fd440300e8e80f00008b4c24083b48085e7407ff510c83c408c333c083c408c3
```

### Byte-level flow

```
SUB ESP,8
PUSH ESI
MOV ESI,EAX                     ; entry: EAX = stoChunkReader*
CALL FUN_0076a900               ; PeekChunkTag (ESI this)
MOV [ESP+4],EAX                 ; tag
CALL FUN_00438ca0               ; MapB bag*
MOV ESI,EAX
LEA ECX,[ESP+4]                 ; &tag
LEA EAX,[ESP+8]                 ; &out_node
CALL FUN_0046c1b0               ; find
CALL FUN_00438ca0               ; bag* again (sentinel host)
MOV ECX,[ESP+8]                 ; node*
CMP ECX,[EAX+8]                 ; vs bag.sentinel
POP ESI                         ; restore reader*
JE  miss
CALL DWORD PTR [ECX+0xC]        ; creator
ADD ESP,8
RET
miss:
XOR EAX,EAX
ADD ESP,8
RET
```

### Call target checks (bytes)

| Site | Target |
|---|---|
| first `E8` | `FUN_0076a900` PeekChunkTag |
| next two `E8` MapB | `FUN_00438ca0` |
| find `E8` | `FUN_0046c1b0` |

### Gaps

- Creator function ABI (decompiler claims tag arg; bytes show bare `CALL [mem]` with ESI=reader restored).
- Product role of sole caller `FUN_00765740`.
- MapA twin `FUN_00437b00` differences owned by W36-A.
- Runtime / bit-exact / differential.
