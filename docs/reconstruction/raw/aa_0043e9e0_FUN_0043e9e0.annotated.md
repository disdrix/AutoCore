# Annotated low-level: FUN_0043e9e0 / GuardedVector_PushBack_Stride2_U32U8

| Field | Value |
|---|---|
| Stable ID | `aa_0043e9e0` |
| VA | `0x0043e9e0`–`0x0043ea4d` (**110 B**) |
| System | STL / guarded ring-deque (stride-2) |
| Date | 2026-07-29 (W31-J) |
| Named | `GuardedVector_PushBack_Stride2_U32U8` (**Inferred**) |

## Machine-level notes

- Bit-twin of `aa_0043e970` except store helper (`FUN_0043ecf0` dword+byte).
- Same EAX container / RET 4 / grow / page wrap / `new(0x10)` / `*8` slot math.
- Element storage still **8-byte** slots (stride-2 page map); POD write width is **5** bytes (u32 + u8).
- Callers: optional-CS wrapper `FUN_0043e7f0` and `FUN_0075de80`.

## Annotated control flow

```
// EAX=container, stack=const {u32,u8}*
// … identical grow + page + alloc as U32x2 sibling …

dest = pages[page] + (abs & 1) * 8
CALL Pod5_CopyIfNonNull(dest /*EAX*/, value /*ECX*/)  // FUN_0043ecf0:
//   if (dest) { *(u32*)dest = *(u32*)src; *((u8*)dest+4) = *((u8*)src+4); }

size += 1
RET 4
```

## Open residual

- Product/PDB element struct English.
- Whether trailing 3 bytes of each 8-byte slot are intentional padding or unused.
- Runtime / bit-exact.
