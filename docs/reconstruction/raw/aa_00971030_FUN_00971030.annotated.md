# Annotated: FUN_00971030 → AssPreloader_PendingRemoveWithSideNotify_Inferred

| Field | Value |
|---|---|
| **Stable ID** | `aa_00971030` |
| **VA** | `0x00971030`–`0x0097104e` exclusive (**30 B** / `0x1E`) |
| **Canonical name** | `AssPreloader_PendingRemoveWithSideNotify_Inferred` (**Inferred**) |
| **Ghidra name** | `FUN_00971030` |
| **Wave** | W36-D 2026-07-29 |
| **Raw** | `aa_00971030_FUN_00971030.md` (scaffold + W36-D append) |

---

## Purpose (structural)

AssPreloader helper used by key-ring step when a tracked key is removed under progress:

1. Notify side-host locked event vector (`host+0xAC`) with `{*key, flag:0}`.
2. Erase first equal key from the **pending** GuardedVector at **preloader+0x1C**.

---

## Signature

```c
// ECX = AssPreloader*; stack key*; RET 0x4
void AssPreloader_PendingRemoveWithSideNotify_Inferred(
    void* preloader /*ECX*/,
    const int* key /*stack*/);
```

| Slot | Source | Conf |
|---|---|---|
| preloader | **ECX** → ESI | **High** |
| key | Stack[+4] → EDI | **High** |
| cleanup | **`RET 0x4`** | **High** |

### Preloader layout (used)

| Off | Field | Conf |
|---|---|---|
| +0x1C | pending GuardedVector (EraseFirstEqual container) | High |
| +0xAC | side-host U32U8 event vector (via nested push) | High (W33-N) |

---

## Stages

```
ESI = preloader; EDI = key*
CALL SideHost_PushKeyFlag0(ECX=key*, stack=preloader)   // 00970b20
ESI = preloader + 0x1C
CALL GuardedVector_EraseFirstEqual_ThiscallRange(ESI, key*)  // 0043e630
RET 4
```

| Stage | Match | Conf |
|---|---|---|
| Push flag0 first | **Yes** | **High** |
| Then erase pending +0x1C | **Yes** | **High** |
| RET 4 | **Yes** | **High** |

---

## Callers / callees

| Dir | Target |
|---|---|
| Caller | `AssPreloader_ProcessKeyRingStep_Inferred` (`004ed310`) @ `004ed3a5` |
| Callee | `SideHost_PushKeyFlag0_Inferred` (`00970b20`, W33-N) |
| Callee | `GuardedVector_EraseFirstEqual_ThiscallRange` (`0043e630`, W35-A) |

---

## Gaps

- Product meaning of flag0 vs flag1 (remove vs miss).
- AL found from erase ignored (no branch).
- Runtime / bit-exact.

---

## Verdict

**accept-with-gaps** — 30 B leaf-worker fully sealed via bytes + nested duals.
