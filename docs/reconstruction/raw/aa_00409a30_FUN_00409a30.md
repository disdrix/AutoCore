# Raw capture: FUN_00409a30

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409a30` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409a30` |
| **Canonical name** | `FUN_00409a30` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00409a30(undefined4 param_1,undefined4 param_2,undefined4 param_3)

{
  int *piVar1;
  int in_EAX;
  undefined4 uVar2;
  
  if (*(int *)(*(int *)(in_EAX + 4) + 0xc) == 0) {
    uVar2 = 0;
  }
  else {
    piVar1 = *(int **)(*(int *)(in_EAX + 4) + 0xc);
    uVar2 = (**(code **)(*piVar1 + 0x24))(piVar1,0,param_2);
  }
  piVar1 = *(int **)(*(int *)(in_EAX + 4) + 0xc);
  (**(code **)(*piVar1 + 0x50))(piVar1,uVar2,param_3,0xffffffff);
  return;
}
```

---

## WQ9J-J re-verify (2026-08-05) — live Ghidra seal

| Field | Value |
|---|---|
| **Agent** | WQ9J-J OWN-ONLY dual |
| **Tools** | `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + callers/xrefs + `disassemble_function` + call-site assembly (**no** `disassemble_bytes`) |
| **Program** | `autoassault.exe` base `0x400000` |
| **Body** | `0x00409a30`–`0x00409a64` exclusive (**52 B** / `0x34`); pad `CC` after `RET 4` |
| **Prologue (read_memory)** | `56 8B F0 8B 46 04 83 78 0C 00` — `PUSH ESI`; **`MOV ESI,EAX`** (host in **EAX**); load `host+4`; CMP service `@+0xc` |
| **Epilogue** | `5E C2 04 00` — `POP ESI`; **`RET 0x4`** (one stack formal) |
| **ABI (machine / call sites)** | Custom register: **EAX** = host ctx (callers load `DAT_00d1f05c`); **EDX** = name C-string; stack = data ptr; **RET 4**. Decompiler labels stack as `param_3`, EDX as `param_2`, phantom `in_EAX` for host. |
| **Classification** | leaf (indirect vcalls only) |
| **Callers (1 / 2 xrefs)** | `FUN_00942840` @ `0x00942c4d`, `0x00942d15` — both `MOV EDX,0xa2cf30` (`"gWorldFogCenter"`) + `MOV EAX,[DAT_00d1f05c]` then push float3* |
| **Callees** | indirect only: service vtbl **`+0x24`** (lookup by name, arg1=0) then **`+0x50`** (write handle, data, `-1`) |
| **Named role (INFERRED)** | `GfxParam_LookupAndWrite_Inferred` |
| **Integrity** | Live decompile 2026-08-05 ≡ raw 2026-07-23 body; ABI corrected via call-site + disasm (EAX host, EDX name, RET4) |

### Live decompile (authoritative 2026-08-05)

```c
void __fastcall FUN_00409a30(undefined4 param_1,undefined4 param_2,undefined4 param_3)

{
  int *piVar1;
  int in_EAX;
  undefined4 uVar2;
  
  if (*(int *)(*(int *)(in_EAX + 4) + 0xc) == 0) {
    uVar2 = 0;
  }
  else {
    piVar1 = *(int **)(*(int *)(in_EAX + 4) + 0xc);
    uVar2 = (**(code **)(*piVar1 + 0x24))(piVar1,0,param_2);
  }
  piVar1 = *(int **)(*(int *)(in_EAX + 4) + 0xc);
  (**(code **)(*piVar1 + 0x50))(piVar1,uVar2,param_3,0xffffffff);
  return;
}
```

### String / chain evidence

| Item | Evidence |
|---|---|
| Name literal at call sites | `0x00a2cf30` = `"gWorldFogCenter\0"` (`read_memory`) |
| Host global | `DAT_00d1f05c` (same host used by fog/env / light param paths) |
| Service path | `host[+4] → +0xc` interface; vtbl+0x24 / +0x50 |
| Sibling pattern | `FUN_00442d50` same lookup (`+0x24`) then float4 write helper; light ctor uses `"gPointLight"` + same service |
| Inline twin in caller | `FUN_00942840` also inlines the same lookup/write for `"gWorldFogCenter"` without calling this VA |

### Disassembly summary (disassemble_function)

```text
00409a30  PUSH ESI
00409a31  MOV ESI,EAX                ; host from EAX
00409a33  MOV EAX,[ESI+0x4]
00409a36  CMP dword ptr [EAX+0xc],0  ; service null?
00409a3a  JNZ 00409a40
00409a3c  XOR EAX,EAX                ; handle = 0
00409a3e  JMP 00409a4c
00409a40  MOV EAX,[EAX+0xc]          ; service*
00409a43  MOV ECX,[EAX]              ; vtbl
00409a45  PUSH EDX                   ; name
00409a46  PUSH 0
00409a48  PUSH EAX                   ; service this (stack)
00409a49  CALL [ECX+0x24]            ; lookup handle
00409a4c  MOV ECX,[ESI+0x4]
00409a4f  MOV ESI,[ESP+0x8]          ; data*
00409a53  MOV ECX,[ECX+0xc]          ; service* (unchecked)
00409a56  MOV EDX,[ECX]
00409a58  PUSH -1
00409a5a  PUSH ESI                   ; data
00409a5b  PUSH EAX                   ; handle
00409a5c  PUSH ECX                   ; service
00409a5d  CALL [EDX+0x50]            ; write
00409a60  POP ESI
00409a61  RET 4
```

### Odd behavior (preserve)

- Null-service check only gates **lookup**; **write path reloads service and will fault if null** (no second null test). Same as decompiler.
