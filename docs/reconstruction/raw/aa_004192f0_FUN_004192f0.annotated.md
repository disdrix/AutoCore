# Annotated low-level: FUN_004192f0 → StdTree_Rrotate_Isnil29_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004192f0` |
| VA | `0x004192f0` |
| Canonical name | `StdTree_Rrotate_Isnil29_Inferred` |
| System | MSVC `std::_Tree` right-rotate (isnil@+0x29) |
| Date | 2026-08-04 (WQ9D-D dual; scaffold 2026-07-23) |

## Machine-level notes

- **Role:** Classical red-black **right-rotate** about node `x` (`param_2`).
- **Node links:** `+0` left, `+4` parent, `+8` right; **isnil** byte at **`+0x29`** (`0` = real node).
- **Tree head:** `this+4` → header; **root** = `header+4` (compared/assigned).
- **Twin:** `FUN_004192a0` left-rotate (same isnil family; WQ9D-C).
- **Peer:** `StdTree_Rrotate_Val12` @ `0x00418c10` — identical CF except isnil@`+0x19`.
- Leaf: no callees. `RET 4`. Body **81 B**.

## Control flow (annotated)

```c
// __thiscall: ECX = tree*, stack = node* x; void; RET 4
void StdTree_Rrotate_Isnil29_Inferred(int tree, int *x)
{
  int y = *x;                              // y = x->left
  *x = *(int *)(y + 8);                    // x->left = y->right
  if (*(char *)(*(int *)(y + 8) + 0x29) == 0) {
    *(int **)(*(int *)(y + 8) + 4) = x;    // y->right->parent = x  (if real)
  }
  *(int *)(y + 4) = x[1];                  // y->parent = x->parent
  if (x == *(int **)(*(int *)(tree + 4) + 4)) {
    // x was root → root = y
    *(int *)(*(int *)(tree + 4) + 4) = y;
    *(int **)(y + 8) = x;                  // y->right = x
    x[1] = y;                              // x->parent = y
    return;
  }
  int *parent = (int *)x[1];
  if (x == (int *)parent[2]) {
    // x was right child
    parent[2] = y;
    *(int **)(y + 8) = x;
    x[1] = y;
    return;
  }
  // x was left child
  *parent = y;
  *(int **)(y + 8) = x;
  x[1] = y;
}
```

## Callers (rebalance / erase consumers)

Shared map/tree helpers (not skill-only): erase/insert rebalance paths including
`FUN_0051cb40` (Map_EraseNode family), `FUN_00406c40`, `FUN_00409220`,
`FUN_004cb740` / `FUN_004cbb60`, `FUN_0058da40` / `FUN_0058dc30`,
`FUN_005a3310`, `FUN_00603830` / `FUN_00603c90`.

## Open questions

- Product / MSVC demangle for each map value type sharing isnil@+0x29.
- Exact value payload size (color likely @+0x28); not required for rotate CF.
- Runtime / bit-exact open.
