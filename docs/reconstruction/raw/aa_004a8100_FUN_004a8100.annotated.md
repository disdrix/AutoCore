# Annotated low-level: StdVector_PushBack_BasicStringW

| Field | Value |
|---|---|
| Stable ID | `aa_004a8100` |
| VA | `0x004a8100`–`0x004a8187` exclusive (**135 B**) |
| Canonical name | `StdVector_PushBack_BasicStringW` |
| Ghidra name | `FUN_004a8100` |
| System | MSVC `vector<basic_string<wchar_t>>::push_back` (elem **0x1c**) |
| Date | 2026-08-04 (W38-K) |

## Machine-level notes

- Source: raw capture + W38-K `read_memory` full body.
- Prefer bytes when decompiler invents extra fill-n formals.
- Prologue: `PUSH EBX; PUSH ESI; PUSH EDI; MOV EDI, ECX` — **thiscall** vector in ECX.
- Magic `0x92492493` twice → `(end-begin)/0x1c` and `(cap_end-begin)/0x1c`.
- Fast: spare capacity → `StdUninitFillN_BasicStringW` count=1 at end; `end += 0x1c`; **`RET 4`**.
- Slow: empty begin or size≥cap → `StdVector_InsertOne_RebindIt_BasicStringW` at end; discard outIt; **`RET 4`**.
- Call site of fill-n pushes **5** dwords (`dest, count, value, alloc=this, value_echo`) + `ADD ESP,0x14` (W36-S / W36-R residual); callee body uses first three.

## Recovered CF (annotated)

```c
// __thiscall ECX=vec*; stack value*; RET 4
void StdVector_PushBack_BasicStringW(StdVectorBasicStringW* vec, const BasicStringW_0x1c* value)
{
  BasicStringW_0x1c* begin = vec->begin; // +0x04
  uint32_t size = 0;
  if (begin != nullptr) {
    size = (uint32_t)((char*)vec->end - (char*)begin) / 0x1c;
  }
  if (begin != nullptr) {
    uint32_t cap = (uint32_t)((char*)vec->cap_end - (char*)begin) / 0x1c;
    if (size < cap) {
      BasicStringW_0x1c* dest = vec->end; // +0x08
      // retail: 5-dword cdecl push pattern into FUN_004a74f0
      StdUninitFillN_BasicStringW(dest, 1, value /*, alloc, value_echo */);
      vec->end = (BasicStringW_0x1c*)((char*)dest + 0x1c);
      return;
    }
  }
  BasicStringW_0x1c* outIt_discard;
  StdVector_InsertOne_RebindIt_BasicStringW(vec, &outIt_discard, vec->end, value);
  (void)outIt_discard;
}
```

## Call graph

| Direction | VA | Name / role |
|---|---|---|
| Caller | `0x004a1d80` @ `0x004a1e57` | wstring token split → push_back each token |
| Callee fast | `0x004a74f0` | `StdUninitFillN_BasicStringW` (W36-R) |
| Callee slow | `0x004a7ff0` | `StdVector_InsertOne_RebindIt_BasicStringW` (W37-W) |

## Open questions

1. Product MSVC demangle English (`std::vector<std::wstring>::push_back`).
2. Runtime / bit-exact under grow.
3. Whether stack formal is `const T&` vs by-value (footprint still one pointer-sized slot at call site from `FUN_004a1d80`).
