# Annotated low-level: FUN_0051dd60 → SkillCNDHash_Ctor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051dd60` |
| VA | `0x0051dd60`–`0x0051ddd2` (**115 B**) |
| System | skills-abilities / skill-family CNDHash |
| Date | 2026-07-23 scaffold; **2026-08-04 WQ9D-A dual seal** |
| Canonical | `SkillCNDHash_Ctor_Inferred` |

## Machine-level notes

- **ABI:** `__thiscall`; `ECX` = raw `0x34`-byte block; stack `uint8_t log2`; **returns this in EAX**; **`ret 4`**.
- **SEH:** `LAB_009a371b` frame around body + `FUN_004cb410` (alloc can throw).
- **Identity:** stores vtbl **`PTR_FUN_009ce1b8`** at `this+0` — skill-family CNDHash (distinct from inventory `009cb45x` / sheet families).
- **Sizing:** `*(this+8) = 1 << (log2 & 0x1f)` provisional count; `*(byte*)(this+0x1c) = log2`; lock `*(byte*)(this+0x1d) = 0`.
- **Zeros:** `+0x04`, `+0x0c`, `+0x10`, `+0x14`, `+0x18`, `+0x20`, `+0x28`, `+0x2c`, `+0x30`.
- **Odd (preserved):** **`this+0x24` is never written** by the ctor (gap). Do not invent a zero store.
- **Post-ctor:** `FUN_004cb410` builds empty bucket table; `@+8` becomes mask when log2 in 1..16.
- **Sole caller:** `Object_CreateOrRecreateSkillHash_Inferred` missing path (`operator_new(0x34)` → this).
- **Scalar dtor peer (not dualled):** `FUN_0051de60` @ nearby — `FUN_0051de80` + optional `operator_delete`.

## Pseudocode (annotated)

```c
// SkillCNDHash* __thiscall SkillCNDHash_Ctor_Inferred(void *mem, uint8_t log2)
undefined4 * __thiscall FUN_0051dd60(undefined4 *thisHash, byte log2)
{
  // SEH: LAB_009a371b
  *thisHash = &PTR_FUN_009ce1b8;                 // vtbl
  thisHash[1] = 0;                               // +0x04
  thisHash[3] = 0;                               // +0x0c
  thisHash[2] = 1 << (log2 & 0x1f);              // +0x08 provisional count
  thisHash[4] = 0;                               // +0x10 bucket table
  thisHash[5] = 0;                               // +0x14 ordered head
  thisHash[6] = 0;                               // +0x18 ordered tail
  *(byte *)(thisHash + 7) = log2;                // +0x1c log2
  *(undefined1 *)((int)thisHash + 0x1d) = 0;     // +0x1d lock clear
  thisHash[8] = 0;                               // +0x20 freelist head _Inferred
  // +0x24 intentionally NOT written
  thisHash[10] = 0;                              // +0x28
  thisHash[0xb] = 0;                             // +0x2c
  thisHash[0xc] = 0;                             // +0x30
  FUN_004cb410();                                // alloc empty buckets
  return thisHash;                               // EAX = this; ret 4
}
```

## Open / residual

- Product English for class behind `009ce1b8`.
- Semantics of unwritten `+0x24` (pad vs forgotten field).
- Nested dual of `FUN_004cb410`.
- Runtime / bit-exact open. Terminal false.
