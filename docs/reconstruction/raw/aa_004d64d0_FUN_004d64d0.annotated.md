# Annotated low-level: FUN_004d64d0 → CVOGSectorMap_Destructor

| Field | Value |
|---|---|
| Stable ID | `aa_004d64d0` |
| VA | `0x004d64d0`–`0x004d6c77` (**1960 B**) |
| System | client / CVOGSectorMap |
| Date | 2026-07-29 (W26-J re-annotate; scaffold 2026-07-23) |

## Machine-level notes

- **Role:** C++ destructor body for **CVOGSectorMap** (product class sealed via ctor twin `aa_004d8a10` + `@@new CVOGSectorMap`).
- **ABI:** `mov esi,ecx`; frame `sub/add esp,8`; bare `ret`. `__thiscall`.
- **Vtbl:** reinstalls `PTR_FUN_009cc770` (same complete-object vtbl as ctor).
- **Lifecycle counter:** `_DAT_00b037e4--` (ctor does `++`).
- **Hash teardown:** object at dword index `0x3971` uses CNDHash traversal lock at `+0x1d`; walk chain via `+0x14` / payload `+8`; destroy via `FUN_004e3610` + optional `operator_delete`.
- **Owned teardown:** long cascade of virtual scalar-delete `(*vtbl)(1)` on member pointers, buffer triple zeros (`begin/end/cap` style), and subsystem helpers (`FUN_004e72xx`, `FUN_00497360`, `FUN_004bcbf0` embedded list, etc.).
- **Decompiler trap:** `operator_delete` marked noreturn → raw shows nested early-out ladder; **bytes** free then continue to shared epilogue (`5E 5B 83 C4 08 C3`).
- Prefer live bytes + this annotation over raw nesting for port order of non-delete calls.

## Offset map (selected, dword indices → byte)

| Index | Byte off | Role (CF) |
|------:|---------:|---|
| 0 | +0x00 | vtbl → `0x009cc770` |
| 0x3a04 | +0xE810 | cleared flag/store at entry |
| 0x3a1d..1f | +0xE874.. | buffer triple free (early + late) |
| 0x3a45 | +0xE914 | virtual-delete member |
| 0x3971 | +0xE5C4 | CNDHash* (traversal/destroy loop) |
| 0x397f / 0x3991 | | lists drained via `ThreadSafeObjectList_PopFront` |
| 0x3a05 / 0x3a03 | | resource free via `DAT_00b05060` vtbl+0x14, size `n*4`, tag `0x12` |
| 0x15..0x17 | +0x54 | late buffer triple |

Full per-store order: see raw pseudocode + live decompile.

## Pseudocode (annotated — sequential model)

```c
// __thiscall  ECX = CVOGSectorMap* this
void CVOGSectorMap_Destructor(CVOGSectorMap *self)
{
  // *self = PTR_FUN_009cc770
  // self[0x3a04] = 0
  // _DAT_00b037e4--

  // free early buffer self[0x3a1d..1f]
  // virtual-delete self[0x3a45]

  // lock-traverse hash self[0x3971]:
  //   for each entry: HashError checks; FUN_004e3610(key, &tmp); maybe operator_delete(tmp)
  // unlock; virtual-delete hash + sibling owned ptrs (0x3973..0x3948 cascade)

  // if self[0x3942]: FUN_005b0680(); operator_delete
  // drain list self[0x397f] via PopFront (discard payloads); clear count; virtual-delete list
  // drain list self[0x3991] via PopFront + payload (*vtbl)(1); virtual-delete list

  // virtual-delete more members; FUN_004d2970(); FUN_005bf720();
  // clear peer link self[0x3a27]+0x2e4; call (*(self[0x3a27]+0x2c)->vtbl+0x10)()

  // buffer frees + FUN_004e7380 / FUN_00497360×2 / FUN_004e7350×2
  // if self[0x3a05] >= 0: (*DAT_00b05060)[0x14](self[0x3a03], self[0x3a05]*4, 0x12)

  // remaining buffer triples + FUN_004e7320 / FUN_004e4460 / FUN_004e4410
  // FUN_004bcbf0() on embedded ThreadSafeObjectList
  // FUN_004e7200×2 / FUN_004e72f0 / FUN_004e72c0 / FUN_004e7290
  // return
}
```

## Open questions

- Exhaustive product English for every 0xE920 field (same gap as ctor).
- Which vtbl slot (if any) points at this body vs Catch-only call path.
- Runtime / bit-exact image diff.
