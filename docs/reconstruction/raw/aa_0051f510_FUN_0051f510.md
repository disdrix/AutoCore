# Raw capture: FUN_0051f510

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051f510` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051f510` |
| **Canonical name** | `FUN_0051f510` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
float10 FUN_0051f510(void *param_1)

{
  int iVar1;
  
  if ((param_1 != (void *)0x0) && (*(int *)((int)param_1 + 0x14c) != 0)) {
    iVar1 = Mission_ComputeObjectiveXp(param_1);
    return (float10)iVar1;
  }
  return (float10)g_flZero;
}
```

---

## W25-I live seal append — 2026-07-29

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual W25-I |
| **Tools** | Ghidra `decompile_function` + `analyze_function_complete` + `read_memory` + callers/xrefs (**no** `disassemble_bytes`) |
| **Live decompile** | Identical CF to raw body above |
| **Body span** | `0x0051f510`–`0x0051f539` (42 B; last `ret 4`) |
| **Classification** | wrapper |
| **Proposed name** | `Mission_ComputeObjectiveXpAsFloat` (**Inferred**) |

### Live decompile (2026-07-29)

```c
float10 FUN_0051f510(void *param_1)

{
  int iVar1;
  
  if ((param_1 != (void *)0x0) && (*(int *)((int)param_1 + 0x14c) != 0)) {
    iVar1 = Mission_ComputeObjectiveXp(param_1);
    return (float10)iVar1;
  }
  return (float10)g_flZero;
}
```

### Byte seal (`read_memory` 48 B @ `0x0051f510`)

```
8b 4c 24 04          mov  ecx, [esp+4]           ; param_1 (objective*)
85 c9                test ecx, ecx
74 19                jz   fail                   ; -> fld g_flZero
83 b9 4c 01 00 00 00 cmp  dword ptr [ecx+0x14c], 0  ; mission def*
74 10                jz   fail
e8 ba e8 07 00       call Mission_ComputeObjectiveXp  ; -> 0x0059dde0
89 44 24 04          mov  [esp+4], eax           ; int XP
db 44 24 04          fild dword ptr [esp+4]      ; ST0 = (float)XP
c2 04 00             ret  4
fail:
d9 05 18 f5 a0 00    fld  dword ptr [0x00a0f518] ; g_flZero = 0.0f
c2 04 00             ret  4
```

Full body hex (42 B, excl. `cc` pad):
```
8b4c240485c9741983b94c010000007410e8bae8070089442404db442404c20400d90518f5a000c20400
```

| Claim | Evidence | Conf |
|---|---|---|
| Stack formal + `ret 4` | `[esp+4]` load; two `C2 04 00` | **High** |
| ECX = objective for callee | `mov ecx,[esp+4]` before call | **High** |
| Callee = `Mission_ComputeObjectiveXp` | rel32 `e8 ba e8 07 00` → `0x0059dde0` | **High** |
| Return float in ST0 | `fild` / `fld` then ret | **High** |
| Fail path loads `g_flZero` | `fld [0x00a0f518]`; memory `00 00 00 00` | **High** |
| Guard `obj+0x14c` mission def | `cmp [ecx+0x14c],0` | **High** |
| Sole code xref | `0x008ac61b` in `Client_ShowMissionRewardChatToast` | **High** |

### Call graph (this unit only)

| Role | Target | Notes |
|---|---|---|
| Callee | `Mission_ComputeObjectiveXp` @ `0x0059dde0` | pure int XP calc (dual residual sealed elsewhere) |
| Caller | `Client_ShowMissionRewardChatToast` @ `0x008ac540` site `0x008ac61b` | presentation float path only; no AddExperience / no bias |

### Live ≡ raw

**Yes** — CF, guards, callee, float convert, zero fail path all match.
