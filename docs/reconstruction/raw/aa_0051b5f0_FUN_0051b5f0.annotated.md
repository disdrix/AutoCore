# Annotated low-level: StdDeque_ThrowTooLong_0051b5f0 (`FUN_0051b5f0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0051b5f0` |
| VA | `0x0051b5f0` |
| Body | `0x0051b5f0`–`0x0051b65f` (112 bytes) |
| Canonical name | `StdDeque_ThrowTooLong_0051b5f0` |
| Ghidra | `FUN_0051b5f0` |
| System | containers / MSVC STL deque |
| Date | 2026-07-29 (W21-E dual seal) |

---

## Machine-level notes

- **SEH frame** installed (`push -1`, `LAB_009a3502`, `fs:[0]`, `sub esp,0x44`) before locals — required because the path throws.
- Message string is a **static C string** at `0x00a31520` = `"deque<T> too long"` (sealed via `read_memory`).
- `basic_string` constructed into stack (`lea ecx,[esp+4]` + IAT `[0x009c62f8]`).
- EH state set to `0` then intermediate `1` around `exception::exception` and message copy-construct.
- Exception object at stack slot uses **two-stage vftable**:
  1. After `exception::exception`: `local_34[0] = &PTR_FUN_009c7628`
  2. After `basic_string` copy into embedded message member: `local_34[0] = &PTR_FUN_009c7634`
- `_CxxThrowException(local_34, &DAT_00acc388)` — **noreturn**. Same `ThrowInfo` family as sealed vector `_Xlen` helpers (`aa_00418130` / `aa_004540b0`).
- **No formals.** Callers treat this as pure side-effect throw on map/size overflow.
- Role twin of other `"deque<T> too long"` clones (`FUN_0040ff30`, `FUN_00436860`, `FUN_005b2a80`) — **not** merged without byte-equality proof. VA-qualified clean name avoids collision.
- Sole dual-sealed caller in this wave: `FUN_0051c3b0` (skill deferred-cast deque map grow).

---

## Pseudocode (annotated)

```c
/* StdDeque_ThrowTooLong_0051b5f0 — MSVC deque::_Xlen()-class helper (noreturn).
   Builds length_error-class exception with plate "deque<T> too long" and throws. */

void __cdecl StdDeque_ThrowTooLong_0051b5f0(void)  /* noreturn */
{
  /* SEH + stack: basic_string msg; exception_object ex; basic_string ex.msg; */
  /* basic_string_ctor(&msg, "deque<T> too long" @ 0x00a31520); */
  /* exception::exception(&ex);  vftable = PTR_FUN_009c7628 */
  /* basic_string_copy(&ex.msg, &msg);  vftable = PTR_FUN_009c7634 */
  /* _CxxThrowException(&ex, &DAT_00acc388); */
}
```

Control flow is **linear single block** — always throws; no success return.

---

## Open questions

1. Product/PDB English symbol (likely `std::deque<T>::_Xlen` or similar) — open; role sealed from plate string + CF.
2. Exact C++ exception type hierarchy (`PTR_FUN_009c7634` RTTI / `length_error` vs custom) — out of owned scope.
3. Why multiple plate-string clones exist (code-gen / per-TU) — not merged without byte-equality proof.
4. Runtime / bit-exact — open.
