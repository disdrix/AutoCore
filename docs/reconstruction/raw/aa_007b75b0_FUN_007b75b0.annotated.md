# Annotated low-level: FUN_007b75b0 → AssManager_InitPrecompileAndResolvers_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_007b75b0` |
| VA | `0x007b75b0`–`0x007b7b6d` (**~1469 B**) |
| System | assets / assManager / NDAssetManager |
| Date | 2026-07-23 scaffold; W30-D seal 2026-07-29 |

---

## Machine-level notes

- Client-side bootstrap for precompiled-effect storage + optional resolver registration.
- ABI: stdcall **2** formals `(self*, char mode)`, **`RET 8`**. Epilogue `ADD ESP,0x50; RET 8`.
- `self+0x04` flags: bit0 resolver-A path, bit1 resolver-B path.
- `self+0x08` / `+0x0c` owned resolver slots (pair with `AssManager_ReleaseOwnedResolvers_Inferred` @ `0x007b65d0`).
- Global `DAT_00d1f058` = assManager host; resolver GuardedVector at **+0x58** (same as RemoveResolver).
- Resolver A insert uses `GuardedVector_PushBackLocked` (`0x0043c4f0`, EDI=host+0x58).
- Resolver B insert inlines host CS (`+0x84`/`+0x6c`) + raw `GuardedVector_PushBack` (not the locked wrapper).
- Duplicate A: soft log; duplicate B: may hard-abort via `FUN_0076d1b0` / `ExitProcess(3)`.
- Path strings + NDAssetManager.cpp plate for mkdir fail; assManager.cpp for duplicate resolver.
- Callers: client window init `FUN_00948bd0` (passes `FUN_007b6a20()` result + `1`) and `FUN_0094a6a0`.
- Large unowned fan-out: path helpers, `FUN_00984ee0` (shader/effect batch), string bag walk.

---

## Pseudocode (stage sketch)

```c
// stack (self*, mode); RET 8
void AssManager_InitPrecompileAndResolvers_Inferred(void* self, char mode)
{
  string path = "../effects";
  // ensure path (+ /precompile) exists or fall back to "."
  // publish path to host+0xb4; host+0xd0=1; side inits

  if (flags(self+4) & 1) {
    // new 0x4c resolver → self+8; find/push host+0x58 via PushBackLocked
    // walk DAT_00d1e570 string table
  }
  if (flags(self+4) & 2) {
    // new 0x5c resolver → self+0xc; find/push under host CS
    // FUN_00984ee0(1) × N (mode-dependent)
  }
}
```

---

## Open residual

- Product/PDB method English on assManager / NDAssetManager classes.
- Exact types of resolvers `00423be0` / `0x5c` vftable object (not OWN).
- Semantics of `FUN_00984ee0(1)` mass calls and `FUN_00754280` / `FUN_00989ef0`.
- Runtime / bit-exact under live FS + contended host CS.
