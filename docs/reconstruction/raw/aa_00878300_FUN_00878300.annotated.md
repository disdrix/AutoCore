# Annotated low-level: FUN_00878300 → UI_ArenaConsole_CreateChildWidgets_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00878300` |
| VA | `0x00878300` |
| System | inventory-transfer (partition; pure client UI construct) |
| Date | 2026-08-05 (R13-010 dual seal) |
| Canonical | `UI_ArenaConsole_CreateChildWidgets_Inferred` |
| Ghidra | `FUN_00878300` |
| Plate alias (retired as canonical) | `UI_arena_console_tab_tournament` |

## Machine-level notes

- **ABI:** `__thiscall` — `ECX` = arena-console host*; `MOV EBP,ECX` at entry; plain `RET` (`C3`); void.
- **Body:** `0x00878300`–`0x00878c14` inclusive (**0x915** B). SEH frame `LAB_009b902f`.
- **Dispatch:** sole xref is **DATA** vtbl slot `0x00a5a640` on host vtbl base `PTR_FUN_00a5a204` (offset **+0x43C**). No direct CALL callers.
- Prefer **disasm** for XML string args and command ids — decompiler drops most `PUSH imm` path/id immediates.
- Parent partition residual under dualed `UI_Widget_MoveToFrontInParentGroup_Inferred` (`0x00756c90`) — called twice after collapsed tournament/advanced tab chrome.
- Dualed tab helper `UI_TabChrome_SetSelected_Inferred` (`0x00822cb0`) — ESI=tab, BL=selected, stack force; used for console (selected) + tournament/advanced (unselected).
- **No inventory C2S / sector send** in body.

## Host pre-init (High)

| Site | Value | Role |
|---|---|---|
| `host+0x524` (byte) | 0 | clear |
| `host+0xc5` (byte) | 0 | clear |
| `host+0x4fc` | `0xf` | host mode/flags dword (shared pattern with arena prefs) |
| `host+0x500` | 1 | companion flag |
| vtbl `+0x110` / `+0x130` | prep | host virtual prep before tree build |
| `FUN_00792600` | host prep | residual (resolution / child-hash reparent family) |
| Saves `host+0x528/+0x52c` | stack | used later as base offsets in mini-bg size normalize |

## Child factory pattern (High)

```
p = operator_new(size)
p = p ? TypedCtor(p [,0]) : 0
host[slot] = p
host.vtbl[+0xa8](p)          // parent/attach child
child.vtbl[+0x28](xml_path)  // load skin/XML
// optional: +0x43c show, +0x74(cmdId), +0xcc(0) hide, +0xc4=1, SetSelected, MoveToFront
```

### Ctor taxonomy

| Ctor | new size | Count | Role |
|---|---:|---:|---|
| `FUN_007b5dd0` | `0x488` | 12 | base NDUI window/label/texture |
| `FUN_00864f20` | `0x4bc` | 1 | FX panel (`NDUIFxPanel_Ctor_Inferred` dualed) |
| `FUN_0078f890` | `0x954` | 1 | scroll/list-style (dynamic news) |
| `FUN_0079c860` | `0x4cc` | 4 | button subclass |
| `FUN_008230b0` | `0x81c` | 3 | tab chrome |

## Sealed child ↔ XML ↔ command map

See raw re-verify table (disasm-sealed). Command ids:

| Id | Widget |
|---:|---|
| `0x9c40` | close button |
| `0x9c41` | instant action |
| `0x9c42` | my stats |
| `0x9c43` | tab console (default selected) |
| `0x9c44` | tab tournament (collapsed + move-to-front) |
| `0x9c45` | tab advanced (collapsed + move-to-front) |
| `0x9c46` | dynamic-news scrollbar/list |
| `0x9c47` | mini cancel |

## Tab policy (High)

1. **Console tab** (`+0x580`): SetSelected(**1** / force 1), then child vtbl `+0x94`.
2. **Tournament** (`+0x584`): SetSelected(**0**), then `UI_Widget_MoveToFrontInParentGroup` (EDI=tab).
3. **Advanced** (`+0x588`): SetSelected(**0**), then MoveToFront.

Default visible tab is **console**; tournament/advanced are built collapsed and reordered to front of parent group (z-order residual).

## Mini overlay + size normalize (High)

If `host+0x530` or `host+0x534` ≤ `DAT_00aaac10` (**-99998.0f**):
- Query mini-bg size via child vtbl `+0x120`
- Normalize: `(base + host offset) * (1200|1600) / (DAT_00d1e818|DAT_00d1e81c)` when `DAT_00d17944 != 0`
- Store back to `host+0x530/+0x534`
- Apply position via child vtbl `+0x110`

## Tail (High)

- `FUN_008778e0(host)` — residual post-build helper (not dualled here)
- If `DAT_00d1b6d8 != 0` and `player+0x4fc != 0`: `FUN_00877c50(host)` (AL=1 path)
- Host vtbl `+0x34c` finalize/refresh
- SEH leave

## Open questions

- Product / RTTI class name of arena console host (CDlg* demangle).
- Exact English meaning of host flags `+0x4fc=0xf`, `+0x500=1`, player `+0x4fc`.
- Full semantics of residual callees `FUN_00792600`, `FUN_00877790`, `FUN_008778e0`, `FUN_00877c50`, tab ctor `FUN_008230b0`.
- When virtual fires relative to dialog show / net open (vtbl-only dispatch).
- Runtime / bit-exact / differential.
