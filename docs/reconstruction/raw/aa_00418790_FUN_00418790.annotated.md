# Annotated low-level: FUN_00418790 → StdList_Incsize_00418790

| Field | Value |
|---|---|
| Stable ID | `aa_00418790` |
| VA | `0x00418790`–`0x004187fe` (**111 B** / `0x6F`) |
| Ghidra name | `FUN_00418790` |
| Canonical name | `StdList_Incsize_00418790` |
| Prior scaffold | `Named_CalleeOf_Named_CalleeOf_Client_RecvBroadcast_00418790` (**retired**) |
| System | missions-progression (partition) + multi-system shared MSVC `std::list` |
| Date | 2026-08-05 (R10-024 dual A/B re-verify) |
| Tools | `decompile_function` + `analyze_function_complete` + `disassemble_function` + callers/xrefs + `read_memory` + `get_assembly_context` + `get_function_by_address` |

---

## Machine-level notes

- **ABI sealed:** **`__fastcall`** — **ECX** = `count`, **EDX** = `list*`; void; plain **`RET`** (`C3`). Not thiscall (list is not in ECX).
- **CF:** SEH frame; one overflow branch; success path increments size; throw path noreturns.
- **Semantics:** MSVC **`std::list::_Incsize(size_type count)`**:
  1. Load `_Mysize` from `list+8`.
  2. If `0x3fffffffU - _Mysize < count` → build `"list<T> too long"` string → `FUN_00401a40` (`Std_LengthError_CtorFromString`) → `_CxxThrowException(..., DAT_00acc388)`.
  3. Else `_Mysize += count`.
- **Does not** allocate nodes, rewire links, or touch `list+0` / `list+4` — callers (push/insert) do BuyNode + splice around this leaf.
- **Twin:** dualed **`StdList_Incsize`** `aa_0043fe60` @ `0x0043fe60` — same CF/ABI/max/string/throw family (W23-E **accept**). This VA is a second image copy used by early insert wrappers (`FUN_004027f0`, `FUN_00403120`, …).
- **Distinct from:** thiscall size leaf `FUN_00404840` (list in ECX); lower-max twin `StdList_Incsize_Max1fffffff` @ `0x00416010`.

## Pseudocode (annotated ≡ live decompile + sealed ABI)

```c
// ECX = count; EDX = list*; plain RET
void __fastcall StdList_Incsize_00418790(uint32_t count /*ECX*/, int list /*EDX*/)
{
  // SEH: LAB_009bc919 / FS:[0] (present in retail; omitted in clean plate narrative)
  uint32_t size = *(uint32_t *)(list + 8);            // _Mysize
  if (0x3fffffffU - size < count) {
    // basic_string ctor("list<T> too long" @ 0x00a1581c);
    // FUN_00401a40(...);  // Std_LengthError_CtorFromString
    // _CxxThrowException(..., &DAT_00acc388);  // noreturn
  }
  *(uint32_t *)(list + 8) = size + count;
  return;
}
```

## Layout

| Off | Field |
|----:|-------|
| list+0x00 | (unused by this body; allocator / base) |
| list+0x04 | `_Myhead` (unused here; used by callers) |
| list+0x08 | `_Mysize` — **read + write** |

## Callers (evidence sample)

| Caller | Role of this leaf |
|--------|-------------------|
| `FUN_004027f0` (dualed push_front) | `ecx=1`, `edx=list` after `_Buynode` |
| `FUN_00403120` | insert-before-node size++ |
| `FUN_00405e20` | related splice + size++ |
| `FUN_00414a20`, `FUN_00919dc0`, … | multi-domain list hosts (29 sites total) |

## Open questions

- Exact C++ exception type English beyond ThrowInfo `DAT_00acc388` + length_error ctor path.
- Why two image copies of identical `_Incsize` (`00418790` vs `0043fe60`) — compile/link artifact; both are shared leaves.
- Runtime / bit-exact / differential verification.
