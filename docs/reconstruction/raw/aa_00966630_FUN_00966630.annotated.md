# Annotated low-level: FUN_00966630 → Gfx_DeviceStateCache_Clear_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00966630` |
| VA | `0x00966630`–`0x00966674` exclusive (**68 B** / `0x44`) |
| System | gfx / device state cache |
| Date | 2026-08-04 (W38-Y) |
| Canonical | `Gfx_DeviceStateCache_Clear_Inferred` (**Inferred**) |

## Machine-level notes

- Source: raw capture + W38-Y re-verify (`read_memory` full body, no `disassemble_bytes`).
- **EDX = host\*** only. Decompiler `__fastcall(param_1, param_2)` phantom ECX — **unused in body**.
- Zero two global **16-dword** stage tables via `REP STOSD` (`ECX=0x10`, `EAX=0`).
- Zero four scalar globals, then `host[+0,+4,+8,+0x3C]`.
- Sole caller: `GfxDevice_Reset` (`FUN_0075eff0`) @ `0x0075f48f` with `EDX=[DAT_00d1f048]`.
- Twin init: `FUN_00966690` sets `DAT_00d1f048` + floats/flags then **same zero block**.

## Pseudocode (annotated)

```c
// EDX=host; plain RET; void; leaf
void Gfx_DeviceStateCache_Clear_Inferred(uint32_t *host /* EDX */)
{
  // DAT_00d0e128[16] = 0  — per-stage texture/object cache
  // DAT_00d0e0e8[16] = 0  — per-stage companion cache
  DAT_00d1a568 = 0;
  DAT_00d1a564 = 0;
  DAT_00d1a558 = 0;
  DAT_00d1a55c = 0;
  host[0] = 0;       // +0x00 — republished by Reset after clear
  host[1] = 0;       // +0x04
  host[2] = 0;       // +0x08
  host[0xf] = 0;     // +0x3C
}
```

## Consumers of cleared tables

- `FUN_0043f4b0`: stage-index compare/update against both tables before D3D `vtbl+0x190` (SetTexture-class).
- `FUN_0074fba0` / `FUN_0075fb90`: partial re-zero on technique / software-mode flips (same tables; not this unit).

## Open questions

- Product English for host struct / Stats Monitor relationship vs pure state tracker.
- Exact product names of scalar globals `DAT_00d1a564` / `DAT_00d1a568`.
- Runtime / bit-exact.
