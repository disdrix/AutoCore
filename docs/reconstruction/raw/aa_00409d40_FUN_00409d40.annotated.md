# Annotated low-level: FUN_00409d40 → StdVector_ConstructN_Elem0x28_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00409d40` |
| VA | `0x00409d40` |
| Body | `0x00409d40`–`0x00409da2` (**98 B** / `0x62`) |
| System | util / container (`std::vector` construct-N, POD **elem 0x28**) |
| Date | 2026-07-23 scaffold; **2026-08-04 WQ9H-B** dual seal |
| Ghidra | `FUN_00409d40` |
| Named | `StdVector_ConstructN_Elem0x28_Inferred` |

## Machine-level notes

- **ECX** = `const T*` template value (0x28 POD).
- Stack: `T* dest`, `int count`.
- Loop: `FUN_00409f30` (EAX=dst, EDX=value) copies **10 dwords**; then **`add dest, 0x28`**; count--.
- SEH frame `LAB_009bc630`; state 0 during loop.
- Plain **`ret`** (`C3`) — caller pops dest+count (Ufill uses `add esp,0x10` with phantoms).
- **Not** grow, free, insert policy, or domain logic.

## Recovered control flow

```c
// ECX = value*; stack dest, count
void StdVector_ConstructN_Elem0x28(const Pod0x28* value, Pod0x28* dest, int count)
{
  // SEH install
  for (; count != 0; --count) {
    PodCopy10Dwords(dest, value);  // FUN_00409f30
    dest = (Pod0x28*)((char*)dest + 0x28);
  }
  // SEH restore
}
```

## Call graph

| Direction | VA | Role |
|---|---|---|
| Callee | `0x00409f30` | PodCopy 10 dwords / 0x28 |
| Caller | `0x00406e50` | Ufill wrapper (returns dest+n×0x28) |
| Caller | `0x00406220` | push_back capacity-ok construct-1 |
| Twin | `0x00409db0` | ConstructN elem **0xC** (3-dword / +0xC) |

## Open (residual)

- Product demangle / English type of 0x28 element.
- Full dual of PodCopy leaf `00409f30` (CF sealed from decompile+parent).
- Runtime / bit-exact / differential.
