# Annotated low-level: Fs_ListDirBasenames_Inferred (`FUN_0076b3f0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0076b3f0` |
| VA | `0x0076b3f0`–`0x0076b54e` (**351 B**) |
| System | filesystem / directory enumeration |
| Date | 2026-07-23 scaffold; **W33-C seal 2026-07-29** |
| Ghidra | `FUN_0076b3f0` |
| Inferred name | `Fs_ListDirBasenames_Inferred` |

## Machine-level notes

- Source: raw capture + W33-C live re-verify (`decompile` + `read_memory` + xrefs). **No** `disassemble_bytes`.
- **Bytes win:** decompiler shows 1 formal; real ABI is **cdecl (path, out_vec\*)**. Out vector is **ECX thiscall** into `FUN_004301f0` (W33-D owns that unit).
- SEH: `LAB_009acd96`. Epilogue `ADD ESP,0x184; RET`.
- Entries pushed are **basenames only** (`WIN32_FIND_DATAA.cFileName`), not full paths.

## Sealed semantics

```
void Fs_ListDirBasenames_Inferred(const char* path, StringVec* out)  // cdecl
{
  std::string pattern(path);
  if (Fs_PathIsDirectory_Stat_Inferred(path))  // FUN_0076adc0
    pattern += "/*";                           // DAT_00a9d210
  HANDLE h = FindFirstFileA(pattern.c_str(), &fd);
  if (h == INVALID_HANDLE_VALUE) return;       // dtor pattern; SEH unlink
  do {
    if (name != "." && name != "..") {         // REPE CMPSB len 2 / 3
      std::string entry(fd.cFileName);
      FUN_004301f0(out, &entry);               // thiscall push; stride 0x1c
    }
  } while (FindNextFileA(h, &fd));
  FindClose(h);
}
```

## Pseudocode (annotated)

```c
// cdecl; 2 formals (param_2 recovered from bytes/callers — decompiler omits)
// SEH LAB_009acd96; local_184 = basic_string path/pattern (SSO cap@local_16c)
void Fs_ListDirBasenames_Inferred(char *path /*stack*/, void *out_vec /*stack*/)
{
  basic_string pattern(path);
  // SSO: if capacity < 0x10 use interior buffer as c_str
  if (FUN_0076adc0(c_str(pattern))) {
    pattern += "/*";                    // DAT_00a9d210
  }
  HANDLE h = FindFirstFileA(c_str(pattern), &find_data);
  if (h == INVALID_HANDLE_VALUE) {
    ~pattern; return;
  }
  do {
    // exact match "." (2 bytes incl NUL) → skip
    // exact match ".." (3 bytes incl NUL) → skip
    if (not_dot_or_dotdot) {
      basic_string tmp(find_data.cFileName);
      FUN_004301f0(out_vec, &tmp);      // ECX=out_vec (bytes win)
      ~tmp;
    }
  } while (FindNextFileA(h, &find_data));
  FindClose(h);
  ~pattern;
}
```

## Callers / callees

| Role | Symbols |
|---|---|
| Callees | `FUN_0076adc0`, `basic_string` ctor/dtor/`+=`, `FindFirstFileA`, `FindNextFileA`, `FindClose`, `FUN_004301f0` |
| Callers | `FUN_0076ba10`, `FUN_009698a0`, `FUN_009839b0` (W32-D), `FUN_00984340` |

## Open / residual

- Product English / demangle.
- Exact MSVC `basic_string` SSO field layout beyond cap threshold `0x10`.
- `FUN_004301f0` product plate (owned W33-D).
- Runtime / bit-exact / differential open.
