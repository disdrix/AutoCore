# Raw capture: FUN_00440ab0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00440ab0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00440ab0` |
| **Canonical name** | `FUN_00440ab0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00440ab0(void *param_1)

{
  if (*(char *)((int)param_1 + 0x3d) == '\0') {
    FUN_00440ab0(*(undefined4 *)((int)param_1 + 8));
    FUN_00442720();
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return;
}
```

---

## MEGA-130 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-130** |
| **Tools** | Ghidra HTTP `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` / `get_xrefs_to` / `get_function_xrefs` + `get_assembly_context` + `batch_decompile` |
| **Forbidden** | `disassemble_bytes` (not used); Launcher; parent ledgers |
| **Image** | `autoassault.exe` base `0x400000` |
| **Integrity** | Original 2026-07-23 raw body retained above; this section appends sealed bytes/CF |

### Function bounds

| Item | Value |
|---|---|
| Entry | `0x00440ab0` |
| Body | `0x00440ab0`–`0x00440aeb` exclusive (**59 B** / `0x3B`) |
| Inclusive end | last byte of `RET 4` = `0x00440aea` |
| Pad | `CC CC CC CC CC` then `FUN_00440af0` @ `0x00440af0` |
| Ghidra body | `00440ab0 - 00440aeb` |
| Classification | worker (`analyze_function_complete`) |

### Full body hex (`read_memory` length 59)

```text
00440ab0: 53 56 57 8B 7C 24 10 80 7F 3D 00 8B D9 8B F7 75
00440ac0: 25 8B 46 08 50 8B CB E8 E4 FF FF FF 8B 36 8B C7
00440ad0: E8 4B 1C 00 00 57 E8 47 8D 04 00 83 C4 04 80 7E
00440ae0: 3D 00 8B FE 74 DB 5F 5E 5B C2 04 00
```

Concatenated:
`5356578b7c2410807f3d008bd98bf775258b4608508bcbe8e4ffffff8b368bc7e84b1c000057e8478d040083c404807e3d008bfe74db5f5e5bc20400`

### Byte-accurate disassembly (from hex; Ghidra listing truncated after `operator_delete` false-noreturn)

```text
00440ab0  PUSH EBX
00440ab1  PUSH ESI
00440ab2  PUSH EDI
00440ab3  MOV  EDI, dword ptr [ESP+0x10]   ; stack node* (after 3 pushes)
00440ab7  CMP  byte ptr [EDI+0x3d], 0      ; isnil @ +0x3D
00440abb  MOV  EBX, ECX                    ; save tree host (this)
00440abd  MOV  ESI, EDI
00440abf  JNZ  00440ae6                    ; nil sentinel → epilogue
; ---- left-spine free loop (head 00440ac1) ----
00440ac1  MOV  EAX, dword ptr [ESI+0x8]    ; right
00440ac4  PUSH EAX
00440ac5  MOV  ECX, EBX
00440ac7  CALL FUN_00440ab0                ; free right subtree
00440acc  MOV  ESI, dword ptr [ESI]        ; left = node->left
00440ace  MOV  EAX, EDI                    ; current node → EAX for val dtor
00440ad0  CALL FUN_00442720                ; destroy value @ node+0x0C
00440ad5  PUSH EDI
00440ad6  CALL operator_delete             ; 0x00489822
00440adb  ADD  ESP, 4
00440ade  CMP  byte ptr [ESI+0x3d], 0
00440ae2  MOV  EDI, ESI
00440ae4  JZ   00440ac1                    ; left non-nil → continue
00440ae6  POP  EDI
00440ae7  POP  ESI
00440ae8  POP  EBX
00440ae9  RET  4
```

### Live decompile (authoritative text; **CF incomplete**)

```c
void FUN_00440ab0(void *param_1)
{
  if (*(char *)((int)param_1 + 0x3d) == '\0') {
    FUN_00440ab0(*(undefined4 *)((int)param_1 + 8));
    FUN_00442720();
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return;
}
```

**Decompiler gap:** false-noreturn on `operator_delete` collapses the **left-walk loop** (`MOV ESI,[ESI]` / rebind `EDI` / `JZ` back). **Bytes win** for CF. Decompile still correct for: isnil@+0x3d gate, recurse on right@+8, value dtor trampoline, delete node.

### ABI (sealed)

| Slot | Evidence | Conf |
|---|---|---|
| **ECX** = tree host / this | `MOV EBX,ECX`; recursive `MOV ECX,EBX`; call sites `MOV ECX,EDI` (host) | **High** |
| Stack arg = node* | `MOV EDI,[ESP+0x10]` after 3 pushes ≡ `[ESP+4]` at entry | **High** |
| Cleanup | **`RET 4`** (`C2 04 00`) | **High** |
| Return | void (no EAX produce) | **High** |
| Convention | MSVC thiscall-style: ECX + 1 stack arg, callee cleanup | **High** |

Ghidra `Signature: undefined FUN_00440ab0(void)` is **wrong** (omits stack arg / this). `analyze` local `param_1` Stack[0x4]:4 matches stack node.

### Value destroy trampoline (callee evidence, not dualled)

```text
FUN_00442720:
  ADD EAX, 0x0C
  PUSH EAX
  CALL FUN_00440770   ; value destroy (buffer@+0x24, iface@+0x18, base FUN_00440230)
  RET
```

Hex: `83c00c50e847e0ffffc3`

### Callers / xrefs (live)

| From | Function | Kind | Role |
|---|---|---|---|
| `0x00440ac7` | `FUN_00440ab0` | UNCONDITIONAL_CALL | self recurse right |
| `0x004413e0` | `FUN_004413c0` | UNCONDITIONAL_CALL | erase-all full-range: free root then reset head/size |
| `0x00759e79` | `FUN_00759de0` | UNCONDITIONAL_CALL | **gfxUIWindow** complete dtor: free tree shell @ **this+0x04** |
| `0x00442506` | `Catch@004424ff` | UNCONDITIONAL_CALL | EH cleanup free of staged node |

**xref_count = 4** (analyze). External non-self callers: 3.

### Call-site seals (assembly context)

**gfxUIWindow complete @ `0x00759e60`:**
```text
LEA  EDI, [ESI+0x4]          ; shell host = this+0x04  ← ClearShell_At04 role
...
MOV  EAX, [EDI+0x4]          ; head
MOV  ECX, [EAX+0x4]          ; root = head->parent
PUSH ECX
MOV  ECX, EDI
CALL FUN_00440ab0            ; free subtree
; then empty-reset: parent/left/right = head, size = 0
```

**Erase-all `FUN_004413c0` @ `0x004413da`:**
```text
MOV  EAX, [EAX+0x4]          ; root
PUSH EAX
MOV  ECX, EDI                ; tree shell
CALL FUN_00440ab0
; reset head links + size=0
```

### Callees

| Target | Role |
|---|---|
| `FUN_00440ab0` | recursive free right |
| `FUN_00442720` | value dtor trampoline (node → node+0x0C) |
| `operator_delete` @ `0x00489822` | free node storage |

### Peer family (not dualled; evidence only)

| VA | isnil | Value path | Partition role |
|---|---|---|---|
| `0x00440ab0` (OWN) | **+0x3D** | `00442720` → val@+0x0C | ClearShell_At04 |
| `0x00440c90` | +0x1D | `00442730` | ClearShell_At10 |
| `0x00440d20` | +0x11 | none (delete only) | ClearShell_AtA4 |

### Name decision

- Structural: **`StdTree_FreeSubtree_Isnil3D_DestroyVal0C_Inferred`**
- Partition alias: `StdTree_ClearShell_At04` (gfxUIWindow shell @ +0x04)
- Retire scaffold: `Named_CalleeOf_Named_gfxUIWindow_00440ab0`
- Product/PDB English for map T / value type: **open** → `_Inferred`

### CF summary (bytes)

```text
if node.isnil@+0x3D: return
loop:
  FreeSubtree(tree, node.right)
  left = node.left
  DestroyVal0C(node)           ; via 00442720
  operator_delete(node)
  node = left
  if node.isnil@+0x3D: break
RET 4
```
