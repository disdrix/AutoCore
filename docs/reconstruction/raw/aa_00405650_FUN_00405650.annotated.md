# Annotated low-level: StdTree_EraseAndRebalance_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00405650` |
| VA | `0x00405650` |
| Ghidra name | `FUN_00405650` |
| Promoted name | `StdTree_EraseAndRebalance_Isnil21_Inferred` |
| System | skills-abilities (MSVC `std::_Tree` erase isnil@+0x21) |
| Date | 2026-08-05 (R12-009 OWN dual; scaffold 2026-07-23) |

## Machine-level notes

- Source: raw capture + R12-009 live re-verify (`decompile_function`, `analyze_function_complete`, `disassemble_function`, `read_memory`, callers/xrefs, `get_assembly_context`).
- Prefer assembly when decompiler conflicts — **especially epilogue** after `operator_delete` (false noreturn truncates Ghidra body at `0x004058b4`).
- Layout sealed: **color@+0x20**, **isnil@+0x21**, node **0x28** (isnil21 / Val16 family).
- ABI sealed: **`__thiscall`** ECX=map; stack `outIt**`, `node*`; **`ret 8`**.
- Parent dual: `StdTree_Rrotate_Isnil21_Inferred` @ `0x005a27f0` (R11-007).
- Twin Lrotate: `FUN_0050e9f0` (R10 dualed).
- Succ: `FUN_004e12c0` (iterator++ isnil21).
- Min/Max: `FUN_004cb2c0` / `FUN_00421a60`.
- Do **not** merge with isnil29 / isnil2d / isnil31 erase instantiations.

## Stages (assembly-aligned)

1. **SEH** — `LAB_009bc919` / FS:[0].
2. **Nil check** — `CMP byte ptr [node+0x21],0`; throw `"invalid map/set<T> iterator"` @ `0x00a152f0` via `FUN_00401bc0` + `_CxxThrowException` / `DAT_00acc34c`.
3. **Successor capture** — `LEA ECX, localIt; CALL FUN_004e12c0` (in-place iterator++).
4. **Unlink / splice** — replacement child selection; parent/root rewire; head left/right extremity fix via min/max. Two-child successor-swap path at `0x0040574b` (decomp "unreachable" warnings — **present in assembly**).
5. **RB rebalance** if erased color black (`[node+0x20]==1`) — recolor + `FUN_0050e9f0` Lrotate / `FUN_005a27f0` Rrotate.
6. **`operator_delete(node)`** — no value dtor.
7. **Epilogue** — `if (size>0) size--; *outIt=succ; ret 8`.

## Pseudocode (annotated; decompiler CF + byte-sealed epilogue)

```c
// __thiscall  ECX = MapShell* map
// stack:      Node** outIt, Node* node
// ret 8

void StdTree_EraseAndRebalance_Isnil21_Inferred(MapShell *map, Node **outIt, Node *node)
{
  // SEH frame (LAB_009bc919)

  if (node->isnil /* +0x21 */ != 0) {
    // basic_string("invalid map/set<T> iterator") @ 0x00a152f0
    // FUN_00401bc0 → _CxxThrowException(..., DAT_00acc34c)  // noreturn
  }

  Node *local = node;
  FUN_004e12c0(&local);          // iterator++ → successor written back
  // retail keeps succ in stack slot for *outIt epilogue

  // --- unlink (MSVC erase plate; two-child path in assembly @ 0x0040574b) ---
  // repl = left if non-nil else right; if both non-nil successor-swap plate
  // rewire parent/root; fix head->left via min (004cb2c0), head->right via max (00421a60)

  // --- rebalance if erased was black (color @ +0x20) ---
  // while repl != root && repl black: sibling cases → Lrot 0050e9f0 / Rrot 005a27f0
  // finally repl->color = black

  operator_delete(node);         // no value dtor

  // --- epilogue (decompiler omits; sealed by read_memory) ---
  if (map->size > 0)
    map->size--;
  *outIt = succ;                 // from FUN_004e12c0 slot
  return;                        // ret 8
}
```

## Callers

| Caller | Site | Role |
|---|---|---|
| `FUN_00407140` | `0x004071df` | erase-range loop (isnil21) |
| `FUN_00932c80` | `0x00933018` | host map @ `this+0xed8` single erase |

## Open questions

- Product/PDB demangle for map `value_type` / host English name.
- Exact field layout of Val16 payload (+0x0C..+0x1F).
- Runtime / bit-exact / differential (Terminal **false**).
