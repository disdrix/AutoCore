# Annotated low-level: PhysHost_AttachObject_Inferred (`FUN_006291e0`)

| Field | Value |
|---|---|
| Stable ID | `aa_006291e0` |
| VA | `0x006291e0`–`0x00629231` exclusive (**81** B) |
| System | world-object / phys host attach |
| Date | 2026-07-23 scaffold; **W30-I dual 2026-07-29** |
| Ghidra | `FUN_006291e0` |
| Proposed | `PhysHost_AttachObject_Inferred` |

## Machine-level notes

- **ABI:** `__thiscall` ECX = **phys host**; stack `WorldObj*`; void; **`ret 4`**.
- **Vector** at `host+0x3c`: `{data*, size, cap|flag}`; grow via `FUN_005b3370(desc, 4)` **cdecl**.
- **Stamps:** `obj+0x44 = host`; after push `obj+0x58 = size-1` (slot index).
- **Notify chain:** `result = (*(obj+0x3c))->vtbl+0x14()` then `host->vtbl+0x10(result)`.
- Sole caller family: `WorldObj_AttachRegisterHost_Inferred` (`0055e760`) with three host-this sources.
- Prefer machine for vcall this-pointers; decompiler CF is otherwise correct.

## Pseudocode (machine-corrected)

```c
// void __thiscall PhysHost_AttachObject_Inferred(Host* host, WorldObj* obj)
// ret 4
void __thiscall FUN_006291e0(int *host, int obj)
{
  int *vec = (int *)((char *)host + 0x3c); // &host[0xf] as int*

  *(int **)(obj + 0x44) = host;

  if (vec[1] == (vec[2] & 0x7fffffff)) {
    FUN_005b3370(vec, 4); // cdecl grow pointer vector
  }

  *(int *)(vec[0] + vec[1] * 4) = obj;
  vec[1] = vec[1] + 1;
  *(int *)(obj + 0x58) = vec[1] - 1;

  // ECX = *(obj+0x3c)
  int nested = *(int *)(obj + 0x3c);
  int result = (**(int (***)(void))(*(int *)nested + 0x14))(); // thiscall nested

  // ECX = host; push result
  (**(void (***)(int))(*host + 0x10))(result);
}
```

## Open questions

- Product type of 0xa8 host block and English for `obj+0x3c` nested object.
- Product names for nested `vtbl+0x14` and host `vtbl+0x10`.
- Runtime attach order vs bind-links (`0055fa40`).
