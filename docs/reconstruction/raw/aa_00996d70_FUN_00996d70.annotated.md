# Annotated low-level: FUN_00996d70 → ResourceCache_Subobj14_Configure_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00996d70` |
| VA | `0x00996d70`–`0x00996dbb` (**76 B**) |
| System | ResourceCache / embedded configure subobject |
| Date | 2026-08-04 (W37-V dual seal) |
| Canonical | `ResourceCache_Subobj14_Configure_Inferred` (**Inferred**) |

## Machine-level notes

- **ABI (bytes):** **EAX** = subobject; **EBX** = buffer size (implicit; decompiler `in_EAX` only); stack **flag** byte; **`RET 4`**; void.
- **Not** ECX-thiscall — prologue `MOV EDI,EAX`. Decompiler `void FUN_00996d70(undefined1)` under-states registers.
- **Layout on subobject (EDI):**
  - `+0x2C` list shell (clear via `FUN_00415e90`, ESI formal)
  - `+0x30` scratch dword saved across `00449a90`
  - `+0x38` vector-like (thiscall `FUN_00449a90(9)`)
  - `+0x48`, `+0x4C` set to **1**
  - `+0x50` flag byte from stack
  - `+0x00…+0x14` buffer state owned by `FUN_009899a0` / `FUN_00989b00` (aligned free/alloc; align **4**)
- **Parent host (W31-F):** ResourceCache-family object — `+0x10` size-like field, `+0x14` this subobject, `+0x68` CS, `+0x80` lock flag.
- **Callee `FUN_00989b00` bytes:** this=ECX, stack byte → `+0x0C`, EAX=alignment, **EBX=size**, then `_aligned_malloc(size, align)`.

## Pseudocode (annotated)

```c
// EAX = subobj (typically ResourceCache+0x14)
// EBX = size (typically ResourceCache+0x10)
// stack flag → subobj+0x50; RET 4
void ResourceCache_Subobj14_Configure_Inferred(
    void *subobj /*EAX*/,
    uint32_t size /*EBX*/,
    uint8_t flag /*stack*/)
{
  // ESI = subobj+0x2C
  ClearListShell_ESI();           // FUN_00415e90
  // ECX = subobj+0x38; push 9
  VectorResetOrFill_Thiscall_9(); // FUN_00449a90
  *(uint32_t *)((char *)subobj + 0x48) = 1;
  *(uint32_t *)((char *)subobj + 0x4c) = 1;
  FreeAlignedBufferState_EDI();   // FUN_009899a0
  *(uint8_t *)((char *)subobj + 0x50) = flag;
  // ECX=subobj, EAX=4, EBX=size, push flag-as-1-or-stack
  ReallocAlignedBuffer_Thiscall(/*align*/ 4, /*size*/ size, /*byte*/ 1);
  // FUN_00989b00 — retail always pushes 1 here (ESI=1), not the stack flag copy
}
```

**Note:** store of stack flag at `+0x50` uses the real stack arg; the subsequent `PUSH ESI` into `00989b00` pushes the constant **1** set earlier (`MOV ESI,1`), not a re-read of `+0x50`. Decompiler shows `FUN_00989b00(1)` correctly for that slot.

## Callers (context)

| Caller | Setup |
|---|---|
| `ResourceCache_SetField10AndConfigure_Inferred` (`0x00989ef0`) | optional CS; `host+0x10 = ECX`; `EAX = host+0x14`; EBX holds field10; stack flag |
| `FUN_00989d50` | full host reset; `EDI = host+0x14`; `EBX = [host+0x10]`; `FUN_00996d70(0)` |

## Open questions

- Product English for subobject class / `+0x48`/`+0x4C` counters / flag `+0x50`.
- Full contracts of unowned callees (`00415e90`, `00449a90`, `009899a0`, `00989b00`).
- Why `00989d50` pre-clears then re-enters this unit (redundant retail).
- Runtime / bit-exact / differential (no Launcher).
