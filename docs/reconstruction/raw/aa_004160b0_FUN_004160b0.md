# Raw capture: FUN_004160b0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004160b0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004160b0` |
| **Canonical name** | `FUN_004160b0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_004160b0(void *param_1,byte param_2)

{
  FUN_0097a8a0();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```

---

## WQ9I-E live re-verify (2026-08-04) — append only

**Tools:** Ghidra `batch_decompile` + `analyze_function_complete` + `read_memory` + `disassemble_function` + xrefs. **No** `disassemble_bytes`.

### Live decompile ≡ raw body

Yes — complete dtor + flags&1 delete + return this.

### Body / ABI (sealed)

| Fact | Evidence |
|------|----------|
| Body | `0x004160b0`–`0x004160cd` inclusive (**30 B** / `0x1E`); `CC` pad after |
| Entry | `56 8B F1` (`PUSH ESI; MOV ESI,ECX`) |
| Exit | `8B C6 5E C2 04 00` (`MOV EAX,ESI; POP ESI; RET 4`) |
| ABI | **thiscall** ECX=this; stack `flags`; **RET 4**; returns this* |
| Complete dtor | `CALL 0x0097a8a0` (installs `PTR_FUN_00a6f61c`, chains `FUN_00792c20`) |
| Free | if `(flags & 1)`: `PUSH ESI; CALL operator_delete` (+ `ADD ESP,4`) |
| Xrefs | 1 DATA — vtbl `PTR_FUN_00a6f61c[0]` @ `0x00a6f61c` = `0x004160b0` (read_memory LE dword) |
| Callers | none by CALL (vtbl dispatch only) |

### Related (not OWN)

- Complete dtor `FUN_0097a8a0` (sole caller of this body)
- Ctor twin `FUN_0097a8e0` installs same vtbl; UI string `i_w_tut_2d_wnd_highlight.dds`
- Neighbor scalar `FUN_004160d0` (WQ9I-F) — different complete dtor `FUN_00834520`

### Name

`Class_00a6f61c_ScalarDeletingDtor` (Inferred structural — vtbl stamp; product English open).
