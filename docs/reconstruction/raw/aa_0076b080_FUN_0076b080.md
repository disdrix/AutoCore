# Raw capture: FUN_0076b080

| Field | Value |
|---|---|
| **Stable ID** | `aa_0076b080` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0076b080` |
| **Canonical name** | `FUN_0076b080` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
bool FUN_0076b080(char param_1)

{
  int iVar1;
  char *unaff_ESI;
  uint local_1e;
  
  if ((((param_1 != '\0') && (iVar1 = stat(), iVar1 == 0)) && ((local_1e >> 0xf & 1) != 0)) &&
     ((iVar1 = stat(), iVar1 == 0 && ((~((byte)local_1e >> 7) & 1) != 0)))) {
    _chmod(unaff_ESI,0x180);
  }
  iVar1 = _unlink(unaff_ESI);
  return iVar1 == 0;
}
```
---

## W37-S re-verify (2026-08-04) — OWN-ONLY dual

**Tools:** `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + `get_function_by_address` + callers/xrefs + peer caller decompile (`FUN_0076ba10`, `FUN_0076bc00`, `FUN_007b75b0`). **No** `disassemble_bytes`. **No** Launcher.

### Body / ABI (bytes win)

| Item | Evidence |
|------|----------|
| Range | `0x0076b080`–`0x0076b0f1` inclusive (**114 B** / `0x72`); pad `CC` then next |
| Ghidra body | `get_function_by_address`: Entry `0076b080` Body `0076b080 - 0076b0f1` |
| Frame | `SUB ESP,0x24` |
| Force formal | `CMP BYTE [esp+0x28],0` after frame (= stack arg at entry `[esp+4]`) |
| Path formal | **ESI** register (not stack); all `stat`/`_chmod`/`_unlink` take ESI |
| Epilogue | `ADD ESP,0x28; RET` (`83 c4 28 c3`) — **cdecl** 1 stack arg |
| Return | bool: `NEG EAX; SBB EAX,EAX; ADD EAX,1` after `_unlink` → `unlink==0` |

### Full body hex (114 B)

```
83ec24807c2428007456578b3d04659c008d4424045056ffd783c40885c0753f8b4c240ac1e90f80e101884c242c742f8d5424045256ffd783c40885c075208a44240ac0e807f6d024018844242c740f688001000056ff1568659c0083c4085f56ff15a0649c00f7d81bc083c00183c428c3
```

### Sealed CF

```
if force != 0:
  if stat(ESI,&sb)==0 and (st_mode>>15)&1:          // S_IFREG
    if stat(ESI,&sb)==0 and (~(st_mode>>7))&1:     // not S_IWRITE
      _chmod(ESI, 0x180)                            // S_IREAD|S_IWRITE
return _unlink(ESI) == 0
```

### Callers (3 xrefs)

| Caller | Site | Notes |
|--------|------|-------|
| `FUN_0076ba10` | `0x0076bb7d` | `LEA ESI,[path]; PUSH force; CALL; ADD ESP,4` — recursive dir delete file arm (W34-A) |
| `FUN_0076bc00` | `0x0076bc5f` | `PUSH 1; CALL; ADD ESP,4` with ESI=path — ensure-dir helper |
| `FUN_007b75b0` | `0x007b765d` | `PUSH 1; CALL; ADD ESP,4` — NDAssetManager precompile dir prep |

### Classification

**worker** — filesystem force-unlink single path (ESI).

### Dual name

`Fs_UnlinkFile_ESI_ForceChmod_Inferred` (Ghidra `FUN_0076b080`; **Inferred**). **Reject** scaffold `Named_CalleeOf_Named_assManager_*` as product plate.

### Live decompile ≡ scaffold raw CF

Yes for force/stat/chmod/unlink/bool return; ESI path sealed by call sites + body.
