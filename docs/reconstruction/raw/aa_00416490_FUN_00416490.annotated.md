# Annotated low-level: StdString_OperatorPlus_Cstr (FUN_00416490)

| Field | Value |
|---|---|
| Stable ID | `aa_00416490` |
| VA | `0x00416490`–`0x00416509` (**122 B** / `0x7A`) |
| System | MSVC free `operator+(basic_string const&, char const*)` |
| Date | 2026-07-29 (W33-F re-verify) |
| Ghidra | `FUN_00416490` |
| Name | `StdString_OperatorPlus_Cstr` (**Inferred** structural) |

## Machine-level notes

- **cdecl** free function; not a member. Stack formals at `Stack[0x4/0x8/0xc]` after SEH install.
- SEH frame: `push -1; push LAB_009bc093; push fs:[0]; mov fs:[0],esp; sub esp,0x20`.
- `ESI` holds `out*` across copy-out; return `EAX = ESI`.
- Epilogue: restore `fs:[0]`, `add esp,0x2c`, **`ret`** (no stack-arg cleanup — caller cleans 12 B).
- IAT calls only (no direct `E8` callees inside body).
- Twin of `aa_00416410` — single differing word is `+=` IAT address.

## Stack map (after prologue / with ESI pushed)

| Slot | Role |
|---|---|
| `out*` | `param_1` — return-value object (hidden first) |
| `lhs*` | `param_2` — left-hand `basic_string` |
| `rhs` | `param_3` — `char const*` |
| local `0x1c` | temp `basic_string` (MSVC layout) |
| EH state | `0` → `1` while temp live → low byte 0 before dtor |

## Pseudocode (annotated)

```c
// cdecl; returns out*
MsvcBasicString* StdString_OperatorPlus_Cstr(
    MsvcBasicString* out,           // param_1  Stack[0x4]
    const MsvcBasicString* lhs,     // param_2  Stack[0x8]
    const char* rhs)                // param_3  Stack[0xc]
{
  // SEH: LAB_009bc093, state = -1
  MsvcBasicString tmp;              // local_28 / 0x1c bytes
  // state = 0 around construction
  basic_string_copy_ctor(&tmp, lhs);          // IAT [0x009c62ec]
  // state = 1
  basic_string_op_plus_eq_cstr(&tmp, rhs);    // IAT [0x009c62b0]  <-- cstr overload
  basic_string_copy_ctor(out, &tmp);          // IAT [0x009c62ec]
  // state low-byte = 0
  basic_string_dtor(&tmp);                    // IAT [0x009c62f4]
  return out;
}
```

## Open questions (gaps)

- Product/MSVC fully demangled export name (structural seal only).
- Exact CRT IAT symbol names for the three slots (targets sealed by pointer).
- Runtime / bit-exact / differential verification.
