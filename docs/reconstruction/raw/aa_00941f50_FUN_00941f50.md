# Raw capture: FUN_00941f50

| Field | Value |
|---|---|
| **Stable ID** | `aa_00941f50` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00941f50` |
| **Canonical name** | `Client_UseInventoryItemByCoid` |
| **System** | inventory-transfer / skills-abilities |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
uint FUN_00941f50(uint param_1,uint param_2,undefined4 param_3)

{
  int in_EAX;
  uint uVar1;
  
  if ((*(int *)(in_EAX + 0xe98) != 0) &&
     (uVar1 = *(uint *)(*(int *)(in_EAX + 0xe98) + 0x250), uVar1 != 0)) {
    if ((param_1 & param_2) != 0xffffffff) {
      FUN_00571010(param_1,param_2);
      uVar1 = FUN_00941d50(param_3);
      return uVar1;
    }
    return uVar1 & 0xffffff00;
  }
  return 0;
}
```

---

## WQ8R-I append (2026-08-04) — live re-decompile + full image seal

**Tools:** `batch_decompile`, `read_memory` (full body), `analyze_function_complete`, `get_assembly_context` on sole xref. **No** `disassemble_bytes`.

### Body / ABI (image) — **bytes supersede decompiler**

| Fact | Value |
|---|---|
| Body | `0x00941f50`–`0x00941fa0` (pad `CC` after second `ret 0x0C`) |
| Convention | **EAX = Client***; stack `coidLo`, `coidHi`, `mode` |
| Epilogue | **`ret 0x0C`** (`C2 0C 00`) all exits |
| Return | **AL** bool: `Client_UseInventoryItem_Inferred` result, or **0** |

### Full body hex (80 B useful + pad)

```
56 8B F0 8B 86 98 0E 00 00 85 C0 74 0A 8B 80 50 02 00 00 85 C0 75 06
32 C0 5E C2 0C 00
8B 54 24 08 57 8B 7C 24 10 8B CA 23 CF 83 F9 FF 74 1E
8B 88 B0 02 00 00 57 52 E8 84 F0 C2 FF
8B 54 24 14 52 8B F8 E8 B8 FD FF FF
5F 5E C2 0C 00
5F 32 C0 5E C2 0C 00
CC...
```

### Control flow (byte-accurate)

```
ESI = EAX (client)
char* = *(client + 0xE98);  if !char → AL=0; ret 0xC
host  = *(char + 0x250);    if !host → AL=0; ret 0xC
if (coidLo & coidHi) == 0xFFFFFFFF → AL=0; ret 0xC
ECX = *(host + 0x2B0)          // InventoryGrid* cargo (type-1 path)
item = InventoryGrid_FindItemByCoid(ECX, coidLo, coidHi)  // 0x00571010
EDI = item;  push mode;  // ESI still client
return Client_UseInventoryItem_Inferred(mode)  // 0x00941d50
```

### Decompiler corrections

| Decompiler | Bytes |
|---|---|
| `return uVar1 & 0xffffff00` on invalid COID | **`xor al,al`** → return **0** |
| Hidden Find thiscall | **ECX = *(host+0x2B0)** cargo grid |
| `in_EAX` phantom | **EAX = client** at entry (`mov esi,eax`) |
| No null-check on Find result | **True** — EDI may be NULL if COID missing (gap for 00941d50) |

### Sole static CALL site

| Site | Notes |
|---|---|
| `0x0086152C` | Orphan region after `FUN_00861280` (Ghidra has **no** function containing site). |
| Prep | Gate `cloneMeta+0x3F2` bit0x80 (skill usable); push `*( *(ui+0x56c)+4 )` as mode; COID from item `ESI+0x160/164`; **EAX = `0x00D1A840`** client singleton |
| After | `test al,al`; on success `FUN_007fbb70` UI closeout then vtbl `+0x33C` |

### Callers / callees

| Dir | VA | Name |
|---|---|---|
| Xref CALL | `0x0086152C` | orphan UI handler (~`0x008612A8`+) |
| Callee | `0x00571010` | `InventoryGrid_FindItemByCoid` |
| Callee | `0x00941d50` | `Client_UseInventoryItem_Inferred` (0x2045 / equip arms) |
