# Annotated low-level: FUN_0068a620 → StdVector_InsertOne_RebindIt_Elem28

| Field | Value |
|---|---|
| Stable ID | `aa_0068a620` |
| VA | `0x0068a620`–`0x0068a68a` exclusive (**106 B**) |
| System | MSVC `vector` insert-one + rebind, POD elem **0x1c** |
| Date | 2026-08-04 (W37-AC re-verify) |
| Canonical name | `StdVector_InsertOne_RebindIt_Elem28` (**Inferred**) |

## Machine-level notes

- **ABI:** `__thiscall` ECX=`vector*`; stack `outIt*`, `where*`, `value*`; **`RET 0x0C`**; void.
- **Vector triad:** begin@`+4`, end@`+8`, capEnd@`+0xC` (cap not read here).
- **Stride:** `0x1c` — signed divide magic `0x92492493` + `IMUL ESI,0x1C`.
- **Callee:** `FUN_0068a1d0` (`StdVector_InsertN_Elem28`, W34-S) with **count=`1`**.
- **Rebind:** after insert (which may reallocate), `*outIt = begin' + index*0x1c` so the out iterator points at the inserted slot.
- **Empty rule:** if `begin==0` or size==0 → index 0 (insert at front / sole element).
- Bytes win over decompiler for value formal type: **pointer to 0x1c POD**, not a lone dword.

## Pseudocode (byte-refined)

```c
// ECX = VectorElem28*; RET 0x0C
void __thiscall StdVector_InsertOne_RebindIt_Elem28(
    VectorElem28* vec,
    Elem28** outIt,
    Elem28* where,
    const Elem28* value)
{
    Elem28* begin = vec->begin;
    uint32_t index = 0;
    if (begin != 0) {
        uint32_t size = (uint32_t)((char*)vec->end - (char*)begin) / 0x1c;
        if (size != 0)
            index = (uint32_t)((char*)where - (char*)begin) / 0x1c;
    }
    StdVector_InsertN_Elem28(vec, where, /*count*/1, value);
    *outIt = (Elem28*)((char*)vec->begin + index * 0x1c);
}
```

## Scaffold decompile (preserved)

```c
void __thiscall FUN_0068a620(int param_1,int *param_2,int param_3,undefined4 param_4)
{
  int iVar1;
  iVar1 = *(int *)(param_1 + 4);
  if (iVar1 != 0) {
    if ((*(int *)(param_1 + 8) - iVar1) / 0x1c != 0) {
      iVar1 = (param_3 - iVar1) / 0x1c;
      goto LAB_0068a669;
    }
  }
  iVar1 = 0;
LAB_0068a669:
  FUN_0068a1d0(param_3,1,param_4);
  *param_2 = iVar1 * 0x1c + *(int *)(param_1 + 4);
  return;
}
```

## Callers / callees

| Role | VA / name |
|---|---|
| Caller | `FUN_0068ae50` @ `0x0068aecd` — push_back slow path when size==cap |
| Callee | `FUN_0068a1d0` / `StdVector_InsertN_Elem28` |

## Open questions

- Product English for POD `T` (0x1c / 7 dwords).
- Whether any non-push_back call sites exist beyond `FUN_0068ae50` (xrefs: 1).
- Runtime / bit-exact / differential.
