# Annotated low-level: UI_MissionJournal_SelectMission_Inferred (FUN_00829b20)

| Field | Value |
|---|---|
| Stable ID | `aa_00829b20` |
| VA | `0x00829b20` |
| Body | `0x00829b20`–`0x00829ca5` (0x185) |
| System | client UI / missions-progression / mission journal |
| Date | 2026-07-29 (W24-J dual seal) |
| Tools | `decompile_function` + `read_memory` (no `disassemble_bytes`) |

## Machine-level notes

- Journal UI context in **EAX** → ESI; selected **mission*** in **EDI** (caller register, not stack).
- Gate virtual `vtbl+0x3d8` must return non-zero; mission must be non-null.
- Title format string `"[%d] %s"` @ `0x00a4abb4` (confirmed bytes).
- Mission level is **signed word** @ mission+0x11c; name via +0x14c through `FUN_007a69d0`/`FUN_007a6de0`.
- Always stores `*mission` → context+0x500 when gates pass.
- `CVOGCharacter_IsMissionJournalReady` framed with ECX=`DAT_00d1b6d8`, stack=EDI; sealed sibling dual.
- Ready AL **not branched** here; `FUN_00829ac0` always follows when character non-null.

## Annotated control flow

```
UI_MissionJournal_SelectMission_Inferred(ctx=EAX, mission=EDI)
  if !ctx->vtbl+0x3d8() or mission==null: return

  if ctx->pTitleWidget:  // +0x508
      name = resolve(mission+0x14c) via 007a69d0/007a6de0
      level = (int16)(mission+0x11c)
      sprintf(buf, "[%d] %s", level, name)
      title.setText(buf); show; invalidate
      if g_localChar: char_root.vtbl+0x27c()
      FUN_0092d580(...)  // color prep vs level
      build RGBA via 3x FUN_006a3db0 + alpha from title color
      title.setColor(1, &rgba)

  if g_localChar:
      CVOGCharacter_IsMissionJournalReady(g_localChar, mission)
      FUN_00829ac0()  // journal objective / list helper

  if ctx->pChromeWidget && ctx->pTitleWidget:
      copy color channel title -> chrome (vtbl+0x154)

  ctx->selectedMissionId = *mission  // +0x500
```

## Offset map

### Journal context (this)

| Offset | Role |
|---:|---|
| `+0x500` | selectedMissionId (out) |
| `+0x508` | title label widget* |
| `+0x50C` | secondary chrome widget* |

### Mission object (EDI)

| Offset | Role |
|---:|---|
| `+0x000` | mission id (dword) |
| `+0x11C` | level / rank (int16) |
| `+0x14C` | name source pointer |

## rel32 / sealed callees

| Site / callee | Target | Name / role |
|---|---:|---|
| `00829c5c` | `0052b3b0` | `CVOGCharacter_IsMissionJournalReady` |
| `00829c61` | `00829ac0` | journal list refresh helper |
| `007a69d0` / `007a6de0` | string resolve | name for sprintf |
| `0092d580` | color prep | residual |
| `006a3db0` ×3 | float→u8 | residual |
| sprintf IAT | format | `"[%d] %s"` |

## Open questions

1. Product English method name on journal panel class.
2. Exact product field names for mission +0x11c / +0x14c.
3. Whether `FUN_00829ac0` consumes ready AL (out of ownership).
4. Full color formula for `FUN_0092d580` / `FUN_006a3db0`.
5. Runtime / bit-exact / differential.
