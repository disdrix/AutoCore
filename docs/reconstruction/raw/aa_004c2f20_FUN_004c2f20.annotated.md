# Annotated low-level: Skill_ApplyBoundStatusMotionIfPresent_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004c2f20` |
| VA | `0x004c2f20` |
| Ghidra symbol | `FUN_004c2f20` |
| Canonical name | `Skill_ApplyBoundStatusMotionIfPresent_Inferred` |
| System | `skills-abilities` / status-effect motion side path |
| Date | 2026-07-29 (W21-H dual seal) |

## Machine-level notes

- **Body is 18 bytes** + `CC` pad. Leaf-style **gate + optional tail**.
- Decompiler shows `__fastcall` + zero-arg `FUN_005d0d60()` — **wrong vs bytes**. Prefer assembly:
  - Load `ECX = *(ECX + 0x1FC)`.
  - If zero → `RET 4`.
  - Else **JMP** `FUN_005d0d60` (stack dword = skill id forwarded as that callee’s `param_2`).
- Sole live caller: `Skill_ApplyEffectsOnTarget_Inferred` after caster **vtbl+0x214** returns non-null and is re-invoked with **skillId** (`skill+0x5FC`).
- Role: optional **bound-subobject status/motion apply** for the skill id when the caster exposes a vtbl+0x214 binding object that carries a non-null `+0x1FC` controller.

## Pseudocode (annotated ≡ raw CF + machine ABI)

```c
// Ghidra raw keeps FUN_* names; ABI corrected from read_memory.
void __thiscall Skill_ApplyBoundStatusMotionIfPresent_Inferred(
    void *boundObj,   /* ECX — result of caster vtbl+0x214(skillId) */
    int skillId)      /* [esp+4]; cleaned by RET 4 or by tail callee */
{
  void *motionCtl = *(void **)((char *)boundObj + 0x1fc);
  if (motionCtl != NULL) {
    // machine: JMP FUN_005d0d60 — not a CALL; no local frame
    FUN_005d0d60(motionCtl, skillId);
    return;
  }
  return;
}
```

## Offsets

| Off | Width | Role |
|---:|---|---|
| boundObj+0x1FC | ptr | Gate + `this` for `FUN_005d0d60` when non-null |

## Open questions

- Product English for the vtbl+0x214 binding object and `+0x1FC` controller (HB / status-motion host).
- Full semantics of `FUN_005d0d60` (sibling-owned residual).
- Whether every caster type’s vtbl+0x214 is cdecl vs thiscall for the shared skillId stack dword (call-site has one push before both vcall and this unit).
