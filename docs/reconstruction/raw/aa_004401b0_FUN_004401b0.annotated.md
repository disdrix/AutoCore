# Annotated low-level: FUN_004401b0 → gfxUIWindow_ScalarDeletingDtor

| Field | Value |
|---|---|
| Stable ID | `aa_004401b0` |
| VA | `0x004401b0`–`0x004401cd` inclusive (**30 B** / `0x1E`) |
| System | UI / graphics — gfxUIWindow MSVC scalar-deleting destructor |
| Date | 2026-08-05 (MEGA-129 re-verify) |
| Canonical | `gfxUIWindow_ScalarDeletingDtor` |
| Ghidra | `FUN_004401b0` |

## Machine-level notes

- Source: raw capture + MEGA-129 live Ghidra re-verify (decompile ≡ disasm ≡ `read_memory` hex).
- **ABI:** `__thiscall`; ECX = `this`; stack `flags`; **`RET 4`**; returns `this` in EAX.
- **Role:** MSVC **scalar-deleting destructor** bound as **vtbl[0]** of `PTR_FUN_00a9f304`.
- Always runs complete dtor `FUN_00759de0` / `gfxUIWindow_CompleteDtor` (no free of `this` there).
- If `(flags & 1)`: host free via `operator_delete` @ `0x00489822`.
- Ghidra marks `operator_delete` as noreturn; **false** — epilogue returns `this` after the free path.
- Not a vector-deleting dtor (no array count; no `operator_delete[]`; single bit0 gate).
- Product class sealed by RTTI **`.?AVgfxUIWindow@@`** (same COL chain as dualed complete peer).

## Pseudocode (annotated)

```c
// ECX = this; stack flags; RET 4; returns this*
void * __thiscall gfxUIWindow_ScalarDeletingDtor(void *self, byte flags)
{
  gfxUIWindow_CompleteDtor(self);   // FUN_00759de0 — thiscall, no free this
  if ((flags & 1) != 0) {
    operator_delete(self);          // 0x00489822; ADD ESP,4
  }
  return self;                      // MOV EAX,ESI; POP ESI; RET 4
}
```

## Call graph

```
DATA PTR_FUN_00a9f304[0] @ 0x00a9f304
  └─ FUN_004401b0  gfxUIWindow_ScalarDeletingDtor   [OWN MEGA-129]
       ├─ FUN_00759de0  gfxUIWindow_CompleteDtor    [dualed WQ9L-F]
       └─ operator_delete @ 0x00489822              (flags&1 only)
```

## Vtbl / RTTI

| Item | Value |
|---|---|
| Vtbl | `PTR_FUN_00a9f304` |
| Slot | `[0]` scalar deleting |
| COL ptr | `0x00a9f300` → `0x00ab96a4` |
| type_info | `0x00afe190` → `.?AVgfxUIWindow@@` |

## Open questions / gaps

- Complete dtor member/slot English not owned here (already dualed **accept-with-gaps** at `aa_00759de0`).
- Runtime / bit-exact / differential — open (no Launcher).
