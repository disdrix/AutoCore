# Raw capture: FUN_0040b890

| Field | Value |
|---|---|
| **Stable ID** | `aa_0040b890` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0040b890` |
| **Canonical name** | `FUN_0040b890` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 * FUN_0040b890(undefined4 *param_1)

{
  undefined4 *puVar1;
  int in_EAX;
  undefined4 *puVar2;
  int *unaff_EBX;
  
  puVar1 = param_1;
  puVar2 = *(undefined4 **)(*(int *)(in_EAX + 4) + 4);
  param_1 = (undefined4 *)CONCAT31(param_1._1_3_,1);
  if (*(char *)((int)puVar2 + 0x19) == '\0') {
    do {
      param_1 = (undefined4 *)CONCAT31(param_1._1_3_,*unaff_EBX < (int)puVar2[3]);
      if (*unaff_EBX < (int)puVar2[3]) {
        puVar2 = (undefined4 *)*puVar2;
      }
      else {
        puVar2 = (undefined4 *)puVar2[2];
      }
    } while (*(char *)((int)puVar2 + 0x19) == '\0');
  }
  puVar2 = (undefined4 *)FUN_0040bcd0(&param_1,param_1,unaff_EBX);
  *puVar1 = *puVar2;
  *(undefined1 *)(puVar1 + 1) = 1;
  return puVar1;
}
```

---

## R13-019 re-verify (2026-08-05) — append only

**Agent:** OWN-ONLY dual R13-019. **Tools:** `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_by_address` + `get_function_callers` / `get_function_xrefs` / `get_xrefs_to` + `get_assembly_context` + `read_memory`. **No** `disassemble_bytes`. **No** Launcher.

### Live decompile (2026-08-05)

Identical CF to 2026-07-23 raw (phantom `in_EAX` / `unaff_EBX`; always-insert via `FUN_0040bcd0`; out bool forced 1).

### Body / epilogue (bytes)

| Item | Value |
|---|---|
| Entry | `0x0040b890` |
| Last insn | `RET 4` @ `0x0040b8eb` (`C2 04 00`) |
| Inclusive end | `0x0040b8eb` |
| Size | **92 B** / `0x5C` |
| Pad | `CC CC` @ `0x0040b8ec`–`0x0040b8ed` |
| Next | `FUN_0040b8f0` @ `0x0040b8f0` |
| Ghidra body listing | `0040b890`–`0040b8ed` (includes pad) |

`read_memory` @ `0x0040b890` length 100 (hex prefix):

```
55 8b 6c 24 08 57 8b f8 8b 4f 04 8b 41 04 80 78 19 00 c6 44 24 0c 01 75 25 56 8b 33 ...
... 8b 00 89 45 00 5f c6 45 04 01 8b c5 5d c2 04 00 cc cc
```

### Assembly (authority for ABI / CF)

```text
0040b890  PUSH EBP
0040b891  MOV  EBP, [ESP+0x8]        ; out pair*
0040b895  PUSH EDI
0040b896  MOV  EDI, EAX              ; map shell (EAX)
0040b898  MOV  ECX, [EDI+0x4]        ; head
0040b89b  MOV  EAX, [ECX+0x4]        ; root = head->parent
0040b89e  CMP  BYTE PTR [EAX+0x19], 0 ; isnil@+0x19
0040b8a2  MOV  BYTE PTR [ESP+0xC], 1  ; goLeft = true
0040b8a7  JNZ  0040b8ce               ; empty tree → insert at head
0040b8a9  PUSH ESI
0040b8aa  MOV  ESI, [EBX]             ; key = *(int*)value
0040b8ac  LEA  ESP, [ESP+0]           ; align/nop
0040b8b0  CMP  ESI, [EAX+0xC]         ; signed key vs node+0x0C
0040b8b3  MOV  ECX, EAX               ; parent = cur (where-node)
0040b8b5  SETL DL                     ; signed less
0040b8b8  TEST DL, DL
0040b8ba  MOV  [ESP+0x10], DL         ; goLeft = (key < node.key)
0040b8be  JZ   0040b8c4
0040b8c0  MOV  EAX, [EAX]             ; left
0040b8c2  JMP  0040b8c7
0040b8c4  MOV  EAX, [EAX+0x8]         ; right
0040b8c7  CMP  BYTE PTR [EAX+0x19], 0
0040b8cb  JZ   0040b8b0               ; while !isnil
0040b8cd  POP  ESI
0040b8ce  MOV  EAX, [ESP+0xC]         ; goLeft
0040b8d2  PUSH EBX                    ; value*
0040b8d3  PUSH EAX                    ; goLeft
0040b8d4  LEA  EDX, [ESP+0x14]
0040b8d8  PUSH EDX                    ; temp out for insert helper
0040b8d9  CALL 0040bcd0               ; EDI=map, ECX=where parent
0040b8de  MOV  EAX, [EAX]             ; *temp.it
0040b8e0  MOV  [EBP], EAX             ; out.it
0040b8e3  POP  EDI
0040b8e4  MOV  BYTE PTR [EBP+0x4], 1  ; out.inserted = ALWAYS 1
0040b8e8  MOV  EAX, EBP
0040b8ea  POP  EBP
0040b8eb  RET  4
```

### ABI (sealed)

| Slot | Role |
|---|---|
| **EAX** | map shell* (`MOV EDI,EAX`; head@+4, size@+8) |
| **EBX** | value* (`key = *(int32*)EBX`) |
| stack | out `{ Node* it; u8 inserted@+4 }` |
| return | **EAX** = out |
| cleanup | **`RET 4`** (`C2 04 00`) |
| ECX at CALL | where-parent (last non-nil / head if empty) — consumed by `FUN_0040bcd0` |
| EDI at CALL | map shell — consumed by `FUN_0040bcd0` |

### Node family

| Field | Offset |
|---|---|
| left / parent / right | +0 / +4 / +8 |
| key (signed int) | **+0x0C** |
| mapped payload | +0x10 … (8 B before color; node buy `operator_new(0x1C)`) |
| color | **+0x18** (callee `FUN_0040bcd0`) |
| isnil | **+0x19** |
| node size | **0x1C** (`FUN_0040c060`) |

### Semantics

**Always-insert** (no equal-key early-out / no `inserted=0` path). Signed lower/upper walk with goLeft flag, then `FUN_0040bcd0` buy+link+RB. Out pair always `{newNode, true}`.

### Callers / xrefs

| Kind | Value |
|---|---|
| Callers | **1** — `FUN_0092b2a0` (`MissionTracker_FillTrackedTargetSlots_Inferred`) |
| Xref | `0x0092b4fc` UNCONDITIONAL_CALL only |
| Callee | `FUN_0040bcd0` only |

### Call site (`0x0092b4fc`) — assembly context + bytes

```text
; ranking temp-map arm inside FUN_0092b2a0
MOVZX EAX, word ptr [EDX+0x14]   ; key source (u16→dword)
MOVSS XMM0, [ESP+0x20]           ; float payload
MOV   [ESP+0x44], EAX            ; value.key
LEA   EAX, [ESP+0x28]            ; out pair
MOVSS [ESP+0x34], XMM0
MOV   EDX, [ESP+0x34]
MOV   ECX, ESI
PUSH  EAX                        ; out
LEA   EBX, [ESP+0x48]            ; → value* (key @ prior [ESP+0x44])
LEA   EAX, [ESP+0x3c]            ; → map shell on stack
MOV   [ESP+0x4c], ECX            ; value+4
MOV   [ESP+0x50], EDX            ; value+8 (float bits)
CALL  0x0040b890
MOV   EBX, [EBP+0x8]             ; restore host
MOV   EDX, [ESP+0x14]
CMP   BYTE PTR [EDX+0x19], 0     ; post-insert isnil guard on related node
```

### Naming (inferred)

`StdTree_Insert_IntKey_Isnil19_EaxEbx_Inferred` — structural plate only; product map T open. Partition system **inventory-transfer** is residual queue assignment (parent dual under that wave); unit itself is generic MSVC tree insert, not inventory item move.

### Completeness (analyze_function_complete)

classification worker; xref_count 1; callees `FUN_0040bcd0`; callers `FUN_0092b2a0`; phantom `in_EAX`; return_type unresolved (decompiler noise — bytes return out in EAX).
