# Raw capture: FUN_0045f150

| Field | Value |
|---|---|
| **Stable ID** | `aa_0045f150` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0045f150` |
| **Canonical name** | `FUN_0045f150` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0045f150(undefined4 *param_1,undefined4 *param_2)

{
  int in_EAX;
  undefined4 *puVar1;
  
  if (in_EAX != 0) {
    puVar1 = param_2 + 5;
    do {
      if (param_2 != (undefined4 *)0x0) {
        *param_2 = *param_1;
        puVar1[-4] = param_1[1];
        puVar1[-3] = param_1[2];
        puVar1[-2] = param_1[3];
        puVar1[-1] = param_1[4];
        *puVar1 = param_1[5];
        puVar1[1] = param_1[6];
      }
      param_2 = param_2 + 7;
      puVar1 = puVar1 + 7;
      in_EAX = in_EAX + -1;
    } while (in_EAX != 0);
  }
  return;
}
```

---

## W35-L re-verify append (2026-07-29)

| Field | Value |
|---|---|
| **Tool** | Ghidra `batch_decompile` + `read_memory` + function xrefs. **No** `disassemble_bytes` |
| **Integrity** | Scaffold raw body above preserved; this section appended only |
| **Live decompile** | ≡ 2026-07-23 scaffold CF (7-dword template fill; count in EAX; null dest gate) |
| **Body** | `0x0045f150`–`0x0045f198` inclusive (**73 B** / `0x49`); pad `CC`×7 then next @ `0x0045f1a0` |
| **Epilog** | bare **`C3`** (`POP EDI; POP ESI; RET`) |
| **Classification** | leaf — 0 callees |
| **Callers (1 func / 2 sites)** | `FUN_0045cd30` (`StdVector_InsertN_Pod28`) @ `0x0045ceae`, `0x0045cfb7` |

### Full body hex (73 B)

```
568bf085f676408d421457eb038d490085d274278b39893a8b79048978f08b79088978f48b790c8978f88b79108978fc8b791489388b791889780483c21c83c01c83ee0175ca5f5ec3
```

### Byte-sealed ABI / CF

```
PUSH ESI
MOV ESI, EAX                 ; count = EAX
TEST ESI,ESI / JBE done       ; unsigned zero/neg early-out
LEA EAX, [EDX+0x14]          ; mid-element write cursor (dest+20)
PUSH EDI
NOP (LEA ECX,[ECX+0])        ; align
loop:
  TEST EDX,EDX / JZ skip     ; null-safe dest
  ; copy 7 dwords from [ECX] template → [EDX] dest (via EDI temps + EAX mid)
  MOV EDI,[ECX]    / MOV [EDX],EDI
  MOV EDI,[ECX+4]  / MOV [EAX-0x10],EDI
  MOV EDI,[ECX+8]  / MOV [EAX-0x0C],EDI
  MOV EDI,[ECX+0xC]/ MOV [EAX-8],EDI
  MOV EDI,[ECX+0x10]/ MOV [EAX-4],EDI
  MOV EDI,[ECX+0x14]/ MOV [EAX],EDI
  MOV EDI,[ECX+0x18]/ MOV [EAX+4],EDI
skip:
  ADD EDX, 0x1C              ; dest += 28
  ADD EAX, 0x1C              ; mid cursor += 28
  SUB ESI, 1 / JNZ loop
POP EDI
POP ESI
RET
```

| Slot | Register | Role |
|---|---|---|
| count | **EAX** (→ ESI) | element count; `TEST/JBE` early-out |
| template* | **ECX** (`param_1`) | const 7-dword / 0x1c source; not advanced |
| dest* | **EDX** (`param_2`) | write cursor; +0x1c each iter even if null |
| return | none | void; bare RET |

Call-site seal (parent `FUN_0045cd30` @ `0x0045ceae`):

```
LEA ECX, [ebp-0x30]   ; value snapshot (0x1c POD on stack)
MOV EAX, ESI          ; count
MOV EDX, EDI          ; dest cursor
CALL FUN_0045f150
```

Decompiler `__fastcall (template*, dest*)` + phantom `in_EAX` ≡ bytes. **Stride 0x1c / 7 dwords.** Not stack formals; bare RET. Sibling fill-n at `FUN_0046a260` uses different register map (count ECX / dest EAX / template EBX + `REP MOVSD`).
