# Annotated low-level: StdTree_EraseRange_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004e4e10` |
| VA | `0x004e4e10`–`0x004e4ec4` exclusive (**180 B** / `0xB4`) |
| Canonical name | `StdTree_EraseRange_Isnil21_Inferred` |
| Ghidra | `FUN_004e4e10` |
| System | skills-abilities / STL map-set (isnil@+0x21 range erase) |
| Date | 2026-08-05 (R13-024 dual; scaffold 2026-07-23) |
| Parent dual | `0x004e3e70` `StdTree_EraseAndRebalance_Isnil21_Inferred` |
| Terminal | **false** |

## Machine-level notes

- Prefer bytes over decompiler for **RET 0x0C** both exits and stack formals.
- **isnil@+0x21** on every successor test (`80 ?? 21 00`).
- Full clear: free subtree of **root** (`head->parent` / `head[1]`) via `FUN_004e2e00`, then reset head L/P/R to self and **size=0**.
- Partial: inline MSVC successor (same algebra as erase's nextnode), then `thiscall` erase `FUN_004e3e70(map, &first, first)`.
- Sole caller `FUN_004e7320` is map shell clear: `erase(begin,end)` then `operator_delete(head)` (decomp may mark delete noreturn).
- Peer twin `FUN_004e52a0`: same isnil21 erase; **different** free `FUN_004e2e40` — do not merge.

## Signature (byte-sealed)

```c
// __thiscall  RET 0x0C
void StdTree_EraseRange_Isnil21_Inferred(
    /*ECX*/ MapShell_Isnil21 *map,   // head@+4, size@+8
    MapNode_Isnil21 **outIt,         // stack+4
    MapNode_Isnil21 *first,          // stack+8  inclusive
    MapNode_Isnil21 *last);          // stack+C  exclusive
```

## Pseudocode (annotated)

```c
void __thiscall StdTree_EraseRange_Isnil21_Inferred(
    MapShell_Isnil21 *map,      // ECX / EDI
    MapNode_Isnil21 **outIt,    // param_2
    MapNode_Isnil21 *first,     // param_3 / ESI
    MapNode_Isnil21 *last)      // param_4 / EBX
{
  MapNode_Isnil21 *head = map->head; // [map+4]

  // Full clear: [begin, end) == [head->left, head]
  if (first == head->left && last == head) {
    FUN_004e2e00(head->parent);      // free non-nil subtree of root
    head->parent = head;
    map->size = 0;
    head->left = head;
    head->right = head;
    *outIt = head->left;             // begin after clear == end
    return;                          // RET 0x0C
  }

  while (first != last) {
    // Inline successor for isnil@+0x21 nodes (leftmost of right, else climb)
    MapNode_Isnil21 *succ = first;
    if (first->isnil == 0) {
      MapNode_Isnil21 *r = first->right;
      if (r->isnil == 0) {
        succ = r;
        MapNode_Isnil21 *l = r->left;
        while (l->isnil == 0) {
          succ = l;
          l = l->left;
        }
      } else {
        MapNode_Isnil21 *p = first->parent;
        MapNode_Isnil21 *cur = first;
        while (p->isnil == 0 && cur == p->right) {
          cur = p;
          p = p->parent;
        }
        succ = p;
      }
    }
    // bytes @ 0x004e4eaf: push first; lea out(&first); push out; mov ecx,map; call 004e3e70
    StdTree_EraseAndRebalance_Isnil21_Inferred(map, &first, first);
    first = succ;
  }
  *outIt = first;
  return;                            // RET 0x0C
}
```

## Layout (shared with parent erase dual)

| Off | Field | Role |
|---|---|---|
| map`+0x04` | head* | end sentinel; left=begin, parent=root, right=rightmost |
| map`+0x08` | size | zeroed on full clear; decremented inside single erase |
| node`+0x00` | left* | |
| node`+0x04` | parent* | |
| node`+0x08` | right* | |
| node`+0x10` | Val16 payload | not destroyed here (free/erase paths own lifecycle) |
| node`+0x20` | color | used inside erase, not this shell |
| node`+0x21` | isnil | successor tests |

## Call graph

| Dir | VA | Role |
|---|---|---|
| Caller | `FUN_004e7320` @ `0x004e7332` | sole xref — clear range then delete head |
| Callee | `FUN_004e2e00` | full-tree free (isnil@+0x21; residual left-recurse decomp) |
| Callee | `FUN_004e3e70` | single-node erase+rebalance (parent dual R12-013) |
| Peer | `FUN_004e52a0` | same erase; free `004e2e40` |
| Parent | `0x004e3e70` | partition parent dual |

## Open questions

- Product / PDB demangle for owning `map<K,V>` of `FUN_004e7320` / skills-abilities user.
- Complete free-subtree algebra of `FUN_004e2e00` (decomp may omit left recurse; false-noreturn on `operator_delete`).
- Whether twin `004e52a0` free differs only by value dtor.
- Runtime / bit-exact / differential.
