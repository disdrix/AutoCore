# Annotated low-level: StdUninitFillN_RcElem28 (`FUN_00461c90`)

| Field | Value |
|---|---|
| Stable ID | `aa_00461c90` |
| VA | `0x00461c90`–`0x00461d74` exclusive (**228 B** / `0xE4`) |
| System | MSVC `_Uninit_fill_n` / refcounted elem stride **0x1c** |
| Date | 2026-07-29 (W35-O) |
| Canonical name | `StdUninitFillN_RcElem28` (**Inferred**) |

## Machine-level notes

- Prefer **bytes** over decompiler for SEH unwind path (decompiler shows main loop only).
- ABI: **ECX=value\***, **EDX=count**, stack **dest\***, **RET 0**. Call sites may push an extra unused dword (`ADD ESP,8`).
- Element stride **0x1c** (`ADD ESI, 0x1C` / `param_3 + 7` dwords).
- Shared resource at **`+0x10`**: AddRef = refcount++ then `vtbl+4` when becomes 1; unwind Release = refcount-- then `vtbl+8` when 0.
- Layout matches parent W34-P `StdVector_InsertN_RcElem28` / copy-ctor `FUN_00461ac0`.

## Sealed signature

```c
// __fastcall; RET 0
void StdUninitFillN_RcElem28(
    const RcElem28* value,   /* ECX */
    int count,               /* EDX */
    RcElem28* dest);         /* stack [ebp+8] */
```

## Pseudocode (annotated)

```c
// SEH frame (state around each element construction)
void StdUninitFillN_RcElem28(const RcElem28* value /*EDI*/, int count /*EBX*/,
                             RcElem28* dest /*ESI = [ebp+8]*/)
{
  // EH: state=0, save dest_start
  if (count == 0) goto done;

  for (; count != 0; --count, dest += 1 /* +0x1c */) {
    // EH state = 1 during construction
    if (dest != nullptr) {
      dest->dw0 = value->dw0;  // +0x00
      dest->dw1 = value->dw1;  // +0x04
      dest->dw2 = value->dw2;  // +0x08
      dest->dw3 = value->dw3;  // +0x0C
      // AddRef shared resource @+0x10
      RcShared* r = value->shared;
      if (r != nullptr) {
        r->refcount += 1;
        if (r->refcount == 1)
          r->vtbl->AddRef_or_OnFirst();  // [vtbl+4]
      }
      dest->shared = value->shared;     // +0x10
      dest->b14 = value->b14;           // +0x14
      dest->b15 = value->b15;           // +0x15
      dest->b16 = value->b16;           // +0x16
      dest->dw18 = value->dw18;         // +0x18
    }
    // EH state = 0 after element
  }

  // EH unwind (if exception): for each constructed dest in [start, cur):
  //   Release shared @+0x10 (refcount-- / vtbl+8 when 0)
done:
  return;
}
```

## Callers

| Site | Role |
|---|---|
| `FUN_004617a0` @ `0x004618e5` | InsertN grow: fill new slots from local value |
| `FUN_004617a0` @ `0x00461a0c` | InsertN in-place / mid path fill |
| `FUN_00460f90` @ `0x00460fe8` | push_back-style: count=1 into vector end |

## Open questions

- Product/PDB English for T (gfx residual only via ancestor names).
- Exact semantics of AddRef gate (refcount becomes 1 → vtbl+4) vs classic COM — port must match observed gate.
- Nested `FUN_00460d70` / resource vtbl not dual-owned here.
- Runtime / bit-exact open.
