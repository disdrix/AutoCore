# Annotated low-level: FUN_00971640

| Field | Value |
|---|---|
| Stable ID | `aa_00971640` |
| VA | `0x00971640`–`0x009716f7` (**184 B**) |
| Structural | `AssPreloader_EnqueueEligibleFromRing_Inferred` |
| System | assPreloader / asset preload pipeline |
| Date | 2026-07-23 scaffold; **W31-K 2026-07-29** dual seal |

## Machine-level notes

- **ABI (bytes):** ESI = source GuardedVector ring; stack host; `SUB ESP,0x10` … `ADD ESP,0x10` / **`RET 4`**.
- **Decompiler** shows `unaff_ESI` + void return — **bytes supersede** (ESI formal, stdcall 1).
- Callers (`FUN_00971700` / `a0` / `820`): push host, then `lea/mov esi, local_ring`, call.
- Ring index math matches GuardedVector family (page=`idx>>2`, wrap by capacity, 4 dwords/page).
- Insert path: `lea edi,[host+0x1c]` → contains (`FUN_0043e5b0`) → push (`FUN_0043e4b0` → `FUN_0043d670` → `FUN_0043d420` copies `*key`).
- Event: IAT `[0x009c618c]` = `SetEvent`, arg `*(host+4)`.

## Sealed control flow

```
dirty = 0
for idx in [source.begin, source.begin+source.size):
  key* = page_map_slot(source, idx)
  it = tree_lower_bound(host+0x10, *key)   // FUN_0043d5e0
  if it != *(host+0x14): continue          // already in map
  node = cache_lookup(*(host+0x110), *key) // FUN_0044e8c0
  if node != cache_end && *(node+0xc) != 0: continue  // loaded
  if ContainsFirstEqual(host+0x1c, key*): continue
  dirty = 1
  Push(host+0x1c, *key)                    // FUN_0043e4b0
if dirty: SetEvent(*(HANDLE*)(host+4))
```

## Pseudocode (annotated copy of raw)

```c
void FUN_00971640(int param_1 /* host; ESI = source ring */)
{
  // unaff_ESI := source GuardedVector (callers)
  // local_10 := tree iterator out
  // local_c  := cache node out
  // host+0x1c pending; host+0x10 tree; host+0x110 cache; host+4 event
  ...
}
```

## Gaps

- Product/PDB method English (AssPreloader family via sibling path string only).
- Full product type of tree at +0x10 and cache node +0xc field.
- Runtime / bit-exact.
