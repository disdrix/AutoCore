# Raw capture: FUN_0065eb10

| Field | Value |
|---|---|
| **Stable ID** | `aa_0065eb10` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0065eb10` |
| **Canonical name** | `FUN_0065eb10` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0065eb10(undefined1 *param_1)

{
  undefined4 uVar1;
  
  uVar1 = DAT_00a0f520;
  *param_1 = 0;
  *(undefined4 *)(param_1 + 4) = uVar1;
  return;
}
```

---

## W25-O dual seal append (2026-07-29)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual W25-O |
| **Tools** | `decompile_function` + `analyze_function_complete` + `read_memory` + `get_function_by_address` + callers/xrefs (**no** `disassemble_bytes`) |
| **Body** | `0x0065eb10`–`0x0065eb22` (**19** bytes; leaf; plain `RET`) |
| **Live decompile** | ≡ original raw CF (`__fastcall` ECX out-desc; `*desc=0`; `*(float*)(desc+4)=DAT_00a0f520`) |
| **Constant** | `DAT_00a0f520` @ `0x00a0f520` = `00 00 7a 44` = **1000.0f** (`read_memory`) |
| **Named role** | `Phys_StackDesc_ResetFlag0_Float1000` (**Inferred**) — stack/CCS descriptor teardown: clear flag byte, write 1000.0f at +4 |
| **Callers (2)** | `FUN_0064fe40` @ `0x0064fe40` (wheels/stack-desc post-ctor); `hkDefaultChassis_vtbl_0x18` @ `0x0064feb0` (optional CCS reset) |
| **Callees** | none (leaf) |
| **Note** | Decompiler shows `void`; bytes leave **EAX = ECX** (`mov eax,ecx`) so callers may treat return as `this` |

### Machine hex (`read_memory` 64 B @ `0x0065eb10`; body 19 B before `CC` pad)

```
f3 0f 10 05 20 f5 a0 00   ; movss xmm0, [DAT_00a0f520]
8b c1                     ; mov eax, ecx
c6 00 00                  ; mov byte ptr [eax], 0
f3 0f 11 40 04            ; movss [eax+4], xmm0
c3                        ; ret
```

Compact body:
```
f30f100520f5a0008bc1c60000f30f114004c3
```

### Byte-level CF map

| Bytes | Meaning |
|---|---|
| `F3 0F 10 05 20 F5 A0 00` | load **1000.0f** from `0x00a0f520` into XMM0 |
| `8B C1` | EAX = ECX (out descriptor; also return value) |
| `C6 00 00` | `*desc = 0` (flag/type byte) |
| `F3 0F 11 40 04` | `*(float*)(desc+4) = 1000.0f` |
| `C3` | ret (no stack cleanup) |
