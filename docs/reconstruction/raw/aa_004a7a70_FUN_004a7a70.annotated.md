# Annotated low-level: FUN_004a7a70 / StdUninitFillN_BasicStringW

| Field | Value |
|---|---|
| Stable ID | `aa_004a7a70` |
| VA | `0x004a7a70`–`0x004a7aa0` exclusive (**48 B**) |
| System | MSVC `_Uninit_fill_n` wrapper for `basic_string<wchar_t>` (elem 0x1c) |
| Date | 2026-07-23 scaffold; W36-S refine 2026-07-29 |

## Machine-level notes

- **ABI:** **thiscall** ECX = alloc/host (forwarded); stack **dest***, **count**, **value***; **`RET 0x0C`**; returns **dest + count×0x1c** in EAX.
- **Body:** thin wrapper — five-arg cdecl call to `FUN_004a74f0(dest, count, value, alloc, value)` then `ADD ESP,0x14` then lea end-pointer.
- **Stride math:** `count*8 - count` then `*4` = `count*28` = `count*0x1c`.
- **Sole caller:** parent insert-n **in-place hole** arm only (`0x004a7d4a`). Grow path fills via `FUN_004a74f0` directly.
- **Twin:** char `StdUninitFillN_BasicString` (`0x00430390`, W33-G) same wrapper shape → `FUN_00431700`.
- Nested loop + SEH lives in unowned `FUN_004a74f0` (W36-R pair with `0x004a6b50`).

## Pseudocode (annotated)

```c
// thiscall ECX=alloc; stack dest*, count, value*; RET 0x0C
// returns dest + count * 0x1c
BasicStringW_0x1c* __thiscall StdUninitFillN_BasicStringW(
    void* alloc,
    BasicStringW_0x1c* dest,
    int count,
    const BasicStringW_0x1c* value)
{
  FUN_004a74f0(dest, count, value, alloc, value); // cdecl +5; ADD ESP,0x14
  return (BasicStringW_0x1c*)((char*)dest + (uint32_t)count * 0x1c);
}
```

## Call graph

| Direction | Targets |
|---|---|
| Callers | `StdVector_InsertN_BasicStringW` (`0x004a7ad0`) @ `0x004a7d4a` only |
| Callees | `FUN_004a74f0` |

## Open / residual

- Nested `FUN_004a74f0` product demangle / SEH plate (not owned).
- Allocator ECX formal English (forwarded residual).
- Runtime / bit-exact / differential.
