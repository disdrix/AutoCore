# Annotated low-level: Client_DismissModalPrompt (`FUN_007fc360`)

| Field | Value |
|---|---|
| Stable ID | `aa_007fc360` |
| VA | `0x007fc360`–`0x007fc487` |
| System | client UI modal / message panel |
| Date | 2026-07-29 (W17-M dual seal) |

## Machine-level notes

- Live dual decompile identical; body bytes 295 B sealed.
- **EAX = client/game** (`MOV ESI,EAX`); void; plain `RET`.
- Outer gate: panel ptr `+0x1118` and host ptr `+0xf40`.
- Inverse of `Client_ShowModalPrompt` dual-id backup (`+0x514/+0x518` → `+0x498/+0x49c`).
- Optional C2S **`0x8006`** size `0x18` when pending flag `+0x30f4`.
- Clears panel flag `+0x4fd` whenever outer gate holds.

## Key offsets (client = EAX/ESI)

| Off | Role in this unit |
|---|---|
| `+0x1118` | message panel / `CDlgPopup*` |
| `+0xf40` | modal host ptr (points to host object used for dual-id) |
| `+0xf38` | secondary UI controller |
| `+0x1030` | array of 0x3A slot object pointers (skip index `0x16`) |
| `+0x30f4` | pending-send flag for opcode `0x8006` |
| `+0x31d8` | byte arg restored into `+0xf38` virtual `+0x3c4` |
| panel`+0x4fd` | flag cleared at common exit |
| host`+0x498`/`+0x49c` | live dual-id (primary/secondary modal codes) |
| host`+0x514`/`+0x518` | backup dual-id restored on dismiss |
| slot`+0x500` (`[0x140]`) | compared to **1** during skip scan |

## Packet buffer (when `+0x30f4`)

| Buffer off | Value |
|---|---|
| `+0x00` | `uint32` opcode **`0x8006`** |
| `+0x11` | `uint8` **0** |
| `+0x14` | `uint32` **9** |
| size | **0x18** passed to `FUN_008073b0` |

## Pseudocode (annotated)

```c
// EAX = ClientGame*
void Client_DismissModalPrompt(void)
{
  ClientGame *client = /* EAX */;
  int **panel = *(int ***)(client + 0x1118);
  int *hostHolder = *(int **)(client + 0xf40);

  if (panel == NULL || hostHolder == 0)
    return;

  if ((*(code **)(*panel + 0x3d8))() != 0) {
    if (*hostHolder != 0) {
      int *host = (int *)*hostHolder;
      (*(code **)(*host + 0xb0))(panel);
    }
    (*(code **)(*panel + 0x440))();

    if (*(char *)(client + 0x30f4) != 0) {
      uint8_t buf[0x18];
      *(char *)(client + 0x30f4) = 0;
      memset-pattern: opcode 0x8006 @0, byte0 @0x11, dword9 @0x14;
      FUN_008073b0(0x18, buf);   // ESI still client
    }

    if (*hostHolder != 0) {
      int *host = (int *)*hostHolder;
      host[0x498/4] = host[0x514/4];
      host[0x49c/4] = host[0x518/4];
    }

    if (*(int *)(client + 0xf38) != 0) {
      int i = 0;
      int **slots = (int **)(client + 0x1030);
      do {
        if (i != 0x16) {
          int *obj = slots[i];
          if (obj && (*(code **)(*obj + 0x3d8))() && obj[0x140] == 1)
            goto clear_flag;
        }
        i++;
      } while (i < 0x3a);
      (**(code **)(**(int **)(client + 0xf38) + 0x3c4))(*(uint8_t *)(client + 0x31d8));
    }
  }
clear_flag:
  *(*(uint8_t **)(client + 0x1118) /* panel base */ + 0x4fd) = 0;
}
```

## Open questions (non-blocking)

- Product English for opcode **`0x8006`** and field **9**.
- Semantic of slot index **0x16** exclusion and `obj+0x500==1`.
- Exact names of virtuals `+0x3d8` / `+0x440` / `+0xb0` / `+0x3c4` (other owners).
