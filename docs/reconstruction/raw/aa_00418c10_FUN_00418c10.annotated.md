# Annotated low-level: StdTree_Rrotate_Val12 (FUN_00418c10)

| Field | Value |
|---|---|
| Stable ID | `aa_00418c10` |
| VA | `0x00418c10` |
| Body | `0x00418c10`–`0x00418c62` exclusive (**82** B) |
| Canonical | `StdTree_Rrotate_Val12` (inferred) |
| Ghidra | `FUN_00418c10` |
| System | MSVC std `_Tree` right-rotate (Val12 node) |
| Date | 2026-07-29 (W26-Q seal; scaffold 2026-07-23) |

## Machine-level notes

- **ABI:** `__thiscall`; **ECX** = tree (`_Myhead` at `this+4`); stack arg = **node* x**; **void**; **`RET 4`**.
- **Leaf:** no callees; three `RET 4` exits (root / right-child / left-child reattach).
- **Node layout (Val12 / 0x1c):** same as Lrotate twin — isnil @ `+0x19` seals Val12.
- **Nil test:** `byte [y->right + 0x19] == 0` → non-nil; then parent = x.
- **Semantics:** classical RB / MSVC `_Rrotate(x)`:
  1. `y = x->left`
  2. `x->left = y->right`; if `y->right` not nil → parent = x
  3. `y->parent = x->parent`
  4. Reattach `y` where `x` hung (root / right / left of parent)
  5. `y->right = x`; `x->parent = y`
- **Pair:** left-rotate twin `FUN_00573170` / `StdTree_Lrotate_Val12`.
- **Shared:** same 13 callers as Lrotate; 32 xrefs (slightly more call sites).

## Pseudocode (annotated)

```c
// __thiscall tree*, node* x  — right-rotate about x
void __thiscall StdTree_Rrotate_Val12(int tree, int *x)
{
  int y;       // x->left (node*)
  int *parent;

  y = *x;                               // y = x->left
  *x = *(int *)(y + 8);                 // x->left = y->right
  if (*(char *)(*(int *)(y + 8) + 0x19) == '\0') {
    *(int **)(*(int *)(y + 8) + 4) = x; // y->right->parent = x
  }
  *(int *)(y + 4) = x[1];               // y->parent = x->parent
  if (x == *(int **)(*(int *)(tree + 4) + 4)) {  // x was root
    *(int *)(*(int *)(tree + 4) + 4) = y;
    *(int **)(y + 8) = x;               // y->right = x
    x[1] = y;                           // x->parent = y
    return;
  }
  parent = (int *)x[1];
  if (x == (int *)parent[2]) {         // x was right child
    parent[2] = y;
    *(int **)(y + 8) = x;
    x[1] = y;
    return;
  }
  *parent = y;                          // x was left child
  *(int **)(y + 8) = x;
  x[1] = y;
  return;
}
```

## Live ≡ raw

Live 2026-07-29 decompile ≡ 2026-07-23 raw (identical CF). Full body hex sealed via `read_memory`.

## Open questions

- Product / MSVC demangle for each instantiating map type (shared helper).
- Runtime / bit-exact golden not run.
- Prior auto name `Named_CalleeOf_…RecvSkillStatusEffect` is **narrow** — this is a shared Val12 rotate, not skill-status exclusive.
