# Annotated low-level: Weather_NfxListFillOrActivate_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00556b10` |
| VA | `0x00556b10`–`0x00556ce2` exclusive (**466 B**) |
| Ghidra | `FUN_00556b10` |
| Inferred name | `Weather_NfxListFillOrActivate_Inferred` |
| System | client weather / NDSpecialFX nfx |
| Date | 2026-07-29 (W32-I re-verify) |

## Machine-level notes

- Source: raw capture + live `batch_decompile` / `read_memory` (W32-I).
- **No** `disassemble_bytes`.
- Frame `SUB ESP,0x108` (path buffer ~260 B); **`RET 0xC`**.
- Entry: ECX = weather host; stack0 = `GuardedVector*` list (cleared first); stack1 = weather object; stack2 = mode.
- `DAT_009ca184` = `"_nfx.xml"` (9 bytes incl NUL) — same constant as `FxCache_ApplyWithNfxEnsure` / `FxMaster_LoadMasterFromXML`.
- Mode 0 fills list with resolved nfx resource ids; mode 1 activates/loads weather FX via `FUN_004b88e0` / `FUN_00553d20`.
- Caller `FUN_005575c0` (weather transition): list A at `host+0x190` mode 0, list B at `host+0x1a4` mode 1.

## ABI

```
// ECX = weather host*
// stack: (GuardedVector* outList, void* weatherObj, char mode); RET 0xC
void Weather_NfxListFillOrActivate_Inferred(
    void* weatherHost /*ECX*/,
    GuardedVectorHeader* outList,
    void* weatherObj,
    char mode);
```

## Control flow (annotated)

```
FUN_0043d650(outList, 0)          // clear / resize-to-0
if weatherObj == 0: return

if mode == 0:                     // fill resource-id list
  if FUN_00555660(weather+0x558 → nameBuf):
    if strstr(name, "_nfx.xml") == null:
      strcat-equivalent append DAT_009ca184 ("_nfx.xml") onto path workbuf
    if resource present (007b6a20 / 007b6730):
      FUN_00989e00 resolve id; PushBack_Thiscall(outList, &id)

  if FUN_00553ce0(TOD slot name → nameBuf):  // host field path +0x150 family
    same _nfx.xml gate + resource open + push_back

else:                             // activate
  if FUN_00555600(weather+0x558):
    FUN_004b88e0(..., 1, 0, 0)
  if TOD index in [0,4) and FUN_00553d20(0) loads weather FX:
    FUN_004b88e0(..., 1, 0, 0); return
```

## Constants

| Address | Bytes / string |
|---|---|
| `0x009ca184` | `5f 6e 66 78 2e 78 6d 6c 00` → `_nfx.xml` |

## Callers / callees

| Direction | Symbol |
|---|---|
| Caller | `FUN_005575c0` (xrefs @ `005577c8`, `00557851`) |
| Callees | `0043d650`, `00555660`, `00553ce0`, `00555600`, `00553d20`, `strstr`, `007b6a20`, `007b6730`, `00989e00`, `0043d670`, `004b88e0` |
