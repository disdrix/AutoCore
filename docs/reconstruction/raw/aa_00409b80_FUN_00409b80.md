# Raw capture: FUN_00409b80

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409b80` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409b80` |
| **Canonical name** | `FUN_00409b80` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00409b80(int param_1)

{
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  puStack_8 = &LAB_009bc656;
  local_c = ExceptionList;
  local_4 = 0xffffffff;
  ExceptionList = &local_c;
  std::basic_string<char,struct_std::char_traits<char>,class_std::allocator<char>_>::
  ~basic_string<char,struct_std::char_traits<char>,class_std::allocator<char>_>
            ((basic_string<char,struct_std::char_traits<char>,class_std::allocator<char>_> *)
             (param_1 + 0xc));
  ExceptionList = local_c;
  return;
}
```

---

## WQ9K-A re-verify (2026-08-05) — append only

| Field | Value |
|---|---|
| **Agent** | WQ9K-A OWN-ONLY |
| **Tools** | `batch_decompile` / `decompile_function` + `analyze_function_complete` + `read_memory` + `disassemble_function` + callers/xrefs + `get_assembly_context` |
| **No** | `disassemble_bytes`; Launcher; parent ledgers |
| **Live decompile** | ≡ scaffold CF (`~basic_string` @ `param+0xc` under SEH) — **ABI sealed as stdcall RET 4** |
| **Named** | `StdTree_DestroyStringAt0C_Inferred` |

### Body / ABI (bytes authority)

- **Range:** `0x00409b80`–`0x00409bc0` inclusive last RET byte (**65 B** / `0x41`); pad `CC`.
- **ABI:** stack `node*`; **void**; **`RET 4`** (`C2 04 00`) — **stdcall**.
- **Full body hex:**
```
64 A1 00 00 00 00 6A FF 68 56 C6 9B 00 50 64 89 25 00 00 00 00
83 C8 FF 89 44 24 08 8B 4C 24 10 83 C1 0C 89 4C 24 10 89 44 24 08
FF 15 F4 62 9C 00 8B 0C 24 64 89 0D 00 00 00 00 83 C4 0C C2 04 00
```
- **`disassemble_function` listing:**
```
00409b80  MOV EAX,FS:[0]
00409b86  PUSH -1
00409b88  PUSH LAB_009bc656
00409b8d  PUSH EAX
00409b8e  MOV FS:[0],ESP          ; SEH frame
00409b95  OR  EAX,-1
00409b98  MOV [ESP+8],EAX         ; try level -1
00409b9c  MOV ECX,[ESP+10]        ; node*
00409ba0  ADD ECX,0x0C            ; string at node+0x0c
00409ba3  MOV [ESP+10],ECX        ; rewrite stack slot (unused after)
00409ba7  MOV [ESP+8],EAX
00409bab  CALL [0x009c62f4]       ; thiscall ~basic_string (ECX)
00409bb1  MOV ECX,[ESP]
00409bb4  MOV FS:[0],ECX          ; SEH teardown
00409bbb  ADD ESP,0x0C
00409bbe  RET 4
```

### Semantics

Shared **tree-node value destroy** used by free-subtree twins:
- Install MSVC SEH frame (`LAB_009bc656`).
- `ECX = node + 0x0c`; call IAT `~std::basic_string<char,...>` (`[0x009c62f4]`).
- Unlink SEH; **RET 4**.
- Does **not** free the node, touch color/isnil, or destroy other fields past the string.

### Callers / xrefs (2)

| Site | Parent | Role |
|---|---|---|
| `0x00409942` | `FUN_00409920` FreeSubtree isnil@+0x131 | after right-recurse; before `operator_delete` |
| `0x0040998f` | `FUN_00409970` FreeSubtree isnil@+0x2d | same pattern (shared dtor) |

Both sites: `PUSH EDI` (node) → CALL this → `PUSH EDI` → `operator_delete` cdecl.

### Callees

- `std::basic_string::~basic_string` via IAT `0x009c62f4` only.

### Gaps

- Product map/key RTTI beyond proven string @+0x0c (node payload sizes differ across isnil families).
- Erase-range parents `00407b70` / `00407f90` residual (not this OWN).
- Runtime / bit-exact / differential.
