# Annotated low-level: FUN_00497360 / StdList_Destroy_FreeHead

| Field | Value |
|---|---|
| Stable ID | `aa_00497360` |
| VA | `0x00497360`–`0x004973a7` (**72 B**) |
| System | shared / MSVC std::list |
| Date | 2026-07-29 (W27-K re-annotate) |
| Ghidra name | `FUN_00497360` |
| Semantic name | `StdList_Destroy_FreeHead` |

## Machine-level notes

- Source: raw 2026-07-23 decompile **incomplete** (loop collapsed; epilogue truncated). W27-K **`read_memory`** seals full body.
- **__fastcall / thiscall-style:** ECX = list object.
- Layout used: `+4` = sentinel head*, `+8` = size (dword). `+0` unused here.
- MSVC circular list: empty ⇔ `head->_Next == head` (and `_Prev == head`).
- Destroys **all nodes** then **frees sentinel** and **nulls** `list+4` — full list-object destructor tail (not clear-only).
- Sibling pattern: `StdList_Destroy_FreeHead_Inferred` `0x00403430` (calls clear helper then free head); this unit **inlines** the free-loop.

## Layout (sealed)

| Offset | Role |
|---|---|
| list+0x04 | `_Myhead` sentinel node* |
| list+0x08 | `_Mysize` |
| node+0x00 | `_Next` |
| node+0x04 | `_Prev` |
| node+0x08… | payload (not touched — nodes freed as blocks) |

## Pseudocode (annotated; CF ≡ **bytes**, not collapsed decompile)

```c
// ECX = list*; bare RET; void
void __fastcall StdList_Destroy_FreeHead(void *list)
{
  uint32_t *head;
  void *node;
  void *next;

  head = *(uint32_t **)((char *)list + 4);
  node = (void *)*head;                 /* first = head->_Next */
  *head = (uint32_t)head;               /* head->_Next = head */
  *(uint32_t *)(*(int *)((char *)list + 4) + 4) =
      *(uint32_t *)((char *)list + 4);  /* head->_Prev = head */
  *(uint32_t *)((char *)list + 8) = 0;  /* size = 0 */

  if (node != *(void **)((char *)list + 4)) {
    do {
      next = *(void **)node;            /* node->_Next */
      operator_delete(node);
      node = next;
    } while (node != *(void **)((char *)list + 4));
  }

  operator_delete(*(void **)((char *)list + 4)); /* free sentinel */
  *(uint32_t *)((char *)list + 4) = 0;           /* null head */
}
```

## Decompiler hazards (do not trust)

1. Ghidra shows a **single** `operator_delete(first)` then unconditional delete of head — **missing loop**.
2. Both deletes marked **noreturn** — false; epilogue is `POP ESI; RET`.
3. Null-head store `list+4 = 0` absent from 2026-07-23 raw decompile body.

## Open questions

- Exact `list+0` field (allocator / padding) — unused in this body.
- Product English beyond MSVC list role — residual.
- Runtime differential — open.
