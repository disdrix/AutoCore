# Raw capture: FUN_00569320

| Field | Value |
|---|---|
| **Stable ID** | `aa_00569320` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00569320` |
| **Canonical name** | `FUN_00569320` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00569320(int param_1,undefined4 *param_2,int *param_3)

{
  undefined4 uVar1;
  bool bVar2;
  int *piVar3;
  undefined4 *puVar4;
  undefined4 *puVar5;
  
  piVar3 = param_3;
  puVar5 = *(undefined4 **)(param_1 + 4);
  bVar2 = true;
  if (*(char *)((int)puVar5[1] + 0x49) == '\0') {
    puVar4 = (undefined4 *)puVar5[1];
    do {
      puVar5 = puVar4;
      bVar2 = *param_3 < (int)puVar5[3];
      if (bVar2) {
        puVar4 = (undefined4 *)*puVar5;
      }
      else {
        puVar4 = (undefined4 *)puVar5[2];
      }
    } while (*(char *)((int)puVar4 + 0x49) == '\0');
  }
  param_3 = puVar5;
  if (bVar2) {
    if (puVar5 == (undefined4 *)**(int **)(param_1 + 4)) {
      puVar5 = (undefined4 *)FUN_005690a0(&param_3,1,puVar5,piVar3);
      uVar1 = *puVar5;
      *(undefined1 *)(param_2 + 1) = 1;
      *param_2 = uVar1;
      return;
    }
    FUN_00537770();
  }
  if (param_3[3] < *piVar3) {
    puVar5 = (undefined4 *)FUN_005690a0(&param_3,bVar2,puVar5,piVar3);
    *param_2 = *puVar5;
    *(undefined1 *)(param_2 + 1) = 1;
    return;
  }
  *(undefined1 *)(param_2 + 1) = 0;
  *param_2 = param_3;
  return;
}
```

# Dual A/B re-verify (W19-J, 2026-07-29)

**Tools:** Ghidra `decompile_function` + `read_memory` only. No `disassemble_bytes`. No Launcher.

### Live bounds / ABI (sealed)

| Item | Value |
|---|---|
| Body | `0x00569320`–`0x005693d8` (**184** bytes) |
| ABI | `__thiscall` map*; out-pair*; key* — **`ret 8`** (`C2 08 00`) |
| Sole caller | `FUN_00569560` @ `0x005696ad` (hinted insert falls through to this) |

### Node / map shell (sealed; matches Map_Predecessor dual)

| Off | Field |
|---|---|
| map `+0x04` | head / end header node* |
| map `+0x08` | size (touched in insert callee `FUN_005690a0`) |
| node `+0x00` | left* |
| node `+0x04` | parent* |
| node `+0x08` | right* |
| node `+0x0C` | **int key** (`node[3]`) |
| node `+0x48` | color (in insert callee) |
| node `+0x49` | **isnil** (walk stop) |

### Algorithm (MSVC map/set insert-or-find)

1. Start at `head->parent` (root); `go_left=true`.
2. While child not isnil: compare `*key < node->key`; left or right; remember last compare.
3. If last went left: if at leftmost (`node == *head`) insert left of head; else `Map_Predecessor` (`FUN_00537770`) then fall through.
4. If `node->key < *key` → insert via `FUN_005690a0` (throws `"map/set<T> too long"` if size huge); out `{it, inserted=1}`.
5. Else key equal → out `{it=node, inserted=0}`.

### CF ≡ raw

Live decompile ≡ raw plate. Insert callee string **`"map/set<T> too long"`** seals std map/set family.

### read_memory seals

- Entry: `51 55 8b 6c 24 10 56 57 8b f9 8b 77 04` = this→EDI, head from `[edi+4]`, key from stack
- Exit paths: `c2 08 00` (ret 8) × multiple return sites; trailing `cc` padding at `0x005693d8`

