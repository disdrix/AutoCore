# Annotated low-level: CVOGHBMoveVehicleToMouse_ScalarDeletingDtor (FUN_005996a0)

| Field | Value |
|---|---|
| Stable ID | `aa_005996a0` |
| VA | `0x005996a0` |
| Canonical name | `CVOGHBMoveVehicleToMouse_ScalarDeletingDtor` |
| Ghidra name | `FUN_005996a0` |
| System | `input-drive-control` / MSVC scalar-deleting dtor |
| Date | 2026-08-05 (R12-028 refresh) |
| Body | `0x005996a0`–`0x005996bd` inclusive (**30 B** / `0x1E`) |

## Machine-level notes

- Source: raw capture + R12-028 live re-verify (`disassemble_function` + `read_memory`). Prefer **assembly/bytes** when decompiler marks `operator_delete` as noreturn (epilogue always runs; `ADD ESP,4` present in machine).
- **this** = `CVOGHBMoveVehicleToMouse*` in **ECX**; stack flags byte; **`RET 4`**; returns **this\*** in **EAX**.
- **vtbl[0]** of `PTR_FUN_009d5590` only — DATA xref `0x009d5590`. Base MoveVehicle uses a different scalar shell (`0x00636d10`, parent dual R11-024).
- Always runs ToMouse complete/body `FUN_00599570` first (`*this = &PTR_FUN_009d5590`, then chain `FUN_00636b90` / MoveVehicle complete → base). Does **not** free when bit0 clear (stack-lifetime / placement delete path).
- Classic MSVC **scalar** form: only **bit0** tested; no element-count / bit2 vector path.
- Product class sealed by RTTI `.?AVCVOGHBMoveVehicleToMouse@@` @ `0x00af33e0` — **no** `_Inferred` suffix.
- Sibling slots OnHeartBeat `00636cc0` and OnEnd `00636ba0` are **shared** with base MoveVehicle vtbl; only dword0 (this scalar shell) and complete body differ.

## Pseudocode (assembly-complete)

```c
// CVOGHBMoveVehicleToMouse::`scalar deleting destructor'  — vtbl[0]
// thiscall: ECX = this; stack flags; RET 4; EAX = this

void *__thiscall CVOGHBMoveVehicleToMouse_ScalarDeletingDtor(
    CVOGHBMoveVehicleToMouse *this /* ECX */,
    uint8_t flags)
{
  // complete/body: *this = &PTR_FUN_009d5590; chain FUN_00636b90
  FUN_00599570(this);

  if ((flags & 1u) != 0u) {
    operator_delete(this);   // cdecl; machine has ADD ESP,4
  }

  return this;
}
```

## Instruction map (`read_memory` @ `0x005996a0`, 30 B)

```text
56                      push  esi
8B F1                   mov   esi, ecx              ; this
E8 C8 FE FF FF          call  FUN_00599570          ; 0x00599570 ToMouse complete/body
F6 44 24 08 01          test  byte ptr [esp+8], 1   ; flags bit0
74 09                   jz    skip                  ; → 005996b8
56                      push  esi
E8 6D 01 EF FF          call  operator_delete       ; 0x00489822
83 C4 04                add   esp, 4
skip:
8B C6                   mov   eax, esi              ; return this*
5E                      pop   esi
C2 04 00                ret   4
```

Hex: `568bf1e8c8fefffff644240801740956e86d01efff83c4048bc65ec20400`

## Related (context only; not OWN)

| VA | Role |
|---|---|
| `0x00599570` | ToMouse complete/body dtor (restore `PTR_FUN_009d5590` + chain `00636b90`) — residual |
| `0x00636b90` | MoveVehicle complete/body (chained by ToMouse complete) — residual |
| `0x00636d10` | `CVOGHBMoveVehicle_ScalarDeletingDtor` (base dword0 — parent dual R11-024; **not** this) |
| `0x00508630` | `CVOGHBBase_ScalarDeletingDtor` (base family shell — different body) |
| `0x00636ba0` | `CVOGHBMoveVehicle_OnEnd` (shared vtbl+0x10, MEGA-017 dualed) |
| `0x00636cc0` | OnHeartBeat / fire (shared vtbl+0x0c) |
| `0x00599550` | ToMouse ctor — installs `PTR_FUN_009d5590` after base ctor |
| `0x009d5590` | `PTR_FUN_009d5590` — this unit is dword0 |
| `0x009e3b70` | MoveVehicle vtbl — **different** dword0 (`0x00636d10`) |
| `0x00489822` | `operator_delete` |
| `0x00af33e0` | RTTI `.?AVCVOGHBMoveVehicleToMouse@@` |

## Open questions

- Runtime / live CE confirmation of heap free path (`flags&1`).
- Full dual of complete body `FUN_00599570` (not OWN this VA).
- PDB method symbol beyond RTTI class + MSVC scalar-deleting pattern.
