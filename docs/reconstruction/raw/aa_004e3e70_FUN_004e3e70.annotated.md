# Annotated low-level: FUN_004e3e70 → StdTree_EraseAndRebalance_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004e3e70` |
| VA | `0x004e3e70`–`0x004e4126` exclusive (**694 B** / `0x2B6`) |
| Canonical | `StdTree_EraseAndRebalance_Isnil21_Inferred` |
| System | skills-abilities / STL isnil@+0x21 |
| Date | 2026-07-23 scaffold; **R12-013 re-annotate 2026-08-05** |

## Machine-level notes

- Source: raw capture for `aa_004e3e70` + live re-verify.
- Prefer **bytes** when decompiler conflicts (epilogue size-- / outIt / ret 8).
- isnil probe: `CMP byte [node+0x21], 0` at entry after arg load.
- Color: `param_3[8]` / `+0x20` (0 red / 1 black).
- SEH: `LAB_009a1e62`; throw string push `68 f0 52 a1 00`.
- Rotates: `FUN_0050e9f0` L, `FUN_005a27f0` R (parent dual).
- Successor prep: `FUN_004e12c0` (isnil21 nextnode).
- Extremities: `FUN_004cb2c0` min, `FUN_00421a60` max.
- Callers: EraseRange `FUN_004e4e10` / `FUN_004e52a0` only.
- **Do not** use Val12 offsets (+0x18/+0x19) from adjacent `FUN_004e4130`.

## Annotated control flow

```text
FUN_004e3e70 (thiscall map, outIt*, node)  RET 8
  SEH setup LAB_009a1e62
  if node.isnil(+0x21):
    throw "invalid map/set<T> iterator" / DAT_00acc34c   ; no-return
  FUN_004e12c0(out/local)                    ; successor prep
  select replacement child (L/R isnil rules)
  rewire parent / root link
  if head.left  == node: head.left  = min(repl) or parent
  if head.right == node: head.right = max(repl) or parent
  if node.color black:
    while x != root and x.color black:
      sibling cases → recolor; Lrotate 0050e9f0; Rrotate 005a27f0
    x.color = black
  operator_delete(node)
  if map.size > 0: map.size--                 ; BYTES (decomp miss)
  *outIt = successor                          ; BYTES (decomp miss)
  ret 8                                       ; C2 08 00
```

## Pseudocode (annotated copy of raw + epilogue fix)

```c
// Live decompile CF preserved; epilogue restored from read_memory.

void __thiscall StdTree_EraseAndRebalance_Isnil21_Inferred(
    MapShell_Isnil21 *map,      // ECX / param_1
    MapNode_Isnil21 **outIt,    // param_2 — NOT dead
    MapNode_Isnil21 *node)      // param_3
{
  if (node->isnil != 0) {
    // basic_string("invalid map/set<T> iterator"); exception; throw DAT_00acc34c
    _CxxThrowException(...); // no-return
  }

  FUN_004e12c0(/* successor into out channel */);

  // splice replacement (isnil @ +0x21)
  MapNode_Isnil21 *repl = node->left;
  if (repl->isnil == 0) {
    if (node->right->isnil == 0)
      repl = node->right;
  } else {
    repl = node->right;
  }
  MapNode_Isnil21 *parent = node->parent;
  if (repl->isnil == 0)
    repl->parent = parent;

  MapNode_Isnil21 *head = map->head;
  if (head->parent == node)
    head->parent = repl;
  else if (parent->left == node)
    parent->left = repl;
  else
    parent->right = repl;

  if (head->left == node) {
    MapNode_Isnil21 *b = parent;
    if (repl->isnil == 0)
      b = FUN_004cb2c0(repl);
    head->left = b;
  }
  if (head->right == node) {
    if (repl->isnil == 0)
      head->right = FUN_00421a60(repl);
    else
      head->right = parent;
  }

  if (node->color == 1) {
    // standard RB erase fixup; colors at +0x20; rotates 0050e9f0 / 005a27f0
    // (see clean .cpp for full left/right sibling cases)
  }

  operator_delete(node);
  if (map->size > 0)
    map->size--;
  // *outIt = successor (retail epilogue)
  // ret 8
}
```

## R12-013 seal cross-ref

See raw append **R12-013 re-verify (2026-08-05)** for entry/epilogue hex and caller xrefs.
