# Raw capture: FUN_00520520

| Field | Value |
|---|---|
| **Stable ID** | `aa_00520520` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00520520` |
| **Canonical name** | `FUN_00520520` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
/* WARNING: Globals starting with '_' overlap smaller symbols at the same address */

int __thiscall FUN_00520520(int param_1,undefined4 param_2)

{
  double dVar1;
  
  switch(param_2) {
  case 0:
  case 1:
    dVar1 = floor((double)((float)*(byte *)(param_1 + 0x598) * _DAT_009da8a4));
    return (int)ROUND(dVar1);
  case 2:
    dVar1 = floor((double)((float)*(byte *)(param_1 + 0x598) * _DAT_00aaaa3c));
    return (int)ROUND(dVar1);
  case 3:
    dVar1 = floor((double)((float)*(byte *)(param_1 + 0x598) * _DAT_009cefc0));
    return (int)ROUND(dVar1);
  case 4:
    dVar1 = floor((double)((float)*(byte *)(param_1 + 0x598) * _DAT_009cefbc));
    return (int)ROUND(dVar1);
  case 5:
    dVar1 = floor((double)((float)*(byte *)(param_1 + 0x598) * DAT_00a0f710));
    return (int)ROUND(dVar1);
  default:
    return 0;
  }
}
```


---

## R10-033 live re-verify (2026-08-05) — append-only

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **R10-033** |
| **Tools** | Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` + `get_xrefs_to` + `get_function_by_address` |
| **Forbidden** | `disassemble_bytes` (not used); Launcher (not used); parent ledgers |
| **Image** | `autoassault.exe` base `0x400000` |
| **System** | inventory-transfer / reverse-engineer skill capacity |
| **Parent dual** | `0x0052e640` `Character_ComputeReverseEngineerSuccessFraction_Inferred` (MEGA-117) |
| **Terminal** | **false** |

### Live decompile (≡ raw CF)

```c
/* WARNING: Globals starting with '_' overlap smaller symbols at the same address */

int __thiscall FUN_00520520(int param_1,undefined4 param_2)

{
  double dVar1;
  
  switch(param_2) {
  case 0:
  case 1:
    dVar1 = floor((double)((float)*(byte *)(param_1 + 0x598) * _DAT_009da8a4));
    return (int)ROUND(dVar1);
  case 2:
    dVar1 = floor((double)((float)*(byte *)(param_1 + 0x598) * _DAT_00aaaa3c));
    return (int)ROUND(dVar1);
  case 3:
    dVar1 = floor((double)((float)*(byte *)(param_1 + 0x598) * _DAT_009cefc0));
    return (int)ROUND(dVar1);
  case 4:
    dVar1 = floor((double)((float)*(byte *)(param_1 + 0x598) * _DAT_009cefbc));
    return (int)ROUND(dVar1);
  case 5:
    dVar1 = floor((double)((float)*(byte *)(param_1 + 0x598) * DAT_00a0f710));
    return (int)ROUND(dVar1);
  default:
    return 0;
  }
}
```

**Verdict vs 2026-07-23 raw body:** **identical** control flow, offsets, and DAT symbols. No body wipe.

### Body / ABI (sealed via `disassemble_function` + `get_function_by_address`)

| Item | Value |
|---|---|
| Entry | `0x00520520` |
| Last insn | `0x005206a8` `RET 4` (`C2 04 00`) — default path |
| Body inclusive | `0x00520520`–`0x005206aa` (**395 B** / `0x18b`) |
| NOP pad | `0x005206ab` (`90`) |
| Jump table data | `0x005206ac`–`0x005206c3` (6× dword) |
| analyze Body field | `00520520 - 005206aa` |
| Frame | `PUSH EBP; MOV EBP,ESP; AND ESP,-8; SUB ESP,8` |
| Tier arg | `[EBP+8]` = stack arg0 (`CMP EAX,5` / `JA default` / `JMP [EAX*4+0x5206ac]`) |
| Skill load | `MOVZX …, byte ptr [ECX+0x598]` on every case path |
| Floor | `CALL dword ptr [0x009c6598]` (IAT → `floor`) |
| Int convert | `FNSTCW` / `OR AH,0xC` (RC=chop) / `FISTP` — Ghidra `ROUND` macro after `floor` |
| Cleanup | **all exits** `MOV ESP,EBP; POP EBP; RET 0x4` |
| Return | **EAX** = non-negative int capacity (default **0**) |

`analyze_function_complete` signature display `undefined FUN_00520520(void)` is **wrong**; decompiler + locals seal ECX this + Stack[0x4] tier + int EAX. Classification: **worker**. Callees: **`floor`** only.

### Jump table @ `0x005206ac` (`read_memory`)

| index (tier) | target | mult DAT |
|---|---|---|
| 0 | `0x0052053c` | `_DAT_009da8a4` |
| 1 | `0x0052053c` | same as 0 |
| 2 | `0x00520583` | `_DAT_00aaaa3c` |
| 3 | `0x005205cb` | `_DAT_009cefc0` |
| 4 | `0x00520613` | `_DAT_009cefbc` |
| 5 | `0x0052065b` | `DAT_00a0f710` |
| default (>5) | `0x005206a3` | XOR EAX,EAX → 0 |

### Constants (`read_memory` LE float)

| VA | Hex | Float | Tier(s) |
|---|---|---:|---|
| `0x009da8a4` | `00006040` | **3.5** | 0, 1 |
| `0x00aaaa3c` | `33333340` | **2.8** (IEEE `2.8f`) | 2 |
| `0x009cefc0` | `67660640` | **2.1** (IEEE `2.1f`) | 3 |
| `0x009cefbc` | `3333b33f` | **1.4** (IEEE `1.4f`) | 4 |
| `0x00a0f710` | `3333333f` | **0.7** (IEEE `0.7f`) | 5 |

Note: mults = `3.5 × {1.0, 1.0, 0.8, 0.6, 0.4, 0.2}` for tiers 0..5 (pattern observation; not a product name).

### Formula (sealed)

```text
capacity = floor( (float)character[+0x598] * mult[tier] )   // as int in EAX
// tier > 5 or unknown → 0
```

### Callers / xrefs (live)

| Kind | Value |
|---|---|
| Callers | **only** `FUN_0052e640` |
| Xrefs | 2× `UNCONDITIONAL_CALL` @ `0x0052ea2b`, `0x0052ea96` |
| xref_count | **2** |

### Call-site ABI seal (parent `FUN_0052e640` bytes)

**Site A** `0x0052ea2b` (recipe-row budget gate):

```text
MOV EDI, [ESP+0x18]          ; accumulated secondary cost for row
MOV ECX, [ESP+0x20]          ; character*
ADD [ESP+0x1c], EDI
LEA ESI, [EBP+1]             ; EBP = recipe row index uVar6
PUSH ESI                     ; stack tier = row_index + 1
MOV [ESP+EBP*4+0x30], EDI    ; store per-row cost
CALL 0x00520520
CMP EDI, EAX                 ; cost vs capacity
JG  fail                     ; if cost > capacity → success fraction 0
```

**Site B** `0x0052ea96` (per-tier score capacity):

```text
PUSH EDI                     ; EDI = tier loop index 0..5
MOV ECX, [ESP+0x24]          ; character*
CALL 0x00520520
; EAX = capacity[tier]; used as (capacity - need) term in score
```

Both sites: **ECX = character** (same object as parent `+0x598` / `+0x6b4`); one stack dword tier; callee **`RET 4`**.

### Name decision (R10-033)

**`Character_GetReverseEngineerSkillCapacityByTier_Inferred`**

Evidence:

1. Sole caller is dualed RE success-fraction parent `Character_ComputeReverseEngineerSuccessFraction_Inferred`.
2. Reads **character** skill byte `+0x598` (same field parent uses for skill×3.5 primary budget).
3. Returns integer **capacity** compared against recipe material cost / used as capacity[t] in score.
4. Tier argument selects decreasing multipliers 3.5…0.7.
5. No in-body product strings / PDB → **`_Inferred`**.

**Retire:** `Named_CalleeOf_Named_CalleeOf_Skill_You_do_not_possess_the_Tink_00520520` (parent-seed scaffold; wrong domain — tinkering string from unrelated ancestor naming, not this body).

### Not claimed

Runtime Confirmed / bit-exact / differential / Launcher. Product English for skill field `+0x598`. Full dual of parent already owned by MEGA-117 (not re-dualled here).

### Integrity

Raw pseudocode body above the first `---` **unchanged**. This section is append-only re-verify.
