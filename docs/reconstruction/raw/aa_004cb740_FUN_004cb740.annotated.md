# Annotated low-level: FUN_004cb740 → StdTree_EraseAndRebalance_Isnil29_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004cb740` |
| VA | `0x004cb740` |
| Canonical name | `StdTree_EraseAndRebalance_Isnil29_Inferred` |
| System | MSVC `std::_Tree` erase + RB rebalance (isnil@+0x29) |
| Date | 2026-08-04 (WQ9E-E); scaffold 2026-07-23 |
| Tools | decompile + read_memory + analyze_function_complete + xrefs (**no** disassemble_bytes) |

---

## Machine-level notes

- **Body:** `0x004cb740`–`0x004cb9f5` exclusive (**693 B**). Ghidra truncates at delete (false noreturn).
- **ABI:** `__thiscall` ECX=map; stack `Node** outIt`, `Node* node`; **RET 8**.
- **isnil@+0x29**, **color@+0x28**; node size **0x30**.
- **Throw:** `"invalid map/set<T> iterator"` → `_CxxThrowException` / `DAT_00acc34c`.
- **Successor:** `FUN_004cb270` (in-order next, isnil29).
- **Extremities:** min `FUN_0051b5d0`, max `FUN_00421b50` (both dualed isnil29).
- **Rotates:** L `FUN_004192a0`, R `FUN_004192f0` (isnil29 duals).
- **Epilogue (bytes):** `operator_delete` → if size>0 size-- → `*outIt = succ` → `add esp,0x54; ret 8`.
- **Reject:** product "VOG_DEBUG_STOP callee" scaffold as domain name — shared container helper.
- Prefer bytes when decompiler marks delete noreturn or voids out-iterator.

---

## Control flow (annotated)

```
if (node->isnil) throw "invalid map/set<T> iterator";
FUN_004cb270(outIt);                    // successor prep
// splice: choose repl from left/right by isnil@+0x29
// reparent repl; fix head->parent / parent left|right
// if head->left == node: leftmost = min(repl) or parent
// if head->right == node: rightmost = max(repl) or parent
if (node->color == black) {
  // classic RB delete fixup:
  //   red sibling → recolor + L/R rotate
  //   both nieces black → recolor sibling red, climb
  //   near niece red → recolor + rotate toward far
  //   recolor + rotate to terminate
  // paint final x black
}
operator_delete(node);
if (map->size > 0) map->size--;
*outIt = successor;                     // sealed by epilogue bytes
ret 8;
```

---

## Callers / callees

| Role | Symbol |
|---|---|
| Callers | `FUN_004c8610`, `FUN_004c8780`, `FUN_004cbaa0` |
| Succ | `FUN_004cb270` |
| Min / Max | `FUN_0051b5d0`, `FUN_00421b50` |
| L / R rotate | `FUN_004192a0`, `FUN_004192f0` |
| Free | `operator_delete` |

---

## Peer do-not-merge

| Peer | VA | Diff |
|---|---|---|
| `StdTree_EraseAndRebalance_Val12` | `0x004e4130` | isnil@+0x19 / color@+0x18 / Val12 rotates |
| Insert twin | `0x004cbb60` | same isnil29 family, insert path |

---

## Pseudocode source

Authoritative raw body: `docs/reconstruction/raw/aa_004cb740_FUN_004cb740.md`  
Clean: `docs/reconstruction/reconstructed-exact/StdTree_EraseAndRebalance_Isnil29_Inferred.cpp`
