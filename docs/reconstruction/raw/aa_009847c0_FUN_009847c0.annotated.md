# Annotated low-level: FUN_009847c0

| Field | Value |
|---|---|
| Stable ID | `aa_009847c0` |
| VA | `0x009847c0` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_009847c0`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
undefined4 __fastcall FUN_009847c0(undefined4 param_1,int param_2)

{
  LPCRITICAL_SECTION lpCriticalSection;
  undefined4 uVar1;
  int local_11c;
  undefined1 local_118 [4];
  LPCRITICAL_SECTION local_114;
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  local_4 = 0xffffffff;
  puStack_8 = &LAB_009af7a1;
  local_c = ExceptionList;
  lpCriticalSection = (LPCRITICAL_SECTION)(param_2 + 0x38);
  ExceptionList = &local_c;
  local_114 = lpCriticalSection;
  if (*(char *)(param_2 + 0x50) != '\0') {
    ExceptionList = &local_c;
    EnterCriticalSection(lpCriticalSection);
  }
  local_4 = 0;
  FUN_00719630();
  FUN_00989e00(local_118,param_1);
  FUN_0044e8c0(&local_11c);
  if (local_11c == *(int *)(param_2 + 8)) {
    if (*(char *)(param_2 + 0x50) != '\0') {
      LeaveCriticalSection(lpCriticalSection);
    }
    uVar1 = 0xffffffff;
  }
  else {
    *(undefined4 *)(local_11c + 0x1c) = DAT_00d1eac0;
    FUN_00469c80();
    if (*(char *)(param_2 + 0x50) != '\0') {
      LeaveCriticalSection(lpCriticalSection);
    }
    uVar1 = 0;
  }
  ExceptionList = local_c;
  return uVar1;
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.

---

## W37-Y annotated update (2026-08-04)

### Purpose

AssPack manager **replace-prep**: given host + name/cookie + path work buffer, look up existing pack entry and mark/clear it for subsequent re-add (null sentinel at entry+0x1C, push into deferred container). Sole callee of AddOrReplace's replace branch.

### Signature (bytes)

```c
// ECX=pathBuf*, EDX=host*, stack nameOrCookie*; RET 4; returns 0 / -1
int AssPackManager_PrepReplaceExistingEntry_Inferred(
    char* pathBuf /*ECX*/,
    AssPackHost* host /*EDX*/,
    const char* nameOrCookie /*stack0*/);
```

### Control flow

1. SEH + optional CS (`host+0x50` / `host+0x38`).
2. Path normalize `FUN_00719630` into pathBuf.
3. ResourceCache name intern/lookup `FUN_00989e00` (`DAT_00d1eac8`).
4. HashMap find `FUN_0044e8c0` on host; miss (`it==host+8`) → −1.
5. Hit: `*(entry+0x1C)=DAT_00d1eac0`; `FUN_00469c80` on `*(entry+0x24)+0x30`; return 0.
6. LeaveCS both paths; SEH restores ExceptionList.

### Resolved

| Item | Resolution |
|---|---|
| ABI | ECX pathBuf, EDX host, stack name, RET 4 |
| CS layout | same as other AssPack workers (+0x38 / +0x50) |
| Miss/hit returns | −1 / 0 |
| Parent | only `AssPackManager_AddOrReplaceFileInPack_Inferred` |
| Name | `AssPackManager_PrepReplaceExistingEntry_Inferred` |

### Still open

- Product English for `nameOrCookie` / entry+0x1C / push container
- Nested `FUN_00469c80` / map node product plates (not OWN)
- Runtime / bit-exact
