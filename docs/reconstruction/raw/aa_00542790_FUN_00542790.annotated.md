# Annotated low-level: FUN_00542790 / FxCache_ApplyWithNfxEnsure

| Field | Value |
|---|---|
| Stable ID | `aa_00542790` |
| VA | `0x00542790` |
| Structural name | `FxCache_ApplyWithNfxEnsure` (INFERRED) |
| Ghidra | `FUN_00542790` |
| System | `client-fx` / skills-abilities |
| Date | 2026-07-29 (W20-I dual; prior scaffold 2026-07-23) |

## Machine-level notes

- **ABI:** `__thiscall`, **`ret 0x1c`** (7 stack dwords). ECX = host with hash @ `+0xf0c`.
- **Body:** `00542790`–`00542a8c` (**764 B**), MSVC SEH.
- **Gate:** `DAT_00d1f050+0x6c` non-zero required (same family as ND resource / special-FX init).
- **Sibling lighter path:** `aa_005408f0` (`FxCache_EnsureMasterAndResolve`) — ensure+resolve only, no vtbl attach / transform fallback.
- **`_nfx.xml`:** appended from `DAT_009ca184..8c` after skill-name materialize; stripped before `FxMaster_LoadMasterFromXML` (which appends again).
- **Entity apply:** `param_7->vtbl[+0x238](event,key,a,b,param_6,param_8,1,fx*)`.
- **Default pose:** `DAT_009d00a8` = eight `0xFF` then zeros (two -1 dwords + two 0) used when actor/source null.
- Prefer assembly for SEH / ret size; decompiler CF matches live 2026-07-29.

## Control-flow outline

```
if *(DAT_00d1f050+0x6c)==0: return 0

snapA = (param_6 ? param_6+0x160 : &DAT_009d00a8)[0..3]
snapB = (param_7 ? param_7+0x160 : &DAT_009d00a8)[0..3]

value = CNDHash_LookupByKey(*(this+0xf0c), key)
if !value: fail_return_0

if value[+0x0a]==0:
    SkillDefMap find key → path = FUN_00540090(def+name)
    append "_nfx.xml"
    path resolve + FUN_004ed310
    if open ok:
        strip "_nfx.xml"; FxMaster_LoadMasterFromXML(base)
        goto READY
    else: cleanup; flag=1
else:
READY:
    FUN_004a04f0 + FUN_004ed310
    if open ok:
        fx = FUN_004a16d0(event, 1, -1)
        if !fx || !param_7: fail_return_0
        param_7->vtbl[+0x238](...)
        goto SUCCESS
    else: cleanup
    FUN_00545d70(&snapA)   // transform fallback path

SUCCESS:
    SEH teardown; return 1
```

## Signature (sealed)

```c
// returns 1 success / 0 fail
uint32_t __thiscall FxCache_ApplyWithNfxEnsure(
    void *host,           // ECX; hash @ +0xf0c
    uint32_t key,         // stack+0x04  skill/fx cache key
    uint32_t eventId,     // stack+0x08  → FUN_004a16d0 / vtbl
    uint32_t argA,        // stack+0x0c
    uint32_t argB,        // stack+0x10
    void *sourceObj,      // stack+0x14  optional; +0x160 transform
    int *targetEntity,    // stack+0x18  optional; vtbl+0x238 apply
    uint32_t argC);       // stack+0x1c
// ret 0x1c
```

## Pseudocode

See raw body (authoritative). Annotated semantics above; names structural only.

## Confidence

| Claim | Level |
|---|---|
| SEH + `ret 0x1c` / 7 stack args | **Confirmed** |
| Gate `DAT_00d1f050+0x6c` | **Confirmed** |
| Hash lookup + `value+0x0a` master gate | **Confirmed** |
| `_nfx.xml` append/strip + LoadMaster | **Confirmed** (string + callees) |
| vtbl `+0x238` apply path | **Confirmed** (decompile) |
| Transform snapshots `+0x160` / default | **Confirmed** |
| Product English / vtbl method name | **Open** |
| Runtime / bit-exact | **Open** |
