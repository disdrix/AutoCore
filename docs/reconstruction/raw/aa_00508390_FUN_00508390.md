# Raw capture: FUN_00508390

| Field | Value |
|---|---|
| **Stable ID** | `aa_00508390` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00508390` |
| **Canonical name** | `FUN_00508390` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00508390(undefined4 *param_1)

{
  *param_1 = &PTR_FUN_009cdab0;
  if ((param_1[6] != 0) && (*(int *)(param_1[6] + 0xb0) != 0)) {
    FUN_005085b0(param_1);
  }
  return;
}
```

---

## Dual A/B re-verify (W21-A, 2026-07-29)

**Tools:** Ghidra `decompile_function` + `read_memory` + `get_function_by_address` + callers. No `disassemble_bytes`. No Launcher.

### Live decompile (re-confirm)

```c
void __fastcall FUN_00508390(undefined4 *param_1)

{
  *param_1 = &PTR_FUN_009cdab0;
  if ((param_1[6] != 0) && (*(int *)(param_1[6] + 0xb0) != 0)) {
    FUN_005085b0(param_1);
  }
  return;
}
```

### Function bounds

| Field | Value |
|---|---|
| Entry | `0x00508390` |
| Body | `0x00508390`–`0x005083af` (**32 bytes** inclusive) |
| Ghidra name | `FUN_00508390` |

### Byte seal (`read_memory` @ `0x00508390`, 32-byte body)

```
hex: 8bc18b481885c9c700b0da9c0074108b89b000000085c9740650e801020000c3

8B C1                 mov  eax, ecx              ; this HB
8B 48 18              mov  ecx, [eax+0x18]       ; pOwnerObject (HB+0x18)
85 C9                 test ecx, ecx
C7 00 B0 DA 9C 00     mov  dword [eax], 0x009CDAB0  ; base vtbl ALWAYS
74 10                 jz   ret_path              ; no owner → skip detach
8B 89 B0 00 00 00     mov  ecx, [ecx+0xB0]       ; listMgr = *(owner+0xB0)
85 C9                 test ecx, ecx
74 06                 jz   ret_path              ; no listMgr → skip detach
50                    push eax                   ; stack arg = HB*
E8 01 02 00 00        call FUN_005085b0          ; ECX=listMgr; → 0x005085b0
C3                    ret                        ; bare ret (no stack args)
```

### Sealed ABI / algorithm

| Claim | Evidence |
|---|---|
| `__thiscall` / ECX = HB | `mov eax,ecx`; no stack args; bare `ret` |
| Always restore base vtbl `PTR_FUN_009cdab0` | `C7 00 B0 DA 9C 00` between tests (unconditional store) |
| Gate detach: owner non-null **and** `*(owner+0xB0)` non-null | dual `test/jz` |
| `FUN_005085b0` this = listMgr, stack = HB* | `push eax; call` with ECX already listMgr (decompiler collapses to `FUN_005085b0(hb)` — machine wins) |
| Does **not** free memory | no `operator_delete` (scalar is sibling `FUN_00508630`) |
| Body 32 B | bounds + hex |

### Named role (sealed)

**`CVOGHBBase_dtor`** — MSVC-style complete object destructor body for `CVOGHBBase`: reinstall base vtbl `0x009cdab0`, optionally detach HB from owner's list manager. Class family product-proven via RTTI elsewhere; this VA has no body string.

### Callers (sample, Ghidra `get_function_callers`)

`FUN_005078c0`, `FUN_00512060` (TargetingLink complete dtor tail), `FUN_00578a60`, `FUN_00606380`, `FUN_0060b3f0`, `FUN_00636b90`, `FUN_006389a0`, `FUN_00642980`, `FUN_00642b00`, `FUN_006504e0`, `FUN_006507b0`, `FUN_00650d20`, plus multiple Unwind@ sites.

### Callees

| VA | Role |
|---|---|
| `0x005085b0` `FUN_005085b0` | list remove-by-key (`__thiscall` listMgr, stack HB*) |

### Related (not owned)

| VA | Role |
|---|---|
| `0x00508200` | `CVOGHBBase_ctor` (installs same vtbl) |
| `0x005083b0` | `CVOGHBBase_AttachOwnerObject` (uses same +0x18 / +0xB0 gates) |
| `0x00508630` | scalar deleting dtor (vtbl+detach + optional `operator_delete`) |
| `0x00512060` | TargetingLink complete dtor → tails into this |
