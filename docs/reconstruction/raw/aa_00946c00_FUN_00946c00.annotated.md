# Annotated low-level: FUN_00946c00 / Client_DispatchUiEventQueue

| Field | Value |
|---|---|
| Stable ID | `aa_00946c00` |
| VA | `0x00946c00` |
| Body | `00946c00`–`009483e6` (~0x17E6 B) |
| Canonical | `Client_DispatchUiEventQueue` (structural) |
| System | `missions-progression` / client UI tick |
| Date | 2026-07-29 (W19-C dual seal) |

## Machine-level notes

- **ABI:** `__fastcall` — **ECX = client\***; void. Large SEH + `__chkstk` / alloca frame.
- **Sole caller:** `FUN_0094b520` (client frame path) immediately after `FUN_00944770`.
- **Queue:** `*(client+0xe04)+0xe8bc` … `+0xe8c0`, record stride **`0x138`** (`float` walker steps `+0x4e` dwords).
- **Type field:** `*(int*)(entry + 0x1c)` — decompiler `switch(pfVar15[7])` with **denormal float case labels** = integer type ids (0,1,2,3,4,8,10,…,55).
- **Gate A:** process full switch walk when `client+0xf40` is null **or** `*f40 != f38` **or** `*(f38+0xc78)==0`.
- **Gate B (else):** linear scan for type **`0x1e`** only → `FUN_009430f0`.
- Prefer assembly when decompiler float cases conflict — treat cases as **int type ids**.

## Structural control flow

```text
client = ECX
session = *(client + 0xe04)
if !session || !queue_begin || (end-begin)/0x138 == 0: return

FUN_007a69d0()   // not OWN — prep/locale?

if (f40 == null || *f40 != f38 || *(f38+0xc78) == 0):
    for entry in [begin, end) step 0x138:
        switch (entry.type /* +0x1c */):
            case 0,1:    UI helper + optional SetHandbrake/Longitudinal
            case 2:      SendSectorPacket 0x207a + matrix copy + handbrake
            case 3:      interact/vendor sound ("b_m_" / biomek) + clear +0xd28
            case 4:      Client_RefreshMissionRelatedUi (FUN_0092ce90)
            case 8:      mission dialog chrome + first-time tips + flags
            case 10:     mission chrome + DAT_00af92d8 = *entry
            case 30:     FUN_009430f0
            case 50:     Client_SendRespawnInSector
            ... dozens more UI/mission/vehicle/toast arms ...
        end switch
    FUN_00406840()  // drain/clear
else:
    scan for type 0x1e → FUN_009430f0 once
return
```

## Event record layout (partial, sealed from uses)

| Offset | Access | Role |
|---|---|---|
| `+0x00` | `*pfVar15` | payload dword / float (toasts, flags, -NAN sentinel) |
| `+0x04` / `+0x08` | `pfVar15[1..2]` | optional numeric toast fields |
| `+0x1c` | `pfVar15[7]` / `*(int*)(entry+0x1c)` | **event type** |
| `+0x20` / `+0x24` | `pfVar15[8..9]` | compare vs vehicle entity id pair `@+0x164/+0x168` |
| `+0x28` | `pfVar15+10` as `TFID_16` | object resolve |
| `+0x38` | `pfVar15+0xe` as `char*` | string payload (":" split, copy to stack) |

## Notable case arms (type id ≈ denormal index)

| Type | Behavior (summary) |
|---|---|
| 0, 1, 46 | `FUN_007fef20` + optional vehicle handbrake/longitudinal zero |
| 2 | Sector pkt `0x207a` + optional matrix → widget `+0x510` |
| 3 | Vendor interact sound `biomek_male_vendor_2` if name has `b_m_`; clear `f38+0xd28` |
| 4 | `FUN_0092ce90` mission-related UI refresh |
| 8 | Mission dialog chrome + first-time tip flags on `DAT_00d17920` |
| 10 | Mission chrome; store `*entry` → `DAT_00af92d8` |
| 19 | "Working.." progress UI |
| 32 | Toast `"%s %s!"` path + optional UI |
| 33–35 | Set `DAT_00d17928/29/2a` + mission chrome (local-player TFID match) |
| 47 | Optional garage/UI + sector pkt `0x20bf` |
| 50 | `Client_SendRespawnInSector` |

## Open questions

- Product enum names for each type id.
- Exact identity of `client+0xe04` session object and queue owner field names.
- Full product name of this dispatcher (PDB unknown).
- Duals of non-OWN callees (`FUN_007fef20`, `FUN_00406840`, …).

## Pseudocode

See raw `aa_00946c00_FUN_00946c00.md` authoritative body (live ≡ 2026-07-23).
