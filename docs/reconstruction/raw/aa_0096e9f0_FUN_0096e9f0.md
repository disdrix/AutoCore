# Raw capture: FUN_0096e9f0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0096e9f0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0096e9f0` |
| **Canonical name** | `FUN_0096e9f0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 * FUN_0096e9f0(undefined4 param_1)

{
  int iVar1;
  undefined4 *puVar2;
  
  iVar1 = FUN_0043f120();
  if (iVar1 == 0) {
    puVar2 = (undefined4 *)0x0;
  }
  else {
    puVar2 = (undefined4 *)FUN_0096bbb0();
  }
  if (puVar2 != (undefined4 *)0x0) {
    iVar1 = FUN_0096cad0(param_1);
    if (iVar1 < 0) {
      (**(code **)*puVar2)();
      return (undefined4 *)0x0;
    }
  }
  return puVar2;
}
```

---

## W38-O re-verify (2026-08-04) — append only

**Tools:** `batch_decompile` + `decompile_function` + `disassemble_function` (listing only; **not** `disassemble_bytes`) + `read_memory` + `analyze_function_complete` + xrefs/callers + callee decompiles + caller-site `read_memory` (`0096f0e0`, `0096ee00`).

| Field | Live |
|---|---|
| Body | `0x0096e9f0`–`0x0096ea5a` exclusive (**106 B** / `0x6A`) |
| Final | `RET 0x14` (`C2 14 00`) @ `0x0096ea58`; pad `CC` |
| Classification | worker (factory) |
| Callers / xrefs | `FUN_0096f0e0` @ `0x0096f102`; second site @ `0x0096ee3e` (code not in named Ghidra function) |
| Callees | `FUN_0043f120` (freelist pop @ `DAT_00d21930`), `FUN_0096bbb0` (ctor), `FUN_0096cad0` (init / CreateTexture path) |

### Live decompile (2026-08-04)

CF skeleton ≡ 2026-07-23 raw. **Formals under-declared** (one stack param; omits EBX dims\* and remaining stack args).

### Full body hex (106 B)

```
5657bf3019d200e82407adff85c07409e8abd1ffff8bf0eb0233f685f674458b44241c8b4c24188b54241050518b0b5283ec088bc489088b53048950048b44242089642430508b44242c8bcee88fe0ffff85c07d0f8b166a018bceff1233c05f5ec214005f8bc65ec21400
```

### Bytes-sealed ABI / CF

```
PUSH ESI; PUSH EDI
MOV EDI,0x00d21930          ; freelist head global
CALL FUN_0043f120           ; pop node → EAX (0 on fail)
TEST EAX; JZ null_ctor
CALL FUN_0096bbb0           ; in-place ctor on EAX
MOV ESI,EAX; JMP have
null_ctor: XOR ESI,ESI
have: TEST ESI; JZ ret_esi
; pack args for FUN_0096cad0 (thiscall ECX=ESI):
;   copy *EBX / *(EBX+4) (width/height) onto stack local
;   forward five stack formals (a0..a4) with dims embedded
CALL FUN_0096cad0
TEST EAX; JGE ret_esi
MOV EDX,[ESI]; PUSH 1; MOV ECX,ESI; CALL [EDX]   ; vtbl[0](this,1) complete dtor
XOR EAX,EAX; POP EDI; POP ESI; RET 0x14
ret_esi: POP EDI; MOV EAX,ESI; POP ESI; RET 0x14
```

**ABI:** **EBX = const uint32_t dims[2]\*** (width, height); **5 stack dwords**; **`RET 0x14`**; returns **object\*** or **null**.  
**Caller `0096f0e0`:** pushes `(p0,p2,p3,p4,p5)` and leaves **EBX=p1** (dims\*); p1 dead in parent after load.  
**Plates (callee `0096cad0`):** `…\Effects\effTextureImpl.cpp` CreateTexture / power-of-2 width-height.
