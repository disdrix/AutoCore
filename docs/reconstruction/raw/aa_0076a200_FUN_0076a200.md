# Raw capture: FUN_0076a200

| Field | Value |
|---|---|
| **Stable ID** | `aa_0076a200` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0076a200` |
| **Canonical name** | `FUN_0076a200` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __fastcall FUN_0076a200(int param_1)

{
  int iVar1;
  undefined4 uVar2;
  undefined4 unaff_EBX;
  undefined1 local_2c [4];
  basic_string<char,struct_std::char_traits<char>,class_std::allocator<char>_> local_28 [28];
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  local_4 = 0xffffffff;
  puStack_8 = &LAB_009b0b25;
  local_c = ExceptionList;
  ExceptionList = &local_c;
  iVar1 = FUN_00769b70(unaff_EBX);
  if (-1 < iVar1) {
    ExceptionList = local_c;
    return 0;
  }
  iVar1 = FUN_00436250();
  local_4 = 0;
  if (*(uint *)(iVar1 + 0x18) < 0x10) {
    iVar1 = iVar1 + 4;
  }
  else {
    iVar1 = *(int *)(iVar1 + 4);
  }
  if (*(uint *)(param_1 + 0x18) < 0x10) {
    param_1 = param_1 + 4;
  }
  else {
    param_1 = *(int *)(param_1 + 4);
  }
  uVar2 = FUN_0076cec0(local_2c,"Expected string but got \"%s\" in chunk \"%s\"",param_1,iVar1);
  vog_LogMessage("C:\\vog\\1_code\\palantir\\arda2\\storage\\stoChunk.cpp",0x44a,3,uVar2);
  local_4 = 0xffffffff;
  std::basic_string<char,struct_std::char_traits<char>,class_std::allocator<char>_>::
  ~basic_string<char,struct_std::char_traits<char>,class_std::allocator<char>_>(local_28);
  ExceptionList = local_c;
  return 0xffffffff;
}
```

---

## W38-J live seal (2026-08-04)

| Field | Value |
|---|---|
| **Tools** | `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + `get_function_by_address` + xrefs + `get_assembly_context`. **No** `disassemble_bytes` |
| **Agent** | W38-J OWN-ONLY |
| **Live ≡ scaffold raw CF** | **Yes** (ReadQuotedTag → success 0 / format Expected string → −1) |
| **Body** | `0x0076a200`–`0x0076a2b5` (Ghidra bounds; **181 B** / `0xB5`); pad `CC` then OpenChunk `0x0076a2c0` |
| **Epilogue** | **`C3`** (plain `ret`, no stack pop) on success and fail |
| **Canonical** | `stoChunkReader_TextReadString` (**Inferred**) |

### ABI (bytes + call sites)

| Fact | Evidence |
|---|---|
| **ECX** = `std::string* out` | Entry `mov edi,ecx` then `FUN_00769b70` (ReadQuotedTag EDI=out) |
| **EBX** = `stoChunkReader*` | `push ebx` before call ReadQuotedTag; call sites `mov ebx, reader` |
| **No stack args on this leaf** | `C3`; dispatcher `0x00439a20` owns stack reader + `ret 4` |
| Return | **0** if ReadQuotedTag ≥ 0; **`0xffffffff`** else |

### Prologue bytes

```
64 A1 …                ; SEH
6A FF 68 25 0B 9B 00   ; push -1 / handler LAB_009b0b25
… 83 EC 20             ; sub esp, 0x20
57 8B F9               ; push edi / mov edi, ecx   ; out → EDI
53                     ; push ebx                  ; reader on stack for callee
E8 …                   ; call FUN_00769b70         ; stoChunkReader_ReadQuotedTag (W25-S)
85 C0 7C 12            ; test eax / jl fail
33 C0 … C3             ; xor eax,eax / restore / ret 0
```

### Fail path

```
FUN_00436250()         ; chunk name string*
c_str(out) + c_str(chunkName)
FUN_0076cec0(..., "Expected string but got \"%s\" in chunk \"%s\"", ...)
vog_LogMessage(stoChunk.cpp, 0x44a, 3, msg)   ; line 1098
return 0xffffffff
```

### Error string (image)

- `"Expected string but got \"%s\" in chunk \"%s\""` @ `0x00a9d22c`
