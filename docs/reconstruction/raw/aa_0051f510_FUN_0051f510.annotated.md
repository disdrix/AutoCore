# Annotated low-level: FUN_0051f510 / Mission_ComputeObjectiveXpAsFloat

| Field | Value |
|---|---|
| Stable ID | `aa_0051f510` |
| VA | `0x0051f510` |
| Body | `0x0051f510`–`0x0051f539` (42 B) |
| System | missions-progression |
| Date | 2026-07-29 (W25-I dual seal) |
| Ghidra | `FUN_0051f510` |
| Canonical (Inferred) | `Mission_ComputeObjectiveXpAsFloat` |

## Machine-level notes

- **ABI:** stack `void* pObjective` at `[esp+4]`; **`ret 4`**. Return **x87 ST0** (`float10` / long double decompiler type for FPU return).
- **Not** pure `__fastcall` on entry: formal is stack; body moves it into **ECX** for the `__fastcall` callee `Mission_ComputeObjectiveXp`.
- **Guards:** null objective → `0.0f`; null mission def at `obj+0x14c` → `0.0f` without calling the calculator.
- **Success path:** call `Mission_ComputeObjectiveXp` (int in **EAX**) → spill to `[esp+4]` → **`fild`** → ST0.
- **Fail path:** **`fld dword [g_flZero]`** @ `0x00a0f518` (`00 00 00 00` = 0.0f).
- **Presentation only:** sole caller is mission reward chat toast; does **not** apply `g_flMissionXpRoundBias` (`0x00aaa6d0`) and does **not** call AddExperience. Grant path uses the int calculator + bias directly.
- Prefer these bytes over any future decompiler that rewrites FPU return as integer.

## Pseudocode (annotated)

```c
// float Mission_ComputeObjectiveXpAsFloat(void* pObjective /* stack, ret 4 */)
// ST0 return
float10 Mission_ComputeObjectiveXpAsFloat(void *pObjective)
{
  int xp;

  if (pObjective != NULL && *(int *)((char *)pObjective + 0x14c) != 0) {
    // ECX = pObjective for __fastcall Mission_ComputeObjectiveXp
    xp = Mission_ComputeObjectiveXp(pObjective);  // 0x0059dde0
    return (float10)xp;  // fild of EAX
  }
  return (float10)g_flZero;  // 0.0f @ 0x00a0f518
}
```

## Offsets / globals

| Symbol | VA / offset | Role |
|---|---|---|
| `obj+0x14c` | +0x14c | mission def* (null → 0.0f) |
| `Mission_ComputeObjectiveXp` | `0x0059dde0` | int XP calculator |
| `g_flZero` | `0x00a0f518` | 0.0f fail return |

## Open / residual

- Product/PDB symbol string for this thin wrapper (Inferred name only).
- Runtime / bit-exact differential open.
- Nested calculator map/table payloads owned by `aa_0059dde0`, not this VA.
