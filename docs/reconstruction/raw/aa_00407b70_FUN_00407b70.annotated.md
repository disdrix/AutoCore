# Annotated low-level: StdMap_EraseRange_Isnil131_DestroyStr0C_Inferred (FUN_00407b70)

| Field | Value |
|---|---|
| Stable ID | `aa_00407b70` |
| VA | `0x00407b70`–`0x00407bea` exclusive (**122 B** / `0x7A`) |
| Canonical name | `StdMap_EraseRange_Isnil131_DestroyStr0C_Inferred` |
| Ghidra name | `FUN_00407b70` |
| System | MSVC `std::_Tree` range erase (isnil@+0x131 string-value family) |
| Date | 2026-08-05 (WQ9K-B OWN dual) |

## Machine-level notes

- **Register-this:** map lives in **ESI** (callers leave it there). Not ECX `__thiscall`.
- Stack formals (after entry pushes): **outIt**, **first**, **last** — **RET 0x0c**.
- Full-clear gate: `first == head->left && last == head`.
- Free: dualed `StdTree_FreeSubtree_Isnil131_DestroyStr0C_Inferred` (`0x00409920`) with `ECX=map`, stack `head->parent`.
- Head reset: parent/left/right self-link; **size@+8 = 0**; `*out = *head` (begin after clear).
- Partial: successor `FUN_00404000` (isnil@+0x131) then single erase `FUN_00408ad0` (string@+0x0c destroy + rebalance).
- Prefer bytes for ABI (decompiler reports `unaff_ESI` / void return type).

## Pseudocode (annotated)

```c
// ESI = Map*; stack (out, first, last); RET 0x0c
Node** StdMap_EraseRange_Isnil131_DestroyStr0C_Inferred(
    /*ESI*/ Map* map, Node** out, Node* first, Node* last)
{
  Node* head = map->Myhead;                 // [ESI+4]
  if (first == head->left && last == head) {
    FreeSubtree_Isnil131(map, head->parent); // CALL 00409920
    head->parent = head;
    map->size = 0;                          // [ESI+8]
    head->left = head;
    head->right = head;
    *out = head->left;
    return out;
  }
  while (first != last) {
    Node* cur = first;
    Tree_Successor_Isnil131(&first);        // CALL 00404000
    EraseNode_Isnil131(map, &first, cur);   // CALL 00408ad0
  }
  *out = first;
  return out;
}
```

## Sealed offsets

| Field | Offset | Evidence |
|---|---|---|
| Myhead | map+4 | `MOV ECX,[ESI+4]` |
| size | map+8 | `MOV [ESI+8],0` |
| left / parent / right | node+0 / +4 / +8 | head self-link stores |
| isnil (family) | node+0x131 | free + successor + erase callees |
| string value | node+0x0c | erase/free DestroyStr0C chain |

## Open questions

- Product map `value_type` (large node through isnil@+0x131).
- Dual residual helpers `00404000` / `00408ad0`.
- Runtime / bit-exact / differential.
