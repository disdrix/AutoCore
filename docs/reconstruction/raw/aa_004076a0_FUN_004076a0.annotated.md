# Annotated low-level: LogicUiEventQueue_InsertN

| Field | Value |
|---|---|
| Stable ID | `aa_004076a0` |
| VA | `0x004076a0`–`0x00407991` (754 B) |
| Canonical name | `LogicUiEventQueue_InsertN` |
| Ghidra name | `FUN_004076a0` |
| System | client-logic-ui / event queue |
| Date | 2026-07-29 (W23-B dual seal) |

## Machine-level notes

- MSVC-style `vector::_Insert_n` specialization for **element stride 0x138** (Logic-UI event record).
- Vector shell: `begin@+4`, `end@+8`, `cap_end@+0xC` (same layout as sealed `LogicUiEventQueue_InsertOne_GrowPath` / toast / floater vectors).
- Value is POD-copied into stack buffer `local_164[312]` via `FUN_00408880` before mutation (alias-safe insert).
- Growth policy: prefer **1.5×** (`cap + (cap>>1)`), else exact `size+count` via `FUN_004059c0`; max elements `0x00D20D20` (`&DAT_00d20d20`); overflow → `FUN_00418130` (std length_error / too long).
- Stride math uses magic mul `0xD20D20D3` (`/ 0x138`) and `lea` ×0x138 chains (`×8 + ×2` fold).
- SEH frame (`LAB_009be300`); state 0 during realloc construct, state 2 near-end fill, −1 otherwise.
- Callers: grow-insert-one wrapper `FUN_004068f0`; resize-append path `FUN_00406840`.
- Nested helpers residual (not owned): move range `FUN_00409a70`, fill-N `FUN_00406970`, patch `FUN_00409a90`, mid-shift `FUN_00409ac0`, construct `FUN_00408880`.

## Signature (sealed)

```c
// __thiscall RET 0xC
void LogicUiEventQueue_InsertN(
    void* queue,           // ECX; begin@+4 end@+8 cap@end@+0xC
    void* insertAt,       // Stack[0x4]  iterator into buffer
    uint32_t count,       // Stack[0x8]
    const void* valueSrc  // Stack[0xC]  POD source (copied 0x138 via FUN_00408880)
);
```

## Pseudocode (annotated, CF ≡ raw)

```c
void __thiscall LogicUiEventQueue_InsertN(int queue, int insertAt, uint count, uint32_t valueSrc)
{
  uint8_t valueBuf[0x138];   // local_164
  uint capacity;
  int size;
  void *newBuf;
  // SEH setup...

  FUN_00408880(/*dest*/ valueBuf, /*src*/ valueSrc);  // POD construct 0x138

  if (*(int *)(queue + 4) == 0) capacity = 0;
  else capacity = (*(int *)(queue + 0xC) - *(int *)(queue + 4)) / 0x138;

  if (count == 0) { /* restore SEH */ return; }

  size = begin ? (end - begin) / 0x138 : 0;
  if ((0x00D20D20 - size) < count) {
    FUN_00418130();  // vector too long
  }

  size = begin ? (end - begin) / 0x138 : 0;
  if (capacity < size + count) {
    // --- realloc path ---
    // grow: capacity + capacity/2 if no overflow vs max, else 0
    // if still < size+count: capacity = FUN_004059c0() + count  (exact need)
    // newBuf = operator_new(capacity * 0x138)
    // move [begin, insertAt) → newBuf
    // fill count copies of valueBuf
    // move [insertAt, end) after fill
    // operator_delete(old begin) if non-null
    // rebind begin/end/cap_end
    return;
  }

  // --- in-place ---
  int endPtr = *(int *)(queue + 8);
  int tailElems = (endPtr - insertAt) / 0x138;
  if ((uint)tailElems < count) {
    // near-end: shift tail out, fill remainder at old end, end+=count*0x138, patch mid
    return;
  }
  // mid: back-shift last `count` elems past end, shift [insertAt, end-count) right, fill hole
}
```

## Open questions / residual

- Full 0x138 event field layout (consumer dual residual).
- Product English for owning queue type beyond Logic-UI linkage.
- Exact semantics of nested move/fill helpers (owned elsewhere).
- Runtime / bit-exact.
