# Annotated low-level: UI_CraftHost_CreateChildWidgets_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_008e6b50` |
| VA | `0x008e6b50`–`0x008e811f` inclusive (**5584 B** / `0x15D0`) |
| Ghidra name | `FUN_008e6b50` |
| Canonical name | `UI_CraftHost_CreateChildWidgets_Inferred` |
| System | inventory-transfer — craft 2d host CreateChildWidgets* |
| Date | 2026-08-05 (R11-036 dual seal) |
| Authority | `disassemble_function` + `read_memory` > decompiler |

## Machine-level notes

- **thiscall** ECX=host (`MOV EBP,ECX`); no stack args; void; **`RET`** (`C3`).
- Virtual method: sole xref is **DATA** vtbl slot `0x00a3aca8`.
- SEH: `LAB_009ba671`.
- Opens with residual host prep `FUN_00792600(host)`.
- Mass-allocates / skins / attaches craft 2d chrome (`i_d_craft_2d_*` XML family).
- Material slots: 5-iter loop, first column lands at **host+0x568..+0x578** (same bank cleared by dualed `UI_CraftHost_BindResolvedObject_Inferred`).
- Geo pane: `new(0x4FC)` + dualed `CNDUIWndBuffered_Ctor_Inferred` -> **host+0x520** (same slot parent bind tests for orphan chrome).
- Selection widgets **+0x530 / +0x54c** match parent show/hide paths.
- Tail rebinds via dualed parent `FUN_008e5990(host, host+0x7cc)` then writes capability byte **host+0x7e2** from residual `FUN_0052eb90(DAT_00d1b6d8, object)`.
- Decompiler unaff_* / string-as-vtbl artifacts **rejected** (assembly authority).

## Signature (sealed)

```c
// ECX = craft/UI host*; no stack args; void; RET 0
void __thiscall UI_CraftHost_CreateChildWidgets_Inferred(void *host);
```

## Control flow (authority)

```text
SEH prolog; host = ECX
FUN_00792600(host)
// --- chrome stages (simplified) ---
// bg fill/texture + frames  -> +0x50c/+0x514/+0x518/+0x74c ...
// geo buffered window       -> +0x520  (0x4FC / 0078caf0, id 0x9c45)
// item name / disc / diff / success labels
// object background base+enh1..5  -> +0x530..+0x544
// object label base+enh1..5       -> +0x54c..+0x560
// 5x item_component rows          -> +0x568 bank (ESI=+0x5b8, EBX=0..4, ESI+=4)
// craftable list/scroll/buttons / dividers / exp chrome
// tabs: crafting +0x784 (id 0x9c43), disciplines +0x788 (id 0x9c44)
// optional DAT_00d1b8b4 player path + FUN_007fca10
FUN_008e5990(host, *(host+0x7cc))
*(u8*)(host+0x7e2) = FUN_0052eb90(*(DAT_00d1b6d8), *(host+0x7cc))
host->vtbl[+0x34c]()
SEH epilogue; RET
```

## Child-build micro-pattern

```text
p = operator_new(sz)
p = p ? Ctor(p[,...]) : 0
host.vtbl[+0xa8](p)
child.vtbl[+0x28](xml)
optional child.vtbl[+0xfc](1, 0.5f)
*(host+slot) = p
// z-order bump uses host+0x68 / +0x70 / +0x74 + vtbl[+0x3f4](1)
```

## Cross-links (evidence only; not OWN)

| VA | Role |
|----|------|
| `0x008e5990` | dualed `UI_CraftHost_BindResolvedObject_Inferred` — called at tail |
| `0x008e5ce0` | dualed TFID bind parent of 008e5990 |
| `0x008e2d60` | dualed recipe panel reset (used inside bind, not here) |
| `0x0078caf0` | dualed `CNDUIWndBuffered_Ctor_Inferred` |
| `0x007b5dd0` | CNDUIWindow base ctor |
| `0x008847f0` | dualed `CDlgTrade2d_CreateChildWidgets` — same role family |

## Open questions

1. Product demangle for craft host class (vtbl `0x00a3aca8` COL/RTTI not sealed here).
2. Full English for every residual helper (`00792600`, `0052eb90`, tab ctor `008230b0`, list ctors).
3. Exact product meaning of ids `0x9c43`/`0x9c44`/`0x9c45` beyond sealed constants.
4. Per-row secondary component widgets (ESI+0x14/0x28/0x3c/0x50) product labels.
5. Runtime / bit-exact / differential.

## Pseudocode (annotated structural)

```c
void __thiscall UI_CraftHost_CreateChildWidgets_Inferred(void *host)
{
    // SEH LAB_009ba671
    FUN_00792600(host);

    // dozens of new+attach+loadxml -> host slots (see raw slot table)
    // 5-iter component loop writing host+0x568 bank

    // tabs crafting / disciplines
    // optional global player chrome path

    UI_CraftHost_BindResolvedObject_Inferred(host, *(void **)((char*)host + 0x7cc));
    *(unsigned char *)((char*)host + 0x7e2) =
        (unsigned char)FUN_0052eb90(*(void **)0x00d1b6d8,
                                    *(void **)((char*)host + 0x7cc));
    // host->vtbl[+0x34c]()
}
```
