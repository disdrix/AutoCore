# Annotated low-level: FUN_004cb410 → SkillCNDHash_AllocBucketTable_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004cb410` |
| VA | `0x004cb410`–`0x004cb4a0` (**145 B** / `0x91`) |
| System | skills-abilities / skill-family CNDHash |
| Date | 2026-07-23 scaffold; **2026-08-04 WQ9E-A dual seal** |
| Canonical | `SkillCNDHash_AllocBucketTable_Inferred` |

## Machine-level notes

- **ABI:** MSVC **`__thiscall`**; `ECX` = skill CNDHash*; **void** on success; **bare `ret`**. Entry `51 56 8B F1`. Fail path **noreturn** throw.
- **Log2 gate:** byte `@+0x1c` must satisfy **`1 ≤ log2 ≤ 16`**. Bytes: `cmp al,1; jl fail; cmp al,0x10; jg fail` (matches decomp `'\0' < log2 && log2 < '\x11'`).
- **Provisional count:** `N = *(hash+8)` — callers (recreate/ctor) set `N = 1 << log2` **before** call.
- **Alloc shape (parallel inventory `CNDHash_AllocBucketTable_*`):**
  - `operator_new[](N * 4)` → pointer table at `+0x10`
  - `malloc(N * 0xc)` → contiguous sentinel slab
  - each bucket[i] → slab + i*0xc; stamp **`PTR_LAB_009cb970`**, next=0, val=0
- **Mask post-condition:** `*(hash+8) = N - 1` (`add [esi+8], -1`). Insert paths use `key & mask`.
- **Fail:** `FUN_007a4480(0,"VOG_DEBUG_STOP")` then `_CxxThrowException` with code **`0x80004003`** (`E_POINTER`) and `ThrowInfo` `DAT_00acc430`.
- **Callers:**
  1. `SkillCNDHash_Recreate_Inferred` (`0x004cbdc0`) @ `0x004cbe0c` after seeding log2/count/head/tail.
  2. `SkillCNDHash_Ctor_Inferred` (`0x0051dd60`) @ `0x0051ddba` after field init.
- **Family stamp:** skill sentinel **`0x009cb970`** (not medal `0x009ce090` / inventory other stamps). Distinct VA from `CNDHash_AllocBucketTable_Inferred` `0x00413d80`.

## Pseudocode (annotated)

```c
// void __thiscall SkillCNDHash_AllocBucketTable_Inferred(SkillCNDHash *hash)
// success: bare ret; fail: noreturn throw
void __thiscall FUN_004cb410(int hash)
{
  char log2 = *(char *)(hash + 0x1c);
  if (!(log2 > 0 && log2 < 0x11)) {           // 1..16
    FUN_007a4480(0, "VOG_DEBUG_STOP");
    int code = 0x80004003;                    // E_POINTER
    _CxxThrowException(&code, &DAT_00acc430); // noreturn
  }

  int N = *(int *)(hash + 8);                 // provisional COUNT
  void **table = (void **)operator_new__(N * 4);
  *(void ***)(hash + 0x10) = table;

  void *slab = malloc(N * 0xc);
  uint i = 0;
  if (N != 0) {
    do {
      table[i] = (char *)slab + i * 0xc;
      uint32_t *sent = (uint32_t *)table[i];
      i = i + 1;
      *sent = (uint32_t)&PTR_LAB_009cb970;    // sentinel stamp
      sent[1] = 0;                            // head/next
      sent[2] = 0;                            // val/pad
    } while (i < (uint)N);
  }

  *(int *)(hash + 8) = N - 1;                 // COUNT → MASK
}
```

## Field map (writes)

| Off | Before | After success |
|-----|--------|---------------|
| +0x08 | provisional count N | **mask** N−1 |
| +0x10 | (typically null from destroy/ctor) | pointer table |
| +0x1c | log2 (read-only here) | unchanged |

## Open / residual

- Product English for sentinel / class (`_Inferred`).
- Full RTTI/map of `PTR_LAB_009cb970` methods.
- Whether slab allocation failure is handled (no null-check in body — preserve as-is).
- Runtime / bit-exact open. Terminal false.
