# Annotated low-level: FUN_004e3260 / HashTable_LookupObjectByCoid_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004e3260` |
| VA | `0x004e3260` – `0x004e328b` |
| System | object-resolve / id-hash |
| Date | 2026-07-29 (dual W16-I strengthen) |

## Machine-level notes

- **Prefer image over decompiler.** Live Ghidra decompile presents a free-function 2-arg wrapper that calls `FUN_004e23d0(param_1,param_2,&param_1)` and returns `*(node+0xc)`. Image shows **`__thiscall`** on a table object with **bucket hash** before the chain walk.
- ABI: ECX = table; stack `coidLo`, `coidHi`; **`ret 8`**.
- Table layout (this body):
  - `+0x8` — u32 bucket mask (`and edx, eax` with coidLo)
  - `+0x10` — pointer to `void*` bucket array; head = `array[coidLo & mask]`
- Callee `FUN_004e23d0` (`0x004e23d0`, `ret 0xc`): ECX = list head; starts at `*(head+4)`; match `node+0x18`/`+0x1c` to (lo,hi); next `node+0x10`; out-prev at stack arg3; result node in EAX.
- Return payload object pointer at **`node+0xc`** (corroborated by `CVOGReaction_RemoveObject` insert layout).

## Image CF (authoritative)

```
// 004e3260 (read_memory 2026-07-29)
// thiscall ECX=pTable; [esp+4]=coidLo; [esp+8]=coidHi; ret 8
edx = coidHi
push &coidLo_slot          // out-prev for chain walk
eax = coidLo
push coidHi
edx = *(pTable + 8)        // mask
edx &= coidLo              // bucket
push coidLo
eax = *(pTable + 0x10)     // bucket table*
ecx = *(eax + edx*4)       // list head
call FUN_004e23d0          // → node* or 0
test eax, eax
jz  ret_null               // ret 8
eax = *(eax + 0xc)         // object*
ret 8
```

## Decompiler CF (incomplete — keep for raw lineage)

```c
undefined4 FUN_004e3260(undefined4 param_1, undefined4 param_2)
{
  int iVar1 = FUN_004e23d0(param_1, param_2, &param_1);
  if (iVar1 == 0) return 0;
  return *(undefined4 *)(iVar1 + 0xc);
}
```

## Callers (live xrefs)

| Site | Function |
|---|---|
| `004bae95`, `004baea8` | `CVOGReaction_ResolveObjectTarget` (global + local arms) |
| `004c8438` | `FUN_004c82b0` |
| `004e0537`, `004e055e` | `FUN_004dfcc0` |
| `005054a7` | `Vehicle_applyCreatePacket` |

## Open questions

- Product / PDB name of the hash table type and this method.
- Whether freelist/sentinel head at bucket is std-like or custom (walk starts at head+4).
