# Annotated low-level: FUN_005194f0 → Gfx_BindMatDiffuseAmbientEmissive_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005194f0` |
| VA | `0x005194f0`–`0x00519657` (359 B) |
| System | graphics / material FX |
| Date | 2026-08-04 (WQ9R-F dual; supersedes 2026-07-23 scaffold notes) |
| Canonical | `Gfx_BindMatDiffuseAmbientEmissive_Inferred` |

## Machine-level notes

- Source: raw capture for `aa_005194f0` + live re-decompile + Capstone/bytes.
- Prefer assembly when decompiler conflicts (binder this, resize epilogue).
- **ABI:** cdecl `(shaderHost*, colorVec*)` void; call sites `ADD ESP,8`.
- **Shader vector:** `host+0xc0` begin, `+0xc4` end, element size 4.
- **Color vector:** `vec+4` begin, `+8` end, element size `0x10` (float4).
- **Assert:** `shaderN * 3 == colorN` else log `VOG_DEBUG_STOP` (`0x00a15844`) — does not abort loop setup.
- **Binder** `FUN_009700f0`: ECX = `*(entries[i]) + 0x20`; stack pushes color float4* then name string (thiscall cleans).
- **Names:** `MatDiffuse` `0x00a2f8cc`, `MatAmbient` `0x00a2f8c0`, `MatEmissive` `0x00a2f8b4`.
- **Group stride:** `0x30` bytes per shader (3×float4).
- **Epilogue:** build `(0,0,0,1.0)` on stack (`g_flOne` @ `0x00a0f2a0`); `FUN_0051def0(colorVec, 0)` → **clear** color bank.
- **Callers:** `FUN_0051b310` (vtbl+0x1cc → host at result+8; colors at this+0x144); `FUN_004c5510`.
- **Not skill-bank cast** — residual only by WQ-009 address partition.

## Pseudocode (annotated)

```c
// cdecl void (shaderHost*, colorVec*)
void Gfx_BindMatDiffuseAmbientEmissive_Inferred(int host, int colorVec)
{
  int shaderN = host_vec_count(host+0xc0);      // >>2
  int colorN  = color_vec_count(colorVec+4);    // >>4
  if (shaderN * 3 != colorN)
    FUN_007a4480(0, "VOG_DEBUG_STOP");

  for (int i = 0, need = 2, off = 0; i < shaderN && need < colorN; ++i, need += 3, off += 0x30) {
    void* fx = *(void**)(*(int*)(host+0xc0) + 4*i) + 0x20; // bytes: entry+0x20
    float* base = *(float**)(colorVec+4);
    FUN_009700f0(fx, "MatDiffuse",  base + off/4 + 0);   // +0x00
    FUN_009700f0(fx, "MatAmbient",  (char*)base + off + 0x10);
    FUN_009700f0(fx, "MatEmissive", (char*)base + off + 0x20);
  }
  FUN_0051def0(colorVec, /*newCount*/ 0 /*, fill 0,0,0,1 */);
}
```

## Open questions

- Product type of `*(entries[i])` and field at `+0x20`.
- Full dual of `FUN_009700f0` / `FUN_0051def0`.
