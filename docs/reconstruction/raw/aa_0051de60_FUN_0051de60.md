# Raw capture: FUN_0051de60

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051de60` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051de60` |
| **Canonical name** | `FUN_0051de60` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_0051de60(void *param_1,byte param_2)

{
  FUN_0051de80();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```

---

## WQ9F-H re-verify (2026-08-04) — append only

| Field | Value |
|---|---|
| **Wave** | WQ9F-H OWN dual A/B |
| **Partition** | `WAVE_2026-08-04_wq009_depth3_partition_map.md` |
| **Tools** | `batch_decompile` + `analyze_function_complete` + `disassemble_function` + `read_memory` + xrefs. **No** `disassemble_bytes`. |
| **Promoted name** | `SkillCNDHash_ScalarDeletingDtor_Inferred` |
| **Body** | `0x0051de60`–`0x0051de7d` inclusive (**30 B** / `0x1E`); pad `CC CC` then complete dtor `0x0051de80` |

### Full-body hex (`read_memory`)

```
568bf1e818000000f644240801740956e8adb9f6ff83c4048bc65ec20400
```

### Disasm (`disassemble_function`)

```text
0051de60  PUSH ESI
0051de61  MOV ESI,ECX
0051de63  CALL 0x0051de80          ; SkillCNDHash complete dtor (HashError:Destructor)
0051de68  TEST byte ptr [ESP+0x8],0x1
0051de6d  JZ  0x0051de78
0051de6f  PUSH ESI
0051de70  CALL operator_delete
0051de75  ADD ESP,0x4
0051de78  MOV EAX,ESI
0051de7a  POP ESI
0051de7b  RET 0x4
```

### Sealed facts

- MSVC **scalar deleting dtor** (vtbl[0] of `PTR_FUN_009ce1b8` @ DATA `0x009ce1b8` = `0x0051de60`).
- Always runs complete dtor `FUN_0051de80`; if `(flags & 1)` free host; return this; **`ret 4`**.
- Same vtbl family as dualed `SkillCNDHash_Ctor_Inferred` (`0x0051dd60`).
- Ghidra noreturn warning on `operator_delete` is **false** — epilogue present in bytes.
