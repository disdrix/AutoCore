# Raw capture: FUN_005d73a0

| Field | Value |
|---|---|
| **Stable ID** | `aa_005d73a0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005d73a0` |
| **Canonical name** | `FUN_005d73a0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_005d73a0(void)

{
  VehicleEntity_SetSteerInput(0);
  VehicleEntity_SetLongitudinalInput(0);
  VehicleEntity_SetHandbrake(1);
  CVOGHBAIFollowVehicle_FireWeapons(0,0);
  VehicleEntity_PushDriveAxesToController();
  FUN_005cc560();
  return;
}
```

---

## Re-verify append — MEGA-015 OWN-ONLY dual 2026-08-05

**Agent:** MEGA-015  
**Tools:** `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + callers/xrefs + `get_function_by_address` + `analyze_control_flow` + `search_strings` + `batch_decompile` (callees) + ctor `FUN_005d7bb0`. **No** `disassemble_bytes`. No Launcher.  
**Integrity:** scaffold body above preserved; this section append-only.

### Live decompile ≡ scaffold CF

Live Ghidra decompile **≡** 2026-07-23 raw CF:

```text
VehicleEntity_SetSteerInput(0)
→ VehicleEntity_SetLongitudinalInput(0)
→ VehicleEntity_SetHandbrake(1)
→ CVOGHBAIFollowVehicle_FireWeapons(0,0)
→ VehicleEntity_PushDriveAxesToController()
→ FUN_005cc560()   // tail-call
```

### Body / ABI (bytes + disassembly)

| Fact | Value |
|---|---|
| Body | `0x005d73a0`–`0x005d73e7` inclusive (**72 B** / `0x48`) |
| Pad | `CC` fill from `0x005d73e8` |
| Convention | **`__thiscall`**: **ECX** = `CVOGHBAIDriver* this`; **no stack args** |
| Return | void; control leaves via **tail `JMP FUN_005cc560`** (no local `RET`) |
| Classification | **worker** (vtable method; no direct CALL xrefs) |

Entry / body hex (full):

```text
56 8B F1
8B 8E BC 00 00 00  6A 00  E8 70 E2 F1 FF   ; SetSteerInput(0)       @ 004f5620
8B 8E BC 00 00 00  6A 00  E8 93 E2 F1 FF   ; SetLongitudinalInput(0) @ 004f5650
8B 8E BC 00 00 00  6A 01  E8 56 C2 F1 FF   ; SetHandbrake(1)        @ 004f3620
6A 00  6A 00  8B CE  E8 2B FD FF FF         ; FireWeapons(0,0)       @ 005d7100  RET 8
8B 8E BC 00 00 00  E8 30 48 F2 FF           ; PushDriveAxes          @ 004fbc10
8B CE  5E  E9 78 51 FF FF                   ; tail JMP FUN_005cc560  @ 005cc560
```

### Sealed dataflow (assembly)

```text
PUSH ESI
MOV  ESI, ECX                         ; this = CVOGHBAIDriver*
MOV  ECX, [ESI+0xbc]                  ; vehicle entity*
PUSH 0
CALL VehicleEntity_SetSteerInput      ; vehicle+0x618 = 0 (steer)
MOV  ECX, [ESI+0xbc]
PUSH 0
CALL VehicleEntity_SetLongitudinalInput ; vehicle+0x614 = 0 (throttle)
MOV  ECX, [ESI+0xbc]
PUSH 1
CALL VehicleEntity_SetHandbrake       ; vehicle+0x61c = 1 (HB)
PUSH 0
PUSH 0
MOV  ECX, ESI
CALL CVOGHBAIFollowVehicle_FireWeapons ; may-fire=0 → clear weapon mask (RET 8)
MOV  ECX, [ESI+0xbc]
CALL VehicleEntity_PushDriveAxesToController
MOV  ECX, ESI
POP  ESI
JMP  FUN_005cc560                     ; owner-counter cleanup on this
```

### Class / vtable identity

| Symbol | Evidence |
|---|---|
| RTTI | `.?AVCVOGHBAIDriver@@` @ `0x00af8d30` (TypeDescriptor `0x00af8d28`) |
| COL / type link | `0x00ab06bc` → TypeDescriptor `0x00af8d28` |
| Vtable | `PTR_FUN_009db02c` installed by ctor `FUN_005d7bb0` (`MOV dword ptr [ESI],0x009db02c`) |
| This slot | `*[vtbl + 0x10]` = `0x005d73a0` (DATA xref only @ `0x009db03c`) |
| Vehicle field | **`this+0xbc`** → vehicle entity (shared with FireWeapons / Flipper / DoLogic) |

### Callers / xrefs / callees

| Kind | Value |
|---|---|
| Direct CALL callers | **none** (`get_function_callers` empty) |
| Xrefs | **1 DATA** @ `0x009db03c` (vtable slot) |
| Virtual dispatch | only via `CVOGHBAIDriver` vtbl `+0x10` |
| Callees (6) | `VehicleEntity_SetSteerInput` `0x004f5620`; `VehicleEntity_SetLongitudinalInput` `0x004f5650`; `VehicleEntity_SetHandbrake` `0x004f3620`; `CVOGHBAIFollowVehicle_FireWeapons` `0x005d7100` (**RET 8**); `VehicleEntity_PushDriveAxesToController` `0x004fbc10`; `FUN_005cc560` (tail) |

### Name decision

Promote **`CVOGHBAIDriver_AxisPark_Inferred`**.

- Class sealed by RTTI + ctor vtbl install (not invent).
- Role sealed by dualed axis writers + Push + HB=1 + FireWeapons(mayFire=0).
- **No** product scoped string in body → **`_Inferred`**.
- Reject: player `DriveControlTick` park, mission-UI thr/steer park, pure SetSteer-only `00636ba0`, FireWeapons itself, DoLogic.

### Gaps

- Product English method name (no `"CVOGHBAIDriver::…"` string here).
- Inventory of sites that virtual-call `vtbl+0x10`.
- Full dual of residual `FUN_005cc560` (counter-dec cleanup).
- FireWeapons unused second stack dword (ABI is RET 8; only `[EBP+8]` read).
- Runtime / bit-exact / differential (Terminal false).
