# Raw capture: FUN_005749d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_005749d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005749d0` |
| **Canonical name** | `FUN_005749d0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int __thiscall FUN_005749d0(int param_1,int param_2)

{
  int iVar1;
  int iVar2;
  uint3 uVar3;
  byte bVar4;
  uint uVar5;
  
  if (param_2 == 0) {
    return 0;
  }
  iVar1 = *(int *)(*(int *)(param_2 + 4) + 4);
  uVar5 = 0;
  while( true ) {
    iVar2 = uVar5 * 0x10;
    uVar3 = (uint3)((uint)iVar2 >> 8);
    if ((*(int *)(iVar1 + 0x164 + param_2) == *(int *)(iVar2 + 0x198 + param_1)) &&
       (*(int *)(iVar1 + 0x168 + param_2) == *(int *)(iVar2 + 0x19c + param_1))) break;
    bVar4 = (char)uVar5 + 1;
    uVar5 = (uint)bVar4;
    if (3 < bVar4) {
      return (uint)uVar3 << 8;
    }
  }
  return CONCAT31(uVar3,1);
}
```

---

## Re-verify append — WQ9R-A dual (2026-08-04)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `analyze_function_complete` + `read_memory` + callers |
| **Live decompile** | = raw body (CF identical; return-type artifacts noted below) |
| **Body** | `0x005749d0`–`0x00574a2e` exclusive (**94 B** / `0x5E`); pad `CC` |
| **ABI** | `__thiscall`; ECX = crew-table host*; stack `candidate*`; **RET 4**; **AL bool** 0/1 |
| **Callers (8)** | `Skill_FilterTargetForResolveList_Inferred`, `FUN_0058a810`, `FUN_0058cd60`, `FUN_005755b0`, `Client_RecvDestroyObject`, `FUN_007ffef0`, `FUN_0082fb30`, `FUN_00930fc0` |
| **Xrefs** | 14 call sites |
| **Callees** | none (leaf) |
| **Named** | `VehicleCrew_ContainsMember_Inferred` |

### Return-type correction (bytes over decompiler)

Ghidra shows `uint3` / `CONCAT31` / `(uint)uVar3<<8` noise. Live epilogue:

```
; not found after 4 slots:
5F          pop edi
32 C0       xor al,al
5E          pop esi
C2 04 00    ret 4

; match:
5F          pop edi
B0 01       mov al,1
5E          pop esi
C2 04 00    ret 4
```

Null candidate early-out: `xor al,al; ret 4`.

### Semantics (sealed)

1. If `candidate == null` ? return 0.
2. `offBase = *(int*)(*(int*)(candidate+4)+4)` — SharedBase offset table.
3. Load candidate object-id pair: `*(int*)(offBase + candidate + 0x164)` / `+0x168` (COID lo/hi).
4. Scan `this+0x198` for slots `i=0..3`, stride **0x10**:
   - match if `slot[0]==coidLo && slot[4]==coidHi` ? return 1.
5. Else return 0.

### Mode-0xb call site (`FUN_0058a810` @ ~`0x0058aa60`)

```
mov ecx,[edi+0xCB0]   ; vehicle crew-host pointer
test ecx,ecx
jz  reject
push eax              ; candidate vehicle / member object
call FUN_005749d0
test al,al
```

Twin writer: `FUN_005755b0` inserts COID into free `+0x198` slot (invalid sentinel `0xFFFFFFFF` pair).