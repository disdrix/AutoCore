# Raw capture: FUN_00480970

| Field | Value |
|---|---|
| **Stable ID** | `aa_00480970` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00480970` |
| **Canonical name** | `FUN_00480970` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00480970(undefined4 *param_1,undefined4 *param_2,undefined4 *param_3)

{
  undefined4 *puVar1;
  int iVar2;
  undefined4 *puVar3;
  undefined4 *puVar4;
  undefined4 *puVar5;
  
  while (param_1 != param_2) {
    puVar3 = param_1 + 0x32;
    puVar1 = param_3 + 0x32;
    puVar4 = param_1;
    puVar5 = param_3;
    for (iVar2 = 0x32; param_1 = puVar3, param_3 = puVar1, iVar2 != 0; iVar2 = iVar2 + -1) {
      *puVar5 = *puVar4;
      puVar4 = puVar4 + 1;
      puVar5 = puVar5 + 1;
    }
  }
  return;
}
```

## Dual A/B re-verify (2026-07-29 W21-M)

| Field | Value |
|---|---|
| **Agent** | W21-M OWN-ONLY dual seal |
| **Tools** | Ghidra `decompile_function` + `read_memory` + `analyze_function_complete` / callers / assembly_context (no `disassemble_bytes`) |
| **Body range** | `0x00480970`–`0x004809a0` (**49 bytes** inclusive; Ghidra body end at RET) |
| **Live decompile** | Matches authoritative body above (identical CF) |
| **Canonical (INFERRED)** | `Mem_Move200B_Blocks_Inferred` |

### Byte seal (`read_memory` @ `0x00480970`, 49 B body + pad)

```
8B 54 24 04             mov  edx, [esp+4]       ; param1 = src cursor
8B 44 24 0C             mov  eax, [esp+0xC]     ; param3 = dest cursor
53                      push ebx
8B 5C 24 0C             mov  ebx, [esp+0xC]     ; param2 = end exclusive (after push)
3B D3                   cmp  edx, ebx
74 1E                   jz   done
56 57                   push esi / edi
8B F2                   mov  esi, edx           ; ESI = src
8B F8                   mov  edi, eax           ; EDI = dest
81 C2 C8 00 00 00       add  edx, 0xC8          ; src += 200
B9 32 00 00 00          mov  ecx, 0x32          ; 50 dwords
05 C8 00 00 00          add  eax, 0xC8          ; dest += 200
3B D3                   cmp  edx, ebx
F3 A5                   rep  movsd              ; copy 200 B
75 E6                   jnz  loop
5F 5E 5B C3             pop  edi/esi/ebx; ret   ; bare RET (cdecl)
```

Hex (49 B):
`8b5424048b44240c538b5c240c3bd3741e56578bf28bf881c2c8000000b93200000005c80000003bd3f3a575e65f5e5bc3`

### ABI (call-site sealed)

| Slot | Role |
|---|---|
| **Stack[0]** (`param_1`) | `void*` / `uint32_t*` **src begin** (inclusive) |
| **Stack[1]** (`param_2`) | **src end** (exclusive) |
| **Stack[2]** (`param_3`) | **dest** cursor |
| **Return** | void; bare `RET` (**cdecl** — caller cleans) |

Notes:
- Block size **Confirmed**: `add …, 0xC8` + `mov ecx, 0x32` + `rep movsd` ⇒ **200 bytes / 50 dwords** per iteration.
- Some callers push a **4th dummy** dword and `ADD ESP, 0x10` (e.g. erase helpers); body only consumes 3 args.
- Typical erase use: `Mem_Move200B_Blocks(pos+200, end, pos)` then `end -= 200`.

### Call sites

| Site | Parent | Pattern |
|---|---|---|
| `0x0072003a` | `FUN_0071ffc0` (`CSoundManager_EraseNamedRecordByName`) | `ADD EDI,0xC8`; push 4; call; `ADD [ESI+0x48], -200`; `ADD ESP,0x10` |
| `0x00720067` | same | second range erase |
| `0x00480579` | `FUN_00480560` (vector erase-one) | `LEA EDX,[EDI+0xC8]`; end via `[ESI+8]`; `ADD [ESI+8],-200` |
| `0x00721f0e` | `FUN_00721d30` | `ADD ESI,0xC8`; end `[EBX+0x48]`; end-=200 |

### Callees

**None** (leaf). Classification: leaf.

