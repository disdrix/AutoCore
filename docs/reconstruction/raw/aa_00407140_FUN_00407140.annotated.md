# Annotated low-level: StdTree_EraseRange_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00407140` |
| VA | `0x00407140`–`0x004071f4` exclusive (**180 B** / `0xB4`) |
| Ghidra | `FUN_00407140` |
| Canonical | `StdTree_EraseRange_Isnil21_Inferred` |
| System | skills-abilities — MSVC `std::_Tree` range erase (isnil@+0x21) |
| Date | 2026-08-05 (R13-016 OWN dual) |
| Parent dual | `0x00405650` StdTree_EraseAndRebalance_Isnil21_Inferred (R12-009) |

## Machine-level notes

- Live decompile CF ≡ raw; both exits **`C2 0C 00`** (`ret 0xc`).
- Full clear: `first==head->left && last==head` → free-subtree `FUN_00406710(root)` then head reset + size 0.
- Partial: isnil@+0x21 in-order successor (inline), then `StdTree_EraseAndRebalance_Isnil21_Inferred` with **`mov ecx,edi`** (map shell).
- Shape twin of dualed isnil29 range erase `0x004cbaa0` (same 180 B / RET 0xC plate) but **isnil@+0x21** + free `00406710` + erase `00405650` — **do not merge**.

## Pseudocode (annotated)

```c
// __thiscall; ECX = MapShell*; stack outIt*, first, last; RET 0x0C
void StdTree_EraseRange_Isnil21_Inferred(
    MapShell_Isnil21 *map,
    MapNode_Isnil21 **outIt,
    MapNode_Isnil21 *first,
    MapNode_Isnil21 *last)
{
  MapNode_Isnil21 *head = map->head; // +0x04

  // Full clear [begin, end) == [head->left, head]
  if (first == head->left && last == head) {
    FUN_00406710(head->parent); // free non-nil subtree (isnil@+0x21)
    head->parent = head;
    map->size = 0;              // +0x08
    head->left = head;
    head->right = head;
    *outIt = head->left;
    return; // ret 0x0c
  }

  while (first != last) {
    MapNode_Isnil21 *succ = first;
    if (first->isnil == 0) {
      // MSVC tree successor (isnil@+0x21)
      succ = first->right;
      if (succ->isnil == 0) {
        MapNode_Isnil21 *l = succ->left;
        while (l->isnil == 0) { succ = l; l = l->left; }
      } else {
        MapNode_Isnil21 *p = first->parent;
        MapNode_Isnil21 *cur = first;
        while (p->isnil == 0 && cur == p->right) {
          cur = p; p = p->parent;
        }
        succ = p;
      }
    }
    // bytes @ 0x004071d7: push node; lea out; push out; mov ecx,map; call 00405650
    StdTree_EraseAndRebalance_Isnil21_Inferred(map, &first, first);
    first = succ;
  }
  *outIt = first;
  return; // ret 0x0c
}
```

## Gaps

1. Product demangle of map value_type / host English at call sites.
2. Full free-subtree algebra of residual `FUN_00406710` (left-recurse decomp residual).
3. Runtime / bit-exact / differential.
