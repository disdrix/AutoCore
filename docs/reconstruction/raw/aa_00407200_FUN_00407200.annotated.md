# Annotated low-level: FUN_00407200

| Field | Value |
|---|---|
| Stable ID | `aa_00407200` |
| VA | `0x00407200` |
| System | STL map/set always-insert + RB rebalance (isnil@+0x21) |
| Date | 2026-08-05 (WQ9I-B OWN dual) |

## Machine-level notes

- Source: raw capture for `aa_00407200` + live re-verify.
- Prefer assembly when decompiler mislabels rotate thiscall (ECX=map still set at call).
- Color field is **byte @ node+0x20** (decomp `piVar5[8]` = dword index 8 → +0x20).
- isnil is **@+0x21** (set 0 in dualed buynode `00408990`; not written here).
- Peer CF of dualed isnil29 always-insert `00406c40` with different max-size, buynode, color offset, rotates.

## Pseudocode (annotated)

```c
// __thiscall; RET 0x10
void StdTree_InsertAndRebalance_Isnil21_Inferred(
    Map* map,            // ECX
    Node** out,          // stack
    char addLeft,        // 1 = attach as left child of where
    Node* where,         // parent for new node
    const void* value)
{
  // SEH frame LAB_009bc919
  if (map->size >= 0xFFFFFFEu) {
    // length_error("map/set<T> too long") — non-returning
  }

  Node* head = map->head;
  // buynode Val16 / color red / isnil0 / size 0x28
  Node* n = StdTree_Buynode_Val16_Isnil21(head, where, head, value, /*red*/ 0);
  map->size += 1;

  if (where == head) {
    head->parent = n;
    head->left = n;
    head->right = n;
  } else if (addLeft == 0) {
    where->right = n;
    if (where == head->right) head->right = n;
  } else {
    where->left = n;
    if (where == head->left) head->left = n;
  }

  // RB fixup while parent->color == red (0)
  Node* x = n;
  while (x->parent->color == 0) {
    // uncle recolor or L/R rotate (FUN_0050e9f0 / FUN_005a27f0)
    // ... standard CLRS insert-fixup on color@+0x20 family ...
  }
  map->head->parent->color = 1;  // root black
  *out = n;
}
```

## Sealed facts

| Fact | Evidence |
|---|---|
| Body 406 B / RET 0x10 | Ghidra bounds + epilogue `C2 10 00` |
| thiscall ECX=map | `MOV EDI,ECX` at entry; callers `MOV ECX,map` |
| Max size ≥ 0xFFFFFFE throw | `CMP [EDI+8],0xFFFFFFFE` / `JB` |
| STL throw string | decompile literal + length_error path |
| Buynode isnil21 Val16 | dualed `00408990`; color arg 0 |
| Color @+0x20 | rebalance reads/writes `+0x20`; root black same |
| Always-insert | no compare; parents pass where/addLeft |
| Callers | InsertHint `00406560` (dualed); InsertOrFind `00407060` |

## Name

**StdTree_InsertAndRebalance_Isnil21_Inferred** — role/CF sealed; product demangle open → `_Inferred`.

## Open questions

- Product value_type / map instantiation English.
- Full dual of rotates `0050e9f0` / `005a27f0` (roles sealed as L/R for this color family).
- Full dual of InsertOrFind `00407060` (not OWN; WQ9I-C).
- Runtime / bit-exact / differential.
