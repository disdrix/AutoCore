# Annotated low-level: FUN_00438040

| Field | Value |
|---|---|
| Stable ID | `aa_00438040` |
| VA | `0x00438040` |
| System | STL / container utility (8-byte element vectors) |
| Date | 2026-08-04 (W38-G dual seal) |

## Machine-level notes

- Source: raw capture for `aa_00438040` + live `read_memory` / xrefs (W38-G).
- Prefer machine when decompiler conflicts (fill width, ABI registers).
- **ABI (sealed):** `__fastcall` — **ECX = newCount**, **EDX = vector\*** (`begin@+4`, `end@+8`); stack **8-byte fill by value**; void; **`ret 8`**.
- **Element stride 8:** size `(end-begin)>>3`; shrink pointer `begin + newCount*8`.
- **Grow:** `FUN_00438d80(vec, end, newCount-size, &fill)` (insert-N at end; capacity policy inside callee).
- **Shrink:** `FUN_00438d00(vec, &tmp, first, end)` — **non-trivial element dtors** in erase range (not POD).
- **Fill epilogue:** if stack dword1 (control block / rep) non-null: `refcount--` at `+4`; if zero call `vtbl+8` (Release). Shared_ptr-shaped by-value fill cleanup — product type still open.
- **SEH:** `LAB_009be178`; state 0 for body, `-1` before release.
- **Reject** scaffold `Named_CalleeOf_Named_gfxBody_00438040` as product name.
- Role name: `StdVector_Resize_Elem8_ViaInsertN_00438d80`.

## Pseudocode (annotated)

```c
// __fastcall: ECX=newCount, EDX=vec*; stack: Elem8 fill (8 B by value); ret 8
void __fastcall StdVector_Resize_Elem8_ViaInsertN_00438d80(
    uint32_t newCount,   // ECX
    void *vec,           // EDX; begin@+4, end@+8
    uint32_t fill_lo,    // stack+4 (first half of 8-byte fill)
    int *fill_rep)       // stack+8 (second half; optional refcount block*)
{
  // size = (begin==0) ? 0 : (end-begin)>>3
  // if size < newCount: FUN_00438d80(vec, end, newCount-size, &fill)
  // else if begin && newCount < size: FUN_00438d00(vec, &tmp, begin+newCount*8, end)
  // if fill_rep: Decref; if 0: (*fill_rep)->vtbl[2]()
}
```

## Callers (3 sites / 2 funcs)

| Site | Function | Notes |
|---|---|---|
| `0x00437cf7` | `FUN_00437ce0` | Thin wrapper; stack proxy `{0xa9da78, 0}` fill |
| `0x00765b15` | `FUN_00765740` | `ECX=count`, `EDX=host+0x128` |
| `0x00765ef5` | `FUN_00765740` | Same vector at `+0x128` |

## Open questions

- Product element type (shared_ptr-shaped 8 B vs custom `{ptr,rep}`).
- Product/MSVC demangle for `vector<T>::resize`.
- Unowned grow/shrink internals (`FUN_00438d80`, `FUN_00438d00`).
- Runtime / bit-exact / differential.
