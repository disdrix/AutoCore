# Annotated low-level: FUN_0098df00

| Field | Value |
|---|---|
| Stable ID | `aa_0098df00` |
| VA | `0x0098df00`–`0x0098e29f` |
| System | palantir graphics / PalTool material + visible shadow volume |
| Date | 2026-08-04 (W37-A dual seal; wave36 residual) |
| Inferred name | `PalToolNoMap_Host0x40_MaterialAndShadowVolInit_Inferred` |

## Machine-level notes

- Live decompile **≡** raw capture (2026-07-23); no CF delta on W37-A re-verify.
- **Sole caller** `FUN_0073c810` @ `0x0073cb1b`: allocates **0x40** host (`FUN_00457b30`), zeros holders at `+0x28` / `+0x30` / `+0x38` and adjacent flag bytes, stores HostPtrTable12 **slot10**, then `FUN_0098df00()` with host still in **EAX** (decompiler `in_EAX` phantom).
- **No stack formal** — host arrives in **EAX** (**High**; register-arg / leftover from store).
- Three sequential material stages:

### Stage 1 — `host+0x28` · `PalToolMaterialNoMap.fx`

Same ladder as `FUN_0098fd80` with **different emissive**:

| Param | Value |
|---|---|
| DepthBias | 0.001f (`g_flMsToSeconds_Inferred`) |
| GhostAlpha | 0.1f (`g_flMultiKillCountBlend`) |
| MatDiffuse / MatSpecular | handle bind if ≥ 0 |
| MatEmissive RGBA | **(1.0, 0.2, 0.2, 1.0)** via `g_flOne` + `DAT_00a0f70c` (0.2f) ×2 + `g_flOne` |
| Finish | `FUN_0073d940` refcount swap into `+0x28` |

### Stage 2 — `host+0x30` · `PalVisibleShadowVolume.fx`

| Param | RGBA (local_10..local_4) |
|---|---|
| SolidColor | **(0, 1, 0, 0.5)** — G=`g_flOne`, A=`DAT_00a0f298` |
| WireColor | **(0, 1, 0, 0.5)** — G=`g_flOne`, A=0.5 (ADV 2026-08-04: prior blue-wire seal was wrong) |
| Finish | refcount swap into `+0x30` |

### Stage 3 — `host+0x38` · `PalVisibleShadowVolume.fx` (second instance)

| Param | RGBA |
|---|---|
| SolidColor | **(0, 0, 1, 0.5)** |
| WireColor | **(0, 0, 1, 0.5)** |
| Finish | refcount swap into `+0x38` |

- Null holder → local `-1` sentinel pointer (same pattern as siblings).
- **Always returns 0**.

## Pseudocode (annotated)

```c
// host in EAX; no stack args (caller FUN_0073c810)
uint32_t FUN_0098df00(void /* host in EAX */)
{
  void *host = (void *)in_EAX;   // decompiler phantom
  int *slot;
  int *param_handle;
  int *new_holder, *old_holder, *refp;
  int name_shell;                // local_24
  float depth_bias;              // local_20
  float ghost_alpha;             // local_1c
  int local_18;
  undefined1 local_14[4];
  float c0, c1, c2, c3;          // local_10..local_4 RGBA pack

  // ---- Stage 1: PalToolMaterialNoMap @ host+0x28 ----
  FUN_00989e00(&name_shell, "PalToolMaterialNoMap.fx");
  slot = (int *)((char *)host + 0x28);
  FUN_009701d0(&name_shell);
  depth_bias = g_flMsToSeconds_Inferred;     // 0.001
  FUN_00442640("DepthBias", &depth_bias);
  ghost_alpha = g_flMultiKillCountBlend;     // 0.1
  FUN_00442640("GhostAlpha", &ghost_alpha);

  // MatDiffuse / MatSpecular (null → -1)
  // MatEmissive RGBA (1, 0.2, 0.2, 1) then bind
  c0 = g_flOne; c1 = (float)DAT_00a0f70c; c2 = (float)DAT_00a0f70c; c3 = g_flOne;
  // ... same null/-1 + FUN_00752370 + FUN_0096fff0 ladder ...
  // refcount swap *slot via FUN_0073d940

  // ---- Stage 2: PalVisibleShadowVolume @ host+0x30 ----
  FUN_00989e00(&name_shell, "PalVisibleShadowVolume.fx");
  slot = (int *)((char *)host + 0x30);
  FUN_009701d0(&name_shell);
  // SolidColor (0, 1, 0, 0.5)
  c1 = g_flOne; c0 = 0.0f; c2 = 0.0f; c3 = (float)DAT_00a0f298;
  // WireColor (0, 1, 0, 0.5) — same green channel as Solid (ADV 2026-08-04)
  c0 = 0.0f; c2 = 0.0f; c1 = g_flOne; c3 = (float)DAT_00a0f298;
  // refcount swap

  // ---- Stage 3: PalVisibleShadowVolume @ host+0x38 ----
  FUN_00989e00(&local_18, "PalVisibleShadowVolume.fx");
  slot = (int *)((char *)host + 0x38);
  FUN_009701d0(&local_18);
  // SolidColor (0, 0, 1, 0.5)
  c2 = g_flOne; c0 = 0.0f; c1 = 0.0f; c3 = (float)DAT_00a0f298;
  // WireColor (0, 0, 1, 0.5)
  c0 = 0.0f; c1 = 0.0f; c2 = g_flOne; c3 = (float)DAT_00a0f298;
  // refcount swap

  return 0;
}
```

Full statement-level CF is preserved in clean source and raw body (no summarization for odd paths).

## Confidence

| Claim | Level |
|---|---|
| Sole caller + host size 0x40 + slots `+0x28/+0x30/+0x38` | **Confirmed** |
| Host via EAX (`in_EAX`) | **High** |
| Three effect stages + plate strings | **Confirmed** |
| Emissive (1,0.2,0.2,1); shadow Solid/Wire colors | **Confirmed** (read_memory) |
| Always return 0 | **High** |
| Product host class English | **Unknown** (`_Inferred`) |
| Why two shadow-volume slots with near-identical colors | **Tentative** (debug viz dual-pass?) |
| Exact thiscall ECX for helpers | **Probable** residual |

## Open questions

- Product/PDB name for 0x40 host / shadow-volume dual slots.
- Why stage-2 Solid is green and stage-3 Solid is blue (both Wire blue).
- Unowned callee full contracts; runtime/diff/bit-exact.
