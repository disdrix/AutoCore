# Annotated low-level: CVOGHB_SlewFacingYaw_ctor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005d6b80` |
| VA | `0x005d6b80`–`0x005d6c0f` (144 B) |
| Canonical | `CVOGHB_SlewFacingYaw_ctor_Inferred` |
| Ghidra | `FUN_005d6b80` |
| System | heartbeat / creature facing |
| Date | 2026-07-29 (W28-K re-seal; scaffold 2026-07-23) |

## Machine-level notes

- **Ctor** for secondary HB allocated as **`0x28`** when host AI def `+0x4e8==1` (`Object_EnsurePrimaryHBAI_FromAIProfile`).
- Installs vtbl **`PTR_FUN_009daee8`**. Tick sibling **`FUN_005d6c10`** @ vtbl+0x0C re-runs slew with `nPeriodMs` then `CVOGHBBase_RescheduleAfterFire`.
- **`param_1[2] = 0x1e`** → pulse/period field at **HB+0x08** = **30 ms** (also initial slew Δt).
- **`SetPeriodAndCounter(this, -1000, true)`** — same sentinel pattern as Regeneration / other period HBs.
- **`param_1[9]` (HB+0x24)** stores **raw** owner; MI-adjusted owner passed to `AttachOwnerObject` (base **+0x18**).
- Zeroes **`*(float*)(rawOwner+0x11c)`** via `xorps`/`movss` (facing yaw write site of sealed `CVOGCreature_SlewFacingYaw_Inferred`).
- Immediate **`CVOGCreature_SlewFacingYaw_Inferred(rawOwner, 0x1e)`** — Ghidra decompile mangles this-arg; bytes seal ECX=raw owner.
- SEH: `LAB_009a7138`; **`RET 4`**.

## Pseudocode (annotated)

```c
// __thiscall; RET 4; returns this
// size: factory new(0x28)
CVOGHBBase * __thiscall CVOGHB_SlewFacingYaw_ctor_Inferred(
    CVOGHBBase *self,
    void *pOwnerRaw)
{
  // SEH frame...
  CVOGHBBase_ctor(self);
  *self = &PTR_FUN_009daee8;          // subclass vtbl
  *(int *)((char *)self + 8) = 0x1e;  // pulse ms
  CVOGHBBase_SetPeriodAndCounter(self, -1000, true);
  *(void **)((char *)self + 0x24) = pOwnerRaw;  // raw owner stash
  void *pAttach = pOwnerRaw;
  if (pAttach != 0)
    pAttach = /* MI adjust via *( *(p+4)+4 ) + 4 + p */;
  CVOGHBBase_AttachOwnerObject(self, pAttach);
  *(float *)((char *)pOwnerRaw + 0x11c) = 0.0f;  // bytes: movss
  CVOGCreature_SlewFacingYaw_Inferred(pOwnerRaw, 0x1e); // ECX=raw owner
  return self;
}
```

## Open questions

- Product/PDB English class name for vtbl `009daee8`.
- Whether `+0x24` raw stash is shared layout name across other HBs of size 0x28.
- Runtime golden: first slew after attach.
