# Annotated low-level: FUN_00759de0 / gfxUIWindow_CompleteDtor

| Field | Value |
|---|---|
| Stable ID | `aa_00759de0` |
| VA | `0x00759de0`–`0x0075a143` (**868 B** / `0x364`) |
| System | UI / graphics — gfxUIWindow complete destructor |
| Date | 2026-08-05 (WQ9L-F dual seal) |
| Ghidra name | `FUN_00759de0` |
| Canonical name | `gfxUIWindow_CompleteDtor` |

---

## Machine-level notes

- **ABI:** `__thiscall` — entry `MOV ESI,ECX`; SEH `LAB_009b3156`; bare `RET` (`C3` @ `0x0075a0f4`); void; does **not** free `this`.
- **Authority:** Prefer `disassemble_function` + `read_memory` over decompile. Ghidra decompile **false-noreturn** on `operator_delete` truncates mid-body; live bytes continue through child-vector assert, residual EH member teardown, and bare `RET`.
- **Product RTTI:** COL `@0x00a9f300` → `0x00ab96a4` → type_info `0x00afe190` → **`.?AVgfxUIWindow@@`**.
- **Primary vtbl:** `PTR_FUN_00a9f304` installed at entry. `vtbl[0]` = scalar peer `FUN_004401b0`.
- **Source string:** `C:\vog\1_code\palantir\palantir\graphics\gfxUIWindow.cpp` line `0x73` — "Window deleted with children still attached!".
- **Role:** MSVC complete-object destructor for **gfxUIWindow**. Super-base of `CNDUIWindow_CompleteDtor` (`0x007b5be0`).

## Offset map (this = ESI)

| Offset | Role in this dtor |
|---|---|
| `+0x00` | Primary vtbl → `PTR_FUN_00a9f304` |
| `+0x04` | Tree/list shell (head @ +4, size @ +8) — cleared via `FUN_00440ab0` then final erase/`delete` head |
| `+0x10` | Tree/list shell — cleared via `FUN_00440c90` then later `FUN_00441720` |
| `+0x20` / `+0x24` / `+0x28` | Vector begin/end/cap — free + always-null |
| `+0x30` | Nested subobject vtbl slot — mid-dtor install `PTR_FUN_00a9db18` |
| `+0x34` | Refcounted iface* — dec; `vcall[+8]` if 0; null |
| `+0x3c` / `+0x40` / `+0x44` | Vector — free + null |
| `+0x5c` / `+0x60` / `+0x64` | Vector — free + null |
| `+0x88` | Non-null triggers `FUN_00756be0(this)` (detach/parent helper) |
| `+0x94` / `+0x98` / `+0x9c` | Children vector of window*; assert if non-empty; zero each child`+0x88`; free |
| `+0xA4` | Tree/list shell — `FUN_00440d20` then `FUN_00441960` + delete head |
| `+0xB0`, `+0xB4` | Owned objects — `FUN_00988710` + `operator_delete` |

## Control flow (annotated)

```
SEH; ESI=this; EBX=0
*this = PTR_FUN_00a9f304
// early clears of three tree shells + refcount + several vectors
// optional detach FUN_00756be0
// children still attached? assert + zero parent links + free vector
// EH-ordered residual: shell @+0xA4, re-free vectors, nested vtbl @+0x30,
//   shell @+0x10, shell @+0x04
// bare RET  (host free is scalar peer 0x004401b0 only)
// side-path: VOGCRASH / ExitProcess(3) if assert returns 3
```

## Pseudocode (decompile scaffold + false-noreturn correction)

See raw capture body + WQ9L-F re-verify append. Clean source: `reconstructed-exact/gfxUIWindow_CompleteDtor.cpp`.

## Open residual (not blocking dual)

- Product English for tree shells / vector element types.
- `FUN_00756be0` / `FUN_00988710` / erase helpers undualed.
- Runtime / bit-exact / differential open.
