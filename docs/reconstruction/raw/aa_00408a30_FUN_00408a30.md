# Raw capture: FUN_00408a30

| Field | Value |
|---|---|
| **Stable ID** | `aa_00408a30` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00408a30` |
| **Canonical name** | `FUN_00408a30` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00408a30(void)

{
  undefined4 *puVar1;
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  local_8 = 0xffffffff;
  puStack_c = &LAB_009bc690;
  local_10 = ExceptionList;
  ExceptionList = &local_10;
  puVar1 = operator_new(0x30);
  if (puVar1 != (undefined4 *)0x0) {
    *puVar1 = 0;
  }
  if (puVar1 + 1 != (undefined4 *)0x0) {
    puVar1[1] = 0;
  }
  if (puVar1 + 2 != (undefined4 *)0x0) {
    puVar1[2] = 0;
  }
  *(undefined1 *)(puVar1 + 10) = 1;
  *(undefined1 *)((int)puVar1 + 0x29) = 0;
  ExceptionList = local_10;
  return;
}
```

---

## Re-verify append — 2026-08-04 WQ9H-J OWN dual

| Field | Value |
|---|---|
| **Agent** | WQ9H-J OWN-ONLY |
| **Tools** | `batch_decompile` / `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + callers/xrefs + `get_assembly_context`. **No** `disassemble_bytes`. No Launcher. |
| **Live decompile** | ≡ scaffold body (new 0x30 → zero links → color=1 → isnil=0) |
| **Body** | `0x00408a30`–`0x00408ab9` inclusive (**138 B** / `0x8A`); bare **`RET`** (`C3`) |
| **ABI** | no stack args; **EAX** = node\* (decompiler may show `void` — false); not thiscall |
| **Layout** | size **0x30**; left/parent/right zeroed; **color=1** (black) @+0x28; **isnil=0** @+0x29; Val24 region untouched |
| **Callers (4)** | `FUN_00405b40`, `FUN_00405e60`, `FUN_00405fb0`, `FUN_004d98f0` — all store head@+4, set **isnil=1**, self-link L/P/R to head, size@+8 = 0 |
| **SEH** | frame present (`LAB_009bc690`); SEH cleanup path after RET deletes node on unwind |
| **Named** | `StdTree_BuyHeadNode_Isnil29_Inferred` |
| **Terminal** | false |
| **Odd** | field-address null checks after OOM (`LEA [eax+4]` when eax==0) + unconditional color/isnil stores — preserve MSVC shape; new typically throws |

### Entry / epilogue bytes (`read_memory`)

- Entry: `55 8B EC 6A FF 68 90 C6 9B 00 … 6A 30 E8 …` (`PUSH 0x30` + `operator_new`)
- Stores: `88 58 28` (color=BL=1), `88 50 29` (isnil=DL=0)
- Epilogue @ `0x00408ab9`: `C3`

### Note on decompiler `void` return

Ghidra may classify as void; **EAX** remains the `operator_new` result through epilogue — callers use `MOV [host+4], EAX`.
