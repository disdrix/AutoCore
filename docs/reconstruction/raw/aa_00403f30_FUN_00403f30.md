# Raw capture: FUN_00403f30

| Field | Value |
|---|---|
| **Stable ID** | `aa_00403f30` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00403f30` |
| **Canonical name** | `FUN_00403f30` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * FUN_00403f30(undefined4 param_1,undefined4 param_2,undefined4 param_3,undefined4 param_4,
                   undefined4 param_5)

{
  void *pvVar1;
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  local_8 = 0xffffffff;
  puStack_c = &LAB_009bc781;
  local_10 = ExceptionList;
  ExceptionList = &local_10;
  pvVar1 = operator_new(0x38);
  local_8 = 1;
  if (pvVar1 != (void *)0x0) {
    FUN_00404360(param_3,param_4,param_5);
  }
  ExceptionList = local_10;
  return pvVar1;
}
```

---

## Re-verify — 2026-08-05 MEGA-048 OWN-ONLY dual seal

| Field | Value |
|---|---|
| **Agent** | MEGA-048 OWN-ONLY dual |
| **Tools** | HTTP Ghidra `127.0.0.1:8089`: `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` / `get_function_callees` / `get_xrefs_to`. **No** `disassemble_bytes`. No Launcher. |
| **Integrity** | scaffold body above preserved; this section append-only |
| **Program** | `autoassault.exe` base `0x400000` |

### Live decompile ≡ scaffold CF

Live `decompile_function` **≡** 2026-07-23 raw control flow: SEH frame → `operator_new(0x38)` → null-skip / else construct via `FUN_00404360` → restore ExceptionList → return node*.

**Decompiler gap (sealed by assembly):** decompile shows only stack args to `FUN_00404360(param_3,param_4,param_5)`. Live disasm passes **all five** stack params into ctor via **EAX=node**, **ECX=left**, **EDX=parent**, stack **right/value/color** (`__fastcall`+stack ctor).

### Body / ABI (bytes + disassembly)

| Fact | Value |
|---|---|
| Body | `0x00403f30`–`0x00403fb2` exclusive (**130 B** / `0x82`); terminal **`RET 0x14`** (`C2 14 00` @ `0x00403faf`); SEH cleanup + `CC` pad follow |
| Convention | **`__stdcall`** 5 stack args; **not** thiscall; **EAX** = node* \| null |
| Epilogue | `POP EDI/ESI/EBX; MOV ESP,EBP; POP EBP; RET 0x14` |
| Alloc | `PUSH 0x38; CALL operator_new @ 0x00489892; ADD ESP,4` |
| Construct | `MOV EAX,ESI; CALL FUN_00404360` only if ESI≠0 |
| Final return | always `MOV EAX,ESI` (OOM path keeps null in ESI; intermediate `XOR EAX,EAX` is local only) |
| SEH | FS:[0] frame; filter `LAB_009bc781` (`PUSH 0x9bc781`) |

Stack layout (EBP-relative, stdcall):

| Slot | Arg | Role (from sole caller + ctor) |
|---|---|---|
| `[EBP+0x08]` | param_1 | left |
| `[EBP+0x0C]` | param_2 | parent |
| `[EBP+0x10]` | param_3 | right |
| `[EBP+0x14]` | param_4 | value* (8×u32) |
| `[EBP+0x18]` | param_5 | color |

Ctor call site (this unit → `FUN_00404360`):

```text
PUSH [EBP+0x18]          ; color
PUSH [EBP+0x14]          ; value*
PUSH [EBP+0x10]          ; right
MOV  EDX, [EBP+0x0C]     ; parent
MOV  ECX, [EBP+0x08]     ; left
MOV  EAX, ESI            ; this = new node
CALL 0x00404360          ; RET 0xC
```

### Ctor layout evidence (`FUN_00404360` — not OWN; evidence only)

| Offset | Write |
|---|---|
| +0x00 | left ← ECX |
| +0x04 | parent ← EDX |
| +0x08 | right ← stack |
| +0x10..+0x2F | `REP MOVSD` **8 dwords** from value* (**Val32**) |
| +0x30 | color ← stack byte |
| +0x31 | **isnil = 0** |
| size | **0x38** |

### Callers / xrefs / callees

| Role | Target |
|---|---|
| **Caller (1)** | `FUN_00403250` @ `0x004032b6` UNCONDITIONAL_CALL |
| **Xrefs** | **xref_count = 1** |
| **Callees** | `operator_new` @ `0x00489892`; `FUN_00404360` @ `0x00404360` |

Call-site pattern (`FUN_00403250` insert/rebalance isnil31 peer — residual MEGA-047):

```text
MOV  EAX, [EDI+4]        ; head
PUSH 0                   ; color = red
PUSH EDX                 ; value
PUSH EAX                 ; right = head
PUSH ESI                 ; parent = where
PUSH EAX                 ; left = head
CALL 0x00403f30
MOV  ECX, EAX            ; node*
ADD  [EDI+8], 1          ; size++
```

### Name decision

Promote **`StdTree_Buynode_Val32_Isnil31_Inferred`**.

- Structural: MSVC `_Tree` buynode; size **0x38**; isnil@**+0x31**; value **32 B** @+0x10; color@+0x30.
- Family peers: dualed Lrotate/Rrotate/erase/BuyHead isnil31; Val24 isnil29 buynode `0x00407e30` (different size/offset).
- Product / PDB demangle open → **`_Inferred`**.
- **Reject** scaffold `Named_CalleeOf_*NotifyActiveMission*` (chain-of-caller plate only; unit is shared STL helper).
- **Reject** merge with isnil29 Val24 buynode / BuyHead (head zeros links, no value copy) / freelist.

### Gaps

- Product / MSVC demangle for host `value_type` (8-dword payload English).
- Full dual of ctor `FUN_00404360` and insert parent `FUN_00403250` (not OWN; MEGA-047 owns insert).
- Runtime / bit-exact / differential.

**Terminal:** **false**
