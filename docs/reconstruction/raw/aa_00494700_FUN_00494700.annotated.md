# Annotated low-level: Host_EnsureRegionCellOwned_Inferred (`aa_00494700`)

| Field | Value |
|---|---|
| Stable ID | `aa_00494700` |
| VA | `0x00494700`–`0x0049484f` exclusive (**335 B**) |
| Canonical name | `Host_EnsureRegionCellOwned_Inferred` |
| Ghidra name | `FUN_00494700` |
| System | host grid region — ensure ListTrackedObj per cell |
| Date | 2026-07-29 (W31-C) |

## Machine-level notes

- Source: raw + live re-decompile + full-body `read_memory`.
- **thiscall** host in EDI (`8B F9`); one stack arg `param_2` (context); epilogue **`RET 4`**.
- Busy flag `host+0x53`: cleared on entry, set on exit (re-entrancy / in-progress marker).
- Grid: array base `host+0x28`, dimX `host+4`, stride **0x28** (same host layout as W30-Q rebuild / W29-F Elem0x28).
- Region AABB integers: x∈`[+0x64,+0x6c)`, y∈`[+0x68,+0x70)`.
- Empty cell (`owned@+0x24==0`): `operator_new(0x5E0)` + `FUN_004becb0(cell,0)` = ListTrackedObj ctor; store context @ `owned+0x5D4`; position via `FUN_004bd890`; content via `FUN_004bf690`; optional pressure path may complete-dtor+delete another cell's owned object (`ListTrackedObj_CompleteDtor` W30-P).
- Occupied cell: `FUN_004bd6f0` MRU-relink on global list.
- False noreturn on `operator_delete` — bytes continue outer loops + flag set + `RET 4`.

## Host / cell offset map

| Offset | Meaning |
|--------|---------|
| host `+0x04` | dimX (row pitch in cells) |
| host `+0x28` | pointer to 0x28-stride cell array |
| host `+0x53` | busy / region-build flag (0 during, 1 after) |
| host `+0x64` / `+0x6c` | region x begin / end |
| host `+0x68` / `+0x70` | region y begin / end |
| cell `+0x24` | owned `ListTrackedObj*` (size **0x5E0**) |
| owned `+0x5A8/+0x5AC` | grid x/y (written by `FUN_004bd890`) |
| owned `+0x5D0` | flag (pressure / list-region path; also W30-P complete dtor gate) |
| owned `+0x5D4` | back-pointer to `param_2` context |
| `param_2+0x7d` | skip-flag for setting victim `+0x5D0` |

## Pseudocode (annotated)

```c
// ECX = host. Stack: context (param_2). SEH LAB_009a0bd0. RET 4.
void __thiscall Host_EnsureRegionCellOwned_Inferred(Host* host, Context* ctx)
{
  host->busy_at_0x53 = 0;
  for (uint x = host->x0_at_0x64; x < host->x1_at_0x6c; ++x) {
    for (uint y = host->y0_at_0x68; y < host->y1_at_0x70; ++y) {
      Elem0x28* cell = host->cells_at_0x28 + (host->dimX_at_4 * y + x);
      if (cell->owned_at_0x24 == nullptr) {
        ListTrackedObj* obj = operator_new(0x5E0);
        if (obj) obj = FUN_004becb0(obj, cell, /*param_3=*/0); // ctor
        obj->ctx_at_0x5d4 = ctx;
        FUN_004bd890(obj, x, y);   // world/grid placement
        FUN_004bf690(obj);         // process nested children
        cell->owned_at_0x24 = obj;
        ListTrackedObj* victim;
        if (FUN_004bd660(&victim)) { // global capacity pressure
          // map victim grid indices back to a cell; optional flag@+0x5D0
          // null cell->owned; ListTrackedObj_CompleteDtor(victim); delete victim
        }
      } else {
        FUN_004bd6f0(cell->owned_at_0x24); // MRU relink
      }
    }
  }
  host->busy_at_0x53 = 1;
}
```

## Callers / callees

| Direction | Targets |
|---|---|
| Callers | `FUN_004951d0` (static culling — string `"Static culling"`); `FUN_00495130` |
| Callees | `operator_new`, `FUN_004becb0`, `FUN_004bd890`, `FUN_004bf690`, `FUN_004bd660`, `ListTrackedObj_CompleteDtor` (`0x004be2a0`), `operator_delete`, `FUN_004bd6f0` |
| Related | host rebuild `0x004941b0` (W30-Q); Elem0x28 dtor `0x005b8000` (W29-F); owned complete dtor W30-P |
