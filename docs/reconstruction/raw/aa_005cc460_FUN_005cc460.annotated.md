# Annotated low-level: Object_IsLevelLessThan6 / FUN_005cc460

| Field | Value |
|---|---|
| Stable ID | `aa_005cc460` |
| VA | `0x005cc460`–`0x005cc488` (40 B) |
| System | `npc-ai` / vehicle pursue gate |
| Date | `2026-07-29` W23-R dual seal |
| Named | `Object_IsLevelLessThan6` |

## Machine-level notes

- **ABI:** stdcall one pointer; epilogue **`ret 4`** (`C2 04 00`) twice (true/false paths).
- **Null:** `param == 0` → return 0 (no vcall).
- **This-adjust:** classic MSVC multi-inheritance resolve:
  - `adj = *(*(obj+4)+4)`; `inner = obj + adj + 4`; vtbl = `*inner`.
- **Vcall:** `[vtbl+0x27c]` — sealed elsewhere as **GetLevel** (int).
- **Compare:** `level < 6` → AL=1; else AL=0.
- **No** float math; **no** callees besides indirect vfunc.
- Prior DoVehiclePursue plate mislabeled this as "heading" — **false**; it is a **level predicate**.

## Recovered CF

```c
// stdcall; ret 4
unsigned char Object_IsLevelLessThan6(void *obj)
{
    if (obj == nullptr)
        return 0;
    int *inner = (int *)((char *)obj + *(int *)(*(int *)((char *)obj + 4) + 4) + 4);
    int level = (*(int (__thiscall **)(void *))(*inner + 0x27c))(inner);
    return level < 6 ? 1 : 0;
}
```

## Constants

| Value | Meaning |
|---|---|
| `6` | exclusive upper bound on level |
| `0x27c` | GetLevel vtable slot |

## Open questions

- Product/PDB English name.
- Why threshold is specifically **6** (tutorial / early-zone pursue bias?).
- Full set of types that implement vtbl+0x27c on the pursue vehicle object.
