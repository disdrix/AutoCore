# Annotated low-level: FUN_00444850 → FourCCFnPtrMap_TryInsert_DAT_00d21958

| Field | Value |
|---|---|
| Stable ID | `aa_00444850` |
| VA | `0x00444850`–`0x00444874` exclusive (**36 B**) |
| System | Palantir FourCC→handler global map insert wrapper |
| Date | 2026-07-29 (W35-H dual seal) |

## Machine-level notes

- Source: raw + W35-H body hex + caller site bytes in `FUN_00754680`.
- Decompiler void/no-arg is incomplete — EAX tag + ECX handler are register formals.
- Map object `DAT_00d21958` loaded into EAX immediately before `FUN_00458510`.
- Sibling `FUN_00444880` is the same pattern for `DAT_00d21964`.

## ABI

```
// EAX=FourCC tag; ECX=handler*; bare RET; void (discards out-pair)
void FourCCFnPtrMap_TryInsert_DAT_00d21958(
    /*eax*/ uint32_t tag,
    /*ecx*/ void* handler);
```

## Annotated flow

```
frame 0x10 + save EBX
out = stack pair buffer
key = { tag: EAX, handler: ECX } on stack
EBX = &key
EAX = &DAT_00d21958          // map*
push &out
CALL FUN_00458510            // find / insert-if-absent (unowned internals)
// discard out {iterator, inserted?}
restore; RET
```

## Call sites (Palantir device start)

| Tag | Handler |
|-----|---------|
| `'EFCT'` `0x45464354` | `0x00990540` |
| `'MRPH'` `0x4d525048` | `0x009906e0` |

## Open / residual

- Exact product English for map / handlers (factory table vs service registry).
- Whether existing key updates value (depends on unowned `FUN_00458510` / `FUN_00438140`).
- Runtime / bit-exact.

