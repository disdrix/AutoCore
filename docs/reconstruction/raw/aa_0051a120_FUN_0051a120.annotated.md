# Annotated low-level: Faction_LookupById (`FUN_0051a120`)

| Field | Value |
|---|---|
| Stable ID | `aa_0051a120` |
| VA | `0x0051a120`–`0x0051a16a` (**75 B** / `0x4B`) |
| System | game-data / faction table |
| Date | 2026-08-04 (WQ9R-G dual seal) |
| Proposed name | `Faction_LookupById` |

## Machine-level notes

- **ABI:** **cdecl** — one stack formal `factionId` (`mov esi,[esp+8]` after `push esi`); bare **`C3`** on all exits (caller cleans).
- **Negative gate:** `(int)id < 0` → return **NULL** immediately (unsigned ids with high bit set rejected).
- **Lazy init:** if `DAT_00b04320 == 0` call **`FUN_005197b0`** (WQ9R-F owned) — loads faction rows (`operator_new(0x64)`), copies id + name strings, inserts via `FUN_0051e5d0`.
- **Hash globals (static image BSS; zero offline):**
  - `DAT_00b0431c` — bucket mask
  - `DAT_00b04320` — ready / count (zero ⇒ init)
  - `DAT_00b04324` — bucket table base
- **Node layout (chain):** head = `*( *(buckets + (mask & id)*4) + 4 )`; key `@+0x10`; next `@+0x0C`; **value ptr `@+0x08`** returned (not dereferenced).
- **Caller string seal:**
  - `FUN_00614380`: miss → `"UNDEFINED FACTION: %i"`; hit → display string at `record+4` (`piVar5+1`).
  - `FUN_00612df0`: miss → `"UNKNOWN: %i"`; hit → same `record+4`; also uses `*record` as type (9/10 ⇒ Creatures suffix).
- **Sibling contrast:** `FUN_00519d20` (nearby) uses different globals (`DAT_00b042b*`) and returns **`*value`**; `FUN_0051a0e0` wraps `CNDHash_LookupByKey(&DAT_00b042e0,…)`. This unit is the **faction** table, not skill-bank.
- Prior scaffold alias `Named_CalleeOf_Drive_Vehicles_0051a120` is **too narrow** (mission/UI objective formatters, not vehicle drive).

## Control flow (annotated)

```
FactionRecord* Faction_LookupById(uint factionId):
  if (int)factionId < 0: return NULL
  if DAT_00b04320 == 0: FUN_005197b0()   // load table
  node = buckets[(DAT_00b0431c & factionId)].chain_head  // +4 off bucket head
  while node:
    if node.key(+0x10) == factionId:
      return node.value(+0x08)
    node = node.next(+0x0C)
  return NULL
```

## Open questions

- Product type name for the 0x64-byte faction record (fields beyond id@0 / name@+4 / name2@+0x24 from loader).
- Whether `DAT_00b04320` is count vs boolean ready flag (tested only `== 0`).
- Runtime Confirmed deferred (no Launcher).
