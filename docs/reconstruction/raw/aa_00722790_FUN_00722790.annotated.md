# Annotated low-level: CSoundManager_AppendOggOrLxxExt_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00722790` |
| VA | `0x00722790` |
| Body | `0x00722790`–`0x007227de` exclusive (78 / `0x4E` B) |
| Canonical name | `CSoundManager_AppendOggOrLxxExt_Inferred` |
| Ghidra name | `FUN_00722790` |
| System | client audio / sound path extension |
| Date | 2026-07-23 scaffold; **2026-08-04 W38-AG dual seal** |

## Machine-level notes

- **thiscall** leaf: **ECX = host\*** (byte flag @ `+0x308`), stack **`char* pathBuf`**, **`RET 4`**.
- Flag **0** → strcat **`".ogg"`** (`0x00A9D9A4`); flag **≠0** → strcat **`"_lxx.ogg"`** (`0x00AA91A0`).
- MSVC bulk copy: `rep movsd` + residual `rep movsb` of suffix **including NUL**.
- No capacity check on `pathBuf` (callers own fixed stack buffers).
- Prefer bytes when decompiler labels params `int`.

## ABI

```c
// thiscall, RET 4
void CSoundManager_AppendOggOrLxxExt_Inferred(void* host /*ECX*/, char* pathBuf /*stack*/);
```

## Pseudocode (annotated)

```c
void __thiscall FUN_00722790(void *host, char *pathBuf)
{
  const char *suffix;
  // host+0x308: locale/variant flag — 0 = plain ogg, else _lxx.ogg
  if (*(char *)((char *)host + 0x308) == '\0')
    suffix = ".ogg";       // 0x00A9D9A4
  else
    suffix = "_lxx.ogg";   // 0x00AA91A0

  // strlen(suffix)+1 via end pointer, then strcat onto pathBuf via rep movs*
  size_t n = strlen(suffix) + 1; // includes NUL (bytes walk past terminator)
  char *dst = pathBuf + strlen(pathBuf);
  memcpy(dst, suffix, n);        // retail: dword then byte residual
}
```

## Call graph

| Direction | Targets |
|---|---|
| **Callees** | none (leaf) |
| **Callers** | `FUN_00724870` ProbeSoundFileExists; `FUN_004B88E0` MissionHost media preloads ×2; `FUN_00720DA0`; `FUN_00721A80`; `FUN_007217C0` |

Most non-probe callers load ECX via `Client_GetMissionCompleteAudioTable` (`0x00723820`) before the call.

## Open questions

- Product/PDB English for helper and for `_lxx` (locale? low-quality? language pack).
- Whether host is always the same singleton as `DAT_00d20210` vs distinct CSoundManager instances.
- Runtime / bit-exact / image diff.
