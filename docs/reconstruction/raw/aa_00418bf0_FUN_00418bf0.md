# Raw capture: FUN_00418bf0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00418bf0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00418bf0` |
| **Canonical name** | `FUN_00418bf0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00418bf0(int param_1)

{
  char cVar1;
  int iVar2;
  
  iVar2 = *(int *)(param_1 + 8);
  cVar1 = *(char *)(iVar2 + 0x19);
  while (cVar1 == '\0') {
    iVar2 = *(int *)(iVar2 + 8);
    cVar1 = *(char *)(iVar2 + 0x19);
  }
  return;
}
```

---

## W28-O re-verify (2026-07-29) — append only

| Field | Value |
|---|---|
| **Tools** | `decompile_function`, `read_memory`, `get_function_by_address`, `get_function_xrefs` — **no** `disassemble_bytes` |
| **Live decompile** | ≡ 2026-07-23 raw body (void / walk-into-nil presentation) |
| **Body exclusive end** | `0x00418c0b` (Ghidra function end; next symbol `FUN_00418c10` Rrotate) |
| **Size** | **27** B (`0x1B`) |
| **Epilogue** | `C3` (`ret`) — **cdecl** |
| **Pad** | `CC` × ≥4 after body |

### Body hex (authoritative)

```
8b442404 8b4808 80791900 750e 8d4900 8bc1 8b4808 80791900 74f5 c3
```

### Byte-level CF (overrides decompiler)

```
mov eax, [esp+4]          ; node*
mov ecx, [eax+8]          ; right = node->right
cmp byte ptr [ecx+0x19], 0
jne  done                 ; if right isnil → return original node
loop:
  mov eax, ecx            ; node = right
  mov ecx, [eax+8]        ; right = node->right
  cmp byte ptr [ecx+0x19], 0
  je   loop
done:
  ret                     ; EAX = last non-nil = Tree_max
```

**Decompiler defect:** same class as `aa_005adfa0` — void return + walk-into-nil. Callers consume EAX (`head->right = FUN_00418bf0(repl)`). **Bytes + caller use are authority.**

### Callers (6) — shared Val12 erase rightmost fixup

`FUN_004188e0`, `FUN_004e4130`, `FUN_005399f0`, `FUN_00573250`, `FUN_005ae990`, `FUN_005e15a0` (same set as Min)

### Sealed name

`StdTree_Max_Val12` (MSVC `_Tree_max` / rightmost non-nil of subtree; Val12 isnil@+0x19)
