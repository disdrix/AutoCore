# Annotated low-level: StdTree_EraseRange_Isnil19_Inferred (`FUN_0040bc10`)

| Field | Value |
|---|---|
| Stable ID | `aa_0040bc10` |
| VA | `0x0040bc10`–`0x0040bcc2` exclusive (**178 B** / `0xB2`) |
| Canonical | `StdTree_EraseRange_Isnil19_Inferred` |
| System | inventory-transfer (partition assignment); unit = MSVC `_Tree` range erase isnil@+0x19 |
| Date | 2026-08-05 R13-020 dual seal |
| Parent | `0x0092b2a0` (evidence only; not dualled) |

---

## Machine-level notes

- **ABI:** register-this **EDI** = `MapShell*`; stack `outIt**`, `first`, `last`; **RET 0x0C**; EAX = `outIt`.
- **Layout:** node left@0 / parent@4 / right@8 / color@**+0x18** / isnil@**+0x19**; shell head@+4 / size@+8 (Val12 family).
- **Full clear:** `first == head->left && last == head` → free root via `FUN_0040c100` (ECX=map, stack root, RET 4) → reset sentinel links + size 0.
- **Partial:** inline in-order successor (isnil@+0x19 plate) then `FUN_0040b440(map, &outIt, cur)`.
- **Callers:** tidy wrappers `FUN_0040b2d0` / `FUN_0040b410` (full clear then `operator_delete(head)`); parent `FUN_0092b2a0` ×2 with `LEA EDI, local_map`.
- Prefer assembly / `read_memory` when decompiler marks `operator_delete` noreturn or drops EDI.

## Pseudocode (annotated)

```c
// EDI = map; ret 0x0c
Node** StdTree_EraseRange_Isnil19_Inferred(Node** outIt, Node* first, Node* last)
{
  Node* head = map->head; // [EDI+4]
  if (first == head->left && last == head) {
    FUN_0040c100(/*ECX=*/map, head->parent); // free-subtree isnil19
    head->parent = head;
    map->size = 0;
    head->left = head;
    head->right = head;
    *outIt = head->left;
    return outIt;
  }
  while (first != last) {
    Node* cur = first;
    first = successor_isnil19(first); // inline; isnil @ +0x19
    FUN_0040b440(map, outIt, cur);    // erase+rebalance residual
  }
  *outIt = first;
  return outIt;
}
```

## Open questions

- Product demangle of map T / value at +0x0c.
- Dual residual free `0040c100` and erase `0040b440` (R13-018).
- Runtime / bit-exact / differential.
