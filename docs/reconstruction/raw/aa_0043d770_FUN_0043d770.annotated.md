# Annotated low-level: FUN_0043d770 → StdMap_EraseEq_UintKey_Isnil15_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0043d770` |
| VA | `0x0043d770`–`0x0043d7c7` exclusive (**87 B** / `0x57`) |
| System | STL / MSVC `_Tree` uint-key map (isnil@+0x15) |
| Date | 2026-08-04 (W38-O dual) |

## Machine-level notes

- Source: raw capture + W38-O re-verify for `aa_0043d770`.
- **Bytes win** over decompiler on register ABI (EAX map / ECX key\*).
- Callee family seals layout: key-u32@`+0x0C`, isnil@`+0x15` (peer of isnil15 maps; **not** Val12 +0x19).
- Sole caller: AssPreloader drain fail path (`FUN_00970fc0` / W37-AD) — removes membership for flag==0 keys.

## Pseudocode (bytes-corrected)

```c
// EAX = map*; ECX = const uint32_t* key; returns erase count; plain RET
int StdMap_EraseEq_UintKey_Isnil15_Inferred(void /*EAX=map*/, void /*ECX=key*/)
{
  // equal_range → {begin, end} on stack via FUN_0043dc20(ECX=map, EBX=key*, EAX=&pair)
  // count = distance(begin, end) via FUN_0046f100 successor (++it)
  // FUN_00439050 erase [begin, end) with EDI=map
  // return count
}
```

## Open questions

- Product/PDB map value_type English (AssPreloader host+0xDC membership tree).
- Whether product ever stores multi-equal keys (count>1) vs unique map (0/1).
- Nested dual depth on `0043dc20` / `0046f100` / `00439050` (not owned).
