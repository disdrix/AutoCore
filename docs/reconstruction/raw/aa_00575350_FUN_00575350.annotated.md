# Annotated low-level: FUN_00575350 / Object_009d33c4_CompleteDtor

| Field | Value |
|---|---|
| Stable ID | `aa_00575350` |
| VA | `0x00575350`–`0x005753ba` exclusive (**106 B**) |
| System | object / derived complete dtor (vtbl `PTR_FUN_009d33c4` → `Object_SharedBase`) |
| Date | 2026-07-29 (W28-M dual seal) |

## Machine-level notes

- **Role:** MSVC complete-object destructor for derived object family `PTR_FUN_009d33c4`.
- **Inheritance:** ctor twin `FUN_00575440` calls `Object_SharedBase_Ctor` (`FUN_00518940`) then installs this vtbl. This dtor **tail-jumps** to `FUN_00518ec0` (Object shared-base destructor body) with ECX=`this`.
- **Scalar deleting sibling:** `FUN_00575c00` = **vtbl[0]** — calls this body (via CALL into complete path; body ends in JMP so chain is body-then-base), optional free, `RET 4`.
- **Vtbl[1]:** `FUN_0056f570` is empty `RET` stub.
- **thiscall:** `mov esi, ecx`; uses EBX=0 as zero constant.
- **Owned ptr @ +0x1EC:** delete-if-nonnull; zero; clear flag bytes `+0x1F0`, `+0x1F1`.
- **Buffer @ +0x1F8/+0x1FC/+0x200:** free begin, zero triple (std::vector-like).
- **StdList @ +0x1DC:** `lea ecx,[esi+0x1DC]; call FUN_00497360`. Decompiler omits LEA.
- **Tail:** `mov ecx,esi; pop esi; pop ebx; jmp FUN_00518ec0` — no local RET.
- **False noreturn** on `operator_delete` is wrong.
- **Product English:** open (class is “object-derived with vtbl 009d33c4”; no sealed product plate).

## Pseudocode (byte-corrected)

```c
// __thiscall ECX=this; ends in JMP to base dtor (no local RET)
void Object_009d33c4_CompleteDtor(void *self)
{
  *(void **)self = &PTR_FUN_009d33c4;

  void *p = *(void **)((char *)self + 0x1EC);
  if (p) operator_delete(p);
  *(void **)((char *)self + 0x1EC) = 0;
  *((uint8_t *)self + 0x1F0) = 0;
  *((uint8_t *)self + 0x1F1) = 0;

  void *begin = *(void **)((char *)self + 0x1F8);
  if (begin) operator_delete(begin);
  *(void **)((char *)self + 0x1F8) = 0;
  *(void **)((char *)self + 0x1FC) = 0;
  *(void **)((char *)self + 0x200) = 0;

  StdList_Destroy_FreeHead((char *)self + 0x1DC);  // LEA ECX sealed

  // tail-call Object shared-base dtor
  FUN_00518ec0(self);  // image: jmp, not call
}
```

## Open questions

- Product / MSVC demangled derived type.
- Meaning of flags at `+0x1F0`/`+0x1F1` and payload at `+0x1EC`.
- Full object size / remaining mid-field dictionary (base owns lower layout).
- Runtime / bit-exact differential.
