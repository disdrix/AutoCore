# Annotated low-level: MeshHost_UploadXformPosNormAndIB16_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00634c50` |
| VA | `0x00634c50`–`0x00634df8` exclusive (**424 B** / `0x1A8`) |
| Ghidra name | `FUN_00634c50` |
| Canonical name | `MeshHost_UploadXformPosNormAndIB16_Inferred` |
| System | mesh / gfx upload (CVOGObject optional mesh-host tick) |
| Dual | WQ9J-D 2026-08-05 — **accept-with-gaps** |

---

## Signature

```c
// ECX = MeshUploadHost*
void __fastcall MeshHost_UploadXformPosNormAndIB16_Inferred(MeshUploadHost* host);
```

| Formal | Source | Conf |
|---|---|---|
| host | ECX → EDI (`8B F9`) | **High** |
| return | void (`C3`, no EAX contract) | **High** |
| stack args | none | **High** |

---

## Host layout (sealed from loads)

| Off | Role |
|---|---|
| `+0x0C` | Mesh owner* (has flags `+0xBC`, VB/IB wrappers, xform at dword index `0x19`) |
| `+0x10` | Source position vec3 array* (stride `0x0C`) |
| `+0x14` | Source normal vec3 array* (stride `0x0C`) |
| `+0x18` | Vertex count (u32) |
| `+0x1C` | Source index array* (u32 elements; low 16 bits used) |
| `+0x20` | Index count (u32) |

Gate: all of `+0x10/+0x14/+0x18/+0x1C/+0x20` non-zero.

---

## Control flow

1. **Flag dirty:** `*(uint32_t*)(owner + 0xBC) |= 1`.
2. **VB lock:** thiscall `FUN_00414ae0(owner+0x10, start=0, count=vertCount, flags=0)` → locked bytes or null.
3. **Stride:** `FUN_0044bcc0` on owner-linked device path → per-vertex stride.
4. **Xform temp:** `src = FUN_00437960(owner)` then `FUN_00416240(stackTemp, src)` (clear `temp+0xBC` + selective assign — WQ9I-H).
5. **FVF elements:** `FUN_007468e0(decl, usage=0, index=0)` position; `usage=3,index=0` normal. Offset = `*(uint16_t*)(elem+2)`.
   - Decl table base: `ESI = *(owner+0x14) + 0x24` (assembly).
6. **Per-vertex loop** (`i < vertCount`):
   - `FUN_009732d0(temp, &positions[i])` → write 3 floats at `locked + posOff`.
   - `FUN_00973240(temp, &normals[i])` → write 3 floats at `locked + nrmOff`.
   - Advance locked by stride; source by `0x0C`.
7. **VB unlock:** if device non-null → `FUN_007464e0` (`gfxDeviceVB.cpp`).
8. **IB lock:** thiscall `FUN_0044ba40(owner+8, start=0, count=indexCount, flags=0x800)`.
9. **Index pack:** for `i < indexCount`: `dst_u16[i] = (uint16_t)src_u32[i]` (loads low half of each dword).
10. **IB unlock:** if device non-null → `FUN_00743ba0` (`gfxDeviceIB.cpp`).

---

## Callers / callees

| Dir | Targets |
|---|---|
| Caller (1) | `FUN_00595230` / `CVOGObject_Tick` @ `0x005952a9` — multi-slot host table at object `+0x1c4`, gated by `+0x90d` bit1 |
| Callees | listed in raw re-verify |

---

## Gaps

- Product English for mesh host / owner class (no RTTI at this VA).
- Exact D3D lock flag meaning of IB `0x800`.
- Whether xform is world/local bone matrix (behavior: used by `FUN_009732d0/40`).
- Runtime / bit-exact / differential.

---

## Naming

`MeshHost_UploadXformPosNormAndIB16_Inferred` — structural from VB/IB unlock strings + pos/normal FVF + u16 index pack. Scaffold long callee-of chain discarded. `_Inferred` until product symbol.
