# Annotated low-level: FUN_007b66c0

| Field | Value |
|---|---|
| Stable ID | `aa_007b66c0` |
| VA | `0x007b66c0`–`0x007b6728` |
| System | ND path / interface resource map |
| Date | 2026-07-29 (W17-M dual seal) |

## Machine-level notes

- Source: live `decompile_function` ≡ `force_decompile` + `read_memory` body 104 B.
- ABI: path in **EAX**; context object on stack; **`RET 4`**; bool in **AL**.
- Gate: `TEST byte [ctx+4], 2` / early fail → return 0.
- Basename: max(`strrchr('\\')`, `strrchr('/')`) + 1, or full path if neither separator.
- Intern via `FUN_00989e00(this=0x00d1eac8, &token, basename)`.
- Lookup: `*(ctx+0xc)->vtbl+0x14(&token)` → non-zero ⇒ return 1.

## Pseudocode (annotated)

```c
// EAX = full path C-string (IN)
// stack arg = resource/path-map context (ctx)
// returns AL: 1 if basename token hits map, else 0
uint8_t FUN_007b66c0(PathMapContext *ctx /* stdcall stack */, /* EAX */ const char *path)
{
  char *slash, *bslash, *basename;
  void *token;          // reuses stack out-slot in decompile
  int hit;

  if ((*(uint8_t *)((char *)ctx + 4) & 2) == 0)
    return 0;           // feature/flag bit 1 (value 2) off

  bslash = strrchr(path, '\\');
  slash  = strrchr(path, '/');
  if (bslash == NULL && slash == NULL) {
    basename = (char *)path;
  } else {
    // take later separator in memory (last path component)
    char *sep = bslash;
    if (sep == NULL || (slash != NULL && slash >= sep))
      sep = slash;
    // bytes: if both non-null, CMP bslash, slash; JBE keep slash; else MOV EAX,bslash
    // decompile: if (pcVar2 < pcVar1) pcVar2 = pcVar1; basename = pcVar2+1
    basename = /* max(bslash,slash) */ + 1;
  }

  FUN_00989e00(/*this=*/0x00d1eac8, &token, basename);
  hit = (**(code **)(**(int **)((char *)ctx + 0xc) + 0x14))(&token);
  return hit != 0;
}
```

## Offsets on context (`ctx`)

| Off | Use |
|---|---|
| `+0x4` | flags byte; **bit 1 (mask 2)** must be set or body returns 0 |
| `+0xc` | pointer to map object with vtable method at **+0x14** |

## Open questions (non-blocking)

- Product name of map object at `ctx+0xc` / method `+0x14` (owned elsewhere).
- Full semantic of flag bit 2 vs sibling bit 1 used in `NDUIWindow_LoadInterfaceFile` (`FUN_007b6a20` path).
- Exact type of interned token from `FUN_00989e00` (string id vs pointer).
