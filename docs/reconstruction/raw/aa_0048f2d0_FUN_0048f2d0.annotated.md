# Annotated low-level: FUN_0048f2d0 / PalantirEnv_ApplyDefaultScalars_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0048f2d0` |
| VA | `0x0048f2d0`–`0x0048f361` exclusive (**145 B** / `0x91`) |
| System | client / Palantir env host |
| Date | 2026-07-29 (W32-Q) |

## Machine-level notes

- ECX = env host (`8B F1` → ESI).
- Optional child reset: `ECX = [ESI+0x100]`; if non-null `call FUN_00497c80` (thiscall on child — decompiler drops formal).
- Scalar bank via SSE `movss` from image globals into env fields.
- Float4-like write at `ESI+0x60`..`+0x6c` using `LEA EAX,[ESI+0x60]` + dword stores.
- Clears `+0x178` (re-cleared later by `PalantirEnv_InitPhases_Inferred`).
- Epilogue: `POP ESI; ADD ESP,0x10; RET` — no stack formals.

## Pseudocode (annotated)

```c
// __thiscall; ECX = PalantirEnv* (0x198 host); void; bare RET
void __thiscall PalantirEnv_ApplyDefaultScalars_Inferred(void *env)
{
  void *child = *(void **)((char *)env + 0x100);
  if (child != 0)
    FUN_00497c80(child);               // thiscall; prepare/reset child list state

  *(float *)((char *)env + 0x10c) = DAT_00aaa7ac;   // 100.0f
  *(float *)((char *)env + 0x110) = DAT_00aaa884;   // 200.0f

  float *bank = (float *)((char *)env + 0x60);
  bank[0] = _DAT_009c774c;             // ~0.215646f
  bank[1] = DAT_009c7748;              // ~0.254894f
  bank[2] = DAT_009c7748;              // same
  bank[3] = g_flOne;                   // 1.0f @ 0x00a0f2a0

  *(uint32_t *)((char *)env + 0x178) = 0;
}
```

## Open questions

- Product English for scalar fields (`+0x10c/+0x110` range-like 100/200; bank at `+0x60`).
- Product name of child class at `+0x100` / `FUN_00497c80` / `FUN_00497920` (W32-P).
- Orphan re-apply site function containing `0x004d966b`.
