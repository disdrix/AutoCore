# Annotated low-level: Object_WriteComposedMatrix

| Field | Value |
|---|---|
| Stable ID | `aa_0044b5c0` |
| VA | `0x0044b5c0`–`0x0044b60f` |
| System | graphics / xform |
| Date | 2026-07-23 scaffold; dual seal annotate 2026-07-29 W22-C |

## Machine-level notes

- Source: raw + live decompile/`read_memory`/`analyze_function_complete` 2026-07-29.
- **ABI:** cdecl **1 stack arg** + **ECX** primary object + **EDI** companion object; **`ret`** (not ret N); **EAX = out**.
- Decompiler `unaff_EDI` is **required** live-in (parent `Bounds_AccumulateTransformedLocal` loads scaleSrc into EDI).
- Decompiler understates second `FUN_00412d40` as 1-arg; bytes always push 3 args + `add esp,0x0C`.
- Primary matrix source: if `flags@+0xbc` **bit1** clear → `obj+0x40`; else `FUN_009730e0(obj)` → `obj+0x40`.
- Companion: if `flags@+0xbc` **bit0** set → `FUN_00972e50(companion)` first; then `FUN_00412d40(out, companion, matA)`.

## Pseudocode (annotated)

```c
/* Object_WriteComposedMatrix
   Resolve primary object's 4x4 at +0x40 (rebuild if dirty bit1),
   optionally refresh companion transform (dirty bit0),
   compose into caller out buffer. Returns out. cdecl. */

float* Object_WriteComposedMatrix(
    ObjectLike* primary,     // ECX
    float* outMatrix,        // stack0 — 16 floats
    ObjectLike* companion)   // EDI
{
  float* matA;
  if (((*(uint32_t*)(primary + 0xbc) >> 1) & 1) == 0)
    matA = (float*)(primary + 0x40);
  else
    matA = (float*)FUN_009730e0(primary); // returns primary+0x40

  if ((*(uint8_t*)(companion + 0xbc) & 1) != 0) {
    ObjectLike* refreshed = (ObjectLike*)FUN_00972e50(companion);
    FUN_00412d40(outMatrix, refreshed, matA);
  } else {
    FUN_00412d40(outMatrix, companion, matA);
  }
  return outMatrix;
}
```

## Open questions

- Product English for ObjectLike / matrix slot `@+0x40`.
- Exact compose order inside `FUN_00412d80` (left/right multiply) — not owned.
- Whether companion path treats object base as matrix storage vs full transform object — callee-owned.
- Runtime / bit-exact.
