# Annotated low-level: CWndVehicle_CompleteDtor

| Field | Value |
|---|---|
| Stable ID | `aa_00834520` |
| VA | `0x00834520`–`0x008345b1` (**146 B**) |
| System | UI / CWndVehicle complete destructor |
| Date | 2026-08-05 (WQ9J-B re-seal) |
| Canonical name | `CWndVehicle_CompleteDtor` |
| Ghidra name | `FUN_00834520` |

## Machine-level notes

- **ABI:** `__thiscall` — ECX = `CWndVehicle*`; bare `RET`; void. SEH frame `LAB_009b4563`.
- **Role:** MSVC **complete-object destructor** only. Does **not** free `this`. Scalar peer `0x004160d0` (`CWndVehicle_ScalarDeletingDtor`, vtbl[0]) calls this then optional `operator_delete`.
- **Vtbl:** install `PTR_FUN_00a6fbcc` at `*this`. RTTI COL seals **`.?AVCWndVehicle@@`**.
- **Offsets (byte):**
  - `+0x2A8` (`this[0xAA]`): nested owned object pointer (notified here; base may free later).
  - Nested `+0x48` flag byte; `+0x40`/`+0x44` dword pair (via sub-object at nested `+0x10` → `+0x30`/`+0x34`).
  - Nested `+0x8`: interface/object used for two virtual calls (`vtbl+0x20`, `vtbl+0x10`).
- **Callees:**
  - `FUN_0076c4d0(nested+0x10)` — RDTSC timestamp into `+8/+0xC`, set byte `+0x18=1` (alive/mark helper).
  - `FUN_00833d50` — **EDI=this** helper: zero `+0x50C`, nested notify again, tear vehicle host `+0x508` (includes `Vehicle_SetWheelset(0)`).
  - `FUN_0078ca80(this)` — CNDUIWndBuffered complete dtor (`PTR_FUN_00a99f74` → chains to `FUN_007b5be0`).
- **Odd sealed behavior:** nested `@+0x2A8` notify path appears both in this body and inside `FUN_00833d50` (duplicate when non-null). Preserve both.
- Prefer assembly when decompiler elides EDI for `FUN_00833d50`.

## Pseudocode (annotated)

```c
// ECX = this (CWndVehicle*). SEH. void. bare RET.
void __thiscall CWndVehicle_CompleteDtor(undefined4 *self)
{
  // *self = &PTR_FUN_00a6fbcc;  // CWndVehicle vtbl
  // EH state = 0

  void *nested = (void *)self[0xAA]; // +0x2A8
  if (nested != NULL) {
    *(uint8_t *)((char *)nested + 0x48) = 1;
    // via nested+0x10 sub-object:
    //   *(+0x34) = *(+0x30)  i.e. nested+0x44 = nested+0x40
    FUN_0076c4d0((char *)nested + 0x10); // RDTSC stamp helper
    // stack binder { &PTR_LAB_00a9f4d8, nested }
    // vcall (*(nested+8))->vtbl[+0x20](&binder);
    // vcall (*(nested+8))->vtbl[+0x10](0);
  }

  FUN_00833d50(); // uses EDI=self (not ECX): +0x50C / nested / +0x508 vehicle clear

  // EH state = -1
  FUN_0078ca80(self); // CNDUIWndBuffered base complete
}
```

## Open residual (not OWN)

- Product English for nested object at `+0x2A8` and binder vtbl `00a9f4d8`.
- Full dual of `FUN_00833d50` / `FUN_0078ca80` / `FUN_0076c4d0` (not OWN).
- Runtime / bit-exact / differential.
