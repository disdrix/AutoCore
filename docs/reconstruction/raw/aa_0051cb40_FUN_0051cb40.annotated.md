# Annotated low-level: Map_EraseNode (FUN_0051cb40)

| Field | Value |
|---|---|
| Stable ID | `aa_0051cb40` |
| VA | `0x0051cb40`–`0x0051cdf5` (**694 B**) |
| Canonical | `Map_EraseNode` |
| Ghidra | `FUN_0051cb40` |
| System | STL / map-set (active-cast + AI aggro consumers) |
| Date | 2026-07-29 (W19-I OWN dual seal) |

## Machine-level notes

- **ABI:** `__thiscall` — `ECX = map*`, stack0 = `void **pOutIt`, stack1 = `node *pNode`, **`ret 8`**.
- **Identity:** MSVC red-black `_Tree` single-node erase. Throw string **`"invalid map/set<T> iterator"`** @ `0x00a152f0` when `*(u8*)(node+0x29) != 0` (isnil / end).
- **Family:** same node schema as `Map_ExactFindByIntKey` (`0x0051c150`) — key `@+0x10`, isnil `@+0x29`, color `@+0x28`. Distinct from smaller-node `Map_EraseNode_IntKey` (`0x005d1df0`, isnil `@+0x15`).

## Control flow (byte-sealed)

```
// ECX = map; [esp+4] = pOutIt; [esp+8] = pNode
if (*(u8*)(pNode + 0x29) != 0)  // isnil
    throw runtime_error("invalid map/set<T> iterator");

// compute successor into return-iterator local (FUN_004cb270 on a node* slot)
successor = tree_successor(pNode);   // FUN_004cb270 — isnil@+0x29 family

// standard RB erase unlink:
//   pick replace = right if left isnil else (left if right isnil else successor-in-subtree)
//   rewire parent left/right; if root → head->parent = replace
//   if begin (head->left) was pNode → update begin
//   if end-right (head->right) was pNode → update end
// if erased was black: rebalance loop with FUN_004192a0 (L-rot) / FUN_004192f0 (R-rot)

operator_delete(pNode);              // FALLS THROUGH (decomp "noreturn" is false)

if (map->size /*+0x08*/ > 0)
    map->size--;

*pOutIt = successor;
ret 8;
```

## Map / node layout

| Object | Offset | Role | Evidence |
|---|---|---|---|
| map | `+0x04` | head / sentinel node* | root/begin/end rewires |
| map | `+0x08` | size | post-delete `dec` if >0 |
| node | `+0x00` | left | unlink / successor |
| node | `+0x04` | parent | unlink |
| node | `+0x08` | right | unlink |
| node | `+0x10` | key (int) | pair with ExactFind |
| node | `+0x18+` | value payload | callers (TFID pack, etc.) |
| node | `+0x28` | color (0/1) | rebalance; also `node[10]` as char |
| node | `+0x29` | isnil | throw + child tests |

## Decompiler noise (falsified)

| Decomp artifact | Reality |
|---|---|
| `operator_delete` noreturn; body ends mid-function | Bytes fall through to size-- and `*pOutIt=` and `ret 8` |
| Ghidra body end `0x0051cdc6` | True end `0x0051cdf5` (`C2 08 00`) |
| `FUN_004cb270()` zero-arg | Successor helper; ECX = `node**` slot (family dual) |
| Chain alias `Named_CalleeOf_CVOGHBAIBase_GetTargetFromAggro_*` | Caller-only scaffold; role is generic map erase |

## Callers

| Caller | Site | Role |
|---|---|---|
| `Skill_ClearCastBindingAndMaybeRestartCd` | `0x0051a756` | erase active-cast map entry |
| `FUN_0051c7c0` | `0x0051c85f` | range erase → single erase (`&it, node`) |
| `FUN_0051d880` | `0x0051d91f` | twin range erase (same shape) |
| `CVOGHBAIBase_GetTargetFromAggro` | `0x0063914a` | prune aggro list/map node |

## Open

- Product/PDB symbol for this MSVC instantiation.
- Exact value-type of each consumer map (cast-binding TFID vs aggro payload) — owner duals.
- Runtime / bit-exact image diff.

## Pseudocode (behavior-preserving)

See `reconstructed-exact/Map_EraseNode.cpp`.
