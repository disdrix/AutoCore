# Annotated low-level: MemZero_DestSize_ReturnOne_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00788db0` |
| VA | `0x00788db0`–`0x00788dfc` exclusive (**76 B** / `0x4C`) |
| Ghidra name | `FUN_00788db0` |
| Canonical name | `MemZero_DestSize_ReturnOne_Inferred` |
| System | util / buffer zero leaf |
| Date | 2026-08-04 (W37-O re-verify; scaffold 2026-07-23) |

## Machine-level notes

- Source: raw capture + W37-O `read_memory` full body.
- **Bytes win** on `REP STOSD` / `REP STOSB`; decompiler expands stores equivalently.
- ABI **cdecl** (bare `RET`); callers `ADD ESP,8` (or co-clean with prior push).
- Always returns **1** — not CRT `memset` (which returns dest).
- **Reject** scaffold `Named_CalleeOf_*Drive_NDRiver_fx_00788db0`.

## Signature (sealed)

```c
// cdecl; dest* @ [ESP+4], size @ [ESP+8]; bare RET; returns 1
uint32_t MemZero_DestSize_ReturnOne_Inferred(void *dest, int size);
```

## Control flow

```
if ((int)size > 4) {
  n = ((size - 5) >> 2) + 1;   // floor((size-1)/4) dwords
  REP STOSD zero n dwords at dest
  size -= n*4; dest += n*4
}
if ((int)size > 0) {
  REP STOSD zero (size>>2) dwords
  REP STOSB zero (size&3) bytes
}
return 1;
```

## Call sites (sealed)

| Site | Args | Role |
|---|---|---|
| `FUN_00497920` @ `0049796b` | `(this+0x14, 0xA0)` | ListTrackedCtx ctor bank zero |
| `FUN_00497c80` @ `00497cbf` | `(this+0x14, 0xA0)` | ListTrackedCtx clear/reset bank zero |
| `FUN_00498380` @ `004985c4` | `(heap 0x52000, 0x52000)` | flair rebuild buffer zero @ `this+0xB4` |

## Pseudocode (annotated ≡ raw)

```c
uint32_t MemZero_DestSize_ReturnOne_Inferred(uint32_t *dest, uint32_t size)
{
  int iVar1;
  uint uVar2;
  int iVar3;
  uint32_t *puVar4;

  if (4 < (int)size) {
    iVar3 = (size - 5 >> 2) + 1;
    puVar4 = dest;
    for (iVar1 = iVar3; iVar1 != 0; iVar1 = iVar1 + -1) {
      *puVar4 = 0;
      puVar4 = puVar4 + 1;
    }
    size = size + iVar3 * -4;
    dest = dest + iVar3;
  }
  if (0 < (int)size) {
    for (uVar2 = size >> 2; uVar2 != 0; uVar2 = uVar2 - 1) {
      *dest = 0;
      dest = dest + 1;
    }
    for (size = size & 3; size != 0; size = size - 1) {
      *(uint8_t *)dest = 0;
      dest = (uint32_t *)((int)dest + 1);
    }
  }
  return 1;
}
```

## Gaps

- Product/MSVC helper demangle English (custom zero-return-1 vs CRT).
- Why return 1 (callback-success shape vs pure util) — open beyond call sites not consuming return.
- Runtime / bit-exact / differential.
