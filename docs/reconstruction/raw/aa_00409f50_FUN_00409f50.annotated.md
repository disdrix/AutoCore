# Annotated low-level: FUN_00409f50

| Field | Value |
|---|---|
| Stable ID | `aa_00409f50` |
| VA | `0x00409f50` |
| System | STL / vector POD helpers (elem stride 0x0C / Elem12) |
| Date | 2026-08-05 (WQ9J-A OWN dual) |

## Machine-level notes

- Source: raw capture for `aa_00409f50` + live re-verify.
- Prefer assembly / `read_memory` when decompiler invents phantom formals.
- Leaf POD element copy for **12-byte** elements (**3 dwords**).
- Callers: dualed ConstructN `StdVector_ConstructN_Elem12_Inferred` (`0x00409db0`); range twin `FUN_0040a590` (step +0x0C both cursors; residual).
- **ABI family trap:** this leaf is **EAX dest / ECX src**; peer `00409f30` (0x28) is **EAX dest / EDX src**.

## Pseudocode (annotated)

```c
// ABI: EAX=dest, ECX=src; plain ret. Decomp "fastcall param_1" is src (ECX).
void StdVector_PodCopyElement_Elem12_Inferred(void /*EAX dest*/, void /*ECX src*/)
{
  if (dest != nullptr) {
    // three dword stores: [0], [4], [8]
    memcpy(dest, src, 0x0C);  // dword-aligned POD only
  }
}
```

## Sealed facts

| Fact | Evidence |
|---|---|
| Body 21 B | Ghidra body `00409f50`–`00409f64` + bytes |
| 3-dword copy | three MOV pairs; no REP |
| Null dest skip | `TEST EAX,EAX` / `JZ` past stores |
| EAX/ECX ABI | callers `MOV EAX,dest; MOV ECX,src` then CALL |
| Stride consumer | ConstructN advances dest `+0x0C` after each call |

## Open questions

- Product / PDB English for the 12-byte POD element type.
- Full dual of range twin `FUN_0040a590` (role sealed as +0x0C range).
- Runtime / bit-exact / differential.

## Name

**StdVector_PodCopyElement_Elem12_Inferred** — role/CF sealed; product demangle open → `_Inferred`.
