# Annotated low-level: Class_00a70524_ScalarDeletingDtor_Inferred (FUN_004165f0)

| Field | Value |
|---|---|
| Stable ID | `aa_004165f0` |
| VA | `0x004165f0`–`0x0041660E` exclusive (**30 B** / `0x1E`) |
| System | MSVC scalar-deleting destructor / `PTR_FUN_00a70524` |
| Date | 2026-08-05 (WQ9I-J re-verify over 2026-07-23 scaffold) |
| Canonical name | `Class_00a70524_ScalarDeletingDtor_Inferred` (**Inferred**) |
| Ghidra name | `FUN_004165f0` |

## Machine-level notes

- Source: live `decompile_function` + full-body `read_memory` + `analyze_function_complete` + xrefs.
- Classic MSVC scalar-deleting dtor (vtbl[0]): complete dtor always; `operator_delete(this)` iff `(flags & 1)`.
- Complete callee is `FUN_008327f0` — installs `PTR_FUN_00a70524`, then chains base complete `FUN_0082d540`.
- ABI: ECX=this; stack flags; **RET 4**; returns this in EAX.
- DATA xref only: dword at `0x00a70524` = `0x004165f0`.
- Decompiler “operator_delete does not return” is false when flags&1; epilogue still returns this.
- Peer ctor (not owned) `FUN_00832830` installs same vtbl and calls `FUN_0082cfb0("i_m_chatlog.xml")` → UI chatlog class evidence.
- **Not** related to neighbor `FUN_00416590` (ptr-vec shell ctor) beyond VA proximity.

## Pseudocode (annotated)

```c
// ECX = this; stack flags; RET 4; returns this*
void* __thiscall Class_00a70524_ScalarDeletingDtor_Inferred(void* self, uint8_t flags)
{
  FUN_008327f0(self);  // complete: *self = &PTR_FUN_00a70524; FUN_0082d540(self)
  if ((flags & 1u) != 0u) {
    operator_delete(self);
  }
  return self;
}
```

## Vtbl `PTR_FUN_00a70524` @ `0x00a70524`

| Slot | VA | Notes |
|---|---|---|
| [0] | `0x004165f0` | this function (scalar-deleting) |
| [1] | `0x00790cc0` | open |
| [2] | `0x007a7de0` | open |
| [3] | `0x007567e0` | open |

## Open questions

- Product/MSVC demangle for class owning `00a70524` (chatlog string is peer-ctor only).
- Vtbl slots [1+] English (not owned).
- Complete `FUN_008327f0` / base `FUN_0082d540` dual residual.
- Runtime / bit-exact / differential — open.
