# Annotated: aa_004e7350 StdMap_Dtor_Val12

| Field | Value |
|---|---|
| **Stable ID** | `aa_004e7350` |
| **VA** | `0x004e7350` |
| **Canonical** | `StdMap_Dtor_Val12` (Inferred) |
| **Ghidra** | `FUN_004e7350` |
| **Date** | 2026-07-29 (W29-M) |

## Role

MSVC-style **std::map / `_Tree` destructor body** for **Val12** nodes (`isnil@+0x19`, value@`+0x0c`, node size `0x1c`):

1. Full-range erase `[begin, end)` via `StdMap_EraseRange_Val12` (`FUN_004e5120`, W28-P) — frees all value nodes; resets head links / size0 on the sentinel.
2. `operator_delete(head)` — frees the header/sentinel node itself.
3. `head = 0; size = 0`.
4. `ret` (`C3`).

## Layouts

```
MapShell {
  ...
  Node* head;   // +4
  size_t size;  // +8
};

// Erase args (thiscall on map):
//   outIt*, first=begin=*head, last=head
```

## ABI

- `__thiscall` — ECX = map*
- No stack args; **RET 0** (`C3`)
- Body **46 B** (`0x004e7350`–`0x004e737e` exclusive); Ghidra end `0x004e736f` stale

## Call graph

| Dir | Symbol | Note |
|---|---|---|
| callee | `FUN_004e5120` | StdMap_EraseRange_Val12 (W28-P); sole parent of that unit |
| callee | `operator_delete` | free head; **falls through** (decomp false-noreturn) |
| caller | `FUN_004d64d0` | large object dtor; **two** sites (`004d6970`, `004d697b`) |

## Decompile residuals

- Scaffold/raw omit post-delete null stores — **sealed from `read_memory`**.
- `operator_delete` marked noreturn in decompiler — **false**.

## Confidence

| Claim | Level |
|---|---|
| Clear-all + free head + null | **High** |
| Val12 via erase-range callee | **High** |
| `ret` / thiscall | **High** |
| Product demangle / map type params | **Inferred** |
| Runtime / bit-exact | Open |
