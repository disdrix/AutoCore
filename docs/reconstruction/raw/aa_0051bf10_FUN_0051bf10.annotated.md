# Annotated low-level: FUN_0051bf10 / ObjectCsListBase_ScalarDeletingDtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051bf10` |
| VA | `0x0051bf10`–`0x0051bf3e` exclusive (**46 B**) |
| System | object core / CS-owned list teardown |
| Date | 2026-07-29 (W31-R) |
| Semantic name | `ObjectCsListBase_ScalarDeletingDtor_Inferred` |

## Machine-level notes

- Source: raw + live `read_memory` full body hex (W31-R).
- DATA-only xref: vtbl `PTR_FUN_009ce07c` slot0 → this (virtual scalar deleting dtor).
- `TEST [ESP+8],1` after `PUSH ESI` targets stack `flags` (thiscall + 1 stack arg; `RET 4`).
- Ghidra marks `operator_delete` noreturn — **false**; epilogue always returns `this` in EAX.
- Base-only phase: installs `009ce07c`, one clear, DeleteCS — no derived `009ce154` install (contrast complete dtor `FUN_0051d0e0`).

## Pseudocode (annotated)

```c
// __thiscall; ECX=list*; stack flags; RET 4; returns this
// Scalar deleting dtor for base vtbl PTR_FUN_009ce07c.
uint32_t * __thiscall ObjectCsListBase_ScalarDeletingDtor_Inferred(
    uint32_t *self, uint8_t flags)
{
  *self = (uint32_t)&PTR_FUN_009ce07c;
  FUN_0051bc90(self);
  DeleteCriticalSection((LPCRITICAL_SECTION)(self + 1));
  if ((flags & 1) != 0) {
    operator_delete(self); // cdecl
  }
  return self;
}
```

## Open questions

- Product base class English for `PTR_FUN_009ce07c`.
- Array-deleting path (`flags&2`) not present in body — only bit0 handled (standard MSVC scalar).
- Relationship of base-only destroy vs derived complete path when clients hold base pointers.
