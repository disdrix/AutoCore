# Raw capture: FUN_0076cf00

| Field | Value |
|---|---|
| **Stable ID** | `aa_0076cf00` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0076cf00` |
| **Canonical name** | `FUN_0076cf00` |
| **System** | client instrumentation / profiler |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __fastcall FUN_0076cf00(int param_1)

{
  return *(undefined4 *)(param_1 + 4);
}
```

---

## Live refresh — 2026-07-29 (W18-G dual seal)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` + `get_function_by_address` + `get_xrefs_to` / callers (no `disassemble_bytes`) |
| **Program** | `autoassault.exe` |
| **Body** | `0x0076cf00` – `0x0076cf05` |
| **Body bytes** | `8b 41 04 c2 04 00` = `mov eax, [ecx+4]` ; `ret 4` |
| **Live decompile** | ≡ original raw (ECX this / fastcall param_1; stack name not shown) |
| **Xrefs** | High (inventory ~120); sample callers load `DAT_00d1f040` into ECX then push zone name string |

### Live decompile (≡ original raw)

```c
undefined4 __fastcall FUN_0076cf00(int param_1)

{
  return *(undefined4 *)(param_1 + 4);
}
```

### Machine ABI (from body + call sites; decompiler incomplete)

```text
// ECX = *DAT_00d1f040  (profiler context / current zone object)
// stack0 = const char* name   (IGNORED in this build)
// returns *(ECX+4) in EAX    (timer block*; zone ctor stores timer at +4)
// ret 4
```

### Call-site pattern (examples)

| Site | Pattern |
|---|---|
| `FUN_004a6390` @ `0x004a63b7` | `mov esi, [DAT_00d1f040]`; `push "LoadMasterFromXML"`; `mov ecx,esi`; `call 0x0076cf00` |
| `FUN_004d6c80` @ `0x004d6c99` | `mov ebp, [DAT_00d1f040]`; `push "CVOGSectorMap::StepTo"`; `mov ecx,ebp`; `call` |
| `FUN_004fc650` | same global + `"CVOGVehicle::MoveToTarget3DPoint"` |

### Sibling leave

| VA | Bytes | Role |
|---|---|---|
| `0x0076cef0` | `c3` (`ret`) | Scope leave no-op |

### Naming (this dual)

Probable: **`Profiler_ScopeEnter`** — stub enter that discards name and returns timer at `ctx+4`. Product plate name open.
