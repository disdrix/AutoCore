# Annotated low-level: FUN_00540890 / Host_AIProfileMap_GetOrReset

| Field | Value |
|---|---|
| Stable ID | `aa_00540890` |
| VA | `0x00540890`–`0x005408e9` (90 B inclusive last RET) |
| System | npc-ai / host tables / profile map |
| Date | 2026-07-29 (W26-N dual seal) |
| Role name | `Host_AIProfileMap_GetOrReset` (Probable from HBAI callers + MSVC map clear shape) |

## Machine-level notes

- **ABI:** `__thiscall`; ECX=host; stack `char forceReset`; **`RET 4`**; **EAX** = `this+0xF00`.
- **Entry:** `57 8B F9` — EDI=this.
- **Gate:** `CMP dword [EDI+0xF08],0` / `JE clear`; else `CMP byte [ESP+8],0` / `JE skip` (skip when size≠0 and forceReset==0).
- **Clear sequence:** classic MSVC `_Tree` empty: free root, head↔self on left/parent/right, size=0.
- **`FUN_005436c0`:** recursive free of non-nil nodes (`operator_delete`) — not owned here.
- **`FUN_007c3b80`:** called with `MOV ECX,[0x00b0469c]; PUSH map; CALL` — global service, not map thiscall.
- Callers (HBAI DoLogic family) pair with `FUN_004cbd50` lower_bound on float key from def `+0x4DC`.
- Prefer decompile CF + bytes; **no** `disassemble_bytes`.

## Pseudocode (annotated)

```c
// thiscall ECX=host (DAT_00b041fc at call sites); stack char forceReset; RET 4
// Returns map at host+0xF00 for AI profile lower_bound

int __thiscall Host_AIProfileMap_GetOrReset(int host, char forceReset)
{
  if ((*(int *)(host + 0xf08) == 0) || (forceReset != '\0')) {
    // free tree from head->parent; re-link head; size=0; notify FUN_007c3b80
  }
  return host + 0xf00;
}
```

## Open questions

- Product type name of host at `DAT_00b041fc` and map value_type English.
- Exact contract of `FUN_007c3b80` (rebuild / register / lock).
- Whether `size==0` branch is pure re-init or also first-use construction side effect inside `FUN_007c3b80`.
