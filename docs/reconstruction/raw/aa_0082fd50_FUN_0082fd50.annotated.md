# Annotated low-level: FUN_0082fd50 → UI_InteractionMenu_AddNameplateLabel_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0082fd50` |
| VA | `0x0082fd50`–`0x0082fe20` exclusive |
| Body size | **208 B** (`0xD0`) |
| Ghidra | `FUN_0082fd50` |
| Named | `UI_InteractionMenu_AddNameplateLabel_Inferred` |
| Prior alias | `UI_int_btn_generic_0082fd50` (plate-only; superseded for role) |
| System | interaction menu / skills-abilities (partition) / social UI |
| Date | 2026-08-05 MEGA-084 |

## Machine-level notes

- Source: raw capture + live Ghidra `decompile_function` / `disassemble_function` / `read_memory` / parent `FUN_0082fe20`.
- Prefer assembly when decompiler elides stack args on `vtbl+0x444` (4 args sealed in asm).
- Register ABI from parent call sites, not formal prototype.

## ABI

| Register | Role |
|---|---|
| **EDI** | menu host* (parent `mov edi,esi`) |
| **EBX** | owner/target object* (parent `mov ebx,ebp` = `target.vtbl+0x210(0)`) |
| Stack | none (local color buffer only) |
| Return | void; **`ret 0`** (`C3` after `add esp,8`) |

## Control flow (annotated)

```c
// EBX = owner/target; EDI = menu host
void UI_InteractionMenu_AddNameplateLabel_Inferred(void /* regs */)
{
  // 1) Resolve display name via object adj/RTTI chain + vtbl+0x160
  //    ECX = *( *( *(EBX+4) + 4 ) + EBX + 4 ); CALL [vtbl+0x160]
  name = Object_GetDisplayName_vtbl160(EBX);

  // 2) Create generic interaction button (id 0x1117F)
  //    host.vtbl+0x444(xml, 0, 0x1117F, -1)
  btn = menu.vtbl_create(EDI, "i_m_int_2d_btn_generic.xml", 0, 0x1117F, -1);

  // 3) Label with dynamic name (not a fixed "Invite"/"Tell" string)
  if (btn) btn.vtbl_setText(btn, name, 1, 1);

  // 4) Store on host nameplate slot
  *(EDI + 0x53C) = btn;   // EDI[0x14F]

  if (btn) {
    // 5) Always non-interactive (display label, not action button)
    btn.vtbl_disable(btn, 0);   // vtbl+0xD4(0)

    // 6) Color by target+0x6B4 gate (same numeric field as convoy pack)
    if (*(int*)(EBX + 0x6B4) < 1)
      color = 0xFF1EA0B4;   // teal-ish ARGB
    else
      color = 0xFFA01EB4;   // magenta-ish ARGB

    btn.vtbl_setColor(btn, 1, &color);   // vtbl+0x15C
    *(btn + 0x170) = 0xFFFFFFFE;         // style / z / sort sentinel (-2)
    btn.vtbl_finalize(btn, 1);           // vtbl+0x1E4(1)
  }
}
```

## Layout / contracts

| Offset / slot | Role |
|---|---|
| host `+0x53C` | nameplate button pointer (`EDI[0x14F]`) |
| target `+0x6B4` | numeric gate for color branch (English open; shared with convoy pack) |
| btn `+0x170` | written `-2` (`0xFFFFFFFE`) |
| object `vtbl+0x160` | display-name getter |
| host `vtbl+0x444` | button factory from XML + id |
| btn `vtbl+0x1D8` | set label text |
| btn `vtbl+0xD4` | disable / non-interactive |
| btn `vtbl+0x15C` | set color (flag + ARGB*) |
| btn `vtbl+0x1E4` | post-setup enable/show |

## Parent pack order (evidence only; dualed parent)

`FUN_0082fe20` (`UI_InteractionMenu_PopulateTargetButtons_Inferred`):

| Flag `vtbl+0x478` | Order (this unit = Name) |
|---|---|
| `== 0` | Ignore, Friends, Tell, Trade, Clan, Convoy, **Name** (`0x0082FF22`) |
| `!= 0` | **Name** (`0x0082FEC0`), Convoy, Clan, Trade, Tell, Friends, Ignore |

## Open questions

1. Product class / MSVC name for menu host.
2. English for `+0x6B4` (level vs rank vs faction flag).
3. Semantic meaning of the two ARGB colors (hostile/friendly? PvP? rank band?).
4. Exact English for `vtbl+0x15C` / `+0x1E4` / `btn+0x170`.
5. Runtime / bit-exact (Terminal false).
