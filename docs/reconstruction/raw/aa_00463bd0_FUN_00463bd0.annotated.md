# Annotated low-level: NestedHash_Ctor_Sentinel0x30_Inferred (`FUN_00463bd0`)

| Field | Value |
|---|---|
| Stable ID | `aa_00463bd0` |
| VA | `0x00463bd0`–`0x00463c31` (**98 B** / `0x62`) |
| System | shared nested hash/map shell (list head + 9-bucket ptr table) |
| Date | 2026-07-29 (W34-F dual seal; scaffold 2026-07-23) |
| Canonical name | `NestedHash_Ctor_Sentinel0x30_Inferred` |

## Machine-level notes

- **ABI (bytes win):** stack formal `self*`; **`RET 4`**; returns `self` in EAX. Not ECX-thiscall.
  - After SEH + `push ecx` + `push esi`: `MOV ESI,[ESP+0x18]` = stack this.
  - Epilog: `MOV EAX,ESI`; restore FS:0; `ADD ESP,0x10`; `C2 04 00`.
- **SEH:** handler `LAB_009bd31b`; scope `-1` → `0` after sentinel install.
- **Callees:**
  - `FUN_005b43b0` — `operator_new(0x30)` + circular next/prev (same CF as `CircularSentinel_Alloc0x28` / W33-B with size **0x30**).
  - `FUN_004646e0` / `PtrVecShell_InitFill_Inferred` (W33-B) — stdcall 3-arg, **count=9**, fill = &sentinel local.
- **Caller (1):** `FUN_0075f7f0` @ `0x0075fa63` allocates **`0x28`** then calls this; stores result at host slot `param_2[0x1d6]` / global `DAT_00d1f628`.
- **Family twin:** identical 98 B CF to `AssResolverA_NestedHash_Ctor_Inferred` (`0x00423cf0`) and `FUN_00469db0`; only sentinel factory differs.
- Prefer bytes for ABI/`RET 4`; decompiler CF matches stores.

## Pseudocode (annotated)

```c
// stdcall 1 formal; RET 4; returns self
// Ghidra: int FUN_00463bd0(int param_1)
NestedHash* NestedHash_Ctor_Sentinel0x30_Inferred(NestedHash* self /*stack*/)
{
  // SEH: LAB_009bd31b, state -1
  void* sentinel = FUN_005b43b0();       // new(0x30) circular
  self->sentinel = sentinel;             // +0x08
  self->size = 0;                        // +0x0c
  // state = 0
  void* fill = self->sentinel;
  PtrVecShell_InitFill_Inferred(&self->shell, /*count*/9, &fill); // +0x10
  self->flag20 = 1;                      // +0x20
  self->flag24 = 1;                      // +0x24
  return self;
}
```

## Layout (span 0x28)

| Off | Field | Written |
|---|---|---|
| +0x00 | pad | no |
| +0x04 | pad | no |
| +0x08 | sentinel* (0x30 node) | yes |
| +0x0c | size | 0 |
| +0x10 | shell+0 | InitFill leaves unwritten |
| +0x14..+0x1c | begin/end/end_cap | via InitFill |
| +0x20 | flag | 1 |
| +0x24 | flag | 1 |

## Open questions

- Product/PDB class English for this nested shell (multi-host).
- Semantic meaning of flags +0x20/+0x24 and unwritten +0x00/+0x04.
- Full product role of sole host `FUN_0075f7f0` slot `0x1d6`.
- Runtime / bit-exact / differential.
