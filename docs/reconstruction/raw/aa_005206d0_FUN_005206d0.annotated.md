# Annotated low-level: FUN_005206d0 → ReverseEngineer_CeilCostToLevelByTier_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005206d0` |
| VA | `0x005206d0`–`0x00520829` inclusive (**346 B** / `0x15A`) |
| Jump table | data `0x0052082c`–`0x00520843` (6× dword) |
| System | inventory-transfer / reverse-engineer |
| Date | 2026-07-23 scaffold; **2026-08-05 R12-019 dual seal** |
| Canonical name | `ReverseEngineer_CeilCostToLevelByTier_Inferred` |
| Ghidra name | `FUN_005206d0` |
| Prior scaffold | `Named_CalleeOf_Named_CalleeOf_Skill_You_do_not_possess_the_Tink_005206d0` (**retired**) |

## Machine-level notes

- Pure **stdcall** helper: two stack args, **`RET 8`**, no ECX/this.
- Switch on **tier / row-count selector** (0..5); default → 0.
- Each case: `FILD cost` × LE float **inv_mult** → CRT **`ceil`** → FISTP ROUND → **EAX int**.
- Inv mults = `1/{3.5, 3.5, 2.8, 2.1, 1.4, 0.7}` — inverse of dualed capacity mults on `0x00520520`.
- Sole caller: parent dual `Character_ComputeReverseEngineerRequiredLevel_Inferred` (`0x0052e1d0`).
- Prefer body bytes + jump table when decompiler signature shows `void`.

## Pseudocode (annotated)

```c
// stdcall: stack tier, stack cost; EAX = ceil(cost * inv_mult[tier]); RET 8
// tier>5 → 0. Cases 0 and 1 share inv = 1/3.5.
int __stdcall ReverseEngineer_CeilCostToLevelByTier_Inferred(
    unsigned tier,
    int cost)
{
  switch (tier) {
  case 0:
  case 1:
    // DAT_009cdff4 = 0.2857143f = 1/3.5
    return (int)ceil((double)((float)cost * 0.2857143f));
  case 2:
    // DAT_009cefcc = 0.3571429f = 1/2.8
    return (int)ceil((double)((float)cost * 0.3571429f));
  case 3:
    // DAT_009cefc8 = 0.4761905f = 1/2.1
    return (int)ceil((double)((float)cost * 0.4761905f));
  case 4:
    // DAT_00aaa7c4 = 0.7142857f = 1/1.4
    return (int)ceil((double)((float)cost * 0.7142857f));
  case 5:
    // DAT_009cefc4 = 1.4285715f = 1/0.7
    return (int)ceil((double)((float)cost * 1.4285715f));
  default:
    return 0;
  }
}
```

## Parent use (evidence only)

```text
required = max(
  ceil(primary_cost * 1/3.5),                 // parent inlines DAT_009cdff4
  FUN_005206d0(row_count, total_secondary),   // this VA
  FUN_005206d0(t, row_cost[t]) for t=1..5     // this VA
)
```

## Open questions

- Product English for tier index vs recipe-row ordinal (`row_count` vs 1..5).
- Whether server enforces the same ceil mapping (client sealed only).
- Runtime / bit-exact / differential.
