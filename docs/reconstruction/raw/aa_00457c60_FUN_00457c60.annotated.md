# Annotated low-level: StdList_AllocSentinel_0x18_Inferred (`FUN_00457c60`)

| Field | Value |
|---|---|
| Stable ID | `aa_00457c60` |
| VA | `0x00457c60`–`0x00457c7a` exclusive (**26 B**) |
| System | shared `std::list`-style sentinel factory |
| Date | 2026-07-29 (W30-E refine) |
| Ghidra name | `FUN_00457c60` |
| Canonical | `StdList_AllocSentinel_0x18_Inferred` |

## Machine-level notes

- Source: raw capture + W30-E live `read_memory` / decompile.
- Decompiler return type `void` is **wrong** — callers consume **EAX** as node*.
- Sibling: `StdList_AllocEmptySentinel_Inferred` (`0x0040fb90`) allocates **0x0C**; this unit allocates **0x18**.
- Only first **8** bytes of the block are written (next/prev self-links). Trailing **0x10** bytes left untouched by this function.

## Pseudocode (annotated)

```c
// no formals; returns node* in EAX; bare RET
ListNode18* StdList_AllocSentinel_0x18_Inferred(void)
{
  ListNode18* node = (ListNode18*)operator_new(0x18);
  if (node != nullptr) {
    node->next = node;   // +0x00
  }
  // LEA ECX,[EAX+4]; TEST ECX — always true if EAX==0 (writes [4]=0 on OOM)
  // Under throwing operator_new the null path is dead.
  if ((uint32_t*)((uint8_t*)node + 4) != nullptr) {
    *(uint32_t*)((uint8_t*)node + 4) = (uint32_t)node;  // prev = self @ +0x04
  }
  return node;  // EAX
}
```

## Call-site contract

Callers treat result as **circular empty-list head**:

| Caller | Head store | Size store |
|--------|------------|------------|
| `FUN_00457b30` | `obj+0x08` | `obj+0x0C = 0` |
| `Class_009c7a1c_Ctor` (`00496f70`) | list `+0x10` (list base `+0x0C`) | `+0x14 = 0` |
| `Object_009d33c4_Ctor` (`00575440`) | list `+0x1E0` (list base `+0x1DC`) | `+0x1E4 = 0` |

## Open questions

- Product element type that needs 0x18 node (payload after 8-byte link header).
- Whether any caller relies on zeroed trailing 0x10 (not written here).
