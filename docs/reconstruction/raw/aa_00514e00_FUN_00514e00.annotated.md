# Annotated low-level: FUN_00514e00

| Field | Value |
|---|---|
| Stable ID | `aa_00514e00` |
| VA | `0x00514e00` |
| System | skills-abilities |
| Date | 2026-07-29 (dual A/B residual) |
| Name status | **leave-FUN** — retain `FUN_00514e00` |

## Machine-level notes

- Source: raw capture for `aa_00514e00`; dual A/B re-decompile 2026-07-29 matches raw body.
- Leaf: no callees, no globals.
- `param_1 + 100` ≡ **`this+0x64`** (decimal). Slot is optional pointer to 0x14 block (alloc via `FUN_005169c0` elsewhere).
- `block+0x10`: active-cast / busy **refcount** (LocalCastValidate: nonzero → eSkillResponses 6).
- `block+0xC`: cleared when refcount hits 0 (queue-id / head linkage **INFERRED** via ClearActiveCastCounter residual).
- Does **not** lazy-alloc; does **not** queue deferred cast id.
- Callers: RecvSkillStatusEffect (3), ApplyStatusEffectLocal (1), FUN_00606180 (1).

## Pseudocode (annotated)

```c
// leave-FUN: FUN_00514e00 @ 0x00514e00
// Role (High, not registry): null-safe dec of active-cast counter on slot at this+0x64
void __fastcall FUN_00514e00(int param_1 /* owner this; product type open */)
{
  int *pSlot;   // optional 0x14 block at this+0x64
  int nCount;

  pSlot = *(int **)(param_1 + 0x64);          // decomp: +100
  if (pSlot != 0 && pSlot[0x10/4] != 0) {     // *(block+0x10)
    nCount = pSlot[0x10/4] - 1;
    pSlot[0x10/4] = nCount;
    if (nCount == 0) {
      pSlot[0xc/4] = 0;                       // clear +0xC when zero
    }
  }
  return;
}
```

## Dual A/B

- Pass A / Pass B decompile: **match** (force refresh identical).
- Reviews: `A_aa_00514e00_FUN_00514e00.md`, `B_aa_00514e00_FUN_00514e00.md`.
- Naming: **leave-FUN** (no string/RTTI).

## Open questions

- Product type of ECX owner.
- Product name for +0xC; who increments +0x10.
- Exact ECX at Recv / WakeupSkill call sites.
