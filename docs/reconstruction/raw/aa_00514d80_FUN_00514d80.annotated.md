# Annotated low-level: FUN_00514d80 / Object_StopTypeF_ReloadEventVariant_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00514d80` |
| VA | `0x00514d80`–`0x00514df3` (116 B) |
| System | client-fx / type-0xF stop + event-variant reload |
| Date | 2026-07-29 (W28-H; scaffold 2026-07-23) |
| Canonical (inferred) | `Object_StopTypeF_ReloadEventVariant_Inferred` |

## Machine-level notes

- Shares primary FX* vector layout with `Object_TeardownOwnedFxLists_Inferred` (`+0x124/+0x128`).
- Type code at FX host `+0x28` compared to **`0x0F` (15)** — same event-id family as sealed `FxMaster_LoadEventVariant_Inferred` callers.
- Stop path uses sealed `NDSpecialFX_StopFinalize_Inferred` only (no TeardownCore / delete in this unit).
- Catalog ensure: sealed peer `FUN_004eb3b0` on object`+0xA8` (clonebase / graphics host with `+0x88` catalog slot).
- Reload: `FxMaster_LoadEventVariant_Inferred(catalog, 0xF, 1, -1)` then virtual attach/apply at **vtbl + 0xF8**.
- Public entry often via MI thunk `FUN_00581500` (`ret 4` ⇒ one stack formal on the virtual; impl ignores it).

## Pseudocode (byte-corrected)

```c
// thiscall impl; plain ret
// public virtual (via 00581500 thunk): thiscall + 1 ignored stack dword, ret 4 on thunk
void __thiscall Object_StopTypeF_ReloadEventVariant_Inferred(void *self)
{
  void **it  = *(void ***)((char *)self + 0x124);
  void **end = *(void ***)((char *)self + 0x128);
  void *linked;
  void *fx;

  if (it != end) {
    do {
      void *entry = *it;
      if (*(int *)((char *)entry + 0x28) == 0x0F)
        NDSpecialFX_StopFinalize_Inferred(entry);
      it = (void **)((char *)it + 4);
    } while (it != *(void ***)((char *)self + 0x128));
  }

  linked = *(void **)((char *)self + 0xA8);
  FUN_004eb3b0(linked);                         /* Ensure catalog host resource */
  if (*(void **)((char *)linked + 0x88) != 0) {
    FUN_004eb3b0(linked);
    fx = FxMaster_LoadEventVariant_Inferred(
        *(void **)((char *)linked + 0x88),
        0x0Fu, 1u, -1);
    if (fx != 0) {
      void **vtbl = *(void ***)self;
      ((void (__thiscall *)(void *, void *, int, int))vtbl[0xF8 / 4])(
          self, fx, 1, 0);
    }
  }
}
```

## Open questions

- Product name for event 0xF / vtbl+0xF8 apply method.
- Whether `+0xA8` is clonebase, graphics body, or another shared host (layout peer of Ensure callers).
- Runtime golden for stop-without-free then reload attach.
