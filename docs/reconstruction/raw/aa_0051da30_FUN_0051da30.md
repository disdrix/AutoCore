# Raw capture: FUN_0051da30

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051da30` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051da30` |
| **Canonical name** | `FUN_0051da30` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0051da30(int param_1)

{
  int local_4;
  
  local_4 = param_1;
  FUN_0051c7c0(&local_4,**(undefined4 **)(param_1 + 4),*(undefined4 **)(param_1 + 4));
                    /* WARNING: Subroutine does not return */
  operator_delete(*(void **)(param_1 + 4));
}
```
## Re-verify — 2026-08-04 WQ9E-J OWN dual

| Field | Value |
|---|---|
| **Tool** | Ghidra `batch_decompile` + `analyze_function_complete` + `read_memory` + xrefs/callers |
| **Disallowed** | `disassemble_bytes`; Launcher |
| **Live ≡ raw CF (core)** | **Yes** for erase+delete order; **decomp truncated** after `operator_delete` |
| **Bytes authority** | After `operator_delete(head)`: **`head=0; size=0`**; bare **`ret`** (`C3`) — decompiler false noreturn omitted this |
| **Body** | `0x0051da30`–`0x0051da5d` inclusive (**46 B** / `0x2E`); exclusive end `0x0051da5e`; `CC` pad |
| **ABI** | ECX=map* (`56 8B F1`); bare `ret`; void; no stack formals |
| **Callees** | dualed `Map_EraseRange` (`0x0051c7c0`) full range; `operator_delete` sentinel head |
| **Callers** | `FUN_00518ec0` @ `0x00518fd3` (object map slot `+0x68` teardown); SEH `Unwind@009a8e4c` |
| **Name** | `Map_Tidy_FreeHead` — peer shape of `StdTree_Tidy_FreeHead_Bare_Val12` (same 46 B tidy template) |
| **Product host class** | Open |
| **Runtime / bit-exact** | Open |

### Body hex (46 B) — authority for post-delete epilogue

```
51568bf18b46048b0850518d44240c508bcee879edffff8b4e0451e8d2bdf6ff83c40433c08946048946085e59c3
```

### Byte CF

```
PUSH ECX                  ; out_it slot
PUSH ESI
MOV ESI, ECX              ; map*
MOV EAX, [ESI+4]          ; head
MOV ECX, [EAX]            ; *head = begin
PUSH EAX                  ; last = head
PUSH ECX                  ; first = *head
LEA EAX, [ESP+0xC]
PUSH EAX                  ; &out
MOV ECX, ESI
CALL Map_EraseRange       ; 0x0051c7c0
MOV ECX, [ESI+4]
PUSH ECX
CALL operator_delete
ADD ESP, 4
XOR EAX, EAX
MOV [ESI+4], EAX          ; head = 0  (decomp missed)
MOV [ESI+8], EAX          ; size = 0  (decomp missed)
POP ESI
POP ECX
RET
```

