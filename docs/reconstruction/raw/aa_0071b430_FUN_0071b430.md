# Raw capture: FUN_0071b430

| Field | Value |
|---|---|
| **Stable ID** | `aa_0071b430` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0071b430` |
| **Canonical name** | `FUN_0071b430` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0071b430(undefined4 *param_1,int param_2)



{

  int iVar1;

  byte *pbVar2;

  uint uVar3;

  int iVar4;

  uint uVar5;

  uint uVar6;

  undefined4 *puVar7;

  char *local_8;

  int local_4;

  

  puVar7 = param_1;

  for (iVar4 = 0x200; iVar4 != 0; iVar4 = iVar4 + -1) {

    *puVar7 = 0;

    puVar7 = puVar7 + 1;

  }

  local_4 = 0;

  iVar4 = 0;

  do {

    uVar6 = 0;

    do {

      local_8 = (char *)(param_2 + 3);

      uVar3 = 1;

      do {

        uVar5 = (int)*(char *)((uVar3 - 1) + param_2) - 1;

        if (((int)uVar5 >> 2 == local_4) &&

           ((*(uint *)(&DAT_00afcd88 + (uVar5 & 3) * 4) & uVar6) != 0)) {

          pbVar2 = (byte *)((int)param_1 + (iVar4 + uVar6) * 8 + ((int)(uVar3 - 1) >> 3));

          *pbVar2 = *pbVar2 | (&DAT_00afcd68)[(uVar3 - 1 & 7) * 4];

        }

        uVar5 = (int)*(char *)(uVar3 + param_2) - 1;

        if (((int)uVar5 >> 2 == local_4) &&

           ((*(uint *)(&DAT_00afcd88 + (uVar5 & 3) * 4) & uVar6) != 0)) {

          pbVar2 = (byte *)((int)param_1 + (iVar4 + uVar6) * 8 + ((int)uVar3 >> 3));

          *pbVar2 = *pbVar2 | (&DAT_00afcd68)[(uVar3 & 7) * 4];

        }

        if (((int)((int)local_8[-1] - 1U) >> 2 == local_4) &&

           ((*(uint *)(&DAT_00afcd88 + ((int)local_8[-1] - 1U & 3) * 4) & uVar6) != 0)) {

          pbVar2 = (byte *)((int)param_1 + (iVar4 + uVar6) * 8 + ((int)(uVar3 + 1) >> 3));

          *pbVar2 = *pbVar2 | (&DAT_00afcd68)[(uVar3 + 1 & 7) * 4];

        }

        if (((int)((int)*local_8 - 1U) >> 2 == local_4) &&

           ((*(uint *)(&DAT_00afcd88 + ((int)*local_8 - 1U & 3) * 4) & uVar6) != 0)) {

          pbVar2 = (byte *)((int)param_1 + (iVar4 + uVar6) * 8 + ((int)(uVar3 + 2) >> 3));

          *pbVar2 = *pbVar2 | (&DAT_00afcd68)[(uVar3 + 2 & 7) * 4];

        }

        local_8 = local_8 + 4;

        iVar1 = uVar3 + 3;

        uVar3 = uVar3 + 4;

      } while (iVar1 < 0x40);

      uVar6 = uVar6 + 1;

    } while ((int)uVar6 < 0x10);

    local_4 = local_4 + 1;

    iVar4 = iVar4 + 0x10;

  } while (iVar4 < 0x100);

  return;

}
```


---

## Live re-verify append — 2026-07-29 W25-Q OWN-ONLY dual seal

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` + `get_function_by_address` + callers (no `disassemble_bytes`) |
| **Program** | `autoassault.exe` base `0x400000` |
| **Body** | `0x0071b430`–`0x0071b59d` exclusive (**365** B) |
| **Canonical (sealed)** | `DES_BuildIPFPLookupTables` |
| **Live decompile** | ≡ original raw body (byte-identical CF) |

### Entry / epilogue bytes (`read_memory`)

```
0071b430  83 ec 08           sub  esp, 8
0071b433  53                 push ebx
0071b434  55                 push ebp
0071b435  8b 6c 24 14        mov  ebp, [esp+0x14]
0071b439  56                 push esi
0071b43a  57                 push edi
0071b43b  33 c0              xor  eax, eax
0071b43d  b9 00 02 00 00     mov  ecx, 0x200        ; zero 0x200 dwords = 0x800 bytes
...
0071b592  5f                 pop  edi
0071b593  5e                 pop  esi
0071b594  5d                 pop  ebp
0071b595  5b                 pop  ebx
0071b596  83 c4 08           add  esp, 8
0071b599  c3                 ret                   ; bare ret → cdecl (caller cleans 8)
0071b59a  cc cc cc           int3 pad
```

Live decompile is authoritative for param order `(dst, perm)`.

### Constants (`read_memory`)

| Symbol | VA | Bytes / role |
|--------|-----|--------------|
| `DAT_00afcd68` | `0x00afcd68` | dwords `0x80,0x40,0x20,0x10,0x08,0x04,0x02,0x01` — output bit masks |
| `DAT_00afcd88` | `0x00afcd88` | dwords `0x08,0x04,0x02,0x01` — nibble-bit test masks |
| `DAT_00afca50` | `0x00afca50` | 64 B FIPS DES **IP** (58,50,42,…,7) — **exact match** |
| `DAT_00afca90` | `0x00afca90` | 64 B FIPS DES **FP** (40,8,48,…,25) — **exact match** |

### Callers / call sites

| Site | Function | Args |
|------|----------|------|
| from `DES_InitializeCipher` | `0x0071c000` | `FUN_0071b430(&DAT_00d16ef0, &DAT_00afca50)` IP out |
| same | | `FUN_0071b430(&DAT_00d12690, &DAT_00afca90)` FP out |

Sole static caller: **`DES_InitializeCipher`** (`get_function_callers`).

### Algorithm seal

1. Zero `param_1` for **0x200** dwords (**0x800** bytes).
2. Outer `local_4` 0..15 with row base `iVar4 = local_4*0x10` while `iVar4 < 0x100`.
3. Mid `uVar6` 0..15 (nibble value / pattern).
4. Inner walk 64 perm entries unrolled ×4 (`uVar3` 1..0x40 step 4).
5. For each 1-based bit index `p`: `src = p-1`; if `(src>>2)==local_4` and `(mask_nibble[src&3] & uVar6)` then OR `mask_out[bit&7]` into `dst[(iVar4+uVar6)*8 + (bit>>3)]`.
6. Leaf (no callees). Void return.

Consumer: `DES_PermuteBlock` uses filled banks at `DAT_00d16ef0` (IP) and `DAT_00d12690` (FP).
