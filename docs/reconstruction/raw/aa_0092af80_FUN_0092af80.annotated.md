# Annotated low-level: TargetFilter_DispatchMatchArms_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0092af80` |
| VA | `0x0092af80` |
| Ghidra | `FUN_0092af80` |
| System | inventory-transfer (partition); skill/reaction target-filter family |
| Date | 2026-08-05 (R11-039 dual seal) |

## Machine-level notes

- **EAX** = `TargetFilterCtx*` (`*ctx` = `Client*`); body keeps it in **EDI**.
- **ESI** = `void **bestOut` (register formal — decompiler `unaff_ESI`).
- Stack: `float *origin`, `float *bestDistSq`; **`ret 8`**; **AL** = any arm ran.
- Direct COID arm: invalid when `(lo & hi) == 0xFFFFFFFF` (same empty-TFID plate as inventory grids).
- Resolve: `ECX = *(client + 0xD34)`; sealed `CVOGReaction_ResolveObjectTarget(bGlobal=0, lo, hi)` → `*bestOut`.
- Count gates (signed `> 0`):
  | Offset | Arm |
  |-------:|-----|
  | `+0x28` | dualed `TargetFilter_FindNearestWorldObjByCbidList_Inferred` (`0x0092adf0`) — locker absence + CBID whitelist nearest |
  | `+0x80` | residual `FUN_0092abc0` — world-list nearest under type/level filters |
  | `+0x54` | residual `FUN_0092a980` — hash-id chain nearest (uses **EBX=ctx**) |
  | `+0xAC` | residual `FUN_0092a760`; on false → residual `FUN_00929cd0(ctx,…)` fallback scan |
- Return **does not** mean `*bestOut != NULL` — only that at least one eligibility gate fired (sticky `AL=1`).
- Sole caller `FUN_0092bf60` supplies character position via vtbl `+0x1A0` and max-dist sentinel `@0x00aaaaf8`.

## Pseudocode (annotated)

```c
// EAX = TargetFilterCtx*  (*ctx = Client*)
// ESI = void **bestOut
// stack: float *origin, float *bestDistSq
// ret 8; returns AL bool (any arm executed)
bool TargetFilter_DispatchMatchArms_Inferred(
    float *origin,
    float *bestDistSq)
{
  TargetFilterCtx *ctx = /*EAX→EDI*/;
  bool any = false;

  // Arm 0 — direct COID resolve into *bestOut
  uint32_t lo = *(uint32_t *)((char *)ctx + 0x20);
  uint32_t hi = *(uint32_t *)((char *)ctx + 0x24);
  if ((lo & hi) != 0xFFFFFFFFu) {
    Client *client = *(Client **)ctx;
    void *resolveCtx = *(void **)((char *)client + 0xD34);
    *bestOut = CVOGReaction_ResolveObjectTarget(resolveCtx, /*bGlobal*/ 0, lo, hi);
    any = true;
  }

  // Arm 1 — CBID list nearest (dualed parent residual)
  if (*(int *)((char *)ctx + 0x28) > 0) {
    TargetFilter_FindNearestWorldObjByCbidList_Inferred(origin, bestOut, bestDistSq);
    // EDI = ctx preserved for callee
    any = true;
  }

  // Arm 2 — residual type/level world scan
  if (*(int *)((char *)ctx + 0x80) > 0) {
    FUN_0092abc0(origin, bestOut, bestDistSq);
    any = true;
  }

  // Arm 3 — residual hash-id nearest (callee expects EBX = ctx)
  if (*(int *)((char *)ctx + 0x54) > 0) {
    FUN_0092a980(origin, bestOut, bestDistSq); // EBX:=ctx
    any = true;
  }

  // Arm 4 — residual primary + fallback when primary returns 0
  if (*(int *)((char *)ctx + 0xAC) > 0) {
    char ok = FUN_0092a760(origin, bestOut, bestDistSq);
    if (ok == 0) {
      FUN_00929cd0(ctx, origin, bestOut, bestDistSq);
    }
    any = true;
  }

  return any;
}
```

## Field plate (TargetFilterCtx — this unit)

| Offset | Role (this body) |
|-------:|------------------|
| `+0x00` | `Client*` |
| `+0x20` / `+0x24` | Direct target COID lo/hi |
| `+0x28` | CBID-list count → arm `0092adf0` |
| `+0x54` | count → arm `0092a980` |
| `+0x80` | count → arm `0092abc0` |
| `+0xAC` | count → arm `0092a760` / fallback `00929cd0` |

Sibling dual `0092adf0` also documents `+0x2C` CBID array (not read here).

## Open / residual

- Product/PDB name of filter-context class (skill vs reaction vs interact tracker).
- Product English for residual arms `0092abc0` / `0092a980` / `0092a760` / `00929cd0` (not owned by R11-039).
- Whether caller treats non-null `*bestOut` separately (yes — `FUN_0092b2a0` after this returns true).
- Runtime / bit-exact / differential.
