# Annotated low-level: Sto_UnserialFactoryMapB_LookupInvoke_Inferred (`FUN_00437c90`)

| Field | Value |
|---|---|
| Stable ID | `aa_00437c90` |
| VA | `0x00437c90`–`0x00437cce` (**63 B** / `0x3F`) |
| System | arda2/storage — stoAbstractUnserializationFactory map B lookup/invoke |
| Date | 2026-07-23 scaffold; **W36-C seal 2026-07-29** |

## Machine-level notes

- **Role:** Peek current chunk tag from **`stoChunkReader*`** (entry **EAX→ESI**), look up in NestedHash **map B**, and if present **invoke** the creator at **`node+0x0C`**. Miss → return **0**.
- **ABI:** no stack args; entry **EAX = reader***; returns **void\*** / object* in EAX (or 0). Plain RET after `ADD ESP,8`.
- **Flow:**
  1. `ESI = EAX` (reader)
  2. `tag = PeekChunkTag()` (`FUN_0076a900`, ESI this)
  3. `bag = MapB_Get()`; `find(tag)` → node
  4. `bag = MapB_Get()`; if node ≠ `bag->sentinel`: restore ESI=reader; `CALL [node+0xC]`; return EAX
  5. else return 0
- **Invoke convention gap:** bytes use bare `CALL DWORD PTR [ECX+0xC]` with **no push**; decompiler claims `(tag)` arg. Seal invoke as indirect call; **do not** invent stack prototype without further evidence. ESI is reader at call site after `POP ESI`.
- **Caller:** sole `FUN_00765740` (1 site @ `0x00765cce`).
- **Twin relation:** MapA lookup `FUN_00437b00` uses MapA + different post-create path (out-param + vcall) — not body-identical.
- **Reject** scaffold `Named_CalleeOf_*_gfxBody_*`.

## Pseudocode (annotated)

```c
// entry EAX = stoChunkReader*
void* Sto_UnserialFactoryMapB_LookupInvoke_Inferred(void /* EAX reader */)
{
  stoChunkReader* reader = (stoChunkReader*)/*EAX*/;
  uint32_t tag = stoChunkReader_PeekChunkTag(reader); // FUN_0076a900, ESI=reader
  NestedHash* bag = Sto_UnserialFactoryMapB_GetInstance_Inferred();
  void* node = NestedHash_Find(bag, &tag);            // FUN_0046c1b0
  bag = Sto_UnserialFactoryMapB_GetInstance_Inferred();
  if (node == bag->sentinel) // +0x08
    return 0;
  // ESI restored to reader; CALL [node+0xC] — creator ABI gap
  return ((creator_fn*)*(void**)((uint8_t*)node + 0x0C))(/*?*/);
}
```

## Sealed offsets / callees

| Symbol | Role |
|---|---|
| `FUN_0076a900` | PeekChunkTag (ESI reader) |
| `FUN_00438ca0` | MapB GetInstance |
| `FUN_0046c1b0` | NestedHash find |
| `node+0x0C` | creator (written by insert twin) |
| `bag+0x08` | sentinel |

## Open gaps

- Exact creator calling convention / formals.
- Product English of caller `FUN_00765740`.
- Runtime / bit-exact / differential.
