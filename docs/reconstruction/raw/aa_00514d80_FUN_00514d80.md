# Raw capture: FUN_00514d80

| Field | Value |
|---|---|
| **Stable ID** | `aa_00514d80` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00514d80` |
| **Canonical name** | `FUN_00514d80` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00514d80(int *param_1)

{
  int iVar1;
  int *piVar2;
  
  piVar2 = (int *)param_1[0x49];
  if (piVar2 != (int *)param_1[0x4a]) {
    do {
      if (*(int *)(*piVar2 + 0x28) == 0xf) {
        FUN_004ba0a0();
      }
      piVar2 = piVar2 + 1;
    } while (piVar2 != (int *)param_1[0x4a]);
  }
  iVar1 = param_1[0x2a];
  FUN_004eb3b0();
  if (*(int *)(iVar1 + 0x88) != 0) {
    FUN_004eb3b0();
    iVar1 = FUN_004a16d0(0xf,1,0xffffffff);
    if (iVar1 != 0) {
      (**(code **)(*param_1 + 0xf8))(iVar1,1,0);
    }
  }
  return;
}
```

---

## Re-verify append — 2026-07-29 OWN-ONLY dual W28-H

| Field | Value |
|---|---|
| **Tool** | `decompile_function` + `analyze_function_complete` + `get_function_by_address` + `read_memory` + callers/xrefs |
| **Excluded** | `disassemble_bytes`; Launcher; parent ledger |
| **Live decompile** | ≡ 2026-07-23 raw outer CF (type-0xF walk + Ensure×2 + LoadEventVariant + vtbl+0xF8) |
| **Body** | `0x00514d80`–`0x00514df3` (**116 B / `0x74`**); pad `CC` after `RET` |
| **ABI (bytes)** | **thiscall** `mov esi,ecx`; plain **`RET` (`C3`)** on impl |
| **Public virtual thunk** | sole CODE caller `FUN_00581500` @ `0x00581500`: vb-adjust `this` via `[ecx-0x8c]` / vbtable`+4`, `call 00514d80`, **`ret 4`** — public virtual takes **1 ignored stack dword** |
| **Decompiler gaps sealed by `read_memory`** | (1) StopFinalize ECX = `*it` (`mov ecx,[edi]`); (2) `FUN_004eb3b0` ECX = `*(this+0xA8)` twice; (3) `FUN_004a16d0` ECX = `*(*(this+0xA8)+0x88)` catalog host, stack `(0xF, 1, -1)`; (4) virtual `[vtbl+0xF8](fx, 1, 0)` with ECX=this |
| **Offsets** | vector `param_1[0x49]/[0x4a]` ≡ host`+0x124/+0x128` (same primary FX* vector as `005179e0`); linked host `param_1[0x2a]` ≡ `+0xA8` |
| **Named role** | `Object_StopTypeF_ReloadEventVariant_Inferred` |

### Byte-backed control flow

1. Walk primary FX* vector `+0x124..+0x128`:
   - if `*(int*)(*it + 0x28) == 0x0F` → `NDSpecialFX_StopFinalize_Inferred(*it)` (**no free** here).
2. `linked = *(this+0xA8)`; `FUN_004eb3b0(linked)` (Ensure FX catalog resource on linked host).
3. If `*(linked+0x88) != 0`:
   - `FUN_004eb3b0(linked)` again
   - `fx = FxMaster_LoadEventVariant_Inferred(*(linked+0x88), eventId=0xF, mode=1, slot=-1)`
   - if `fx != 0` → `this->vtbl[+0xF8](fx, 1, 0)`
4. return.
