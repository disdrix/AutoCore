# Annotated low-level: FUN_00887780 / UI_TabsSo_SetActiveTab_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00887780` |
| VA | `0x00887780`–`0x00887a63` (**740 B** / `0x2E4`) |
| System | inventory-transfer (`i_d_tabs_so_*`) |
| Date | 2026-07-23 scaffold; **R13-012 dual seal 2026-08-05** |
| Canonical | `UI_TabsSo_SetActiveTab_Inferred` |

---

## Machine-level notes (R13-012)

- **ABI:** hybrid usercall — **EAX=tabIndex**, stack **dialog*** + **force**, **`RET 8`**, void. Entry: `55 8B 6C 24 08 56 8B F0`.
- **Gate:** host vtbl`+0x3d8` visible AND (`force || tabIndex != dialog+0x534`).
- **Store:** `dialog+0x534` = tabIndex before mode switch.
- **Panes (linear):** tab0 `+0x50c`, tab1 `+0x510`, tab2 `+0x514`.
- **Tab buttons:** friends `+0x518` / clan `+0x51c` / enemies `+0x520` (ctor XML).
- **Select path:** `ESI=btn; BL=1; PUSH force; CALL 00822cb0; CALL [btn.vtbl+0x94]`.
- **Deselect path:** `ESI=btn; BL=0; PUSH force; CALL 00822cb0; EDI=btn; CALL 00756c90`.
- **Mode 2 early RET** @ `0x008878b3` after enemies select — do not fold into shared tail.
- **Host:** `DAT_00d1b878`. **9** CALL xrefs.
- Prefer assembly when decompiler omits ESI/EDI/BL or drops EAX arity.
- Product English / RTTI open — keep `_Inferred`. Never Runtime Confirmed this pass.

## Pseudocode (annotated copy of raw + machine corrections)

```c
// hybrid __usercall: tabIndex@EAX, dialog@stack+4, force@stack+8; RET 8
void UI_TabsSo_SetActiveTab_Inferred(int *dialog /*param_1*/, char force /*param_2*/,
                                     int tabIndex /*in_EAX*/)
{
  char cVar1;
  // ESI = tabIndex (from EAX at entry); EBP = dialog

  cVar1 = dialog->vtbl[+0x3d8]();  // is-visible
  if ((cVar1 != '\0') && ((force != '\0' || (tabIndex != dialog[+0x534])))) {
    dialog[+0x534] = tabIndex;
    if (tabIndex == 0) {
      // friends: show +0x50c; hide +0x510,+0x514; select +0x518; deselect +0x51c,+0x520
      ...
    } else if (tabIndex == 1) {
      // clan: show +0x510; hide +0x50c,+0x514; select +0x51c; deselect +0x518,+0x520
      ...
    } else if (tabIndex == 2) {
      // enemies: show +0x514; hide +0x50c,+0x510; select +0x520; early RET 8
      ...
      return; // RET 8 @ 0x008878b3
    }
    // else: store-only
  }
  return; // RET 8
}
```

Full machine matrix and clean rewrite: `UI_TabsSo_SetActiveTab_Inferred.cpp`.
Raw body preserved in `aa_00887780_FUN_00887780.md` (authoritative decompile + re-verify append).
