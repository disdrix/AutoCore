# Annotated low-level: StdVector_UninitializedCopy_BasicString (FUN_00431430)

| Field | Value |
|---|---|
| Stable ID | `aa_00431430` |
| VA | `0x00431430`–`0x00431450` (**32 B** / `0x20`) |
| System | MSVC `vector<basic_string<char>>` uninitialized copy (thin rebind) |
| Date | 2026-07-29 (W33-H seal) |

## Machine-level notes

- **Thiscall** ECX = vector*; three stack pointers; **`RET 0x0C`**.
- Entire body is arg shuffle + `CALL FUN_0046a2c0` + `ADD ESP,0x14` + `RET 0x0C`.
- Forward: `FUN_0046a2c0(first, last, dest, this, dest)`.
- **Return**: EAX preserved from callee (= dest + N*0x1c). Decompiler `void` is wrong for consumers.
- Sibling helpers (not owned): `FUN_00431450` assign-fill; `FUN_00431480` → `FUN_00431790` (copy-backward family).
- Core string loop lives in `FUN_0046a2c0` (placement copy-ctor, stride 0x1c, SEH).

## Pseudocode (bytes-confirmed)

```c
// ECX = vector*; stack first, last, dest; RET 0x0C
// returns end of constructed range (EAX)
BasicString_0x1c* StdVector_UninitializedCopy_BasicString(
    VectorBasicString* vec,
    BasicString_0x1c* first,
    BasicString_0x1c* last,
    BasicString_0x1c* dest)
{
  return FUN_0046a2c0(first, last, dest, vec, dest);
}
```

## Open questions

- Product plate (`_Ucopy` / `_Umove` / allocator-aware overload).
- Why trailing 5th arg = dest and 4th = this (unused triad vs residual ABI).
- Dual-own `FUN_0046a2c0` for full string Ucopy plate.
