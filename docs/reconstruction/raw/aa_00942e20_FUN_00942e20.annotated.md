# Annotated low-level: Client_FrameTick_EnvDayCycleAndSystems_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00942e20` |
| VA | `0x00942e20`–`0x009430f0` exclusive (**720 B**) |
| Ghidra | `FUN_00942e20` |
| Canonical | `Client_FrameTick_EnvDayCycleAndSystems_Inferred` |
| System | client frame / environment day-cycle parent |
| Date | 2026-08-04 (WQ9H-G dual) |

## Machine-level notes

- **EDI** is the client/session host (register this). Entry: `MOV EAX,EDI` then gate. Decompiler `unaff_EDI` is this ABI, not a bug.
- Stack frame `SUB ESP,0x114`; locals: clamped dt at `[ESP]`, debug sprintf buffer, list cursor.
- **Bare `RET`** (no stack args). Early fail path also bare RET with EAX=1.
- Sky-blend consumer path (disasm authority over decompile):
  - `EAX = [EDI+0xe04]`; if non-null
  - `ECX = [EAX+0xe894]`  ← env host for dualed `Env_DayCycle_ApplySkyBlendAndLight_Inferred`
  - `PUSH` clamped dt; `CALL 0x004912c0`
- Decompile omits ECX setup (shows bare `FUN_004912c0(local_114)`); sealed by disasm @ `0x00942e6e`–`0x00942e75`.
- Fog path: sample triple at source `+0x90/+0x94/+0x98`; scale middle by `DAT_00af9120` (0.01f); material vtbl+0x10(3); optional per-entry `fFogHeight` when `DAT_00d1f06d`.
- Sole caller: main client frame `FUN_0094b520` under profile bucket 1 (`FUN_0079a1c0(1)` … call … `FUN_0079a120(1)`).

## Pseudocode (annotated)

```c
// EDI = ClientSession* host (register this)
// returns 0 ok, 1 gate fail
uint32_t Client_FrameTick_EnvDayCycleAndSystems_Inferred(void)
{
  // gate: FUN_0092e510 with EAX=EDI
  if (!GateOk(host)) return 1;

  float dt = *(float*)(g_timeSys + 0x30);   // DAT_00d09874
  if (dt > 0.2f) dt = 0.2f;                 // DAT_00a0f70c

  void* worldRoot = *(void**)(host + 0xe04);
  if (worldRoot != nullptr) {
    void* env = *(void**)((char*)worldRoot + 0xe894);
    // dualed: Env_DayCycle_ApplySkyBlendAndLight_Inferred(env, dt)  // ret 4; dt unread in body
    Env_DayCycle_ApplySkyBlendAndLight_Inferred(env, dt);
  }

  FUN_00942840();                // ECX = host
  FUN_00931a50(dt);              // ESI = host
  FUN_007a2e50(*(host+0xde8), *(g_timeSys+0x238));
  FUN_009307b0(host, dt);
  FUN_0092cef0();
  FUN_0092ec60();                // EAX = host
  FUN_0092f000();                // EAX = host

  // optional fog material push + fFogHeight loop (see raw)
  // optional FUN_00753a50 / FUN_007545c0 / FUN_004c0ce0 / FUN_007f9cc0
  // drain host+0xd24 list: vtbl tick(dt); fail keeps, success deletes via 00402850
  // host+0xf40 primary object vtbl+0x344(dt)

  if (DAT_00d1f06c == 0) {
    // one-shot: "%%%%%%%%%%%%%%%%%% All load time (%f)\n"
    DAT_00d1f06c = 1;
    // store end time host+0x9ac; delta vs host+0x9a8
  }
  return 0;
}
```

## Constants (read_memory)

| Symbol | VA | Bytes | Value |
|---|---|---|---|
| `DAT_00a0f70c` | `0x00a0f70c` | `CD CC 4C 3E` | 0.2f dt clamp |
| `DAT_00af9120` | `0x00af9120` | `0A D7 23 3C` | 0.01f fog scale |
| `DAT_00af911c` | `0x00af911c` | `00 00 80 3F` | 1.0f fFogHeight |
| load-time fmt | `0x00a2cf40` | ASCII | `%%%%%%%%%%%%%%%%%% All load time (%f)\n` |
| fog name | `0x00a2cf68` | ASCII | `fFogHeight` |

## Open residuals

- Product English for host class / gate `FUN_0092e510` / many undualed callees.
- Exact type of `[host+0xe04]` world root vs env at `+0xe894`.
- Runtime Confirmed open.
