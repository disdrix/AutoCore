# Raw capture: FUN_0073be50

| Field | Value |
|---|---|
| **Stable ID** | `aa_0073be50` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0073be50` |
| **Canonical name** | `FUN_0073be50` |
| **System** | graphics / GlyphCache device-reset broadcast |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_0073be50(void)

{
  int unaff_EBX;
  int *piVar1;
  
  piVar1 = (int *)**(int **)(unaff_EBX + 8);
  if (piVar1 != *(int **)(unaff_EBX + 8)) {
    do {
      FUN_00755820();
      piVar1 = (int *)*piVar1;
    } while (piVar1 != (int *)*(int *)(unaff_EBX + 8));
  }
  return 0;
}
```

---

# =============================================================================
# W38-U re-verify append -- 2026-08-04 OWN-ONLY dual
# Tools: batch_decompile + read_memory + analyze_function_complete + xrefs +
#        get_assembly_context + callee FUN_00755820 decompile + GfxDevice_Reset caller.
# No disassemble_bytes. No Launcher. No parent ledger edits.
# =============================================================================

## Body / size (bytes)

| Field | Value |
|---|---|
| **Body** | `0x0073be50`-`0x0073be74` exclusive (**36 B** / `0x24`) |
| **Ghidra listing** | `0073be50`-`0073be73` (last insn `RET` @ `0073be73`) |
| **Pad** | `CC` then next unit |
| **Sibling before** | `FUN_0073bc50` GlyphCache_GetOrCreate (W37-AE) ends ~`0073be47` then `CC` |

## Full body hex (36 B)

```
8b 43 08 57 8b 38 3b f8 74 16 56 eb 03 8d 49 00
8b 77 2c e8 b8 99 01 00 8b 3f 3b 7b 08 75 f1
5e 33 c0 5f c3
```

Relative call: `E8 B8 99 01 00` -> `0x0073be63 + 5 + 0x199B8 = 0x00755820`.

## ABI (bytes win)

| Slot | Value | Conf |
|---|---|---|
| container | **EBX** = list/manager (`[EBX+8]` = circular sentinel) | **High** |
| ECX | unused in body | High |
| stack formals | none | High |
| return | **EAX = 0** always (`XOR EAX,EAX`) | **High** |
| cleanup | plain **`RET`** (`C3`) | **High** |
| callee arg | **ESI** = `*(node + 0x2c)` into `FUN_00755820` | **High** |

Decompiler `unaff_EBX` / bare `FUN_00755820()` is incomplete -- **bytes win**.

## Semantics

Walk **circular doubly-linked list** (sentinel at `container+8`, nodes linked via `node->next` at `+0`):

```
sentinel = *(container + 8)
for (node = sentinel->next; node != sentinel; node = node->next)
    FUN_00755820( *(node + 0x2c) );   // ESI = payload
return 0;
```

Empty list: first `CMP EDI,EAX` equal -> skip loop -> return 0.

Payload at **node+0x2c** matches GlyphCache map-node payload slot sealed in W37-AE (`GlyphCache_GetOrCreate` hit returns `node[+0x2c]`). Callee `FUN_00755820` (unowned) treats ESI as GlyphCache-sized object (uint16 vector @+0xC/+0x10, flags @+0x64/+0x68, per-id `FUN_00755100`) -- device-reset rebuild/rebind path.

## Sole caller

| Caller | Site | Setup |
|---|---|---|
| `FUN_0075eff0` GfxDevice_Reset | `0x0075f36d` | **Success path** after factories re-init: `MOV EBX,[EBP+0x758]; CALL 0073be50` |

`device+0x758` = manager owning the circular list of GlyphCache map nodes (or equivalent intrusive list of caches). Called immediately after `FUN_00968680` on successful Reset, before copying present-params back and `FUN_0075e710`.

## Callees

| VA | Role |
|---|---|
| `0x00755820` | Per-payload device-reset hook (ESI=GlyphCache*; unowned) |

## Name

`GlyphCacheList_ForEach_OnDeviceReset_Inferred` -- **Inferred** from GfxDevice_Reset call site + node+0x2c GlyphCache payload family + sibling GlyphCache factory at `0073bc50`.
**Reject** scaffold `Named_CalleeOf_Named_gfxDevice_0073be50`.

## Decompile vs bytes

CF spine == decompile (walk + call + return 0). **Bytes win** on EBX container ABI, ESI payload formal into callee, empty-list fast path, and exact body size. Decompiler phantom `unaff_EBX` is the real live-in.

## Gaps

- Product English for manager at `device+0x758` (GlyphCache factory host vs dedicated list mgr).
- Full dual of `FUN_00755820` / `FUN_00755100` (unowned).
- Whether list is NestedHash intrusive walk or separate registry.
- Runtime / bit-exact / differential.
