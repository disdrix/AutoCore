# Annotated low-level: NestedHash_Ctor_Sentinel0x10_Inferred (FUN_00457ac0)

| Field | Value |
|---|---|
| Stable ID | `aa_00457ac0` |
| VA | `0x00457ac0`–`0x00457b21` (**98 B**) |
| System | containers / nested hash bag (statics + gfx + heap hosts) |
| Date | 2026-07-23 scaffold; W34-E seal 2026-07-29 |

## Machine-level notes

- **ABI:** stack placement ctor `self*`; **`RET 4`**; returns `self` in EAX. **Not** ECX-thiscall.
- **SEH:** `LAB_009bc89b` (byte-identical prolog to `0043bdf0` except sentinel CALL).
- **Layout (span 0x28):** same offsets as Sentinel0x14 twin; node size **0x10** via `FUN_004397f0`.
- **Family:** NestedHash 9-bucket + sentinel-fill; only callees differ from `0043bdf0`.
- Scaffold alias `Named_gfxDeviceIB_*` is a parent-seed name only — reject as product.

## Pseudocode (annotated)

```c
// NestedHash_Ctor_Sentinel0x10_Inferred — stack(self*); RET 4
NestedHash10* FUN_00457ac0(NestedHash10* self /*stack*/)
{
  // SEH LAB_009bc89b
  void* node = FUN_004397f0();          // new(0x10) circular sentinel
  self->sentinel = node;                // +0x08
  self->size = 0;                       // +0x0c
  void* fill = node;
  FUN_004646e0(&self->shell, 9, &fill); // +0x10, cap 9
  self->flag20 = 1;
  self->flag24 = 1;
  return self;
}
```

## Callers (5)

| Site | Use |
|---|---|
| `FUN_00438c40` / `00438ca0` / `0044fee0` | once-flag static globals |
| `FUN_0073c810` | `operator_new(0x30)` host bag + tail zeros |
| `FUN_0098fef0` | stack-local bag on gfxDeviceIB path |

## Open questions

- Product English per static / host.
- Flag / head dword meanings.
- Runtime / bit-exact / differential.
