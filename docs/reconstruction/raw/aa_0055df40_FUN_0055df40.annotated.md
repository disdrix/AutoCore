# Annotated low-level: FUN_0055df40 / ActionManager_UnregisterAndRelease_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0055df40` |
| VA | `0x0055df40`–`0x0055df76` exclusive (**54 B**) |
| System | vehicle / world action lifecycle |
| Date | 2026-07-29 (W23-P live seal) |
| Canonical (inferred) | `ActionManager_UnregisterAndRelease_Inferred` |
| Ghidra | `FUN_0055df40` |

## Machine-level notes

- Source: live `decompile_function` ≡ 2026-07-23 raw; `read_memory` body seal.
- Prefer bytes when decompiler drops `FUN_00628f10` this pointer.
- **No** `disassemble_bytes` used.

## ABI (sealed)

```c
// ECX = ActionManager* (often *(world+0xe4a4))
// [esp+4] = ActionObject*
// ret 4; void
void __thiscall ActionManager_UnregisterAndRelease(
    void *manager,
    void *action);
```

| Element | Fact |
|---|---|
| Convention | `__thiscall` |
| `this` | **ECX** = manager |
| Stack | one pointer (action) |
| Epilogue | `C2 04 00` |
| Return | void |

## Object fields touched on action

| Offset | Width | Use |
|---|---|---|
| `+0x00` | ptr | vtbl; `(*vtbl[0])(action, 1)` when ref hits 0 |
| `+0x06` | int16 | refcount; decremented once here |
| `+0x0C` | int32 | cleared to **0** (registered / link flag; callers gate on nonzero before call) |
| `+0x10` | ptr | reverse host for list remove (`FUN_00628f10`); nested callee nulls it |

## Control flow (annotated)

```c
void __thiscall ActionManager_UnregisterAndRelease(void *manager, void *action)
{
  // 1) Notify manager listener table (manager+0x3c / +0x40)
  FUN_0062a410(manager, action);

  // 2) Remove action from reverse-list host at action+0x10
  //    TRUE ABI: thiscall ECX=*(action+0x10), stack=action  (decomp residual omits ECX)
  FUN_00628f10(/*this=*/*(void**)((char*)action + 0x10), action);

  // 3) Release one ref; clear registered dword
  *(int16_t*)((char*)action + 6) -= 1;
  *(uint32_t*)((char*)action + 0x0C) = 0;
  if (*(int16_t*)((char*)action + 6) == 0) {
    // COM-style destroy: first vtbl slot, arg 1
    (*(void(__thiscall**)(void*, int))(*(void**)action))(action, 1);
  }
}
```

## Callers (role)

| Caller | Role |
|---|---|
| `FUN_004f7d60` / `Vehicle_DestroyVehicleAction_Inferred` | Teardown vehicle action holder; ECX from world `+0xe4a4` |
| `FUN_00507830` | Similar unregister + release pattern |
| `FUN_006427e0` / `FUN_00642860` | Clear held action when `action+0xC != 0` |

**Note:** Several callers also perform an **additional** short@+6 release *after* this function returns — this unit already released once.

## Open questions

1. Product / PDB English for manager and action classes.
2. Full layout of manager (`+0x3c` listener array, `FUN_005fff20` tail).
3. Whether `vtbl[0](1)` is scalar-deleting dtor vs Release.
4. Runtime / bit-exact — open.
