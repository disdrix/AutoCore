# Raw capture: FUN_00575350

| Field | Value |
|---|---|
| **Stable ID** | `aa_00575350` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00575350` |
| **Canonical name** | `FUN_00575350` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00575350(undefined4 *param_1)

{
  *param_1 = &PTR_FUN_009d33c4;
  if ((void *)param_1[0x7b] != (void *)0x0) {
                    /* WARNING: Subroutine does not return */
    operator_delete((void *)param_1[0x7b]);
  }
  param_1[0x7b] = 0;
  *(undefined1 *)(param_1 + 0x7c) = 0;
  *(undefined1 *)((int)param_1 + 0x1f1) = 0;
  if ((void *)param_1[0x7e] != (void *)0x0) {
                    /* WARNING: Subroutine does not return */
    operator_delete((void *)param_1[0x7e]);
  }
  param_1[0x7e] = 0;
  param_1[0x7f] = 0;
  param_1[0x80] = 0;
  FUN_00497360();
  FUN_00518ec0();
  return;
}
```
---

## W28-M re-verify (2026-07-29)

**Tools:** live `decompile_function` + `analyze_function_complete` + `read_memory` (full body 106 B). **No** `disassemble_bytes`.

### Sealed corrections vs scaffold decompile

1. **Body size:** `0x00575350`–`0x005753ba` exclusive (**106 B** / `0x6A`). Final `E9` tail-jmp; pad `CC` after.
2. **ABI:** `__thiscall`; ECX=`this`; no stack formals. Does not local-`RET` — **tail-jumps** to base dtor.
3. **Vtbl:** `mov dword ptr [esi], 0x009d33c4` → `PTR_FUN_009d33c4` (vtbl[0]=scalar deleting `0x00575c00`, vtbl[1]=empty stub `0x0056f570`).
4. **Owned free @ +0x1EC:** if non-null `operator_delete`; zero `+0x1EC`; clear bytes `+0x1F0`, `+0x1F1`.
5. **Buffer triple @ +0x1F8/+0x1FC/+0x200:** if begin non-null `operator_delete`; zero all three.
6. **List @ +0x1DC:** `lea ecx, [esi+0x1DC]` then `call FUN_00497360` (`StdList_Destroy_FreeHead`). Decompiler loses LEA.
7. **Base chain:** `mov ecx, esi; pop esi; pop ebx; jmp FUN_00518ec0` — **tail-call** Object shared-base destructor (`Object_SharedBase` dtor family / `FUN_00518ec0`). Constructor twin `FUN_00575440` calls `FUN_00518940` (`Object_SharedBase_Ctor`) then installs same vtbl.
8. **False noreturn** on `operator_delete` — fall-through sealed.
9. **Caller:** sole xref `FUN_00575c00` @ `0x00575c03` — scalar deleting dtor (`vtbl[0]`): body then optional free, `RET 4`.
10. Live decompile ≡ raw high-level CF; list LEA + tail-jmp sealed by bytes.

### Entry / epilogue hex (read_memory)

- Entry: `53 56 8B F1 8B 86 EC 01 00 00 33 DB 3B C3 C7 06 C4 33 9D 00 …`
- List: `8D 8E DC 01 00 00 … E8 AF 1F F2 FF` → `call 0x00497360`
- Tail: `8B CE 5E 5B E9 06 3B FA FF` → `jmp 0x00518ec0`
- Pad: `CC × 6+` after body
