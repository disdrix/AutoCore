# Annotated low-level: SoftCastHitList_SortIfCountGt1_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0048a060` |
| VA | `0x0048a060`–`0x0048a082` inclusive (**35 B** / `0x23`) |
| Ghidra name | `FUN_0048a060` |
| Canonical name | `SoftCastHitList_SortIfCountGt1_Inferred` |
| System | interaction-activation |
| Date | 2026-08-05 (MEGA-116 OWN dual refresh) |
| Prior scaffold | 2026-07-23 annotated stub (replaced in place) |

## Machine-level notes

- **ABI:** **thiscall** — **ECX** = soft-cast hit-list object. Decompiler `__fastcall(uint param_1)` is the ECX register view; not a free-standing integer.
- **Return:** void; bare **`RET`** (`C3`). No stack args.
- **Body:** `PUSH ECX` creates a 4-byte local; `MOV byte ptr [ESP],0` zeros the low byte → `local = this & 0xFFFFFF00`.
- **Gate:** `CMP [ECX+0x14], 1` / `JLE epilog` — sort only when **count > 1**.
- **Dispatch:** `cdecl` call to `FUN_00489f20(base@+0x10, 0, count-1, masked_this)` with `ADD ESP,0x10`.
- **Prefer assembly** for ABI; decompile CF is faithful once ECX=this is restored.
- **Callee role (not dualed here):** `FUN_00489f20` = quicksort of **0x30**-byte hits by **float @ +0x14** (ascending). 4th arg is recursive pass-through only.
- **Caller (dualed W17-C):** `Client_InteractWorldClickHub` (`0x00925820`) @ `0x00925c3e` after soft-cast fill `FUN_0055e1e0` → `FUN_006ca890`.

## Pseudocode (annotated)

```c
// ECX = SoftCastHitList* this
// void SoftCastHitList_SortIfCountGt1_Inferred(this)
void __thiscall SoftCastHitList_SortIfCountGt1_Inferred(SoftCastHitList *this)
{
  // local_4: stack slot seeded with ECX, low byte forced 0 → this & 0xFFFFFF00
  uint32_t sort_cookie = ((uint32_t)(uintptr_t)this) & 0xFFFFFF00u;
  int count = this->count;                 // +0x14

  if (count > 1) {
    // FUN_00489f20: qsort hits[0 .. count-1] by float key at elem+0x14, stride 0x30
    SoftCastHitList_QsortByKey014_Inferred(  // MEGA-115 residual name provisional
        this->array_base,                    // +0x10
        /*lo=*/ 0,
        /*hi=*/ count - 1,
        sort_cookie);                        // unused in compare; preserve mask
  }
}
```

## Hit-list layout (consumer-sealed)

| Offset | Field | Evidence |
|---|---|---|
| `+0x00` | vtbl | `FUN_0040c410` init |
| `+0x10` | `void* array_base` | this body; ctor points at inline `this+0x20` |
| `+0x14` | `int count` | this body; hub reads post-call |
| elem `0x30` | hit record | sort stride in `FUN_00489f20` |
| elem `+0x14` | `float` sort key | FLD/FCOMP in sort |
| elem `+0x20` | object-related ptr | hub → `FUN_0040afb0` resolve |

## Open questions

- Product English for container / sort-key (distance vs other float) — **open** → `_Inferred`.
- Whether `sort_cookie` ever mattered in a non-stripped build — dead in retail sort body.
- Full dual of sort core `0x00489f20` is **MEGA-115** OWN (not this agent).

## Confidence

| Claim | Level |
|---|---|
| CF gate count>1 + call sort | **Confirmed** (bytes) |
| thiscall ECX + bare RET | **Confirmed** |
| Sole caller InteractWorldClickHub | **Confirmed** (1 xref) |
| Soft-cast hit-list role | **High** (call-site chain) |
| Product name | **Inferred** |
| Runtime / bit-exact | **Open** |
