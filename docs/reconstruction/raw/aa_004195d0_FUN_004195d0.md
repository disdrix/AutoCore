# Raw capture: FUN_004195d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004195d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004195d0` |
| **Canonical name** | `CNDHash_Recreate_00a2c2e4` (sealed 2026-08-04; Ghidra still `FUN_004195d0`) |
| **System** | container / CNDHash (stamp `00a2c2e4`) |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_004195d0(void)

{
  int in_EAX;
  byte unaff_BL;
  
  if (*(char *)(in_EAX + 0x1d) != '\0') {
    FUN_007a4480(0,"HashError:Recreate, already locked for traversal");
    FUN_007a4480(0,"VOG_DEBUG_STOP");
  }
  FUN_00406fc0();
  *(byte *)(in_EAX + 0x1c) = unaff_BL;
  *(int *)(in_EAX + 8) = 1 << (unaff_BL & 0x1f);
  *(undefined4 *)(in_EAX + 0x18) = 0;
  *(undefined4 *)(in_EAX + 0x14) = 0;
  FUN_00406f20();
  return;
}
```

---

## Re-verify 2026-08-04 (WQ9H-E OWN-ONLY dual seal)

| Field | Value |
|---|---|
| **Tools** | `batch_decompile` / `analyze_function_complete` + `read_memory` + callers/xrefs + `get_assembly_context`. **No** `disassemble_bytes`. |
| **Live decompile** | ≡ raw body above (same CF / lock-continue / seed / callees) |
| **Body** | `0x004195d0`–`0x00419618` inclusive (**73 B** / `0x49`); `CC` pad after bare `ret` |
| **ABI** | Custom registers: **EAX = CNDHash\***; **BL = log2**; no stack args; **bare `ret`** (`5E C3`) |
| **Entry bytes** | `56 8B F0` — `push esi; mov esi,eax` (not thiscall) |
| **Lock `@+0x1d`** | log `"HashError:Recreate, already locked for traversal"` + `"VOG_DEBUG_STOP"` then **continue** (no early return) |
| **Strings** | Recreate msg @ `0x00a64c48`; `VOG_DEBUG_STOP` @ `0x00a15844` (read_memory) |
| **Callees** | `FUN_00406fc0` destroy (`mov eax,esi`); seed log2/`1<<log2`/`+0x14`/`+0x18`; `FUN_00406f20` alloc (ESI) |
| **Callers (1)** | `FUN_007fe640` @ `0x007fe707` — unlock `@+0x1d`, `mov eax,hash@+0x3084`, **`mov bl,2`**, call |
| **Xrefs** | 1 UNCONDITIONAL_CALL |
| **Name** | `CNDHash_Recreate_00a2c2e4` (stamp family via dualed alloc/destroy; string Recreate) |
| **Rejected** | `Named_VOG_DEBUG_STOP_004195d0` (assert string only); type-merge skill thiscall `004cbdc0` without ABI note |
| **Parallel** | Inventory `FUN_004138d0` same CF/ABI different helpers; skill `SkillCNDHash_Recreate_Inferred` thiscall+`ret 4` |
