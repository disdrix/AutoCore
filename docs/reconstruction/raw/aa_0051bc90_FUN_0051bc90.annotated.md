# Annotated low-level: FUN_0051bc90 → ObjectCsList_ClearDestroyNoNode4_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051bc90` |
| VA | `0x0051bc90`–`0x0051bd8b` exclusive (**251 B** / `0xFB`) |
| System | object core / CS-owned list teardown |
| Date | 2026-07-29 (W31-S re-verify) |
| Named | `ObjectCsList_ClearDestroyNoNode4_Inferred` |

## Machine-level notes

- Source: raw capture + live decompile/`read_memory` 2026-07-29.
- **Near twin of** `ObjectCsList_ClearDestroy_Inferred` (`0x0051b8a0`, W30-F): same CS/flag/head/next/zero layout; **difference = this unit never writes `node+4` before scalar-delete**.
- SEH `LAB_009a3520` + `__chkstk 0x1004` frame (throw path); success epilogue restores FS:[0] and `C3`.
- Callers are **list dtor** methods (not SharedBase):  
  - `FUN_0051bf10` scalar-deleting dtor: vtbl→`PTR_FUN_009ce07c`, this clear, `DeleteCriticalSection`, optional `operator_delete`.  
  - `FUN_0051d0e0` complete dtor: vtbl→`PTR_FUN_009ce154`, clear; vtbl→`PTR_FUN_009ce07c`, clear; `DeleteCriticalSection` (no free; free is `FUN_0051d0c0` wrapper).
- SharedBase dtor (`0x00518ec0`) uses the **other** twin `0051b8a0` (with `node+4=0`) before scalar-deleting `core+0xB0`.

## Pseudocode (annotated)

```c
// __thiscall/__fastcall; ECX = ObjectCsList*; void; bare RET
// Layout (same family as 0051b8a0):
//   +0x00 vtbl, +0x04 CRITICAL_SECTION, +0x1C head, +0x20, +0x24, +0x28 traversal flag

void __fastcall ObjectCsList_ClearDestroyNoNode4_Inferred(int list)
{
  undefined4 *node;
  // SEH setup omitted

  EnterCriticalSection((LPCRITICAL_SECTION)(list + 4));
  node = *(undefined4 **)(list + 0x1c);

  if (*(char *)(list + 0x28) != 0) {
    LeaveCriticalSection((LPCRITICAL_SECTION)(list + 4));
    // HRESULT 0x80070005 E_ACCESSDENIED
    _CxxThrowException(/*hr*/, (ThrowInfo *)&DAT_00acc430); // noreturn
  }

  while (node != 0) {
    *(undefined4 *)(list + 0x1c) = node[2];   // next @ +8
    // NOTE: unlike 0051b8a0, does NOT clear node+4
    (**(code **)*node)(1);                   // scalar-deleting dtor
    node = *(undefined4 **)(list + 0x1c);
  }

  *(undefined4 *)(list + 0x24) = 0;
  *(undefined4 *)(list + 0x20) = 0;
  *(undefined4 *)(list + 0x1c) = 0;
  LeaveCriticalSection((LPCRITICAL_SECTION)(list + 4));
}
```

## Open questions

- Product / MSVC demangle for CS-list class (`009ce154` / `009ce07c` vtbl pair).
- Why dtor path omits `node+4=0` while SharedBase pre-clear path includes it (lifetime vs double-clear).
- Field English for `+0x20` / `+0x24`.
