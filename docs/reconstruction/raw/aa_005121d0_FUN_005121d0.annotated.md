# Annotated low-level: FUN_005121d0 / IsPlayerTargetingLinkAndDestroy

| Field | Value |
|---|---|
| Stable ID | `aa_005121d0` |
| VA | `0x005121d0` |
| System | object / combat-target / HB |
| Date | 2026-07-29 (W16-O dual seal) |
| Canonical (inferred) | `IsPlayerTargetingLinkAndDestroy` |
| Ghidra name | `FUN_005121d0` |

## Machine-level notes

- **Role:** Predicate for list purge of self-side targeting heartbeat objects. RTTI-cast node to `CVOGHBPlayerTargetingLink`; on success invoke destructor-ish vtbl slot `+0x18(1, 0)` and return **1** (remove); else return **0** (keep).
- **Sole consumer:** `Object_SetSelectedTarget` (`0x005172d0`) pushes this address as predicate after `mov ecx, [entity+0xB0]` (HB action list).
- **ABI:** Free function, **`ret 0xC`** — three stack args. Only arg0 used (list node / HB*). Arg1/arg2 are list-walk context (caller passes 0, 0). Not `__thiscall` on entry (ECX unused at entry; ESI = `[esp+8]`).
- **RTTI:** `src = CVOGHBBase` type descriptor `0x00af8d98` (string `.?AVCVOGHBBase@@`); `target = CVOGHBPlayerTargetingLink` `0x00af1bb0` (`.?AVCVOGHBPlayerTargetingLink@@`). Cast is **downcast filter** — only the PlayerTargetingLink subclass matches (not base-vtbl target-side HB).
- **Destroy call:** `thiscall` `vtbl[+0x18](this, 1, 0)` — same “destroy with flags” pattern as other HB teardown. Slot `+0x18` = vtable index **6** (0-based dword index 6).
- **Return:** `AL` 1 or 0 (decomp `undefined4` / bool-as-int).
- Prefer assembly when decompiler arity conflicts — **decompiler under-counts formals** (1 vs 3).

## Pseudocode (annotated)

```c
// stdcall / ret 0xC — predicate for CNDDoubleList remove-if
// arg0 = HB node*; arg1, arg2 unused (list ctx)
uint32_t IsPlayerTargetingLinkAndDestroy(void *node /* , void *ctx, void *ctx2 */)
{
  // __RTDynamicCast(node, 0, &CVOGHBBase::RTTI, &CVOGHBPlayerTargetingLink::RTTI, 0)
  void *asLink = __RTDynamicCast(
      node, 0,
      &CVOGHBBase::RTTI_Type_Descriptor,              // 0x00af8d98
      &CVOGHBPlayerTargetingLink::RTTI_Type_Descriptor, // 0x00af1bb0
      0);
  if (asLink != 0) {
    // thiscall destroy: vtbl+0x18(this=node, 1, 0)
    (*(void (__thiscall **)(void *, int, int))(*(int *)node + 0x18))(node, 1, 0);
    return 1; // remove from list
  }
  return 0; // keep
}
```

## Cross-links

| Related | Role |
|---|---|
| `aa_005172d0` `Object_SetSelectedTarget` | Sole caller; purge `entity+0xB0` before rebind |
| `aa_00512090` `CVOGHBPlayerTargetingLink_ctor` | Constructs the subclass this predicate destroys |
| Base HB ctor `0x00604d40` | Target-side HB uses base vtbl — **not** matched by this RTTI cast |

## Open questions

1. Product English name of vtbl+0x18 (dtor vs Release vs Delete).
2. Whether list walker ever passes non-zero ctx args (all sealed sites use 0,0).
3. Exact MSVC RTTI complete-object locator layout beyond type-desc strings (not required for CF).
