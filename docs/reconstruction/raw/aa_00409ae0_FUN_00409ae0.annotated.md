# Annotated low-level: FUN_00409ae0 → StdVector_UninitCopyTrampoline_Elem0x28_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00409ae0` |
| VA | `0x00409ae0`–`0x00409afa` inclusive (**27 B** / `0x1b`) |
| System | MSVC-style vector uninit_copy ABI trampoline (elem 0x28) |
| Date | 2026-08-05 (WQ9K-A OWN dual re-annotate; scaffold 2026-07-23) |
| Named | `StdVector_UninitCopyTrampoline_Elem0x28_Inferred` |

## Machine-level notes

- Source: raw capture + WQ9K-A re-verify for `aa_00409ae0`.
- Prefer assembly when decompiler conflicts — decompiler drops 2nd stack arg and shows plain return; machine is **`RET 8`**.
- Target `FUN_0040a520` is dualed as `StdVector_UninitializedCopy_Elem0x28_Inferred` (WQ9J-A).
- Sole consumer: InsertN/reallocate grow path `FUN_00408050` (2 sites).

## Pseudocode (annotated)

```c
// ABI (bytes): ECX=host/this (unused by target), EDX=dest,
//              stack src_begin, src_end; EAX=dest_end; RET 8
// Decompiler shows incomplete __fastcall(3 params) without RET 8.
void* __fastcall StdVector_UninitCopyTrampoline_Elem0x28_Inferred(
    void* /*ECX*/ host_unused,
    void* /*EDX*/ dest,
    const void* /*stack*/ src_begin,
    const void* /*stack*/ src_end)
{
  // Reorder into free-helper ABI of 0040a520:
  //   ECX = src_end; stack: src_begin, dest, (src_begin, host) extras
  // Target ignores extra stack slots; same 4-push + ADD ESP,0x10 glue as direct sites.
  return StdVector_UninitializedCopy_Elem0x28_Inferred(
      /*ECX*/ src_end,
      /*stack*/ src_begin,
      /*stack*/ dest);
}
```

## Control flow

| Stage | Match |
|---|---|
| Load src_begin from `[ESP+4]` | Confirmed |
| Push begin; push host ECX | Confirmed |
| Load src_end into ECX | Confirmed |
| Push dest EDX; push begin | Confirmed |
| CALL `0040a520` | Confirmed (rel `+0x0a2b`) |
| `ADD ESP,0x10` | Confirmed |
| `RET 8` | Confirmed |

## Call sites (InsertN grow)

```
// Prefix @ 0x00408171:
//   PUSH where ([EBP+8]); PUSH begin ([EBX+4]); EDX=new; ECX=this → [begin,where)→new
// Suffix @ 0x00408198:
//   PUSH end ([EBX+8]); PUSH where ([EBP+8]); EDX=after Ufill; ECX=this → [where,end)→dest
```

## Open residual

- Product T demangle for 0x28 POD element.
- Full dual of parent InsertN `00408050`.
- Runtime / bit-exact / differential.
