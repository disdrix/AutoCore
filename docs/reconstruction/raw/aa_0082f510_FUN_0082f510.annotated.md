# Annotated low-level: FUN_0082f510 → UI_Window_OnCommand_Class8_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0082f510` |
| VA | `0x0082f510` |
| System | UI command dispatch / skills |
| Date | 2026-07-23 scaffold; **WQ8R-F 2026-08-04** |

---

## Machine-level notes

- **Role:** UI window **OnCommand** override specializing **command class 8**: switch on subcommand id 1..0xd (dialog show/hide toggles + one hardcoded skill cast), with dead-vehicle early divert and fallback to base `FUN_0082c780`.
- **ABI:** `__thiscall` — ECX = window/`this`; stack arg0 = **cmdClass** (`param_2`); stack arg1 = **cmdId** (`param_3`); **`RET 8`**; typically returns **1** when class-8 handled (or 70000 absorb).
- **Entry bind:** no direct CALL sites; **DATA** vtable slot `0x00a72514` → this VA (indirect dispatch only).
- **Dead-vehicle gate:** if `cmdId != 10` and local player has vehicle `@+0x250` and vehicle entity `vtbl+0x194()` non-zero (dead/waiting family from prior duals) → `this.vtbl+0x47c()` then **return** `FUN_0082c780(class,id)`.
- **Class-8 switch (sealed table):**

| cmdId | Action |
|---|---|
| 1 | `Client_CastSkillFromQuickBarSlot(0xED5)` with ESI=`&DAT_00d1a840`; return 1 |
| 2, 3 | no-op fallthrough → return 1 |
| 4 | toggle path on `DAT_00d1b874` (visible `vtbl+0x3d8`, field `[0x14c]`, `FUN_007fca10` / `FUN_007fef20(1,1,0)` / `FUN_00889410`) |
| 5 | same family on `DAT_00d1b870` (`[0x14d]`, `FUN_007fef20(0,1,0)`, `FUN_00888b00`) |
| 6 | `FUN_007fef20(2,0,0)` → return 1 |
| 7 | `FUN_007fef20(0x12,0,0)` return 1 |
| 8 | `FUN_007fef20(0x13,0,0)` return 1 |
| 9 | `FUN_007fef20(0x11,0,0)` return 1 |
| 10 | `FUN_007fef20(0x29,0,0)` return 1 (note: dead-gate skips when id==10) |
| 0xb | `DAT_00d1b880` path (`[0x14a]`, `FUN_007fef20(4,1,0)`, `FUN_00889fb0`) |
| 0xc | `FUN_007fef20(3,0,0)` return 1 |
| 0xd | `FUN_007fef20(5,0,0)` return 1 |
| default / class≠8 | if cmdId≠**70000** → `FUN_0082c780`; else return 1 |

- **Skill 0xED5:** only hardcoded cast in this unit; product skill name **open**.
- **Dialog globals** `DAT_00d1b870/874/880` match patterns in `Client_Input_PollBoundActions` menu section (same vtbl`+0x3d8` / `FUN_007fef20` family) — names residual.
- **Not** inventory transfer wire; not hardpoint list walk (contrast `aa_008c4fc0`).

## Pseudocode (annotated)

```c
// UI_Window_OnCommand_Class8_Inferred  @ 0x0082f510
// INFERRED — vtable OnCommand-shaped; no class RTTI sealed this pass.
uint32_t __thiscall UI_Window_OnCommand_Class8_Inferred(
    UIWindow* self, int cmdClass, int cmdId)
{
  Character* player = DAT_00d1b6d8;
  if (cmdId != 10 && player && player->vehicle_0x250) {
    Entity* veh = MI_entity(player->vehicle_0x250);
    if (veh->vtbl[+0x194]()) {                 // dead / non-operable
      self->vtbl[+0x47c]();
      return FUN_0082c780(self, cmdClass, cmdId); // base forwarder
    }
  }

  if (cmdClass == 8) {
    switch (cmdId) {
    case 1:
      // ESI = &DAT_00d1a840
      Client_CastSkillFromQuickBarSlot(client, /*skillId=*/0xED5);
      return 1;
    case 2: case 3:
      return 1;
    case 4:  /* DAT_00d1b874 dialog toggle family */ ...
    case 5:  /* DAT_00d1b870 */ ...
    case 6:  FUN_007fef20(2,0,0); return 1;
    case 7:  FUN_007fef20(0x12,0,0); return 1;
    case 8:  FUN_007fef20(0x13,0,0); return 1;
    case 9:  FUN_007fef20(0x11,0,0); return 1;
    case 10: FUN_007fef20(0x29,0,0); return 1;
    case 11: /* DAT_00d1b880 */ ...
    case 12: FUN_007fef20(3,0,0); return 1;
    case 13: FUN_007fef20(5,0,0); return 1;
    default: break;
    }
  }
  if (cmdId != 70000)
    return FUN_0082c780(self, cmdClass, cmdId);
  return 1;
}
```

## Open questions

1. Product class name for vtable containing slot `0x00a72514`.
2. Skill product name / data row for id **0xED5**.
3. English names for dialog globals `DAT_00d1b870/874/880` and `FUN_007fef20` dialog indices.
4. Product meaning of cmdId **10** exception on dead-vehicle gate.
5. Runtime / bit-exact.
