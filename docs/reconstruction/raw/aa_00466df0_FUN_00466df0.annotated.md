# Annotated low-level: StdVector_Resize_Elem28_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00466df0` |
| VA | `0x00466df0`–`0x00466e92` exclusive (**162 B** / `0xA2`) |
| Ghidra | `FUN_00466df0` |
| Preferred | `StdVector_Resize_Elem28_Inferred` |
| System | MSVC `vector<T>::resize(n, value)`, T POD **0x1c** (Elem28 / CountEcx insert family) |
| Date | 2026-07-29 (W36-P dual) |

## Machine-level notes

- **ABI:** **EBX=vec\***, **ECX=new_size**, stack **Elem28 by value (0x1c)**; **`RET 0x1C`** (`C2 1C 00`) on both grow and shrink epilogues.
- **Triad:** begin `@+4`, end `@+8`; cap `@+0xC` unused in this unit.
- **Size:** null begin → 0; else `(end−begin)/0x1c` via magic **`0x92492493`** (same imul family as `StdVector_Elem28_Size` / `StdVector_Resize_Pod28`).
- **Grow:** `count = new_size − size`; call sealed **`StdVector_InsertN_Elem28_CountEcx_Inferred`** (`0x00466ea0`) with **ECX=count**, **EDX=vec**, stack **where=end** + **value\*** (`LEA ESI,[ESP+0xC]`).
- **Shrink:** `new_end = begin + new_size*0x1c` via `LEA EAX,[ECX*8]; SUB EAX,ECX; LEA EAX,[EDI+EAX*4]`; call **`FUN_0045cce0`** erase `[new_end, end)`.
- **Equal / empty:** no mutation; fall through to `RET 0x1C`.
- **Decompile gap:** omits stack value blob and InsertN formals; shows `unaff_EBX` / single ECX formal. **Bytes + caller win.**
- **Twin:** structural clone of `StdVector_Resize_Pod28` (`0x0045c4b0`) with different InsertN grow callee.
- **Reject** scaffold `Named_CalleeOf_…_anmAnimMaster_00466df0`.

## Pseudocode (bytes authority)

```c
// EBX=vec*, ECX=new_size, stack value[0x1c]; RET 0x1C
void StdVector_Resize_Elem28_Inferred(VectorElem28* vec, uint32_t new_size, Elem28 value)
{
  Elem28* begin = vec->begin;
  uint32_t size = 0;
  if (begin)
    size = (uint32_t)((char*)vec->end - (char*)begin) / 0x1c;

  if (size < new_size) {
    // InsertN CountEcx: ECX=count, EDX=vec, stack where + &value
    StdVector_InsertN_Elem28_CountEcx_Inferred(new_size - size, vec, vec->end, &value);
    return; // RET 0x1C
  }

  if (begin && new_size < size) {
    Elem28* new_end = (Elem28*)((char*)begin + new_size * 0x1c);
    FUN_0045cce0(vec, &tmp_it, new_end, vec->end);
  }
  // RET 0x1C
}
```

## Open questions

- Product/PDB T English (anmAnimMaster scaffold residual only).
- Shrink helper `FUN_0045cce0` dual-unowned.
- Sole parent `FUN_00963fb0` not dual-owned.
- Runtime / bit-exact / differential.
