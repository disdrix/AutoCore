# Annotated low-level: FUN_00405920 → CsSList_RemoveFirstByPredicate_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00405920` |
| VA | `0x00405920`–`0x004059b9` inclusive (**154 B** / `0x9A`) |
| Ghidra | `FUN_00405920` |
| Canonical name | `CsSList_RemoveFirstByPredicate_Inferred` |
| System | client containers / CS-protected singly-linked list (inventory-transfer residual plate; body is generic CsSList) |
| Date | 2026-08-05 (MEGA-042 OWN dual refresh) |
| Prior scaffold | `Named_CalleeOf_Named_CalleeOf_Client_SendInventoryAddItem_00405920` (**retired**) |

## Machine-level notes

- Source: raw capture + MEGA-042 live re-verify (decompile ≡ scaffold CF).
- Prefer assembly for ABI: **`RET 8`**, **ECX=list**, stack pred + cookie.
- Same list/node layout as dualed **Append** `004024d0` and **Contains** `004e15a0`.
- Always dual-CS (outer `+0x28` then inner `+0x10`) — like Contains; unlike Append's count≤3 outer gate.
- Removes **first** matching node only; not remove-all.
- Node freed via vtbl slot0 scalar-deleting dtor (`PUSH 1; CALL [EDX]` thiscall).

## Signature (sealed)

```c
// __thiscall ECX = CsSList*; stack pred*, cookie; void; RET 8
typedef char (__cdecl *CsSList_Pred_Inferred)(void *payload, void *cookie);

void __thiscall CsSList_RemoveFirstByPredicate_Inferred(
    void *list /*ECX*/,
    CsSList_Pred_Inferred pred,
    void *cookie);
```

| Formal | Source | Conf |
|---|---|---|
| list | ECX → EDI | **Confirmed** |
| pred | stack `[ESP+4]` | **Confirmed** |
| cookie | stack `[ESP+8]` | **Confirmed** |
| cleanup | `RET 8` (`C2 08 00`) | **Confirmed** |
| return | void | **Confirmed** |

## Layout (sealed)

| Off | Field |
|----:|-------|
| list+0x04 | head* |
| list+0x08 | tail* |
| list+0x0C | count (InterlockedDecrement on remove) |
| list+0x10 | CRITICAL_SECTION (inner, always) |
| list+0x28 | CRITICAL_SECTION (outer, always) |
| node+0x00 | vtbl* |
| node+0x04 | payload* |
| node+0x08 | next* |

## Control flow (annotated)

```c
void __thiscall CsSList_RemoveFirstByPredicate_Inferred(
    int list, code *pred, undefined4 cookie)
{
  undefined4 *node;
  undefined4 *prev;
  undefined4 *cursor;
  char match;

  // Outer then inner — order matches Contains twin
  EnterCriticalSection((LPCRITICAL_SECTION)(list + 0x28));
  EnterCriticalSection((LPCRITICAL_SECTION)(list + 0x10));

  cursor = *(undefined4 **)(list + 4); // head
  prev = (undefined4 *)0x0;

  do {
    node = cursor;
    if (node == (undefined4 *)0x0) {
leave_both:
      LeaveCriticalSection((LPCRITICAL_SECTION)(list + 0x10));
      LeaveCriticalSection((LPCRITICAL_SECTION)(list + 0x28));
      return;
    }

    // pred(payload, cookie) — live sites use pointer equality stubs
    match = (*pred)(node[1], cookie);
    if (match != '\0') {
      // Unlink first match
      if (prev == (undefined4 *)0x0) {
        *(undefined4 *)(list + 4) = node[2]; // head = next
      } else {
        prev[2] = node[2];                   // prev->next = next
      }
      if (node[2] == 0) {
        *(undefined4 **)(list + 8) = prev;   // tail = prev
      }
      node[1] = 0;                           // clear payload
      (**(code **)*node)(1);                 // vtbl[0](1) delete-this
      InterlockedDecrement((LONG *)(list + 0xc));
      goto leave_both;
    }

    cursor = (undefined4 *)node[2]; // next
    prev = node;
  } while (true);
}
```

## Callers (evidence; not dualled)

| Site | Parent | Notes |
|------|--------|-------|
| `0x004d4855` | `FUN_004d4790` Object_QueueDelete | After Contains true; list @ `this+0xe648`; pred `0x4cd580` (ptr-eq) |
| `0x00944d8f` / `0x00944e1e` | `FUN_00944d50` object teardown | list @ `+0xe5fc`; pred `0x92d8e0` (ptr-eq) |

Hint plate `SendInventoryAddItem nested` is **parent seed only** — body is multi-system CsSList remove.

## Open gaps

- Product C++ class / vtbl English for node `PTR_FUN_009cb340` (shared with Append).
- Which list instance is inventory-transfer vs delete-queue vs world-manager.
- Runtime / bit-exact / differential (Terminal **false**).
