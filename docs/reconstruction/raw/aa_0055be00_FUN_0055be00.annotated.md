# Annotated low-level: StdVector_InsertN_Elem28 (`FUN_0055be00`)

| Field | Value |
|---|---|
| Stable ID | `aa_0055be00` |
| VA | `0x0055be00`–`0x0055c11c` exclusive (**796 B**) |
| System | MSVC `vector<T>` insert-n for POD elem stride **0x1c** |
| Date | 2026-07-29 (W34-R) |

## Machine-level notes

- Source: raw capture + W34-R `read_memory` seal.
- Prefer assembly when decompiler conflicts — **bytes win** on RET / magic / movss.
- **ABI:** `__thiscall` ECX=vec; stack where*, count, value*; **`C2 0C 00`**.
- **Stride 0x1c**, max **`0x9249249`**, growth **1.5×**.
- **POD:** 7× movss value copy; reallocation **delete without destroy-range**.
- Distinct from wstring twin `004a7ad0` and char twin `004306b0`.
- Reject scaffold VOG_DEBUG / `Named_CalleeOf_*` plate.

## Sealed algorithm

1. Local POD copy of `*value` (7 dwords).
2. `count==0` → return.
3. size/cap via `/0x1c`.
4. `size+count > max` → `FUN_0055b3a0`.
5. Grow: 1.5× floor; new; relocate+fill; **delete only**; rebind.
6. In-place: hole or mid-shift.
7. **RET 0x0C** (no local dtor).

## Callers

- `FUN_0055c250` @ `0x0055c2ae` — resize grow (insert at end).
- `FUN_0055c310` @ `0x0055c363` — insert-one rebind (`count=1`).

## Pseudocode (annotated)

See raw W34-R append + clean `StdVector_InsertN_Elem28.cpp`.
