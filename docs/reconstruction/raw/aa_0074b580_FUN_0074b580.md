# Raw capture: FUN_0074b580

| Field | Value |
|---|---|
| **Stable ID** | `aa_0074b580` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0074b580` |
| **Canonical name** | `EmptyDtorStub` (prior scaffold `FUN_0074b580`) |
| **System** | shared empty leaf / EH unwind + trivial member dtor |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0074b580(void)

{
  return;
}
```

---

## Re-verify 2026-07-29 W31-B (append only)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `analyze_function_complete` + `read_memory` + xrefs. **No** `disassemble_bytes` |
| **Address** | `0x0074b580` only (**1 B** / `c3`); exclusive end `0x0074b581`; `cc` pad after |
| **ABI** | bare **`RET`** (`C3`); body ignores ECX and any stack args |
| **Classification** | **stub** |
| **Callees** | none |

### Live decompile

```c
void FUN_0074b580(void)

{
  return;
}
```

≡ original raw CF.

### `read_memory` @ `0x0074b580` (32 B)

```
c3 cc cc cc cc cc cc cc cc cc cc cc cc cc cc cc
8b 44 24 04 f3 0f 10 44 24 08 53 56 8b 30 8b d1 ...
```

| Bytes | Note |
|---|---|
| `c3` | sole body byte (`ret`) |
| `cc…` | int3 pad — **not** in function body |
| next code @ `0x0074b590` | unrelated leaf |

### Xref roles (20 CALL sites; sample)

| Role | Evidence |
|---|---|
| ListTrackedObj complete dtor empty member dtors | `FUN_004be2a0` @ `0x004be3ce`, `0x004be3de`, `0x004be3ee` — three LEA this+`0x474`/`0x444`/`0x414` |
| MSVC EH Unwind no-op dtor | `Unwind@009a0c87`…`Unwind@009a18be` (many) |
| Other owners | `FUN_004921e0`, `FUN_00496410`, `FUN_00496600`, `FUN_00496920` |

Distinct VA from mass-shared `EmptyRet` @ `0x0056f570` and `ObjectMotion_SlotEmptyDtor` @ `0x004b0bd0`.

### Rejected scaffold plate

`Named_CalleeOf_Mission_Uses_CVOGReaction_FailMissionNotify_0074b580` — body has no mission/reaction logic; multi-role empty stub only.
