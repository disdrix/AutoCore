# Raw capture: FUN_00497360

| Field | Value |
|---|---|
| **Stable ID** | `aa_00497360` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00497360` |
| **Canonical name** | `FUN_00497360` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00497360(int param_1)

{
  undefined4 *puVar1;
  void *pvVar2;
  
  puVar1 = *(undefined4 **)(param_1 + 4);
  pvVar2 = (void *)*puVar1;
  *puVar1 = puVar1;
  *(int *)(*(int *)(param_1 + 4) + 4) = *(int *)(param_1 + 4);
  *(undefined4 *)(param_1 + 8) = 0;
  if (pvVar2 != *(void **)(param_1 + 4)) {
                    /* WARNING: Subroutine does not return */
    operator_delete(pvVar2);
  }
                    /* WARNING: Subroutine does not return */
  operator_delete(*(void **)(param_1 + 4));
}
```
---

## Re-verify (W27-K OWN-ONLY dual) — 2026-07-29

| Field | Value |
|---|---|
| **Tools** | Ghidra `decompile_function` + `analyze_function_complete` + `read_memory` + `get_assembly_context`. **No** `disassemble_bytes`. |
| **Image** | `autoassault.exe` base `0x400000` |
| **Body** | `0x00497360`–`0x004973a7` (**72 B** / `0x48`; pad `CC` after `RET`) |
| **Ghidra range note** | `get_function_by_address` reported end `0x0049739b` (mid-CALL) — **incomplete**; full epilogue confirmed via `read_memory` through `MOV [ESI+4],0` / `POP ESI` / `RET`. |
| **Live decompile** | Ghidra collapses the node-walk into a single conditional `operator_delete` + false **noreturn** warnings. **Bytes seal a full free-loop**. |
| **Callees** | `operator_delete` only (x N nodes + 1 sentinel) |
| **Callers (sample)** | `FUN_004d64d0` CVOGSectorMap_Destructor (multiple LEA ECX list members); `FUN_004970b0`; `FUN_00575350`; `FUN_005b3b20`; `FUN_009c3800` — 11 xrefs |
| **Semantic name** | `StdList_Destroy_FreeHead` (MSVC circular list: clear all nodes, free sentinel, null `list+4`) |

### Full body hex (`read_memory` @ `0x00497360`, through RET)

```
568bf18b4e048b0189098b4e048949043b4604c74608000000007417578d49008b3850e89a24ffff83c4043b7e048bc775ee5f8b460450e88624ffff83c404c74604000000005ec3
```

### Byte-level CF (authoritative)

```
ESI = ECX                          ; list*
head = [ESI+4]
first = [head]                     ; head->_Next
[head] = head                      ; head->_Next = head
[head+4] = head                    ; head->_Prev = head
[ESI+8] = 0                        ; size = 0
if first == head: goto free_sentinel
loop:
  next = [first]                   ; node->_Next
  operator_delete(first)
  first = next
  if first != head: goto loop
free_sentinel:
  operator_delete([ESI+4])         ; free sentinel
  [ESI+4] = 0                      ; null head
  RET
```

### Machine notes vs decompile

1. **Loop sealed:** `MOV EDI,[EAX]` / `PUSH EAX` / `CALL operator_delete` / `CMP EDI,[ESI+4]` / `MOV EAX,EDI` / `JNZ loop` — decompiler single-delete is **wrong**.
2. **operator_delete noreturn:** false positive; both deletes fall through (`ADD ESP,4`).
3. **Null head after free:** `C7 46 04 00 00 00 00` then `5E C3` — not present in raw 2026-07-23 decompile body (decompiler stopped early).
4. **ABI sealed:** ECX = list object (`+4` head/sentinel*, `+8` size); bare `RET`; void.
