# Annotated low-level: CNDHash_TraversalLock_Set_Inferred (FUN_00402c40)

| Field | Value |
|---|---|
| Stable ID | `aa_00402c40` |
| VA | `0x00402c40`–`0x00402c69` (42 B / `0x2A`) |
| System | interaction-activation (shared hash primitive; partition MEGA-110) |
| Date | 2026-08-05 (MEGA-110 refresh) |
| Ghidra name | `FUN_00402c40` |
| Proposed name | `CNDHash_TraversalLock_Set_Inferred` |

## Machine-level notes

- Source: live Ghidra re-verify (MEGA-110) + preserved raw scaffold.
- **ABI:** ECX = CNDHash*; bare `RET`; void. Decompiler `__fastcall` is correct (ECX-only formal).
- **Flag:** byte at **`hash+0x1d`** is the TraversalLock flag (1 = locked for ordered walk).
- **Soft assert:** re-lock logs via `FUN_007a4480` then **still sets** flag=1 (no hard abort).
- **Not** CS-list TraversalLock (`List_TraversalLock` `0x004294f0` uses `+0x28` + EnterCS).
- **Not** identity `Named_VOG_DEBUG_STOP_*` — that string is assert-only.
- Pair consumer: dualed `CNDHash_TraverseToNext` (`0x00411900`) requires lock; many sites unlock by writing `*(hash+0x1d)=0` inline.

## Pseudocode (annotated)

```c
/* CNDHash_TraversalLock_Set_Inferred
 * Soft-set TraversalLock on a CNDHash (flag @ +0x1d).
 * ECX = hash*; bare RET; void.
 * If already locked: log HashError + VOG_DEBUG_STOP, then force lock anyway.
 */
void __fastcall CNDHash_TraversalLock_Set_Inferred(int hash /* ECX */)
{
  /* CMP byte [ESI+0x1d], 0 / JZ skip_log */
  if (*(char *)(hash + 0x1d) != '\0') {
    /* cdecl debug log: level 0, static C strings */
    FUN_007a4480(0, "HashError:TraversalLock, already locked for traversal"); /* 0x00a27cd0 */
    FUN_007a4480(0, "VOG_DEBUG_STOP");                                       /* 0x00a15844 */
    /* ADD ESP, 0x10 — cleanup both calls */
  }
  /* always: */
  *(unsigned char *)(hash + 0x1d) = 1;
  return; /* bare RET */
}
```

## Layout (sealed for this VA)

| Off | Role |
|----:|------|
| `hash+0x1d` | TraversalLock flag (byte): 0 free, 1 locked |

## Call graph (OWN VA only)

```text
callers (21 funcs / 30 CALL) ──► FUN_00402c40 ──► FUN_007a4480 ×2 (soft log only)
                                      │
                                      └─ sets hash+0x1d = 1
```

Sample parent chain (evidence, not dualled here):

```text
Client_UpdateNpcInteractIcons (0x0091b8d0)
  ESI = *(DAT_00d1b6d8 + 0x548)   ; CNDHash*
  ECX = ESI
  CALL CNDHash_TraversalLock_Set_Inferred
  CALL CNDHash_TraverseToNext (0x00411900)
  … walk …
  (unlock often inline: hash+0x1d = 0)
```

## Open questions

- Product/PDB CNDHash method English (SetTraversalLock vs LockForTraversal, etc.).
- Whether a shared unlock helper exists elsewhere (many sites inline clear).
- Runtime Confirmed / bit-exact (no Launcher this wave).
