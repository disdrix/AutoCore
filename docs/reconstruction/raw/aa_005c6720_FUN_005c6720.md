# Raw capture: FUN_005c6720

| Field | Value |
|---|---|
| **Stable ID** | `aa_005c6720` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005c6720` |
| **Canonical name** | `FUN_005c6720` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
bool __thiscall FUN_005c6720(int param_1,int param_2)

{
  int *piVar1;
  int iVar2;
  
  if (((*(uint *)(param_1 + 0x18) & *(uint *)(param_1 + 0x1c)) == 0xffffffff) &&
     (iVar2 = *(int *)(*(int *)(param_2 + 4) + 4),
     (*(uint *)(iVar2 + 0x164 + param_2) & *(uint *)(iVar2 + 0x168 + param_2)) == 0xffffffff)) {
    return *(int *)(param_1 + 0x10) == param_2;
  }
  piVar1 = (int *)(*(int *)(*(int *)(param_2 + 4) + 4) + 0x164 + param_2);
  if ((*piVar1 == *(int *)(param_1 + 0x18)) &&
     ((piVar1[1] == *(int *)(param_1 + 0x1c) && ((char)piVar1[2] == *(char *)(param_1 + 0x20))))) {
    return true;
  }
  return false;
}
```

---

## W38-S re-verify (2026-08-04)

**Tools:** `batch_decompile` / `decompile_function` + `analyze_function_complete` + `get_function_by_address` + `read_memory` + xrefs/callers. **No** `disassemble_bytes`.

| Field | Value |
|---|---|
| Body | `0x005c6720`–`0x005c6788` exclusive (**104 B** / `0x68`); pad `CC` |
| Ghidra meta | Entry `005c6720`, end `005c6787` (last insn last-byte inclusive) — **matches** exclusive end |
| ABI | `__thiscall`; ECX = **payload**; stack **`(object*)`**; returns **bool** in AL/EAX; **`ret 4`** (`c2 04 00` ×3 exits) |
| Leaf | callees: none |
| Callers | `FUN_004bfd10` @ `004bfd3a` (untrack); `FUN_004bfdb0` @ `004bfdda` (set flag `+0x51`) |
| Classification | leaf match predicate |

### Semantics (bytes ≡ decompile)

1. **Invalid-TFID pair path:** if `(payload.tfid_lo & payload.tfid_hi) == 0xFFFFFFFF` **and** same for object TFID at `object + *(*(object+4)+4) + 0x164` (lo&hi), return **`payload+0x10 == object`** (pointer identity via `setz`).
2. **Else TFID path:** compare payload `{+0x18 int32, +0x1c int32, +0x20 int8}` to object TFID triple at same base+`0x164` (fields `+0/+4/+8`) — same 9-byte equality as sealed `TFID_EqualsObjectId` (`0x00404aa0`).
3. Mismatch → **false**.

### Full body hex (104 B)

```
8b411823411c83f8ff8b44240475288b50048b5204568bb4026401000023b4026801000083feff5e750d33d23941100f94c28ac2c204008b50048b52048d8402640100008b103b511875188b50043b511c75108a40083a41207508b801000000c2040033c0c20400
```

### Notes

- Invalid test is **`(lo & hi) == -1`**, which requires **both** dwords `0xFFFFFFFF` (not merely either).
- Object TFID address uses MI/vb-style adjustment: `adj = *(*(object+4)+4)`; TFID @ `object+adj+0x164`.
- Closes W37-T residual “full dual of match predicate `FUN_005c6720`”.
