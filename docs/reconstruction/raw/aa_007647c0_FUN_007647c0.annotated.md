# Annotated low-level: FUN_007647c0 → Host_RegisterObjectAndAccumulateBounds

| Field | Value |
|---|---|
| Stable ID | `aa_007647c0` |
| VA | `0x007647c0`–`0x0076482d` exclusive (**109 B**) |
| System | host / graphics bounds |
| Date | 2026-07-29 W36-L |
| Canonical | `Host_RegisterObjectAndAccumulateBounds` (**Inferred**) |

## Machine-level notes

- **thiscall + 1 stack arg**, **RET 4**: ECX=host, stack0=object*.
- First act: `FUN_004406e0` with EAX=host+0xbc, ESI=&object* → ptr-vector push_back (same helper as pack-manager vector push).
- Object virt +0x20(host); optional virt +0x80 (linkage gate on +0x58/+0x5c/+0x60); optional virt +0x5c (flag bit0 @ +0xbc).
- Host virt +0x0c(object+0x28, object+0x64) → xformRef; then `Bounds_AccumulateTransformedLocal`(host+4, xform, local@+0x28, scale@+0x64).
- Stack packing: `push scale; push local; call host+0x0c; push xform; lea ecx,[host+4]; call 0074c9c0` — at Bounds entry stack is (xform, local, scale) for **ret 12**.
- Prefer bytes over decompiler void return (RET 4 sealed).
- Reject scaffold name `Named_CalleeOf_CVOGTacArc_InitializeMesh_007647c0` — multi-caller host helper, not TacArc-only.

## Pseudocode (annotated)

```c
// ECX=host, stack0=object*, RET 4
void Host_RegisterObjectAndAccumulateBounds(Host* host, Object* obj)
{
  // push obj* into host->childPtrs @ +0xbc  (FUN_004406e0)
  VecPtr_PushBack(&host->childPtrs /*+0xbc*/, &obj);

  obj->vtbl[+0x20](host);                 // object prep with host

  if (obj->field_58 && obj->field_5c &&
      *(int*)(obj->field_5c + 0x1c) != obj->field_60)
    obj->vtbl[+0x80]();

  Object* scaleSrc = obj->field_64;       // +0x64
  if (obj->flags_bc & 1)
    obj->vtbl[+0x5c]();

  BoundsVolume* local = &obj->localBounds; // +0x28
  void* xform = host->vtbl[+0x0c](local, scaleSrc);
  Bounds_AccumulateTransformedLocal(
      &host->bounds /*+0x04*/, xform, local, scaleSrc);
}
```

## Related (not owned)

| VA | Role |
|---|---|
| `0x004406e0` | ptr-vector push helper (EAX=vec, ESI=&value) |
| `0x0074c9c0` | `Bounds_AccumulateTransformedLocal` (W21-O sealed) |
