# Annotated low-level: FUN_004970b0 / Class_009c7a1c_CompleteDtor

| Field | Value |
|---|---|
| Stable ID | `aa_004970b0` |
| VA | `0x004970b0`–`0x004971e0` exclusive (**304 B**) |
| System | class / complete object destructor (vtbl `PTR_FUN_009c7a1c`) |
| Date | 2026-07-29 (W28-M dual seal) |

## Machine-level notes

- **Role:** MSVC complete-object destructor body for the class whose vtbl is `PTR_FUN_009c7a1c`.
- **Scalar deleting sibling:** `FUN_004975e0` = **vtbl[0]** — calls this, then optional `operator_delete(this)`, `RET 4`.
- **thiscall:** `mov esi, ecx` at entry; bare `RET` after SEH teardown.
- **SEH:** push `-1`, handler `LAB_009a0d7e`, install FS:[0]; EH state `6` while destroying the `0x28`-stride array.
- **Array @ +0x28:** cookie-prefixed (`count` at `ptr-4`); `_eh_vector_destructor_iterator_(ptr, 0x28, count, FUN_005b8000)` then `operator_delete[](ptr-4)`.
- **After array:** zero `+0x28`, `+4`, `+8` (ctor twin `FUN_00496f70` writes `0x40` into `+4`/`+8` — capacity/limit pair, not the StdList head).
- **Six raw buffers:** each `if (begin) operator_delete(begin); begin=end=cap=0` at offsets listed in raw W28-M append. Ghidra false-noreturn on `operator_delete` is **wrong** (CALL+ADD ESP,4+continue).
- **StdList @ +0x0C:** bytes `lea ecx,[esi+0x0C]; call FUN_00497360` (`StdList_Destroy_FreeHead`, W27-K accept). Decompiler drops the LEA — **do not** treat as destroying root `this`.
- **Product English:** open (vtbl-adjacent rdata includes `"_den.pgm"` / `"../maps/"` but not proven class name).

## Pseudocode (byte-corrected)

```c
// __thiscall ECX=this; void; bare RET
void Class_009c7a1c_CompleteDtor(void *self)
{
  // SEH frame (LAB_009a0d7e), state=6 for vector path
  *(void **)self = &PTR_FUN_009c7a1c;

  void *arr = *(void **)((char *)self + 0x28);
  if (arr) {
    int n = *(int *)((char *)arr - 4);
    _eh_vector_destructor_iterator_(arr, 0x28, n, FUN_005b8000);
    operator_delete[]((char *)arr - 4);
  }
  *(void **)((char *)self + 0x28) = 0;
  *(void **)((char *)self + 0x04) = 0;
  *(void **)((char *)self + 0x08) = 0;

  // six buffer triples (begin/end/cap) — see sealed offsets
  free_buf_triple(self, 0xBC);
  free_buf_triple(self, 0xAC);
  free_buf_triple(self, 0x9C);
  free_buf_triple(self, 0x40);
  free_buf_triple(self, 0x30);
  free_buf_triple(self, 0x1C);

  StdList_Destroy_FreeHead((char *)self + 0x0C);  // LEA ECX sealed
  // restore FS:[0], RET
}
```

## Open questions

- Product / MSVC demangled class name.
- Semantic of the six buffer triples (vector vs arena).
- Element type of `0x28` array beyond dtor `FUN_005b8000`.
- Runtime / bit-exact differential.
