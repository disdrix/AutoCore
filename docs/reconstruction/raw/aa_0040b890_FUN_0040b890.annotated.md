# Annotated low-level: StdTree_Insert_IntKey_Isnil19_EaxEbx_Inferred (FUN_0040b890)

| Field | Value |
|---|---|
| Stable ID | `aa_0040b890` |
| VA | `0x0040b890`–`0x0040b8eb` inclusive (**92 B** / `0x5C`) |
| System | inventory-transfer partition residual; unit = MSVC tree always-insert |
| Date | 2026-08-05 (R13-019 dual; scaffold 2026-07-23) |
| Canonical | `StdTree_Insert_IntKey_Isnil19_EaxEbx_Inferred` |

---

## Machine-level notes

- **Authority:** assembly + `read_memory` over decompiler phantoms (`in_EAX` / `unaff_EBX`).
- **ABI:** EAX=map, EBX=value*, stack out pair, RET 4, returns out.
- **isnil @ +0x19**, key **signed int @ +0x0C**, node **0x1C**, color @ **+0x18** (callee).
- **Always insert** — no equal-key `inserted=0` arm; out.inserted forced **1**.
- Sole caller: MissionTracker secondary fill `FUN_0092b2a0` @ `0x0092b4fc` (temp ranking map).
- Callee: `FUN_0040bcd0` insert+RB (`map/set<T> too long`, buy `0x1C`, Lrot `0040b910` / Rrot `00446b10`).

## Annotated CF

```c
// EAX = MapShell*; EBX = Val* (key = *(int32*)EBX); stack = InsertPair* out; RET 4
InsertPair *StdTree_Insert_IntKey_Isnil19_EaxEbx_Inferred(
    MapShell *map /*EAX*/, InsertPair *out /*stack*/, const Val *value /*EBX*/)
{
  MapNode *head = map->head;          // [EDI+4]
  MapNode *cur  = head->parent;       // root
  bool goLeft   = true;
  MapNode *where = head;              // ECX when empty

  if (cur->isnil == 0) {              // isnil @ +0x19
    int key = value->key;             // [EBX]
    do {
      where = cur;                    // ECX = parent before descend
      goLeft = (key < (int)cur->key); // SETL vs node+0x0C (signed)
      cur = goLeft ? cur->left : cur->right;
    } while (cur->isnil == 0);
  }

  // FUN_0040bcd0: EDI=map, ECX=where, stack (tempOut, goLeft, value*)
  InsertPair *tmp = Tree_InsertAndRebalance_Isnil19(where, goLeft, value);
  out->it = tmp->it;
  out->inserted = 1;                  // ALWAYS — no find/equal path
  return out;
}
```

## Open residual (not OWN)

- Dual of insert helper `FUN_0040bcd0` / buynode `FUN_0040c060` / node ctor `FUN_0040c210`.
- Product English for temp map T in MissionTracker ranking arm.
- Runtime / bit-exact / differential.
