# Annotated low-level: CWndWaypointIcon_CompleteDtor

| Field | Value |
|---|---|
| Stable ID | `aa_00832fa0` |
| VA | `0x00832fa0`–`0x0083300f` (**112 B**) |
| System | UI / CWndWaypointIcon complete destructor |
| Date | 2026-08-05 (WQ9J-B re-seal) |
| Canonical name | `CWndWaypointIcon_CompleteDtor` |
| Ghidra name | `FUN_00832fa0` |

## Machine-level notes

- **ABI:** `__thiscall` — ECX = `CWndWaypointIcon*`; bare `RET`; void. SEH frame `LAB_009b33be`.
- **Role:** MSVC **complete-object destructor** only. Does **not** free `this`. Scalar peer `0x00416350` (vtbl[0] of `PTR_FUN_00a7000c`) calls this then optional `operator_delete`.
- **Vtbl:** install `PTR_FUN_00a7000c` at `*this`. RTTI COL seals **`.?AVCWndWaypointIcon@@`**.
- **Offsets (byte):**
  - `+0x488` (`this[0x122]`): owned heap pointer. If non-null: run `FUN_0096efd0(owned)` then `operator_delete(owned)` (`ADD ESP,4` cdecl). Always store `0` afterward.
- **Callees:**
  - `FUN_0096efd0` — refcount/smart-ptr release helper on the owned block (thiscall ECX=owned).
  - `operator_delete` @ `0x00489822` — Ghidra false-noreturn; bytes fall through.
  - `FUN_007b5be0(this)` — base UI window complete (`PTR_FUN_00a960ac`; owner soft-assert strings).
- **Inheritance note:** CWndWaypointIcon complete chains **directly** to `FUN_007b5be0` (not via CNDUIWndBuffered `FUN_0078ca80`, unlike CWndVehicle).
- Prefer assembly when decompiler truncates after `operator_delete`.

## Pseudocode (annotated)

```c
// ECX = this (CWndWaypointIcon*). SEH. void. bare RET.
void __thiscall CWndWaypointIcon_CompleteDtor(undefined4 *self)
{
  // *self = &PTR_FUN_00a7000c;  // CWndWaypointIcon vtbl
  // EH state = 0

  void *owned = (void *)self[0x122]; // +0x488
  if (owned != NULL) {
    FUN_0096efd0(owned);      // thiscall; release nested refs
    operator_delete(owned);   // cdecl; falls through (not noreturn)
  }
  self[0x122] = 0;            // always null +0x488

  // EH state = -1
  FUN_007b5be0(self);         // base CNDUIWnd-family complete
}
```

## Open residual (not OWN)

- Product layout of owned block at `+0x488` and full dual of `FUN_0096efd0` / `FUN_007b5be0`.
- Parent rename of scalar `Class_00a7000c_ScalarDeletingDtor_Inferred` → `CWndWaypointIcon_ScalarDeletingDtor` (not this agent).
- Runtime / bit-exact / differential.
