# Raw capture: FUN_00409db0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409db0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409db0` |
| **Canonical name** | `FUN_00409db0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00409db0(undefined4 param_1,int param_2)

{
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  puStack_c = &LAB_009bc620;
  local_10 = ExceptionList;
  local_8 = 0;
  ExceptionList = &local_10;
  for (; param_2 != 0; param_2 = param_2 + -1) {
    FUN_00409f50();
  }
  ExceptionList = local_10;
  return;
}
```

## WQ9I-A re-verify (2026-08-05) — append only

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **WQ9I-A** |
| **Tools** | `batch_decompile` + `analyze_function_complete` + `read_memory` + callers/xrefs; **no** `disassemble_bytes` |
| **Live decompile ≡ raw CF** | **Yes** (loop `FUN_00409f50` × count; SEH frame) |
| **Body** | `0x00409db0`–`0x00409e12` inclusive (**99 B** / `0x63`); plain ret `C3` |
| **Named** | `StdVector_ConstructN_Elem12_Inferred` |

### Byte ABI (read_memory @ entry)

```
push ebp; mov ebp,esp
SEH prolog (push -1; push LAB_009bc620; fs:[0] chain)
push ecx (space); push ebx,esi,edi
mov [ebp-0x10],esp
mov ebx,ecx              ; EBX = value* (ECX formal)
mov edi,[ebp+0xC]        ; EDI = count
mov esi,[ebp+8]          ; ESI = dest
mov [ebp-4],0            ; SEH state
loop:
  test edi,edi / jbe done   ; unsigned empty early-out
  mov ecx,ebx            ; ECX = value* (src for PodCopy)
  mov eax,esi            ; EAX = dest
  call FUN_00409f50      ; PodCopy 3 dwords (0x0C)
  sub edi,1
  mov [ebp+0xC],edi
  add esi,0x0C           ; advance dest by 12
  mov [ebp+8],esi
  jmp loop
done:
  SEH teardown; pop edi,esi,ebx
  leave; ret             ; C3 (caller cleans 8 B stack)
```

### Callee `FUN_00409f50` (sealed via decompile + read_memory)

- **EAX** = dest dword*; **ECX** = src value*
- Copy **3** dwords (`[0]`,`[1]`,`[2]`); null-dest no-op (`test eax; jz`)
- = POD construct of **0x0C** bytes (parent steps +0xC)

### Callers / xrefs (2)

| Site | Function | Role |
|---|---|---|
| `0x00406eec` | `FUN_00406ee0` Ufill elem12 | construct-N then return `dest + count*0xC` |
| `0x004062ef` | `FUN_004062a0` push_back fast | construct 1 at end when capacity remains |

### Scaffold gaps closed

- Decomp omits ECX value formal, dest/count registers, and `add dest,0xC` — sealed by bytes.
- PodCopy leaf uses **EAX/ECX** (not EAX/EDX like 0x28 twin `00409f30`).
