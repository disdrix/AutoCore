# Annotated: `aa_00507290` Host_ActionList_Push

| Field | Value |
|---|---|
| Stable ID | `aa_00507290` |
| VA | `0x00507290` |
| Body | `0x00507290`–`0x005072c5` |
| Canonical | `Host_ActionList_Push` (was `FUN_00507290`) |
| System | physics / action host list |
| Date | 2026-07-29 (W19-O dual A/B) |
| Source | live Ghidra `decompile_function` + `read_memory`; raw body preserved |

---

## Signature

```c
// MSVC __thiscall: ECX = host object; stack = void* item; RET 4
void Host_ActionList_Push(void *host, void *itemPtr);
```

---

## Layout (host vector)

| Offset | Type | Role |
|---:|---|---|
| `host+0x94` | `void**` | data pointer (array of 4-byte elements) |
| `host+0x98` | `int` | **count** |
| `host+0x9c` | `uint` | **capacity** (low 31 bits); high bit may mark allocator ownership (masked before compare) |

Stride / element size: **4** (pointer). Grow via `FUN_005b3370(&vector, 4)` when `count == (capacity & 0x7fffffff)`.

---

## Pseudocode (annotated)

```c
void __thiscall Host_ActionList_Push(int host /* ECX */, void *item /* stack */)
{
  // vector lives contiguously at host+0x94 { data, count, capacity }
  if (*(uint*)(host + 0x98) == (*(uint*)(host + 0x9c) & 0x7fffffff)) {
    FUN_005b3370((int*)(host + 0x94), /*elemSize*/ 4);  // cdecl grow; doubles capacity
  }
  // store then bump count
  *(void**)(*(int*)(host + 0x94) + *(int*)(host + 0x98) * 4) = item;
  *(int*)(host + 0x98) += 1;
}
```

---

## Callers (ECX must be host — decompiler often wrong)

| Caller | VA call site | Stack item | ECX at call |
|---|---|---|---|
| `Phys_ActionBase_ctor` | `0x006363a1` | action `this` | **host** (still in ECX from host arg) |
| `FUN_00636220` | `0x00636246` | action | **host** from source+4 |
| `FUN_0066e660` | `0x0066e698`, `0x0066e6ac` | action | **host** at action+0x18 / +0x1c |

Phys_ActionBase dual already seals: after push, caller addrefs host `*(u16*)(host+6)++`. This unit does **not** touch refcounts.

---

## Callee

| Address | Name | Role |
|---|---|---|
| `0x005b3370` | `FUN_005b3370` | growable buffer resize (alloc via `DAT_00b05060` vtbl+0x10, free old via +0x14, copy via `FUN_006380a0`) |

---

## Open gaps

1. Product C++ type of **host** (unified across callers? residual from Phys_ActionBase dual).
2. High-bit semantics on capacity `+0x9c` (allocator-owned flag — inferred from mask only).
3. Whether this list is island/world action list vs local host owner list (sibling note: world register `FUN_0055fe50` is a **different** path).
4. Runtime / bit-exact — open.
