# Annotated low-level: FUN_0092ce90 / Client_RefreshMissionRelatedUi

| Field | Value |
|---|---|
| Stable ID | `aa_0092ce90` |
| VA | `0x0092ce90` |
| System | missions-progression |
| Date | 2026-07-23; dual A/B seal 2026-07-29 |

## Machine-level notes

- Prologue: `PUSH ESI`; client base in **EDI**; ESI holds panel pointers.
- Primary panel: `[EDI+0x107c]` — mission UI root (shared with HideMissionDialogIfOpen).
- Secondary panel: `[EDI+0x10B8]` — dirty-flag path only.
- Vtable gates (thiscall, ECX=panel):
  - `+0xD0` → bool (ready/active; product name open)
  - `+0x3D8` → bool (visible-style; product name open)
  - `+0x448` → void action on primary only (refresh/update family; **not** proven “hide”)
- Secondary store: `MOV BYTE PTR [ESI+0x520], 1` (dirty flag).
- Epilogue: `POP ESI; RET`. No stack args. Leaf.
- Sibling `Client_HideMissionDialogIfOpen` @ `0x0092ce50`: ESI client, `+0x107c` only, no `+0xd0`, action `+0x448` + layout `+0x34c`.

## Pseudocode (annotated)

```c
// client in EDI (caller convention)
void FUN_0092ce90(void)
{
  int *panel;   // ESI
  char ok;

  panel = *(int **)(client + 0x107c);
  if (panel != NULL) {
    ok = panel->vtbl[0xd0/4](panel);       // ready?
    if (ok) {
      ok = panel->vtbl[0x3d8/4](panel);    // visible?
      if (ok) {
        panel->vtbl[0x448/4](panel);       // refresh/update
      }
    }
  }

  panel = *(int **)(client + 0x10b8);
  if (panel != NULL) {
    ok = panel->vtbl[0xd0/4](panel);
    if (ok) {
      ok = panel->vtbl[0x3d8/4](panel);
      if (ok) {
        *(uint8_t *)((char *)panel + 0x520) = 1;  // dirty
      }
    }
  }
}
```

## Callers (Ghidra xrefs)

`Client_RecvObjectiveState`, `Client_RecvInventoryAddItem`, `Client_RecvInventoryDropResponse`,
`FUN_00810170`, `FUN_008136b0`, `FUN_0093c3b0`, `FUN_00946c00`, `FUN_00952bc0`, `FUN_00955010`.

## Open questions

- Product names for panels / vtbl methods.
- Consumer of dirty `+0x520`.
- Whether `+0x448` is shared hide/refresh base method.
