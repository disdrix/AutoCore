# Annotated low-level: Client_LoadSkillsFX

| Field | Value |
|---|---|
| Stable ID | `aa_00541aa0` |
| VA | `0x00541aa0`–`0x00541baf` (271 B) |
| System | skills-abilities / client-fx |
| Date | 2026-07-23 scaffold; dual seal annotate 2026-07-29 W23-F |

## Machine-level notes

- Source: raw + live decompile/`read_memory`/`analyze_function_complete` + parent `FUN_00541e70` decompile.
- **ABI:** `__thiscall` ECX = CloneBase/content host*; plain **`ret`** (`C3`); SEH (`LAB_009a3ff9`).
- **Name:** Parent logs `"before LoadSkillsFX and SpecialFXMaster"` immediately before this call → **`Client_LoadSkillsFX`**.
- Walks global **SkillDefMap** (`FUN_0054b480` / sealed `SkillDefMap_GetGlobal` → `&DAT_00b04734`).
- SkillDef RB nodes: isnil `@+0x641`; int skill id at `[3]` (`+0x0C`); C-string at `+0x5B6`.
- Non-empty string test: scan from `node+0x5B6` to NUL; if end ptr ≠ `start+1` (i.e. length > 0) then load FX.
- Per hit: `operator_new(0xC)` → construct `FxMasterCatalog` at entry+4 (`FUN_004a1620`) → store skill id at entry+0 → `FUN_00545890(host+0xF0C, skillId, entry, 0)` CNDHash insert (fail on duplicate).
- In-order successor uses isnil `@+0x641` (standard left-min / parent climb).
- Sole xref caller: `FUN_00541e70` @ `0x005426d0`.

## Pseudocode (annotated)

```c
/* Client_LoadSkillsFX — populate host skills-FX CNDHash from SkillDefMap
   this (ECX) = content host with CNDHash* at +0xF0C
   For each skill def with non-empty string@+0x5B6:
     entry = new SkillFxEntry{ skillId, FxMasterCatalog{} }
     CNDHash_Insert(host->skillsFxHash, skillId, entry, checkOnly=0)
*/

struct SkillFxEntry {
  int  skillId;            // +0
  // FxMasterCatalog at +4 (8 B: map* + flags) via FUN_004a1620
};

void __thiscall Client_LoadSkillsFX(void* host /* ECX */)
{
  void* map = SkillDefMap_GetGlobal();           // FUN_0054b480
  void* header = *(void**)((char*)map + 4);
  void* node = *(void**)header;                  // begin = header->_Left

  if (node == header) return;

  do {
    char* p = (char*)node + 0x5B6;
    char* start = p;
    while (*p != '\0') p++;
    if (p != start + 1) {                        // non-empty string
      SkillFxEntry* entry = (SkillFxEntry*)operator_new(0xC);
      if (entry) {
        FxMasterCatalog_Ctor((char*)entry + 4);  // FUN_004a1620 thiscall
      } else {
        entry = 0;
      }
      int skillId = ((int*)node)[3];             // node+0x0C
      entry->skillId = skillId;
      // FUN_00545890(this=*(host+0xF0C), key=skillId, value=entry, flag=0)
      CNDHash_Insert(*(void**)((char*)host + 0xF0C), skillId, entry, 0);
    }

    // in-order successor (isnil @ +0x641)
    node = TreeSuccessor_Isnil641(node);
  } while (node != header);
}
```

## Confidence

| Claim | Level |
|---|---|
| Role = LoadSkillsFX (parent string) | **High** |
| thiscall host +0xF0C hash | **High** (bytes) |
| SkillDefMap walk + string gate | **High** |
| entry 0xC = id + FxMasterCatalog | **High** |
| Product English for string@+0x5B6 | **Open** (FX path/name) |
| Full SkillDef node layout | **Open** |
| Runtime / bit-exact | **Open**
