# Annotated low-level: FUN_00466ea0 → StdVector_InsertN_Elem28_CountEcx_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00466ea0` |
| VA | `0x00466ea0`–`0x00467174` (725 B) |
| System | util / container (generic POD vector insert-n) |
| Date | 2026-07-29 (W34-Q dual seal) |
| Canonical | `StdVector_InsertN_Elem28_CountEcx_Inferred` |

## Machine-level notes

- Source: raw capture + W34-Q re-verify (`decompile` + `read_memory`).
- **Bytes win** on ABI: ECX=count, EDX=vec, stack where+value, `RET 8`.
- Element stride **0x1c**; max elems **0x9249249**; grow **1.5×**.
- Throw path: `FUN_004540b0` (`"vector<T> too long"`).
- Prefer assembly/bytes when decompiler under-counts formals (value* @ `[ebp+0xC]`).
- Helpers use register formals (EAX dest, etc.) — residual outside OWN scope.
- **Not** basic_string insert (`004306b0`); POD 7-dword copy only.

## ABI

```
void InsertN(uint32_t count /*ECX*/, VectorElem28* vec /*EDX*/,
             Elem28* where /*[ebp+8]*/, const Elem28* value /*[ebp+0xC]*/);
// ret 8
```

## Control flow (annotated)

1. SEH frame (`LAB_009bdc90`); snapshot `*value` (0x1c) to stack locals.
2. `cap = begin ? (capEnd-begin)/0x1c : 0`.
3. if `count==0` → restore EH, ret 8.
4. `size = begin ? (end-begin)/0x1c : 0`.
5. if `0x9249249 - size < count` → `FUN_004540b0` (noreturn).
6. **Realloc** if `cap < size+count`:
   - `newCap = cap+(cap>>1)` or 0 on overflow; floor to `size+count` (via `FUN_00469c50`+count).
   - `operator_new(newCap*0x1c)`; copy prefix / fill-n / copy suffix (`FUN_004673b0`/`FUN_00467320`);
   - `operator_delete` old begin; write triad; ret 8.
7. **In-place**:
   - if `(end-where)/0x1c < count`: extend hole, fill, bump end.
   - else: reverse-move (`FUN_0046a240`), assign (`FUN_0045efe0`).
8. Restore EH; ret 8.

## Pseudocode (sealed structural)

```c
// See clean: StdVector_InsertN_Elem28_CountEcx_Inferred.cpp
// Live decompile body preserved in raw (authoritative CF text).
```

## Confidence

| Claim | Level |
|---|---|
| Body bounds + RET 8 | **High** |
| ABI formals | **High** |
| Insert-n algorithm | **High** |
| Helper register map | Medium |
| Product T name | Open |
