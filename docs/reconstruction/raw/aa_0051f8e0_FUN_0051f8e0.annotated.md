# Annotated low-level: FUN_0051f8e0 → LookupRaceDisplayName_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0051f8e0` |
| VA | `0x0051f8e0` |
| Body | `0051f8e0`–`0051f90b` exclusive (**43 B** / `0x2B`) |
| System | skills-abilities |
| Date | 2026-08-05 (R12-017 dual seal) |
| Role name | `LookupRaceDisplayName_Inferred` |
| Rejected alias | `Named_CalleeOf_Named_Combat_0051f8e0` |

## Machine-level notes

- Source: raw scaffold 2026-07-23 + R12-017 live re-verify.
- Pure **cdecl** leaf: stack raceId → static product English race name.
- Prefer assembly when decompiler conflicts (none here; CF matches).
- No callees; no stores; no TLS ring (callers apply `FUN_007a6de0` localization wrapper).
- Cascade encoding: successive `sub eax, imm` / `jz` (not a jump table).

## Pseudocode (annotated)

```c
// cdecl; raceId @ [esp+4]; returns const char* (rodata); plain RET
const char * LookupRaceDisplayName_Inferred(int raceId /* param_1 */)
{
  if (raceId == 0) {
    return "Human";   // 0x00a40958
  }
  if (raceId != 1) {
    if (raceId != 2) {
      return "Unknown"; // 0x00a37c4c  (any other int, incl. negative)
    }
    return "Biomek";    // 0x00a40948
  }
  return "Mutant";      // 0x00a40950
}
```

## Call graph (owned leaf only)

```text
UI_AppendItemRequirements (0x00845360)        @ 0x00845520
  └─ FUN_0051f8e0(raceId)  [OWN R12-017]

UI_AppendCraftRequirements_Inferred (0x00846820)  @ 0x00846d8c  [partition parent]
  └─ FUN_0051f8e0(raceId) → "Required Faction" line

Character_GetRaceDisplayName_Inferred (0x00521800)  [dualed peer — NOT a caller]
  └─ embeds same 0/1/2/else map + TLS ring; does not call this leaf

FUN_0051f910  [short codes HUM/MUT/BIO — sibling, not owned]
LookupClassDisplayName_Inferred (0x0051f940)  [dualed class table]
```

## Race dictionary (sealed)

| Id | English | Notes |
|---:|---|---|
| 0 | Human | matches player race byte `@+0x532` usage in callers |
| 1 | Mutant | |
| 2 | Biomek | |
| * | Unknown | default; includes −1 if ever passed (callers skip when vfunc returns −1) |

## Open questions

- Product/PDB method English for this table helper.
- Whether short-code twin `0x0051f910` shares a product enum name prefix.
- Runtime golden matrix (not run; Terminal false).
