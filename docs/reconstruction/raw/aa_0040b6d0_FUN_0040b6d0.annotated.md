# Annotated low-level: FUN_0040b6d0 → StdVector_InsertOne_RebindIt_Via419880_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0040b6d0` |
| VA | `0x0040b6d0` |
| System | util / container (std::vector&lt;dword&gt;) |
| Date | 2026-08-04 (WQ7R-E dual seal) |
| Canonical | `StdVector_InsertOne_RebindIt_Via419880_Inferred` |

## Machine-level notes

- Source: raw + WQ7R-E `read_memory` + parent `FUN_00411180` (no `disassemble_bytes`).
- Decompiler `unaff_EDI` / `unaff_EBX` are real customcc registers, not noise.
- **Stride 4:** size/index via `>>2` / `*4`.
- **Count forced to 1:** `mov ecx, 1` before `FUN_00419880` (CountEcx insert-N engine).
- **Index save before realloc** is load-bearing: after insert-N, `begin` may move; rebind `*outIt = begin' + index*4`.
- Empty vector (`begin==0` or size==0) forces index 0.
- Parent `FUN_00411180` is push_back: fast path `*end++=*value` when room; slow path calls this with `where=end`.

## ABI (sealed)

| Slot | Role |
|---|---|
| EDI | vector* (`+4` begin, `+8` end, `+0xC` capEnd) |
| EBX | `T**` out-iterator |
| stack | `T* where`, `const T* value` |
| ret | `EAX=EBX`; **`ret 8`** |

## Pseudocode (annotated)

```c
// customcc: EDI=vec*, EBX=outIt**, stack(where*, value*), ret 8
void FUN_0040b6d0(int where /*pos*/, undefined4 value /*const T**/)
{
  int begin = *(int *)(EDI + 4);
  int index;
  if (begin == 0 || (*(int *)(EDI + 8) - begin >> 2) == 0)
    index = 0;
  else
    index = (where - begin) >> 2;

  // ECX=1; stack vec=EDI, where, value → StdVector_InsertN_Dword_CountEcx
  FUN_00419880(/*ecx*/1, /*stack*/ EDI, where, value);

  *EBX = *(int *)(EDI + 4) + index * 4;  // rebased iterator
  // EAX = EBX; ret 8
}
```

## Open questions

- Product / MSVC demangle (`vector::insert` / `_Insert_n` wrapper).
- Whether any non-push_back caller exists (xrefs list only `FUN_00411180` today).
- Runtime / bit-exact verification.
