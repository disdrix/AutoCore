# Annotated low-level: FUN_00786a00 / Profiler_EnterNamedZone

| Field | Value |
|---|---|
| Stable ID | `aa_00786a00` |
| VA | `0x00786a00` |
| System | client instrumentation (SearchAutoMissions path callee) |
| Date | 2026-07-29 (dual A/B seal; assembly correction) |
| Dual | `reviews/A_aa_00786a00_Profiler_EnterNamedZone.md` + `B_*` |

---

## Machine-level notes

- Source: raw capture for `aa_00786a00` + live Ghidra `disassemble_function` / call-site `get_assembly_context` (2026-07-29).
- **Prefer assembly** when decompiler conflicts — frozen raw omits parent-zone load into `ECX` for `FUN_00786470`.
- Global this at all call sites: `DAT_00afa27c` (profiler*).
- Sibling leave: `FUN_00786990` @ `0x00786990` (OWN separate).
- Strings: e.g. `@0x009cff4c` `"CVOGCharacter::SearchAutoMissions"`.

### Offsets (profiler object)

| Offset | Role |
|--------|------|
| `+0x4c` | Thread-slot vector begin (`T*` base) |
| `+0x50` | Vector end (used inside `FUN_007866e0`) |
| `+0x58` | `CRITICAL_SECTION` (inside EnsureThreadSlot) |
| `+0x175` | `u8` enable; **0 → enter returns 0** |

### Thread slot (stride `0xc`)

| Offset | Role |
|--------|------|
| `+0x0` | `DWORD` thread id (EnsureThreadSlot) |
| `+0x8` | **Current zone\*** (stack top); enter loads parent / stores new zone |

### ABI

```
EAX = Profiler_EnterNamedZone(ECX=profiler*, stack0=const char* name);
// RET 4
```

---

## Pseudocode (assembly-corrected; not identical to frozen raw)

```c
// __thiscall — ECX = profiler*, stack name cleaned by RET 4
void* __thiscall FUN_00786a00(void* profiler, const char* name)
{
  void* zone;
  int idx;
  void* parent;
  void* slots;

  if (*(char*)((char*)profiler + 0x175) == 0) {
    return 0;
  }

  idx = FUN_007866e0(profiler);                 // EnsureThreadSlot
  slots = *(void**)((char*)profiler + 0x4c);
  parent = *(void**)((char*)slots + idx * 0xc + 8);

  zone = FUN_00786470(parent, name);            // FindOrCreateChild — this = parent zone
  // FUN_00785e00: StartZoneTimer; entered with EAX = zone
  FUN_00785e00(/* zone in EAX */);

  idx = FUN_007866e0(profiler);
  slots = *(void**)((char*)profiler + 0x4c);
  *(void**)((char*)slots + idx * 0xc + 8) = zone;  // push

  return zone;
}
```

### Frozen raw decompiler (for contrast — residual errors)

```c
undefined4 __thiscall FUN_00786a00(int param_1,undefined4 param_2)
{
  undefined4 uVar1;
  int iVar2;
  
  if (*(char *)(param_1 + 0x175) == '\0') {
    return 0;
  }
  FUN_007866e0(param_1);          // residual: return discarded
  uVar1 = FUN_00786470(param_2);  // residual: this mis-bound to param_1
  FUN_00785e00();
  iVar2 = FUN_007866e0(param_1);
  *(undefined4 *)(*(int *)(param_1 + 0x4c) + iVar2 * 0xc + 8) = uVar1;
  return uVar1;
}
```

---

## Callers (xrefs)

| From | Context string |
|------|----------------|
| `CVOGCharacter_SearchAutoMissions` `0x00532b85` | `CVOGCharacter::SearchAutoMissions` |
| `FUN_00520dc0` `0x00520de8` | `CVOGCharacter::CleanupDraggingObject` |
| `FUN_0051b350` `0x0051b376` | `ServerCreateFromPacket` |
| `FUN_00727440` `0x00727634` | `UpdateSounds::updateSoundRegions()` |

## Callees

| VA | Role |
|----|------|
| `0x007866e0` | Ensure / index per-thread slot |
| `0x00786470` | Find or create named child zone under parent |
| `0x00785e00` | Start QPC timing on zone node |

## Open questions

- Retail default of enable byte `+0x175`.
- Full zone-node layout (partial via callees only).
- Leave dual and thread-slot `+0x4` field.
