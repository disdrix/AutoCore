# Annotated low-level: FUN_007a4480 → NDError_Log

| Field | Value |
|---|---|
| Stable ID | `aa_007a4480` |
| VA | `0x007a4480`–`0x007a44d0` |
| System | libND diagnostics |
| Date | 2026-07-29 (dual A/B; supersedes 2026-07-23 scaffold notes) |

## Machine-level notes

- `SUB ESP,0x800` / `ADD ESP,0x800` frame; body length 81 B; trailing `CC` pad.
- Gate: `CMP BYTE [enable], 1` then `TEST fmt` — **exact 1**, not truthy.
- Path push `0x00a97124` = `C:\vog\1_code\vog_14_117_branch\libND\NDError.cpp`.
- Line `0x65` (101), level `3`.
- `_vsnprintf` via IAT `FF 15 B4 64 9C 00` → `0x009c64b4`.
- `ADD ESP,0x20` after logger cleans 4+4 cdecl arg slots from both calls.
- Plain `RET` → `__cdecl` + caller-owned varargs.
- **Not** VOG_DEBUG_STOP identity; that string is call-site payload with enable often 0 → no-op.

## Pseudocode (annotated)

```c
// NDError_Log / FUN_007a4480
void FUN_007a4480(char enable /*param_1*/, char *fmt /*param_2*/, ...)
{
  char local_800[2048];

  if ((enable == '\x01') && (fmt != (char *)0x0)) {
    _vsnprintf(local_800, 0x800, fmt, /* va: stack after fmt */);
    vog_LogMessage(
        "C:\\vog\\1_code\\vog_14_117_branch\\libND\\NDError.cpp",
        0x65,   /* line 101 */
        3,      /* level */
        local_800);
  }
  return; /* void */
}
```

## Open questions

- Product label for log level `3`.
- Which call sites pass `enable == 1` in retail (parent residual; ~1921 xrefs).
- Live capture of `vog_LogMessage` sink behavior.
