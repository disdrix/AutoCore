# Annotated low-level: StdVector_InsertN_Elem0x10_Inferred (`FUN_00408640`)

| Field | Value |
|---|---|
| Stable ID | `aa_00408640` |
| VA | `0x00408640`–`0x0040887c` exclusive (**572 B** / `0x23C`) |
| System | util / container (generic) |
| Date | 2026-08-04 WQ9H-H dual seal |
| Ghidra name | `FUN_00408640` |
| Canonical | `StdVector_InsertN_Elem0x10_Inferred` |

## Machine-level notes

- MSVC-style `std::vector<T>::_Insert_n` for **16-byte** elements (4 dwords).
- **ABI (custom / fastcall hybrid + frame):**
  - **ECX** = `count` (entry `MOV ESI,ECX`)
  - **EDX** = `vector*` (entry `MOV EBX,EDX`; triad at `+4/+8/+0xC`)
  - stack `where` (insert position, absolute `T*`)
  - stack `const T* value` (template; copied as 4 dwords to locals)
  - **`RET 8`** cleans two stack args
- **Stride 0x10:** capacity/size via `(ptr-begin)>>4`; byte sizes `<<4` / `*0x10`.
- **Growth:** if `size+count > capacity`: newCap = min-fit after 1.5× (`cap+(cap>>1)`), clamp overflow to 0 then exact; `operator_new(newCap*0x10)`; copy prefix → construct-N value → copy suffix → `operator_delete` old; write triad.
- **In-place:** two sub-paths by distance-to-end vs count (shift via `FUN_0040a670` / fill helpers), then `FUN_00465bc0`.
- **Max size:** `0x0FFFFFFF` elements (same family as other vector InsertN).
- **Overflow:** `FUN_00418130` length_error / abort helper.
- **Sole caller:** `FUN_00930360` inserts **count=1** (push_back-style via InsertN) with 0x10 stride add nearby.
- Scaffold `Named_CalleeOf_…Input_Poll…` is call-chain noise — role is generic container insert.
- Decompiler warns `operator_delete` does not return on realloc path — **incorrect**; triad update follows.

## Control-flow spine

```text
value_local = *value  (4 dwords)
cap = begin ? (capEnd-begin)>>4 : 0
if count==0: return
size = begin ? (end-begin)>>4 : 0
if size > max - count: FUN_00418130()   // noreturn-ish throw
if cap < size+count:
  newCap = 1.5× or exact; bytes = newCap<<4
  newbuf = operator_new(bytes)
  copy [begin, where) via FUN_00409b60 → FUN_0040a670
  construct-N value via FUN_00407000 (&value_local)  // uses ESI/EDI custom
  copy [where, end)
  delete old begin; triad = newbuf / end' / capEnd'
  return
// capacity enough:
if (end-where)>>4 < count:  // insert near end
  shift/fill hole; end += count*0x10
else:
  shift from end; fill hole
FUN_00465bc0()  // residual fill/assign helper
return (ret 8)
```

## Helpers (undualed residual)

| VA | Role (inferred) |
|---|---|
| `FUN_00409b60` | thin range-copy wrapper → `FUN_0040a670` |
| `FUN_00407000` | construct-N / fill_n for 0x10 elems (`FUN_00409e20`; returns `esi*0x10+edi`) |
| `FUN_0040a670` | uninitialized copy / move range |
| `FUN_0045f050` | in-place fill / assign hole |
| `FUN_00465bc0` | post-insert fill remainder |
| `FUN_00418130` | size overflow throw (shared) |

## Open questions

- Product POD English type for 16-byte element.
- Full dual seal of relocate/fill callees.
- Runtime / bit-exact / differential.
