# Annotated low-level: FUN_0076c4d0 / RdtscTimer_StampAndArm_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0076c4d0` |
| VA | `0x0076c4d0`–`0x0076c4f1` exclusive (**33 B**) |
| System | timer / HostBase nested stamp; UI dtor chain leaf |
| Date | 2026-08-05 (WQ9L-D dual; scaffold 2026-07-23) |
| Named | `RdtscTimer_StampAndArm_Inferred` |

## Machine-level notes

- **ABI:** ECX = timer subobject*. Bare `C3`. Void. No stack formals.
- **Body hex sealed** via `read_memory` (see raw WQ9L-D append).
- **RDTSC** intrinsic: EAX=low, EDX=high → stored at `+8` / `+0xC`.
- **Arm flag:** byte `+0x18 = 1` (paired with `FUN_0076c500` which zeros it during init).
- Prefer bytes if decompiler ever rewrites RDTSC spill; current decompile ≡ hex CF.
- High fan-in (~90 xrefs): HostBase ctor, CWndVehicle complete/derived teardown, drive/UI sites.

## Field map (timer subobject)

| Offset | Write | Meaning |
|--------|-------|---------|
| `+0x08` | RDTSC EAX | stamp low dword |
| `+0x0C` | RDTSC EDX | stamp high dword |
| `+0x18` | `1` | armed / stamp-valid flag |

Related init (`FUN_0076c500`, not owned): zeros `+0..+0x18`, fills period/div at `+0x20/+0x24` and double at `+0x28`, then calls this stamp.

## Pseudocode (annotated)

```c
// ECX = timer*
void __fastcall RdtscTimer_StampAndArm_Inferred(void *timer /*ECX*/)
{
  // machine: SUB ESP,8; RDTSC; spill EAX/EDX; restore to regs
  uint64_t stamp = rdtsc();
  *(uint32_t *)((char *)timer + 0x08) = (uint32_t)stamp;
  *(uint32_t *)((char *)timer + 0x0C) = (uint32_t)(stamp >> 32);
  *((uint8_t *)timer + 0x18) = 1;
  return; // C3
}
```

## Call chain context (evidence only)

```
HostBase_DefaultCtor (0x007560d0)
  FUN_0076c500(1000) on this+0x10
  +0x40/+0x44 = 1000
  └─ FUN_0076c4d0(this+0x10)   [OWN WQ9L-D]

CWndVehicle_CompleteDtor / DerivedTeardown_EDI
  nested = this+0x2A8
  nested+0x48 = 1; (nested+0x44) = (nested+0x40)
  └─ FUN_0076c4d0(nested+0x10) [OWN WQ9L-D]
```

## Open / residual

- Product class English for the timer subobject (no RTTI on this leaf).
- Exact product meaning of `+0x18` beyond “armed after stamp” (shape sealed).
- Runtime / bit-exact / differential.
