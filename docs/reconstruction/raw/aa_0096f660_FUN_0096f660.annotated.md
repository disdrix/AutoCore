# Annotated low-level: FUN_0096f660 / Material_ReleaseAndNullField0x18_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0096f660` |
| VA | `0x0096f660`–`0x0096f681` (**34 B**) |
| System | gfx / material resource |
| Date | 2026-07-29 (W26-M; prior scaffold 2026-07-23) |
| Canonical | `Material_ReleaseAndNullField0x18_Inferred` (Ghidra `FUN_0096f660`) |

## Machine-level notes

- Source: raw + live `decompile_function` + full-body `read_memory` (34 B exact).
- Classification: **near-leaf** — no direct FUN_* callees; one **indirect** vtbl call.
- Callers only from material color apply paths after `FUN_009700f0("MatAmbient"|"MatDiffuse"|"MatEmissive", ...)`.

## Sealed ABI

| Slot | Role | Conf |
|---|---|---|
| **ECX** | holder `T**` / `int*` — first dword is object pointer | **High** |
| Stack formals | none | **High** |
| Return | void; plain `RET` | **High** |
| Convention | `__fastcall` (ECX only) | **High** |

## Control flow (byte-faithful)

```c
// ECX = holder (pointer to object pointer / wrapper whose *holder is the material/gfx object)
void Material_ReleaseAndNullField0x18_Inferred(int* holder /*ECX*/)
{
  int* obj = (int*)*holder;          // MOV ESI,[ECX]
  if (obj == NULL)
    return;

  int* resource = *(int**)((char*)obj + 0x18);  // [ESI+0x18]
  if (resource != NULL) {
    resource[1] -= 1;                // ADD dword [ECX+4], -1  (refcount at +4)
    if (resource[1] == 0) {
      // EAX = *resource (vtbl); CALL [EAX+8]
      ((void (**)(void)) *resource)[2]();   // vtbl slot +8 = index 2
    }
  }
  *(uint32_t*)((char*)obj + 0x18) = 0;  // always null the slot
}
```

## Byte map

| Off | Bytes | Meaning |
|---|---|---|
| +0 | `56` | PUSH ESI |
| +1 | `8B 31` | ESI = *ECX |
| +3 | `85 F6` / `74 19` | test / jz ret |
| +7 | `8B 4E 18` | ECX = [ESI+0x18] |
| +A | `85 C9` / `74 0B` | null resource → skip release |
| +E | `83 41 04 FF` | refcount-- at resource+4 |
| +12 | `75 05` | jnz skip dtor |
| +14 | `8B 01` / `FF 50 08` | call vtbl[+8] |
| +19 | `C7 46 18 00 00 00 00` | [ESI+0x18] = 0 |
| +20 | `5E C3` | POP ESI; RET |

## Caller contract

| Caller | Role | When |
|---|---|---|
| `FUN_005dbca0` | store paint RGB×(1/255) into MatDiffuse/Emissive/Ambient | if `this+0x48` material non-null |
| `FUN_0051aed0` | pickup special FX paint path (`def+0x3F4==8`) | after same three Mat* sets |

ECX at call sites is the material/holder used for the preceding `FUN_009700f0` thiscalls (Ghidra often elides ECX in the parent decompile).

## Open questions

- Product type name of the object at `*holder` and the resource at `+0x18` (COM-like vs internal refcount).
- Whether vtbl[+8] is `Release`, `Destroy`, or a material-specific free.
- Whether `FUN_009700f0` itself allocates the `+0x18` slot this clears — sibling unit, not OWN.
