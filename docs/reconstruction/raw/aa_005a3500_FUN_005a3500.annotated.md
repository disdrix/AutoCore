# Annotated low-level: FUN_005a3500 → StdTree_InsertAndRebalance_Val16_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005a3500` |
| VA | `0x005a3500`–`0x005a36ea` inclusive (**491 B** / `0x1EB`) |
| System | skills-abilities / MSVC std::_Tree always-insert + RB rebalance (Val16 / isnil@+0x21) |
| Date | 2026-08-05 (R12-031 OWN dual; scaffold 2026-07-23) |
| Canonical name | `StdTree_InsertAndRebalance_Val16_Isnil21_Inferred` |
| Parent dual | `0x005a27f0` `StdTree_Rrotate_Isnil21_Inferred` |
| Twin | `0x004e37e0` (R12-012; same CF/size/buynode; different SEH + caller) |

## Machine-level notes

- **thiscall** ECX = map shell (`+4` head, `+8` size); stack args out / addLeft / where / value; **`ret 0x10`**.
- Size gate: `cmp [edi+8], 0x0FFFFFFE` → throw `"map/set<T> too long"` @ `0x00a1530c`.
- Buynode residual `FUN_004e2b80`: `operator_new(0x28)`; copies 4 dwords Val16 @ +0x10; color @ +0x20 from arg (0=red); isnil @ +0x21 = 0.
- Link: first-node (where==head) / right+rightmost / left+leftmost.
- RB while parent color red (`+0x20 == 0`): uncle recolor, or Lrotate `FUN_0050e9f0` / Rrotate `FUN_005a27f0` (parent dual); one Lrotate path **inlined** with isnil test `80 7a 21 00`.
- Exit: root black; `*out = new node`.
- **Always-insert** — no key compare. Parent `FUN_005a3be0` (InsertOrFind-style; sole caller, 2 sites) owns lower-bound / uniqueness.
- Do **not** merge with peer `00407200` (buynode `00408990`) or isnil29/2D/31 families.
- Prefer assembly / `read_memory` when decompiler conflicts; this wave sealed via live decompile ≡ raw CF + body bytes.

## Pseudocode (annotated)

```c
// __thiscall map; RET 0x10
void __thiscall StdTree_InsertAndRebalance_Val16_Isnil21_Inferred(
    MapShell* map,          // ECX  +4 head, +8 size
    Node** out,             // stack+4
    char addLeft,           // stack+8  1=left, 0=right
    Node* where,            // stack+C  attach parent (or head)
    const void* value)      // stack+10 Val16 source (4 dwords)
{
  // SEH LAB_009a6342
  if (map->size >= 0x0FFFFFFE) {
    // basic_string("map/set<T> too long") + length_error + _CxxThrowException(DAT_00acc388)
    // noreturn
  }

  Node* head = map->head;
  Node* n = FUN_004e2b80(head, where, head, value, /*color red*/ 0);
  map->size += 1;

  if (where == head) {
    head->parent = n; head->left = n; head->right = n;
  } else if (addLeft == 0) {
    where->right = n;
    if (where == head->right) head->right = n;
  } else {
    where->left = n;
    if (where == head->left) head->left = n;
  }

  // while parent red: recolor / L(0050e9f0) / R(005a27f0) / inlined L
  // color@+0x20; isnil@+0x21
  // root->color = black; *out = n; ret 0x10
}
```

## Call graph (this unit)

```text
FUN_005a3be0  (InsertOrFind-style residual; sole caller, 2 sites)
  └─ FUN_005a3500  StdTree_InsertAndRebalance_Val16_Isnil21_Inferred  [OWN R12-031]
       ├─ FUN_004e2b80   buynode Val16 / node 0x28 / isnil21  [residual]
       ├─ FUN_0050e9f0   StdTree_Lrotate_Isnil21_Inferred     [dualed]
       └─ FUN_005a27f0   StdTree_Rrotate_Isnil21_Inferred     [parent dual R11-007]

Twin clone: 0x004e37e0 (R12-012) — same body size 0x1EB / same buynode; caller FUN_004e48b0
Peer family: 0x00407200 StdTree_InsertAndRebalance_Isnil21_Inferred (buynode 00408990)
```

## Open questions

- Product/PDB demangle for map value_type under `FUN_005a3be0` (pair key at node+0x10/+0x14 observed at parent).
- Residual dual of buynode `004e2b80` and parent shell `005a3be0`.
- Runtime / bit-exact / differential.
