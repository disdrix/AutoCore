# Raw capture: FUN_00567860

| Field | Value |
|---|---|
| **Stable ID** | `aa_00567860` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00567860` |
| **Canonical name** | `FUN_00567860` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00567860(int param_1,undefined4 *param_2,int *param_3)

{
  bool bVar1;
  undefined4 *puVar2;
  undefined4 *puVar3;
  
  puVar3 = *(undefined4 **)(param_1 + 4);
  bVar1 = true;
  if (*(char *)((int)puVar3[1] + 0x19) == '\0') {
    puVar2 = (undefined4 *)puVar3[1];
    do {
      puVar3 = puVar2;
      bVar1 = *param_3 < (int)puVar3[3];
      if (bVar1) {
        puVar2 = (undefined4 *)*puVar3;
      }
      else {
        puVar2 = (undefined4 *)puVar3[2];
      }
    } while (*(char *)((int)puVar2 + 0x19) == '\0');
  }
  puVar3 = (undefined4 *)FUN_00567450(&param_3,bVar1,puVar3,param_3);
  *param_2 = *puVar3;
  *(undefined1 *)(param_2 + 1) = 1;
  return;
}
```

---

## W29-M re-verify (2026-07-29) — append only

**Tools:** `decompile_function` / `batch_decompile`, `analyze_function_complete`, `get_function_by_address`, `read_memory`. **No** `disassemble_bytes`.

### Live decompile (2026-07-29)

```c
void __thiscall FUN_00567860(int param_1,undefined4 *param_2,int *param_3)
{
  bool bVar1;
  undefined4 *puVar2;
  undefined4 *puVar3;
  
  puVar3 = *(undefined4 **)(param_1 + 4);
  bVar1 = true;
  if (*(char *)((int)puVar3[1] + 0x19) == '\0') {
    puVar2 = (undefined4 *)puVar3[1];
    do {
      puVar3 = puVar2;
      bVar1 = *param_3 < (int)puVar3[3];
      if (bVar1) {
        puVar2 = (undefined4 *)*puVar3;
      }
      else {
        puVar2 = (undefined4 *)puVar3[2];
      }
    } while (*(char *)((int)puVar2 + 0x19) == '\0');
  }
  puVar3 = (undefined4 *)FUN_00567450(&param_3,bVar1,puVar3,param_3);
  *param_2 = *puVar3;
  *(undefined1 *)(param_2 + 1) = 1;
  return;
}
```

Live ≡ scaffold raw body (bit-identical CF).

### Byte-sealed facts (`read_memory` @ `0x00567860`, 128 B)

Epilogue region ends with **`C2 08 00`** (`ret 8`) then `CC` pad. Body range Ghidra: `0x00567860`–`0x005678c0` exclusive (**96** B / `0x60`).

Key points from hex + decompile:

| Field | Value |
|---|---|
| ABI | `__thiscall` ECX=map; stack `(outPair*, value*)`; **RET 8** |
| Map shell | head @ `+4` |
| Node | key @ `+0x0c` (dword3); isnil @ `+0x19`; left `*n`, right `n[2]` |
| Key compare | **signed** int: `*value < (int)node.key`; equal goes **right** |
| Insert | **always** via `FUN_00567450` (Map_TreeInsertAndRebalance_Val12, W27-R) |
| Out pair | `it` @ +0; **`inserted=1` always** @ +4 (byte) |
| Uniqueness | **not** enforced here — **not** insert-or-find (contrast `0053a1a0` / `00573810`) |
| Callers / xrefs | `FUN_00565950` @ `0x0056641a`, `0x00566452` (two sites) |
| Canonical (W29-M) | `StdMap_InsertAlways_Val12_IntKey` |
