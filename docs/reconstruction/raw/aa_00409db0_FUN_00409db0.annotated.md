# Annotated low-level: StdVector_ConstructN_Elem12_Inferred (`FUN_00409db0`)

| Field | Value |
|---|---|
| Stable ID | `aa_00409db0` |
| VA | `0x00409db0`–`0x00409e12` (**99 B** / `0x63`) |
| System | util / container (`std::vector` construct-N, POD **elem 0xC**) |
| Date | 2026-07-23 scaffold; **2026-08-05 WQ9I-A dual seal** |

## Machine-level notes

- Prefer assembly when decompiler conflicts: **ECX = value***, stack dest+count; dest advances **`+0xC`** each iter.
- PodCopy `FUN_00409f50`: **EAX=dst**, **ECX=src**, 3 dwords; null-dst no-op.
- Plain **`ret` (`C3`)** — caller cleans 8 B.
- Twin ConstructN elem **0x28** at `0x00409d40` (PodCopy `00409f30`, +0x28 / 10 dwords).

## Pseudocode (annotated)

```c
// ABI: ECX = const T* value; stack T* dest, int count; plain ret
void StdVector_ConstructN_Elem12_Inferred(
    /*ECX*/ const Pod12 *value, Pod12 *dest, int count)
{
  // SEH LAB_009bc620
  for (; count != 0; --count) {
    FUN_00409f50(/*EAX*/ dest, /*ECX*/ value); // 3 dwords
    dest = (Pod12 *)((char *)dest + 0x0C);
  }
}
```

## Call graph

```text
FUN_00406ee0 Ufill elem12 (WQ9G-I sealed)
  └─ FUN_00409db0(dest, count) w/ ECX=value*   [OWN WQ9I-A]
       └─ FUN_00409f50 PodCopy 3 dwords        [residual leaf]
FUN_004062a0 push_back fast (WQ9H-C sealed)
  └─ FUN_00409db0(end, 1) then end += 0xC
```

## Open residual

- Product English for 12-byte `T`.
- Full dual of PodCopy `00409f50` (CF sealed here).
- Runtime / bit-exact / differential.
