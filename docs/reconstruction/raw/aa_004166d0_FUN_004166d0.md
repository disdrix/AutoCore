# Raw capture: FUN_004166d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004166d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004166d0` |
| **Canonical name** | `FUN_004166d0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_004166d0(void *param_1,byte param_2)

{
  FUN_0082d540();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```

---

## MEGA-138 re-verify append (2026-08-05)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-138** |
| **Tools** | Ghidra HTTP `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_by_address` + `get_function_callers` / callees / xrefs + `read_memory` (body + vtbl + COL + type_info). **No** `disassemble_bytes`. |
| **Live decompile** | ≡ scaffold 2026-07-23 (complete + flags&1 delete + return this) |
| **Body** | `0x004166d0`–`0x004166ed` inclusive (**30 B** / `0x1E`); pad `CC` |
| **ABI** | **ECX=`this`**; stack **`flags`**; **`RET 4`** (`C2 04 00`); returns **this** in EAX |
| **Callees** | `FUN_0082d540` / `CVOGMenu_CompleteDtor` (dualed WQ9K-G); `operator_delete` @ `0x00489822` |
| **Callers (code)** | none — DATA only |
| **Xrefs** | DATA `PTR_FUN_00a72f6c[0]` @ `0x00a72f6c` = `0x004166d0` |
| **RTTI** | COL dword @ `0x00a72f68` → `0x00ab6214` → type_info `0x00afe39c` → **`.?AVCVOGMenu@@`** → **CVOGMenu** |
| **Name (product)** | `CVOGMenu_ScalarDeletingDtor` (Ghidra `FUN_004166d0`) |
| **Classification** | worker (MSVC scalar-deleting / vtbl[0]) |
| **System** | skills-abilities (partition); UI menu intermediate over CNDUIDialog |
| **Decompile ≡ bytes** | Yes for ESI=this / CALL complete / TEST bit0 / delete+ADD ESP,4 / MOV EAX,ESI / RET 4. Decompiler omits `ADD ESP,4` after delete (noreturn false-positive); bytes seal epilogue. |

### Full-body hex (`read_memory`, 30 B)

```
568bf1e8686e4100f644240801740956e83d31070083c4048bc65ec20400
```

### Byte-sealed facts

1. `ESI = this`; `CALL FUN_0082d540` (CVOGMenu complete @ `0x0082d540`).
2. `TEST byte [esp+8], 1` / `JZ` skip — flags bit0 only (scalar, **not** vector-deleting).
3. `operator_delete(this)` cdecl + `ADD ESP,4` when bit0 set (decompiler “does not return” is **false**).
4. `MOV EAX,ESI; POP ESI; RET 4` — always returns this; cleans flags.
5. Pad `CC` at `0x004166ee`.

### Vtbl `PTR_FUN_00a72f6c` @ `0x00a72f6c` (`read_memory`, first slots)

| Slot | Value | Role |
|---|---|---|
| [0] | `0x004166d0` | this scalar-deleting dtor |
| [1] | `0x00790cc0` | shared family (open) |
| [2] | `0x007a7de0` | shared family (open) |
| [3] | `0x007567e0` | shared family (open) |

### RTTI chain (`read_memory`)

| Item | Address / value |
|---|---|
| COL ptr dword @ `0x00a72f68` | `0x00ab6214` |
| type_info @ COL+0xC | `0x00afe39c` |
| name | `.?AVCVOGMenu@@` → **CVOGMenu** |

Integrity: original scaffold body retained above; this section appends only.
