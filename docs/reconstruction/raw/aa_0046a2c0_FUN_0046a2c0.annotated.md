# Annotated low-level: StdUninitCopy_BasicString (`FUN_0046a2c0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0046a2c0` |
| VA | `0x0046a2c0`–`0x0046a34c` exclusive (**140 B**) |
| System | MSVC `vector` / STL uninit helpers — `basic_string` stride `0x1c` |
| Date | 2026-07-29 (W33-G OWN-ONLY dual) |
| Canonical name | `StdUninitCopy_BasicString` |

## Machine-level notes

- Source: raw capture + W33-G re-verify (`decompile` + `read_memory` + xrefs). **No** `disassemble_bytes`.
- **cdecl** three pointers; bare `C3`. Return dest-end in EAX.
- Elem stride **0x1C** sealed by `ADD ESI/EDI, 0x1C` and caller `/0x1c` math (insert-n, assign).
- Happy path: loop copy-ctor via IAT `[0x009C62EC]`; success `JZ` → epilogue @ `0x0046A339`.
- Catch residual (EH only): destroy partial range via IAT dtor `[0x009C62F4]`; `_CxxThrowException(0,0)` @ `0x006A3D60`.
- Prefer bytes when decompiler omits catch block (correct for happy-path CF).

## Pseudocode (annotated)

```c
// cdecl: (first, last, dest) -> dest_end; bare RET
// optional trailing allocator args ignored by body
BasicString_0x1c* StdUninitCopy_BasicString(
    BasicString_0x1c* first,
    BasicString_0x1c* last,
    BasicString_0x1c* dest)
{
  // SEH LAB_009bcce1; state @ [ebp-4]
  BasicString_0x1c* dest_start = dest;
  for (; first != last; first = (BasicString_0x1c*)((char*)first + 0x1c)) {
    // state = 1
    if (dest != nullptr) {
      // thiscall IAT copy-ctor(dest, first)  @ 0x006EAD80
      basic_string_copy_ctor(dest, first);
    }
    dest = (BasicString_0x1c*)((char*)dest + 0x1c);
    // state = 0
  }
  // success: jump over catch to epilogue; EAX = dest
  return dest;

  // --- EH catch only (0x0046A318) ---
  // destroy [dest_start, dest_cur) via IAT dtor @ 0x006EAD30
  // _CxxThrowException(nullptr, nullptr);
}
```

## Call graph

| Direction | Targets |
|---|---|
| Callers | `StdVector_InsertN_BasicString` (`FUN_004306b0`) ×2 grow relocate; `FUN_00431430` wrapper; `FUN_00469d10` range assign |
| Callees | `basic_string` copy-ctor IAT; dtor IAT + `_CxxThrowException` (catch) |

## Open questions

- Exact MSVC demangle (`std::_Uninit_copy<...>`) / CRT version plate.
- Whether null-`dest` skip is debug-iterator residue only.
- Product English for `FUN_00431430` / `FUN_00469d10` hosts (not owned).
