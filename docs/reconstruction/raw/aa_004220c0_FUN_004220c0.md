# Raw capture: FUN_004220c0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004220c0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004220c0` |
| **Canonical name** | `FUN_004220c0` |
| **Named dual** | `SkillRow_UninitializedFillN_0x934` |
| **System** | skills-abilities / vector helpers (stride 0x934 skill-character row) |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_004220c0(int param_1)

{
  undefined4 *in_EAX;
  int iVar1;
  undefined4 *unaff_EBX;
  undefined4 *puVar2;
  undefined4 *puVar3;
  
  for (; param_1 != 0; param_1 = param_1 + -1) {
    if (in_EAX != (undefined4 *)0x0) {
      puVar2 = unaff_EBX;
      puVar3 = in_EAX;
      for (iVar1 = 0x24d; iVar1 != 0; iVar1 = iVar1 + -1) {
        *puVar3 = *puVar2;
        puVar2 = puVar2 + 1;
        puVar3 = puVar3 + 1;
      }
    }
    in_EAX = in_EAX + 0x24d;
  }
  return;
}
```

---

## W22-M re-verify (2026-07-29) — live Ghidra decompile + read_memory

| Field | Value |
|---|---|
| **Agent** | W22-M OWN-ONLY |
| **Tools** | `decompile_function` / `batch_decompile` + `read_memory` + callers/callees/xrefs |
| **No** | `disassemble_bytes`; Launcher; parent ledger edits |
| **Body range** | `0x004220c0`–`0x004220e3` (**36 bytes**; bare `ret`) |
| **Live decompile** | ≡ prior raw (leaf fill-N) |
| **Classification** | leaf |

### Full body bytes (`read_memory` 36 B @ `0x004220c0`)

```
85 c9                test  ecx, ecx
76 1f                jbe   ret                 ; count==0 → return
56                   push  esi
8b d1                mov   edx, ecx            ; remaining = count
57                   push  edi
loop:
85 c0                test  eax, eax            ; dst
74 0b                jz    skip_copy
b9 4d 02 00 00       mov   ecx, 0x24d          ; 589 dwords
8b f3                mov   esi, ebx            ; src prototype
8b f8                mov   edi, eax            ; dst
f3 a5                rep   movsd               ; copy 0x934 bytes
skip_copy:
05 34 09 00 00       add   eax, 0x934          ; next element
83 ea 01             sub   edx, 1
75 e7                jnz   loop
5f                   pop   edi
5e                   pop   esi
c3                   ret
```

### ABI (byte + call-site sealed)

| Register | Role |
|---|---|
| **ECX** | element **count** (`__fastcall` count) |
| **EAX** | destination start pointer (advanced by `0x934` each iter) |
| **EBX** | **source prototype** (fixed; re-copied each slot) |
| stack | none (bare `ret`) |

Constants: dword count **`0x24d`** (=589); byte stride **`0x934`** (=2356 = `0x24d*4`).

### Callers (3 xrefs)

| Caller | Site | Notes |
|---|---|---|
| `FUN_0041aef0` / `SkillRowVec_EmplaceBack_Stride934_Inferred` | `0x0041af46` | `mov ecx,1` / `eax=end` / EBX=prototype from parent |
| `FUN_0041c9a0` | `0x0041c9a9` | fill-N then `return base + n*0x934` |
| `FUN_0041f740` | `0x0041f884` | grow path; EBX = local `0x94c`-sized prototype buffer |

### Leaf

No callees. No SEH. POD dword bulk-copy only (no ctors).
