# Annotated low-level: Object_SetMode278_SelectFromTFID228_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004c3c80` |
| VA | `0x004c3c80`–`0x004c3d17` (151 B) |
| Ghidra | `FUN_004c3c80` |
| Canonical | `Object_SetMode278_SelectFromTFID228_Inferred` |
| System | object / AI owner mode / selected-target apply |
| Date | 2026-07-29 (W28-L) |

## Machine-level notes

- **ABI:** `__thiscall`; ECX = object; stack `char mode`; epilogue `POP EDI; POP ESI; ADD ESP,0x10; RET 4`.
- **Always:** `*(char*)(this+0x278) = mode`.
- **Gate:** only `mode == 1` enters resolve/select path; any other mode is store-only.
- **TFID source:** 16 bytes at `this+0x228` (coid lo/hi + words) → stack `TFID_16`.
- **Resolve:** manager ECX rebuilt via `*(this+4)` → `+0xA8` → `+0xE4E8` (surface decompile omits); CALL `Object_ResolveFromTFID` @ `0x004bb950`.
- **Candidate:** `resolved+0xA0` (selected-target / linked entity slot — same offset sealed by `Object_SetSelectedTarget`).
- **Gate virtual:** MI-adjusted self `vtbl+0x298(candidate)` must return non-zero (enemy/valid-target family used elsewhere).
- **Apply:** `Object_SetSelectedTarget(mi_self, candidate)` @ `0x005172d0` — one stack formal in bytes.
- **Callers:** AI clear paths pass `0`; skill spawn path passes `1`; resolve-fail helper clears with `0`.

## Pseudocode (bytes-faithful)

```c
// thiscall; ret 4
void Object_SetMode278_SelectFromTFID228_Inferred(void *self, char mode)
{
  *(char *)((char *)self + 0x278) = mode;
  if (mode != 1)
    return;

  TFID_16 local;
  memcpy(&local, (char *)self + 0x228, 16);
  /* ECX = manager from host chain (+0xA8 / +0xE4E8) — surface residual */
  void *resolved = Object_ResolveFromTFID(&local);
  if (resolved == 0)
    return;
  void *candidate = *(void **)((char *)resolved + 0xa0);
  if (candidate == 0)
    return;

  void *mi = (char *)self
           + *(int *)(*(int *)((char *)self + 4) + 4)
           + 4;
  char ok = (*(char (__thiscall **)(void *, void *))(*(int *)mi + 0x298))(mi, candidate);
  if (ok == 0)
    return;
  Object_SetSelectedTarget(mi, candidate);
}
```

## Open questions

- Product English for mode values at `+0x278` (AI docs also use 2=dead / 3=respawn — multi-use residual).
- Product name of TFID block at `+0x228` (assigned-target TFID vs other identity).
- Product English for `vtbl+0x298` predicate.
- Whether `resolved+0xA0` is always “selected target” vs type-specific linked entity.
- Runtime / bit-exact — open.
