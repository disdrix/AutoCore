# Annotated: FUN_00971280 → AssPreloader_TryPreloadChildAssetsForAbsentKeys_Inferred

| Field | Value |
|---|---|
| **Stable ID** | `aa_00971280` |
| **VA** | `0x00971280`–`0x00971472` (**498 B**) |
| **Canonical name** | `AssPreloader_TryPreloadChildAssetsForAbsentKeys_Inferred` |
| **Ghidra name** | `FUN_00971280` |
| **System** | assPreloader / asset preload pipeline |
| **Dual** | W32-J 2026-07-29 |

---

## Signature (byte-sealed)

```c
// stack: (AssPreloader* host, KeyVec* keys); RET 8; void
// KeyVec: +0x04 begin*, +0x08 end* (dword stride; not GuardedVector)
void AssPreloader_TryPreloadChildAssetsForAbsentKeys_Inferred(
    void* host /*stack*/,
    void* key_vec /*stack*/);
```

---

## Control flow (annotated)

```
SEH frame LAB_009afdde
cur = key_vec->begin   // *(+4)
end = key_vec->end     // *(+8)
while cur != end:
  FUN_00970fc0()                  // host in EAX (pre-loop drain/process; not owned)
  out_it = lower_bound(host+0xDC, *cur)   // FUN_0044a860; EBX=key*
  if *out_it == *(host+0xE0):     // == tree header/end → key ABSENT from +0xDC map
    zero local GuardedVector ring
    FUN_0043c5f0: ECX=*(host+0x110)+0x28, stack=&local
      // snapshot child list from resource cache object into local ring
    for each child object slot in local:
      result = (*child->vtbl[0x0C/4])(key*)   // try preload child for this parent key
      if result != 0:
        pair = { *key, result }
        if *(host+0x78): EnterCS(host+0x60)
        GuardedVector_PushBack_Stride2_U32x2(host+0x4C, &pair)  // FUN_0043e970; EAX=host+0x4C
        if *(host+0x78): LeaveCS
        *find_or_insert(host+0xDC, key) = 1   // FUN_0043d700; EDI=tree, ECX=key*
        goto join
    // all children failed:
    name = FUN_0096a630(key) → "assId: [%s]" string
    log assPreloader.cpp:0x241 "PRELOAD: unable to preload child asset %s"
    if *(host+0xA8): EnterCS(host+0x90)
    GuardedVector_PushBack_Thiscall(host+0x7C, key*)  // FUN_0043d670
    if *(host+0xA8): LeaveCS
  join:
    Sleep(0)
    GuardedVector_DestroyPages(local)  // FUN_0040d9c0; ESI=local
  cur++
return  // RET 8
```

---

## Host offsets used

| Off | Role |
|-----|------|
| `+0x4C` | stride-2 result ring (U32 key + U32 child result); CS `+0x60` / flag `+0x78` |
| `+0x7C` | fail/tracked key ring; CS `+0x90` / flag `+0xA8` |
| `+0xDC` | membership tree (lower_bound / find-or-insert); header/end at `+0xE0` |
| `+0x110` | resource cache / manager; child source at `*(+0x110)+0x28` |

---

## Notes

- Decompiler shows `FUN_0044a860()` / `FUN_0043d700()` / `FUN_0043e970()` without this/EDI formals — **bytes win**.
- Gate `*it == *(host+0xE0)` is **absent-from-tree** (end sentinel), not “key equals type id”.
- Classification: **worker**.
