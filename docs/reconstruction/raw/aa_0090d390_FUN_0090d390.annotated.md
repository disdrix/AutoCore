# Annotated low-level: FUN_0090d390 / Client_Input_FindFirstActiveUiWindowTable5_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0090d390` |
| VA | `0x0090d390`–`0x0090d3c4` inclusive (**53 B** / `0x35`) |
| System | `input-drive-control` |
| Date | 2026-08-05 (MEGA-096 dual seal; scaffold 2026-07-23) |
| Canonical name | `Client_Input_FindFirstActiveUiWindowTable5_Inferred` |
| Ghidra name | `FUN_0090d390` |

## Machine-level notes

- **Authority:** live `disassemble_function` + `read_memory` (full 53 B) + call-site `get_assembly_context`. Decompile CF matches raw scaffold.
- **ABI:** no stack args; plain **`RET`** (`C3` ×2). ESI = slot index. EAX = first active window* or 0.
- **Table:** global `DAT_00d09a38[5]` (exclusive end `0x00d09a4c`). Each slot holds a UI window object pointer (or null).
- **Active gate:** thiscall `vtbl+0x3d8` → `char`/`bool`. Same virtual used by parent ESC gate on the client object and by dismiss peer `FUN_0090d400`.
- **Not a callee leaf for named FUNs:** only indirect vcall; `analyze_function_complete` correctly lists callees `[]`.
- Prefer assembly when decompiler conflicts (none observed here).

## Pseudocode (annotated ≡ raw CF)

```c
// Returns first non-null DAT_00d09a38[i] whose vtbl+0x3d8 is true; else 0.
// No stack args; plain RET; ESI preserved.
undefined4 /* window* or 0 */ FUN_0090d390(void)
{
  char cVar1;   // AL from vtbl+0x3d8
  int iVar2;    // ESI slot index 0..4

  iVar2 = 0;
  do {
    if ((&DAT_00d09a38)[iVar2] != 0) {
      // ECX = window*; CALL [vtbl+0x3d8]
      cVar1 = (**(code **)(*(int *)(&DAT_00d09a38)[iVar2] + 0x3d8))();
      if (cVar1 != '\0') {
        return (&DAT_00d09a38)[iVar2];  // EAX = window*
      }
    }
    iVar2 = iVar2 + 1;
  } while (iVar2 < 5);
  return 0;
}
```

## Call graph (OWN VA only dualled)

```text
Client_Input_OnKeyDown_MatchAction  @ 0x0091106a
  ESC + client vtbl+0x3d8 + *(client+0x50d)==0:
    set flag+0x50d
    FUN_0090d390  [OWN]  → if nonzero:
      FUN_0090dab0  Client_Input_DismissUiWindowTable5_Inferred  [MEGA-097]
        └─ FUN_0090d400 ×5  [residual]

FUN_0092cef0  @ 0x0092cf32
  FUN_0090d390  [OWN]  → if nonzero: JMP window.vtbl+0x94
```

## Open questions / gaps

- Product English names for the five window types written by `FUN_0093e7e0` (sizes 0x554/0x5d8/0x604/0x54c/0x588 per MEGA-097).
- Product name for vtbl+`0x3d8` (IsActive / IsVisible / IsOpen — unproven).
- Runtime / bit-exact / differential open (no Launcher).
