# Annotated low-level: FUN_0040d450 → Math_FloatNearlyEqual_AbsRel

| Field | Value |
|---|---|
| Stable ID | `aa_0040d450` |
| VA | `0x0040d450`–`0x0040d4ac` (93 B) |
| System | math / float compare |
| Date | 2026-07-29 (W25-T seal; prior scaffold 2026-07-23) |
| Canonical name | `Math_FloatNearlyEqual_AbsRel` |
| Ghidra name | `FUN_0040d450` |

## Machine-level notes

- **Leaf** — no callees; FPU abs via `fabs` pattern + SSE mul/cmp on relative gate.
- **Prologue:** `PUSH ECX` (FPU/stack scratch); formals on stack at `[esp+8..+14]` after push.
- **Epilogue:** `POP ECX; RET` — **cdecl**, caller cleans 16 B (4×float).
- **Return:** `EAX = 0` (not near) or `EAX = 1` (near). Decompiler `undefined4` is bool-width.

## Semantics (sealed)

```
diff = a - b
if |diff| <= absTol:          return true   // absolute gate passes
m = max(|a|, |b|)
if |diff| <= m * relTol:      return true   // relative gate passes
return false
```

Pointer dance on stack formals:

1. After absolute fail: store `|b|` over absTol slot, `|a|` over `b` slot.
2. `pf` initially points at `|a|` slot; if `|a| <= |b|`, retarget to `|b|` slot.
3. `*pf` is therefore `max(|a|,|b|)`.
4. Fail only if `maxAbs * relTol < |diff|`.

## Call-site roles

| Caller | Role |
|---|---|
| `FUN_0040d3f0` | Component-wise **vec3** nearness (3× this) |
| `FUN_00416260` | **Quaternion** nearness including double-cover (`q` vs `-q`) |
| `FUN_0076e5e0` | Quaternion normalize: skip normalize when `lenSq` near `1.0` with `rel=abs≈1e-6` (`0x358637bd`) |
| `FUN_0076f240` | Sibling path (same family; xref present) |

Caller-passed epsilon (from `FUN_0076e5e0`): float bits `0x358637BD` ≈ **1e-6** (`read_memory` of literal / `_DAT_00a240ec`).

## Pseudocode (annotated)

```c
// cdecl; returns 1 if nearly equal, else 0
uint32_t Math_FloatNearlyEqual_AbsRel(float a, float b, float relTol, float absTol)
{
  float diff = a - b;
  if (absTol < ABS(diff)) {
    // maxAbs = max(|a|, |b|) via stack rewrite + pointer
    float absB = ABS(b);
    float absA = ABS(a);
    float *pMax = /* points at absA */;
    if (absA <= absB) pMax = /* points at absB */;
    if (*pMax * relTol < ABS(diff))
      return 0;  // both abs and rel thresholds exceeded
  }
  return 1;
}
```

## Open questions

- Product English / TGE name (likely Torque-style `mIsEqual` / similar) — residual only.
- Exact SSE vs x87 path choice under different FPU control words — CF sealed without runtime.
