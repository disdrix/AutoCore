# Annotated low-level: AssPackObject_OpenFromPath_Inferred (`FUN_0071a9d0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0071a9d0` |
| VA | `0x0071a9d0`–`0x0071aae4` (**277 B** / `0x115`) |
| System | palantir assets / assPackManager pack open |
| Date | 2026-07-23 scaffold; **W37-X seal 2026-08-04** |
| Ghidra | `FUN_0071a9d0` |
| Canonical | `AssPackObject_OpenFromPath_Inferred` |

## Machine-level notes

- **ABI (bytes):** **EAX** = path; **EDI** = pack object* (0x44); **stack** = AssPackManager* owner; **`RET 4`**.
- Sole caller: `AssPackManager_OpenPackFile_Inferred` (`FUN_00984ee0` @ `0x00984f66`) — product plate `assPackManager.cpp`.
- Path copy at `pack+0x28` via `_strdup`.
- Path build uses **`"glm"`** @ `0x00aa61bc` through `FUN_0076b190` (extension / path normalize family).
- Mode select: `pack+0x40 != 0` → open mode **1**, else **3**.
- `stat` + MSVC `_S_IFREG` (bit 15 of `st_mode`): regular file → dual `FUN_007669d0` into **pack** and **pack+0x14**; else single open mode **6**.
- File flags `0x10000000` passed as CreateFile attributes-ish third arg into `stoFileOSFile` open (`FUN_007669d0`, plate `stoFileOSFile.cpp`).
- After open: vcall size (`+0x1c`), seek (`+4`), read 4-byte footer (`+0x14`), then `FUN_0071a4a0` parse.

## Pseudocode (annotated)

```c
// EAX = const char* path
// EDI = AssPackObject* pack (0x44)
// stack = AssPackManager* owner
// returns 0 / parse status / -1; RET 4
uint32_t AssPackObject_OpenFromPath_Inferred(void)
{
  char *path;           // in EAX → ESI
  char *owned;
  int rc;
  int size;
  int seek_rc;
  int read_rc;
  uint32_t mode;
  AssPackObject *pack;  // EDI
  // locals: path buf ~248, stat buf, footer u32

  owned = _strdup(path);
  pack->path_at_0x28 = owned;           // [EDI+0x28]
  FUN_0076b190(/*path, out, ext="glm"*/);

  mode = 3;
  if ((char)pack->flag_at_0x40 != 0) {  // [EDI+0x40]
    mode = 1;
  }

  rc = stat(path_buf, &st);
  if (rc == 0 && ((st_mode_dword >> 15) & 1) != 0) {
    // regular file: open primary + secondary file subobject
    rc = FUN_007669d0(pack, path_buf, mode, 0x10000000);
    if (rc != 0) return 0xffffffff;
    rc = FUN_007669d0(pack + 0x14, path_buf, mode, 0x10000000);
  } else {
    rc = FUN_007669d0(pack, path_buf, /*mode*/6, 0x10000000);
  }

  if (rc == 0) {
    size = pack->vtbl[0x1c/4](pack);    // get size
    if (size == 0) return 0;
    if (size != -1 &&
        pack->vtbl[0x4/4](pack, size - 4, 0) == 0 &&
        pack->vtbl[0x14/4](pack, &footer, 4) == 0) {
      return FUN_0071a4a0(pack, owner_or_footer_args, size);
    }
  }
  return 0xffffffff;
}
```

## Open questions

- Product English for 0x44 pack class (beyond assPackManager caller plate).
- Exact CreateFile mapping of modes 1/3/6 (owned by `FUN_007669d0` / W37-E).
- Full `FUN_0071a4a0` TOC contract (unowned).
- Whether stack formal is only manager or also used as parse context inside `FUN_0071a4a0`.
- Runtime / bit-exact / differential.
