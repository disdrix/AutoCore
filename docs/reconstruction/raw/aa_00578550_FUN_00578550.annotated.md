# Annotated low-level: CVOGHBBase_AppendRemainingText / FUN_00578550

| Field | Value |
|---|---|
| Stable ID | `aa_00578550` |
| VA | `0x00578550`–`0x005786ee` (414 B) |
| System | `heartbeat` / timed-actions UI tooltip |
| Date | `2026-07-29` W23-R dual seal |
| Named | `CVOGHBBase_AppendRemainingText` |

## Machine-level notes

- **ABI:** `__thiscall` (ECX = `CVOGHBBase*`); **four** stack dwords cleaned by **`ret 0x10`**.
- **Formals:** `dest` (char*), two **body-dead** stack slots, `enable` (char at Stack[0x10]).
- **Gate:** `enable != 0` **and** `nPeriodCounter (+0x10) != -1000`. Combat-pool sentinel skip matches `GetRemainingSeconds` dual notes.
- **Loc:** `FUN_007a69d0` returns singleton loc ctx (`DAT_00d1f660`); `FUN_007a6de0(this, key, -1)` localizes unit + `"Remaining"`.
- **Remaining:** sealed `CVOGHBBase_GetRemainingSeconds` @ `0x00508280` (float seconds).
- **Unit cascade (seconds remaining):**
  - `> 86400` → scale `1/86400`, unit `"days"`, quantize `FUN_00578140`, fmt `"%g %s %s\n"`
  - `> 3600`  → scale `1/3600`, unit `"hrs"`, quantize `FUN_00412c60`, fmt `"%g %s %s\n"`
  - `> 60`    → scale `1/60`, unit `"min"`, quantize `FUN_00412c60`, fmt `"%g %s %s\n"`
  - else     → raw seconds, unit `"sec"`, fmt `"%.0f %s %s\n"`
- **sprintf** into 1024-byte stack buffer; **rep movsd/movsb-style** append onto existing `dest` C-string (find NUL, copy including NUL).
- **Return:** AL=1 if text appended, else 0.
- Thunk `FUN_005786f0` always passes `enable=1`.

## Constants (read_memory sealed)

| VA | Value | Role |
|---|---|---|
| `0x009d3fd0` | `86400.0f` | day threshold |
| `0x00aaaba4` | `3600.0f` | hour threshold |
| `0x00aaa9f0` | `60.0f` | minute threshold |
| `0x009d3fc0` | `≈1/86400` | day scale (float bytes share ASCII view `"E.B7"` before `"Remaining"`) |
| `0x009d3fbc` | `≈1/3600` | hour scale |
| `0x00aaa9ec` | `≈1/60` | minute scale |
| `0x009d3fc4` | `"Remaining"` | label key |
| `0x009d21bc` | `"days"` | unit key |
| `0x009d21a8` | `"hrs"` | unit key |
| `0x00a71bc8` | `"min"` | unit key |
| `0x00a69f58` | `"sec"` | unit key |
| `0x009d21ac` | `"%g %s %s\n"` | non-second format |
| `0x009d3fb0` | `"%.0f %s %s\n"` | second format |

## Recovered CF

```c
// __thiscall; ret 0x10
uint8_t CVOGHBBase_AppendRemainingText(
    CVOGHBBase *self,
    char *dest,
    void * /*unused*/,
    void * /*unused*/,
    char enable)
{
    void *loc = Loc_GetContext(); // FUN_007a69d0
    if (!enable || self->nPeriodCounter == -1000)
        return 0;

    float rem = CVOGHBBase_GetRemainingSeconds(self);
    const char *fmt;
    const char *unitKey;
    double value;

    if (rem > 86400.0f) {
        unitKey = "days";
        value = (double)FUN_00578140(rem * (1.f/86400.f));
        fmt = "%g %s %s\n";
    } else if (rem > 3600.0f) {
        unitKey = "hrs";
        value = (double)FUN_00412c60(rem * (1.f/3600.f));
        fmt = "%g %s %s\n";
    } else if (rem > 60.0f) {
        unitKey = "min";
        value = (double)FUN_00412c60(rem * (1.f/60.f));
        fmt = "%g %s %s\n";
    } else {
        unitKey = "sec";
        value = (double)rem;
        fmt = "%.0f %s %s\n";
    }

    char *label = Loc_Lookup(loc, "Remaining", -1); // FUN_007a6de0
    char *unit  = Loc_Lookup(loc, unitKey, -1);
    char buf[1024];
    sprintf(buf, fmt, value, unit, label);
    // strcat-equivalent dword/byte copy onto dest
    return 1;
}
```

## Open questions

- Product/PDB name.
- Semantic of two body-dead stack formals (vtable signature padding?).
- Exact product rounding bias constants inside `FUN_00412c60` / `FUN_00578140` (quantize helpers — residual).
- Runtime / bit-exact string golden.