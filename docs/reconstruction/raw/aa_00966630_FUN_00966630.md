# Raw capture: FUN_00966630

| Field | Value |
|---|---|
| **Stable ID** | `aa_00966630` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00966630` |
| **Canonical name** | `FUN_00966630` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00966630(undefined4 param_1,undefined4 *param_2)

{
  int iVar1;
  undefined4 *puVar2;
  
  puVar2 = &DAT_00d0e128;
  for (iVar1 = 0x10; iVar1 != 0; iVar1 = iVar1 + -1) {
    *puVar2 = 0;
    puVar2 = puVar2 + 1;
  }
  puVar2 = &DAT_00d0e0e8;
  for (iVar1 = 0x10; iVar1 != 0; iVar1 = iVar1 + -1) {
    *puVar2 = 0;
    puVar2 = puVar2 + 1;
  }
  DAT_00d1a568 = 0;
  DAT_00d1a564 = 0;
  DAT_00d1a558 = 0;
  DAT_00d1a55c = 0;
  *param_2 = 0;
  param_2[1] = 0;
  param_2[2] = 0;
  param_2[0xf] = 0;
  return;
}
```


## W38-Y re-verify (2026-08-04)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual W38-Y |
| **Tools** | `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + callers/xrefs + sibling `FUN_00966690` / consumer `FUN_0043f4b0` decompile + call-site context. **No** `disassemble_bytes` |
| **Live decompile** | == scaffold CF; **bytes win** on phantom ECX |
| **Body** | `0x00966630`–`0x00966674` exclusive (**68 B** / `0x44`); pad `CC` then `FUN_00966680` |
| **Classification** | leaf |

### Bytes (`read_memory` full body 68 B)

```text
565733f633c0b910000000bf28e1d000f3abb910000000bfe8e0d000f3ab5f893568a5d100893564a5d100893558a5d10089355ca5d100893289720489720889723c5ec3
```

### Byte-sealed ABI

| Slot | Evidence | Role |
|---|---|---|
| host | `EDX` — sole arg; call site `MOV EDX,[DAT_00d1f048]` then `CALL` | `uint32_t* host` / state-tracker* |
| ECX | **unused** in body | phantom decompiler `param_1` — drop |
| zeros | `XOR ESI,ESI` then stores | all cleared slots ← 0 |
| epilogue | `POP ESI; RET` (`C3`) | plain **fastcall-EDX** / no stack formals |
| return | void | void |

### Semantics (sealed)

Clear **device-bound state caches** held by the gfx state-tracker host and two global 16-stage tables:

```
REP STOSD zero DAT_00d0e128[0..15]   // 16 dwords @ 0x00d0e128
REP STOSD zero DAT_00d0e0e8[0..15]   // 16 dwords @ 0x00d0e0e8
DAT_00d1a568 = DAT_00d1a564 = DAT_00d1a558 = DAT_00d1a55c = 0
host[0] = host[1] = host[2] = host[0xf] = 0   // +0x00,+0x04,+0x08,+0x3C
```

| Region | Role (from consumers) | Conf |
|---|---|---|
| `DAT_00d0e128[16]` | Per-stage texture/object cache (index = stage); compared+updated in `FUN_0043f4b0` before D3D `vtbl+0x190` | **High** |
| `DAT_00d0e0e8[16]` | Per-stage companion cache (handle/id) same consumer | **High** |
| `DAT_00d1a558` / `DAT_00d1a55c` | Scalar stage-state companions (written by same path; read by `FUN_0075e8e0` / `FUN_0075e9f0`) | **High** |
| `DAT_00d1a564` / `DAT_00d1a568` | Additional scalars zeroed with the pair | **High** (role product English open) |
| `host+0/4/8` | Resource slots re-filled by `GfxDevice_Reset` after this clear (surfaces/state) | **High** |
| `host+0x3C` (`[0xf]`) | Additional host dword zeroed | **High** (English open) |

Twin: `FUN_00966690` full init of same host (`DAT_00d1f048=param_2` + float flags + **same zero block**). This unit is the **partial clear** used on successful device reset.

### Callers (1)

| Caller | Site | Notes |
|---|---|---|
| `FUN_0075eff0` `GfxDevice_Reset` | `0x0075f48f` | After CreateStateBlock/`vtbl+0x48` and companion `vtbl+0xA0`; `EDX=[DAT_00d1f048]`; then republish `host[0..2]` from device slots |

### Callees

None (leaf; uses `REP STOSD` / direct stores).

### Name

`Gfx_DeviceStateCache_Clear_Inferred` (**Inferred**). Reject scaffold `Named_CalleeOf_Named_gfxDevice_00966630`.

