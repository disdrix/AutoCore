# Raw capture: FUN_0050843d

| Field | Value |
|---|---|
| **Stable ID** | `aa_0050843d` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0050843d` |
| **Canonical name** | `FUN_0050843d` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0050843d(void)

{
  int unaff_EBX;
  int unaff_EBP;
  int unaff_ESI;
  
  *(int *)(unaff_EBP + -4) = unaff_EBX;
  *(undefined1 *)(unaff_EBP + -4) = 3;
  if (((*(char *)(unaff_EBP + 0xc) != (char)unaff_EBX) && (*(int *)(unaff_ESI + 0x18) != unaff_EBX))
     && (*(int *)(*(int *)(unaff_ESI + 0x18) + 0xb0) != unaff_EBX)) {
    FUN_005085b0(unaff_ESI);
    FUN_0050846f();
    return;
  }
  *(undefined1 *)(unaff_ESI + 0x20) = 1;
  *(int *)(unaff_ESI + 0x18) = unaff_EBX;
  ExceptionList = *(void **)(unaff_EBP + -0xc);
  return;
}
```

---

## W20-R re-verify (2026-07-29) — dual A/B seal

| Field | Value |
|---|---|
| **Agent** | W20-R OWN-ONLY |
| **Tools** | `decompile_function` + `read_memory` + `get_function_by_address` + callers (no `disassemble_bytes`) |
| **Live decompile** | Same residual shape as 2026-07-23 (unaff_* SEH fragment) |
| **Ghidra body range** | `0x0050843d`–`0x00508463` (**39 bytes** carved fragment) |
| **Sole caller** | `FUN_005083f0` / `CVOGHBBase_EndOrDestroy` (fall-through / direct call into tail) |
| **Inferred name** | `CVOGHBBase_EndOrDestroy_HardListUnlink` |

### Context — parent `FUN_005083f0` (not owned; sealed by prior dual)

Parent sets SEH, optionally calls OnEnd (`vtbl+0x14`), then enters this tail with:

- **ESI** = HB `this`
- **EBX** = 0
- **EBP** = frame; **`[ebp+8]`** = flag_onEnd (consumed above); **`[ebp+0xc]`** = flag_hard
- SEH state at `[ebp-4]` advanced here to **3**

### `read_memory` @ `0x0050843d` length 64 (hex)

```
385d0c895dfcc645fc0374268b46183bc3741f8b80b00000003bc37415568bc8
e84e010000eb0bb86a845000c38b75ec33dbc6462001895e188b4df45f5e6489
```

### Byte-level CF seal (fragment `0x0050843d`–`0x00508463`)

```text
cmp  [ebp+0xc], bl           ; flag_hard == 0?
mov  [ebp-4], ebx
mov  byte [ebp-4], 3         ; SEH state = 3
jz   FUN_0050846f            ; soft shared tail
mov  eax, [esi+0x18]         ; owner/list link
cmp  eax, ebx
jz   FUN_0050846f
mov  eax, [eax+0xb0]         ; list manager from owner
cmp  eax, ebx
jz   FUN_0050846f
push esi                     ; key = HB*
mov  ecx, eax                ; this = listMgr
call FUN_005085b0            ; remove-by-key (dual sealed)
jmp  FUN_0050846f            ; shared soft-mark + epilogue
; 00508464..69: orphan mov eax,0050846a / ret (not in Ghidra body)
```

### Decomp gaps (prefer bytes)

1. **`FUN_005085b0` arity:** decompiler shows `FUN_005085b0(unaff_ESI)` only. Bytes: **ECX = `*( *(this+0x18) + 0xb0 )`**, stack arg **ESI** (HB*). Matches `FUN_005085b0` dual (listMgr thiscall + key).
2. **Soft-path body** (`byte this+0x20 = 1`, clear `+0x18`, ExceptionList restore) lives in **`FUN_0050846f`** (`0x0050846f`), not in the 39-byte carved range. Decompiler inlines/follows the jump and presents it as part of this symbol.
3. This is an **SEH continuation fragment**, not a standalone prologue function.

### Shared soft tail `FUN_0050846f` (callee, not owned)

```c
// decompile outline
*(uint8_t *)(esi + 0x20) = 1;   // dead / ended flag
*(uint32_t *)(esi + 0x18) = 0;  // clear list link (ebx==0 from parent)
ExceptionList = *(void **)(ebp - 0xc);
// epilogue region completes parent ret 8
```

Both hard (after optional unlink) and soft (flag_hard==0 or missing list) paths end at this tail.

### Callers / callees

| Direction | Target |
|---|---|
| Caller | `FUN_005083f0` only |
| Callee | `FUN_005085b0` (conditional), then always `FUN_0050846f` |

### Gaps

- Product name of SEH state machine levels beyond “state=3”
- Whether `FUN_005085b0` success/fail is ignored (yes — return discarded; always soft-tail)
- Runtime not run
