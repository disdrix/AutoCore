# Annotated low-level: FUN_004099b0 → StdTree_NodeCtor_Val16_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004099b0` |
| VA | `0x004099b0` |
| Body | `0x004099b0`–`0x004099f3` exclusive (**67 B** / `0x43`) |
| System | MSVC `std::_Tree` node construct (Val16 / isnil@+0x21) |
| Date | 2026-08-05 (WQ9I-C dual; scaffold 2026-07-23) |
| Dual | A/B accept-with-gaps |

## Machine-level notes

- Source: raw capture + live `disassemble_function` / `read_memory` (WQ9I-C).
- Leaf: no callees. Sole caller dualed buynode `StdTree_Buynode_Val16_Isnil21_Inferred` (`0x00408990`).
- **Val16** region is four dwords at `+0x10..+0x1c` (16 bytes). Node alloc size is **0x28** (caller).
- **color @ +0x20**, **isnil forced 0 @ +0x21**. `+0x0c` gap not written (MSVC node layout).
- Entry `MOV EAX,ECX` preserves `this` in EAX through epilogue → returns constructed node*.
- Epilogue **`RET 0x14`** matches 5 stack args (left/parent/right/value*/color).

## ABI

```text
__thiscall  RET 0x14
  ECX              = NodeIsnil21* self
  [esp+4]  left
  [esp+8]  parent
  [esp+c]  right
  [esp+10] value4*   // 4× dword source
  [esp+14] color     // byte
  EAX return         = self
```

## Layout

```text
+0x00  left*
+0x04  parent*
+0x08  right*
+0x0C  (untouched)
+0x10  val[0]
+0x14  val[1]   // often pair key_hi when value is pair+mapped
+0x18  val[2]
+0x1C  val[3]
+0x20  color
+0x21  isnil = 0
size   0x28 (alloc in buynode)
```

## Pseudocode (annotated)

```c
// thiscall RET 0x14; EAX = this
NodeIsnil21* __thiscall StdTree_NodeCtor_Val16_Isnil21_Inferred(
    NodeIsnil21 *self,
    NodeIsnil21 *left,
    NodeIsnil21 *parent,
    NodeIsnil21 *right,
    const uint32_t *value4,
    uint8_t color)
{
  self->left = left;
  self->parent = parent;
  self->right = right;
  // skip +0x0c
  self->val[0] = value4[0];
  self->val[1] = value4[1];
  self->val[2] = value4[2];
  self->val[3] = value4[3];
  self->color = color;
  self->isnil = 0;
  return self;
}
```

## Open questions

- Product value_type English for the 4 dwords (pair key + mapped?).
- Runtime / bit-exact (trivial leaf — low risk).
