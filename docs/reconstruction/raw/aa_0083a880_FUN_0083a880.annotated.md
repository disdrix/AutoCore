# Annotated low-level: ShopVehObject_ApplyDefaultOrientQuat_Flag_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0083a880` |
| VA | `0x0083a880`–`0x0083a94d` inclusive (**206 B** / `0xCE`) |
| Ghidra | `FUN_0083a880` |
| Named | `ShopVehObject_ApplyDefaultOrientQuat_Flag_Inferred` |
| System | shopveh list UI — slot-object default orientation |
| Date | 2026-08-05 (WQ9L-J dual seal) |
| Evidence | `batch_decompile` + `analyze_function_complete` + `read_memory` + callers/xrefs + `get_assembly_context`. **No** `disassemble_bytes`. |

---

## Machine-level notes

- **ABI (bytes):** `MOV ESI,EAX` → **EAX** = slot object*; stack **flag** (`CMP byte [ESP+0x18],0` after `SUB ESP,0x10; PUSH ESI`); void; **`RET 4`**.
- **Null object:** early `TEST ESI` / `JZ` to shared epilogue (`POP ESI; ADD ESP,0x10; RET 4`).
- **Default quat:** four `MOVSS` from `DAT_00aaa988` / `984` / `980` / `97c` into a 16-byte stack local; then `Math_QuatNormalize(local, local)` (`FUN_0076e5e0`, dualed W26-B). Floats form a unit quat (‖q‖≈1).
- **flag == 0** (reindex match/swap refresh sites push `0`):
  - Copy local quat → `obj+0x5e0..+0x5ec` (same 16 B block swapped by dualed `ShopVehObject_SwapSelectState_ESI_EDI_Inferred`).
  - `obj+0x5dc = 1` (orient-seed flag; also in swap byte set).
  - `Math_QuatNormalize(obj+0x5e0, obj+0x5e0)`.
- **flag != 0** (from `FUN_0083ab90` after bind, push `1`):
  - `LEA ECX,[ESI+0x510]; PUSH &local; CALL FUN_0040d1a0` — thiscall sets xform quat at `this+0x80` → absolute `obj+0x590` (inside XformState@`+0x510`).
  - `CALL FUN_00833490` (uses ESI = obj; reads `+0x590` block).
  - If `obj+0x508 != 0`: `thiscall vtbl+0x44` on nested.
  - `MOV EAX,[ESI+0x508]; CALL FUN_00833390`.
- **Parent role:** shopveh five-slot reindex (`FUN_0083ac90`) calls with flag `0` after id-match refresh or post-swap; bind helper `FUN_0083ab90` calls with flag `1`.

## Pseudocode (annotated)

```c
// EAX = ShopVehObject* obj; stack uint8 flag; RET 4
void ShopVehObject_ApplyDefaultOrientQuat_Flag_Inferred(uint8_t flag /* + EAX obj */)
{
  // in_EAX → ESI
  if (obj == nullptr) return;

  float q[4] = {
    DAT_00aaa988, DAT_00aaa984, DAT_00aaa980, DAT_00aaa97c
  };
  Math_QuatNormalize(q, q);                 // FUN_0076e5e0

  if (flag != 0) {
    // assembly: this = obj+0x510 (XformState), arg = &q
    FUN_0040d1a0(/*this*/ obj + 0x510, q);  // writes obj+0x590..0x59c if changed
    FUN_00833490(/* ESI = obj */);
    if (*(void **)(obj + 0x508) != nullptr) {
      // thiscall vtbl+0x44 on nested
      (***(void ****)(obj + 0x508) /* +0x44 */)();
    }
    FUN_00833390(/* EAX = *(obj+0x508) */);
    return;
  }

  // flag == 0: seed cached orient block (swap-set +0x5e0 / +0x5dc)
  float *dst = (float *)(obj + 0x5e0);
  dst[0] = q[0]; dst[1] = q[1]; dst[2] = q[2]; dst[3] = q[3];
  *(uint8_t *)(obj + 0x5dc) = 1;
  Math_QuatNormalize(dst, dst);
}
```

## Open questions

- Product / RTTI demangle for slot class.
- Exact English for `FUN_00833490` / `FUN_00833390` / vtbl`+0x44` (helpers residual).
- Semantic label of `DAT_00aaa97c` quat (default display pose — not identity).
- Runtime / bit-exact / differential.
