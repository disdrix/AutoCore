# Annotated low-level: FUN_00406de0 / StdVector_InsertOne_RebindIt_Elem0x28_Via408050_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00406de0` |
| VA | `0x00406de0` |
| Body | `0x00406de0`–`0x00406e48` (104 B) |
| System | util / container (elem **0x28**) |
| Date | 2026-08-04 (WQ9G-H dual seal) |
| Named | `StdVector_InsertOne_RebindIt_Elem0x28_Via408050_Inferred` |

## Machine-level notes

- **ABI (bytes):** EDI = vector*; stack out**, where*, value*; **ret 0xC**; EAX = out**.
- **Stride:** **0x28** (signed div magic `0x66666667` / sar 4).
- **Callee:** `FUN_00408050` insert-N — ECX=vec, EDX=value, stack where + count(1).
- **Rebind:** `*out = begin' + index*0x28` after possible realloc.
- **Parent:** `FUN_00406220` push_back slow path only.
- Prefer assembly/bytes when decompiler drops EDI/value as unaff_*.
- **Not** tree insert (neighbor `0x00406c40`).

## Pseudocode (annotated — registers recovered)

```c
// customcc; ret 0xC
void **StdVector_InsertOne_RebindIt_Elem0x28_Via408050_Inferred(
    /*EDI*/ void *vec,
    /*stack*/ void **outIt,
    /*stack*/ void *where,
    /*stack*/ const void *value)
{
  int begin = *(int *)((char *)vec + 4);
  int index;
  if (begin != 0 &&
      ((*(int *)((char *)vec + 8) - begin) / 0x28) != 0) {
    index = ((int)where - begin) / 0x28;
  } else {
    index = 0;
  }
  FUN_00408050(vec /*ECX*/, value /*EDX*/, where, 1);
  *outIt = (void *)(*(int *)((char *)vec + 4) + index * 0x28);
  return outIt;
}
```

## Open questions

- Product demangle / 0x28 POD field layout.
- Dual seal for insert-N `FUN_00408050` and parent `FUN_00406220`.
- Runtime / bit-exact.
