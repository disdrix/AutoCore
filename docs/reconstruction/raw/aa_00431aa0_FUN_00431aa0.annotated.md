# Annotated low-level: StdVector_DestroyRange_BasicString (FUN_00431aa0)

| Field | Value |
|---|---|
| Stable ID | `aa_00431aa0` |
| VA | `0x00431aa0`–`0x00431ac2` (**34 B** / `0x22`) |
| System | MSVC `vector<basic_string<char>>` destroy-range |
| Date | 2026-07-29 (W33-H seal) |

## Machine-level notes

- Stack stdcall: `first`, `last`; epilogue `C2 08 00`.
- Loop stride **`0x1C`** — MSVC `basic_string` object size in this build.
- Dtor via IAT slot `0x009c62f4` → `0x006eace6` (string library / `~basic_string`).
- No SEH in this body; EH paths that need range destroy call this from catch blocks of insert-n.
- Prefer bytes for ABI (`RET 8`); decompile already correct on CF.

## Pseudocode (bytes-confirmed)

```c
// stack first*, last*; RET 0x08; void
void StdVector_DestroyRange_BasicString(BasicString_0x1c* first, BasicString_0x1c* last)
{
  for (; first != last; first++) {
    basic_string_dtor(first);  // IAT ~basic_string
  }
}
```

## Open questions

- Product/MSVC demangle (`_Destroy_range` vs vector member plate).
- Exact string IAT symbol English beyond decompiler label.
