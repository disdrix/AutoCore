# Annotated low-level: FUN_0052c780 (leave-FUN)

| Field | Value |
|---|---|
| Stable ID | `aa_0052c780` |
| VA | `0x0052c780`–`0x0052c854` |
| Canonical | `FUN_0052c780` (**leave-FUN**) |
| System | `skills-abilities` |
| Date | 2026-07-29 (W18-I OWN dual seal) |

## Machine-level notes

- **ABI:** `__thiscall` — `ECX =` large client object (creature/vehicle-family; uses `this+4` secondary-base adjustor). Push `EBP`/`ESI`/`EDI`; ends with `mov ecx,esi; pop esi; pop ebp; jmp FUN_004c81f0`.
- **No product / RTTI name.** Assert strings only. Scaffold `Named_VOG_DEBUG_STOP_0052c780` is **rejected** (string is shared debug halt plate, not method identity).

## Control flow (sealed)

```
*(uint8*)(this + 0x6bb) = 0

// secondary complete-object adjust (MSVC multi-base):
// adj = *(*(this+4)+4)
// sec = this + adj + 4
// vtbl = *(sec)  [via edx load pattern]
push 1
call sec->vtbl[+0x40](1)

// busy / active-cast slot on secondary:
block = FUN_005169c0(sec)           // lazy *(sec+0x64) 0x14 block
FUN_005b2ba0(block)                 // drain counter + free internal array

// skill hash at *(sec + 0x74)  [hash object*]
hash = *(sec + 0x74)
if hash->traversalLock(+0x1d) already set:
    log HashError:TraversalLock + VOG_DEBUG_STOP
hash->traversalLock = 1
node = null
loop:
    re-load hash; assert lock still set (else HashError:TraverseToNext + VOG_DEBUG_STOP)
    node = (node==0) ? hash->first(+0x14) : node->next(+0x14)
    skill = node ? *(node+8) : null
    if skill == null: break
    Skill_SetIsCastingFlag(skill, 0)   // clear is-casting; RET 4; one stack arg
hash->traversalLock = 0

*(uint32*)(this + 0x1fc) = 0
tail FUN_004c81f0(this)                // related TFID walk / flag set when gate holds
```

## Sealed offsets (this unit)

| Offset | Width | Role |
|---|---|---|
| `this+0x6bb` | u8 | cleared to 0 at entry (product English open) |
| `this+0x1fc` | u32 | cleared to 0 before tail |
| `this+4` → adj → `sec` | — | secondary base for vcall +0x40, `FUN_005169c0`, skill hash @ `sec+0x74` |
| hash `+0x1d` | u8 | traversal lock |
| hash `+0x14` | ptr | first node |
| node `+0x14` | ptr | next node |
| node `+0x8` | ptr | skill runtime* |

## Decompiler noise (falsified)

| Artifact | Reality |
|---|---|
| `Skill_SetIsCastingFlag(skill, 0, (char)uVar3)` with `uVar3=1` | Bytes: **`push 0` only**; `uVar3=1` is the **vcall +0x40** arg |
| Discard of `FUN_005169c0` result | **Falsified** — `mov ecx,eax; call FUN_005b2ba0` |
| Name `Named_VOG_DEBUG_STOP_*` | Assert string only |

## Related sealed units (context, not owned)

| VA | Name | Relation |
|---|---|---|
| `0x005169c0` | `FUN_005169c0` leave-FUN | lazy busy/counter block |
| `0x005502d0` | `Skill_SetIsCastingFlag` | clear walk consumer |
| `0x005b2ba0` | `FUN_005b2ba0` | drain/delete 0x14 block |
| `0x004c81f0` | `FUN_004c81f0` | tail cleanup |

## Open

- Product method / class name.
- Owning type of `this` and `sec` (layout partial via adjustor only).
- Meaning of `+0x6bb` / `+0x1fc` / vcall `+0x40(1)`.
- Containing function for sole call site `0x004fb9d0`.
- Runtime / bit-exact.

## Pseudocode

See `reconstructed-exact/FUN_0052c780.cpp`.
