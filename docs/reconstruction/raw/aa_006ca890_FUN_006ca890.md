# Raw capture: FUN_006ca890

| Field | Value |
|---|---|
| **Stable ID** | `aa_006ca890` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x006ca890` |
| **Canonical name** | `FUN_006ca890` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall
FUN_006ca890(int param_1,int *param_2,undefined4 *param_3,int param_4,undefined4 param_5,
            undefined4 param_6)

{
  int iVar1;
  undefined4 local_30;
  undefined4 local_2c;
  undefined4 local_28;
  undefined4 local_24;
  undefined4 local_20;
  undefined4 *local_1c;
  undefined4 local_18;
  undefined4 local_14;
  
  *(undefined4 **)(param_1 + 4) = param_3;
  *(undefined4 *)(param_1 + 0xc) = param_6;
  *(undefined4 *)(param_1 + 0x10) = 0;
  if (param_4 == 0) {
    iVar1 = 0;
  }
  else {
    iVar1 = param_4 + 0x14;
  }
  *(int *)(param_1 + 8) = iVar1;
  if (*(char *)(param_3 + 8) == '\0') {
    *(undefined4 *)(param_1 + 0x44) = 0;
  }
  else if (param_4 == 0) {
    *(undefined4 *)(param_1 + 0x44) = 0;
    *(undefined4 *)(param_1 + 0x40) = param_3[9];
  }
  else {
    *(int *)(param_1 + 0x44) = param_4 + 0x10;
    *(undefined4 *)(param_1 + 0x40) = param_3[9];
  }
  local_30 = *param_3;
  local_2c = param_3[1];
  local_28 = param_3[2];
  local_24 = param_3[3];
  local_1c = param_3 + 4;
  local_14 = param_5;
  local_20 = 1;
  local_18 = 0x10;
  (**(code **)(*param_2 + 0x30))(&local_30,param_1,0);
  return;
}
```

---

## MEGA-062 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-062** |
| **Tools** | `decompile_function` + `analyze_function_complete` + `get_function_callers` + `get_function_xrefs` + `disassemble_function` + `read_memory` + `get_function_by_address` + `get_assembly_context` |
| **Forbidden** | `disassemble_bytes` (not used); Launcher (not used); parent ledgers (not touched) |
| **Image** | `autoassault.exe` base `0x400000` |
| **Live ≡ scaffold CF** | **Yes** — decompile body identical to 2026-07-23 raw pseudocode |
| **Body** | `0x006ca890`–`0x006ca93b` inclusive (**172 B** / `0xAC`); end exclusive Ghidra `0x006ca93c`; pad `CC CC CC` then code at `0x006ca940` (undefined as function) |
| **Epilog** | `RET 0x14` (`C2 14 00`) @ `0x006ca93a` — **5** stack formals × 4 |
| **ABI** | **thiscall**: **ECX** = hit-entry object (`this`); stack `[EBP+8..+18]` = five formals; void; **no** EAX return contract |
| **Classification** | worker (indirect vcall only; no direct `FUN_*` callees) — analyze_function_complete labeled "leaf" meaning no static callees |
| **Callers** | **1** — `FUN_0055e1e0` @ `0x0055e215` `[UNCONDITIONAL_CALL]` |
| **Xrefs** | **1** UNCONDITIONAL_CALL from `0x0055e215` |
| **Callees** | none direct; **1** indirect: `(*param_2->vtbl)[+0x30](&query, this, 0)` |
| **Parent wrapper** | `FUN_0055e1e0` @ `0x0055e1e0`–`0x0055e21d` (`RET 0x8`); builds **local** entry with vtbl `0x009d27a8`, passes `*(controller+0xc4)` / soft-desc / `*(controller+0xd0)` / `0` / hub arg |
| **Grandparent** | dualed `Client_InteractWorldClickHub` (`0x00925820`) @ `0x00925c32` after hit-list init `FUN_0040c410`; next `SoftCastHitList_SortIfCountGt1` `0x0048a060` |
| **Stack align** | `AND ESP,0xfffffff0`; `SUB ESP,0x2c` for 16-byte-aligned query packet |
| **Flag offset** | soft-desc **byte @ +0x20** (decompiler `*(char*)(param_3+8)` on `undefined4*`) |
| **Field +0x24** | soft-desc dword @ +0x24 → entry `+0x40` when flag ≠ 0 |
| **Related object** | `param_4`: if non-null, entry`+8`=`param_4+0x14`, entry`+0x44`=`param_4+0x10`; else zeros (and still may copy `+0x40` when flag set) |
| **Query packet** | 4×dword header from soft-desc`[0..0xc]`; ptr soft-desc`+0x10`; flags `1` / size `0x10`; payload `param_5`; then vcall |
| **Name (inferred)** | `SoftCastHitList_CoreFillAndRegister_Inferred` |
| **Runtime Confirmed** | **not claimed** |

### Assembly ABI map (sealed)

```text
ECX              = this (hit-entry object being filled)
[EBP+0x08]       = param_2  manager / list iface (vtbl+0x30 register)
[EBP+0x0c]       = param_3  soft-cast descriptor (header + flag@+0x20 + field@+0x24)
[EBP+0x10]       = param_4  optional related object* (or null)
[EBP+0x14]       = param_5  query payload dword (wrapper always 0)
[EBP+0x18]       = param_6  backref (hub SoftCastHitList* via wrapper)
RET 0x14         = cleanup 0x14 stack bytes
```

### Sole caller push order (`FUN_0055e1e0` @ `0x0055e1e8`–`0x0055e215`)

```text
; ECX in = soft-cast controller (hub this)
MOV EAX, [EBP+0x0C]          ; hub arg2 (SoftCastHitList*)
MOV EDX, [ECX+0xD0]          ; controller related/desc field
MOV ECX, [ECX+0xC4]          ; controller manager / list iface
PUSH EAX                     ; → param_6
PUSH 0                       ; → param_5
PUSH EDX                     ; → param_4
PUSH [EBP+0x08]              ; hub arg1 → param_3 (soft desc / query)
PUSH ECX                     ; → param_2 (manager)
LEA ECX, [ESP+0x14]          ; local hit-entry this
MOV [ESP+0x14], 0x009D27A8   ; install entry vtbl
MOV [ESP+0x58], 0
CALL FUN_006ca890
```

### Entry field writes (this)

| Offset | Value |
|---|---|
| `+0x04` | `param_3` (soft-desc*) |
| `+0x08` | `param_4 ? param_4+0x14 : 0` |
| `+0x0C` | `param_6` |
| `+0x10` | `0` |
| `+0x40` | `param_3[+0x24]` when flag@+0x20 ≠ 0 (both related-null and related-set arms) |
| `+0x44` | `0` if flag==0 or param_4==0; else `param_4+0x10` |

### Hex seal (entry + epilog)

```text
entry 006ca890: 55 8B EC 83 E4 F0 83 EC 2C 8B 55 18 8B C1 8B 4D ...
epilog 006ca93a: C2 14 00
pad    006ca93d: CC CC CC
```

### Decompiler caveats (corrected via assembly)

1. Parent `FUN_0055e1e0` decompile **drops** local entry construction and shows a 5-arg free call — assembly seals local `this` + 5 stack args + vtbl install.
2. analyze signature `undefined FUN_006ca890(void)` is untyped; formals recovered from storage + `RET 0x14`.
3. `void` return is consistent (no EAX live-out); warning on undefined return type is noise for this unit.
