# Annotated low-level: Sto_UnserialFactoryMapA_GetInstance_Inferred (`FUN_00438c40`)

| Field | Value |
|---|---|
| Stable ID | `aa_00438c40` |
| VA | `0x00438c40`–`0x00438c99` (**90 B**) |
| System | arda2/storage — abstract unserialization factory map A |
| Date | 2026-07-23 scaffold; **W35-D seal 2026-07-29** |

## Machine-level notes

- **Role:** Meyers-style singleton getter for process-static NestedHash bag **`DAT_00d1fd18`** (sentinel node size **0x10**, layout **0x28** — W34-E `NestedHash_Ctor_Sentinel0x10_Inferred`).
- **Once-flag:** `DAT_00d1fd40` **bit0** only (`TEST`/`OR` with `EAX=1`).
- **Cold path:** `FUN_00457ac0(&DAT_00d1fd18)` then `_atexit(LAB_009c3310)`.
- **Always:** return `&DAT_00d1fd18` in **EAX**.
- **ABI:** cdecl, no stack args, plain `RET` (`ADD ESP,0xC` tears SEH frame only).
- **atexit:** thunk loads static into EAX and jumps to **`0x004493eb`** (`RET 4`) — **no NestedHash deep free** at process exit (heap reclaimed by CRT).
- **Callers:** exclusive to Sto factory insert (`FUN_004449b0`, string `stoAbstractUnserializationFactory.h` / "Inserting duplicate creator tag to factory") and lookup (`FUN_00437b00`). Callers read **`*(map+8)`** as empty-sentinel sentinel pointer.
- **Reject** scaffold name `Named_CalleeOf_*_gfxBody_*` — not graphics.

## Pseudocode (annotated)

```c
// cdecl void → NestedHash_Sentinel0x10*
NestedHash_Sentinel0x10* Sto_UnserialFactoryMapA_GetInstance_Inferred(void)
{
  // SEH: LAB_009bcc4d
  if ((DAT_00d1fd40 & 1) == 0) {
    DAT_00d1fd40 |= 1;                         // once bit0
    NestedHash_Ctor_Sentinel0x10_Inferred(&DAT_00d1fd18); // FUN_00457ac0
    _atexit(LAB_009c3310);                     // no-op RET4 teardown
  }
  return &DAT_00d1fd18;
}
```

## Sealed offsets / globals

| Symbol | Role |
|---|---|
| `DAT_00d1fd18` | Static NestedHash A (0x28) |
| `DAT_00d1fd40` | Once-flag dword (bit0) |
| `FUN_00457ac0` | NestedHash placement ctor (node 0x10) |
| `LAB_009c3310` | atexit thunk → `0x004493eb` RET4 |

## Open gaps

- Product/PDB class English for the map value / creator node (`+0xc` function pointer on insert path — unowned).
- Why atexit is no-op (likely intentional process-exit).
- Runtime / bit-exact / differential.
