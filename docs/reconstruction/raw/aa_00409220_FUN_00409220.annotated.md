# Annotated low-level: Map_EraseNode_B (FUN_00409220)

| Field | Value |
|---|---|
| Stable ID | `aa_00409220` |
| VA | `0x00409220`–`0x004094b4` exclusive (**660 B** by bytes) |
| System | STL map/set — MSVC `_Tree` single-node erase (isnil@+0x29) |
| Date | 2026-08-04 (WQ9E-G dual; scaffold 2026-07-23) |
| Canonical | `Map_EraseNode_B` |
| Ghidra | `FUN_00409220` |
| Twin | `Map_EraseNode` @ `0x0051cb40` (W19-I) |

## Machine-level notes

- **Decompiler hazard:** plate marks `operator_delete` noreturn and drops post-delete **size--** + **`*outIt = successor`**. Bytes seal fall-through (same as dualed twin `Map_EraseNode`).
- **Unreachable-block warnings** on two-child transplant path are false — those blocks are live in image.
- isnil gate `@node+0x29`; color `@+0x28`.
- Successor: dualed-family `FUN_004cb270` (isnil@+0x29 ++).
- Extremum refresh: `StdTree_Min_Isnil29` (`0x0051b5d0`), `StdTree_Max_Isnil29_Inferred` (`0x00421b50`).
- Rebalance rotates: `StdTree_Lrotate_Isnil29` / `StdTree_Rrotate_Isnil29_Inferred` with **ECX=map**.
- Throw uses `FUN_00401bc0` path (simpler than twin's nested `exception` objects) — CF of erase body still twin.

## ABI

```text
__thiscall
  ECX  = MapShell*     (head @ +4, size @ +8)
  stack0 = Node** outIt
  stack1 = Node* pNode
  RET 8
  *outIt = in-order successor of erased node
  map->size-- if size > 0 (after free)
```

## Pseudocode (role-annotated)

```c
void __thiscall Map_EraseNode_B(MapShell* map, Node** outIt, Node* z)
{
  // 1) if z->isnil (+0x29) → throw "invalid map/set<T> iterator"
  // 2) succ = z; FUN_004cb270(&succ)   // publish later
  // 3) choose repl (left-only / right-only / successor for two children)
  // 4) unlink z; fix parent / root links; if two children: transplant successor
  // 5) if z was leftmost → head->left = Min(repl) or parent
  // 6) if z was rightmost → head->right = Max(repl) or parent
  // 7) if z was black → RB fixup loop (uncle red/black cases + L/R rotates)
  // 8) operator_delete(z)   // FALLS THROUGH
  // 9) if (map->size > 0) map->size--
  // 10) *outIt = succ; ret 8
}
```

## Callees

| VA / symbol | Role |
|---|---|
| `FUN_004cb270` | iterator successor |
| `StdTree_Min_Isnil29` `0x0051b5d0` | leftmost |
| `StdTree_Max_Isnil29_Inferred` `0x00421b50` | rightmost |
| `StdTree_Lrotate_Isnil29` `0x004192a0` | left rotate |
| `StdTree_Rrotate_Isnil29_Inferred` `0x004192f0` | right rotate |
| `operator_delete` | free node |
| `FUN_00401bc0` + `_CxxThrowException` | invalid iterator |

## Callers

| Caller | Sites (xrefs) |
|---|---|
| `FUN_00407d70` | range-erase loop |
| `FUN_005a0470` | 1 |
| `FUN_005a3750` | 1 |
| `FUN_00807550` | 1 |
| `FUN_008078b0` | 2 |

## Residuals

- Product / PDB for this MSVC instantiation vs twin `0x0051cb40`.
- Per-caller map payload English.
- Runtime / bit-exact / differential.
