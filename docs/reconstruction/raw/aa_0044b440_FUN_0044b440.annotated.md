# Annotated low-level: FUN_0044b440 → Xform_SetScaleXYZ_DetectUniform_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0044b440` |
| VA | `0x0044b440`–`0x0044b4EF` exclusive (**175 B**) |
| System | gfx / xform-matrix subobject |
| Date | 2026-08-04 (W37-R) |

## Machine-level notes

- Source: raw capture + W37-R `read_memory` full body.
- **`__thiscall`** + **`RET 0x0C`** (three stack floats). Leaf (no CALLs).
- SSE compares (`UCOMISS` / `JP`/`JNZ` change path) then FPU `|x−y|` / `|x−z|` vs `DAT_00aaa620` (~1e-5).
- Called on matrix/xform subobjects (e.g. `CVOGEnvironmentReflect` `this+0x90` after `FUN_00972cc0`).
- Reflection stamp **(1, −1, 1)** yields non-uniform flag (`+0xB8=1`) because `|1−(−1)|=2 > eps`.

## Layout (relative to ECX this)

| Offset | Field role |
|--------|------------|
| +0x9C | scale X (float) |
| +0xA0 | scale Y (float) |
| +0xA4 | scale Z (float) |
| +0xB4 | revision / dirty generation (int, always ++ on change) |
| +0xB8 | uniform flag: **0** if |x−y|≤eps and |x−z|≤eps else **1** |
| +0xBC | cache token set to **−1** on any change |

## Pseudocode (annotated)

```c
void __thiscall Xform_SetScaleXYZ_DetectUniform_Inferred(
    XformSub* this, float x, float y, float z)
{
  if (x == this->sx && y == this->sy && z == this->sz)
    return; // exact float equality; no bump

  this->sx = x; this->sy = y; this->sz = z;
  this->cache_token = 0xFFFFFFFFu; // +0xBC

  if (fabsf(x - y) <= 1e-5f && fabsf(x - z) <= 1e-5f) {
    this->non_uniform = 0; // +0xB8
    this->revision++;      // +0xB4
    return;
  }
  this->revision++;
  this->non_uniform = 1;
}
```

## Open questions

- Product type name of the xform/matrix subobject (`FUN_00972cc0` family).
- Consumers of `+0xB8` / `+0xB4` / `+0xBC` (recompute paths).
- Runtime / bit-exact / differential — open.
