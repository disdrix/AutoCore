# Annotated low-level: FUN_00578a60 → CVOGHBSkillBase_dtor

| Field | Value |
|---|---|
| Stable ID | `aa_00578a60` |
| VA | `0x00578a60`–`0x00578ab2` inclusive (**83 B** / `0x53`) |
| System | `skills-abilities` |
| Canonical | `CVOGHBSkillBase_dtor` |
| Ghidra | `FUN_00578a60` |
| Date | 2026-08-05 (R12-026 OWN dual re-annotate) |
| Prior scaffold | 2026-07-23 generic plate |

## Machine-level notes

- Prefer assembly / `read_memory` when decompiler conflicts (noreturn delete, CALL vs JMP, missing `ADD ESP,4` in listing).
- Entry: `PUSH ESI; MOV ESI,ECX` → **`__thiscall`** this in ECX; no stack formals.
- Vtbl restore: `MOV dword ptr [ESI], 0x009d3fdc` → `PTR_FUN_009d3fdc` (same as ctor install; RTTI `".?AVCVOGHBSkillBase@@"`).
- `+0x6a0`: array heap allocated in ctor via `FUN_0058ab60` → `operator_new__(0x4b0)` + 300-dword seed; freed with **`operator_delete[]`** (`0x0048981c`).
- `+0x6b0/+0x6b4/+0x6b8`: pointer vector shell (begin/end/capacity). Complete dtor frees **buffer only** with `operator_delete` (`0x00489822`); **no** per-element teardown (contrast OnEnd clear @ `0x00578ac0`).
- Epilogue: `MOV ECX,ESI; POP ESI; JMP 0x00508390` → dualed `CVOGHBBase_dtor` (base vtbl `0x009cdab0` + optional owner detach).
- Primary retail entry: scalar deleting dtor `CVOGHBSkillBase_ScalarDeletingDtor` @ `0x00578de0` always calls this before optional free.
- Also: `thunk_FUN_00578a60` @ `0x00548f90` (5-byte JMP); EH Unwind frames under skill-HB ctors.

## Pseudocode (assembly-corrected)

```c
// __thiscall; ECX = this; no stack args; tail-jumps base dtor
void CVOGHBSkillBase_dtor(void *thisHb /* ECX */)
{
  void *pArr = *(void **)((char *)thisHb + 0x6a0);
  void *pBuf;

  *(void **)thisHb = &PTR_FUN_009d3fdc;   // skill-base vtbl

  if (pArr != 0) {
    operator_delete[](pArr);              // cdecl; returns
  }
  *(void **)((char *)thisHb + 0x6a0) = 0;

  pBuf = *(void **)((char *)thisHb + 0x6b0);
  if (pBuf != 0) {
    operator_delete(pBuf);                // cdecl; returns (decompiler false noreturn)
  }
  *(void **)((char *)thisHb + 0x6b0) = 0;
  *(void **)((char *)thisHb + 0x6b4) = 0;
  *(void **)((char *)thisHb + 0x6b8) = 0;

  // tail: ECX=this; JMP CVOGHBBase_dtor
  CVOGHBBase_dtor(thisHb);
}
```

## Scaffold raw (preserved; decompiler shape)

```c
void __fastcall FUN_00578a60(undefined4 *param_1)
{
  *param_1 = &PTR_FUN_009d3fdc;
  if ((void *)param_1[0x1a8] != (void *)0x0) {
    operator_delete__((void *)param_1[0x1a8]);
  }
  param_1[0x1a8] = 0;
  if ((void *)param_1[0x1ac] != (void *)0x0) {
                    /* WARNING: Subroutine does not return */
    operator_delete((void *)param_1[0x1ac]);
  }
  param_1[0x1ac] = 0;
  param_1[0x1ad] = 0;
  param_1[0x1ae] = 0;
  FUN_00508390();
  return;
}
```

## Open questions

- Product mangled `??_GCVOGHBSkillBase@@…` complete-body form.
- Product English for `+0x6a0` heap (seeded 0x4b0 table from `FUN_0058ab60`) and `+0x6b0` vector field.
- Whether any path can destroy skill-HB without prior OnEnd element teardown (would leak element objects if non-null).
- Runtime / bit-exact / differential.
