# Annotated low-level: FUN_0053b920 → CNDHash_Dtor_009cfa7c

| Field | Value |
|---|---|
| Stable ID | `aa_0053b920` |
| VA | `0x0053b920` |
| Body | `0x0053b920`–`0x0053b98a` (107 B / `0x6B`, bare `ret`) |
| Canonical | `CNDHash_Dtor_009cfa7c` (Ghidra `FUN_0053b920`; **Inferred**) |
| System | container / CNDHash (object vtbl `PTR_FUN_009cfa7c`, **owning** FreeBuckets) |
| Date | 2026-07-29 W24-M dual seal |
| Tools | `decompile_function` + `analyze_function_complete` + `read_memory` (no `disassemble_bytes`) |

---

## Machine-level notes

- **ABI:** `__thiscall` / `__fastcall` ECX = `CNDHash*`; bare `ret` (no stack args).
- **SEH:** scope `LAB_009a3edb`; ExceptionList chain install/restore.
- **Vtbl re-stamp:** `*this = PTR_FUN_009cfa7c` (`c7 06 7c fa 9c 00`).
- **Lock `+0x1d`:** if non-zero → log `"HashError:Destructor, already locked for traversal"` + `"VOG_DEBUG_STOP"` via `FUN_007a4480`; **non-fatal** (continues).
- **FreeBuckets:** `mov ecx,esi` → `FUN_005390d0` (`CNDHash_FreeBuckets_Owning_009cfa7c`):
  - `count(+0x0c)=0`
  - reclaim **`FUN_00538190`** (node stamp `009cefe4`, **`operator_delete` values**)
  - free sentinel slab + pointer table; null `+0x10`
- **Freelist slabs:** `lea ecx, [esi+0x20]` → `FUN_0059c8a0` (`FreelistSlabVector_dtor`).
  - Decompiler omits the `+0x20` adjust — **machine seals** it.
- **Scalar wrapper:** `FUN_0053b900` (vtbl[0] of `009cfa7c`) → this + optional `operator_delete`.

---

## Pseudocode (annotated, machine-corrected)

```c
// void __thiscall CNDHash_Dtor_009cfa7c(CNDHash *this)
void __fastcall FUN_0053b920(undefined4 *param_1 /* ECX this */)
{
  // SEH frame (LAB_009a3edb)
  *param_1 = &PTR_FUN_009cfa7c;                 // object vtbl re-stamp
  if (*(char *)((int)param_1 + 0x1d) != '\0') { // lock
    FUN_007a4480(0, "HashError:Destructor, already locked for traversal");
    FUN_007a4480(0, "VOG_DEBUG_STOP");
    // continue — non-fatal
  }
  FUN_005390d0(param_1);                        // FreeBuckets owning (ECX=this)
  FUN_0059c8a0((int)param_1 + 0x20);            // freelist slab vector (ECX=this+0x20)
  // SEH restore
}
```

---

## Family context

| Unit | VA | Role |
|---|---|---|
| Scalar dtor | `0x0053b900` | vtbl[0]; wraps this |
| **Dtor body** | **`0x0053b920`** | this unit |
| FreeBuckets owning | `0x005390d0` | count=0 + reclaim + free table |
| Reclaim owning | `0x00538190` | stamp `009cefe4`, delete values, freelist push `+0x20` |
| Freelist slab dtor | `0x0059c8a0` | `_aligned_free` slabs + `operator_delete` vector |
| Recreate | `0x00539dd0` | shares FreeBuckets `005390d0` |

Twin non-owning dtor for comparison: `CNDHash_Dtor_009cfa70` @ `0x0053b810` (FreeBuckets `0053b880` / reclaim `0053b8c0`).

---

## Open / gaps

1. Product / PDB English class name for object family `009cfa7c`.
2. Exact freelist subobject `+0x04` field (unused by this dtor path).
3. Runtime / bit-exact not run.
