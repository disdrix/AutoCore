# Annotated low-level: stoFileOSFile_Open (`FUN_007669d0`)

| Field | Value |
|---|---|
| Stable ID | `aa_007669d0` |
| VA | `0x007669d0`–`0x00766d8c` exclusive (**956 B** / `0x3BC`) |
| System | arda2 storage — `stoFileOSFile` OS open |
| Date | 2026-08-04 (W37-E OWN-ONLY) |
| Name | `stoFileOSFile_Open` (class/file **Confirmed**; method Open **Inferred**) |

## Machine-level notes

- Source path string: `C:\vog\1_code\palantir\arda2\storage\stoFileOSFile.cpp` line `0xB9` (185) on fail log.
- Fail format: `"File open error <%s> - %s"`.
- **ABI:** ECX=`stoFileOSFile*`; stack path, mode, dwFlagsAndAttributes; **`RET 0x0C`** twice (success/fail); returns `0` / `-1`.
- **this+4** = mode (written at entry); **this+8** = `HANDLE` from `CreateFileA`.
- Share mode fixed `FILE_SHARE_READ` (1); security/template null.
- Optional quality remap gated by `FUN_007a69d0()->+0x2c != 0` for `.ogg` / `.dds` only; probe via `fopen("rb")` then switch path pointer to scratch `DAT_00d206c8`.
- Quality tags: `1→_FR`, `2→_DE`, `3→_UK` (`read_memory` @ `0x00a9d998`..`0x00a9d9a0`).
- DDS suffix table `PTR_DAT_00b01838`…`<0xb0184c`: `"_on"`, `"_off"`, `"_over"`, `"_down"`, +5th ptr.
- Mode 0 fails without CreateFile; modes 4/5 leave access=0 (CreateFile fails).
- Caller evidence: open-read mode **1** (`FUN_009841d0`, `FUN_00984910`); open-write mode **6** (`FUN_00984d30`).

## Pseudocode (annotated summary)

```c
// ECX = stoFileOSFile* this
// stack: path, mode, dwFlagsAndAttributes
// RET 0x0C; 0 = ok, 0xFFFFFFFF = fail
int __thiscall stoFileOSFile_Open(stoFileOSFile* this, const char* path,
                                  uint32_t mode, DWORD attrs)
{
  this->mode = mode;  // +4
  // optional .ogg/.dds quality remap → path may become &DAT_00d206c8
  DWORD access = 0;
  DWORD disp = OPEN_EXISTING;  // 3
  switch (mode) {
    case 0: return -1;
    case 1: access = 0x20001; break;
    case 3: access = 0x20007; break;
    case 6: disp = CREATE_ALWAYS; // fallthrough
    case 2: access = 0x20006; break;
    // 4,5: access stays 0
  }
  HANDLE h = CreateFileA(path, access, FILE_SHARE_READ, NULL, disp, attrs, NULL);
  this->handle = h;  // +8
  if (h != INVALID_HANDLE_VALUE)
    return 0;
  // FormatMessage + vog_LogMessage(stoFileOSFile.cpp:0xB9, level 2, …)
  return -1;
}
```

## Open residual

- Exact product method demangle (Open / OpenEx / OpenOS).
- Full semantics of `FUN_007a69d0` quality field and 5th DDS table entry.
- Runtime / bit-exact / differential (no Launcher).
