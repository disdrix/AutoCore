# Annotated low-level: EmptyBase_Ctor_ReturnThis_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0063f5b0` |
| VA | `0x0063f5b0`–`0x0063f5bb` (Ghidra body) |
| Canonical | `EmptyBase_Ctor_ReturnThis_Inferred` |
| Ghidra | `FUN_0063f5b0` |
| System | object / POD base construction (physics & small components) |
| Date | 2026-07-29 (W25-L dual seal) |
| Tools | `decompile_function` + `read_memory` + analyze/xrefs; no `disassemble_bytes` |

## Machine-level notes

- **12-byte body**: save ECX→ESI, call identity `FUN_005edf20` (`mov eax,ecx; ret`), restore return this in EAX.
- Ghidra surfaces `__fastcall param_1`; machine is **thiscall / ECX=this**, bare `ret` (no stack cleanup).
- Callee is a pure identity used elsewhere as a trivial function pointer; the call does not mutate `this`.
- Callers are small constructors that invoke this unit first, then write POD fields / defaults (e.g. `FUN_0064dda0` zeros dwords; `FUN_005d6720` loads float constants).

## Control flow

```
ESI = this (ECX)
call FUN_005edf20()   // identity: EAX = ECX (this); ignored
EAX = ESI             // return this
ret
```

## Pseudocode (annotated)

```c
// ABI: thiscall (ECX=this). Bare ret. EAX = this.
// Ghidra: undefined4 __fastcall FUN_0063f5b0(undefined4 param_1)

void* EmptyBase_Ctor_ReturnThis_Inferred(void* this_obj)
{
  IdentityPassThrough_Inferred(this_obj); // FUN_005edf20 — no side effects
  return this_obj;
}
```

## Call surface

| Direction | Target | Note |
|---|---|---|
| **Callee** | `FUN_005edf20` @ `0x005edf20` | 3 B: `mov eax,ecx; ret` |
| **Callers** | `FUN_005d6720`, `FUN_005d67f0`, `FUN_005fe5c0`, `FUN_005fe650`, `FUN_0064dda0`, `FUN_0064de10`, `FUN_006520c0`, `0x00652028` | first-step base ctor |

## Offsets / globals

None (no field stores; no DAT_* loads).

## Open gaps

1. Product C++ base class name for this empty ctor.
2. Whether `FUN_005edf20` was historically a hook / telemetry / vbases stub (retail is identity).
3. Runtime / bit-exact — open.
