# Annotated low-level: FUN_005a2850 / Map_Tree_Predecessor_Isnil15

| Field | Value |
|---|---|
| Stable ID | `aa_005a2850` |
| VA | `0x005a2850` |
| Structural name | `Map_Tree_Predecessor_Isnil15` |
| System | shared map/tree helper (isnil +0x15 family) |
| Date | 2026-07-29 (W19-N dual seal) |

## Machine-level notes

- **ABI:** `__fastcall` — **ECX = Node\*\*** (iterator / node pointer holder); **no stack args**; plain **`RET` (`C3`)**.
- **Leaf:** zero callees; no allocation, rebalance, key compare, or color touch.
- **Role:** MSVC-style ordered-tree **iterator decrement** (in-order **predecessor**), same algorithm as `Map_Tree_Predecessor_Isnil49` (`0x00537770`) with isnil at **`+0x15`** instead of `+0x49`.
- **Not successor:** end/isnil → **right** (max); left subtree → **rightmost**; climb while **left** child.

## Node assumptions (partial — this unit only)

```
+0x00  left*
+0x04  parent*
+0x08  right*
+0x15  isnil (u8)   // 0 = real node; !=0 = head/end sentinel
```

Key / color / value layout is **caller-map specific** (e.g. `FUN_004e47b0` uses byte key @ `+0x0C` with this isnil; insert duals may differ).

## Sealed control flow

```c
// void __fastcall Map_Tree_Predecessor_Isnil15(Node** pIt)
void __fastcall FUN_005a2850(int *param_1)
{
  int *cur = (int *)*param_1;
  if (*(char *)((int)cur + 0x15) != '\0') {
    *param_1 = cur[2];          // right — end → max
    return;
  }
  int left = *cur;
  if (*(char *)(left + 0x15) == '\0') {
    // rightmost of left subtree
    int n = left;
    int r = *(int *)(left + 8);
    while (*(char *)(r + 0x15) == '\0') {
      n = r;
      r = *(int *)(r + 8);
    }
    *param_1 = n;
    return;
  }
  // climb while current is left child of parent
  int *parent = (int *)cur[1];
  if (*(char *)((int)parent + 0x15) == '\0') {
    do {
      if (*param_1 != *parent) break;  // cur != parent->left
      *param_1 = (int)parent;
      parent = (int *)parent[1];
    } while (*(char *)((int)parent + 0x15) == '\0');
    if (*(char *)((int)parent + 0x15) == '\0') {
      *param_1 = (int)parent;
    }
  }
}
```

## Open questions

- Product / MSVC template English (`_Tree_unchecked_const_iterator::operator--`, etc.).
- Full node schemas per caller map (only links+isnil sealed here).
- Runtime edge cases (`--begin`, empty, single node).
- Bit-exact image diff.
