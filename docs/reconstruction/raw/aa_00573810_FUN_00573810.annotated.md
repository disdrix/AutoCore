# Annotated low-level: StdMap_InsertOrFind_Val12_UintKey (`FUN_00573810`)

| Field | Value |
|---|---|
| Stable ID | `aa_00573810` |
| VA | `0x00573810` |
| Body | `0x00573810`–`0x005738c8` exclusive (**184** B) |
| System | std map insert-or-find (Val12 / uint key) |
| Date | 2026-07-29 (W28-T dual seal) |

## Machine-level notes

- Source: raw capture for `aa_00573810` + W28-T live re-verify.
- Classic MSVC `_Tree` **unique insert** (insert-or-find): walk by key, predecessor when going left and not leftmost, always-insert helper on miss, out-pair on hit with **no value rewrite**.
- Map shell: head @ `this+4`, size @ `this+8` (size not touched here — insert helper owns `size++`).
- Node Val12: key @ `+0x0c` (uint), isnil @ `+0x19`, color @ `+0x18`.
- Legacy Named_CalleeOf Client_ShowInventory seed is **narrow** (callers include inventory grid helpers, but unit is generic map).

## Signature (asm-sealed)

```c
struct Val12 {
    uint32_t key;   // +0 compared as unsigned
    uint32_t v1;
    uint32_t v2;
};

struct InsertPair {
    void*   it;       // +0  node*
    uint8_t inserted; // +4
};

// __thiscall ECX = map; RET 8
void StdMap_InsertOrFind_Val12_UintKey(
    MapShell_Val12* map,
    InsertPair* out,
    const Val12* value);
```

## Pseudocode (annotated)

```c
void __thiscall FUN_00573810(Map* map, InsertPair* out, const uint* value)
{
  Node* head = map->head;          // *(map+4)
  Node* parent = head;
  bool addLeft = true;

  if (root = head->left, !root->isnil) {
    Node* cur = root;
    do {
      parent = cur;
      addLeft = (*value < parent->key);  // key @ node+0xc
      cur = addLeft ? parent->left : parent->right;
    } while (!cur->isnil);
  }

  // parent is insertion candidate (lower_bound-shaped)
  if (addLeft) {
    if (parent == head->leftmost) {     // **head
      Node* n = InsertAndRebalance(map, &parent, 1, parent, value); // FUN_00573510
      out->it = n; out->inserted = 1;
      return;
    }
    Predecessor(&parent);               // FUN_005ae050
  }
  if (parent->key < *value) {
    Node* n = InsertAndRebalance(map, &parent, addLeft, parent, value);
    out->it = n; out->inserted = 1;
    return;
  }
  // equal key
  out->it = parent; out->inserted = 0;
  return;
}
```

## Call graph

| Direction | Targets |
|---|---|
| Callees | `FUN_00573510` (`StdTree_InsertAndRebalance_Val12`, W27-S) → buys via `FUN_005ae220`; `FUN_005ae050` (predecessor) |
| Callers | `FUN_00572360` (InventoryGrid_RePlaceItems_Inferred), `FUN_00572730`, `FUN_00572ab0` (2 sites) |

## Twin / peer

Peer insert-or-find pattern: `Skill_CategoryCooldownMap_Insert` (`0x0051d2f0`) — same CF family, different always-insert helper (`0x0051c1c0`). **Do not merge** product names.

## Open

- Product English / demangle for this map's value payload (v1/v2 meaning).
- Runtime / bit-exact / differential.
