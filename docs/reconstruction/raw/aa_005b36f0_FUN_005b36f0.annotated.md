# Annotated low-level: FUN_005b36f0 → Env_ApplyFogShaderAndReflect_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005b36f0` |
| VA | `0x005b36f0`–`0x005b3992` (674 B / `0x2A2`) |
| Canonical | `Env_ApplyFogShaderAndReflect_Inferred` |
| System | client environment / fog shader |
| Date | 2026-07-23 scaffold; **W29-I dual seal 2026-07-29** |

## Machine-level notes (W29-I)

- **Virtual method**: `PTR_FUN_009d95a0[1]`; no direct CALL xrefs.
- Class host constructed by `FUN_005b35a0` (stores parent at `this+0x15c`, material/view ptrs).
- **ret 4** thiscall with one stack argument.
- Fog shader constants driven by embedded strings + `this+0x164` block.
- Color path uses sealed W28-E packer `Color_PackFloat4_Scale255_Inferred` (`00973820`) then nested pack leaf (W29-I `00973590`).
- Ghidra `s___AVCVOGEnvironmentReflect___00af3eb8[0x1d/0x1e]` are **static flag bytes** at `0x00af3ed5` / `0x00af3ed6` (image `01 01`), not product proof that *this* class is `CVOGEnvironmentReflect`.

## Algorithm (sealed high level)

```
// ECX = this; stack: arg0; ret 4; returns 0 or -1

if (this->ctx_15c == 0 || this->ctx_15c->field_e4f8 == 0)
  return -1;

if (this->flag_80 != 0) {
  // copy flag byte this+0x78 into two slots of global DAT_00d1f048
  // bind shader params from this+0x164:
  //   gFogEnable  ← +0x70
  //   gFogStart   ← +0x10c
  //   gFogEnd     ← +0x110
  //   gFogColor   ← float4 at +0x60  (FUN_00442d50 + Color_PackFloat4_Scale255)
  // secondary gfx setup (FUN_0075bf40, FUN_0040d340, FUN_00442b90, …)
  // update reflect/light vectors on *(this+0x158)+8 with dirty counters
  // optional FUN_005b39d0 / FUN_0099b8a0 when flag 00af3ed5
  // optional vfunc (*obj+0x3c)(arg0) when present
}
return 0;
```

## Key field map (`this`)

| Offset | Role (structural) |
|---|---|
| `+0x4` | object/id passed to `FUN_00442b90` / optional `FUN_0074dfa0` |
| `+0x78` | byte mirrored into `DAT_00d1f048+0x0F` and `+0x2D` |
| `+0x80` | work-enable flag |
| `+0x158` | ptr → env/reflect host (`+8` holds float/vector block) |
| `+0x15c` | parent/context (ctor from `param_2`) |
| `+0x164` | fog/shader param block (enable/start/end/color) |

## Open questions

- Product class English for host (`PTR_FUN_009d95a0`).
- Exact meaning of `param_2` and vfunc `+0x3c`.
- Full duals of nested helpers (not OWN).
- Runtime / bit-exact.
