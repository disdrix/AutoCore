# Annotated low-level: Item_PatenteeMatchesCharacterOrEmpty_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00522950` |
| VA | `0x00522950`–`0x005229b1` inclusive (**98 B** / `0x62`) |
| Ghidra name | `FUN_00522950` |
| Canonical name | `Item_PatenteeMatchesCharacterOrEmpty_Inferred` |
| System | `inventory-transfer` |
| Date | `2026-08-05` (MEGA-108 dual refresh) |
| Prior scaffold | `Named_CalleeOf_Mission_Mission_Object_00522950` — **retired** (mission misname) |

## Machine-level notes

- **ABI sealed:** thiscall `ECX=Character*`, stack `Item*`, return `AL` bool, `RET 4`.
- **Field `item+0x101`:** C-string displayed as `"Patented By: "` by `FUN_00847240` — patentee name, not craft-discipline English.
- **Empty string short-circuit:** length scan lands at `item+0x102` → skip compare → **true** (no patent restriction).
- **Character name:** MSVC MI this-adjust then **vtbl+0x160** returns `char*`; compared via `_stricmp` (IAT `0x009c6678`).
- Prefer assembly when decompiler collapses MI adjust; raw CF ≡ live.

## Pseudocode (annotated)

```c
// ECX = character (local player / host character)
// stack arg0 = item*
// returns AL: 1 = patentee empty or matches character name; 0 = null item or mismatch
bool __thiscall Item_PatenteeMatchesCharacterOrEmpty_Inferred(
    Character *character,
    Item *item)
{
  char c;
  char *p;
  int cmp;

  if (item == NULL) {
    return false;                         // 00522958: xor al,al; ret 4
  }

  p = (char *)item + 0x101;               // patented-by C-string
  // strlen-style scan
  do {
    c = *p;
    p = p + 1;
  } while (c != '\0');

  // empty iff scan advanced only one past start → p == item+0x102
  if (p != (char *)item + 0x102) {
    // MI this-adjust: character secondary base + vtbl slot 0x160
    //   eax = *(character+4); edx = *(eax+4);
    //   eax = *(edx+character+4); ecx = edx+character+4;
    //   call [eax+0x160]  → char* characterName
    char *characterName = Character_GetName_Vtbl160_Inferred(character);

    cmp = _stricmp((char *)item + 0x101, characterName);
    if (cmp != 0) {
      return false;                       // mismatch → cannot RE (parents)
    }
  }
  return true;                            // empty patent OR name match
}
```

## Callers (4 UNCONDITIONAL_CALL)

| Site | Parent | Role |
|---|---|---|
| `0x0085cee3` | `FUN_0085ce20` ItemHover RE/tinker/gadget tooltip | fail → `"Cannot be Reverse Engineered."` |
| `0x0093d507` | `FUN_0093d110` Inv RE action | fail → `"Cannot be Reverse Engineered."` |
| `0x0084797c` | `FUN_00847240` item detail / patented-by line | fail → `"Cannot be Reverse Engineered"` |
| `0x0052e699` | `FUN_0052e640` RE eligibility float helper | fail → early out |

## Open questions

- Product/PDB English for character vtbl+0x160 (almost certainly GetName / display name).
- Exact `Character` MI layout for the this-adjust (`[this+4]` vbtable path).
- Whether empty `+0x101` means "never patented" vs "cleared patent" product-side.
- Runtime / bit-exact / differential: OPEN.
