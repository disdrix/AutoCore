# Annotated low-level: StdTree_Lrotate_Val12 (FUN_00573170)

| Field | Value |
|---|---|
| Stable ID | `aa_00573170` |
| VA | `0x00573170` |
| Body | `0x00573170`–`0x005731BE` exclusive (**78** B) |
| Canonical | `StdTree_Lrotate_Val12` (inferred) |
| Ghidra | `FUN_00573170` |
| System | MSVC std `_Tree` left-rotate (Val12 node) |
| Date | 2026-07-29 (W26-Q seal; scaffold 2026-07-23) |

## Machine-level notes

- **ABI:** `__thiscall`; **ECX** = tree (`_Myhead` at `this+4`); stack arg = **node* x**; **void**; **`RET 4`**.
- **Leaf:** no callees; three `RET 4` exits (root / left-child / right-child reattach).
- **Node layout (Val12 / 0x1c):** `left@0`, `parent@4`, `right@8`, value 12 B @ `+0x0c`, `color@+0x18`, `isnil@+0x19`.
- **Nil test:** `byte [y->left + 0x19] == 0` means non-nil child; only then set parent link.
- **Semantics:** classical RB / MSVC `_Lrotate(x)`:
  1. `y = x->right`
  2. `x->right = y->left`; if `y->left` not nil → parent = x
  3. `y->parent = x->parent`
  4. Reattach `y` where `x` hung (root / left / right of parent)
  5. `y->left = x`; `x->parent = y`
- **Pair:** right-rotate twin `FUN_00418c10` / `StdTree_Rrotate_Val12`.
- **Shared:** 13 callers (insert/erase rebalance for Val12 maps/sets), incl. `Map_TreeInsertAndRebalance_Val12` (`0x0051c1c0`).

## Pseudocode (annotated)

```c
// __thiscall tree*, node* x  — left-rotate about x
void __thiscall StdTree_Lrotate_Val12(int tree, int x)
{
  int *y;      // x->right
  int *parent;

  y = *(int **)(x + 8);                 // y = x->right
  *(int *)(x + 8) = *y;                 // x->right = y->left
  if (*(char *)(*y + 0x19) == '\0') {  // if y->left not isnil
    *(int *)(*y + 4) = x;               // y->left->parent = x
  }
  y[1] = *(int *)(x + 4);               // y->parent = x->parent
  if (x == *(int *)(*(int *)(tree + 4) + 4)) {  // x was root
    *(int **)(*(int *)(tree + 4) + 4) = y;
    *y = x;                             // y->left = x
    *(int **)(x + 4) = y;               // x->parent = y
    return;
  }
  parent = *(int **)(x + 4);
  if (x == *parent) {                  // x was left child
    *parent = (int)y;
    *y = x;
    *(int **)(x + 4) = y;
    return;
  }
  parent[2] = (int)y;                   // x was right child
  *y = x;
  *(int **)(x + 4) = y;
  return;
}
```

## Live ≡ raw

Live 2026-07-29 decompile ≡ 2026-07-23 raw (identical CF). Full body hex sealed via `read_memory`.

## Open questions

- Product / MSVC demangle for each instantiating map type (shared helper).
- Runtime / bit-exact golden not run.
