# Raw capture: FUN_0044a380

| Field | Value |
|---|---|
| **Stable ID** | `aa_0044a380` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0044a380` |
| **Canonical name** | `FUN_0044a380` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0044a380(int param_1)

{
  uint in_EAX;
  uint uVar1;
  int iVar2;
  
  iVar2 = *(int *)(param_1 + 4);
  if (iVar2 == 0) {
    uVar1 = 0;
  }
  else {
    uVar1 = *(int *)(param_1 + 8) - iVar2 >> 2;
  }
  if (uVar1 < in_EAX) {
    if (iVar2 == 0) {
      iVar2 = 0;
    }
    else {
      iVar2 = *(int *)(param_1 + 8) - iVar2 >> 2;
    }
    FUN_00463e30(*(undefined4 *)(param_1 + 8),in_EAX - iVar2,&stack0x00000004);
    return;
  }
  if ((iVar2 != 0) && (in_EAX < (uint)(*(int *)(param_1 + 8) - iVar2 >> 2))) {
    FUN_00469f10(iVar2 + in_EAX * 4,*(int *)(param_1 + 8));
  }
  return;
}
```

---

## W37-Z re-verify (2026-08-04)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual W37-Z |
| **Tools** | `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + callers/xrefs + call-site `get_assembly_context`. **No** `disassemble_bytes` |
| **Live decompile** | ≡ scaffold CF (grow/shrink/no-op) |
| **Body** | `0x0044a380`–`0x0044a3e9` exclusive (**105 B** / `0x69`); pad `CC` then next |
| **Classification** | worker |

### Bytes (`read_memory` full body 105 B)

```text
568bf08b410485c057750433d2eb088b51082bd0c1fa023bd6732685c0750433d2eb088b51082bd0c1fa028b41088d7c240c572bf25650e8749a01005f5ec2040085c074208b79088bd72bd0c1fa023bf27312538d04b057508d4424188bd9e82cfb01005b5f5ec20400
```

### Byte-sealed ABI (overrides decompiler formals)

| Slot | Evidence | Role |
|---|---|---|
| **ECX** | `MOV EAX,[ECX+4]` begin; used as `this` | `vec*` (MSVC vector control block) |
| **EAX** | `PUSH ESI; MOV ESI,EAX` at entry | **newCount** (dword element count) |
| **stack+4** | `RET 4` (`C2 04 00` ×2); grow passes `&stack` as fill* | **fill value** (dword by value) |
| cleanup | `C2 04 00` both exits | **RET 4** |

Decompiler `void __fastcall (int param_1)` + phantom `in_EAX` is **incomplete**: missing fill formal and `RET 4`. **Bytes win**.

### Semantics (sealed)

MSVC-style **`vector<uint32_t>::resize(n, value)`** with **EAX=n** (not stack count):

```
begin = *(this+4); end = *(this+8)
size  = begin ? (end - begin) >> 2 : 0

if size < newCount:   // grow
  FUN_00463e30(this, end, newCount - size, &fill)   // InsertN at end
  RET 4

if begin && newCount < size:  // shrink
  // EBX=this; erase [begin + newCount*4, end)
  FUN_00469f10(begin + newCount*4, end)  // EBX=this; publishes new end
  RET 4

// equal / empty: no-op
RET 4
```

Layout: begin `@+4`, end `@+8`, capacity end `@+0xC` (grow callee only). Element stride **4**.

### Callers (12 function + 1 raw)

`FUN_00436b00`, `FUN_0043ba40`, `FUN_0043fb50`, `FUN_00443530`, `FUN_0044a140`, `FUN_0044e650`, `FUN_004638c0`, `FUN_00464340`, `FUN_00469910`, `FUN_0046bf90` (NestedHash0x10 insert rehash), `FUN_0046c8f0`, `FUN_00746d40`; raw xref `0x0096015d`.

Rehash pattern (e.g. `0x0046bfe3`): `ECX=bucket_vec*`, compute mask, `PUSH fill` (often map end-sentinel `@map+8`), `EAX = mask+2`, `CALL`.

### Callees

| VA | Role |
|---|---|
| `0x00463e30` | dword vector InsertN / grow-realloc (capacity `@+0xC`) |
| `0x00469f10` | dword erase-range shrink (EBX=vec; memmove-publish end) |

### Twin distinction

Sibling **`FUN_004367f0`** (`StdVector_DwordResize`) is the same CF shape but **stack newCount + fill**, **`RET 8`**. This unit is the **EAX-count / RET-4** twin used heavily by NestedHash / hash-map rehash paths.

### Name

`StdVector_DwordResize_EaxCount_Inferred` (**Inferred**). Reject scaffold `Named_CalleeOf_Named_effVertexDecl_0044a380`.
