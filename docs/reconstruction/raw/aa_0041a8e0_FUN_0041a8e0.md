# Raw capture: FUN_0041a8e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0041a8e0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0041a8e0` |
| **Canonical name** | `FUN_0041a8e0` |
| **Named dual** | `Com_LoaderCtx_NestedTeardown_Inferred` |
| **System** | `crt-com` / DB-WAD loader context nested teardown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0041a8e0(int param_1)

{
  int *piVar1;
  void *pvStack_c;
  undefined1 *puStack_8;
  uint local_4;
  
  puStack_8 = &LAB_009bd810;
  pvStack_c = ExceptionList;
  local_4 = 0;
  ExceptionList = &pvStack_c;
  if (*(int *)(param_1 + 0x10) != 0) {
    ExceptionList = &pvStack_c;
    FUN_00422de0();
    FUN_0041bf70();
  }
  local_4 = CONCAT31(local_4._1_3_,1);
  FUN_0041bf70();
  piVar1 = *(int **)(param_1 + 0x14);
  if (piVar1 != (int *)0x0) {
    (**(code **)(*piVar1 + 8))(piVar1);
  }
  local_4 = local_4 & 0xffffff00;
  piVar1 = *(int **)(param_1 + 0x10);
  if (piVar1 != (int *)0x0) {
    (**(code **)(*piVar1 + 8))(piVar1);
  }
  if (*(int *)(param_1 + 0xc) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(*(void **)(*(int *)(param_1 + 0xc) + 0xc));
  }
  ExceptionList = pvStack_c;
  return;
}
```

---

## W22-M re-verify (2026-07-29) — live Ghidra decompile + read_memory

| Field | Value |
|---|---|
| **Agent** | W22-M OWN-ONLY |
| **Tools** | `decompile_function` / `batch_decompile` + `read_memory` + callers/callees/xrefs |
| **No** | `disassemble_bytes`; Launcher; parent ledger edits |
| **Body range** | `0x0041a8e0`–`0x0041a988` (169 bytes; ends `ret 4`) |
| **Live decompile** | Truncated — first `operator_delete` mis-marked noreturn; **bytes supersede** tail |
| **Live ≡ prior raw head** | Yes through Release of `+0x10` (before truncated delete arm) |

### Entry / epilogue bytes (`read_memory` 169 B @ `0x0041a8e0`)

```
6a ff 68 10 d8 9b 00 64 a1 00 00 00 00 50 64 89 25 00 00 00 00
51 53 56 8b 74 24 1c 57 33 db 8d 7e 10 ... c2 04 00
```

- SEH filter **`LAB_009bd810`** (`push 0x009bd810`).
- **`ret 4`** → **`__stdcall`**, one stack formal (`ctx`).

### Byte-sealed control flow (authoritative over truncated decompile)

```
ESI = ctx                              ; [esp+0x1c] after SEH/push
EDI = &ctx->field_10                   ; lea edi,[esi+0x10]
EBX = 0
SEH_state = 0

if (*(int*)EDI != 0) {                 ; ctx+0x10 non-null
  FUN_00422de0();                      ; OleDb_ReleaseAccessorHandleArray_Inferred (EAX=iface, ESI=ctx)
  FUN_0041bf70();                      ; AdoRowsetHolder_Teardown_Inferred (EDI=&ctx+0x10)
}
SEH_state = 1
FUN_0041bf70();                        ; always (EDI still &ctx+0x10)

if (*(ctx+0x14) != 0)                  ; [edi+4]
  IUnknown_Release(*(ctx+0x14));       ; vtbl+8
SEH_state = 0

if (*(ctx+0x10) != 0) {                ; reload [edi]
  IUnknown_Release(*(ctx+0x10));       ; vtbl+8
}

p = *(ctx+0x0C);
if (p != 0) {
  operator_delete(*(void**)(p+0x0C));  ; cdecl @ 0x00489822
  if (*(void**)p != 0)
    free(*(void**)p);                  ; IAT [0x009c6524] (sealed free elsewhere)
  *(p+0)=*(p+4)=*(p+8)=0;
  operator_delete(p);                  ; second delete @ 0x00489822
}
// restore FS ExceptionList; ret 4
```

### Call targets (relative `e8` / IAT)

| Site | Target |
|---|---|
| `0x0041a90c` | `FUN_00422de0` / `OleDb_ReleaseAccessorHandleArray_Inferred` |
| `0x0041a911`, `0x0041a91f` | `FUN_0041bf70` / `AdoRowsetHolder_Teardown_Inferred` |
| `0x0041a92e`, `0x0041a93e` | COM **vtbl+8** Release |
| `0x0041a94c`, `0x0041a96d` | `operator_delete` @ `0x00489822` |
| `0x0041a95b` | **`free`** IAT `[0x009c6524]` |

### Callers

| Caller | Site | Role |
|---|---|---|
| `FUN_0041a3e0` / `Com_LoaderCtx_Teardown_Inferred` | `0x0041a428` | after Release `*(ctx+0x24)` |
| Unwind `@0x009bd840` | SEH | exception path |

### Note on decompiler gap

Ghidra labels `operator_delete` @ `0x00489822` as **noreturn**, so the live decompile **stops** after the first `delete *(p+0x0C)` and omits `free` / zero / second `delete` / epilogue. **Bytes are authoritative** for the full arm.
