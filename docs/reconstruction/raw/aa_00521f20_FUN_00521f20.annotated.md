# Annotated low-level: FUN_00521f20 / Character_ComputeTinkerSuccessChance_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00521f20` |
| VA | `0x00521f20`–`0x00521fe7` (**200 B** / `0xC8`) |
| System | inventory-transfer / tinker success chance |
| Date | 2026-08-05 (R10-035 dual refresh) |
| Canonical name | `Character_ComputeTinkerSuccessChance_Inferred` |
| Prior scaffold | `Named_CalleeOf_Skill_You_do_not_possess_the_Tinkering_skill_to_00521f20` (**retired**) |

## Machine-level notes

- Source: raw capture + R10-035 live `disassemble_function` / `read_memory` / sole caller `FUN_0085f660`.
- Prefer assembly when decompiler conflicts (param roles, `extraout_DL`, compare labels).
- **ABI:** `__thiscall` ECX=**character**; stack **item***, **kit***; float **ST0**; **`RET 8`**.
- **Purpose:** success-chance fraction for tinker-apply UI (caller ×100 → percent string).
- Parent dual context: `Item_GetTinkerSkillOrCap_Inferred` (`0x00521eb0`) leaf callee; validate parent `Item_ValidateTinkerKitUse_Inferred` (`0x00513c10`) gates status==4 before this is called.

## Pseudocode (annotated; assembly-corrected roles)

```c
// ECX = character*
// stack+4 = item*   (vtbl+0x8c → open slot count)
// stack+8 = kit*    (int16 level @ +0xb0)
// return float ST0; RET 8
float __thiscall Character_ComputeTinkerSuccessChance_Inferred(
    void *character, void *item, void *kit)
{
  float base = 0.0f;
  unsigned need;
  int tier;
  int term;

  // Bypass: character+0x6b4 >= 1 → 100% (same flag validate uses to skip skill fail)
  if (*(int *)((char *)character + 0x6b4) >= 1) {
    return 1.0f;  // g_flOne @ 0x00a0f2a0
  }

  // item open-slot count via vfunc +0x8c (short in AX); need = slots + 1
  need = (unsigned)(unsigned char)(
      (**(int (***)())(*(int *)item + 0x8c))() + 1);

  // skill tier 0..5 from character+0x59b via FUN_00521eb0 (ECX=character)
  tier = (int)FUN_00521eb0(character);
  if (tier < (int)need) {
    return 0.0f;  // g_flZero @ 0x00a0f518
  }

  // base by need (opening next slot index): harder slots → lower base
  switch (need) {
  case 1: base = 0.4f;  break;  // DAT_00a0f720
  case 2: base = 0.35f; break;  // DAT_00a0f728
  case 3: base = 0.3f;  break;  // g_flOverheatCoolFrac @ 0x00a0f714
  case 4: base = 0.25f; break;  // DAT_00a0f704
  case 5: base = 0.2f;  break;  // DAT_009ceea8
  default: base = 0.0f; break;  // need outside 1..5
  }

  // DL still holds need after CALL 00521eb0 (decompiler "extraout_DL")
  // term = (int16)( character[+0x59b] - 2*need + kit[+0xb0] )
  term = (int)(short)(
      (unsigned short)*(unsigned char *)((char *)character + 0x59b)
      + (unsigned short)need * (unsigned short)-2
      + *(short *)((char *)kit + 0xb0));

  return (float)term * 0.04f /* DAT_00aaa8f4 */ + base;
}
```

## Fields

| Object | Offset | Width | Role |
|---|---|---|---|
| character | `+0x6b4` | i32 | ≥1 → return 1.0 (skill bypass / guaranteed) |
| character | `+0x59b` | u8 | raw rank byte in linear term; also source for `FUN_00521eb0` tier |
| item | vtbl `+0x8c` | vfunc | open slot count (short) → need = slots+1 |
| kit | `+0xb0` | i16 | kit level (same field validate uses vs need) |

## Float pool (`read_memory`)

| VA | Value | Use |
|---|---|---|
| `0x00a0f2a0` | 1.0 | bypass return |
| `0x00a0f518` | 0.0 | insufficient-tier return |
| `0x00a0f720` | 0.4 | base need=1 |
| `0x00a0f728` | 0.35 | base need=2 |
| `0x00a0f714` | 0.3 | base need=3 |
| `0x00a0f704` | 0.25 | base need=4 |
| `0x009ceea8` | 0.2 | base need=5 |
| `0x00aaa8f4` | 0.04 | linear scale |
| `0x00aaa7ac` | 100.0 | **caller only** percent scale |

## Call graph

```text
FUN_0085f660  (tinker apply UI; validate status==4)
  └─ FUN_00521f20  Character_ComputeTinkerSuccessChance_Inferred  [OWN R10-035]
        ├─ item->vtbl[+0x8c]()     open slots
        └─ FUN_00521eb0            skill tier (ECX=character this)
```

## Open questions

- Product English for `character+0x59b` / `character+0x6b4` / item vfunc `+0x8c`.
- Sibling dual `Item_GetTinkerSkillOrCap_Inferred` labels ECX as item; this call site passes **character** (aligns with `Item_ValidateTinkerKitUse` dual). Out of OWN scope to re-seal `0x00521eb0`.
- Runtime / bit-exact / differential (Terminal false).
