# Annotated low-level: GfxIndexBufferFactory_PreRestoreDevice_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00985580` |
| VA | `0x00985580`–`0x00985601` exclusive (**129 B**) |
| System | client / Palantir graphics — `gfxIndexBufferFactory` device-lost teardown |
| Date | 2026-08-04 W38-AC |
| Canonical name | `GfxIndexBufferFactory_PreRestoreDevice_Inferred` |
| Ghidra | `FUN_00985580` |

## Machine-level notes

- **ABI:** **EAX** = factory host; plain **`RET`** (`C3`). Not ECX-thiscall.
- **Map:** factory `+0x14` → red-black sentinel; walk uses `is_nil` @ `+0x21`.
- **Slot vector:** node `+0x14`/`+0x18` half-open, stride **`0x20`**; first dword = `gfxDeviceIB*`.
- **Gate:** skip when `(flags@node+0x0C & 0x28) != 0` (pool bits matching managed/system paths that skip DEFAULT-pool release).
- **Callee:** `FUN_00743c50` with **ESI** = device-IB* — unlock loop, COM Release (vtbl+8), null iface, stats decrement (`gfxDeviceIB.cpp`).
- **Caller:** sole — `GfxDevice_Reset` shutdown: `MOV EAX,[device_factory_slot]; CALL 0x00985580` between texture-factory pre and VB-factory pre.
- Prefer bytes over decompiler for ESI formal into `FUN_00743c50`.

## Pseudocode (annotated)

```c
// EAX = GfxIndexBufferFactory* factory
void GfxIndexBufferFactory_PreRestoreDevice_Inferred(void /* EAX=factory */)
{
  Node* sentinel = *(Node**)(factory + 0x14);
  for (Node* n = sentinel->leftmost /* *sentinel */; n != sentinel; n = tree_successor(n, is_nil@+0x21)) {
    if ((n->flags_byte /* +0x0C */ & 0x28) == 0) {
      for (uint8_t* e = n->vec_begin /* +0x14 */; e != n->vec_end /* +0x18 */; e += 0x20) {
        // ESI = *(GfxDeviceIB**)e
        gfxDeviceIB_ReleaseDeviceResources(*(GfxDeviceIB**)e); // FUN_00743c50
      }
    }
  }
}
```

## Open questions

- Exact product English for method (PreRestoreDevice vs OnLostDevice / ReleaseDeviceObjects) — **Inferred** from Reset cascade + PostRestore twin.
- Node key / payload English beyond IB factory map shape.
- Runtime / bit-exact.
