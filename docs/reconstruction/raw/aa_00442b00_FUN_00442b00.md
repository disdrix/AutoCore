# Raw capture: FUN_00442b00

| Field | Value |
|---|---|
| **Stable ID** | `aa_00442b00` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00442b00` |
| **Canonical name** | `FUN_00442b00` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00442b00(undefined4 *param_1,undefined4 *param_2)

{
  undefined4 *in_EAX;
  
  for (; param_1 != param_2; param_1 = param_1 + 6) {
    if (in_EAX != (undefined4 *)0x0) {
      *in_EAX = *param_1;
      in_EAX[1] = param_1[1];
      in_EAX[2] = param_1[2];
      in_EAX[3] = param_1[3];
      in_EAX[4] = param_1[4];
      in_EAX[5] = param_1[5];
    }
    in_EAX = in_EAX + 6;
  }
  return;
}
```

---

# APPEND 2026-07-29 W22-S live dual seal — do not alter body above

## Live re-decompile (Ghidra MCP `decompile_function` + `analyze_function_complete`, 2026-07-29)

```c
void __fastcall FUN_00442b00(undefined4 *param_1,undefined4 *param_2)

{
  undefined4 *in_EAX;
  
  for (; param_1 != param_2; param_1 = param_1 + 6) {
    if (in_EAX != (undefined4 *)0x0) {
      *in_EAX = *param_1;
      in_EAX[1] = param_1[1];
      in_EAX[2] = param_1[2];
      in_EAX[3] = param_1[3];
      in_EAX[4] = param_1[4];
      in_EAX[5] = param_1[5];
    }
    in_EAX = in_EAX + 6;
  }
  return;
}
```

Live decompile ≡ 2026-07-23 raw. Phantom `in_EAX` = dest cursor; ECX/EDX = [begin,end). Parent vector insert sites treat return as **dest end** (EAX after advances).

## Machine bytes (`read_memory` @ `0x00442b00`, length 128; body 63 B)

Hex (body through final `RET`):
```
3bca743a53565785c074268bf18b1e8bf8891f8b5e04895f048b5e08895f088b5e0c895f0c8b5e10895f108b761489771483c11883c0183bca75cc5f5e5bc3
```

Range: **`0x00442b00`–`0x00442b3e`** (63 B). Trailing `CC` pad; sibling entry at `0x00442b40` (stack-arg clone — **not** this unit).

## Byte-reconstructed control flow (≡ decompile + ABI fill)

```
// EAX = dest*, ECX = src begin*, EDX = src end*  (exclusive)
// element stride 0x18 (6 dwords)
// returns EAX = dest end (advanced by n*0x18)
cmp  ecx, edx
jz   empty_ret          ; ret without push (EAX unchanged = dest begin)
push ebx, esi, edi
loop:
  test eax, eax
  jz   advance          ; skip 6-dword copy when dest cursor null
  ; copy [ecx+0..0x14] -> [eax+0..0x14] via ebx/esi/edi temps
advance:
  add  ecx, 0x18
  add  eax, 0x18
  cmp  ecx, edx
  jnz  loop
  pop  edi, esi, ebx
empty_ret:
  ret                   ; EAX = dest end
```

## Callers (8 xrefs, 2 functions)

| Site | Parent |
|---|---|
| `0x004410b1`, `0x004410e0`, `0x0044118b`, `0x004411e8` | `FUN_00440f80` (SkillSet vector insert / reallocate, elem `0x18`) |
| `0x00442880`, `0x004428a6`, `0x0044294f`, `0x004429cc` | `FUN_00442760` (sibling SkillSet bulk insert) |

Parent uses `operator_new(n*0x18)`, max-size guard `0xaaaaaaa` (= `INT_MAX/0x18`), and `SkillSet_GetEntryCount` — seals role as **POD range copy / uninitialized_copy** for 0x18-byte skill entries.

## Callees

None (leaf).
