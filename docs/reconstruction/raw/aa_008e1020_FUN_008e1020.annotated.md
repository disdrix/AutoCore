# Annotated low-level: FUN_008e1020 / UI_DtHost_CreateChildWidgets_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_008e1020` |
| VA | `0x008e1020`–`0x008e17be` inclusive (**1951 B** / `0x79F`) |
| System | inventory-transfer — dt 2d host CreateChildWidgets* |
| Date | 2026-08-05 (R13-014 dual seal) |
| Ghidra name | `FUN_008e1020` |
| Canonical | `UI_DtHost_CreateChildWidgets_Inferred` |
| Decompile | **FAILED** (Overlapping input varnodes) — assembly authority |

## Machine-level notes

- Source: live Ghidra `disassemble_function` + `read_memory` + DATA xref (R13-014). Scaffold decompile error preserved as historical.
- Prefer assembly when decompiler conflicts (here: decompiler unavailable).
- Sibling family: craft host CreateChildWidgets `0x008e6b50` (`i_d_craft_2d_*`); trade `0x008847f0`.
- Parent dual (partition residual): `0x00756c90` UI_Widget_MoveToFrontInParentGroup_Inferred — called for collapsed crafting tab.

## Signature (sealed)

```c
// ECX = dt 2d host*; no stack args; void; RET 0
void __thiscall UI_DtHost_CreateChildWidgets_Inferred(void *host);
```

## ABI / prologue / epilogue

| Item | Evidence |
|---|---|
| this | `MOV EBP,ECX` @ `0x008e103c` |
| SEH | `PUSH -1; PUSH 0x9b72a8; FS:[0]` |
| Frame | `SUB ESP,0xC`; push EBX/EBP/ESI/EDI |
| Exit | `POP EDI/ESI/EBP/EBX; MOV FS:[0],ECX; ADD ESP,0x18; RET` (`C3`) |
| Stack args | **none** |

## Pseudocode (assembly-structural)

```c
void __thiscall UI_DtHost_CreateChildWidgets_Inferred(void *host)
{
    // SEH (0x009b72a8) omitted
    FUN_00792600(host); // residual prep / clear children

    // Pattern for most chrome:
    //   p = operator_new(size);
    //   if (p) p = Ctor(p[, 0]);
    //   host->vtbl[0xa8/4](p);          // attach
    //   p->vtbl[0x28/4](xml_path);      // load skin
    //   p->vtbl[0xfc/4](1, 0.5f);       // show + half alpha (most)
    //   *(void**)((char*)host + slot) = p;
    //   optional z-order bump: ++host+0x68; host+0x74=-(float)count; max host+0x70; host->vtbl[0x3f4/4](1)

    // +0x520  new(0x488)+007b5dd0  i_d_dt_2d_wnd_bg_fill.xml
    // +0x524  new(0x4BC)+00864f20  i_d_dt_2d_wnd_fx.xml
    // +0x540  new(0x488)+007b5dd0  i_d_dt_2d_wnd_frame_instructions.xml
    // +0x544  new(0x488)+007b5dd0  i_d_dt_2d_wnd_frame_instructions_bottom.xml
    // +0x548  new(0x488)+007b5dd0  i_d_dt_2d_wnd_frame_image.xml
    // +0x52c  new(0x488)+007b5dd0  i_d_dt_2d_wnd_bg_image.xml
    // +0x528  new(0x488)+007b5dd0  i_d_dt_2d_wnd_bg_texture.xml
    // +0x530  title XML then OVERWRITTEN by icon XML (same slot)
    // host.vtbl[+0x404](&tmp, "i_d_dt_2d_wnd_icon_cover.xml", -1)
    // +0x538  i_d_dt_2d_wnd_instructions.xml
    // +0x53c  i_d_dt_2d_wnd_instructions_bottom.xml
    // +0x504  new(0x4CC)+0079c860  i_d_dt_2d_btn_close.xml ; id 0x9c40
    // +0x54c  new(0x81C)+008230b0  tab_crafting ; id 0x9c41 ;
    //         UI_TabChrome_SetSelected_Inferred(force=1, BL=0);
    //         UI_Widget_MoveToFrontInParentGroup_Inferred(EDI=tab);
    // +0x550  new(0x81C)+008230b0  tab_disciplines ; id 0x9c42 ;
    //         UI_TabChrome_SetSelected_Inferred(force=1, BL=1);
    //         tab->vtbl[+0x94]();

    if (*(void**)0x00d1b8a8) {
        void *g = *(void**)0x00d1b8a8;
        if (g->vtbl[0x3d8/4]()) {
            void *tmp;
            void *t = g->vtbl[0x120/4](&tmp, 1, 0);
            host->vtbl[0x114/4](t);
            // EBX = 0x00d1a840 context
            FUN_007fca10();
        }
    }

    host->vtbl[0x448/4]();
    host->vtbl[0x34c/4]();
    // SEH restore; RET
}
```

## Call sites for dualed helpers

| Call site | Pattern |
|---|---|
| `0x008e1685` | `PUSH 1; XOR BL,BL; CALL 00822cb0` — crafting tab unselect |
| `0x008e168e` | `MOV EDI,[EBP+0x54c]; CALL 00756c90` — move crafting tab to front |
| `0x008e170e` | `PUSH 1; MOV BL,1; CALL 00822cb0` — disciplines tab select |

## Open questions

- Product / RTTI demangle for dt host class (English beyond `i_d_dt_2d_*`).
- Why `+0x530` title is overwritten by icon (bug vs intentional intermediate).
- Product meaning of host `vtbl+0x404` icon_cover path and `vtbl+0x448` post-build.
- Residual dual of `00792600`, tab ctor `008230b0`, close ctor `0079c860`, fx ctor `00864f20`.
- Runtime / bit-exact / differential.

## Confidence

| Claim | Level |
|---|---|
| thiscall RET0 + virtual DATA dispatch | **High** |
| `i_d_dt_2d_*` XML family + slot map | **High** |
| Tab pair crafting/disciplines + dualed helpers | **High** |
| Product class demangle | **Open** (`_Inferred`) |
