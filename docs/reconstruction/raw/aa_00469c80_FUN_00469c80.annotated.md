# Annotated low-level: StdVector_PushBack_Elem28_ValueEax_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00469c80` |
| VA | `0x00469c80`–`0x00469d04` exclusive (**132 B**) |
| Canonical | `StdVector_PushBack_Elem28_ValueEax_Inferred` |
| Ghidra | `FUN_00469c80` |
| System | MSVC `vector::push_back`, POD elem **0x1c** |
| Date | 2026-08-04 (W37-C) |

## Machine-level notes

- **ABI:** **ECX=`vec*`** (→ EDI), **EAX=`value*`** (→ EBX), bare **RET**. Not the RC push_back ABI (`00460f90`: EAX=vec, ECX=value).
- Vector layout: `+0x04 begin`, `+0x08 end`, `+0x0c cap_end`; elem stride **0x1c**; size/cap via magic `0x92492493`.
- **Fast path:** spare capacity → `StdUninitFillN_Elem28_CountEcx` with **count=1**, dest=end, tmpl=value; `end += 0x1c`.
- **Slow path:** `StdVector_InsertOne_RebindIt_Elem28_ValueEdx` at `where=end` (grows via insert-n); outIt stack slot discarded.
- Decompiler `FUN_0046a260(param_1)` is wrong on formals — **bytes** set ECX=1 / EAX=end / EBX=value; ignored stack push of vec.
- Prefer bytes over decompiler for register formals; CF of size/cap/end-advance matches decompile.

## Pseudocode (annotated)

```c
// ECX=vec*, EAX=value*; RET 0
void StdVector_PushBack_Elem28_ValueEax_Inferred(StdVectorElem28* vec /*ECX*/,
                                                   const Elem28* value /*EAX*/)
{
  Elem28* begin = vec->begin;           // +0x04
  uint32_t size = 0;
  if (begin) {
    size = (uint32_t)((vec->end - begin) / 0x1c);  // magic 0x92492493
  }
  if (begin && size < (uint32_t)((vec->cap_end - begin) / 0x1c)) {
    Elem28* dest = vec->end;
    StdUninitFillN_Elem28_CountEcx(/*ECX*/1, /*EAX*/dest, /*EBX*/value);
    vec->end = (Elem28*)((char*)dest + 0x1c);
    return;
  }
  Elem28* outIt; // stack slot from prologue push ecx
  StdVector_InsertOne_RebindIt_Elem28_ValueEdx(
      /*EDI*/vec, &outIt, vec->end, value); // RET 0xC
  (void)outIt;
}
```

## Open gaps

- Product / PDB T English for POD elem28.
- Concrete identity of caller containers (assPack residual parents not OWN).
- Runtime / bit-exact / differential (no Launcher).
