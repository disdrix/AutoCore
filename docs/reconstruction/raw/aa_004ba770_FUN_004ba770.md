# Raw capture: FUN_004ba770

| Field | Value |
|---|---|
| **Stable ID** | `aa_004ba770` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004ba770` |
| **Canonical name** | `FUN_004ba770` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_004ba770(void *param_1)



{

  if (*(char *)((int)param_1 + 0x15) == '\0') {

    FUN_004ba770(*(undefined4 *)((int)param_1 + 8));

                    /* WARNING: Subroutine does not return */

    operator_delete(param_1);

  }

  return;

}
```

---

## v2026-07-29 W20-N OWN-ONLY re-verify

| Field | Value |
|---|---|
| **Agent** | W20-N OWN-ONLY dual A/B |
| **Tools** | Ghidra `decompile_function` + `read_memory` + `analyze_function_complete` + `get_function_xrefs` (no `disassemble_bytes`) |
| **Program** | `autoassault.exe` base `0x400000` |
| **Body** | `0x004ba770`–`0x004ba7a4` |
| **Live decompile** | ≡ 2026-07-23 raw body (decompiler still collapses left-walk) |
| **Machine** | `read_memory` 64 B — ends `C2 04 00`; pad `CC` |

### Live decompile (2026-07-29)

```c
void FUN_004ba770(void *param_1)
{
  if (*(char *)((int)param_1 + 0x15) == '\0') {
    FUN_004ba770(*(undefined4 *)((int)param_1 + 8));
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return;
}
```

### Body bytes (`read_memory` @ `0x004ba770`, length 52)

```text
53 56 57                   push ebx, esi, edi
8B 7C 24 10                mov  edi, [esp+0x10]   ; node* stack arg
80 7F 15 00                cmp  byte [edi+0x15], 0  ; isnil
8B D9                      mov  ebx, ecx          ; thread ECX
8B F7                      mov  esi, edi
75 1E                      jnz  done              ; nil → return
                           ; loop:
8B 46 08                   mov  eax, [esi+8]      ; right
50                         push eax
8B CB                      mov  ecx, ebx
E8 E4 FF FF FF             call FUN_004ba770      ; free right subtree
8B 36                      mov  esi, [esi]        ; left = node[0]
57                         push edi
E8 8E F0 FC FF             call operator_delete
83 C4 04                   add  esp, 4
80 7E 15 00                cmp  byte [esi+0x15], 0
8B FE                      mov  edi, esi
74 E6                      jz   loop              ; non-nil left → continue
                           ; done:
5F 5E 5B                   pop  edi, esi, ebx
C2 04 00                   ret  4
```

### Byte-corrected CF (authoritative over decompiler)

1. Stack arg = node*; **`ret 4`**. ECX preserved/threaded on recurse (unused for walk).
2. If **isnil @ `+0x15`** → return.
3. Else: recurse free **right @ `+8`**, `operator_delete` current, **advance to left @ `+0`**, loop until isnil.
4. Decompiler WARNING "does not return" is **false** — `operator_delete` returns; left-walk continues.
5. Decompiler omits the **left-walk loop** (same residual family as `MissionStagingTree_DestroySubtree` / `SkillDefTree_DestroySubtree`).

### Xrefs

| From | Function | Type |
|---|---|---|
| `0x004bad0c` | `FUN_004bacf0` (`StdMap_EraseRange_Inferred`) | CALL |
| `0x004b8858` | `FUN_004b85d0` (`NDSpecialFX_ClearChildLists_Inferred`) | CALL |
| `0x004ba787` | self | CALL |

### Canonical (this dual)

`StdMap_FreeSubtree_Isnil15_Inferred`

