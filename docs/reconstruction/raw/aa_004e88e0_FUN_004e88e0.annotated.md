# Annotated low-level: Vec3_Copy (`aa_004e88e0`)

| Field | Value |
|---|---|
| Stable ID | `aa_004e88e0` |
| VA | `0x004e88e0` |
| Body | `0x004e88e0`–`0x004e8904` (37 bytes) |
| Canonical name | `Vec3_Copy` |
| Ghidra name | `FUN_004e88e0` |
| System | `math` / vector |
| Date | `2026-07-29` (dual A/B seal; prior scaffold 2026-07-23) |
| Tools | `decompile_function` + `read_memory` only |

---

## Machine-level notes

- **ABI:** `__cdecl` — `dst` at `[esp+4]`, `src` at `[esp+8]`, void return, bare `ret`.
- **ISA:** three `movss` loads from `src`, three `movss` stores to `dst` (SSE scalar float).
- **Width:** exactly **3** components (12 bytes). Does **not** touch `src/dst + 0xC` (W of float4).
- Decompiler shows `undefined4` integer temps; runtime path is float bits via `movss` (bit-identical for IEEE float values).
- Sibling context (not owned): `FUN_004e8830` copies **4** DWORDs (float4); this unit is the **3-component** sibling.

## Byte-accurate pseudocode

```c
/* Vec3_Copy — copy float XYZ (3×32-bit) from src to dst.
   __cdecl: void Vec3_Copy(float *dst, float const *src); */

void __cdecl Vec3_Copy(float *dst, float const *src)
{
  float x;
  float y;
  float z;

  /* movss load order matches bytes */
  x = src[0];
  y = src[1];
  z = src[2];
  dst[0] = x;
  dst[1] = y;
  dst[2] = z;
  return;
}
```

## Ghidra raw (authoritative for CF)

```c
void FUN_004e88e0(undefined4 *param_1,undefined4 *param_2)
{
  undefined4 uVar1;
  undefined4 uVar2;
  
  uVar1 = param_2[1];
  uVar2 = param_2[2];
  *param_1 = *param_2;
  param_1[1] = uVar1;
  param_1[2] = uVar2;
  return;
}
```

## Open questions

- Product/PDB English name (none in body) — `Vec3_Copy` is role-sealed INFERRED name.
- Whether any caller relies on non-overlap of `dst`/`src` (no memmove; overlapping regions are undefined if partial overlap, though full-overlap self-copy is fine via register temps).
