# Raw capture: FUN_0073d870

| Field | Value |
|---|---|
| **Stable ID** | `aa_0073d870` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0073d870` |
| **Canonical name** | `FUN_0073d870` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_0073d870(void)



{

  int *piVar1;

  int *piVar2;

  int iVar3;

  int in_EAX;

  int *piVar4;

  int iVar5;

  

  piVar1 = *(int **)(in_EAX + 8);

  for (piVar4 = *(int **)(in_EAX + 4); piVar4 != piVar1; piVar4 = piVar4 + 5) {

    if (*(int *)(*piVar4 + 0xc) != 0) {

      piVar2 = *(int **)(*piVar4 + 0xc);

      (**(code **)(*piVar2 + 0x118))(piVar2);

    }

    iVar3 = piVar4[3];

    for (iVar5 = piVar4[2]; iVar5 != iVar3; iVar5 = iVar5 + 4) {

      FUN_007525f0();

    }

  }

  return 0;

}
```

---

## W38-V re-verify (2026-08-04)

| Field | Value |
|---|---|
| **Tools** | `batch_decompile` / `decompile_function` + `read_memory` + `analyze_function_complete` + callers/xrefs + `get_assembly_context` + callee decompile. **No** `disassemble_bytes`. |
| **Body** | `0x0073d870`–`0x0073d8c2` exclusive (**82 B** / `0x52`); pad `CC`×14 then `FUN_0073d8d0` @ `0x0073d8d0` |
| **ABI** | **EAX = effect-factory host\*** (register formal); no stack args; plain **`RET`**; returns **0** (`XOR EAX,EAX`) |
| **Caller** | sole `FUN_0075eff0` @ `0x0075f2f6` — `gfxDevice.cpp` Device Reset; `MOV EAX,[EBP+0x764]` then `CALL`; fail string **"Failed to Reset() device on Effect Factory"** (line `0x242`) |
| **Callees** | `FUN_007525f0` (inner param walk; EDI=param entry) |
| **Decompile ≡ bytes** | CF match; `piVar4+5` ≡ stride **0x14**; always-return-0 sealed |

### Full body hex (82 B)

```text
51538b5804578b78083bdf897c2408743b55568b0383780c00740c8b400c8b0850ff91180100008b73088b6b0c3bf574128b3ee8484d010083c6043bf575f28b7c241083c3143bdf75c95e5d5f33c05b59c3
```

### Byte-level CF (authority)

```text
PUSH ECX; PUSH EBX
MOV EBX,[EAX+4]          ; vec begin
PUSH EDI
MOV EDI,[EAX+8]          ; vec end
CMP EBX,EDI / MOV [ESP+8],EDI / JZ done
PUSH EBP; PUSH ESI
loop_entry:
  MOV EAX,[EBX]          ; *entry
  CMP DWORD [EAX+0xC],0
  JZ skip_vcall
    MOV EAX,[EAX+0xC]
    MOV ECX,[EAX]        ; vtbl
    PUSH EAX
    CALL [ECX+0x118]     ; OnReset-style on child
  skip_vcall:
  MOV ESI,[EBX+8]; MOV EBP,[EBX+0xC]
  CMP ESI,EBP / JZ next
  inner:
    MOV EDI,[ESI]
    CALL FUN_007525f0    ; EDI formal
    ADD ESI,4
    CMP ESI,EBP / JNZ inner
  next:
  MOV EDI,[ESP+0x10]     ; restore end
  ADD EBX,0x14
  CMP EBX,EDI / JNZ loop_entry
POP ESI; POP EBP; POP EDI
XOR EAX,EAX
POP EBX; POP ECX
RET
```

### Host / entry layout (sealed by this body + siblings)

| Offset | Role |
|---|---|
| host `+4` | entry* begin |
| host `+8` | entry* end |
| entry stride | **0x14** (5 dwords) |
| entry `+0` | object* (`obj+0xC` optional child) |
| entry `+8` / `+0xC` | param* begin / end (stride 4) |

### Role

**Effect Factory post-`IDirect3DDevice9::Reset` recreate walk** — pair of `FUN_0073d8d0` (pre-reset release). Always returns success `0`; caller still checks `< 0`.
