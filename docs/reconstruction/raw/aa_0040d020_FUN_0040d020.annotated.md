# Annotated low-level: FUN_0040d020 → Math_Vec3Length_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0040d020` |
| VA | `0x0040d020` |
| System | interaction-activation (partition) / **shared pure math leaf** |
| Date | 2026-08-05 (MEGA-137 dual refresh) |
| Named form | `Math_Vec3Length_Inferred` |

---

## Machine-level notes

- **Leaf:** no `CALL`; only x87 `FLD`/`FMUL`/`FADDP`/`FSQRT`/`RET`.
- **ABI:** `__cdecl`, three `float` stack args, return length in **ST0**, plain `RET`.
- **Body:** `0x0040d020`–`0x0040d03e` (31 B); `CC` pad then unrelated `FUN_0040d040`.
- Prefer assembly when decompiler signature display says `void`/`undefined` — storage + disasm seal three floats + ST0.
- Callers use result as **Euclidean distance** of a 3-vector delta (pick hit − camera; path node deltas). Pick multiplies by `_DAT_00aaacbc` (= **0.015f**) **outside** this leaf.

## Pseudocode (annotated)

```c
// __cdecl — stack: x@[esp+4], y@[esp+8], z@[esp+0xc]; return length in ST0
// Ghidra: FUN_0040d020
// Named:  Math_Vec3Length_Inferred
float10 Math_Vec3Length_Inferred(float x, float y, float z)
{
  // FLD x; FMUL x; FLD y; FMUL y; FADDP; FLD z; FMUL z; FADDP; FSQRT; RET
  return SQRT((float10)z * (float10)z +
              (float10)y * (float10)y +
              (float10)x * (float10)x);
}
```

## Call graph (OWN VA only)

```text
Math_Vec3Length_Inferred  (0x0040d020)  [leaf]
  ↑ UNCONDITIONAL_CALL ×3
  ├─ Client_InteractClickPickTarget  0x009247b0  @ 0x009249c0, 0x00924c76
  └─ FUN_005ba440                    0x005ba440  @ 0x005bad63
```

## Open questions

- Product / PDB name (inferred `Math_Vec3Length_Inferred` only).
- NaN / −0 / denormal edge English (static only; `FSQRT` IEEE path).
- Residual caller `FUN_005ba440` product role (out of OWN).
- Runtime / differential harness.
