# Annotated low-level: ClientSpecialEvent_Respawn_TeardownAirlift (`FUN_00979290`)

| Field | Value |
|---|---|
| Stable ID | `aa_00979290` |
| VA | `0x00979290`–`0x009792ff` (112 B / `0x70`) |
| Canonical name | `ClientSpecialEvent_Respawn_TeardownAirlift` |
| Ghidra name | `FUN_00979290` |
| System | client special-event / respawn (INC airlift) |
| Date | 2026-07-29 (W23-I dual seal) |

## Machine-level notes

- Source: raw capture + live W23-I `decompile_function` / `read_memory` / callers.
- **Role:** Shared **airlift teardown** for `ClientSpecialEvent_Respawn` — detach/hide resolved entity side-effects, restore mode dword, refresh phys helper, optional re-enter world, clear active latch.
- **ABI:** **EDI = event this** (same convention as sealed `ClientSpecialEvent_Respawn_SetupAirlift` @ `0x00979310`); no stack formals; plain `ret` (`C3`).
- **Gate:** `*(char*)(this+0x38) != 0` — full-sequence / airlift-active latch (same field as Update phase-2 early complete). Always cleared to `0` when gate was open (even if resolve fails).
- **Resolve:** `mov ecx, [DAT_00b04830]`; `push this+0x28` (TFID blob); `call FUN_004bb010` — sealed `Object_ResolveTfIdVtbl1d4_Inferred`.
- **On non-null resolve:**
  1. MI-adjusted `vtbl+0x1bc(0)` — virtual with arg `0` (detach/hide/cancel visual; product slot name open).
  2. `*(dword*)(*(resolved+8) + 0x28) = *(dword*)(this+0x68)` — restore entity mode/flags from event snapshot (SetupAirlift forces mode `0x10` path).
  3. `FUN_00560e90` on phys/body at `resolved+8` (stack byte `1` staged; callee body sets nested flag).
  4. If `*(char*)(this+4) == 0`: **`Vehicle_ActivateEnterWorld`** with **ECX = resolved** (decomp drops thiscall ECX).
- **Callers:** `ClientSpecialEvent_Respawn_Update` phase 4→5 (after clearing lift-follow `+0x6c`); `ClientSpecialEvent_Respawn_dtor` after local-player camera restore branch.
- **Not** SetupAirlift (`00979310`), not camera restore (`0090dd50`), not ship geo alloc path.

## Recovered CF (byte-aligned)

```c
// EDI = ClientSpecialEvent_Respawn* this
void ClientSpecialEvent_Respawn_TeardownAirlift(void /*EDI this*/)
{
    if (*(char *)(this + 0x38) == 0)
        return;

    // ECX = *(DAT_00b04830) resolve context
    void *obj = Object_ResolveTfIdVtbl1d4_Inferred(ctx, this + 0x28);
    if (obj != 0) {
        // MI vtbl +0x1bc
        obj->vtbl[0x1bc/4](0);
        void *phys = *(void **)((char *)obj + 8);
        *(uint32_t *)((char *)phys + 0x28) = *(uint32_t *)(this + 0x68);
        FUN_00560e90(phys /*, flag 1 staged on stack */);
        if (*(char *)(this + 4) == 0)
            Vehicle_ActivateEnterWorld(obj); // ECX = obj
    }
    *(char *)(this + 0x38) = 0;
}
```

## Call graph (OWN context)

| Direction | VA | Name |
|---|---|---|
| Caller | `0x00979730` | `ClientSpecialEvent_Respawn_Update` (phase 4→5) |
| Caller | `0x00979c70` | `ClientSpecialEvent_Respawn_dtor` |
| Callee | `0x004bb010` | `Object_ResolveTfIdVtbl1d4_Inferred` |
| Callee | `0x00560e90` | phys refresh / nested flag set |
| Callee | `0x00503f30` | `Vehicle_ActivateEnterWorld` |
| Sibling setup | `0x00979310` | `ClientSpecialEvent_Respawn_SetupAirlift` |

## Layout fields touched

| Offset | Role |
|---:|---|
| `+0x04` | `char` — suppress `Vehicle_ActivateEnterWorld` when non-zero |
| `+0x28` | TFID16 blob head for controlled entity |
| `+0x38` | `char` airlift/full-sequence active latch (cleared) |
| `+0x68` | `dword` snapshot restored onto phys/entity `+0x28` |

## Open questions

1. Product name for vtbl slot `+0x1bc` and exact mode dword at entity/phys `+0x28`.
2. Product meaning of event `+0x04` (when Update/dtor set it).
3. Whether `FUN_00560e90` second staged byte is a formal or MSVC stack hole (callee dual residual).
4. Runtime / bit-exact — open.
