# Annotated low-level: StdPairKey_Less_HiSignedLoUnsigned_Inferred (FUN_00401480)

| Field | Value |
|---|---|
| Stable ID | `aa_00401480` |
| VA | `0x00401480` |
| System | std pair-key less-than for map insert-hint / ordered maps |
| Date | 2026-08-04 (WQ9G-B dual seal) |
| Ghidra | `FUN_00401480` |
| Named | `StdPairKey_Less_HiSignedLoUnsigned_Inferred` |

## Machine-level notes

- **Role:** strict **less-than** on a **2-dword key**: primary **hi** @+4 **signed**, secondary **lo** @+0 **unsigned**.
- **ABI:** two stack pointers; **`ret 8`** (`C2 08 00`); returns **1** if a < b else **0** in EAX. Leaf.
- Used by dualed `StdTree_InsertHint_Isnil29_Inferred` (`0x004cc220`) and peer hint `FUN_00406560` against node key @ **+0x10/+0x14** (lo/hi) and value pair @+0/+4.
- Matches pair-key insert-or-find order in `StdMap_InsertOrFind_PairKey_Isnil29_Inferred` (hi signed primary, lo unsigned secondary) — this unit is the **shared leaf comparator**.
- Scaffold `Named_CalleeOf_…_VOG_DEBUG_ST_00401480` — **reject** as product English.

## Algebra (sealed by bytes)

```
return (a.hi < b.hi) || (a.hi == b.hi && a.lo < b.lo)
```

with:

- hi: **signed** (`JG`/`JL` after `CMP`)
- lo: **unsigned** (`JAE` after `CMP`)

Equivalent decompiler form:

```c
if (((int)a[1] <= (int)b[1]) &&
    (((int)a[1] < (int)b[1]) || (*a < *b)))
  return 1;
return 0;
```

## Pseudocode (annotated)

```c
// stack a*, b*; RET 8; AL/EAX = 0|1
uint32_t StdPairKey_Less_HiSignedLoUnsigned_Inferred(
    const uint32_t* a, const uint32_t* b)
{
  int32_t aHi = (int32_t)a[1];
  int32_t bHi = (int32_t)b[1];
  if (aHi > bHi) return 0;
  if (aHi < bHi) return 1;
  // hi equal: lo unsigned
  if (a[0] < b[0]) return 1;
  return 0;
}
```

## Callers (static)

| Caller | Sites | Role |
|---|---|---|
| `FUN_004cc220` `StdTree_InsertHint_Isnil29_Inferred` | 5 | pair-key hint insert |
| `FUN_00406560` | 5 | insert-hint peer (isnil@+0x21 attachment checks) |

## Open / residual

- Product pair type English (TFID vs custom 2-dword).
- Full host inventory beyond the two hint shells.
- Runtime / bit-exact / differential.
