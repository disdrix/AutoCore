# Annotated low-level: FUN_00457cc0 → StdTree_IteratorIncrement_Isnil19

| Field | Value |
|---|---|
| Stable ID | `aa_00457cc0` |
| VA | `0x00457cc0`–`0x00457d0b` exclusive (**75 B**) |
| System | shared `client::stdtree` |
| Date | 2026-07-23 scaffold; **2026-08-04 W38-B dual seal** |

## Machine-level notes

- Source: raw capture + live re-verify (`decompile_function` + `read_memory`).
- **No** `disassemble_bytes`.
- Entry bytes: `8B 02` = `MOV EAX,[EDX]` → iterator lives in **EDX**, not ECX.
- Call sites: `LEA EDX,[ESP+…]` then `CALL 0x00457cc0` (e.g. `0x004473b0`, `0x00971114`, `0x0040b4a0`, `0x0097160a`).
- Every nil guard uses displacement **`0x19`** (`80 xx 19 00`).
- Links: left `+0`, parent `+4`, right `+8` (MSVC `_Tree_node` header).
- Twin family: `StdTree_IteratorIncrement_Inferred` (`+0x29`), `StdTree_IteratorIncrement_Isnil49` (`+0x49`); this unit is **isnil@+0x19** (Val12 / CategoryCooldown-class nodes).
- Not predecessor: walk is right → leftmost, or climb while **right**-child.

## Pseudocode (annotated)

```c
// ABI: EDX = node**; void; plain RET
// Ghidra shows __fastcall (unused ECX, it in EDX)
void StdTree_IteratorIncrement_Isnil19(int **it /*EDX*/)
{
  int *node = *it;
  // already end()/nil → no-op
  if (*(char *)((int)node + 0x19) == '\0') {
    int *right = *(int **)((int)node + 8);
    if (*(char *)((int)right + 0x19) == '\0') {
      // successor = leftmost of right subtree
      int *cur = right;
      int *left = (int *)*right;
      while (*(char *)((int)left + 0x19) == '\0') {
        cur = left;
        left = (int *)*left;  // +0 left link
      }
      *it = cur;
      return;
    }
    // climb while current was right child of parent
    int *parent = *(int **)((int)node + 4);
    while (*(char *)((int)parent + 0x19) == '\0'
           && *it == *(int **)((int)parent + 8)) {
      *it = parent;
      parent = *(int **)((int)parent + 4);
    }
    *it = parent;
  }
}
```

## Open questions

- Product map key/value English per caller (shared helper).
- Color bit offset unused by this unit (sibling ctors).
- Runtime / bit-exact / differential.
