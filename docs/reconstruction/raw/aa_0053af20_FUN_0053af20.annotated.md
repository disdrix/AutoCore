# Annotated low-level: FUN_0053af20 → StdTree_OperatorIndex_Val12

| Field | Value |
|---|---|
| Stable ID | `aa_0053af20` |
| VA | `0x0053af20` |
| System | std map operator[] / get-or-insert (Val12) |
| Date | 2026-07-29 (W29-Q OWN-ONLY dual) |
| Body | `0x0053af20`–`0x0053af91` exclusive (**113** B) |
| ABI | `__thiscall`; ECX=map; stack `(int* key)`; `ret 4`; returns `mapped*` @ node+0x10 |

## Machine-level notes

- Source: raw capture + live Ghidra decompile/read_memory (no `disassemble_bytes`).
- Val12 family: isnil@+0x19, key@+0x0c, mapped@+0x10/+0x14 (same as InsertHint W28-Q).
- Sole callee: `FUN_0053a8e0` StdTree_InsertHint_Val12.
- Call sites write two dwords into returned mapped pointer (mission counters / create-from-pack).

## Pseudocode (annotated)

```c
// __thiscall  this = MapShell_Val12*
// returns pointer to mapped pair (int val1, int val2) at node+0x10
int32_t* StdTree_OperatorIndex_Val12(MapShell_Val12* map, int* key)
{
    MapNode_Val12* head = map->head;          // *(this+4)
    MapNode_Val12* candidate = head;
    MapNode_Val12* node = head->left;         // root

    if (node->isnil == 0) {
        // lower_bound: first node with key >= *key
        do {
            if (node->key < *key)
                node = node->right;
            else {
                candidate = node;
                node = node->left;
            }
        } while (node->isnil == 0);
    }

    // Hit: candidate in-tree and candidate.key <= *key  → equality
    if (candidate != head && candidate->key <= *key)
        return &candidate->val1;              // node+0x10

    // Miss: insert Val12{*key, 0, 0} with hint=candidate
    Val12 v = { *key, 0, 0 };
    MapNode_Val12* outIt;
    StdTree_InsertHint_Val12(map, &outIt, candidate, &v);
    return &outIt->val1;                      // *outIt + 0x10
}
```

## Call graph

| Direction | Symbol |
|---|---|
| Callee | `FUN_0053a8e0` StdTree_InsertHint_Val12 |
| Callers | `FUN_005305b0`, `FUN_00530610`, `CVOGCharacter_ApplyCreateFromPacket` |

## Seal confidence

| Claim | Level |
|---|---|
| CF + ABI ret 4 thiscall | High |
| lower_bound + equality hit | High |
| Miss → InsertHint Val12 zeros | High |
| Return mapped @ +0x10 | High |
| Product map demangle | Open |
