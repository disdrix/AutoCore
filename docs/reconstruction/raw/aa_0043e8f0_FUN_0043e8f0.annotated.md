# Annotated low-level: FUN_0043e8f0 → PodU32U8_StoreByValue

| Field | Value |
|---|---|
| Stable ID | `aa_0043e8f0` |
| VA | `0x0043e8f0`–`0x0043e902` (**19 B**) |
| System | POD store adapter (by-value → split store) |
| Date | 2026-07-29 (W33-M seal) |
| Canonical | `PodU32U8_StoreByValue` (**Inferred**) |

## Machine-level notes

- Source: raw capture + W33-M live `read_memory` / decompile / listing.
- **Bytes win** over decompiler `void FUN_0043e8f0(void)` plate.
- Nested leaf `FUN_0043ea50` sealed W32-M as `PodU32U8_StoreSplit`.

## Sealed ABI

```
// ESI = dest (nullable not checked here; StoreSplit also has no null gate)
// stack [ESP+4] = uint32_t value (by value; address taken)
// stack [ESP+8] = uint8_t  value (by value in dword slot; address taken)
// bare RET (cdecl); returns dest in EAX
// Callers clean 8 bytes (ADD ESP,8) after CALL.
```

## Pseudocode (bytes-faithful)

```c
void* PodU32U8_StoreByValue(void* dest /*ESI*/,
                            uint32_t u32_val /*stack*/,
                            uint32_t u8_val  /*stack; low byte used*/)
{
  // LEA EAX, &u8_val; PUSH EAX
  // LEA ECX, &u32_val
  // MOV EAX, ESI
  PodU32U8_StoreSplit(dest, &u32_val, &u8_val); // RET 4 cleans pU8 push
  return dest; // MOV EAX,ESI; RET
}
```

## Call graph

| Direction | Targets |
|---|---|
| Callee | `PodU32U8_StoreSplit` (`0x0043ea50`) |
| Callers | NDResourceCache_LookupOrCreate; FUN_00970b20/60; FUN_0075de80 (×4); FUN_0075d3c0; FUN_00970e50 |

## Open residual

- Product/PDB English for the u32+u8 pair fields.
- Runtime / bit-exact under resource-cache / ass rings.
