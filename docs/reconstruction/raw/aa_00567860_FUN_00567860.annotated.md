# Annotated: aa_00567860 StdMap_InsertAlways_Val12_IntKey

| Field | Value |
|---|---|
| **Stable ID** | `aa_00567860` |
| **VA** | `0x00567860` |
| **Canonical** | `StdMap_InsertAlways_Val12_IntKey` (Inferred) |
| **Ghidra** | `FUN_00567860` |
| **Date** | 2026-07-29 (W29-M) |

## Role

Walk Val12 RB tree by **signed int key** (`value[0]` vs `node+0x0c`), then **always insert** via `Map_TreeInsertAndRebalance_Val12` (`FUN_00567450`, W27-R). Always writes out-pair `{it, inserted=1}`.

**Not** insert-or-find: no equal-key short-circuit, no `inserted=0` path. Equal keys walk **right** and still insert (multimap-style / caller-unique).

Contrast peers:

| VA | Role |
|---|---|
| `0x0053a1a0` | StdMap_InsertOrFindByIntKey_Val12 (W28-P) — equal → `{it,0}` |
| `0x00573810` | StdMap_InsertOrFind_Val12_UintKey (W28-T) — uint, equal no-write |
| `0x00567860` | **this** — always insert int key |

## Algorithm

```
head = map.head
parent = head; addLeft = true
if root is non-nil:
  n = root
  while n not isnil:
    parent = n
    addLeft = (key < (int)parent.key)   // signed
    n = addLeft ? parent.left : parent.right
node = InsertAndRebalance(map, &value, addLeft, parent, value)  // always
out.it = node
out.inserted = 1
// RET 8
```

## Layouts

```
MapShell { head@+4, size@+8 }
Node_Val12 {
  left@0, parent@4, right@8,
  value dwords @+0x0c (key = dword0),
  color@+0x18, isnil@+0x19
}
InsertPair { Node* it@+0; uint8_t inserted@+4 }
```

## ABI

- `__thiscall` ECX=map
- stack `(InsertPair* out, int* value)` — value is Val12 payload (key + 2 dwords)
- **RET 8** (`C2 08 00`)
- Body `0x00567860`–`0x005678c0` exclusive (**96** B)

## Call graph

| Dir | Symbol | Note |
|---|---|---|
| callee | `FUN_00567450` | Map_TreeInsertAndRebalance_Val12 (W27-R); sole parent |
| caller | `FUN_00565950` | 2 xrefs (`0056641a`, `00566452`) — spawn/unpack path |

## Confidence

| Claim | Level |
|---|---|
| Always-insert CF | **High** |
| Signed int key @ +0x0c | **High** |
| isnil@+0x19 Val12 | **High** |
| `ret 8` + out pair | **High** |
| Product map type / demangle | **Inferred** |
| Runtime / bit-exact | Open |
