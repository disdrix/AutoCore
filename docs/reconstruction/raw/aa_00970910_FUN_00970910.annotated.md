# Annotated low-level: FUN_00970910 / AssPreloader_ExtBucketFromPath_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00970910` |
| VA | `0x00970910`–`0x00970a24` exclusive (**276 B**) |
| System | AssPreloader / asset path bucket |
| Date | 2026-08-04 (W38-C) |
| Canonical | `AssPreloader_ExtBucketFromPath_Inferred` |

## Machine-level notes

- **ABI:** stack `(char** name_holder)`; return `int` 0..4; **`ret 4`**.
- **Once-init** table of five MSVC `basic_string` @ `DAT_00d1f7f0` stride `0x1c`: `.geo`, `.dds`, `.ogg`, `.xml`, `OTHER`; guard `DAT_00d1f87c` bit0; `atexit(LAB_009c3320)`.
- **Classify:** walk first **four** extensions with `strstr(haystack, ext)`; haystack = `*name_holder` or empty string if null.
- **Default bucket 4** when no match — `"OTHER"` string is constructed for table/atexit symmetry but **not** searched (loop bound stops before `DAT_00d1f864`).
- **SSO:** capacity@`+0x14` > 0xf → heap c_str else buffer at string+0 (decompiler shows +4 field start `DAT_00d1f7f4`).
- **Callers:** AssPreloader tally family uses return as index into `host+0xe8` (bytes) and `host+0xfc` (counts) — 5 slots.
- Prefer bytes for RET/ABI; decompile CF matches.

## Recovered CF

```c
// stack arg; ret 4
int AssPreloader_ExtBucketFromPath_Inferred(const char **name_holder)
{
  // once: build static basic_string table[.geo,.dds,.ogg,.xml,OTHER]
  const char *hay = *name_holder;
  if (hay == NULL) hay = /* empty */ *(char**)0x00afa2bc;
  for (int i = 0; i < 4; ++i) {
    const char *ext = /* c_str of table[i] */;
    if (strstr(hay, ext) != NULL)
      return i; // 0=.geo 1=.dds 2=.ogg 3=.xml
  }
  return 4; // OTHER
}
```

## Open questions

- Product demangle / exact class method name.
- Whether substring (not suffix) `strstr` is intentional (e.g. matches mid-path).
- Case sensitivity (CRT `strstr` is case-sensitive).
- Runtime / bit-exact.
