# Annotated low-level: SoftCastHitList_QuickSortRange_KeyFloat14_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00489f20` |
| VA | `0x00489f20`–`0x0048a05d` inclusive (**318 B** / `0x13E`) |
| Ghidra | `FUN_00489f20` |
| Canonical | `SoftCastHitList_QuickSortRange_KeyFloat14_Inferred` |
| System | interaction-activation (soft-cast hit list) |
| Date | 2026-08-05 (MEGA-115 OWN dual) |
| Parent dual | `SoftCastHitList_SortIfCountGt1_Inferred` (`0x0048a060`, MEGA-116) |

## Machine-level notes

- **ABI:** **cdecl** — 4 stack args; bare **`RET`** (`C3`); caller `ADD ESP,0x10`.
- **Stack:** `[EBP+8]=array_base`, `[EBP+0xC]=lo`, `[EBP+0x10]=hi`, `[EBP+0x14]=cookie` (unused in body).
- **Frame:** `AND ESP,0xFFFFFFF0; SUB ESP,0x64` — 16-byte aligned locals for FPU pivot temp.
- **Stride:** `LEA r,[idx+idx*2]; SHL r,4` → **elem size 0x30**.
- **Key:** `FLD float ptr [elem+0x14]` mid-index pivot; ascending strict `<`.
- **Swap:** dwords at `+0x00,+0x04,+0x08,+0x0C,+0x10,+0x14,+0x20` only (partial element exchange).
- **CF:** classic Hoare partition; recurse left when `lo < j`; set `lo = i` and loop for right (tail-call elimination).
- Prefer assembly when decompiler flattens FPU compare (`TEST AH,0x5` / `JP`).

## Pseudocode (annotated)

```c
// cdecl; void; bare RET
// SoftCastHitList_QuickSortRange_KeyFloat14_Inferred
void FUN_00489f20(void *base /*+0*/, int lo /*+4*/, int hi /*+8*/,
                  uint32_t cookie /*+0xC unused*/)
{
  // outer: process [lo, hi] until empty
  do {
    // pivot = base[(lo+hi)>>1].key_f32  (+0x14)
    float pivot = *(float *)((char *)base + (((lo + hi) >> 1) * 0x30) + 0x14);
    int j = hi;
    int i = lo;
    do {
      // while base[i].key < pivot: i++
      for (float *p = (float *)((char *)base + i * 0x30 + 0x14);
           *p < pivot;
           p = (float *)((char *)p + 0x30)) {
        i++;
      }
      // while pivot < base[j].key: j--
      for (float *p = (float *)((char *)base + j * 0x30 + 0x14);
           pivot < *p;
           p = (float *)((char *)p - 0x30)) {
        j--;
      }
      if (j < i) break;
      if (j != i) {
        // partial swap of 0x30-slot: +0,+4,+8,+c,+10,+14,+20
        // (does NOT touch +0x18,+0x1C,+0x24,+0x28,+0x2C)
        /* ... see clean source ... */
      }
      j--;
      i++;
    } while (i <= j);
    if (lo < j) {
      FUN_00489f20(base, lo, j, cookie);   // recurse left
    }
    lo = i;                                // tail right
    if (hi <= i) return;
  } while (true);
}
```

## Callers / callees

| Role | Symbol | Note |
|---|---|---|
| External caller | `SoftCastHitList_SortIfCountGt1_Inferred` `0x0048a060` | `base=this+0x10`, `lo=0`, `hi=count-1`, cookie=`this&0xFFFFFF00` |
| Self | recurse @ `0x0048a040` | left partition only |
| Callees | self only | leaf of external graph |

## Open questions

- Product English for float@+0x14 (distance / score / priority).
- Semantic identity of unswapped dwords at +0x18..+0x1C and +0x24..+0x2C.
- Runtime / bit-exact / differential.
