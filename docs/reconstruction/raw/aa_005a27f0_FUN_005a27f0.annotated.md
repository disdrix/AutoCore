# Annotated low-level: FUN_005a27f0 → StdTree_Rrotate_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005a27f0` |
| VA | `0x005a27f0`–`0x005a2841` inclusive (**82 B**) |
| System | MSVC std `_Tree` right-rotate (isnil@+0x21 / color@+0x20) |
| Date | 2026-08-05 (R11-007 OWN dual) |
| Ghidra | `FUN_005a27f0` |
| Promoted | `StdTree_Rrotate_Isnil21_Inferred` |

## Machine-level notes

- Live re-verify 2026-08-05: decompile ≡ raw CF; leaf; **RET 4** ×3; isnil test `80 7E 21 00`.
- Node layout: **+0 left**, **+4 parent**, **+8 right**, **+0x20 color**, **+0x21 isnil**.
- Tree shell: **this+4 = head**; **head+4 = root**.
- Classical `_Rrotate`: `y = x->left`; rehang `y->right` under `x`; promote `y` into `x`'s parent slot; hang `x` as `y->right`.
- Twin Lrotate `FUN_0050e9f0` / `StdTree_Lrotate_Isnil21_Inferred` (R10-030 dualed).
- Peer dualed: `StdTree_Rrotate_Isnil29_Inferred` `0x004192f0` (same CF; isnil imm `0x29` vs `0x21`); also isnil2D `0042a840` / isnil31 `00403ee0`.
- Shared helper — **not** skill/reaction-specific despite old auto alias chain (`Named_CalleeOf_…_CVOGReaction_…`).

## Pseudocode (annotated)

```c
// __thiscall; ECX = tree*; stack = Node* x; void; RET 4
void __thiscall StdTree_Rrotate_Isnil21_Inferred(int tree, int *x)
{
  int y;          // x->left (promoted)
  int *parent;

  y = *x;                                   // y = x->left
  *x = *(int *)(y + 8);                     // x->left = y->right
  if (*(char *)(*(int *)(y + 8) + 0x21) == '\0') {  // if y->right not isnil
    *(int **)(*(int *)(y + 8) + 4) = x;     //   y->right->parent = x
  }
  *(int *)(y + 4) = x[1];                   // y->parent = x->parent
  if (x == *(int **)(*(int *)(tree + 4) + 4)) {
    // x was root
    *(int *)(*(int *)(tree + 4) + 4) = y;   // root = y
    *(int **)(y + 8) = x;                   // y->right = x
    x[1] = y;                               // x->parent = y
    return;                                 // ret 4
  }
  parent = (int *)x[1];
  if (x == (int *)parent[2]) {
    // x was right child
    parent[2] = y;
    *(int **)(y + 8) = x;
    x[1] = y;
    return;                                 // ret 4
  }
  // x was left child
  *parent = y;
  *(int **)(y + 8) = x;
  x[1] = y;
  return;                                   // ret 4
}
```

## Control flow

| Stage | Action |
|---|---|
| 1 | `y = x.left`; `x.left = y.right` |
| 2 | If `y.right` non-nil → `y.right.parent = x` (isnil@+0x21) |
| 3 | `y.parent = x.parent` |
| 4a | Root case → `head.parent = y` |
| 4b | Right-of-parent → `parent.right = y` |
| 4c | Left-of-parent → `parent.left = y` |
| 5 | `y.right = x`; `x.parent = y` |

## Open questions

- Product / MSVC demangle for each map instantiation using this family.
- Runtime / bit-exact / differential.
