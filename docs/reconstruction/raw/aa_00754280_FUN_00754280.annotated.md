# Annotated low-level: Palantir_StartPreloadThread_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00754280` |
| VA | `0x00754280`–`0x007542e8` (**105 B**) |
| System | Palantir / thrThread preload |
| Date | 2026-07-29 (W31-F dual seal) |
| Ghidra | `FUN_00754280` |
| Canonical | `Palantir_StartPreloadThread_Inferred` |

## Machine-level notes

- **ABI:** EDI = Palantir host (`*DAT_00d1f058` at both known call sites); 0 stack formals; plain RET; AL success.
- **Already-running gate:** `CMP [EDI+0xD4], ESI` (ESI zeroed); non-zero → log + AL=0.
- **Thread object:** `PUSH 0x10` / `operator_new`; ctor-inline: vftable `PTR_FUN_00a9e9bc`, zero +4/+8/+c then `+0xC = *(*(DAT_00d1f050)+0x6C)`.
- **Start:** ESI=thread, EDX=`0x40000`, AL=0 → `FUN_0071aef0` (thrThread.cpp CreateThread wrapper). Decompiler drops EDX/AL/ESI contract — trust bytes.
- **Null new:** still stores ESI=0 to `+0xD4` and calls start; still returns AL=1 (retail).
- Prefer assembly when decompiler conflicts (this unit: arg regs into thrThread create).

## Pseudocode (byte-corrected)

```c
// EDI = Palantir host; plain RET; AL = 0 already-running / 1 attempted-start
uint8_t Palantir_StartPreloadThread_Inferred(void /*EDI host*/)
{
  ThrThread* thread = NULL;

  if (*(ThrThread**)(host + 0xd4) != NULL) {
    vog_LogMessage(
      "C:\\vog\\1_code\\palantir\\palantir\\framework\\Palantir.cpp",
      0x1fd, 2,
      "Preload thread already runnning.");
    return 0;
  }

  ThrThread* raw = (ThrThread*)operator_new(0x10);
  if (raw != NULL) {
    raw->vftable = &PTR_FUN_00a9e9bc;
    raw->threadId = 0;
    raw->handle = 0;
    raw->context = *(void**)(*(uint8_t**)DAT_00d1f050 + 0x6c);
    thread = raw;
  }

  *(ThrThread**)(host + 0xd4) = thread;

  // ESI=thread, EDX=0x40000 stack, AL=0 (not suspended)
  thrThread_Create(thread /*ESI*/, /*EDX*/ 0x40000, /*AL*/ 0);

  return 1;
}
```

## Open questions

- Product/PDB method English on Palantir class.
- Exact thrThread vftable methods at `PTR_FUN_00a9e9bc` (start routine `LAB_0071aed0` body residual).
- Runtime / OOM path behavior under live process.
