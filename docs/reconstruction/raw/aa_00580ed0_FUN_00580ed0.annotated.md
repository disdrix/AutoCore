# Annotated low-level: TtPhantom_castRay (`FUN_00580ed0`)

| Field | Value |
|---|---|
| Stable ID | `aa_00580ed0` |
| VA | `0x00580ed0`–`0x005810fd` inclusive (**558 B** / `0x22E`) |
| System | input-drive-control / physics (wheel suspension + creature phantom path) |
| Date | 2026-08-05 (MEGA-101 dual refresh) |
| Ghidra name | `FUN_00580ed0` |
| Canonical name | `TtPhantom_castRay` |
| Prior scaffolds (retired) | `Skill_TtPhantom_castRay`; `Named_CalleeOf_CVOGCreature_FindTerrainHeight_00580ed0` |

**Tools:** live `decompile_function` + `analyze_function_complete` + callers/xrefs/callees + `disassemble_function` + `read_memory` + `search_strings`. **No** `disassemble_bytes`. No Launcher.

---

## Machine-level notes

- **ABI sealed:** `__thiscall`, **ECX** = phantom/this, stack ray*, result*, epilogue **`RET 8`** (`C2 08 00` @ `0x005810fb`).
- **Plate sealed:** profiler entry stores pointer `0x009d4574` → ASCII `"TtPhantom::castRay"`.
- **Not** `CVOGMap_CastTerrainHeight` / map heightfield — that is the sibling path when creature has no body/phantom (`creature+0x254 == 0`).
- Prefer assembly when decompiler conflicts; live decompile ≡ 2026-07-23 raw CF.
- Filter prep (`local_1c`) and `ray[9]` load appear unused in decompile — treat as **gap**, not semantic rename drivers.

---

## Signature (annotated)

```c
// ECX = TtPhantom* (this)
// [EBP+8]  = ray packet (world start/end + flag)
// [EBP+0xc]= raycast result accumulator
// RET 8
void __thiscall TtPhantom_castRay(void *phantom, float *ray, void *result);
```

### Ray packet (`ray` / `param_2`)

| Off | Access | Meaning |
|---|---|---|
| `+0x00..0x08` | `ray[0..2]` | world **start** xyz |
| `+0x10..0x18` | `ray[4..6]` | world **end** xyz |
| `+0x20` | `*(char*)(ray+8)` | filter enable flag (wheel sets **1**) |
| `+0x24` | `ray[9]` | loaded to stack; **unused** in body (wheel stores `*(fw+0x1f8)` here) |

### Result (`result` / `param_3`)

| Off | Who | Meaning |
|---|---|---|
| `+0x00..` | shape cast + post | contact normal (body-local → **world** after `FUN_005d6ae0`) |
| `+0x14` | shape cast | hit **fraction** (closest-hit; caller seeds **1.0**) |
| `+0x20` | **this function** | hit **collidable***; **0** = miss |

### Phantom (`this` / `param_1`)

| Off | Meaning |
|---|---|
| `+0x08` → `+0xd0` | optional filter object base (prep only in decompile) |
| `+0x80` | `collidable**` overlap array |
| `+0x84` | `int` overlap count |

---

## Pseudocode (annotated; live decompile structure)

```c
void __thiscall TtPhantom_castRay(int phantom, float *ray, int result)
{
  // --- profiler entry: "TtPhantom::castRay" @ 0x009d4574 + rdtsc ---
  if (DAT_00bc5644 < DAT_00bc5648) {
    *DAT_00bc5644 = "TtPhantom::castRay";
    DAT_00bc5644[1] = (int)rdtsc();
    DAT_00bc5644 = DAT_00bc5644 + 3;
  }

  // ray[9] loaded (dead in decompile)
  float unused_ray9 = ray[9];

  // optional filter prep — NOT passed to shape vcall in decompile
  int filter;
  if ((*(char *)(ray + 8) == 0) ||
      (filter = *(int *)(*(int *)(phantom + 8) + 0xd0), filter == 0)) {
    filter = 0;
  } else {
    filter = filter + 0x10;
  }

  int count = *(int *)(phantom + 0x84);
  int **list = *(int ***)(phantom + 0x80);

  if (count - 1 >= 0) {
    do {
      int *collidable = *list;
      int *shape = (int *)*collidable;          // collidable+0
      if (shape != 0) {
        int bodyXform = collidable[2];          // collidable+8

        // world start → body-local (R^T * (P - T)); R cols @ body+0x20/30/40, T @ +0x50
        float dx = ray[0] - *(float *)(bodyXform + 0x50);
        float dy = ray[1] - *(float *)(bodyXform + 0x54);
        float dz = ray[2] - *(float *)(bodyXform + 0x58);
        float localStart[3];
        localStart[0] = *(float *)(bodyXform + 0x28) * dz + *(float *)(bodyXform + 0x24) * dy
                      + *(float *)(bodyXform + 0x20) * dx;
        localStart[1] = *(float *)(bodyXform + 0x38) * dz + *(float *)(bodyXform + 0x34) * dy
                      + *(float *)(bodyXform + 0x30) * dx;
        localStart[2] = *(float *)(bodyXform + 0x48) * dz + *(float *)(bodyXform + 0x44) * dy
                      + *(float *)(bodyXform + 0x40) * dx;

        // world end → body-local
        dx = ray[4] - *(float *)(bodyXform + 0x50);
        dy = ray[5] - *(float *)(bodyXform + 0x54);
        dz = ray[6] - *(float *)(bodyXform + 0x58);
        float localEnd[3];
        localEnd[0] = /* same R^T pattern */;
        localEnd[1] = /* ... */;
        localEnd[2] = /* ... */;

        // shape->vtbl[+0x20](hitFlag, &localRay, result)  — closest-hit into shared result
        char hitFlag[4];
        (**(code **)(*shape + 0x20))(hitFlag, &localStart /* contiguous local ray */, result);
        if (hitFlag[0] != 0) {
          *(int *)(result + 0x20) = (int)collidable;
        }
      }
      list = list + 1;
      count = count - 1;
    } while (count != 0);
  }

  // if hit: rotate result.normal body-local → world via FUN_005d6ae0(R @ body+0x20, result)
  if (*(int *)(result + 0x20) != 0) {
    FUN_005d6ae0(*(int *)(*(int *)(result + 0x20) + 8) + 0x20, result);
    // disasm: ECX=result, push result, push bodyXform+0x20, CALL 0x005d6ae0
  }

  // --- profiler exit: DAT_009d2878 ("Et") + rdtsc ---
  if (DAT_00bc5644 < DAT_00bc5648) {
    *DAT_00bc5644 = &DAT_009d2878;
    DAT_00bc5644[1] = (int)rdtsc();
    DAT_00bc5644 = DAT_00bc5644 + 3;
  }
  return; // RET 8
}
```

---

## Call graph

```text
FUN_0064bbd0  (hk vehicle wheel collide packer)  @ 0x0064bc61
  └─ TtPhantom_castRay  [THIS]
       └─ shape vtbl+0x20  (per overlap collidable)
       └─ FUN_005d6ae0     (normal local→world)

CVOGCreature_FindTerrainHeight  @ 0x004c629f
  ├─ (no phantom @ +0x254) → CVOGMap_CastTerrainHeight   [SIBLING, not this]
  └─ (phantom present)     → TtPhantom_castRay  [THIS]
```

---

## Open questions / gaps

1. Confirm whether filter (`local_1c`) is truly dead or a decompiler omission into shape cast.
2. Identity of `*(fw+0x38)` vs `*(fw+0x1f8)` (packer uses +0x38 as this; +0x1f8 written into ray+0x24).
3. Dual remaining shape `castRay` implementers if ports need per-shape fidelity.
4. Runtime Confirmed / bit-exact / differential.

---

## Confidence

| Claim | Level |
|---|---|
| CF from live decompile ≡ raw | **Confirmed** |
| `RET 8` / thiscall / 2 stack args | **Confirmed** (epilogue + packer) |
| Plate name `TtPhantom::castRay` | **Confirmed** |
| Overlap list + world→local + shape cast + normal rotate | **Confirmed** |
| Distinct from map terrain cast | **Confirmed** |
| Filter semantics | **Low** (prep only) |
| Runtime | **Open** |
