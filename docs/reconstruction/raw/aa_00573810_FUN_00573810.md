# Raw capture: FUN_00573810

| Field | Value |
|---|---|
| **Stable ID** | `aa_00573810` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00573810` |
| **Canonical name** | `FUN_00573810` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00573810(int param_1,undefined4 *param_2,uint *param_3)

{
  undefined4 uVar1;
  uint *puVar2;
  undefined4 *puVar3;
  undefined4 *puVar4;
  bool bVar5;
  
  puVar2 = param_3;
  puVar4 = *(undefined4 **)(param_1 + 4);
  bVar5 = true;
  if (*(char *)((int)puVar4[1] + 0x19) == '\0') {
    puVar3 = (undefined4 *)puVar4[1];
    do {
      puVar4 = puVar3;
      bVar5 = *param_3 < (uint)puVar4[3];
      if (bVar5) {
        puVar3 = (undefined4 *)*puVar4;
      }
      else {
        puVar3 = (undefined4 *)puVar4[2];
      }
    } while (*(char *)((int)puVar3 + 0x19) == '\0');
  }
  param_3 = puVar4;
  if (bVar5) {
    if (puVar4 == (undefined4 *)**(int **)(param_1 + 4)) {
      puVar4 = (undefined4 *)FUN_00573510(&param_3,1,puVar4,puVar2);
      uVar1 = *puVar4;
      *(undefined1 *)(param_2 + 1) = 1;
      *param_2 = uVar1;
      return;
    }
    FUN_005ae050();
  }
  if (param_3[3] < *puVar2) {
    puVar4 = (undefined4 *)FUN_00573510(&param_3,bVar5,puVar4,puVar2);
    *param_2 = *puVar4;
    *(undefined1 *)(param_2 + 1) = 1;
    return;
  }
  *(undefined1 *)(param_2 + 1) = 0;
  *param_2 = param_3;
  return;
}
```

---

## W28-T live re-verify (2026-07-29) — append only

| Field | Value |
|---|---|
| **Tools** | `decompile_function`, `get_function_by_address`, xrefs/callers/callees, `read_memory`. **No** `disassemble_bytes`. |
| **Body** | `0x00573810`–`0x005738c8` exclusive (**184** B / `0xB8`); pad `CC` after last `ret 8` |
| **Live decompile** | ≡ scaffold raw CF |
| **Callees** | `FUN_00573510` (StdTree_InsertAndRebalance_Val12), `FUN_005ae050` (tree predecessor) |
| **Callers (3 / 4 xrefs)** | `FUN_00572360` @ `0x005724d5`; `FUN_00572730` @ `0x005729b8`; `FUN_00572ab0` @ `0x00572d41`, `0x00572deb` |

### Byte / ABI seal

- Prolog: push ECX; push EBP; `mov ebp,[esp+0x10]` (value*); push ESI; push EDI; `mov edi,ecx` (map this).
- Key compare: `cmp [edx], [eax+0xc]` style — key is **uint at value+0 / node+0x0c** (`puVar4[3]`).
- isnil gate: `cmp byte [node+0x19],0`.
- Insert helper: `call FUN_00573510` (rel32 sites `0x00573869`, `0x005738a0`) with `(outIt*, addLeft, where, value*)`.
- Predecessor: `call FUN_005ae050` (no stack args; mutates iterator on stack).
- Out pair: `*out = node`; `mov byte [out+4], 0|1`.
- Three exits: all **`c2 08 00`** (`ret 8`).

### Hex head + tails (`read_memory`)

Entry (first 96 B of body):

```
51558b6c241056578bf98b77048b460480781900b101884c240c75218b5500903b500c8bf00f92c184c9884c240c74048b00eb038b40088078190074e3…
```

Epilogue region @ `0x005738b0`:

```
… c6 40 04 01 5d 59 c2 08 00          ; insert path inserted=1; ret 8
8b 44 24 14 5f 5e c6 40 04 00 89 10 5d 59 c2 08 00  ; equal path inserted=0; ret 8
cc …
```

### Algorithm (MSVC unique insert)

```
head = map->head (+4); size shell standard
addLeft = true
if root not nil:
  walk: parent = cur; addLeft = (key < parent.key); cur = addLeft ? left : right
if addLeft:
  if parent == leftmost: insert left → out{n,1}; return
  else: predecessor(parent)
if parent.key < key: insert(addLeft) → out{n,1}; return
else: out{parent,0}  // equal — no value rewrite
```

### Structural name (inferred)

`StdMap_InsertOrFind_Val12_UintKey` — Val12 map insert-or-find; key = first dword of value (uint).
