# Annotated low-level: Object_Store16At80_Inferred (`FUN_00404c60`)

| Field | Value |
|---|---|
| Stable ID | `aa_00404c60` |
| VA | `0x00404c60`–`0x00404c88` inclusive (**41 B** / `0x29`) |
| Ghidra name | `FUN_00404c60` |
| Canonical name | `Object_Store16At80_Inferred` |
| System | skills-abilities (partition); body is generic object field-store leaf |
| Date | 2026-08-05 (MEGA-053 dual refresh) |
| Twin (not OWN) | `FUN_00404c30` stores 16 B at `this+0x90..0x9c` |

---

## Machine-level notes

- Source: raw capture `aa_00404c60` + MEGA-053 live re-verify append.
- Prefer assembly when decompiler conflicts: decompiler of callers often drops the stack src or invents wrong arg counts; body assembly seals **ECX=dst**, **`[ESP+4]=src`**, **`RET 4`**.
- Leaf: **no callees**, no branches, no SEH.
- Four dword stores only — no float ops in body; callers may pack floats or identity-like dwords into the same 16 B.

## Pseudocode (annotated)

```c
// thiscall: ECX = object*; stack = const uint32_t src[4]*; RET 4
void __thiscall Object_Store16At80_Inferred(void *thisObj, const uint32_t *src16)
{
  // this+0x80..0x8c ← src[0..3]
  *(uint32_t *)((char *)thisObj + 0x80) = src16[0];
  *(uint32_t *)((char *)thisObj + 0x84) = src16[1];
  *(uint32_t *)((char *)thisObj + 0x88) = src16[2];
  *(uint32_t *)((char *)thisObj + 0x8c) = src16[3];
  return; // RET 4
}
```

## Control flow

| Stage | Match | Conf |
|---|---|---|
| Load src from stack | **Yes** | **High** |
| Store dword0 @ +0x80 | **Yes** | **High** |
| Store dword1 @ +0x84 | **Yes** | **High** |
| Store dword2 @ +0x88 | **Yes** | **High** |
| Store dword3 @ +0x8c | **Yes** | **High** |
| `RET 4` | **Yes** | **High** |
| Leaf (no CALL) | **Yes** | **High** |

## Callers (roles — evidence only)

| Caller | Role sketch |
|---|---|
| `FUN_0058a1b0` | Skill/creature apply path; conditional store + twin `00404c30` when flag bit set |
| `FUN_005c76f0` | `CLoadNode::_initPhysics`; type `0xe` path copies local buffers via this + twin |
| `FUN_00620480` | Skill spawn / template path (string `"Skill %d had invalid template %d."` on failure branch); stores stack blob then twin |
| `FUN_0091da70` | Client local-player / sector-net path; packs floats then stores via multi-inh this |

## Naming gate

| Evidence | Present? |
|---|---|
| 16-byte store @ +0x80 sealed in bytes | **Yes** |
| thiscall + RET 4 | **Yes** |
| Twin store @ +0x90 | **Yes** (peer VA, not dualled here) |
| Product English for field (pos/quat/TFID/…) | **No** |
| Skill invalid-string body | **No** (parent only) |

**Decision:** promote **`Object_Store16At80_Inferred`**. Reject skill-string identity; reject merge with twin `00404c30`; reject getter / out-copy direction.

## Open / residual

1. Product/PDB symbol for the +0x80 field (and +0x90 twin half).
2. Shared CVOG object layout plate for multi-inh `this + *(this+4) + 4` bases.
3. Dual of twin `FUN_00404c30` (residual, not MEGA-053 OWN).
4. Runtime / bit-exact / differential.
