# Annotated low-level: FUN_00745d70 → NDResource_Ctor

| Field | Value |
|---|---|
| Stable ID | `aa_00745d70` |
| VA | `0x00745d70`–`0x00745dec` |
| System | `ndresource` / ND resource object base |
| Date | 2026-07-23 scaffold; **2026-07-29 W19-R dual seal** |
| Proposed name | `NDResource_Ctor` |

## Machine-level notes

- Source: raw capture + live Ghidra `decompile_function` / `read_memory` (no `disassemble_bytes`).
- **ABI sealed:** 3 stack args + **EDX type token**; `RET 0xC`; returns constructed `this`.
- Stack order at call: `push optionalSrc; push nameKey*; push this`.
- Object size **0x3c** (from `NDResourceCache_LookupOrCreate` `operator_new(0x3c)`).
- Intermediate vtbl **`PTR_FUN_00aa1d40`**; default-resource callers then store **`PTR_FUN_00a9e850`**.
- CS at `+0x20` (24 B RTL_CRITICAL_SECTION); ready flag byte at `+0x38`.
- Optional `param_5->vtbl[+4]()` result → `this+0x14` (payload / linked resource).
- Sibling dtor `FUN_00745ff0` uses same base vtbl + `DeleteCriticalSection`.

## Layout (dword indices as decompiler `param_3[n]`)

| Off | Field | Init |
|---:|---|---|
| `+0x00` | vtbl | `PTR_FUN_00aa1d40` |
| `+0x04` | name key copy | `*nameKey` |
| `+0x08` | (reserved/zero) | `0` |
| `+0x0c` | type token | `EDX` |
| `+0x10` | (reserved/zero) | `0` |
| `+0x14` | payload / linked | `0` or `optionalSrc->vtbl+4()` |
| `+0x18` | sentinel | `0xffffffff` |
| `+0x20` | `CRITICAL_SECTION` | `InitializeCriticalSection` |
| `+0x38` | ready flag (u8) | `1` |

## Pseudocode (annotated)

```c
// __fastcall-ish: EDX = typeToken
// stack: this, nameKey*, optionalSrc*  ; RET 0xC
// returns this
NDResource *NDResource_Ctor(void *unused_ecx_slot,
                            uint32_t typeToken,      // EDX
                            NDResource *self,        // stack
                            uint32_t *nameKey,       // stack
                            void *optionalSrc)       // stack
{
  // SEH omitted
  self->vtbl = &PTR_FUN_00aa1d40;
  self->nameKey = *nameKey;
  self->z8 = 0;
  self->typeToken = typeToken;
  self->z10 = 0;
  self->payload = 0;
  self->sentinel = 0xffffffff;
  InitializeCriticalSection(&self->cs);   // +0x20
  self->ready = 1;                        // +0x38
  if (optionalSrc != NULL) {
    self->payload = optionalSrc->vtbl[1](optionalSrc); // +4
  }
  return self;
}
```

## Open questions

- Product C++ class name for base vtbl `00aa1d40` vs default `00a9e850` (NDResource family High; exact spelling Tentative).
- Semantics of `vtbl+4` on optionalSrc (clone vs attach).
- Exact type of name key object at `+0x04`.
