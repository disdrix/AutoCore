# Annotated low-level: Client_Vehicle_StopHbAndZeroLongitudinal_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00914c20` |
| VA | `0x00914c20`–`0x00914c59` (58 B) |
| Ghidra name | `FUN_00914c20` |
| Canonical name | `Client_Vehicle_StopHbAndZeroLongitudinal_Inferred` |
| System | `input-drive-control` |
| Date | 2026-08-05 (MEGA-098 dual refresh) |
| Prior scaffold | 2026-07-23 partial |

## Machine-level notes

- Prefer assembly when decompiler conflicts — here decompile ≡ `disassemble_function` ≡ entry bytes.
- **ABI:** no stack args; **client host in EAX** at entry (register-carried). Callers both do `MOV EAX,EDI` then `CALL`. Epilogue bare `RET` (not `RET n`).
- **Gate:** `DAT_00d1a860 != 0` **and** `*(client+0xc70) != 0` (HB object pointer non-null).
- On allow:
  1. `*(u8*)(client+0xa35) = 0` — clear client latch (product English open).
  2. `CVOGHBBase_Stop` (`0x005081d0`) with **ECX = *(client+0xc70)** (thiscall).
  3. If `DAT_00d1b6d8` and vehicle `*(+0x250)`: `VehicleEntity_SetLongitudinalInput(0)` thiscall **ECX=vehicle**, stack `0` (bits of `+0.0f`).
- Does **not** SetHandbrake, SetSteer, PushDriveAxes, or touch `vehicle+0x101`. Contrast dualed sibling `Client_Vehicle_EngageHbAndPushAxes_Inferred` (`0x00915670`) which engages HB=1 + Push under the opposite `DAT_00d1a860==0` Stop sub-gate.
- Hub `Client_InteractWorldClickHub` (`0x00925820`) also **inlines** an equivalent CF early (same globals/offsets) and later **calls** this helper on drop-destroy path — dual ownership is this leaf only.

## Pseudocode (annotated)

```c
// EAX = client / game host (register-carried; not a formal stack param)
void Client_Vehicle_StopHbAndZeroLongitudinal_Inferred(void /* host in EAX */)
{
  // in_EAX: client host
  if (DAT_00d1a860 != 0 && *(int *)(host + 0xc70) != 0) {
    *(uint8_t *)(host + 0xa35) = 0;          // clear latch
    CVOGHBBase_Stop(/* ECX = *(host+0xc70) */);
    if (DAT_00d1b6d8 != 0 && *(int *)(DAT_00d1b6d8 + 0x250) != 0) {
      VehicleEntity_SetLongitudinalInput(/* ECX = vehicle */, 0.0f);
    }
  }
}
```

## Callers / callees

| Direction | Target | Conf |
|---|---|---|
| Caller | `FUN_00914c60` @ `0x00914ca5` | High (xref + `MOV EAX,EDI`) |
| Caller | `Client_InteractWorldClickHub` `FUN_00925820` @ `0x00925984` | High (xref + `8B C7; E8…`) |
| Callee | `CVOGHBBase_Stop` `0x005081d0` | High (bytes + dual seal) |
| Callee | `VehicleEntity_SetLongitudinalInput` `0x004f5650` | High (bytes + dual seal) |

## Open questions

- Product English for `DAT_00d1a860` mode flag and `client+0xa35` latch.
- Whether `+0xc70` is always a `CVOGHBBase*` vs a richer timer object (Stop dual treats +0x21 as fDormant).
- Runtime / bit-exact / differential — open (no Launcher).
