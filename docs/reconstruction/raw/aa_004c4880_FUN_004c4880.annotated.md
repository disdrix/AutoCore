# Annotated low-level: Object_SetTFID_ApplyPhysMode_Rebind

| Field | Value |
|---|---|
| Stable ID | `aa_004c4880` |
| VA | `0x004c4880`–`0x004c48f1` (**114 B**) |
| Ghidra name | `FUN_004c4880` |
| Canonical name | `Object_SetTFID_ApplyPhysMode_Rebind` |
| System | object / TFID / phys rebind |
| Date | 2026-07-29 (W26-F live seal) |
| Tools | `decompile_function` + `read_memory` + `analyze_function_complete` (no `disassemble_bytes`) |

## Machine-level notes

- **ABI:** thiscall; `RET 4`; ECX = object; stack = `const uint32_t src[4]` (TFID_16).
- **Body:** copy 16 B → `this+0x108`; if `*(this+8)` set phys mode **10** or **0xB** at `phys+0x28`; rebind via `FUN_00560e90`.
- **Invalid TFID:** `(lo & hi) == ~0 && (char)word2 == 0` → mode **0xB**; else **10**. Identical to pure leaf `FUN_004c4620`.
- **TFID layout** (from sibling `FUN_004c4440`): `dwCoidLo`, `dwCoidHi`, word2, word3 — type `TFID_16`.
- **Callee:** `WorldObj_RebindActivate_Inferred` (`0x00560e90`) — unbind/rebind manager helper; dual-sealed W24-B.
- **Caller:** sole xref `FUN_00626890` (post-resolve TFID apply before HB enqueue).

## Pseudocode (annotated)

```c
// thiscall; void
void Object_SetTFID_ApplyPhysMode_Rebind(int this, uint32_t *src_tfid /* 4 dwords */)
{
  // 1) store TFID_16 at this+0x108
  *(uint32_t *)(this + 0x108) = src_tfid[0];  // dwCoidLo
  *(uint32_t *)(this + 0x10c) = src_tfid[1];  // dwCoidHi
  *(uint32_t *)(this + 0x110) = src_tfid[2];
  *(uint32_t *)(this + 0x114) = src_tfid[3];

  int phys = *(int *)(this + 8);
  if (phys != 0) {
    // 2) same predicate as FUN_004c4620
    if (((*(uint32_t *)(this + 0x108) & *(uint32_t *)(this + 0x10c)) == 0xFFFFFFFFu) &&
        (*(char *)(this + 0x110) == 0)) {
      *(uint32_t *)(phys + 0x28) = 0xB;   // unassigned / invalid-TFID phys mode
    } else {
      *(uint32_t *)(phys + 0x28) = 10;    // assigned-TFID phys mode
    }
    // 3) WorldObj_RebindActivate(manager, phys) with flag 1
    //    decompiler surface: FUN_00560e90(phys, 1)
    FUN_00560e90(*(uint32_t *)(this + 8), 1);
  }
}
```

## Open questions

- Product English for modes **10** / **0xB** (phys state enum names).
- Exact manager chain (`+0xA8` / `+0xE4A4`) type names (owned by `00560e90` dual).
- Call-site source buffer owner field product name (`[ebp+0x6c0]` / subobject `+0x164`).
- Runtime / differential — open.
