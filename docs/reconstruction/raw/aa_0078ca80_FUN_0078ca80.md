# Raw capture: FUN_0078ca80

| Field | Value |
|---|---|
| **Stable ID** | `aa_0078ca80` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0078ca80` |
| **Canonical name** | `FUN_0078ca80` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0078ca80(undefined4 *param_1)

{
  void *pvStack_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  puStack_8 = &LAB_009b3fee;
  pvStack_c = ExceptionList;
  ExceptionList = &pvStack_c;
  *param_1 = &PTR_FUN_00a99f74;
  local_4 = 0;
  FUN_007a8580();
  FUN_0078c3d0();
  if ((undefined4 *)param_1[0xaa] != (undefined4 *)0x0) {
    (*(code *)**(undefined4 **)param_1[0xaa])(1);
  }
  param_1[0xaa] = 0;
  local_4 = 0xffffffff;
  FUN_007b5be0();
  ExceptionList = pvStack_c;
  return;
}
```

---

## WQ9K-F re-verify (2026-08-05) — append only

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **WQ9K-F** |
| **Tools** | `batch_decompile` + `analyze_function_complete` + `disassemble_function` + `read_memory` + RTTI COL + callers. **No** `disassemble_bytes`. |
| **Live decompile** | ≡ scaffold raw CF (void complete dtor; no free of this) |
| **Body** | `0x0078ca80`–`0x0078caef` inclusive (**112 B** / `0x70`); next `FUN_0078caf0` (CNDUIWndBuffered_Ctor) |
| **ABI** | `__thiscall`; entry `MOV ESI,ECX`; SEH `LAB_009b3fee`; bare **`RET`** (`C3`); **void** |
| **Classification** | worker (complete dtor body) |
| **Product name** | **`CNDUIWndBuffered_CompleteDtor`** — RTTI sealed |

### Full body hex (112 B, `read_memory`)

```
6aff68ee3f9b0064a100000000506489250000000051568bf189742404c706749fa900c744241000000000e8d0ba01008bcee819f9ffff8b8ea802000085c974068b016a01ff108bcec786a802000000000000c7442410ffffffffe8009102008b4c24085e64890d0000000083c410c3
```

### Control flow (bytes authority)

```
SEH prolog LAB_009b3fee; ESI = ECX (this)
*this = PTR_FUN_00a99f74          ; C7 06 74 9F A9 00
EH state = 0
CALL FUN_007a8580                 ; ECX may be unset / thiscall helper
MOV ECX,ESI; CALL FUN_0078c3d0
ECX = *(this+0x2A8)
if ECX:
  EAX = *ECX; PUSH 1; CALL [EAX]  ; nested scalar-deleting dtor
MOV ECX,ESI
*(this+0x2A8) = 0
EH state = -1
CALL FUN_007b5be0                 ; base CNDUIWindow complete
SEH restore; ADD ESP,0x10; RET
```

### RTTI / vtbl

| Item | Value |
|---|---|
| Primary vtbl | `PTR_FUN_00a99f74` |
| COL (vtbl−4) | `@0x00a99f70` → `0x00ab8280` |
| type_info | `0x00afe4d0` → **`.?AVCNDUIWndBuffered@@`** |
| vtbl[0] | dword `@0x00a99f74` = `0x004293f0` (scalar peer: complete then flags&1 delete) |

### Callers (code)

| Caller | Role |
|---|---|
| `FUN_00834520` / `CWndVehicle_CompleteDtor` @ `0x0083459c` | derived complete → this base |
| `FUN_004293f0` | scalar deleting of CNDUIWndBuffered |
| `FUN_00856380` | another derived complete path |
| Unwind `@009b455b` / `@009b456d` / `@009b4938` / `@009b494a` / `@009b495c` / `@009b4989` | EH |

### Callees

`FUN_007a8580`, `FUN_0078c3d0`, nested `vtbl[0](1)`, `FUN_007b5be0`

### Notes

- Not scalar/vector: no flags arg; no free of host; bare RET.
- Nested free is **owned HostBase @ +0x2A8** via its scalar dtor (flag 1), then field null (always after if — actually null is after the if always per disasm: null store is at cac9 regardless; wait — disasm shows null AFTER the if block at cac7 MOV ECX,ESI then cac9 store zero — yes always null).
- Actually looking at disasm: JZ skips only the CALL; null store is always executed. Confirmed.
