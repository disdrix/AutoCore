# Annotated low-level: FUN_00570af0 → InventoryGrid_MoveItemFootprint_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00570af0` |
| VA | `0x00570af0`–`0x00570cfb` |
| System | inventory-transfer |
| Date | 2026-07-23 scaffold; **2026-08-04 dual seal** |

## Machine-level notes (2026-08-04)

- **ABI:** `__thiscall` grid in ECX; stack `item, originX, originY`; **`RET 0x0C`**.
- **Role:** move/relocate footprint (not first place; not remove/detach).
- **CanPlace:** InvSize from clonebase `+0x406/+0x407`; **local conflictOut** (overlap-safe).
- **Clear:** current origin via vtbl `+0x250` / `+0x254`; empty `0xFFFFFFFF` pairs; index `height*x+y`.
- **SetPos:** vtbl `+0x24c(originX, originY)`.
- **Stamp:** COID `item+0x160/+0x164`; index `height*(ox+dx)+(oy+dy)` — **bytes override** decompiler `unaff_*`.
- **Post:** if `grid+0x24` non-null → `FUN_00512670(that)`; `grid+0x21 = 1`.
- **Callers:** Ghidra xrefs empty (orphan/indirect residual).
- Prefer clean `InventoryGrid_MoveItemFootprint_Inferred.cpp` over raw stamp expression.

## Offsets

| Location | Role |
|----------|------|
| grid `+0x0c` | height / index stride |
| grid `+0x21` | dirty flag |
| grid `+0x24` | owner ptr for optional helper |
| grid `+0x28` | cell array (8 B COID pairs) |
| item `+0xa8` → `+0x3c` | type host → clonebase blob |
| blob `+0x406/+0x407` | InvSizeX / InvSizeY |
| item `+0x160/+0x164` | COID lo/hi |
| vtbl `+0x250/+0x254` | GetX / GetY |
| vtbl `+0x24c` | SetPos(x,y) |

## Pseudocode (annotated — corrected stamp)

```c
// Corrected model (do not use raw unaff stamp)
uint32_t __thiscall MoveItemFootprint(InventoryGrid* grid, Item* item,
                                      uint32_t originX, uint32_t originY)
{
  if (!item) return 0;
  uint8_t sizeX = blob[0x406], sizeY = blob[0x407];
  uint32_t conflict[2];
  if (!CanPlace(grid, sizeX, sizeY, originX, originY, conflict)) return 0;
  // clear old footprint at GetX/GetY
  // item->SetPos(originX, originY)  // vtbl+0x24c
  // stamp COID for dx,dy
  if (*(void**)(grid+0x24)) FUN_00512670(*(void**)(grid+0x24));
  *(uint8_t*)(grid+0x21) = 1;
  return 1;
}
```

## Open questions

- Discover callers / confirm live use.
- Runtime overlapping move dump.
- Product/PDB English.
