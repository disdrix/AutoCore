# Raw capture: FUN_005b8340

| Field | Value |
|---|---|
| **Stable ID** | `aa_005b8340` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005b8340` |
| **Canonical name** | `FUN_005b8340` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_005b8340(void *param_1)

{
  void *pvVar1;
  
  pvVar1 = *(void **)((int)param_1 + 4);
  *(void **)((int)param_1 + 4) = param_1;
  *(void **)param_1 = param_1;
  if ((pvVar1 != (void *)0x0) && (pvVar1 != param_1)) {
                    /* WARNING: Subroutine does not return */
    operator_delete(pvVar1);
  }
  return;
}
```


---

## W37-AC re-verify (2026-08-04)

| Field | Value |
|---|---|
| **Tools** | `decompile_function` + `read_memory` + `get_function_by_address` + `analyze_function_complete` + callers/callees/xrefs. **No** `disassemble_bytes` |
| **Body** | `0x005b8340`–`0x005b8369` exclusive (**41 B** / `0x29`); pad `CC` |
| **Live decompile** | CF incomplete — collapses free-loop (false noreturn on `operator_delete`) |
| **Epilogue** | bare `C3` |
| **Classification** | worker (list clear) |

### Full body hex (41 B)

```
568bf18b460485c0897604893674183bc67414578b780450e8c514edff83c4043bfe8bc775ee5f5ec3
```

### Byte-refined CF (authority — free loop)

```
// ECX = circular list head/sentinel (links at +0 and +4)
// bare RET; void
//
// PUSH ESI; MOV ESI, ECX
// EAX = [ESI+4]                 // first = head->link4
// [ESI+4] = ESI; [ESI] = ESI    // empty: both links self
// if EAX==0 OR EAX==ESI: done   // null or already empty circular
// PUSH EDI
// loop:
//   EDI = [EAX+4]               // next
//   PUSH EAX; CALL operator_delete; ADD ESP,4   // cdecl free node; CONTINUES
//   CMP EDI,ESI; MOV EAX,EDI; JNZ loop
// POP EDI; POP ESI; RET
```

### Call graph

| Role | Targets |
|---|---|
| Callee | `operator_delete` @ `0x00489822` (cdecl; `ADD ESP,4` after call) |
| Callers (5 fn / 15 sites) | `FUN_004ba0a0` (3), `FUN_00593e10` (3), `FUN_00594030` (3), `FUN_005b85e0` (1), `FUN_005bbc50` (5) |

### Notes

- Decompiler shows single `operator_delete` + "does not return" — **false**. Bytes: free-loop `75 EE` + `83 C4 04`.
- No size field zeroed (unlike `StdList_Clear` `0x00404060` which zeros `list+8`).
- ECX is the **sentinel node itself** (not a list object with `_Myhead` pointer at +4).
- Nodes freed with bare `operator_delete` — no element dtor (POD/trivial payload or external ownership).
