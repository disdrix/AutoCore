# Raw capture: FUN_00415e90

| Field | Value |
|---|---|
| **Stable ID** | `aa_00415e90` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00415e90` |
| **Canonical name** | `FUN_00415e90` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00415e90(void)

{
  undefined4 *puVar1;
  void *pvVar2;
  int unaff_ESI;
  
  puVar1 = *(undefined4 **)(unaff_ESI + 4);
  pvVar2 = (void *)*puVar1;
  *puVar1 = puVar1;
  *(int *)(*(int *)(unaff_ESI + 4) + 4) = *(int *)(unaff_ESI + 4);
  *(undefined4 *)(unaff_ESI + 8) = 0;
  if (pvVar2 != *(void **)(unaff_ESI + 4)) {
                    /* WARNING: Subroutine does not return */
    operator_delete(pvVar2);
  }
  return;
}
```

---

## WQ9L-H re-verify (2026-08-05) — append only

**Tools:** `decompile_function` @ `0x00415e90` + `analyze_function_complete` + `read_memory` + `get_function_callers` / xrefs + `get_assembly_context` (sites `00792cde`, `00402283`). **No** `disassemble_bytes`. No Launcher.

### Live decompile ≡ scaffold raw

Ghidra 2026-08-05 decompile **matches** 2026-07-23 raw CF exactly (ESI list*, re-ring head next/prev to self, size@+8=0, conditional `operator_delete` with false noreturn warning).

### Body / ABI (read_memory sealed)

| Item | Value |
|---|---|
| Body | `0x00415e90`–`0x00415ec6` exclusive (**54 B** / `0x36`); pad `CC` |
| Entry | `8B 4E 04` `MOV ECX,[ESI+4]` — **ESI = list*** (no `MOV ESI,ECX` prologue) |
| Epilogue | free-loop + `5F C3` `POP EDI; RET` |
| Free loop (bytes, decompiler-truncated) | `MOV EDI,[EAX]; PUSH EAX; CALL operator_delete; ADD ESP,4; CMP EDI,[ESI+4]; MOV EAX,EDI; JNE loop` — **multi-node free Confirmed** |
| Sentinel | re-ringed to self; **not** deleted here |

### Call-site ABI evidence

| Site | Context |
|---|---|
| `CNDUIDialog_CompleteDtor` @ `0x00792cde` | `LEA ESI,[EDI+0x4dc]` then `CALL 00415e90`; after: `MOV EAX,[ESI+4]; PUSH EAX; CALL operator_delete` (free sentinel @ list+4 / host+0x4e0) |
| `FUN_00402280` @ `0x00402283` | `MOV ESI,ECX` then `CALL 00415e90` (ECX thiscall host → ESI list*) |
| ~78 xrefs / ~49 named callers | shared list-clear leaf |

### Twin (do not merge)

`StdList_Clear_Inferred` @ `0x00404060` — **ECX** thiscall wrapper (`PUSH ESI; MOV ESI,ECX; …; POP ESI; RET`) with same free-loop body. This unit is the **ESI-entry** clone.

### Classification

worker / list-clear leaf. Canonical dual name: **`StdList_Clear_ESI`**.
