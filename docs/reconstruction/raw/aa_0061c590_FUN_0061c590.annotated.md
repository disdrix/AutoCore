# Annotated low-level: CVOGHBSkill_XP_ApplyToTargetList_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0061c590` |
| VA | `0x0061c590`–`0x0061c6af` (288 B) |
| Ghidra name | `FUN_0061c590` |
| Canonical (R11-015) | `CVOGHBSkill_XP_ApplyToTargetList_Inferred` |
| Class (RTTI) | **`CVOGHBSkill_XP`** — `.?AVCVOGHBSkill_XP@@` @ type_info `0x00af29ec` |
| System | skills-abilities |
| Parent | `CVOGHBSkillBase_ctor` `0x005788d0` |
| Date | 2026-08-05 (R11-015 dual) |

## Machine-level notes

- **ABI:** six stack formals; callee cleanup **`ret 0x18`**; returns **`1`**. Body does not consume ECX as `this` (vtbl slot still holds this VA at +0x2c).
- **Role:** walk a **0x10-byte TFID row table** until sentinel; for each resolvable object, **construct + arm** a `CVOGHBSkill_XP` heartbeat (`0x6c0`), enqueue on world HB list, Start — or virtual-delete if ValidateTarget left `pOwner==0`.
- **Decompiler gap (closed in annotated/clean):** `CVOGReaction_ResolveObjectTarget` is **`__thiscall`** with **ECX = `*(world+0xe4e8)`** (resolve context). Live decompile shows only the three stack args via a bogus `CONCAT31` of the mode byte.
- Prefer bytes when decompiler conflicts (resolve this, period field +0x08 vs SetPeriod pair).

## Signature (image-sealed)

```c
// ret 0x18; EAX = 1 on normal terminator
uint32_t CVOGHBSkill_XP_ApplyToTargetList_Inferred(
    void *pSourceObj,       // → CVOGHBSkillBase_ctor pSourceObj
    void *pSkillBlob,       // skill runtime; [+0x1c] → child HB +0x08 pulse
    void *pWorld,           // → ctor arg4 / +0x688; +0xe4e8 resolve ctx; +0xe4ec HB list
    TfidRow16 *pRows,       // array of 0x10-byte entries; sentinel terminates
    uint32_t *pTfid16,      // → ctor pTfid16
    uint32_t arg7);         // → ctor arg7
```

### TFID row (0x10)

| Off | Type | Role |
|----:|------|------|
| +0x00 | u32 | coidLo (`-1` sentinel half) |
| +0x04 | u32 | coidHi (`-1` sentinel half) |
| +0x08 | u8 | bGlobal / domain for ResolveObjectTarget |
| +0x09.. | — | padding / unused by this unit |

**Sentinel:** `{0xFFFFFFFF, 0xFFFFFFFF, bGlobal==0}` → return 1.

## Pseudocode (annotated; decompiler gap fixed)

```c
uint32_t CVOGHBSkill_XP_ApplyToTargetList_Inferred(
    void *pSourceObj,
    void *pSkillBlob,
    void *pWorld,
    int *pRows,              // treated as 0x10-byte records
    uint32_t *pTfid16,
    uint32_t arg7)
{
  char index = 0;
  /* SEH: LAB_009a879b */

  for (;;) {
    int *row = (int *)((char *)pRows + index * 0x10);

    // terminator
    if (row[0] == -1 && row[1] == -1 && *(char *)&row[2] == 0) {
      return 1;
    }

    // ECX = resolve context at world+0xe4e8 (bytes; decompiler omit)
    void *ctx = *(void **)((char *)pWorld + 0xe4e8);
    void *target = CVOGReaction_ResolveObjectTarget(
        ctx,
        *(unsigned char *)((char *)row + 8),  // bGlobal
        (unsigned)row[0],
        (unsigned)row[1]);

    if (target != 0) {
      CVOGHBBase *hb = (CVOGHBBase *)operator_new(0x6c0);
      if (hb != 0) {
        // thiscall ECX=hb; six stack formals match skill-base ctor
        CVOGHBSkillBase_ctor(hb, pSourceObj, pSkillBlob, pWorld,
                             target, pTfid16, arg7);
        hb->pVTable = &PTR_FUN_009d119c;           // CVOGHBSkill_XP
        *(int *)((char *)hb + 0x08) = *(int *)((char *)pSkillBlob + 0x1c);
        CVOGHBBase_SetPeriodAndCounter(hb, -1000, true);  // +0x0C/+0x10 always-ready
      }
      // retail: null-new falls into owner check (UB) — preserved
      if (hb->pOwnerObject != 0) {
        CVOGHBList_Enqueue(*(void **)((char *)pWorld + 0xe4ec), hb);
        CVOGHBBase_Start(hb);
      } else {
        // scalar deleting dtor via vtbl[0](1)
        (*(void (__thiscall **)(CVOGHBBase *, int))hb->pVTable)(hb, 1);
      }
    }
    index++;
  }
}
```

## Vtbl context (`PTR_FUN_009d119c`)

| Slot | VA | Notes (sibling; not OWN) |
|-----:|----|--------------------------|
| +0x00 | `0x00651190` | scalar deleting dtor → skill-base dtor thunk |
| +0x0C | `0x0061c940` | XP HB virtual (uses this; convoy/personal branches) |
| **+0x2C** | **`0x0061c590`** | **THIS unit** |
| +0x38 / +0x3C | `0x0061c7c0` / `0x0061c830` | debug strings: `conversion_percent` (personal xp %), `op_scalar_1` (convoy xp %) |

## Confidence

| Claim | Level |
|---|---|
| Body bounds + `ret 0x18` + return 1 | **Confirmed** (bytes) |
| Class RTTI `CVOGHBSkill_XP` | **Confirmed** (type_info string) |
| Vtbl imm `009d119c` install | **Confirmed** |
| Base = `CVOGHBSkillBase_ctor` | **Confirmed** (rel32 + parent dual) |
| Resolve this = `world+0xe4e8` | **Confirmed** (bytes) |
| Enqueue list = `world+0xe4ec` | **Confirmed** |
| Period `-1000,true` always-ready | **Confirmed** (SetPeriodAndCounter dual) |
| Method English “ApplyToTargetList” | **Inferred** (role; no PDB) |
| Product formal names beyond sealed base plate | Tentative |

## Open questions

1. Product C++ method name on `CVOGHBSkill_XP` (PDB).
2. Who fills the 0x10-byte row table and how many max rows (no hard cap in unit — sentinel only).
3. Whether virtual dispatch passes a ignored `this` in ECX (no CALL xrefs; DATA-only).
4. Runtime / bit-exact / differential.
