# Annotated low-level: StdVector_ThrowTooLong_00418130 (`FUN_00418130`)

| Field | Value |
|---|---|
| Stable ID | `aa_00418130` |
| VA | `0x00418130` |
| Body | `0x00418130`–`0x0041817a` (75 bytes) |
| Canonical name | `StdVector_ThrowTooLong_00418130` |
| Ghidra | `FUN_00418130` |
| System | containers / MSVC STL vector |
| Date | 2026-07-29 (W20-G dual seal) |

---

## Machine-level notes

- **SEH frame** installed (`push -1`, `LAB_009bc919`, `fs:[0]`) before locals — required because the path throws.
- Message string is a **static C string** at `0x00a15830` = `"vector<T> too long"` (sealed via `read_memory`).
- `basic_string` is constructed into stack (`lea ecx,[esp+4]` + IAT call `[0x009c62f8]`).
- EH state dword set to `0` at `[esp+0x4C]` **before** exception object construction (ordering differs from sibling `0x004540b0`).
- `FUN_00401a40(string*)` is **thiscall** on ECX = exception object (`lea ecx,[esp+0x20]`), stack arg = string ref — builds a typed exception.
- `_CxxThrowException(exception_object, &DAT_00acc388)` — **noreturn**. Same `ThrowInfo` as sibling throw helpers.
- **No formals.** Callers treat this as pure side-effect throw on overflow (size > max).
- **Sibling clone** of sealed `StdVector_ThrowTooLong` @ `0x004540b0` (same plate + ThrowInfo; minor push order delta). This VA is the overflow throw used by toast/floater insert-N families among others.

---

## Pseudocode (annotated)

```c
/* StdVector_ThrowTooLong_00418130 — MSVC vector::_Xlen()-class helper (noreturn).
   Builds length_error-class exception with plate "vector<T> too long" and throws. */

void __cdecl StdVector_ThrowTooLong_00418130(void)  /* noreturn */
{
  /* SEH + stack: basic_string msg; exception_object ex; */
  /* basic_string_ctor(&msg, "vector<T> too long" @ 0x00a15830); */
  /* FUN_00401a40(&ex, &msg);  // exception from string */
  /* _CxxThrowException(&ex, &DAT_00acc388); */
}
```

Control flow is **linear single block** — always throws; no success return.

---

## Open questions

1. Product/PDB English symbol (likely `std::vector<T>::_Xlen` or similar) — open; role sealed from plate string + CF.
2. Exact C++ exception type hierarchy behind `FUN_00401a40` / `ThrowInfo` — out of owned scope.
3. Why multiple identical plate-string clones exist (code-gen / per-TU) — not merged without byte-equality proof.
4. Runtime / bit-exact — open.
