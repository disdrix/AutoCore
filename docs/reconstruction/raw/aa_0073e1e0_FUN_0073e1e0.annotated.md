# Annotated low-level: FUN_0073e1e0

| Field | Value |
|---|---|
| Stable ID | `aa_0073e1e0` |
| VA | `0x0073e1e0` |
| System | graphics / effect create+load |
| Date | 2026-07-23 |
| W38-Q | 2026-08-04 |

## Machine-level notes

- Source: raw capture for `aa_0073e1e0` + W38-Q body seal.
- Prefer body bytes for callee ABI: path→ECX of `FUN_007533e0`, manager→ECX of `FUN_0073d830`, **`ret 0xc`**.
- Freelist pool `DAT_00d21970` via `FUN_00457040` (EDI=&pool); node size **0x30**.
- Vtbl `PTR_FUN_00a9f9c4`; fail path `vtbl[0](1)` → `FUN_00445020`.
- Primary consumer: `EffEffectSlot_BindByPath_Inferred` (`FUN_009701d0`); also Phase effect load in `FUN_0072a660`.

## Pseudocode (annotated)

```c
// stdcall RET 0x0C
// (manager*, pathOrToken, int* outStatus) → effect* or null
undefined4 * EffEffect_CreateAndLoad_Inferred(
    undefined4 manager, undefined4 pathOrToken, int *outStatus)
{
  undefined4 *obj = (undefined4 *)FUN_00457040(); // EDI=&DAT_00d21970
  if (obj != 0) {
    obj[1] = 0; // refcount
    obj[2] = 0; // inner
    *obj = &PTR_FUN_00a9f9c4;
    obj[3] = obj[4] = obj[5] = obj[6] = 0;
    *(undefined2 *)(obj + 7) = 0xffff;       // +0x1c
    *(undefined2 *)((int)obj + 0x1e) = 0xffff;
    obj[8] = 0;                               // +0x20
  }
  // bytes: thiscall ECX=pathOrToken, stack (obj, 0)
  int status = FUN_007533e0(/*ecx*/pathOrToken, obj, 0);
  *outStatus = status;
  if (status < 0) {
    if (obj != 0) (**(code **)*obj)(1); // dtor+free
    return 0;
  }
  // bytes: thiscall ECX=manager, stack (obj)
  FUN_0073d830(/*ecx*/manager, obj);
  return obj;
}
```

## Open questions

- Product type name of effect class / manager.
- Full status code domain from `FUN_007533e0` / resource acquire.
- Null freelist + successful load edge (theoretical).
- Runtime / bit-exact.
