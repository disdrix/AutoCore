# Annotated low-level: GuardedVector_ContainsFirstEqual

| Field | Value |
|---|---|
| Stable ID | `aa_0043e5b0` |
| VA | `0x0043e5b0`–`0x0043e62a` (**123 B** / `0x7B`) |
| Ghidra | `FUN_0043e5b0` |
| Canonical | `GuardedVector_ContainsFirstEqual` (**Inferred**) |
| System | STL / guarded ring-deque container |
| Date | 2026-07-23 scaffold; **2026-07-29 W30-B dual seal** |

## Machine-level notes

- **ABI (bytes):** **EAX** = container; stdcall **1** stack formal (`const int*` / dword key); entry `MOV EDI,EAX`; **`RET 0x4`**; return **AL** (0/1).
- **CS gate:** if `*(uint8_t*)(container+0x2c)` → `EnterCriticalSection(container+0x14)`; leave checks same flag via `[ESI+0x18]` where ESI=`container+0x14`.
- **Scan:** half-open absolute range `[begin, begin+size)`; page map `+0x04`, capacity pages `+0x08`; `page = idx>>2` with wrap `page -= capacity` when `capacity <= page`; sub = `idx & 3`.
- **Match:** first `pages[page][sub] == *key` → AL=1; miss → AL=0. **No mutate**.
- **Callees:** `EnterCriticalSection`, `LeaveCriticalSection` only (worker).
- **Callers (5 funcs / 6 sites):** `FUN_00423e00` (AssManager_RemoveResolver), `FUN_007b65d0`, `FUN_007b75b0`, `FUN_00971640`, `FUN_00971900`.
- **Pair:** W28-C `GuardedVector_EraseFirstEqual` (`0x0043c550`) is the erase twin; this is membership-only.

## Pseudocode (annotated)

```c
// EAX=container; stdcall 1; RET 4; AL=found
uint8_t GuardedVector_ContainsFirstEqual(
    GuardedVectorHeader* container /*EAX*/,
    const int* key /*stack*/)
{
  if (container->cs_flag)  // +0x2c
    EnterCriticalSection(&container->cs);  // +0x14

  uint32_t idx = container->begin;          // +0x0c
  uint8_t found = 0;
  uint32_t end = container->size + idx;     // +0x10

  while (idx != end) {
    uint32_t page = idx >> 2;
    uint32_t sub  = idx & 3u;               // via idx + page*(-4)
    if ((uint32_t)container->capacity <= page)
      page -= (uint32_t)container->capacity;
    if (container->pages[page][sub] == *key) {
      found = 1;
      break;
    }
    idx++;
  }

  if (container->cs_flag)
    LeaveCriticalSection(&container->cs);
  return found;
}
```

## Open questions

- Product/PDB class English (`GuardedVector_*` structural; retail map overflow says `deque<T> too long`).
- Element type beyond dword POD equality.
- Runtime / bit-exact under multi-caller parents.
- Null page dereference residual if map hole (bytes do not null-check page before load).
