# Annotated low-level: StdTree_EraseAndRebalance_Val12 (FUN_004e4130)

| Field | Value |
|---|---|
| Stable ID | `aa_004e4130` |
| VA | `0x004e4130` |
| Body | `0x004e4130`–`0x004e43e6` exclusive (**694** B) |
| Canonical | `StdTree_EraseAndRebalance_Val12` (inferred) |
| Ghidra | `FUN_004e4130` |
| System | MSVC std `_Tree` erase + RB rebalance (Val12 node) |
| Date | 2026-07-29 (W27-Q seal; scaffold 2026-07-23) |

## Machine-level notes

- **ABI:** `__thiscall`; **ECX** = map shell (`head` @ `+4`, `size` @ `+8`); stack **`out_it*`**, **`node*`**; **`RET 8`**.
- **Node Val12 (0x1c):** `left@0`, `parent@4`, `right@8`, value 12 B @ `+0x0c`, **`color@+0x18`**, **`isnil@+0x19`**. Color also addressed as `(char)node[6]` (dword index 6 = +0x18).
- **Nil throw:** if `node->isnil != 0` → construct exception with `"invalid map/set<T> iterator"` @ `0x00a152f0` → `_CxxThrowException(..., &DAT_00acc34c)`.
- **Pre-delete:** `FUN_005ae0b0()` (iterator advance / successor prep — peer role of isnil@15's `FUN_00673070`; product name open).
- **Unlink:** choose replacement child (right if both non-nil after left-prefer path; else right when left is nil); reparent; if root/leftmost/rightmost was erased → update via `FUN_005adfa0` (leftmost) / `FUN_00418bf0` (rightmost).
- **RB fixup:** if erased node was **black** (`color == 1`), walk up while replacement black, recolor / `FUN_00573170` Lrotate / `FUN_00418c10` Rrotate; paint final black.
- **Delete + size:** `operator_delete(node)`; if `map->size > 0` then `--size` (**byte-sealed**; decompiler drops due to false noreturn).
- **Out iterator:** store successor shadow into `*out_it` after size update.
- **Decomp gap:** Ghidra lists body end `004e43b6` and marks delete as noreturn — full body continues to `ret 8` at exclusive end `004e43e6`.
- **Peer:** `StdMap_EraseIterator_Inferred` @ `0x004baa30` is the **isnil@+0x15** clone (different rotates / helpers); same 694 B body size coincidence.
- **Caller:** sole `FUN_004e5120` (1 xref).

## Annotated control flow

```c
// __thiscall map*, out_it*, node*  — erase one Val12 tree node
void __thiscall StdTree_EraseAndRebalance_Val12(MapShell* map, Node** out_it, Node* node)
{
  // SEH LAB_009a1e62
  if (node->isnil) throw_invalid_iterator();   // string @ 0x00a152f0

  FUN_005ae0b0();  // advance / capture successor for *out_it

  // Replacement child (classic erase splice; isnil @ +0x19)
  Node* repl = node->left;
  if (!repl->isnil) {
    if (!node->right->isnil)
      repl = node->right;   // both non-nil → start from right (then min later in full MSVC path)
  } else {
    repl = node->right;
  }
  Node* parent = node->parent;
  if (!repl->isnil)
    repl->parent = parent;

  Node* head = map->head;
  if (head->parent == node) head->parent = repl;       // root
  else if (parent->left == node) parent->left = repl;
  else parent->right = repl;

  if (head->left == node) {                            // leftmost
    Node* b = parent;
    if (!repl->isnil) b = FUN_005adfa0(repl);           // min
    head->left = b;
  }
  if (head->right == node) {                           // rightmost
    if (!repl->isnil) head->right = FUN_00418bf0(repl); // max
    else head->right = parent;
  }

  if (node->color == 1) {  // erased black → rebalance
    // while repl != root && repl black: sibling recolor / Lrotate(FUN_00573170) / Rrotate(FUN_00418c10)
    // final: repl->color = black
  }

  operator_delete(node);
  if (map->size > 0) map->size--;
  // *out_it = successor (from FUN_005ae0b0 prep)
  // ret 8
}
```

## Live ≡ raw

Live 2026-07-29 decompile CF ≡ 2026-07-23 raw. **Epilogue size-- / ret 8 sealed via `read_memory`** (not in raw decompile body).

## Open questions

- Product / MSVC demangle for map type of sole caller `FUN_004e5120`.
- Exact product name of `FUN_005ae0b0` / min-max helpers.
- Runtime / bit-exact golden not run.
