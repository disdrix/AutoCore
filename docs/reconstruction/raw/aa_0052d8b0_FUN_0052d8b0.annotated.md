# Annotated low-level: FUN_0052d8b0

| Field | Value |
|---|---|
| Stable ID | `aa_0052d8b0` |
| VA | `0x0052d8b0`–`0x0052da2f` |
| System | `missions-progression` |
| Date | 2026-07-23 scaffold; **2026-07-29 dual seal** |

## Machine-level notes

- Prefer **image** (`read_memory`) when decompiler conflicts (map `this`, resolve `this`, full-clear loop).
- Sibling upsert: `FUN_0052c700` @ `0x0052c700` (same map).
- All live call sites pass **`destroyFlag = 0`**.

## Assembly seals (2026-07-29)

| Fact | Bytes / site |
|---|---|
| Prologue stack frame | `83 EC 0C` `PUSH EBX/EBP/ESI`; `MOV ESI,[ESP+0x20]` = missionId |
| `missionId == -1` → header | `CMP ESI,-1` / `MOV EAX,[EBX+0x50C]` |
| Map this for find | `LEA ECX,[EBX+0x508]` → `CALL FUN_00538a40` |
| Payload base | `LEA EDI,[EBP+0x18]`; counter `4` |
| destroyFlag gate | `CMP byte [flag],0` / `JZ` skip resolve |
| sectorMap gate | `[adj+0xA8]` then `ECX=[sectorMap+0xE4E8]` |
| Resolve | `PUSH hi; PUSH lo; PUSH 1; CALL ResolveObjectTarget` |
| List unlink | `LEA ECX,[EBX+0xCC4]` → `FUN_005875c0` |
| Epilogue | `ADD ESP,0xC; RET 8` (`C2 08 00`) |
| Size clear | `MOV dword [map+8], 0` → `char+0x510` |

## Pseudocode (annotated)

```c
// thiscall Character* ECX; ret 8
// destroyFlag: 0 = map only; non-0 = also destroy staged objects
// missionId: -1 = entire tree; else one key
void __thiscall FUN_0052d8b0(int character, char destroyFlag, int missionId)
{
  // 1) iterator = begin (header) or lower_bound(map@+0x508, key)
  // 2) for each node until end:
  //      for 4 pairs at node+0x18:
  //        if destroyFlag && sectorMap && Resolve(bGlobal=1, lo, hi):
  //          unlink char+0xcc4; FUN_004d4790(obj)
  //      if missionId != -1: erase node; return
  //      else: inorder next
  // 3) if missionId == -1: free all nodes; header empty; size=0
}
```

## Callers (flag, missionId)

| Caller | flag | missionId |
|---|---:|---|
| GiveMission / CompleteObjective | 0 | `*def` / key |
| EndQuest, RecvNpcMissionDialog, RecvCompleteDynamicObjective, interact, ctor, dtor | 0 | -1 |

## Dual

- A: `docs/reconstruction/reviews/A_aa_0052d8b0_ClearMissionStagingTree.md`
- B: `docs/reconstruction/reviews/B_aa_0052d8b0_ClearMissionStagingTree.md`
