# Raw capture: FUN_00409d40

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409d40` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409d40` |
| **Canonical name** | `FUN_00409d40` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00409d40(undefined4 param_1,int param_2)

{
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  puStack_c = &LAB_009bc630;
  local_10 = ExceptionList;
  local_8 = 0;
  ExceptionList = &local_10;
  for (; param_2 != 0; param_2 = param_2 + -1) {
    FUN_00409f30();
  }
  ExceptionList = local_10;
  return;
}
```

---

## WQ9H-B re-verify (2026-08-04) — append only

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **WQ9H-B** |
| **Tools** | `batch_decompile` + `analyze_function_complete` + `read_memory` + callers/xrefs; **no** `disassemble_bytes` |
| **Live decompile ≡ raw CF** | **Yes** (loop `FUN_00409f30` × count; SEH frame) |
| **Body** | `0x00409d40`–`0x00409da2` exclusive (**98 B** / `0x62`) |
| **Named** | `StdVector_ConstructN_Elem0x28_Inferred` |

### Byte ABI (read_memory @ entry)

```
push ebp; mov ebp,esp
SEH prolog (push -1; push LAB_009bc630; fs:[0] chain)
push ebx,esi,edi
mov ebx,ecx              ; EBX = value* (ECX formal)
mov edi,[ebp+0xC]        ; EDI = count
mov esi,[ebp+8]          ; ESI = dest
mov [ebp-4],0            ; SEH state
loop:
  test edi,edi / jbe done
  mov edx,ebx            ; EDX = value*
  mov eax,esi            ; EAX = dest
  call FUN_00409f30      ; PodCopy 10 dwords (0x28)
  sub edi,1
  add esi,0x28           ; advance dest
  jmp loop
done:
  SEH teardown; pop edi,esi,ebx
  ret                    ; C3 (caller cleans 8 B stack)
```

### Callee `FUN_00409f30` (sealed via decompile)

- **EAX** = dest dword*; **EDX**/fastcall src = value*
- Loop **10** dword stores; null dest no-op
- = POD construct of **0x28** bytes (no further advance inside leaf; parent steps +0x28)

### Callers / xrefs

| Site | Function | Role |
|---|---|---|
| `0x00406e5c` | `FUN_00406e50` Ufill elem0x28 | construct-N then return end |
| `0x00406271` | `FUN_00406220` push_back fast | construct 1 at end |

### Scaffold gaps closed

- Decomp omits ECX value formal and `add dest,0x28` — sealed by bytes
- Signature is **ECX=value\***, stack **dest + count**, plain **ret**
