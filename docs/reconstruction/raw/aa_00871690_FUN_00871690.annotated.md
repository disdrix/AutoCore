# Annotated low-level: UiHost_FindTrackedPairNode_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00871690` |
| VA | `0x00871690` |
| Ghidra | `FUN_00871690` |
| Canonical | `UiHost_FindTrackedPairNode_Inferred` |
| System | client UI / vehicle-garage host residual |
| Date | 2026-07-29 (W21-R dual seal) |

## Machine-level notes

- **ABI (bytes):** `mov esi,ecx` → `__thiscall`; stack `keyA`/`keyB`; exit `ret 8`.
- **List base:** `add esi, 0x628` then `List_TraversalLock` (`FUN_004294f0`) on `this+0x628`.
- **CS / lock flag:** CS at `this+0x62c` (= list+4); entered flag at `this+0x650` (= list+0x28). Matches `LeaveCriticalSection` path.
- **Iterate:** `FUN_004113b0` walks locked list; out-node via stack local.
- **Match:** `node != 0 && node[+8] == keyA && node[+0xc] == keyB` → return node; end-of-list → return 0 (after unlock if locked).
- **Callees:** `FUN_004294f0`, `FUN_004113b0`, `LeaveCriticalSection` only.
- **Callers:** `FUN_008718a0` (ClearTrackedPairAndRefresh), `FUN_008718d0`, `FUN_008719d0`.
- Prefer `read_memory` over disassembly; live decompile **≡** 2026-07-23 raw.

## Pseudocode (annotated)

```c
// thiscall ECX = host; stack keyA, keyB; RET 8
// returns matching list node* or NULL
int* __thiscall UiHost_FindTrackedPairNode(UiHost* this, int keyA, int keyB)
{
  int done;          // FUN_004113b0 result: non-zero = end/error
  int* node = NULL;
  int unused = 0;    // local_4 zeroed (iterator residual)

  List_TraversalLock(this + 0x628);          // FUN_004294f0; EnterCS list+4, flag list+0x28
  done = List_IterateNext(this + 0x628, &node);  // FUN_004113b0
  for (;;) {
    if (done != 0) {
      if (this->lockHeld_650) {              // *(char*)(this+0x650)
        this->lockHeld_650 = 0;
        LeaveCriticalSection(this + 0x62c);
      }
      return NULL;
    }
    if (node != NULL && node[2] == keyA && node[3] == keyB)  // +8 / +0xc
      break;
    done = List_IterateNext(this + 0x628, &node);
  }
  if (this->lockHeld_650) {
    this->lockHeld_650 = 0;
    LeaveCriticalSection(this + 0x62c);
  }
  return node;
}
```

## Offsets (host)

| Off | Role |
|----:|------|
| `+0x628` | Locked list head (arg to TraversalLock / Iterate) |
| `+0x62c` | `CRITICAL_SECTION` (list+4) |
| `+0x650` | Lock-held byte (list+0x28) |

## Node fields

| Off | Role |
|----:|------|
| `+0x00` | payload / flag dword (cleared by sibling `FUN_008718a0`) |
| `+0x08` | keyA |
| `+0x0c` | keyB |

## Open questions

- Product host class / node struct English names.
- Semantic type of keyA/keyB (TFID pair vs inventory slot ids — **Tentative**).
- Whether `FUN_004113b0` end code `0x80070005` path can surface here (not observed in this body).
