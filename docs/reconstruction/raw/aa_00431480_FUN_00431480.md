# Raw capture: FUN_00431480

| Field | Value |
|---|---|
| **Stable ID** | `aa_00431480` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00431480` |
| **Canonical name** | `FUN_00431480` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall
FUN_00431480(undefined4 param_1,undefined4 param_2,undefined4 param_3,undefined4 param_4)

{
  FUN_00431790(param_2,param_3,param_4,param_1);
  return;
}
```

---

## W33-I append (2026-07-29) — decompile + read_memory seal

| Field | Value |
|---|---|
| **Tools** | `batch_decompile` / `decompile_function`, `read_memory`, callers/xrefs. **No** `disassemble_bytes`. |
| **Body** | `0x00431480`–`0x0043149d` exclusive (**29 B** / `0x1D`); pad `CC` then next fn |
| **ABI (call-site)** | **cdecl** 3 stack args: `first*`, `last*`, `dest_end*`; plain **`RET`** (`C3`); void. Caller (`FUN_004306b0` @ `0x00430984`) pushes 3, later `ADD ESP,18` cleans this + fill call. |
| **ABI (body / Ghidra)** | Entry `PUSH ECX`; reorders stack → free `FUN_00431790(first, last, dest_end)` and cleans **`ADD ESP,14`** (4 pushes + entry push). ECX not set by sole caller — dead reordering scratch; **semantics = 3-arg copy-backward**. |
| **Callee** | `FUN_00431790` @ `0x00431790` — `copy_backward` on `basic_string` stride **0x1c** via same IAT `operator=` |
| **Callers** | Sole: `StdVector_InsertN_BasicString` (`FUN_004306b0`) @ `0x00430984` |
| **Name** | `BasicString_CopyBackward_Thunk_0x1c` (**Inferred**) |
| **Role** | Thin trampoline to range copy-backward used in vector insert mid-shift |

### Body hex (29 B)

```
51 8B 04 24 8B 4C 24 10 8B 54 24 0C 50 8B 44 24 0C 51 52 50
E8 F7 02 00 00 83 C4 14 C3
```

### Callee `FUN_00431790` (not dual-owned; sealed for role only)

```
if (first == last) return dest_end;
do {
  last -= 0x1c; dest_end -= 0x1c;
  operator=(dest_end, last);   // *dest = *src
} while (last != first);
return dest_end;
```

Plain `RET` cdecl 3-arg; body hex starts `53 8B 5C 24 08 56 8B 74 24 10 …`.

### Parent use (insert mid path)

```
// after forward-move of tail count elems beyond old end:
FUN_00431480(where, end - count, old_end);  // copy_backward remainder → open hole
FUN_00431450(where, where + count, value);  // assign-fill hole
```
