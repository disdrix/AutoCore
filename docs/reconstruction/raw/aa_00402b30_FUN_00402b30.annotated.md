# Annotated low-level: StdMap_InsertOrFind_UintKey_Isnil31_EaxEbx_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00402b30` |
| VA | `0x00402b30`–`0x00402be6` inclusive (**183 B** / `0xB7`) |
| Ghidra | `FUN_00402b30` |
| Canonical | `StdMap_InsertOrFind_UintKey_Isnil31_EaxEbx_Inferred` |
| System | missions-progression host map; unit = generic MSVC std map insert-or-find |
| Date | 2026-08-05 (MEGA-040 dual refresh) |
| Rejected scaffold | `Named_CalleeOf_Client_NotifyActiveMissionChanged_00402b30` |

## Machine-level notes

- Prefer **assembly** when decompiler invents `in_EAX` / `unaff_EBX` phantoms — both are real register formals.
- **ABI:** `EAX` = map shell, `EBX` = value* (key dword0), stack = out `{it*, inserted@+4}`, **`RET 4`**, returns out in `EAX`.
- **Not** thiscall (map is not ECX). Same register ABI family as dualed isnil15 `StdMap_InsertOrFind_UintKey_Isnil15_EaxEbx_Inferred` (`0x00458510`).
- Tree family: **isnil @ +0x31**, **color @ +0x30**, **key @ +0x10**, node size **0x38** (peer BuyHead / Lrotate / erase isnil31).
- Compare is **unsigned** (`SETC` / `JNC`).
- Equal key: return existing iterator, `inserted=0`, **do not rewrite** mapped payload.
- Callees: insert+RB `FUN_00403250` (unOWN); predecessor `FUN_00404290`.
- Primary host: `Client_NotifyActiveMissionChanged` (`0x00944770`) map at **client+0xf14** (2 sites); thin wrapper `FUN_00933310` (1 site).

## Pseudocode (annotated; ABI corrected)

```c
// EAX = MapShell* map
// EBX = const Val* value   (key = value->key as uint32)
// stack: InsertPair* out
// RET 4; returns out
InsertPair *StdMap_InsertOrFind_UintKey_Isnil31_EaxEbx_Inferred(
    MapShell *map /*EAX*/, InsertPair *out /*stack*/, const Val *value /*EBX*/)
{
  Node *head = map->head;                 // [map+4]
  Node *node = head->parent;              // root = [head+4]
  bool goLeft = true;

  if (node->isnil == 0) {                 // isnil @ +0x31
    Node *cur = node;
    do {
      node = cur;
      goLeft = (value->key < node->key);  // unsigned; key @ +0x10
      cur = goLeft ? node->left : node->right;
    } while (cur->isnil == 0);
  }

  Node *hint = node;

  if (goLeft) {
    if (node == head->left) {             // insert at begin
      Node *r = FUN_00403250(&hint, /*addLeft*/1, value);
      out->it = r;
      out->inserted = 1;
      return out;
    }
    FUN_00404290(/*pred on &hint*/);      // predecessor
    // hint / node refreshed from local after pred
  }

  if (hint->key < value->key) {           // unsigned; miss → insert
    Node *r = FUN_00403250(&hint, goLeft, value);
    out->it = r;
    out->inserted = 1;
    return out;
  }

  out->it = hint;                         // equal hit
  out->inserted = 0;
  return out;
}
```

## Open questions

- Product / English type of host map at client+`0xf14` (mission debug queue entries — key appears as mission object pointer in NotifyActiveMissionChanged).
- Full dual of insert worker `FUN_00403250` and predecessor `FUN_00404290` (unOWN).
- Runtime / bit-exact / differential.
