# Raw capture: FUN_00405920

| Field | Value |
|---|---|
| **Stable ID** | `aa_00405920` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00405920` |
| **Canonical name** | `FUN_00405920` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00405920(int param_1,code *param_2,undefined4 param_3)

{
  undefined4 *puVar1;
  undefined4 *puVar2;
  undefined4 *puVar3;
  char cVar4;
  
  EnterCriticalSection((LPCRITICAL_SECTION)(param_1 + 0x28));
  EnterCriticalSection((LPCRITICAL_SECTION)(param_1 + 0x10));
  puVar3 = *(undefined4 **)(param_1 + 4);
  puVar2 = (undefined4 *)0x0;
  do {
    puVar1 = puVar3;
    if (puVar1 == (undefined4 *)0x0) {
LAB_0040599d:
      LeaveCriticalSection((LPCRITICAL_SECTION)(param_1 + 0x10));
      LeaveCriticalSection((LPCRITICAL_SECTION)(param_1 + 0x28));
      return;
    }
    cVar4 = (*param_2)(puVar1[1],param_3);
    if (cVar4 != '\0') {
      if (puVar2 == (undefined4 *)0x0) {
        *(undefined4 *)(param_1 + 4) = puVar1[2];
      }
      else {
        puVar2[2] = puVar1[2];
      }
      if (puVar1[2] == 0) {
        *(undefined4 **)(param_1 + 8) = puVar2;
      }
      puVar1[1] = 0;
      (**(code **)*puVar1)(1);
      InterlockedDecrement((LONG *)(param_1 + 0xc));
      goto LAB_0040599d;
    }
    puVar3 = (undefined4 *)puVar1[2];
    puVar2 = puVar1;
  } while( true );
}
```

---

## MEGA-042 live re-verify (APPEND ONLY) — 2026-08-05

**Agent:** OWN-ONLY dual **MEGA-042**  
**Tools:** `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_callers` + `get_function_xrefs` + `get_assembly_context` + `read_memory` + `batch_decompile` (callers) + `analyze_control_flow`. **No** `disassemble_bytes`. No Launcher.  
**Image:** `autoassault.exe` base `0x400000`.

### Live decompile (2026-08-05) ≡ scaffold CF

```c
void __thiscall FUN_00405920(int param_1,code *param_2,undefined4 param_3)
{
  undefined4 *puVar1;
  undefined4 *puVar2;
  undefined4 *puVar3;
  char cVar4;
  
  EnterCriticalSection((LPCRITICAL_SECTION)(param_1 + 0x28));
  EnterCriticalSection((LPCRITICAL_SECTION)(param_1 + 0x10));
  puVar3 = *(undefined4 **)(param_1 + 4);
  puVar2 = (undefined4 *)0x0;
  do {
    puVar1 = puVar3;
    if (puVar1 == (undefined4 *)0x0) {
LAB_0040599d:
      LeaveCriticalSection((LPCRITICAL_SECTION)(param_1 + 0x10));
      LeaveCriticalSection((LPCRITICAL_SECTION)(param_1 + 0x28));
      return;
    }
    cVar4 = (*param_2)(puVar1[1],param_3);
    if (cVar4 != '\0') {
      if (puVar2 == (undefined4 *)0x0) {
        *(undefined4 *)(param_1 + 4) = puVar1[2];
      }
      else {
        puVar2[2] = puVar1[2];
      }
      if (puVar1[2] == 0) {
        *(undefined4 **)(param_1 + 8) = puVar2;
      }
      puVar1[1] = 0;
      (**(code **)*puVar1)(1);
      InterlockedDecrement((LONG *)(param_1 + 0xc));
      goto LAB_0040599d;
    }
    puVar3 = (undefined4 *)puVar1[2];
    puVar2 = puVar1;
  } while( true );
}
```

### Body / ABI (bytes sealed)

| Item | Value |
|------|-------|
| Entry | `0x00405920` |
| Last insn | `0x004059b7` `RET 0x8` (`C2 08 00`) |
| Inclusive end | `0x004059b9` |
| Size | **154 B** / `0x9A` (`analyze_control_flow`) |
| Pad | `CC` from `0x004059ba` |
| this | **ECX** → EDI (`MOV EDI,ECX`) |
| stack arg0 | predicate `bool (*)(payload, cookie)` @ `[ESP+4]` entry |
| stack arg1 | cookie / match key @ `[ESP+8]` entry |
| return | void (no meaningful EAX) |
| cleanup | **`RET 8`** |
| CF metrics | cyclomatic 11; 13 BB; 1 loop; 7 calls; 1 return |

### Import IAT (read via body)

| VA | Role |
|----|------|
| `DAT_009c61bc` | `EnterCriticalSection` (loaded ESI, called ×2) |
| `DAT_009c61b8` | `LeaveCriticalSection` (loaded ESI, called ×2) |
| `DAT_009c61f4` | `InterlockedDecrement` (called once on match) |

### Layout (matches dualed CsSList family)

| Off | Field |
|----:|-------|
| list+0x04 | head* |
| list+0x08 | tail* |
| list+0x0C | count (`InterlockedDecrement` on remove) |
| list+0x10 | `CRITICAL_SECTION` inner (always) |
| list+0x28 | `CRITICAL_SECTION` outer (always here; no count≤3 gate) |
| node+0x00 | vtbl* |
| node+0x04 | payload* (passed to predicate) |
| node+0x08 | next* |

### Semantics (sealed)

1. Enter outer CS (`+0x28`) then inner CS (`+0x10`).
2. Walk head→next singly-linked list.
3. Call `predicate(node->payload, cookie)`; if **false**, advance (prev=node).
4. On **first true**:
   - Unlink: head = next if prev null else prev->next = next.
   - If next null → tail = prev.
   - Clear payload; `node->vtbl[0](1)` thiscall delete-this (scalar deleting dtor).
   - `InterlockedDecrement(list+0xC)`.
   - Leave both CS (inner then outer) and return.
5. Exhaust list without match → leave both CS and return.
6. **Removes at most one** node per call.

### Callers / xrefs (live)

| Site | Function | Context |
|------|----------|---------|
| `0x004d4855` | `FUN_004d4790` (Object_QueueDelete path) | `MOV ECX,[EDI+0xe648]`; `PUSH ESI`; `PUSH 0x4cd580`; CALL — after `CsSList_ContainsPayload` true |
| `0x00944d8f` | `FUN_00944d50` | `MOV ECX,[…+0xe5fc]`; `PUSH EBP`; `PUSH 0x92d8e0`; CALL |
| `0x00944e1e` | `FUN_00944d50` | second remove with related object cookie |

Xrefs: **3** UNCONDITIONAL_CALL. Callers: **2** functions.

### Predicate stubs (not functions; `read_memory`)

Both `0x004cd580` and `0x0092d8e0`:

```text
8B 44 24 04    MOV EAX,[ESP+4]   ; payload
3B 44 24 08    CMP EAX,[ESP+8]   ; cookie
0F 94 C0       SETE AL
C2 08 00       RET 8
```

→ pointer-equality predicates used by both live call sites.

### Family

Twin of dualed:
- `CsSList_AppendPayload_Inferred` `0x004024d0` (aa_004024d0)
- `CsSList_ContainsPayload_Inferred` `0x004e15a0` (aa_004e15a0)

### Name decision

`CsSList_RemoveFirstByPredicate_Inferred` — product class English open → `_Inferred`.  
Retire scaffold `Named_CalleeOf_Named_CalleeOf_Client_SendInventoryAddItem_00405920`.

### Integrity

Scaffold raw body **preserved** above. This section is **append-only** re-verify. Terminal **false**.
