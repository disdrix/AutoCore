# Annotated low-level: FUN_008ac890 → UI_MissionDialog_SetResponseCountAndRebuild_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_008ac890` |
| VA | `0x008ac890`–`0x008ac8af` exclusive (**31** B) |
| System | missions-progression / client UI mission dialog |
| Date | 2026-08-05 (MEGA-109 OWN-ONLY dual refresh) |
| Ghidra symbol | `FUN_008ac890` |
| Inferred name | `UI_MissionDialog_SetResponseCountAndRebuild_Inferred` |
| Peer | dualed `UI_MissionDialog_BuildResponseButtons_Inferred` (`0x008ac110`) |

---

## Machine-level notes

- **Live tools:** `decompile_function`, `analyze_function_complete` (name), `disassemble_function`, callers/xrefs, `read_memory`. **No** `disassemble_bytes`.
- Decompiler CF ≡ bytes for store / clamp / call.
- **ABI correction vs pure thiscall:** count arrives in **EAX**; this in **ECX** → EDI for peer.
- Field `+0x510` is dualed as **response_count** (loop bound of BuildResponseButtons).
- Immediate **8** = hard max response buttons in chrome strip.
- Bare `RET` — no stack args, no `RET n`.

---

## Pseudocode (annotated)

```c
// ECX = MissionDialogUi* dialog
// EAX = requested_response_count
void UI_MissionDialog_SetResponseCountAndRebuild_Inferred(void /* ECX this, EAX count */)
{
  // CMP EAX, 8 ; PUSH EDI ; MOV EDI, ECX
  dialog->response_count /* +0x510 */ = requested_count; // EAX
  if (requested_count > 8) {
    dialog->response_count = 8; // clamp
  }
  // EDI = dialog for peer (EDI-this ABI)
  UI_MissionDialog_BuildResponseButtons_Inferred(); // FUN_008ac110
  // POP EDI ; RET
}
```

---

## Call graph (OWN unit + evidence)

```text
FUN_008ac8b0  (NUM_RESPONSES XML prop @ 0x008acb02)
  └─ FUN_008ac890  [OWN MEGA-109]
       └─ FUN_008ac110  UI_MissionDialog_BuildResponseButtons_Inferred [dualed WQ7R-B]

ASCII NUM_RESPONSES path @ 0x008ad224
  └─ FUN_008ac890

FUN_0093e450 @ 0x0093e6e9
  ├─ FUN_005795b0  vector size (end-begin)>>2 → EAX
  └─ FUN_008ac890  (ECX=dialog EBX)
```

---

## Open questions / gaps

- Product / PDB English spelling of this wrapper.
- Full dual of `FUN_008ac8b0` / `FUN_0093e450` (not OWN).
- Whether max **8** is design-time only or also server-enforced (UI-side only observed here).
- Runtime / bit-exact / differential: **OPEN** (no Launcher).
