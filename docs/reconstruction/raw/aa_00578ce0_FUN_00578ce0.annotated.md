# Annotated low-level: FUN_00578ce0 → Skill_HB_OnEnd_ClearTargetFxAndCastBinding_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00578ce0` |
| VA | `0x00578ce0`–`0x00578de0` exclusive |
| System | `skills-abilities` / heartbeat |
| Date | 2026-08-04 WQ9D-F dual |
| Tools | `decompile_function` + `read_memory` + callers/xrefs (**no** `disassemble_bytes`) |

---

## Machine-level notes

- **Role:** Shared skill HB **OnEnd** (`vtbl+0x10`) teardown — inverse of `Skill_HB_FireTail_FxTargetAndPeriodStamp` (`0x00578b30`).
- **ABI:** `__thiscall` / ECX=this skill HB; **no** stack args; epilogue **tail-jmp** `0x005081f0` (`CVOGHBBase_EmptyVFunc`), not a true call.
- **PTR_FUN_00af31e0:** static image → empty `FUN_004648a0`; runtime rebind (`Client_InitInstance`) → `FUN_00802b70` (UI/target-selection cleanup when skill HB ends). Twin of FireTail's `PTR_FUN_00af31e4`.
- **ResolveObjectTarget:** before call, ECX ← `*(*(this+0x688)+0xe4e8)` (world lookup host). Stack packs TFID fields `+0x658/+0x65c/+0x660` (decomp `CONCAT31` is byte-merge noise on `edx`).
- **FUN_00578ac0:** destroys pointer vector at `this+0x6b0…+0x6b8` (delete elements + free buffer).
- **Owner notify:** `owner->vtbl+0x238(1, skillId@+0x620, rank=(i16)(+0x61a)+(+0x198), 0, target, 0,0,0)`.
- **Flag `+0x639` bit `0x10`:** `owner+0x26--` (FireTail does `++`).
- **Flag bit `0x02`:** after target `vtbl+0x214` non-null → `Skill_ClearCastBindingAndMaybeRestartCd` with skillId `+0x620` and TFID dwords `+0x678…+0x684` (FireTail **inserts** via `FUN_00518ca0` / insert dual).
- **Gate `+0x140`:** target `vtbl+0x210(0)` → `FUN_0051f880(0)` sets result `+0x6b8=0` (FireTail passes **1**).

## Pseudocode (annotated)

```c
// __thiscall  // ECX = skill HB; OnEnd vtbl+0x10
void Skill_HB_OnEnd_ClearTargetFxAndCastBinding_Inferred(void* hb)
{
  // 1) Client/UI end hook (runtime-rebound pointer; static empty)
  (*PTR_FUN_00af31e0)(hb, 0);

  // 2) Resolve primary target from HB TFID pack; ECX = world+0xe4e8 host
  void* world = *(void**)((char*)hb + 0x688);
  void* mapHost = *(void**)((char*)world + 0xe4e8);
  int* target = CVOGReaction_ResolveObjectTarget(/* ECX=mapHost, TFID @ +0x658.. */);

  // 3) Tear down HB-owned pointer vector @ +0x6b0
  FUN_00578ac0(hb);

  void* owner = *(void**)((char*)hb + 0x18);
  if (owner != nullptr) {
    // 4) Owner skill-end notify (mode=1) + optional refcount dec
    int skillId = *(int*)((char*)hb + 0x620);
    int rank = (int)(short)(*(short*)((char*)hb + 0x61a) + *(short*)((char*)hb + 0x198));
    (*(owner->vtbl + 0x238))(owner, 1, skillId, rank, 0, target, 0, 0, 0);
    if ((*(uint8_t*)((char*)hb + 0x639) & 0x10) != 0)
      (*(char*)((char*)owner + 0x26))--;
  }

  if (target != nullptr) {
    // 5) Clear active cast binding when flag 0x02 (inverse of FireTail insert)
    int nested = target->vtbl[0x214/4](target);
    if (nested != 0 && (*(uint8_t*)((char*)hb + 0x639) & 0x02) != 0) {
      void* castOwner = /* nested +4 chain +4 */;
      Skill_ClearCastBindingAndMaybeRestartCd(
          castOwner, *(void**)((char*)hb + 0x620),
          *(int*)((char*)hb + 0x678), *(int*)((char*)hb + 0x67c),
          *(int*)((char*)hb + 0x680), (char)*(int*)((char*)hb + 0x684));
    }
    // 6) Optional secondary clear: +0x140 → vtbl+0x210(0) → set +0x6b8 = 0
    if (*(char*)((char*)hb + 0x140) != 0) {
      int obj = target->vtbl[0x210/4](target, 0);
      if (obj != 0)
        FUN_0051f880(obj, 0);  // *(obj+0x6b8) = 0
    }
  }

  // 7) Tail to empty base OnEnd
  goto CVOGHBBase_EmptyVFunc;  // 0x005081f0
}
```

## Symmetry with FireTail (`0x00578b30`)

| Stage | FireTail (fire) | This OnEnd |
|---|---|---|
| Hook PTR | `00af31e4` | `00af31e0` |
| Resolve target | yes | yes |
| Vector +0x6b0 | (not in FireTail head) | **clear** `FUN_00578ac0` |
| Flag `0x10` / owner+0x26 | **++** | **--** |
| Flag `0x02` / cast map | **Insert** | **Clear** + maybe restart CD |
| Gate +0x140 / `FUN_0051f880` | **1** | **0** |
| Period / FX stamp | yes | no |

## Open questions

1. Product/PDB name of this OnEnd body.
2. Exact English for owner `vtbl+0x238` and `owner+0x26` counter.
3. Product type of nested cast-map owner from `vtbl+0x214`.
4. Runtime / bit-exact open (no Launcher).
