# Raw capture: FUN_004e2940

| Field | Value |
|---|---|
| **Stable ID** | `aa_004e2940` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004e2940` |
| **Canonical name** | `FUN_004e2940` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_004e2940(void *param_1)

{
  if (*(char *)((int)param_1 + 0x19) == '\0') {
    FUN_004e2940(*(undefined4 *)((int)param_1 + 8));
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return;
}
```
## Re-verify (W29-L OWN dual) — 2026-07-29

| Field | Value |
|---|---|
| **Tools** | `decompile_function` / `batch_decompile` + `read_memory` + `get_function_by_address` + xrefs. **No** `disassemble_bytes` |
| **Body (bytes)** | `0x004e2940`–`0x004e2975` exclusive (**53 B**); epilogue `C2 04 00`; pad `CC` after. Ghidra listed end `004e2974` (short by last ret imm byte) |
| **ABI** | Stack `node*`; **`ret 4`**. ECX saved/threaded on recursive call but **unused for loads** (same pattern as isnil15 peer `004ba770`) |
| **Layout** | left@+0; right@+8; **isnil@+0x19** (Val12 family) |
| **CF (bytes override decompiler)** | If isnil → return. Else: recurse free **right**; `operator_delete` current; walk **left**; loop until isnil. Decompiler **false-noreturn** on delete and **omits left walk** |
| **Callees** | self; `operator_delete` @ `0x00489222` |
| **Callers** | `FUN_004e5120` (`StdMap_EraseRange_Val12`) @ `004e513c`; self; additional sites `004d968e`, `004d96de` (no named function in image) |
| **Name** | `StdMap_FreeSubtree_Val12` (peer `StdMap_FreeSubtree_Isnil15_Inferred`) |

### Live decompile residual (authoritative raw above; incomplete)

Scaffold raw body retained. Live Ghidra still collapses left-walk after `operator_delete` with false noreturn warning — **bytes authoritative**.
