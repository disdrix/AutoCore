# Annotated low-level: FUN_004363b0 / BitVec_EnsureOneAndLocateRef_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004363b0` |
| VA | `0x004363b0`–`0x0043640f` exclusive (**95 B**) |
| System | Bit vector / packed bit storage |
| Date | 2026-08-04 (W38-C) |
| Canonical | `BitVec_EnsureOneAndLocateRef_Inferred` |

## Machine-level notes

- **Decompiler traps:** `in_EAX` and `unaff_EDI` are real formals (host / out-ref), not accidental. `param_3` forwarded to ensure; ebp zeroed so no extra bias.
- **Storage layout:** host`+8` = begin of dword-packed bit words (same family as bitset callers of dword erase `00410490` via `004365e0`).
- **Bit ref proxy:** two dwords `{bit:0..31, word*}`.
- **Ensure first:** always calls `FUN_004364b0` with **count=1** in EAX before locate — handles growth/realloc so locate uses post-ensure begin.
- **ABI:** not pure thiscall — host in **EAX**, out in **EDI**, three stack args, **ret 0xC**, returns out* in EAX.
- **Sibling:** `FUN_00436410` more complex insert-range + locate (not OWN).
- Sole static caller: gfxBody unserialize path `FUN_00765740`.

## Recovered CF

```c
// EAX=host*, EDI=out{bit,word*}*, stack (bitAdd, dwordPos*, param3); ret 0xC; returns out*
uint32_t* BitVec_EnsureOneAndLocateRef_Inferred(
    void* host, int bitAdd, uint32_t* dwordPos, uint32_t param3, uint32_t* out)
{
  uint32_t* old_begin = *(uint32_t**)((char*)host + 8);
  // thiscall FUN_004364b0(host, bitAdd, dwordPos, param3) with EAX=1
  FUN_004364b0(/*ecx=*/host, bitAdd, dwordPos, param3 /*, eax=1*/);
  uint32_t* begin = *(uint32_t**)((char*)host + 8);
  uint32_t bitIndex = (uint32_t)(((int)dwordPos - (int)old_begin) >> 2) * 32u + (uint32_t)bitAdd;
  out[0] = bitIndex & 31u;
  out[1] = (uint32_t)(uintptr_t)(begin + (bitIndex >> 5));
  return out;
}
```

## Open questions

- Product type name (`vector<bool>`-like vs custom bitset).  
- Exact meaning of param_3 (flag / fill / width) — forwarded only.  
- Full dual of `FUN_004364b0` / `00436510` / `00436920`.  
- Runtime / bit-exact.
