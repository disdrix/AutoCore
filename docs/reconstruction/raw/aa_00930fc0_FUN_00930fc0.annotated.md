# Annotated low-level: Ui_ResolveEntityNameColor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00930fc0` |
| VA | `0x00930fc0`–`0x009313bf` |
| System | client UI / entity label color |
| Date | 2026-08-04 (WQ9D-J dual seal) |
| Ghidra name | `FUN_00930fc0` |
| Proposed name | `Ui_ResolveEntityNameColor_Inferred` |

## Machine-level notes

- **ABI:** `__thiscall`; ECX = entity; 4 stack args; **`ret 16`** (`C2 10 00`).
- **Out:** `*outArgb` packed **AARRGGBB** dword (D3D-style).
- **SEH:** full frame (`LAB_009ac684`); ExceptionList restore on all exits.
- Prefer dual-sealed CF over raw iVar paste for ports.
- Callee `FUN_00930f40` is a **fixed ARGB palette** by small integer mode (not OWN).
- Level-diff color uses `FUN_0092d580` (band table of float4 on host) × `255.0f`.

## Control-flow map

```
null entity ──────────────────────────────► palette default
entity.sub(+0x210).field+0x6b4 > 0 ───────► purple 0xFFC364EB
no local player (ctx+0xe98) ──────────────► palette
entity is local self / privileged peer ───► palette (+ optional +0xcb0 load)
else:
  race gate fail ─────────────────────────► palette
  vehicle crew contains entity ───────────► palette (suppress special)
  dead/ghost (0040b1b0) ──────────────────► gray 0xFFEBEBEB
  owner TFID == local+0x164 ──────────────► lime 0xDFCAFF0B
  local.vtbl+0x298(entity)==0 ────────────► palette
  flagForcePalette ───────────────────────► palette
  has identity (+0x214) ──────────────────► level-band RGB pack A=FF
  else ───────────────────────────────────► palette
*outArgb = chosen; return
```

## Key field offsets (local player at ctx+0xe98)

| Offset | Use |
|---|---|
| local root via shared-base adjust | self identity compare |
| `+0x250` | selected/target object |
| `+0x6b9` | flag gating target path |
| `+0xa4` | peer/related object pointer |
| `+0x164` | owner TFID slot (16 B region start) |
| `+0xcb0` | vehicle/crew host for `VehicleCrew_ContainsMember` |
| entity `vtbl+0x210` | sub-object (vehicle/plant?) |
| entity `vtbl+0x1d8` | corpse/state carrier for dead test |
| entity `vtbl+0x214` | TFID / identity provider |
| entity `vtbl+0x27c` | level (int) |
| local `vtbl+0x298` | relationship/hostility predicate |
| local `vtbl+0x27c` | local level |

## Pseudocode (annotated)

```c
// See clean: reconstructed-exact/Ui_ResolveEntityNameColor_Inferred.cpp
// Raw authoritative body: aa_00930fc0_FUN_00930fc0.md
```

## Gaps

1. Product/PDB symbol for color resolver and palette modes.
2. Exact English for `+0x6b4` purple gate (PvP? bounty? group?).
3. Semantic of `flagAllowSelfStyle` / `flagForcePalette` product names.
4. Full type of `clientCtx` beyond `+0xe98` local root.
5. Runtime / bit-exact open.
