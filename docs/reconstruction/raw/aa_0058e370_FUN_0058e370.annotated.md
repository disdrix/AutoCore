# Annotated low-level: FUN_0058e370 → CVOGObject_SetGfxBodyLink_Mode1AndTarget

| Field | Value |
|---|---|
| Stable ID | `aa_0058e370` |
| VA | `0x0058e370` |
| Body | `0x0058e370`–`0x0058e398` (40 B) |
| Canonical | `CVOGObject_SetGfxBodyLink_Mode1AndTarget` (**Inferred**) |
| System | client CVOG object / gfxBody link packet |
| Date | 2026-07-29 (W20-M dual seal; prior scaffold 2026-07-23) |

## Machine-level notes

- **`this+0x914`** = **gfxBody** pointer on a CVOG object. Proven by creator `FUN_0058f780` string:  
  `"CVOGFXGeometry particle already has a gfxBody <%s>, tried changing it to <%s>"`  
  and assignment `*(this+0x914) = new CVOGFX…`.
- **`gfxBody+0x78`** = pointer to a small **link packet** (allocated `operator_new(0xC)` at create time in `0058f780`):
  - `+0x00` **byte mode** (create defaults to **9**; this unit writes **1**)
  - `+0x04` **dword/pointer** (create defaults to **self**; this unit writes **target arg**)
- Dual null gates: no-op if gfxBody null or packet null.
- **ABI:** `__thiscall` (`this` in ECX), one stack arg, `ret 4`.
- **Single caller:** `FUN_004b73c0` @ `0x004b7408` — walks manager list at `+0x204`, and for each non-null entry value calls this with the selected target entity as arg.

## Pseudocode (annotated)

```c
// __thiscall; ret 4
void CVOGObject_SetGfxBodyLink_Mode1AndTarget(void *self, void *target)
{
  void *gfxBody = *(void **)((char *)self + 0x914);
  if (gfxBody != NULL) {
    uint8_t *packet = *(uint8_t **)((char *)gfxBody + 0x78);
    if (packet != NULL) {
      packet[0] = 1;                          // mode = 1
      *(void **)(packet + 4) = target;        // associated object
    }
  }
}
```

## Sibling modes (context, not this unit)

| VA | Mode byte | +4 value | Role sketch |
|---|---|---|---|
| `0x0058f780` create | **9** | self | init packet on gfxBody attach |
| `0x00590f00` / `0x00593e10` teardown | **9** | self | reset packet before cleanup |
| **`0x0058e370` (this)** | **1** | **target arg** | arm link toward external object |

## Open questions

- Product name of the 0xC packet / meaning of mode enum (1 vs 9).
- Exact type of `target` beyond "object pointer from `004b73c0` param_2".
- Whether mode 1 is consumed by a gfx tick/render path (not sealed here).
