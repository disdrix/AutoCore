# Raw capture: FUN_005081f0

| Field | Value |
|---|---|
| **Stable ID** | `aa_005081f0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005081f0` |
| **Body range** | `0x005081f0` only (1 byte `c3`; exclusive end `0x005081f1`) |
| **Canonical name** | `CVOGHBBase_EmptyVFunc` (structural; Ghidra `FUN_005081f0`) |
| **System** | heartbeat / timed-actions (base empty leaf; multi-caller nop) |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_005081f0(void)

{
  return;
}
```

---

## Live re-verify append (2026-07-29 W18-A)

| Check | Result |
|---|---|
| `decompile_function` @ `0x005081f0` | empty `return;` — **≡** raw |
| `get_function_by_address` | Body `005081f0`–`005081f0` |
| `read_memory` length 32 | **`c3`** then `cc` pad; next distinct leaves nearby |
| ABI | bare **`ret`** (`c3`, not `ret n`); ignores ECX / stack args |
| Callees | none — classification **stub** |
| DATA xrefs | base HB vtbl `0x009cdab0` **slot +0x14** = this; many other DATA slots |
| Code xrefs | CALL/JMP from HB OnEnd tails (e.g. `CVOGHBOKToCastAgain_OnEnd` `0x0051e3c6`), list helpers, other empty-tail sites |

**Not the same VA as** mass shared `EmptyRet` @ `0x0056f570` or neighbor empty @ `0x005081e0` (`c3` one byte earlier).
