# Raw capture: FUN_004130d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004130d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004130d0` |
| **Canonical name** | `FUN_004130d0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_004130d0(void *param_1,byte param_2)

{
  FUN_0087b7d0();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```

---

## R12-010 re-verify (2026-08-05) — append only

| Field | Value |
|---|---|
| **Tools** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_xrefs_to` + `get_function_callers` + `get_function_by_address`. **No** `disassemble_bytes` |
| **Live decompile** | ≡ raw CF; Ghidra false-noreturn on `operator_delete` ignored (epilogue `8B C6 5E C2 04 00` present) |
| **Body** | `0x004130d0`–`0x004130ee` exclusive (**30 B** / `0x1E`); pad `CC CC`; Ghidra body end `004130ed` (last byte of RET) |
| **ABI** | ECX=`this`; stack `flags` (4 B cleaned); **`RET 4`** (`C2 04 00`); returns `this` in EAX |
| **Callees** | `FUN_0087b7d0` (CVOGDialog complete dtor), `operator_delete` @ `0x00489822` |
| **Callers** | DATA only: `PTR_FUN_00a58c6c[0]` @ `0x00a58c6c` = `0x004130d0` (no code CALL sites) |
| **RTTI / host** | Parent dual R11-030 `CVOGDialog_Ctor` @ `0x0087b890` installs same vtbl; COL `0x00ab4984` / type_info `0x00af8e50` → **`.?AVCVOGDialog@@`** |
| **Classification** | worker (MSVC scalar-deleting destructor) |
| **Canonical** | `CVOGDialog_ScalarDeletingDtor` |
| **Partition** | R12-010; system missions-progression; parent dual `0x0087b890`; dual start 2646; Terminal **false** |

### Full body hex (30 B)

```
568bf1e8f8864600f644240801740956e83d67070083c4048bc65ec20400
```

### Byte-sealed algorithm

```
ESI = this                    // 56 8B F1
call FUN_0087b7d0             // complete dtor (thiscall; ECX→ESI preserved into callee)
test byte [esp+8], 1          // flags bit0 (after PUSH ESI, stack flags at +8)
je   skip
push ESI
call operator_delete          // 0x00489822
add  esp, 4
skip:
mov  eax, esi
pop  esi
ret  4
```

### Related (not owned)

- Complete dtor: `FUN_0087b7d0` — installs `PTR_FUN_00a58c6c`, chains dualed `CNDUIDialog_CompleteDtor` `0x00792c20`
- Ctor twin: `CVOGDialog_Ctor` / `FUN_0087b890` (parent dual R11-030)
- Vtbl: `PTR_FUN_00a58c6c` @ `0x00a58c6c` (slot[0] = this VA)
