# Annotated low-level: Client_SortTargetListByViewAngle_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00930360` |
| VA | `0x00930360`–`0x00930691` inclusive (**0x332** B) |
| Ghidra name | `FUN_00930360` |
| Canonical name | `Client_SortTargetListByViewAngle_Inferred` |
| System | skills-abilities / client target selection |
| Date | 2026-08-05 (MEGA-023 refresh) |
| Prior scaffold | 2026-07-23 (unknown system; generic plate) |

## Machine-level notes

- Source: raw `aa_00930360_FUN_00930360.md` + MEGA-023 live re-verify.
- **ABI:** stdcall **RET 0xC**; three stack args — **not** thiscall.
- Assembly authoritative over decompiler for: post-`operator_delete` fallthrough, `FUN_00409e20` arity, resolve ECX preload.
- Sort key is **float @ vector elem +4** (score); comparator `LAB_0092cdd0` implements ascending `score`.
- Element stride **0x10**; TFID stride **0x10**; object identity blob at **`obj+0x160`**.

## Signature (sealed)

```c
// stdcall RET 0xC; void
void Client_SortTargetListByViewAngle_Inferred(
    void *client,          // [EBP+8]  ClientShell*
    TFID_16 *tfidList,     // [EBP+0xC] in/out array
    unsigned *countInOut); // [EBP+0x10] in: N; out: resolved/sorted N
```

## Semantics (annotated)

```c
// SEH frame (LAB_009ac665)
// --- 1. Player basis -------------------------------------------------------
// player = *(client + 0xe98)
// interior = player + *(player+4)->+4 + 4  (CVOG interior this-adjust)
// pos  = vtbl[+0x1a0]()   // float3 world position
// quat = vtbl[+0x1a4]()   // float4 orientation XYZW
// forward = QuatExtractForward(quat)   // FUN_004e8a40
// right   = QuatExtractRight(quat)     // FUN_004e8ad0  (dualed)

// --- 2. Build scored vector<Elem0x10> --------------------------------------
// Elem0x10 layout (sort uses +4 only):
//   +0x00 Object*  resolved target
//   +0x04 float    view score
//   +0x08 float    distance
//   +0x0c float    (pad / construct-path distSq; not sort key)

// for i in [0, *count):
//   obj = Object_ResolveFromTFID(&tfidList[i])  // CALL 004bb950; site loads ECX=*(client+0xd34)
//   if (!obj) continue
//   obj->vtbl[+0x144]()                        // ensure/update
//   d = obj.pos(0x80/84/88) - pos
//   distSq = dot(d,d); dist = sqrt(distSq)
//   if (dist <= 0) score = 2.0f                // g_flLevelUpUiBase_Inferred
//   else:
//     u = d * (1/dist)
//     fDot = forward · u
//     rDot = right · u
//     if (rDot <= 0) score = fDot + 1.0f       // left / on-plane half
//     else           score = -fDot - 1.0f       // right half (DAT_00aaa668 = -1)
//   vector.push({obj, score, dist, ...})       // InsertN or construct-N

// --- 3. Sort ascending by score --------------------------------------------
// FUN_00409bd0(begin, end, n, &LAB_0092cdd0)
// cmp(L,R): return R.score > L.score  // i.e. L.score < R.score

// --- 4. Write back TFID list -----------------------------------------------
// for j in [0, n): tfidList[j] = *(TFID_16*)(vec[j].obj + 0x160)
// *count = n   // or 0 if begin was null

// --- 5. Free buffer + SEH teardown + RET 0xC -------------------------------
```

## Score geometry (inferred product role)

- Left half-plane of facing (right·dir ≤ 0): score ∈ roughly **[0, 2]** with **forward best** (score→2).
- Right half-plane: score ∈ roughly **[−2, 0]** with forward-right **most negative**.
- Default for zero-distance: **2.0f** (same as best left-forward).
- Sort is **ascending** → lower scores first (right-forward before left-forward). Relative order is what hostile cycle (`Client_CycleHostileTarget_Inferred`) walks; do not invent “best-first tab” without product strings.

## Call surface

| Dir | Symbol | VA / slot |
|---|---|---|
| Caller (sole) | `Client_CycleHostileTarget_Inferred` | `0x00943340` @ `0x00943440` |
| Callee | player vtbl +0x1a0 / +0x1a4 | origin / quat |
| Callee | `FUN_004e8a40` / `FUN_004e8ad0` | forward / right |
| Callee | `Object_ResolveFromTFID` | `0x004bb950` |
| Callee | `StdVector_InsertN_Elem0x10_Inferred` | `0x00408640` |
| Callee | `FUN_00409e20` / `FUN_00409bd0` | construct / sort |
| Callee | `LAB_0092cdd0` | score comparator |
| Callee | `operator_delete` | `0x00489822` |

## Open questions

- Product C++ method name / keybind plate (keep `_Inferred`).
- Exact English for virtual `+0x144` and client `+0xd34` manager.
- Whether 4th dword of Elem0x10 is always distSq or only on one insert path.
- Runtime / bit-exact / differential.
