# Raw capture: FUN_0040d9c0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0040d9c0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0040d9c0` |
| **Canonical name** | `FUN_0040d9c0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0040d9c0(void)

{
  void *pvVar1;
  int iVar2;
  int unaff_ESI;
  
  iVar2 = *(int *)(unaff_ESI + 0x10);
  while (iVar2 != 0) {
    if ((*(int *)(unaff_ESI + 0x10) != 0) &&
       (iVar2 = *(int *)(unaff_ESI + 0x10) + -1, *(int *)(unaff_ESI + 0x10) = iVar2, iVar2 == 0)) {
      *(undefined4 *)(unaff_ESI + 0xc) = 0;
    }
    iVar2 = *(int *)(unaff_ESI + 0x10);
  }
  iVar2 = *(int *)(unaff_ESI + 8);
  while (iVar2 != 0) {
    pvVar1 = *(void **)(*(int *)(unaff_ESI + 4) + -4 + iVar2 * 4);
    iVar2 = iVar2 + -1;
    if (pvVar1 != (void *)0x0) {
                    /* WARNING: Subroutine does not return */
      operator_delete(pvVar1);
    }
  }
  if (*(void **)(unaff_ESI + 4) != (void *)0x0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(*(void **)(unaff_ESI + 4));
  }
  *(undefined4 *)(unaff_ESI + 8) = 0;
  *(undefined4 *)(unaff_ESI + 4) = 0;
  return;
}
```

---

## Append — dual seal 2026-07-29 (W18-M OWN-ONLY)

| Field | Value |
|---|---|
| **Agent** | W18-M OWN-ONLY dual A/B |
| **Tools** | `decompile_function` + `force_decompile` (identical) + `read_memory` + `analyze_function_complete` + `get_assembly_context` sample call sites. **No** `disassemble_bytes`. |
| **Dual decompile** | Rep1 ≡ Rep2 — third-rep **not required** |
| **Body range** | `0x0040d9c0`–`0x0040da23` (plain `RET`); `CC` pad follows |
| **Body hex** (`read_memory` @ `0x0040d9c0`, 100 B to RET) | `5333db395e1074198b46103bc3740d83c0ff3bc38946107503895e0c395e1075e7578b7e083bfb76228da424000000008b46048b44b8fc83ef013bc3740950e81ebe070083c4043bfb77e58b46043bc35f740950e809be070083c404895e08895e045bc3` |

### Byte-level algorithm (sealed)

```
PUSH EBX
XOR  EBX, EBX                 ; EBX = 0
; --- spin down live counter @ +0x10; null cache @ +0x0C when it hits 0 ---
CMP  [ESI+0x10], EBX
JZ   skip_spin
spin:
  MOV  EAX, [ESI+0x10]
  CMP  EAX, EBX
  JZ   recheck
  ADD  EAX, -1
  CMP  EAX, EBX
  MOV  [ESI+0x10], EAX
  JNZ  recheck
  MOV  [ESI+0x0C], EBX        ; cache ptr cleared on final decrement
recheck:
  CMP  [ESI+0x10], EBX
  JNZ  spin
skip_spin:
PUSH EDI
MOV  EDI, [ESI+0x08]          ; count
CMP  EDI, EBX
JBE  after_elems              ; count <= 0
elem_loop:
  MOV  EAX, [ESI+0x04]        ; base
  MOV  EAX, [EAX+EDI*4-4]     ; element [count-1]
  SUB  EDI, 1
  CMP  EAX, EBX
  JZ   next
  PUSH EAX
  CALL operator_delete        ; cdecl; ADD ESP,4 continues (NOT noreturn)
  ADD  ESP, 4
next:
  CMP  EDI, EBX
  JA   elem_loop
after_elems:
MOV  EAX, [ESI+0x04]
CMP  EAX, EBX
POP  EDI
JZ   zero_fields
PUSH EAX
CALL operator_delete          ; free array buffer
ADD  ESP, 4
zero_fields:
MOV  [ESI+0x08], EBX          ; count = 0
MOV  [ESI+0x04], EBX          ; base = null
POP  EBX
RET
```

### ABI (sealed)

- **This pointer in ESI** (not ECX): callers `MOV ESI, ECX` / `LEA ESI, [obj+disp]` / `MOV ESI, EAX` then `CALL`.
- **Plain RET** (no stack args cleaned by callee).
- **Callee:** `operator_delete` only (cdecl, returns).
- **Thunk:** `0x0040fd50` = `JMP 0x0040d9c0`.
- **Wrapper:** `FUN_0040d910` installs SEH then calls this (used as outer dtor path; `RET 4` on wrapper).
- **Xrefs:** 19; named callers include `NDResourceCache_LookupOrCreate`, plus many FUN_* cleanup paths.

### Object layout (this@ESI)

| Off | Role (inferred) |
|---|---|
| `+0x04` | `void**` element array base |
| `+0x08` | `int` element count |
| `+0x0C` | cached pointer; nulled when `+0x10` hits 0 |
| `+0x10` | live/refcount counter spun down to 0 |

### Decompiler caveats

- `unaff_ESI` is the intentional this-in-ESI ABI.
- `/* Subroutine does not return */` on `operator_delete` is **false** — bytes continue after `ADD ESP,4`.
