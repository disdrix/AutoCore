# Annotated low-level: CNDUIWndBuffered_ScalarDeletingDtor

| Field | Value |
|---|---|
| Stable ID | `aa_004293f0` |
| VA | `0x004293f0`–`0x0042940d` exclusive (**29 B** / `0x1D`) |
| Ghidra name | `FUN_004293f0` |
| Canonical name | `CNDUIWndBuffered_ScalarDeletingDtor` |
| System | skills-abilities / CNDUIWndBuffered (MSVC scalar-deleting destructor) |
| Date | 2026-08-05 (R10-009 dual refresh) |
| Parent | `0x0078ca80` `CNDUIWndBuffered_CompleteDtor` |

## Machine-level notes

- Source: raw capture + R10-009 live re-verify (`decompile_function`, `analyze_function_complete`, `read_memory`, xrefs). **No** `disassemble_bytes`.
- Classic MSVC **scalar-deleting destructor** (vtbl[0] of `PTR_FUN_00a99f74`):
  1. Always run complete dtor `FUN_0078ca80` / `CNDUIWndBuffered_CompleteDtor` (void; does not free host).
  2. If `(flags & 1)`: `operator_delete(this)`.
  3. Return `this` in EAX; epilogue **`RET 4`**.
- Entry: `PUSH ESI; MOV ESI,ECX` (`56 8B F1`). Exit: `MOV EAX,ESI; POP ESI; RET 4` (`8B C6 5E C2 04 00`).
- Ghidra WARNING "does not return" on `operator_delete` is **false** for this unit — full 29 B reaches epilogue.
- Not a vector deleting dtor: single bit0 gate only; no array count; no `operator_delete[]`.
- DATA xref only @ `0x00a99f74` (primary vtbl slot 0). No code CALL sites in bulk xrefs.
- Product class name sealed by parent dual RTTI (`.?AVCNDUIWndBuffered@@`) — **not** `_Inferred`.

## Pseudocode (annotated)

```c
// ECX = this (CNDUIWndBuffered*); stack flags; RET 4; returns this*
void* __thiscall CNDUIWndBuffered_ScalarDeletingDtor(void* self, uint8_t flags)
{
  CNDUIWndBuffered_CompleteDtor(self);  // FUN_0078ca80 — separate unit (WQ9K-F)

  if ((flags & 1u) != 0u) {
    operator_delete(self);              // 0x00489822
  }

  return self;
}
```

## Open questions

- Runtime / bit-exact / differential verification (deferred; Terminal false).
- Complete dtor member map owned by parent unit `aa_0078ca80` (not re-owned here).
