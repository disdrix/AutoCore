# Annotated low-level: StdTree_BuynodeEmpty_Isnil21_Size0x28_Inferred (FUN_004088f0)

| Field | Value |
|---|---|
| Stable ID | `aa_004088f0` |
| VA | `0x004088f0`–`0x00408979` (**138 B** / `0x8A`) |
| System | util / MSVC `_Tree` empty-node buy (isnil@**+0x21**, size **0x28**) |
| Date | 2026-08-04 (WQ9H-I dual seal; scaffold 2026-07-23) |
| Dual name | `StdTree_BuynodeEmpty_Isnil21_Size0x28_Inferred` |

## Machine-level notes

- Source: raw capture + live Ghidra re-verify.
- **Worker** — sole callee `operator_new`.
- **ABI:** **cdecl**; no stack formals; **`RET 0`** (`C3`); **EAX = node\*** (decompiler `void` is **wrong**).
- **SEH** frame with handler `LAB_009bc6c0`.
- **Role:** allocate one **0x28-byte** red-black tree node and initialize:
  - left/parent/right = **0**
  - **color@+0x20 = 1** (black)
  - **isnil@+0x21 = 0** (real node flag; head ctors overwrite to **1**)
- **Not** CNDHash freelist pop (`CNDHash_FreelistPop_Node0x28` is a different VA/family).
- **Not** full value buynode — peer `FUN_00408990` (WQ9H-J) takes left/parent/right/value/color.
- **Twin empty-head:** `FUN_00408a30` same CF shape for **size 0x30 / isnil@+0x29**.

### Callers (4) — all head-sentinel init

```text
head = StdTree_BuynodeEmpty_Isnil21_Size0x28_Inferred();
tree._Myhead = head;
head->isnil = 1;          // +0x21
head->parent = head;      // +0x04
head->left   = head;      // +0x00
head->right  = head;      // +0x08
tree._Mysize = 0;         // +0x08 on tree shell
```

Sites: `FUN_00405560`, `FUN_00405d00`, `FUN_00406100`, `FUN_00501970` (Vehicle-class map member @ dword index 0xaf).

### Node layout (size 0x28)

| Offset | Role |
|---|---|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x0C … +0x1F | value (**Val20** region; product open) |
| +0x20 | color |
| +0x21 | isnil |
| +0x22 … +0x27 | pad to 0x28 |

Matches char-key map family (`Map_LowerBoundFindByCharKey` isnil@+0x21, key@+0x0C).

## Signature (sealed)

```c
// cdecl; RET 0; EAX = node* (may be null if new fails before stores)
NodeIsnil21* StdTree_BuynodeEmpty_Isnil21_Size0x28_Inferred(void);
```

## Pseudocode (annotated)

```c
NodeIsnil21* StdTree_BuynodeEmpty_Isnil21_Size0x28_Inferred(void)
{
  // SEH frame omitted in clean plate; present in retail.
  NodeIsnil21* node = (NodeIsnil21*)operator_new(0x28);
  if (node != nullptr) {
    node->left = nullptr;     // +0x00
  }
  // MSVC null-checks on &node->parent / &node->right are always-true if node non-null
  if (node != nullptr) {
    node->parent = nullptr;   // +0x04
    node->right  = nullptr;   // +0x08
  }
  // Written even when decompiler structure is awkward; bytes always hit +0x20/+0x21 after new
  node->color = 1;            // +0x20
  node->isnil = 0;            // +0x21
  return node;                // EAX — not void
}
```

**Odd behavior preserved:** if `operator_new` returned null, the color/isnil stores still appear in the straight-line decompiler (retail path after successful new is the live path; OOM is CRT-dependent). Prefer: only construct when non-null; callers assume non-null head.

## Open residual

- Product map `value_type` for Val20 / owning tree shells.
- Dual of full buynode `00408990` / empty isnil29 twin `00408a30` (WQ9H-J).
- Dual of head-init parents `00405560` / `00405d00` / `00406100`.
- Runtime / bit-exact / differential.
