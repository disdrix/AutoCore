# Raw capture: FUN_0051b230

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051b230` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051b230` |
| **Canonical name** | `FUN_0051b230` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int * __fastcall FUN_0051b230(int param_1)

{
  int *piVar1;
  
  piVar1 = (int *)CVOGReaction_GiveItemByCbid(*(int *)(*(int *)(param_1 + 0xa8) + 0x34));
  if (piVar1 != (int *)0x0) {
    (**(code **)(*piVar1 + 8))
              (*(undefined4 *)(*(int *)(param_1 + 0xa8) + 0x34),*(undefined4 *)(param_1 + 0xa4),1);
    (**(code **)(*piVar1 + 0xb8))(*(undefined4 *)(param_1 + 0xb8));
    FUN_00516720(*(undefined4 *)(param_1 + 0x158));
    piVar1[0x31] = *(int *)(param_1 + 0xc4);
  }
  return piVar1;
}
```

---

## Re-verify append — 2026-08-04 WQ9R-H dual seal

**Tools:** `decompile_function` + `analyze_function_complete` + `get_function_by_address` + `read_memory` + `get_assembly_context` (call sites). **No** `disassemble_bytes`. No Launcher.

### Body / ABI (read_memory)

| Item | Value |
|------|-------|
| Body | `0x0051b230`–`0x0051b298` exclusive (**104 B** / `0x68`) |
| Last insn | `C3` RET @ `0x0051b297` |
| Pad | `CC` to next function `0x0051b2a0` |
| ABI | **`__thiscall`**: ECX=`this` (`MOV EDI,ECX`); no stack formals; return object* in EAX (`MOV EAX,ESI`) |
| Frame | `PUSH ESI/EDI` … `POP EDI; MOV EAX,ESI; POP ESI; RET` |

Hex (104 B):
```
56578bf98b87a80000008b40346a0050e82befffff8bf083c40885f674458b87a40000008b8fa80000008b166a01508b4134508bceff52088b87b80000008b16508bceff92b80000008b8f58010000518bcee899b4ffff8b97c40000008996c40000005f8bc65ec3
```

### Callee seal (bytes)

1. **`CVOGReaction_GiveItemByCbid`** @ `0x0051a170`: `PUSH 0; PUSH cbid; CALL; ADD ESP,8` — **two** cdecl stack args (cbid, trailing 0). cbid = `*(*(this+0xA8)+0x34)`.
2. **vtbl+8** thiscall on created obj: args `(cbid, *(this+0xA4), 1)` — same init family as `AllocateNewObjectFromCbid`.
3. **vtbl+0xB8** thiscall: arg `*(this+0xB8)`.
4. **`FUN_00516720`** @ `0x00516720`: `MOV ECX,obj; PUSH *(this+0x158); CALL` — thiscall name/string assign (matches AllocateNewObjectFromCbid template path).
5. **`obj+0xC4 = *(this+0xC4)`** (`piVar1[0x31]`).

### Callers / xrefs

| Kind | Detail |
|------|--------|
| Code callers | `FUN_004c8f00` @ `0x004c8f23`; `FUN_0056b0a0` @ `0x0056b0c2` |
| Pattern | this-adjust LEA ECX → `CALL 0x0051b230` → `PUSH EAX; CALL __RTDynamicCast` (`0x004898a4`); `ADD ESP,0x14` |
| Cast targets | `dynamic_cast<CVOGCreature*>` (RTTI `.?AVCVOGCreature@@` @ `0x00af8d0c`, src `.?AVCVOGClonedObjectBase@@` @ `0x00af8e10`); weapon path uses `.?AVCVOGWeapon@@` @ `0x00aff630` |
| Vtable DATA xrefs | ~21 slots (`0x009cd698` … `0x009df498`) — shared virtual method on cloned-object hierarchy |

### Decompiler traps (corrected)

| Trap | Correction |
|------|------------|
| `__fastcall` / bare `int param_1` | **`__thiscall`** ECX=this |
| GiveItemByCbid one-arg display | Image pushes **cbid + 0** |
| `FUN_00516720(string)` only | thiscall **(obj, string)** |
| Callers appear to pass RTTI into this VA | RTTI pushes are for **following** `__RTDynamicCast`; this unit only consumes ECX |

### Classification

Worker / virtual factory method: spawn object from embedded CBID descriptor on `this`, init, return pointer (often immediately dynamic_cast by caller).
