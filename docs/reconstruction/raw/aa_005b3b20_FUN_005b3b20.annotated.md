# Annotated low-level: FUN_005b3b20 / Obj_BodyDtor_SetVtbl_DestroyStdListAt10

| Field | Value |
|---|---|
| Stable ID | `aa_005b3b20` |
| VA | `0x005b3b20`–`0x005b3b2e` exclusive (**14 B**) |
| System | host object / MSVC std::list member dtor |
| Date | 2026-07-29 (W28-N re-verify) |

## Machine-level notes

- **Bytes authority:** decompiler omits `ADD ECX,0x10` and shows CALL+RET; body is **vtbl store + this+=0x10 + tail JMP** to `StdList_Destroy_FreeHead` (`0x00497360`).
- Full hex: `c701b4959d0083c110e93238eeff` then `cc cc`.
- Relative JMP target: `0x005b3b2e + 0xFFEE3832` = `0x00497360` (signed).
- Vtbl `0x009d95b4` → first slot `0x005b44d0` = scalar-deleting dtor that calls this body then optional `operator_delete`.
- Ctor sites (`FUN_005b3f60`, `FUN_005b4260`) install same vtbl and construct list shell at `this+0x10` via `FUN_005b43b0` (`operator_new(0x30)` self-link sentinel).

## Pseudocode (byte-corrected)

```c
// ECX = this*; bare RET via tail into StdList_Destroy_FreeHead
void __fastcall Obj_BodyDtor_SetVtbl_DestroyStdListAt10(void *self)
{
  *(void **)self = &PTR_FUN_009d95b4;          // final vtbl
  StdList_Destroy_FreeHead((uint8_t *)self + 0x10); // tail jmp; ECX = this+0x10
}
```

## Layout (from ctor + this body)

| Offset | Role |
|---|---|
| +0x00 | vtbl* (`PTR_FUN_009d95b4`) |
| +0x10 | embedded list object start (passed as ECX to list destroy) |
| +0x14 | list head* (list+4) |
| +0x18 | list size (list+8) |

## Open questions

- Product English class name (no plate; vtbl adjacent rdata is shader text, not class string).
- Full field map for +0x04..+0x0f and +0x1c+ (owned by ctor units, not this dtor).
- Runtime / bit-exact — open.
