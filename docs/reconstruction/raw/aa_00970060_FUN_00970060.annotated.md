# Annotated low-level: FUN_00970060

| Field | Value |
|---|---|
| Stable ID | `aa_00970060` |
| VA | `0x00970060` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_00970060`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
int FUN_00970060(undefined4 param_1,undefined4 param_2)

{
  int iVar1;
  char cVar2;
  int iVar3;
  int iVar4;
  int *unaff_ESI;
  
  cVar2 = FUN_0096fdf0();
  iVar3 = FUN_007528f0(param_1,param_2);
  if (((iVar3 == -1) && (cVar2 != '\0')) && (iVar1 = *unaff_ESI, iVar1 != 0)) {
    iVar4 = *(int *)(iVar1 + 0x18);
    if (*(int *)(iVar1 + 0x18) == 0) {
      iVar4 = iVar1;
    }
    if (iVar4 != iVar1) {
      FUN_00448fc0(iVar4);
    }
  }
  return iVar3;
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.

# W38-P re-verify append (2026-08-04)

## Annotated CF (bytes)

ESI-this effect-handle helper:

1. **CoW** via `FUN_0096fdf0(ESI)`: if `*handle` non-null and binder refcount `@+4 > 1`, clone through factory `DAT_00d1f61c` / `FUN_0073df30`, re-link parent `@+0x18`, install clone into handle; return 1 if cloned else 0.
2. **Set slot** `FUN_007528f0(*handle, slotIndex, resource)`: if `slotIndex < 0` → **-1**; else bind/unbind parameter slot (resource AddRef/Release at resource `+8` / iface `+4`) and return **0**.
3. **Fail recovery** (only when status==-1 **and** CoW ran): if `parent = (*handle)+0x18` differs from self, `FUN_00448fc0(handle, parent)` rebinds the outer handle to the parent binder (COM-style rc at binder`+4`).

Normal callers (`0075bf40`, `00970140`) **gate on index≥0**, so step 3 is rarely live — still present in the machine.

## Decompiler pitfalls

- `unaff_ESI` is **ABI this**, not garbage.
- `FUN_00448fc0(iVar4)` understates: **ECX=handle**, stack=parent.
- `FUN_0096fdf0()` / `FUN_007528f0(a,b)` omit this wiring.

## Confidence

| Claim | Level |
|-------|-------|
| Body 73 B / `ret 8` | Confirmed |
| ESI this + 2 stack args | Confirmed |
| CoW then set-slot | Confirmed |
| -1 only from index\<0 in `007528f0` | High (callee bytes) |
| Product effect class English | Open |
