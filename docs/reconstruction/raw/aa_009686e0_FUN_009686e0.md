# Raw capture: FUN_009686e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_009686e0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x009686e0` |
| **Canonical name** | `FUN_009686e0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_009686e0(void)



{

  int *piVar1;

  int *piVar2;

  

  piVar2 = (int *)*DAT_00d1ee30;

  piVar1 = DAT_00d1ee30;

  if (piVar2 != DAT_00d1ee30) {

    do {

      if ((piVar2[2] != 0) && (*(int *)(*(int *)(piVar2[2] + 0x44) + 0x30) == 0)) {

        FUN_0096c8e0();

        piVar1 = DAT_00d1ee30;

      }

      piVar2 = (int *)*piVar2;

    } while (piVar2 != piVar1);

  }

  return;

}
```

---

## W38-AA live re-verify (2026-08-04)

| Field | Value |
|---|---|
| **Tooling** | `decompile_function` + `analyze_function_complete` + `read_memory` + `get_function_by_address` + `get_xrefs_to` + callers/callees + `get_assembly_context` at sole call site. **No** `disassemble_bytes`. |
| **Body** | `0x009686e0`–`0x00968715` exclusive (**53 B** / `0x35`); final `C3` @ `0x00968714`; pad `CC`×11 then `FUN_00968720` |
| **Ghidra body** | `009686e0`–`00968714` (last insn) |
| **Signature** | `void FUN_009686e0(void)` — no stack formals; plain `RET`; ESI scratch |
| **Xrefs** | **1** — `FUN_0075eff0` (`GfxDevice_Reset`) @ `0x0075f0ec` `[UNCONDITIONAL_CALL]` |
| **Callees** | `FUN_0096c8e0` (thiscall ECX = texture at `view+0x44`) |
| **Classification** | worker |
| **Live decompile** | ≡ scaffold raw CF (list walk + gate + release call) |

### Full body hex (53 B)

```
8b 0d 30 ee d1 00 56 8b 31 3b f1 74 26 8d 49 00 8b 46 08 85 c0 74 16 8b 40 44 83 78 30 00 75 0d 8b c8 e8 d9 41 00 00 8b 0d 30 ee d1 00 8b 36 3b f1 75 dd 5e c3
```

### Call-site context (`0x0075f0ec`)

Shutdown factory chain inside `GfxDevice_Reset` after `"Beginning shutdown for Device Reset()"`:

`FUN_00985f40` → `FUN_00985580` → `FUN_009864e0` → **`FUN_009686e0`** → `FUN_0073d8d0` → texture-list release walk → …

### Semantics (sealed)

Walk process-wide `std::list` sentinel `DAT_00d1ee30` (gfxBufferedView registration list; ctor `FUN_00968a50` / W37-I). For each node with non-null payload `node[2]`:

1. `tex = *(payload + 0x44)`
2. If `*(tex + 0x30) == 0`: `FUN_0096c8e0(tex)` via thiscall (`MOV ECX,EAX; CALL`)

Does **not** unlink list nodes or destroy views — only releases device-dependent texture resources for views whose texture reset-flag field is zero.

### Name

**Inferred:** `GfxBufferedViews_ReleaseDeviceTextures_Inferred`  
**Reject** scaffold `Named_CalleeOf_Named_gfxDevice_009686e0`.
