# Annotated low-level: FUN_00570f50 → InventoryGrid_RemoveAllAndReEmptyCells_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00570f50` |
| VA | `0x00570f50`–`0x00570f62` (**19 B** / `0x13`) |
| Ghidra name | `FUN_00570f50` |
| Semantic name | `InventoryGrid_RemoveAllAndReEmptyCells_Inferred` |
| System | `inventory-transfer` |
| Date | 2026-08-05 (R10-013 dual refresh; supersedes 2026-07-23 scaffold) |
| Twin | `0x00570f70` `InventoryGrid_ClearItemsAndReEmptyCells_Inferred` (full item walk) |
| Parent dual | `0x00570720` `InventoryGrid_AllocateCellArray_Inferred` |

---

## Machine-level notes

- **ABI:** MSVC `__fastcall` / thiscall-shaped; sole arg **ECX = InventoryGrid***; void; **tail-JMP** (no local RET).
- Decompiler shows bare `FUN_004bc580()` — **assembly restores** `LEA ECX,[ESI+0x2c]` before call (list host, not grid).
- Prefer assembly for ECX/list split; decompiler CF order (RemoveAll → Allocate) is correct.
- Does **not** TraversalLock, iterate items, detach (`vtbl+0x158`), or call `FUN_004d4790` — that is the twin at `0x00570f70`.
- `List_RemoveAll` still destroys residual list nodes via scalar-deleting dtor if any remain; throws if list is traversal-locked.

## Layout (this unit)

| Off | Object | Role | Conf |
|----:|--------|------|------|
| ECX | grid | `InventoryGrid*` this | **High** |
| `grid+0x2c` | list | Embedded List host passed to `List_RemoveAll` | **High** (LEA) |
| `grid+0x28` | cells* | Rewritten by Allocate tail | **High** (callee) |

List host (via dualed `List_RemoveAll` `0x004bc580`):

| Off | Role |
|----:|------|
| `+0x04` | `CRITICAL_SECTION` |
| `+0x1c` | head / walk cursor |
| `+0x20`, `+0x24` | cleared to 0 |
| `+0x28` | traversal-lock flag (non-zero → throw `0x80070005`) |

## Pseudocode (annotated)

```c
// InventoryGrid_RemoveAllAndReEmptyCells_Inferred  @ 0x00570f50
// __fastcall  ECX = InventoryGrid* grid
void __fastcall InventoryGrid_RemoveAllAndReEmptyCells_Inferred(InventoryGrid *grid)
{
  // ESI = grid; ECX = grid+0x2c
  List_RemoveAll(/* list */ (ListHost *)((char *)grid + 0x2c));  // 0x004bc580

  // tail-JMP — ECX restored to grid
  InventoryGrid_AllocateCellArray_Inferred(grid);                 // 0x00570720
}
```

## Assembly (authoritative ABI)

```text
00570f50  PUSH ESI
00570f51  MOV  ESI, ECX              ; grid*
00570f53  LEA  ECX, [ESI+0x2c]       ; list*
00570f56  CALL List_RemoveAll        ; 0x004bc580
00570f5b  MOV  ECX, ESI              ; grid*
00570f5d  POP  ESI
00570f5e  JMP  InventoryGrid_AllocateCellArray_Inferred  ; 0x00570720
```

## Callers (sample — inventory-transfer create/clear paths)

| Caller | Role |
|--------|------|
| `CVOGCharacter_ApplyCreateFromPacket` | Character create re-empty |
| `Vehicle_applyCreatePacket` | Vehicle create re-empty |
| `FUN_00884720` | Cargo/UI: thin path on `DAT_00d1b6d8+0xce0` (full clear twin on host cargo first) |
| `FUN_0051f1f0` | Up to 3 conditional grids |
| `FUN_0052f260` | Triple call sites |

## Open questions

- Product English for when thin path is chosen vs full clear twin (ownership already transferred? empty list expected?).
- Retail symbol unknown — keep `_Inferred`.
- Runtime / bit-exact / differential open (no Launcher this dual).
- Exact type of list node vtable objects destroyed inside `List_RemoveAll` — sealed at list dual, not re-owned here.
