# Raw capture: FUN_00406ee0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00406ee0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00406ee0` |
| **Canonical name** | `FUN_00406ee0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int FUN_00406ee0(void)

{
  int unaff_ESI;
  int unaff_EDI;
  
  FUN_00409db0();
  return unaff_EDI + unaff_ESI * 0xc;
}
```

---

## WQ9G-I re-verify (2026-08-04)

**Tooling:** Ghidra `batch_decompile` / `decompile_function` + `analyze_function_complete` + `read_memory` + callers/xrefs. **No** `disassemble_bytes`. **No** Launcher.

### Live decompile (equiv scaffold body)

```c
int FUN_00406ee0(void)

{
  int unaff_ESI;
  int unaff_EDI;
  
  FUN_00409db0();
  return unaff_EDI + unaff_ESI * 0xc;
}
```

### Byte seal (`read_memory` @ `0x00406ee0`, 64 B)

- Body **29 B** / `0x1d`: `0x00406ee0`–`0x00406efd` exclusive; pad `CC` to `0x00406f00`.
- `mov eax,[esp+4]; push eax; push ecx; mov ecx,[esp+0xc]` — stack formal0 = value*; ECX reloaded to value* for construct-N.
- `push esi; push edi; call FUN_00409db0` then **`add esp,0x10`** (cdecl construct-N + phantom pushes).
- End pointer: `lea edx,[esi+esi*2]; lea eax,[edi+edx*4]` → **`dest + count * 0xc`**.
- Epilogue: **`C2 04 00`** (`ret 4`) — one stack formal (value*).

### Callee construct-N (`FUN_00409db0` + `FUN_00409f50`)

- `00409db0`: ECX=value*, stack dest + count; loop count times; each `00409f50` copies **3 dwords** EAX=dest←ECX=src; **`add esi,0x0C`** per iter.
- `00409f50`: POD copy 12 B (null-dest skip stores).

### Cross-refs

| Kind | Value |
|------|-------|
| Callers | `FUN_004082f0` only (2 sites @ `0x00408426`, `0x0040851b`) |
| Callees | `FUN_00409db0` |
| Parent role | InsertN hole fill: after uninit-copy prefix, fill N template copies, then copy suffix |

### Recovered ABI / name

- **Name:** `StdVector_UninitializedFillN_Elem12_Inferred`
- **ABI:** EDI=dest*, ESI=count, stack `const T* value`; **ret 4**; EAX=`dest + count*0xC`
- **Stride:** **0xC** — Confirmed
- **Role:** uninitialized_fill_n for 12-byte POD vector elements

