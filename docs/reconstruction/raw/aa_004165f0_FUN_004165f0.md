# Raw capture: FUN_004165f0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004165f0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004165f0` |
| **Canonical name** | `FUN_004165f0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_004165f0(void *param_1,byte param_2)

{
  FUN_008327f0();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```

---

## WQ9I-J re-verify append (2026-08-05)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **WQ9I-J** |
| **Tools** | `batch_decompile` / `analyze_function_complete` + `read_memory` + xrefs. **No** `disassemble_bytes` |
| **Live decompile** | ≡ scaffold 2026-07-23 (complete + flags&1 delete + return this) |
| **Body** | `0x004165f0`–`0x0041660E` exclusive (**30 B** / `0x1E`); pad `CC` |
| **ABI** | **ECX=`this`**; stack **`flags`**; **`RET 4`** (`C2 04 00`); returns **this** in EAX |
| **Callees** | `FUN_008327f0` (complete dtor — install `PTR_FUN_00a70524` then base `FUN_0082d540`); `operator_delete` |
| **Callers** | DATA only — `PTR_FUN_00a70524[0]` @ `0x00a70524` = `0x004165f0` |
| **Name (Inferred)** | `Class_00a70524_ScalarDeletingDtor_Inferred` |
| **Domain note** | Peer ctor `FUN_00832830` installs same vtbl and loads `"i_m_chatlog.xml"` (UI chatlog class; product demangle open) |
| **Adjacent** | Neighbor scalar twin shape `FUN_00416610` (not owned; different complete) |

### Full-body hex (`read_memory`, 30 B)

```
568bf1e8f8c14100f644240801740956e81d32070083c4048bc65ec20400
```

### Vtbl `PTR_FUN_00a70524` @ `0x00a70524` (`read_memory`, first slots)

| Slot | Value | Role |
|---|---|---|
| [0] | `0x004165f0` | this scalar-deleting dtor |
| [1] | `0x00790cc0` | open (not owned) |
| [2] | `0x007a7de0` | open (not owned) |
| [3] | `0x007567e0` | open (not owned) |

### Byte-sealed facts

1. `ESI = this`; `CALL FUN_008327f0` (complete @ `0x008327f0`).
2. `TEST byte [esp+8], 1` / `JE` skip — flags bit0 only (scalar, not vector-deleting).
3. `operator_delete(this)` cdecl + `ADD ESP,4` when bit0 set (decompiler “does not return” is **false**).
4. `MOV EAX,ESI; POP ESI; RET 4` — always returns this; cleans flags.
5. Classification: worker (MSVC scalar-deleting / vtbl[0]).
