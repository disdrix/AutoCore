# Annotated low-level: FUN_00578de0 → CVOGHBSkillBase_ScalarDeletingDtor

| Field | Value |
|---|---|
| Stable ID | `aa_00578de0` |
| VA | `0x00578de0`–`0x00578dfe` exclusive (**30** B / `0x1E`) |
| System | `skills-abilities` |
| Date | 2026-08-05 (R11-006 OWN dual; scaffold 2026-07-23) |
| Canonical name | `CVOGHBSkillBase_ScalarDeletingDtor` |
| Ghidra | `FUN_00578de0` |

## Machine-level notes

- Source: raw capture + R11-006 re-verify for `aa_00578de0`.
- Prefer assembly when decompiler conflicts (here CF matches; decompiler `operator_delete` "does not return" is a false positive — control continues to `return this`).
- ABI: MSVC **`__thiscall`**, ECX = `this`, stack `uint8_t flags`, **`ret 4`**, EAX = `this`.
- Role: MSVC **scalar deleting destructor** (vtbl slot **0** of `PTR_FUN_009d3fdc` / `CVOGHBSkillBase`).
- Complete body sibling (not OWN): `FUN_00578a60` restores vtbl `PTR_FUN_009d3fdc`, frees owned buffers, tails into `CVOGHBBase_dtor` `FUN_00508390`.
- No CODE callers — only **DATA** xref from vtbl[0] @ `0x009d3fdc` (virtual delete path).
- Partition parent `0x00578ce0` is image-adjacent OnEnd dual (body ends exclusive at this VA); **not** a structural caller.

## Pseudocode (annotated)

```c
// __thiscall  ret 4  → EAX = this
void * __thiscall CVOGHBSkillBase_ScalarDeletingDtor(void *thisHb /* ECX */, byte flags)
{
  FUN_00578a60();                 // complete dtor (ECX=this); restores CVOGHBSkillBase vtbl
  if ((flags & 1) != 0) {
    operator_delete(thisHb);      // heap free only when bit0 set
  }
  return thisHb;
}
```

## Sealed claims

| Claim | Level |
|---|---|
| Body 30 B + `ret 4` | **Confirmed** (`read_memory` + `disassemble_function`) |
| `flags&1` → `operator_delete` | **Confirmed** |
| Complete dtor call `FUN_00578a60` | **Confirmed** (rel32) |
| vtbl[0] @ `0x009d3fdc` → this VA | **Confirmed** |
| Class `CVOGHBSkillBase` (RTTI) | **Confirmed** (family dual + COL/type_info on same vtbl) |
| Product mangled `??_G…` | **Open** |
| Runtime / bit-exact | **Open** (Terminal false) |

## Open questions

- Product/PDB mangled scalar-deleting symbol.
- Dual seal of complete body `FUN_00578a60` (OWN residual elsewhere).
