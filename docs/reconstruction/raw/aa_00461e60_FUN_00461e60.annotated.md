# Annotated low-level: FUN_00461e60

| Field | Value |
|---|---|
| Stable ID | `aa_00461e60` |
| VA | `0x00461e60` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_00461e60`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
undefined4 * FUN_00461e60(undefined4 *param_1,undefined4 *param_2)

{
  int *piVar1;
  int *piVar2;
  int *piVar3;
  undefined4 *in_EAX;
  undefined1 *puVar4;
  
  if (param_1 != param_2) {
    puVar4 = (undefined1 *)((int)param_1 + 0x15);
    do {
      *in_EAX = *param_1;
      in_EAX[1] = param_1[1];
      in_EAX[2] = param_1[2];
      in_EAX[3] = param_1[3];
      piVar2 = *(int **)(puVar4 + -5);
      if ((piVar2 != (int *)0x0) && (piVar2[1] = piVar2[1] + 1, piVar2[1] == 1)) {
        (**(code **)(*piVar2 + 4))();
      }
      piVar3 = (int *)in_EAX[4];
      if (piVar3 != (int *)0x0) {
        piVar1 = piVar3 + 1;
        *piVar1 = *piVar1 + -1;
        if (*piVar1 == 0) {
          (**(code **)(*piVar3 + 8))();
        }
      }
      in_EAX[4] = piVar2;
      *(undefined1 *)(in_EAX + 5) = puVar4[-1];
      *(undefined1 *)((int)in_EAX + 0x15) = *puVar4;
      *(undefined1 *)((int)in_EAX + 0x16) = puVar4[1];
      in_EAX[6] = *(undefined4 *)(puVar4 + 3);
      param_1 = param_1 + 7;
      in_EAX = in_EAX + 7;
      puVar4 = puVar4 + 0x1c;
    } while (param_1 != param_2);
  }
  return in_EAX;
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.

---

## W37-Y annotated update (2026-08-04)

### Purpose

Forward **RC assign-range** for live **RcElem28** elements (stride **0x1c**): assign `[src_first, src_last)` onto `[dest, …)` with AddRef/Release on shared resource `@+0x10`. Sole erase-tail mover under `StdVector_EraseRange_RcElem28`.

### Signature (bytes)

```c
// EAX=dest*, stack src_first*, src_last*; plain RET; returns final dest*
RcElem28* RcElem28_AssignRange(RcElem28* dest /*EAX*/,
                               RcElem28* src_first /*stack0*/,
                               RcElem28* src_last  /*stack1*/);
```

Parent may push a third stack dword (dest again) that this leaf never reads; cleans `ADD ESP,0xC`.

### Control flow

1. If `src_first == src_last` → return dest.
2. Else loop stride 0x1c:
   - Copy dwords +0x00..+0x0C from src → dest
   - AddRef src resource `@+0x10` (if non-null; ++refcount; if becomes 1 call vtbl+4)
   - Release dest resource (if non-null; −−refcount; if 0 call vtbl+8)
   - Store resource ptr; copy +0x14..+0x16 and +0x18
3. Return advanced dest cursor (one past last assigned).

### Resolved

| Item | Resolution |
|---|---|
| Calling convention | EAX dest + 2 stack; cdecl plain RET; returns dest* |
| Stride / RC | 0x1c; same assign lanes as W35-P fill/backward |
| Role | erase-range tail move-assign (not uninit, not destroy) |
| Name | `RcElem28_AssignRange` (Inferred structural) |

### Still open

- Product/PDB T English (gfx residual only on family)
- Exact AddRef-when-becomes-1 product contract (shared family residual)
- Runtime / bit-exact
