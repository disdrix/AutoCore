# Raw capture: FUN_004e24d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004e24d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004e24d0` |
| **Canonical name** | `FUN_004e24d0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_004e24d0(void *param_1,int param_2,int param_3)

{
  int iVar1;
  
  iVar1 = param_2 - (int)param_1 >> 2;
  memmove((void *)(param_3 + iVar1 * -4),param_1,iVar1 * 4);
  return;
}
```
---

## Live re-verify append (2026-07-29 W20-H)

| Check | Result |
|---|---|
| `decompile_function` @ `0x004e24d0` | ≡ raw (count = (end−begin)>>2; memmove(endExcl − count*4, begin, count*4)) |
| `get_function_by_address` | Body `004e24d0`–`004e24f3` (exclusive end); **35** B |
| `read_memory` length 64 | body hex below; trailing `cc` pad after final `c3` |
| ABI | three stack formals; **bare `ret` (`c3`)** — **cdecl** (not `ret 0xC` stdcall sibling) |
| Callees | `memmove` only via IAT `[0x009c652c]` |
| Callers | 25 code xrefs — vector insert/grow family (`FUN_004073a0`, `FUN_004507a0`, `FUN_004e39d0`, `FUN_005064e0`, `FUN_00539810`, …) |
| Sibling | `aa_004e2e80` `Mem_MoveDwordRange_ReturnEnd` — same size math, **dest = param3 forward**, returns end, **`ret 0xC`** |

**Body hex (0x23 B, exclusive end `0x004e24f3`):**

```
8b4c24048b4424082bc1c1f80203c003c050518b4c24142bc851ff152c659c0083c40cc3
```

**Byte-level CF:**

```
8b 4c 24 04       mov  ecx, [esp+4]      ; srcBegin
8b 44 24 08       mov  eax, [esp+8]      ; srcEnd
2b c1             sub  eax, ecx
c1 f8 02          sar  eax, 2            ; dword count
03 c0             add  eax, eax
03 c0             add  eax, eax          ; size = count * 4
50                push eax               ; size
51                push ecx               ; src
8b 4c 24 14       mov  ecx, [esp+14h]    ; exclusiveEnd (after 2 pushes)
2b c8             sub  ecx, eax          ; dest = exclusiveEnd - size
51                push ecx
ff 15 2c 65 9c 00 call [memmove IAT]
83 c4 0c          add  esp, 0Ch
c3                ret
```

**Not** `aa_004e2e80` / `aa_00506430` (those write at `param3` forward and return end pointer).
