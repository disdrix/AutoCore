# Annotated low-level: FUN_00518ec0 → Object_SharedBase_Dtor

| Field | Value |
|---|---|
| Stable ID | `aa_00518ec0` |
| VA | `0x00518ec0`–`0x005190b1` exclusive (**497 B** / `0x1F1`) |
| System | object core / shared-base field destructor |
| Date | 2026-07-29 W29-G |
| Tools | decompile + analyze_function_complete + read_memory (**no** `disassemble_bytes`) |

---

## Machine-level notes

- **ABI:** `__thiscall` / `__fastcall` ECX=`this`; **no** stack formals; void; bare **`C3` RET**.
- **Role:** shared dual-base / virtual-base **field destructor** for the client object core — twin of `Object_SharedBase_Ctor` (`0x00518940`, W22-B). Outer complete-object dtors call or **tail-jmp** here (e.g. `Object_009d33c4_CompleteDtor` `0x00575350` W28-M).
- **String:** fatal log if still registered in CO list while destroying — product confirms **object destructor**.
- **Ghidra false noreturn** on `operator_delete`: body continues after each free (zeros next fields; final triad + RET sealed by bytes).
- **Predicate pack:** stack local starts as `this`, then low byte cleared (`this & ~0xFF`) before `CNDDoubleList_InvokePredicateAndRemove_Inferred(&LAB_005120e0, &local, 0)` (raw still names `FUN_004e2600`).
- **CS helper @ +0xb0:** scalar-deleting vcall `(**obj)(1)` then null — matches ctor `operator_new(0x2c)` CS helper.
- **FX lists:** `Object_TeardownOwnedFxLists_Inferred` (`FUN_005179e0`, W28-H) before residual buffer frees.
- **Owned frees with typed helpers then `operator_delete`:** `+0x15c`/`0040ec70`, `+0x6c`/`0051da00`, `+0x68`/`0051da30`, `+0x64`/`005b2ba0`.
- **Also:** vcall stop on `+0x14`; stamp `GetTickCount` → `+0x18`; delete `+0x70`; `delete[]` `+0x158`; optional `FUN_004eb010` if `+0xa8` and `DAT_00b03e64`; free buffer triples `+0x148`, `+0x138`, `+0x124`.

---

## Control flow (sealed order)

```
if world(+0xa4) && world[+0xe4e8]:
  if Object_ResolveFromTFID(this+0x160) == this:
    log crash-still-in-co-list; FUN_004bb970(...)
if ptr@+0x14: vcall[+0x2c](0); +0x14=0
+0x18 = GetTickCount()
local = this & ~0xFF
CNDDoubleList_InvokePredicateAndRemove(LAB_005120e0, &local, 0)
FUN_0051b8a0()
if +0xb0: scalar_delete(+0xb0, 1); +0xb0=0
Object_TeardownOwnedFxLists_Inferred()   // FUN_005179e0
free/delete chain: +0x15c, +0x6c, +0x68, +0x64, +0x70, +0x158
if +0xa8 && flag: FUN_004eb010()
free buffer triples: +0x148, +0x138, +0x124  (always zero triads)
RET
```

---

## Confidence

| Claim | Level |
|---|---|
| Shared-base object field dtor (not scalar-deleting) | **High** |
| ABI / bare RET / body size | **High** |
| CO-list safety check + TFID@+0x160 | **High** |
| operator_delete fall-through | **High** (bytes) |
| Pair with Object_SharedBase_Ctor | **High** |
| Product C++ class English | **Open** |
| Full owned-subobject product types | **Open / partial** |
