# Raw capture: FUN_008ac890

| Field | Value |
|---|---|
| **Stable ID** | `aa_008ac890` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x008ac890` |
| **Canonical name** | `FUN_008ac890` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_008ac890(int param_1)

{
  int in_EAX;
  
  *(int *)(param_1 + 0x510) = in_EAX;
  if (8 < in_EAX) {
    *(undefined4 *)(param_1 + 0x510) = 8;
  }
  FUN_008ac110();
  return;
}
```

---

## MEGA-109 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-109** |
| **Tools** | Ghidra MCP HTTP: `decompile_function`, `analyze_function_complete` (name=`FUN_008ac890`), `disassemble_function`, `get_function_by_address`, `get_function_callers`, `get_function_callees`, `get_xrefs_to`, `get_function_signature`, `read_memory` / `inspect_memory_content` |
| **Not used** | `disassemble_bytes` (primary), Launcher, runtime Confirmed |
| **Image** | `autoassault.exe` base `0x400000` |
| **Plugin** | GhidraMCP 5.9.1 / Ghidra 12.0.4 |

### Live decompile (≡ raw CF)

```c
void __fastcall FUN_008ac890(int param_1)
{
  int in_EAX;
  *(int *)(param_1 + 0x510) = in_EAX;
  if (8 < in_EAX) {
    *(undefined4 *)(param_1 + 0x510) = 8;
  }
  FUN_008ac110();
  return;
}
```

### Body / pad

| Item | Value |
|---|---|
| Entry | `0x008ac890` |
| Inclusive end (RET) | `0x008ac8ae` |
| Exclusive end | `0x008ac8af` |
| Size | **31** B (`0x1F`) |
| Pad | `CC` at `0x008ac8af` before `FUN_008ac8b0` |

### Live disassembly (authoritative ABI)

```
008ac890  CMP  EAX, 0x8
008ac893  PUSH EDI
008ac894  MOV  EDI, ECX
008ac896  MOV  dword ptr [EDI + 0x510], EAX
008ac89c  JLE  0x008ac8a8          ; skip clamp if count <= 8
008ac89e  MOV  dword ptr [EDI + 0x510], 0x8
008ac8a8  CALL 0x008ac110          ; UI_MissionDialog_BuildResponseButtons_Inferred
008ac8ad  POP  EDI
008ac8ae  RET                      ; bare C3 — no stack args
```

### Body bytes (`read_memory` 48 B from entry)

```
83 F8 08 57 8B F9 89 87 10 05 00 00 7E 0A C7 87
10 05 00 00 08 00 00 00 E8 63 F8 FF FF 5F C3 CC
```

Call rel: `E8 63 F8 FF FF` @ `0x008ac8a8` → `0x008ac110` (verified).

### ABI seal

| Register / ret | Role |
|---|---|
| **ECX** | `MissionDialogUi*` this (moved to EDI for callee) |
| **EAX** | requested response count (`in_EAX` / phantom param) |
| Store | `dialog+0x510` = `min(count, 8)` |
| Return | void; bare **`RET`** (`C3`) |
| Convention | **ECX+EAX** register pair (not stack); not pure thiscall |

### Callers / xrefs (3 UNCONDITIONAL_CALL)

| Site | Enclosing | Setup |
|---|---|---|
| `0x008acb02` | `FUN_008ac8b0` (NUM_RESPONSES / NUM_RESPONSE_COLUMNS XML prop dispatch) | after `swscanf("%d;%d")`: `MOV EAX,[esp+0x20]; MOV ECX,EDI; CALL` |
| `0x008ad224` | adjacent ASCII key path matching `"NUM_RESPONSES"` @ `0x00a4adc0` | parse int → `MOV ECX,EBX; CALL` (EAX = parsed count) |
| `0x0093e6e9` | `FUN_0093e450` body `0x0093e450`–`0x0093e7cc` | `CALL FUN_005795b0` (vector size `(end-begin)>>2`) → `MOV ECX,EBX; CALL` |

`get_function_callers` lists **FUN_008ac8b0** only (other sites unbound / non-entry xrefs); `get_xrefs_to` / analyze complete list **all three**.

### Callees

| VA | Name |
|---|---|
| `0x008ac110` | dualed `UI_MissionDialog_BuildResponseButtons_Inferred` (EDI-this; uses `+0x510` as loop count) |

### analyze_function_complete summary

- classification: **wrapper**
- cyclomatic_complexity: **2** (via signature hash path; 3 BBs)
- param_count Ghidra: 0 (phantom `in_EAX` recovered in decompile)
- string_constants: none
- immediate: **8** (max response buttons)

### Semantics (sealed)

Set mission-dialog **response count** at `+0x510` to the EAX argument, **clamp to ≤ 8**, then rebuild response-button chrome via dualed peer `FUN_008ac110`. UI-only; no packet send.

### Name (inferred)

`UI_MissionDialog_SetResponseCountAndRebuild_Inferred` — product plate open; scaffold `Named_CalleeOf_Named_NUM_RESPONSE_COLUMNS_*` retired.

