# Annotated low-level: CVOGRegionMissions_ctor (`aa_0060b870`)

| Field | Value |
|---|---|
| Stable ID | `aa_0060b870` |
| VA | `0x0060b870` |
| Body | `0x0060b870`–`0x0060bb77` (0x308 bytes) |
| Canonical name | `CVOGRegionMissions_ctor` |
| Ghidra name | `FUN_0060b870` |
| System | `missions-progression` |
| Date | `2026-07-29` (dual A/B seal; prior scaffold 2026-07-23) |
| Tools | `decompile_function` + `read_memory` only |

---

## Machine-level notes

- **ABI:** `__fastcall` — `ECX = this` (0x10-byte wrapper), returns `this` in **EAX**. Bare `ret` after SEH teardown.
- **SEH:** standard MSVC frame (`push -1`, handler `LAB_009a8327`, `fs:[0]`).
- **Product file:** `...\libVOG\VOGRegionMissions.cpp` (OOM log line `0x33`).
- **Does not** store the wrapper into owner `+0xf18` — callers (`Mission_EnsureRegionMissions` `0x0053fff0`) allocate `0x10` then call this ctor then store.

## Layout (`this` = 4× void*)

| Off | Role | Construction |
|---|---|---|
| `+0x00` | Primary mission-id **CNDHash** (0x34, mask bits=8) | `new(0x34)` + `FUN_0053b0b0(8)` |
| `+0x04` | Objective-id **CNDHash** (0x34, distinct vtbl ctor) | `new(0x34)` + `FUN_0053b1b0(8)` |
| `+0x08` | Extended index hash (0x38, vtbl `PTR_FUN_009df688`, `+0x34=0`) | `new(0x38)` + base init + vtbl patch |
| `+0x0C` | Extended index hash (same shape as `+0x08`) | same |

## Algorithm (annotated)

```
// __fastcall ECX = this (CVOGRegionMissions*)
CVOGRegionMissions* CVOGRegionMissions_ctor(CVOGRegionMissions *this)
{
  int count = 0;
  void *dbRows = NULL;
  bool usePreload = false;

  // 1. Choose mission-def source
  if (DAT_00b0434c == 0 || ((DAT_00b04350 - DAT_00b0434c) >> 2) == 0) {
    // cold: DB enter → load quest/objective rows → DB leave
    status = FUN_007e1d80();            // DBReader_PrepareDataConnection (INFERRED)
    if (status < 0) { NDError + throw; }
    FUN_007c0350(&count, &dbRows);      // Mission_DbLoadQuestsAndObjectives (INFERRED)
    FUN_007b7df0();                     // DBReader_ReleaseDataConnection (INFERRED)
  } else {
    usePreload = true;
    count = (DAT_00b04350 - DAT_00b0434c) >> 2;
  }

  // 2. Build four hashes into this[0..3]
  this[0] = new(0x34) ? FUN_0053b0b0(8) : 0;   // primary
  this[1] = new(0x34) ? FUN_0053b1b0(8) : 0;   // objectives
  this[2] = new(0x38) ? (FUN_0053b0b0(8), vtbl=009df688, +0x34=0) : 0;
  this[3] = new(0x38) ? (same as this[2]) : 0;
  if any null: log OOM + VOG_DEBUG_STOP;

  // 3. Populate from defs
  for i in 0..count-1:
    if usePreload:
      def = DAT_00b0434c[i]
    else:
      def = FUN_005480d0(dbRows[i], 1) on new(0x170)  // materialize def

    FUN_0053c360(this[0], key=*def, value=def, soft=0)     // mission id
    if def[+0x88] != -1:
      FUN_0060c010(this[2], key=def[+0x88], value=def)
    FUN_0060c010(this[3], key=byte def[+0x86], value=def)
    for each objective j < byte def[+0x130]:
      obj = *(def[+0x13c] + j*4)
      FUN_0053c460(this[1], key=obj[+0x10], value=obj, soft=0)

    if !usePreload: CoTaskMemFree objective strings / row / ...
  if !usePreload: CoTaskMemFree(dbRows)

  return this;
}
```

### Offset gloss on mission def (from bytes + prior duals)

| Def offset | Access | Meaning (sealed in this unit) |
|---|---|---|
| `+0x00` | `*def` key for primary | mission id |
| `+0x86` | byte key → `this[3]` | secondary index (product gloss open) |
| `+0x88` | dword key → `this[2]` if ≠ -1 | secondary index (product gloss open) |
| `+0x130` (`def[0x4c]` as byte*) | objective count | loop bound |
| `+0x13c` (`def[0x4f]`) | objective pointer table | obj* array |

## Open questions

1. Product English names for secondary keys at `+0x86` / `+0x88` (region? type? chain?).
2. Exact product names of `FUN_0053b0b0` / `FUN_0053b1b0` / `FUN_0060c010` (hash ctor / insert variants).
3. Runtime / differential verification.
4. Whether OOM path continues after `VOG_DEBUG_STOP` log (no throw on OOM of hashes — only DB enter failure throws).
