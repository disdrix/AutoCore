# Annotated low-level: FUN_0048ed00 → PalantirEnv_InitEffectTextures_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0048ed00` |
| VA | `0x0048ed00`–`0x0048ed81` exclusive (**129 B** / `0x81`) |
| System | client / Palantir env effect textures (`effTexture.cpp` via `FUN_0096f0e0`) |
| Date | 2026-07-29 (W32-R dual seal; scaffold 2026-07-23 retained in raw) |

## Machine-level notes

- **ABI:** `__thiscall`; ECX → ESI (`8B F1`); bare `C3`; no SEH.
- **Role:** create/bind two A8R8G8B8 effect-texture instances onto env slots via `FUN_0096f0e0` (`effTexture.cpp` "Failed to create instance of texture.").
- **Slot 1:** `this = env+0xDC`; dims = `*(DAT_00d1f058+0x2c)+0x80` (device/display width@+0x80 height@+0x84 pair); related ptr `env+0xD4`.
- **Slot 2:** `this = env+0xE0`; dims = stack `{width/2, height/2}`; related **0**.
- **Shared args:** format `0x15` (D3DFMT_A8R8G8B8), count `1`, flag/usage `0x4011`, zero pad.
- **Decompile note:** Ghidra omits thiscall ECX for `FUN_0096f0e0`; raw bytes `LEA ECX,[ESI+0xDC]` / `LEA ECX,[ESI+0xE0]` seal slot this-adjusts.
- **Reject** scaffold `Named_CalleeOf_Drive_NDRiver_fx_0048ed00`.

## Pseudocode (annotated)

```c
// __thiscall; ECX = env*; void; bare RET
void __thiscall PalantirEnv_InitEffectTextures_Inferred(void *env)
{
  int *device = *(int **)(*(int *)(DAT_00d1f058 + 0x2c)); // decompiler: *(DAT+0x2c)
  // actually: base = *(DAT_00d1f058+0x2c); dims at base+0x80 / +0x84

  // full-res texture into slot env+0xDC; related env+0xD4
  FUN_0096f0e0(/*this=*/(int*)((char*)env + 0xDC),
               0x15,
               (void*)(*(int*)(DAT_00d1f058 + 0x2c) + 0x80),
               1, 0x4011, 0,
               (void*)((char*)env + 0xD4));

  int halfH = *(int*)(*(int*)(DAT_00d1f058 + 0x2c) + 0x84) / 2;
  int halfW = *(int*)(*(int*)(DAT_00d1f058 + 0x2c) + 0x80) / 2;
  int halfDims[2] = { halfW, halfH }; // stack layout: local_8=W/2, local_4=H/2

  // half-res texture into slot env+0xE0; related null
  FUN_0096f0e0(/*this=*/(int*)((char*)env + 0xE0),
               0x15, halfDims, 1, 0x4011, 0, 0);
}
```

## Call graph (static)

| Dir | Target | Notes |
|---|---|---|
| callers | `FUN_0048fc90` (`PalantirEnv_InitPhases_Inferred`) | sole @ `0x0048fcbf` |
| callees | `FUN_0096f0e0` ×2 | texture create/bind (`effTexture.cpp`) |

## Confidence

| Claim | Level |
|---|---|
| thiscall env method; bare RET; body 0x81 | **High** |
| dual texture create; format 0x15; half dims | **High** |
| slots +0xDC / +0xE0; related +0xD4 / 0 | **High** (bytes) |
| product texture-slot English | **Low** (open) |
| exact meaning of `0x4011` | **Medium** (sealed as constant; product enum open) |
| runtime / bit-exact | **Open** |
