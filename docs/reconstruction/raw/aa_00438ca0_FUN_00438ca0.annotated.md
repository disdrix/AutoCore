# Annotated low-level: Sto_UnserialFactoryMapB_GetInstance_Inferred (`FUN_00438ca0`)

| Field | Value |
|---|---|
| Stable ID | `aa_00438ca0` |
| VA | `0x00438ca0`–`0x00438cf9` (**90 B**) |
| System | arda2/storage — abstract unserialization factory map B |
| Date | 2026-07-23 scaffold; **W35-D seal 2026-07-29** |

## Machine-level notes

- **Role:** Meyers-style singleton getter for process-static NestedHash bag **`DAT_00d1fcec`** (same NestedHash **0x10** / span **0x28** as MapA).
- **Once-flag:** `DAT_00d1fd14` **bit0** only.
- **Cold path:** `FUN_00457ac0(&DAT_00d1fcec)` then `_atexit(LAB_009c3300)`.
- **Always:** return `&DAT_00d1fcec` in **EAX**.
- **ABI:** cdecl, no stack args, plain `RET`.
- **atexit:** `MOV EAX,&DAT_00d1fcec; JMP 0x004493eb` → same **RET 4** no-op as MapA.
- **Callers:** exclusive to Sto factory sibling insert (`FUN_00444a40`, same header string) and lookup (`FUN_00437c90`).
- **Twin of** `0x00438c40` — body template identical; only immediates (SEH / flag / static / atexit / call rel) differ.
- **BSS:** bag B sits between sibling bag C (`DAT_00d1fcc0` / `FUN_0044fee0`) and bag A (`DAT_00d1fd18`); once-flag immediately after each bag.
- **Reject** scaffold `Named_CalleeOf_*_gfxBody_*`.

## Pseudocode (annotated)

```c
// cdecl void → NestedHash_Sentinel0x10*
NestedHash_Sentinel0x10* Sto_UnserialFactoryMapB_GetInstance_Inferred(void)
{
  // SEH: LAB_009bcc2d
  if ((DAT_00d1fd14 & 1) == 0) {
    DAT_00d1fd14 |= 1;
    NestedHash_Ctor_Sentinel0x10_Inferred(&DAT_00d1fcec); // FUN_00457ac0
    _atexit(LAB_009c3300);                                // no-op RET4 teardown
  }
  return &DAT_00d1fcec;
}
```

## Sealed offsets / globals

| Symbol | Role |
|---|---|
| `DAT_00d1fcec` | Static NestedHash B (0x28) |
| `DAT_00d1fd14` | Once-flag dword (bit0) |
| `FUN_00457ac0` | NestedHash placement ctor (node 0x10) |
| `LAB_009c3300` | atexit thunk → `0x004493eb` RET4 |

## Open gaps

- Product distinction A vs B maps (tag domains / creator sets) — callers parallel but separate statics.
- Creator node / map-op callees (`FUN_0046c1b0`, `FUN_0046bf90`) unowned.
- Runtime / bit-exact / differential.
