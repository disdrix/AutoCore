# Annotated low-level: FUN_004642d0 / HeadBag_Ctor_Sentinel0x14_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004642d0` |
| VA | `0x004642d0`–`0x00464332` exclusive (**98 B** / `0x62`) |
| System | shared list-head + pointer-vector bag (0x14 circular sentinel) |
| Date | 2026-07-23 scaffold; W34-H dual 2026-07-29 |

## Machine-level notes

- Placement ctor: stack `self*`, **`RET 4`**, returns `self` in EAX.
- SEH frame `LAB_009bdd3b`.
- Sentinel: `FUN_0043c460` → `operator_new(0x14)` + circular next/prev (not 0x28 twin).
- Ptr-vec shell at `self+0x10` via `FUN_004646e0(..., 9, &head)` (W33-B).
- Flags `+0x20/+0x24 = 1`.
- **0 static callers** (orphan / dynamic / unreferenced in image).
- Twin CF of AssResolver bag ctors `FUN_00423cf0` / `FUN_00469db0` with different sentinel size.

## Pseudocode (annotated)

```c
// stdcall placement: stack(self*); RET 4; return self
HeadBag* HeadBag_Ctor_Sentinel0x14_Inferred(HeadBag* self)
{
  // SEH LAB_009bdd3b
  void* head = FUN_0043c460();       // new(0x14) circular sentinel
  self->list_head = head;            // +0x08
  self->list_aux  = 0;               // +0x0c
  {
    void* fill = head;
    FUN_004646e0(self + 0x10, 9, &fill); // 9-slot ptr-vec fill
  }
  self->flag_20 = 1;                 // +0x20
  self->flag_24 = 1;                 // +0x24
  return self;
}
```

## Open questions

- Product owner of this bag (not AssResolver — different sentinel size; no xrefs).
- Meaning of `+0x20/+0x24` flags and unwritten `+0x00/+0x04`.
- Why zero static callers (dead code vs delayed load vs missed data ref).
