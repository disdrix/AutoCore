# Annotated low-level: FUN_00520520 → Character_GetReverseEngineerSkillCapacityByTier_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00520520` |
| VA | `0x00520520`–`0x005206aa` inclusive (**395 B** / `0x18b`) |
| Jump table | `0x005206ac`–`0x005206c3` (6× dword); NOP pad `0x005206ab` |
| System | inventory-transfer / reverse-engineer skill capacity |
| Date | 2026-08-05 (R10-033 OWN dual refresh) |
| Canonical name | `Character_GetReverseEngineerSkillCapacityByTier_Inferred` |
| Ghidra name | `FUN_00520520` |
| Prior scaffold | `Named_CalleeOf_Named_CalleeOf_Skill_You_do_not_possess_the_Tink_00520520` (**retired**) |
| Parent dual | `0x0052e640` `Character_ComputeReverseEngineerSuccessFraction_Inferred` |
| Terminal | **false** |

## Machine-level notes

- Live tools: `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + callers/xrefs. **No** `disassemble_bytes`.
- Decompile ≡ 2026-07-23 raw CF; ABI sealed by assembly (`RET 4`, `[ECX+0x598]`, jump table).
- `analyze` signature `undefined FUN_00520520(void)` is **incorrect** — thiscall + one stack tier arg.
- Sole caller: RE success-fraction parent; ECX is **character**, not item.

## Signature (sealed)

```c
// __thiscall: ECX = character; stack arg0 = tier (0..5); RET 4; EAX = capacity
int __thiscall Character_GetReverseEngineerSkillCapacityByTier_Inferred(
    void *character,   // ECX — skill byte at +0x598
    unsigned tier);    // stack — case select; >5 → 0
```

## Multipliers (read_memory)

| Tier | Mult | DAT |
|---:|---:|---|
| 0, 1 | 3.5 | `0x009da8a4` |
| 2 | 2.8 | `0x00aaaa3c` |
| 3 | 2.1 | `0x009cefc0` |
| 4 | 1.4 | `0x009cefbc` |
| 5 | 0.7 | `0x00a0f710` |
| >5 | — | return 0 |

Pattern: `3.5 × {1.0, 1.0, 0.8, 0.6, 0.4, 0.2}`.

## Pseudocode (annotated)

```c
/* character+0x598 = RE skill byte (product English open → _Inferred)
 * tier selects decreasing capacity scale for recipe-row / score tiers.
 * floor via CRT; FISTP with RC=chop implements Ghidra ROUND after floor.
 */

int __thiscall Character_GetReverseEngineerSkillCapacityByTier_Inferred(
    int character, unsigned tier)
{
  double dVar1;
  float skill = (float)*(unsigned char *)(character + 0x598);

  switch (tier) {
  case 0:
  case 1:
    dVar1 = floor((double)(skill * 3.5f));   // _DAT_009da8a4
    return (int)dVar1;                       // ROUND/FISTP chop
  case 2:
    dVar1 = floor((double)(skill * 2.8f));   // _DAT_00aaaa3c
    return (int)dVar1;
  case 3:
    dVar1 = floor((double)(skill * 2.1f));   // _DAT_009cefc0
    return (int)dVar1;
  case 4:
    dVar1 = floor((double)(skill * 1.4f));   // _DAT_009cefbc
    return (int)dVar1;
  case 5:
    dVar1 = floor((double)(skill * 0.7f));   // DAT_00a0f710
    return (int)dVar1;
  default:
    return 0;
  }
}
```

## Callers

| Site | Parent use |
|---|---|
| `0x0052ea2b` | Recipe-row gate: `PUSH row_index+1`; fail if secondary cost > capacity |
| `0x0052ea96` | Score loop: `PUSH tier 0..5`; capacity[t] in `(capacity − need)` term |

## Open questions

- Product English for skill field `+0x598` (Reverse Engineering skill rank?).
- Whether tier index is designer-facing “quality band” vs internal recipe-row ordinal only.
- Runtime / bit-exact / differential (Terminal false).
