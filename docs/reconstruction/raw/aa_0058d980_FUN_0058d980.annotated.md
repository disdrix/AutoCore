# Annotated low-level: CVOGPhysics_ApplyPointImpulse (`aa_0058d980`)

| Field | Value |
|---|---|
| Stable ID | `aa_0058d980` |
| VA | `0x0058d980` |
| Canonical name | `CVOGPhysics_ApplyPointImpulse` (inferred) |
| Ghidra name | `FUN_0058d980` |
| System | `physics` / `CVOGPhysics` |
| Date | 2026-07-29 (W16-B dual refine) |

## Machine-level notes

- Source: raw capture for `aa_0058d980` + live `decompile_function` / `read_memory` (2026-07-29).
- Sibling family: same ready-gate prologue as `CVOGPhysics_SetAngularVelocity` (`0x0040d040`, vtbl+0x54) and `CVOGPhysics_ApplyImpulseVector` (`0x0040d260`, vtbl+0x50); **this unit** ends `call [edx+0x60]` with **two** stack args (`ret 8`).
- Verified physics docs name body vtbl **`+0x60`** as **`applyPointImpulse`** (impulse vec + contact/point object) — see `physics/0.4-suspension.md`, postTick hardpoint impulse.
- Decompiler form `FUN_005070b0((int)&uStack_4+3)` is out-byte scaffolding; asm keeps ECX=`phys` for both helpers.

## Offsets / slots

| Slot | Role | Confidence |
|---|---|---|
| `phys+0x3c` | Bound rigid body* | **High** (sibling duals) |
| `phys+0x44` | Host object* (ready probe source) | **High** behavioral / type **Tentative** |
| `*(host+0x29)` | Ready flag byte | **High** via `FUN_005070b0` dual |
| body vtbl **`+0x60`** | applyPointImpulse (impulse, point) | **High** imm + prior art |
| Stack arg1 | impulse vector* (typically 4×f32) | **High** as passthrough |
| Stack arg2 | point / wheel / contact object* | **High** as second arg; English layout **Open** |

## Pseudocode (annotated copy of raw)

```c
void __thiscall FUN_0058d980(int phys, undefined4 impulse, undefined4 point)
{
  char *outReady;
  undefined4 uStack_4;

  uStack_4 = phys;                                  /* decompiler stack artifact */
  /* asm: lea out-byte; thiscall ProbeReadyFlag(phys, &out) → EAX=out* */
  outReady = (char *)FUN_005070b0((int)&uStack_4 + 3);
  if ((*outReady == '\0') && (*(int *)(phys + 0x44) != 0)) {
    FUN_005070d0();                                 /* this = phys (ECX) */
  }
  /* body = *(phys+0x3c); no null check */
  (**(code **)(**(int **)(phys + 0x3c) + 0x60))(impulse, point);
  return;
}
```

## ABI

```
__thiscall  ECX = phys
stack       [esp+4] = impulse*, [esp+8] = point*
ret 8
```

## Open questions

- Exact Havok / product symbol string for vtbl+0x60 (prior art uses applyPointImpulse).
- Full type of `point` arg (wheel struct vs raw float3 vs hkVector4) — caller duals.
- Full caller inventory (OWN-ONLY: xrefs tools not used this pass; scaffold alias cites VOG_DEBUG path).
