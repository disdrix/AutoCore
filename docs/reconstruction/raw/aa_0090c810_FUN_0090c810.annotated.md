# Annotated low-level: MedalUI_CountDisplayableEntries_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0090c810` |
| VA | `0x0090c810`–`0x0090c8c9` (**186 B** / `0xBA`) |
| Ghidra | `FUN_0090c810` |
| Named | `MedalUI_CountDisplayableEntries_Inferred` |
| System | `missions-progression` |
| Date | 2026-08-05 (MEGA-103 OWN dual refresh) |
| Prior scaffold | 2026-07-23 auto annotate (superseded) |

---

## Machine-level notes

- Source: raw capture + MEGA-103 live re-verify append.
- Prefer assembly when decompiler conflicts (`unaff_EBX` = **EBX register this**, not phantom).
- Custom convention: callers load **`DAT_00d1b6d8` → EBX** before `CALL`.
- Callee `FUN_0051a0e0` = dualed **`MedalDef_LookupById`** (cdecl, 1 stack arg, caller `ADD ESP,4`).
- Hash traversal lock byte at **`hash+0x1d`**; warn-only via `FUN_007a4480` + `"VOG_DEBUG_STOP"` (does not abort).
- Scaffold name `Named_VOG_DEBUG_STOP_0090c810` **retired** — VOG string is debug path only.

---

## Pseudocode (annotated)

```c
// EBX = character / medal-state object* (from DAT_00d1b6d8)
// EAX = total displayable entry count
// bare RET
int MedalUI_CountDisplayableEntries_Inferred(void /* EBX = char* */)
{
  int hashObj;
  int count;
  int cursor;   // hash chain node*
  int payload;

  if (EBX == 0) {
    return 0;
  }

  // Phase A: dword vector [begin,end) at +0x574 / +0x578
  if (*(int *)(EBX + 0x574) == 0) {
    count = 0;
  } else {
    count = (*(int *)(EBX + 0x578) - *(int *)(EBX + 0x574)) >> 2;
  }

  // Phase B: hash table at +0x540
  hashObj = *(int *)(EBX + 0x540);
  cursor = 0;

  // TraversalLock: warn if already locked, then force lock=1
  if (*(char *)(hashObj + 0x1d) != 0) {
    FUN_007a4480(0, "HashError:TraversalLock, already locked for traversal");
    FUN_007a4480(0, "VOG_DEBUG_STOP");
  }
  *(uint8_t *)(hashObj + 0x1d) = 1;

  while (true) {
    hashObj = *(int *)(EBX + 0x540);

    // TraverseToNext: warn if lock cleared mid-walk
    if (*(char *)(hashObj + 0x1d) == 0) {
      FUN_007a4480(0, "HashError:TraverseToNext, not locked for traversal");
      FUN_007a4480(0, "VOG_DEBUG_STOP");
    }

    // first step: hash[+0x14]; subsequent: node[+0x14]
    if (cursor == 0) {
      cursor = *(int *)(hashObj + 0x14);
    } else {
      cursor = *(int *)(cursor + 0x14);
    }

    if (cursor == 0) {
      payload = 0;
    } else {
      payload = *(int *)(cursor + 8);
    }

    if (payload == 0) {
      break;  // end of chain
    }

    // MedalDef_LookupById(payload[+0x100])
    if (FUN_0051a0e0(*(uint32_t *)(payload + 0x100)) != 0) {
      count = count + 1;
    }
  }

  *(uint8_t *)(*(int *)(EBX + 0x540) + 0x1d) = 0;  // unlock
  return count;
}
```

---

## Offsets (character / medal-state object)

| Offset | Role | Conf |
|---|---|---|
| `+0x540` | Hash table object* (chain head `@+0x14`, lock `@+0x1d`) | **High** |
| `+0x574` | Owned medal-id vector begin (`uint32_t*`) | **High** |
| `+0x578` | Owned medal-id vector end | **High** |

## Hash / chain node

| Offset | Role | Conf |
|---|---|---|
| hash `+0x14` | First chain node | **High** |
| hash `+0x1d` | Traversal lock byte | **High** |
| node `+0x14` | Next chain node | **High** |
| node `+0x08` | Payload / entry* | **High** |
| payload `+0x100` | Medal definition id (lookup key) | **High** |

---

## Call graph (this unit)

```text
MedalUI_CountDisplayableEntries_Inferred  [OWN MEGA-103]
  ├─ FUN_007a4480  debug log (warn-only)
  └─ FUN_0051a0e0  MedalDef_LookupById  [dualed]

Callers:
  FUN_0090cbc0  @ 0x0090cbfa  medal UI rebuild (after prep clear)
  FUN_0090c920  @ 0x0090c938  count-threshold visibility gate
  0x0090c8d0    @ 0x0090c8ed  twin gate (RET 8; Ghidra unnamed)
```

---

## Open questions

- Product English for character class holding `+0x540` / `+0x574` (global `DAT_00d1b6d8`).
- Whether vector entries are guaranteed pre-validated (count uses raw size).
- Product name for hash container type (lock strings say "HashError").
- Runtime / bit-exact / differential.
