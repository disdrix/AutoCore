# Annotated low-level: stoChunkReader_ReadU32_ModeDispatch (FUN_00436900)

| Field | Value |
|---|---|
| Stable ID | `aa_00436900` |
| VA | `0x00436900`–`0x0043691a` |
| Size | 27 bytes |
| System | storage / arda2 `stoChunk` |
| Date | 2026-07-29 (W22-R dual seal) |
| Prior scaffold | 2026-07-23 |

## Machine-level notes

- Source: raw capture + W22-R `decompile_function` / `read_memory` re-verify.
- Prefer entry bytes when decompiler understates return type (`void` vs status in EAX).
- Mode field `reader+0x4044`: **0 = binary**, non-zero = **text** (same convention as `stoChunkReader_OpenChunk` / `PeekChunkTag` / f32 array readers).
- Binary arm: thin trampoline — `push ecx; call ReadU32; ret` (no EDI save).
- Text arm: must place reader in **EDI** for `FUN_007689e0` (`unaff_EDI` in decompiler); save/restore EDI around the call.
- Both arms pass **out*** as the single stack formal (ECX at entry → `push ecx`).

## ABI

```
// Entry: EAX = StoChunkReader*, ECX = uint32_t* out
// Exit:  EAX = 0 success / 0xffffffff fail; plain ret
int32_t stoChunkReader_ReadU32_ModeDispatch(/*EAX*/ reader, /*ECX*/ uint32_t* out);
```

## Annotated pseudocode

```c
// Ghidra: FUN_00436900
int32_t stoChunkReader_ReadU32_ModeDispatch(StoChunkReader* reader /*EAX*/, uint32_t* out /*ECX*/)
{
  if (*(int32_t*)((uint8_t*)reader + 0x4044) == 0) {
    // binary: LE dword via stream window
    return stoChunkReader_ReadU32(reader, out); // FUN_00437050
  }
  // text: token/string → parse uint32 (plate stoChunk.cpp)
  return FUN_007689e0(out); // reader via EDI
}
```

## Layout touch

| Offset | Role |
|---:|---|
| `reader+0x4044` | mode selector (0 binary / ≠0 text) |

No other reader fields are read in this unit (callees own stream window / text state).

## Open questions

- Product English name of the dispatch helper (may be inlined as `ReadU32` overload in source).
- Exact product type of non-zero `+0x4044` (bool vs enum); observed as integer compare-to-zero only.
- Runtime / bit-exact image diff.
