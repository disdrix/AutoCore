# Annotated low-level: CVOGEnvironmentReflect_ResolveLightEffectSlots_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005b3520` |
| VA | `0x005b3520`–`0x005b357a` (**91 B** / `0x5B`) |
| Ghidra | `FUN_005b3520` |
| Canonical | `CVOGEnvironmentReflect_ResolveLightEffectSlots_Inferred` |
| System | client / VOGEnvironmentSector / CVOGEnvironmentReflect |
| Date | 2026-08-04 (W37-F dual seal) |

## Machine-level notes

- Source: raw capture + W37-F `read_memory` full 91 B + live decompile.
- Prefer bytes when decompiler omits `MOV ECX,[ECX+4]` this-adjust into `FUN_0074e310`.
- Host class sealed by ctor/enable RTTI (W32-T/W33-T), not by strings in this body.
- **No** product strings in body.

## ABI

| Item | Evidence | Conf |
|---|---|---|
| ECX = Reflect `this` | callers pass Reflect / phase; ctor family | **High** |
| No stack args | bare `C3`; no `RET n` | **High** |
| Void | no `XOR EAX` / return value | **High** |
| Once-gate `+0x154` | `CMP [ECX+0x154],0` / `JNZ` | **High** |
| Parent `+0x15c` | ctor `param_1[0x57]=param_2` | **High** |
| Nested host `parent+0xe894` | bytes + W32-R light host path | **High** |
| Dir light `host+0x108`, hemi `host+0x104` | W32-R rebuild slots | **High** |
| Effect core at light `+0x8c` | W32-R | **High** |
| Bind via `FUN_0074e310(this=[self+4], res)` | `8B 49 04; 50; E8…` | **High** |

## Pseudocode (annotated)

```c
// ECX = CVOGEnvironmentReflect* self
void __thiscall CVOGEnvironmentReflect_ResolveLightEffectSlots_Inferred(void *self)
{
  // Once: only if self+0x154 empty
  if (*(int *)((char *)self + 0x154) != 0)
    return;

  void *parent = *(void **)((char *)self + 0x15c);
  void *lightHost = *(void **)((char *)parent + 0xe894); // nested env / light table host

  // Require directional light present
  if (*(int *)((char *)lightHost + 0x108) == 0)
    return;

  // Bytes: enter only when hemi slot == 0 (see residual)
  if (*(int *)((char *)lightHost + 0x104) != 0)
    return;

  // Copy dir light effect core → self+0x154
  void *dir = *(void **)((char *)lightHost + 0x108);
  *(void **)((char *)self + 0x154) = *(void **)((char *)dir + 0x8c);

  // Reload host; copy hemi effect core → self+0x150; bind on aux
  lightHost = *(void **)((char *)parent + 0xe894);
  void *hemi = *(void **)((char *)lightHost + 0x104); // null if gate held
  void *res = *(void **)((char *)hemi + 0x8c);
  *(void **)((char *)self + 0x150) = res;

  void *aux = *(void **)((char *)self + 4);
  FUN_0074e310(aux, res); // thiscall
}
```

## Call graph

| Direction | Target | Note |
|---|---|---|
| Caller | `0x005b39a0` Enable | after EnsureAuxPtrs, before fog child alloc |
| Caller | `0x0048f370` RebuildDefaultLights | tail if `env+0xC4` (Reflect) non-null |
| Callee | `0x0074e310` | refcount install of res onto aux |
| Peer | `0x005b3580` | re-push/bind `+0x150` when set (not owned) |

## Open / residual

1. Hemi-null gate vs immediate hemi deref — product residual (no-op after full light rebuild).
2. Product English for light/effect objects at `+0x8c` beyond W32-R structural plate.
3. Runtime / bit-exact / differential — open.
