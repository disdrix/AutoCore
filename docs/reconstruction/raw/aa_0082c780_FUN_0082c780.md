# Raw capture: FUN_0082c780

| Field | Value |
|---|---|
| **Stable ID** | `aa_0082c780` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0082c780` |
| **Canonical name** | `FUN_0082c780` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 __thiscall FUN_0082c780(int *param_1,int param_2,int param_3)

{
  char cVar1;
  undefined4 uVar2;
  
  if ((param_3 == 70000) && (((param_2 == 8 || (param_2 == 0xb)) || (param_2 == 0xe)))) {
    FUN_0090d400();
    return 1;
  }
  cVar1 = (**(code **)(*param_1 + 0xd8))();
  if ((cVar1 != '\0') && (param_1[0xac] != 0)) {
    uVar2 = (**(code **)(*(int *)param_1[0xac] + 0x338))(param_2,param_3);
    return uVar2;
  }
  return 0;
}
```

---

## R11-027 re-verify append (2026-08-05) — do not wipe prior body

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **R11-027** |
| **Canonical name (promoted)** | `UI_Window_OnCommand_Base_Inferred` |
| **System** | inventory-transfer (partition host) / UI OnCommand base |
| **Tools** | Ghidra HTTP: `decompile_function`, `analyze_function_complete`, `disassemble_function`, `read_memory`, callers/xrefs/callees, `get_function_by_address`. **No** `disassemble_bytes`. No Launcher. |
| **Image** | `autoassault.exe` base `0x400000` |
| **Partition parent** | `0x0082f510` (`UI_Window_OnCommand_Class8_Inferred`, dualed WQ8R-F) |
| **Live decompile ≡ 2026-07-23 raw CF** | **Yes** (byte-identical structure) |

### Body / ABI (sealed)

| Claim | Evidence |
|---|---|
| Body `0x0082c780`–`0x0082c7eb` inclusive last byte (**108 B** / `0x6C`); pad `CC×4` before next code | `get_function_by_address` Body `0082c780 - 0082c7eb`; `read_memory` 112 B ends `…C2 08 00 CC CC CC CC` |
| `__thiscall` ECX=`this`; stack `(cmdClass, cmdId)`; **`RET 8`** (`C2 08 00`) ×3 exit sites | `disassemble_function` |
| Stack formals: `[ESP+0xc]` after push = cmdId → EBX; `[ESP+0x10]` after more pushes = cmdClass → EDI; ESI=ECX | disasm @ `0082c781`/`0082c78d`/`0082c791` |

### Control flow (sealed)

```text
UI_Window_OnCommand_Base_Inferred(this, cmdClass, cmdId):
  if cmdId == 70000 (0x11170) && cmdClass ∈ {8, 0xb, 0xe}:
    EDI = this[+0x2b0]          // child/subwindow ptr (for FUN_0090d400 unaff_EDI)
    // ESI = this (already)
    FUN_0090d400()              // ESI=self, EDI=child@+0x2b0 — dismiss/hide helper
    return 1                    // AL=1
  if this.vtbl[+0xd8]() != 0 && this[+0x2b0] != 0:
    return child(+0x2b0).vtbl[+0x338](cmdClass, cmdId)   // PUSH EBX=cmdId; PUSH EDI=cmdClass; thiscall child
  return 0                      // XOR AL,AL
```

### Disassembly (authoritative)

```text
0082c780: PUSH EBX
0082c781: MOV EBX,dword ptr [ESP + 0xc]     ; cmdId
0082c785: CMP EBX,0x11170                  ; 70000
0082c78b: PUSH ESI
0082c78c: PUSH EDI
0082c78d: MOV EDI,dword ptr [ESP + 0x10]    ; cmdClass
0082c791: MOV ESI,ECX                      ; this
0082c793: JNZ 0x0082c7a4                   ; not 70000 → forward path
0082c795: CMP EDI,0x8
0082c798: JZ 0x0082c7d9
0082c79a: CMP EDI,0xb
0082c79d: JZ 0x0082c7d9
0082c79f: CMP EDI,0xe
0082c7a2: JZ 0x0082c7d9
0082c7a4: MOV EAX,dword ptr [ESI]          ; vtbl
0082c7a6: MOV ECX,ESI
0082c7a8: CALL dword ptr [EAX + 0xd8]      ; predicate
0082c7ae: TEST AL,AL
0082c7b0: JZ 0x0082c7d1
0082c7b2: CMP dword ptr [ESI + 0x2b0],0x0  ; child ptr
0082c7b9: JZ 0x0082c7d1
0082c7bb: MOV ECX,dword ptr [ESI + 0x2b0]
0082c7c1: MOV EDX,dword ptr [ECX]
0082c7c3: PUSH EBX                         ; cmdId
0082c7c4: PUSH EDI                         ; cmdClass
0082c7c5: CALL dword ptr [EDX + 0x338]     ; child OnCommand
0082c7cb: POP EDI / POP ESI / POP EBX
0082c7ce: RET 0x8
0082c7d1: POP EDI / POP ESI
0082c7d3: XOR AL,AL
0082c7d5: POP EBX
0082c7d6: RET 0x8
0082c7d9: MOV EDI,dword ptr [ESI + 0x2b0]  ; child → EDI for FUN_0090d400
0082c7df: CALL 0x0090d400
0082c7e4: POP EDI / POP ESI
0082c7e6: MOV AL,0x1
0082c7e8: POP EBX
0082c7e9: RET 0x8
```

### Body hex (`read_memory` 108 live + 4 pad)

```text
538b5c240c81fb7011010056578b7c24108bf1750f83ff08743f83ff0b743a83ff0e7435
8b068bceff90d800000084c0741f83beb00200000074168b8eb00200008b115357ff9238030000
5f5e5bc208005f5e32c05bc208008bbeb0020000e81c0c0e005f5eb0015bc20800
cccccccc
```

Immediates sealed: `0x11170` (70000), class `8`/`0xb`/`0xe`, vtbl slots `0xd8` / `0x338`, field `+0x2b0`, epilogue `C2 08 00`.

### Xrefs / callers / callees

| Kind | Detail |
|---|---|
| **Callees** | `FUN_0090d400` @ `0x0090d400` only (direct CALL); child `vtbl+0x338` and `this.vtbl+0xd8` are indirect |
| **Callers (named)** | `FUN_0082f510` @ `0082f558`, `0082f786` (dualed Class8 OnCommand); `FUN_00830aa0` @ `00830b36` |
| **Other CALL xrefs** | `0082de0c`, `00831272`, `00831821`, `00832b41` (Ghidra has no enclosing function symbol at those VAs this pass) |
| **DATA** | `0x00a732a4` → dword `0x0082c780` (vtable slot) |

### Callee note (context only; not OWN)

`FUN_0090d400` is register-convention helper: **ESI** = self window, **EDI** = child@`+0x2b0`. Body (live decompile): if ESI≠0 and `ESI.vtbl+0x3d8()` then hide/detach chain on ESI (`+0xcc(0)`, `+0x440`) and EDI (`+0xb0`, optional `+0x3bc(DAT_00d1b978)`, `+0x3f4(-1)`). This seals the **70000** path as dismiss/teardown, not inventory transfer.

### Decompiler caveats (not CF conflicts)

1. Decompiler omits `MOV EDI,[ESI+0x2b0]` before `FUN_0090d400` — asm seals EDI setup for unaff_EDI.
2. Some returns only set **AL** (`XOR AL,AL` / `MOV AL,1`); forward path returns full EAX from child call. Typed as `uint32_t` with low-byte-significant false path.
3. `param_1[0xac]` in decompile = dword index → byte offset **`+0x2b0`**.

### Naming

| Candidate | Ruling |
|---|---|
| `FUN_0082c780` | Scaffold only |
| `UI_Window_OnCommand_Base_Inferred` | **Accept** — base OnCommand: 70000 dismiss for classes {8,0xb,0xe} or forward to child `vtbl+0x338`; matches dualed Class8 parent terminology ("Base_OnCommand") |
| Inventory packet / grid transfer name | **Reject** — no packets, slots, or item IDs |
| Product English / PDB demangle | **Open** → keep `_Inferred` |

