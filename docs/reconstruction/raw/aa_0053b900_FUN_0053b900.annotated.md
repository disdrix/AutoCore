# Annotated low-level: CNDHash_scalar_dtor_009cfa7c (FUN_0053b900)

| Field | Value |
|---|---|
| Stable ID | `aa_0053b900` |
| VA | `0x0053b900`–`0x0053b91d` |
| Canonical | `CNDHash_scalar_dtor_009cfa7c` (**Inferred**) |
| Ghidra | `FUN_0053b900` |
| System | container / CNDHash |
| Date | 2026-07-29 (W25-H dual seal) |
| Tools | `decompile_function` + `read_memory` + xrefs (no `disassemble_bytes`) |

---

## Purpose

MSVC **scalar-deleting destructor** for the **owning CNDHash** family whose primary vtbl is **`PTR_FUN_009cfa7c`**:

1. Call the real destructor body `FUN_0053b920` / `CNDHash_Dtor_009cfa7c` (SEH, FreeBuckets owning, freelist `this+0x20`).
2. If **`flags & 1`**, `operator_delete(this)`.
3. Return `this` (EAX).

Stack cleanup: **`ret 4`** (one stack arg = flags).

---

## Annotated CF

```c
// __thiscall: ECX = this (CNDHash*), stack[0] = flags
// Returns this*; ret 4
void *__thiscall CNDHash_scalar_dtor_009cfa7c(void *thisHash, unsigned char flags)
{
  // MACHINE: ESI = this; call bare body dtor (thiscall ECX=this still, or ESI-saved)
  CNDHash_Dtor_009cfa7c(thisHash); // FUN_0053b920

  if ((flags & 1) != 0) {
    operator_delete(thisHash); // cdecl; add esp,4
  }
  return thisHash;
}
```

### Decompiler residual

Ghidra warns `operator_delete` "does not return" — **false** for CRT `operator_delete`. Machine continues to `mov eax,esi; pop esi; ret 4`.

---

## Machine seal

Hex @ `0x0053b900`:

```
56 8b f1 e8 18 00 00 00 f6 44 24 08 01 74 09 56
e8 0d df f4 ff 83 c4 04 8b c6 5e c2 04 00 cc cc
```

| Claim | Evidence | Conf |
|---|---|---|
| thiscall ECX→ESI | `56 8B F1` | **High** |
| Always runs body dtor `0053b920` | `E8 18 00 00 00` → `0x0053b920` | **High** |
| flags&1 gate | `F6 44 24 08 01` / `74 09` | **High** |
| Conditional heap free of **this only** | push esi; call `operator_delete` | **High** |
| Returns this; **ret 4** | `8B C6 5E C2 04 00` | **High** |
| vtbl[0] of `009cfa7c` | DATA xref + dword0 `00 b9 53 00` | **High** |

---

## Family

| Unit | VA | Role |
|---|---|---|
| **This** scalar | `0x0053b900` | vtbl[0] wrapper + optional delete |
| Body dtor | `0x0053b920` | owning FreeBuckets + freelist |
| Freelist dtor | `0x0059c8a0` | called from body via `lea ecx,[this+0x20]` |

---

## Open

1. Product mangled name / RTTI for the CNDHash specialization.
2. Full product English for every method on `009cfa7c` (out of sole ownership).
3. Runtime / bit-exact — open.
