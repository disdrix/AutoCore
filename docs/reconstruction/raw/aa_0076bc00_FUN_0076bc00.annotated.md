# Annotated low-level: FUN_0076bc00 → Fs_CreateDirectoryForce_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0076bc00` |
| VA | `0x0076bc00`–`0x0076bc79` exclusive (**121 B**) |
| System | filesystem / directory create with optional force clear |
| Date | 2026-08-04 (W37-Z) |

## Machine-level notes

- Source: raw capture + W37-Z re-verify for `aa_0076bc00`.
- cdecl 2 args; bool AL; bare `RET` after `ADD ESP,0x24`.
- Force path: is-dir → recursive wipe then recreate; is-regular-file → force unlink then create.
- `st_mode >> 15 & 1` is CRT **`_S_IFREG` (0x8000)**, not directory (dir is bit 14 / `0x4000`).
- `FUN_0076b080(1)` takes force flag on stack; **path in ESI** (peer convention from `Fs_DeleteDirTreeRecursive`).

## Signature (byte-sealed)

```c
// cdecl; path + force_clear; bool AL
bool __cdecl Fs_CreateDirectoryForce_Inferred(const char* path, char force_clear);
```

## Pseudocode (annotated)

```c
bool Fs_CreateDirectoryForce_Inferred(const char* path, char force_clear)
{
  struct _stat st; // CRT layout; st_mode at +6

  if (force_clear) {
    if (Fs_PathIsDirectory_Stat(path)) {           // FUN_0076adc0
      Fs_DeleteDirTreeRecursive(path, /*force*/1); // FUN_0076ba10
      return CreateDirectoryA(path, NULL) != 0;
    }
    if (stat(path, &st) == 0 && (st.st_mode & _S_IFREG)) {
      // ESI=path; force unlink / chmod path
      FUN_0076b080(/*force*/1);
    }
  }
  return CreateDirectoryA(path, NULL) != 0;
}
```

## Call graph

| Direction | Targets |
|---|---|
| Callees | `FUN_0076adc0`, `FUN_0076ba10`, `FUN_0076b080`, `stat`, `CreateDirectoryA` |
| Callers | `FUN_004b52e0`, `FUN_0076bc80`, `FUN_007a13e0`, `FUN_00985010` (CompactPackFile `"_pk_"`) |

## Open questions

- Product English plate (structural `_Inferred` only).
- Full contract of unowned `FUN_0076b080` (force bits / return).
- Whether non-force + already-exists-as-dir is intentional fail (CreateDirectoryA ERROR_ALREADY_EXISTS).
- Runtime / differential verification.
