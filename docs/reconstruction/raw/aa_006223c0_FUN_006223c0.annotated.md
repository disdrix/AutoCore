# Annotated low-level: FUN_006223c0 / CVOGHBSkill_KillCharge_Ctor

| Field | Value |
|---|---|
| Stable ID | `aa_006223c0` |
| VA | `0x006223c0`–`0x006223f3` (52 B / `0x34`) |
| System | skills-abilities |
| Date | 2026-08-05 R11-019 OWN-ONLY dual |
| Canonical (Ghidra) | `FUN_006223c0` |
| Named | `CVOGHBSkill_KillCharge_Ctor` (RTTI Confirmed) |

## Machine-level notes

- Source: raw capture for `aa_006223c0` + live Ghidra re-verify (no `disassemble_bytes`).
- **Role:** trivial subclass constructor for `CVOGHBSkill_KillCharge`.
- **ABI:** `__thiscall` ECX=this; **6 stack dwords** forwarded unchanged into `CVOGHBSkillBase_ctor`; **`ret 0x18`**; **EAX=this**.
- **Body:** base skill-HB ctor → install vtbl `PTR_FUN_009d16e4` → return this.
- **No** field stores beyond vtbl; no Enqueue/Start; no period seed in this leaf.
- Sole CALL site is `FUN_00622f70` (CommonCastOnDeath factory) which allocates `0x6d0` and then **replaces** vtbl with `PTR_FUN_009d1744`. Peer factory `FUN_006226a0` inlines the same base+vtbl pattern for pure KillCharge objects of size `0x6c0`.

## Pseudocode (annotated)

```c
// __thiscall  ECX = this (fresh CVOGHBSkill_KillCharge / intermediate for subclass)
// stack: pCaster, pSkillBlob, pWorldOrCtx, pTarget, pTfidBlob, arg7
// ret 0x18; EAX = this
void *CVOGHBSkill_KillCharge_Ctor(
    void *thisHb,
    void *pCaster,       // → CVOGHBSkillBase_ctor param_2 (TFID @ +0x160)
    void *pSkillBlob,    // → skill blob copy (0x18c dwords into this+0x24)
    void *pWorldOrCtx,   // → base param_4 (+0x1a2)
    void *pTarget,       // → ValidateTarget + AttachOwner
    void *pTfidBlob,     // → 16 B @ +0x1a4
    uint32_t arg7)       // → +0x1aa / FUN_0058ab60
{
  // live: CVOGHBSkillBase_ctor @ 0x005788d0 (partition parent; RTTI Confirmed)
  CVOGHBSkillBase_ctor(thisHb, pCaster, pSkillBlob, pWorldOrCtx, pTarget, pTfidBlob, arg7);

  // bytes: C7 06 E4 16 9D 00
  *(void **)thisHb = &PTR_FUN_009d16e4;  // RTTI .?AVCVOGHBSkill_KillCharge@@

  return thisHb;
}
```

## Open questions

- Product design English for "KillCharge" beyond RTTI class name.
- Full semantic names for the six base-ctor stack args (owned by parent dual of `0x005788d0`).
- Whether any non-factory path constructs KillCharge without going through `FUN_006226a0` / subclass factories (only one CALL xref to this VA).
