# Raw capture: FUN_0096ef70

| Field | Value |
|---|---|
| **Stable ID** | `aa_0096ef70` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0096ef70` |
| **Canonical name** | `FUN_0096ef70` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0096ef70(undefined4 *param_1)

{
  *param_1 = 0;
  return;
}
```

# W25-N dual seal append — 2026-07-29
# Integrity: prior raw body above is authoritative; this section is live re-verify only.

## W25-N live re-verify (2026-07-29)

**Tools:** Ghidra `decompile_function` @ `0x0096ef70`; nested peer dtor `FUN_0096efd0` @ `0x0096efd0` for role context only; `read_memory` 16 B @ entry; bulk xrefs (62). No `disassemble_bytes`. No Launcher.

### Bounds
| Field | Value |
|---|---|
| Ghidra body | `0x0096ef70`–`0x0096ef78` (**9 B** inclusive) |
| Epilogue | `C3` **ret** (no stack cleanup) |

### Live decompile
```c
void __fastcall FUN_0096ef70(undefined4 *param_1)
{
  *param_1 = 0;
  return;
}
```

### Entry bytes (sealed CF) — full body
```
0096ef70: 8B C1                 mov eax,ecx
0096ef72: C7 00 00 00 00 00     mov dword ptr [eax],0
0096ef78: C3                    ret
0096ef79: CC …                  int3 pad
```

### ABI (Confirmed)
| Formal | Source | Notes |
|---|---|---|
| `this` / handle* | **ECX** (`__fastcall` ≡ MSVC thiscall for single ptr) | copied to EAX |
| Stack formals | none | `ret` not `ret N` |
| Return | **this*** in EAX (ctor idiom) | decompiler `void` understates |

### Behavior (Confirmed)
Stores **0** into the first dword of the object pointed by ECX. No other fields touched. No callees.

### Role (INFERRED from peer + callers)
Paired destructor `FUN_0096efd0` (not OWN) releases a refcounted payload:
- if `*handle != 0`: `--(*(int*)(obj+8))`; if zero call `(*(vtbl*)(obj+4))[2]` (release); then null handle
- so this unit is the **default ctor / null-init** of a 4-byte **refcounted handle** (`ptr` field only).

Callers (~62): `CVOGTacArc_ctor` (×2 embedded slots), paint/`FUN_00583170`, graphics loaders, asset/UI paths — all construct empty handles before later attach.

Sibling init (different type, not OWN): `FUN_0096f530` zeros dword + 3 flag bytes.

### Name (role-sealed INFERRED)
`RefCountedPtr_DefaultCtor_Inferred` — null-init first dword of intrusive/shared handle.
