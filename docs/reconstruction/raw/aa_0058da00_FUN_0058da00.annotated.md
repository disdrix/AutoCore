# Annotated low-level: MapFloatTfid_FreeSubtree_Isnil29_Inferred (FUN_0058da00)

| Field | Value |
|---|---|
| Stable ID | `aa_0058da00` |
| VA | `0x0058da00`–`0x0058da34` (53 B) |
| Canonical name | `MapFloatTfid_FreeSubtree_Isnil29_Inferred` |
| Ghidra name | `FUN_0058da00` |
| System | shared tree helper / MapFloatTfid (gather distance map) |
| Date | 2026-07-29 W24-T dual seal |

## Machine-level notes

- Source: live Ghidra decompile + `read_memory` body; **decompiler omits left-walk** (same residual as `StdMap_FreeSubtree_Isnil15` @ `0x004ba770`).
- Bytes seal MSVC map free: recurse right, delete self, walk left until isnil.
- Sole external caller: `MapFloatTfid_EraseRange_Inferred` full-tree clear (`head[1]` = root).
- Family: alloc `0058d9c0`, insert `0058def0`, rebalance `0058da40`, erase-one `0058dc30`, erase-range `0058df60`, destroy `0058e020`.

## ABI (byte-sealed)

```text
stack formal + RET 4; ECX threaded unused
Stack[0x4]   = node*
ECX          = tree/this (preserved into recursive call; never loaded for fields)
void return
```

## Node layout (this family)

| Off | Field |
|---:|---|
| +0x00 | left* |
| +0x04 | parent* (unused here) |
| +0x08 | right* |
| +0x29 | isnil (1 = sentinel/nil) |

Value payload sits between links and isnil (float key + TFID pair family — see gather duals).

## Control flow (annotated; bytes override decompile)

```
node = arg
if node->isnil: return
loop:
  FreeSubtree(node->right)   // recursive, same ECX
  left = node->left
  operator_delete(node)
  node = left
  if node->isnil: break
return
```

## Callers / callees

| Direction | Target | Notes |
|---|---|---|
| Caller | `FUN_0058df60` @ `0x0058df7c` | full clear: free root subtree |
| Caller | self @ `0x0058da17` | right-child recurse |
| Callee | `operator_delete` | node free |
| Twin | `0x004ba770` isnil+0x15 | identical CF shape |

## Pseudocode (byte-correct)

```c
void MapFloatTfid_FreeSubtree_Isnil29_Inferred(void *node)
{
  if (*(char *)((int)node + 0x29) != 0)
    return;
  for (;;) {
    MapFloatTfid_FreeSubtree_Isnil29_Inferred(*(void **)((int)node + 8));
    {
      void *left = *(void **)node;
      operator_delete(node);
      node = left;
    }
    if (*(char *)((int)node + 0x29) != 0)
      break;
  }
}
```

## Open residual

- Product mangled `_Tree` / STL name.
- Whether value payloads need dtor before delete (erase path may pre-destroy; this free assumes node-only or POD values).
- Runtime leak check.
