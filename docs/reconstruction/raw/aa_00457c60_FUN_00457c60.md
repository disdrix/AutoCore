# Raw capture: FUN_00457c60

| Field | Value |
|---|---|
| **Stable ID** | `aa_00457c60` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00457c60` |
| **Canonical name** | `FUN_00457c60` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00457c60(void)

{
  void *pvVar1;
  
  pvVar1 = operator_new(0x18);
  if (pvVar1 != (void *)0x0) {
    *(void **)pvVar1 = pvVar1;
  }
  if ((undefined4 *)((int)pvVar1 + 4) != (undefined4 *)0x0) {
    *(undefined4 *)((int)pvVar1 + 4) = pvVar1;
  }
  return;
}
```

---

## W30-E re-verify (2026-07-29)

**Tooling:** live `batch_decompile` / `decompile_function` + `analyze_function_complete` + `read_memory`. **No** `disassemble_bytes`.

### Body / ABI

| Field | Value |
|-------|-------|
| Body | `0x00457c60`–`0x00457c7a` exclusive (**26 B** / `0x1A`) |
| Pad | `CC` fill from `0x00457c7a` |
| ABI | no formals; **EAX out** = `operator_new(0x18)` result (decompiler `void` is wrong) |
| Epilogue | bare `C3` RET |
| Classification | wrapper / factory |

### Full hex (26 B)

`
6a18e82b1c030083c40485c0740289008d480485c974028901c3
`

### Byte-level CF

`
push 0x18
call operator_new
add  esp, 4
test eax, eax
jz   +2
mov  [eax], eax          ; next = self
lea  ecx, [eax+4]
test ecx, ecx            ; always non-zero when eax==0 ? OOM writes [4]=0 (dead if new throws)
jz   +2
mov  [ecx], eax          ; prev = self
ret
`

### Callers (3)

| Caller | Site | Use |
|--------|------|-----|
| `FUN_00457b30` | `0x00457b4b` | head ? `+8`, size `+0xc=0` |
| `FUN_00496f70` / Class_009c7a1c_Ctor | `0x00496fa0` | list@`+0x0C` head ? `+0x10`, size 0 |
| `FUN_00575440` / Object_009d33c4_Ctor | `0x00575478` | list@`+0x1DC` head ? `+0x1E0`, size 0 |

### Live decompile = raw 2026-07-23 CF

Yes — `operator_new(0x18)` + dual self-link. Return-type display still wrong (`void`).

### Notes

- Sibling of `StdList_AllocEmptySentinel_Inferred` (`0x0040fb90`, node **0x0C**). This unit allocates **0x18** (larger node / payload room); only first **8** bytes initialized.
- Remaining **0x10** bytes of the block are uninitialized by this function (CRT may zero; not sealed here).
