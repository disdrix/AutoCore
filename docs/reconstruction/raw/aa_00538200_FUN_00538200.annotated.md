# Annotated low-level: CNDHash_ReclaimAll_ValueOwning_009cefec (FUN_00538200)

| Field | Value |
|---|---|
| Stable ID | `aa_00538200` |
| VA | `0x00538200` |
| Body | `0x00538200`–`0x00538261` (`ret`) |
| Canonical | `CNDHash_ReclaimAll_ValueOwning_009cefec` (Inferred) |
| System | container / CNDHash |
| Date | 2026-07-29 (dual W18-J) |

## Machine-level notes

- Source: raw `aa_00538200` + live `decompile_function` + `read_memory` (no `disassemble_bytes`).
- Live decompile body **≡** 2026-07-23 raw surface.
- **Decompiler gap:** `operator_delete` annotated “does not return” — **FALSE**. Bytes continue after `add esp,4`.

## ABI

| Slot | Role | Evidence |
|---|---|---|
| ECX / `this` | `CNDHash*` (value-owning / `009cefec` node family) | `mov ebx, ecx` |
| stack | none | bare `c3` |
| return | void | — |

## Control flow (annotated)

```c
// void __thiscall CNDHash_ReclaimAll_ValueOwning_009cefec(CNDHash *this)
// for bucketIndex = 0 .. this->mask(+0x08) inclusive:
//   node = bucketTable[+0x10][bucketIndex]->head(+4)
//   while node:
//     next = node->bucketNext(+0x0c)
//     *node = PTR_FUN_009cefec          // stamp value-owning vtbl
//     if (node->value(+0x08)) operator_delete(value)  // owns payload
//     node->value = 0
//     *node = this->freelist(+0x20)      // push node to freelist (overwrites vtbl)
//     this->freelist = node
//     node = next
//   bucket->head = 0
// Does NOT: zero count(+0x0c), free table(+0x10), free freelist slab
```

## Layout (shared with remove dual `aa_00538000`)

| Hash off | Role |
|---|---|
| `+0x08` | bucket mask (loop upper bound, inclusive) |
| `+0x0c` | count — **untouched here** |
| `+0x10` | bucket table* |
| `+0x20` | freelist head* |

| Node off | Role |
|---|---|
| `+0x00` | vtbl / freelist next |
| `+0x08` | **owned** value* (deleted) |
| `+0x0c` | bucket next |

## Caller chain

| Unit | VA | Role |
|---|---|---|
| **This reclaim** | `0x00538200` | clear buckets → freelist, delete values |
| `CNDHash_FreeBuckets_Owning_009cfa88` | `0x00539110` | count=0; **call this**; free table |
| `CNDHash_Recreate_009cfa88` | `0x00539e30` | FreeBuckets then rebuild |
| Dtor family | `0x0053b9b0` | FreeBuckets + freelist slab free |
| Node scalar dtor | `0x00537550` | vtbl[0] of `009cefec` (not called by this body) |

## Open questions

1. Product FreeBuckets/Reclaim English names.
2. Whether every hash using this reclaim always owns heap values at `+8` (pending-objectives primary evidence).
3. Runtime / bit-exact open.
