# Annotated low-level: FUN_008aa610 → UI_MissionPanel_SyncListScrollbar_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_008aa610` |
| VA | `0x008aa610`–`0x008aa758` (328 B) |
| System | client UI / mission panel |
| Date | 2026-08-04 (WQ7R-B dual) |

## Machine-level notes

- **ESI = this** (panel). `sub/add esp,0x18`. Bare `ret`.
- `+0x700` scrollbar (null → return); `+0x6E0` list; `+0x704` page count.
- Sole caller `FUN_008aaf60` after list rebuild.
- Pages: `(lineStep - viewport + content - 1) / lineStep`, `lineStep` at metrics`+0x7C`, clamp min 1.
- Show path uses **0.5f** (`0x3F000000`) when bar not visible; thumb `g_flOne/pages`.

## Pseudocode (annotated)

```c
// ESI = panel*
void UI_MissionPanel_SyncListScrollbar_Inferred(void)
{
  if (panel[+0x700] == 0) return;
  list = panel[+0x6E0];
  if (!list || !list.vtbl+0x1B8()) {
    bar.vtbl+0x04(/*0*/); return;
  }
  pair = list.vtbl+0x140(&scratch);
  viewport = pair[1];
  content = 1; list.vtbl+0x204(&content);
  if (content <= viewport) {
    bar.vtbl+0x04(0); return;
  }
  lineStep = *(*list.vtbl+0x1B8() + 0x7C);
  pages = (lineStep - viewport + content - 1) / lineStep;
  if (pages < 1) pages = 1;
  panel[+0x704] = pages;
  if (!bar.vtbl+0xD0()) bar.vtbl+0xFC(1, 0.5f);
  bar.vtbl+0x460(1.0f / pages);
  bar.vtbl+0x454(list.vtbl+0x218(0) / (float)pages);
}
```

## Open questions

- Product scrollbar/list vtbl English names.
- Dual of caller `FUN_008aaf60` (sibling residual).
