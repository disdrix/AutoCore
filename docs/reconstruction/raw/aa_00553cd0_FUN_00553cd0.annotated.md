# Annotated low-level: WorldClock_GetSkyBlendAmount_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00553cd0` |
| VA | `0x00553cd0`–`0x00553cd6` (**7 B**) |
| System | environment / world clock / sky blend |
| Date | 2026-08-04 (WQ9F-J dual seal) |
| Ghidra name | `FUN_00553cd0` |
| Proposed name | `WorldClock_GetSkyBlendAmount_Inferred` |

## Machine-level notes

- **ABI:** ECX = clock host; **bare `ret`** (`C3`); result on **x87 ST0**.
- **Body:** `fld dword ptr [ecx+0x15c]` — load stored **f32** at **`+0x15c`**.
- **No callees**, no branches, no stack args.
- **Sibling field map** (same host as `WorldClock_GetQuarterPhase01_Inferred`):
  - `+0x24` phase cache / −1.0 sentinel (sibling unit)
  - `+0x154` quarter mode (sibling unit)
  - **`+0x15c` this unit** — stored blend scalar
  - `+0x160` time base int64 (sibling unit)

## Control-flow map

```
return *(float*)(this + 0x15c);  // x87 ST0
```

## Caller contract

`FUN_004912c0` (sole xref @ `0x004912e5`):

1. If no clock at `*(host+0xb8)+0x78` or host flag `+0x180 == 0` → use `g_flOne`.
2. Else call this getter → `local_4`.
3. Call sibling phase getter; remap by mode into light/audio blend.
4. Pass `local_4` to `FUN_00490af0` which writes material param **`fBlendAmount`** on SkyBox / SkyBoxCloud / SkyBoxForeground.
5. If `local_4 >= 1.0`, also `FUN_00490820` texture path.

## Gaps

1. Product class / field English for `+0x15c`.
2. Writers of `+0x15c`.
3. Runtime / bit-exact open.
