# Raw capture: FUN_0043ce90

| Field | Value |
|---|---|
| **Stable ID** | `aa_0043ce90` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0043ce90` |
| **Canonical name** | `FUN_0043ce90` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_0043ce90(void)



{

  FUN_0043d390();

  return;

}
```

---

## Live re-verify — 2026-07-29 (W27-B OWN)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `decompile_function` + `read_memory` + `analyze_function_complete` (+ callers/xrefs) |
| **Program** | `autoassault.exe` base `0x400000` |
| **Body** | `0x0043ce90`–`0x0043cebe` (**47 B**); pad `CC` after `RET` |
| **Epilogue** | `ADD ESP,0x24` then plain `C3` RET |
| **Classification** | **worker** — sole callee `FUN_0043d390` |
| **Callers** | exclusive `FUN_0043c730` (`GuardedVector_CopyAssign`) @ `0x0043c76e` (fit) and `0x0043c7aa` (grow) — 2 xrefs |
| **Decompile** | ≡ raw 2026-07-23 scaffold high-level CF (`call FUN_0043d390; return`) |

### Live decompile (2026-07-29)

```c
void FUN_0043ce90(void)

{
  FUN_0043d390();
  return;
}
```

### Machine bytes (`read_memory` 47 B)

```
51 8B 04 24 8B 4C 24 1C 8B 54 24 18 50 8B 44 24
18 51 8B 4C 24 18 52 8B 54 24 18 50 8B 44 24 18
51 52 50 56 E8 D7 04 00 00 8B C6 83 C4 24 C3
```

### Byte-level CF (from hex; no `disassemble_bytes`)

1. `PUSH ECX` + reload stack slots → reshape parent 6 stack args + retained regs for `FUN_0043d390`.
2. `PUSH ESI` (out-cursor pair — parent set `ESI = &local_8` before the assign paths).
3. `CALL FUN_0043d390` (rel32 `+0x4d7` → `0x0043d390`).
4. `MOV EAX, ESI` — return out-cursor pointer.
5. `ADD ESP, 0x24` — clean this unit’s internal pushes.
6. `RET` — parent still cleans its own 6 args (`ADD ESP, 0x18` after return).

### Parent call pattern (`FUN_0043c730`)

Both non-empty paths push six dwords then call this unit:

- Fit (`source.size <= dest.size`) @ `0x0043c76e`
- Grow (`source.size > dest.size`) @ `0x0043c7aa`

`FUN_0043d390` (unowned): ring-aware element walk; writes `*out = {container, endIndex}`.
