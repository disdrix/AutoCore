# Annotated low-level: FUN_0044e100 / RbTree_AllocEmptyNode_0x30

| Field | Value |
|---|---|
| Stable ID | `aa_0044e100` |
| VA | `0x0044e100`–`0x0044e136` |
| Canonical | `RbTree_AllocEmptyNode_0x30` |
| System | containers / RB-tree |
| Date | `2026-07-29` (W21-O OWN-ONLY dual) |

## Machine-level notes

- **Alloc size:** `0x30` (`push 0x30`).
- **Links zeroed:** `+0` left, `+4` parent, `+8` right (three dwords).
- **Flag bytes:** `+0x2c = 1` (color black / factory seed — English **Probable** by MSVC tree convention + sibling), `+0x2d = 0` (isnil seed 0; **header callers set `+0x2d = 1`** after return).
- **No self-links, no size, no value ctor** in this unit — same split as `aa_00439770`.
- **ABI:** cdecl, 0 args, **EAX = node\***, bare `ret`.
- Prefer bytes when decompiler says `void`.

## Layout (0x30 node seed)

| Off | Role | Set here |
|---:|---|---|
| `+0x00` | left\* | 0 |
| `+0x04` | parent\* | 0 |
| `+0x08` | right\* | 0 |
| `+0x0c`..`+0x2b` | value / padding region | **untouched** |
| `+0x2c` | color (1=black Probable) | 1 |
| `+0x2d` | isnil (header callers →1) | 0 |

## Pseudocode (annotated)

```c
// Machine: node* __cdecl RbTree_AllocEmptyNode_0x30(void);  // EAX
void *FUN_0044e100(void)
{
  undefined4 *puVar1 = operator_new(0x30);
  if (puVar1 != NULL) {
    *puVar1 = 0;           // left
  }
  if (puVar1 + 1 != NULL) {
    puVar1[1] = 0;         // parent
  }
  if (puVar1 + 2 != NULL) {
    puVar1[2] = 0;         // right
  }
  // Always executed when new returns; OOM leaves garbage writes risk on null
  // (same decompiler-shaped guards as 0x18 sibling).
  *(undefined1 *)(puVar1 + 0xb) = 1;              // +0x2c
  *(undefined1 *)((int)puVar1 + 0x2d) = 0;       // +0x2d
  return puVar1;  // EAX (decomp void is surface only)
}
```

## Caller completion pattern (Profiler_Zone_Ctor `0x00786280`)

```
head = FUN_0044e100();
head[+0x2d] = 1;          // isnil
head.left = head.parent = head.right = head;
map_size = 0;
```

## Open questions

- Product/PDB type name for value_type that forces **0x30** node size.
- Whether any caller uses the node without setting `+0x2d=1`.
- OOM path (null new) — not hardened in retail body.
