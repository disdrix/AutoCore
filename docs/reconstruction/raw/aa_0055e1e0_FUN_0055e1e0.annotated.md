# Annotated low-level: SoftCastHitList_ControllerFillWrapper_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0055e1e0` |
| VA | `0x0055e1e0`–`0x0055e21f` inclusive (**64 B** / `0x40`) |
| Ghidra name | `FUN_0055e1e0` |
| Canonical name | `SoftCastHitList_ControllerFillWrapper_Inferred` |
| System | interaction-activation |
| Date | 2026-08-05 (R11-003 OWN dual; prior scaffold 2026-07-23) |
| Partition parent | `0x006ca890` SoftCastHitList_CoreFillAndRegister_Inferred |

## Machine-level notes

- Prefer assembly when decompiler conflicts: **decompiler drops local-entry construction** and presents a free-looking call of `FUN_006ca890`; retail installs vtbl `0x009D27A8` on a stack entry and sets **ECX = &entry**.
- ABI: **thiscall**, ECX = controller*, stack soft_desc + hit_list_backref, **`RET 0x8`**.
- Frame forced **16-byte aligned** (`AND ESP,~0xF`) with **`SUB ESP,0x50`** before local entry.
- Controller fields: manager @ **+0xC4**, related @ **+0xD0** (may be null; core null-safe).
- Payload to core always **hard 0**.
- Pre-zeros entry **+0x44** before core (core rewrites +0x44 on all arms).
- Sole retail caller: dualed **Client_InteractWorldClickHub** (`0x00925820`) @ `0x00925c32` after hit-list ctor `SoftCastHitList_CtorInitBuffer_Inferred` (`0x0040c410`) and before sort gate `SoftCastHitList_SortIfCountGt1_Inferred` (`0x0048a060`).

## Pseudocode (assembly-corrected)

```c
// ECX = controller*; RET 0x8
void __thiscall SoftCastHitList_ControllerFillWrapper_Inferred(
    void *controller,
    uint32_t *soft_desc,          // [EBP+8]
    uint32_t hit_list_backref)    // [EBP+0xC]
{
  // 16-byte-aligned local frame (0x50 bytes)
  SoftCastHitEntry local_entry;   // at aligned ESP
  void *manager = *(void **)((char *)controller + 0xC4);
  int related   = *(int *)((char *)controller + 0xD0);

  local_entry.vtbl = (void *)0x009D27A8;
  local_entry.related_plus_10 = 0;  // entry+0x44 pre-zero

  SoftCastHitList_CoreFillAndRegister_Inferred(
      &local_entry,      // ECX this for core
      manager,           // stack0
      soft_desc,         // stack1
      related,           // stack2
      0,                 // stack3 payload hard 0
      hit_list_backref); // stack4
}
```

## Call graph (evidence)

```text
Client_InteractWorldClickHub  FUN_00925820  [dualed]
  ├─ SoftCastHitList_CtorInitBuffer_Inferred  0x0040c410  [dualed MEGA-111]
  ├─ SoftCastHitList_ControllerFillWrapper_Inferred  0x0055e1e0  [OWN R11-003]
  │    └─ SoftCastHitList_CoreFillAndRegister_Inferred  0x006ca890  [dualed MEGA-062]
  ├─ SoftCastHitList_SortIfCountGt1_Inferred  0x0048a060  [dualed MEGA-116]
  │    └─ SoftCastHitList_QuickSortRange_KeyFloat14_Inferred  0x00489f20  [dualed]
  └─ walk → FUN_0040afb0  resolve object*  [residual]
```

## Open questions

- Product English for controller class and fields +0xC4 / +0xD0.
- Full vtbl `0x009D27A8` method map (only install site sealed here).
- Runtime Confirmed open (no Launcher; Terminal false).
