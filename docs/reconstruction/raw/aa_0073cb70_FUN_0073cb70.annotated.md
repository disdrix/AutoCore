# Annotated low-level: FUN_0073cb70 → HostEDI_VecCursor44_GetOrCreate_PalDiffMap14c_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0073cb70` |
| VA | `0x0073cb70`–`0x0073cd69` (**506 B**) |
| System | EDI host vector get-or-create (PalDiffMap.fx / 0x14c) |
| Date | 2026-07-29 (W36-G dual seal) |
| Ghidra | `FUN_0073cb70` |
| Named | `HostEDI_VecCursor44_GetOrCreate_PalDiffMap14c_Inferred` |

---

## Machine-level notes

- Source: raw + live W36-G re-verify (full 506 B hex + 2 callers).
- **ABI:** **EDI host**; no stack formals; returns **pointer to 0x14c object** (not 0). Decompiler `void` signature is wrong.
- **Fast path:** if cursor at **+0x44** is inside pointer vector **[+0x4c, +0x50)**, return `vec[cursor++]` without allocation.
- **Slow path:** construct `new(0x14c)+FUN_00764030` host + `new(0xC4)+FUN_00748960` material; load effect **PalDiffMap.fx**; bind **DiffuseMap** param; optional 16×16 surface; fill material color block from globals; vcall material **+0x5c**; **`FUN_007647c0(host14c, material)`** wire; grow vector via **`FUN_004367f0`** on subobject at **EDI+0x48**; push host into vec; increment cursor; destroy stack material local; return host.
- **Callers:** thin wrappers `FUN_0073d020` / `FUN_0073d0a0` (post DiffuseMap bind + host vcall **+0x18**).
- **Family:** W35-E `HostPtrTable12_Bootstrap_EDI` also constructs 0x14c/`FUN_00764030` + 0xC4/`FUN_00748960` pairs — same construction family, different host ownership (12-slot table vs cursor vector).

## Pseudocode (annotated)

```c
// HostEDI_VecCursor44_GetOrCreate_PalDiffMap14c_Inferred
// EDI = host with vector { begin+0x4c, end+0x50, cursor+0x44, grow@+0x48 }
// Returns: void* 0x14c host (cached or new)
void *HostEDI_VecCursor44_GetOrCreate_PalDiffMap14c_Inferred(void /* EDI host */)
{
  uint32_t count = (edi->vec_begin == 0) ? 0
      : (uint32_t)((edi->vec_end - edi->vec_begin) >> 2);
  uint32_t cur = edi->cursor; /* +0x44 */
  if (cur < count) {
    void *hit = edi->vec_begin[cur];
    edi->cursor = cur + 1;
    return hit;
  }
  void *host14c = operator_new(0x14c);
  host14c = host14c ? FUN_00764030(host14c) : 0;
  void *mat = operator_new(0xc4);
  mat = mat ? FUN_00748960(mat) : 0;
  // load "PalDiffMap.fx", resolve "DiffuseMap", optional tex 0x10x0x10 fmt 0x16
  // material local init + FUN_00749d10; mat->vtbl[+0x5c]();
  FUN_007647c0(host14c, mat);
  FUN_004367f0(&edi->vec_header /* +0x48 */, cur + 1, /*fill*/ 0);
  edi->vec_begin[edi->cursor] = host14c;
  edi->cursor++;
  // dtor material local
  return host14c;
}
```

## Call graph

| Direction | Symbol | Role |
|---|---|---|
| Caller | `FUN_0073d020`, `FUN_0073d0a0` | post-get DiffuseMap + vcall +0x18 |
| Callee | `FUN_00764030` | 0x14c placement ctor |
| Callee | `FUN_00748960` | 0xC4 material host ctor |
| Callee | `FUN_007647c0` | wire host14c ← material |
| Callee | `FUN_004367f0` | vector grow |
| Data | `"PalDiffMap.fx"`, `"DiffuseMap"` | product strings |

## Open questions

- Product C++ class name for EDI host / 0x14c / 0xC4 material (structural only).
- Full contracts of unowned material/FX helpers.
- Twin at `0x0073cd70` (different vector offsets +0x30/+0x38/+0x3c) — not owned this wave.
- Runtime / bit-exact / differential.
