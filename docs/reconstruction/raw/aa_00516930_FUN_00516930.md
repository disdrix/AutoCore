
# Raw capture: FUN_00516930

| Field | Value |
|---|---|
| **Stable ID** | `aa_00516930` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00516930` |
| **Canonical name** | `FUN_00516930` |
| **System** | client-fx / object secondary FX ptr-vector teardown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite the 2026-07-23 raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile, 2026-07-23)

```c
void __fastcall FUN_00516930(int param_1)

{
  void *pvVar1;
  undefined4 *puVar2;
  
  puVar2 = *(undefined4 **)(param_1 + 0x138);
  if (puVar2 != *(undefined4 **)(param_1 + 0x13c)) {
    do {
      pvVar1 = (void *)*puVar2;
      FUN_004b7150();
      if (pvVar1 != (void *)0x0) {
        FUN_004b99c0();
                    /* WARNING: Subroutine does not return */
        operator_delete(pvVar1);
      }
      puVar2 = puVar2 + 1;
    } while (puVar2 != *(undefined4 **)(param_1 + 0x13c));
  }
  *(uint *)(param_1 + 0x184) = *(uint *)(param_1 + 0x184) & 0xfffffffb;
  if (*(void **)(param_1 + 0x138) == (void *)0x0) {
    *(undefined4 *)(param_1 + 0x138) = 0;
    *(undefined4 *)(param_1 + 0x13c) = 0;
    *(undefined4 *)(param_1 + 0x140) = 0;
    return;
  }
                    /* WARNING: Subroutine does not return */
  operator_delete(*(void **)(param_1 + 0x138));
}
```

---

## W29-N re-verify (2026-07-29) — append only

**Tools:** `decompile_function` + `analyze_function_complete` + `get_function_by_address` + `read_memory` + callers. **No** `disassemble_bytes`.

| Field | Live seal |
|---|---|
| Body | `0x00516930`–`0x005169a8` (**121 B / `0x79`** inclusive end); pad `CC` |
| Entry | `push esi; mov esi,ecx` — **thiscall** ECX = object host |
| Exit | **`ret 4`** (`C2 04 00`) — one stack formal cleaned; **body never reads it** |
| Live decompile | Same outer CF as 2026-07-23 raw; still drops stack formal + post-delete triad zero |

### Byte spine (sealed)

1. Walk secondary ptr-vector `this+0x138` (begin) .. `this+0x13c` (end), stride 4.
2. Each entry `fx=*it`: `NDSpecialFX_DeactivateClear_Inferred(fx)` (`FUN_004b7150`, ECX=fx); if `fx!=0` → `NDSpecialFX_TeardownCore_Inferred(fx)` (`FUN_004b99c0`) + `operator_delete(fx)`.
3. `*(this+0x184) &= ~4` (clear flag bit 2).
4. If begin non-null → `operator_delete(begin)`.
5. **Always** zero triad `+0x138/+0x13c/+0x140` (decomp false-noreturn collapsed this).

### Callers (live)

| Caller | Site | Stack formal |
|---|---|---|
| `FUN_005179e0` Object_TeardownOwnedFxLists | `0x005179e7` | **push 0** |
| `FUN_00518370` | `0x005183cd` | **push 1** |

Callees: `FUN_004b7150`, `FUN_004b99c0`, `operator_delete`. Classification: **worker**.
