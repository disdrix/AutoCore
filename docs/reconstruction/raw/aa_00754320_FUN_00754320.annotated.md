# Annotated low-level: Palantir_CompleteDtor

| Field | Value |
|---|---|
| Stable ID | `aa_00754320` |
| VA | `0x00754320`–`0x007543A9` exclusive (**137 B**) |
| Ghidra | `FUN_00754320` |
| Preferred | `Palantir_CompleteDtor` |
| System | client / Palantir host complete destructor |
| Date | 2026-07-29 (W34-K dual) |

## Machine-level notes

- **ABI:** `MOV ESI,ECX` — thiscall complete dtor; bare `C3`; **does not** `operator_delete(this)` (scalar wrapper is `FUN_004447e0`).
- **Vtbl swap:** `MOV [ESI], PTR_FUN_00a9f76c`
  - `[0] = 0x004447e0` scalar deleting dtor
  - `[1] = 0x00754480` → logs `Palantir.cpp` / `Palantir::ResetDevice failed...` (**product class sealed**)
- **Member destroy order (EH states 3→4→1→0):**
  1. `~basic_string` at **`this+0xB4`** (IAT `0x009c62f4`)
  2. `StringVec_Tidy(this+0xA0)` then **`DAT_00d1f1fc = 0`**
  3. `FUN_0075d8e0(this+0x30)` — **stack** 1-arg cdecl
  4. `FUN_00964d00(this+0x10)` — **stack** 1-arg cdecl
  5. **`DAT_00d1f058 = 0`** (global host / Palantir singleton)
- **SEH:** handler `LAB_009afd1c`; nested tidy helper `0x00444810` for path-list phase unwind.
- **Decompile gaps:** false `ExceptionList = param_1`; missing `LEA ECX,[ESI+0xA0]` before tidy; stack vs fastcall on nested dtors — **bytes win**.

## Pseudocode (bytes authority)

```c
void Palantir_CompleteDtor(Palantir* self)
{
  self->vtbl = &PTR_FUN_00a9f76c;
  BasicString_Dtor(self + 0xB4);
  StringVec_Tidy(self + 0xA0);
  DAT_00d1f1fc = nullptr;
  FUN_0075d8e0(self + 0x30);
  FUN_00964d00(self + 0x10);
  DAT_00d1f058 = nullptr;
}
```

## Open questions

- Full product layout of subobjects at `+0x10` / `+0x30` (nested duals open).
- Exact MSVC demangle string for Palantir complete dtor plate.
- Runtime / bit-exact.
