# Annotated low-level: PerfStatRings_CommitAndTrim_Inferred (`FUN_009668a0`)

| Field | Value |
|---|---|
| Stable ID | `aa_009668a0` |
| VA | `0x009668a0`–`0x00966c03` exclusive (**867 B** / `0x363`) |
| System | Palantir / frame perf stat rings |
| Date | 2026-07-29 (W30-E refine) |
| Ghidra name | `FUN_009668a0` |
| Canonical | `PerfStatRings_CommitAndTrim_Inferred` |

## Machine-level notes

- **ABI:** one stack formal (stats object*); **`ret 4`** (`C2 04 00`). Not thiscall.
- **Callee `GuardedVector_PushBack` (`0043c830`):** EAX=container, stack=value*; RET 4 (W29-B sealed). Decompiler under-reports as `FUN_0043c830(piVar5)`.
- **Four channel rings** at `+0x5C`, `+0x70`, `+0x84`, `+0x98` (header stride **0x14**).
- **Sum ring** at `+0xAC`.
- **Bins** `+0x34..+0x40` (4 dwords) accumulate current period; **channel totals** `+0x44..+0x50`.
- Short window sum `+0x54` threshold **500**; long window sum `+0xC0` threshold **3000**.
- Sample count `+0x58`; frame/call counter `+0x3D0`.
- Timing flag `+0xC8`; phase `+0xC4` (3 during flush body, 0 after); last tick `+0x30`.
- Snapshot block: copy `0x4C` dwords `+0xCC → +0x1FC`, then zero `+0xCC`.
- Sole caller: `FUN_007545c0` with global `DAT_00d1f614` on Palantir render frame path.

## Object field map (sealed offsets)

| Offset | Role |
|--------|------|
| `+0x30` | last timer sample |
| `+0x34..+0x40` | current period bins[4] (cleared each commit) |
| `+0x44..+0x50` | channel window totals[4] |
| `+0x54` | short-window sum (trim while `> 500`) |
| `+0x58` | sample count (inc on commit; dec on short trim) |
| `+0x5C + i*0x14` | GuardedVector ring i (blocks/cap/begin/size at +4/+8/+C/+10) |
| `+0xAC` | GuardedVector sum ring |
| `+0xC0` | long-window sum (trim while `> 3000`) |
| `+0xC4` | timing phase (3 open / 0 close) |
| `+0xC8` | char: timing disabled if non-zero |
| `+0xCC` | live snapshot source (0x4C dwords) |
| `+0x1FC` | published snapshot dest (0x4C dwords) |
| `+0x3D0` | commit counter |

## Pseudocode (annotated)

```c
// stdcall; stack: PerfStat* self; ret 4
void PerfStatRings_CommitAndTrim_Inferred(PerfStat* self)
{
  self->commit_count_3d0++;

  if (self->timing_disabled_c8 == 0) {
    int now = Timer_Sample_0076c3c0(self);   // ECX=self
    self->bins_34[self->phase_c4] += (now - self->last_tick_30);
    self->last_tick_30 = now;
    self->phase_c4 = 3;
  }

  int sum = 0;
  for (int i = 0; i < 4; i++) {
    GuardedVector* ring = &self->rings_5c[i];          // stride 0x14
    GuardedVector_PushBack(ring, &self->bins_34[i]);   // EAX=ring, stack=&bin
    self->channel_totals_44[i] += self->bins_34[i];
    sum += self->bins_34[i];
  }
  self->short_sum_54 += sum;
  self->long_sum_c0  += sum;
  GuardedVector_PushBack(&self->sum_ring_ac, &sum);
  self->sample_count_58++;

  // Short window: while short_sum > 500, subtract oldest sample from 4 rings
  while (self->short_sum_54 > 500) {
    for each of 4 rings:
      idx = ring.begin + (ring.size - sample_count); // page-wrapped absolute
      v = load_ring(ring, idx);
      channel_totals[i] -= v;
      short_sum_54      -= v;
    sample_count_58--;
  }

  // Long window: while long_sum > 3000, pop_front-ish advance begin on 4+1 rings
  while (self->long_sum_c0 > 3000) {
    for each of 4 rings: if size!=0 advance begin (wrap), size--, reset begin if empty;
    long_sum_c0 -= load_at_begin(sum_ring);
    if sum_ring.size: advance begin, size--, reset if empty;
  }

  // Clear current bins
  bins_34[0..3] = 0;

  // Publish snapshot then clear live block
  memcpy(self+0x1FC, self+0xCC, 0x4C * 4);
  memset(self+0xCC, 0, 0x4C * 4);

  if (self->timing_disabled_c8 == 0) {
    int now = Timer_Sample_0076c3c0(self);
    self->bins_34[self->phase_c4] += (now - self->last_tick_30);
    self->last_tick_30 = now;
    self->phase_c4 = 0;
  }
}
```

## GuardedVector header (per ring base B)

| Off | Field |
|-----|-------|
| `B+0x04` | block map ptr |
| `B+0x08` | capacity (pages) |
| `B+0x0C` | begin index |
| `B+0x10` | size |

Matches W28/W29 GuardedVector duals.

## Open questions

- Product English for 4 channel names / Palantir overlay labels.
- Units of timer sample and of thresholds 500 / 3000.
- Full type of snapshot blob at `+0xCC` / `+0x1FC`.
- Runtime / bit-exact under multi-frame load.
