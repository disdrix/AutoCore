# Annotated low-level: FUN_0099f450 → Gfx_PalDiffMapFx_BindAndInitMesh_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0099f450` |
| VA | `0x0099f450`–`0x0099f5c3` (**372 B** / `0x174`) |
| System | gfx / material effect host init |
| Date | 2026-07-29 (W36-I dual seal) |

## Machine-level notes

- **ABI (bytes):** stdcall **2** stack formals (`host*`, `float scale`); optional **EAX** third (saved → EBX); frame `SUB ESP,0x14` + EBX/EBP/ESI/EDI; **`RET 0x8`**; return OR status in EAX.
- **Bytes win** over Ghidra `undefined FUN_0099f450(void)` plate and phantom-only `in_EAX` without stack formals on the symbol.
- Material slot at **`host+0x20`**. Sole caller `HostPtrTable12_Bootstrap_EDI` slot8 pushes **10.0f** and **XOR EAX,EAX**.
- Float4 constants: `DAT_00afdf40`=(1,1,1,1) Ambient/Diffuse; `DAT_00afdf30`=(0,0,0,1) Specular/Emissive — passed in **EAX** into `FUN_0096fff0`.
- Optional path loads `"DiffuseTexture"` into EAX for `FUN_0043ed30` when entry EAX≠0 (dead at sole sealed xref).
- Tail `FUN_0099f270(scale, 1.0f)` consumes **EDI=host** (callee `unaff_EDI`).

## Pseudocode (annotated)

```c
// stdcall RET 8; EAX optional; return status OR-mask
uint32_t Gfx_PalDiffMapFx_BindAndInitMesh_Inferred(
    void* host /* stack */,
    uint32_t scale_bits /* stack, float bits */,
    /* optional EAX */ void* opt_ctx /* register; sole caller 0 */)
{
  // EBX = opt_ctx (entry EAX)
  // ResourceCache_LookupByName("PalDiffMap.fx") → local fx handle
  // mat = (int**)(host + 0x20)
  // status = BindEffect(mat, fx)                    // FUN_009701d0
  // status |= SetMatParam(mat, "MatAmbient",  white4)
  // status |= SetMatParam(mat, "MatDiffuse",  white4)
  // status |= SetMatParam(mat, "MatSpecular", blackA1)
  // status |= SetMatParam(mat, "MatEmissive", blackA1)
  // if (opt_ctx) status |= DiffuseTexturePath(mat, opt_ctx)  // FUN_0043ed30
  // TryReplaceMaterialWithDerived(mat)              // FUN_0073d940 + refcount swap
  // status |= InitMeshQuads(host, scale, 1.0f)      // FUN_0099f270 (EDI=host)
  // return status
}
```

## Open questions

- Product/PDB English for host class (slot8 dual-vtbl `00aa33fc` / `00aa33ec`).
- Full contracts of unowned callees (`009701d0`, `0096fff0`, `0073d940`, `0099f270`, `0043ed30`).
- Meaning of optional EAX when non-zero (no live sealed caller).
- Runtime / bit-exact.
