# Annotated low-level: FUN_00461ac0 → RcElem28_CopyCtor_EsiEdi

| Field | Value |
|---|---|
| Stable ID | `aa_00461ac0` |
| VA | `0x00461ac0`–`0x00461b11` exclusive (**81 B**) |
| System | MSVC-style copy-ctor for refcounted elem **0x1c** |
| Date | 2026-07-23 scaffold; W35-N seal 2026-07-29 |
| Canonical name | `RcElem28_CopyCtor_EsiEdi` (**Inferred**) |

## Machine-level notes

- **Bytes win** on ABI: **ESI=src**, **EDI=dst**, plain **`C3`**, **EAX=dst**.
- Copies 4 dwords, then shared resource `@+0x10` with **AddRef** gate (refcount++ at obj+4; if becomes 1 → vtbl+4), then 3 bytes @+0x14..+0x16 and dword @+0x18.
- Sole caller: `StdVector_InsertN_RcElem28` (`FUN_004617a0` @ `0x004617c9`) for local value materialization.
- Sibling leaf for range construct: `FUN_004620b0` / `StdVector_UninitializedCopy_RcElem28`.
- Reject scaffold `Named_CalleeOf_*gfxDevice*`.

## Signature (sealed)

```c
// ESI=src*, EDI=dst*; returns dst* in EAX; plain RET
RcElem28* RcElem28_CopyCtor_EsiEdi(void /* ESI, EDI */);
```

## Element (0x1c) — same as parent RcElem28

| Off | Field |
|---|---|
| +0x00..+0x0C | 4× u32 POD |
| +0x10 | shared resource* (refcount at `*(p+4)`) |
| +0x14..+0x16 | 3× u8 |
| +0x18 | u32 |

## Control flow (annotated)

1. Load ESI→EAX; copy dword lanes 0..3 ESI→EDI.
2. `resource = ESI[4]`; if non-null: `++*(resource+4)`; if `==1` call `(*vtbl)[1]` (offset +4).
3. Store resource ptr to EDI[4]; copy bytes +0x14..+0x16; copy dword +0x18.
4. `EAX = EDI`; `RET`.

## Full hex

See raw W35-N append (81 B).
