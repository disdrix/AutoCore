# Annotated low-level: CWndTutorial_CompleteDtor (FUN_0097a8a0)

| Field | Value |
|---|---|
| Stable ID | `aa_0097a8a0` |
| VA | `0x0097a8a0`–`0x0097a8da` inclusive (**59 B** / `0x3B`) |
| System | MSVC derived complete destructor / `CWndTutorial` / `PTR_FUN_00a6f61c` |
| Date | 2026-08-05 (WQ9J-C OWN dual) |
| Canonical name | `CWndTutorial_CompleteDtor` |
| Ghidra name | `FUN_0097a8a0` |

## Machine-level notes

- Source: live `batch_decompile` + `disassemble_function` + full-body `read_memory` + `analyze_function_complete` + RTTI COL walk.
- Classic MSVC **complete** dtor (not scalar): reinstall most-derived vtbl, chain base teardown, leave host free to scalar.
- ABI: **ECX=this**; SEH frame; **`ADD ESP,0x10; RET`**; void; no stack args.
- Sole CALL site: scalar `FUN_004160b0` (vtbl[0]) dualed WQ9I-E.
- Base (not OWN): `FUN_00792c20` — large CNDUIWnd-family teardown (owned child ptrs at high offsets, list cleanup, etc.).
- RTTI product name **CWndTutorial** sealed; peer ctor asset `"i_w_tut_2d_wnd_highlight.dds"` is corroborating only.
- Structural twin of `CMenuChatlog_CompleteDtor` (`0x008327f0`) but different vtbl/class and **direct** base call (chatlog uses intermediate `0082d540`).

## Pseudocode (annotated)

```c
// ECX = this; SEH; ADD ESP,0x10; RET; complete dtor — does not free this
void __fastcall CWndTutorial_CompleteDtor(void* self)
{
  // SEH: push -1; push LAB_009b4587; link FS:[0]
  *reinterpret_cast<void**>(self) = &PTR_FUN_00a6f61c;  // CWndTutorial vtbl
  // EH state = 0xFFFFFFFF
  FUN_00792c20(self);  // deep UI base complete
  // restore ExceptionList; ADD ESP,0x10; RET
}
```

## Control flow

```text
SEH prologue (LAB_009b4587)
*this = PTR_FUN_00a6f61c
EH = -1
CALL FUN_00792c20(this)     // base teardown
SEH epilogue; ADD ESP,0x10; RET
```

## Related (not owned)

| Unit | VA | Note |
|---|---|---|
| Scalar | `0x004160b0` | WQ9I-E dual (vtbl[0]); prior name `Class_00a6f61c_ScalarDeletingDtor` |
| Deep base | `0x00792c20` | CNDUIWnd-family teardown residual |
| Peer ctor | `0x0097a8e0` | same vtbl; tutorial highlight dds |
| Twin complete | `0x008327f0` | `CMenuChatlog_CompleteDtor` (same wave OWN) |

## Open questions

- Full dual of deep base `00792c20` (not OWN).
- Vtbl slots [1+] English.
- Runtime / bit-exact / differential — open.
