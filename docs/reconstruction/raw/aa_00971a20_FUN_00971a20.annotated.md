# Annotated low-level: FUN_00971a20 / AssPreloader_IsKeyTreeReady_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00971a20` |
| VA | `0x00971a20`–`0x00971b74` (**341 B**) |
| System | AssPreloader / asset preload readiness |
| Date | 2026-07-23 scaffold; W36-E dual seal 2026-07-29 |

## Machine-level notes

- **Bytes win:** thiscall ECX=AssPreloader; 2 stack formals; **`RET 8`**; AL 0/1. Ghidra bare `undefined FUN(void)` is wrong.
- Entry always calls **`FUN_00971480`** (drain pending / log already-loaded; plate `assPreloader.cpp`) — not a pure predicate; side-effectful.
- **`FUN_0043d5e0`:** register ABI `ECX=map(this+0x10)`, `EBX=key*`, `EAX=out_it*`; lower_bound walk + exact (`node[3] <= *key`); miss writes end `*(map+4)` = `*(this+0x14)`.
- Decompiler shows `FUN_0043d5e0()` with no args / `param_2 == *(this+0x14)` — **bytes win**: find overwrites key stack slot with node*.
- Recurse path inlines same present-check as `NDResourceCache_IsKeyPresent` (`0044e8c0` + end + `node+0x0C`) against `cache@this+0x110`.
- GuardedVector page map for dep walk matches family (`idx>>2`, wrap capacity) used by ProcessKeyRingStep / FillPreloadList.
- Dep recurse forces **recurse=0** (shallow membership only per dep).
- Gate `*(cache+0x64) != 0` must be true to enter dep walk when recurse!=0.

## Pseudocode (annotated)

```c
// ECX = AssPreloader*; stack (key*, char recurse); RET 8; AL 0/1
uint8_t __thiscall AssPreloader_IsKeyTreeReady_Inferred(
    AssPreloader* self, const void* key, char recurse)
{
  AssPreloader_DrainPending_Inferred(self);   // FUN_00971480

  void* node = (void*)key;                    // slot reused as out_it
  MapLowerBoundExact_Inferred(self+0x10, &node, key); // FUN_0043d5e0
  if (node == *(void**)((char*)self + 0x14))
    return 0;                                 // not in tree

  NDResourceCache* cache = *(NDResourceCache**)((char*)self + 0x110);
  if (recurse != 0 && cache != nullptr && *(uint32_t*)((char*)cache + 0x64) != 0) {
    GuardedVector deps = {};
    FillDeps_Inferred(key, &deps);            // FUN_00744360
    for (each dep slot in deps) {
      if (!AssPreloader_IsKeyTreeReady_Inferred(self, dep, 0)
          && !NDResourceCache_IsKeyPresent_Inferred(cache, dep)) {
        GuardedVector_Dtor(&deps);            // FUN_005b2ba0
        return 0;
      }
    }
    GuardedVector_Dtor(&deps);
  }
  return 1;
}
```

## Open / residual

- Product method English (IsReady / IsPreloaded / CanUse).
- Full contracts of unowned `00971480` / `0043d5e0` / `00744360`.
- Exact meaning of `cache+0x64` gate (ctor zeros it; other writers open).
- Runtime / bit-exact / concurrent tree mutation.
