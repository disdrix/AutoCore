# Annotated low-level: FUN_004b7330 / NDSpecialFX_LivenessDone_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004b7330` |
| VA | `0x004b7330`–`0x004b73bc` (140 B) |
| System | client-fx / NDSpecialFX lifecycle |
| Date | 2026-07-29 (W26-H dual seal) |
| Canonical (inferred) | `NDSpecialFX_LivenessDone_Inferred` |
| Ghidra | `FUN_004b7330` |

## Machine-level notes

- Source: raw capture + live re-verify 2026-07-29.
- **Leaf** — no callees; no DAT_* loads.
- **ABI:** `__fastcall` / thiscall-style **ECX = fx host**; plain `RET`; **AL** bool.
- Sole consumer: dualed `NDSpecialFX_HostTick_Inferred` (`FUN_004b8ed0`) — non-zero → DeactivateClear path.
- List layout at `+0x204` shared with HostTick owned-child walk (payload at `node[2]`).
- Prefer this annotation for role; bytes + raw body remain authoritative for CF.

## Role

**Liveness / completion probe** for an NDSpecialFX host:

| Return | Meaning to HostTick |
|---|---|
| **0** | Still live — keep ticking children |
| **1** | Done — HostTick calls DeactivateClear and returns 1 |

## Field map (host `this`)

| Offset | Use |
|---|---|
| `+6`, `+7`, `+8` | Gate bytes for secondary list probe (all must be 0) |
| `+9`, `+0xA` | Active residual flags (any non-zero → not done) |
| `+0x1E4` | Int residual counter/ptr (non-zero → not done) |
| `+0x1EC` | Circular list head (secondary / related) |
| `+0x1F0` | Count or enable for secondary list probe |
| `+0x204` | Circular list head (owned children; shared with HostTick) |
| `+0x214` | Int residual (non-zero → not done) |
| child `+0x795` | Done byte on owned payload (0 → still live) |
| related `+0x10B` | Active byte on secondary payload (non-zero → not done) |

## Pseudocode (annotated)

```c
// ECX = fx host; return AL 0|1; plain RET
uint8_t __fastcall NDSpecialFX_LivenessDone_Inferred(int this_fx)
{
  uint32_t *node;
  int *listHead;
  uint8_t done;
  int *it;

  // Residual host gates must all be clear
  if ((*(char *)(this_fx + 10) != '\0') ||
      (*(char *)(this_fx + 9) != '\0') ||
      (*(int *)(this_fx + 0x1e4) != 0) ||
      (*(int *)(this_fx + 0x214) != 0)) {
    return 0;
  }

  // Owned list (+0x204): every child payload must have +0x795 != 0
  for (node = (uint32_t *)**(uint32_t **)(this_fx + 0x204);
       node != *(uint32_t **)(this_fx + 0x204);
       node = (uint32_t *)*node) {
    if (*(char *)(node[2] + 0x795) == '\0') {
      return 0; // child still live
    }
  }

  done = 1;

  // Optional secondary list (+0x1ec) when +0x1f0 set and +6/+7/+8 clear
  if ((*(int *)(this_fx + 0x1f0) != 0) &&
      (*(char *)(this_fx + 6) == '\0') &&
      (*(char *)(this_fx + 7) == '\0') &&
      (*(char *)(this_fx + 8) == '\0')) {
    listHead = *(int **)(this_fx + 0x1ec);
    it = (int *)*listHead;
    if (it != listHead) {
      while (*(char *)(it[2] + 0x10b) == '\0') {
        it = (int *)*it;
        if (it == listHead) {
          return done; // all secondary inactive
        }
      }
      done = 0; // found active secondary node
    }
  }
  return done;
}
```

## Open questions

- Product / PDB method name (no string xref).
- English meaning of host flags `+6..+A` and payload `+0x795` / `+0x10B` beyond CF polarity.
- Runtime golden vs HostTick deactivate timing.
