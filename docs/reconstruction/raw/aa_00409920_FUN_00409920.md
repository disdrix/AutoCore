# Raw capture: FUN_00409920

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409920` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409920` |
| **Canonical name** | `FUN_00409920` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00409920(void *param_1)

{
  if (*(char *)((int)param_1 + 0x131) == '\0') {
    FUN_00409920(*(undefined4 *)((int)param_1 + 8));
    FUN_00409b80(param_1);
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return;
}
```

---

## WQ9J-I re-verify (2026-08-05)

| Field | Value |
|---|---|
| **Agent** | WQ9J-I OWN-ONLY |
| **Tools** | `batch_decompile` / `decompile_function` + `analyze_function_complete` + `read_memory` + `disassemble_function` + callers/xrefs + `get_assembly_context` |
| **No** | `disassemble_bytes`; Launcher; parent ledgers |
| **Live decompile** | ≡ scaffold (collapsed right-only + false noreturn) — **bytes win** |
| **Named** | `StdTree_FreeSubtree_Isnil131_DestroyStr0C_Inferred` |

### Body / ABI (bytes authority)

- **Range:** `0x00409920`–`0x00409960` inclusive last RET byte (**65 B** / `0x41`); pad `CC` to `0x00409970`.
- **ABI:** ECX = tree_base (threaded; unused in body except recursive pass); stack `node*`; **void**; **`RET 4`** (`C2 04 00`).
- **Full body hex:**
```
53 56 57 8B 7C 24 10 80 BF 31 01 00 00 00 8B D9 8B F7 75 27
8B 46 08 50 8B CB E8 E1 FF FF FF 8B 36 57 E8 39 02 00 00
57 E8 D5 FE 07 00 83 C4 04 80 BE 31 01 00 00 00 8B FE 74 D9
5F 5E 5B C2 04 00
```

### Semantics (bytes — decompiler incomplete)

```
if node.isnil@+0x131: ret 4
loop:
  FreeSubtree(tree_base, node.right@+8)   // self, ECX=tree_base
  left = node.left@+0
  FUN_00409b80(node)                      // stdcall RET4; ~basic_string @ node+0x0c
  operator_delete(node)                   // cdecl; ADD ESP,4
  node = left
  if !node.isnil@+0x131: continue
ret 4
```

- **Value destroy:** yes — shared `FUN_00409b80` destroys `std::basic_string` at **+0x0c** only.
- Decompiler drops left-walk and marks delete noreturn — **false**.
- `disassemble_function` also omits loop-tail (`ADD ESP` / isnil recheck / `JZ`); hex complete.

### Callers / xrefs (2)

| Site | Parent | Role |
|---|---|---|
| `0x0040993a` | self | recursive right free |
| `0x00407b8f` | `FUN_00407b70` | full-range clear: `MOV ECX,ESI`; push `head->parent`; free; head L/P/R self-link; size=0 |

### Callees

- self, `FUN_00409b80` (string value dtor @+0x0c, stdcall RET4), `operator_delete` (`0x00489822`).
