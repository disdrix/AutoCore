# Annotated low-level: StdMap_StringKey_InsertOrFind_Via401db0_Inferred (FUN_00401c50)

| Field | Value |
|---|---|
| Stable ID | `aa_00401c50` |
| VA | `0x00401c50`–`0x00401d26` inclusive (**215 B** / `0xD7`) |
| Canonical name | `StdMap_StringKey_InsertOrFind_Via401db0_Inferred` |
| Ghidra symbol | `FUN_00401c50` |
| System | stl-map / missions-progression host (string-key map nested under Mission null-dialog path) |
| Date | 2026-08-05 (MEGA-064 OWN-ONLY dual refresh) |
| Prior scaffold | 2026-07-23 raw/annotated; Named_CalleeOf plate **retired** |

## Machine-level notes

- Live re-verify: decompile ≡ raw CF; ABI sealed by `disassemble_function` + call-site context + `read_memory`.
- Prefer assembly when decompiler conflicts (register formals, `operator<<` vs true `operator<`, void return).
- isnil@**+0x2d** family (node size 0x30 family via isnil2D peers).
- Monomorph twin: dualed `StdMap_StringKey_InsertOrFind` @ `0x004309c0` (insert via `0x00430b60`).

## ABI (sealed)

```text
stack[0]  = MapShell*          // head@+4, size@+8
EAX       = const basic_string* key
EBX       = InsertPair* out    // { node* it @+0, uint8_t inserted @+4 }
return    = EBX in EAX
exit      = RET 4  (×3)
```

## Node layout (this unit)

| Off | Field |
|----:|-------|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x0C | `basic_string` key |
| +0x2C | color (insert-worker family) |
| +0x2D | isnil |

## Pseudocode (ABI-corrected)

```c
// Ghidra FUN_00401c50 — MEGA-064 annotated
InsertPair* StdMap_StringKey_InsertOrFind_Via401db0_Inferred(
    MapShell* map /*stack*/,
    const basic_string* key /*EAX*/,
    InsertPair* out /*EBX*/)
{
  Node* header = map->head;                 // [map+4]
  Node* cur = header->parent;               // root
  Node* parent = header;
  bool goLeft = true;

  while (cur->isnil == 0) {                 // isnil @ +0x2d
    // IAT [0x009c62e8] std::operator<(key, cur->key)
    goLeft = basic_string_lt(key, &cur->key);
    parent = cur;
    cur = goLeft ? cur->left : cur->right;
  }

  if (goLeft) {
    if (parent == header->left) {
      // insert at leftmost: FUN_00401db0(where=parent, addLeft=1, key)
      // ECX=where, EDI=map, stack outIt*/addLeft/key
      Node** r = InsertHint_Via00401db0(parent, 1, key);
      out->it = *r;
      out->inserted = 1;
      return out;
    }
    // dualed StdTree_IteratorDecrement — EDX = &parent
    StdTree_IteratorDecrement(&parent);
  }

  // if parent->key < key → insert; else equal (found)
  if (basic_string_lt(&parent->key, key)) {
    Node** r = InsertHint_Via00401db0(parent, goLeft ? 1 : 0, key);
    out->it = *r;
    out->inserted = 1;
    return out;
  }

  out->it = parent;
  out->inserted = 0;   // equal key: no mapped-value rewrite
  return out;
}
```

## Call graph

| Role | Target |
|---|---|
| Caller | `FUN_00980160` @ `0x009801dd` (parses `)` substr; builds string pair; map @ host+0x0C) |
| String `<` | IAT `0x009c62e8` → `0x006eadd0` |
| Insert+RB | `FUN_00401db0` (**undualed**) |
| Predecessor | `StdTree_IteratorDecrement` `0x004313d0` (**dualed** W22-O; EDX ABI; isnil2D) |
| Twin (do not merge) | `StdMap_StringKey_InsertOrFind` `0x004309c0` (insert `0x00430b60`) |

## Open questions

- Product English for map value type / host object at Mission dialog path.
- Dual of insert worker `FUN_00401db0` / buynode `FUN_00401f40`.
- Runtime / bit-exact / differential.
