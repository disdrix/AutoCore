# Raw capture: FUN_006c6c50

| Field | Value |
|---|---|
| **Stable ID** | `aa_006c6c50` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x006c6c50` |
| **Canonical name** | `FUN_006c6c50` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_006c6c50(int param_1,undefined1 *param_2,uint param_3,uint param_4)

{
  if ((((param_3 ^ param_4) & 0xffff0000) == 0) && ((param_3 & 0xffff0000) != 0)) {
    *param_2 = 0;
    return;
  }
  *param_2 = (*(uint *)(param_1 + 0x1c + (param_3 & 0x1f) * 4) & 1 << ((byte)param_4 & 0x1f)) != 0;
  return;
}
```

---

## MEGA-039 live re-verify (APPEND-ONLY) — 2026-08-05

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-039** |
| **VA** | `0x006c6c50`–`0x006c6c98` inclusive (**73 B** / `0x49`); pad `CC` after |
| **Tools** | Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` / `get_function_xrefs` + `get_assembly_context` + `analyze_control_flow`. **No** `disassemble_bytes`. No Launcher. |
| **Image** | `autoassault.exe` base `0x400000` |
| **Integrity** | Prior raw body (2026-07-23) **preserved**; this section appends only |

### Live decompile (2026-08-05) ≡ raw CF

```c
void __thiscall FUN_006c6c50(int param_1,undefined1 *param_2,uint param_3,uint param_4)

{
  if ((((param_3 ^ param_4) & 0xffff0000) == 0) && ((param_3 & 0xffff0000) != 0)) {
    *param_2 = 0;
    return;
  }
  *param_2 = (*(uint *)(param_1 + 0x1c + (param_3 & 0x1f) * 4) & 1 << ((byte)param_4 & 0x1f)) != 0;
  return;
}
```

### Body bytes (`read_memory` 80 B @ `0x006c6c50`)

```text
8B 44 24 08 56 8B D1 8B 4C 24 10 8B F0 33 F1 F7 C6 00 00 FF FF 75 12
A9 00 00 FF FF 74 0B 8B 44 24 08 C6 00 00 5E C2 0C 00
83 E1 1F BE 01 00 00 00 D3 E6 83 E0 1F 8B 4C 82 1C 8B 44 24 08 85 F1
0F 95 C1 88 08 5E C2 0C 00 CC CC CC CC CC CC CC
```

Body **73 B** through final `RET 0xC`; trailing `CC` pad not part of function.

### Disassembly (sealed)

```text
006c6c50  MOV  EAX, [ESP+0x8]          ; a  = stack arg2 (object type/category)
006c6c54  PUSH ESI
006c6c55  MOV  EDX, ECX                ; this → EDX (filter bit-matrix owner)
006c6c57  MOV  ECX, [ESP+0x10]         ; b  = stack arg3 (query filter flags)
006c6c5b  MOV  ESI, EAX
006c6c5d  XOR  ESI, ECX                ; a ^ b
006c6c5f  TEST ESI, 0xFFFF0000
006c6c65  JNZ  006c6c79                ; high words differ → bit test
006c6c67  TEST EAX, 0xFFFF0000
006c6c6c  JZ   006c6c79                ; high(a)==0 → bit test
006c6c6e  MOV  EAX, [ESP+0x8]          ; out*
006c6c72  MOV  BYTE PTR [EAX], 0       ; *out = false
006c6c75  POP  ESI
006c6c76  RET  0xC                     ; cleanup 12 B (3 stack args)

006c6c79  AND  ECX, 0x1F               ; bit = b & 31
006c6c7c  MOV  ESI, 1
006c6c81  SHL  ESI, CL                 ; mask = 1 << bit
006c6c83  AND  EAX, 0x1F               ; row = a & 31
006c6c86  MOV  ECX, [EDX+EAX*4+0x1C]   ; dword = this->bits[row] @ +0x1c
006c6c8a  MOV  EAX, [ESP+0x8]          ; out*
006c6c8e  TEST ECX, ESI
006c6c90  SETNZ CL
006c6c93  MOV  [EAX], CL               ; *out = (bits & mask) != 0
006c6c95  POP  ESI
006c6c96  RET  0xC
```

### ABI (sealed)

| Item | Value | Evidence |
|---|---|---|
| Convention | **`__thiscall`** | ECX = this @ entry → EDX |
| Stack args | `uint8_t *out`, `uint a`, `uint b` | `[ESP+4/8/C]` after entry; after PUSH ESI offsets +4 |
| Cleanup | **`RET 0xC`** (`C2 0C 00`) ×2 exits | both epilogues |
| Result | out-param byte; EAX left holding `out*` | both paths `MOV EAX,[ESP+8]` then store |
| Callees | **none** (leaf) | analyze / CF |

### Control flow (sealed)

```text
AreaQuery_FilterBitTest(this, out, a, b):
  if  ((a ^ b) & 0xFFFF0000) == 0  AND  (a & 0xFFFF0000) != 0:
    *out = 0; return          // same non-zero high-16 → reject
  row  = a & 0x1F
  bit  = b & 0x1F
  *out = ( *(uint32*)(this + 0x1C + row*4) & (1u << bit) ) != 0
```

CF metrics (`analyze_control_flow`): 4 BB, cyclomatic 2, 0 loops, 0 calls, 2 returns.

### Callers / xrefs (sealed)

| Role | Value |
|---|---|
| Callers | **1** — `FUN_004ea350` (`CVOGPhysicsUtils_GetObjectsInArea`, dualed) |
| Xrefs | **1** UNCONDITIONAL_CALL @ `0x004ea560` |
| Call site | default AABB hit loop; filter gate before object resolve |

Call-site assembly context (`get_assembly_context` @ `0x004ea560`):

```text
004ea551  MOV  ECX, [EBP+0x1c]     ; parent filterFlags (param_6)
004ea554  MOV  EAX, [ESI+0x1c]     ; hit object field +0x1c (type/category)
004ea557  PUSH ECX                 ; b = filterFlags
004ea558  PUSH EAX                 ; a = object type
004ea559  LEA  EDX, [ESP+0x47]     ; out byte on stack
004ea55d  PUSH EDX                 ; out*
004ea55e  MOV  ECX, EBX            ; this = filter bit-matrix owner
004ea560  CALL FUN_006c6c50
004ea565  CMP  BYTE PTR [EAX], 0   ; fail → skip hit (LAB_004ea612)
004ea568  JZ   004ea612
```

Parent dual (`A_aa_004ea350_CVOGPhysicsUtils_GetObjectsInArea`): last arg is query filter; gather flags sample `1` / `0x11` / `0x0a`.

### Classification

| Item | Value |
|---|---|
| Role | **Area-query filter bit test** (interaction-activation / physics broadphase gate) |
| System | interaction-activation (partition); parent CVOGPhysicsUtils GetObjectsInArea |
| Prior scaffold | `Named_CalleeOf_Named_VOG_DEBUG_STOP_006c6c50` — **rejected** (parent debug string is fail path, not this leaf) |
| Promoted name | `CVOGPhysicsUtils_AreaQueryFilterBitTest_Inferred` (`_Inferred`: product English for this-object / high-word gate open) |

### Gaps (not sealed here)

- Product English for filter bit-matrix object (EBX / this) and field names at `this+0x1c`.
- Semantic meaning of high-16 early-out (same-class reject vs packing convention).
- Per-bit product meanings for filter flags `1` / `0x11` / `0x0a`.
- Runtime / bit-exact / differential (no Launcher).

### Dual seal

| Path | Review | Verdict |
|---|---|---|
| A fidelity | `reviews/A_aa_006c6c50_CVOGPhysicsUtils_AreaQueryFilterBitTest_Inferred.md` | **accept-with-gaps** |
| B adversarial | `reviews/B_aa_006c6c50_CVOGPhysicsUtils_AreaQueryFilterBitTest_Inferred.md` | **accept-with-gaps** |

**Terminal:** false.
