# Raw capture: FUN_0051e180

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051e180` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051e180` |
| **Canonical name** | `FUN_0051e180` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0051e180(int param_1)

{
  int local_4;
  
  local_4 = param_1;
  FUN_0051d880(&local_4,**(undefined4 **)(param_1 + 4),*(undefined4 **)(param_1 + 4));
                    /* WARNING: Subroutine does not return */
  operator_delete(*(void **)(param_1 + 4));
}
```

---

## WQ9F-H re-verify (2026-08-04) — append only

| Field | Value |
|---|---|
| **Wave** | WQ9F-H OWN dual A/B |
| **Partition** | `WAVE_2026-08-04_wq009_depth3_partition_map.md` |
| **Tools** | `batch_decompile` + `analyze_function_complete` + `disassemble_function` + `read_memory` + xrefs + `get_assembly_context`. **No** `disassemble_bytes`. |
| **Promoted name** | `Map_Tidy_FreeHead_B` |
| **Body** | `0x0051e180`–`0x0051e1ad` inclusive (**46 B** / `0x2E`); pad `CC CC` then next |

### Full-body hex (`read_memory`)

```
51568bf18b46048b0850518d44240c508bcee8e9f6ffff8b4e0451e882b6f6ff83c40433c08946048946085e59c3
```

### Control flow (bytes authority — decompiler truncates after delete)

```text
PUSH ECX                    ; out-iter slot
PUSH ESI
MOV  ESI, ECX               ; map*
MOV  EAX, [ESI+4]           ; head
MOV  ECX, [EAX]             ; *head = begin
PUSH EAX                    ; last = head
PUSH ECX                    ; first = *head
LEA  EAX, [ESP+0xC]
PUSH EAX                    ; &out
MOV  ECX, ESI
CALL Map_EraseRange_B       ; 0x0051d880  full-range erase
MOV  ECX, [ESI+4]
PUSH ECX
CALL operator_delete        ; free sentinel head
ADD  ESP, 4
XOR  EAX, EAX
MOV  [ESI+4], EAX           ; head = 0
MOV  [ESI+8], EAX           ; size = 0
POP  ESI
POP  ECX
RET                         ; bare C3
```

### Sealed facts

- **46 B** twin of dualed `Map_Tidy_FreeHead` (`0x0051da30`) but erase callee is dualed **`Map_EraseRange_B`** (`0x0051d880`), not `Map_EraseRange`.
- Does **not** free the map facade; only free head + null head/size.
- Sole external code path: atexit thunk `FUN_009c3970` = `MOV ECX,0x00b0427c` / `JMP 0x0051e180` (static map @ `0x00b0427c`).
- Decompiler false-noreturn: post-delete nulls sealed by bytes only.
