# Annotated low-level: FUN_005204f0 → Character_SumCommodityTierMaxCapacities_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005204f0` |
| VA | `0x005204f0`–`0x00520517` inclusive (**40 B** / `0x28`) |
| System | `inventory-transfer` |
| Date | 2026-08-05 (R13-033 dual re-annotate) |
| Canonical | `Character_SumCommodityTierMaxCapacities_Inferred` |
| Ghidra | `FUN_005204f0` |
| Partition parent | `0x00520340` Character_CalcCommodityTierMaxCapacity_Inferred |

## Machine-level notes

- **ABI:** `__thiscall` — **ECX = Character\***; **no stack args** on entry; exit **`RET`** (`C3`). Callee `FUN_00520340` is `ret 4` (cleans the pushed tier each iteration).
- **Decompiler gap:** surface shows `int FUN_005204f0(void)` with no this. Bytes: `MOV EBX,ECX` then `MOV ECX,EBX` before every `CALL` — thiscall is sealed.
- **Body:** sum of per-tier max capacities:
  - `sum = 0; for (tier = 0; tier < 6; ++tier) sum += CalcMax(this, tier); return sum;`
  - Parent returns **0** for tier ∉ {1..5}, so **tier 0 is a dead zero contribution**.
  - Effective total = Σ max for tiers **1..5** (same effective total as sibling `FUN_005226e0`, different loop shape).
- **Registers:** EBX=this; ESI=tier; EDI=sum; EAX=per-iter max then final return.
- **Align nop:** `LEA ESP,[ESP]` (`8D A4 24 00 00 00 00`) between prolog and loop head.
- **No mutation:** pure capacity aggregate; no grid place/stamp/store.
- **Callers:** **none** recovered (static xrefs / LE data pointer search empty). Orphan unit; known only as a caller of the max-capacity leaf.
- **Role:** total commodity **Complexity** budget across all product tiers for a character (level-scaled via parent).

## Pseudocode (annotated)

```c
// __thiscall Character* this; ret 0 (no stack args)
// Returns Σ Character_CalcCommodityTierMaxCapacity_Inferred(this, tier)
// for tier in [0, 6). Tier 0 → 0; effective Σ over tiers 1..5.
int Character_SumCommodityTierMaxCapacities_Inferred(Character* ch)
{
  int sum = 0;
  int tier = 0;
  do {
    sum += Character_CalcCommodityTierMaxCapacity_Inferred(ch, tier);
    tier = tier + 1;
  } while (tier < 6);
  return sum;
}
```

## Callees (owned parent only as evidence)

| Callee | Site | Role |
|---|---|---|
| `FUN_00520340` Character_CalcCommodityTierMaxCapacity_Inferred | `0x00520503` | per-tier max capacity (dualed R12-018) |

## Callers

| Caller | Site | Role |
|---|---|---|
| *(none recovered)* | — | No static CALL/data xref; possible dead code or dynamic dispatch not recovered |

## Sibling (not owned)

| Unit | Difference |
|---|---|
| `FUN_005226e0` (R13-034) | Loop tiers **1..5** only (`LEA ESI,[EAX+1]`, bound `< 5`); same effective Σ max; **do not merge** plates |

## Open / gaps

- Product/PDB method English.
- Why two sum wrappers (0..5 vs 1..5) both exist; which product path uses which (this unit has **no** recovered callers).
- Runtime / bit-exact / differential.
- Whether orphan status is true dead code vs missed indirect call.
