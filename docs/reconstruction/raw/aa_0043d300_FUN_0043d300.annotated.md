# Annotated low-level: FUN_0043d300 / GuardedVector_RingAssignBackward

| Field | Value |
|---|---|
| Stable ID | `aa_0043d300` |
| VA | `0x0043d300`–`0x0043d38d` (**142 B**) |
| Canonical | `GuardedVector_RingAssignBackward` (**Inferred**) |
| Ghidra | `FUN_0043d300` |
| System | STL / guarded ring-vector container |
| Date | 2026-07-29 (OWN-ONLY W28-C seal) |

## Machine-level notes

- Leaf worker: no callees; pure reverse ring dword assign.
- Sibling forward walker is unowned `FUN_0043d390` (param_3++/param_7++).
- Primary consumer: `GuardedVector_EraseRange` shorter-prefix arm (`prefix < suffix`).
- Also called from grow/assign packing paths `FUN_0043cb00`, `FUN_0043cf40`.
- Element addressing: block=`idx>>2`, wrap if `block >= capacity`, lane=`idx+(block*-4)` (= `idx%4`).
- ABI: **7** stack args, plain `RET` (caller cleans). Out pair written through `param_1`.

## Pseudocode (annotated)

```c
// out_pair, stop_container, stop_index, src_container, src_index,
// dst_container, dst_index  — all stack; plain RET
int* GuardedVector_RingAssignBackward(
    int* out_pair,
    int stop_container, uint stop_index,
    int src_container, uint src_index,
    int dst_container, uint dst_index)
{
  while (stop_container != src_container || stop_index != src_index) {
    src_index--;
    // map src_index → (block, lane) with capacity wrap on src_container
    dst_index--;
    // map dst_index → (block, lane) with capacity wrap on dst_container
    dst_elem = src_elem;  // dword assign
  }
  out_pair[0] = dst_container;
  out_pair[1] = dst_index;
  return out_pair;
}
```

## Open residual

- Product/PDB class English.
- Exact call-site stack packing shapes inside unowned parents (`0043c8c0` sealed separately).
- Runtime / bit-exact.
