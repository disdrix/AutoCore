# Raw capture: FUN_00568b70

| Field | Value |
|---|---|
| **Stable ID** | `aa_00568b70` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00568b70` |
| **Canonical name** | `FUN_00568b70` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00568b70(int param_1,int param_2)



{

  int *piVar1;

  int *piVar2;

  

  piVar1 = *(int **)(param_2 + 8);

  *(int *)(param_2 + 8) = *piVar1;

  if (*(char *)(*piVar1 + 0x49) == '\0') {

    *(int *)(*piVar1 + 4) = param_2;

  }

  piVar1[1] = *(int *)(param_2 + 4);

  if (param_2 == *(int *)(*(int *)(param_1 + 4) + 4)) {

    *(int **)(*(int *)(param_1 + 4) + 4) = piVar1;

    *piVar1 = param_2;

    *(int **)(param_2 + 4) = piVar1;

    return;

  }

  piVar2 = *(int **)(param_2 + 4);

  if (param_2 == *piVar2) {

    *piVar2 = (int)piVar1;

    *piVar1 = param_2;

    *(int **)(param_2 + 4) = piVar1;

    return;

  }

  piVar2[2] = (int)piVar1;

  *piVar1 = param_2;

  *(int **)(param_2 + 4) = piVar1;

  return;

}
```


---

## Dual A/B re-verify (W19-K, 2026-07-29)

**Tools:** Ghidra `decompile_function` + `read_memory` only. No `disassemble_bytes`. No Launcher.
**OWN VA:** `0x00568b70` only.

### Live decompile (re-confirm ≡ 2026-07-23 raw)

```c
void __thiscall FUN_00568b70(int param_1,int param_2)

{
  int *piVar1;
  int *piVar2;
  
  piVar1 = *(int **)(param_2 + 8);
  *(int *)(param_2 + 8) = *piVar1;
  if (*(char *)(*piVar1 + 0x49) == '\0') {
    *(int *)(*piVar1 + 4) = param_2;
  }
  piVar1[1] = *(int *)(param_2 + 4);
  if (param_2 == *(int *)(*(int *)(param_1 + 4) + 4)) {
    *(int **)(*(int *)(param_1 + 4) + 4) = piVar1;
    *piVar1 = param_2;
    *(int **)(param_2 + 4) = piVar1;
    return;
  }
  piVar2 = *(int **)(param_2 + 4);
  if (param_2 == *piVar2) {
    *piVar2 = (int)piVar1;
    *piVar1 = param_2;
    *(int **)(param_2 + 4) = piVar1;
    return;
  }
  piVar2[2] = (int)piVar1;
  *piVar1 = param_2;
  *(int **)(param_2 + 4) = piVar1;
  return;
}
```

### Function bounds

| Field | Value |
|---|---|
| Entry | `0x00568b70` |
| Body | `0x00568b70`–`0x00568bbd` (**78 bytes** through last `ret 4`) |
| Ghidra name | `FUN_00568b70` |
| Leaf | **Yes** — no callees |

### Byte seal (`read_memory` @ `0x00568b70`, 256 B; body 78 B + INT3 pad)

```
hex: 8b5424048b4208568b308972088b30807e490075038956048b72048970048b49043b51045e750b8941048910894204c204008b4a043b11750a89018910894204c204008941088910894204c20400 cc...

8B 54 24 04        mov  edx, [esp+4]       ; x = node*
8B 42 08           mov  eax, [edx+8]       ; y = x->right
56                 push esi
8B 30              mov  esi, [eax]         ; y->left
89 72 08           mov  [edx+8], esi       ; x->right = y->left
8B 30              mov  esi, [eax]
80 7E 49 00        cmp  byte ptr [esi+0x49], 0  ; isnil(y->left)?
75 03              jne  skip_parent
89 56 04           mov  [esi+4], edx       ; y->left->parent = x
8B 72 04           mov  esi, [edx+4]       ; x->parent
89 70 04           mov  [eax+4], esi       ; y->parent = x->parent
8B 49 04           mov  ecx, [ecx+4]       ; head = map+4
3B 51 04           cmp  edx, [ecx+4]       ; x == root (head[1])?
5E                 pop  esi
75 0B              jne  not_root
89 41 04           mov  [ecx+4], eax       ; root = y
89 10              mov  [eax], edx         ; y->left = x
89 42 04           mov  [edx+4], eax       ; x->parent = y
C2 04 00           ret  4
; not_root:
8B 4A 04           mov  ecx, [edx+4]       ; parent
3B 11              cmp  edx, [ecx]         ; x == parent->left?
75 0A              jne  was_right
89 01              mov  [ecx], eax         ; parent->left = y
89 10              mov  [eax], edx
89 42 04           mov  [edx+4], eax
C2 04 00           ret  4
; was_right:
89 41 08           mov  [ecx+8], eax       ; parent->right = y
89 10              mov  [eax], edx
89 42 04           mov  [edx+4], eax
C2 04 00           ret  4
```

### Sealed ABI / algorithm

| Claim | Evidence |
|---|---|
| `__thiscall` map in ECX, node on stack | ECX used as map (`[ecx+4]`); `[esp+4]` = node; **`ret 4`** |
| **Left rotate** on RB/map tree | Pivot child = `node+8` (right); splice `right.left`; reattach as left of pivot |
| isnil guard at **`+0x49`** | `cmp byte ptr [esi+0x49],0` before writing parent of former right.left |
| Head: root at `head+4` | `cmp edx,[ecx+4]` / `mov [ecx+4],eax` with `ecx=map+4` |
| Parent left vs right | `cmp edx,[parent+0]` vs store to `parent+8` |
| Leaf | no `call` / no external `jmp` |
| Node link layout | left@0, parent@4, right@8 (MSVC `_Tree` style) |

### Named role (sealed, Inferred)

**`Tree_RotateLeft`** — MSVC-style red-black/map left rotate for nodes with **isnil @ +0x49** (staging map node size 0x50 family and any sibling tree sharing that layout).

### Callers (Ghidra `get_function_callers`)

| Caller | VA | Context (not dual-owned here) |
|---|---|---|
| `FUN_00538780` | `0x00538780` | MissionStagingTree_EraseNode (RB fixup) |
| `FUN_00538ea0` | `0x00538ea0` | Map_InsertNodeAndRebalance (L-rotate) |
| `FUN_00568c60` | `0x00568c60` | sibling tree helper (not owned) |
| `FUN_005690a0` | `0x005690a0` | sibling tree helper (not owned) |

### Related (not owned)

- `FUN_005681a0` — **right** rotate twin (child = left@0)
