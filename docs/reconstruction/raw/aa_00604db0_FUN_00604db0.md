# Raw capture: FUN_00604db0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00604db0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00604db0` |
| **Canonical name** | `FUN_00604db0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00604db0(int param_1,int param_2)

{
  if ((param_2 == 0) || (*(int *)(param_2 + 0x18) != *(int *)(param_1 + 0x18))) {
    *(int *)(param_1 + 0x24) = param_2;
  }
  return;
}
```

---

## W20-S re-verify append (2026-07-29)

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual W20-S |
| **Tools** | `decompile_function` + `read_memory` + `analyze_function_complete` + `get_function_xrefs`. **No** `disassemble_bytes` |
| **Live decompile** | Bit-identical body to 2026-07-23 raw above |
| **Inferred canonical** | `CVOGHBBase_SetPartnerLink` |

### Bytes (`read_memory` @ `0x00604db0`, 32 B)

```
8b 44 24 04    mov eax, [esp+4]     ; partner
85 c0          test eax, eax
74 08          jz  set              ; null partner → always write
8b 50 18       mov edx, [eax+0x18]  ; partner.owner
3b 51 18       cmp edx, [ecx+0x18]  ; this.owner
74 03          jz  ret              ; same owner → skip write
89 41 24       mov [ecx+0x24], eax  ; this.partner = partner
c2 04 00       ret 4
cc ×10         int3 pad
```

### ABI

- **`__thiscall`**: ECX = `CVOGHBBase* this`, stack0 = partner pointer (or null).
- **`RET 4`**.
- Void (no meaningful return value; EAX not set).

### Semantics (sealed)

```
if (partner == null || partner->owner(+0x18) != this->owner(+0x18))
    this->partner(+0x24) = partner;
// else leave +0x24 unchanged
```

- `+0x18` = **owner object** (sealed elsewhere via `CVOGHBBase_AttachOwnerObject`).
- `+0x24` = **partner HB link** (cross-link field used by targeting pair).

### Callers

| Site | Function | Role |
|---|---|---|
| `0x005173b6` | `Object_SetSelectedTarget` | cross-link self↔target HB (first direction) |
| `0x005173be` | `Object_SetSelectedTarget` | cross-link reverse |

Parent dual (`A_aa_005172d0`): asm `push edi; mov ecx,ebx; call` / `push ebx; mov ecx,edi; call` — decomp of parent drops 2nd args; this leaf body is complete.

### Prior misname

Scaffold alias `Named_CalleeOf_Named_CalleeOf_Client_Input_DriveControlTick_00604db0` is **wrong for direct callers** — sole direct caller is `Object_SetSelectedTarget` (which is reachable from drive tick among many). Prefer `CVOGHBBase_SetPartnerLink`.
