# Annotated low-level: FUN_00421b50 / StdTree_Max_Isnil29_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00421b50` |
| VA | `0x00421b50`–`0x00421b6c` exclusive (28 B) |
| System | MSVC std `_Tree` max / rightmost (isnil@+0x29 node family) |
| Date | 2026-08-04 WQ9D-G dual (scaffold 2026-07-23) |

## Machine-level notes

- Source: raw capture + live Ghidra `decompile_function` / `read_memory` / `analyze_function_complete` / callers+xrefs (2026-08-04).
- **Leaf** — no callees.
- **cdecl** one stack arg; plain `C3` RET; return in **EAX**.
- Decompiler prints `void` and a while-into-nil walk — **assembly/bytes are authoritative**.

### Node layout (isnil@0x29 family)

| Offset | Role | Evidence |
|---|---|---|
| `node+0` | left | twin min `FUN_0051b5d0` walks `*node` |
| `node+4` | parent | erase rewire uses `param_3[1]` |
| `node+8` (`[2]`) | right | this unit walks `[2]` |
| `node+0x28` | color | erase `param_3[10]` |
| `node+0x29` | isnil; **0 = real** | `CMP BYTE PTR [reg+0x29],0` |

Same isnil family as `Map_ExactFindByIntKey` (`0x0051c150`). Distinct from Val12 (isnil@`+0x19`) and char-key (isnil@`+0x21`).

### Semantics (byte-accurate)

```
// __cdecl; returns node*
node* StdTree_Max_Isnil29_Inferred(node* start) {
  node* right = start->right;          // +8
  if (right->isnil) return start;      // +0x29 != 0
  do {
    start = right;
    right = start->right;
  } while (!right->isnil);
  return start;
}
```

### Call-site role

Map erase helpers (`FUN_00409220` / clones): when erased node was `head->rightmost` (`*(head+8)`), if replacement subtree is non-nil call this to set new rightmost; else parent. Symmetric to `FUN_0051b5d0` for `head->leftmost` (`*head`).

## Pseudocode (annotated; CF from bytes)

```c
// Decompiler void rejected — EAX return sealed by call-site stores + leave EAX.

int *StdTree_Max_Isnil29_Inferred(int *node)
{
  int *right;

  right = (int *)node[2];
  if (*(char *)((int)right + 0x29) != '\0')
    return node;
  do {
    node = right;
    right = (int *)node[2];
  } while (*(char *)((int)right + 0x29) == '\0');
  return node;
}
```

## Open questions

- Product demangle / exact value_type of this map family.
- Whether all five erase clones share identical node size (isnil@0x29 sealed; key layout varies by map).
