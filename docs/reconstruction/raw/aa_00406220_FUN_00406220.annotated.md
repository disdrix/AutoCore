# Annotated low-level: FUN_00406220 / StdVector_PushBack_Elem0x28_EaxVec_EcxVal_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00406220` |
| VA | `0x00406220` |
| Body | `0x00406220`–`0x00406298` exclusive (**120 B** / `0x78`); pad `CC` |
| System | util / container (elem **0x28**) |
| Date | 2026-08-04 (WQ9H-C dual seal) |
| Named | `StdVector_PushBack_Elem0x28_EaxVec_EcxVal_Inferred` |

## Machine-level notes

- **ABI (bytes):** **EAX** = vector* (+4 begin, +8 end, +0xC capEnd); **ECX** = `const T*` value; **ret 0** (`C3`). Immediate `MOV EDI,EAX` threads host for body + slow-path insert-one.
- **Stride:** **0x28** (div magic `0x66666667`).
- **Fast path (room):** `size < capacity` and `begin != 0` → ConstructN `FUN_00409d40(end, count=1)` with value* on stack (phantom vec push + `add esp,0x10`); then `end += 0x28`.
- **Slow path (grow):** insert-one rebind `StdVector_InsertOne_RebindIt_Elem0x28_Via408050_Inferred` (`0x00406de0`, dualed WQ9G-H) with `where = end`; out-iterator discarded.
- **Callers:** `FUN_00933480` (overheat sample → vec at `*(ESI+0xc80)+0x68`); `FUN_00976900` (path parse → vec at `ESI+0x68`, stride check `/0x28`).
- Prefer bytes when decompiler drops EAX host as `in_EAX` / mislabels signature `void`.
- **Not** tree insert (neighbor `0x00406c40`); **not** dword push_back ABI (`EAX`/`ESI`).

## Pseudocode (annotated — registers recovered)

```c
// customcc / fastcall-shaped; ret 0
void StdVector_PushBack_Elem0x28_EaxVec_EcxVal_Inferred(
    /*EAX*/ void *vec,
    /*ECX*/ const void *value)
{
  int begin = *(int *)((char *)vec + 4);
  unsigned size, capacity;
  if (begin == 0) {
    // empty → grow path
  } else {
    size = (*(int *)((char *)vec + 8) - begin) / 0x28;
    capacity = (*(int *)((char *)vec + 0xc) - begin) / 0x28;
    if (size < capacity) {
      int end = *(int *)((char *)vec + 8);
      FUN_00409d40(end, 1); /* ConstructN @ end; value via ECX/stack packing */
      *(int *)((char *)vec + 8) = end + 0x28;
      return;
    }
  }
  // grow / empty: insert-one at end (EDI = vec inside callee)
  void *outScratch = (void *)value; /* stack slot reuse; result discarded */
  FUN_00406de0(/*EDI*/ vec, &outScratch, *(void **)((char *)vec + 8), value);
}
```

## Open questions

- Product demangle / 0x28 POD field layout (caller locals ~10 dwords).
- ConstructN `FUN_00409d40` dual (owned WQ9H-B residual).
- Runtime / bit-exact.
