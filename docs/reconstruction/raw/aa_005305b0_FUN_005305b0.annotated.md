# Annotated low-level: CVOGCharacter_SetMissionCounter (`FUN_005305b0`)

| Field | Value |
|---|---|
| Stable ID | `aa_005305b0` |
| VA | `0x005305b0`–`0x00530603` exclusive (**83** B) |
| System | `missions-progression` (shared Val12 map @ char+0x584) |
| Date | 2026-07-23 scaffold; **W30-I dual 2026-07-29** |
| Ghidra | `FUN_005305b0` |
| Proposed | `CVOGCharacter_SetMissionCounter` |

## Machine-level notes

- **ABI:** `__thiscall` ECX = character; stack `(uint32_t key, int32_t value)`; void; **`ret 8`**.
- **Map:** `LEA ESI,[this+0x584]` as find/insert this; compare result vs `*(this+0x588)` head/end.
- **Miss:** `StdTree_OperatorIndex_Val12` (`FUN_0053af20`) then write mapped pair `{0, value}`.
- **Hit:** absolute assign `*(node+0x14) = value` (not `+=`; sibling `00530610` adds).
- Prefer machine over decompiler for map this-pointer (decompile drops ECX on both callees).
- Sibling family: GetResourceBalance (`0052ada0`, read +0x10); AddMissionCounter (`00530610`, += +0x14, ret 0xC).

## Pseudocode (machine-corrected)

```c
// void __thiscall CVOGCharacter_SetMissionCounter(Character* this, uint32_t key, int32_t value)
// ret 8
void __thiscall FUN_005305b0(int character, uint32_t key, int32_t value)
{
  int node = character; // out-slot preloaded with this; find overwrites
  // thiscall ECX = character+0x584
  FUN_00418b80(/*map*/ character + 0x584, &node, &key);
  if (node == *(int *)(character + 0x588)) {
    // miss → get-or-insert; returns mapped* at node+0x10
    uint32_t *mapped = (uint32_t *)FUN_0053af20(/*map*/ character + 0x584, &key);
    mapped[0] = 0;
    mapped[1] = (uint32_t)value; // node+0x14
    return;
  }
  *(int *)(node + 0x14) = value;
}
```

## Open questions

- Product English for counter vs resource/currency dual use of map +0x584.
- Why GetResourceBalance reads **+0x10** while Set/Add write **+0x14** (same Val12 mapped pair).
- Full catalog of keys written by `FUN_0094e530` token path and `FUN_00530670` clear path.
