# Annotated low-level: FUN_0056ff00 / WeaponHitResultVec_PushBack

| Field | Value |
|---|---|
| Stable ID | `aa_0056ff00` |
| VA | `0x0056ff00`–`0x0056ff60` (97 B) |
| System | combat / weapon multi-hit staging |
| Date | 2026-07-29 (W24-R dual seal) |
| Canonical name | `WeaponHitResultVec_PushBack` (Inferred) |

## Machine-level notes

- **Tools:** Ghidra `decompile_function` + `read_memory` + `analyze_function_complete` (no `disassemble_bytes`).
- **ABI:** `__thiscall` ECX=vector shell*; stack `const Elem20*`; **`ret 4`**.
- **Stride:** element size **0x20** (`>> 5`, end bump `+0x20`).
- **Shell:** `+4` begin, `+8` end, `+0xc` capacity end.
- **Fast path:** `FUN_0056f1e0(end, 1, value)` copies **8 dwords**; advance end.
- **Slow path:** `FUN_0056fab0(this, end, 1, value)` grow/insert.
- **Caller:** sole `Weapon_ApplyDamageToTargets_Inferred` @ `0x0056e000` (2 sites).
- **Hex (97 B):** `568bf18b560485d2750433c9eb088b4e082bcac1f90585d274318b460c2bc2c1f8053bc873258b4424088b4c2408578b7e085056516a0157e8a3f2ffff83c41483c720897e085f5ec204008b5424088b4608526a01508bcee853fbffff5ec20400`

## Pseudocode (annotated)

```c
// __thiscall; RET 4
void WeaponHitResultVec_PushBack(VecShell_4 *vec, const uint32_t *value /* 8 dwords */)
{
  void *begin = vec->begin;           // +4
  if (begin != NULL) {
    size_t size = (vec->end - begin) >> 5;     // +8
    size_t cap  = (vec->cap_end - begin) >> 5; // +0xc
    if (size < cap) {
      void *end = vec->end;
      FUN_0056f1e0(end, 1, value);  // POD copy 0x20
      vec->end = (char*)end + 0x20;
      return;
    }
  }
  FUN_0056fab0(vec, vec->end, 1, value); // grow
}
```

## Open residual

- Product English for the 0x20 hit-record layout (guid dwords + score + flags from caller staging).
- Nested helper duals for `FUN_0056f1e0` / `FUN_0056fab0` (scaffolds exist; not re-sealed here).
- Runtime / bit-exact.
