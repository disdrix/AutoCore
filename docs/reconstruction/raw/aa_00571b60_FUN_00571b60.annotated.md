# Annotated low-level: InventoryGrid_ContainsItem (FUN_00571b60)

| Field | Value |
|---|---|
| Stable ID | `aa_00571b60` |
| VA | `0x00571b60` |
| Canonical name | `InventoryGrid_ContainsItem` |
| System | `inventory-transfer` |
| Date | 2026-07-29 (dual A/B byte seal) |

## Machine-level notes

- Source: raw decompile + `read_memory` byte seal (prefer bytes when decompiler conflicts).
- Decompiler drops thiscall `ECX` and list-base rewrite; does not show `ADD ECX,0x2C`.
- Callee `FUN_00415c00` = TraversalLock list membership (pointer compare).
- Sibling: Place attaches via `FUN_00566f00(grid+0x2c, item)` — same list embed.
- Cells are at `grid+0x28` (8 B COID); **not** touched here.

## Pseudocode (byte-corrected)

```c
// __thiscall  RET 4
uint8_t InventoryGrid_ContainsItem(InventoryGrid* this, void* item)
{
  if (item == NULL) {
    return 0;
  }
  // ECX = this + 0x2c; stack arg = item; tail
  return FUN_00415c00(/*list*/ (char*)this + 0x2c, item);
}
```

### Raw decompiler (incomplete ABI)

```c
undefined4 FUN_00571b60(int param_1)
{
  undefined4 uVar1;
  if (param_1 == 0) {
    return 0;
  }
  uVar1 = FUN_00415c00();  // args recovered from bytes: (this+0x2c, param_1)
  return uVar1;
}
```

## Call-site pattern (DropResponse)

```c
already = ContainsItem(grid, item);
if (!already || item.GetX() != locX || item.GetY() != locY)
  PlaceItemFootprint(grid, item, locX, locY, qty);
```

## Open questions

- PDB / retail English name.
- Full list node layout inside `FUN_00415c00` (out of unit).
- Runtime membership dump.
