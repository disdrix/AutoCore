# Raw capture: FUN_0051d0c0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051d0c0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051d0c0` |
| **Canonical name** | `FUN_0051d0c0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_0051d0c0(void *param_1,byte param_2)

{
  FUN_0051d0e0();
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```

---

## W37-O append (2026-08-04) — OWN-ONLY dual seal

| Field | Value |
|---|---|
| **Wave** | W37-O |
| **Tools** | `batch_decompile` / `decompile_function` + `analyze_function_complete` + `read_memory` + xrefs. **No** `disassemble_bytes`. |
| **Body** | `0x0051d0c0`–`0x0051d0de` exclusive (**30 B** / `0x1E`); pad `CC` then complete dtor @ `0x0051d0e0` |
| **Ghidra inclusive end** | last insn `RET 4` (`C2 04 00`) ends @ `0x0051d0dd` |
| **ABI** | **ECX=`this`**; stack **`flags`**; **`RET 4`** (`C2 04 00`); returns **`this`** in EAX |
| **Semantic name** | `ObjectCsList_ScalarDeletingDtor_Inferred` |
| **Classification** | worker |
| **Callers** | **DATA only** — vtbl `PTR_FUN_009ce154` @ `0x009ce154` slot0 → `0x0051d0c0`. No CODE callers. |
| **Callees** | `FUN_0051d0e0` (complete dtor @ +0x20), `operator_delete` (cdecl, `ADD ESP,4`) |
| **Live decompile** | ≡ scaffold raw (2026-07-23) CF; **false noreturn** on `operator_delete` — bytes fall through to `MOV EAX,ESI` / `RET 4` |
| **Accuracy note** | **Not skill.** Object-core CS-list derived scalar deleting dtor. Pair of sealed `ObjectCsList_CompleteDtor_Inferred` (`0x0051d0e0`, W31-R). Nearby string `CVOGClonedObject` is object system, not skill. |

### Full body hex (30 B)

```
568bf1e818000000f644240801740956e84dc7f6ff83c4048bc65ec20400
```

### Byte-level CF

1. `PUSH ESI`; `MOV ESI,ECX` — save **this**.
2. `CALL FUN_0051d0e0` (rel `E8 18 00 00 00` → `0x0051d0e0`) — complete dtor (derived vtbl → clear → base vtbl → clear → `DeleteCriticalSection(+4)`); **does not free**.
3. `TEST BYTE PTR [ESP+8],1` — **flags bit0** (after push ESI, stack formal at +8).
4. `JZ` skip free: if bit0 clear, skip delete.
5. Free path: `PUSH ESI`; `CALL operator_delete`; `ADD ESP,4` (cdecl).
6. `MOV EAX,ESI`; `POP ESI`; **`RET 4`**.

### Related (not owned)

| VA / symbol | Role |
|---|---|
| `FUN_0051d0e0` | `ObjectCsList_CompleteDtor_Inferred` (W31-R) — complete body this wraps |
| `FUN_0051bf10` | `ObjectCsListBase_ScalarDeletingDtor_Inferred` (W31-R) — base vtbl `009ce07c` slot0 |
| `FUN_0051bc90` | drain under CS (called from complete) |
| `FUN_0051b8a0` | `ObjectCsList_ClearDestroy_Inferred` (W30-F) |
| vtbl `0x009ce154` | derived; slot0 = this unit |
| vtbl `0x009ce07c` | base; slot0 = `0x0051bf10` |
