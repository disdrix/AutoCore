# Annotated low-level: FUN_00423b10 / NDResource_GetDefaultTypeToken_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00423b10` |
| VA | `0x00423b10`–`0x00423b3f` |
| System | ND resource cache / type token |
| Date | 2026-07-29 (W18-D dual A/B seal) |
| Tools | `decompile_function` + `read_memory` only |

## Machine-level notes

- Source: raw capture + live re-verify 2026-07-29.
- Classic one-shot CRT init: once-flag bit 0 at `0x00d1f738`, object vptr at `0x00d1f734`, atexit teardown `LAB_009c3070`.
- Returns **`&DAT_00d1f734`** (address of the singleton object), not the vtable pointer value.
- Sole caller `NDResource_LoadByKey` (`0x00423ac0`) inserts the return between cache key and remaining args as the **type token** for `NDResourceCache_LookupOrCreate`.

## Globals

| Address | Role |
|---|---|
| `DAT_00d1f734` | Singleton object storage (first dword = vptr) |
| `_DAT_00d1f738` / flag dword | Once-init bit 0 |
| `PTR_FUN_00a95ae8` | Static vtable installed into the singleton |
| `LAB_009c3070` | atexit thunk: `mov eax,&DAT_00d1f734; jmp dtor` |

## Pseudocode (annotated)

```c
// void* NDResource_GetDefaultTypeToken_Inferred(void)
// no args, cdecl/default; returns pointer to type-token object

void *NDResource_GetDefaultTypeToken_Inferred(void)
{
  if ((*(unsigned int *)0x00d1f738 & 1) == 0) {
    *(unsigned int *)0x00d1f738 |= 1;
    *(void **)0x00d1f734 = (void *)0x00a95ae8;   /* &PTR_FUN_00a95ae8 */
    _atexit((void (*)(void))0x009c3070);
  }
  return (void *)0x00d1f734;
}
```

## Open questions

- Product class name behind `PTR_FUN_00a95ae8`.
- Whether other type-token factories exist (sibling one-shots near `0x00423b40`).
