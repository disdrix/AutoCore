# Annotated low-level: FUN_005718a0 → InventoryGrid_SetOwner_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005718a0` |
| VA | `0x005718a0` |
| System | `inventory-transfer` |
| Date | `2026-08-04` (WQ8R-A dual; supersedes 2026-07-23 scaffold annotation) |
| Ghidra name | `FUN_005718a0` |
| Proposed name | `InventoryGrid_SetOwner_Inferred` |

## Machine-level notes

- **ABI:** `__thiscall` — `ECX` = `InventoryGrid*`; stack `ownerObject*`; `RET 4`.
- **Body:** `0x005718a0`–`0x00571911` (`0x72` B).
- **Vtable:** sole xref is DATA at `0x009d33a8` = `PTR_FUN_009d3390 + 0x18` (InventoryGrid vtbl slot +0x18). No direct CALL xrefs.
- **Prologue (read_memory):** `LEA ESI,[EDI+0x2c]` → embedded item list at `grid+0x2c`; traversal lock helpers take **list** in ECX, not grid root.
- **Lock geometry:** list CS at `grid+0x30` (= list+4); lock-held flag at `grid+0x54` (= list+0x28). Matches `FUN_004294f0` / unlock pattern on other grid list walkers (`0x00571920`).
- **Odd stack reuse (preserve):** after `*(grid+0x24)=owner`, the decompiler reuses the **same stack slot as `param_2`** as the list-item cursor via `FUN_004022a0(&iter, &param_2)`. Owner remains safely at `+0x24` for the vfunc arg.
- Prefer clean named locals for readability; CF order and dual-use semantics sealed.

## Offsets (body-backed)

| Offset | Role |
|--------|------|
| `grid+0x24` | Owner object* (written first) |
| `grid+0x2c` | Embedded list object (traversal this) |
| `grid+0x30` | `CRITICAL_SECTION` (list+4) |
| `grid+0x54` | `char` lock-held flag (list+0x28) |

## Pseudocode (annotated)

```c
// InventoryGrid_SetOwner_Inferred
// vtbl+0x18 of InventoryGrid (PTR_FUN_009d3390)
void __thiscall InventoryGrid_SetOwner_Inferred(InventoryGrid* grid, void* ownerObject)
{
  int iterState = 0;          // local_4 — list walk cursor state
  void* item;                 // decompiler reuses param_2 stack after store

  *(void**)((char*)grid + 0x24) = ownerObject;

  // ECX = grid+0x2c (embedded list). Enter CS + set lock flag.
  List_BeginTraversal_Inferred(grid + 0x2c);   // FUN_004294f0

  // Returns 0 while a live item* is produced into `item`.
  int done = List_Next_Inferred(grid + 0x2c, &iterState, &item); // FUN_004022a0
  while (done == 0) {
    // item vtbl +0x158(owner) — rebind each occupant to new owner
    void** vtbl = *(void***)item;
    ((void(__thiscall*)(void*, void*))vtbl[0x158/4])(item, *(void**)((char*)grid + 0x24));
    done = List_Next_Inferred(grid + 0x2c, &iterState, &item);
  }

  // Standard grid-list unlock: if flag @+0x54, clear + LeaveCriticalSection(+0x30)
  if (*(char*)((char*)grid + 0x54) != 0) {
    *(char*)((char*)grid + 0x54) = 0;
    LeaveCriticalSection((LPCRITICAL_SECTION)((char*)grid + 0x30));
  }
}
```

## Cross-seals

| Claim | Evidence |
|-------|----------|
| Stores owner at `+0x24` | Body first store; `MergeStackQty` / `RemoveItem` dirty owner via `+0x24` |
| vtbl+0x18 | `read_memory` dword at `0x009d33a8` = `0x005718a0`; cargo create calls this slot |
| Item rebind via `+0x158` | Same slot used by Place type-4 and CommitPlacementMap with `grid+0x24` |
| List lock pattern | Twin walker `FUN_00571920` / `FUN_004294f0` TraversalLock |

## Open questions

- Product English for owner object type (vehicle / character / abstract CVOG object).
- Exact product name of item vfunc `+0x158`.
- Runtime / bit-exact.
