# Annotated low-level: FUN_004617a0 → StdVector_InsertN_RcElem28

| Field | Value |
|---|---|
| Stable ID | `aa_004617a0` |
| VA | `0x004617a0`–`0x00461abb` exclusive (**795 B**) |
| System | MSVC `vector` insert-n, refcounted elem **0x1c** |
| Date | 2026-07-23 scaffold; W34-P seal 2026-07-29 |
| Canonical name | `StdVector_InsertN_RcElem28` (**Inferred**) |

## Machine-level notes

- **Bytes win** on ABI: **ECX=value\*** (copy-ctor source), stack **vector\*** / **where** / **count**, **`RET 0xC`**.
- Local fill via `FUN_00461ac0` (ESI→EDI) with **AddRef** on resource `@+0x10`; epilogue **Release** (vtbl+8).
- Element **0x1c**: 4 dwords + shared ptr@+0x10 + 3 bytes + dword@+0x18.
- Grow/in-place helpers are **non-trivial**: `FUN_004620b0` uninit-copy, `FUN_00461c90` fill-n, `FUN_00461780` destroy, `FUN_00461b80`/`FUN_00461ef0` assign with AddRef/Release.
- Callers: `FUN_004611e0` (resize grow), `FUN_004612d0` (insert-one).
- Reject scaffold `Named_CalleeOf_*_gfxDevice_*`.

## Signature (sealed)

```c
// ECX=value*, stack (vec*, where, count); RET 0xC; void
void StdVector_InsertN_RcElem28(
    const RcElem28* value,    /* ECX */
    StdVectorRc28* vec,       /* [ebp+8] */
    RcElem28* where,          /* [ebp+0xc] */
    uint32_t count);          /* [ebp+0x10] */
```

## Element (0x1c)

| Off | Field |
|---|---|
| +0x00..+0x0C | 4× u32 POD |
| +0x10 | shared resource* (refcount at `*(p+4)`) |
| +0x14..+0x16 | 3× u8 |
| +0x18 | u32 |

## Control flow (annotated)

1. SEH `LAB_009be5d8`; copy-ctor local from ECX value.
2. Cap/size triad; early-out `count==0`.
3. Overflow → `FUN_004540b0`.
4. **Grow:** 1.5×; new buffer; uninit-copy prefix/suffix + fill-n; destroy+delete old; publish.
5. **In-place:** hole (shift end + fill + assign) vs mid (uninit-copy end + reverse assign + fill assign).
6. Release local; `RET 0xC`.

## Full hex

See raw W34-P append (795 B).
