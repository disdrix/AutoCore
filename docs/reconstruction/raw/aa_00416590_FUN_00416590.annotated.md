# Annotated low-level: PtrVecShell0x14_DefaultCtor_ZeroSkip4_Inferred (FUN_00416590)

| Field | Value |
|---|---|
| Stable ID | `aa_00416590` |
| VA | `0x00416590`–`0x004165CF` exclusive (**63 B** / `0x3F`) |
| System | util / container shell (0x14 ptr-vector host) |
| Date | 2026-08-05 (WQ9I-J re-verify over 2026-07-23 scaffold) |
| Canonical name | `PtrVecShell0x14_DefaultCtor_ZeroSkip4_Inferred` (**Inferred**) |
| Ghidra name | `FUN_00416590` |

## Machine-level notes

- Source: live `batch_decompile` / `analyze_function_complete` + full-body `read_memory` + xrefs + `get_assembly_context`.
- **No** `disassemble_bytes`.
- Decompiler omits SEH; bytes show full FS:[0] install with handler `LAB_009bcfab` (shared with elem dtor `FUN_00417ee0`).
- **ECX=this**; plain `RET`; leaf (no callees).
- Zeros dwords at `+0`, `+8`, `+0xC`, `+0x10`; **skips `+4`** (intentional vs bug residual).
- Element stride **0x14** sealed by `_eh_vector_constructor_iterator_` count/size pack in `FUN_00820830` and by BSS statics at `0xd218e0` / `+0x14` / `+0x28` / `+0x3C`.
- Layout triad at `+8/+0xC/+0x10` matches pointer-vector begin/end/cap (peer dtor frees `+8`; static atexit path `FUN_00414950` walks begin→end with `_aligned_free`).

## Pseudocode (annotated)

```c
// ECX = Elem0x14*; ret 0; leaf
// Layout: +0 unk0; +4 unk4 (untouched); +8 begin; +0xC end; +0x10 capEnd
void __thiscall PtrVecShell0x14_DefaultCtor_ZeroSkip4_Inferred(uint32_t* self)
{
  // SEH: LAB_009bcfab; EH state -1 throughout
  self[0] = 0;   // +0
  // self[1] / +4 NOT written
  self[2] = 0;   // +8  begin
  self[3] = 0;   // +0xC end
  self[4] = 0;   // +0x10 capEnd
}
```

## Call sites

| Site | Role |
|---|---|
| `FUN_00820830` @ `0x0082085d` | `PUSH` ctor into eh_vector over 9×0x14 array at host+0xEE4 (audio UI `i_o_audio.xml` parent) |
| `0x009bf3c5` … `0x009bf635` | four static shells `0xd218e0`..`0xd2191c` then `_atexit` |

## Open questions

- Product English for the 0x14 shell / leading +0,+4 fields.
- Why +4 is not zeroed (UB on non-BSS paths vs intentional).
- Elem dtor `FUN_00417ee0` / complete static dtor `FUN_00414950` not dual-owned here.
- Runtime / bit-exact / differential — open.
