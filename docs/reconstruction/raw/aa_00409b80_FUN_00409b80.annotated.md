# Annotated low-level: FUN_00409b80 → StdTree_DestroyStringAt0C_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00409b80` |
| VA | `0x00409b80`–`0x00409bc0` inclusive (**65 B** / `0x41`) |
| System | MSVC-style tree node value destroy (`basic_string` @ +0x0c) |
| Date | 2026-08-05 (WQ9K-A OWN dual re-annotate; scaffold 2026-07-23) |
| Named | `StdTree_DestroyStringAt0C_Inferred` |

## Machine-level notes

- Source: raw capture + WQ9K-A re-verify for `aa_00409b80`.
- Decompiler correctly names `~basic_string` at `param+0xc` and SEH frame; machine seals **stdcall `RET 4`**.
- Shared by free-subtree twins isnil@+0x131 (`00409920`) and isnil@+0x2d (`00409970`) — both dualed WQ9J-I.
- Does **not** delete the node; callers `operator_delete` after this returns.

## Pseudocode (annotated)

```c
// ABI: stdcall; stack node*; void; RET 4
// Ghidra shows cdecl-ish `void(int)` without cleanup annotation.
void __stdcall StdTree_DestroyStringAt0C_Inferred(void* node)
{
  // SEH: PUSH -1; PUSH LAB_009bc656; FS:[0]
  // thiscall ~basic_string at node+0x0c via IAT [0x009c62f4]
  std::basic_string<char>::~basic_string(
      reinterpret_cast<std::string*>(static_cast<char*>(node) + 0x0c));
  // SEH teardown; ADD ESP,0x0C; RET 4
}
```

## Control flow

| Stage | Match |
|---|---|
| SEH prolog FS:[0] + `LAB_009bc656` | Confirmed |
| Try level `-1` | Confirmed |
| `ECX = node + 0x0c` | Confirmed |
| IAT CALL `[0x009c62f4]` | Confirmed |
| SEH epilog + `ADD ESP,0x0C` | Confirmed |
| `RET 4` | Confirmed |

## Call sites (free-subtree)

```
// Both free-subtree twins (isnil131 / isnil2d):
//   ... FreeSubtree(right);
//   left = node->left;
//   PUSH node; CALL 00409b80;          // destroy string @+0x0c
//   PUSH node; CALL operator_delete;  // cdecl ADD ESP,4
//   node = left;
```

## Open residual

- Product key/map type English (string proven; remaining payload open).
- Erase-range parents residual (WQ9K-B OWN pair).
- Runtime / bit-exact / differential.
