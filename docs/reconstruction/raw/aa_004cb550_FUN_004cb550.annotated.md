# Annotated low-level: StdTree_FreeSubtree_Isnil29_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004cb550` |
| VA | `0x004cb550`–`0x004cb584` inclusive (**53 B** / `0x35`) |
| Ghidra | `FUN_004cb550` |
| Canonical | `StdTree_FreeSubtree_Isnil29_Inferred` |
| System | skills-abilities / std map-set (isnil@+0x29 free-subtree for StdTree_EraseRange) |
| Date | 2026-08-04 (WQ9G-A OWN dual) |

## Machine-level notes

- Source: raw capture + WQ9G-A re-verify (`read_memory` full body + callers/xrefs + assembly_context).
- **Bytes authority** for left-spine walk after `operator_delete` (decomp warns noreturn incorrectly and drops loop).
- Role: MSVC-style `_Tree` free-subtree for large nodes (**isnil @ +0x29**). No value destroy.
- Sole external caller: dualed `StdTree_EraseRange_Isnil29_Inferred` (`0x004cbaa0`) full-clear path.
- Twin CF (do **not** merge): `Map_FreeSubtree_Isnil29` @ `0x0051bbc0` (Map_EraseRange free pair).

## Signature (sealed)

```c
// ECX = tree_base (threaded; unused in body except recursive pass)
// stack = node*; RET 4; void
void __fastcall StdTree_FreeSubtree_Isnil29_Inferred(void *tree_base, void *node);
```

## Pseudocode (byte-corrected)

```c
void __fastcall StdTree_FreeSubtree_Isnil29_Inferred(void *tree_base, MapNode_Isnil29 *node)
{
  (void)tree_base; // EBX preserves ECX across recursive calls only
  if (node->isnil /* @+0x29 */ != 0)
    return; // ret 4

  for (;;) {
    StdTree_FreeSubtree_Isnil29_Inferred(tree_base, node->right /* @+8 */);
    MapNode_Isnil29 *left = node->left; // @+0
    operator_delete(node);              // cdecl; ADD ESP,4 continues
    node = left;
    if (node->isnil != 0)
      break;
  }
  // ret 4
}
```

## Xrefs

| Kind | Address | Notes |
|---|---|---|
| CALL | `0x004cbabc` | `StdTree_EraseRange_Isnil29` full clear — push root |
| CALL | `0x004cb567` | self recursive (right child) |

## Open gaps

- Product/PDB demangle for map value_type of callers.
- Whether any value-dtor is required before free for this instantiation (none in this body).
- Runtime / bit-exact open.
