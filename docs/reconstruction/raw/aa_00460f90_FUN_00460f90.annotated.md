# Annotated low-level: FUN_00460f90 → StdVector_PushBack_RcElem28

| Field | Value |
|---|---|
| Stable ID | `aa_00460f90` |
| VA | `0x00460f90`–`0x00461011` exclusive (**129 B**) |
| System | MSVC `vector::push_back` for refcounted elem **0x1c** |
| Date | 2026-07-23 scaffold; W36-O seal 2026-07-29 |
| Canonical name | `StdVector_PushBack_RcElem28` (**Inferred**) |

## Machine-level notes

- **Bytes win** on ABI: **EAX=`vec*`**, **ECX=`value*`**, **`RET 0`**. Decompiler `__fastcall(param_1)` + phantom `in_EAX` is correct shape; stack has no T by-value.
- Prologue `PUSH ECX` both preserves value* across size/cap math and provides the outIt dword for slow-path insert-one (written then discarded).
- Size/cap use MSVC magic `0x92492493` signed divide by **28** (`0x1c`).
- Fast path only when `begin != 0` **and** `size < capacity`.
- Callees sealed: `StdUninitFillN_RcElem28` (W35-O), `StdVector_InsertOne_RebindIt_RcElem28` (W35-Q).
- Sole caller: `FUN_00965ab0` @ `0x00966449` (mesh/UI residual; vec at object+0x68).
- Reject scaffold `Named_CalleeOf_*gfxDevice*`.

## Signature (sealed)

```c
// EAX=vec*, ECX=value*, RET 0
void StdVector_PushBack_RcElem28(
    StdVectorRc28* vec,       /* EAX → EDI */
    const RcElem28* value);   /* ECX */
```

## Control flow (annotated)

1. `edi = vec` (from EAX); `ebp = begin = vec->begin`.
2. Compute `size = begin ? (end-begin)/0x1c : 0`.
3. If `begin == 0` **or** `size >= capacity` → slow path (4).
4. **Fast:** push value* + end; `EDX=1`; call fill-n; `ADD ESP,8`; `end += 0x1c`; return.
5. **Slow:** push value* + where=end + &outIt; call insert-one-rebind; return (ignore rebind).

## Full hex

See raw W36-O append (129 B).
