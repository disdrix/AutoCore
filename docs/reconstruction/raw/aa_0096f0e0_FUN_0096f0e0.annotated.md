# Annotated low-level: FUN_0096f0e0 → EffTexture_CreateOrAssignSlot_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0096f0e0` |
| VA | `0x0096f0e0`–`0x0096f15c` exclusive (**124 B**) |
| System | palantir graphics Effects — effTexture create/assign |
| Date | 2026-08-04 (W37-H) |
| Canonical | `EffTexture_CreateOrAssignSlot_Inferred` |

## Machine-level notes

- **ECX** = slot (`payload**`); preserved in EDI.
- **RET 0x18** seals six stack formals.
- Create path: five pushes then `call FUN_0096e9f0` (`e8 e9 f8 ff ff`).
- Fail: push msg `0x00a9ed24`, severity 3, line `0x57`, path `0x00a9ed50` → `vog_LogMessage`; `or eax,-1`; `ret 0x18`.
- Success acquire: `add [esi+8],1`; if `==1`: `lea ecx,[esi+4]`; `call [vtbl+4]`.
- Prior release: load `*edi`; if non-null `add [eax+8],-1`; if zero `lea ecx,[eax+4]`; `call [vtbl+8]`.
- Store `*edi = esi`; `xor eax,eax`; `ret 0x18`.
- Dead: `mov ebx,[esp+…]` never consumed.
- Prefer bytes over decompiler for factory arg count / RET size.

## Pseudocode (annotated)

```c
// thiscall ECX=slot*; stack p0..p5; RET 0x18
// returns 0 / -1
int EffTexture_CreateOrAssignSlot_Inferred(payload** slot, p0, p1_dead, p2, p3, p4, p5)
{
  payload* neu = FUN_0096e9f0(p0, p2, p3, p4, p5); // 5 args pushed
  if (!neu) {
    vog_LogMessage(effTexture.cpp, 0x57, 3, "Failed to create instance of texture.");
    return -1;
  }
  neu->ref@+8++;
  if (neu->ref@+8 == 1)
    thiscall (neu+4)->vtbl[+4]();   // first acquire
  payload* old = *slot;
  if (old) {
    old->ref@+8--;
    if (old->ref@+8 == 0)
      thiscall (old+4)->vtbl[+8](); // final release
  }
  *slot = neu;
  return 0;
}
```

## Open questions

- Exact English of factory args (format, dims, usage, pool) — see caller `PalantirEnv_InitEffectTextures` (`0x0048ed00`).
- Virtual method names at vtbl[+4]/[+8] (AddRef/Release vs custom).
- Runtime / bit-exact.
