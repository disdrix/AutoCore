# Annotated low-level: FUN_0093d880 / Client_ApplyVehicleSwitch_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0093d880` |
| VA | `0x0093d880`–`0x0093da25` (422 B) |
| Ghidra | `FUN_0093d880` |
| Named | `Client_ApplyVehicleSwitch_Inferred` |
| System | vehicle / client chassis switch apply |
| Date | 2026-07-29 (W26-S seal) |
| Dual | A/B accept-with-gaps |

## Machine-level notes

### ABI

| Fact | Evidence |
|---|---|
| Client in **EAX** | prologue `8B D8` (mov ebx,eax); offsets `+0xe98` character, `+0x1078`, `+0xf38`, `+0x9c4`, `+0x30b4` match client layout |
| Stack formal | `[ebp+8]` = new vehicle* (`8B 4D 08` before SwitchVehicle) |
| Cleanup | **`RET 4`** — one stack dword |
| Return | **AL = 1** always (`B0 01` before epilogue) |

### Control-flow stages

1. **Vehicle host notify** — `character+0x250` vehicle → vbase this-adjust vfunc **`+0x1c8`**.
2. **UI prep flags** — `client+0x30b4=1`, `+0x30b5=0`; optional `client+0x309c` vtbl`+4`(0); vehicle subobj `+0x40` vtbl`+0x1c`(0).
3. **Optional dialog close** — if `client+0x1078` and vtbl`+0xd0` true → `FUN_007fca10` (dialog in EAX, client EBX convention).
4. **Clear two UI object slots** on `client+0xf38`: slots `+0xc70` / `+0xc74` — if non-null vtbl`+0x18`(1,1) then null both.
5. **Chassis switch** — `Character_SwitchVehicle_Inferred(character, newVehicle, &outOld, 0xFFFFFFFF, 0xFFFFFFFF)`.
6. **`FUN_009147a0`** — post-switch helper (scaffold; not owned).
7. **Garage/UI rebind** — `*(client+0x1078)+0x514` (`[0x145]`) ← new `character+0x250`; `FUN_008801b0(piVar2)`.
8. **Optional** `FUN_0085e970` when `*(client+0x1040)+0x50c` and nested `+0x580` non-null (also used from switch-failure path).
9. **Dialog open** — `FUN_007fef20(client, dialogIndex=0x12, show=1, flag=0)`.
10. **Copy client `+0x9c4`** → vehicle `+0x105` and `+0x106`.
11. **Combat pools** — `Vehicle_EnsureRegenerationHeartbeat` + `Vehicle_RecalcCombatPools` on new vehicle.
12. **`Client_RefreshOpenMissionUiWindows(client)`**.
13. Return 1.

### Switch call-site bytes @ `0x0093d93x`–`0x0093d95b`

```
mov ecx, [ebp+8]          ; newVehicle
xor eax, eax
push -1                   ; argB
mov [esi+0xc70], eax      ; null UI slots (esi = client+0xf38)
mov [esi+0xc74], eax
push -1                   ; argA
lea eax, [esp+0x14]
push eax                  ; &outOld
push ecx                  ; newVehicle
mov ecx, [ebx+0xe98]      ; character
call FUN_005252f0         ; @ 0x0093d95b; RET 0x10
```

### Xrefs

Ghidra reports **0 CODE xrefs** and **0 data xrefs** to entry. Invoker is indirect or not recovered; do not invent callers. Pairing with `Client_OnVehicleSwitchFailure` is **role contrast only** (that unit owns a success subpath that also calls SwitchVehicle).

## Pseudocode (annotated)

```c
// uint8_t Client_ApplyVehicleSwitch_Inferred(Client* client /*EAX*/, Vehicle* newVehicle /*stack*/)
// RET 4; always returns 1

undefined4 FUN_0093d880(undefined4 param_1 /* newVehicle */)
{
  int in_EAX; /* client */
  // ... stages 1–12 as above ...
  FUN_005252f0(/*ECX=character*/ param_1, &outOld, 0xffffffff, 0xffffffff);
  // ... refresh UI / pools ...
  return 1;
}
```

## Open questions

- Product/PDB name; static invoker (vtable/dispatch table) not found in Ghidra xrefs.
- English for dialog index **0x12** opened post-switch.
- Full contracts of `FUN_009147a0`, `FUN_008801b0`, `FUN_0085e970` (not owned).
- Whether SwitchVehicle HRESULT is ignored deliberately (no branch on return).
