# Annotated low-level: PalantirSub10_DestroyOwnedPtrVecAndComUninit

| Field | Value |
|---|---|
| Stable ID | `aa_00964d00` |
| VA | `0x00964d00`–`0x00964DAA` exclusive (**170 B**) |
| Ghidra | `FUN_00964d00` |
| System | client / Palantir nested subobject at `host+0x10` |
| Date | 2026-07-29 (OWN-ONLY W35-J) |

## Machine-level notes

- Source: raw capture + W35-J `read_memory` full body. **Bytes win** over decompiler.
- ABI: **stdcall** one stack `self*`; **`RET 4`**. Not thiscall; not cdecl (W34-K “cdecl” corrected).
- SEH handler `LAB_009ab766`.
- Owned-ptr vector triad at `+4/+8/+0xC`; COM iface at `+0x10` (Release via vtbl+8).
- Element complete dtor `FUN_00735390` then `operator_delete` per non-null slot — element dual **unowned**.
- Always calls IAT `CoUninitialize` after Release (Ghidra: `PTR_CoUninitialize_009c697c`).
- Nest: called from `Palantir_CompleteDtor` (`0x00754320`) and matching SEH unwind sites with `host+0x10`.

## Pseudocode (bytes authority)

```c
// stdcall; RET 4; void
void PalantirSub10_DestroyOwnedPtrVecAndComUninit(PalantirSub10* self)
{
  // SEH frame LAB_009ab766
  void** it = self->begin;
  void** end = self->end;
  while (it != end) {
    void* elem = *it;
    if (elem != nullptr) {
      FUN_00735390(elem);       // element complete dtor (unowned dual)
      operator_delete(elem);
    }
    ++it;
  }
  if (self->begin != nullptr)
    operator_delete(self->begin);

  self->begin = self->end = self->capEnd = nullptr;

  if (self->com != nullptr)
    self->com->Release();       // vtbl+8
  self->com = nullptr;
  CoUninitialize();

  // dead second free of begin (always null after zero)
  self->begin = self->end = self->capEnd = nullptr;
  // unlink SEH; RET 4
}
```

## Open questions

- Product type / English name of element (`FUN_00735390` plate) and of `+0x00` field.
- Whether `CoUninitialize` is always balanced by a matching `CoInitialize*` on the ctor path (ctor dual open).
- Runtime / bit-exact.
