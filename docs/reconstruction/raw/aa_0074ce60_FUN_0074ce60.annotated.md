# Annotated low-level: FUN_0074ce60 / phyBoundingBox_Unserialize

| Field | Value |
|---|---|
| Stable ID | `aa_0074ce60` |
| VA | `0x0074ce60`–`0x0074d0b4` exclusive (**596 B** / `0x254`) |
| System | palantir physics / sto-chunk (`phyBoundingBox.cpp`) |
| Date | 2026-08-04 (W38-H dual) |

## Machine-level notes

- Product path: `C:\vog\1_code\palantir\palantir\physics\phyBoundingBox.cpp`.
- Tag **`'BBOX'`** = `0x42424F58`; versions **1** and **2**.
- ABI: **ECX = bbox\***, **EBX = stoChunkReader\*** (chunk-reader convention), return **0 / -1**.
- SEH frame `LAB_009b1ab3`; leave via `FUN_00769e40` when scope flag set.
- Binary vs text: reader `+0x4044 == 0` → binary readers (`FUN_00435fe0` f32, `FUN_00437000` u8/bool); else text (`FUN_00767fd0` f32, `FUN_00769660` bool).
- Three `stoChunkReader_readF32Array` with **EAX=3**, dest **ECX**, reader stack: fields **`+0x14`, `+0x20`, `+0x00`** (stream order B→C→A; ADV 2026-08-04 corrected from false A→B→C seal).
- Radius at `+0x10`: `abs(sqrt((z1-z0)^2 + (x1-x0)^2))` using `+0x00/+0x08` vs `+0x20/+0x28`.
- v1 sets bool `+0x2C` from `(float@+0x0C) <= 0`; v2 reads bool from stream first.

## Signature (byte-sealed)

```c
// ECX = phyBoundingBox*; EBX = stoChunkReader*; returns 0 ok / -1 fail
int phyBoundingBox_Unserialize(phyBoundingBox* self /*ECX*/);
```

## Object layout (partial)

| Off | Field |
|---|---|
| `+0x00` | float3 A |
| `+0x0C` | float scalar |
| `+0x10` | float computed horizontal extent |
| `+0x14` | float3 B |
| `+0x20` | float3 C |
| `+0x2C` | bool |

## Callers

`gfxGeometryPiece_Unserialize` (`0x0074af60`), `FUN_00765740` (physics asset path), `FUN_0095ef50`, `FUN_0095f560`, `FUN_00960f80`.

## Confidence

| Claim | Level |
|---|---|
| phyBoundingBox unserialize role | **Confirmed** (path + strings) |
| Tag / versions / return codes | **Confirmed** |
| ECX object / EBX reader | **Confirmed** (entry + callers) |
| Field English (min/max/center labels) | Probable / open |
| Runtime / bit-exact | Open |
