# Annotated low-level: Color_LerpU8x3_Inferred / FUN_004b2910

| Field | Value |
|---|---|
| Stable ID | `aa_004b2910` |
| VA | `0x004b2910`–`0x004b29cd` (190 B) |
| System | client color / object-motion stage params |
| Date | 2026-07-29 (W28-E OWN re-annotate) |

## Machine-level notes

- **ABI:** cdecl stack `(float t, uint8_t* out, uint8_t* from, uint8_t* to)`; plain **`RET`** (`C3`); void. Callers `add esp, 0x10`.
- **Leaf:** no `CALL` to other FUN_*; uses FPU load + SSE mul + CRT/`ROUND` pattern thrice.
- **Channel order:** processes index **2, then 1, then 0** (matches BGR-ish packed triples used by stage color fields; **no alpha**).
- **Sole caller:** `ObjectMotion_ApplyStageParams_Inferred` (`FUN_004b2b90`):
  - dirty path bit2: RNG `t` → lerp stage endpoints → `slot+0xd`
  - dirty path bit4: RNG `t` → lerp stage endpoints → `slot+0x47`
  - continuous path bit: progress `t` → lerp `slot+0x2a`→`slot+0x47` into `slot+0xd`

## Pseudocode (behavior-preserving)

```c
// cdecl void; ret 0; leaf
void Color_LerpU8x3_Inferred(float t, uint8_t *out, const uint8_t *from, const uint8_t *to)
{
  // channel 2
  out[2] = (uint8_t)(from[2] + (int)ROUND((float)((int)to[2] - (int)from[2]) * t));
  // channel 1
  out[1] = (uint8_t)(from[1] + (int)ROUND((float)((int)to[1] - (int)from[1]) * t));
  // channel 0
  out[0] = (uint8_t)(from[0] + (int)ROUND((float)((int)to[0] - (int)from[0]) * t));
}
```

## Open questions

- Product/PDB name.
- Exact English of the three stage color slots (tint vs light vs mesh).
- Whether `out` may alias `from`/`to` (safe if out≡from; out≡to is progressive).
- Runtime / bit-exact — open.
