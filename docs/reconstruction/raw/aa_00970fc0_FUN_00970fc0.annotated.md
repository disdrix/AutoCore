# Annotated low-level: FUN_00970fc0 → AssPreloader_DrainAcRingToTree_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00970fc0` |
| VA | `0x00970fc0`–`0x00971024` inclusive (**101 B** / `0x65`) |
| System | assPreloader / asset preload pipeline |
| Date | 2026-08-04 (W37-AD re-verify) |
| Canonical name | `AssPreloader_DrainAcRingToTree_Inferred` |

## Machine-level notes

- **Host ABI:** retail passes **AssPreloader\*** in **EAX** (`MOV EBX,EAX`). Decompiler `void FUN_00970fc0(void)` is incomplete.
- **Ring:** `ESI = host+0xAC` — locked `{u32 key, u8 flag}` ring (pop via `FUN_0043e850`, EDI=out entry).
- **Tree:** after first successful pop, `ADD EBX,0xDC` once → membership tree at **host+0xDC** (same tree as W32-J lower_bound / find-or-insert).
- **Local entry:** stack `{u32 key @+0, u8 flag @+4}`; zeroed by `FUN_0043d6f0` (EAX=local), filled by each pop.
- **Branch:**
  - **flag ≠ 0 (success):** `FUN_0043d700` find-or-insert (ECX=key\*, EDI=tree) → `*value = 1`.
  - **flag == 0 (fail/side):** `FUN_0043d770` (EAX=tree, ECX=key\*) — sole caller; product role open.
- **Callers (2):** `FUN_00971280` (W32-J TryPreloadChildAssets… pre-loop drain), `FUN_00972140` (W32-K PreloadWorkerLoop). Both `MOV EAX, host`.
- Prefer bytes over decompiler for register ABI and one-time `ADD EBX,0xDC`.

## Pseudocode (byte-corrected)

```c
// EAX = AssPreloader* host; bare RET; void
void AssPreloader_DrainAcRingToTree_Inferred(void /*EAX=host*/)
{
  PodU32U8 local = {0, 0};          // FUN_0043d6f0
  GuardedRing* ring = host + 0xAC;  // ESI
  // first pop
  if (!RingPop(ring, &local))       // FUN_0043e850 → AL
    return;
  Tree* tree = host + 0xDC;         // ADD EBX,0xDC once
  do {
    if (local.flag == 0)
      FUN_0043d770(tree, &local.key);   // fail path (unowned)
    else {
      uint32_t* val = FUN_0043d700(tree, &local.key); // find-or-insert
      *val = 1;
    }
  } while (RingPop(ring, &local));
}
```

## Open questions

- Product/PDB method English for drain.
- Exact product role of fail-path `FUN_0043d770` (erase / notify / cascade).
- Flag polarity English beyond structural success/fail split used by pushers (`flag=1` seed/success, `flag=0` side/fail).
- Runtime / bit-exact / differential.
