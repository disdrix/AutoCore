# Annotated low-level: FUN_0046a260 → StdUninitFillN_Elem28_CountEcx_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0046a260` |
| VA | `0x0046a260`–`0x0046a281` (**34 B**) |
| System | stl-helpers / POD uninit fill-n (elem 0x1c) |
| Date | 2026-07-23 scaffold; **W36-M dual seal 2026-07-29** |

## Machine-level notes

- **Bytes win** on ABI: **ECX=count**, **EAX=dest***, **EBX=template***; bare **RET**.
- Decompiler shows only `param_1` (ECX) + phantoms `in_EAX` / `unaff_EBX` — register formals recovered from body + parents.
- Copy uses **`REP MOVSD`** with **ECX=7** (7 dwords = 0x1c), not the explicit 7-store form of `0045f150`.
- Null dest: skip store, still `ADD EAX,0x1C`.
- Leaf: 0 callees. Callers: `FUN_00469c80`, `FUN_00469f50` (3 sites total).

## Pseudocode (annotated)

```c
// ECX=count, EAX=dest*, EBX=template*; bare RET
void StdUninitFillN_Elem28_CountEcx_Inferred(int count, uint32_t* dest, const uint32_t* tmpl)
{
  for (; count != 0; count = count - 1) {
    if (dest != nullptr) {
      // MOV ECX,7; MOV ESI,tmpl; MOV EDI,dest; REP MOVSD
      const uint32_t* s = tmpl;
      uint32_t* d = dest;
      for (int i = 7; i != 0; i = i - 1) {
        *d = *s;
        s = s + 1;
        d = d + 1;
      }
    }
    dest = dest + 7; // +0x1c bytes
  }
}
```

## Open questions

- Concrete POD T English / product demangle.
- Runtime / bit-exact (deferred).
