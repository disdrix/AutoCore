# Raw capture: FUN_00576860

| Field | Value |
|---|---|
| **Stable ID** | `aa_00576860` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00576860` |
| **Canonical name** | `WaterPhase_RegisterToken_Inferred` (prior scaffold `FUN_00576860`) |
| **System** | client / VOGEnvironmentSector water phase |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 FUN_00576860(void)

{
  void *pvVar1;
  int iVar2;
  undefined4 uVar3;
  
  pvVar1 = operator_new(4);
  if (pvVar1 != (void *)0x0) {
    iVar2 = FUN_00576c40();
    if (iVar2 != 0) {
      uVar3 = FUN_00746400(iVar2);
      return uVar3;
    }
  }
  return 0xffffffff;
}
```

---

## Re-verify 2026-07-29 W32-S (append only)

| Field | Value |
|---|---|
| **Tool** | Ghidra MCP `batch_decompile` / `decompile_function` + `read_memory` (**no** `disassemble_bytes`) |
| **Partition** | W32-S dual (`0x005769c0`, `0x00576860`) |
| **Address** | `0x00576860` |
| **Body** | `0x00576860`–`0x00576889` exclusive (**41 B** / `0x29`) |
| **Epilogue** | bare **`C3`** (`ret`); pad `CC` then next SEH function |

### Live decompile (2026-07-29)

```c
undefined4 FUN_00576860(void)

{
  void *pvVar1;
  int iVar2;
  undefined4 uVar3;
  
  pvVar1 = operator_new(4);
  if (pvVar1 != (void *)0x0) {
    iVar2 = FUN_00576c40();
    if (iVar2 != 0) {
      uVar3 = FUN_00746400(iVar2);
      return uVar3;
    }
  }
  return 0xffffffff;
}
```

≡ original raw CF. **Decompiler omits** the sealed `ECX = DAT_00d1f620` this-arg into `FUN_00746400` (bytes below).

### `read_memory` @ `0x00576860` (48 B)

Hex:

```
6a 04 e8 2b 30 f1 ff 83 c4 04 85 c0 74 0b
8b c8 e8 cb 03 00 00 85 c0 75 04
83 c8 ff c3
8b 0d 20 f6 d1 00 50 e8 77 fb 1c 00 c3
cc cc cc ...
```

| Offset | Bytes | Meaning |
|---|---|---|
| +0 | `6A 04; E8 …; 83 C4 04` | `operator_new(4)` (rel → ~`0x00489892`) |
| +0xA | `85 C0; 74 0B` | null → fail (`OR EAX,-1; RET`) |
| +0xE | `8B C8; E8 CB 03 00 00` | `ECX=token`; **`call FUN_00576c40`** (vtbl install) |
| +0x15 | `85 C0; 75 04` | null → fail |
| +0x19 | `83 C8 FF; C3` | **return `-1`** |
| +0x1C | `8B 0D 20 F6 D1 00` | **`ECX = [DAT_00d1f620]`** |
| +0x22 | `50; E8 77 FB 1C 00; C3` | push token; **`call FUN_00746400`**; return its EAX |

### Callee roles (context only; not OWN deliverables)

| Addr | Role (decompile + bytes) |
|---|---|
| `FUN_00576c40` | 4-byte object field ctor: `*this = PTR_FUN_009d3ac4`; returns this |
| `FUN_00746400` | `__thiscall` container@`DAT_00d1f620`: linear find token in `[+4,+8)`; hit→**1**; miss→append path (`FUN_004406e0`)→**0** |
| vtbl `0x009d3ac4` | nearby rdata `"CVOGPhas…"` (phase token family) |

### Return contract (caller-sealed)

`PalantirEnv_InitPhases_Inferred` treats **`hr < 0`** as water fail log. So:

| EAX | Meaning |
|---|---|
| `-1` / `0xFFFFFFFF` | OOM or null token after ctor | **fail** |
| `0` | token not previously in list (append path) | **ok** |
| `1` | token already present | **ok** |

### Twin note

Distort peer `FUN_00576d70` is **byte-isomorphic** CF with different micro-ctor (`FUN_00577030`) — same registry `DAT_00d1f620`.

### Name

`WaterPhase_RegisterToken_Inferred` — role-sealed **INFERRED** (water arm post-vcall(+0x20) registration). Body itself is registry-generic; water binding is caller path. **Reject** scaffold `Named_CalleeOf_Drive_NDRiver_fx_00576860`.
