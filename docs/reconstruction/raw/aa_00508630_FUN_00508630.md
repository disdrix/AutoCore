# Raw capture: FUN_00508630

| Field | Value |
|---|---|
| **Stable ID** | `aa_00508630` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00508630` |
| **Canonical name** | `FUN_00508630` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 * __thiscall FUN_00508630(undefined4 *param_1,byte param_2)

{
  *param_1 = &PTR_FUN_009cdab0;
  if ((param_1[6] != 0) && (*(int *)(param_1[6] + 0xb0) != 0)) {
    FUN_005085b0(param_1);
  }
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```
---

## Live dual seal (W23-Q, 2026-07-29)

**Tools:** Ghidra `decompile_function` + `analyze_function_complete` + `read_memory` + `get_function_by_address`. **No** `disassemble_bytes`. No Launcher.

| Field | Value |
|---|---|
| Body | `0x00508630`–`0x00508667` (**55** B / `0x37`) |
| ABI | **`__thiscall`**: ECX = `CVOGHBBase*`; stack `byte flags`; epilogue **`ret 4`** (`C2 04 00`); EAX = this |
| Xref | Base vtbl slot 0 at `PTR_FUN_009cdab0` (`0x009cdab0`) |
| Role | MSVC **scalar deleting destructor**: complete-dtor body + optional `operator_delete` when `(flags & 1)` |
| Sibling | Complete body only: `CVOGHBBase_dtor` @ `0x00508390` (no free) |
| Name | `CVOGHBBase_ScalarDeletingDtor` |

### Full body bytes (`read_memory` @ `0x00508630`, 48 B; pad `CC` after `ret 4`)

```
56                   push esi
8B F1                mov  esi, ecx              ; this
8B 46 18             mov  eax, [esi+0x18]       ; pOwnerObject
85 C0                test eax, eax
C7 06 B0 DA 9C 00    mov  dword [esi], 0x009CDAB0  ; base vtbl always
74 12                jz   skip_unlink
8B 80 B0 00 00 00    mov  eax, [eax+0xB0]       ; listMgr
85 C0                test eax, eax
74 08                jz   skip_unlink
56                   push esi                   ; HB*
8B C8                mov  ecx, eax              ; ECX = listMgr
E8 5E FF FF FF       call FUN_005085b0          ; rel → 0x005085b0
skip_unlink:
F6 44 24 08 01       test byte [esp+8], 1       ; flags (after push esi)
74 09                jz   no_free
56                   push esi
E8 C3 11 F8 FF       call operator_delete
83 C4 04             add  esp, 4
no_free:
8B C6                mov  eax, esi
5E                   pop  esi
C2 04 00             ret  4
```

Hex: `568bf18b461885c0c706b0da9c0074128b80b000000085c07408568bc8e85efffffff644240801740956e8c311f8ff83c4048bc65ec20400`

### Live decompile (matches raw CF; callee this corrected by machine)

```c
undefined4 * __thiscall FUN_00508630(undefined4 *param_1, byte param_2)
{
  *param_1 = &PTR_FUN_009cdab0;
  if ((param_1[6] != 0) && (*(int *)(param_1[6] + 0xb0) != 0)) {
    // machine: ECX=*(owner+0xB0), stack=this — not thiscall on HB
    FUN_005085b0(param_1);
  }
  if ((param_2 & 1) != 0) {
    operator_delete(param_1);  // may not return
  }
  return param_1;
}
```

`param_1[6]` ≡ offset `+0x18` (pOwnerObject). Machine order loads owner before vtbl store (observable-equivalent for single-thread dtor).
