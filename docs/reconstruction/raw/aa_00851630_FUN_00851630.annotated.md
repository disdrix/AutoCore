# Annotated low-level: Client_UI_MapView_DrawMarkersFovOverlay (`FUN_00851630`)

| Field | Value |
|---|---|
| Stable ID | `aa_00851630` |
| VA | `0x00851630`–`0x00852428` (3576 B / `0xDF8`) |
| Canonical name | `Client_UI_MapView_DrawMarkersFovOverlay` (INFERRED) |
| Ghidra name | `FUN_00851630` |
| System | client UI / map-view overlay |
| Date | 2026-07-29 (W23-N dual seal) |

## Machine-level notes

- Source: raw capture + live W23-N `decompile_function` / `read_memory` / `analyze_function_complete`.
- **Role:** Composite **map-view draw pass** for a UI map host (`this`). Draws marker icons, auto-patrol waypoints, mission objectives, special point, squad mates, then a player FOV/range textured quad and optional radar cone.
- **ABI:** **`__thiscall`** — ECX = this*. Stack 16-byte aligned; large frame `0x164`. Plain **`ret` (`C3`)** — no stack args.
- **Primary host:** `*(this + 0x488)` = map config / map-view settings object.
- **Enable flag:** `*(char*)(config + 0x4fc) != 0` required (this pass is opt-in).
- **Globals:** `DAT_00d1b644` world/client host; `DAT_00d1b6d8` local entity; `DAT_00d1d964` marker circular list; `DAT_00d1ad10` auto-patrol; `DAT_00d1b938` mission tree; `DAT_00d1ad1c..28` special point.
- **Layer counters on this:** `+0x68` current layer (++ between phases), `+0x70` max, `+0x74 = (float)-layer`.
- **Icon half-size:** `config+0x538` (X), `config+0x53c` (Y). Marker textures `config+0x600 + type*4` for type 0..8.
- **FOV textures:** main quad `config+0x5a0`, cone `config+0x5a4`; fallback `DAT_00d1ec78`.
- **Color cookie:** `this+0x224` (RGBA bytes) → scaled to ARGB; default `0xFF32FF32`.
- **Constants:** FOV scale **300.0** (`DAT_00aaa8a8`); cone angle scale **2.5** (`DAT_00aaa6c4`); cone size **30.0** (`DAT_00a0f694`).
- **Dispatch:** vtable DATA only @ `0x00a68204` (sibling slot neighbors shared with entity-overlay class).
- Decompiler shows `__fastcall` with ECX param — sealed as thiscall method of map UI object.
- Note: decompiler `1.4013e-45` is integer **1** mis-typed as float before `(1.0f / viewportDim) * 300.0f`.

## Recovered CF (phase-accurate)

```c
// __thiscall; no stack args; void
void Client_UI_MapView_DrawMarkersFovOverlay(MapView* self)
{
    MapConfig* cfg = self->config; // +0x488
    if (!cfg || !g_world || !g_world->listHost /*+0xe4e8*/ || !g_local || !cfg->overlayEnable /*+0x4fc*/)
        return;

    // pose = g_local->vtbl[+0x1a0]()  // 4 floats

    // --- markers (DAT_00d1d964) ---
    for (node = list->next; node != list; node = node->next) {
        screen = Project(node); // FUN_0084f610 (+ optional attach vfunc +0x144)
        x = max(0, screen.x - cfg->iconHalfW); // +0x538
        y = max(0, screen.y - cfg->iconHalfH); // +0x53c
        type = node->type; // +8
        if (type in [0,8] && cfg->markerTex[type] /*+0x600*/)
            DrawTextured(rect, tex, color); // FUN_00413f50 + FUN_00413680
        else
            DrawDefault(rect, DAT_00d1ec78, color); // FUN_00758460
    }
    BumpLayer(self); // +0x68/+0x70/+0x74

    // auto-patrol waypoints → FUN_008513d0
    // mission tree DAT_00d1b938 → FUN_008513d0(..., 0x65)
    BumpLayer(self);
    // special point DAT_00d1ad1c → FUN_008513d0(..., 100)
    BumpLayer(self);
    // squad FUN_00574760 ×4 if local+0xcb0 → FUN_008513d0(..., 1)
    BumpLayer(self);

    // FOV quad + optional cone (camera pair DAT_00d1b780==DAT_00d1b778)
    // half = 300 / viewportDim; four corners via FUN_0084f4f0; FUN_00757890
    // cone if sin(cfg->fovAngle * 2.5) > 0; tex cfg+0x5a4
}
```

## Call graph (OWN context)

| Direction | VA | Name / role |
|---|---|---|
| Callee | `0x008513d0` | Map icon draw helper (`Named_VOG_DEBUG_STOP_008513d0`) |
| Callee | `0x0084f610` | Screen project |
| Callee | `0x0084f4f0` | Corner project (FOV) |
| Callee | `0x00758460` | Default textured rect draw |
| Callee | `0x00757890` | Multi-vert textured draw (FOV/cone) |
| Callee | `0x00413680` / `0x00413f50` | Textured marker path |
| Callee | `Client_EvalAutoPatrolWaypoint` | Patrol waypoint eval |
| Callee | `0x0046f100` | RB tree step |
| Callee | `0x00574760` | Squad slot lookup |
| Callee | `0x004e8a40` / `0x004e8ad0` | Basis extract |
| Sibling | `0x00859bc0` | Entity-rich overlay twin (other vtbl) |
| Vtbl | `0x00a68204` | Sole xref (DATA) |

## Open questions

1. Product / PDB class and method name.
2. Exact English for icon type enum (`0x65`, `'d'/'e'→1`, squad=1).
3. Full `MapConfig` layout beyond cited offsets.
4. Runtime / bit-exact image diff — open.
5. Nested dual of `FUN_008513d0` / `FUN_00757890` (out of OWN scope).
