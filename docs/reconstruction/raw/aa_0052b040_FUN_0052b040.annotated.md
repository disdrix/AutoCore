# Annotated low-level: FUN_0052b040 → DisciplineDefMap_TryCopyPayloadById_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0052b040` |
| VA | `0x0052b040`–`0x0052b078` |
| System | `skills-abilities` |
| Date | 2026-08-05 (R12-020 dual re-annotate) |
| Proposed name | `DisciplineDefMap_TryCopyPayloadById_Inferred` |

---

## Machine-level notes

- **ABI:** `__cdecl` — stack `key` then `out*`; bare `RET`; callers `ADD ESP,8`.
- **Return:** `AL` bool — `0` miss (node == `DAT_00b045b4`), `1` hit after copy.
- **Map:** process-global header `0x00b045b0`; end/head sentinel `DAT_00b045b4` (= `*(map+4)`).
- **Find:** `FUN_00538ab0` thiscall exact-find — nil `@node+0x20d`, int key `@node+0x0C`, `RET 8`.
- **Copy:** `REP MOVSD` **0x7f** dwords from `node+0x10` → caller buffer (**0x1FC** bytes). Parent allocates `operator_new(0x1fc)`.
- **Distinct from SkillDefMap** (`DAT_00b04734`, nil `+0x641`, payload ~`0x630`).
- Domain sealed by parent UI `"Discipline: "` + name at `out+0x2a` + pairing with `CVOGCharacter_GetResourceBalanceByType` (`0x0052ada0`).

## Pseudocode (annotated)

```c
// bool __cdecl DisciplineDefMap_TryCopyPayloadById_Inferred(int disciplineId, void *outPayload0x1FC)
int FUN_0052b040(undefined4 param_1 /* key */, undefined4 *param_2 /* out 0x1FC */)
{
  int *piVar2;
  int iVar1;
  undefined1 local_4[4]; // out iterator from find

  // ECX = &DAT_00b045b0 (hardcoded); exact-find by int key
  piVar2 = (int *)FUN_00538ab0(local_4, &param_1);
  iVar1 = *piVar2; // node*
  if (iVar1 == DAT_00b045b4) {
    return 0; // AL=0 miss (decompiler noise on high bytes)
  }
  // memcpy(out, node+0x10, 0x1FC) via REP MOVSD ECX=0x7f
  {
    undefined4 *src = (undefined4 *)(iVar1 + 0x10);
    undefined4 *dst = param_2;
    for (int n = 0x7f; n != 0; --n) {
      *dst++ = *src++;
    }
  }
  return 1; // AL=1 hit
}
```

## Layout (node family via FUN_00538ab0)

| Off | Role |
|----:|------|
| `+0x00` | left |
| `+0x04` | parent |
| `+0x08` | right |
| `+0x0C` | int key (discipline id) |
| `+0x10` | value payload start (copy source) |
| `+0x10 + 0x1FC` | end of copied region |
| `+0x20D` | isnil byte |

### Known payload consumer offsets (evidence only; not full schema)

| Out off | Use |
|--------:|-----|
| `+0x2A` | wide/mbcs name source for UI `"Discipline: "` (`FUN_00403450`) |

## Open questions

- Product/PDB class/method English (keep `_Inferred`).
- Full 0x1FC field schema beyond name `@+0x2A`.
- Runtime Confirmed / bit-exact / differential (terminal false).
