# Raw capture: FUN_0040c7c0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0040c7c0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0040c7c0` |
| **Canonical name** | `FUN_0040c7c0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void * __thiscall FUN_0040c7c0(void *param_1,void *param_2)

{
  size_t _Size;
  int in_EAX;
  void *pvVar1;
  
  _Size = (in_EAX - (int)param_1 >> 2) * 4;
  pvVar1 = memmove(param_2,param_1,_Size);
  return (void *)((int)pvVar1 + _Size);
}
```

---

## W22-T live re-verify append (2026-07-29)

**Tools:** `decompile_function` @ `0x0040c7c0` + `read_memory` 64 B + `get_function_by_address` + callers/callees. No `disassemble_bytes`. No Launcher. **Scaffold raw body above preserved.**

### Function bounds

| Field | Value |
|---|---|
| Entry | `0x0040c7c0` |
| Body | `0x0040c7c0`–`0x0040c7e2` (**35 bytes** through `ret 4`) |
| Ghidra name | `FUN_0040c7c0` |
| Callees | `memmove` (IAT via `0x009c652c`) |

### Bytes (`read_memory` @ `0x0040c7c0`, body through `c2 04 00`)

```
2b c1 56 c1 f8 02 8d 34 85 00 00 00 00 8b 44 24 08
56 51 50 ff 15 2c 65 9c 00 83 c4 0c 03 c6 5e c2 04 00
```

Hex continuous:

```
2bc156c1f8028d3485000000008b442408565150ff152c659c0083c40c03c65ec20400
```

Post-body: `cc` pad then unrelated `8a 44 24 04 ... c2 04 00`.

### Byte-decoded control flow (no disassemble_bytes tool)

| Addr | Bytes | Meaning |
|---|---|---|
| `0040c7c0` | `2b c1` | `sub eax, ecx` — span = srcEnd − srcBegin |
| `0040c7c2` | `56` | `push esi` |
| `0040c7c3` | `c1 f8 02` | `sar eax, 2` — dword count |
| `0040c7c6` | `8d 34 85 00 00 00 00` | `lea esi, [eax*4]` — size bytes |
| `0040c7cd` | `8b 44 24 08` | `mov eax, [esp+8]` — dest (after push esi) |
| `0040c7d1` | `56` | `push esi` — size |
| `0040c7d2` | `51` | `push ecx` — src |
| `0040c7d3` | `50` | `push eax` — dest |
| `0040c7d4` | `ff 15 2c 65 9c 00` | `call [0x009c652c]` — memmove IAT |
| `0040c7da` | `83 c4 0c` | `add esp, 0xC` |
| `0040c7dd` | `03 c6` | `add eax, esi` — return dest+size |
| `0040c7df` | `5e` | `pop esi` |
| `0040c7e0` | `c2 04 00` | `ret 4` — clean one stack arg |

### ABI seal (custom, not pure thiscall)

| Slot | Role |
|---|---|
| **ECX** | srcBegin |
| **EAX (in)** | srcEnd (exclusive) |
| **Stack [esp+4]** | dest |
| Epilogue | `ret 4` (callee cleans dest) |
| **EAX out** | dest + size |
| Size | `((srcEnd - srcBegin) >> 2) * 4` |

Decompiler tags `__thiscall` because ECX is first pointer — **misleading**: ECX is the range begin, not an object `this`. Sibling algorithm: `Mem_MoveDwordRange_ReturnEnd` @ `0x004e2e80` (all three args stack, `ret 0xC`).

### IAT

`read_memory` @ `0x009c652c` → dword `0x006ea9d0` (import slot / thunk target for CRT `memmove` family). Callee list: `memmove`.

### Callers (`get_function_callers` / xrefs)

| Caller | Sites |
|---|---|
| `FUN_00456960` | `00456ac3`, `00456b11` |
| `FUN_00419880` | `00419966`, `00419985`, `00419a0e`, `00419a78` |
| `FUN_0043a1d0` | `0043a376`, `0043a3cf` |
| `FUN_0043a090` | `0043a170`, `0043a1b2` |

Role: vector insert-N / grow **relocate** of dword ranges; pairs with fill helper `FUN_004651d0`.

### Live decompile ≡ scaffold raw body

**Confirmed** — same size math, memmove(dest, src, size), return dest+size.
