# Annotated low-level: StdTree_ConstructEmpty_Isnil19_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0040b3b0` |
| VA | `0x0040b3b0` |
| Canonical name | `StdTree_ConstructEmpty_Isnil19_Inferred` |
| Ghidra | `FUN_0040b3b0` |
| System | inventory-transfer (partition); structural role = MSVC `std::_Tree` empty construct |
| Date | 2026-08-05 (R13-017 OWN dual) |
| Body | `0x0040b3b0`–`0x0040b40a` exclusive (**90 B** / `0x5A`) |
| ABI | `__stdcall` shell*; **`ret 4`**; EAX = shell* |

## Machine-level notes

- Source: raw capture + R13-017 re-verify (`decompile` + `disassemble_function` + `read_memory` + callers/xrefs).
- Prefer assembly when decompiler conflicts (Ghidra listed `undefined …(void)` — **false**).
- **Not** thiscall: `MOV ESI,[ESP+0x14]` loads stack shell*.
- Sole callee `FUN_0040bfc0` = residual buyhead (`operator_new(0x1c)`, color@+0x18=1, isnil@+0x19=0) — dual of buyhead **not** this OWN.
- This unit: install head, force isnil=1, self-link L/P/R, size=0, return shell.
- Sole caller: parent `FUN_0092b2a0` @ `0x0092b319` (`LEA EAX,[ESP+0x38]; PUSH EAX; CALL`) — stack-local temp map/set for MissionTracker secondary fill.
- Family: **isnil@+0x19 / node 0x1c** (Val12 plate). Do **not** merge with isnil21/29/2d/31 ConstructEmpty or BuyHead peers.

## Pseudocode (annotated)

```c
// __stdcall RET 4; EAX = shell*
TreeShellIsnil19* FUN_0040b3b0(TreeShellIsnil19* shell /* stack */)
{
  // SEH: LAB_009bda98 / FS:[0]
  NodeIsnil19_0x1c* head = FUN_0040bfc0(); // residual BuyHeadNode isnil19 / 0x1c
  shell->myhead = head;                     // +0x04
  head->isnil = 1;                          // +0x19
  head->parent = head;                      // +0x04
  head->left   = head;                      // +0x00
  head->right  = head;                      // +0x08
  shell->mysize = 0;                        // +0x08
  return shell;
}
```

## Open questions

- Product key/value English for the temp tree under MissionTracker secondary fill.
- Dual of residual buyhead `FUN_0040bfc0` (queued elsewhere if at all).
- Runtime / bit-exact / differential (deferred; never Runtime Confirmed here).
