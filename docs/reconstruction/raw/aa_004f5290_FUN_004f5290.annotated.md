# Annotated low-level: Vehicle_CanFireAnyPrimaryHeatOK_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004f5290` |
| VA | `0x004f5290` |
| System | combat / vehicle weapon heat gate |
| Date | 2026-07-29 |
| Dual | `reviews/A_aa_004f5290_Vehicle_CanFireAnyPrimaryHeatOK_Inferred.md` / `B_…` |

## Machine-level notes

- Source: raw capture for `aa_004f5290` + 2026-07-29 re-decompile + `read_memory` prolog.
- Prefer assembly when decompiler conflicts.
- **Sealed:** 3-slot hardpoint table `*(vehicle+0x260)`, group filter weapon vtbl **+0x38**, heat via `Weapon_CanFireHeatCheck`, quantifier **∃** (first heat-OK → 1).
- Twin: secondary `FUN_004f52e0` uses vtbl **+0x3C** and **∀** heat-OK.
- Caller: only `Input_TryFirePrimaryWeapons_Inferred` (`0x00922270`).

## Pseudocode (annotated)

```c
/* Vehicle_CanFireAnyPrimaryHeatOK_Inferred
 *
 * thiscall: ECX = vehicle
 * Walk *(vehicle+0x260) weapon*[3]:
 *   if weapon && weapon.vtbl[+0x38]() && Weapon_CanFireHeatCheck(weapon)
 *     return 1;   // existential primary heat OK
 * return 0;
 */

undefined4 __fastcall Vehicle_CanFireAnyPrimaryHeatOK_Inferred(int vehicle /* ECX */)
{
  int *pWeapon;
  char isPrimaryGroup;
  bool heatOk;
  int slotByte;

  slotByte = 0;
  do {
    pWeapon = *(int **)(slotByte + *(int *)(vehicle + 0x260));
    if (pWeapon != (int *)0x0) {
      isPrimaryGroup = (**(code **)(*pWeapon + 0x38))();  /* thiscall weapon */
      if (isPrimaryGroup != '\0') {
        heatOk = Weapon_CanFireHeatCheck(pWeapon);
        if (heatOk) {
          return 1;
        }
      }
    }
    slotByte = slotByte + 4;
  } while (slotByte < 0xc);
  return 0;
}
```

## Asm anchors

| Form | Meaning |
|---|---|
| `mov ebx, ecx` | vehicle this |
| `[ebx+0x260]` | hardpoint table base pointer |
| `call [vtbl+0x38]` | primary-group predicate |
| `call Weapon_CanFireHeatCheck` | vehicle heat &lt; max |
| `i += 4; i < 0xC` | slots 0..2 |
| early `al=1` / fallthrough `al=0` | ∃ quantifier |

## Open questions

- Product names for vtbl+0x38 / +0x3C (IsPrimary/IsSecondary_Inferred).
- Turret `+0x264` membership in table vs out-of-band only.
