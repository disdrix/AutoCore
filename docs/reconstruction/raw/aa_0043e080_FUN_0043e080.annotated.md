# Annotated low-level: GuardedVector_InsertN_Thiscall (`FUN_0043e080`)

| Field | Value |
|---|---|
| Stable ID | `aa_0043e080` |
| VA | `0x0043e080`–`0x0043e37d` (**766 B**) |
| System | STL / guarded ring-deque container |
| Date | 2026-07-29 (W32-F seal) |
| Ghidra | `FUN_0043e080` |
| Name | `GuardedVector_InsertN_Thiscall` (**Inferred**) |

## Machine-level notes

- **ABI:** **ECX=container** (`MOV ESI,ECX`); stack `(seed, insert_at, N, fill*)`; **`RET 0x10`**.
- Twin of W29-A `GuardedVector_InsertN` (`0x0043cb00`) which uses **ECX=N** + EAX push polarity.
- This unit uses sealed W31-I thiscall push pair: `0043d670` / `0043e3d0`.
- Layout: `+0x04` pages, `+0x08` capacity, `+0x0c` begin, `+0x10` size.
- SEH frame (`6A FF` / `LAB_009bd5f0`); cleanup islands pop/throw in bytes.

## Control flow (success)

```
prefix = insert_at - begin
suffix = size - prefix
if prefix < suffix:          // front-biased
  if prefix < N:
    push_front(fill) × (N-prefix)
    push_front(mapped old-front) × prefix
    FUN_0043e910 fill window; return
  else:
    push_front(mapped) × N
    FUN_0043ec50 ring-move
    fallthrough FUN_0043e910
else:                        // back-biased
  if suffix < N:
    push_back(fill) × (N-suffix)
    push_back(mapped old-back) × suffix
    FUN_0043e910 fill window; return
  else:
    push_back(mapped) × N
    FUN_0043ebb0 ring-move
    fallthrough FUN_0043e910
```

Sole caller `FUN_0043d940` (resize grow) always inserts at **end** → back-biased `suffix < N` arm.

## Pseudocode shape (from raw + bytes)

```c
// ECX=container; stack seed, insert_at, N, fill*; RET 0x10
void __thiscall GuardedVector_InsertN_Thiscall(
    GuardedVectorHeader* self /*ECX*/,
    GuardedVectorHeader* /*seed*/,
    int32_t insert_at,
    uint32_t n,
    const uint32_t* fill);
```

## Call graph

| Dir | Targets |
|-----|---------|
| Callers (1) | `FUN_0043d940` (resize grow) |
| Callees | `GuardedVector_PushBack_Thiscall` (`0043d670`), `GuardedVector_PushFront_Thiscall` (`0043e3d0`), `FUN_0043e910`, `FUN_0043ebb0`, `FUN_0043ec50` |

## Open residual

- Product/PDB class English.
- Nested `0043e910` / `0043ebb0` / `0043ec50` free (unowned).
- Front-biased arms not call-site exercised by sole known caller.
- Runtime / bit-exact.
