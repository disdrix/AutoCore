# Annotated low-level: Vehicle_ActivateHardpointWeapons_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004f4f00` |
| VA | `0x004f4f00`–`0x004f4faa` (171 B) |
| Ghidra | `FUN_004f4f00` |
| Named (INFERRED) | `Vehicle_ActivateHardpointWeapons_Inferred` |
| System | combat / vehicle hardpoint weapons |
| Date | 2026-07-29 (W25-E dual seal) |
| Twin | `Vehicle_DeactivateHardpointWeapons_Inferred` @ `0x004f4eb0` |

## Machine-level notes

- Source: live `decompile_function` + full-body `read_memory` (no `disassemble_bytes`).
- ABI: thiscall/fastcall **ECX = vehicle***; **0 stack formals**; bare `ret` after local cleanup.
- Table: `*(vehicle+0x260)` — three weapon hardpoint pointers (stride 4, bound 0xC).
- Per slot: clear fire-flag pair (`Weapon_SetFireFlagPair_Inferred` / `FUN_0056a260(0)`), then activate:
  - if `weapon+0x08 == 0`: full `vtbl[0](1, zeros…, g_flOne, 0)`
  - else: `vtbl+0x18(1)`
- Does **not** clear suppress (`0056b400`) — that is deactivate twin only.
- Sole caller: vehicle switch `Character_SwitchVehicle_Inferred` (`0x005252f0`) after cargo/phys bind.

## Pseudocode (annotated)

```c
// thiscall vehicle*
void Vehicle_ActivateHardpointWeapons_Inferred(Vehicle *vehicle /*ECX*/)
{
  // table = *(vehicle + 0x260)  // WeaponHardpoint*[3]
  for (int off = 0; off < 0x0C; off += 4) {
    WeaponHardpoint *w = *(WeaponHardpoint **)((char *)table + off);
    if (w == NULL) continue;

    Weapon_SetFireFlagPair_Inferred(w, 0);   // FUN_0056a260(0)

    if (w->modeOrHost /* +0x08 */ == 0) {
      // full activate path: vtbl[0]
      (*w->vtbl)(w, /*active*/1, 0,0,0, 0,0,0, g_flOne /*1.0f*/, 0);
    } else {
      // short activate: vtbl+0x18(1)
      w->vtbl[0x18/4](w, 1);
    }
  }
}
```

## Sealed residuals

| Claim | Confidence |
|---|---|
| 3-slot walk at +0x260 | **High** |
| Fire clear then activate(1) | **High** |
| Twin of deactivate `004f4eb0` | **High** |
| Product English for vtbl+0x18 / vtbl[0] | **Medium** |
| Runtime / bit-exact | **Open** |

## Open questions

1. Product/PDB symbol for activate helper and hardpoint class.
2. Exact semantic of `weapon+0x08` gate (host-bound vs free).
3. Whether call-site `ECX=*(char+0x250)` is old or newly rebound vehicle at that instruction (pointer may update mid-switch).
