# Annotated low-level: CVOGPhysicsUtils_AreaQueryFilterBitTest_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_006c6c50` |
| VA | `0x006c6c50`–`0x006c6c98` inclusive (**73 B** / `0x49`) |
| Ghidra name | `FUN_006c6c50` |
| Canonical name | `CVOGPhysicsUtils_AreaQueryFilterBitTest_Inferred` |
| System | interaction-activation / CVOGPhysicsUtils area-query filter |
| Date | 2026-08-05 (MEGA-039 dual seal) |
| Prior scaffold | `Named_CalleeOf_Named_VOG_DEBUG_STOP_006c6c50` (**retired**) |

## Machine-level notes

- Source: raw capture `aa_006c6c50` + MEGA-039 live re-verify (decompile ≡ bytes).
- Prefer assembly when decompiler conflicts — here decompile CF matches body bytes.
- **ABI sealed:** `__thiscall` ECX=this; stack `out*`, `a`, `b`; **`RET 0xC`**.
- Leaf: no callees. Single caller: dualed `CVOGPhysicsUtils_GetObjectsInArea` `0x004ea350` @ `0x004ea560`.
- Result via out-byte; EAX left as `out*` (parent `CMP BYTE PTR [EAX],0`).

## Semantics (sealed CF)

```text
// this: filter bit-matrix owner (caller EBX)
// out:  uint8_t* result (1 = pass filter, 0 = reject)
// a:    object type/category from hit object +0x1c
// b:    query filter flags from GetObjectsInArea last arg (param_6)

if (((a ^ b) & 0xFFFF0000) == 0) && ((a & 0xFFFF0000) != 0):
    *out = 0                    // same non-zero high-16 → hard reject
    return
row  = a & 0x1F                 // 0..31 row into dword table
bit  = b & 0x1F                 // 0..31 bit within dword
dword = *(uint32*)(this + 0x1C + row*4)
*out = (dword & (1u << bit)) != 0
```

### Layout (this)

| Offset | Role |
|---|---|
| `+0x1C` | Start of **32× uint32** bit-row table (`row = a & 0x1F`) |
| each dword | 32 filter bits; tested by `b & 0x1F` |

### High-16 early-out

When the upper 16 bits of `a` and `b` match **and** are non-zero, the function forces `*out = 0` without reading the matrix. Product English for this packing/class rule is **open** (documented as CF fact only).

## Pseudocode (annotated)

```c
// __thiscall: ECX = this; stack out*, a, b; RET 0xC
void __thiscall CVOGPhysicsUtils_AreaQueryFilterBitTest_Inferred(
    void *this_filter_matrix,   // param_1 / ECX
    uint8_t *out,               // param_2
    uint32_t object_type,       // param_3  (hit +0x1c)
    uint32_t filter_flags)      // param_4  (GetObjectsInArea param_6)
{
  if ((((object_type ^ filter_flags) & 0xffff0000) == 0) &&
      ((object_type & 0xffff0000) != 0)) {
    *out = 0;
    return;
  }
  *out = (*(uint32_t *)((char *)this_filter_matrix + 0x1c +
                        (object_type & 0x1f) * 4) &
          (1u << (filter_flags & 0x1f))) != 0;
}
```

## Call graph

```text
CVOGPhysicsUtils_GetObjectsInArea  (0x004ea350)  [dualed]
  └─ default AABB hit loop @ 0x004ea560
       CALL CVOGPhysicsUtils_AreaQueryFilterBitTest_Inferred  (0x006c6c50)  [OWN MEGA-039]
       CMP  [out], 0 → skip hit if false
       else resolve object (FUN_0040afb0) + append
```

Gather consumer flags (parent dual evidence, not re-dualled):

| gatherMode | filterFlags (b) |
|---:|---:|
| default | `1` |
| 1 or 2 | `0x11` |
| 7 | `0x0a` |

## Naming

| Candidate | Decision |
|---|---|
| `Named_CalleeOf_Named_VOG_DEBUG_STOP_*` | **Reject** — `VOG_DEBUG_STOP` is parent fail path string, not this leaf |
| `CVOGPhysicsUtils_AreaQueryFilterBitTest_Inferred` | **Accept** — sole caller is dualed GetObjectsInArea; role is filter bit test; product bit English open → `_Inferred` |

## Open questions

- Product name of the bit-matrix object (EBX this) and formal type.
- High-16 early-out product rule.
- Mapping of individual filter bits to gameplay categories.
- Runtime golden / bit-exact / differential.

## Confidence

| Claim | Level |
|---|---|
| CF / body size / RET 0xC | **Confirmed** |
| thiscall + 3 stack args | **Confirmed** |
| Bit test at `this+0x1c+(a&0x1f)*4` bit `(b&0x1f)` | **Confirmed** |
| Sole caller GetObjectsInArea @ `0x004ea560` | **Confirmed** |
| Area-query filter role | **High** (caller + dual parent) |
| Product English / bit meanings | **Inferred** / open |
