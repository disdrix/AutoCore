# Annotated low-level: FUN_004673b0 / StdVector_UninitializedCopy_Elem28_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004673b0` |
| VA | `0x004673b0`–`0x00467406` (**87 B** / `0x57`) |
| System | containers / std::vector uninitialized_copy (POD stride 0x1c) |
| Date | 2026-07-29 (W35-T seal; scaffold 2026-07-23) |
| Inferred name | `StdVector_UninitializedCopy_Elem28_Inferred` |

## Machine-level notes

- Source: raw capture + W35-T live re-verify (`decompile` + `read_memory` + xrefs).
- **ABI (bytes win):** **EAX=dest**, **ECX=first**, stack **last**; bare `RET`. Reject Ghidra `__thiscall` plate (ECX is range first, not vector host).
- Stride **0x1C**: 7 dword fieldwise assign per element; advance `+0x1C` on dest/first and tail cursors (`+0x18` LEA pairs).
- Early-out when `first == last`.
- Null dest: skip stores, still advance (compiler artifact; parent passes `operator_new` buffer).
- Leaf; sole caller `FUN_00466ea0` (W34-Q insert-n grow/hole relocate prefix/suffix).

## Pseudocode (annotated)

```c
// EAX = dest*, ECX = first*, stack last*; bare RET
// POD T size 0x1c — trivial uninitialized_copy [first, last) → dest
void StdVector_UninitializedCopy_Elem28(
    Pod28* dest /*EAX*/,
    Pod28* first /*ECX*/,
    Pod28* last /*stack*/)
{
  if (first == last) {
    return;
  }
  do {
    if (dest != nullptr) {
      dest->d0 = first->d0;
      dest->d1 = first->d1;
      dest->d2 = first->d2;
      dest->d3 = first->d3;
      dest->d4 = first->d4;
      dest->d5 = first->d5;
      dest->d6 = first->d6;
    }
    first += 1; // +0x1c
    dest  += 1;
  } while (first != last);
}
```

## Open questions

- Product/PDB T English (parent residual: anmAnimMaster-ish scaffold chain only).
- Runtime / bit-exact open.
- Null-dest path never exercised by sole known caller.
