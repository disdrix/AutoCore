# Annotated low-level: RunElem_WStringDtor_Plus0C_Trampoline_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00426f70` |
| VA | `0x00426f70`–`0x00426f78` inclusive (**9 B**) |
| Ghidra name | `FUN_00426f70` |
| Canonical name | `RunElem_WStringDtor_Plus0C_Trampoline_Inferred` |
| System | skills-abilities / UI text-layout run element (member dtor trampoline) |
| Date | 2026-08-05 (R10-026 live re-verify) |
| Parent dual | `0x004271c0` `StdVector_DestroyRuns_0x80_Inferred` |

## Machine-level notes

- Source: raw capture + R10-026 append for `aa_00426f70`.
- **Bytes authority** over decompiler call-shape: body is two instructions only.
- Prefer assembly when decompiler conflicts (jumptable / call vs tail-JMP).
- IAT `0x009c62ac` is named `PTR_~basic_string<wchar_t,...>` in Ghidra — sealed target class.

## Pseudocode (annotated)

```c
// ECX = RunElem0x80* (element base). No stack args. Void.
// Does NOT destroy the whole element — only the embedded wstring at +0x0C.
void __fastcall RunElem_WStringDtor_Plus0C_Trampoline_Inferred(void* elem /* ECX */)
{
    // ADD ECX, 0x0C
    // JMP DWORD PTR [IAT ~basic_string<wchar_t>]  @ 0x009c62ac
    // Equivalent portable form:
    std::wstring* w = (std::wstring*)((char*)elem + 0x0C);
    w->~basic_string(); // MSVC runtime via IAT
}
```

## Byte plate

```
83 C1 0C                ADD ECX, 0x0C
FF 25 AC 62 9C 00       JMP DWORD PTR [0x009C62AC]
```

Hex: `83c10cff25ac629c00`

## Call graph (this VA only)

**Callers (code):**
- `FUN_004271c0` / `StdVector_DestroyRuns_0x80_Inferred` — per-elem in stride-0x80 vector tidy
- `FUN_00427590` — destroy-range stride 0x80
- `FUN_005ac510` — tree/node free path (still ECX thiscall into trampoline)

**Callers (SEH):** four `Unwind@*` sites

**Callee:** IAT tail → `~basic_string<wchar_t>`

## Open questions

- Full product English for the 0x80-byte element beyond dualed "run" inference (layout+0x80 under UiTextLayout).
- Other fields of the 0x80 element (only +0x0C wstring sealed here).
- Runtime / bit-exact / differential: open.

## Rejected interpretations

- Full scalar deleting dtor / vtbl element dtor
- Char/`std::string` (narrow) dtor — IAT is **wchar_t** basic_string
- Freeing the element buffer (parent vector tidy owns `operator_delete`)
- Named_CalleeOf_* VOG_DEBUG scaffold product name
