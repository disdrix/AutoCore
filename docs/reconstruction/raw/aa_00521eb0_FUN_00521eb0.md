# Raw capture: FUN_00521eb0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00521eb0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00521eb0` |
| **Canonical name** | `FUN_00521eb0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
byte __fastcall FUN_00521eb0(int param_1)

{
  byte bVar1;
  
  bVar1 = *(byte *)(param_1 + 0x59b);
  if (bVar1 != 0) {
    switch(bVar1) {
    case 1:
    case 2:
      return 1;
    case 3:
    case 4:
      return 2;
    case 5:
    case 6:
      return 3;
    case 7:
    case 8:
      return 4;
    case 9:
      return 5;
    default:
      return -(9 < bVar1) & 5;
    }
  }
  return 0;
}
```

---

## MEGA-106 re-verify append (2026-08-05) — DO NOT WIPE RAW ABOVE

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-106** |
| **Tools** | Ghidra HTTP/`decompile_function` + `analyze_function_complete` + `disassemble_function` + `read_memory` + `get_function_callers` + `get_xrefs_to` + `get_function_by_address` + caller disasm. **No** `disassemble_bytes`. No Launcher. |
| **Image** | `autoassault.exe` base `0x400000` |
| **Live decompile ≡ raw CF** | **Yes** (byte-identical switch structure) |

### Body / size

| Item | Value |
|---|---|
| Entry | `0x00521eb0` |
| Last insn | `0x00521ef6` `RET` |
| Inclusive span | `0x00521eb0`–`0x00521ef6` (**71 B** / `0x47`) |
| Ghidra body | `00521eb0 - 00521ef6` |
| Pad | `0x90` NOP @ `0x00521ef7` |
| Jump table (data) | `0x00521ef8`–`0x00521f1b` (9× LE dword) then `CC` pad before `FUN_00521f20` |

### ABI (sealed)

| Slot | Value | Evidence |
|---|---|---|
| This / object | **ECX** | `MOV AL,[ECX+0x59b]` @ entry |
| Stack args | **none** | plain **`RET`** (`C3`) all exits |
| Return | **EAX** zero-ext byte tier 0..5 | `XOR EAX,EAX` / `MOV EAX,imm` / `SBB`+`AND` |
| Convention | **__thiscall** (single-arg this in ECX; Ghidra labels `__fastcall`) | call sites `MOV ECX,<item>; CALL` |

### Live disassembly (authoritative)

```text
00521eb0  MOV AL,byte ptr [ECX + 0x59b]
00521eb6  TEST AL,AL
00521eb8  JA  0x00521ebd          ; AL != 0
00521eba  XOR EAX,EAX
00521ebc  RET                     ; return 0
00521ebd  MOVZX ECX,AL
00521ec0  ADD ECX,-0x1            ; index = value - 1
00521ec3  CMP ECX,0x8
00521ec6  JA  0x00521eed          ; value > 9 → default
00521ec8  JMP dword ptr [ECX*4 + 0x521ef8]
00521ecf  MOV EAX,0x1 / RET       ; cases 1,2
00521ed5  MOV EAX,0x2 / RET       ; cases 3,4
00521edb  MOV EAX,0x3 / RET       ; cases 5,6
00521ee1  MOV EAX,0x4 / RET       ; cases 7,8
00521ee7  MOV EAX,0x5 / RET       ; case 9
00521eed  MOV CL,0x9
00521eef  CMP CL,AL
00521ef1  SBB EAX,EAX             ; -1 if AL > 9 else 0
00521ef3  AND EAX,0x5             ; 5 if AL > 9 else 0
00521ef6  RET
```

### Jump table @ `0x00521ef8` (`read_memory` hex `cf1e5200` ×2 …)

| index (value-1) | value | target | return |
|---|---|---|---|
| 0,1 | 1,2 | `0x00521ecf` | 1 |
| 2,3 | 3,4 | `0x00521ed5` | 2 |
| 4,5 | 5,6 | `0x00521edb` | 3 |
| 6,7 | 7,8 | `0x00521ee1` | 4 |
| 8 | 9 | `0x00521ee7` | 5 |
| default | ≥10 | `0x00521eed` | 5 |

Mapping: `0 → 0`; `v∈[1,9] → (v+1)/2` (integer); `v≥10 → 5`. Cap **5**.

### Field

| Offset | Use |
|---|---|
| **+0x59b** | `uint8` source byte on **ECX object** (item, not character — sealed by callers) |

### Callers / xrefs (live)

| Site | Function | Role |
|---|---|---|
| `0x00513ca4` | `FUN_00513c10` | UNCONDITIONAL_CALL — tinker kit validate |
| `0x00521f59` | `FUN_00521f20` | UNCONDITIONAL_CALL — tinker float helper |

**xref_count = 2**. Callees: **none** (leaf). Classification: **leaf**.

### Call-site ECX (sealed — item)

`FUN_00513c10` @ `0x00513c9f`–`0x00513ca4`:
```text
MOV ECX,EDI          ; EDI = stack param_2 (item; has +0x6b4)
ADD ESI,0x1          ; ESI = character vfunc(+0x8c) skill + 1
CALL 0x00521eb0
CMP EAX,ESI
```

`FUN_00521f20` @ `0x00521f54`–`0x00521f59`:
```text
MOV ECX,EDI          ; EDI = this item (entry MOV EDI,ECX; has +0x6b4 / +0x59b)
CALL 0x00521eb0
CMP EAX,ESI          ; ESI = character vfunc(+0x8c)+1
```

Partition hint `Character_GetTinkerSkillOrCap` **corrected**: ECX is the **item**; character skill comes from separate vfunc `+0x8c` in parents.

### analyze_function_complete notes

- signature display `undefined FUN_00521eb0(void)` is **wrong** — decompiler/params show ECX object; return byte in EAX.
- return_type_warning: verify EAX at RET — **done** (all exits set EAX).

### Name decision (MEGA-106)

`Item_GetTinkerSkillOrCap_Inferred` — map item `+0x59b` to tinker skill-tier/cap 0..5. `_Inferred`: product English for field open; no in-body strings. Retire scaffold `Named_CalleeOf_Named_CalleeOf_Skill_You_do_not_possess_the_Tink_00521eb0`.

### Integrity

Raw body above **unchanged**. This section is append-only re-verify.
