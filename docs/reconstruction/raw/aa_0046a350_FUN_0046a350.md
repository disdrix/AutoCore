# Raw capture: FUN_0046a350

| Field | Value |
|---|---|
| **Stable ID** | `aa_0046a350` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0046a350` |
| **Canonical name** | `FUN_0046a350` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0046a350(undefined4 param_1,undefined4 *param_2)

{
  undefined4 *in_EAX;
  int iVar1;
  undefined4 *unaff_EBX;
  undefined4 *puVar2;
  undefined4 *puVar3;
  
  for (; param_2 != unaff_EBX; param_2 = param_2 + 7) {
    if (in_EAX != (undefined4 *)0x0) {
      puVar2 = param_2;
      puVar3 = in_EAX;
      for (iVar1 = 7; iVar1 != 0; iVar1 = iVar1 + -1) {
        *puVar3 = *puVar2;
        puVar2 = puVar2 + 1;
        puVar3 = puVar3 + 1;
      }
    }
    in_EAX = in_EAX + 7;
  }
  return;
}
```

---

## W36-P re-verify append (2026-07-29)

**Tools:** `batch_decompile` + `analyze_function_complete` + `read_memory` + xrefs/assembly context. **No** `disassemble_bytes`.

### Body / classification

| Item | Value |
|---|---|
| Body | `0x0046a350`–`0x0046a371` inclusive (**34 B** / `0x22`); exclusive end `0x0046a372`; pad `CC`×14 then unrelated |
| Classification | **leaf** (0 callees) |
| Callers (1 func / 4 sites) | `FUN_00469f50` @ `0x0046a07f`, `0x0046a0b4`, `0x0046a16b`, `0x0046a1c8` (ValueEdx InsertN grow/in-place; W34-Q) |

### ABI (bytes win)

- **EAX = dest\*** (in) / **dest_end\*** (out) — live-out published by parents (`MOV [EDI+8],EAX` etc.)
- **EDX = src_begin\***
- **EBX = src_end\***
- bare **`RET`** (`C3`); saves/restores ESI/EDI
- Decompiler `param_1` is **phantom** (empty storage); `param_2` = EDX; `unaff_EBX` = end; `in_EAX` real formal

### Semantics

MSVC-style **uninitialized_copy** / trivial forward range copy for POD T size **0x1c**:

1. Early-out if `EDX == EBX` (`CMP EDX,EBX; JE`)
2. Loop: if `EAX != 0`, `MOV ECX,7; MOV ESI,EDX; MOV EDI,EAX; REP MOVSD` (7 dwords)
3. Always `ADD EDX,0x1C; ADD EAX,0x1C`; until `EDX == EBX`
4. Null dest skips stores but still advances (same family as Pod28/`004673b0` uninit copy)

### Full body hex (34 B)

```
3bd3741d565785c0740bb9070000008bf28bf8f3a583c21c83c01c3bd375e75f5ec3
```

### Name

`StdUninitCopy_Elem28_Inferred` — register-ABI peer of `StdVector_UninitializedCopy_Elem28_Inferred` (`0x004673b0`, stack last) and `StdUninitCopy_Pod28` (`0x0045f360`, EDI end). **Reject** long scaffold `Named_CalleeOf_…_0046a350`.

### Live decompile 2026-07-29

Matches scaffold raw CF (7-dword inner loop). **Bytes win** on three-register formals + `REP MOVSD` + bare RET + EAX live-out.
