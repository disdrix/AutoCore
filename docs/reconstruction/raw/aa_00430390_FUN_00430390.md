# Raw capture: FUN_00430390

| Field | Value |
|---|---|
| **Stable ID** | `aa_00430390` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00430390` |
| **Canonical name** | `FUN_00430390` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
int __thiscall FUN_00430390(undefined4 param_1,int param_2,int param_3,undefined4 param_4)

{
  FUN_00431700(param_2,param_3,param_4,param_1,param_4);
  return param_2 + param_3 * 0x1c;
}
```

---

## W33-G re-verify append (2026-07-29)

**Agent:** OWN-ONLY dual W33-G  
**Tools:** `decompile_function` / `batch_decompile` + `analyze_function_complete` + `read_memory` + xrefs. **No** `disassemble_bytes`.  
**Scaffold raw body above preserved.**

### Body / epilogue

| Field | Value |
|---|---|
| Range | `0x00430390`–`0x004303c0` exclusive |
| Size | **48 B** / `0x30` |
| Epilogue | `5F 5E C2 0C 00` (**`RET 0x0C`**) |
| Next | contiguous next function (no `CC` pad required between) |

### ABI (sealed from bytes)

| Formal | Storage | Role |
|---|---|---|
| `alloc` / host | **ECX** (`param_1`) | forwarded into fill helper (unused by fill loop body) |
| `dest` | stack arg1 → EDI | first unconstructed slot |
| `count` | stack arg2 → ESI | number of elements to fill |
| `value` | stack arg3 → EAX then stack | `const basic_string*` source for copy-ctor |
| return | EAX | `dest + count * 0x1C` |
| cleanup | `C2 0C 00` | callee pops **3** stack dwords (thiscall + 3 args) |

Call shape to `FUN_00431700`:
`FUN_00431700(dest, count, value, alloc, value)` — five pushes; helper’s live formals are dest/count/value (extra = allocator pattern).

### Semantics (sealed)

Thin wrapper: **`_Uninit_fill_n`** for `basic_string` stride **0x1C**.

1. Load value / count / dest; push `(value, alloc, value, count, dest)` order as bytes dictate → `CALL FUN_00431700` @ `0x00431700` (relative `E8 54 13 00 00`).
2. `ADD ESP, 0x14` (cdecl clean 5 args).
3. `EAX = dest + count*0x1C` via `lea edx,[count*8]; sub edx,count; lea eax,[dest+edx*4]` ≡ `count*28`.
4. `RET 0x0C`.

Nested `FUN_00431700` (not owned): SEH loop `count` times; if dest≠null, copy-ctor from `value` into dest; dest+=0x1C.

### Xrefs

| Kind | Sites |
|---|---|
| Callers | **only** `FUN_004306b0` (`StdVector_InsertN_BasicString`) @ `0x004307f9`, `0x00430910` |
| Callees | `FUN_00431700` |

### Full body hex (48 B)

```
8B 44 24 0C 56 8B 74 24 0C 57 8B 7C 24 0C 50 51
8B 4C 24 1C 51 56 57 E8 54 13 00 00 8D 14 F5 00
00 00 00 2B D6 83 C4 14 8D 04 97 5F 5E C2 0C 00
```

### Name

**`StdUninitFillN_BasicString`** (Ghidra `FUN_00430390`; structural **Inferred**). Reject scaffold `Named_CalleeOf_Named_*`.
