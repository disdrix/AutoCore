# Raw capture: FUN_00433c00

| Field | Value |
|---|---|
| **Stable ID** | `aa_00433c00` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00433c00` |
| **Canonical name** | `FUN_00433c00` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00433c00(int param_1)

{
  void *_Memory;
  uint in_EAX;
  void *pvVar1;
  int iVar2;
  
  if (*(uint *)(param_1 + 4) < in_EAX) {
    iVar2 = in_EAX + (0x10 - (in_EAX & 0xf));
    pvVar1 = malloc(iVar2 * 0xc);
    _Memory = *(void **)(param_1 + 8);
    *(void **)(param_1 + 8) = pvVar1;
    FUN_00433ca0();
    *(void **)(param_1 + 8) = _Memory;
    free(_Memory);
    *(void **)(param_1 + 8) = pvVar1;
    *(int *)(param_1 + 4) = iVar2;
  }
  return;
}
```
---

## Re-verify append — 2026-07-29 W26-B (OWN-ONLY dual)

| Field | Value |
|---|---|
| **Agent** | W26-B OWN-ONLY dual |
| **Tools** | `decompile_function` + `analyze_function_complete` + `read_memory` (no `disassemble_bytes`) |
| **Live decompile** | = raw 2026-07-23 body (CF identical; `in_EAX` = requested capacity) |
| **Body** | `0x00433c00`–`0x00433c53` exclusive (**83 B**); pad `CC` after last `RET` |
| **ABI** | **fastcall-style**: **ECX** = container base; **EAX** = requested min capacity (`in_EAX`); void; plain `RET` after callee-saved pops |
| **Container layout** | `+0` size (`DAT_00d1eab4`), `+4` capacity, `+8` data ptr (`DAT_00d1eabc`); **elem stride 0xC** |
| **Growth** | if `capacity < need`: `newCap = need + (0x10 - (need & 0xF))`; `malloc(newCap * 0xC)`; `FUN_00433ca0` copies live elems old?new; `free(old)`; commit ptr + capacity |
| **Callers (1)** | `FUN_0077d520` (`BitStream_HuffmanBuildTables`) only — call site `mov eax,0x100; mov ecx,0x00d1eab4` |
| **Callees** | `malloc`, `free`, `FUN_00433ca0` |
| **Name** | `BitStream_HuffmanAlphabetEnsureCapacity` (Ghidra `FUN_00433c00`) |
| **Verdict** | dual A/B **accept** |