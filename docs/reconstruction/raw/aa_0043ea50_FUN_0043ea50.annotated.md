# Annotated low-level: PodU32U8_StoreSplit (`FUN_0043ea50`)

| Field | Value |
|---|---|
| Stable ID | `aa_0043ea50` |
| VA | `0x0043ea50`–`0x0043ea5f` (**16 B**) |
| System | POD store helper (split-source u32+u8) |
| Date | 2026-07-29 (W32-M dual) |

## Machine-level notes

- **Bytes win** on ABI: **EAX=dst**, **ECX=`const uint32_t*`**, stack **`const uint8_t*`**, **`RET 0x4`**.
- Decompiler labels `__thiscall` (ECX formal) but **omits EAX dest** as an explicit parameter — treat dest as register argument.
- **No** null check on dest (contrast `0043ece0` / `0043ecf0`).
- Sources are **split pointers**, not one contiguous `PodU32U8*`. Contiguous null-safe twin is `FUN_0043ecf0`.
- Sole direct caller `FUN_0043e8f0` is a thin ESI/stack adapter (not owned).

## Pseudocode (annotated)

```c
// EAX = dest; ECX = const uint32_t* pU32; stack = const uint8_t* pU8; RET 4
void PodU32U8_StoreSplit(void* dest /*EAX*/,
                         const uint32_t* pU32 /*ECX*/,
                         const uint8_t* pU8 /*stack*/)
{
  *(uint32_t*)dest = *pU32;                 // [EAX] = [ECX]
  *((uint8_t*)dest + 4) = *pU8;             // [EAX+4] = *pU8
  // trailing 3 bytes of 8-byte slot untouched
}
```

## Byte map

```
8B 11           MOV  EDX, [ECX]
8B 4C 24 04     MOV  ECX, [ESP+4]
89 10           MOV  [EAX], EDX
8A 11           MOV  DL, [ECX]
88 50 04        MOV  [EAX+4], DL
C2 04 00        RET  4
```

## Call graph

| Dir | Target |
|---|---|
| Caller | `FUN_0043e8f0` @ call `0x0043e8fb` (adapter; parents include `NDResourceCache_LookupOrCreate`, `FUN_0075de80`, ass/preloader rings) |
| Callees | none (leaf) |

## Open residual

- Product/PDB English; element field names for the u32/u8 pair.
- Parent adapter `0043e8f0` dual free.
- Runtime / bit-exact.
