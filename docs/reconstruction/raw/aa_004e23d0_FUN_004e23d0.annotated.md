# Annotated low-level: FUN_004e23d0 / List_FindByDualKeyWithPred_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004e23d0` |
| VA | `0x004e23d0` |
| Body span | `004e23d0` – `004e240c` |
| System | shared container / object-id map (reaction, client resolve) |
| Date | 2026-07-29 (OWN-ONLY dual A/B seal) |
| Canonical (structural) | `List_FindByDualKeyWithPred_Inferred` |
| Ghidra | `FUN_004e23d0` |
| Parent-seed alias | `Named_CalleeOf_CVOGReaction_SpawnObject_004e23d0` (not exclusive) |

---

## Machine-level notes

- **Convention:** `__thiscall` — `this` in ECX; three stack args; `ret 0xc`.
- **Leaf:** no calls (`call_count=0`).
- **Return:** decompiler prints `void`; **image leaves node* in EAX** (match or null). Prefer `int` return of walk cursor.
- **Out-param:** `*param_4` = predecessor node (0 if match is head / empty). Updated only on **mismatch** steps (classic list-find-with-pred).
- Prefer this plate + entry bytes when decompiler omits EAX return.

### Node / container layout (from this body only)

| Offset | Role |
|---|---|
| `this+0x4` | list head pointer |
| `node+0x10` | next |
| `node+0x18` | key dword 0 (lo) |
| `node+0x1c` | key dword 1 (hi) |

Callers often read `node+0xc` as payload after a hit (e.g. `FUN_004e3260`) — not accessed here.

### Key semantics (caller-proven, not strings in this leaf)

Multiple sites pass **CoidLo/CoidHi** / TFID halves (`FUN_0092ff00`, reaction spawn/remove). Structural name uses **DualKey**; product “Coid map” is **Probable**.

---

## Pseudocode (annotated; CF ≡ raw + EAX return seal)

```c
/* List_FindByDualKeyWithPred_Inferred
   Walk singly-linked list at *(this+4); match node+0x18/0x1c to (keyLo,keyHi).
   *outPred = previous node (0 if none). Return matched node or 0. */
int __thiscall FUN_004e23d0(int this, int keyLo, int keyHi, int *outPred)
{
  int node;

  node = *(int *)(this + 4);
  *outPred = 0;
  for (; (node != 0 &&
          ((keyLo != *(int *)(node + 0x18) || (keyHi != *(int *)(node + 0x1c)))));
       node = *(int *)(node + 0x10)) {
    *outPred = node;   /* only when current is NOT a match */
  }
  return node;         /* match or NULL — asm EAX; decompiler said void */
}
```

---

## Open questions

1. Official product / STL-container English name (none in image for this VA).
2. Full English inventory of all 27 call sites beyond reaction/client resolve.
3. Payload type at `node+0xc` (varies by container instance?).
4. Runtime CE walk on a known Coid insert/remove pair.
