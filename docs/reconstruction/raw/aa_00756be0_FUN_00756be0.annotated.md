# Annotated low-level: FUN_00756be0 → gfxUIWindow_DetachParent_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00756be0` |
| VA | `0x00756be0`–`0x00756c44` inclusive (**101 B** / `0x65`) |
| System | skills-abilities / gfxUIWindow child list |
| Date | 2026-08-05 (MEGA-121 dual refresh) |
| Canonical | `gfxUIWindow_DetachParent_Inferred` |
| Ghidra | `FUN_00756be0` |

## Machine-level notes

- Source: live Ghidra `decompile_function` + `disassemble_function` + `read_memory` + dualed call-site `get_assembly_context`.
- Prefer assembly when decompiler conflicts — here decompile **≡** body bytes.
- **ABI:** `__thiscall` ECX=parent, stack=child*, **`RET 4`**, EAX status.
- **Not** a dtor and **not** a free: only erases one child* from parent vector and clears `child+0x88`.

## Offset map

| Object | Offset | Meaning |
|---|---|---|
| parent (ECX) | `+0x94` | child pointer vector begin |
| parent (ECX) | `+0x98` | child pointer vector end (exclusive) |
| child (stack) | `+0x88` | parent back-pointer (zeroed on successful detach) |

Vector element size = **4** (pointer). Search is linear equality on the pointer value.

## Pseudocode (annotated)

```c
// __thiscall: ECX = parent; stack = child*; RET 4
// return 0 = detached; 0xffffffff = child not in parent vector
undefined4 __thiscall FUN_00756be0(int parent, int child)
{
  int *end   = *(int **)(parent + 0x98);
  int *it    = *(int **)(parent + 0x94);

  if (it != end) {
    do {
      if (*it == child) break;   // found pointer slot
      it = it + 1;               // +4 bytes
    } while (it != end);

    if (it != end) {
      // compact: shift [it+1, end) down over it
      memmove(it, it + 1, (*(int *)(parent + 0x98) - (int)(it + 1) >> 2) * 4);
      *(int *)(parent + 0x98) = *(int *)(parent + 0x98) - 4;  // end--
      *(undefined4 *)(child + 0x88) = 0;                      // clear parent link
      return 0;
    }
  }
  return 0xffffffff;
}
```

## Call-site patterns (sealed)

1. **Child dtor detach** (`gfxUIWindow_CompleteDtor` @ `0x00759f3a`):
   - if `child+0x88 != 0`: `ECX = [child+0x88]` (parent); `PUSH child`; CALL.
2. **Parent erases child** (`CNDUIDialog_TeardownChildHash` @ `0x007918a5`):
   - `ECX = dialog`; `PUSH child`; CALL (after child release / clear `child+0x2b0`).
3. **Reparent / attach peer** (`FUN_00756c50` @ `0x00756c63`):
   - detaches from old parent via same helper, then writes new `+0x88`.

## Open questions

- Exact product method demangle (use `_Inferred`).
- Whether capacity lives at `+0x9c` (untouched here; not required for erase-by-memmove).
- Runtime golden path / bit-exact.
