# Annotated low-level: PodCopyConstruct_Elem0x138_Inferred (FUN_00408880)

| Field | Value |
|---|---|
| Stable ID | `aa_00408880` |
| VA | `0x00408880`–`0x004088ef` (**112 B** / `0x70`) |
| System | util / POD construct (Logic-UI event element width **0x138**) |
| Date | 2026-08-04 (WQ9H-I dual seal; scaffold 2026-07-23) |
| Dual name | `PodCopyConstruct_Elem0x138_Inferred` |

## Machine-level notes

- Source: raw capture + live Ghidra re-verify (decompile ≡ raw CF; bytes seal ABI).
- **Leaf** — no callees.
- **ABI:** `__thiscall`; **ECX = dest**; stack **`const void* src`**; **void**; **`RET 4`**.
- **Width sealed:** 14 dword stores (0x00–0x37) + `rep movsd` of **0x40** dwords from **+0x38** = **0x138** bytes total.
- Not a tree/freelist/CNDHash op despite VA neighborhood of empty buynode `0x004088f0`.
- Callers:
  - `LogicUiEventQueue_InsertN` (`0x004076a0`) — stack buffer construct before insert-N.
  - `FUN_0040a6e0` — placement POD copy when dest non-null (undualed residual).

## Signature (sealed)

```c
// __thiscall; RET 4
void __thiscall PodCopyConstruct_Elem0x138_Inferred(
    void* dest,          // ECX
    const void* src);    // Stack[0x4]
```

## Pseudocode (annotated)

```c
void __thiscall PodCopyConstruct_Elem0x138_Inferred(uint32_t* dest, const uint32_t* src)
{
  // Unrolled dword copy indices 0..13  (0x38 bytes)
  for (int i = 0; i < 14; ++i)
    dest[i] = src[i];

  // rep movsd: 0x40 dwords from +0x38
  // total = 0x38 + 0x100 = 0x138
  const uint32_t* s = src + 14;
  uint32_t* d = dest + 14;
  for (int n = 0x40; n != 0; --n) {
    *d++ = *s++;
  }
}
```

## Open residual

- Product / English struct for the 0x138 Logic-UI event record fields.
- Full dual of placement wrapper `FUN_0040a6e0`.
- Runtime / bit-exact / differential.
