# Annotated low-level: Map_TreeInsertAndRebalance_Val12 (`FUN_00567450`)

| Field | Value |
|---|---|
| Stable ID | `aa_00567450` |
| VA | `0x00567450` |
| Canonical | `Map_TreeInsertAndRebalance_Val12` (**Inferred**; clone of `aa_0051c1c0`) |
| System | std map/set red-black tree insert+rebalance (Val12) |
| Date | 2026-07-29 (W27-R; supersedes 2026-07-23 scaffold annotate) |

## Machine-level notes

- **Body:** `0x00567450`–`0x0056763a` exclusive (**490** B / `0x1EA`) — same length as `aa_0051c1c0`.
- **ABI:** `__thiscall` ECX=map*; four stack formals; **`ret 0x10`**.
- **Max size:** `0x15555553` (not SkillElementTypeMap's `0x1ffffffd`).
- **Buynode:** `FUN_005ae220` / `StdTree_Buynode_Val12` with **color=0 (red)**.
- **Throw:** `"map/set<T> too long"` → `DAT_00acc388` / length_error vtbl `PTR_FUN_009c7634`.
- **Always-insert:** no key compare here; parent `FUN_00567860` walks int key (`node[3]` / +0x0C).
- Entry: `mov edi,ecx` then size compare — thiscall confirmed.

## Pseudocode (annotated)

```c
// __thiscall; RET 0x10
void Map_TreeInsertAndRebalance_Val12(
    MapShell* map, Node** outIt, char addLeft, Node* where, const Val12* value)
{
  // SEH LAB_009a4c12
  if (map->size > 0x15555553u) {
    // length_error "map/set<T> too long"; _CxxThrowException(..., DAT_00acc388)
  }
  Node* n = StdTree_Buynode_Val12(map->head, where, map->head, value, /*color*/0);
  map->size++;
  if (where == map->head) {
    map->head->parent = map->head->left = map->head->right = n;
  } else if (addLeft == 0) {
    where->right = n;
    if (where == map->head->right) map->head->right = n;
  } else {
    where->left = n;
    if (where == map->head->left) map->head->left = n;
  }
  // while parent red: uncle recolor OR rotate (L/R Val12); one L-rotate inlined
  map->head->parent->color = 1; // root black
  *outIt = n;
  // ret 0x10
}
```

## Twin / do-not-merge

| VA | Sole caller | Notes |
|---|---|---|
| `0x0051c1c0` | Skill category-cooldown insert | W25-G sealed |
| `0x00567450` | `FUN_00567860` int-key insert-or-find | this dual |

Same structural role + buynode family; separate instantiations.
