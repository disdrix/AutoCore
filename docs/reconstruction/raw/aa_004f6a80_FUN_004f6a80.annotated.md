# Annotated low-level: Inventory_ValidateEquipmentChange_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004f6a80` |
| VA | `0x004f6a80` |
| Ghidra | `FUN_004f6a80` |
| Canonical (inferred) | `Inventory_ValidateEquipmentChange_Inferred` |
| System | inventory-transfer |
| Date | 2026-08-05 (MEGA-056 refresh) |
| Prior scaffold | 2026-07-23 (system unknown / Named_CalleeOf) |

---

## Machine-level notes

- Source: raw capture + MEGA-056 live re-verify append.
- Prefer assembly when decompiler conflicts (especially `this+0x2b0` grid ECX for FindFreeForItem; decompiler may elide).
- **ABI sealed:** `__thiscall`, ECX = inventory host (`player+0x250`), stack0 = item*, `RET 4`, EAX = status code.
- **Not** pure space-check: partition hint `Inventory_HasSpaceForItem` is only ret=2; full unit gates unequip/grab with arena/town/hazard/swap/equip-location codes.
- UI mapping of codes is owned by residual `FUN_00931db0` (callers pass EAX in EDI).

## Signature (sealed)

```c
uint32_t __thiscall Inventory_ValidateEquipmentChange_Inferred(
    void* inventory,   // ECX — host at player+0x250
    void* item);       // stack0 — null → 8
// ret 4
```

## Control flow (annotated)

```c
// ECX = inventory (EDI), stack item* (ESI)
// Body 0x004f6a80..0x004f6ed1 (0x452 bytes); RET 4 all exits

uint32_t __thiscall Inventory_ValidateEquipmentChange_Inferred(void* inv, void* item)
{
  if (item == NULL)
    return 8;   // "This item cannot be equipped."

  // stateObj = *( *(inv+4)+4 + inv + 0xa8 )
  state = inv_state_at_a8(inv);
  if (state != NULL && *(char*)(state + 0xf6) != 0)
    return 0xf; // "…cannot be changed in an arena."

  // ECX = inv+0x2b0  → InventoryGrid_FindFreeForItem (dualed 0x005714e0)
  // outs on stack scratch; allowMerge=1; pageIndex=-1
  if (!InventoryGrid_FindFreeForItem(inv+0x2b0, item, &outX, &outY, 1, -1))
    return 2;   // "There is not enough space…"

  type = *(int*)(*(int*)(item + 0xa8) + 0x38);  // item type class

  if (type == 0xc) {
    // vtbl+0x1e0 related; subtype char at clonebase+0x3f4
    // ==9 → match TFID vs inv+0x264
    // else flags at related+0xc4: bit2→inv+0x260[0], bit0x10→[1], bit4→[2]
    // TFID pair +0x164/+0x168  → return 0 on match
  }
  else if (type == 0x1c) {
    // vtbl+0x1f8; match inv+0x254 → 0
  }
  else if (type == 0xa) {
    // vtbl+0x1f4; match inv+0x268 → 0
  }
  else if (type == 0x10) {
    // vtbl+0x1f0; match inv+0x258;
    // if state+0xf5 → 0 else 1 (town-only)
  }
  else if (type == 6) {
    // __RTDynamicCast(item, CVOGClonedObjectBase → CVOGSimpleObject<CVOGGraphicsBase>)
    // subtype word at *(item+0xa8)+0x3c +0x3f4
    // 10 → match inv+0x26c → 0
    // 11 → match inv+0x270; then host vtbl+0x210(0):
    //      null or !*(char*)(result+0x6b8) → 0; else 0xd (Hazard Mode)
  }
  else if (type == 0xe) {
    // no state+0xa8 → 1
    // FUN_004ce5f0(inv) false → 1; true → 0xa (must swap)
  }

  return 8; // default cannot equip / not in recognized equip location
}
```

## Inventory host field map (this offsets — sealed as used)

| Offset | Role in this unit |
|---|---|
| `+0x4` | adjust base for `+0xa8` state and vtbl `+0x210` |
| `+0x254` | type `0x1c` container / hardpoint object* |
| `+0x258` | type `0x10` container object* |
| `+0x260` | type `0xc` flag-selected array of 3 object* |
| `+0x264` | type `0xc` subtype-9 container* |
| `+0x268` | type `0xa` container* |
| `+0x26c` | type `6` subtype 10 container* |
| `+0x270` | type `6` subtype 11 container* |
| `+0x2b0` | **InventoryGrid*** for FindFreeForItem |

## Callers (sealed)

1. `Client_SendInventoryUnequip` `0x00862c00` @ `0x00862c4c` — C2S `0x203E`
2. `Client_SendInventoryGrab_Hardpoint` `0x00862d90` @ `0x00862dd5` — C2S `0x2034`

Both: nonzero EAX → `FUN_00931db0` message UI; zero → proceed with packet.

## Open questions / gaps

- Product English names for type classes `0xc/0x1c/0xa/0x10/6/0xe` and host slot fields.
- Exact meaning of state flags `+0xf5` / `+0xf6` and hazard `+0x6b8` (strings sealed; field names open).
- Retail symbol (no plate on function).
- Runtime / bit-exact / differential.
- Whether any other xref exists outside the two named callers (Ghidra shows 2 only).
