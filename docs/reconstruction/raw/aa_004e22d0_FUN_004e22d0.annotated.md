# Annotated: Tree_RotateLeft_Isnil15 (`aa_004e22d0` / `FUN_004e22d0`)

| Field | Value |
|---|---|
| Stable ID | `aa_004e22d0` |
| VA | `0x004e22d0`–`0x004e231d` (78 B) |
| System | container / map-set tree (isnil `+0x15`) |
| Date | 2026-07-29 (W24-O dual seal) |
| Ghidra | `FUN_004e22d0` |
| Named | `Tree_RotateLeft_Isnil15` (**Inferred**) |

## Machine-level notes

- **Leaf** left-rotate for MSVC `_Tree` / `std::map` nodes with **isnil at +0x15**, color at +0x14.
- Layout links: left@0, parent@4, right@8 (same as staging family; only isnil offset differs).
- Twin right-rotate: `Tree_RotateRight_Isnil15` @ `0x006753b0`.
- Staging twin (isnil@+0x49): `Tree_RotateLeft` @ `0x00568b70` (W19-K) — same CF, different isnil immediate (`15` vs `49`).
- Shared by ~30 insert/erase rebalance sites (e.g. `SkillElementTypeMap_TreeInsertAndRebalance` @ `0x0054de50`, `Map_EraseNode_IntKey`, `FUN_004baa30`).

## Pseudocode (named)

```c
// __thiscall; ECX = map; [esp+4] = x; RET 4
void Tree_RotateLeft_Isnil15(MapShell *map, TreeNode_Isnil15 *x)
{
  TreeNode_Isnil15 *y = x->right;       // +8
  TreeNode_Isnil15 *yl = y->left;       // *y

  x->right = yl;
  if (yl->isnil == 0) {                 // +0x15
    yl->parent = x;
  }
  y->parent = x->parent;

  TreeNode_Isnil15 *head = map->head;   // *(map+4)
  if (x == head->parent) {              // root
    head->parent = y;
  } else {
    TreeNode_Isnil15 *p = x->parent;
    if (x == p->left) {
      p->left = y;
    } else {
      p->right = y;
    }
  }
  y->left = x;
  x->parent = y;
}
```

## ABI / layout

| Item | Value | Confidence |
|---|---|---|
| Convention | `__thiscall` + `RET 4` | **High** (ECX head load; three epilogues `C2 04 00`) |
| map shell | head at `+4`; root = head->parent | **High** |
| isnil | `+0x15` | **High** (`80 7E 15 00`) |
| Polarity | LEFT (y from right) | **High** (`8B 42 08`) |
| Color | not written | **High** |
| Leaf | no CALL | **High** |

## Open / residual

- Product/PDB symbol (Inferred name).
- Runtime / bit-exact.
- Individual map value typedefs (0x18 int-ptr is one consumer; any isnil@15 tree can share this leaf).
