# Annotated low-level: FUN_005be310 / Emitter_ReleaseToFreelist_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005be310` |
| VA | `0x005be310`–`0x005be32e` exclusive (**30** B) |
| Canonical | `Emitter_ReleaseToFreelist_Inferred` |
| System | client NDSpecialFX / emitter freelist |
| Date | 2026-07-29 (W30-R dual seal) |

## Machine-level notes

- **Prologue:** `push esi; mov esi,[esp+8]; push edi; mov edi,ecx; mov ecx,esi`
  - ESI = stack emitter node; EDI = freelist head*; ECX rebound to node for teardown.
- **Call:** `call FUN_005bbc50` (rel32 from `0x005be31a` → `0x005bbc50`). Decompiler shows bare `FUN_005bbc50()`; bytes prove **this = node**.
- **Null gate:** `test esi,esi; jz` skips freelist push only — teardown still runs with ECX=node (including null).
- **Freelist push:** `mov eax,[edi]; mov [esi],eax; mov [edi],esi` — classic `node->next = *head; *head = node` via dword0.
- **Epilogue:** `pop edi; pop esi; ret 4`.
- **Call-site ABI:** every observed site does `push <node>; mov ecx, 0x00B454FC; call FUN_005be310`.

## Pseudocode (annotated)

```c
// __thiscall; ECX = freelist head* (DAT_00b454fc at sites)
// stack: Emitter *node; RET 4; void
void Emitter_ReleaseToFreelist_Inferred(Emitter **head, Emitter *node)
{
  // bytes: mov ecx, node before call
  FUN_005bbc50(node);   // heavy emitter body teardown (not OWN here)
  if (node != 0) {
    *(void **)node = *head;  // next @ +0x0
    *head = node;
  }
}
```

## Callers

| Caller | Site | Context |
|---|---|---|
| `NDSpecialFX_HostTick_Inferred` (`FUN_004b8ed0`) | `0x004b913b` | After `Emitter_UpdateTick` returns 0: unlink list node, then release payload |
| `NDSpecialFX_ClearChildLists_Inferred` (`FUN_004b85d0`) | `0x004b85eb` | Walk host list `+0x1E0`, release each `node[2]` payload |
| `FUN_004a34f0` | 7 sites | Path/spawn cleanup; same `B9 FC 54 B4 00` freelist load |

## Open residual

- Product/PDB English for this VA and for teardown child `FUN_005bbc50` (W30-S).
- Whether null-node teardown is reachable (callers appear to push live payloads).
- Runtime freelist integrity under concurrent HostTick — open.
