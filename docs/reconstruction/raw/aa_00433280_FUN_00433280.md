# Raw capture: FUN_00433280

| Field | Value |
|---|---|
| **Stable ID** | `aa_00433280` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00433280` |
| **Canonical name** | `FUN_00433280` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00433280(undefined4 param_1,int param_2,undefined1 param_3)

{
  int in_EAX;
  int unaff_ESI;
  
  if ((((*(int *)(in_EAX + 0x1b0) != 0) && (-1 < unaff_ESI)) &&
      (unaff_ESI < *(int *)(in_EAX + 0x10))) &&
     ((-1 < param_2 && (param_2 < *(int *)(in_EAX + 0xc))))) {
    *(undefined1 *)(*(int *)(in_EAX + 0xc) * unaff_ESI + *(int *)(in_EAX + 0x1b0) + param_2) =
         param_3;
  }
  return;
}
```
# Residual 2026-07-29 (W23-A dual A/B seal)

**Tools:** `decompile_function` `0x00433280`, `read_memory` length 80, `analyze_function_complete`, `get_function_by_address`, `get_assembly_context` on call sites `0x00434efb` / `0x00435a10`. Sibling read decompile `0x004332b0`. No `disassemble_bytes`.

**Body:** `0x00433280`–`0x004332ac` (45 bytes incl. `CC` pad).

**Live entry bytes (hex):**

```
8B 88 B0 01 00 00 85 C9 74 20 85 F6 7C 1C 3B 70 10 7D 17
85 D2 7C 13 8B 40 0C 3B D0 7D 0C 0F AF C6 03 C1 8A 4C 24 04
88 0C 10 C2 04 00 CC CC CC
```

**Machine reconstruction (from bytes, not disassemble_bytes tool):**

| Addr | Bytes | Meaning |
|---|---|---|
| `00433280` | `8B 88 B0 01 00 00` | `plane = *(EAX + 0x1b0)` — **this in EAX** |
| `00433286` | `85 C9` / `74 20` | plane null → return |
| `0043328a` | `85 F6` / `7C 1C` | **ESI** (row) < 0 → return |
| `0043328e` | `3B 70 10` / `7D 17` | row ≥ height@`+0x10` → return |
| `00433293` | `85 D2` / `7C 13` | **EDX** (col) < 0 → return |
| `00433297` | `8B 40 0C` | `width = *(this + 0x0c)` (clobbers EAX) |
| `0043329a` | `3B D0` / `7D 0C` | col ≥ width → return |
| `0043329e` | `0F AF C6` | `eax = width * row` |
| `004332a1` | `03 C1` | `eax += plane` |
| `004332a3` | `8A 4C 24 04` | `cl = [esp+4]` stack byte |
| `004332a7` | `88 0C 10` | `*(plane + row*width + col) = cl` |
| `004332aa` | `C2 04 00` | **`RET 4`** — one stack arg |

**ABI sealed (decompiler under-specified):**

- **EAX** = `NDAssetImage* this` (not ECX thiscall)
- **ESI** = row `y` (int)
- **EDX** = col `x` (int)
- **Stack `[esp+4]`** = `uint8_t value` (alpha)
- Epilogue **`RET 4`**
- Return: void (no meaningful EAX)

**Callees:** none (leaf). **Callers:** `FUN_00434dd0` / `NDAssetImage_DecodeUncompressedTgaRow` @ `0x00434efb` (32bpp TGA alpha); `FUN_00435410` (PNG path) @ `0x00435a10`.

**Call-site register proof:**

- TGA: `MOV EAX,[EBP+8]` image; `MOV ESI,[EBP+0x14]` row; EDX built as x0+index; `PUSH` alpha from TGA A; `CALL`.
- PNG: `MOV EAX,EBX` image; `MOV ESI,[EBP-0x38]` row; EDX = col loop; `PUSH` A from `RGBA[+3]`; `CALL`; `ADD EDX,1`.

**Sibling:** `FUN_004332b0` @ `0x004332b0` — same plane/bounds layout, **reads** byte (write counterpart).

**Canonical structural name:** `NDAssetImage_WriteAlphaPlaneByte` (family established by sealed `NDAssetImage_LoadTGA` / TGA row duals; plane role from alpha write sites; no product RTTI string on this VA).
