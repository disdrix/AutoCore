# Annotated low-level: FUN_007a41f0 / Rng_Source_ExtractU32

| Field | Value |
|---|---|
| Stable ID | `aa_007a41f0` |
| VA | `0x007a41f0` |
| Canonical name | `Rng_Source_ExtractU32` |
| System | RNG / MT19937 source object |
| Date | 2026-07-29 (W21-L dual seal) |

## Machine-level notes

- Body is **7 bytes**: load MT state from `this+0x14` into EAX, **jmp** to sealed `Rng_Mt19937_Extract` (`0x00424830`).
- Decompiler collapses to `FUN_00424830(); return;` and drops the `this+0x14` load — **bytes are authoritative**.
- Extract callee uses **EAX** as state pointer (not ECX); this wrapper bridges thiscall source → EAX-state extract.
- Not a loot-table index helper; loot callers use this as the integer RNG step (with `CVOGReaction_RandomUnitScalar` nearby).

## Pseudocode (annotated)

```c
/* Rng_Source_ExtractU32
   this = RNG source object (table-capable; MT* at +0x14).
   return = tempered uint32 from Rng_Mt19937_Extract. */

uint32_t __thiscall Rng_Source_ExtractU32(void *source)
{
  void *mt = *(void **)((uint8_t *)source + 0x14);
  /* EAX = mt; tail call */
  return Rng_Mt19937_Extract(mt); /* FUN_00424830 — EAX ABI */
}
```

## Open questions

- Product class name of the source object (`FUN_007a4200` owner).
- Whether loot paths always use live extract vs table path (`+8` buffer in sibling).
