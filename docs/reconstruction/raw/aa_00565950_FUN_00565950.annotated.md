# Annotated low-level: CVOGSpawnPoint_ActivateFromStream_Inferred (`FUN_00565950`)

| Field | Value |
|---|---|
| Stable ID | `aa_00565950` |
| VA | `0x00565950`–`0x00566485` (**2870 B / `0xB36`**) |
| System | client spawn-point stream unpack + activation |
| Date | 2026-07-23 scaffold; **2026-07-29 W30-H seal** |

## Machine-level notes

- **ABI (bytes):** thiscall; ECX = spawn-point **subobject** (fields at negative offsets vs ECX — classic MI); stack formals cleaned by **`ret 0x10`** (`C2 10 00`).
- **Entry:** frame + 16-byte align + SEH (`LAB_009a4b5f`); `mov esi,ecx`; `mov ebx,[ebp+8]`; null stream → `xor al,al` return **0**.
- **Epilogue:** `mov al,1` then restore SEH / `ret 0x10` → success **1**.
- **Product strings (High):**
  - `"******BAD SPAWN POINT ACTIVATION RANGE: %d %f"` + `OutputDebugStringA`
  - `"Spawn:%I64d"` name for spawned entity
- **Default activation range:** `_DAT_009d2eb8` = **75.0f** (`read_memory` `00 00 96 42`).
- **Stream reader:** `(*(code **)(*stream + 0x14))(dst, size)` — byte-block unpack (same pattern as other game-data stream consumers).
- **Version (`param_3` as int):** gates additional fields (`>0x0E`, `>0x17`, `>0x19`, `<0x1D`, `>0x1E`, `>0x1F`, special `<0x1D` loot/table remap, `>=0x1D` zero-out empty slots).
- **World offset:** `param_4` float4 **added** into position at `(base-0x15c)` after reading 0x10 bytes.
- **MI thunk entry:** `0x00567CBE` `sub ecx,[ecx-4]; jmp FUN_00565950` — sole direct CODE xref (vtable-facing adjustor).
- **Nested sealed callees (other duals):**
  - `FUN_00567860` = `StdMap_InsertAlways_Val12_IntKey` (W29-M) — race/slot map inserts near end
  - `FUN_0040b330` GetOrInsert by int key
  - `Object_GetRootRaceId`
  - `FUN_005630f0` loot/table remap (when version `<0x1D`)
  - Spawn helpers `FUN_0057b0e0` (0x270), `FUN_005dbfa0` (0x350) under sector flag conditions
- **Neighbor names:** `CVOGSpawnPoint_CreateTemplateVehicle` `0x00564290`, `SetObjectActiveState` `0x00564700`, `CreateCreature` `0x00564f60`.

## Spine (high level)

```
if stream==null: return 0
vtbl+0xB0 on adjusted object base          // pre-activate hook
read float4 pos @ base-0x15c; pos += worldOffset
read float4 @ base-0x14c
read several scalar fields (range, flags, bits…)
if range<=0: log BAD SPAWN POINT…; range = 75.0f
version-gated table of 12×0xC slots @ -0xF0 family
optional string/name @ version>0xE via FUN_00512460
conditional spawn of linked objects when sector+flags set
version>0x1F: read length-prefixed blob then free (skip-consume)
if sector present: for each active slot → race resolve + InsertAlways maps
return 1
```

## Gaps

- Full field atlas product names / exact version schema table.
- Identity of all spawn helper ctors (`0057b0e0`, `005dbfa0`) and register paths.
- Fourth stack formal implied by `ret 0x10` vs three decompiler formals.
- Runtime / bit-exact open.
- Decompiler stack alias noise (SEH large frame) — CF from live ≡ raw still holds.
