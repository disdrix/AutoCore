# Annotated low-level: FUN_005e18d0 → StdMap_InsertOrFindByIntKey_Val12

| Field | Value |
|---|---|
| Stable ID | `aa_005e18d0` |
| VA | `0x005e18d0`–`0x005e1988` exclusive (184 B / `0xB8`) |
| System | std map unique insert-or-find (Val12 / signed int key) |
| Date | 2026-07-23 scaffold; 2026-07-29 W30-L dual seal |

## Machine-level notes

- **Role:** MSVC `_Tree` **insert-or-find** (unique map). Peer of `StdMap_InsertOrFindByIntKey_Val12` @ `0x0053a1a0` (W28-P); distinct always-insert helper (`005e13b0` vs `00539210`).
- **ABI:** `__thiscall` ECX=map; stack `(InsertPair* out, Val12* value)`; **`ret 8`** on all three exits.
- **Out pair:** `{ Node* it @+0, uint8_t inserted @+4 }`. Equal-key path sets `inserted=0` and **does not** rewrite value fields.
- **Key:** signed int at value dword0 / node `+0x0C`. Bytes: walk uses **`setl`**, second gate **`jge`** — not the unsigned peer (`00573810`).
- **isnil:** `+0x19` (Val12 family).
- **Callees:** `FUN_005e13b0` always-insert+rebalance (buynode `005ae220`, max size `0x15555553`, `ret 0x10`); `FUN_005ae050` / `StdTree_Dec_Val12` predecessor.
- **Caller:** `FUN_005e0610` only (mission/reaction consumer; product map type open).
- Legacy human alias `Named_CalleeOf_…_Mission_…` is **narrow** (caller domain, not this unit’s role).

## Pseudocode (annotated)

```c
void __thiscall StdMap_InsertOrFindByIntKey_Val12(MapShell* map, InsertPair* out, const int* value)
{
  Node* head = map->head;
  Node* where = head;
  bool addLeft = true;

  if (!head->parent->isnil) {
    Node* cur = head->parent; // root
    do {
      where = cur;
      addLeft = (value[0] < where->key); // SIGNED
      cur = addLeft ? where->left : where->right;
    } while (!cur->isnil);
  }

  if (addLeft) {
    if (where == head->left) { // leftmost
      Node** p = FUN_005e13b0(map, &scratch, 1, where, value);
      out->it = *p; out->inserted = 1; return; // ret 8
    }
    FUN_005ae050(&where); // predecessor
  }

  if (where->key < value[0]) { // SIGNED
    Node** p = FUN_005e13b0(map, &scratch, addLeft, where, value);
    out->it = *p; out->inserted = 1; return; // ret 8
  }

  out->inserted = 0;
  out->it = where;
  // ret 8 — no value mutate
}
```

## Open questions

- Product demangle for map in `FUN_005e0610`.
- Full dual of always-insert helper `FUN_005e13b0` (context only; not OWN).
- Runtime / bit-exact.
