# Annotated low-level: AssPreloader_ProcessKeyRingStep_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_004ed310` |
| VA | `0x004ed310`–`0x004ed46b` (**348 B** / `0x15C`) |
| Ghidra | `FUN_004ed310` |
| Canonical | `AssPreloader_ProcessKeyRingStep_Inferred` (**Inferred**) |
| System | AssPreloader / client-fx preload key ring |
| Date | 2026-07-29 (W35-A dual seal) |

## Machine-level notes

- Source: live `decompile_function` + full-body `read_memory` (W35-A).
- **Bytes win** on ABI: Ghidra void surface; all exits `ADD ESP,8; RET` (plain `C3`); callers `ADD ESP,0xC` → **cdecl**, 3 stack formals; return **AL**.
- Global gate: `*(DAT_00d1f050 + 0x6c) == 0` → return **1** (no AssPreloader → treat as done).
- Walks a GuardedVector **key ring** (`param_1`) with absolute index `begin + *cursor`; page-map same family (`>>2` / wrap by capacity).
- Nested AssPreloader ops use **ECX = \*(DAT_00d1f050+0x6c)**:
  - `FUN_00971900(key*, recurse=0)` — IsKeyTrackedRecursive (W31-K)
  - `FUN_00971030(key*)` — pending-side remove (push flag0 + EraseFirstEqual on preloader+0x1c via this unit's sibling `0043e630`)
  - `FUN_00971a20(key*, 0)` — tree-ready / present check
  - `FUN_009717a0(key*)` — EnqueueKeyAndDeps (W32-J)
- Cache present: `FUN_0075d610(key*)` with **ECX = DAT_00d1f050** (resource map hit with non-null value).
- Erase on tracked keys: `FUN_0043df90` EraseRange_Thiscall `[idx, idx+1)`; resume at published out-iterator.
- Reject scaffold plate `Named_CalleeOf_Named_CalleeOf_Named_VOG_DEBUG_STOP_004ed310`.

## Signature (sealed)

```c
// cdecl; 3 stack formals; plain RET; AL = 1 done / 0 yield
uint8_t AssPreloader_ProcessKeyRingStep_Inferred(
    GuardedVectorHeader* ring,   // param_1
    char* progress_flag,         // param_2  (in/out)
    uint32_t* cursor);           // param_3  (in/out relative index)
```

## Control flow (annotated)

```
if *(global_cache + 0x6c) == 0: return 1

if *cursor < 0 or ring->size <= *cursor: *cursor = 0

cur_container = ring
abs_idx = ring->begin + *cursor

loop:
  end = ring->begin + ring->size
  if cur_container == ring and abs_idx == end:
    *cursor = 0; *progress_flag = 0; return 1   // exhausted

  slot* = resolve_page(cur_container, abs_idx)

  if IsKeyTracked(preloader, slot, recurse=0):
    if *progress_flag != 0:
      FUN_00971030(preloader, slot)   // side-host push flag0 + erase pending +0x1c
    EraseRange_Thiscall([abs_idx, abs_idx+1)) on cur_container
    (cur_container, abs_idx) = out_iterator
    continue   // do not advance cursor

  // not tracked
  if *progress_flag == 0:
    if !CacheHasResource(cache, slot) and !TreeReady(preloader, slot, 0):
      EnqueueKeyAndDeps(preloader, slot)
      *progress_flag = 1
      return 0   // yield — work enqueued
    // else fall through → advance
  else:
    if !CacheHasResource(cache, slot):
      if !TreeReady(preloader, slot, 0):
        return 0   // yield — still waiting
      *cursor++; abs_idx++; continue
    // else fall through → advance

  *cursor++
  abs_idx++
```

## Call graph

| Dir | Target | Role |
|---|---|---|
| Calls | `FUN_00971900` | tracked? (recurse=0) |
| Calls | `FUN_00971030` | pending remove when flag set |
| Calls | `FUN_0043df90` | EraseRange_Thiscall one slot |
| Calls | `FUN_0075d610` | NDResourceCache key present |
| Calls | `FUN_00971a20` | AssPreloader tree ready |
| Calls | `FUN_009717a0` | EnqueueKeyAndDeps |
| Called by | `FUN_00542790` FxCache_ApplyWithNfxEnsure | 2 sites |
| Called by | `FUN_00542e20`, `FUN_005575c0`, `FUN_005c6ad0` | preload/FX paths |

## Open residual

- Product/PDB method English.
- Nested `FUN_0075d610` / `FUN_00971a20` / `FUN_00971030` product dual free (not owned).
- Exact product meaning of progress_flag (cooperative yield token).
- Runtime / bit-exact / multi-thread races under CS on nested vectors.
