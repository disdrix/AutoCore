# Annotated low-level: StdVector_UninitializedFillN_Elem12_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00406ee0` |
| VA | `0x00406ee0` |
| Canonical name | `StdVector_UninitializedFillN_Elem12_Inferred` |
| Ghidra name | `FUN_00406ee0` |
| System | util / container (`std::vector` POD elem **0xC**) |
| Date | 2026-08-04 (WQ9G-I dual seal) |

## Machine-level notes

- Source: raw capture + WQ9G-I re-verify (`decompile` + `read_memory`).
- **EDI** = dest start, **ESI** = count (decompiler `unaff_*`).
- Stack formal0 = `const T* value`; **`ret 4`**.
- Thin wrapper: construct-N via `FUN_00409db0`, then return **`dest + count * 0xC`**.
- Construct-N: ECX=value*, stack dest+count; each iter `FUN_00409f50` copies **3 dwords** and advances dest by **0xC**.
- Phantom extra pushes into construct-N cleaned with `add esp,0x10` (MSVC pattern; same family as toast/floater Ufill wrappers).
- Sole caller: `FUN_004082f0` InsertN (2 sites — realloc path + in-place hole path).

## Pseudocode (annotated)

```c
// customcc: EDI = dest*, ESI = count
// stack: const T* value   (T = 12-byte POD)
// ret 4; EAX = dest + count*12
T* StdVector_UninitializedFillN_Elem12_Inferred(
    /*EDI*/ T* dest,
    /*ESI*/ int count,
    /*stack*/ const T* value)
{
  // FUN_00409db0: for i in [0,count): copy 3 dwords value → dest+i
  FUN_00409db0(dest, count /*, ECX=value*/);
  return (T*)((char*)dest + count * 12);
}
```

## Open questions

- Product type English for the 12-byte element.
- Whether construct-N `FUN_00409db0` is shared across other elem-12 maps (undualed residual).
- Runtime / bit-exact / differential open.
