# Raw capture: FUN_007227e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_007227e0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x007227e0`–`0x00722821` |
| **Canonical name** | `FUN_007227e0` (inferred: `Audio_LinearGainToMilliBels`) |
| **System** | audio / interface sound |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
/* WARNING: Globals starting with '_' overlap smaller symbols at the same address */

float10 FUN_007227e0(float param_1)

{
  if (param_1 <= 0.0) {
    return (float10)DAT_00aaa5d8;
  }
  if (g_flOne <= param_1) {
    return (float10)g_flZero;
  }
  return (float10)0.3010299956639812 * ((float10)g_flOne / (float10)param_1) *
         (float10)_DAT_00aaa5d0;
}
```

---

## Append — dual seal 2026-07-29 (W18-M OWN-ONLY)

| Field | Value |
|---|---|
| **Agent** | W18-M OWN-ONLY dual A/B |
| **Tools** | `decompile_function` + `force_decompile` (identical) + `read_memory` + `get_function_pcode` basic + `analyze_function_complete` + `get_assembly_context` sample call sites. **No** `disassemble_bytes`. |
| **Dual decompile** | Rep1 ≡ Rep2 — third-rep **not required** for text identity |
| **Body range** | `0x007227e0`–`0x00722821` (`RET 4`); `CC` pad follows |
| **Body hex** (`read_memory` @ `0x007227e0`, 66 B to RET) | `0f57c9f30f104424040f2fc87209d905d8a5aa00c204000f2f05a0f2a0007209d90518f5a000c20400d905a0f2a000d8742404d9ecd9c9d9f1dc0dd0a5aa00c20400` |

### Byte / FPU algorithm (sealed — overrides decompiler main path)

```
XORPS XMM1, XMM1
MOVSS XMM0, [ESP+4]          ; x = param (stdcall float)
COMISS XMM1, XMM0            ; 0 ? x
JC    mid                    ; if 0 < x (i.e. x > 0) continue
FLD   dword [DAT_00aaa5d8]   ; -10000.0f
RET   4
mid:
COMISS XMM0, [g_flOne]       ; x ? 1.0
JC    calc                   ; if x < 1 continue
FLD   dword [g_flZero]       ; 0.0f
RET   4
calc:
FLD   dword [g_flOne]        ; 1.0
FDIV  dword [ESP+4]          ; 1/x
FLDLG2                       ; push log10(2)
FXCH                         ; ST0=1/x, ST1=log10(2)
FYL2X                        ; log10(2)*log2(1/x) = log10(1/x) = -log10(x)
FMUL  qword [_DAT_00aaa5d0]  ; * (-2000.0)  =>  2000 * log10(x)
RET   4
```

### Constants (`read_memory`)

| Symbol | VA | Bytes | Value |
|---|---|---|---|
| `_DAT_00aaa5d0` | `0x00aaa5d0` | `00 00 00 00 00 40 9f c0` | **double −2000.0** |
| `DAT_00aaa5d8` | `0x00aaa5d8` | `00 40 1c c6` | **float −10000.0** |
| `g_flOne` | `0x00a0f2a0` | `00 00 80 3f` | **float 1.0** |
| `g_flZero` | `0x00a0f518` | `00 00 00 00` | **float 0.0** |

### Decompiler caveat (High)

Ghidra rewrites `FLDLG2`/`FYL2X` as `0.30103 * (1/x) * (−2000)` (**linear**, wrong). Sealed math is **logarithmic**:

`result = 2000 * log10(x)` for `0 < x < 1`, else clamps `x<=0 → −10000`, `x>=1 → 0`.

Equivalence: `log10(1/x) * (−2000) = 2000 * log10(x)`.

Units: **2000·log10(gain) = millibels** of amplitude gain (100 × 20·log10).

### ABI (sealed)

- **Convention:** `__stdcall`, one `float` on stack; **`RET 4`**
- **Return:** x87 `ST0` (`float10` / 80-bit extended) — callers `FSTP` to `float` (often object `+0x1b0`)
- **Callees:** none (leaf)
- **Xrefs:** 22; callers include `Client_PlayNamedInterfaceSound`, `FUN_00723ab0`, `FUN_007228a0`, `FUN_00724d70`, `FUN_004bb3a0`, `FUN_004bb5b0`, …

### Call surface sample

- `PUSH 0` / `PUSH ECX` then `CALL 0x007227e0` then `FSTP dword [obj+0x1b0]` (or stack then convert via `FISTP`)
