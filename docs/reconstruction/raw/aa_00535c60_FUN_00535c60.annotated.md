# Annotated low-level: CVOGCharacter_TryEvalInteractObjectivesNearObject_Inferred (FUN_00535c60)

| Field | Value |
|---|---|
| Stable ID | `aa_00535c60` |
| VA | `0x00535c60`–`0x00535ec1` |
| System | `missions-progression` / interact-near-object |
| Date | 2026-07-29 W24-K live seal |
| Tools | Ghidra `decompile_function` + `read_memory` (no `disassemble_bytes`) |

## Machine-level notes

- **ABI:** `__thiscall` ECX=character; stack object*; `ret 4`; bool AL.
- **Gate A — inventory COID:** `FindItemByCoid` on grid at `*( *(char+0x250) + 0x2b0 )` with keys `object+0x160/0x164`. Non-null → **skip** distance gate.
- **Gate B — range:** if no inventory hit:
  - If object vtbl`+0x1c8` null or `*(result+8)==0`: Euclidean distance player pos (vbase `+0x1a0`) vs object pos `+0x80..+0x88`, subtract radius `*(vbase+0x19c result + 0x34)`, compare to **`DAT_00aaa6e8` = 20.0f**.
  - Else: alternate distance via `FUN_0053e510` after vbase `+0x19c`(nested).
  - If `distance > 20.0f` → return 0.
- **Hash walk:** lock byte `@hash+0x1d` on `char+0x548` (HashError strings); traverse nodes `hash+0x14` chain `node+0x14`, payload `node+8` = mission/objective def*.
- **Per def evaluators:** vector `[def+0x158, def+0x15c)` (count = end-begin>>2). For each evaluator: `vtbl+0x50` type; require **`0xC`**; `vtbl+0x40(char, object)` must be true.
- **Success:** unlock; `EvaluatePendingObjectives(char, 9, (float)*(object+0xa8)+0x34, 0, 0)`; return 1.
- **Fail:** unlock; return 0.
- **Static callers:** none recovered (gap). Kind **9** identity sealed by image `push 9` into EvaluatePending (owned dual elsewhere).

## Pseudocode (annotated copy of raw)

```c
bool __thiscall FUN_00535c60(void *character, int *object)
{
  // Gate A: inventory has instance COID?
  InventoryGrid *grid = *(InventoryGrid**)(*(char*)(character + 0x250) + 0x2b0);
  if (!InventoryGrid_FindItemByCoid(grid, object[0x58], object[0x59])) {
    // Gate B: range ≤ 20.0f
    float dist;
    void *nested = object->vtbl[+0x1c8]();
    if (!nested || *(int*)(nested + 8) == 0) {
      float *ppos = character_vbase->vtbl[+0x1a0]();
      float *radius_host = character_vbase->vtbl[+0x19c]();
      dist = sqrt(sumsq(object_pos - ppos)) - *(float*)(radius_host + 0x34);
    } else {
      // alternate path via FUN_0053e510
      dist = FUN_0053e510(...);
    }
    if (dist > *(float*)0x00AAA6E8) // 20.0f
      return 0;
  }

  CNDHash *hash = *(CNDHash**)(character + 0x548);
  lock(hash);
  for (node = first(hash); node; node = next(node)) {
    def = node->value; // +8
    if (!def) { unlock; return 0; }
    for (i = 0; i < evaluator_count(def); i++) {
      eval = evaluators(def)[i];
      if (eval->vtbl[+0x50]() == 0xC && eval->vtbl[+0x40](character, object)) {
        unlock(hash);
        CVOGCharacter_EvaluatePendingObjectives(
          character, 9, (float)*(int*)(*(int*)(object + 0xa8 /*[0x2a]*/) + 0x34), 0, 0);
        return 1;
      }
    }
  }
  unlock(hash);
  return 0;
}
```

## Live seal cross-check

| Claim | Evidence | Conf |
|---|---|---|
| thiscall + ret 4 | prologue ECX; epilogue `C2 04 00` | High |
| FindByCoid keys `+0x160/164` | prologue movs + sealed callee dual | High |
| Threshold 20.0f @ `0x00aaa6e8` | `comiss [0x00aaa6e8]`; `read_memory` `00 00 A0 41` | High |
| Active hash `+0x548` + lock `+0x1d` | image + HashError strings | High |
| Type filter `0xC` | decompile `iVar12 == 0xc` | High |
| Kind 9 EvaluatePending | image `6A 09` + push float cbid | High |
| Static callers | none | Open / gap |
