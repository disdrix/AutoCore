# Annotated low-level: WorldObj_SnapshotHostRefsAndUnbind_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_005617c0` |
| VA | `0x005617c0`–`0x005618a0` (**224 B**) |
| Ghidra | `FUN_005617c0` |
| Canonical | `WorldObj_SnapshotHostRefsAndUnbind_Inferred` |
| System | world-object / mode-7 leave snapshot |
| Date | 2026-07-29 (W29-T dual) |

## Machine-level notes

- Source: raw scaffold 2026-07-23 + live re-verify 2026-07-29.
- ABI: **thiscall**; ECX = manager; stack #1 = world object; stack #2 = snapshot worklist desc `{data,count,cap}`; void; **`ret 8`**.
- Sole caller: `FUN_005d4440` when **old** HBAI mode code == **7** (before HBAI swap); snapshot later fed to `WorldObj_RehomeLinksFromSnapshot_Inferred` (`00560f70`).
- Decompiler `FUN_005614f0(&param_2,param_2)` is thiscall on **manager** with out-flag slot = address of stack obj formal and value = obj.

## Control flow (machine ≡ decompile stages)

```
host = *(manager + 0x2c)
snapshot.count = 0
for i in 0 .. host.count(+0x5c)-1:
  entry = host.list(+0x58)[i]
  if entry+0xc == obj OR entry+0x10 == obj:
    grow snapshot if full (elem 4)
    push entry
    entry->vtbl+0x28(local16)
    host->vtbl+0xc(entry, &managerLocal)
    compact-remove list[i]; i--
FUN_005614f0(manager, &objSlot, obj)  // defer or unbind
```

## Pseudocode (annotated)

```c
// thiscall; ECX=manager; stack=obj, snapshot*; ret 8
void __thiscall WorldObj_SnapshotHostRefsAndUnbind_Inferred(
    int manager, int obj, int *snapshot /* {data,count,cap} */)
{
  int *hostList;
  int host;
  int i;
  int entry;
  int managerLocal;
  uint8_t local16[16];

  host = *(int *)(manager + 0x2c);
  hostList = (int *)(host + 0x58);
  snapshot[1] = 0;                 // count
  managerLocal = manager;

  i = 0;
  if (*(int *)(host + 0x5c) > 0) {
    do {
      entry = *(int *)(*hostList + i * 4);
      if (*(int *)(entry + 0xc) == obj || *(int *)(entry + 0x10) == obj) {
        if (snapshot[1] == (snapshot[2] & 0x7fffffff)) {
          FUN_005b3370(snapshot, 4);
        }
        *(int *)(*snapshot + snapshot[1] * 4) = entry;
        snapshot[1] += 1;

        (**(code **)(*(int *)entry + 0x28))(local16);
        (**(code **)(**(int **)(manager + 0x2c) + 0xc))(entry, &managerLocal);

        host = *(int *)(manager + 0x2c);
        *(int *)(host + 0x5c) -= 1;
        // shift entries [i+1..] down
        {
          int j = i;
          while (j < *(int *)(host + 0x5c)) {
            *(uint32_t *)(*(int *)(host + 0x58) + j * 4) =
                *(uint32_t *)(*(int *)(host + 0x58) + j * 4 + 4);
            j++;
          }
        }
        i -= 1;
      }
      i += 1;
    } while (i < *(int *)(host + 0x5c));  // note: loop bound uses original host count seed in decompile (iVar3)
  }

  FUN_005614f0(manager, (uint8_t *)&obj, obj);
}
```

### Loop-bound note

Decompiler seeds `iVar3 = *(manager+0x2c)` once and compares `i < *(iVar3+0x5c)`. After compact removes, `*(host+0x5c)` decreases; if `iVar3` still aliases the same host object, the bound tracks live count. Compact + `i--` keeps iteration correct for multi-match.

## Nested callees (not OWN)

| Symbol | Role |
|---|---|
| `FUN_005b3370` | Grow snapshot dword list |
| entry `vtbl+0x28` | Per-entry virtual before remove (16 B out) |
| host `vtbl+0xc` | Host notify/remove virtual |
| `FUN_005614f0` | If `manager+0x12c==1`: defer obj onto `manager+0xf0` list; else unbind (`0055fbf0`/`0055f0b0`/`0055fd80`) |

## Open questions

- Product English for mode **7** / host entry type (fields `+0xc` / `+0x10`).
- Product names for entry `vtbl+0x28` and host `vtbl+0xc`.
- Whether snapshot `data`/`cap` are pre-inited by caller (count cleared here; cap 0 forces grow on first push).
- Runtime / bit-exact.
