# Raw capture: FUN_0051bf10

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051bf10` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051bf10` |
| **Canonical name** | `FUN_0051bf10` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
undefined4 * __thiscall FUN_0051bf10(undefined4 *param_1,byte param_2)

{
  *param_1 = &PTR_FUN_009ce07c;
  FUN_0051bc90();
  DeleteCriticalSection((LPCRITICAL_SECTION)(param_1 + 1));
  if ((param_2 & 1) != 0) {
                    /* WARNING: Subroutine does not return */
    operator_delete(param_1);
  }
  return param_1;
}
```

---

## W31-R append (2026-07-29) — OWN-ONLY dual seal

| Field | Value |
|---|---|
| **Wave** | W31-R |
| **Tools** | `decompile_function` + `analyze_function_complete` + `read_memory` + xrefs. **No** `disassemble_bytes`. |
| **Body** | `0x0051bf10`–`0x0051bf3e` exclusive (**46 B** / `0x2E`); pad `CC` |
| **Ghidra body** | `0051bf10`–`0051bf3d` (inclusive last byte of `C2 04 00`) |
| **ABI** | **ECX=`this`**; stack **`byte flags`**; **`RET 4`** (`C2 04 00`); returns **`this`** in EAX; MSVC **scalar deleting dtor** |
| **Semantic name** | `ObjectCsListBase_ScalarDeletingDtor_Inferred` |
| **Classification** | worker |
| **Xrefs** | DATA only: vtbl `PTR_FUN_009ce07c` slot0 @ `0x009ce07c` → `0x0051bf10` (no CODE callers) |
| **Callees** | `FUN_0051bc90`, `DeleteCriticalSection` IAT `[0x009c61f0]`, `operator_delete` (cdecl, `ADD ESP,4`) |
| **Live decompile** | ≡ scaffold raw; Ghidra **false noreturn** on `operator_delete` — bytes fall through to `MOV EAX,ESI` / `RET 4` |

### Full body hex (46 B)

```
568bf1c7067ce09c00e872fdffff8d460450ff15f0619c00f644240801740956e8edd8f6ff83c4048bc65ec20400
```

### Byte-level CF

1. `MOV ESI,ECX`; store **`PTR_FUN_009ce07c`** @ `this+0`.
2. `CALL FUN_0051bc90` — base-phase list drain under CS.
3. `LEA EAX,[ESI+4]`; `PUSH EAX`; IAT `DeleteCriticalSection` — CS at **`this+0x04`**.
4. `TEST [ESP+8],1` — flags after `PUSH ESI` still at stack slot (`param_2`).
5. If bit0 set: `PUSH ESI`; `CALL operator_delete`; `ADD ESP,4`.
6. `MOV EAX,ESI`; `POP ESI`; **`RET 4`**.

### Contrast vs complete dtor `FUN_0051d0e0`

| | `0051d0e0` complete | `0051bf10` scalar base |
|---|---|---|
| Derived vtbl `009ce154` phase | **Yes** + clear | **No** |
| Base vtbl `009ce07c` phase | **Yes** + clear | **Yes** + clear |
| `DeleteCriticalSection` | **Yes** | **Yes** |
| `operator_delete(this)` | **Never** | If `flags&1` |
| Cleanup | bare `RET` | `RET 4` |
