# Annotated low-level: FUN_00416350 → Class_00a7000c_ScalarDeletingDtor_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00416350` |
| VA | `0x00416350`–`0x0041636d` inclusive (**30 B** / `0x1E`) |
| System | MSVC scalar-deleting destructor / `PTR_FUN_00a7000c` |
| Date | 2026-08-05 (WQ9I-H dual) |
| Canonical | `Class_00a7000c_ScalarDeletingDtor_Inferred` |

## Machine-level notes

- **ABI:** `__thiscall`; ECX = this; stack `flags`; **RET 4**; EAX = this.
- **Bytes:** `56 8bf1 e848cc4100 f644240801 7409 56 e8bd340700 83c404 8bc6 5e c20400`
  - `PUSH ESI; MOV ESI,ECX` → this
  - `CALL FUN_00832fa0` → complete dtor (ECX still this)
  - `TEST byte [ESP+8], 1` → flags bit0 (after one PUSH, stack formal at +8)
  - `JZ skip_delete`
  - `PUSH ESI; CALL operator_delete; ADD ESP,4`
  - `MOV EAX,ESI; POP ESI; RET 4`
- **Vtbl:** DATA xref only — `*(uint32_t*)0x00a7000c == 0x00416350` = vtbl[0].
- **Complete:** `FUN_00832fa0` installs `*this = &PTR_FUN_00a7000c`, frees owned ptr at `this[0x122]`, chains `FUN_007b5be0` (not OWN).
- **False noreturn:** decompiler WARNING on `operator_delete` is wrong; epilogue returns this.
- **Not** vector-deleting (no element count / loop); free is flags bit0 only.

## Pseudocode (annotated)

```c
// ECX = this; stack flags; RET 4; returns this*
// vtbl[0] of PTR_FUN_00a7000c
void* __thiscall Class_00a7000c_ScalarDeletingDtor_Inferred(void* self, uint8_t flags)
{
  FUN_00832fa0(self);                 // complete dtor
  if ((flags & 1u) != 0u) {
    operator_delete(self);            // free host when scalar-new path
  }
  return self;
}
```

## Open questions

- Product/MSVC demangle for class of vtbl `00a7000c`.
- Complete dtor member map (`FUN_00832fa0` / base `FUN_007b5be0`) — residual under other OWN.
- Vtbl slots [1+] English.
