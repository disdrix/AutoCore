# Annotated: `aa_00531570` CVOGCharacter_TryType1aSimpleExchange_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00531570` |
| VA | `0x00531570` |
| Canonical | `CVOGCharacter_TryType1aSimpleExchange_Inferred` |
| System | `inventory-transfer` |
| Date | `2026-08-04` (WQ8R-B dual seal) |

## Machine-level notes

- **ABI:** MSVC `__thiscall`, `ret 8` — ECX = `CVOGCharacter*`; stack0 = type-`0x1a` object*; stack1 = request qty.
- **Return:** `uint8`/`u32` **1** success, **0** fail (AL set; high bytes undefined on some edges).
- Decompiler loses dual-grid ECX; **authoritative grid assignment is `read_memory` bytes** (see raw live section).
- Callees (named when dualed): `InventoryGrid_CountItemsByCbid`, `InventoryGrid_FindFreeSlot`, `CVOGReaction_RemoveInventoryItem`, give helper `FUN_005310a0` (cbid+qty, `ret 8`).

## Annotated control flow

```c
// this = character
// pObj  = inventory object with clone type at +0x38 and def blob at +0x3c
// qty   = requested exchange count

if (pObj == NULL) return 0;

cargoHolder = *(this + 0x250);
if (cargoHolder == NULL) return 0;
cargo  = *(cargoHolder + 0x2b0);   // DropResponse type-1 grid
if (cargo == NULL) return 0;
locker = *(this + 0xcbc);          // DropResponse type-3 grid
if (locker == NULL) return 0;

if (*(pObj + 0x38) != 0x1a) return 0;   // clone/object type gate
blob = *(pObj + 0x3c);
if (blob == NULL) return 0;

ingredientCbid = *(blob + 0x4c0);
// Dual-grid sum (includeBroken=0 both)
have = CountItemsByCbid(cargo, ingredientCbid, 0)
     + CountItemsByCbid(locker, ingredientCbid, 0);
if (qty > have) return 0;

outCount = (uint8)(blob + 0x4c9) * qty;   // product multiplier
outCbid  = *(pObj + 0x34);
if (outCount <= 0 || outCbid == -1) return 0;

// Space probe on CARGO only, whole grid (page = -1)
// InvSizeX/Y from product/recipe blob +0x406/+0x407 (same footprint family as Place/Find)
if (!FindFreeSlot(cargo, InvSizeX, InvSizeY, &ox, &oy, -1)) return 0;

// Consume ingredient: cargo first, remainder from locker
removed = RemoveInventoryItem(cargo, ingredientCbid, qty);
if (qty - removed > 0)
    RemoveInventoryItem(locker, ingredientCbid, qty - removed);

// Grant product stacks (FUN_005310a0 loops qty; sets stack via vtbl+0x260)
FUN_005310a0(character_ctx, outCbid, outCount);
return 1;
```

## Field map (type-0x1a object / blob)

| Offset | Role | Confidence |
|--------|------|------------|
| `object+0x34` | Output / product CBID | **High** (passed to give helper as cbid) |
| `object+0x38` | Type code; must be **`0x1a`** | **High** (cmp imm) |
| `object+0x3c` | Definition blob pointer | **High** |
| `blob+0x406` | InvSizeX (footprint) | **High** (shared inventory map) |
| `blob+0x407` | InvSizeY | **High** |
| `blob+0x4c0` | Ingredient CBID | **High** |
| `blob+0x4c9` | `u8` product multiplier per request unit | **High** |

## Character inventory pointers

| Offset | Role | Confidence |
|--------|------|------------|
| `char+0x250` → `+0x2b0` | Cargo `InventoryGrid*` | **High** (DropResponse type 1; Collect) |
| `char+0xcbc` | Locker `InventoryGrid*` | **High** (DropResponse type 3) |

## Open

- Product English name for type **`0x1a`** (no string/RTTI at this unit).
- Callers: **no direct xrefs** (likely vtable / reaction dispatch — residual).
- `FUN_005310a0` full qty-loop dual not owned this partition (used as sealed callee ABI `ret 8` + cbid/qty from call sites).
