# Annotated low-level: FUN_00405bd0 → StdMap_InsertOrFind_PairKey_Isnil29_EbxMap_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00405bd0` |
| VA | `0x00405bd0` |
| Canonical | `StdMap_InsertOrFind_PairKey_Isnil29_EbxMap_Inferred` |
| System | MSVC std::map unique insert-or-find (isnil@+0x29, pair key) |
| Date | 2026-08-05 (WQ9K-D dual; scaffold 2026-07-23) |

## Machine-level notes

- Body **175 B** / `0xAF`; bare **RET** (`C3`) on both exits.
- Register ABI (not stack formals): **EBX**=map*, **EAX**=value*, **ESI**=out `{it, inserted}`*, return EAX=ESI.
- **isnil@+0x29** family. Node value pair: lo@+0x10 unsigned, hi@+0x14 signed (primary).
- Walk: go right iff node ≤ key (signed hi, then unsigned lo); else left; track addLeft.
- If final addLeft: if where==leftmost → insert with addLeft=1; else predecessor via dualed `StdTree_Predecessor_Isnil29_Edx_Inferred` (`0x00409820`, EDX=&where).
- Equal path: if !(where < key) → out.it=where, inserted=0.
- Insert path: `FUN_004069f0` (undualed; EDI=map, ECX=where, stack out/addLeft/value) → inserted=1.
- Twin: thiscall RET8 `StdMap_InsertOrFind_PairKey_Isnil29_Inferred` (`0x004cbee0`) — **same role/family, different ABI**; do not merge symbols.
- Caller: `FUN_0092ff00` (TFID resolve path) builds value from coidLo/Hi + flags + tick — **host evidence only**, not product demangle for map type.
- Reject scaffold `Named_CalleeOf_Named_CalleeOf_Named_Error_Received_a_packet_of_00405bd0`.

## Pseudocode (annotated register ABI)

```c
// EBX=map*, EAX=value* (key_lo@0, key_hi@4), ESI=out*
InsertPair* StdMap_InsertOrFind_PairKey_Isnil29_EbxMap_Inferred(
    MapShell* map /*EBX*/,
    InsertPair* out /*ESI*/,
    const PairKeyVal* value /*EAX*/)
{
  Node* head = map->myhead;           // [EBX+4]
  Node* where = head;
  bool goLeft = true;
  char addLeft = 1;
  Node* node = head->parent;          // root
  if (node->isnil == 0) {             // +0x29
    do {
      where = node;
      // node <= key ? right : left  (hi signed primary, lo unsigned secondary)
      if (NodeLeKey(where, value)) {
        node = where->right; goLeft = false; addLeft = 0;
      } else {
        node = where->left;  goLeft = true;  addLeft = 1;
      }
    } while (node->isnil == 0);
  }
  if (goLeft) {
    if (where == head->left) {        // leftmost
      addLeft = 1;
      goto do_insert;
    }
    StdTree_Predecessor_Isnil29_Edx_Inferred(&where); // EDX
  }
  if (!NodeLtKey(where, value)) {     // equal / found
    out->it = where;
    out->inserted = 0;
    return out;
  }
do_insert:
  Node** p = InsertHint_Isnil29_Undualed_004069f0(map, &where, addLeft, where, value);
  out->it = *p;
  out->inserted = 1;
  return out;
}
```

## Cross-checks

| Check | Result |
|---|---|
| Live decompile ≡ raw CF | **pass** |
| isnil@+0x29 | **pass** |
| Pair hi/lo compare opcodes | **pass** |
| Pred dual EDX | **pass** (WQ9J-G) |
| Insert worker 004069f0 | **gap** (undualed) |
| Product pair English | **open** |

## Dual

- A: `docs/reconstruction/reviews/A_aa_00405bd0_StdMap_InsertOrFind_PairKey_Isnil29_EbxMap_Inferred.md`
- B: `docs/reconstruction/reviews/B_aa_00405bd0_StdMap_InsertOrFind_PairKey_Isnil29_EbxMap_Inferred.md`
