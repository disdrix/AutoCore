# Raw capture: FUN_0051de80

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051de80` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0051de80` |
| **Canonical name** | `FUN_0051de80` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0051de80(undefined4 *param_1)

{
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;
  
  puStack_8 = &LAB_009a371b;
  local_c = ExceptionList;
  ExceptionList = &local_c;
  *param_1 = &PTR_FUN_009ce1b8;
  local_4 = 0;
  if (*(char *)((int)param_1 + 0x1d) != '\0') {
    FUN_007a4480(0,"HashError:Destructor, already locked for traversal");
    FUN_007a4480(0,"VOG_DEBUG_STOP");
  }
  FUN_004cba60();
  FUN_0059c8a0();
  ExceptionList = local_c;
  return;
}
```

---

## Re-verify append — 2026-08-04 WQ9G-A OWN-ONLY

**Tools:** `batch_decompile` + `analyze_function_complete` + `read_memory` (128 B body + string/vtbl) + callers/xrefs + `get_assembly_context`. **No** `disassemble_bytes`. No Launcher.

| Claim | Result |
|---|---|
| Live decompile | **Match CF** for SEH / vtbl / lock log / two teardown callees (≡ raw 2026-07-23) |
| Body | `0x0051de80`–`0x0051deea` inclusive (**107 B** / `0x6B`); terminal bare **`C3`**; pad `CC` |
| ABI | MSVC **`__thiscall`**: ECX = skill CNDHash*; **no stack args**; void; bare `ret` |
| Vtbl | `*this = PTR_FUN_009ce1b8` (`C7 06 B8 E1 9C 00`); same install as `SkillCNDHash_Ctor_Inferred` |
| Lock | byte `@+0x1d`; if set: log strings then **continue** (no abort) — mirrors recreate soft-log |
| Strings | `"HashError:Destructor, already locked for traversal"` @ `0x00a2c270`; `"VOG_DEBUG_STOP"` @ `0x00a15844` |
| Callee 1 | `SkillCNDHash_DestroyBucketTable_Inferred` (`0x004cba60`) — `MOV ECX,ESI` thiscall |
| Callee 2 | `FUN_0059c8a0` with **`LEA ECX,[ESI+0x20]`** — freelist/slab-vector host teardown (not dualled here) |
| SEH | `PUSH -1; PUSH LAB_009a371b; FS:[0]` frame (same handler family as ctor) |
| Sole caller | `SkillCNDHash_ScalarDeletingDtor_Inferred` (`0x0051de60`) @ `0x0051de63` |
| Canonical name | `SkillCNDHash_CompleteDtor_Inferred` |

### Full body hex (107 B)

```
6A FF 68 1B 37 9A 00 64 A1 00 00 00 00 50 64 89 25 00 00 00 00
51 56 8B F1 89 74 24 04 C7 06 B8 E1 9C 00 80 7E 1D 00
C7 44 24 10 00 00 00 00 74 1B
68 70 C2 A2 00 6A 00 E8 C3 65 28 00
68 44 58 A1 00 6A 00 E8 B7 65 28 00
83 C4 10
8B CE E8 8D DB FA FF
8D 4E 20 E8 C5 E9 07 00
8B 4C 24 08 5E 64 89 0D 00 00 00 00 83 C4 10 C3
```

### Byte-corrected CF (authoritative)

```
// ECX = SkillCNDHash*; void; bare ret
SEH_enter(LAB_009a371b);
*this = &PTR_FUN_009ce1b8;              // vtbl reinstall for dtor phase
if (this->lock@+0x1d != 0) {
  log(0, "HashError:Destructor, already locked for traversal");
  log(0, "VOG_DEBUG_STOP");
  // continue — no abort
}
SkillCNDHash_DestroyBucketTable_Inferred(this);   // 004cba60
FUN_0059c8a0(&this->freelistHost@+0x20);          // LEA ECX,[ESI+0x20]
SEH_leave;
ret;  // C3
```

### Vtbl DATA seal

`read_memory` @ `0x009ce1b8`: dword0 = `0x0051de60` (scalar deleting dtor). This complete body is the **callee** of vtbl[0], not the vtbl entry itself.
