# Annotated low-level: StdVector_InsertN_Elem28 (`FUN_0068a1d0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0068a1d0` |
| VA | `0x0068a1d0`–`0x0068a49a` exclusive (**714 B** / `0x2CA`) |
| System | MSVC `vector<T>` insert-n, **POD elem 0x1c** |
| Date | 2026-07-29 (W34-S OWN-ONLY dual) |
| Canonical name | `StdVector_InsertN_Elem28` (**Inferred**) |
| Ghidra name | `FUN_0068a1d0` |

## Machine-level notes

- **ABI:** ECX=`vector*`; stack `where*`, `count`, `value*`; **`RET 0x0C`**.
- **Layout:** begin@`+4`, end@`+8`, capEnd@`+0xC` (elem stride **0x1c**).
- **Max elems:** `0x9249249` (`~0u/0x1c`); overflow → `FUN_00686070`.
- **Value local:** `rep movsd` **7 dwords** (not string IAT copy-ctor).
- **Grow:** 1.5× (`cap+(cap>>1)`), floor `size+count`; `operator_new(newCap*0x1c)`; uninit_copy prefix/suffix via `FUN_00687d10`; fill via `FUN_00688550`; `operator_delete` old (**cdecl**, continues — decompiler false-noreturn).
- **In-place:** hole (suffix short) vs mid-shift (`FUN_00687ce0` + `FUN_006860f0` assign).
- **Sibling size helper:** `StdVector_Elem28_Size` (`0x00469c50`, W33-J).
- **Contrast:** `StdVector_InsertN_BasicString` (`0x004306b0`) — same insert shape, **string** ctor/dtor helpers.
- Prefer bytes over decompiler on delete noreturn and epilogue.

## Pseudocode (annotated)

```c
// ECX = vec; stack where*, count, value*; RET 0x0C; void
void __thiscall StdVector_InsertN_Elem28(
    VectorElem28* vec,
    Elem28* where,
    uint32_t count,
    const Elem28* value)
{
  Elem28 local;
  // rep movsd 7 dwords: local = *value (POD)

  uint32_t cap = vec->begin ? (vec->capEnd - vec->begin) / 0x1c : 0;

  if (count == 0) return;

  uint32_t size = vec->begin ? (vec->end - vec->begin) / 0x1c : 0;

  if (0x9249249U - size < count)
    FUN_00686070();  // vector too long

  if (cap < size + count) {
    // grow 1.5x, floor size+count
    uint32_t newCap = (0x9249249 - (cap >> 1) < cap) ? 0 : cap + (cap >> 1);
    if (newCap < size + count)
      newCap = StdVector_Elem28_Size(vec) + count;  // FUN_00469c50

    Elem28* neu = (Elem28*)operator_new(newCap * 0x1c);
    // uninit_copy [begin, where) → neu
    // fill_n count copies of local at hole
    // uninit_copy [where, end) → after hole
    // operator_delete(old begin) if non-null  // cdecl; continues
    vec->capEnd = neu + newCap;
    vec->end = neu + size + count;
    vec->begin = neu;
    return;
  }

  // in-place: hole vs mid depending on (end-where)/0x1c vs count
  // FUN_00687d10 / FUN_0068a1a0 / FUN_00687ce0 / FUN_006860f0
}
```

## Open residual

- Product `T` English for the 0x1c POD (not sealed as basic_string).
- Nested helper plates (`00687d10` / `00688550` / `006860f0` / `00687ce0` / `00686070` / insert-one `0068a620`).
- Runtime / bit-exact / differential.
