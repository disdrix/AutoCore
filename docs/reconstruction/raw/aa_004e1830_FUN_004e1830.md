# Raw capture: FUN_004e1830

| Field | Value |
|---|---|
| **Stable ID** | `aa_004e1830` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004e1830` |
| **Canonical name** | `FUN_004e1830` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
uint __thiscall FUN_004e1830(int param_1,int *param_2,undefined4 *param_3)

{
  LPCRITICAL_SECTION lpCriticalSection;
  int iVar1;
  BOOL BVar2;
  undefined4 uVar3;
  
  iVar1 = *param_2;
  if (*(char *)(param_1 + 0x28) != '\0') {
    lpCriticalSection = (LPCRITICAL_SECTION)(param_1 + 4);
    BVar2 = TryEnterCriticalSection(lpCriticalSection);
    if (BVar2 != 0) {
      if (*param_2 == 0) {
        if (*(int *)(param_1 + 0x1c) == 0) {
          uVar3 = 0;
        }
        else {
          uVar3 = *(undefined4 *)(*(int *)(param_1 + 0x1c) + 4);
        }
        *param_3 = uVar3;
        *param_2 = *(int *)(param_1 + 0x1c);
        LeaveCriticalSection(lpCriticalSection);
        return (uint)(*(int *)(param_1 + 0x1c) == 0);
      }
      if (*(int *)(iVar1 + 8) != 0) {
        *param_2 = *(int *)(iVar1 + 8);
        *param_3 = *(undefined4 *)(*(int *)(iVar1 + 8) + 4);
        LeaveCriticalSection(lpCriticalSection);
        return 0;
      }
      *param_2 = 0;
      *param_3 = 0;
      LeaveCriticalSection(lpCriticalSection);
      return 1;
    }
  }
  return 0x80070005;
}
```

---

## W23-T dual re-verify (2026-07-29)

| Field | Value |
|---|---|
| **Agent** | W23-T OWN-ONLY |
| **Tools** | Ghidra `decompile_function` + `read_memory` (no `disassemble_bytes`) |
| **Program** | `autoassault.exe` base `0x400000` |
| **Body** | `0x004e1830`–`0x004e18dc` (**172 bytes**) |
| **Epilogue** | `ret 8` (`c2 08 00`) on all exits |
| **Live decompile** | ≡ authoritative raw body above (CF match) |

### Live callees (Ghidra)

| Symbol | Note |
|---|---|
| `TryEnterCriticalSection` | IAT via `[0x009c61a0]` |
| `LeaveCriticalSection` | IAT via `[0x009c61b8]` |

### Live callers

| Symbol | VA | Domain |
|---|---|---|
| `FUN_004d5c10` | `0x004d5c10` | weapon-from-group residual |
| `FUN_0050a7d0` | `0x0050a7d0` | loot residual |
| `FUN_0050aa80` / `KillLoot_RollTableItems_Inferred` | `0x0050aa80` | kill loot table walk |

### Live bytes (entry 64 B hex)

```
535556578b7c24148b1f8bf1807e28000f848b0000008d6e0455ff15a0619c0085c0747d833f007449837b08005574208b430889078b4b088b51048b44241c8910
```

### Role seal

**`LockedList_TryAdvanceIterator_Loot_Inferred`** — algorithm twin of `List_IterateNext` / `LockedList_TryAdvanceIterator_Inferred` (`0x004022a0`) **without SEH frame**. CS-guarded singly-linked list cursor advance used by loot/weapon table walks. Returns `0` (yielded), `1` (end), `0x80070005` (not locked / TryEnter fail).

