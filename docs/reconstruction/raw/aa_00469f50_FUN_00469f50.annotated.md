# Annotated low-level: FUN_00469f50 → StdVector_InsertN_Elem28_ValueEdx_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00469f50` |
| VA | `0x00469f50`–`0x0046a203` (692 B) |
| System | util / container (generic POD vector insert-n) |
| Date | 2026-07-29 (W34-Q dual seal) |
| Canonical | `StdVector_InsertN_Elem28_ValueEdx_Inferred` |

## Machine-level notes

- Source: raw capture + W34-Q re-verify (`decompile` + `read_memory`).
- **Bytes win** on ABI: EDX=value*, stack vec+where+count, `RET 0x0C`.
- Entry value snapshot: `mov ecx,7; rep movsd` into `local_34[7]` — ECX **not** a formal.
- Same stride/max/growth/throw policy as `00466ea0`; different helper set (`0046a350`/`0046a260`).
- Sole parent: insert-one wrapper `FUN_00469e20`.

## ABI

```
void InsertN(const Elem28* value /*EDX*/, VectorElem28* vec /*[ebp+8]*/,
             Elem28* where /*[ebp+0xC]*/, uint32_t count /*[ebp+0x10]*/);
// ret 0x0C
```

## Control flow (annotated)

1. SEH frame (`LAB_009bc8b0`); `rep movsd` ×7 from EDX → stack temp.
2. `cap` / `size` from triad `+4/+8/+0xC`.
3. if `count==0` → ret 0x0C.
4. overflow → `FUN_004540b0`.
5. **Realloc** if `cap < size+count`: 1.5×/exact; new; `FUN_0046a350` / `FUN_0046a260` / `FUN_0046a350`; delete; rewrite triad; ret 0x0C.
6. **In-place** hole/mid via same helpers + `FUN_0046a240` / `FUN_0045efe0`.
7. Restore EH; ret 0x0C.

## Pseudocode (sealed structural)

```c
// See clean: StdVector_InsertN_Elem28_ValueEdx_Inferred.cpp
// Live decompile body preserved in raw (authoritative CF text).
```

## Confidence

| Claim | Level |
|---|---|
| Body bounds + RET 0x0C | **High** |
| ABI formals | **High** |
| Insert-n algorithm | **High** |
| Helper register map | Medium |
| Product T name | Open |
