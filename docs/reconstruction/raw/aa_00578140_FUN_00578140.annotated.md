# Annotated low-level: FUN_00578140

| Field | Value |
|---|---|
| Stable ID | `aa_00578140` |
| VA | `0x00578140` |
| System | unknown |
| Date | 2026-07-23 |

## Machine-level notes

- Source: raw capture for `aa_00578140`.
- Prefer assembly when decompiler conflicts.
- Recover types for still-generic parameters via callers/xrefs.
- Map DAT_* globals and FUN_* callees in follow-up waves.

## Pseudocode (annotated copy of raw)

```c
float10 FUN_00578140(float param_1)

{
  param_1 = param_1 * DAT_00aaa7ac;
  if (param_1 < 0.0) {
    return (float10)(int)(param_1 - g_flMissionXpRoundBias) * (float10)DAT_00a0f718;
  }
  return (float10)(int)(param_1 + g_flMissionXpRoundBias) * (float10)DAT_00a0f718;
}
```

## Open questions

- Confirm calling convention and full signature against callers.
- Recover meaningful types for still-generic parameters.
