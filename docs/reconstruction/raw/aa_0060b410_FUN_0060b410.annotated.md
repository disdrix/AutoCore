# Annotated low-level: FUN_0060b410

| Field | Value |
|---|---|
| Stable ID | `aa_0060b410` |
| VA | `0x0060b410`–`0x0060b438` |
| System | heartbeat / missions-progression (pending use-object aux) |
| Date | 2026-07-29 (W26-T seal; prior scaffold 2026-07-23) |
| Proposed name | `CVOGHBPendingUseObject_Stop_Inferred` (**Probable**) |

## Machine-level notes

- Source: raw capture + live `decompile_function` / `read_memory` 2026-07-29.
- Prefer assembly when decompiler conflicts (this-adjust on `FUN_00535ed0`).
- HB subclass created by `FUN_0060b380` (size `0x2c`), stored at `char+0xca0` from `CVOGReaction_RecordFirstTimeEvent`.
- vtbl slot `+0x18` = EndOrDestroy (subclass thunk `0x0060b400` → `0x005083f0`).

## Pseudocode (annotated)

```c
// __thiscall  ECX = CVOGHBPendingUseObject* (aux at char+0xca0)
// stack char flag_cancel_pending; ret 4
void __thiscall FUN_0060b410(int *this_hb, char flag_cancel_pending)
{
  // flag!=0 AND owner present → cancel pending use-object on character (commit=0)
  if ((flag_cancel_pending != '\0') && (this_hb[6] /* +0x18 pOwnerObject */ != 0)) {
    // ECX = *(this+0x28) character*  (decomp omits this-adjust)
    FUN_00535ed0(/*this=char*/ 0);  // CVOGCharacter_ResolveUseObjectPending(commit=0)
  }
  // Always: EndOrDestroy(flag_onEnd=1, flag_hard=1)
  (**(code **)(*this_hb + 0x18))(1, 1);
  return;
}
```

## Control flow

```
Stop(flag):
  if flag && pOwnerObject(+0x18):
    ResolveUseObjectPending(character(+0x28), commit=0)
  EndOrDestroy(this, onEnd=1, hard=1)
```

## Callers

| Caller | Flag | Role |
|---|---|---|
| `CVOGReaction_RecordFirstTimeEvent` | **1** | Replace prior aux before new HB |
| `FUN_0052f260` char dtor | **1** | Tear down + cancel if owned |
| `FUN_00535ed0` resolve clear | **0** | End HB without re-entering cancel (already resolving) |

## Open questions

- Product English for this HB class (no RTTI string in this unit).
- Whether subclass overrides OnEnd (`vtbl+0x14` = empty `0x005081f0`) intentionally.
