# Annotated low-level: FUN_0073bc50 → GlyphCache_GetOrCreate_ByFontKey_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0073bc50` |
| VA | `0x0073bc50`–`0x0073be47` exclusive (**503 B**) |
| System | GlyphCache / gfxFont font-key map |
| Date | 2026-07-23 scaffold; **W37-AE dual seal 2026-08-04** |

## Machine-level notes

- **ABI:** ECX + 3 stack formals; **`RET 12`** (`C2 0C 00`); SEH `LAB_009b11c0`.
- **Role:** map get-or-create for **GlyphCache** (`operator_new(0xB8)` + W35-H placement ctor).
- **Miss:** rasterize via `FUN_00754a10(face)` then `"Arial"` @ `0x00aa267c`; fail → scalar dtor + null.
- **Hit:** return `*(node+0x2c)`.
- **Sole CODE caller:** `FUN_0099ed40` @ `0x0099ed4e` — `gfxFont.cpp` / `"Failed to create font: %s"`.
- **Map site:** `DAT_00d1f628`.
- Prefer bytes for RET 12 / stack shape; decompiler `thiscall` label ≠ "map is this".
- **No** `disassemble_bytes` used in W37-AE.

## Pseudocode (annotated)

```c
// GlyphCache_GetOrCreate_ByFontKey_Inferred
// ECX = size_or_style; stack = (map*, face_name*, key_dword_b)
int * __thiscall FUN_0073bc50(undefined4 size_a, int map, char *face, undefined4 size_b)
{
  // basic_string key = face; pack size_a/size_b into key sideband
  FUN_00463c40(map, &it);
  if (it == *(void **)(map + 8)) {           // miss (sentinel)
    p = operator_new(0xb8);
    p = p ? FUN_00754ee0(p) : 0;             // GlyphCache_PlacementCtor
    if (FUN_00754a10(face, size_a) < 0 &&
        FUN_00754a10("Arial", size_a) < 0) {
      if (p) (*p->vtbl)(1);                  // scalar deleting
      return 0;
    }
    // AddRef p; insert via FUN_004638c0
  } else {
    p = *(int **)((int)it + 0x2c);           // hit value
  }
  return p;
}
```

## Open questions

- Full dual of `FUN_00463c40` / `FUN_004638c0` node layout.
- Product English for ECX vs second key dword.
- Complete `FUN_00754a10` GDI contract (unowned).
