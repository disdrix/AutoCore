# Annotated low-level: StdMap_EraseRange_Isnil2d_DestroyStr0C_Inferred (FUN_00407f90)

| Field | Value |
|---|---|
| Stable ID | `aa_00407f90` |
| VA | `0x00407f90`–`0x00408042` exclusive (**178 B** / `0xB2`) |
| Canonical name | `StdMap_EraseRange_Isnil2d_DestroyStr0C_Inferred` |
| Ghidra name | `FUN_00407f90` |
| System | MSVC `std::_Tree` range erase (isnil@+0x2d string-key family) |
| Date | 2026-08-05 (WQ9K-B OWN dual) |

## Machine-level notes

- **Register-this:** map lives in **EDI** (callers leave it there). Not ECX `__thiscall`.
- Stack formals: **outIt**, **first**, **last** — **RET 0x0c**.
- Full-clear gate: `first == head->left && last == head`.
- Free: dualed `StdTree_FreeSubtree_Isnil2d_DestroyStr0C_Inferred` (`0x00409970`) with `ECX=map`, stack `head->parent`.
- Head reset + size@+8 = 0; `*out = *head`.
- Partial: **inlined** successor with **isnil@+0x2d** immediates (`80 7e 2d 00` / `80 78 2d 00`); then dualed `StdMap_StringKey_EraseAndRebalance_Inferred` (`0x004094c0`).
- Decompiler CF ≡ successor + erase shape; register-this ABI sealed by bytes/callers.

## Pseudocode (annotated)

```c
// EDI = Map*; stack (out, first, last); RET 0x0c
Node** StdMap_EraseRange_Isnil2d_DestroyStr0C_Inferred(
    /*EDI*/ Map* map, Node** out, Node* first, Node* last)
{
  Node* head = map->Myhead;                 // [EDI+4]
  if (first == head->left && last == head) {
    FreeSubtree_Isnil2d(map, head->parent); // CALL 00409970
    head->parent = head;
    map->size = 0;                          // [EDI+8]
    head->left = head;
    head->right = head;
    *out = head->left;
    return out;
  }
  while (first != last) {
    // inlined successor using isnil@+0x2d
    Node* cur = first;
    first = tree_successor_isnil2d(first);
    StringKey_EraseAndRebalance(map, &first, cur); // CALL 004094c0
  }
  *out = first;
  return out;
}
```

## Sealed offsets

| Field | Offset | Evidence |
|---|---|---|
| Myhead | map+4 | `MOV EAX,[EDI+4]` |
| size | map+8 | `MOV [EDI+8],0` |
| left / parent / right | node+0 / +4 / +8 | head self-link + successor walks |
| isnil | node+0x2d | immediates in body + free/erase callees |
| color (peer) | node+0x2c | dualed StringKey erase peer |
| string key | node+0x0c | DestroyStr0C free + StringKey erase |

## Open questions

- Product / English host map type for compact string-key map.
- Runtime / bit-exact / differential.
