# Raw capture: FUN_004363b0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004363b0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004363b0` |
| **Canonical name** | `FUN_004363b0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_004363b0(int param_1,int param_2,undefined4 param_3)

{
  int iVar1;
  int iVar2;
  int in_EAX;
  uint uVar3;
  uint *unaff_EDI;
  
  iVar1 = *(int *)(in_EAX + 8);
  FUN_004364b0(param_1,param_2,param_3);
  iVar2 = *(int *)(in_EAX + 8);
  uVar3 = (param_2 - iVar1 >> 2) * 0x20 + param_1;
  *unaff_EDI = uVar3 & 0x1f;
  unaff_EDI[1] = iVar2 + (uVar3 >> 5) * 4;
  return;
}
```

---

## W38-C re-verify (2026-08-04) — append only

| Field | Value |
|---|---|
| **Wave** | W38-C OWN-ONLY dual |
| **Tools** | `batch_decompile` / `analyze_function_complete` / `get_function_by_address` / `read_memory` / `get_assembly_context` / parent decompile `FUN_00765740`. **No** `disassemble_bytes` |
| **Body** | `0x004363b0`–`0x0043640f` exclusive (**95 B** / `0x5F`); next sibling `FUN_00436410` @ `0x00436410` |
| **ABI** | **EAX** = bitvec host*; **EDI** = out `{bit, dword*}` pair*; stack **`(bitAdd, dwordPos*, flagOrWidth)`** three formals; returns **EDI** in EAX; **`RET 0x0C`** (`C2 0C 00`) |
| **Live decompile** | CF shape OK; register formals `in_EAX` / `unaff_EDI` need bytes/caller seal |

### Machine seal (bytes)

```
sub esp,8
push ebx
mov ebx, eax              ; host = EAX
mov ecx, [ebx+8]          ; old begin (dword* storage @ host+8)
push ebp
xor eax, eax
push esi
mov esi, [esp+0x1c]       ; param_2 (dword pos*)
mov ebp, eax              ; ebp = 0
mov eax, [esp+0x20]       ; param_3
push eax
mov [esp+0x14], ecx       ; save old begin
mov ecx, [esp+0x1c]       ; param_1
push esi                  ; param_2
push ecx                  ; param_1
mov eax, 1                ; ensure count = 1 bit
mov ecx, ebx              ; thiscall host
call FUN_004364b0         ; ensure/insert
sub esi, [esp+0x10]       ; param_2 - old_begin
mov ebx, [ebx+8]          ; new begin (may realloc)
sar esi, 2
shl esi, 5                ; word_index * 32
sub esi, ebp              ; -0
add esi, [esp+0x18]       ; + param_1 → global bit index
xor eax, eax
mov eax, esi
mov edx, eax
shr edx, 5                ; dword index
pop esi
and eax, 0x1f             ; bit in dword
lea ecx, [ebx+edx*4]      ; dword*
pop ebp
mov [edi], eax            ; out.bit
mov [edi+4], ecx          ; out.word*
mov eax, edi              ; return out*
pop ebx
add esp, 8
ret 0x0C
```

### Full body hex (95 B)

```
83ec08538bd88b4b085533c0568b74241c8be88b44242050894c24148b4c241c5651b801000000
8bcbe8d20000002b7424108b5b08c1fe02c1e6052bf50374241833c08bc68bd0c1ea055e83e01f
8d0c935d8907894f048bc75b83c408c20c00
```

### Semantics

1. Snapshot `begin = *(host+8)`.
2. `FUN_004364b0(host, param_1, param_2, param_3)` with **EAX=1** (ensure/insert **one** bit) — thiscall ECX=host.
3. Re-read `begin'`.
4. `bitIndex = ((param_2 - begin) >> 2) * 32 + param_1`.
5. `*out = { bitIndex & 31, begin' + (bitIndex >> 5)*4 }`.
6. Return `out`.

### Sole caller

`FUN_00765740` @ `0x00765c31` (`gfxBody.cpp` plate path — storage/stream bit cursor into host).  
Context: build bit-ref from stream bit count (`*reader & 0x1f`, `base + (count>>5)`), push three args, `lea edi,[esp+…]`, `mov eax, ebp` (bitvec), call.

### Callee

`FUN_004364b0` — bitvec ensure/insert range (not OWN). Sibling locate/insert `FUN_00436410` immediately after body.

### Named plate

`BitVec_EnsureOneAndLocateRef_Inferred` — ensure one bit of capacity then write bit-reference proxy `{bit_in_dword, dword*}`.
