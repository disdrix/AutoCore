# Raw capture: FUN_0076c4d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0076c4d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x0076c4d0` |
| **Canonical name** | `FUN_0076c4d0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_0076c4d0(int param_1)

{
  undefined8 uVar1;
  
  uVar1 = rdtsc();
  *(int *)(param_1 + 8) = (int)uVar1;
  *(int *)(param_1 + 0xc) = (int)((ulonglong)uVar1 >> 0x20);
  *(undefined1 *)(param_1 + 0x18) = 1;
  return;
}
```

---

## WQ9L-D re-verify (2026-08-05) — append only

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **WQ9L-D** |
| **Tools** | `decompile_function` + `analyze_function_complete` + `read_memory` + callers/xrefs. **No** `disassemble_bytes`. **No** Launcher. |
| **Image** | `autoassault.exe` base `0x400000` |
| **Named (inferred)** | `RdtscTimer_StampAndArm_Inferred` |
| **Body** | `0x0076c4d0`–`0x0076c4f1` exclusive (**33 B** / `0x21`); terminal **`C3`** |
| **ABI** | **`__thiscall`/`__fastcall`**: ECX = timer subobject*; no stack formals; void; bare RET |
| **Classification** | leaf (no callees; intrinsic `rdtsc`) |
| **Xrefs** | **90** (DATA + code); many UNCONDITIONAL_CALL sites |
| **Callers (sample)** | `FUN_0076c500`, `HostBase_DefaultCtor` (`0x007560d0`), `CWndVehicle_CompleteDtor` (`0x00834520`), `CWndVehicle_DerivedTeardown_EDI` (`0x00833d50`), `FUN_005d9020`, `FUN_004deb20`, many UI/drive/host sites |
| **Decompile ≡ raw CF** | **Yes** (identical to 2026-07-23 body) |
| **Odd / preserve** | Stack spills RDTSC via `SUB ESP,8` then stores EAX/EDX to `this+8`/`this+0xC`; byte `this+0x18 = 1` always |

### Full-body hex (`read_memory`, 33 B)

```
83 EC 08 0F 31 89 04 24 89 54 24 04 8B 04 24 8B 54 24 04
89 41 08 89 51 0C C6 41 18 01 83 C4 08 C3
```

### Machine CF (from hex + decompile)

```
SUB ESP, 8
RDTSC
MOV [ESP], EAX
MOV [ESP+4], EDX
MOV EAX, [ESP]
MOV EDX, [ESP+4]
MOV [ECX+8], EAX      ; stamp low
MOV [ECX+0xC], EDX    ; stamp high
MOV BYTE PTR [ECX+0x18], 1  ; arm / active
ADD ESP, 8
RET
```

### Semantics (sealed)

MSVC leaf that **stamps** a 64-bit **RDTSC** into a timer/perf subobject at **`+0x08/+0x0C`** and sets **armed flag** at **`+0x18 = 1`**.

Paired with constructor/init `FUN_0076c500` (period setup) on HostBase nested `+0x10`; CWndVehicle teardown paths pass `nested+0x10` after setting nested `+0x48=1` and copying `+0x40→+0x44`.

### Rejected names

- `Named_CalleeOf_Named_gfxPerfGraph_0076c4d0` — scaffold; not product demangle.
- Product timer class English without RTTI/string — open → `_Inferred`.

### Dual

A/B filed under `RdtscTimer_StampAndArm_Inferred`. Terminal **false**.
