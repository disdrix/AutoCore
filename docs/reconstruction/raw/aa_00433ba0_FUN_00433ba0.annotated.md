# Annotated low-level: FUN_00433ba0 / BitStream_HuffmanTree_EnsureCapacity

| Field | Value |
|---|---|
| Stable ID | `aa_00433ba0` |
| VA | `0x00433ba0`–`0x00433bfe` (**94 B**) |
| System | net / TNL BitStream Huffman (tree container) |
| Date | 2026-07-29 W26-C |
| Named alias | `BitStream_HuffmanTree_EnsureCapacity` (inferred from exclusive parent + strides) |

## Machine-level notes

- **ABI:** container **this in EAX**; stack `needCount` (uint); epilogue **`RET 4`** (stdcall-style one formal). Not ECX-thiscall.
- **Container layout** (parent passes `EAX = 0x00d1eaa8`):
  - `+0` count (`DAT_00d1eaa8`)
  - `+4` capacity (`DAT_00d1eaac`)
  - `+8` buffer pointer (`DAT_00d1eab0`) — **8-byte** elements (Huffman tree nodes)
- **Grow gate:** if `capacity < needCount`, allocate larger buffer.
- **New capacity:** `need + (0x10 - (need & 0xf))` ≡ `(need & ~0xf) + 0x10` (ceil to next 16-slot boundary; always +16 when already multiple of 16).
- **Alloc size:** `newCap * 8`.
- **Relocate:** stash old buf; install new; `FUN_00433c60` copies old elems into new (register-passing helper); free old; commit buffer + capacity.
- **No-op** when already large enough.
- **Exclusive caller:** `BitStream_HuffmanBuildTables` (`FUN_0077d520`) ×2 — ensure tree cap ≥ 0x100, then ≥ count+1.
- Sister alphabet container grow is `FUN_00433c00` (elem stride **0xC**, this-in-ECX) — **not OWN** (W26-B).

## Pseudocode (annotated)

```c
// this-in-EAX; stack need; RET 4
void FUN_00433ba0(/*EAX*/ Container8* self, uint32_t needCount)
{
  // self+4 = capacity, self+8 = void* buf (8 B elems)
  if (self->capacity < needCount) {
    uint32_t newCap = needCount + (0x10 - (needCount & 0xf));
    void* neu = malloc(newCap * 8);
    void* old = self->buf;
    self->buf = neu;          // so FUN_00433c60 sees destination
    FUN_00433c60();           // copy old→new by registers (EBX=self, ESI=old, EDI=count)
    self->buf = old;
    free(old);
    self->buf = neu;
    self->capacity = newCap;
  }
}
```

## Open questions

- Product English / TNL original type name for the 8-byte container.
- Exact `FUN_00433c60` register contract (not OWN; sealed only as copy callee).
