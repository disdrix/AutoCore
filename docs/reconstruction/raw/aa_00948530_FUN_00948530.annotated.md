# Annotated low-level: Client_InitRenderEngine (`FUN_00948530`)

| Field | Value |
|---|---|
| Stable ID | `aa_00948530` |
| VA | `0x00948530`–`0x00948ba7` (**1656 B** / `0x678`) |
| System | client boot / window + render engine + Palantir |
| Date | 2026-07-29 (W31-Q re-verify) |
| Semantic name | `Client_InitRenderEngine` (product banners in `Client_InitInstance`) |

---

## Machine-level notes

- Live `decompile_function` ≡ 2026-07-23 raw CF for all sealed stages.
- Entry `55 8B EC` + SEH `LAB_009bbb25`; epilogue bare `C3` (no stack formals).
- Sole caller `Client_InitInstance` treats nonzero as hard graphics-card failure → `_exit(1)`.
- Reject scaffold plate `Named_Could_not_get_Palantir_desktop_window_00948530` as sole product name (string is one fail path only).
- Mid-body Ghidra false-noreturn on `operator_delete` inside `DAT_00d1f050` loop does **not** kill the function (bytes reach `FUN_0040d9c0` and success/fail tails).

## Signature

```c
// ECX = client app*; bare RET; 0 success / 0x80004005 E_FAIL
uint32_t __fastcall Client_InitRenderEngine(void *app);
```

## Key app offsets (sealed)

| Off | Role | Conf |
|----:|------|------|
| `+0x11` | mode/dev flag feeding `+0xa0d` / `+0xb5` | **High** (use) |
| `+0x9c1` | windowed/fullscreen-ish (also → `DAT_00afa250`) | **High** |
| `+0x9c2` | with `+0x9c1` selects non-popup style `DAT_00ca0000` | **High** |
| `+0x9c6` | passed to `FUN_007a3e90` | **High** (pass-through) |
| `+0x9ce` | optional `FUN_0092f190(1)` | **High** |
| `+0x9dd` | optional `FUN_00935fe0` | **High** |
| `+0xa0d` | fused flag (also source for `+0xb5` path) | **High** |
| `+0xa14` | u16 monitor index | **High** |
| `+0xa18/+0xa1c` | client width/height | **High** |
| `+0xb5` | derived visibility/mode byte | **High** |
| `+0xde8` | Palantir desktop host* | **High** |
| `+0xe04` | sector-map / large parent* (Palantir bundle target) | **High** |
| `+0xf38/+0xf40` | compare pair for `+0xb5` branch | **High** (use) |
| `+0x1168` | helper object* (`sizeof 0x590`) | **High** |
| `+0x3020` | published as `DAT_00d17948` on success | **High** |
| `+0x3180` | `HINSTANCE` | **High** |
| `+0x3188` | main `HWND` | **High** |
| `+0x3190..0x319c` | outer window rect (L/T/R/B) | **High** |
| `+0x31a0..0x31ac` | pre-adjust snapshot | **High** |
| `+0x31b0/+0x31b4` | window center X/Y | **High** |

## Stage annotations

1. **Monitor enum** — `EnumDisplayMonitors(0,0,LAB_00930d30,0)` fills vector `DAT_00d1a830..DAT_00d1a834` (elem size **12**).
2. **Class + window** — `"XutoAssault"` / `"Auto Assault"`; style **0x302B**; ex **0x40000**; icon **101** (`0x65`).
3. **RenderEngine** — `FUN_007a3e90` with splash `"aa.bmp"`; codes **0**=fail, **2**=repos window.
4. **Optional table rewrite** — when `DAT_00d1f050 != 0`: `FUN_0043c5f0` copies into local guarded vector; each elem slot `+4` replaced with `FUN_0040af40(new(4))` after deleting previous; then `FUN_0040d9c0`.
5. **Palantir** — stamp desktop into `*(app+0xe04)+0xe89c`; `FUN_004cda90` factory (W30-F sealed); require desktop child `+0x2c`.
6. **Success bring-up** — graphics/input/UI helpers then **return 0**.
7. **Fail** — `FUN_007a4480(1, pcVar13)` → **`0x80004005`**.

## Call graph

| Direction | Target | Notes |
|---|---|---|
| Caller | `Client_InitInstance` `0x0094a6a0` | sole; hard-fail on nonzero |
| Nested OWN (other waves) | `FUN_004cda90`, `FUN_0043c5f0`, `FUN_0040d9c0`, … | not re-owned here |
| Win32 | RegisterClass/CreateWindow/Show/Update/SetWindowPos/EnumDisplayMonitors/AdjustWindowRectEx | sealed strings |

## Open gaps

- Product C++ class for client app / `DAT_00d1a840`.
- English for flags `+0x9c1/+0x9c2/+0x9c6/+0x9ce/+0x9dd/+0x11`.
- Full product plate for `FUN_007a3e90` / `FUN_007fb0a0` / success-path helpers.
- WndProc `LAB_009484d0` body (not owned).
- Runtime / bit-exact / differential.
