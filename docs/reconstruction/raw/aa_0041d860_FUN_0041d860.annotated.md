# Annotated low-level: StdTree_Buynode_CharKey_Isnil21_Inferred (`FUN_0041d860`)

| Field | Value |
|---|---|
| Stable ID | `aa_0041d860` |
| VA | `0x0041d860`–`0x0041d8e5` inclusive (**134 B** / `0x86`) |
| System | skills-abilities / STL map-set buynode |
| Date | 2026-08-05 (R13-023 OWN dual; scaffold 2026-07-23) |
| Parent dual | `0x0041ba30` StdTree_InsertAndRebalance_CharKey_Isnil21_Inferred (R12-011) |
| Dual start | 2686 |
| Terminal | **false** |

## Machine-level notes

- Prefer assembly / `read_memory` when decompiler elides thiscall ECX on `FUN_0041c140`.
- Decompiler shows `FUN_0041c140(param_4 + 4)` only; live bytes: `LEA ECX,[ESI+0x10]` then CALL — **thiscall** on node+0x10.
- ABI is **`__stdcall` `RET 0x14`**, not thiscall on the map (ECX unused on entry).
- Sole caller is dualed always-insert `0041ba30` @ `0041ba9d` with args `(head, where, head, value*, 0)`.

## Layout (node size **0x24**)

| Offset | Field | Evidence |
|---|---|---|
| +0x00 | left | `MOV [ESI], EAX` from `[EBP+8]` |
| +0x04 | parent | `MOV [ESI+4], ECX` from `[EBP+0xC]` |
| +0x08 | right | `MOV [ESI+8], EDX` from `[EBP+0x10]` |
| +0x0C | **char key** | `MOV [ESI+0xC], CL` from `*[EBP+0x14]` |
| +0x10 | value body (vector-like; residual) | `LEA ECX,[ESI+0x10]` + `FUN_0041c140` |
| +0x20 | color (0=red / 1=black) | `MOV [ESI+0x20], DL` from `[EBP+0x18]` |
| +0x21 | isnil (forced **0**) | `MOV byte ptr [ESI+0x21], 0` |
| size | **0x24** | `PUSH 0x24` + `operator_new` |

Value residual: `FUN_0041c140` treats `this`/`src` as a growable buffer of **0x18-byte** elements (`/ 0x18` in residual ctor). Product element type **Open**.

## Pseudocode (annotated)

```c
// __stdcall RET 0x14; EAX = Node* | null
NodeCharKeyIsnil21*
FUN_0041d860(Node* left, Node* parent, Node* right,
             const CharKeyValue* value,  // *value = char key; value+4 = payload
             uint8_t color)
{
  // SEH: ExceptionList / LAB_009bd7d1
  Node* node = (Node*)operator_new(0x24);
  if (node != NULL) {
    node->left   = left;     // +0x00
    node->parent = parent;   // +0x04
    node->right  = right;    // +0x08
    node->key    = value->key; // +0x0C  (char)
    // thiscall construct payload at node+0x10 from &value->payload
    FUN_0041c140(/*this*/ &node->payload, /*src*/ &value->payload);
    node->color  = color;    // +0x20
    node->isnil  = 0;        // +0x21
  }
  return node;
}
```

## Call graph

```text
FUN_0041ba30  StdTree_InsertAndRebalance_CharKey_Isnil21_Inferred  [dualed R12-011]
  └─ FUN_0041d860  StdTree_Buynode_CharKey_Isnil21_Inferred  [OWN R13-023]
       ├─ operator_new(0x24)
       └─ FUN_0041c140  residual vector-like copy-ctor @ node+0x10  [undualed]
```

## Open questions

- Product / PDB demangle for map instantiation / value_type (char key + 0x18-elem container).
- Full dual of residual `FUN_0041c140` / `FUN_0041c250` / `FUN_00422580` (not OWN).
- Runtime / bit-exact / differential — never Runtime Confirmed this dual.
