# Annotated low-level: CVOGCharacter_CompleteRegionMissions (FUN_00536740)

| Field | Value |
|---|---|
| Stable ID | `aa_00536740` |
| VA | `0x00536740` |
| Body | `0x00536740`–`0x0053691f` (`ret 8`) |
| Canonical | `CVOGCharacter_CompleteRegionMissions` (Inferred) |
| System | missions-progression |
| Date | 2026-07-29 (dual W18-J) |

## Machine-level notes

- Source: raw `aa_00536740` + live `decompile_function` + `read_memory` (no `disassemble_bytes`).
- Live decompile body **≡** 2026-07-23 raw surface.
- Bytes win on ABI (`ret 8`, thiscall ECX) and filter immediates.

## ABI

| Slot | Role | Evidence |
|---|---|---|
| ECX / `this` | `CVOGCharacter*` | entry `mov ebx, ecx`; completed hash `this+0x538` |
| stack0 | **region** id (`uint`) | cmp `def+0x128`; sole caller pushes ESI=region |
| stack1 | **pocket** id (`uint`, **`-1` = any**) | cmp `def+0x12c` unless EDI==0xFFFFFFFF |
| return | always **1** | `mov al,1` before SEH teardown |
| epilogue | `ret 8` | `c2 08 00` |

## Control flow (annotated)

```c
// uint __thiscall CVOGCharacter_CompleteRegionMissions(char* this, uint region, uint pocket)
// 1) Build temporary intrusive list of ALL mission defs from global region-missions hash
//    (FUN_0053fff0 / Mission_EnsureRegionMissions → *wrapper hash).
//    Lock hash+0x1d; walk ordered list +0x14; value @node+8; unlock.
// 2) For each collected def:
//    if def+0x128 != region → skip
//    if pocket != -1 && def+0x12c != pocket → skip
//    resolve game blob: *(*( *(this+4)+4 ) + this + 0xac ) + 0x3c
//    if (u16)def+0x90 != (u8)game+0x532 → skip          // race/req field vs char state
//    if (u16)def+0x92 != 0xFFFF && != (u8)game+0x531 → skip  // class/faction gate
//    if completed-hash this+0x538 has key==missionId with live payload (node+8) → skip
//    else CVOGCharacter_CheckMissionPrerequisites(this, missionId, autoComplete=1, checkLevel=1)
// 3) destroy temp list; return 1
```

## Field seals

| Offset | Role in this unit |
|---|---|
| `def+0x00` | mission id (`*puVar1`) |
| `def+0x90` | u16 compared to `game+0x532` (shared ReqRace surface in prereq dual) |
| `def+0x92` | u16 ReqClass/faction; `0xFFFF` = any |
| `def+0x128` | **region** filter (param_2) |
| `def+0x12c` | **pocket** filter (param_3; -1 wildcards) |
| `char+0x538` | completed-mission CNDHash (skip when live) |
| hash `+0x1d` | traversal lock |
| hash `+0x14` | ordered-list cursor start |

## Sole caller

| Site | Notes |
|---|---|
| `0x009544aa` | Client debug chat cmd (~`0x00954400`). ECX=`*(client+0xe98)`. Toast `"Completing region: %i, pocket: %i"` @ `0x00a28a18`. Pocket defaults to `-1` before optional parse. |

## Contrast (not this unit)

| Unit | VA | Diff |
|---|---|---|
| `SearchAutoMissions` | `0x00532b60` | filters **AutoAssign** `def+0xF8`; uses Requirements+GiveMission; no region/pocket |
| `CheckMissionPrerequisites` | `0x00536540` | single-id award cascade; **this unit is bulk driver** |

## Open questions

1. Product English for `def+0x128` / `+0x12c` beyond debug “region/pocket”.
2. Exact identity of game blob field `+0x531` / `+0x532` (race/class vs continent).
3. Why build temp list before cascade (re-entrancy safety vs Requirements — Probable).
4. Runtime / bit-exact open.
