# Annotated low-level: StdVector_ConstructN_Elem0x10_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00409e20` |
| VA | `0x00409e20`–`0x00409e82` inclusive (**99 B** / `0x63`) |
| Ghidra | `FUN_00409e20` |
| Canonical | `StdVector_ConstructN_Elem0x10_Inferred` |
| Prior scaffold | `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Client_Input_PollB_00409e20` (**retired**) |
| System | util / container (elem stride **0x10**); parent chain interaction-activation / PollBoundActions nested via SortTargetList |
| Date | 2026-08-05 MEGA-044 dual seal |
| Dual | A/B **accept-with-gaps** |

## Machine-level notes

- Source: raw capture + MEGA-044 live re-verify (`decompile` ≡ raw CF; ABI sealed via `disassemble_function` + `read_memory` + call-site context).
- Prefer assembly when decompiler conflicts — **this unit's decompiler signature is incomplete** (hides ECX template, dest advance, register args into PodCopy).
- Element stride **0x10** sealed by `ADD ESI,0x10` and PodCopy of 4 dwords.
- Role = **ConstructN / uninitialized_fill_n** from a **single fixed template** (source not advanced). Not range-copy; not insert-N grow; not destroy.

## ABI (sealed)

| Slot | Role |
|---|---|
| **ECX** | `const Elem0x10* value` — template, held in EBX for whole loop |
| **[EBP+8]** | `void* dest` — start of uninitialized range; advanced +0x10/iter |
| **[EBP+0xc]** | `int count` — iteration count; decremented to 0 |
| Return | void / undefined EAX; **`RET` (`C3`)** cdecl |
| Stack clean | **Caller** `ADD ESP,0x10` (4 dwords pushed; 2 phantoms unused by body) |
| SEH | `LAB_009bc610`; state 0 during loop; `-1` on exit |

## Pseudocode (annotated)

```c
// cdecl — ConstructN for 0x10-byte POD elements
// ECX = const Elem0x10* value; stack: void* dest, int count
void StdVector_ConstructN_Elem0x10_Inferred(
    /*ECX*/ const void* value,
    /*stack*/ void* dest,
    /*stack*/ int count)
{
  // SEH frame: handler LAB_009bc610, state=0
  for (; count != 0; --count, dest = (char*)dest + 0x10) {
    // FUN_00409f70: if (dest) copy 4 dwords from *value → *dest
    PodCopy_Elem0x10(/*EAX*/ dest, /*ECX*/ value);
  }
  // SEH state=-1; unlink FS:[0]
  return; // RET (C3)
}
```

## Control flow

```text
entry → install SEH
loop_test: count==0? → teardown → RET
         else: PodCopy(dest, value); count--; dest+=0x10; goto loop_test
```

| Stage | Match raw / live / bytes |
|---|---|
| SEH install/teardown | Yes |
| count==0 early exit (`TEST/JBE`) | Yes |
| PodCopy via `00409f70` | Yes |
| dest += 0x10 / count-- | Yes (asm; decompiler elides dest advance) |
| template fixed (no source++) | Yes |
| plain RET cdecl | Yes (`C3` @ `00409e82`) |

## Call graph (this VA only dualled)

```text
FUN_00408640  StdVector_InsertN_Elem0x10_Inferred  [dualed prior]
  └─ FUN_00407000  construct-N wrapper → end = dest+count*0x10  [residual]
       └─ FUN_00409e20  StdVector_ConstructN_Elem0x10_Inferred  [OWN MEGA-044]
            └─ FUN_00409f70  PodCopy 4 dwords / 0x10  [residual]

FUN_00930360  Client_SortTargetListByViewAngle  [partition peer MEGA-023]
  └─ FUN_00409e20  (capacity-hit push count=1)  [OWN MEGA-044]
  └─ FUN_00408640  (grow path)  [dualed prior]
```

## Open questions / gaps

1. Product English type of the 0x10 POD (object*+float score pack at SortTargetList; generic triad slot elsewhere).
2. Dual seal of PodCopy `00409f70` and wrapper `00407000` residual.
3. Runtime / bit-exact / differential — not run (Terminal false).

