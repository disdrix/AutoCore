# Annotated low-level: Palantir_ScalarDeletingDtor

| Field | Value |
|---|---|
| Stable ID | `aa_004447e0` |
| VA | `0x004447e0`–`0x004447FE` exclusive (**30 B**) |
| Ghidra | `FUN_004447e0` |
| System | client / Palantir host scalar deleting destructor |
| Date | 2026-07-29 (OWN-ONLY W35-J) |

## Machine-level notes

- Source: raw capture + W35-J `read_memory` full body + vtbl `PTR_FUN_00a9f76c[0]`.
- Classic MSVC **scalar deleting destructor**: complete dtor then optional `operator_delete(this)` when `flags & 1`.
- ABI: **ECX=this**; stack **flags**; **`RET 4`**; returns **this** in EAX.
- Complete body is sealed W34-K `Palantir_CompleteDtor` (`0x00754320`) — does **not** free `this`.
- Product class **Palantir** sealed via vtbl[1] peer log (`Palantir.cpp` / `Palantir::ResetDevice`).

## Pseudocode (bytes authority)

```c
// ECX=this; stack flags; RET 4; returns this
void* __thiscall Palantir_ScalarDeletingDtor(Palantir* self, unsigned char flags)
{
  Palantir_CompleteDtor(self);   // FUN_00754320, ECX=this
  if (flags & 1)
    operator_delete(self);
  return self;
}
```

## Open questions

- Exact MSVC demangle plate (`scalar deleting destructor`).
- Runtime / bit-exact (matrix).
