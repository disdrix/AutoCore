# Raw capture: FUN_00409e20

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409e20` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409e20` |
| **Canonical name** | `FUN_00409e20` |
| **System** | util / container (elem stride 0x10; interaction-activation parent chain) |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00409e20(undefined4 param_1,int param_2)

{
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  puStack_c = &LAB_009bc610;
  local_10 = ExceptionList;
  local_8 = 0;
  ExceptionList = &local_10;
  for (; param_2 != 0; param_2 = param_2 + -1) {
    FUN_00409f70();
  }
  ExceptionList = local_10;
  return;
}
```

---

## MEGA-044 re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-044** |
| **Tools** | `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_callers` + `get_function_xrefs` + `get_assembly_context` + `analyze_control_flow` + `read_memory` |
| **Forbidden avoided** | no `disassemble_bytes` primary; no Launcher; no parent ledgers; no other VAs dualled |
| **Terminal** | **false** |

### Live decompile (re-confirm ≡ raw)

```c
void FUN_00409e20(undefined4 param_1,int param_2)

{
  void *local_10;
  undefined1 *puStack_c;
  undefined4 local_8;
  
  puStack_c = &LAB_009bc610;
  local_10 = ExceptionList;
  local_8 = 0;
  ExceptionList = &local_10;
  for (; param_2 != 0; param_2 = param_2 + -1) {
    FUN_00409f70();
  }
  ExceptionList = local_10;
  return;
}
```

**Decompiler caveats (sealed by disassembly, not by decompile alone):**

- Signature display `void FUN_00409e20(undefined4 param_1,int param_2)` is **incomplete**: `param_1` has empty storage; real template pointer is **ECX** (saved to EBX). Stack formals are **dest** @ `[EBP+8]` and **count** @ `[EBP+0xc]`.
- Loop body call `FUN_00409f70()` hides register args: **EAX=dest**, **ECX=template** (fixed).
- Decompiler omits `ADD ESI,0x10` dest advance and does not show that template/source does **not** advance (fill-from-one, not range-copy).
- Return type `void` is accurate for this VA (no meaningful EAX); plain **`RET`** (`C3`) — **cdecl**, caller cleans.

### Body bounds / size

| Item | Value |
|---|---|
| Entry | `0x00409e20` |
| Last insn | `0x00409e82` `RET` (`C3`) |
| Inclusive size | **99 B** / `0x63` |
| CF metrics | cyclomatic 3; 4 BBs; 1 loop; 1 call; 1 return |
| Next | `0x00409e83` `PUSH 0; PUSH 0; CALL …` (next function) |

### Disassembly (authoritative ABI / CF)

```
00409e20  PUSH EBP
00409e21  MOV EBP,ESP
00409e23  PUSH -0x1
00409e25  PUSH 0x9bc610              ; SEH handler LAB_009bc610
00409e2a  MOV EAX,FS:[0x0]
00409e30  PUSH EAX
00409e31  MOV dword ptr FS:[0x0],ESP
00409e38  PUSH ECX
00409e39  PUSH EBX
00409e3a  PUSH ESI
00409e3b  PUSH EDI
00409e3c  MOV dword ptr [EBP-0x10],ESP
00409e3f  MOV EBX,ECX                ; EBX = template (fixed)
00409e41  MOV EDI,dword ptr [EBP+0xc]; EDI = count
00409e44  MOV ESI,dword ptr [EBP+0x8]; ESI = dest
00409e47  MOV dword ptr [EBP-0x4],0x0; SEH state = 0
00409e4e  MOV EDI,EDI                ; align nop
00409e50  TEST EDI,EDI
00409e52  JBE 0x00409e6b             ; count==0 → exit
00409e54  MOV ECX,EBX                ; ECX = template
00409e56  MOV EAX,ESI                ; EAX = dest slot
00409e58  CALL 0x00409f70            ; PodCopy 4 dwords if EAX!=0
00409e5d  SUB EDI,0x1
00409e60  MOV dword ptr [EBP+0xc],EDI
00409e63  ADD ESI,0x10               ; dest += 0x10
00409e66  MOV dword ptr [EBP+0x8],ESI
00409e69  JMP 0x00409e50
00409e6b  MOV dword ptr [EBP-0x4],0xffffffff
00409e72  MOV ECX,dword ptr [EBP-0xc]
00409e75  MOV dword ptr FS:[0x0],ECX
00409e7c  POP EDI
00409e7d  POP ESI
00409e7e  POP EBX
00409e7f  MOV ESP,EBP
00409e81  POP EBP
00409e82  RET                        ; C3 — cdecl, no callee stack pop
```

### `read_memory` seals

| Address | Length | Notes |
|---|---|---|
| `0x00409e20` | 80 B | prologue + loop head through mid-loop; hex starts `55 8b ec 6a ff 68 10 c6 9b 00 …` |
| `0x00409e6b` | 32 B | SEH teardown + `C3` RET; next fn `6a 00 6a 00 e8…` |
| `0x009bc610` | 16 B | SEH scope table entry `b8 7c 1b ae 00 e9 …` + `CC` pad |

### Callee (evidence only — not dualled here)

`FUN_00409f70` @ `0x00409f70` (leaf PodCopy 0x10):

```
TEST EAX,EAX
JZ  done
; *EAX = *ECX  (4 dwords / 0x10 bytes)
RET
```

Xrefs to `00409f70`: from `00409e58` (this) and `0040a6a8` (`FUN_0040a670`).

### Callers / xrefs (live)

| Site | Parent | Kind |
|---|---|---|
| `0x0040700c` | `FUN_00407000` | UNCONDITIONAL_CALL |
| `0x009305a5` | `FUN_00930360` | UNCONDITIONAL_CALL |

**Xref count:** 2.

#### Call site `0040700c` (`FUN_00407000` construct-N wrapper)

```
MOV EAX,[ESP+4]          ; stack formal of wrapper
PUSH EAX
PUSH ECX
MOV ECX,[ESP+0xc]        ; ECX := template (wrapper arg)
PUSH ESI                 ; count (live reg into ConstructN)
PUSH EDI                 ; dest  (live reg into ConstructN)
CALL 0x00409e20
MOV EAX,ESI
SHL EAX,0x4
ADD ESP,0x10             ; cdecl clean 4 dwords
ADD EAX,EDI              ; return dest + count*0x10
RET 4
```

Into `00409e20`: **ECX=template**, **[EBP+8]=EDI=dest**, **[EBP+0xc]=ESI=count**. Two phantom stack slots pushed (unused by body). Parent of wrapper: insert-N `FUN_00408640` @ `0x00408746` / `0x0040880c`.

#### Call site `009305a5` (`FUN_00930360` SortTargetListByViewAngle)

```
PUSH EAX
PUSH ECX                 ; phantoms
PUSH 0x1                 ; count = 1
PUSH ESI                 ; dest = current end
LEA ECX,[ESP+0x40]       ; template = stack local (object* + score pack)
CALL 0x00409e20
ADD ESP,0x10
ADD ESI,0x10             ; advance end by one elem
```

Capacity-hit path: when free slots remain, construct **one** 0x10 element in-place; else grow via `FUN_00408640` insert-N.

### Sealed contract

```text
// cdecl ConstructN / uninitialized_fill_n for 0x10-byte POD
// ECX      = const Elem0x10* value  (fixed template; not advanced)
// [EBP+8]  = void* dest
// [EBP+0xc]= int count
// callers push 4 dwords and ADD ESP,0x10 (2 phantoms)
// RET (C3) — no meaningful return value from this VA
// loop: while count>0: PodCopy0x10(dest, value); dest+=0x10; count--
// SEH: LAB_009bc610, state 0 in loop, -1 on exit
```

### Named reconstruction (this wave)

| Kind | Name |
|---|---|
| Meaningful | `StdVector_ConstructN_Elem0x10_Inferred` |
| Prior scaffold (retire) | `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Client_Input_PollB_00409e20` |
| Ghidra | `FUN_00409e20` |

### Dual verdict (summary)

| Path | Verdict |
|---|---|
| A fidelity | **accept-with-gaps** |
| B adversarial | **accept-with-gaps** |

Gaps: product POD English type; dual seal of PodCopy `00409f70` / wrapper `00407000` residual; runtime/bit-exact open. Not reject.

