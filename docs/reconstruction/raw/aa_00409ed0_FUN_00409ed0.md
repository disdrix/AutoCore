# Raw capture: FUN_00409ed0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00409ed0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00409ed0` |
| **Canonical name** | `FUN_00409ed0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __fastcall FUN_00409ed0(undefined4 *param_1,undefined4 *param_2)

{
  undefined4 *in_EAX;
  
  while (param_2 != param_1) {
    in_EAX[-6] = param_1[-6];
    in_EAX[-5] = param_1[-5];
    in_EAX[-4] = param_1[-4];
    in_EAX[-3] = param_1[-3];
    in_EAX[-2] = param_1[-2];
    in_EAX[-1] = param_1[-1];
    param_1 = param_1 + -6;
    in_EAX = in_EAX + -6;
  }
  return;
}
```

---

## MEGA-054 live re-verify (2026-08-05) — APPEND ONLY

| Field | Value |
|---|---|
| **Agent** | OWN-ONLY dual **MEGA-054** |
| **Tools** | Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_callers` / `get_function_xrefs` + `get_assembly_context` + `read_memory` |
| **Forbidden avoided** | `disassemble_bytes` primary; Launcher; parent ledgers; other VAs dualled |
| **Image** | `autoassault.exe` base `0x400000` |

### Live decompile (re-confirm = scaffold raw)

```c
void __fastcall FUN_00409ed0(undefined4 *param_1,undefined4 *param_2)

{
  undefined4 *in_EAX;
  
  while (param_2 != param_1) {
    in_EAX[-6] = param_1[-6];
    in_EAX[-5] = param_1[-5];
    in_EAX[-4] = param_1[-4];
    in_EAX[-3] = param_1[-3];
    in_EAX[-2] = param_1[-2];
    in_EAX[-1] = param_1[-1];
    param_1 = param_1 + -6;
    in_EAX = in_EAX + -6;
  }
  return;
}
```

### Body bounds / bytes

- **VA range:** `0x00409ed0`–`0x00409f0a` inclusive (**59 B** / `0x3B`); pad `CC` at `0x00409f0b+`.
- **Leaf:** no callees; classification `leaf`.
- **Hex (`read_memory` length 64, body 59):**
  `3bd1743653565783e91883e8183bca8bf18b1e8bf8891f8b5e04895f048b5e08895f088b5e0c895f0c8b5e10895f108b761489771475d05f5e5bc3`

### Disassembly (`disassemble_function` — not `disassemble_bytes`)

```text
00409ed0  CMP  EDX,ECX
00409ed2  JZ   0x00409f0a          ; empty range: ret (EAX unchanged = dest_end = dest_begin)
00409ed4  PUSH EBX
00409ed5  PUSH ESI
00409ed6  PUSH EDI
00409ed7  SUB  ECX,0x18            ; step src end back one 0x18 element
00409eda  SUB  EAX,0x18            ; step dest end back one 0x18 element
00409edd  CMP  ECX,EDX
00409edf  MOV  ESI,ECX
00409ee1  MOV  EBX,dword ptr [ESI]
00409ee3  MOV  EDI,EAX
00409ee5  MOV  dword ptr [EDI],EBX
00409ee7  MOV  EBX,dword ptr [ESI+0x4]
00409eea  MOV  dword ptr [EDI+0x4],EBX
00409eed  MOV  EBX,dword ptr [ESI+0x8]
00409ef0  MOV  dword ptr [EDI+0x8],EBX
00409ef3  MOV  EBX,dword ptr [ESI+0xc]
00409ef6  MOV  dword ptr [EDI+0xc],EBX
00409ef9  MOV  EBX,dword ptr [ESI+0x10]
00409efc  MOV  dword ptr [EDI+0x10],EBX
00409eff  MOV  ESI,dword ptr [ESI+0x14]
00409f02  MOV  dword ptr [EDI+0x14],ESI
00409f05  JNZ  0x00409ed7          ; flags from CMP ECX,EDX (MOVs preserve)
00409f07  POP  EDI
00409f08  POP  ESI
00409f09  POP  EBX
00409f0a  RET                      ; RET 0 — no stack args
```

### ABI seal (registers — decompiler incomplete)

| Formal | Register | Role |
|---|---|---|
| dest_end | **EAX** in | one-past-last dest slot |
| src_end | **ECX** (`param_1`) | one-past-last source element |
| src_begin | **EDX** (`param_2`) | first source element (stop after stepping back to it) |
| return | **EAX** out | dest_begin after reverse walk (`std::copy_backward` return) |
| stack | none | **`RET`** (not `RET n`) |

Decompiler shows `void` + phantom `in_EAX`; live assembly + call sites seal EAX dest/return. No null-dest skip (unlike forward twin `FUN_00442b00`).

### Semantics

MSVC-style **copy_backward** of **0x18-byte POD** elements (6 dwords / `NPCSkillSetEntry` stride):

1. If `EDX == ECX` ? empty; return immediately.
2. Else repeatedly: `ECX-=0x18; EAX-=0x18;` copy 6 dwords `[ECX]?[EAX]`; until `ECX == EDX`.

Forward twin: dualed **`SkillSet_UninitializedCopy_0x18`** @ `0x00442b00` (add +0x18, null-dest skip).

### Callers / xrefs (5 UNCONDITIONAL_CALL)

| Caller | Call site | Pre-call register setup (assembly context) |
|---|---|---|
| `FUN_00412730` | `0x004129c0` | `MOV EAX,EDI`; `MOV ECX,[EBP-0x24]`; `MOV EDX,ESI` |
| `FUN_00440f80` | `0x004411fc` | `MOV EAX,[EBP+0xc]`; ECX from prior path; `MOV EDX,EBX`; prior `CALL 0x00442b00` |
| `FUN_00442760` | `0x004429df` | `MOV ECX,EDI`; `MOV EDX,EBX`; prior `CALL 0x00442b00` |
| `FUN_00454f30` | `0x004551ba` | `MOV ECX,[EBP+0x8]`; `MOV EDX,EBX` |
| `FUN_0046bba0` | `0x0046be30` | `MOV EAX,EDI`; `MOV EDX,ESI` |

All five are SkillSet vector insert/reallocate parents (scaffold `Skill_Uses_SkillSet_GetEntryCount_*` family). Shared with forward copy `00442b00` at several sites.

### Callees

**None** (leaf).

### Completeness notes (analyze_function_complete)

- `return_type_resolved: false` / warning: do not trust decompiler void — EAX holds dest_begin.
- `phantom_count: 1` (`in_EAX`) — filled as dest_end/return.
- `has_calling_convention: false` in Ghidra metadata; sealed as custom register triple (EAX/ECX/EDX) from bytes + callers.

### Naming

| Candidate | Ruling |
|---|---|
| `FUN_00409ed0` | Scaffold only |
| `Named_CalleeOf_Skill_Uses_SkillSet_GetEntryCount_00409ed0` | **Retire** — chain-of-caller scaffold; wrong role |
| GetEntryCount / count helper | **Reject** — real GetEntryCount is `0x00402d80` |
| Forward `UninitializedCopy` merge | **Reject** — reverse walk; no null skip |
| `SkillSet_CopyBackward_0x18` | **Accept** — 0x18 POD reverse range copy; SkillSet parent family sealed |

### CF fidelity

Live decompile = scaffold raw body; assembly seals stride, direction, empty exit, RET 0. Terminal **false** (no runtime Confirmed).
