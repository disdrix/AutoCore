# Annotated low-level: LogicUiEventQueue_InsertOne_GrowPath

| Field | Value |
|---|---|
| Stable ID | `aa_004068f0` |
| VA | `0x004068f0`–`0x00406964` |
| Ghidra name | `FUN_004068f0` |
| Canonical name | `LogicUiEventQueue_InsertOne_GrowPath` |
| System | client-logic-ui / event queue |
| Date | 2026-07-29 (W22-I seal) |

## Machine-level notes

- Source: live Ghidra `decompile_function` + `read_memory` (no `disassemble_bytes`).
- Sole caller: `Client_SendLogicUiPacket` (`0x004059f0`) capacity-overflow / reallocate path.
- Element stride **0x138** matches Logic-UI event records (`Client_DispatchUiEventQueue` stride note).
- Callee `FUN_004076a0` is the full insert-N / grow helper (realloc, move, construct).
- This unit only: **save index → insert 1 → recompute slot pointer** (iterator invalidation safe).
- Twin pattern of `CombatFloaterQueue_InsertOne_GrowPath` (`0x00402e20`) with stride 0x38.

## Signature (sealed)

```c
// __thiscall, RET 0xC
// ECX = queue* (begin@+4, end@+8, capacity_end@+0xC)
// stack0 = void** outSlot
// stack1 = void* insertAt (usually current end from caller)
// stack2 = payload (forwarded to insert-N)
void LogicUiEventQueue_InsertOne_GrowPath(
    void* this,
    void** outSlot,
    void* insertAt,
    uint32_t payload);
```

| Formal | Source | Conf |
|---|---|---|
| this | ECX → EDI | **High** |
| outSlot | Stack[0x4] | **High** |
| insertAt | Stack[0x8] → EBX | **High** |
| payload | Stack[0xC] | **High** |
| cleanup | `RET 0xC` | **High** |

## Pseudocode (annotated)

```c
void __thiscall LogicUiEventQueue_InsertOne_GrowPath(
    int this, int *outSlot, int insertAt, uint32_t payload)
{
  int index;
  int begin = *(int *)(this + 4);

  if (begin != 0) {
    if ((*(int *)(this + 8) - begin) / 0x138 != 0) {
      index = (insertAt - begin) / 0x138;   // magic 0xD20D20D3
      goto after_index;
    }
  }
  index = 0;
after_index:
  FUN_004076a0(insertAt, /*count*/ 1, payload);  // thiscall this=queue
  *outSlot = *(int *)(this + 4) + index * 0x138;
  return;
}
```

## Bytes (full 116 B)

```
538b5c240c56578bf98b770485f6741e8b4f082bceb8d3200dd2f7e903d1c1fa088bc2c1e81f03c289442414750433f6eb178bcb2bceb8d3200dd2f7e903d1c1fa088bf2c1ee1f03f28b4c2418516a01538bcfe8580d00008b47048d14b603d203d203d22bd65f8d0cd08b44240c5e89085bc20c00
```

- Div `/0x138`: imul magic `0xD20D20D3`, sar 8, sign-fix.
- Mul `*0x138`: `lea edx,[esi+esi*4]` → ×5; three `add edx,edx` → ×40; `sub edx,esi` → ×39; `lea ecx,[eax+edx*8]` → ×312.

## Open questions

- Product English name of the queue owner type (session Logic-UI ring vs dedicated queue object).
- Whether `payload` is by-value copy of first dword only vs pointer into 0x138 blob (callee `FUN_004076a0` copies via `FUN_00408880` / construct helpers — residual on callee).
- Runtime / bit-exact not run.
