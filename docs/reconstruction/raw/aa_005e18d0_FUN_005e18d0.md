# Raw capture: FUN_005e18d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_005e18d0` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x005e18d0` |
| **Canonical name** | `FUN_005e18d0` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_005e18d0(int param_1,undefined4 *param_2,int *param_3)

{
  undefined4 uVar1;
  bool bVar2;
  int *piVar3;
  undefined4 *puVar4;
  undefined4 *puVar5;
  
  piVar3 = param_3;
  puVar5 = *(undefined4 **)(param_1 + 4);
  bVar2 = true;
  if (*(char *)((int)puVar5[1] + 0x19) == '\0') {
    puVar4 = (undefined4 *)puVar5[1];
    do {
      puVar5 = puVar4;
      bVar2 = *param_3 < (int)puVar5[3];
      if (bVar2) {
        puVar4 = (undefined4 *)*puVar5;
      }
      else {
        puVar4 = (undefined4 *)puVar5[2];
      }
    } while (*(char *)((int)puVar4 + 0x19) == '\0');
  }
  param_3 = puVar5;
  if (bVar2) {
    if (puVar5 == (undefined4 *)**(int **)(param_1 + 4)) {
      puVar5 = (undefined4 *)FUN_005e13b0(&param_3,1,puVar5,piVar3);
      uVar1 = *puVar5;
      *(undefined1 *)(param_2 + 1) = 1;
      *param_2 = uVar1;
      return;
    }
    FUN_005ae050();
  }
  if (param_3[3] < *piVar3) {
    puVar5 = (undefined4 *)FUN_005e13b0(&param_3,bVar2,puVar5,piVar3);
    *param_2 = *puVar5;
    *(undefined1 *)(param_2 + 1) = 1;
    return;
  }
  *(undefined1 *)(param_2 + 1) = 0;
  *param_2 = param_3;
  return;
}
```

---

## W30-L re-verify (2026-07-29) — append only

**Tools:** Ghidra `decompile_function` + `get_function_by_address` + `read_memory` + xrefs/callers/callees + `analyze_function_complete`. **No** `disassemble_bytes`.

### Live decompile

Live 2026-07-29 ≡ raw 2026-07-23 CF (root walk, leftmost insert, predecessor, insert-or-equal out-pair).

### Bounds / ABI (bytes)

| Item | Value |
|------|-------|
| Body | `0x005e18d0`–`0x005e1988` exclusive (**184** B / `0xB8`) |
| Exits | three paths; each ends **`C2 08 00`** (`ret 8`); pad `CC` |
| ABI | `__thiscall` ECX=map; stack `(InsertPair* out, int* value)`; void |

Epilogue (`read_memory` @ `0x005e1960`):

```
… e84bfaffff 8b08 8b442414 5f5e 8908 c6400401 5d 59 c20800  ; insert path inserted=1
8b442414 5f5e c6400400 8910 5d 59 c20800 cccccccc           ; equal path inserted=0
```

### Key compare (signed)

Walk: `3B 50 0C` + **`0F 9C C1`** (`setl`) — **signed** less.  
Equal gate: `3B 45 00` + **`7D`** (`jge`) — **signed**.

Key at value dword0 / node `+0x0C` (`node[3]`). isnil@`+0x19`.

### Xrefs

- **Callees:** `FUN_005e13b0` (always-insert+rebalance Val12, buynode `005ae220`, max `0x15555553`), `FUN_005ae050` (StdTree_Dec_Val12 / predecessor)
- **Callers (1):** `FUN_005e0610` @ `0x005e06db` (mission/reaction path; also calls `CVOGReaction_FailMissionNotify`)
