# Annotated low-level: Item_SetBroken (`FUN_00513de0`)

| Field | Value |
|---|---|
| Stable ID | `aa_00513de0` |
| VA | `0x00513de0` |
| System | `inventory-transfer` |
| Date | 2026-07-23 scaffold; dual refine 2026-07-29 |

## Machine-level notes

- Source: raw capture for `aa_00513de0` + live re-decompile / `read_memory` 2026-07-29.
- Prefer assembly when decompiler conflicts (`extraout_ECX` is artifact; ECX preserved).
- **Sealed write mask:** `0x80000` = bit 19 = `ItemFlag_Broken`.
- Dirty on change: `FUN_00512670` → `| 0x20000` self + `+0xac` chain.
- Post-set: `test [ecx+0x17e], 8` then **tail jmp** `[vtbl+0x90]` with stack arg forced 0.
- ABI: thiscall ECX=item, 1 stack arg, **`ret 4`**.

## Pseudocode (annotated)

```c
// Item_SetBroken / FUN_00513de0
// ECX = Item*; stack desiredBroken (low byte); ret 4
void __thiscall Item_SetBroken(int *item, uint desiredBroken)
{
  int *self = item;

  // Compare current bit19 vs desired low byte (not only bit0 of desired)
  if (((uint)self[0x5f] >> 0x13 & 1) != (desiredBroken & 0xff)) {
    FUN_00512670();           // mark dirty 0x20000 (+ chain); this still ECX
    self = item;              // decompiler extraout_ECX — bytes reuse ECX
  }

  // SEALED: mask 0x80000 — set bit19 := (desiredBroken & 1)
  self[0x5f] = self[0x5f] ^ (((desiredBroken & 0xff) << 0x13 ^ self[0x5f]) & 0x80000);

  // byte +0x17e bit3 == flags bit19
  if ((*(byte *)((int)self + 0x17e) & 8) != 0) {
    // WARNING: decompiler "call"; bytes are tail jmp [vtbl+0x90], arg=0
    (**(code **)(*self + 0x90))();
    return;
  }
  return;
}
```

## Field map

| Offset | Access | Meaning |
|--------|--------|---------|
| `item+0x17c` | dword R/W | flags; bit 19 = `ItemFlag_Broken` (**mask `0x80000`**) |
| `item+0x17e` | byte test `&8` | same bit19 via third byte of flags dword |
| `item+0xac` | (in callee) | next in dirty-propagation chain |
| vtbl `+0x90` | tail jmp | notify/hook when Broken **set** (product name open) |

## Byte anchors (`read_memory` 2026-07-29)

| VA | Hex | Note |
|----|-----|------|
| `00513de0` | `8b817c010000` | load flags |
| `00513e04` | `81e600000800` | **`and esi, 0x80000`** |
| `00513e0a` | `31b17c010000` | store xor flags |
| `00513e10` | `f6817e01000008` | test Broken set |
| `00513e24` | `ffa290000000` | jmp vtbl+0x90 |
| `00513e2a` | `c20400` | ret 4 |

## Open questions

- Product name of vtable slot `+0x90`.
- Full caller census (samples only).
- Whether any caller passes desired values other than 0/1.
