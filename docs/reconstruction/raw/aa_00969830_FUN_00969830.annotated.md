# Annotated low-level: FUN_00969830

| Field | Value |
|---|---|
| Stable ID | `aa_00969830` |
| VA | `0x00969830`–`0x00969897` exclusive (**103 B**) |
| System | assManager / `StringVecShell` path list (`DAT_00d1f1fc`) |
| Date | 2026-07-23 scaffold; W31-E seal 2026-07-29 |
| Canonical | `StringVec_PushFront_EnsureTrailingDirSep` (**Inferred**) |

## Machine-level notes

- Source: raw + W31-E decompile + full-body hex + caller call-site bytes.
- Ghidra `unaff_ESI` is real ABI: callers do `mov esi, [DAT_00d1f1fc]` before the call.
- `StringVecShell`: `begin@+0x04`, `end@+0x08`, element stride **0x1c** (MSVC `basic_string`).
- Insert position is **`*(ESI+4)` = begin** → front insert (push_front semantics).
- `FUN_009694e0` normalizes: if non-empty and last char not `:` `/` `\`, append `/`; else copy.
- SEH: `LAB_009acaf6`; local string EH states 0 then -1; IAT dtor at `[0x009c62f4]`.
- Epilogue: `add esp, 0x28; ret 4`.

## Pseudocode (annotated)

```c
// ESI = StringVecShell* (*DAT_00d1f1fc); stack = const basic_string* path; ret 4
void StringVec_PushFront_EnsureTrailingDirSep(StringVecShell* self /*ESI*/,
                                              const basic_string* path /*stack*/)
{
  basic_string local;
  FUN_009694e0(&local, path);           // ensure trailing dir separator
  basic_string* out_it;                 // stack scratch (return iterator)
  // thiscall insert-at: pos = begin, count=1, value=local
  FUN_00430310(self, &out_it, self->begin, &local);
  local.~basic_string();
}
```

## Callers

| Site | Context |
|---|---|
| `FUN_007b75b0` @ `0x007b770c` | After precompile path ensure; push normalized `../effects[/precompile]` path |
| `FUN_007b75b0` @ `0x007b77cb` | Walk `DAT_00d1e570`..`0xd1e7a0` step `0x1c` under resolver-A arm |

## Open questions

- Product English for the path list (search/include roots vs cache dirs).
- Nested helpers `FUN_009694e0`, `FUN_00430310` product plates (not owned).
- Runtime / bit-exact / differential — open.
