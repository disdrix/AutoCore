# Annotated low-level: FUN_005715d0 → InventoryGrid_CanPlaceItem

| Field | Value |
|---|---|
| Stable ID | `aa_005715d0` |
| VA | `0x005715d0`–`0x0057161d` |
| System | inventory-transfer |
| Date | 2026-07-23 scaffold; **2026-08-04 dual seal** |

## Machine-level notes (2026-08-04)

- **ABI:** `__thiscall` grid in ECX (`PUSH ECX` prologue); stack `item, originX, originY, conflictOut`; **`RET 0x10`**.
- **Role:** InvSize-aware CanPlace probe only — **no** cell mutation.
- **InvSize:** `item+0xa8` → host `+0x3c` blob → `+0x406/+0x407`.
- **Handoff:** `InventoryGrid_CanPlace` `0x00570840` with this=grid.
- **Callers:** equip / UI place probes (`008012f0`, `00941b20`, `00952bc0`, `00955010`, …).
- Decompiler omits thiscall display — **bytes seal ECX**.

## Pseudocode (annotated)

```c
uint32_t __thiscall CanPlaceItem(InventoryGrid* grid, Item* item,
                                 uint32_t originX, uint32_t originY,
                                 uint32_t* conflictOut)
{
  if (!item) return 0;
  uint8_t* blob = *(uint8_t**)(*(uint8_t**)(item + 0xa8) + 0x3c);
  return CanPlace(grid, blob[0x406], blob[0x407], originX, originY, conflictOut);
}
```

## Open questions

- Product/PDB English.
- Non-null conflictOut path from this wrapper (callers often pass 0).
