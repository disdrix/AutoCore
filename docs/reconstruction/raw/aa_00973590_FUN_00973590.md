# Raw capture: FUN_00973590

| Field | Value |
|---|---|
| **Stable ID** | `aa_00973590` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00973590` |
| **Canonical name** | `FUN_00973590` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00973590(float param_1,float param_2,float param_3,float param_4)

{
  int iVar1;
  undefined1 *in_EAX;
  undefined4 *puVar2;
  undefined4 local_8;
  undefined4 local_4;
  
  local_4 = 0xff;
  local_8 = 0;
  if ((int)ROUND(param_1) < 0) {
    puVar2 = &local_8;
  }
  else {
    puVar2 = &local_4;
    if ((int)ROUND(param_1) < 0x100) {
      puVar2 = &param_1;
    }
  }
  in_EAX[2] = *(undefined1 *)puVar2;
  local_8 = 0xff;
  local_4 = 0;
  if ((int)ROUND(param_2) < 0) {
    puVar2 = &local_4;
  }
  else {
    puVar2 = &local_8;
    if ((int)ROUND(param_2) < 0x100) {
      puVar2 = &param_1;
    }
  }
  in_EAX[1] = *(undefined1 *)puVar2;
  local_4 = 0xff;
  if ((int)ROUND(param_3) < 0) {
    puVar2 = &param_2;
  }
  else {
    puVar2 = &local_4;
    if ((int)ROUND(param_3) < 0x100) {
      puVar2 = &param_1;
    }
  }
  *in_EAX = *(undefined1 *)puVar2;
  iVar1 = (int)ROUND(param_4);
  if (iVar1 < 0) {
    in_EAX[3] = 0;
    return;
  }
  if (0xff < iVar1) {
    in_EAX[3] = 0xff;
    return;
  }
  param_1._0_1_ = (undefined1)iVar1;
  in_EAX[3] = param_1._0_1_;
  return;
}
```

---

## W29-I re-verify (2026-07-29) — append only

**Tools:** `decompile_function` / `batch_decompile`, `analyze_function_complete`, `get_function_by_address`, `read_memory`, `get_xrefs_to` / `get_bulk_xrefs`. **No** `disassemble_bytes`.

| Seal | Value |
|---|---|
| Body | `0x00973590`–`0x00973680` exclusive (**240 B / `0xF0`**) |
| Pad | `CC` after epilogue |
| Epilogue | `add esp,0xC` then **`ret 0x10`** (`C2 10 00`) |
| Class | **leaf** (no `E8` callees; FPU ROUND/`fistp` only) |
| Dest ABI | **EAX** = `uint8_t *dest` (`in_EAX`; callers `mov eax,ecx` before call) |
| Stack | four `float` args (f0..f3); callee cleans **16** bytes |
| Channel map (bytes) | `dest[2]=clamp(f0)`, `dest[1]=clamp(f1)`, `dest[0]=clamp(f2)`, `dest[3]=clamp(f3)` |
| Clamp | per channel: `ROUND(f)` then select **0** / **value** / **0xFF** via lea-of-constant pattern (not branchy store) for RGB; A uses three early-ret paths |
| Live decompile ≡ 2026-07-23 raw body | **Yes** for CF/channel indices |
| Decompiler gaps | mid-channel clamp pointer labels alias stack slots (`&param_1` / `&param_2` when out of range) — **bytes supersede**; algorithm is still 0..255 clamp |
| Callers (7 named + more xrefs) | `FUN_008e4430`, `FUN_00923c50`, `FUN_00973690`, `FUN_00973820`, `FUN_00973770`, `FUN_0073d200` (×5), `FUN_00973880`, plus sites in `0079ba*`, `007ad3*`, `008547*`, `00913f75` (**20** code xrefs) |

Canonical dual name: **`Color_ClampRoundPackBGRA_Inferred`**.
