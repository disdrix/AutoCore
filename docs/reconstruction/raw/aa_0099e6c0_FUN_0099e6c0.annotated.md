# Annotated: FUN_0099e6c0 → PalVisibleBoundingShape_EffectMaterialInit_Inferred

| Field | Value |
|---|---|
| **Stable ID** | `aa_0099e6c0` |
| **VA** | `0x0099e6c0`–`0x0099e894` (**469 B**) |
| **Canonical** | `PalVisibleBoundingShape_EffectMaterialInit_Inferred` |
| **Ghidra** | `FUN_0099e6c0` |
| **Wave** | W36-H |
| **Classification** | **worker** (post-ctor typed init) |

---

## Signature

```c
// ECX = mesh host (0xC4 object after MeshHost0xC4_BaseCtor + dual-vtbl override)
// Returns bitwise OR of stage status codes (0 = all OK paths that returned 0)
uint32_t __fastcall PalVisibleBoundingShape_EffectMaterialInit_Inferred(void *self /* ECX */);
```

## Semantics

1. SEH frame `LAB_009b2d4b`.
2. Load effect path **`PalVisibleBoundingShape.fx`** via `FUN_00989e00` → bind through `FUN_009701d0` into resource slot `self+0x20`.
3. Build temp material/desc object (`FUN_0098ef00` + color `DAT_00d1a6c0..c8` + `g_flOne` + `FUN_0098f2b0(200)` + `FUN_00749d10`).
4. Resolve four material params on `*(self+0x20)`:
   - `SolidMatDiffuse`
   - `SolidMatEmissive`
   - `WireMatDiffuse`
   - `WireMatEmissive`  
   Missing handle → status `0xFFFFFFFF`; else `FUN_0096fff0(handle, paramIndex)`.
5. Refcount swap: `FUN_0073d940(*handle)` → AddRef/first-use vfunc(+4) / Release old vfunc(+8) / store new.
6. Destroy temp (`FUN_00457a00`); return `statusFx | statusMat | s0 | s1 | s2 | s3`.

## Call graph context

- **Sole caller:** `HostPtrTable12_Bootstrap_EDI` (`FUN_0073c810`) slot 0:
  1. `new(0xC4)`
  2. `FUN_00748960` base ctor
  3. dual vtbl override `PTR_FUN_00aa37b4` / `PTR_LAB_00aa37a8`
  4. **this init**
- Sibling slot inits: `FUN_0099e4a0`, `FUN_0099e2c0`, `FUN_0099e0a0`, `FUN_0099d060`, `FUN_0099eb60`, `FUN_0099f450` (different FX plates; unowned).

## Gaps

- Product class English for dual-vtbl override pair `00aa37b4` / `00aa37a8`.
- Exact contracts of unowned FX/material helpers.
- Runtime / bit-exact / differential.
