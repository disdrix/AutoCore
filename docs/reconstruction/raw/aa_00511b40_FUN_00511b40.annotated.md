# Annotated low-level: StdMap_InsertOrFind_IntKey_Isnil21_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00511b40` |
| VA | `0x00511b40`–`0x00511bf8` inclusive (**185 B** / `0xB9`) |
| Ghidra | `FUN_00511b40` |
| Canonical | `StdMap_InsertOrFind_IntKey_Isnil21_Inferred` |
| System | skills-abilities partition / generic MSVC `_Tree` map insert-or-find (isnil@+0x21) |
| Date | 2026-08-05 (R13-031 OWN dual; dual start 2686) |
| Parent | `0x00511950` StdTree_InsertAndRebalance_Node24_Isnil21_Inferred (R12-014) |
| Terminal | **false** |

## Machine-level notes

- Prefer assembly when decompiler conflicts (none material here — decompile ≡ raw ≡ live).
- **ABI:** `__thiscall` — ECX = map shell; stack `(out pair*, value*)`; **`ret 8`**.
- **Layout (Node24 family):** left@+0, parent@+4, right@+8, **signed int key@+0x0C**, payload@+0x10.., color@+0x20, **isnil@+0x21**; node size **0x24** (from dualed insert buynode).
- **Shell:** head @ map+4; size @ map+8 (size not read here).
- **Out pair:** `it` @ +0, `inserted` bool @ +4 (`C6 40 04 00/01`).
- **Walk:** classic MSVC lower-bound; `SETL` seals **signed** key compare.
- **Insert:** dualed `StdTree_InsertAndRebalance_Node24_Isnil21_Inferred` @ `0x00511950` (addLeft=1 at begin; else goLeft flag).
- **Pred:** residual `FUN_005adff0` (isnil21 predecessor / iterator--) when goLeft and not begin.
- **Equal:** `hint->key >= value->key` after pred → `inserted=0`, keep existing node; **no mapped rewrite**.
- **Callers:** sole `FUN_0050ba90` @ `0x0050bbc4` (host map insert of int-keyed value blob with vector payload) — product English open.

## Pseudocode (annotated)

```c
// ECX = map; stack out*, value*; ret 8
void __thiscall StdMap_InsertOrFind_IntKey_Isnil21_Inferred(
    MapShell *map, InsertPair *out, const int *value /*key at [0]*/)
{
  MapNode *head = map->head;                 // [map+4]
  MapNode *where = head;
  bool goLeft = true;

  MapNode *node = head->parent;              // root
  if (node->isnil == 0) {                    // isnil @ +0x21
    MapNode *child = node;
    do {
      where = child;
      goLeft = (*value < where->key);        // key @ +0x0C; SETL signed
      child = goLeft ? where->left : where->right;
    } while (child->isnil == 0);
  }

  MapNode *hint = where;

  if (goLeft) {
    if (where == head->left) {               // begin() — leftmost
      // insert with addLeft=1
      MapNode **pNew = StdTree_InsertAndRebalance_Node24_Isnil21_Inferred(
          map, &hint, 1, where, value);
      out->it = *pNew;
      out->inserted = 1;
      return;
    }
    FUN_005adff0(&hint);                     // predecessor
  }

  if (hint->key < *value) {                  // open slot
    MapNode **pNew = StdTree_InsertAndRebalance_Node24_Isnil21_Inferred(
        map, &hint, (char)goLeft, where, value);
    out->it = *pNew;
    out->inserted = 1;
    return;
  }

  // equal key — find path
  out->inserted = 0;
  out->it = hint;
}
```

## Open questions

- Product / MSVC demangle for mapped value_type (parent: int key + vector of 0x10 elements).
- Dual of predecessor `FUN_005adff0` and sole caller host `FUN_0050ba90`.
- Runtime / bit-exact / differential — open (Terminal false).
