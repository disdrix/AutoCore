# Annotated low-level: FUN_0061f940 → CVOGHBSkill_Virus_Ctor

| Field | Value |
|---|---|
| Stable ID | `aa_0061f940` |
| VA | `0x0061f940`–`0x0061f998` |
| System | skills-abilities |
| Date | 2026-08-04 WQ9D-B dual seal |
| Proposed name | `CVOGHBSkill_Virus_Ctor` |

## Machine-level notes

- **Class (Confirmed):** RTTI `.?AVCVOGHBSkill_Virus@@` @ type_info `0x00af2b08`.
- **Vtbl:** installs `PTR_FUN_009d14bc`; slot +0x0c = `CVOGHBSkill_Virus_OnHeartBeat` (`0x0061fdf0`).
- **Object size:** consumer `operator_new(0x6e0)` in OnHeartBeat.
- **ABI:** MSVC `__thiscall`; 8 stack args; **`ret 0x20`**; returns `this` in EAX.
- **Construction sequence:**
  1. `FUN_00618180(p2..p7, 0, 1.0f)` — shared skill-HB intermediate ctor (sets base vtbl then fields).
  2. `this+0x6d0 = remainBudget` (param_8) — Virus multi-spread budget (OnHeartBeat decrements).
  3. `*this = PTR_FUN_009d14bc` — override vtbl to Virus.
  4. `this+0x6d4 = remainPeriod` (param_9).
  5. `CVOGHBBase_SetPeriodAndCounter(this, remainPeriod, true)`.
- **Sole direct CALL** from Virus OnHeartBeat child-spawn path (after gather allies).
- **DATA xrefs** to vtbl also from other Virus lifecycle helpers (`FUN_0061fa40` etc.) — not OWN.

## Pseudocode (annotated)

```c
// __thiscall; ret 0x20
CVOGHBSkill_Virus* CVOGHBSkill_Virus_Ctor(
    CVOGHBSkill_Virus *this,
    void *pPrimaryTarget,   // param_2
    void *pSkillBlob,       // param_3  (caller: parent+0x24 / parent+9 dwords)
    void *pWorld,           // param_4
    void *pNewTarget,       // param_5
    void *pTfidArg,         // param_6
    uint32_t arg7,          // param_7  (caller: parent[0x1aa])
    int remainBudget,       // param_8 → this+0x6d0
    int remainPeriod)       // param_9 → this+0x6d4 + SetPeriodAndCounter
{
  FUN_00618180(pPrimaryTarget, pSkillBlob, pWorld, pNewTarget, pTfidArg, arg7,
               /*u8*/0, /*float*/1.0f);
  this->nSpreadBudget = remainBudget;           // +0x6d0
  this->pVTable = &PTR_FUN_009d14bc;
  this->nPeriodSeed = remainPeriod;             // +0x6d4
  CVOGHBBase_SetPeriodAndCounter(this, remainPeriod, true);
  return this;
}
```

## Open questions

1. Product English for Virus skill beyond RTTI class name.
2. Full field English for base ctor args / `arg7` / skill blob at parent+0x24.
3. Dual of intermediate base `FUN_00618180` / pulse `FUN_0061b6f0` (other OWN partitions).
4. Runtime / bit-exact open.
