# Annotated low-level: FUN_00538ab0 → DisciplineDefMap_ExactFindByIntKey_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00538ab0` |
| VA | `0x00538ab0`–`0x00538b17` |
| System | `skills-abilities` |
| Inferred name | `DisciplineDefMap_ExactFindByIntKey_Inferred` |
| Dual | R13-037 OWN-ONLY 2026-08-05 (dual start 2686) |
| Parent | `0x0052b040` DisciplineDefMap_TryCopyPayloadById_Inferred |
| Date | 2026-08-05 |

## Machine-level notes

- **ABI:** `__thiscall` ECX=map header; stack `Node **pOut`, `int *pKey`; dual exits **`RET 8`** (`C2 08 00`).
- **Leaf:** no callees; read-only (stores only through `*pOut`).
- **Head/end:** `*(this+4)` — image end symbol `DAT_00b045b4` when map is the global DisciplineDef table.
- **Root:** `head[1]` (`*(head+4)` parent link).
- **Nil:** byte `@node+0x20d == 0` means real node; non-zero stops walk (sentinel).
- **Key:** signed int `@node+0x0C` (`node[3]`).
- **Children:** left `*node` (+0); right `node+8` (+8). Parent link at +4 not walked here.
- **Walk:** classic lower_bound — if `node.key < *pKey` go right; else candidate=node, go left.
- **Exact gate:** if candidate ≠ head **and** `candidate.key <= *pKey` → hit (equality under walk invariant); else miss writes head.
- **Return shape:** writes node* into `*pOut`; EAX ends as `pOut` address so callers do `MOV EAX,[EAX]` for the node (see parent `0x0052b055`).
- **Domain:** all 9 image callers `MOV ECX,0x00b045b0` before call — DisciplineDef map only in this binary.
- **Distinct:** SkillDefMap find `0x00418890` (nil `+0x641`); nearby `0x00538a40` Map_FindByIntKey (nil `+0x49`, key `+0x10`); `0x0051c150` (nil `+0x29`, key `+0x10`).
- Prefer assembly when decompiler prints `void` without noting EAX=`pOut`.

## Pseudocode (annotated)

```c
// void* __thiscall DisciplineDefMap_ExactFindByIntKey_Inferred(
//     MapHeader *this,   // ECX — image callers: 0x00b045b0
//     Node **pOut,       // stack +4
//     int *pKey);        // stack +8
// ret 8; *pOut = hit node or end; EAX = pOut on return

void __thiscall FUN_00538ab0(int param_1,undefined4 *param_2,int *param_3)
{
  undefined4 *puVar1;  // walk cursor
  undefined4 *puVar2;  // next child
  undefined4 *puVar3;  // lower_bound candidate (init = head)

  puVar3 = *(undefined4 **)(param_1 + 4);           // head / end
  if (*(char *)((int)puVar3[1] + 0x20d) == '\0') {  // root not nil
    puVar1 = (undefined4 *)puVar3[1];               // root = head->_Parent
    do {
      if ((int)puVar1[3] < *param_3) {              // key @ +0x0C
        puVar2 = (undefined4 *)puVar1[2];           // right @ +8
      }
      else {
        puVar2 = (undefined4 *)*puVar1;             // left @ +0
        puVar3 = puVar1;                            // candidate
      }
      puVar1 = puVar2;
    } while (*(char *)((int)puVar2 + 0x20d) == '\0');
  }
  if ((puVar3 != *(undefined4 **)(param_1 + 4)) && ((int)puVar3[3] <= *param_3)) {
    *param_2 = puVar3;                              // exact hit
    return;
  }
  *param_2 = *(undefined4 **)(param_1 + 4);         // miss → end
  return;
}
```

## Layout (this unit)

```
Map header (this)                 Node
  +0x04  head/end node*             +0x00  left
                                    +0x04  parent
                                    +0x08  right
                                    +0x0C  int key
                                    +0x10  payload start (0x1FC for DisciplineDef — caller)
                                    +0x20D isnil byte
```

## Open questions

- Product/PDB method English for map type (keep `_Inferred`).
- Whether any non-image path reuses this helper on a different header with same nil geometry (none in static xrefs).
- Full node fields beyond key/links/nil/payload base.
- Runtime / bit-exact / cold map contents.
