# Annotated low-level: StdMap_InsertOrFindByIntKey_Val12 (`FUN_0053a1a0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0053a1a0` |
| VA | `0x0053a1a0` |
| Canonical | `StdMap_InsertOrFindByIntKey_Val12` (**Inferred**) |
| System | std map insert-or-find (Val12 / int key @ value+0) |
| Date | 2026-07-29 (W28-P; supersedes 2026-07-23 scaffold annotate) |

## Machine-level notes

- **Body:** `0x0053a1a0`–`0x0053a259` exclusive (**185** B / `0xB9`); pad `CC`.
- **ABI:** `__thiscall` ECX=map*; stack `(InsertPair* out, Val12* value)`; **`ret 8`** (three exits, all `c2 08 00`).
- **InsertPair:** `out->it` @ +0; `out->inserted` as **byte @ +4** (`mov byte [eax+4], 0/1`).
- **Key:** signed int compare `*value` vs `node[3]` (**+0x0C** first dword of 3-dword Val12 payload).
- **Walk:** start at `head->parent` (root); stop on **isnil@+0x19**; track last "went left" as `addLeft`.
- **Miss insert:** `FUN_00539210` / `StdTree_InsertAndRebalance_Val12` (W27-Q) with `(outIt, addLeft, where, value)`.
- **Predecessor on left-insert not-leftmost:** `FUN_005ae050` (bidirectional `--` on iterator).
- **Hit:** `inserted=0`, `it=where` (equal key).
- **Sole caller:** `FUN_0053a8e0` hint-insert path falls back here when hint is not usable.

## Pseudocode (annotated)

```c
// __thiscall; RET 8
// InsertPair = { Node* it; bool inserted; }  // inserted at +4
void StdMap_InsertOrFindByIntKey_Val12(MapShell* map, InsertPair* out, const Val12* value)
{
  Node* head = map->head;
  Node* where = head;
  bool addLeft = true;
  if (!head->parent->isnil) {
    Node* cur = head->parent;
    do {
      where = cur;
      addLeft = (*value /* int key */ < where->key /* node[3] */);
      cur = addLeft ? where->left : where->right;
    } while (!cur->isnil);
  }
  Node* parent = where;
  if (addLeft) {
    if (where == head->left) {
      // insert as new leftmost
      Node** p = StdTree_InsertAndRebalance_Val12(map, &parent, 1, where, value);
      out->it = *p;
      out->inserted = 1;
      return;
    }
    FUN_005ae050(&where); // predecessor
  }
  if (where->key < *value) {
    Node** p = StdTree_InsertAndRebalance_Val12(map, &parent, addLeft, where, value);
    out->it = *p;
    out->inserted = 1;
    return;
  }
  out->inserted = 0;
  out->it = where;
  // ret 8
}
```

## Ghidra residual

- Surface return type `void` — real contract is write-through `out` + `ret 8`; EAX not product.
- `FUN_00539210` decompiler shows return as `undefined4*` used as iterator out-slot; matches W27-Q always-insert.
- Product map type / English name open; algorithm identity is MSVC `_Tree::insert` unique path.
