# Annotated low-level: FUN_0076c130 → Debug_OutputDebugStringFmt_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_0076c130` |
| VA | `0x0076c130`–`0x0076c16b` exclusive (**59 B** / `0x3B`) |
| System | debug / OutputDebugString |
| Date | 2026-08-04 (W38-Y) |
| Canonical | `Debug_OutputDebugStringFmt_Inferred` (**Inferred**) |

## Machine-level notes

- Source: raw capture + W38-Y re-verify (`read_memory` full body, no `disassemble_bytes`).
- **cdecl varargs**: format at `[ESP+4]`; remaining stack = `va_list`.
- Frame: `SUB ESP,0x800` (2048-byte buffer); epilogue `ADD ESP,0x800; RET`.
- Null format: `TEST EAX,EAX` / `JZ` skips format+ODS.
- IAT: `_vsnprintf` (`0x009c64b4`), `OutputDebugStringA` (`0x009c61a4`).
- Callers clean stack (`ADD ESP,4/8/C/20` depending on arg count).

## Pseudocode (annotated)

```c
// cdecl varargs; void; plain RET
void Debug_OutputDebugStringFmt_Inferred(const char *format, ...)
{
  char buf[2048];

  if (format == NULL)
    return;

  _vsnprintf(buf, 0x800, format, /* va_list */ &format + 1);
  OutputDebugStringA(buf);
}
```

## Call graph (summary)

- **78** xrefs; worker (import calls only).
- High-value consumers: `GfxDevice_Reset` D3D HRESULT ERROR map; voice capture fail strings; login fail; pack/texture diagnostics.

## Open questions

- Product/PDB English (was it `DebugPrint` / `ODS` / custom name?).
- Whether `_vsnprintf` always NUL-terminates under all MSVCRT builds (standard CRT does when count > 0).
- Runtime / bit-exact.
