# Annotated low-level: Client_Camera_ApplyLookOffset (`FUN_0091a5f0`)

| Field | Value |
|---|---|
| Stable ID | `aa_0091a5f0` |
| VA | `0x0091a5f0` |
| System | `input-drive-control` (camera / look apply) |
| Date | 2026-07-29 (dual A/B seal; supersedes 2026-07-23 scaffold notes) |
| Dual | `reviews/A_aa_0091a5f0_*` / `reviews/B_aa_0091a5f0_*` |

## Machine-level notes

- Source: live Ghidra `decompile_function` `0x0091a5f0` (2026-07-29) matches 2026-07-23 raw body CF.
- Prefer assembly when decompiler conflicts; raw body is authoritative for CF.
- **ESI** = look-helper object (DCT `piStack_a8`: character or vtbl`+0x1d8` camera object) — decompiler `unaff_ESI`.
- Stack: `lookScale` (float), `lookVec` (float*).
- Callees: basis extract (`004e8a40` forward, `004e8ad0` right), safe acos (`0040d0a0`), half-angle quat (`00567ce0`), world offset (`004e8bf0`), gated apply (`004c3a40`).
- **Not** a drive-axis writer. Soft `0.2` DAT reuse is damp-only here (≠ soft-steer ±0.5).

## Sealed constants (`read_memory`)

| DAT | f32 | Role |
|-----|----:|------|
| `DAT_00aaa668` | −1.0 | clamp / fold sign |
| `DAT_00aaa6fc` | 25.0 | scale rate |
| `DAT_00aaa7dc` | ≈2π | hemisphere wrap |
| `DAT_00aaa948` | ≈0.99999 | mode low |
| `_DAT_00aaa9bc` | ≈1.00001 | mode high |
| `DAT_00a0f70c` | 0.2 | zero-xz damp |
| `DAT_00a0f298` | 0.5 | half-angle (callee) |
| `DAT_00afda60..68` | (0,0,1) | world Z |
| `_DAT_00afda70..78` | (1,0,0) | world X |

## Pseudocode (annotated)

```c
void Client_Camera_ApplyLookOffset(float lookScale, float *lookVec)
// ESI = pHelper (look-helper object*)
{
  float dtLike = *(float *)(DAT_00d09874 + 0x30);

  if (pHelper == 0 || *(int *)(pHelper + 8) == 0)
    return;

  // SECTION A — orientation when lookScale != 1.0
  if (lookScale != g_flOne) {
    int rb = *(int *)(*(int *)(pHelper + 8) + 0x3c);
    float quat[4] = { rb+0x30 .. +0x3c };

    float forward[4];
    FUN_004e8a40(quat, forward);                    // +Z basis

    float pitchDot = forward · worldZ(0,0,1);       // DAT_00afda60..68
    pitchDot = clamp(pitchDot, -1, 1);
    float pitch = safe_acos(pitchDot);              // FUN_0040d0a0; nonfinite → 0

    FUN_004e8ad0(quat, tmpRight);                   // +X basis (side product)
    if (forward · worldX(1,0,0) < 0)                // DAT_00afda70..78
      pitch = TAU - pitch;                          // DAT_00aaa7dc

    // fold scale about 1.0
    float sign = +1, folded = lookScale;
    if (lookScale > 1) { sign = -1; folded = 2 - lookScale; }
    if (folded < -1) folded = -1;
    float ang = safe_acos(folded);                  // nonfinite → 0 contribution

    // angle = dtLike * 25 * ang * sign + pitch
    FUN_00567ce0(&DAT_00afda50, dtLike * 25.0f * ang * sign + pitch);
    FUN_00404dc0();
  }

  // SECTION B — mode byte ESI+0x14c
  uint8_t mode;
  if (lookScale < ~0.99999f)       mode = 1;
  else if (lookScale <= ~1.00001f) mode = 0;
  else                             mode = 0xFF;
  *(uint8_t *)(pHelper + 0x14c) = mode;

  // SECTION C — look vector world apply
  if (lookVec[0] != 0 || lookVec[2] != 0 || *(char *)(pHelper + 0x304) != 0) {
    if (*(pHelper+8) && type_vtbl(rb, +0x18) == 6) {
      FUN_0053d970();
      CVOGSpawnPoint_SetObjectActiveState(..., active=1, flag=0);
    }
    pos  = rb ? rb+0xb0 : entity_local+0x84;
    quat = rb ? rb+0x30 : entity_local+0x94;
    FUN_004e8bf0(pos, quat, lookVec, out);          // out = pos + R*lookVec
    FUN_004c3a40(out);

    if (lookVec[0] == 0 && lookVec[2] == 0) {
      // damp: scale forward * 0.2, zero pos, apply, return (no sticky set)
      FUN_004e8a40(quat, forward);
      forward *= 0.2f;                              // DAT_00a0f70c
      zero_pos; FUN_004c3a40(); return;
    }
    *(uint8_t *)(pHelper + 0x304) = 1;              // sticky look-active
  }
}
```

## Offsets (helper object)

| Off | Role | Confidence |
|----:|------|------------|
| `+8` | object / physics link | **High** CF |
| `+0x14c` | look mode byte (0 / 1 / 0xFF) | **High** CF |
| `+0x304` | sticky look-active char | **High** CF |
| `+4` | multi-inherit base ptr (for fallbacks) | **High** CF |

## Open questions

- ESI C++ type name; lookVec yaw/pitch labels; lookScale product meaning; `DAT_00d09874+0x30` field; `FUN_00567ce0` full stack wiring; type id 6 product name; runtime.
