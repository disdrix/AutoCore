
# Raw capture: FUN_00404e00

| Field | Value |
|---|---|
| **Stable ID** | `aa_00404e00` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00404e00` |
| **Canonical name** | `FUN_00404e00` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void __thiscall FUN_00404e00(int param_1,undefined1 *param_2)

{
  if ((*(char *)(param_1 + 0x40) != '\0') && (*(int *)(param_1 + 8) != 0)) {
    *param_2 = 0;
    return;
  }
  *param_2 = 1;
  return;
}
```

---

## MEGA-049 live re-verify (2026-08-05) — APPEND ONLY

**Agent:** OWN-ONLY dual **MEGA-049**  
**Tools:** Ghidra `decompile_function` + `analyze_function_complete` + `disassemble_function` + `get_function_callers` + `get_function_xrefs` + `get_assembly_context` + `read_memory`. **No** `disassemble_bytes`. No Launcher.  
**Integrity:** Scaffold body above **unchanged**; this section only.

### Live decompile (≡ scaffold CF)

```c
void __thiscall FUN_00404e00(int param_1,undefined1 *param_2)

{
  if ((*(char *)(param_1 + 0x40) != '\0') && (*(int *)(param_1 + 8) != 0)) {
    *param_2 = 0;
    return;
  }
  *param_2 = 1;
  return;
}
```

### Body range / hex (`read_memory` 40 B @ `0x00404e00`)

| Item | Value |
|---|---|
| Start | `0x00404e00` |
| Last insn | `RET 4` @ `0x00404e20` (`C2 04 00`) |
| Inclusive end | `0x00404e22` |
| Size | **35 B** / `0x23` |
| Pad after | `CC CC CC CC CC` @ `0x00404e23` |
| Hex | `8A 41 40 84 C0 74 11 83 79 08 00 74 0B 8B 44 24 04 32 C9 88 08 C2 04 00 8B 44 24 04 B1 01 88 08 C2 04 00` |

### Assembly seal (`disassemble_function`)

```text
00404e00  MOV AL,byte ptr [ECX + 0x40]
00404e03  TEST AL,AL
00404e05  JZ  0x00404e18          ; not fully-ready path
00404e07  CMP dword ptr [ECX + 0x8],0x0
00404e0b  JZ  0x00404e18          ; not fully-ready path
00404e0d  MOV EAX,dword ptr [ESP + 0x4]   ; out*
00404e11  XOR CL,CL                       ; *out = 0  (fully ready)
00404e13  MOV byte ptr [EAX],CL
00404e15  RET 0x4
00404e18  MOV EAX,dword ptr [ESP + 0x4]   ; out*
00404e1c  MOV CL,0x1                      ; *out = 1  (not fully ready)
00404e1e  MOV byte ptr [EAX],CL
00404e20  RET 0x4
```

### ABI seal

| Slot | Source | Conf |
|---|---|---|
| `this` (CVOGPhysics*) | **ECX** | **High** |
| `out` (uint8_t*) | stack `[ESP+4]` after entry | **High** |
| cleanup | **`RET 4`** ×2 exits | **High** |
| EAX on return | = out pointer (loaded before store) | **High** |
| Callees | **none** (leaf) | **High** |

### Fully-ready polarity (sealed)

| Condition | `*out` | English (behavioral) |
|---|---|---|
| `byte(this+0x40) != 0` **AND** `dword(this+0x08) != 0` | **0** | fully ready |
| else | **1** | not fully ready / soft path allowed |

Same dual fields as dualed `CVOGPhysics_SetRotation` (`0x00404dc0`) fully-ready skip (there: skip body dispatch when fully ready).

### Callers / xrefs (live)

| From | In function | Type |
|---|---|---|
| `0x005d0fb6` | `FUN_005d0d60` `Skill_ApplyBoundStatusMotionTable_Inferred` | UNCONDITIONAL_CALL |
| `0x005d80a5` | `FUN_005d7f70` `CVOGHBAICreatureBase_DoLogic` | UNCONDITIONAL_CALL |

**xref_count = 2**. Classification: **leaf**.

### Call-site ECX seal (`get_assembly_context`)

**Site `0x005d0fb6` (skill status motion):**
```text
MOV ESI, [EDI + 0x64]     ; entity host
MOV ECX, [ESI + 0x8]      ; phys = entity+8
LEA EDX, [ESP + 0xf]      ; out-byte
PUSH EDX
CALL 0x00404e00
CMP byte ptr [EAX], 0
JZ  skip_soft_heading
```

**Site `0x005d80a5` (AI DoLogic):**
```text
MOV EDX, [ESI + 0x64]     ; entity host (param_1[0x19])
MOV ECX, [EDX + 0x8]      ; phys = entity+8
; CMP ECX,0 / JZ skip (null phys guard at caller)
LEA EAX, [ESP + 0x13]
PUSH EAX
CALL 0x00404e00
CMP byte ptr [EAX], BL    ; BL=0
JZ  skip_state_branch
```

Both sites: **ECX = `*(entity+0x08)`** = CVOGPhysics this; out-byte stack temp; callers branch on `*out != 0` (not fully ready → soft path).

### Naming gate (MEGA-049)

| Evidence | Present? |
|---|---|
| Leaf out-byte writer; no callees | **Yes** |
| Fields `+0x40` / `+0x08` match CVOGPhysics fully-ready | **Yes** (dualed SetRotation family) |
| Call-site ECX = entity+8 phys | **Yes** (2/2) |
| Polarity: fully ready → *out=0 | **Yes** |
| Product / PDB English symbol | **No** |
| Partition hint RecvSkillStatusEffect | nested via `005d0d60` only; body is physics probe |

**Promoted name:** `CVOGPhysics_WriteNotFullyReadyOut_Inferred`  
**Retire:** `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Cli_00404e00`

### Decompiler notes

- Signature shows `void`; EAX still holds out* — callers treat return as `char*`.
- Decompiler of parents may drop/scramble this; assembly seals ECX=phys.

### Gaps (open)

1. Product English for `+0x40` / `+0x08` beyond behavioral "fully ready".
2. Runtime / bit-exact / differential.
3. Whether any non-function DATA xref exists (none in live function xrefs).

**Terminal:** false (no runtime Confirmed).

