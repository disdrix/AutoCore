# Annotated low-level: FUN_00514ff0 / Object_PurgeSkillHBListChain_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00514ff0` |
| VA | `0x00514ff0` |
| Structural name | `Object_PurgeSkillHBListChain_Inferred` |
| System | skills-abilities / object HB bookkeeping |
| Date | 2026-07-29 (W19-N dual seal) |

## Machine-level notes

- **ABI sealed by body bytes:** `__thiscall` — object in **ECX**; one stack dword (predicate ctx byte host); epilogue **`RET 4`**.
- Decompiler `__fastcall(int)` + `FUN_00514ff0(0)` without ECX rebind is **noise** — prefer bytes.
- List host is **pointer** at `this+0xB0` (`mov ecx,[esi+0xB0]`), not embedded list (`lea`).
- Linked child/parent object pointer at `this+0xAC`; recurse always pushes **0** (does not forward caller ctx).
- Predicate `LAB_00512eb0` is not a Ghidra function; sealed from `read_memory` + RTTI strings.
- Sibling loop form: `FUN_00515030` (flag `+0x26`, predicate `LAB_00512f00`, walks `+0xAC` chain in a `do/while`).

## Sealed control flow

```c
// void __thiscall Object_PurgeSkillHBListChain_Inferred(Object* this, uint8_t* /*or byte host*/ pCtx)
// RET 4

void __thiscall FUN_00514ff0(int this, /* stack */ int stackArg)
{
  // stackArg address is passed as ctx* to the list predicate (lea of the stack slot).
  if (*(unsigned char *)(this + 0x25) != 0) {
    // thiscall list = *(this + 0xB0)
    CNDDoubleList_InvokePredicateAndRemove_Inferred(
        *(void **)(this + 0xB0),
        (code *)0x00512eb0,   // LAB_00512eb0 — CVOGHBSkillBase stop/remove
        &stackArg,            // ctx* (caller typically passes 0 or 1 as the dword)
        /*nullPayloadFlag=*/0);
  }
  {
    int linked = *(int *)(this + 0xAC);
    if (linked != 0) {
      FUN_00514ff0(linked, 0);  // ECX=linked; stack arg forced 0
    }
  }
}
```

## Predicate `LAB_00512eb0` (context only; not OWN VA)

| Step | Action |
|---|---|
| 1 | Require `payload+0x1c == 1` (CVOGHBSkillBase type tag) |
| 2 | `__RTDynamicCast` Base→SkillBase |
| 3 | Require `hb+0x44 != 0` |
| 4 | If `*ctx != 0`: `hb+0x6a4 = 1` |
| 5 | `hb->vtbl+0x18(1, 0)` (Stop/End family) |
| 6 | Return 1 → list unlinks + destroys node |

## Offsets (this object)

| Off | Role | Conf |
|---|---|---|
| `+0x25` | gate byte — non-zero enables list purge | **High** |
| `+0xB0` | `CNDDoubleList*` (of HB payloads) | **High** |
| `+0xAC` | linked object* for chain recurse | **High** |

## Open questions

- Product English method name (keep `_Inferred`).
- Exact English for `+0x25` / `+0xAC` / `hb+0x44` / `hb+0x6a4`.
- Whether stack arg is formally `uint8_t` vs int (only low byte read by predicate).
- Runtime / bit-exact image diff.
