# Raw capture: FUN_00970b20

| Field | Value |
|---|---|
| **Stable ID** | `aa_00970b20` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00970b20` |
| **Canonical name** | `FUN_00970b20` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00970b20(void)

{
  FUN_0043e8f0();
  FUN_0043e7f0();
  return;
}
```

---

## W33-N live seal (2026-07-29)

### Tools

- `batch_decompile` / `decompile_function` @ `0x00970b20`
- `read_memory` 64 B @ entry (body 54 B used)
- `get_function_by_address` → body `00970b20`–`00970b55`
- `analyze_function_complete` + `get_xrefs_to`
- **No** `disassemble_bytes`

### Full body hex (`read_memory` 54 B)

```
83ec088b1156576a00518bc4896424108d7424108910e8b5ddacff8b7c241c83c4085081c7ac000000e8a2dcacff5f5e83c408c20400
```

### ABI (bytes supersede decompiler)

- **ECX** = key holder; key = `*(uint32_t*)ecx` (`8B 11` then stored as pod dword).
- Stack formal 1: **host***; **`RET 0x4`** (`C2 04 00`).
- Local pod built at `[esp+0x10]` via `LEA ESI,[esp+0x10]` → adapter `FUN_0043e8f0` → `PodU32U8_StoreSplit`.
- Flag imm: **`6A 00`** (flag = 0).
- Container: `MOV EDI,[esp+0x1c]` (host) then **`ADD EDI,0xAC`** before `CALL FUN_0043e7f0`.

### Control flow

1. `SUB ESP,8`; save ESI/EDI.
2. `PUSH 0`; push slot then overwrite with `*ecx` (key).
3. `CALL FUN_0043e8f0` — stores `{key,0}` into ESI dest.
4. `ADD ESP,8` (drop key/flag stack args to adapter); `PUSH EAX` (pod*).
5. `EDI = host + 0xAC`; `CALL FUN_0043e7f0` (locked Stride2 U32U8 push).
6. restore ESI/EDI; `ADD ESP,8`; `RET 4`.

### Callers (2)

| Site | Function | Setup |
|------|----------|--------|
| `0x0075e333` | `FUN_0075e2d0` | `push [esi+0x6c]; mov ecx, local; call` |
| `0x0097103b` | `FUN_00971030` | `push this; mov ecx, arg; call` |

### Canonical name (inferred)

`SideHost_PushKeyFlag0_Inferred` — push `{*keyHolder, flag:0}` onto locked U32U8 vector at `host+0xAC`.

### Decompile note

Live Ghidra surface is incomplete (`void FUN_00970b20(void){ FUN_0043e8f0(); FUN_0043e7f0(); }`). **Bytes win** for full ABI + flag + host offset.
