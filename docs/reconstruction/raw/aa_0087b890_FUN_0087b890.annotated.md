# Annotated low-level: FUN_0087b890 / CVOGDialog_Ctor

| Field | Value |
|---|---|
| Stable ID | `aa_0087b890` |
| VA | `0x0087b890`–`0x0087b905` (**118 B**) |
| System | missions-progression (partition); shared NDUI dialog base |
| Date | 2026-08-05 (R11-030 re-annotate) |
| Product class | **CVOGDialog** (RTTI `.?AVCVOGDialog@@`) |
| Canonical (inferred product) | `CVOGDialog_Ctor` |
| Ghidra | `FUN_0087b890` |

## Machine-level notes

- Source: raw capture + R11-030 live re-verify (`decompile` ≡ raw CF).
- **Role:** CVOGDialog constructor — extends CNDUIDialog base ctor `FUN_00792d20`, installs CVOGDialog vtbl `0x00a58c6c`, zeros a small dialog-mode field pack, stores ctor arg at `+0x2A4`.
- **ABI:** two stack dwords (`this`, `param_2`); **`RET 8`**; **EAX = this**. Not ECX-thiscall.
- **SEH:** frame `LAB_009b531d`; states −1 → 0 (during body) → −1.
- Prefer bytes over decompiler for ABI (`RET 8`) and absolute offsets.
- Do **not** treat scaffold `Named_CalleeOf_Mission_i_d_npc_xml_0087b890` as product name.
- Do **not** invent English for `param_2` beyond "stored at +0x2A4" (callers overwhelmingly pass `0`).

## Pseudocode (annotated)

```c
// CVOGDialog * __stdcall CVOGDialog_Ctor(CVOGDialog *this, uint32_t param_2)
// Body 0x0087b890..0x0087b905; RET 8; returns this in EAX
undefined4 * FUN_0087b890(undefined4 *param_1 /*this*/, undefined4 param_2)
{
  // SEH: local_4=-1; puStack_8=&LAB_009b531d; link ExceptionList
  void *local_c;
  undefined1 *puStack_8;
  undefined4 local_4;

  local_4 = 0xffffffff;
  puStack_8 = &LAB_009b531d;
  local_c = ExceptionList;
  ExceptionList = &local_c;

  // Base: CNDUIDialog / container ctor (vtbl 0x00a98f44 temporarily)
  FUN_00792d20(param_1);

  // Dialog mode / pack zeros (dword indices → byte offsets)
  param_1[0x141] = 0;                         // +0x504
  param_1[0x142] = 0;                         // +0x508
  param_1[0x13f] = 0;                         // +0x4FC
  param_1[0x140] = 0;                         // +0x500
  *(undefined1 *)((int)param_1 + 0xbf) = 0;   // +0xBF flag byte
  *(undefined1 *)(param_1 + 0x124) = 0;       // +0x490 — clears base default 1

  // Install CVOGDialog primary vtbl (RTTI .?AVCVOGDialog@@)
  *param_1 = &PTR_FUN_00a58c6c;               // 0x00a58c6c

  // Ctor argument retained on object
  param_1[0xa9] = param_2;                    // +0x2A4

  ExceptionList = local_c;
  return param_1;                             // EAX = this; RET 8
}
```

## Field map (this body only)

| Offset | Index | Write | Notes |
|---|---|---|---|
| `+0x00` | `[0]` | `0x00a58c6c` | CVOGDialog vtbl |
| `+0x2A4` | `[0xa9]` | `param_2` | ctor arg (often 0) |
| `+0xBF` | byte | `0` | flag; also zeroed in base |
| `+0x490` | byte of `[0x124]` | `0` | **overrides** base `=1` |
| `+0x4FC` | `[0x13f]` | `0` | subclass often rewrites (e.g. mission host `0x0C`) |
| `+0x500` | `[0x140]` | `0` | subclass often rewrites to 1 |
| `+0x504` | `[0x141]` | `0` | |
| `+0x508` | `[0x142]` | `0` | |

## Call graph (owned surface)

```
[many dialog subclass ctors — 78 xrefs]
  CALL 0x0087b890  CVOGDialog_Ctor
    CALL 0x00792d20  CNDUIDialog_Ctor peer (FUN_00792d20)
    *this = CVOGDialog vtbl
  (caller typically overwrites *this with subclass vtbl)
```

Partition parent evidence: `FUN_008ac3f0` calls with `(this, 0)` then installs `0x00a4a51c`.

## Open questions

- Product English for `param_2` / field `+0x2A4`.
- Full CVOGDialog layout beyond sealed writes (object size varies by subclass: mission host `0x748`, first-time `0x5B0`, …).
- Official names for zeroed mode dwords at `+0x4FC..+0x508`.
- Runtime / bit-exact (Terminal false).
