# Annotated: Tree_RotateRight_Isnil15 (`aa_006753b0` / `FUN_006753b0`)

| Field | Value |
|---|---|
| Stable ID | `aa_006753b0` |
| VA | `0x006753b0`–`0x00675401` (82 B) |
| System | container / map-set tree (isnil `+0x15`) |
| Date | 2026-07-29 (W24-O dual seal) |
| Ghidra | `FUN_006753b0` |
| Named | `Tree_RotateRight_Isnil15` (**Inferred**) |

## Machine-level notes

- **Leaf** right-rotate for MSVC `_Tree` / `std::map` nodes with **isnil at +0x15**.
- Twin left-rotate: `Tree_RotateLeft_Isnil15` @ `0x004e22d0`.
- Staging twin (isnil@+0x49): `Tree_RotateRight` @ `0x005681a0` (W23-B) — body is a near-clone with isnil immediate `15` instead of `49`.
- Paired with `004e22d0` in every insert/erase rebalance site that uses this family (~30 callers).

## Pseudocode (named)

```c
// __thiscall; ECX = map; [esp+4] = x; RET 4
void Tree_RotateRight_Isnil15(MapShell *map, TreeNode_Isnil15 *x)
{
  TreeNode_Isnil15 *y = x->left;        // *x
  TreeNode_Isnil15 *yr = y->right;      // y+8

  x->left = yr;
  if (yr->isnil == 0) {                 // +0x15
    yr->parent = x;
  }
  y->parent = x->parent;

  TreeNode_Isnil15 *head = map->head;   // *(map+4)
  if (x == head->parent) {
    head->parent = y;
  } else {
    TreeNode_Isnil15 *p = x->parent;
    if (x == p->right) {
      p->right = y;
    } else {
      p->left = y;
    }
  }
  y->right = x;
  x->parent = y;
}
```

## ABI / layout

| Item | Value | Confidence |
|---|---|---|
| Convention | `__thiscall` + `RET 4` | **High** |
| map shell | head at `+4`; root = head->parent | **High** |
| isnil | `+0x15` | **High** (`80 7E 15 00`) |
| Polarity | RIGHT (y from left) | **High** (`8B 02`) |
| Color | not written | **High** |
| Leaf | no CALL | **High** |

## Open / residual

- Product/PDB symbol (Inferred name).
- Runtime / bit-exact.
