# Annotated low-level: NDSpecialFX_Ctor (`FUN_004ba310`)

| Field | Value |
|---|---|
| Stable ID | `aa_004ba310` |
| VA | `0x004ba310`–`0x004ba545` |
| System | `client-fx` / `NDSpecialFX` |
| Date | 2026-07-29 (W18-F OWN dual) |
| Proposed name | `NDSpecialFX_Ctor` (**High** — host ctor of 0x240 NDSpecialFX) |

## Machine-level notes

- **ABI:** `__thiscall` — `ECX = NDSpecialFX* this`, stack `const char *name`, `RET 4`, returns `this`.
- **Role:** default-construct FX host used by XML parse (`0x004a4fa0`) and template instantiate (`0x004a0b90`). Does **not** set event-type at `+0x28` (callers write after).
- **Name field:** `strncpy(this+0xd5, name, 0x104)` — 260-char path/name buffer.
- **Containers:** six empty vector-like heads (`FUN_004933f0`); one tree sentinel (`FUN_00439770`) at `+0x228`.
- **Subobject:** `FUN_0074ba00` on `this+0xa0` (not dual-owned here).
- Prefer live decompile + `read_memory`; **no** `disassemble_bytes`.

## Control flow

1. SEH push (`LAB_009a1671`).
2. Flag/byte defaults + three identity-like 16 B blocks from `DAT_009cb2a0`.
3. Nested `FUN_0074ba00(this+0xa0)`.
4. Six empty list heads + tree node.
5. Zero float/param slots; stamp `g_dwClientTickMs` @ `+0xd0`.
6. Copy name @ `+0xd5`.
7. Return `this`.

## Pseudocode (annotated)

```c
// __thiscall NDSpecialFX_Ctor(this, name)
// sizeof(*this) == 0x240 (callers operator_new)
NDSpecialFX * __thiscall NDSpecialFX_Ctor(NDSpecialFX *this, const char *name)
{
  // *this &= ~3; flag bytes; DAT_009cb2a0 blocks; g_flOne scales
  // FUN_0074ba00(this+0xa0);
  // six FUN_004933f0 empty vectors @ +0x1e0..+0x21c
  // FUN_00439770 tree @ +0x228; +0x230 = 2
  // this->tickMs = g_dwClientTickMs; // +0xd0
  strncpy((char *)this + 0xd5, name, 0x104);
  return this;
}
```

## Cross-refs

| Kind | Symbol / VA |
|---|---|
| Caller | `NDSpecialFX_InstantiateFromTemplate` `0x004a0b90` |
| Caller | `NDSpecialFX_ParseXmlEventNode` `0x004a4fa0` |
| Sibling teardown | `NDSpecialFX_TeardownCore_Inferred` `0x004b99c0` |
