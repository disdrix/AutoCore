# Raw capture: VehicleEntity_SetSteerInput

| Field | Value |
|---|---|
| **Stable ID** | `aa_004f5620` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x004f5620` |
| **Body end (approx)** | `0x004f564a` (~0x2b bytes) |
| **Canonical name** | `VehicleEntity_SetSteerInput` |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `decompile_function` |
| **Convention** | MSVC `__thiscall` (`this` in `ECX`) |
| **Sibling** | `VehicleEntity_SetLongitudinalInput` @ `0x004f5650` (identical gate → `+0x614`) |
| **Related verified note** | `docs/reconstruction/physics/verified/fn_004f5620_setSteerInput.md` |

---

## Signature (decompiler)

```c
void __thiscall VehicleEntity_SetSteerInput(int param_1, undefined4 param_2)
```

| Param | Register / stack | Decompiler type | Notes |
|---|---|---|---|
| `param_1` | `ECX` (`this`) | `int` | Vehicle entity pointer |
| `param_2` | stack / arg | `undefined4` | Steer axis; bit-pattern of `float` |

---

## Raw pseudocode (authoritative decompile, 2026-07-23)

```c
/* WI-MOV-001: thiscall. Writes float to this+0x618 (steer input). DriveControlTick: SteerLeft held
   DAT_00d1bc8e → +1; SteerRight DAT_00d1bcc2 → -1; soft L/R ±0.5. VehicleAction_applyAction
   ramps this+0x618 into VA+0x24. */

void __thiscall VehicleEntity_SetSteerInput(int param_1,undefined4 param_2)
{
  int iVar1;
  
  iVar1 = *(int *)(*(int *)(*(int *)(param_1 + 4) + 4) + 0xb0 + param_1);
  if ((iVar1 == 0) || ((*(byte *)(iVar1 + 0xb4) & 199) == 0)) {
    *(undefined4 *)(param_1 + 0x618) = param_2;
  }
  return;
}
```

---

## Tool notes

- Decompiler presents the mask as decimal **`199`**. That is **`0xC7`** unsigned: bits `0x01|0x02|0x04|0x40|0x80`.
- The `& 199` is a **byte** load (`*(byte *)(iVar1 + 0xb4)`), not a dword AND.
- `param_2` / store at `param_1 + 0x618` are `undefined4` in Ghidra; semantic type is **f32** (no float ops inside this function — pure store of the bit pattern).
- Pointer expression is left-associative as written: load `*(param_1+4)`, then `*(that+4)`, then add `param_1 + 0xb0` as the final load address offset.
- No calls, no clamps, no FP arithmetic, no other stores.
- Plate comment (`WI-MOV-001`) is pre-existing Ghidra annotation, not generated by this capture.

---

## Callers (xref sample; not re-enumerated this capture)

| Caller | Address |
|---|---|
| `Client_Input_DriveControlTick` | `0x009223b0` |
| `FUN_004fc650` | `0x004fc650` |
| `FUN_005d73a0` | `0x005d73a0` |
| `FUN_00636ba0` | `0x00636ba0` |
| `FUN_0092f090` | `0x0092f090` |
| `FUN_009373e0` | `0x009373e0` |

---

## Integrity

- **Do not overwrite** this raw capture with cleaned or reinterpreted text.
- Corrections, integer-width notes, and semantic naming live in:
  - `aa_004f5620_VehicleEntity_SetSteerInput.annotated.md`
  - `docs/reconstruction/functions/aa_004f5620_VehicleEntity_SetSteerInput.md`
  - `docs/reconstruction/reconstructed-exact/VehicleEntity_SetSteerInput.cpp`
