# Raw capture: FUN_00444850

| Field | Value |
|---|---|
| **Stable ID** | `aa_00444850` |
| **Module** | `autoassault.exe` |
| **Image base** | `0x400000` |
| **VA** | `0x00444850` |
| **Canonical name** | `FUN_00444850` |
| **System** | unknown |
| **Capture timestamp** | `2026-07-23` |
| **Tool** | Ghidra MCP `batch_decompile` |
| **Integrity** | Do not overwrite this raw body; append versioned sections only |

---

## Raw pseudocode (authoritative decompile)

```c
void FUN_00444850(void)

{
  undefined1 local_8 [8];
  
  FUN_00458510(local_8);
  return;
}
```

---

## W35-H re-verify append (2026-07-29)

**Tools:** `batch_decompile` / `decompile_function` + `analyze_function_complete` + `read_memory` (body + caller sites). **No** `disassemble_bytes`. **No** Launcher.

### Body

| Item | Value |
|------|-------|
| Range | `0x00444850`–`0x00444874` exclusive |
| Size | **36 B** / `0x24` |
| Pad | `CC` then sibling `FUN_00444880` @ `0x00444880` |
| Epilogue | bare **`RET`** (`C3`) |

### Full body hex (36 B)

```
00444850: 83ec10538d54240c89442404528d5c24
00444860: 08b85819d200894c240ce8a13c01005b
00444870: 83c410c3
```

### ABI (bytes + caller sites authority)

Decompiler shows `void FUN_00444850(void)` — **under-specified**.

Bytes:

1. `SUB ESP,0x10; PUSH EBX`
2. Save **EAX** (FourCC tag) and stage **ECX** (handler*) next to key on stack
3. `LEA EBX, &key` — key = `{uint32 tag; void* handler}` pair for insert helper
4. `MOV EAX, DAT_00d21958` — **map object** in EAX (callee `in_EAX`)
5. `PUSH &out_pair` → `CALL FUN_00458510` (map lower_bound / insert-if-absent)
6. Discard out-pair; `POP EBX; ADD ESP,0x10; RET`

**Register contract:**

| Reg | Role |
|-----|------|
| **EAX in** | FourCC `uint32` tag (e.g. `'EFCT'`, `'MRPH'`) |
| **ECX in** | function / handler pointer value |
| **EAX during call** | overwritten with `&DAT_00d21958` |
| Stack args | none beyond out-pair for callee |
| Cleanup | bare `C3` |

### Callers (2 sites, same function)

`FUN_00754680` (Palantir device start — path `Palantir.cpp`):

| Site | EAX (tag) | ECX (handler) |
|------|-----------|---------------|
| `0x0075482f` | `0x45464354` **`'EFCT'`** | `0x00990540` |
| `0x0075483e` | `0x4d525048` **`'MRPH'`** | `0x009906e0` |

Handlers are code entrypoints (SEH prolog bytes). Sibling `FUN_00444880` uses map **`DAT_00d21964`** with `'SKEL'`.

### Callees / classification

| Item | Value |
|------|-------|
| Callee | `FUN_00458510` — tree map find / insert (`in_EAX`=map, `EBX`=key*, stack out-pair) |
| Map | `DAT_00d21958` |
| Classification | **wrapper** |
| Xrefs | 2 (both in `FUN_00754680`) |

### Decompile vs bytes

- Decompile omits EAX/ECX register formals and map global load — **bytes + call sites win**.
- Out-pair is constructed and discarded (insert side-effect only).

