# Annotated low-level: FUN_00581500 / Object_StopTypeF_ReloadEventVariant_MIThunk_Inferred

| Field | Value |
|---|---|
| Stable ID | `aa_00581500` |
| VA | `0x00581500`–`0x00581517` (**24 B**) |
| System | client-fx / type-0xF stop + event-variant reload (MI public entry) |
| Date | 2026-07-29 (W29-O dual; scaffold 2026-07-23) |

## Machine-level notes

- Decompiler shows only `FUN_00514d80(); return;` — **wrong surface** for ABI (hides this-adjust + `ret 4`).
- Bytes seal full contract (prefer over decompile).
- Impl dual sealed W28-H as `Object_StopTypeF_ReloadEventVariant_Inferred`.

## Byte-level spine

1. `mov eax, [ecx-0x8C]` — load pointer at `this - 0x8C` (vbtable / complete-object relative).
2. `mov edx, [eax+4]` — second dword (vb delta slot).
3. `lea ecx, [edx+ecx-0x8C]` — adjusted `this` for primary Object layout expected by impl.
4. `call 0x00514d80` — relative sealed.
5. `ret 4` — virtual ABI cleans one stack dword (often unused by impl which uses plain `ret`).

## Peer adjustor family (not OWN)

Seven sites JMP here after `sub ecx,[ecx-4]; sub ecx,imm` — classic MSVC multi-inheritance adjustors sharing this common entry.

## Sole direct CALL

`FUN_005d49e0` @ `0x005d49f8`: after another helper with `lea ecx,[esi-0x90]`, calls this with `lea ecx,[esi-0x10]` + one stack dword.

## Pseudocode (byte-accurate)

```c
// __thiscall ECX = secondary/subobject this; ret 4
void Object_StopTypeF_ReloadEventVariant_MIThunk_Inferred(void *self_sub /* + ignored stack dword */)
{
  void *vb = *(void **)((char *)self_sub - 0x8C);
  int delta = *(int *)((char *)vb + 4);
  void *self_obj = (char *)self_sub + delta - 0x8C;
  Object_StopTypeF_ReloadEventVariant_Inferred(self_obj); // thiscall; plain ret
}
```

## Open questions

- Product/PDB name of the subobject that installs this slot.
- Exhaustive vtable slot index catalog (peer JMP sites not dualed).
