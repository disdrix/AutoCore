# Annotated low-level: StdDeque_ThrowTooLong_00436860

| Field | Value |
|---|---|
| Stable ID | `aa_00436860` |
| VA | `0x00436860`–`0x004368aa` (**75 B**) |
| Ghidra | `FUN_00436860` |
| Canonical | `StdDeque_ThrowTooLong_00436860` (**Inferred**) |
| System | `containers` / MSVC STL deque |
| Date | 2026-07-23 scaffold; **2026-07-29 W29-C dual seal** |

## Machine-level notes

- **ABI:** no formals; SEH frame; **noreturn** (ends in `_CxxThrowException`).
- **Plate:** `"deque<T> too long"` @ `0x00a31520` (shared with other deque `_Xlen` clones).
- **ThrowInfo:** `DAT_00acc388` (same family as vector/deque length-error helpers).
- **Diff vs `StdDeque_ThrowTooLong_0051b5f0`:** this body is **compact** (75 B vs 112 B) — folds exception+vftable into `FUN_00401a40` rather than inlining two-step base/copy construction. **Not** byte-merged with siblings.
- **Role:** parents call only after max-size / map-grow guards fail (e.g. `CircularPtrBuf_Grow` `FUN_00436650`, `GuardedVector_GrowBlockMap` `FUN_0043c9b0`).

## Pseudocode (annotated)

```c
// noreturn — MSVC deque::_Xlen-class helper
void StdDeque_ThrowTooLong_00436860(void)
{
  // SEH: local_4=-1; handler LAB_009bc919; link ExceptionList / FS:[0]
  // stack: basic_string local_50; exception blob local_34

  basic_string_ctor(local_50, "deque<T> too long"); // plate @ 0x00a31520
  // EH state = 0
  FUN_00401a40(local_34 /*this*/, local_50 /*msg*/);
  //   → exception ctor from string; *ex = PTR_FUN_009c7634

  /* WARNING: does not return */
  _CxxThrowException(local_34, (ThrowInfo *)&DAT_00acc388);
}
```

## Open questions

- Product/PDB symbol (`std::deque<T>::_Xlen` or equivalent).
- Exact C++ exception type behind `PTR_FUN_009c7634` / ThrowInfo RTTI.
- Runtime / bit-exact / differential.
