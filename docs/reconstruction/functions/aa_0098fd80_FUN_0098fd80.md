# Function record: FUN_0098fd80

| Field | Value |
|---|---|
| **Stable ID** | `aa_0098fd80` |
| **Canonical name** | `FUN_0098fd80` |
| **Inferred name** | `PalToolNoMap_Host0x30_MaterialSlot0x28_Init_Inferred` |
| **Address** | `0x0098fd80`–`0x0098fee9` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | palantir graphics / PalTool material (no-map) |
| **Completion status** | **Dual sealed W37-A** — raw/annotated/clean + A/B reviews |
| **Bit-for-bit / runtime / diff** | Open (no Launcher; terminal coverage **false**) |
| **Verdict** | **accept-with-gaps** |

## Alias

- `PalToolNoMap_Host0x30_MaterialSlot0x28_Init_Inferred` — structural (host size + material slot). **Inferred**.
- `Gfx_PalToolMaterialNoMap_0098fd80` — plate string only; **incomplete** as sole surface (omits Host0x30/+0x28).

## Purpose

Post-ctor init for HostPtrTable12 **slot9** host (`new(0x30)` in `FUN_0073c810`):

1. Resolve/bind `PalToolMaterialNoMap.fx` via `FUN_00989e00` + `FUN_009701d0`.
2. Set effect scalars: `DepthBias=0.001`, `GhostAlpha=0.1` (`FUN_00442640`).
3. Bind `MatDiffuse` / `MatSpecular` / `MatEmissive` into material holder at **`host+0x28`** (null holder → `-1` sentinel; bind only if handle ≥ 0).
4. Emissive float4 **(1.0, 0.4, 0.4, 1.0)**.
5. `FUN_0073d940` refcount-swap into `* (host+0x28)`.
6. Return **0**.

## Signature (decompiler-derived)

```c
// Stack formal host*; sole caller pushes. Returns 0.
uint32_t FUN_0098fd80(void *host /* size 0x30 */);
// Named:
uint32_t PalToolNoMap_Host0x30_MaterialSlot0x28_Init_Inferred(void *host);
```

## Artifacts

| Kind | Path |
|---|---|
| Raw | `docs/reconstruction/raw/aa_0098fd80_FUN_0098fd80.md` |
| Annotated | `docs/reconstruction/raw/aa_0098fd80_FUN_0098fd80.annotated.md` |
| Clean named | `docs/reconstruction/reconstructed-exact/PalToolNoMap_Host0x30_MaterialSlot0x28_Init_Inferred.cpp` |
| Clean twin | `docs/reconstruction/reconstructed-exact/FUN_0098fd80.cpp` |
| Plate alias | `docs/reconstruction/reconstructed-exact/Gfx_PalToolMaterialNoMap_0098fd80.cpp` |
| Review A | `docs/reconstruction/reviews/A_aa_0098fd80_PalToolNoMap_Host0x30_MaterialSlot0x28_Init_Inferred.md` |
| Review B | `docs/reconstruction/reviews/B_aa_0098fd80_PalToolNoMap_Host0x30_MaterialSlot0x28_Init_Inferred.md` |

## Callers / callees

| Direction | Targets |
|---|---|
| **Callers (1)** | `FUN_0073c810` @ `0x0073cac3` |
| **Callees** | `FUN_00989e00`, `FUN_009701d0`, `FUN_00442640`, `FUN_00752370`, `FUN_0096fff0`, `FUN_0073d940` |

## Evidence (W37-A)

- Live `decompile_function` ≡ raw CF.
- `read_memory`: `DAT_00a0f720`=0.4f; pool floats 0.001/0.1/1.0.
- Caller decompile: `operator_new(0x30)` + `FUN_00457ac0` + zero `+0x28` + `FUN_0098fd80(pvVar2)`.

## Confidence

| Claim | Level |
|---|---|
| Control flow from decompiler | **High** / live-confirmed |
| Sole caller + host size 0x30 + slot +0x28 | **Confirmed** |
| Plate + param strings + float values | **Confirmed** |
| Product host class English | **Unknown** (`_Inferred`) |
| Helper thiscall ECX recovery | **Probable** residual |
| Runtime / bit-exact | **Open** |
