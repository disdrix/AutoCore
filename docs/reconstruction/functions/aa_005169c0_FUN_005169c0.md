# Function record: FUN_005169c0 (name withheld)

| Field | Value |
|---|---|
| **Stable ID** | `aa_005169c0` |
| **Canonical name** | `FUN_005169c0` |
| **Address** | `0x005169c0` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | skills-abilities |
| **Completion status** | **leave-FUN** — behavior dual A/B complete; product name withheld |
| **Name status** | **FUN_* retained** |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Decision 2026-07-29 (evidence-only)

**Leave `FUN_005169c0`.** Do **not** rename and do **not** promote `Skill_LazyAllocBusySlot_Inferred`.

| Gate | Result |
|---|---|
| Product string / debug name on body | **None** (no data refs; `audit_globals_in_function` empty) |
| RTTI on allocated block | **None** (plain `operator_new(0x14)` + zero fields; no vtbl write) |
| Exclusive `Skill_LocalCastValidate` use | **Not proven** — **3** named callers (5 UNCONDITIONAL call sites) |
| Promote `_Inferred` busy-slot name | **Blocked** — exclusive LocalCastValidate use required by naming rule; multi-caller |

Prior alias `Named_CalleeOf_Named_VOG_DEBUG_STOP_005169c0` is **not** a product name (scaffold from caller string in `FUN_0052c780`); discarded for registry.

## Behavior (High CF — dual A/B)

```c
void * __fastcall FUN_005169c0(int param_1)
{
  void *pvVar1;
  if (*(int *)(param_1 + 100) == 0) {          // this+0x64
    pvVar1 = operator_new(0x14);
    if (pvVar1 != (void *)0x0) {
      *(undefined4 *)((int)pvVar1 + 4) = 0;
      *(undefined4 *)((int)pvVar1 + 8) = 0;
      *(undefined4 *)((int)pvVar1 + 0xc) = 0;
      *(undefined4 *)((int)pvVar1 + 0x10) = 0;
      *(void **)(param_1 + 100) = pvVar1;
      return pvVar1;
    }
    *(undefined4 *)(param_1 + 100) = 0;
  }
  return *(void **)(param_1 + 100);
}
```

- Lazy-get pointer at **`this+0x64`** (decimal 100).
- On first use: allocate **0x14** bytes; zero dwords at **+4, +8, +0xC, +0x10**; store and return.
- On OOM: store null at `this+0x64` and fall through to return that null.
- Else return existing pointer. Callee-only: `operator_new`.

### Caller uses of returned block

| Caller | VA / sites | Use of return |
|---|---|---|
| `Skill_LocalCastValidate` | `0x0051a790` @ `0x0051a806` | If `bSkipBusyCheck==0` and `*(ret+0x10) != 0` → **eSkillResponses 6** (busy) |
| `Skill_ClearActiveCastCounterAndQueueId` | `0x00517b90` @ `0x00517b96`, `0x00517ba3`, `0x00517bcd` | Read/decrement `+0x10`; clear `+0xC` when counter hits 0; third site feeds `Skill_QueueDeferredCastId` path |
| `FUN_0052c780` | `0x0052c780` @ `0x0052c7a9` | Call then clear-is-casting traversal (`Skill_SetIsCastingFlag`); parent has `VOG_DEBUG_STOP` strings |

**Behavioral note (not a name):** slot `+0x10` participates in busy/active-cast counting, but multi-caller + missing product label keep the symbol as `FUN_*`.

## Signature (decompiler-derived)

```c
void * __fastcall FUN_005169c0(int param_1);
```

Object type of `param_1` / ECX not sealed (shared across cast helpers; not proven skill-vs-caster product type).

## Artifacts

- Raw: `docs/reconstruction/raw/aa_005169c0_FUN_005169c0.md`
- Annotated: `docs/reconstruction/raw/aa_005169c0_FUN_005169c0.annotated.md`
- Clean scaffold: `docs/reconstruction/reconstructed-exact/FUN_005169c0.cpp` (not three-rep promoted)
- Dual A/B: `docs/reconstruction/reviews/A_aa_005169c0_FUN_005169c0.md`, `B_aa_005169c0_FUN_005169c0.md`

## Confidence

| Claim | Level |
|---|---|
| Control flow / lazy alloc size 0x14 @ +0x64 | **Confirmed** (decompile) |
| Zero-init +4/+8/+0xC/+0x10 | **Confirmed** |
| No string / no RTTI on body | **Confirmed** |
| Callers: LocalCastValidate + ClearActiveCastCounter + FUN_0052c780 | **Confirmed** (xrefs) |
| Exclusive LocalCastValidate use | **Falsified** |
| Product / `_Inferred` name | **Withheld** |
| Field product meaning beyond counter-like +0x10 | Tentative |
