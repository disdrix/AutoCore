# Function record: Item_SumEquippedComponentValues (`FUN_00513580`)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00513580` |
| **Canonical name** | `Item_SumEquippedComponentValues` (proposed) |
| **Ghidra name** | `FUN_00513580` |
| **Address** | `0x00513580` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | `inventory-transfer` (vendor / item value) |
| **Completion status** | **Dual complete (accept-with-gaps)** — 2026-07-29 W19-H |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Alias

- Prior scaffold: `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Client_RefreshOpen_00513580`
- Human role: **sum equipped component values** (`component+0x94`) for pricing

## Purpose

Iterate an item’s equipped component/mod slots (count via vtbl `+0x60`, id via vtbl `+0x5c`), resolve each slot against a type-keyed catalog lookup (host `clonebase+0x38`), and **sum** the integer field at **`component+0x94`**. Returns 0 if catalog singleton missing or no slots/components.

Used by store sell price (`0x005142a0`: `sum + item+0xC4` then scale) and sibling price helper `0x00514250`.

## Signature (decompiler + bytes)

```c
// ECX = Item*; ret int in EAX (plain ret)
int __thiscall Item_SumEquippedComponentValues(int *item /* ECX */);
```

## Closed form

```text
catalog = FUN_004ce940()
if !catalog: return 0
sum = 0
for i in 0 .. item.vtbl[+0x60]()-1:
  slotId = item.vtbl[+0x5c](i)
  switch item.clonebase[+0x38]:
    10,0xc,0xe,0x1c,0x44,0x46 → type-specific catalog lookup(slotId)
    else → skip
  if component: sum += component[+0x94]
return sum
```

## Sealed facts

| Field | Value | Evidence |
|-------|-------|----------|
| ECX = item | thiscall | `mov esi,ecx` prologue |
| Catalog gate | `FUN_004ce940` | relative call + null je |
| Sum field | **`+0x94`** | `add ebx, [eax+0x94]` |
| Live types | 10 / 0xc / 0xe / 0x1c / 0x44 / 0x46 | decompile + jump table |
| Return | int sum | `mov eax, ebx; ret` |

## Not this unit

| Role | VA |
|------|-----|
| Catalog singleton | `0x004ce940` |
| Per-type lookups | `0x00508b60` family |
| Store sell price (caller) | `0x005142a0` |
| Related price helper | `0x00514250` |

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00513580_FUN_00513580.md`
- Annotated: `docs/reconstruction/raw/aa_00513580_FUN_00513580.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/Item_SumEquippedComponentValues.cpp`
- Scaffold clean: `docs/reconstruction/reconstructed-exact/FUN_00513580.cpp`
- Dual A: `docs/reconstruction/reviews/A_aa_00513580_Item_SumEquippedComponentValues.md`
- Dual B: `docs/reconstruction/reviews/B_aa_00513580_Item_SumEquippedComponentValues.md`
- Report: `docs/agents/task-dual-ab-00513e30-00513580-w19h-report.md`

## Callers / callees

| Direction | Examples |
|-----------|----------|
| Callees | `FUN_004ce940`; `FUN_00508b60` / `508ac0` / `508bb0` / `508b10` / `508c50` / `508c00`; vtbl `+0x5c`/`+0x60` |
| Callers | `FUN_005142a0` @ `0x005142d9`; `FUN_00514250` @ `0x00514266` |

## Confidence

| Claim | Level |
|---|---|
| Control flow + loop + switch cases | **High / Sealed** |
| Sum field `+0x94` | **High / Sealed** |
| Role = component value sum for pricing | **High** CF; English **Probable** |
| Type enum product names | Tentative / Open |
| Original PDB symbol | Open |
