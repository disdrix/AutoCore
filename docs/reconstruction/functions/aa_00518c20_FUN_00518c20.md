# Function record: FUN_00518c20 (name withheld)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00518c20` |
| **Canonical name** | `FUN_00518c20` |
| **Address** | `0x00518c20` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | skills-abilities |
| **Completion status** | **leave-FUN** — behavior dual A/B complete; product name withheld |
| **Name status** | **FUN_* retained** |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Decision 2026-07-29 (evidence-only)

**Leave `FUN_00518c20`.** Do **not** rename and do **not** promote `Skill_GetActiveCastBindingMap[_Inferred]`.

| Gate | Result |
|---|---|
| Product string / debug name on body | **None** (`audit_globals_in_function`: SEH only) |
| RTTI on allocated header/node | **None** (plain `operator_new` + tree links; no vtbl) |
| Exclusive `Skill_LocalCastValidate` use | **Not proven** — **4** callers / **7** UNCONDITIONAL call sites |
| Twin slot `Skill_GetCategoryCooldownMap` @ +0x6c | Structural analogy only — **not** a rename seal |
| Promote product / `_Inferred` map name | **Blocked** — same leave-FUN bar as `aa_005169c0` |

Prior alias `Named_CalleeOf_Skill_LocalCastValidate_00518c20` is **not** a product name (scaffold from one caller); discarded for registry.

## Behavior (High CF — dual A/B)

```c
undefined4 __fastcall FUN_00518c20(int param_1)
{
  void *pvVar1;
  int iVar2;
  // SEH frame: ExceptionList / LAB_009a35fd
  if (*(int *)(param_1 + 0x68) == 0) {
    pvVar1 = operator_new(0xc);
    if (pvVar1 == (void *)0x0) {
      pvVar1 = (void *)0x0;
    } else {
      iVar2 = FUN_0058d9c0();                 // head node, 0x30 bytes
      *(int *)((int)pvVar1 + 4) = iVar2;      // _Myhead
      *(undefined1 *)(iVar2 + 0x29) = 1;      // head color/flag
      // self-link parent / left / right of head
      *(int *)(*(int *)((int)pvVar1 + 4) + 4) = *(int *)((int)pvVar1 + 4);
      *(undefined4 *)*(undefined4 *)((int)pvVar1 + 4) = *(undefined4 *)((int)pvVar1 + 4);
      *(int *)(*(int *)((int)pvVar1 + 4) + 8) = *(int *)((int)pvVar1 + 4);
      *(undefined4 *)((int)pvVar1 + 8) = 0;   // size
    }
    *(void **)(param_1 + 0x68) = pvVar1;
  }
  return *(undefined4 *)(param_1 + 0x68);
}
```

- Lazy-get pointer at **`this+0x68`**.
- On first use: allocate **0xC** map header; init empty red-black-style tree via **`FUN_0058d9c0`**; size **0** at header `+8`.
- Else return existing pointer.
- Callees: `operator_new`, `FUN_0058d9c0`.

### Twin

| Function | Slot | Node factory | Named? |
|---|---|---|---|
| `FUN_00518c20` | `+0x68` | `FUN_0058d9c0` (0x30 node, flag +0x29) | **No** (this unit) |
| `Skill_GetCategoryCooldownMap` | `+0x6c` | `FUN_005ae2b0` (flag +0x19) | Yes |

### Caller uses of returned map

| Caller | Sites | Use of return |
|---|---|---|
| `Skill_LookupActiveCastBinding` | `0x00518d00`, `0x00518d0e` | Map lower_bound/find by skillId; copy node values `+0x18…+0x24` → out TFID; end-iterator compare vs `map+4` |
| `Skill_ClearCastBindingAndMaybeRestartCd` | `0x0051a710`, `0x0051a71e`, `0x0051a74f` | Find node matching skillId+TFID; erase path |
| `FUN_00518ca0` | `0x00518cd3` | Insert helper (`FUN_004cbe20`) |
| `Skill_LocalCastValidate` | `0x0051a8c7` | If `*(map+8) != 0` and skill `+0x615 & 2` (and no plant) → **eSkillResponses 10** |

**Behavioral note (not a name):** this is the active-cast **binding map** shell used by Lookup/Clear; LocalCastValidate only tests non-empty size. Multi-caller + missing product label keep the symbol as `FUN_*`.

## Signature (decompiler-derived)

```c
undefined4 /* map* */ __fastcall FUN_00518c20(int param_1);
```

Object type of `param_1` / ECX not sealed (map owner shared by cast helpers). Some Ghidra call sites show extra stack args — those belong to adjacent map ops, not this unary getter.

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00518c20_FUN_00518c20.md`
- Annotated: `docs/reconstruction/raw/aa_00518c20_FUN_00518c20.annotated.md`
- Clean scaffold: `docs/reconstruction/reconstructed-exact/FUN_00518c20.cpp` (not three-rep promoted)
- Dual A/B: `docs/reconstruction/reviews/A_aa_00518c20_FUN_00518c20.md`, `B_aa_00518c20_FUN_00518c20.md`
- Scratch: `tmp/a_00518c20.md`

## Confidence

| Claim | Level |
|---|---|
| Control flow / lazy alloc 0xC @ +0x68 | **Confirmed** (decompile) |
| Empty map init (head + size 0) | **Confirmed** |
| No string / no RTTI on body | **Confirmed** |
| Callers: Lookup + Clear + FUN_00518ca0 + LocalCastValidate | **Confirmed** (xrefs) |
| Exclusive LocalCastValidate use | **Falsified** |
| Twin of GetCategoryCooldownMap | **High** structure |
| Product / `_Inferred` name | **Withheld** |
