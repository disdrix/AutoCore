# Function record: Item_SetCustomized (`FUN_00513e30`)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00513e30` |
| **Canonical name** | `Item_SetCustomized` (proposed) |
| **Ghidra name** | `FUN_00513e30` |
| **Address** | `0x00513e30` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | `inventory-transfer` |
| **Completion status** | **Dual complete (accept-with-gaps)** — 2026-07-29 W19-H; **mask `0x100000` sealed** |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Alias

- Prior scaffold: `Named_CalleeOf_Client_OnVehicleSwitchFailure_00513e30`
- Human role: **`ItemFlag_Customized` setter** (write bit 20)

## Purpose

Set or clear the **Customized** flag on an inventory item object. Flag lives at **`item+0x17c` bit 20**, mask **`0x100000`**. On value change, calls dirty helper `FUN_00512670` (`| 0x20000` on self and `+0xac` chain). **No** post-write virtual tail (contrast `Item_SetBroken`).

## Signature (decompiler + bytes)

```c
// ECX = Item*; stack: desiredCustomized (low byte); ret 4
void __thiscall Item_SetCustomized(int *item /* ECX */, unsigned desiredCustomized /* stack */);
```

## Closed form

```text
if (Customized(item) != (desired & 0xff))  // compare bit20 vs full low byte
  FUN_00512670(item);                      // | 0x20000 dirty chain
item.flags[+0x17c] ^= (((desired & 0xff) << 20) ^ flags) & 0x100000;
// return — no vtbl
```

## Sealed mask

| Field | Value | Evidence |
|-------|-------|----------|
| **Mask** | **`0x100000`** | decompile + bytes `81 e6 00 00 10 00` |
| **Bit** | **20** (`0x14`) | `shr`/`shl` 0x14; `1<<20` |
| **Offset** | `item+0x17c` (`object[0x5f]`) | `mov`/`xor [ecx+0x17c]` |
| **Name** | `ItemFlag_Customized` | trade string + permanent-customize path (Probable/High) |

## Not this unit

| Role | VA |
|------|-----|
| Dirty mark helper (`\| 0x20000`) | `0x00512670` |
| Broken setter (`0x80000`) | `0x00513de0` |
| Stack eligibility (reads Broken) | `0x00513e70` |
| Trade drop (reads bit20) | `0x00860a50` |

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00513e30_FUN_00513e30.md`
- Annotated: `docs/reconstruction/raw/aa_00513e30_FUN_00513e30.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/Item_SetCustomized.cpp`
- Scaffold clean: `docs/reconstruction/reconstructed-exact/FUN_00513e30.cpp`
- Dual A: `docs/reconstruction/reviews/A_aa_00513e30_Item_SetCustomized.md`
- Dual B: `docs/reconstruction/reviews/B_aa_00513e30_Item_SetCustomized.md`
- Report: `docs/agents/task-dual-ab-00513e30-00513580-w19h-report.md`

## Callers / callees (sample)

| Direction | Examples |
|-----------|----------|
| Callees | `FUN_00512670` only |
| Callers (xrefs) | `FUN_00810360` @ `0x008104e1`; `FUN_005cb520` @ `0x005cb63a`; `FUN_005cbcb0` @ `0x005cbdc7`; permanent-customize Yes `0x0091260e`, `0x009126a9` |

## Confidence

| Claim | Level |
|---|---|
| Control flow from decompiler + bytes | **High / Sealed** |
| Mask `0x100000` / bit 20 | **High / Sealed** |
| Role = Customized setter | **Probable / High** (mask sealed; English from peer duals) |
| Parameter semantic names | Probable |
| Original PDB symbol | Open |
