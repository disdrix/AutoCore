# Function record: CSoundManager_Ctor (`FUN_00720a40`)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00720a40` |
| **Canonical name** | `CSoundManager_Ctor` (inferred) |
| **Ghidra name** | `FUN_00720a40` |
| **Address** | `0x00720a40` |
| **Body** | `0x00720a40`–`0x00720b44` (`ret 4`) |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | client audio / `CSoundManager` |
| **Completion status** | **Dual A/B sealed** (W16-N, 2026-07-29) |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Alias

- Prior scaffold: `Named_CalleeOf_Named_CalleeOf_Named_VOGMapText_00720a40` (xref-weak; superseded)
- Ghidra: `FUN_00720a40`

## Purpose

In-place constructor for the `CSoundManager` singleton storage at `DAT_00d20070`. Initializes flags, float slots (incl. −1.0f from `DAT_00aaa668` into +0x20/+0x24), zeros a 50-dword block at +0x90, and creates two critical sections at +0x158 and +0x170. Returns `this`.

## Signature (machine)

```c
// __stdcall one arg (ret 4); returns this
uint8_t *CSoundManager_Ctor(uint8_t *self);
```

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00720a40_FUN_00720a40.md`
- Annotated: `docs/reconstruction/raw/aa_00720a40_FUN_00720a40.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/FUN_00720a40.cpp`
- Named clean: `docs/reconstruction/reconstructed-exact/CSoundManager_Ctor.cpp`
- Review A: `docs/reconstruction/reviews/A_aa_00720a40_CSoundManager_Ctor.md`
- Review B: `docs/reconstruction/reviews/B_aa_00720a40_CSoundManager_Ctor.md`

## Callers / callees

**Callees:** `InitializeCriticalSection` ×2.

**Callers:** only `FUN_00720d40` / `CSoundManager_GetInstance` @ `0x00720d40` (once-init of `&DAT_00d20070`).

## Related duals

| Unit | Role |
|---|---|
| `aa_00720d40` | GetInstance once-flag + atexit + call this ctor |
| `aa_007203b0` | MarkStopByName (method on same manager) |

## Confidence

| Claim | Level |
|---|---|
| Field init CF + CS pair | **Confirmed** |
| `DAT_00aaa668` = −1.0f | **Confirmed** (`read_memory`) |
| Sole caller GetInstance | **Confirmed** |
| Product family `CSoundManager` | **High** (sibling `_stopSound` string) |
| Exact English name `Ctor` | **Probable** |
| Full object size / late slots | **Open** |
