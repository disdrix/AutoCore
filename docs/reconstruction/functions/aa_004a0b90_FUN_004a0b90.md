# Function record: NDSpecialFX_InstantiateFromTemplate (FUN_004a0b90)

| Field | Value |
|---|---|
| **Stable ID** | `aa_004a0b90` |
| **Canonical name** | `NDSpecialFX_InstantiateFromTemplate` (inferred) |
| **Ghidra name** | `FUN_004a0b90` |
| **Address** | `0x004a0b90` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | `client-fx` / `NDSpecialFX` |
| **Completion status** | **Dual-reviewed** (W16-K) — A accept-with-gaps, B accept-with-gaps |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |
| **Dual reviews** | `reviews/A_aa_004a0b90_NDSpecialFX_InstantiateFromTemplate.md`, `reviews/B_aa_004a0b90_NDSpecialFX_InstantiateFromTemplate.md` |
| **Wave owner** | W16-K (OWN-ONLY) |

## Alias

- Scaffold: `Named_CalleeOf_Named_VOG_DEBUG_STOP_004a0b90`
- Caller context: post-parse instantiate in `NDSpecialFX_LoadFromScriptName` (`aa_004a61b0`)

## Purpose

Allocate and **deep-clone/instantiate** an `NDSpecialFX` host (`0x240` bytes via `FUN_004ba310`) from a template: copy flags/blob/maps/lists; spawn scaled children under filter; stamp instances. Primary caller passes `(fx, fx+0xd5, 1, -1, stamp)`.

## Signature (decompiler-derived)

```c
void FUN_004a0b90(
  int param_1,      // template host*
  undefined4 param_2, // name → ctor (often template+0xd5)
  int param_3,      // scale
  int param_4,      // filter id (-1 = all)
  int param_5       // stamp
);
```

Return of clone host is **residual** (decompiler `void` / SEH epilogue).

## Artifacts

- Raw: `docs/reconstruction/raw/aa_004a0b90_FUN_004a0b90.md`
- Annotated: `docs/reconstruction/raw/aa_004a0b90_FUN_004a0b90.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/FUN_004a0b90.cpp`
- Dual A: `docs/reconstruction/reviews/A_aa_004a0b90_NDSpecialFX_InstantiateFromTemplate.md`
- Dual B: `docs/reconstruction/reviews/B_aa_004a0b90_NDSpecialFX_InstantiateFromTemplate.md`

## Callers / callees

**Callers (sample):** `NDSpecialFX_LoadFromScriptName` @ `0x004a61b0` — `FUN_004a0b90(fx, fx+0xd5, 1, -1, stamp)`.

**Callees (notable):** `FUN_004ba310` (ctor), `FUN_004a7e30`, `FUN_004a7580`/`FUN_004a7670`, `FUN_00439770`/`FUN_004397f0`, `FUN_00590800`, `FUN_005bdcf0`, `FUN_005b94e0`, `FUN_004a6c80`, `FUN_005b8500`, `FUN_005b0990`, `FUN_006759b0`, `FUN_004a6a80`/`FUN_004a6ab0`/`FUN_004a6be0`, `FUN_004a7210`, `FUN_0058f610`, `FUN_0049a120`/`FUN_004b92b0`, `operator_new`/`operator_delete`.

## Confidence

| Claim | Level |
|---|---|
| Control flow of multi-list clone | **High** (live ≡ raw) |
| Host size `0x240` + ctor `FUN_004ba310` | **High** |
| Scale × short count; filter −1 = all | **High** |
| Stamp onto instances (`+0x8c4`, `+0x28`/index, `+0x242`) | **High** |
| Name `NDSpecialFX_*` | **Probable** (caller system) |
| Return / ownership of clone | **Residual / Open** |
| Sub-object product types | Tentative |

## Residual

1. Clone host return / registration / lifetime vs template delete in loader.
2. Null `operator_new` then stores (OOM UB).
3. Full type recovery for list payloads.
4. Runtime / image diff not run.
