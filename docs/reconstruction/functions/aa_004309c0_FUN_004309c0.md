# Function record: FUN_004309c0 / StdMap_StringKey_InsertOrFind

| Field | Value |
|---|---|
| **Stable ID** | `aa_004309c0` |
| **Canonical name** | `StdMap_StringKey_InsertOrFind` |
| **Ghidra name** | `FUN_004309c0` |
| **Address** | `0x004309c0`–`0x00430a96` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | stl-map |
| **Completion status** | **Dual sealed 2026-07-29 W21-P** — accept-with-gaps; ABI + insert-or-find CF High |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |
| **Dual A/B** | `reviews/A_aa_004309c0_StdMap_StringKey_InsertOrFind.md`, `reviews/B_aa_004309c0_StdMap_StringKey_InsertOrFind.md` |
| **Scratch** | `tmp/a_004309c0.md` |

## Purpose

MSVC red-black **insert-or-find** for a `std::map`-style tree keyed by **`std::basic_string`**. New keys call `FUN_00430b60` (insert+rebalance); existing keys return the node with `inserted=0` and **do not** rewrite mapped values.

## Signature (asm-sealed)

```c
struct InsertPair {
    void*   it;        // +0  node*
    uint8_t inserted;  // +4
};

// stack[0] = map shell* (head@+4, size@+8)
// EAX      = const basic_string* key
// EBX      = InsertPair* out
// return   = EBX; RET 4
InsertPair* StdMap_StringKey_InsertOrFind(
    void* map,
    const void* keyString /*EAX*/,
    InsertPair* out /*EBX*/);
```

### Node layout (from this unit + insert helper)

| Off | Field |
|----:|-------|
| +0x00 | left |
| +0x04 | parent |
| +0x08 | right |
| +0x0C | `basic_string` key |
| +0x2C | color |
| +0x2D | isnil |

Mapped value sits after the string (caller `FUN_00786470` uses `node+0x28` as object*) — not owned here.

## Artifacts

- Raw: `docs/reconstruction/raw/aa_004309c0_FUN_004309c0.md` (+ 2026-07-29 append)
- Annotated: `docs/reconstruction/raw/aa_004309c0_FUN_004309c0.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/StdMap_StringKey_InsertOrFind.cpp`
- Clean twin: `docs/reconstruction/reconstructed-exact/FUN_004309c0.cpp`
- Named scaffold: `docs/reconstruction/reconstructed-exact/Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_CVOGCharacter_Sear_004309c0.cpp`

## Callers / callees

**Callers (xrefs 2026-07-29):**

| Caller | Site |
|--------|------|
| `FUN_004303c0` (hint-insert front) | `0x00430521` |
| `FUN_00786470` (string→object ensure) | `0x007865b1` |

**Callees:**

| VA / symbol | Role |
|-------------|------|
| IAT `0x009c62e8` `operator<` | string key compare |
| `0x00430b60` | tree insert + RB rebalance |
| `0x004313d0` | tree predecessor |

## Confidence

| Claim | Level |
|---|---|
| Control flow decompile ≡ bytes | **High** |
| ABI stack map* + EAX key + EBX out; `RET 4` | **High** |
| Insert-or-find; equal key no value write | **High** |
| isnil `+0x2D`, key `+0x0C` | **High** |
| Helper product English names | Probable |
| Runtime / diff | Open |
