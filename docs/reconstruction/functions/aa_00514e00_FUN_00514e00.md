# Function record: FUN_00514e00 (name withheld)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00514e00` |
| **Canonical name** | `FUN_00514e00` |
| **Address** | `0x00514e00` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | skills-abilities |
| **Completion status** | **leave-FUN** — behavior dual A/B complete; product name withheld |
| **Name status** | **FUN_* retained** |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Decision 2026-07-29 (evidence-only)

**Leave `FUN_00514e00`.** Do **not** rename and do **not** promote `Skill_DecActiveCastCounter` / `_Inferred`.

| Gate | Result |
|---|---|
| Product string / debug name on body | **None** (leaf; no data refs) |
| RTTI | **None** |
| Exclusive single-caller use | **Not proven** — **3** callers / **5** UNCONDITIONAL sites |
| Promote role name from residual | **Blocked** — role High only; no string/RTTI |

Prior alias `Named_CalleeOf_Client_RecvSkillStatusEffect_00514e00` is **scaffold only** (exclusive Recv use falsified). Prior residual label `Skill_DecActiveCastCounter` (`reviews/a_0051aa00.md`) remains a **role** note, not a registry name.

## Behavior (Confirmed CF — dual A/B)

```c
void __fastcall FUN_00514e00(int param_1)
{
  int iVar1;
  int iVar2;

  iVar1 = *(int *)(param_1 + 100);   // this+0x64
  if (((iVar1 != 0) && (*(int *)(iVar1 + 0x10) != 0)) &&
     (iVar2 = *(int *)(iVar1 + 0x10) + -1, *(int *)(iVar1 + 0x10) = iVar2, iVar2 == 0)) {
    *(undefined4 *)(iVar1 + 0xc) = 0;
  }
  return;
}
```

- Load optional slot pointer at **`this+0x64`** (decimal 100). Same slot family as **`FUN_005169c0`** lazy-get (0x14 block).
- If slot non-null and counter **`+0x10` ≠ 0**: decrement; when result is 0, clear **`+0xC`**.
- **No** `operator_new` / lazy alloc; null slot → no-op.
- **No** `Skill_QueueDeferredCastId`; compare **`Skill_ClearActiveCastCounterAndQueueId`** which does lazy-get + same dec + queue + AL.
- Leaf: **no callees**.

### Callers

| Caller | Sites | Use |
|---|---|---|
| `Client_RecvSkillStatusEffect` | `0x008111c5`, `0x00811200`, `0x00811482` | Error abort (2×); wire **`0x11`** remove (1×) |
| `Skill_ApplyStatusEffectLocal` | `0x0051abd4` | After binding clear when not sim (`world+0x7e==0`) |
| `FUN_00606180` | `0x00606193` | WakeupSkill path when `*(hb+0x658)==0` |

**Inline twin:** `Skill_ApplyStatusEffectLocal` immediate-apply path also decrements `*(pCasterObj+0x64)+0x10` / clears `+0xC` in-line (not a call).

## Signature (decompiler-derived)

```c
void __fastcall FUN_00514e00(int param_1);
```

Object type of `param_1` / ECX not sealed (shared gap with `FUN_005169c0`).

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00514e00_FUN_00514e00.md`
- Annotated: `docs/reconstruction/raw/aa_00514e00_FUN_00514e00.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/FUN_00514e00.cpp`
- Dual A/B: `docs/reconstruction/reviews/A_aa_00514e00_FUN_00514e00.md`, `B_aa_00514e00_FUN_00514e00.md`
- Residual scratch: `docs/reconstruction/reviews/a_00514e00.md`

## Confidence

| Claim | Level |
|---|---|
| Control flow / offsets +0x64 / +0x10 / +0xC | **Confirmed** (dual decompile) |
| Leaf / null-safe / no queue | **Confirmed** |
| Callers 3 funcs / 5 sites | **Confirmed** (xrefs) |
| Subset of ClearActiveCastCounter dec half | **High** |
| Product / `_Inferred` name | **Withheld** |
| ECX owner product type | Open |
