# Function record: Object_HasFlag8SelfOrLinked (`FUN_005134e0`)

| Field | Value |
|---|---|
| **Stable ID** | `aa_005134e0` |
| **Canonical name** | `Object_HasFlag8SelfOrLinked` (proposed) |
| **Ghidra name** | `FUN_005134e0` |
| **Address** | `0x005134e0` |
| **Body** | `0x005134e0` … `0x00513540` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | targeting / object-flags |
| **Completion status** | **Dual complete (accept-with-gaps)** — 2026-07-29 W17-E; **mask `0x08` @ `+0x184` sealed** |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Alias

- Prior scaffold: `Named_CalleeOf_Client_INC_ContactCountdownTick_005134e0`
- Human role: **link-aware flag-8 predicate** (reject/exclude when true)

## Purpose

Return whether an object (or its linked parent/related entity) carries flag mask **`0x08`** (bit **3**) at **`+0x184`** (related MI path uses **`+0x188`** after this-adjust). Used as a soft-exclude gate by targeting, skill gather, AI, and contact countdown.

## Signature (decompiler + bytes)

```c
// ECX = Object*; no stack args; bool AL; ret
bool __fastcall Object_HasFlag8SelfOrLinked(int obj /* ECX */);
```

## Closed form

```text
if (obj+0xac == null)
  return (obj+0x184 & 8) != 0;
related = (obj+0xac)->vtbl[+0x19c]();
self = (obj+0x184 & 8) != 0;
if (related)
  return self || ((related + *(*(related+4)+4) + 0x188) & 8) != 0;
return self || ((obj+0xac)+0x184 & 8) != 0;
```

## Sealed flag

| Field | Value | Evidence |
|-------|-------|----------|
| **Mask** | **`0x08`** | `b2 08` / `24 08` / `test …, dl` |
| **Bit index** | **3** | `1<<3 = 8` (peers sometimes say “bit 8” meaning mask) |
| **Self offset** | `obj+0x184` | `test [esi+0x184], dl` |
| **Link field** | `obj+0xac` | same as dirty chain link |
| **Related virtual** | vtbl `+0x19c` | `call [eax+0x19c]` |
| **Related flag** | `related+adj+0x188` | MI path |

## Not this unit

| Role | VA |
|------|-----|
| Dirty mark helper (`\| 0x20000`) | `0x00512670` |
| Skill pair-accept helper (calls this when `filterB==0`) | `0x0058a810` |
| Targeting OnHeartBeat (calls this on owner) | `0x00604dd0` |
| Item Broken setter | `0x00513de0` |

## Artifacts

- Raw: `docs/reconstruction/raw/aa_005134e0_FUN_005134e0.md`
- Annotated: `docs/reconstruction/raw/aa_005134e0_FUN_005134e0.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/Object_HasFlag8SelfOrLinked.cpp`
- Scaffold clean: `docs/reconstruction/reconstructed-exact/FUN_005134e0.cpp`
- Dual A: `docs/reconstruction/reviews/A_aa_005134e0_Object_HasFlag8SelfOrLinked.md`
- Dual B: `docs/reconstruction/reviews/B_aa_005134e0_Object_HasFlag8SelfOrLinked.md`
- Report: `docs/agents/task-dual-ab-005134e0-object-hasflag8selforlinked-report.md`

## Callers / callees (sample; full census open)

| Direction | Examples |
|-----------|----------|
| Callees | parent vtbl `+0x19c` only (no FUN_* leaf) |
| Callers | `FUN_0058a810`, `CVOGHBAIBase_FindTargetToAttack`, `CVOGHBAIBase_GetTargetFromAggro`, `Client_INC_ContactCountdownTick`, `FUN_00604dd0` (targeting HB), many UI/interact |

## Confidence

| Claim | Level |
|---|---|
| Control flow from decompiler + bytes | **High / Sealed** |
| Mask `0x08` / bit 3 @ `+0x184` | **High / Sealed** |
| Link-aware OR semantics | **High / Sealed** |
| Product English for the flag | Open / Tentative |
| Original PDB symbol | Open |
