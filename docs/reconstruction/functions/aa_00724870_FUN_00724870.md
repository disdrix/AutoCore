# Function record: CSoundManager_ProbeSoundFileExists_Inferred

| Field | Value |
|---|---|
| **Stable ID** | `aa_00724870` |
| **Canonical name** | `CSoundManager_ProbeSoundFileExists_Inferred` |
| **Ghidra name** | `FUN_00724870` |
| **Address** | `0x00724870` |
| **Body range** | `0x00724870`–`0x007249b0` (320 / `0x140` bytes) |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | client audio / CSoundManager path probe |
| **Completion status** | **partial** — dual A/B **accept-with-gaps**; CF + ABI + callees sealed; product name / outKind enum / root filler open |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |
| **Dual A/B** | `reviews/A_aa_00724870_CSoundManager_ProbeSoundFileExists_Inferred.md`, `reviews/B_aa_00724870_CSoundManager_ProbeSoundFileExists_Inferred.md` (2026-07-29 W21-M) |
| **Last reviewed** | `2026-07-29` |

## Alias

- `FUN_00724870`
- `Named_CalleeOf_Named_CalleeOf_Client_PlayNamedInterfaceSound_00724870` (legacy)
- `Named_CalleeOf_CSoundManager_ResolveSoundPath_00724870`

## Purpose

Probe whether a sound path exists on disk (after appending `.ogg` / `_lxx.ogg`), under configured search roots, or via a VFS/config fallback. Optional outs: copy normalized path; write kind **`2`**. Returns bool in **AL**. Used by `CSoundManager_ResolveSoundPath` before play/resolve.

## Signature

```c
// customcc + stdcall stack cleanup
bool CSoundManager_ProbeSoundFileExists_Inferred(
    CSoundManager *self /*stack0*/,
    char *outPath /*stack1, opt*/,
    int *outKind /*stack2, opt*/,
    const char *path /*EAX*/);
// RET 0x0C
```

## Algorithm

1. Copy `path` → stack buffer; `FUN_00722790(self, buf)` appends extension from `self+0x308`.
2. `cfg = FUN_007b6a20()`. If `cfg+4 & 1`:
   - `PathFileExistsA(buf)` → success.
   - Else for each root string in `DAT_00d1e574`..`0x00d1e7a4` step `0x1c`: build `root + basename(buf)` (`FUN_007b6420`); `PathFileExistsA`.
3. Else/miss: `FUN_007b66c0(cfg)` fallback; fail → return 0.
4. Success: optional `strcpy(outPath, buf)`; optional `*outKind = 2`; return 1.

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00724870_FUN_00724870.md`
- Annotated: `docs/reconstruction/raw/aa_00724870_FUN_00724870.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/CSoundManager_ProbeSoundFileExists_Inferred.cpp`
- Scaffold: `docs/reconstruction/reconstructed-exact/FUN_00724870.cpp`
- Review A/B: as above
- Report: `docs/agents/task-dual-ab-00480970-00724870-w21m-report.md`

## Callers / callees

| Direction | Notes |
|---|---|
| **Callees** | `FUN_00722790`, `FUN_007b6a20`, `PathFileExistsA`, `FUN_007b6420`, `FUN_007b66c0` |
| **Callers** | `FUN_007249c0` (`CSoundManager_ResolveSoundPath`) ×2; `FUN_00726390`; `FUN_00727140` |

## Confidence

| Claim | Level |
|---|---|
| Control flow + body size | **High** |
| EAX path + stdcall 3 args + RET 0xC | **Confirmed** |
| Extension append + PathFileExistsA + root walk | **Confirmed** |
| `*outKind = 2` on success | **Confirmed** |
| Product name / kind enum English | **Inferred** / open |
| Root table filler | Open |

## Related

- `aa_007249c0` — `CSoundManager_ResolveSoundPath_Inferred` (primary parent)
- `aa_00722790` — ogg extension append
- Search roots `DAT_00d1e574` (20 × `0x1c`)
