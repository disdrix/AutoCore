# Function record: FUN_00538ea0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00538ea0` |
| **Canonical name** | `FUN_00538ea0` |
| **Inferred name** | `Map_InsertNodeAndRebalance` (mission-staging 0x50-node specialization) |
| **Address** | `0x00538ea0` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | `missions-progression` |
| **Completion status** | **Dual-sealed 2026-07-29** — accept-with-gaps |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Alias

Human chain name (scaffold): `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Client_RecvNpcMiss_00538ea0`  
Role name (dual): **map/set tree insert node + red-black rebalance** (always allocates).

## Purpose

`__thiscall` MSVC-style `_Tree` insert helper for the mission-staging map shell (`head@+4`, `size@+8`). Allocates a **0x50** node via `FUN_005380d0` (14-dword value @ `+0x10`, color @ `+0x48` red=0, isnil @ `+0x49`), links under `where` by `addLeft`, rebalances (rotates `FUN_005681a0` / `FUN_00568b70` + one inlined L-rotate), paints root black, writes `*outIt`. Throws `"map/set<T> too long"` if `size > 0x4924922`.

**Not** insert-or-find: parent `FUN_00539cb0` performs the key walk and only calls this on miss.

## Signature (byte-sealed)

```c
// __thiscall; RET 0x10
uint32_t* /* outIt* */ __thiscall FUN_00538ea0(
    int mapThis,              // ECX — MapShell*
    uint32_t* outIt,          // *outIt = new node
    char addLeft,             // 1 left / 0 right of where
    uint32_t* where,          // parent node* (or head if empty)
    const void* valuePair);   // 14 dwords → node+0x10
```

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00538ea0_FUN_00538ea0.md`
- Annotated: `docs/reconstruction/raw/aa_00538ea0_FUN_00538ea0.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/FUN_00538ea0.cpp`
- Dual A: `docs/reconstruction/reviews/A_aa_00538ea0_Map_InsertNodeAndRebalance.md`
- Dual B: `docs/reconstruction/reviews/B_aa_00538ea0_Map_InsertNodeAndRebalance.md`
- Parent insert dual: `reviews/A_aa_00539cb0_MissionStagingMap_Insert.md` / `B_…`

## Callers / callees

| Direction | VA | Name / role |
|---|---|---|
| Caller (sole static) | `0x00539cb0` | MissionStagingMap_Insert (×2 sites) |
| Callee | `0x005380d0` | Node construct `new(0x50)` |
| Callee | `0x005681a0` | Tree rotate right |
| Callee | `0x00568b70` | Tree rotate left |
| Callee | STL | `basic_string` / `exception` / `_CxxThrowException` |

## Confidence

| Claim | Level |
|---|---|
| Control flow from decompiler ≡ raw ≡ body bytes | **High** |
| `RET 0x10` ABI + always-insert + RB rebalance | **High** |
| Node `0x50`, color `+0x48`, isnil `+0x49`, 0=red/1=black | **High** |
| Sole static caller `FUN_00539cb0` | **High** |
| Product English / typedef name | **Open** |
| Runtime / bit-exact | **Open** |
