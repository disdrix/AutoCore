# Function record: FUN_00404c90 (Object_GetWorldPositionPtr)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00404c90` |
| **Canonical name** | `FUN_00404c90` |
| **Probable role name** | `Object_GetWorldPositionPtr` (inferred; no retail string in body) |
| **Address** | `0x00404c90` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **Body range** | `00404c90`–`00404cad` |
| **System** | `shared-object-pose` (DriveControlTick / airStab / aim / AI / network pose) |
| **Completion status** | **Dual-reviewed** — three-rep present; A/B sealed CF 2026-07-29 |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Alias

- Scaffold: `Named_CalleeOf_Client_RecvBroadcast_00404c90` (parent-seed only; **not** exclusive owner)
- Twin: `FUN_00404a20` @ `0x00404a20` (orientation / quat pointer getter)

## Purpose

Leaf **world-position pointer** getter for an entity/object (`this` in ECX):

- If `*(this+8) != 0`: return `*(*(this+8)+0x3c) + 0xb0` (rigid-body world pos slot).
- Else: return `*(*(this+4)+4) + this + 0x84` (entity-embedded pose fallback).

Returns a **pointer** (callers load floats from the address). No side effects.

## Signature (decompiler-derived + bytes)

```c
int __fastcall FUN_00404c90(int pEntity);  // ECX = this; returns float* as int
```

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00404c90_FUN_00404c90.md`
- Annotated: `docs/reconstruction/raw/aa_00404c90_FUN_00404c90.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/FUN_00404c90.cpp`
- Review A: `docs/reconstruction/reviews/A_aa_00404c90_Object_GetWorldPositionPtr.md`
- Review B: `docs/reconstruction/reviews/B_aa_00404c90_Object_GetWorldPositionPtr.md`
- Prior RB map: `docs/reconstruction/physics/verified/fn_offsets_rigidbody.md`

## Callers / callees

- **Callees:** none (leaf).
- **Callers (sample):** `Client_Input_DriveControlTick` (`0x0092339d`, `0x009233af`), `VehicleAction_airStabilization`, `CVOGPlayerVehicle_UpdateTurretAiming`, `Client_SendRespawnInSector`, `Client_CastSkillFromQuickBarSlot`, `Client_RecvBroadcast`, `CVOGHBAICreatureBase_DoVehiclePursue` (many), `CVOGCombat_OnDeathAwardKillXp`, … (60+ xref sites).

## Confidence

| Claim | Level |
|---|---|
| Control flow from decompiler + body bytes | **Confirmed** |
| Physics path `rb+0xb0` / fallback `+0x84` | **Confirmed** |
| ECX this / pointer return | **Confirmed** / **High** |
| Role = world position pointer | **High** |
| Product symbol name | **Open** (inferred) |
| Types for `this+4` chain | **Probable** / Tentative |
