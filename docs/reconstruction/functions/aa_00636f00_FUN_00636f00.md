# Function record: EMSG_Sector_Damage_Unpack

| Field | Value |
|---|---|
| **Stable ID** | `aa_00636f00` |
| **Canonical name** | `EMSG_Sector_Damage_Unpack` (Ghidra; scaffold also `FUN_00636f00`) |
| **Address** | `0x00636f00` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | `client-net` / `combat-floaters` |
| **Completion status** | **Dual-reviewed (accept-with-gaps)** — A+B 2026-07-29 |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Purpose

Unpack TNL special message **opcode `0x2023`** (`EMSG_Sector_Damage`) from a bound `BitStream` into an owned packed buffer (header `0x1C` + `count × 0x20` entries), wrap in a refcounted `0x18`-byte object, return via out-pointer. No damage apply / no floater queue in this unit.

## Signature (decompiler-derived)

```c
undefined4 * EMSG_Sector_Damage_Unpack(undefined4 *param_1 /* out */, int *param_2 /* TNL msg */);
```

- `param_1`: out slot; `*param_1 = wrapper`.
- `param_2`: refcounted message; data ptr/len at `param_2[3]` / `param_2[4]`; refcount at `param_2[2]`.

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00636f00_FUN_00636f00.md`
- Annotated: `docs/reconstruction/raw/aa_00636f00_FUN_00636f00.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/FUN_00636f00.cpp`
- Review A: `docs/reconstruction/reviews/A_aa_00636f00_EMSG_Sector_Damage_Unpack.md`
- Review B: `docs/reconstruction/reviews/B_aa_00636f00_EMSG_Sector_Damage_Unpack.md`

## Callers / callees

| Direction | Symbol | VA / notes |
|---|---|---|
| Caller | `Client_UnpackSpecialMessage` | `0x00637c20` — only when opcode `0x2023` (call site `0x00637c87`) |
| Callee | `FUN_0042b3a0` | BitStream bind |
| Callee | `BitStream_readBits` | `0x0042b670` |
| Callee | `BitStream_readFlag` | `0x0042ad50` (header; loop often inlined) |
| Callee | `FUN_0042b250` / `FUN_0042b270` | wrapper base / stream teardown |
| Callee | `malloc` / `free` / `operator_new` / `operator_new[]` / `operator_delete[]` | buffers |

## Wire (summary)

| Field | Bits / form |
|---|---|
| Source coid | 64 |
| Source global | flag |
| Count | 16 |
| Per entry | flag Crit → i16 amount → 64 target coid → flag global → flag Resist → flag Deflect → 7 residual flags |
| Entry memory | `0x20` bytes; residual flag stores non-sequential (`+0x1B,1C,1D` then `+0x15…1A`) |
| Packed size | `0x1C + count*0x20` |

## Confidence

| Claim | Level |
|---|---|
| Control flow from decompiler | **High** |
| Opcode / caller binding | **High** |
| Wire order + entry stride | **High** |
| Residual flag semantics | Open |
| Wrapper class name | Open |
| Parameter semantic names | High for roles; C++ types tentative |
