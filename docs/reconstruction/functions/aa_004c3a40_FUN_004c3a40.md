# Function record: `aa_004c3a40` CVOGObject_SetLocalPosition4_Gated_Inferred

| Field | Value |
|---|---|
| **Stable ID** | `aa_004c3a40` |
| **Canonical name (Ghidra)** | `FUN_004c3a40` |
| **Proposed name** | `CVOGObject_SetLocalPosition4_Gated_Inferred` (**Probable**) |
| **Rejected primary alias** | `Named_VOG_DEBUG_STOP_004c3a40` — string is NaN trap only, not primary role |
| **Address** | `0x004c3a40` |
| **Body span** | `0x004c3a40` – `0x004c3ab0` (113 bytes; exclusive end `0x004c3ab1`) |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | `input-drive-control` (DriveControlTick callee) + object pose |
| **Completion status** | **Partial** — dual A/B sealed 2026-07-29; runtime / product name / bit producers open |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |
| **Dual reviews** | `reviews/A_aa_004c3a40_CVOGObject_SetLocalPosition4_Gated_Inferred.md`, `reviews/B_aa_004c3a40_CVOGObject_SetLocalPosition4_Gated_Inferred.md` |
| **Scratch** | `reviews/a_004c3a40.md` |

## Purpose

Gated store of a **float4** into **`object+0x240..+0x24c`** (local position / pose override). Runs `_isnan` on **xyz** only; on any NaN calls `FUN_007a4480(0,"VOG_DEBUG_STOP")` (logger **no-op** when first arg is 0) then **continues**. Writes only when `(component_flags_at_typeOff+0xb8 & 0xC7) == 0`, via leaf `FUN_004cb3e0`.

Sibling getter `FUN_004c3ac0` returns `this+0x240` when `(flags & 7) == 0`, else phys body position or embedded slot.

## Signature (sealed)

```c
void __thiscall FUN_004c3a40(void *self, float *in /* vec4; NaN on [0..2] */);
// ABI: ECX=self, stack arg in, ret 4
```

## Control flow (sealed)

```
isnan(in[0]) || isnan(in[1]) || isnan(in[2]) → FUN_007a4480(0, "VOG_DEBUG_STOP")
// fall through:
typeOff = *(*(self+4)+4)
if ((*(u8*)(self + typeOff + 0xb8) & 0xC7) == 0)
    FUN_004cb3e0(self, in)   // self+0x240..0x24c = in[0..3]
```

## Artifacts

- Raw: `docs/reconstruction/raw/aa_004c3a40_FUN_004c3a40.md`
- Annotated: `docs/reconstruction/raw/aa_004c3a40_FUN_004c3a40.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/FUN_004c3a40.cpp`
- Named scaffold (historical): `docs/reconstruction/reconstructed-exact/Named_VOG_DEBUG_STOP_004c3a40.cpp`
- Dual A/B: `docs/reconstruction/reviews/A_aa_004c3a40_*.md`, `B_aa_004c3a40_*.md`
- Residual: `docs/reconstruction/reviews/a_004c3a40.md`

## Callers (live xrefs 2026-07-29)

| Function | VA | Sites |
|----------|-----|-------|
| `Client_Input_DriveControlTick` | `0x009223b0` | `0x00922866` |
| `FUN_0091a5f0` (look apply) | `0x0091a5f0` | `0x0091a960`, `0x0091aa07` |
| `FUN_005cc7c0` | `0x005cc7c0` | `0x005cc7df` |

## Callees

| Callee | Role |
|--------|------|
| `_isnan` (IAT `0x009c655c`) | xyz finite check |
| `FUN_007a4480` | debug log helper (no-op when mode=0) |
| `FUN_004cb3e0` | copy 4×u32 → `this+0x240..0x24c` |

## Confidence

| Claim | Level |
|---|---|
| Control flow / gate imm / store layout | **High** (three-rep + `read_memory`) |
| Trap no-op @ mode 0 | **High** |
| Caller inventory (4 sites) | **High** |
| Proposed English name | **Probable** (`_Inferred`) |
| `+0xb8` bit producers / product symbol | **Open** |
| Runtime / differential | **Open** |
