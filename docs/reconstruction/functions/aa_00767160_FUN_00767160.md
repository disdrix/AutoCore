# Function record: stoChunkWriter_WriteFormatted (FUN_00767160)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00767160` |
| **Canonical name** | `stoChunkWriter_WriteFormatted` |
| **Ghidra name** | `FUN_00767160` |
| **Address** | `0x00767160` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | `storage` / `arda2.stoChunk` (text mode) |
| **Completion status** | **Dual-reviewed** (W16-K) — A accept-with-gaps, B accept-with-gaps |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |
| **Dual reviews** | `reviews/A_aa_00767160_stoChunkWriter_WriteFormatted.md`, `reviews/B_aa_00767160_stoChunkWriter_WriteFormatted.md` |
| **Wave owner** | W16-K (OWN-ONLY) |

## Alias

- Scaffold: `Named_CalleeOf_Named_stoChunk_00767160`
- Structural: `stoChunkWriter_PrintfText` / text-mode formatted write

## Purpose

Printf-style text emit for **stoChunkWriter**: `_vsnprintf` into 1 KiB stack buffer, soft-wrap at column **0x78** with `"\r\n"`, indent at line start (`FUN_00767070`), single-space mid-line separators, write body via stream `vt+0x18`, OR status into `writer+0x04`, return status. Writer arrives in **EAX** (not ECX).

## Signature (decompiler + asm sealed)

```c
// Retail ABI: EAX = stoChunkWriter*; stack format + varargs; returns status
uint32_t FUN_00767160(char *format, /* ... */);
```

## Writer fields (this unit)

| Off | Field |
|---|---|
| `+0x00` | `stream*` |
| `+0x04` | status (OR of write results; returned) |
| `+0x20` | text column |

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00767160_FUN_00767160.md`
- Annotated: `docs/reconstruction/raw/aa_00767160_FUN_00767160.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/FUN_00767160.cpp`
- Dual A: `docs/reconstruction/reviews/A_aa_00767160_stoChunkWriter_WriteFormatted.md`
- Dual B: `docs/reconstruction/reviews/B_aa_00767160_stoChunkWriter_WriteFormatted.md`

## Callers / callees

**Callees:** `_vsnprintf` (IAT); stream `vt+0x18` write; `FUN_00767070` (indent).

**Callers (sample):** `stoChunkWriter_EndChunk` (text `"}"`), `gfxGeometryPiece_Serialize` string writers, other sto text serializers.

## Confidence

| Claim | Level |
|---|---|
| Control flow (wrap / indent / space / body write) | **High** (decompile ≡ asm) |
| DAT `"\r\n"` / `" "` | **High** (`read_memory`) |
| Column @ `+0x20`, status @ `+0x04` | **High** |
| Writer in **EAX** | **High** (`mov esi,eax` + EndChunk plate) |
| Product English name | Tentative |
| `vsnprintf` negative/overflow length | Residual |

## Residual

1. Product name / exact plate symbol in `stoChunk.cpp`.
2. Full contract of indent helper `FUN_00767070` (own VA residual).
3. Runtime / image diff not run.
