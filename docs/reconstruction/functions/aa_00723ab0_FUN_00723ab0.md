# Function record: FUN_00723ab0 / CSoundManager::_stopSound

| Field | Value |
|---|---|
| **Stable ID** | `aa_00723ab0` |
| **Canonical name** | `CSoundManager::_stopSound` (string-sealed); Ghidra `FUN_00723ab0` |
| **Address** | `0x00723ab0` – `0x00723b14` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | client audio / `CSoundManager` |
| **Completion status** | **Dual A/B sealed** (OWN W16-H, 2026-07-29) |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |
| **Verdict** | **accept-with-gaps** |

## Alias

- Human / product: `CSoundManager::_stopSound`
- Prior scaffold alias: `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_CVOGReaction_Spawn_00723ab0` (caller-chain naming; superseded for product identity by log string)

## Purpose

Stop a single sound **slot by index**: validate index `< 0x30` and non-null table entry at `this+0x31c`, require stop-eligible flags (`+0x38==0` and `+0x3a==0`), write envelope float at `slot+0x1b0` via `FUN_007227e0(0)` → **`-10000.0f`**, mark stopped / clear start-playing, return **AL=1**. On bad flags, log and return **0**.

## Signature (assembly-sealed)

```c
// ECX = CSoundManager* this; EAX = uint slotIndex; return AL bool
uint8_t __fastcall FUN_00723ab0(int this /*ECX*/ /*, EAX index */);
```

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00723ab0_FUN_00723ab0.md`
- Annotated: `docs/reconstruction/raw/aa_00723ab0_FUN_00723ab0.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/FUN_00723ab0.cpp`
- Review A: `docs/reconstruction/reviews/A_aa_00723ab0_CSoundManager_stopSound.md`
- Review B: `docs/reconstruction/reviews/B_aa_00723ab0_CSoundManager_stopSound.md`

## Callers / callees

| Direction | Target | Notes |
|---|---|---|
| Callee | `FUN_007227e0` | float helper; arg 0 → `-10000.0f` |
| Callee | `FUN_007a4480` | log |
| Caller (sibling dual) | `FUN_00723b20` StopMatchingSounds | walks `+0x31c`, calls this per match |

## Confidence

| Claim | Level |
|---|---|
| Control flow from live decompile + `read_memory` | **High** |
| Product name `_stopSound` | **High** (string) |
| Index in EAX; this in ECX | **High** |
| Flags `+0x38` / `+0x3a` semantics from log text | **High** |
| `+0x1b0` product field name | **Tentative** |
| Types beyond byte/float widths | Tentative |
