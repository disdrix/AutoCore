# Function record: FUN_005380d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_005380d0` |
| **Canonical name** | `FUN_005380d0` (inferred: `StagingMapNode_Construct`) |
| **Address** | `0x005380d0` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | `missions-progression` |
| **Completion status** | **Dual sealed 2026-07-29** — accept-with-gaps |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |
| **Dual A** | `docs/reconstruction/reviews/A_aa_005380d0_StagingMapNode_Construct.md` |
| **Dual B** | `docs/reconstruction/reviews/B_aa_005380d0_StagingMapNode_Construct.md` |

## Alias

- Scaffold human name: `Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Named_CalleeOf_Cli_005380d0`
- Inferred role name: **`StagingMapNode_Construct`** / `MissionStagingMap_Node_Ctor` (product English open)

## Purpose

Freestanding node constructor for the character **mission-staging** red-black map (`Character+0x508` via insert chain). Allocates **0x50** bytes, writes left/parent/right, copies **14 dwords** (key + pad + 12-dword payload) to `+0x10`, sets color from arg and **isnil=0**. Does not link into the tree or rebalance — sole caller `FUN_00538ea0` owns that.

## Signature (image-sealed)

```c
// RET 0x14; result in EAX (0 if operator_new failed)
StagingMapNode* FUN_005380d0(
    StagingMapNode* left,       // stack +4
    StagingMapNode* parent,     // stack +8
    StagingMapNode* right,      // stack +C
    const uint32_t* valuePair,  // stack +10 — 14 dwords
    uint8_t color);             // stack +14 (byte in dword slot)
```

Ghidra decomp labels return `void` — **incorrect**; caller consumes EAX.

## Sealed node layout

```
StagingMapNode  // size 0x50
  +0x00  left*
  +0x04  parent*
  +0x08  right*
  +0x0C  (uninitialized by this ctor)
  +0x10  key (mission id)
  +0x14  pad dword
  +0x18  MissionStagingPayload[12]  // 0x30
  +0x48  color (u8)
  +0x49  isnil (u8) = 0
  +0x4A  pad to 0x50
```

## Artifacts

- Raw: `docs/reconstruction/raw/aa_005380d0_FUN_005380d0.md`
- Annotated: `docs/reconstruction/raw/aa_005380d0_FUN_005380d0.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/FUN_005380d0.cpp`
- Scratch: `docs/reconstruction/reviews/a_005380d0.md`

## Callers / callees

| Direction | VA | Name | Notes |
|---|---|---|---|
| Callee | `0x00489892` | `operator_new` | size `0x50` |
| Caller | `0x00538ea0` | staging map insert+rebalance | sole static; color arg `0`; left=right=head |

## Confidence

| Claim | Level |
|---|---|
| Control flow + bytes (`RET 0x14`, `REP MOVSD` 0xE, offsets) | **High** |
| Return node\* in EAX | **High** |
| Role: staging map node construct only | **High** |
| Product English name | **Open** |
| Parameter semantic names (left/parent/right/value/color) | **High** (caller + MSVC map shape) |
| Pad `+0x14` English | **Open** (layout sealed) |
