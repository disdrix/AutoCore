# Function record: NDError_Log (`FUN_007a4480`)

| Field | Value |
|---|---|
| **Stable ID** | `aa_007a4480` |
| **Canonical name** | `NDError_Log` |
| **Ghidra name** | `FUN_007a4480` |
| **Address** | `0x007a4480`–`0x007a44d0` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | libND diagnostics (`NDError.cpp`) |
| **Completion status** | **Sealed body** — dual A/B accept 2026-07-29 (OWN-ONLY) |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Alias

- `Named_NDError_007a4480` (path string `NDError`)
- Informal: “VOG_DEBUG_STOP helper” — **misnomer**; that string is call-site data only

## Purpose

Conditional logger: if `enable == 1` and `fmt != NULL`, format with `_vsnprintf` into a 2048-byte buffer and call `vog_LogMessage` with fixed stamp (`...\libND\NDError.cpp`, line `0x65`=101, level `3`). Otherwise return without logging. **Not** a hard stop.

## Signature (sealed)

```c
void __cdecl NDError_Log(char enable, const char *fmt, ...);
// Ghidra display: void FUN_007a4480(char param_1, char *param_2)
// Stack: [esp+4]=enable, [esp+8]=fmt, [esp+0xC]=first vararg
```

## Artifacts

- Raw: `docs/reconstruction/raw/aa_007a4480_FUN_007a4480.md`
- Annotated: `docs/reconstruction/raw/aa_007a4480_FUN_007a4480.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/FUN_007a4480.cpp`
- Named clean: `docs/reconstruction/reconstructed-exact/NDError_Log.cpp`
- Named alias: `docs/reconstruction/reconstructed-exact/Named_NDError_007a4480.cpp`
- Review A: `docs/reconstruction/reviews/A_aa_007a4480_NDError_Log.md`
- Review B: `docs/reconstruction/reviews/B_aa_007a4480_NDError_Log.md`
- Dual report: `docs/agents/task-dual-ab-007a4480-nderror-log-report.md`

## Callers / callees

| Direction | Notes |
|---|---|
| **Callees** | `_vsnprintf` (IAT `0x009c64b4`), `vog_LogMessage` |
| **Callers** | ~**1921** xrefs (inventory). Sample: hash lock asserts, mission/creature/map fail paths; many pass `enable=0` + `"VOG_DEBUG_STOP"` → **no-op** |

## Confidence

| Claim | Level |
|---|---|
| Control flow / enable gate | **High** |
| Varargs cdecl ABI | **High** |
| Fixed path / line / level | **High** |
| Not a hard-stop | **High** |
| Product name of log level `3` | Medium |
| Exhaustive enable==1 site map | Open (not OWN residual) |
