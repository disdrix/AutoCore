# Function record: Item_SetBroken (`FUN_00513de0`)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00513de0` |
| **Canonical name** | `Item_SetBroken` (proposed) |
| **Ghidra name** | `FUN_00513de0` |
| **Address** | `0x00513de0` |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | `inventory-transfer` |
| **Completion status** | **Dual complete (accept-with-gaps)** — 2026-07-29 first full dual; **mask `0x80000` sealed** |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |

## Alias

- Prior scaffold: `Named_CalleeOf_Client_OnReverseEngineerFailure_00513de0`
- Human role: **`ItemFlag_Broken` setter** (write bit 19)

## Purpose

Set or clear `ItemFlag_Broken` on an inventory item object. Flag lives at **`item+0x17c` bit 19**, mask **`0x80000`**. On value change, calls dirty helper `FUN_00512670` (`| 0x20000` on self and `+0xac` chain). If Broken is set after the write, **tail-jumps** to **vtable `+0x90`** with stack argument **0**.

## Signature (decompiler + bytes)

```c
// ECX = Item*; stack: desiredBroken (low byte); ret 4
void __thiscall Item_SetBroken(int *item /* ECX */, unsigned desiredBroken /* stack */);
```

## Closed form

```text
if (Broken(item) != (desired & 0xff))  // compare bit19 vs full low byte
  FUN_00512670(item);                  // | 0x20000 dirty chain
item.flags[+0x17c] ^= (((desired & 0xff) << 19) ^ flags) & 0x80000;
if (Broken(item))                      // post-write
  tail → vtbl[+0x90](0);
```

## Sealed mask

| Field | Value | Evidence |
|-------|-------|----------|
| **Mask** | **`0x80000`** | decompile + bytes `81 e6 00 00 08 00` |
| **Bit** | **19** (`0x13`) | `shr`/`shl` 0x13; `1<<19` |
| **Offset** | `item+0x17c` (`object[0x5f]`) | `mov`/`xor [ecx+0x17c]` |
| **Name** | `ItemFlag_Broken` | registry + peer duals |

## Not this unit

| Role | VA |
|------|-----|
| Dirty mark helper (`| 0x20000`) | `0x00512670` |
| Bit-20 sibling setter (`0x100000`) | `0x00513e30` |
| Stack eligibility (reads Broken) | `0x00513e70` |
| Stack capacity | `0x0040abf0` |

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00513de0_FUN_00513de0.md`
- Annotated: `docs/reconstruction/raw/aa_00513de0_FUN_00513de0.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/Item_SetBroken.cpp`
- Scaffold clean: `docs/reconstruction/reconstructed-exact/FUN_00513de0.cpp`
- Dual A: `docs/reconstruction/reviews/A_aa_00513de0_Item_SetBroken.md`
- Dual B: `docs/reconstruction/reviews/B_aa_00513de0_Item_SetBroken.md`
- Report: `docs/agents/task-dual-ab-00513de0-item-setbroken-report.md`

## Callers / callees (sample; full census open)

| Direction | Examples |
|-----------|----------|
| Callees | `FUN_00512670`; vtbl `+0x90` (tail, when Broken set) |
| Callers (raw peers) | `FUN_004d08c0` (set 1); `FUN_004d4440` (set 1); `FUN_0050ac80`; ReverseEngineerFailure path |

## Confidence

| Claim | Level |
|---|---|
| Control flow from decompiler + bytes | **High / Sealed** |
| Mask `0x80000` / bit 19 | **High / Sealed** |
| Role = Broken setter | **High / Sealed** (mask + peer string chain) |
| Parameter semantic names | Probable |
| Vtbl `+0x90` product name | Tentative / Open |
| Original PDB symbol | Open |
