# Function record: FUN_00423e00

| Field | Value |
|---|---|
| **Stable ID** | `aa_00423e00` |
| **Canonical name** | `AssManager_RemoveResolver_Inferred` |
| **Ghidra name** | `FUN_00423e00` |
| **Address** | `0x00423e00`–`0x00423e43` (**68 B**) |
| **Module** | `autoassault.exe` (image base `0x400000`) |
| **System** | `assets` / `assManager` |
| **Completion status** | **partial** — dual A/B **accept**; CF + bytes + strings + ABI sealed; runtime open |
| **Bit-for-bit / runtime / diff** | Open (deferred / not run) |
| **Dual A/B** | `reviews/A_aa_00423e00_AssManager_RemoveResolver_Inferred.md`, `reviews/B_aa_00423e00_AssManager_RemoveResolver_Inferred.md` (2026-07-29 W29-C) |
| **Last reviewed** | `2026-07-29` |

## Alias

- `FUN_00423e00`
- `Named_assManager_00423e00` (legacy string scaffold)

## Purpose

**assManager** remove-resolver worker: test whether resolver key exists in GuardedVector at `this+0x58`; if missing, log `"Removing resolver not found"` from `assManager.cpp` line `0x27b` level 3 and return; if present, erase first equal via `GuardedVector_EraseFirstEqual` (`FUN_0043c550`). Soft-fail on miss (no throw).

## Signature

```c
// ECX = assManager*; EAX = resolver key (dword); plain RET
void AssManager_RemoveResolver_Inferred(void *assManager /*ECX*/, int key /*EAX*/);
```

Decompiler surface `void(void)` is **incomplete** — formals are register-only.

## Algorithm

1. `local_key = EAX`; `ESI = ECX + 0x58` (resolver list container).
2. `found = FUN_0043e5b0(&local_key)` with **EAX = container** (membership / first-equal scan, optional CS).
3. If `!found`: `vog_LogMessage(path, 0x27b, 3, "Removing resolver not found")`; return.
4. Else: `FUN_0043c550(&local_key)` with **ESI = container** (`RET 4`).

## Artifacts

- Raw: `docs/reconstruction/raw/aa_00423e00_FUN_00423e00.md`
- Annotated: `docs/reconstruction/raw/aa_00423e00_FUN_00423e00.annotated.md`
- Clean: `docs/reconstruction/reconstructed-exact/AssManager_RemoveResolver_Inferred.cpp`
- Alias clean: `docs/reconstruction/reconstructed-exact/FUN_00423e00.cpp`
- Legacy: `docs/reconstruction/reconstructed-exact/Named_assManager_00423e00.cpp`
- Named record: `docs/reconstruction/functions/aa_00423e00_AssManager_RemoveResolver_Inferred.md`
- Review A/B: as above
- Report: `docs/agents/task-dual-ab-00436860-00423e00-w29c-report.md`

## Callers / callees

| Direction | Notes |
|---|---|
| **Callees** | `FUN_0043e5b0` (find), `vog_LogMessage`, `FUN_0043c550` / `GuardedVector_EraseFirstEqual` (W28-C) |
| **Callers (1)** | `FUN_007b65d0` @ `0x007b65df`: `EAX = [EBX+8]`, `ECX = [DAT_00d1f058]` (assManager singleton) |

## Confidence

| Claim | Level |
|---|---|
| Control flow + bytes | **High** |
| ECX this / EAX key ABI | **High** (caller + body bytes) |
| Container `this+0x58` | **High** |
| Soft-fail log on miss | **High** (string + CF) |
| Erase via first-equal | **High** (W28-C callee dual) |
| Product method English | **Inferred** |
| Key type (ptr vs id) | Open beyond dword equality |
| Runtime / differential | Open |

## Related

- Erase callee dual: `GuardedVector_EraseFirstEqual` `aa_0043c550` (W28-C)
- Parent teardown path: `FUN_007b65d0` (also inlines same find/log/erase for `[EBX+0xC]`)
