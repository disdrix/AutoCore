# Review A (reconstruction fidelity): `aa_007a42d0` FUN_007a42d0 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_007a42d0` |
| **VA** | `0x007a42d0` |
| **Canonical name** | `FUN_007a42d0` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Reconstruction fidelity / evidence-only naming |
| **Counterpart** | `reviews/B_aa_007a42d0_FUN_007a42d0.md` |
| **System** | util-rng (callee of `CVOGReaction_RandomUnitScalar`; LocalRange accuracy path) |
| **Verdict** | **leave-FUN** — ctor CF sealed; product class name withheld |

**Evidence:** Ghidra `decompile_function` @ `0x007a42d0`; parent dual `aa_007a4330`. Prefer decompile (no `disassemble_bytes`).

---

## 1. Purpose (behavioral)

**In-place construct** of the Meyers-singleton RNG object whose storage is `&DAT_00d20c1c` (EDI on cold path from `CVOGReaction_RandomUnitScalar`).

Body:

1. Write vtbl `PTR_FUN_00a97168` at `*this`.
2. Zero fields: `+4` u8 flag, `+8`/`+0xC`/`+0x10`/`+0x14` dwords.
3. `time(NULL)` → seed candidate.
4. `operator_new(0x9c8)` → optional engine block at `this+0x14`; on success call `FUN_00424890` + `FUN_004248c0` (engine init leaves, out of OWN).
5. If `*(u8*)(this+4) == 0`, call `FUN_007a4200(this, seed)` to allocate/fill the 0x200000 table.

Does **not** return a unit float. Parent name `RandomUnitScalar` is historical; this unit is the **object constructor**.

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Live decompile | Ghidra MCP `0x007a42d0` |
| Raw | `docs/reconstruction/raw/aa_007a42d0_FUN_007a42d0.md` |
| Annotated | `docs/reconstruction/raw/aa_007a42d0_FUN_007a42d0.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/FUN_007a42d0.cpp` |
| Function record | `docs/reconstruction/functions/aa_007a42d0_FUN_007a42d0.md` |
| Parent | `CVOGReaction_RandomUnitScalar` `0x007a4330` |
| Nested seed | `FUN_007a4200` `0x007a4200` (separate dual) |

---

## 3. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| Vtbl store `PTR_FUN_00a97168` | **Confirmed** | decompile |
| Zero `+4`..`+0x14` before engine alloc | **Confirmed** | |
| Heap engine size `0x9c8` at `this+0x14` | **Confirmed** | |
| `time(0)` feeds seed path | **Confirmed** | |
| Seed fill only if `+4 == 0` | **Confirmed** | |
| Returns void (in-place this) | **Confirmed** | |
| Product RNG class name | **Open** | no RTTI/string on body |
| Exact engine init in `FUN_00424890/c0` | **Out of OWN** | leave residual |

---

## 4. Layout (this object)

| Offset | Role | Conf |
|---|---|---|
| `+0` | vtbl | **High** |
| `+4` | u8 “table ready” (0 → seed in ctor) | **High** |
| `+8` | table pointer (set by `FUN_007a4200`) | **High** (sibling) |
| `+0xC` | table cursor (sibling) | **Probable** |
| `+0x10` | seed dword (sibling) | **High** |
| `+0x14` | engine / MT-like state (`0x9c8`) | **High** |

---

## 5. Gaps

1. Product name / RTTI for vtbl `PTR_FUN_00a97168`.
2. Semantics of `FUN_00424890` / `FUN_004248c0` (OWN residual).
3. Whether any caller constructs outside the singleton path.

**Verdict:** **leave-FUN** (behavior dual OK; name withheld).
