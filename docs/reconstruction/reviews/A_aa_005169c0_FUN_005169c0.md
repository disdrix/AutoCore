# Review A (reconstruction fidelity): `aa_005169c0` FUN_005169c0 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_005169c0` |
| **VA** | `0x005169c0` |
| **Canonical name** | `FUN_005169c0` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Reconstruction fidelity / evidence-only naming |
| **Counterpart** | `reviews/B_aa_005169c0_FUN_005169c0.md` |
| **System** | skills-abilities |
| **Verdict** | **leave-FUN** — behavior notes accept; name withheld |

---

## 1. Purpose (behavioral, not product name)

Lazy getter for a **0x14-byte** heap block stored at **`this+0x64`**. First call allocates and zeroes four dwords (`+4`…`+0x10`); later calls return the cached pointer. Used by local cast validation (busy via `+0x10`), active-cast counter clear/queue, and a cast-flag clear path (`FUN_0052c780`).

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Fresh decompile | Ghidra MCP `decompile_function` / `analyze_function_complete` @ `0x005169c0` |
| Xrefs | 5 UNCONDITIONAL_CALL → 3 functions |
| Globals | `audit_globals_in_function` → empty |
| Raw | `docs/reconstruction/raw/aa_005169c0_FUN_005169c0.md` |
| Function record | `docs/reconstruction/functions/aa_005169c0_FUN_005169c0.md` |
| Related | `Skill_LocalCastValidate`, `Skill_ClearActiveCastCounterAndQueueId`, `FUN_0052c780` |

---

## 3. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| Lazy alloc `operator_new(0x14)` when `*(this+0x64)==0` | **Confirmed** | decompile |
| Zero +4/+8/+0xC/+0x10 before store | **Confirmed** | |
| Return existing or new pointer | **Confirmed** | |
| No string / RTTI / vtbl on block | **Confirmed** | no globals; no vptr write |
| LocalCastValidate: `+0x10 != 0` → code **6** | **High** | busy gate when skip formal is 0 |
| ClearActiveCastCounter decrements `+0x10` | **High** | three call sites |
| FUN_0052c780 also calls | **Confirmed** | xref `0x0052c7a9` |
| Exclusive LocalCastValidate use | **Falsified** | multi-caller |
| `Skill_LazyAllocBusySlot_Inferred` as name | **Rejected** | exclusive-use rule fails |

---

## 4. Naming gate

Evidence-only rule: promote `_Inferred` busy-slot name **only if** exclusive LocalCastValidate use is proven.

| Evidence class | Present? |
|---|---|
| String | No |
| RTTI | No |
| Sole caller LocalCastValidate | **No** (3 callers) |

**Decision:** retain `FUN_005169c0`. No three-rep promote.

---

## 5. Gaps

1. Product type of object owning `+0x64` (caster vs skill subsystem vs other).
2. Product field names for block `+4`…`+0x10` (counter-like use of `+0x10` only partially sealed by callers).
3. Whether `FUN_0052c780` call result is intentionally discarded (side-effect lazy init only).

**Verdict:** **leave-FUN** (behavior dual OK; name withheld).
