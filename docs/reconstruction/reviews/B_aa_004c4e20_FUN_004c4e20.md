# Review B (skeptical / adversarial): `aa_004c4e20` FUN_004c4e20

| Field | Value |
|---|---|
| **Stable ID** | `aa_004c4e20` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` (dual A/B seal — live Ghidra) |
| **Counterpart** | `reviews/A_aa_004c4e20_FUN_004c4e20.md` |
| **System** | `input-drive-control` |
| **Dual status** | **Present (first dual)** |
| **Verdict** | **accept-with-gaps** on base `+0x1f4` + optional `driver+0xd48` AND-gate; **reject** overclaims that collapse dual d48 flags, that ECX is always VehicleEntity, or that this leaf applies AbsoluteTopSpeed / thr zeroing |

---

## 1. Claims under attack

| # | Claim | Attack result |
|---|---|---|
| 1 | Body unknown / unreadable | **Falsified** — 12-line leaf; live ≡ raw; machine bytes seal offsets |
| 2 | Base max is `vehicleData+0x634` | **Falsified** — that clamp is **outer** PushDrive; this leaf only returns `this+0x1f4` (± d48) |
| 3 | Optional path **multiplies** `d48 * 1f4` | **Falsified** — `fld d48; fadd [esi+0x1f4]` |
| 4 | Gate is only `DAT_00af1854` | **Falsified** — this unit reads **`DAT_00b037d4` only** (sole xref) |
| 5 | Outer `af1854` and inner `b037d4` are the same gate | **Falsified** — distinct VAs; image `01` vs `00` |
| 6 | ECX is always VehicleEntity `this` | **Falsified as universal** — PushDrive does `MOV ECX,EAX` after wobj **`vfunc+0x1d8`**; other callers pass their own ESI |
| 7 | Leaf zeros thr / copies HB | **Falsified** — no thr/HB stores; pure float return |
| 8 | With image defaults, d48 always affects base | **Falsified for this leaf** — `b037d4=0` → plain `+0x1f4` only (outer PushDrive may still use d48 under `af1854`) |
| 9 | Driver resolve uses vfunc `0x1d8` | **Falsified** — leaf uses **`0x210`**; `0x1d8` is **caller-side** object selection in PushDrive |
| 10 | Ready for product rename + bit-exact seal | **Fail** — type of `this` / driver and product names open |

---

## 2. Decisive dataflow (raw ≡ live ≡ asm)

```
// entry: ECX = this → ESI
host = component_at(this)           // *( *(this+4)+4 ) + this + 4; vtable from *host
driver = host->vfunc[0x210](0)
if driver == 0:
    return *(float*)(this + 0x1f4)
if DAT_00b037d4 == 0:
    return *(float*)(this + 0x1f4)
driver = host->vfunc[0x210](0)      // re-call (not cached)
return *(float*)(driver + 0xd48) + *(float*)(this + 0x1f4)
```

**Prologue bytes (authoritative):**

```
56                   push esi
8B F1                mov esi, ecx
...
FF 92 10 02 00 00    call [edx+0x210]
85 C0                test eax, eax
74 2D                jz plain
80 3D D4 37 B0 00 00 cmp byte ptr [00b037d4], 0
74 24                jz plain
... re-call 0x210 ...
D9 80 48 0D 00 00    fld [eax+0xd48]
D8 86 F4 01 00 00    fadd [esi+0x1f4]
5E C3                pop esi; ret
plain:
D9 86 F4 01 00 00    fld [esi+0x1f4]
5E C3                pop esi; ret
```

---

## 3. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| `+0x1f4` always base | **High** | Wrong base max in every consumer |
| `+0xd48` additive under dual gate | **High** | Miss / invent driver speed bonus on base |
| Flag identity `b037d4` ≠ `af1854` | **High** | Double-count or wrong enable |
| Image `b037d4=0` | **High** | Port enables dead path by default |
| PushDrive ECX = vfunc `0x1d8` return | **High** | Port passes vehicle ESI and reads wrong object’s `+0x1f4` |
| Driver type / product field names | **Open** | Wrong struct mapping |
| Runtime flag flips | **Open** | Live bonus path unseen in image |

---

## 4. Offset / formula attack checklist

| Item | Attack | Result |
|------|--------|--------|
| `param_1+500` = `+0x1f4` | Decimal confusion? | **No** — asm immediate `0x1f4` |
| `+0xd48` | Invented? | **No** — asm `0xd48` |
| Add vs multiply | Decompiler artifact? | **No** — `fadd` |
| Gate polarity | Active-low flag? | **No** — `cmp byte,0; jz plain` → flag **non-zero enables** add |
| Double vfunc call | Decompiler dupe? | **No** — two `call [edx+0x210]` on add path; probe result only used for null test |
| `DAT_00b037d4` shared with thr clamp | Same as 0.9? | **No** — sole xref; thr uses `DAT_00a0f734` in PushDrive |
| Always vehicle entity | Compatible with call sites? | **No as universal** — see ECX table in Review A |

---

## 5. Surviving contract for AutoCore

```
// aa_004c4e20 / FUN_004c4e20
// this: object with f32 at +0x1f4 and CVOG-style component chain for vfunc 0x210
float GetBaseMaxSpeed(Host* this) {
  auto* host = ComponentHost(this);          // *( *(this+4)+4 ) + this + 4
  auto* driver = host->vfunc_0x210(0);
  float base = this->f32_at_0x1f4;           // +0x1f4
  if (driver != nullptr && g_b037d4 != 0) {  // DAT_00b037d4
    driver = host->vfunc_0x210(0);           // re-resolve
    base = driver->f32_at_0xd48 + base;      // +0xd48 ADD
  }
  return base;
}

// PushDriveAxes caller setup (not this leaf):
//   obj = wobj->vfunc_0x1d8(); if !obj → baseMax=0; else baseMax = GetBaseMaxSpeed(obj);
// Outer bonus (DAT_00af1854) is SEPARATE — may also fold driver+0xd48 into multiplier.
```

**Port defaults matching image:** `g_b037d4 = false` → return pure `+0x1f4`.

---

## 6. What this dual does **not** claim

- Product names / rename seal
- That PushDrive’s vfunc-`0x1d8` return **is** the vehicle entity (layout-compatible possible; identity unproven)
- Outer `(bonus+1)*baseMax` / AbsoluteTopSpeed / thr polarity (owned by `aa_004fbc10`)
- Runtime golden or bit-exact

**Verdict:** **accept-with-gaps** — gates **`this+0x1f4`** and **`driver+0xd48` ∧ `DAT_00b037d4`** survive adversarial review.
