# Dual residual: `0x00570af0` InventoryGrid_MoveItemFootprint_Inferred

| Field | Value |
|-------|-------|
| Stable ID | `aa_00570af0` |
| Address | `0x00570af0`–`0x00570cfb` |
| Canonical name | `InventoryGrid_MoveItemFootprint_Inferred` |
| Owner | dual residual WQ8R-C (OWN-ONLY) |
| Date | 2026-08-04 |
| Prior | scaffold only (`FUN_00570af0` raw/annotated/clean 2026-07-23) |

## Residual questions (this pass)

1. Seal move CF: CanPlace(conflictOut local) → clear old → SetPos → stamp new
2. Seal stamp/clear index `height*x+y` / `height*(ox+dx)+(oy+dy)`
3. Seal ABI thiscall + 3 stack / `RET 0x0C`
4. Note Ghidra xref gap (no callers)

## Live evidence this pass

| Source | Result |
|--------|--------|
| Ghidra `batch_decompile` @ `0x00570af0` | Body matches raw scaffold; stamp `unaff_*` still wrong |
| Ghidra `analyze_function_complete` | callees `00570840`, `00512670`; callers **empty** |
| Ghidra `get_xrefs_to` | **none** |
| `read_memory` prologue | `SUB ESP,14; PUSH EBP; PUSH ESI; MOV ESI,[ESP+20]; TEST ESI; MOV EBP,ECX` |
| `read_memory` stamp core ~`0x00570c70` | `IMUL height, baseX; +dy; +oy; COID stores *8` |
| `read_memory` epilogue | `MOV [EBP+21],1; MOV AL,1; ADD ESP,14; RET 0x0C` |
| CanPlace residual `a_00570840` | lists this VA as InvSize caller |
| Place / Remove duals | same index family |

**Not performed:** `disassemble_bytes`, Launcher, CE pack, bit-exact.

---

## Seal summary

```text
// __thiscall InventoryGrid_MoveItemFootprint_Inferred(this, item, originX, originY)
// RET 0x0C

if (item == NULL) return 0
sizeX/Y = blob+0x406/407
if (!CanPlace(this, sizeX, sizeY, originX, originY, &localConflict[2])) return 0
// clear footprint at (GetX,GetY) to empty pairs
// item.vtbl+0x24c(originX, originY)
// stamp COID at height*(ox+dx)+(oy+dy)
if (*(this+0x24)) FUN_00512670(*(this+0x24))
*(this+0x21) = 1
return 1
```

### Index family — SEALED

| Phase | Formula |
|-------|---------|
| Clear | `height * curX + curY` |
| Stamp | `height * (originX+dx) + (originY+dy)` |

### Gaps (do not unseal CF/index)

1. No Ghidra callers — orphan/indirect open.
2. Runtime overlapping move observation.
3. Product/PDB English.
4. Bit-exact open.
