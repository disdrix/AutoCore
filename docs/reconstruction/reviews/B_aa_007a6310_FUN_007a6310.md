# Review B (skeptical / independent): `aa_007a6310` FUN_007a6310 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_007a6310` |
| **VA** | `0x007a6310` |
| **Canonical name** | `FUN_007a6310` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Skeptical |
| **Counterpart** | `reviews/A_aa_007a6310_FUN_007a6310.md` |
| **System** | locale / language packs |
| **Verdict** | **leave-FUN** |

---

## 1. Claims under attack

| # | Claim | Result |
|---|---|---|
| 1 | Loads English `languageEN*.pak` | **Falsified** — only FR/DE/UK patterns |
| 2 | Mode 0 uses this function | **Falsified** — mode 0 is strcpy path in parent |
| 3 | Silent success when fopen fails | **Confirmed as risk** — no fallback string; parent may leave stale slot |
| 4 | `param_2` is continent id | **Falsified** — matches locale mode 1/2/3 |
| 5 | Bit-exact PAK format fully known | **Overstated** — only seek/read path sealed |

---

## 2. Surviving contract

```
FUN_007a6310(pool, mode, packIndex):  // + entryIndex in EAX
  if mode not in {1,2,3}: return
  path = sprintf("language{FR|DE|UK}%d.pak", packIndex)
  f = fopen(path, "rb")
  if !f or entryIndex == -1: return
  seek(base + entry.offset)
  ensure_slot(entry.size)
  fread(slot, entry.size)
  fclose(f)
```

Agree with A on CF. **Verdict:** **leave-FUN**.
