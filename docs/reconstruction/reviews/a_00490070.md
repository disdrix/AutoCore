# Residual scratch: `aa_00490070` SelectBestPickTarget

| Field | Value |
|---|---|
| **Stable ID** | `aa_00490070` |
| **Date** | `2026-07-29` |
| **Role** | Residual for dual A/B |

## Bytes / memory sealed

- Entry: `sub esp,0x78; push edi; mov edi,ecx; mov ecx,[edi+0xC0]; test ecx; cmp [edi+0xBC],0`.
- `DAT_00aaa630` @ image: `FF FF 7F 7F` = **FLT_MAX**.

## Open residual

- `FUN_004bae00` this/host (flag 0 vs 1 list meaning).
- Which gfx view is ECX into `0075c340` at call site.
- Exact ECX for `00524520`/`005245d0` in this function (raw pushes object; character from world+0xE8A0 path).
- Runtime multi-object priority capture.
