# Review B (skeptical / adversarial): `aa_007203b0` FUN_007203b0

| Field | Value |
|---|---|
| **Stable ID** | `aa_007203b0` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_007203b0_FUN_007203b0.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | Immediately frees/stops hardware voice | Only sets `+0x38=1` and clears `+0x1B0` under CS | **Falsified hard-stop** — **marks** for stop |
| 2 | Case-sensitive name match | `_stricmp` | **Falsified** — **case-insensitive** |
| 3 | Always runs extra path | `param_3` gate on `FUN_0071ffc0` | **Confirmed optional** |
| 4 | Works when subsystem off | Early `this+0x10==0` | **Confirmed gated** |
| 5 | No concurrency control | CS at `+0x158` | **Falsified** — **locked** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| thiscall + name + doExtra | **High** | Wrong stop API |
| Enable `+0x10` / CS `+0x158` | **High** | Race / no-op |
| Name at entry `+0x48` | **High** | Miss stop |
| Stop flag `+0x38` | **High** | Sounds never stop |
| Slot table full geometry | **Medium** | Miss some slots |

---

## 3. Cross-check against raw + bytes

```
raw ≡ decompile.
entry: cmp [esi+0x10],0; lea CS at +0x158; import EnterCriticalSection;
optional 0071ffc0; loop stricmp on +0x48; set +0x38 / clear +0x1b0.

≡ A sealed. this from FUN_00720d40 singleton.
```

---

## 4. Surviving contract for AutoCore

```csharp
void InterfaceSound_RequestStopByName(InterfaceSoundTable t, string name, bool doExtra = true)
{
    if (!t.Enabled /*+0x10*/) return;
    lock (t.Cs /*+0x158*/)
    {
        if (doExtra) ExtraCleanup(name); // FUN_0071ffc0
        foreach (var entry in t.ActiveSlots /*@+0x80*/)
        {
            if (entry != null && string.Equals(entry.Name /*+0x48*/, name, OrdinalIgnoreCase))
            {
                entry.StopRequested /*+0x38*/ = true;
                entry.Field_1B0 = 0;
            }
        }
    }
}
```

---

## 5. Open questions

1. Who consumes `+0x38` and actually stops the voice.
2. Slot capacity / overflow policy.
3. Runtime verification.

**Verdict:** **accept**
