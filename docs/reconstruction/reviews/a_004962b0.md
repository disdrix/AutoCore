# Dual residual / scratch: `0x004962b0` EquipGfxHost_ClearListIfFlag_Inferred

| Field | Value |
|-------|-------|
| Stable ID | `aa_004962b0` |
| Address | `0x004962b0` |
| Owner | dual residual only (no WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / ledgers) |
| Date | 2026-07-29 (first dual A/B seal) |
| Tools | Ghidra `decompile_function` / `force_decompile` / `read_memory` / `get_function_xrefs` / `get_function_callers` / `get_assembly_context` / callee identity `004367f0` |
| Dual | `A_aa_004962b0_*` / `B_aa_004962b0_*` |

---

## Residual questions (entry)

1. Is scaffold name `Named_CalleeOf_CVOGCreature_PostCreateFromPacket` adequate as primary?
2. Does decompile `FUN_004367f0(0,0)` miss a this-adjust?
3. What is at `host+0x53` / `host+0xA8`?
4. Exact body span / ABI?
5. Full caller set beyond PostCreate?

---

## Seal summary

| Item | Status |
|------|--------|
| Body `0x004962b0`–`0x004962c5`, 22 B, plain `ret` | **Sealed High** |
| Gate `*(u8*)(host+0x53) != 0` | **Sealed High** |
| Armed: `push 0; push 0; add ecx,0xA8; call FUN_004367f0` | **Sealed High** |
| Decompile missing `+0xA8` this | **Confirmed bug** — bytes win |
| Callee = dword vector resize; count=0 clears | **Sealed High CF** (identity) |
| 13 xrefs; dominant `+0xE898` host | **Sealed High** |
| Parent `0092f120` is dual-null + tail jmp | **Sealed High** |
| Scaffold PostCreate-only name | **Rejected as primary** |
| Proposed `EquipGfxHost_ClearListIfFlag_Inferred` | **Probable** |
| Element type / flag English / runtime | **Open** |

---

## Residual after dual

| Item | Status |
|------|--------|
| Product / RTTI name | Open |
| Vector element English | Open |
| `+0x53` producers | Open |
| Full `FUN_004367f0` dual | Out of OWN scope |
| Runtime / bit-exact | Open |
