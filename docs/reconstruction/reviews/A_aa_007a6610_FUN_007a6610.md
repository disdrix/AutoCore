# Review A (reconstruction fidelity): `aa_007a6610` FUN_007a6610 (leave-FUN / gender tags)

| Field | Value |
|---|---|
| **Stable ID** | `aa_007a6610` |
| **VA** | `0x007a6610` |
| **Canonical name** | `FUN_007a6610` (**retained**; tag strings seal behavior) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Reconstruction fidelity |
| **Counterpart** | `reviews/B_aa_007a6610_FUN_007a6610.md` |
| **System** | locale / string-pool post-process (FormatFailure modes 1–3 after pak load) |
| **Verdict** | **leave-FUN** — tag CF sealed; product name open |

**Evidence:** Ghidra `decompile_function` @ `0x007a6610` (literals `[$gender]` / `[$/gender]`).

---

## 1. Purpose

**In-place expand gender alternate tags** inside the current ring slot string:

1. `strstr(slot, "[$gender]")` loop until none.
2. Find matching `[$/gender]` closer; if missing, **abort** (leave partial).
3. Body between open+9 and closer is `optionA|optionB` style:
   - If `*(int*)(pool+0xbc) == 1`: copy chars from after open until `|` into the open position (male/first branch).
   - Else: skip to after `|`, copy until `[` (female/second branch — decompiler walks until `[`).
4. Append remainder after closer; NUL terminate; scan again for next tag.

Operates on `pool->slot[pool->ring]` via EBX=pool register protocol.

---

## 2. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| Tags `[$gender]` / `[$/gender]` | **Confirmed** | literals |
| Gender select field `pool+0xbc == 1` vs other | **Confirmed** | |
| `|` separates variants | **Confirmed** | |
| Missing closer aborts | **Confirmed** | |
| Multi-tag loop | **Confirmed** | re-strstr from slot base |
| Product enum for `+0xbc` | **Open** | 1 vs ≠1 only |
| Exact second-branch terminator | **Tentative** | decompiler uses `[` stop |

---

## 3. Artifacts

| Artifact | Path |
|---|---|
| Three-rep | `aa_007a6610_*` |
| Parent | `FUN_007a6de0` after `FUN_007a6310` when mode ∈ {1,2,3} and mode arg ≠ −1 |

---

## 4. Gaps

1. Who sets `pool+0xbc` (character gender).
2. Nested tags / malformed `|` residual.
3. Whether FormatFailure English path (mode 0 / −1 default 0) ever hits this (usually no).

**Verdict:** **leave-FUN**.
