# Dual residual: `0x00550300` Skill_ResolveTargetList

| Field | Value |
|-------|-------|
| Stable ID | `aa_00550300` |
| Address | `0x00550300` |
| Owner | dual residual only (no WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 (strengthen pass — bits 1/2 + Filter/RangeCheck) |
| Prior dual | A/B accept-with-gaps — open: **FUN_0054ff00 / FUN_0058c0a0**, **outFlags bit4/8 writers**, **+0x614 dict**, Gather ABI, runtime |
| Link | `Skill_FilterTargetForResolveList_Inferred` `0x0054ff00` + `Skill_RangeCheckTargetForResolveList_Inferred` `0x0058c0a0` |

## Residual questions (from dual A/B + function record)

1. Identity of `FUN_0054ff00` (filter) and who ORs outFlags bits **4 / 8**
2. Identity of `FUN_0058c0a0` (range helper after filter) for bit **2** contract
3. Full outFlags vocabulary vs `Client_Skill_ResolveCastTarget` / CastSkillOnTarget maps
4. `skill+0x614` bit English (partial seal via FormatTargetFilterTooltip)
5. Invalid-TFID global identity (`DAT_009d1ca8` family)
6. Interaction with `Skill_ValidateTargetForSkill` (does this body call it?)
7. Gather ABI / live multi-target — still open (not residual-sealable static-only)
8. **Strengthen:** dual residual outFlags bits **1/2** exclusive sites + dual-bit fallthrough + consumer priority

## Seal: outFlags ownership map (writer chain)

| Bit | Writer | Condition | Consumer map |
|----:|--------|-----------|--------------|
| **1** | **This body** `Skill_ResolveTargetList` @ `LAB_005504d7` | Null primary after filter **or** post-bit2 `goto`; **and** `skill+0x614` bit0 clear **and** `bHasPrimary` **and** `+0x614` bit5 (`0x20` Ground) clear | ResolveCastTarget: `"Nothing is targeted."` path; Self bit may related-redirect |
| **2** | **This body** after range helper | `Skill_RangeCheckTargetForResolveList_Inferred` returns `g_flZero` | ResolveCastTarget → `FormatFailureMessage(0xd)` → **`"Target out of range."`** |
| **4** | **`Skill_FilterTargetForResolveList_Inferred`** `0x0054ff00` | Fallthrough reject `LAB_005502bd` | ResolveCastTarget → code **0xe** / wrong-target; CastSkillOnTarget → **0xe** |
| **8** | **Filter** `0x0054ff00` | Hostile/alive gate (`vtbl+0x198`) while `skill+0x615` bit2 clear | ResolveCastTarget → `FormatFailureMessage(3, …)` |
| **0x10** | **Filter** `0x0054ff00` | Object type not in `{0xe, 0x12, 0x14}` | Present on filter reject; UX optional |

**Confirmed (re-decompile 2026-07-29):** this body stores only `*outFlags |= 1` and `|= 2`. Bits **4 / 8 / 0x10** are **not** written here — exclusive filter writer for 4/8/0x10 in this chain (Filter dual A/B accept).

### Dual-bit strengthen (bits 1 + 2 co-set)

| Claim | Evidence | Conf |
|-------|----------|------|
| Range-fail ORs bit2 then `goto LAB_005504d7` | decompile lines: `|= 2` then goto no-primary | **Confirmed** |
| No-primary may also OR bit1 after bit2 | same gate tree; `bHasPrimary` often still true when TFID was present but OOR | **Confirmed CF** |
| Consumer prioritizes bit2 over bit1 | ResolveCastTarget: `if ((flags & 2) == 0) { … bit4/8/1 … } else { FormatFailure(0xd) }` | **Confirmed** |
| English bit2 | FormatFailure case 0xd string | **High** |
| English bit1 | literal `"Nothing is targeted."` | **High** string; product enum name **Probable** |

### Link: FilterTarget (`0x0054ff00`)

| Evidence | Detail | Conf |
|----------|--------|------|
| Sole caller | `get_function_callers(0x0054ff00)` → only `Skill_ResolveTargetList` | **Confirmed** |
| Call site | `thiscall` skill in ECX; args `(resolvedObj, pSource, outFlags)` | **High** (decomp) |
| Role | First-stage legality; returns `object*` or null | **High** |
| Dual | `reviews/A\|B_aa_0054ff00_Skill_FilterTargetForResolveList_Inferred.md` | accept |
| Closes prior open Q | “Who ORs outFlags bits 4 and 8?” | **Sealed** |

Filter does **not** set bits 1 or 2. Null filter result may still continue (self-pos / ground) when `+0x614` bit0 set or Ground `0x20` set (suppresses bit1).

### Link: Range helper (`0x0058c0a0`)

| Evidence | Detail | Conf |
|----------|--------|------|
| Ghidra name | `Skill_RangeCheckTargetForResolveList_Inferred` (was `FUN_0058c0a0`) | **High role** (`_Inferred`) |
| Sole caller | only `Skill_ResolveTargetList` | **Confirmed** |
| Contract | returns `g_flZero` → caller `*outFlags \|= 2`; non-zero distance or `g_flOne` auto-pass (self / same vehicle) | **High CF** |
| Args from skill | `+0x2c` min, `+0x28` max, `+0x34`, `+0x5e7` flag | **High** |
| Does not write outFlags | no formal; dual B falsified “writes bit2 itself” | **Confirmed** |
| Full dual of RangeCheck | accept on role (separate unit) | product name open |

## Seal: `skill+0x614` / `+0x615` bits used here (tooltip strings)

Source: `Skill_FormatTargetFilterTooltip` `0x005506a0` product strings (localize `" Self,"` / `" Ally,"` / …).

| Offset | Mask | Tooltip English | Use in ResolveTargetList / Filter |
|--------|-----:|-----------------|-----------------------------------|
| `+0x614` | `0x01` | *(no tooltip line)* | Self-position / ground-style: allows null primary continue; Filter null-candidate redirect |
| `+0x614` | `0x02` | **Self** | Filter: allow self / same vehicle / same hardpoint occupant; ResolveCastTarget related redirect on bit1/4/8 |
| `+0x614` | `0x04` | **Ally** | Filter ally gates |
| `+0x614` | `0x08` | **Enemy** | Filter enemy gate (`vtbl+0x298`); hardpoint path on bit4 consumer |
| `+0x614` | `0x10` | **Minion** | Filter minion TFID-equal path |
| `+0x614` | `0x20` | **Ground** | Null primary: skip `outFlags\|=1` when ground allowed |
| `+0x614` | `0x80` | *(no tooltip in Format path)* | Filter type-id / remains-adjacent accept |
| `+0x615` | `0x04` | **Remains** | Filter hostile-remains path (bit8 suppress) |
| `+0x615` | `0x20` | **Convoy Member** | Filter convoy accept |

**Confidence:** High on string↔bit pairing (FormatTargetFilterTooltip); Probable that these are the product targeting-filter flags (original enum names unknown).

## Seal: invalid TFID wipe sentinel

| Symbol | VA | Image bytes (16) | Conf |
|--------|-----|------------------|------|
| `DAT_009d1ca8..b4` (used to wipe 100 slots) | `0x009d1ca8` | `ff…ff 00…00` | **Confirmed** `read_memory` |
| `g_abTfidInvalid_A15870` | `0x00a15870` | same | **Confirmed** |
| `DAT_009cbf68` (CastSkillOnTarget null-target TFID src) | `0x009cbf68` | same | **Confirmed** (prior residual) |

All three are the same 16-byte invalid TFID pattern: lo/hi = `0xffffffff`, global/pad = 0. Matches primary-absent test `((tfidLo & tfidHi) == 0xffffffff) && ((char)tfidGlobal == 0)`.

## Seal: does **not** call ValidateTargetForSkill

| Check | Result |
|-------|--------|
| Callees (Ghidra) | `CVOGReaction_ResolveObjectTarget`, `Skill_FilterTargetForResolveList_Inferred`, `Skill_RangeCheckTargetForResolveList_Inferred`, `Skill_GatherTargetsInArea` only |
| `Skill_ValidateTargetForSkill` / LocalCastValidate | **Not** in this body |

Target legality for list build = Filter + Range (+ optional Gather). Handler `+0x5F0` ValidateTarget is a **separate** cast gate (see `aa_00553650`).

## Seal: callers + CastSkillOnTarget packing (static)

| Caller | VA | Role |
|--------|-----|------|
| `Client_Skill_ResolveCastTarget` | `0x0093b3a0` | Player TFID resolve; outFlags → toasts / invalid TFID |
| `CVOGReaction_CastSkillOnTarget` | `0x004d09a0` | Reaction path; if `pTargetList==null` alloc `0x640` (100×TFID16), call this, map flags→0xd/0xe |
| `NPC_TryCastSkillFromSet` | `0x005d1280` | AI cast set |

Callers of this unit reconfirmed: `get_function_callers(0x00550300)` → those three only.

CastSkillOnTarget call (clean / prior dual): `thiscall` skill; args  
`(outList, worldSaved, pSource, tfid×4, pAimPos, &resolveFlags)` — matches formals; unused middle formal still present in prototype plate.

## Re-decompile check (Ghidra 2026-07-29 residual strengthen)

Body matches frozen raw CF:

1. Wipe 100 TFID slots from `DAT_009d1ca8`
2. ResolveObjectTarget → **Filter** → null? no-primary / bit1
3. Types `0x12`/`0x14` vehicle remap → **RangeCheck** → zero? bit2 → no-primary
4. Write primary TFID slot0; center pos; `skill+0x624` → optional `Skill_GatherTargetsInArea`
5. Return count

No CF change. Ghidra plates already use Filter + RangeCheck `_Inferred` names. Live sole-caller recheck on Filter + RangeCheck: **only this**.

## `skill+0x624` targeting mode (behavior only — not full enum English)

| Test | Behavior |
|------|----------|
| `(mode & 0x20)==0 && (mode & 0x4000)==0` | If `(mode & 1)==0` → no gather; else top-up gather if `maxTargets (+0xde) > count` |
| else (bit5 or bit14 set) | Full area gather: radius arg `skill+0x38`, float sentinel `0xbf800000` (−1.f), maxAdd = full `+0xde` |
| `modeFlag` | `1` if `mode==0 \|\| mode==0x20`, else `0` |
| bit20 / bit4000 args | `(mode>>5)&1`, `(mode>>14)&1` passed into Gather |

Full product enum names for `+0x624` remain **open** (no string table in this unit).

## Residual after this strengthen pass

| Item | Status |
|------|--------|
| FilterTarget link + outFlags 4/8/0x10 writers | **Sealed High** |
| outFlags bit1/bit2 writers in this body | **Sealed Confirmed** |
| Dual-bit (1\|2 co-set) + consumer bit2 priority | **Sealed Confirmed** |
| Bit2 UX English via 0xd | **Sealed High** |
| RangeCheck sole-caller + zero signal (no outFlags write) | **Sealed Confirmed** |
| `+0x614` Self/Ally/Enemy/Minion/Ground (+ Remains/Convoy on `+0x615`) | **Sealed High** via tooltip strings |
| Invalid TFID wipe == client invalid sentinel | **Sealed Confirmed** (image bytes) |
| Not ValidateTargetForSkill | **Sealed Confirmed** |
| Full `+0x624` English enum | Open |
| Gather 15-arg ABI product names | Open (behavior preserved) |
| Live multi-target cast capture | Open |
| Bit-exact / runtime / binary diff | Open (matrix policy) |

## Artifacts touched by residual owner

- This scratch: `docs/reconstruction/reviews/a_00550300.md`
- Function record `functions/aa_00550300_Skill_ResolveTargetList.md`
- Dual residual strengthen `A_|B_aa_00550300_*`
- Clean already had bit1/bit2 writer comments (no CF change needed)
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
- **No Launcher**
