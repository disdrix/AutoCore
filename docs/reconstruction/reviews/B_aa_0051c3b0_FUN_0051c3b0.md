# Review B (skeptical / adversarial): `aa_0051c3b0` FUN_0051c3b0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051c3b0` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_0051c3b0_FUN_0051c3b0.md` |
| **Verdict** | **accept-with-gaps** |

---

## 1. Claims under attack

| # | Claim | Attack | Outcome |
|---|---|---|---|
| 1 | Grows element buffer (data blocks) | Grows **map of block pointers** only | **Falsified** as element buffer |
| 2 | Always grows by exactly `param_2` | May raise to `max(mapsize/2, 8)` | **Falsified fixed grow** |
| 3 | Silent OOM | Overflow path throws **deque too long** | **Falsified silent** |
| 4 | Multiple skill callers | Sole QueueDeferred | **Confirmed sole** |
| 5 | `this` is character owner | Same **slot** as Queue (`FUN_005169c0` result) | **Falsified owner** |
| 6 | Always returns to caller after delete | Decomp marks `operator_delete` noreturn | **Survives as decomp hazard** — verify before trusting epilogue |
| 7 | Invented beyond MSVC deque | String + layout match dual Queue | **No invention** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Map/mapsize/off layout | **Confirmed** | Corrupt deferred IDs |
| Prefer-grow policy | **Confirmed** | Capacity thrash / OOM |
| Sole caller + PUSH 1 | **Confirmed** | Low |
| Free-path mapsize update | **Medium** | Leak / stale size |
| Exact STL typename | **Open** | Naming |

---

## 3. Surviving contract for AutoCore

```
// Only from Skill_QueueDeferredCastId when need more map slots:
FUN_0051c3b0(pSlot, 1);
// Implements MSVC-like deque map grow:
//   map[+4], mapsize[+8], rotate around (off[+0xC]>>2)
// Does not push skill ids (Queue does). Does not cast.
// On mapsize overflow: throw "deque<T> too long".
```

---

## 4. Open questions

1. Disasm epilogue after `operator_delete` for mapsize write.
2. Whether grow is ever called with `param_2 != 1` (today only 1).
3. Promote `_Growmap` name only with stronger RTTI.

**Verdict:** **accept-with-gaps** — adversarial pass keeps deque-map grow; free-path bookkeeping residual only.
