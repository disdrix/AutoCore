# Review B (skeptical / adversarial): `aa_00423370` FUN_00423370

| Field | Value |
|---|---|
| **Stable ID** | `aa_00423370` |
| **VA** | `0x00423370` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_00423370_FUN_00423370.md` |
| **Tools** | Ghidra decompile + caller cross-check only (no Launcher) |
| **Verdict** | **accept-with-gaps** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | This function alone "connects the DB" | Enter does more after return | **Partial** — this is **one step**; enter still QI/opens RM and version-checks |
| 2 | Always runs VariantInit property setup | Ensure passes `param_2=0` | **Falsified for ensure path** — property branch dead |
| 3 | Decompiler signature is complete | `in_EAX` / hidden outs | **Sustained doubt** — ABI incomplete |
| 4 | Fail returns without cleanup | LAB path | **Falsified** — `FUN_0041d7b0` on fail and success |
| 5 | CLSCTX value 1 is INPROC | Constant `1` in CoCreate | **Probable** (CLSCTX_INPROC_SERVER=1) not re-proven vs header |
| 6 | Needed for mission XP math | XP formulas use tables | **Falsified for server** — only client table load cold path |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Role in enter cold path | **High** | Wrong open failure diagnosis |
| Ensure uses flag 0 | **High** | Porting unused prop branch |
| Exact COM types | **Low** | Wrong IID → open always fails in reimpl |
| Clean/raw `in_EAX` fidelity | **Low** | May mis-model success object |

---

## 3. Surviving contract

```
// Client-only cold open helper (not AutoCore server):
hr = CoCreate + OpenFromInitializationString(wideCnxn @ DBReader+0x2c)
// caller: if hr < 0 → DBReader log "Unable to connect to RM."
// param_2 extras unused on ensure path
```

---

## 4. Open questions

1. GUID identification for CoCreate CLSIDs.
2. Whether XML mode (`DAT_00d1793c`) ever reaches this function (enter skips cold path when set — **likely never** under XML mode).

**Verdict:** **accept-with-gaps**
