# Dual residual: `0x00809460` Client_RecvObjectiveState

| Field | Value |
|-------|-------|
| Stable ID | `aa_00809460` |
| Address | `0x00809460` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Pass | residual strengthen — **slot floats** + **Action vtbl+0x20** |
| Prior dual | A/B accept-with-gaps — open: slot float vs int, `FUN_0092ce90`, EvaluatePending scheduling, UI `+0x107c`, full Action map |

## Residual questions (from dual A/B)

1. Slot values: IEEE float bits vs integer counters
2. Identity / role of `FUN_0092ce90`
3. Whether dirty `*(client+0x4d0)+4` or `FUN_0092ce90` schedules `CVOGCharacter_EvaluatePendingObjectives`
4. Mission UI object at `client+0x107c`
5. **What requirement `vtable+0x20` actually is** (type-specific Action vs shared toast)

## Seal: slots are **IEEE float bit patterns** (absolute or ratio by type)

| Evidence | Location | Confidence |
|----------|----------|------------|
| Handler stores raw dwords; no int/float convert | RecvObjectiveState body `piVar4[1..4] = *(int*)(pkt+0x18..)` | **High** |
| UseItem_Eval reads `*(float*)(state+4+slot*4)` vs `(float)RepeatCount` int@req+0x50 | `0x0060cfe0` | **High** |
| Kill_Eval same float slot read vs `(float)NumToKill` | `0x00613910` | **High** |
| Patrol_Eval float slot ≥ `(float)(requiredVisits*weight)` | `0x0060e0f0` | **High** |
| UseItem_SlotAction display: `(int)*(float*)slot` in `"%s: %i / %i"` | `0x0060d060` | **High** |
| Patrol progress fraction (vtbl **+0x10**, not Recv target): `slotFloat / denom`, clamp ≤ 1 | `0x0060e120` | **High** |
| AutoCore wire already `float[] SlotProgress`; builder absolute for UseItem/Kill/Collect/Patrol | `ObjectiveStatePacket` / `ObjectiveStateBuilder` | **High** (product) |

**Conclusion:** Wire + runtime node store **float bits**. Semantics by requirement family:

| Family | Slot meaning | Eval compare |
|--------|--------------|--------------|
| UseItem / Kill / KillAggregate / Patrol pads | **Absolute** count as float (`1.0f`, `2.0f`, …) | `threshold_int_as_float <= slotFloat` |
| Collect | Live cargo path (Eval does **not** need 0x2071 float) | inventory count |
| Other / default builder | **0..1 ratio** | type-specific |

Handler itself is type-agnostic: always dword-copy four slots.

## Seal: `vtable+0x20` = **shared** progress-toast Action `FUN_00637de0`

### What Recv calls

```text
req = *(*(objDef + 0x158) + i * 4)
req->vtbl[+0x20](character, stateNode)   // thiscall: ECX=req
```

### Cross-type vtable sampling (Ghidra `read_memory` + `get_xrefs_to` 2026-07-29)

| Class / base (sample) | `+0x04` | Eval | SlotAction `+0x18` | **`+0x20`** |
|-----------------------|---------|------|--------------------|-------------|
| Collect `PTR_FUN_009e12c4` | `Collect_Action` `0x00611f20` (event) | `+0x10` `0x00611940` | `0x006124b0` | **`FUN_00637de0`** |
| UseItem `0x009dfc1c` | type-local | `+0x08` `0x0060cfe0` | `0x0060d060` | **`FUN_00637de0`** |
| Patrol `0x009e0404` | type-local | `+0x08` `0x0060e0f0` | local | **`FUN_00637de0`** |
| Kill `0x009e19e4` | Precheck `0x00613b50` | `+0x08` `0x00613910` | local | **`FUN_00637de0`** |
| 10+ further DATA xrefs @ `…+0x20` | — | — | — | all → **`0x00637de0`** |

**DATA xrefs to `0x00637de0` (vtable slots only, sample complete list):**  
`009df724`, `009dfab4`, `009dfc3c`, `009e0424`, `009e0694`, `009e08ec`, `009e0b8c`, `009e0cac`, `009e0e24`, `009e0f44`, `009e12e4`, `009e1874`, `009e1a04`, `009e3bcc` — each is a class vtable **+0x20**.

### Body of `FUN_00637de0` (fresh decompile)

```text
// this = requirement*; arg0 = character; arg1 = stateNode*
if (req.type@+0x0c == 7 || req.type@+0x0c == 8) return;   // skip toast
buf[0] = 0;
req->vtbl[+0x1c](character, stateNode, buf, 0x7f);        // fill progress string
// setup toast payload (color 0xffeee3d8, scale, objective id from *stateNode+0x10)
FUN_0040c5c0(&toastPayload);                               // enqueue UI toast element
```

| Evidence | Confidence |
|----------|------------|
| Shared across all sampled requirement vtables at byte offset **+0x20** | **High** |
| **Not** Collect event Action (`+0x04`), **not** Eval (`+0x08`/`+0x10`), **not** SlotAction (`+0x18`) | **High** |
| `+0x1c` often trampoline `0x00637dd0` = `jmp [vtbl+0x18]` → SlotAction text | **High** (bytes `8B 01 FF 60 18`) |
| Some classes install type-local `+0x1c` instead of trampoline; toast still shared | **High** |
| Types **7 / 8** (req `+0x0c`) skip toast — matches `RequirementType` Km / TimePlayed if client codes align | **Probable** (enum map product-side) |
| Does **not** CompleteObjective / EvaluatePending / mutate slots | **High** |
| `FUN_0040c5c0` = growable toast/floater queue push (elem size `0x98` in decompile) | **High** (role); product name open |

**Prior residual language correction:** Patrol_Action `0x0060e120` is **not** Recv's `+0x20` target (it sits at Patrol vtbl **+0x10** and returns a progress ratio). Recv always dispatches the **shared toast** at `+0x20`.

### Implication for server mask bits

Bit *i* in packet `+0x10` means “show progress toast for requirement *i* via SlotAction text,” **not** “run event Action / Eval / complete.” Mask can safely track which slots changed; it never completes.

## Seal: `FUN_0092ce90` = mission UI panel refresh (not eval scheduler)

Fresh Ghidra decompile `0x0092ce90` (2026-07-29 residual):

```text
// client context in EDI (packet-handler sibling convention)
ui = *(client + 0x107c)
if ui && ui.vtbl[+0xd0]() && ui.vtbl[+0x3d8]():
    ui.vtbl[+0x448]()          // same slot family as HideMissionDialogIfOpen

ui2 = *(client + 0x10b8)
if ui2 && ui2.vtbl[+0xd0]() && ui2.vtbl[+0x3d8]():
    *(byte*)(ui2 + 0x520) = 1  // dirty flag on secondary panel
```

| Evidence | Confidence |
|----------|------------|
| Body only touches `client+0x107c` / `+0x10b8` vtable gates — no hash, no CompleteObjective, no EvaluatePending | **High** |
| Sibling `Client_HideMissionDialogIfOpen` @ `0x0092ce50` uses same `+0x107c` / `+0x3d8` / `+0x448` / `+0x34c` | **High** |
| Callers: RecvObjectiveState, RecvInventoryAddItem, RecvInventoryDropResponse, FUN_00810170, FUN_008136b0, FUN_0093c3b0, FUN_00946c00, FUN_00952bc0, FUN_00955010 | **High** (role = post-mutation UI sync) |

**Proposed role name:** `Client_RefreshMissionRelatedUi` (original PDB unknown). Keep `FUN_0092ce90` in clean until registry promote.

## Seal: **no** EvaluatePending from this handler

| Claim | Result | Evidence |
|-------|--------|----------|
| Body calls EvaluatePending | **Falsified** | RecvObjectiveState CF — only Lookup, slot stores, optional `+0x107c` vcalls, bitmask `vtable+0x20`, FUN_0092ce90, dirty byte |
| FUN_0092ce90 calls EvaluatePending | **Falsified** | decompile above |
| Dirty `*(client+0x4d0)+4=1` alone completes objectives | **Falsified as complete path** | no call chain; flag is UI dirty on mission-tracker object |
| Bitmask `vtable+0x20` completes | **Falsified** | shared toast `FUN_00637de0` only; Complete is **0x2070** / `EvaluatePendingObjectives` / local event paths |

**EvaluatePending callers (xrefs):** AddCredits, Patrol helpers, CompleteObjective, combat-refresh helpers, `FUN_00535c60`/`FUN_00535ed0` — **not** RecvObjectiveState / FUN_0092ce90 / FUN_00637de0.

Surviving contract (strengthened):

```
On S2C 0x2071:
  write 4 slot floats (absolute or ratio bits by type);
  optional mission UI root refresh (+0x107c);
  for each mask bit i: req[i].vtbl[+0x20] == FUN_00637de0  // shared progress toast
  Client_RefreshMissionRelatedUi (FUN_0092ce90);
  dirty *(client+0x4d0)+4 = 1;
  // never CompleteObjective; never EvaluatePendingObjectives here
```

## Seal: `client+0x107c` = mission dialog / mission UI root

| Evidence | Confidence |
|----------|------------|
| HideMissionDialogIfOpen plate: mission dialog root | **High** |
| RecvObjectiveState optional block: if present, `+0x3d8` then `+0x448`/`+0x34c` | **High** |
| FUN_0092ce90 same pointer + secondary `+0x10b8` | **High** |
| Exact class name / full vtable map | Open (not required for RecvObjectiveState CF) |

## Re-decompile check (Ghidra 2026-07-29 residual)

Body matches frozen raw `aa_00809460_Client_RecvObjectiveState.md`: character `+0xe98`, pending hash `char+0x55c`, id@`+0x14`, slots@`+0x18..+0x24`, mask@`+0x10` → **shared** Action `vtable+0x20` (`FUN_00637de0`), `FUN_0092ce90`, dirty `*(client+0x4d0)+4`. No CF change.

## Residual after this pass

| Item | Status |
|------|--------|
| Slot float vs int | **Sealed High** = IEEE float bits; absolute vs ratio by req type |
| `vtable+0x20` identity | **Sealed High** = shared `FUN_00637de0` progress toast (not event Action / Eval / SlotAction) |
| `+0x1c` → SlotAction text path | **Sealed High** (trampoline `0x00637dd0` or type-local) |
| Types 7/8 toast skip | **Probable** as Km/TimePlayed if client `+0x0c` ≡ `RequirementType` |
| `FUN_0092ce90` role | **Sealed High** = mission-related UI refresh; proposed `Client_RefreshMissionRelatedUi` |
| EvaluatePending from dirty / FUN_0092ce90 / toast | **Sealed High** = does **not** schedule |
| `client+0x107c` role | **Sealed High** as mission UI root; original class name open |
| `*(client+0x4d0)+4` product name | Probable UI dirty on active-mission tracker |
| `FUN_0040c5c0` / `FUN_00637de0` product names | Open (roles sealed) |
| Runtime / bit-exact / image diff | Still open (matrix policy) |
| Original PDB symbols | Open |

## Artifacts touched by residual owner

- This scratch: `reviews/a_00809460.md`
- Duals: `A_aa_00809460_*`, `B_aa_00809460_*`
- Function record, clean `Client_RecvObjectiveState.cpp`, annotated open-Qs
- `FUN_00637de0` function record (role note)
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
