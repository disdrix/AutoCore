# Scratch — `aa_005380d0` staging map node construct

Date: 2026-07-29  
Dual: `A_aa_005380d0_StagingMapNode_Construct.md` / `B_aa_005380d0_StagingMapNode_Construct.md`

## Bytes (full body)

```
push 0x50
call operator_new
add  esp, 4
test eax, eax
jz   epilog                 ; return null
mov  ecx, [esp+4]           ; left
mov  edx, [esp+8]           ; parent
push esi
mov  esi, [esp+14]          ; valuePair* (stack adjusted)
mov  [eax], ecx             ; +0 left
mov  ecx, [esp+10]          ; right
push edi
mov  [eax+8], ecx           ; +8 right
mov  [eax+4], edx           ; +4 parent
mov  dl, [esp+1c]           ; color
lea  edi, [eax+0x10]
mov  ecx, 0xE
rep  movsd                  ; 14 dwords → +0x10
pop  edi
mov  [eax+0x48], dl         ; color
mov  byte [eax+0x49], 0     ; isnil
pop  esi
ret  0x14                   ; C2 14 00
```

## Sole caller `0x00538ea0` @ `0x00538f32`

```
push 0                      ; color = red
push valuePair*
push head                   ; right = *(map+4)
push where                  ; parent
push head                   ; left  = *(map+4)
call FUN_005380d0
mov  ecx, eax               ; consumes return
```

## Contract one-liner

`StagingMapNode* Construct(left, parent, right, valuePair14*, color)` — `new(0x50)`, copy key+pad+payload[12] @ `+0x10`, color `+0x48`, isnil `+0x49=0`; **not** insert/rebalance.
