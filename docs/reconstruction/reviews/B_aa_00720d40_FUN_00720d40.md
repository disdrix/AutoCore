# Review B (skeptical / adversarial): `aa_00720d40` FUN_00720d40

| Field | Value |
|---|---|
| **Stable ID** | `aa_00720d40` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_00720d40_FUN_00720d40.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | Takes `(p, 1)` like caller decompiles suggest | Body: no stack arg reads; returns fixed global | **Falsified** — **zero-arg getter** |
| 2 | Re-inits every call | bit0 of `DAT_00d20200` | **Falsified** — **once** |
| 3 | Same object as mission-complete table `00723820` | Different storage `DAT_00d20070` vs `DAT_00d20210` | **Confirmed distinct** |
| 4 | Thread-safe without external care | Flag or without interlocked; classic static-init race | **Accept race risk** (client single-thread UI likely) |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Once-init + return pointer | **High** | Double-init / null |
| `DAT_00d20070` / flag addr | **High** | Wrong singleton |
| No user args | **High** | Mis-port wrappers |
| Object layout | **Medium** (via sibling methods) | Incomplete API |

---

## 3. Cross-check against raw + bytes

```
raw ≡ live decompile.
entry SEH + test DAT_00d20200 bit0 + or + push DAT_00d20070 + init + atexit.
Caller decompiles that pass args into 00720d40 are decompiler artifacts —
seal the callee body, not the mis-merged call sites.
```

---

## 4. Surviving contract for AutoCore

```csharp
static InterfaceSoundTable? _iface;
static int _ifaceInitFlags;

InterfaceSoundTable GetInterfaceSoundTable()
{
    if ((_ifaceInitFlags & 1) == 0)
    {
        _ifaceInitFlags |= 1;
        _iface = new InterfaceSoundTable(); // FUN_00720a40
        // atexit destroy
    }
    return _iface!;
}
```

---

## 5. Open questions

1. Full product type name vs mission-complete table sibling.
2. Whether multi-thread init needs locking (probably not in original client).

**Verdict:** **accept**
