# Dual residual: `0x0051a790` Skill_LocalCastValidate

| Field | Value |
|-------|-------|
| Stable ID | `aa_0051a790` |
| Address | `0x0051a790` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Pass | **gate-ladder strengthen** (re-decompile + dual A/B + function record) |
| Prior dual | A/B accept-with-gaps; UX codes sealed earlier same day |
| Re-decompile | Ghidra `decompile_function` `0x0051a790` — body ≡ frozen raw |
| Callers | 9 via `get_function_callers` (see Seal 7) |

## Residual questions (entry set)

1. Ordered gate ladder priorities (early-exit order) as port contract
2. Full `eSkillResponses` UX for codes this unit emits
3. Working ABI vs plate names
4. `FUN_005169c0` / `FUN_00518c20` product identity → **leave FUN_***
5. TFID active-binding polarity + compare width
6. Power-cost predicate (`plant+0x6b4` / `+0x12c` / `skill+8`) dual use
7. Codes 13/14/15 as this-body literals?
8. Hide ladder (11/12) complete CF

---

## Seal 0 — Ordered gate ladder (port contract)

**Early-exit order is the product.** First matching reject wins; later gates never run.

```text
G0  this+0xb4 & 0x02 != 0
      → return 5                                          // status suppress

G1  if vtbl+0x198() != 0:                                 // vehicle-like / body?
      plant = vtbl+0x210(0)
      if plant==0 OR plant+0x6b4 < 1 → return 3           // wrecked toast

G2  if bSkipBusyCheck low-byte == 0:                      // all known callers: 0
      if (this+0xb4 & 0x08) AND *(this+0xa8)+0x38 == 0x14
        → return 5                                        // status (alt)
      if *(FUN_005169c0() + 0x10) != 0 → return 6         // busy / active use

G3  if pSkill == null
      → return 0                                          // lightweight allow

G4  LookupActiveCastBinding(out, skill+0x5fc)
    if TFID_NotEquals(out, g_abTfidInvalid) != 0
      → return 0                                          // occupied bind → allow
    // else binding == invalid → deep skill gates:

G5  plant = vtbl+0x210()                                  // note: no (0) arg in decomp
    if plant!=0 AND plant+0x6b4 < 1 AND (i16)plant+0x12c < (i16)skill+8
      → return 4                                          // power

G6  if skill+0x628 != 0 OR categoryCD(skill+0x5e8) != 0
      → return 7                                          // casting / recharge

G7  if plant==0 AND *(FUN_00518c20()+8)!=0 AND skill+0x615&0x02
      → return 10                                         // prevent (active map)

G8  related = vtbl+0x19c()
    // hide / form ladder → 12 | 11 | LocalRangeTargetCheck (see Seal 6)

G9  (no deep reject) → return 0
```

| Property | Status |
|----------|--------|
| Order G0→G9 as early-exit | **High** (raw + re-decompile) |
| No stores in body | **High** (read/call only) |
| Not server authority | **High** |
| LocalRangeTargetCheck may return 0 / 0xf / handler codes | **High** (downstream body) |

---

## Seal 1: eSkillResponses strings via `Skill_FormatFailureMessage` (`0x0054fa20`)

Caller bridge **Confirmed**: `Client_RequestCastSkill` on nonzero → `Skill_FormatFailureMessage(code, buf, casterCtx)` → toast.

| Code | FormatFailureMessage string | LocalCastValidate trigger | Confidence |
|-----:|-----------------------------|---------------------------|------------|
| **0** | (no message; cast continues) | G3/G4/G9 fallthrough | **High** |
| **3** | `"Cannot do that while wrecked!"` | G1 plant null / `+0x6b4 < 1` | **High** string; gate ≠ wrecked gloss **Probable** |
| **4** | `"You don't have enough power!"` | G5 triple conjunct | **High** |
| **5** | `"You can't do that with your current status."` | G0 or G2 alt | **High** |
| **6** | `"You're already actively using a skill!"` | G2 busy `+0x10` | **High** |
| **7** | `"This skill is not ready to use again yet!"` | G6 casting/CD | **High** |
| **10** | `"You have an active skill that prevents you from doing that."` | G7 | **High** UX; CF **High** |
| **11** | `"You must be hidden to use this."` | G8 fallthrough `return 0xb` | **High** UX; CF **High** |
| **12** | `"You can't do this while hidden."` | G8 form + no `0x4000` | **High** UX; CF **High** |

### Never returned as **literals** by this body

| Code | String (formatter) | Why absent here |
|-----:|--------------------|-----------------|
| 1 | Server checks failed! | server-only |
| 2 | Skill failed. | other |
| 8 | too many summon | other |
| 9 | while in the air | other |
| **13** | Target out of range. | may bubble via LocalRangeTargetCheck handler |
| **14** | Wrong target type. | same |
| **15** | (default / accuracy path) | LocalRangeTargetCheck returns `0xf` on accuracy miss |
| 16 / 18 | summon / stunted | other |

**Falsified:** 10–12 as unnamed “form flags” without UX. **Falsified:** 13/14 as *this* unit’s literal returns.

---

## Seal 2: Working ABI (callers + body; plate names still false)

```c
// MSVC __thiscall — ECX = caster entity
int Skill_LocalCastValidate(
    void* thisCaster,     // ECX
    int   bSkipBusyCheck, // stack0 — low byte; 0 = run G2 (all known callers pass 0)
    void* pSkill,         // stack1 — skill runtime; null = G3 lightweight
    void* pRangeExtra);   // stack2 — toward LocalRangeTargetCheck (callers pass 0)
```

| Evidence | Result | Confidence |
|----------|--------|------------|
| 9 callers (Seal 7); RequestCast/QuickBar/Stance/NPC pass `(entity, 0, pSkill|null, 0)` | working map | **High** |
| Body: 2nd stack as char skip; 3rd as skill `+0x5fc/+0x614/+0x628…` | plate names **falsified** | **High** |
| Ghidra plate `(this, pCaster, char bSkipBusy, pSkill)` | raw fidelity; do not port plate | **High** |
| Full assembly formal widths | open by policy | Open |

---

## Seal 3: TFID active-binding polarity + compare width

`TFID_NotEquals` (`0x0040b150`):

```c
// equal → 0; unequal → 1
// compares ONLY: *(i32)a==*(i32)b && *(i32)(a+4)==*(i32)(b+4) && *(char)(a+8)==*(char)(b+8)
// NOT full 16-byte TFID
```

Invalid sentinel `g_abTfidInvalid_9CDF88` @ `0x009cdf88` (read_memory):  
`ff ff ff ff ff ff ff ff 00 00 00 00 00 00 00 00` → `{ -1, -1, 0, 0 }` as two dwords + zeros.

`Skill_LookupActiveCastBinding` (`0x00518cf0`): map via **`FUN_00518c20`**; hit copies node `+0x18..+0x24` (16 B); miss fills invalid sentinel.

| Claim | Result |
|-------|--------|
| Deep checks only when binding **equals invalid** (free) | **High** |
| Occupied binding → skip G5–G8 → `return 0` | **High CF** (product rationale open — not “must fail busy”; busy is G2/code 6) |
| Compare width 8+1 bytes (not 16) | **High** (TFID_NotEquals body) |

---

## Seal 4: helpers (leave FUN_*)

| Addr | Role | LocalCast use | Name status |
|------|------|---------------|-------------|
| `0x005169c0` | lazy-get `this+0x64` → `operator_new(0x14)` zero `+4…+0x10` | G2: `*(ret+0x10)!=0` → **6** | **leave FUN_*** |
| `0x00518c20` | lazy-get `this+0x68` → map-like `0xc`; size at `+8` | G7: plant==0 ∧ `*(ret+8)!=0` ∧ skill`+0x615&2` → **10**; also Lookup map | **leave FUN_*** |

Do **not** promote inferred busy-slot / active-cast-map product names without string/RTTI/exclusive plate.

---

## Seal 5: power predicate + dual `+0x6b4` (body-exact)

```c
// G5 — power fail (code 4):
if (plant != 0
    && *(int*)(plant + 0x6b4) < 1
    && *(short*)(plant + 0x12c) < *(short*)(pSkill + 8))
  return 4;
```

| Claim | Status |
|-------|--------|
| Cost field `skill+8` i16 | **High** |
| Current power `plant+0x12c` (dec 300) i16 | **High** |
| Extra conjunct `plant+0x6b4 < 1` required for **4** | **High CF** |
| **If `plant+0x6b4 >= 1`, insufficient power does NOT reject here** | **High CF** (port-critical) |
| G1 vehicle gate: plant null OR `+0x6b4 < 1` → **3** | **High** — same field, earlier stage |
| Product name of `+0x6b4` | **Open** (not “IsWrecked”; toast is wrecked for G1 only) |
| Annotated old `+0x6b4 >= 1` for code 4 | **Falsified** |

---

## Seal 6: hide ladder (codes 11/12) vs range path

```text
related = vtbl+0x19c()
formFlag = byte at related MSVC layout (+0x29 via *( *(related+4)+4 ) + related + 0x29)

if related == 0:
  if (skill+0x615 & 0x40) == 0 → LocalRangeTargetCheck
  else → return 11

else:  // related != 0
  if plant==0 && formFlag:
    if (skill+0x614 & 0x4000) == 0 → return 12   // can't while hidden
    // else fall through
  else if (skill+0x614 & 0x4000) == 0 → LocalRangeTargetCheck
  if formFlag → LocalRangeTargetCheck
  return 11   // must be hidden
```

| Flag | Working name | Confidence |
|------|--------------|------------|
| `skill+0x614 & 0x4000` | allows-while-hidden / skip hide deny | **Probable** |
| `skill+0x615 & 0x40` | requires-hidden when no related | **Probable** |
| `skill+0x615 & 0x02` | pairs with nonempty active map → 10 | **Probable** |
| formFlag layout `+0x29` | hidden/form on related | **Probable** |

`Skill_LocalRangeTargetCheck` (`0x00553130`): may return **0**, **0xf** (accuracy miss + optional StartCastAgainHeartbeat), or handler `*(skill+0x5f0)+0x1c` result (range/target codes).

---

## Seal 7: callers (complete set from Ghidra)

| Caller | VA | Role sketch |
|--------|-----|-------------|
| `Client_QuickBarActivateSkillSlot` | `0x00921b50` | cast only if validate == 0 |
| `Client_RequestCastSkill` | `0x00941590` | fail → FormatFailureMessage toast |
| `Client_StanceOrGadgetActivatePath` | `0x0093a3d0` | stance/gadget path |
| `NPC_TryCastSkillFromSet` | `0x005d1280` | NPC; often null skill lightweight |
| `FUN_00521650` | `0x00521650` | (HB summon path sibling) |
| `FUN_00606180` | `0x00606180` | |
| `FUN_00829190` | `0x00829190` | |
| `FUN_0091f6b0` | `0x0091f6b0` | |
| `FUN_00941d50` | `0x00941d50` | |

Known sealed arg pattern: `(caster, 0, pSkill|null, 0)` — skipBusy always 0 at audited sites.

---

## Re-decompile check (Ghidra 2026-07-29 gate-ladder pass)

- CF, return constants `3,4,5,6,7,10,0xb,0xc`, callees, plate scramble **unchanged** vs raw.
- FormatFailureMessage switch re-checked for codes 3–7, 10–14.
- TFID invalid sentinel re-read via `read_memory`.
- Leave-FUN helpers **not** renamed.

---

## Residual after this pass

| Item | Status |
|------|--------|
| Ordered gate ladder G0–G9 | **Sealed High** |
| UX strings for codes this unit returns | **Sealed High** |
| Codes 10/11/12 product UX labels | **Sealed High** |
| Working ABI vs plate | **High** working; assembly formal open |
| Power: `+0x6b4>=1` skips power deny | **Sealed High CF** (new emphasis) |
| TFID polarity + partial compare | **Sealed High** |
| Caller set (9) | **Sealed High** |
| `FUN_005169c0` / `FUN_00518c20` | leave FUN_* (role sealed) |
| `plant+0x6b4` product field name | Open |
| Skill flag bit product enum | Probable only |
| Occupied binding → allow 0 rationale | High CF; product open |
| Runtime deny-code capture matrix | Open |
| Bit-exact / image diff | Open (policy) |
| 13/14 as *this* literals | **Falsified**; downstream only |

## Verdict after residual

**accept-with-gaps** — dual residual **gate ladder + UX bridge + power dual-use of +0x6b4 + TFID + helpers + callers** strengthened; assembly formal seal and runtime matrix remain.

## Artifacts touched by residual owner

- This scratch: `docs/reconstruction/reviews/a_0051a790.md`
- Duals: `A_aa_0051a790_Skill_LocalCastValidate.md`, `B_aa_0051a790_Skill_LocalCastValidate.md`
- Function record: `functions/aa_0051a790_Skill_LocalCastValidate.md`
- Annotated ladder note: `raw/aa_0051a790_Skill_LocalCastValidate.annotated.md`
- Clean plate comment only if needed (no CF rewrite): `reconstructed-exact/Skill_LocalCastValidate.cpp`
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
- **Not** Launcher
- **Not** rename `FUN_005169c0` / `FUN_00518c20`
