# Review B (skeptical / adversarial): `aa_004b7e50` FUN_004b7e50

| Field | Value |
|---|---|
| **Stable ID** | `aa_004b7e50` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_004b7e50_FUN_004b7e50.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | Always plays all entries | Gate `this+0x1F0==0` → return; hash suppress; name filter; many per-entry skips | **Falsified** — heavily gated |
| 2 | No writes to FX entries | `param_2` forces `+0x104=0x32`; TFID block writes `+0x120..` | **Falsified** — **mutates entries** |
| 3 | Target always required | `param_3` optional; null path still reaches list with synthetic TFID | **Confirmed optional** |
| 4 | `param_5..7` always used | Only some attach branches; zero-length uses other basis | **Confirmed conditional** |
| 5 | Is mission-complete only | Callers: weapon `004b8dc0`/`0056d160`, combat damage, many hosts | **Falsify exclusive mission** |
| 6 | Clean invented `+0x1F0` gate | Entry bytes `83 BB F0 01 00 00 00` | **Confirmed** |
| 7 | Body is small helper | Meta end `004b85c4` | **Falsified** — **~0x774 byte** complex unit |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| ABI thiscall + 6 stack args | **High** | Call wrappers crash |
| Master gate `+0x1F0` | **High** | Spurious FX when disabled |
| List head `+0x1EC`, payload `node[2]` | **High** | Walk corruption |
| Force type `0x32` at `+0x104` | **High** | Wrong FX class |
| Entry flag offsets 0x108–0x131 | **High** for presence / **Medium** for product names | Mis-port filters |
| Full type matrix / every leaf | **Low–Medium** | Incomplete cosmetic parity |
| Ghidra multi-arg audio names | **Medium** | Wrong server-side expectations |

---

## 3. Cross-check against raw + bytes

```
raw skeleton:
  if (*(this+0x1f0)==0) return;
  resolve param_3 via this+0x10 / vtbl+0x1cc when null;
  optional CNDHash suppress return;
  build TStack_50 (global DAT_00b036f0 path | object coid | counter--);
  for list at this+0x1ec:
    if param_2: entry[0x104]=0x32
    name filter / empty-name gate
    multi-way play (PlayNamedInterfaceSound / 007258a0 / 00723120 / position)

entry bytes @ 004b7e50:
  558BEC83E4F083EC74538BD9 83BBF001000000 5657 ... 0F844D070000
  8B7D0C 85FF 751A ...

≡ CF for gates + list. Large mid-body: decompile ≡ raw (same labels/branches);
not every constant re-read via read_memory (prefer decompile per Agents.md).
```

---

## 4. Surviving contract for AutoCore

```csharp
// Client FX host: process attached named FX entries (cosmetics / UI sounds).
void SpecialFxHost_ProcessAttachedList(
    Host h,
    bool forceType0x32,
    object? target,
    string? nameFilter,
    Vector3 worldHint)
{
    if (h.MasterEnable /*+0x1F0*/ == 0) return;
    target ??= h.DefaultTarget /*+0x10*/?.Resolve();
    if (target != null && ShouldSuppress(h, target)) return;
    var ctx = BuildAttachContext(h, target); // TFID / synthetic id
    foreach (var entry in h.FxList /*+0x1EC*/)
    {
        if (forceType0x32) entry.Kind = 0x32;
        if (!NameAllows(entry, nameFilter)) continue;
        PlayOrAttach(entry, ctx, worldHint); // branchy; see raw
    }
}
```

- AutoCore server: **do not** require this for combat authority.
- Client parity: at least honor master enable, list membership, and name filter when driving local VFX.

---

## 5. Open questions

1. Full FX entry schema and every `Kind` value’s play path.
2. Authoritative names for audio helpers (table getter vs play).
3. `DAT_00b036f0` feature flag product name.
4. Whether synthetic `+0x2E8` counter is process-global only.
5. Runtime / differential verification.

**Verdict:** **accept**
