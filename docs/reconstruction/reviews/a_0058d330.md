# Dual residual: `0x0058d330` Skill_GatherTargetsInArea

| Field | Value |
|-------|-------|
| Stable ID | `aa_0058d330` |
| Address | `0x0058d330` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 (strengthen pass) |
| Prior dual | A/B accept-with-gaps — residual: **gather mode enum CF** (`param_8`) |
| Tool | Ghidra MCP `decompile_function` + `get_function_callers` + `get_assembly_context` + `batch_decompile` |

## Residual question

Publish behavioral semantics of `param_8` (`gatherMode` / `filterMode`) used by:

1. Broadphase query flag selection + candidate keep gates **inside** this function
2. Pair-accept helper `FUN_0058a810` (`param_4` = same mode; **not owned** — referenced for CF only)
3. Literal call sites + skill runtime `skill+0x60c` → mode via `Skill_ResolveTargetList` / `NPC_TryCastSkillFromSet`

## Seal: gather-mode table (CF High; English names INFERRED)

`param_8` is an integer **relationship / pick class**, not a free bitfield. Skill casts feed it from **`skill+0x60c`**; UI/input callers pass **literals**.

### Spatial query flags (`FUN_004ea350` last arg) — body CF

```text
queryFlags = 1
if (0 < gatherMode) {
  if (gatherMode < 3) queryFlags = 0x11;   // modes 1 and 2 only
  else if (gatherMode == 7) queryFlags = 10; // 0x0a
}
// else (≤0 or ≥3 except 7): stay 1
```

| `gatherMode` | query flags | Notes |
|---:|---:|---|
| default (≤0 or ≥3, except 7) | `1` | baseline broadphase |
| `1` or `2` (`0 < mode < 3`) | `0x11` | friend/foe combat pick radius query |
| `7` | `10` (`0x0a`) | special type-filtered interact query |

### Pass-1 candidate keep (entity interior vector) — body CF

Polarity is **prefer-primary-slot when test nonzero**; zero falls into link path:

| `gatherMode` | Keep rule (this function) | Confidence |
|---:|---|---|
| `7` | `vtbl+0x1dc()`; if **nonzero** take entity slot immediately; if **0** fall into `+0xd4` then `+0xdc` link path | **High** CF |
| `10` | `vtbl+0x198()`; if **nonzero** take entity slot immediately; if **0** fall into link path | **High** CF |
| `6` | skip `+0xd4` gate; only `+0xdc` nonzero → entity slot | **High** CF |
| other | require `+0xd4` nonzero, else `+0xdc` nonzero | **High** CF |

Cross-unit: `vtbl+0x198 == 0` is required for attack candidates in `CVOGHBAIBase_FindTargetToAttack` (**alive** gate). Non-zero ⇒ **not-alive / dead-like**. Mode **10** is the only path that **allows** `+0x198 != 0` in `FUN_0058a810`. Pass-1 for mode 10 **prefers** dead-like (`+0x198 != 0`) for the direct slot, but still admits alive via the link path before the helper.

### Pass-2 accept (`FUN_0058a810` `param_4`) — helper CF (xref only)

**Early exit mode 8:** copy `entity[0x20..0x23]` → out scratch, `return 1` (skips all gates below).

**Common prefilters** (all modes except early **8**):

1. Reject null candidate or `candidate == caster` (identity).
2. Reject `vtbl+0x198() != 0` unless mode **10**.
3. Reject if candidate TFID (`entity+0x58` / byte `+0x160`) equals TFID at **`outList` base** (`TFID_EqualsObjectId`) — primary/first-slot dedupe, not full-list scan.
4. If `filterB==0` and `FUN_005134e0()!=0` → reject.
5. If `(entity[0x5f]>>10)&1` and mode≠**3** and `filterC==0` → reject.
6. Type branch on `clonebase+0x38`:
   - **0xe Vehicle / 0x12 Creature / 0x14 Character**: special position remap paths; **`filterA` is not applied** as an exact type match on these three.
   - **other types**: `filterA==-1` → `FUN_005130e0(1)` gate; else require `filterA == clonebase+0x38`.

**Mode-specific accept** (after common filters; original `clonebase+0x38` as `typeId`):

| Mode | INFERRED role | Accept CF | Self allowed in gather body | Literal / data sources |
|---:|---|---|---|---|
| **0** | Self / related-owner | Target `vtbl+0x214` entity equals caster, or both null-ish | **Yes** (`mode==0`) | skill `+0x60c` only (no literal) |
| **1** | Ally / non-hostile | Caster `vtbl+0x298(target)==0` | If `param_11` (`allowSelfMode1`) | **`FUN_0061fdf0`** literal `1`, `filterA=-1`; skill |
| **2** | Hostile / enemy | Caster `vtbl+0x298(target)!=0` | No (self flags ignored for mode 2) | DriveControlTick; InteractClickPick; `FUN_00921f70`; `FUN_00943340`; `FUN_00943520` |
| **3** | Living interactables | `typeId ∈ {0xe Vehicle, 0x12 Creature, 0x14 Character}` only | No | PollBoundActions; InteractClickPick; `FUN_009210e0` |
| **4** | Same related TFID | Both sides `+0x214`; TFID match via `FUN_0040aff0` / `TFID_EqualsObjectId` | No | skill data only (no literal call) |
| **6** | `+0xdc`-only broadphase | After common filters, **default accept** (`return 1`) | No | body residual; sibling `FUN_0058cd60(..., mode 6)` in `FUN_009210e0` (not this unit) |
| **7** | Type-filtered special | Default accept after common filters; query flags `10`; pass-1 `+0x1dc` prefer | No | PollBound / `FUN_009210e0` with **mode 7, filterA=0x14** — note: Veh/Cre/Char **bypass** filterA exactness, so 0x14 does **not** alone force Character-only |
| **8** | Unfiltered hit | **Early accept** — skips nearly all gates | N/A (early) | **PollBoundActions** literal `8` (use-object fallback) |
| **9** | Type-3 **or** hostile | If `typeId==3` (`ObjectGraphicsPhysics`) accept; else same as mode **2** (`+0x298`) | No | skill data only |
| **10** | Dead / corpse-like | Allows `vtbl+0x198 != 0`; pass-1 prefers that set; default accept after commons | No | skill data; body+helper only |
| **0xb (11)** | Same vehicle / plant crew | Caster & target `vtbl+0x210` same non-null, or crew helper `FUN_005749d0` when `vehicle+0xcb0!=0` | **Yes** | skill data; allow-self in gather |
| **unlisted** (e.g. 5, 12+) | Default relationship | Query flags `1`; pass-1 default `+0xd4/+0xdc`; helper falls to **`return 1`** after commons | No | no known literal |

CloneBase type IDs cross-checked with `CloneBaseObjectType` (`src/AutoCore.Game/Constants/ClonebaseObjectType.cs`): ObjectGraphicsPhysics=3, Vehicle=14=`0xe`, Creature=18=`0x12`, Character=20=`0x14`.

### Self-allow formula (this function, post-helper)

After `FUN_0058a810` accepts, gather still skips identity-related entity unless:

```text
(selfRelated != candidate)
  || (gatherMode == 0)
  || (gatherMode == 0xb)
  || (gatherMode == 1 && allowSelfMode1 != 0)
```

`selfRelated` = caster `vtbl+0x19c()` then interior `+4` (may be null). Distinct from helper’s `candidate == caster` identity reject.

### Related params (not mode enum, but sealed for ABI)

| Param | Role | Confidence |
|------:|---|---|
| `param_9` filterA | Type filter into helper (`-1` = any via `FUN_005130e0`, else exact) — **bypassed** for Veh/Cre/Char remap paths | **High** |
| `param_10` chainRefresh | On accept, re-origin position to last hit (chain); also skips candidates whose `vtbl+0x1c8` equals source body when set | **High** |
| `param_11` allowSelfMode1 | With mode **1** only, allow self TFID write | **High** |
| `param_12` filterB | Helper: when **0**, `FUN_005134e0` may reject | **High** CF |
| `param_13` filterC | Helper: when non-0, allows flag bit10 objects outside mode 3 | **High** CF |
| `param_14` sortByDistance | Deferred sort via `FUN_0058def0` / `FUN_0058c7a0` | **High** |
| Out TFID stride | **0x10**; hard cap **99**; terminator `DAT_009d4d28..` | **High** |
| Stack cleanup | Callers `ADD ESP,0x3c` → **15** dwords | **High** |

Skill path mapping (`Skill_ResolveTargetList` `0x00550300` → this) — decompile sealed:

```text
p8  gatherMode      = skill+0x60c
p9  filterA         = skill+0x610
p10 chainRefresh    = (skill+0x624 >> 14) & 1
p11 allowSelfMode1  = (skill+0x624 >> 5) & 1
p12 filterB         = 1
p13 filterC         = uVar2:
        multi/area branch (bits 0x20|0x4000 clear + other): 0
        else if skill+0x624 == 0 or == 0x20: 1
        else: 0
p14 sort            = 0
p15 scoreArg        = skill+0x34 or 0xbf800000 (path-dependent)
p6  range           = skill+0x28 or +0x38 (path-dependent)
p7  maxTargets      = skill+0xde capacity residue (path-dependent)
```

Also: `NPC_TryCastSkillFromSet` `0x005d1280` calls this (skill-data path; same unit as Resolve for mode source — not re-owned).

### Caller inventory (literal modes verified)

| Caller | VA (call) | Mode | filterA | Notes |
|---|---|---:|---:|---|
| `Skill_ResolveTargetList` | `0x00550616` | `skill+0x60c` | `skill+0x610` | primary skill cast path |
| `NPC_TryCastSkillFromSet` | `0x005d1706` | skill fields | skill fields | NPC cast path |
| `FUN_0061fdf0` | `0x0061ff9d` | **1** | **-1** | ally/non-hostile gather |
| `FUN_009210e0` | `0x009212a7` | **3** | 0 | living interact |
| `FUN_009210e0` | `0x00921314` | **7** | **0x14** | special; Character filterA |
| `FUN_00921f70` | `0x0092216f` | **2** | 0 | hostile; filterB=0, filterC=1 |
| `Client_Input_DriveControlTick` | `0x00923442` | **2** | 0 | hostile pick |
| `Client_InteractClickPickTarget` | `0x00924a3c` | **3** | 0 | living interact |
| `Client_InteractClickPickTarget` | `0x00924cf3` | **2** | 0 | hostile pick |
| `Client_Input_PollBoundActions` | `0x00927770` / `0x00927af9` | **7** | **0x14** | special Character |
| `Client_Input_PollBoundActions` | `0x00927a8c` | **3** | 0 | living interact |
| `Client_Input_PollBoundActions` | `0x00927b98` | **8** | 0 | unfiltered early-accept |
| `FUN_00943340` / `FUN_00943520` | `0x00943429` / `0x009435fb` / `0x0094366b` | **2** (prior dual) | — | hostile UI paths |

Modes **0, 4, 6, 9, 10, 0xb**: no direct literal at this function’s call sites in current xref set (skill `+0x60c` or body-only residual). Mode **6** appears as literal on sibling `FUN_0058cd60` inside `FUN_009210e0` (out of ownership).

## Re-decompile check (Ghidra 2026-07-29 strengthen)

Body matches frozen raw/clean: query flag ladder; modes 7/10/6 pass-1 polarity; self allow `{0, 0xb, 1&&flag}`; TFID stride `0x10`; cap 99; `FUN_0058a810` call; `ADD ESP,0x3c`. Helper mode switch exhaustiveness re-read (8 early; 2/1/0xb/0/3/4/9 special; else `return 1`). No CF drift vs prior seal; **caller table + filterA bypass + pass-1 polarity** tightened.

## Residual after this pass

| Item | Status |
|------|--------|
| Mode table **behavioral** CF | **Sealed High** |
| English/PDB enum names | Open (INFERRED labels only) |
| `FUN_0058a810` / spatial helper **rename** | Open (out of ownership; role documented) |
| filterA=0x14 with mode 7 semantic intent | CF: does **not** exclude Veh/Cre; intent INFERRED |
| Runtime / bit-exact / image diff | Still open (matrix policy) |

## Artifacts touched by residual owner

- This scratch: `reviews/a_0058d330.md`
- Duals: `A_aa_0058d330_Skill_GatherTargetsInArea.md`, `B_aa_0058d330_Skill_GatherTargetsInArea.md`
- Function record, annotated raw plate notes (pass-1 polarity fix)
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
- **Not** Launcher
