# Review A (reconstruction fidelity): `aa_007203b0` FUN_007203b0

| Field | Value |
|---|---|
| **Stable ID** | `aa_007203b0` |
| **VA** | `0x007203b0` |
| **Canonical name** | `FUN_007203b0` |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Independent reconstruction review |
| **Counterpart** | `reviews/B_aa_007203b0_FUN_007203b0.md` |
| **System** | client audio — mark named interface-sound slots for stop |
| **Verdict** | **accept** — CS + slot scan + stop flags **sealed** |

---

## 1. Purpose

**Stop / mark-for-stop named interface sounds** on the singleton table (`this` typically from `FUN_00720d40`).

1. If **`this+0x10 == 0`**, return immediately (subsystem disabled).
2. **`EnterCriticalSection(this+0x158)`**.
3. If **`param_3 != 0`**: call **`FUN_0071ffc0(param_2)`** (extra cleanup/prep with the name).
4. Scan **two pairs of slots** starting at **`this+0x80`** (loop count 2; each iteration checks `piVar3[2]` and `*piVar3`, then `piVar3++` — decompile layout is a small fixed table):
   - For each non-null slot entry, **`_stricmp(entry+0x48, param_2)`**.
   - On match: set **`entry+0x38 = 1`** (stop flag) and **`entry+0x1B0 = 0`**.
5. **`LeaveCriticalSection(this+0x158)`**.

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Raw | `docs/reconstruction/raw/aa_007203b0_FUN_007203b0.md` |
| Annotated | `docs/reconstruction/raw/aa_007203b0_FUN_007203b0.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/FUN_007203b0.cpp` |
| Function record | `docs/reconstruction/functions/aa_007203b0_FUN_007203b0.md` |
| Live decompile | Ghidra `batch_decompile` `0x007203b0` — ≡ raw |
| Live bytes | Ghidra `read_memory` `0x007203b0` length 48 |
| Callees | `EnterCriticalSection`, `LeaveCriticalSection`, `_stricmp`, `FUN_0071ffc0` |
| Callers | FX host deactivate secondary path; UI stop sites |

---

## 3. Sealed residuals

### 3.1 ABI — **SEALED**

| Fact | Evidence |
|---|---|
| Convention | **`__thiscall`**: `this` in **ECX** (`mov esi, ecx`) |
| Args | **`char* name`**, **`char doExtra`** (`param_3`) |
| Gate | **`this+0x10 != 0`** |

```c
void __thiscall FUN_007203b0(void *this, char *name, char doExtra);
```

### 3.2 Algorithm — **SEALED**

```
if (this+0x10 == 0) return
EnterCS(this+0x158)
if (doExtra) FUN_0071ffc0(name)
for 2 iterations over slot table @ this+0x80:
  for each non-null entry pointer in the pair:
    if stricmp(entry+0x48, name) == 0:
      entry+0x38 = 1
      entry+0x1B0 = 0
LeaveCS(this+0x158)
```

### 3.3 Layout fields — **SEALED**

**Table object (`this`):**

| Off | Role |
|---|---|
| `+0x10` | master enable (byte); 0 → no-op |
| `+0x80` | start of fixed active-slot table |
| `+0x158` | **CRITICAL_SECTION** |

**Slot entry:**

| Off | Role |
|---|---|
| `+0x38` | **stop requested** (set to 1) |
| `+0x48` | **name C-string** (case-insensitive match) |
| `+0x1B0` | cleared to 0 on match (handle/ref / lifetime) |

### 3.4 Entry machine bytes — **SEALED**

```
51                      push ecx
56                      push esi
8B F1                   mov  esi, ecx
80 7E 10 00             cmp  byte ptr [esi+0x10], 0
0F 84 9D 00 00 00       jz   → epilogue
53 55                   push ebx/ebp
8D 86 58 01 00 00       lea  eax, [esi+0x158]
57 50                   push edi; push eax  ; CS*
89 44 24 14
FF 15 BC 61 9C 00       call dword ptr [EnterCriticalSection]
80 7C 24 1C 00          cmp  byte ptr [esp+0x1C], 0  ; doExtra
8B 6C 24 18             mov  ebp, [esp+0x18]          ; name
74 06                   jz   skip extra
55 E8 ...               push name; call FUN_0071ffc0
```

Hex prefix:  
`51568bf1807e10000f849d00000053558d8658010000575089442414ff15bc619c00807c241c008b6c2418740655e8dd`

---

## 4. Control flow: clean ≡ raw ≡ live

| Stage | Match |
|---|---|
| enable gate `+0x10` | Yes |
| CS enter/leave | Yes |
| optional `0071ffc0` | Yes |
| slot stricmp + mark stop | Yes |

---

## 5. Gaps / open

1. Exact slot-table geometry (2×2 pointers vs packed layout) — decompile is slightly awkward but match/stop behavior is clear.
2. Full meaning of `+0x1B0` clear.
3. Runtime verification not run.

**Verdict:** **accept**
