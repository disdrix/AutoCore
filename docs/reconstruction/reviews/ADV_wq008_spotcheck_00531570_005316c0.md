# Independent adversarial spot-check — WQ8R-B dual `0x00531570` + `0x005316c0`

| Field | Value |
|---|---|
| **Date** | `2026-08-04` |
| **Role** | Independent adversarial verifier (**not** WQ8R-B dual author) |
| **Workspace** | `C:\Users\josh\Documents\GitHub\AutoCore` |
| **Partition** | WQ8R-B OWN pair (WQ-008 residual inventory-transfer) |
| **Units** | `0x00531570` `CVOGCharacter_TryType1aSimpleExchange_Inferred`; `0x005316c0` `CVOGCharacter_TryType1aCraft_Inferred` |
| **Tools** | Ghidra MCP `batch_decompile`, `decompile_function`, `read_memory`, `get_xrefs_to`, `get_function_callers`; artifact read-through |
| **Forbidden** | `disassemble_bytes`; parent ledger edits; dual rewrite |
| **Image** | `autoassault.exe` base `0x400000` |

---

## Inspected evidence

### Dual / report surfaces

| Path |
|---|
| `docs/agents/task-dual-ab-00531570-005316c0-wq8rb-report.md` |
| `docs/reconstruction/reviews/A_aa_00531570_CVOGCharacter_TryType1aSimpleExchange_Inferred.md` |
| `docs/reconstruction/reviews/B_aa_00531570_CVOGCharacter_TryType1aSimpleExchange_Inferred.md` |
| `docs/reconstruction/reviews/A_aa_005316c0_CVOGCharacter_TryType1aCraft_Inferred.md` |
| `docs/reconstruction/reviews/B_aa_005316c0_CVOGCharacter_TryType1aCraft_Inferred.md` |

### Artifacts (both units)

| Kind | `aa_00531570` | `aa_005316c0` |
|---|---|---|
| Raw | `docs/reconstruction/raw/aa_00531570_FUN_00531570.md` | `docs/reconstruction/raw/aa_005316c0_FUN_005316c0.md` |
| Annotated | `…/aa_00531570_FUN_00531570.annotated.md` | `…/aa_005316c0_FUN_005316c0.annotated.md` |
| Clean named | `…/CVOGCharacter_TryType1aSimpleExchange_Inferred.cpp` | `…/CVOGCharacter_TryType1aCraft_Inferred.cpp` |
| Clean scaffold | `…/FUN_00531570.cpp` | `…/FUN_005316c0.cpp` |
| Function named | `…/aa_00531570_CVOGCharacter_TryType1aSimpleExchange_Inferred.md` | `…/aa_005316c0_CVOGCharacter_TryType1aCraft_Inferred.md` |

### Live Ghidra spot-checks

| Check | Result |
|---|---|
| `batch_decompile(0x00531570,0x005316c0)` | Live CF **≡** 2026-07-23 raw bodies (bit-identical structure) |
| `read_memory` @ `0x00531570` (256 B+) | Prologue, dual-grid ECX, type `0x1a`, ingredient/mult/product, FindFree page `-1`, remove/give, `ret 8` |
| `read_memory` @ `0x005316c0` / mid / epilogue | Prologue, cargo/locker, soft-success `jz`→`mov al,1`, mode 1/2, primary `+0x4d0/+0x4d4`, secondary `0x498`, product `blob+4`, `ret 12` |
| `decompile_function(0x00522060)` | Type-`0x1a` gate; mode 1/2 bank masks; simple path uses `+0x4c0` when `param_5!=0` |
| `decompile_function(0x00599dd0)` | Counts non-`-1` among five dwords `+0x498…+0x4a8` |
| `decompile_function(0x005310a0)` | Give-by-CBID path (Ghidra arity incomplete — see niggle); call boundary sealed at callers |
| `get_xrefs_to` / `get_function_callers` both VAs | **No** direct code xrefs / callers |

---

## Unit A — `0x00531570` CVOGCharacter_TryType1aSimpleExchange_Inferred

### Body / ABI seal (independent)

| Claim | Independent result |
|---|---|
| `__thiscall`, **`ret 8`** (two stack args) | **Confirmed** — early fail `c2 08 00`; success/fail epilogues same |
| Null object → 0 | **Confirmed** — `test edx,edx` / `xor al,al` / `ret 8` |
| Cargo `*(*(this+0x250)+0x2b0)` → `ebx` | **Confirmed** — `mov eax,[ecx+0x250]`; `mov ebx,[eax+0x2b0]` |
| Locker `*(this+0xcbc)` → `ebp` | **Confirmed** — `mov ebp,[ecx+0xcbc]` |
| Type gate `object+0x38 == 0x1a` | **Confirmed** — `83 7a 38 1a` |
| Blob `object+0x3c`; ingredient `[blob+0x4c0]` | **Confirmed** — `mov esi,[edx+0x3c]`; `mov eax,[esi+0x4c0]` |
| Dual Count ECX cargo then locker | **Confirmed** — `mov ecx,ebx` / `call 0x005711c0`; then `mov ecx,ebp` / `call 0x005711c0`; `add eax,edi` |
| Mult `(u8)blob+0x4c9 * qty` | **Confirmed** — `movzx … [esi+0x4c9]`; `imul` |
| Product CBID `object+0x34` | **Confirmed** — `mov ecx,[eax+0x34]` before give path |
| FindFree **cargo**, page **`-1`**, sizes `+0x406/+0x407` | **Confirmed** — `push -1` (`6a ff`); `mov cl/dl` from `+0x407/+0x406`; `mov ecx,ebx`; `call 0x005713a0` |
| Remove cargo then locker remainder | **Confirmed** — first Remove ECX=cargo; second uses saved locker + remainder qty; targets `0x005721c0` |
| Give `FUN_005310a0(self, outCbid, outCount)` | **Confirmed** at call boundary — character in ECX; two stack dwords; `E8` → `0x005310a0` |
| Clean restores dual-grid ECX (not double-count one grid) | **Confirmed** — clean matches sealed ECX model |
| Zero direct callers | **Confirmed** — `get_xrefs_to` / `get_function_callers` empty |

### Adversarial attacks (simple exchange)

| # | Attack | Result |
|---|---|---|
| 1 | Decompiler two `CountItemsByCbid(cbid,0)` = double-count same grid | **Fails** — ECX alternates `ebx` cargo / `ebp` locker |
| 2 | FindFree on locker / whole character | **Fails** — `mov ecx,ebx` cargo only |
| 3 | Unit stamps footprint at FindFree (ox,oy) | **Fails** — outs local only; grant via give helper |
| 4 | Product English is "recipe"/"blueprint" without `_Inferred` | **Fails** — no string/RTTI here; dual naming hygiene OK |
| 5 | `object+0x34` proven TFID layout | **Overstated if claimed** — dual correctly treats as **give CBID** only |
| 6 | Scaffold 2026-07-23 already dual-sealed | **Fails** — FUN paste only; this dual supersedes |
| 7 | Runtime **Confirmed** | **Fails** — no Launcher / CE this pass (dual honest) |
| 8 | Remove always drains both grids fully | **Nuanced** — second remove only if `qty - removed > 0` |

### Soft niggle (not sealed-claim failure)

- Ghidra `FUN_005310a0` decompile shows incomplete arity (qty not modeled cleanly) while call sites push **(cbid, qty)**. Dual correctly seals **call boundary**, not a full dual of the give helper. Gap already listed.

### Unit A dual quality

**PASS** — keep **`accept-with-gaps`**.

---

## Unit B — `0x005316c0` CVOGCharacter_TryType1aCraft_Inferred

### Body / ABI seal (independent)

| Claim | Independent result |
|---|---|
| `__thiscall`, **`ret 12`** (three stack args) | **Confirmed** — early/`c2 0c 00` throughout; success `mov al,1; … ret 12` |
| Null object / missing cargo → 0 | **Confirmed** |
| Cargo → `esi` (`local_10`); locker `[char+0xcbc]` | **Confirmed** — same pointer family as twin |
| `craftQty > 0` and locker required | **Confirmed** — decompile + qty load / tests |
| Blob ensure `FUN_004f1e20` if `object+0x3c==0` | **Confirmed** — `cmp [ebp+0x3c],0`; `push 1; push 0; mov ecx,ebp; call` |
| `FUN_00522060(character, obj, &available, mode, 0)` | **Confirmed** — `mov ecx,edi` (character); `E8` → `0x00522060` |
| **Soft no-op: count helper false → return 1** | **Confirmed (bytes)** — `test al,al` / `jz` lands on **`5B 5E 5F B0 01 5D 83 C4 1C C2 0C 00`** (success epilogue), **not** the `32 C0` fail path |
| Hard fail `craftQty > available` | **Confirmed** — compare then fail path |
| FindFree cargo, page `-1`, sizes `+0x406/+0x407` | **Confirmed** — `push -1`; `mov ecx,esi`; `call 0x005713a0` |
| Mode **1** skips primary; mode **2** skips secondary | **Confirmed** — `cmp al,1` / `cmp al,2` + `setne` secondary-enable |
| Primary CBID `blob+0x4d0`, cost `s8 blob+0x4d4` | **Confirmed** — `mov eax,[edi+0x4d0]`; `movsx … [edi+0x4d4]`; dual Count + Remove |
| Primary shortfall split (`qtyPrimary = have/cost`, `secondaryNeed = qty - qtyPrimary`) | **Confirmed** — decompile math + `idiv` path in body |
| Secondary base offset `0x498`, stride 4, skip CBID `-1` | **Confirmed** — `mov [esp+…],0x498`; load; `cmp …,-1` |
| Loop bound via `FUN_00599dd0` | **Confirmed** — callee counts non-`-1` among five slots; loop `i < count` from offset 0 |
| Product CBID **`*(blob+4)`** (not `object+0x34`) | **Confirmed** — give site `mov ecx,[edi+4]`; push qty; push cbid; ECX=character; `call 0x005310a0` |
| Type `0x1a` not re-checked in this body | **Confirmed** — enforced inside `FUN_00522060` (`object+0x38==0x1a`) |
| Zero direct callers | **Confirmed** |

### Adversarial attacks (craft)

| # | Attack | Result |
|---|---|---|
| 1 | Soft `return 1` is decompiler bug / should be 0 | **Fails** — live re-decompile + **epilogue `jz`→`mov al,1`** |
| 2 | Same product field as simple twin (`object+0x34`) | **Fails** — this unit gives `*(blob+4)` |
| 3 | Always uses ingredient `+0x4c0` like simple twin | **Fails** — multi path `+0x4d0/+0x4d4` + `+0x498` list; `+0x4c0` is simple-exchange / count helper `param_5!=0` |
| 4 | Mode is inventoryType 1/3/5 | **Fails** — compared to **1** and **2** as bank enable flags |
| 5 | FindFree uses locker | **Fails** — ECX = cargo (`esi`) |
| 6 | Secondary always walks fixed 5 slots | **Nuanced** — bound = `FUN_00599dd0()` non-`-1` **count**, iterate from index 0 (dense-prefix schemas OK; sparse holes can truncate — image CF) |
| 7 | Places crafted item into FindFree (ox,oy) | **Fails** — outs discarded for placement; give helper creates/adds |
| 8 | Scaffold dual already sealed | **Fails** — 2026-07-23 FUN record only |
| 9 | Runtime Confirmed | **Fails** — no Launcher |

### Soft niggle (not sealed-claim failure)

1. **Review B wording** “prefix count” for `FUN_00599dd0` is slightly imprecise: the helper returns the **total** number of non-`-1` among five slots, and the loop runs that many steps from offset `0x498`. Equivalent for dense prefixes; not a full fixed-5 scan. Clean loop matches image semantics.
2. When primary is skipped or secondary list is empty after a primary shortfall, `totalOut` may be partial / zero while the unit still returns **1** after give — dual already flags soft-success / shortfall product rationale as residual.
3. Full dual of `FUN_00522060` / `FUN_00599dd0` / `FUN_005310a0` / `FUN_004f1e20` not owned here (correct exclusion).

### Unit B dual quality

**PASS** — keep **`accept-with-gaps`**.

---

## Cross-unit / report seal

| Report claim | ADV result |
|---|---|
| Shared cargo/locker pointer family | **Confirmed** both units |
| Dual-grid remove cargo→locker remainder | **Confirmed** both |
| FindFree cargo-only, page `-1`, InvSize blob `+0x406/+0x407` | **Confirmed** both |
| Simple product `object+0x34` vs craft `*(blob+4)` | **Confirmed** distinct |
| Simple `ret 8` / craft `ret 12` | **Confirmed** |
| Zero static CALL xrefs either entry | **Confirmed** |
| Live decompile ≡ raw | **Confirmed** this session |
| Path A accept-with-gaps; Path B rejects product English / place-stamp / inventoryType-mode | **Agree** |
| Never claim runtime Confirmed | **Agree** |

### Clean ≡ raw CF

| Unit | Match |
|---|---|
| Simple exchange clean | **Yes** — dual-grid ECX restored; gate/mult/product/FindFree/remove/give |
| Craft clean | **Yes** — soft return 1; mode banks; shortfall split; secondary list; product `blob+4` |

---

## Verdict

| Unit | Dual verdict | ADV verdict |
|------|--------------|-------------|
| `aa_00531570` | accept-with-gaps | **PASS — keep accept-with-gaps** |
| `aa_005316c0` | accept-with-gaps | **PASS — keep accept-with-gaps** |

No sealed-claim falsification. Residual gaps dual already lists (type-`0x1a` English, vtable callers, helper duals, runtime/bit-exact, soft-success product rationale) remain fair and do not block accept-with-gaps.

**Parent ledgers:** not touched (per charter).

---

## Deliverable

- `docs/reconstruction/reviews/ADV_wq008_spotcheck_00531570_005316c0.md` (this file)
