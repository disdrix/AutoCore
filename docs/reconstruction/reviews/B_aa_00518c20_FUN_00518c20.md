# Review B (skeptical / independent): `aa_00518c20` FUN_00518c20 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00518c20` |
| **VA** | `0x00518c20` |
| **Canonical name** | `FUN_00518c20` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Skeptical naming / anti-overclaim |
| **Counterpart** | `reviews/A_aa_00518c20_FUN_00518c20.md` |
| **System** | skills-abilities |
| **Verdict** | **leave-FUN** — agree with A; block product / `_Inferred` map name |

---

## 1. Adversarial read of proposed names

| Candidate | Why reject |
|---|---|
| `Skill_GetActiveCastBindingMap` | Strong **usage** from Lookup/Clear, but **no** string/RTTI/plate. CategoryCooldown twin is analogy, not a retail symbol for this VA. Promote only with stronger product evidence. |
| `Skill_GetActiveCastBindingMap_Inferred` | Same as above under `_Inferred`. Sibling leave-FUN rule for LocalCast residuals requires string/RTTI/exclusive plate — multi-caller map shell does not clear that bar alone. |
| `Skill_LazyAllocFormGateMap` / form-only names | LocalCastValidate size check (`+8`) is one consumer only; Lookup/Clear prove map holds skillId→TFID bindings. Form-gate is caller policy, not body identity. |
| `Named_CalleeOf_Skill_LocalCastValidate_00518c20` | Scaffold pollution; exclusive LocalCast use **false**. |

---

## 2. Xref challenge (must not collapse to LocalCastValidate-only)

```
0x00518cd3  FUN_00518ca0
0x00518d00  Skill_LookupActiveCastBinding
0x00518d0e  Skill_LookupActiveCastBinding
0x0051a710  Skill_ClearCastBindingAndMaybeRestartCd
0x0051a71e  Skill_ClearCastBindingAndMaybeRestartCd
0x0051a74f  Skill_ClearCastBindingAndMaybeRestartCd
0x0051a8c7  Skill_LocalCastValidate
```

**7** call sites / **4** functions. Any name implying “LocalCastValidate form helper only” is **wrong**.

---

## 3. What is safe to claim

| Safe | Unsafe |
|---|---|
| Lazy `this+0x68` → 0xC map header | Product class / method symbol |
| Empty-tree init via `FUN_0058d9c0` | Exact `std::map` template args / key type seal from this body alone |
| Twin pattern vs GetCategoryCooldownMap (+0x6c) | Auto-rename because twin is named |
| Multi skill-path consumers (lookup/clear/insert/validate) | Exclusive busy/form allocator |
| Leave FUN_* | Promote without string/RTTI/strong plate |

---

## 4. CF challenge of Review A

- Lazy null-check / alloc / return: **agree Confirmed**
- Map shell layout (head @ +4, size @ +8): **agree Confirmed**
- No string/RTTI: **agree**
- Twin of GetCategoryCooldownMap: **agree High** (structure), **not** a naming seal
- Leave FUN_*: **agree**
- Do not promote active-cast map product name this pass: **agree**

Decompiler multi-arg call noise at Lookup/Clear sites: **do not** invent a second signature for this VA — body is unary getter; find/erase are sibling callees (`FUN_0051c150`, `FUN_0051cb40`, etc.).

---

## 5. Residual risks

- Map owner `this` may be a subsystem singleton/context, not the caster — not sealed from this body.
- Node size 0x30 and value offsets used by Lookup (`+0x18…+0x24`) imply value payload after tree links; full node type open.
- `FUN_00518ca0` still FUN_* — insert path not product-named.

**Verdict:** **leave-FUN**. Reopen rename only on string, RTTI, or stronger product plate — not on LocalCastValidate form-gate or twin-slot analogy alone.
