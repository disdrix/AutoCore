# Dual residual: `0x0054fa20` Skill_FormatFailureMessage

| Field | Value |
|-------|-------|
| Stable ID | `aa_0054fa20` |
| Address | `0x0054fa20`–`0x0054fdf9` |
| Owner | dual residual only (no WORK_QUEUE / RESUME / CHANGE_LOG) |
| Date | 2026-07-29 (residual pass) |
| Prior dual | A **accept** + B **accept** (table sealed 2026-07-29; 0x10 helpers residual) |
| Re-decompile | Ghidra `decompile_function` `0x0054fa20` (2026-07-29 residual) — body **≡** frozen raw |

---

## Residual questions (from dual A/B + wave2)

1. Full **eSkillResponses → English key** table including **0x10** / **0x12** / default set (wave2 line “0x12 summon level” was wrong)
2. Crosswalk vs `Documentation/PACKET STRUCTURES.md` enum **names** (product labels ≠ UX strings)
3. Case **0x10** summon-cap CF: which `FUN_004c*` is count vs cap; level `> 0x50` branch
4. Whether codes **0 / 0x0F / 0x11 / 99** intentionally share default unhandled toast
5. Locale helper product names (`FUN_007a69d0` / `FUN_007a6de0`)

---

## Seal 1: Failure string table (VERIFIED — this unit only)

Fresh decompile **≡** raw switch. Fixed cases are **locale keys** passed to `FUN_007a6de0(key, -1)` then byte-copy into `pOutBuf`.

| Code | Hex | PACKET STRUCTURES name | Dedicated case | English key / path | Conf |
|---:|---|---|---|---|---|
| 0 | 0x00 | `SKILL_RESPONSE_OK` | no | **default** if ever formatted (callers should not) | High |
| 1 | 0x01 | `SERVER_CHECKS_FAILED` | yes | `Server checks failed!` | High |
| 2 | 0x02 | `GENERIC_FAILED` | yes | `Skill failed.` | High |
| 3 | 0x03 | `CORPSE` | yes | `Cannot do that while wrecked!` | High string; name≠gloss residual |
| 4 | 0x04 | `POWER` | yes | `You don't have enough power!` | High |
| 5 | 0x05 | `STATUS` | yes | `You can't do that with your current status.` | High |
| 6 | 0x06 | `BUSY` | yes | `You're already actively using a skill!` | High |
| 7 | 0x07 | `RECHARGE` | yes | `This skill is not ready to use again yet!` | High |
| 8 | 0x08 | `SUMMONCOUNT` | yes | `You have too many of that summon.` | High |
| 9 | 0x09 | `NOAIR` | yes | `You can't do that while in the air.` | High |
| 10 | 0x0A | `EXCLUSIVE` | yes | `You have an active skill that prevents you from doing that.` | High |
| 11 | 0x0B | `NEEDSTEALTH` | yes | `You must be hidden to use this.` | High |
| 12 | 0x0C | `NOSTEALTH` | yes | `You can't do this while hidden.` | High |
| 13 | 0x0D | `RANGE` | yes | `Target out of range.` | High |
| 14 | 0x0E | `FACTION` | yes | `Wrong target type.` | High string; name≠gloss residual |
| 15 | 0x0F | `AI_DIDNT_CAST` | no | **default** `Unhandled reason for skill failure.` | High path; producer outside unit |
| 16 | 0x10 | `SUMMONCOUNT_TOTAL` | yes | summon-cap **sprintf** or case-8 fallback | High templates; helpers Tentative |
| 17 | 0x11 | `CANCELLED_ACTIVE` | no | **default** unhandled | High |
| 18 | 0x12 | `TOO_SOON` | yes | `Target has just been stunted` | High string; name≠gloss residual |
| 99 | 0x63 | `DEATHCAST` | no | **default** unhandled | High (falls switch default) |

### Case 0x10 sprintf templates (English fragments — High)

| Branch | Format | Fragments |
|---|---|---|
| `requiredLevel > 0x50` | `"%s %d %s."` | `You can only have` + maxCount + `summons at a time` |
| else (level known) | `"%s %d %s %s %d."` | `You can only have` + maxCount + `summons at a time` + `until Level` + level |
| no ctx / plant fail / count gate fail | same as case **8** | `You have too many of that summon.` |

Gate shape (raw; helper product names open):

```
if (pCharacterCtx && vtbl[+0x210](0) != 0) {
  vtbl[+0x210](0, 0);  uVar3 = FUN_004c3f10(...);
  vtbl[+0x210](0);     uVar4 = FUN_004c2e60();
  if (uVar4 <= uVar3) {
    // more vtbl+0x210 + FUN_004c2e60 / FUN_004c2eb0 → requiredLevel
    // then sprintf templates above
  }
}
// fallback case 8
```

**Do not invent** which of `uVar3`/`uVar4` is “current” vs “max” without sealing `FUN_004c3f10` / `FUN_004c2e60` (out of unit). Templates + CF topology are sealed.

### Falsified prior claims

| Claim | Outcome |
|---|---|
| Case **0x12** = summon level-cap (wave2 high-signal #3) | **Falsified** — 0x12 = stunted; **0x10** = summon sprintf |
| Case 9 = active-skill / 11 = range / 14 = stunted (2026-07-23 dual A prose) | **Falsified** — see Seal 1 table |
| Unit is server deny authority | **Falsified** — client UX formatter only |
| Mutates cast state | **Falsified** — `pOutBuf` only |

---

## Seal 2: ABI / CF / callees

```c
// MSVC __cdecl
char* Skill_FormatFailureMessage(
    int   eSkillResponse,  // eSkillResponses
    char* pOutBuf,         // caller-owned C buffer
    void* pCharacterCtx);  // optional; used only for case 0x10
// Returns: pOutBuf; or "" if pOutBuf == null (no write)
```

| Claim | Result | Conf |
|---|---|---|
| Span `0054fa20`–`0054fdf9` | Ghidra function body | High |
| Null `pOutBuf` → `""` | early return after outer if | High |
| Else `FUN_007a69d0()` once then switch | raw | High |
| Fixed cases: lookup + strcpy-style loop | raw | High |
| No skill/cast/entity stores | body inspection | High |

**Callers (Ghidra, this unit not owned):**

| Caller | VA |
|---|---|
| `Client_RecvSkillStatusEffect` | `0x00811170` |
| `Client_RequestCastSkill` | `0x00941590` |
| `Client_Skill_ResolveCastTarget` | `0x0093b3a0` |
| `Client_StanceOrGadgetActivatePath` | `0x0093a3d0` |
| `FUN_0091f6b0` | `0x0091f6b0` |

**Callees:** `FUN_007a69d0`, `FUN_007a6de0`, `sprintf`; case 0x10 also `FUN_004c3f10`, `FUN_004c2e60`, `FUN_004c2eb0` + `pCharacterCtx` vtbl `+0x210`.

**Leave-FUN (no product evidence in this unit):** `FUN_007a69d0`, `FUN_007a6de0`, `FUN_004c3f10`, `FUN_004c2e60`, `FUN_004c2eb0`.

---

## Seal 3: Enum name vs UX string (documented residual, not a table bug)

`PACKET STRUCTURES.md` names are **product enum labels**. This unit seals **user-facing English keys**. Name/string mismatches are **expected** and must not be “fixed” in clean:

| Code | Enum name | UX string | Note |
|---:|---|---|---|
| 3 | CORPSE | wrecked | LocalCastValidate also uses 3 for missing plant |
| 14 | FACTION | Wrong target type | Do not invent faction text |
| 18 | TOO_SOON | Target has just been stunted | Stunt/cooldown-soon product gloss |

AutoCore fail-UX parity uses **this table’s English keys**, not enum identifier spelling.

---

## Dual A/B residual verdict

| Review | Prior | Residual pass |
|---|---|---|
| A (fidelity) | accept | **accept** — table + CF sealed; strengthen enum crosswalk |
| B (skeptical) | accept | **accept** — wave2 0x12-summon + mid-table shift stay falsified; 0x10 math residual |

Overall unit: **Human-refined; string table sealed; 0x10 helper math + locale names + runtime open.**

---

## Gaps (still open — unit-local)

1. **0x10** `FUN_004c*` / vtbl+0x210 product semantics (count vs cap vs level table).
2. Locale singleton / lookup product names.
3. Runtime toast capture per deny code (especially **0x0F** generic unhandled vs dedicated).
4. Whether any live path formats **0** or **99**.
5. Annotated “currentCount ≤ maxAllowed” prose vs raw `uVar4 <= uVar3` — **label residual only**; do not rename operands without helper seal.

## Explicit non-goals this pass

- No WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG edits
- No Launcher / servers
- No reconstructed-exact modernization
- Did not own LocalCastValidate, Recv, locale subsystem, or summon helpers

## Artifacts touched

| Kind | Path |
|---|---|
| Dual residual scratch | `docs/reconstruction/reviews/a_0054fa20.md` (this file) |
| Dual A | `docs/reconstruction/reviews/A_aa_0054fa20_Skill_FormatFailureMessage.md` |
| Dual B | `docs/reconstruction/reviews/B_aa_0054fa20_Skill_FormatFailureMessage.md` |
| Function record | `docs/reconstruction/functions/aa_0054fa20_Skill_FormatFailureMessage.md` |
| Annotated | `docs/reconstruction/raw/aa_0054fa20_Skill_FormatFailureMessage.annotated.md` |
| Raw | append-only re-verify note if needed |
| Clean | **unchanged** (table already correct; no modernization) |
| Prior scratch | `tmp/a_format_fail.md` |
