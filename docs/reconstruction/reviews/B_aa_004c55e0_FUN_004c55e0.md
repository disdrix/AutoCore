# Review B (skeptical / adversarial): `aa_004c55e0` FUN_004c55e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004c55e0` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` (dual A/B seal — live Ghidra) |
| **Counterpart** | `reviews/A_aa_004c55e0_FUN_004c55e0.md` |
| **System** | `input-drive-control` |
| **Dual status** | **Present (first dual)** |
| **Verdict** | **accept-with-gaps** on sealed CF/constants; **reject** overclaims that this is PushDrive max, that flag deltas match PushDrive ±0.3/0.5, that ECX is always the vehicle entity, or that early path runs whenever `+0x250` is set |

---

## 1. Claims under attack

| # | Claim | Attack result |
|---|---|---|
| 1 | Body unknown / unreadable | **Falsified** — ~0xBA-byte leaf; live ≡ raw; machine bytes seal |
| 2 | Same formula as PushDriveAxes max (`local_3c`) | **Falsified** — different constants (−0.7/+0.2 vs −0.3/+0.5), fold on base not bonus, scale field **`+0x1f0`** not AbsoluteTopSpeed |
| 3 | Flag 0x1000 always subtracts 0.3 | **Falsified here** — this unit reads **`DAT_00a0f710 = 0.7`** |
| 4 | Flag 0x4000 always adds 0.5 | **Falsified here** — this unit reads **`DAT_00a0f70c = 0.2`** |
| 5 | Both flag bits can combine | **Falsified** — 0x1000 branch jumps past 0x4000 test |
| 6 | Early path is only `+0x250 != 0` | **Falsified** — also requires **`comp+0xf5 == 0`**; non-zero `+0xf5` forces default path |
| 7 | Early path returns max / sum of fields | **Falsified** — `comiss` + `jbe` → **min(+0x110, +0x10c)** |
| 8 | Leaf writes thr/steer/HB | **Falsified** — pure float return; no axis stores |
| 9 | DriveControlTick passes vehicle `param_2` as ECX | **Falsified as universal** — call site `MOV ECX,EBX` (look-object / `DAT_00d1b6d8` lineage); vehicle is `param_2` for setters only |
| 10 | `DAT_00a0f70c` role is “soft steer 0.2” inside this leaf | **Falsified** — here it is **+0.2 base adjust**; soft-look use is caller DCT, not this body |
| 11 | Ready for product rename + bit-exact seal | **Fail** — types / product names open |

---

## 2. Decisive dataflow (raw ≡ live ≡ asm)

```
// entry: ECX = this → ESI; push ecx (local_4 slot)
obj = *(this + 0x250)
if obj != 0:
    comp = component_at(this, +0xa8)   // *( *( *(this+4)+4 ) + this + 0xa8 )
    if *(u8*)(comp + 0xf5) == 0:
        return min( *(f32*)(obj+0x110), *(f32*)(obj+0x10c) )

base = FUN_004c4e20(this)              // sealed sibling: this+0x1f4 ± driver d48
flags = *(u32*)(component_base + this + 0xb8)
if flags & 0x1000:
    base -= 0.7                        // DAT_00a0f710
elif flags & 0x4000:
    base += 0.2                        // DAT_00a0f70c
return (base + 1.0) * *(f32*)(this + 0x1f0)
```

**Prologue / early-path bytes (authoritative):**

```
51                         push ecx
56                         push esi
8B F1                      mov esi, ecx
8B 86 50 02 00 00          mov eax, [esi+0x250]
85 C0                      test eax, eax
74 53                      jz default
...
80 B9 F5 00 00 00 00       cmp byte ptr [ecx+0xf5], 0
75 3D                      jnz default
...
0F 2F 86 0C 01 00 00       comiss xmm0, [esi+0x10c]   ; after load +0x110
76 15                      jbe ret_110
... ret +0x10c / +0x110 via fld
```

**Default-path flag + fold:**

```
F6 C4 10                   test ah, 0x10     ; bit 0x1000
74 10                      jz check_4000
F3 0F 5C 05 10 F7 A0 00    subss xmm0, [00a0f710]
EB 13                      jmp store
F6 C4 40                   test ah, 0x40     ; bit 0x4000
...
F3 0F 58 05 0C F7 A0 00    addss xmm0, [00a0f70c]
...
D8 05 A0 F2 A0 00          fadd [g_flOne]
D8 8E F0 01 00 00          fmul [esi+0x1f0]
5E 59 C3                   pop esi; pop ecx; ret
```

---

## 3. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Early min path CF + offsets | **High** | Wrong override when attachment present |
| `+0xf5==0` AND-gate (not OR) | **High** | Fire early path in combat/air modes |
| Flag 0x1000 → −0.7 exclusive | **High** | Port PushDrive −0.3 or combine both bits |
| Flag 0x4000 → +0.2 | **High** | Port +0.5 |
| `(base+1)*+0x1f0` | **High** | Invent AbsoluteTopSpeed clamp here |
| DCT look-scale consumer role | **High CF** | Mis-place as thr cap |
| ECX identity across all callers | **Open / multi-type** | Wrong object’s `+0x1f0` / `+0x250` |
| Product field names | **Open** | Struct map errors |
| Runtime live values | **Open** | No Launcher |

---

## 4. Offset / formula attack checklist

| Item | Attack | Result |
|------|--------|--------|
| `param_1+0x250` | Wrong decimal? | **No** — asm `50 02 00 00` |
| `+0x10c` / `+0x110` | Swapped min operands? | **No** — load 110, comiss vs 10c; jbe → return 110 (≤) |
| `+0x1f0` vs `+0x1f4` | Confuse with `FUN_004c4e20` base? | **No** — this multiplies **`+0x1f0`**; callee returns **`+0x1f4`** |
| `+0xa8` vs `+0xb8` | Same component? | **Related chain, different offsets** — `+0xa8`→ptr then `+0xf5`; `+0xb8`→flags dword |
| 0.2 / 0.7 constants | Decompiler invent? | **No** — `read_memory` + `subss`/`addss` immediates |
| Both flags additive | Possible? | **No** — jmp after 0x1000 path |
| Same as PushDrive bonus | Shared DAT? | **No** — PushDrive uses `g_flOverheatCoolFrac` 0.3 / `DAT_009cd0d8` 0.5 |

---

## 5. Surviving contract for AutoCore

```
// aa_004c55e0 / FUN_004c55e0
// this: object with f32 +0x1f0, ptr +0x250, CVOG-style component chain
//       (+0xa8 → object with u8 +0xf5; +0xb8 → u32 flags)
float GetSpeedDerivedScale(Host* this) {
  auto* obj = *(Host**)( (char*)this + 0x250 );
  auto* comp = ComponentAt(this, /*slot*/ 0xa8);
  if (obj != nullptr && *(uint8_t*)((char*)comp + 0xf5) == 0) {
    float a = *(float*)((char*)obj + 0x110);
    float b = *(float*)((char*)obj + 0x10c);
    return (a <= b) ? a : b;                 // min
  }

  float base = GetBaseMaxSpeed(this);        // FUN_004c4e20 / aa_004c4e20
  uint32_t flags = *(uint32_t*)(ComponentBase(this) + 0xb8);
  if (flags & 0x1000) {
    base -= 0.7f;                            // DAT_00a0f710
  } else if (flags & 0x4000) {
    base += 0.2f;                            // DAT_00a0f70c
  }
  return (base + 1.0f) * *(float*)((char*)this + 0x1f0);
}

// DriveControlTick consumer (not this leaf):
//   if look_object != null:
//     scale = GetSpeedDerivedScale(EBX_object) * (reverse_held ? DAT_00a0f74c : 1.0)
//     look_vec *= scale; FUN_0091a5f0(local_d4, &look_vec)
```

**Port cautions:**

* Do **not** reuse PushDriveAxes ±0.3/0.5 table for this leaf.
* Do **not** skip the `+0xf5==0` half of the early gate.
* Do **not** pass vehicle entity blindly if the live call site binds a different host (DCT: **EBX**).

---

## 6. Residual risks (not blockers for dual presence)

1. Product naming of host / secondary object / flags.
2. Whether `+0x1f0` is “scale” vs “base speed” in design docs (machine: multiplier after `(base+1)`).
3. Interaction of early-path min with entity `+0x10c` meaning in PushDrive (same offset on **different** object when early path fires).
4. Runtime golden for flag combinations and zero-speed callers.

**Verdict:** **accept-with-gaps** — adversarial checks fail to break High CF on min path, flag priority, constants 0.7/0.2, and `(base+1)*+0x1f0`.
