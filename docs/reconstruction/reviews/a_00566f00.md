# Dual residual: `0x00566f00` CNDDoubleList_InsertTail_Inferred

| Field | Value |
|-------|-------|
| Stable ID | `aa_00566f00` |
| Address | `0x00566f00` |
| Owner | dual residual only (no WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 |
| Prior | scaffold only (`FUN_00566f00` / mistaken `Named_VOG_DEBUG_STOP`) |

## Wave focus

Partition map: **List insert on place** (post-`PlaceItemFootprint` / type-4 light path).

## Seal: role = insert at tail (append)

| Evidence | Location | Confidence |
|----------|----------|------------|
| New node always written to `list+0x20` (tail) | bytes `@ link` | **Confirmed** |
| Old tail `+8` (next) ← new; new `+0xc` (prev) ← old tail | bytes | **Confirmed** |
| Head `+0x1c` set only when empty | `CMP [ESI+0x1c],0` / conditional store | **Confirmed** |
| Count `+0x24`++ | `ADD [ESI+0x24],1` | **Confirmed** |
| Payload at node `+4` from stack arg | `MOV [EAX+4], [EBP+8]` | **Confirmed** |
| Same layout as CNDDoubleList remove / IterateNext | duals `aa_004e2600`, residual `a_005711c0` | **Confirmed** family |

**Not** push_front. **Not** cell stamp.

## Seal: place consumer ABI

| Claim | Evidence | Confidence |
|-------|----------|------------|
| Place post-stamp: `this = grid+0x2c` | `0x005717f0`: `8D 4F 2C; E8 → 00566f00` | **Confirmed** |
| Type-4 place early path same list | prior place dual `a_00571620` / A place | **High** |
| Payload = item (`ESI` in place) | place stack0 item; this unit stores stack0 | **High** |
| After insert: item `\|0x10` at `+0x17c` | place bytes immediately after call | **Confirmed** (place unit) |

## Seal: concurrency / errors

| Claim | Evidence | Confidence |
|-------|----------|------------|
| CS at `list+4` | Enter/Leave IAT with `EDI=ESI+4` | **Confirmed** |
| Reject if `*(u8*)(list+0x28)` (traversal lock) | `CMP BYTE [ESI+0x28],0` → throw | **Confirmed** |
| Assert string `VOG_DEBUG_STOP` only on that path | decompile + push string | **Confirmed** |
| HRESULT `0x80070005` lock / `0x8007000e` OOM | stack constants before `_CxxThrowException` | **Confirmed** |
| Success return **0** | `XOR EAX,EAX` | **Confirmed** |
| `RET 4` | epilogue `C2 04 00` | **Confirmed** |

## Callers (Ghidra; not fully re-typed)

| VA | Role note |
|----|-----------|
| `0x00571620` | `InventoryGrid_PlaceItemFootprint` |
| `0x00572730` | grid rebuild/repack walks list + re-places |
| `0x00564f60` | `CVOGSpawnPoint_CreateCreature` |
| `0x00564290` | `CVOGSpawnPoint_CreateTemplateVehicle` |
| `0x00563a30` / `0x00563a70` | related spawn/list helpers |

→ **Generic** list method; inventory is primary dual context, not sole host.

## Residual after this pass

| Item | Status |
|------|--------|
| Append CF + offsets | **Sealed Confirmed** |
| Place `grid+0x2c` this | **Sealed Confirmed** |
| Return 0 / RET 4 / throws | **Sealed Confirmed** |
| CNDDoubleList family layout | **Sealed High/Confirmed** |
| Product method name | **Open** (`_Inferred`) |
| Inventory list RTTI template arg | **Open** |
| vtbl+4 AllocateNode symbol | **Open** (role sealed) |
| Runtime head→tail dump after place | **Open** |
| Bit-exact / image diff | **Open** |

## Artifacts touched by residual owner

- Duals: `reviews/A_aa_00566f00_CNDDoubleList_InsertTail_Inferred.md`, `reviews/B_aa_00566f00_CNDDoubleList_InsertTail_Inferred.md`
- This scratch: `reviews/a_00566f00.md`
- Function record: `functions/aa_00566f00_CNDDoubleList_InsertTail_Inferred.md`
- Clean: `reconstructed-exact/CNDDoubleList_InsertTail_Inferred.cpp`
- Annotated append: `raw/aa_00566f00_FUN_00566f00.annotated.md`
- Raw: **not overwritten** (integrity rule); live decompile matches frozen body
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
