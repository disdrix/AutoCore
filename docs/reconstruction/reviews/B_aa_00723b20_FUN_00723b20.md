# Review B (skeptical / adversarial): `aa_00723b20` FUN_00723b20

| Field | Value |
|---|---|
| **Stable ID** | `aa_00723b20` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_00723b20_FUN_00723b20.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | Always stops all sounds | Enable gate; multi-match filters | **Falsified** — **selective** |
| 2 | Name-only always | Specific TFID mode when ids not both -1 | **Confirmed dual mode** |
| 3 | Returns success code | Always returns 0 | **Confirmed void-ish** |
| 4 | Is `Client_GetMissionCompleteAudioTable` | Distinct VA; different body | **Falsified name collision at call sites** |
| 5 | No enable gate | `cmp [this+0x258],0` | **Confirmed** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| thiscall + 5 stack args | **High** | Wrong stop wrapper |
| Enable `+0x258`, count `+0x210`, array `+0x31C` | **High** | Walk corruption |
| TFID match `+0x40/+0x44/+0x35` | **High** | Wrong instance stopped |
| Name at `+0x48` | **High** | Miss/false stop |
| Suffix / type-0x46 edge cases | **Medium** | Rare leaks |
| `00723ab0` exact args | **Medium** | Incomplete port |

---

## 3. Cross-check against raw + bytes

```
raw large body ≡ live batch_decompile.
entry: sub esp,0x10C; mov edx,ecx; cmp [edx+0x258],0; jz exit
mode split on (param2 & param3) == -1

≡ structure sealed. Parent: 004b8dc0 deactivate walks +0x1EC → this stop.
```

---

## 4. Surviving contract for AutoCore

```csharp
int AudioTable_StopMatching(
    AudioTable t,
    uint idLo, uint idHi, byte idByte,
    string? name, bool forceFlag)
{
    if (!t.Enabled /*+0x258*/) return 0;
    bool wildcard = idLo == 0xFFFFFFFFu && idHi == 0xFFFFFFFFu;
    for (int i = 0; i < t.Count /*+0x210*/; i++)
    {
        var e = t.Slots[i]; // array @ +0x31C
        if (e == null || !IsLive(e)) continue;
        if (wildcard)
        {
            if (name != null && NameMatch(e, name) && NameGate(e, forceFlag))
                StopInstance(e); // FUN_00723ab0
        }
        else if (e.IdLo == idLo && e.IdHi == idHi && e.IdByte == idByte)
        {
            // name-null / name / suffix branches — see raw
            MaybeStop(e, name);
        }
    }
    return 0;
}
```

---

## 5. Open questions

1. `FUN_00723ab0` full contract.
2. Suffix string at `DAT_00aa9268`.
3. Runtime verification.

**Verdict:** **accept**
