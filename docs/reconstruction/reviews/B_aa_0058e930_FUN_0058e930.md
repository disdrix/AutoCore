# Review B (skeptical / adversarial): `aa_0058e930` FUN_0058e930

| Field | Value |
|---|---|
| **Stable ID** | `aa_0058e930` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_0058e930_FUN_0058e930.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | Overwrites whole `+0x90E` byte | XOR deposit only bit2 (`and al,4`) | **Falsified** — **bit-local** |
| 2 | Always bumps counter | Needs `+0x90F&2` and non-null `+0x91C` | **Confirmed gated** |
| 3 | Receives `param_3` like 005bbb90 | Single stack byte only | **Confirmed one-arg** |
| 4 | Complex CF | Straight-line + one optional block | **Confirmed simple** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Bit2 of `+0x90E` | **High** | Wrong flag state |
| Counter path gates | **High** | Missed pulses |
| Offsets `+0x91C/+0x8C/+0xD0/+0xDC` | **High** | Wrong object mutate |
| Product names | **Low** | Naming only |

---

## 3. Cross-check against raw + bytes

```
raw bit idiom ≡ entry: flag*4 XOR-select into +0x90E bit2
optional: test +0x90F bit1; load +0x91c; inc +0xdc; store +0xd0

≡ A sealed. Parent chain: 004b8dc0 list +0x204.
```

---

## 4. Surviving contract for AutoCore

```csharp
void Object_OnFxHostActiveBit(Obj o, bool on)
{
    o.Flags_90E = (byte)((o.Flags_90E & ~0x04) | (on ? 0x04 : 0));
    if ((o.Flags_90F & 0x02) != 0 && o.Related_91C != null)
    {
        var baseObj = o.Related_91C.Field_8C;
        baseObj.Counter_DC++;
        baseObj.Flag_D0 = on;
    }
}
```

---

## 5. Open questions

1. Product classes for `this` / related.
2. Runtime verification.

**Verdict:** **accept**
