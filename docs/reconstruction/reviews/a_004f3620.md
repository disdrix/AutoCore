# Dual residual: `0x004f3620` VehicleEntity_SetHandbrake

| Field | Value |
|-------|-------|
| Stable ID | `aa_004f3620` |
| Address | `0x004f3620` |
| Owner | dual residual only (no WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 (strengthen pass) |
| Tools | Ghidra `decompile_function` / `batch_decompile` / `read_memory` / `get_xrefs_to` / `get_function_callers` only |
| Prior dual | A/B **accept-with-gaps** (2026-07-29 first residual); thin scaffold 2026-07-23 |

---

## Residual questions (entry)

1. Controller / consumer read of `entity+0x61c`
2. Handbrake vs sharp-turn semantics
3. Unit-test / static proof of **ungated** store under `0xC7`
4. Full writer inventory (bypass setters)
5. Non-0/1 value semantics
6. Exact body bytes / end VA (prior record said `~0x004f3626`)

---

## Seal: machine body (`read_memory` @ `0x004f3620`, len 16)

| Bytes (hex) | Meaning |
|-------------|---------|
| `8a 44 24 04` | `mov al, [esp+4]` — stack arg (`handbrakeOn`) |
| `88 81 1c 06 00 00` | `mov [ecx+0x0000061c], al` — store `this+0x61c` |
| `c2 04 00` | `ret 4` — pop one stack dword after thiscall |
| trailing `cc cc cc` | int3 padding (not body) |

**Body span (corrected):** `0x004f3620`–`0x004f362c` inclusive (**13** bytes). Exclusive end `0x004f362d`.  
Prior “~`0x004f3626`” was **wrong** (mid-instruction).

**Live decompile ≡ raw ≡ bytes:**

```c
void __thiscall VehicleEntity_SetHandbrake(int param_1, undefined1 param_2)
{
  *(undefined1 *)(param_1 + 0x61c) = param_2;
  return;
}
```

No branches, callees, globals, or `0xC7` walk. **Static High** for ungated store.

### Sibling contrast (same session `batch_decompile`)

| Setter | VA | Store | Gate |
|--------|-----|-------|------|
| `SetSteerInput` | `0x004f5620` | `+0x618` f32 | `wobj` null **or** `(wobj+0xb4)&0xC7==0` |
| `SetLongitudinalInput` | `0x004f5650` | `+0x614` f32 | same |
| **`SetHandbrake`** | **`0x004f3620`** | **`+0x61c` u8** | **none** |
| `SetFlag_109` | `0x004f3630` | `+0x109` u8 | none (hard-stop; different byte) |

---

## Seal: consumers of `+0x61c`

| Consumer | Behavior | Evidence | Confidence |
|----------|----------|----------|------------|
| `VehicleEntity_PushDriveAxesToController` `0x004fbc10` | Gate: `+0x101==0` && `+0x1a0!=0`. Hard-stop `+0x109≠0` → `ctrl+0x20=0`, **`ctrl+0x24=1`**, **return** (no `+0x61c` read). Else after thr path: **`ctrl+0x24 = entity+0x61c`**. | live decompile | **High** |
| `VehicleAction_calcWheelTorque` `0x00598040` | If `entity+0x61c≠0` **and** wheel index is rear (`i > rearSplit@…+0x4cc`): `torque *= DAT_00a0f298`. | live decompile | **High** |
| `DAT_00a0f298` | `read_memory` → `00 00 00 3f` = **0.5f** | `read_memory` | **High** |
| `hkDefaultBrake_update` | Framework pedal / wheel lock — **distinct**; not entity HB byte | brake-spec / prior dual | **High** |

**Conclusion:** `+0x61c` is **handbrake / sharp-assist flag** (bool-like, consumers ≠0). Same byte: (a) player Brake hold → parking-style HB; (b) NPC sharp when fast + large heading error. Not service-brake torque.

---

## Seal: callers of **SetHandbrake** (live xrefs 2026-07-29 strengthen)

**10 caller functions** / **18** `UNCONDITIONAL_CALL` sites (`get_function_callers` + `get_xrefs_to`):

| Function | VA | Call sites | Observed role |
|----------|-----|------------|---------------|
| `Client_Input_DriveControlTick` | `0x009223b0` | `0x009224d9`, `0x009228ee`, `0x00922979`, `0x00922dd7`, `0x0092358e` → **0**; `0x009235b9` → **1** | Primary local input (clear ×5, Brake/bounce ×1) |
| `Client_Input_PollBoundActions` | `0x00925d60` | `0x00927d58` | Bound-action HB engage **1** |
| `FUN_005d0d60` | `0x005d0d60` | `0x005d0f4c` | AI/spawn HB **1** |
| `FUN_005d73a0` | `0x005d73a0` | `0x005d73c5` | AI stop pack thr0/steer0/HB1 + Push |
| `FUN_00915670` | `0x00915670` | `0x00915698` | Client vehicle HB pulse + Push |
| `FUN_0092f090` | `0x0092f090` | `0x0092f0cc` | Stop axes thr0/steer0/HB1 |
| `FUN_009373e0` | `0x009373e0` | `0x0093745b` | UI/modal stop pack |
| `FUN_00938670` | `0x00938670` | `0x00938698` | Store/interaction stop (HB1 + SetFlag_109) |
| `FUN_0093a5c0` | `0x0093a5c0` | `0x0093a5f8` | Focus-loss HB |
| `FUN_00946c00` | `0x00946c00` | `0x00946d67`, `0x00946da7`, `0x00946de7`, `0x00946eb8` | Dialog/mission case arms (×4) |

**Correction sealed:** `FUN_004fc650` / `Drive_CVOGVehicle_MoveToTarget3DPoint` is **not** a caller (absent from xrefs).

---

## Seal: writers that **bypass** SetHandbrake

| Writer | How | Live decompile note | Confidence |
|--------|-----|---------------------|------------|
| `Drive_CVOGVehicle_MoveToTarget3DPoint` `0x004fc650` | **Direct** `*(u8*)(entity+0x61c)` | Drive body: `+0x61c = 0` if `speed <= DAT_00aaa7a4` **or** `\|headingErr\| <= DAT_00a0f710`; else `1`. Arrival arm: forces `+0x61c = 1` then thr0 + Push. | **High** |
| `VehicleEntity_SetDriveAxes` `0x004fbec0` | Bundled thr/steer/**HB** then Push | `*(+0x614)=thr; *(+0x618)=steer; *(+0x61c)=hb; PushDriveAxes()` | **High** |

Port note: AutoCore NPC sharp/handbrake may set ghost `VehicleFlags.Handbreak` (wire bit0 → client `+0x61c`) without a C# “SetHandbrake” twin — same **byte contract**.

---

## Non-0/1 values

| Claim | Status |
|-------|--------|
| Documented writers use **0** or **1** only | **High** (DriveControlTick, MoveToTarget, AI packs, SetDriveAxes u8) |
| Consumers use **≠0** test (not `==1`) | **High** (calcWheelTorque; Push copies full byte) |
| Other values if raw store | **Open / low risk** — treat as engaged if non-zero |

---

## Residual after strengthen

| Item | Status |
|------|--------|
| Body = ungated `+0x61c` store | **Sealed High** (bytes + decompile ≡ raw) |
| Body span 13 B / end `0x004f362c` | **Sealed High** (`read_memory`) |
| No 0xC7 gate | **Sealed High** (body + sibling contrast) |
| PushDriveAxes / calcWheelTorque / 0.5f | **Sealed High** |
| Dual semantics (player HB + NPC sharp) | **Sealed High** |
| Caller inventory 10 fn / 18 sites | **Sealed High** |
| Bypass writers (MoveToTarget, SetDriveAxes) | **Sealed High** |
| Original PDB symbol | Open |
| Runtime / bit-exact / image diff | Open (matrix policy) |
| English names of every non-input caller | Probable roles only |
| Exhaustive scan of **all** direct `+0x61c` stores | Optional residual (known bypasses sealed) |

---

## Artifacts (this owner)

- Scratch: `docs/reconstruction/reviews/a_004f3620.md`
- Duals: `A_aa_004f3620_VehicleEntity_SetHandbrake.md`, `B_aa_004f3620_VehicleEntity_SetHandbrake.md`
- Function record: `functions/aa_004f3620_VehicleEntity_SetHandbrake.md`
- Clean: `reconstructed-exact/VehicleEntity_SetHandbrake.cpp`
- Annotated: `raw/aa_004f3620_VehicleEntity_SetHandbrake.annotated.md`
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / Launcher
