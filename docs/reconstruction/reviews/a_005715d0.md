# Dual residual: `0x005715d0` InventoryGrid_CanPlaceItem

| Field | Value |
|-------|-------|
| Stable ID | `aa_005715d0` |
| Address | `0x005715d0`–`0x0057161d` |
| Canonical name | `InventoryGrid_CanPlaceItem` |
| Owner | dual residual WQ8R-C (OWN-ONLY) |
| Date | 2026-08-04 |
| Prior | scaffold only (`FUN_005715d0` raw/annotated/clean 2026-07-23) |

## Residual questions (this pass)

1. Seal thin InvSize → CanPlace wrapper
2. Seal thiscall + 4 stack / `RET 0x10`
3. Catalog callers (equip / place probe)

## Live evidence this pass

| Source | Result |
|--------|--------|
| Ghidra `batch_decompile` | ≡ raw leaf body |
| Ghidra `get_function_callers` | `FUN_008012f0`, `FUN_00941b20`, `FUN_00952bc0`, `FUN_00955010` |
| Ghidra `get_xrefs_to` | 18 call sites |
| `read_memory` full body | null gate; InvSize loads; `CALL 00570840`; `RET 0x10` |
| CanPlace residual | InvSize footprint probe role confirmed |
| Call-site sample `008012f0` | probe current XY then FindFree/Place |

**Not performed:** `disassemble_bytes`, Launcher, bit-exact.

---

## Seal summary

```text
// __thiscall InventoryGrid_CanPlaceItem(this, item, originX, originY, conflictOut)
// RET 0x10

if (item == NULL) return 0
sizeX = u8(*(*(item+0xa8)+0x3c)+0x406)
sizeY = u8(*(*(item+0xa8)+0x3c)+0x407)
return CanPlace(this, sizeX, sizeY, originX, originY, conflictOut)
```

### Gaps

1. Product/PDB English (name Probable).
2. Runtime observation of conflictOut non-null path from this wrapper (callers typically pass 0).
