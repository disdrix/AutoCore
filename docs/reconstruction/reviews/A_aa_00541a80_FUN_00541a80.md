# Review A (reconstruction fidelity): `aa_00541a80` FUN_00541a80

| Field | Value |
|---|---|
| **Stable ID** | `aa_00541a80` |
| **VA** | `0x00541a80` |
| **Canonical name** | `FUN_00541a80` |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Independent reconstruction review |
| **Counterpart** | `reviews/B_aa_00541a80_FUN_00541a80.md` |
| **System** | client — ensure/get CNDHash table pointer at `this+0xF1C` |
| **Verdict** | **accept** — lazy init + return pointer **sealed** |

---

## 1. Purpose

**Ensure the hash table at `this+0xF1C` is populated**, then return that pointer.

1. Load `table = *(this+0xF1C)`.
2. If **`*(table+0xC) < 1`** (empty / not ready): call **`FUN_00541950()`** (populate/rebuild).
3. Return **`*(this+0xF1C)`**.

Used by `FUN_004b7e50` as the table argument path into **`CNDHash_LookupByKey`** for the type-suppress gate.

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Raw | `docs/reconstruction/raw/aa_00541a80_FUN_00541a80.md` |
| Annotated | `docs/reconstruction/raw/aa_00541a80_FUN_00541a80.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/FUN_00541a80.cpp` |
| Function record | `docs/reconstruction/functions/aa_00541a80_FUN_00541a80.md` |
| Live decompile | Ghidra `batch_decompile` `0x00541a80` — ≡ raw |
| Live bytes | Ghidra `read_memory` `0x00541a80` length 32 |
| Callees | `FUN_00541950` |
| Callers | `FUN_004b7e50` (suppress gate), other hash consumers |

---

## 3. Sealed residuals

### 3.1 ABI — **SEALED**

| Fact | Evidence |
|---|---|
| Convention | **`__fastcall` / thiscall**: `this` in **ECX** (`mov esi, ecx`) |
| Returns | **pointer** at `this+0xF1C` |
| Side call | `FUN_00541950` when count `@ table+0xC < 1` |

```c
void* __fastcall FUN_00541a80(void *this);
```

### 3.2 Algorithm — **SEALED**

```
table = *(this+0xF1C)
if (*(int*)(table+0xC) < 1)
  FUN_00541950()   // rebuild/populate (this via ecx convention)
return *(this+0xF1C)
```

### 3.3 Layout — **SEALED**

| Off | Role |
|---|---|
| `this+0xF1C` | **hash table object pointer** |
| `table+0xC` | **entry count / ready int**; `<1` → rebuild |

### 3.4 Entry machine bytes — **SEALED**

```
56                      push esi
8B F1                   mov  esi, ecx
8B 86 1C 0F 00 00       mov  eax, [esi+0xF1C]
83 78 0C 00             cmp  dword ptr [eax+0xC], 0
7F 05                   jg   → skip init
E8 BC FE FF FF          call FUN_00541950
8B 86 1C 0F 00 00       mov  eax, [esi+0xF1C]
5E                      pop  esi
C3                      ret
```

Hex:  
`568bf18b861c0f000083780c007f05e8bcfeffff8b861c0f00005ec3`

---

## 4. Control flow: clean ≡ raw ≡ live

| Stage | Match |
|---|---|
| load `+0xF1C` | Yes |
| count `<1` → rebuild | Yes |
| return table ptr | Yes |

Note: decompile uses `< 1`; bytes show `cmp [eax+0xC],0` / `jg` — equivalent for integer count (`<=0` rebuild).

---

## 5. Gaps / open

1. Product owner type of `this` (global manager vs world).
2. Full contract of `FUN_00541950`.
3. Runtime verification not run.

**Verdict:** **accept**
