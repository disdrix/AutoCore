# Review A (reconstruction fidelity): `aa_008335a0` FUN_008335a0

| Field | Value |
|---|---|
| **Stable ID** | `aa_008335a0` |
| **VA** | `0x008335a0`–`0x0083363b` |
| **Canonical name** | `FUN_008335a0` |
| **Ghidra name** | `FUN_008335a0` |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Reconstruction fidelity (OWN-ONLY W27-A) |
| **Counterpart** | `reviews/B_aa_008335a0_FUN_008335a0.md` |
| **System** | object / orientation step (client) |
| **Evidence pass** | Live Ghidra `decompile_function` + `read_memory` + `analyze_function_complete` + `get_assembly_context` |
| **Verdict** | **accept** |

---

## 1. Purpose

Orientation-step worker on a host object (EBX):

1. Copy current quat `@+0x590` to stack.
2. If `Math_QuatNearlyEqual_DoubleCover(stack, @+0x5e0, 0.002, 0.002)` → clear `@+0x5dc` and return.
3. Else slerp stack toward `@+0x5e0` with **t = 0.25**, write orient via `FUN_0040d1a0(ECX=host+0x510)`, run `FUN_00833490`, optional `[*+0x508]+0x44`, then `FUN_00833390`.

---

## 2. Inspected artifacts / evidence

| Artifact | Path / tool |
|---|---|
| Raw | `docs/reconstruction/raw/aa_008335a0_FUN_008335a0.md` (+ 2026-07-29 W27-A append) |
| Annotated | `docs/reconstruction/raw/aa_008335a0_FUN_008335a0.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/FUN_008335a0.cpp` |
| Function record | `docs/reconstruction/functions/aa_008335a0_FUN_008335a0.md` |
| Sealed callee | `Math_QuatNearlyEqual_DoubleCover` (W26-A) |
| Live | decompile ≡ raw; full 156 B hex; caller gate at `0x00833a3d` |

---

## 3. Signature (sealed)

```c
// EBX = host; no stack formals; void; plain RET
void FUN_008335a0(void);
```

| Formal | Source | Conf |
|---|---|---|
| host | EBX (caller `MOV EBX,ECX`) | **High** |
| return | void | **High** |
| cleanup | `ADD ESP,0x10; RET` | **High** |

---

## 4. Control flow (clean ≡ raw ≡ bytes)

```
copy quat +0x590 → stack
if QuatNearlyEqual(stack, +0x5e0, 0.002, 0.002):
  +0x5dc = 0; return
slerp(stack ← +0x5e0, t=0.25)
FUN_0040d1a0(host+0x510, stack)
FUN_00833490()
if +0x508: vfunc+0x44
FUN_00833390()
return
```

| Stage | Match | Conf |
|---|---|---|
| Quat copy + double-cover gate | **Yes** | **High** |
| Flag clear on true | **Yes** | **High** |
| Slerp t=0.25 | **Yes** (`0x3E800000`) | **High** |
| Orient write via +0x510 thiscall | **Yes** | **High** |
| Sibling callees order | **Yes** | **High** |

---

## 5. Machine bytes (`read_memory`)

Full 156 B body hex in raw append. Key immediates: `0x3B03126F` ×2 (0.002f), `0x3E800000` (0.25f), offsets `0x590` / `0x5e0` / `0x5dc` / `0x510` / `0x508`.

---

## 6. Gaps

- Product English / class name — residual; keep Ghidra `FUN_008335a0`.
- Full semantics of siblings `FUN_00833490` / `FUN_00833390` — not owned.
- Runtime bit-exact slerp (acos CW) — open.

---

## 7. Verdict

CF, EBX ABI, sealed quat early-out, slerp+write path, and single caller gate are sealed. → **accept**.
