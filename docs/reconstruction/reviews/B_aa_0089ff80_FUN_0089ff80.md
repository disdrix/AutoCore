# Review B (skeptical / adversarial): `aa_0089ff80` FUN_0089ff80

| Field | Value |
|---|---|
| **Stable ID** | `aa_0089ff80` |
| **VA** | `0x0089ff80` |
| **Canonical name** | `FUN_0089ff80` (**leave-FUN**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Skeptical / adversarial (Path B) |
| **Counterpart** | `reviews/A_aa_0089ff80_FUN_0089ff80.md` |
| **Live tools** | Independent `force_decompile` + callers |
| **Verdict** | **leave-FUN** |

---

## 1. Claims under attack

| # | Claim | Attack result |
|---|---|---|
| 1 | Packs / sends cast packet | **Falsified** — parent packs `0x2030`; this is post-send UI only |
| 2 | Always runs after QuickBar activate | **Falsified** — gated by `DAT_00d1b8f0` vtbl+0x3d8 at caller |
| 3 | Modes are skill vs item exclusively | **Unsupported** — only AL 0/1 + two host slots sealed |
| 4 | `+0x4fe` is cooldown remaining | **Overstated** — single byte store 0; English open |
| 5 | Jumptable failure changes CF | **Falsified** — warning only; single indirection sealed |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| AL 0/1 dispatch | **High** | Wrong button clears |
| Host ESI +0x522 gate | **High** | Spurious clears |
| No network | **High** | Wrong layer port |
| Product name | **None** | — |
| Slot object identity | **Tentative** | Mis-label UI type |

---

## 3. Caller challenge

| Caller | Must not claim |
|---|---|
| QuickBarActivateSkillSlot | That AL is always 0 at call — **register state at call site still open** without call-site asm |
| Fire primary/secondary | Same AL provenance residual |

---

## 4. CF challenge of Review A

- Dual identity: **agree**
- leave-FUN: **agree**
- Residual: AL provenance at each caller — **add**

---

## 5. Surviving contract

```
FUN_0089ff80(host /*ESI*/, mode /*AL*/):
  if !host.flag(+0x522): return
  obj = (mode==0) ? host+0x66c : (mode==1) ? host+0x670 : null
  if !obj: return
  if !obj.vtbl+0xd0(): return
  obj.byte(+0x4fe) = 0
  tail obj.vtbl+0x448()
```

**Verdict:** **leave-FUN.** Accept A. AL caller provenance open.
