# Review B (skeptical / adversarial): `aa_005085b0` FUN_005085b0 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_005085b0` |
| **VA** | `0x005085b0` |
| **Canonical name** | `FUN_005085b0` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Skeptical / adversarial (Path B) |
| **Counterpart** | `reviews/A_aa_005085b0_FUN_005085b0.md` |
| **System** | shared list / heartbeat-owner + inventory |
| **Live tools** | Independent `force_decompile` + `read_memory` + caller decompiles |
| **Verdict** | **leave-FUN** — accept A's CF; reject product / CVOGHBList rename |

---

## 1. Claims under attack

| # | Claim | Attack result |
|---|---|---|
| 1 | This is `CVOGHBList` remove (pending/active arrays) | **Falsified** — walks intrusive nodes at `+0x1c`, not `+0x28` pointer buffer; `CVOGHBList_Enqueue` layout incompatible |
| 2 | Always called with HB as `this` | **Falsified** — AttachOwnerObject machine: **ECX = listMgr** `*(owner+0xB0)`, stack = HB; dtor path same shape |
| 3 | Busy `+0x28` means “list empty” | **Falsified** — `List_TraversalLock` sets `+0x28=1` under CS; remove refuses while traversal held |
| 4 | Returns HRESULT / void | **Falsified** — bool-as-uint 0/1; early AL=0; no `0x8007…` |
| 5 | Does not free the node | **Falsified** — `(*vtbl)(1)` scalar delete after unlink |
| 6 | Safe to remove during TraversalLock iteration | **Falsified** — early return 0 when `+0x28 != 0` |
| 7 | Single-domain “HB only” API | **Falsified** — `FUN_00571b80` inventory-style path also calls it with item keys |
| 8 | Clean scaffold invents extra callees | Scaffold auto-note listed self; real callees = CS + vcall only |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Unlink math (prev/next/head/tail) | **High** | Double-free / list corruption |
| Key at `node+4`, next `+8`, prev `+0xC` | **High** | Wrong search / wrong twin insert |
| Count at `+0x24` | **High** | Stale counts vs iterate |
| Busy gate `+0x28` | **High** | Race with TraversalLock walkers |
| Multi-caller (not Attach-only) | **High** | Over-narrow name |
| Product method title | **None** | Registry pollution |
| Equivalence to CVOGHBList | **Rejected** | Wrong server port |

---

## 3. Cross-check vs twin insert `FUN_00508490`

Insert (sealed from decompile):

- Throws if `this+0x28` busy (`VOG_DEBUG_STOP` + `0x80070005`).
- Alloc via `(*this->vtbl)(+4)(0)`; OOM → `0x8007000e`.
- Links new node at **tail** (`+0x20`); first node also sets head `+0x1c`.
- Stores key at `node+4`; count++.

Remove is the structural inverse under the same CS and same slots. **Agree High** with A.

Difference: insert **throws** on busy; remove **returns 0**. Do not unify error policy in ports.

---

## 4. Decompiler pitfalls (must survive port)

1. **Collapsed thiscall** — prints `FUN_005085b0(key)` omitting list ECX.
2. **AttachOwnerObject plate** historically implied HB this for callee — machine `mov ecx, listMgr` wins (already sealed in `A_aa_005083b0`).
3. **Prototype `undefined FUN_005085b0(void)`** surface warning — ignore; body is `__thiscall` + 1 stack arg + `ret 4`.

---

## 5. What is safe to claim

| Safe | Unsafe |
|---|---|
| Remove-by-key under CS | Product class name |
| Bool 0/1 return | HRESULT family |
| Shared list layout with insert / TraversalLock / IterateNext | “CVOGHBList_*” rename |
| Role: detach membership node for key | Exclusive HB-only semantics |
| Leave `FUN_*` | Promote `List_RemoveByKey` to registry without string/RTTI |

---

## 6. CF challenge of Review A

- Dual force/non-force identity: **agree**
- Offsets head/tail/count/busy: **agree High**
- Node key/next/prev: **agree High**
- Early busy return without LeaveCS: **agree** (never Entered)
- leave-FUN: **agree**
- Reject CVOGHBList identity: **agree**

---

## 7. Open questions (narrow)

1. RTTI / constructor of list manager at `owner+0xB0` vs inventory grid list object — same vtbl?
2. Node alloc size / vtbl identity from insert `vtbl+4`.
3. Live race: IterateNext while another thread Remove — depends on TryEnter path in `List_IterateNext`.

**Verdict:** **leave-FUN**. Accept A's sealed CF. Reopen rename only on string, RTTI, or product plate — not on AttachOwnerObject role alone.
