# Review B (skeptical / adversarial): `aa_00415c00` FUN_00415c00 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00415c00` |
| **VA** | `0x00415c00` |
| **Canonical name** | `FUN_00415c00` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Skeptical / adversarial (Path B) |
| **Counterpart** | `reviews/A_aa_00415c00_FUN_00415c00.md` |
| **System** | shared list (inventory + other) |
| **Live tools** | Independent `force_decompile` + callee decompiles + callers |
| **Verdict** | **leave-FUN** — accept A's CF; reject overclaims |

---

## 1. Claims under attack

| # | Claim | Attack result |
|---|---|---|
| 1 | Removes the found key | **Falsified** — read-only walk; remove is `FUN_005085b0` |
| 2 | Returns full IterateNext HRESULT (`0x80070005`) | **Falsified** — AL bool only; any IterateNext non-0 → treat as miss (uVar2 stays 0) |
| 3 | Skips unlock on not-found | **Falsified** — shared epilogue clears `+0x28` + LeaveCS whenever flag set |
| 4 | Nested TraversalLock is always fatal | **Overstated** — lock helper on already-locked logs debug stack and returns **without** setting flag again; Contains still walks if prior lock held by same thread |
| 5 | This **is** `List_IterateNext` | **Falsified** — compose of Lock + loop IterateNext + compare + unlock |
| 6 | Exclusive inventory-equip helper | **Falsified** — 3 callers including thin wrapper + UI path `0x0083aa50` |
| 7 | Decompiler `FUN_00415c00(param_2)` means free function | **Falsified** — `mov esi,ecx`; thiscall list this |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Membership bool semantics | **High** | Callers gate remove/UI on wrong presence |
| Unlock always if flag set | **High** | CS leak → deadlock |
| Key compare uses IterateNext outKey (`node+4`) | **High** | False miss |
| Does not modify links/count | **High** | Accidental mutation ports |
| Product name | **None** | Registry noise |
| IterateNext fail vs empty end distinguishable to caller | **None** — both → 0 | May hide lock fail as “not present” |

---

## 3. Callee contract cross-check

### `List_TraversalLock` (`0x004294f0`) — agree High

```
if (this+0x28 already set):
  debug VOG_DEBUG_STOP + "List Error! TraversalLock Call Stack..."
  return  // does NOT EnterCS again
EnterCS(this+4)
*(this+0x28) = 1
```

### `List_IterateNext` (`0x004022a0`) — agree High

Under TraversalLock (`+0x28`):
- TryEnter CS; if fail → `0x80070005`.
- cursor 0 → seed from head `+0x1c`, outKey = head->key (`+4`); return head==0 ? 1 : 0.
- else advance cursor = node->next (`+8`); return 1 if end.

Contains collapses **all** non-zero IterateNext results into “stop, not found” — including rare `0x80070005`. **Port risk:** presence false-negative under lock contention. Documented residual, not CF error in A.

---

## 4. Caller challenge

| Caller | Must not claim |
|---|---|
| `FUN_00571b60` | That Contains is only this wrapper |
| `FUN_00571b80` | That Contains **is** the extract; extract also calls remove `FUN_005085b0` after other item math |
| `FUN_0083aa50` | Inventory-only; client multi-slot presence |

---

## 5. What is safe to claim

| Safe | Unsafe |
|---|---|
| Bool Contains(list, key) under TraversalLock | Product method name |
| Unlocks if `+0x28` set | That IterateNext errors surface to caller |
| Uses registry helpers Lock + IterateNext | That this VA needs its own registry row now |
| leave-FUN | Equip-packet-only naming |

---

## 6. CF challenge of Review A

- Dual force/non-force identity: **agree**
- Call order Lock → Iterate loop → unlock: **agree**
- Byte return 0/1: **agree**
- leave-FUN: **agree**
- Residual on IterateNext fail→0: **add** as explicit G2 (A already notes)

---

## 7. Surviving contract for AutoCore

```
contains = List_Contains(list, key):  // FUN_00415c00 — leave FUN_*
  TraversalLock(list)
  for each keyK in IterateNext(list):
    if keyK == key: found=1; break
  if list.traversalFlag: clear; LeaveCS
  return found   // 0 also on iterate error / empty
```

Do not implement as unlocked linear walk. Do not merge with `FUN_005085b0` remove.

**Verdict:** **leave-FUN**. Accept A. Reopen rename only with string/RTTI or exclusive product plate.
