# Dual residual: `0x004f5620` VehicleEntity_SetSteerInput

| Field | Value |
|-------|-------|
| Stable ID | `aa_004f5620` |
| Address | `0x004f5620` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior dual | A/B **accept-with-gaps** (2026-07-23 thin scaffold); skeptical full package **needs-more-evidence** |

## Residual focus

**Dual residual gate chain → store `entity+0x618`.** Strengthen instruction identity, sibling parity, MoveToTarget chain/bit identity, callers, bypass writers, and consumers. Do **not** invent UF-001 / UF-002 resolutions (`wobj` type / `0xC7` per-bit producers).

## Live re-decompile (Ghidra 2026-07-29 residual)

```c
void __thiscall VehicleEntity_SetSteerInput(int param_1, undefined4 param_2)
{
  int iVar1;

  iVar1 = *(int *)(*(int *)(*(int *)(param_1 + 4) + 4) + 0xb0 + param_1);
  if ((iVar1 == 0) || ((*(byte *)(iVar1 + 0xb4) & 199) == 0)) {
    *(undefined4 *)(param_1 + 0x618) = param_2;
  }
  return;
}
```

**≡ frozen raw** (`raw/aa_004f5620_*`). Live ≡ 2026-07-23 capture.

## Seal: instruction-level body (`read_memory` @ `0x004f5620`, 48 B)

Hex (first 43 code bytes + padding):

```
8b 41 04                mov eax, [ecx+0x4]              ; p0
8b 50 04                mov edx, [eax+0x4]              ; p1
8b 84 0a b0 00 00 00    mov eax, [edx+ecx+0xb0]         ; wobj = *(p1+this+0xb0)
85 c0                   test eax, eax
74 09                   jz  STORE                       ; null wobj → allow
f6 80 b4 00 00 00 c7    test byte ptr [eax+0xb4], 0xC7
75 0e                   jnz  RET                        ; any 0xC7 bit → suppress
f3 0f 10 44 24 04       movss xmm0, [esp+4]             ; stack f32 arg
f3 0f 11 81 18 06 00 00 movss [ecx+0x618], xmm0         ; ONLY store
c2 04 00                ret 4                           ; thiscall stack cleanup
```

| Item | Value | Confidence |
|------|-------|:----------:|
| Body span | `0x004f5620` – `0x004f564a` inclusive (43 B / `0x2b`) | **High** |
| Mask immediate | **`0xC7`** at `TEST byte [wobj+0xb4], imm8` | **High** (byte immediate; not decompiler-only) |
| Store offset | **`0x618`** (`18 06 00 00` LE) | **High** |
| Convention | MSVC `__thiscall` + `ret 4` (1 stack arg) | **High** |
| Intermediate nulls | `p0`/`p1` **unchecked** (fault); only final `wobj` null is allow-path | **High** |
| FP ops | Load/store only (`movss`); no clamp/math | **High** |

**Closes residual R1** from prior skeptical review (instruction immediate / body size).

## Seal: sibling gate identity (`SetLongitudinalInput` `0x004f5650`)

`read_memory` @ `0x004f5650` — **byte-identical** gate path; only store offset differs:

| Function | Store | Imm LE |
|----------|-------|--------|
| SetSteerInput | `[ecx+0x618]` | `18 06 00 00` |
| SetLongitudinalInput | `[ecx+0x614]` | `14 06 00 00` |

Same chain, same `test imm8 0xC7`, same `ret 4`. **High**.

## Seal: MoveToTarget inline gate = same chain + same mask (`0x004fc650`)

Live decompile of proportional path:

```text
iVar4 = *( *(*(param_1+4)+4) + 0xb0 + param_1 );   // identical chain
if (iVar4 == 0 ||
    ((flags&2)==0 && (flags&4)==0 && (flags&1)==0 &&
     (flags&0x80)==0 && (flags&0x40)==0)) {
  *(float*)(param_1 + 0x618) = fVar7;               // inline store
}
```

| Claim | Status |
|-------|--------|
| Pointer chain parenthesization matches setter | **Sealed High** (same Ghidra expression) |
| Bit set `{0x01,0x02,0x04,0x40,0x80}` ≡ mask `0xC7` | **Sealed High** (individual tests, same bits) |
| Deadband branch calls `VehicleEntity_SetSteerInput` | **Sealed High** — callee still applies gate |
| Stale “deadband bypasses gate” | **Falsified** (callee applies gate) |

## Seal: callers of **SetSteerInput** (live xrefs 2026-07-29)

| Function | VA | Role (working) |
|----------|-----|----------------|
| `Client_Input_DriveControlTick` | `0x009223b0` | Local steer keys / soft / clear |
| `FUN_004fc650` MoveToTarget3DPoint | `0x004fc650` | Deadband straighten / reverse-align spin |
| `FUN_005d73a0` | `0x005d73a0` | Stop pack (thr0/steer0/HB1 + Push) |
| `FUN_00636ba0` | `0x00636ba0` | (xref sealed; English name open) |
| `FUN_0092f090` | `0x0092f090` | Stop axes pack |
| `FUN_009373e0` | `0x009373e0` | UI/modal stop pack |

**Six callers total** — matches prior sample; no new xrefs this pass.

## Seal: writers of `+0x618` that **bypass** SetSteerInput

| Writer | How | Gate? | Confidence |
|--------|-----|-------|:----------:|
| MoveToTarget proportional | Direct `*(f32*)(ent+0x618)` | **Yes** — inline same `0xC7` chain | **High** |
| `VehicleEntity_SetDriveAxes` `0x004fbec0` | Bundled thr/steer/HB then Push | **No** | **High** |
| `Vehicle_setDrivingInputs` `0x00504c70` | Network/local axes then Push | **No** | **High** |

Port note: network / SetDriveAxes can force steer even when setter gate would suppress. NPC proportional reimplements the gate; deadband uses the setter.

Contrast (out of body, sealed for system): MoveToTarget writes **`+0x614` longitudinal directly without** the `0xC7` gate.

## Seal: consumers of `+0x618` (not this body)

| Consumer | Behavior | Confidence |
|----------|----------|------------|
| `VehicleAction_applyAction` `0x00598650` | Stage-1 ramp: `VA+0x24` toward `entity+0x618` → driverInput / wheelsDesc | **High** (verified applyAction / steer feed) |
| `VehicleEntity_PushDriveAxesToController` `0x004fbc10` | **Does not read or copy** `+0x618` | **High** (push dual residual) |
| `hkDefaultSteering_update` `0x0064f840` | Downstream of DI after curve — **not** a direct entity load | **High** (steer feed note) |

## Sibling contrast (axis family)

| Setter | Store | Gate |
|--------|-------|------|
| **`SetSteerInput` `0x004f5620`** | **`+0x618` f32** | **`wobj` null OR `flags&0xC7==0`** |
| `SetLongitudinalInput` `0x004f5650` | `+0x614` f32 | same |
| `SetHandbrake` `0x004f3620` | `+0x61c` u8 | **none** |

## Still open (not invented)

| # | Gap | Blocks body CF seal? |
|---|-----|:--------------------:|
| G1 | `wobj` C++ type / full layout (UF-class naming) | No |
| G2 | Producers / clear paths for `wobj+0xb4` bits in `0xC7` | No |
| G3 | Per-bit English semantics | No |
| G4 | Runtime / CE: force bits → observe suppress | No (matrix policy) |
| G5 | Soft L/R polarity ownership in DriveControlTick plates | No (caller, not body) |
| G6 | Exhaustive scan of every `+0x618` store beyond table above | Mild for system map |

**Body CF dual residual: sealed High.** Full “lock story” / UF bit producers: still open.
