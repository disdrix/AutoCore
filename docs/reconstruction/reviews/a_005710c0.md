# Residual scratch: `aa_005710c0` InventoryGrid_FindItemByCbid

| Field | Value |
|---|---|
| **VA** | `0x005710c0` |
| **Date** | `2026-07-29` |
| **Tools** | Ghidra MCP `decompile_function`, `read_memory` (256 B @ entry), `batch_decompile` callees/callers |
| **Not used** | `disassemble_bytes`, Launcher, live debug |

## Live decompile ≡ raw 2026-07-23

Body bit-identical to `raw/aa_005710c0_FUN_005710c0.md`. No decompiler drift.

## ASM seals (read_memory)

```
005710c0  83 EC 08              sub  esp, 8
005710c3  53 55 56              push ebx/ebp/esi
005710c6  8D 69 2C              lea  ebp, [ecx+0x2C]   ; list subobject
005710c9  57                    push edi
005710ca  8B CD                 mov  ecx, ebp          ; TraversalLock this
...
005710d7  E8 ..                 call FUN_004294f0
...
005710f7  8B 87 A8 00 00 00     mov  eax, [edi+0xA8]   ; clonebase
005710fd  8B 54 24 1C           mov  edx, [esp+..]     ; cbid arg
00571101  39 50 34              cmp  [eax+0x34], edx   ; CBID match
...
0057110e  8B 8F 7C 01 00 00     mov  ecx, [edi+0x17C]  ; flags
00571114  C1 E9 13              shr  ecx, 0x13
00571117  F6 C1 01              test cl, 1             ; Broken bit19
...
0057111c  83 78 38 04           cmp  dword [eax+0x38], 4
00571120  75 07                 jnz  not_type4
00571122  BE 3F 42 0F 00        mov  esi, 0x000F423F   ; 999999
...
0057112b  8B 50 3C              mov  edx, [eax+0x3C]
0057112e  66 8B 82 BA 04 00 00  mov  ax, [edx+0x4BA]
...
0057113f  BE C8 00 00 00        mov  esi, 200          ; default max
...
0057114b  FF 90 5C 02 00 00     call [eax+0x25C]       ; qty vfunc
00571151  0F B7 CB              movzx ecx, bx          ; addQty u16
00571154  0F B7 D6              movzx edx, si          ; max & 0xFFFF
00571157  03 C1                 add  eax, ecx
00571159  3B C2                 cmp  eax, edx
0057115b  7E 35                 jle  accept
...
00571192  C2 0C 00              ret  0x0C              ; thiscall, 12 B stack
```

### SEALED formulas

```
this = InventoryGrid*  (ECX)
list = this + 0x2C
lockFlag = this + 0x54   (= list + 0x28)
cs       = this + 0x30   (= list + 0x04)

match:  item[+0xA8] → clonebase; *(clonebase+0x34) == cbid

if skipStackGate (param_3 != 0):
  return first match (Broken ignored)

else:
  if Broken (item[+0x17C] bit 19): skip
  maxStack =
    clonebase[+0x38]==4 ? 999999 : ( *( *(clonebase+0x3c)+0x4ba ) || 200 )
  effectiveMax = maxStack & 0xFFFF     // type-4 → 16959 (0x423F)
  if qty(vtbl+0x25c) + (u16)addQty <= (i32)effectiveMax: return item
  else continue
return null after unlock
```

## Naming correction

Task / QuickBar plate said “find-by-**COID**”. **Falsified.**
Match key is **CBID** at `clonebase+0x34` (same as `aa_005711c0`).
Instance COID is `item+0x160/+0x164`; sibling `FUN_00571010` is the COID walk.

## Callers (key)

| Caller | Args | Use |
|--------|------|-----|
| `0x005714e0` | `(cbid, 0, qty)` | merge-slot: room for qty |
| `0x0052aae0` | `(cbid, 0, 0)` | same-CBID exists? |
| `0x00587c00` | `(cbid, 0, param_6)` | vendor/buy stack merge |
| `0x005879e0` | `(0x21A3, 0, 0)` | fixed CBID consume |
| `0x008134d0` | `(clonebase+0x34, 0, 0)` | trade re-add existing |
| `0x008e40f0` | `(cbid, 0, 0)` | recipe/grid presence → copy COID out |
| `0x0060d460` | `(req+0x2c, 0, 0)` | mission req optional CBID gate |
| `0x008a8770` | `(obj[6], 0, 0)` | mission UI “Look in Mission Inventory” |
| QuickBar `0x009436c0` | (slot value…) | item-slot resolve (misnamed COID in older clean) |

## Gaps

- Runtime / bit-exact / image diff open
- Original PDB name unknown
- `param_3 != 0` path rarely observed; semantic “include broken / skip capacity” is CF-correct but name is interpretive
- Whether `clonebase+0x38==4` type enum label is “currency/unlimited” not sealed beyond constant path
- `FUN_004022a0` full list-node layout owned elsewhere
