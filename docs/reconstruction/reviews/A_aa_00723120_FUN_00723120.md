# Review A (reconstruction fidelity): `aa_00723120` FUN_00723120

| Field | Value |
|---|---|
| **Stable ID** | `aa_00723120` |
| **VA** | `0x00723120` |
| **Canonical name** | `FUN_00723120` |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Independent reconstruction review |
| **Counterpart** | `reviews/B_aa_00723120_FUN_00723120.md` |
| **System** | client audio/FX — allocate 0x150 instance and construct/register play object |
| **Verdict** | **accept-with-gaps** — alloc size + ctor call **sealed**; full arg map **partial** |

---

## 1. Purpose

**Allocate a 0x150-byte sound/FX play object**, construct it, and register/push it into a container.

Live bytes show **`this` in ECX** (`mov esi, ecx`) — **thiscall** on a manager/container — then:

1. **`operator_new(0x150)`** (push size; call).
2. If non-null: **`FUN_0071e570(...)`** construct with a subset of stack args (decompile: params 2,3,4,5,8,9,10).
3. **`FUN_004406e0()`** — insert/register into container (args elided by decompiler).

Called from `FUN_004b7e50` alternate play/attach paths.

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Raw | `docs/reconstruction/raw/aa_00723120_FUN_00723120.md` |
| Annotated | `docs/reconstruction/raw/aa_00723120_FUN_00723120.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/FUN_00723120.cpp` |
| Function record | `docs/reconstruction/functions/aa_00723120_FUN_00723120.md` |
| Live decompile | Ghidra `batch_decompile` `0x00723120` |
| Live bytes | Ghidra `read_memory` `0x00723120` length 48 |
| Callees | `operator_new`, `FUN_0071e570`, `FUN_004406e0` |
| Callers | `FUN_004b7e50` play branches |

---

## 3. Sealed residuals

### 3.1 ABI — **SEALED (partial)**

| Fact | Evidence |
|---|---|
| Convention | **`__thiscall`**: ECX = container (`mov esi, ecx` in entry bytes) |
| Alloc size | **`0x150`** (`push 0x150`) |
| Decompiler | shows ~10 stack args; names/order **not all verified** |

```c
// Approximate — thiscall on audio/FX manager
void __thiscall FUN_00723120(void *this, /* multi stack args for ctor/register */);
```

### 3.2 Algorithm — **SEALED structure**

```
this = ECX (manager)
obj = operator_new(0x150)
if (obj):
  FUN_0071e570(obj, ... subset of args ...)  // construct
FUN_004406e0(...)  // register into this / global list
return
```

### 3.3 Entry machine bytes — **SEALED**

```
56                      push esi
68 50 01 00 00          push 0x150
8B F1                   mov  esi, ecx          ; this
E8 ...                  call operator_new
83 C4 04                add  esp, 4
85 C0                   test eax, eax
74 44                   jz   → skip ctor
8B 4C 24 2C             mov  ecx, [esp+0x2C]   ; stack args
8B 54 24 28             mov  edx, [esp+0x28]
F3 0F 10 44 24 20       movss xmm0, [esp+0x20]
... push/setup for FUN_0071e570 ...
```

Hex prefix:  
`5668500100008bf1e86567d6ff83c40485c074448b4c242c8b542428f30f1044242051...`

### 3.4 Side effects — **SEALED**

- Heap alloc **0x150** on success path.
- Constructs play object; registers via `004406e0`.
- Null alloc: still calls register path (decompile shows `FUN_004406e0` after the if — verify: raw has it **outside** the if → may register null / no-op).

Raw:

```c
if (pvVar1 != 0) { FUN_0071e570(...); }
FUN_004406e0();
```

So **register always runs**; ctor only if alloc ok.

---

## 4. Control flow: clean ≡ raw ≡ live

| Stage | Match |
|---|---|
| thiscall ECX | Yes (bytes; decompile missed) |
| new 0x150 | Yes |
| ctor if non-null | Yes |
| always `004406e0` after | Yes (raw) |

---

## 5. Gaps / open

1. Full stack arg list / product meanings (name, TFID, position, flags).
2. Exact `FUN_0071e570` / `FUN_004406e0` signatures.
3. Decompiler thiscall recovery incomplete — prefer bytes for ECX.
4. Runtime verification not run.

**Verdict:** **accept-with-gaps**
