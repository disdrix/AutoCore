# Dual residual: `0x008151a0` Client_RecvInventoryAddItem

| Field | Value |
|-------|-------|
| Stable ID | `aa_008151a0` |
| Address | `0x008151a0` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior dual | A accept-with-gaps + B accept-with-gaps / needs-more-evidence on opcode, `@+0x12`, register contract |
| Re-decompile | Ghidra `decompile_function` `0x008151a0` (2026-07-29) — body ≡ frozen raw |

## Residual questions (from dual A/B)

1. Exact PacketDispatch opcode for this VA
2. Full S2C struct vs C2S field parity (`+0x10/+0x11` XY unused by body?)
3. Semantics of `FUN_00945540` vs `FUN_00571830`
4. Whether loot channel `0x17` is combat-log only
5. Register framing (EBX packet / stack client)
6. Plate “skip place when `@+0x12` non-zero” polarity

---

## Seal 1: Opcode = S2C `0x2047` (dispatch, not body)

| Evidence | Location | Confidence |
|----------|----------|------------|
| `case 0x2047: Client_RecvInventoryAddItem(param_3); return 1` | raw `aa_00815710_Client_PacketDispatch` | **High** |
| Same case in clean `Client_PacketDispatch.cpp` | reconstructed-exact | **High** |
| Body has **no** opcode immediate / self-check | live re-decompile | **High** |
| `0x2048` not handled by this client | `GameOpcode.cs` + PACKET STRUCTURES | **High** |
| Docs table: `0x2047` → `Client_RecvInventoryAddItem` | `Documentation/PACKET STRUCTURES.md` | **High** |

**Conclusion:** Handler identity is **dispatch-bound** to opcode **`0x2047`**. Not a body claim; do not require an opcode load inside `0x008151a0`.

Bidirectional note: C2S builders also use `0x2047` (`Client_SendInventoryAddItem` / `Inventory_SerializeAddItemPacket`). Demux is **transport direction** (same pattern as unequip `0x203E`) — S2C → PacketDispatch → this unit; C2S → serialize/send.

---

## Seal 2: Register / ABI framing

Prologue bytes at `0x008151a0` (Ghidra `read_memory`):

```text
81 ec 04 01 00 00   sub  esp, 0x104     ; 256-byte sprintf buf + frame
55 56 57           push ebp / esi / edi
e8 …               call FUN_007a69d0
8b 4b 08           mov  ecx, [ebx+8]   ; packet COID lo
8b ac 24 14 01 00 00  mov ebp, [esp+0x114]  ; stack formal (client)
8b f0              mov  esi, eax
8b 43 0c           mov  eax, [ebx+0xc] ; packet COID hi
50 51              push eax / ecx      ; resolve args
```

| Claim | Status | Confidence |
|-------|--------|------------|
| Packet base = **EBX** (`unaff_EBX`) | **Sealed** — `[ebx+8]/[ebx+0xc]` loads | **High** |
| Stack formal = **client** (`param_1` / ebp after frame) | **Sealed** — uses `+0xe98` char, `+0xf38` loot UI | **High** |
| Portable pure `__cdecl(client, packet*)` without EBX | **Not** retail shape | — |
| Dispatch call site `Client_RecvInventoryAddItem(param_3)` | **High** — formal is client/context; packet already in EBX from framing | **High** working |

---

## Seal 3: Packet field map (body-used vs wire-only)

Authoritative wire size `0x20` (`InventoryAddItem` / AutoCore `InventoryAddItemResponsePacket`).

| Offset | Wire name | Body use | Confidence |
|-------:|-----------|----------|------------|
| `+0x00` | opcode `0x2047` | **Not read** (dispatch already selected) | High |
| `+0x08/+0x0c` | item COID lo/hi | Resolve + FindInGrid + place helpers | **High** |
| `+0x10` | cargo X | **Never loaded in this body** | **High** (absent) |
| `+0x11` | cargo Y | **Never loaded in this body** | **High** (absent) |
| `+0x12` | `bAddToExistingItem` | Place vs re-find gate (see Seal 4) | **High** |
| `+0x14` | quantity i32 | Loot `x%d` + `FUN_00571830` delta | **High** |
| `+0x18` | `bWasSuccessful` / wasAdded | Early return if `== 0` | **High** |

**Sealed product fact:** S2C notify **does not honor server XY** for placement. Free-slot / footprint work lives inside `FUN_00945540` (re-find). Merge path keys by **COID**, not slot. Matches cargo wire RE: “client re-finds slot.”

C2S serialize (`Inventory_SerializeAddItemPacket`) **writes** X/Y on free-slot path and clears them on merge — layout parity, **asymmetric consumer**.

---

## Seal 4: `@+0x12` polarity = add-to-existing → merge place

Body compound (re-decompile ≡ raw):

```text
if (pkt[+0x12] == 0
    || client+0xe98 == 0
    || vehicle(char+0x250) == 0
    || FUN_00571010(coidLo, coidHi) == 0)
  FUN_00945540(client);          // re-find / full loot place
else
  FUN_00571830(qty, coidLo, coidHi);  // merge onto existing stack
```

| Claim | Result | Confidence |
|-------|--------|------------|
| Non-zero `@+0x12` **and** char/vehicle ready **and** COID already in grid → **merge place** | **Sealed** | **High** |
| Zero `@+0x12` **or** missing context **or** not in grid → **re-find path** | **Sealed** | **High** |
| Plate “skip place when non-zero” | **Falsified** | — |
| Aligns with C2S `bAddToExistingItem` (serialize merge path sets `+0x12=1`) | **Sealed** cross-unit | **High** |

---

## Seal 5: Helper roles (leave FUN_* names; roles High)

### `FUN_00571010` @ `0x00571010` — Find item in cargo by COID

`__thiscall` inventory walker: iterate with `FUN_004022a0`; match `item+0x160/+0x164 ==` args; return item ptr or 0.

### `FUN_00571830` @ `0x00571830` — Merge quantity onto existing stack

```text
item = FUN_00571010(coidLo, coidHi);
if !item → VOG_DEBUG_STOP, return 0
cur = item.vtbl+0x25c()           // get qty
item.vtbl+0x260(cur + delta)      // set qty
FUN_00512670(); maybe again if this+0x24
this+0x21 = 1; return 1
```

**Role sealed:** **stack-qty merge**, not footprint place. Caller Recv passes qty `@+0x14` as delta (thiscall `this` = grid — framing residual at call site only).

### `FUN_00945540` @ `0x00945540` — Full loot / re-find place (`Inv_lootpickup` alias)

Uses item in **EDI** (parent resolve result; register residual) + client formal:

- Early out if no item / no char / vehicle “busy” vtbl
- If `FUN_00513e70` allows stack scan: walk cargo for same CBID + broken-bit clear + qty-compat → may call **`FUN_00571830`** on match
- Else free-slot: `FUN_005714e0` + `FUN_00571620` PlaceFootprint; attach owner; optional UI
- Credits path when clonebase type `0x42`
- Optional `"lootpickup"` audio; `Client_RefreshOpenMissionUiWindows`

**Role sealed:** **authoritative place/re-find** for non-merge-notify path (and internal merge when stack-eligible).

### `FUN_00513e70` @ `0x00513e70` — Stack/loot eligibility probe

Returns 1 iff (clone flag path with bit `0x40` at blob+0x3f2 **or** broken bit on item) **and** type `clonebase+0x38 == 4` … else 0. Shared with serialize merge probe + loot line gate.

### Trailing UI

| Addr | Role | Confidence |
|------|------|------------|
| `FUN_0089c6c0(1)` | Inventory widget dual-pane refresh when `DAT_00d1b894` visible | **High CF** / name open |
| `FUN_0092ce90` | `Client_RefreshMissionRelatedUi` (prior residual) | **High** role |

---

## Seal 6: Loot line contract

Only when `DAT_00d1b8dc != 0` **and** success `@+0x18`:

| Element | Rule | Confidence |
|---------|------|------------|
| Broken prefix | `item[0x5f] bit 19` (`>>0x13 & 1`) → `"Broken: "` else `""` | **High** |
| Name | item vtbl `+0x15c` (1-arg if qty&lt;2 or probe0; 2-arg with qty if probe+qty≥2) | **High** |
| Header | `"Received Loot"` via `FUN_007a6de0` | **High** |
| Format | `"%s: %s%s"` or `"%s: %s%s x%d"` | **High** |
| Emit | `FUN_008f8200(host, **0x17**, empty, buf, 0)` | **High** channel constant |
| Channel product label | combat/loot log (sibling inventory handlers share `0x17`) | **Probable** product name |

`FUN_00513e70` false **or** qty &lt; 2 → single-item format (no `xN`); both true with qty≥2 → `x%d`.

---

## Re-decompile check (Ghidra 2026-07-29 residual)

- CF, strings (`Broken: `, `Received Loot`, sprintf formats), offsets `+8/+0xc/+0x12/+0x14/+0x18`, callees — **unchanged** vs frozen raw `aa_008151a0`.
- Helpers `00571010` / `00571830` / `00945540` / `00513e70` re-decompiled; roles above.
- **Not** used by body: `+0x10`, `+0x11`.

---

## Residual after this pass

| Item | Status |
|------|--------|
| Opcode `0x2047` via PacketDispatch | **Sealed High** |
| EBX packet + stack client | **Sealed High** (prologue + uses) |
| `@+0x12` polarity (add-to-existing → merge) | **Sealed High**; plate skip-place **falsified** |
| XY `@+0x10/+0x11` unread by Recv | **Sealed High** (body absence) |
| `FUN_00571830` = qty merge | **Sealed High** role |
| `FUN_00945540` = re-find / loot place | **Sealed High** role |
| `FUN_00571010` = find-by-COID | **Sealed High** role |
| Loot channel `0x17` product enum name | **Probable** (constant sealed) |
| Full thiscall ECX for `FUN_00571830` at Recv call site | Open (decompiler lost this; helper is thiscall) |
| EDI item into `FUN_00945540` | Open register residual (body uses unaff_EDI) |
| Runtime add/loot/merge capture matrix | Open |
| Bit-exact / image diff | Open (policy) |

## Verdict after residual

**accept-with-gaps** — dual residual **opcode + ABI framing + field map + @+0x12 polarity + helper roles + XY non-use** strengthened; runtime/diff and a few register handoffs remain.

## Artifacts touched by residual owner

- This scratch: `docs/reconstruction/reviews/a_008151a0.md`
- Duals: `A_aa_008151a0_Client_RecvInventoryAddItem.md`, `B_aa_008151a0_Client_RecvInventoryAddItem.md`
- Function record: `functions/aa_008151a0_Client_RecvInventoryAddItem.md`
- Clean plate polarity fix only: `reconstructed-exact/Client_RecvInventoryAddItem.cpp`
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
