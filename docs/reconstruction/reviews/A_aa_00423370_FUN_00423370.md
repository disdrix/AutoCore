# Review A (reconstruction fidelity): `aa_00423370` FUN_00423370

| Field | Value |
|---|---|
| **Stable ID** | `aa_00423370` |
| **VA** | `0x00423370` |
| **Canonical name** | `FUN_00423370` |
| **INFERRED product role** | ADO / MSXML **OpenFromInitializationString** wrapper (CoCreate + open) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Independent reconstruction review (fidelity) |
| **Counterpart** | `reviews/B_aa_00423370_FUN_00423370.md` |
| **System** | `missions-progression` (DBReader cold-open path) |
| **Tools** | Ghidra `decompile_function`, `get_function_callees`, `get_function_by_address` |
| **Verdict** | **accept-with-gaps** |

---

## 1. Purpose

Callee of `FUN_007e1d80` (DBReader enter) immediately before the debug string `"After OpenFromInitializationString\n"`. Opens a COM data source from an initialization string:

1. `FUN_00437cd0()` — stack/cookie helper (INFERRED)
2. `CoCreateInstance(clsid@DAT_00a8605c, NULL, CLSCTX_INPROC_SERVER=1, iid@DAT_00a8604c, &punk)`
3. On success: vtable `+0x0c` open call with `(punk, 0, 1, initString, &DAT_00a8607c)`
4. Optional branch when `param_2 != 0`: VariantInit + property bag (`vt=2`, value 3) via another COM method `+0x14`
5. Final method `+0x0c` on object in `*in_EAX` (decompiler residual)
6. Cleanup `FUN_0041d7b0` on all exits; returns **HRESULT**

Primary ensure path calls with **`param_2 = 0`**, so the VariantInit property branch is **skipped** for table loads.

---

## 2. Inspected artifacts

| Artifact | Path |
|---|---|
| Raw | `docs/reconstruction/raw/aa_00423370_FUN_00423370.md` |
| Annotated | `docs/reconstruction/raw/aa_00423370_FUN_00423370.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/FUN_00423370.cpp` |
| Function record | `docs/reconstruction/functions/aa_00423370_FUN_00423370.md` |
| Caller | `FUN_007e1d80` @ `0x007e1d80` (passes `this+0x2c` wide cnxn string, `0`) |

---

## 3. Body range

| Field | Value |
|---|---|
| Entry | `0x00423370` |
| Body | `0x00423370`–`0x004234d5` |

Callees: `CoCreateInstance`, `VariantInit`, `FUN_00437cd0`, `FUN_0041d7b0`.

---

## 4. Signature / ABI notes

Decompiler signature:

```c
HRESULT FUN_00423370(undefined4 param_1 /* init string */, char param_2);
```

Residual: heavy use of `in_EAX` / `piStack_4` suggests custom calling convention or missing prologue typing (object out-parameter may be in EAX from prior setup). **Do not seal full ABI beyond**: stack args include init string pointer and char flag; return is HRESULT (`>=0` success checks in caller).

Caller site in enter:

```c
iVar1 = FUN_00423370(param_1 + 0x2c, 0);
```

---

## 5. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| Used as OpenFromInitializationString step | **High** | Caller debug string adjacency |
| CoCreateInstance + open vcall | **High** | Decompile |
| `param_2==0` skips VariantInit branch | **High** | Ensure path |
| CLSID/IID bytes at DAT_00a8605c/4c | **Medium** | Not re-read this pass; GUIDs still DAT_* |
| Exact interface names (ADO vs MSXML) | **Medium** | OpenFromInitializationString is ADO-ish; loaders also use MSXML xpaths under XML mode |
| Full out-object lifetime | **Low** | `in_EAX` decompiler noise |
| Required for AutoCore server | **N/A** | Client-only data bring-up |

---

## 6. Control flow: clean ≡ raw

| Stage | Match |
|---|---|
| CoCreate → open → optional props → final call → cleanup | **Yes** |
| Fail LAB_004234a6 cleanup + return hr | **Yes** |

---

## 7. Gaps / open

1. Dump GUID bytes at `0x00a8605c` / `0x00a8604c` / `0x00a8607c` / `0x00a8606c` for interface naming.
2. Resolve `in_EAX` — true out parameter location.
3. Whether success path AddRefs into `DBReader+0x24` (done in caller after return, not here).

**Verdict:** **accept-with-gaps**
