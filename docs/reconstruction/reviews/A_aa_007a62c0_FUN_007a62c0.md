# Review A (reconstruction fidelity): `aa_007a62c0` FUN_007a62c0 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_007a62c0` |
| **VA** | `0x007a62c0` |
| **Canonical name** | `FUN_007a62c0` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Reconstruction fidelity |
| **Counterpart** | `reviews/B_aa_007a62c0_FUN_007a62c0.md` |
| **System** | locale / string-pool (mode-0 prepare; FormatFailure path uses mode −1→default) |
| **Verdict** | **leave-FUN** |

**Evidence:** Ghidra `decompile_function` @ `0x007a62c0`.

---

## 1. Purpose

**Prepare current ring slot for a strcpy-style write** of string in **EAX**:

- If `EAX == pool->slot[ringIndex]`: **zero** the existing slot capacity in place (self-copy / alias path) and return.
- Else: measure `strlen(EAX)`, call `FUN_007a6260` (ensure capacity for that length — register handoff) so parent can strcpy into the slot.

Used by `FUN_007a6de0` mode **0** and the mode 1–3 branch when original `mode == -1` (parent dual documents call order).

---

## 2. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| Alias check vs current ring slot pointer | **Confirmed** | |
| Zero-in-place on alias | **Confirmed** | dword/byte clear of cap |
| Non-alias → strlen + ensure | **Confirmed** | |
| Does not itself strcpy | **Confirmed** | parent copies after return |
| Product name | **Open** | leave FUN_* |

---

## 3. Artifacts

| Artifact | Path |
|---|---|
| Raw / annotated / clean / record | `aa_007a62c0_*` |
| Parent | `FUN_007a6de0` / FormatFailure locale lookup |
| Callee | `FUN_007a6260` |

---

## 4. Gaps

1. Exact required size passed to ensure (strlen vs strlen+1) — decompiler elides EAX set before `FUN_007a6260`.
2. Whether alias path is intentional for in-place clear of ring buffer before overwrite.

**Verdict:** **leave-FUN**.
