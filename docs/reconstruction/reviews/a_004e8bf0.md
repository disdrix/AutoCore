# Scratch: `aa_004e8bf0` Math_QuatTransformPoint_Inferred

| Field | Value |
|---|---|
| VA | `0x004e8bf0` |
| Date | `2026-07-29` |
| Role | OWN-ONLY dual A/B notes |

## Machine

- Span: `004e8bf0`–`004e8db8` (`ret`); size **457** B
- Prolog: `55 8B EC 83 E4 F0 83 EC 0C 56` (frame + 16-align + push esi)
- Epilogue: `5E 8B E5 5D C3`
- Leaf; cdecl; 4× float*

## Constants (`read_memory`)

| Addr | LE | f32 |
|---|---|---|
| `00a0f2a0` `g_flOne` | `00 00 80 3f` | **1.0** |
| `00a10e74` `g_flLevelUpUiBase_Inferred` | `00 00 00 40` | **2.0** (misnomer) |

## Contract

```
out.xyz = pos.xyz + R(quat_xyzw) * vec.xyz
// out.w not written
```

R columns ≡ `004e8ad0` (right) / `004e8b60` (up) / `004e8a40` (forward).

## Callers

13 functions / 16 sites. Notable: `FUN_0091a5f0` camera look (`world = pos + R*lookVec`).

## Sibling

`004e8590` = inverse delta `Rᵀ·(p3−p1)` — **not** this.

## Verdict lean

accept-with-gaps; `_Inferred`; reject skybox-only scaffold name.
