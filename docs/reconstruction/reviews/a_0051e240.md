# Dual residual: `0x0051e240` CVOGHBOKToCastAgain_ctor

| Field | Value |
|-------|-------|
| Stable ID | `aa_0051e240` |
| Address | `0x0051e240` |
| Owner | dual residual only (**no** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 |
| Prior dual | A/B accept-with-gaps (2026-07-23) — open: category insert ABI, FPU no-plant path, plate arity |

## Residual questions (from prior dual / matrix)

1. Plate formals `(this, skill, chargeMs)` vs true arity
2. Category insert CONCAT / arg pack
3. `unaff_EBX` / FPU path when no vehicle
4. AttachOwner target (skill vs owner)
5. GetCategoryCooldownMap `this` (skill vs owner)
6. Vehicle path: skill vtbl vs owner vtbl
7. +0x6b4 English / plant collision

## Seal: ABI from machine code (High)

### Epilogue

```
0051e38b  C2 0C 00     RET 0xC
```

**High:** MSVC `__thiscall` cleans **12 bytes** = **three** stack dwords (+ ECX = new HB).

### Corrected signature

```c
void* __thiscall CVOGHBOKToCastAgain_ctor(
    void* thisHb,          // ECX = operator_new(0x28) from StartCastAgainHeartbeat
    void* pOwnerEntity,    // stack0 — character/owner; vehicle/pool vfuncs; map host; AttachOwner
    void* pSkillRuntime,   // stack1 — skill (+0x10 CD, +0x5e8 category, stored HB+0x24)
    int   nChargeDelayMs); // stack2 — added after ROUND(ceil(cd*mod))
// RET 0xC
```

| Prior plate | Reality | Conf |
|-------------|---------|------|
| 2 stack formals `(skill, charge)` | **3** stack — owner + skill + charge | **High** (`RET 0xC` + caller 3 pushes) |
| Ghidra decomp arity | Under-counted; fused owner into skill | **Falsified** |

Cross-check: `reviews/a_00519200.md` — `PUSH charge; PUSH skill; PUSH owner; CALL 0x0051e240`.

### Register map after prolog (High)

| Reg / slot | Role |
|------------|------|
| ESI | `thisHb` |
| EDI | `pOwnerEntity` (`[esp+…]` stack0) |
| EBX | `pSkillRuntime` (stack1) |
| stack2 | `nChargeDelayMs` |

## Seal: control flow (High)

1. SEH + `CVOGHBBase_ctor(this)`
2. `*this = g_pVtbl_CVOGHBOKToCastAgain` (`0x009ce1c4`)
3. `CVOGHBBase_SetPeriodAndCounter(this, 1, 1)` — both immediates **1**
4. **Equip modifier**
   - `movss` load **`g_flOne`** `@ 0x00a0f2a0` (`00 00 80 3f` = 1.0f) to stack local
   - `owner.vtbl+0x210(0)` — if **EAX==0**: `fld` that 1.0 → mod
   - else: `owner.vtbl+0x210` again (pushes involve `skill+0x5ec`) → `Vehicle_GetSkillCooldownModifier(ECX=result)`; mod = **ST0**
5. **Duration**
   - `fild skill+0x10` × mod → `ceil` (IAT) → x87 store → `fistp` (ROUND) → **+ chargeMs** → **`HB+0x8`**
6. **Clamp**
   - `pool = owner.vtbl+0x1dc()`; if non-null and `*(pool+0x6b4) >= 1` and `HB+0x8 >= 500` → `HB+0x8 = 500`
7. **Category map** (if `skill+0x5e8 != -1`)
   - entry `{ key=skill+0x5e8, start=DAT_00b041cc (g_dwClientTickMs), duration=HB+0x8 }`
   - `map = Skill_GetCategoryCooldownMap(owner)` — **ECX=EDI**, map host **`owner+0x6c`**
   - `Skill_CategoryCooldownMap_Insert(map, &outPair, &entry)` — same shape as `Skill_InsertCategoryCooldown`
8. `CVOGHBBase_AttachOwnerObject(hb, **owner**)` — push EDI, **not** skill
9. `HB+0x24 = skill`; `HB+0x1c = 8`
10. return this; `RET 0xC`

## Seal: falsified decompiler claims

| Decomp / old plate claim | Asm reality | Conf |
|--------------------------|-------------|------|
| `AttachOwnerObject(hb, skill)` | **owner** (EDI) | **High** |
| `GetCategoryCooldownMap(skill)` | **owner** (EDI) | **High** |
| Vehicle path via `skill.vtbl+0x210` | **owner.vtbl+0x210** | **High** |
| Clamp pool via `skill.vtbl+0x1dc` | **owner.vtbl+0x1dc** | **High** |
| No-vehicle mod = `unaff_EBX` garbage | **`g_flOne` (1.0f)** | **High** (`read_memory` @ `0x00a0f2a0`) |
| `flCategoryScale = 0.0` | Constant load is **1.0** | **High** |
| Category insert args unrecovered CONCAT | Contiguous `{key,start,duration}` + map this + out pair | **High** |
| 2-stack signature | **3-stack `RET 0xC`** | **High** |

## Seal: duration formula (High)

```
HB+0x8 = ROUND(ceil( (float)(int)skill+0x10 * equipMod )) + nChargeDelayMs
if owner.vtbl+0x1dc() != 0 && *(pool+0x6b4) >= 1:
  HB+0x8 = min(HB+0x8, 500)   // cmp 0x1F4; mov 500 if not below
```

| Claim | Conf | Evidence |
|-------|------|----------|
| CD int at skill+0x10 | **High** | `mov eax,[ebx+0x10]` |
| ceil via IAT then fistp ROUND | **High** | bytes @ formula block |
| + charge before store HB+0x8 | **High** | `add edx,ecx; mov [esi+8],edx` |
| clamp constant **500** (0x1F4) | **High** | `cmp eax,0x1f4` / `mov eax,0x1f4` |
| type **8** @ +0x1c | **High** | `mov dword [esi+0x1c],8` |
| skill ptr @ +0x24 | **High** | `mov [esi+0x24],ebx` |
| vtbl `0x009ce1c4` | **High** | `mov [esi],0x009ce1c4` |
| category skip if key == -1 | **High** | `cmp edx,-1; je` |
| start tick `DAT_00b041cc` | **High** | `mov eax,[0x00b041cc]` |
| map insert ABI ≡ InsertCategoryCooldown | **High** | layout + callees `0x00518d70` / `0x0051d2f0` |

## Seal: category insert pack (High)

```
// locals (stack)
int   key;       // skill+0x5e8
uint  startTick; // g_dwClientTickMs
int   duration;  // HB+0x8  (post-clamp)

map = Skill_GetCategoryCooldownMap(pOwnerEntity);  // lazy map @ owner+0x6c
Skill_CategoryCooldownMap_Insert(map, &outPair8, &key);
// Insert signature: __thiscall (map, outIterator*, entry*) where entry starts at key
```

Matches `aa_00518df0` Skill_InsertCategoryCooldown body (start=tick, duration=param, key=category).

## Residual after this pass

| Item | Status |
|------|--------|
| ABI 3-stack + `RET 0xC` | **Sealed High** |
| Owner vs skill roles (vfuncs, map, attach, HB+0x24) | **Sealed High** |
| Duration formula + 500 clamp gate | **Sealed High** |
| No-vehicle mod = 1.0 (`g_flOne`) | **Sealed High** |
| Category insert entry pack | **Sealed High** |
| Type 8 + vtbl | **Sealed High** |
| Product English for `owner.vtbl+0x210` / `+0x1dc` | Open (CF sealed) |
| Product English for pool `+0x6b4` | Open |
| Full `Vehicle_GetSkillCooldownModifier` body / ST0 plant math | Sibling unit; call shape sealed here |
| Exact second `+0x210` push ABI (skill+0x5ec role) | Open (mod path still uses ST0) |
| Runtime / bit-exact / image diff | Open (matrix policy) |
| Original PDB symbol | Unknown (reconstructed name retained) |

## Artifacts touched

- This scratch: `reviews/a_0051e240.md`
- Duals: `A_aa_0051e240_*`, `B_aa_0051e240_*`
- Function record, clean, annotated append
- `VERIFICATION_MATRIX` row; `systems/skills-abilities.md` unit status
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
