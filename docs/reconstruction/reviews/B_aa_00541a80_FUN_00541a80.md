# Review B (skeptical / adversarial): `aa_00541a80` FUN_00541a80

| Field | Value |
|---|---|
| **Stable ID** | `aa_00541a80` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_00541a80_FUN_00541a80.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | Always rebuilds | Only when count `+0xC` not positive | **Falsified** — **lazy** |
| 2 | Performs the hash lookup | Only returns table; lookup is `CNDHash_LookupByKey` | **Confirmed getter only** |
| 3 | Null-safe if `+0xF1C` null | Direct `cmp [eax+0xC]` — would fault | **Accept risk** — assumes non-null |
| 4 | Complex multi-arg | Zero stack args; ECX this | **Confirmed simple** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| `+0xF1C` table ptr | **High** | Wrong hash |
| count at `table+0xC` | **High** | Spurious rebuild / miss |
| returns table not entry | **High** | Type confusion with Lookup |

---

## 3. Cross-check against raw + bytes

```
raw ≡ 28-byte leaf.
568BF1 8B861C0F0000 83780C00 7F05 E8... 8B861C0F0000 5EC3
≡ A sealed.
```

---

## 4. Surviving contract for AutoCore

```csharp
CndHash EnsureTypeHash(Manager m)
{
    var t = m.HashAt_F1C;
    if (t.Count /*+0xC*/ < 1)
        RebuildHash(m); // FUN_00541950
    return m.HashAt_F1C;
}
```

---

## 5. Open questions

1. Owner object product name.
2. Rebuild cost / when count drops to 0.

**Verdict:** **accept**
