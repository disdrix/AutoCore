# Residual scratch: `aa_00571830` InventoryGrid_MergeStackQty

| Field | Value |
|---|---|
| **VA** | `0x00571830` |
| **Date** | `2026-07-29` |
| **Tools** | Ghidra MCP `force_decompile`, `decompile_function`, `read_memory` (128 B @ entry + string @ `0x00A15844`), `get_function_pcode`, `get_function_callers`, `batch_decompile` of callees `00571010` / `00512670` / `007a4480` |
| **Not used** | `disassemble_bytes`, Launcher, live debug |

## Live decompile ≡ raw 2026-07-23

Body control flow bit-identical to `raw/aa_00571830_FUN_00571830.md`. Force re-decompile 2026-07-29 unchanged. Scaffold mis-name `Named_VOG_DEBUG_STOP` is **string-only**, not purpose.

## ASM seals (read_memory)

```
00571830  8B 44 24 0C           mov  eax, [esp+0x0C]   ; coidHi (pre-push)
00571834  56                    push esi
00571835  57                    push edi
00571836  8B F9                 mov  edi, ecx          ; this = InventoryGrid*
00571838  8B 4C 24 10           mov  ecx, [esp+0x10]   ; coidLo (post 2 push)
0057183C  50                    push eax               ; coidHi
0057183D  51                    push ecx               ; coidLo
0057183E  8B CF                 mov  ecx, edi          ; this for FindByCoid
00571840  E8 CB F7 FF FF        call FUN_00571010      ; FindItemByCoid
00571845  8B F0                 mov  esi, eax          ; item*
00571847  85 F6                 test esi, esi
00571849  75 15                 jnz  found

; miss:
0057184B  68 44 58 A1 00        push 0x00A15844        ; "VOG_DEBUG_STOP"
00571850  50                    push 0                 ; log enable = 0 (no-op)
00571851  E8 2A 2C 23 00        call FUN_007a4480
00571856  83 C4 08              add  esp, 8
00571859  5F                    pop  edi
0057185A  32 C0                 xor  al, al            ; return 0
0057185C  5E                    pop  esi
0057185D  C2 0C 00              ret  0x0C              ; 3 stack dwords

; found:
00571860  8B 16                 mov  edx, [esi]        ; vtbl
00571862  8B CE                 mov  ecx, esi
00571864  FF 92 5C 02 00 00     call dword ptr [edx+0x25C]  ; GetQty
0057186A  03 44 24 0C           add  eax, [esp+0x0C]   ; + qtyDelta
0057186E  8B 16                 mov  edx, [esi]
00571870  50                    push eax
00571871  8B CE                 mov  ecx, esi
00571873  FF 92 60 02 00 00     call dword ptr [edx+0x260]  ; SetQty(cur+delta)
00571879  8B CE                 mov  ecx, esi
0057187B  E8 F0 0D FA FF        call FUN_00512670      ; dirty item |0x20000
00571880  8B 4F 24              mov  ecx, [edi+0x24]   ; grid.owner
00571883  85 C9                 test ecx, ecx
00571885  74 05                 jz   skip_owner
00571887  E8 E4 0D FA FF        call FUN_00512670      ; dirty owner (ECX preset)
0057188C  B0 01                 mov  al, 1
0057188E  88 47 21              mov  [edi+0x21], al    ; grid dirty byte
00571891  5F                    pop  edi
00571892  5E                    pop  esi
00571893  C2 0C 00              ret  0x0C
```

Function ends `00571893`; next function prologue at `005718a0` (after `0xCC` pad). Size ≈ **0x64** bytes.

### SEALED formulas

```
this      = InventoryGrid*   (ECX → EDI)
qtyDelta  = stack0
coidLo    = stack1
coidHi    = stack2
ret stack = 0x0C  (3 dwords)  // thiscall

item = InventoryGrid_FindItemByCoid(this, coidLo, coidHi)
if item == NULL:
    FUN_007a4480(0, "VOG_DEBUG_STOP")   // flag 0 → body no-ops (never logs)
    return 0

cur = item->vtbl[+0x25C]()              // GetQty thiscall
item->vtbl[+0x260](cur + qtyDelta)      // SetQty thiscall
Object_MarkDirtyFlag_0x20000(item)      // FUN_00512670: *(obj+0x17C) |= 0x20000; walk +0xAC
if (owner = *(this+0x24)) != NULL:
    Object_MarkDirtyFlag_0x20000(owner)
*(u8*)(this+0x21) = 1
return 1
```

### Explicit absences (body-proven)

- **No** max-stack clamp / capacity compare
- **No** CBID / Broken / stack-eligibility probe
- **No** footprint place / free-slot search / cell stamp
- **No** XY / inventoryType / packet field reads
- **No** quantity underflow guard (signed add of `qtyDelta` as-is)

## Callee seals (supporting)

| Addr | Role in this unit | Body fact used |
|------|-------------------|----------------|
| `0x00571010` | Find item by COID lo/hi | Dual sealed 2026-07-29 |
| `0x00512670` | `*(obj+0x17C) \|= 0x20000` + child chain `@+0xAC` | Live decompile 2026-07-29 |
| `0x007a4480` | Log only if `param_1 == 1` | Live decompile — **call site always passes 0** |

## Callers (Ghidra)

| Caller | VA | Use of this unit |
|--------|-----|------------------|
| `Client_RecvInventoryAddItem` | `0x008151a0` | Merge path when add-to-existing + in-grid |
| `Client_SendInventoryAddItem` | `0x00530df0` | C2S merge path |
| `FUN_00945540` | `0x00945540` | Loot place internal stack merge |
| `FUN_008012f0` | `0x008012f0` | Equip/inventory path (×2 xrefs) |
| `FUN_00587c00` | `0x00587c00` | Alloc/new-object path |
| `FUN_0052aae0` | `0x0052aae0` | Inventory helper |

## Gaps (owned residual)

| Item | Status |
|------|--------|
| PDB / original symbol | Open |
| `qtyDelta` signed vs unsigned product type | Open (body is raw `add`) |
| Product name of dirty bit `0x20000` / grid `@+0x21` | Open (role sealed) |
| Whether negative `qtyDelta` is intentional (split residual path) | Open (callers own) |
| Runtime golden merge matrix | Open |
| Bit-exact image diff | Open |

## Naming

| Candidate | Verdict |
|-----------|---------|
| `Named_VOG_DEBUG_STOP_00571830` | **Reject** — string is miss-path assert only |
| `InventoryGrid_MergeStackQty` | **Adopt** (behavior) — Probable product name |
| `InventoryGrid_AddQtyByCoid` | Alias OK |
