# Dual tighten: `0x00531b00` CVOGMission_AddActiveObjective

| Field | Value |
|-------|-------|
| Stable ID | `aa_00531b00` |
| Address | `0x00531b00` |
| Owner | dual tighten only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior dual | A/B accept-with-gaps — open: **helpers / +0x548**, **arity vs GiveMission**, **noreturn delete** |

## Residual questions (from prior dual A/B)

1. Does this body (or its helpers) touch character mission hash **`+0x548`**?
2. Call arity vs GiveMission decompile showing one explicit arg
3. `operator_delete` Ghidra noreturn — fall-through or abort?
4. Bodies of `FUN_0053c460` / `FUN_0053c760`

## Seal: insert targets = **+0x548** (def) and **+0x55c** (state node)

| Evidence | Location | Confidence |
|----------|----------|------------|
| Before `FUN_0053c460`: `MOV EBP,ECX` then `MOV ECX,dword ptr [EBP+0x548]` | asm `0x00531b0b`–`0x00531b0d` | **High** |
| Push order for def insert: `push 0; push ESI=objDef; push EAX=objId; call FUN_0053c460` with `ECX=hash` | asm `0x00531b09`–`0x00531b15` | **High** |
| Before `FUN_0053c760`: `MOV ECX,dword ptr [EBP+0x55c]` | asm `0x00531b6f` | **High** |
| Push order for state insert: `push 0; push EBX=node; push EAX=objId; call FUN_0053c760` | asm `0x00531b68`–`0x00531b77` | **High** |
| `missionState.md`: `+0x548` active objectives; `+0x55c` pending objectives | layout doc | **High** (layout); matches asm |
| Callee bodies are twin hash-insert thiscalls: `(hash*, key, value, char skipIfExists)` | decompile `0x0053c460` / `0x0053c760` | **High** role |
| Node vtable differs: `PTR_LAB_009cf02c` vs `PTR_FUN_009cefec` | callees | **High** (distinct node types) |

**Conclusion:** Plate “hash (+0x548)” is **body-backed via ECX load**, not a direct store. Def registration → active-objectives hash **`char+0x548`**. Runtime state node → pending hash **`char+0x55c`**. Ghidra decompile of this unit **drops** both hash `this` pointers.

## Seal: call arity = **thiscall(character, objectiveDef)**

| Caller | Asm | Confidence |
|--------|-----|------------|
| GiveMission `0x005328b2` | `PUSH EAX` (obj def); `MOV ECX,EBP` (character); `CALL 0x00531b00` | **High** |
| CompleteObjective `0x005348a2` | `PUSH EBX` (obj); `MOV ECX,ESI` (character); `CALL 0x00531b00` | **High** |
| Prologue of this unit | `param_1` in **ECX**; `param_2` on **Stack[0x4]** (analyze_function_complete) | **High** |

**Conclusion:** Prior “GiveMission single-arg” tension was **Ghidra dropping ECX** at the call site. Two-param thiscall is sealed. GiveMission clean still declares one-arg form — **cross-unit doc debt**, not a bug in this unit.

## Seal: delete path **falls through** (not noreturn)

| Evidence | Location | Confidence |
|----------|----------|------------|
| `TEST EAX,EAX` / `JZ skip_delete` / `CALL operator_delete` / label continues into evaluator loop | asm `0x00531b7c`–`0x00531b8f` | **High** |
| Dirty `OR dword ptr [EBP+0x634],0x10` after loop always (when evaluators non-empty) | asm `0x00531bbf` | **High** |

**Conclusion:** Ghidra WARNING “does not return” is a decompiler artifact. Real control flow: on nonzero insert result, free node, **still** run evaluator vtable `+0x28` with possibly-freed `stateNode`, then dirty. UAF hazard on failed insert is **crash-parity** of the original, not fixed here. Annotated prior “else only setup” nesting was **wrong** vs raw/asm.

## Helper return codes (context only; not this unit’s responsibility to rename)

From `FUN_0053c460` / twin `FUN_0053c760` decompile:

| Code | Meaning |
|------|---------|
| `0` | insert ok |
| `1` | skip-if-exists and key already present (`param_4 != 0`) |
| `0x80004003` | null value |
| `0x80004005` | duplicate key (log `"Duplicate hash insert %u, failing out"`) |

This unit always passes `param_4 = 0`, so code `1` path unused here. First insert (`FUN_0053c460`) **return ignored**. Second insert failure → delete + continue.

## Re-decompile check (Ghidra 2026-07-29)

Pseudocode body ≡ 2026-07-23 raw (same branches). New seals are from **call-site / helper asm**, not CF rewrite of the dropped-this decompile.

## Residual after this pass

| Item | Status |
|------|--------|
| Hash targets +0x548 / +0x55c | **Sealed High** (asm ECX) |
| Arity thiscall(char, objDef) | **Sealed High** (two callers + prologue) |
| Delete fall-through | **Sealed High** |
| Helper role = CNDHash insert (distinct vtables) | **High** role; original names open |
| Evaluator vtable +0x28 English name | Open (Probable “setup/bind”) |
| Dirty `+0x634\|=0x10` consumers | Open (shared with FailMission) |
| Runtime / bit-exact / image diff | Open |
| GiveMission clean one-arg declaration | Cross-unit residual (not owned here) |

## Artifacts touched

- This scratch: `reviews/a_00531b00.md`
- Duals: `A_aa_00531b00_*`, `B_aa_00531b00_*`
- Clean, annotated, function record; raw append (asm note)
- **Not** WORK_QUEUE / RESUME
