# Review B (skeptical / adversarial): `aa_007e1d80` FUN_007e1d80

| Field | Value |
|---|---|
| **Stable ID** | `aa_007e1d80` |
| **VA** | `0x007e1d80` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_007e1d80_FUN_007e1d80.md` |
| **Tools** | Ghidra decompile + `read_memory` + globals audit only (no Launcher) |
| **Verdict** | **accept-with-gaps** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | This is a Win32 critical section enter | Nearby string `Left CriticalSection` exists in rodata near `Cnxn Str` | **Falsified for this body** — no `EnterCriticalSection`; body is connection/refcount |
| 2 | Function returns HRESULT of open | Name "PrepareDataConnection" / COM hr locals | **Falsified as caller contract** — success and fail paths return **0**; ensures ignore EAX |
| 3 | Refcount is only on cold open | Fast path still useful | **Falsified** — `LAB_007e1f8e` always `+1` on `this+0x20` including re-enter |
| 4 | `DAT_00d1793c` means "already loaded table" | Shared with ensures | **Falsified** — when set, **skips DB open** entirely; table loaders use it for MSXML xpath path, not table-ready flags |
| 5 | Fail always pairs with leave | Ensure wrappers always call leave after | **Survives for ensure pattern** — but **this unit alone** can return 0 after failed open without bump; leave would still decrement → risk of underflow if mis-paired. Ensures call enter→load→leave unconditionally, so leave always runs |
| 6 | `FUN_00423370` second arg 0 means "no extra props" | Decompile `param_2` gates VariantInit path | **Probable** — not fully sealed without IIDs |
| 7 | Version must be 9 forever | Magic `0x9` compare | **High for this build** — not a portable product rule without more builds |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Enter/leave pairing for Ensures | **High** | Double-free / stuck connection |
| Offsets `+0x20` refcount, `+0x24`/`+0x28` COM | **High** | Wrong leave order releases |
| Return value ignored by Ensures | **High** | False "open failed" ports |
| Error severity of `FUN_007a4480(1, ...)` | **Medium** | May be log-only (same as NDError path) |
| Need for AutoCore server parity | **Low** | Server tables not via this client DBReader |

---

## 3. Cross-check against ensure wrappers

Asm-sealed ensure skeleton (credits/XP family):

```
mov  ecx, [0x00b0469c]
call FUN_007e1d80          ; this unit
mov  ecx, [0x00b0469c]
push &map
call LoadXxxFromWad
mov  ecx, [0x00b0469c]
mov  esi, eax
call FUN_007b7df0          ; leave twin
test esi, esi / jge ret / VOG_DEBUG_STOP
```

So enter/leave **this pointer is always the same global ctx**, not the map root. Decompiler views that omit ECX on Load/Ensure under-state the ABI.

---

## 4. Surviving contract for AutoCore

```
Client table ensure bookend:
  ctx = g_pDbReaderCtx  // 0x00b0469c
  DBReader_Enter(ctx)   // FUN_007e1d80: refcount++ ; cold open if needed
  status = LoadTable(ctx, &mapRoot)
  DBReader_Leave(ctx)   // FUN_007b7df0: refcount-- ; release COM at 0

AutoCore server: do NOT port ADO OpenFromInitializationString.
  Port only the loaded table semantics (row fields) for mission XP/credits.
```

---

## 5. Open questions

1. Underflow if leave called without enter (debug only?).
2. Exact meaning of version `9` vs retail content DB.
3. Whether nested enter during load is required because load re-enters.

**Verdict:** **accept-with-gaps**
