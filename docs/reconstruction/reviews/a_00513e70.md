# Dual residual: `0x00513e70` Item_IsStackEligible

| Field | Value |
|-------|-------|
| Stable ID | `aa_00513e70` |
| Address | `0x00513e70` |
| Canonical name (proposed) | `Item_IsStackEligible` (was `FUN_00513e70`) |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior state | scaffold raw/annotated/clean only (2026-07-23); coverage `full` name only |

## Residual questions (this pass)

1. Is this **stack capacity** (qty room) or **stack eligibility** (may attempt merge)?
2. Exact boolean on blob `+0x3f2` bit `0x40`, item `+0x17c` bit 19, type `+0x38==4`.
3. Does a separate **broken helper** (getter) exist, or is broken inlined?
4. Calling convention / `this` identity.

## Live evidence this pass

| Source | Result |
|--------|--------|
| Ghidra `decompile_function` / `batch_decompile` @ `0x00513e70` | Body ≡ raw 2026-07-23 capture |
| Ghidra `read_memory` @ `0x00513e70` (64 B) | Full body through `mov eax,1; ret` + `CC` pad; **no callees** |
| Ghidra `batch_decompile` @ `0x0040abf0` | **Stack capacity** peer (qty + delta ≤ max) — **not** this unit |
| Ghidra `batch_decompile` @ `0x00513de0` | **Broken setter** (`ItemFlag_Broken` xor `0x80000`) — sibling, not this unit |
| Ghidra `batch_decompile` @ `0x00513e30` | Different flag setter (bit **20** / mask `0x100000`) |
| Prior dual seals | `ItemFlag_Broken` (a_005711c0); FindFreeForItem stack gate (a_005714e0) |
| Not performed | `disassemble_bytes`, Launcher, CE runtime, bit-exact image diff |

---

## Seal: identity — **eligibility**, not capacity

```text
// __fastcall / single-arg this-in-ECX (MSVC ECX item*)
// bool Item_IsStackEligible(Item* item)   // ECX
// returns 1 / 0 in AL/EAX; plain `ret` (0 stack args)

// Paths (from bytes @ 0x00513e70):
//   holder = *(item+0xa8)
//   blob   = *(holder+0x3c)
//   stackFlag = (blob[+0x3f2] >> 6) & 1     // mask 0x40 on the byte
//   if stackFlag:
//     if !(item[+0x17c] bit 19): return 1   // not Broken → eligible
//   if *(holder+0x38) == 4: return 1        // type class 4 always eligible
//   return 0
```

**Equivalent closed form (return 1):**

```text
typeClass(item) == 4
  OR  ( (cloneblob[+0x3f2] & 0x40) != 0  AND  !ItemFlag_Broken(item) )
```

**Return 0:**

```text
typeClass != 4  AND  ( stackFlag clear  OR  Broken set )
```

### Asm map (read_memory hex, image base 0x400000)

| VA | Bytes | Meaning |
|----|-------|---------|
| `00513e70` | `8b 81 a8 00 00 00` | `mov eax,[ecx+0xa8]` holder |
| `00513e76` | `8b 50 3c` | `mov edx,[eax+0x3c]` blob* |
| `00513e79` | `8a 92 f2 03 00 00` | `mov dl,[edx+0x3f2]` |
| `00513e7f` | `c1 ea 06` / `f6 c2 01` | bit 6 → test stack flag |
| `00513e85` | `74 0e` | flag **clear** → type-4 test |
| `00513e87` | `8b 89 7c 01 00 00` | `mov ecx,[ecx+0x17c]` flags dword |
| `00513e8d` | `c1 e9 13` / `f6 c1 01` | bit 19 Broken |
| `00513e93` | `74 09` | Broken **clear** → return 1 |
| `00513e95` | `83 78 38 04` | `cmp [eax+0x38],4` |
| `00513e99` | `74 03` | type==4 → return 1 |
| `00513e9b` | `33 c0` / `c3` | return 0 |
| `00513e9e` | `b8 01 00 00 00` / `c3` | return 1 |
| `00513ea4` | `cc…` | pad; function end |

**Size:** `0x00513e70`–`0x00513ea3` inclusive (`0x34` bytes). **Callees:** none.

### What is sealed vs open

| Claim | Status |
|-------|--------|
| Predicate only; no capacity / no qty math | **Sealed High** |
| Stack capacity lives at `FUN_0040abf0` / `aa_0040abf0` | **Sealed High** (separate unit; max 999999 type4 / blob+0x4ba else 200 default) |
| Blob `+0x3f2` bit `0x40` required for non-type-4 eligibility | **Sealed High** |
| Item `+0x17c` bit 19 **blocks** eligibility when set | **Sealed High** (same `ItemFlag_Broken`) |
| Type holder `+0x38 == 4` → always 1 (bypass flag/broken) | **Sealed High** CF |
| ECX = item; 0 stack args; plain `ret` | **Sealed High** |
| Product name of type class 4 | **Open** (currency/quest/etc. interpretive) |
| Original PDB symbol for this function | **Open** |
| Runtime / bit-exact | **Open** |

---

## Seal: **no separate broken getter helper** in this unit

| Addr | Role | Relation to `0x00513e70` |
|------|------|---------------------------|
| `0x00513e70` | Eligibility: **inlines** Broken test | This unit |
| `0x00513de0` | **Setter** for Broken (`xor` mask `0x80000`; optional dirty `FUN_00512670`) | Sibling flag mutator — **not** a callee of this unit |
| `0x00513e30` | Setter for **bit 20** (`0x100000`) | Unrelated flag |
| — | Pure `IsBroken()` getter | **Not found** as dedicated function used here; callers test bit inline or via this eligibility wrapper |

**Sealed:** “Broken helper” for **read** in the stack path is **this function’s inline bit test**, not a separate call. Dedicated **write** helper is `0x00513de0` (owned/sealed under `ItemFlag_Broken` residual, not re-owned here).

---

## Seal: wave-map mislabel correction

| Source | Label | Correction |
|--------|-------|------------|
| `WAVE_2026-07-29_wave4_partition_map.md` | “Stack capacity helper” | **Wrong product role** — capacity = `0x0040abf0` |
| `a_008151a0.md` residual note on `FUN_00513e70` | “1 iff (flag **or** broken) **and** type==4” | **Falsified** — see closed form above (Broken **blocks**; type4 **ORs** success) |

Call-site roles that remain valid (CF only):

- `InventoryGrid_FindFreeForItem` stack branch gate
- `Inventory_SerializeAddItemPacket` free-slot vs stack-scan
- `Client_RecvInventoryAddItem` loot `xN` gate + `FUN_00945540` stack scan
- Deposit / qty display paths that multiply by stack qty when true

---

## Residual after this pass

| Item | Status |
|------|--------|
| Eligibility vs capacity | **Sealed High** — eligibility only |
| Boolean closed form | **Sealed High** (bytes + decompile) |
| Broken: inline read; setter `0x00513de0` | **Sealed High** (no getter helper call) |
| Type-4 product taxonomy | **Open** |
| Original name | **Open** |
| Runtime / image diff | **Open** |

## Artifacts touched by residual owner

- This scratch: `docs/reconstruction/reviews/a_00513e70.md`
- Duals: `A_aa_00513e70_Item_IsStackEligible.md`, `B_aa_00513e70_Item_IsStackEligible.md`
- Function record / annotated / clean under `aa_00513e70`
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
- **Not** Launcher
