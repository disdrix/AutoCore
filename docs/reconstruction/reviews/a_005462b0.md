# Dual residual: `0x005462b0` CVOGCharacter_CheckMissionRequirements

| Field | Value |
|-------|-------|
| Stable ID | `aa_005462b0` |
| Address | `0x005462b0` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior dual | A/B accept-with-gaps (2026-07-28) — open: prereq mode English, FUN helpers, third-arg ABI |

## Residual questions (from dual A/B)

1. Retail English for mission `+0x120` / `+0x124`
2. Identity of `FUN_0052ada0` / `FUN_005405e0`
3. Whether third formal exists (`unaff_ESI` / stack1)
4. Map of `+0x104`/`+0x108`/`+0x118` to WAD / `Mission.cs` names

## Seal: ABI = **2-arg thiscall only** (no third formal)

| Evidence | Detail | Conf |
|----------|--------|------|
| Epilogue | fail/success paths `RET 4` (one stack dword) | **High** |
| Call site `SearchAutoMissions` `0x00532cd3` | `PUSH EBP` / `MOV ECX,ESI` / `CALL` — single push | **High** |
| Call site `EvalOfferable` `0x004d5ba0` | same one-push pattern | **High** |
| Call sites in `FUN_004d2e50` / `FUN_004d5c10` | one push (EBP or EDI) + ECX=def | **High** |
| Decompiler stack1 | never loaded in body | **High** |

**Conclusion:** Canonical ABI is  
`int __thiscall CheckMissionRequirements(MissionDef* this, Character* character)`.  
Decompiler third formal / `unaff_ESI` is **phantom**. Clean signature must drop unused stack1.

## Seal: prereq modes = **RequirementsOred / RequirementsNegative**

WAD/`Mission.Read` layout anchors Id@0 → race@`+0x90` (math below). Sequential fields:

| Client offset | WAD / `Mission.cs` name | Role |
|---------------|-------------------------|------|
| `+0x120` | `RequirementsOred` | non-zero → OR of nonempty prereqs; 0 → AND-all |
| `+0x124` | `RequirementsNegative` | non-zero → invert pass sense |

Cross-check: `NpcInteractHandler` / `MissionWorldPhaseRules.MeetsMissionPrerequisites` already document Ored; Freelancer/Shields Up use Ored=`-1`.

### Truth table (body CF, sealed)

When at least one `+0x9c` slot ≠ `0xFFFFFFFF` (`bAnyPrereq`):

| `RequirementsNegative` (`+0x124`) | `RequirementsOred` (`+0x120`) | Fail when | Return on fail |
|-----------------------------------|-------------------------------|-----------|----------------|
| `0` | `0` | `!allPass` | `firstFailId` |
| `0` | ≠0 | `!anyPass` | `firstFailId` |
| ≠0 | `0` | `allPass` | `-1` |
| ≠0 | ≠0 | `anyPass` (≡ `allPass \|\| anyPass`) | `-1` |

If no nonempty prereq slots → skip block → `0` (earlier gates passed).

**Server gap (not this unit):** `MeetsMissionPrerequisites` implements Ored only; **Negative not ported**.

## Seal: field names for `+0x104` / `+0x108` / `+0x118`

Offset walk from Id@0 (matches race@`+0x90`):

| Offset | `Mission.cs` / WAD | Check body use |
|--------|--------------------|----------------|
| `+0x104` | `Discipline` | type id; skip if `< 0` |
| `+0x108` | `DisciplineValue` | threshold; balance must be `>=` |
| `+0x118` | `RequirementEventId` | skip if `-1`; must be **present** in active-event vector |

Plate label “currency” for `+0x104/+0x108` is **role-approximate** (same balance map as item costs) but **product field names are Discipline / DisciplineValue**.

## Seal: `FUN_0052ada0` role (image bytes @ `0x0052ada0`)

```
MOV ESI, ECX
LEA ECX, [ESI+0x584]     ; map header
CALL map_find (FUN_00418b80) with key = stack currency/type id
CMP result, [ESI+0x588]  ; end sentinel
JZ  → return -1
MOV EAX, [node+0x10]     ; balance value
RET 4
```

Call site inside Requirements (`0x00546370`):

```
PUSH [mission+0x104]   ; type
MOV  ECX, EBP          ; character (stack0)
CALL FUN_0052ada0
CMP  EAX, [mission+0x108 threshold in EDI]
```

| Claim | Conf |
|-------|------|
| `__thiscall(Character*, typeId) → int` | **High** (RET 4 + ECX=char) |
| Map @ char `+0x584`, end `+0x588`, value @ node `+0x10` | **High** |
| Missing key → `-1` | **High** |
| Product name | **Open** — role: **resource/currency balance by type id**; leave `FUN_*` unless string/RTTI |

## Seal: `FUN_005405e0` role (image @ `0x005405e0`)

Linear scan global range `[DAT_00b04718, DAT_00b0471c)` for `*p == RequirementEventId`; return `1` if found else `0`. **Sole xref** = this function. Call site: `PUSH id; CALL; ADD ESP,4` (cdecl).

| Claim | Conf |
|-------|------|
| Membership test of active event ids | **High** |
| Argument = mission `+0x118` = `RequirementEventId` | **High** |
| Product name | **Open** — role sealed; leave `FUN_*` |

## Level vtbl `+0x27c`

Unchanged: slot **High** (two call sites min/max); English GetLevel **Probable** (shared with XpToReachRelativeLevel dual).

## Residual after this pass

| Item | Status |
|------|--------|
| Third formal / stack1 | **Sealed High** — does not exist (RET 4) |
| `+0x120` / `+0x124` English | **Sealed High** = RequirementsOred / RequirementsNegative |
| Prereq truth table | **Sealed High** (CF + names) |
| `+0x104/+0x108/+0x118` product fields | **Sealed High** = Discipline / DisciplineValue / RequirementEventId |
| `FUN_0052ada0` role + ABI | **Sealed High**; product symbol open |
| `FUN_005405e0` role | **Sealed High**; product symbol open |
| GetLevel name for `+0x27c` | Probable (unchanged) |
| Runtime / bit-exact / image diff | Still open (matrix policy) |
| Server Negative port | Open (AutoCore port gap, not client CF) |

## Artifacts touched

- This scratch: `reviews/a_005462b0.md`
- Duals: `A_aa_005462b0_*`, `B_aa_005462b0_*`
- Function record + clean plate comments
- `VERIFICATION_MATRIX.md` open-issues cell + Last reviewed
- `CHANGE_LOG.md` entry
- **Not** WORK_QUEUE / RESUME
