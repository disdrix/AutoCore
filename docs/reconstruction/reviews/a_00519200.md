# Dual quality refresh: `0x00519200` Skill_StartCastAgainHeartbeat

| Field | Value |
|-------|-------|
| Stable ID | `aa_00519200` |
| Address | `0x00519200` |
| Owner | dual A/B residual only (**no** WORK_QUEUE / RESUME / CHANGE_LOG) |
| Date | 2026-07-29 (residual strengthen pass) |
| Prior dual | A/B accept-with-gaps (quality refresh earlier 2026-07-29) |
| Tools | Ghidra `decompile` + `read_memory` + `get_function_xrefs` + `get_assembly_context` + pcode (no `disassemble_bytes`) |

## Residual questions entering this pass

1. Full caller set completeness (was 4–5 sites cited; matrix open on double-start)
2. Per-caller charge-delay wiring (esp. non-client paths)
3. Null-`operator_new` path (enqueue/start with null?)
4. Re-seal ABI / type-8 / list from **raw bytes** (not prior notes alone)
5. Ghidra plate / decomp ctor arity still wrong?

---

## Seal: body machine bytes (High) — `read_memory` @ `0x00519200` len 128

Body range: **`00519200`–`00519279`** (function info). Trailing `CC` pad after `C2 08 00`.

Decoded control flow (bytes authority):

| Offset | Bytes | Meaning |
|--------|-------|---------|
| +0x00 | `6A FF` / `68 51 36 9A 00` / FS:[0] frame | SEH |
| +0x15 | `6A 28` | `push 0x28` → alloc size |
| +0x17 | `8B F9` | `mov edi, ecx` — **owner entity** saved |
| +0x19 | `E8 …` → `operator_new` | `00489892` |
| +0x21 | `33 F6` / `3B C6` / `74 14` | `esi=0`; **je skip ctor** if null |
| +0x2A | `8B 4C 24 20` / `8B 54 24 1C` | load stack1 / stack0 |
| +0x32 | `51 52 57` / `8B C8` / `E8 …` | **push charge; push skill; push owner; ecx=mem; call ctor** `0051e240` |
| +0x3E | `8B F0` | `esi = hb` (or remains 0 if skipped) |
| +0x40 | `8B 87 A4 00 00 00` | `eax = [edi+0xa4]` world |
| +0x46 | `8B 88 EC E4 00 00` | `ecx = [eax+0xe4ec]` HB list |
| +0x4C | `56` / `E8 …` Enqueue | **always** — no second null check |
| +0x58 | `8B CE` / `E8 …` Start | **always** — `ecx = esi` (may be 0) |
| +0x74 | **`C2 08 00`** | **`RET 0x8`** — exactly **2** stack dwords |

### Sealed from body alone

| Fact | Conf |
|------|------|
| `__thiscall` + **`RET 0x8`** | **High** |
| Alloc size **0x28** | **High** |
| Ctor arity **3 stack** (owner, skill, charge) before `CALL 0051e240` | **High** |
| World via **`owner+0xa4`**, list **`+0xe4ec`** | **High** |
| CF: new → ctor(if) → Enqueue → Start; no send; no duration math | **High** |
| **No** type-8 write in this unit | **High** |
| **No** existing-HB lookup / dedup | **High** |
| Null alloc: still **Enqueue(list, 0)** + **Start(0)** | **High** (merge after `je`; no early ret) |

Ghidra plate still: `(this, pOwnerCtx, pSkill, nChargeDelayMs)` with 3 stack formals — **falsified** by `C2 08 00`. Decomp ctor call still drops skill arg — **falsified** by three pushes + ctor `RET 0xC`.

---

## Seal: ctor type-8 + `RET 0xC` (cite only; owned by `aa_0051e240`)

`read_memory` @ `0x0051e340` region:

```
C7 46 1C 08 00 00 00    mov dword ptr [esi+0x1c], 8
…
C2 0C 00                ret 0xc
```

| Fact | Conf |
|------|------|
| HB type field **`+0x1c = 8`** | **High** (ctor) |
| Ctor cleans **12** stack bytes | **High** |

---

## Seal: complete xref set (High) — 6 unconditional calls

| # | Caller | Call VA | ECX | stack0 | stack1 (charge) | Entry gate (caller-owned) |
|---|--------|---------|-----|--------|-----------------|---------------------------|
| 1 | `Client_RequestCastSkill` | `0x00941731` | entity (table LEA) | skill ESI | `movsx` **skill+0x14** | `skill+0x61c == 0` (+ prior ClearActiveCast) |
| 2 | `Client_QuickBarActivateSkillSlot` | `0x00921cfd` | entity (table LEA) | skill ESI | `movsx` **skill+0x14** | `skill+0x61c == 0` |
| 3 | `Client_StanceOrGadgetActivatePath` | `0x0093a525` | entity (table LEA) | skill EDI | `movsx` **skill+0x14** | `skill+0x61c == 0` |
| 4 | `Skill_ClearCastBindingAndMaybeRestartCd` | `0x0051a77c` | owner ESI | skill (vtbl+0x234) | **imm 0** | `skill+0x61c != 0` |
| 5 | `Skill_ApplyStatusEffectLocal` | `0x0051ac53` | caster ESI | skill EBX | **packet+0x10** | `world+0x7e != 0` **and** `skill+0x61c == 0` |
| 6 | `Skill_LocalRangeTargetCheck` | `0x005531ac` | owner EDI | skill ESI | **skill+0x10** (dword) | accuracy miss path: COMISS fail + `[esp+0x14]==0`; then **return 0x0f** |

### Call-site byte seals (selected)

**ApplyStatusEffectLocal** (`read_memory` @ `0x0051ac37`):

```
MOV EAX,[ESI+0xa4]
CMP byte [EAX+0x7e], 0
JZ  skip          ; require world+0x7e != 0
CMP byte [EBX+0x61c], 0
JNZ skip          ; require skill+0x61c == 0
MOV EDX,[EDI+0x10]
PUSH EDX          ; charge = packet+0x10
PUSH EBX          ; skill
MOV ECX,ESI
CALL 00519200
```

**LocalRangeTargetCheck** (`read_memory` @ `0x00553195` region):

```
COMISS XMM0, [ESI+0x108]
JBE  other_path
CMP byte [ESP+0x14], 0
JNZ  no_hb        ; skip HB if flag nonzero
MOV ECX,[ESI+0x10]
PUSH ECX          ; charge = skill+0x10  (!= client +0x14)
PUSH ESI          ; skill
MOV ECX,EDI
CALL 00519200
MOV EAX, 0x0f     ; miss code after optional HB
RET 0xc
```

### Charge-delay taxonomy (High)

| Source class | Offset / value | Callers |
|--------------|----------------|---------|
| Client charge word | skill **`+0x14`** (`movsx`) | RequestCast, QuickBar, StanceOrGadget |
| Restart zero | **0** | ClearCastBindingAndMaybeRestartCd |
| Packet field | packet **`+0x10`** | ApplyStatusEffectLocal |
| Skill cooldown dword | skill **`+0x10`** | LocalRangeTargetCheck (accuracy-miss) |

**Disposition:** prior dual said “often skill+0x14; restart 0; apply packet+0x10” — **confirmed**, and **extended** with LocalRange **`skill+0x10`** (was under-cited on this unit’s dual).

---

## Seal: stacking / double-start (this unit vs callers)

| Layer | Finding | Conf |
|-------|---------|------|
| This body | Always `new(0x28)` → enqueue → start; **no** type-8 scan | **High** |
| Client entry | Common gate `skill+0x61c == 0` before call | **High** (3 client sites) |
| ClearCastBinding | Opposite: calls when `+0x61c != 0`, charge 0 | **High** |
| Apply (sim) | `world+0x7e != 0` and `+0x61c == 0` | **High** |
| LocalRange miss | Flag `[esp+0x14]==0` only; **no** `+0x61c` check at call site | **High** (asm) |
| Live multi-HB same skill | Not observable from static | **Open** |
| Whether Enqueue/Start(null) crashes | Depends on callees | **Open** (not this body) |

Stacking open item remains **narrowed**: no in-unit guard; caller gates **partially** documented; LocalRange miss path is an extra entry without `+0x61c`.

---

## Callees (this unit only)

| VA | Name |
|----|------|
| `0x00489892` | `operator_new` |
| `0x0051e240` | `CVOGHBOKToCastAgain_ctor` |
| `0x005078f0` | `CVOGHBList_Enqueue` |
| `0x005081c0` | `CVOGHBBase_Start` |

---

## Asm-sealed signature (unchanged; re-proved)

```c
void __thiscall Skill_StartCastAgainHeartbeat(
    void* pOwnerEntity,    // ECX; +0xa4 → world
    void* pSkillRuntime,   // stack0
    int   nChargeDelayMs); // stack1 (caller-dependent source)
// RET 0x8
```

Ctor (callee contract used by this unit):

```c
void* __thiscall CVOGHBOKToCastAgain_ctor(
    void* thisHb, void* pOwnerEntity, void* pSkillRuntime, int nChargeDelayMs);
// RET 0xC; HB+0x1c = 8; duration at HB+0x8 in ctor
```

---

## Residual after strengthen pass

| Item | Status |
|------|--------|
| ABI `RET 0x8`, (owner, skill, chargeMs) | **Sealed High** (re-proved bytes) |
| Alloc **0x28**, list `*(owner+0xa4)+0xe4ec` | **Sealed High** |
| Type **8** via ctor `+0x1c` | **Sealed High** (ctor bytes) |
| Null-alloc still Enqueue/Start | **Sealed High** |
| **6** callers complete xref set | **Sealed High** |
| Per-caller charge sources (incl. LocalRange `+0x10`) | **Sealed High** |
| In-unit stacking guard | **Sealed: none** |
| Caller double-start policy completeness | **Partial** — gates known at sites; live policy open |
| LocalRange `[esp+0x14]` flag meaning | **Open** (caller-owned) |
| Duration / category map | Ctor-owned — not this unit |
| Runtime / bit-exact / image diff | Open (matrix policy) |
| Original PDB symbol | Unknown |
| Enqueue/Start null safety | Open (callee) |

**Verdict held:** **accept-with-gaps** — residual strengthened; no new body unknowns that block port of this unit.

## Artifacts

- Scratch: this file
- Dual A/B: `reviews/A_aa_00519200_*`, `reviews/B_aa_00519200_*`
- Function record, annotated raw append, clean header note
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
