# Review B (skeptical / adversarial): `aa_004188e0` FUN_004188e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004188e0` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_004188e0_FUN_004188e0.md` |
| **Verdict** | **accept-with-gaps** |

---

## 1. Claims under attack

| # | Claim | Attack | Outcome |
|---|---|---|---|
| 1 | Soft-fails on bad iterator | Throws **invalid map/set iterator** | **Falsified soft-fail** |
| 2 | Skill-only erase | Multiple non-skill callers | **Falsified exclusive** |
| 3 | Only unlinks without rebalance | Black-node loop + rotations | **Falsified unlink-only** |
| 4 | Frees whole map | Deletes **one node** | **Falsified** |
| 5 | Ghidra unreachable = dead code wrong CF | Folded RB dual-color cases | **Survives as noise** — do not “fix” via those |
| 6 | Returns remaining CD time | No return value of interest; container only | **Falsified** |
| 7 | Safe concurrent erase | No locks in body | **Caller responsibility** |
| 8 | Line-by-line RB ≡ MSVC source sealed | Large; not fully asm-audited this batch | **Survives residual** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Throw string + nil check | **Confirmed** | Silent corruption vs exception |
| Erase + delete one node | **High** | Leak / double-free |
| Used to drop expired category CD | **High** | Stale recharge 7 forever |
| Full RB correctness proof | **Medium** | Rare tree corruption edge |
| Product name | **Open** | Naming |

---

## 3. Surviving contract for AutoCore

```
// GetCategoryCooldownRemaining after lower_bound hit when now >= start+duration:
FUN_004188e0(map, /*scratch*/, it);  // erase expired category CD node
// Do not treat as skill authority. Shared STL-like map erase.
// Invalid it → C++ exception with "invalid map/set<T> iterator".
```

---

## 4. Open questions

1. Seal `param_2` against call-site asm (unused?).
2. Optional full RB dual against known MSVC 7.1/8 map erase.
3. Runtime: expire mid-cast category CD once.

**Verdict:** **accept-with-gaps** — erase contract + skill CD consumer sealed; exhaustive RB proof residual.
