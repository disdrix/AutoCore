# Dual residual: `0x0051e390` CVOGHBOKToCastAgain_OnStart

| Field | Value |
|-------|-------|
| Stable ID | `aa_0051e390` |
| Address | `0x0051e390` |
| Owner | dual residual only (**no** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 |
| Prior dual | A/B accept-with-gaps (2026-07-23) — open: SetIsCastingFlag ABI/width; +0x18 owner vs skill; `unaff_SI` |
| Live tools | Ghidra `decompile_function` + `read_memory` body/callee/vtbl (no disassemble_bytes) |

## Residual questions (from prior dual)

1. `Skill_SetIsCastingFlag` ABI / flag width
2. Whether HB `+0x18` is skill or character (plate said skill at `+0x24`)
3. Decomp `unaff_SI` third arg on flag call
4. B open: StartCastAgainHeartbeat alloc `0x28` cross-check
5. Seal flag setter side effects used by this site (true path)

## Seal: body bytes (High)

```
0051e390  56             push esi
0051e391  8B F1          mov  esi, ecx            ; this = pHeartbeat
0051e393  83 7E 18 00    cmp  dword [esi+0x18], 0
0051e397  74 0A          je   skip_flag           ; +0x0A → 0051e3a3
0051e399  8B 4E 24       mov  ecx, [esi+0x24]     ; skill → thiscall ECX
0051e39c  6A 01          push 1                   ; char flag = 1
0051e39e  E8 2D1F0300    call Skill_SetIsCastingFlag  ; → 0x005502d0
0051e3a3  8B CE          mov  ecx, esi            ; restore HB this
0051e3a5  5E             pop  esi
0051e3a6  E9 C59EFEFF    jmp  CVOGHBBase_StampLastFireTime  ; → 0x00508270 (tail)
```

| Claim | Evidence | Conf |
|-------|----------|------|
| `__fastcall`/`__thiscall` ECX = HB; **no stack formals** | prolog `mov esi,ecx`; no arg pops | **High** |
| Gate on **`HB+0x18 != 0`** | `cmp [esi+0x18],0` / `je` | **High** |
| Flag target **`HB+0x24`** (skill) as ECX | `mov ecx,[esi+0x24]` | **High** |
| Flag value **immediate 1** (byte) | `push 1` | **High** |
| Stamp **always** (even if gate skip) | fallthrough after je target | **High** |
| Tail-call stamp (no local RET) | `jmp 0x00508270` | **High** |
| `unaff_SI` third formal | **Falsified** — ESI is saved `this`, not pushed | **High** |

## Seal: `Skill_SetIsCastingFlag` as used here (High)

Callee `0x005502d0` bytes:

```
8A 44 24 04              mov  al, [esp+4]         ; char bIsCasting
84 C0                    test al, al
88 81 28 06 00 00        mov  [ecx+0x628], al     ; skill+0x628 = flag byte
74 16                    je   ret_if_false
0F 57 C0                 xorps xmm0, xmm0
F3 0F 11 81 70 01 00 00  movss [ecx+0x170], xmm0  ; skill+0x170 = 0.0f
A1 CC 41 B0 00           mov  eax, [0x00b041cc]   ; g_dwClientTickMs
89 81 78 01 00 00        mov  [ecx+0x178], eax    ; skill+0x178 = tick
C2 04 00                 ret  4                   ; one stack dword
```

| Claim | Conf |
|-------|------|
| `__thiscall` skill in ECX; **1** stack arg (`char`); **`RET 4`** | **High** |
| Flag storage = **byte** at **`skill+0x628`** | **High** |
| OnStart pushes **1** → true path | **High** |
| True path: clear **`+0x170`** as **float 0** (`movss`), stamp **`+0x178`** = `g_dwClientTickMs` | **High** |
| False path would leave `+0x178` untouched (not this site) | **High** (body) |
| Decomp 3-arg / `(void*)0x1` / `unaff_SI` | **Falsified** for this call shape |

## Seal: `CVOGHBBase_StampLastFireTime` (High)

```
00508270  A1 CC41B000    mov eax, [0x00b041cc]
00508275  89 41 14       mov [ecx+0x14], eax      ; HB+0x14 = last fire tick
00508278  C3             ret
```

| Claim | Conf |
|-------|------|
| Single store **`HB+0x14 ← g_dwClientTickMs`** | **High** |
| No period / counter mutation here | **High** |
| Plate “ResetStartTick” name | **Misnomer** — body is stamp last-fire @ `+0x14` |

## Seal: HB slots for this unit (High)

| Offset | Role in OnStart | Source of meaning | Conf |
|--------|-----------------|-------------------|------|
| `+0x18` | **Gate only** — must be non-null to set casting | `AttachOwnerObject` store; ctor residual attaches **owner entity** (not skill) | **High** gate; **High** owner-vs-skill via sibling `aa_0051e240` |
| `+0x24` | **Skill runtime** thiscall target for flag | ctor `HB+0x24 = skill`; OnStart load | **High** |
| `+0x14` | Last-fire tick written by stamp | StampLastFireTime body | **High** |
| (skill)`+0x628` | Cast flag byte = 1 | SetIsCastingFlag | **High** |
| (skill)`+0x170` / `+0x178` | 0.0f / tick on true | SetIsCastingFlag true path | **High** |

**Answer to dual residual #2:** `+0x18` is **owner entity** (character/vehicle host from AttachOwner); `+0x24` is **skill**. They are **different fields**. OnStart does **not** null-check `+0x24` after the `+0x18` gate.

## Seal: vtbl / activation path (High)

| Item | Value | Conf |
|------|-------|------|
| Vtbl base | `g_pVtbl_CVOGHBOKToCastAgain` = `0x009ce1c4` | **High** (ctor + `read_memory`) |
| Slot at `0x009ce1c8` | `0x0051e390` OnStart | **High** (`read_memory` dword1) |
| Slot at `0x009ce1d8` | `0x0051e3b0` OnEnd (sibling) | **High** |
| Code xrefs to OnStart | **data-only** vtbl (no direct CALL) | **High** (`get_xrefs_to` → `009ce1c8`) |
| Activation | HB list Start → vfunc OnStart | **High** CF (caller chain via `CVOGHBBase_Start`) |

## Seal: B residual alloc cross-check (High, caller-owned)

`Skill_StartCastAgainHeartbeat` `0x00519200`: `push 0x28` / `operator_new` before ctor. **Alloc size 0x28 sealed** for type-8 HB; not re-owned here beyond citation.

## Control flow (clean ≡ raw ≡ asm)

```
ESI = this (HB)
if HB+0x18 != 0:
  Skill_SetIsCastingFlag(ECX=HB+0x24, stack char 1)
  // → skill+0x628=1; skill+0x170=0.f; skill+0x178=g_dwClientTickMs
// always:
tail → StampLastFireTime(ECX=HB)  // HB+0x14 = g_dwClientTickMs
```

| Stage | Match |
|-------|-------|
| Owner gate + set flag + stamp | **Yes** |
| Stamp unconditional | **Yes** |
| No invented work / no duration math | **Yes** |
| No stack args / no return value | **Yes** |

## Corrected clean plate

```c
void __fastcall CVOGHBOKToCastAgain_OnStart(void* pHeartbeat /* ECX */);
// if (pHeartbeat->pOwnerObject /*+0x18*/) 
//   Skill_SetIsCastingFlag(pHeartbeat->pSkill /*+0x24*/, 1);
// CVOGHBBase_StampLastFireTime(pHeartbeat);  // tail; HB+0x14 = tick
```

Pairs with **OnEnd** (`0x0051e3b0`): same gate, `push 0`, then base end stub — clears casting when window ends.

## Residual after this pass

| Item | Status |
|------|--------|
| Body CF + gate/order | **Sealed High** |
| `SetIsCastingFlag` call ABI (`thiscall` + `char` + `RET 4`) | **Sealed High** |
| Flag width **byte** @ skill `+0x628` | **Sealed High** |
| True-path `+0x170` float clear + `+0x178` tick | **Sealed High** |
| `unaff_SI` phantom third arg | **Falsified / closed** |
| `+0x18` owner vs `+0x24` skill | **Sealed High** (body + ctor attach) |
| Stamp target `HB+0x14` | **Sealed High** |
| Vtbl slot OnStart | **Sealed High** |
| Alloc `0x28` (StartCastAgainHeartbeat) | **Sealed High** (caller cite) |
| Product English for skill `+0x628` / `+0x170` / `+0x178` | Open (numeric CF sealed; sibling `aa_005502d0` accepted) |
| Whether any production path starts HB with null `+0x18` | Open (ctor always AttachOwner(owner) when well-formed) |
| Runtime / bit-exact / image diff | Open |
| Original PDB symbol | Unknown (reconstructed name retained) |

## Verdict

| Side | Verdict |
|------|---------|
| **A (fidelity)** | **accept-with-gaps** (core CF/ABI sealed; product English open) |
| **B (skeptical)** | **accept-with-gaps** (prior flag-ABI / owner-slot attacks closed by bytes) |
| **Combined** | Dual residual **strengthened**; package not complete (runtime open) |

## Artifacts touched

- This scratch: `docs/reconstruction/reviews/a_0051e390.md`
- Duals: `A_aa_0051e390_*`, `B_aa_0051e390_*`
- Function record, clean, annotated append
- **Not touched:** WORK_QUEUE, RESUME, ACTIVE_WORK, CHANGE_LOG, Launcher
