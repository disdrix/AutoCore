# Dual residual: `0x00512bb0` Skill_SiblingCastClearPredicate

| Field | Value |
|-------|-------|
| Stable ID | `aa_00512bb0` |
| Address | `0x00512bb0` |
| Ghidra name | `Skill_SiblingCastClearPredicate_INFERRED` (was `LAB_00512bb0`) |
| Owner | dual A/B only (no WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 |
| Body referenced by | `0x00514e20` Skill_SiblingCastClear (+ inlined list walk in `FUN_00517400`) |
| Prior | inventory gap close plate; CF summarized under `aa_00514e20` residual |

## Questions

1. ABI (arg count / convention / return)
2. Type-1 vs type-8 arm CF and order
3. RTTI descriptors and cast targets
4. Exact field compares (skillId / TFID partial / flag)
5. Match action (`vtbl+0x18`) and remove return
6. Who pushes this VA (DATA xrefs)
7. Product / demangled symbol

## Seal: ABI (**Confirmed**)

`read_memory` body `0x00512bb0`…`0x00512c5f`:

```
mov eax, [esp+4]           ; arg1 = HB* payload
mov ecx, [eax+0x1c]        ; type tag
sub esp, 0x10
cmp ecx, 1
push esi / push edi
jnz arm_else
… type-1 arm …
… match_common …
ret 0x0C                   ; stdcall, 3 stack formals (12 bytes)
```

| Formal | Stack | Role |
|--------|-------|------|
| `pHb` | +4 | list payload (`CVOGHBBase*`) |
| `pCtx` | +8 | `SiblingCastClearCtx*` packed by parent |
| `pStop` (unused) | +0xc | list utility stop out-param; **never written** |

Return: `AL` 0 = keep node; 1 = remove after Stop. Decompiler `undefined4`/`return 1` is fine; epilogue uses `mov al,1` / `xor al,al`.

## Seal: body CF (**Confirmed** asm)

### Arm A — `*(pHb+0x1c) == 1`

```
__RTDynamicCast(pHb, 0, CVOGHBBase@0x00af8d98, CVOGHBSkillBase@0x00af8cec, 0)
  → type_info strings Confirmed read_memory:
     0x00af8d98 → ".?AVCVOGHBBase@@"
     0x00af8cec → ".?AVCVOGHBSkillBase@@"
require all:
  *(hb+0x620) == *ctx                 ; skillId
  *(hb+0x658) == ctx[2] / ctx+0x08    ; TFID dword0
  *(hb+0x65c) == ctx[3] / ctx+0x0c    ; TFID dword1
  (u8)*(hb+0x660) == (u8)ctx[4]/+0x10 ; TFID dword2 **low byte only**
  ; *(hb+0x664) loaded then spilled — NEVER compared
  ; ctx+0x04 hole and ctx+0x14 (tfid3) unused
on match → match_common
```

### Arm B — type ≠ 1 path

```
if ((u8)ctx[+0x18] == 0) return 0;     ; alsoOkToCastAgain flag
if (*(pHb+0x1c) != 8) return 0;
__RTDynamicCast(pHb, 0, CVOGHBBase@0x00af8d98, CVOGHBOKToCastAgain@0x00b0015c, 0)
  → 0x00b0015c → ".?AVCVOGHBOKToCastAgain@@" Confirmed
require:
  *(*(hb+0x24) + 0x5fc) == *ctx        ; skill* → skillId; no TFID
on match → match_common
```

### Match common

```
if !match: AL=0; ret 0x0C
else:
  thiscall hb->vtbl[+0x18](1, 0)       ; Stop/End family (name Open)
  AL=1; ret 0x0C                       ; list removes node
```

No null check after `__RTDynamicCast` (type tag pre-filters; cast fail = crash path).

## Seal: ctx layout (consumer of parent pack)

| Off | Field | Used by pred? |
|-----|-------|---------------|
| +0x00 | skillId | yes (both arms) |
| +0x04 | pad / unset | **no** |
| +0x08 | tfid0 | type-1 only |
| +0x0c | tfid1 | type-1 only |
| +0x10 | tfid2 | type-1 **byte only** |
| +0x14 | tfid3 | **no** |
| +0x18 | alsoOkToCastAgain | type-8 gate |

## Seal: DATA xrefs (**Confirmed**)

| Site | Function | Role |
|------|----------|------|
| `0x00514e5b` | `Skill_SiblingCastClear` (`0x00514e20`) | push predicate imm → InvokePredicateAndRemove |
| `0x00517520` | `FUN_00517400` | same predicate, **inlined** list invoke (flag=0; skill+0x5fc; host TFID `this+0x160`) |

Not a free function call site — address taken as callback only.

## Product name

| Name | Confidence |
|------|------------|
| `Skill_SiblingCastClearPredicate` | **Inferred** role (owner dual); demangled **Open** |
| Ghidra | `Skill_SiblingCastClearPredicate_INFERRED` |

## Gaps

1. Product demangled symbol.
2. Why TFID match is partial (2 dwords + 1 byte); unused `tfid3` / hole +4.
3. `vtbl+0x18(1,0)` product English (Stop/End family **High**).
4. No-null-after-RTTI: intentional vs missing check (**asm sealed** no check).
5. Runtime / bit-exact.

## Artifacts

- Dual: `reviews/A_aa_00512bb0_Skill_SiblingCastClearPredicate.md`, `B_aa_00512bb0_Skill_SiblingCastClearPredicate.md`
- Scratch: this file
- Raw / annotated / function / clean
- Parent dual: `A|B_aa_00514e20_Skill_SiblingCastClear.md`
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
- No Launcher
