# Review A (reconstruction fidelity): `aa_0052c780` FUN_0052c780 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_0052c780` |
| **VA** | `0x0052c780`–`0x0052c854` |
| **Canonical name** | `FUN_0052c780` (**retained**) |
| **Review date** | `2026-07-29` (W18-I OWN dual) |
| **Reviewer role** | Reconstruction fidelity / evidence-only naming |
| **Counterpart** | `reviews/B_aa_0052c780_FUN_0052c780.md` |
| **System** | `skills-abilities` |
| **Verdict** | **leave-FUN** — behavior dual OK; product name withheld |

---

## 1. Purpose (behavioral, not product name)

Entity-side **skill cast-state teardown**:

1. Clear `this+0x6bb`.
2. Secondary-base **`vtbl+0x40(1)`**.
3. `block = FUN_005169c0(sec)` then **`FUN_005b2ba0(block)`** — drain/delete busy 0x14 block (result **not** discarded).
4. Traversal-lock skill hash at `sec+0x74`; walk nodes (`+0x14` next, skill at `+8`); **`Skill_SetIsCastingFlag(skill, 0)`** each.
5. Unlock; clear `this+0x1fc`; tail **`FUN_004c81f0(this)`**.

Assert-only strings: `HashError:TraversalLock…`, `HashError:TraverseToNext…`, `VOG_DEBUG_STOP`.

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Fresh decompile | `0x0052c780` |
| Bytes | `read_memory` 212 B |
| Xrefs | sole CODE `0x004fb9d0` (containing fn **undefined** in Ghidra) |
| Related duals | `Skill_SetIsCastingFlag`, `FUN_005169c0` leave-FUN |
| Raw / clean | `raw/aa_0052c780_*`, `reconstructed-exact/FUN_0052c780.cpp` |

---

## 3. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| `__thiscall` entity this; void | **High** | ESI=ECX; tail jmp |
| Clear `+0x6bb` first | **Confirmed** | `C6 86 BB 06 00 00 00` |
| Vcall `+0x40` with **push 1** | **Confirmed** | before 005169c0 |
| `FUN_005169c0` → `FUN_005b2ba0` chain | **Confirmed** | `mov ecx,eax` between calls — closes leave-FUN residual "discarded?" |
| Hash lock `+0x1d` set/clear around walk | **Confirmed** | |
| SetIsCastingFlag **push 0** only | **Confirmed** | decomp `uVar3` third arg **falsified** (same as SetIsCastingFlag dual) |
| Clear `+0x1fc` then tail `FUN_004c81f0` | **Confirmed** | |
| Product / RTTI method name | **None** | assert strings ≠ identity |
| `Named_VOG_DEBUG_STOP_*` | **Rejected** | scaffold pollution |
| Sole caller context | **Partial** | site known; function blob not defined |

---

## 4. Naming gate

| Evidence class | Present? |
|---|---|
| String product method | **No** (asserts only) |
| RTTI on function | **No** |
| Exclusive single semantic role name safe | Role High for clear-walk; multi-step (vcall + busy drain + hash + tail) — no exclusive plate |

**Decision:** retain **`FUN_0052c780`**. Document role; do **not** three-rep promote.

---

## 5. Gaps

1. Product type of `this` / secondary base.
2. Meaning of `+0x6bb`, `+0x1fc`, vcall `+0x40(1)`.
3. Containing function for `0x004fb9d0`.
4. Runtime / bit-exact / image diff.

**Verdict:** **leave-FUN** (behavior dual OK; name withheld).
