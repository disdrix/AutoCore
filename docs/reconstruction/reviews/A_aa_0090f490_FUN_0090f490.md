# Review A (reconstruction fidelity): `aa_0090f490` FUN_0090f490

| Field | Value |
|---|---|
| **Stable ID** | `aa_0090f490` |
| **VA** | `0x0090f490`–`0x0090f8d3` |
| **Canonical name** | `FUN_0090f490` |
| **Ghidra name** | `FUN_0090f490` |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Reconstruction fidelity (OWN-ONLY W27-A) |
| **Counterpart** | `reviews/B_aa_0090f490_FUN_0090f490.md` |
| **System** | client / object pose track |
| **Evidence pass** | Live Ghidra `decompile_function` + `read_memory` + `analyze_function_complete` + call-site bytes |
| **Verdict** | **accept** |

---

## 1. Purpose

Host (EBX) pose-track worker:

1. Ensure `@+0x544` non-zero (default **0.1f**); invoke global service vfunc.
2. If related object `@+0xa20` present: measure distance from global sample pos to live entity pos (`vfunc+0xc` → `+0x90`).
3. **Far** (dist ≥ 0.1): gate on sealed `Math_Vec3NearlyEqual_AbsRel(live+0x90, host+0x4fc, ~1e-6, ~1e-6)`. On miss, refresh cache and rebuild pose (normalize / basis / matrix / quat / write). Then optional `param_1` vfunc `+0x10`(3).
4. **Near**: if secondary vector length > plate **20.0f**, snap zero-rot + global pos.

---

## 2. Inspected artifacts / evidence

| Artifact | Path / tool |
|---|---|
| Raw | `docs/reconstruction/raw/aa_0090f490_FUN_0090f490.md` (+ 2026-07-29 W27-A append) |
| Annotated | `docs/reconstruction/raw/aa_0090f490_FUN_0090f490.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/FUN_0090f490.cpp` |
| Function record | `docs/reconstruction/functions/aa_0090f490_FUN_0090f490.md` |
| Sealed callee | `Math_Vec3NearlyEqual_AbsRel` (W26-A) |
| Live | decompile ≡ raw; prologue/`RET 4` epilogue; call site `0x0090f625` |

---

## 3. Signature (sealed)

```c
// EBX = host; stack param_1 optional; RET 4; void
void FUN_0090f490(int *param_1);
```

| Formal | Source | Conf |
|---|---|---|
| host | EBX | **High** |
| param_1 | stack `[EBP+8]`; `RET 4` | **High** |
| return | void | **High** |

---

## 4. Control flow (clean ≡ raw ≡ bytes)

```
default +0x544 to 0.1 if zero
service_vfunc(+0x544, 1)
seed pos from DAT_00d1a6c0/c4/c8
if +0xa20:
  if dist(global, live.pos) >= 0.1:
    if !Vec3NearlyEqual(live.pos, cached+0x4fc, 1e-6):
      update cache; rebuild basis/pose; write orient+pos
    if param_1: vfunc+0x10(3)
  else if |secondary.vec| > 20:
    write identity-ish quat + global pos; return
return
```

| Stage | Match | Conf |
|---|---|---|
| Far/near split on 0.1f | **Yes** | **High** |
| Near-equal gate + 1e-6 tols | **Yes** | **High** |
| Cache update `@+0x4fc` on miss | **Yes** | **High** |
| Basis rebuild + normalize×3 | **Yes** | **High** |
| Optional param_1 callback | **Yes** | **High** |
| Near-path snap | **Yes** | **High** |

---

## 5. Machine bytes (`read_memory`)

- Prologue: `55 8B EC 83 E4 F0 81 EC 98 00 00 00 …`
- Epilogue: `5F 5E 8B E5 5D C2 04 00` (`RET 4`)
- Call site `0x0090f625` region: `PUSH 0x358637BD; LEA EDI,[EBX+0x4fc]; PUSH 0x358637BD; LEA ESI,[EAX+0x90]; CALL FUN_0040d3f0`

---

## 6. Gaps

- Product English / class names — residual; keep `FUN_0090f490`.
- Direct callers — **no static xrefs** (indirect dispatch).
- Exact ECX identity at every `FUN_0040d1a0` / `FUN_0040cf90` site in far rebuild — Medium (decompiler omits explicit this on some sites).
- Runtime FPU CW / bit-exact — open.

---

## 7. Verdict

Outer CF, EBX+`RET 4` ABI, sealed vec3 near-equal gate, cache offsets, and far/near split are sealed. Residual caller-table and product-name gaps do not block accept. → **accept**.
