# Review B (skeptical / adversarial): `aa_0090f490` FUN_0090f490

| Field | Value |
|---|---|
| **Stable ID** | `aa_0090f490` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` (OWN-ONLY W27-A) |
| **Counterpart** | `reviews/A_aa_0090f490_FUN_0090f490.md` |
| **Scratch** | `tmp/a_0090f490.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack / result |
|---|---|---|
| 1 | Pure cdecl with host on stack | **Falsified** — host is **EBX** (`unaff_EBX`); only `param_1` on stack; **`RET 4`** |
| 2 | Near-equal compares 2D only | **Falsified** — sealed vec3 helper loads +0/+4/+8 |
| 3 | Cached pos is `@+0x90` on host | **Falsified** — cache is `@+0x4fc`; live is entity `@+0x90` |
| 4 | Always rebuilds pose every call | **Falsified** — `JNZ` after near-equal **skips** rebuild when near |
| 5 | `g_flMultiKillCountBlend` proves multi-kill UI role | **Overstated** — constant is **0.1f** reused; plate English not product proof for this unit |
| 6 | Direct static callers exist | **Falsified for Ghidra graph** — zero xrefs; must be indirect |
| 7 | Distance test uses squared distance only | **Falsified** — decompile uses `SQRT(...)` vs 0.1 |
| 8 | ECX for pose writers is always host EBX | **Open residual** — some sites resolve live object via vfunc; do not over-claim |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Far/near + near-equal CF | **High** | Wrong pose thrash / skip |
| EBX + RET 4 ABI | **High** | Port ABI break |
| Cache `@+0x4fc` vs live `@+0x90` | **High** | Wrong field map |
| 1e-6 dual tol at call site | **High** | Equality jitter |
| Indirect callers | **Open** | Dispatch recovery only |
| Pose-write this pointers | **Medium** | Wrong write target |
| Product English | **Low** | Naming only |
| Runtime | **Open** | Matrix policy |

---

## 3. Cross-check against raw + bytes + sealed callee

```
EBX=host, param_1 on stack
if +0xa20:
  if dist(global, live.pos) >= 0.1:
    ESI=live+0x90, EDI=host+0x4fc
    if !Math_Vec3NearlyEqual_AbsRel(..., 1e-6, 1e-6):
      cache = live.pos; rebuild basis; write pose
    if param_1: vfunc+0x10(3)
  else if |secondary| > 20: snap pose; return
```

Live decompile 2026-07-29 ≡ raw 2026-07-23. Call site bytes at `0x0090f625` match W26-A sealed ABI (ESI/EDI + dual `0x358637BD`).

---

## 4. Surviving contract for AutoCore

```
// Port far-path gate with sealed vec3 helper:
if distance(globalSample, livePos) >= 0.1f:
  if !Math_Vec3NearlyEqual_AbsRel(livePos, cachedPos, 1e-6f, 1e-6f):
    cachedPos = livePos
    rebuild_pose_from_related_delta()
  optional_callback(param_1, 3)
```

---

## 5. Verdict

Adversarial pass **confirms** A on CF/ABI/near-equal gate; residual indirect callers + product English + some thiscall ECX sites remain open without overturning seal → **accept**.
