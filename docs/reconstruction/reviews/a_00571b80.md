# Residual scratch: `aa_00571b80` InventoryGrid_RemoveItem

| Field | Value |
|---|---|
| **VA** | `0x00571b80` |
| **Date** | 2026-07-29 |
| **Dual** | `A_aa_00571b80_InventoryGrid_RemoveItem.md` / `B_…` |

## Sealed this pass

- ABI: `__thiscall`, 3 stack args, `RET 0x0C` (`C2 0C 00`).
- Membership + detach both `this = grid+0x2c`.
- Unstamp: `idx = height*(ox+dx)+(oy+dy)` → `(-1,-1)` per 8-byte cell.
- Type 4: no clear loop.
- Peel path: no clear when remain&gt;0 && force==0.

## Still open

- Runtime CE dumps (full remove + peel).
- Bit15 of `item+0x17c` name.
- PDB product name.
- Deep identity of list APIs `00415c00` / `005085b0` / Place `00566f00`.

## Not done

- Ledger edits, Launcher, `disassemble_bytes`.
