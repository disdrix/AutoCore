# Dual residual: `0x006124b0` CVOGObjectiveRequirement_Collect_SlotAction

| Field | Value |
|-------|-------|
| Stable ID | `aa_006124b0` |
| Address | `0x006124b0` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior dual | A/B accept-with-gaps — open: character as `unaff_retaddr`, full stack ABI, double Count reality, format-string VAs |

## Residual questions (from dual A/B + function record)

1. Full stack ABI (`RET` size; how many stack formals)
2. Character identity — decompiler `unaff_retaddr` vs real stack formal
3. Is second `FUN_005711c0` call real or decompiler CSE failure?
4. Count thiscall ECX framing (cargo grid) + includeBroken
5. Format strings / Level append constants
6. Contrast with UseItem_SlotAction (slot floats vs cargo)
7. Display cap semantics (min(count, NumToCollect) pure UI)

## Live evidence this pass

| Source | Result |
|--------|--------|
| Ghidra `decompile_function` @ `0x006124b0` | Matches frozen raw (cargo gate, Count ×2, sprintf, Level, strncat) |
| Ghidra `read_memory` body `0x006124b0`–`0x006126c1` | Full asm seal (below) |
| Ghidra `get_function_xrefs` / `get_xrefs` | Sole xref **DATA** `0x009e12dc` (Collect vtable +0x18) |
| Ghidra `get_function_callers` | None (virtual only) |
| Collect vtable `read_memory` `0x009e12c4` | slot +0x18 dword = `0x006124b0` |
| UseItem_SlotAction `0x0060d060` body end | same family **`RET 0x10`** |
| Count residual `a_005711c0.md` | includeBroken / Broken bit19 sealed |
| Format strings `read_memory` | VAs below |

**Not performed:** `disassemble_bytes`, Launcher, live CE, bit-exact image diff.

---

## Seal: stack ABI (shared SlotAction formals)

```text
int __thiscall CVOGObjectiveRequirement_Collect_SlotAction(
    Requirement* this,       // ECX → ESI
    Character*   character,  // stack0 → EBP  (cargo host)
    void*        objState,   // stack1 — common SlotAction ABI; **unread by this body**
    char*        outBuf,     // stack2
    size_t       outCap);    // stack3
// RET 0x10  (cleans four stack dwords)
```

| Prior decompiler artifact | Asm reality | Confidence |
|---------------------------|-------------|------------|
| Character as `unaff_retaddr` | **`MOV EBP,[ESP+0x32c]`** after `PUSH EBP` @ `0x00612522` — stack0 after SEH+locals+saved regs | **High** |
| Signature 3 stack formals only | Exit **`RET 0x10`** @ `0x006126bf` | **High** |
| stack1 unused | No load of stack1; UseItem uses stack1 as objective state | **High** family |
| outBuf / outCap | `strncat` uses `[ESP+0x334]`=stack2 dest, `[ESP+0x338]`=stack3 cap (EBP still live) | **High** |

Cross-check UseItem_SlotAction asm (`0x0060d060`–`0x0060d0db`): **`RET 0x10`**; body loads **stack1** as objective state for slot floats; stack0 unread. Shared formal shape:

| Formal | UseItem SlotAction | Collect SlotAction |
|--------|--------------------|--------------------|
| ECX this | requirement | requirement |
| stack0 | unused | **character** (cargo) |
| stack1 | **objective state** (slot floats) | unused (inventory replaces slots) |
| stack2 | outBuf | outBuf |
| stack3 | outCap | outCap |
| cleanup | `RET 0x10` | `RET 0x10` |

---

## Seal: control flow (asm)

```text
// prologue: SEH + SUB ESP,0x30C; PUSH EBX/ESI; ESI=ECX (req); PUSH EDI
item = FUN_00404d70(*(ESI+0x10))   // CBID → clonebase/item
FUN_007a69d0()
if item==0: return 0

bVar12 = (*(item+0x3c) == 0)
if bVar12: FUN_004f1e20(item, 0, 1)   // lock/name path when +0x3c null

PUSH EBP
EBP = stack0  // character
if EBP==0 || *(EBP+0x250)==0 || *(*(EBP+0x250)+0x2b0)==0: return 0

EDI = *(ESI+0x14)                   // NumToCollect (display default)
ECX = *(*(EBP+0x250)+0x2b0)         // cargo InventoryGrid*
PUSH 0; PUSH *(ESI+0x10); CALL CountItemsByCbid @ 0x0061255e
// EAX=count, EDI=NumToCollect
if EAX < EDI:                       // JGE skip when count >= need
    ECX = cargo again
    PUSH 0; PUSH *(ESI+0x10); CALL CountItemsByCbid @ 0x0061257b
    EDI = EAX                       // display = live count
// else keep EDI = NumToCollect     // display capped at need

nameId path: FUN_00402d50(*(item+0x3c)+0x92) → FUN_007a6de0(*nameBlob, -1)
sprintf(local_210, "%s: %d / %d", name, EDI, *(ESI+0x14))   // fmt @ 0x009df764

if (*(char*)(ESI+0x58) != 0):
    FUN_007a6de0("Level", -1)       // "Level" @ 0x00a41404
    sprintf(local_314, " %s: %d-%d", ..., *(ESI+0x5c), *(ESI+0x60))  // fmt @ 0x009e1734
    dword/byte append local_314 onto tail of local_210

if bVar12: FUN_004eb010()
strncat(outBuf, local_210, outCap)
return strlen(local_210)
// epilogue: restore SEH; ADD ESP,0x318; RET 0x10
```

### Double Count call

| Claim | Status |
|-------|--------|
| Two `CALL 0x005711c0` sites in image | **Sealed High** (`0x0061255e`, `0x0061257b`) |
| First always when cargo present | **High** |
| Second only when count **&lt;** NumToCollect | **High** (`CMP EAX,EDI` / `JGE` over re-count) |
| Both pass `includeBroken = 0` | **High** (`PUSH 0` both sites) |
| Both set ECX = cargo grid | **High** (`MOV ECX, [vehicle+0x2b0]` before each CALL) |

Compiler does not CSE the under-target count into EDI without re-walk — second CALL is real. Equivalent to `display = min(count, NumToCollect)` if cargo is stable across the two walks.

### Format / Level constants

| Label | VA | Bytes / string |
|-------|-----|----------------|
| main fmt | `0x009df764` | `"%s: %d / %d"` |
| level fmt | `0x009e1734` | `" %s: %d-%d"` |
| Level word | `0x00a41404` | `"Level"` |
| UseItem contrast | `0x009dfc7c` | `"%s: %i / %i"` (different string; `%i` vs `%d`) |

### Display cap (UI only)

```text
display = (count < NumToCollect) ? count : NumToCollect
// does not write mission state / objective slot floats
```

**High** — pure sprintf input; no store to req or objective state in this body.

---

## Seal: vtable + fields

| Item | Value | Confidence |
|------|-------|------------|
| Collect vtable | `PTR_FUN_009e12c4` | **High** (ctor store) |
| SlotAction slot | `+0x18` → `0x006124b0` (`DATA` @ `0x009e12dc`) | **High** |
| Eval slot | `+0x10` → `0x00611940` | **High** |
| Action slot | `+0x04` → `0x00611f20` | **High** |
| type code | ctor `param_1[3] = 2` | **High** |
| req `+0x10` | XML `CBID` | **High** |
| req `+0x14` | XML `NumToCollect` | **High** |
| req `+0x58` / `+0x5c` / `+0x60` | level-range flag / min / max | **High** (body loads) |
| cargo path | `char+0x250` → `+0x2b0` | **High** |
| Body range | `0x006124b0`–`0x006126c1` | **High** |

---

## Seal: progress authority (B challenge)

| Path | Role |
|------|------|
| **This function** | Client **live inventory** journal/UI text for Collect (vtable+0x18) |
| Collect_Eval `0x00611940` | Same cargo recount → fraction `[0,1]` (vtable+0x10) |
| S2C `0x2071` ObjectiveState | Server **absolute** slot floats; UseItem_SlotAction reads those |
| Collect contrast | Collect SlotAction **does not** read objective-state slot floats |

Both inventory UI text and server absolute progress exist; they are not interchangeable. Broken stacks: Count residual sealed bit19 = Broken; this path always passes `includeBroken=0`.

---

## Residual after this pass

| Item | Status |
|------|--------|
| Stack ABI `RET 0x10` / four stack formals | **Sealed High** |
| Character = stack0 (not unaff residue) | **Sealed High** |
| stack1 unread family formal | **Sealed High** (sibling UseItem uses it) |
| Double Count calls | **Sealed High** (real; incomplete path only for 2nd) |
| Count ECX = cargo; includeBroken=0 | **Sealed High** |
| Format / Level string VAs | **Sealed High** |
| Display min(count, need) UI-only | **Sealed High** |
| Vtable +0x18 SlotAction | **Sealed High** |
| Cargo path + CBID/NumToCollect | **Sealed High** |
| Item name blob `+0x3c+0x92` product type | Open (role High; English field name open) |
| `FUN_00404d70` / `FUN_007a6de0` / lock pair product names | Open |
| Original PDB symbol | Open |
| Runtime journal refresh after grab | Open |
| Bit-exact / image diff | Open (policy) |
| Virtual invokers (no CODE xrefs) | Open (DATA-only) |

## Artifacts touched by residual owner

- This scratch: `reviews/a_006124b0.md`
- Duals: `A_aa_006124b0_*`, `B_aa_006124b0_*`
- Function record `functions/aa_006124b0_CVOGObjectiveRequirement_Collect_SlotAction.md`
- Annotated append on `raw/aa_006124b0_FUN_006124b0.annotated.md`
- Clean signature note `reconstructed-exact/CVOGObjectiveRequirement_Collect_SlotAction.cpp`
- VERIFICATION_MATRIX row for `aa_006124b0`
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
