# Residual scratch: `aa_007fb690` Client_ShowTimedProgressBar_Inferred

| Field | Value |
|---|---|
| Owner | dual A/B residual only (**no** WORK_QUEUE / RESUME / CHANGE_LOG / ledgers) |
| VA | `0x007fb690` |
| Date | 2026-07-29 |

## Sealed this pass

1. **ABI:** EAX=client, stdcall `RET 0x20`, 8 stack formals.
2. **Rate:** `(g_flOne@0xa0f2a0 - arg0) / arg1` → `vtbl+0x460`.
3. **Gates:** `client+0x1120` (bar) and `client+0xf40` (UI host).
4. **Dismiss first:** `FUN_007fb640`.
5. **RequestCast site:** charge×`g_flMsToSeconds` + `"...Activating Skill..."` + colors `DAT_00afdf08` / `0xFF400000`.
6. **Not skill-only:** Working / INC / Training / Crafting string census.

## Residual questions

1. Product type name for widget at `+0x1120`.
2. Vtbl slot English (`+0x43c`, `+0x450..0x46c`, `+0x15c`, `+0xcc`, `+0x34c`).
3. Color channel layout (ARGB component order vs opaque id).
4. Whether any caller passes `startOrMode != 0` (non-zero seed).
5. Relationship of bar max/`+0x450` to QB button gauge `vtbl+0x3b0` family (sibling UI, different object).

## Tools used

- `ghidra-mcp__decompile_function` `0x007fb690`, `0x007fb640`
- `ghidra-mcp__disassemble_function` `0x007fb690`
- `ghidra-mcp__analyze_function_complete`
- `ghidra-mcp__get_assembly_context` on 11 call sites
- `ghidra-mcp__read_memory` on `0xa0f2a0`, `0xa0f72c`, status strings
