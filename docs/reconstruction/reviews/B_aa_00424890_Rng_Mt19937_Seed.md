# Review B (skeptical / adversarial): `aa_00424890` Rng_Mt19937_Seed

| Field | Value |
|---|---|
| **Stable ID** | `aa_00424890` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` (W19-A OWN-ONLY dual) |
| **Counterpart** | `reviews/A_aa_00424890_Rng_Mt19937_Seed.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | `__thiscall` / ECX this | Entry uses ECX as loop counter `1`; no object via ECX | **Falsified** — **EAX mt + EDX seed** |
| 2 | Has stack args / RET N | Ends **`C3`** | **Falsified** — **0 stack args** |
| 3 | Is twist (`0x9908b0df`) | No MATRIX_A; no 227/396 loops; no cursor write | **Falsified** — **seed only** |
| 4 | Is temper / extract | No right-shift temper cascade; no remaining-- | **Falsified** — **seed only** |
| 5 | Decompiler `param_1` is real arg | Unused in body; first meaningful arg is EDX seed into `[eax]` | **Falsified as formal** — phantom |
| 6 | Sets remaining=624 after seed | No `+0x9c4` store in body (bytes end at `C3`) | **Confirmed absent** |
| 7 | Non-standard init constant | `0x6c078965` = textbook MT19937 F | **Confirmed standard** |
| 8 | Has callees | `analyze_function_complete` callees **[]** | **Confirmed leaf** |
| 9 | Scaffold “unknown system” | Constants + callers into RNG fill/ctor | **System is RNG/MT19937** |
| 10 | Only fills 623 words | Loop `i=1; i<0x270` with write at `mt[i]` via walk = 623 updates + mt[0] = **624** total | **Confirmed full N** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| MT19937 seed identity | **High** | Wrong PRNG init breaks skill tables / dice |
| EAX/EDX ABI | **High** | Crash / write wrong memory |
| No cursor/remaining side effects | **High** | Mis-order with twist would desync extract |
| Product English name | **Medium** | Naming only |
| Exact uint32 wrap of imul | **High** | x86 `imul r32,imm32` truncates to 32-bit |

---

## 3. Cross-check against raw + bytes

```
raw:
  *in_EAX = param_2
  loop: in_EAX[1] = (*in_EAX >> 0x1e ^ *in_EAX) * 0x6c078965 + iVar1
        iVar1++; in_EAX++
  while iVar1 < 0x270

bytes @ 00424890:
  B901000000 8910 56 8B10 8BF2 C1EE1E 33F2
  69F66589076C 03F1 897004 83C101 83C004
  81F970020000 7CDE 5E C3

twist sibling @ 004248c0 (context):
  MATRIX_A 0x9908b0df; remaining←0x270; cursor←esi

≡ consistent seed-then-twist engine model (not classic mti index alone).
```

---

## 4. Surviving contract for AutoCore

```csharp
// Client MT19937 seed fill (mt[624] only; no cursor/remaining).
void RngMt19937Seed(uint[] mt /* 624 */, uint seed)
{
    mt[0] = seed;
    for (int i = 1; i < 624; i++)
        mt[i] = unchecked((uint)(0x6C078965u * (mt[i - 1] ^ (mt[i - 1] >> 30)) + (uint)i));
}
// Caller must still RngMt19937Twist to arm remaining=624 + cursor=&mt[0].
```

- Do not confuse with twist `0x9908b0df` or extract temper masks.
- Object size **0x9c8** at `operator_new` sites (mt + cursor + remaining).

---

## 5. Gaps surviving review

1. Product / PDB name.
2. Runtime vector vs reference MT (temper sibling may differ from textbook masks).
3. Bit-exact image diff.

**Verdict:** **accept** — seed CF, constant `0x6c078965`, N=624, EAX/EDX ABI **sealed**.
