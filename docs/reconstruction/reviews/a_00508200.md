# Dual residual: `0x00508200` CVOGHBBase_ctor

| Field | Value |
|-------|-------|
| Stable ID | `aa_00508200` |
| Address | `0x00508200`–`0x0050822c` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior dual | A/B **accept** 2026-07-23 — open: **offset names +0x08..+0x24**, **type field +0x1C**, **types registry** |

## Residual questions (from dual A/B + function record)

1. Exact offset names for `+0x08..+0x24`
2. Type field `+0x1C` usage (OKToCastAgain sets 8)
3. Publish `CVOGHBBase` layout / `sizeof`
4. Callers inventory
5. Plate `g_ActionSchedulerTickMs` vs `g_dwClientTickMs`

## Seal: machine init sequence (**High**)

Source: `read_memory` @ `0x00508200` (45-byte body). Live `force_decompile` 2026-07-29 ≡ frozen raw.

| Order | Offset | Write | Sealed name |
|------:|-------:|-------|---|
| 1 | +0x00 | `0x009cdab0` | `pVTable` → `PTR_FUN_009cdab0` |
| 2 | +0x1C | 0 | `nTypeTag` (Ghidra `nUnusedOrFlags`) |
| 3 | +0x21 | 1 | `fDormant` |
| 4 | +0x08 | 0 | `nPeriodMs` |
| 5 | +0x0C | 0 | `nPeriodSentinel` |
| 6 | +0x10 | 0 | `nPeriodCounter` |
| 7 | +0x14 | `[0x00b041cc]` | `dwLastFireTickMs` ← `g_dwClientTickMs` |
| 8 | +0x20 | 0 | `cStopped` |
| 9 | +0x18 | 0 | `pOwnerObject` |
| 10 | +0x04 | 0 | `nRefOrFlags` |

**Not written:** `+0x22..+0x27` (still inside `sizeof==0x28`).

## Seal: `sizeof(CVOGHBBase)==0x28` layout (**High**)

`get_struct_layout` size **40**. Cross-checked with GetRemaining / SetPeriod / AttachOwner / TryFire duals.

| Off | Name | Notes |
|----:|------|-------|
| +0x00 | `pVTable` | base until subclass overwrite |
| +0x04 | `nRefOrFlags` | zero here |
| +0x08 | `nPeriodMs` | real pulse; TryFire gate; GetRemaining |
| +0x0C | `nPeriodSentinel` | SetPeriodAndCounter; RescheduleAfterFire |
| +0x10 | `nPeriodCounter` | multi-period / SetPeriod mirror |
| +0x14 | `dwLastFireTickMs` | StampLastFireTime; TryFire on fire |
| +0x18 | `pOwnerObject` | AttachOwnerObject |
| +0x1C | `nTypeTag` | see values below |
| +0x20 | `cStopped` | char |
| +0x21 | `fDormant` | bool |
| +0x22..+0x23 | pad | ctor-uninit |
| +0x24..+0x27 | subclass / debounce | ctor-uninit; Regen zeros 3B; OKToCast skill* |

### Dual period (do not alias)

Combat pool pattern: `SetPeriodAndCounter(-1000, true)` → **+0x0C/+0x10**; real **3000/5000 at +0x08**.  
TryFire machine compares **`[this+0x08]`** to `-1000` and uses **`+0x08`** for elapsed; Reschedule reads **sentinel +0x0C**.

### Type tag values (sibling-sealed)

| Tag | Source ctor |
|----:|---|
| 0 | this base ctor |
| 1 | `CVOGHBSkillBase_ctor` — listener hard-gate |
| 4 | `Vehicle_CreateWeaponTrackAction` |
| 8 | `CVOGHBOKToCastAgain_ctor` |

## Seal: globals / plate (**High**)

| Symbol | VA | Role |
|--------|-----|------|
| `g_dwClientTickMs` | `0x00b041cc` | lastFire seed (plate alias `g_ActionSchedulerTickMs`) |
| `PTR_FUN_009cdab0` | `0x009cdab0` | base vtbl |

Base vtbl[1] = `CVOGHBBase_StampLastFireTime` (`0x00508270`) — **default OnStart** when `Start` clears dormant.

## Seal: ABI (**High**)

| Formal | Storage | Role |
|--------|---------|------|
| `this` | ECX | `CVOGHBBase*` / void* |
| (none) | — | no stack args; void return |

Convention: MSVC `__thiscall` (decomp may label `__thiscall` on void*).

## Callers (xref sample — `get_function_xrefs`, not exhaustive rename)

Named / high-value: `CVOGHBOKToCastAgain_ctor`, `CVOGHBRegeneration_ctor`, `CVOGHBSkillBase_ctor`, `Vehicle_CreateWeaponTrackAction`, `TimedAction_CreateTokenFlagAction`, `FUN_006061e0` (`CVOGHBWakeupSkill_ctor` per other residual), mission/skill HB factories under `0x00642*`, `0x00650*`, HBAI-adjacent `0x005d*`.

No callees from this unit.

## Re-decompile check (Ghidra 2026-07-29 residual)

CF matches frozen raw store-for-store. Delta vs prior dual gaps: **layout names sealed**, **type-tag role sealed**, **sizeof 0x28**, **uninit tail documented**, **tick global VA sealed**.

## Residual after this pass

| Item | Status |
|------|--------|
| Offset names + dual period | **Sealed** |
| +0x1C type tag role + sample values | **Sealed** (full enum open) |
| sizeof / Ghidra layout | **Sealed** |
| g_dwClientTickMs VA | **Sealed** |
| Full type-tag enum | **Open** |
| nRefOrFlags semantics | **Open** |
| Allocator pre-zero of +0x22..+0x27 | **Open** (out of unit) |
| Types registry publish | **Optional follow-up** (layout sealed here) |

## Artifacts touched

- `docs/reconstruction/reviews/A_aa_00508200_CVOGHBBase_ctor.md`
- `docs/reconstruction/reviews/B_aa_00508200_CVOGHBBase_ctor.md`
- `docs/reconstruction/reviews/a_00508200.md` (this file)
- `docs/reconstruction/reconstructed-exact/CVOGHBBase_ctor.cpp`
- `docs/reconstruction/functions/aa_00508200_CVOGHBBase_ctor.md`
- `docs/reconstruction/raw/aa_00508200_CVOGHBBase_ctor.annotated.md`
