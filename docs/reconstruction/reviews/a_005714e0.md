# Dual residual: `0x005714e0` InventoryGrid_FindFreeForItem

| Field | Value |
|-------|-------|
| Stable ID | `aa_005714e0` |
| Address | `0x005714e0` |
| Canonical name | `InventoryGrid_FindFreeForItem` (proposed; was `FUN_005714e0`) |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior state | scaffold raw/clean only (2026-07-23); no dual |

## Residual questions (this pass)

1. Exact relation to `InventoryGrid_FindFreeSlot` @ `0x005713a0`
2. Calling convention (`this` = grid vs item)
3. Item-type-4 / stack-merge / InvSize sources
4. Whether any caller passes non-`-1` `pageIndex`

## Live evidence this pass

| Source | Result |
|--------|--------|
| Ghidra `batch_decompile` @ `0x005714e0` | Body ≡ raw 2026-07-23 capture |
| Ghidra `batch_decompile` @ `0x005713a0` | FindFreeSlot (peer residual sealed) |
| Ghidra `batch_decompile` @ `0x00513e70`, `0x005710c0`, `0x0040abf0` | Stack-eligibility + peer find + capacity |
| Ghidra `read_memory` @ `0x005714e0` (64 B) | Prologue: `mov ebx,ecx`; `ret 0x14` → **thiscall**, 5 stack args |
| Ghidra `read_memory` @ `0x00571580` | Epilogue to FindFree: `mov ecx,ebx` + `call 0x005713a0` |
| Ghidra callers | 22+ functions (HandleButton, SerializeAddItem, Unequip, …) |
| `get_assembly_context` call sites | ECX loaded from cargo grid (`+0x2b0` / `+0x35c`); push `1`, push `-1` |
| Not performed | `disassemble_bytes`, Launcher, CE runtime pack |

---

## Seal: relation to FindFreeSlot `0x005713a0`

```text
// __thiscall InventoryGrid_FindFreeForItem(
//     InventoryGrid* this,   // ECX — saved to EBX for whole body
//     Item* item,            // stack0
//     uint8_t* outX, uint8_t* outY,
//     char allowStackMerge,  // stack3; 0 skips stack path
//     char pageIndex)        // stack4 — PASSTHROUGH to FindFreeSlot
// returns 1 hit / 0 miss; ret 0x14

if (item == null) return 0

// Type class 4 (item+0xa8 → +0x38): no grid footprint
if (*(i32*)(item[0x2a] + 0x38) == 4) {
  *outX = 0; *outY = 0; return 1
}

// Optional stack-on-existing (when allowStackMerge != 0)
if (allowStackMerge) {
  if FUN_00513e70(item):                    // __fastcall ECX=item
    cbid = *(item[0x2a] + 0x34)
    qty  = item->vtbl[0x25c]()              // quantity
    peer = FUN_005710c0(this, cbid, 0, qty) // __thiscall grid
    if peer && FUN_0040abf0(peer, qty):
      *outX = peer->vtbl[0x250]()           // grid X
      *outY = peer->vtbl[0x254]()           // grid Y
      return 1
  // miss → fall through
}

// --- SEALED FALLTHROUGH to FindFreeSlot ---
sizeX = *(u8*)(*(item[0x2a] + 0x3c) + 0x406)   // clonebase InvSizeX
sizeY = *(u8*)(*(item[0x2a] + 0x3c) + 0x407)   // clonebase InvSizeY
// asm: mov ecx, ebx  (grid this preserved)
return InventoryGrid_FindFreeSlot(this, sizeX, sizeY, outX, outY, pageIndex)
```

### What is sealed vs open

| Claim | Status |
|-------|--------|
| Wrapper is **thiscall** on `InventoryGrid*` (ECX) | **Sealed High** (prologue `mov ebx,ecx`; call sites load grid into ECX) |
| 5 stack args, `ret 0x14` | **Sealed High** |
| Final path is **direct** `call 0x005713a0` with ECX=grid | **Sealed High** (`mov ecx,ebx` @ fallthrough) |
| Footprint from clonebase `+0x406/+0x407` | **Sealed High** (body + same family as Place residual) |
| `pageIndex` is **pure passthrough** to FindFreeSlot | **Sealed High** |
| Type `+0x38==4` → success (0,0) without FindFree | **Sealed High** CF |
| Stack path optional, gated by `allowStackMerge` | **Sealed High** CF |
| Does **not** stamp cells (still search/outs only) | **Sealed High** |
| Observed callers: `allowStackMerge=1`, `pageIndex=-1` | **High** for sampled sites; full arg audit residual |
| Non-`-1` page producers via this wrapper | **Open** (CF supports it; no site sealed with non-`-1`) |
| Exact semantic name of type class 4 | **Open** (currency/quest/etc. interpretive) |
| Vfunc names 0x250/0x254/0x25c | **Probable** (grid X/Y / qty from call-site use) |

### How this differs from bare FindFreeSlot

| | FindFreeSlot `0x005713a0` | FindFreeForItem `0x005714e0` |
|--|---------------------------|------------------------------|
| `this` | grid | grid |
| Footprint | caller-supplied sizeX/Y | from item clonebase InvSize |
| Page | caller pageIndex | same (passthrough) |
| Type-4 short-circuit | no | yes → (0,0) |
| Stack merge | no | optional (`allowStackMerge`) |
| Zero-size special | success (0,0) inside FindFree | only if InvSize 0,0 via fallthrough |

---

## Seal: caller inventory (sample; pageIndex / stack flag)

| Caller | Role | allowStack | pageIndex |
|--------|------|------------|-----------|
| `Client_MissionDialogHandleButton` `0x008ae7c0` | Mission reward space check | `1` | `0xffffffff` |
| `Inventory_SerializeAddItemPacket` `0x004fadb0` | AddItem XY fill | `1` | `0xffffffff` |
| `Client_SendInventoryUnequip` `0x00862c00` | Unequip dest XY | `1` | `0xffffffff` |
| `FUN_00588290` | Place after stack-or-free | `1` | `0xffffffff` |
| `FUN_00520dc0` / `FUN_00533220` / drag cleanup family | Place prep | `1` | `0xffffffff` |

Asm (SerializeAddItem @ `0x004faef9`): `mov ecx,[ebx+0x2b0]` (cargo grid), `push -1`, `push 1`, item, outX@pkt+0x10, outY@pkt+0x11.

Asm (FUN_00588290 @ `0x005883f4`): `mov ecx,[ebp+0x35c]` (grid), same `1` / `-1`.

**Sealed for observed sites:** whole-grid page + stack-merge enabled. Page-restricted path only if some unexpanded caller passes non-`-1` into this wrapper (FindFree residual already notes that).

---

## Item / blob offsets (body-backed)

| Path | Offset | Role |
|------|--------|------|
| item | `+0xa8` (`[0x2a]`) | type/clonebase holder |
| holder | `+0x34` | CBID for stack peer match |
| holder | `+0x38` | type class; **4** = no footprint |
| holder | `+0x3c` | clonebase blob* |
| blob | `+0x406` / `+0x407` | InvSizeX / InvSizeY |
| blob | `+0x3f2` bit `0x40` | stack-eligible (via `FUN_00513e70`) |
| item | `+0x17c` bit 19 | blocks stack path when set |

---

## Residual after this pass

| Item | Status |
|------|--------|
| Relation wrapper → FindFreeSlot | **Sealed High** |
| thiscall + ECX=grid | **Sealed High** |
| InvSize + page passthrough | **Sealed High** |
| Type-4 and stack-merge CF | **Sealed High** |
| All callers page/stack args | **Partial** (sampled High; full xref arg matrix open) |
| Type-4 product meaning | **Open** |
| Runtime / bit-exact | **Open** |

## Artifacts touched by residual owner

- This scratch: `docs/reconstruction/reviews/a_005714e0.md`
- Duals: `A_aa_005714e0_InventoryGrid_FindFreeForItem.md`, `B_aa_005714e0_InventoryGrid_FindFreeForItem.md`
- Function record: `functions/aa_005714e0_InventoryGrid_FindFreeForItem.md`
- Clean: `reconstructed-exact/InventoryGrid_FindFreeForItem.cpp`
- Raw append: `raw/aa_005714e0_FUN_005714e0.md` (versioned section only)
- **Not** WORK_QUEUE / RESUME / CHANGE_LOG / ACTIVE_WORK
- **Not** Launcher / live servers
