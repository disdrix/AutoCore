# Review A (reconstruction fidelity): `aa_004188e0` FUN_004188e0

| Field | Value |
|---|---|
| **Stable ID** | `aa_004188e0` |
| **VA** | `0x004188e0` |
| **Body** | `0x004188e0`–`0x00418b44` |
| **Canonical name** | `FUN_004188e0` (leave-FUN; MSVC **map/set erase** node) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Independent reconstruction review |
| **Counterpart** | `reviews/B_aa_004188e0_FUN_004188e0.md` |
| **System** | shared map util; **skills** via category-CD expire + RecvSkillStatusEffect |
| **Verdict** | **accept-with-gaps** |

---

## 1. Purpose

**Erase one node** from an MSVC red-black `map`/`set`:

1. If iterator nil-byte (`node+0x19`) set → throw **`"invalid map/set<T> iterator"`** via `basic_string` + `_CxxThrowException`.
2. Splice successor / child pointers; fix head leftmost/rightmost (`head[0]`, `head[1]` root, `head[2]`).
3. If erased node was **red-black color black** (`node[6]` / `+0x18` == 1), run **fix-black rebalance** (rotations `FUN_00573170` / `FUN_00418c10`, color flips).
4. `operator_delete(node)` (decomp: noreturn-ish tail).

**Skill path:** `Skill_GetCategoryCooldownRemaining` erases expired category cooldown entries after lower_bound hit (`FUN_00418b80`). Also RecvSkillStatusEffect and other map owners.

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Raw / annotated / clean / function record | `aa_004188e0_*` |
| Live decompile | ≡ raw (many “unreachable” RB helpers Ghidra folds) |
| String evidence | `"invalid map/set<T> iterator"` |
| Callers (skill-relevant) | `Skill_GetCategoryCooldownRemaining`; `Client_RecvSkillStatusEffect`; `FUN_0051c700`; `FUN_0040d700` |
| Twin | lower_bound `FUN_00418b80` |

---

## 3. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| Span `004188e0`–`00418b44` | **Confirmed** | |
| thiscall map + unused/placeholder arg + node* | **High** | decomp `param_2` often dead; consumers pass key scratch |
| Invalid iterator throw string | **Confirmed** | |
| Nil at `+0x19`; color at `+0x18` / `node[6]` | **Confirmed** | |
| Pointer rewiring + RB rebalance | **High** | large body; classic STL erase |
| Ends in `operator_delete(node)` | **Confirmed** | |
| Category-CD expire consumer | **High** | GetCategoryCooldownRemaining |
| Exact MSVC mangled erase | **High shape / Open name** | |
| Every rotation helper named | **Tentative** | leave FUN_* rotations |
| Ghidra “unreachable block” warnings change CF | **Falsified risk** | warnings are RB case folding, not alt CF |

---

## 4. Control flow (contract-level)

```
if (it.nil) throw "invalid map/set<T> iterator";
// unlink it; fix root / leftmost / rightmost
if (it was black) rebalance while climbing to root;
operator_delete(it);
```

Not a skill gate; container maintenance only. Clean ≡ raw CF at this contract level (full RB line-by-line not re-proved instruction-by-instruction this pass).

---

## 5. Gaps / open

1. Formal `param_2` role (decomp leftover vs real 3-arg erase API).
2. Full RB case matrix vs MSVC reference (rotations already delegated).
3. Product promote only with RTTI/mangling.
4. Bit-exact / runtime open.

**Verdict:** **accept-with-gaps** — erase/throw/delete contract High for skill CD path; full RB formal proof residual.
