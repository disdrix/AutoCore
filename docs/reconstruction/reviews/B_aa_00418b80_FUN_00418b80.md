# Review B (skeptical / adversarial): `aa_00418b80` FUN_00418b80

| Field | Value |
|---|---|
| **Stable ID** | `aa_00418b80` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_00418b80_FUN_00418b80.md` |
| **Verdict** | **accept-with-gaps** |

---

## 1. Claims under attack

| # | Claim | Attack | Outcome |
|---|---|---|---|
| 1 | Exact-find only (equal keys) | Uses lower_bound walk + `<=` accept | **Falsified exact-only** |
| 2 | Mutates tree | Out pointer only | **Falsified** |
| 3 | Skill-only helper | Many non-skill callers | **Falsified exclusive** |
| 4 | Key is float category | Key compared as **int** | **Falsified float key** |
| 5 | Nil flag at +0x18 color only | Walk uses **+0x19** | **Survives** — color at +0x18 is erase twin |
| 6 | Returns found value payload | Writes **node iterator**, not value | **Falsified** |
| 7 | Safe if map uninitialized | Assumes head at +4 valid | **Caller invariant** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| lower_bound CF + key@+0xC | **Confirmed** | Wrong CD lookup |
| end() = head sentinel | **Confirmed** | False hit / miss |
| Shared utility | **Confirmed** | Over-specializing port |
| Value type layout | **Open** | Consumer responsibility |

---

## 3. Surviving contract for AutoCore

```
// Category CD path:
map = Skill_GetCategoryCooldownMap(owner);
FUN_00418b80(map, &it, &categoryId);
if (it != map.head) { /* read it+0x10 / +0x14 in GetCategoryCooldownRemaining */ }
// Generic MSVC map lower_bound by int key — leave FUN_*.
```

---

## 4. Open questions

1. Confirm value type size vs other map instantiations using same helper.
2. Whether any caller passes non-int keys (would be type-pun UB).
3. Optional promote `Map_LowerBoundByIntKey_Inferred` if registry wants.

**Verdict:** **accept-with-gaps** — tree walk sealed; naming/value type open.
