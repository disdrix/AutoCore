# Dual residual: `0x005502d0` Skill_SetIsCastingFlag

| Field | Value |
|-------|-------|
| Stable ID | `aa_005502d0` |
| Address | `0x005502d0` |
| Owner | dual residual only (**no** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 |
| Prior dual | A/B **accept** (2026-07-23) — open: false-path tick clear; full caller matrix; product English |
| Live tools | Ghidra `decompile_function` + `read_memory` body/callers; `get_function_xrefs` (no disassemble_bytes; no Launcher) |

## Residual questions (from prior dual)

1. Does false path clear `+0x178` / `+0x170`?
2. Full callers matrix (beyond HBOK OnStart/End).
3. ABI: decomp shows 3-arg / `void* pSkill` as flag — real shape?
4. Width of `skill+0x628` (byte vs dword)?
5. Type of `+0x170` write (uint32 zero vs float 0.0f)?
6. Seal `skill+0x628` as **is-casting flag** for consumers (LocalCastValidate / QuickBar).

## Seal: machine body (High)

Body `0x005502d0`–`0x005502f6` (`read_memory` 48 B; live code ends `C2 04 00`):

```
005502d0  8A 44 24 04              mov  al, [esp+4]         ; char bIsCasting
005502d4  84 C0                    test al, al
005502d6  88 81 28 06 00 00        mov  [ecx+0x628], al     ; skill+0x628 = flag (byte)
005502dc  74 16                    je   +0x16 → 005502f4    ; false: skip side effects
005502de  0F 57 C0                 xorps xmm0, xmm0
005502e1  F3 0F 11 81 70 01 00 00  movss [ecx+0x170], xmm0  ; skill+0x170 = 0.0f
005502e9  A1 CC 41 B0 00           mov  eax, [0x00b041cc]   ; g_dwClientTickMs
005502ee  89 81 78 01 00 00        mov  [ecx+0x178], eax    ; skill+0x178 = tick ms
005502f4  C2 04 00                 ret  4                   ; one stack dword
```

| Claim | Conf | Evidence |
|-------|------|----------|
| `__thiscall` skill in **ECX** | **High** | `mov [ecx+…]`; no prolog `push ebp` |
| **One** stack formal: **byte** flag at `[esp+4]` | **High** | `mov al,[esp+4]`; `ret 4` |
| Always write **byte** `skill+0x628 = bIsCasting` | **High** | `mov [ecx+0x628], al` before branch |
| **False path:** only `+0x628=0`; **no** touch `+0x170`/`+0x178` | **High** | `je` to `ret 4` |
| **True path:** `+0x170 = 0.0f` via **`movss`** (not integer store) | **High** | `xorps` + `movss` |
| **True path:** `+0x178 = g_dwClientTickMs` (`DAT_00b041cc`) | **High** | `mov eax,[0x00b041cc]; mov [ecx+0x178],eax` |
| Decomp `void* pSkill` as flag / 3-arg / `unaff_SI` | **Falsified** | body = 1 stack arg; plate misnames |
| No callees / no net / no validation | **High** | pure stores |

## Seal: ABI signature (High)

```c
void __thiscall Skill_SetIsCastingFlag(void* thisSkill /* ECX */, char bIsCasting /* stack */);
// RET 4
// always:  *(uint8_t*)(thisSkill + 0x628) = (uint8_t)bIsCasting;
// if (bIsCasting):
//   *(float*)(thisSkill + 0x170) = 0.0f;           // movss
//   *(uint32_t*)(thisSkill + 0x178) = g_dwClientTickMs;  // DAT_00b041cc
```

## Seal: skill field map for this unit (High)

| Offset | Width | Role (behavioral) | When written |
|--------|-------|-------------------|--------------|
| `+0x628` | **u8** | **Is-casting flag** | Always (0 or non-0 from arg) |
| `+0x170` | **float** | Cleared to **0.0f** on cast-start stamp | Only when flag **true** |
| `+0x178` | **u32** | Cast-start client tick ms (for charge/CD elapsed) | Only when flag **true** |

**Answer residual #1:** false does **not** clear tick/`+0x170`. Stale `+0x178` can remain until next true set; consumers gate on `+0x628` first.

## Seal: callers matrix (High — complete for CODE xrefs)

`get_function_xrefs` → **3** unconditional CALL sites:

| Site | Function | Flag arg | ECX source |
|------|----------|----------|------------|
| `0x0051e39e` | `CVOGHBOKToCastAgain_OnStart` | **`push 1`** | `HB+0x24` skill (if `HB+0x18` owner ≠ 0) |
| `0x0051e3be` | `CVOGHBOKToCastAgain_OnEnd` | **`push 0`** | `HB+0x24` skill (same gate) |
| `0x0052c82c` | `FUN_0052c780` | **`push 0`** | skill ptr from hash node `node+0x8` while traversal-locked |

### OnStart site bytes (`read_memory` `0x0051e390`)

```
cmp [esi+0x18],0 / je skip
mov ecx,[esi+0x24]
push 1
call 0x005502d0
; then tail StampLastFireTime
```

### OnEnd site bytes (`read_memory` `0x0051e3b0`)

```
cmp [esi+0x18],0 / je skip
mov ecx,[esi+0x24]
push 0
call 0x005502d0
; then tail FUN_005081f0 (empty ret)
```

### FUN_0052c780 clear-walk (`read_memory` around `0x0052c820`)

```
test ecx,ecx / je break
push 0
call 0x005502d0
jmp loop
```

Bulk-clear is-casting on all skills in map at entity `+0x74` (after `TraversalLock`); decomp third-arg noise falsified.

## Seal: consumers of `skill+0x628` (High for gate; role name product-open)

| Consumer | Use of `+0x628` | Conf |
|----------|-----------------|------|
| `Skill_LocalCastValidate` | non-zero → **eSkillResponses 7** (recharge/busy) with category CD | **High** (clean/body gate) |
| `QuickBar_UpdateSkillSlotCooldownGauge` | non-zero → charge path using `tick - skill+0x178`, `+0x14`, `+0x10` | **High** |
| `QuickBar_UpdateSlotCooldownOverlay` | zero → alternate overlay path; non-zero uses `+0x178` | **High** |
| `Skill_InitializeRuntimeObject` / `Skill_CopyRuntimeFieldsFromTemplate` | init/clear `+0x628 = 0` (not via this mutator) | **High** (sibling clean) |

**Sealed meaning of `skill+0x628`:** client-local **is-casting / cast-active** byte. Set **1** when cast-again HB starts; cleared **0** on HB end or bulk skill teardown walk. Drives cast-block UX and QB charge timing when set.

## Control flow (clean ≡ raw ≡ asm)

```
AL = bIsCasting
skill+0x628 = AL
if AL != 0:
  skill+0x170 = 0.0f
  skill+0x178 = g_dwClientTickMs
return (RET 4)
```

| Stage | Match |
|-------|-------|
| Always store flag byte | **Yes** |
| True-only float clear + tick stamp | **Yes** |
| False leaves `+0x170`/`+0x178` | **Yes** (prior open **closed**) |
| No invented net / validation | **Yes** |

## Residual after this pass

| Item | Status |
|------|--------|
| Body CF + offsets + widths | **Sealed High** |
| ABI `__thiscall` + `char` + `RET 4` | **Sealed High** |
| **`skill+0x628` is-casting flag (byte)** | **Sealed High** (numeric + consumer gates) |
| False path no tick clear | **Sealed High** (prior open closed) |
| True path `+0x170` float0 / `+0x178` tick | **Sealed High** |
| Callers = OnStart(1), OnEnd(0), FUN_0052c780(0)×N | **Sealed High** |
| Decomp 3-arg / `void*` flag | **Falsified / closed** |
| Product / PDB English names for fields | **Open** (behavior sealed) |
| Semantic role of `+0x170` beyond zero-on-start (other writers) | **Open** (this unit only zeros it) |
| `FUN_0052c780` full product name / when invoked | **Open** (clear-walk CF sealed) |
| Runtime / bit-exact / image diff | **Open** |

## Verdict

| Side | Verdict |
|------|---------|
| A (fidelity) | **accept** — CF sealed; prior false-path gap closed |
| B (adversarial) | **accept** — no surviving attack on +0x628 width/role at CF level |
