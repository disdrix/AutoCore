# Dual residual scratch — `0x00525bd0` Client_FindObjectiveMatchingTarget

| Field | Value |
|---|---|
| Stable | `aa_00525bd0` |
| Owner | dual residual only (this VA) |
| Date | 2026-07-29 |
| Promote to | `A_aa_00525bd0_*` / `B_aa_00525bd0_*` |
| Out of scope | WORK_QUEUE, RESUME, other VAs' duals |

## Live evidence (2026-07-29)

- Ghidra `batch_decompile` `0x00525bd0` ≡ raw body (pending `+0x55c` count, active `+0x548` lock walk, eval `+0x38`, unlock, def*).
- Sole caller: `Client_SendUseObject` `0x00916740` — stack arg `*( *(target+0xa8)+0x34 )`; return def`+0x10` or −1; thiscall ECX residual.
- Contrast live: `0x00524520` (id, `+0x40` helper, no pending gate); `0x0059d9c0` (eval `+0x40`).
- UseItem `0x0060d7f0`: thunk `JMP 0x0060d460` (`E9 6B FC FF FF`); helper ABI from `FUN_0060df70` call = `(req this, character, worldObject)`.

## Residual seals this pass

| Gap (prior dual) | Outcome |
|---|---|
| First hash-order match only | **Sealed High** — unlock+return on first `cMatch!=0` |
| Walk is active `+0x548` not pending | **Sealed High** — pending only count-gate |
| Returns def* not id | **Sealed High** — caller reads `+0x10` |
| Match slot `vtable+0x38` | **Sealed High** for this walker |
| Pending gate purpose | **High** empty-progress early-out; stale-count false-neg **Medium** residual |
| UseItem `FUN_0060d460` as key-match body | **Not sealed for this call ABI** — helper is (char, worldObj); FindObjective passes (key, char). Object-event path `FUN_0060df70` is the proven consumer |
| Kill/Patrol `+0x38` implementers | **Open** — catalog residual outside body |
| Character ECX at SendUseObject | **Probable / Open** — decompiler under-shows this |

## Contrast table (do not merge)

| Unit | Hash | Gate | Match | Returns |
|---|---|---|---|---|
| **This** `00525bd0` | `+0x548` | pending count `+0x55c` ≥1 | eval **`+0x38`(key, char)** | **def*** |
| `00524520` | `+0x548` | none | helper → eval **`+0x40`(char, world)** | **id** `@+0x10` |
| `005245d0` | `+0x548` | none | helper → eval **`+0x44`** | **id** |

## Port contract

```
def* FindObjectiveMatchingTarget(Character* ch, uint32 targetKey /* often clone+0x34 */) {
  if (*(int*)(*(ch+0x55c)+0xc) < 1) return 0;
  lock activeObjectives hash +0x548 (+0x1d);
  for each def in hash order:
    for each eval in [def+0x158, def+0x15c):
      if MatchTarget(+0x38)(targetKey, ch): unlock; return def;
  unlock; return 0;
}
// SendUseObject: id = def ? def+0x10 : -1
// Do NOT use as IfInteractable id finder; do NOT swap +0x38/+0x40; do NOT drop pending gate.
```

## Artifacts refreshed

- [x] This scratch
- [x] A dual (fidelity) — `A_aa_00525bd0_Client_FindObjectiveMatchingTarget.md`
- [x] B dual (skeptical) — `B_aa_00525bd0_Client_FindObjectiveMatchingTarget.md`
- [x] Function record status date
- [x] Clean plate sibling/residual notes only
- [x] Annotated open questions
- [x] **Not** WORK_QUEUE / RESUME (out of scope)
