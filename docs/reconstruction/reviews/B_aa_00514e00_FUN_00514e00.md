# Review B (skeptical / independent): `aa_00514e00` FUN_00514e00 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00514e00` |
| **VA** | `0x00514e00` |
| **Canonical name** | `FUN_00514e00` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Skeptical naming / anti-overclaim |
| **Counterpart** | `reviews/A_aa_00514e00_FUN_00514e00.md` |
| **System** | skills-abilities |
| **Verdict** | **leave-FUN** — agree with A; block product / `_Inferred` name |

---

## 1. Adversarial read of proposed names

| Candidate | Why reject |
|---|---|
| `Skill_DecActiveCastCounter` | Prior residual **role** label only (`a_0051aa00`). No string/RTTI. Sibling clear path already owns a product-ish name (`Skill_ClearActiveCastCounterAndQueueId`) that does **more** (lazy-get + queue + AL). Promoting this leaf as “the” counter API overclaims exclusivity. |
| `Skill_DecActiveCastCounter_Inferred` | Same bar failure under `_Inferred`. Multi-caller; null-safe raw ptr path is a **subset**, not a sealed product method. |
| `Skill_ClearCastBinding*` | **Wrong family.** Binding clear is `Skill_ClearCastBindingAndMaybeRestartCd` / map at `+0x68`. This VA only touches slot at `+0x64` count/`+0xC`. |
| `Named_CalleeOf_Client_RecvSkillStatusEffect_00514e00` | Scaffold pollution; exclusive Recv use **false** (ApplyStatus + FUN_00606180). |

---

## 2. Xref challenge (must not collapse to one caller)

```
0x0051abd4  Skill_ApplyStatusEffectLocal
0x008111c5  Client_RecvSkillStatusEffect
0x00811200  Client_RecvSkillStatusEffect
0x00811482  Client_RecvSkillStatusEffect
0x00606193  FUN_00606180
```

**5** sites / **3** functions. Any name implying “Recv cancel helper only” or “ApplyStatus only” is **wrong**.

---

## 3. What is safe to claim

| Safe | Unsafe |
|---|---|
| Leaf: `this+0x64` → optional block | Product class / method symbol |
| Dec `block+0x10` when nonzero; clear `+0xC` at 0 | That this is the only counter writer |
| Null block is no-op (no lazy alloc) | Equating this VA with ClearActiveCastCounter (missing queue/AL) |
| Same arithmetic as ClearActiveCastCounter’s dec half | Product English for +0xC |
| Role High “active-cast/busy refcount dec” | Registry rename without string/RTTI |
| Leave FUN_* | Promote because sibling Clear* is named |

---

## 4. CF challenge of Review A

- Dual force/non-force decompile identity: **agree Confirmed**
- Offsets `+0x64` / block `+0x10` / `+0xC`: **agree Confirmed**
- Leaf / no callees: **agree**
- Related to ClearActiveCastCounter as **subset**: **agree High** (not identity)
- Leave FUN_*: **agree**
- Do not promote `Skill_DecActiveCastCounter[_Inferred]`: **agree**

Caveat: Ghidra prototype surface sometimes shows `undefined FUN_00514e00(void)` with return-type warning — treat as incomplete prototype, not void-thiscall-no-ECX. Body uses `param_1` as object base; **agree High** with A’s `__fastcall this` reading.

---

## 5. Residual risks

- `FUN_00606180` ECX may be WakeupSkill HB (`sizeof` 0x674) vs caster — if so, either HB shares a +0x64 slot layout or decomp lost a re-load of caster into ECX. **Not sealed** from this leaf alone.
- Recv error-path calls may rely on ambient ECX from surrounding client/character glue — verify in live debugger before claiming exact owner instance.
- Increment site still open (gap #3 in A).

**Verdict:** **leave-FUN**. Reopen rename only on string, RTTI, or stronger product plate — not on role analogy to ClearActiveCastCounter alone.
