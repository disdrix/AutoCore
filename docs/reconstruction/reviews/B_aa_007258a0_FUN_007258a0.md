# Review B (skeptical / adversarial): `aa_007258a0` FUN_007258a0

| Field | Value |
|---|---|
| **Stable ID** | `aa_007258a0` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_007258a0_FUN_007258a0.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | Always queues | Enable, type suppress, range, name-null gates | **Falsified** — many **return 0** |
| 2 | Plays immediately | Enqueues via `00480350` | **Confirmed deferred queue** |
| 3 | 3D full distance | Uses x/z vs `this+0x220/228` primarily | **Confirmed planar-ish** |
| 4 | `cdecl` | `ret 8` + thiscall ECX | **Confirmed thiscall stdcall-ish** |
| 5 | Same as `PlayNamedInterfaceSound` | Distinct path: positioned entry clone | **Confirmed different unit** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Enable `+0x258`, ret 0/1 | **High** | Spurious FX |
| Type 0xC/0xD gate | **High** | Dup plays |
| Range uses `entry+0x114` | **High** | Hear/not-hear wrong |
| Field copy map | **High** | Corrupt queue objects |
| Exact distance formula edge | **Medium** | Rare range bugs |

---

## 3. Cross-check against raw + bytes

```
entry: mov ebp,ecx; cmp [ebp+0x258],0; jz ret0; load entry; cmp type 0xC/0xD
raw distance + 007249c0 + strncpy + copies + 00480350 + return 1
≡ A sealed. Parent play branch: 004b7e50.
```

---

## 4. Surviving contract for AutoCore

```csharp
bool Audio_TryEnqueuePositioned(AudioTable t, FxEntry e, Vector3 worldPos)
{
    if (!t.Enabled) return false;
    if ((e.Type is 0xC or 0xD) && ConflictCheck(e)) return false;
    if (worldPos.Length() > 0 && PlanarDist2(t.RefXZ, worldPos) > e.MaxRange * e.MaxRange)
        return false;
    var name = ResolveName(e);
    if (name == null) return false;
    var q = CloneEntryForQueue(e, name);
    t.Queue.Push(q); // FUN_00480350(this+0x310)
    return true;
}
```

---

## 5. Open questions

1. `FUN_00722b80` / `FUN_007249c0` duals.
2. Whether range uses y as well in any build (this body: x/z dominant).
3. Runtime verification.

**Verdict:** **accept**
