# Dual residual: `0x009247b0` Client_InteractClickPickTarget

| Field | Value |
|-------|-------|
| Stable ID | `aa_009247b0` |
| Address | `0x009247b0` |
| Owner | dual residual only (OWN ONLY this unit; no WORK_QUEUE / RESUME / CHANGE_LOG) |
| Date | 2026-07-29 (strengthen) |
| Prior dual | A/B **accept-with-gaps** — UseObject gate 25.0f High; open: **ECX into 00524520**, gather taxonomy, dual-send wire, 0.015 scale product role |

## Residual questions (from dual A/B)

1. Exact float table for pick/gather radii: **25f** gate vs **0.015f** scale (task “25f/15f”).
2. Character **ECX** provenance into `FUN_00524520` / `FUN_005245d0` at this call site.
3. Soft gather **mode/filter** args vs sealed `Skill_GatherTargetsInArea` mode table (`aa_0058d330`).
4. Can one click fire **both** IfInteractable and SendUseObject (CF order + conditions).
5. Type-4 secondary still non-UseObject (`0x205D`).

## Seal A — Constants **25.0f** / **0.015f** (and companions)

Ghidra `read_memory` 2026-07-29 (re-check this pass ≡ prior):

| Symbol | VA | Bytes (LE) | IEEE float | Role in this unit |
|--------|-----|------------|------------:|-------------------|
| `DAT_00aaa6fc` | `0x00aaa6fc` | `00 00 c8 41` | **25.0** | (1) UseObject Euclidean gate; (2) soft gather **cap** path A/A2; (3) path-B radius **scale** (`scale*25`) |
| `_DAT_00aaacbc` | `0x00aaacbc` | `8f c2 75 3c` | **0.015** (exact float rep of 0.015) | Post-`FUN_0040d020` scale → `fStack_104` before radius mults |
| `DAT_00a110d8` | `0x00a110d8` | `00 00 20 41` | **10.0** | Soft A: `radius = scale * 10`, then min(radius, 25) |
| `DAT_00aaa688` | `0x00aaa688` | `00 00 a0 40` | **5.0** | Soft A2 empty-fallback: `radius = scale * 5`, cap 25 |
| `DAT_00a1109c` | `0x00a1109c` | `00 00 20 42` | **40.0** | Soft B: `radius = scale * 25`, then min(radius, 40) |

### Asm anchors (this function)

| Site | Asm | Meaning |
|------|-----|---------|
| `0x009249cd` | `FMUL [00aaacbc]` | ray-hit dist × **0.015** |
| `0x009249e0` | `MULSS XMM0,[00a110d8]` | × **10**; cap vs XMM1=`[00aaa6fc]` **25** |
| `0x00924aaf` | `MULSS XMM0,[00aaa688]` | × **5**; cap **25** |
| `0x00924c7b` / `0x00924c8e` | `FMUL 0.015` then `MULSS [00aaa6fc]` | path B: ×25 then cap **40** (`00a1109c`) |
| `0x00924ed1` | `FLD [00aaa6fc]` + `FSQRT` + `FCOMIP` | **UseObject** if `√(dx²+dy²+dz²) ≤ 25.0` |

UseObject polarity (FPU): `FCOMIP ST0=25, ST1=dist`; `JC` skips send when **25 < dist**. Equal / less → `Client_SendUseObject`. **High**.

**Do not** merge client **25f** with product server ~**30f** (`NpcInteractHandler`).

## Seal B — Matcher ECX + dual UseObject order

Live `get_assembly_context` at call sites inside `0x009247b0`:

### Stage 5 — store + objective matchers + IfInteractable

```
MOV  [EBX+0xd24], EDI          ; select slot [0x349]
MOV  [EBX+0x758], EDI          ; select slot [0x1d6]
…
MOV  ECX, [DAT_00d1b6d8]       ; *** character this ***
PUSH EDI                       ; world object
CALL 0x005245d0                ; CVOGCharacter_FindActiveObjectiveIdForPick (+0x44 eval)
TEST EAX,EAX
MOV  EAX, [EBX+0x758]
JNZ  → IfInteractable
MOV  ECX, [DAT_00d1b6d8]       ; *** character this again ***
PUSH EAX                       ; selected object
CALL 0x00524520                ; CVOGCharacter_FindActiveObjectiveIdForInteract (+0x40 eval)
TEST EAX,EAX
JZ   skip_IfInteractable
MOV  ESI, 0xd1a840
CALL 0x00930d70                ; Client_SendUseObject_IfInteractable
skip:
MOV  ECX, [EBX+0x758]
PUSH ECX
CALL 0x0093e120                ; selection UI always (if pick non-null path)
```

| Claim | Result | Conf |
|-------|--------|------|
| ECX for both matchers | **`DAT_00d1b6d8`** (player character global) | **Confirmed** |
| Stack arg | Picked / selected world object | **Confirmed** |
| Order | pick-matcher **then** interact-matcher OR → IfInteractable | **Confirmed** |
| Selection UI | `FUN_0093e120` after optional IfInteractable | **Confirmed** |

Closes dual open “Exact ECX character provenance into `FUN_00524520` at this site”.

### Stage 7 — unconditional UseObject (after alt-mode / skill-bind gates)

```
; obj = [EBX+0x758]; require obj+0x11c != 0 (earlier)
; pos delta vs player body vtbl+0x1a0
FSQRT
FLD  [DAT_00aaa6fc]            ; 25.0f
FCOMIP ST0,ST1
JC   skip                      ; dist > 25
MOV  ECX, EBX                  ; client controller this
CALL 0x00916740                ; Client_SendUseObject
```

### Dual-send product (CF sealed; wire open)

Same click **can** invoke both senders when:

1. Stage 5: `005245d0(obj)` **or** `00524520(char, obj)` ≠ 0 → **0x2072** via IfInteractable, **and**
2. Stage 7 still reached (`DAT_00d1bdfa==0`, select non-null, `DAT_00d1d900==-1`), and `obj+0x11c≠0`, and dist ≤ **25f** → **0x2072** via SendUseObject.

Not mutually exclusive in CF. Whether production content dual-fires is **runtime/wire open** (Probable static).

## Seal C — Soft gather taxonomy (modes, not “masks”)

Using sealed `Skill_GatherTargetsInArea` ABI (`aa_0058d330`):

```c
// (out, start, query, source, origin, radius, maxTargets, gatherMode,
//  filterA, chainRefresh, allowSelfMode1, filterB, filterC, sort, score)
```

| Path | When | radius formula | max | **mode** | other flags | Resolve |
|------|------|----------------|----:|---------:|-------------|---------|
| Soft A | `param_3==0` after ray hit | `scale*10` cap **25** | 1 | **3** living interact (Veh/Cre/Char) | allowSelf=1, filterB=1, score=−1.0 | type **0x12** only if `vtbl+0x1d8` + `FUN_0040b1b0` |
| Soft A2 | A empty TFID (`lo&hi==−1`) | `scale*5` cap **25** | — | **`FUN_0058cd60`** args `(…,1,5,0,0,1)` sibling gather | mode-5 product name open | resolve if non-empty |
| Soft B | still null (any `param_3`) | `scale*25` cap **40** | 1 | **2** hostile / enemy | allowSelf=1, filterB=0, filterC=1 | resolve any hit |

Literal “1,3 / 1,5 / 1,2” in older duals = **(maxTargets, gatherMode)** pairs (A/B) — **not** bitmasks. Mode English names remain INFERRED from gather dual residual; **numeric CF High**.

## Seal D — Type-4 still not UseObject

Switch on `*( *(obj+0xa8)+0x38 )`; case **4**: packet opcode imm bits **`0x205D`** size `0x10` via `Client_SendSectorPacket` — QuestItemPickup. Unchanged High.

## Re-decompile check

`decompile_function 0x009247b0` 2026-07-29 residual pass ≡ frozen raw body (no CF delta). Asm supplies ECX + FPU polarity missing from decompiler surface.

## Residual after this pass

| Item | Status |
|------|--------|
| 25.0f UseObject gate + gather multi-use | **Sealed Confirmed** |
| 0.015f post-dist scale | **Sealed Confirmed** |
| Companion 10/5/40 gather scales | **Sealed Confirmed** |
| Matcher ECX = `DAT_00d1b6d8` | **Sealed Confirmed** |
| Dual-send CF possible (order 5 then 7) | **Sealed High** static; wire open |
| Gather mode 3 / 2 numeric roles | **Sealed High** (via `aa_0058d330`) |
| Mode / helper **PDB English names** | Open (INFERRED only) |
| `FUN_0058cd60` product name / mode 5 | Open (sibling; out of deep ownership) |
| Soft vs commit UX product English | Open (`param_3` 0/1 from `FUN_009251d0`) |
| Runtime click / dual-wire capture | Open |
| Bit-exact / image diff | Open |

## Artifacts touched

- This residual: `docs/reconstruction/reviews/a_009247b0.md`
- Dual A/B: `A_aa_009247b0_*`, `B_aa_009247b0_*`
- Function record: `functions/aa_009247b0_Client_InteractClickPickTarget.md`
- Scratch refresh: `tmp/a_009247b0.md`
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / VERIFICATION_MATRIX
- **Not** Launcher
