# Review A (reconstruction fidelity): `aa_007a6260` FUN_007a6260 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_007a6260` |
| **VA** | `0x007a6260` |
| **Canonical name** | `FUN_007a6260` (**retained**; behavior = ensure ring-slot capacity) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Reconstruction fidelity |
| **Counterpart** | `reviews/B_aa_007a6260_FUN_007a6260.md` |
| **System** | locale / string-pool (FormatFailure → `FUN_007a6de0` helpers) |
| **Verdict** | **leave-FUN** — CF sealed; exclusive-use naming gate fails (multi-caller helper) |

**Evidence:** Ghidra `decompile_function` @ `0x007a6260`; parent dual `aa_007a6de0`.

---

## 1. Purpose

**Ensure current ring slot buffer** on string-pool `this` (ESI) holds at least **EAX** bytes; zero the buffer.

1. `idx = *(this+0xc0)`; `cap = *(this+0x6c + idx*4)`; `slot = *(this+4 + idx*4)`.
2. If `cap < needed(EAX)`: `delete[] slot`; store new cap; `slot = new[](cap)`; write back pointer.
3. Always: dword-then-byte **zero** of entire capacity.

Register contract (decompiler unaff): **ESI = pool**, **EAX = required size**. Callers compute strlen-related sizes before call.

---

## 2. Inspected artifacts

| Artifact | Path |
|---|---|
| Live decompile | `0x007a6260` |
| Raw | `docs/reconstruction/raw/aa_007a6260_FUN_007a6260.md` |
| Annotated / clean / record | `aa_007a6260_*` three-rep scaffold |
| Callers (string pool) | `FUN_007a62c0`, `FUN_007a6310`, `FUN_007a6490`, mode paths in `FUN_007a6de0` |

---

## 3. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| Grow when cap < needed | **Confirmed** | |
| `delete[]` / `new[]` on ring slot only | **Confirmed** | |
| Zero full capacity after ensure | **Confirmed** | |
| Uses ring index `+0xc0` | **Confirmed** | matches parent pool layout |
| Cap array at `+0x6c` | **Confirmed** | |
| Product method name | **Open** | leave FUN_* |
| ABI formalization beyond ESI/EAX | **Tentative** | custom register protocol |

---

## 4. Pool offsets (shared with `FUN_007a6de0`)

| Offset | Role |
|---|---|
| `+4 + idx*4` | slot pointer |
| `+0x6c + idx*4` | slot capacity |
| `+0xc0` | ring index 0..9 |

---

## 5. Gaps

1. Exact caller size formulas (strlen vs strlen+1 vs 2*strlen) live in parents.
2. No null-check on `new[]` failure (OOM UB residual).

**Verdict:** **leave-FUN**.
