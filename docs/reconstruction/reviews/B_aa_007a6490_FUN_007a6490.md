# Review B (skeptical / independent): `aa_007a6490` FUN_007a6490 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_007a6490` |
| **VA** | `0x007a6490` |
| **Canonical name** | `FUN_007a6490` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Skeptical |
| **Counterpart** | `reviews/A_aa_007a6490_FUN_007a6490.md` |
| **System** | locale / string-pool |
| **Verdict** | **leave-FUN** |

---

## 1. Claims under attack

| # | Claim | Result |
|---|---|---|
| 1 | Identity hash / pointer cast | **Falsified** — full bit poly walk |
| 2 | ISO-HDLC CRC32 bit-exact without audit | **Overstated** — poly yes; finalization open |
| 3 | Handles all C escapes (`\\t`, `\\xHH`) | **Falsified** — only `\\n` special-cased |
| 4 | Does not mutate pool slot | **Falsified** — writes escaped key into ring |
| 5 | Localizes to target language by itself | **Falsified** — hash only; load is 6310 |

---

## 2. Surviving contract

```
hash = FUN_007a6490(pool, src):
  ensure; strcpy(slot, src)
  escape \\n / drop CR & 0xA0 → stack
  ensure; strcpy(slot, escaped)
  return crc32_poly_4c11db7_init_ffffffff(slot)  // final xor residual
```

Agree with A. **Verdict:** **leave-FUN**.
