# Independent adversarial spot-check — WQ9R-J dual `0x0051b550` + `0x00518e30`

| Field | Value |
|---|---|
| **Date** | `2026-08-04` |
| **Role** | Independent adversarial verifier (**not** WQ9R-J dual author) |
| **Workspace** | `C:\Users\josh\Documents\GitHub\AutoCore` |
| **Partition** | WQ9R-J OWN pair (WQ-009 residual — skill-bank helpers) |
| **Units** | `0x0051b550` `Object_LoadCloneBaseSkillTrio_Inferred`; `0x00518e30` `Object_CreateOrRecreateSkillHash_Inferred` |
| **Tools** | Ghidra MCP `batch_decompile`, `read_memory`, `get_function_callers`, `get_xrefs_to`; dual/report + clean artifact read-through |
| **Forbidden** | `disassemble_bytes`; parent ledger edits; dual rewrite |
| **Image** | `autoassault.exe` base `0x400000` |

---

## Inspected evidence

### Dual / report surfaces

| Path |
|---|
| `docs/agents/task-dual-ab-0051b550-00518e30-wq9rj-report.md` |
| `docs/reconstruction/reviews/A_aa_0051b550_Object_LoadCloneBaseSkillTrio_Inferred.md` |
| `docs/reconstruction/reviews/B_aa_0051b550_Object_LoadCloneBaseSkillTrio_Inferred.md` |
| `docs/reconstruction/reviews/A_aa_00518e30_Object_CreateOrRecreateSkillHash_Inferred.md` |
| `docs/reconstruction/reviews/B_aa_00518e30_Object_CreateOrRecreateSkillHash_Inferred.md` |

### Artifacts (both units)

| Kind | `aa_0051b550` | `aa_00518e30` |
|---|---|---|
| Raw | `docs/reconstruction/raw/aa_0051b550_FUN_0051b550.md` | `docs/reconstruction/raw/aa_00518e30_FUN_00518e30.md` |
| Annotated | `…/aa_0051b550_FUN_0051b550.annotated.md` | `…/aa_00518e30_FUN_00518e30.annotated.md` |
| Clean named | `…/Object_LoadCloneBaseSkillTrio_Inferred.cpp` | `…/Object_CreateOrRecreateSkillHash_Inferred.cpp` |
| Clean twin | `…/FUN_0051b550.cpp` | `…/FUN_00518e30.cpp` |
| Function named | `…/aa_0051b550_Object_LoadCloneBaseSkillTrio_Inferred.md` | `…/aa_00518e30_Object_CreateOrRecreateSkillHash_Inferred.md` |

### Live Ghidra spot-checks

| Check | Result |
|---|---|
| `batch_decompile(0x0051b550,0x00518e30)` | Live CF matches dual seals (hash-then-trio; create-or-recreate at `+0x70`) |
| `batch_decompile(0x004cbdc0,0x0051dd60)` | Recreate logs locked-traversal strings then continues; ctor sets vtbl `PTR_FUN_009ce1b8`, mask `1<<log2`, log2 @ `+0x1c` |
| `read_memory` full body `0x0051b550` (128 B) | `56 57 6A 01 8B F1 E8…5F 5E C3` — push 1, call hash factory, three ensure patterns, bare ret |
| `read_memory` full body `0x00518e30` (144 B incl. pad) | SEH `6A FF 68 27 36 9A 00…`; three exits `C2 04 00`; pad `CC CC`; next `0x00518ec0` |
| `read_memory` orphan site `0x0052a2b8+` | `6A 04` then `E8` → `0x00518e30` (log2=4) |
| `read_memory` bulk loadout call `0x004c8a75` | `E8` rel → `0x0051b550` (first action of `FUN_004c8a60`) |
| `get_function_callers` / `get_xrefs_to` `@0x0051b550` | **1** CODE (`FUN_004c8a60` @ `0x004c8a75`) + **24** DATA vtbl slots = **25** total |
| `get_function_callers` / `get_xrefs_to` `@0x00518e30` | **2** CODE: `FUN_0051b550` @ `0x0051b556`; orphan @ `0x0052a2c5` |

---

## Unit A — `0x0051b550` Object_LoadCloneBaseSkillTrio_Inferred

### Body / ABI seal (independent)

Full body hex (128 B exclusive end `0x0051b5d0`; next code starts immediately, no `CC` pad):

```text
56 57 6A 01 8B F1 E8 D5 D8 FF FF   ; push esi/edi; push 1; mov esi,ecx; call 0x00518e30
8B 86 A8 00 00 00 8B 48 3C         ; clonebase path: [esi+0xA8] → [+0x3C]
8B B9 BC 03 00 00                   ; skill0 = [cloneBase+0x3BC]
8B 16 8B CE FF 92 64 02 00 00       ; call [vtbl+0x264]
66 0F B6 C0 8B CE 50 57 E8 …        ; movzx rank; EnsureLoadedInTree → 0x0051a980
… same pattern for +0x3C0 / vtbl+0x268 and +0x3C4 / vtbl+0x26C …
5F 5E C3                            ; pop edi; pop esi; ret
```

| Claim | Independent result |
|---|---|
| Range `0x0051b550`–`0x0051b5d0` exclusive (**128 B** / `0x80`) | **Confirmed** (`C3` last byte; next fn @ `0x0051b5d0`) |
| **thiscall** / **fastcall** ECX=`owner*`; no stack formals; bare **`C3`** | **Confirmed** (`8B F1`; epilogue `5F 5E C3`) |
| First call `Object_CreateOrRecreateSkillHash` with imm **1** | **Confirmed** (`6A 01`; `E8` @ `0x0051b556` → `0x00518e30`) |
| Clonebase: `this+0xA8` → `+0x3C` | **Confirmed** (`8B 86 A8 00 00 00` / `8B 48 3C`; decomp `param_1[0x2a]` ≡ `+0xA8`) |
| Skill ids `+0x3BC` / `+0x3C0` / `+0x3C4` | **Confirmed** (`BC 03` / `C0 03` / `C4 03`) |
| Ranks from vtbl `+0x264` / `+0x268` / `+0x26C` via **byte** return + `movzx` | **Confirmed** (`FF 92 64 02…` / `68 02` / `6C 02` + `66 0F B6`) |
| `Skill_EnsureLoadedInTree` ×3 (`0x0051a980`) | **Confirmed** (three `E8` rels land on `0x0051a980`; decomp names callee) |
| Order: hash recreate **then** slots 0→1→2 | **Confirmed** |
| Void (no EAX contract) | **Confirmed** (no mov-to-eax return path) |
| Virtual method + bulk caller | **Confirmed** (24 DATA + 1 CODE; `FUN_004c8a60` calls this **first**, then walks equip tree at clonebase `+0x520`) |

### Live decompile (independent summary)

```c
void __fastcall FUN_0051b550(int *param_1)
{
  FUN_00518e30(1);
  // skill id from *(*(param_1[0x2a]+0x3c)+0x3bc/0x3c0/0x3c4)
  // rank = (byte)(*param_1 + 0x264/268/26c)()
  // Skill_EnsureLoadedInTree(param_1, skillId, (ushort)rank) ×3
}
```

Matches clean named reconstruction and dual A CF table. Decompiler omits explicit this on `FUN_00518e30` display but bytes seal ECX=owner.

### Adversarial attacks (LoadCloneBaseSkillTrio)

| # | Attack | Result |
|---|---|---|
| 1 | This VA **is** the bulk loadout walker `FUN_004c8a60` | **Fails** — `004c8a60` **calls** this first, then walks equip tree |
| 2 | Hash call is soft "ensure if null only" | **Fails** — callee `0x00518e30` **recreates** when `+0x70` already set |
| 3 | Ranks come from clonebase memory next to skill ids | **Fails** — ranks from **vfunc** returns (`movzx` after `call [vtbl+off]`) |
| 4 | Skill ids from `this` direct fields | **Fails** — path `this+0xA8 → +0x3C → +0x3BC/C0/C4` |
| 5 | Returns skill pointers / count | **Fails** — void; bare `C3`; no EAX contract |
| 6 | `vtbl+0x264` is vehicle melee hardpoint **field** | **Fails** — here **vtable method offset**, not object field `+0x264` |
| 7 | Only one caller total | **Fails** (as sole-xref claim) — one **code** caller; **24** vtbl DATA installs |
| 8 | log2 always 1 means product "two buckets forever" | **Weak / site-local only** — imm 1 sealed **here**; orphan site uses push **4** |
| 9 | Product/PDB English sealed | **Fails** — `_Inferred` hygiene correct; demangle open |
| 10 | Ready for bit-exact / Confirmed runtime | **Fails** — dual gap correctly owned |

### Soft niggles (not sealed-claim failures)

1. **Decompiler this-elision** on nested `FUN_00518e30(1)` — display omits ECX; dual/clean document thiscall correctly.
2. **Type of `*(this+0xA8)`** remains unlabeled (dual open gap).
3. **Rank vfunc English** per concrete class open (docs-only).

### Unit A dual quality

**PASS** — keep **`accept-with-gaps`**.

Wipe-then-three-clonebase-skill CF, ABI (thiscall/fastcall, bare ret), body 128 B, callee graph, and vtbl+bulk caller role are independently confirmed. Dual B correctly rejects bulk-alias, soft-ensure-hash, rank-from-memory, and hardpoint-field confusions. Gaps (product English, host type, runtime) remain fair residuals.

---

## Unit B — `0x00518e30` Object_CreateOrRecreateSkillHash_Inferred

### Body / ABI seal (independent)

Prologue / branch / exits (from `read_memory` @ `0x00518e30`, 144 B window including pad):

```text
6A FF 68 27 36 9A 00                 ; SEH: push -1; push LAB_009a3627
64 A1 00 00 00 00 50 64 89 25 …      ; fs:[0] frame
51 56 8B F1                           ; push ecx; push esi; mov esi, ecx  (owner)
8B 4E 70 85 C9 74 1C                  ; mov ecx,[esi+0x70]; test; jz create
8B 44 24 18 50 E8 66 2F FB FF         ; push log2; call FUN_004cbdc0 (ECX still = hash)
… SEH teardown … C2 04 00            ; ret 4
6A 34 E8 …                            ; push 0x34; operator_new
… if non-null: mov ecx,block; push log2; call FUN_0051dd60; store [esi+0x70]
… else: store 0 at [esi+0x70]
… C2 04 00 (×2 remaining exits)
CC CC                                 ; pad; next Object_SharedBase_Dtor @ 0x00518ec0
```

| Claim | Independent result |
|---|---|
| Range `0x00518e30`–`0x00518ebe` exclusive (**142 B** / `0x8E`) + `CC CC` pad | **Confirmed** (three `C2 04 00`; pad @ `0x00518ebe`; next @ `0x00518ec0`) |
| **thiscall**; ECX=owner; stack **log2**; void; **`ret 4`** | **Confirmed** (`8B F1`; all exits `C2 04 00`) |
| Slot `owner+0x70` is skill hash | **Confirmed** (load/test/store only that field; shared with EnsureLoadedInTree duals) |
| Existing → recreate on **hash** ECX | **Confirmed** (`8B 4E 70` leaves ECX=hash through `E8` → `0x004cbdc0`) |
| Missing → `new(0x34)` + ctor `FUN_0051dd60` | **Confirmed** (`6A 34`; call `0x0051dd60`; store result) |
| OOM stores 0 | **Confirmed** (fall-through zero store path) |
| log2 = size exponent (`1 << (log2&0x1f)`) | **Confirmed** via live decompile of ctor/recreate callees |
| Callers: trio loader push 1; orphan push 4 | **Confirmed** (`0x0051b556` push 1; `0x0052a2c5` preceded by `6A 04`) |
| Not inventory `CNDHash_Recreate` `0x004138d0` type-identity | **Confirmed** (different VA/helper `0x004cbdc0`; skill vtbl `009ce1b8`) |

### Live decompile (independent summary)

```c
void __thiscall FUN_00518e30(int param_1, undefined4 param_2)
{
  // SEH LAB_009a3627
  if (*(int *)(param_1 + 0x70) != 0) {
    FUN_004cbdc0(param_2);   // decompiler elides hash ECX; bytes: ECX=[param_1+0x70]
    return;
  }
  pvVar1 = operator_new(0x34);
  if (pvVar1) *(undefined4 *)(param_1 + 0x70) = FUN_0051dd60(param_2);
  else        *(undefined4 *)(param_1 + 0x70) = 0;
}
```

**Decompiler hazard (real):** recreate path shown as `FUN_004cbdc0(param_2)` without hash this. Dual A/B and clean correctly restore ECX=hash from asm. Clean named `Object_CreateOrRecreateSkillHash_Inferred.cpp` matches byte CF.

### Support callees (spot, not full dual)

| Callee | Independent note | Dual claim |
|---|---|---|
| `FUN_004cbdc0` | If lock flag `@+0x1d`: log `"HashError:Recreate, already locked for traversal"` + `"VOG_DEBUG_STOP"` then **continues**; sets log2 `@+0x1c`, count `1<<(log2&0x1f)` | **OK** — no hard-abort |
| `FUN_0051dd60` | vtbl `PTR_FUN_009ce1b8`; bucket count `1<<log2`; log2 stored as byte at `hash+0x1c` (`param_1[7]`); returns this | **OK** |

### Adversarial attacks (CreateOrRecreateSkillHash)

| # | Attack | Result |
|---|---|---|
| 1 | Soft ensure: no-op when `+0x70` already set | **Fails** — existing path **always** calls recreate |
| 2 | `FUN_004cbdc0` thiscall ECX is owner | **Fails** — `mov ecx,[esi+0x70]` before call |
| 3 | Same type as inventory `CNDHash_Recreate` `0x004138d0` | **Fails as identity** — shared string *family* only; different VA/vtbl/helpers |
| 4 | Returns hash pointer in EAX for callers | **Fails** — void; `ret 4`; trio loader ignores EAX |
| 5 | `param_2` is skill id or rank | **Fails** — log2 size exponent (ctor/recreate) |
| 6 | `new(0x34)` is skill runtime object size (e.g. 0x630) | **Fails** — hash header sizeof only |
| 7 | Sole caller is `FUN_0051b550` | **Fails** (as sole-xref) — second xref `0x0052a2c5` with push 4 |
| 8 | Recreate aborts process when traversal-locked | **Fails for stop** — callee logs then continues |
| 9 | Product class name sealed | **Fails** — vtbl only; `_Inferred` correct |
| 10 | Bit-exact complete | **Fails** — open (dual gap) |

### Soft niggles (not sealed-claim failures)

1. **Orphan owner function** for `0x0052a2c5` still unnamed in Ghidra (dual open gap).
2. **Full dual** of `FUN_004cbdc0` / `FUN_0051dd60` support-only here — sealed enough for this unit's CF, not promoted as complete product duals.
3. Whether freelist/node layout matches inventory CNDHash **bit-for-bit** remains open (docs/port risk only if merging layouts).

### Unit B dual quality

**PASS** — keep **`accept-with-gaps`**.

Create-or-recreate CF, ABI (`ret 4`, stack log2), sizeof `0x34`, slot `+0x70`, and both call sites are independently confirmed. Dual B correctly rejects soft-ensure, owner-as-hash-this, inventory type-merge, and return-pointer claims. Product English residual only.

---

## Cross-unit consistency

| Topic | Independent |
|---|---|
| Pair relationship | **Confirmed** — trio loader always recreates hash (log2=1) then EnsureLoadedInTree ×3 |
| Shared skill hash slot `owner+0x70` | **Both** — factory writes; trio/ensure consumers (prior duals) |
| Role split | `00518e30` = hash lifecycle only; `0051b550` = wipe + default trio seed |
| Bulk path | `FUN_004c8a60` → trio first → equip-tree walk with more EnsureLoadedInTree |
| log2 values | Site-sealed: **1** (trio), **4** (orphan) — not a global constant |
| Clean vs decompile | Both cleans restore hash ECX on recreate; trio clean matches byte offsets |
| Naming | Both `_Inferred`; scaffold auto names correctly not promoted |

```text
FUN_004c8a60 (bulk loadout)
  └─ Object_LoadCloneBaseSkillTrio_Inferred (0x0051b550)
        ├─ Object_CreateOrRecreateSkillHash_Inferred (0x00518e30, log2=1)
        └─ Skill_EnsureLoadedInTree ×3  (cloneBase skills + vfunc ranks)
  └─ … walk equip tree (+0x520), more EnsureLoadedInTree …
```

---

## Confirmations

1. WQ9R-J dual A/B + report seals for both VAs are **faithful** to live decompile + `read_memory` + callers/xrefs.
2. Trio: wipe-first (recreate, not soft ensure), three clonebase skill ids, three rank vfuncs, void bare ret — **confirmed**.
3. Hash factory: `+0x70` create-or-recreate, sizeof `0x34`, `ret 4`, hash-as-this on recreate — **confirmed**.
4. Decompiler hazards dual documents (recreate ECX elision; optional thiscall display on trio→hash) are **real and correctly rejected**.
5. Clean named sources match byte-sealed semantics (hash ECX restored; offsets exact).
6. Xref counts match dual: trio **25** (1 CODE + 24 DATA); hash **2** CODE sites.

---

## Gaps (remain open — dual already owns)

| Gap | Unit | Blocks accept? |
|---|---|---|
| Product / MSVC demangle | Both | No (`accept-with-gaps`) |
| Clonebase skill-column English; rank vfunc English | Trio | No |
| Type name for `*(this+0xA8)` host | Trio | No |
| Product name for skill CNDHash (`vtbl 009ce1b8`) | Hash | No |
| Full dual of `FUN_004cbdc0` / `FUN_0051dd60` | Hash (support) | No |
| Owning function for orphan call `0x0052a2c5` | Hash | No |
| Runtime / bit-exact / differential | Both | No (policy; terminal false) |

---

## Pass / fail

| Unit | Dual verdict | ADV result |
|---|---|---|
| `0x0051b550` Object_LoadCloneBaseSkillTrio_Inferred | **accept-with-gaps** | **PASS** — retain **accept-with-gaps** |
| `0x00518e30` Object_CreateOrRecreateSkillHash_Inferred | **accept-with-gaps** | **PASS** — retain **accept-with-gaps** |

### Overall

**PASS** — WQ9R-J dual seals hold under independent Ghidra `batch_decompile` + `read_memory` + caller/xref verification. No sealed-claim failure requiring dual rewrite or verdict downgrade. Soft-ensure and inventory-hash type-merge attacks remain correctly rejected.

---

## Process notes

- Independent ADV verifier; **not** WQ9R-J author.
- Tools: `batch_decompile`, `read_memory`, `get_function_callers`, `get_xrefs_to` only for RE; **no** `disassemble_bytes`.
- **No** parent ledger / WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG edits.
- Output: `docs/reconstruction/reviews/ADV_wq009_spotcheck_0051b550_00518e30.md` (this file).
