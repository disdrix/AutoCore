# Review A (reconstruction fidelity): `aa_007a6490` FUN_007a6490 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_007a6490` |
| **VA** | `0x007a6490` |
| **Canonical name** | `FUN_007a6490` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Reconstruction fidelity |
| **Counterpart** | `reviews/B_aa_007a6490_FUN_007a6490.md` |
| **System** | locale / string-pool (hash + escape; FormatFailure modes 1–3) |
| **Verdict** | **leave-FUN** — escape + CRC-like hash sealed; product name open |

**Evidence:** Ghidra `decompile_function` @ `0x007a6490` (`__chkstk` / 4K local).

---

## 1. Purpose

**Normalize a key string and compute a 32-bit hash** used by `FUN_007a6410`:

1. `strlen` of source (EDI); `FUN_007a6260` ensure current ring slot.
2. Copy source → ring slot.
3. Walk slot: build escaped form into 4K stack buffer:
   - `\\n` → newline
   - skip `0xA0` and `\r`
   - other bytes copy through
4. Ensure again; copy escaped buffer → ring slot.
5. **CRC-style hash** over slot bytes:
   - poly **`0x4c11db7`** (Ethernet/CRC32 poly family)
   - init `0xffffffff`
   - per-byte: xor into high byte, then **8** conditional double-xor steps
6. Return hash in EAX (decompiler shows compute; parent uses it as EBX key).

---

## 2. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| Escape `\\n` → `\n` | **Confirmed** | |
| Skip `\r` and `0xA0` | **Confirmed** | |
| Poly `0x4c11db7` | **Confirmed** | |
| Init `0xffffffff` | **Confirmed** | |
| Uses ring slot + ensure | **Confirmed** | |
| Standard CRC32 final xor `^0xffffffff` | **Open** | not observed in body |
| Product “CRC32” label | **Tentative** | poly match only |

---

## 3. Artifacts

| Artifact | Path |
|---|---|
| Three-rep | `aa_007a6490_*` |
| Parent | `FUN_007a6de0` modes 1–3 |
| Consumers | hash → `FUN_007a6410` |

---

## 4. Gaps

1. Final XOR / reflection vs ISO CRC32.
2. 4K stack cap on escaped length (truncation residual if over).
3. Register ABI (EDI source, ECX pool).

**Verdict:** **leave-FUN**.
