# Dual residual: `0x00941ac0` Client_CastAfterPrompt

| Field | Value |
|-------|-------|
| Stable ID | `aa_00941ac0` |
| Address | `0x00941ac0` |
| Canonical name | `Client_CastAfterPrompt` |
| Owner | dual residual only (no WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 |
| Prior | scaffold only (`FUN_00941ac0`); open: unaff ESI/EDI, signature, cast pipeline link |

## Residual questions (from scaffold)

1. Recover `unaff_ESI` / `unaff_EDI` (decomp register formals).
2. Confirm skill arg is `*(client+0x30c0)` into `Client_RequestCastSkill`.
3. Role of stack `param_1` (aim).
4. Callers / when this path runs vs direct `CastSkillFromQuickBarSlot`.
5. Always-clear of prompt after cast / block.

## Seal: ABI (Confirmed)

Live bytes @ `0x00941ac0` + both call sites:

```
; entry
SUB  ESP, 8
MOV  EAX, EDI                 ; client → CastBlockFeedback
CALL Client_CastBlockFeedback ; 0x0093b7f0
TEST AL, AL
JNZ  clear_only               ; blocked → skip RequestCast

MOV  EDX, [ESP+0xC]           ; param_1 = aim*
MOV  ECX, [EDI+0x30C4]        ; show-flag load (dead store to stack; not a cast arg)
MOV  EAX, [EDI+0x30C0]        ; **pending skill id**
PUSH EDX                      ; aim
; copy 16B TFID from *ESI onto stack (SUB ESP,10 + 4 dwords)
PUSH EAX                      ; skillId
MOV  EAX, EDI                 ; client for RequestCast (EAX formal)
CALL Client_RequestCastSkill  ; 0x00941590 ; callee RET 0x18

clear_only:
PUSH 0                        ; showPrompt = 0
PUSH EDI                      ; client
OR   EAX, -1                  ; modeOrSkill = -1
CALL Client_PromptSkillTargetSelect  ; 0x0093bac0
ADD  ESP, 8
RET  4                        ; stdcall, 1 stack formal (aim*)
```

| Formal | Location | Meaning | Conf |
|--------|----------|---------|------|
| client | **EDI** (not stack) | global client / `&DAT_00d1a840` | **Confirmed** |
| pTfid | **ESI** | 16-byte TFID seed (typically `obj+0x160`) | **Confirmed** |
| pAim | stack `[ESP+4]` at entry / `RET 4` | aim float3* | **Confirmed** |
| skillId | **not a formal** — read `*(EDI+0x30c0)` | pending skill from prompt | **Confirmed** |

### Call-site recovery (both xrefs)

| Site | VA | Pre-call | aim arg |
|------|-----|----------|---------|
| `Client_InteractClickPickTarget` | `0x00924e29` | `CMP [DAT_00d1d900],-1`; `LEA ESI,[EAX+0x160]`; `MOV EDI,0xD1A840`; `PUSH &DAT_00d1a640` | look/default aim `0x00d1a640` |
| `FUN_00861680` | `0x00861a52` | same gate + ESI/EDI pattern; `PUSH &DAT_00d1a660` | sibling aim buffer `0x00d1a660` |

**Gate:** only when `DAT_00d1d900 != -1`.

## Seal: `DAT_00d1d900` ≡ `client+0x30c0` (Confirmed)

```
&DAT_00d1a840 + 0x30c0 = 0x00d1d900
&DAT_00d1a840 + 0x30c4 = 0x00d1d904
```

Absolute globals used by interact/ESC paths **are** the client singleton fields for pending skill / show-prompt. Call-site `CMP DAT_00d1d900,-1` ≡ “pending mode/skill is active (not cleared)”.

## Seal: `+0x30c0` → RequestCastSkill skill arg (Confirmed)

| Step | Evidence |
|------|----------|
| Enter prompt stores skill | `CastSkillFromQuickBarSlot` `MOV EAX,skillId` → `PromptSkillTargetSelect` → `*(client+0x30c0)=EAX` |
| This unit reads it | `MOV EAX,[EDI+0x30c0]` then `PUSH EAX` as RequestCast arg0 |
| Packet skillId | RequestCast residual: formal `nSkillId` @ packet `+0x18` |
| Clear after | `OR EAX,-1` → Prompt → `*(client+0x30c0)=-1` |

**Product:** cast-after-prompt completes target-select by casting the **stored pending skill id**, not a skill passed on the stack.

## Seal: TFID from pick object `+0x160` (Confirmed)

Both callers:

```
LEA ESI, [selectedObj + 0x160]
```

Body copies `ESI[0..3]` as four dwords into RequestCast’s TFID formals (seed for `Client_Skill_ResolveCastTarget` inside RequestCast — not a raw packet bypass).

## Seal: CastBlock then always clear (Confirmed)

```
if (Client_CastBlockFeedback(client) == 0)   // 0 = allow
    Client_RequestCastSkill(skillFrom+0x30c0, *tfid…, pAim);  // EAX=client
Client_PromptSkillTargetSelect(client, show=0);  // EAX=-1 always
```

Blocked path (dead/town/stunned/… toast from CastBlock) **still clears** target-select chrome. Matches sibling `CastSkillFromQuickBarSlot` treat of nonzero CastBlock as abort cast (there: early `return` before prompt enter; here: skip send, still clear).

## Pipeline (sealed)

```
CastSkillFromQuickBarSlot
  flags skill+0x614: 0x800 set, 0x20 clear
  → PromptSkillTargetSelect(client, show=1)  // EAX=skillId → +0x30c0
  → return (player aims)

Player click pick (InteractClickPickTarget / FUN_00861680)
  when DAT_00d1d900 != -1  // pending active
  → Client_CastAfterPrompt(aim*)
       EDI=client, ESI=obj+0x160 TFID
       → CastBlock? skip send : RequestCast(*(client+0x30c0), TFID, aim)
       → Prompt(-1, 0) clear

ESC (OnKeyDown): Prompt(-1,0) only — no RequestCast
```

Closes scaffold gaps: unaff registers, skill source, aim formal, callers, clear polarity.

## Re-decompile parity (2026-07-29)

Live Ghidra `decompile_function` @ `0x00941ac0` **≡** frozen raw body (CastBlock → conditional RequestCast from `+0x30c0`/`*ESI` → Prompt). Asm supplies register formals decomp labeled `unaff_*` and dead `+0x30c4` load.

## Residual after this pass

| Item | Status |
|------|--------|
| Name `Client_CastAfterPrompt` | **Sealed** (role; Ghidra still `FUN_00941ac0` until rename) |
| EDI=client, ESI=pTfid, stack=pAim | **Sealed Confirmed** |
| skillId = `*(client+0x30c0)` | **Sealed Confirmed** |
| `DAT_00d1d900` = absolute `+0x30c0` | **Sealed Confirmed** |
| Always Prompt clear EAX=-1 | **Sealed Confirmed** |
| CastBlock gate polarity | **Sealed Confirmed** (0=allow) |
| `+0x30c4` load before cast | **Dead store High** — not passed to RequestCast |
| Aim buffers `DAT_00d1a640` / `00d1a660` product names | Open (role: default aim float3) |
| `FUN_00861680` product name | Open (second complete-cast click path) |
| Entry `SUB ESP,8` micro-frame vs `RET 4` | Low residual (behavior OK at call sites; not product-facing) |
| Runtime / CE / bit-exact | Open |

## Artifacts touched by residual owner

- This scratch: `reviews/a_00941ac0.md`
- Duals: `A_aa_00941ac0_Client_CastAfterPrompt.md`, `B_aa_00941ac0_Client_CastAfterPrompt.md`
- Function record, clean `Client_CastAfterPrompt.cpp` (replaces scaffold `FUN_00941ac0.cpp` plate), annotated
- `systems/skills-abilities.md` entry + complete path line
- `NAMING_REGISTRY.md` row
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / VERIFICATION_MATRIX
