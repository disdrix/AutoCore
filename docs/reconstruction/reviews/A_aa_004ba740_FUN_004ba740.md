# Review A (reconstruction fidelity): `aa_004ba740` FUN_004ba740

| Field | Value |
|---|---|
| **Stable ID** | `aa_004ba740` |
| **VA** | `0x004ba740` |
| **Canonical name** | `FUN_004ba740` |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Independent reconstruction review |
| **Counterpart** | `reviews/B_aa_004ba740_FUN_004ba740.md` |
| **System** | client FX entry — read float4/param block at `+0x108` |
| **Verdict** | **accept** — 4-dword copy out **sealed** |

---

## 1. Purpose

**Copy four dwords** from FX/entry object fields **`this+0x108 .. +0x114`** into caller-provided out buffer `param_2`.

Used by `FUN_004b7e50` (and peers) to read the param/float block adjacent to FX entry flags (`+0x104` type, `+0x108` flag byte in entry layout — here treated as start of a 16-byte block when `this` is the entry).

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Raw | `docs/reconstruction/raw/aa_004ba740_FUN_004ba740.md` |
| Annotated | `docs/reconstruction/raw/aa_004ba740_FUN_004ba740.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/FUN_004ba740.cpp` |
| Function record | `docs/reconstruction/functions/aa_004ba740_FUN_004ba740.md` |
| Live decompile | Ghidra `batch_decompile` `0x004ba740` — ≡ raw |
| Live bytes | Ghidra `read_memory` `0x004ba740` length 32 |
| Callers | `FUN_004b7e50` and other FX readers |

---

## 3. Sealed residuals

### 3.1 ABI — **SEALED**

| Fact | Evidence |
|---|---|
| Convention | **`__thiscall`**: `this` in **ECX** |
| Args | **`undefined4* out`** (4 dwords) |

```c
void __thiscall FUN_004ba740(void *this, uint32_t *out4);
// out[0]=this+0x108, out[1]=+0x10C, out[2]=+0x110, out[3]=+0x114
```

### 3.2 Algorithm — **SEALED**

```
out[0] = *(this+0x108)
out[1] = *(this+0x10C)
out[2] = *(this+0x110)
out[3] = *(this+0x114)
```

### 3.3 Entry machine bytes — **SEALED**

```
8B 44 24 04             mov  eax, [esp+4]     ; out
81 C1 08 01 00 00       add  ecx, 0x108       ; this → +0x108
56                      push esi
8B 31                   mov  esi, [ecx]
8B D0                   mov  edx, eax
89 32                   mov  [edx], esi
8B 71 04                mov  esi, [ecx+4]
89 72 04                mov  [edx+4], esi
8B 71 08                mov  esi, [ecx+8]
89 72 08                mov  [edx+8], esi
8B 49 0C                mov  ecx, [ecx+0xC]
... store to [edx+0xC]
```

Hex prefix:  
`8b44240481c108010000568b318bd089328b71048972048b71088972088b490c`

---

## 4. Control flow: clean ≡ raw ≡ live

| Stage | Match |
|---|---|
| 4× dword copy from `+0x108` | Yes |
| No branches | Yes |

---

## 5. Gaps / open

1. Whether the four dwords are floats, mixed flags, or a float3+pad (entry layout in `004b7e50` dual notes `+0x108` as flag byte — **reinterpretation risk**: same offset used as both flag byte and block start depending on object type).
2. Product field names.
3. Runtime verification not run.

**Verdict:** **accept** — ABI/offsets sealed; semantic type of the four dwords **open**.
