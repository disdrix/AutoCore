# Dual residual strengthen: `0x00519150` Skill_GetCategoryCooldownRemaining

| Field | Value |
|-------|-------|
| Stable ID | `aa_00519150` |
| Address | `0x00519150` |
| Owner | dual residual only (**no** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 |
| Prior dual | A/B **accept-with-gaps** (2026-07-23) — open: clock source, float ms→sec constant |
| Live evidence | Ghidra `decompile_function` + `read_memory` body/tail + `audit_globals_in_function` + caller `get_assembly_context` (2026-07-29) |

## Residual questions (from prior dual)

1. Exact global clock for start/deadline compare
2. Float ms→sec constant seal
3. (Implicit) whether false `bAsSeconds` path is raw ms — **was wrong in plate**

## Seal: ABI (High / Confirmed)

```
005191b6 / 005191dc / 005191f8   C2 08 00   RET 0x8
```

```c
float __thiscall Skill_GetCategoryCooldownRemaining(
    void *pMapOwner,   // ECX — owner of map @ +0x6c
    int   nCategoryId, // stack0
    char  bAsSeconds); // stack1
// ST0 return; RET 0x8
```

## Seal: control flow (High)

1. `map = Skill_GetCategoryCooldownMap(ECX)`  
2. `FUN_00418b80(map, &it, &nCategoryId)` — find by key @ node+0x0C  
3. If `it == map->head (+4)` → `fld g_flZero` → ret  
4. `deadline = *(it+0x10) + *(it+0x14)`  
5. If `g_dwClientTickMs >= deadline` → erase `FUN_004188e0` → `fld 0` → ret  
6. `rem = deadline - g_dwClientTickMs`  
7. If `bAsSeconds != 0`: `fild rem` [+2^32 if signed neg] `fmul g_flMsToSeconds (0.001)` → ret  
8. Else: `fild rem` [+2^32] `fidiv duration` → **fraction** → ret  

## Seal: globals (High)

| VA | Symbol | Role |
|----|--------|------|
| `0x00b041cc` | `g_dwClientTickMs` | now / Insert startTick |
| `0x00a0f518` | `g_flZero` | miss/expired |
| `0x00a0f72c` | `g_flMsToSeconds_Inferred` | **0.001f** seconds path |
| `0x00aaa5dc` | (2^32 float) | uint→x87 assist |

## Falsified prior wording

| Prior | Reality |
|-------|---------|
| Clock Medium/Tentative | **`g_dwClientTickMs` Confirmed** |
| Float scale open | **`0.001f` @ `0x00a0f72c` Confirmed** |
| `bAsSeconds==0` → remaining ms | **`rem/duration` fraction** |
| Decompiler dual identical returns | Broken decomp; asm dual is real |

## Callers (complete static set)

`Skill_LocalCastValidate`, `QuickBar_UpdateSkillSlotCooldownGauge`, `QuickBar_UpdateSlotCooldownOverlay`, `NPC_TryCastSkillFromSet`, `FUN_00892890`, `FUN_00922270` — 9 call sites.

## Remaining gaps

1. Product names for find (`FUN_00418b80`) / erase (`FUN_004188e0`)  
2. `duration==0` `fidiv` edge  
3. Scaffold clean/raw rewrite  
4. Server CD authority (out of unit)

## Verdict

**accept** — prior accept-with-gaps **closed**. See full A/B reviews.
