# Review B (skeptical / adversarial): `aa_00508490` FUN_00508490 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00508490` |
| **VA** | `0x00508490` |
| **Canonical name** | `FUN_00508490` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Skeptical / adversarial (Path B) |
| **Counterpart** | `reviews/A_aa_00508490_FUN_00508490.md` |
| **System** | shared list / heartbeat-owner + inventory |
| **Live tools** | Independent `force_decompile` + `read_memory` + twin remove dual + AttachOwnerObject dual |
| **Verdict** | **leave-FUN** — accept A's CF; reject product / CVOGHBList / VOG_DEBUG_STOP rename |

---

## 1. Claims under attack

| # | Claim | Attack result |
|---|---|---|
| 1 | This is `CVOGHBList_Enqueue` | **Falsified** — intrusive doubly-linked nodes + CS + factory; not pointer-array pending list |
| 2 | Always called with HB as `this` | **Falsified** — AttachOwnerObject: **ECX = listMgr** `*(owner+0xB0)`, stack = HB* |
| 3 | String `"VOG_DEBUG_STOP"` is the product method name | **Falsified** — only on busy-throw path via logger; success path has no string |
| 4 | Inserts at head | **Falsified** — updates **tail** `+0x20`; head `+0x1c` only when empty |
| 5 | Busy policy matches remove | **Falsified** — insert **throws** after EnterCS; remove **returns 0** before EnterCS |
| 6 | Returns HRESULT success codes | **Falsified** — success returns **0**; failures **throw** 0x80070005 / 0x8007000e |
| 7 | Leaves CS before throw | **Falsified as explicit Leave** — throw after Enter; SEH frame owns unwind |
| 8 | Node next/prev swapped vs remove | **Falsified** — both use key+4, next+8, prev+0xC |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Tail-insert math | **High** | List corruption / lost nodes |
| Head-if-empty | **High** | Empty-list walk never finds first |
| Count `+0x24` | **High** | Stale iterate counts |
| Busy `+0x28` throw | **High** | Silent insert during TraversalLock |
| CS + SEH | **High** | Deadlock if port omits SEH Leave |
| Multi-domain list | **High** | Over-narrow “HB-only” name |
| Product method title | **None** | Registry pollution |
| `Named_VOG_DEBUG_STOP` as canonical | **Rejected** | String ≠ symbol |

---

## 3. Cross-check vs twin remove `FUN_005085b0`

| | Insert `0x00508490` | Remove `0x005085b0` |
|---|---|---|
| Busy `+0x28` | throw `E_ACCESSDENIED` (after CS) | return 0 (before CS) |
| CS | yes | yes |
| Node layout | key+4 / next+8 / prev+0xC | same |
| Head/tail/count | +0x1c / +0x20 / +0x24 | same |
| Return | 0 success | 0 miss / 1 hit |
| Free node | factory alloc only | `(*node->vtbl)(1)` |

**Agree High** with A: structural inverse under shared layout. Do **not** unify busy error policy in ports.

---

## 4. Decompiler pitfalls (must survive port)

1. **Collapsed thiscall** — AttachOwner plate historically printed `FUN_00508490(hb)` omitting list ECX.
2. **int\* indexing** — `param_1[10]` is byte at `+0x28`, not a 10th “field” of opaque type.
3. **Prototype `undefined FUN_00508490(void)`** surface — ignore; body is `__thiscall` + 1 stack arg + `ret 4`.
4. **Alias `Named_VOG_DEBUG_STOP_*`** in clean plate — documentation of string only; **leave-FUN**.

---

## 5. What is safe to claim

| Safe | Unsafe |
|---|---|
| Insert-by-key under CS at tail | Product class/method name |
| Success return 0; busy/OOM throw HRESULTs | “Always returns HRESULT” |
| Shared layout with remove / TraversalLock | `CVOGHBList_*` rename |
| Role: attach membership node for key | Exclusive HB-only semantics |
| Leave `FUN_*` | Promote `List_InsertByKey` to registry without string/RTTI |

---

## 6. CF challenge of Review A

- Dual force/non-force identity: **agree**
- Offsets head/tail/count/busy/key/next/prev: **agree High**
- Tail insert + head-if-empty: **agree High**
- leave-FUN + reject VOG_DEBUG_STOP rename: **agree**
- Reject CVOGHBList identity: **agree**

**Verdict:** **leave-FUN**
