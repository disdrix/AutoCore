# Review A (reconstruction fidelity): `aa_007a6310` FUN_007a6310 (leave-FUN / locale pak load)

| Field | Value |
|---|---|
| **Stable ID** | `aa_007a6310` |
| **VA** | `0x007a6310` |
| **Canonical name** | `FUN_007a6310` (**retained**; strings seal pak naming) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Reconstruction fidelity |
| **Counterpart** | `reviews/B_aa_007a6310_FUN_007a6310.md` |
| **System** | locale / language packs (FormatFailure string path modes 1–3) |
| **Verdict** | **leave-FUN** — CF + filename table sealed; product API name open |

**Evidence:** Ghidra `decompile_function` @ `0x007a6310` (literal filenames).

---

## 1. Purpose

**Load a localized string blob** from a language PAK into the current ring slot:

| `param_2` (mode) | Filename pattern |
|---:|---|
| 1 | `languageFR%d.pak` |
| 2 | `languageDE%d.pak` |
| 3 | `languageUK%d.pak` |
| other | **return** (no I/O) |

`param_3` is sprintf pack index. `fopen(..., "rb")`; if file open and entry index (`in_EAX`) ≠ −1:

1. Resolve directory entry at `this + (param_3 + 0xc + param_2*2)*4` + `index * 0x14`.
2. `fseek` to base (`this+0x94 + (param_3 + param_2*2)*4`) + entry offset `entry+4`.
3. `FUN_007a6260` ensure slot for size `entry+8`.
4. `fread` into current ring slot; `fclose`.

---

## 2. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| FR/DE/UK pak name table | **Confirmed** | string literals |
| Mode ∉ {1,2,3} early out | **Confirmed** | |
| Entry stride `0x14` | **Confirmed** | |
| fread size from entry `+8` | **Confirmed** | |
| Depends on `FUN_007a6260` | **Confirmed** | |
| Index `in_EAX == -1` skips I/O | **Confirmed** | |
| Product class method name | **Open** | leave FUN_* |
| Full PAK directory layout | **Tentative** | offsets sealed in formula only |

---

## 3. Artifacts

| Artifact | Path |
|---|---|
| Raw / annotated / clean / record | `aa_007a6310_*` |
| Parent | `FUN_007a6de0` modes 1–3 (explicit mode ≠ −1) |
| Chain | FormatFailure → locale ensure → 007a6de0 → this |

---

## 4. Gaps

1. Who populates PAK directory tables at `this+…` (load-at-startup residual).
2. Entry fields beyond offset `+4` / size `+8` / key at `+0`.
3. Whether EN is mode 0 (no pak) only.

**Verdict:** **leave-FUN** (strings do not alone promote without RTTI/exclusive product plate).
