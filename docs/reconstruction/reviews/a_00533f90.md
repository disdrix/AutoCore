# Dual residual: `0x00533f90` CVOGReaction_CompleteObjective

| Field | Value |
|-------|-------|
| Stable ID | `aa_00533f90` |
| Address | `0x00533f90` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior dual | A/B accept-with-gaps — open: **flag +0x7e polarity**, FUN_* insert/retire/min-pass/medal, skill/attrib absolute vs swap, GiveMission/AddActiveObjective arity |
| Live re-decompile | Ghidra 2026-07-29 — body CF ≡ frozen raw |

## Residual questions (from dual A/B + matrix)

1. Entity flag `+0x7e` polarity (missing-obj early-out vs inventory / SearchAuto)
2. `FUN_0053c360` / `FUN_00538b20` / `FUN_00538bd0` / `FUN_00538e00` / `FUN_00538e50` roles + which hash `this`
3. `FUN_0059d880` min-pass semantics + ECX object
4. Skill vs attribute pool naming / absolute vs delta at call sites
5. `AddActiveObjective` / `GiveMission` arity
6. Medal / inventory helper FUN surface

---

## Seal: flag `+0x7e` = **local player / local-authority**

Expression (asm `00533fe3`–`00533ff0`):

```
entityData = *(*(character+4)+4) + character + 0xa8
flag = *(char*)(entityData + 0x7e)
```

| Site in this unit | Polarity | Effect |
|-------------------|----------|--------|
| Missing active objective | **flag ≠ 0 → return 0** | Local cannot synthesize GiveMission from bare Complete |
| Missing active objective | **flag == 0 → GiveMission chain** | Non-local may grant parent mission then fall through |
| Final inventory rewards (4 COID slots) | **flag ≠ 0 → run** | Local-only inventory grant |
| Post-final `SearchAutoMissions` | **flag ≠ 0 → run** | Local-only auto-mission scan |
| XP / credits / skill / attrib / medals | **not gated by +0x7e** | Apply whenever final rewards block runs |

Cross-unit corroboration (same chain / same offset):

| Unit | Behavior when `+0x7e ≠ 0` | Confidence |
|------|--------------------------|------------|
| `CVOGReaction_AddExperience` | Level-up/down loops | **High** (dual A) |
| `CVOGReaction_TransferMap` | Requires flag | **High** |
| `CVOGCharacter_SetAreaExploredBit` | Local unlock path | **High** |

**Conclusion:** English role = **IsLocalPlayer** (local entity authority). Not inverted — missing-obj reject-when-local is intentional (local must already hold the objective; non-local mirror may GiveMission). Inventory/SearchAuto are local-only side effects; XP/credits are **not** “local only” despite plate prose.

**Plate correction:** Raw plate “FINAL: XP/credits/medals/inventory rewards (local player only)” is **overstated** — inventory yes; XP/credits/medals no.

Confidence: **High** (asm + multi-unit pattern).

---

## Seal: skill / attribute pools (not swapped; absolute set)

| Helper | VA | Pool store | LogicUI on increase | CompleteObjective arg |
|--------|-----|------------|---------------------|------------------------|
| `CVOGCharacter_AddSkillPoints` | `0x005312c0` | `char+0x6ce` (short) | type `0x21` | `*(short*)(char+0x6ce) + *(short*)(obj+0x130)` |
| `CVOGCharacter_AddAttributePoints` | `0x00531250` | `char+0x6cc` (short) = `this[0x1b3]` | type `0x23` | `(ushort)obj+0x134 + (short)this[0x1b3]` |

Asm advance path (`005348c6`–`005348d7`):

```
MOV DX, [ESI+0x6ce]
ADD DX, [EDI+0x130]     ; EDI = objective
MOV ECX, ESI
PUSH EDX
CALL AddSkillPoints     ; sets pool absolute
```

Both callees **assign** the short (not `+=`). Caller supplies **current + grant** → absolute.

| Objective field | Type | Role |
|-----------------|------|------|
| `obj+0x130` | short | Skill-point grant |
| `obj+0x134` | byte | Attribute-point grant |

**Falsifies** “names may swap pools”. Clean plate comment lines that labeled `+0x6ce` as attrib are **doc noise only** — body calls match names.

Confidence: **High**.

---

## Seal: `FUN_0059d880` = min required pass count @ `objective+0x164`

| Evidence | Detail |
|----------|--------|
| Body | `__fastcall` return `*(uint32*)(ecx + 0x164)` only |
| CompleteObjective call sites | `00534120`/`0053412b`: **`MOV ECX, EDI`** then `CALL` (EDI = objective) |
| EvaluatePendingObjectives | `MOV ECX, [ESI]` (objective from pending node) then same |
| Gate (asm) | `TEST EAX; JLE failCheck` → if min≤0 need allOk/force; else `CMP nPassed, EAX; JGE ok` |

Gate equivalent:

```
min = Objective_GetMinRequiredPassCount(obj)  // +0x164
if (min <= 0) {
  if (!allReqsOk && !force) return 0;
} else {
  if (nActioned < min && !allReqsOk && !force) return 0;
}
```

Proposed name: `CVOGObjective_GetMinRequiredPassCount` (product PDB unknown).

Confidence: **High** (asm ECX + trivial body).

---

## Seal: hash insert / remove / destroy (this = which hash)

### `FUN_0053c360` — CNDHash insert (mission-style node)

| Call site | ECX (this) | Stack | Role |
|-----------|------------|-------|------|
| Final complete `005346a3`→`005346dc` | **`char+0x538`** completed | key=`*missionDef`, def, flags=0 | Insert completed if miss |

Body: alloc node (`FUN_0053ada0`), vftable `PTR_LAB_009cf024`, link bucket + list, optional soft-dup check via `param_4`.

### Double `FUN_00538b20` — **not** same-hash twice

| Call | ECX | Key | Role |
|------|-----|-----|------|
| `005347ad`→`005347b6` | **`char+0x540`** active missions | mission id | Remove active mission |
| `005347bd`→`005347c6` | **`char+0x544`** paired active | mission id | Remove sibling active hash |

Matches GiveMission residual (`reviews/a_005327c0.md`): insert pair `+0x540` / `+0x544`; CompleteObjective retires both.

### `FUN_00538bd0` — objective-hash remove (vftable `009cf02c`)

| Call | ECX | Key |
|------|-----|-----|
| Final `005347d2`→`005347db` | **`char+0x548`** | completed objective id (`local_2d4+0x10`) |
| Advance `005348aa`→`005348b3` | **`char+0x548`** | retiring objective id |

### `FUN_00538e00` / `FUN_00538e50` — hash destroy-by-key

| FUN | CompleteObjective this | Key | Role |
|-----|------------------------|-----|------|
| `00538e00` @ `005347e2` | **`char+0x530`** | mission id | Destroy reward-state / bind blob (GiveMission inserts via `FUN_0053c660`) |
| `00538e50` after req walk | pending/objective destroy helper | objective id | Retire pending requirement node |

### Manual `+0x53c` insert

After completed insert: walk/insert into **`char+0x53c`** instance-completed (same node shape, vftable `009cf024`) — raw preserved; not a second `FUN_0053c360`.

Confidence: **High** for roles + which hash; product English for `+0x544` / `+0x530` still open (paired with GiveMission residual).

---

## Seal: GiveMission / AddActiveObjective arity

| Callee | Convention | Args (asm) | Confidence |
|--------|------------|------------|------------|
| `CVOGReaction_GiveMission` `0x005327c0` | `__thiscall` | `ECX=character`, stack missionId | **High** |
| `CVOGMission_AddActiveObjective` `0x00531b00` | `__thiscall` | `ECX=character` (`MOV ECX,ESI`), stack `objectiveDef*` | **High** |

AddActiveObjective body: insert objective hash via `FUN_0053c460`, optional pending node `FUN_0053c760`, req vtbl+0x28 init, dirty `char+0x634 |= 0x10`.

---

## Seal: medal / reward FUN roles (CF)

| FUN | Role | Evidence | Conf |
|-----|------|----------|------|
| `FUN_00530550` | Award medal by id: lookup `FUN_0051a0e0`, push notify (`FailMissionNotify` vector), dirty `\|0x20`, optional chain `FUN_005304a0` | body | **High** CF; product name open |
| `FUN_00530610` | Map at `char+0x588`: insert or `+=` counter | body | **High** CF |
| `FUN_00531960` | Add short to `char+0x580`; LogicUI **0x22** if delta>0; dirty `\|1` | body | **High** CF; English (rep/talent?) open |
| `FUN_004cf100` | Type-0 mission reward table fill → `FUN_0052c700(*def, table)` | thin body | **High** |
| `FUN_004d08c0` | Non-type-0 reward fill (roll-driven COID slots) | body | **High** CF; call arity vs thiscall residual mild |
| `FUN_004d4790` | Queue world-object delete (non-matching reward COID filter) | body + string | **High** |
| `FUN_0052d8b0` | Clear mission reward staging tree at `char+0x50c` for mission id | body | **High** CF |
| `FUN_005875c0` | Resolve/register reward object id | usage | Probable |
| `Client_SendInventoryAddItem` | Grant filtered reward | named | **High** |

---

## Re-decompile check (2026-07-29)

Live `decompile_function(0x00533f90)` matches frozen raw CF:

- Active hash walk `this[0x152]` / `+0x548`
- Missing → flag gate → GiveMission
- Pending req vtbl +0x8 / +0x2c + min-pass + force
- Advance `seq < count-1` → dirty-only (no XP/credits)
- Final rewards gated on completed-hash miss
- Toast primary-entity gate `+0xe8a0` (separate from +0x7e)
- Dirty `this[0x18d] |= 0x10` (and `|=1` on credits)

No CF change.

---

## Callers (xref, force flag)

| Caller | Force | Notes |
|--------|-------|-------|
| `Client_RecvCompleteDynamicObjective` `0x0080ff00` | **1** | S2C `0x2070` |
| `Client_MissionDialogHandleButton` `0x008ae7c0` | local path | Do not also send `0x2070` |
| `CVOGCharacter_CompleteMissionObjectives` `0x00536080` | reason pass-through | Bulk |
| `CVOGCharacter_EvaluatePendingObjectives` `0x00534920` | deferred complete | |
| `FUN_006508d0`, `0x0057e274`, `0x00954648` | other | open product names |

---

## Residual after this pass

| Item | Status |
|------|--------|
| Flag `+0x7e` polarity / IsLocalPlayer | **Sealed High** |
| XP/credits not local-gated | **Sealed High** (plate overstatement noted) |
| Skill `+0x6ce` / attrib `+0x6cc` + absolute caller math | **Sealed High** |
| `FUN_0059d880` → `obj+0x164` min pass | **Sealed High** |
| Hash insert/remove/destroy this targets (+0x538/540/544/548/530/53c) | **Sealed High** |
| GiveMission / AddActiveObjective arity | **Sealed High** |
| Medal/inventory FUN **roles** | **Sealed High** CF |
| Product English for `+0x544`, `+0x530`, medal/rep helpers | Open |
| Original PDB symbols for FUN_* | Open |
| Runtime / bit-exact / image diff | Still open (matrix policy) |
| Req vtable bodies (+0x8 / +0x2c) | Open (sibling units) |
| `FUN_004d08c0` full arg map vs decompiler | Mild open |

---

## Surviving contract (updated)

```
CompleteObjective(character, objectiveId, rewardCoidLo, rewardCoidHi, force):
  resolve active objective @ +0x548
  if missing:
    if IsLocal(+0x7e): return 0
    else: def lookup → GiveMission(parent) if not completed
  evaluate pending reqs (force bypasses fail); minPass = obj+0x164
  if seq < count-1:
    AddActiveObjective(next); remove old @ +0x548; unlock;
    set skill(+0x6ce) and attrib(+0x6cc) absolute; dirty|0x10; return 1
  if mission not in completed +0x538:
    skill/attrib; XP non-kill; credits 64-bit @ +0x720; medals/counters
    if IsLocal: inventory×4 (COID filter) + reward table fill
  toast if primary entity; insert completed +0x538 / +0x53c
  remove active mission +0x540 and paired +0x544; remove obj +0x548;
  destroy reward bind +0x530; EvaluatePending if final-was-new;
  if IsLocal: SearchAutoMissions
  dirty|0x10; return 1
```

---

## Artifacts touched by residual owner

- This scratch: `docs/reconstruction/reviews/a_00533f90.md`
- **Promoted 2026-07-29 (this pass):** dual A/B rewritten + function record strengthened from sealed residual
  - `reviews/A_aa_00533f90_CVOGReaction_CompleteObjective.md`
  - `reviews/B_aa_00533f90_CVOGReaction_CompleteObjective.md`
  - `functions/aa_00533f90_CVOGReaction_CompleteObjective.md`
- Sibling residual (read-only): `reviews/a_005327c0.md` (+0x540/+0x544 pair)
- Re-verify 2026-07-29: live decompile helpers (`0059d880`, skill/attrib, medal/hash remove/destroy) + bias float `00AAA6D0` = `8e06003f` ≈ 0.5001f; callers via `get_function_callers`
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / matrix (parent-owned)
- **Not** clean wholesale rewrite (plate still notes residual; CF ≡ raw)
