# Review A (reconstruction fidelity): `aa_0089ff80` FUN_0089ff80

| Field | Value |
|---|---|
| **Stable ID** | `aa_0089ff80` |
| **VA** | `0x0089ff80` |
| **Body** | `0x0089ff80`–`0x008a0002` (**~130** bytes; `ret`) |
| **Canonical name** | `FUN_0089ff80` (**leave-FUN**) |
| **Human alias** | `Named_CalleeOf_Client_QuickBarActivateSkillSlot_0089ff80` — post-cast quick-bar visual clear |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Independent reconstruction review (Path A — fidelity) |
| **Counterpart** | `reviews/B_aa_0089ff80_FUN_0089ff80.md` |
| **System** | skills-abilities / client UI |
| **Live tools** | Ghidra dual decompile A/B, `read_memory`, callers |
| **Verdict** | **accept-with-gaps** / **leave-FUN** |

---

## 1. Purpose

**Conditional quick-bar / stance UI visual clear** after skill activate paths.

Host in **ESI**; mode selector in **AL** (`0` or `1`); void return.

1. Gate: `*(char*)(ESI+0x522) != 0` else return.
2. **Mode 0** (`AL==0`): object `*(ESI+0x66c)`
   - null → return
   - `obj.vtbl+0xd0()` → char; if 0 return
   - store `*(obj+0x4fe) = 0`
   - tail-jump `obj.vtbl+0x448()` (decompiler: indirect jump as call)
3. **Mode 1** (`AL==1`): object `*(ESI+0x670)` — same vtbl/`+0x4fe`/`+0x448` pattern.
4. Other AL → return.

Callers: `Client_QuickBarActivateSkillSlot` `0x00921b50` (after sector send + `DAT_00d1b8f0` vtbl+0x3d8 gate), `FUN_00922270` (primary fire path), `Input_TryFireSecondaryWeapons` `0x0091a550`.

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Pass A/B | `batch_decompile` / `force_decompile` — **identical** |
| Machine | `read_memory` length 140 — `cmp [esi+0x522]`; `movsx eax,al` / sub 0 / sub 1 switch; offsets `0x66c`/`0x670`/`0x4fe`; `ff a2 48 04 00 00` tail jmp vtbl+0x448 |
| Parent dual context | QuickBarActivateSkillSlot plate |
| Raw / clean | `raw/aa_0089ff80_*`, `reconstructed-exact/FUN_0089ff80.cpp` |

---

## 3. Authoritative body (dual A≡B)

```c
void FUN_0089ff80(void)
{
  char in_AL;
  char cVar1;
  int unaff_ESI;

  if (*(char *)(unaff_ESI + 0x522) == 0) return;
  if (in_AL == 0) {
    if (*(int *)(unaff_ESI + 0x66c) != 0) {
      cVar1 = (**(code **)(**(int **)(unaff_ESI + 0x66c) + 0xd0))();
      if (cVar1 != 0) {
        *(undefined1 *)(*(int *)(unaff_ESI + 0x66c) + 0x4fe) = 0;
        (**(code **)(**(int **)(unaff_ESI + 0x66c) + 0x448))(); // tail
        return;
      }
    }
  }
  else if (in_AL == 1 && *(int *)(unaff_ESI + 0x670) != 0) {
    cVar1 = (**(code **)(**(int **)(unaff_ESI + 0x670) + 0xd0))();
    if (cVar1 != 0) {
      *(undefined1 *)(*(int *)(unaff_ESI + 0x670) + 0x4fe) = 0;
      (**(code **)(**(int **)(unaff_ESI + 0x670) + 0x448))(); // tail
      return;
    }
  }
}
```

---

## 4. Confidence

| Claim | Level |
|---|---|
| CF dual A≡B | **Confirmed** |
| AL selects +0x66c vs +0x670 | **Confirmed** |
| Flag clear +0x4fe before visual 0x448 | **Confirmed** |
| Gate +0x522 | **Confirmed** |
| English: "clear armed quickbar button visual" | **Probable** |
| Product name | **None** — leave-FUN |
| Exact meaning of vtbl+0xd0 predicate | **Tentative** |

**Verdict:** **leave-FUN** / **accept-with-gaps.**
