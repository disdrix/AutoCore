# Review A (reconstruction fidelity): `aa_004e9530` FUN_004e9530

| Field | Value |
|---|---|
| **Stable ID** | `aa_004e9530` |
| **VA** | `0x004e9530` |
| **Canonical name** | `FUN_004e9530` |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Independent reconstruction review |
| **Counterpart** | `reviews/B_aa_004e9530_FUN_004e9530.md` |
| **System** | math — copy float3 (+ write 4th float from stack local) |
| **Verdict** | **accept** — SSE float3 copy **sealed**; **not** a normalizer |

---

## 1. Purpose

**Copy three floats** from `src` (`param_2`) to `dst` (`param_1`), and also write **`dst[3]`** from a **stack local** that is **never initialized** in this function (compiler residue / ABI leftover).

**Corrects** the parent dual note on `FUN_004b7e50` that labeled this “vector normalize / basis” — live bytes show **loads/stores only**, no sqrt/div/rsqrt.

```
dst.x = src.x
dst.y = src.y
dst.z = src.z
dst.w = <uninitialized stack float>   // written; value not defined by this unit
```

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Raw | `docs/reconstruction/raw/aa_004e9530_FUN_004e9530.md` |
| Annotated | `docs/reconstruction/raw/aa_004e9530_FUN_004e9530.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/FUN_004e9530.cpp` |
| Function record | `docs/reconstruction/functions/aa_004e9530_FUN_004e9530.md` |
| Live decompile | Ghidra `batch_decompile` `0x004e9530` — matches raw (junk `local_14`) |
| Live bytes | Ghidra `read_memory` `0x004e9530` length 64 — SSE path |
| Callers | `FUN_004b7e50` position path; other math sites |

---

## 3. Sealed residuals

### 3.1 ABI — **SEALED**

| Fact | Evidence |
|---|---|
| Convention | **`cdecl`**: `dst` then `src` on stack (`[ebp+8]`, `[ebp+0xC]`) |
| Body | frame + 16-byte align + 0x10 locals |

```c
void FUN_004e9530(float *dst /*out*/, const float *src /*xyz*/);
```

### 3.2 Entry machine bytes — **SEALED**

```
55 8B EC 83 E4 F0 83 EC 10
8B 45 0C                   ; src
F3 0F 10 00                ; xmm0 = src[0]
F3 0F 10 48 04             ; xmm1 = src[1]
F3 0F 10 50 08             ; xmm2 = src[2]
8B 45 08                   ; dst
F3 0F 11 00                ; dst[0] = xmm0
F3 0F 10 44 24 0C          ; xmm0 = [esp+0xC]  // UNINIT local
F3 0F 11 48 04             ; dst[1] = xmm1
F3 0F 11 50 08             ; dst[2] = xmm2
F3 0F 11 40 0C             ; dst[3] = xmm0 (junk)
8B E5 5D C3
```

Hex:  
`558bec83e4f083ec108b450cf30f1000f30f104804f30f1050088b4508f30f1100f30f1044240cf30f114804f30f115008f30f11400c8be55dc3`

### 3.3 Side effects — **SEALED**

- Writes **16 bytes** at `dst`.
- **xyz** are faithful copies of `src`.
- **w** is **not** sourced from `src[3]`; not computed; **undefined**.

---

## 4. Control flow: clean ≡ raw ≡ live

| Stage | Match |
|---|---|
| float3 copy | Yes |
| fourth write from local | Yes (raw `local_14`) |
| no normalize math | Yes (bytes) |

---

## 5. Gaps / open

1. Why emit `dst.w` at all — likely MSVC float4 ABI artifact; callers may ignore w.
2. Whether any caller depends on residual stack w (unlikely / UB).
3. Runtime verification not run.

**Verdict:** **accept**
