# Dual residual: `0x005711c0` InventoryGrid_CountItemsByCbid

| Field | Value |
|-------|-------|
| Stable ID | `aa_005711c0` |
| Address | `0x005711c0` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior dual | A/B accept-with-gaps — open: **flag bit19**, enumerator FUN names |

## Residual questions (from dual A/B)

1. Exact semantics of `object[0x5f] >> 0x13 & 1` (`item+0x17c` bit 19 / mask `0x80000`)
2. Identity of `FUN_004294f0` / `FUN_004022a0`

## Seal: flag bit 19 = **Broken**

| Evidence | Location | Confidence |
|----------|----------|------------|
| Loot combat-log prefix `"Broken: "` when bit set, else empty | `Client_RecvInventoryAddItem` / raw `aa_008151a0` | **High** |
| UI tooltip/name prefix `"Broken: "` when bit set | `Client_UI_InventoryItemClickDispatch` `0x008635b0` | **High** |
| Overlay texture `i_g_2d_overlay_broken.dds` when bit set | `UI_overlay_broken` | **High** |
| Setter `FUN_00513de0` xor-writes mask `0x80000` (bit 19) | `0x00513de0` | **High** |
| Stack merge requires **both** items bit19 clear | `Inventory_SerializeAddItemPacket` `0x004fadb0` | **High** (behavior) |
| Equip path returns code **9** if bit set | `FUN_004fabc0` | **High** (gate) |
| Collect_Eval / SlotAction / Deliver call Count with **param_3 = 0** | `0x00611940`, `0x006124b0`, `0x00611290` (Deliver OnInventoryEvent), `0x00610f20` (Deliver CargoReady) | **High** |
| At least one inventory audit path passes **param_3 = 1** | `FUN_00502590` | **High** |

**Conclusion:** `param_3` is **includeBroken** (not a generic “mission flag”). When `0`, broken stacks are omitted from the sum; when non-zero, broken stacks count. Collect/Deliver progress deliberately ignores broken cargo of the target CBID.

English name for the bit: **`ItemFlag_Broken`** at `CVOGClonedObject`/`item+0x17c` bit 19. Original symbol unknown.

## Seal: enumerator helpers

| Addr | String / behavior | Proposed name | Confidence |
|------|-------------------|---------------|------------|
| `0x004294f0` | `"List Error!  TraversalLock Call Stack:\n%s\n"` + `VOG_DEBUG_STOP`; `EnterCriticalSection(this+4)`; set `*(this+0x28)=1` if not already locked | `List_TraversalLock` | **High** (role + string) |
| `0x004022a0` | Walk list head `this+0x1c`; node `+4` payload, `+8` next; under CS `this+4`; returns `0` more / `1` done / `0x80070005` no lock | `List_IterateNext` (TraversalLock list next) | **High** (role); original unknown |

### Grid unlock pairing

CountItems leaves CS at **`grid+0x30`** and clears **`grid+0x54`**. TraversalLock uses CS **`this+4`** and flag **`this+0x28`**. Delta **0x2c** both → **Probable** embedded list subobject at **`InventoryGrid+0x2c`** (so list CS = grid+0x30, list lock flag = grid+0x54). Cell array remains at grid **`+0x28`** (separate field). Full layout seal still open in `OBJECT_LAYOUTS` (not required for CountItems CF).

## Re-decompile check (Ghidra 2026-07-29 residual)

Body matches frozen raw: CBID match `*(obj[0x2a]+0x34)`, bit19 filter, vtbl `+0x25c` qty sum, unlock `+0x54`/`+0x30`. No CF change.

## Callers (param_3)

| Caller | param_3 | Effect |
|--------|---------|--------|
| Collect_Eval `0x00611940` | `0` | exclude broken |
| Deliver OnInventoryEvent `0x00611290` (req type **3** / event type **4**; **not** Collect) | `0` | exclude broken |
| Collect_SlotAction `0x006124b0` | `0` | exclude broken |
| Collect_Action inv arm `0x00611f20` | `0` | exclude broken |
| Deliver CargoReady `0x00610f20` | `0` | exclude broken |
| `FUN_00502590` | `1` | **include** broken |

**UF-014 seal:** Prior dual A labeled `0x00611290` as “Collect type-4”. Correct role (chain + Deliver duals + Deliver ctor type **3**): **Deliver OnInventoryEvent** — inventory event when event type **4**. Canonical name remains `CVOGObjectiveRequirement_Deliver_OnInventoryEvent_Inferred` (do not invent). Collect type-2 Eval is `0x00611940` only.

## Residual after this pass

| Item | Status |
|------|--------|
| Flag bit19 semantics | **Sealed High** = Broken |
| `param_3` meaning | **Sealed High** = includeBroken |
| `FUN_004294f0` / `FUN_004022a0` role names | **Sealed High** as TraversalLock / IterateNext |
| Caller `0x00611290` label | **Sealed High** = Deliver OnInventoryEvent (req 3 / event 4); not Collect |
| Original PDB symbols | Open (unknown) |
| Runtime / bit-exact / image diff | Still open (matrix policy) |
| Grid+0x2c list embed | Probable only |

## Artifacts touched by residual owner

- This scratch: `reviews/a_005711c0.md`
- Duals: `A_aa_005711c0_*`, `B_aa_005711c0_*`
- Function record, clean `InventoryGrid_CountItemsByCbid.cpp`, experiment bridge, VERIFICATION_MATRIX open-issues cell
- **Not** WORK_QUEUE / RESUME
