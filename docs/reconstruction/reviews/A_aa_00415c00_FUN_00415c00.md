# Review A (reconstruction fidelity): `aa_00415c00` FUN_00415c00 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00415c00` |
| **VA** | `0x00415c00` |
| **Body** | `0x00415c00`–`0x00415c6d` (**110** bytes incl. `ret 4`; pad `CC`) |
| **Canonical name** | `FUN_00415c00` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Independent reconstruction review (Path A — fidelity) |
| **Counterpart** | `reviews/B_aa_00415c00_FUN_00415c00.md` |
| **System** | shared list (inventory + other) |
| **Live tools** | Ghidra `decompile_function`, `force_decompile`, `analyze_function_complete`, `read_memory`, `get_function_callers` (no `disassemble_bytes`) |
| **Verdict** | **leave-FUN** — CF sealed **High**; product name withheld |

---

## 1. Purpose (behavioral)

**Membership test** on the same critical-section list family as `List_TraversalLock` / `List_IterateNext`:

1. Cursor = 0.
2. `List_TraversalLock` (`FUN_004294f0` / `0x004294f0`) — if not already locked: EnterCS(`this+4`), set **`*(this+0x28)=1`**. If already locked, logs `"List Error! TraversalLock Call Stack"` path (debug) and returns without re-enter.
3. Loop `List_IterateNext` (`FUN_004022a0` / `0x004022a0`) with `(cursor*, outKey*)`:
   - IterateNext returns **0** → more nodes; compare `outKey == param_2` (sought key).
   - On key match → result **1**, exit loop.
   - IterateNext returns **non-zero** (end / fail) → result **0**.
4. Exit epilogue: if **`*(this+0x28) != 0`**, clear it and **`LeaveCriticalSection(this+4)`**.
5. Return **byte** 0/1 (not found / found).

Does **not** mutate list membership. Companion of remove `FUN_005085b0` and insert `FUN_00508490` on the same layout.

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Fresh decompile | Ghidra `decompile_function` @ `0x00415c00` |
| Force re-decompile | Ghidra `force_decompile` @ `0x00415c00` (**identical**) |
| Complete analysis | `analyze_function_complete` — 3 xrefs |
| Machine | `read_memory` @ `0x00415c00` length 112 |
| Function range | `get_function_by_address` entry `00415c00`, body `00415c00`–`00415c6d` |
| TraversalLock | decompile `0x004294f0` — string `List Error! TraversalLock Call Stack` |
| IterateNext | decompile `0x004022a0` — head/next walk under TryEnter when locked |
| Registry siblings | `List_TraversalLock` `aa_004294f0`, `List_IterateNext` `aa_004022a0` |
| Raw / clean | `raw/aa_00415c00_FUN_00415c00.md`, `reconstructed-exact/FUN_00415c00.cpp` |
| Function record | `functions/aa_00415c00_FUN_00415c00.md` |

---

## 3. Authoritative body (Ghidra dual A/B 2026-07-29)

**Pass A** ≡ **Pass B**:

```c
undefined1 __thiscall FUN_00415c00(int param_1, int param_2)
{
  int iVar1;
  undefined1 uVar2;
  int local_8;
  undefined4 local_4;

  local_4 = 0;
  FUN_004294f0();
  iVar1 = FUN_004022a0(&local_4, &local_8);
  do {
    uVar2 = 0;
    if (iVar1 != 0) {
LAB_00415c50:
      if (*(char *)(param_1 + 0x28) != '\0') {
        *(undefined1 *)(param_1 + 0x28) = 0;
        LeaveCriticalSection((LPCRITICAL_SECTION)(param_1 + 4));
      }
      return uVar2;
    }
    if (local_8 == param_2) {
      uVar2 = 1;
      goto LAB_00415c50;
    }
    iVar1 = FUN_004022a0(&local_4, &local_8);
  } while (true);
}
```

Notes on decompiler surface:

- `FUN_004294f0()` / `FUN_004022a0(...)` omit explicit list `this` — **ECX = param_1** throughout (thiscall chain).
- `FUN_004022a0` true signature (from its own decompile): `__thiscall (list, int *cursor, undefined4 *outKey) → uint` (0=have item, non-0=done/error).

---

## 4. Machine seal (`read_memory`)

Hex start: `83ec0853568bf132db…` ends `…c20400` + `cccc`.

| Claim | Evidence | Conf |
|---|---|---|
| `__thiscall`; **1 stack arg** key; **`ret 4`** | `mov esi,ecx`; `c2 04 00` | **High** |
| Stack frame 8 for cursor + outKey | `sub esp,8` | **High** |
| Call TraversalLock then IterateNext | relative calls to `0x004294f0` / `0x004022a0` | **High** |
| Result in **AL** (byte) | `mov al,bl`; bl set 0 or 1 | **High** |
| Unlock epilogue: clear `+0x28` + LeaveCS(`this+4`) | matches decompile | **High** |
| Dual A/B decompile identity | force ≡ non-force | **High** |

---

## 5. Layout reuse (list manager)

Same sealed subset as remove/insert:

| Offset | Role |
|---|---|
| `+0x04` | `CRITICAL_SECTION` |
| `+0x1c` | head (via IterateNext) |
| `+0x28` | TraversalLock flag (set by lock helper; cleared here on exit) |

Node key compared = IterateNext `outKey` ← `*(node+4)`.

---

## 6. Call graph

**Callees:** `List_TraversalLock` (`0x004294f0`), `List_IterateNext` (`0x004022a0`), `LeaveCriticalSection`.

**Callers (Ghidra):**

| Caller | Sites | Context |
|---|---|---|
| `FUN_00571b60` `0x00571b60` | thin wrapper: null key → 0 else Contains | membership API surface |
| `FUN_00571b80` `0x00571b80` | gate before remove/`FUN_005085b0` | inventory grid extract path; also uses `Item_IsStackEligible` `0x00513e70` |
| `FUN_0083aa50` `0x0083aa50` | client UI/list presence check | multi-slot walk |

---

## 7. Control flow: clean ≡ raw ≡ dual

| Stage | Match |
|---|---|
| cursor=0; TraversalLock | **Yes** |
| Iterate until end or key hit | **Yes** |
| bool 0/1 | **Yes** |
| Always attempt unlock if `+0x28` set | **Yes** (even on not-found) |
| No insert/remove | **Yes** |

---

## 8. Naming gate

| Candidate | Gate |
|---|---|
| `List_Contains` / `List_HasKey` | Role **High**; no string on this VA |
| `Named_CalleeOf_…_RecvInventoryEquip_00415c00` | Scaffold; not exclusive |
| **Retain `FUN_00415c00`** | **Yes** — helpers already named; this is thin compose |

Do **not** invent a third registry name until string/RTTI or exclusive domain proof.

---

## 9. Gaps

| # | Gap | Severity |
|---|---|---|
| G1 | Product method title | Open |
| G2 | IterateNext `0x80070005` path when TryEnter fails mid-walk — Contains treats any non-0 as “stop / not found” | Probable (returns 0) |
| G3 | Exact list instance at each caller (grid vs owner+0xB0) | Per-caller |
| G4 | Runtime / bit-exact | Deferred |

**Verdict:** **leave-FUN** — Contains CF **High**; name withheld.
