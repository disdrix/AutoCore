# Review B (skeptical / adversarial): `aa_00569cb0` FUN_00569cb0

| Field | Value |
|---|---|
| **Stable ID** | `aa_00569cb0` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_00569cb0_FUN_00569cb0.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | Stack arg / `RET 4` | Epilogue **`C3`**; no stack loads of params | **Falsified** — **this-only** |
| 2 | Always virtual-calls | `test [esi+30]; jz` over call | **Falsified** — gated on **`+0x30`** |
| 3 | Writes `+0x78` only when handle live | Store is **after** branch merge | **Falsified** — **always −1** |
| 4 | Float is 0.2 from physics table only by coincidence | Immediate `68 CD CC 4C 3E` in body | **Confirmed encoding**; **product role** open |
| 5 | Direct named callee | Only `call [edx+18h]` | **Confirmed** — **indirect only** |
| 6 | Nulls handle at `+0x30` | No store to `+0x30` | **Falsified** — handle field **untouched** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Offsets +0x24 / +0x30 / +0x78 | **High** | Wrong weapon layout ports |
| vtbl slot +0x18 | **High** | Wrong method |
| Args (handle, 0.2f, 0) | **High** | Wrong fade / flags |
| 0.2f meaning (seconds vs scale) | **Medium** | Tuning mismatch only |
| Owner type = weapon | **Medium** | Caller cluster suggests weapon; not plate-sealed |

---

## 3. Cross-check against raw + bytes

```
raw:
  if (*(this+0x30)) (*( **(this+0x24) + 0x18 ))(*(this+0x30), 0x3e4ccccd, 0);
  *(this+0x78) = 0xffffffff;

bytes @ 00569cb0:
  56 8BF1 8B4630 85C0 7410 8B4E24 8B11 6A00 68CDCC4C3E 50 FF5218
  C74678FFFFFFFF 5E C3

≡ identical CF. 0x3e4ccccd == 0.2f.
```

---

## 4. Surviving contract for AutoCore

```csharp
// Client-only: release attached FX/sound handle then invalidate slot.
void Weapon_ReleaseAttachedHandle(Weapon w)
{
    if (w.Handle /*+0x30*/ != null)
        w.Service /*+0x24*/.Vtbl_0x18(w.Handle, fadeOrScale: 0.2f, flags: 0);
    w.SlotIndex /*+0x78*/ = -1;
    // Note: does not clear Handle itself.
}
```

- Pair with fire-start paths that arm `+0x30` / `+0x78` (sibling units).
- Do not treat as heat math — heat is `Weapon_CanFireHeatCheck` (`0x0056aca0`).

---

## 5. Open questions

1. Identity of vtbl+0x18 target (sound system vs NDSpecialFX).
2. Why 0.2f specifically (fade seconds vs gain ramp).
3. Full weapon object layout beyond these three fields.
4. Live process confirmation.

**Verdict:** **accept**
