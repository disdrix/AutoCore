# Dual residual: `0x0053fff0` Mission_EnsureRegionMissions

| Field | Value |
|-------|-------|
| Stable ID | `aa_0053fff0` |
| Address | `0x0053fff0` |
| Owner | dual residual only (no WORK_QUEUE / RESUME / CHANGE_LOG) |
| Date | 2026-07-29 (wave3 — seal `+0xf18` wrapper) |
| Dual | A **accept** + B **accept** |
| Re-decompile | Ghidra `decompile_function` `0x0053fff0` + ctor `0x0060b870` + hash ctors (2026-07-29) — body **≡** frozen raw |

---

## Residual questions (pre-seal)

1. Does the getter return the CNDHash or a wrapper?
2. What lives at `owner+0xf18` (size / fields)?
3. Product type of the allocated object?
4. Relationship of secondary hashes / load path?

---

## Seal 1: Getter (VERIFIED)

| Claim | Result | Conf |
|---|---|---|
| `__fastcall` `ECX=owner` | live + bytes | **High** |
| Lazy `*(owner+0xf18)` | live | **High** |
| `new(0x10)` + `FUN_0060b870` | live | **High** |
| OOM → store 0 | live | **High** |
| Return wrapper\* not hash\* | live (no second deref) | **High** |

```c
// MSVC __fastcall
void* Mission_EnsureRegionMissions(void* owner); // returns *(owner+0xf18)
```

**Sibling (not owned):** `FUN_0053ff80` ensure-void + optional `FUN_0056e4e0`.

---

## Seal 2: `+0xf18` wrapper (VERIFIED layout)

Product class **High** via ctor log path `...\libVOG\VOGRegionMissions.cpp` and `"Out-o-memory... missions!"`.

| Off | Slot | Alloc | Init | Role |
|---|---|---|---|---|
| `+0x0` | `[0]` | 0x34 | `FUN_0053b0b0(8)` vtbl `009cfa4c` | Primary **mission-id** CNDHash — `*wrapper` |
| `+0x4` | `[1]` | 0x34 | `FUN_0053b1b0(8)` vtbl `009cfa58` | Objective inserts `FUN_0053c460` |
| `+0x8` | `[2]` | 0x38 | `FUN_0053b0b0(8)` + vtbl `009df688`, `+0x34=0` | Extended hash (key domain residual) |
| `+0xC` | `[3]` | 0x38 | same as `[2]` | Extended hash (key domain residual) |

Populate (ctor only, first ensure):

- Preload: non-empty `DAT_00b0434c`..`DAT_00b04350` pointer vector (WAD path).
- Else: `FUN_007e1d80` → `Mission_DbLoadQuestsAndObjectives` (`FUN_007c0350`) → `FUN_007b7df0`, materialize via `FUN_005480d0`.
- Per def: `FUN_0053c360` into `[0]`; secondary `FUN_0060c010` keys `def+0x88` / `def+0x86`; objectives into `[1]`.

### Falsified prior claims

| Claim | Outcome |
|---|---|
| Getter returns hash | **Falsified** — wrapper\* |
| `+0xf18` is bare CNDHash\* | **Falsified** — 0x10 / 4 hashes |
| Getter performs DB load every call | **Falsified** — once, in ctor |

---

## Seal 3: Callers / owners (this unit not owning callers)

| Owner pattern | Notes |
|---|---|
| `DAT_00b041fc` | Dominant global ECX (SearchAuto / Complete / …) |
| `*(clientCtx+0xd30)` | NpcMissionDialog |

High-signal named callers: GiveMission, SearchAutoMissions, HasCompletedMission, CompleteMissionObjectives, CheckMissionPrerequisites, CompleteObjective, ApplyCreateFromPacket, EvalOfferable / InteractState, Recv/Show NpcMissionDialog, UpdateMissionJournal, EvaluateComputed.

---

## Dual A/B residual verdict

| Review | Residual pass |
|---|---|
| A (fidelity) | **accept** — getter + wrapper layout sealed |
| B (skeptical) | **accept** — hash-return / bare-slot myths falsified |

Overall unit: **Human-refined; `+0xf18` wrapper sealed; secondary index keys + owner product names open.**

---

## Gaps (still open — unit-local)

1. Method product English (`Get` vs `Ensure` vs `GetOrCreate`).
2. Owner type name for `DAT_00b041fc`.
3. `FUN_0060c010` this-slot + keys `+0x88` / `+0x86` product meaning.
4. Extended hash type (`PTR_FUN_009df688`).
5. Destroy / reset of `owner+0xf18`.
6. Runtime capture of filled wrapper (static seal only).

## Explicit non-goals this pass

- No WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG edits
- No Launcher / servers
- Did not own ctor full dual (`aa_0060b870`) beyond wrapper layout seal
- Did not modernize unrelated reconstructed-exact callers

## Artifacts touched

| Kind | Path |
|---|---|
| Dual residual scratch | `docs/reconstruction/reviews/a_0053fff0.md` (this file) |
| Dual A | `docs/reconstruction/reviews/A_aa_0053fff0_Mission_EnsureRegionMissions.md` |
| Dual B | `docs/reconstruction/reviews/B_aa_0053fff0_Mission_EnsureRegionMissions.md` |
| Function record | `docs/reconstruction/functions/aa_0053fff0_Mission_EnsureRegionMissions.md` |
| Annotated | `docs/reconstruction/raw/aa_0053fff0_FUN_0053fff0.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/Mission_EnsureRegionMissions.cpp` |
| Raw | append-only re-verify note |
