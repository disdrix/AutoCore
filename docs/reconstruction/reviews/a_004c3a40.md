# Dual residual: `0x004c3a40` CVOGObject_SetLocalPosition4_Gated_Inferred

| Field | Value |
|-------|-------|
| Stable ID | `aa_004c3a40` |
| Address | `0x004c3a40` |
| Owner | dual residual only (no WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG / ledgers) |
| Date | 2026-07-29 (first dual A/B seal) |
| Tools | Ghidra `decompile_function` / `force_decompile` / `disassemble_function` / `read_memory` / `get_function_xrefs` / `get_assembly_context` / sibling+trap decompiles |
| Dual | `A_aa_004c3a40_*` / `B_aa_004c3a40_*` |

---

## Residual questions (entry)

1. Is scaffold name `Named_VOG_DEBUG_STOP` correct as primary role?
2. Does NaN abort the store?
3. Same gate as SetSteerInput?
4. Exact body span / ABI?
5. What is stored and where?
6. Full caller set beyond DriveControlTick?

---

## Seal summary

| Item | Status |
|------|--------|
| Body `0x004c3a40`–`0x004c3ab0`, 113 B, `ret 4` thiscall | **Sealed High** |
| NaN xyz only; fall-through after trap | **Sealed High** |
| Trap `"VOG_DEBUG_STOP"` @ `0x00a15844`; arg0=0 → logger no-op | **Sealed High** |
| Gate `this+*(*(this+4)+4)+0xb8` & **`0xC7`** | **Sealed High** (imm in `TEST`) |
| Store `FUN_004cb3e0` → `+0x240..+0x24c` | **Sealed High** |
| Sibling getter `004c3ac0` prefers `+0x240` when `&7==0` | **Sealed High** |
| Gate ≠ SetSteer (`+0xb0` ptr / `wobj+0xb4`) | **Sealed High** |
| Callers DCT + `0091a5f0`×2 + `005cc7c0` | **Sealed High** |
| Scaffold primary name VOG_DEBUG_STOP | **Rejected** (trap only) |
| Proposed `…_SetLocalPosition4_Gated_Inferred` | **Probable** role |
| Bit producers / product name / runtime | **Open** |

---

## Residual after dual

| Item | Status |
|------|--------|
| Product / RTTI name | Open |
| `+0xb8` bit producers | Open (UF) |
| Full `+0x240` consumer graph | Partial (getter sealed) |
| `FUN_005cd3b0` ungated leaf dual | Out of OWN scope |
| `FUN_0091a5f0` deep dual | Out of OWN scope |
| Runtime / bit-exact | Open |
