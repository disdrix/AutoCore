# Review A (reconstruction fidelity): `aa_00514e00` FUN_00514e00 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_00514e00` |
| **VA** | `0x00514e00` |
| **Canonical name** | `FUN_00514e00` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Reconstruction fidelity / evidence-only naming |
| **Counterpart** | `reviews/B_aa_00514e00_FUN_00514e00.md` |
| **System** | skills-abilities |
| **Verdict** | **leave-FUN** — behavior dual OK; product name withheld |

---

## 1. Purpose (behavioral, not product name)

Null-safe **decrement** of the active-cast / busy **refcount** on the lazy 0x14 block at **`this+0x64`**:

1. Load `block = *(this+0x64)`.
2. If `block != 0` and `*(block+0x10) != 0`: store `*(block+0x10) -= 1`.
3. When post-dec count is **0**, store `*(block+0xC) = 0`.

**Does not** allocate the slot (unlike `FUN_005169c0`), **does not** queue a skill id (unlike `Skill_ClearActiveCastCounterAndQueueId`), **returns void**. Same dec/+0xC-clear sequence is **inlined** after immediate apply in `Skill_ApplyStatusEffectLocal` on `pCasterObj+0x64`.

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Fresh decompile | Ghidra MCP `decompile_function` @ `0x00514e00` |
| Force re-decompile | Ghidra MCP `force_decompile` @ `0x00514e00` (identical body) |
| Complete analysis | `analyze_function_complete` — leaf; 5 xrefs; 0 callees |
| Callers | `get_function_callers` / xrefs |
| Related | `FUN_005169c0` (lazy slot @ +0x64); `Skill_ClearActiveCastCounterAndQueueId` (dec + queue) |
| Prior residual | `reviews/a_0051aa00.md` role note `Skill_DecActiveCastCounter` (INFERRED) |
| Raw / clean scaffold | `raw/aa_00514e00_FUN_00514e00.md`, `reconstructed-exact/FUN_00514e00.cpp` |

---

## 3. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| `__fastcall` unary `this` in ECX; void return | **High** | decompile; leaf; no stack formals |
| Load `*(this+0x64)` (decimal 100) | **Confirmed** | dual decompile |
| Null block → no-op | **Confirmed** | |
| Dec only when `*(block+0x10) != 0` | **Confirmed** | |
| Zero `block+0xC` iff post-dec == 0 | **Confirmed** | same pattern as ClearActiveCastCounter |
| Leaf — no callees / no globals | **Confirmed** | analyze_function_complete |
| 5 UNCONDITIONAL_CALL / 3 callers | **Confirmed** | xrefs |
| Same dec half as `Skill_ClearActiveCastCounterAndQueueId` (without lazy-get / queue / AL) | **High** | side-by-side decompile of `0x00517b90` |
| Inline twin after immediate apply on `pCasterObj+0x64` in ApplyStatusEffectLocal | **High** | CF at delay&lt;1 path |
| Product string / RTTI | **None** | no data refs; leaf |
| `Skill_DecActiveCastCounter[_Inferred]` as registry name | **Withheld** | role High only; no string/RTTI; leave-FUN |

---

## 4. Callers (Ghidra 2026-07-29)

| Caller | Sites | Context (behavioral) |
|---|---|---|
| `Client_RecvSkillStatusEffect` `0x00811170` | `0x008111c5`, `0x00811200`, `0x00811482` | Error abort path (2×); wire status **`0x11`** remove path (1×) before LookupActiveCastBinding / vtbl+0x220 |
| `Skill_ApplyStatusEffectLocal` `0x0051aa00` | `0x0051abd4` | After active-cast binding clear when **not** sim (`world+0x7e == 0`) |
| `FUN_00606180` `0x00606180` | `0x00606193` | WakeupSkill tick path when `*(hb+0x658)==0` (before LocalCastValidate / ApplyEffects) |

Decompiler often prints `FUN_00514e00()` with no formals — ECX `this` still required; outer type not sealed (same gap as `FUN_005169c0`).

---

## 5. Control flow: dual A/B decompile

**Pass A** (`decompile_function`) ≡ **Pass B** (`force_decompile`) — exact match:

```c
void __fastcall FUN_00514e00(int param_1)
{
  int iVar1;
  int iVar2;

  iVar1 = *(int *)(param_1 + 100);
  if (((iVar1 != 0) && (*(int *)(iVar1 + 0x10) != 0)) &&
     (iVar2 = *(int *)(iVar1 + 0x10) + -1, *(int *)(iVar1 + 0x10) = iVar2, iVar2 == 0)) {
    *(undefined4 *)(iVar1 + 0xc) = 0;
  }
  return;
}
```

| Stage | Match |
|---|---|
| Load this+0x64 | Yes |
| Triple compound if (non-null, count!=0, dec) | Yes |
| Clear +0xC on zero | Yes |
| No invent network / validate / queue | Yes |

---

## 6. Naming gate

Evidence-only rule (siblings `aa_005169c0`, `aa_00518c20`): promote product / `_Inferred` name only with string, RTTI, or strong exclusive plate.

| Evidence class | Present? |
|---|---|
| String | **No** |
| RTTI | **No** |
| Sole exclusive semantic caller | **No** (3 callers across recv / apply / HB) |
| Prior residual role name `Skill_DecActiveCastCounter` | Analogy / role only — **not** a rename seal |

**Decision:** retain `FUN_00514e00`. Document role as active-cast counter **dec** on slot at `this+0x64`; do **not** three-rep promote.

---

## 7. Gaps

1. Product type of ECX owner (caster vs skill subsystem vs HB) — shared open with `FUN_005169c0`.
2. Product English for `block+0xC` (queue-id / head linkage remains INFERRED from ClearActiveCastCounter residual).
3. Who **increments** `+0x10` (pairing producer; not this unit).
4. Precise ECX at `FUN_00606180` / Recv call sites (decomp elides formals).
5. Runtime / bit-exact / image diff (matrix policy).

**Verdict:** **leave-FUN** (behavior dual OK; name withheld).
