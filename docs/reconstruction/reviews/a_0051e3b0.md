# Dual residual: `0x0051e3b0` CVOGHBOKToCastAgain_OnEnd

| Field | Value |
|-------|-------|
| Stable ID | `aa_0051e3b0` |
| Address | `0x0051e3b0` |
| Owner | dual residual only (**no** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 |
| Prior dual | A/B accept-with-gaps (2026-07-23) — open: FUN_005081f0 identity, category map erase, abort vs expire sites |

## Residual questions (from prior dual)

1. True identity / role of `FUN_005081f0` (“Stop/detach?”)
2. Whether category cooldown map is erased on end
3. Abort vs natural-expire call sites
4. `unaff_SI` third arg on `Skill_SetIsCastingFlag`
5. Gate slot: owner vs skill (`HB+0x18` vs `HB+0x24`)
6. Whether “base cleanup” does real work

## Seal: machine body (High)

Body `0x0051e3b0`–`0x0051e3ca` (`read_memory`):

```
0051e3b0  56                 push  esi
0051e3b1  8B F1              mov   esi, ecx          ; this = HB
0051e3b3  83 7E 18 00        cmp   dword [esi+0x18], 0
0051e3b7  74 0A              je    +0x0A             ; → 0051e3c3
0051e3b9  8B 4E 24           mov   ecx, [esi+0x24]   ; skill
0051e3bc  6A 00              push  0                 ; bIsCasting = false
0051e3be  E8 0D1F0300        call  Skill_SetIsCastingFlag  ; 0x005502d0
0051e3c3  8B CE              mov   ecx, esi          ; this again
0051e3c5  5E                 pop   esi
0051e3c6  E9 259EFEFF        jmp   FUN_005081f0      ; 0x005081f0 (tail)
```

| Claim | Conf | Evidence |
|-------|------|----------|
| `__thiscall` / ECX = HB; **zero** stack formals | **High** | prolog `mov esi,ecx`; no `ret n`; tail `jmp` |
| Gate = `HB+0x18 != 0` | **High** | `cmp [esi+0x18],0` |
| `HB+0x18` = **owner object** (not skill) | **High** | `CVOGHBBase_AttachOwnerObject` stores owner @ `+0x18`; ctor attaches **owner**, skill @ `+0x24` |
| Skill ptr = `HB+0x24` | **High** | `mov ecx,[esi+0x24]` + ctor residual |
| Flag call: `ECX=skill`, **one** stack arg **imm 0** | **High** | `push 0; call 0x005502d0` |
| `unaff_SI` third arg | **Falsified** | no third push; decomp phantom |
| No category-map / erase / list ops in body | **High** | only cmp/call/jmp |
| Ends in **tail-call** (not `call`+`ret`) | **High** | `mov ecx,esi; pop esi; jmp` |

## Seal: `FUN_005081f0` = empty stub (High)

`read_memory @ 0x005081f0` (16 bytes):

```
C3 CC CC CC ...     ; RET; int3 padding
```

Function body range: entry `005081f0`–end `005081f0` (single-byte).

| Prior dual claim | Reality | Conf |
|------------------|---------|------|
| “Base cleanup helper” (meaningful work) | **Empty `ret`** — no stores, no callees | **High** |
| “Name FUN_005081f0 (Stop/detach?)” | Shared **nop vfunc** / empty thiscall target | **High** |
| “Leak HB resources if wrong” | **No resource work** in this unit’s tail | **High** (for this VA) |

Xrefs: many **DATA** (vtbl slots) + many call/jmp sites — shared empty stub across HB / other classes. OnEnd preserves ECX=this into the stub purely so a normal thiscall epilogue shape works; stub immediately returns.

**Product English name still unknown** (keep `FUN_005081f0` / alias `CVOGHBBase_EmptyVFunc_ret` if needed). Identity for *behavior* is sealed: **no-op**.

## Seal: vtbl slot (High)

`g_pVtbl_CVOGHBOKToCastAgain` = `0x009ce1c4` (`read_memory`):

| Slot | VA | Ptr | Role |
|------|-----|-----|------|
| +0x00 | `0x009ce1c4` | `0x005d6c40` | scalar dtor thunk |
| +0x04 | `0x009ce1c8` | `0x0051e390` | **OnStart** |
| +0x08 | `0x009ce1cc` | `0x005082c0` | `CVOGHBBase_TryFire` |
| +0x0c | `0x009ce1d0` | `0x00508350` | `CVOGHBBase_RescheduleAfterFire` |
| +0x10 | `0x009ce1d4` | `0x005081e0` | (base / other; not this residual) |
| **+0x14** | **`0x009ce1d8`** | **`0x0051e3b0`** | **OnEnd (this unit)** |
| +0x18 | `0x009ce1dc` | `0x005083f0` | base helper that **vcalls +0x14** when nonzero flag |

Sole Ghidra xref to OnEnd: **DATA** from `0x009ce1d8` (vtable). No direct CALL sites — only virtual dispatch (list remove / base dtor path / abort walk that vcalls OnEnd).

## Seal: `Skill_SetIsCastingFlag` site ABI (High)

Callee `0x005502d0` (`read_memory` + dual accept):

```
mov al, [esp+4]
mov [ecx+0x628], al
test al,al / je skip
  movss [ecx+0x170], 0
  mov [ecx+0x178], g_dwClientTickMs   ; DAT_00b041cc
ret 4
```

| Claim | Conf |
|-------|------|
| OnEnd site passes **false (0)** | **High** (`push 0`) |
| Writes `skill+0x628 = 0` | **High** |
| **Does not** clear `+0x178` / `+0x170` on false | **High** (`je` skips stamp path) |
| Symmetric OnStart site passes **1** then stamps tick | **High** (OnStart body `push 1`; sibling residual) |

## Seal: control flow (High)

```
void __thiscall CVOGHBOKToCastAgain_OnEnd(void* thisHb)  // ECX; 0 stack args
{
  if (*(void**)((char*)thisHb + 0x18) != nullptr) {      // owner object
    Skill_SetIsCastingFlag(*(void**)((char*)thisHb + 0x24), /*bIsCasting=*/0);
    //   → skill+0x628 = 0; no tick stamp/clear
  }
  // tail: ECX = thisHb; jmp FUN_005081f0 → ret  (no-op)
}
```

### Symmetry with OnStart (`0x0051e390`)

| Step | OnStart | OnEnd |
|------|---------|-------|
| Gate `HB+0x18` | same | same |
| Flag | **1** | **0** |
| Skill slot | `HB+0x24` | `HB+0x24` |
| Trailing | tail-jmp `CVOGHBBase_StampLastFireTime` | tail-jmp **empty** `FUN_005081f0` |

## Seal: falsified prior dual claims

| Claim | Verdict | Conf |
|-------|---------|------|
| Leaves casting stuck | **Falsified** — sets flag 0 | High |
| Sets casting 1 | **Falsified** — OnStart only | High |
| Meaningful “base cleanup” via FUN_005081f0 | **Falsified** — empty RET | High |
| Category map erase in OnEnd | **Falsified** — absent from body | High |
| `unaff_SI` real ABI arg | **Falsified** — decomp phantom | High |
| Gate is “skill attached” | **Refined** — gate is **owner @ +0x18**; skill is **+0x24** | High |

## Residual after this pass

| Item | Status |
|------|--------|
| Body CF + offsets + push-0 | **Sealed High** |
| `FUN_005081f0` empty / no-op | **Sealed High** |
| Vtbl +0x14 / DATA-only entry | **Sealed High** |
| SetIsCastingFlag site ABI (1 stack char, RET 4) | **Sealed High** |
| No category erase here | **Sealed High** (map may outlive HB — ctor inserts) |
| Product English for empty stub | Open (behavior sealed) |
| Full enumerate of *which* remove/abort sites vcall +0x14 | Open (sibling list/dtor units; no direct CALL here) |
| Whether UI reads `+0x628` only vs category map remaining | Sibling / hotbar (out of scope) |
| Runtime / bit-exact / image diff | Open (matrix policy) |
| Original PDB symbol | Unknown |

## Surviving AutoCore contract

```
OKToCastAgain_OnEnd (type-8 HB vtbl+0x14):
  if HB.owner (+0x18) != null:
    skill = HB.skill (+0x24)
    skill.isCasting (+0x628) = 0   // no tick clear on false
  // no category-map erase; duration/category UI uses map from ctor
  // trailing base empty vfunc — no extra teardown in this unit
```

## Artifacts touched

- This residual: `reviews/a_0051e3b0.md`
- Duals: `A_aa_0051e3b0_*`, `B_aa_0051e3b0_*`
- Function record, clean, annotated, raw append
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
