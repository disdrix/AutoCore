# Dual residual: `0x00518cf0` Skill_LookupActiveCastBinding

| Field | Value |
|-------|-------|
| Stable ID | `aa_00518cf0` |
| Address | `0x00518cf0` |
| Owner | dual residual only (no WORK_QUEUE / RESUME / CHANGE_LOG) |
| Date | 2026-07-29 (ABI/ECX byte re-seal pass) |
| Prior dual | A/B accept-with-gaps (2026-07-23 scaffold) — open: out-struct, DAT_00a15870 “map identity” |
| Fresh sources | Ghidra decompile + pcode + callers/xrefs; `read_memory` body `0x00518cf0` (0x80), sentinels `0x009cdf88` / `0x00a15870`; callees `FUN_00518c20` / `FUN_0051c150`; sibling dual `aa_00518c20` |

## Residual questions (from prior dual)

1. Full out-struct layout (16-byte activeBinding) — **sealed**
2. DAT_00a15870 map identity — **falsified**
3. Map key / find helper roles — **sealed**
4. Caller inventory + cast-lock contract — **sealed**
5. *(thin after first residual)* ABI ret cleanup / hidden mapOwner register — **sealed this pass**

## Seal: out is TFID16 (not a larger binding blob)

| Evidence | Detail | Conf |
|----------|--------|------|
| Hit arm (bytes) | `add ecx, 0x18` then 4× dword `[edx+i] = [ecx+i]`, `i∈{0,4,8,0xc}` @ `0x00518d22…0x00518d3a` | **Confirmed** |
| Miss arm (bytes) | 4× `mov ecx,[0x009cdf88+i]; mov [edx+i],ecx` @ `0x00518d40…0x00518d62` | **Confirmed** |
| Sentinel bytes | `read_memory 0x009cdf88` → `FFFFFFFF FFFFFFFF 00000000 00000000` | **Confirmed** |
| Callers | LocalCastValidate / ResolveCastTarget / RequestCastSkill treat out as TFID for `TFID_NotEquals` / `TFID_EqualsObjectId` vs invalid | **High** |

**Closes prior open Q** “Full out-struct layout” → **sealed as 16-byte TFID**.

## Seal: DAT_00a15870 is **not** the map

| Claim | Result | Conf |
|-------|--------|------|
| Plate text “DAT_009cdf88 / DAT_00a15870” as unbound fill | **Half-true** — body fill uses **`g_abTfidInvalid_9CDF88` only** (no load of `A15870` in body bytes) | **Confirmed** |
| `0x00a15870` content | Same 16 bytes as `0x009cdf88` (`read_memory`) | **Confirmed** |
| Role of `g_abTfidInvalid_A15870` | **Caller-side** compare constant (ResolveCastTarget, RequestCastSkill, Stance path) | **High** |
| Map identity | **`FUN_00518c20` → lazy header @ owner `+0x68`** (sibling dual leave-FUN) | **Confirmed** |

**Falsifies prior open Q** “DAT_00a15870 map identity”.

## Seal: ABI + hidden mapOwner ECX (byte residual)

Body `read_memory` `0x00518cf0` length ≥ `0x78` (ends `CC` pad):

| Site | Bytes | Recovered | Conf |
|------|-------|-----------|------|
| Prologue | `51 56 8B F1` | `push` local slot; `push esi`; **`mov esi, ecx`** → **entry ECX = mapOwner** preserved in ESI | **Confirmed** |
| Setup | `lea eax,[esp+0x10]; push eax; lea ecx,[esp+8]; push ecx; mov ecx,esi` | push **`&nSkillId`**, push **`&it`**, set ECX=owner | **Confirmed** |
| `0x00518d00` | `E8 …` → `FUN_00518c20` | unary `__fastcall(owner)` — **stack args ignored** by getter | **Confirmed** |
| `0x00518d05` | `8B C8` + call `FUN_0051c150` | `mov ecx,eax` (map*); thiscall find consumes the two stack args | **Confirmed** |
| `0x00518d0c` | `8B CE` + call `FUN_00518c20` | second get-map with ESI owner | **Confirmed** |
| `0x00518d13` | `8B 4C 24 04; 3B 48 04` | `it = [esp+4]`; **`cmp it, [map+4]`** end test | **Confirmed** |
| Hit exit | `5E 59 C2 08 00` @ `0x00518d3b` | `pop esi; pop ecx; **ret 8**` | **Confirmed** |
| Miss exit | `5E 59 C2 08 00` @ `0x00518d63` | same **`ret 8`** | **Confirmed** |

**Contract (register + stack):**

```
// Entry:
//   ECX              = mapOwner  (same host as FUN_00518c20 this; product type OPEN)
//   Stack[+4]        = void *pOutTfid   (TFID16 out)
//   Stack[+8]        = int nSkillId
// Exit: ret 8  →  __stdcall cleanup of two stack formals  (Confirmed)
// Ghidra may omit ECX from signature; body requires it.
```

| Claim | Conf | Notes |
|-------|------|-------|
| `__stdcall` stack cleanup `ret 8` | **Confirmed** | both arms; upgrades prior **High** |
| Stack formals `(pOutTfid, nSkillId)` | **Confirmed** | Stack[+4]/[+8] |
| Entry **ECX = mapOwner** | **Confirmed** | `mov esi,ecx`; both map gets use ESI |
| Map owner **product type** | **Open** | leave-FUN sibling; not invent “Character*” |
| Decompiler multi-arg `FUN_00518c20(pTmp,key)` | **Noise Confirmed** | pushes are for following `FUN_0051c150` |

## Seal: find path = map get + lower_bound/exact + end test

```
// Recovered CF (byte-level; decompiler multi-arg FUN_00518c20 noise corrected)
// mapOwner = ECX_entry
pMap = FUN_00518c20(mapOwner);          // sites 0x00518d00, 0x00518d0e
FUN_0051c150(pMap, &it, &nSkillId);     // site 0x00518d07; thiscall map*
if (it != *(int*)(pMap + 4)) {          // end = map header _Myhead @ +4
  copy node[ +0x18 .. +0x24 ] → out;    // value TFID16
} else {
  copy g_abTfidInvalid_9CDF88 → out;
}
```

| Claim | Conf | Notes |
|-------|------|-------|
| `FUN_00518c20` unary `__fastcall` lazy map @ `+0x68` | **Confirmed** | sibling dual `aa_00518c20` |
| `FUN_0051c150` = map find by `int` key | **Confirmed** | body: walk tree; key @ `node+0x10` (`puVar[4]`); nil flag `+0x29` |
| Hit ⇒ exact key (`node.key == skillId`) | **Confirmed** | lower_bound then keep only if `key <= skillId` (and not end) ≡ equality |
| End iterator = `*(map+4)` | **Confirmed** | body `cmp` + classic MSVC map `_Myhead` |
| Node value offsets `+0x18…+0x24` | **Confirmed** | this body only |
| Read-only (no insert/erase) | **Confirmed** | callees map-get + find only |
| Creates binding / mutates map | **Falsified** | |

## Seal: callers

| Item | Value | Conf |
|------|-------|------|
| Return | void (writes through out) | **Confirmed** |
| Xrefs | **9** UNCONDITIONAL_CALL | **Confirmed** (live re-list) |
| Distinct callers | **8** | list below |
| LocalCastValidate key | `skill+0x5fc`; ECX=`this` of validate host | **High** (decompile + site bytes) |

| Caller | Site(s) | Typical key | Use |
|--------|---------|-------------|-----|
| `Skill_LocalCastValidate` | `0x0051a83d` | `skill+0x5fc` | unbound (`NotEquals` false vs invalid) → free power/CD/range ladder |
| `Client_RequestCastSkill` | `0x009416e4`, `0x00941762` | `skill+0x5fc` | unbound → cast-again HB / FX path |
| `Client_Skill_ResolveCastTarget` | `0x0093b759` | `skill+0x5fc` | sticky form bit: binding **present** → silent invalid list (no promote) |
| `Client_StanceOrGadgetActivatePath` | `0x0093a4de` | skill id | cast-again gate |
| `Skill_ApplyStatusEffectLocal` | `0x0051ab1c` | packet skill id | status apply path |
| `Client_RecvSkillStatusEffect` | `0x00811496` | packet `+8` | S2C 0x2031 residual |
| `FUN_00521650` (`Skill_CVOGHBSkill_Summon`) | `0x005216a7` | summon skill id | HB summon |
| `FUN_00525cc0` | `0x00525d4a` | `+0x5fc` | named packet-side helper |

**Plate note:** “Cast-again HB only starts when lookup equals invalid” is **caller policy** (RequestCast / Stance), not body logic.

## Still open

1. Product type of map **owner** (entry ECX / `FUN_00518c20` `this` / `+0x68` host) — shared open with leave-FUN sibling. **Register role sealed; type not.**
2. Product name for `FUN_0051c150` (map find) and node factory `FUN_0058d9c0`.
3. Full map node type beyond key `+0x10` + TFID `+0x18…+0x24` (Clear path may use additional TFID compare fields — out of ownership).
4. Runtime / bit-exact image diff.
5. Whether both invalid TFID globals are always equal at runtime (static image match only sealed).

## Verdict target

**accept-with-gaps** — residual closes out-layout, falsifies A15870-as-map, seals `ret 8` + entry ECX mapOwner; map-owner **product type** remains open.
