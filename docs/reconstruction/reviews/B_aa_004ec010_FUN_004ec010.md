# Review B (skeptical / adversarial): `aa_004ec010` FUN_004ec010

| Field | Value |
|---|---|
| **Stable ID** | `aa_004ec010` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_004ec010_FUN_004ec010.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | Zero-arg void “tick” | Bytes: stack load + **`RET 4`**; callers pass event codes | **Falsified** — **`thiscall(this, eventId)`** |
| 2 | Always calls `FUN_004a16d0` | Branch on `this+0x88` after ensure | **Falsified** — **null → return 0** |
| 3 | Hardcodes event id | `mov eax,[esp+4]` pushed as first stack arg | **Falsified** — **caller-supplied** |
| 4 | Mode/slot free or zero | Immediate pushes `1` and `0xffffffff` | **Confirmed** — fixed **(1, −1)** |
| 5 | Clean invented `+0x88` | `8B 8E 88 00 00 00` | **Confirmed** |
| 6 | Is itself the `"%s_FOR_EVENT_%d"` formatter | Body only calls `004eb3b0`/`004a16d0` | **Falsified** — formatting is **callee** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| ABI thiscall + eventId + RET 4 | **High** | Wrong wrappers / stack smash |
| Gate field `+0x88` | **High** | Silent no-op ports |
| Ensure-then-dispatch order | **High** | Race / use-before-init |
| Constant args 1 / −1 | **High** | Wrong FX instance selection |
| Event-id product catalog | **Low** | Caller-owned; not this unit |
| Product class name | **Medium** | Keep `FUN_*` until plate |

---

## 3. Cross-check against raw + bytes

```
raw:
  FUN_004eb3b0();
  if (*(this+0x88)==0) return 0;
  return FUN_004a16d0(param_2, 1, 0xffffffff);  // this = +0x88 in ECX at call

bytes @ 004ec010:
  56 8B F1 E8.. 8B8E88000000 85C9 5E 7411
  8B442404 6AFF 6A01 50 E8.. C20400
  33C0 C20400 CC...

≡ CF. Sibling 004a16d0 uses snprintf "%s_FOR_EVENT_%d" → event semantics confirmed
without requiring rename of this unit.
```

---

## 4. Surviving contract for AutoCore

```csharp
// Client special-FX host: trigger scripted FOR_EVENT_<id> on attached resource.
object? SpecialFxHost_TriggerEvent(SpecialFxHost host, uint eventId)
{
    SpecialFxHost_EnsureResource(host);          // FUN_004eb3b0 → host.Resource @ +0x88
    if (host.Resource == null)
        return null;
    return SpecialFx_PlayForEvent(host.Resource, eventId, mode: 1, slot: -1);
}
```

- Server does **not** need this body for authority; client cosmetics only unless parity VFX required.
- Do not invent event ids — mirror caller tables from weapon/object paths (`4/7/8/0xc/…`).

---

## 5. Open questions

1. Exact type of `+0x88` (map vs NDSpecialFX instance).
2. Full meaning of mode `1` and slot `−1` inside `FUN_004a16d0`.
3. Whether `FUN_004eb3b0` can fail without clearing a stale non-null `+0x88`.
4. Runtime confirmation on live process (static only this session).

**Verdict:** **accept**
