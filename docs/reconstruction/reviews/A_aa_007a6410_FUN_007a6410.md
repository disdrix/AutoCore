# Review A (reconstruction fidelity): `aa_007a6410` FUN_007a6410 (leave-FUN)

| Field | Value |
|---|---|
| **Stable ID** | `aa_007a6410` |
| **VA** | `0x007a6410` |
| **Canonical name** | `FUN_007a6410` (**retained**) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Reconstruction fidelity |
| **Counterpart** | `reviews/B_aa_007a6410_FUN_007a6410.md` |
| **System** | locale / string-pool hash trees (FormatFailure modes 1–3 prep) |
| **Verdict** | **leave-FUN** |

**Evidence:** Ghidra `decompile_function` @ `0x007a6410`.

---

## 1. Purpose

**Lookup a hash key** in per-locale binary trees / layers on the string-pool object; write hit indices to out-params.

- `in_EAX` selects tree bank (`this+0x58 + bank*4` = layer count).
- Search key in **EBX** (`unaff_EBX`).
- Walk layers from high to low; each node is **0x14** bytes: key@+0, left@+0xc, right@+0x10 (child indices; −1 end).
- On key match: `*param_2 = layerIndex`, `*param_3 = nodeIndex`; return.
- Miss: both outs = **−1**.

Parent `FUN_007a6de0` modes 1–3 call `FUN_007a6490` (hash string) then this lookup; hit + explicit mode → `FUN_007a6310` pak load.

---

## 2. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| Out-params pack/layer + entry index | **Confirmed** | |
| Miss → (−1,−1) | **Confirmed** | |
| Node stride 0x14; BST via +0xc/+0x10 | **High** | |
| Multi-layer scan descending | **Confirmed** | |
| Product “translation table” name | **Tentative** | structure only |
| Key is CRC from 007a6490 | **High** | parent call order |

---

## 3. Artifacts

| Artifact | Path |
|---|---|
| Three-rep | `aa_007a6410_*` |
| Parent | `FUN_007a6de0` |
| Sibling hash | `FUN_007a6490` |

---

## 4. Gaps

1. Exact bank selector (`in_EAX`) mapping to language mode.
2. Who builds trees at load time.
3. Collision policy (first match only observed).

**Verdict:** **leave-FUN**.
