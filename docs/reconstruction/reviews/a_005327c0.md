# Dual residual: `0x005327c0` CVOGReaction_GiveMission

| Field | Value |
|-------|-------|
| Stable ID | `aa_005327c0` |
| Address | `0x005327c0` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior dual | A/B accept-with-gaps (2026-07-28) — open: insert FUN_*, def fields, double `FUN_0053c360`, AddActiveObjective arity, mode gate |

## Residual questions (from dual A/B)

1. Semantic identity of `FUN_0053c360` / `FUN_0053c660` / `FUN_00538a40` / `FUN_00538b20` / `FUN_00547920` / `FUN_0052d8b0` / `FUN_004111f0`
2. Whether double `FUN_0053c360` is accidental duplicate vs second-hash insert
3. Def field meanings at dword indices `+0x2b` / `+0x3e` / `+0x40` / `+0x4c` / `+0x5a` (byte-offset map)
4. `WeaponAllowsKillXpBonus` true object / mode semantics on grant path
5. AddActiveObjective arity tension vs sibling unit
6. Callers treating outer return-0 (already active) as hard error vs soft

## Method

- Live re-decompile of GiveMission + callees (Ghidra MCP `batch_decompile`)
- **Call-site assembly** via `get_assembly_context` on xrefs inside `0x005327c0` (no `disassemble_bytes`)
- Cross-check end-quest `FUN_005307e0` remove of `+0x540`/`+0x544` pair

Live body **matches frozen raw** (CF unchanged). Residual is callee/`this` recovery + byte offsets.

---

## Seal: hash insert / remove helpers

| Addr | Role | Evidence | Conf |
|------|------|----------|------|
| `0x0053c360` | **CNDHash_Insert** (node vtbl `PTR_LAB_009cf024`) | Strings `"Duplicate hash insert %u"`, `"HashError:insert, already locked…"`; body allocates node, links bucket + list | **High** |
| `0x0053c660` | **CNDHash_Insert** twin (node vtbl `PTR_FUN_009cefe4`) | Near-identical body; different vtbl only | **High** (role) |
| `0x00538b20` | **CNDHash_Remove** | String `"HashError:remove…"`; unlinks + freelist | **High** |
| `0x00538a40` | **Mission-state tree lower_bound** by mission id | Walks tree at `this+4`; compares key at node`[4]`; used with sentinel `char+0x50c` | **High** CF |
| `0x005b0920` | `CNDHash_LookupByKey` (already named) | Plate + body | **High** |

### Call-site `this` / args (GiveMission asm)

| Site | ECX (`this`) | Stack | Effect |
|------|--------------|-------|--------|
| `00532915` `FUN_0053c360` | `char+0x540` (EDI from `[EBP+0x540]`) | key=`ESI` missionId, payload=`EBX` def, soft=`0` | Insert **active mission** |
| `0053294d` `FUN_0053c360` | **`char+0x544`** | same key/def/soft | Insert **sibling active hash** (not same-hash double) |
| `00532973` `FUN_00538b20` | `char+0x538` (completed) | missionId, out=0 | Strip completed on re-grant |
| `005329e2` `FUN_0053c660` | **`char+0x530`** | key=`*def`, payload=0x30 blob, soft=0 | Bind runtime state blob |
| `005329af` `FUN_00538a40` | **`LEA [EBP+0x508]`** | out-node, &missionId | Prior-state tree lookup |
| `005329ee` `FUN_0052d8b0` | character (`EBP`) | flag=`0`, missionId=`*def` | State-tree notify / retarget slice |

**Double-insert residual closed:** second `FUN_0053c360` targets **`+0x544`**, not a duplicate into `+0x540`. End-quest (`FUN_005307e0`) removes both `+0x540` and `+0x544` for the same mission id — confirms paired active hashes.

Secondary insert gate (asm): mode gate AL≠0 **and** (`word [def+0xF8]==0` **or** `dword [def+0x100] != -1`).

Product English for `+0x544` still open (instance-active vs journal-shadow vs other). Role: **paired active-mission hash**.

---

## Seal: `FUN_00547920` = mission def XML ensure-load

| Evidence | Detail | Conf |
|----------|--------|------|
| Call sites | `MOV ECX,EBX` (def); `PUSH 1` then maybe `PUSH 0` | **High** |
| Gate | second call only if `byte [def+0x168]==0` | **High** |
| Body | if `def+0x168==0` set loaded=1; open `"%s%S.xml"`; parse `Objective` / `CoreMission` / `Title` / `Internal` / `Description` / `OneLineAccept`… | **High** |
| Stack 0/1 meaning | Present on every site; full force/mode semantics not fully recovered from truncated body | **Tentative** |

Decompiler showing `FUN_00547920(1)` / `(0)` as free calls is **wrong framing** — ECX is always mission def. Residual: leave product name optional; role **MissionDef_EnsureXmlLoaded**.

---

## Seal: state blob + notify

| Addr | Role | Evidence | Conf |
|------|------|----------|------|
| `0x004111f0` | Init **0x30** blob: store `0xFF…` dwords + clear byte at `+0x2c` | Pure stores on `operator_new(0x30)` result | **High** |
| `0x0052d8b0` | Walk mission-state tree (`+0x50c`); optional `ResolveObjectTarget` + clear-all when missionId==-1 | Body; grant passes `(char, 0, *def)` | **High** CF; product name open |

---

## Seal: def **byte** offsets (asm, not dword-index prose)

| Byte off | Asm use | Role | Conf |
|----------|---------|------|------|
| `+0x130` | `CMP byte,1` / `JC` reject; end-quest loops `byte` count | **Objective count (byte)** — grant requires ≥1 | **High** |
| `+0x13c` | `MOV EDI,[EBX+0x13c]` first obj list | Objectives pointer array | **High** |
| `+0xac` | `CMP word,-1` | **Repeatable marker**: `-1` skips completed reject + enables completed strip | **High** (behavior) |
| `+0xf8` | `CMP word,0` toast; secondary insert OR arm | Type/toast class: **0** → "Received Mission" + `gen_give_quest` | **High** (gate) |
| `+0x100` | `CMP dword,-1` secondary insert OR arm | Companion field for `+0x544` insert eligibility | **High** (gate); product word open |
| `+0x168` | XML loaded flag for `FUN_00547920` | Lazy-load once | **High** |
| `+0x14c` | Toast name string key | Localized mission title | **High** |

Prior dual “awardable flag at +0x130” is **overstated**: field is objective **count** byte; grant needs count≠0. Not a separate awardable bit.

---

## Seal: `WeaponAllowsKillXpBonus` call framing

```
MOV EAX, [char+4]
MOV ECX, [EAX+4]
MOV ECX, [ECX + char + 0xa8]   ; object at entity-chain +0xa8
CALL 0x004ce340
```

- **Not** character-as-`this` on this path.
- Predicate body (dual `aa_004ce340`): true iff linked object live, `obj+0x100==0`, `obj+0xac != 2`.
- Same gate selects completed hash `+0x538` vs instance-completed `+0x53c` and secondary active insert.

**Conf:** call framing **High**; product name remains historical (XP-spree reuse) **High**; meaning of mode `==2` / flag `+0x100` still **open** (shared with other duals).

---

## Seal: AddActiveObjective arity

```
MOV ECX, EBP          ; character
PUSH [firstObj]       ; objective def
CALL 0x00531b00
```

**High:** `__thiscall(character, objectiveDef)` — two formals. GiveMission raw single-arg form is decompiler this-elision. Sibling raw already shows 2 formals.

---

## Return-value consumers

Outer already-active / reject → `XOR AL,AL` / `RET 4` (**0**).  
Grant body / “Already had mission” log → `MOV AL,1` (**1**).

Callers (SearchAutoMissions, Dispatch 0x1e, dialog accept) do **not** branch on the return in recovered CF — treat 0/1 as status for logs/automation; **not** proved as hard UI error. Residual **open** for product meaning only.

---

## Residual after this pass

| Item | Status |
|------|--------|
| `FUN_0053c360` / `0053c660` / `00538b20` roles | **Sealed High** as hash insert/remove |
| Double insert → `+0x540` then `+0x544` | **Sealed High** |
| `FUN_00547920` XML ensure-load | **Sealed High** (stack 0/1 nuance Tentative) |
| State blob init + `+0x530` bind + tree lower_bound | **Sealed High** CF |
| Def byte map 130/13c/ac/f8/100/168/14c | **Sealed High** as gates/count |
| `+0x544` / `+0x530` product English | Open |
| Mode object `+0xa8` identity / mode==2 | Open (shared) |
| Runtime grant capture | Open |
| AddActiveObjective arity | **Sealed High** from this call site |

## Artifacts touched by residual owner

- This scratch: `reviews/a_005327c0.md`
- Duals: `A_aa_005327c0_*`, `B_aa_005327c0_*`
- Function record open Q; annotated corrections append; light clean plate comments only
- VERIFICATION_MATRIX open-issues cell; CHANGE_LOG
- **Not** WORK_QUEUE / RESUME
- **Not** wholesale clean rewrite
