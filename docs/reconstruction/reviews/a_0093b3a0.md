# Dual residual: `0x0093b3a0` Client_Skill_ResolveCastTarget

| Field | Value |
|-------|-------|
| Stable ID | `aa_0093b3a0` |
| Address | `0x0093b3a0` |
| Owner | dual residual only (no WORK_QUEUE / RESUME) |
| Date | 2026-07-29 |
| Prior dual | A/B accept-with-gaps — closed: **FUN_004f8b80**, **unaff reg on active-bind**, **outFlags bit4/8 writers**, **hardpoint this/reload/sentinel content** |
| Scratch twin | `tmp/a_0093b3a0.md` |

## Residual questions (from dual A/B + VERIFICATION_MATRIX)

1. ~~Identity of `FUN_004f8b80` (alt TFID helper)~~ **Closed**
2. ~~Recover `TFID_NotEquals` first arg (`pA` / `unaffReg`) after `Skill_LookupActiveCastBinding`~~ **Closed**
3. ~~Who ORs outFlags bits **4 / 8** (consumed here)~~ **Closed**
4. ~~Whether sticky/active-bind promotes binding TFID into out~~ **Closed** (no)
5. ~~Hardpoint this / reload / sentinel content equivalence~~ **Closed** (this strengthen)
6. Runtime / binary diff — still open (not residual-sealable static-only)

## Seal: `FUN_004f8b80` → hardpoint occupant TFID

| Evidence | Detail | Conf |
|----------|--------|------|
| Live decompile name | `Vehicle_ResolveFirstHardpointOccupantTfid_Inferred` at call site | **Confirmed** |
| Sole primary consumer | this unit (bit4 + `skill+0x614 & 8`) | **Confirmed** |
| Dual | `reviews/A\|B_aa_004f8b80_*` | accept-with-gaps |
| Fail UX | `"No enemies found."` @ `0x00a2e04c` if returned TFID == invalid | **High** string; product “enemy” gloss **overstated** |
| this | `ECX = *(char+0x250)` | **Confirmed** asm |

**Closes prior open Q** “Seal FUN_004f8b80”.

## Seal: hardpoint call-site plate (strengthened)

```
0093b56c  TEST AL, 0x8                    ; skill+0x614 bit3 (after &2 related failed)
0093b56e  JZ   0x0093b5df                 ; code 0xe fail
0093b570  MOV  EDX, [ESI + 0xe98]         ; local character
0093b576  MOV  ECX, [EDX + 0x250]         ; vehicle/entity this
0093b57c  CALL 0x004f8b80                 ; Vehicle_ResolveFirstHardpointOccupantTfid_Inferred
0093b581  MOV  EDI, [EAX]                 ; reload write_seed plate from *returned TFID
0093b583  MOV  EBP, [EAX+4]
0093b586  MOV  EBX, [EAX+8]
0093b589  MOV  EAX, [EAX+0xc]
0093b58c  LEA  ECX, [ESP+0x18]
0093b590  PUSH 0x00a15870                 ; g_abTfidInvalid_A15870
0093b595  PUSH ECX
0093b5a6  CALL TFID_EqualsObjectId        ; 0x00404aa0 (1=match, 0=diff)
0093b5ae  TEST AL, AL
0093b5b0  JZ   write_seed                 ; not-equal invalid → success
          ; else: report && !on-foot → "No enemies found." → write_invalid
```

### Sentinel content equivalence (`read_memory`)

| Symbol | VA | 16B |
|--------|-----|-----|
| `DAT_009cd0f8` (helper empty) | `0x009cd0f8` | `ffffffffffffffff0000000000000000` |
| `g_abTfidInvalid_A15870` | `0x00a15870` | **identical** |

Pointer identity differs; content identity **Confirmed** → empty hardpoint always fails Equals check.

| Claim | Result | Conf |
|-------|--------|------|
| Related (`&2`) before hardpoint (`&8`) on bit4 | **Sealed** | **Confirmed** |
| ECX this = `*(char+0x250)` | **Sealed** | **Confirmed** |
| Hardpoint reloads write_seed regs | **Sealed** | **Confirmed** |
| Empty ≡ invalid content | **Sealed** | **Confirmed** |
| Product table word | open (callee dual `_Inferred`) | Probable |

## Seal: outFlags bit4/8 writers (consumer side)

Producers sealed on `Skill_ResolveTargetList` residual `reviews/a_00550300.md` + filter dual:

| Bit | Writer | This unit consume |
|----:|--------|-------------------|
| **1** | ResolveTargetList | clear → list/bind/toast; set → related or “Nothing is targeted.” |
| **2** | ResolveTargetList + range helper | code **0xd** toast |
| **4** | `Skill_FilterTargetForResolveList_Inferred` | related if `+0x614&2`; hardpoint if `&8`; else code **0xe** |
| **8** | same filter | related if `&2`; else code **3** |
| **0x10** | same filter | not branched here |

**Closes prior open Q** “Who sets outFlags bits 4 and 8”.

## Seal: active-bind `pA` / sticky semantics (asm)

Call sites: Lookup `0x0093b759`, NotEquals `0x0093b75f`.

```
TEST  [skill+0x615], 2
JZ    skip
MOV   ECX, [skill+0x5fc]
PUSH  g_abTfidInvalid_A15870     ; pB (pre-push for NotEquals)
PUSH  ECX                        ; skillId
LEA   ECX, [ESP+0x34]            ; local_750 activeBindScratch
PUSH  ECX                        ; Lookup out
CALL  Skill_LookupActiveCastBinding
PUSH  EAX                        ; pA = out buffer
CALL  TFID_NotEquals
TEST  AL, AL
JNZ   write_seed                 ; binding present
```

| Claim | Result | Conf |
|-------|--------|------|
| `pA` == `local_750` / `activeBindScratch` | **Sealed** | **Confirmed** |
| Sibling pattern | RecvSkillStatusEffect `NotEquals(&local_220, …)` | **High** |
| write_seed plate | EDI/EBP/EBX/`[ESP+0x24]` after list load @ `0x0093b48f` | **Confirmed** |
| Sticky promotes binding TFID? | **No** — no reload of scratch into EDI… before `JNZ write_seed` | **Confirmed** |
| Sticky effect | silent write of **invalid listOut** (skip “Can't find a target!”) | **Confirmed** |

**Closes prior open Q** “unaff reg on active-bind”.

### write_seed register plate

| Out | Source after ResolveTargetList |
|-----|--------------------------------|
| +0 | EDI ← listOut0 |
| +4 | EBP ← listOut1 |
| +8 | EBX ← listOut2 |
| +c | `[ESP+0x24]` ← listOut3 |

Related path (`vtbl+0x19c` → `+0x164`) and hardpoint path **do** reload these regs before write_seed.

## Surviving contract (tightened)

```
Client_Skill_ResolveCastTarget (client EAX; 5 stack formals; RET 0x14):
  require local character (+0xe98)
  if skill+0x614 == 1: copy seed TFID → out; return
  resolveSource = dual-base; on-foot+selection → selection
  Skill_ResolveTargetList → listOut + outFlags
  dispatch outFlags (table above)
  bit4 + skill&2 → related TFID
  bit4 + skill&8 → ECX=*(char+0x250);
                   Vehicle_ResolveFirstHardpointOccupantTfid_Inferred;
                   reload write_seed; Equals vs invalid
  list invalid + skill+0x615&2 + active bind:
       write invalid silently (do not promote binding TFID)
  hard fail: optional chat (bReportFailure && !on-foot); invalid sentinel
```

## Durable outputs

- Dual A/B hardpoint strengthen
- Function record refreshed
- Clean / annotated hardpoint plate notes
- **No** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG

## Still open

- Runtime cast trace / binary diff
- Secondary callers `FUN_00829190` / `FUN_00941d50`
- Product English for remaining `skill+0x614/+0x615` bits not used here
- Hardpoint table product word (owned by `aa_004f8b80`)
