# Review B (skeptical / adversarial): `aa_00723120` FUN_00723120

| Field | Value |
|---|---|
| **Stable ID** | `aa_00723120` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_00723120_FUN_00723120.md` |
| **Verdict** | **accept-with-gaps** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | Pure free function (decompile) | Entry `mov esi, ecx` | **Falsified** — **thiscall** |
| 2 | Skips all work on OOM | `FUN_004406e0` after null check | **Confirmed register always called** |
| 3 | Object size 0x100 | `push 0x150` | **Falsified** — **0x150** |
| 4 | Full arg map sealed | Decompile partial / reordered | **Confirmed gap** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Alloc 0x150 | **High** | Heap corruption if wrong size |
| thiscall container | **High** | Wrong insert target |
| Ctor only if non-null | **High** | Null deref |
| Full arg semantics | **Low** | Incomplete play parity |

---

## 3. Cross-check against raw + bytes

```
bytes: push 0x150; mov esi,ecx; call new; test eax; jz; stack loads; ctor
raw: if new: 0071e570; always 004406e0
decompile missed thiscall — A notes it.
≡ structure accept-with-gaps.
```

---

## 4. Surviving contract for AutoCore

```csharp
void AudioFx_SpawnPlayInstance(Manager mgr, PlayArgs args)
{
    var obj = Alloc(0x150);
    if (obj != null)
        ConstructPlayObject(obj, args); // FUN_0071e570
    RegisterPlay(mgr, obj);             // FUN_004406e0 — may no-op on null
}
```

---

## 5. Open questions

1. Complete `PlayArgs` field list from call sites in `004b7e50`.
2. `004406e0` null behavior.
3. Runtime verification.

**Verdict:** **accept-with-gaps**
