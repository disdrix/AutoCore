# Dual residual: `0x00514e20` Skill_SiblingCastClear

| Field | Value |
|-------|-------|
| Stable ID | `aa_00514e20` |
| Address | `0x00514e20` |
| Owner | dual A/B only (no WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 |
| Prior | scaffold only (`FUN_00514e20`); product role open in `a_0051aa00` residual |

## Questions

1. ABI / thiscall + list host
2. Context pack layout
3. Predicate match rules (`LAB_00512bb0`)
4. Flag byte semantics
5. Callers / relationship to binding clear and OKToCastAgain
6. Product name

## Seal: ABI (**Confirmed**)

`read_memory` body `0x00514e20`…`0x00514e6c`:

```
sub esp, 0x20
… pack stack locals …
mov ecx, [ecx+0xB0]          ; thiscall host → list*
push 0                       ; nullPayloadFlag
lea eax, [ctx]
push eax                     ; ctx
push 0x00512bb0              ; predicate
call CNDDoubleList_InvokePredicateAndRemove_Inferred
add esp, 0x20
ret 0x18                     ; 6 stack dwords
```

| Formal | Storage | Role |
|--------|---------|------|
| `pHost` | **ECX** | object with HB list at `+0xB0` |
| `nSkillId` | stack+4 | match key |
| `tfid0..tfid3` | stack+8..+0x14 | source TFID pack (partial match) |
| `bAlsoOkToCastAgain` | stack+0x18 (byte) | gate type-8 arm |

Decompiler surface omits `this` / ECX load — **noise**; asm sealed.

## Seal: body CF (**Confirmed**)

```
ctx = {
  +0x00 nSkillId,          // param_1
  +0x04 <unset>,           // local_20[1] never written
  +0x08 tfid0..            // params 2..5 contiguous from +8
  +0x0c tfid1,
  +0x10 tfid2,
  +0x14 tfid3,
  +0x18 flag byte          // param_6
}
list = *(pHost + 0xB0)
CNDDoubleList_InvokePredicateAndRemove_Inferred(
  list, Skill_SiblingCastClearPredicate, &ctx, /*nullPayloadFlag=*/0)
```

`nullPayloadFlag=0` → node payload ptr zeroed before node destroy (list dual); payload already stopped in predicate.

## Seal: predicate `0x00512bb0` (**Confirmed** RTTI + CF)

Created as Ghidra fn `Skill_SiblingCastClearPredicate_INFERRED` (was `LAB_00512bb0`).  
Callback shape (list dual): `(payload, ctx, &stop)` — body never writes stop → removes **all** matches.

### Arm A — type tag `payload+0x1c == 1` (`CVOGHBSkillBase`)

1. `__RTDynamicCast(payload, 0, CVOGHBBase, CVOGHBSkillBase, 0)`
   - type_info `0x00af8d98` = `".?AVCVOGHBBase@@"`
   - type_info `0x00af8cec` = `".?AVCVOGHBSkillBase@@"` (**Confirmed** `read_memory`)
2. Match **all**:
   - `hb+0x620 == ctx+0` (skillId; = embedded skill blob `@+0x24` field `+0x5fc`)
   - `hb+0x658 == ctx+8` (source TFID dword0; ctor seeds from `source+0x160`)
   - `hb+0x65c == ctx+0xc`
   - `(u8)(hb+0x660) == (u8)(ctx+0x10)` — **byte only** of third TFID dword
3. On match: `hb->vtbl+0x18(1, 0)` then return **1** (remove node)

### Arm B — type tag `== 8` (`CVOGHBOKToCastAgain`)

1. Requires `ctx.flag != 0` else return 0
2. Requires `payload+0x1c == 8`
3. RTTI → `CVOGHBOKToCastAgain` type_info `0x00b0015c` = `".?AVCVOGHBOKToCastAgain@@"` (**Confirmed**)
4. Match: `*(hb+0x24 skill* + 0x5fc) == ctx.skillId` (no TFID compare)
5. On match: same `vtbl+0x18(1,0)` → return 1

Cross-unit: `CVOGHBSkillBase_ctor` dual seeds TFID `@+0x658` from `source+0x160` and type tag `+0x1c=1`; `CVOGHBOKToCastAgain_ctor` sets `+0x1c=8`, skill `*@+0x24`.

## Seal: callers (**Confirmed** xrefs)

| Caller | Site | ECX host | flag | Role |
|--------|------|----------|------|------|
| `Skill_ApplyStatusEffectLocal` | `0x0051ab93` | `pCasterObj` | **0** | binding present → clear skill-base cast HB before re-apply / cancel ladder |
| `FUN_00514e70` | `0x00514ee3` | character `this` | **0** | skill lookup `vtbl+0x234`; gate `skill+0x615 & 1`; then maybe re-enqueue WakeupSkill / ApplyEffects |
| `FUN_0052db50` | `0x0052dbae` | this-adjusted object | **1** | skill-tree remove by `skill+0x5fc`; also clears OKToCastAgain; then `operator_delete` skill |

Caller TFID sources:

- ApplyStatus / FUN_00514e70: host `+0x160..+0x16c`
- FUN_0052db50: after `this = this + *(vtbl[-1?]+4)` style adjust, reads `+0x164..+0x170` — **delta vs +0x160 open** (layout after adjust)

## Seal: role vs siblings

| Unit | Role | Distinct |
|------|------|----------|
| `Skill_LookupActiveCastBinding` | read-only map skillId→TFID | no list walk |
| `Skill_ClearCastBindingAndMaybeRestartCd` | mutate binding map + optional cast-again HB start | map, not `+0xB0` list |
| **`Skill_SiblingCastClear` (this)** | remove matching cast HBs from host `+0xB0` | list predicate remove |
| `Skill_DecActiveCastCounter` (`0x00514e00`) | `--` counter at `obj+0x64` block | not list |
| `CNDDoubleList_InvokePredicateAndRemove` | generic CS list machinery | utility |

Not LocalCastValidate. Not wire send. Client HB bookkeeping on host list.

## Product name

| Name | Confidence |
|------|------------|
| `Skill_SiblingCastClear` | **High** role (task + CF); demangled symbol **Open** → keep usable name, note Inferred if registry requires |
| Predicate `Skill_SiblingCastClearPredicate` | **Inferred** (no symbol) |

## Gaps remaining

1. Full 16-byte TFID compare not used — only 2 dwords + 1 byte; intentional vs decompiler/asm completeness (**asm sealed as partial**).
2. `ctx+0x04` hole and `ctx+0x14` (tfid3) unused by predicate.
3. Product English for `vtbl+0x18(1,0)` (Stop/End family; shared with listener notify path).
4. `FUN_00514e70` / `FUN_0052db50` product names.
5. Exact this-adjust / TFID base on `FUN_0052db50` path.
6. List identity: `host+0xB0` is CNDDoubleList-of-HB (**High** via utility dual); product field name open.
7. Runtime / bit-exact / image diff.

## Artifacts

- Dual: `reviews/A_aa_00514e20_Skill_SiblingCastClear.md`, `B_aa_00514e20_Skill_SiblingCastClear.md`
- Scratch: this file
- Function record / annotated / clean updates
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
- No Launcher
