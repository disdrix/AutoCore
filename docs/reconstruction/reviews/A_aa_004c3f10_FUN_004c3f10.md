# Review A (reconstruction fidelity): `aa_004c3f10` FUN_004c3f10

| Field | Value |
|---|---|
| **Stable ID** | `aa_004c3f10` |
| **VA** | `0x004c3f10` |
| **Body** | `0x004c3f10`–`0x004c3f50` |
| **Canonical name** | `FUN_004c3f10` (leave-FUN) |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Independent reconstruction review |
| **Counterpart** | `reviews/B_aa_004c3f10_FUN_004c3f10.md` |
| **System** | `skills-abilities` (+ shared character helpers) |
| **Verdict** | **accept-with-gaps** |

---

## 1. Purpose

**Count 16-byte vector elements** on a character/object layout:

| Offset | Role (layout) |
|---:|---|
| `+0x1bc` | begin ptr vector A (null → count 0) |
| `+0x1c0` | end ptr vector A |
| `+0x1cc` | begin ptr vector B |
| `+0x1d0` | end ptr vector B |

```
countA = (beginA==0) ? 0 : (endA - beginA) >> 4;   // elem size 0x10
if (flag != 0) return countA;
return countA - countB;   // countB same formula on +0x1cc/+0x1d0; beginB==0 → subtract 0
```

FormatFailure case **0x10** calls with **flag=0** → net count (A minus B) compared to `FUN_004c2e60` cap. Multi-caller utility (not skill-exclusive).

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Raw / annotated / clean / function record | `aa_004c3f10_*` |
| Live decompile | ≡ raw |
| Body bytes | `read_memory` 64 B — two `RET 4` exits |
| Callers | FormatFailure + `FUN_00518630`, `FUN_00615020`, `FUN_006170b0`, `FUN_006508d0`, `FUN_008027e0`, `FUN_008028e0`, `FUN_008a0770`, `FUN_0091eaf0`, `FUN_00923770` |

---

## 3. Confidence table

| Claim | Confidence | Notes |
|---|---|---|
| Span / thiscall + `char flag` stack / **RET 4** | **Confirmed** | bytes `c2 04 00` |
| Elem stride **16** (`>> 4`) | **Confirmed** | both vectors |
| Null begin → 0 for that vector | **Confirmed** | |
| `flag != 0` → raw countA only | **Confirmed** | `cmp byte [esp+4],0; jnz` early path |
| `flag == 0` → countA − countB | **Confirmed** | |
| Used as **current summon count** in FormatFailure 0x10 | **High** (context) | layout product name open |
| Vector element type product | **Tentative** | 0x10 pods only |
| Product function name | **Open** | leave FUN_* |
| Skill-exclusive | **Falsified** | many callers |

---

## 4. Control flow

```c
/* __thiscall this; stack: char bIncludeOrNet; RET 4 */
int __thiscall FUN_004c3f10(void *pThis, char flag)
{
  int *bA = *(int **)((char *)pThis + 0x1bc);
  int countA = bA ? ((*(int *)((char *)pThis + 0x1c0) - (int)bA) >> 4) : 0;
  if (flag != 0)
    return countA;
  int *bB = *(int **)((char *)pThis + 0x1cc);
  if (!bB) return countA;
  int countB = (*(int *)((char *)pThis + 0x1d0) - (int)bB) >> 4;
  return countA - countB;
}
```

Clean ≡ raw ≡ live. No alloc / no write.

---

## 5. Gaps / open

1. Product meaning of vectors A vs B (active summons vs reserved? pets vs turrets?).
2. Why FormatFailure always passes flag **0**.
3. Object type owning `+0x1bc` block (character plant from vtbl+0x210).
4. Promote name only with string/RTTI.

**Verdict:** **accept-with-gaps** — CF/ABI/offsets sealed; product vector roles open.
