# Dual residual: `0x00611940` CVOGObjectiveRequirement_Collect_Eval

| Field | Value |
|-------|-------|
| Stable ID | `aa_00611940` |
| Address | `0x00611940` |
| Owner | dual residual only (no WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 (strengthen pass) |
| Scope | CountItems bridge + dual A/B + function record |

## Residual questions (closed this wave)

1. Full stack ABI (`RET 8` vs single formal in decomp) → **Sealed**
2. Second `FUN_005711c0` call real or CSE failure? → **Sealed real**
3. `float10` return vs SSE path → **Sealed** x87 return of f32
4. Count thiscall ECX framing (cargo grid) → **Sealed**
5. Progress authority: inventory-derived vs S2C `0x2071` → **Sealed dual-path**
6. Constants `g_flOne` / `g_flZero` → **Sealed**
7. COMISS complete polarity / equality → **Sealed** (≥ completes)

## Live evidence this pass

| Source | Result |
|--------|--------|
| Ghidra `decompile_function` @ `0x00611940` | NumToCollect +0x14; cargo +0x250/+0x2b0; Count ×2; float10 |
| Ghidra `read_memory` body `0x00611940`..+160 | Full control bytes; both `E8 …` → `0x005711c0` |
| Ghidra `read_memory` `0x009e12c4` | Vtable +0x10 LE `40 19 61 00` = Eval |
| Ghidra `read_memory` `0x00a0f2a0` / `0x00a0f518` | `00 00 80 3f` / `00 00 00 00` |
| Ghidra `get_xrefs_to` | Sole **DATA** `0x009e12d4` |
| Ghidra `get_function_callers` | None (virtual only) |
| Count decomp `0x005711c0` | includeBroken gate; bit19 filter |
| SlotAction `0x006124b0` | same cargo + double Count when incomplete |
| XML `0x00611a00` | `CBID`→+0x10, `NumToCollect`→+0x14 |
| UseItem_Eval `0x0060cfe0` | family RET 8; uses stack1 state |

**Not performed:** `disassemble_bytes`, Launcher, live CE, bit-exact image diff.

---

## Seal: stack ABI (shared Eval formals)

```text
float10 __thiscall CVOGObjectiveRequirement_Collect_Eval(
    Requirement* this,       // ECX → ESI
    Character*   character,  // stack0 → EDI  (cargo host)
    void*        objState);  // stack1 — common Eval ABI; **unread by this body**
// RET 8  (cleans two stack dwords)
```

| Prior decompiler artifact | Asm reality | Confidence |
|---------------------------|-------------|------------|
| Single stack formal only | **`RET 8`** three exit sites | **High / Confirmed** |
| Character as sole arg | Body loads **stack0 only** (`[ESP+0xC]` after PUSH ESI/EDI) | **High / Confirmed** |
| Second stack unused | No load of stack1 | **High / Confirmed** |

| Formal | UseItem / Kill Eval | Collect Eval |
|--------|---------------------|--------------|
| ECX this | requirement | requirement |
| stack0 | unused | **character** (cargo) |
| stack1 | **objective state** (slot floats) | unused (inventory replaces slots) |

---

## Seal: control flow (body bytes)

```text
PUSH ESI; ESI = ECX (req)
if (*(ESI+0x14) == 0)           // NumToCollect
    FLD [g_flOne]; POP ESI; RET 8

PUSH EDI; EDI = [ESP+0xC]       // character (stack0)
EAX = *(EDI+0x250)              // vehicle / cargo host
if EAX==0 → zero_exit
ECX = *(EAX+0x2b0)              // InventoryGrid*
if ECX==0 → zero_exit

// first count (for compare) — CALL @ 0x00611972
PUSH 0; PUSH *(ESI+0x10); CALL CountItemsByCbid  // ECX=cargo, includeBroken=0
XMM1 = (float)EAX / (float)*(ESI+0x14)
XMM0 = 1.0
// COMISS XMM0, XMM1; JBE when 1.0 <= ratio → skip re-count, keep XMM0=1.0
else:
    // second count — CALL @ 0x006119a3
    ECX = *(*(EDI+0x250)+0x2b0)
    PUSH 0; PUSH *(ESI+0x10); CALL CountItemsByCbid
    XMM0 = (float)EAX / (float)*(ESI+0x14)

MOVSS [ESP+0xC], XMM0; FLD [ESP+0xC]; POP EDI; POP ESI; RET 8

zero_exit:
    FLD [g_flZero]; POP EDI; POP ESI; RET 8
```

### Double Count + bridge

| Claim | Status |
|-------|--------|
| Two `CALL 0x005711c0` sites | **Sealed High** (`0x00611972`, `0x006119a3`) |
| First always when cargo present | **High** |
| Second only when ratio **&lt; 1.0** | **High** |
| Both pass `includeBroken = 0` | **High** |
| Both set ECX = cargo grid | **High** |
| Equality (count == need) completes | **High** (`JBE` on 1.0 ≤ ratio) |
| SlotAction double-count isomorph | **High** (sibling bridge consumer) |

### Constants

| Label | VA | Bytes LE | f32 |
|-------|-----|----------|-----|
| `g_flOne` | `0x00a0f2a0` | `00 00 80 3f` | **1.0** |
| `g_flZero` | `0x00a0f518` | `00 00 00 00` | **0.0** |

---

## Seal: vtable + fields

| Item | Value | Confidence |
|------|-------|------------|
| Collect vtable | `PTR_FUN_009e12c4` | **High** |
| Eval slot | `+0x10` → `0x00611940` (`DATA` @ `0x009e12d4`) | **High / Confirmed** |
| Action slot | `+0x04` → `0x00611f20` | **High** |
| SlotAction slot | `+0x18` → `0x006124b0` | **High** |
| type code | ctor `= 2` | **High** |
| req `+0x10` / `+0x14` | CBID / NumToCollect | **High** |
| cargo path | `char+0x250` → `+0x2b0` | **High** |

---

## Seal: progress authority (B challenge)

| Path | Role |
|------|------|
| **This function** | Client **live inventory** fraction (vtable+0x10 Eval) |
| S2C `0x2071` ObjectiveState | Server **absolute** slot floats; UseItem/Kill Eval read those |
| Collect contrast | Collect Eval **does not** read slot floats — cargo recount only |

Broken stacks: Count bit19 = Broken; this path always `includeBroken=0` → broken cargo does **not** advance Collect_Eval.

---

## Residual after this pass

| Item | Status |
|------|--------|
| Stack ABI `RET 8` / character = stack0 | **Sealed High** |
| stack1 identity (objState family formal) | **Sealed High** (unread; sibling pattern) |
| Double Count calls | **Sealed High** |
| Count ECX = cargo; includeBroken=0 | **Sealed High** |
| SSE math + x87 return | **Sealed High** |
| g_flOne / g_flZero | **Sealed High** |
| Vtable +0x10 Eval | **Sealed High** |
| COMISS equality completes | **Sealed High** |
| Only Collect progress source? | **Sealed:** no — also server `0x2071` |
| Original PDB symbol | Open |
| Runtime journal refresh after grab | Open |
| Bit-exact / image diff | Open (policy) |
| Pickup auto-invokes Eval without UI tick | Open |

## Artifacts (this owner)

- Scratch: `reviews/a_00611940.md`
- Duals: `A_aa_00611940_*`, `B_aa_00611940_*`
- Function record: `functions/aa_00611940_CVOGObjectiveRequirement_Collect_Eval.md`
- Clean: `reconstructed-exact/CVOGObjectiveRequirement_Collect_Eval.cpp`
- Experiment: `experiments/inventory_collect_bridge.py` (+ tests)
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
