# Review B (skeptical / adversarial): `aa_004e9530` FUN_004e9530

| Field | Value |
|---|---|
| **Stable ID** | `aa_004e9530` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_004e9530_FUN_004e9530.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | Normalizes / length-scales vector | No mul/div/sqrt/rsqrt in bytes | **Falsified hard** — **copy only** |
| 2 | Copies float4 from src | Only 3 loads from src; w from stack | **Falsified float4-from-src** |
| 3 | thiscall | Stack args via ebp | **Confirmed cdecl** |
| 4 | Parent dual “normalize” label | Re-read bytes | **Parent note wrong**; this dual corrects |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| xyz copy | **High** | Wrong position feed |
| w undefined | **High** | If port assumes src.w |
| Not normalize | **High** | Wrong math if assumed |

---

## 3. Cross-check against raw + bytes

```
raw writes param_1[3]=local_14 (uninit) ≡ movss from [esp+0xC].
Three movss loads from src, three stores to dst + fourth store.
No math ops. ≡ A sealed + corrects 004b7e50 dual wording.
```

---

## 4. Surviving contract for AutoCore

```csharp
void CopyFloat3_MaybeWriteJunkW(Span<float> dst4, ReadOnlySpan<float> src3)
{
    dst4[0] = src3[0];
    dst4[1] = src3[1];
    dst4[2] = src3[2];
    // dst4[3] intentionally unspecified (original stack garbage)
}
// Prefer: only consume xyz in ports.
```

---

## 5. Open questions

1. Prefer re-label clean/raw comments if they still say “normalize”.
2. Runtime verification optional (trivial).

**Verdict:** **accept**
