# Independent adversarial spot-check — WQ9R-A dual `0x0051b720` + `0x005749d0`

| Field | Value |
|---|---|
| **Date** | `2026-08-04` |
| **Role** | Independent adversarial verifier (**not** WQ9R-A dual author) |
| **Workspace** | `C:\Users\josh\Documents\GitHub\AutoCore` |
| **Partition** | WQ9R-A OWN pair (WQ-009 residual / skills priority 93) |
| **Units** | `0x0051b720` `Rng_CopyUnitTableWindow_Inferred`; `0x005749d0` `VehicleCrew_ContainsMember_Inferred` |
| **Tools** | Ghidra MCP `decompile_function`, `read_memory`, `get_function_by_address`, `get_function_callers`, `get_function_xrefs`, `analyze_function_complete`; dual report + A/B + clean/raw read-through |
| **Forbidden** | `disassemble_bytes`; parent ledger edits; dual rewrite; Launcher |
| **Image** | `autoassault.exe` base `0x400000` |

---

## Inspected evidence

### Dual / report surfaces

| Path |
|---|
| `docs/agents/task-dual-ab-0051b720-005749d0-wq9ra-report.md` |
| `docs/reconstruction/reviews/A_aa_0051b720_Rng_CopyUnitTableWindow_Inferred.md` |
| `docs/reconstruction/reviews/B_aa_0051b720_Rng_CopyUnitTableWindow_Inferred.md` |
| `docs/reconstruction/reviews/A_aa_005749d0_VehicleCrew_ContainsMember_Inferred.md` |
| `docs/reconstruction/reviews/B_aa_005749d0_VehicleCrew_ContainsMember_Inferred.md` |

### Artifacts (both units)

| Kind | `aa_0051b720` | `aa_005749d0` |
|---|---|---|
| Raw | `docs/reconstruction/raw/aa_0051b720_FUN_0051b720.md` | `docs/reconstruction/raw/aa_005749d0_FUN_005749d0.md` |
| Annotated | `…/aa_0051b720_FUN_0051b720.annotated.md` | `…/aa_005749d0_FUN_005749d0.annotated.md` |
| Clean named | `…/Rng_CopyUnitTableWindow_Inferred.cpp` | `…/VehicleCrew_ContainsMember_Inferred.cpp` |
| Clean twin | `…/FUN_0051b720.cpp` | `…/FUN_005749d0.cpp` |
| Function named | `…/aa_0051b720_Rng_CopyUnitTableWindow_Inferred.md` | `…/aa_005749d0_VehicleCrew_ContainsMember_Inferred.md` |

### Live Ghidra spot-checks

| Check | Result |
|---|---|
| `decompile_function(0x0051b720)` | Live CF ≡ dual: wrap → table+cursor\*2 → dword/byte copy of `nWords*2` → `cursor++`; decompiler still shows **void** (return-type warning active) |
| `decompile_function(0x005749d0)` | Live CF ≡ dual: null→0; COID via offBase+0x164/0x168; 4×0x10 scan @ +0x198; decompiler **uint3/CONCAT31** noise present |
| `read_memory` 80 B @ `0x0051b720` | Full **72 B** body through `C2 08 00`; pad `CC` |
| `read_memory` 100 B @ `0x005749d0` | Full **94 B** body through dual `C2 04 00`; pad `CC` |
| `get_function_by_address` both | `0051b720–0051b767` (excl. `0051b768`); `005749d0–00574a2d` (excl. `00574a2e`) |
| `get_function_callers(0x0051b720)` | **2**: `Skill_ApplyStatusEffectLocal`, `FUN_0061fdf0` |
| `get_function_callers(0x005749d0)` | **8** named (filter, gather helper, insert, destroy, +4) |
| `get_function_xrefs(0x005749d0)` | **14** `UNCONDITIONAL_CALL` sites |
| `analyze_function_complete` both | **leaf**; 0051b720 return_type_warning (“do not trust void”) |
| `decompile` `CVOGReaction_RandomUnitScalar` @ `0x007a4330` | returns `&DAT_00d20c1c` |
| `decompile` mode-0xb path in `FUN_0058a810` | same `vtbl+0x210` vehicle OR `FUN_005749d0` with host at `+0xCB0` |
| Call-site `read_memory` | ApplyStatusEffectLocal seed store; 0061fdf0 discard; mode-0xb `mov ecx,[edi+0xCB0]` |

---

## Unit A — `0x0051b720` Rng_CopyUnitTableWindow_Inferred

### Body / ABI seal (independent)

Full body hex (**72 B** / `0x48`, exclusive of trailing `CC`):

```
8B442404 8BD1 8B4A0C 03C8 81F900001000 7C07 C7420C00000000
56 8B7208 8D0C00 8B420C 8D3446 8BC1 57 8B7C2410
C1E902 F3A5 8BC8 83E103 F3A4
8B420C 8D4801 5F 894A0C 5E C20800
```

Independent byte decode (no `disassemble_bytes`):

| Claim | Independent result |
|---|---|
| Range half-open `0x0051b720`–`0x0051b768` (**72 B**) + `CC` pad | **Confirmed** — epilogue ends `C2 08 00` @ `0x0051b765`–`0x0051b767`; pad from `0x0051b768` |
| `__thiscall` host in ECX | **Confirmed** — `8B D1` (`MOV EDX,ECX`) then host fields via EDX |
| Stack args: `nWords` then `dst*`; **`RET 8`** | **Confirmed** — `8B 44 24 04` nWords; `8B 7C 24 10` dst after 2 pushes; `C2 08 00` |
| Wrap when `cursor + nWords >= 0x100000` (≡ `> 0xFFFFF`) | **Confirmed** — `ADD ECX,EAX; CMP ECX,0x100000; JL skip; MOV [EDX+0xC],0` |
| Table base `@+0x08`, cursor `@+0x0C` | **Confirmed** — `8B 72 08` / `8B 4A 0C` / `8B 42 0C` |
| Source `base + cursor*2` (ushort index) | **Confirmed** — `8D 34 46` (`LEA ESI,[ESI+EAX*2]`) |
| Copy `nWords*2` bytes (`rep movsd` + residual `rep movsb`) | **Confirmed** — `LEA ECX,[EAX+EAX]`; `SHR ECX,2; F3 A5`; `AND ECX,3; F3 A4` |
| `cursor += 1` (**not** `+= nWords`) | **Confirmed** — `8D 48 01` then `89 4A 0C` |
| **EAX = pre-increment cursor** (decompiler void wrong) | **Confirmed** — final `8B 42 0C` loads cursor into EAX **before** `LEA ECX,[EAX+1]` store; never clobbered to exit |
| Leaf | **Confirmed** — no callees; analyze complete |
| Callers exactly **2** | **Confirmed** — callers API + 2 xrefs |
| `this` = RNG singleton | **Confirmed** — both sites: `call 0x007a4330` → `MOV ECX,EAX` → `call 0x0051b720`; getter returns `&DAT_00d20c1c` |
| nWords literal **600** at skill sites | **Confirmed** — `68 58 02 00 00` (`PUSH 0x258`) at both call sites |
| ApplyStatusEffectLocal uses return as seed | **Confirmed** — @ `0x0051ab00` `89 47 3C` (`MOV [EDI+0x3C],EAX`) immediately after call @ `0x0051aafb` |
| `FUN_0061fdf0` discards return | **Confirmed** — call @ `0x0061ffdc`; next insn is `ADD [ESI+0x6D0],EBX` (no EAX store) |
| Live decompile ≡ raw CF | **Confirmed** — wrap/copy/cursor++ identical to 2026-07-23 raw body |
| Clean named ≡ sealed CF + return | **Confirmed** — returns `oldCursor`; preserves +1 advance |

### Call-site rel32 seal

| Site | Bytes | Decode |
|---|---|---|
| `0x0051aaf4` getter | `E8 37 98 28 00` | next `0x0051aaf9` + `0x00289837` = **`0x007a4330`** |
| `0x0051aafb` unit | `E8 20 0C 00 00` | next `0x0051ab00` + `0x00000C20` = **`0x0051b720`** |
| `0x0061ffd5` getter | `E8 56 43 18 00` | → **`0x007a4330`** |
| `0x0061ffdc` unit | `E8 3F B7 EF FF` | next `0x0061ffe1` + signed rel → **`0x0051b720`** |

### Adversarial attacks (CopyUnitTableWindow)

| # | Attack | Result |
|---|---|---|
| 1 | Function is true `void` / no useful return | **Fails** — EAX holds old cursor; ApplyStatusEffectLocal stores to `pkt+0x3C` |
| 2 | Advances cursor by full window (`+= nWords`) | **Fails** — `LEA ECX,[EAX+1]` only |
| 3 | Index is dword-scaled (`*4`) | **Fails** — `LEA ESI,[ESI+EAX*2]`; wrap headroom is nWords ushorts |
| 4 | Getter consumes the two stack args | **Fails** — getter is 0-arg; this unit `RET 8` pops them |
| 5 | Same as `Rng_CloneSeededTable` (`0x0058ab60`) | **Fails** — no alloc / no force-seed / nWords param / returns cursor not heap |
| 6 | Writes into global table | **Fails** — `REP MOVS` direction is table → `dst` (ESI=src, EDI=dst) |
| 7 | Only one caller | **Fails** — second site `FUN_0061fdf0` |
| 8 | Wrap threshold is `0x80000` (u32 count) | **Fails** — `CMP …,0x100000` |
| 9 | Product/PDB name known | **Fails** — `_Inferred` required |
| 10 | Runtime Confirmed without Launcher | **Fails** — dual correctly leaves Terminal false |

### Soft niggle (not sealed-claim failure)

- Clean header comment “MT-backed” is sibling/context English, not proven by this leaf alone. Body CF and ABI remain correct.
- Dual table-size English (`0x200000` bytes) is layout context from init siblings, not encoded in this 72 B body.

### Unit A dual quality

**PASS** — keep **`accept-with-gaps`**.

Sealed CF/ABI/wrap/scale/`cursor+1`/EAX return/2 callers/nWords=600 all re-verified. Gaps dual already flags (product English, post-copy buffer readers, runtime) remain fair.

---

## Unit B — `0x005749d0` VehicleCrew_ContainsMember_Inferred

### Body / ABI seal (independent)

Epilogue / return bytes (`read_memory` @ `0x00574a20`):

```
5F 32 C0 5E C2 04 00   ; miss: pop edi; xor al,al; pop esi; ret 4
5F B0 01 5E C2 04 00   ; hit:  pop edi; mov al,1; pop esi; ret 4
CC CC                  ; pad
```

Full body starts:

```
8B 44 24 04 85 C0 75 05 32 C0 C2 04 00   ; candidate; null → AL=0, RET 4
8B 50 04 8B 52 04                         ; offBase = *(*(cand+4)+4)
56 8B B4 02 64 01 00 00                   ; ESI = coidLo @ offBase+cand+0x164
57 8B BC 02 68 01 00 00                   ; EDI = coidHi @ offBase+cand+0x168
32 D2 … 0F B6 C2 C1 E0 04                 ; slot index → *0x10
3B B4 08 98 01 00 00                      ; cmp lo vs this+0x198+slot*0x10
3B BC 08 9C 01 00 00                      ; cmp hi vs this+0x19c+slot*0x10
80 C2 01 80 FA 04 72 E0                   ; slot++; if slot < 4 continue
```

| Claim | Independent result |
|---|---|
| Range half-open `0x005749d0`–`0x00574a2e` (**94 B** / `0x5E`) + `CC` pad | **Confirmed** |
| `__thiscall` + **`RET 4`** | **Confirmed** — dual exits `C2 04 00`; candidate from `[esp+4]` |
| Null candidate → 0 | **Confirmed** — early `TEST EAX; JNZ; XOR AL,AL; RET 4` |
| COID via SharedBase offBase +0x164/+0x168 | **Confirmed** — `MOV ESI,[EDX+EAX+0x164]` / `MOV EDI,[EDX+EAX+0x168]` |
| 4 slots × stride **0x10** @ `this+0x198` | **Confirmed** — `SHL EAX,4`; cmp `@+0x198`/`@+0x19c`; loop exit `CMP DL,4; JB` |
| Return **AL 0/1** (not CONCAT31) | **Confirmed** — miss `32 C0`; hit `B0 01`; decompiler noise rejected |
| Leaf | **Confirmed** — callees empty |
| **8** named callers / **14** call xrefs | **Confirmed** — callers API + xrefs list (14 sites across 8 functions) |
| Mode 0xb ECX = `vehicle+0xCB0` | **Confirmed** — see site seal below |
| Live decompile ≡ raw CF | **Confirmed** — structure matches 2026-07-23 raw; return-type artifacts noted |
| Clean named ≡ sealed CF | **Confirmed** — bool AL; 4×0x10; COID pair only |

### Mode 0xb call-site seal (`FUN_0058a810` @ `0x0058aa3x`)

Bytes @ `0x0058aa3b`–`0x0058aa6b` (independent):

```
FF 92 10 02 00 00     call [EDX+0x210]   ; caster vehicle → EDI
…
FF 90 10 02 00 00     call [EAX+0x210]   ; candidate vehicle → EAX
85 FF                 test EDI,EDI
74 ..                 jz reject
3B F8                 cmp EDI,EAX
0F 84 …               je accept          ; same vehicle
8B 8F B0 0C 00 00     mov ECX,[EDI+0xCB0]  ; crew host this
85 C9                 test ECX,ECX
74 ..                 jz reject
50                    push EAX           ; candidate vehicle
E8 65 9F FE FF        call FUN_005749d0  ; @ 0x0058aa66 → 0x005749d0
84 C0                 test AL,AL
```

| Claim | Independent result |
|---|---|
| Same-vehicle short-circuit via `vtbl+0x210` | **Confirmed** |
| Crew host at **`vehicle+0xCB0`** as ECX | **Confirmed** — `8B 8F B0 0C 00 00` |
| Candidate pushed as stack arg | **Confirmed** — `50` before call |
| AL bool test after call | **Confirmed** — `84 C0` |
| Faction path is separate (`vtbl+0x298` modes 1/2) | **Confirmed** — mode 0xb branch never uses `+0x298` |

### Adversarial attacks (ContainsMember)

| # | Attack | Result |
|---|---|---|
| 1 | Returns packed `CONCAT31` / garbage on miss | **Fails** — miss path `XOR AL,AL; RET 4` |
| 2 | Compares full 16-byte TFID | **Fails** — only two dwords (slot +0/+4 vs COID lo/hi) |
| 3 | Scans unbounded list | **Fails** — hard stop after slot index `>= 4` |
| 4 | `this` is always the vehicle object | **Fails for mode 0xb** — ECX = `*(vehicle+0xCB0)` |
| 5 | This is faction / friendly-fire math | **Fails as sole role** — pure COID roster; faction is `vtbl+0x298` elsewhere in same helper |
| 6 | Only called from destroy-object | **Fails** — 8 callers / 14 xrefs including skill filter + gather |
| 7 | Slot stride is 8 (COID pair only) | **Fails** — `SHL EAX,4` (0x10) |
| 8 | Null candidate undefined | **Fails** — explicit early return 0 |
| 9 | Product name is retail-known | **Fails** — `_Inferred` |
| 10 | Runtime Confirmed | **Fails** — no Launcher; Terminal false |

### Soft niggle (not sealed-claim failure)

- Slot bytes beyond COID pair (writer object* field) remain open — dual already gaps this; not a false seal.
- English “VehicleCrew” / “plant crew” is structural + gather dual labeling, not PDB demangle.

### Unit B dual quality

**PASS** — keep **`accept-with-gaps`**.

Sealed CF/ABI/4×0x10/AL bool/mode-0xb this-pointer all re-verified. Gaps dual already flags (product English, slot payload, member object types, runtime) remain fair.

---

## Cross-unit dual report quality

| Check | Result |
|---|---|
| Dual report verdicts match A/B | **Yes** — both units **accept-with-gaps** on A, B, and report plate |
| Terminal false honesty | **Yes** — no runtime Confirmed claimed |
| OWN scope discipline | **Yes** — only `0x0051b720` / `0x005749d0`; no parent ledger edits claimed |
| Tool discipline | **Yes** — decompile + `read_memory` + callers; **no** `disassemble_bytes` |
| Decompiler artifact honesty | **Yes** — void return and CONCAT31 explicitly corrected from bytes |
| Odd behavior preserved | **Yes** — cursor `+1` after large window; AL bool over decompiler noise |
| Clean sources meaningful names | **Yes** — not Ghidra iVar paste |
| AutoCore port notes | **Accurate** — RNG host method + crew membership; mode 0xb wiring; sibling distinctions |

### Independent overall verdict

| Unit | Dual verdict | ADV verdict |
|---|---|---|
| `aa_0051b720` Rng_CopyUnitTableWindow_Inferred | accept-with-gaps | **PASS — keep accept-with-gaps** |
| `aa_005749d0` VehicleCrew_ContainsMember_Inferred | accept-with-gaps | **PASS — keep accept-with-gaps** |

**No sealed-claim failures found.** No dual rewrite required. Product English and runtime remain open by design.

---

## Process

- Independent ADV verifier; **not** WQ9R-A author.
- Tools: `decompile_function`, `read_memory`, `get_function_by_address`, `get_function_callers`, `get_function_xrefs`, `analyze_function_complete` only.
- **No** `disassemble_bytes`. **No** parent ledger edits. **No** Launcher.
- Output: `docs/reconstruction/reviews/ADV_wq009_spotcheck_0051b720_005749d0.md` (this file).

Absolute root: `C:\Users\josh\Documents\GitHub\AutoCore\`
