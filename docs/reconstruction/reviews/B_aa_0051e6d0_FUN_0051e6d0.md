# Review B (skeptical / adversarial): `aa_0051e6d0` FUN_0051e6d0

| Field | Value |
|---|---|
| **Stable ID** | `aa_0051e6d0` |
| **VA** | `0x0051e6d0` |
| **Canonical name** | `FUN_0051e6d0` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_0051e6d0_FUN_0051e6d0.md` |
| **System** | `skills-abilities` (hash insert helper) |
| **Verdict** | **accept-with-gaps** |

---

## 1. Claims under attack

| # | Claim | Attack | Result |
|---|---|---|---|
| 1 | This is skill materialize / `ResolveSkillTargets` | Body only allocates **hash node** and links; never builds skill blob | **Falsified** — pure insert |
| 2 | Soft flag means multi-value same key | Soft path returns **1** without linking a second node; hard path fails on hit | **Falsified** multi-insert — soft-skip only |
| 3 | Soft flag `0` means replace existing | Flag 0 skips soft arm; existing key → **E_FAIL** log | **Falsified** replace |
| 4 | Locked traversal aborts insert | Logs then continues to lookup/alloc | **Falsified** hard abort — warn-only |
| 5 | Same function as `CNDHash_Insert` `0x0053c560` | CF matches; node vtbl **`009cb968` ≠ `009cefd4`**; different call sites | **Falsified** identity — **twin**, not alias |
| 6 | Alias of mission twins `0053c360` / `0053c660` | Different VAs/vtbls; this one owned by skill tree path | **Falsified** merge |
| 7 | `FUN_0053ada0` is `operator_new` | Freelist slabs (`_aligned_malloc(700)`, stride 0x1c); ECX = **`this+0x20`** | **Falsified** plain new |
| 8 | Freelist offset unknown / global | Body `LEA ECX,[EDI+0x20]` before alloc | **Attack fails** — sealed `+0x20` |
| 9 | `FUN_00537d30` grows / rehashes | Marks touch flags on bucket chain; repair on re-touch | **Falsified** grow — integrity only |
| 10 | Named `Named_VOG_DEBUG_STOP_0051e6d0` is product identity | String is **lock-warn path only**; insert role is hash insert | **Falsified** product name from that string |
| 11 | Multiple insert call sites in skill code | Static xrefs: **one** call from EnsureLoadedInTree | **Agree** sole static caller |
| 12 | EnsureLoaded depends on soft success return 1 | Caller passes flag **0** after its own miss lookup | **Agree** soft arm unused at known site |
| 13 | Returns skill* | Returns HRESULT-style int; EnsureLoaded ignores return | **Falsified** pointer return |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| Insert role + HRESULTs | **Confirmed** | Wrong null/dup handling |
| Soft vs hard duplicate policy | **High** | Multi-insert bug if misread flag name |
| Hash fields +8/+c/+10/+14/+18/+1d/+20 | **High** (`+0x20` Confirmed) | Corrupt freelist / wrong offsets |
| Node layout 0x1c + vtbl `009cb968` | **High** | Wrong dtor / free path |
| Twin ≠ `CNDHash_Insert` / mission inserts | **High** | Merge vtbls → wrong node methods |
| Sole EnsureLoaded caller (static) | **Confirmed** | Over-generalize ownership |
| Node vtbl method table contents | **Open** | Value free ownership unknown |
| Product English name | **Open** | Keep FUN |
| Runtime | **Open** | No live insert capture |

---

## 3. Cross-check against raw / live / siblings

```
raw/clean CF ≡ live decompile (2026-07-29).

Body seals (read_memory 0x0051e6d0):
  ret 0x0C                          // 3 stack args
  B8 03 40 00 80                    // 0x80004003 null value
  B8 05 40 00 80                    // 0x80004005 hard dup
  B8 01 00 00 00                    // soft hit return 1
  8D 4F 20 ; call FUN_0053ada0      // freelist this+0x20
  C7 06 68 B9 9C 00                 // node vtbl = 0x009cb968

Xrefs:
  0051a9cd  Skill_EnsureLoadedInTree  — tree=owner+0x70, flag=0

Sibling CNDHash_Insert 0x0053c560:
  same CF; vtbl 0x009cefd4; freelist also +0x20 pattern; different callers
```

### Soft-flag trap

Do **not** implement flag as “allow second node with same key” or “replace value.” Policy:

| softIfExists | key present | Result |
|---|---|---|
| 0 | yes | `0x80004005` + log (no mutation) |
| ≠0 | yes | **return 1**, map unchanged |
| * | no | insert, return 0 |

Known caller passes **0**.

### Twin / merge trap

Do **not** redirect EnsureLoaded’s insert to `CNDHash_Insert` (`0x0053c560`) or mission twins. Control flow matches; **node vtbl differs** — skill-tree nodes use `009cb968`.

### Allocator trap

Do **not** `operator_new` these nodes. Pool is freelist at **`hash+0x20`**, slabs of 700 bytes, node stride **0x1c**.

### EnsureLoaded ownership

On success, **tree owns** the skill runtime pointer stored as node value. EnsureLoaded does not free on insert success. Do not free after insert.

---

## 4. Surviving contract for AutoCore

```
// Twin insert — skill-tree node family (vtbl 0x009cb968)
int HashInsert_SkillNode(CNDHash *hash, uint key, void *value, char softIfExists)
{
  if (value == null) return 0x80004003;
  if (hash->lockedForTraversal /* +0x1d */) { log lock; /* continue */ }
  if (softIfExists && Lookup(hash, key)) return 1;
  if (Lookup(hash, key)) { log dup; return 0x80004005; }
  Node *n = FreelistPop(hash + 0x20);   // FUN_0053ada0
  n->vtbl = 0x009cb968;
  n->value = value; n->key = key;
  bucket_head_insert(...); list_append(...); count++;
  return 0;
}

// Known site:
//   Skill_EnsureLoadedInTree miss → materialize → HashInsert_SkillNode(tree, skillId, skill*, 0)

AutoCore must NOT:
  - treat soft flag as multi-map or replace
  - abort on lock without continuing
  - merge with CNDHash_Insert / mission twin VAs
  - operator_new skill-hash nodes
  - free skill runtime after successful insert (tree owns)
  - invent product name from VOG_DEBUG_STOP string alone
```

---

## 5. CF challenge of Review A

| A claim | B position |
|---|---|
| Signature thiscall key/value/flag → int, ret 0x0C | **Agree Confirmed** |
| HRESULTs E_POINTER / E_FAIL | **Agree Confirmed** |
| Soft-skip semantics | **Agree High** |
| Lock warn-only | **Agree High** |
| Layout + freelist `+0x20` | **Agree Confirmed** (B stronger on freelist seal) |
| Node vtbl `009cb968` | **Agree Confirmed** |
| Twin of `CNDHash_Insert`, not same VA | **Agree High** |
| Sole EnsureLoaded static caller | **Agree Confirmed** |
| Product name Open | **Agree** |
| clean ≡ raw ≡ live CF | **Agree** (presentation lag only) |
| accept-with-gaps | **Agree** |

Disputes: none on sealed CF. Naming: reject scaffold alias `Named_VOG_DEBUG_STOP_0051e6d0` as product identity.

---

## 6. Open questions

1. Product rename batch for this twin insert family.
2. Method table at `0x009cb968` (dtor / free / value ownership on remove).
3. Any dynamic dispatch into this VA beyond EnsureLoaded.
4. Live skill-tree insert capture after materialize.
5. Bit-exact vs retail.

**Verdict:** **accept-with-gaps** — agree with A; block multi-insert misread, twin merge with `0x0053c560`, plain-malloc nodes, and VOG_DEBUG_STOP-as-name.
