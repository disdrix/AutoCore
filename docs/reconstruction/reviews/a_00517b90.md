# Residual scratch: `aa_00517b90` Skill_ClearActiveCastCounterAndQueueId

**Date:** 2026-07-29  
**Tools:** Ghidra `decompile_function`, `disassemble_function`, `get_function_callers`, `get_assembly_context`, `get_function_pcode`, pair-filter decomp of `FUN_005169c0` / `Skill_QueueDeferredCastId`  
**Ownership:** this VA only — no WORK_QUEUE / RESUME / Launcher

---

## Sealed facts

1. **Entry:** `MOV ESI,ECX` — owner `this` required (thiscall-shaped). `RET 0x4` cleans `nSkillId`.
2. **Counter gate:** `CMP dword ptr [EAX+0x10], 1` / `JC` → skip if counter **&lt; 1**.
3. **Mutations on live path:** re-get slot; if `+0x10 != 0`: `+0x10 -= 1`; if result 0: `+0xC = 0`; set `BL = 1`.
4. **Always (both paths):** `PUSH &nSkillId`; `FUN_005169c0(this)` → `ECX = slot`; `Skill_QueueDeferredCastId`.
5. **Return:** `MOV AL,BL` — was-counter-live.
6. **Callees only:** `FUN_005169c0`, `Skill_QueueDeferredCastId`.
7. **Callers (4):** ApplyStatusEffectLocal @ `0x0051ac5e`; QuickBarActivateSkillSlot @ `0x00921cdf`; RequestCastSkill @ `0x0094170d`; StanceOrGadgetActivatePath @ `0x0093a501`.
8. **Return unused:** none of the four sites `TEST AL` / branch on AL; cast-again gates use skill **`+0x61c`** after the call.
9. **Decomp noise:** third call shown as `FUN_005169c0(piVar4); Queue(piVar4)` — asm proves slot→ECX, `&nSkillId` stack.

## Gaps

- Product type of ECX owner; English names for +0xC / ring fields.
- Incrementer(s) of +0x10.
- Runtime / bit-exact verification.
- Name accuracy (“Clear” vs decrement) — keep canonical unless rename wave.

## Files touched this residual

- `reviews/A_aa_00517b90_Skill_ClearActiveCastCounterAndQueueId.md`
- `reviews/B_aa_00517b90_Skill_ClearActiveCastCounterAndQueueId.md`
- `functions/aa_00517b90_Skill_ClearActiveCastCounterAndQueueId.md`
- `reviews/a_00517b90.md` (this)
- `raw/aa_00517b90_Skill_ClearActiveCastCounterAndQueueId.annotated.md` (strengthen notes)
- `reconstructed-exact/Skill_ClearActiveCastCounterAndQueueId.cpp` (plate + body CF)
