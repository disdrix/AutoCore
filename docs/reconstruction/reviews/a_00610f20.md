# Dual residual: `0x00610f20` Deliver CargoReady

| Field | Value |
|-------|-------|
| Stable ID | `aa_00610f20` |
| Address | `0x00610f20` |
| Owner | dual residual only (no WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG) |
| Date | 2026-07-29 |
| Prior dual | A/B thin accept-with-gaps — open: **`+0x23` polarity / source**, stack ABI, vehicle-null, name |

## Residual questions (from dual A/B + function record)

1. Exact polarity of `req+0x23` in the qty OR (skip-when-set vs enforce-when-set)
2. Where `+0x23` is authored (XML field vs derived)
3. Stack ABI (`RET n`, matchKey vs character formals)
4. Cargo host path vs Collect (`char+0x250 → +0x2b0`)
5. Count thiscall ECX / `includeBroken`
6. Vehicle `+0x250` null safety (raw vs clean)

## Live evidence this pass

| Source | Result |
|--------|--------|
| Ghidra `decompile_function` @ `0x00610f20` | Matches frozen raw (match `+0x18`, cargo gate, Count, qty OR `+0x23==0`) |
| Ghidra `read_memory` body `0x00610f20`..`0x00610f61` | Full asm seal (below); trailing `CC` pad |
| Ghidra `get_xrefs_to` | Sole xref **DATA** `0x009e0f5c` = Deliver vtable **`PTR_FUN_009e0f24 + 0x38`** |
| Ghidra `get_function_callers` | None (virtual only) |
| Deliver ctor `0x00610bc0` (`read_memory`) | type **3**; `+0x20..+0x23 = 1`; CBID/NPC/continent **−1**; Num **0**; vtable `009e0f24` |
| Deliver XML unserialize region `0x00610f70+` | `CBIDItem` → `+0x10`; if value **== −1** then **`+0x23 = 0`** (BL from `xor ebx,ebx`); else leave ctor **1** |
| GetFlags dump `0x00610c40` | dumps slot, CBIDItem, NumToDeliver, TargetNPCCBID, ContinentID, GiveItemAtStart (`+0x20`), TakeItemAtEnd (`+0x21`), NPCTargetCompletes (`+0x22`) — **no `+0x23` dump** (not a standalone XML tag) |
| Sibling OnInventoryEvent `0x00611290` | identical qty OR form on `+0x23` |
| Count residual `a_005711c0.md` | includeBroken **0** sealed; this caller listed |

**Not performed:** `disassemble_bytes`, Launcher, live CE, bit-exact image diff.

---

## Seal: stack ABI

```text
bool/uint8 __thiscall CVOGObjectiveRequirement_Deliver_CargoReady(
    Requirement* this,       // ECX → ESI
    int          matchKey,   // stack0  compared to *(this+0x18)
    Character*   character); // stack1  cargo host
// RET 8  (both exits: fail @ 0x00610f59, ready @ 0x00610f5f)
```

| Prior decompiler artifact | Asm reality | Confidence |
|---------------------------|-------------|------------|
| `undefined4` return | **`AL` only** (`xor al,al` / `mov al,1`); upper bits undefined | **High** |
| Three formals, thiscall | ECX + **two stack dwords**, `RET 8` | **High** |
| Count shows only (cbid, flag) | **ECX = cargo grid** thiscall into `0x005711c0` | **High** |

---

## Seal: control flow (asm)

```text
PUSH ESI; ESI = ECX (req)
if (*(ESI+0x18) != matchKey)           // [ESP+8] after push
    → fail0
ECX = character                        // [ESP+0xC]
EDX = *(ECX+0x250)                     // vehicle / cargo host — **no null test**
ECX = *(EDX+0x2b0)                     // InventoryGrid*
if ECX == 0 → fail0

PUSH 0; PUSH *(ESI+0x10); CALL CountItemsByCbid   // includeBroken=0; ECX=grid
if EAX >= *(ESI+0x14)                  // count >= need  (JGE)
    → ready1
if *(byte*)(ESI+0x23) == 0             // CMP byte,0 / JE ready
    → ready1
fail0: XOR AL,AL; POP ESI; RET 8
ready1: MOV AL,1; POP ESI; RET 8
```

Image bytes (`read_memory`):

```text
56 8B F1 8B 46 18 3B 44 24 08 75 2A 8B 4C 24 0C 8B 91 50 02 00 00
8B 8A B0 02 00 00 85 C9 74 16 8B 46 10 6A 00 50 E8 75 02 F6 FF
3B 46 14 7D 0C 80 7E 23 00 74 06 32 C0 5E C2 08 00 B0 01 5E C2 08 00
```

Call target: `E8` at `0x00610f46` → `0x005711c0` (`InventoryGrid_CountItemsByCbid`).

### Predicate (boolean)

```
ready =
  (req.matchKey(+0x18) == matchKey)
  && (cargoGrid != 0)
  && ( count(CBID) >= need  ||  req[+0x23] == 0 )
```

No side effects (no progress write, no toast, no inventory mutate).

---

## Seal: `+0x23` polarity (**require-count / enforce-qty**)

| Claim | Status | Evidence |
|-------|--------|----------|
| `+0x23 == 0` **bypasses** quantity only | **Sealed High** | `JE ready` after `CMP byte [ESI+0x23], 0` when count &lt; need |
| `+0x23 != 0` **enforces** `count >= need` | **Sealed High** | fall-through to `fail0` when flag non-zero and short |
| Default ctor value **1** (enforce) | **Sealed High** | ctor `0x00610bc0`: `mov al,1; mov [esi+0x23], al` |
| **Not** “skip-when-set” | **Sealed High** | non-zero enforces; zero skips qty |
| Same OR form as OnInventoryEvent | **Sealed High** | sibling `0x00611290` raw: `(need <= count) \|\| (+0x23 == 0)` |

### Authorship (not a free-standing XML tag)

| Path | Behavior | Confidence |
|------|----------|------------|
| Ctor | `+0x23 = 1` | **High** |
| XML `CBIDItem` parse | store `+0x10`; **if EAX == −1 then `+0x23 = 0`** (BL=0); else leave **1** | **High** (image at ~`0x00611070`: `83 F8 FF 89 46 10 75 03 88 5E 23`) |
| GetFlags dump | dumps `+0x20/+0x21/+0x22` bools; **no** dump of `+0x23` | **High** |
| English role | **require-count / enforce-qty** when non-zero; CBID **−1** means “no item gate” | **High** (behavior); original symbol unknown |

**Plate name (inferred):** `bRequireItemCount` / `RequireItemToComplete` — keep **Inferred**. Do not invent PDB.

### AutoCore server cross-check (parity gap — not owned)

`ObjectiveRequirementDeliver.UnSerialize` sets `RequireItemToComplete = (ItemCBID == -1)` after reading `CBIDItem`. That is **inverted** vs client `+0x23` (client: CBID **−1 → flag 0**). Server field is currently **unused** outside UnSerialize/tests. Flag as **parity residual**; do not rewrite server under this OWN.

---

## Seal: field map (Deliver requirement object)

| Offset | Role | Default (ctor) | Confidence |
|-------:|------|----------------|------------|
| `+0x0c` | Requirement type code | **3** | **High** |
| `+0x10` | Item CBID (`CBIDItem`) | **−1** | **High** |
| `+0x14` | Need (`NumToDeliver`) | **0** | **High** |
| `+0x18` | Match key (`TargetNPCCBID` / event id) | **−1** | **High** |
| `+0x1c` | Continent id | **−1** | **High** |
| `+0x20` | `GiveItemAtStart` | **1** | **High** |
| `+0x21` | `TakeItemAtEnd` | **1** | **High** |
| `+0x22` | `NPCTargetCompletes` (also toast enable in OnInventoryEvent) | **1** | **High** |
| `+0x23` | **require-count** (enforce qty when ≠0) | **1**; forced **0** if CBIDItem **−1** | **High** |

Cargo path (same as Collect / Drop type-1):

```
character +0x250 → vehicle/host
host +0x2b0 → InventoryGrid*
```

---

## Seal: vtable + identity

| Item | Value | Confidence |
|------|-------|------------|
| Vtable base | `PTR_FUN_009e0f24` | **High** |
| This method slot | **`+0x38`** → `0x00610f20` (xref `0x009e0f5c`) | **High** |
| OnInventoryEvent slot | **`+0x04`** → `0x00611290` | **High** |
| RTTI string | `".?AVCVOGObjectiveRequirement_Deliver@@"` @ `0x00afe97c` | **High** (family) |
| Method name | `…_CargoReady_Inferred` | **Probable** (pure readiness predicate role) |
| Class name Deliver | type **3** + RTTI + XML `type="deliver"` | **High** |

---

## Clean-source divergence (document only)

`reconstructed-exact/CVOGObjectiveRequirement_Deliver_CargoReady.cpp` adds `if (vehicle == 0) return 0` before `+0x2b0`. **Raw image does not null-check `+0x250`** — only the grid pointer. Defensive modernization; not bit-identical.

---

## Residual after this pass

| Item | Status |
|------|--------|
| `+0x23` polarity | **Sealed High** — 0 bypass qty; ≠0 enforce |
| `+0x23` authorship | **Sealed High** — derived from `CBIDItem == -1`, not separate tag |
| Stack ABI `RET 8` | **Sealed High** |
| Virtual-only call | **Sealed High** (DATA xref only) |
| Count includeBroken=0 | **Sealed High** |
| Original PDB method name | Open (`_Inferred`) |
| Who dispatches vtbl+0x38 (call sites of virtual) | Open (virtual; no static callers) |
| Semantic of matchKey beyond int equality | Open (NPC/target id family; OnInventoryEvent uses event float cast) |
| Server `RequireItemToComplete` polarity | **Parity gap** (inverted vs client) |
| Runtime / bit-exact / image diff | Open (matrix policy) |

## Artifacts touched by residual owner

- This scratch: `docs/reconstruction/reviews/a_00610f20.md`
- Duals: `A_aa_00610f20_*`, `B_aa_00610f20_*`
- Function record: `functions/aa_00610f20_CVOGObjectiveRequirement_Deliver_CargoReady.md`
- Annotated: `raw/aa_00610f20_CVOGObjectiveRequirement_Deliver_CargoReady.annotated.md`
- VERIFICATION_MATRIX open-issues cell for `aa_00610f20` only
- **Not** WORK_QUEUE / RESUME / ACTIVE_WORK / CHANGE_LOG
- **Not** Launcher / sibling unit rewrites (OnInventoryEvent / Count duals cross-ref only)
