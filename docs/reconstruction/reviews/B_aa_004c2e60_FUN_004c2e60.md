# Review B (skeptical / adversarial): `aa_004c2e60` FUN_004c2e60

| Field | Value |
|---|---|
| **Stable ID** | `aa_004c2e60` |
| **VA** | `0x004c2e60` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` |
| **Counterpart** | `reviews/A_aa_004c2e60_FUN_004c2e60.md` |
| **Verdict** | **accept-with-gaps** |

---

## 1. Claims under attack

| # | Claim | Attack | Outcome |
|---|---|---|---|
| 1 | Mutates cast / skill state | Body: vcall + float math + ret | **Falsified** |
| 2 | `__fastcall` with free ECX param (decomp) | ECX is **this**; no stack formal | **Confirmed risk in decomp label** — treat as **thiscall** |
| 3 | Addend is “level-up UI base” | Constant is **`DAT_00a10e74` = 2.0**; shared physics default name pollution | **Survives as mislabel** — value sealed 2.0 |
| 4 | Round bias is 0.5 exactly | `DAT_00aaa6d0` ≈ **0.5001** | **Survives** — not pure 0.5 |
| 5 | Scale is arbitrary | **1/14** exact float `0x3d924925` | **Falsified as arbitrary** |
| 6 | Only FormatFailure caller | Xrefs include `FUN_006170b0` | **Falsified exclusive** |
| 7 | Returns character level raw | Formula `levelish*(1/14)+2` | **Falsified as raw level** — returns **derived cap** |
| 8 | Clean invents behavior | Clean ≡ raw ≡ live | **No invention** |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| thiscall + vtbl+0x27c | **Confirmed** | Wrong AutoCore this |
| Formula `i/14 + 2` then round | **Confirmed** | Wrong summon max toast |
| Constant addresses/values | **Confirmed** | Port float mismatch |
| “max summons” product role | **High** (caller) | Misnamed port comment only |
| vtbl+0x27c product | **Tentative** | Wrong input domain |

---

## 3. Cross-check assembly

```
004c2e60  mov eax,[ecx+4]
          mov edx,[eax+4]
          mov eax,[edx+ecx+4]
          lea ecx,[edx+ecx+4]
          call [eax+0x27c]          ; vcall
          xorps xmm1,xmm1
          cvtsi2ss xmm0,eax
          mulss xmm0,dword [0xa110e8]  ; 1/14
          addss xmm0,dword [0xa10e74]  ; 2.0
          comiss xmm1,xmm0
          jbe  +bias
          subss xmm0,dword [0xaaa6d0]  ; ~0.5001
          cvttss2si eax,xmm0
          ret
+bias:    addss xmm0,dword [0xaaa6d0]
          cvttss2si eax,xmm0
          ret
```

Live 2026-07-29 decompile ≡ frozen raw (minus label quality).

---

## 4. Surviving contract for AutoCore

```
// FormatFailure case 0x10:
maxCap = FUN_004c2e60(characterCtx);   // ≈ round(vtbl27c()/14 + 2)
// Compare to FUN_004c3f10 count; do not treat return as raw player level.
// Pure; leave product name FUN_* until string/RTTI.
```

---

## 5. Open questions

1. Confirm vtbl+0x27c against a named character level getter elsewhere.
2. Design doc for base-2 / per-14 level bands.
3. Why ~0.5001 not 0.5 (MSVC artifact vs intentional).

**Verdict:** **accept-with-gaps** — adversarial pass seals CF/constants; product naming + vcall remain open.
