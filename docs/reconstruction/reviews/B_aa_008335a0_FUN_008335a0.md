# Review B (skeptical / adversarial): `aa_008335a0` FUN_008335a0

| Field | Value |
|---|---|
| **Stable ID** | `aa_008335a0` |
| **Review type** | Skeptical / adversarial |
| **Date** | `2026-07-29` (OWN-ONLY W27-A) |
| **Counterpart** | `reviews/A_aa_008335a0_FUN_008335a0.md` |
| **Scratch** | `tmp/a_008335a0.md` |
| **Verdict** | **accept** |

---

## 1. Claims under attack

| # | Claim | Attack / result |
|---|---|---|
| 1 | This is pure thiscall with ECX live inside body | **Partially careful** — caller moves ECX→EBX; body uses **EBX**, not ECX, as host |
| 2 | Early-out compares position not quaternion | **Falsified** — 4 dwords from `+0x590`, sealed `FUN_00416260` quat helper, EDI=`+0x5e0` |
| 3 | Flag `@+0x5dc` is set here on failure | **Falsified** — body only **clears** to 0 on success; caller tests non-zero before call |
| 4 | Slerp t is 1.0 / instantaneous snap | **Falsified** — imm `0x3E800000` = **0.25f** into `FUN_0076e780` |
| 5 | No double-cover — only positive q≈q | **Falsified** — callee is sealed double-cover (W26-A); this unit only supplies tols |
| 6 | Product name is retail string | **Overstated** — no string evidence; keep `FUN_` |
| 7 | `FUN_0040d1a0` writes to host+0x590 directly as this | **Careful** — ECX is `host+0x510`; writer uses `this+0x80` → absolute `+0x590` |

---

## 2. Confidence table (adversarial)

| Area | Confidence | Risk if wrong |
|---|---|---|
| EBX host + flag CF | **High** | Wrong enable/clear semantics |
| Quat nearness gate + 0.002f | **High** | Orientation thrash / spin |
| Slerp t=0.25 path | **High** | Wrong interpolation rate |
| Sibling workers | **Medium** | Incomplete side effects |
| Product English | **Low** | Naming only |
| Runtime | **Open** | Matrix policy |

---

## 3. Cross-check against raw + bytes + caller

```
// FUN_00833a30:
EBX = ECX (this)
if byte[EBX+0x5dc] != 0: CALL FUN_008335a0

// FUN_008335a0:
copy [EBX+0x590..59c] → stack
ESI=stack, EDI=[EBX+0x5e0], push 0.002f×2
if Math_QuatNearlyEqual_DoubleCover: [EBX+0x5dc]=0; ret
else: slerp t=0.25; write orient; siblings
```

Live decompile 2026-07-29 ≡ raw 2026-07-23. Full body hex matches call immediates and offsets.

---

## 4. Surviving contract for AutoCore

```
// Port as host-driven orientation step:
if pending_flag:
  if QuatNearlyEqualDoubleCover(cur, target, 0.002, 0.002):
    pending_flag = false
  else:
    cur = Slerp(cur, target, 0.25)
    WriteOrient(cur)
    // plus sibling side effects
```

---

## 5. Verdict

Adversarial pass **confirms** A: ABI/CF/sealed quat gate sealed; residual naming + sibling roles only → **accept**.
