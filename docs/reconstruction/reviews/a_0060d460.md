# Residual scratch: `aa_0060d460` MatchTargetCore

| Field | Value |
|---|---|
| **Stable ID** | `aa_0060d460` |
| **Date** | `2026-07-29` |
| **Role** | Byte-level residual for dual A/B |

## Bytes sealed

- Entry: `push ebx; mov ebx,[esp+8]; push esi; mov esi,ecx` → this=req, stack0=char.
- After second push: `mov eax,[esp+10]` → target.
- Grid: `mov ecx,[ebx+0x250]`; `mov edi,[ecx+0x2b0]`.
- FindByCoid: `push hi; push lo; mov ecx,edi; call 0x00571010`.
- Invert: `movzx edx,byte [esi+0x1d]`; `setz cl`; `xor ecx,edx`; `jnz fail`.
- FindByCbid: `push 0; push 0; push [esi+0x2c]; mov ecx,edi; call 0x005710c0`.
- Success: `mov al,1; pop edi; pop esi; pop ebx; ret 8`.
- Fail: `xor al,al; …; ret 8`.

## Open residual

- Product name of character `clonebase+0xFC` vs `req+0x54`.
- Runtime mission capture.
