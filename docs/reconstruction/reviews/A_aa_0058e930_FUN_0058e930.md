# Review A (reconstruction fidelity): `aa_0058e930` FUN_0058e930

| Field | Value |
|---|---|
| **Stable ID** | `aa_0058e930` |
| **VA** | `0x0058e930` |
| **Canonical name** | `FUN_0058e930` |
| **Review date** | `2026-07-29` |
| **Reviewer role** | Independent reconstruction review |
| **Counterpart** | `reviews/B_aa_0058e930_FUN_0058e930.md` |
| **System** | client object — bit flag notify from FX host list `+0x204` |
| **Verdict** | **accept** — bit2 set/clear + optional counter bump **sealed** |

---

## 1. Purpose

**Second notify** from `FUN_004b8dc0` list walk on host **`+0x204`**.

1. Set/clear **bit 2 (`0x04`)** of **`this+0x90E`** from `param_2` (boolean as 0/1, then `*4` to form the bit mask via XOR-select idiom).
2. If **`this+0x90F & 2`** and **`this+0x91C != 0`**:
   - Resolve `iVar2 = *(this+0x91C)+0x8C`.
   - Increment counter at **`iVar2+0xDC`**.
   - Store **`*(char*)(iVar2+0xD0) = param_2`**.

---

## 2. Inspected artifacts

| Artifact | Path / source |
|---|---|
| Raw | `docs/reconstruction/raw/aa_0058e930_FUN_0058e930.md` |
| Annotated | `docs/reconstruction/raw/aa_0058e930_FUN_0058e930.annotated.md` |
| Clean | `docs/reconstruction/reconstructed-exact/FUN_0058e930.cpp` |
| Function record | `docs/reconstruction/functions/aa_0058e930_FUN_0058e930.md` |
| Live decompile | Ghidra `batch_decompile` `0x0058e930` — ≡ raw |
| Live bytes | Ghidra `read_memory` `0x0058e930` length 48 |
| Callers | `FUN_004b8dc0` list `+0x204` |

---

## 3. Sealed residuals

### 3.1 ABI — **SEALED**

| Fact | Evidence |
|---|---|
| Convention | **`__thiscall`**: `this` in **ECX** |
| Args | **`char flag`** (`param_2`) |
| Bit op | `flag*4` then XOR/AND to insert bit2 of `+0x90E` |

```c
void __thiscall FUN_0058e930(void *this, char flag);
```

Equivalent bit semantics:

```c
// set bit2 of +0x90E to (flag ? 1 : 0)
*(byte*)(this+0x90E) =
    (*(byte*)(this+0x90E) & ~4) | ((flag ? 1 : 0) << 2);
// decompiler form: x ^ (flag*4 ^ x) & 4  ≡ same
```

### 3.2 Algorithm — **SEALED**

```
set/clear bit2 of this+0x90E from flag
if ((this+0x90F & 2) && this+0x91C != 0):
  p = *(this+0x91C) + 0x8C
  *(p+0xDC) += 1
  *(char*)(p+0xD0) = flag
return
```

### 3.3 Layout fields — **SEALED**

| Off | Role |
|---|---|
| `+0x90E` bit2 | **active/notify bit** driven by host |
| `+0x90F` bit1 | enables secondary counter path |
| `+0x91C` | pointer to related object (null-checked) |
| `*91C+0x8C` | base for counter/flag writes |
| `base+0xD0` | mirror of `flag` |
| `base+0xDC` | **incrementing counter** (event stamp) |

### 3.4 Entry machine bytes — **SEALED**

```
8A 54 24 04             mov  dl, [esp+4]      ; flag
8A C2                   mov  al, dl
02 C0                   add  al, al
02 C0                   add  al, al           ; al = flag * 4
32 81 0E 09 00 00       xor  al, [ecx+0x90E]
24 04                   and  al, 4
30 81 0E 09 00 00       xor  [ecx+0x90E], al  ; deposit bit2
F6 81 0F 09 00 00 02    test byte [ecx+0x90F], 2
74 1D                   jz   → ret
8B 89 1C 09 00 00       mov  ecx, [ecx+0x91C]
85 C9                   test ecx, ecx
74 13                   jz   → ret
8B 81 8C 00 00 00       mov  eax, [ecx+0x8C]
...
```

Hex prefix:  
`8a5424048ac202c002c032810e090000240430810e090000f6810f09000002741d8b891c09000085c974138b818c0000`

---

## 4. Control flow: clean ≡ raw ≡ live

| Stage | Match |
|---|---|
| bit2 deposit on `+0x90E` | Yes |
| optional counter/flag via `+0x91C` | Yes |

---

## 5. Gaps / open

1. Product type of `this` and of `*(+0x91C)`.
2. Counter semantic (generation / dirty / pulse count).
3. Runtime verification not run.

**Verdict:** **accept**
